'use strict';
/* 작업 948 — 안내문 «위:아래» 여백 비의 **과녁과 대역, 한 곳**.
 *
 * 이 값을 부르는 자가 다섯이다 — `verify813`[3] · `verify887`[4][5][6][R] ·
 * `verify905`[6][7][R] · `verify920`[1][2][4][5] · `probe920`.
 * 905 까지는 다섯이 **각자 손으로** 같은 두 수를 적고 있었고, 932 7회차가 자를 부분 화소로
 * 갈자 그 사본들이 한꺼번에 낡았다(402 «사본을 지운다» · 125 H2 «네 번째 부패였을 자리»).
 * ⇒ 값은 여기 한 곳에 있고, 다섯은 읽기만 한다.
 *
 * ── 과녁은 왜 0.750 이 아니라 0.7338 인가 ──────────────────────────────────
 * 0.750 은 **정수 두 개의 비**다(ref 위 12 : 아래 9 ref px). 정수 걸음은 안티에일리어싱
 * 분수를 통째로 버리는데 레퍼런스의 끝점은 격자 위에 있지 않다(밑판 +0.101). 그리고 그
 * 버림이 우리 다섯 프레임 중 **넷을 한 칸(0.750)에 몰아넣어** 1600 과의 실재하는 4.8%
 * 갈림을 지운다. 부분 화소로 재면 ref 0.7338 · 1600 0.7393 · 나머지 넷 0.7748 이다.
 *   (문턱 110 · 끝점 U3(위 물체의 마지막 칠해진 행)/B3(테두리 조립체 최상단) — 905 규약)
 *
 * ── 대역은 왜 ±9.6% 인가 (이것이 948 의 본체다) ────────────────────────────
 * 옛 대역 [0.67, 0.83] 의 근거는 «±1 눈금»(= ref 정수 격자의 한 칸)이었는데,
 * 부분 화소 축에는 그 칸이 없다 — 근거가 축과 함께 사라졌다(등재문 948).
 * 새 근거는 **자 자신의 재현성을 실제로 잰 값**이다: `probe948` [4] 가 `#relw` 를 통째로
 * δ px 내려(위·아래 두 끝점이 같이 내려가므로 **참 여백은 Δ0**) 위상만 바꾸면,
 * 같은 기하가 **0.7258 ~ 0.8667** 로 읽힌다 — 진폭 0.1409 = 과녁의 **19.2%**.
 * 흔들리는 자리는 «테두리 조립체 최상단»(B3)이 압도적이고(δ 를 뺀 값이 2.24px 이동),
 * 위상이 반 화소를 넘으면 안티에일리어싱된 앞줄이 «칠해진 행» 문턱을 넘어 **끝점이 한 행
 * 통째로 건너뛴다**. ⇒ 이보다 좁은 대역은 «결함» 이 아니라 **자의 흔들림**을 잡는다.
 *   대역 = 과녁 × (1 ± 진폭/과녁/2) = 과녁 ± 9.6% = [0.663, 0.804].
 * ⚠ 이 폭은 **줄어들 수 있는 값**이다 — 끝점 자가 위상에 둔감해지면 `verify948` [3] 이
 *   먼저 빨개지고(실측 진폭 ↔ 심긴 PHASE_AMP 불일치), 그때 대역을 좁힌다.
 *
 * ── 은퇴한 값 (333: 자리를 비우지 않는다) ──────────────────────────────────
 *   0.900 · 대역 0.82~1.00   — 887 이 세우고 905 가 은퇴시켰다(위 끝점 U1 이 두 그림에서
 *                              밑판 외곽선의 반대편을 짚었다)
 *   0.750 · 대역 0.67~0.83   — 905 가 세우고 **948 이 은퇴시켰다**(정수 격자 위에서 세운 값)
 */

/* 레퍼런스 확정값 — 부분 화소 · 문턱 110 · U3/B3 (`scan887.py` · `probe948.js` 가 매 실행 다시 잰다) */
const REF_RATIO = 0.7338;
/* 같은 그림의 정수 걸음 값 — 대조용으로만 쓴다(과녁이 아니다) */
const INT_RATIO = 0.750;
/* `probe948` [4] 위상 스윕의 실측 진폭 — 대역의 유일한 근거다 */
const PHASE_AMP = 0.1409;
/* 정수 걸음도 같은 스윕에서 이만큼 흔들린다 — «정수로 되돌아가면 안정하다» 를 막는 수 */
const PHASE_AMP_INT = 0.1130;

const BAND_HALF = PHASE_AMP / REF_RATIO / 2;                 /* 0.0960 */
const BAND = [+(REF_RATIO * (1 - BAND_HALF)).toFixed(3),     /* 0.663 */
              +(REF_RATIO * (1 + BAND_HALF)).toFixed(3)];    /* 0.804 */

/* 은퇴한 값 — `verify920` [6] 브리핑 위생과 `verify948` [7] 사본 래칫이 읽는다 */
const RETIRED = [
  { ratio: 0.900, band: '0.82~1.00', by: 905 },
  { ratio: 0.750, band: '0.67~0.83', by: 948 },
];

module.exports = { REF_RATIO, INT_RATIO, PHASE_AMP, PHASE_AMP_INT, BAND_HALF, BAND, RETIRED };
