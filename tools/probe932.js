/* 재현·전수 932 — «정수로 세는 자» 가 이 저장소에 또 어디 있는가
 *
 *   node tools/probe932.js                — 갈래별 표
 *   node tools/probe932.js --list R       — 그 갈래의 파일 이름만 (한 줄에 하나)
 *   node tools/probe932.js --json         — 기계가 읽을 꼴
 *   node tools/probe932.js --physics      — 물리 재현(합성 프로파일) 한 표
 *
 * 왜 이 자가 있나 —
 *   895 2회차가 자기 자(`tools/scan895.py` 의 획 걸음)를 «검정 화소를 한 칸씩 세는» 정수에서
 *   **부분 화소**로 갈아 끼웠다. 그때 남긴 교훈이 «7 이 맞다» 가 아니라
 *   **«정수로 세는 자로 ref 를 재면 언제나 우리가 더 두껍게 나온다»** 였고,
 *   «같은 자를 쓰는 축이 더 있는지» 를 이 번호(932)로 넘겼다.
 *
 *   ⚑ 결함의 조건은 셋이 **동시에** 서야 성립한다 —
 *     ① **얇은 축**   참값이 ref 1 px 안팎인 것(획·테두리·틈·경계선). 바닥깎기가 값의 몇십 %다.
 *     ② **비대칭**   ref 의 «가장자리 한 겹» 이 우리 것보다 넓다. 문턱 자는 그 한 겹을 버리므로
 *                     **ref 만** 얇아진다. 비대칭의 뿌리는 둘이고 크기가 다르다 —
 *                       ⓐ **축척** (K=2.0628 · S=2.2222) — 잘라 낸 ref 두 장. ref 1 px = 우리 2.06~2.22 px.
 *                       ⓑ **번짐** (1:1 인 전체 화면 ref) — ref 는 JPEG 사진, 우리는 딱 떨어지는 렌더.
 *                          ⚑ 추측이 아니다: `probe409i.py` `cov_ray()` 주석이 «같은 7px 띠가
 *                          ref 6.0 / cap 7.0» 이라고 적어 뒀다 — **1:1 인데 −14%**.
 *     ③ **정수 걸음** 길이를 «이진 마스크 화소 세기» 로 낸다(합·run-length·EDT·정수 while 걷기).
 *   셋 중 하나만 빠져도 결함이 아니다 — ① 이 없으면 1 px 이 1% 미만이고,
 *   ② 가 없으면 두 쪽이 **같은 만큼** 깎여 상쇄되며, ③ 이 없으면 애초에 안 깎인다.
 *
 * ⚠⚠ **1회차가 처음에 ② 를 «ref 를 읽는가» 로 물었다가 44개를 후보로 잡았다.**
 *   전체 화면 ref 는 1080×2340 · 우리 캡처는 1080×2280 으로 **가로가 1:1** 이라(변환은 y−84 뿐)
 *   축척 편향이 원리적으로 없다 — 그런데 번짐 편향은 남는다. 그래서 ② 를 둘로 갈랐다.
 *
 * ⚠ **이 자는 «판정기» 가 아니라 «선별기» 다.** 소스 글자만 보고는 «그 자가 재는 참값이 얇은가» 를
 *   못 가른다(같은 `.sum()` 이 획에도 쓰이고 판 넓이에도 쓰인다). 그래서 갈래는
 *   **신호(자동) + 판정(손으로 읽어 적은 장부)** 둘로 적는다 —
 *   신호가 바뀌거나 새 자가 생기면 판정이 **다시 열린다**(`verify932.js` [2] 가 그것을 못박는다).
 *   931 이 «갈래를 손으로 적으면 다음에 자가 늘 때 아무도 안 센다» 고 적어 둔 그 함정을
 *   여기서는 «신호는 기계가 · 판정은 사람이, 단 신호가 바뀌면 판정 무효» 로 피한다.
 *
 * 갈래 —
 *   R  빨강   ①∧②ⓐ∧③ — 895 와 **같은 얼굴**(축척 비대칭). 부분 화소로 갈아 끼워야 한다.
 *   B  주홍   ①∧②ⓑ∧③ — 같은 뿌리, 1:1 이라 크기가 작다(저장소 실측 −14%). **942 로 등재**한다.
 *   S  면역   얇은 축을 **부분 화소**로 잰다. 이 저장소의 선례는 둘이고 **서로 다른 처방**이다:
 *              ⓐ 문턱 교차점 선형 보간 — `scan895.py` `stroke_thk()`
 *              ⓑ 검정 «질량» 적분 Σ(1 − L/L_고원) — `scan667c.py` `dark_mass()`
 *              ⓑ 는 AA 화소를 자기 비율만큼만 세므로 «AA 가 있는 ref ↔ 없는 우리» 를 같은 자로 견준다.
 *   A  주황   ②∧③ 이지만 축이 얇지 않다(bbox·피치·중심). 1 px 바닥깎기가 비율로 묻힌다.
 *   N  무관   ① 도 ② 도 아니다(ref 한 장만 열거나, 우리 쪽을 화소로 안 재거나, 길이를 안 낸다).
 */
'use strict';
const fs = require('fs');
const path = require('path');

const TOOLS = __dirname;

/* ── 신호(자동) ─────────────────────────────────────────────────────────────
   넷 다 «소스 글자» 다. 판정이 아니라 **선별**이라는 것을 이름으로 못박는다. */
const SIG = {
  /* ②ⓐ **축척이 다르다** — ref↔우리 환산 배율을 든다. 축척이 다른 ref 는 잘라 낸 두 장뿐이다:
       151-이용권-카드.png 504×709 (K = 2.0628) · 89-유물-팝업.png 486×687 (S = 2.2222)
     전체 화면 ref 는 1080×2340 이고 우리 캡처는 1080×2280 이라 **가로가 1:1** 이다(변환은 y−84 뿐). */
  scale: /151-이용권-카드|89-유물-팝업|2\.0628|2\.2222|978\s*\/\s*474|1080\s*\/\s*486/,
  /* ②ⓑ **가장자리 폭이 다르다** — 축척이 같아도 ref 는 JPEG 사진이라 가장자리 경사면이
     우리 렌더보다 넓다. 문턱 자는 그 경사면을 «검정이 아니다» 로 버리므로 **ref 만** 얇아진다.
     ⚑ 이것은 추측이 아니라 저장소 안의 실측이다 — `probe409i.py` `cov_ray()` 주석이
     «같은 7px 띠가 ref 6.0 / cap 7.0» 이라고 적어 두었다(1:1 인데 −14%). */
  refimg: /docs\/ref\//,
  /* ① 얇은 축 — 얇은 것을 말하는 낱말이 있는가 (있다고 재는 것은 아니다) */
  thin: /획|두께|틈|테두리|외곽선|경계선|hairline|stroke|thk|thick|\bgap\b|border|edge|ring/g,
  /* ③ 정수 걸음 — 길이를 화소 세기로 내는 관용구 */
  intlen: /\.sum\(\)|count_nonzero|np\.ptp|\bptp\(|argwhere|distance_transform_edt|np\.where\(|len\(runs?\b/g,
  /* 부분 화소 관용구 — 교차점 보간 ⓐ 또는 질량 적분 ⓑ */
  frac: /_bilin|np\.interp|map_coordinates|부분 화소|서브픽셀|질량|dark_mass|1 - prof\[|1 - p\[/,
};

function signals(src) {
  return {
    scale: SIG.scale.test(src),
    refimg: SIG.refimg.test(src),
    thin: (src.match(SIG.thin) || []).length,
    intlen: (src.match(SIG.intlen) || []).length,
    frac: SIG.frac.test(src),
  };
}

/* ── 판정(장부) ───────────────────────────────────────────────────────────
   손으로 소스를 읽어 적은 값이다. `sig` 는 **적을 당시의 신호**이고,
   지금 신호가 그것과 다르면 판정은 무효다(= verify932 [2] 가 빨개진다).
   sig 표기: k=축척(0/1) · r=ref 그림(0/1) · t=얇은 말(개수) · i=정수 걸음(개수) · f=부분 화소(0/1) */
const LEDGER = {
  'scan885e.py': {
    v: 'S', sig: 'k1 r1 t23 i8 f1',
    axis: "금색에 붙은 «검정 띠» 두께 ring_thickness (ref 2.83~4.00 ref px) — **932 6회차가 부분 화소로 갈았다**",
    why: "**R → S(932 6회차).** 옛 걸음은 이진 마스크에 distance_transform_edt 를 깔아 거리를 정수 격자의 √합에만 떨어뜨렸다 — ref 두께가 16단에서 {2.83, 3.41, 3.85, 4.00} 네 값에만 앉고 우리 쪽은 16단 **전부 2.24**(=√5)로 굳어 있었다. 처방은 «창 안 그림만 겹선형 4배 → 같은 마스크·같은 문턱 → EDT → ÷SS»(ⓐ 의 2차원 꼴) — 걸음만 1/4 px 이 되고 정의는 그대로다. 실측(우리 px 환산): ref 5.83/7.04/7.95/8.25 → **5.45~7.18** · 우리 4.47 고정 → **4.03~4.86** · Δ 는 −23~−46% → **−23~−35%**(부호·크기 모두 살아 있다).",
    fix: "**없음(면역).** ⚑ 갈고 나서 드러난 두 번째 결과 — 머리말이 손 상수 과녁(1.176/1.231)의 근거로 든 «ref 가 문턱 네 단에서 안 움직인다» 가 **격자였다**(부분 화소로 재면 가로 1.136→1.169→1.199→1.228 · 세로 1.211→1.254→1.293→1.348). ⇒ 과녁을 **같은 단의 ref** 로 바꿔 손 상수를 없앴다(옛 상수 판정은 `--int` 가 되살린다). ⚠ 겹선형 확대는 «닿았다» 의 뜻을 깬다 — 금색↔검정 사이에 둘 다 아닌 경사면 띠가 생겨 `label(cand|g)` 인접이 띠를 통째로 버린다(ring 670 → 0). «금색까지 1 원래-화소 안» 으로 물어야 SS 가 몇이든 같은 것을 센다.",
  },
  'scan885b.py': {
    v: 'S', sig: 'k1 r1 t29 i3 f1',
    axis: "윗줄 «2000%» 글리프 사이 «틈»(ref 2~5 ref px = 4.1~10.3 우리 px) — **932 5회차가 부분 화소로 갈았다**",
    why: "옛 glyph_split 이 «잉크 없는 열 개수» 로 틈을 세고(gaps = runs[i+1][0]−runs[i][1]−1) ref 에만 ×K 를 곱해 ref 틈이 2.06 우리 px 눈금에 갇혀 있었다(이 값이 885 3회차 처방을 letter-spacing 으로 뒤집은 근거였다). 5회차 실측(문턱 90 · 이제 소수 두 자리): ref 틈 합 24.75(6.19 ×4 — 넷 다 정확히 3 ref px) → **26.63**(6.28/6.30/6.33/7.72) · 글리프 합 130.04 → 129.27 — 전부 K 배수에서 풀렸다.",
    fix: "**덩이(run)와 창은 옛 마스크 그대로 두고 모서리만** `_edge` 로 민다(처방 ⓐ — 여기는 두께가 아니라 «두 모서리의 차» 다 · 4회차 probe866 과 같은 이유). 좌표는 **모서리 좌표**(화소 i = [i,i+1))라 교차가 중심 사이 한가운데면 옛 정수값이 그대로 나온다(치우침 0) ⇒ «글리프 합 + 틈 합 = 총 bbox» 가 정의상 닫힌다. ⚠ 교차 보간은 마스크의 **주 조건**(min(R,G)−B)만 타야 한다 — 색 자격(R>140·G>110)까지 −∞ 로 눌러 넣으면 프로파일이 끊겨 교차가 화소 경계에 도로 붙는다(ref x81 실측 0.63 px). 못 재는 모서리는 억지로 밀지 않고 옛 정수값을 쓰고 그 수를 «옛 모서리 n/2N» 으로 밝힌다. 남은 정수 축(문턱 스윕의 판·글자 bbox 143~181 px)은 얇은 축이 아니라 **고치면 안 되는 쪽**이다(±1 ref px = ±1.3% · 3회차 ⓛ 판정과 같은 꼴).",
  },
  'scan887.py': {
    v: 'S', sig: 'k1 r1 t39 i0 f1',
    axis: "하단 «금테 띠» 두께(ref 2 px = 4.44 프레임 px ↔ 우리 5 px) + 아래/위 비의 네 끝점 — **932 7회차가 부분 화소로 갈았다**",
    why: "**R → S(932 7회차).** find_border 가 띠와 어두운 안쪽 선의 경계를 «문턱을 넘는 행» 으로 세고 네 끝점이 전부 정수 행이라, 위 12 : 아래 9 라는 **작은 두 정수의 비**가 곧 과녁이었다(파일이 스스로 «ref ±1 눈금 = 비 ±11%» 라고 적어 둔 그 흔들림). 실측 — 띠 두께 ref 2 → **1.470**(ref y681 이 금색 고원의 47.0% 인 부분 화소다) · 우리 5 → 5.000 ⇒ 어긋남 **+12.5% → +53.0%**(장부의 축 이름이 가리킨 그 비대칭이 네 배였다). 비 자체는 ref 0.750 → **0.7338** · 우리 넷 0.750 → **0.7748**(정수 자가 «완전 일치» 로 읽던 것이 **+5.6%**) · 1600 은 0.714 → **0.7393** 으로 **이상치에서 풀린다**(다섯 중 최악 → 최선).",
    fix: "**한 자 안에 갈래가 둘이다** — 띠 «두께» 는 ⓑ(덮개 적분 `band_edges`), 네 «끝점» 은 위치라 ⓐ(교차 보간 `plateau_edge`/`base_edge`/`ink_edge`). ⚑ 네 끝점 중 **둘은 원래 격자 위에 정확히 앉아 있었다** — `dark_top` Δ 0.000(여섯 장 전부) · `base` Δ 0.000(우리 다섯 · ref 만 +0.101). 움직인 것은 잉크 두 끝이고 그 중 **우리 `ink_top` 이 한 행 통째(−0.981)** 다: `find_ink` 의 `lo = max(4, span·0.008)` 이 ref 1.08% ↔ 우리 0.73% 로 분율이 안 맞고, «몇 열이 잉크인가» 는 덮개가 아니라 **글리프 인구**라 우리 y1998 의 세 열(204·212)이 통째로 버려졌다(ref y638 은 최댓값 41.6 = 진짜로 잉크 없음). ⚠ 밑판 아랫변을 **행 평균**의 50% 교차로 잡으면 여섯 장 전부 `.000` 이 나오는데 그것은 «잰 척» 이다 — 검은 화소가 성겨(ref 25/369 열) 행 평균이 고원이 아니라서 교차가 언제나 두 중심의 한가운데로 떨어진다(⇒ 열마다 재고 중앙값). ⚠ 띠 질량은 바깥 고원을 **위·아래 각자의 것**으로 써야 한다 — 한쪽(검정선) 값을 양쪽에 쓰면 우리 캡처의 배경 행이 새어 들어와 5.000 이 5.155 로 부푼다. 과녁·대역(`REF_RATIO 0.750` · `[0.67,0.83]`)은 **한 자도 안 옮겼다** — 좁히면 제품이 빨개지는 폴리시 판단이라 별도 등재다. 옛 정수 판정은 `--int` 가 되살린다.",
  },
  'scan667b.py': {
    v: 'S', sig: 'k1 r1 t21 i5 f1',
    axis: "리본 좌단 돌출 prot · 금판 솟음 ptop · 잉크 솟음 dtop · 리본 띠 두께 — **얇은 축 넷이 전부 부분 화소다**",
    why: "932 2회차가 `left_edge`(prot) 를, **3회차가 나머지 셋**(`edge_sub` — 같은 투영·같은 문턱의 교차점 선형 보간)을 갈아 끼웠다. ref 실측: ptop 6/7/6/5 → **6.76/6.60/6.43/5.01** · dtop 0/2/4/7 → **−0.39/+1.72/+3.82/+6.70** · 두께 26/26/29/29 → **26.63/26.87/29.24/29.49** (전부 K 배수에서 풀렸다).",
    fix: "**남은 정수 축(판 폭·높이 33~38 · 잉크 폭 42~47)은 «못 고친 것» 이 아니라 «고치면 안 되는 것» 이다** — ⓐ 얇은 축이 아니고(±1 눈금 = ±1.4%) ⓑ 금판 바깥에 밝은 금 테가 한 겹 있어 두 색 경계가 아니다. 소속도로 갈면 폭 38 → 42.96(+13%) 로 **재는 것이 바뀐다**(2회차 +10% · 3회차 +13% 실측). 3회차 판정.",
  },
  'probe866.py': {
    v: 'S', sig: 'k1 r1 t37 i0 f1',
    axis: "알약 «테» 두께 = 바깥 117×24 − 속 113×20 ⇒ **2 ref px = 4.4 우리 px** · [947] **우리 쪽 바깥 상자**(차분)도 같은 축이다",
    why: "**R → S(932 4회차).** 장부의 처방 그대로 갈았다 — 바깥 네 모서리는 같은 `lum`·같은 `DARK_TH`, 속 네 모서리는 같은 `pill_d`·같은 `PILL_TOL` 의 **교차점 선형 보간**(ⓐ · 경계 위치 축이라 ⓑ 가 아니다 — 3회차 ⓚ). `edge()` 의 국면 걸음(904)과 `inside()` 판정은 한 글자도 안 건드렸다. ref: 속 113×20 → **112.10×19.09** · 바깥 117×24 → **116.72×24.38**(넷 다 ±1.0 안) · 우리 쪽 `flat()` 도 같이(250.00×43.00 → **250.50×43.33**) — 한쪽만 갈면 새 비대칭이 생긴다.",
    fix: "**없음(면역) — 걸음은. 창은 945 가 옮겼다.** 932 4회차가 갈고 나니 테가 가로 2.31 ↔ 세로 2.65 로 갈렸는데 **그것은 창의 인공물**이었다 — 세로를 재던 열 `vx = l + 10` 이 알약의 둥근 캡 어깨 위라 테를 비스듬히 가로지른다(열 스윕: 가운데 중앙값 **2.23** ≈ 가로 2.31 · 캡 **4.91**). ⇒ **945 가 그 창을 `v_band`(바깥 폭 18~82%) 열들의 중앙값으로 옮겼다** — 세로 2.65 → **2.23** 으로 갈림이 닫혔고(Δ 0.34 → 0.08), 짝인 자 `probe904.js` 의 `phase` 걸음(씨앗 행 span 전체)과 **같은 자리를 본다**. ⚑ 904 의 «등방 2» 는 살아 있고 `verify866` 과녁은 **945 도 안 옮겼다**(남은 «53.3 ↔ 52.5» 는 정수↔부분화소 축 = 942 몫). `--int` 는 여전히 옛 값 117×24 · 113×20 · 테 2/2 를 한 글자도 안 틀리고 되살린다. 자가 매 실행 다시 찍는다 — `probe866.py --ring-sweep` · `verify932` [7-f][7-g] · `verify945`. ⚑ **[947] 마지막 한 자리 — «우리» 쪽 바깥 상자**: 932 4회차는 `flat()`(속)만 갈았고 바깥 `oq` 는 `diff_box` 의 **정수 상자** 그대로여서 속 Δ +1.9% ↔ 바깥 Δ **+4.8%** 로 갈렸다(945 가 ref 쪽 캡 인공물을 걷어내자 우연한 상쇄가 사라져 드러났다). ⚠⚠ **여기서는 처방 ⓐ(`_cross`)를 못 쓴다 — 재현이 기각했다**: 차분 경계는 램프가 아니라 **계단**이라(밖 화소의 차분이 정확히 0) 문턱을 지나갈 이웃이 없고, 얹으면 f = 1 − th/dv 가 되어 **진할수록 밖으로 민다**(합성 진실 100.40 → `_cross` 101.87 = **정수 101.00 보다도 더 틀린다**). ⇒ 차분 마스크의 부분 화소 축은 **소속도** 하나다(`cov_f` — α = dv(경계)/dv(안쪽 이웃 셋의 중앙값), 밀기 = α − 0.5 라 α=1 이면 옛 정수 걸음 그대로). 끝 행 통계는 **945 규약대로 `v_band` 중앙값**(최댓값은 둥근 어깨에 속아 +0.5px). 실측 바깥 262×55 → **260.58×53.44**(Δ +0.5%/+1.8%) 로 속(+0.6%/+1.9%)과 **같은 방향·같은 크기**가 됐다. 수반(ⓐⓑⓒⓓ)은 ref 쪽도 행 수·화소 개수라 **정수 자 그대로 둔다**(한쪽만 갈면 거기 새 비대칭이 생긴다). 자 `verify947`.",
  },
  'scan934.py': {
    v: 'S', sig: 'k1 r1 t4 i8 f1',
    axis: "배지 두 줄 덩어리의 «판 안 자리» — 판 원점(AABB 중심 · 생 무게중심 · 덮개 무게중심) − 글자 무게중심",
    why: "**934 1회차 판정.** 얇은 축을 안 잰다 — 재는 것은 ~181px 판 안의 «자리»(1.4px)다. 그래도 정수 걸음이 둘 있어 후보로 선다: ⓐ AABB 중심(min/max)은 ref 에서 ±0.5 ref px = **±1.03 우리 px** 로 굳고 그 눈금이 재려는 양과 같은 크기다 · ⓑ 이진 마스크 무게중심은 «화소가 마스크에 드는가» 계단을 한 번 거친다. ⇒ **판정을 지는 축은 부분 피복 질량 적분**(`pink_cov` — 색거리를 문턱에서 band 40 으로 풀어 α∈[0,1] 로 적분)이고, AABB 는 방증으로만 쓴다. 이진 덮개 +1.82 ↔ 질량적분 **+1.78**(Δ0.04) 이라 **계단이 답을 만들지 않았다** — `verify934` [B6] 가 그것을 매 실행 다시 묻는다.",
    fix: "**없음(면역).** ⚑ 이 자가 잡은 것은 자기 계단이 아니라 **남의 계단**이다 — 895 1회차 채점 GH 가 쓴 «생 무게중심» 원점은 분홍이 글자 구멍 뚫린 도넛이라 **재려는 대상(글자)에 의존한다**(구멍 ref 9.0% ↔ 우리 21.8%). 그 의존이 두 비평의 4.4px 갈림을 통째로 만들었다. `verify934` [C1][C2] 가 «글자를 옮기면 생 원점은 따라 움직이고(2.08px) 덮개 원점은 안 움직인다(0.46px)» 를 실캡처로 못박는다.",
  },
  'probe352.py': {
    v: 'B', sig: 'k0 r1 t12 i0 f0',
    axis: "셸 검정 테두리 두께(6~8px) · .stab-sep 구분선 h · 알약 코너 인셋",
    why: "runlen 이 문턱 이하 화소를 센다. 파일 자신이 ««97·85·6» 은 정수 문턱이 부분화소를 버린 값» 이라며 결론을 폐기해 뒀다.",
  },
  'probe384.py': {
    v: 'B', sig: 'k0 r1 t3 i0 f0',
    axis: "검정 옆띠 두께(≈7px) · 채움면 좌 경계 인셋",
    why: "최근접 팔레트 분류 문자열의 런/개수를 세므로 값이 언제나 정수 — ref 의 JPEG 경사면이 통째로 깎인다.",
  },
  'probe409.py': {
    v: 'S', sig: 'k0 r1 t17 i0 f1',
    axis: "코너 법선 «검정 옆띠» 두께(2~7px · 각도별) — **942 5회차가 갈아 끼웠다**(옛 자는 `--int`)",
    why: "**942 5회차.** 걸음은 0.5px 인데 표본을 **이긴 팔레트 색에 통째로** 주는 승자독식 런이라 두께가 언제나 0.5 의 배수였다. 합성 재현(`--physics` · 판은 `probe409g.phys_cols`)이 크기를 못박는다 — 이 자가 ref 에서 실제로 읽는 차례 그대로(`S6 K? D4 B7`) 그려 재면 옛 자는 **참값이 무엇이든 번진 판에서만 정확히 0.50px** 를 잃는다(K7 −7% · K3 −17% · **K2 −25%**). 판정값이 2~7px 이라 그 반 화소가 곧 «1:1 인데도 ref 만 얇다» 다. ⇒ 층 두께를 **`probe409g.runs_from`** 이 내게 했다(사본 0 — ① K↔D 사이 경사면이 세우는 «없는 층» `S` 를 접고 ② 표본 몫을 이웃한 두 층에 비례로 나눈다). 번진 판 읽기 오차 **0.50 → 0.06px** · ref 실측도 격자에서 풀렸다(좌하·좌상 18칸이 **18/18 0.5 배수 → 18/18 비배수** · 45° 좌하 6.0 → **6.94**). 자는 `tools/verify942.js` §10.",
    fix: "**없음(면역).** ⚑ 표본 자리·개수·창(밖 6.0 · 안 22.0)·걸음 0.5·`cls()` 6색·코너 원(H,R = 84,30)·`BOX` 손 값을 **한 칸도 안 건드렸다** — 가장 짧은 증거는 두 모드의 «다음» 글자줄이 **글자까지 같다**는 것이다([10-j]). 층을 고르는 규칙도 `ring_run` 그대로다(윤곽 d=0 에 가장 가까운 K 층 — «처음 나오는 검정» 은 셸 테두리다). ⚠ 남은 ±0.06px 은 942 1회차가 적어 둔 **분해 한계**다(표본 2개짜리 경사면은 t 훑음이 `PH_T` 아래다) — [10-d] 가 그 자리에서 **진짜 2.0px 짜리 얇은 검정이 안 먹히는 것**(1.94)을 지킨다. ⚑ 곁다리 — 캡처(`docs/review/96-full-hero.png`)가 없으면 **즉사**하던 것을 ref 절만 돌게 고쳤다(커밋 금지 자산이라 없는 클론이 정상 · 942 2·3·4회차와 같은 얼굴로 **넷째**다).",
  },
  'probe409c.py': {
    v: 'S', sig: 'k0 r1 t15 i0 f1',
    axis: "열별 «검정 두께»(0~13px · 접선 3열이 판정값) — **942 2회차가 갈아 끼웠다**(옛 자는 `--int`)",
    why: "**942 2회차.** 옛 셈은 `sum(1 for … == \"K\")` — 불리언 세기 그 자체라 값이 언제나 정수였고, 판정값이 0~7px 이라 ±1px 이 곧 ±14% 다. 합성 재현(`--physics`)이 그 크기를 못박는다: 같은 참값 기둥을 «칼같은 판 ↔ 번진 판» 으로 그리면 옛 자는 **번진 쪽만 정확히 1.00px 를 잃는다**(K7 −14.3% · K5 −20% · K3 −33% · **K2 −50%**) — 1:1 인데도 ref 만 얇게 읽히는 그 얼굴이다. ⇒ 열 표본 20개를 **층으로 갈라 K 층 두께의 합**을 낸다(`probe409g.runs_from` 을 그대로 부른다 — 사본 0). 번진 판 ↔ 칼같은 판 |Δ| **1.00 → 0.16px**(K2 에서도 0.16 — 진짜 얇은 층이 안 먹힌다). ref 실측도 정수 격자에서 풀렸다(위 코너 dx8 13.00 → **12.70** · dx18 9.00 → **8.59** · dx34 1.00 → **0.77**). 자는 `tools/verify942.js` §7.",
    fix: "**없음(면역).** ⚑ 창은 한 칸도 안 움직였다 — span 20 · 바닥값 열 dx 45 · 팔레트 6색 · `cls()` 가 소스에 그대로이고 층의 첫 차례도 그 분류가 정한다. ⚠ **남은 +0.16px 은 «못 고친 것» 이 아니라 번짐 모형의 바닥이다** — 참값 폭·앞 층 두께와 무관한 **상수**이고(K2·K7 둘 다 0.16 · S3/S6/S10 에서 같은 7.165), 팔레트 세 색이 색공간에서 한 직선 위에 있지 않아 사영이 그만큼 어긋난다. σ 를 흔들면 6.65(σ0.6) / 7.16(σ1.1) / 5.73(σ1.8) 이라 **σ 1.8 은 이 접기의 한계다**(그 자리는 옛 자도 −1.00px 이다). 판정값 3열에서는 ±0.2px.",
  },
  'probe409f.py': {
    v: 'S', sig: 'k0 r1 t8 i0 f1',
    axis: "«어깨» = 검정 기둥 윗끝 증가분 — **942 4회차가 갈아 끼웠다**(옛 자는 `--int`)",
    why: "**942 4회차.** `top_of_column` 이 `while y -= 1` 로 **정수 y 만** 잡아 윗끝이 «검정인 첫 화소» 에 스냅했다. 합성 재현(`--physics` · 판은 `probe409g.phys_cols`)이 그 크기를 못박는다 — 참 윗끝을 0.05px 씩 옮기면 **참값 6.55·6.60 에서 옛 자가 두 판을 한 화소 통째로 갈라 놓는다**(칼같은 판 7.00 ↔ 번진 판 6.00). 판정값이 0~9px 이라 그 1px 이 곧 ±14~50% 다. ⇒ «어두워지는 마지막 한 칸» 만 문턱 교차로 선형 보간한다(처방 ⓐ · 걸음·문턱 `DARK_MAX 42`·탐침 행 `PROBE_Y 88`·기준선 열 x=39·창 x0 14 x1 40 전부 불변). 번진 판 읽기 오차 **0.60 → 0.18px** · 판 사이 최대 편차 **1.00 → 0.54px**. ref 실측도 정수 격자에서 풀렸다(어깨 x16~25 **9·8·8·7·6·5·4·4·4·3 → 9.25·8.19·7.57·6.93·6.07·5.17·4.19·4.20·3.84·2.53** · 26/26 이 정수였고 25/26 이 비정수가 됐다). 자는 `tools/verify942.js` §9.",
    fix: "**없음(면역).** ⚠ 남은 편차 0.54px 은 «못 고친 것» 이 아니라 **칼같은 판에는 부분 화소 정보가 없다**는 원리다 — 경계가 계단이면 어느 자로 재도 ±0.5 이고, 실제로 두 자의 cap 오차가 0.45 ↔ 0.44 로 같다([9-d]). ⚑ 좌표 규약 함정 하나 — 표본은 화소 **중심**에서 오므로 교차 자리에 **+0.5** 를 더해야 옛 자(= 검정인 첫 화소의 윗변)와 같은 공간이다. 빼먹으면 칼같은 판이 참값보다 언제나 0.5 작게 나온다([9-e] 가 그 자리를 지킨다).",
  },
  'probe409g.py': {
    v: 'S', sig: 'k0 r1 t21 i1 f1',
    axis: "--diag 코너 층 두께(K7.0 · B7.0 …) — **942 1회차가 갈아 끼웠다**(옛 자는 `--int`)",
    why: "**942 1회차.** 결함이 «0.5 격자» 하나가 아니었다 — 번진 판(ref)에서 두 층 사이 경사면이 «그 사이에 있는 세 번째 팔레트 색»(K↔D 의 S · D↔B 의 F)으로 이겨 **없는 층**을 만든다(합성 재현: 참값 S3 K7 D4 B7 → 옛 자 `S3.00 K6.50 S1.50 D2.00 F1.50 B6.50`, 층이 4 → 6). ⇒ ① 좁고(≤2px) 이웃 두 색 선분 위에 있고(잔차 ≤14) **사영 t 가 훑는** 런만 접고 ② 표본 몫을 **이웃한 두 층**에 비례로 나눈다(질량 보존). 번진 판 ↔ 칼같은 판 최대 |Δ| **5.00 → 0.13 px**. 자는 `tools/verify942.js`.",
    fix: "⚠ **남은 한 자리는 «못 고친 것» 이 아니라 분해 한계다** — 표본 2개(1.0px)짜리 경사면은 t 훑음이 문턱 아래라 안 접힌다(덫 더미 K7 D2 B7 에서 `F1.15` 가 남는다). 접기를 더 무르게 하면 **진짜 1px 층**과 못 가른다(σ 1.1px 번짐에서는 원리적으로 같은 그림이다) — 그래서 남긴다. `verify942` [2] 가 그 자리에서 진짜 층 D 가 안 먹히는 것을 지킨다.",
  },
  'probe409i.py': {
    v: 'S', sig: 'k0 r1 t42 i0 f1',
    axis: "층 두께 K(≈7) · 1층 D · 2층 B(2~7px) — **942 3회차가 갈아 끼웠다**(옛 자는 `--int`)",
    why: "**942 3회차.** 이 자는 상자·윤곽을 서브픽셀로 잡아 놓고 정작 «층 두께» 는 최근접 런으로 셌다(값이 언제나 0.5 배수). ⚑ 뿌리가 **둘**이고 둘째가 이 자 고유다 — ⓐ `gate_layers`·`norm_layers` 의 승자독식 런(409g 와 같은 얼굴: 번진 판에서 층이 4 → 6 · K −0.5 · D −2.0) · ⓑ **자기가 처방으로 들고 있던 `cov_ray` 자신이 샜다.** 몫을 «가장 가까운 **두 팔레트 색**» 에 나누는데 이 팔레트는 `S`(43,35,26)가 `K`(0,0,0)↔`D`(65,49,34) **사이**라 검정 경사면의 몫이 **없는 S 층**으로 간다 — 합성 재현(`--physics`)이 그 크기를 못박는다: 참값 `S3 K7 D4 B7` 에서 번진 판만 **S 3.00 → 4.93(+64%) · D 4.00 → 1.90(−52.5%) · 가짜 F1.63**. 942 1회차가 «`cov_ray` 를 베끼지 마라» 로 남긴 그 자리다. ⇒ 두 축 다 **차례가 정한 이웃 층**에만 나눈다(`probe409g.runs_from` 을 그대로 부른다 — 사본 0). 번진 판 ↔ 칼같은 판 최대 |Δ|: 층 자 **5.00 → 0.13px** · 덮개 자 **2.10 → 0.04px**. ref 실측도 0.5 격자에서 풀렸다(D 띠 45/60/75° **3.00·4.25·6.00 → 2.66·3.89·5.15**). 자는 `tools/verify942.js` §8.",
    fix: "**없음(면역).** ⚑ 표본 자리·각도·보폭·문턱(`EDGE_T 45`)·분류(`cls()` 6색)·윤곽 자(`find_box`·`contour` 는 처음부터 문턱 교차 보간)를 **한 칸도 안 건드렸다** — `--int` 가 옛 값을 글자까지 재현하고, 두 모드의 **클래스 글자줄이 완전히 같다**(바뀐 것은 그 줄을 두께로 바꾸는 셈뿐). ⚠ 남은 자리 둘은 «못 고친 것» 이 아니다 — ⓐ 덫 더미(K7 **D2** B7)에서 진짜 얇은 D 가 2.45 로 살아 있으나 +0.45px 이 남는다(σ 1.1px 번짐의 분해 한계 · 더 무르게 접으면 진짜 1px 층을 먹는다) · ⓑ 그 더미에서 층 자가 `F1.15` 하나를 남긴다(942 1회차 [2-d] 와 같은 자리 · 표본 2개짜리 경사면은 t 훑음이 문턱 아래다).",
  },
  'probe449.py': {
    v: 'B', sig: 'k0 r1 t2 i0 f0',
    axis: "코너 광선 «첫 실런» 두께(K/B ≈7px)",
    why: "ref 쪽 «목표값» 을 정수 최근접 런으로 뽑아 짝인 probe449.js 에 넘긴다 = 결함 있는 상수의 **생산지**.",
  },
  'scan335.py': {
    v: 'B', sig: 'k0 r1 t3 i0 f0',
    axis: "색 밴드 두께 h(검정 테·이음매 띠 · h≥2 만 남긴다)",
    why: "정수 y 걸음으로 밴드를 센다. 1회차 판정자는 «축척이 같아 상쇄된다» 로 N 을 냈으나, 번짐 비대칭(②ⓑ)에서는 상쇄가 안 된다.",
  },
  'scanA4.py': {
    v: 'B', sig: 'k0 r1 t8 i0 f0',
    axis: "링 어두운 띠 두께 · 노란 밴드 두께",
    why: "0.5px 반지름 걸음이지만 두께를 b−a+.5 로 내는 문턱 자다. 1:1 이라 축척 편향은 없고 번짐 편향만 남는다.",
  },
  'scanA4b.py': {
    v: 'B', sig: 'k0 r1 t2 i0 f0',
    axis: "노란 밴드 두께",
    why: "scanA4 와 같은 규약·같은 이유.",
  },
  'scan895.py': {
    v: 'S', sig: 'k1 r1 t56 i11 f1',
    axis: "배지 두 줄의 «검정 획»(stroke_thk)",
    why: "895 2회차가 갈아 끼운 자리 — R_MID 교차점을 STEP 0.1 로 선형 보간한다(ⓐ 선례).",
  },
  'scan667c.py': {
    v: 'S', sig: 'k1 r1 t22 i7 f1',
    axis: "리본·글자의 «검정 테» 두께(dark_mass)",
    why: "Σ(1 − L/L고원) 질량 적분 — AA 화소가 자기 비율만큼만 세어진다(ⓑ 선례). 모서리도 ±0.5 소수로 낸다.",
  },
  'scan833.py': {
    v: 'S', sig: 'k1 r1 t2 i3 f1',
    axis: "머리띠 «위 검정 테»(ref 4.37~4.92 ref px)",
    why: "cross() 50% 교차 선형보간으로 테·채움·총높이를 전부 낸다.",
  },
  'scan885f.py': {
    v: 'S', sig: 'k1 r1 t2 i0 f0',
    axis: "리본 크림 캡의 «검정 립»(ref 0.8 ref px)",
    why: "«몇 열이냐»(2)와 «잉크로 세면»(0.8)의 차이를 문서에 적고 후자를 택했다 — 처방 ⓑ 를 이미 쓴 자.",
  },
  'probe409d.py': {
    v: 'S', sig: 'k0 r1 t23 i2 f1',
    axis: "링 두께 th=r_o−r_i(≈7px) · 링↔바 «틈»",
    why: "0.1px 걸음 겹선형 프로파일 + 문턱 교차 보간(ⓐ 와 같은 자).",
  },
  'probe437.py': {
    v: 'S', sig: 'k0 r1 t11 i0 f0',
    axis: "셸 검정 테두리 · 알약 검정(6~8px)",
    why: "r_cov 커버리지 적분(ⓑ) 이 본표다. 우리 캡처의 CSS 진실값(6.00/7.00)으로 자를 먼저 검산한다.",
  },
  'probe450.py': {
    v: 'S', sig: 'k0 r1 t7 i0 f0',
    axis: "검정 두께(≈7px) · «립»(≈1.4px) · «림»(1~2px) — 저장소에서 가장 얇은 축",
    why: "두께는 질량 적분, 경계는 색 사영 부분화소. 1.4px 축을 정수로 못 잰다는 것을 알고 설계됐다.",
  },
  'scan667.py': {
    v: 'A', sig: 'k1 r1 t6 i2 f0',
    axis: "노치 깊이 ≈15 ref px(≈31 우리 px)",
    why: "정수 인덱스지만 깊이가 두꺼워 ±1 ref px 이 6% 수준이다.",
  },
  'scan892.py': {
    v: 'A', sig: 'k1 r0 t2 i0 f0',
    axis: "라벨 잉크 w×h (ref 65×15 = 우리 144×33)",
    why: "⚠ 자 자신이 «±1 ref px = 세로 ±6.7%» 라 적고 그 차를 «분해 불가» 로 처리한다 — 얇아지면 R 로 넘어갈 자리.",
  },
  'scan885c.py': {
    v: 'A', sig: 'k1 r1 t13 i6 f0',
    axis: "각도 · cen 38.5 · base 35.8 (우리 px)",
    why: "마스크는 정수지만 cen 은 수백 화소의 평균 · base 는 99.5 분위라 바닥깎기가 안 생긴다.",
  },
  'scan885g.py': {
    v: 'A', sig: 'k1 r1 t1 i4 f0',
    axis: "낱자 폭 중앙값(ref 8 ref px) · 피치 · 라벨 bbox",
    why: "⚠ 폭은 ref ±1px = ±12% 라 8회차가 다툰 −8~−11% 와 크기가 맞먹는다. **피치만 부분화소인 것이 비대칭**이다.",
  },
  'scan923.py': {
    v: 'S', sig: 'k1 r1 t23 i2 f0',
    axis: "평탄부 F · 깊이 D · 노치 길이 · **검정 띠 두께**(923 8회차 `--band` 신설 — 곧은변 ≈10 우리 px)",
    why: "**S 유지 · 축이 하나 늘어 재판정(956).** ① 옛 판정(932 4회차)의 근거는 그대로다 — 923 4회차(`46e41be`)가 `outer_x` 를 덮개 적분(α = |p−bg|₁ / |검정−bg|₁)으로, `width_at` 세로 폭을 이웃 행 선형 보간(`cross`)으로 갈아 끼웠다. 남은 정수 걸음 **둘은 여전히 길이가 아니다**: L143(옛 L112) `np.abs(blk−bg).sum()` 은 색 거리(덮개의 **분모**) · L477(옛 L449·L302) `(…).sum() > W*0.40` 은 카드 띠를 **찾는** 행 술어다(`runs(minlen=20)` — 수백 px 대역). ② **새 걸음이 본체다** — 923 8회차(`4b34940`)가 `--band`(`inner_x`·`band_prof`·`band_table`, +109줄)로 **얇은 축을 하나 신설했다**(검정 띠 두께 ≈10 우리 px = 이 장부가 존재하는 바로 그 종류의 축). 그 축도 **두 모서리가 다 부분 화소**다: 바깥은 위 `outer_x`(덮개 적분 ⓑ) · 안쪽 `inner_x` 는 «밝은 고원 V 의 50%» 문턱을 **선형 보간**(`f = (g[i]−lvl)/(g[i]−g[i+1])`, `return i + f`)으로 지난다(ⓐ). 두께 자체는 두 부분화소 점구름 사이 **최소 유클리드 거리**(`np.hypot(…).argmin()`)라 화소 세기가 한 번도 안 들어간다 — 실측이 소수로 나온다(ref 배너 곧은변 10.31 · 우리 10.05/10.06 · 칸 중앙값 9.43~10.34). ⓐ 는 W<3.7 에서 무너지지만([3-d]) 이 축은 ≈10 우리 px 이라 [3-e] 대역 안이다.",
    fix: "**없음(면역).** ⚑ **옛 `fix` 의 «두 자리만 다시 보면 된다» 는 이번에 틀렸다** — `.sum()` 이 안 늘어 `i2` 가 그대로였는데도(신호로는 `t14 → t24` 만 움직였다) 자에는 **새 얇은 축**이 들어와 있었다. 신호가 움직이면 세는 것은 옛 자리가 아니라 **늘어난 걸음**이다(931 «손으로 적은 갈래는 자가 늘 때 아무도 안 센다»). 자는 `tools/verify956.js`.<br>⚑ **923 8회차 2단계(2026-09-05)가 그 편향을 닫았다** — 옛 경고(«ref 불릿 곧은변 11.81 ↔ 채점 2인 10.17~10.23 · 뿌리는 그림자 램프»)의 **가설 쪽은 기각**됐다. 뿌리는 그림자가 아니라 **바탕 표본**이었다(자가 카드 한복판 화소 하나를 400행 전부의 바탕으로 썼고, 초록 카드에서 그 행이 ref 화면의 밝은 파랑 패널 띠 (45,46,66) 위에 앉았다 ⇒ 덮개 분모 127→157 · +1.18 우리px). `bg_rows()` 로 **행마다** 재게 하니 곧은변이 배너 10.34 · 불릿 **10.32** 로 만나고 두 카드의 곧은변 x 가 **둘 다 485.63** 이다. 이 걸음은 부분 화소 축을 하나도 안 늘렸다(중앙값은 색이지 길이가 아니다).",
  },
  'scan833b.py': {
    v: 'A', sig: 'k0 r0 t13 i9 f0',
    axis: "리본 좌·우단·높이 · 라벨 잉크 · 탭 솟은 높이 · 제목 피치",
    why: "전부 20 우리 px 이상 — 이진 마스크지만 바닥깎기가 비율로 묻힌다.",
  },
  'probeA3.py': {
    v: 'A', sig: 'k0 r1 t1 i1 f0',
    axis: "판·알약·배너·플레이트 bbox(w 32~500 · h 40~166)",
    why: "두께·틈 축을 아예 안 잰다(잉크는 inkA3.py 몫). 1px 이 0.2~2.5%.",
  },
  'scan01.py': {
    v: 'A', sig: 'k0 r1 t8 i0 f0',
    axis: "버튼 외곽·알약·아트·글자 잉크 bbox(64~500px)",
    why: "헤더가 «두께는 순색 코어끼리» 를 인용하지만 실제로 두께 축은 하나도 안 잰다.",
  },
  'scan12r.py': {
    v: 'A', sig: 'k0 r1 t5 i0 f0',
    axis: "잉크 bbox · 런의 시작 x · 검정 런의 아래 끝 · 패널 상변(위치 축)",
    why: "두께를 안 낸다 — 위치 축이라 바닥깎기가 양쪽에 같은 방향으로 걸려 상쇄된다.",
  },
  'scan151.py': {
    v: 'N', sig: 'k1 r1 t1 i6 f0',
    axis: "ref 카드 테 두께(card_frame)",
    why: "**ref 한 장만** 연다 — 캡처를 아예 안 열어 견줄 상대가 없다.",
  },
  'scan207.py': {
    v: 'N', sig: 'k1 r0 t1 i0 f0',
    axis: "젬 잉크 bbox 52×50 · 판 중심 Δ",
    why: "ref 는 손으로 적힌 상수(REF_W/REF_H)라 ref 쪽에 바닥깎기가 생길 코드가 없다.",
  },
  'scan813c.py': {
    v: 'N', sig: 'k1 r1 t25 i0 f0',
    axis: "위 12 : 아래 9 ref px 여백과 그 비",
    why: "ref 한 장만 연다. 금테 띠는 y 좌표만 인쇄하고 두께 수치를 안 낸다.",
  },
  'scan885.py': {
    v: 'N', sig: 'k1 r1 t32 i1 f0',
    axis: "아트 덩이 bbox · 돌출 20.6 · 카드-로컬 오프셋",
    why: "ref 만 화소로 재고 우리 값은 geo JSON 의 DOM 상자다 — «같은 자로 두 해상도» 가 성립 안 한다.",
  },
  'scan813d.py': {
    v: 'N', sig: 'k0 r0 t7 i0 f0',
    axis: "위 ~26 : 아래 ~20 프레임 px 와 그 비",
    why: "비(무차원)만 내므로 K 가 안 곱해진다. ⚠ 다만 그 과녁 0.750 자체가 scan887 의 정수 자에서 나왔다.",
  },
  'scan13.py': {
    v: 'N', sig: 'k0 r1 t1 i1 f0',
    axis: "색 영역 bbox x,y,w,h",
    why: "1:1 · 큰 축.",
  },
  'scan335x.py': {
    v: 'N', sig: 'k0 r1 t3 i0 f0',
    axis: "바 안쪽 폭·바깥 폭·좌우 여백",
    why: "테두리 위를 정수로 걷지만 그 두께 자체를 출력하지 않는다.",
  },
  'scan337.py': {
    v: 'N', sig: 'k0 r1 t1 i0 f0',
    axis: "알약 위/아래 여백 · 바 면 노출",
    why: "잉크 bbox 는 큰 축이고 1:1 이다.",
  },
  'scan342.py': {
    v: 'N', sig: 'k0 r1 t6 i0 f0',
    axis: "던전명·알약 라벨 bbox · 알약 우측 여백",
    why: "재는 축이 전부 6px 을 훨씬 넘는다.",
  },
  'scan35.py': {
    v: 'N', sig: 'k0 r1 t3 i1 f0',
    axis: "아이콘·배지·자물쇠·육각·리본 bbox w×h",
    why: "축이 전부 큰 bbox 이고 1:1.",
  },
  'scan36.py': {
    v: 'N', sig: 'k0 r1 t1 i3 f0',
    axis: "글자 코어·배지·알약 캡슐 bbox w×h",
    why: "같은 임계·같은 창을 같은 축척 두 장에 댄다.",
  },
  'scan55.py': {
    v: 'N', sig: 'k0 r1 t3 i0 f0',
    axis: "row/col 색 전이 구간 폭",
    why: "IMG 로 받은 **한 장**만 연다 — 대조 코드가 없다.",
  },
  'scan70.py': {
    v: 'N', sig: 'k0 r1 t2 i0 f0',
    axis: "row/col 전이 구간 폭 · ink bbox",
    why: "ref 전용 대화형 스캐너.",
  },
  'scan72.py': {
    v: 'N', sig: 'k0 r1 t4 i0 f0',
    axis: "썸네일 그라디언트 밀도 bbox",
    why: "ref 한 장에서 큰 bbox 만 뽑는다.",
  },
};

const ORDER = { R: 0, B: 1, S: 2, A: 3, U: 4, N: 5 };

function census() {
  const files = fs.readdirSync(TOOLS)
    .filter(f => /^(scan|probe)\w*\.py$/.test(f))
    .sort();
  return files.map(f => {
    const src = fs.readFileSync(path.join(TOOLS, f), 'utf8');
    const s = signals(src);
    const key = `k${s.scale ? 1 : 0} r${s.refimg ? 1 : 0} t${s.thin} i${s.intlen} f${s.frac ? 1 : 0}`;
    const led = LEDGER[f];
    /* 후보 = ① 낌새 ∧ ② 낌새. 후보가 아니면 판정 없이 N 이다. */
    const cand = (s.scale || s.refimg) && s.thin > 0;
    let verdict, stale = false;
    if (led) {
      stale = led.sig !== key;
      verdict = stale ? '?' : led.v;
    } else if (!cand) {
      verdict = 'N';                 /* 후보조차 아니다 — ① 도 ② 도 낌새가 없다 */
    } else {
      verdict = 'U';                 /* ⚠ 후보인데 장부에 없다 = **아직 안 읽었다.** 초록이 아니다. */
    }
    return { file: f, sig: key, ...s, cand, verdict, stale, led: led || null };
  });
}

/* ── 물리 재현 ───────────────────────────────────────────────────────────
   합성 1차원 프로파일로 «정수 걸음이 왜 ref 만 깎는가» 를 그대로 보인다.
   같은 참값 W(우리 px)를 두 해상도로 그린다 — 우리(1배)와 ref(1/K 배) —
   두 자로 재서 우리 px 로 환산해 견준다. 이미지도 브라우저도 안 쓴다. */
const K = 2.0628;
const R_MID = 125.0;

/* 번짐 — 어느 그림이든 **자기 화소 단위로** 이만큼 번진다(렌더러 AA · 축소 · JPEG).
   ⚑ 이 한 상수가 결함의 전부다: ref 의 0.6 ref px 는 **우리 px 로 1.24** 라,
   같은 «가장자리 한 겹» 이 ref 에서는 얇은 띠의 절반을 차지한다. */
const SIGMA = 0.6;

/** 밝은 고원(255) 위에 검정(0) 띠 하나. 화소 값은 «덮인 넓이» 를 자기 화소 단위로 번지게 한 것. */
function profile(widthPx, startPx, n, sigma = SIGMA) {
  /* 잘게 나눠 적분 → 가우시안 번짐 → 화소로 모은다. 번짐은 «이 그림의 화소» 단위다. */
  const SS = 16;                       /* 화소당 표본 수 */
  const cov = [];
  for (let s = 0; s < n * SS; s++) {
    const x = (s + 0.5) / SS;
    cov.push(x >= startPx && x < startPx + widthPx ? 1 : 0);
  }
  const rad = Math.ceil(3 * sigma * SS);
  const ker = [];
  for (let d = -rad; d <= rad; d++) ker.push(Math.exp(-0.5 * ((d / SS) / sigma) ** 2));
  const ksum = ker.reduce((a, b) => a + b, 0);
  const p = [];
  for (let i = 0; i < n; i++) {
    let acc = 0;
    for (let j = 0; j < SS; j++) {
      const s = i * SS + j;
      let v = 0;
      for (let d = -rad; d <= rad; d++) {
        const t = s + d;
        v += (t >= 0 && t < cov.length ? cov[t] : 0) * ker[d + rad];
      }
      acc += v / ksum;
    }
    p.push(255 * (1 - acc / SS));
  }
  return p;
}

/* «확실히 검정» 문턱 — 옛 `scan895` 의 `BLACK_T` 와 같은 값이다.
   ⚑ 두 자의 차이는 «걸음» 만이 아니라 **묻는 말**이다:
     정수 걸음은 «이 화소가 검정인가»(BLACK_T) 를 묻고,
     부분 화소는 «어디서 한가운데를 지나는가»(R_MID) 를 묻는다.
   번짐 경사면은 앞의 물음에 «아니오» 라고 답한다 — 그 경사면이 ref 에서 K 배 넓다. */
const BLACK_T = 90.0;

/** ③ 정수 걸음 — «검정» 화소를 한 칸씩 센다. 값이 언제나 정수다. */
function intRuler(p, t = BLACK_T) {
  let n = 0;
  for (const v of p) if (v < t) n++;
  return n;
}

/** ⓐ 부분 화소 — 문턱 교차점을 선형 보간해 두 점 사이를 잰다. */
function fracRuler(p) {
  let a = null, b = null;
  for (let i = 1; i < p.length; i++) {
    if (a === null && p[i - 1] >= R_MID && p[i] < R_MID) {
      a = i - 1 + (p[i - 1] - R_MID) / (p[i - 1] - p[i]);
    } else if (a !== null && b === null && p[i - 1] < R_MID && p[i] >= R_MID) {
      b = i - 1 + (R_MID - p[i - 1]) / (p[i] - p[i - 1]);
    }
  }
  return a === null || b === null ? NaN : b - a;
}

/** ⓑ 질량 적분 — Σ(1 − L/L_고원). AA 화소가 자기 비율만큼만 세어진다. */
function massRuler(p) {
  let m = 0;
  for (const v of p) m += Math.max(0, 1 - v / 255);
  return m;
}

/** 참값 W(우리 px)를 두 해상도에서 세 자로 잰 표 한 줄. */
function physicsRow(W, phase) {
  const our = profile(W, 8 + phase, 40);
  const ref = profile(W / K, (8 + phase) / K, 40);
  return {
    W,
    ourInt: intRuler(our), refInt: intRuler(ref) * K,
    ourFrac: fracRuler(our), refFrac: fracRuler(ref) * K,
    ourMass: massRuler(our), refMass: massRuler(ref) * K,
  };
}

/** 여러 위상에서 평균 — 한 위상만 보면 «우연히 맞는» 자리가 나온다. */
function physics(widths = [2.0, 3.0, 3.73, 4.13, 5.0, 7.0]) {
  const PH = [0, 0.17, 0.33, 0.5, 0.67, 0.83];
  return widths.map(W => {
    const rows = PH.map(ph => physicsRow(W, ph));
    const avg = k => rows.reduce((s, r) => s + r[k], 0) / rows.length;
    const o = { W, int: [avg('ourInt'), avg('refInt')], frac: [avg('ourFrac'), avg('refFrac')], mass: [avg('ourMass'), avg('refMass')] };
    o.intBias = (o.int[1] - o.int[0]) / o.int[0] * 100;   /* «ref 가 우리보다 얼마나 얇게 읽히는가» */
    o.fracBias = (o.frac[1] - o.frac[0]) / o.frac[0] * 100;
    o.massBias = (o.mass[1] - o.mass[0]) / o.mass[0] * 100;
    return o;
  });
}

/* ── 출력 ───────────────────────────────────────────────────────────────── */
function main() {
  const args = process.argv.slice(2);
  const rows = census();
  if (args[0] === '--json') {
    console.log(JSON.stringify({ census: rows, physics: physics() }, null, 2));
    return;
  }
  if (args[0] === '--list') {
    rows.filter(r => r.verdict === (args[1] || 'R')).forEach(r => console.log(r.file));
    return;
  }
  if (args[0] === '--physics') {
    console.log('=== 물리 재현 — 같은 참값 W 를 두 해상도로 그려 세 자로 잰다 (우리 px 환산 · 위상 6개 평균) ===');
    console.log(`    번짐 σ = ${SIGMA} (각 그림의 **자기 화소** 단위 — ref 의 σ 는 우리 px 로 ${(SIGMA * K).toFixed(2)})\n`);
    const f = (x, w = 5) => (Number.isFinite(x) ? x.toFixed(2) : 'nan').padStart(w);
    const pc = x => (x >= 0 ? '+' : '') + x.toFixed(1) + '%';
    console.log('    참값 W │ ③ 정수 걸음            │ ⓐ 교차점 보간          │ ⓑ 질량 적분');
    for (const p of physics()) {
      console.log(`    ${p.W.toFixed(2).padStart(6)} │ 우리${f(p.int[0])} ref${f(p.int[1])} ${pc(p.intBias).padStart(7)} │`
        + ` 우리${f(p.frac[0])} ref${f(p.frac[1])} ${pc(p.fracBias).padStart(7)} │`
        + ` 우리${f(p.mass[0])} ref${f(p.mass[1])} ${pc(p.massBias).padStart(7)}`);
    }
    console.log('\n    (% = ref 가 우리보다 얼마나 크게 읽히는가. 음수 = **ref 가 얇게 읽힌다** = 895 가 겪은 얼굴)');
    return;
  }
  const by = {};
  for (const r of rows) (by[r.verdict] = by[r.verdict] || []).push(r);
  console.log(`=== 932 전수 — tools/scan*.py · tools/probe*.py ${rows.length} 개 ===\n`);
  for (const v of Object.keys(ORDER).concat('?').filter(k => by[k])) {
    const label = {
      R: '빨강 — ①∧②ⓐ 축척 비대칭 (895 와 같은 얼굴)',
      B: '주홍 — ①∧②ⓑ 번짐 비대칭 (1:1 · 942 로 등재)',
      S: '면역 — 얇은 축을 부분 화소로 잰다',
      A: '주황 — 정수 걸음이지만 축이 얇지 않다',
      U: '⚠ 미판정 후보(아직 안 읽었다 — 초록이 아니다)',
      N: '무관 — ① 도 ② 도 아니다', '?': '⚠ 판정 무효(신호가 바뀌었다 — 다시 읽어라)',
    }[v];
    console.log(`[${v}] ${label} — ${by[v].length}개`);
    for (const r of by[v]) {
      const tail = r.led ? `  ← ${r.led.axis}` : '';
      console.log(`    ${r.file.padEnd(18)} ${r.sig}${tail}`);
    }
    console.log('');
  }
}

module.exports = { census, signals, physics, intRuler, fracRuler, massRuler, profile, LEDGER, K, R_MID };
if (require.main === module) main();
