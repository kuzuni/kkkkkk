# 측정 — 작업 265 「행운 룰렛 팝업 글씨 크기」

> 주인 지시(2026-08-27): «룰렛 팝업의 글씨가 너무 작다 — 22 퀘스트 팝업 글씨 크기에 맞출 것».
> 측정기 `tools/measure265.js` (1080×2280 · deviceScaleFactor 1 · 눈대중 금지).
> **126(디자인 토큰·폰트 통일)이 ⏹종료 상태라 여기서 정한 값이 이 화면의 기준값이다**(등재문 주의 ⓒ).

## 1. 실측 — 착수 전(2026-08-28, `cb9c0d4` 시점)

| 자리 | 선택자 | font-size | line-height | 잉크 폭 |
|---|---|---|---|---|
| 모달 제목 | `.mhead h2` | **45.6px** | 45.6px | 182.3 («행운 룰렛») |
| 안내줄 | `.mbody p`(공용) | **24px** | 40.8px | 191.0 («오늘 남은 횟수 5 / 5») |
| 결과줄 | `#rouRes` (`.mbody p` 상속) | **24px** | 40.8px | — |
| 세그먼트 아이콘 | `.rlt-ic` | 44px | 50px | 62.1 |
| 세그먼트 텍스트 | `.rlt-tx` | **23px** | 25.76px(1.12) | 54.5 («다이아») |
| 세그먼트 값 | `.rlt-vl` | **26px** | 29.12px(1.12) | 43~66 («100»~«1,000») |
| 버튼 라벨 | `.gbtn` | 26px | normal | 180 / 44 |

## 2. 비교 기준 — 22 퀘스트 팝업(주인이 지정한 표본)

| 자리 | 선택자 | font-size |
|---|---|---|
| 모달 제목 | `.mhead h2` | 45.6px (룰렛과 **같다** — A5 공용) |
| 행 제목 | `.qs-t` | **35px** |
| 행 버튼 라벨 | `.qs-b b` | **37.5px** |
| [모두 받기] | `.qs-all b` | 46.6px |
| 탭 라벨 | `.qs-tg b` | 44.7px |

→ 제목만 이미 같고, **본문·라벨이 22 보다 10~14px 작다**. 이것이 주인이 본 «너무 작다» 다.

## 3. 결정한 기준값 (265 이후)

| 자리 | 이전 | 이후 | 근거(22 대응 자리) |
|---|---|---|---|
| 안내줄 `#rouGuide` | 24px | **35px** | `.qs-t` 35px (본문 글) |
| 결과줄 `#rouRes` | 24px | **35px** | `.qs-t` 35px |
| `.rlt-tx` | 23px | **35px** | `.qs-t` 35px |
| `.rlt-vl` | 26px | **37.5px** | `.qs-b b` 37.5px (강조 값) |
| `.rlt-ic` | 44px | 44px (유지) | 아트 자리 — 글씨가 아니다(§5) |
| `.gbtn` 라벨 | 26px | 26px (유지) | **작업 267 의 구간** — §5 |

`line-height` 를 `.rlt-tx 39px` · `.rlt-vl 42px` 로 **명시**했다. 부모 `.rlt-lb{line-height:1.12}`
배수에 맡기면 폰트를 만질 때마다 라벨 블록 높이가 따라 흔들려 §4 의 «가용 폭» 계산이 무효가 된다.

## 4. 45° 조각 기하 — 라벨이 넘치지 않는다는 근거 (등재문 주의 ⓑ)

`.rlt` 660×660, 중심 (330, 330). 세그먼트는 45°(반각 22.5°)라 중심에서 거리 d 인 곳의
가용 폭은 `2·d·tan22.5° = 2·d·0.4142` 다. 라벨은 위(`top:26px`)에서 아래로 자라므로
줄의 **아래변** y 가 가장 좁다 — `가용(y) = 2·(330 − y)·0.4142`.

| 줄 | 이전 y (위→아래) | 이후 y | 이후 가용 폭 | 최대 잉크 | 여유 |
|---|---|---|---|---|---|
| `.rlt-ic` | 26 → 77 | 26 → 77 | 209.6 | 62.1 | +147.5 |
| `.rlt-tx` | 77 → 103 | 77 → 116 | **177.3** | 84.5 | **+92.8** |
| `.rlt-vl` | 102 → 131 | 116 → **158** | **142.5** | **96.0** («1,000») | **+46.5** |

- 원판 안쪽 반지름 306 → 라벨 위끝 26 ≥ 24 **✓**(디스크 안)
- 허브 반지름 75 → 라벨 아래끝 158 ≤ 255 **✓**(허브 밖, 여유 97px)
- 라벨 상자 `.rlt-lb` 폭은 186px 그대로다. 최대 잉크 96 < 186 이라
  «글리프 advance 보다 좁은 상자 + text-align:center» 함정(LESSONS / A1 비고)에 안 걸린다.

→ **글씨를 22 급으로 올리고도 조각 밖으로 나가는 칸은 0개다.** 자간 압축·`scaleX` 는 필요 없었다.
   (155 는 이미 들어가 있어 8칸 전부 «다이아» 라 텍스트 줄이 가장 짧은 상태다.)

## 5. 이 작업이 **일부러 안 건드린** 자리

| 자리 | 이유 |
|---|---|
| `.gbtn`(룰렛 돌리기 · 닫기) 26px | **작업 267** 이 «룰렛·승급전·길라잡이 팝업의 [닫기] 제거 + [룰렛 돌리기]를 22 [모두 받기](46.6px) 급으로» 를 통째로 소유한다. 265 가 폰트만 올려 두면 267 이 버튼을 지우거나 규격을 다시 잡을 때 두 번 일한다 — 267 이 잡을 때 `.qs-all b` 46.6px 기준으로 한 번에 정할 것. |
| `.rlt-ic` 44px | 이모지 = **아트 자리**(§6). 크기를 지금 정하면 실제 아이콘이 들어올 때 다시 잰다. |
| `.mhead h2` 45.6px | 이미 22 와 같은 값이다(A5 공용). |
| `.mbody p` 24px (공용) | 여기를 키우면 **모든 팝업**이 같이 커진다(등재문 주의 ⓐ). 265 는 `#rouGuide`·`#rouRes` 두 id 로만 가뒀다. |

## 6. 아트 필요

| 요소 | w × h | 용도 | 현재 대체물 |
|---|---|---|---|
| 세그먼트 보상 아이콘 `.rlt-ic` | 62 × 62 (잉크) / 186 × 50 (상자) | 원판 8칸의 보상 그림 | `curIc('dia')` 이모지 44px |
| 허브 `.rlt-hub` | 150 × 150 | 원판 중앙 엠블럼 | 🎰 이모지 62px |
| 포인터 `.rlt-ptr` | 62 × 78 | 당첨 표시 삼각 | CSS 삼각형(#E4552B) |

## 7. 추가 실측 — [룰렛 돌리기] 버튼 **안쪽 기하** (2026-08-29, 작업 367, `tools/probe367.js`·`verify367` [G])

§5 가 «267 의 구간» 으로 남겨 둔 자리다. 367 이 그 버튼 안에 ▶AD 뱃지를 얹으면서 처음 쟀다.

| 자리 | 실측 |
|---|---|
| 버튼 `#rouBtn`(`.ifbtn.pbtn`) | **820 × 130** @ 화면 x130 y1531.25 (모달 본문 폭 100%) |
| 테두리 `--gb-bw` | **7px** → **절대배치 자식의 원점은 padding box 806 × 116** |
| 라벨 `<b>` 잉크(«룰렛 돌리기 (무료)») | 312.4 × 46 · 버튼 기준 x253.8~566.2 (가운데) |
| 좌·우 빈 폭 | 각 **253.8px** |
| 레드닷 `.updot`(321) | CSS `right:12 · top:12` → **화면 인셋 19** (⚠ 주석의 «24» 는 앞선 규칙이고 나중 것이 이긴다) |
| ▶AD 뱃지 `.ad`(367) | CSS `left:12 · top:19.8` · **99.1 × 76.4** → 화면 인셋 좌 19 · 위/아래 26.8 / 26.81 |
| 뱃지 ↔ 라벨 잉크 간격 | **138.2px** (겹침 0) |

⚑ **뱃지 규격은 지어낸 값이 아니다** — 13 광고 상품 뱃지(`.cn-cd>.bt>.ad`, 80px 버튼용 실측)를
**호스트 높이비 k = 130 / 80 = 1.625** 로 옮긴 한 벌이다. 이 버튼에 새 부품을 얹는 세션은 같은 k 를 쓰라.
