# 23 훈련 팝업 — 측정표

> 실측 기준: `docs/ref/23-훈련-팝업.jpg` (1080x2340), Python+PIL 픽셀 스캔, 2026-08-24 (sess-1715-31871).
> 마스크: 검정 = `max(RGB) ≤ 50~60` · 순백 = `min(RGB) ≥ 235~246` · 색상별 마스크는 항목마다 명시.
> 모든 수치는 **레퍼런스 원본 절대 px**. 가로는 구현에서 1:1 로 쓴다.
> 세로는 **시트가 하단 고정**이므로 `local y = ref y − 924` (시트 상단 기준) 로 변환해 쓴다.
> ⚠ 첫 측정 세션(sess-1258)은 기록 없이 죽었고, 이 표는 sess-1715 가 처음부터 다시 잰 것이다.

## 0. 화면 구조 · 세로 좌표계

| 구간 | ref y | 내용 |
|---|---|---|
| 0..919 | 게임 화면(전투·HUD)이 그대로 보임 | 시트 위쪽 |
| **920..927** | 시트 **검정 상단 테두리** (컬럼 1061개 중 972개가 y924 에서 검정→갈색 전환) | 두께 6~8px |
| 928..933 | 갈색 밝은 하이라이트 띠 `#A17D52` | h6 |
| 934..1024 | 헤더 갈색 본체 `#71462E` | h91 |
| 1025..1033 | 헤더 아래 어두운 띠 `#553622` | h9 |
| 1035..2159 | 시트 베이지 바탕 `#E6C695` | h1125 |
| **2160..2164** | 시트 하단 검정 경계 | 시트 bottom = **2164** |
| 2165..2340 | 게임 하단 내비(레퍼런스 게임 것) | 우리 탭바 자리 |

- **시트 border-box = y920..2164 → 높이 1245.** 폭은 전폭(x0..1079), 좌우에 검정 테두리 없음
  (y1600 좌측 x0~13 이 전부 베이지 `#E6C695`).
- **상단 모서리 radius ≈ 28** — 좌상단에서 갈색 최좌측 x 가 y928:34 → y936:16 → y948:9 → y956:7 로 수렴.
  (「물결 곡선」이라고 적혀 있었으나 **실측은 직선 + 라운드 코너**다. 1061 컬럼 중 972 컬럼이 같은 y924 에서 전환)
- 우상단도 대칭(y928:1050 → y952:1072).

## 1. 헤더 «훈련»

| 항목 | 값 |
|---|---|
| 헤더 갈색 밴드 | y934..1024 (h91), 전폭 |
| 밴드 색 | 본체 `#71462E` (113,70,46) · 상단 하이라이트 `#A17D52` (161,125,82) h6 · 하단 그늘 `#553622` (85,54,35) h9 |
| «훈련» 흰 잉크 | x495..581 (w87) · y951..1001 (h51) · **중심 x538** |
| «훈련» 검정 스트로크 포함 | x490..589 (w100) · y946..1009 (h64) → **한쪽 5px → `-webkit-text-stroke:10px`** |
| 환산 font-size | 한글 잉크 51 / 0.93 ≈ **55px** (2글자 폭 87 → 글자당 43.5) |

## 2. 안쪽 크림 박스

| 항목 | 값 |
|---|---|
| border-box | x17..1062 · y1063..2152 (테두리 포함) |
| 테두리 | `#CDA26C` (205,162,108) **6px** (x17..22 실측) |
| 내부 | x23..1056 (w1034) · y1069..2146 (h1078) |
| 내부 색 | 상단 `#F3E6CD` (243,230,205) → 카드 구간 `#EDDABC` (237,218,188) 로 살짝 어두워짐 |
| radius | 코너 스캔상 ≈ 20 |

## 3. 리본 «훈련 5 단계»

- 전체 bbox **x264..814 (w551) · y1098..1206 (h109)**, 중심 x539.
- 형상: 가운데 바가 x~298..783, **좌우 꼬리(깃)가 x264..298 / x783..814 로 돌출**.
  행별 실측 — y1098: 312..770 / y1114: 299..783 / y1138: **264..814** / y1162: 274..804 / y1194: 266..812.
- 색: 본체 `#6B4A34` (107,74,52) · 그늘/테두리 `#4C311F` (76,49,31).
- 흰 잉크(«훈련», «단계») **x425..654 (w230) · y1117..1162 (h46)** → font ≈ 46/0.93 ≈ **49px**
- 주황 숫자 «5» **x525..549 (w25) · y1122..1158 (h37)** → 숫자 font ≈ 37/0.70 ≈ **53px**, 색 `#FA8153` (250,129,83)

## 4. 진행바 + ↑ 버튼

| 항목 | 값 (border-box 기준) |
|---|---|
| 진행바 | **x194..855 (w662) · y1228..1282 (h55)** |
| 테두리 | 검정, 좌측 x194..202 실측 → **8px**, radius ≈ 27 (완전 알약) |
| 트랙(빈 구간) | `#1C1E2B` (28,30,43) |
| 초록 채움 | **x203..423 (w221) · y1235..1275 (h41)**, 색 `#9EF12F` (158,241,47) — 좌측 하이라이트 (165,236,70) |
| 채움 비율 | 채움 221 / 내부 645 = **34%** (표시값 98/300 = 32.7% 와 일치) |
| «98/300» 흰 잉크 | **x481..598 (w118) · y1243..1265 (h23)** · 중심 x539.5 → 숫자 font ≈ 23/0.70 ≈ **33px** |
| ↑ 버튼 border-box | **x855..962 (w108) · y1203..1307 (h105)** · radius ≈ 28 (행별 폭: y1205 w72 → y1230 w122) |
| 버튼 면 | 은색 `#989898` (152,152,152), 바깥 밝은 림 `#D3D3D3` (211,211,211) 6px, 검정 테두리 |
| 화살표 | 초록 `#B0EA26` (176,234,38) 잉크 x893..919 (w27), 검정 스트로크 x876..940 → 한쪽 **8px** |
| 비고 | 레퍼런스는 98/300(미달) 상태라 **버튼 면이 은색(비활성)** 이고 화살표만 초록이다 |

## 5. 배수 탭 x1 / x10 / x30

| 항목 | 값 |
|---|---|
| 바 전체 | **x159..925 (w767) · y1328..1400 (h73)** (아래 y1401..1407 에 그늘) |
| 칸 경계 | 구분선 x401..405 · x664..668 (색 `#A0684 5`(160,104,69)) → 칸 **x159..400 (w242) · x406..663 (w258) · x669..925 (w257)** |
| 선택 칸(x1) | 밝은 살구 `#FCD2AC` (252,210,172) |
| 비선택 칸 | `#B8825E` (184,130,94) · `#C38D69` (195,141,105) |
| 상단 하이라이트 | y1328..1332 `#E6C09C`, 하단 그늘 y1388..1396 `#A2684 3` |
| 글자 | 회백 `#F9F1EF`(선택) / `#C6BCBA`(비선택), 잉크 높이 ≈ 22 (y1354..1376) |
| 레드닷 | 칸 우상단. 1번 칸 실측 **x394..412 (w19) · y1326..1344 (h19), 중심 (403,1335)** → **Ø19**. 2·3번 중심 x656 · x909 |

## 6. 스탯 카드 3장

검정 세로 기둥(테두리) 실측: **(35..42) (353..360) | (377..384) (694..702) | (718..724) (1036..1042)**

| 항목 | 값 |
|---|---|
| 카드 border-box | 카드1 **x35..360 (w326)** · 카드2 **x377..702 (w326)** · 카드3 **x718..1042 (w325)** |
| 세로 | **y1436..1945 (h510)** (검정 가로줄 1436..1441 / 1940..1945) |
| pitch | **342** (간격 17) · 3장 전체 x35..1042, 중심 x538.5 |
| 테두리 | 검정 **6~9px** (기둥 두께 8, 상하 6) |
| radius | 좌상 코너 y1436:x61 → y1444:x46 → y1464:x35 → **radius ≈ 26** |
| 카드 배경 | `#FFFDEE` (255,253,238) |

### 카드 내부 (카드1 기준, 괄호는 카드 좌상단 기준 local)

| 요소 | ref 좌표 | local | 색 |
|---|---|---|---|
| 상단 헤더 밴드 | y1448..1532 (h85) | y12..96 | `#EEC889` (238,200,137) ~ `#F3D39A` 상단 |
| «Lv. 380» 흰 잉크 | x107..290 (w184) · y1468..1505 (h38) | x72..255 · y32..69 | 흰 + 검정 스트로크(한쪽 ≈5px) |
| 아이콘 잉크 | x123..302 (w180) · y1566..1729 (h164) | x88..267 · y130..293 | 중심 x212.5 (카드 중심 197.5 대비 **+15**) |
| 초록 증가치 «+480» | 잉크 x137..260 (w124) · y1734..1769 (h36) | x102..225 · y298..333 | 초록 잉크, 스트로크 포함 x132..265 · y1730..1774 |
| 스탯명 «햄지 공격력» | 갈색 잉크 x129..265 (w137) · y1787..1813 (h27) | x94..230 · y351..377 | `#6E502F` 계열 — **흰색 아님, 검정 스트로크 없음** |
| 하단 바 | y1832..1938 (h107) | y396..502 | `#BBA38B` (187,163,139) + 하단 라인 `#AA7F54` (170,127,84) h4 |
| 골드 아이콘 | x125..199 (w75) · y1860..1904 (h45) | x90..164 · y424..468 | 노랑 `#F9EB6E` (249,235,110) |
| 비용 숫자 | 검정 스트로크 bbox x200..345 · y1862..1934 | x165..310 | 흰 잉크 + 검정 스트로크 |
| 레드닷 | 중심 (352.5, 1446) **Ø25** | 우상단 모서리에 걸침 | 빨강 `#EF3457` (239,52,87) |

## 7. 하단 서브탭 4칸

> **⛔ 폐기(2026-08-25, 작업 88 — 저장소 주인 지시)**: 서브탭 행은 47 (4칸→«훈련·스탯 훈련» 2칸)을 거쳐
> 88 에서 **통째로 제거**됐다(스탯 훈련·캐릭터 레벨·스탯 포인트 시스템 폐기 — 훈련 단일 화면).
> 아래 실측은 레퍼런스 기록용으로만 남긴다. 이 절의 «아트 필요» 항목도 발주 불필요.

| 항목 | 값 |
|---|---|
| 바 border-box | **x54..1013 (w960) · y2021..2117 (h97)** |
| 테두리 | 검정 6px (x54..59 / y2021..2026 / y2112..2117) |
| radius | 좌우 끝이 완전 라운드(행별 폭 y2024 w913 → y2064 w962) → **radius ≈ 48** |
| 칸 | 4등분 **240px** — 칸 중심 x174 / 414 / 654 / 894 |
| 활성 칸(1번 «훈련») | 배경 `#4B3E2D` (75,62,45) — **비활성보다 어둡다** |
| 비활성 칸 | `#5D5039` (93,80,61) ~ `#61503E` |
| 활성 라벨 | 탄색 `#E7B88E` (231,184,142) 잉크, 잉크 높이 ≈ 30 |
| 잠금 아이콘 | 흰 자물쇠 — 2·3·4번 칸 중심 x421 / 658 / 896, 잉크 x397..445 (w49) · y2055..2090 |

## 8. 팔레트

| 색 | RGB | 샘플 좌표 | 용도 |
|---|---|---|---|
| `#71462E` | (113,70,46) | (960,40) | 헤더 갈색 본체 |
| `#A17D52` | (161,125,82) | (931,40) | 헤더 상단 하이라이트 |
| `#553622` | (85,54,35) | (1029,40) | 헤더 하단 그늘 |
| `#E6C695` | (230,198,149) | (1600,5) | 시트 베이지 바탕 |
| `#CDA26C` | (205,162,108) | (1600,19) | 크림 박스 테두리 |
| `#F3E6CD` | (243,230,205) | (1090,540) | 크림 박스 내부(상단) |
| `#EDDABC` | (237,218,188) | (1600,26) | 크림 박스 내부(카드 구간) |
| `#6B4A34` | (107,74,52) | (1150,700) | 리본 본체 |
| `#4C311F` | (76,49,31) | (1150,780) | 리본 그늘 |
| `#FA8153` | (250,129,83) | (1125,540) | 리본 숫자 주황 |
| `#1C1E2B` | (28,30,43) | (1255,700) | 진행바 트랙 |
| `#9EF12F` | (158,241,47) | (1255,300) | 진행바 초록 채움 |
| `#B0EA26` | (176,234,38) | (1255,905) | ↑ 화살표 초록 |
| `#989898` | (152,152,152) | (1255,868) | ↑ 버튼 은색 면 |
| `#FCD2AC` | (252,210,172) | (1365,200) | 배수 탭 선택 |
| `#B8825E` | (184,130,94) | (1365,450) | 배수 탭 비선택 |
| `#FFFDEE` | (255,253,238) | (1550,198) | 카드 배경 |
| `#EEC889` | (238,200,137) | (1520,198) | 카드 헤더 노랑 |
| `#BBA38B` | (187,163,139) | (1850,90) | 카드 하단 바 |
| `#EF3457` | (239,52,87) | (1446,352) | 레드닷 |

## 9. 불확실 항목

- **리본 꼬리 형상** — 행별 좌우 경계가 y1130·y1186 두 구간에서 넓어졌다 좁아진다.
  「가운데 바 + 좌우 깃」으로 해석했으나 JPEG 번짐 때문에 깃의 정확한 다각형은 ±4px 불확실.
- **카드 비용 숫자 잉크** — 골드 아이콘과 같은 행이라 흰 마스크가 아이콘 하이라이트를 물어
  순백 bbox 가 비었다. 검정 스트로크 bbox(x200..345)만 신뢰할 것.
- **스탯명 색** — 갈색 계열인 것은 확실하나 안티에일리어싱 때문에 대표색은 `#6E502F` ±10.
- 서브탭 2·3·4번의 자물쇠는 흰색 글리프로만 확인했고, 4번 칸의 «마법상판» 라벨은
  이 캡처에서 자물쇠에 가려 **읽히지 않는다**(UI-REFERENCE 기술을 따랐다).

---

## 아트 필요 표 (2026-08-25 규칙 — GPT 아트 발주서용)

> 이모지·CSS 로 대체한 요소는 **레퍼런스 bbox(w×h·중심)를 그대로 차지**하게 만들어 두었다.
> 나중에 이미지 파일로 **교체만** 하면 되도록 하는 것이 목적이다.
> 좌표는 전부 **레퍼런스 절대 px**(1080x2340). 구현 좌표는 `가로 1:1 · 세로 y − 425`.
>
> **8회차(2026-08-24, sess-2016-5369) 갱신** — 코인·잠금 아이콘·리본 꼬리 3행을 실측으로 고쳤다.
> 이 환경에는 PIL 이 없어 스캔은 `probe23.js`(playwright 캔버스)로 했다.

| 요소 | ref bbox (w×h) | 중심 | 용도 | 현재 대체물 |
|---|---|---|---|---|
| 카드1 메인 아트 | 180×175 (x123..302 · y1565..1739) | (212, 1652) | 공격력 카드 아이콘 | 이모지 `⚔️` 105px |
| 카드2 메인 아트 | 175×175 (x464..638 · y1565..1739) | (551, 1652) | 체력 카드 아이콘 | 이모지 `❤️` 105px |
| 카드3 메인 아트 | 152×175 (x805..956 · y1565..1739) | (880, 1652) | 체력 회복 카드 아이콘 | 이모지 `✨` 105px |
| 카드 아트 뒤 슬롯 플레이트 | 150×150 (마름모 bbox) | 카드 중심 x · y1640 | 아트가 얹히는 판 | **CSS 로 구현됨**(45° 회전 라운드 스퀘어 `#EAD6B3`) — 아트 불필요 |
| 카드 비용 골드 코인 | **53×55** (x121..173 · y1856..1910, 카드1) | (147, 1883) | 골드 재화 아이콘 | 이모지 `💰` `fs49 + scaleX(1.08)` |
| 서브탭 잠금 아이콘 | **59×60** (x629..687 · y2040..2099, 3번 칸) | (658, 2069.5) | 미해금 표시 | 이모지 `🔒` `fs57 + scaleX(1.43)` — **작업 47 이 이 탭을 없앤다(발주 불필요)** |
| ↑ 버튼 화살표 | 초록 잉크 57×53 (머리 h29 최대폭 55 + 자루 h22 폭 27) | 버튼 중심보다 **8px 위** | 단계 상승 버튼 | **⏸ 아트 대기(18회차 등재)** — 인라인 SVG 폴리곤으로는 «날개 두께» 를 못 맞춘다. ref 는 **모서리가 볼록한 곡선**이라 x18.5 열(=화면 x890)에서 초록이 svg y24..41(**17~18px**)인데, 꼭짓점(36.5,8)에서 밑변 모서리로 **직선**을 그으면 같은 열이 최대 14px 이다(현재 13px). bbox 57×52 · 외곽선 4~5px · 밑변 svg41 은 이미 Δ≤1 이므로, **곡선 화살표 아트(57×52, 투명 배경)** 가 들어오면 그때 채운다 |
| 리본 꼬리 접힌 깃 | 76×79 · **2로브 제비꼬리**(외곽 최대 x265@y1141 · 노치 x278@y1166 · 2로브 x267@y1190) + 안쪽 접힘 밴드 26px | 좌 (302, 1165) / 우 (776, 1165) | 리본 장식 | **CSS 27점 `clip-path` 로 구현됨 — 아트 불필요**(8회차, 전 행 Δ≤2px) |
| 시트 배경 텍스처 | 전면 | — | 베이지 바탕 무늬 | 단색 `#E6C695` |

**주의** — 아트를 실제 이미지로 교체하면 카드 아이콘의 `font-size:105px` 는 없어지고
`.tr-card>.ci::before` 슬롯 플레이트는 **그대로 둔다**(ref 도 아트 «뒤»에 판이 따로 있다).
이모지를 150px → 105px 로 줄인 것은 «이모지가 판을 통째로 덮어서» 이므로,
투명 배경 아트로 교체하면 원래 bbox(위 표)로 되돌리는 것이 맞다.

---

## §271 — 두 겹 탭 기하 (2026-08-28, sess-0046-4531 워커 C)

> **이 팝업 최초의 «두 겹 탭» 이다.** 주인 정정(2026-08-27): «룬은 행으로 나눠졌다기보다
> 일반룬·고급룬·천상룬이 **탭으로** 나눠져 있게». 상위 탭(훈련·룬·단련) 안에 하위 탭 3칸이
> 생기므로 선례가 없는 기하를 여기서 못 박는다. 레퍼런스 스크린샷이 없는 신규 시스템이라
> **ref 실측이 아니라 «이 팝업이 이미 쓰는 규약에서 유도한 값»** 이다 — 유도 근거를 같이 적는다.
> 좌표는 전부 `.tr-box` local(박스 1046×1093) 또는 룬 본문 local(998×778).

### 1. 두 바의 자리 — 상·하위가 **같은 세로선** 위에 선다

| 바 | 클래스 | 박스 local | 유도 근거 |
|---|---|---|---|
| 상위(훈련·룬·단련) | `.stabs.sp3.tr-subs` | **left 126 · bottom 40 · w 794 · h 99** | 203 이 03 던전(`.dns-sub`)에서 물려받은 값. **안 건드린다** |
| 하위(일반·고급·천상) | `.stabs.sp3.rn-subs` | **left 126 · top 34 · w 794 · h 99** | left·width 를 상위 바와 **같은 값**으로. 96 «다른 공용 바는 전부 대칭» |

- 하위 바가 리본 자리(local 34)를 그대로 쓴다 → **23 훈련 탭의 좌표는 한 값도 안 움직인다**
  (훈련 탭에서는 `display:none` — `.tr-runes` 와 같은 «번갈아 쓰는 형제» 규약).
  게이트 `verify271` [2] 가 203 이전 5요소 표(rib 247,34,551,108 · prog 177,165,668,55 ·
  up 838,139,108,107 · qty 142,265,761,75 · cards 0,373,1046,510)로 Δ0 을 잰다.
- 두 바가 x 로 정확히 겹쳐 서므로 «같은 부품의 두 층» 으로 읽힌다. 층 구분은 **자리**로만 준다
  (상위 = 박스 바닥 / 하위 = 본문 머리). 부품 자체(높이 99 · fs 43/41 · 활성 알약)는 96 것 그대로.

### 2. 세로 예산 — 쓸 수 있는 자리 920 (박스 local 34 ~ 954)

```
하위 바  99   (34 ~ 133)
사이     24
카드    700   (157 ~ 857)      ← 본문 local 0 ~ 700
사이     32
총효과   46   (889 ~ 935)      ← 본문 local 732 ~ 778
────────────
합      901   (바닥 여유 19)
```
`.tr-runes` = box local **left 24 · top 157 · 998×778**.
⚠ 카드 높이를 바꾸려면 이 뺄셈부터 다시 하라 — 203 이 268×3 으로 짤 때 288 로 잡았다가
요약줄이 3번 카드 위로 66px 올라탄 전례가 있다.

> ### §271 정오표 — 687 재화 잔량 헤더 (2026-09-01, sess-1816-20787 · 3회차 확정값)
> 주인 지시(2026-09-02 01:00 «룬강화석 개수를 일반룬·고급룬·천상룬 탭 **위에**») 로 본문 머리에
> `.rn-hd`(24/**24**/998/**88** — 단련 `.tp-hd` 와 **같은 부품 급**(높이까지 88), «(아이콘) 수량»
> 중앙 정렬 · fs52 · 아이콘 = **TR_CUR_PX 53**(584 기준선 — 버튼과 같은 스케일)) 가 섰다.
> 회차 이력: 1회차 h56/fs36/아이콘36 → 비평 2인 최저 5(«숫자가 최소 티어 · 띠가 유일하게 얇다 ·
> 바와의 사이 9px 만 리듬 밖») → 2회차 h64/fs46/아이콘40 → 최저 6·7(«같은 재화 아이콘이 버튼 53
> 과 다른 스케일 · 숫자가 탭 라벨(39) 아래 · 안여백 4~5px») → 3회차 h88/fs52/아이콘53.
> 헤더 top 은 «리본 자리 34» 가 아니라 **24** — 그 34 는 훈련 탭 리본의 값이고, 띠가 서면 위 여백
> 16 이 간격 리듬(15~20)과 맞는다(688 이 단련 쪽을 이 부품으로 끌어올 때 top 도 같이 볼 것).
> 위 §1·§2·§3 의 아래 값들이 개정됐다(주인 지시 우선의 의도적 이탈 — 새 뺄셈은 review 687 §2):
> - 하위 바 `.rn-subs`: top 34 → **132** (헤더 24~112 · 사이 20)
> - `.tr-runes`: top 157 → **246** · 높이 778 → **702**
> - 카드 `.tr-rn`: 700 → **648** (잉크 없는 세로 여백만 걷었다 — `.rd` 280→**256** ·
>   `.rst` 452→**428** · `.rbt`/`.rmax` 512→**478** · `.rhint` 632→**598** · `.rlk` lh 684→**632**)
> - 총효과 `.rsum`: 본문 local 732 → **664** · 높이 46 → **38** (박스 910~948 · 바닥 여유 19 → 6)
> 게이트: `verify687`(신설) · `verify271`(«바 top 34» → «헤더 24/88 + 바 132» 방향 이관).

### 3. 카드 내부 (998×700 → **648** — §271 정오표, 카드 좌상단 기준 local)

| 요소 | 클래스 | left/right · top | w×h | 비고 |
|---|---|---|---|---|
| 아이콘 액자 | `.ri` | l44 · t40 | 200×200 | radius 32 · 검정 테 8 · fs118 — **아트 자리** |
| 이름 | `.rn` | l276 · t46 | –×68 | fs58 |
| 레벨 | `.rl` | l276 · t126 | –×50 | fs36 |
| 진행바 | `.rb` | l276 · t190 | 500×34 | 트랙+채움 2겹(`.sk-slv` 계열) |
| 성공 확률 | `.rp` | r44 · t36 | 320×116 | 숫자 fs66 / 라벨 fs30 — 카드에서 제일 큰 글자 |
| 효과 블록 | `.rd` | l44 · t280 | 910×162 | **축 하나가 한 줄**, 줄 높이 54, 최대 3줄 |
| └ 현재값 | `.rd>.rw>i` | l0 | 610×54 | fs34 — 「공격력 +6272%」 |
| └ 다음 1레벨 | `.rd>.rw>s` | r0 | 300×54 | fs28 우측정렬 — 「다음 +26%」 / 만렙 「최대」 |
| 계단 | `.rst` | l44 · t452 | 910×40 | fs28 — 「계단 5 / 5 — Lv 400~499 구간은 1레벨당 ×6.6」 |
| 시도 버튼 | `.rbt.b1/.b2` | r534 / r44 · t512 | 420×112 | fs38 · 사이 74 |
| MAX 판 | `.rmax` | r44 · t512 | 910×112 | 두 버튼 자리를 한 장으로 |
| 실패 안내 | `.rhint` | l44 · t632 | 910×40 | fs28 가운데 — 「실패해도 레벨은 그대로입니다」 |
| 잠금 덮개 | `.rlk` | inset 8 | 982×684 | line-height 684 · fs40 |

### 4. 하위 탭 칸의 잠금 표시

- 잠긴 칸: `.stab.lk` → 라벨 `opacity .45` + `.sk-lock`(272 부품)을 **칸 우측** `right 16 · top 14 · scale .62`.
  ⚠ 공용 규칙 `.stabs .sk-lock{left:50%}` 은 «칸 한가운데» 라 라벨 위에 그대로 얹힌다 — 여기서만 덮어쓴다.
- 잠긴 칸도 **눌린다**. 누르면 그 룬 카드가 뜨고 `.rlk` 덮개가 개방 조건을 말한다(186 관례).
  토스트를 겹쳐 띄우지 않는다 — 조건 문구가 이미 화면에 있다.
- 칸별 레드닷(`.bdg`)은 166 규약 그대로 «지금 **재료로** 시도할 수 있는 칸» 에만. 다이아는 상시 가능하므로 뺀다.

### 5. 아트 필요 (271분 — 위 «아트 필요 표» 에 이어서)

| 요소 | bbox (w×h) | 중심(카드 local) | 용도 | 현재 대체물 |
|---|---|---|---|---|
| 룬 아이콘 (3종) | **200×200** | (144, 140) | 일반/고급/천상 룬 아트 | 이모지 `🔹`/`🔷`/`🔶` fs118, 액자 면색 `--rt` |
| 하위 탭 자물쇠 | 49×57 → `scale(.62)` = **30×35** | 칸 우상단 (칸폭−16, 14) | 미개방 룬 칸 표시 | `.sk-lock` CSS 부품(272) — 아트 불필요 |

> 룬 아이콘만 실제 아트가 필요하다. 액자(200×200, radius 32, 검정 테 8)는 CSS 로 두고
> **투명 배경 200×200 아트**를 안에 넣으면 된다 — 이모지의 `font-size:118px` 만 걷어내면 그대로 자리가 맞는다.

---

### 📌 정오표 — 작업 356 7회차 (2026-08-29, 저장소 주인 지시 · **의도적 이탈**)

**카드 두 자리의 «폭을 ref 에 맞추는» 손잡이를 걷었다.** 주인 지시(«원래 이미지 비율대로»)가
레퍼런스보다 우선한다(354 선례).

| 자리 | 옛 규격(이 표의 근거) | 새 규격 | 왜 |
|---|---|---|---|
| `.tr-card>.ci` ⚔️ | 16회차 «fs 165 + `scale(.829,.893)` ⇒ 잉크 152×151 = ref Δ0» | **`translate(0,1.63px) scale(.81183)` ⇒ 151×151** | 자연 잉크가 **186×186(정사각)** 이라 두 축을 따로 눌러야만 152×151 이 나왔다 = 종횡 0.928 로 찌그러진 상태. contain `min(152/186, 151/186)` |
| `.tr-card>.cb>s` 코인 | 18회차 «fs 50.9 + `scaleX(.968)` ⇒ 잉크 53×55 = ref Δ0» | **`scale(.96364)` ⇒ 53×53** | 자연 **55×55(정사각)**. contain `min(53/55, 55/55)` |

⚠ **⚔️ 만 되돌림 이동이 붙는다** — `line-height:205` > `height:164` 라 잉크 중심이 요소 상자 중심보다
14px 아래이고, `scaleY` 를 `.893 → .81183` 으로 내리면 잉크가 상자 중심 쪽으로 끌려가
**중심이 1.63px 위로 뜬다**. 16·22회차가 ref 에 맞춰 둔 중심(1640)을 지키려고 그만큼 되민다 —
**중심 Δ0 · 레이아웃 상자 Δ0px.** 코인 쪽 되돌림값은 (−0.01, −0.09) 로 측정 분해능 아래라 안 적었다.

남는 잔차는 «높이 −2px(코인)» 하나뿐이고 CSS 로는 못 닫는다 — **ref 종횡의 아트가 들어오면
같은 s 가 그대로 정답**이 된다. 역산기 `node tools/cal356r7.js` · 게이트 `verify356` [S2] · [R7].

---

### 📌 정오표 — 작업 584 (2026-08-31, 저장소 주인 지시 · **의도적 이탈 3자리**)

주인 지시(«업글 버튼 크기 존내 작으니까 더 크게» · «룬부분 아이콘 … 다른 팝업들의 슬롯들처럼» ·
«룬, 단련 버튼에 화폐 크기 … 훈련 팝업에서 처럼»)가 레퍼런스보다 우선한다(354·425 선례).

| 자리 | 옛 규격(이 표의 근거) | 새 규격 | 왜 · 무엇이 값을 정했나 |
|---|---|---|---|
| `.tr-up` ↑ 버튼 | 8회차 «ref y1202..1308 = **108×107** @ (838,139) · radius 34» | **128×128 @ (838, 128.5)** · radius 40(안쪽 34) | 두 축을 잠그고 남는 한 축만 푼다 — 좌변 838 고정(진행바 캡을 7px 덮는 관계) · 세로 중심 **192.5 = 진행바 중심** 고정 ⇒ `192.5 + S/2 ≤ 265(.tr-qty) − 8` ⇒ **S = 128**. 화살표 svg 는 같은 배율 ×1.1852(73×69 @16.5,11.5 → 86.5×81.8 @19.6,13.6) — 22회차의 «잉크 중심이 버튼 중심보다 위» 광학 보정 보존. **링 두께 6/13 은 안 키웠다**(팝업의 베벨 문법) |
| 룬 아이콘 `.tr-rn>.ri` | 위 «아트 필요» 표 «이모지 `🔹` **fs118**, 액자 200×200» | 액자 **200×200 그대로** · 안쪽 그림이 `RUNE_ART`(**149.6×139.1**)를 채운다 | fs118 은 이모지 잉크비(0.53~1.16)를 못 이긴다 — 🔹 실측 잉크 **48.38 = 액자의 0.2419**(411 슬롯 비의 34.8%). 411 의 `SLOT_ART`/`.sk-slot 115` 비를 200 으로 옮긴 **파생값**이고 손으로 적은 수가 아니다 ⇒ 잉크비 **0.6956**(411 대비 100.0%) |
| 단련 [단련] 버튼 `.tr-tp>.tb` | 210 «right 26 · top 128 · **300×74**» · 라벨 «투자 n pt» | **340×74**(right 26 불변) · 라벨 «단련 🪨 n pt» | 아이콘 53 + 여백 6 이 들어와 300 은 **8자리부터** 넘친다(`probe584` §E). 넓히는 쪽은 왼쪽이고 좌변 **632** ↔ 같은 행 `.td` 우변 586 = 46px 여유 |

**화폐 아이콘 기준선(신설)** — `TR_CUR_PX = 53`. 주인이 «훈련 팝업에서 처럼» 이라고 기준을 지목했으므로
훈련 카드 코인(`.tr-card>.cb>s` fs50.9 × `.cic` 1.08em × `scale(.96364)` = **52.97**)이 그 값이다.

| 화폐 자리 | 수리 전 | 수리 후 |
|---|---|---|
| 룬 [강화] `.rbt` | 34 (기준선의 64.2%) | **53** · `vertical-align:-15.5px`(상하 29.5 대칭) |
| 단련 [단련] `.tb` | **0장** | **53** · `vertical-align:-17.5px`(상하 10.5 대칭) |
| 단련 [충전] `.cg` | 26 | **26 — 못 올린다** |
| 단련 [회수] `.tp-ft>.rb` | 32 | **32 — 못 올린다** |

⚠ **뒤 두 자리가 «안 한 것» 이 아닌 이유를 여기 남긴다.** [충전]은 577 이 라벨을 **27px 두 줄**로
못박은 자리라(한 줄이면 7자리부터 접혀 헤더 88 밖으로 샌다) 53px 그림이 27px 행에 안 들어가고,
[회수]는 `.tp-ft` 88px 띠 안의 66px 버튼이다. 둘을 올리려면 **210 이 정한 단련 탭 세로 예산 898**
(= 88 + 14 + 222×3 + 14×2 + 14 + 88)을 통째로 다시 짜야 한다 — 그 일은 이 행의 지시가 아니다.

게이트 `node tools/verify584.js`(52/52) · 재현기 `node tools/probe584.js` ·
이관 `verify203` [2] · `verify210` [B] · `verify271` [2] 의 `.tr-up` 핀.

## §612 정오표 — 단련 축 아이콘 확대 (2026-08-31, sess-1618-23172 워커 A · 주인 지시 «단련부분에 아이콘들 좀 작은데 크게되게해»)

주인 지시가 210 의 축 카드 내부 좌표보다 우선한다(354·425·584 선례). 행 껍데기(998×222 · top 102/338/574)와
584 가 정한 `.tc`(552,14)·`.tb`(632,128 · 340×74)는 **한 값도 안 움직였다.**

| 자리 | 옛 규격(210·584) | 새 규격 | 왜 · 무엇이 값을 정했나 |
|---|---|---|---|
| 축 아이콘 액자 `.tr-tp>.ti` | 26,22 · **104×104** · r20 · 링 6 · 이모지 fs62(잉크 69.4 = 룬 잉크의 절반) | 26,22 · **178×178** · r32 · 링 8 · 그림 = `TEMPER_ART`(**133.1×123.8**) | 크기는 뺄셈 — 행 222 에서 위 여백 22 를 아래에도 주고 남는 전부(= `.tr-up` 의 «두 축 잠그고 남는 축» 방식). 테는 «룬 .ri 규격을 축소한 것» 이라는 원 주석의 축소를 해제해 .ri(200) 규격 그대로. 잉크는 411 «그림 자리» 세 번째 자리 — `TEMPER_ART = SLOT_ART/115 × 178`(파생, 잉크비 0.6957 = 411 대비 100%) · 세 글리프가 saInk 정규화로 «서로 고른가» 1.012 |
| 이름 `.tn` · 레벨 `.tl` | left **150**(= 26+104+20) | left **224**(= 26+178+20) | 아이콘 우변이 130→204 로 오면서 여백 20 유지 |
| 효과 줄 `.td` | **26,150 · 560×36 한 줄** | **224,118 · 392×68 두 줄**(lh34 · «·» 자리를 `<br>` 로) | 한 줄 잉크 최악 411px(Lv 99,999) > 남는 폭 392 — 두 줄이면 최장 줄 ~208px. 우변 616 ↔ `.tb` 좌변 632 = 16px(verify584 [7-b] 초록 유지) |
| 488 봉투(플로터 레인) | `--hb-x .445`(중앙 빈 띠) · sw104 | `--hb-x .1152`(아이콘 중심 115) · **sw88 · pad12** | 글줄이 옛 빈 띠로 들어와 [I6] 이 [td]/[tn] 을 밟았다 — 훈련 카드 [I4] 규약(«아이콘은 정보 요소가 아니다»)대로 두 줄기를 액자 위로. sw104 면 왼 칸이 클램프에 11px 밀려([K3]) 88 로 |

게이트 `node tools/verify612.js`(25/25 — 액자·잉크비·파생·이웃 Δ0·두 줄 스윕·1600·되돌림) ·
이관 `verify488` [I6] `.ti` skip + **[I6b]** «봉투가 액자 안» 신설.

## 📌 실측 메모 — 작업 882 (2026-09-03, sess-1643-4572 워커 C · **제품 0줄 · 이탈 없음**)

이 표의 값을 **한 줄도 안 고친다.** 838-③ 이 «카드 하단 바를 세로로 키우자»(106 → 155)고 처방해 그 판을
실제로 만들어 쟀고, **기각**했기 때문이다(상세 `docs/review/882-훈련카드상자종횡비.md`). 다음 세션이 같은
실험을 다시 하지 않도록 **그때 잰 두 값만** 남긴다.

| 물음 | 실측 (`tools/probe882.js`) |
|---|---|
| 카드 바닥(local 883) ↔ 203 서브탭 바 상변(953)의 **빈 세로** | **70px** — 9:19(2280)·9:13.3(1600) **둘 다 같다**(686 이 단련 행에서 확인한 «두 프레임 행 기하 동일» 과 같은 꼴) |
| 카드를 키우면 무엇이 따라 움직이나 | 위쪽 잉크 넷(`.ch`·`.ci`·`.cv`·`.cn`)과 바 **상변**은 **Δ0** · 그러나 밴드 안 **코인·비용 잉크는 «가운데 정렬» 이라 밴드가 커진 절반만큼 내려간다**(버튼 118 판 +6px · 155 판 **+24.5px** — 이 표 §6 의 «골드 아이콘 local y424..468» 이 그만큼 어긋난다) |

⚠ 래칫 `node tools/verify882.js` (13/13) 가 이 두 값과 §6 의 카드 326×510 · 바 310×106 @ local y396 을 지킨다.
키우려면 `.tr-cards`·`.tr-card` 의 높이 두 값이면 되지만, **키운다고 버스트 산포가 좋아지지 않는다**(위 review §1).
