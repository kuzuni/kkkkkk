# 20 프로필 팝업 — 플레이어 스펙 정보(종합 스탯 탭) 측정표

> **출처**: `docs/ref/20-프로필-팝업-플레이어-스펙-정보.jpg` — 원본 **1080×2340**, 좌표 전부 절대 px(정수).
> **측정법**: python3 + PIL 픽셀 스캔(행/열 색 프로파일, 잉크 카운트). 눈대중 없음. JPEG 블러 감안해 색은 면 중앙에서 샘플.
> **폰트 규칙**: 한글 `font = 잉크높이 ÷ 0.93`, 라틴/혼합 `÷ 0.72`, 순수 숫자 `÷ 0.70`. 잉크높이 원값 병기.
> **껍데기**: `19-프로필-팝업.jpg` 와 **동일 셸**(19 crema 111–968 / 534–1891 → 완전 일치). 20 은 19 의 "종합 스탯" 탭 상태.

---

## §1. 껍데기 판정 — (다) 딤 위 가운데 다이얼로그

| 판정 근거 | 스캔 결과 | 결론 |
| --- | --- | --- |
| 폭 1080? | 패널 검정 외곽 x 92–98 / 981–987 → 폭 896 | full-bleed 아님 |
| 바닥에 붙나 | 패널 하단 1910, 화면하단 2340 → 아래여백 430 | 바텀시트 아님 |
| 딤 | 있음(§2). 바깥은 어두워진 게임 씬(하단 탭바 실루엣 보임) | — |
| 세로중심 | (515+1910)/2 = **1212.5** = 화면중심 1170 **+42.5** | 08/16 다이얼로그와 동일 오프셋 → **(다) 가운데 다이얼로그** |

딤 불투명도: 02(딤 없음) 하단 탭바 동일좌표 9점 비교, 채널비 평균 0.444 / 중앙값 0.456 → **딤 ≈ `rgba(0,0,0,0.55)`** (±0.03).

## §2. 다이얼로그 박스

| 항목 | 값 |
| --- | --- |
| 바깥 박스(검정 외곽선 포함) | x **92–987** (w **896**), y **515–1910** (h **1395**), 중심 (539.5, 1212.5) |
| 테두리 겹(안→밖, y1000 좌변) | ① 하이라이트 1px `#887661` → ② 갈색 **11px** `#564438` (x99–109) → ③ 검정 **7px** `#000000` (x92–98) |
| 내부 크림 박스 | x **111–968** (w **858**), y **534–1890** (h **1357**), 색 `#F0D9BA` (민무늬) |
| 코너 radius | 바깥 ≈ **42–45**, 크림 내부 ≈ **24** (좌상 arc: y534 x129 → y557 x111 직선화) |
| 닫기(✕) 버튼 | **없음** (우상단 y540–700·x840–965 어두운 px 23개 = 노이즈뿐) |

※ 좌표는 검정 외곽선 **바깥** 기준.

## §3. 상단 Gamer Id 텍스트

| 항목 | 값 |
| --- | --- |
| 내용 | `Gamer Id: 6df25ce0-9f0c-11f1-9ebb-0d86c4900cc3` |
| 잉크 | y **555–569** (h **15**), x **256–822** (중심 539 = 패널중심) |
| 색 | 흰색 `#FBFBFD`, 외곽선 없음(가는 그림자 정도) |
| 폰트 | 라틴 15 ÷ 0.72 ≈ **21px** |

## §4. 아바타 액자

| 항목 | 값 |
| --- | --- |
| 바깥 bbox | x **451–629** (w **179**), y **594–773** (h **180**), 중심 x540 |
| 테두리 겹(밖→안, y680) | 검정 ~5px `#000` → 갈색 **12px** `#715A4A` (x456–467) → 검정 라인 2–3px → 초상화 |
| 초상화 내부 | ≈ x472–608, 배경 파랑 `#3B5A86` 계열 + 캐릭터 |

## §5. 칭호 리본 「칭호 없음」

> **정오표(834, 2026-09-02)** — 「칭호 없음」은 **레퍼런스 플레이어가 칭호를 안 달고 있었다**는 뜻이지
> 이 자리의 라벨이 상수라는 뜻이 아니다. 아래 값은 전부 **기하**이고 Δ0 으로 그대로 살아 있다.
> 라벨만 «장착 칭호» 파생으로 바뀌었다(`#spcTtl` · 19 `#pfTtl` 과 같은 `paintTitleChips()` 한 곳).
> ⚠ 텍스트 잉크는 라벨마다 폭이 다르다 — 실측(834) **최단 «골드» 48.7 ↔ 최장 «그랜드마스터» 151.3**(면 300 안쪽,
> 최장에서도 좌우 여유 **74.3px**). 폭을 다시 잴 일이 있으면 **최장 라벨로** 재라(736 «최장 라벨을 담는가» 와 같은 자).
> `verify834` [H] 가 이 자리를 못박는다.

| 항목 | 값 |
| --- | --- |
| 밴드 | y **784–837** (h **53**), 최대폭 x **380–698** (w **318**), 좌우 뾰족한 6각 배너 |
| 색 | 채움 `#433A35`, 안쪽 사면 하이라이트 `#635145`, 검정 외곽선 ~3px |
| 텍스트 잉크 | y **797–824** (h **28**) → 한글 28 ÷ 0.93 ≈ **30px**, 흰색 채움 |
| 아바타와 gap | 773 → 784 = **11** |

## §6. 닉네임 스트립 + 편집 버튼

| 항목 | 값 |
| --- | --- |
| 스트립 밴드 | y **844–896** (h **52**), x ≈ **362–695** (편집버튼 앞까지), 좌단 ~30px 페이드 |
| 스트립 색 | 반투명 흑갈 오버레이: 중앙 `#736757`, x380 `#988974`(페이드 중), 크림 위 얹힘 |
| 텍스트 | `U_1787501115822`, 잉크 y **859–881** (h **23**), x ≈ 405–670 (중심 ≈537) → 라틴/숫자 23 ÷ 0.72 ≈ **32px**, 흰 채움 + 진갈 외곽선 ~3px |
| 편집(연필) 버튼 | x **694–750** (w **56**), y **844–897** (h **54**), radius ≈12, 테두리 4–6px `#52433C`, 채움 크림 `#F6E9C6`, 연필 아이콘 진갈 |
| 리본과 gap | 837 → 844 = **7**;  스트립 하단 → 리스트 상단 gap = 896 → 982 = **86** |

## §7. 스탯 리스트 (핵심)

### 7-1 컨테이너
| 항목 | 값 |
| --- | --- |
| bbox | x **139–938** (w **800**), y **982–1726** (h **744**), radius ≈ **20** (상/하 모서리 arc ~18px) |
| 패널 크림과의 여백 | 좌 139−111=**28**, 우 968−938=**30** |
| 스크롤 | 스크롤바 **없음**. 뷰포트 하단 y≈1726 에서 클리핑, 13번째 행 텍스트가 페이드로 y1733–1737 에 살짝 보임(스크롤 계속됨) |

### 7-2 행(row) 격자
| 항목 | 값 |
| --- | --- |
| 행 피치 | **60px** (경계 y: 1000·1060·1120·1180·1241·1301·1361·1420·1480·1540·1601·1661·1721, 오차 ±1) |
| 행 밴드 | row n = y 1000+60(n−1) … +60. 컨테이너 상단 982–1000(18px)은 row1 색으로 채워진 패딩 |
| 줄무늬 색 | 홀수행(1·3·5…) **`#FEEFD2`**(밝음) / 짝수행 **`#F6E2C7`**(어두움). 구분선 없음(색 교대만) |
| 보이는 행 수 | 12행 풀 + 13행 클리핑 |

### 7-3 행 내부 텍스트
| 항목 | 값 |
| --- | --- |
| 이름(좌) 잉크 | 좌변 x **176** 시작(외곽선 ~173), h **25–26** → 한글 ≈ **27–28px**. **흰 채움 `#F9F9F9` + 검정 외곽선 2–3px**(스템 스캔 (178,3)(203,3)…) |
| 값(우) 잉크 | 우변 잉크 끝 x **901**(외곽선 ~904, 우여백 ≈37), h **22–23** → 숫자혼합 ≈ **31–32px** |
| 값 색 | **전 행 동일 연두 `#BAFD60`** (1.09A 도, 100% 도, A% 도 모두) + 진색 외곽선 2–3px. "일반 값 vs 초록 %"의 구분 **없음** — 이 화면은 전부 초록 |
| 세로 정렬 | 잉크 중심 = 행 밴드 중심 (예: row2 잉크 1078–1102 중심 1090 = 밴드 1060–1120 중심) |

### 7-4 행 내용(위→아래, 값은 전부 `#BAFD60` 초록)
| # | 라벨 | 값 |
| --- | --- | --- |
| 1 | 햄지 공격력 | 1.09A |
| 2 | 햄지 체력 | 1.15A |
| 3 | 햄지 공격력 | 50.23A% |
| 4 | 햄지 체력 | 3.26A% |
| 5 | 햄지 일반 공격 피해량 | 100% |
| 6 | 햄지 일반 몬스터 피해량 | 0.4% |
| 7 | 햄지 보스 피해량 | 0.3% |
| 8 | 매옹이 공격력 | 1.15A |
| 9 | 매옹이 체력 | 1.15A |
| 10 | 매옹이 공격력 | 46.80A% |
| 11 | 매옹이 체력 | 3.17A% |
| 12 | 매옹이 이동 속도 | 0.2% |
| 13 | (클리핑, 추정 「매옹이 일반 공격 피해량 100%」) | — |

> **정오표 358(2026-08-29)** — 12행 「이동 속도」 는 **줄은 그대로 두고 값은 영구히 `0%`** 다.
> 저장소 주인 지시로 플레이어 이동 속도를 올리는 축을 전부 폐지했으므로(`UPG spd` 삭제 ·
> `stat.speed = PLAYER_SPEED` 상수) 이 행의 «증가율» 은 구조적으로 0 이다.
> 레퍼런스에 있는 줄이라 **지우지 않는다** — 지우면 행 수·세로 리듬이 어긋난다.
> 여기에 축을 다시 이어붙이지 마라(`verify358` [3]-ⓑ 가 «행은 있고 값은 0%» 를 못박는다).

## §8. 하단 토글 탭 (종합 스탯 / 프로필)

### 8-1 컨테이너
| 항목 | 값 |
| --- | --- |
| bbox | x **157–923** (w **767**), y **1776–1870** (h **94**), radius ≈ **22** |
| 겹 | 검정 테두리 ~5px `#000` + 채움 `#3D3630`(진갈회) |
| 리스트와 gap | 1726 → 1776 = **50**;  탭 하단 → 크림 하단 1890 = **20** |

### 8-2 활성 탭 「종합 스탯」(금색 배너)
| 항목 | 값 |
| --- | --- |
| 금색 채움 bbox | x **159–533**, y **1795–1850**; 검정 외곽선 포함 x **154–540**, y **1789–1856** (w ≈386, h ≈67) |
| 형태 | 좌우로 화살촉 팁이 돌출(좌 팁 x≈150 — 컨테이너 좌변 밖으로 살짝 나감) |
| 그라데이션 | 상 `#FFE983` → 중 `#FFC736` → 하 `#F8A63A` |
| 텍스트 | 잉크 y **1807–1839** (h **33**) → 한글 ≈ **35px**, x 282–407(중심 344.5 = 배너중심 345), 크림흰 `#FEF7C1` + 검정 외곽선 ~3px |

### 8-3 비활성 탭 「프로필」
| 항목 | 값 |
| --- | --- |
| 별도 박스 없음 | 컨테이너 채움 `#3D3630` 그대로 (구분선 없음) |
| 텍스트 | 잉크 y **1808–1838** (h **31**) → 한글 ≈ **33px**, x 692–776(중심 734), 색 라벤더-그레이 `#9A99A0` (`#939298`~`#9B9A9F`), 외곽선 없음 |

## §9. 기타 크롬
- 스크롤바·페이지 인디케이터·닫기 버튼 **없음**. 팝업 밖 영역(상단 HUD·탭바)은 딤 아래 게임 화면 그대로.

---

## 구현 힌트 (1080×1920 매핑)

- 셸 = **(다) 가운데 다이얼로그** → 팝업 전체 그룹(§2–§8)은 **`y = ref_y − 210`** (다이얼로그 컨벤션). 매핑 후 박스 y 305–1700, 중심 y 1002 (= 프레임중심 960 +42, 08/16 과 동일 편차 — 기존 다이얼로그들과 같은 앵커 유지).
- 딤: `rgba(0,0,0,0.55)` 풀스크린.
- 팝업 밖 요소는 이 화면에서 새로 그릴 것 없음(기존 HUD −84, 탭바 −419 규칙 그대로 두고 딤만 덮음).
- 리스트는 콘텐츠 13행+ 이므로 **높이 744 뷰포트 + 내부 스크롤(스크롤바 없음, 하단 클립)** 으로 구현. 행 60px 피치, 홀수 `#FEEFD2` / 짝수 `#F6E2C7`, 좌 텍스트 x=컨테이너+37, 우 값 right=컨테이너−37.
- 탭 토글: 활성=금 그라데이션 배너(화살촉 팁, 검정 외곽선), 비활성=텍스트만 `#9A99A0`. 탭 전환 시 19(프로필 탭)와 동일 셸 재사용.

---

## §11. 정오표 (2026-08-25, sess-0228-11508 5회차 — ref 픽셀 재실측)

> **⚠ 아래 4건은 위 본문이 틀렸다. 구현·비평은 이 절의 값을 쓴다.**
> 세로 변환도 위 «구현 힌트» 의 `ref − 210`(1080×1920 시절)이 아니라 **`ref − 84`(1080×2280)** 가 맞다 — 작업 63 에서 확정됐다.

| # | 절 | 본문(오기) | 재실측 | 근거 |
| --- | --- | --- | --- | --- |
| 1 | §7-1 리스트 bbox | y **982–1726** (h **744**) | **y977–1736 (h760)** | `scan20.py` 세로 연속구간 x540·x700 둘 다 977..1736. 1행 밴드 시작은 y1000 그대로라 **상단 패딩이 18 이 아니라 23** 이고, 하단은 13행이 **16px 노출된 채 클리핑**된다(§7-2 가 «13행이 살짝 보인다» 고 적은 것의 정체 — 텍스트 페이드가 아니라 **컨테이너가 그만큼 더 길다**). 비평가 I·J 가 «13행 슬라이버 누락» 으로 독립 지적 |
| 2 | §7-1 리스트 radius | ≈ **20** | **≈26–30 (구현 28)** | 상변에서 좌우 26px 들어간 지점부터 시작, dy24 에 풀폭 도달(r20 이면 dy16) |
| 3 | §6 편집 버튼 | x694–750 (w **56**), y844–897 (h **54**) | **53×52 @ x696–748, y845–896** | 진갈 테두리(`#52433C`) 바깥 기준. 본문 값은 **검정 AA 한 겹을 포함**한 것. 비평가 I 가 독립적으로 같은 값을 읽었다 |
| 4 | §4 아바타 액자 radius | (기재 없음) | **≈6–7** (안쪽 검정 라인 **4px**) | 좌상 코너에서 ref 는 dy6 에 풀폭 x451 도달. 구현이 18 이었다 |

### §12. 아트 필요 표 (GPT 아트 발주서용)

| 요소 | w×h | 용도 | 현재 대체물 |
| --- | --- | --- | --- |
| 초상화 | 137×138 | 아바타 액자 안 캐릭터 | 🛡️ 이모지 (파랑 `#3B5A86` 판 위) |
| 연필 아이콘 | 43×? (ref 잉크 x707–749) | 닉네임 편집 버튼 | ✏️ 이모지 — **ref 잉크 폭의 33% 밖에 안 찬다**(x715–728). 아트 교체 대상 1순위 |
| 크림 배경 별 워터마크 | 53×51 (타일 236×234) | 패널 바탕 무늬 | CSS 인라인 SVG 타일 — **33 재화정보 팝업(`.ci-body`)과 같은 자산**이라 실측이 일치했다. 아트가 들어오면 두 화면을 같이 교체할 것 |
| 칭호 리본 | 318×56 | 「칭호 없음」 육각 배너 | CSS `clip-path` 6각 + 3단 그라 |
| 금색 활성 탭 배너 | 386×67 | 「종합 스탯」 | CSS `clip-path` 셰브론 + 금 2단 그라 |
