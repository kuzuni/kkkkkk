# 17 스탯업 보너스 팝업 연출 측정표

> **출처**: `docs/ref/17-스탯업-보너스-팝업-연출.jpg` — 원본 **1080×2340**, 좌표는 전부 원본 절대 px.
> **측정 방법**: python3 + PIL(+numpy) **픽셀 스캔만** 사용. 눈대중 수치 없음.
> - 딤 농도는 같은 장면의 `23-훈련-팝업.jpg` 와 **픽셀 대 픽셀 나눗셈**(B>60 마스크, 밴드별 중앙값)
> - 리본은 `09-일괄-강화-결과-팝업.jpg` 의 리본과 **행 단위 orange 마스크 정합**으로 대조
> - 텍스트 외곽선은 순검정(<35) 세로획 폭으로 측정 (bbox 금지 — 08 교훈)
> **폰트 규칙**: `font-size = 잉크높이 ÷ 0.93`(한글) / `÷ 0.70`(숫자·캡하이트)
> **가로 1:1**, 세로 변환: **위쪽 클러스터 `ref_y − 84` / 탭바 고정 요소 `ref_y − 419`** (PROGRESS «확정(03)», 12 에서 검증)

---

## 0. 껍데기 판정 — **(라) 껍데기 없음 · 딤 위 자유 배치 연출** (09 와 동종)

패널·박스·헤더·전폭 밴드 전부 없음. 딤 위에 ① 주황 리본(「능력 획득」) ② 크림슨 능력 아이콘 카드
③ 흰 설명 텍스트 1줄 ④ «터치하여 닫기» ⑤ 별 파티클 2개만 얹었다. 탭하면 닫힌다.
뒤 화면은 **23 훈련 팝업과 동일 장면**(픽셀 대조 일치) — 상단 도전 HUD·배수 탭·훈련 카드 3열·서브탭 전부 딤 아래다.

## 1. 딤 — **상하 2톤** (09 의 균일 .80 과 다름)

| y 대역(ref) | 투과율(중앙값) | CSS |
| --- | --- | --- |
| 0 ~ ≈1000 | **0.114** (n≥42k/밴드, y156–936 전 밴드 0.113~0.115) | `rgba(0,0,0,.885)` |
| ≈1050 ~ 2340 | **0.21** (y1014–1950 전 밴드 0.202~0.218) | `rgba(0,0,0,.79)` |
| 전환 | ref y≈1000→1050 사이 | 프레임 **y916 → y966** 그라디언트 |

(y2028·2184 밴드의 0.14 는 17/23 캡처 간 내용 차이로 인한 노이즈 — 서브탭·탭바 상태가 다름)

## 2. 리본 «능력 획득» — **09 리본과 픽셀 단위 동일 에셋** (y 만 −135)

orange 마스크를 행 단위로 정합: 최적 오프셋 y889(17) ↔ y1024(09), 정합 후 x-범위가
행별 최대 Δ1px (예: +40행 x256–824 vs x256–824), 밴드 색도 동일 (254,189,85).
→ **`.upr-rb`/`.upr-tl` CSS 를 그대로 재사용하고 그룹 top 만 바꾼다.**

| 항목 | ref | 프레임(−84) |
| --- | --- | --- |
| 몸통(검정 포함) | x277–803 (w527) y877–977 (h101) | top **793** |
| 꼬리 | 좌 x242–333 / 우 x748–838, y902–998 | group 내 top 25 (09 와 동일) |
| 글자 «능력 획득» | 잉크 y906–949 (h44 → 47px), 글리프 x456–489/496–529/548–582/590–622, 외곽선 2~5px | `.upr-rb>i` 그대로, 텍스트만 교체 |

## 3. 능력 아이콘 카드 (중앙)

| 항목 | 값 |
| --- | --- |
| 외곽 박스(검정 포함) | **x416–663 (w248) · y1088–1359 (h272)** — 중심 x539.5 ✓ / 프레임 top **1004** |
| 검정 외곽선 | 5px (row1225: x416–420 / col521: y1088–1092) |
| 안쪽 밝은 림 | ~6px, 상·좌·우만 (lum 111–138, ≈`#E04868`) — 하단은 림 없음 |
| 몸통 | 크림슨 **(180,24,60) ≈ #B41C3C** |
| 하단 안쪽 그늘 | 마지막 ≈30px lum 27–28 (몸통의 ~0.3 배) |
| 모서리 radius | 좌상 코너 아크 (439,1096)→(423,1117), ≈**28** |
| 내부 아트 | 살색(240,192,144)+주황(240,84,12)+검정 — 캐릭터 공격 일러스트. **이모지 대체**(아트 한계) |

## 4. 설명 텍스트 «일반 공격 피해량 30% 증가»

| 항목 | 값 |
| --- | --- |
| 잉크(한글) | y1442–1486 **h45** → font-size **48** (÷0.93) |
| 잉크(«30%») | h37 (캡하이트 — 같은 48px 에서 0.70×48=33.6 예상, ref 가 +3 큼. 오차 허용) |
| 가로 | x269–812 (**전체 잉크폭 544**), 중심 x540.5 ✓ |
| 색 | 흰 (240,240,240) 채움 + 검정 외곽선 |
| 외곽선 | 순검정 세로획 5–7px → CSS `-webkit-text-stroke:11px` + `paint-order:stroke fill` |
| 어절 | 일반(269–354) 공격(368–446) 피해량(465–595) 30%(611–714) 증가(730–812), 어간 14–16px |
| 프레임 y | 잉크 **1357–1402** (−84) |

## 5. «터치하여 닫기» — 09 와 동일

잉크 y2123–2165 x411–668 — 09(y2122–2166 x413–665)와 일치. **`.upr-close` 그대로 재사용** (bottom:175).

## 6. 별 파티클 2개 (연출 장식)

| | bbox(ref) | 크기 | 색 | 프레임(−84) |
| --- | --- | --- | --- | --- |
| P1 (리본 좌하) | x140–203 · y959–1021 | 64×63 | 크림 (255,254,197) | 중심 (171, 906) |
| P2 (아이콘 좌하) | x226–263 · y1322–1358 | 38×37 | 노랑 (223,213,27) + 올리브 글로우(아이콘 좌하변 x360~415 로 번짐, lum 55→186) | 중심 (244, 1256) |

4포인트 별 형상. 글로우는 P2 쪽이 크다(halo 가 아이콘 테두리까지 닿음).

## 7. 배경(딤 아래) 참고 — 23 훈련 화면 블록 (23 담당용, ±3px)

17 캡처는 23 캡처와 동일 장면이므로 별도 실측하지 않았다. 23 작업 시 `23-훈련-팝업.jpg` 를 직접 측정할 것.
17 에서 식별된 대역: 상단 도전 HUD(⏱·STAGE·포기하기) y120–250 · 배수 탭 y1550 부근 · 훈련 카드 3열 y1600–2000 · 서브탭 y2030–2120.

## 정오표 — 754 (2026-09-02, sess-0742-27872 워커 C)

**§1~§6 의 y 는 «프레임 상단 기준 절대값»(= ref y − 84) 인데, 754 부터 이 화면의 묶음은
«프레임 세로 중앙» 에 걸린다.** 표의 값을 버리지 마라 — 그 값은 이제 **묶음 좌표계 안의 값**이고,
자식은 `(VH/2 − 1104) + T` 에 그려진다(`.st-grp{top:calc(50% - 1104px);height:0}`).

* 묶음 bbox = **793(.upr-grp 상변) .. 1415(.st-desc 하변)** = h622 · 중심 **1104**
* ⇒ 기준 해상도 2280 에서 묶음이 **36px 아래로** 간다(중심 1104 → 1140). **ref 이탈이고 의도된 것**이다 —
  주인 지시(2026-09-02 05:45 «중앙 피벗이나 앵커로»)가 레퍼런스보다 우선한다(360·426·661 선례).
* 「터치하여 닫기」(`.upr-close`)는 **묶음 밖**이다 — 754 예외 목록 ①(탭바 상단 기준).
  표의 그 줄 값은 **바뀌지 않았다.**
* 수리 전에는 다섯이 프레임 상단에 못 박혀 있어 **1600 에서 문구와 닫기가 35px 겹쳤다**
  (주인 스크린샷). 상세 `docs/review/754-팝업앵커피벗전면감사.md`.
