# 194 측정표 — 코스튬 개편(강화석 · 등급 폐지 · 강화)

> 레퍼런스 스크린샷이 **없는** 설계 변경이다(50 코스튬 시트 자체가 레퍼런스 없이 07 스킬 시트 규격을
> 복제한 화면). 따라서 이 표는 «레퍼런스 실측» 이 아니라 **바꾼 값과 안 바꾼 값의 목록**이다.
> 지시서 [3]-(가) 대로 비평가는 띄우지 않았다.

## 1. 기하 — 바뀐 것이 없다 (7 스킬 시트 규격 그대로)

| 요소 | 값 | 194 |
|---|---|---|
| 시트 헤더 `.sk-head` | 07 규격 | 불변 |
| 착용 슬롯 `.sk-slot` | 115×… · `left:461px`(패널 1037 안 중앙) | 불변 (라벨 문자열만 등급명 → `Lv. n`) |
| 격자 상자 `.sk-gp` | `left 33 · top 387 · 1000×680 · radius 25` | 불변 |
| 카드 `.sk-card` | 168×171 · 열 pitch **190** · 행 pitch **220** | 불변 |
| 격자 첫 행 top | 87 은 «등급 헤더 46 + 여백» 뒤라 가변이었다 | **20 고정**(헤더가 없어졌다) |
| 격자 행 수 | 등급 6섹션 × 가변 | **10행 고정**(50 ÷ 5열) |
| 총효과 줄 `.sk-tot` | y 1085 | 불변(문자열만 늘었다) |
| 버튼 `.sk-b1/.sk-b2` | `left 240 / 551` · `275×…` · `top 1158` | 불변 (`verify171` 가 offsetLeft 로 잰다) |
| 08 상세 껍데기 `.skd` | 800×701 · `.sk-ow` top 604 h 63 | 불변 |

**폐기한 CSS**: `#bCos .cos-hd` 3규칙(등급 섹션 헤더 띠·좌 라벨·우 카운터).
**신설한 CSS**: `#bCos .sk-bar.up>i` 1규칙(강화 가능 칸의 진행바 채움색 초록 — bbox 불변).

## 2. 상수표 (밸런스 축 — 199 가 다시 잡을 자리)

| 상수 | 값 | 뜻 |
|---|---|---|
| `COS_MAXLV` | **500** | 주인 확정 |
| `COS_OWN` | `atk .10 · hp .08 · gold .05` | 보유 효과(전 코스튬 동일, 코스튬마다 곱) |
| `COS_LV` | `atk .004 · hp .003 · gold .002` | 레벨 1당(전 코스튬 동일, **총합에 1회 곱**) |
| `COS_ST_BASE` / `COS_ST_R` | `2` / `1.012` | 강화 비용 `ceil(2 × 1.012^l)` |
| `COS_ST_RAID` | `60` | DPS 측정장 1회(신기록 ×2) |
| `COS_ST_ARENA` | `40` | 아레나 승리(패배 1/5 = 8) |
| 던전 `stone` req | `4200 × 1.76^(f−1)` | 유물 1단(5000×1.78)보다 살짝 낮게 |
| 던전 `stone` rw | `150 + 120f` | 층 선형(유물 던전 관례) |
| `PROMO_CUT` | `[21,10,8,6,2,1,1]` | 182 의 배분 그대로, 이제 **순번** 컷 |

### 곡선 실측(verify194 [3])

| Lv | 0 | 100 | 250 | 499 | 누적(0→500) |
|---|---|---|---|---|---|
| 비용 | 2 | 7 | 40 | 770 | **64,958** |

### 배수 상한

| 축 | 공격 | 체력 | 골드 |
|---|---|---|---|
| 보유(50종) | ×117.4 | ×46.9 | ×11.5 |
| 강화(25,000Lv) | ×101 | ×76 | ×51 |
| 옛 값(등급 있던 시절, 보유만) | ×159.8 | ×52.0 | ×17.1 |

## 3. 아트 필요 표 (GPT 아트 발주서용)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 강화석 아이콘 | 64×64 (viewBox) | 재화 아이콘 — HUD·보상·가방·33 팝업·58 비행 파티클 | `assets/ui/cur-stone.svg` (CSS 도형 — 주황 팔각 원석 + 하이라이트) |
| 강화석 던전 입장권 | 64×64 (viewBox) | 04 세부 팝업 입장권 · 53 가방 소모품 | `assets/ui/cur-ticket-stone.svg` (CSS 도형 — 갈색 티켓 + 원석) |
| 각성의 동굴 카드 썸네일 | 330×(341−11) | 03 던전 카드 · 04 세부 · 178 던전 보스 | `dragon` 아틀라스 `f1` 포즈(97 규칙 «없는 몬스터를 그리지 않는다») |

> 코스튬 50종의 «그림» 자체는 87 이 이미 «단일 knight 아틀라스 + 틴트» 로 대체해 두었고
> 194 는 한 색도 안 건드렸다 — 그 아트 발주 항목은 `docs/measure/87-코스튬50종.md` 가 갖는다.
