# 22 퀘스트 팝업 측정표 (레퍼런스 1080x2340 기준, 절대 px)

- 측정 일자: **2026-08-24**
- 레퍼런스: `docs/ref/22-퀘스트-팝업.jpg` (1080 x 2340, JPEG)
- 측정 방식: **python3 + PIL 픽셀 직접 스캔**. 스캔라인 색 전이점 / 색 마스크 bbox / 코너 원호 최소자승 피팅 / `02-기본-메인-화면.jpg` 와의 픽셀 대조(딤 산출). 눈대중 값 없음.
- 표기: `x`/`y` 는 요소 **좌상단**, 값은 정수 px, 색은 실제 픽셀 RGB.
- **✔ = 픽셀 스캔으로 확정한 값** / **(추정) = 피팅·계산·역산으로 얻은 값** / **(미확정)** = 판정 불가.
- font-size 는 **실측 잉크높이 / 0.93**. 라틴/숫자만 있는 문자열은 잉크가 캡하이트라 과소평가 — 해당 항목에 별도 표기.

---

## 0. 껍데기 판정 — A5-모달 «가운데 다이얼로그» 와 **일치**

| 판정 근거 | 실측값 | A5 스펙 | 판정 |
|---|---|---|---|
| 폭 | **898** ✔ (x 91..988) | 898 | ✔ 일치 |
| 좌/우 여백 | **91 / 92** ✔ | 91 | ✔ 일치 |
| 검정 테두리 | **7px** ✔ (좌 92~98 / 우 981~987 / 상 464~470 / 하 1954~1960) | 7px | ✔ 일치 |
| 갈색 프레임 | 편측 **10px** ✔ — 바깥 6px `#564438`(100~105) + 안쪽 4px `#47392E`(106~109) | 11px | ✔ 동일 계열 (±1) |
| radius | **42** (추정) — 코너 뒤 배경이 딤 먹은 아이콘들이라 로컬 피팅은 26~44 로 노이즈. TL 최량 피팅 44(MSE 16) | 42 | ✔ 모순 없음 |
| 딤 | `rgba(0,0,0,0.56)` ✔ — 02번(무딤 메인화면)과 픽셀 대조, HUD 투과배율 0.437 / 네비바 0.442 (n=21,450) | ~0.54 | ✔ 일치 |
| 세로 중심 | **1211.5** ✔ = 화면중심 1170 **+42** | +42 | ✔ 일치 |

### y-extent (이 팝업의 고유값)

| 항목 | x | y | width | height | 확정 |
|---|---|---|---|---|---|
| 바깥 박스 | **91** | **462** | **898** | **1500** | ✔ (91..988 / 462..1961, 검정 코어 기준 464..1960 = h 1497) |
| 콘텐츠(안쪽) 영역 | **111** | **563** | **858** | **1379** | ✔ (111..968 / 563..1941) |

- 상/하 화면 여백: **462 / 379** ✔ — 05번(478/394)보다 위아래로 각 16px 큼.
- 드롭섀도 없음, 닫기(✕) 버튼 **없음** ✔ (헤더 전역 밝은 픽셀 스캔 = 타이틀 잉크뿐).

---

## 1. 전역 색

| 용도 | HEX | 확정 |
|---|---|---|
| 딤 | `#000000` × **0.56** | ✔ |
| 모달 본문 배경 | `#F0D9BA` | ✔ |
| 본문 별 무늬 | `#ECD3B4` (가장 진한 곳 `#E7D0B1`) | ✔ |
| 리스트 패널 배경 | `#D7C0A1` | ✔ |
| 퀘스트 행(카드) 배경 | `#F7ECDA` | ✔ |

### 본문 별 무늬 (star-pattern beige — **있음**)

| 항목 | 값 | 확정 |
|---|---|---|
| 별 크기 | **~52 x 50** (실측 bbox w 51~52, 상하는 패널에 잘려 h≥48) | ✔ |
| 가로 pitch | **235** (중심 x 171 / 407 / 642 / 877) | ✔ |
| 세로 pitch | **~118**, 짝수행은 가로로 **117** 오프셋 (체커보드) — 노출 행 중심 y 588 / 1645 / 1764 | ✔ |
| 대비 | 본문 대비 ΔRGB ≈ (−4,−6,−6) — 매우 옅음 | ✔ |

---

## 2. 타이틀바 `퀘스트`

| 항목 | 값 | 확정 |
|---|---|---|
| 밴드 y | **471 ~ 562** (height **~91**) | ✔ |
| 배경색 | `#533F3E` (상단 8px 밝은 rim `#5B4746`) — 05번과 동일 | ✔ |
| 하단 구분선 | 없음 — 563 부터 바로 본문 `#F0D9BA` | ✔ |
| 타이틀 잉크(흰 채움) | x **482~596** (w 115) / y **500~533** (h **34**) | ✔ |
| 타이틀 색 | `#FFFFFF` 채움 + **검정 외곽선** | ✔ |
| 외곽선 두께 | **5px** (y=516 순검정 세로획 폭 5 / 6 / 5) | ✔ |
| font-size | **37** (34 / 0.93) | (추정) |
| 정렬 | 잉크중심 x 539 / y 516.5 → 밴드 정중앙 | ✔ |

> 05번 타이틀(h 37, 외곽선 없음)과 달리 22번 타이틀은 **검정 외곽선이 있다**.

---

## 3. 리스트 패널 (흰 카드 리스트 영역)

| 항목 | x | y | width | height | radius | 배경 | 확정 |
|---|---|---|---|---|---|---|---|
| 패널 | **118** | **628** | **844** | **999** | **21** (추정, MSE 0.63) | `#D7C0A1` | ✔ (118..961 / 628..1626) |

- 테두리 **없음** ✔ (1px AA 뿐)
- 본문 상단 563 → 패널 628 = **위 여백 65** ✔ (그 사이로 별 무늬 노출)
- 패널 내부 padding: 상 **33** / 좌 **13** / 우 **13** ✔

---

## 4. 퀘스트 행 (5행)

| 행 | y (상단~하단) | height | 확정 |
|---|---|---|---|
| 1 | **661 ~ 839** | **179** | ✔ |
| 2 | **861 ~ 1039** | **179** | ✔ |
| 3 | **1061 ~ 1239** | **179** | ✔ |
| 4 | **1261 ~ 1438** | **178** | ✔ |
| 5 | **1461 ~ 1626** | **166** — 패널 하단(1626)에서 **~13px 잘림** → 스크롤 뷰포트 | ✔ |

| 항목 | 값 | 확정 |
|---|---|---|
| 행 x / width | **131 / 818** (131..948) — 전 행 공통 | ✔ |
| 세로 pitch | **200** | ✔ |
| 행 간 gap | **21~22** (패널색이 사이로 노출 — 별도 구분선 없음) | ✔ |
| 행 배경 | `#F7ECDA` 단색, 테두리 없음 | ✔ |
| 행 radius | **23** (추정, 원호 피팅 MSE 0.17) | ✔ |

---

## 5. 행 내부 (1행 기준 — 2~5행은 y+200×n, 구조 동일)

행 내부 가로 리듬: 행좌단 131 →(36)→ 아이콘 167..272 →(26)→ 타이틀/진행바 298 · 299..700 →(22)→ 버튼 722..916 →(32)→ 행우단 948.

### 5-1. 좌측 보상 아이콘 프레임

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **167~272** / y **696~800** (w **106** / h **105**) | ✔ |
| 테두리 | **5px** `#000000` | ✔ |
| radius (바깥) | **36** (추정, TL 피팅 MSE 0.83) | ✔ |
| 안쪽 밝은 rim | **~7px** `#FFC334` (금색) | ✔ |
| 내부 배경 | `#D37D12` (진한 주황) 단색 | ✔ |
| 아이콘 | 하늘색 보석(젬) — 하이라이트 `#62FAFF` / 본색 `#3ADDFE` / 음영 `#05B0F5`, 검정 외곽선 | ✔ |
| 세로 중심 | 748 ≈ 행 중심 750 | ✔ |
| 5행 모두 동일 프레임·동일 보석 | rim `#FFC334` / fill `#D37C13` 5행 전부 실측 일치 | ✔ |

### 5-2. 수량 텍스트 (프레임 하단에 겹침)

| 항목 | 값 | 확정 |
|---|---|---|
| 잉크(흰 채움) | 1행 `50`: x **197~242** (w **46**) / y **782~808** (h **27**) | ✔ **(126 5회차 정정)** |
| 각 행 잉크 | 2자리 `50` **46×27** (1·3·4·5행 전부 동일) · 3자리 `100` **67×27**(x186~252) | ✔ |
| 자당 폭 | **≈21** (46/2 = 23 · 67/3 = 22.3 — 자릿수에 선형) | ✔ |
| 정렬 | 중심 x **219.5** = 프레임 중심 219.5 — 정확히 가운데 정렬 | ✔ |
| 색 | `#FFFFFF` 채움 + 검정 외곽선 **~4px** | ✔ |
| font-size | 숫자만 → 잉크=캡하이트. 27/0.70 ≈ **38.5** (프레임 폭 106 대비 **.364**) | (추정) |

> **⚠ 정오표 (126 ②-2, 5회차) — 옛 «x174~250 (w 77)» 은 틀렸다.**
> 그 창의 x174~196 구간을 픽셀로 직접 읽으면 숫자 잉크가 **한 픽셀도 없다** —
> y783 은 `(255,196,53)`·`(220,141,22)`(프레임 금색 림·주황 채움), y800 은 `(0,0,0)`(프레임 검정 테두리),
> y807 은 `(247,236,218)`(프레임 아래 카드 배경)이다. 즉 옛 bbox 는 **프레임 좌측 테두리·금색 림을
> 숫자와 함께 마스크에 넣은 오염 표본**이다.
> 수치로도 모순이었다: 2자리 46 / 3자리 67 로 **자당 21~23px 선형**인데, 77 은 2자리 기준 자당 38.5 로
> 이 계열에서 혼자 벗어난다. 프레임을 공유하는 자리의 «흰 채움» 을 잴 때는 창을 프레임 안쪽으로
> 좁히거나 잉크에서 8px 이내로 팽창 제한을 걸어야 한다.
>
> **구현과의 차이(미해결)**: 우리 배지는 지금 2자리 **32×20** · 3자리 **45×20** 으로 ref 대비
> **폭 −30% · 높이 −26%** 다. 원인은 이 화면이 아니라 **작업 104 의 배지 통일**이다 —
> `.ifq` 를 «프레임 폭 × .235» 로 묶었는데 22 의 ref 는 **.364** 라, 104 주석이 스스로
> «통일의 대가로 여기가 가장 많이 줄었다» 고 적어 둔 그 자리다. 되돌리는 것은 104 구간이라
> 126 5회차는 **손대지 않고 등재만** 했다(PROGRESS 141).

### 5-3. 타이틀 텍스트

| 행 | 잉크 bbox (외곽선 포함) | h | 확정 |
|---|---|---|---|
| 1 `아무거나 소환 100회` | x 298~567 / y **709~741** | **33** | ✔ |
| 2 `아무 훈련 1000회` | x 298~533 / y 908~941 | 34 | ✔ |
| 3 `햄지 공격력 훈련 200회` | x 297~609 / y 1108~1143 | 36 | ✔ |
| 4 `매옹이 공격력 훈련 200회` | x 298~635 / y 1308~1343 | 36 | ✔ |
| 5 `체력 훈련 200회` | x 298~519 / y 1507~1541 | 35 | ✔ |

- **좌측 정렬 x 298** (진행바 좌단 299 와 정렬) ✔
- 색: **흰 채움 `#FFFFFF` + 검정 외곽선** (외곽선이 두꺼워 멀리서 검정 글자처럼 보임). 순검정 세로획 폭 **3~6px** ✔
- 숫자부(`100회` 의 100)는 **주황 채움 `#FD8354`** + 검정 외곽선 ✔ (`회` 는 흰 채움)
- font-size: **35~38** (33~36 / 0.93) (추정)

### 5-4. 진행바

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **299~700** / y **762~799** (w **402** / h **38**) — 전 행 x 동일, y +200×n | ✔ |
| radius | **19** (= h/2, 완전 캡슐) | ✔ |
| 테두리 | **4px** `#000000` (상 762~765 / 하 796~799 / 좌우 300~304 / 695~699) | ✔ |
| 트랙(빈) 색 | 상 `#8182A0` → 하 `#6F7899` (하단 1/3 살짝 어두움) | ✔ |
| 채움 색 | 상 **`#FFC720`** (13px) → 하 **`#FFB514`** — 2톤 | ✔ |
| 채움 시작 | x **306** (안쪽 좌단) | ✔ |
| 채움 경계 | 3행: x≈**388** (= **21%**, 45/200=22.5% 근사) / 4행: x≈**637** (= **85.5%** = 171/200 ✔). 1·2·5행 경계는 중앙 텍스트에 가려짐 (미확정, 각 50%/53%/58% 예상) | ✔ / (미확정) |
| 중앙 텍스트 | `50/100` 잉크 x 458~541 / y **772~787** (h **16**), 2행 `532/1,000` x 439~560 (h 17) | ✔ |
| 텍스트 색 | `#FAFAFA` + 검정 외곽선 **~2px** (순검정 획 폭 1~2) | ✔ |
| 텍스트 정렬 | 잉크중심 499.5 = 바 중심 499.5 → 정중앙 | ✔ |
| font-size | 16/0.93=17 (숫자만 → 캡하이트 기준 최대 **22**) | (추정) |

### 5-5. 우측 `[보상 받기]` 버튼

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **722~916** / y **692~809** (w **195** / h **118**) — 5행 전부 y+200×n 동일 | ✔ |
| radius | **40** (추정, 피팅 MSE 2.8) | ✔ |
| 테두리 | **6px** `#000000` | ✔ |
| 면 (상→하) | 상단 rim 7px `#D3D3D3` → 그라데이션 `#A8A8A8`→`#8A8A8A` → 재하이라이트 7px `#D3D3D3`(788~794) → 바닥 그림자 6px `#757575`(795~801) | ✔ |
| 텍스트 잉크 | x **759~878** (w 120) / y **732~759** (h **28**) | ✔ |
| 텍스트 색 | `#DFDFDF` + 검정 외곽선 | ✔ |
| font-size | **30** (28 / 0.93) | (추정) |
| 정렬 | 잉크중심 818.5 ≈ 버튼중심 819 | ✔ |
| 상태 | **5행 전부 동일한 회색(비활성) 스타일** ✔ — 완료된 퀘스트가 없어 활성(초록 등) 색상은 이 캡처에 없음 **(미확정)** | ✔ |

---

## 6. `[모두 받기]` 버튼

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **404~675** / y **1647~1776** (w **272** / h **130**) | ✔ |
| 정렬 | 중심 x 539.5 = 모달 정중앙 | ✔ |
| radius | **43** (추정, 피팅 MSE 1.0) | ✔ |
| 테두리 | **7px** `#000000` (1647~1654 / 1770~1776) | ✔ |
| 면 | `[보상 받기]` 와 동일 회색 bevel: `#D3D3D3` → `#A7A7A7`→`#8C8C8C` → `#D1D1D1` → `#757575` | ✔ |
| 텍스트 잉크 | x **459~618** (w **160**) / y **1688~1730** (h **43**), `#DFDFDF` + 검정 외곽선 | ✔ **(126 5회차 정정)** |
| font-size | **46** (43 / 0.93) | (추정) |

> **⚠ 정오표 (126 ②-2, 5회차) — 옛 높이 «36» 은 «두» 의 세로획 하단을 자른 값이다.**
> 확대 크롭으로 보면 「모두 받기」의 «두» 가 가진 모음 ㅜ 의 세로획이 다른 글자보다 뚜렷이 아래
> (y≈1730)까지 내려간다. 옛 bbox y1689~1724 는 그 획의 하단부를 잘랐다. 실측은 y1688~1730 (h **43**).
> 한글 라벨의 잉크 높이를 잴 때 **아래로 뻗는 획(ㅜ·ㅗ·ㅠ·받침)** 이 있는 글자가 섞이면
> «대표 글자» 로 재지 말고 문자열 전체 bbox 로 재야 한다.
>
> **구현과의 차이(미해결)**: 우리는 흰 채움 **133×36** 으로 ref 대비 폭 −17% · 높이 −16% 다.
| 패널 하단과 간격 | 1626 → 1647 = **21** | ✔ |

---

## 7. 하단 2분할 토글 `일일 · 반복`

### 바 전체

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **151~947** / y **1801~1899** (w **797** / h **99**) | ✔ |
| radius | **49** (= h/2, 완전 캡슐) | ✔ |
| 가로 중심 | 549 = 모달중심 539.5 **+9.5** (우측 치우침 — 05번 서브탭 바의 −7 과 유사한 비대칭) | ✔ |
| 테두리 | 검정 아님 — **밝은 갈색 외곽 라인 ~5px `#726252`** + **어두운 안쪽 라인 ~5px `#473B2F`** | ✔ |
| 바(비선택부) 배경 | `#61513D` | ✔ |
| `[모두 받기]` 와 간격 | 1776 → 1801 = **25** | ✔ |
| 본문 하단과 간격 | 1899 → 1941 = **42** | ✔ |

### 선택 세그먼트 (`반복`, 우측 알약)

| 항목 | 값 | 확정 |
|---|---|---|
| bbox | x **522~946** / y **1800~1898** (w **425** / h **99**) — 바 우단에 플러시 | ✔ |
| radius | **49** (완전 캡슐) | ✔ |
| 테두리 | 어두운 외곽선 ~3px `#483829` + 밝은 rim ~5px `#645038` | ✔ |
| 배경 | **`#473A2A`** (바보다 어두움 = 선택 상태) | ✔ |

### 라벨

| 라벨 | 잉크 x | 잉크 y | h | 색 | 확정 |
|---|---|---|---|---|---|
| `일일` (비선택) | **310~377** (w 68) | **1835~1870** | **36** | `#A9A8AD` (연회색) + 검정 외곽선 | ✔ |
| `반복` (선택) | **701~768** (w 68) | **1830~1865** | **36** | `#8DDDFF` (하늘색) + 검정 외곽선 | ✔ |

- font-size **39** (36/0.93) (추정). `반복` 잉크중심 734.5 = 알약중심 734 ✔. `일일` 잉크중심 343.5 (좌측 영역중심 336.5 대비 +7).
- 선택 = 어두운 알약 + 하늘색 글자 / 비선택 = 바 바탕색 그대로 + 회색 글자.

---

## 8. 기타 요소

| 항목 | 판정 |
|---|---|
| 닫기(✕) 버튼 | **없음** ✔ |
| 빨간 점/뱃지 | 모달 내부 **없음** ✔ (모달 영역 적색 픽셀 스캔 0개. 배경 네비바의 빨간 점은 딤 뒤 게임화면 소속) |
| 스크롤바 | **없음** ✔ (패널 우측 스트립 118~961 전체 단색) |
| 스크롤 근거 | 5행이 패널 하단에서 ~13px 잘림 (§4) |

---

## 9. 세로 리듬 요약 (모달 내부)

| 구간 | y | 값 |
|---|---|---|
| 타이틀바 | 471 ~ 562 | h 91 |
| 본문 상단 여백 | 563 → 628 | **65** |
| 리스트 패널 | 628 ~ 1626 | h 999 |
| ↳ 행 5개 | 661 시작, pitch 200, gap 21~22 | h 179 |
| 간격 | 1626 → 1647 | **21** |
| 모두 받기 | 1647 ~ 1776 | h 130 |
| 간격 | 1776 → 1801 | **25** |
| 토글 바 | 1801 ~ 1899 | h 99 |
| 본문 하단 여백 | 1899 → 1941 | **42** |

---

## 10. 화면에 보이는 실제 데이터 (위→아래)

| # | 퀘스트 타이틀 | 보상(보석) | 진행 | 버튼 |
|---|---|---|---|---|
| 1 | 아무거나 소환 **100**회 | 50 | 50/100 | 보상 받기 (회색) |
| 2 | 아무 훈련 **1000**회 | 100 | 532/1,000 | 보상 받기 (회색) |
| 3 | 햄지 공격력 훈련 **200**회 | 50 | 45/200 | 보상 받기 (회색) |
| 4 | 매옹이 공격력 훈련 **200**회 | 50 | 171/200 | 보상 받기 (회색) |
| 5 | 체력 훈련 **200**회 | 50 | 116/200 | 보상 받기 (회색) |

- 하단 버튼: **모두 받기** (회색) / 토글: 일일 · **반복(선택)**. 숫자부는 전부 주황 `#FD8354`.

---

## 11. 미확정 / 주의

| 항목 | 사유 |
|---|---|
| 모달 radius 로컬 피팅 | 코너 뒤 배경(딤 먹은 사이드 아이콘)이 어두워 검정 경계 분리 불가 → A5 스펙 42 준용 |
| `보상 받기` 활성(완료) 상태 색 | 이 캡처엔 회색만 존재 |
| 1·2·5행 채움 경계 | 중앙 `n/n` 텍스트가 경계를 덮음 |
| 리스트 총 행 수 | 5행째 13px 잘림 — 스크롤 시 추가 행 존재 여부 판정 불가 |
| 토글 바 +9.5px 우측 치우침 | 픽셀상 사실. 의도인지 캡처 아티팩트인지 판정 불가 |

---

## 12. 정오표 (2026-08-25, sess-0313-29422 워커 A — 레퍼런스 픽셀 재스캔)

측정표를 그대로 구현했는데 비평 점수가 안 오르는 항목을 **레퍼런스 픽셀과 직접** 대조해 잡은 오기다
(LESSONS 20-① «회차가 정체되면 구현 vs 측정표가 아니라 구현 vs 레퍼런스 픽셀을 대조하라»).
**아래 값이 §5-1·§5-4 보다 우선한다.**

| 절 | 원래 값 | **정정** | 근거 |
|---|---|---|---|
| §5-1 보상 프레임 radius | 36 (추정, TL 단일코너 피팅 MSE 0.83) | **31** | 좌·우 **14개 열의 «세로 연속 높이»** 로 (h, r) 최소자승 피팅 → h 105.8 · **r 30.9 (MSE 0.27)**. 같은 표본에서 h105·r36 은 **MSE 41**. 단일 코너 피팅은 표본이 한쪽뿐이라 반경이 과대 추정됐다. |
| §5-1 프레임 y / height | 696~800 (h105) | **698~803.5 (h≈105.8)** | 비가림 열(x191·246)의 검정 연속 구간. 불변식은 «프레임 세로중심 750.75 ≈ 행 세로중심 750.5» — **행 중심 정렬**로 구현할 것. |
| §5-4 진행바 bbox | x299~700 (w402) | **x300~699 (w400)** | 1·3행 모두 검정 마스크(max<80)가 300~699. JPEG 번짐은 어두운 영역을 **넓히지 좁히지 않으므로** 400 은 상한이 아니라 실측이다. |
| §1 별 무늬 | «세로 pitch ~118, 짝수행 117 오프셋» (맞다) | (정정 아님 — **구현 주의**) | A5 공용 `.mbody` 타일은 **별이 1개**(235×235)라 이 체커보드를 못 만든다. 33 `.ci-body`·20 `.spc-body` 가 쓰는 «별 2개 반칸 오프셋» 자산을 재사용할 것. 22 의 위상: 별 중심 **ref x171 / y587.5**. |

> §5-5 [보상 받기] 의 «재하이라이트 7px(788~794) / 바닥 그림자 6px(795~801)» 은 재스캔에서
> **재HL 788~794 · 그림자 795~802(8px)** 였다. 둘을 CSS 그라디언트로 동시에 만족시킬 수 없어
> 구현은 **재하이라이트 위치**를 맞추고 그림자는 10px 로 뒀다(Δ2).

---

## 13. 아트 필요 표 (GPT 아트 발주서용)

이모지·CSS 작화로 대체한 요소. **레퍼런스 bbox 를 그대로 차지**하게 만들어 두었으므로 이미지로 갈아끼우기만 하면 된다.

| 요소 | 레퍼런스 bbox (잉크) | 자리(프레임 좌표, 1행 기준) | 용도 | 현재 대체물 | 비고 |
|---|---|---|---|---|---|
| 보상 보석 아이콘 | **55 × 55**, 중심 (220, ref 750) | 프레임 167..272 × 614..719 안, 중심 (219.5, 666) | 퀘스트 보상 재화(다이아) | 💎 이모지 `font-size:50px` | 잉크 실측 **54×47** — 폭은 맞고 **세로가 8px 짧다**. 이모지 종횡비 한계라 폭을 맞추면 세로가 안 맞는다(세로를 맞추면 폭이 +18%). 아트로 교체할 때 **55×55 정사각**으로 그리면 그대로 들어간다. |
| 보상 프레임 | 106 × 106 · radius **31** · 검정 5 + 금 rim 7 | 행 원점 기준 (36, 37) | 보상 아이콘 틀 | CSS(테두리 + inset box-shadow) | 아트로 대체할 필요 없음(도형이다). §12 정오표의 radius 31 을 쓸 것. |
| 별 무늬 워터마크 | 별 52~53 × 50 · 가로 pitch 235.3 · 세로 pitch 117.5 · 짝수행 +114 | 본문 전체, 위상 = 별 중심 ref (171, 587.5) | 본문 배경 | 인라인 SVG 타일 (33 `.ci-body` · 20 `.spc-body` 와 **같은 자산**) | 한 자산을 세 화면이 가리키므로 **아트 교체는 한 번**이면 된다. |
| 버튼 베벨 (보상 받기 / 모두 받기) | 195×118 · 272×130 | — | 버튼 면 | CSS `linear-gradient` 5단 | 레퍼런스는 연속 그라디언트인데 CSS 는 계단식이라 ⑤ 에서 1점 깎였다(R). 아트로 바꾸면 회수된다. |
