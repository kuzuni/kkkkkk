# 12 소환 결과 팝업 측정표

- **측정 기준: 원본 1080×2340 절대 px, PIL 픽셀 스캔**
- 레퍼런스: `docs/ref/12-소환-결과-팝업.jpg` (1080 × 2340, JPEG)
- 대조 레퍼런스(딤 산출용): `docs/ref/10-상점-팝업-소환-탭.jpg` (동일 배경 = 상점 팝업 소환 탭)
- 측정 방식: Python + Pillow. 스캔라인 색 전이점 / 색 마스크 bbox / 코너 원호 피팅 / A·B 이미지 픽셀 대 픽셀 최소자승 회귀. **눈대중 값 없음.**
- 좌표는 모두 **원본 1080px 폭 기준 절대 px**, `x`/`y`는 요소 **좌상단**, 정수 반올림.
- 텍스트 높이는 **잉크 bbox**(실제 글자 픽셀의 최소~최대 y). `환산 font-size = 잉크높이 / 0.93`.
- 측정 일자: 2026-08-24

---

## 0. 껍데기 판정 — **(라) 전면 딤 + 전폭(full-bleed) 결과 밴드 오버레이**

**결론: ⓐ 가운데 다이얼로그도, ⓑ 전체화면 페이지도, ⓒ 바닥 시트도 아니다.**
**화면 전체를 진하게 딤(α≈0.80) 처리한 위에, ① 리본 헤더 ② 전폭 가로 밴드 패널(그리드) ③ 하단 재소환 버튼 3개 ④ `터치하여 닫기` 4덩어리가 따로따로 떠 있는 «결과 연출 오버레이»다.**
그리드 패널을 감싸는 좌우 테두리·모서리 라운드가 **전혀 없다** — 패널이 x=0 과 x=1079 를 그대로 물고 있다.

| 판정 근거 | 실측 |
|---|---|
| 패널 좌우 끝 | x **0 ~ 1079** — `y=795` / `y=1000` / `y=1329` **가로 전 라인 스캔**으로 확인. x=0,2,5,100,300,540,800,900,1050,1077,1079 전부 동일색 |
| 패널 상하 | y **793 ~ 1331** (금색 라인 바깥 기준). 화면 위로 793px, 아래로 1009px 배경이 남음 → 전체화면 페이지 아님 |
| 바닥 시트 여부 | **아님**. 패널이 화면 하단(2340)에 붙어 있지 않고, 패널 아래에 딤된 배경 + 별도 버튼/닫기 안내가 있음 |
| 뒤에 비치는 화면 | **있음**. `무기 상자` 카드(y 240~690), `스킬 상자` 카드(y 1350~1650), 하단 서브탭·탭바가 그대로 비침 → 딤 확정 |
| 배경 동일성 검증 | `10-상점-팝업-소환-탭.jpg` 와 `무기 상자` 카드 좌표가 **완전 일치**(픽셀 대 픽셀 상관). 같은 페이지 위 오버레이 |
| 하단 버튼 3개 | **딤 안 걸린 전경**. 같은 좌표의 B 이미지는 `#272731`(빈 배경)인데 A 는 `#989898` — B 에는 아예 없는 요소 |
| 패널 투명도 | **불투명**. 패널 내부는 x=0~1079 전 구간 `#2A2835` 단일색인데, B 이미지 같은 좌표엔 분홍 `방어구 상자` 카드가 있음 → 전혀 비치지 않음 |

### 0-1. 딤 오버레이 알파 산출

동일 콘텐츠 영역(`x 60~900, y 240~690` + `x 60~900, y 1360~1650`, 3px 간격 = 채널 샘플 20.7만개)에서
`A = k·B` 최소자승 회귀.

| 항목 | 값 |
|---|---|
| 영역 | 화면 전체 `0,0 ~ 1080×2340` (패널/리본/버튼/닫기문구 제외 전면) |
| 색 | `#000000` |
| **sRGB 최소자승 k** | **0.1979** → **α = 0.802** |
| 절편 포함 fit | `A = 0.2135·B − 2.75` |
| 밝기 구간별 비율 | b 160~200 → 0.198 / b 200~240 → **0.208** / b 240~255 → 0.203 (밝은 구간에서 안정) |
| **권장 CSS** | **`background: rgba(0,0,0,0.80)`** |
| 딤 후 대표 배경색 | 페이지 여백 `#020202`~`#030303`, 순수 검정과 거의 구분 안 됨 |

> 참고: 어두운 구간(b<80)에서는 비율이 0.13~0.26 으로 튀지만 JPEG 양자화 잡음 때문이며,
> 밝은 구간이 일관되게 0.20 이므로 **α = 0.80** 이 맞다. (linear-light 감마 2.2 가정 시 α=0.97 이 나오지만
> 밝기 구간별 비율이 sRGB 공간에서 훨씬 평평하므로 **sRGB 직접 합성**이 맞다.)

---

## 1. 전체 세로 구조 (절대 y)

| # | 요소 | top | bottom | height | 앞 요소와 gap |
|---|---|---|---|---|---|
| — | 화면 상단 | 0 | — | — | — |
| B1 | (배경) 상단 HUD | ~90 | ~160 | — | 딤됨 |
| B2 | (배경) `무기 상자` 카드 | ~240 | ~690 | — | 딤됨 |
| **1** | **리본 헤더 (면 기준)** | **725** | **829** | **105** | — |
| 1a | └ 리본 중앙 밴드 | 725 | 811 | 87 | — |
| 1b | └ 리본 좌·우 꼬리 | 749 | 829 | 81 | — |
| 1c | └ 타이틀 `소환 결과` 잉크 | 747 | 787 | 41 | 밴드 상단에서 22 |
| **2** | **결과 패널 (금색 라인 바깥)** | **793** | **1331** | **539** | 리본과 겹침 |
| 2a | └ 상단 금색 라인 | 793 | 797 | 5 | — |
| 2b | └ 상단 안쪽 베벨(밝은 띠) | 800 | 806 | 7 | 2 |
| 2c | └ 패널 본문 | 807 | 1316 | 510 | 0 |
| 2d | └ 하단 안쪽 베벨 | 1317 | 1324 | 8 | 0 |
| 2e | └ 하단 금색 라인 | 1327 | 1331 | 5 | 2 |
| 3 | 아이템 그리드 1행 | 899 | 1055 | 157 | 본문 상단에서 **92** |
| 3a | └ 1행 개수 `1` 잉크 | 1039 | 1063 | 25 | — |
| 4 | 아이템 그리드 2행 | 1069 | 1225 | 157 | 1행에서 **13** |
| 4a | └ 2행 개수 `1` 잉크 | 1209 | 1234 | 26 | — |
| — | 그리드 끝 → 패널 본문 끝 | — | — | — | 1225 → 1316 = **91** |
| B3 | (배경) `스킬 상자` 카드 | 1332 | ~1650 | — | 딤됨 |
| **5** | **하단 버튼 3개 (검정 외곽선 포함)** | **1766** | **1913** | **148** | 패널 하단에서 **435** |
| 5a | └ 버튼 면(외곽선 안쪽) | 1772 | 1907 | 136 | — |
| 5b | └ 버튼 타이틀 잉크 | 1797 | 1826 | 30 | 면 상단에서 25 |
| 5c | └ 내부 어두운 알약 | 1842 | 1877 | 36 | 타이틀에서 16 |
| B4 | (배경) 서브탭 행 | ~1950 | ~2060 | — | 딤됨 |
| **6** | **`터치하여 닫기` 잉크** | **2124** | **2164** | **41** | 버튼에서 **211** |
| B5 | (배경) 하단 탭바 | ~2190 | 2340 | — | 딤됨 |

### 1-1. 가로 중심 정렬 검증 (전부 화면 중앙 540)

| 요소 | 중심 x | 측정 근거 |
|---|---|---|
| 리본 (면 249~831) | **540.0** | 249+831 = 1080 |
| 타이틀 `소환 결과` (456~625) | **540.5** | — |
| 패널 | 539.5 | 전폭 0~1079 |
| 그리드 (36~1042) | **539.0** | 좌 여백 36 / 우 여백 37 |
| 버튼 그룹 (외곽 117~962) | **539.5** | 좌 여백 117 / 우 여백 117 |
| 가운데 버튼 면 (423~657) | **540.0** | — |
| `터치하여 닫기` (412~667) | **539.5** | — |

---

## 2. 리본 헤더 `소환 결과`

주황 리본. 중앙 밴드 + 좌우 «모래시계형 꼬리» 2개. 꼬리는 밴드보다 **어두운 주황**이고 밴드보다 아래로 내려온다.
전체가 패널 상단 금색 라인(793) 위에 걸쳐 있어, 리본 하단 일부가 패널 안으로 파고든다.

| 요소 | x | y | width | height | 색 | 측정 근거 |
|---|---|---|---|---|---|---|
| **리본 전체(면 bbox)** | 249 | 725 | **583** | **105** | — | y 718~840 전 행 주황 마스크 스캔, 최좌 249(y819~825)·최우 831 |
| 중앙 밴드 | 284 | 725 | **513** | **87** | `#FFBE56` (255,190,86) | y=730~737 행에서 x 284~796 |
| — 상단 하이라이트 | 284 | 725 | 513 | **9** | `#FEEF88` (254,239,136) | col x=300: y 726~734 |
| — 밴드 radius | — | — | — | — | — | **약 8** (dy 0→dx 5, dy 5→dx 0, 45° 챔퍼형 짧은 원호) |
| 좌 꼬리 | 249 | 749 | **77** | **81** | `#F69632` (246,150,50) | y=820 행: x 249~325 |
| 우 꼬리 | 755 | 749 | **77** | **81** | `#F69632` | y=820 행: x 755~831 (좌우 완전 대칭, 249+831=1080) |
| — 꼬리 안쪽 그늘 | — | — | — | — | `#DD8335` → `#A55019` | y=820 행: 292~310 `#DD8335`, 313~324 `#A55019` |
| — 밴드/꼬리 경계 | — | — | — | — | `#DE8436` (2~5px) | y=760 행: x 284~290 |
| 검정 외곽선 | — | — | — | — | `#000000` | **좌우 4~6px** (y=805: x 249~252 / y=820: x 243~247). **상단은 측정 불가**(배경이 딤으로 거의 검정) |

### 2-1. 꼬리 외곽 프로파일 (모래시계형 — 원호 아님)

좌 꼬리 바깥쪽 x 좌표 (행별):

| y | 749 | 756 | 761 | 770 | 780 | **787** | 800 | 810 | **822** | 829 |
|---|---|---|---|---|---|---|---|---|---|---|
| x | 256 | 251 | 251 | 256 | 260 | **263 (가장 안쪽=허리)** | 257 | 253 | **249 (가장 바깥)** | 253 |

→ 위 뾰족점 y≈758, 허리 y≈787, 아래 뾰족점 y≈822. 우 꼬리는 x 좌표 미러(1080−x).

### 2-2. 타이틀 텍스트 `소환 결과`

| 항목 | 값 | 측정 근거 |
|---|---|---|
| 흰 잉크 bbox | x **456 ~ 625** (w **170**), y **747 ~ 787** (h **41**) | x 300~790 / y 726~812, `RGB>195 & 회색` 마스크 |
| **환산 font-size** | **44** (41 / 0.93) | — |
| 색 | `#FFFFFF` | — |
| 검정 외곽선 bbox | x 452 ~ 630, y 744 ~ 793 | `max(RGB)<70` 마스크 |
| **외곽선 두께** | **4~5px** (좌 4 / 우 5 / 상 3 / 하 6) | 위 두 bbox 차 |
| 글리프 런 | 소 456~487 (32) / 환 493~530 (38) / (공백) / 결 547~579 (33) / 과 587~625 (39) | 열별 흰 픽셀 프로파일 |
| advance | 소→환 **37**, 결→과 **40**, 어절 공백 gap **17** | — |
| 자간 느낌 | font-size 44 대비 advance 37~40 = **0.84~0.91em** → **letter-spacing ≈ −5** 또는 **scaleX ≈ 0.88** | — |
| 세로 정렬 | 잉크 중심 767 vs 밴드 중심 768 → 사실상 밴드 세로 중앙 | — |

---

## 3. 결과 패널 (전폭 밴드)

**좌우 테두리·radius 없음. x=0 ~ 1079 를 그대로 물고 있다.**

| 레이어 | y 범위 | 두께 | 색 | 측정 근거 |
|---|---|---|---|---|
| 상단 금색 라인 | **793 ~ 797** | **5** | `#FFDC38` (위) → `#F0BA1A` (아래) | col x=5 / x=950 동일. y=795 가로 전 라인 확인 |
| 상단 어두운 구분선 | 798 ~ 799 | 2 | `#624E1C` → `#2A231B` | col x=100 |
| **상단 안쪽 베벨** | **800 ~ 806** | **7** | 피크 `#414455` (65,68,85) @ y 803~804 | col x=100 / x=950 |
| **패널 본문** | **807 ~ 1316** | **510** | **`#2A2835`** (42,40,53) | y=860 / y=1300 에서 x 0~1079 전부 동일. 무늬·그라데이션 **없음** |
| **하단 안쪽 베벨** | **1317 ~ 1324** | **8** | 피크 `#424556` (66,69,86) @ y 1320~1321 | col x=100 / x=900 |
| 하단 어두운 구분선 | 1325 ~ 1326 | 2 | `#262223` → `#2B230E` | col x=300 |
| 하단 금색 라인 | **1327 ~ 1331** | **5** | `#F8DE4D` (위) → `#EAC22F` (아래) | col x=2 / x=900 / x=1077 동일 |
| **패널 바깥 bbox** | **793 ~ 1331** | **539** | — | 금색 라인 바깥 기준 |
| radius | — | — | — | **없음 (0)** — 전폭이라 모서리 자체가 없음 |
| 그림자 | — | — | — | **없음** (금색 라인이 경계 역할) |

---

## 4. 아이템 그리드

### 4-1. 그리드 규격

| 항목 | 값 | 측정 근거 |
|---|---|---|
| **열 × 행** | **6열 × 2행** | y=930 행 스캔에서 6개 런, y=1140 행 스캔에서 4개 런 |
| **총 칸 수** | **10칸** (1행 6칸 + 2행 4칸, 좌측 정렬 = 마지막 행은 4칸만) | — |
| 그리드 bbox | x **36 ~ 1042** (w **1007**), y **899 ~ 1225** (h **327**) | 카드 검정 외곽선 바깥 기준 |
| **카드 크기(검정 외곽선 포함)** | **157 × 157** | y=978 행: 검정 36~40 … 188~192 → 36~192 = 157 / col x=100·114: 899~1055 = 157 |
| **열 pitch** | **170** | 카드 좌단 35·206·376·546·716·885 → (885−35)/5 = 170.0. 우단으로도 (1044−194)/5 = 170.0 |
| **행 pitch** | **170** | 1행 외곽선 상단 899, 2행 1069 |
| **열 gap** | **13** | 192 → 206 |
| **행 gap** | **13** | 1055 → 1069 |
| 좌 여백 | **36** | 화면 좌단 → 첫 카드 |
| 우 여백 | **37** | 마지막 카드(1042) → 1079 |
| 패널 본문 위 여백 | **92** | 807 → 899 |
| 패널 본문 아래 여백 | **91** | 1225 → 1316 |

**열별 카드 좌단 x**: `36 / 206 / 376 / 546 / 716 / 885` (마지막만 반올림 오차 −1)
**행별 카드 상단 y**: `899 / 1069`

### 4-2. 카드 1장 내부 레이어 (모든 카드 공통)

| 레이어 | 두께 | 좌표 예 (1행1열, y=978 가로 / x=114 세로) | 색 |
|---|---|---|---|
| 검정 외곽선 | **5px** | 좌 36~40, 우 188~192, 상 899~903, 하 1051~1055 | `#000000` |
| **안쪽 밝은 림** | **6px** | 좌 42~47, 우 182~186(5), 상 904~909, 하 1045~1049 | 등급색의 밝은 톤 (아래 표) |
| 면(채움) | — | 48~181 / 910~1044 | 등급색 (아래 표) |
| **radius (바깥)** | **≈ 34** | 원호 피팅: dy 8→dx 12(r=33.9), dy 12→dx 8(r=33.9), dy 20→dx 4(r=36.6), dy 25→dx 2(r=37) | — |
| radius (면) | ≈ 29 | 추정: 34 − 5 | — |
| 면 그라데이션 | **없음** | x=60 세로 스캔 y 928~1040 전부 `#95BE34`~`#98C135` (Δ≤4) | — |

### 4-3. 각 칸 등급색 (좌상단부터 행 우선)

| 칸 | 좌단 x | 상단 y | 등급색 면 | 밝은 림 | 아이콘 |
|---|---|---|---|---|---|
| r0c0 | 36 | 899 | **`#97C034`** 초록 | `#C7E746` | 치즈 |
| r0c1 | 206 | 899 | **`#7D5A44`** 갈색 | `#AC7B5B` | 주전자 |
| r0c2 | 376 | 899 | **`#97C034`** 초록 | `#C7E746` | 강판 |
| r0c3 | 546 | 899 | **`#97C034`** 초록 | `#C7E746` | 바나나 |
| r0c4 | 716 | 899 | **`#97C034`** 초록 | `#C7E746` | 모종삽 |
| r0c5 | 885 | 899 | **`#97C034`** 초록 | `#C7E746` | 강판 |
| r1c0 | 36 | 1069 | **`#7D5A44`** 갈색 | `#AC7B5B` | 보라 구슬 |
| r1c1 | 206 | 1069 | **`#97C034`** 초록 | `#C7E746` | 모종삽 |
| r1c2 | 376 | 1069 | **`#347DC1`** 파랑 | `#47A7E1` | 번개 |
| r1c3 | 546 | 1069 | **`#97C034`** 초록 | `#C7E746` | 모종삽 |

**등급색 3종 요약**: 초록 7칸 / 갈색 2칸 / 파랑 1칸.

| 등급 | 면 | 밝은 림 |
|---|---|---|
| 초록(가장 흔함) | `#97C034` (151,192,52) | `#C7E746` (199,231,70) |
| 갈색 | `#7D5A44` (125,90,68) | `#AC7B5B` (172,123,91) |
| 파랑 | `#347DC1` (52,125,193) | `#47A7E1` (71,167,225) |

### 4-4. 카드 안 아이콘 잉크 bbox

카드 안쪽 24px 여백을 제외한 영역에서 «면색과 Δ>55» 마스크로 측정 (밝은 림 배제).

| 칸 | 아이콘 잉크 x | y | w | h | 잉크 중심 (x, y) | 카드 중심 |
|---|---|---|---|---|---|---|
| r0c0 | 81 | 944 | 73 | 67 | (117.5, 977.5) | (114.5, 977) |
| r0c1 | 251 | 942 | 75 | 70 | (288.5, 977.0) | (284.5, 977) |
| r0c2 | 428 | 942 | 60 | 70 | (458.0, 977.0) | (454.5, 977) |
| r0c3 | 594 | 940 | 68 | 73 | (628.0, 976.5) | (624.5, 977) |
| r0c4 | 758 | 935 | 77 | 77 | (796.5, 973.5) | (794.5, 977) |
| r0c5 | 938 | 942 | 60 | 70 | (968.0, 977.0) | (963.5, 977) |
| r1c0 | 80 | 1114 | 75 | 68 | (117.5, 1148.0) | (114.5, 1147) |
| r1c1 | 248 | 1106 | 76 | 75 | (286.0, 1143.5) | (284.5, 1147) |
| r1c2 | 429 | 1109 | 52 | 75 | (455.0, 1146.5) | (454.5, 1147) |
| r1c3 | 588 | 1106 | 76 | 75 | (626.0, 1147.0) | (624.5, 1147) |

→ 아이콘은 **카드 중앙 정렬**(비정형 아트라 잉크 중심이 2~4px 흔들림). 아이콘 표준 박스는 **약 76×75**, 카드 안쪽 여백 상하좌우 **약 40px**.

### 4-5. 개수 배지 `1`

**⚠ 우하단이 아니라 «하단 정중앙»이다.** 그리고 카드 바닥선 아래로 튀어나온다.

| 항목 | 값 | 측정 근거 |
|---|---|---|
| 흰 잉크 bbox (r0c0) | x **110 ~ 118** (w **9**), y **1039 ~ 1063** (h **25**) | y 1030~1075 행별 흰 픽셀 런 |
| **환산 font-size** | **27** (25 / 0.93) | — |
| 색 | `#FBFBFB` ~ `#FFFFFF` | — |
| 검정 외곽선 | x **105 ~ 122** (w 18), y **1035 ~ 약 1068** | `max(RGB)<30` 마스크 (카드 바닥 외곽선과 분리해 판독) |
| **외곽선 두께** | **4~5px** (좌 5 / 우 4) | — |
| 가로 위치 | 잉크 중심 **114.0** vs 카드 중심 **114.5** → **정중앙** | 6칸 전부 중심 115.5/285.5/455.5/625.5/795.5/965.5 |
| 세로 위치 | 카드 바닥(1055) **아래로 8px 돌출** (잉크 하단 1063) | y=1060 행 스캔에서 카드는 없고 `1` 만 6개 남음 |
| 2행 값 | 잉크 y **1209 ~ 1234** (h 26) → font-size **28** | 동일 마스크 |
| 글리프 모양 | 세리프 없는 단순 `1` (윗 깃 110~118, 세로획 114~118) | — |

---

## 5. 하단 재소환 버튼 3개

3개 모두 **동일한 은색(silver) 버튼 스타일**. `10-상점-팝업-소환-탭.jpg` 의 `30회 소환 3,000` 은색 버튼과
레이어 색·두께가 완전히 같음 → **딤이 걸린 게 아니라 원래 은색 버튼**이다.

### 5-1. 버튼 bbox

| 버튼 | 외곽 x | 외곽 y | 외곽 w | 외곽 h | 면 x | 면 y | 면 w | 면 h |
|---|---|---|---|---|---|---|---|---|
| ① `10회 소환 / FREE 0/1` | **117** | **1766** | **247** | **148** | 122 | 1772 | **236** | **136** |
| ② `10회 소환 / 💎1,000` | **416** | **1766** | **248** | **148** | 423 | 1772 | **235** | **136** |
| ③ `30회 소환 / 💎3,000` | **716** | **1766** | **247** | **148** | 723 | 1772 | **235** | **136** |

- 외곽 = **순수 `#000000` 외곽선 바깥** 기준. 딤 배경이 `#020202~#030303` 이라 (0,0,0) 과 구분 가능해서 판독함
  (예: y=1838 에서 x 116=(1,1,1) → 117~120=(0,0,0) → 121=(49,49,49) AA → 122=(200,200,200) 면).
- **버튼 그룹 전체**: x **117 ~ 962** (w **846**), 중심 539.5. 좌 여백 117 / 우 여백 117 **대칭**.
- **버튼 pitch: 300** (면 좌단 122 / 423 / 722)
- **버튼 사이 gap: 외곽 기준 52** (363→416, 663→716) / **면 기준 65·64**

### 5-2. 레이어 구조 (3개 공통, col x=240 / x=540 실측)

| 레이어 | 두께 | y 범위 | 색 |
|---|---|---|---|
| 검정 외곽선 | 상하 **6px**, 좌우 **4~6px** | 상 1766~1771, 하 1908~1913 | `#000000` |
| **밝은 안쪽 림** | **6~7px** (전 둘레) | 상 1773~1779, 좌 122~128, 우 351~356 | `#D3D3D3` (211,211,211) |
| 면 그라데이션 | — | 1780~1893 | `#A5A5A5` (위) → `#989898` (중) → `#8D8D8D` (아래) |
| 하단 밝은 림 | **5px** | 1895~1899 | `#D3D3D3` |
| **하단 어두운 립(3D)** | **6px** | 1901~1906 | `#767676` (118,118,118) |
| **radius (면)** | **≈ 29** | 원호 피팅 btn② 좌상단: dy 8→dx 9(r=29.0), dy 13→dx 5(r=29.4), dy 17→dx 2(r=27.2) | — |
| radius (바깥) | ≈ **34** | 추정: 29 + 5 | — |

### 5-3. 버튼 타이틀 텍스트

| 버튼 | 문자열 | 잉크 x | 잉크 y | w | h | 환산 font-size | 중심 x | 색 |
|---|---|---|---|---|---|---|---|---|
| ① | `10회 소환` | 171 ~ 309 | 1797 ~ 1826 | **139** | **30** | **32** | 240.0 (면 중심 239.5) | `#DFDFDF` |
| ② | `10회 소환` | 471 ~ 609 | 1797 ~ 1826 | **139** | **30** | **32** | 540.0 (면 중심 540.0) | `#DFDFDF` |
| ③ | `30회 소환` | 768 ~ 913 | 1797 ~ 1826 | **146** | **30** | **32** | 840.5 (면 중심 840.0) | `#DFDFDF` |

- 글리프 런 ①: `1` 171~178 / `0` 186~205 / `회` 212~236 / (공백) / `소` 252~275 / `환` 281~309
- 글리프 런 ③: `3` 768~784 / `0` 789~809 / `회` 815~840 / (공백) / `소` 856~879 / `환` 885~913
- advance: `0`→`회` **26**, `소`→`환` **29**, 어절 공백 gap **16**
- 자간: font-size 32 대비 한글 advance 29 = **0.91em** → **letter-spacing ≈ −3**
- 검정 외곽선 **3~4px** (외곽선 포함 bbox ② x 456~615, y 1794~1830)
- 면 상단(1772) → 잉크 상단(1797) = **25**

### 5-4. 내부 어두운 알약 (가격/횟수 배경)

3개 모두 y **1842 ~ 1877** (h **36**), radius = **18 (= h/2, 완전 알약)**, 테두리 **없음**.

| 버튼 | 알약 x | w | 색 | 측정 근거 |
|---|---|---|---|---|
| ① | **204 ~ 335** | **132** | `#737373` | y=1858 행: 195~203 면 → 205~334 알약 → 335~349 면 |
| ② | **≈442 ~ 634** | **≈193** | `#747474` | 우단 634 확정(y=1860). **좌단은 젬 아이콘에 가려짐**, x=440 은 면색 `#9A9899` 이므로 441~443 사이 |
| ③ | **≈742 ~ 934** | **≈193** | `#747474` | 우단 934 확정(y=1860). 좌단 동일 사유로 추정 |

- 알약 세로: col x=215(①) / x=620(②) / x=920(③) 전부 **1842~1877** 로 완전 일치.
- 버튼 면 기준 안쪽 여백: ② 좌 19 / 우 23, ③ 좌 20 / 우 24. **①만 좌 82** (FREE 뱃지 자리 확보로 알약이 오른쪽으로 밀림).

### 5-5. 버튼 ① 전용 — `FREE` 뱃지

버튼 면 **왼쪽 바깥으로 튀어나온** 리본형 태그. (면 좌단 122 보다 왼쪽인 118 부터 시작)

| 항목 | 값 | 측정 근거 |
|---|---|---|
| 분홍 면 bbox | x **118 ~ 190** (w **73**), y **1843 ~ 1875** (h **33**) | 분홍 마스크 |
| 검정 외곽선 포함 | x ≈116 ~ 192 (w 77), y **1840 ~ 1878** (h **39**) | col x=155: 1840~1842 검정, 1877~1878 검정 |
| 외곽선 두께 | **2~3px** | — |
| 면 색 (좌 2/3) | `#FF5D7A` (255,93,122) | y=1845 행: x 121~166 |
| 면 색 (우 1/3) | `#FF3964` (255,57,100) | y=1845 행: x 167~190 |
| `FREE` 잉크 bbox | x **131 ~ 179** (w **49**), y **1853 ~ 1866** (h **14**) | 노랑 마스크 |
| **환산 font-size** | **15** (14 / 0.93) — ※ 라틴 대문자라 0.93 비율 부적합, 아래 «불확실» 참조 | — |
| 잉크 색 | `#F7EB73` (247,235,115), 그늘 `#B2A944` | — |
| 뱃지 ↔ 알약 gap | 190 → 204 = **14** | — |

### 5-6. 버튼 ①·②·③ 알약 내부 텍스트 / 아이콘

| 버튼 | 요소 | x | y | w | h | 환산 font-size | 색 |
|---|---|---|---|---|---|---|---|
| ① | `0/1` 잉크 | **245** | **1846** | **50** | **25** | **27** | `#FC716C` (252,113,108) |
| ② | 젬 아이콘 (시안 면) | **443** | **1837** | **44** | **45** | — | 하이라이트 `#B3FFFF` → `#61F9FE` → 하단 `#009EDF` |
| ② | 젬 (검정 외곽선 포함) | ≈440 | **1834** | ≈50 | **53** | — | `#000000` 2~3px |
| ② | `1,000` 잉크 | **511** | **1848** | **90** | **25** | **27** | `#FC716C` |
| ③ | 젬 아이콘 (시안 면) | **743** | **1837** | **44** | **45** | — | ②와 동일 |
| ③ | `3,000` 잉크 | **808** | **1848** | **97** | **25** | **27** | `#FC716C` |

- `1,000` 글리프 런: `1` 511~517 / `,` 524~528 / `0` 535~552 / `0` 559~576 / `0` 583~600 → advance **24**
- `3,000` 글리프 런: `3` 808~822 / `,` 828~831 / `0` 838~855 / `0` 862~879 / `0` 887~904 → advance **24~25**
- `0/1` 글리프 런: `0` 245~262 / `/` 271~279 / `1` 287~294
- 가격 텍스트 검정 외곽선 **3px** 내외.
- **젬은 알약의 왼쪽 끝을 완전히 덮는다** — 젬 y 범위(1834~1886)가 알약(1842~1877)보다 위아래로 넓어서 알약 좌측 캡이 전혀 안 보임.

---

## 6. `터치하여 닫기`

| 항목 | 값 | 측정 근거 |
|---|---|---|
| 흰 잉크 bbox | x **412 ~ 667** (w **256**), y **2124 ~ 2164** (h **41**) | x 200~900 / y 2090~2190 흰 마스크 |
| **환산 font-size** | **44** (41 / 0.93) | — |
| 색 | **`#FFFFFF`** (순수 흰색) | col x=420 y 2128~2133 = (255,255,255) |
| **외곽선(스트로크)** | **없음** | col x=420: 2124 `#000000`(=딤 배경) → 2127 `#D0D0D0` → 2128 흰색. AA 2~3px 외에 검정 링 없음 |
| 글리프 런 | 터 412~444 / 치 453~486 / 하 494~532 / 여 537~571 / (공백) / 닫 592~628 / 기 634~667 | 열별 흰 픽셀 프로파일 |
| advance | 터→치 **41**, 치→하 **41**, 하→여 **43**, 닫→기 **42** (평균 **41.8**) / 어절 공백 gap **21** | — |
| 자간 | font-size 44 대비 advance 41.8 = **0.95em** → **letter-spacing ≈ −2** | — |
| 정렬 | 중심 x **539.5** = 화면 중앙 | — |
| 세로 위치 | 버튼 하단(1913)에서 **211** 아래, 화면 하단(2340)에서 **176** 위 | — |

---

## 7. 이 화면에 **없는 것** (명시)

| 요소 | 상태 |
|---|---|
| 모달 껍데기 / 다이얼로그 프레임 | **없음** — 패널이 전폭이고 좌우 테두리·radius 자체가 없다 |
| 닫기 `✕` 버튼 | **없음** (리본·패널 전 영역 스캔) |
| 타이틀바 | **없음** (리본이 대신함) |
| 스크롤바 | **없음** |
| 페이지 인디케이터 | **없음** |
| 패널 배경 무늬·그라데이션 | **없음** (`#2A2835` 단일색) |
| 카드 면 그라데이션 | **없음** (세로 Δ ≤ 4) |
| 패널 외곽 그림자 | **없음** |
| `터치하여 닫기` 검정 외곽선 | **없음** |

---

## 8. 주요 색 팔레트 요약

| 역할 | 색 | RGB |
|---|---|---|
| 딤 오버레이 | `rgba(0,0,0,0.80)` | — |
| 패널 본문 | **`#2A2835`** | 42,40,53 |
| 패널 안쪽 베벨 | `#414455` / `#424556` | 65,68,85 |
| 패널 금색 라인 (상) | `#FFDC38` → `#F0BA1A` | — |
| 패널 금색 라인 (하) | `#F8DE4D` → `#EAC22F` | — |
| 리본 중앙 밴드 | **`#FFBE56`** | 255,190,86 |
| 리본 밴드 하이라이트 | `#FEEF88` | 254,239,136 |
| 리본 꼬리 | **`#F69632`** | 246,150,50 |
| 리본 꼬리 그늘 | `#DD8335` / `#A55019` | — |
| 카드 초록 면 / 림 | **`#97C034`** / `#C7E746` | 151,192,52 |
| 카드 갈색 면 / 림 | **`#7D5A44`** / `#AC7B5B` | 125,90,68 |
| 카드 파랑 면 / 림 | **`#347DC1`** / `#47A7E1` | 52,125,193 |
| 카드·아이콘 외곽선 | `#000000` | 0,0,0 |
| 버튼 밝은 림 | **`#D3D3D3`** | 211,211,211 |
| 버튼 면 | `#A5A5A5` → `#989898` → `#8D8D8D` | — |
| 버튼 하단 립 | `#767676` | 118,118,118 |
| 버튼 내부 알약 | `#747474` | 116,116,116 |
| 버튼 타이틀 잉크 | `#DFDFDF` | 223,223,223 |
| 가격/횟수 텍스트 | **`#FC716C`** | 252,113,108 |
| FREE 뱃지 (좌/우) | `#FF5D7A` / `#FF3964` | — |
| FREE 잉크 | `#F7EB73` | 247,235,115 |
| 젬 아이콘 | `#B3FFFF` → `#61F9FE` → `#009EDF` | — |
| `터치하여 닫기` | `#FFFFFF` | 255,255,255 |

---

## 9. 불확실한 항목

| 항목 | 잰 값 | 왜 애매한가 |
|---|---|---|
| 딤 알파 | **0.80** | sRGB 최소자승 k=0.1979. 밝은 구간(b≥160)에서 0.198~0.208 로 안정적이라 신뢰도 높음. 다만 절편 포함 fit 은 `0.2135·B − 2.75` 라 **α=0.78~0.80** 범위. 어두운 구간은 JPEG 잡음으로 판독 불가 |
| 리본 검정 외곽선 **상단** 두께 | 측정 불가 | 리본 위 배경이 딤으로 `#000000~#030301`. 검정 외곽선과 배경이 구분 안 됨. 좌우(4~6px)로 유추할 수밖에 없음 |
| 리본 중앙 밴드 radius | **≈ 8** | dx가 dy에 대해 5,4,3,2,1,0 으로 **선형**이라 원호라기보다 45° 챔퍼에 가깝다. 실제 CSS 는 `border-radius: 8px~10px` 급 |
| 버튼 검정 외곽선 두께 | **좌우 4~6 / 상하 6** | ①좌 4px(117~120) vs ①우 6px(358~363), ②좌 6px(416~421). JPEG AA 때문에 4/5/6 확정 불가. **5px 권장** |
| 버튼 ②③ 알약 좌단 | **≈442 / ≈742** | **젬 아이콘이 알약 좌측 캡을 완전히 덮는다.** 젬(y 1834~1886)이 알약(y 1842~1877)보다 상하로 넓어 «가림 없는 행»이 존재하지 않음. x=440 이 면색이고 x=443 부터 젬이므로 441~443 사이로만 좁힘. 우단(634/934)과 세로(1842~1877)는 확정 |
| 버튼 타이틀 색 `#DFDFDF` | 223 | 순수 흰색(255)이 아님. 같은 화면의 `터치하여 닫기` 는 정확히 `#FFFFFF` 이므로 **버튼 텍스트는 원래 약간 회색**이거나 은색 버튼 스타일의 일부. `10-상점-팝업-소환-탭.jpg` 의 은색 버튼도 동일 톤 |
| `FREE` 환산 font-size 15 | 14 / 0.93 | **0.93 잉크비는 한글 기준.** `FREE` 는 라틴 **대문자만**이라 잉크높이 = cap-height ≈ 0.70~0.72em. 그 비율로 환산하면 **font-size ≈ 19~20**. 실제로는 19~20 이 맞을 가능성이 높다 |
| 숫자 텍스트 환산 font-size (`1` 27 / `0/1` 27 / `1,000` 27) | — | 마찬가지로 숫자는 어센더·디센더가 없어 잉크비가 한글보다 작다. 실제 font-size 는 **여기 적은 값보다 큼**(28~32 추정). 한글 항목(`소환 결과` 44 / `10회 소환` 32 / `터치하여 닫기` 44)은 신뢰도 높음 |
| `소환 결과` 자간 −5 | — | advance 37~40 vs 환산 font-size 44 에서 역산. font-size 가 실제로 40 이고 자간 0 일 가능성도 남음. **scaleX 0.88 로 맞추는 편이 안전** |
| 카드 크기 157 vs 158 | **157** | 검정 외곽선 바깥 AA 픽셀(값 0x16/0x1B)을 포함하면 159~160, 완전 불투명 검정만 세면 157. pitch 170 은 6열 평균이라 확정. **157×157 + gap 13** 권장 |
| 카드 radius 34 | 34 | 원호 피팅이 dy 8~12 에서 r≈33.9, dy 20~25 에서 r≈36.6~37 로 벌어짐. 외곽선 AA 때문. **33~36 범위**, 34 채택 |
| 아이콘 «표준 박스» 76×75 | — | 잉크 bbox 이므로 아트마다 다르다(52~77). 실제 스프라이트 프레임 크기는 알 수 없음. 카드 안쪽 여백 40px 기준으로 **77×77 프레임** 추정 |
| 개수 `1` 배지 배경 | **없음** | 알약·원형 배경 없이 글자만 있다. 카드 바닥 아래로 8px 돌출하므로 `overflow: visible` 필요 |
| 배경(딤 뒤) 요소 좌표 | — | 딤 상태에서 잰 값. **색은 원색이 아님**(≈ 원색 × 0.20). 원색은 `10-상점-팝업-소환-탭.jpg` 로 재측정 필요 |

---

## 10. §1 정오표 — 하단 버튼·«터치하여 닫기» 는 **하단 앵커**다 (작업 84, 2026-08-26)

§0 판정 그대로 «패널 아래에 딤된 배경 + 별도 버튼/닫기» 이므로, 이 두 덩어리는 패널이 아니라
**화면 하단(탭바) 기준**으로 놓인다. 레퍼런스 2340 → 프레임 2280 변환은 요소마다 다르다:

| 요소 유형 | 변환 | 이 화면의 대상 |
|---|---|---|
| 상단 앵커 | `프레임 y = ref y − 84` | 리본(§1 1) · 결과 패널(§1 2) · 그리드(§1 3·4) |
| **하단 앵커** | **`프레임 y = ref y − 60`** | **하단 버튼 3개(§1 5) · «터치하여 닫기»(§1 6)** |

(2280 = 2340 − 상단 84 + 하단 24. 남는 24px 은 전투 캔버스가 흡수한다 → 하단 거리는 보존, 차이는 60.)

### 프레임 목표값 (1080×2280)

| 요소 | ref (2340) | 프레임 (2280) | 하단에서 |
|---|---|---|---|
| 버튼 3개 외곽 | y 1766~1913 (h148) | y **1706~1853** | edge **426** |
| 닫기 잉크 | y 2124~2164 (h41) | y **2064~2104** | edge **175**(잉크 하변) |
| 버튼↔닫기 간격 | 211 | 211 | — |

- **CSS 는 `.sm-btns{bottom:426px}` · `.sm-close{bottom:175px}`.**
  §1 의 y 가 **포함 행**이라 하변 edge 는 1914(=2340−426)다 — «2340−1913=427» 로 잡으면 1px 위로 간다.
- 닫기의 175 는 **우리 서체 실측**값이다. 우리 잉크는 43px(ref 41)이고 CSS 박스 하변보다 1px 아래로
  나가므로 상·하변 동시 일치가 불가능 → **잉크 중심**(프레임 2084)을 맞추는 값으로 골랐다.
- 짧은 프레임(#app 높이 clamp 하한 1600)에서는 절대값 합이 프레임을 넘으므로
  `min(426px, calc(100% - 1416px))` / `min(175px, calc(100% - 1480px))` 로 겹침만 막았다(작업 84 §4).
- 게이트: `node tools/verify84.js` (30항목) · 캡처 `tools/cap12.js` · 잉크 스캔 `tools/scan12.py`.

## 11. 정오표 — 작업 102 (2026-08-26): §5 의 은색·빨강은 **«부족» 상태**의 실측이다

§5 머리말의 «3개 모두 동일한 은색(silver) 버튼 스타일» 은 맞지만, 그것이 **이 화면의 고정 색은 아니다.**
이 레퍼런스는 **무료 0/1 · 다이아 0**(§5-6: ① `0/1` · ② `1,000` · ③ `3,000` 이 전부 `#FC716C` 빨강)
상태로 찍힌 것이라 버튼 3개가 전부 «못 사는» 색이었다. 10 레퍼런스에서는 **같은 프레임에**
10회(1,000)=노랑+흰 가격 / 30회(3,000)=은색+빨강 가격이 공존한다 → 색은 **구매 가능 여부**의 표시다.
(저장소 주인 보고 2026-08-26: «돈이 충분해도 회색 버튼에 빨간 글씨다»)

| 상태 | 면 | 알약 | 라벨 | 가격/횟수 |
|---|---|---|---|---|
| **부족(`.lack`) — §5 실측 그대로** | 은색 밴드(림 `#D3D3D3` · 면 `#A8A8A8`→`#989898`→`#8B8B8B` · 립 `#757575`) | `#737373` | `#DFDFDF` | `#FC716C` |
| **충분 — ②③ 다이아 버튼** | 같은 밴드 구조에 10 레퍼런스 노랑 대입(림 `#F6EF3E` · 면 `#FCEF31`→`#FEC01D` · 립 `#D87C0D`) | `#C39313`→`#C38607` | `#F8F8F8` | `#FFFFFF` |
| **충분 — ① 무료 버튼** | 10 레퍼런스 무료(▶AD) 버튼 시안(림 `#53E6FA` · 면 `#3DD5EC`→`#18B1D3` · 립 `#0DA2CA`) | `#0F8FAE`→`#0D82A0` | `#F8F8F8` | `#FFFFFF` |

- **부족 상태의 픽셀은 한 개도 안 바뀐다** — `.lack` 이 §5 의 값을 그대로 되살린다(게이트가 색·기하 Δ0 로 잰다).
- «충분» 은 이 레퍼런스에 없는 상태다 → **디자인 결정**: 10 레퍼런스의 활성 색(노랑·시안)을
  이 화면의 밴드 구조(6/4/30/60/12/6/6/1/6/1px)에 그대로 대입했다. ① 무료 버튼의 알약색만
  레퍼런스에 조합이 없어(시안 면 + 알약) 면 대비 명도비를 노랑의 그것(−38%)과 맞춘 값이다.
- 게이트: `node tools/verify102.js` (80항목) · 기록 `docs/review/102-소환버튼가격표시.md`.

---

## 12. 정오표 — 작업 12 2차 폴리시 라운드 (2026-08-26, sess-0335-11085 워커 C)

### 12-1. 캡처 상태 규약 — **레퍼런스는 «부족» 상태다**

이 화면을 다시 잴 때는 반드시 `node tools/cap12.js <경로> --ref` 로 띄운다.
`--ref` 없이 띄우면 `S.dia = 1e9` 라 버튼이 «충분»(시안·노랑) 색이 되고, 무작위 10연이라
중복이 개수로 합쳐져 **칸 수가 6열 2행 10칸이 아니게 된다.** 04 비고 1(«캡처 상태가 다르면 그 회차 비평은
통째로 무효») 에 그대로 걸린다 — 실제로 이번 라운드에서 비평가 2인분을 그렇게 날렸다.

### 12-2. 텍스트 잉크 목표 bbox (게이트 `tools/verify12.js` 가 지키는 값)

`scaleX`·`font-size` 같은 **보정 계수는 서체가 바뀌면 전부 무효**가 된다(작업 126 의 Jua 전환에서
이 화면 텍스트 8종이 전부 −11~15% / +11~12% 로 어긋났다). 그래서 계수가 아니라 **목표 잉크 bbox** 를 남긴다.

| 요소 | ref 잉크 (1080×2340 좌표) | 프레임 y (변환) |
|---|---|---|
| 타이틀 `소환 결과` | x455 y747 **171×41** | 663 (상단 앵커 −84) |
| `터치하여 닫기` | x412 y2123 **256×42** | 2063 (하단 앵커 −60) |
| 버튼①② 라벨 `10회 소환` | x170/470 y1796 **140×31** | 1736 |
| 버튼③ 라벨 `30회 소환` | x767 y1796 **147×31** | 1736 |
| `1,000` / `3,000` | y1847 **92×26** / **98×27** | 1787 |
| `0/1` (현재 `0/2`) | x244 y1846 **51×25** | 1786 |
| `FREE` | x131 y1853 **49×14** | 1793 |
| 젬 아이콘 (시안 면) | x443 y1837 **44×45** | 1777 |
| 개수 배지 `1` (r0c0 / r1c0) | x109 y1039 / y1209 **10×26** | 955 / 1125 |

- **배지는 «카드 바닥 외곽선 아래로 8px 돌출»** 이 형태의 핵심이다(ref 잉크 하단 1064 vs 카드 바닥 1056).
  안쪽으로 들어가면 «걸터앉은 배지» 가 «갇힌 숫자» 가 된다 — 비평가 4인이 전원 지적한 항목이다.
- **버튼 «부족» 상태의 계조는 ref 그대로여야 한다**(x540 열 기준 림 211 · 면 171 · 면 162 · 하단 림 211).
  `.sm-b:disabled` 에 filter 를 걸면 여기가 0.72 배로 내려앉는다 — `:not(.lack)` 로 막아 뒀다.

### 12-3. ⚠ 임계값 함정 — ref 는 JPEG 다

카드 타일·행 간격을 잴 때 마스크 임계값에 따라 **ref 만** 값이 변한다(캡처 PNG 는 고정):

| `max(RGB) <` | ref 카드 h / 행간 | cap |
|---|---|---|
| 15 · 20 | **157 / 13** | 157 / 13 |
| 25 | 158 / 12 | 157 / 13 |
| 30 · 40 | 158 / 11 | 157 / 13 |

**엄격한 값(<20)이 참이다 — 카드 157 · 행간 13 은 이미 맞다.** 느슨한 마스크로 재고 «−1~2px 작다» 고
고치면 열 pitch 가 169 가 되어 6열 정렬이 마지막 열에서 −5px 까지 밀린다.

> ⚠ **위 표는 «세로(카드 h / 행간)» 축만이다 — 가로에 일반화하지 마라.** 5회차에서 뒤집혔다: 12-5 를 볼 것.

### 12-4. «아트 필요» 표 (GPT 아트 발주서용)

이모지·CSS 로 대체한 요소들이다. **아래 bbox 를 정확히 차지하고 있으므로 이미지로 갈아 끼우기만 하면 된다.**

| 요소 | w×h | 중심 (프레임 좌표) | 용도 | 현재 대체물 |
|---|---|---|---|---|
| 카드 아이템 아트 ×10 | **76×75** (§4-4) | 1행 (114.5, **893**) 부터 pitch 170 ×6 · 2행 y **1063** | 소환 결과 아이템 그림 | 이모지 (`itemIcon`), `--if-ic:64px` |
| 다이아 젬 아이콘 ×2 | **44×45** (검정 외곽선 포함 50×53) | (**464.5, 1799.5**) / (764.5, 1799.5) | 가격 앞 재화 아이콘 | `.cic` SVG(`CUR_ICON.dia`, 원본 64×64) + `scale(.983)` **등방** ← 정오표 356-16 |
| 리본 꼬리 ×2 | **89×93** | (287.5, 705.5) / (792.5, 705.5) | 리본 좌·우 모래시계형 꼬리 | CSS `clip-path` 다각형 |
| `FREE` 리본 태그 | 분홍 면 **73×33** (외곽선 포함 79×39) | 면 중심 (**154, 1799**) — 실측 x118~190 · 프레임 y1783~1815 | 무료 소환 뱃지 | CSS `clip-path` + 텍스트 |
| 버튼 은색/시안/노랑 밴드 ×3 | **247×148** | (240.5, 1780) / (539.5, 1780) / (839.5, 1780) | 재소환 버튼 3종 상태 | CSS 다단 `linear-gradient` |

> 서체 대기분(⑤⑥, 통과를 막지 않음): 한글 글리프 형태(획 끝맺음·받침)와 숫자 `1` 자형(레퍼런스는 밑굽 없는
> 막대형). **잉크 bbox 는 위 표대로 이미 Δ≤2 로 맞다** — 서체를 갈아 끼울 때 bbox 를 유지할 것.


### 12-5. 정오표 — 카드 bbox 는 **158×158**(가로축 재판정, 5회차)

12-3 의 «임계값 노이즈» 판정은 **세로 축에서만 참**이었다. 5회차에 비평가 K(엄격 임계 `max<20` 에서 158)와
L(50% 서브픽셀 교차 158.2)이 **각자 다른 방법으로 같은 값**을 냈고, L 은 원 픽셀값(`ref x193=16` 은
`<20` 마스크에 이미 포함)으로 «`<20` 이면 157 로 같아진다» 를 정면 반박했다. 다시 재니 **폭은 `<20` 에서도
ref 158** 이다 — 4회차가 세로 결과를 가로에 일반화한 것이 틀렸다.

| | 4회차까지 | **확정(5회차~)** |
|---|---|---|
| 카드 타일 (`--if-w`) | 157 | **158** |
| 칸 간격 gap | 13 | **12** |
| 열 pitch | 170 | **170 (불변 — 158+12 = 157+13)** |
| 6열 우단 Δ / 그리드 우측 여백 | −1~−2 / 37.5 (ref 36) | **0 / 36 (Δ0)** |

pitch 를 유지하는 방식이라 좌단 정렬은 건드리지 않았다. 구현은 `index.html` `.sm-c{--if-w:158px}`,
게이트는 `tools/verify12.js` 와 `tools/verify104.js` ⑦ `BBOX['12 .sm-c'] = [158,158]` 이 함께 지킨다.
근거 전문 `docs/review/12-소환결과팝업.md` «통과 후 추가 반영 1건».

> **교훈: 한 축에서 얻은 «임계값 노이즈» 판정을 다른 축에 그대로 옮기지 마라. 축마다 따로 스윕한다.**

### 12-6. 정오표 — 개수 배지 배율 `--ifq-k` 는 **.218**(12 전용, 5회차)

공용 `.ifr>.ifq` 의 기본 배율은 141 이 정한 **.235** 이고, 12 는 자기 화면에서만 `#sumw .sm-c{--ifq-k:.218}`
로 덮는다(공용 규칙은 안 건드린다 — 141 이 열어 둔 «화면별 입력» 구조 그대로다).
잉크 높이 **28 → ref 26**(−7.7%)이고, 비평가 4인이 «+8~12% 과대» 로 일치 지적한 건이다.
`tools/verify104.js` ⑥ 이 «선언값이 승인값(.218)인가» + «렌더가 그 선언을 따르는가» 두 겹으로 지킨다.

## 13. 정오표 — 패널 높이 **539 는 «2행 케이스» 한정**이다 (작업 187, 2026-08-27)

§1 의 «패널 793~1331(h539)» 과 §12 계열 게이트 값은 **레퍼런스와 같은 10칸 2행일 때**의 값이다.
작업 187(저장소 주인 지시 «30연 결과가 가려진다 — 2배 정도로»)부터 패널은 **결과 행수를 따라간다**:

| 결과 행수 | `--sm-gh`(그리드 h) | 패널 h | 패널 top~하변 | 보이는 행 |
|---|---|---|---|---|
| 1~2 (= **ref 케이스**) | **335** | **539** | 709~1248 | 2 |
| 3 | 506 | 710 | 709~1419 | 3 |
| 4 이상 | 676 (상한) | 880 | 709~1589 | **4** |

n행 높이 = `170n − 12(마지막 gap 없음) + 8(배지 돌출)` = **170n − 4** (pitch 170 = 카드 158 + gap 12).

**이 표를 읽는 법 — 30연 캡처를 ref 와 직접 대면 안 된다.**
ref(`docs/ref/12-*.jpg`)는 10칸 2행 캡처다. 12 를 다시 잡는 세션은 반드시 **2행 상태로 캡처**해야
§1·§12 값과 같은 잣대가 된다. 30연 상태는 «주인 지시로 ref 와 다른 것» 이므로 감점 사유가 아니다.

상한 676 의 근거 — 84 가 정한 하단 앵커(`.sm-btns{bottom:min(426px,100%−1416px)}`)를 건드리지 않고
«패널 하변 + 20 ≤ 버튼 상변» 을 지키는 값이다. 2280 프레임의 산술 최대는 977(5.7행)이지만
**«2배» 라는 주인 확정 배율**을 그대로 지켰다 — 더 키우려면 주인 재확인이 필요하다.

게이트 `tools/verify187.js`(63/63) A 절이 «2행 = ref 픽셀 Δ0», C 절이 «30칸 = 보이는 행 4» 를 지킨다.

---

## 14. 정오표 — §13 을 **작업 327 이 대체한다**: 패널은 **1080 고정**(= ref 539 × 2) (2026-08-28)

저장소 주인 재지시: «소환결과가 창이 너무 작음. 걍 세로로 2배 정도 늘리던지 해라» (187 후속).

§13 의 «행수 가변» 표는 **더 이상 유효하지 않다.** 187 이 두 가지를 놓쳤기 때문이다:

* ① 저레벨 배너처럼 중복이 개수 배지로 합쳐지는 **흔한 결과**(칸 ≤ 12)는 계속 ref 크기 539 였다 —
  주인이 «또» 작다고 한 것이 이 흔한 쪽이다(187 §7 의 실측 메모가 이미 예고하고 있었다).
* ② 상한 676(4행) 탓에 고유 30종이 나오는 최악 케이스는 **여전히 6칸이 가려졌다**(24/30).

327 은 «가변» 을 버리고 **고정** 으로 간다.

### 14-1. 프레임 목표값 (1080×2280)

| 항목 | ref (§1) | 187 | **327** |
|---|---|---|---|
| `--sm-gh` (그리드 h) | 335 | 335 / 506 / 676 (가변) | **876 고정** |
| 패널 h | 539 | 539 / 710 / 880 | **1080** (= 204 + 876 = ref × 2.00) |
| 패널 top | 709 | 709 | **606** |
| 패널 하변 | 1248 | 1248 / 1419 / 1589 | **1686** (= 버튼 상변 1706 − 20) |
| 리본(`.sm-rb`) top | 641 | 641 | **538** (= 패널 top − 68) |
| 버튼 상변 (84 앵커) | 1706 | 1706 | **1706** (Δ0 — 안 움직인다) |
| 30연에서 보이는 칸 | — | 24 / 30 | **30 / 30** |
| 카드 첫 행 top (2행) | 815 | 815 | **982** (세로 중앙정렬) |

### 14-2. 왜 «위로 뜨는가» — 패널 top 은 계산값이지 상수가 아니다

패널이 1080 이면 top 709 로는 84 가 정한 버튼(상변 1706)을 침범한다(709 + 1080 = 1789).
그래서 **필요한 만큼만** 위로 뜬다. 프레임 높이 H 에 대해:

```
--sm-bot = max(100% − 594px, 1248px)      ← 버튼 상변 − 20 을 푼 값
                                             (H ≥ 1842: H − 574 − 20 · H < 1842: 1268 − 20)
--sm-top = max(24px, min(709px, --sm-bot − 1080px))
패널 h   = min(1080px, --sm-bot − --sm-top)
```

| 프레임 H | `--sm-bot` | 패널 top | 리본 top | 패널 하변 | 버튼 상변 |
|---|---|---|---|---|---|
| 2600 (상한) | 2006 | **709** (ref 자리 그대로) | 641 | 1789 | 2026 |
| 2280 (기준) | 1686 | **606** | 538 | 1686 | 1706 |
| 1920 | 1326 | 246 | 178 | 1326 | 1346 |
| 1600 (하한) | 1248 | 168 | 100 | 1248 | 1268 |

즉 **긴 프레임에서는 ref 자리 그대로**이고, 짧아질수록 위로 뜬다. 어느 경우에도
패널 하변 ↔ 버튼 상변은 정확히 20 이고 리본 top 은 0 아래로 안 내려간다(`verify327` §D).

### 14-3. 세로 중앙정렬 — «고정 확대» 를 쓸 수 있게 만드는 한 줄

187 이 고정 확대를 물린 이유가 «2행일 때 카드 아래로 340px 빈 검은 면» 이었다.
327 은 `.sm-grid-in{min-height:100%;box-sizing:border-box;align-content:safe center}` 로
빈 면을 **위아래로 나눈다**. 카드 첫 행 top = `그리드 top + (그리드 h − 8 − (170n − 12)) / 2`:

| 결과 행수 n | 카드 블록 h | 첫 행 top | 남는 여백(위=아래) |
|---|---|---|---|
| 1 | 158 | 1067 | 355 |
| 2 (= ref 케이스) | 328 | **982** | 270 |
| 3 | 498 | 897 | 185 |
| 5 (= 30연 최악) | 838 | 727 | 15 |

`safe` 키워드는 «내용이 상자보다 커지면 중앙 대신 위 정렬» 이라 스크롤 컨테이너에서 첫 행이
위로 잘려 못 닿는 고전 버그를 막는다. 876 ≥ 846 이라 지금은 안 걸리지만 `--sm-gh` 를
줄이는 순간 걸린다 — `verify327` G5 가 그 경우를 실제로 만들어 확인한다.

### 14-4. **30연 캡처를 ref 와 직접 대면 안 된다** (§13 의 이 문장은 그대로 유효하다)

ref(`docs/ref/12-*.jpg`)는 10칸 2행 캡처다. 다만 327 이후로는 **2행 상태로 캡처해도**
패널·리본·카드가 ref 와 다르다. 게이트 `tools/verify12.js` 는 이것을 «오차» 가 아니라
**평행이동**으로 처리한다(`tools/scan12r.py` 의 `SM_TOP=187` · `SM_CARD=−83`) —
리본·패널 상단 그룹은 위로 103, 카드·배지 그룹은 아래로 167. 버튼·«터치하여 닫기»(오프셋 60)는
**한 픽셀도 안 움직인다**. 12 를 다시 잡는 세션은 이 두 상수를 먼저 읽어라.

게이트: `tools/verify327.js`(85/85 신설) · `tools/verify187.js`(63/63, 327 값으로 이관) ·
`tools/verify12.js`(15/15) · `tools/verify84.js`(31/31, «패널 하변 ↔ 버튼 상변 20» 단언 신설).

---

## 15. 정오표 356-16 — **젬 알약의 「현재 대체물」은 이모지가 아니다** (2026-08-31, sess-1419-17298 워커 A)

§14 «아트 필요» 표의 «다이아 젬 아이콘 ×2» 행은 대체물을 오래 **«💎 이모지 + `scaleX(.983)`»** 로
적어 두고 있었다. 그 문장은 12 4회차의 것이고, 그 뒤 **125** 가 이 자리를 `data-cur-slot="dia"` 로
갈면서 span 안에 들어가는 것은 **`.cic` SVG**(`CUR_ICON.dia` · 원본 **64×64 정사각**)가 됐다.

표만 늙은 것이 아니라 **CSS 도 같이 늙어 있었다** — 이모지 잉크(40×45)를 ref 폭에 맞추려고
«가로만» 누른 `scaleX(.983)` 이 그대로 남아, 그림이 바뀐 뒤로는 **정사각 원본을 1.7% 옆으로**
눌러 왔다. 356 이 주인 지시로 폐기한 관행의 잔재다.

356 16회차가 397 «작은 쪽으로» 로 **등방화**했다(`transform:scale(.983)`). 실측(`tools/cal356r16.js`):

| 갈래 | 상자 | 잉크 | 종횡 | 원본비 편차 | 잉크 중심 |
|---|---|---|---|---|---|
| 수리 전 `scaleX(.983)` | 55.2016×56.1563 | 52×54 | 0.9630 | **−3.70%** | (464, 1799) |
| **채택 — `scale(.983)`** | 55.2016×55.2017 | **52×52** | **1.0000** | **0.00%** | (464, 1799) **Δ0** |
| (기각) 선언째 삭제 | 56.1563×56.1563 | 54×54 | 1.0000 | 0.00% | (464, 1799) |
| (기각) 삭제 + 정수 상자 56 | 56.0000×56.0000 | 54×54 | 1.0000 | 0.00% | (464, 1799) |

⚠ **«선언째 삭제» 는 종횡으로는 옳지만 크기가 ref 에서 멀어진다** — 잉크가 52 → 54 가 되어
이 표의 ref 젬(검정 외곽선 포함 **50×53**)에서 가로로 +2 → +4 로 벌어진다. 01 오프라인 보상
코인이 «걷어내는 것이 곧 ref» 였던 것과 **방향이 반대**라 같은 처방을 쓸 수 없었다.

**§14 의 bbox 목표값(44×45 / 50×53)은 안 고쳤다** — 아트가 들어오면 채울 자리 그대로다.
바뀐 것은 «현재 대체물» 칸 하나뿐이고, 레이아웃 좌표는 **한 픽셀도 안 움직였다**(중심 Δ0).

---

## §정오표 — 713 (2026-09-02 주인 정정): 그리드 안 카드가 ref 보다 **11px 위**에 앉는다

주인 정정으로 **소환 배수 토글(×1/×10/×100/×1000)** 이 이 팝업에 들어왔다(작업 713).
공용 셸 `.stabs` 는 96·437 규약으로 **98px 고정**인데, 이 팝업에서 그만한 빈 띠는
패널 아래 여백뿐이고 그 띠가 **83px**(패딩 98 − 하단 크롬 15) 밖에 없었다.

띠를 98 로 넓히느라 패널 안의 나눔이 바뀌었다 — **패널 총높이 1080(327 «세로 2배»)은 불변**이다:

| 값 | 327 | **713** |
|---|---|---|
| 위 여백 (`padding-top`) | 106 | **99** |
| 그리드 (`--sm-gh`) | 876 | **868** |
| 아래 여백 (`padding-bottom`) | 98 | **113** = 크롬 15 + 셸 98 |
| 패딩분 / 패널 | 204 / **1080** | 212 / **1080** |

⚑ **카드가 위로 가는 것은 피할 수 없다.** 이 패널은 하변이 84 버튼 줄에 묶여 있어
(패널 하변 = 버튼 상변 − 20) 아래에 자리를 만들면 내용이 그만큼 위로 밀린다:

```
카드 y = 패널상변 + 위여백 + (그리드 − 846)/2 ,  위여백 + 그리드 = 1080 − 113 = 967
      ⇒ 이동 = 위여백/2 − 60.5 ,  위여백 ≤ 99  ⇒ 최소 −11
```

패널을 키워도 상변이 같이 내려가 상쇄돼 **−11 이 하한**이다. ⇒ `tools/verify12.js` 는
ref 축 목표에 **`DY = −11` 한 상수**를 명시해 얹는다(무르게 푼 것이 아니라 한 값으로 적어
그 이상의 표류는 그대로 빨갛다). 영향 받는 항: `badge0`·`badge1` 의 y · «카드 2행 상·하단».

⚑ **그리드 하한이 846 이 아니라 868 이라는 것도 여기 적어 둔다** — 개수 배지(`.ifq`)는
절대배치라 스크롤 넘침에 그대로 들고 카드 바닥 아래로 **18.69px** 나오며, 줄은 중앙정렬이라
그리드를 줄여도 절반만 따라 올라온다: `(H − 846)/2 + 838 + 18.69 ≤ H ⇔ H ≥ 867.4`.
327 이 적은 «최악 5행 846» 은 그래서 **하한이 아니다**.

배수 바 자리: `left 36`(그리드 좌단) · `width 724`(= 4 × 181) · `bottom 15`(띠 [15,113] 을 꽉 채움) ·
오른쪽 «연출 스킵» 토글(363)은 `bottom 36` 으로 **같은 띠 한가운데** ⇒ 두 컨트롤의 세로 중심 **64 로 일치**.

---

## §정오표 — 746 (2026-09-01): 가격 잉크는 **레퍼런스보다 작다** (배수 ×1000 을 같은 회랑에 넣는 값)

§5-6 의 가격 잉크 실측(② `1,000` **90×25** · ③ `3,000` **97×25** · 환산 font-size **27**)은
**레퍼런스가 정의하는 ×1 상태의 값**이고, 우리 구현은 그 값을 **의도적으로 이탈한다.**

| 항목 | ref(§5-6) | 746 전 | **746 후(현행)** |
|---|---|---|---|
| ② `1,000` 잉크(외곽선 포함) | 90×25 | 99×33 | **72×23** |
| ③ `3,000` 잉크(외곽선 포함) | 97×25 | 104×32 | **75×23** |
| `.sm-b .cost` `font-size` | (환산 27) | 33 | **23** |
| `.sm-b .cost` `top` | — | 65 | **61** |
| `.sm-b2/.sm-b3 .cost` 상자 | — | `left 61 · w 146`(면 67..213) | **`left 69 · w 142`(면 75..217)** |

**이유** — 713 이 이 팝업으로 옮긴 배수 토글(×1/×10/×100/×1000)이 가격을 «1,000» 에서
**«3,000,000»(9자리)** 까지 늘린다. 젬 잉크 우변(면 **75**) ↔ 판 우변(면 **217**) 사이 가용 회랑은
**142px** 인데 33px 에서 그 문자열의 잉크는 **172px** 다. 자리를 옮겨서는 30px 이 모자라고,
배수마다 글자를 좁히는 길은 **735 가 금지**하며(주인 원문 «커졌다 작아졌다 그런거 하지마»),
축약 표기는 `fmt` 머리말(150·188)의 «재화·**가격**·개수·레벨·층수는 숫자 그대로» 가 막는다.
⇒ **네 상태 모두 한 크기**로 줄이는 길만 남았고, 그 크기가 23 이다.

⚠ **상자를 회랑과 같게 맞춘 것이 축소량을 한 단계 줄였다** — 옛 상자(면 67..213)는 회랑보다 6px
왼쪽으로 치우쳐 젬 쪽만 먼저 닿고 오른쪽 16px 을 놀렸다. 회랑 정렬 후 fs 23 에서 양쪽 여유 **6/7px**.
(`node tools/probe746.js --sweep` 이 후보별 최악 여유 표를 찍는다: 24 는 3/5 로 게이트 문턱 3 에 딱 앉아 무르다.)

⚠ **`top` 을 65 → 61 로 되올린 이유**는 `line-height:50` 이 고정이라 글자만 줄면 베이스라인이 안 움직여
잉크가 알약 안에서 3.3px 처지기 때문이다(알약 중심 Δy 는 −2.5 로 유지).

게이트 `node tools/verify746.js`(9/9) · 재현 `node tools/probe746.js`(4/4) ·
상세 `docs/review/746-소환가격젬침범.md`. 되돌리는 법은 `index.html` 의 746 주석에 세 값째 적혀 있다.
