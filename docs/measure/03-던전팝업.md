# 03 던전 팝업 측정표 (레퍼런스 1080x2340 기준, 가로 1:1)

- 측정 일자: **2026-08-23**
- 레퍼런스: `docs/ref/03-던전-팝업.jpg` (1080 x 2340, JPEG)
- 측정 방식: 원본 픽셀 직접 스캔(스캔라인 색 전이점 + 색 마스크 bbox + 코너 원호 피팅 + 수평 엣지 스텝 검출). 표기 좌표는 **원본 1080px 절대 좌표**, `x`/`y`는 요소 **좌상단**, 값은 모두 **정수 px**.
- `(추정)` 표기는 픽셀에서 직접 확정되지 않고 계산/보간/역산으로 얻은 값. 확정 불가한 항목은 **미확정**으로 명시했다.
- 카드 내부 요소는 **절대 좌표**와 **카드 좌상단(50, T) 기준 상대 좌표**를 같이 적었다. `T` = 각 카드의 top y.

---

## 0. 전역

| 항목 | 값 |
|---|---|
| 캡처 크기 | 1080 x 2340 |
| 팝업 배경(카드 바깥) | `#26211B` → 정확히 **`#26211B`** (38,33,27), 표준편차 ±2 (JPEG 노이즈 수준) |
| 전면 딤 오버레이 | **없음** |
| 근거 | 02번(비딤 메인 화면) 같은 좌표는 하늘색 (58,158,210). 03번은 (38,33,27)로 **스칼라 감쇠로 설명 불가**(R 0.66 / G 0.21 / B 0.13). 또 배경 영역(x 0~48, 1032~1079 / y 200~2150, 187,200px) 표준편차가 2.0~2.9뿐 → 게임 화면이 비치지 않는 **불투명 단색 패널**. |
| 하단 탭바 딤 여부 | 딤 없음 — (10,2200) 픽셀이 02번과 **완전히 동일** (66,54,42) |
| 상단 HUD 딤 여부 | 판정 불가(미확정) — 02번과 HUD 내용 자체가 달라 대조 불가 |
| 잠금 카드 전용 딤 | `rgba(0,0,0,0.65)` **(추정)** — 아래 3-6 참조 |

---

## 1. 모달(패널) 박스

| 항목 | 값 |
|---|---|
| 모달 패널 | **없음** |
| 근거 | x=10 세로 스캔의 색 전이점이 `86, 91, 92, 185, 191, 2164, 2333`뿐 — 상단 HUD/하단 탭바 테두리 외에 **좌우 어디에도 패널 테두리가 없다**. 던전 화면은 팝업창이 아니라 화면 전체를 채우는 **리스트 페이지**(불투명 배경 + 카드 5장 + 하단 서브탭). |
| 콘텐츠 영역 | x **0 ~ 1079** (전체 폭), y **192 ~ 2159** (HUD 하단 191 다음 ~ 탭바 상단 2160 직전) |
| 배경색 | `#26211B` (38,33,27) 단색 |
| 테두리 / radius / 그림자 | 없음 |
| 리스트 좌우 여백 | 좌 **50** / 우 **50** (카드 x 50 ~ 1029, 화면 중심 539.5 기준 완전 대칭) |

---

## 2. 타이틀바

| 항목 | 값 |
|---|---|
| 모달 타이틀바 | **없음** — `던전` 타이틀 텍스트/타이틀바가 존재하지 않는다 |
| 근거 | HUD 하단(191)과 첫 카드 상단(241) 사이 y 192~240 구간이 전 폭에 걸쳐 배경색(38,33,27) 단색. 글리프·박스 픽셀 0개 (해당 영역 평균 (39.4,34.5,28.7), 표준편차 2.7/3.4/2.0) |
| HUD 하단 ~ 첫 카드 간격 | **49** (191 → 241) |

---

## 3. 던전 카드 5장

### 3-1. 카드 공통 박스

| 항목 | 값 |
|---|---|
| x | **50** |
| width | **980** (50 ~ 1029) |
| height | **350** |
| 카드 pitch (세로 반복 간격) | **360** |
| 카드 간 세로 gap | **10** (예: 카드1 하단 590 → 카드2 상단 601, 배경 노출 591~600) |
| 첫 카드 y | **241** |
| 카드별 y (top) | 카드1 **241** / 카드2 **601** / 카드3 **961** / 카드4 **1321** / 카드5 **1681** |
| 카드별 y (bottom) | 590 / 950 / 1310 / 1670 / **2030** |
| 리스트 전체 세로 범위 | **241 ~ 2030** |
| 테두리 색 | `#000000` |
| 테두리 두께 | 좌우 **7** (x 50~56 / 1023~1029), 상하 **8** (y 241~248 / 583~590) |
| border-radius (바깥) | **12** (추정) — 좌상단 원호 최소자승 피팅 5장 평균 r=11.4/7.4*/14.9/12.1/11.1 (\*카드2는 JPEG 이상치) |
| border-radius (안쪽) | **5** (추정) — 바깥 12 − 테두리 7 |
| 안쪽 상단 하이라이트 | y+1~+3에 밝은 띠 1줄 (카드1 기준 `#806473` (128,100,115)), 이후 3px 만에 본색으로 수렴 |
| 안쪽 좌측 음영 | 좌측 안쪽 **약 13px** 어두운 그라데이션 (x 57 → 70에서 본색 도달). 별도 색 테두리는 **없음** |

### 3-2. 카드별 배경 / 등급색

카드 배경은 **좌측 단색 패널 + 우측 방사형(선버스트) 일러스트** 2영역으로 나뉘며, **하드 엣지가 아니라 그라데이션 페이드**다.

| 카드 | 던전명 | 좌측 단색 영역 대표색 | 우측 일러스트 바탕색 | 선버스트 밝은 광선색 |
|---|---|---|---|---|
| 1 | 골드 던전 | `#592F45` (89,47,69) | `#834B64` (131,75,100) | `#8F576F` (143,87,112) |
| 2 | 보석 던전 | `#154B3D` (21,75,61) | `#227C58` (34,124,88) | `#2C8A65` (44,138,101) |
| 3 | 아티팩트 던전(잠금) | `#14171E` (20,23,30) — 좌측도 일러스트라 완전 단색은 아님 | `#0D0F1B` (13,15,27) | — (딤으로 소실) |
| 4 | 동료 주사위 던전(잠금) | `#030E20` (3,14,32) | `#071832` (7,24,50) | — |
| 5 | 목걸이 던전(잠금) | `#0A1D48` (10,29,72) | `#0D3A51` (13,58,81) | — |

- 잠금 카드 3·4·5의 위 값은 **딤이 적용된 상태의 실측값**이다.
- 딤 역산한 원색 **(추정)**: 카드3 `#394256` / 카드4 `#092A5C` / 카드5 `#1D53CE` (실측값 ÷ 0.349)
- 단색↔일러스트 전이 x 실측: 카드1 y=255에서 **407~443**, y=575에서 **518~526** / 카드2 y=800에서 **471~474**. → 아래로 갈수록 경계가 오른쪽으로 밀리는 **우측 중심 방사 그라데이션**.

### 3-3. 던전명 텍스트

| 항목 | 값 (절대) | 상대(카드 좌상단 기준) |
|---|---|---|
| x (글리프 좌단) | **92** (5장 모두 동일) | **+42** |
| y (글리프 상단) | T+44 → 285 / 645 / 1005 / 1365 / 1725 | **+44** |
| 글리프 잉크 높이 | **51~52** | |
| 카드별 글리프 폭 | 골드 던전 **216** (92~307) / 보석 던전 **220** (92~311) / 아티팩트 던전 **377** (93~469) / 동료 주사위 던전 **378** (92~469) / 목걸이 던전 **275** (92~366) | |
| 색 | `#FDFF84` (253,255,132) — 세로 그라데이션 없음, 단색 | |
| 외곽선 | `#000000` **4~5px** (실측: x 86~90 검정 → 91부터 노랑) | |
| 글자 advance(실측) | **50~53** px/자 (골=92, 드=142 / 던=209, 전=262) | |
| font-size | **69** (추정, 잉크높이 52 ÷ 0.75) | |
| letter-spacing | **-17** (추정, advance 51.5 − 69) | |
| 정렬 | 좌측 정렬 (모두 x=92 시작) | |
| 잠금 카드 색 | `#565829` (86,88,41) — 딤 적용 결과 | |

### 3-4. 획득 가능 재화 알약

| 항목 | 값 | 비고 |
|---|---|---|
| y (1번째 알약) | T+**121** → 362 / 722 / 1082 / 1442 / 1802 | 5장 모두 동일 |
| y (2번째 알약, 카드3·5만) | T+**182** → 1143 / 1863 | 알약 간 세로 gap **15** |
| height | **46** | |
| border-radius | **23** (완전 알약) — 우측 캡 원호 실측: 중심 y=385에서 x=428, dy=22에서 x=406 → r≈22~23 | |
| x (좌단) | **142** (추정) | 좌단이 재화 아이콘에 완전히 가려 직접 측정 불가. 상단 엣지 스텝 검출 시작점 150 − 캡 보정 8로 역산 |
| x (우단) — 카드1·2 | **429** | 직접 실측 |
| width — 카드1·2 | **288** (추정, 좌단 추정치 기준) | |
| x (우단) — 카드3·4·5 | **미확정** | 자물쇠 아이콘(x 487~592)과 일러스트가 우측 끝을 가림. 엣지 검출이 x 476~478에서 끊김 |
| 배경색 (카드1 실측) | `#28121E` (40,18,30) | |
| 배경색 (카드2 실측) | `#041F18` (4,31,24) | |
| 배경 = 검정 오버레이 | `rgba(0,0,0,0.58)` **(추정)** — 카드1 40/89=0.449, 카드2 G 31/73=0.425 → 투과율 ≈0.42 | |
| 테두리 | **없음** (검정 외곽선 없음, 상단 경계가 361→363에서 바로 전이) | |

#### 3-4-1. 알약 안 아이콘

| 카드 | 아이콘 | bbox | 크기 | 대표색 |
|---|---|---|---|---|
| 1 | 골드 코인 | x **110~160**, y **360~412** | **51 x 53** | 본체 `#E69E0C` (230,158,12) / 링 `#FFE92D` (255,233,45) |
| 2 | 보석 | x **112~156**, y **722~768** | **45 x 47** | `#61FAFF` (97,250,255) |
| 3·4·5 | 각 재화 아이콘 | 위치·크기 동일 계열 (x 112 부근, y T+121 부근) | 미측정 | 딤 적용 |

- 배치: 아이콘 **중심이 알약 좌측 끝 근처**에 오도록 알약 위에 겹쳐 배치 (코인 중심 x=135, 알약 좌단 추정 142).

#### 3-4-2. 알약 텍스트

| 항목 | 카드1 `골드 획득 가능` | 카드2 `보석 획득 가능` |
|---|---|---|
| 글리프 bbox | x **202 ~ 377** (w **176**), y **372 ~ 398** (h **27**) | x **200 ~ 379** (w **180**), y **731 ~ 758** (h **28**) |
| 상대 좌표 | x +152, y +131 | x +150, y +130 |
| 색 | `#BB89A4` (187,137,164) | `#A5CBB2` (165,203,178) |
| 외곽선 | 없음 | 없음 |
| advance(실측) | **27~29** px/자 | 동일 |
| font-size | **36** (추정, 27 ÷ 0.75) | |
| letter-spacing | **-8** (추정) | |
| 잠금 카드 텍스트 우단(실측) | 카드3-1 ≥456(가려짐) / 카드3-2 **615** / 카드4-1 **473** / 카드5-1 **474** / 카드5-2 **562** | 색 = 딤된 회백색 |

### 3-5. 하단 `레벨` / `남은 횟수` 영역 (5장 공통, 좌표 완전 동일)

#### 3-5-1. 라벨 텍스트

| 항목 | `레벨` | `남은 횟수` |
|---|---|---|
| 글리프 bbox (카드1) | x **186 ~ 234** (w **49**), y **489 ~ 512** (h **24**) | x **368 ~ 472** (w **105**), y **488 ~ 513** (h **26**) |
| 상대 좌표 | x +136, y **+248** | x +318, y **+247** |
| 색 | `#FFFFFF` | `#FFFFFF` |
| advance(실측) | **26** px/자 | **26~27** px/자 |
| font-size | **33** (추정, 24~26 ÷ 0.75) | **33** (추정) |
| letter-spacing | **-7** (추정) | **-7** (추정) |

#### 3-5-2. 레벨 알약 (🔥)

| 항목 | 값 |
|---|---|
| y | **518** (= T+277), height **47** (518~564) |
| x (좌단) | **134** (추정) — 불꽃 아이콘에 가림. 엣지 검출 시작 142 − 캡 보정 8 |
| x (우단) | **289** (실측, 우측 캡 최대 x=289 @ y=540~542) |
| width | **156** (추정) |
| border-radius | **23** (완전 알약) |
| 배경색 (카드1) | `#190913` (25,9,19) |
| 배경 = 검정 오버레이 | `rgba(0,0,0,0.75)` **(추정)** — 25/89=0.281, 9/47=0.191, 19/69=0.275 |
| 테두리 | 없음 |
| 🔥 불꽃 아이콘 bbox | x **110 ~ 158**, y **511 ~ 571** → **49 x 61** (알약보다 위아래로 각각 7px 튀어나옴) |
| 불꽃 색 | 심 `#F78F0C` (247,143,12) / 외염 `#F25510` (242,85,16) / 외곽선 검정 |
| 숫자 bbox | `3`: x **201~219** (w 19), y **527~554** (h **28**) · `1`(잠금카드): x **204~213** (w 10), y T+286~311 |
| 숫자 색 | `#FFFFFF` (254,254,254) |
| 숫자 font-size | **40** (추정, cap 28 ÷ 0.694 — 01번 문서 숫자 환산비) |

#### 3-5-3. 남은 횟수 알약 (입장권)

| 항목 | 값 |
|---|---|
| y | **518** (= T+277), height **47** |
| x (좌단) | **329** (추정) — 입장권 아이콘에 가림. 엣지 검출 시작 337 − 캡 보정 8 |
| x (우단) | **499** (실측, 우측 캡 최대 x=499 @ y=541) |
| width | **171** (추정) |
| border-radius | **23** (완전 알약) |
| 배경색 / 테두리 | 레벨 알약과 동일 (`#190913`, 테두리 없음) |
| 레벨 알약과의 gap | **40** (289 → 329, 추정치 기준) |
| 입장권 아이콘 bbox | 카드1 x **313~376**, y **518~567** → **64 x 50** · 카드2 x **307~373**, y **878~927** → **67 x 50** (기울어진 티켓, 카드마다 다른 아트) |
| 숫자 bbox | 카드1 `3/2`: x **402~468** (w **67**), y **525~555** (h **31**) — `3` 402~420 / `/` 428~441 / `2` 448~468 |
| | 카드2 `1/2`: x **405~463** (w **59**), y **885~915** |
| | 카드3·4·5 `2/2`: x **401~467**, y T+284~313 |
| 숫자 색 | `#FFFFFF` |
| 숫자 cap 높이 | **28** (슬래시 포함 시 31) |
| 숫자 font-size | **40** (추정) |
| 숫자 advance | `3`→`/` 26, `/`→`2` 20 |

### 3-6. 우상단 레드닷 (카드1·카드2만, 카드3·4·5는 없음)

| 항목 | 값 |
|---|---|
| 중심 좌표 | 카드1 **(1019, 255)** / 카드2 **(1019, 615)** |
| 상대 중심 | **(+969, +14)** |
| 진한 빨강 코어 지름 | **24 ~ 26** (카드1 x 1008~1031 / y 242~267) |
| 밝은 림 포함 지름 | **31 ~ 33** (x 1004~1034 / y 239~271) |
| 검정 외곽선 포함 지름 | **41 ~ 43** (x 999~1039 / y 234~276) → 외곽선 두께 **5** |
| 색 | 본체 `#F22E52` (242,46,82), 상단 하이라이트 `#FF7596` 계열 |
| 배치 | 카드 우상단 모서리에 걸쳐 배치 — 카드 상단(241)보다 **7px 위**, 카드 우변(1029)보다 **10px 오른쪽**으로 튀어나감 |

### 3-7. 잠금 카드(3·4·5) 전용 요소

#### 3-7-1. 딤 오버레이

| 항목 | 값 |
|---|---|
| 영역 | 카드 전체 (테두리 안쪽 전면) |
| 색 | `#000000` |
| 불투명도 | **0.65** (추정) |
| 역산 근거 | 흰 숫자 255 → **89** (0.349), 던전명 `#FDFF84` (253,255,132) → (86,88,41) (0.340/0.345/0.311). 평균 투과율 0.349 → alpha = 1 − 0.349 ≈ 0.65 |
| CSS | `background: rgba(0,0,0,0.65)` |

#### 3-7-2. 자물쇠 아이콘

| 항목 | 값 |
|---|---|
| bbox (흰색 본체) | x **487 ~ 592** (w **106**), y T+**60** ~ T+**182** (h **123**) |
| 절대 y | 카드3 1021~1143 / 카드4 1381~1503 / 카드5 1741~1862 |
| 중심 좌표 | **(539.5, T+121)** → 1082 / 1442 / 1802 |
| 정렬 | 카드 가로 중앙 (카드 중심 539.5와 일치) |
| 구성 | 고리(shackle) y T+60 ~ T+96 (h 37) / 몸체 y T+109 ~ T+182 (h 74) |
| 색 | `#FFFFFF` (열쇠구멍만 `#000000`) — 딤 **위**에 그려져 딤 영향 없음 |
| 외곽선 | 육안상 없음 (미확정 — 어두운 배경과 구분 불가) |

#### 3-7-3. `가이드미션 NNN 클리어` 텍스트

| 항목 | 카드3 (261) | 카드4 (311) | 카드5 (540) |
|---|---|---|---|
| 글리프 bbox | x **331 ~ 747** (w **417**), y **1184 ~ 1224** | x **338 ~ 740** (w **403**), y **1544 ~ 1584** | x **324 ~ 754** (w **431**), y **1904 ~ 1944** |
| 상대 y | **+223** | **+223** | **+223** |
| 잉크 높이 | **41** | **41** | **41** |
| 가로 중심 | **539** | **539** | **539** — 카드 중앙 정렬 |
| 한글 색 | `#C8C8C8` (200,200,200) | 동일 | 동일 |
| 숫자 색 | `#FF8252` (255,130,82) | 동일 | 동일 |
| 숫자 bbox | x 544~616 (w 73), y 1188~1220 (h **33**) | x 552~609 (w 58) | x 538~624 (w 87) |
| 외곽선 | `#000000` **4~5px** (추정 — 검정 딤 배경과 겹쳐 정확한 두께 확정 불가) | | |
| advance(실측) | **40** px/자 (가=331, 이=373, 드=414, 미=453, 션=492 / 클=635, 리=674, 어=713) | | |
| font-size | **55** (추정, 41 ÷ 0.75) | | |
| letter-spacing | **-15** (추정) | | |

> 주의: 숫자 cap 높이 33 ÷ 0.694 = **48**로 한글 기준 55와 **불일치(약 13%)**. 한글 받침이 잉크 높이를 늘리는 영향으로 판단해 한글 기준 55를 채택했다. 실제 값은 **48~55 사이**로 봐야 한다.

#### 3-7-4. 잠금 카드 레이아웃 동일성

던전명 / 재화 알약 / 레벨 라벨 / 레벨 알약 / 남은 횟수 라벨 / 횟수 알약의 **카드 기준 상대 좌표가 5장 모두 동일**하다 (실측으로 교차 확인).
다만 잠금 카드는 재화 알약이 **2줄**(카드3·5) 또는 **1줄**(카드4)이며, 2번째 줄은 y T+182에 온다.

---

## 4. 하단 서브탭 `[🔒] [던전]`

### 4-1. 서브탭 바(컨테이너)

| 항목 | 값 |
|---|---|
| x | **151** |
| width | **794** (151 ~ 944) |
| y (상단) | **2021** (추정) |
| y (하단) | **2118** (실측) |
| height | **98** (추정) |
| 상단 y가 추정인 이유 | 서브탭 상단 검정 테두리가 **카드5 하단 검정 테두리(2021~2028)와 완전히 맞붙어** 분리 불가. 하단 좌측 코너 원호 피팅(r≈44~49)에서 세로 중심 y≈2069~2070을 얻어 역산했다. 픽셀로 확정 가능한 가시 상단은 **2029** |
| border-radius | **49** (추정, 완전 알약 형태 — 코너 원호 피팅 r 44~49) |
| 테두리 색·두께 | `#000000` · 좌 **8** (x 151~158) / 우 **6** (x 939~944) / 하 **6** (y 2112~2117) |
| 카드5 하단(2030)과의 간격 | **0** (맞붙음) |
| 하단 탭바(2160)와의 간격 | **41** |
| 가로 정렬 | 중심 (151+944)/2 = **547.5** — 화면 중심 539.5보다 **8px 오른쪽** |

### 4-2. 좌측 탭 `🔒` (비선택)

| 항목 | 값 |
|---|---|
| x | **151** |
| x (우단) | **미확정** — 던전 탭이 위에 겹쳐 그려져 우측 캡이 완전히 가려짐. 노출된 채움 끝은 **523** |
| width | **≥373** (미확정) |
| y | 컨테이너와 동일 (2021~2118, 추정) |
| 배경색 | `#61503C` (97,80,60) |
| 안쪽 밝은 림 | `#705F4B` (112,95,75) · **6~7px** (x 160~166, y 2030~2035 / 2105~2110) |
| 테두리 | `#000000` 8px (좌측) |
| 자물쇠 아이콘 bbox | x **320 ~ 369**, y **2041 ~ 2097** → **50 x 57** |
| 자물쇠 중심 | **(344.5, 2069)** |
| 자물쇠 색 | `#FFFFFF` (열쇠구멍 `#000000`) |
| 글자 | 없음 (아이콘만) |

### 4-3. 우측 탭 `던전` (선택)

| 항목 | 값 |
|---|---|
| x | **524** |
| width | **421** (524 ~ 944) |
| y | 컨테이너와 동일 (2021~2118, 추정) |
| 좌측 캡 형태 | 좌변이 **볼록 원호** — 최좌단 x=524는 y 2058~2079 구간, y=2031에서 x=538, y=2109에서 x=540 |
| 테두리 색·두께 | `#000000` · **6px** (좌 524~529 / 우 939~944 / 하 2112~2117) |
| 안쪽 밝은 림 | `#634F37` (99,79,55) · **7~8px** (x 531~538, 932~937) |
| 배경색(선택 상태) | `#4B3E2D` (75,62,45) — 비선택 탭(`#61503C`)보다 **어둡게 눌린(inset)** 표현 |
| 배경색(비선택 상태) | `#61503C` (97,80,60) (좌측 탭 참조) |
| 두 탭 사이 gap | **0** — 겹쳐 그려져 검정 6px 한 줄만 보임 |
| `던전` 글자 bbox | x **701 ~ 768** (w **68**), y **2047 ~ 2083** (h **37**) |
| 글자 색 | `#F3BC8C` (243,188,140) |
| 글자 외곽선 | `#000000` **4~5px** (추정) |
| 글자 advance(실측) | **37** px/자 (던=701, 전=738) |
| font-size | **49** (추정, 37 ÷ 0.75) |
| letter-spacing | **-12** (추정) |
| 글자 정렬 | 탭 중심 734.5, 글자 중심 734.5 → **정확히 중앙** |
| 우상단 레드닷 | 중심 **(914.5, 2034)**, 진한 코어 지름 **24~25** (x 903~926, y 2022~2046), 색 `#F22E52` — 탭 우상단 모서리에 걸침 |

### 4-4. 서브탭 영역 전체 y 범위

| 항목 | 값 |
|---|---|
| 확정 범위 | **2029 ~ 2118** (가시 높이 90) |
| 추정 범위 | **2021 ~ 2118** (높이 98) — 상단 테두리 겹침 보정 |

---

## 5. 팝업 위/아래 HUD·탭바 y 경계

| y 범위 | 요소 | 색 |
|---|---|---|
| **0 ~ 86** | 최상단 상태바 스트립 | `#2E211B` 계열 (46,33,27) |
| **87 ~ 91** | 상단 HUD 위쪽 검정 테두리 | `#000000` (5px) |
| **92 ~ 186** | 상단 HUD 본체 (골드 `40.77A` / 보석 `1,300`) | `#42362A` (66,54,42) |
| **187 ~ 190** | 상단 HUD 아래쪽 검정 테두리 | `#000000` (4px) |
| **191** | HUD 끝 | |
| **192 ~ 2159** | **던전 팝업(리스트) 영역** | `#26211B` (38,33,27) |
| **2160 ~ 2164** | 하단 탭바 상단 테두리 | `#191919` 계열 (25,25,25) |
| **2165 ~ 2339** | 하단 탭바 본체 (영웅/성장/✕/보물상자/상점) | 좌우 `#42362A` (66,54,42), 중앙 어두움 |

- **팝업은 y 192에서 시작해 y 2159에서 끝난다** (HUD·탭바는 팝업 위에 남아 있고 딤되지 않음).
- 실제 콘텐츠(카드1 상단 ~ 서브탭 하단)는 **y 241 ~ 2118**.

---

## 부록 A. 세로 좌표 요약 (위 → 아래)

| y | 요소 |
|---|---|
| 87 ~ 191 | 상단 HUD |
| 241 ~ 590 | 카드1 `골드 던전` |
| 285 ~ 336 | 카드1 던전명 |
| 362 ~ 407 | 카드1 재화 알약 |
| 489 ~ 512 | 카드1 `레벨` / `남은 횟수` 라벨 |
| 518 ~ 564 | 카드1 레벨·횟수 알약 |
| 591 ~ 600 | 카드 간 gap |
| 601 ~ 950 | 카드2 `보석 던전` |
| 961 ~ 1310 | 카드3 `아티팩트 던전` (잠금) |
| 1021 ~ 1143 | 카드3 자물쇠 |
| 1184 ~ 1224 | 카드3 `가이드미션 261 클리어` |
| 1321 ~ 1670 | 카드4 `동료 주사위 던전` (잠금) |
| 1544 ~ 1584 | 카드4 `가이드미션 311 클리어` |
| 1681 ~ 2030 | 카드5 `목걸이 던전` (잠금) |
| 1904 ~ 1944 | 카드5 `가이드미션 540 클리어` |
| 2021 ~ 2118 | 서브탭 바 (상단 추정) |
| 2160 ~ 2339 | 하단 탭바 |

## 부록 B. 폰트 크기 산정 근거

01번 문서와 동일한 환산비를 적용했다.

- **한글**: `font-size (추정)` = 흰색/유채색 글리프 잉크 높이 ÷ **0.75** (받침 포함 기준)
- **숫자**: `font-size (추정)` = 숫자 cap 높이 ÷ **0.694** (01번 `6.49A` 검증치)
- `letter-spacing (추정)` = 실측 advance − 추정 font-size

| 텍스트 | 잉크 높이 | advance | font-size (추정) | letter-spacing (추정) |
|---|---|---|---|---|
| 던전명 | 52 | 50~53 | **69** | -17 |
| 재화 알약 문구 | 27 | 27~29 | **36** | -8 |
| `레벨` / `남은 횟수` 라벨 | 24 / 26 | 26~27 | **33** | -7 |
| 레벨·횟수 숫자 | 28 (cap) | 20~26 | **40** | — |
| `가이드미션 NNN 클리어` | 41 | 40 | **55** (숫자 기준으로는 48) | -15 |
| 서브탭 `던전` | 37 | 37 | **49** | -12 |

## 부록 C. 확정하지 못한 항목 목록

1. **재화 알약·레벨 알약·횟수 알약의 좌측 끝 x** — 아이콘이 항상 알약 좌측 캡을 덮는다. 상단/하단 엣지 스텝 검출 시작점에서 캡 보정 8px를 빼 역산한 **추정치**(142 / 134 / 329).
2. **잠금 카드(3·4·5) 재화 알약의 우측 끝 x·width** — 자물쇠 아이콘(x 487~592)과 일러스트가 우단을 가려 **미확정**. 엣지 신호는 x 476~478에서 끊긴다.
3. **좌측 서브탭(🔒)의 우측 끝 x·width** — 던전 탭이 위에 겹쳐 그려져 **미확정**. 노출된 채움 끝은 523.
4. **서브탭 바의 상단 y와 height** — 카드5 하단 테두리와 검정이 붙어 분리 불가. 코너 원호 피팅 역산으로 **2021 / 98 (추정)**.
5. **잠금 카드 자물쇠 아이콘의 외곽선 유무·두께** — 어두운 딤 배경과 구분되지 않음.
6. **`가이드미션 …` 문구의 font-size** — 한글 기준 55, 숫자 기준 48로 13% 불일치. 48~55 구간으로만 확정.
7. **`가이드미션 …` 문구 외곽선 두께** — 검정 딤 위 검정 외곽선이라 경계 분리 불가 (4~5px 추정).
8. **상단 HUD의 딤 적용 여부** — 02번 레퍼런스와 HUD 내용 자체가 달라 대조 불가. (하단 탭바는 02번과 픽셀 동일 → 딤 없음 확정)
9. **카드 border-radius 안쪽 값** — 바깥 12에서 테두리 7을 뺀 계산값 5. 원호 직접 피팅은 안쪽 곡률이 너무 작아 실패.
10. **잠금 카드 등급색의 원색** — 딤 역산값(÷0.349)이므로 모두 추정.

---

## 3-8. 카드 우측 몬스터 일러스트(썸네일) — 작업 72 신규 측정 (2026-08-25, sess-0703-19500 워커 A)

초판 측정표는 카드 우측을 «선버스트 일러스트 바탕» 으로만 적었고 **그 위에 올라간 몬스터 일러스트의
bbox 가 없었다.** 작업 72 에서 스캐너 `tools/scan72.py` 로 새로 잰다.

측정 방식 — **국소 경사(3px 간격 RGB 합차) 밀도**. 선버스트는 큰 부채꼴이라 열당 3~4px 수준으로
완만하고, 몬스터 아트는 외곽선·하이라이트 때문에 20~120px 로 뛴다. 그 전이점이 일러스트 좌측 경계다.

| 항목 | 카드1 (골드 던전) | 카드2 (보석 던전) |
|---|---|---|
| 경사 밀도 전이 x (배경 4/열 → 20+/열) | **713** | **723** |
| x 질량 2% / 50% / 95% / 98% | 713 / 855 / 1008 / 1017 | 670 / 860 / 1017 / 1019 |
| y 질량 2% / 95% / 98% (카드 top 기준 상대) | 9 / 323 / 336 | 9 / 335 / 338 |
| 어두운 외곽선(휘도<45) 밀집 시작 x | 780 | 740 |

- 카드 안쪽 박스: x **57~1022**, y = 카드 top+8 ~ top+341 (테두리 좌우 7 · 상하 8 — §3-1).
- **결론: 일러스트는 카드 안쪽 «우측 정렬 315×334»** — 절대 x **708~1022**, 세로는 카드 안쪽 전체.
  **아래가 카드 하단 테두리에서 잘려 들어간다**(y 질량 98% 가 상대 336~338 = 안쪽 바닥 341 코앞).
- 좌측 경계는 **하드 엣지가 아니라 페이드** — 카드1 은 713, 카드2 는 670 부터 신호가 뜨는데
  둘 다 «5% 질량» 은 728~738 이다. 즉 **약 60px 구간에 걸쳐 배경으로 녹아든다.**
- 좌측 텍스트 클러스터의 우단(재화 알약 429, `남은 횟수` 알약 450)과 **250px 이상 떨어져 있어 겹치지 않는다.**
- 잠금 카드(3·4·5)는 딤(§3-7-1) 때문에 경사 신호가 죽어 개별 측정 불가. 레이아웃은 5장 동일(§3-7-4)이고
  **딤과 자물쇠가 일러스트 위에** 온다.

## 부록 D. 아트 필요 표 (GPT 아트 발주서용)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 던전 카드1 썸네일 (황금 동굴) | **311×305** @ 카드top+36 (우·하단 잘림) | 카드 우측 대표 몬스터 일러스트 | 이모지 🐉 (fs291 · scaleX 1.147) |
| 던전 카드2 썸네일 (수정 광산) | 296×289 @ 카드top+52 | 〃 | 이모지 🐍 (fs282 · scaleX 0.972) |
| 던전 카드3 썸네일 (고대 유적) | 330×329 | 〃 (잠금 시 딤 아래) | 이모지 🧌 (fs309 · scaleX 1.059) — 🗿 는 잉크 폭이 0.81em 뿐이라 우단 67px 이 비어 교체 |
| 던전 카드4 썸네일 (수련의 탑) | 330×329 | 〃 (잠금 시 딤 아래) | 이모지 👺 (fs312 · scaleX 1.028) |
| 던전 카드5 썸네일 (마왕의 성) | 330×306 @ 카드top+35 | 〃 (잠금 시 딤 아래) | 이모지 👹 (fs289 · scaleX 1.063) |
| **잠금 카드 3·4·5 씬 아트** | **966×334 풀블리드**(카드 이너 전면) + 좌측 45% 사선 스크림 | 잠금 카드 배경 씬(딤 아래) | CSS 벽돌 텍스처 판 `.dnc>.scn` — **실제 아트 필요** |

> 발주 메모: 5장 모두 **투명 배경 PNG**, 캔버스 315×334, **인물/몬스터가 캔버스 하단을 넘어가도 무방**
> (카드가 잘라 낸다). 좌측 60px 은 알파 페이드로 배경에 녹아들게. 교체는 `.dnc>.th>em` 을
> `background-image` 로 바꾸기만 하면 된다 — 슬롯 기하는 그대로 둔다.

### 부록 D 정오표 — 2026-08-26 저장소 주인 재지시 («아트 대기» 해제) · 작업 72 8회차

> **위 표(부록 D)는 «레퍼런스대로 흘러넘치는 일러스트» 를 전제한 발주서다. 주인 재지시로 전제가 바뀌었다.**
> 지시: «던전들 행에 썸네일이 없다 — 넣게 하라. 이모지 대체가 아니라 실제 스프라이트로,
> 잠금은 어둡게 + 🔒. 썸네일 프레임은 69 아이템 칸처럼 **«액자»** 로 읽히되 색·테두리는 104 공용 토큰.»

바뀐 것 세 가지 — **레퍼런스 대비 의도적 이탈**이다(120 의 «꽉 채움» 과 같은 종류. 레퍼런스에는 액자가 없다):

| | 부록 D(구) | 재지시 후(현행) |
|---|---|---|
| 슬롯의 성격 | 카드 우·하단으로 **잘려 나가는** 일러스트 자리 | **액자** — 104 토큰(검정 5 · 안쪽 림 6 · 코너 = 폭×0.233)으로 네 변이 다 보인다 |
| 안에 드는 것 | 이모지(잉크비를 fs·scaleX 로 정규화) | **게임 안 스프라이트 1프레임**(캔버스). 121-⑥ 아이들 8fps 순환 그대로 |
| 채우는 방식 | 잉크가 슬롯 bbox 를 **꽉 채운다**(늘려서) | **담는다(contain)** — 사방 여백 16px, 종횡 왜곡 **0** |

- ~~**슬롯 bbox(§3-8 실측값 311×305 / 296×289 / 330×330, 우단 inset 7)는 한 픽셀도 안 바뀌었다.**~~
  ~~액자는 `box-sizing:border-box` 로 그 **안쪽에** 그려지므로 위 표의 w×h·상단 인셋·좌단 x 는 전부 유효하다.~~
  → **작업 259(2026-08-27)로 폐기.** 아래 «§3-8 정오표 — 작업 259» 를 볼 것: 8장 전부 **330×330 @ +11** 이다.
- ~~캔버스 픽셀 = 액자 안쪽 = 슬롯 − (검정 5 + 림 6) × 2 → 카드1 **289×283** · 카드2 **274×267** · 잠금 **308×308**.~~
  → 259 이후 **8장 전부 308×308**. 계산식(슬롯 − (검정 5 + 림 6) × 2)은 그대로 유효하다.
  CSS 크기와 1:1 이라 픽셀 아트가 안 흐려진다(게이트 `verify72 [1-2]`).
- 좌측 60px 알파 페이드는 **폐기**했다 — 액자는 네 변이 다 보여야 하므로 `.dnc:not(.rd)>.th` 에서
  마스크를 걷어냈다. 레이드 카드(`.dnc.rd`)는 97 의 규칙 그대로라 마스크·꽉 채우기가 살아 있다.

**현행 대체 아트 — 6장 전부 다른 아틀라스** (틴트 없음. 어두운 스프라이트만 밝기 보정):

| 카드 | 아틀라스 / 정지 프레임 | 아이들 애니 | 밝기 | 액자 면 휘도 → 잉크 휘도 |
|---|---|---|---|---|
| 1 골드 | `dragon` / `f3` | `fly` | ×2.70 | 95 → **130** (1.37) |
| 2 다이아 | `bird` / `frame0` | `anim` | ×1.00 | 93 → **157** (1.69) |
| 3 유물조각 1단 | `elves` / `green_idle_0` (고블린) | `green_idle` | ×1.25 | 85 → 128 (1.51) |
| 4 유물조각 2단 | `zombie` / `walk_004` | `walk` | ×1.60 | 53 → 122 (2.30) |
| 5 유물조각 3단 | `robo` / `Running_007` | `Running` | ×1.55 | 102 → 135 (1.32) |
| 6 유물조각 4단 | `elves` / `blue_idle_0` (군주) | `blue_idle` | ×1.30 | 70 → 116 (1.66) |

> 「면 → 잉크 (비율)」의 «면» 은 카드 `--i` **원색**이다. 실제 액자 면은 그 위에 검정 22~52% 가 덮여
> 더 어두우므로 표의 비율은 **보수적**이다(화면에서는 ≈1.9:1 이상). 게이트 기준은 **≥ ×1.3**이다 —
> 97 의 «면 − 20» 은 «면보다 어둡기만 안 하면 통과» 라 비율 1.00 짜리를 통과시켰다.

> 밝기는 **CSS filter 가 아니라 `drawSpriteTo` 안**에서 건다 — CSS 로 걸면 캔버스 픽셀은 원본 그대로라
> 게이트가 «잉크가 액자 면에 묻히지 않는가»(97 [2] 규칙 «잉크 휘도 > 면 휘도 − 20»)를 화면과 다른 값으로 재게 된다.
> 틴트(multiply)로는 어둡게만 되므로 용(원본 휘도 57)·좀비(77)를 올릴 수단이 이것뿐이다.

**아트가 실제로 들어오면**: 위 6칸을 `<canvas class="thcv">` 대신 `<img>`/`background-image` 로 바꾸면 된다.
발주 규격은 **액자 안쪽 크기**(~~289×283 · 274×267 · 308×308 ×4~~ → **259 이후 전 카드 308×308**),
투명 배경 PNG, **사방 16px 여백을 두고 피사체가 그 안에 담기게**(잘려 나가지 않게 — 액자다).
잠금 카드 씬 아트(966×334)는 위 부록 D 그대로 유효하다.

### §3-8 정오표 — 작업 259 «액자 기하 8장 통일» (2026-08-27, sess-2116-20884 워커 A)

> **저장소 주인 보고**: «03 던전 행 — 황금 동굴·수정 광산의 몬스터 액자가 다른 행보다 작고
> 위치도 어긋나 통일감이 없다.» **T1 버그로 등재(259)되어 이번에 닫았다.**

실측이 지적 그대로였다 — 카드 1·2 만 나머지 여섯과 달랐다:

| 카드 | 259 이전 (슬롯 w×h @ 상단인셋 · 좌단 x) | 259 이후 |
|---|---|---|
| 1 황금 동굴 | **311×305 @ +36 · x662** | **330×330 @ +11 · x643** |
| 2 수정 광산 | **296×289 @ +52 · x677** | **330×330 @ +11 · x643** |
| 3~8 (유물 4단·강화석·룬강화석) | 330×330 @ +11 · x643 | 330×330 @ +11 · x643 (불변) |

**왜 달랐나 — 폐기된 전제의 잔재다.** 311/36 · 296/52 는 작업 72 1~2회차가 레퍼런스 일러스트의
**카드별 bbox**(카드1 rel 36~341 · 카드2 rel 52~341)를 그대로 옮긴 값이다. 그런데 그 뒤
**주인 재지시(2026-08-26)로 슬롯의 성격이 «흘러넘치는 일러스트» → «액자에 담긴 그림» 으로 바뀌었다**
(바로 위 «부록 D 정오표»). **액자가 되는 순간 카드마다 크기·위치가 다를 이유가 사라진다** — 액자는
격자의 부품이고, 여덟 장이 세로로 늘어선 목록에서 두 장만 작고 내려앉으면 그것이 곧 «통일감 없음» 이다.
성격은 바뀌었는데 수치가 안 따라와서 **넉 달 치 잔재가 그대로 남아 있던 것**이고, 그래서 이 건은
«레퍼런스와 다시 맞추는 일» 이 아니라 **«폐기된 전제가 남긴 값을 걷어내는 일»** 이다
(지시서 [3]-(가) 기계적 작업 — 비평가 없이 수치로 닫았다).

기준값으로 **330/11 (relic 규격)** 을 골랐다: ① 여덟 장 중 **여섯 장이 이미 그 값**이라 바뀌는 카드가
최소다 ② 정사각(1.000)이라 담김(contain) 배율이 카드마다 안 갈린다 ③ 카드 이너 높이(341)를 상단 11만
비우고 다 쓰므로 **여덟 장 중 가장 큰 액자** = 주인이 지적한 «작다» 를 키우는 방향이다.

같이 맞춘 것 — **`scn`(잠금 카드 씬 아트 자리)**: 카드 1·2 에만 없었다. 둘은 `gm:0` 이라 잠기는 일이
없어 `.scn` 이 렌더되지 않으므로 **화면상 변화는 0**이지만, DUN_UI 행이 여덟 장 같은 모양이 되도록
`scn:'7px'` 을 넣었다(등재문 «`scn` 유무도 맞춘다»). `verify72` 의 «해금 카드 1·2 에는 씬 자리가 없다»
단언은 **렌더 조건(`lock && u.scn`)이 지키므로 그대로 통과**한다 — 실제로 통과를 확인했다.

**따라 움직인 값** (계산식은 그대로, 입력만 바뀐다):

| | 카드1 | 카드2 | 카드3~8 |
|---|---|---|---|
| 캔버스 픽셀 = 슬롯 − (검정 5 + 림 6)×2 | 289×283 → **308×308** | 274×267 → **308×308** | 308×308 (불변) |
| 액자 코너 반경 = 폭 × 0.233 | 72.5 → **76.9** | 69.0 → **76.9** | 76.9 (불변) |

**겹침·잘림 실측** (`node tools/probe259.js` — 이번에 신설). 액자 좌단이 662/677 → **643** 으로
19~34px 왼쪽으로 오므로 그 자리의 던전명·보상 알약·하단 라벨·좌우 캡슐과 새로 겹칠 수 있어 전수 측정했다.
가장 오른쪽까지 오는 것은 `.sp.tk`(남은 횟수 캡슐) **우단 450** 으로 액자 좌단 643 과 **193px** 떨어져 있다
(그 다음이 `.lb.b` 395.2 · `.pill` 380) —
여덟 장 전부 겹침 0 · 카드(980×350) 밖 이탈 0 · 캔버스 CSS = 픽셀(1:1) 유지 · 콘솔 에러 0.

⚠ **레이드·아레나 카드(`.dnc.rd`)는 건드리지 않았다.** 3-9·3-10 이 적어 둔 311×305/36 · 296×289/52 는
**그대로 유효**하다 — 그쪽은 97 «슬롯을 꽉 채운다» 규칙이라 액자가 아니고, 주인 지적 대상도 아니다.
(213/216 의 «기록 알약 ↔ 슬롯 좌단» 클램프도 `.dnc.rd` 전용이라 영향 없다 — 실측해서 역산하는 코드다.)

## 3-9. 레이드 탭 카드 썸네일 — 작업 97 (2026-08-26, sess-2018-32515 워커 B)

레이드 탭(46, 서브탭 «레이드»)에는 **레퍼런스 이미지가 없다** — 46 이 «껍데기·카드 규격은 03 던전 것을
그대로 쓰고 내용만 레이드» 로 만든 화면이라, 썸네일 슬롯도 **§3-8 던전 값을 그대로 옮겨 쓴다.**
새로 «잰» 값이 아니라 «인용» 이므로 여기 따로 스캐너 결과를 적지 않는다.

| 카드 | 슬롯 w×h | 상단 인셋(카드 top 기준) | 슬롯 좌단 x(카드 기준) | 인용처 |
|---|---|---|---|---|
| r60 DPS 측정장 | **311×305** | 36 | 662 | §3-8 던전 카드1 |
| 아레나 (123) | **296×289** | 52 | 677 | §3-8 던전 카드2 |
| ~~r120 장기 측정장~~ | ~~330×330~~ | ~~11~~ | ~~643~~ | **폐기(123)** |

> ### 정오표 — 작업 123 (2026-08-26, sess-2325-5678 워커 B)
>
> 저장소 주인 지시로 **탭 이름이 «레이드» → «컨텐츠»**, 카드 구성이 **측정장 3장 → «DPS 측정장 1장 + 아레나 1장»** 이 됐다.
> - `r30 단기 측정장`·`r120 장기 측정장` 삭제 → 위 표의 2·3행은 **폐기**. 2행 슬롯 규격(296×289 / 인셋 52)은
>   그대로 **아레나 카드**가 물려받는다(기하는 한 줄도 안 바뀌었다 — 들어가는 그림만 바뀐다).
> - `r60` 은 «정규 측정장» → **«DPS 측정장»** 으로 개명. 슬롯·팔레트 불변.
> - 아레나 카드 팔레트는 폐기된 r120 의 **어두운 보라**(`--s #2B1F52 / --i #453380 / --r #563F9C / --pt #B6A9DF`)를
>   물려받았다. 카드2 의 노랑(`--i #7A6320`, 휘도 98.2) 위에서는 기사 스프라이트 잉크(틴트 후 휘도 **60.8**)가
>   배경보다 어두워 97 의 «잉크가 배경에 묻히지 않을 것» 판정을 통과하지 못한다(`node tools/fnchk97.js` [2] 실측).
>   보라(휘도 65.2)에서는 두 칸(102.2 / 60.8) 다 산다.
> - **아레나 썸네일만 캔버스가 2장이다** — 슬롯을 좌·우 절반(각 148×289)으로 갈라 «마주 본 플레이어 2명» 을 세운다.
>   좌 = 내 코스튬(`cosCur()`), 우 = 더미 상대(미리보기는 `av1` 고정). 좌측만 `flip` 이라 서로 마주 본다.
>   기사 아틀라스 `idle[0]` 프레임 rect 는 44×46 인데 잉크가 (1,1)~(42,45) 라 **사방에 투명 1px** 이 있고,
>   슬롯으로 늘리면 가로 3.4px · 세로 6.3px 여백이 된다(`verify72` 의 «bbox 를 채운다» 허용치 tx4/ty7 근거).

- 슬롯 우단은 카드 안쪽 우단(x 973 = 980−7), 하단은 안쪽 바닥(341) — 던전과 동일하게 **우·하 두 방향에서 잘린다.**
- **잠금 여부로 기하가 바뀌지 않는다.** r30·r120 은 스테이지 15·30 에서 해금되는데, 해금 순간
  슬롯이 리사이즈되면 칸이 흔들린다. 던전 카드도 relic 4장이 잠금·해금과 무관하게 330 고정이다.
- 좌측 텍스트 클러스터와의 간격(실측, `tools/cap97.js`): 알약 우단 380 · `최고 DPS` 알약 우단은
  기록 자릿수에 따라 늘어난다(`.dnc.rd .sp{width:auto}`). ~~**최악(9.9e12 = 9자리+단위)에서도 496.8** 이라
  슬롯 좌단(643~677)과 **146px 이상** 떨어진다.~~ §3-8 던전의 «250px 이상» 보다 좁지만 겹침은 0이다.

  > **정오 (작업 213, 2026-08-27) — 위 «496.8 / 146px 이상» 은 `fmt` 규약이 바뀌기 전 수치다.**
  > 그 값은 `fmt(9.9e12)` 가 **«9.9C»(4자)** 이던 시절 것이고, **150**(주인 지시 «골드 빼고 숫자 그대로»)이
  > 단위 접기를 걷어 내자 같은 기록이 **«9,900,000,000,000»(17자)** 이 되면서 우단이 **811.1** 로 자라
  > 슬롯 좌단 662 를 **149.1px 침범**했다(`verify72`·`fnchk97` 동시 FAIL).
  > ⇒ 이제 «자릿수만큼 자란다» 는 **폭 클램프가 붙은 채로만** 참이다 — `raidFitNums()`(213)가
  > «슬롯 좌단 − `RD_SPGAP` **16px** − padding-right» 를 실측 역산해 `fitNum` 으로 글자를 눌러 넣는다
  > (150 의 «한 글자도 안 버리고 폭에만 맞춘다». `max-width` 로 자르면 상자만 멈추고 글자는 그대로 넘는다).
  > **알약 우단은 이제 «슬롯 좌단 − 16» 에 고정**이라고 읽어라(카드1 646 / 카드2 661).
  > 자간은 `px` → `em`(.0976em = 4/41)으로 옮겼다 — 기본 크기에서는 불변이고, 클램프가 걸릴 때만
  > 자간까지 같이 줄어 **같은 자리를 지키면서 글자가 더 크다**(17자에서 fs 22.92 → **25.59**).
  >
  > **한계(측정, 프레임 px)** — `fitNum` 바닥 `FITMIN 0.55`(= fs 22.55) 때문에 자릿수에 천장이 있다:
  >
  > | 기록 | 글자수 | 클램프 후 fs | 알약 우단 ~ 슬롯 좌단 |
  > |---|---|---|---|
  > | 9,900,000 | 9 | 41 (클램프 안 걸림) | +56.8 |
  > | 9.9e9 | 13 | 33.42 | +16.1 |
  > | **9.9e12**(게이트 최악값) | 17 | 25.59 | **+16.0** |
  > | 9.9e14 | 19 | 22.59 | +16.1 |
  > | 9.9e15 | 21 | **22.55(바닥)** | **−8.1** |
  > | 9.9e18 | 25 | 22.55(바닥) | −64.7 |
  >
  > **19자리(1e15 대)까지 안전 · 21자리(1e16)부터 다시 겹친다** → PROGRESS **216** 으로 등재.
- 대체 아트는 이모지가 아니라 **게임 안 스프라이트 1프레임**이다(아래 부록 D-2).

## 부록 D-2. 아트 필요 표 — 레이드 (작업 97)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 카드1 썸네일 (DPS 측정장) | **311×305** @ 카드top+36 (우·하단 잘림) | 카드 우측 «측정 대상 보스» 일러스트 | `elves` 아틀라스 `blue_attack_2` 프레임(잉크 121×132)을 슬롯에 늘려 채움 · 틴트 `#EFC9B4` |
| 카드2 썸네일 (아레나 — 123) | 296×289 @ 카드top+52 (좌우 **148×289 2칸**) | «내 캐릭터 vs 상대 플레이어» 대치 구도 | `knight` 아틀라스 `idle[0]` 2장 · 좌는 내 코스튬 틴트 + flip, 우는 더미 상대 틴트 |
| ~~카드3 썸네일 (장기 측정장)~~ | ~~330×330 @ 카드top+11~~ | 〃 | **폐기(123 — r120 삭제)** |
| 아레나 HUD «VS» 뱃지 (123) | 96×48 @ 프레임 (492,330) | 두 HP 바 사이 대치 표시 | 노란 «VS» 글자(검정 외곽선 8px) |

> **발주 메모 — «레이드 보스 3종»**: 세 측정장은 지금 **같은 샌드백 보스**(`ETYPE.boss` = elves 파랑 «군주»)를
> 소환한다. 그래서 대체 아트도 셋 다 같은 프레임이고, **틴트만 카드 팔레트로 달리해** 칸을 구분한다.
> 아트를 발주할 때 선택지는 둘이다 —
> ① 측정장이 계속 같은 보스면 **군주 일러스트 1종**만 그리고 지금처럼 색만 달리한다,
> ② 측정장마다 다른 보스를 세울 거면 **3종**을 그리고 `RAIDS[].ui.thk/thf` 를 스프라이트별로 나눈다.
> 어느 쪽이든 캔버스는 위 표의 w×h, 투명 배경 PNG, 하단·우측이 잘려도 무방.
> 교체는 `renderRaidPage` 의 `<canvas class="thcv">` 를 `background-image` 로 바꾸기만 하면 된다 —
> 슬롯 기하(`--thw`/`--tht`)는 그대로 둔다.
>
> 후보 프레임 실측(`node tools/probe97.js`, 잉크 알파 bbox) — 슬롯 종횡비 1.00~1.02 에 맞춰 고른 근거:
> `blue_attack_2` 121×132(0.917) · `blue_idle_0` 105×132(0.795) · `green_attack_2` 130×131(0.992) ·
> `zombie walk_004` 195×195(1.000) · `dragon f0` 94×64(1.469) · `robo Running_000` 124×214(0.579).
> 늘림은 9~11% 로 던전 카드 이모지의 `--tsx` 범위(0.977~1.158) 안이다.


---

## 정오표 — 작업 96 «서브탭 공용화» (2026-08-26, sess-2010-10986)

**§서브탭(`.dns-sub`/`.dns-t`) 의 «칸» 실측치는 더 이상 구현 기준이 아니다.**
저장소 주인 지시 ⓐ(«레이드/던전 탭이 선택됐을 때 너무 과해 보인다»)로 03 전용 탭 부품을 폐기하고
**영웅 서브탭과 같은 공용 부품 `.stabs.sp2 > .stab`** 으로 바꿨다.

| 항목 | 옛 실측(레퍼런스) | 96 이후(구현) |
|---|---|---|
| 칸 나눔 | 좌 524 / 우 421 이 x373~524 에서 **겹침** | 바 콘텐츠 **2등분**(각 391) |
| 활성 칸 | `border 6 #000` + `inset 0 0 0 7px #634F37` = 13px 이중 테두리, z 스왑 | 칸 안쪽 `inset ±7 #000 / ±14 #604F3D` + 면 그라디언트 |
| 라벨 | `.dns-t>b` fs42 · `-webkit-text-stroke 8px` | `.stab>i` fs43(활성 41) · `.ol3/.ol4` text-shadow 외곽선 |
| 레드닷 | `.dns-t.r>s` 칸 안 절대좌표 left 371 | `.stab>.bdg` 칸 우상단 right 23 · top −6 |

**바 기하(x151 · w794 · 탭바 위 40)는 실측 그대로 살아 있다.** 바뀐 것은 칸과 활성 표시뿐이다.
픽셀 게이트에서 **서브탭 «칸» 항목은 제외**하고, 대신 `tools/verify96.js` 가
«영웅 바와 computed style Δ0» 로 검사한다. 상세: `docs/review/96-서브탭공용화.md`.


---

## 배경 레이어 `.dnc>.bgm` — **주인 지시(연출), 레퍼런스에 없음** (작업 121, 2026-08-26)

레퍼런스 `docs/ref/03-던전-팝업.jpg` 의 카드 배경은 **정지 화면 1장**이라 이 절에는 대조할 실측치가 없다.
저장소 주인 지시(2026-08-26 «던전·레이드 행의 몬스터 말고, 그 뒤 백그라운드가 좀 움직이는 느낌으로»)로
**레퍼런스에 없는 레이어를 하나 추가**한 것이므로, 픽셀 대조 대상이 아니며 **레이아웃 채점(①~④)의 감점원으로 쓰지 않는다.**
바닥 그라디언트(`.bg`)·프레임(`.fr`)·썸네일 슬롯(`.th`)·글자·알약의 실측치는 **1px도 바뀌지 않았다**
(`node tools/verify121.js` §4 가 매 실행 그것을 확인한다).

### 레이어 규격 (아트 교체 시 이 자리를 그대로 쓴다)

| 항목 | 값 | 비고 |
|---|---|---|
| DOM | `.dnc > .bgm` **1개 고정** | `.bg` 바로 다음 형제 = 바닥색 위·썸네일 아래 |
| 면적 | `inset: 8px 7px` (= 966×334) | `.bg` 와 완전히 같다(radius 5) |
| 움직이는 것 | `.bgm::before`(흐름) · `.bgm::after`(입자·안개) | 둘 다 `transform:translate3d` 만 — GPU 합성 |
| 정지 레이어 | `.bgm` 자신의 `background` = 상단 비네팅 | 위→아래 rgba(0,0,0,.34)→0, 33% 지점에서 소멸 |
| 레이어 폭 | `200%` (=1932), 높이 `100%` | 130% 로 키우면 스크롤 fps −5% (합성 텍스처만 커진다) |
| 이동 거리 | `--bgw1` / `--bgw2` = 무늬 타일 폭(`--bgz*`)의 **정수배** | 이음매 없는 루프의 조건 |
| 주기 | `--bgt1` 52~82s · `--bgt2` 68~88s (카드 인덱스에서 파생) | 지시 «40~90초» |
| 위상 | `--bgd1` −7s×i · `--bgd2` −11s×i | 카드마다 다르게(음수 delay) |
| 테마 | `.bgm-gold` / `.bgm-dia` / `.bgm-rel` / `.bgm-raid` | 무늬 **모양**만 테마별. 색은 카드가 이미 가진 `--r`·`--pt` 를 그대로 쓴다 → 유물석 1~4 «층별 색 단계» 가 데이터 추가 없이 따라온다 |
| 잠금 카드 | `.dnc.lkd` → 애니메이션 `paused` + `.bgm{opacity:.45}` | ⚠ `filter:brightness()` 로 어둡게 하면 스크롤 fps 31.9 → 26.4 (−17%) |
| 페이지 닫힘 | `#dunw` 가 `display:none` → 상자 자체가 없다. 추가로 `#dunw.on` 밖에서는 `paused` + `will-change:auto` | 안 보이는 카드에 합성 레이어를 남기지 않는다 |

### 썸네일 «들썩» (지시 ⑥)

슬롯(`.th`) 기하 `--thw/--tht/--thcx/--thcy` 는 **불변**이다. 움직이는 것은 슬롯 **안의 잉크**뿐이다.
`transform` 은 이미 폭 정규화(`scaleX(--tsx)`)가 쓰고 있으므로 건드리지 않고, 개별 변환 속성
`translate`(들썩)·`scale`(스쿼시)에 따로 애니메이션을 건다 — 개별 속성이 `transform` 보다 먼저 적용돼
72 의 폭 정규화가 그대로 살아 있다.

| 항목 | 값 |
|---|---|
| 짧은 들썩 | `translate: 0 −8px`, 주기 `--thb/5` = 0.78~1.00s (카드별) |
| 큰 점프 | 같은 루프의 90% 지점에서 `−14px`, 주기 `--thb` = 3.9~5.0s |
| 스쿼시 | `scale: 1 .96` ↔ `1 1`, 짧은 들썩과 같은 주기, `transform-origin:50% 100%` |
| 위상 | `--thd` = −0.37s × 카드 인덱스 |
| **아이들 프레임 창** (6회차) | `TH_IDLE` — `dragon/fly`→`f2 f3 f4` · `zombie/walk`→`walk_006..008` · `elves/blue_idle`·`green_idle`→중복 rect 접은 4장. 나머지(bird·robo)는 전 사이클 |

> **왜 창을 두는가(6회차)**: `fly`·`walk` 는 아이들이 아니라 비행·걷기 사이클이라, 전 사이클을 돌리면
> 스프라이트 자체의 세로 운동(용 59px · 좀비 16px)이 위 표의 CSS 들썩(−8/−14)을 **통째로 덮는다.**
> 지시 ⑥ 의 «idle 프레임 2~4장 순환» 이 바로 이 조항이다. 창은 «세로로 안 튀면서(≤2px) 프레임끼리는
> 실제로 달라 보이는(인접 픽셀차 ≥20)» 연속 구간으로 고른다 — 잉크 bbox 만 보고 고르면 시트의 다른
> 줄에 있는 «같은 bbox 다른 포즈» 를 짝지어 스트로브가 된다(용 `f0+f6` 픽셀차 145).
> 게이트: `node tools/inkjit121.js` (창 잉크 세로 흔들림 ≤ 9px = 들썩 진폭 18 의 절반).

### «아트 필요» 표 (121 추가분)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 카드 배경 흐름 레이어 (골드) | 966×334 (타일 256×334) | 반짝이는 금빛 입자 흐름 | 사선 `repeating-linear-gradient` 줄기 + 점 4개 타일 |
| 카드 배경 흐름 레이어 (다이아) | 966×334 (타일 288×334) | 푸른 결정 빛 | 교차 사선 2겹 + 부드러운 광점 2개 |
| 카드 배경 흐름 레이어 (유물석 1~4) | 966×334 (타일 330×334) | 보랏빛 안개(층별 색 단계) | `radial-gradient` 안개 덩어리 3+2개 |
| 카드 배경 흐름 레이어 (컨텐츠/레이드) | 966×334 (타일 192×334 · 입자층 264×180) | 붉은 열기 + 번개 잔광 | 세로 아지랑이 줄무늬 + opacity 펄스(`bgmFlash`) · 6회차부터 입자층이 **대각선(가로 +264 / 세로 −180, 각 타일 1배)** 으로 흐른다 |
| 컨텐츠 카드 몬스터 아이들 | 311×305 / 296×289 / 330×330 | 카드 썸네일이 «살아 있는» 아이들 루프 | `elves.blue_idle` 8fps 순환(6회차부터 중복 rect 접은 4장 창) |
| **던전 카드1 «용» 아이들 포즈** | 잉크 **252×112**(슬롯 289×283 의 가로 98% · 세로 45%) | 액자를 세로로도 채우는 제자리 아이들 | `dragon.fly` 의 «날개 접은» 3장 창(`f2 f3 f4`) — 아래 ⚠ 참고 |

> ⚠ **아이들 순환의 종횡 대가(97-③ 의 재계산)**: 97 은 정지 프레임으로 `blue_attack_2`(잉크 123×134,
> 종횡 0.918)를 골라 슬롯 종횡(1.00~1.02) 대비 늘림을 11% 로 맞췄다. 아이들 순환은 `blue_idle` 5프레임
> (106~107×134~135, 종횡 0.785~0.799)을 쓰므로 **가로 늘림이 11% → 28% 로 커진다.**
> 대신 5프레임끼리는 잉크가 서로 1px 이내라 **순환 중 폭 흔들림은 0** 이다(프레임마다 종횡이 다른
> `blue_attack` 계열은 107→234 로 튀어 못 쓴다). 아트가 들어오면 이 자리는 통째로 교체되므로
> «bbox 를 정확히 차지한다»(97) 는 규칙만 지키고 늘림 값은 **아트 교체 때 자연히 해소**된다.

> ⚠ **던전 카드1 «용» — 세로가 45% 밖에 안 찬다 (121 6회차, CSS 로는 못 고친다).**
> `dragon` 아틀라스는 12프레임 전부 `96×64`(종횡 1.5)이고 슬롯 종횡은 1.02 다. 72 의 «액자에 담는다»
> (contain) 규칙에서는 **가로가 먼저 걸려** `k = 257/96 = 2.677` 로 고정되므로, 잉크를 아무리 타이트하게
> 잡아도 세로는 그대로다. 6회차가 아이들 창을 «날개 접은» 3장으로 좁히면서 세로 채움이
> 68%(날개 편 f0·f6) → **45%** 로 내려갔는데, 이건 창 선택의 부작용이 아니라 원화 종횡의 결과다.
> — 날개를 편 f0·f6 은 세로 잉크가 171px(68%)이지만 접은 포즈와 **높이가 +53% 차이**라
> 같은 루프에 넣으면 125ms 마다 48px 을 순간이동한다(축 ① 이 금지하는 «팝» 이 초당 8회).
> **발주 시**: 용은 «제자리 아이들»(호버링) 포즈로, 프레임 사이 **잉크 높이 편차 ±5% 이내** ·
> 종횡 **1.0 안팎**(예: 잉크 240×240) 으로 그릴 것. 그러면 창을 좁히지 않아도 되고 세로도 찬다.
> 측정 근거: `node tools/inkjit121.js` (프레임별 잉크 bbox 전수 · 창 흔들림 게이트).

## «아트 필요» 추가 등재 — 던전 카드 썸네일 **아틀라스 중복** (작업 245, 2026-08-27, sess-1939-9878 워커 B)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| **「각성의 동굴」(강화석 던전 `stone`) 씬** | **330×330** (썸네일 슬롯, 상단 인셋 11) | 던전 카드 우측 대표 몬스터 | **골드 던전과 같은** `dragon` 아틀라스의 같은 `fly` 애니 — 시작 프레임만 `f1`(골드는 `f3`)로 갈랐다. 순환 창이 `['f2','f3','f4']` 3장뿐이라 **돌다 보면 골드와 같은 포즈를 지난다** |

> 194 가 이 칸을 만들 때 97 «없는 몬스터를 그리지 않는다» 를 지킨 결과다 — 남는 아틀라스가 없었다.
> **CSS 로도 게이트로도 못 푼다.** 전용 씬이 들어오면 `DUN_UI.stone` 의 `thk/thf/thi` **세 값만** 갈아
> 끼우면 되고, 그 순간 `verify72` 의 «아틀라스+애니 재사용» 로그 줄이 0쌍으로 떨어진다.
> (210 이 「절망의 탑」 `despair` 를 `relic2` 와 같은 `zombie walk_004` 로 등재해 둔 것과 같은 형식이다.)
>
> ⚠ **잠금 카드는 자기 `thf` 로 굳지 않는다** — `raidIdleTick` 이 잠금 카드를 세우는데(지시 ④),
> 그 전에 한 번 그려질 때 프레임을 고르는 것은 `thf` 가 아니라 아이들 창 `TH_IDLE[akey/anim]` 과
> **카드 순서에서 온 초기 위상**(`data-thn × 0.7`)이다. `stone`(ci 6)은 `floor(4.20) % 3 = 1` →
> **`f3` 에서 굳는다**(= 골드의 순환 프레임 중 하나). 작업 245 가 게이트를 설계값으로 옮긴 이유다.

### §3-8 정오표 — 작업 72 12~14회차 «액자에 앉히는 규칙» (2026-08-28, sess-0523-5397 워커 F)

§3-8 과 «부록 D 정오표»(8회차)는 액자의 **기하**(330×330 @ 상단 인셋 11 · 우단 inset 7)만 적었고,
**그 안에 그림을 어떻게 앉히는지**는 «contain(사방 TH_PAD 16)» 한 줄뿐이었다. 그 한 줄이 두 가지를
안 적어 둬서 둘 다 결함으로 남아 있었다 — «무엇을 담는가»(배율)와 «어디에 앉히는가»(중심).

#### (1) 배율 — **프레임 자기 rect 를 담는다(현행 유지)**. 맥동은 «창» 으로 고친다

실측 `node tools/probe72k.js` (도는 프레임 집합 = `TH_IDLE` 창이 있으면 창, 없으면 전 사이클):

| 카드 | 아트(키/애니) | 배율 스윙 (전) | 잉크 top p2p (전) | 조치 | 후 |
|---|---|---|---|---|---|
| 8 룬강화석 | `elves/blue_attack` | **77.3%** | **58px** | 아이들 창 `_0 _1 _2 _1` 신설 | **1.9% / 0px** |
| 4 유물조각2 | `zombie/walk` | 8.9% | 2px | — (121 이 고른 창의 잔여) | 8.9% / 2px |
| 2 다이아 | `bird/anim` | 3.0% | 0px | — | 3.0% / 0px |
| 1·7 | `dragon/fly` | 0.0% | 0px | — | 0.0% / 0px |
| 3·5·6 | | ≤1.6% | ≤2px | — | ≤1.6% / ≤2px |

⚠ **«사이클 최대 박스로 배율을 상수화» 는 재어 보고 기각한 안이다**(12회차에 넣었다가 14회차에 되돌렸다).
크기 맥동은 0.5% 로 사라지지만 **잉크 top 흔들림이 2 → 12px** 로 늘어 121 기준선(9px = thBob 진폭 18 의 절반)을
넘는다. 121 6회차가 같은 처방을 이미 실측해 기각해 뒀다(`tools/inkjit121.js` 의 `uni` 열).
**맥동의 원인은 배율식이 아니라 «공격 사이클을 썸네일 아이들로 쓰는 것» 이다** — 그러므로:

> **데이터 규약: 공격 사이클(`*_attack`)을 썸네일 아이들(`thi`)로 쓰지 않는다.**
> 쓸 수밖에 없으면 `TH_IDLE` 에 «뻗기 전» 3포즈 창을 같이 넣는다. 게이트 `verify72 [1-3]` 이
> 배율 맥동 ≤15% · 잉크 top p2p ≤9px 두 축으로 지킨다.

#### (2) 중심 — **rect 중심이 아니라 «잉크 중심»** (14회차 확정)

rect 는 스프라이트 시트의 «칸» 이라 잉크가 그 안에서 가운데 있으리라는 보장이 없다.
`node tools/scan72c.js` 로 8장이 쓰는 7종을 전수 측정 — **6종은 rect 가 타이트 크롭이라 보정 0**,
`dragon` 만 96×64 **무트림 격자**라 용이 칸 아래쪽에 몰려 있다:

| 키/애니 | 잉크 중심 보정(원본 px) | 화면 환산 | 잉크 합집합 |
|---|---|---|---|
| `dragon/fly` | **[-1, -11]** | **−2.9, −31.6px** | 94×42 |
| `bird/anim` · `elves/green_idle` · `zombie/walk` · `robo/Running` · `elves/blue_idle` · `elves/blue_attack` | [0, 0] | — | 타이트 크롭 |

> **규칙: 사이클 전체 «잉크 합집합» 의 중심을 액자 중심에 맞춘다.** 프레임마다 맞추면 애니가
> 제자리걸음이 된다 — 움직이는 것은 애니의 몫, 액자에 앉히는 것만 우리 몫이다.
> 값은 `index.html` 의 `TH_INKC` 상수표(제품은 픽셀을 안 읽는다 — `getImageData` 는 file:// 에서 막힌다).
> 표가 늦게 따라오는 병(245·259 계열)은 `verify72 [1-4]` 가 **매번 아틀라스에서 다시 재서** 막는다.

결과: 잉크 합집합 중심 이탈 **(3, 31.5) → (0, −0.5)** (비평가 3명이 독립 검출한 «용이 24~25px 내려앉는다»).

#### (3) 아직 안 한 것 — 장축이 액자의 77~87% 에서 멈춘다

13회차 비평가 AD·AE 가 독립적으로 짚었다. 원인은 사방 `TH_PAD 16px`(= 306 의 10.4%) + 프레임 투명 여백.
**그 여백은 121 들썩의 자리**(아래로 14px + 스쿼시)라 0 으로 못 만든다. 움직임이 아래로만 가므로
**비대칭 인셋**(위·좌·우 ~4 / 아래 18)이 상한이고, 그래도 가로 +8.7% · 세로 +3.6% 뿐이다.
장축 100% 는 «캔버스를 액자보다 세로로 18px 크게 잡고 들썩 여유를 보이는 영역 밖에 두는» 구조 변경이라
`verify72` «캔버스 픽셀 = 액자 안쪽 1:1» 과 `verify121` «슬롯 불변» 6항목을 같이 옮겨야 한다. 다음 회차 몫.


### §3-8 정오표 — 작업 306 «공격 사이클 규약의 마지막 예외를 닫았다» (2026-08-28, sess-0740-1968 워커 B)

위 (1) 의 데이터 규약(«공격 사이클(`*_attack`)을 썸네일 아이들(`thi`)로 쓰지 않는다»)에
**예외가 하나 남아 있었다** — 209 의 `tower`(시련의 탑) 카드가 `elves/green_attack` 을
창 없이 통째로 돌아 `inkjit121` 이 **잉크 top p2p 67px**(기준선 9px 의 7.4배)로 빨갰다.
`rstone`(72 12회차)과 아틀라스 구조까지 판박이라 처방도 같은 한 줄이다.

| 카드 | 아트(키/애니) | 잉크 top p2p (전) | 조치 | 후 |
|---|---|---|---|---|
| 209 `tower` | `elves/green_attack` | **67px** | 아이들 창 `_0 _1 _2 _1` 신설 | **0px** |

`green_attack` rect: `_0` 115×131 · `_1` 122×133 · `_2` 131×133 | `_3` 194×133 · `_4` **262×133** · `_5` ≡ `_0`
(뻗은 포즈가 기준 포즈의 **2.28배 폭** — `blue_attack` 의 2.19배와 같은 구조).

> **`.dnc.rd` 경로(97 «슬롯을 꽉 채운다», `fit:0`)에서도 창을 좁혀도 안전하다** — 72 14회차가
> «채움% 가 깨질 수 있다» 고 재지 않고 남겨 둔 자리다. 재어 보니 **99% → 99%** 로 그대로다:
> 좁혀 낸 `_3`·`_4` 는 **가로만** 큰 프레임이고 **세로는 6장이 전부 131~133** 이라,
> 짧은 축(세로)이 안 줄면 채움률도 안 준다. 줄어든 것은 «칼을 뻗은 가로» 뿐이다.
> 같은 이유로 `TH_INKC` 는 손대지 않는다 — `thPaint` 가 `inkC: rd ? null : …` 라
> `rd` 경로는 중심 보정표를 읽지 않는다(위 (2) 표는 액자 8장 전용).

게이트: `INKJIT121 PASS 10/10` · `VERIFY72 259/259` · `VERIFY121 149/149` · `VERIFY209 61/61` · `SMOKE PASS`.
기록 `docs/review/306-탑썸네일공격사이클.md`.


### 부록 D 개정 — 던전 카드 썸네일 **발주 종횡비** (2026-08-28, 작업 72 14회차)

부록 D 는 «투명 배경 PNG · 캔버스 315×334» 만 적었고 **종횡비 조건이 없었다.** 그런데 259 이후
액자는 **306×306 정사각**이고 규칙은 «종횡비 유지(contain)» 이므로, 짧은 축 채움률의 상한이
`min(AR, 1/AR)` 로 **수학적으로 고정된다.** 13회차 비평가 AD·AE 가 **독립적으로 같은 식**에 도달했다.

| 카드 | 현재 대체물 | 잉크 AR | 짧은 축 채움 **천장** |
|---|---|---|---|
| 1 황금 동굴 · 7 유물조각4 | `dragon/fly` 88×42 | **2.10** | **47.6%** |
| 5 유물조각3 | `robo/Running` 125×214 | **0.58** | **58.1%** |
| 4 유물조각2 | `zombie/walk` 176×223 | 0.79 | 79.0% |
| 2 수정 광산 | `bird/anim` | 0.85 | 85.0% ✅ |
| 3 유물조각1 | `elves/green_idle` 114×129 | 0.88 | 88.0% ✅ |

> **발주 조건 추가: 던전 카드 썸네일 아트는 잉크 종횡비 `0.85 ≤ AR ≤ 1.18` 로 그린다**
> (양축 ≥85% 채움. ≥90% 를 원하면 0.90~1.11). 몬스터 단독으로 정사각이 안 나오면
> **지면·배경 요소를 넣어 정사각을 채우는 구성**으로 그린다.

**카드1·5(및 7)는 그때까지 «보류(아트 대기)»** — CSS·기하로는 47.6% / 58.1% 가 절대 상한이고,
cover(크롭)는 각각 **52% / 42%** 를 잘라내야 해서 «왜곡» 이 «잘림» 으로 바뀔 뿐이다(두 비평가 독립 확인).

---

## §4 정오표 — 작업 335 «앵커가 둘이다» (2026-08-28, sess-1521-8678 워커 A)

**§4 의 y 실측치에 `−84` 를 적용하지 마라.** 서브탭 바는 **하단 앵커**다.

03 은 상단 HUD 와 하단 탭바 사이를 꽉 채우는 페이지라, 위에 붙는 요소와 아래에 붙는 요소가
서로 다른 자로 프레임 좌표로 옮겨진다. ROUTINE [2] 의 «프레임 y = 레퍼런스 y − 84» 는
**상단 앵커 요소**의 변환이다.

| 앵커 | 요소 | 변환 | 검산 |
|---|---|---|---|
| 상단 | §3 카드 5장 · §41 팝업 내장 재화 바 | `cap_y = ref_y − 84` | 카드1 상변 241 → **157** (구현 157) |
| **하단** | **§4 서브탭 바** · §5 앱 하단 탭바 | **`cap_y = ref_y − 60`** | 탭바 상변 2160 → **2100** (구현 2100) · 바 상변 2021 → **1961** (구현 1961) |

두 자의 차 **24px** 은 프레임(2280)이 레퍼런스 콘텐츠 영역(2256)보다 긴 몫이고,
이 화면에서는 **«카드 리스트 하단 ↔ 서브탭 바 상변» 이음매**에 고인다. 감점 사유가 아니다.

⚠ **비평가에게 이 표를 반드시 알려 줄 것.** ROUTINE [3] 은 «세로는 ref y − 84» 만 알려 주게
돼 있어서, 하단 앵커 요소는 자동으로 +24 어긋나 보인다. 72 의 15~17회차에서 비평가 넷이
그 때문에 유령 셋을 일치 지적했고(바 «+34px 하향» · 높이 «79→99» · 활성 알약 «−25.8%»),
그것이 72 의 ①~④ 를 세 회차 동안 6 에 묶었다. 셋 다 실측 Δ≤2px 이다.
상세: `docs/review/335-던전서브탭블록.md`.

### §4-1 · §4-3 확정치 (335 이 픽셀로 확정 — `python3 tools/scan335.py` · `scan335x.py`)

| 항목 | ref (1080×2340) | 프레임 (1080×2280) | 구현 |
|---|---|---|---|
| 바 검정 상변 | 2021 ~ 2026 (6px) | 1961 ~ 1966 | 1961 ~ 1966 |
| 바 검정 하변 | 2112 ~ 2117 (6px) | 2052 ~ 2057 | 2054 ~ 2059 |
| 바 높이(외곽) | 97 | 97 | 99 |
| 바 x (외곽) | 151 ~ 944 (w 794) | 1:1 | 151 ~ 945 |
| 바 안쪽 면 x | 160 ~ 937 | 1:1 | 149 ~ 930 → **335 이후 157 ~ 938** |
| **바 축** | 중심 547.5 = 화면중심 **+8 비대칭** | 1:1 | **+8** (96 2회차가 0 으로 만든 것을 335 가 되돌림) |
| **활성 알약 높이** | 바 검정 «안쪽» 2027 ~ 2111 = **85** | 85 | 85 |
| **이음매(카드 하변 ↔ 바 상변 바탕)** | **0px** — 카드 검정 하변(8)과 바 검정 상변(6)이 **겹친다** | 0 | 96 2회차 14px → **335 이후 0** |

**«레퍼런스가 기울어 찍혔다» 가 아니다** — 같은 화면의 카드는 x50~1029 로 좌우 여백 50/50 대칭이다.
이 바만 오른쪽으로 8px 밀린 것이 원본 디자인이다.

⚠ §4-2·§4-3 의 **칸** 실측치는 96 정오표 그대로 구현 기준이 아니다(2칸 → 우리 3칸, 209 «탑» 신설).
335 도 칸은 한 줄도 안 건드렸다 — 고친 것은 **껍데기 축**과 **이음매**뿐이다.

---

## §3-3 · §3-4 · §3-7-3 정오표 — 작업 342 «카드 내부 세로 리듬» (2026-08-29, sess-0032-11010 워커 B)

`tools/scan342.py`(ref↔cap 를 **같은 마스크로 동시에**) · `tools/probe342.js`(제품에게 **찍힌 픽셀**로)
두 자가 맞물린 값이다. 세로 앵커는 **카드 리스트 = 상단 앵커**(cap_y = ref_y − 84 · 335 «앵커가 둘»).

### ① §3-4 «width **288**» 은 **상수가 아니라 결과값**이다 ★

§3-4 는 카드1·2 의 알약을 «width 288(추정)» 으로 적고 카드3·4·5 우단을 «미확정» 으로 남겼는데,
바로 아래 §3-4-2 가 그 잠금 카드 라벨 잉크 우단을 **473 · 474 · 562 · 615**(절대)로 적고 있다.
카드1·2 의 알약 우단은 **429** 다 — 즉 **레퍼런스의 캡슐은 고정폭이 아니라 글자를 감싸고 자란다.**

| | 구성 | 합 |
|---|---|---|
| ref 카드1 알약 | 아이콘 자리 **58** + 라벨 잉크 **177** + 우측 여백 **52** | **287 ≈ 288** |

⇒ 288 은 «7글자 라벨 + 고정 패딩» 의 **결과**다. 342 이전 구현은 그 결과값을 `min-width` 로 박아
두고 라벨을 `position:absolute` 로 얹고 있어서 ⓐ 짧은 라벨은 오른쪽이 87px 비고
ⓑ 긴 라벨은 캡슐 밖으로 새 **90 이 `--psx` scaleX 로 눌러야** 했다. 342 가 캡슐을
**텍스트 shrink-wrap + 좌우 패딩(58 / 51) 고정**으로 바꾸며 `--psx` 는 선언째 사라졌다.
**알약의 자리(좌변 카드+92 · 상변 +121)와 높이(46)는 불변이다 — 폭만 내용을 따라간다.**

### ② 잉크 실측 확정치 (구현이 맞춰야 할 값)

| 항목 | ref 실측 | 근거 |
|---|---|---|
| 던전명 잉크 상변 | **카드+44** | §3-3 (5장 동일) |
| 던전명 잉크 높이 | **50~51** | §3-3 «51~52» 재확인 |
| 던전명 잉크 하변 ↔ 재화 알약 상변 | **26~27** | 위 둘 + §3-4 «알약 y 카드+121» 의 산술 결과 |
| 알약 라벨 잉크 좌변 | **카드+150~152** | §3-4-2 (구현 Δ0 — 원래 맞았다) |
| 알약 라벨 잉크 높이 | **28~29** | §3-4-2 |
| 알약 우변 − 라벨 잉크 우변(우측 여백) | **50~52** | §3-4 429 − §3-4-2 377/379 |
| 잠금 문구 잉크 상변 | **카드+223** | §3-7-3 |
| 잠금 문구 잉크 높이 | **41** | §3-7-3 |
| 잠금 문구 가로 중심 | **카드+489** (절대 539) | §3-7-3 |

### ③ ⚑ 잠금 문구는 썸네일 액자를 **밟고 지나간다** — «클리어런스» 는 레퍼런스에 없다

72 18회차 AQ 가 «잠금 문구 우변이 액자 좌변과 3~5px» 를 지적해 342 ⓒ-③ 으로 등재됐으나,
§3-7-3 과 §3-8 을 나란히 놓으면 **레퍼런스 자신이 요구하지 않는 조건**이다:

| | 문구 잉크 우단(카드기준) | 액자 좌변(카드기준) | 클리어런스 |
|---|---|---|---|
| ref 카드3 | **697** (절대 747) | 643 | **−54** |
| ref 카드5 | **704** (절대 754) | 643 | **−61** |

잠금 딤 **위**에 얹히는 글자라 액자를 밟아도 읽힌다. 좁아 보였던 것은 클리어런스가 아니라
**우리 문구가 ref 보다 작아서 왼쪽에 웅크리고 있던 것**이고(잉크 높이 37 ↔ ref 41),
높이를 ref 로 세우자 우단이 642 → **671** 로 ref 쪽에 가까워졌다. **같은 수리가 닫는다.**
⇒ 이 자리에 «클리어런스 ≥ N px» 게이트를 세우지 마라 — ref 와 반대 방향으로 끌고 간다.

### ④ 다음 세션이 이 화면을 픽셀로 잴 때의 함정 둘

- **노란 마스크는 «밝고 노랗다» 로는 모자라다.** 골드 던전의 **121 배경 입자 레이어**(`.bgm` —
  레퍼런스에 **없는** 우리 쪽 연출)가 (225,190,126) «따뜻한 금색» 이라 같이 걸린다.
  던전명 `#FDFF84` 는 **g−b = 123 · r≈g** 인 초록빛 노랑이다. 알약 안 **골드 코인 링**(`#FFE92D`,
  ref y360~412)도 같은 함정이라 세로 창을 카드+112 에서 끊어야 한다.
- **잉크를 오프스크린 캔버스에 다시 그려 재지 마라.** 폭은 ±2px 로 맞지만 **세로가 통째로 틀린다**
  (342 1회차: 카드+49 ↔ 실제 +39). 캔버스에는 `-webkit-text-stroke` 가 없고 베이스라인↔라인박스
  관계는 폰트 ascent 라 `font-size`·half-leading 으로 복원되지 않는다. **찍힌 픽셀을 읽어라**(350 처방).

상세: `docs/review/342-03던전카드내부세로리듬.md`.
