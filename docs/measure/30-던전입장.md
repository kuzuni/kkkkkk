# 30 — 던전 입장 화면 측정표 (레퍼런스 `docs/ref/30-던전-입장모습.jpg`)

> 원본 **1080×2340**. 전부 PIL/numpy 픽셀 스캔 실측(눈대중 없음).
> 배경색 `#82626F`(130,98,111) 대비 non-bg 마스크(임계 40, 저대비 재검 12) + 행/열 프로파일 +
> 연결성분 라벨링 + 스캔라인 색전이 추적.
>
> **좌표 변환** — 프로젝트 표준 하나뿐: **가로 1:1 · 프레임 y = ref y − 84**.
> - 상단 고정 요소 → `#stagearea` 기준 top = `ref y − 84` (**던전 모드는 상단 HUD 가 없어 stagearea top = 프레임 0 = ref 84**.
>   02/39 처럼 `ref y − 188` 을 쓰면 안 된다 — 그 값은 «상단 HUD 104px 이 있는» 화면 전용이다).
> - 하단 고정 요소 → 프레임 하단 기준 bottom = `2340 − ref bottom`.

---

## 0. 이 화면이 02·39 와 다른 점 — **먼저 이것부터**

| 요소 | 02 기본 메인 | 39 보스전 | **30 던전 입장** |
|---|---|---|---|
| 상단 재화 HUD 바(`#top`, 전폭 104) | 있음 | 있음 | **없음** — 캔버스가 `y=0` 까지 채운다 |
| 좌측 프로필 플레이트 | 있음 | 있음 | **없음** |
| 스테이지 헤더 반투명 캡슐 (320,324,480,92) | 있음 | 없음 | **없음** |
| 스테이지 라벨 | `STAGE 37` | `STAGE n` (110 하강) | **`골드 던전 - 레벨 6`** (§2) |
| 스테이지 진행바 + 노드 3종 | 있음 | 없음 | **없음** → §4 던전 진행바로 교체 |
| ⏱ 남은 시간 | 없음 | **있음** (397,231,242,79) | **있음 — 39 와 좌표 동일**(397,230,242,80) |
| 가로 700 진행바 | 없음 | 보스 체력바 (190,315,700,**67**) | **던전 진행바 (190,315,700,80)** — 39 와 같은 자리, **우측에 13px 내려온 보스 탭**이 붙는다 |
| 우상단 ▦ 메뉴 버튼 | 있음 | 있음 | **없음** |
| 좌·우 사이드 아이콘 스택 | 있음 | 있음 | **없음 — 전멸** |
| 좌하단 채팅 Ø100 · 마을 버튼 | 있음 | 있음 | **없음** → 자리에 **뒤로가기 화살표 버튼**(§5) |
| 우하단 진행형 미션 배너 460×151 | 있음 | 있음 | **없음** |
| **하단 탭바 180** | 있음 (y2160..2340) | 있음 | **없음 — 통째로 삭제** |
| 스킬 슬롯 8칸 행 y | 2015..2133 | 동일 | **2195..2313 (+180 = 탭바 높이만큼 정확히 하강)** |
| 배속 버튼 | (1010,1775) Ø82 · `rgba(0,0,0,.72)` · 흰 자물쇠 · `x1.3` | 동일 | **(1003,2100) Ø81 · `rgba(0,0,0,.60)` · 금색 자물쇠 · `x1.0`** |
| 전투 캔버스 색 | 물색 `#3A9ED2` 단색 | 자주 (100,57,66) | **`#82626F` 단색**(그라디언트·타일·비네트·딤 전부 없음, y0..2340 × x0..1080 ±1) |

> **구현 전략은 28 과 같다 — «02 를 고치지 않고 상태 클래스로 덮는다».**
> `#app.dunrun` 하나로 위 «없음» 을 전부 감추고, 던전 전용 요소(§2~§5)만 새로 얹는다.
> 클래스가 빠진 기본 상태는 02 그대로 한 줄도 바뀌지 않는다.

---

## 1. 화면 전체 구성 (위 → 아래)

| # | 요소 | ref x | ref y | w | h | 프레임 환산 |
|---|---|---|---|---|---|---|
| 0 | 전투 캔버스 배경 `#82626F` | 0 | 0 | 1080 | 2340 | 프레임 전면(#stagearea 가 프레임 전체) |
| 1 | 연기 파티클(배경 데코) | 318 | 126 | 118 | 97 | 런타임(구현 안 함) |
| 2 | **던전 타이틀** `골드 던전 - 레벨 6` | 잉크 388 | 잉크 177 | 303 | **35** | top **93** (§2) |
| 3 | **⏱ 스톱워치 아이콘** | 397 | 230 | 66 | 80 | top **146** (§3) |
| 4 | **타이머 값** `29.6` | 잉크 498 | 잉크 252 | 132 | 47 | 잉크 top **168** |
| 5 | **던전 진행바**(보스 탭 포함) | 190 | 315 | **700** | **80** | top **231** (§4) |
| 6 | 데코 바위 A(좌상, 화면 밖 잘림) | 4 | 647 | 76 | 169 | 런타임 |
| 7 | 적 진영 스프라이트 군집 | 403 | 808 | 573 | 392 | 런타임 |
| 8 | 아군 HP 바(캐릭터 머리 위) | 4 | 1040 | 141 | 26 | 런타임(이미 구현됨) |
| 9 | 플레이어(고양이 창병) | 7 | 1067 | 291 | 214 | 런타임 |
| 12 | 데코 바위 B(좌하) | 87 | 1775 | 181 | 170 | 런타임 |
| 13 | **뒤로가기(나가기) 버튼** | **47** | **1999** | **207** | **129** | left 47 · **bottom 212** (§5) |
| 14 | **배속 버튼(잠금)** | **963** | **2060** | **81** | **81** | **right 36 · bottom 198** (§6) |
| 16 | **스킬 슬롯 8칸** | 26 | **2195** | 1030 | 119 | **bottom 27** (§8) |
| — | **탭바** | — | — | — | — | **없음** |

---

## 2. 던전 타이틀 (상단 중앙)

| 항목 | 값 |
|---|---|
| 문자열 | **`골드 던전 - 레벨 6`** — 하이픈 1개, 앞뒤 공백 있음. 우리 게임은 **`<던전 이름> - 레벨 <층>`**(예 `황금 동굴 - 레벨 3`) 로 **글자 수가 같다** |
| 흰 글리프 잉크 bbox | x **388..690** (w **303**) / y **177..211** (h **35**) |
| 외곽선 포함 bbox | x 383..696 / y 172..217 (314 × 46) |
| 중심 x | **539** — 화면 정중앙 정렬 |
| 색 · 외곽선 | 흰 `#FFFFFF` + 검정 외곽선 **5px** (중심 그리기라 CSS `-webkit-text-stroke` 는 **10px**) |
| font-size | **≈ 38** (한글 잉크비 0.93 → 35 ÷ 0.93 = 37.6). 굵기 900 |
| 글리프 x 구간 | 골 388-416 / 드 422-449 · 던 467-494 / 전 499-530 · `-` 539-567 · 레벨 584-651 · 6 669-690 |
| 어절 간 공백 | 17~18 |
| 배경 캡슐/판 | **없음** — 02 의 반투명 pill 이 완전히 삭제됐다. 배경 위에 글자만 |
| 프레임 | 잉크 top **93** |

---

## 3. ⏱ 남은 시간 — **39 보스전과 같은 위젯·같은 좌표**

### 3-1. 스톱워치 아이콘

| 항목 | ref | 프레임 |
|---|---|---|
| 전체 bbox | (397, 230, **66**, **80**) | left 397 · top **146** |
| 본체 원 | Ø **66**, 중심 (429.5, 277) · y 245..309 | — |
| 상단 용두(knob) | x 411..446 (w 36) / y 230..250 (h 21) | — |
| 색 | 검정 외곽선 **4~5** → 금색 `#FDEB4E` 링 → 크림 문자판 `#FFFCDF` → 갈색 바늘 `#4E4235` |

> 39 실측(28 측정표 §1)은 (397,231,66,79) · 몸통 `#FEEB4F` — **오차 1px 이내로 같은 에셋**이다.
> 그래서 구현은 `#bossTm` 의 CSS 기하(용두 27×20 @ (18,2) · 원 60 @ (4,15))를 그대로 복제한다.

### 3-2. 값 텍스트 `29.6`

| 항목 | ref | 프레임 |
|---|---|---|
| 문자열 | **`29.6`** — 초 + 소수 1자리, 단위 문자 없음 | — |
| 흰 잉크 bbox | (498, 252, **132**, **47**) | 잉크 top **168** |
| 외곽선 포함 | (493, 246, 146, 62) → 스트로크 **5~6**(CSS 지정 **13**) | — |
| font-size | **≈ 66~68** (숫자 잉크비 0.69~0.71) | — |
| 자간 | 숫자 advance **39** (`2`498-530 · `9`537-570 · `6`596-629), `.` advance ≈ 18 |
| 아이콘 → 잉크 gap | 462 → 498 = **36** (외곽선 기준 31) |
| 세로 정렬 | 아이콘 중심 y 277 / 잉크 중심 y 275 — 중앙 정렬 |
| 그룹 bbox | x 397..639 (w **243**) → 중심 x **518** (화면 중앙보다 22 왼쪽) |
| 타이틀↔타이머 | 타이틀 외곽선 하단 217 → 아이콘 상단 230 = **13** |

> ⚠ **불확실** — 30·31 둘 다 값이 4글자(`29.6`/`28.4`)라 «중앙정렬 vs 좌측고정» 구분 불가.
> **아이콘 x=397 고정**으로 구현한다(39 와 같은 규칙이라 두 화면이 어긋나지 않는다).

---

## 4. 던전 진행바 — 02 스테이지 진행바를 완전히 대체

### 4-1. 바깥 형태 (**계단형** — 우측에 보스 탭이 13px 내려온다)

| 항목 | ref | 프레임 (바 좌상단 기준 local) |
|---|---|---|
| 전체 bbox | x **190..889** (w **700**) / y **315..394** (h **80**) | left 190 · top **231** |
| 중심 x | **539.5** — 좌여백 190 · 우여백 190 (정중앙) | — |
| 본체(막대) | x 190..889 / y 315..**381** (h **67**) | local (0,0,700,67) |
| **보스 탭** | x **784..889** (w **106**) / y 315..**394** (h **80**) | local (**594**,0,106,80) |
| 계단 x | **784~788** 에서 바닥이 381 → 394 로 꺾임 | — |
| radius | **≈ 22** (좌상·우상 코너 — 초판 «18» 은 과소, 1회차 비평 2인 독립 재실측 21~23), 보스 탭 하단 코너 ≈ 18. **캡슐 아님**(h67 에 r22) |
| 바깥 스트로크 | **6px** `#C1B6BA`(193,182,186) |
| 안쪽 트랙(검정) | x **196..883** / y **321..376**(본체) · 321..**389**(보스 탭) · `#0A0A0A~#141414` | local (6,6,688,55) / 탭 (594,6,100,68) |
| 프레임 | top **231** · 본체 bottom **298** · 탭 bottom **310** |

### 4-2. 채움(fill)

| 항목 | 값 |
|---|---|
| bbox | x **205..679** (w **475**) / y **328..369** (h **42**) → local (15, 13, 475, 42) |
| 트랙 안쪽 inset | 좌 **9** / 상 **7** / 하 **7** |
| 색 | 가로 그라디언트 좌 `#E49D21` → `#E68D15` → `#E37915` → `#E26C15` → 우 `#DB3E11` |
| 상단 하이라이트 | 맨 위 1~2px `#E1A642` |
| radius | 좌·우 끝 거의 직각 (r ≤ 4) |
| 100% 기준 | **불확실**. ⓐ 계단 전 x205..779(**574**) 기준 → 83 % · ⓑ 트랙 대칭 inset x205..874(669) 기준 → 71 % |

> **채택: ⓐ 574** — 보스 탭(해골)은 «끝점 표식» 이라 채움이 그 밑을 지나가면 해골을 가린다.
> 계단 앞에서 멈추는 편이 형태와 의미가 둘 다 맞는다. `max-width:574px`.

### 4-3. 바 안 수치 텍스트 — **02 에는 없던 것**

| 항목 | 값 |
|---|---|
| 문자열(ref) | **`9.61C`** — 게임 수 표기(접미사 C). 우리는 `fmt(누적 피해량)` 을 넣는다 |
| 흰 잉크 bbox | x **506..573** (w 68) / y **340..359** (h **20**) → local top **25** |
| 중심 x | **539.5** = **바 전체(=화면) 정중앙** — 채움 끝(679)이 아니라 **바 중앙 고정** |
| 색 · font-size | 흰 + 검정 외곽선 **4~5**(CSS 10) · **≈ 29** (cap 20 ÷ 0.69) |

> 문자열이 다르므로 **폭은 대조 대상이 아니다**. 잉크 «높이 20» 과 중심(539.5, 349.5)만 맞춘다.

### 4-4. 보스 해골 아이콘

| 항목 | 값 |
|---|---|
| 흰 잉크 bbox | x **818..862** (w **45**) / y **335..375** (h **41**) |
| 중심 | **(840, 355)** = 보스 탭 세로 중심(354.5) |
| 색 | `#FEFDF8` 흰 단색. **링·원판 없음** (02 의 3중 링 구조와 전혀 다르다) |
| 배경 | 검정 트랙 위에 그대로 얹힘 |

> 39 의 체력바 끝 두개골(817,334,46,42)과 **1px 이내로 같은 에셋**이다 → `#bossHp u` 의
> 이모지 흰색화 레시피(`grayscale(1) brightness(3)` + `scale(1.122,.824)`)를 그대로 복제한다.

---

## 5. 뒤로가기(나가기) 버튼 — 좌하단, 02 에 없던 신규 요소

| 항목 | ref | 프레임 |
|---|---|---|
| bbox (검정 외곽 포함) | x **47..253** (w **207**) / y **1999..2127** (h **129**) | left **47** · **bottom 212** |
| radius | **≈ 13~14** (손그림 에셋이라 상 14 / 좌 11 로 미세하게 다름, ±3) | **CSS 20** — `box-sizing:border-box` + `border:6px` 라 지정 14 는 외곽 곡률 10 으로 나온다(1회차 비평 A4) |
| 검정 테두리 | **6px** `#000` (좌 48..53 · 상 2000..2005 · 우 247..252 · 하 2122..2127) |
| 판 채움 | 세로 그라디언트 상 `#7C7D9C` → 본체 `#757590` → 최하단 `#5F5F7B` |
| 광택 점 | x 61..74 / y 2013..2025 → **Ø 14** 원 `#9E9EBA` (판 좌상단 기준 (14,14)) |
| 흰 화살표 ◀ | 잉크 x **101..197** (w **97**) / y **2024..2100** (h **77**) · `#FDFDFD` + 검정 외곽선 **6** |
| 화살표 중심 | (149, 2062) ≈ 판 중심 (150, 2064.5) |
| 드롭 섀도 | y 2128..2135 (8px), x 51..249, `#7F5F6C` (배경보다 어두운 띠) |
| 텍스트 라벨 | **없음** — 아이콘만 (`나가기`·`포기` 같은 글자 0개) |

---

## 6. 배속 버튼 (우하단, 잠금)

| 항목 | ref | 프레임 |
|---|---|---|
| 원 bbox | x **963..1043** / y **2060..2141** → **Ø 81**, 중심 (1003, 2100) | **right 36 · bottom 198** |
| 채움 | `#372A2F` = **`rgba(0,0,0,.60)`** 를 `#82626F` 에 합성. 테두리 없음 |
| 자물쇠 | x 986..1021 (w 36) / y 2078..2120 (h 43) — **금색** `#FDD16E` + 하이라이트 `#FFE894` + 검정 외곽선 3~4 |
| 배율 텍스트 | `x1.0` — 잉크 x 984..1022 (w 39) / y 2125..2140 (h **16**), `#7E7E7E`, fs ≈ 22 |

> 02 대비 **위치만** 바뀌는 게 사실상 전부다(자물쇠 색·알파는 ⑤ 색 항목이라 통과 기준 밖).
> 구현은 `#app.dunrun #spdb{right:37;bottom:200;width:81;height:81}` 한 규칙.

---

## 7. 스킬 슬롯 8칸 — **탭바 높이(180)만큼 그대로 내려온 것**

| 항목 | 30 (던전) | 02 (메인) | 차이 |
|---|---|---|---|
| 개수 · 외경 · pitch | 8 · **119~120** · **130** | 8 · 120 · 130 | 동일 |
| 중심 x | 86.5 / 216.5 / 347 / 475.5 / 606 / 736.5 / 866.5 / 996.5 | 86 / 216.5 / 347 / 476.5 / 606 / 736.5 / 866.5 / 996.5 | **동일(±1)** |
| 좌·우 여백 | 26 / 24 | 26 / 23.5 | 동일 |
| 행 y | **2195..2313** (중심 2254) | 2015..2133 (중심 2074) | **+180 = 탭바 높이** |
| 화면 하단 여백 | **26** (2340 − 2313 − 1) | (탭바 상단까지 27) | 던전 모드에서만 `#slots{margin-bottom:16}` 로 1px 줄인다 |
| 링·well 색 | 활성 `#FFAE2B` / 대기 `#26D9FF` / 잠금 `#B9BCCB`, well `#377DC2`·`#454463` | 동일 | 동일 |

> **구현상 아무 것도 안 해도 된다.** `#battlefoot` 이 `#stagearea` 바닥에 붙어 있으므로
> 탭바를 `display:none` 하면 `#stagearea` 가 180 자라고 슬롯이 정확히 180 내려온다.
> 현재 슬롯 바닥 여백 = `battlefoot padding-bottom 10 + #slots margin-bottom 17` = **27** — ref 와 일치.

---

## 8. 진행도 / 오버레이 / 연출 — 확인 결과 (있음/없음은 **픽셀 확정**)

| 항목 | 결과 |
|---|---|
| 웨이브·처치 수 표시(`3/10` 등) | **없음.** 진행 정보는 바 안 수치 하나뿐 |
| 스테이지 번호 | **없음** (`레벨 6` 이 그 역할) |
| 던전 이름 / 층 | **있음** — `골드 던전 - 레벨 6` |
| 제한 시간 | **있음** — `29.6`, ⏱ 아이콘, 소수 1자리 카운트다운 |
| 포기/나가기 **텍스트** 버튼 | **없음** — 좌하단 화살표 아이콘 버튼만 |
| 입장 배너·「던전 입장」 문구 | **없음** |
| 화면 딤 / 페이드 / 비네트 | **없음** — 배경 RGB 가 전 영역 ±1 로 균일 |
| 팝업 · 모달 | **없음** — 04 던전 세부 팝업은 이미 닫힌 상태 |
| 전투영역 가로 HP 바(`#hpwrap`) | **없음** (02 와 동일) |
| 가이드 미션 배너 | **없음** |

---

## 9. 측정 신뢰도

| 값 | 근거 |
|---|---|
| 이미지 크기 · 배경색 · 요소 유무 | **픽셀 확정** (임계 40 + 12 이중 검사) |
| 바 bbox·트랙·채움·계단 x·해골 / 타이틀·타이머 잉크 bbox / 뒤로가기·배속·슬롯 bbox | **픽셀 확정** (행·열 프로파일 + 스캔라인) |
| radius (바 18 · 뒤로가기 14) | 코너 첫 픽셀 역산 **±3** (손그림 에셋이라 상·좌가 미세하게 다름) |
| font-size 전부 | 잉크비 환산 **±2** (한글 0.93 · 숫자 0.69~0.71) |
| **채움 100% 기준** | **불확실** → ⓐ 574 채택 (§4-2) |
| **타이머 그룹 가로 정렬 규칙** | **불확실** → 아이콘 x=397 고정 채택 (§3-2) |

### 9-1. 초판 오류 정정 (1회차 비평 2인이 독립 재실측해 잡은 것 — 위 표에 이미 반영)

| 항목 | 초판 | 정정 | 근거 |
|---|---|---|---|
| 뒤로가기 판 h | 132 | **129** | y2128..2138 은 판이 아니라 **드롭섀도**다(A·B 일치) |
| 진행바 radius | ≈18 | **≈22** (21~23) | 코너 곡률 재실측 — 지정 18 이면 −19 % |
| 뒤로가기 radius | 14 그대로 지정 | **CSS 20** | border-box + border 6 이면 지정 14 → 외곽 곡률 10 |
| 배속 원 | (963,2059,81,82) · right 37 · bottom 200 | **(963,2060,81,81) · right 36 · bottom 198** | A·B 둘 다 우여백 36 · 하단 198 |
| 슬롯 하단 여백 | 27 | **26** | 2340−2313−1 |
| 타이머 숫자 검정 스트로크 | «5~6 → CSS 13» | **6 → CSS 11** | CSS 13 은 캡처 실측 8px 로 +33 % |
| ⏱ 아이콘 h | 80 | **78~79** | 캡처 80 은 +1~2 (허용 범위, 미수정) |
| ⏱ 계단 꺾임 구간 | «x784~788» | **x784..799 (16px 라운드 전이)** | 시작점 784 만 맞다 |

---

## 10. 아트 필요 표 (GPT 아트 발주서용)

| 요소 | w×h | 중심 (프레임 기준) | 용도 | 현재 대체물 |
|---|---|---|---|---|
| 스톱워치 아이콘 | 66×80 | (430, 186) | 던전 제한 시간 아이콘 | **CSS 작화** — 금색 원 Ø60(검정 4) + 용두 27×20 (`#bossTm s` 복제) |
| 진행바 보스 해골 | 45×41 | (840, 271) | 진행바 우측 끝 표식 | 이모지 💀 (`grayscale(1) brightness(3)`, `scale(1.122,.824)`) |
| 뒤로가기 화살표 | 97×77 | (149, 1978) | 던전 나가기 | **CSS 작화** — `◀` 글리프 + 검정 외곽선 6 |
| 진행바 껍데기(계단형) | 700×80 | (540, 271) | 던전 진행도 | **CSS 작화** — 회백 림 6 + 검정 트랙 + 주황→적 그라디언트 채움 |
| 뒤로가기 판 | 207×132 | (150, 1981) | 버튼 배경 | **CSS 작화** — 남보라 그라디언트 + 검정 6 + 광택점 Ø14 |
| 던전 바닥 | 1080×2280 | — | 던전 전용 배경 | 캔버스 기존 바닥 그대로(색 교체는 ⑤ 항목이라 보류) |
