# 55 설정 팝업 — 측정표

> 레퍼런스 `docs/ref/55-설정팝업.jpg` (1080×2340, 상단 84px 상태바).
> **프레임 y = 레퍼런스 y − 84** (가로 1:1). 아래 표의 «ref» 는 레퍼런스 절대 y, «프레임» 은 −84 한 값이다.
> 측정 도구: `tools/scan55.py` (PIL 행/열 색전이 스캔 · 밴드 · 다크픽셀 bbox · 코너 반경 프로브 · ASCII 색지도).
> 담당 세션: sess-0758-2927 (워커 D, 2026-08-25) — T3, 이전 기록 없음(첫 구현).

## 0. 껍데기 판정

**(가) 딤 위 가운데 다이얼로그.** 03·10·14 식 전체화면 페이지가 아니고, 06·07 식 바닥 시트도 아니다.
팝업 아래위로 딤이 보이고, 팝업 폭이 798 로 1080 에 못 미친다.

| 항목 | ref | 프레임 | 비고 |
|---|---|---|---|
| 팝업 외곽 bbox | x142..937 · y539..1885 | y455..1801 | **796 × 1347** |
| 가로 중심 | 539.5 | — | 프레임 정중앙(540) — Δ0.5 |
| 세로 중심 | 1212 | **1128** | 프레임 중심 1140 보다 **12px 위** |
| 외곽 검정 테두리 | **7px** | | 좌우는 6(142..147 / 932..937) · 상하는 8(539..546 / 1878..1885) 로 찍힌다 — 7 로 잡는다 |
| 갈색 림 | 12px | | 좌 149..160 · 우 919..930 · 하 1867..1877 |
| 모서리 반경 | 외곽 **44** (검정 안쪽 36) | | 코너 프로브 8점 전부 r_in=36 원호에 Δ≤1 로 맞음 |
| 헤더 밴드 | y547..637 (**h91**) | y463..553 | 갈색 `#503838`, 좌우 검정 안쪽 전폭 |
| 크림 본문 | x161..918 (**758**) · y639..1866 (**1228**) | y555..1782 | `#F0D8B8` |
| 딤 | **α = 0.545** | | 아래 «딤 검산» 참고 |

> **딤 검산(LESSONS 52-① — 어긋나면 제3의 독립 검산으로 가른다)**
> ① 고정 UI 3구역(탭바·HUD·좌사이드)의 «평균색» 을 52(딤 없음)와 비교 → 0.401 / 0.388 / 0.389 → α 0.61.
> ② 52 에서 **거의 흰(min>235) 픽셀 1,965 표본**을 골라 55 의 같은 좌표를 읽음 → 중앙값 비율 **0.455** → α **0.545**.
> ①은 어두운 픽셀이 JPEG 노이즈 바닥에 눌려 위로 편향된다(0 으로 내려가지 못한다). **밝은 표본 쪽(②)이 옳다.**
> 구현은 `rgba(0,0,0,.55)`.

> 53 가방 팝업과 **껍데기 규격이 같다**(검정 8 + 림 12 + r44 + 헤더 91 + 크림 본문). 폭·높이만 다르다.

## 1. 헤더 — 타이틀

| 요소 | ref bbox | 크기 | 비고 |
|---|---|---|---|
| 타이틀 잉크(외곽선 포함) | x491..587 · y566..624 | 97×59 | 중심 x539, 헤더 중심 y592 |
| 타이틀 코어(흰 글자) | x497..579 · y571..616 | **83×46** | font-size = 46/0.93 ≈ **49** (LESSONS 01 잉크비 0.93) |

## 2. 본문 요소 — 절대 좌표 (ref)

| # | 요소 | ref bbox | 크기 | 색 |
|---|---|---|---|---|
| a | 소제목(슬라이더 위) | x234..285 · y657..689 | 52×33 | 회색 잉크 |
| b | **슬라이더 트랙** | x216..894 · y703..752 | **679×50** | 검정 외곽 6 + 주황 `#F8B010` |
| b-1 | 주황 채움 | x222..834 · y709..746 | 613×38 | |
| b-2 | 흰 노브 | x835..894 · y703..752 | **60×50** | 흰 `#F8F8F8` + 검정 외곽 |
| c | 행 A 라벨 | x223..354 · y841..894 | 132×54 | |
| c-2 | 행 A 우측 버튼 | x685..853 · y831..899 | **169×69** | 연금색 `#EAC076` + 검정 외곽 **3~4**(직선부 x770 실측) · 잉크 「한국어」 **82×34** @ x729..810 y846..879 |
| d | 행 B 라벨 | x222..371 · y927..972 | 150×46 | |
| d-2 | 행 B 토글 | 트랙 x685..853 · **169×56** | 노브 **99×69** | 트랙 외곽 검정 **3** · 안쪽 «밝은 띠 4 + 본색 42 + 밝은 띠 4». **라벨(ON/OFF)이 노브 안에 있다** |
| e | 행 C 라벨 | x217..359 · y1017..1057 | 143×41 | |
| e-2 | 행 C 알약 | x686..849 · y1003..1064 | 164×62 | 초록 |
| f | 행 D 라벨 | x215..439 · y1091..1144 | 225×54 | |
| f-2 | 행 D 알약 | x686..849 · y1088..1150 | 164×62 | 초록 |
| g | 행 E 라벨 | x227..414 · y1181..1228 | 188×48 | |
| g-2 | 행 E 알약 | x686..849 · y1179..1240 | 164×62 | **회색 트랙 `#909090` + 우측 청회색 노브 `#9098C0` (x780..848 · 69×48)** |
| h | 행 F 라벨 | x219..538 · y1258..1318 | 320×61 | |
| h-2 | 행 F 알약 | x686..849 · y1263..1324 | 164×62 | 행 E 와 같은 회색/청회색 |
| i | 가운데 텍스트 | x472..608 · y1385..1423 | 137×39 | |
| j | 흰 밴드 | x416..663 · y1454..1531 | **248×78** | 흰 `#FBF4EC`, 안에 노랑·파랑 아이콘 |
| k | 하단 버튼 1 | x201..418 · y1588..1686 | **218×99** | 청록. 3칸 = x201..418 / 432..647 / 661..878, **간격 13** (inkcols 전 구간 실측 — 모서리 곡선 안에서 재면 210·간격 20 으로 잘못 읽힌다) |
| l | 하단 버튼 2 | x432..647 · y1588..1686 | 217×99 | 금색 `#F8A808`~`#F8D028` |
| m | 하단 버튼 3 | x662..878 · y1588..1686 | 217×99 | 금색 |
| n | 가운데 소문자 | x495..586 · y1727..1751 | 92×25 | |
| o | 안내 1행 | x191..890 · y1777..1798 | 700×22 | |
| p | 안내 2행 | x212..869 · y1828..1848 | 658×21 | |

**행 pitch** — 알약 위끝 기준 919 → 1003 → 1088 → 1179 → 1263 : **84 / 85 / 91 / 84** (평균 **85**).
알약 우끝은 5행 전부 x849 로 일치(본문 우끝 918 에서 **인셋 69**), 좌끝 686.
하단 버튼 pitch **231**(폭 217 + 간격 14~15), 3칸 전체 x201..878 = 677, 중심 539.5.

## 3. 모서리 반경 (첫 행 인셋 / 코너 프로파일로 실측)

| 요소 | 반경 | 근거 |
|---|---|---|
| 팝업 `.cf55` | 외곽 **44**(검정 안쪽 36) | 코너 프로브 8점이 r_in=36 원호에 Δ≤1 |
| 옵션 리스트 | **0**(각) | 좌상·좌하 코너 offset 이 전부 0 |
| 계정 패널 | **45** | 좌상 코너 4점(28/22/17/9)이 R=45 원호와 Δ≤1 |
| 슬라이더 트랙 | **18** | 첫 행 인셋 ref 14 / 캡처 14 |
| 하단 버튼 | **41** | 첫 행 인셋 ref 37 / 캡처 38 |
| 언어 값 버튼 | **22** | 좌상 프로파일 ref 19-14-12-10-9-8 / 캡처 −-17-13-12-10-8 |
| 토글 트랙 / 노브 | **29 / 34.5** | 트랙 58 · 노브 69 의 완전 알약 |
| 구글 카드 | **12** | |

> **반경은 «첫 행 인셋» 으로 재라.** 반경 자체를 코너 곡선으로 재려 하면 라운드 사각형의 직선부·곡선부 경계에서
> 값이 흔들린다. 상자의 위끝 한 행에서 «왼쪽 변에서 몇 px 들어가야 잉크가 시작되는가» 를 재면 한 번에 갈린다
> (레퍼런스와 캡처를 **같은 방법으로** 재는 것이 핵심 — 절대값이 아니라 두 값의 일치를 본다).

## 4. 아트 필요 표

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 행 아이콘 — 언어 | **56×56** | 말풍선 | 💬 (`--sf 1.0368`) |
| 행 아이콘 — BGM | **50×50** | 음표 | 🎵 (`--sf 1.06`) |
| 행 아이콘 — SFX | **64×44** | 스피커+음파 | 🔊 (`--sf .85`) |
| 행 아이콘 — 화면 흔들림 | **71×56** | 흔들리는 카메라 | 🎥 (`--sf 1.10`) |
| 행 아이콘 — 푸시 알림 | **48×50** | 종 | 🔔 (`--sf .9384`) |
| 행 아이콘 — 야간 알림 | **58×64** | 종+초승달 | 🌙 (`--sf 1.1136 --dx -4`) |
| 구글 「G」 배지 | **39×39** | 계정 연동 플랫폼 로고 | CSS 4색 링 + 흰 속 + 파란 막대 |
| 슬라이더 노브 | **60×50**(흰 코어 50×40) | 볼륨 노브 | CSS 흰 라운드 사각(r16) |

> ### ⚠ 정오표 (2026-08-29, 작업 356 4회차)
> 위 표의 «현재 대체물» 칸에 있던 **`--sx`(가로만 늘이던 배율 .87~1.48)는 폐기했다** —
> 저장소 주인 지시 356 «어떤 아이콘도 찌그러뜨리지 않는다». `--sx > 1` 이던 셋(BGM·SFX·흔들림)은
> 선언만 뗐고, `< 1` 이던 셋은 그 값을 `--sf` 에 곱해 등방으로 흡수했다
> (언어 1.08×.96 · 푸시 1.02×.92 · 야간 1.28×.87). 데이터는 `CF_ROWS`(index.html) 의 `ix` 필드였고
> **필드째 없어졌다** — 되살리지 마라(게이트 `verify356` [A] `i.cf55-ic` · [R3]).
>
> 그래서 **아래 한 줄은 이제 틀렸다**: 「아이콘 6종은 레퍼런스 bbox 를 이미 정확히 차지한다(Δ≤2px)」.
> 지금은 «**bbox 안에 등방으로 들어가 있다**» 가 맞다. ref 6개 중 정사각이 아닌 넷(64×44 · 71×56 · 48×50 · 58×64)은
> 남는 폭·높이가 비어 있고, 그 빈 자리는 **아트가 들어오면 채워진다**(이모지로는 못 채운다 — 채우려면 찌그러뜨려야 한다).
> 아트 발주 시 w×h 는 위 표의 값 그대로다.

> ### ⚠ 정오표 (2026-08-29, 작업 400 — «짧은 프레임에서는 이 표가 절대값이 아니다»)
> 이 측정표의 세로 좌표는 전부 **9:19(1080×2280) 절대값**이고, 그 전제는 «상자가 늘 1347px» 이다.
> **1600(9:13.3) 프레임에서는 그 전제가 깨진다** — 쓸 수 있는 띠가
> **1278px**(상단 HUD 판때기 `.pedge` 하변 **142** ~ 하단 탭바 상변 **1420**)뿐이라
> 상자가 **69px 눌린다**. 눌리기 전에는 상자가 탭바를 42px · HUD 를 27px 파고들고 있었다(`probe400`).
>
> **주인 지시(351 재지시 ②)로 레퍼런스 이탈이 허용된 자리다** — 이탈은 «1600 에서 보이게 하는 데
> 필요한 만큼» 인 69px 뿐이고, **2280·1920 은 원소별로 Δ0px** 다(`verify400` §2 의 20자리 표가 못박는다).
>
> 1600 에서만 달라지는 값(나머지는 전부 이 표 그대로):
>
> | 자리 | 2280(이 표) | 1600 | 비고 |
> |---|---|---|---|
> | `.cf55` 높이 | 1347 | **1278** | 띠를 꽉 채운다(상변 142 · 하변 1420) |
> | 옵션 리스트 `.cf55-list` | 509 (행 pitch 84.8) | **488** (행 **81.3**) | 이웃 토글 데드존 15.8 → **12.3** |
> | 계정 패널 `.cf55-acc` 높이 | 198 | **181** | 안쪽 여백 16/28/30 → 13/22/22 |
> | 리스트 ↔ 계정 패널 | 31 | **24** | |
> | 계정 패널 ↔ 버튼 3칸 | 26 | **20** | |
> | 버튼 3칸 ↔ 「계정 삭제」 | 34 | **26** | |
> | 「계정 삭제」 ↔ UID 줄 | 16 | **12.5** | |
> | UID 줄 ↔ Gamer Id 줄 | 19 | **15** | |
> | 본문 바닥 여유 | 11 | **8.5** | |
>
> ⚑ **볼륨 그룹(제목·슬라이더·0/최대·구분선, 본문 0..185)은 한 픽셀도 안 줄었다** — 두 해상도 동일.
> ⚑ 배분은 상수가 아니라 **`--sq`(= max(0, 1669 − frameH), 상한 69) × 자리별 지분**이라
> 1600↔1669 사이가 **연속**이다. 문턱(`.shortf`)으로 갈랐으면 1669 에서 리스트가 49px 튄다.
> ⚑ 1회차는 69px 을 **리스트 한 장에 몰아** 행 pitch 를 73.3 으로 만들었고(데드존 **4.3px**),
> 비평가 CG·CH 가 ④ 조작성 **6·5** 로 기각했다 — «잘림·겹침·가독은 무해, 표적 간격만으로 갈린다».

> ## 정오표 — 699 «연출 스킵» 토글 (2026-09-01, sess-1834-27281 워커 B · 주인 지시 2026-09-02 02:00)
> 레퍼런스에 없는 요소 하나가 얹혔다: `#cfSkip` = 363/514 의 공용 토글(`.sm-sk` 56px).
> 자리는 **「볼륨」 제목 줄의 오른쪽 빈 구간** — 오른끝을 슬라이더 우단(본문 x734)에 맞추고
> (`right:24px`), 세로는 제목 띠(본문 9..54)의 한가운데(`top 3.5`). 짧은 프레임 보정은 제목과
> **같은 지분 계수**(`--sq × 0.028986`)를 쓴다.
> ⚑ **6행 리스트·상자 높이·`--sq` 지분표는 한 픽셀도 안 건드렸다**(`verify400` 156/156 · `verify55` 66/66 무수정).
> 7번째 행으로 넣어 보면 ⓐ 행 84.86 → 72.7 로 데드존이 15.8 → **3.9px**(400 1회차가 기각된 값) ·
> ⓑ 상자를 한 행 키우면 1600 에서 눌림이 69 → **154** 라 헤더가 91 → 55.3 으로 눌려 **제목이 잘린다**(실측).
