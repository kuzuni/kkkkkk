# 91 — 도감 시스템 «부위 × 등급 세트» 설계표

> 작업 91 (저장소 주인 지시 2026-08-26). 레퍼런스 이미지가 없는 **시스템 교체**라 지시서 [3]-(가)로 진행한다
> (비평 루프 없음). 여기 표가 곧 밸런스 근거이고, 실행 검증은 `node tools/verify91.js` 가 63개 항목으로 건다.
> 기록: `docs/review/91-도감시스템교체.md`

---

## 1. 세트 정의 (37세트 → **118 이후 41세트**)

세트 = «같은 부위(계열) · 같은 등급» 의 **전 종**. 구 구조(카테고리 4개 × «보유 종 수 ≥ need → 티어») 는 폐기.

| 키 | 탭 | 카테고리 | 세트 수 | 구성원 | 비고 |
|---|---|---|---|---|---|
| `equip:weapon:{0..7}` | 무기 | equip | 8 | 5·5·5·5·5·5·5·**1** | 85 의 8등급 체계. 최종 등급(불멸)만 1종 |
| `equip:shield:{0..7}` | 방패 | equip | 8 | 위와 동일 | |
| `equip:amulet:{0..7}` | 목걸이 | equip | 8 | 위와 동일 | |
| `skill:{0..5}` | 스킬 | skill | 6 | 4 × 6 | 86 의 «6등급 × 4종 = 24종». 스킬엔 7·8등급이 없다 |
| `pet:{0..5}` | 펫 | pet | 6 | 1·2·2·2·1·1 = 9 | 현 9종 분포 그대로 |
| ~~`relic:0`~~ | 유물 | relic | ~~1~~ | ~~10~~ | 91 당시 — 89 의 10종이 «등급 혼합 1세트» 였다 |
| `relic:{0..2}` | 유물 | relic | **3** | **3 · 4 · 3** | **118**(주인 지시) — `RELICS` 배열 순으로 자른 «등급 혼합 3세트». Lv 는 유물 Lv |
| **합계** | 6탭 | | **41**(118 · 106 반영) | 아이템 178종 | 한 아이템이 두 세트에 속하지 않는다(검증 [1]·verify118 [1]) |

**118 — 유물 3세트 (2026-08-26, 저장소 주인 지시 «유물은 임의로 3개 / 4개 / 3개로 짝지어 도감 세트로»)**

| 키 | 세트명 | 구성원 | 구성원 등급(mul) | 세트 mul(평균) | 단계당 «모든 피해» |
|---|---|---|---|---|---|
| `relic:0` | 전사의 유물 | rl0 낡은 부적 · rl1 전사의 문장 · rl2 광전사의 뿔 | 0(1.0) · 1(1.5) · 2(2.3) | **1.600** | +3.20% |
| `relic:1` | 현자의 유물 | rl3 현자의 눈 · rl4 용의 심장 · rl5 폭풍의 인장 · rl6 황금 왕관 | 2(2.3) · 3(3.6) · 3(3.6) · 4(6.0) | **3.875** | +7.75% |
| `relic:2` | 창세의 유물 | rl7 세계수의 씨앗 · rl8 창세의 파편 · rl9 심판의 저울 | 4(6.0) · 5(10.0) · 1(1.5) | **5.833** | +11.67% |

* 컷은 `RELIC_CUT = [3,4,3]` 한 곳에만 적는다. **종수가 늘면 남는 종은 마지막 세트로 붙는다** —
  세트 «개수 3» 은 주인이 못박은 상수라 자동 생성 대상이 아니다(LESSONS 91-4 의 단서 그대로).

* 구성원 목록은 코드가 `EQUIPS`/`SKILLS`/`PETS`/`RELICS` 에서 **자동으로** 만든다(`buildCollSets()`).
  종수를 늘리는 작업(75·85·86·87 계열)이 또 와도 도감은 손댈 필요가 없다.
* 유물 세트의 등급 배율은 구성원 `GRADE[g].mul` 의 **평균 = 3.78** 이다
  (구성원 등급 0·1·1·2·2·3·3·4·4·5).

---

## 2. 단계 규칙 — «가능 단계 = min(세트 최저 Lv, 10)»

> 지시 ③ 이 «1레벨 단위» 와 «10레벨 단위» 중 담당이 밸런스로 정하라고 위임했다. **1레벨 단위 · 상한 10단계**로 정했다.

| 근거 | 내용 |
|---|---|
| 지시의 규칙문 | «전부 Lv.1 이상 → 1단계, 전부 Lv.2 이상 → 2단계» = **1레벨 단위** |
| 지시의 검증문 | «무기·일반 5종 Lv.1 → 1단계 가능 / 전부 Lv.2 → 2단계 가능» = 같은 결론 |
| 지시 ③ 의 우려 | `MAX_LEVEL 100` 을 그대로 쓰면 세트당 100단계 → 과하다 |
| 결정 | 사다리는 1레벨 단위로 두고 **상한만 10** 으로 끊는다. 레퍼런스 21 의 «단계 N» 표기와도 맞는다 |

* **미보유 = Lv 0** 이므로 한 종이라도 없으면 0단계다.
* 최종 등급(불멸) 세트는 1종이라 그 한 종의 Lv 가 곧 세트 최저 Lv 다(`maxLv` 가 Infinity 라 10단계 도달은 쉽다 —
  대신 그 1종을 얻는 것 자체가 Lv.100 신화 조합이라 관문은 앞단에 있다).
* [강화] 버튼은 **한 번에 한 단계**만 올린다(가이드 미션 «도감 보너스 1회 받기» 카운터와 결이 맞는다).

---

## 3. 보너스 표 — 단계당 값 = `기준값 × 세트 등급 mul`

지시 ② «세트 등급 mul 에 비례한 고정 스탯». 지시의 예시 «무기 · 일반 1단계 = 공격력 +2%» 가 그대로 기준값이다.

### 3-1. 축 배정

| 탭 | 축 | 기준값(등급 mul 1.0 기준) | 근거 |
|---|---|---|---|
| 무기 | 공격력 | +2.00% | 지시 «무기→공격력», 예시값 |
| 방패 | 최대 체력 | +2.00% | 지시 «방패→최대 체력» |
| 목걸이 | 치명타 피해 / 골드 획득 | +0.50% / +1.00% | 지시 «목걸이→치명/골드». 치명타 피해는 `stat.cdmg`(배수)에 **가산**이라 값을 낮게 잡았다 |
| 스킬 | 스킬 피해 / 공격 속도 | +1.20% / +0.40% | 지시 «스킬→공격속도/스킬 피해». 스킬 피해는 `b.atk` 축이다 |
| 펫 | 동료 피해 / 골드 획득 | +2.00% / +1.00% | 지시에 축이 없어 구 펫 도감 성격(펫 피해·골드)을 이어받았다 |
| 유물 | 모든 피해 | +2.00% (118 — 세트별 × 1.600 / 3.875 / 5.833 = **+3.20% / +7.75% / +11.67%**) | 지시 «유물→모든 피해». 이 게임에서 «모든 피해» = `b.atk` |

### 3-2. 등급별 단계당 값 (`GRADE.mul` = 1.0 / 1.5 / 2.3 / 3.6 / 6.0 / 10.0 / 16.0 / 26.0)

| 탭 | 일반 | 고급 | 희귀 | 영웅 | 전설 | 신화 | 초월 | 불멸 |
|---|---|---|---|---|---|---|---|---|
| 무기 (공격력) | +2.0% | +3.0% | +4.6% | +7.2% | +12.0% | +20.0% | +32.0% | +52.0% |
| 방패 (최대 체력) | +2.0% | +3.0% | +4.6% | +7.2% | +12.0% | +20.0% | +32.0% | +52.0% |
| 목걸이 (치명타 피해) | +0.50% | +0.75% | +1.15% | +1.80% | +3.00% | +5.00% | +8.00% | +13.00% |
| 목걸이 (골드 획득) | +1.0% | +1.5% | +2.3% | +3.6% | +6.0% | +10.0% | +16.0% | +26.0% |
| 스킬 (스킬 피해) | +1.20% | +1.80% | +2.76% | +4.32% | +7.20% | +12.00% | — | — |
| 스킬 (공격 속도) | +0.40% | +0.60% | +0.92% | +1.44% | +2.40% | +4.00% | — | — |
| 펫 (동료 피해) | +2.0% | +3.0% | +4.6% | +7.2% | +12.0% | +20.0% | — | — |
| 펫 (골드 획득) | +1.0% | +1.5% | +2.3% | +3.6% | +6.0% | +10.0% | — | — |
| 유물 (모든 피해) | — | — | — | — | — | — | — | **+3.20 / +7.75 / +11.67%** (118 — 혼합 3세트) |

### 3-3. 합산 방식 — **축별로 전 세트를 더한 뒤 한 번만 곱한다**

⚠ 세트마다 곱하면(`∏(1 + v·n)`) 37세트가 전부 만단계일 때 **공격력이 무기 세트만으로 ×673** 이 된다.
그래서 `bonus()` 는 축별 누계를 먼저 세우고 마지막에 한 번만 곱한다:

```
cb[축] = Σ(세트별 단계당 값 × 받은 단계 수)
b.atk *= 1 + cb.atk   ·   b.cdmg += cb.cdmg   (cdmg 만 가산 축)
```

### 3-4. 만단계 상한 (전투력 회귀 검산 — 지시 ② 요구)

| 축 | 새 도감 상한 | 구 도감 상한 | 판정 |
|---|---|---|---|
| 공격력 | **×19.47** (무기 13.28 + 스킬 2.928 + **유물 2.262**) | ×40.97 (장비 1.725 × 스킬 2.957 × 펫 1.512 × 유물 5.313) | ✅ 낮아짐 |
| 최대 체력 | **×14.28** | ×4.83 | ⚠ 상승 — 체력은 DPS 축이 아니라 생존 축이라 허용 |
| 골드 획득 | **×10.08** (목걸이 6.64 + 펫 2.44) | ×1.68 | 골드는 강화 재화 회전율 축 |
| 공격 속도 | **×1.98** | ×1.08 | |
| 동료 피해 | **×5.88** | ×2.00 | |
| 치명타 피해 | **+3.32** (`stat.cdmg` 가산) | +0.30 | 유물 Lv 선형(+2.25)과 같은 자릿수 |

* **118 — 유물 축이 +75.6% → +226.2%(×3)** 로 올랐다. 세트가 3개가 됐으니 «세트당 보너스» 도 3벌이 되는
  구조상 필연이고, 공격력 총상한은 ×17.96 → **×19.47**(+8.4%) 로만 움직인다(유물 축은 전체의 12%).
  구 도감 상한 ×40.97 대비 여전히 절반이다 — 91 의 «곱연산 폭주» 선을 넘지 않는다.
* ⚠ 이 표의 나머지 행은 **91 당시(2026-08-25 오전)** 값이다. 이후 106(펫 8등급 36종)·86 이 종수를 늘려
  실측이 달라졌다(2026-08-26 실측: 최대 체력 ×14.28 · 골드 ×14.28 · 동료 피해 ×14.28 · 공격 속도 ×1.98 · 치명타 피해 +3.32).
  갱신은 그 작업들의 몫이라 여기서는 **유물·공격력 행만** 고쳤다.
* 이 상한은 **41세트 전부 10단계** = 178종 전부 Lv.10 이상 보유일 때다. 불멸 등급 3종은 Lv.100 신화 조합으로만
  나오므로 실제 도달은 최종 구간이다.
* 초기 구간 체감: 무기·일반 5종을 Lv.1 까지만 올리면 공격력 +2%. 지시 예시와 일치한다.

---

## 4. 21 팝업 UI (지시 ④)

| 요소 | 값 | 비고 |
|---|---|---|
| 상단 탭 | **6개** — 스킬 · 무기 · 방패 · 목걸이 · 펫 · 유물 | 기존 4개(무기·방어구·스킬·동료) 교체 |
| 탭 기하 | 폭 **147** · pitch **138** (5×138 + 147 = 837) | 컨테이너 840 · 겹침 6px 은 4탭 시절 그대로 |
| 탭 내부판 인셋 | 25.5 → **17.5** (내부 112) | «목걸이» 3자(31px × 3 ≈ 92) 수용 |
| 탭 라벨 | 37 → **31px** · 스트로크 10 → 8 · margin-top 17 → 21 | 칸 비율 147/214 |
| 알림점 | left 170 → **103** | 칸 우끝에서 20px, 4탭 시절과 같은 여백 |
| 블록(세트 카드) | 기존 `.clb` 기하 **불변** (818×370 · pitch 410) | 6회차까지 채점된 값 |
| 뱃지 Lv | 세트 **최저** Lv | |
| 머리 우측 | `단계 {받은}/{가능}` (`.clb-st`, 신설) | 강화 가능하면 초록(`#BAFD60`) |
| 카드 라벨 | `Lv. {현재}/{다음 단계 요구}` · 미달은 붉은색 | 미보유 아이콘은 82 규칙대로 흐리게(`grayscale`) |
| 효과 바 | 받은 단계 기준 **누적** 수치 | 다축(목걸이·스킬·펫)은 ` · ` 로 연결 |
| [강화] 버튼 | 받은 단계 < 가능 단계일 때만 활성 · 10단계면 `MAX` | |
| 카드 상자 | 폭 **791.5** · 가로 스크롤 | 91 당시 유물 10칸(1293px)용. **118 이후 유물도 3·4·3 이라 어느 탭도 넘치지 않는다** — 스크롤은 남겨 둔 안전판이고 칸 규격(121 + margin 8.25 · pitch 129.25)은 전 탭 공통, 유물 전용 특례 없음 |

> ⚠ **21 화면의 레이아웃 점수는 다시 받아야 한다.** 21 은 «①~④ 최저 7 · 5/10회차» 로 아직 미완이고,
> 이 작업이 **탭 4 → 6** 과 머리밴드 우측 요소를 바꿨다. 남은 5회차는 새 6탭 기준으로 돌 것.

---

## 5. 아트 필요 표

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 세트 카드 아이콘 | 66×70 | 세트 구성원 1종 | 아이템 이모지(`it.ic`) · 펫은 `COLL21_PETIC` |
| 탭 아이콘 | (없음) | 6탭 라벨 | 텍스트만 — 레퍼런스도 텍스트 |
| 단계 뱃지 | 101×108 | 세트 최저 Lv | CSS 작화(방패 + 골드 링), 유지 |

---

## 6. 세이브 마이그레이션

* `S.coll` 이 «카테고리 → 받은 티어 수»(`{skill,equip,pet,relic}`) 에서 **«세트 키 → 받은 단계 수»** 로 바뀐다.
* `load()` 가 **세트 키가 아닌 항목을 전부 버리고**(`COLL_SET[k]` 로 판별) `collLegacy` 를 세운다.
  구 키 `relic` 과 새 키 `relic:0` 이 다른 것도 이 필터가 보장한다 — 구 값이 단계로 새지 않는다.
* KEY(`idle_hunter_save_v4`)는 **올리지 않는다.** 다른 진행도를 전부 날리는 대신 도감만 초기화하고,
  로드 후 한 줄 안내(«도감이 바뀌었습니다»)를 1회 띄운다.
* 아이템 레벨은 그대로 남으므로 **바로 다시 강화**할 수 있다(검증 [4]: Lv.5 세트 → 즉시 5단계 가능).

### 6-1. 118 — 유물 3세트 전환 (2026-08-26)

* 새 키는 `relic:0` · `relic:1` · `relic:2` 이고 **`relic:0` 은 91 과 이름이 같다** → 구 세이브의
  «유물 세트 1 을 n단계 수령» 이 **새 «전사의 유물» 로 그대로 이월**된다(지시 ④). 새로 생긴
  `relic:1`·`relic:2` 는 세이브에 키가 없어 0단계에서 시작한다 — 별도 마이그레이션 코드가 필요 없다.
* «받은 단계 > 가능 단계» 가 되는 구간은 **생기지 않는다.** 세트가 10종 → 3종으로 좁아졌으니
  세트 최저 Lv 는 같거나 올라가고, 가능 단계도 같거나 올라간다.
* KEY 는 그대로 둔다(91 과 같은 이유). 검증: `node tools/verify118.js` [4] — 구 세이브를
  `S` 에 만들고 `save()` → `reload()`(LESSONS 91-2: `localStorage.setItem` 은 `beforeunload` 의 `save()` 가 덮는다).
