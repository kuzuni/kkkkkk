# 163 — 첫 접속 로딩 화면 측정표

> 레퍼런스 스크린샷이 **없는** 신설 화면이다(주인 지시만 있다). 따라서 이 표는 «레퍼런스에서 잰 값» 이
> 아니라 **구현이 실제로 쓰는 값과 그것이 나온 근거**다. 값을 바꿀 때는 근거 칸을 같이 고칠 것.
> 기준 프레임 1080×2280.

## 1. 기하 (프레임 px)

| 요소 | id | 값 | 근거 |
|---|---|---|---|
| 오버레이 | `#loading` | inset 0 · z-index 40 · 세로 중앙 정렬 flex column | 기존 구조 유지 |
| 배경 | `#loading` | `radial-gradient(120% 62% at 50% 78%, #181528 0%, #0e1020 45%, #0b0d18 100%)` | 1회차 균일 #0b0d18 은 페이드 뒤 게임 화면과 평균 휘도가 **6.4배** 튀었다(비평 ④) |
| 무대 | `#ldStage` | 1080 × **720** | 캐릭터 잉크(≈360h) + 발밑 여백 + 머리 위 여유 |
| 바닥선 | `#ldGr` | left 120 · right 120(폭 **840**) · bottom 70 · h **8** | 1회차 left/right 150 · h4 는 «경로의 43% 에 발밑 기준선이 없다»·«헤어라인» 지적 |
| 그림자(6회차) | `#ldSh` | 760 × 85 · bottom 39 · **중심 = 프레임 중앙 540** | ★ 6회차에 편심을 **캐릭터 쪽으로** 옮겼다(아래 «축» 절) — 그림자는 다시 중앙 정렬(`margin-left:-380 = -width/2`) |
| ~~그림자(3~5회차)~~ | `#ldSh` | **520 × 58** · bottom 52 · **중심이 프레임 중앙 −56px(=484)** | ★ `drawHeroTo` 의 `c0` 는 «칼·망토 포함 잉크» 중심을 맞춘다 — **발 스팬 중심은 그보다 56px 왼쪽**이다. 3회차 비평가 두 명이 독립적으로 55·56px 로 실측했고 «왼발 88~120px 이 그림자 밖» 이었다. 게이트 §12 가 다시 잰다 | **발 스팬(≈262px)보다 넓게.** 색은 검정이 아니라 «고인 빛» — 아래 §3 |
| 캐릭터 캔버스 | `#ldHero` | **720 × 624**(backing **= CSS 1:1**) · bottom 78 · **margin-left −306**(6회차 · = −360 + `LD_FDX` 54) · `image-rendering:pixelated` · **`transform-origin:42.5% 100%`**(= 발밑) | 논리 프레임 79×63 × 배율 **12** = 948×756 중 **실제 잉크 612×600** 이 들어갈 크기. ★ backing 과 CSS 가 다르면 **비정수 CSS 확대**가 걸려 정수 배율 규칙이 최종 출력에서 깨지고, 백버퍼가 모자라면 **머리 위가 잘린다** — 2회차의 진짜 결함이 이것이었다 |
| 캐릭터 잉크 | — | **달리기 최대 588×588 · 정지 최대 540×540**(idle f0 은 504×540) | ★ 7회차 정정 — 5~6회차의 «run 612×600 · idle 528×504» 는 **잉크가 아니라 프레임 박스**(`fr[2]×fr[3]`) 값이었다. 6회차 비평 I·J 가 각각 «정지 세로가 36px 틀렸다» · «종횡비 11% 오차» 로 잡아 줬다. 이제 `probe163b.py --gate` 가 **알파 bbox** 를 직접 재서 이 줄과 대조한다 |
| 문구 | `#ldTx` | font 36 / 800 · #9db0d8 · 1.4s 호흡(.66↔1) | 1회차 28px 은 «프레임 세로의 1.05%» 지적 |
| 진행바 | `#ldBar` | **400** × 18 · radius 9 · 트랙 **#5f6a9c** + inset 2px **#8a94c6** | ★ 두 비평가의 요구가 반대였다 — «트랙이 **배경** 대비 3:1»(E) vs «채움이 **트랙** 대비 3:1»(F). 어두운 트랙 + 중간 파랑 채움으로는 동시에 못 만족한다(1.3:1 로 붙는다) → **3단 명도 사다리**: 배경 → 트랙 → 채움 | ★ **캐릭터 잉크(352)보다 좁아야 한다** — 1회차 520 은 «히어로 화면인데 진행바가 제일 크다» |
| 진행바 채움 | `#ldBar>i` | width % · `linear-gradient(90deg,#a9c6ff,#e6efff)` · transition .12s | 도착한 아틀라스 수 / 총수 |
| 진행바 스윕 | `#ldBar::after` | **균일 사선 줄무늬** 28px 주기 · .7s linear infinite · 알파 .13 | 큰 파일 하나를 받는 동안 채움이 수백 ms 안 움직인다(실측 745ms 정지) |
| 진행 표기 | `#ldNum` | font 26 / 700 · #7286ae · min-height 30 | min-height 가 없으면 표기가 채워지는 순간 위가 밀려 «덜컹» 한다 |

## 2. 시간 (ms)

| 상수 | 값 | 근거 |
|---|---|---|
| `LD_RUN` | **640** | 등장 길이. 1회차 300 은 «횡방향 등장 인지 하한 200ms 미달» · «이동의 93%가 앞 155ms 에 몰림» |
| `LD_FOOT` · `LD_RUNMS` | **7.943 src px/frame(§2b 표의 평균 — 6회차부터 파생값)** · **59(역산 · 평균 칸 95.32px)** | ★ **간격은 스프라이트가 정한다.** 접지발이 프레임당 −7.67 src px 로 그려져 있으므로 «한 프레임에 몸이 7.67×배율 만큼» 나가야 안 미끄러진다 → `RUNMS = LD_FOOT·LD_SC / 등속속도` = 60.6 → 61. 40ms 는 요구치의 66% 라 프레임당 31px, 등속 480ms 누적 **282px(등장의 33%)** 이 미끄럼이었다. «스트라이드 2회» 는 이 이동 거리에서 발 속도와 **양립 불가**(2바퀴엔 1,472px 필요) |
| `LD_IDMS` | 125 | 8fps — 전투 `setAnim(player,'knight','idle',8)` 과 같은 속도 |
| `LD_HOLD` | 90 | 착지 뒤 여운 |
| `LD_SQ` · `LD_SQA` | **120** · ±10% | 착지 스쿼시. 2회차 ±6% 는 실측 −1.8% 로 «160ms 를 쓰고 얻은 임팩트가 1.8%»(비평 D) |
| `LD_MIN` | 400 | 전부 캐시된 재방문에서도 한 박자는 보이게 |
| `LD_GRACE` | 90 | knight 가 늦게 와도 캐릭터를 최소 이만큼은(«연출 0프레임» 방지) |
| `LD_FADE` | **150** | 페이드아웃. **CSS `#loading{transition:opacity .11s}` 와 한 벌** — 게이트 §2 가 두 값을 대조한다 |
| `LD_HARD` | **530** | ★ «이 시각을 넘겨서까지 등장을 기다리지 않는다». 유도: load 이벤트 **550**(아틀라스까지 받은 시점) + 저장소 게이트 최단 대기 **300** = 가장 이른 캡처 **850**. 거기서 페이드 150 과 타이머 굶주림 여유를 뺀 값. 굶주림은 실행마다 **20~115ms** 흔들려서(실측) 여유를 넉넉히 잡아야 «뜨고 지는 FAIL» 이 안 된다 |

**측정된 시간축**(`node tools/probe163.js`, file:// · 전부 로컬 = «재방문» 경로):
`#loading` DOM 90 → knight 도착·등장 시작 ≈**380~450** → 부팅(.thru) ≈**420~495** → 페이드 시작 ≈**640~680** → 안 보임 ≈**790~830**.
이 경로에서는 등장 640ms 가 `LD_HARD` 에 **잘린다** — 의도된 동작이다(위 `LD_HARD` 칸).
주인 지시가 가리키는 **첫 접속**(1.8MB 를 네트워크로 받는 경우)에는 부팅이 훨씬 늦어 등장이 통째로 보인다.
캐릭터가 400ms 에야 그려지는 이유는 **본 인라인 스크립트가 672KB** 라 파싱에 ~350ms 가 걸리고
그동안 메인 스레드가 막혀 이미지 `onload` 조차 못 돌기 때문이다. 연출 모듈을 `atlas-data.js` 바로 뒤로
옮겨 **로딩 시작**은 앞당겼지만(그래서 부팅도 빨라졌다), 자바스크립트 실행 자체는 파싱을 못 앞지른다.

## 2b. 케이던스 표 (6회차 — 균일 자 폐기)

`python3 tools/probe163b.py` 가 아틀라스 `knight.png` 에서 **run 프레임별 접지발**(프레임 박스 바닥 2px 밴드
안의 잉크) x 중심을 잰 값이다. 게이트 `python3 tools/probe163b.py --gate` 가 index.html 의 `LD_FEET` 와 대조한다.

| 프레임 | f0 | f1 | f2 | f3 | f4 | f5 | f6 | f7 |
|---|---|---|---|---|---|---|---|---|
| 접지발 x (src px) | 35.33 | 25.04 | 17.79 | 11.50 | 35.33 | 25.04 | 18.00 | 11.50 |
| 다음 프레임까지 요구 이동 | **10.29** | 7.26 | 6.29 | (체공) | **10.29** | 7.04 | 6.50 | (체공) |
| ×배율 12 = 화면 px | 123.5 | 87.1 | 75.4 | 95.3 | 123.5 | 84.5 | 78.0 | 95.3 |

- **최대/최소 1.64:1** — 5회차까지의 «한 칸 = 92.04px» 균일 자로는 f0→f1 에서 요구의 **−25%** 가 남았다
  (5회차 비평 H: «f0→f1 126px vs f1→f2 90px = 1.40:1 인데 자는 한 값»).
- **체공 칸(f3·f7)만 자유롭다** — 발이 바뀌는 칸이라 접지가 정하는 값이 없다. 접지 칸들의 평균(7.94 src px)을
  넣는다. 그래야 주기 평균이 접지 요구 평균과 같아져 «주기 전체로는 스프라이트가 요구하는 속도» 가 유지된다.
- 한 주기 = **762.7px**(8칸), 등장 이동 904px = 1.19주기.
- 아치는 이 표의 **체공 프레임에서만** 뜬다(옛 «주기의 뒤 25%» 비율 폐기 — 칸 길이가 다르면 비율과 칸이 어긋난다).

## 2c. 축 (6회차 — 발이 프레임 중앙)

| 값 | 실측 | 근거 |
|---|---|---|
| idle f0 잉크 | x 6..47 · 중심 26.5 (src px) | `probe163b.py` |
| idle f0 발 스팬 | x 6..38 · 중심 22.0 · 폭 33 (=396px @12) | 같은 도구. 그림자 «어두운 코어 418px» 의 근거 |
| `LD_FDX` | **54px** = (26.5 − 22.0) × 12 | `drawHeroTo` 의 c0 는 **잉크** 중심을 맞추므로 발은 −54px 에 선다 |

5회차 인계 ②(«발 축이 다른 요소 축과 −54px — 어느 쪽을 축으로 삼을지는 설계 판단»)의 답: **발을 축으로 삼는다.**
바닥선·그림자·진행바·문구가 전부 540 을 쓰는 화면에서 축이 둘이면 안 되고, «달려와 서는» 장면의 축은
캐릭터가 **서는 자리**다. 그래서 캔버스를 +54px 옮기고(margin-left −306) 그림자는 중앙으로 되돌렸다.
잉크(칼 포함)는 그만큼 오른쪽으로 치우치는데, 오른쪽으로 달려온 실루엣에서는 그것이 자연스럽다.
`transform-origin` 도 42.5%(=발밑)로 옮겼다 — 50% 로 두면 착지 스쿼시 ±10% 가 발을 5px 흔든다.

## 2d. 체공·발 높이 (8회차)

| 값 | 어디서 오나 | 근거 |
|---|---|---|
| `LD_AIRMS` **46.7ms** | 등속 속도에서 역산(`LD_STEPS[체공] / 등속속도`) | 7회차 비평 K·L: 아치를 «칸 안의 이동 거리» 로 돌리면 감속 구간에 걸친 마지막 도약만 체공 **113.4ms**(앞 도약의 2.4배)가 되어 «내려오다 멈춘 뒤 스쿼시» 가 된다. 체공은 시간이다 |
| 체공 초과 처리 | τ ≥ 1 이면 **다음 접지 프레임**으로 | 감속 중에 도약 자세로 얼어 있지 않게(K «마지막 실칸 117.3ms = 8.5fps») |
| `LD_LIFT` [0,0,0,1,0,0,0,1] | 아틀라스 실측(프레임 박스 바닥 ↔ 잉크 바닥, src px) | 체공 프레임은 아트가 이미 1px(=12px) 들어 놨는데 아치는 칸 경계에서 0 이라 **발이 12~13px 순간이동**했다(K·L). 아치가 이 값을 상쇄한다 |
| `ldTimeAt(d)` | `ldAt` 의 **해석적 역함수**(조각별) | 체공 진입 시각을 알아야 τ 를 잰다. 표본으로 재면 2회차의 322px 오차가 재발한다 |

## 3. 궤적

- 시작 오프셋 `LD_X0 = **−904**`(6회차) — 정지 자세의 잉크 우변이 x=**882**(캔버스 좌변 **234** + 720/2 + 앵커 −324 + 51×12)
  이므로 −904 면 우변 −22, **딱 프레임 밖**이다. 5회차의 −850 은 캔버스 좌변이 180 이던 시절 값이고,
  축 보정(`LD_FDX` 54)으로 캔버스가 오른쪽으로 옮겨간 만큼 시작점도 같이 밀렸다(여백 −22 는 그대로).
  1·2회차 값(−560 / −450)은 **게이트가 틀린 값을 줘서** 나온 숫자였다 — §4 가 `display:none` 인
  오버레이에서 `offsetLeft`(실제 180 → 0)를 읽었고 CSS 확대(1.5×)도 안 쳤다. 비평가가 픽셀로 잡았다.
  게이트는 이제 오버레이를 **보이는 상태로 되돌려 놓고** `getBoundingClientRect()` 로 잰다.
- 이징 — **등속 + 감속**. 순수 함수 `LD.at(t)`:
  시간의 앞 `LD_CRZ = .75` 를 등속으로 `LD_CRZD` 만큼 가고, 남은 25% 시간에 남은 거리를 2차 감속으로.
  ★ `LD_CRZD` 는 **손으로 적지 않는다** — `.80` 으로 적었더니 이음매에서 속도가 1.067 → 1.6(**+50%**)로
  튀어 감속해야 할 자리에서 가속했다(게이트가 잡았다). 속도 연속 조건을 풀면
  `LD_CRZD = 2·LD_CRZ / (1 + LD_CRZ)` = 0.857.
  2회차의 `easeOutCubic` 은 앞 21% 시간에 거리의 50%를 써서 뒤가 **트레드밀**이었다(비평 C·D 독립 확인).
  등속 구간의 프레임당(40ms) 이동은 **57px = 잉크폭 612px 의 9.3%** 로 고르다.
- 상하 아치 `LD_ARC = 20px` — **주기뿐 아니라 «위상» 까지 스프라이트에 맞춘다.**
  아틀라스 바닥행 실측(3회차 비평 E): f0 발끝 2px · f1 발바닥 전체 9px · f2 밀어내기 4px ·
  **f3 = 0px(체공)** — 4프레임 중 **3프레임이 접지**다. `|sin|` 을 그대로 쓰면 정점이 f2(아직 닿아
  있는 프레임)에 얹혀 «발이 바닥 2px 이내인 시간이 주기의 3.2%» 가 된다.
  → `LD_AIR = .75` 로 **접지 3프레임은 arc=0 으로 붙이고 체공 1프레임에만** 아치를 준다.
  감속 구간(뒤 25%)도 아치 0 = 발이 붙은 채로 선다.
  ★ **6회차 정정** — «주기의 뒤 25%» 라는 **비율**은 칸 길이가 균일할 때만 체공 칸과 겹친다. 이제 아치는
  §2b 표의 **체공 프레임(f3·f7)에서만** 뜨고, `LD_AIR` 은 손으로 적은 상수가 아니라 «접지 프레임 수 / 전체» 파생값이다.
- 그림자는 공중에서 작아지고(−18%) 옅어진다(−45%), 착지에서 가로로 퍼진다(+30%).

## 4. «아트 필요» 표 (GPT 발주서 행)

| 요소 | w×h | 용도 | 현재 대체물 |
|---|---|---|---|
| 로딩 바닥선 | 840 × 8 | 캐릭터가 달려 오는 지평선 | CSS 좌우 페이드 그라디언트(#607CC4 .62) |
| 발밑 그림자 | 420 × 54 | 접지 | CSS radial **어두운 코어(rgba(2,3,8,.88)) + 밝은 테(#84A6EC .30)**. ★ 검정만 쓰면 배경(#0b0d18)에 묻혀 명도비 1.045:1 로 **안 보이고**(1회차), 빛만 쓰면 바닥보다 **+37% 밝아 «발광» 으로 읽혀 무게가 없다**(2회차). 둘을 겹쳐야 접지가 된다 |
| 로딩 배경 | 1080 × 2280 | 무대 | CSS radial-gradient(#181528 → #0b0d18). 게임 화면(따뜻한 보라·금)과의 휘도 점프를 줄이려 아래를 데웠다 |
| 캐릭터 | 612 × 600 | 주인공 등장 | **대체물 아님** — 전투와 같은 `knight` 아틀라스를 같은 앵커 산식으로 그린다(게이트 §10 이 `drawHeroTo` 와 대조) |
