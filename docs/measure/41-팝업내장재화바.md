# 41 팝업 내장 재화 바 측정표 (03 던전 · 14 보물상자 공통)

- 측정 일자: **2026-08-24** (sess-2113-22980, 워커 A)
- 레퍼런스: `docs/ref/03-던전-팝업.jpg` · `docs/ref/14-유물-팝업-보물상자-탭.jpg` (둘 다 1080×2340 JPEG)
- 측정 방식: PIL 원본 픽셀 스캔(스캔라인 색 전이점 + 색 마스크 bbox + 열/행 잉크 프로파일).
  표기 좌표는 **원본 1080px 절대 좌표**. `프레임 y` 열은 `ref y − 84` (02 비고 1의 상단 고정 요소 변환).
- `(추정)` = 픽셀에서 직접 확정 못 하고 대칭·기하로 역산한 값.

---

## 0. 이 바가 «메인 HUD 재사용» 이 아니라는 근거

| 항목 | 02 메인 HUD (ref) | 03·14 팝업 재화 바 (ref) |
|---|---|---|
| 좌측 프로필 플레이트(초상화·닉네임·칭호·🔥전투력) | **있음** (x0~468) | **없음** — x0~496 전 구간이 배경 단색 (전이점 0개) |
| 본체 톤 | **단색** `#42362A` (y95~184 전 구간) | **하드 2단** — `#42362A`(y93~143) + `#3A2E22`(y147~183) |
| 하단 검정 테두리 | y185~188 (4px) | y186~191 (**6px**) |
| 재화 알약 | 211×48 (프레임 x843/1103 상당, gap 49) | **254×49**, x503 / x803 (gap 46), 아이콘 57×57 |
| 알약 배경 | `#221B13` 단색 | `#231A13` → `#1D160E` 2단 |

→ 폭·높이·개수·색·톤 분할이 전부 다르다. **팝업 자체 요소로 신설**하는 것이 맞다 (UI-REFERENCE 3·14번 «설계 확정(2026-08-25, 작업 41)»).

**03 과 14 의 재화 바는 서로 픽셀 동일하다** — 숫자 문자열과, 14 에만 있는 좌측 요소(§5)를 빼면
y80~200 전 영역에서 차이 픽셀 0. 그래서 **공용 컴포넌트 1개**로 만들고 두 페이지가 같이 쓴다.

---

## 1. 바 본체

| 항목 | ref y | 프레임 y | 색 |
|---|---|---|---|
| 상단 검정 테두리 | 87 ~ 92 | 3 ~ 8 | `(4,0,0)` → `(0,0,0)` → `(29,24,18)` |
| 본체 상단 램프 | 93 ~ 95 | 9 ~ 11 | `(55,46,37)` → `(66,54,42)` |
| **본체 밝은 단** | 96 ~ 143 | 12 ~ 59 | **`#42362A` (66,54,42)** |
| 단 전환 램프 | 144 ~ 146 | 60 ~ 62 | `(61,48,39)` → `(57,45,33)` |
| **본체 어두운 단** | 147 ~ 183 | 63 ~ 99 | **`#3A2E22` (58,46,34)** |
| 하단 램프 | 184 ~ 185 | 100 ~ 101 | `(52,42,32)` → `(40,31,24)` |
| **하단 검정 테두리** | 186 ~ 191 | 102 ~ 107 | `(14,10,7)` → `(0,0,0)` → `(8,7,5)` |
| 아래(페이지 배경) 시작 | 192 | 108 | `#26211B`(03) / `#27272F`(14) |

- 좌우 전폭(x 0~1079). 좌측 x0~496 은 **요소 0개**(y93~185 전 행 전이점 없음).
- 프레임 기준 **0 ~ 107 (108px)** 을 덮는다. `#top`(0~104)보다 4px 아래까지 내려오므로
  페이지(top:104) 상단 4px 을 이 바가 덮는 구조가 맞다.

---

## 2. 재화 알약 2개

| 항목 | 골드 | 다이아(보석) | 비고 |
|---|---|---|---|
| x (좌단) | **503** (추정) | **803** (추정) | 좌측 캡을 아이콘이 완전히 덮어 직접 측정 불가 → §2.1 로 역산 |
| x (우단) | **757** | **1057** | AA 1px(758/1058) |
| width | **254** | **254** | |
| y (top) | 115 | 115 | 프레임 **31** |
| y (bottom) | 163 | 163 | 프레임 **79** |
| height | **49** | **49** | x=900·1000 열에서 정확히 49행 |
| border-radius | **24.5** (= h/2, 완전 캡슐) | 동일 | 우측 캡 원호로 확인(x750에서 y120~168, x755에서 126~153) |
| 테두리 | **없음** (검정 아웃라인 0px) | 동일 | y115 에서 배경 → `(36,27,18)` 로 바로 전이 |
| 배경 상단 | `#231A13` (35,26,19) | 동일 | y115~149 |
| 배경 하단 | `#1D160E` (29,22,14) | 동일 | y150~162, 전환은 알약 로컬 **35/49 (71%)** |
| 두 알약 간격 | 757 → 803 = **46** | | 좌단 간 피치 **300** |
| 우측 여백 | 1057 → 1079 = **22** | | |

### 2.0 정오표 (2026-08-30, 작업 534 — 유물 페이지는 알약이 **3개**다)

이 표는 **2026-08-24 기록**이라 «03·14 두 페이지가 픽셀 동일 · 알약 2개» 를 전제한다.
그 뒤 지시 둘이 **유물 페이지만** 바꿨다(03 던전·10 상점은 이 표 그대로):

| 지시 | 무엇 | 현재 값 |
|---|---|---|
| **89** (유물 시스템 전면 교체) | «유물조각» 알약 신설 ⇒ 2개 → **3개** | `.pcb-r{left:805px}` |
| **429** (좌상단 [?] 도움말) | 세 알약을 왼쪽으로 당김 | `#relw .pcb-g{left:205px}` · `#relw .pcb-d{left:505px}` |

**칸 규격은 여전히 공용**이다 — `254×49` · `top:31` · 좌단 피치 **300**(205/505/805).
자(`verify41.js` [4])는 2026-08-30 부터 이 값을 **페이지별 표(`PILLS`)** 로 들고 있다.
그 전까지는 두 페이지 모두에 «505/805 2개» 를 기대해 유물 페이지에서 빨갛게 나왔다
(다만 그 자리는 55행 `openRelicPage` 즉사 뒤라 **한 번도 실행되지 못했다**). 상세 `docs/review/534-verify41게이트부패.md`.

### 2.1 좌단 역산 근거

아이콘 bbox 중심(골드 528 / 다이아 828.5)이 **알약 좌측 캡의 원 중심**(= 좌단 + 24.5)과 일치한다고 두면
좌단 = 503.5 / 804 이고, 이때 폭이 각각 253.5 / 253 으로 **우단 실측(757/1057)과 254 로 수렴**한다.
두 알약 독립 역산이 같은 폭을 주므로 채택. (measure/03 §3-4 의 «캡 보정» 과 같은 방법)

---

## 3. 아이콘 (알약 좌측 캡 위에 겹침)

> ### ⚑ §3 정오표 (2026-08-28, 작업 340 — `node tools/verify340.js` [1] · `node tools/probe340.js`)
>
> 아래 표의 **«bbox(검정 아웃라인 포함) 57×57» 은 아웃라인 포함 실루엣이 아니다.**
> ref 를 색 마스크와 배경 마스크로 각각 다시 재면
>
> | | 코인 | 젬 |
> |---|---|---|
> | **실루엣**(배경이 아닌 화소 전체 = 검정 아웃라인 포함) | **64 × 65** | **64 × 65** |
> | **색 잉크**(노란 원판 / 시안 몸통) | **57 × 57** | **56 × 57** |
>
> 즉 §3 의 57 은 **색 잉크**이고, 아웃라인 몫 7~8px 이 빠져 있었다.
> A3 가 HUD 에서 9회차에 뒤집은 값(«ref 외곽 64×65»)과 **같다** — 두 바의 아이콘은 ref 에서 같은 크기다.
>
> 이 오독 때문에 41 은 상자를 57 로 만들었고, 자산(`cur-*.svg`)이 viewBox 를 덜 채우는 몫까지 겹쳐
> 실제 잉크가 **코인 −14.0% · 젬 −19.6%** 였다(72 비평가 6명이 13·15·16·17회차에 네 번 지적한 그 줄).
> 340 이 HUD 와 같은 보정(코인 이미지 65.3 · 젬 이미지 `scaleX(1.16)`)을 걸어 ±2% 로 붙였다.
>
> **⚑ 재정오 (2026-09-01, 작업 671 — `node tools/probe671.js` · `node tools/verify671.js`)**
> 위 «젬 `scaleX(1.16)`» 은 356 이 폐기했고(비균등 확대 금지), 그 뒤 젬 상자만 **59.06** 으로 남아
> **같은 프레임 안 코인 65.3 ↔ 젬 59.06 = 비 1.106** 이 411·356 눈금(≤1.05)을 넘고 있었다.
> 뿌리는 상자가 아니라 **아트의 «색 잉크 ÷ 실루엣» 이 축마다 달랐던 것**이다 —
> `cur-dia.svg` 가로 **.848** · 세로 **.973**(비대칭 1.147)인데 **ref 젬은 .875/.877 등방**이다
> (ⓐ 뾰족한 좌우 꼭짓점에서 straddle stroke 가 `2/sin(51°)=2.6` 씩 파고들고 ⓑ 면 분할선이
> 위·아래 꼭짓점에서 검정 테 위로 삐져나온다).
> 671 이 테 규격을 **.875 등방**으로 다시 그린 뒤 **코인·젬 상자가 한 값 65.3** 으로 모였다:
>
> | | 상자 | 그려진 색 잉크 | 그려진 실루엣 | ref |
> |---|---|---|---|---|
> | 코인 | **65.3** | 56×58 | 66×66 | 색 57×57 · 실루엣 64×65 |
> | 젬 | **65.3** | **55×58** (전 49×58) | **65×66** (전 59×60) | 색 56×57 · 실루엣 64×65 |
>
> **상자 `.pcb-p` 57×57 은 여전히 그대로다** — 알약·숫자 그릇(235)이 그 위에 서 있고,
> 키운 것은 그 안의 이미지뿐이다(`.pcb-g`/`.pcb-d` override).
> **상자 57×57 은 그대로다** — 알약·숫자 그릇(235)이 그 위에 서 있다. 상세 `docs/review/337-*.md`.

| 항목 | 골드 코인 | 다이아(보석) |
|---|---|---|
| bbox (검정 아웃라인 포함) | x **500~556**, y **111~167** | x **801~856**, y **111~167** |
| 크기 | **57 × 57** | **56 × 57** |
| 중심 | (528, 139) | (828.5, 139) |
| 프레임 y | **27 ~ 83** | 동일 |
| 알약 대비 | 위·아래로 각 **4px** 씩 삐져나옴 (알약 115~163) | 동일 |
| 검정 아웃라인 | 2~3px (x497~499 / x557~558 이 `(1,0,0)`) | 동일 |
| 주요 색 | 링 `(255,238,34)` / 면 `(197,124,0)~(227,151,16)` / 하이라이트 `(255,255,245)` | 시안 `(0,172,238)` / 밝은 면 `(98,250,255)` / 하이라이트 `(187,255,254)` |

---

## 4. 숫자

| 항목 | 골드 `40.77A` | 다이아 `1,300` |
|---|---|---|
| 흰 잉크 bbox | x **593~712** (w 120), y **127~151** | x **903~994** (w 92), y **127~154** |
| 숫자 잉크 높이 | **23** (y127~149. 150~151 은 AA) | **23** (152~154 는 쉼표 디센더) |
| 잉크 세로 중심 | 138 (알약 중심 139 → **Δ1, 세로 중앙**) | 138 |
| 잉크 가로 중심 | 652.5 | 948.5 |
| 글리프 열 런 | 593–611 / 617–636 / 642–646 / 653–668 / 674–689 / 691–712 | 903–910 / 917–922 / 928–944 / 950–969 / 976–994 |
| 색 | 흰색 + 검정 외곽선 (외곽선 한쪽 **4~5px**) | 동일 |

**정렬**: 아이콘 우단~알약 우단 구간의 **가운데 정렬**이 가장 잘 맞는다
(골드 박스 556~757 중심 656.5 vs 잉크 중심 652.5 = Δ4 / 다이아 856~1057 중심 956.5 vs 948.5 = Δ8).
좌측 정렬(Δ37 vs Δ47)·우측 정렬(Δ45 vs Δ63)은 두 알약이 서로 안 맞는다.
→ 구현은 `padding-left:53px` + `justify-content:center`.

**font-size**: 28 교훈 1(숫자 잉크비 **0.69**) 적용 → `23 / 0.69 ≈ 33.3px`.
폭은 그 상태에서 실측해 `<b>` 의 `scaleX()` 로 맞춘다(우리 서체는 숫자 폭이 모자란다 — 05 비고).

---

## 5. 14 에만 있는 좌측 요소 (**이 작업 범위 밖**)

`14-유물-팝업-보물상자-탭.jpg` 의 바 좌측에 03 에는 없는 요소가 하나 있다.

| 항목 | 값 |
|---|---|
| bbox | x **14~83**, y **102~172** (70 × 71) |
| 색 | 크림/살구 `(248,208,157)` · `(233,192,146)` 계열, 3분절 실루엣 |
| 정체 | 미확정 (뒤로가기/홈 계열 아이콘으로 추정) |

**재화 바가 아니므로 41 에서 구현하지 않는다.** 14 를 다시 잡는 세션(또는 별도 작업 단위)이 처리할 것.

---

## 6. 아트 필요 표 (GPT 발주서용)

| 요소 | w × h | 용도 | 현재 대체물 |
|---|---|---|---|
| 골드 코인 아이콘 | 색 잉크 **57 × 57** · 실루엣 **64 × 65** | 팝업 재화 바 골드 알약 좌측 캡 | `assets/ui/cur-gold.svg`(125) — 링 `stroke-width:4` 가 두꺼워 노란 원판이 덜 차서 **이미지 상자를 65.3 으로** 키워 쓴다(340). 자산이 고쳐지면 그 override 를 지운다 |
| 다이아(보석) 아이콘 | 색 잉크 **56 × 57** · 실루엣 **64 × 65** | 팝업 재화 바 다이아 알약 좌측 캡 | `assets/ui/cur-dia.svg`(125) — 마름모가 viewBox 64 안에서 덜 차 **이미지에 `scaleX(1.16)`**(340). ⚠ 그 대가로 실루엣 세로가 ref −5px 다. 마름모가 viewBox 를 꽉 채우면 override 2개(HUD·`.pcb`)가 함께 내려가고 세로도 붙는다 |
| 14 좌측 아이콘(§5) | **70 × 71** | 14 바 좌측 (범위 밖) | 없음(미구현) |

---

## 정오표 — 작업 585 «`.pcb` 는 안 키운다» 는 판단과 그 근거 (2026-08-31, sess-0721-11840 워커 A)

주인 원문: «던전행에서도 화폐들 크기좀 키우기». 585 등재문은 처방으로
«`.pcb` 는 세 화면 공용이니 **세 화면을 같이 올리거나**, 던전만 올려야 할 근거를 측정표에 적는다»
를 적어 뒀다. **재는 순간 그 전제가 뒤집혀서, 여기 적는 것은 «안 올린 근거» 다.**

### 실측 (`node tools/probe585.js` · 눈금 = √잉크면적 «시각 덩치»)

| 자리 | 그려진 잉크 | 덩치 | 기준선(23 훈련 팝업 골드 **52.97**) 대비 |
|---|---|---|---|
| `.pcb-g` 골드 | 65.30 × 65.30 | **65.30** | **+23.3%** |
| `.pcb-d` 다이아 | 59.06 × 59.06 | **59.06** | **+11.5%** |
| (참고) 03 카드 입장권 — 수리 전 | 46.51 × 26.36 | 35.01 | −33.9% |
| (참고) 04 세부 입장권 — 수리 전 | 45.40 × 34.18 (∠18°) | 30.35 | −42.7% |

주인이 «크게» 의 기준으로 지목한 것은 훈련 팝업이다. **`.pcb` 의 두 아이콘은 그 기준선보다
이미 11~23% 크고, 이 게임에서 가장 큰 화폐 아이콘이다** — 340 이 HUD 값(65.3 / 63)으로 맞춰 둔 자리다.
⇒ 주인이 본 «작다» 는 `.pcb` 가 아니라 **입장권·보상 알약**이었고, 585 는 그 세 자리만 고쳤다.

**같이 올렸으면 치렀을 값**: `.pcb` 는 이 문서 서두대로 **03 던전 · 89 유물 · 10 상점 공용**이라
한 줄이 세 화면을 움직인다. 340·A3 가 세운 ref 정합(HUD 와 «같은 아트 · 같은 목표 잉크 = 같은 손잡이»)이
세 화면에서 한꺼번에 깨졌을 것이고, 10 상점 쪽은 543 의 «13 재화 탭» 예외 가드
(`#app:not(:has(#shopw.on .shp-list.coin))`)와 겹치는 자리다.

**불변을 자가 지킨다**: `verify585` §5 가 «03 `.pcb` 골드 65.3 · 다이아 59.06» 과
«03 던전 ↔ 10 상점이 **같은 값**»(공용 부품이 안 갈렸다)을 못박고, §7 이 543 가드를 두 상태로 잰다.

⚠ **이 바를 재는 다음 세션에게** — `.pcb-p` 는 재화가 오를 때마다 58 «펀치» 연출(`.fx-punch`)이
붙어 **조상에 한시적 배율**을 얹는다. 그 프레임에 재면 골드가 65.3 이 아니라 66.85 · 70.01 로 읽혀
«`.pcb` 가 커졌다» 는 **거짓 신호**가 난다. 게임이 자동 전투로 골드를 계속 벌기 때문에
«연출이 걷힐 때까지 기다린다» 로는 못 잡는다(기다림이 잡는 것은 순간의 틈뿐이다) —
재기 전에 `.pcb-p.fx-punch{animation:none!important;transform:none!important}` 로 **무력화**해야 한다
(정상 상태의 `.pcb-p` 에는 transform 이 없으므로 같은 값이다). `verify585` 의 `open()` 이 그렇게 한다.
