# A5 공용 팝업(모달) 껍데기 측정표

> 출처: `docs/ref/22-퀘스트-팝업.jpg` (1080×2340). 교차 측정: `07-스킬-팝업.jpg` · `06-장비-팝업.jpg` · `03-던전-팝업.jpg` · `14-유물-팝업-보물상자-탭.jpg`
> 가로 수치는 1:1. 세로는 아래 «세로 환산» 규칙 참고.

## ⚠ 먼저 — 이 게임의 팝업 껍데기는 **2종류**다

측정해 보니 파일명에 "팝업"이 붙었다고 전부 모달이 아니다.

| 종류 | 해당 레퍼런스 | 형태 |
|---|---|---|
| **(가) 가운데 다이얼로그** | **22 퀘스트** | 폭 897, 좌우 여백 91, 위아래 여백 있음, 별무늬 베이지, radius 42 |
| (나) 바닥 시트(full-bleed) | 07 스킬, 06 장비 | 폭 **1080**(꽉 참), 하단 네비바에 **바닥 정렬**, 위쪽만 가변, radius 위 28 / 아래 0, 무늬 **없음** |
| (다) 모달 아님 (전체화면 페이지) | 03 던전, 14 유물 | 딤·헤더·테두리 전부 없음. 배경 `#26211B` / `#272731` 불투명 |

**A5 가 구현하는 것은 (가) 가운데 다이얼로그다.** 근거: `showModal()` 을 쓰는 화면(퀘스트·출석·룰렛·우편·던전목록·프로필·승급전·소환결과·아이템상세)은
전부 짧은 대화상자 성격이고, 22 퀘스트는 그중 하나가 그대로 레퍼런스로 존재한다.
(나) 바닥 시트는 우리 구조에서 **모달이 아니라 `#panel`(탭 콘텐츠)** 에 해당하므로 A5 범위 밖이다.

## 세로 환산 규칙

레퍼런스 프레임은 2340, 기준 프레임은 1920 이다. 모달은 상단 HUD·탭바 같은 **고정 밴드가 아니라
오버레이**라서 y 좌표를 그대로 옮길 수 없다 (레퍼런스에서 모달 아래 여백 379px 은 높이 190px 짜리
하단 네비바 위이고, 우리 프레임의 같은 자리는 높이 844px 짜리 `#panel` 이다).

따라서 **높이·폭·두께·radius·색 같은 형상값은 1:1 로 쓰고, 세로 위치만 «중앙 + 아래로 42px»** 로 옮긴다.
42px 은 레퍼런스 실측 편차(모달 중심 1212.5 vs 화면 중심 1170)를 그대로 가져온 값이다.
`docs/UI-REFERENCE.md` 의 «모달은 세로 중앙 정렬 + max-height + 내부 스크롤» 규칙과도 일치한다.

## 딤 오버레이

| 항목 | 값 |
| --- | --- |
| 색 | `#000000` |
| 불투명도 | **65%** (±4) → `rgba(0,0,0,.65)` |
| 범위 | 화면 전체 (상단 HUD 포함) |
| 뒤 전투화면 | 비침 (전투 배경 블루가 `#164760` 으로 식별됨) |
| 모달 둘레 드롭섀도 | 약 **24px** 소프트 |

> 참고: (나) 바닥 시트(07)의 딤은 **28%** 로 훨씬 옅다. 껍데기가 다르면 딤도 다르다.

## 모달 박스

| 항목 | 값 |
| --- | --- |
| left x / right x | **91** / **988** |
| **width** | **897** (좌 91 · 우 92 — 좌우 대칭) |
| 가로 중심 | 539.5 (화면 중심 540) → 정중앙 |
| top y / bottom y (ref) | 464 / 1961 |
| **height (ref)** | **1497** → `max-height` 로 사용 |
| 세로 중심 (ref) | 1212.5 vs 화면 중심 1170 → **아래로 +42.5px** |

## 외곽선 · 모서리

| 항목 | 값 |
| --- | --- |
| 바깥 겹 | **7px** `#000000` |
| 안쪽 갈색 프레임 | **11px** — 바깥 6px `#564438` + 안쪽 5px `#46382D` |
| (헤더 구간의 프레임 색) | `#5B4643` |
| 편측 합계 | 18px |
| radius (바깥) | **42px** (±4) |
| radius (본문/헤더 = 42−7−11) | **24px** |

## 상단 헤더 바

| 항목 | 값 |
| --- | --- |
| 높이 | **91px** (ref y 471~562) |
| 배경 | 평면 `#523E3D` (그라디언트 아님) |
| 상단 내부 하이라이트 | **9px** `#5B4643` (y 472~480) |
| 하단 구분선 | **없음 (0px)** |
| 좌우 폭 | 모달 폭 전체 (인셋 0px) |

## 제목 텍스트

| 항목 | 값 |
| --- | --- |
| 잉크 높이 | **34px** (스트로크 포함 45px) |
| font-size | **44px** (±4) |
| 색 | `#FFFFFF` |
| 스트로크 | 검정, 한쪽 **5.5px** → `-webkit-text-stroke:11px` + `paint-order:stroke fill` |
| 정렬 | 가로 가운데(중심 540.5) · 세로 완전 중앙 |

## 닫기 버튼

**없음.** 22·07·06·03 네 장 모두 헤더 전수 스캔에서 ✕/X 버튼 픽셀 0개.
이 게임의 팝업은 헤더에 닫기 버튼을 두지 않고 **본문 하단 버튼**으로 닫는다.
→ 구현에서도 헤더 ✕ 를 넣지 않는다. (우리 모달은 호출부 10곳 전부 하단에 `닫기`/`확인` 버튼이 있어 문제 없음)

## 본문(베이지) 영역

| 항목 | 값 |
| --- | --- |
| 사각형 | x 111~968 (w 858), y 563~1941 (h 1379) |
| 배경색 | **`#F0D9BA`** |
| 무늬 | **별무늬 있음** — 색 `#ECD3B4`, 별 **53×50px**, 가로 pitch **235px**, 세로 pitch **235px**, 첫 별 x=146 · y=564 |
| 첫 콘텐츠까지(상) | 99px (베이지 상단 563 → 첫 카드 662) |
| 좌우 인셋 | 카드 x 130~949 → 모달 바깥 기준 39px, **본문 박스 기준 21px** |

> (나) 바닥 시트(07)의 본문은 같은 `#F0D9BA` 지만 **무늬가 없다**(표준편차 0.93 = JPEG 노이즈 수준).
> 무늬는 (가) 다이얼로그 전용이다.

## 본문 안 요소 (참고 — A5 범위 밖, 화면별 작업에서 사용)

| 항목 | 값 |
| --- | --- |
| 안쪽 스크롤 컨테이너 | x 118~961 (w 844), y 628~1620, 배경 `#D7C0A1`, radius 22px |
| 카드 1칸 | 820×176px, 세로 gap 25px (pitch 200), radius 22px, 배경 `#F7ECDA`, **테두리 없음** |
| 하단 액션 버튼 | 274×126px, radius 40px, 검정 외곽선 8px, 회색 채움 `#999999`(비활성), 라벨 48px |
| 하단 2분할 토글 | 796×100px, radius 50(pill), 트랙 `#61503C` / 선택 `#47392A`, 라벨 44px |

## 구현 매핑 (index.html)

| 측정 항목 | CSS |
| --- | --- |
| 딤 65% | `#modal{background:rgba(0,0,0,.65)}` |
| 좌우 여백 91 · 세로 +42 | `#modal{padding:126px 91px 42px}` (위-아래 패딩 차 84 → 중앙에서 아래로 42) |
| width 897 | `.mbox{width:100%}` (1080−91×2 = 898) |
| height 1497 | `.mbox{max-height:1497px}` |
| 검정 7 + 갈색 6/5 | `.mbox{border:7px solid #000;padding:11px;background:#46382D;box-shadow:inset 0 0 0 6px #564438}` |
| radius 42 / 24 | `.mbox{border-radius:42px}` · `.mhead`/`.mbody` 24px |
| 헤더 91 + 상단 9px 밴드 | `.mhead{height:91px;background:linear-gradient(180deg,#5B4643 0 9px,#523E3D 9px)}` |
| 제목 44 + 스트로크 | `.mhead h2{font-size:44px;-webkit-text-stroke:11px #000;paint-order:stroke fill}` |
| 베이지 + 별무늬 | `.mbody{background:#F0D9BA url(별 SVG) 35px 1px/235px 235px repeat}` |
