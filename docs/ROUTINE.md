# 루틴 작업 지시서 (병렬 워커 공용)

> # ⚙ 이력 정리(2026-08-30): 캡처 PNG 이력 제거·.gitignore 로 차단. 캡처는 커밋하지 마라(`git add -f` 금지). 증거는 review .md 의 수치로. 클론 뒤엔 `git fetch && git checkout -B main origin/main`(pull --rebase 금지).

> # ▶ 재개 지시 (2026-08-28, 저장소 주인 — 대화형 세션 kkkkkk-30 이 대행 기록)
> 2026-08-27 의 ⛔ 정지 지시는 해제됐다. 루틴은 ~~8개~~ **4개(A~D, 2026-08-31 주인 지시 «루틴 네개로 하고 fable로» — E~H 는 비활성, 모델은 넷 다 claude-fable-5)** 다. **이 계정(김문 / kkkkkk 워커 계정)의 루틴 실행 시간 = 매시 A :12 · B :27 · C :42 · D :57 (분 단위라 KST/UTC 동일).** (2026-08-31 23:15 KST 정정 — 앞서 :15/:30/:45/:58 로 적었던 것은 API 실측과 달랐다. fable 전환도 그 시점엔 실제 적용이 안 돼 있었고 23:17 KST 에 4개 전부 claude-fable-5 로 실전환 완료.) 주인이 계정을 4~5개 쓰므로 다른 계정 루틴과 구분할 때 이 표를 본다. 동시 워커가 4이므로 선점 lock 을 반드시 먼저 잡고, 같은 화면·같은 파일 구간을 잡은 다른 lock 이 있으면 피한다.
>
> **⚙ 다중 계정 규약(2026-08-31)** — 선점·경쟁 규칙은 전부 **저장소 안**(`docs/claims/<ID>.lock` + SID + push 경쟁 rebase 규칙)에 있어서 **어느 계정의 워커든 같은 규약이면 안전하다**(SID = `sess-HHMM-$RANDOM` 라 계정과 무관하게 유일). 새 계정을 편입할 때 지킬 것 셋:
> 1. **분(minute) 슬롯을 비는 자리에서 잡는다** — 사용 중: **김문 계정 = :12(A) · :27(B) · :42(C) · :57(D)** (2026-08-31 API 실측 정정). 빈 슬롯: :00 · :05 · :20 · :35 · :50 등 사용 중 슬롯과 ±3분 이상 떨어진 자리. 새 계정은 아래 표에 «계정 별칭 = 슬롯» 한 줄을 **먼저 커밋하고** 루틴을 만든다(슬롯이 겹쳐도 lock 이 지켜 주지만, 정시 몰림·번호 경쟁이 줄어든다).
> 2. **워커 이름에 계정 접두를 붙인다**(예: 2번 계정은 E~H 또는 B-A~B-D) — 커밋·PROGRESS 의 «워커 X» 표기가 계정 간에 안 겹치게.
> 3. **프롬프트는 이 서두의 것과 같은 규격**(fetch+checkout -B · 승인 프롬프트 금지 · 캡처 PNG 금지 · ROUTINE.md 가 유일 지시서)으로, 그 계정에 kuzuni/kkkkkk 쓰기 권한이 있는지 먼저 확인.
>
> | 계정 | 워커·슬롯 |
> |---|---|
> | 김문(1번) | A :12 · B :27 · C :42 · D :57 (claude-opus-5 — 2026-09-01 16:52 KST 주인 지시로 fable→opus 재전환) |
> **남은 todo 는 전부 루틴이 처리한다**(주인 지시 2026-08-28 — kkkkkk-30 의 단발 작업 처리는 종료됨).
>
> **현재 남은 작업(2026-08-28 09:55Z 기준, 아래 순서 권장 — 상세는 PROGRESS 표):**
> 1. ~~**T1 게이트 3건**~~ — **전부 완료(2026-08-28)**: 315(verify268 CRLF) · 316(verify192 sp 순서) · 317(verify91 — 282 가 av45 를 미출시로 밀어낸 뒤 굳은 표본. 게이트 이관, 제품 0줄).
> 1-b. ~~**신규 주인 지시(2026-08-28): 318 — 출석 보상 받을 수 있으면 «해당 버튼» 에 레드닷**~~ — **완료(2026-08-28, sess-1120-8629 워커 A)**. 진입 버튼(사이드 «출석»)은 원래 정상이었고, 결손은 293(우편)과 같은 «경로가 한 칸 짧다» 였다(팝업 딤 z30 이 사이드 z3 을 덮는다) → 70 팝업 «오늘 카드» 에 레드닷 신설. `verify318` 21/21 · `verify166` 72/72 · `verify299` 19/19 · SMOKE PASS.
> 1-c. ~~**신규 T1(2026-08-28 등재): 319 — `tools/verify204.js` 즉사(`bagUse is not defined`, 292 가 지운 함수)**~~ — **완료(2026-08-28, sess-1148-31285 워커 C)**. 예상대로 **제품 0줄**(게이트 부패). 53 가방 단언은 292(«화폐만») 이후 폐기 대상이라 «가방이 던전 입장권을 다시 들지 않는지» 로 방향을 뒤집었고, `page.evaluate` 예외를 `ev`/`blk` 로 감싸 **즉사 대신 그 블록만 빨개지게** 했다(278 처방). 한 번도 안 돌던 §5 후반·§6(구 세이브 승계 7건)이 처음 돌아 전부 초록. `verify204` 36/36 · `verify53` 78/78 · `probe319` 6/6 신설 · SMOKE PASS.
> 1-d. ~~**신규 주인 지시(2026-08-28): 320 — 승급전 팝업에서 «최고 스테이지 n/n · 전투력 n/n» 두 줄 폐지, «권장 스테이지» 한 줄만**~~ — **완료(2026-08-28, sess-1236-12003 워커 B, 323 과 한 세션)**. 두 줄과 색분기(`okS`/`okC`)를 통째로 걷어내고 «권장 스테이지 n» 한 줄만 남겼다. 색분기를 안 되살린 이유는 295 «언제든 도전» 이라 «미달=빨강» 이 거짓 신호이고 «충족» 신호는 323 의 버튼 레드닷이 주기 때문 — 신호를 한 곳으로 모았다. ⚠ 인라인 색이 빠지자 `.mbody p b`(갈색)가 남색 상자 위에 깔려 179 결함이 재발할 뻔해 `.pr-cond p b{color:#EADCC6}` 를 신설했다. `verify320` 102/102 신설 · `verify179` 135/135 · `verify267` 66/66 · `verify295` 25/25 · SMOKE PASS.
> 1-e. **신규 주인 지시(2026-08-28) 3건 — «버튼 자체 레드닷» 계열(318 과 같은 «경로가 한 칸 짧다», 선례·처방은 각 PROGRESS 행):**
>    **321** [룰렛 돌리기] 버튼(돌릴 수 있으면) · **322** 22 퀘스트 행 [보상 받기]·[모두 받기](받을 수 있으면) · ~~**323** [승급전 시작]·[승급전 도전]~~ — **완료(2026-08-28, sess-1236-12003 워커 B, 320 과 한 세션)**: `#pgo`·`#promoBtn` 에 `promoReady()` 축의 `.alert` + `<s class="updot">`, 위치 `.ifbtn.pbtn>.updot{right:24px;top:24px}` 신설(299 우상단 사분면 · 라벨과 250px 여백). 승급 성공 직후 자동 소등까지 단언. `verify320` 102/102 · `verify299` 20종 21/21 · SMOKE PASS · **321·322 에 그대로 쓸 수 있는 선례다**(`.ifbtn` 계열 버튼의 배지 자리 규칙이 이제 있다) · ~~**325** 34 축복 카드 [받기] 알약(켤 수 있으면 레드닷 + 알약 초록색)~~ — **완료(2026-08-28, sess-1347-8342 워커 C)**: 결손은 판정식이 아니라 **부품**이었다(`blessTick` 이 이미 `sideAlert('bless', blessAny())` 를 켜고 있었다) — 318 계열 «경로 한 칸». `.bls-c .tm>.updot{right:12,top:12}` **안쪽** 우상단(⚠ `.bls-c{overflow:hidden}` 이라 318·322 의 바깥 코너는 잘린다) + `.bls-c.off .tm` 을 `#4CBA2E`/`#2F8F1E` 초록으로(⚠ `.off` 는 «꺼진 카드» 가 아니라 «지금 받을 수 있는 카드» 다). 34 에도 `#blsw s{display:inline-block}` **특이성 함정**이 있어 스코프 짝을 신설했다(없으면 상시 점등). `verify325` 69/69 신설 · `probe325` 18/18 신설 · `verify299` 25/25 · SMOKE PASS. 레이아웃 0px 변경(34 는 통과 화면). **전부 폴리시 라운드보다 먼저.**
> 1-f. ~~**신규 주인 지시(2026-08-28): 324 — 강화(도감 포함)·장착으로 전투력이 오르면 «⚔️ 전투력 +X» 토스트**~~ — **완료(2026-08-28, sess-1333-12458 워커 B)**. 처방대로 **공용 감시자 1개** — `cp()` 를 올리는 경로 **15곳 중 14곳이 이미 `markDirty()` 를 지나므로** 그 한 줄을 «묶음 열림» 신호로 쓰고 기준값은 **직전 프레임의 `cp()`** 로 잡는다(호출부는 예외 없이 «변이 → markDirty()» 순서라 그 시점 cp 는 이미 오른 값이다). ⚠ **나머지 1곳이 하필 지시의 한복판** — 강화 탭(`$('bUp')`)만 markDirty 를 안 불러(`S.lv` 는 `bonus()` 캐시 밖) 그 한 곳만 `cpFxArm()` 을 직접 부른다. 묶음 창 420ms 가 64/262/297 홀드 상수 전부보다 커서 **홀드는 합계 1장**. 침묵 4종(Δ≤0 · Δ=0 · 자동 구매 · 부팅). `verify324` 29/29 신설(«되돌림 시험» 포함) · 이관 8종 전부 초록 · SMOKE PASS.
> 1-g. ~~**신규 주인 지시(2026-08-28): 326 — 훈련 단계 요구치 증가식(300 → 600 → 900 …, 스탯당 100·n)**~~ — **완료(2026-08-28, sess-1400-17098 워커 D)**. 상한을 «단계×100» 에서 **단계 몫 누적합** `trainCapAt(n)=100·n(n+1)/2` 로 바꾼 것이 전부다(index.html 4줄). 183 규약(«단계 업 직후 0») 은 항등식 `cap(n)−cap(n−1)=100n` 위에서 그대로 성립해 **눈금의 길이만** 길어졌고, **세이브 이관은 «없음» 이 정답**이다(구 상한 100n ≤ 신 상한이라 넘침이 구조적으로 없다 — KEY 안 올림). `verify326` 82/82 신설 · `verify183` 27/27 이관 · `verify112` 38/38 · `verify168` 56/56 · SMOKE PASS.<br>⚑ **밸런스 계수는 0줄 변경 — 4칸을 199 로 넘겼다.** 비용 곡선을 안 건드려 «s80 도달 Lv 308» 은 불변이고 바뀐 것은 **단계 이름표(4 → 3)** 뿐인데, 단계 보너스가 ×1.30 → ×1.20(−7.7%) 이 되면서 적 곡선 역산이 밀렸다: sim177 ③⑧ · sim249 ⑤⑥ 을 **«⏸199 대기»**(실패로 안 세되 값·초과%·지렛대를 매 실행 찍는 `ck199`)로 등재. 최악 몹 1.024초(+2.4%) · 관문 보스 15.66초(+4.4%) — 하드락 아님. **★ 199 가 반드시 볼 것**: 누적합 상한 × 레벨당 ×1.05 지수라 4단계(Lv 1000)가 2.5e25 골드로 sim112 지평 밖이다 → «단계당 +10%» 축이 s80 부근에서 **+20% 로 굳는다**(상세표 `docs/review/326-훈련단계요구치증가식.md` §4-3).
> 1-h. **신규 주인 지시(2026-08-28) 추가 4건**: ~~**327** 12 소환 결과 창 세로 «2배 정도» 확대(187 후속)~~ — **완료(2026-08-28, sess-1423-26201 워커 A)**. 187 의 «행수 가변» 이 둘을 놓치고 있었다 — ① 중복이 개수로 합쳐지는 **흔한 결과**는 계속 ref 크기(539)였고(주인이 «또» 작다고 한 쪽), ② 상한 676 탓에 고유 30칸은 **여전히 6칸이 가려졌다**(24/30). → «가변» 폐지, **고정 2배**: `--sm-gh` 876 상수 · 패널 **1080 = ref 539 × 2.00** · 그리드 876 ≥ 30연 최악 5행 846 ⇒ **가려짐 0**(스크롤조차 안 쓴다). 패널이 1080 이면 84 버튼을 침범하므로 **패널·리본이 통째로 103 위로**(709→606 · 641→538, 84 앵커 Δ0 · 하변↔버튼 20 유지), 빈 면은 `align-content:safe center` 중앙정렬이 받는다. 부수 효과로 «창이 소환마다 커졌다 작아졌다» 하던 187 부작용도 사라졌다. `verify327` 85/85 신설 · `verify187` 63/63 · `verify12` 15/15 · `verify84` 31/31 이관 · SMOKE PASS. · ~~**328** 294 정정 — 소환 레드닷을 카드가 아니라 «10회 소환 n/n» 버튼(`.cbtn.b1`)으로 이동 · **329** 13 재화 탭 «광고 보고 받기» 버튼 레드닷 · **330** 89 유물 소환 버튼 레드닷~~ — **완료(2026-08-28, sess-1448-26003 워커 C, 권장대로 한 세션 묶음)**. 세 자리가 같은 지시·같은 부품·같은 규약인데 **자리의 답이 전부 달랐다** — 닷이 먹는 **42px**(27 + 링 7.5×2)와 «호스트를 무엇이 자르는가» 가 갈랐다: **328** `.cbtn{overflow:hidden}` + 안쪽 37.79px → 노드는 카드 자식으로 두고 좌표만 버튼 코너로(`.shp-card>.updot{right:46.5;top:132.5}`, ▶AD 뱃지 선례) · **329** `.cn-cd{overflow:hidden}` 이 바깥 코너를 5.24px 자름 + 안쪽 55.08px → **여기만 안쪽**, 단 규약값 12 는 봉우리 1.3 에서 «(n/n)» 을 3.99px 밟아 **4** 로 · **330** 수반 우상단 코너가 **투명**이라 그릇 림 오른쪽 끝으로(`#rwBasin>.updot{right:18;top:12}`). `verify328` 19/19 · `verify329` 25/25 · `verify330` 20/20 · `probe328` 22/22 **전부 신설** · `verify299` 27/27(2자리 등재) · `verify166` **73/73 이관** · SMOKE PASS. ⚑ **이관이 이 작업의 핵심 교훈이다** — 329 가 탭바 «상점» 조건을 OR 로 넓히자 `verify166` 2건이 빨개졌는데, 항을 눌러 초록으로 되돌리는 것만으로 끝냈으면 **«329 가 통째로 사라져도 초록인 게이트»** 가 됐다(누른 항을 묻는 항을 한 줄 더 넣어 72→73). 곁다리 수리 1건: `.shp-card .clk` z 5→7(닷이 «도감 완성» 딤 위에 뜨는 것 — 321 «레드닷 = 지금 누를 수 있다» 약속). 상세 `docs/review/328-330-버튼레드닷3자리.md`. **전부 폴리시보다 먼저.**
> 1-i. ~~**신규 T1 «버그» (2026-08-28 주인 보고): 331 — 던전 «잡몹 국면» 폐지, 입장 즉시 보스전만**~~ — **완료(2026-08-28, sess-1233-14318 워커 A)**. 255 는 «판정» 만 보스 격파로 옮겼고 «보스를 부르는 경로» 는 178 그대로여서(입장 `queueMobs()` 50마리 → 눈금 `DUN_BOSS_P` 에 차야 보스) 앞 국면이 통째로 남아 있었다. 입장이 곧 보스 예약 · 몹 리필 삭제 · `DUN_BOSS_P`/`DUN_BOSS_AT` **선언째 삭제** · 진행바 1국면. `verify331` 79/79 신설 · 이관 6종(178·255·257·264·285·30) 전부 초록 · SMOKE PASS. **밸런스는 재측정만 하고 계수는 안 건드렸다 — 수치 확정은 199 몫**(판당 필요 피해 절반, 최악 판 예산의 76%→47%, 클리어율 30/30 불변).
> 1-j. ~~**신규 주인 지시(2026-08-28): 332 — 보스 격파 시퀀스: 터짐 → «클리어» 표시 → 1초 뒤 완료 화면**~~ — **완료(2026-08-28, sess-1233-14318 워커 A · 331 과 한 세션)**. 타이머 체인이 아니라 런에 얹은 상태(`clrT`/`clrDie`/`clrMsg`)라 포기·시간 초과와 경합이 **구조적으로** 없다. die 애니 길이는 아틀라스에서 잰다(`dunBossDieSec`). `verify332` 80/80 신설 · SMOKE PASS.
> 1-k. ~~**신규 T1 «버그(게이트 부패)»(2026-08-28 등재): 333 — `tools/verify149.js` 6건 실패, 295 가 지운 «승급 조건 미달» 을 아직 찾는다**~~ — **완료(2026-08-28, sess-1326-25814 워커 A)**. 예상대로 **제품 0줄**. `verify149`(문구가 있어야 통과)와 `verify295`(없어야 통과)가 서로 반대를 단언하던 자리라 나중 지시(295)가 옳은 쪽으로 게이트를 덜어냈다. 자리를 비우지 않고 **살아 있는 승급 계열 토스트**(코스튬 [강화] 미보유 반려)로 §1·§2 를 갈아 끼웠고(표본 51/51 유지 + 149 가 안 보던 notify 자리 신규 편입), 계급 이름을 끼울 데가 없는 §3 폭 표본만 재료(`D.rank`)째 지웠다(310 선례). ⚠ 갈아 끼우다 **§2 가 «자기가 부른 notify» 를 세고 있던 것**을 잡아 실제 버튼 클릭으로 바꿨다(LESSONS 333-③). `verify149` 273/273 · `verify295` 25/25 · SMOKE PASS.
>
> 1-l. ~~**신규 T1 «게이트 부패» (2026-08-28 등재, 325 워커 C 곁다리 관측): 334 — `tools/verify117.js` C3 1건 실패(39/40)**~~ — **완료(2026-08-28, sess-1434-23454 워커 B)**. 예상대로 **제품 0줄**(게이트 부패). 34 의 13회차가 알약을 «시계 `<b class="ck">` + 숫자 `<i>`» 로 쪼갠 뒤 게이트만 한 노드 시절의 `⏱ HH:MM:SS` 형식에 굳어 있었다 — 값(경험치 1 · 30분)은 내내 정상이었고 같은 줄의 `prog === 1` 도 초록이었다. 처방 ①(두 부품 **각각** 단언)을 골랐다: ②(정규식에서 `⏱ ` 접두어만 빼기)는 **시계가 통째로 사라져도 초록**이라 «타이머 표시» 라는 뜻을 잃는다. 무르게 푼 수리가 아님은 **되돌림 시험 `C3n` 신설**이 못박는다(시계 삭제 · 옛 합본 주입 둘 다 빨강, 원복하면 초록). `verify117` 41/41 · `verify325` 69/69 · `probe325` 18/18 · `verify299` 25/25 · SMOKE PASS. 전수 스윕 결과 같은 합본 단언은 이것 하나뿐이었고 **헛초록도 없었다**.
> 1-m. ~~**신규 T4(2026-08-28 등재, 72 가 넘긴 본체): 335 — 03 서브탭 블록 기하**~~ — **완료(2026-08-28, sess-1521-8678 워커 A · 2회차, 제품 1줄)**. **인계된 «4인 일치» 지적 5줄 중 4줄이 «유령»** 이었다. 뿌리는 하나 — **이 화면에는 앵커가 둘이다**: 카드 리스트는 `ref y − 84`, **서브탭 바·앱 탭바는 `ref y − 60`**(ref 2340 ↔ cap 2280). ROUTINE [3] 이 비평가에게 «−84» 만 알려 주게 돼 있어 **하단 앵커 요소는 예외 없이 +24 어긋나 보인다** — 바 위치 실측 **Δ0** · 높이 **Δ+1~2** · 활성 알약 **Δ0** 이고, «이음매 0→14px» 도 **14px 이 정답**이다(ref −8 + 구조적 24 = 16 안팎). 검산은 공짜다: **앱 탭바**가 `−60` 으로 Δ0(2100)인데 탭바는 아무도 지적한 적이 없다. 2회차에 비평가 AN·AO 에게 **앵커 표를 프롬프트로 주자 넷이 전부 [달성] 로 넘어왔다.** **실재는 축 하나** → `.dns-sub{left:143→151}`(축 화면중심 **+8** = ref 비대칭 복원. 레퍼런스가 기운 게 아님은 같은 화면 카드가 50/50 대칭인 것으로 기각). ⚠ 1회차에 이음매를 닫으려다 **«유령 가로줄»**(6번째 카드 상변 4px — 카드 980폭 vs 바 794폭이라 바 밖으로 좌우 124px)을 만들어 2회차에 원복했다. `verify335` 24/24 신설(되돌림 시험 §R 포함) · `verify96` 64/64 이관 · 회귀 7종 · SMOKE PASS · 측정표 03 §4 정오표(«앵커가 둘») · LESSONS 교훈 4개.
> **AN 8 / AO 7 — AO 를 막은 유일한 항목(서브탭 라벨 잉크 −14~21%)과 잔여 감점이 한 줄도 03 것이 아니라 전부 공용 부품 `.stabs` 몫이다 → 337 로 등재.**
> ⚑ **72 를 다시 잡는 세션은 비평가에게 «앵커가 둘» 표를 반드시 줄 것** — 안 주면 같은 유령 넷이 그대로 다시 나온다(72 는 그 때문에 세 회차를 6 에 묶여 있었다).
> **⇒ 320~335 가 전부 완료됐다. 새로 등재된 **336**(⚑ 게이트 부패 «버그» — T1) 과 **337**(공용 서브탭 부품 `.stabs` 재측정 — T4) 이 남았고, T1 인 336 이 먼저다. 그 뒤 2번 폴리시 라운드.**
>
> # 🛑 주인 지시(2026-08-29): 58·72 폴리시는 «돌고 있던 세션까지만»
> · **58 — 이 시각으로 마감됐다.** 마지막 세션(sess-1501-3810, 43·44회차)이 끝나고 lock 이 풀린 상태라 44/50 에서 «종료(주인 지시)» 처리 완료(PROGRESS 행 반영). **누구도 58 을 새로 선점하지 마라.** 아래 2번 절의 58 항목(45회차 지시·주인 판단 대기 2건)은 **실행하지 말 것** — 판단 대기 2건은 기록으로만 남는다.
> · **72 — 지금 돌고 있는 세션(sess-1617-25930)까지만.** 그 세션이 끝나 lock 이 풀리면 회차·점수 무관 «종료(주인 지시)» 로 마감한다(상태 칸만 바꾸는 마감 처리는 허용 — 작업 재개 아님). **그 뒤 72 를 새로 선점하지 마라.**
> · 이 지시 이후 남는 것(2026-08-29 갱신 5 — **411·412 완료 · 351 10회차 완료**): **351 은 계속 돈다(11회차 = SET1·SET2 25화면 재촬영·재채점)** → **337** → **199**. 그 사이 351 이 낸 기등재분은 **각자의 번호로** 잡는다(`390`·`400`·`407`·`409`·`413`·`414`·`415`·`419`·`420`·`421`·`422`). (412 는 완료된 402 의 주인 재지시라 앞이었다. 402·403·404 는 완료. 199 선행 조건 «폴리시 소진» 은 58·72 마감으로 충족된 것으로 본다.)
>

# ⚑ 신규 주인 지시(2026-09-02 00:33~00:40) — **683 · 684 · 685**: 683 = 유물 소환 획득 시 **그 유물 카드 자리**에 해당 유물 파티클(연속 소환 포함 · 666 의 «버튼 밖 0건» 게이트는 지불 버스트 한정으로 이관). 684 = 대량 소환에도 ⚔️ 전투력 알림. **685 = 324 홀드 규약 주인 개정 — 알림이 연속 강화 «중에도» 떠야 한다**: «살아있는 한 장» 실시간 갱신(동시 ≤1장 · Δ≤0 침묵 유지 · verify324 방향 반전 이관). 684 도 같은 꼴. 상세 각 PROGRESS 행.

# ⚑ 신규 주인 지시·보고(2026-09-02 00:25~00:28) — **681 · 682**: 681 = **다이아 → 재화 교환은 즉시 지급**(우편 경유 폐지 — 기존 우편 소급 삭제 금지 · IAP/광고 보상 스코프 밖). **682 = 유물 버스트가 «3방향 고정» — 랜덤 산포로(666 진행 세션이 그 회차에서 잡아라 · 660 공용 이미터면 같이)**. 상세 각 PROGRESS 행.

# ⚑ 신규 주인 지시(2026-09-02 01:08) — **670**: 단련 버튼 비용 표기 = «(단련석 아이콘) 수량» 만(«단련» 텍스트 제거). 660 계열(658~660·670)과 한 세션 권장. 상세 PROGRESS 670.

# ⚑ 신규 주인 지시(2026-09-02 00:58~01:02) — **668 · 669 (같은 소환 탭 — 한 세션 권장)**: 668 = 소환 배수 토글 ×1/×10/×100/×1000(라벨·가격 선형 배수) — **핵심 게이트 = 시드 고정 «×100 1번 ↔ ×1 100번» 완전 등가성**(배치 중 소환 레벨이 올라도 그 시점 확률표 — «확률표 고정 후 N회 샘플» 금지). 669 = 소환 레벨·경험치 글씨 확대(잉크 실측 · 침범 0 · 655 겹침과 같이 재기). 상세 각 PROGRESS 행.

# ⚑ 신규 주인 지시(2026-09-02 00:50) — **667**: 이용권 UI 디자인 재작업 — 레퍼런스 원본 대조 루프(실측→Δ표→수리→비평 2인 ≥9). ⚠ 199 결3 ⓑ 로 이용권이 «×배율» 상품으로 개편 중 — **새 상품 문구·수치 전제**로 그릴 것(«+4h» 로 되그리면 충돌). 상세 PROGRESS 667.

# ⚑ 신규 주인 지시(2026-09-02 00:40) — **666**: 유물 소환도 660 규약 — 텍스트 이펙트 폐지, **유물 소환 버튼에서 유물화폐 아이콘 버스트**(스폰=버튼뿐 · 캔슬 금지). 658~660 묶음 세션이 같이 처리 권장. 상세 PROGRESS 666.

# ⚑ 신규 주인 지시(2026-09-02 00:35) — **665 (T1급 · 순서 엄수)**: 스테이지 도전 중 던전·승급전 진입 개방. 단 **막아뒀던 이유(주인 증언)** = 전환 순간 이전 모드가 «갑자기 클리어» 되는 오판정 버그 — **① probe665 로 그 오판정을 재현(빨강 확인) → ② 판정을 런 소속으로 격리(332 선례) → ③ 차단 해제** 순서. 게이트 = 교차 진입 매트릭스 전수(오판정 0 · 보상 이중 지급 0). 상세 PROGRESS 665.

# ⚑ 신규 주인 지시(2026-09-02 00:20~00:27) — **662 · 663 · 664**: «미개방 정보 공개» 규약 — 662 = 미개방 스킬 세부 «?» 폐지, 실명·설명·수치 표시 + **24종 설명 품질 감사**(수치는 데이터 파생) · 663 = 08 세부 아이콘 아래 쏠림 수정(662 와 한 세션, 24종 전수) · **664 = 장비(06)·코스튬(50)·펫(26)도 마찬가지** — 미개방 카드 선택 가능 + 실정보 표시 + **장착/출전/착용 버튼만 비활성**(+반려 피드백). 규약 문구는 662와 한 벌 공유. 상세 각 PROGRESS 행.

# ⚑ 신규 주인 지시(2026-09-02 00:10) — **661**: 장비 강화 결과(단건·일괄)를 **화면 중앙**에 — 현행 자리 실측 후 중앙 통일, 두 프레임(9:19·9:13.3) 모두. 상세 PROGRESS 661.

# ⚑ 신규 주인 지시(2026-09-01 23:40~23:53) — **657~660** · ⚑ **660 이 훈련·단련 강화 연출의 확정 스펙**이다 (상세는 각 PROGRESS 행)
> · **660 (658·659 와 한 세션 강권)** — **훈련·단련·룬 강화 연출 리디자인(세 탭 공통 확정 스펙)**: 숫자 플로터 폐지(셋 다) · 알갱이 파티클 폐지 → **아이콘 버스트**(훈련 = 골드 · 단련 = 단련석 · **룬 = 룬 아이콘**)로 교체 · **스폰 위치는 강화 버튼뿐**(아이콘/슬롯 쪽 이펙트 0건 — 주인 지목) · 골드가 버튼으로 가는 이동 연출 폐지(658) · 발화 틱당 1회(619 규약) · **캔슬-재시작 금지**(틱마다 독립 스폰, 날아가던 입자는 수명 끝까지). **657 은 흡수 종료 — 착수 금지, 660 을 하라.**
> · **657** — 훈련 꾹누르기 파티클 수 너무 적음 → 밀도 상향. **지금 돌고 있는 619 폴리시 라운드에 편입**(채점 축에 «파티클 풍성함» 추가 · 619 의 성능·keep-out 가드는 유지).
> · **658** — **골드가 훈련 버튼 쪽으로 날아가는 연출 통째 폐지**(주인: «존나 후지다»). fxSilent 축 대체 + 게이트 동시 철거(333·592). 654 와 같은 fx 축이라 lock 확인 후.
> · **659** — **단련 버튼 숫자 플로터 폐지**(주인: «존나 후지다»). 520 선례 · 부품은 남기고 단련 호출부만 · 터짐 파티클(619/657)은 남긴다. 658 과 한 세션 권장.

# ⚑ 신규 주인 보고(2026-09-01 23:18~23:20) — **656 «버그» (전면 스윕)**: 버튼을 누르면 축소되며 뒤로 «검정 테두리 찌꺼기» 가 비친다 — 소환 버튼에서 보고됐고 주인 보강 «많은 버튼들이 그렇다, 다 수정» 으로 **전 버튼 전면 스윕 격상**. 공용 눌림 레이어 규약 + probe656(전 화면 × 눌림 봉우리 프레임 전수 자) 먼저. 621·625 결과 되돌리지 마라. 상세 PROGRESS 656.

# ⚑ 신규 주인 보고(2026-09-01 23:15) — **655 «버그»**: 소환 카드 «Lv.31» 알약 끝자리가 게이지 캡슐에 덮여 안 보임(주인 스크린샷 — 단검 카드·우상단 🔍). **470 기각 마감이 기다리던 원본 증거가 왔다** — scan470 미개방 팝업(🔍 확률 팝업 계열 1순위)을 열어 재현부터. 상세 PROGRESS 655.

# ~~⚑ 신규 주인 보고(2026-09-01 23:05) — **654 «버그» (T1급 · 먼저)**~~ — **완료(2026-09-01, sess-1341-11341 워커 C · 1회차)**: 재현(`probe654`)이 **가설 ⓐⓑ 를 둘 다 확인**했다(수리 전 코인 — 보스 격파 4 · 파도 4 · 던전 클리어 6 · 잡몹 킬 0 · 비전투 6). 뿌리가 둘이고 **기계가 서로 다르다**: ⓐ 592 의 표가 ⑵⑶ 을 켠 채 · ⓑ 던전 클리어 보상은 그 표를 **한 번도 안 지나고** 512 `fxReward` 로 들어온다. ⇒ `FX_COMBAT_FX` 네 항 전부 false + `giveReward(r, mute)`·`fxClearMute()`. ⚑ **코인만 껐으면 절반만 지킨 것** — 무음 재화«만» 받은 지급은 공용 크림 `FXPAL.reward` 로 터지고 그 값이 512 가 «주인이 본 골드» 로 지목했던 그 색이다(남은 종류 0 이면 버스트도 건너뛴다). 무음은 **골드 한 축뿐**(아레나 다이아·강화석 연출은 그대로). `verify654` 24/24 신설 · `probe654` 4/4 신설 · `verify592` **35/35 이관**(333 처방 — [A2]·[5b]·[5c] 방향만 뒤집었다) · `verify512` 31/31 무수정 · 회귀 10종 · **SMOKE PASS**. 곁다리 등재 **0건**(⚠ `verify93` 3항이 빨개 등재 직전까지 갔으나 **rebase 전의 낡은 자**를 돌린 것이었다 — pull 뒤 26/26 PASS. **회귀는 pull 뒤에 돌려라**). 상세 `docs/review/654-보스클리어골드연출무음.md`.<br>↓ 아래는 등재문(보존): 보스 격파 직후 골드 획득 이펙트가 또 뜬다. 592 «전투발 골드 연출 폐지» 의 보스/클리어 경로 구멍 또는 512 보상 연출과의 충돌 — **전투·보스·클리어 계열 골드 연출은 전부 무음, 비전투 512 연출은 유지**(위임 규약 채택, 상세 PROGRESS 654).

# ⚑⚑ 위임 규약(2026-09-01 22:35 — 모든 작업 공통, 주인 원문 «결정 나한테 하라 하지 말고 추천해서 걍 알아서 해달라하기»)
> **«주인 상신/결정 대기» 로 회차를 세우지 마라.** 결-류 판단이 생기면 ① 선택지·대가 표 ② **최선 권고안을 그 회차에서 즉시 채택·진행** ③ «위임 규약 채택» 표식 + 되돌림 방법 기록. 무응답 기본값 대기도 폐지 — 기본값이 곧 결정. 주인이 기록을 보고 뒤집으면 그때 되돌린다(되돌림 가능하게 짤 것).

# ⚑ 주인 결정 확정(2026-09-01 22:35) — 199 결12(①=×1.4 문장이 과녁·목록 재작성 / ③ 첫 항 폐기→sim249 게이트로 갈음) · 목표액 2,720.5만 정정 승인 · **결1 ⓑ 분할 지급 이행 = 다음 회차 1순위**(6회차째 미이행). 상세는 `docs/review/199-최종밸런스.md` 맨 끝.

# ⚑ 주인 결정 확정(2026-09-01 14:05) — 199 밸런스 결1·결2·결3·결11 전부 닫힘 (상세·원문은 `docs/review/199-최종밸런스.md` 맨 끝 «주인 결정 확정» 절)
> **결1 ⓑ**(첫날 100만 총량 유지·분할 지급) · **결2 ⓐ**(2,730만 = 소환 전용 장부 · 입장권 교환가 인하는 보조 손잡이) · **결3 ⓑ**(오프라인 1회 6h → 하루 예산 1,440분 · **151 이용권은 ×배율 상품으로 개편**) · **결11 = 자 수리 우선**(2~3회차 · 20회차 상한은 그만큼 연장 인정). 199 다음 회차는 **자 수리부터** — 계수는 그 뒤 손잡이 하나씩.

# ⚑ 신규 주인 지시(2026-09-01 11:25) — 619 (대화형 세션이 대행 등재 · 상세는 PROGRESS 행)
> · **621 (T2 · 619 와 한 세션 권장 — 같은 홀드 틱 축)** — **연속 강화 중 버튼이 «작아진 채 진동» — 틱마다 원래 크기 ↔ 눌린 크기 왕복으로.** 눌림 애니를 «누름 상태 유지» 가 아니라 «틱 이벤트» 에 묶는다. 619 선점 세션이 살아 있으면 그 회차에 합류.
> · **619 (T2)** — **연속 강화(연타·64 홀드) 때 이펙트가 매 강화마다 터져야 한다 — 훈련·단련·룬 전부.** 트리거 경로의 중복 방지 가드/스로틀부터 실측하고, 발화는 틱당 1회 원칙(성능은 파티클 풀·동시 상한으로). ⚠ 324 토스트의 홀드 묶음(합계 1장)은 주인 승인 설계라 그대로 — 이번 지시는 이펙트다. 게이트 = «강화 N회 ↔ 발화 N회» 자동 검증, 채점 = 연속 프레임 캡처 비평 2인(60 방식) × 세 화면.

# ~~⚑ 신규 주인 지시(2026-09-01 01:06~01:13) 4건 — 611 · 612 · 613 · 614~~ — **전부 완료(2026-09-01 02:14 까지)** (대화형 세션이 대행 등재 · 상세는 각 PROGRESS 행)
> · ~~**611 «버그» (T1급 · 먼저)**~~ — **완료(2026-08-31, sess-1614-24022 워커 A · 3회차 CE 9/CF 10)**. 현행 main 재현됐고(배포 지연 아님) **자리가 정정됐다** — 06 `.eqsl` 은 규약 그대로(11/11), 본체는 스크린샷의 실제 화면인 **05 장비 팝업 카드 `.wgc`**(471 마지막 예외 ④ · 26.5/63.5). Lv.n 좌상단 거울 + overflow 제거 + 가로 in-x 22(클립 산수 — ⚠ 마지막 열은 더미가 아니라 실물 5종/등급). 상세 PROGRESS 611 행 · `docs/review/611-장비팝업카드레드닷.md`.
> · **612 (T2)** — 단련 탭 아이콘 확대. 411·394(눈금 축 먼저)·356(등방) 규약 + 두 겹 채점(잉크 자 + 나란히 대조 캡처 비평 2인). 9:19·9:13.3 둘 다.
> · **613 (T2) · 614 (T2) — 한 세션 권장(같은 화면)**: 613 = 단련석을 포인트처럼 직접 지불해 단련(강화 절차 제거) + **보강: «단련석 포인트» 표시부를 없애고 그 자리에 현재 단련석 개수 표시**. 계수는 199 몫. 범위는 **단련 탭만**(주인 확인 2026-09-01 — 장비/일괄 강화는 그대로). 614 = 단련석 «다이아로 회수» 기능 통째 폐지 — 죽는 분기와 그 게이트 항을 같이 걷어내고(333·399 규약) 199 에 통지.

# ⚑ 신규 주인 지시(2026-08-29) 2건 — 412 · 411 (대화형 세션이 대행 등재 · ⚠ 412 는 등재 경쟁으로 410 에서 번호 개정)
> · ~~**412 (T1 · 402 재지시)**~~ — **완료(2026-08-29, sess-0847-4051 워커 C · 1회차, 제품 0줄 · 자산 SVG 8장).** `probe412` 신설로 **찍힌 픽셀** 재현 → 등재문 확인(최소 ΔE **12.4** · 문양 중복 **15쌍** · L\* 폭 44.8). 402 의 «색 = 카드의 두 톤» 을 폐기하고 **색상환 배분 + 한 밴드**(L\*64 고정 · C\*≤55 · 실측 폭 0.1)로 갈았고 여섯 장이 공유하던 별을 **8종 실루엣**으로 갈랐다 ⇒ 최소 ΔE **39.6** · 문양 중복 0. ⚠ **균등 45° 는 오답이다**(sRGB 색역 비대칭 — 균등 안은 33.7 로 목표 미달). ⚠ 밴드로 묶은 대가로 **회색조에서는 8장이 같은 밝기**라 문양이 유일한 축이다. `verify402` [B] 10항 갈아 끼움·신설 **39/39** · `probe412` 8/8 신설 · 회귀 13종 무수정 · SMOKE PASS. ⚑ **411 에게**: 여기서 «잉크 bbox 최대÷최소 ≤ 1.05» 눈금을 먼저 써서 **1.000** 을 받았다(`verify402` B10·B11) — 그 자를 그대로 가져다 쓸 수 있다. 상세 `docs/review/412-던전입장권색구분강화.md`.<br>**↓ 아래는 등재문(보존)** — **던전 티켓이 «확실하게» 안 갈린다.** 주인 원문: «던전 티켓 좀 더 확실하게 색 구분되게좀». 402 는 접혀 있던 매핑을 풀었고(8종 분리 · 완료), 이건 **풀린 8종이 실제로 갈리는가** 다.
>   **실측(등재 전 계산 · CIE-Lab ΔE76, 채움색)**: 28쌍 **최소 ΔE 12.4**(`dia` ↔ `rstone`) · 다음이 24.0(`relic2`↔`relic3`) — 중앙값 86.7 이라 **못 갈리는 자리가 둘에 몰려 있다.** 뿌리 둘: ⓐ 402 가 색을 **카드에서 그대로** 물려받아 유물 3장이 색상각 **281 / 287 / 286°** = **6° 안**(갈리는 건 명도뿐) · ⓑ **속 문양이 8장 중 6장 똑같은 별**(path 문자열까지 동일) = 그 여섯은 **색 하나로만** 갈린다.
>   ⚑ **같은 지시의 다른 쪽 — «통일감 있게»**: 색을 갈라도 **8장이 한 세트로 읽혀야 한다.** 통일감을 지탱하는 것은 **껍데기 기하가 픽셀 동일**하다는 것 하나뿐이니 그건 건드리지 말고, 색은 **채도·명도를 한 밴드로 묶고 «색상만» 돌린다**(87 방식). 문양도 **획 두께·흰색 opacity·외곽선 규격은 그대로 두고 실루엣만** 바꾼다.
>   ⚑ **좁다는 정량 근거가 저장소 안에 있다** — 87(코스튬 **50색**)이 쌍별 최소 ΔE **10.3** 을 통과선으로 썼다. 50색을 색상환에 밀어 넣어 얻는 값을 **8색이 겨우 넘고 있고**, 게다가 이건 03 카드 알약 안 **64×50** 짜리다. ⇒ 색상환 배분(Δh≈45°) + **문양도 같이 가르기** + 게이트에 «쌍별 ΔE ≥ 목표» · «문양 path 중복 0건» 신설.
> · **411 (T2)** — **07 스킬 · 26 펫 · 50 코스튬의 «장착 슬롯» 아이콘 확대.** 주인 원문: «펫, 스킬, 코스튬 전부 아이콘이 좀 가독성있게 커져야함. 내가 하는말은 그 팝업에서 그 **슬롯부분**» ⇒ **격자 카드가 아니라 슬롯 줄(`.sk-slot`)이 스코프**이고, 세 시트가 **한 부품**을 공유한다.
>   **실측**: 슬롯 115×115(링 5+12 ⇒ 안쪽 91) · 아이콘 상자 12..82 · 그 아래 `Lv. n` 76..108 로 **6px 겹침** · 실제 잉크 **63×53**(= 슬롯의 **54.8%** · 안쪽의 69%). **기본안 ⓐ 는 «슬롯 안에서만» 키우는 것**(Lv 를 하단 띠로 눌러 상자를 12..103 으로 ⇒ 잉크 63 → 약 82, **행 기하 Δ0**).
>   ⚑ **주인 추가 지시: «통일감 있게» — 이것이 «크게» 보다 큰 결손이다.** 세 시트가 상자는 공유하는데 **그림 출처가 셋 다 다르다**: 스킬 **이모지**(잉크 63×53) · 펫 **스프라이트 캔버스**(`PET_TH.slot` 69×59) · 코스튬 **기사 캔버스 96×126**(`.sk-si` 상자 70 보다 56px 크고 `flex-end` 로 위로 삐져나온다) ⇒ **세로 덩치가 최대 2.4배 어긋난다.** 처방은 **크기를 세 곳(CSS font-size · `PET_TH` · 마크업 width/height)에서 따로 정하지 말고 «그림 자리» 를 한 곳에 선언해 셋이 읽게** 하는 것. ⚠ **394 선례** — «무엇을 눈금으로 삼는가»(폭/높이/면적)를 먼저 정해 적을 것(종횡이 이모지 1.19 · 펫 1.17 · **코스튬 0.76** 이라 안 정하면 어느 축을 맞춰도 다른 축이 벌어진다).
>   ⚑ **통과 기준은 «스샷» 이다(주인 재확인).** 숫자만으로 닫지 마라 — 상자를 맞춰 놓고도 눈에는 제각각인 것이 394 가 34 축복에서 겪은 일이다. **두 겹으로 채점**: ① 자 — `probe` 로 **그려진 잉크** bbox 를 재 «세로 덩치 최대÷최소 ≤ 1.05»(근거: ref 아트 편차 0.7~2.0% · 우리 12.9% 가 결함으로 읽혔다) ② 눈 — **세 시트 슬롯 줄을 한 장에 나란히 놓은 대조 캡처**로 비평가 2인 독립 «덩치가 고른가» 통과까지 회차(356 방식 · 9:19·9:13.3 둘 다). ⚠ **나란히 안 놓으면 어긋남이 안 보인다** — 따로 보면 셋 다 그럴듯하다.
>   ⚠ **슬롯 자체를 키우는 안(ⓑ)은 404 가 만든 흡수 구조에 값을 치른다** — 격자가 `calc(100% - 657px)` 라 **1600 에서 이미 474(2행)** 뿐이다. 2행 4칸으로 바꾸면 격자가 311(1.4행)로 내려가므로 **격자 하한을 먼저 정하고 남는 만큼만** 슬롯에 줘라. ⚠ **356 규약 — 확대는 등방**이고, 펫·코스튬은 캔버스라 `PET_TH.slot` 을 종횡 그대로 키워야 한다(21983 주석의 «정사각으로 잡았다가 bird·robo 가 18% 커져 라벨을 밀었다» 1회차 실패를 먼저 읽을 것). ⚠ 같은 상자를 쓰는 형제 4개(`.sk-lock`·`.sk-slk`·`.sk-plus`·`.sk-slv`)가 **같이 비례**해야 한다.


# ⚑ 신규 주인 지시(2026-08-29) 3건 + 351 재지시 — 402 · 403 · 404 (대화형 세션이 대행 등재)
> · ~~**402 «버그» (T1)**~~ — **완료(2026-08-29, sess-0717-10471 워커 A · 1회차, 제품 6자리 · SVG 4장 신설 · 1장 삭제).** 재현(`probe402`)이 등재문 가설을 그대로 확인했다 — 수리 전 **8던전 → 5종**이 03 카드·04 세부·13 교환 카드 **세 자리 전부**(53 가방에는 자리가 없다), 수리 후 전부 **8종**. 뿌리는 `dunTk()` 삼항 사슬의 **마지막 폴백 한 문자열**이고, 처방은 표가 아니라 **id 파생**(`'tk' + Id`)이다 — 표도 손으로 적는 목록이라 던전이 늘면 똑같이 뒤처진다. ⚑ **사본을 지운 것이 핵심** — `dunTk()` 만 고쳤으면 03 카드는 여전히 옛 그림을 그렸다(`DUN_UI[].tk` 사본을 읽는다). `tk:` **10칸 삭제**로 죽은 필드 둘(`tower`·`despair` — 후자는 **단련석 아이콘**)도 같이 사라졌다. `verify125` **H2 «5종뿐» 을 갈아 끼운 것이 본체**(그 상수가 194·203 에 이어 **네 번째 부패였을 자리**) — H1·H2·**H3 신설**(그린 것 = 선언). 세이브 이관 **없음**이 정답임을 `verify402` [F] 가 구 세이브 실로드로 못박는다(KEY 안 올림). `verify402` 29/29 신설 · `probe402` 7/7 신설 · `verify125` 38/38 · 회귀 9종 무수정 · SMOKE PASS. 곁다리 **405** 등재(`fnchk125` 3건 — **수리 전 트리에서도 같은 12/15** 임을 대조로 확인).
> ~~· **402 등재문**~~ — **던전 입장권 색이 던전마다 달라야 하는데 유물 던전 4단이 전부 같은 보라다.** 주인 원문: «던전 입장권 색이 다 달라야하는데 안그러고 있네 수정해». 입장권 SVG 5장은 **실제로 색이 다르고**, 빨간 것은 **매핑**이다 — `dunTk()`(index.html **15448**)의 마지막 폴백이 `relic1~4` 를 `tkRelic` 하나로 접는다(`DUN_UI` 에도 같은 값이 네 번 적혀 있다).
>   ⚑ **이 지시는 게이트와 정면으로 부딪히고 그게 본체다** — `tools/verify125.js` **H2** 가 «입장권 종류는 5종뿐» 을 단언한다(551행). 125 의 «계열» 단위를 **«던전» 단위로 갱신**하고 H1·H2 를 갈아 끼운다(356 이 «비균등 스케일 금지» 로 관행을 뒤집은 것과 같은 꼴). ✅ **세이브 이관 «없음» 이 정답** — `S.dunTk` 는 처음부터 던전 id 별이라 수량은 이미 따로 세고 **아이콘만 접혀 있었다**. ⚠ 죽은 필드 둘(`tower.tk` · `despair.tk` = 입장권이 아닌 **단련석 아이콘**)도 같이 정리.
> · **403 (T2)** — **06 장비 시트가 9:13.3 에서 스크롤 없이는 다 안 보인다.** 주인 원문: «장비부분이 영웅→장비 부분탭이 스크롤 안해도 제대로 보이게 해줘야함 9:13.3에서도. **9:19의 레이아웃 좀 바꿔서라도** 그래야함». 산수: 시트 = `min(1584, frameH − 322)` · 뷰포트 = 시트 − 140 ⇒ 1600 에서 **1138 vs 콘텐츠 1418 = 280px 부족**(2280·1920 은 1444 로 여유 26).
>   ⚑ **351 이 두 번 «구조 한계 · 스크롤로 회수» 로 넘긴 바로 그 자리이고, 주인이 그 판정을 뒤집었다** — «칸 228 · 피치 400 은 레퍼런스 절대값» 이라는 제약이 이번 지시로 풀린다(측정표 06 정오표가 그 대가). ⚠ **가드 142 를 낮춰서 푸는 길은 막혀 있다**(351 이 두 번 올린 값 — 내리면 `verify351` [2-b2]·[R-d]~[R-f] 가 즉시 빨개진다).
> · **404 (T2 · 403 과 한 세션 묶음 권장 — 뿌리가 같은 상수다)** — **07 스킬·26 동료·50 코스튬 시트의 [스킬 소환]·[일괄 강화] 버튼이 9:13.3 에서 한 픽셀도 안 보인다.** 주인 원문: «9:16에서는 … 잘보이는데 9:13.3에서는 안보이고 그럼». 산수: 뷰포트 **1138**(403 과 같은 값) vs 콘텐츠 1289 ⇒ **151px 부족**이고 `.sk-btn` 이 **1158..1289** 라 버튼 전체가 밖이다.
>   ⚑ **351 5회차가 이미 `probe351b` 로 «스크롤 전 보임 0%» 를 찍어 두고 다음 회차로 미뤄 뒀다** — 처방 전에 그 자를 먼저 돌릴 것(338 규칙). 1순위 후보는 **버튼을 스크롤 그릇 밖으로 꺼내 시트 고정 자식으로**(같은 시트의 `.sk-head` 가 이미 그렇게 얹혀 있다 = 선례가 안에 있다).
> · **351 재지시 — 채점 규칙 두 조항이 바뀌었다(상세는 PROGRESS 351 행 꼬리표)**: ① **«스크롤로 닿으면 감점 아님» 폐기** — 주인이 «스크롤 안 해도 보이게» 라고 못박았다. 자에 «스크롤 **전** 보임 %» 축을 세우고 액션 버튼·주 CTA 는 스크롤 0 에서 100% 를 요구한다. ② **«9:19 를 안 건드린다» 제약 해제** — 레퍼런스 이탈 허용(측정표 정오표 필수 · 이탈은 «1600 에서 보이게 하는 데 필요한 만큼» 으로 제한). 주인이 «다른 UI들도 그런게 많음 … 그런거 다 수정해야함» 이라고 했으므로 **351 은 재개 대상이고, 남은 23화면은 `cap351.js` 의 `SET3` 에 이미 적혀 있다.**


# ⚑ 신규 주인 지시(2026-08-29) 3건 — 397 · 398 · 399 (주인 원문 한 묶음, 대화형 세션이 대행 등재)
> **주인 원문**: «패스 부분에 아직도 다이아 아이콘 찌그러져있더라. **게임내에 모든 아이콘 다 안찌그러지게해라. 원래 이미지 비율대로 되게.** 그리고 **패스 보상 출석보상 전부 다이아로 줘라**»
>
> · **397 «버그» (T1 · 먼저)** — 36 출석 패스 보상 젬이 `#psw.att .ps-bx>i{transform:scaleX(.76)}`(index.html **12626**)로 **가로 24% 눌려 있다**. `.cic` 자신은 `object-fit:contain` 이라 **호스트 `<i>` 가 누르는 것**이고, 356 이 폐기한 관행(«키운 뒤 scaleX 로 되눌러 bbox 를 맞춘다»)의 잔재다.
>   ⚑ **뿌리는 자리 하나가 아니라 스코프 구멍이다** — `tools/scan356.js` SCREENS(55행)가 «35 패스» 를 `['#menub','#psGo']` 까지만 열어 **출석 패스 탭을 한 번도 스캔한 적이 없다.** ⇒ **① 스캐너에 «탭·서브탭을 갈아타야 보이는 화면» 을 채우고 전수 재스캔 → ② 나온 자리를 «작은 쪽으로»(s = min(sx,sy)) 고치고 → ③ `verify356` [A] SCOPE 등재 + [B] 래칫 REMAIN 을 재스캔 값으로 다시 적는다.** 356 행의 «잔여 6자리 / 12노드» 는 **하한**이므로 그 숫자를 완료 근거로 쓰지 마라.
> · **398 (T2)** — **35 스테이지 패스 보상을 전부 다이아로**(`PASS_CUR` 3종 → dia 하나). 36 출석 패스는 **이미 전 칸 dia** 라 확인만 한다. 선례는 **155**(룰렛 «다이아만»)·**156**(퀘스트 다이아 전용) — 게이트도 그 꼴(«다른 재화가 되살아나면 빨강»)로 짠다. ⚠ **골드 칸 수량(20000·(i+1)²)을 그대로 다이아로 옮기지 마라** — 40단계에서 3,200만 다이아다. 곡선을 다시 잡되 **계수 확정은 199 로 넘긴다**(326·331 처리).
> · **399 (T2 · 398 과 한 벌)** — **70 출석 보상 `ATTEND` 28칸을 전부 다이아로**(골드 goldMul · 유물조각 · 28일차 `ic:'🔥'` 폐지). ⚑ **`verify155` §2 가 `ATTEND.some(r => r.goldMul)` 이 참이어야 통과하도록 못을 박아 뒀다**(104·108행) — 걷어내면 그 항이 곧바로 빨개지고, `giveReward` 의 `goldMul` 분기는 **실제로 죽는다**(그 표를 쓰는 곳이 ATTEND 하나뿐이다). 333 처방대로 **죽은 분기와 §2 항을 같이 걷어내되 자리는 비우지 마라.** 유물조각 일일 수입원이 하나 줄어드는 것은 **199 몫**으로 넘긴다.
>
> **순서·묶음 권장**: **397 → 398·399(한 세션)**. 397 이 먼저인 이유는 398 이 스테이지 패스 보상을 전부 보석으로 바꾸면 **두 패스 탭이 같은 아이콘을 쓰게 되어** 눌린 젬이 나란히 놓이기 때문이다. 상세 실측·처방·게이트 이관 목록은 각 PROGRESS 행(397·398·399)에 있다.

> # ✅ 완료(2026-08-29, sess-1633-16734 워커 B): 338 — 던전 보스 체력바가 격파 후에도 덜 깎여 있음
> **제품 4줄**(선언 2 + 그리기 2). ⚑ **등재문의 가설을 재현으로 기각한 것이 이 작업의 본체다** — `tools/probe338.js` 를 신설해
> 던전 8종 + 탑 2종 × 보스 1/2/3마리 × 동시/페이즈를 실제 진입점·실제 피해로 굴려 보니 **수리 전에도 격파 프레임의 폭이 예외 없이
> 574px = 가득**이었다(덜 찬 프레임 **0건**). 등재문의 처방(«격파 프레임에 폭 = 574 를 단언»)을 그대로 따랐으면 **이미 참인 것을
> 게이트로 굳히고 주인이 본 그림은 하나도 안 바뀌었을 것**이다.
> 결손은 «눈금이 안 따라온다» 가 아니라 **«눈금이 거꾸로다»** 였다 — 이 바는 레퍼런스에서 **39 보스전 체력바와 같은 부품**인데
> (측정표 30 §1 «보스 체력바» · §4-4 «두개골이 1px 이내로 같은 에셋»), 우리 게임의 그 부품 `#bossHp` 는 `hp/max` 로 **줄어드는데
> 던전만 차오르고** 있었다. 바 안 수치도 **255 이전 «누적 피해» 시절의 눈금**이 남아 있었다(LESSONS 295-②).
> ⇒ 폭·수치를 «남은 보스 체력» 으로 뒤집었고 `dunRunProg()` 는 **한 줄도 안 건드렸다**(등가식 `left = tot × (1 − prog)` — 축을 새로 안 만든다).
> **밸런스·판정 0줄 · 레이아웃 0px 변경.** `verify338` **179/179 신설** · `probe338` 신설 · 이관 `verify255` 140/140 · `verify257` 92/92 ·
> `verify188` 싱크 1행 · 회귀 8종(`verify331` **79/79 무수정** = «판정 0줄» 의 가장 짧은 증거) · **SMOKE PASS** · 측정표 30 **§9-2 정오표** 신설.
> 곁다리 관측 2건을 **343**(`verify188` 2건 게이트 부패) · **344**(`verify257` «최소 간격» 항 플레이키)로 등재했다 —
> 둘 다 **수리 전 커밋에서도 같은 점수**임을 직접 대조 실행으로 확인했다. 상세 `docs/review/338-던전보스체력바.md`.
>
> # ✅ 완료(2026-08-28, sess-1746-23625 워커 C): 341 — 03 던전 카드 «입장 가능» 레드닷
> **제품 0줄 · 레이아웃 Δ0px.** 338 규칙대로 처방을 따르기 전에 `tools/probe341.js` 로 재현했고
> **등재문의 가설이 기각됐다** — 노드도 CSS 도 **둘 다 이미 있었다**(`renderDunPage()` 24356 ·
> `.dnc .dot` 4706, 299 규약 주석까지). 부품 이름이 `.updot` 이 아니라 **`.dot`**(299 이전부터 있던
> 
> # ✅ 완료(2026-08-29, sess-2200-20742 워커 D): 364 · 365 · 366 — 13 재화 탭 광고 상품 (한 세션 묶음)
**제품 `index.html` 4자리** · 칸 내부 규격·행 pitch·§6 이하 좌표 **Δ0** · 밸런스 계수 0줄.
⚑ **364 는 «옮겼다» 가 아니라 «규약을 처음 지킨다» 였다** — `tools/probe364.js` 재현으로 수리 전 닷 중심이
카드 기준 **(0.81, 0.74) = 우하단**임을 실측했다. 329 가 초록이던 이유는 게이트가 사분면을 **버튼 상자 기준**으로
쟀기 때문이다(245×80 안에서는 우상단이 맞다) — 그래서 `verify299` 의 이 자리도 **카드 기준**으로 갈아 끼웠다.
자리는 카드 코너가 아니라 **헤더 띠 아래 크림 패널 우상단**(right 18 · top 84) — 코너는 `overflow:hidden`+`radius 35`
가 자르고 그 안쪽은 타이틀 잉크 자리다. 여유는 **세로(9.2px)에 몰았다**(가로는 3.67px — 두 축 다 좁으면 글자 한 자에 물린다).
365 는 6종 → **4종**(보석 ×100 · 유물 ×50 · 스킨 강화석 ×50 · 단련석 ×50), 격자 3열 → **2열 중앙**(x256/546, 칸·pitch 불변),
366 은 전부 **cap 5**(폴백 구조라 세이브 이관 없음 · 수량은 안 줄였다 — 일일 총량은 던전 1~2판 급).
⚑ **id 를 재사용하지 않았다**(a7·a8) — `S.daily.adBuy` 는 상품을 id 로만 기억해서, 지운 번호를 물려주면
«오늘 골드 상자를 받은 세이브» 가 새 상품을 **소진 상태로** 연다. 죽은 키는 `load()` 가 정리한다.
`verify364` **17/17 신설**(§R 되돌림 시험) · `verify365` **36/36 신설**(365·366 공용, 구 세이브 실로드 포함) ·
이관 `verify329` 26/26(+[G0]) · `verify299` 27/27 · `verify173` [F](폐기된 표본 → 살아 있는 표본으로, 333 처방) ·
회귀 `verify116` 41/41 · `verify153` 67/67 · `verify124` 57/57 · `verify166` 73/73 · `verify190` 31/31 · **SMOKE PASS**.
상세 `docs/review/364-366-광고상품개편.md` · 측정표 13 정오표 신설 · 교훈 4개 LESSONS 364·365·366.
⚠ **곁다리 등재 369(T1 «버그» — 등재 중 368 이 겹쳐 옮겼다)** — `verify173` A1(«주석 밖 «동료» 0건»)이 **2건으로 빨갛다**. 08 세부 팝업의
파생 문자열 둘(`index.html` 23369·23393)이 남은 것이고, **수리 전 커밋에서도 같은 2건**임을 대조로 확인했다
(내 변경과 무관 · 내 작업 단위가 아니라 안 고쳤다). 상세는 PROGRESS 369 행.

# ✅ 완료(2026-08-28, sess-2234-12735 워커 B): 368 — `verify348` [3-b] 좌끝 클램프
**제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정·밸런스 0줄.** 고친 것은 `tools/verify348.js` 한 파일이다.
⚑ **등재문 세 갈래 중 ⓑ 가 맞았다 — 제품은 내내 옳았다.** `probe368` 을 신설해 제품에게 직접 물었더니
`fxClampX` 는 띠 «안» 에서 좌변 **78.5**(= `sideBox.x2` 72.5 + 6)로 **밀고 있었다**. 빨간 것은 **표본이 놓인 자리**다 —
[3-a] «좌끝» 이 놓는 (30, VH/2) 에서 바 상변이 **440.5** 인데 띠는 **36..428**(+8 여유 = 436)이라 **4.5px 차이로 띠 밖**이다.
⇒ 그 자리의 24.0 은 결함이 아니라 **정답**이었다. **누가 옮겼는지는 세 커밋 대조로 못박았다**
(`0de7663` 348 당시 36..**442** · `5e25577^` 360 직전 36..**442** · 현재 36..**428**): **뿌리는 360**(주인 지시
«출석·축복 크기·간격 통일»)이 열을 **14px 줄인 것**이고, **360 은 잘못한 게 없다** — 상수를 박아 둔 쪽이 게이트다.
처방은 **자리를 상수에서 빼고 제품에게 묻는 것**(`shotBand()` 가 `sideBox` 에서 표본 행을 만든다 · 새 상수 0개).
⚠ **두 번째 함정을 §R2 가 잡았다 — 비네트**(`createRadialGradient(VW/2, VH/2, VH*0.34, …)` ~21012)가 화면 전체를 덮어
**중심에서 339px 밖의 바는 색이 어두워진다**(`#ff6b8a` → 249,104,135) ⇒ 350 의 «정확한 색» 자에는 **0개**로 읽힌다.
띠 한복판(232)은 클램프 **뒤** 자리는 안쪽인데 클램프 **전** 자리는 바깥이라 **양성항만 보이는** 모양이었다.
**무르게 풀지 않았음을 세 겹으로 못박았다** — **[전제 3-b]**(표본이 띠 밖이면 빨갛다) · **[3-d]** 음성항(띠 아래는 24)
· **§R2 되돌림 시험**(클램프를 뺀 사본은 좌변 11.5 < 72.5 = 열 위를 덮는다). **허용 오차는 한 칸도 안 넓혔다.**
`verify348` **33/34 → 38/38** · `probe368` **7/7 신설** · 회귀 무수정 `verify299` 27/27 · `verify166` 73/73 ·
`verify360` 42/42 · `verify338` 179/179 · `verify350` 37/37 · `verifyA2` 24/24 · `verify66` · **SMOKE PASS**.
곁다리 관측 1건을 **372**(`verify348` [R-b] 플레이키)로 등재 — **수리 전 커밋에서도 같은 값으로 흔들림**을
대조로 확인했고(338·344 규칙), 가설 둘(플레이어 가림 · 보스 등장 연출)은 **직접 대조로 기각**했다.
상세 `docs/review/368-verify348좌끝클램프.md` · 교훈 LESSONS 368(4개).
⚑ **캔버스 픽셀을 «정확한 색» 으로 세는 다음 세션에게**: 그 자리가 비네트 안쪽(중심에서 `VH*0.34`)인지부터 확인하라.


# 신규 주인 지시(2026-08-29) 2건: 366 광고 상품 전부 «하루 5회»(365 와 한 세션 — cap 폴백이라 이관 불요) · 367 룰렛 «3회 무료 + 2회 광고»(ROUL_FREE 분리·▶AD 표기·124/190 이용권 생략 규약 — verify267/321/29 이관)
> 상세는 각 PROGRESS 행.
> 
> # 신규 주인 지시(2026-08-29): 365 — 광고 상품 4종(보석·유물·스킨 강화석·단련석)으로 개편, 잔재 상품 제거
> COIN_ADS 실측·격자 2행 재배치·356 비율 정책 정합·게이트 이관은 PROGRESS 365 행. **364(닷 위치)와 같은 화면 — 한 세션 묶음 처리 권장.**
> 
> # 신규 주인 보고(2026-08-29): 364 — 13 재화 광고 상품 레드닷 위치 어정쩡([받기] 버튼 안 글자 옆) → 카드 안쪽 우상단으로
> kkkkkk-30 이 probe364 로 실측 완료(x226,y229/278×309 — 우하단). 325 안쪽 우상단 선례·verify299 호스트 기준 교정 포함, 상세는 PROGRESS 364 행. 363 과 같은 급 순위.
> 
> # ✅ 완료(2026-08-28, sess-2146-28487 워커 C): 363 — 12 소환 결과 «연출 스킵» 토글
> **제품 6자리 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 338 규칙대로 처방 전에 `tools/probe363.js` 로 재현했고
> **등재문 가설이 그대로 확인됐다**(338·341 은 기각됐던 자리다) — 수리 전 30연은 전 칸이 최종 상태에 닿기까지
> **2047ms**(63프레임 중 58이 미완성) · 10연도 940ms. 수리 후 같은 명령이 **24~36ms · 미완성 프레임 0**.
> ⚑ **본체는 «표시가 하나로는 모자랐다» 였다** — 252 는 `fx-pop` 을 «내 등장은 내가 건다» 는 표시로 써서 60 쥬시
> 스태거를 막는데, 스킵이 그 표시를 떼자 쥬시가 그것을 «등장 연출이 **없는** 그리드» 로 읽고 **자기 스태거를 대신
> 얹었다**(`.jz-st` 최대 535ms) = 스킵을 켰는데 연출이 반쯤 살아 있었다. «내가 건다» 와 «아무것도 안 건다» 는
> 다른 말이라 표시도 둘이어야 한다 → **`jz-x` = «손대지 마라»** 신설 + `jzStagger` 가드 한 항.
> ⚑ **자리를 고른 것은 취향이 아니라 기하다** — 넓어 보이는 «버튼 하변↔닫기» 띠는 2280 에서 209px 이지만
> **1600 프레임에서 12px** 로 줄어(짧은 프레임 보호항이 둘을 1416↔1431 로 붙인다) 못 쓴다. 쓴 자리는
> **패널 아래 여백**(크롬 15 ↔ 그리드 98 사이 83px — 프레임과 무관하게 같다). 84 앵커·327 패널 전부 Δ0.
> ⚠ **버스트는 «지연 0» 이 아니라 접었다** — 30칸이면 파티클 420개가 한 프레임에 터져 «스킵» 이 아니라 더 센
> 연출이 된다(소리는 98 규약대로 flip 5→1 · rare n→1). 등급은 카드 면색이 상시로 말해 정보 손실 0.
> `verify363` **80/80 신설** · `probe363` 8/8 신설 · 회귀 9종(12·84·327·252·187·102·106·195·46) **전부 무수정** ·
> **SMOKE PASS**. 상세 `docs/review/363-소환결과연출스킵.md` · 교훈 LESSONS 363(4개).
> ⚑ **다음 세션이 볼 것 둘** — ① 세이브 이관을 `reload` 로 재면 못 잰다(`beforeunload → save()` 가 지운 키를
> 되살린다). `load()` 를 직접 부르되 그 함수는 상태가 아니라 **`d.time`** 을 돌려준다(17542·17554).
> ② `docs/UI-REFERENCE.md` 에 **커밋돼 있던 병합 충돌 표시 3줄**(`bfa6966 done(348)`)을 걷어냈다 —
> 지시서 [4] 대로 **양쪽(346·348 체크리스트)을 모두 살렸고** 내용 삭제는 0줄이다.
> 
> # 신규 주인 지시(2026-08-29) 4건: 356~359
> · **356** 아이콘 원본 비율 전수(비균등 scaleX 금지 — 정책 전환, 라벨 글자는 예외) + 비평 루프로 «찌그러진 아이콘 0건» 채점 · **357** 02 메인 스킬 슬롯 아이콘 우측 밀림 → 중앙 교정 «버그» · ~~**358** 플레이어 이속 증가 효과 전면 제거(UPG spd·유물 등 전부 — **359 보다 먼저**)~~ — **완료(2026-08-29, sess-2033-23950 워커 B)**: 재현이 «유물이든 뭐나» 를 **하나로 좁혔다** — 유물/도감/축복의 «속도» 는 전부 **공격 속도**이고 `bonus()` 에 이동 축이 없다. 이동을 올리는 것은 **UPG `spd` 하나뿐**(수리 전 Lv20 에서 115 → 205, 그리고 «강화» 탭에서 **골드로 실제로 살 수 있었다**). 행을 지우고 `stat.speed` 를 상수 `PLAYER_SPEED = 115`(구 Lv0 값)로 고정 — 밸런스 모델의 «훈련만» 플레이어는 spd 를 원래 안 사므로 **설계 기준선 Δ0**(sim 4종 같은 값 PASS, 계수 0줄). 20 프로필 스펙 «이동 속도» 행은 **레퍼런스 줄이라 살리고 값만 영구 0%**. `verify358` 63/63 신설 · `probe358` 신설 · `verify112`·`verify168` 이관 · 회귀 6종 무수정 · SMOKE PASS. ⚑ **359 에게**: 지금 `BOSS_CHASE=1.08` 은 «플레이어보다 1.08배 빠름» 이다 — 1 아래로 내리면 66 이 그 값을 정한 근거(«1.0 이하면 30초 안에 한 번도 못 붙는다»)를 **대시 공격이 메워야** 한다 · **359** 보스 속도 상향(플레이어보다 살짝 느리게) + 보스·일반 적 «대시 공격» 신설(예고 후 돌진·쿨다운, 66/172/233/234 게이트 이관 필수).
> 순서 권장: 버그(357) → 358 → 359 → 356(전수 루프, 351 과 병행 가능). 상세는 각 PROGRESS 행.
> 
> # 신규 주인 지시(2026-08-29): 354 — 좌측 사이드 «출석»(.solo 단독 규격)·«축복»(--sx 1.414) 을 다른 버튼들과 크기·간격 통일
> 주인 지시가 레퍼런스보다 우선(의도적 이탈 — A2 측정표 정오표 필수). 출석 라벨 신설·표준 그리드·배지 위치 재확인 — 상세는 PROGRESS 354 행. 350·353 다음 순위.
> 부품 — 299 는 자리만 옮기고 이름은 뒀다)이라 `grep updot` 에 안 걸렸을 뿐이다.
> 비평가 AP·AQ 가 **2인 독립으로 일치**해 본 «8장 전부 0개» 는 **그 상태에서 정상**이었다 —
> 166 조건 셋 중 **전투력 축**을 `cap72.js` 가 안 세워 부팅 세이브 `cp()=505` 로는 해금된 두 던전
> (요구 2500·4000)조차 못 넘겼다. ⇒ 고친 곳은 **`tools/cap72.js` 한 곳**, 값은 상수가 아니라
> «해금 던전이 요구하는 전투력» 역산(336 처방 · `trainCap()` 안에서만 올려 세이브로서 합법).
> 결과는 ref 와 같은 그림(카드 1·2 만 점등). `verify341` **32/32 신설**([전제] 절을 본체와 갈라
> «부팅 직후 8장 소등이 정상» 을 못박았다 — 유령이 돌아온 자리는 항상 거기다) · `probe341` 5/5 신설 ·
> 회귀 6종(`verify72` 267/267 · `verify121` 149/149 · `verify166` 73/73 · `verify299` 27/27 ·
> `verify46` 70/70 · `verify113` 82/82) **전부 무수정** · SMOKE PASS.
> 상세 `docs/review/341-던전카드입장가능레드닷.md` · 교훈 7개 LESSONS 341.
> ⚑ **342 를 잡는 워커에게**: 342 도 **같은 회차·같은 두 비평가의 같은 캡처**에서 나왔다.
> 341 이 그 캡처의 상태 축 하나를 고쳤으니 **다시 찍고 시작해라**. 그리고 335 의 «앵커가 둘» 표
> (카드 리스트 `ref y − 84` · 서브탭 바·앱 탭바 `ref y − 60`)를 비평가에게 반드시 줄 것.
>
> # 신규 T2 주인 지시(2026-08-29): 339 — 던전 «연속 도전» 실동작
> 기존 04 세부 팝업의 체크박스는 **표시 전용**이었다(`dgdAutoOn` «자동 반복 미구현»). 체크 시 31 클리어 화면에도 토글 + «5초 뒤 다음 층 자동 도전» 카운트다운 → 자동 입장. 중단 조건(토글 해제·닫기·입장권 소진)·소탕 판단·게이트는 PROGRESS 339 행. **큐: ~~338(T1) 완료~~ → 339(T2) → 337(T4) → 199.** (338·339 를 한 세션에 묶는 것은 허용이었으나 338 은 단독으로 끝났다 — 339 는 그대로 남아 있다.)
> ⚑ **339 를 잡는 세션에게**: 338 이 «던전 런 HUD 의 바 = **남은 보스 체력**» 으로 뜻을 굳혔다(측정표 30 §9-2).
> 31 클리어 화면에 카운트다운을 얹을 때 그 바를 다시 읽지 마라 — 격파 시점에 **0px 로 굳어 있는 것이 정상**이다.
> 
> # 주인 지시(2026-08-29) 큐 개편: 199 보류 · 351 신설
> · **199(최종 밸런스) 는 ⏸ 보류 — 선점 금지**(«일단 todo 에서 빼보고»). 이관돼 있던 계수 확정·ck199 대기 항목은 보존.
> · **351 — 9:13.3(1080×1600) 가독성 폴리시 루프, 비평가 «3명»** 신설(주인 보강 2026-08-29): 화면마다 **9:19(2280)·9:13.3(1600) 캡처를 짝으로** 찍어 3명에게 둘 다 주고, «**9:19 대비 9:13.3 에서 나빠진 것**» 만 잡는다(9:19 에도 있는 문제는 별도 등재). 잘림·겹침·글자 가독·조작성 4축, 3명 최저 ≥8 통과, smoke 오프너 목록 순회·화면당 10회 준용, 수정 후 재캡처도 짝으로(9:19 회귀 확인). 상세는 PROGRESS 351 행. **현재 T1(345·349)·버그(350)·기존 대기(346·348·341·342·344·347·337)가 소진되면 351 루프로 들어간다** — 폴리시 자리(구 199 자리)를 이것이 대신한다.
> 
> # ✅ 완료(2026-08-28, sess-1848-8948 워커 C): 350 — 보스전 HUD 파란 원(.kboss) 삐져나옴
> **제품 1줄**(`#stinfo.bfight` 숨김 목록에 `.kboss` 한 항 추가) · 레이아웃 Δ0px · 판정·밸런스 0줄.
> ⚑ **338·341 과 달리 등재문 가설이 그대로 확인됐다** — `tools/probe350.js` 신설로 재현: `.kboss`(699..781 / 245..327 Ø82)가
> 보스 체력바(190..890 / 231..298) 뒤에 남아 **하변 아래로 정확히 29px** 삐져나오고, 가로로는 바 안쪽(겹침 82px)이다.
> 세 모드(stage·raid·promo = BOSS_HUD28) 전부 같은 값. **처방 ③(«다른 요소일 가능성»)은 «찍힌 픽셀» 로 기각** —
> 바 밑이 원판 #2A3E81 → 링 #4CBAED → 테 #141414 로 `.kboss` 3중 원판 순서 그대로다(주인 원문 «파란거» = 시안 링).
> 뿌리는 새 결정이 아니라 **규칙이 안 따라온 것**이다 — 측정표 28 «해골 노드» 행은 39 를 이미
> «**없음**(체력바 끝 소형 두개골 46×42 로 대체)» 이라 적어 뒀다. ⚠ 클래스가 `.knode` 가 아니라 `.kboss` 라
> 숨김 목록에서 한 항이 통째로 빠져 있었고, `.bfarm` 은 같은 노드를 **중앙 Ø120 으로 쓰는** 설계(40)라 `.bfight` 쪽만 껐다.
> `verify350` **37/37 신설** · `probe350` **12/12 신설** · 회귀 10종 무수정(161·162·184·208·273·284·46·30·331·338) · **SMOKE PASS**.
> 상세 `docs/review/350-보스전HUD파란원.md` · 교훈 6개 LESSONS 350.
> ⚑ **표시 전용 HUD 를 캡처로 가리는 다음 세션에게**: `#stinfo`·`#bossHp` 는 `pointer-events:none` 이라
> `elementFromPoint` 가 그대로 캔버스로 빠진다(1회차에 «kboss 표본 0/10» 이 나와 정반대 결론을 낼 뻔했다) —
> 캡처를 data URL 로 페이지에 되돌려 **찍힌 픽셀**을 읽고, 60 쥬시가 걷힌 뒤 rect·캡처를 **같은 순간**에 잡아라.
> 
> # ⛔ 승인 프롬프트 금지(2026-08-30, 주인 지시 «항상 허용·자동»): 이 환경엔 사람이 없다 — 승인이 뜨는 행동은 세션을 멈춘 채 버린다.
> · `.git/` 내부(objects·pack·tmp_pack_*)는 **읽기 외 절대 손대지 마라**(워커 A 가 2026-08-29 19:23Z 에 `rm tmp_pack_*` 로 «민감 파일 편집» 승인에 걸려 11분 멈췄다). 디스크가 차도 그대로 둔다.
> · fetch/pull 이 2분 타임아웃이면 죽이지 말고 `run_in_background` + timeout 600000 으로 한 번만 재시도, 그동안 지시서는 GitHub 도구로 읽는다. 뿌리는 저장소 pack **3.25 GiB**(과거 스크린샷 커밋 이력) — 역사 정리는 주인 판단 사항이라 워커가 손대지 않는다.
>
> # ⛔⛔ 정지 상태 해소(2026-08-31 13:55) — **199 밸런스 루프를 «지금 즉시» 시작하라.** 주인 보고: «루프 왜 안하고 잇는데». **실측**: 미착수 **0건** · lock **1개(356)** · **13:05Z 이후 50분간 커밋 0건**(워커 전부 정지) · 199 회차 **0/9**(한 번도 안 돌았다). **뿌리 = 데드락**: ⓐ 199 착수 조건이 «lock 0 + 미착수 0» 인데 **356 의 회차 상한이 `∞`** 라 **정의상 그 lock 은 안 풀린다** ⓑ 미착수가 0 이라 다른 워커는 티어 스캔에서 잡을 게 없어 그냥 종료한다. **⇒ 규칙 개정(주인 의도 불변 — «다 하고 나서» 는 «끝나는 일» 을 가리킨 말이지 `∞` 루프를 기다리라는 뜻이 아니다)**: **① 회차 상한이 `∞` 인 폴리시 루프(`351` 18/∞ · `356` 14/∞)의 lock 은 199 착수를 막지 않는다 — «다른 lock» 수에서 뺀다.** ② 나머지 조건(유한 회차 lock 0 + 미착수 0)은 **이미 충족**돼 있다. **③ 다음 워커는 티어 스캔에 미착수가 없어도 종료하지 말고 `199.lock` 을 잡고 루프를 시작한다.** ④ 199 와 351·356 은 평행 진행 가능 — 축이 다르다. 단 **199 는 계수(상수 표)만, 폴리시 루프는 CSS·자산만** 만져 파일 구간이 겹치지 않게 한다. ⚑ 199 루프 규약(확정): 비평가 **3명 독립** · **3명 모두 ≥ 9/10** · 상한 **20회차** · 회차마다 `tools/bot199.js`(494, ✅ 완료) 재실행 표 첨부. ⚑ 화해시킬 이관 **39행**과 충돌 짝(501·502 ↔ 541·580 / **589 결제 목업 ↔ 497·498·588** / 513 ↔ 498 / 517 ↔ 504·484·472·485)은 PROGRESS 199 행에 이미 적혀 있다.
>
> # ⚑⚑ 주인 재확인(2026-08-31 09:10) — **199 밸런스: 순서 그대로 · 루프로**. 원문: «쨋든 다하고 밸런스 하냐? **다하고나서 밸런스하면되긴함**. **루프로 밸런스**». 선행 도구 `494`(`tools/bot199.js` 봇 플레이어)는 ✅ 완료. 루프 규약도 확정돼 있다(비평가 3명 · 3명 모두 ≥9/10 · 상한 20회차 · 회차마다 봇 표). ⚠⚠ **막힘은 «착수 조건이 한 번도 충족되지 않는다» 는 것**이다 — «lock 0 + 미착수 0» 인데 워커 8~9대가 상시 lock 을 쥐고 곁다리를 계속 등재한다(하루에 번호 543 → 605). 실측 09:00: lock 9 · 미착수 9 · **199 이관 39행**. **⇒ 판정 규칙을 199 행에 박았다**(순서 지시를 바꾸는 게 아니라 실행되게 하는 절차): ① 판정은 **매 `unclaim` 직후** — 조건이 맞으면 **그 자리에서 199 선점**(티어 스캔까지 미루면 새 lock 이 생겨 또 막힌다) ② **199 선점 뒤의 새 곁다리는 199 를 막지 않는다**(«199 이후» 로 표시, 루프는 계속) ③ 미착수 ≤ 2 면 새 곁다리는 **등재만 하고 선점하지 않는다**. ⚑ **199 가 먼저 볼 짝**(방향이 반대이거나 서로를 무의미하게 만든다): 501·502 ↔ 541·580 · **589 결제 목업 ↔ 497·498·588**(목업 동안 유료 축은 밸런스 근거가 아니다) · 513 ↔ 498 · 517 ↔ 504·484·472·485. 상세 PROGRESS 199 행.
>
> # 신규 주인 지시(2026-08-31 08:05): 592 **적 죽을 때 골드 코인 연출 폐지** 「T2」. 원문: «적죽을때 골드 이펙트 뜨는거 빼줘 **안쓸래 그거**». 자리는 `killEnemy`(21782)의 **`fxAt(fxWorld(e.x, e.y − e.r), 'combat')`**(21789) 한 줄 — 그 발원점을 찍고 다음 줄 `S.gold += g` 를 `fxWatch` 가 잡아 코인을 쏜다. ⚠ **`'combat'` 태그 자리는 셋**(⑴ 21789 킬 드랍 ⑵ 23283 스테이지 클리어 보너스 ⑶ 23309) — **주인이 지목한 것은 ⑴ 하나**다. 스위치를 한 상수로 모으되 기본값은 ⑴만 끔. ⚠ **`burst(e.x, …, e.T.col, …)`(21795)은 지우지 마라** — 재화가 아니라 **적 색 사망 파티클**이다. 같이 지우면 적이 소리 없이 사라진다. ⚠⚠ **`verify518` ⑸ «전투 화면의 골드 연출은 수리 전과 동일» 을 정면으로 뒤집는다** — 333 처방대로 **방향을 뒤집어 갈아 끼워라**(«킬 드랍 코인 0»). **그 항을 그냥 지우면 반려.** ⚠ **죽은 코드 금지**(295-②·399·460) — ⑴을 끄면 `#fxlc`·`FXFLY_MAX_C`·`fxOrigBurstT`·combat 롤링 분기의 소비처가 준다. 0 이 되는 선언은 **선언째** 걷어낸다. ⚑ 부수 이득: 연속 킬마다 DOM 을 만들던 경로가 사라져 `#fxlc` 노드·`FXMAX` 압력·프레임 시간이 좋아진다 — 전·후로 재서 적어라. ⚠ **583 과 같은 게이트(`verify518`)를 만진다** — 선점 겹침 주의. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-31 07:40): 590 **펫 크기 ×2** 「T3」. 원문: «펫 크기도 2배로 하라해». ⇒ **541 이 만든 축에 펫만 빠져 있다** — `PLAYER_DRAW_SC 1.5`·`MOB_DRAW_SC 1.2`·`SK_DRAW_SC 2`·`unitSc()`(19802~19805)를 펫 그리기(23579 `drawFrameC(…, p.sp.scale, …)`)는 **하나도 안 지난다**. 처방: **`PET_DRAW_SC = 2` 를 그 셋 옆에 선언하고 그리기 입구에서 곱한다** — `PET_SP[].scale`(0.34/0.16/0.62) 표 값을 **손으로 2배 하지 마라**(541 ① 규약). ⚠ `unitSc()` 는 인자가 ETYPE 키라 펫에 못 쓴다 — 억지로 합치면 잡몹·아레나·보스 세 갈래가 흔들린다. **별도 상수 + 그리기 한 곳**이 맞다. ⚑ **밸런스 파급 없음** — 펫은 피격 대상이 아니고 전투 파라미터가 `PET_CD` 하나뿐이다(481). 541 과 달리 **199 이관 불필요**. ⚠ **587 과 같은 표를 만진다**(`PET_SP[].faceRight` — 587 이 그것을 «아틀라스 축» 으로 승격한다) — **선점 겹침 금지**. ⚠ 3마리 + 플레이어(이미 ×1.5) 겹침을 실측하고, 가리면 **크기를 되깎지 말고 따라다니는 자리를 벌려라**. 상세 PROGRESS 행.
>
> # ⚑⚑ 신규 주인 지시 묶음(2026-08-31 07:10) — **17건 등재: 580~589 + 471 재개**. 원문은 각 PROGRESS 행에 그대로 있다. 티어 순 권장 착수: **T1 «버그» 3건 먼저** — **587** 스프라이트가 이동 방향과 **반대**(뿌리 둘: ⓐ 플레이어가 «이동» 이 아니라 «조준(`player.aim`)» 을 본다 — 카이팅 게임이라 상시 반대 ⓑ 아틀라스별 원본 방향 표가 **펫에만** 있다(`faceRight`) · 플레이어 `> 0` · 적 `dx < 0` 하드코딩) · **586** 던전 팝업 열 때 깜빡임(후보 6개 — **재현으로 갈래 확정 먼저**) · **471 재개**(주인: «슬롯 빨간점 위치 여전히 좆같음» — 보류 사유였던 «주인 판단» 이 해소됐다. 상한 5/5 → 5/10, review §10 의 남은 4+1 개부터). **그다음 T2** — **580** 이동속도 ×2(⚠ **541 과 합쳐 199 이관** — 크기 +32% 위에 속도 2배가 겹친다 · 터널링 실측 필수) · **583** 강화 화폐 연출(훈련=골드·단련=단련석·룬=룬강화석 · `−n` 금액 제거하되 **43회차의 «움푹» 은 남긴다** · ⚠⚠ **518 게이트와 정면 충돌 — «소모 vs 획득» 방향 축으로 갈아 끼우는 것이 본체**, 무르게 풀면 반려) · **588+589 한 벌**(이용권 다이아 «대체가» 제거 + **결제 목업**: 주인 «클릭시 걍 결제된거로 쳐주기 … 우편함으로 보내줘야함 … 우편함확인하라고 해주기». ⇒ **153 의 `sendMail()` 이 절반을 이미 만들어 뒀다** — 이 행은 «빠진 자리 편입» 이다. ⚠ 19715 의 «목업을 만들지 않는다» 방침 **번복** · ⚠ `S.mailx` 무한 증가 · ⚠ **199 에 «목업 기간 유료 축은 밸런스 근거가 아니다» 를 적어 넘길 것**). **T3(부품 통일·크기)** — **581** 도감 [강화]·축복 [받기]를 공용 `.ifbtn` 로(**색을 칠하지 말고 부품을 바꿔라** — 주인이 지목한 «일괄 받기 버튼» 이 그 부품이고 그 토큰이 곧 초록) · **582** 승급 계열 통일(179 코스튬 프레임 = 공용 슬롯 부품 · 182 = 179 톤. ⚠ 179 는 515 가, 182 는 518 이 방금 만졌다) · **584** 23 훈련 팝업 부품 크기·라벨([투자]→**[단련]** + 버튼 안 화폐 아이콘 · `.tr-up` 108×107 vs 같은 팝업 `.rbt` 420×112) · **585** 던전 화폐 크기(584 와 같은 기준선). ⚠ **같은 화면 선점 충돌 주의**: 583↔584(23 훈련 3탭) · 585↔586(03 던전) · 581↔582↔471(닷·버튼 토큰) · 588↔589↔477(결제 표기).
>
> # ⚑ 신규 주인 지시(2026-08-31 06:20): 543 **재화 흡수 알갱이 — 다이아 기준 ×3** 「T2」. 원문: «재화 흡수 이펙트 알갱이 크기 3배 … 다이아 크기가 너무 작아보여서 … 골드 크기랑 같은 건가 … 다이아 크기 기준 3배로». **주인 추측이 맞다** — `FXCUR` 의 `ics`·`fs` 가 골드·다이아 둘 다 55/46 동일. ⚑ **작아 보이는 진짜 뿌리는 이 파일이 이미 적어 놨다**(3416~3418): HUD 는 다이아에 `scaleX(1.16)`(세로로 긴 보석을 가로로 편다)을 거는데 «`.fx-fly`·`.fx-lit` 는 그 밖이라 보정을 통째로 잃는다» 고 진단해 놓고 **골드만** 되찾아 줬다(3421 `scale(1.0365)`). ⇒ 비행에서 다이아만 좁은 채로 난다. ⚠ **갈림길**: ⓐ 박스 3배(`ics` 55→165) vs ⓑ **다이아 «잉크» 3배 + 골드를 다이아와 같은 시각 크기로 정렬**. **권장 ⓑ**(주인 문장의 인과가 «다이아가 작아 보여서 → 3배»). 단 **잉크 실측 없이 고르지 마라.** ⚠⚠ **크기만 올리고 끝내면 반려**: 알갱이 하나가 `FRAME_W` 1080 의 **15.3%** 가 되고 UI 발은 **8~16개 × 2재화**다 — 개수 · 밴드 밀어내기(30회차 «잉크 32.5» 기준) · `FXFLY_MAX 32`/`FXMAX 120` · `fxBurst` 드롭 **네 가지를 같이 재산정**. 93 의 도착 스프레드·롤링 계단은 개수에 물려 있다. ⚠ `curIc()` 가 width 를 **인라인**으로 박으므로 CSS width 로는 못 이긴다 — 골드처럼 **transform** 으로 준다. 13 재화 탭 제외 가드(`#app:not(:has(#shopw.on .shp-list.coin))`)를 그대로 따라갈 것. ⚠ **518 과 같은 파일 구간**(`fxFly`/`FXCUR`) — `sess-1936-12590` 선점 중이니 겹치지 마라. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 지시(2026-08-31 05:45): 541 **전투 화면 크기 — 플레이어 ×1.5 · 스킬 ×2 · 잡몹 ×1.2 · 보스는 그대로** 「T2」. 원문: «플레이어 크기 1.5배 키우고 스킬들 크기 2배 키우기 적들 크기 1.2배 키우기. 보스는 그대로». ⚑ **`ETYPE.boss` 를 안 건드리면 승급 수호자·던전 보스도 자동으로 그대로다**(`bossDrawnH()`/`bossRK()` 에서 역산 — `promo` 의 4.84/66 은 폴백일 뿐). ⚠⚠ **갈림길**: 그림만인가 판정까지인가. 권장 — 플레이어·잡몹은 그림+판정을 **같이**(그림↔판정 일치 규약, `bossRK()` 주석), **스킬은 그림만** 2배(장판 `r` 은 피해 판정 겸용이라 2배면 **면적 4배**). 배율은 **상수 3개**(`PLAYER_DRAW_SC`/`MOB_DRAW_SC`/`SK_DRAW_SC`)로 뽑고 `ETYPE` 표 값을 손으로 곱하지 마라. ⚠⚠ **지뢰 둘**: ⓐ `sim249` 가 `boss: {` 뒤 **320자 안**에서 `hp:`·`gold:` 를 정규식으로 읽는다 — **그 근처에 주석 넣으면 즉사**(285 전례) ⓑ `arena`(123 도전자)는 «플레이어와 같게» 맞춘 항이라 **플레이어를 따라가야** «같은 종족 1:1» 이 산다. ⚠ **199 이관 필수 — 방향이 정반대다**: 방금 끝난 501·502 가 «잡몹 구간에서 너무 죽는다» 를 완화했는데, 이 행은 피격 거리 `e.r + player.r + 6` 을 39 → 51.6(**+32%**)로 벌린다. 크기는 지시대로 하되 **난이도 상쇄를 이 행에서 임의로 하지 마라.** 상세 PROGRESS 행.
>
> # ⚑⚑ 주인 재촉(2026-08-31 04:15) — ~~**514 룰렛 «연출 스킵» 토글이 아직 아무도 안 잡았다.**~~ **완료(2026-08-30, sess-2006-25886 워커 E · 1회차)** — 363 의 `.sm-sk` 에서 **자리만 빼** 공용 부품으로 승격(새 부품 0개 · CSS 자리 2줄 + 색 2줄)하고 상태를 `S.opt.fxSkip` **한 벌**로 넓혔다(구 `sumSkip` 이관 한 줄 · KEY 불변). 스킵 ON 이면 **3913ms → 42ms**, 회전 한복판에 켜도 그 자리에서 끝난다. ⚠ Δ0px 자리가 없어(빈 띠 전수 실측) 팝업이 **위로 32.2px** 올라갔다 — 측정표 265 정오표. `verify514` 64/64 신설 · `verify363` 80/80 이관 · 회귀 12종 무수정 · SMOKE PASS. 곁다리 **544** 등재(`verify181` [B]). ↓ 아래는 재촉문(보존). 원문: «룰렛 연출스킵 토긓 넣으라 햇는데 넣엇나». 주인이 **세 번째** 말한 항목이고(03:30 등재 · 그 전에 두 번), 소스에 `roulSkip`/`fxSkip` **0건**·`sumSkipOn` 3건(12 소환 전용)으로 **미착수**임을 확인했다. **T2 → 「⚑ 우선」으로 올린다** — 다음으로 비는 워커가 잡는다. 크기는 작다(363 `.sm-sk` 부품 **공용 승격** + 상태 하나 + 스킵 시 `roulFinish(idx)` 즉시 — 새 부품을 짜면 반려). ⚠ 같은 재촉에서 **518**(«골드 이펙트 아직도 안 고쳐졌네») 도 확인했다: `sess-1838-12782` 가 18:39Z 부터 잡고 있고 lock 은 살아 있다 — **뺏지 말고**, 그 세션은 «전 화면 0» 루프를 끝까지 돌 것. 상세 PROGRESS 행.
>
> # ⚑⚑ 신규 주인 보고(2026-08-31 03:40, **재차·강한 어조**): 518 **«쌩뚱맞은 곳에서 골드 이펙트가 터진다» — 전수 루프** 「T1 최우선」. 원문: «소환결과에서 골드를 안얻는데 골드얻는 효과나옴 … 재화 얻는 상황 아닌데 얻는 효과 나오는거도 싹다 안나오게 고쳐» + «자꾸 **장비 일괄강화**할때도 골드 얻는 이펙트 뜨고 아주 좆같음. 그거들 싹다 잡아라 **루프 돌려서라도 꼭 잡아라**. 쌩뚱맞는곳에서 골드이펙트 터지는거 싹다 잡아». 유력 뿌리 둘: ⓐ `fxUpOk`(34533) **앰버 파티클 `#FFC02E`** 가 `FXCUR.gold.col`(`#FFDE6A`)·코인과 색이 겹친다 — 그런데 일괄 강화는 골드를 **쓰는** 자리다(182 주석: `fxUpOk` 는 09·12·17 과 **같은 한 세트** ⇒ 한 줄이 네 화면을 움직인다) ⓑ 팝업이 떠 있는 동안 **배경 전투 골드**를 `fxWatch` 가 잡아 `.cGold`(#top, 팝업 **위**)로 쏜다. **⚑ 루프 필수**(351 꼴 · 오프너 전 화면 · 비평가 2인 · **전 화면 0 까지**) — «한 자리 고치고 완료» 는 반려. **512 과 한 세션 권장.** ⚠ 재현 전에 코드 고치지 마라(전투 발 연출을 죽이면 58/77/93 이 무너진다). 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-31 03:35): 519 **단련 탭 레드닷 오점등** 「T1 버그·거짓 신호」. 원문: «단련할꺼가 없는데 단련탭에 빨간점 알림뜨더라 … 단련할거 잇을때나 알림떠야지». 뿌리는 31792 `temperAlert()` 의 첫 항 — `TEMPER_PT_COST = 1`(18756) 이라 사실상 «단련석 1개라도 있으면» 상시 참인데, 전환해 봐야 올릴 축이 없으면 누를 것이 없다. 처방: 전환+투자를 **한 판정**으로(`pts = temperPts() + floor(tstone/COST)` 로 올릴 축이 있는가). 2차 용의자는 **166 특이성 함정**(도감 탭·훈련 배수 탭에서 이미 두 번) — `.alert` 를 뗀 상태에서도 닷이 찍히는지 반드시 확인. **516 와 한 벌**(같은 축의 반대편). 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-31 03:30) 4건: **514** 룰렛 «연출 스킵» 토글 — **363 의 `.sm-sk` 부품을 공용 승격**(새로 짜면 반려) · 상태는 하나로(`S.opt.sumSkip` → `fxSkip`, 이관 한 줄) · 주인이 **두 번** 말했다 「T2 우선」 · **515** 승급전 «권장 스테이지» 한 줄 **가운데 정렬** — CSS 2117 `.pr-cond{text-align:left}` 하나. 상자 기하·2125 `<b>` 색 특이도는 **불변** 「T3, 516 와 묶어도 됨」 · **516** 도감 [강화]·[일괄 강화] **레드닷** — 283 이 «세 자리 전부» 켰다고 적혀 있으니 **재현이 먼저**. 유력 용의자는 166 특이성 함정(이번엔 **반대 방향** = 한 번도 안 켜짐) · 개별 `[강화]`(`claimColl`)는 283 범위 밖이었을 수 있다. **판정식 새로 짜면 반려**(`canLevel`/`upReady`/`collReady` 그대로) ⚠ **`471.lock` 이 풀린 뒤 착수**(같은 코너 규약 11px 을 만진다) 「T2 우선」 · ~~**517** 훈련 단계 필요 경험치 **1~4단계 300 · 5~7단계 600 · 8 이후 900**(3종 합) = **326 번복**~~ — **완료(2026-08-31, sess-2159-12315 워커 D · 1회차)**. `TRAIN_NEED` 구간표 + 접근자 한 벌(500 선례) · 옛 상수는 이름째 삭제. ⚑ **재현이 등재문을 한 칸 정정했다** — 넘치는 세이브는 «9단계» 가 아니라 **2단계 이상 전부**이고, 이관을 안 하면 [↑] 가 **연쇄로** 열린다(단계 4 → 10, 6회). **그 연쇄의 끝이 곧 «자연 단계»** 라 이관(`trainStageFor`)은 선물이 아니라 그 6번을 로드 시점에 접는 것이다 — 483 의 «내리는» 방향도 같은 한 줄에 산다. ⚠ **KEY 는 안 올렸다**(등재문 권장과 다름 — 키 하나로 읽는 `load()` 에서 KEY 상승 = 구 세이브 전멸, LESSONS 44-②). ⚑ **326 이 199 로 넘긴 4칸이 계수 0줄로 닫혔다**(`sim177` ③⑧ · `sim249` ⑤⑥ PASS · `sim112` «s80 ≈ 4단계» 복귀). 새 축(8단계 이후 고정 몫 → 단계 선형 무한 상승 → `TRAIN_BONUS` 발산)은 **199 이관**. `verify517` 119/119 신설 · `verify326` 90/90 이관 · SMOKE PASS. 상세 `docs/review/517-훈련단계필요경험치구간표.md`. `TRAIN_CAP_STEP` 상수 → `TRAIN_NEED` 구간표 + 접근자(**500** 축복 표와 같은 길) ⚠⚠ **세이브 이관이 «없음» 이 아니다 — 326 과 반대로 상한이 작아진다**(9단계 스탯당 4,500 → 1,600): 그대로 두면 `trainReady()` 가 즉시 참이 되어 **단계 폭등**. 이관 규칙(권장: 레벨 안 깎고 단계를 맞춰 올림 · KEY 상승)을 **정하기 전에 코드 고치지 마라**. **483 선행 권장**(같은 진행도 식) · 밸런스는 199 이관 「T2 우선」. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-31 03:20) 2건: ~~**512** **보상 연출이 «받은 재화» 를 안 따라간다**~~ — **완료(2026-08-30, sess-2146-9538 워커 C · 1회차)**: `probe512` 재현으로 등재문 확인(버스트는 재화 무관 상수 크림 하나 · 다섯 재화는 연출 **0건**) ⇒ **본체는 «색» 이 아니라 «표의 길이»** 였다. `FXCUR` 를 `CUR_ICON` 과 키를 공유해 **7종**으로 넓히고, 손으로 적은 `{gold,dia}` 상태 사전 **열둘을 `fxMap()`** 으로, 색 리터럴 4자리를 표(`FXPAL` 신설)로, `fxReward` 를 **지급 뒤**로 옮겨 받은 재화의 색으로 터지게 했다. 알약 없는 재화는 «도착 없는 비행» 대신 **버스트 + `+n`**. `verify512` **31/31 신설**(찍힌 픽셀 · 되돌림) · `verify156` 27/27 이관 · 회귀 10종 무수정 · SMOKE PASS. **↓ 아래는 등재문(보존)** — — 원문 «다이아 보상이면 다이아 효과, 골드면 골드효과, 유물석이면 유물석». `fxReward`(34342)가 **상수 `#FFE9A8`**(금색) 하나라 155 이후 전 칸 dia 인 룰렛에서도 금색이 터진다(= 주인이 본 «골드가 섞여 있다» 는 **표가 아니라 색**이다 — `ROULETTE` 는 건드리지 마라). 게다가 `FXCUR`(33223)가 **gold·dia 두 항뿐**이라 유물조각·강화석·룬강화석·단련석·마일리지는 연출이 **0건**이다. 처방: 색은 `FXCUR` **한 표에서만**(키는 `CUR_ICON` 과 공유 — 402 «표 두 벌» 부패 방지) · 알약 없는 재화는 비행 없이 버스트+`+n` · ⚠ `fxReward` 를 `giveReward` **입구에서 지급 뒤로** 옮겨야 종류를 안다(181 규약 확인). **518 과 한 세션 권장.** 「T2 우선」 · **513** **출석 보상 1~7일 무한 순환** — 원문 «1~7일로 순환하게 하고 8일이후를 넣지마». `ATTEND` 28칸 → **7칸** · `% 28` 두 자리(24523·26494) → `% ATTEND.length` · `base` 주차 계산 삭제 · 7일차 전폭 카드는 유지(뜻만 «순환 최종») ⚠ 28일차 5,000 특전은 **같이 사라진다**(살려 두는 절충 금지) ⚠ 출석 수입 **−39%** → 값 확정은 **199 이관**, 1일차는 **498 과 한 벌** ⚠ **36 출석 패스·493 은 «누적 접속일» 이라 독립** — 순환에 끌려가면 안 된다 「T2 우선」. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-31 01:50): 502 잡몹 이속 너무 빠름 — `(110+0.6×스테이지)×계수` 라 고블린이 s20 부터 플레이어 추월 → 스테이지 비례항 삭제 + 상한 = 플레이어 ×0.85, 고블린 계수 1.10→0.85. 501 과 한 세션. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-31 01:45): 501 보스가 느려 플레이어를 못 때림(잡몹 구간에서 더 죽음) — 359 «살짝 느리게(BOSS_CHASE 0.94)» 번복 → 1.10~1.15 + 대시 주기 0.8/1.4 · 예고 유지 · 잡몹 불변 · 재현(보스 타격 수) 먼저. 상세 PROGRESS 행.
>
> # 신규(2026-08-31 01:20, 주인 위임): 498 첫날 환영 100만 다이아(환영 우편 20만 · 가이드 미션 10×5만 · 출석 1일차 10만 · 첫 승급 10만 · 스테이지 30 5만 · 첫 10연 무료) + 2일차 이후 수급 곡선 20만→25만→30만(평균 27만) — 값 조정은 199 몫, 이 행은 설계·현재치 표까지. 상세 PROGRESS 행.
>
> # 신규 주인 확정(2026-08-31 01:10): 497 다이아 팩 5종 ×2(₩110,000 = 200만) · 마일리지 교환 500만 · 가격/쿠폰 불변 — 199 최종 목표: 무과금 100일(하루 27만) · 과금 한 벌 ₩150만 · 이후 씽크. 상세 PROGRESS 행.
>
> # 신규 주인 확정(2026-08-31 00:50): 496 소환 레벨 개편 — 배너별 5벌 → **공용 하나** · 만렙 50 · 경험치 `200+210n`(누적 268,000뽑 = 2,680만 다이아 ≈ ₩295만) · 팩 유지 · 등급 개방 비례 · 세이브 이관(뽑기 합 보존). 494 봇 전에. 199 기준값(하루 수급 ≈45만)도 기록. 상세 PROGRESS 행.
>
> # ⚑ 주인 지시(2026-08-31 00:20): **199 최종 밸런스 = todo 의 맨 마지막** — 다른 행(494 봇 포함)이 전부 완료되고 lock 이 0 일 때만 선점. 비평가 **3명** · **3명 모두 ≥ 9/10**(01:35 상향) 통과 · 상한 20회차 · 회차마다 봇 표. **목표(주인 확정 00:30): 부지런한 유저 p50 이 **100일 ±10**(최종 · 하루 수급 ≈27만 · 대충 180~200일)에 · **과금: 팩 ×2 로 한 벌 ≈ ₩150만(1일), 그 이후는 한 벌 이후 씽크가 고래 매출 본체** 불멸 장비 3·스킬 1·펫 1 한 벌** — 다이아 유입은 불멸 0.1% 기대 뽑기 수로 역산. 손잡이 4종(적·수급·비용·확률)만, 주인 뼈대 표 불가침. PROGRESS 199 행.
>
> # 신규 주인 지시(2026-08-31 00:05) 2건: 493 패스 길이 확장 — 스테이지 3,000(`PASS_N` 600) · 출석 100일 · 탑 패스 100레벨(600행 렌더 성능 확인) · 494 **199 준비 — 봇 플레이어 시뮬 `tools/bot199.js`**(매일 전부 하고 항상 제일 센 것 장착 · 실전 표본 + 수식 접기 · 30일 ≤ 2분 · 시드 20 · 정책 2종). 494 는 472·484·485 완료 뒤. 상세 PROGRESS 행.
>
> # 신규 주인 보고(2026-08-30 23:35, 스크린샷): 492 50 코스튬 시트 카드 스프라이트가 너무 작음(08 세부는 큼) — 카드 아이콘 영역의 ≥80% 로, 정수 배 pixelated · 411 «아트 대기» 보류 해제(스케일로). 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 지시(2026-08-30 23:30) 4건 — 룬·재화·쥬시: 488 룬 연속 강화 중 시도마다 카드 피드백(성공 팝/실패 흔들림/재화 −n 플로팅, 토스트는 end 1장) · 489 룬 강화 수치 **선형**(주인 확정 23:40 — 상한 500 유지 · 레벨당 증가 상수 하나 = 현행 ×3) · 490 룬 강화는 룬강화석만(다이아 폐지) + 상점 다이아→유물조각/룬강화석 **1:1** + 입장권 교환 **전부 1,000** + 룬강화석 아이콘 **초록** + 입장권도 초록(430 개정) + **수정 광산 입장권 하늘색** · 491 UI 쥬시니스 **비평 루프 2명 ≥ 8/10, 상한 8회차**, 1회차 룬·단련 탭. 489+490 한 세션 · 488+491 한 세션. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 23:20, 스크린샷): 486 23 훈련 카드 알약을 «+20 공격력»(증가분) → «지금 최종값»(`stat.dmg/maxHp/regen`)으로. 같은 스크린샷(9단계 · 0/2700 · Lv 921~983)이 **483 의 원인을 확정** — 326 누적합 상한 vs 구 세이브(base 3600 > lv 921 → 진행 0). 483 행에 이관 처방 적음. 483·486 한 세션. 상세 PROGRESS 행.
>
> # ⚑ 주인 수치 확정(2026-08-30 23:15) — 밸런스 3건 한 세션: 472 장비 장착 효과 = 일반 1티어 10% · 티어 ×1.5 · 등급 ×3(= `0.10×1.5^t×15.1875^g`, 장비 전용 표 `EQ_BASE`) · 484 스킬은 같은 등급 DPS 동일 + 등급 ×3(260 스킬 절 교체) · 485 펫에 «장착 효과 공격력 +n%» 신설, 표는 장비와 동일(481 레벨 결정을 장착 효과 축으로 정정). 규모 ×1.8×10⁸ 라 199 를 «재역산 필요» 로 이관. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-30 23:10): 483 23 훈련 — 스탯 사도 훈련 경험치(진행바) 안 오름 «버그» T1. 재현 먼저(가설 ⓐ 326 누적합 경계/구 세이브 · ⓑ 키 불일치 · ⓒ 갱신 누락 · ⓓ room 0 · ⓔ 단계 ↑ 되돌림 표시) — 확정된 갈래만 고친다. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 22:50): 482 05 장비 팝업 — 열 때·[일괄 강화] 뒤 «보유 중 제일 좋은 것(`equipVal` 최대)» 자동 **선택**(장착은 버튼 — 263/105 자동 장착 금지 불변) · 07 스킬·26 펫·50 코스튬 시트도 같은 규칙(시트별 척도 review 표). 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 22:45): 481 펫 피해 = 플레이어 공격력 그대로(m·등급·레벨 계수 제거) · 등급은 공격 주기만 빠르게(`PET_CD` 재설계 · 등급 안 v 는 주기 축으로 · 260 규칙 이동) · 펫 레벨은 **보유 효과에만**(주인 확정 23:00 — 재질문 금지). 472 와 한 세션 가능. 상세 PROGRESS 행.
>
> # ⚑ 주인 보강(2026-08-30 22:40): 471 레드닷 통일은 **비평 루프**로 — 기준 그림 = [모두 받기] 버튼 캡처(점이 버튼 우상단 코너에 «걸침», 1/3~1/2 밖으로) = 우리 22 퀘스트 [모두 받기](`.qs-all>.updot` −9/−9) — 착수 세션이 직접 캡처해 `docs/ref/471-레드닷-코너.png` 저장 · 비평가 **2명** 독립 · **둘 다 ≥ 9/10** 통과 · 상한 5회차 · 479·480 포함. PROGRESS 471 행 갱신.
>
> # 신규 주인 보고(2026-08-30 22:32, 스크린샷): 480 124 이용권 «광고 제거» 버튼의 ▶ 아이콘이 배경 노랑과 대비 0 — 검정 테 + 흰 코어로(13 ▶AD 뱃지와 한 규칙). 470·477·478 과 한 세션. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-30 22:30, 스크린샷): 479 13 재화 광고 카드 레드닷을 «[받기] 버튼 우상단 코너» 로 — 364 «카드 우상단» 번복. 471 규약(점 중심 = 호스트 코너)의 첫 적용 — 471 세션이 같이 잡을 것. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 22:20): 478 10 상점 재화·이용권 탭 바닥에 «청약철회 7일 … [더보기]» 고지 띠 신설(레퍼런스 캡처 · 부품 하나 공용 · 더보기 = A5 팝업 실동작 · 소환 탭 제외). 470·477 과 한 세션. 상세 PROGRESS 행.
>
> # 신규 주인 보고(2026-08-30 22:15, 스크린샷): 477 10 상점 이용권 탭 하단 «결제 미연동 …» 안내문 좌우 잘림(T1) — 한 줄에 3종 대체가를 다 담아 858 을 넘는다. 권장: 총론 한 줄 + 대체가는 각 카드에. 470 과 한 세션 가능. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 22:10): ~~476~~ **→ 500**(번호 이동 2026-08-30, sess-1551-22038 워커 H — PROGRESS 476 행은 다른 작업 `probe351` D7 이라 이 지시에 행이 **하나도 없었다** = 티어 스캔에 안 잡힘. 500 으로 등재했다) 축복 레벨업 필요 경험치 표 4·10·15·…·90·100, 이후 100 고정(필요량 캡 · 레벨 캡 51 은 그대로) — `BLESS_STEP` 상수 → `BLESS_NEED` 표 + 접근자, 두 벌 식 통합, 진행바 «n/need». 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 지시(2026-08-30 22:05): 475 모든 보스전 격파 시퀀스 — 332 던전 «터짐(die) → 알림 → 1초 → 후속» 을 스테이지 보스(지금은 죽는 프레임에 즉시 `S.stage++`)·승급전(즉시 `endPromo`)으로 확장. 공용 `bossClear` 상태 · hold 는 `DUN_CLR_HOLD` 공유 · 레이드/아레나 제외 · 페이즈 던전은 마지막 보스만. 457/458/473 세션이 이어서. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 지시(2026-08-30 22:00): 473 보스전 등장 국면 중 스킬 쿨타임 정지 + 발동 금지(펫 공격도) — step 스킬 루프에 `bossIntro.on` 가드 한 줄 · 459 «국면 끝 0 초기화» 는 그대로. 457/459 세션이 이어서. 상세 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 21:55): 472 장비 장착 효과 «티어당 증가폭» 확대 — 권장 해석 ⓑ 등급 안 1~5번째 `v` 폭 0.92~1.13 → 0.80~1.40(260 규칙 유지 검산) · 보유 효과 불변 · 폭 상수 하나로 · 199 이관. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 보고(2026-08-30 21:50, 스크린샷) 2건: 470 10 소환 탭 레벨 알약 «Lv.n» 글씨 잘림(T1) · 471 【⚑】레드닷 위치 전면 통일 — «호스트 우상단 코너에 점 중심» 규약 하나(`--dot-r` 변수) · 클리핑 조상은 푼다(서브탭 `.stabs` 가 점을 자름) · 임의 좌표형(`.wgc`·`.cn-cd`·`.shp-card`)은 대상 노드 자식으로 이동 · `.ifbtn.pbtn` 이중 선언 제거 · 15+ 호스트 전수 한 세션. 상세 PROGRESS 행.
>
> # ⚑ 신규 주인 지시(2026-08-30 15:45~16:00) 6건: 455 룰렛 회전 중 팝업 닫기 잠금 · 456 축복 지속 «항상 30분» · 457 425 보스 등장 국면을 **모든 보스전**(스테이지·승급전·레이드)으로 — 425 «던전만» 제한 해제, **425 를 끝낸 세션이 이어서** · 458 【⚑】모든 보스전 사망 = 즉시 실패(지금 던전·승급전·레이드는 죽어도 부활해 계속 싸움) · 459 보스전 시작 시 스킬 쿨타임 전부 초기화 · 460 플레이어 애니는 idle·run 둘만(attack_A·get_hit 분기 삭제, die 유지). 457·458·459 는 같은 start 함수 5곳이라 한 세션 묶음 권장. 상세 각 PROGRESS 행.
>
> # 신규 주인 지시(2026-08-30 15:40): 453 — 전투 모드(보스전·승급전·던전·탑·레이드/아레나) 중 03 던전 내비 팝업·승급전 팝업 열기 금지 + 모든 전투 입장 금지. 판정은 `bossMode()` 하나로(`battleBusy()`), 입구 8곳 가드 통일 · 탭/아이콘 `.off` 딤 · «승급전은 보스전 중 끼어들 수 있다» 설계 폐지 · 339 연속 도전 예외. 425 완료 뒤(같은 분기). 상세 PROGRESS 453 행.
>
> # ⚑ 신규 주인 재재지시(2026-08-30): 430 — 던전 입장권 8색을 이름대로 확정(황금 노랑 · 수정 초록 · 고대 갈색 · 잊힌 회색 · 용 빨강 · 창세 흰색 · 각성 주황 · 룬 파랑). 412 의 «L\*64 한 밴드» 규칙 폐기 — 그 규칙이 «파랑 계열 넷» 을 만들었다. 자산 8장·제품 0줄·bbox Δ0. 세 번째 지시라 **425~429 보다 먼저**. 상세 PROGRESS 430 행.
>
> # ~~신규 주인 지시(2026-08-30): 429 — 89 유물 페이지 좌상단 [?] 도움말(269 코스튬 `cos-help`/`cosHelp()` 부품 재사용) + 세부 팝업 `descNow()` 유물 분기의 «공통 2줄»(보유만으로 적용 · 소환마다 Lv+1)을 그 팝업으로 이동~~ — **완료(2026-08-30, sess-0559-25025 워커 D · 1회차)**. 제품 4자리 · 레이아웃 Δ0px. 자리는 등재문의 두 후보를 `probe429` 로 실측해 **① 41 재화 바 왼쪽 빈 구간**(0~204.6 이 통째로 비었다 · 여유 99.5px · 겹침 0 · 2280↔1600 Δ0)으로 골랐다 — ② `.rw-panel` 좌상단은 좌측 기둥 `.rw-pil.l`(폭 85) 위인 데다 **프레임 높이에 따라 덮는 아트가 바뀐다**(120 세로 예산). ⚑ **부품을 복제하지 않고 선택자를 늘렸다**(`#bCos .cos-help,#relw .rl-help` 한 규칙). ⚠ 그런데 그것만으로 안 끝났다 — 외곽선 유틸 `.ol1~.ol5` 가 세 시트·`.stabs` 스코프라 **«?» 만 민짜로 그려졌다**(397 계열 «스코프 구멍»). `verify429` 50/50 신설 · `probe429` 10/10 신설 · 이관·회귀 11종 무수정 · SMOKE PASS(오프너 `rel:help`). 상세 PROGRESS 429 행 · `docs/review/429-유물도움말.md`.
>
> # 신규 주인 지시(2026-08-30) 2건 추가: 427 탑도 «층»→«레벨» 표기(209 결정 번복 · 데이터 0줄) · 428 35 패스 하단 탭 «보물상자·시련의탑» → «시련의 탑·절망의 탑» + 🔒 해제 = **실제 패스 탭**(PASS_TABS.tower/tower2 신설 · prog = S.tower/S.tower2)
> 428 은 «준비 중» 토스트로 두면 미완이다(주인 «자물쇠 풀기 / 잠금 풀기» 2회 지시). 427·428 한 세션 권장. 상세는 각 PROGRESS 행. 순위: 363~367 다음(425·426 과 같은 급).
>
> # 신규 주인 지시(2026-08-30) 2건: 425 던전 보스 «등장 연출»(보스 잠깐 비춤 → 플레이어 복귀 → 전투 시작 · **제한 시간은 전투 시작부터**) · 426 31 클리어 «보상 묶음» 앵커를 프레임 세로 중앙으로(짧은 프레임에서 하단 몰림)
> 425 는 108(카메라 연출 폐기)에 대한 **주인 지시 우선의 의도적 이탈** — 팬 한 번뿐, 67 의 줌·플래시·슬로모 부활 금지·범위는 던전 런만. 426 은 360 선례대로 측정표 정오표 필수. 상세는 각 PROGRESS 행. 순위: 363~367 다음, 356·351 대형 루프보다 먼저. 두 건 한 세션 묶음 권장.
> 
> ⚙ 루틴 편성 알림(2026-08-30, kkkkkk-30): 워커 A~D 4개를 **같은 규격으로 재생성**했다(A :15 · B :30 · C :45 · D :58, Opus 5). 지시서·선점 규약은 그대로.
>
> # ⚑ 신규 T1 «버그» (2026-08-29 주인 보고): 349 — 룬 [강화] 꾹 누르기 연속 강화 안 됨 (297 재발/미완)
> 재료/다이아·터치/마우스부터 가르고, 64 홀드 셀렉터 누락·확률 실패 조기 종료·재렌더 포인터 캡처 절단(64 교훈 1) 세 갈래 의심 — 상세는 PROGRESS 349 행. 345 와 같은 T1 급.
> 
> # ✅ 완료(2026-08-28, sess-2018-17221 워커 A): 348 — 화면 밖 몬스터 HP바 제거
> **제품 6줄**(`eOnScreen()` + 호출부 가드 한 항) · 레이아웃 Δ0px · 밸런스·판정 0줄. 처방대로 `fxClampX` 는 **한 줄도 안 건드리고** 그리기 조건 앞에 가시성 한 겹만 세웠다.
> ⚑ **재현이 축을 하나로 좁혔다** — `probe348` 신설(찍힌 픽셀). 등재문 가설은 맞았지만 `fxClampX` 는 **x 만** 밀므로 위·아래로 벗어난 적의 바는 **수리 전에도 픽셀 0** 이었고 실재하는 자리는 **좌·우 둘**이다. 찍힌 x `24.0`·`500.5` 가 클램프 상수와 정확히 맞아 «다른 요소» 분기가 픽셀로 기각된다(350 처방).
> 크기는 작지 않았다 — 스테이지 20 일반 전투 60초에서 적-프레임 178,150 중 **81,030(45.48%)** 이 몸통 없는 유령 바였다.
> ④ 는 «반쯤 걸치면 보이는 것»(몸통 bbox 교차) · 판정은 월드가 아니라 **화면 좌표**라 카메라가 밀려 벗어난 경우도 걸린다. ③ 은 **대체물을 안 만들었다** — 보스전 30초에서 보스가 화면 밖인 프레임 0건이고 던전·탑·승급 보스는 전용 HUD 바(338)가 따로 있다.
> `verify348` **34/34 신설**(§R 되돌림 시험은 «밖 4자리 중 **2자리**» 를 세어서 단언한다 — 전부를 기대했으면 영원히 빨간 게이트가 됐다) · `probe348` 6/6 신설 · 회귀 `verify338` 179/179 · `verify66` PASS · `verify172` PASS · **SMOKE PASS**. 상세 `docs/review/348-화면밖몬스터HP바.md` · 교훈 3개 LESSONS 348.
> ⚑ **적 오버헤드에 새 표시(상태 아이콘·이름표)를 얹는 세션은 `eOnScreen(e)` 를 같이 태울 것** — 안 태우면 348 이 통째로 되살아난다.
> 
> # 신규 T2 주인 지시(2026-08-29): 346 — 코스튬 세부 «강화 효과» 표시 삭제, «보유 효과» 에 합산 표시
> 스크린샷 지시(마을 위병 상세). 두 줄 삭제 + 라벨 교체 + 합산 한 줄 — .sk-db 박스 구멍(264px)·미보유 분기 유지·문구 게이트 이관 주의는 PROGRESS 346 행. 345(T1) 다음 순위.
> 
> # ⚑ 신규 T1 «버그» (2026-08-29 주인 보고): 345 — 우편함 팝업이 «찌그러지며» 닫힘
> 원인 가설(kkkkkk-30 실측): closeModal() 이 껍데기 클래스(ml69 등)를 **즉시** 떼는데 60 쥬시 닫힘 연출(0.12s)은 그 뒤 역재생이라, 연출 동안 상자가 기본 규격으로 리플로우된 채 축소된다 = «찌그러짐». **q22·at70·sk8·rl16 도 같은 remove 목록 — 전수 확인.** 처방·게이트는 PROGRESS 345 행. **T1 이라 현재 큐(339·337·341·342·344·199) 최우선.**
>
> # ✅ 완료(2026-08-28, sess-1601-5235 워커 D): 340 — 41 팝업 재화 바 아이콘 «잉크» −14~20% 회수
> 72 의 ② 를 누르던 두 본체 중 **«A3(헤더 아이콘)» 쪽이 닫혔다**(남은 하나는 47 = 335·337).
> 03 화면에서 보이는 헤더는 메인 HUD 가 아니라 **`.pcb`(41 컴포넌트)** 이고, 그 아이콘 잉크가 **코인 −14.0% · 젬 −19.6%** 였다.
> 게이트(`verify125` D3)가 초록이었던 이유는 그것이 **레이아웃 박스(57×57)** 를 쟀기 때문이다 — 사람이 보는 것은 **아트의 색 잉크**다.
> 뿌리는 측정표 41 §3 의 «bbox 57×57» 이 실은 색 잉크였던 것(ref 실루엣은 **64×65** — A3 가 HUD 에서 9회차에 뒤집은 값과 같다).
> 처방은 **새 상수 없이 HUD 와 같은 손잡이**(코인 이미지 65.3 · 젬 이미지 `scaleX(1.16)`). `verify340` 39/39 신설(되돌림 시험 포함) ·
> `verify125` D3 이관 + D3b 신설 · 회귀 5종 초록 · SMOKE PASS. ⚠ **번호를 337 → 339 → 340 으로 두 번 옮겼다** —
> 착수 중에 337·339 가 각각 다른 작업으로 등재돼 남의 번호를 비켜 준 것이다(281·308 계열 사고 예방).
> 2. **폴리시 라운드**: **122**(⚠ 다음 1회차가 마지막 — 끝나면 점수 무관 마감, 주인 지시) · ~~**163**~~(**2차 라운드 종료 2026-08-28 — 15/15 소진, ⏸ 보류(최고 7점). 새 라운드 금지**) · **72**(2차 **17/20** — ⛔ **남은 3회차를 72 구간에 쓰지 마라.** 2026-08-28 sess-1501-3810 이 잡았다가 **회차 0 소모로 놓았다**: `docs/review/72-던전카드썸네일.md` §32 판정대로 AJ·AK 의 ②③ 감점 8건 중 72 구간은 1건(아트 대기)뿐이고 나머지 7건이 **47·A3·03/서체** 구간이라, 그 본체를 **335 로 등재**했고 워커 A 가 그것을 끝냈다. ⇒ **72 는 제품 0줄로 «한 회차 재채점»** 만 하면 되고, 그때 비평가에게 위 1-m 의 «앵커가 둘» 표를 반드시 줄 것) · **58**(5차 **44/50** — 42회차 최저 3→4 → **43회차 BK 6 / BL 5**(두 사람 다 ③=7, 42차 4/4 에서 BK 두 칸·BL 한 칸 상승) → **44회차 = «채점 직후 수정분»**. ⚑ **r44 캡처 42장이 이미 찍혀 있다 — 다시 찍지 말고 그 위에서 45회차를 채점하라.** 45회차가 잡을 것·주인 판단 대기 2건은 PROGRESS 58 행 참조)
> · **⚑ 58 — 주인 판단 대기 2건(2026-08-28 42회차, 실측으로 «회수 불가» 를 증명했다. 다음 워커가 임의로 고르지 마라):**
>   ⓐ 41차가 세운 «인접 구간 속도비 ≤2.0» 은 **도착 봉투(0.50 / 1.22s)와 산술적으로 양립 불가** — 3박자의 «머묾» 을 유지할지, 2박자로 갈지, 봉투를 늘릴지.
>   ⓑ «재화 복도를 팝업 안으로» 는 **기하가 없다**(팝업 우변 974 · 형제 행 우변 949 · 아이콘 반폭 19~23 ⇒ 가능 구간 −21px). 현행 유지 / 복도 아이콘 축소 / 복도를 팝업 위로 셋 중 하나를 골라야 한다.
>   상세·검산은 `docs/review/58-UI연출모듈.md` 42회차 절.
> 3. **199 최종 밸런스 비평 라운드** — 위가 전부 소진된 뒤에만 착수(선행 조건 그대로).
>    ⚑ **199 가 볼 것이 하나 더 늘었다(326 이관, 2026-08-28)**: 훈련 단계 요구치가 증가식이 되면서 같은 레벨의 단계가
>    4 → 3 으로 내려가 훈련만 축 배수가 **×1.30 → ×1.20(−7.7%)** 이다. 적 곡선 역산 4칸이 «⏸199 대기» 로 등재돼 있고
>    (`sim177` ③⑧ · `sim249` ⑤⑥ — 최악 몹 1.024초 · 관문 보스 15.66초, 둘 다 제한을 2~4% 초과), 더 큰 건
>    **«단계당 +10%» 축이 s80 부근에서 +20% 로 굳는다**는 것이다(4단계 Lv 1000 = 2.5e25 골드로 지평 밖).
>    지렛대는 셋 — 적 곡선을 내리거나 `TRAIN_BONUS` 를 올리거나 그대로 두거나. 실측표는
>    `docs/review/326-훈련단계요구치증가식.md` §4.
>    ⚑ **199 가 볼 것이 하나 늘었다(331 이관, 2026-08-28)**: 몹 국면이 사라져 던전 판당 필요 피해가
>    `need × 0.60` → **`need × 0.30`**(정확히 절반)이 됐다. 최악 판이 15초 예산의 **76% → 47%**,
>    클리어율은 두 배율 전부 30/30 불변 = 하드락은 없고 **쉬워지는** 방향이다. 331 은 지시 ③ 대로
>    `DUN_DMG_K`·`DUN_BOSS_HPK` 를 **한 줄도 안 건드렸다**. 난이도를 되돌리려면 `DUN_BOSS_HPK` 한 줄이면
>    된다(난이도 축이 전부 `need` 의 «몇 %» 라 나머지가 따라온다). 실측표는 `docs/review/331-던전잡몹국면폐지.md` §5.
> · **62 는 주인 보류 유지 — 선점 금지.**
> · kkkkkk-30 이 오늘 처리 완료: 296·299·300·301·302·313·314·224 (게이트 verify299·verify301 신설, verify267/166/271/174 갱신 — 되돌리지 마라).

---

> 클라우드 루틴 워커 A~D 가 **이 파일을 읽고 그대로 수행**한다.
> 지시를 바꾸려면 이 파일만 고치면 된다 (루틴 4개를 각각 고칠 필요 없음).
>
> 루틴 자체를 걸고·끄고·**다른 계정으로 옮기는 방법**은 `docs/ROUTINE-OPS.md` 를 본다.
> (이 파일 = 워커가 무엇을 하는지 / 저 파일 = 루틴을 누가 어떻게 굴리는지)

---

너는 kuzuni/kkkkkk 저장소의 모바일 방치형 게임(index.html) UI를 레퍼런스 스크린샷과 동일하게 만드는 작업을 이어서 진행한다. 이전 세션의 대화 기억은 없다고 가정하고, 모든 상태는 저장소 파일에서 읽어라.

[중요] 이 작업은 **여러 세션이 동시에 병렬로** 돈다. 다른 세션이 돌고 있어도 정상이며, 너는 겹치지 않는 작업 단위를 하나 잡아서 그것만 한다.

[-2] git 준비 (맨 처음)
이 환경은 detached HEAD 로 체크아웃되므로 그냥 `git pull --rebase` 하면 실패한다. 반드시 아래로 시작한다:
  git checkout main 2>/dev/null || git checkout -B main origin/main
  git branch --set-upstream-to=origin/main main 2>/dev/null
  git pull --rebase origin main
이후 모든 push 는 `git push origin main`. push 전에는 항상 `git pull --rebase origin main` 을 먼저 한다.
세션 식별자를 하나 만들어 둔다: SID=sess-$(date -u +%H%M)-$RANDOM

[-1] 작업 단위 선점 (전역 락 없음 — 작업 단위로만 잠근다)
1. docs/PROGRESS.md 의 «병렬 세션 규칙» 과 «작업 단위» 표를 읽는다.
2. 작업 선택은 **아래 티어 순서**로 한다. 같은 티어 안에서는 표 순서(A1 → A5 → 01 → … 마지막 번호).
   잡는 대상은 항상 **미완료 + `docs/claims/<ID>.lock` 없음**.
   - **T1 버그** — 【⚑ 우선】 표시가 붙은 작업, 그리고 비고에 «버그» 라고 적힌 작업. 깨진 것부터 고친다.
   - **T2 기능 TODO(저장소 주인 지시)** — 비고에 «설계 확정/지시» 가 적힌 작업 전부(현재 28·29·33·41~50·57~62 등).
     설계 변경·신규 기능 계열. 일반 화면 루프보다 먼저. **이 티어의 작업은 반드시 실제로 동작해야 완료다**(아래 «기능 완성 규칙»).
   - **T3 한 번도 구현된 적 없는 화면**(`docs/review/<ID>-*.md` 없음) — 골고루: 이런 게 남아 있으면 재폴리시 금지.
     화면 작업은 기존 기준(레이아웃 8점)대로. 기능 연결은 가능한 범위에서 하되, **전 화면 실동작 감사(62)는 주인 지시로 보류 중**.
   - **T4 레이아웃 폴리시(2차 라운드) — 맨 나중.** 위 세 티어가 전부 비어야 잡는다. 점수 낮은 화면부터 8점까지.

   **기능 완성 규칙(2026-08-25, 저장소 주인 지시 — T2 에 적용)**: 주인이 지시한 기능 작업의 완료 조건은 «만들어 놓음» 이 아니라
   **«실제 게임 데이터로 동작하고, 결과가 저장(S)·HUD·다른 화면에 반영됨»** 이다. 목업·«준비 중» 처리는 결제처럼 문서가
   명시한 경우만 허용. 완료 보고에 **기능 체크 표**(버튼별 «눌렀을 때 무엇이 바뀌는지» 헤드리스로 확인한 결과)를 review 파일에 남긴다.
   - lock 파일이 있어도 안의 UTC 시각이 **90분 넘게 지났으면 죽은 것으로 보고 뺏는다**(덮어쓴다).
     (2026-08-26: 50분 → **90분 복귀**(저장소 주인 결정). 한 회차(캡처·비평·재채점)가 30~40분이라 50분은 살아 있는 세션의 heartbeat 한 번 지연으로도 뺏겨 **같은 작업을 둘이 하는** 역효과가 났다. 회차마다 heartbeat 갱신 의무는 그대로.)
     **뺏기 전에 반드시 `git log --oneline origin/main -- docs/claims/<ID>.lock index.html docs/review/<ID>-*.md` 로 그 번호의 마지막 `wip(<ID>)` 커밋 시각도 본다** — lock 시각과 마지막 wip 이 **둘 다** 90분 넘었을 때만 죽은 것이다(lock 갱신을 빼먹었지만 커밋은 계속 올리는 세션은 살아 있다).
   - **뺏은 작업은 «처음부터» 가 아니라 «이어서» 한다.** 앞 세션은 한도 소진·중단으로 죽은 것이고,
     거기까지의 결과물은 이미 커밋돼 있다. 뺏었으면 **반드시 먼저 읽는다**:
     `docs/review/<ID>-*.md`(몇 회차까지 돌았고 무엇이 지적됐는지) ·
     `docs/measure/<ID>-*.md`(측정 끝났으면 **재측정 금지**) · `git log --oneline -20 | grep '(<ID>)'`.
     3회차까지 간 작업을 1회차부터 다시 돌리면 남은 회차 예산을 통째로 날린다.
     비평 회차는 **이어서 센다**(3회차까지 돌았으면 다음은 4회차).
3. `echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) $SID" > docs/claims/<ID>.lock` 후 커밋 + push.
4. push 가 거부되면 `git pull --rebase origin main` 후 다시 확인한다.
   - rebase 가 `docs/claims/<ID>.lock` 에서 충돌하면 **선점 경쟁에서 진 것이다**(다른 워커가 먼저 push 했다).
     `git rebase --abort` 후 `git reset --hard origin/main` 으로 내 클레임을 버린다 — 선점 단계에서는
     로컬 커밋이 클레임 하나뿐이라 안전하다. **충돌을 내 쪽 lock 으로 풀어서 남의 클레임을 덮지 마라.**
   - rebase 가 깨끗이 끝났어도 `<ID>.lock` 내용이 내 SID 가 아니면 남이 잡은 것이다.
   - 어느 쪽이든 **그 작업은 포기하고 다음 미선점 작업**으로 간다. (90분 지난 lock 을 뺏을 때도 같은 규칙 —
     뺏기 push 가 충돌하면 남이 먼저 뺏은 것이니 포기한다.)
   - 잡을 게 하나도 없으면 '선점 가능한 작업 없음'이라고만 보고하고 즉시 종료한다.
5. 작업을 마치거나 중단할 때는 성공·실패 무관하게 lock 을 **삭제**하고 커밋 + push 한다. 빼먹으면 90분간 그 작업이 막힌다.

[0] 시작 절차
1. docs/PROGRESS.md (단일 진실 공급원) 와 docs/UI-REFERENCE.md (화면 스펙 + 24번 구현 체크리스트 — 잡은 화면의 절과 서두 «설계 확정»만) 를 읽는다.
   **docs/LESSONS.md(앞 세션들의 교훈 모음)도 한 번 읽는다** — 같은 실수가 화면마다 재발한다.
2. git log --oneline -10 / git status 로 직전 상태를 확인한다.

[1] 이번 실행의 목표
- **잡은 작업 단위 딱 1개만 끝낸다.** 다른 작업까지 손대지 마라 — 병렬 세션과 충돌한다.
- **레이아웃을 실제로 바꾸는 것이 목적이다.** 스케일 변환·상수 정리 같은 기계적 작업으로 회차를 통째로 쓰지 마라.
- 60분 안에 끝내는 것을 목표로 한다.
- **회차마다 커밋·push 한다(2026-08-25 세션 사망 대책).** 캡처→채점→수정 한 바퀴가 끝날 때마다
  `wip(<ID>): n회차 — 요약` 으로 커밋하고, **같은 커밋에서 `docs/claims/<ID>.lock` 의 시각을 현재 UTC 로 갱신**(heartbeat)한다.
  갱신은 `node tools/claim.js --beat <ID> <SID>` 로 한다 — **작업 트리가 더러워도 찍힌다**(2026-08-28, 작업 307:
  beat 경로는 내부 `pull --rebase` 를 돌지 않는다). **실패는 stderr 로 나가고 종료 코드 2 는 «이미 뺏겼다» 는 뜻이니
  `>/dev/null` 로 묶더라도 종료 코드는 확인할 것** — 조용히 안 찍힌 heartbeat 가 산 세션을 90분 규칙에 걸리게 한다.
  세션이 한도로 죽어도 손실은 마지막 1회차뿐이고, lock 이 90분 규칙으로 정확히 회수된다.
  회차 기록(`docs/review/<ID>-*.md`)도 회차마다 같이 커밋한다 — 다음 세션이 이어받는 유일한 근거다.
- 끝나면 PROGRESS.md 해당 행 + UI-REFERENCE.md 체크리스트 + docs/review/<ID>-이름.md 를 갱신하고 lock 삭제 후 커밋 · push.
  ⚠ **PROGRESS 행은 «비고 칸 끝에 완료문을 덧붙이는» 것으로 끝나지 않는다(2026-08-29, 작업 388).**
  **① 구현 칸**(`–` → `✅ 완료(날짜, 세션 · n회차) — 한 줄 요약`) · **② 루프 횟수**(`0/5` → 실제 회차) ·
  **③ 비고 머리말**(`미착수`/`등재만` → 등재문임을 밝히는 말. 등재문 **본문은 지우지 마라**) — **셋을 같이** 고친다.
  다음 워커의 티어 스캔은 그 세 칸을 읽는다. 371·378·308 이 실제로 그래서 «미완료» 로 보였고,
  sess-0400-28780 은 끝난 371 을 선점했다가 **회차 0 소모로 놓았다.** 자는 `node tools/verifyProgress.js` 가 겸한다(§2).

[2] 작업 규칙 (상세는 문서 0·24·25번)
- **기준 해상도는 1080x2280 (9:19) 이다.** (2026-08-25 확정. 08-24 의 9:16 기준은 폐기 — 레퍼런스 게임의 실제 콘텐츠 영역이 1080×2256≈9:19 다)
- 레퍼런스 스크린샷(docs/ref/*.jpg)은 1080x2340 캡처이고 **최상단 84px 이 상태바(안전영역)** 다. 따라서 변환은 **단 하나: 프레임 y = 레퍼런스 y − 84** (가로는 1:1).
  남는 24px(2280 − 2256) 은 전투 캔버스가 흡수한다. 옛 «y−419/−420/−210» 식 화면별 변환은 **전부 폐기** — 새로 쓰지 마라.
  9:16 등 짧은 기기에서는 전투 캔버스만 줄어든다(UI 고정 요소는 절대값 그대로). **절대 전체를 비율로 축소하지 마라.** 눈대중도 금지.
- **기존 UI를 래퍼로 감싸 scale() 로 확대·축소하는 꾸수는 금지한다.** 각 요소의 CSS 값을 레퍼런스 측정치대로 직접 다시 적는다.
- 폭 1080 고정 · 세로 가변(#app 높이 = 1080 x 안전영역높이/폭, clamp 1600~2600). 남는 높이는 전투 캔버스가 흡수.
- 안전영역은 프레임 바깥에서 흡수(#wrap 에 env(safe-area-inset-*), fit()은 wrap.clientWidth/clientHeight 기준).
- 구현 전에 측정한다: docs/measure/NN-이름.md 에 요소별 x/y/width/height/gap/font-size/border/radius 를 절대 px로 기록. 이미 있으면 재측정 금지.

[3] 검증 — 작업 종류에 따라 다르게 한다 (중요)

(가) **기계적 작업**(단위 변환, 상수 일괄 치환, 리네이밍, 값 스케일링 등 레퍼런스 대조가 필요 없는 작업)
  → **비평가를 절대 띄우지 마라.** grep 으로 미변환분이 남았는지 세고, 헤드리스로 한 번 띄워 콘솔 에러 0건과 요소 겹침·잘림 0건만 확인하면 통과다.
  → 이런 작업에 비평 루프를 돌리면 회차 예산을 다 태우고 정작 레이아웃을 못 바꾼다. 실제로 이전 회차가 그렇게 날아갔다.

(나) **레이아웃 재구성**(레퍼런스와 배치·비율을 맞춰야 하는 작업) → 아래 루프를 최대 5회 돈다.
  1. 헤드리스 브라우저로 그 화면을 **1080x2280** 으로 캡처한다(playwright. 2026-08-25 부터 — 1920 캡처는 폐기). 상태가 필요하면 임시 주입 스크립트로 상태를 만든 뒤 캡처.
  2. Task 도구로 서로 독립적인 비평가 2명을 띄운다. 각자에게 레퍼런스(docs/ref/NN-*.jpg)와 캡처본을 둘 다 Read 시키고 채점하게 한다.
     비평가에게 **레퍼런스는 1080x2340(상단 84px 상태바), 캡처는 1080x2280 이며 가로는 1:1, 세로는 «레퍼런스 y − 84 = 캡처 y» 로 비교하고 전투 캔버스 높이 24px 차이는 감점하지 말 것**이라고 반드시 알려준다.
     항목은 ①요소 배치 ②상대 크기 비율 ③여백·간격 ④정렬·그리드 ⑤색·명암 ⑥폰트 크기 비율. 각 10점이며 최저 항목 점수를 그 비평가의 점수로 삼는다.
     지적은 '몇 px 어긋남 / 몇 % 큼'처럼 수치로만. 두루뭉술한 총평 금지. 후하게 주지 말라고 명시한다.
     **통과 판정은 ①②③④(배치·상대 크기 비율·여백·정렬) 만으로 한다(2026-08-25).** ⑤ 색·명암 ⑥ 폰트는 채점해서
     기록은 남기되 **통과를 막지 않는다** — 아트·서체는 나중에 GPT 로 만들어 넣을 예정이라, 지금은 **레이아웃의
     비율·비례**가 전부다. 단 ⑤⑥ 지적 중 크기·bbox·비율에 관한 것(아이콘 박스 w×h, 글자 잉크 높이·폭, 두께)은
     ②·⑥ 비율 항목이므로 반영한다. 비평가에게 «색·아트 내용·서체 모양은 감점 사유로 쓰지 말고 크기·위치만 보라» 고 명시.
  3. 2명 중 ①~④ 최저 점수가 8 미만이면 수정 후 1번부터 반복.
  4. ①~④ 최저 8 이상이면 통과 → 문서 갱신 + 커밋. (⑤⑥ 점수는 비고에 «아트 대기: …» 로 기록)
  - **아트 자리 규칙**: 이모지·CSS 작화로 대체한 요소는 **레퍼런스 bbox(w×h·중심)를 정확히 차지**하게 만든다 —
    나중에 이미지로 교체만 하면 되게. 그리고 측정표(`docs/measure/<ID>-*.md`) 끝에 **«아트 필요» 표**(요소명 · w×h · 용도 · 현재 대체물)를
    남긴다. 이 표가 모여서 GPT 아트 발주서가 된다.
  5회 안에 8이 안 나오면 남은 문제를 PROGRESS.md 비고에 적고 lock 을 풀고 종료한다.
  - **최종 목표는 전 화면 8점이다(2026-08-25 통과 기준 9→8 하향). 다만 «한 화면 몰아치기»가 아니라 «전 화면 라운드»로 돈다.**
  - **라운드당 회차 상한(2026-08-25): 화면당 누적 10회 — 세션을 넘어 합산한다.** 시작 전에
    `docs/review/<ID>-*.md` 로 앞 세션들의 회차를 확인하고, 이미 10회를 채운 화면은 **«보류»** 처리한다
    (구현 칸에 `⏸ 보류(n회, 최고 m점)` 로 적고 남은 문제를 비고에 기록. 완료가 아니다).
  - **2차 폴리시 라운드**: 미구현 화면이 하나도 안 남으면(전부 구현 or 보류), 보류 화면을 **점수 낮은 순**으로
    다시 잡아 8점까지 올린다. 이때 회차 상한이 +10회 리셋된다. 8점 도달 화면만 완료다.
  - 두 비평가가 남은 감점을 **«아트 리소스/게임 서체 없이는 회수 불가»** 로 분류한 화면은 CSS 로 더 밀어도
    소용없으므로 **«보류(아트 대기)»** 로 적고 넘어간다 — 아트 리소스가 들어오면 일괄 재검해서 8점을 채운다.

(다) **연출 작업**(UI-REFERENCE 58번: 재화 획득·보상·강화·퀘스트 등 피드백 이펙트) → (나) 와 같이 최대 5회 루프.
  단 캡처는 정지 1장이 아니라 **연속 프레임 6~8장**(playwright 로 트리거 직후 80~100ms 간격)을 비평가 2명에게 준다.
  채점 축은 ①타이밍(길이·이징) ②크기 비율 ③위치·경로(재화는 HUD 알약에 정확히 도착) ④가독성·일관성 — 최저 8 통과.
  «연출 없음» 은 무조건 0점이다. 09·12·17 의 기존 연출 톤과 어긋나면 ④ 감점.
  - **골고루 원칙은 연출·쥬시(58·60)에도 그대로 적용된다.** 58·60 은 «공용 모듈 1회 제작 + 전역 부착» 이 작업 범위다 —
    한 화면의 연출만 파고들어 회차를 태우지 마라. 공용 모듈이 올라간 뒤로는 **각 화면 비평에 ⑦ 인터랙션 손맛(쥬시) 축을
    추가**해 8 기준으로 같이 채점하되, 화면당 회차 상한(10회)은 그대로다. 쥬시 때문에 특정 화면만 계속 잡히면 안 된다.

[4] 병렬 충돌 처리
- push 전 항상 `git pull --rebase origin main`.
- `index.html` 이 충돌하면 **양쪽 변경을 모두 살린다**(서로 다른 구간이라 대부분 자동 병합된다). 내 작업 구간이 아닌 곳은 남의 버전을 그대로 둔다.
- 병합 후 반드시 문법 검사 + **`node tools/smoke.js` 통과** 확인. 깨졌으면 고쳐서 push 한다.
- `PROGRESS.md` 가 충돌하면 **양쪽 행을 모두 살린다.**
- `docs/claims/*.lock` 이 충돌하면: **내 <ID>.lock 이 아닌 lock 파일은 절대 건드리지 않는다** — 남의 lock 은
  남의 버전 그대로 살린다(생성이든 삭제든). 내 <ID>.lock 자체의 충돌은 [-1] 4 규칙(경쟁 패배 = 포기)을 따른다.
- **push 전 `node tools/verifyProgress.js` — 종료 코드 0 이어야 한다(2026-08-29, 작업 374·388·557·571).**
  이 자는 **절이 넷**이다 — **§1 되돌림**(374: 완료행이 병합으로 등재문으로 되돌아갔다) ·
  **§2 자기모순**(388: `done()` 이 구현 칸·루프 횟수·비고 머리말을 등재 상태로 남겨 표가 «미완료» 로 읽힌다) ·
  **§3 마감 누락**(557: 제품·자·review 는 다 push 하고 **표를 아예 안 건드렸다** — 완료 표지가 0건이라
  §2 가 볼 모순이 없고 done() 이 얕은 클론 밖이면 §1 도 못 본다. 498 이 그래서 재선점됐다) ·
  **§4 병합 표시 잔재**(571: `claim.js` 의 autostash pop 이 충돌하면 트리에 병합 표시가 남는데
  **pull 자신은 종료 코드 0** 이라 조용하다 — 그 뒤 `git add -A` 가 표시째 커밋해 §1~§3 이 읽을 표가 깨진다.
  `618d000` 이 실제로 그랬고 363 이 `UI-REFERENCE.md` 에서 같은 것을 걷어낸 **재발**이다).
  **§4 가 빨개지면 절대 `git add -A` 로 밀지 마라** — 표시 세 줄만 지우고 **위·아래를 둘 다 남긴 뒤**
  그 파일만 `git add` 한다([4] «양쪽을 모두 살린다»). 남의 완료 행을 지우면 이번엔 §1 이 빨개진다.
  ⚠ 기록에 표시 문자열을 적을 때는 **줄 첫머리가 아니라 줄 가운데 백틱**으로 인용해라(§4 가 잡는다).
  같은 사고를 `claim.js` 가 먼저 막는다 — 코드 1 로 «병합 표시» 를 말하면 **lock 은 안 건드려진 것**이니
  풀고 나서 같은 명령을 다시 부르면 된다(90분 규칙에 안 걸린다). 자 자신의 회귀는 `node tools/verify571.js`.
  §3 이 빨개지면 **그 행이 마감을 안 한 것**이다 — review 의 마지막 회차 기록을 읽고 정말 끝났으면
  세 칸을 채워 마감하고, 안 끝났으면 lock 을 잡아 이어서 한다. **자산이 있어도 lock 이 살아 있으면 §3 은 제외한다**
  (회차 기록도 같은 파일 이름을 쓴다). 자 실행이 붙어 있어 느리면 `--no-gate`(⚠ E2 축이 꺼진다) · `--gate-timeout <초>`.
  §2 가 빨개지면 **행을 지우지 말고 위 [1] 의 세 칸을 채워라.** 자 자신의 회귀는 `node tools/verify388.js`.
  `PROGRESS.md` 충돌을 «내 사본» 으로 풀면 **남의 완료 행이 등재 당시의 «미착수» 로 되돌아간다.**
  2026-08-29 에 실제로 그랬고(`5fe9d37` 이 368·369 를 되돌렸다), 표가 곧 작업 배정 입력이라
  **끝난 369 를 다음 워커가 통째로 다시 선점했다.** 이 자는 그 순간 빨개진다 —
  빨개지면 그 `done(<ID>)` 커밋의 행을 그대로 되살리고, done 이후의 정당한 편집이 있으면 그것까지 살린다.
- 비평 기록은 `docs/review/<ID>-이름.md` 에 자기 파일로만 쓴다. 공용 `docs/review-log.md` 에는 추가하지 마라.

[5] 컨텍스트 관리 (매우 중요)
- 이미지는 절대 메인 세션에서 직접 Read 하지 마라. 측정·채점은 Task 서브에이전트에게만 시키고 텍스트 결과만 받는다.
- 서브에이전트를 띄웠으면 **결과를 기다렸다가 받아서 진행한다.** 기다리는 동안 세션을 끝내지 말 것.
- index.html 전체를 반복해서 읽지 말고 grep 으로 필요한 구간만 찾아 부분 편집한다.
- 컨텍스트가 70%를 넘으면 하던 작업을 마무리 짓고 PROGRESS.md 갱신 → lock 삭제 → 커밋 → 종료한다.

[6] 지켜야 할 것
- 매 편집 후 문법 검사 + 실제 브라우저 실행으로 런타임 에러가 없는지 확인한다.
- **push 전 `node tools/smoke.js` 는 필수 게이트다(2026-08-25).** `SMOKE PASS` 가 아니면 push 하지 않는다 — 자동 플레이·팝업 전부 열기·
  NaN/undefined 검출·화면비 4종 프레임/시트 잘림을 본다. 실패하면 원인을 고쳐 다시 PASS 시킨 뒤 push. 남의 구간이 원인이면
  그 구간을 건드리지 말고 PROGRESS 비고에 «smoke 실패: …» 로 남기고 lock 을 풀고 종료한다.
  준비: `node tools/smoke.js` 가 playwright 를 못 찾으면 `npm i --no-save playwright && npx playwright install chromium` 후 재실행.
  새 팝업·탭·오버레이를 만들면 **smoke.js 의 오프너 목록·시트 id 목록에 추가**하는 것까지가 그 작업의 범위다.
- 저장 구조를 바꾸면 세이브 마이그레이션 또는 KEY 버전을 올린다.
- main 에 push 하면 GitHub Pages(https://kuzuni.github.io/kkkkkk/)로 자동 배포된다. 깨진 상태를 push 하지 마라.
- 모든 작업 단위가 완료되어 있으면 '완료됨'이라고만 보고하고 종료한다.
- 마지막에 3줄 이내로 요약한다. **어떤 <ID> 를 잡아 무엇을 바꿨는지**를 반드시 포함한다.
