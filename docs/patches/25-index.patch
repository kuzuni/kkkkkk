diff --git a/index.html b/index.html
index 7335827..ce36b12 100644
--- a/index.html
+++ b/index.html
@@ -2480,7 +2480,6 @@
   .eqc .sky{background:#F9E9C8}
   /* 하늘/바닥 경계는 직선이 아니라 완만한 아치 — 중앙 394, 좌우 끝 413(+19) */
   .eqc .gnd{background:#E1BE99;border-radius:50% 50% 0 0/24px 24px 0 0}
-  .eqc .band{background:#B38362}
   /* 배경 실루엣 무늬(#F3DCBA, 낱개 73x68, pitch 152) — 하늘 영역에만 */
   .eqc .sky::before{content:'';position:absolute;inset:0;
     background-image:radial-gradient(ellipse 30px 26px at 26px 40px,#F3DCBA 0 99%,transparent 99%),
@@ -2502,7 +2501,7 @@
   /* 카드 전경 레이어 — 자식은 «패널 local y = ref−577», x 는 ref 1:1 을 그대로 쓴다 */
   #eqCards{left:0;right:0;top:0;bottom:0;pointer-events:none}
   #eqCards>*{position:absolute}
-  #eqCards .eqsl,#eqCards .eqlk{pointer-events:auto}
+  #eqCards .eqsl{pointer-events:auto}
 
   /* ---- 이름 리본 (359x105, 가운데 판 299 + 좌우 꼬리) ---- */
   .eqrb{width:359px;height:105px}
@@ -2617,26 +2616,11 @@
       linear-gradient(#9BE227,#9BE227) center/9px 25px no-repeat}
 
   /* ---- 캐릭터 일러스트 (이모지 대체 — 실제 아트 들어오면 교체) ---- */
-  .eqil{text-align:center;pointer-events:none}
-  .eqil.c1{left:150px;width:340px;top:367px;height:254px;font-size:236px;line-height:254px}
-  .eqil.c2{left:160px;width:340px;top:851px;height:350px;font-size:300px;line-height:350px}
-
-  /* ---- 잠금 슬롯 3개 (Ø109, pitch 170, 중심 369.5/539.5/709.5) ---- */
-  /* 가장자리가 소프트한 그림자 원판이라 단색 원이 아니라 radial-gradient */
-  /* 정지점 84% 면 «색차 30 이내» 로 잡히는 지름이 106 이라 실측 109 보다 3px 작다.
-     r58 · 검출반경 = (s + 0.47·(100−s))% → s=88.5 에서 54.5 → **Ø109** */
-  .eqlk{top:1293px;width:116px;height:116px;
-    background:radial-gradient(circle closest-side at 50% 50%,
-      #74553F 0 88.5%,rgba(116,85,63,0) 100%)}
-  .eqlk.p1{left:312px}
-  .eqlk.p2{left:482px}
-  .eqlk.p3{left:652px}
-  /* 라벨 — 잉크 1962~1988(h27) → font 29, 회색 #B5B5B5 + 검정 외곽선 한쪽 4(=8) */
-  .eqlk>u{position:absolute;left:-20px;right:-20px;top:84px;height:40px;line-height:40px;
-    text-align:center;font-size:29px;font-weight:900;color:#B5B5B5;
-    -webkit-text-stroke:8px #000;paint-order:stroke fill;letter-spacing:-2px;
-    text-indent:-2px;white-space:nowrap}
-  .eqlk>u>i{transform:scaleX(.94)}
+  /* 작업 25: 카드 1장이 컨테이너 전체(1284)를 쓴다. 리본·슬롯·알약·Lv 은 레퍼런스 카드1 규격
+     그대로 고정이고, **늘어난 높이는 일러스트가 흡수한다**(340x254 → 440x470, 바닥 정렬 유지).
+     지면 비율도 카드1 그대로 — 하늘 68.5% / 지면 31.5%, 발끝은 지면 48.6% 지점 */
+  .eqil{text-align:center;pointer-events:none;
+    left:100px;width:440px;top:740px;height:470px;font-size:420px;line-height:470px}
 
   /* 자물쇠 아이콘 — 흰 고리 34x18 + 몸통 55x39, 전체 bbox 67x72 */
   /* 원반 안 자물쇠·라벨이 통째로 5~6px 위에 있었다 —
@@ -3698,11 +3682,10 @@
       <div class="eqp-ti"><i>장비</i></div>
       <div class="eqp-bd"></div>
       <div class="eqc">
-        <div class="sky" style="top:0;height:581px"></div>
-        <div class="gnd" style="top:398px;height:183px"></div>
-        <div class="sky" style="top:581px;height:577px"></div>
-        <div class="gnd" style="top:967px;height:191px"></div>
-        <div class="band" style="top:1158px;height:126px"></div>
+        <!-- 작업 25: 카드 1장. 하늘/지면 비율은 레퍼런스 카드1 그대로(398/581 = 68.5%),
+             잠금 슬롯 띠(.band)는 폐기 -->
+        <div class="sky" style="top:0;height:1284px"></div>
+        <div class="gnd" style="top:880px;height:404px"></div>
       </div>
       <div class="eqc-fr"></div>
       <div class="eqc-hl"></div>
@@ -3712,7 +3695,9 @@
         <div class="eqtd d2"></div>
         <div class="eqtc on" style="left:-12px"><b><i>장비</i></b></div>
         <div class="eqtc" style="left:237px" data-eqtab="sk"><b><i>스킬</i></b></div>
-        <div class="eqtc lk" style="left:474px" data-eqlock="동료"><b><i>동료</i></b><s class="eqli"></s></div>
+        <!-- 작업 25: «동료» 해금 → `bPet`. 같은 날 올라온 작업 26 이 `#bPet` 을 동료 팝업으로
+             교체했으므로 이 탭은 곧바로 26 동료 팝업으로 들어간다 -->
+        <div class="eqtc" style="left:474px" data-eqtab="pet"><b><i>동료</i></b></div>
         <div class="eqtc lk" style="left:711px" data-eqlock="기타"><b><i>기타</i></b><s class="eqli"></s></div>
       </div>
     </div>
@@ -3744,7 +3729,8 @@
        공용 #modal 껍데기로는 표현이 안 된다. 측정표 docs/measure/05-무기팝업.md -->
   <div id="wpnw">
     <div class="wm">
-      <div class="wm-head"><i>무기</i></div>
+      <!-- 작업 25: 무기 전용 → 부위별(무기·방패·목걸이) 공용. 타이틀·효과 라벨은 renderWpn 이 채운다 -->
+      <div class="wm-head" id="wpnHead"><i>무기</i></div>
       <div class="wm-fr"></div>
       <div class="wm-ar l" id="wpnPrev"></div>
       <div class="wm-ar r" id="wpnNext"></div>
@@ -3759,8 +3745,8 @@
           <div class="wm-tb">
             <div class="hd"></div><div class="bd"></div><div class="dv"></div>
             <div class="h1"><i>보유 효과</i></div><div class="h2"><i>장착 효과</i></div>
-            <div class="l1"><i>공격력</i></div><div class="v1" id="wpnOwnV"></div>
-            <div class="l2"><i>공격력</i></div><div class="v2" id="wpnEqV"></div>
+            <div class="l1" id="wpnL1"><i>공격력</i></div><div class="v1" id="wpnOwnV"></div>
+            <div class="l2" id="wpnL2"><i>공격력</i></div><div class="v2" id="wpnEqV"></div>
           </div>
         </div>
         <div class="wm-grid" id="wpnGrid"></div>
@@ -7280,16 +7266,26 @@ const WGRADE = [
 const wpct  = v => (v*100).toFixed(1) + '%';
 const wpct2 = v => (v*100).toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/, ',') + '%';
 
-const wpnList = () => EQUIPS.filter(e => e.slot === 'weapon');
+/* 작업 25 — 이 팝업은 «무기» 전용이 아니라 **부위별 아이템 팝업**이다.
+   06 이 무기·방패·목걸이 3슬롯이 되면서 세 부위가 같은 껍데기를 부위만 바꿔 재사용한다
+   (UI-REFERENCE §6 «우리 게임 설계»). 레이아웃·측정값은 05 그대로, 바뀌는 것은
+   타이틀 · 효과 라벨 · 빈 칸 아이콘 · 대상 슬롯(S.eqSlot[wpnPart]) 4가지뿐이다. */
+let wpnPart = 'weapon';
+const wpnSlotDef = () => SLOTS.find(s => s.k === wpnPart) || SLOTS[0];
+const wpnList = () => EQUIPS.filter(e => e.slot === wpnPart);
 function wpnCur(){
   const l = wpnList();
-  return l.find(e => e.id === wpnSel) || l.find(e => e.id === S.eqSlot.weapon) || l[0];
+  return l.find(e => e.id === wpnSel) || l.find(e => e.id === S.eqSlot[wpnPart]) || l[0];
 }
 const wpnTotalOwn = () => wpnList().reduce((s, e) => s + (has(e.id) ? ownVal(e) : 0), 0);
 
 function renderWpn(){
-  const l = wpnList(), cur = wpnCur(), g = GRADE[cur.g], wg = WGRADE[cur.g];
+  const l = wpnList(), cur = wpnCur(), g = GRADE[cur.g], wg = WGRADE[cur.g], sd = wpnSlotDef();
   const own = has(cur.id), lvNow = oLv(cur.id), need = fragNeed(lvNow), got = own ? frag(cur.id) : 0;
+  /* 부위별 문구 — 타이틀 · 보유/장착 효과 라벨 */
+  $('wpnHead').innerHTML = '<i>' + sd.n + '</i>';
+  $('wpnL1').innerHTML   = '<i>' + sd.stat + '</i>';
+  $('wpnL2').innerHTML   = '<i>' + sd.stat + '</i>';
   /* 상단 선택 아이템 정보 */
   const ib = $('wpnIc');
   ib.textContent = cur.ic;
@@ -7310,13 +7306,13 @@ function renderWpn(){
     const w = WGRADE[gi], it = l.find(e => e.g === gi);
     for(let c = 0; c < WPN_COLS; c++){
       const real = c === 0 ? it : null;
-      const mine = real && has(real.id), eq = real && S.eqSlot.weapon === real.id;
+      const mine = real && has(real.id), eq = real && S.eqSlot[wpnPart] === real.id;
       const n = mine ? frag(real.id) : 0, nd = real ? fragNeed(mine ? oLv(real.id) : 1) : 2;
       h += '<div class="wgc' + (eq ? ' eq' : '') + (mine ? '' : ' lk') + '"'
         +  (mine ? ' data-wpn="' + real.id + '"' : '')
         +  ' style="left:' + (10 + c*170) + 'px;top:' + (32 + r*190) + 'px'
         +  ';--gc:' + w.c + ';--gr:' + w.r + ';--ge:' + w.e + '">'
-        +  '<em class="ic">' + (real ? real.ic : '⚔️') + '</em>'
+        +  '<em class="ic">' + (real ? real.ic : sd.ic) + '</em>'
         +  (mine ? '<b class="lv"><i>Lv.' + oLv(real.id) + '</i></b>' : '')
         +  (eq   ? '<u class="eqt"><i>장착 중</i></u>' : '')
         +  (mine ? '' : '<div class="lock"></div>')
@@ -7327,8 +7323,8 @@ function renderWpn(){
   }
   $('wpnGrid').innerHTML = h;
   /* 하단 총 보유 효과 + 버튼 2개 */
-  $('wpnTotal').innerHTML = '<i>총 보유 효과: <b>공격력 +' + wpct2(wpnTotalOwn()) + '</b></i>';
-  const eqNow = S.eqSlot.weapon === cur.id;
+  $('wpnTotal').innerHTML = '<i>총 보유 효과: <b>' + sd.stat + ' +' + wpct2(wpnTotalOwn()) + '</b></i>';
+  const eqNow = S.eqSlot[wpnPart] === cur.id;
   const bEq = $('wpnBtnEq');
   bEq.classList.toggle('off', eqNow || !own);
   bEq.innerHTML = '<i>' + (eqNow ? '장착 중' : '장착') + '</i>';
@@ -7336,8 +7332,10 @@ function renderWpn(){
   $('wpnBtnUp').classList.toggle('off', !up);
   $('wpnBtnUp').innerHTML = '<i>일괄 강화' + (up ? ' (' + up + ')' : '') + '</i>';
 }
-function openWeapon(id){
-  wpnSel = id || S.eqSlot.weapon || null;
+function openWeapon(id, part){
+  wpnPart = (part && SLOTS.some(s => s.k === part)) ? part
+          : (id && EQ[id]) ? EQ[id].slot : 'weapon';
+  wpnSel = id || S.eqSlot[wpnPart] || null;
   wpnPage = 0;
   renderWpn();
   $('wpnw').classList.add('on');
@@ -7351,13 +7349,14 @@ $('wpnGrid').addEventListener('click', e => {
 });
 $('wpnBtnEq').onclick = () => {
   const cur = wpnCur();
-  if(!has(cur.id) || S.eqSlot.weapon === cur.id) return;
-  S.eqSlot.weapon = cur.id; markDirty(); uiDirty = true; save(); renderWpn();
+  if(!has(cur.id) || S.eqSlot[wpnPart] === cur.id) return;
+  S.eqSlot[wpnPart] = cur.id; markDirty(); uiDirty = true; save(); renderWpn();
 };
 $('wpnBtnUp').onclick = () => {
   const r = levelUpAll(wpnList());
   renderWpn();
-  if(!openUpAll(r.ups)) popup('강화 재료 부족', '<p>강화할 수 있는 무기가 없습니다.</p>');
+  if(!openUpAll(r.ups))
+    popup('강화 재료 부족', '<p>강화할 수 있는 ' + wpnSlotDef().n + '가 없습니다.</p>');
 };
 /* 페이지 화살표 — 등급 4행씩 넘긴다 */
 const wpnPages = () => Math.ceil(GRADE.length / WPN_ROWS);
@@ -7366,14 +7365,15 @@ $('wpnNext').onclick = () => { wpnPage = (wpnPage + 1) % wpnPages(); renderWpn()
 /* 딤(모달 바깥)을 누르면 닫힌다. 탭바를 눌러도 닫는다 */
 $('wpnw').addEventListener('click', e => { if(e.target.id === 'wpnw') closeWeapon(); });
 $('tabbar').addEventListener('click', closeWeapon, true);
-/* 정식 진입점 — 06 장비 팝업의 «무기 슬롯».
+/* 정식 진입점 — 06 장비 팝업의 부위 슬롯 3칸(무기·방패·목걸이).
    06 이 «슬롯을 누르면 05 구간으로 내려간다» 고 설계해 뒀으므로(그 쪽 주석 참조)
-   06 의 핸들러는 건드리지 않고 추가 리스너만 붙인다. 방어구/반지 등 다른 부위는
-   해당 화면이 생길 때까지 06 의 기존 동작(배너 패널)에 맡긴다.
+   06 의 핸들러는 건드리지 않고 추가 리스너만 붙인다.
+   작업 25 전에는 무기 칸만 열렸고 방패·목걸이는 배너 패널에 맡겼는데, 3슬롯이 되면서
+   세 부위 모두 이 팝업을 «부위만 바꿔» 연다.
    (05 가 임시로 쓰던 «영웅 → 장비 서브탭» 직결 진입은 06 이 그 자리를 가져가면서 제거했다) */
 $('eqCards').addEventListener('click', e => {
   const s = e.target.closest('[data-eqslot]');
-  if(s && s.dataset.eqslot === 'weapon') openWeapon();
+  if(s) openWeapon(null, s.dataset.eqslot);
 });
 
 /* ==========================================================================
@@ -7823,30 +7823,21 @@ $('bPet').addEventListener('click', e => {
 
 /* ==========================================================================
    06 장비 팝업 (바닥 시트) — 측정표 docs/measure/06-장비팝업.md
-   레퍼런스는 «캐릭터 2명(햄지·매옹이) × 부위 슬롯 2칸» + «잠금 3칸(반지·목걸이·탈것)» 이다.
-   우리 저장 구조는 캐릭터 1명 + 장비 3부위(무기·방패·목걸이)뿐이라 아래처럼 **잠정 매핑**했다.
-   «캐릭터 2명 구조»는 PROGRESS.md 의 미결 항목이라 이 회차에서 확정하지 않았고,
-   **저장 구조는 전혀 건드리지 않았다(표시 전용)**.
-     · 햄지   = 플레이어 — 무기 = S.eqSlot.weapon / 방어구 = S.eqSlot.shield
-     · 매옹이 = 동료     — 두 칸에 장착 펫 1·2 를 표시(공격력은 실제 펫 합산 데미지)
-     · 잠금 3칸은 레퍼런스대로 전부 잠금 표시. 단 목걸이는 우리 게임에 이미 있는 장비라
-       클릭하면 기존 목걸이 목록으로 내려가도록 접근 경로만 남겼다.
+   레퍼런스는 «캐릭터 2명(햄지·매옹이) × 부위 슬롯 2칸» + «잠금 3칸(반지·목걸이·탈것)» 이지만
+   **작업 25(저장소 주인 지시, UI-REFERENCE §6 «우리 게임 설계»)로 아래처럼 확정됐다**:
+     · 캐릭터 카드는 **1장(«용사» = 플레이어)** 이고 카드 1장이 컨테이너 전체를 쓴다.
+     · 부위 슬롯은 세로 **3칸 = 무기·방패·목걸이**(SLOTS 그대로) — 2칸 아님.
+     · 잠금 슬롯 행(반지·목걸이·탈것)은 **폐기**(목걸이는 정식 부위로 승격, 반지·탈것은 없다).
+     · 서브탭 «동료» 해금 → 26 동료 팝업 자리(현재는 기존 동료 패널 `bPet`).
+   리본·슬롯·뱃지·Lv·스탯 알약의 **규격은 레퍼런스 카드1 실측값 그대로**이고, 카드가 581 →
+   1284 로 커지며 남은 높이는 **일러스트와 하늘/지면 배경이 흡수**한다(.eqil 주석 참조).
+   **저장 구조는 전혀 건드리지 않았다(표시 전용 — S.eqSlot 을 읽기만 한다)**.
    ========================================================================== */
-const EQP_PETIC = { bird:'🐦', robo:'🤖', dragon:'🐉' };
-/* 카드별 기하 — 전부 «패널 local y = 레퍼런스 y − 577». 카드2 슬롯만 실측대로 +4 */
-const EQP_GEO = [
-  { rb:167, sl:[189,444], st:627, sw:614, hp:623, il:'c1' },
-  { rb:746, sl:[770,1025], st:1204, sw:1191, hp:1200, il:'c2' }
-];
-const EQP_CARD = [
-  { n:'햄지',   rb:'#F87D2F', rd:'#C7501D', rt:'#E86F2A', ic:'🐹' },
-  { n:'매옹이', rb:'#0AAFEF', rd:'#0A75A4', rt:'#0C8FC5', ic:'🐱' }
-];
-const EQP_LOCK = [
-  { n:'반지',   k:null },
-  { n:'목걸이', k:'amulet' },   /* 우리 게임엔 이미 있는 부위 — 클릭 시 목록으로 */
-  { n:'탈것',   k:null }
-];
+/* 카드 1장 기하 — 리본·슬롯은 레퍼런스 카드1 값 그대로(패널 local y = 레퍼런스 y − 577),
+   슬롯 3번째는 같은 pitch 255 로 이어 붙였다. 스탯 알약은 카드1 과 같은 «카드 바닥에서 39px»
+   규칙으로 카드 바닥(패널 1418)에 맞춰 내렸다. sw/hp 아이콘 오프셋(−13/−4)도 카드1 그대로 */
+const EQP_GEO = { rb:167, sl:[189, 444, 699], st:1330, sw:1317, hp:1326 };
+const EQP_CARD = { n:'용사', rb:'#F87D2F', rd:'#C7501D', rt:'#E86F2A', ic:'🦸' };
 
 /* 슬롯 1칸 HTML. 뱃지는 슬롯 좌상단 모서리에 걸치므로 슬롯 **뒤에** 그린다(뒤 형제가 위) */
 function eqpSlot(y, badge, ic, lv, attr){
@@ -7857,44 +7848,22 @@ function eqpSlot(y, badge, ic, lv, attr){
        + '<div class="eqbd" style="top:' + (y - 19) + 'px"><em>' + badge + '</em></div>';
 }
 function renderEqPage(){
-  const pets = S.eqPet.filter(id => PT[id]);
-  const petAtk = pets.reduce((s, id) => s + petDmg(PT[id]), 0);
-  const rows = [
-    { atk:stat.dmg, hp:stat.maxHp, sl:[
-        { k:'weapon', id:S.eqSlot.weapon }, { k:'shield', id:S.eqSlot.shield } ] },
-    /* 매옹이 체력은 대응 데이터가 없어 플레이어 체력의 75% 로 **임시 표시**한다(밸런스 무관) */
-    { atk:petAtk, hp:stat.maxHp * 0.75, sl:[
-        { p:pets[0] }, { p:pets[1] } ] }
-  ];
-  let h = '';
-  rows.forEach((r, i) => {
-    const g = EQP_GEO[i], c = EQP_CARD[i];
-    h += '<div class="eqil ' + g.il + '"><em>' + c.ic + '</em></div>'
-      +  '<div class="eqrb" style="top:' + g.rb + 'px;left:' + (i ? 179 : 174) + 'px;'
-      +    '--rb:' + c.rb + ';--rd:' + c.rd + ';--rt:' + c.rt + '">'
-      +    '<div class="tl l"></div><div class="tl r"></div>'
-      +    '<div class="pl"></div><b><i>' + c.n + '</i></b></div>';
-    r.sl.forEach((s, j) => {
-      const badge = j ? '🛡️' : '⚔️';
-      if(s.k !== undefined){
-        const it = s.id ? EQ[s.id] : null;
-        h += eqpSlot(g.sl[j], badge, it ? it.ic : '', it ? oLv(it.id) : null,
-                     'data-eqslot="' + s.k + '"');
-      }else{
-        const p = s.p ? PT[s.p] : null;
-        h += eqpSlot(g.sl[j], badge, p ? EQP_PETIC[p.sp] : '', p ? oLv(p.id) : null,
-                     'data-eqpet="1"');
-      }
-    });
-    h += '<div class="eqic sw" style="top:' + g.sw + 'px"><i></i></div>'
-      +  '<div class="eqic hp" style="top:' + g.hp + 'px"><i></i></div>'
-      +  '<div class="eqst a" style="top:' + g.st + 'px"><i>' + fmt(r.atk) + '</i></div>'
-      +  '<div class="eqst b" style="top:' + g.st + 'px"><i>' + fmt(r.hp) + '</i></div>';
-  });
-  EQP_LOCK.forEach((l, i) => {
-    h += '<div class="eqlk p' + (i+1) + '"' + (l.k ? ' data-eqslot="' + l.k + '"' : '')
-      +  '><s class="eqli"></s><u><i>' + l.n + '</i></u></div>';
+  const g = EQP_GEO, c = EQP_CARD;
+  let h = '<div class="eqil"><em>' + c.ic + '</em></div>'
+    +  '<div class="eqrb" style="top:' + g.rb + 'px;left:174px;'
+    +    '--rb:' + c.rb + ';--rd:' + c.rd + ';--rt:' + c.rt + '">'
+    +    '<div class="tl l"></div><div class="tl r"></div>'
+    +    '<div class="pl"></div><b><i>' + c.n + '</i></b></div>';
+  /* 부위 슬롯 3칸 = SLOTS(무기·방패·목걸이). 뱃지 아이콘도 SLOTS 정의를 그대로 쓴다 */
+  SLOTS.forEach((s, j) => {
+    const id = S.eqSlot[s.k], it = id ? EQ[id] : null;
+    h += eqpSlot(g.sl[j], s.ic, it ? it.ic : '', it ? oLv(it.id) : null,
+                 'data-eqslot="' + s.k + '"');
   });
+  h += '<div class="eqic sw" style="top:' + g.sw + 'px"><i></i></div>'
+    +  '<div class="eqic hp" style="top:' + g.hp + 'px"><i></i></div>'
+    +  '<div class="eqst a" style="top:' + g.st + 'px"><i>' + fmt(stat.dmg) + '</i></div>'
+    +  '<div class="eqst b" style="top:' + g.st + 'px"><i>' + fmt(stat.maxHp) + '</i></div>';
   $('eqCards').innerHTML = h;
 }
 
@@ -7909,15 +7878,13 @@ function syncEquipPage(){
   if(on) renderEqPage();
 }
 $('eqCards').addEventListener('click', e => {
-  const s = e.target.closest('[data-eqslot]'), p = e.target.closest('[data-eqpet]');
-  if(s){ S.eqTab = s.dataset.eqslot; eqPageOn = false; syncEquipPage(); uiDirty = true; renderUI(); return; }
-  if(p){ heroTab = 'pet'; S.heroTab = 'pet'; syncPanel(); uiDirty = true; renderUI(); return; }
-  const l = e.target.closest('.eqlk');
-  if(l) popup('🔒 ' + l.textContent.trim(), '<p>아직 해금되지 않은 부위입니다.</p>');
+  const s = e.target.closest('[data-eqslot]');
+  if(s){ S.eqTab = s.dataset.eqslot; eqPageOn = false; syncEquipPage(); uiDirty = true; renderUI(); }
 });
 $('eqTabs').addEventListener('click', e => {
   const t = e.target.closest('[data-eqtab]'), k = e.target.closest('[data-eqlock]');
-  if(t){ heroTab = 'sk'; S.heroTab = 'sk'; syncPanel(); uiDirty = true; renderUI(); return; }
+  /* 작업 25: 서브탭 목적지를 data 속성에서 읽는다(장비=현재 화면 · 스킬=07 · 동료=bPet) */
+  if(t){ heroTab = t.dataset.eqtab; S.heroTab = heroTab; syncPanel(); uiDirty = true; renderUI(); return; }
   if(k) popup('🔒 ' + k.dataset.eqlock, '<p>아직 해금되지 않은 콘텐츠입니다.</p>');
 });
 
