# 874 — `tools/verify814.js` [B11] 1건 실패(28/29) 판정

**T1 «버그(회귀)»** · 지시서 [3]-**(가)**(자 작업 — 비평가 없음) · 1회차 · sess-1257-12925 루틴 워커 D (2026-09-03)

---

## 1회차 (2026-09-03) — 갈래 ⓐ 확정, **제품 `index.html` 0줄 · 자 1자리(§R 신설)**

### 결론 한 줄
**등재 시점의 빨강은 이미 862 가 닫았다(유령).** 남은 일은 «그 닫음이 «문턱을 넓힌 수리» 인지» 를
가리는 것이었고 — **아니다.** 862 는 문턱을 **호스트 액자 띠에서 파생**시켰다. 그것을 못박는
**되돌림 시험 [R-e] 를 신설**해 항이 헛초록이 아님을 증명했다. `verify814` **29/29 → 30/30 PASS**.

### ① 등재문의 빨강을 현행 트리에서 재현 — **안 나온다**
등재문(sess-1142-20793, 838 7회차 곁다리)은 `[B11]` 이 «크기 Δ10.0px (허용 ≤2)» 로 빨갛다고 적었다.
현행 `main`(5cbca99)에서 자를 그대로 돌리면 **29/29 PASS** 이고 `[B11]` 은 초록이다.

- 자리: 등재는 **11:42**, 862 의 `done` 은 **11:50** — **등재가 8분 빨랐다.**
- 즉 874 는 «862 가 자기 회귀 스윕에서 닫기 직전» 의 창을 찍은 행이다.
  등재문 자신이 그 순서를 예고하고 있었다 — «862 lock 이 풀린 뒤에도 빨가면 그때 선점하라».

### ② 갈래 판정 — **ⓐ 가 옳고, 862 가 이미 ⓐ 를 골랐다**
등재문의 두 갈래:
- **ⓐ** 862 의 들이기가 옳고 `[B11]` 의 «Δ ≤ 2» 가 갱신돼야 한다.
- **ⓑ** 862 가 코스튬 카드까지 넓힌 것이 과잉이다.

`3c44468`(done(862)) 의 `tools/verify814.js` diff 를 읽으면 862 가 한 것은 **ⓐ 이고, 그중에서도
«문턱을 넓히는» 쪽이 아니다**:

```
-    ok(!!f && dc <= 2 && ds <= 2 && /fxFlash/.test(f.an),
+    ok(!!f && dc <= 2 && Math.abs(ds - 2 * ring) <= 2 && /fxFlash/.test(f.an),
```
`ring` 은 손 상수가 아니라 **제품이 답한다**(`hostRing: fxRingIn(sel)` — 호스트의 테두리 + 안쪽 링).

- **축은 그대로**다 — «플래시가 누른 카드 본체를 덮는가». 중심 Δ ≤ 2 는 **한 글자도 안 바뀌었다**.
- 크기 항은 «Δ 가 작아야 한다» 에서 «Δ 가 **띠 ×2 와 같아야** 한다» 로 **방향이 갈렸다**(333 처방).
  넓힌 것이라면 Δ 가 12 든 40 이든 초록이겠지만, 지금은 **10 ± 2 를 벗어나면 양쪽으로 빨개진다.**

### ③ 헛초록이 아님을 못박는다 — **[R-e] 신설**(이 회차의 실제 산출물)
«넓히는 수리는 반려» 라는 등재문의 요구는 **읽어서** 가 아니라 **자로** 답해야 한다.
862 이전 한 줄(`const ring = inset ? fxRingIn(el) : 0;`)로 되돌리는 주입을 §R 에 넣었다.

| 판 | 플래시 상자 크기 Δ | 액자 띠 ×2 | 어긋남 | `[B11]` |
|---|---|---|---|---|
| 현행(862 판) | **10.0px** | 10px | 0.0px | 초록 |
| 862 이전 판(주입) | **0.0px** | 10px | **10.0px** | **빨강** |

중심 Δ 는 두 판 다 0.0px — **움직인 것은 상자 크기 하나**이고, 그것이 정확히 등재문이 찍은
**Δ10.0 = 띠 5px × 2** 다. 즉 등재문의 수치와 862 의 처방과 이 자가 **같은 자리를 가리킨다**.

> ⚠ `hostRing` 은 제품 코드가 아니라 **호스트 기하**에서 읽으므로 주입해도 5px 그대로다 —
> 주입이 자의 양쪽을 동시에 흔들어 «항상 초록» 이 되는 구멍은 없다.

### ④ 게이트·회귀
- `tools/verify814.js` **30/30 PASS**([R-e] 1항 신설 · 기존 29항 무수정)
- 회귀(862 와 같은 부품): `verify619` **65/65** · `verify666` **32/32** · `verify816` **27/27** — 전부 무수정 초록
- `verify862` — **14/17 ↔ 17/17 이 실행마다 갈린다(플레이키). 내 변경과 무관**하며 **876 으로 등재**했다(아래 ⑤).
- **SMOKE PASS**
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄

### ⑤ 곁다리 등재 — **876 (T1 «버그(자 · 플레이키)»)**
회귀 스윕에서 `verify862` 가 **같은 트리 · 같은 명령**으로 세 번 돌려 세 번 다른 값을 냈다:

| 실행 | 액자선 잔존(`[C1]`) | `[C2]` t0 «4.5:1 미만» | 결과 |
|---|---|---|---|
| 1 | **16.5%** (299/1812) | 10% | 14/17 FAIL |
| 2 | **100%** (1812/1812) | 6% | **17/17 PASS** |
| 3 | **1%** (18/1812) | 11% | 14/17 FAIL |

`[C1]` 은 «띠 화소의 90% 이상» 을 요구하는데 관측이 1~100% 를 오간다 — **문턱 문제가 아니라
«어느 프레임을 재는가» 가 안 고정된 것**이다(t0 캡처가 흰 판 애니메이션의 임의 위상에 걸린다).
`[R1]` 이 같이 빨개지는 것도 «지금 값» 을 기준으로 비교하기 때문이다(38.3% ↔ 지금 1%).
내 변경은 `tools/verify814.js` 한 파일이고 `verify862` 는 그 파일을 읽지 않는다 — **내 것이 아니다.**
상세·처방 후보는 PROGRESS 876 행. ⚠ 처음 875 로 적었다가 같은 시각 다른 워커가 875(`verify432`)를 먼저 등재해 **876 으로 개정**했다(번호 재사용 금지 규약).

### ⑥ 교훈
- **«빨갛다» 는 등재는 시각(時刻)까지 읽어야 한다.** 874 는 862 가 닫기 **8분 전**의 트리를 찍었고,
  등재문이 그 순서를 예고해 두지 않았다면 «자를 문턱으로 무르게 푸는» 수리가 들어갈 자리였다.
- **«이미 초록» 은 마감 사유가 못 된다.** 초록이 «항이 옳아서» 인지 «항이 헛돌아서» 인지는
  되돌림 시험만 답한다 — 그래서 이 회차의 산출물은 마감문이 아니라 **[R-e] 한 항**이다.
