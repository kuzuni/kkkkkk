# 30 — 던전 입장 화면 (비평 기록)

> 측정표 `docs/measure/30-던전입장.md` · 캡처 `docs/review/30-r1.png`(1회차) `30-r2.png`(2회차) `30-r3.png`(3회차)
> 검증기 `tools/verify30.js` · 캡처기 `tools/cap30.js`
> 워커 B · sess-0328-19087 (2026-08-25)

---

## 무엇을 만들었나

레퍼런스 30 은 «04 던전 세부 → [도전]» 을 누른 뒤의 **던전 전투 화면**이다.
02 기본 화면에서 **상단 HUD·좌우 사이드 아이콘·우상단 ▦ 메뉴·스테이지 헤더·좌하단 유틸·
우하단 미션 배너·하단 탭바가 전부 사라지고**, 그 자리에 던전 전용 UI 4종이 들어온다.

구현은 **28 보스전과 같은 방식** — 02 를 한 줄도 고치지 않고 상태 클래스 `#app.dunrun` 으로 덮는다.
클래스가 빠진 기본 상태는 그대로다.

| 새 요소 | ref bbox | 프레임 | 구현 |
|---|---|---|---|
| 던전 타이틀 `<이름> - 레벨 <층>` | 잉크 (388,177,303,35) | top 93 | `#dunTtl` — fs 38 · 검정 외곽선 10(=실측 5) |
| ⏱ 스톱워치 + `29.6` | (397,230,242,80) | top 146 | `#dunTm` — 39 보스전과 같은 에셋·같은 좌표 |
| 던전 진행바(계단형) | (190,315,700,80) | top 231 | `#dunBar` — 본체 700×67 + 우측 보스 탭 106×80(13px 하강) |
| 뒤로가기 화살표 | (47,1999,207,129) | left 47 · bottom 212 | `#dunOut` — CSS 삼각형(글리프 아님) |
| 배속 버튼 이동 | (963,2060,81,81) | right 36 · bottom 198 | `#app.dunrun #spdb` 규칙 1줄 |
| 스킬 슬롯 +180 | 2195..2313 | bottom 26 | **자동** — 탭바를 숨기면 `#stagearea` 가 180 자라 그대로 내려온다 |

### 기능 — 04 [도전] 은 이제 «30초 제한 전투» 다

| 조작 | 눌렀을 때 실제로 무엇이 바뀌는가 (헤드리스 확인) |
|---|---|
| 04 [도전] | 입장 횟수 −1 · `S.cnt.dungeon` +1 · 전장 초기화 · `#app.dunrun` 부착 · 던전 HUD 표시 · 상단 HUD/탭바 소멸 |
| (자동 전투) | ⏱ 가 30.0 → 0.0 으로 감소(`29.6` 형식) · 모든 피해원이 `hitEnemy()` 한 곳을 지나 누적 · 진행바 채움·수치 갱신 |
| 몹 전멸 | 스테이지를 **올리지 않고** 같은 스테이지로 다시 채운다(때릴 대상이 끊기면 진행바가 멈춘다) |
| 누적 피해 ≥ 요구 피해 | 그 자리에서 클리어 → 층 해금(`S.dun[id]++`) + 보상 지급 + **31 던전 클리어 화면** |
| 시간 초과 | 실패 팝업(누적/요구 피해 표기) · 층 유지 |
| ◀ 뒤로가기 | 런 중단(보상 없음) · `.dunrun` 해제 · 스테이지 복원 · 탭바·상단 HUD 복귀 |

`node tools/verify30.js` → **VERIFY30 PASS 49/49** (기하 33 + 기능 체크 13 + 콘솔 0).
`node tools/smoke.js` → **SMOKE PASS** (새 절 `[2-1] 던전 입장 화면` 4항목 포함).

### 밸런스 보정 — 판정이 «전투력» 에서 «누적 피해» 로 바뀐다

기존 `challengeDungeon` 은 `cp() ≥ d.req(f)` 를 즉시 비교하고 팝업을 띄웠다.
제한 시간 전투가 생겼으니 판정도 **30초 누적 피해 ≥ 요구 피해**로 옮겼다.
`요구 피해 = d.req(f) × DUN_DMG_K`. 계수는 눈대중이 아니라 **헤드리스 실측**으로 잡았다 —
기본 캐릭터(cp 503)를 스테이지별로 놓고 30초를 꽉 채워 실제 누적 피해를 쟀다:

| 스테이지 | cp() | `stat.dps` | 30초 실제 누적 피해 | 실제 DPS | **누적 피해 ÷ cp** |
|---|---|---|---|---|---|
| 1 | 503 | 26.3 | 791 | 26.4 | **1.57** |
| 12 | 503 | 26.3 | 589 | 19.6 | **1.17** |
| 40 | 503 | 26.3 | 315 | 10.5 | **0.63** |
| 90 | 503 | 26.3 | 329 | 11.0 | **0.65** |

적이 세질수록 사망·카이팅으로 가동률이 떨어진다. «자기 전투력 = 요구 전투력» 인 플레이어가
딱 통과하도록 **중앙값 1.2** 를 썼다(`DUN_DMG_K = 1.2`). 난이도는 기존 `cp ≥ req` 와 사실상 같고,
**던전 상수(`DUNGEONS`·`DUN_TRY`)와 저장 구조(`KEY`)는 한 줄도 건드리지 않았다.**

---

## 1회차 — 비평가 A 5 / B 5 (①~④ 최저)

두 비평가가 **독립적으로 같은 결함 2개**를 최상위로 지목했다.

| # | 지적 | A | B | 처리 |
|---|---|---|---|---|
| 1 | **`#battlefoot` 의 어두운 그라디언트 1080×178 이 남았다** — ref 같은 구간은 배경 단색 ±1, 판 0px. 프레임의 7.8 % | ① | ① | `#app.dunrun #battlefoot{background:none}` |
| 2 | **`STAGE 1` 토스트**가 화면 한가운데(424,1012,232,38) 남았다 — ref 스테이지 표기 0px | ① | ① | `startDunRun()` 에서 `msgTxt=''; msgT=0` |
| 3 | 진행바 radius 실측 **21~23** 인데 18 (−19 %) | ② | — | `em` 18→22 · `em::before` 12→16 · 탭 코너 동반 |
| 4 | 뒤로가기 판 h **129** 인데 132 (+2.7 %) — 2128~ 는 드롭섀도다 | ② | ② | h 132→129 · bottom 210→212 |
| 5 | 뒤로가기 radius 외곽 곡률 **10** (지정 14, border-box+border 6 탓) | ② | — | 지정 20 |
| 6 | 화살표 잉크 **95×74** (목표 97×77) | ② | ② | 삼각형 기하를 99×79 로(AA 가 사면 1~1.5px 을 먹는다) → 재실측 **97×76** |
| 7 | 배속 자물쇠 잉크 **32×46** (ref 36×43, 종횡비 −21 %) | ② | ② | `.sk{scale(1.12,.93)}` |
| 8 | 배속 우여백 37(ref 36) · 하단 200(ref 198) | ③ | ③ | right 36 · bottom 198 |
| 9 | 슬롯 하단 여백 28(ref 26~27) | ③ | ③ | `#app.dunrun #slots{margin-bottom:16px}` |
| 10 | 타이머 숫자 검정 스트로크 캡처 8px (ref 6, +33 %) | ⑤ | — | `-webkit-text-stroke` 13→11 |
| — | 슬롯 좌여백 27(ref 26) | ③ | ③ | **A4/02 구간이라 손대지 않았다** — 아래 «넘기는 것» |

### 1회차가 잡아낸 «측정표 초판의 오류» (2인 독립 재실측 일치)

측정표 §9-1 에 정정표로 남겼다. 요지:
- **뒤로가기 판 h 132 → 129** (2128~2138 은 판이 아니라 드롭섀도)
- **진행바 radius 18 → 22**
- **배속 원 (963,2059,81,82) → (963,2060,81,81), 우여백 36 · 하단 198**
- **슬롯 하단 여백 27 → 26**
- ⏱ 계단 꺾임은 «x784~788» 이 아니라 **x784..799 의 16px 라운드 전이**

정확했던 값: 타이틀 잉크 (388,177,303,35) · 타이머 잉크 (498,252,133,47) ·
해골 (818,335,45,41) · 바 안 수치 h20 cx539.5 · 슬롯 pitch 130 · 광택점 (61,2012,14,14) — 전부 1px 이내.

### 1회차 수치 중 «이미 맞았던» 것 (2회차에서도 유지)

| 요소 | 목표 | 캡처 실측 |
|---|---|---|
| 타이틀 잉크 | y93..127 (h35) · 중심 x539 | y93..127 (h35) · 중심 540 |
| 타이머 숫자 잉크 | (498,168,132,47) | (498,168,133,47) |
| 진행바 껍데기 | (190,231,700,80) · 중심 x540 | 동일 · Δ0 |
| 계단 돌출 | 13~14 | 13 |
| 주황 채움 | (205,244,·,42) | (205,244,·,42) |
| 바 안 수치 잉크 | h20 · 중심 x539.5 | h20 · 중심 540.5 |
| 해골 잉크 | (818,251,45,41) | (817,250,46,42) |
| 광택점 | (61,1952,14,14) | (61,1952,14,14) |
| 슬롯 8칸 중심 x · pitch · Ø | 86.5…996.5 · 130 · 120 | 전부 Δ≤0.5 |

---

## 2회차 — 수정 반영 캡처 `30-r2.png`

| 확인 | 결과 |
|---|---|
| 하단 어두운 판 | **사라짐** — x1060..1080 열 y2050..2270 이 (104,81,104)~(83,60,89) 로 캔버스 바닥 톤과 같다(1회차는 솔리드 `#0F0F1D`) |
| `STAGE 1` 토스트 | **0px** |
| ◀ 잉크 | **97×76** (1회차 95×74, 목표 97×77) |
| 타이틀·타이머 잉크 | Δ0 유지 |
| `tools/verify30.js` | **PASS 49/49** (기대치도 정정값으로 갱신) |
| `tools/smoke.js` | **SMOKE PASS** |

---

## 넘기는 것

1. **슬롯 좌여백 27(ref 26)** — `#slots{padding-left:4px}` 은 **A4/02 구간**이라 손대지 않았다.
   던전 화면만의 문제가 아니라 기본 화면에도 같이 있다. A4 나 02 를 다시 잡는 세션이 처리할 것.
2. **전투 캔버스 바닥 톤** — ref 는 `#82626F` 완전 단색이고 우리는 타일+비네트다.
   ⑤ 색 항목이라 통과 기준 밖이고, 던전별 바닥 색은 아트가 들어올 때 같이 정하는 편이 낫다.
3. **배속 자물쇠 색** — ref 금색 `#FDD16E`, 우리는 흰색. ⑤ 항목이라 «아트 대기».

## 교훈 (다른 화면에서도 재발한다)

1. **«요소를 숨겼다» 와 «그 요소가 만들던 그림자를 지웠다» 는 다르다.**
   `#tabbar{display:none}` 으로 탭바를 없앴는데, 탭바 위에 깔려 있던 `#battlefoot` 의
   **스크림 그라디언트(1080×178)** 는 그대로 남아 화면 하단 7.8 % 를 덮었다.
   비평가 2인이 독립적으로 이걸 1순위로 짚었다. 화면 상태를 바꿀 때는 **숨긴 요소의 «배경·그림자·
   스크림» 형제도 같이 훑을 것.**
2. **«토스트·타이머 같은 시간 의존 상태»는 화면 전환 시 명시적으로 지워야 한다.**
   로드 직후의 `showMsg('STAGE 1')` 이 1.7초 살아 있어 던전 화면 한가운데 남았다.
   캡처 타이밍에 따라 보이기도 안 보이기도 해서 **회차마다 다른 화면을 채점하게 된다.**
   전환 함수에서 `msgT = 0` 을 지우는 게 맞다(캡처 스크립트에서 우회하면 안 된다).
3. **글리프로 아이콘을 대체할 때는 «잉크 종횡비»부터 재라.**
   `◀` 글리프 잉크는 72×89(세로로 길다)인데 레퍼런스 화살표는 97×77(가로로 넓다)이라
   `scaleX(1.35)` 가 필요했고, 그러면 외곽선까지 찌그러진다. **CSS `border` 삼각형**으로 바꾸니
   bbox 를 정확히 차지하면서 외곽선 두께도 유지됐다(아트 자리 규칙에도 맞는다).
   덤: **안티에일리어싱이 사면에서 1~1.5px 을 먹는다** — 잉크 목표가 97×77 이면 기하는 99×79 로 잡아라.
4. **`box-sizing:border-box` + `border` 를 준 요소의 `border-radius` 는 «외곽 곡률» 이 아니다.**
   지정 14 가 실제 외곽에서는 10 으로 나온다(테두리 두께만큼 안쪽 곡률이 기준이 된다).
   레퍼런스에서 잰 «바깥 코너 반지름» 을 그대로 적으면 −25 % 가 된다.
5. **하단 고정 요소에 «ref y − 84» 를 쓰면 안 된다.** 프레임(2280)이 레퍼런스 콘텐츠(2256)보다
   24px 길고 그 차이는 캔버스가 먹는다. 슬롯 행 top 을 `2195−84 = 2111` 로 기대했다가
   실측 2133 에 «틀렸다» 고 판정할 뻔했다 — **하단 요소는 `bottom = 2340 − ref_bottom` 이 유일한 규칙**이다.
   (검증기 기대치가 틀렸던 것이지 구현이 틀린 게 아니었다.)

---

## 3회차 — 비평가 E 8 / F 9 (①~④ 최저) → **통과**

2회차는 **C 7 / D 9** 였다. C 가 ② 에서 깎은 항목만 골라 고쳤다.

| 지적 (2회차 C) | 수정 | 3회차 재실측 |
|---|---|---|
| 진행바 코너 radius 캡처 20/18 (ref 24) | CSS 22 → **26**(본체) / **29**(보스 탭 TR). AA 가 2~3px 을 먹는다 | 최상단행 inset **ref 24 / cap 23** (E 는 «+4 둥글다», F 는 «Δ0», 자체 스캔 Δ1 — 2:1 로 유지) |
| ◀ 검정 외곽선 6px → 2px (−67 %) | 검정 삼각형 111×89 → **124×106** | 중앙 열 위 5px / 아래 7px (ref 4~5 / 5) — F ②9 |
| ◀ 흰 잉크 h 76 (ref 78) | 흰 삼각형 99×79 → **99×81**, 2px 하강 | **97×78** = ref 97×78 (E·F 일치) |
| 광택점 Ø14 (ref 15×14) | **15×15 @ 패딩박스 (8,6)** | ref 14×13~15×14 vs cap 15×15 (Δ1) |
| 자물쇠 잉크 Δy +3 | `translateY(-3px)` 추가 | ref (986,2078,36,43) vs cap (986,2018,35,42) → Δ≤1 |
| 배율 라벨 잉크 h 14 (ref 16) | `font-size` 21 → **24** | h 17~19 (E 는 «폭 +12.5 %», F 는 «h +2» — ⑥ 기록만) |
| 흰 글자 검정 스트로크 두께 | 타이틀 10→**9** · 타이머 11→**9** · 바 수치 9→**7** | 타이틀 Δ≤1 · 타이머 좌우 Δ≤1 · 바 수치 여전히 +2~3(⑤⑥ 기록) |

### 3회차 최종 점수

| | E | F |
|---|---|---|
| ① 요소 배치 | **9** | **9** |
| ② 상대 크기 비율 | **8** | **9** |
| ③ 여백·간격 | **9** | **9** |
| ④ 정렬·그리드 | **9** | **9** |
| **①~④ 최저** | **8** | **9** |
| ⑤ 색·명암 (참고) | 8 | 8 |
| ⑥ 폰트 크기 비율 (참고) | 9 | 8 |

**통과 기준(①~④ 최저 8) 을 두 비평가 모두 충족 → 3회차에서 통과.**
두 비평가 모두 «레퍼런스에 없는데 캡처에 남은 UI 0건 / 있는데 없는 UI 0건» 을 픽셀로 확인했다.
2회차에서 잡은 하단 스크림·중앙 토스트는 3회차에서 **행평균 휘도에 계단 변화 0건**으로 재확인됐다.

### 3회차 기준 주요 요소 Δ (E·F 공통)

| 요소 | ref | cap | Δ |
|---|---|---|---|
| 타이틀 잉크 | (388,177,303,35) | (390,93,301,35) | x+2 · h 0 |
| 타이머 숫자 잉크 | (498,252,133,47) | (498,168,133,47) | **0** |
| 진행바 껍데기 | (190,315,700,80) | (190,231,700,80) | **0** |
| 검정 트랙 | (196,321,688,54) | (196,237,688,55) | h+1 |
| 주황 채움 | (205,328,·,42) | (205,244,·,42) | **0** |
| 바 안 수치 잉크 | h20 · 중심 x539.5 | h20 · 중심 x539.5 | **0** (폭은 문자열 상이) |
| 해골 잉크 | (818,335,45,41) | (817,250,46,42) | Δ1 |
| 뒤로가기 판 | (47,1999,207,129) | (47,1939,207,129) | **0** |
| ◀ 흰 잉크 | (101,2024,97,78) | (102,1964,97,78) | Δ1 |
| 배속 원 | Ø80~81 · 우 36 · 하 198 | Ø81 · 우 36~37 · 하 198~200 | Δ≤2 |
| 슬롯 8칸 | 중심 86.5…996.5 · pitch 130 · Ø119 | 동일 · pitch 130 · Ø120 | Δ≤0.5 |

### 남은 감점 — «아트 대기» 로 넘긴다

- **자물쇠 색** ref 금색 `#FDD16E` → 우리 흰색 (⑤)
- **배율 라벨 대비** ref 잉크 휘도 130~189 → 우리 96~106 (⑤) — 02 구간의 `#spdb .sv{color:#606060}`
- **바 안 수치 스트로크 +2~3px** (⑤⑥) · **⏱ 노란 본체 +7 %**(검정 아웃라인이 1px 얇은 결과, ②의 잔차)
- **전투 캔버스 바닥** ref 는 `#82626F` 완전 단색, 우리는 타일+비네트 (⑤)

이 4건은 전부 **색·아트 리소스 문제**라 CSS 로 더 밀어도 회수되지 않는다.
아트가 들어오면 일괄 재검한다.
