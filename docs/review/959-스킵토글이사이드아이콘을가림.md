# 959 — `#bossSkip`(연출 스킵 토글)의 투명 여백이 사이드 아이콘 탭을 먹는다

> **T1 «버그(제품 · 겹침)»** · 1회차 · sess-2043-13968 루틴 워커 C (2026-09-05)
> 제품 `index.html` **1줄**(+ 주석) · 레이아웃 **Δ0px** · 밸런스 계수 **0줄**

## 0. 등재문과 재현

등재문(PROGRESS 959, sess-1828-1415 워커 B — 946 2회차 회귀 스윕 곁다리):

> `tools/verify74.js` 「사이드 아이콘」 **191/200** · 실패 9건이 전부 한 얼굴 —
> «가려짐 · 최상위=`div#bossSkip.sm-sk.sh` · 착지도 같은 노드».

**등재문의 가설은 그대로 확인됐다**(338 과 달리 기각된 것이 없다). 다만 **낱말 하나를 정정한다** —
등재문은 «좌측 사이드 아이콘 **9칸**» 이라고 적었지만 `#sideR` 은 **작업 49 에서 통째로 삭제**됐고
사이드 열은 **좌 6칸**뿐이다. 74 가 낸 «9» 는 칸 수가 아니라 **200발 중 빗나간 발 수**다
(74 의 이 대상은 `#sideL .ibtn[data-pop="quest"]` **한 칸** × 200발).
가려지는 칸도 실측하면 **정확히 하나**다 — 아래 §2.

재현자 `tools/probe959.js` 는 **수리 전 사본(`f8e073a`)과 현재 트리를 둘 다 띄워 나란히 잰다**
(probe360·probe654 와 같은 처방 · 756 사다리로 얕은 클론에서도 판다). **PROBE959 15/15 PASS.**

## 1. 뿌리 — «남는 자리를 가운데로» 가 만든 전폭 상자

699(주인 지시 2026-09-02 «보스 도전할 때 설정 가능하고 설정 팝업에도 있게 해줘»)가 28 보스전 HUD 에
공용 부품 `.sm-sk` 를 얹으면서 **정렬 손잡이**로 이것을 골랐다:

```css
#bossSkip{position:absolute;left:0;right:0;top:396px;justify-content:center;z-index:5;display:none}
```

`left:0;right:0` 은 **상자를 프레임 전폭으로 늘리고**, `justify-content:center` 는 그 안에서
**내용만** 가운데로 민다. 즉 보이는 것과 상자가 다르다:

| | x | 폭 |
|---|---|---|
| 상자 | 0.0 .. 1080.0 | **1080.0** |
| 잉크(라벨 + 노브) | 421.3 .. 660.8 | 239.4 |
| **투명 여백** | 좌 **421.3** · 우 **419.3** | 상자의 **77.8%** |

그런데 이 토글은 **표시 전용 HUD(`#stinfo`·`#bossHp`)와 달리 «누르는 것»** 이라
`pointer-events` 가 살아 있다(699 가 주석에 명시). ⇒ **여백 77.8% 가 그 y 띠의 탭을 통째로 먹는다.**

`z-index:5` 이고 `#sideL` 은 `z-index:3` 이라 같은 쌓임 맥락에서 토글이 위다.

## 2. 어디가 몇 칸 — 실측

프레임 1080×2280 · 사이드 6행은 `top 72 · pitch 134 · 셀 114`(360 이 세운 등간격),
`#bossSkip` 잉크 띠는 **y 500..556**.

| 칸 | 중심 y | 최상위(수리 전) | 판정 |
|---|---|---|---|
| attend | 233 | `span.si` | 맨 위 |
| roul | 367 | `span.si` | 맨 위 |
| **quest** | **501** | **`div#bossSkip.sm-sk.sh`** | **✗ 가려짐** |
| promo | 635 | `span.si` | 맨 위 |
| coll | 769 | `span.si` | 맨 위 |
| bless | 903 | `span.si` | 맨 위 |

띠 전폭 스윕(`button, [onclick], [data-pop], .ibtn, .tab, .bbtn, #menub, .clk`)에서도
먹힌 것은 **그 한 칸뿐**이다 — 「퀘스트」 칸 중심 y 501 이 띠 500..556 의 **정확히 안쪽**이다.

### 피해는 «안 열린다» 가 아니라 «다른 것이 켜진다»

`probe959` [4] 가 실제 탭(CDP 터치 90ms · 74 와 같은 발)으로 찍은 것:

```
[4] quest 탭 → 그 칸에 도달 ✗ · 착지=div#bossSkip.sm-sk.sh.jz-up · fxSkip **뒤집힘**
```

퀘스트 팝업이 안 열리는 데서 끝나지 않는다 — 그 탭이 `fxSkipToggle()` 로 흘러
**«연출 스킵» 설정이 뒤집히고 `save()` 까지 된다.** 사용자가 지시한 적 없는 설정 변경이다.

## 3. 처방 — 상자를 잉크만큼

등재문의 후보 셋 중 **ⓐ(히트 영역을 잉크로)** 를 골랐고, 그 구현을 **정렬 손잡이 교체 한 줄**로 했다:

```css
#bossSkip{position:absolute;left:0;right:0;top:396px;width:fit-content;margin:0 auto;z-index:5;display:none}
```

`left:0;right:0` + `width:fit-content` + auto 마진 = 절대배치의 표준 중앙정렬이고,
상자가 **잉크만큼**이라 여백이 애초에 안 생긴다. **선례가 저장소 안에 있다** — 같은 부품의 다른 호스트
`#rouSkip{width:fit-content;margin:14px 0 0 auto}` 가 이미 그 꼴이다(514 «부품 한 벌 · 호스트 여럿»).

⚠ 마진은 `margin-inline:auto` 가 아니라 **`margin:0 auto`** 로 적었다 — 두 값은 이 자리에서
산술적으로 같지만(위·아래 마진은 이미 0이다), `margin-inline` 은 이 저장소에 **한 번도 안 쓰인 속성**이라
쓰면 이 한 줄만 다른 어휘가 된다. 바로 위 선례(`#rouSkip`)가 쓰는 말을 그대로 쓴다.

### 기각한 길 셋 (되돌리려는 다음 세션이 읽을 것)

| 후보 | 왜 아닌가 |
|---|---|
| ⓑ z 층을 갈라 사이드를 위로 | 가려짐이 «어느 칸이 위냐» 로 바뀔 뿐 **투명 여백은 그대로 남는다** — 그 띠의 **빈 곳** 탭이 계속 설정을 뒤집는다. 결함의 절반만 고친다. |
| ⓒ 보스 연출 중에만 히트 | 결함이 나는 상태가 바로 그 «연출 중»(`bf`)과 «재도전 대기»(`fm`)다. 조건을 좁혀도 그 두 상태에서 그대로 난다. |
| `left:50%` + `translateX(-50%)` | **오답이다** — `.sm-sk:active{transform:scale(.96)}` 가 **같은 속성**을 덮어써 누르는 순간 토글이 오른쪽으로 튄다(손잡이 둘이 한 속성을 다툰다). |

### 자리는 한 칸도 안 움직였다

`probe959` 판정 마지막 줄 — **잉크 중심 Δ0.00px · 상변 Δ0.00px · 폭 Δ0.00px.**
699 가 세운 «버튼 두 개의 중심 x540 축 · 396..452 띠» 는 그대로다. 보이는 그림은 **한 픽셀도** 안 바뀐다.

⚑ 그래서 이 결함이 오래 남았다 — `verify959` [E] 되돌림 시험이 그것을 못박는다:
옛 손잡이를 도로 먹여도 **잉크 중심은 같은 값**이고, 달라지는 것은 오직 «탭이 어디에 떨어지는가» 다.

## 4. 게이트

`tools/verify959.js` **29/29 신설**. 여섯 절:

| 절 | 무엇 |
|---|---|
| [A] 기하 | 상자 ≈ 잉크(여백 ≤ 3px) · 잉크 중심 541.05 · 폭 239.4 · 띠 500..556 (699 값 Δ0) |
| [B] 규약 | 자리 규칙 **한 벌** · `.sh` 는 `display` 만 만진다(⇒ 상자가 `bf`/`fm` 두 상태와 무관하다) · `justify-content:center` 로 되돌리지 않았다 · `left:50%`/`translateX` 금지 |
| [C] 덮임 0 | 사이드 6칸 전수 + 그 y 띠 전폭의 «누르는 것» 0건 |
| [D] 기능 | 잉크 탭 → `fxSkip` 뒤집힘 · `fxSyncSkip` 전 호스트 동기 · 노브 라벨 일치 · **그리고 「퀘스트」 칸 탭이 그 칸 핸들러에 닿고 스킵을 안 건드린다** |
| [E] **되돌림 시험** | 런타임에 옛 손잡이를 도로 먹이면 여백 421.3/419.3 과 가려짐이 **되살아난다** = 이 자가 정말 그 축을 잰다(334 규약 — 없으면 «무르게 푼 수리» 와 구분이 안 된다) |
| [F] 형제 | `.sm-sk` 호스트 전수(`#bossSkip`·`#sumSkip`·`#cfSkip` 소스 + `#rouSkip` 포함 런타임 3곳) — 같은 함정 0건 |

⚑ **[F] 가 이번 작업의 «이관» 몫이다.** 이 결함은 부품(`.sm-sk`)이 아니라 **호스트의 자리 규칙**에서
났으므로, 호스트가 늘 때마다 같은 함정이 다시 열린다. 소스 스윕 항이 그 자리를 지킨다
(«`left:0;right:0` 로 상자를 늘려 놓고 `fit-content` 가 없는 자리» 를 묻는다).

### 회귀

| 자 | 결과 |
|---|---|
| `tools/verify959.js` | **29/29 PASS**(신설) |
| `tools/probe959.js` | **15/15 PASS**(신설 · 수리 전/후 대조) |
| `tools/verify74.js` | 「사이드 아이콘」 **191/200 → 200/200** · 전 대상 200/200 **PASS** |
| `tools/verify699.js` | **79/79 PASS**(연출 스킵 토글 본체 — 무수정) |
| `tools/verify514.js` | **64/64 PASS**(공용 부품 승격 규약 — 무수정) |
| `tools/verify363.js` | **82/82 PASS**(토글 원형 — 무수정) |
| `tools/verify621.js` · `tools/verify625.js` | **42/42** · **24/24 PASS**(눌림 계열 — 무수정) |
| `tools/verify284.js` | **70/70 PASS**(보스 HUD 모드 판정 — 무수정) |
| `node tools/smoke.js` | **SMOKE PASS**(화면비 5종) |

**이관 0종.** 이 결함은 어느 자도 «반대» 를 단언하고 있지 않았다 — `verify699` 는 «토글이 뜨는가·먹히는가»
를 묻고 `verify514` 는 «부품이 한 벌인가» 를 물었을 뿐, **«상자가 잉크보다 큰가»** 를 묻는 항이
한 자에도 없었다. 그 자리를 `verify959` [A]·[C]·[F] 가 처음 채운다.

## 5. 실행 기록 — ⚠ 회귀는 **한 줄로** 세워라

1회차에 회귀 다섯을 **병렬**로 띄웠더니 셋이 빨개졌다:

| 자 | 병렬(크로미움 5개 동시) | 단독 |
|---|---|---|
| `verify621` | 34/42 → 재실행 **28/42**(같은 트리에서 점수가 흔들린다) | **42/42 PASS** |
| `verify514` | 63/64 ([2-3] «OFF 는 «결과 없는» 프레임이 많다(≥60)») | **64/64 PASS** |
| `verify363` | 80/82 | **82/82 PASS** |

셋 다 **프레임 수를 세는 항**이고, 같은 러너에서 크로미움 다섯이 소프트웨어 래스터로 돌면 그 수가 준다.
`#bossSkip` 과는 아무 접점이 없다(눌림 듀티 · 룰렛 회전 프레임 · 토글 원형).
338·344 의 «수리 전 트리와 대조» 규칙은 **그 대조 실행도 같은 부하 조건**이어야 뜻이 있다 —
여기서는 부하를 없애는 쪽(단독 재실행)으로 갈랐다. **곁다리 등재 0건.**

`verify74` 는 200발 × 7대상이라 단독으로도 **약 22분**이 든다(이번 실행 21:06 → 21:28 UTC).
회차 예산을 짤 때 이 자는 «한 번만» 돌린다고 잡아라.
