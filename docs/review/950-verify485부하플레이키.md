# 950 — `verify485` 가 부하에서 흔들린다 (자 플레이키 · 291 353 구멍)

- **세션** `sess-1858-29529` 루틴 워커 D · **1회차**
- **지시서 [3]-(가)** 기계·자 작업 — **비평가 없음**(레퍼런스 대조가 없는 자리다)
- **제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/settle291.js` 와 `tools/verify485.js` 두 파일이다.

---

## §1 재현 — 등재문이 맞았고, **한 겹 더 있었다**

등재문(931 2회차 곁다리)의 주장은 «[E5]·[E6] 이 연출 한복판에서 잰 rect 다 · 한 배율(×0.920)이 두 축에 같이 걸렸다 · 뿌리는 353 구멍» 이었다. 착수 시점에 **그대로 재현했다**(`par 7` 부하 · 14회):

| | 판정 | 값 |
|---|---|---|
| **전(수리 전)** | **5/14 빨강** | `690×267`(×0.920 · 3회) · `751×290` · `754×292` |
| **후(수리 후)** | **0/14 빨강** | 전부 `750×290` / `684×120` |

빨간 항은 언제나 **[E5]·[E6] 둘뿐**이고 나머지 25항은 흔들리지 않았다 — 등재문의 «판정 집합이 전후 같다» 와 일치한다.

**⚑ 그런데 등재문이 적어 준 처방(«`settle291()` 한 줄»)은 이 자리를 못 닫는다.** `probe950` [2] 가 그것을 직접 찍었다 — 그 자리에서 `settle291()` 을 불러도 **여전히 `690×267`** 이다.

> **291 의 필터는 `/^jz(Pg|Sheet)/` 인데, 가운데 다이얼로그의 등장 연출은 `jzBoxIn` 이다.**
> (`index.html` ~16321 `.jz-o.jz-dlg>*{animation:jzBoxIn .22s linear both}`)
> 곧 353 구멍(«대기를 페이지 안에서 해서 훅을 안 지난다»)과 **이름 구멍**(«지나가도 그 애니를 안 본다»)이 **겹친 자리**다. 등재문은 앞의 하나만 보고 있었다.

### 위상 스윕 (`probe950` [1] · **부하 없이 결정적**)

| t | `.sk-db` | 문단 | 살아 있는 애니 |
|---|---|---|---|
| 0 · 40 · 80ms | **690×267** | 629×110 | `jzBoxIn:running`(0% `scale:.92`) |
| 120ms | 737×285 | 672×118 | `jzBoxIn:running` |
| 160ms | 750×290 | 684×120 | `jzBoxIn:running` (스쳐 지나간다) |
| 200 · 220ms | **764×295** | 697×122 | `jzBoxIn:running`(62% `scale:1.02`) |
| 260ms~ | 750×290 | 684×120 | (없음) |

**곡선이 단조가 아니다** — 0.92 로 시작해 1.02 로 넘겼다가 1 로 돌아온다. 그래서 «몇 ms 기다린다» 는 어떤 값을 골라도 **양쪽 어느 한쪽을 집는다**. 등재문이 관측한 세 값(690 / 751 / 754)이 정확히 이 곡선 위의 세 점이다.

---

## §2 처방 — `settle291.js` **§box** 신설, 훅은 한 글자도 안 건드렸다

```
const QUIET_SRC = `(pat, cap) => { … re = new RegExp(pat || '^jzBox') …
  「두 프레임 연속으로 돌 것이 없을 때만 끝낸다」 · 상한 1500ms · window.__settleBoxOff 로 끈다 }`
```

`addInitScript` 가 `window.settleAnim291(패턴, 상한)` 과 그 별칭 `window.settleBox(상한)` 을 심는다. 부르지 않으면 **정의만 올라가고 아무 일도 안 한다**(§in-page 와 같은 규약).

**⚠ 훅(`/^jz(Pg|Sheet)/`)을 넓혀서 풀지 않았다.** 764 가 `verify429` 에서 이미 그 길을 재고 **안 갔다**:

- ⓐ 그 필터는 `page.waitForTimeout` 훅으로 **게이트 44개를 전부** 지나간다. `64`·`262`·`107` 처럼 **시간 자체를 재는** 자는 rAF 두 프레임이 얹히면 문턱을 넘는다(그 파일 `PENDING_SRC` 주석이 «300ms 홀드가 350ms 문턱을 넘어 Δ2 가 됐다» 를 실측으로 적어 뒀다).
- ⓑ §in-page 사다리는 «부를 때 pending 이 0 이면 곧바로 끝낸다» 라서, 연출이 **다음 프레임에 붙는** 창을 못 닫는다.

ⓑ 는 이번에 **실물로 찍혔다** — `verify950` **R1b**: `showItem()` 과 **같은 태스크**에서 재면 연출이 아직 안 붙어 `750×290`(정답값!)이 나오고, 한 태스크 뒤에 재면 `690×267` 이 된다. «pending 0 이면 통과» 규칙은 이 자리에서 **정답을 우연히 맞히거나 0% 프레임을 집거나** 둘 중 하나다. ⇒ §box 는 **두 프레임 연속 조용**을 요구한다(764 의 규칙 그대로).

### 되돌림 스위치를 291 과 **가른** 이유

`PW_SETTLEBOX=0`(→ `window.__settleBoxOff`)이 §box 의 스위치다. 291 의 `PW_SETTLE` 에 묶지 않은 이유가 있다 — **931·946 의 전후 대조가 `PW_SETTLE=0` 으로 «장치 없는 세상» 을 만든다.** 거기에 §box 까지 묶이면 그 대조가 이 부품의 회귀까지 같이 뒤집어 «사슬이 깼다» 로 읽힌다(925-① 이 경고한 그 모양이다).

실측으로 확인했다 — `PW_SETTLE=0 node tools/verify485.js` → **PASS 27/27**(§box 는 안 꺼진다) · `PW_SETTLEBOX=0` 로 par 7 부하 7회 → **4/7 빨강**(= 수리 전 그대로).

---

## §3 자리 — `verify485` [E] 한 줄

```js
-  await new Promise(r => setTimeout(r, 400));
+  if (window.settleBox && !window.__settleBoxOff) await window.settleBox();
+  else await new Promise(r => setTimeout(r, 400));
```

폴백(고정 400ms)을 **지우지 않았다** — 사슬 밖에서 돌거나 되돌림 스위치가 켜지면 종전 동작이 그대로 산다. §box 가 실제로 기다린 시간은 **372~448ms**(`verify950` 5d)로 상한 1500ms 에 한 번도 안 걸렸다.

---

## §4 인구조사 — **353 의 표가 낡았다**

353 은 «이 모양(한 evaluate 안에서 열고 → 기다리고 → 잰다)을 쓰는 게이트는 **5개**(96·22·46·125·299)» 라고 적어 뒀다. 등재문이 «`verify485` 가 여섯째» 라고 했지만 **전수 재조사 결과 그 표는 두 배 이상 낡았다**:

> **14자 / 15곳** — `verify77`·`verify93`·`verify96`(2곳)·`verify120`·`verify186`·`verify252`·`verify283`·`verify472`·**`verify485`**·`verify543`·`verify656`·`verify814`·`verify860`·`verify916`
> 그중 **정착을 부르는 자는 둘**(`verify96` [6] · `verify485`) — 나머지 **13곳은 정착 0회**다.

(옛 표의 22·46·125·299 는 «같은 블록에서 화면을 열지 않아» 이 지문에 안 걸린다. 353 이 «그중 입장 연출을 같은 블록에서 여는 것은 96 과 77 뿐» 이라고 갈라 둔 그 축을 조사자에 심었다.)

이 수는 `verify950` [7] 이 **래칫**으로 굳혔다(15곳 이하 · 정착 0회 13곳 이하). 새 게이트가 같은 모양을 더 만들면 그 자리에서 빨개진다. **표는 한 벌뿐이다** — `probe950.census()` 를 `verify950` 이 그대로 부른다(402 «사본을 지운다»).

### 곁다리 관측 — 상자 정착을 **손으로 적은** 자가 다섯 (`probe950` [5])

`verify46`(244) · `verify429`(764) · `probe764` · `verify268` · `smoke.js`. 전부 «`jzBox` 가 멎기를 기다린다» 는 같은 일을 각자 적고 있다. **이번에 접지 않았다** — 셋 다 지금 초록이고(회귀 확인: `verify46` 70/70 · `verify429` 53/53), 각자의 되돌림 팔이 자기 사본에 매여 있어 공용으로 갈아 끼우면 **그 자들의 되돌림 시험이 이 부품의 스위치에 끌려간다**. 접는 것은 별건이다(**957** 로 등재).

---

## §5 게이트·회귀

| 자 | 결과 |
|---|---|
| `tools/verify950.js` | **27/27 신설** (부품 · 훅 불변 · 규칙 · 자리 · 실동작 12회 · 되돌림 R1·R1b·R2 · 스위치 가름 · 래칫) |
| `tools/probe950.js` | **15/15 신설** (위상 스윕 · 필터 구멍 · 처방 · 인구조사 · 사본 조사) |
| `tools/verify485.js` | **27/27** · par 7 부하 14회 **전부 초록**(수리 전 5/14 빨강) |
| `verify96` | 101/101 무수정 |
| `verify429` | 53/53 무수정 |
| `verify46` | 70/70 무수정 |
| `verify925` | 34/34 무수정 |
| `verify931` | 26/26 무수정 |
| `tools/smoke.js` | **SMOKE PASS** |
| `tools/verifyProgress.js` | 종료 코드 0 |

**되돌림 팔**(`PW_SETTLEBOX=0` · par 7 · 7회): **4/7 빨강** = 수리 전 분포. 무르게 푼 수리가 아니다.

---

## §6 다음 세션이 볼 것

1. **«고정 대기 뒤에 rect 를 잰다» 는 그 자체가 지문이다.** 값이 한 배율로 가로·세로에 **같이** 걸려 있으면 «자리가 틀렸다» 가 아니라 «한 프레임을 읽었다» 다 — 허용 오차를 넓히지 마라(그러면 진짜 회귀가 안 잡힌다).
2. **정착 부품은 이름으로 갈라져 있다.** 전체화면 페이지·바닥 시트는 `/^jz(Pg|Sheet)/`(291 훅), 가운데 다이얼로그는 `^jzBox`(§box). **한 자에 둘 다 필요한 자리가 있을 수 있다** — 그때는 `window.settleAnim291('^jz(Pg|Sheet|Box)')` 한 번이면 된다(일반형을 그래서 같이 심었다).
3. §4 의 «정착 0회 13곳» 은 **잠복 목록**이다. 그중 어떤 자가 부하에서 흔들리면 뿌리는 십중팔구 여기다 — `probe950` 을 먼저 돌려 위상 곡선부터 뜨고 시작하라.
