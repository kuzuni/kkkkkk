# 작업 81 — 10 상점 페이지 상단 41 재화 바 (2026-08-26, sess-1705-5760)

**T2 기능 TODO(저장소 주인 지시 2026-08-26).** 검증 유형 **(가) 기계적/기능 작업** — 비평가 루프 없음
(ROUTINE [3]-(가) + 작업 비고의 검증 지시: «캡처에서 03 과 바 위치·크기 Δ0, 10연 소환 후 다이아 즉시 감소, smoke PASS»).

## 바꾼 것 (1회차 = 완료)

1. **마크업**: `#shopw` 첫 자식으로 41 `.pcb` 블록(골드·다이아 2알약, `data-cur`) 복제 — 03 `#dunw` 와 동일.
   `data-cur` 전역 위임(33)으로 재화 정보 팝업 진입도 그대로 된다.
2. **CSS**: `#dunw .pcb-p>i,#relw .pcb-p>i{display:flex}` 에 `#shopw .pcb-p>i` 추가
   (`#shopw i{display:inline-block}` 의 id 특이성 되돌림 — 03·89 와 같은 처방).
3. **갱신 경로**: 추가 코드 0 — `renderPcb()` 가 `document.querySelectorAll('.pcb-g>b')` 전역 갱신이고
   `drawHud()` 가 매 프레임 부르므로 마크업만으로 편입된다(41 비고의 기대 그대로).
4. **재화 비행 도착지(41 규칙)**: `fxPill(C)` 신설 — `.pcb` 가 보이는(오버레이 `.on`) 페이지가 있으면
   비행 도착지·`+n` 앵커·도착 펀치를 HUD 알약(`.cGold/.cDia`) 대신 **그 바의 알약**으로.
   `fxFly()`·`fxPlus()` 의 `document.querySelector(C.pill)` 2곳 교체. 03 던전·89 유물도 같이 이 규칙을 탄다
   (지금까지는 바가 있어도 HUD 알약 좌표로 날았다 — 골드는 ~78px 어긋난 지점).
5. **`#shopList` top 은 유지** — 바는 page-local `top:-104` 로 페이지 «위»(HUD 밴드 0~108)를 덮고
   페이지 안쪽으로는 4px(+그림자 4px)만 들어오므로 첫 카드(y163)는 가려지지 않는다.
   작업 비고의 «03 과 같은 top 값» 은 이 구조에선 불필요(03 도 리스트 top 은 바와 무관한 45).

## 기능 체크 표 (`tools/verify81.js` — 헤드리스 11/11 PASS)

| 확인 | 방법 | 결과 |
|---|---|---|
| 바 위치·크기 = 03 과 Δ0 | `#shopw .pcb` vs `#dunw .pcb` rect 비교(바·골드·다이아 알약 3종) | ✅ Δ0 |
| 캡처 픽셀 Δ0 | 두 페이지 캡처(1080×2280)의 y0~108 밴드 diff | ✅ 0px (81-shop.png · 81-dun.png) |
| 첫 카드 안 가림 | 첫 카드 y163 ≥ 바 하단 108 | ✅ |
| 값 표시 | 주입 다이아 5000 → 바 텍스트 = `fmt(S.dia)` (롤링 수렴 후) | ✅ 5.00K |
| 유료 10연 소환 → 즉시 감소 | `.cbtn.b2` 실클릭(가이드 미션 차단 배너 회피 — `gmBan()` 배너 선택) → `S.dia` 5000→4000, 바 텍스트 4.00K | ✅ |
| 비행 도착지 | 상점 열림 → `fxPill(FXCUR.dia)` 가 `#shopw` 안 · 던전 열림 → `#dunw` 안 | ✅ |
| 콘솔 에러 | 페이지 로드~소환까지 0건 | ✅ |
| 스모크 | `node tools/smoke.js` | ✅ SMOKE PASS |

## 다음 세션 참고

- 코인 탭(13)의 «HUD 알약 재스타일» 규칙(`#app:has(#shopw.on .shp-list.coin) .curs …`, index.html ~2501)은
  바(전폭 불투명, z28>z6)에 가려져 **죽은 규칙**이 됐다. 13 을 다시 잡는 세션이 정리해도 된다(동작 무해).
- 캡처 하네스 교훈 2개(둘 다 «가짜 diff» ~13,000px 를 만들었다): ① 골드는 표시 경계에서 먼 큰 고정값(5.00B)으로 —
  자동 전투 킬 수입이 캡처 사이에 표시 자릿수를 바꾼다(999999999 는 1.00B 경계 바로 밑이라 실패). ② 킬 골드 «도착
  펀치»(`fx-punch` 420ms 펄스)가 바의 골드 알약에 계속 걸리므로, 밴드 diff 캡처에서는 하네스가 `addStyleTag` 로
  펄스만 끈다(제품 동작 불변). 반영 후 4연속 11/11 PASS.
