# 607 — `verify125` A1·A3 4건 실패 (게이트 부패, 세 번째)

- 세션: `sess-0935-9065` (루틴 워커 G) · 2026-08-31 · **1회차 · 완료**
- 티어: **T1 «버그(게이트 부패 재발)»** · 지시서 [3]-**(가)** 기계+자 — 비평가 없음(레퍼런스 대조가 없는 작업)
- 제품 `index.html` **0줄** · 레이아웃 **Δ0px** · 판정·밸런스 **0줄**. 고친 파일은 `tools/verify125.js` 하나 + 신설 `tools/probe607.js`.
- 결과: `verify125` **34/38 → 43/43 PASS** · `probe607` **7/7 신설** · **SMOKE PASS**

---

## 1. 등재문이 준 것과, 재현이 더한 것

등재문의 진단(뿌리 둘)은 **둘 다 맞았다**.

| | 자리 | 실측(수리 전) |
|---|---|---|
| ① A1 | `index.html` 34154 `sendMail({ t:'🎫 프리미엄 패스 — ' + …, ic:'🎫', … })` (589 결제 목업) | «소스에 남은 화폐 이모지» 1건 |
| ② A3 | `ALLOW` 의 `"' 이용권 — '"` | 죽은 항목 1건 — 588·589 가 그 토스트를 없앴다 |
| | §R4·§R5 | ②에 딸린 되돌림·음성항이라 ②를 고치면 같이 풀린다 |

`e8efd91`(596 수리 전) 트리와 현재 트리 둘 다 **34/38** — 596 워커가 이미 대조로 못박아 둔 그대로다.

**재현이 더한 것은 «그래서 갈래 ⓑ 를 골라도 되는가» 다.** 289·211·370 이 «🎫 는 화폐가 아니다» 를
세 번 판정했지만 그 셋은 전부 **소스 한 줄** 판정이었다. 허용 목록에 한 줄을 더하는 순간 그 줄은
**A1 의 눈 밖**으로 나가므로, 그 자리가 **화면에 무엇을 흘리는지**를 먼저 재 두지 않으면
«게이트만 초록» 인 수리가 된다(334 교훈 · 373 전례).

## 2. `tools/probe607.js` — 찍힌 글자 (7/7)

프리미엄 패스를 **실제로 사서**(`buyPassPrem('stage')`) 우편함을 열고 잰 값이다.

| 항 | 값 |
|---|---|
| [1] 사기 **전** 우편함 PURE 글리프 | **0건** ← 지금 스윕이 보는 그림 |
| [2] 구매 | 성공 · 통 2개(`📅 월별 다이아` · `🎫 프리미엄 패스 — 스테이지`) |
| [2] 산 **뒤** 우편함 PURE 글리프 | **1건** — `span.ml-i.ifr` **108×108** «🎫» |
| [3] 제목 자리 `.ml-t` | «프리미엄 패스 — 스테이지» — **🎫 없음** |
| [4] 썸네일 자리 `.ml-i` | «🎫» **108×108** — 보이는 채로 있다 |
| [5] 수령 토스트 | «우편 확인 — 프리미엄 패스 — 스테이지» — **🎫 없음** |
| [6] 같은 글리프를 **문장 가운데**로 옮기면 | 제목 «프리미엄 🎫 패스 — 대조» — **샌다**(1건) |
| [7] 산 세이브에서 F2 로 새는 자리 | **썸네일 하나뿐**(제목은 안 샌다) |

⇒ **제목 머리글자는 화면에 한 번도 안 온다.** 우편 제목을 그리는 두 자리가 모두
`m.t.replace(/^[^가-힣\w]+/, '')` 로 머리 기호를 뗀다(우편 행 `.ml-t` 28231 · 수령 토스트 25810).
그것이 **우연이 아님을 [6] 음성항이 못박는다** — 머리가 아니면 그대로 샌다.

## 3. 판정

**갈래 ⓑ**(허용 목록을 옮기고 죽은 항목을 정리)를 골랐다. 근거 셋:

- ⓐ **집 관행이 「우편 제목 머리에 글리프 한 자」다** — 같은 파일의 우편 여섯 통이 전부 그렇다
  (`📅 월별 다이아` · `🛒 …` ×3 · `📦 …` ×2). 589 는 관행을 그대로 따랐고, **하필 고른 글리프 하나가
  `CUR_EMOJI` 안에 있었을 뿐**이다. 지울 대상이 아니다.
- ⓑ 등재문 갈래 ⓐ(«`curIc()`/`CUR_ICON` 로 옮긴다»)는 **오표기**다 — `cur-ticket-*.svg` 는 전부
  **던전 입장권**이고(H1~H3 이 던전 수와의 일치로 못 박는다), 패스 구매 우편에 붙이면
  289(이용권)·211(쿠폰)·370(패스 토스트)이 **이미 세 번 기각한** 그 오표기가 된다.
- ⓒ 제목 글리프는 **화면에 안 온다**(§2 [3][5][6]).

### 3-1. 죽은 항목은 «지운» 것이 아니라 «옮긴» 것이다 (333 처방)

`"' 이용권 — '"` 을 그냥 지웠으면 289 가 세운 판정(«🎫/이용권 표기는 화폐가 아니다»)을 지키는 항이
**한 줄도 안 남는다.** 방향을 안 바꾸고 **같은 판정의 살아 있는 자리**로 갈아 끼웠다 —
그 자리가 곧 A1 이 새로 빨개진 줄이다.

    "' 이용권 — '"            →   "'🎫 프리미엄 패스 — '"

**목록 길이는 10 그대로**(A2 상한 불변) · 항목에 **글리프까지 적었다**(370 규약).

### 3-2. §R 짝도 같이 옮겼다 — 안 그러면 목록 절반이 짝 없이 남는다

370 이 세운 규약이 «허용 목록에 한 줄을 더할 때마다 그 항목이 무엇을 **여전히** 잡는가를 같이
못박는다» 이므로, 항목만 갈아 끼우고 시험을 안 옮기면 **그 자리가 곧 A3 의 다음 부패**다.

- **§R2b 신설** — 새 항목이 «프리미엄 패스» 라는 **말**이 아니라 **🎫 라는 글리프**에 걸려 있다
  (머리글자를 🪙 로 바꾸면 빨강).
- **§R4b 신설** — 제품에서 그 줄이 사라지면 **A3 이 먼저** 잡는다(방금 고친 부패가 그대로 재발하지 않는다).
- **§R 의 그물(`got` 정규식)도 같이 늙고 있었다** — «이용권» 은 588·589 이후 아무 줄도 안 잡는다.
  살아 있는 표본(«프리미엄 패스»)으로 갈았다. 그물이 죽으면 §R1~§R3 이 «0건인데 초록» 이 아니라
  **엉뚱한 줄을 안 세고 조용히 통과**한다.

## 4. ⚑ 이 작업의 본체 — 남은 절반은 런타임에 있었다 (373 과 **같은 병**)

`probe607` [4] 가 찍은 대로 **썸네일 `ic:'🎫'` 은 화면에 실제로 그려진다**(108×108).
A1 은 이 자리를 `ART_SLOT`(`ic:'…'` 한 글리프) 규칙으로 **이미 구조로 면제**하는데 **F2 는 몰랐다** —
그리고 **F2 가 초록이던 이유는 판정이 일치해서가 아니라 스윕이 그 그림을 한 번도 안 봐서**다
(기본 세이브엔 그 통이 없고, 스윕에서 아무도 **사지 않는다** · `probe607` [1] 0건).

373 이 ▦ 메뉴에서 잡은 문장 그대로다. 그래서 373 과 **같은 처방**을 썼다:

1. **스윕에 «우편-결제확인» 단계 신설** — 실제로 사서 **보게** 한다(21 → **22화면**).
   ⚠ **맨 끝에 둔다** — `S.pass.prem`·`S.mailx` 를 바꾸므로 앞 화면들이 그 상태를 얹은 채 찍히면 안 된다.
2. **`ART_NODE` 두 번째 항목 신설** — `.ml-r .ml-i` + 🎫 + «노드 텍스트가 그 글리프 하나뿐».
   면제는 셋이 **모두** 맞을 때만 듣는다(373 판정식 그대로 · 자기 사본 안 만들었다).
3. **F2b 가 그 항목을 지킨다** — 적중 0 이면 «스윕이 그 화면을 다시 안 본다» 는 뜻으로 먼저 빨개진다.
   실측 `#mnw …i.mn-i ×1 · .ml-r .ml-i ×1`.

### 4-1. 무르게 풀지 않았음을 §R9~§R10 이 못박는다

| 시험 | 변조 | 결과 |
|---|---|---|
| **§R9a** | 썸네일 자리 글리프만 🪙 로 | **빨강** `🪙@span.ml-i.ifr[108×108]` |
| **§R9b** | 글리프는 🎫 그대로, 자리만 **제목 칸**으로 | **빨강** `🎫@div.ml-t[383×38]` |
| **§R10** | 원복 | 유출 0 · 면제 1 (시험이 «항상 빨강» 이 아니다) |

**§R9b 가 이 작업 판정의 본체다** — 면제한 것은 **썸네일**이지 **제목**이 아니다.
제목 머리글자는 애초에 화면에 안 오고, 만약 오게 되면 `.ml-t` 는 면제 목록에 없으므로 F2 가 그대로 잡는다.
`probe607` [6] 이 그 자리가 **제품에서 실제로 샌다**는 것을 미리 확인해 두었다(시험이 허공을 치지 않는다).

## 5. 게이트

- `verify125` **34/38 → 43/43 PASS**
  (A1 · A3 · §R4 · §R5 복구 + **§R2b·§R4b·§R9a·§R9b·§R10 신설** · F2b 항목 2개 · 스윕 21 → 22화면)
- `probe607` **7/7 신설**
- 회귀 **무수정**: `probe370` 7/7 · `verify402` 39/39 · `verify588` 29/29 · `verify589` 53/53 · **SMOKE PASS** · `verifyProgress` 종료 0
- ⚠ `probe373` 은 **12/13** 인데 **수리 전 트리에서도 같은 12/13**(같은 [1] 항)임을 `git stash` 대조로 확인했다.
  373 자신의 수리가 스윕에 ▦ 메뉴를 넣은 순간 그 재현항(«현행 스윕은 ▦ 메뉴를 한 번도 열지 않는다»)은
  **구조적으로 빨개진다** — 566/573 이 잡은 «수리 전 세계에 굳은 재현기» 와 같은 병이라 **608 로 등재**했다.
  (내 변경으로 «21화면 중 0» → «22화면 중 0» 으로 숫자만 바뀌었고 판정은 전후 동일.)

## 6. 다음 세션이 볼 것

- **`ALLOW` 에 한 줄을 더할 때는 §R 짝 둘(글리프 고정 · 줄이 사라지면 A3)을 같이 단다.**
  이 항목은 이제 넷째 자리(289 → 211 → 370 → 607)이고, **네 번 다 «새 토스트/우편이 생겼는데
  목록이 안 따라왔다»** 였다. 게이트가 제 일을 한 것이지 부패가 아니다 — **판정만 하면 된다.**
- **우편 제목에 글리프를 넣을 거면 반드시 «머리»에 넣어라.** 문장 가운데는 화면에 그대로 샌다
  (`probe607` [6] · §R9b). 집 관행(📅🛒📦)도 전부 머리다.
- **A1(소스)과 F2(런타임)는 같은 글리프를 두 층에서 본다.** 소스에서 한 자리를 면제했으면
  **그 자리가 화면에 그려지는지**를 반드시 같이 물어라 — 373 과 607 이 같은 이유로 두 번 열렸다.
