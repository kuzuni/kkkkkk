# 713 — 소환 배수 토글의 «자리» 정정 (주인 정정 2026-09-02 02:50)

> 주인 원문: «그 소환결과쪽에 x1 x10 x100 이런거 놔달라니까 소환팝업에 놧네»

**세션** sess-1628-10305 워커 B · 1회차 · 2026-09-01
**제품** `index.html` 12자리 · **패널 1080 · 버튼·닫기·리본 앵커 Δ0px** · 밸런스 0줄 · 세이브 이관 없음
**자** `tools/probe713.js` 5/5 신설 · `tools/verify713.js` 45/45 신설
**이관** `verify668` 62/62 · `verify327` 85/85 · `verify363` 80/80 · `verify514` 64/64 · `verify187` 63/63 · `verify12` 15/15

---

## 1. 재현 — 등재문 그대로였고, 거기에 **한 줄이 더 있었다**

`probe713` 을 먼저 돌렸다(338 규칙). 등재문의 «자리를 잘못 잡았다» 는 그대로 확인됐고,
그 자가 **처방을 바꾼 두 번째 값**을 같이 찍었다:

| 항 | 수리 전 | 수리 후 |
|---|---|---|
| [1] 토글의 호스트 | **`#shopw`**(10 상점 소환 탭) | `#sumw` · 4칸 |
| [2] 결과 팝업 하단 자유 띠 | **83px** (셸 98 에 **−15px**) | 98px (Δ0) |
| [3] 상점 리스트 하변 | **266**(668 이 154 에서 내렸다) | **154**(원복) |
| [4] 상점 카드 버튼이 배수를 타는가 | **«10,000회 소환»** | «10회 소환» |

⚑ **[2] 가 이 작업을 «옮기기» 에서 «자리를 만들기» 로 바꿨다.** 공용 셸 `.stabs` 는 96·437 규약으로
**98px 고정**인데 결과 팝업에는 그만한 빈 띠가 **한 곳도 없었다**. 셸을 줄이는 길은 막혀 있다 —
`.stab.on` 알약의 정지점(0..7 / 7..70 / 70..77 / 77..84)은 352 4·6회차가 ref 커버리지 적분으로
확정한 표고, 높이를 줄이면 그 표를 다시 적어야 해서 그 순간 **«표 두 벌»(402)** 이 된다.

⚑ **[4] 도 처방을 바꿨다.** 668 은 배수를 상점 카드 버튼에도 걸었다. 토글만 옮기고 그 축을 두면
«화면에 없는 ×1000 이 카드 가격에 걸린» 상태가 된다 ⇒ 상점은 668 **이전**으로 원복하고,
배수는 **토글이 보이는 두 버튼**(`#sumB10`·`#sumB30`)에만 산다.

---

## 2. 자리 — 15px 을 어디서 가져왔는가

패널은 `padding:106 0 98` 이고 하단 크롬(`.sm-panel::after`)이 15px 이라 쓸 수 있는 띠가
**[15, 98] = 83px**. 셸 98 을 받으려면 아래 여백이 **113**(= 15 + 98) 이어야 하고, 그 15px 을
어디서 빼는가가 실제 결정이었다:

| 후보 | 대가 |
|---|---|
| ⓐ 패널을 키운다 | 327 주인 지시 «세로 2배»(패널 1080 = ref 539 × 2.00)가 깨진다 |
| ⓑ 위 여백 106 에서 뺀다 | 106(크롬 14 + 본문위 92)은 **레퍼런스 실측값**이다 |
| **ⓒ 그리드에서 뺀다(주 채택)** | 876 은 ref 값이 아니라 327 이 «최악 판을 담는다» 는 목적으로 고른 수다 |

**결론은 ⓒ + ⓑ 를 8:7 로 나눠 낸 것**이고, 그 비율은 취향이 아니라 산수가 정했다.

### ⚑ 327 의 산수 하나가 여기서 정정됐다

327 은 «최악 판 = 5행 846»(170×5 − 4) 이라고 적었다. **아니다.**
개수 배지(`.ifq`)는 절대배치라 **스크롤 넘침에 그대로 들고** 카드 바닥 아래로 **18.69px** 나오며,
줄들은 `align-content:safe center` 라 그리드를 줄여도 **절반만 따라 올라온다**:

```
배지 하단 = (H − 846)/2 + 838 + 18.69 ≤ H   ⇔   H ≥ 867.4   ⇒ 하한 868
```

876 은 그것을 담고 있었고 **846·862·865·866 은 못 담는다** — `verify327` [E5] 가 그 넷에서
연달아 빨개져 이 식을 드러냈다(1회차에 세 번 되잡았다). 그래서 그리드는 **−8**(876 → 868)만
가져가고 나머지 **7px 은 위 여백**(106 → 99)에서 냈다.

⚑ **[E5] 를 «참/거짓» 에서 «수치» 로 바꾼 것이 그 세 번을 가능하게 했다** — `got=false` 만
보였을 때는 얼마나 모자란지 알 수 없었다. 지금은 `got=867 want=866` 처럼 찍힌다.

### 최종 값 (전부 `#sumw`·`.sm-panel` 두 줄에 있다)

```
--sm-gh   876 → 868     (−8)
padding   106 0 98 → 99 0 113
--sm-ph   204 + gh → 212 + gh      ⇒ 패널 1080 **불변**
#sumMulBar  left 36 · width 724 · bottom 15   (띠 [15,113] 을 꽉 채운다)
#sumSkip    bottom 28 → 36         (363 규칙 «띠 한가운데» 그대로 · 15 + (98−56)/2)
```

두 컨트롤의 **세로 중심이 64 로 같다**(`verify713` [B1]). 폭 724 = 4 × 181 이고 가장 긴
«×1,000» 잉크 139.93 이 칸 181 안에서 좌우 19px 씩 남긴다([C3] 38.36px).

---

## 3. ⚑ 카드가 11px 위로 간다 — 피할 수 없다(의도적 이탈)

`verify12`(레퍼런스 대조 게이트)가 그것을 잡았다: 배지·카드 행 y 가 전부 **−11**.
이것은 조절 실수가 아니라 **구조**다. 이 패널은 하변이 84 버튼 줄에 묶여 있어
(패널 하변 = 버튼 상변 − 20) 아래에 자리를 만들면 내용이 그만큼 위로 밀린다:

```
카드 y = 패널상변 + 위여백 + (그리드 − 846)/2 ,  위여백 + 그리드 = 1080 − 113 = 967
      ⇒ 이동 = 위여백/2 − 60.5 ,  위여백 ≤ 99(그리드 하한 868)  ⇒ **최소 −11**
```

패널을 키워도(ph + X) 상변이 같이 내려가 상쇄돼 **−11 이 하한**이다.
⇒ `verify12` 에 **`DY = −11` 한 상수**를 명시해 얹었다. 무르게 푼 것이 아니라 한 값으로 적어
**그 이상의 표류는 그대로 빨개지게** 뒀다(측정표 12 §정오표에 같은 값).

---

## 4. 668 의 «자릿수 좁힘» 계단도 같이 옮겼다

668 은 배수를 켜면 길어지는 라벨·가격을 `sumFitCls` 3단 계단으로 좁혔다 — 그 계단이
**상점 카드**에 있었다. 배수가 팝업으로 온 지금 그 계단은 상점에서 **죽은 분기**이고
(라벨이 «10회 소환» 을 못 넘는다), 정작 필요한 곳은 팝업 버튼이다:

- `.shp-card .cbtn>.lab/.cost.fit1~3` **6줄 삭제**(333 — 죽은 분기와 그 자리를 같이)
- `.sm-b>.lab/.cost.fit1~3` **신설**(35 → 33/30/28 · 33 → 31/29/27)
- 임계는 «실제로 넘치기 시작하는 글자 수»: 라벨 **9자** · 가격 **8자**

측정으로 잡았다 — `.sm-b` 는 검정 6 + 안쪽 림 6 이라 쓸 수 있는 안쪽 폭이 **223** 인데
×1000 라벨 «30,000회 소환» 잉크가 **224.4** 로 림을 0.7px 밟았다(`verify668` [F×1000] 이
1회차에 그 값으로 빨갰다). «3,000회 소환» 201.8 은 통과라 **×1·×10·×100 은 클래스가 아예 안 붙는다**
= 레퍼런스 상태 잉크 Δ0.

---

## 5. 위임 규약 채택 1건 — 팝업을 닫으면 배수가 ×1 로 돌아간다

| 선택지 | 대가 |
|---|---|
| 세션 내 유지(668 그대로) | 다음에 팝업이 열릴 때 ×1000 이 **먼저 켜져 있다** — 오타 한 번이 💎3,000,000 |
| **닫으면 ×1(채택)** | 연속 재소환 중에는 팝업이 안 닫히므로 선택은 그대로 산다 |

668 ⓑ 가 «어제 켜 둔 ×1000 이 오늘 첫 탭에 살아 있으면 그것이 곧 사고» 라며 세이브를 거부한 것과
**같은 이유**다. 토글이 이 팝업 안에만 있으므로 «닫았다» = «그 소환 흐름을 떠났다» 이다.
되돌리려면 `closeSummonResult()` 의 `sumMul = 1` **한 줄**만 지우면 된다.

---

## 6. 기능 체크 표 (T1/T2 «실제로 동작해야 완료» 규칙)

| 누른 것 | 무엇이 바뀌는가 | 확인 |
|---|---|---|
| 결과 팝업의 ×1000 칸 | 알약이 그 칸으로 · [10회]·[30회] 라벨·가격이 그 배수로 | `verify713` [D2][D3][D4] |
| [10회 소환](×1000) | **10,000회 뽑히고 💎1,000,000 차감** | [D5][D6] |
| 배수 바를 눌렀을 때 | 팝업이 **안 닫힌다**(84 «떨어져 나간 노드» 규약) | [G1] |
| 팝업 닫기 | 배수가 ×1 로 복귀 · 알약도 첫 칸 | [H1] |
| 재화 부족 상태로 클릭 | `.lack` 회색 + **0회 뽑힘 · 0 차감** | `verify668` [G1][G2] |
| [무료 10회](배수 ×1000) | 라벨·실제 횟수 **10 고정** | `verify668` [D1][D2][D3] |
| 상점 소환 탭 | 토글 **0건** · 라벨 «10회 소환» · 리스트 하변 154 | `verify713` [E1]~[E6] |

---

## 7. 이관·회귀

| 자 | 결과 | 비고 |
|---|---|---|
| `verify713` | **45/45 신설** | 자리·짝·라벨·동작·상점 0건·회귀·되돌림 |
| `probe713` | **5/5 신설** | 재현(수리 전 1/5) |
| `verify668` | **62/62 이관** | [A5][A7][A8]·[C]·[D]·[F]·[R3] 을 새 자리의 같은 물음으로 |
| `verify327` | **85/85 이관** | GH 876→868 · PAD 204→212 · [E5] 를 수치 항으로 · [E6] 하한 868 |
| `verify187` | **63/63 이관** | 같은 `--sm-gh` 상수 3자리 |
| `verify363` | **80/80 이관** | SK_BOT 28→36 · 리터럴 14 를 띠 산수로 되돌림 |
| `verify514` | **64/64 이관** | 1-13 같은 값 |
| `verify12` | **15/15 이관** | `DY = −11` 한 상수(§3) |
| `probe668` | 12/12 **무수정** | ⚑ 등가성 코어를 한 줄도 안 건드렸다는 가장 짧은 증거 |
| `verify45`·`verify195`·`verify102`·`verify84` | 39/39 · 36/36 · 92/92 · 31/31 무수정 | 상점 원복이 레퍼런스 상태로 돌아왔다는 뜻 |
| `verify96`·`verify379`·`verify437`·`verify352`·`verify378` | 101/101 · 101/101 · 52/52 · 68/68 · 48/48 무수정 | **공용 셸을 한 줄도 안 고쳤다** |
| `verify478`·`verify124`·`verify166`·`verify299`·`verify328` | 113/113 · 77/77 · 73/73 · 27/27 · 19/19 무수정 | |
| **SMOKE** | **PASS** | |

⚠ `verify12`·`verify84` 는 이 컨테이너에 `PIL` 이 없어 처음에 즉사했다 — `pip install pillow` 로
해결되는 **환경 문제**이고 제품과 무관하다. 다음 세션도 같은 걸 만나면 그렇게 하면 된다.

---

## 8. 곁다리 관측 (등재만 · 내 작업 단위 아님)

**714 등재 — 668 의 ×배수는 «고유 종 ≤ 30» 이라는 327 의 전제를 넘어선다.**
327·713 의 «가려짐 0» 은 «버튼이 10·10·30 뿐이라 결과는 최대 30칸» 위에 서 있다.
그런데 668 의 ×1000 은 30,000장을 뽑아 **배너 종수(36)까지** 고유 칸이 나올 수 있고,
36칸은 6행 = 1016px 이라 그리드(868)를 넘어 **스크롤이 생긴다**. 지금도 그렇고(668 이후 내내)
713 이 만든 상태가 아니다 — `verify668` [H2] 가 «15칸 / 배너 종수 36» 으로 그 여지를 이미 찍어 두었다.
스크롤이 나는 것 자체는 187 의 원래 설계라 **버그는 아니고**, 327 이 약속한 «스크롤조차 안 쓴다» 가
배수 상황에서는 참이 아니라는 **문서·게이트의 어긋남**이다. 상세는 PROGRESS 714 행.

---

## 9. 다음 세션이 볼 것 (700·701 에게)

⚑ **위치 규약은 이제 하나다 — 배수 토글은 «그 실행 버튼이 있는 화면» 에 둔다.**
713 이 그것을 못박았으니 700(유물 소환)·701(단련·룬)은 각자의 **결과/실행 화면** 쪽에 붙여라.
상점·상위 탭에 두면 668 이 받은 정정을 그대로 다시 받는다.

⚑ **그리고 «배수는 토글이 보이는 곳에만 건다» 도 한 벌이다** — 700·701 도 토글이 안 보이는
다른 화면의 같은 버튼에 배수를 걸지 마라(713 [4]).

⚑ **띠를 찾을 때는 «셸 98» 부터 재라.** 공용 `.stabs` 는 98 고정이고 줄이면 352 의 알약 표를
다시 적어야 한다. 팝업에 98 이 안 들어가면 **셸이 아니라 띠를 넓히는 쪽**이 정답이다 —
그리고 그 팝업이 하변 앵커면 §3 의 산수(내용이 위로 밀린다)를 먼저 풀어 보고 시작해라.
