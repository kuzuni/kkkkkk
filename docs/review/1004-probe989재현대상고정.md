# 1004 — `probe989` 의 «수리 전» 이 «지금 트리» 였다 (T1 · 게이트 부패 · 자)

**완료(2026-09-06, sess-1743-28985 루틴 워커 C · 1회차)** · 제품 `index.html` **0줄** ·
`probe989` **4/7 → 8/8**([8] 신설) · 회귀 5종 무수정 · **SMOKE PASS**

---

## 1. 재현 (등재문 그대로)

`node tools/probe989.js` → **4/7 FAIL**. 빨간 셋은 [2]«자의 상자 R60 이 자른다» · [4]«같은 눈금인데
상자만 키우면 판정이 뒤집힌다» · [5]«제 손 부품을 눈금에 넣으면 밴드 밖이 생긴다» 다.

이 자는 `index.html` **하나만** 잰다(`require` 는 `path`·`pwlaunch` 둘뿐). 989 리뷰의
«`probe989` 7/7 신설» 은 **수리 전 트리에서 재현하던 값**이고, 989 가 그 결함을 고친 뒤의 트리에서는
저 셋이 **구조적으로 설 수 없다** — `verify989` [A4] 가 «R60 이 한 종도 안 자른다» 를 초록으로
단언하는 것과 **같은 사실**이다. ⇒ 573(«`probe566` 의 «수리 전» 이 움직이는 ref 였다»)과 같은 병이다.

## 2. ⚑ 뿌리가 **둘**이었다 — 처방 ⓐ 만 따랐으면 [4] 는 여전히 빨갛다

등재문의 처방 후보는 ⓐ «재현 대상을 고정 SHA 로 박는다» · ⓑ «두 판을 한 실행에서 재서 뒤집힘을 묻는다»
였다. **ⓐ 를 먼저 그대로 해 봤고, 그 자리에서 [4] 만 빨간 채 남았다(7/8)** — 이 회차의 본체가 그것이다.

| 무엇을 재나 | 989 직전 트리 R60 | 989 직전 트리 R480 |
|---|---|---|
| `meteor` 본체 눈금 · **고친 바탕**(ⓒ `mf:0` 판) | 157 | **157** (안 움직인다) |
| `meteor` 본체 눈금 · **옛 바탕**(«발이 없는 장면») | 157 | **687.7** |

**이 자에는 989 의 수리가 이미 한 축 들어가 있었다.** 989 는 세 축을 고쳤는데(ⓐ 눈금 · ⓑ 상자 · ⓒ 바탕),
`probe989` 는 등재 시점에 **ⓒ 를 자기 안에 넣은 채**(발을 놓되 그림만 끈 판) 옛 판정([4] = 본체 눈금이
상자를 따라 뒤집힌다)을 물었다. 그런데 [4] 를 뒤집던 것은 **운석의 낙하 예고 링**이고, ⓒ 는 바로 그 링을
바탕으로 흡수하는 수리다 ⇒ 트리만 옛것으로 돌려놔도 **상자를 키워도 값이 안 움직인다.**
⇒ **재현은 «옛 트리» 가 아니라 «옛 자 한 벌»(옛 바탕 + 본체 눈금 + 작은 상자)로 해야 선다.**

## 3. 처방 (자 1벌 · 제품 0줄)

| 축 | 종전 | 지금 |
|---|---|---|
| 재는 트리 | `index.html`(= 움직이는 ref) | **`PRE_SHA` 고정 SHA**(`534fce3` = `claim(989)` 의 부모)에서 **굽지 않고** 통째로 꺼낸다 — `gitrev756` 사다리(얕은 클론이면 판다 · 못 파면 **환경**이라 ⏸ 보류로 **안 센다**) |
| [2]·[4] 의 바탕 | 고친 바탕(ⓒ) 하나 | **바탕 둘을 같이 잰다** — `bodyRing`·`edgeRing`(옛 바탕) 신설. [4] 는 옛 자 한 벌로 묻고, 나머지 축은 고친 바탕 그대로 |
| [5] | «새 눈금 밖 > 0» 한 쪽만 | **같은 트리·같은 상자에서 «옛 눈금 밖 0종(초록) ↔ 새 눈금 밖 3종(빨강)»** 짝으로(=`verify989` [R2] 가 세운 그 대조) |
| 특이성 | 없음 | **[8] 신설** — 같은 사다리를 **지금 트리**에도 돌려 «그 세 조건이 여기서는 하나도 안 선다» 를 묻는다 |

- **문턱·조건은 한 글자도 안 풀었다.** 이 자가 들고 있는 것은 «989 가 무엇을 고쳤나» 의 재현 근거라,
  무르게 풀면 근거가 통째로 사라진다(979-② · 등재문의 ⚠ 그대로).
- `PRE_SHA`·`PRE_SINCE` 는 `verify989` 와 **같은 값**이다(자마다 따로 적으면 그것이 곧 사본 — 402).
- 옛 바탕 값은 **본체 눈금과 테두리만** 잰다 — 거리변환(`dtm`)·제손 눈금은 [4] 가 안 묻는다.

**결과 — 989 리뷰 §1 의 표가 칸까지 그대로 되살아났다**(그 표가 이 자의 등재값이다):

| 상자 R | 닿는 종(옛바탕) | 옛 자 본체 눈금 밖 | 제손 눈금 밖 |
|---|---|---|---|
| 60 (자의 상수) | meteor **30화소** | **0종** | boom · meteor · flask |
| 120 | meteor **107화소** | meteor | boom · meteor · flask |
| 240 | 0 | meteor | boom · meteor · flask |
| 480 | 0 | meteor(**687.7**) | boom · meteor · flask |

`meteor:687.7` 은 `verify989` [R5] 가 적어 둔 값과 **글자까지 같다** — 두 자가 서로 다른 길로 같은 수를
찍었으므로 재현이 진짜다.

## 4. 되돌림 시험 ([8] · `--now` 신설)

[8] 이 «옛 트리에서만 초록인 자» 를 막는 항이다. 스스로도 되돌려 봤다 —
`git show 534fce3:index.html > .p989-rev.html && node tools/probe989.js --now .p989-rev.html`
(⚠ 사본은 **저장소 뿌리**에 둔다 — `/tmp` 에 두면 상대 경로 자산이 안 붙어 콘솔 오류 87건으로 [7] 이 빨개진다):

```
  ok   [1]~[7]  (재현 축은 그대로 초록)
  FAIL [8] 되돌림/특이성 — R60 이 자르는 종 1종(meteor) · 본체 눈금 밴드 밖이 R60 0종 ↔ R480 0종 ·
           제손 눈금 밴드 밖 3종(boom,meteor,flask)
```
⇒ 989 의 수리가 되돌아가면 **[8] 하나가 정확히 빨개진다**(7/8). 이 항이 없었으면 이 자는 989 가
통째로 되돌아가도 8/8 로 조용했을 것이다(573-② «빨간 항보다 그 옆의 초록이 위험하다»).

## 5. 회귀 (전부 무수정)

`verify989` **11/11** · `probe792r12` **5/5** · `verify981` **26/26** · `verify982` **14/14** ·
`node tools/smoke.js` **SMOKE PASS** · `node tools/verifyProgress.js` 코드 0.

⚠ **`verify792` 는 31/32([E1] 밖 5종 — shuri·stone·arrow·meteor·lance)** 인데 **내 것이 아니다** —
`git stash` 대조로 손 안 댄 트리에서도 **같은 31/32 · 같은 다섯 종**임을 확인했고(338·344),
그 빨강은 **995 행이 «이 행의 결과물» 로 적어 둔 것**이다(981 5회차 §⑧ 도 같은 자리를 같은 결론으로
못박아 두었다). 곁다리 등재 **0건**.

## 6. 값싼 확인 한 줄

`node tools/probe989.js` — 얕은 클론이면 첫 실행에 `git fetch --deepen=160`(실측 5.3s)이 한 번 붙고,
그 뒤로는 두 트리 × 상자 4칸을 약 1분에 다 잰다.
