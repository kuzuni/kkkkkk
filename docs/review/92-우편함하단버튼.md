# 92 — 69 우편함 하단 버튼 재구성 (좌 [읽음 전체 삭제] / 우 [일괄 읽기&수령])

> 저장소 주인 지시(2026-08-26). T2 «기능 TODO» — 완료 조건은 «만들어 놓음» 이 아니라
> **«실제 게임 데이터로 동작하고 결과가 세이브·화면에 반영됨»** 이다(ROUTINE.md «기능 완성 규칙»).
> 검증 [3]-(가) 기계적 + [3]-(다) 연출.

담당: sess-1840-805 (워커 C) · 2026-08-25

---

## 1. 무엇을 바꿨나

| 자리 | 이전 (작업 69) | **이후 (작업 92)** |
|---|---|---|
| 좌 버튼 | `#mailClose` «닫기» · 갈색 | **`#mailDel` 「읽음 전체 삭제」 · 빨강** |
| 우 버튼 | `#mailBtn` «전체 수령» ↔ «수령 완료» (라벨 가변) | **`#mailBtn` 「일괄 읽기&수령」 · 초록 · 라벨 고정** |
| 닫기 경로 | 좌 버튼 + 바닥 ✕ (2가지) | **바닥 ✕ 하나뿐** |
| 목록 | 수령한 우편이 «완료» 로 계속 남음 | **[읽음 전체 삭제] 로 치울 수 있음** |
| 빈 목록 | (있을 수 없었다) | **`.ml-empty` 「우편이 없습니다」** |

**크기·좌표·간격은 측정표 §7·§10 그대로**다(305×117 · gap 27 · span 637 · 좌우 여백 112).
바뀐 것은 색·라벨·동작뿐 — `verify69` 의 기하 게이트가 그대로 통과하는 것으로 확인했다.
측정표 정오표는 `docs/measure/69-우편함팝업.md` **§12**.

### 세이브 — `S.mail` 이 2상태 → **3상태**

| 값 | 뜻 | 목록 | `mailLeft()` |
|---|---|---|---|
| 없음/0 | 미수령 | 보임(받기 활성) | 센다 |
| 1 | 수령 완료 | 보임(완료·`.done`) | 안 셈 |
| **2** | **삭제됨** | **안 보임** | 안 셈 |

`MAILS` 원본은 불변이고 «삭제» 는 렌더 필터(`mailList()`)일 뿐이다.
**미수령(0)은 절대 지워지지 않는다** — `delReadMail()` 이 `S.mail[id] === 1` 인 것만 2 로 넘긴다.
구 세이브에는 0/1 만 있으므로 **마이그레이션 불필요**(KEY 버전 그대로).

---

## 2. 기능 체크 표 (ROUTINE.md «기능 완성 규칙» — 헤드리스 실클릭)

하네스 `tools/fnchk92.js` — 버튼을 실제로 눌러 «무엇이 바뀌는지» 를 재고 표로 찍는다.
아래는 `node tools/fnchk92.js` 출력 그대로다.

| 버튼 | 사전 상태 | 동작 | 기대 | 실제 | |
|---|---|---|---|---|---|
| (첫 진입) | 미수령 5통 | — | [읽음 전체 삭제] 비활성 · [일괄 읽기&수령] 활성 · 5행 | 삭제 비활성 · 수령 활성 · 5행 | ✅ |
| [읽음 전체 삭제] | 읽음 0통(비활성) | 클릭 | 아무 변화 없음(5행 유지 · 재화 불변) | 5행 · 다이아 Δ0 | ✅ |
| 행 [받기] | m1 미수령 | 클릭 | S.mail[m1]=1 · 다이아 +1500 · 유물석 +300 · 행 «완료» · 삭제 버튼 활성 | S.mail=1 · 다이아 Δ1500 · 유물석 Δ300 · 삭제 활성 | ✅ |
| 행 [받기] | (같은 클릭) | 세이브 확인 | localStorage[KEY].mail[m1] = 1 | = 1 | ✅ |
| [읽음 전체 삭제] | 읽음 1통 · 미수령 4통 | 클릭 | 읽음 1통만 목록에서 제거(S.mail=2) · 미수령 4통 유지 · 재화 불변 · 팝업 유지 | 남은 4행(m2,m3,m4,m5) · 삭제분 S.mail=2 · 다이아 Δ0 · 팝업 열림 | ✅ |
| [읽음 전체 삭제] | (같은 클릭) | 세이브 확인 | localStorage[KEY].mail 에 2 반영 | = 2 | ✅ |
| [읽음 전체 삭제] | (같은 클릭) | MAILS 원본 | 불변 5통 | 5통 | ✅ |
| [일괄 읽기&수령] | 미수령 4통 | 클릭 | 다이아 +3500 · 유물석 +700 · 전 행 «완료» · 버튼 비활성 · 라벨 고정 | 다이아 Δ3500 · 유물석 Δ700 · 버튼 비활성 · 라벨 «일괄 읽기&수령» | ✅ |
| 행 [받기] 연타 3회 | m1 미수령 | 3연타 | 다이아 +1500 (1회분만) | 다이아 Δ1500 | ✅ |
| [읽음 전체 삭제] | 전부 읽음 | 클릭 | 행 0 · «우편이 없습니다» · 두 버튼 비활성 | 0행 · 빈문구 있음 · 삭제 비활성 · 수령 비활성 | ✅ |
| (새로고침) | 전부 삭제된 세이브 | F5 → 우편함 | 행 0 · 빈 상태 유지 | 0행 · 빈문구 있음 | ✅ |
| 바닥 ✕ | 우편함 열림 | 클릭 | 닫힘 · 버튼 «닫기» 는 존재하지 않음 | 닫힘 · #mailClose 없음 | ✅ |
| (전 과정) | — | 콘솔 | 에러 0건 · 화면에 NaN/undefined 0건 | 에러 0건 · NaN 0건 | ✅ |

**FNCHK92 PASS 13/13**

---

## 3. 게이트

| 게이트 | 결과 |
|---|---|
| `node tools/verify69.js` | **VERIFY69 PASS 96/96** (69 시절 77 → 92 에서 19 항목 추가) |
| `node tools/fnchk92.js` | **FNCHK92 PASS 13/13** |
| `node tools/smoke.js` | **SMOKE PASS** |
| 문법 | 인라인 스크립트 파싱 OK · 콘솔 에러 0 |

### verify69 에 추가한 게이트 (19항)

- 좌/우 버튼 **라벨 고정** 문구, `#mailClose` 부재
- 좌 = 빨강 · 우 = 초록 **면 색 판정**(그라디언트 스톱 중 채도 최대값으로. `:disabled` 는 둘 다
  같은 회색이라 **활성 상태로 되돌려 놓고** 잰다 — 첫 진입에서 삭제 버튼은 비활성이다)
- 라벨 잉크가 버튼 면(305 − 테두리 7×2 = **291**) 안 — 실측 좌 166 · 우 178
- 삭제: 읽음만 제거 · 미수령 유지 · `S.mail=2` · 세이브 반영 · 재화 불변 · 팝업 유지 · `MAILS` 불변
- 빈 상태 문구 · 두 버튼 비활성 · 우 버튼 라벨 고정
- 닫기 경로 = ✕ 하나

### verify69 에서 **고친** 옛 게이트 (main 에 이미 있던 실패 5건)

작업 92 를 잡았을 때 `main` 의 `verify69` 는 이미 **72/77 로 FAIL** 이었다(내 변경 전 `git stash` 로 확인).
원인은 앱 버그가 아니라 **테스트의 낡은 타이밍 가정**이다:

> 행 [받기] 의 재렌더는 58 연출 계약상 `fxHoldList()` 의 **FXHOLD(620ms)** 가 풀린 뒤에 온다
> (`fxRenderLater`). 그런데 테스트는 **500ms** 에 재고 «수령했는데 버튼이 아직 활성 / .done 없음 /
> 라벨이 «받기» / 연타분 0» 이라고 오판했다. → **900ms** 로 고쳤다.

그리고 `post2.btn === '수령 완료'` 게이트는 92 에서 라벨이 고정되었으므로 «비활성 + 라벨 고정» 으로 바꿨다.

---

## 4. 연출 — [3]-(다) 비평 루프

### 4-1. 캡처 하네스 (1회차에서 얻은 교훈 — **다른 화면에도 그대로 적용된다**)

1회차 캡처는 지시서 [3]-(다) 문구대로 «80~100ms 간격 연속 프레임» 을 `waitForTimeout` + `screenshot`
으로 찍었다. **그렇게 하면 짧은 연출은 절대 못 찍는다** — `page.screenshot()` 한 장이 100~200ms 걸려서
«+35ms 프레임» 이 실제로는 +300ms 에 찍힌다. 결과는 아래와 같았다:

- 비평가 A: «260ms 짜리인데 관측 지속시간 ≤70ms — 73% 짧게 끝난다», «m5 가 한 프레임에 331px 순간이동»
- 비평가 B: «260ms 예산 중 210ms(81%)가 아무 변화도 렌더하지 않는다»

**둘 다 캡처 아티팩트다.** 실제 타임라인을 RAF 프로브로 재 보니 애니메이션은 정확히 260ms 동안
147 → 136 → 70 → 9 → 0 으로 접히고 있었다. 비평가 2명이 **독립적으로 같은 오독**을 했다는 것이
이 함정의 위험도를 말해 준다.

→ 하네스를 **Web Animations API 정지·탐색** 방식으로 바꿨다(`tools/cap92fx.js`):
1. 클릭 → **레이아웃을 강제로 플러시한 뒤**(`void document.body.offsetHeight` — 이게 없으면
   `getAnimations()` 가 새 애니메이션을 아직 몰라서 **0개**가 나온다) 해당 애니메이션을 골라 `pause()`.
   상자(`mlOut`)와 내용(`mlOutIn`)을 **같이** 잡아야 알파가 프레임과 어긋나지 않는다
2. `currentTime` 을 0·75·150·…·540ms 로 **정확히** 옮겨 가며 1장씩 찍는다
3. 연출 뒤 재렌더 `setTimeout` 은 가로채 붙잡아 뒀다가 마지막에 손으로 실행(착지 프레임)

이제 파일 이름의 ms 가 실제 진행 시각과 일치하고, 프레임마다 `h`(높이)·`x`(translateX)·`a`(opacity)
실측값을 같이 찍어 비평가에게 **근거 수치**로 준다.

### 4-2. 1회차 (A 3 / B 3) — 캡처 오독을 걷어내고 남은 진짜 지적

| # | 지적 | 판정 |
|---|---|---|
| A① B① | «260ms 중 81% 가 빈 구간» | ❌ 캡처 아티팩트 |
| A③ | «남는 카드 m5 가 한 프레임에 331px 순간이동» | ❌ 캡처 아티팩트(수축에 따라 연속 이동) |
| **B①** | 이징 `cubic-bezier(.4,0,.7,1)` 은 ease-in — t=35ms 에 진행률 **3.7%** | ✅ **커브의 실제 성질** |
| **B②** | `translateX 40px` 은 카드 폭 784 의 **5.1%** — «나간다» 로 안 읽힌다 | ✅ |
| **B①** | 높이 수축이 전 구간에 퍼져 표본이 없다 | ✅ |
| **B④** | `fxCheck` 는 `.6s` 라 260ms 카드 연출보다 **340ms 더** 살아 버튼 라벨을 덮는다 (92px 디스크 = 버튼 높이 117 의 **79%**) | ✅ |
| **A④** | 파티클이 버튼에만 8개 — **사라지는 대상 행에는 신호가 0개** | ✅ |
| A③ | 이탈 방향(오른쪽)이 트리거 버튼(좌하단)과 무연고 | ⚠ 채택 안 함 — «스와이프 삭제» 는 목록 삭제의 표준 어휘다 |

### 4-3. 회차별 점수와 «무엇이 진짜 지적이었나»

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치 | ④가독성 | **최저** |
|---|---|---|---|---|---|---|
| 1 | A / B | 3 / 3 | 5 / 5 | 3 / 4 | 5 / 6 | **3** |
| 2 | C / D | 6 / 6 | 5 / 5 | 7 / 5 | 5 / 5 | **5** |
| 3 | E / F | 6 / 6 | 5 / 5 | 6 / 6 | 6 / 5 | **5** |
| 4 | G / H | 7 / 7 | 7 / 7 | 8 / 7 | 6 / 6 | **6** |
| 5 | I / J | 7 / 7 | 7 / 7 | 7 / 7 | 7 / 6 | **6** |

매 회차 **서로 모르는 비평가 2명이 독립적으로 같은 곳을 짚었다** — 그게 진짜 지적을 가려내는 필터였다.
1회차만은 둘이 같은 **캡처 아티팩트**에 속았다(§4-1).

### 4-4. 2회차 — 고친 것 (1회차 A 3 / B 3 → 5)

1. **길이 .26s → .42s**(58 «0.3~0.8초» 규칙 안), **구간별 이징**(초동/등속/수축).
2. **이탈 40px → 168px** — 40px 은 카드 폭 784 의 5.1% 라 «나간다» 가 아니라 «쪼그라든다» 로 읽힌다.
3. **높이를 46% 까지 붙잡고** 남은 54% 에 걸쳐 접는다 → 수축이 4프레임에 표본된다.
4. **행마다 90ms 스태거** · **남는 행 FLIP** · **`fxCheck` 제거**(«보상 확인» 표식이라 삭제에 의미가 안 맞고
   `.6s` 라 카드보다 340ms 더 살아 버튼 라벨을 덮었다) · **행별 미니 버스트** 추가.

### 4-5. 3·4회차 — 2회차에 «고친 것» 이 만든 새 문제들

2회차 수정 중 **셋이 역효과**였다. 비평가 C·D 가 독립적으로 잡았다:

| 2회차에 넣은 것 | 왜 역효과였나 | 4회차 처리 |
|---|---|---|
| 행별 `fxBurst` | 링 반경이 **짧은 변**에서 나온다 — 행(784×147)이면 세로 확산 127px 가 행 반높이 73.5 를 넘어 **pitch 164 의 이웃(남는) 행 위에 71px 얹힌다** | **제거.** 대신 행 자신의 붉은 기 + 내용 소거로 신호를 준다(스필 0) |
| 남는 행 FLIP | 접힘(height)이 이미 flow 수축으로 행을 올려 놓는데, FLIP 이 **클릭 시점 좌표로 되돌린 뒤 다시 올라오게** 해서 같은 이동을 두 번 시켰다 | **제거.** flow 수축만으로 착지 점프 0px(비평가 C·F 실측 확인) |
| `fxPop` (버튼) | `0%{scale(0);opacity:0}` 이라 **계속 있어야 할 버튼이 한 프레임 통째로 사라졌다가 ×1.24 로 튀어** 우측 초록 버튼과의 35px 간격을 4.5px 로 좁혔다 | **제거.** 눌림은 60 `jz-dn` 담당 |

그리고 «가만있다 훅» 은 **고치면 다른 요소로 옮겨 갔다**:

- 1회차: 사라지는 행이 «가만있다 훅» → 홀드를 46% 로
- 2회차: 이번엔 **남는 행 3장이 193ms 정지**했다가 164px 중 143px(87%)을 140ms 에 몰아 올라감(C-①)
  → 홀드를 **18%(72ms)** 로 당기고 수축을 3단으로
- 3회차: **끝에서 알파 절벽**(α .62 → .03 을 75ms 에 = 앞 300ms 평균의 5.8배)(E-① · F-①)
  → 85%·94% 키프레임을 끼우고, 꼬리는 **이징이 아니라 값 간격**으로 감속
  (ease-out 커브를 걸면 높이는 가라앉지만 **알파도 같이 앞으로 쏠려** 절벽이 되살아난다)

«내용을 상자보다 먼저 지운다» 도 한 번에 안 됐다 — 2회차에 알파만 앞당겼더니 **잘리는 대상이
글자에서 행 버튼으로 옮겨 갔을 뿐**이고(150ms 에 h107·α.69 로 «완료» 가 세로로 잘림), 반대편에는
«내용 없는 분홍 슬래브» 가 140ms 남았다. 4회차에 내용에 **자기 `translateX(150px)`** 를 줘
**잘리기 전에 옆으로 빼면서** 둘 다 없어졌다.

### 4-6. 4·5회차 — 마지막 두 결함

4회차(G 6 / H 6)에서 둘이 독립적으로 같은 두 가지를 짚었다. **둘은 서로 반대 방향의 요구**였다:

| 지적 | 요구 | 충돌 |
|---|---|---|
| «내용 없는 분홍판이 180ms(행 수명의 45%)» | 내용을 **늦게** 지워라 | ↕ |
| «+75ms 에 행 버튼 「완료」가 α .81 인 채 34% 잘린다» | 내용을 **일찍** 지워라 | ↕ |

하나의 곡선으로는 둘을 동시에 만족할 수 없다 — **좌우를 갈랐다**:

- **좌측**(제목·요약·악센트)은 클립선(x953)에서 멀다 → `mlOutIn` 을 **70%(280ms)** 까지 살린다
  → 내용 0 인 판이 180ms → **120ms**
- **우측**(썸네일 `.ml-i` · 행 버튼 `.ml-b`)은 클립선에 먼저 닿는다 → `mlOutR` 로 **22%(88ms)** 에 지운다
  → +75ms 의 「완료」 절단이 **α .81 → α .03**(잘려도 안 읽힌다)

겸해서 내용 이탈을 150px → **60px**. 150px 중 **29px 은 내용 자신이 클립 쪽으로 밀어 만든 잘림**이었다
(구조적 여유 26px 을 넘는 초과분).

그리고 «유일한 악센트» 문제 — 4회차에 버튼 파티클을 뺀 뒤 임펄스는 림 플래시 하나뿐인데
inset +5px 는 카드 높이 147 의 **3.4%** 라 «악센트가 아니라 렌더 노이즈» 였다(G·H 공통)
→ **+16px(11%)**, 피크를 12% → **8%(32ms)** 로 당겨 초동 홀드를 지탱하게 했다(홀드도 48 → 32ms).

### 4-7. 좌우 분리 실측 (프로브)

`t` 별 상자 / 좌측 제목 / 우측 행버튼의 α·x, 그리고 버튼 우변이 클립을 넘는 양:

```
t(ms) | 상자 α  x  | 좌측 제목 α  x | 우측 행버튼 α  x | 클립 초과
   0  | 1.00    0 | 1.00       0 | 1.00        0 | 안 잘림
  40  | 0.99   29 | 1.00       0 | 0.59       14 | 9px
  75  | 0.95   58 | 0.80      11 | 0.03       33 | 57px   ← α .03 이라 안 읽힌다
 150  | 0.85  133 | 0.36      36 | 0.00       34 | 133px
 225  | 0.68  215 | 0.26      41 | 0.00       34 | 215px
 280  | 0.52  270 | 0.00      60 | 0.00       34 | ← 내용 완전 소거
 400  | 0.00  360 | 0.00      60 | 0.00       34 |
```

### 4-8. 최종 사양 (5회차 지적 반영 후)

```
.ml-r.out{animation:mlOut .40s both;animation-delay:var(--mld);background:#F2C7C7}
   /* 붉은 기는 .out 이 붙는 «즉시» — 스태거를 타면 2번째 대상이 평상 상태로 남아
      «대상이 2장» 임이 첫 프레임에 안 읽힌다(F-④) */
@keyframes mlOut{                                        전 구간 linear (0~8% 만 bezier)
  0%   h147 x  0  a1.00  inset  5px   cubic-bezier(.15,.8,.5,.92)  ← 끝기울기 ≠ 0
  8%   h146 x 22  a1.00  inset 16px #FFF7EC   ← 림 플래시(유일한 임펄스, 높이의 11%)
  30%  h120 x110  a .90  inset  5px           ← x 96→110 으로 속도 킹크 제거
  45%  h 96 x170  a .80
  72%  h 40 x278  a .50
  85%  h 18 x322  a .28                       ┐ 꼬리는 이징이 아니라 «값 간격» 으로 감속
  94%  h  5 x348  a .07                       │ (마지막 24ms 가 페이드의 7%)
  97%  h  2 x355  a .03                       ┘
  100% h  0 x360  a 0    margin0
}
/* 내용은 «어느 변에 먼저 닿느냐» 로 가른다 — 좌우가 아니다 */
@keyframes mlOutIn{ 0,10% α1 x0 → 55% α.12 x44 → 78%,100% α0 x60 }  /* .ml-ac · .ml-t · .ml-s */
@keyframes mlOutR { 0, 6% α1 x0 →                22%,100% α0 x34 }  /* .ml-i · .ml-b(클립선) · .ml-d(하변) */

스태거 = min(140, 360 ÷ (n−1))  →  2장 140(총 540) · 3장 140(680) · 4장 120(760) · 5장 90(760)
재렌더 = 400 + (n−1)×스태거 + 20
```

**남는 행 상승 속도**: 0.03 → 0.30 → 0.40 → 0.52 → 0.42 → 0.36 → 0.25 → 0.17 px/ms (끝에서 가라앉는다)
**x 속도**: 8~30% 1.00 → 30~45% 1.00 → 이후 0.85 → 0.72 → 0.58 → 0.42 (킹크 없음)
**알파 낙차**: 피크가 중간(85~94%), 마지막 24ms 몫 **7%**
**내용 0 인 «빈 분홍판»**: 180ms → 120ms → **88ms**(행 수명 400 의 22%)

### 4-9. 이 작업에서 **못 고치는** 것 (다른 워커 구간 / 구조)

1. **`fxBurst` 의 `dCap`** — `max(70, min(w,h)/2 + 40 − rr)` 이라 대상이 작으면 이동 거리가 `rnd(70,70)`
   으로 **분산 0** 이 되고, 링 시작 반경(버튼 45.6)이 대상 반높이(58.5)보다 작아 파티클이 **라벨 위에서
   태어난다**. 비평가 4명(C·D·E·F)이 독립적으로 지적했다. 이건 **58·60 공용 모듈**이라 여기서 고치면
   전 화면 버스트가 같이 바뀐다 → ROUTINE [4] «남의 구간 절대 금지» 에 따라 **쓰지 않는 쪽**을 택했다.
   58/60 담당 워커에게 넘길 항목이다.
2. **리스트 패널 우측 클립이 26px** — 카드 우변 927, 클립 953. 이탈을 아무리 늘려도 26px 뒤부터는
   가려진다. 360px(카드 폭 46%)까지 늘려 «퇴장» 으로 읽히게 했지만, 완전한 스와이프 아웃은
   패널 구조를 바꿔야 한다(69 측정표 §5 패널 = ref 실측치).

### 4-10. 5회차 실측 타임라인 (`node tools/cap92fx.js`)

`h` = 5행 높이 · `x` = translateX · `a` = opacity

```
+  0ms  h 147/147/147/147/147  x 0/  0/0/  0/0  a 1.00/1.00/1.00/1.00/1.00
+ 75ms  h 147/133/147/147/147  x 0/ 58/0/  0/0  a 1.00/0.95/1.00/1.00/1.00
+150ms  h 147/108/147/146/147  x 0/133/0/ 18/0  a 1.00/0.85/1.00/1.00/1.00
+225ms  h 147/ 73/147/130/147  x 0/215/0/ 67/0  a 1.00/0.68/1.00/0.94/1.00
+300ms  h 147/ 35/147/104/147  x 0/288/0/145/0  a 1.00/0.45/1.00/0.83/1.00
+375ms  h 147/  5/147/ 67/147  x 0/347/0/225/0  a 1.00/0.08/1.00/0.65/1.00
+450ms  h 147/  0/147/ 31/147  x 0/360/0/297/0  a 1.00/0.00/1.00/0.41/1.00
+540ms  h 147/  0/147/  0/147  x 0/360/0/360/0  a 1.00/0.00/1.00/0.00/1.00
end     h 147/147/147 (3행 · m1·m3·m5)
```

캡처: `docs/review/92-fx-{pre,0,75,150,225,300,375,450,540,end}.png` · 정지 1장 `92-r1.png`
하네스는 **게임 로직(`window.step`)도 멈춘다**(LESSONS 58-2) — 안 멈추면 유휴 전투 수입이
프레임마다 HUD 골드를 굴려서 삭제 연출과 무관한 변화가 캡처에 섞이고, 비평가가 그걸 채점하려 든다
(4회차 비평가 G 가 실제로 «375·450 프레임의 헤더 골드 1.9→4» 를 물었다).

### 4-11. 5회차 결과 — **①~④ 최저 6** (I 7 / J 6). 8 미달로 **⏸ 보류**

5회차 비평가 둘이 독립적으로 **같은 한 줄**을 처방했다:

> `.ml-d`(«수령 완료 / 기한 없음», 카드 로컬 y96~128)는 **접히는 하변**에 먼저 닿는다 —
> 하변이 y128 에 이르는 시각이 **93ms(23%)** 인데 좌측 곡선(70% 생존)에 묶여 있어 그때 α **.79** 였다.
> 4회차에 «클립선에 먼저 닿는» 우측을 22% 로 옮기면서, «하변에 먼저 닿는» `.ml-d` 는 그대로 뒀다.

즉 가르는 기준이 «좌우» 가 아니라 **«어느 변에 먼저 닿느냐»** 였다. 5회차 지적은 전부 반영했다:

| 지적 | 처방(I·J 공통) | 반영 |
|---|---|---|
| `.ml-d` 가 87ms 동안 α .79→.44 로 가로 절단 | 빠른 그룹(`mlOutR`, 88ms)으로 | ✅ |
| 빈 분홍판 120ms | `.ml-d` 를 빼면 `.ml-t` 를 78% 까지 살릴 수 있다 | ✅ **88ms** |
| x 속도가 120ms 이음매에서 **+46%** 만 튄다 | 30% 키프레임 x 96 → 110 | ✅ 1.00/1.00 px/ms |
| 0~8% bezier 종료 기울기가 0 → «정지-재출발» | 끝기울기를 살린 커브로 | ✅ `cubic-bezier(.15,.8,.5,.92)` |
| 상한 2 라 **읽음 4·5통이면 뒤 3장이 동시** 출발 | 스태거를 행 수로 나눠라 | ✅ 예산 360ms ÷ (n−1) |

스태거 최종: 2장 140ms(총 540) · 3장 140(680) · 4장 120(760) · 5장 90(760)
— **모든 행이 서로 다르게** 출발하면서 총 길이는 항상 **≤ 760ms**(58 «0.3~0.8초» 안).

**⏸ 보류 사유** — 지시서 [3]-(다) 의 **비평 루프 상한 5회**를 다 썼다. 위 5건을 반영한 뒤의
**재채점은 못 했다.** 점수 추이는 **3 → 5 → 5 → 6 → 6** 이고, 매 회차 비평가 2명이 독립적으로
같은 곳을 짚어 수렴은 계속되고 있었다. 남은 감점의 성격은 아래 §4-9 (구조적 · 다른 워커 구간) 쪽으로
옮겨 가는 중이다 — 특히 ② 는 **패널 클립 26px** 때문에 «이탈 360px 중 실제 퇴장은 7.2%» 라는
천장이 있어, 패널 구조를 건드리지 않는 한 CSS 로 더 밀어도 오르기 어렵다.

> **기능은 완료다.** 보류는 «삭제 연출의 레이아웃 점수» 에만 걸린다 —
> 버튼·동작·세이브·빈 상태는 FNCHK92 13/13 · VERIFY69 96/96 으로 전부 통과했다.


---

# 6회차 (2026-08-26, sess-0033-24209 워커 C) — 2차 폴리시 라운드 착수

5회차는 지적 5건을 반영한 뒤 **재채점을 못 하고** 루프 상한(5회)으로 보류됐다.
2차 폴리시 라운드에서 이어받아 **6회차 = 그 반영본의 첫 채점**으로 센다(회차는 «이어서»).

## 6-1. 채점 전에 잡힌 회귀 2건

캡처보다 먼저 기능 게이트를 돌렸더니 두 곳이 빨갛게 죽어 있었다. **둘 다 다른 작업이 지나가며 생긴 것**이다.

### ① 한 통 수령 직후 [읽음 전체 삭제] 가 **1.33초 동안 죽는다** (제품 버그)

`FNCHK92` 13항 중 3항 FAIL. 증상은 «행 [받기] → 삭제 버튼이 계속 비활성 → 눌러도 아무 일도 안 남».

- 129 가 «수령 후 그 행만 in-place 로 «완료» 로 바꾸고 **목록 통재렌더는 `fxRenderLater`(= `FXHOLD`) 뒤로 미룬다»
  로 고쳤다. 그때 고친 것은 **행**뿐이고 **바닥 두 버튼은 옛 상태 그대로** 남았다.
- 그 틈은 원래 `FXHOLD` = 620ms 라 눈에 안 띄었는데, **작업 93 이 «퍼짐 → 머묾 → 흡수» 3박자로 늘리며
  `FXHOLD` 를 620 → 1330** 으로 올렸다. 틈이 2.1배가 되면서 실사용에 드러났다.

처방 — `mailBtnsSync()` 를 만들어 `mailRowDone()` 끝에서 부른다.

```js
function mailBtnsSync(){
  const rows = [...document.querySelectorAll('#mbox .ml-r')];
  const dl = $('mailDel'), bt = $('mailBtn');
  if(dl) dl.disabled = !rows.some(r => r.classList.contains('done'));
  if(bt) bt.disabled = !rows.some(r => !r.classList.contains('done'));
}
```

**`S` 가 아니라 DOM 을 재는 이유**: 이 시점의 `S.mail` 은 아직 옛 값이다 —
행 클릭 핸들러가 `mailRowDone(row, b)` 를 **먼저** 부르고 `claimMail()` 은 `fxThen`(2중 rAF) 뒤에 돈다.
행 상태는 바로 위에서 in-place 로 고쳐 놨으므로 **DOM 이 «지금» 의 진실**이다.
→ `FNCHK92 10/13 → 13/13`.

> **교훈**: «통재렌더를 미룬다» 는 최적화는 **미룬 렌더가 갱신하던 것 전부**를 in-place 목록에 넣어야 끝난다.
> 129 는 행만 옮기고 껍데기(바닥 버튼)를 빠뜨렸고, 그 미완성은 **다른 작업(93)이 상수를 만지는 날** 버그가 된다.
> 미룬 렌더의 «갱신 대상 목록» 을 주석으로 남겨 두지 않으면 다음 사람은 알 수가 없다.

### ② `verify69` 「대표 썸네일 = 최대 보상」 이 **영구 FAIL** (게이트 노후 — 제품은 정상)

`VERIFY69 98/99`. 125(화폐 아이콘 통일) 뒤로 `rw.ic` 는 이모지 **글자**가 아니라
`<img class="cic" data-cur-ic="gold" src="assets/ui/cur-gold.svg">` **마크업 문자열**이다.
게이트는 `cell.querySelector('.ml-i').firstChild.textContent` 로 비교하는데, `firstChild` 가 이제 **요소 노드**라
그 `textContent` 는 **항상 ''** 다 → 무엇을 그리든 FAIL. 수량 5/5 는 실제로 전부 맞았다.

처방 — 텍스트가 아니라 «무엇을 그렸나»(`data-cur-ic` → `src` → `outerHTML`, 텍스트 노드면 텍스트)로 비교.
기대값 쪽도 같은 함수로 정규화한다.

**순환 논증 방지(LESSONS 109-3)** — 훼손 빌드에 돌려 실제로 잡는지 확인했다:

| 빌드 | 결과 |
|---|---|
| 정상 | `true` |
| 1행 아이콘을 `dia` 로 훼손 | `false` ✓ |
| 1행 수량을 `9Z` 로 훼손 | `false` ✓ |

→ `VERIFY69 99/99`.

### ③ 하네스 2개가 클라우드 러너에서 못 돌았다

`tools/cap92fx.js`·`tools/fnchk92.js` 가 `require('playwright')` + `chromium.launch()` 를 직접 부른다.
127 이 게이트들을 `tools/pwlaunch.js`(번들 브라우저 없으면 `/opt/pw-browsers` 폴백)로 이관할 때 **이 둘은 빠져 있었다**
— `Executable doesn't exist at …chromium_headless_shell-1234/…` 로 즉사한다. 둘 다 이관했다.

## 6-2. 6회차 캡처

`node tools/cap92fx.js docs/review/92-r6` — 콘솔 에러 0건. 5회차와 같은 상태(5통 중 2·4번째만 수령)에서
WAAPI 정지 + `currentTime` 탐색으로 9프레임(pre·0·75·150·225·300·375·450·540·end).

```
+  0ms  h 147/147/147/147/147  x 0/  0/0/  0/0  a 1.00/1.00/1.00/1.00/1.00
+ 75ms  h 147/133/147/147/147  x 0/ 65/0/  0/0  a 1.00/0.95/1.00/1.00/1.00
+150ms  h 147/108/147/146/147  x 0/140/0/ 16/0  a 1.00/0.85/1.00/1.00/1.00
+225ms  h 147/ 73/147/130/147  x 0/215/0/ 75/0  a 1.00/0.68/1.00/0.94/1.00
+300ms  h 147/ 35/147/104/147  x 0/288/0/150/0  a 1.00/0.45/1.00/0.83/1.00
+375ms  h 147/  5/147/ 67/147  x 0/347/0/225/0  a 1.00/0.08/1.00/0.65/1.00
+450ms  h 147/  0/147/ 31/147  x 0/360/0/297/0  a 1.00/0.00/1.00/0.41/1.00
+540ms  h 147/  0/147/  0/147  x 0/360/0/360/0  a 1.00/0.00/1.00/0.00/1.00
end     h 147/147/147 (m1·m3·m5)
```

5회차 표와 견주면 **x 만 다르다**(75ms 58→65 · 150ms 133→140) — 5회차의 «30% 키프레임 x 96→110»
(속도 킹크 제거)이 실제로 걸려 있다는 확인이다. h·α 는 동일.

## 6-3. 6회차 채점 결과 — **①~④ 최저 5** (K 6 / L 5)

| 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** |
|---|---|---|---|---|---|
| K | 6 | 6 | 7 | 6 | **6** |
| L | 6 | 6 | 5 | 5 | **5** |

점수 추이 **3 → 5 → 5 → 6 → 6 → 5/6**. 둘 다 «5회차 수정 5건은 전부 유지, 되살아난 것 없음» 을 독립 확인했다.

**둘이 독립적으로 같은 곳을 짚은 것 = 3건** (LESSONS 92-6 의 필터). 7회차는 이 셋만 고쳤다.

| # | K 의 말 | L 의 말 | 공통 진단 |
|---|---|---|---|
| ⓐ | 0~8% bezier 가 22px 을 4ms 에 쏟고 32ms 에 **0.12px/ms** 로 내려앉았다 재출발 — 60fps 표본 19.1/**4.2**/16.7/16.7px | (5회차 #4 는 유지되나) 이음매 문제 | **5회차에 bezier 로 «정지-재출발» 을 고쳤는데 증상이 남았다** |
| ⓑ | 이탈 360px 중 **82px(23%)이 288ms 이후** = h≤40px·α≤.50 에서 소비 → 과하다. x 를 72% 에 끝내라 | 클립선이 카드 우변 +17px 뿐이라 375ms 에도 원래 자리의 **73%** 를 덮는다 → «퇴장» 이 아니라 «표류». 140~160px 로 줄여라 | **꼬리가 낭비다. 줄이고 «접힘» 을 주역으로** (K 280 · L 140~160) |
| ⓒ | 우측이 빈 채 좌측만 남는 구간 **242ms(60%)** | `.ml-s` 86% 절단·`.ml-t` 53% 절단이 최대 **117ms** 렌더된다. 내용은 세로 이동 0 인데 상자 하변은 0.507px/ms 로 올라와 **L자로 갈라진다** | **내용이 «잘린 채» 읽힌다 — 뿌리는 축이 갈린 것** |

### 반증해 «고치지 않은» 것

**L-① «탭 지점이 540ms 동안 완전 무반응 — 버튼 채움색이 전 프레임 픽셀 동일»** — **캡처 아티팩트다.**
하네스가 `document.getElementById('mailDel').click()` 로 눌렀는데, 합성 `click()` 은 **pointer 이벤트를
하나도 만들지 않는다.** 60 쥬시니스의 눌림은 `pointerdown` 캡처 리스너에 걸려 있어 그대로는 안 걸린다.
진짜 마우스 입력으로 재 보니 멀쩡히 눌린다:

```
t(ms)   50    87   119   152   174   198   268   300   334   386
scale  1.00  1.00  .975  .943  .940  .940 1.008 1.035 1.040 1.001   (jzDn → jzUp 스프링)
실폭    305   305 297.4 287.5 286.7 286.7 307.5 315.6 317.2 305.4
```

**이것이 «캡처 아티팩트를 연출 결함으로 채점당한» 3번째 사례다**(1회차 고정간격 촬영 · 4회차 HUD 골드 롤링 · 6회차 이것).
→ 하네스를 고쳤다: 진짜 `mouse.down/up` 으로 누르고, `jzDn`·`jzUp` 도 `mlOut` 과 **같은 타임라인으로 정지·탐색**한다
(안 그러면 눌림만 실시간으로 흘러 프레임과 어긋난다). r7 재캡처 실측 버튼 빨강 폭
**291(평상) → 273(눌림) → 300~301(스프링) → 291(안정)**.

**L-④ «토스트가 눌린 버튼에서 1463px(화면의 64%) 떨어진 HUD 위에 뜬다»** — 지적 자체는 맞지만
`fxToast` 는 **58·60 공용 모듈**이라 여기서 위치를 바꾸면 전 화면 토스트가 같이 움직인다.
ROUTINE [4] «남의 구간 절대 금지» → §4-9 의 «58/60 담당 워커에게 넘길 항목» 에 함께 적어 둔다.

## 6-4. 7회차 — 공통 지적 3건 수정

| 무엇 | 전 | 후 | 근거 |
|---|---|---|---|
| 0~8% 이징 | `cubic-bezier(.15,.8,.5,.92)` | **전 구간 linear**, 앤티시페이션은 `box-shadow` 창만 | ⓐ |
| x 이탈 | 360px, 400ms 내내 이동 | **200px**(카드폭 25.5%), **216ms 에 정지** | ⓑ |
| 상자 수명 | 400ms | **300ms**(K «280» · L «300») | ⓑⓒ |
| 내용 이동 | 가로만 (`translateX`) | **가로+세로** — 수축량의 40%만큼 딸려 올라간다 | ⓒ |
| 림 플래시 | inset **16px**, 창 32ms | inset **8px**, 창 **60ms** | K «겉높이의 22%를 32ms 에 = 높이 점프» + L «1.9프레임 = 드랍프레임» → **값은 내리고 시간은 늘렸다** |
| 내용 이징 | 선언 없음(= 기본 `ease`) | `linear` 명시 | K-④ «75ms 실측 α≈0.02 vs 선언 함의 .20 — 10배» |

### 7회차 실측 (10ms 간격 47표본)

```
t    | m2  h   x    α  | m4  h   x    α  | 남는 행 상승 px/ms
  0  | 147   0 1.00 | 147   0 1.00 | 0.05
 60  | 139  60 1.00 | 147   0 1.00 | 0.20
120  | 113 120  .85 | 147   0 1.00 | 0.55
180  |  73 171  .65 | 143  40 1.00 | 1.01
240  |  31 200  .38 | 123 100  .90 | 1.30
300  |   0 200  .00 |  88 155  .73 | 1.06
360  |   0 200  .00 |  44 200  .50 | 0.78
440  |   0 200  .00 |   0 200  .00 | 0.25
```

- **x 속도**: 0~130ms **1.00 px/ms 균일**(13표본 전부) → 140ms 0.90 → 150~210ms 0.80 → **220ms 정지**.
  ⓐ 의 «한 프레임만 4.5배 짧다» 는 구조적으로 불가능해졌다 — 램프가 하나뿐이다.
- **내용이 «상자 하변 밖으로 잘린 채 α>0.2 로» 그려지는 프레임: 242ms → 0개**(47표본 전수).
- **남는 행 상승 속도**의 최대 단차 0.30 → **0.23 px/ms**(K-③ «+63% 튄다» 대응 — 수축 램프를 완만하게).
- **패널 우측 림 훼손 0px**(§6-1 ① 의 구조 수정 유지).

게이트: `VERIFY69 99/99` · `FNCHK92 13/13` · `SMOKE PASS` · 콘솔 에러 0.
캡처 `docs/review/92-r7-{pre,0,60,120,180,240,300,360,440,end}.png`.
