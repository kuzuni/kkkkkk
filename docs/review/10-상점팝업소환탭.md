# 10 상점 팝업 — 소환 탭 · 비평 기록

- 작업 세션: sess-0421-22524 (병렬 워커 B)
- 껍데기: **(다) 전체화면 페이지** — 딤·헤더·테두리 없음. 03 던전(`#dunw`)과 같은 부류의 전용 오버레이 `#shopw` 신설.
- 캡처 조건: 헤드리스 1080×1920, `상점 탭` 정식 진입점(`goTab('shop')`). 상태 주입: weapon Lv2(exp 7/9)·shield Lv2(8/9)·skill Lv3(2/13), 무료소환 2/2.
- 좌표 환산: 카드 자식은 카드 좌상단(ref 50,250) 기준 상대좌표(측정표 §2)를 그대로 CSS 로 박음.

## 진입점 결정 (다른 세션이 알아야 함)

- 레퍼런스 화면명은 «상점 팝업 — 소환 탭», 하단 카테고리 탭 `재화` 는 13번 «상점 팝업 (재화 탭)» 과 짝이다.
  둘이 한 팝업의 카테고리 탭이므로 **상점 탭(🏪, `data-t="shop"`) 진입**으로 붙였다.
- 기존 상점 탭의 다이아·아바타 상점(`renderShop`/`bShop` 패널)은 **`재화` 카테고리 → `goTab('shop', true)`** 로 연결해 **기능을 보존**했다(패널 상점 그대로). 13번 담당이 재화 탭을 정식 페이지로 만들면 이 임시 연결을 교체하면 된다.
- **보물상자 탭(🎁, `data-t="box"`)은 손대지 않았다** — 유물(relic) 소환 배너 그대로. 유물 소환 기능 회귀 없음.
- `특별`·`일일/주간` 카테고리는 «준비 중» 안내(추후 작업 단위).

## 구현 요약

- `#shopw` : `top:104 bottom:180`, 배경 `#272731`. 카드 리스트 `.shp-list`(스크롤) + 하단 카테고리 탭바 `.shp-cats`.
- 상자 카드 3장 = 기존 소환 배너(weapon/shield/skill)에 연결. 카드별로 바뀌는 값: 헤더색·본문 그라디언트·상자 아이콘·Lv·경험치.
- 소환 버튼 3개: **무료 10연**(일 2회, `S.daily.freeSum`, `doSummonFree` 신설) / **💎 10연**(`summonCost×0.9`) / **💎 30연**. 실제 게임 경제값을 표시(레퍼런스의 1,000/3,000 은 목업값이라 실제 비용 1.44K/4.32K 로 대체).
- 🔍 확률 버튼 → `gradeProbs` 기반 등급별 확률 팝업(11번 «확률 정보» 의 간이 버전 — 11 담당이 정식 페이지로 교체 가능).
- 저장 구조 변경(`S.daily.freeSum`)은 `load()` 마이그레이션 + `dailyCheck()` 리셋으로 하위호환. KEY 버전 불변(신규 필드라 기존 세이브에 자동 채움).

## 검증

- 문법 검사 통과, 헤드리스 콘솔 에러 0건.
- 상호작용 테스트: 페이지 오픈(카드 3장)·🔍 팝업·무료 10연(횟수 차감)·💎 10연(1,440 차감)·재화→패널 상점·다른 탭 이동 시 페이지 닫힘·보물상자 탭 유물 배너 유지 — 전부 정상.

## 비평 회차

### 1회차 (A 7 / B 5)
- **A(7, 최저 ①③)**: ▶AD 뱃지가 시안버튼 좌상단에서 라벨 «10회 소환» 을 가림 / 카드 좌우 인셋·세로 gap.
- **B(5, 최저 ⑤)**: **카드 검정 외곽 프레임(7px)이 통째로 누락** → 카드 정의 붕괴.
- **자가 픽셀검증으로 반증한 B 지적**: Lv 알약 글자색은 레퍼런스가 **청록 #7DE5ED**(측정표 §2)라 흰색 지적은 오독 / NEW 리본 글자는 실제 **노랑 #F8E759**(구현 스캔 243,219,85) / 버튼 종횡비는 208×127=**1.64**(측정표 §2)로 정상(B 의 1.35 는 오측).
- **채택·수정 2건**: ① 카드 프레임 `.cfr` 마크업 누락 복구(측정 스캔으로 좌·상변 7px 순검정 확인). ② ▶AD 뱃지를 버튼 `overflow` 밖(카드 직속 자식)으로 빼 좌하단 코너(707,198)에 −6° 배치 — 라벨 가림 해소(구현 스캔 badge x760..817 / 라벨 x768..923 분리 확인).

### 2회차 (C 7 / D 6)
- **C(7, 최저 ②⑤)**: 상자 컨테이너(보물상자) 스프라이트 부재 = 아트 한계 / 방사 스파클 과다 발광 / 자주·보라 바디 명도 −16R.
- **D(6, 최저 ⑤)**: 상자 컨테이너 부재(정체성) + 일러 중심 +31px 우측 / Lv 알약 글자 채도 저하(검정 위 소형 청록 AA 한계) / 활성 알약·✦ 구분선 소폭 우측.
- **자가검증 반증**: 활성 알약 높이는 fill 66px(레퍼런스 65)로 정상, 바 안에 들어감(D 의 «+15px 돌출» 오독). 시안버튼 폭 200 은 측정표 §2 확정치(C 의 «−가로축소» 미채택).
- **채택·수정 3건**: ① 방사 스파클 감광(opacity .16→.09, 마스크 축소) ② 상자 일러스트 좌측 이동(center 312→**298**, 목표 289; 이모지 사이드베어링 보정 `left 103→79`) ③ ✦ 구분선 −6px 좌측(257/747→251/741).

### 최종
- **최고 7 / 최저 6 (2회차)**. 통과 기준 9 미달. 남은 감점은 **전부 아트 리소스로만 회수 가능**한 항목으로 수렴:
  ① **보물상자 컨테이너 스프라이트 부재**(이모지 아이템 단독 대체) — ②상대크기·⑤색명암의 천장.
  ② 게임 전용 서체 부재(시스템 폰트 근사) — ⑥.
  ③ 젬·🔍·▶ 등 아이콘 이모지 대체 — ⑤.
- A1(6)·A2(5)·A3(6)·A4(7)·01(5)·02(5)·05(7) 과 **같은 «아트 한계» 천장**이다. 레이아웃(배치·여백·정렬·폰트비)은 ③여백 9, ①배치·④정렬 7~8 로 도달.
