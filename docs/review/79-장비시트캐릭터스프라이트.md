# 79 — 06 장비 시트 캐릭터 = 전투 플레이어 스프라이트 【⚑ 우선 · T1】

- **세션**: sess-1607-20978 (2026-08-25) · 1회차 완료
- **지시**: PROGRESS 79행 (2026-08-25 주인 지시, 08-26 재촉으로 ⚑ 승격) — 장비 시트의 이모지 🦸 자리표시를
  전투 씬 플레이어 스프라이트 그대로(같은 아틀라스·같은 틴트)로 교체.
- **게이트**: `node tools/verify79.js` → **VERIFY79 17/17 PASS** · `node tools/smoke.js` → **SMOKE PASS**
- **캡처**: `docs/shots/79-r1-av0.png` / `79-r1-av3.png` (1080×2280)

## 구현 (지시 ①~⑤ 전부)

| 지시 | 구현 |
|---|---|
| ① 캔버스 + 같은 아틀라스·틴트 | `renderEqPage()` 가 `<canvas class="eqil-cv" width=640 height=831>` 출력(측정표 06 bbox 45,495 그대로). `drawHeroTo()` 가 `tinted('knight', AV[cosCur()].tint)` 로 그림 — 전투 `drawFrame` 과 **같은 tintCache 캔버스**라 이미지 복제 없음. 코스튬(61) 변경 시 전투·시트 동시 반영(검증 G) |
| ② idle 애니메이션 | `eqHero` 상태(akey knight·idle·**8fps** = 전투 player 초기화와 동일) + `eqHeroTick` rAF. `#eqw.on` 이 아닐 때 첫 틱에서 자멸 — 시트 닫히면 정지(검증 F2) |
| ③ 장착 무기 표시 | 하지 않음(지시대로 — 무기 슬롯 3칸이 그 역할) |
| ④ `text-indent:-40px` 제거 | `.eqil` 규칙 통째 폐기 → `.eqil-cv`(image-rendering:pixelated). `EQP_CARD.ic`(🦸)도 제거 |
| ⑤ 공용 헬퍼 | `drawHeroTo(canvas, {avatar, frame, scale, flip})` — 발밑=캔버스 바닥 중앙 앵커, 미지정 시 정수 배율 floor(h/63). **작업 80 랭킹 시상대는 인자만 바꿔 재사용** |

- 배율: 정수 **13**(논리 프레임 79×63 기준 floor(831/63)) → 잉크 ≈598×598. 세로 831 을 다 못 채우지만
  비정수·비율 왜곡 스케일은 픽셀 아트가 흐려져 금지(지시 «정수 배율»). 가로는 idle 0프레임 잉크 중심으로
  재중심(c0=+12.5src px) — 프레임별 ±1src px 흔들림은 원래 애니메이션이라 살렸다.
- 우향(무보정) 유지 — 레퍼런스 구도(캐릭터가 화면 안쪽·슬롯 쪽을 봄)와 일치. 슬롯 열(x724) 침범 없음(잉크 x47..618).

## 기능 체크 표 (verify79 — 헤드리스 실측)

| # | 확인 | 결과 |
|---|---|---|
| A | `.eqil-cv` 존재 · 박스 45,495 640×831(Δ≤1.5) · 캔버스 해상도 1:1 · pixelated | PASS ×4 |
| B | `.eqil`/🦸 DOM 에 없음 | PASS |
| C | 잉크 209,560px · 발밑=박스 바닥(830) · 가로 47..618 박스 안 · 정수 배율 13 · 잉크 높이 정상 범위 | PASS ×5 |
| D | **av0·av3 각각 시트 캔버스 = `tinted()` 13× 재도시 전 픽셀 diff Δmax=0** (≤3 기준) | PASS ×2 |
| E | **전투 캔버스**에서 플레이어 현재 프레임의 불투명 텍셀 1점을 카메라 역산으로 샘플 → tinted 원본색과 **Δ=0** (≤3 기준) | PASS |
| F | 400ms 간격 캔버스 변화(idle 재생 중) · 시트 닫으면 `eqHeroRaf=0` | PASS ×2 |
| G | 열린 채 `S.avatar` av0→av3 → Δmean 40.3 실시간 색 변화 | PASS |
| Z | 콘솔·페이지 에러 0건 | PASS |

시각 검수(서브에이전트, av0·av3 캡처): 기사 스프라이트 선명(블러·이중상 없음), 발이 바닥 그림자 위·스탯 알약 바로 위,
리본·슬롯 열과 겹침 없음, av3 보라 틴트 뚜렷 — 결함 0건.

## 남은 문제 / 다음 세션 참고

- **없음(79 범위)**. 잉크가 박스 세로의 72%(598/831)인 것은 정수 배율의 구조적 한계 — 주인 지시가
  «전투 스프라이트 그대로» 이므로 이 칸은 더 이상 **아트 발주 대상이 아니다**(측정표 06 §10 갱신).
- 작업 80(랭킹 시상대)은 `drawHeroTo(cv, {avatar:'av2', frame:'idle/frame0000', scale:n})` 처럼 정지 1프레임로 쓰면 된다 —
  다른 플레이어 외형은 avatar 인자만 다르게.
- verify79 [E] 는 `--allow-file-access-from-files` 로 띄워야 한다(file:// 아틀라스 getImageData 오염 방지). smoke 와 무관.
