# 234 — `tools/verify66.js` ③ «수렴 구간 멀어짐%» 재정의 (233 처방 이식)

- **분류** T1 버그(게이트) 【⚑ 우선】 · 지시서 [3]-**(가)** 기계적/수치 검증 → **비평가 없음**
- **세션** sess-1459-7878 (워커 D) · 2026-08-27 · 1회차
- **index.html 변경 0줄** — 게임 코드는 한 글자도 안 건드렸다. `tools/verify66.js` 만 고쳤다.
- 앞선 작업 **233**(`tools/verify172.js`)이 «같은 정의의 원본이 같은 병을 앓는다» 며 등재한 건이다.
  처방은 233 이 확정한 것을 **그대로 옮겼다**(등재문 ⓐ~ⓓ).

---

## 1. 증상 재현 (인계 상태)

```
node tools/verify66.js
| after | 10 | 286 | 76px | 86px | 4.4s | 75px |  8.6% | 89.5% | … | 26회 |
| after | 30 | 286 | 76px | 83px | 4.0s | 75px | 11.2% | 90.9% | … | 27회 |
  ✗ stage 30: 수렴 구간 멀어짐 11.2% ≥ 10%
V66 FAIL
```

등재 당시 실측(연속 2회 **12.5% FAIL → 9.3/9.4% PASS**)과 같은 자리다.
**stage 10 은 8.6%, stage 30 은 11.2%** — 두 값이 판정선 10% 를 **양옆에서 걸치고 진동**한다.
그래서 «뜨고 지는 FAIL» 이었다. 같은 표의 다른 열은 전부 멀쩡하다
(접촉까지 4.0~4.4s · 붙어있음 89.5~90.9% · 수렴 평균 75px = 사거리 76px · 공격 26~27회).

## 2. 원인 — 233 과 같은 «부호가 뒤집힌 자»

66 자신이 넣은 규칙(`index.html` ~16400):

```js
/* 사거리 안에 들어오면 공격 «모션 중에만» 멈춘다 — 쿨다운 중에는 계속 붙어 따라간다 */
if(e.atkT > 0 && d < e.r + player.r + 6) spd = 0;
```

공격 모션 0.45초 동안 **보스는 설계대로 제자리에 선다.** 그 사이 카이팅(59)하는 플레이어만
움직이니 거리가 벌어진다. 옛 ③ 은 그 표본을 «멀어졌다» 로 세었다 — 재고 있던 것은
«추격이 밀린다» 가 아니라 **«지금 때리고 있다»** 였다(«때리는가» 는 ④ 가 이미 따로 잰다).

**66 이 못 박은 거동을 66 의 게이트가 감점하고 있었다**는 것이 이 건의 요점이다.
표본 분해가 그대로 보여 준다 — 수렴 구간 256개 중 **모션이 걸친 표본이 133개(52%)** 이고,
**옛 ③ 의 멀어짐은 전부 그 절반에서 나온다**(아래 §4 «모션 표본» 열 대비 «추격 표본» 열).

## 3. 처방 — 완화가 아니라 재정의 (177-③ 계열)

등재문 ⓐ~ⓓ 를 그대로 따랐다. ⓒ(문턱만 올리기)는 **금지** — 자가 무뎌질 뿐 부호는 그대로다.

| | 내용 |
|---|---|
| ⓐ | ③ 의 표본을 **«추격 표본»** 으로 재정의 — 표본 창(6프레임 = 0.1초) 안에 `e.atkT > 0` 인 프레임이 **한 개도 없던** 표본. 모션이 걸친 표본은 분자·분모에서 **함께** 뺐다 |
| ⓑ | **최소 표본 단언** — 추격 표본 `< 30` 이면 FAIL(«표본이 사라져서 통과» 방지). `MIN_CHASE = 30` |
| ⓒ | 옛 정의 값은 `convAwayRaw` 로 **표에만** 남기고 판정에서 뺐다 |
| ⓓ | 문턱 `LIM_AWAY` **10 → 4%** — 재정의 뒤 실측이 2개체 × 3회 **전부 0.0%** 라 10 은 너무 헐겁다 |

172 와 정의·상수·열 이름을 **글자 그대로 맞췄다**(`chaseN` · `atkSampN` · `convAwayRaw` ·
`LIM_AWAY` · `MIN_CHASE`). 두 파일이 다시 갈라지면 같은 병이 한쪽에서만 재발한다.

## 4. 검증

### (가) 본 게이트 3회 반복 — 전부 PASS, ③ 은 전부 0.0%

| 회 | stage 10 ③ / 추격표본 / 모션표본 / 옛 정의 | stage 30 ③ / 추격표본 / 모션표본 / 옛 정의 | 판정 |
|---|---|---|---|
| 1 | **0.0%** / 123 / 133 / 9.7% | **0.0%** / 122 / 133 / 9.4% | V66 PASS |
| 2 | **0.0%** / 123 / 133 / 9.3% | **0.0%** / 104 / 152 / 9.7% | V66 PASS |
| 3 | **0.0%** / 125 / 133 / 9.7% | **0.0%** / 124 / 133 / 10.5% | V66 PASS |

옛 정의는 **9.3~11.2%** 로 10% 선을 걸치고 진동한다(수정 전 재현분 8.6·11.2% 포함).
새 정의는 2개체 · 3회차 **전부 0.0%** 이고 추격 표본은 104~125개(최소 30 조건에 여유 3배 이상).
233 의 172 쪽 실측(0.0% / 87~133개)과 **자릿수까지 같다** — 같은 병, 같은 회복이다.

### (나) 되돌림 시험 — 자가 아직 무는가

자를 무디게 만들지 않았다는 것을 반증으로 확인했다. 둘 다 **임시 사본에만** 주입하고
(`V66_FILE=` 로 지정) 즉시 지웠다. 저장소 `index.html` 은 0줄 변경이다.

| 시험 | 주입 | 결과 |
|---|---|---|
| **A** | `const isBoss = false` (SOLO_CHASER 직진 추격 예외 자체를 끔) | **V66 FAIL** — 보스속도 55~57px/s(플레이어 115 의 절반) · «한 번도 못 붙음» · 공격 0회 · **추격 표본 0 < 30**(ⓑ 새 단언이 «표본이 없어서 통과» 를 막았다) |
| **B** | 순수 추격 중 **모션과 무관하게** 0.5초마다 25px 뒤로 밀기 (185 가 걷어낸 넉백 재현) | **V66 FAIL** — stage 10·30 둘 다 ③ **19.8% ≥ 4%**(추격 표본 247 · 모션 표본 0) · 수렴 평균 112px > 사거리+50 |

**시험 B 가 핵심이다** — «모션과 무관한 진짜 밀려남» 은 새 ③ 이 그대로 잡는다.
19.8% 는 233 이 172 에서 받은 값(19.8·19.9%)과 같다. 시험 B 에서 모션 표본이 **0** 인 것도
자연스럽다: 0.5초마다 25px 밀리면 사거리 안에 머물지 못해 공격 자체가 안 나온다(공격 0회).

### (다) 회귀

- `node tools/verify172.js` → **V172 PASS** (③ 세 개체 전부 0.0% · 추격 표본 105~121)
- `node tools/smoke.js` → **SMOKE PASS**
- `node --check tools/verify66.js` OK · 콘솔 pageerror 0건 · 저장 구조 미변경(KEY 유지)

## 5. 남은 것

없음. 234 는 이 회차로 닫는다(1/5회차).

233 이 «172 의 ③ 은 66 에서 복사돼 나왔다» 고 짚어 둔 덕에 원본까지 같은 자로 맞췄다.
**이 정의를 쓰는 파일은 이제 `verify66.js` · `verify172.js` 둘뿐이고 내용이 동일하다** —
앞으로 한쪽을 고치면 반드시 다른 쪽도 같이 고칠 것.
