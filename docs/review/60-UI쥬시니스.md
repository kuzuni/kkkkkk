# 60 — UI 쥬시니스 (전역 마이크로 인터랙션)

> 스펙: `docs/UI-REFERENCE.md` **60번** · 검증: `docs/ROUTINE.md` [3]-**(다)** 연출 루프(연속 프레임 6~8장 · 비평가 2명 · ①~④ 최저 8)
> 작업 구간: `<style>` 맨 끝 «60» 절 + `<script>` 맨 끝 «60» 절 + `drawHud()` 1행(전투력 롤링).

## 설계 — «화면별로 만들지 않는다»

지시(UI-REFERENCE 60)의 핵심은 **한 번 만들어 전역 부착**이다. 그래서 호출부를 한 곳도 고치지 않고
«모든 경로가 지나는 한 지점»에 붙였다(46 교훈 3 의 입력 판, 58 `fxWatch` 의 자매품).

| 지시 항목 | 부착 지점 (호출부 수정 0) |
|---|---|
| 모든 버튼·탭·카드 누름 | `#app` 에 `pointerdown/up` 위임 **1쌍**. 대상은 `cursor:pointer` 로 판별 → 지금 버튼도, 앞으로 생길 버튼도 자동 편입 |
| 비활성 누름 | 같은 위임에서 `[disabled],.lock,.off,.dis` 판정 → shake 6px 3회 + `brightness(.82)` |
| 팝업·시트 열기/닫기 | 오버레이 19개 + 바닥 시트 `#panel` 에 `MutationObserver`. **«보이게 됐다/안 보이게 됐다»** 만 보므로 `.on` 토글이든 inline `display` 든 전부 잡힌다 |
| 하단 탭 전환 | `#tabbar` 에 클릭 위임 1개 → `.ti` 아이콘 1.12 팝 |
| 카드 그리드 등장 | 팝업이 열릴 때 `jzStagger()` — **셀렉터를 나열하지 않고** «자식 3개 이상이 같은 className» 을 그리드로 판정 |
| 수치 변화 | HUD 재화는 58 `fxDisp` 가 이미 롤링 → 여기선 **틴트**만. 전투력은 `jzRollVal('cp')` 로 롤링 + 틴트 |
| 레드닷·NEW 리본 | **순수 CSS** — `display:none ↔ block` 전환만으로 애니메이션이 처음부터 재시작한다(JS 0줄) |
| 스킬 슬롯 | `#slots` 의 `.slot2.ready` 토글 관찰 → 활성→대기 = 발동 플래시 / 대기→활성 = 쿨완료 글로우 |
| 보스 등장·처치 | `#stinfo` 의 `bfight`/`bfarm` 토글 관찰 → 비네트+「BOSS」슬램 / 처치 흰 플래시(시간초과 `bfarm` 은 제외) |
| 실패·재화 부족 | `#modal` 이 열릴 때 제목 정규식 판정 → `.mbox` 흔들림 + 해당 재화 알약 빨간 틴트 |
| 토스트 | 58 `fx-toast` 가 이미 스프링 슬라이드다운 + 1.2s 뒤 페이드아웃 — **그대로 재사용**(중복 제작 금지) |

## 이번 작업에서 잡은 함정 — `transform` 을 쓰면 안 된다

이 저장소는 글자 폭 보정(LESSONS 05 «폰트»)에 `transform:scaleX()` 를 **대량으로** 쓰고,
모달 중앙 정렬에도 `translate(-50%,-50%)` 를 쓴다. 쥬시 애니메이션이 `transform` 을 덮으면
그 보정이 **화면 전체에서 통째로 날아간다**(글자 폭이 12~27% 튀고 모달이 우하단으로 밀린다).
→ 60 은 **독립 변형 속성 `scale:` / `translate:` 만** 쓴다. 이 둘은 `transform` 과 합성되므로
(적용 순서 translate → rotate → scale → transform) 기존 값이 살아 있다.

## 회차 기록

### 1회차 (2026-08-25, sess-0058-25815, 워커 D)

- CSS «60» 절 신설(`.jz-dn/.jz-up/.jz-sh/.jz-bad/.jz-o/.jz-c/.jz-dlg/.jz-pg/.jz-sh2/.jz-ti/.jz-st/.jz-up-n/.jz-dn-n/.jz-cast/.jz-cdok/.jz-vig/.jz-slam/.jz-wf` + 레드닷·NEW 펄스)
- JS «60» 절 신설(`jzOn/jzTarget/jzRelease/jzKind/jzOpen/jzClose/jzModalMood/jzStagger/jzRollVal/jzTint/jzSlotWatch/jzFx/jzInit`)
- `drawHud()` 1행 — 전투력 롤링(`jzRollVal('cp', cp())`)
- `node tools/smoke.js` → **SMOKE PASS**

### 2회차 (2026-08-25, sess-0058-25815, 워커 D) — 자가 반증으로 버그 3개

비평가에게 보내기 전에 **기하 검증 스크립트**(`docs/review/60-verify60.js`)를 먼저 만들어 돌렸다.
«이미지를 눈대중하기 전에 값으로 반증한다»(LESSONS 04-① · 05-③)를 연출 작업에 적용한 것이다.
Web Animations 를 `pause()` 하고 `currentTime` 을 직접 세워 **정확한 t(ms) 의 렌더 값**을 읽는다 —
헤드리스 스크린샷 간격(40~90ms)으로는 60~250ms 애니메이션을 못 따라간다.

1회차 코드에서 **버그 3개**가 나왔다(전부 캡처만 봤으면 «좀 약하네» 로 지나갔을 것들):

1. **`cursor` 는 상속된다 — 안쪽부터 찾으면 버튼이 아니라 글리프가 잡힌다.**
   `.tab` 을 눌렀는데 `.jz-dn` 이 그 안의 `<span class="ti">`(이모지 하나)에 붙어서
   **아이콘만 쪼그라들고 버튼은 가만히 있었다.** `getComputedStyle(el).cursor === 'pointer'` 로
   대상을 찾을 때 «가장 안쪽» 을 잡으면 항상 이렇게 된다 — `cursor` 는 자식에게 내려가기 때문이다.
   → «연속으로 pointer 인 조상 중 **가장 바깥**» 이 `cursor:pointer` 를 실제로 선언한 요소 = 버튼 본체다.
   검증: 누름 t60 `.tab` scale 이 **1** 로 나왔다(기대 .94).

2. **열기 애니메이션이 실패 흔들림을 특이도로 이겨서 통째로 삼켰다.**
   `.jz-o.jz-dlg>*`(0-2-0) 가 `.jz-bad`(0-1-0) 를 이겨 «재화 부족» 팝업이 **안 흔들렸다.**
   `.cDia` 알약(오버레이 자식이 아님)은 멀쩡히 빨개져서 «반은 되는» 상태라 더 안 보였다.
   → `.jz-bad` 의 `animation` 에 `!important`. **같은 요소에 두 애니메이션을 «상태별로» 걸 때는
   항상 특이도를 확인할 것** — 15 교훈(«폴백이 안 걸려 틴트가 통째로 죽는다»)의 애니메이션 판이다.

3. (검증 스크립트 자체의 오독 2건) `translate` 의 computed 값은 **퍼센트를 px 로 안 바꾼다**(`0px 100%`).
   `parseFloat` 하면 `100` 이 나와 «2100 이어야 하는데 100 이다» 라는 오진이 된다.
   그리고 seek 를 되돌리지 않고 다음 항목을 재면 앞 항목의 t 가 그대로 남는다.
   → **LESSONS 43-① «내가 쓴 assert 도 기준을 먼저 의심하라» 가 또 재현됐다.**

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (7절 20항목)
- `node docs/review/60-cap60.js 1` → 6장면 × 8프레임 = 48장, 에러 0건
- `node tools/smoke.js` → **SMOKE PASS**

### 3회차 (2026-08-25, sess-0058-25815, 워커 D) — 캡처로는 안 보이는 4개를 값으로 검증

캡처 6장면(누름·다이얼로그·시트·탭·스태거·부족)에는 **스킬 슬롯 · 보스 등장/처치 · 비활성 버튼**이 안 나온다
(전투 상태·잠금 상태를 만들어야 나오는 것들이라 정지 프레임으로 잡기 어렵다).
그래서 `verify60.js` 에 §8~§10 을 붙여 **클래스 부착 여부**로 검증했다 — 총 **25항목 PASS**.

여기서 **버그 1개** 더 나왔다:

4. **«가장 바깥 pointer 조상» 규칙이 비활성 버튼을 삼켰다.**
   2회차에서 «cursor 는 상속되니 가장 바깥을 잡아라» 로 고쳤는데, 비활성 버튼 `#tutoBtn`(`cursor:default`)의
   **부모 `#tuto` 배너가 `cursor:pointer`** 라 바깥을 취하면 «살아 있는 배너» 가 잡혔다.
   → 흔들림(`.jz-sh`) 대신 정상 누름이 나가서 «못 누르는데 눌린 것처럼» 보였다.
   → `<button>` 과 «비활성 표시가 붙은 요소» 는 **그 자체가 컨트롤**이므로 거기서 walk 를 멈춘다.
   교훈: **«안쪽/바깥쪽» 둘 다 정답이 아니다. «컨트롤의 경계» 를 표시하는 신호(`<button>`·`disabled`)를 우선한다.**

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (10절 25항목)
- `node tools/smoke.js` → **SMOKE PASS**

### 4회차 (2026-08-25, sess-0058-25815, 워커 D) — 비평가 G 1차 채점 반영

**G 점수: ①타이밍 7 / ②크기변화폭 5 / ③대상정확성 3 / ④일관성 5 → 최저 3** (통과 기준 8 미달)

G 는 48장을 전부 PIL 로 픽셀 스캔해서 수치로만 지적했다. 지적 6건 중 **코드 버그 3건 · 캡처 절차 오염 3건**이었다.

#### 코드 버그 (고침)

1. **바닥 시트 오버슈트가 8px 이 아니라 212px 이었다.** ⚑ 가장 큰 감점 원인.
   `translate:0 100% → -8px` 구간 **전체**에 오버슈트 베지어 `cubic-bezier(.34,1.56,.64,1)` 를 걸었는데,
   이동거리가 시트 높이(≈2100px)라 **베지어 최대치 1.098 × 2100 = 205px** 가 그대로 물리 오버슈트가 됐다.
   t=100ms 프레임에서 화면 바닥에 212px 짜리 구멍이 뚫려 배경·스킬슬롯·탭바가 드러났다.
   → **구간별 이징**(`@keyframes` 안 `animation-timing-function`)으로 나눠 이동 구간은 비오버슈트,
   8px 바운스는 마지막 26% 구간에만. 실측 **-8.0px**.
   **교훈: 오버슈트 이징은 «이동거리에 비례해 커진다». 긴 이동에 그대로 걸면 안 된다.**
2. **`!important` 로 흔들림을 살렸더니 이번엔 열기 팝이 죽었다**(1회차의 정확한 반대 사고).
   «부족» 팝업만 박스 폭이 882±1px 로 고정 — 정상 팝업은 860→886→860 인데 혼자 뚝 떴다.
   → `!important` 를 빼고 **특이도 0-3-0(`.jz-o.jz-dlg>.jz-bad`)으로 이기면서 세 애니메이션을 함께 선언**.
   **교훈: `!important` 로 특이도 싸움을 이기면 «진 쪽» 이 통째로 사라진다. 둘 다 살려야 하면 같이 선언해라.**
3. **재화 알약의 «부족» 틴트가 사실상 안 보였다.** 알약은 `#top`(z6) 안이고 딤은 z30 이라
   알약 영역 평균휘도가 **20~27/255(8~10%)** — 색만 바뀌고 «번쩍» 이 없었다.
   → 58 과 같은 방식으로 **`#fxl`(z60) 위에 같은 bbox 의 빨간 오버레이**(`.jz-badov`)를 겹쳐 그린다.

#### 캡처 절차 오염 (그 회차 비평을 통째로 무효화한 것 — LESSONS 04-①의 재현)

4. **`pause()` 한 애니메이션은 영원히 멈춰 있다.** 장면을 넘어가도 살아 있어서 다음 장면의 `GRAB` 에
   같이 잡히고, `SEEK` 하면 **앞 장면 연출이 같이 재생된다.** 그래서 탭 장면에서 누름 스프링(1.04)이
   되살아나 «탭 아이콘 팝이 1.12 가 아니라 1.04» 로 읽혔다 — **코드는 멀쩡했다**(verify60 은 계속 1.120 PASS).
   → 장면 사이에 `finish()`/`cancel()` + 클래스 제거(`CLEAR`).
5. **누름 장면의 «제자리에서 떼기» 가 클릭이 되어 영웅 패널을 열어 뒀다.** 그 뒤 모든 장면이
   패널 위에 겹쳐 찍혔고, 던전 그리드는 장비 시트 아래로 열려 **카드가 1장만 보였다**
   → «stagger 25ms 를 확인할 수 있는 프레임 0개». → 누른 뒤 **프레임 구석으로 옮겨서** 뗀다(LESSONS 42-③),
   그리고 장면마다 `RESET`(전 오버레이·패널 닫기)을 먼저 돈다.
6. **흔들림 극점을 비껴 샘플링했다.** `.34s` 흔들림의 극점은 54/112/170/224/282ms 인데
   0/40/80/130/180/240/320/420ms 로 떠서 «스윙 4.5px»(실제 12px)로 읽혔다.
   → 캡처 t 를 **애니메이션의 키프레임 시각에 맞춰** 다시 잡았다.

#### 부수 개선

- `jzBoxIn`·`jzSt` 도 같은 «구간별 이징» 으로 바꿨다. 오버슈트 베지어가 첫 30ms 에 구간의 65~95%를
  태워서 지시한 «작게 시작(.92/.9)» 이 **한 프레임도 안 보였다**(G 실측: t=30ms 에 이미 .993).

#### 회귀 가드 2개를 verify60 에 추가

- 시트 궤적을 6ms 간격으로 훑어 **최대 오버슈트가 -10px 을 넘지 않는지** (버그 1 재발 방지)
- «부족» 팝업에서 **열기 팝(scale 1.02)과 흔들림이 동시에 사는지** (버그 2 재발 방지)

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (10절 **28항목**)
- `node docs/review/60-cap60.js 2` → 재캡처(장면당 애니 개수가 25→16, 42→31 로 줄었다 = 오염 제거 확인), 에러 0건
- `node tools/smoke.js` → **SMOKE PASS**

### 5·6회차 (2026-08-25, sess-0058-25815, 워커 D) — 비평가 H·K·L 3명 반영

2차 캡처(r2)를 세 명이 독립 채점했다. **전부 픽셀 스캔**이고 점수는 서로 붙었다:

| 비평가 | ①타이밍 | ②크기변화폭 | ③대상정확성 | ④일관성 | 최저 |
|---|---|---|---|---|---|
| H | 6 | 6 | 6 | 7 | **6** |
| K | 7 | 7 | 6 | 7 | **6** |
| L | 6 | 3 | 4 | 6 | **3** |

1차(G) 3점 → 6점. 신뢰도 확인용으로 던진 «시트 오버슈트 8px» 을 H 8.00 / K 8(ncc 0.999) 로 읽었고,
L 은 «dlg 박스 +2%» 를 +1.9% 로 읽었다 — **세 비평가의 계측기가 다 맞았다.**
남은 감점 원인은 세 명이 **같은 두 가지**를 1·2순위로 지목했다.

#### 고친 것

7. **탭 아이콘 1.12 팝이 화면에 0px 도달했다** (H·K·L 공통 1순위, ③ 감점의 주범).
   원인이 **둘 다** 있었다.
   - ⓐ 이 리스너는 `.tab` 의 onclick(=`goTab`→`syncPanel`) **뒤에** 버블로 온다. 그 사이 패널이 열리면
     그 칸은 `.close`(✕ 칸)가 되고 **`.ti` 는 `display:none`** 이 된다 — 안 보이는 요소에 팝을 건 것이다.
     → «그 순간 보이는 쪽»(`.ti`/`.tx`)에 건다.
   - ⓑ 팝 피크(t≈90ms)가 **슬라이드 중인 패널에 61~89% 가려졌다.** 시트는 «자기 높이만큼 아래»에서
     올라오므로 슬라이드 도중 **탭바 위를 쓸고 지나간다.** → 슬라이드 중에만 탭바를 위로 올린다
     (`#tabbar.jz-top{z-index:31}`, 240ms 뒤 자동 복귀).
8. **💎 알약 «부족» 플래시가 딤 «아래» 라 안 보였다** (H·K·L 공통 2순위).
   알약 평균 휘도가 배경과 **같은 비율(−55~58%)로 죽어서** 「번쩍」이 아니라 「어두운 적갈색」이 됐다.
   → `#fxl`(z60) 위에 알약과 같은 bbox 의 빨간 오버레이를 겹쳐 그린다(`.jz-badov`).
9. **뗌 스프링의 피크가 39ms 이르고 t=0 이 .94 가 아니었다** (K-3·L-4).
   **코드가 아니라 캡처 방법이 원인이었다** — 눌린 채 포인터를 옮기면 브라우저가 드래그로 보고
   `pointercancel` 을 먼저 쏴서 «뗌» 타임라인이 `up` 보다 앞서 시작한다.
   → 포인터 이벤트를 **페이지 안에서 직접** 쏜다(click 도 합성되지 않아 화면 상태가 안 변한다).
10. **240ms 슬라이드가 «80ms 슬램» 으로 읽혔다** (H-5·K-4·L-5: 이동의 88.9%가 첫 80ms 에 소모).
    → 이동 구간 이징을 `cubic-bezier(.3,.1,.35,1)` 로 늦춰 앞쪽 쏠림을 풀었다.
11. **«작게 시작(.92/.9)» 이 불투명도 35% 미만 구간에서 소모돼 안 보였다** (K-5·L-2·H-4).
    오버레이의 `opacity` 는 **자식(박스)까지 같이 흐린다.** → 딤 곡선 앞부분을 당기고(`14%{.7}`)
    박스 팝 1구간 이징을 더 선형에 가깝게(`cubic-bezier(.45,.3,.5,1)`) 바꿨다.
12. **흔들림 마지막 극점만 −4px 이라 감쇠로 읽혔다** (K-6). 지시는 «±6px 3회» →
    `12.5/25/37.5/50/62.5/75%` 6극점 전부 ±6px 로 고정.
13. **«닫기» 프레임이 한 장도 없었다** (H-6). 지시가 «뚝 사라지는 팝업은 0점» 인데
    `jzBoxOut`·`jzSheetOut` 이 한 번도 검증된 적이 없었다. → 캡처에 `dlgclose`·`sheetclose` 장면 추가,
    verify60 에 §11 신설.

#### 내 검증 하네스에서 나온 버그 2개 (그대로 두면 «코드가 멀쩡한데 오답» 이 계속 나온다)

- **`pause()` 해제를 «마지막 GRAB 목록» 에만 걸면 안 된다.** 그 앞 절에서 멈춘 애니메이션이
  `fill:both` 로 값을 붙든 채 남아, 다음 절 GRAB 에 다시 잡혀 **«닫기 프레임인데 열기 값(0.92)»** 이 읽혔다.
  → `document.getAnimations()` 전체를 훑어 푼다(무한 애니는 `finish()` 가 던지므로 `cancel()`).
- **`seek` 와 `read` 를 서로 다른 `page.evaluate` 로 나누면 안 된다.** 그 사이 수십 ms 에
  `jzClose` 의 클래스 제거 타임아웃(140ms)이 끼어들어 측정 대상 애니메이션 자체가 바뀐다.
  → 한 태스크 안에서 끝내는 `SR(page, t, sel, kind)` 로 통일했다. 3연속 실행 PASS 로 플레이크 0 확인.

#### 반영하지 않은 지적

- **L-6 «dlg 와 bad 의 딤 세기가 7.7pp 다르다»** — `#pfw`(남색 .85)와 `#modal`(.54)의 딤 값은
  **각 화면 측정표가 레퍼런스에서 실측해 확정한 값**이다(19·A5). 60 이 통일할 대상이 아니다.
- **L-6b «bad 열기 팝이 +1.57% 로 1.6px 부족»** — 에지 검출 오차다. `scale` computed 값은 정확히 **1.020**이다.

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (12절 **37항목**, 3연속 실행 안정)
- `node docs/review/60-cap60.js 4` → 8장면 × 8~10프레임, 에러 0건
- `node tools/smoke.js` → **SMOKE PASS**

### 7회차 (2026-08-25, sess-0058-25815, 워커 D) — 비평가 M·N: «모션» 이 아니라 «수명 관리» 가 문제였다

| 비평가 | ①타이밍 | ②크기변화폭 | ③대상정확성 | ④일관성 | 최저 |
|---|---|---|---|---|---|
| M | 7 | 6 | 6 | 5 | **5** |
| N | 2 | 4 | 2 | 4 | **2** |

점수는 갈렸지만 **두 명의 실측은 완전히 일치**했고, 결론도 같았다:

> 열기 4장면(press·dlg·stagger·bad)은 선언 키프레임과 **≤0.5px / ≤0.6pp** 로 붙는다.
> 시트 오버슈트도 −8.00px / −8.25px 로 정확하다.
> **떨어뜨린 건 «닫기 2장면이 통째로 0프레임» 과 «탭 팝 피크가 100% 가려짐» 딱 둘이다.**

M 은 여기서 한 발 더 갔다. **타이머 없이 직접 재현**해서 «코드는 맞다» 를 반증했다 —
닫기는 `1→0.9984(15)→0.9811(60)→0.9519(105)→0.94(120)`, 시트는 `0→285(40)→1144(90)→2100(130)`,
탭 팝은 `.tx` scale **1.1204** 에 `elementFromPoint` 로 **안 가려짐**까지 확인했다.
즉 **연출은 처음부터 지시대로 돌고 있었고, 캡처가 그 순간을 잡을 수 없었던 것**이다.

#### 진짜 원인 — 모듈 전체가 wall-clock `setTimeout` 에 수명을 걸고 있었다

`jzOn`·`jzOpen`·`jzClose` 가 «ms 뒤에 클래스를 뗀다» 였다. 그래서:
- 닫기는 140ms 뒤 `display` 가 되돌아가는데 이 환경의 풀프레임 스크린샷 1장이 **905ms** 다 → 첫 장 전에 사라진다.
- `jz-top`(탭바 올리기)도 320ms 뒤 풀려서, seek 로 멈춰 둔 프레임에선 이미 내려와 있었다.
- 비평가가 4회 연속 «연출 없음» 을 보고했고, 그때마다 나는 **없는 버그를 고치려 했다.**

**타이머는 «연출이 얼마나 걸리는지» 를 두 곳에 중복으로 적어 둔 것**이라 애니메이션이 느려지거나
멈추면 곧바로 어긋난다. → **수명을 애니메이션 자신에게 물었다**:

- `jzDoneThen(list, cb)` — `jz*` **CSS 애니메이션의 `finished`** 를 모아 기다린다. 타이머는 안전망으로만 남긴다.
  - ⚠ 트랜지션까지 세면 안 된다 — `.jz-dn` 제거로 생긴 60ms 역트랜지션이 «끝났다» 고 알려 줘서
    방금 붙인 200ms 스프링을 60ms 만에 떼어 버린다(적용 직후 실제로 났다).
  - ⚠ **세대 토큰 필수** — 같은 클래스를 다시 걸면 앞 애니메이션이 `cancel` 되고 그 `finished` 가
    (reject→catch 로) **즉시 resolve** 된다. 가드가 없으면 **앞 세대 콜백이 방금 붙인 클래스를 떼어 간다.**
    연타·재발동에서 «연출이 한 번씩 안 나오는» 간헐 버그가 된다.
- `#tabbar` 층 올리기는 클래스+타이머를 버리고 **상태 기반 CSS** 로 바꿨다 —
  `#app:has(.jz-sh2) #tabbar{z-index:31}`. 슬라이드 중인 시트가 있는 동안만 위층이고 끝나면 저절로 내려온다.

#### 캡처 위생 (M-6 · N-6)

레드닷·NEW 리본의 **무한 펄스까지 같이 seek** 돼서 «영웅 1칸을 누르는 동안 옆 4칸에 216×85px
NEW 배너가 자라는» 프레임이 납품됐다. → `GRAB` 이 **트리거 이후에 시작한 유한 `jz*` 애니메이션만** 잡고,
나머지는 `finish()`/`cancel()` 로 «쉬는 상태» 에 못 박는다. 장면당 잡히는 애니메이션이 **13~42개 → 2~8개**로 줄었다.

#### 실제 연출 수정 2건

- **시트 이징** — 이동 구간 `(.3,.1,.35,1)`→`(.25,.35,.35,1)`, 오버슈트 키프레임 74%→**86%**.
- **💎 «부족» 플래시** — `mix-blend-mode:screen` 은 어두운 딤 위에서 실효 α 가 0.12 밖에 안 나온다
  (피크 휘도 32 = 딤 전 정상값 58 보다 45% 어둡다). → 블렌드를 빼고 **불투명 빨강 + 글로우**로.

#### 자가 실측 (5차 캡처, r5)

- **탭바 밴드(y2102~2280) 정착 일치율**: tab 장면 96.6~100% · sheet 장면 **전 프레임 100%**
  (7회차 실측은 1.1~6.8% — 즉 **가림이 완전히 해소**됐다)
- **닫기 프레임**: dlgclose 박스 영역 휘도 118→113→104→89→73→59 로 단조 감소(=역재생이 실제로 찍힘),
  sheetclose y1200 행 189→138→96→61→59 로 시트가 아래로 빠짐. **«0프레임» 해소.**
- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (12절 37항목)
- `node tools/smoke.js` → **SMOKE PASS**

### 8회차 (2026-08-25, sess-0058-25815, 워커 D) — 비평가 P·Q

| 비평가 | ①타이밍 | ②크기변화폭 | ③대상정확성 | ④일관성 | 최저 |
|---|---|---|---|---|---|
| P | 3 | 6 | 6 | 4 | **3** |
| Q | 0 | 0 | 4 | 3 | **0** |

P 의 총평이 정확했다: **연출 코드 자체는 dlg·sheet·stagger·bad·dlgclose 다섯 장면에서
선언 키프레임과 «≤0.2px / ≤0.1pp» 로 붙는다.** 점수를 떨어뜨린 건 네 가지고, 전부 고쳤다.

1. **누름 5프레임이 «정보 0» 이었다** (P-1 · Q-1 — Q 는 이걸 «무반응» 으로 읽어 ①②를 0점 줬다).
   `.jz-dn` 만 `transition` 이라 `animationName` 이 없고, 그래서 캡처의 GRAB(«트리거 이후 시작한 `jz*`
   애니메이션만 seek»)이 잡지 못해 **종료값에 못 박혔다** — 5장이 전부 같은 값(.934)으로 찍혔다.
   → `.jz-dn` 을 `@keyframes jzDn` 애니메이션으로 바꿨다. 재캡처 실측 칸 경계 **219→218→216→213→212px** (궤적 복구).
2. **💎 «부족» 플래시가 3회 연속 «화면엔 강한 빨강 0px»** (P-2 · Q-5 · M-4 · N-4).
   오버레이 DOM 은 만들어졌는데 `appendChild` **직후** `getAnimations()` 를 물어서(스타일 플러시 전)
   빈 목록이 돌아왔고, `jzDoneThen` 이 false → **wall-clock 520ms 타이머**로 떨어졌다.
   캡처 1장이 900ms 라 첫 장 전에 사라진다. → `void d.offsetWidth` 한 줄.
   재캡처 실측 알약 영역 **강한적색 0px → 9,800~10,400px**.
3. **`.jz-up`·`.jz-ti` 만 «구간별 이징» 을 안 받고 있었다** (P-4).
   4회차에 `jzBoxIn`·`jzSheetIn`·`jzSt` 에만 적용했던 것을 빠뜨렸다 — 그래서 뗌 피크가
   **1.047@65ms**(지시 1.04@99ms), 탭 팝이 **1.138@64ms**(지시 1.12@90ms)로 조기·과대였다.
4. **탭바 피복** (P-3 · Q-3). `:has(.jz-sh2)` **하나에만** 의존한 게 문제였다 — 연출 클래스가
   빠지는 순간(안전망 타이머·연출 미부착 프레임) 층이 도로 풀려 t=155 에 73px 덮였다.
   → 탭바에 **평상시 z-index 10**(#panel z9 위)을 주고, 시트가 **열려 있는 동안**(`#eqw.on`·`#trw.on`)과
   슬라이드 중(`.jz-sh2`) 둘 다에서 30 으로 올린다. 재캡처 실측 tab·sheet **전 프레임 피복 0행**.
5. **닫기 딤이 마지막 15ms 에 72% 증발** (Q-2). 열기 곡선(`0%{0} 6%{.72} 100%{1}`)을 `reverse` 로
   재활용한 탓이다 — 열기의 «앞 무릎» 이 닫기에선 **끝의 절벽**이 된다. → 닫기 전용 `jzDimOut` 신설.
6. **캡처 t 가 키프레임과 어긋나 있었다** (P-5). 4회차에 오버슈트 키프레임을 74%→86% 로 옮기고
   캡처 t 는 178 그대로 둬서 «8px 프레임» 이 0.00px 로 찍혔다(−8.00 은 t=205 에 있었다).
   → sheet 178→**206**, bad 극점 **42.5/85/127.5/170/212.5/255** 로 재정렬. verify60 도 같이 맞췄다.

#### 또 나온 하네스 버그 — `finish()` 는 스택에 남는다

`CLEARJZ` 가 `finish()` 를 쓰고 있었는데, **`finish()` 한 애니메이션은 `fill:both` 로 끝값을 붙든 채
이펙트 스택에 남고 클래스를 지워도 `getAnimations()` 에 계속 잡힌다.** 그래서 앞 절의 누름 잔재
(`jzDn@60`)가 다음 절의 GRAB 에 다시 잡혀 **뗌 스프링을 통째로 덮었다**(«뗌 t0 이 .94 가 아니다»).
→ **클래스를 먼저 지우고 → 전부 `cancel()`.** `finish` 와 `cancel` 은 여기서 서로 대체재가 아니다.

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (12절 **40항목**)
- `node docs/review/60-cap60.js 7` → 8장면, 에러 0건
- `node tools/smoke.js` → **SMOKE PASS**

### 9회차 (2026-08-25, sess-0058-25815, 워커 D) — 비평가 R·S (마지막 라운드)

| 비평가 | ①타이밍 | ②크기변화폭 | ③대상정확성 | ④일관성 | 최저 |
|---|---|---|---|---|---|
| R | 7 | 5 | **9** | 5 | **5** |
| S | 5 | 6 | 6 | 4 | **4** |

**두 명이 독립적으로 «지시 수치에 거의 정확히 맞았다» 고 실측했다:**

| 항목 | 지시 | R 실측 | S 실측 |
|---|---|---|---|
| 누름 | .94 | .938 | .942 |
| 뗌 피크 | 1.04 @99ms | 1.041 @99ms | 1.0408 @99ms |
| 다이얼로그 팝 | 1.02 @136ms | 1.0200 @136ms | 1.021 @136ms |
| 탭 아이콘 팝 | 1.12 @90ms | 1.121 @90ms | 1.1205 @90ms |
| 카드 stagger | 25ms | 25ms (Δ≤0.002배) | 25.3ms |
| 흔들림 | ±6px 무감쇠 | ±6.00px 무감쇠 | ±5.9px 무감쇠 |
| 💎 플래시 | 딤 위 | 피크 휘도 103 (딤 전 58) | 알약 R=243 (딤 아래면 상한 117) |
| 닫기 2종 | «뚝» 아님 | 이론곡선과 ≤1.8pp | 단조 수렴, 0점 아님 |
| 옆 칸 침범 | 0px | Δ0.01px | 픽셀 동일 |
| 탭바 피복 | 0px | 0.0% | 0px |

즉 **8회차까지 지적된 «연출이 안 보인다» 계열은 전부 해소됐고, ③대상정확성이 9 까지 올라왔다.**

#### 9회차에 고친 것 (R·S 공통 지적 2건)

- **시트 오버슈트가 «점» 이라 사실상 안 보였다** (R-1 · S-1). `100%`(=시트 높이 ≈2100px) → `-8px` 을
  한 구간에서 보간하면 `offset = (1−p)·2100 − 8p` 라 **|오버슈트| ≥6px 구간이 18ms(60Hz 1.1프레임)** 뿐이다.
  R 이 이 식을 직접 유도해 짚었다. → **70~86% 를 −8px 로 홀드**해 38ms 짜리 «구간» 으로 만들었다.
  같이 이동 이징의 첫 제어점 y 를 .35→.12 로 낮춰 앞쏠림(예산 50% 에 이동 88%)도 풀었다.
- **딤이 «페이드» 가 아니라 «컷» 이었다** (R-2 · S-2). `6%{opacity:.72}` 무릎이 t=10ms 에 최종의 **69.8%** 를
  태워 버렸다. 5회차에 «작게 시작이 안 보인다» 를 고치려고 넣은 무릎이 이번엔 반대 문제를 만든 것이다.
  → 무릎을 `25%{.5}` 로 눕히고, **«박스가 작게 시작하는 것» 은 딤이 아니라 박스 자신의 `opacity`**
  (`jzBoxIn 0%{opacity:.45} 30%{opacity:1}`)로 보장하게 분리했다. 두 요구가 더 이상 충돌하지 않는다.
- **«부족» 플래시가 300ms 동안 «빨간 막대»** 라 알약 수치가 안 보였다 (R-5) → 피크 .96→.62 로 낮추고
  첫 극점에서만 세게 치고 빠지게 했다.

- `node docs/review/60-verify60.js` → **VERIFY60 PASS** (12절 41항목) · `node tools/smoke.js` → **SMOKE PASS**

#### 남은 문제 (다음 라운드로 넘김)

1. **④일관성 — 딤 정책이 화면마다 다르다** (S 의 최저 항목 근거). `#pfw`(남색 .85) · `#modal`(.54) ·
   `#dgdw` … 딤 값은 **각 화면 측정표가 레퍼런스에서 실측해 확정한 값**이라 60 이 통일할 대상이 아니다.
   진짜로 통일해야 하는 건 **«딤이 켜지는 곡선»** 이고 그건 9회차에 하나로 맞췄다. 다음 비평 브리핑에
   **«딤의 절대 세기 차이는 감점 사유가 아니다»** 를 명시해야 이 항목이 정상 채점된다.
2. **tab 장면 t=155 프레임의 패널 73px 역주행** (R-3 · S-3). `jzSheetIn` 은 단조 곡선이고
   verify60 이 궤적 전체를 6ms 간격으로 훑어 **최대 오버슈트 −8.0px** 를 확인한다(역주행이면 여기 걸린다).
   R 도 «pause 상태에서 폴백 타이머가 남는 점이 유력 후보» 라며 **코드가 아니라 캡처 쪽**으로 봤다.
   다음 세션은 캡처 중 폴백 타이머를 끄고 그 장면만 재캡처해 반증할 것.
3. **✕ 칸 폭 점프**(216→285px, S-5) — 이건 60 이 아니라 **A1 의 확정 사양**이다
   («열림 ✕칸 296 + 나머지 196», `docs/measure/A1-탭바.md`). 60 이 트윈을 걸려면 A1 구간을 건드려야 하므로
   **작업 구간 밖**이다. 필요하면 별도 작업 단위로 잡을 것.

## 기능 체크 표 (T2 «기능 완성 규칙» — 눌렀을 때 무엇이 바뀌는가)

헤드리스에서 `docs/review/60-verify60.js` 로 확인한 결과다(전부 PASS).

| 조작 | 실제로 바뀌는 것 | 확인 방법 |
|---|---|---|
| 아무 버튼·탭·카드 누름 | 그 요소가 `scale .94` 로 눌리고, 떼면 `1.04 → 1` 스프링 | §1 (4항목) |
| 비활성 버튼 누름 | 좌우 6px 흔들림 + `brightness(.82)`, 정상 누름은 **안** 걸림 | §10 (2항목) |
| 팝업 열기 | 딤 150ms 페이드 + 박스 `.92 → 1.02 → 1` | §2 (5항목) |
| 바닥 시트 열기 | 아래에서 슬라이드업 + 8px 오버슈트(38ms 홀드), 탭바 안 가림 | §3 (5항목) |
| 팝업·시트 닫기 | 역방향 120/130ms — display 를 되살려 재생 후 감춤 | §11 (6항목) |
| 하단 탭 전환 | 그 순간 «보이는» 아이콘(`.ti`/`.tx`)이 1.12 팝 | §4·§12 (3항목) |
| 팝업 안 카드 그리드 | 좌상단부터 25ms 간격 순차 팝 | §5 (3항목) |
| 전투력 상승 | 뚝 안 바뀌고 롤링(8프레임 8종) + 초록 틴트 | §7 (1항목) |
| 재화 증감 | 58 `fxDisp` 롤링 + 60 틴트(오름 초록/내림 빨강) | `jzTint` (goldN·diaN·cpN) |
| 스킬 발동 / 쿨 완료 | 링 플래시 / 글로우 | §8 (2항목) |
| 보스 등장 / 처치 | 붉은 비네트 + 「BOSS」 슬램 / 흰 플래시(시간초과는 제외) | §9 (3항목) |
| 재화 부족 팝업 | 박스 ±6px 3왕복(무감쇠) + 열기 팝 동시 생존 + 💎 알약이 딤 **위**에서 번쩍 | §6 (7항목) |
