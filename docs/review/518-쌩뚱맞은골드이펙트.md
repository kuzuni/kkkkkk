# 518 — 재화를 얻지 않았는데 «골드 획득» 연출이 터진다 (전수 루프)

> 주인 보고(2026-08-31, 재차·강한 어조): «소환결과에서 골드를 안얻는데 골드얻는 효과나옴 …
> 자꾸 **장비 일괄강화**할때도 골드 얻는 이펙트 뜨고 … 쌩뚱맞는곳에서 골드이펙트 터지는거 싹다 잡아»

세션 `sess-1838-12782` 워커 B · **1~2회차**(계속 진행 중 — lock 해제, 다음 워커가 이어서)

---

## 1회차 (2026-08-31)

### ⓪ 338 규칙 — 처방 전에 재현부터

`tools/probe518.js` 신설. 등재문의 일곱 갈래(ⓐ~ⓖ)를 각각 실측했다.

| 갈래 | 등재문 가설 | 재현 결과 |
|---|---|---|
| ⓐ | 소환은 골드를 주지 않는다 | **확인** — 10회 소환 `S.gold` 델타 **0** |
| ⓑ | 일괄 강화는 골드를 «쓰는» 자리다 | **정정** — 장비 일괄 강화는 골드를 **쓰지도 주지도 않는다**(델타 0 · 156단계). 재료는 **조각**이다 |
| ⓒ | `fxUpOk` 앰버 `#FFC02E` 가 골드색과 겹친다 | **기각(이 자리 아님)** — ΔE(`#FFC02E` ↔ `#FFDE6A`) = **21.2** 로 겹치지 않고, 애초에 **장비 일괄 강화는 `fxUpOk` 를 부르지 않는다**(호출부 9곳 중 하나도 이 경로가 아니다) |
| ⓓ | `fxReward` 상수 크림 | **512 몫** — 이 행에서는 안 건드렸다 |
| ⓔ | 팝업 위 레이어로 온다 | **확인 — 이것이 본체** |
| ⓕ | 발원이 «마지막으로 누른 버튼» 으로 찍힌다 | **확인 — 이것이 본체** |
| ⓖ | `fxPay` 와 겹친다 | 해당 없음(골드를 안 쓴다) |

### ① 뿌리 — «추측한 발원» 이 팝업 위로 올라간다

`fxSrc`(33539)는 발원을 두 힌트 중 **더 최근** 것으로 고른다:
`fxAt(…, 'combat')`(창 600ms, 화면이 «여기서 났다» 고 알려 준 자리)과
`fxTapEl`(창 1200ms, «마지막으로 누른 버튼» = **추측**).

77 이 «전투 발 코인은 `#fxlc`(z7 · 팝업 아래)» 를 세워 뒀지만 그 규칙은 **표시가 붙은 증가분에만**
적용된다. 표시가 없으면 추측이 이기고, 추측은 «UI 발» 이라 `#fxl`(**z60 = 모든 팝업 위**)에 그려지며
딤 위 밝은 알약 복제(`fx-lit`)까지 켠다.

**실측(수리 전)** — 소환 결과 팝업 안 카드를 누른 직후 `fxSrc()` 가 **0프레임 만에** 그 카드를
발원으로 돌려줬고, 그 뒤에 들어온 배경 전투 골드가 **코인 8~16개를 `#fxl` 로**, `+n` 도 `#fxl` 로 쐈다.
⇒ 주인이 본 그림 그대로다: **내가 방금 누른 [일괄 강화]·소환 카드에서 금화가 튀어나온다.**

**표시가 빠진 자리도 실재한다**(정적 스캔) — `S.gold +=` 11자리 중 전투 수입 두 곳
(`22291` 스테이지 클리어 보너스 · `22316` 파도 전멸 보너스)이 `fxAt` 을 **한 번도 안 불렀다**.
게다가 475 가 격파와 클리어 보너스 사이에 **1초 홀드**를 넣은 뒤로는 `killEnemy` 의 전투 힌트(600ms)가
그 골드가 들어올 때 **이미 만료**돼 있어, 이 두 자리는 **거의 언제나** 추측으로 넘어간다.

> ⚠ **레이어만으로는 못 가른다** — 팝업 «안» 카드를 누른 경우 그 노드는 가려져 있지도 않다.
> 1회차 초안이 «가려진 자리인가» 로 짰다가 이 케이스에서 그대로 새는 것을 재현으로 확인하고
> 판정을 **«아는 발원인가 추측인가»** 로 옮겼다.

### ② 처방 — 제품 4자리

1. **`22291`·`22316` 에 전투 발원 표시를 단다** — `fxAt(fxWorld(player.x, player.y − player.r), 'combat')`.
   이 골드는 전투 수입이므로 코인이 전장에서 나는 것이 맞고, 77 규칙에 자동으로 편입된다.
2. **`fxSrc` 의 탭 폴백에 «추측» 표시를 붙인다** — `{ x, y, el, tap:true }`.
3. **`fxFly` — «덮는 층이 떠 있는데 발원이 추측» 이면 그리는 층만 내린다**
   (`const buried = !combat && !!(from && from.tap) && fxCovered()`).
   · 코인 층 `#fxl` → `#fxlc` · `+n` 도 같은 층(`batch.combat = combat || buried`) · 딤 위 알약 복제 생략.
   · **타이밍·개수·소리·경로는 한 값도 안 바꿨다** — 아래 계산은 전부 `combat` 만 본다.
4. **`fxCovered()` 신설** — `#app` 직속 층 중 z > 7 이고 화면을 90% 이상 덮는 것이 떠 있는가.
   프레임당 1회만 재고 캐시한다(`fxSc`/`fxStageTop` 과 같은 관례 — 레이아웃 스래싱 방지).

**무르게 풀지 않았다** — 화면이 `fxAt(요소)` 로 **알려 준** 보상(우편 27033 · 퀘스트 27175 ·
룰렛/상자 24572 · 24719 · 패스 26921 · 32721)은 추측이 아니므로 **종전 그대로 팝업 위에서** 난다
(`verify518` [C2]). 메인 화면의 UI 발도 한 값도 안 바뀐다([D2]).

### ③ 곁다리로 확인한 것

* **소환 결과 팝업 아래의 코인은 «보이지 않는다»** — 심어 둔 코인 55×55 를 `#sumw`(z37, 딤 .80)
  아래에 두고 같은 자리 2장을 대조하니 **바뀐 픽셀 0/4489**. 즉 층을 내리는 것으로 «안 보이게» 는 충분하다.
  ⚠ 첫 대조에서 4256px 이 바뀌어 «보인다» 로 읽힐 뻔했는데, 그것은 **카드 등장 연출이 아직 도는 중**
  이어서였다(2.2초 정착 뒤 재측정 → 0). 캡처 대조는 **연출이 멎은 뒤에** 해야 한다.
* **닫힘 연출 중에도 팝업은 «화면에 있다»** — `.on` 을 떼면 60 쥬시가 `jz-db jz-c jz-pg` 로 바꿔
  `display:block` 인 채 opacity 를 떨어뜨린다(실측 0.29). `fxCovered()` 는 그 구간을 **덮는 층으로 센다** —
  옳은 동작이다(아직 화면에 있다). 게이트가 «팝업 없음» 을 재려면 그 연출이 끝날 때까지 기다려야 한다.

### ④ 게이트

`tools/verify518.js` **신설** — [A] 정적 5항 · [B] 팝업+추측 8항 · [C] 팝업+아는 발원 2항 ·
[D] 메인 회귀 3항 · [X] 콘솔 · **[R] 되돌림 시험**(`fxCovered` 를 «항상 false» 로 묶은 사본에서
코인이 다시 `#fxl` 로 간다 = 수리 전 그림).
`tools/probe518.js` 신설(측정 전용 — 수리 전/후 같은 명령으로 대조).

**결과** — `verify518` **21/21 신설** · `probe518` **7/7 신설** · **SMOKE PASS** ·
회귀 무수정 8종: `verify58` 26/26 · `verify77` PASS · `verify93` 21/21 · `verify158` PASS ·
`verify338` 181/181 · `verify350` 44/44 · `verify363` 80/80 · `verify364` 18/18.
(58·93·77·158 은 이 부품의 타이밍·개수·경로·발원 규칙을 재는 자들이다 — **한 항도 안 고쳤다** 는 것이
«층만 내렸다» 의 가장 짧은 증거다.)

## 2회차 (2026-08-31) — 전수 스윕

`tools/probe518s.js` **신설**. 오프너(탭 5 · 사이드 6 · 메뉴 + 8칸 · 재화 아이콘 3 = **22개**)를 돌며
화면마다 «그 화면이 재화를 준 적이 없는데 `#fxl`(팝업 위)에 재화 연출이 뜨는가» 를 센다.
각 화면에서 **탭 → 2.5초 관찰** 순서로 재는데, 이것이 주인이 겪은 순서 그대로다.

**결과: 덮는 화면 19개 중 «샌 화면» 0개 · 콘솔 에러 0.** (배경 전투는 그동안 계속 골드를 벌었다 —
화면마다 +8~12 골드가 실제로 들어왔고, 그 연출은 전부 팝업 아래로 갔다.)

⚠ **그물 밖이 셋 남았다** — `tab:adv`·`tab:box`·`tab:shop` 이 «덮음 ·» 으로 찍혔다.
실측하니 `dunw`·`relw`·`shopw` 는 **`#app` 직속 · z28 · display:block** 인데 **상단 HUD 를 안 덮어**
`fxCovered()` 의 «세로 90%» 를 못 넘는다. 이 셋은 다음 회차 몫이다(아래 ⑤-1).

### ⑤ 남은 것 (다음 회차 — 이 행은 «전수 루프» 다)

주인 지시가 «전 화면 0 까지» 이므로 **이 회차로 닫지 않는다.** 다음 회차가 할 일:

1. **오프너 전 화면 순회**(351 `cap351.js` SET1~SET3 목록) — 화면마다 «재화를 안 받았는데 재화색
   연출이 뜨는 자리» 를 세는 스윕(`probe518s`)을 만들어 **비평가 2인**에게 준다.
2. **페이지형 화면 3개**(`dunw`·`relw`·`shopw` z28)를 그물 안으로. ⚠ **판정을 그냥 느슨하게 하지 마라** —
   그 페이지 «안» 의 진짜 보상(상점 구매 다이아처럼 **탭 폴백에 기대는** 경로)까지 같이 가려진다.
   **먼저 그 경로들에 `fxAt(요소)` 를 달아 «아는 발원» 으로 만든 뒤에** 넓혀야 안전하다(LESSONS 518-⑤).
3. **소리** — `fxFly` 의 `sfx('coin')` 은 층과 무관하게 난다. 팝업 위에서 코인이 안 보이는데 «촤르륵» 만
   들리는 것이 «효과가 난다» 로 읽히는지 주인 판단 필요(이 회차에서는 **안 건드렸다** — 소리를 끄면
   58/93/98 이 재는 축이 바뀐다).
4. **512 와 겹치는 축**(`FXCUR`·`fxReward` 색) — 512 가 잡는다.

---

## 3회차 (2026-08-30, sess-1936-12590 워커 G — 2회차가 넘긴 ⑤-2 부터)

2회차가 **그물 밖에 셋**을 남겨 놓았다(`tab:adv`·`tab:box`·`tab:shop`). 이 회차가 그것을 닫는다.

### ⓪ 재현 — 셋이 새던 이유는 «2.5%p»

세 화면은 03 던전 `#dunw` · 10 상점 `#shopw` · 89 유물 `#relw` 로, 전부 **z28 «(다) 전체화면 페이지»** 다.
실측(`fxRect`) — 셋 다 **1080×1996 @ y104**, 즉 프레임(1080×2280)의 **가로 100% · 세로 87.5%**.
2회차의 자는 «프레임의 90%×90%» 였으므로 **2.5%p 차이로 못 넘었다.**

못 넘은 이유가 중요하다 — 이 셋이 **안 덮는 두 띠는 상단 HUD(0..104)와 탭바(2100..2280)** 이고,
그 둘은 애초에 **전투 화면이 아니다**. 전투 화면(`#stagearea`)은 **104..2100** 이라 셋이 **100% 가린다**.

| 층 | z | 프레임 대비 | 전투 화면 대비 | 2회차 자 | 3회차 자 |
|---|---|---|---|---|---|
| `#modal`·`#collw`·`#blsw`·`#rkw`·`#bagw`·`#svw` … | 30~62 | 100×100 | **100%** | 덮음 | 덮음 |
| `#eqw`(02 영웅)·`#trw`(23 훈련) | 28·29 | 100×92.1 | **100%** | 덮음 | 덮음 |
| **`#dunw`·`#shopw`·`#relw`** | 28 | 100×**87.5** | **100%** | **·(구멍)** | **덮음** |
| `#tabbar` | 10 | 100×7.9 | **0%** | · | · |

### ① 처방 — 문턱이 아니라 «무엇의 90% 인가» 를 고쳤다

문턱을 87% 로 내리는 길은 **그 값이 어디서 왔는지 아무도 못 적는다**(다음에 84% 짜리 층이 나오면 또 내린다).
자의 **기준면**을 바꿨다 — `fxCovered()` 가 프레임이 아니라 **`#stagearea`(전투 화면)** 를 90% 이상 가리는
층을 찾는다. 추측 발원을 못 믿게 만드는 것은 «프레임이 가려짐» 이 아니라 «**전투 화면이 가려짐**» 이다
(안 가려진 전투 화면 위라면 그 코인은 갈 곳이 있다).

검산 — 종전에 세던 층은 **한 개도 안 빠진다**(위 표 1·2행 전부 100%) · `#tabbar` 는 종전처럼 **0%** 로 안 센다.
제품 변경은 `fxCovered()` **한 곳**이고, 타이밍·개수·소리·경로는 여전히 한 값도 안 바뀐다.

### ② ⚠ 넓히기 «전에» 해야 했던 것 — `probe518p` 93자리 O/X 표

2회차 ⑤-2 의 경고(«판정을 그냥 느슨하게 하지 마라 — 페이지 «안» 의 진짜 보상까지 같이 가려진다»)를
자로 확인했다. `tools/probe518p.js` **신설** — 세 페이지(서브탭 포함 7화면)의 **누를 수 있는 자리 93개**를
한 자리씩 **새 로드에서** 눌러 ① 재화를 주는가 ② `fxAt(요소)` 로 발원을 알려 주는가 ③ 어느 층인가 를 찍는다.

⚠ **1차 실행은 표가 통째로 오염됐다** — 배경 전투가 700ms 동안 골드를 벌고 킬마다 `fxAt(…,'combat')` 을
부르는 탓에 **93자리 전부가 «재화 O · 발원 O»** 로 찍혔다. 두 축을 갈라야 표가 선다:
· 재화 = **클릭 핸들러가 «같은 틱» 에 올린 몫**(수령·교환 경로는 전부 동기다)
· 발원 = **`src !== 'combat'` 인 fxAt 호출만**

**수리 전 표 — «재화 O + 발원 X» = 13자리(전부 10 상점 «재화» 탭)**

| 자리 | 주는 것 | 왜 발원이 없었나 |
|---|---|---|
| `[data-cnad]` ×4 (a1 보석 · a2 유물 · a7 스킨강화석 · a8 단련석) | dia+100 · relic+50 · stone+50 · tstone+50 | 수령 분기가 `fxAt` 을 안 부른다 |
| `[data-ex=rstone]`(490 §9 재화 교환 — 즉시 지급 갈래) | rstone+1 | `fxReward()` 만 부른다(그 함수는 인자 없이 부르면 **추측**을 쓴다) |
| `[data-dunex]` ×8(204 §10 입장권 교환) | 입장권 +1 | 같음 |

⇒ **넓히기 전에** 이 세 분기에 `fxAt(ex0)` · `fxAt(dx)` · `fxAt(b)` 를 달았다(제품 3자리).
달지 않고 자만 넓혔으면 **주인이 실제로 받은 보상 13자리가 페이지 아래로 묻혔을 것**이다 — 이 회차가
«쌩뚱맞은 연출» 을 잡으면서 «받았는데 안 보이는 연출» 을 새로 만드는 꼴이 된다.
수리 후 같은 표: **«재화 O + 발원 X» 0자리**.

곁다리로 확인한 것 — `#cnExch`(마일리지)와 `[data-ex=relic]` 은 **우편 지급**(153)이라 즉시 증가가 없다.
그 둘은 손댈 자리가 아니다.

### ③ 결과 — 전수 스윕 «전 화면 0»

`probe518s`(2회차 신설) 재실행:

| | 2회차 | 3회차 |
|---|---|---|
| 오프너 | 22 | 22 |
| **덮는 화면으로 잡힌 수** | **19** | **22** (adv·box·shop 편입) |
| **샌 화면** | 0 | **0** |
| 콘솔 에러 | 0 | 0 |

그 사이 배경 전투는 화면마다 **+8~12 골드**를 실제로 벌었고(연출을 죽인 것이 아니다), 그 연출은 전부 페이지 아래로 갔다.

### ④ 게이트 — `verify518` **21 → 32**

신설 11항: **[A7]** 자가 전투 화면을 기준으로 잰다 · **[A8]** 상점 지급 3경로가 발원을 알려 준다 ·
**[E1~E3]** 세 페이지가 «덮는 층» 으로 잡힌다 · **[F1~F3]** 그 화면에서 추측 발원 코인이 페이지 아래로만 간다 ·
**[G1·G2]** 그 화면의 **진짜 보상**(광고 상품 수령)은 **페이지 위**에서 난다 ·
**[R2]** 자를 «프레임 90%»(2회차 판)로 되돌린 사본에서는 10 상점이 다시 그물 밖이고 코인이 페이지 위로 간다.

⚑ **[R2] 가 이 회차의 «무르게 안 풀었다» 다** — [R](판정 자체를 끔)만으로는 «어차피 팝업이 다 잡았을 것» 과
구별이 안 된다. [R2] 는 **자의 기준면만** 되돌려 «이 회차가 실제로 새 화면을 편입했다» 를 못박는다.

**게이트 자신의 함정 넷을 잡느라 4번 고쳐 돌렸다** (전부 자 쪽 · 제품 아님):
1. **453 이 페이지 열기를 막는다** — 50킬이 차 보스전이 서면 `openDungeon()`·`openShopPage()` 가 통째로
   no-op 이라 [E]·[F] 셋이 동시에 빨개졌다. 씬을 **«파밍 대기»(`S.bossFarm = true`, 273 의 정상 상태)** 로
   두면 새 보스전이 안 선다 — 재는 축(배경 골드·발원·층)은 한 값도 안 바뀐다.
2. **`.on` 을 손으로 뜯으면 다음 열기가 opacity 0 에 갇힌다** — 60 쥬시가 `jz-o jz-pg` 인 채 멈춘다
   (실측: 500ms 뒤에도 opacity 0). 정리는 **제품 자신의 닫기 함수**(`closeDungeon`·`closeShopPage`·`closeRelw`)로.
3. **대기를 `fxCovered()` 로 걸면 안 된다** — 그 함수는 **프레임당 1회 캐시**라 앞 씬의 팝업이 남긴 `true` 를
   그대로 돌려주고 루프가 **0프레임에** 빠져나간다. 열림 대기는 **`opacity`** 로 건다(그래도 안 서면
   제품 손잡이 `jzHush()` 로 연출을 건너뛴다 — 96 서브탭 전환이 쓰는 그것).
4. **[R] 이 플레이키했다**(31/32 ↔ 32/32) — 클릭과 다음 `fxWatch` 프레임 사이에 킬이 하나 끼면 158 규약대로
   그 묶음의 발원이 **전투로 덮인다**. [B]·[C] 와 같은 규율(«탭이 이긴 시행이 나올 때까지 돌린다»)로 맞췄다.
   **연속 4회 32/32 로 확인**했다.

**회귀 무수정** — `verify58` 26/26 · `verify77` PASS · `verify93` 21/21 · `verify338` 181/181 ·
`verify350` 44/44 · `verify363` 80/80 · `verify364` 18/18 · `verify124` 68/68 ·
상점 계열 `verify204` 36/36 · `verify329` 26/26 · `verify365` 36/36 · `verify490` 33/33 · `verify153` 67/67 ·
**SMOKE PASS**.

⚠ **곁다리 등재 542** (등재 push 경쟁으로 531 → 541 → 542 번호 개정) — `verify158` 1건이 **플레이키**하다(«발원이 «누른 탭» 쪽이다»).
338·344·369·372 규칙대로 **수리 전 커밋(`ced75ff`)에서도 같은 항이 같은 식으로 흔들리는지** 직접 대조했다:
수리 전 **7회 중 2 PASS / 5 FAIL**, 수리 후 3회 전부 FAIL — **같은 병**이고 내 변경과 구조적으로도 무관하다
(그 항은 «발원까지의 거리» 를 재는데 이 회차는 `fxSrc`/`fxOrig` 우선순위를 한 줄도 안 건드렸다. 바꾼 것은
**층 판정의 기준면**과 상점 3분기의 `fxAt` 뿐이다). 상세는 PROGRESS 542 행.

### ⑤ 남은 것 — 이 행에서는 없다

1. ~~페이지형 3화면~~ — **이 회차로 닫혔다.** 게다가 자가 «전투 화면을 가리는가» 라는 **기하**로 바뀌었으므로
   앞으로 새 화면이 생겨도 **아무것도 안 해도 그물 안**이다(2회차까지는 «프레임 90%» 를 넘는 화면만 편입됐다).
2. **소리** — `fxFly` 의 `sfx('coin')` 은 층과 무관하게 난다. **주인 판단 대기**(2회차와 같음) — 끄면
   58/93/98 이 재는 축이 바뀐다. 임의로 고치지 마라.
3. **`FXCUR`·`fxReward` 색축** — **512 몫**(유물조각·강화석·룬강화석·단련석·마일리지는 지금도 비행 연출이 0건이다.
   `probe518p` 표의 a2·a7·a8 행이 그 증거 — 재화는 주는데 `#fxl` 에 코인이 한 개도 안 난다).

### ⑥ 시도했다가 접은 것(3회차 · 다음 세션이 다시 하지 않게)

«오프너 순회» 대신 **`#app` 직속 층 30개를 하나씩 켜서 «기하 ↔ 자» 를 대조하는 전수 census» 를 짜 봤다.
**못 쓴다** — 이 오버레이들의 가시성 스위치는 `.on` **클래스가 아니라** 각자의 열기 함수가 만드는 상태다
(`.on` 만 붙이면 30개 중 29개가 `display:none` 그대로다). 결국 «오프너를 부르는 것» 과 같은 일이 되므로
`probe518s`(§1)와 `probe518p` 두 자로 충분하다.

---

## 4회차 (2026-08-31, sess-0322-17083 워커 F — «전 화면» 을 진짜 전 화면으로 · 비평가 2인 첫 투입)

3회차가 «⑤ 남은 것 — 이 행에서는 없다» 로 닫아 두었지만, 등재문이 요구한 **비평가 2인**은 아직
한 번도 안 돌았고 스윕의 그물도 실은 절반이었다. 이 회차는 그 둘을 동시에 연다.

### ⓪ 자부터 다시 봤다 — «전 화면 0» 은 실은 «화면의 41% 에서 0» 이었다

2·3회차의 `probe518s` 는 오프너를 **자기가 모은다**(탭 5 · 사이드 6 · 메뉴 + 8칸 · 재화 알약 3 = **22개**).
그 그물에 **한 번도 안 걸린 화면이 32개**다 — 03 던전/탑/레이드 서브탭 · 23 훈련/룬/단련 ·
10 상점 구획 3 · 08 영웅 시트 4(스킬·펫·코스튬) · 21 도감 카테고리 6 · 22 퀘스트 2 · 35 패스 5 ·
19/20 프로필 · 56 절전 · 05 장비 세부 3.

⇒ `tools/probe518t.js` **신설**. 목록을 새로 적지 않고 **351 의 오프너 수집기**
(`probe351lib.collectOpeners` — 351 이 8·10·12·13·14회차에 걸쳐 «제품에게 묻도록» 고쳐 놓은 그것)를
그대로 받아 **54화면**을 돈다. 게이트 **[A9]** 가 이 재사용을 못박는다(손으로 적은 목록이 다시 생기면 빨강).

**자 자신의 함정 둘을 먼저 잡았다**(둘 다 «조용한 초록» 이라 더 위험하다):

| # | 무엇 | 왜 초록으로 읽혔나 | 처방 |
|---|---|---|---|
| ⓐ | 3회차가 편입한 89 유물(`#relw`)이 **덮음 ·** 으로 찍혔다 | `drive()` 의 450ms 로는 60 쥬시 개봉이 안 끝나 그 순간 **opacity 0** | `settle()` + 400ms 뒤에 잰다 |
| ⓑ | **54화면 중 한 곳도 안 눌렀다**(탭 못 함 54) | «화면» 을 고를 때 z 가 제일 큰 층을 골랐는데 그것이 **연출 층 자신 `#fxl`(z60)** | 층 목록에서 `fxl`·`fxlc`·`top`·`tabbar`·`stagearea` 제외 + 요약에 «탭 못 한 화면 n» 을 찍는다 |

ⓑ 를 고친 뒤에도 28화면이 «탭 못 함» 이었다(내 후보 선택자에 안 걸리는 화면들). `fxTapEl` 은
**아무 노드나** 받으므로(34438 — `closest(…) || t`) 폴백을 **그 화면 한복판의 `elementFromPoint`** 로 두어
**탭 못 한 화면 28 → 7**(남은 7은 한복판이 빈 화면). 스윕 결과는 세 실행 모두 같다:

| | 2·3회차(`probe518s`) | 4회차(`probe518t`) |
|---|---|---|
| 화면 | 22 | **54** |
| 덮는 화면 | 22 | **51** |
| **샌 화면** | 0 | **0** |
| 콘솔 에러 | 0 | 0 |

### ① 그런데 «눈» 이 자가 못 보던 자리를 잡았다 — 07 스킬 시트

지시서 [3]-(다) 대로 `tools/cap518.js` **신설**(장면 4개 × 100ms 간격 8프레임)해서
**비평가 CX·CY 2인 독립** 채점을 붙였다. 장면은 주인이 말한 두 자리(A 12 소환 결과 · B 09 일괄 강화 결과)와
D 21 도감 [일괄 강화], 그리고 4회차가 처음 본 **C 07 스킬 시트**다.

| 장면 | CX | CY |
|---|---|---|
| A 소환 결과 | PASS(0/0/0/0) | PASS |
| B 일괄 강화 결과 | PASS | PASS |
| **C 07 스킬 시트** | **FAIL — f1 금화 5개** `x142..277 / y811..961`, «+ 슬롯» 버튼(228,872)을 완전 은폐 · f2 3개 `x948..1075` | **FAIL — f1** `(136,806)–(278,966)` 142×160px 금화 더미 · f2 `(992,724)–(1078,968)` 3매가 «St.80» 을 가림 |
| D 도감 | PASS | PASS |
| **점수(①~④ 최저)** | **3/10**(③ 위치·경로 3) | **2/10**(③ 2) |

두 사람이 **좌표까지 일치**했고, 둘 다 딤 계수로 층을 갈랐다 — A·B 는 피크 51/184 = 0.28,
D 는 0.65 로 **딤 아래**, C 만 **(254,237,177) = 딤 0 = 시트 위**.
⇒ 주인이 말한 그 그림(«누른 버튼에서 금화가 튄다»)이 **한 화면에 그대로 남아 있었다.**
하필 404 가 적어 둔 그 시트, 즉 **[스킬 소환]·[일괄 강화] 가 사는 화면**이다.

### ② 왜 자는 못 봤나 — «덮는 층» 은 다 가리는 것만 센다

07 스킬·26 펫·50 코스튬 시트는 `#panel`(**z9**)이고 실측 **1080×1484 @ y616** =
전투 화면(104..2100)의 **74%** 다. 3회차가 세운 «전투 화면의 90%» 를 못 넘으므로
`fxCovered()` 는 **false**(게이트 [H1] 이 이 사실을 못박는다) → 추측 발원이 그대로 이겨 코인이 `#fxl` 로 갔다.

⚠ **문턱을 74% 로 내리는 길은 518-⑥ 이 막아 뒀다** — 그 값의 근거를 아무도 못 적고, 다음엔 60% 짜리가 온다.

### ③ 처방 — «얼마나 가리나» 가 아니라 «누가 그 자리를 소유하나»

제품 **2자리**(`fxOverlaid()` 신설 + `buried` 한 줄):

```
const buried = !combat && !!(from && from.tap) && (fxCovered() || fxOverlaid(from.el));
```

`fxOverlaid(el)` = «추측한 발원이 얹힌 `#app` 직속 층이 **전투 화면 한복판을 가지고 있는가**».
가지고 있으면 그 코인은 그 층 **위**에 그려질 것이므로 묻는다.

* 문턱이 **없다** — 점 하나의 포함 여부다.
* 화면 가장자리 크롬(사이드 레일 · 탭바 · 상단 HUD)은 한복판을 **안 가지므로** 한 값도 안 바뀐다
  ⇒ 메인 화면의 UI 발(58·93)은 종전 그대로 `#fxl`(게이트 [D2] 무수정 초록).
* 화면이 `fxAt(요소)` 로 **알려 준** 보상은 애초에 `from.tap` 이 아니므로 그대로 위에서 난다([C2]·[G2]).
* 타이밍·개수·소리·경로는 여전히 **한 값도 안 바뀐다**(전부 `combat` 만 본다).

실측(같은 시나리오 3회씩) — 수리 전 `["fxlc","fxl"]`·`["fxl"]` ↔ 수리 후 **3회 전부 `["fxlc"]`**.

### ④ 게이트 — `verify518` **32 → 38/38**

신설 6항: **[A9]** 스윕이 화면 목록을 제품에게 묻는다 · **[H1]** 07 스킬 시트는 «덮는 층» 이 **아니다**
(이 항이 초록이어야 [H2] 가 «새 자리» 를 재는 것이다 — 341 [전제] 절과 같은 규율) ·
**[H2]** 그래도 그 시트 안을 누른 뒤의 추측 발원 코인은 시트 아래 · **[H3]** «+n»·알약 복제도 같이 ·
**[R3]** `fxOverlaid` **만** 끈 사본에서 그 코인이 다시 시트 위로 간다.

⚑ **되돌림 두 개를 같이 고친 것이 이 회차의 «무르게 안 풀었다» 다** — 판정이 둘이 되면서
[R](판정 전체 끄기)과 [R2](자를 2회차 판으로)가 **`fxOverlaid` 하나 때문에 초록으로 읽혔다**
(팝업·페이지도 «한복판을 가진 층» 이라 새 판정이 대신 잡아 준다). 둘 다 **두 판정을 함께** 되돌리게 고쳤고,
그러자 [R] `["fxl"]` · [R2] `["fxl","fxlc"]` 로 수리 전 그림이 정확히 되살아난다.
[R3] 은 반대로 **새 판정만** 꺼서 «이 회차가 실제로 새 자리를 편입했다» 를 못박는다.

### ⑤ 4회차 결과

| 자 | 값 |
|---|---|
| `probe518t` 전수 스윕(수리 후) | 화면 **54** · 덮는 화면 **51** · **샌 화면 0** · 콘솔 0 |
| `verify518` | **38/38**(32 → 38, 신설 6항) |
| `tools/smoke.js` | **SMOKE PASS**(제품 변경 뒤 재실행) |
| 회귀 **무수정 10종** | `verify58` 27/27 · `verify77` PASS · `verify93` 21/21 · `verify158` PASS · `verify338` 181/181 · `verify363` 80/80 · `verify364` 18/18 · `verify512` 31/31 · `verify543` 23/23 · `verify569` 16/16 |

(58·93·77·158 은 이 부품의 타이밍·개수·경로·발원 규칙을 재는 자들이고 543·569 는 **개수**를 재는 자들이다 —
한 항도 안 고쳤다는 것이 «층만 내렸다» 의 가장 짧은 증거다.)

---

## 5회차 (2026-08-31, sess-0322-17083 워커 F · 4회차와 같은 세션) — 수리 후 재캡처·재채점

같은 `cap518.js` 로 **같은 네 장면을 다시 찍고**(`518-r4b-*`) **새 비평가 2인(CZ·DA)** 에게 독립으로 줬다.
앞 회차 지적 좌표 두 창을 **명시해서** 확인시켰다(자·눈이 같은 자리를 봐야 대조가 선다).

| 장면 | CZ | DA |
|---|---|---|
| A 12 소환 결과 | PASS(0/0/0/0/0) | PASS |
| B 09 일괄 강화 결과 | PASS | PASS(8프레임 재화금색 화소 **37,241 고정 · 변동 0px**) |
| **C 07 스킬 시트** | **PASS** — 시트 밴드(y616–2098) 재화색 화소 5프레임 **2082 동일 · 전이 0px**(= 정적 아트) | **PASS** |
| D 21 도감 | PASS | PASS |
| 점수(①~④ 최저) | **6/10**(④) | **8/10**(④) |

⚑ **DA 가 앞 회차 두 창을 픽셀로 대조해 «되돌아왔다» 를 못박았다:**

| 창 | r4 f1 | r4 f2 | **r4b f1** | **r4b f2** | 기준선(정적 아트) |
|---|---|---|---|---|---|
| L `x136..278 / y806..966` | **7792 px** | 2118 | **2119** | **2118** | 2118 |
| R `x948..1078 / y724..968` | 399 | **8736 px** | **399** | **399** | 399 |

⇒ f1 의 원색 금화 **+5674px**, f2 의 **+8337px** 가 **정확히 기준선으로 소멸**했다.

⚑ **«죽인» 것이 아니라 «내린» 것임도 픽셀이 말한다**(DA) — A f2 코인이 배너 상변 **y534 에서 절단** ·
B f2·f5 코인이 배너(y930..1023) 뒤로 사라짐 · D f2 코인이 팝업 우변 **x980 에서 잘림**.
딤 천장(장면별 최대 채널값 51 / 51 / 184 / 120)을 **1픽셀도 안 넘는다** = 전부 스크림 아래.

### ① 남은 감점 2건은 이 행 것이 아니다 — 곁다리 등재

두 비평가의 ④ 감점 사유가 **같다**. 둘 다 «이번 회차의 리그레션이 아니라 4장면 공통 기저 동작» 이라고 적었다:

1. **`+n` 플로트가 두 줄 겹쳐 인쇄된다** — C f3 `x688..793 / y163..189` **24행 안에 두 값이 덧찍혀**
   «+54.1A» 로 읽힌다(실제는 `+4.1` 과 `+54.3A`) · D f3 도 `y100..169 / y170..200` 로 맞닿는다.
   덧붙여 **플로트 값 ↔ 알약 델타가 서로를 설명하지 못한다**(A f3 알약 14.8A ↔ 플로트 +54.3A).
   ⇒ 58·93·512 가 소유한 «묶음/플로트» 축이다. **575** 로 등재.
2. **스크림(딤) α 가 화면마다 다르다** — A·B 0.80(천장 51) · D 0.53(120) · **C 0.28(184)**.
   같은 «딤 아래» 가 C 에서만 3.6배 밝게 통과한다(= 4회차에 C 만 눈에 띈 이유이자, 재발하면 제일 늦게 보일 구간).
   ⇒ A5·60 이 소유한 팝업 껍데기 축이다. **576** 으로 등재.

이 행이 지키는 축(«재화를 안 받았는데 재화 획득 연출이 팝업/시트 «위» 에 뜨는가»)은
**자 54화면 0 · 눈 2인 × 20프레임 0** 으로 닫혔다. 위 둘을 이 행에서 고치면 58/93/A5/60 의 축을 이 세션이 임의로 흔드는 것이 된다.

### ② 남은 것 — 주인 판단 대기 1건(3회차와 같음)

* **소리** — `fxFly` 의 `sfx('coin')` 은 층과 무관하게 난다. 코인이 안 보이는데 «촤르륵» 만 들리는 것이
  «효과가 난다» 로 읽히는지는 **주인 판단**이다(끄면 58/93/98 이 재는 축이 바뀐다 — 임의로 고치지 마라).
