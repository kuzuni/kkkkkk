# 518 — 재화를 얻지 않았는데 «골드 획득» 연출이 터진다 (전수 루프)

> 주인 보고(2026-08-31, 재차·강한 어조): «소환결과에서 골드를 안얻는데 골드얻는 효과나옴 …
> 자꾸 **장비 일괄강화**할때도 골드 얻는 이펙트 뜨고 … 쌩뚱맞는곳에서 골드이펙트 터지는거 싹다 잡아»

세션 `sess-1838-12782` 워커 B · **1~2회차**(계속 진행 중 — lock 해제, 다음 워커가 이어서)

---

## 1회차 (2026-08-31)

### ⓪ 338 규칙 — 처방 전에 재현부터

`tools/probe518.js` 신설. 등재문의 일곱 갈래(ⓐ~ⓖ)를 각각 실측했다.

| 갈래 | 등재문 가설 | 재현 결과 |
|---|---|---|
| ⓐ | 소환은 골드를 주지 않는다 | **확인** — 10회 소환 `S.gold` 델타 **0** |
| ⓑ | 일괄 강화는 골드를 «쓰는» 자리다 | **정정** — 장비 일괄 강화는 골드를 **쓰지도 주지도 않는다**(델타 0 · 156단계). 재료는 **조각**이다 |
| ⓒ | `fxUpOk` 앰버 `#FFC02E` 가 골드색과 겹친다 | **기각(이 자리 아님)** — ΔE(`#FFC02E` ↔ `#FFDE6A`) = **21.2** 로 겹치지 않고, 애초에 **장비 일괄 강화는 `fxUpOk` 를 부르지 않는다**(호출부 9곳 중 하나도 이 경로가 아니다) |
| ⓓ | `fxReward` 상수 크림 | **512 몫** — 이 행에서는 안 건드렸다 |
| ⓔ | 팝업 위 레이어로 온다 | **확인 — 이것이 본체** |
| ⓕ | 발원이 «마지막으로 누른 버튼» 으로 찍힌다 | **확인 — 이것이 본체** |
| ⓖ | `fxPay` 와 겹친다 | 해당 없음(골드를 안 쓴다) |

### ① 뿌리 — «추측한 발원» 이 팝업 위로 올라간다

`fxSrc`(33539)는 발원을 두 힌트 중 **더 최근** 것으로 고른다:
`fxAt(…, 'combat')`(창 600ms, 화면이 «여기서 났다» 고 알려 준 자리)과
`fxTapEl`(창 1200ms, «마지막으로 누른 버튼» = **추측**).

77 이 «전투 발 코인은 `#fxlc`(z7 · 팝업 아래)» 를 세워 뒀지만 그 규칙은 **표시가 붙은 증가분에만**
적용된다. 표시가 없으면 추측이 이기고, 추측은 «UI 발» 이라 `#fxl`(**z60 = 모든 팝업 위**)에 그려지며
딤 위 밝은 알약 복제(`fx-lit`)까지 켠다.

**실측(수리 전)** — 소환 결과 팝업 안 카드를 누른 직후 `fxSrc()` 가 **0프레임 만에** 그 카드를
발원으로 돌려줬고, 그 뒤에 들어온 배경 전투 골드가 **코인 8~16개를 `#fxl` 로**, `+n` 도 `#fxl` 로 쐈다.
⇒ 주인이 본 그림 그대로다: **내가 방금 누른 [일괄 강화]·소환 카드에서 금화가 튀어나온다.**

**표시가 빠진 자리도 실재한다**(정적 스캔) — `S.gold +=` 11자리 중 전투 수입 두 곳
(`22291` 스테이지 클리어 보너스 · `22316` 파도 전멸 보너스)이 `fxAt` 을 **한 번도 안 불렀다**.
게다가 475 가 격파와 클리어 보너스 사이에 **1초 홀드**를 넣은 뒤로는 `killEnemy` 의 전투 힌트(600ms)가
그 골드가 들어올 때 **이미 만료**돼 있어, 이 두 자리는 **거의 언제나** 추측으로 넘어간다.

> ⚠ **레이어만으로는 못 가른다** — 팝업 «안» 카드를 누른 경우 그 노드는 가려져 있지도 않다.
> 1회차 초안이 «가려진 자리인가» 로 짰다가 이 케이스에서 그대로 새는 것을 재현으로 확인하고
> 판정을 **«아는 발원인가 추측인가»** 로 옮겼다.

### ② 처방 — 제품 4자리

1. **`22291`·`22316` 에 전투 발원 표시를 단다** — `fxAt(fxWorld(player.x, player.y − player.r), 'combat')`.
   이 골드는 전투 수입이므로 코인이 전장에서 나는 것이 맞고, 77 규칙에 자동으로 편입된다.
2. **`fxSrc` 의 탭 폴백에 «추측» 표시를 붙인다** — `{ x, y, el, tap:true }`.
3. **`fxFly` — «덮는 층이 떠 있는데 발원이 추측» 이면 그리는 층만 내린다**
   (`const buried = !combat && !!(from && from.tap) && fxCovered()`).
   · 코인 층 `#fxl` → `#fxlc` · `+n` 도 같은 층(`batch.combat = combat || buried`) · 딤 위 알약 복제 생략.
   · **타이밍·개수·소리·경로는 한 값도 안 바꿨다** — 아래 계산은 전부 `combat` 만 본다.
4. **`fxCovered()` 신설** — `#app` 직속 층 중 z > 7 이고 화면을 90% 이상 덮는 것이 떠 있는가.
   프레임당 1회만 재고 캐시한다(`fxSc`/`fxStageTop` 과 같은 관례 — 레이아웃 스래싱 방지).

**무르게 풀지 않았다** — 화면이 `fxAt(요소)` 로 **알려 준** 보상(우편 27033 · 퀘스트 27175 ·
룰렛/상자 24572 · 24719 · 패스 26921 · 32721)은 추측이 아니므로 **종전 그대로 팝업 위에서** 난다
(`verify518` [C2]). 메인 화면의 UI 발도 한 값도 안 바뀐다([D2]).

### ③ 곁다리로 확인한 것

* **소환 결과 팝업 아래의 코인은 «보이지 않는다»** — 심어 둔 코인 55×55 를 `#sumw`(z37, 딤 .80)
  아래에 두고 같은 자리 2장을 대조하니 **바뀐 픽셀 0/4489**. 즉 층을 내리는 것으로 «안 보이게» 는 충분하다.
  ⚠ 첫 대조에서 4256px 이 바뀌어 «보인다» 로 읽힐 뻔했는데, 그것은 **카드 등장 연출이 아직 도는 중**
  이어서였다(2.2초 정착 뒤 재측정 → 0). 캡처 대조는 **연출이 멎은 뒤에** 해야 한다.
* **닫힘 연출 중에도 팝업은 «화면에 있다»** — `.on` 을 떼면 60 쥬시가 `jz-db jz-c jz-pg` 로 바꿔
  `display:block` 인 채 opacity 를 떨어뜨린다(실측 0.29). `fxCovered()` 는 그 구간을 **덮는 층으로 센다** —
  옳은 동작이다(아직 화면에 있다). 게이트가 «팝업 없음» 을 재려면 그 연출이 끝날 때까지 기다려야 한다.

### ④ 게이트

`tools/verify518.js` **신설** — [A] 정적 5항 · [B] 팝업+추측 8항 · [C] 팝업+아는 발원 2항 ·
[D] 메인 회귀 3항 · [X] 콘솔 · **[R] 되돌림 시험**(`fxCovered` 를 «항상 false» 로 묶은 사본에서
코인이 다시 `#fxl` 로 간다 = 수리 전 그림).
`tools/probe518.js` 신설(측정 전용 — 수리 전/후 같은 명령으로 대조).

**결과** — `verify518` **21/21 신설** · `probe518` **7/7 신설** · **SMOKE PASS** ·
회귀 무수정 8종: `verify58` 26/26 · `verify77` PASS · `verify93` 21/21 · `verify158` PASS ·
`verify338` 181/181 · `verify350` 44/44 · `verify363` 80/80 · `verify364` 18/18.
(58·93·77·158 은 이 부품의 타이밍·개수·경로·발원 규칙을 재는 자들이다 — **한 항도 안 고쳤다** 는 것이
«층만 내렸다» 의 가장 짧은 증거다.)

## 2회차 (2026-08-31) — 전수 스윕

`tools/probe518s.js` **신설**. 오프너(탭 5 · 사이드 6 · 메뉴 + 8칸 · 재화 아이콘 3 = **22개**)를 돌며
화면마다 «그 화면이 재화를 준 적이 없는데 `#fxl`(팝업 위)에 재화 연출이 뜨는가» 를 센다.
각 화면에서 **탭 → 2.5초 관찰** 순서로 재는데, 이것이 주인이 겪은 순서 그대로다.

**결과: 덮는 화면 19개 중 «샌 화면» 0개 · 콘솔 에러 0.** (배경 전투는 그동안 계속 골드를 벌었다 —
화면마다 +8~12 골드가 실제로 들어왔고, 그 연출은 전부 팝업 아래로 갔다.)

⚠ **그물 밖이 셋 남았다** — `tab:adv`·`tab:box`·`tab:shop` 이 «덮음 ·» 으로 찍혔다.
실측하니 `dunw`·`relw`·`shopw` 는 **`#app` 직속 · z28 · display:block** 인데 **상단 HUD 를 안 덮어**
`fxCovered()` 의 «세로 90%» 를 못 넘는다. 이 셋은 다음 회차 몫이다(아래 ⑤-1).

### ⑤ 남은 것 (다음 회차 — 이 행은 «전수 루프» 다)

주인 지시가 «전 화면 0 까지» 이므로 **이 회차로 닫지 않는다.** 다음 회차가 할 일:

1. **오프너 전 화면 순회**(351 `cap351.js` SET1~SET3 목록) — 화면마다 «재화를 안 받았는데 재화색
   연출이 뜨는 자리» 를 세는 스윕(`probe518s`)을 만들어 **비평가 2인**에게 준다.
2. **페이지형 화면 3개**(`dunw`·`relw`·`shopw` z28)를 그물 안으로. ⚠ **판정을 그냥 느슨하게 하지 마라** —
   그 페이지 «안» 의 진짜 보상(상점 구매 다이아처럼 **탭 폴백에 기대는** 경로)까지 같이 가려진다.
   **먼저 그 경로들에 `fxAt(요소)` 를 달아 «아는 발원» 으로 만든 뒤에** 넓혀야 안전하다(LESSONS 518-⑤).
3. **소리** — `fxFly` 의 `sfx('coin')` 은 층과 무관하게 난다. 팝업 위에서 코인이 안 보이는데 «촤르륵» 만
   들리는 것이 «효과가 난다» 로 읽히는지 주인 판단 필요(이 회차에서는 **안 건드렸다** — 소리를 끄면
   58/93/98 이 재는 축이 바뀐다).
4. **512 와 겹치는 축**(`FXCUR`·`fxReward` 색) — 512 가 잡는다.
