# 333 — `tools/verify149.js` 6건 실패 · 게이트 부패 («승급 조건 미달» 은 295 가 지웠다)

작업 종류: **T1 버그(게이트 부패)** — ROUTINE [3]-(가) 기계적/게이트 계열이라 **비평가는 띄우지 않았다.**
1회차로 종료. 제품(`index.html`) **0줄** — 등재문의 예상 그대로 게이트만 썩어 있었다(319 선례와 같은 꼴).

담당: 2026-08-28, `sess-1326-25814` 루틴 워커 A.

---

## 1회차 (2026-08-28)

### 재현 (착수 기준선)

```
node tools/verify149.js
…
실패 항목:
  · §1 토스트 — 승급 조건 미달  — 조각을 못 찾음
  · §1 토스트 전환 50/51
  · §2 승급 조건 미달  — 토스트 0 · 모달 off      ← 프레임 4종에서 각 1건
  · §2 승급 조건 미달  — 토스트 0 · 모달 off
  · §2 승급 조건 미달  — 토스트 0 · 모달 off
  · §2 승급 조건 미달  — 토스트 0 · 모달 off
VERIFY149 272/278 FAIL
```

실패 6건은 **두 케이스**다 — ⓐ §1 정적(조각 + 전환 요약) ⓑ §2 실동작이 `HEIGHTS` 4종에서 네 번 잡히는 것.

### 뿌리

세 자리가 전부 **`startPromo()` 의 «승급 조건 미달» 반려**를 찾는다.

| 자리 | 무엇을 찾고 있었나 |
| --- | --- |
| `verify149.js:62` | §1 조각 표 `['승급 조건 미달', '승급 조건 미달']` |
| `verify149.js:162` | §3 폭 표본 `D => "🏅 " + D.rank + " 승급 조건 미달"` |
| `verify149.js:244` | §2 실동작 `run('승급 조건 미달', () => { S.best = 0; startPromo(); })` |

그 반려는 **295(승급전 «입장 제한 폐지», 2026-08-27 저장소 주인 지시)가 삭제**했다. 지금 `startPromo()` 에
남은 가드는 «최고 계급이라 도전할 것이 없다»(`!r`)와 «이미 승급전 중»(`promo`) 둘뿐이고 **둘 다 조용히
`return`** 한다 — 토스트가 0인 것이 정상이다(`index.html:22183~22187`).

그리고 `verify295` 는 **그 문구가 «없어야»** 통과한다(«[0] `startPromo()` 에 «승급 조건 미달» 반려가 없다»).
즉 **두 게이트가 서로 반대를 단언**하는 상태였다. 제품을 고치면 `verify295` 가 깨지므로,
고칠 곳은 제품이 아니라 `verify149` 다.

### 처방 — 자리를 «비우지» 않고 **살아 있는 승급 계열 토스트**로 갈아 끼웠다

살아 있는 승급 계열 토스트는 셋뿐이다(`index.html` 전수):

| 자리 | 문구 | 149 표에 있었나 |
| --- | --- | --- |
| `22257` `endPromo(false)` | `💀 승급 실패 — 시간 안에 <b>승급 수호자</b>를 못 잡았습니다` | 있음(`206 승급 실패`) |
| `27247` 코스튬 [착용] | `🔒 승급전에서 획득해야 착용합니다` | 있음(`미보유 코스튬`) |
| `27260` 코스튬 [강화] | `🔒 승급전에서 획득해야 강화합니다` | **없음 ← 여기로 갈아 끼웠다** |

- **§1(조각) · §2(실동작)** → `코스튬 미보유 강화` 로 교체. 표본 수가 그대로다(§1 **51/51**).
  덤으로 **149 가 한 번도 안 보던 notify 자리**를 그물에 넣었다.
- **§3(폭)** → 항목을 **지웠다**. 살아 있는 승급 토스트 셋 중 **계급 이름이 들어가는 것이 하나도 없어**
  «가장 긴 이름» 표본 자체가 없다. 재료 `D.rank`(329행)도 같이 지웠다 —
  **310 의 «가방 칸»(`D.bag`) 처리와 같은 규칙**이다(죽은 재료를 남기면 다음 세션이 «그 토스트가 아직 있다» 고 읽는다).

### ⚠ 갈아 끼우다 발견한 것 — §2 표본이 «분기를 베껴 쓰고» 있었다

첫 판은 이웃한 243행(`코스튬 — 미보유 착용`)을 그대로 베껴 `if (!cosUpgrade(…) && !cosOwn(…)) notify(…)` 로 썼다.
되돌림 시험을 돌리자 **§1 만 빨개지고 §2 는 초록이었다** — 게이트가 자기가 부른 `notify()` 를 세고 있었으니
당연하다. 그건 «notify() 가 살아 있다» 만 재는 표본이다.

그래서 §2 를 **실제 버튼 클릭**으로 바꿨다 (`#bCos` 위임 리스너를 그대로 탄다):

```js
cosSel = a.id; renderCos();
const b = document.querySelector('#bCos [data-cosup]');
b.click();
```

### 되돌림 시험 (게이트에 이빨이 있는가)

`index.html:27260` 의 `notify(...)` 를 `popup('알림', ...)` 로 되돌린 트리에서:

```
실패 항목:
  · §1 토스트 — 코스튬 미보유 강화  — 아직 popup()
  · §1 토스트 전환 50/51
  · §2 코스튬 — 미보유 강화  — 토스트 0 · 모달 ON(← 아직 팝업)   ← 프레임 4종
  · …
VERIFY149 268/274 FAIL
```

**6건**이 빨개진다 — 부패 전 표본이 잡던 것과 **정확히 같은 모양**이다(§1 2건 + §2 4건).
시험 뒤 `index.html` 은 원상 복구했다(`git diff --stat index.html` = 변경 0).

### 검증

| 게이트 | 결과 |
| --- | --- |
| `node tools/verify149.js` | **273/273 PASS** (기준선 272/278 FAIL) |
| `node tools/verify295.js` | **25/25 PASS** — 문구를 제품에 되살리지 않았다 |
| `node tools/smoke.js` | **SMOKE PASS** |

총 단언 수가 278 → 273 으로 준 내역: §1 실패 ck 1건이 사라지고(통과 항목은 요약 한 줄로만 찍힌다),
§3 «승급 조건 미달» 폭 항목 1건 × 프레임 4종 = 4건이 빠졌다. **줄어든 것은 전부 «잴 것이 없어진» 자리**이고,
§1·§2 표본 수는 그대로다.

### 제품 변경

**0줄.** `index.html` 은 되돌림 시험 동안만 건드렸고 원상 복구했다.
