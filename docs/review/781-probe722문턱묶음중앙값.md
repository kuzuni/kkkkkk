# 781 — `probe722` [6-b]·[6-c] 도 «최댓값 하나 + 손 문턱» 이었다 (775·778 과 같은 자리·같은 처방)

- **작업**: T1 «버그(게이트 플레이키)» · 지시서 [3]-**(가)** 기계적 작업 ⇒ **비평가 안 띄움**
- **세션**: sess-0043-1891 루틴 워커 C · **2회차**(1회차 = 처방 · 2회차 = 내가 심은 새 동전 수리)
- **제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정·밸런스 0줄** — 만진 파일은 `tools/probe722.js` 하나
- **등재문**: PROGRESS 781 행(778 §5 곁다리 관측 · sess-2328-6713 워커 B)

## §1 재현 — 등재문 그대로다

착수 직후 **수리 전 트리 무경쟁 5회**(직렬 · 다른 자 동시 실행 0):

| 회 | `[6-b]` `inflWorst` (바 > 1.5) | `[6-c]` `spreadWorst` (바 < 0.6) | 자유 판 `worst.w` | 총점 |
|---|---|---|---|---|
| #0 | ×1.73 (여유 0.23) | **65% ← 빨강** | 155% | **20/21** |
| #1 | ×1.59 (여유 0.09) | 56% (여유 0.04) | 139% | 21/21 |
| #2 | ×1.66 (여유 0.16) | 50% | 208% | 21/21 |
| #3 | ×1.64 (여유 0.14) | 46% | 107% | 21/21 |
| #4 | ×1.62 (여유 0.12) | **62% ← 빨강** | 129% | **19/21** |

**`[6-c]` 는 1회차에 곧바로 재현됐다** — 5회 중 **2회 빨강**(등재문의 «7회 중 1회» 보다 오히려 잦다).
`[6-b]` 는 이 5회에서는 안 터졌지만 **여유가 0.09~0.23** 으로 등재문의 실측 분포
(×1.49·1.54·1.55·1.61·1.62·1.67·1.99 · 빨강 1회)와 같은 자리에 앉아 있다 —
**우연히 안 터진 것**이다(338·344 규칙). 빨강을 정하는 종도 실행마다 갈렸다
(#0 poison · #1 flask · #2 poison · #3 flask · #4 flask).
⚠ #4 의 나머지 빨강 1건은 **이 작업의 확정 대상 밖**이다(등재문이 «부하에서만 본 것» 으로 따로 적어 둔
`[1-c]`·`[3-c]` 계열 · 771 «부하에서 갈린다»).

## §2 뿌리 — 두 항 다 «빨강을 표본 하나가 정한다»

### ⓐ `[6-b]` = `inflWorst > 1.5` — 775 `[C2]` · 778 `[8-b]` 와 **글자 그대로 같은 모양**

`inflWorst` 는 6종의 부풀림 배수 중 **최댓값 하나**다. 그 종은 실측에서 `flask` ↔ `poison`
사이를 오간다(#0 poison · #1 flask · #2 poison · #3 flask) — **이름조차 못 쓴다**
(775 §4 «분포가 겹치면 최솟값의 이름이 갈린다»의 거울상).

### ⓑ `[6-c]` = `spreadWorst < 0.6` — 여기에 **한 겹이 더 있다**

왼쪽이 18 표본(3회 × 6종)의 **최댓값**인 것은 ⓐ 와 같은 병이고, 그보다 큰 것은
**항의 문장과 코드가 서로 다른 것을 본다**는 점이다:

- 문장 — «채택 눈금의 K회 폭은 **[C] 자유 판의 실행 간 폭보다** 좁다»
- 코드 — `spreadWorst < 0.6` ⇒ **자유 판 폭(`worst.w`)을 한 번도 안 본다**

`worst.w` 는 실측 **107~208%** 로 실행마다 두 배씩 움직이는데 바는 손 상수 0.6 에 못 박혀 있었다.
판이 시끄러운 실행에서는 양쪽이 같이 커지지만 바만 제자리다 —
778 §7 «절대 문턱을 못 쓰는 자리에서는 그 실행이 스스로 잰 널에 대어 재라» 가 그대로 적용되는 자리.

### ⓒ 문턱을 다시 뽑는 길은 여기서도 답이 아니다

695-④ · 759 · 766 · 775 · 778 이 **다섯 번** 기각했다. 0.6 → 0.7 은 다음 꼬리에서 같은 얼굴로 돌아온다.

## §3 처방 — 333 처방대로 자리를 비우지 않고 **통계의 모양을 바꿨다**

### `[6-b]` — 최댓값 → **묶음 중앙값의 비** + 그 실행이 잰 널

- `upMed` = 오르는 묶음(배수 > 1)의 중앙값 · `dnMed` = 내리는 묶음의 중앙값 · `gap = upMed / dnMed`
- 바 두 겹 — ① `gap ≥ 1.5`(778 `[8-b]` 와 같은 눈금) ② `(gap − 1) ≥ 2 × (nullGap − 1)`
- `nullGap` = **같은 `P6` 안의 실제 판끼리** 나눈 짝(하네스 0 · 새 실행 0)을 같은 묶음 모양으로 낸 비
- «묶음이 둘로 갈린다» 자체는 바로 위 `[6-a]` 가 이미 지므로 여기는 **갈림의 크기**만 진다
- **문턱은 내린 게 아니라 올랐다** — 최댓값 1.5 → 중앙값 비 1.5 + 널 초과 2배

### `[6-c]` — 양쪽을 같은 모양의 통계로, 바는 **그 실행이 잰 자유 판 폭에 비례**

- 왼쪽 `spreadMed` = 채택 눈금 K회 폭의 **중앙값**(18 표본) · 오른쪽 `freeMed` = 자유 판 종별 폭의 **중앙값**
- 바 — `freeMed / spreadMed ≥ 1.30`
- ⚠ **무르게 푼 것이 아니다**: 옛 바 0.6 은 자유 판 폭 중앙값(실측)의 **약 2배**라 사실상 아무것도 안 막았다.
  새 바는 «채택 눈금이 자유 판보다 **실제로** 좁다» 를 요구한다.

### 신설 되돌림 2항

- **`[6-br]`** — 하네스를 끈 짝(실제↔실제)의 `nullGap` 이 `[6-b]` 의 **같은 바 1.5** 를 못 넘는다
  ⇒ `[6-b]` 가 잰 것은 회차 잡음이 아니라 하네스다(778 `[8-br]` 의 같은 자리).
- **`[6-cr]`**(최종형) — **채택 눈금을 앞 R회만 써서 다시 재** 자유 판과 **같은 표본 수**로 견줘도
  여전히 좁다 ⇒ `[6-c]` 가 잰 것은 **표본 수 차이(K=6 ↔ R=5)가 아니라 POP 고정**이다.
  ⚠ 이 항이 없으면 «좁다» 가 표본 수의 부산물일 가능성을 아무도 안 묻는다.
  ⚠ **1안(자유 판을 3회로 깎는 쪽)은 내가 심은 새 동전이었고 자가 잡았다 — §5.**

## §4 실측 — 수리 후 1회차 무경쟁 5회

| 회 | `[6-b]` 묶음 중앙값 비 (바 1.5) | `[6-br]` 널 (바 1.5) | `[6-c]` 좁은 배수 (바 1.30) | 그 실행의 **옛** `spreadWorst` |
|---|---|---|---|---|
| #1 | ×1.66 (널 ×1.11) | ×1.11 | ×1.50 | **73% ← 옛 바로는 빨강** |
| #2 | ×2.11 (널 ×1.08) | ×1.08 | ×3.11 | 52% |
| #3 | ×1.79 (널 ×1.17) | ×1.17 | ×1.81 | **62% ← 옛 바로는 빨강** |
| #4 | ×1.65 (널 ×1.04) | ×1.04 | ×2.31 | 41% |
| #5 | ×1.94 (널 ×1.15) | ×1.15 | ×1.99 | 38% |

⚑ **이 5회가 곧 대조다** — 같은 5회에서 **옛 `[6-c]` 는 2회 빨갛고**(73% · 62%) 새 `[6-c]` 는
**5회 전부 초록 · 최소 여유 ×0.20**(1.50 vs 1.30)이다. 옛 `[6-b]` 의 `inflWorst` 는 같은 5회에서
×1.60~1.66 = **여유 0.10~0.16** 으로 여전히 문턱에 붙어 살았고, 새 묶음 중앙값 비는
×1.65~2.11 = **여유 0.15~0.61** 에 널 초과 조건까지 매 회 크게 남았다.

## §5 내가 **새 동전을 한 번 심었고 자가 3회차에 잡았다**

`[6-cr]` 1안은 «자유 판에 표본을 3회만 줘도 그래도 넓다» 였다. 5회 중 **#3 에서 빨갛다** —
자유 판 3회 폭이 44% → **21%** 로 주저앉아 채택 눈금 24% **아래**로 내려갔다.
범위(max−min)가 표본 수에 단조라는 것은 맞았지만, **한쪽만 깎는 보정은 보정이 아니라 새 편향**이다
(778 §4 가 미리 경고한 그 자리 — «플레이키를 고치러 가서 플레이키를 새로 심지 않았는지 새 항도 K회 돌려라»).

⇒ 깎는 쪽을 **표본을 더 가진 쪽**(채택 눈금 K=6 → R=5)으로 뒤집어 **양쪽을 같은 표본 수**로 맞췄다.
⚑ 이 과정에서 `[6-c]` 본항의 성질도 하나 드러났다 — K=6 ↔ R=5 라 **왼쪽이 공짜로 손해를 보는**
방향의 편향이 원래부터 있었다. 즉 `[6-c]` 의 초록은 그 편향을 **이기고 난** 초록이다.

## §6 실측 — 수리 후 2회차(바로잡은 `[6-cr]`) 무경쟁 6회 · 회귀

| 회 | `[6-b]` 비 (바 1.5) | `[6-br]` 널 (바 1.5) | `[6-c]` 좁은 배수 (바 1.30) | `[6-cr]` 표본 수 맞춤 |
|---|---|---|---|---|
| #1 | ×1.89 | ×1.12 | ×2.37 | 23% < 62% |
| #2 | ×1.96 | ×1.13 | ×1.73 | 21% < 48% |
| #3 | ×1.78 | ×1.09 | ×1.74 | 18% < 32% |
| #4 | ×1.78 | ×1.09 | ×2.20 | 14% < 41% |
| #5 | ×2.03 | ×1.08 | ×1.66 | 19% < 37% |
| #6 | ×2.50 | ×1.05 | ×1.80 | 22% < 44% |

**6회 전부 `PROBE722 23/23`.** 1·2회차를 합친 **수리 후 11회**에서
`[6-b]` ×1.65~2.50(**최소 여유 0.15**) · `[6-c]` ×1.50~3.11(**최소 여유 0.20**) ·
널 ×1.04~1.17(바 1.5 — 되돌림 여유 ≥ 0.33). **같은 11회에서 옛 `[6-c]` 는 2회 빨갛다.**

**회귀** — `probe504` **15/15** · `verify504` **15/15** · `probe695` **18/18** · `node tools/smoke.js` **SMOKE PASS** ·
`node tools/verifyProgress.js` **종료 코드 0** · `index.html` **0줄** · `tools/rul504.js` **0줄**.

⚠ **회귀 중 빨간 하나는 이 작업과 무관함을 대조로 확인했고 783 으로 등재했다** —
`probe680` **16/17**(`FAIL 3-c … ice(cd1.6 폭 17%)`). **같은 트리에서 곧바로 재실행하면 17/17 초록**이고
그 실행의 밴드 밖 명단에는 `ice` 가 **아예 없다** — 759 가 «밴드 밖에 서는 종은 `whirl` 하나» 라는
그 시점 명단 위에 면제를 적어 둔 탓에, 명단이 바뀌면 같은 자리가 다시 빨개진다.
`grep -c 722 tools/probe680.js` = **0** 이라 781 이 닿을 수 없는 자다.

## §7 다음 세션이 볼 것



- **자 안에서 «문장» 과 «코드» 가 다른 것을 보고 있지 않은지부터 보라.** `[6-c]` 는 «자유 판 폭보다
  좁다» 라고 적어 두고 그 폭을 한 번도 안 봤다. 플레이키의 절반은 거기서 났다.
- **표본 수가 다른 두 «범위» 를 견줄 때는 깎는 쪽을 «많이 가진 쪽» 으로 잡아라**(`[6-cr]` — §5).
  범위(max−min)는 표본 수에 단조라 되돌림 자체는 반드시 필요하지만, **덜 가진 쪽을 더 깎으면**
  보정이 아니라 **새 편향**이고 그것이 곧 새 동전이다. 내가 1안에서 실제로 그랬다.
- **새 항도 K회 돌려라**(778 §4 의 재확인). 이번 빨강은 **내가 새로 심은 것**이었고, 5회를 안 돌렸으면
  «플레이키를 고쳤다» 며 플레이키를 하나 더 남기고 닫았을 것이다.
- **`probe680` `[3-c]` 는 아직 열려 있다(783)** — 759 의 면제가 «그 시점 밴드 밖 명단» 위에 적혀 있어
  명단이 바뀌면 다시 빨개진다. 목록을 손으로 늘리는 길은 쓰지 마라(여섯 번 기각됐다).
