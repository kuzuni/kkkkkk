# 469 — `tools/verify462.js` 14건 실패(38/52) «버그(게이트 부패)»

**결론: 제품 `index.html` 0줄 · 게이트 `tools/verify462.js` 0줄 · 레이아웃 Δ0px.**
착수 시점의 `main`(`bccd459`)에서 이미 **52/52 ALL PASS** 였다 — 469 는 등재된 뒤,
그 원인을 만든 세션 자신이 `ac66b9d fix(409)` 로 닫았다. 이 세션이 한 일은 **그것을 A/B 로 못박고
등재문의 진단·처방이 틀렸음을 기록**하는 것이다(1회차 · sess-1446-23678 워커 C · 2026-08-30).

---

## §1 재현 — 338 규칙대로 «처방 전에 먼저 물었다»

착수 트리(`bccd459` + 내 claim 커밋)에서 그대로 실행:

| 실행 | 결과 |
|---|---|
| `node tools/verify462.js` #1 | **52/52 ALL PASS** |
| #2 | **52/52 ALL PASS** |
| #3 | **52/52 ALL PASS** |

3회 연속 같은 값이라 **플레이키(344·372·392·408 계열)도 아니다.**
⇒ 등재문이 적은 «14건 실패» 는 **오늘의 트리에 없다.**

## §2 «헛초록이 아니다» — 한 줄만 되돌린 A/B 로 격리

초록이 «자를 무르게 푼 결과» 일 가능성부터 지웠다. **`tools/verify462.js` 는 생성 이후 단 한 번도
수정된 적이 없다**(`git log -- tools/verify462.js` = 커밋 1개). 자는 그대로이고 바뀐 것은 제품이다.

그래서 **오늘의 트리에서 `ac66b9d` 의 `index.html` 한 자리만 역적용**한 사본을 만들어 같은 자를 돌렸다
(작업 트리는 안 건드리고 스크래치 사본 — `assets`·`node_modules` 는 심링크):

| 트리 | `verify462` |
|---|---|
| 현재 `main` | **52/52 ALL PASS** |
| 현재 `main` − 462 의 제품 규칙 한 자리(`.stab.on…::after{box-shadow: 검정 7 + `inset 0 -7px 0 7px #413122` + `inset 0 -14px 0 7px #634F37`}`) | **38/52 FAIL 14** |

**38/52 · 14건**은 등재문이 적은 숫자와 **정확히 같다.** 빨간 항의 모양도 그대로다:

- `[1]` 선언 3건 — 끝 칸 `::after` 에 어두운 띠·베벨 두 겹이 «없다»
- `[2]` 실측 1건 — 10 상점 칸3 BL 이 검정 뒤 첫 실런에서 `B7.5`(순서가 D→B 가 아니다)
- `[3]` 실측·동치 10건 — 아래 코너 어두운 띠가 **D 2.0~3.0**(가운데 칸은 같은 자리가 **D 5.5/6.0/6.0**)

⇒ 자는 여전히 «있어야 할 것이 없으면» 빨개진다. 초록은 **제품이 돌아왔기 때문**이다.

## §3 등재문 정정 — 뿌리도 처방도 틀렸다

| | 등재문(2026-08-30, sess-1321-11352 워커 F) | 실제 |
|---|---|---|
| 뿌리 | «409 6회차가 만진 **코너 기하**가 되돌림 뒤에도 462 의 자에 안 맞는다» | **`3832847 revert(409)` 이 커밋 «범위» 를 되돌리면서 그 사이에 들어온 462 의 제품 CSS 한 자리를 같이 지웠다** — 코너 기하는 되돌림 뒤 409 4회차 값 그대로였고(`verify409` 123/123) 없어진 것은 462 의 두 겹이다 |
| 빨간 자리 | «전부 75° 한 각도에 몰려 있다» | **45°·60°·75° 세 각도 전부**(위 표 [3] 10건). 등재 당시 트리에는 409 6회차의 잔재가 섞여 있어 각도별 모양이 달라 보였다 |
| 처방 | «409 7회차가 코너를 다시 앉힌 **뒤** 462 의 자를 이관한다» | **이관할 것이 없었다.** 그 처방을 따라 `verify462` 의 기대값을 손댔으면 «지워진 제품 규칙» 에 자를 맞추는 것이 되어 **462 가 통째로 사라져도 초록인 게이트**가 됐다(334·368 이 두 번 겪은 사고) |

등재문 자신이 «지금 462 의 기대값을 손대면 409 가 아직 안 정한 값에 게이트를 굳히게 된다» 며
**자를 건드리지 말라고 적어 둔 것이 결과적으로 이 작업을 구했다.**

원인·수리는 409 7회차 세션(sess-1346-18993)이 스스로 찾아 닫았다 — `ac66b9d fix(409)`:
`verify462` 38/52 → 52/52 · `verify96` 91/93 → 97/97. 교훈은 이미 `LESSONS.md` **409-7 ⑤**
(«되돌릴 때는 커밋 «범위» 가 아니라 «내가 만진 자리» 를 되돌려라»)로 적혀 있다 — 같은 말을 다시 적지 않는다.

## §4 회귀 — 같은 부품을 묻는 자 전부 초록(무수정)

| 자 | 결과 |
|---|---|
| `verify462` | **52/52** (×3) |
| `verify409` | 123/123 |
| `verify384` | 104/104 |
| `verify449` | 42/42 |
| `verify378` | 48/48 |
| `verify468` | 22/22 |
| `verify96` | 97/97 |
| `node tools/smoke.js` | **SMOKE PASS** |

## §5 다음 세션에게

- **409 8회차는 이 자리를 다시 지울 수 있다.** 끝 칸 `::after` 는 409(마스크·링)·462(아래 두 겹)·
  384(바닥 띠 코너 감김)가 **한 선언을 공유**한다. 409 가 그 선언을 만지면 되돌릴 때도 **462·384 의 항까지
  같이 사라지는지**를 `verify462`·`verify384` 로 확인하고 나가라(이번이 그 사고의 두 번째다).
- **이 행은 «게이트 이관» 이 아니라 «제품 복구» 로 닫혔다.** 게이트 부패로 등재된 T1 이라도
  **자를 먼저 의심하지 말고 A/B 를 한 줄로 좁히는 것**이 빠르다 — 여기서는 자 대신 제품 한 자리를
  되돌린 사본 하나가 등재문의 숫자(38/52 · 14건)를 그대로 재현했다.
