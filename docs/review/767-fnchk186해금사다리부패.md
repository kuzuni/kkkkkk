# 767 — `tools/fnchk186.js` 2건 실패(9/11): 05 장비 팝업 «미해금 등급 안내» 표본이 굳어 있었다

**완료(2026-09-01, sess-2058-18506 루틴 워커 D · 1회차) — 제품 `index.html` 0줄.**
지시서 [3]-**(가)** 기계적 작업(자 수리) · 비평가 없음 · `FNCHK186` **9/11 → 12/12** · `VERIFY186` 34/34 · **SMOKE PASS**.

---

## 1. 재현 — 등재문의 «갈래 미확정» 을 한 번에 갈랐다 (338 규칙)

등재문은 갈래 둘을 열어 뒀다:
- ⓐ 662·664 가 미개방 표시를 바꿨는데 자가 안 따라왔다(**게이트 부패**)
- ⓑ 표기 자체가 사양에서 벗어났다(**제품 결함**)

수리 전 실행:

```
| 2 | 소환 Lv 1 상태로 팝업을 봄 | 미해금 6등급 × 5칸 = 30칸에 해금 레벨 표기
    | 30칸 · 소환 Lv.10 소환 Lv.16 소환 Lv.24 소환 Lv.32 소환 Lv.40 소환 Lv.49 | ✘ |
| 3 | 소환 Lv 를 30 으로 올린 뒤 다시 봄 | 희귀·영웅·전설 안내는 사라지고 신화·초월·불멸만 남음
    | 소환 Lv.32 소환 Lv.40 소환 Lv.49 | ✘ |
```

**실측이 이미 사양대로였다.** 행 2 는 «30칸 · 6종 안내» 를 냈고(칸 수도 종수도 기대와 같다),
행 3 은 아래 3등급이 사라지고 상위 3등급만 남았다 — **문면이 기대하는 일이 실제로 일어나고 있다.**
어긋난 것은 **숫자뿐**이었다. ⇒ 갈래 **ⓐ 게이트 부패**로 확정, 제품은 0줄.

**⚑ 등재문이 지목한 662·664 는 범인이 아니다.** 뿌리는 **해금 사다리 그 자체**이고 두 세대 전에 옮겨졌다:

| 세대 | 만렙 | 해금 사다리(희귀→불멸) | 출처 |
|---|---|---|---|
| 186 당시 | 100 | 5 · 15 · 30 · 40 · 55 · 75 | `fnchk186` 이 박아 둔 값 |
| **196**(2026-08-27) | 25 | 5 · 8 · 12 · 16 · 20 · 24 | 주인 확정 사다리 |
| **496**(2026-08-31) | **50** | **10 · 16 · 24 · 32 · 40 · `SUM_MAXLV−1`(=49)** | 현재 `GRADE_ROLL_EQ` |

제품은 표 하나(`GRADE_ROLL_EQ` · index.html 19493~19520)로 두 번 다 따라갔고,
렌더(`32921`)도 `roll[gi].unlock` 을 읽는다. **`fnchk186` 의 두 상수만 못 따라왔다.**

## 2. 처방 — 상수 갱신이 아니라 재료 파생 (522-① · 765-①)

`5/15/30/…` 을 `10/16/24/…` 로 **고쳐 적는 길은 다음 개편에 똑같이 빨개진다.**
496 은 만렙을 25 → 50 으로 옮기며 사다리를 비례 이동시켰고, 199 밸런스 라운드가 아직 돌고 있다.

⇒ 기대값을 **재료에서 매 실행 파생**한다:

- 행 2 — 잠긴 등급 = `GRADE_ROLL_EQ` 중 `unlock > 1` 인 행. 기대 칸 수 = `잠긴 등급 수 × WPN_COLS`,
  기대 문자열 = 그 행들의 `unlock` 을 **행 순서 그대로** 이어 붙인 것(순서까지 보므로 인덱스가 밀리면 잡힌다).
- 행 3 — 끊는 레벨도 표에서 고른다: «잠긴 등급의 **아래 절반**이 열리는 레벨»
  (`locked[ceil(n/2)−1].unlock` — 현재 24). 사다리가 옮겨지면 이 레벨도 같이 옮겨간다.
  기대 = `unlock > cut` 인 행들. `gone > 0` · `want !== ''` 로 «양쪽이 다 비지 않았는지» 를 같이 못박아
  «0건 ↔ 0건» 으로 닫히는 빈 초록을 막았다.

**⚑ 이 처방은 새로 지어낸 것이 아니다 — 형제 자가 처음부터 그렇게 적혀 있었다.**
`tools/verify186.js` **[E]** 는 같은 화면·같은 안내를 `GRADE_ROLL_EQ.map(g => g.unlock)` 에서 역산하고
있었고 그래서 두 세대 내내 **34/34 초록**이었다. 같은 작업의 자 둘이 같은 숫자를 **한쪽은 파생, 한쪽은 상수**로
들고 있었던 것이 이번 부패의 전부다.

## 3. 헛초록 방지 — 되돌림 시험을 자 안에 넣었다 (765-②)

파생은 잘못 쓰면 «제품에게 답을 묻고 그 답을 채점하는» 헛초록이 된다. 셋으로 막았다:

1. **제품 함수가 아니라 표를 읽는다** — `rollOf('weapon')`(제품의 슬라이스 함수)이 아니라 `GRADE_ROLL_EQ` 자체.
2. **칸 수를 같이 센다** — 문자열만 보면 «안내가 한 칸만 남아도» 통과할 수 있다.
3. **행 4 «되돌림» 신설** — 표의 첫 잠금 등급 `unlock` 을 **+7 흔들고 다시 그린 뒤 원복**해서,
   안내가 표를 따라 `소환 Lv.17` 로 바뀌었다가 `소환 Lv.10` 으로 돌아오는지 본다.
   **상수를 그리는 회귀(= 767 이 고친 부패의 제품판)라면 여기가 즉시 빨개진다.**

### 3-1. 대조군 두 벌(임시 사본 — 저장소 트리는 안 건드렸다)

⚠ 임시 사본 이름에 SID 를 붙였다(648 «고정 이름 임시 사본» 함정).

| 대조군 | 무엇을 흔들었나 | 행 2 | 행 3 | 행 4(되돌림) | 판정 |
|---|---|---|---|---|---|
| **ⓐ 제품 동결** | 렌더가 `need` 대신 **상수 `소환 Lv.5`** 를 찍게 | ✘ | ✘ | ✘ | **셋 다 빨강** = 헛초록 아님 |
| **ⓑ 사다리 이동** | 표를 `12·18·26·34·42` 로 옮김(496 같은 개편의 예행) | ✔ | ✔ | ✔ | **셋 다 초록** = 자가 따라온다 |

ⓑ 는 12칸 중 11 이었는데 **빨간 한 칸은 «콘솔 에러 0건»** 이고, 원인은 임시 사본 디렉터리에
`assets/` 를 안 복사해 생긴 `ERR_FILE_NOT_FOUND` 125건이다 — **대조군 설비의 자국이지 이 수리와 무관**하다
(ⓐ 도 같은 자국을 함께 안고 8/12 였다: 행 2·3·4 + 콘솔).

## 4. 같은 숫자의 나머지 자리 (522-②)

`tools/` 전수 스윕 — 옛 사다리(5·15·30·40·55·75)를 들고 있는 자리:

| 파일 | 자리 | 상태 |
|---|---|---|
| `tools/fnchk186.js` | **단언 2건** | **이번에 파생으로 교체** |
| `tools/verify186.js` | 주석 2줄([E] 머리말 · [E4] «소환 Lv.75») | **낱말로 되돌림**(단언은 처음부터 파생 — 거동 0줄) |
| `tools/verify196.js:9` · `tools/verify115.js:4` | «구 사다리는 …» | **이력 서술이라 그대로 둔다**(그 시점의 사실이다) |

**헛초록은 0건**이었다 — 옛 사다리를 «참» 으로 단언하고도 초록이던 자리는 없다.

## 5. 기능 체크 표 (수리 후 — `docs/review/186-장비팝업8등급노출.md` 갱신분)

`FNCHK186 PASS — 12/12`. 표 전문은 `node tools/fnchk186.js` 출력 그대로.
새 행은 **4번(되돌림)** 하나이고 나머지 11 행은 «누른 것·기대» 를 안 바꿨다
(행 2·3 은 기대 문면이 **파생값을 그대로 인쇄**하게 바뀌어, 사다리가 또 옮겨지면 표에도 새 숫자가 찍힌다).

## 6. 회귀

| 자 | 결과 |
|---|---|
| `node tools/fnchk186.js` | **12/12 PASS**(수리 전 9/11) |
| `node tools/verify186.js` | **34/34 PASS**(무수정 — 제품 0줄의 가장 짧은 증거) |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verifyProgress.js` | 종료 코드 0 |

제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 계수 0줄 · 세이브 이관 없음.

## 7. 곁다리 등재

**0건.** 스윕에서 나온 두 주석은 이 수리 안에서 닫았고(§4), 그 밖에 새로 빨간 자리는 없었다.
