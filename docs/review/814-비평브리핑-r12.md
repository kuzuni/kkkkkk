# 814 — 12회차 비평 브리핑 (50 코스튬 [강화] 성공 연출) · **878 이 든 뒤 재채점**

## 0. 무엇을 채점하는가

50 코스튬 시트에서 카드 하나를 고르고 **[강화]** 를 눌렀을 때의 **성공 연출**이다.
지시서 [3]-**(다)**(연출) 축으로 채점한다 — ①타이밍(길이·이징) ②크기 비율 ③위치·경로
④가독성·일관성. 각 10점, **네 항의 최저가 당신의 점수**다. 통과선은 8.
색·아트 «내용»·서체 «모양» 은 감점 사유가 아니다 — **크기·위치·대비**만 본다.

## 0-1. ⚑ 이 회차는 왜 다시 찍었는가 — **바 색이 바뀌었다**

11회차(EA·EB)가 **①7 ②9 ③8 ④7 = 7/7** 로 소수까지 일치했고, 둘 다 8 을 막는 «단 하나» 를
**④ «n/500» 의 대비 여유 없음**으로 지목했다. 그 자리의 남은 손잡이가 **바 자신의 색**뿐임을
11회차가 실측으로 닫았고(워시 세기는 하한 · 잉크 들기는 여유 0 · 바 통째 들기는 자로 기각),
그것을 **878** 로 등재했다. **878 이 그 뒤 들었다** — `#bCos .sk-bar` 바탕 두 스톱을
`#8182A0→#70718B` · `#6E7798→#5F6784` 로 어둡게 했다(기하 **Δ0px** · `#bCos` 로 가둬 07 스킬·26 펫
공용 바는 그대로).

⇒ **이 캡처가 878 이 든 첫 판이다.** 11회차 대비 달라진 것은 그 두 스톱뿐이고, 이 작업 단위의
제품은 **12회차에 0줄** 이다.

## 1. 판이 셋이다 — 나란히 놓고 재라

| 판 | 파일 | 무엇 |
|---|---|---|
| **A (지금)** | `814-r12-now-step-1..8.png` · `814-r12-now-live-1..6.png` · **`814-r12-now-rest.png`** | 현행 트리 = 채점 대상 |
| **P (직전)** | `814-r12-p7-step-1..8.png` · `814-r12-p7-live-1..6.png` · **`814-r12-p7-rest.png`** | **7회차판** — 알갱이 손잡이는 A 와 **완전히 같고**, 8회차가 더한 «값 줄을 워시 위로 + 워시 세기» 두 줄만 없다 |
| **B (바닥)** | `814-r12-pre-step-1..8.png` · `814-r12-pre-live-1..6.png` · **`814-r12-pre-rest.png`** | 814 이전 원 결함(텍스트 델타가 «n/500» 을 덮던 판) |

⇒ **A ↔ P 의 차가 8회차(이 작업 단위의 마지막 제품 변경)가 만든 것 전부**다.
세 판 모두 878 의 바 색을 쓴다(색은 셋에 공통) — 그러니 **A ↔ P 대조는 878 과 무관하게 성립한다.**

크롭은 **620×480**, 배율 1. 이 크롭 안에서 **카드 좌상단은 (226,150)** 이고 카드는 168×약190 이다.
`step` 8장의 시간축은 0/80/160/240/320/400/480/560ms 다. **step-6/7/8 은 연출이 다 꺼진 정착 상태**라
서로 같다(md5 동일 — 정상). 살아 있는 램프는 step-1..5 다.

## 2. 유령 방지 — 이 셋은 감점 사유가 아니다

* ⓐ `step` 벌의 «실제 경과 시간·수거» 는 채점 대상이 아니다. CSS 애니를 정지시키고 진행도를
  직접 준 것이라 시간축이 정확한 대신 **파티클은 JS 틱이라 정지**다.
* ⓑ 그래서 `step` 벌에서 «알이 안 움직인다» 는 **하네스의 성질**이지 제품이 아니다.
  입자 흩어짐은 아래 4절의 **자 출력**이 답한다.
* ⓒ 크롭 경계·이웃 카드는 채점 대상이 아니다.
* ⓓ ⚑⚑ **«정지 상태» 는 `-rest.png` 에서만 읽어라.** 실측(`probe814c` [P9][P10][P11] · 이번 실행)은 —
  플래시와 패치가 **같은 프레임(396ms)에 같이 사라지고**, `#fxl` 자식이 **0개**로 완전히 비고,
  정지 «Lv. n» 은 `rgb(255,255,255)` 로 이웃 카드와 **같은 색**이다.
  ⇒ 400ms 이후를 «정지» 로 읽은 «keep 사본 영구 잔존» 지적은 감점 사유가 아니다
  (9·10회차를 두 번 뒤집은 유령이 그것이다). 대신 `-rest` 에서 A ↔ P ↔ 이웃 카드가 다르면
  **그것은 진짜다 — 반드시 잡아라.**

## 3. 8회차가 손댄 것 / 안 댄 것

7회차 2인(CX·CY)의 ④ 최저 사유이자 각자 1순위는:
> **플래시 워시가 두 값 줄(«Lv. n» · «n/500») 위에 얹혀 대비를 무너뜨린다.**

8회차가 **그것만 손댔다** — 두 값 줄을 워시 «위» 에 다시 그리고(`--flash-keep`), 글자가 앉은 면이
들리는 만큼 워시 세기를 눌렀다(`--flash-k` = .70). **손대지 않은 것(= 이 회차 지적으로 부적절)**:
알갱이 손잡이 셋(A 와 P 가 같은 값) · 타이밍 곡선(공용 `@keyframes` — 이 작업 밖, **868**) ·
흰 테 9px·하드 림·안쪽 글로우(앞 회차들이 «0 도 답이 아니다» 로 못박음).

## 4. 자 출력 — ③ 을 «미관측» 으로 깎지 말라고 같이 준다

`step` 벌이 파티클을 정지시키므로 흩어짐은 캡처로 못 본다. DOM 좌표 자(손잡이 불변이라 r10~r12 동일):
* 버스트 알이 **누른 카드 상자 안**에서 뜬다 — 밖 0알 / 잰 10알
* 입자가 **값 줄 두 곳 위에 안 앉는다** — 0알
* 알 최대 지름 17px · 이동 벡터 평균 8.6px
* 가로 링 — rx 54.0px · 최악 끝점 63.7px(지터 상한 포함) · 가둠 벽 71.5px · 잉크 끝 72.2px ≤ 카드 반폭 84.0px ⇒ **눌림 0 · 액자 절단 0**

## 5. 대비 실측 (찍힌 픽셀 · `probe814c` — **이번 실행값**)

정의: **(잉크 상자 안 상위 1% 휘도 + .05) ÷ (중앙값 + .05)** · WCAG 상대휘도.
(⚠ «최대÷최소» 로 재면 검은 외곽선 탓에 어느 판에서나 21.00:1 — 그 자는 글자와 자기 외곽선의 비다.)

| 값 줄 | 정지(연출 없음) | 판 A 연출 봉우리(52%) | 11회차(878 전) 봉우리 |
|---|---|---|---|
| «Lv. n» | **6.76:1** | **5.36:1** (정지의 79%) | 5.36:1 |
| «n/500» | **5.59:1** (r11 4.42) | **4.23:1** (정지의 76%) | **3.59:1** |

* **878 이 «n/500» 봉우리를 3.59 → 4.23 으로 올렸다**(Δ+0.64). 11회차 두 채점자가 «3.0 하한에
  여유가 없다»(최악 3.02)고 적은 그 자리다. 이제 하한 위 여유가 **1.23** 이다.
* 글리프 **채움 밝기**는 A 에서 정지와 **Δ0.000** — 글자 자체는 처음부터 완전히 돌아와 있었다.
* «Lv. n» 은 **한 값도 안 움직였다**(878 은 바 «면» 만 눌렀고 «Lv. n» 은 바 위가 아니다).

## 5-1. ⚠⚠ 처방을 적기 전에 반드시 읽을 것 — **이미 스윕으로 기각된 값**

11회차 두 채점자(EA·EB)가 각자 «`--flash-k` .70 → .40 / .50» 을 처방했는데 **둘 다 채택되지
않았다.** 7회차 채점자 CX 가 이 손잡이를 스윕해 .70 을 고른 근거가 코드 옆에 적혀 있다
(index.html ~43431):

> «0.80 은 3.09 로 걸치고 **0.60 은 플래시가 눈에 띄게 죽는다**»

⇒ .40·.50 은 이미 기각된 .60 **아래**다. 두 사람이 그것을 못 본 이유는 구조적이다 — 브리핑이
«색은 감점 사유가 아니다» 로 채점 축을 대비로 좁혀 **«플래시가 죽는가» 가 그들의 자에 없었다.**
**그래서 이번엔 그 사실을 미리 준다**: 워시 세기를 내리는 처방은 ④ 를 사는 대신 ①② 를 판다.
그 교환을 하겠다면 «플래시가 죽지 않는다» 를 **당신 자로 보여라**(그냥 숫자만 적지 마라).

## 5-2. 이 회차에 살아 있는 두 지적 (각자 아직 유효한지 확인해라)

* **ⓔ 워시 감쇠 절벽** — 11회차 2인이 «마지막 한 칸이 남은 진폭의 51%(EA) / 240→320ms 에서 97%(EB)»
  를 각자 쟀다. **판 A/P/B 공통 · 공용 `@keyframes fxFlash`** 라 이 작업 밖이고 **868** 이 이미
  한 번 재배치했는데도 두 사람이 절벽을 봤다. ① 점수에는 사실대로 반영하되,
  «이 회차가 만든 것» 은 아니다. ⚠ `FXFLASH_MS`(340)가 80ms 표본 간격의 4.25배뿐이라
  **표본 자체가 거친 것**도 갈래에 남아 있다 — 절벽을 적을 거면 그 갈래를 먼저 갈라라.
* **ⓕ 이동 벡터** — 알 평균 이동 8.6px = 링 반경 54.0px 의 15.9%. «터져 나간다» 보다 «제자리에서 뜬다».
  838 이 같은 축을 다룬다(그 행도 보류).

## 6. 당신이 답할 것

1. **①~④ 각 점수와 최저 점수**(정수). 후하게 주지 마라.
2. 지적은 **수치로만** — «몇 px 어긋남 / 몇 % 큼 / 대비 몇 :1». 두루뭉술한 총평 금지.
   가능하면 `node` + `pngjs` 로 픽셀을 직접 재라(리포 루트에 설치돼 있다).
   **어떤 마스크·열창으로 쟀는지 지적마다 밝혀라**(A1 10회차 교훈 — 계측 정의가 다르면
   일치해도 틀린다).
3. **8점을 막는 «단 하나»** 를 하나만 고르고, 그것을 **판 P 와 대조해** 이 회차가 만든 것인지
   원래 있던 것인지, 아니면 이 작업 밖(공용 곡선·바 색 등)인지 밝혀라.
4. 그 «단 하나» 의 처방을 **숫자까지** 적어라. 5-1 을 읽고 적어라.
