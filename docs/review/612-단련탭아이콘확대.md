# 612 — 단련 탭 아이콘 확대 (주인 지시 2026-09-01 01:08 «단련부분에 아이콘들 좀 작은데 크게되게해»)

세션: sess-1618-23172 (루틴 워커 A, 2026-08-31 16:18Z~)

## §1 실측(수리 전) — «작다» 의 정체

probe584 §C 재실행 + 자체 실측(스크립트는 세션 스크래치 · 수치는 여기):

| 자리 | 액자 | 잉크(세로) | 비고 |
|---|---|---|---|
| 훈련 카드 `.ci` | 180×164 | **184.8** | 잉크가 액자보다 큼(1.388) — 설계상 넘침 허용 자리 |
| 룬 액자 `.ri` | 200×200 | **139.1** | 584 가 411 비(0.6957)로 맞춘 자리 |
| 단련 액자 `.ti` | **104×104** | **69.4** | 룬의 절반 · 훈련의 37% — 주인이 본 «작다» |

같은 팝업의 세 형제 중 단련만 액자가 절반이었다. 잉크/액자 비(0.6677)는 이미 411 의 96% —
**비가 아니라 액자 자체**가 결손이었다.

## §2 처방 — 값은 전부 파생(발명 0)

- **액자 178** = 행 222 − 위 여백 22×2 (584 `.tr-up` 의 «두 축 잠그고 남는 축만 푼다» 뺄셈).
  테·radius 는 «룬 .ri 규격을 축소한 것» 이라는 원 주석의 **축소 해제** — .ri 규격(링 8 · r32) 그대로.
- **잉크 = `TEMPER_ART`** — 411 «그림 자리» **세 번째 자리**(SLOT_ART → RUNE_ART → TEMPER_ART).
  `SLOT_ART/115 × 178 = 133.1×123.8`. 실측 잉크 122.4~123.8(비 0.6953 = 411 대비 99.9%).
  덤: fs62 시절 글리프마다 다르던 잉크가 saInk 정규화로 **최대÷최소 1.012**(411 자 ≤1.05 통과).
- **글줄 이동**: `.tn`/`.tl` left 150→224(여백 20 유지) · `.td` 는 한 줄로는 못 살아
  (잉크 최악 411px > 남는 폭 392) — «·» 자리를 `<br>` 로 갈라 **두 줄**(224,118 · 392×68).
  liveTemper 의 `.td` 를 html 경로로 짝 맞춤(584 `.tb` 규칙).
- **488 봉투 이관**: 글줄이 옛 빈 띠(중앙 x357..530)로 들어와 [I6] 이 [td]/[tn] 으로 빨개졌다 →
  훈련 카드 [I4] 규약(«아이콘은 정보 요소가 아니다»)대로 두 줄기를 액자 위로(`--hb-x .1152`),
  칸 88 · pad 12(104 면 클램프가 왼 칸을 11px 민다 — [K3] 이 잡았다).
  `verify488` [I6] `.ti` skip + **[I6b] «봉투가 액자 안»** 신설(무르게 풀지 않음).

**스코프 판단** — 단련 탭의 다른 아이콘들: [단련] 버튼 화폐 53(584 완료) · 헤더/회수 화폐 26/32 는
584 정오표가 «못 올린다» 로 못박은 자리인 데다 **613·614 가 그 부품 자체를 걷어냈다**(같은 시각 병렬 진행)
— 이 행은 축 아이콘 3장이 본체다.

## §3 게이트

- `verify612` **25/25 신설** — [A] 액자 178·잉크 = 411 비 ±3% [B] 서로 고름 ≤1.05 [C] 파생(소스 정규식)
  [D] 이웃 Δ0(584 값)·겹침 0 [E] 두 줄·자릿수 스윕(Lv 99,999)·liveTemper 동일 그림 [F] 1600 잘림 0
  [R] 되돌림(액자 104 → [A1] 빨강 · sa-e 벗기면 [A3] 빨강)
- 이관: `verify488` [I6]/[I6b]/[K3] → **77/77**
- 회귀 무수정: `verify584` 52/52 · `verify210` 101/101(병합 전) · `verify519` 31/31 · `verify577` 28/28 ·
  `verify583` 36/36 · `verify491` 93/93 · `verify203` 91/91 · SMOKE PASS
- ⚠ 병합 후 `verify488` [G6] · `verify210` 즉사는 **origin/main(613 wip) 대조로 동일 재현** —
  613 세션(sess-1628-22831, lock 생존)의 진행 중 이관 몫이라 손대지 않았다(338·344 규칙).

## §4 병렬 충돌 기록

613·614(sess-1628-22831)가 이 lock(16:18) 뒤(16:28) 같은 화면을 선점·push 해 rebase 충돌 3곳 —
전부 «양쪽 살리기» 로 병합: ① `.tr-tp` hb 값(내 .1152/88/12) + 613 의 `.tp-hd` hb 철거 주석
② eff(내 `<br>`) + cost(613 의 `curIc('tstone',34)`) ③ liveTemper `.td`·`.tc` 둘 다 html 경로.
`verify612` [F] 의 `.tp-ft` 표본이 614 철거로 죽어 살아 있는 표본으로 교체(333 규약).

## §5 비평 회차 (두 겹 채점 ② — 대조 캡처 나란히 · 2인 독립)

캡처: `docs/shots/612-sidebyside.png`(훈련 카드/룬 상단/단련 행 나란히) ·
`612-temper-2280.png` · `612-temper-1600.png` · `612-train-2280.png` (PNG 는 .gitignore — 수치로 기록)

### 1회차
(채점 대기)
