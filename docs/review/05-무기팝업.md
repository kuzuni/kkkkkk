# 05 무기 팝업 — 작업 · 비평 기록

- 작업 단위 **05** / 세션 `sess-0036-17355` / 2026-08-24
- 측정표: `docs/measure/05-무기팝업.md`
- 구현 구간: `#wpnw` (CSS `.wm*` · `.wgc*`), JS `renderWpn()` / `openWeapon()`
- 캡처 조건: 1080x1920 헤드리스, 주입 상태
  `S.own.weapon0={n:0,l:5}; S.own.weapon1={n:2,l:2}; S.eqSlot.weapon='weapon1'; openWeapon('weapon1')`

---

## 0. 껍데기 판정 — 처음 세운 가설이 픽셀로 반증됐다

조율 단계에서 좌측 여백(x4~20)을 세로로 훑고 **「y582~2164 를 채우는 바닥 시트」** 라고 판단했는데,
측정 담당이 픽셀로 반증했다. 다른 화면에서도 그대로 재발할 함정이라 남겨 둔다.

| 내가 본 것 | 실제 | 왜 틀렸나 |
|---|---|---|
| x6~8 에 `#514433`→`#634F37` 세로 띠 = 시트 좌측 테두리 | **뒤에 깔린 06 장비 페이지**가 딤(×0.448) 먹은 색 | 팝업이 아니라 배경 페이지였다 |
| x0~6 / x1073~1079 검정 = 시트 프레임 | **게임 캔버스 자체 테두리** (y1000·1900·2000 어디서나 동일) | 팝업과 무관 |
| y582~588 검정 = 시트 상단 테두리 | x200~900 가로스캔 시 **검정 픽셀 0/700**, x540 은 전부 크림 | 좌측 한 열만 보고 전폭을 단정했다 |
| 딤 없음 | 06번과 픽셀 대조 → 투과배율 **0.448** (n=37,554) = `rgba(0,0,0,0.55)` | 단일 이미지만 봐선 딤 판정 불가 |

> **교훈: 세로 1열 스캔만으로 «전폭 경계» 를 단정하지 마라.** 경계라고 생각한 y 에서 반드시
> **가로 스캔을 한 번 더** 해 그 색이 전폭에 걸쳐 있는지 확인해야 한다.
> 딤 유무는 **같은 배경을 담은 다른 레퍼런스와 픽셀 대조**해야 판정된다(여기선 06번).

**확정 판정**: 05 는 **딤 위 가운데 다이얼로그**다. 폭 898 · radius 42 · 검정 7px + 갈색 프레임 11px 로
A5 모달(22 퀘스트 팝업)과 **같은 규격**이고, 세로중심도 화면중심 +42 로 동일하다.

다만 **페이지 화살표 ◀▶ 가 모달 박스 바깥**(x14~76 / 1003~1065)에 있어 공용 `#modal` 껍데기로는
표현할 수 없다 → 01(`#offw`)·03(`#dunw`) 과 같이 전용 오버레이 `#wpnw` 를 썼다.

---

## 1. 좌표 대응 — `구현y = 레퍼런스y − 210`

모달 높이 1469 는 **실측 그대로** 쓰고, 화면 높이 차이 420px 은 **모달 바깥(전투 화면)이 흡수**한다.
레퍼런스 모달 top 478 ↔ 구현 268 이므로 모달 안의 모든 y 는 정확히 **−210** 이다.
가로는 1:1 (모달 x91, 폭 898 그대로).

세로중심은 `top:50% / translateY(calc(-50% + 42px))` 로 걸어 프레임 높이가 바뀌어도 관계가 유지된다.

### 헤드리스 실측 대조 (전 요소 0~2px 이내 일치)

| 요소 | 레퍼런스 (x,y,w,h) | 구현 | 차이 |
|---|---|---|---|
| 모달 박스 | 91, 478, 898, 1469 | 91, **268**, 898, 1469 | 0 |
| 타이틀바 | 100, 487, 880, 92 | 98, **277**, 884, 92 | x −2 / w +4 |
| 본문 | 111, 579, 858, 1347 | 111, **369**, 858, 1347 | 0 |
| 정보 패널 | 118, 611, 844, 396 | 118, **401**, 844, 396 | 0 |
| 아이콘 박스 | 166, 644, 148, 147 | 166, **434**, 148, 147 | 0 |
| 이름 알약 | 337, 654, 349, 50 | 337, **444**, 349, 50 | 0 |
| 등급 알약 | 691, 655, 209, 49 | 691, **445**, 209, 49 | 0 |
| Lv 알약 | 355, 723, 139, 70 | 355, **513**, 139, 70 | 0 |
| 효과표 | 166, 843, 748, 150 | 166, **633**, 748, 150 | 0 |
| 그리드 패널 | 118, 1037, 844, 621 | 118, **827**, 844, 621 | 0 |
| 카드 1행1열 | 128, 1069, 148, 158 | 128, **859**, 148, 158 | 0 |
| 카드 1행5열 | 808, 1069 | 808, **859** | 0 |
| 카드 2행1열 | 128, 1259 | 128, **1049** | 0 |
| 버튼 1 / 2 | 248 / 558, 1763, 274, 131 | 248 / 558, **1553**, 274, 131 | 0 |
| 화살표 좌 / 우 | 14 / 1003, 1152, 63, 116 | 14 / 1003, **942**, 62, 115 | ≤1 |

4행째가 그리드 패널 하단에서 **19px 만 노출**되는 것까지 레퍼런스와 일치한다(스크롤 뷰포트).

---

## 2. 자체 픽셀 대조로 잡은 것 (비평가 투입 전)

캡처와 레퍼런스의 **텍스트 잉크 높이**를 같은 방식으로 재서 대조했다.

| 요소 | ref 잉크h | 1차 구현 | 조치 | 결과 |
|---|---|---|---|---|
| Lv 알약 | 25 | **31 (+24%)** | font-size 44 → **35** | 24 (−1) |
| 카드 `Lv.n` | 20 | 18 (−10%), 4px 아래 | 26 → **29**, top 15 → **11** | 20 (0) |
| 타이틀 `무기` | 36 | 36, 3px 아래 | `top:-3px` | 36 (0) |
| 재료 진행바 `0/3` | 20 | 19, **7px 아래** | `top:8 → 1` | 19 (0) |
| 버튼 글자 | 37 | 38, **7px 아래** | `top:-7px` | 38 (0) |
| 이름 알약 / 등급 알약 / 표 헤더·라벨·값 / 카드 진행바 | — | 전부 ±1~2px | 조치 불필요 | — |

그 밖에 형상 2건:

1. **화살표** — 45° L자 회전(`border-left`+`border-top` + `rotate(-45deg)`)으로는
   레퍼런스 비율(63×116, 폭/높이 **0.54**)이 나오지 않는다. 회전 결과는 **60×97** (폭/높이 0.62)로,
   45° 셰브런의 폭/높이는 구조적으로 0.6 근처에 고정된다.
   → `clip-path:polygon(...)` 으로 외곽·내곽을 직접 그려 **62×115** 로 맞췄다.
   **다른 화면의 셰브런도 같은 함정을 밟는다 — 각도가 45°가 아니면 회전으로는 못 만든다.**
2. **장착 중 카드** — 레퍼런스는 **바깥 어두운 시안 `#134E52` + 안쪽 밝은 rim `#35DCEE`** 인데
   밝은 시안을 바깥 테두리에 줘서 안팎이 뒤집혀 있었다.

색은 17개 영역을 중앙값으로 대조해 **Δmax ≤ 12** 로 일치한다(타이틀바 1 · 정보패널 1 · 표 헤더/본문 0 ·
그리드 패널 0 · 카드 레어 1 · 아이콘 박스 2 · 이름 알약 2 · 진행바 트랙 6).

또 **본문 베이지에 별무늬가 없다**는 것을 확인했다(패널 사이 베이지 stdev **0.4**).
A5 공용 모달(`.mbody`)은 별 타일을 깔지만 **05 는 평면 `#F0D9BA`** 다 — 공용 껍데기를 그대로 썼다면 틀렸을 지점이다.

---

## 3. 콘텐츠 대응 (레이아웃이 아니라 게임 데이터 차이)

레퍼런스 격자는 **1행 커먼 / 2행 언커먼 / 3행 레어 / 4행 에픽(잘림)** 으로 **행마다 등급 띠**를 이루고
등급당 5종이 들어간다. 우리 게임은 **등급당 무기가 1종뿐**이다.

→ **행 = 등급** 구조를 유지하되 각 행 1번 칸에 그 등급 무기를 놓고 나머지 4칸은 **미해금 자물쇠 칸**으로 채웠다.
레퍼런스도 미보유 칸을 자물쇠로 그리므로 띠 구조가 그대로 재현된다. 밸런스 영향 없음.
화살표는 등급 4행씩(일반~영웅 / 전설~신화) 페이지를 넘긴다.

레퍼런스에 없는 5·6단계(전설·신화) 등급색은 실측 4종의 채도 흐름을 이어 붙였다(`WGRADE`).

---

## 4. 남은 문제

1. **딤 뒤 배경이 다르다.** 레퍼런스는 뒤에 **06 장비 페이지**(밝은 갈색)가 깔려 딤 후 `#6C5C43` 인데,
   우리는 전투 화면이 깔려 `#29202C` 다. **06 이 구현되기 전에는 회수 불가**이며 05 구간 밖이다.
2. **서브탭 바(장비·스킬·동료·기타)는 모달 밖 y2020~2118 에 있고 06 장비 페이지 소속**이다.
   깃발/리본이 아니라 **완전 캡슐 바 1개를 4등분한 세그먼트 컨트롤**(x53 w961 h99 r49)이라는 것까지
   측정해 뒀으니(측정표 §10) **06 담당이 그 값을 그대로 쓰면 된다.**
3. **아이콘이 이모지다.** 레퍼런스는 칸마다 폭이 다른 일러스트라 A1·A3·A4·01·02 와 같은 한계다.
4. **임시 진입점** — 지금은 영웅 탭의 «장비» 서브탭에서 바로 연다.
   **06 장비 팝업이 들어오면 그 화면의 «무기 슬롯» 이 이 진입점을 가져가야 한다**
   (`openWeapon(id)` 를 호출하면 된다. `herosub` 리스너만 떼면 됨).

---

## 5. 비평 회차

### 1회차 — 비평가 A / B (둘 다 최저 **4/10**)

두 비평가가 **독립적으로 같은 결론**에 도달했다. ⑥폰트만 4점이고 나머지는 전부 7~9점이다.

| 항목 | A | B |
|---|---|---|
| ① 요소 배치 | 9 | 7 |
| ② 상대 크기 비율 | 8 | 6 |
| ③ 여백·간격 | 8 | 6 |
| ④ 정렬·그리드 | 8 | 7 |
| ⑤ 색·명암 | 7 | 6 |
| ⑥ **폰트 크기 비율** | **4** | **4** |

**원인은 하나다.** 이 프로젝트 서체(-apple-system / Malgun Gothic)가 레퍼런스의
**좁고 높은 게임용 한글 서체**보다 넓다. 잉크 «높이»를 맞추면 «폭»이 12~27% 넘치고,
반대로 숫자는 폭이 31~37% 모자란다. A1 비고 2번이 지적한 그 트레이드오프와 같은 뿌리다.

→ PROGRESS 01 교훈대로 **음수 `letter-spacing` 대신 `<i>` 의 `transform:scaleX()`** 로 맞췄다.

| 대상 | 방향 | scaleX |
|---|---|---|
| 이름 알약 / 등급 알약 | 한글이 넓음 | .89 / .93 |
| 표 헤더 「보유 효과」「장착 효과」 | 〃 | .87 |
| 표 라벨 「공격력」 | 〃 | .79 (`transform-origin:0 50%`) |
| 총 보유 효과 줄 | 〃 | .82 (+ font 43 → 44) |
| 상단 재료 진행바 숫자 | 숫자가 좁음 | 1.45 |
| 카드 하단 `0/N` | 〃 | 1.60 |
| 카드 `Lv.n` | 〃 | 1.19 (+ font 25 → 28) |

그 밖에 1회차 지적 중 실제로 고친 것:
- **버튼 면이 단색**이었다 → 실측 세로 그라디언트로 교체 (y1773=211 / 1781=168 / 1869=138 / 1877=190)
- **버튼 비활성 `filter:saturate(.2)`** 가 레퍼런스의 버튼1 라벨색 `#AAEFFF` 를 회청색으로 죽이고 있었다 → 제거
- 효과표 헤더 라벨을 각 열 중심에서 **우측 9 / 4px** 로 이동 (측정표 §4-6 과 일치)

### 1회차 지적 중 «반증»한 것 (그대로 반영했으면 오히려 멀어졌다)

| 지적 | 반증 |
|---|---|
| B: 카드 폭 ref **151** → 구현 **138** (−8.6%) | ref 는 **바깥**(테두리 포함), 구현은 **안쪽**(테두리 제외)을 잰 것. 같은 기준으로 재면 ref 외곽 128~275(148) / 구현 128~275(148), 안쪽 ref 134~270(137) / 구현 133~270(138) — **1px 이내 일치** |
| B: 카드 거터 ref 18 → 구현 32 | 위와 같은 원인. 열 pitch 는 양쪽 다 **170** 으로 일치(A 도 0px 일치로 확인) |
| A: 등급칩 w208 → 199 (−4.3%) | 구현 실측 폭은 **209**. A 는 검정 4px 테두리 «안쪽» 면만 쟀다 |
| A: 이름칩↔등급칩 간격 6 → 11 | 같은 원인. 구현 실측 간격은 **6** |
| B: 잠금 카드에 `Lv.1` 이 있어야 한다(다른 서브에이전트 주장) | ref 3행 카드 Lv 자리(y1455~1488)를 스캔하니 **흰 픽셀 0개**. 레퍼런스는 **미보유 칸에 Lv 를 안 그린다** — 현 구현이 맞다 |
| A: 총효과 초록값 잉크 h31 (구현 40) 이므로 축소해야 한다 | 좁은 초록 마스크는 **JPEG 번짐 때문에 ref 만 작게** 읽힌다. «배경색 아닌 픽셀» 마스크로 양쪽을 같게 재면 ref 42 / 구현 46 → 폰트는 유지하고 **폭만** 줄이는 게 맞았다 |
| A/B: 카드 `Lv.n` 이 과대(+36%) | 흰색 마스크에 **⚔️ 이모지의 은색 픽셀**이 섞인 것. 아이콘을 숨기고 재측정하니 오히려 **−36% 과소**였다. 방향을 반대로 고칠 뻔했다 |

> **교훈: 비평가 수치는 «어디를 기준으로 쟀는지»를 확인하기 전엔 그대로 반영하지 마라.**
> 테두리 안/밖, JPEG vs PNG 마스크, 이모지 오염 — 이 세 가지로 1회차 지적의 **7건이 반대 방향**이었다.

### 2회차 결과 (같은 predicate 로 ref·구현을 동일하게 측정)

| 텍스트 | 폭 오차 | 높이 오차 |
|---|---|---|
| 타이틀 「무기」 | +1% | 0% |
| 표 헤더 「보유 효과」 / 「장착 효과」 | +2% / −3% | +4% / −4% |
| 표 라벨 「공격력」 | 0% | −3% |
| 총 보유 효과 줄 | **0%** | **0%** |
| 카드 `Lv.n` (아이콘 숨기고 측정) | +3% | −5% |
| 카드 `0/N` | 0% | −6% |
| 상단 진행바 숫자 | +2% | −5% |
| 이름 알약 / 등급 알약 | 글자수가 달라 **글자당 폭**으로 검증 → 27.0/27.0 · 21.0/21.0 **일치** | 0% / 0% |

전 텍스트 **±5% 이내**. 요소 좌표는 1회차부터 전부 0~2px 이내를 유지하고 있다.


---

## 6. 2회차 — 비평가 C / D (**5 / 6**, 1회차 4 에서 상승)

| 항목 | C | D |
|---|---|---|
| ① 요소 배치 | 9 | 9 |
| ② 상대 크기 비율 | 6 | 8 |
| ③ 여백·간격 | 9 | 9 |
| ④ 정렬·그리드 | 9 | 8 |
| ⑤ 색·명암 | **5** | 6 |
| ⑥ 폰트 크기 비율 | **5** | 7 |

D 에게 시킨 **집중 검증 6항목 중 5개 합격** — 화살표(63x116 · x14/1003 · y1152) · 카드 테두리 5px + rim 6px ·
장착중 시안 «바깥 어두움 → 안쪽 밝음» 순서 · 캐릭터 뱃지 존재 · 진행바 숫자 중심(cx 687 → 684).
불합격 1건은 버튼 하단 하이라이트 밴드였다.

### ★ C 가 잡아낸 치명적 회귀 1건

**「총 보유 효과」 줄의 초록 픽셀이 ref 3088 개인데 구현은 0 개였다.**
1회차에서 줄 전체를 `scaleX` 하려고 `<i>` 로 감쌌는데, 그 순간 `.wm-tot>b` 라는 **직계 선택자가 끊겨**
초록값 `#B8FE5D` 와 외곽선이 통째로 날아가고 상속된 검정으로 렌더되고 있었다.
→ `.wm-tot b` 로 고쳐 **2906** 복구.

> **교훈: 폭을 맞추려고 래퍼를 끼우면 `>` 직계 선택자가 끊긴다.**
> 이 프로젝트는 `scaleX` 를 자주 쓰므로 다른 화면에서도 그대로 재발한다.
> 래퍼를 넣은 뒤에는 **그 안쪽 요소의 색이 살아 있는지 픽셀로 확인할 것.**

### ★ 측정표 오류 2건 정정 — 글자는 «검정»이 아니라 «흰 코어 + 검정 외곽선»

측정표 §4-6 · §7 은 「공격력」 과 「총 보유 효과:」 를 `#000000` 으로 적었는데, **외곽선을 글자색으로 집은 오류**다.
가로 단면을 뜨면 바깥이 검정이고 안쪽이 흰색이다:

- `공격력` y940 → `#0A0A08` → `#171717` → **`#EEEEEE` `#EFEFEF`** → `#272727` → `#040402`
- `총 보유 효과:` y1705 → `#0A0907` → **`#F6F6F6` `#FFFFFF`** → `#212121` → `#030000`

레퍼런스에 있는데 우리에게 없던 **검정 외곽선**을 전부 넣었다(검정 픽셀 ref / 구현 전 → 후):

| 요소 | ref | 전 | 후 |
|---|---|---|---|
| 이름 알약 | 1704 | **0** | 1515 |
| 등급 알약 | 590 | 71 | 466 |
| 효과표 헤더 | 1442 | **0** | 1291 |
| Lv 알약 | 1541 | 1040 | 1380 |
| 총 보유 효과 줄 | 4886 | — | 4562 |

버튼 라벨도 외곽선 5 → 8px 로 올려 잉크가 **ref 228x60 / 구현 230x60** 으로 일치했다
(직전에는 h43 으로 −15% 였는데, 원인은 폰트 크기가 아니라 **외곽선 두께**였다).

그 밖에: 버튼 하단 하이라이트 `#BEBEBE` 12px → **`#D3D3D3` 7px** (ref y1871~1876 = 211),
카드 하단 `0/N` 에 외곽선 3px, 캐릭터 뱃지 −5x / −10y · font 62 → 56.

### 2회차에서 «반증»한 지적

| 지적 | 반증 |
|---|---|
| C: 카드 폭 148 → ref **150** 으로 넓혀라 | C 자신도 «배경마스크 기준 148x158» 이라 적었다. 150 은 JPEG 번짐 포함값. 측정 담당·A·D 모두 **148** |
| C: Lv 알약 글자 h24 → **h34** 로 키워라 (−29%) | 폰트가 아니라 **외곽선 누락** 때문이었다. 배경 아닌 픽셀로 재면 ref 58 / 구현 58 로 이미 같았다. 폰트를 키웠으면 29% 과대가 됐을 것 |
| C: 모달 높이 1477 → 1467, 하단 테두리 15px → 8px | C 가 읽은 캡처는 flex 리팩터 **이전** 것이다. 현재 `.wm` 은 실측 **1469** 이고 5개 비율에서 전부 확인된다 |
| C: 버튼 라벨 h43 → **h50 으로 폰트를 키워라** | 폭은 이미 179 vs ref 180 으로 일치했다. 폰트를 키웠으면 폭이 넘쳤을 것 — **외곽선**만 두껍게 하는 게 정답이었다 |

### 반응형 결함 1건 (비평가 지적이 아니라 자체 검증에서 발견)

4:3 태블릿(`#app` clamp 하한 **1600**)에서 모달 1469 가 콘텐츠 영역 1316 에 안 들어가 **하단이 잘렸다.**
→ A5 공용 모달과 같은 «위 126 / 아래 42» 패딩 flex 중앙정렬 + `max-height:100%` + 본문 스크롤로 교체.
절대좌표 자식은 안쪽 래퍼 `.wm-in`(858x1347)이 기준이라 눌려도 좌표가 흔들리지 않는다.

**부수 효과로 검증이 하나 생겼다** — 프레임을 레퍼런스와 같은 **2340** 으로 키우면
모달이 **정확히 y478**(레퍼런스 값)에 온다. 9:16 / 9:18 / 9:19.5 / 9:21 / 3:4 다섯 비율 전부 런타임 에러 0.


---

## 7. 3회차 — 비평가 E (**7/10**)

| 항목 | E |
|---|---|
| ① 요소 배치 | 9 |
| ② 상대 크기 비율 | 8 |
| ③ 여백·간격 | 8 |
| ④ 정렬·그리드 | 9 |
| ⑤ 색·명암 | 7 |
| ⑥ 폰트 크기 비율 | 7 |

**집중 검증 4항목 전부 합격** — 초록 픽셀 ref 3377 / 구현 3073(회귀 없음) · 6개 요소 전부 «흰 코어 + 검정 외곽선» ·
버튼 라벨과 하단 하이라이트(211, 7px) · 카드 `0/N` 42x16 → 42x15.

### 3회차 지적 반영 (전부 자체 실측으로 확인)

| 지적 | 조치 | 결과 |
|---|---|---|
| 총효과 줄 +14% 과대 | font 44 → **40**, scaleX .82 → **.90**, 외곽선 4 → 3 | ref 462x42 / 구현 **464x42** |
| 버튼 「장착 중」 폭 +15% | `.wm-b1>i` 에 scaleX **.87** (ref 는 좌버튼만 폰트가 작다) | ref 122x49 / 구현 **122x47** |
| 버튼 하단 «굽» 12px → 8px | 면 그라디언트 스톱 재배치(하이라이트 102~108, 굽 108~116) | ref 6px / 구현 **6px**, 211 값 일치 |
| 화살표 획이 얇다 (면적 2172 vs 3702) | clip-path 안쪽 꼭짓점 37.5% → 50.4% → **68.7%** | 면적 **3264** (충전율 .45 vs ref .51) |
| 등급 알약 그린이 어둡다 | 알약은 카드보다 한 톤 밝다 → `WGRADE.p` 신설 | ref `#9BC437` / 구현 **`#9DC737`** |
| 진행바 슬레이트 B −6 | `#82829A` → **`#8182A0`** | **완전 일치** |

헤더 밴드 h(94 vs 90)는 **반영하지 않았다** — 측정표(y487~578, h92)와 자체 실측이 92 로 일치하고,
E 의 90 은 JPEG 경계 ±2 로 보인다. 여기서 3px 을 움직이면 오히려 멀어진다.

---

## 8. 06 장비 팝업과의 인계 (2026-08-24, 같은 회차 중 완료)

작업 도중 다른 세션이 **06 장비 팝업(`#eqw`)** 을 올렸다. 06 은 «영웅 → 장비» 의 기본 화면이고
슬롯을 누르면 05 로 내려가도록 설계돼 있어, **05 가 쓰던 임시 진입점(영웅→장비 서브탭 직결)을 제거하고**
06 의 무기 슬롯에 정식으로 연결했다.

- 06 의 핸들러는 **건드리지 않고** `#eqCards` 에 추가 리스너만 붙였다(`data-eqslot === 'weapon'` → `openWeapon()`).
- 방어구·반지 등 다른 부위는 해당 화면이 생길 때까지 06 의 기존 동작에 맡긴다.
- 실제 경로 헤드리스 검증: 영웅 탭 → 06 장비 페이지(`eqw:true`) → 무기 슬롯 → **05 열림(`wpn:true`, 카드 20칸,
  장착 중인 «강철 장검» 이 선택된 상태, 버튼 「장착 중」)** → 카드 클릭으로 선택 변경 → 닫기. **런타임 에러 0.**

---

## 9. 최종 상태

| 회차 | 비평가 | 최저 점수 |
|---|---|---|
| 1 | A / B | 4 / 4 |
| 2 | C / D | 5 / 6 |
| 3 | E | **7** |

**9 미달로 종료한다(3/5 회차).** 남은 감점은 아래 두 가지로 수렴하며 **아트 리소스 없이는 회수 불가**다.

1. **무기 아이콘·캐릭터 뱃지가 이모지(⚔️ / 🐱)** — 레퍼런스는 칸마다 폭이 다른 일러스트다.
   E 가 ⑤에서 감점한 항목이 이것 하나로 수렴한다(다른 크롬 색은 «전부 완전 일치» 로 확인됐다:
   크림 240,217,186 · 패널 215,192,161 · 표헤더 92,75,65 · 표본문 247,236,218 · 버튼 그레이·하이라이트).
2. **좁고 높은 게임용 한글 서체 부재** — `scaleX` 로 폭은 맞췄지만 글자 «모양» 자체는 다르다.
   A1 비고 2·A3 와 같은 뿌리다.

레이아웃 수치는 사실상 소진됐다: 전 요소 좌표 **0~2px**, 카드 pitch·열 x·구분선·중심 정렬 **완전 일치**,
텍스트 잉크 **±5%**, 색 **Δ≤2**(이모지 제외).

---

## 10. 2차 폴리시 라운드 — 4회차 (2026-08-25, sess-1034-19527 워커 C)

**전제가 바뀌었다.** 3회차까지는 «최저 항목» 에 ⑤⑥ 이 포함돼 7점으로 종료했지만, 2026-08-25 지시서 개정으로
**통과 판정은 ①②③④ 만으로 한다.** 그리고 기준 화면비가 9:16(1080×1920) → **9:19(1080×2280)** 로 바뀌어
좌표 대응도 «구현 y = ref y − 210» 이 아니라 **«ref y − 84»** 다(작업 63 에서 `#wpnw` 는 이미 전환 완료).

캡처기 `node tools/cap05.js [출력] [--geo]` 를 신설했다(1080×2280 · 상태 주입 · 기하 덤프).
**착수 시점 기하 덤프가 측정표와 0.5px 이내로 일치**한다 — 모달 91/477.5 898×1469 · 정보패널 118/610.5 844×396 ·
카드 pitch 170×190 · 버튼 248/558 y1762.5 274×131 · 화살표 14/1003 y1151.5 63×116.

### 4회차 채점 — 비평가 K / L (①~④ 최저 **7 / 7**, 두 사람 다 병목이 **②**)

| 항목 | K | L |
|---|---|---|
| ① 요소 배치 | 9 | 9 |
| ② 상대 크기 비율 | **7** | **7** |
| ③ 여백·간격 | 9 | 8 |
| ④ 정렬·그리드 | 9 | 9 |
| ⑤ 색·명암 | 8 | 9 |
| ⑥ 폰트 크기 비율 | 6 | 6 |

### 반영한 것 — **두 비평가가 같은 방향으로 몰린 지적만** (LESSONS 52-④)

| # | 지적 | ref 실측 | 4회차 전 | 조치 | 4회차 후 |
|---|---|---|---|---|---|
| 1 | **버튼 안쪽 밝은 링이 좌·우에서 통째로 빠짐** (K·L 독립 지적) | y1828 단면: 검정 → **밝은회색 211 이 7px** → 채움 152 | 검정 다음이 바로 채움(링 **0px**), 회색 면이 좌우로 4px 넓음 | 좌·우·상 3면만 도는 링을 의사요소로 신설(`.wm-btn::before`, 아래 16px 은 비움) + 검정 테두리 6 → **7px**(ref 세로단면 y1763~1769) + 패딩박스 119→117 이라 바닥 기준 스톱 전부 −1 | 검정 7 + 링 7 + 채움 262~ (ref 264, 차 2px 은 ref 검정의 JPEG 번짐) |
| 2 | **페이지 화살표 팔이 20% 얇다** (L ②의 최상위, 3회차 E 도 «면적 2172 vs 3702» 로 지적했던 미회수분) | 잉크 **3718**(fill 50.9%) · y24 가로런 33 · y36 44 · y48 52 | 3256(44.6%) · 27 · 40 · 43 | ref 행 프로파일을 직접 떠서 **셰브런 형상을 다시 유도**(아래 §10-1) — 평평한 윗변 89% · 절개 꼭짓점 77% · 절개 y36.5~63.5% | **3782**(51.8%) · **31 · 42 · 48** |
| 3 | 효과행 라벨 「공격력」 잉크 +9~12% | 77×33 | 81×36 | font 35 → **32**, scaleX .79 → **.82** | **77×33 (Δ0)** |
| 4 | 값 열 우측 정렬이 3~4px 안쪽 | 값 우단 520 / 889 | 516 / 886 | `.wm-tb .v1/.v2` width 180 → **183** | **520 / 890 (Δ0/+1)** |
| 5 | 타이틀 밴드가 2~3px 높다 | 92 | 94 | `.wm-head` top 2 → **1**(글자 보정 −3 → −2) | 92 |
| 6 | 캐릭터 뱃지가 3px 위 | 상단 ref 624 | 환산 −3 | `.wm-cat` top 4 → **6** | **Δ0** |
| 7 | **글자 외곽선이 전부 40~60% 얇다**(K, 4개 요소 실측) — `-webkit-text-stroke` 는 안팎 절반씩 나가므로 «한쪽 5px» 은 **10px** 이 필요하다(A5 교훈의 재발) | 타이틀 한쪽 5.0 · 버튼 6.5 · 초록값 2.5 · 카드 0/N 3.5 | 2.5 · 2.75 · 1.5 · 1.5 | 5→**9** · 5.5→**11** · 3→**5** · 3→**6** | 타이틀 외곽 82×47 → **83×46** · 버튼2 178×51 → **177×49** |
| 8 | 7번의 부작용 — 버튼 라벨 폭 +5px(+2.8%) | 120 / 178 | 125 / 183 | `.wm-btn>i` scaleX **.97** 신설, `.wm-b1>i` .87 → **.845** | **122 / 177** |

### 반영하지 않은 것 — **두 비평가가 서로 반대 방향** (LESSONS 34-④)

| 지적 | K | L | 판정 |
|---|---|---|---|
| Lv 알약 크기 | 145×70 → **줄여라**(ref 144×67) | 143×68 → **키워라**(ref 145×69) | 부호가 반대다. 측정표(139×70 @355,723)와 기하 덤프가 일치하므로 **유지** |
| 캐릭터 뱃지 크기 | −10% → **키워라** | +3.9% → **줄여라** | 부호가 반대. **y 이동(둘 다 «3px 위»)만** 반영 |
| 등급 알약 | 초록 채움 h −2px | 폭 −4px | 둘 다 «약간 작다» 지만 축이 다르고 폭·높이 모두 측정표 209×49 와 일치 → 유지 |

### 이월 (⑥ 계열 — 통과를 막지 않는다)

- **「총 보유 효과」 줄 자폭 +13%**(K #4). 3회차에서 «ref 462×42 / 구현 464×42» 로 총폭을 맞춰 둔 구간이라
  scaleX 를 .90 → .885 로 더 좁히면 **총폭이 ref 보다 11% 좁아진다.** 문자열이 서로 달라(ref `+369.6%` / 캡처 `+4.1%`)
  글자당 폭과 총폭 중 어느 쪽이 옳은지 이 캡처만으로는 못 가린다 → **같은 문자열을 주입해 재측정할 것.**
- **「%」 글리프 폭 +21~26%**(K #5). 숫자 글리프·자간은 양쪽 완전 일치라 **`%` 한 글자만의 서체 차이**다.
  `%` 를 따로 감싸 scaleX 를 걸어야 하는데 `renderWpn()` 의 문자열 조립 4곳을 고쳐야 해서 이월한다.

### 10-1. 화살표 형상 재유도 — «45° 셰브런» 가정이 또 틀렸다

1회차에 «45° 회전으로는 63×116(폭/높이 .54)이 안 나온다» 를 잡고 `clip-path` 로 갔는데, 그 clip-path 자체가
**바깥 모서리를 꼭짓점(100% 0)으로** 두고 있었다. ref 의 행 프로파일을 6px 간격으로 뜨면 그렇지 않다:

| y | 0 | 12 | 24 | 36 | 48 | 54 | 60 | 66 |
|---|---|---|---|---|---|---|---|---|
| ref 잉크 | 56~59 | 42~62 | 30~62 | 19~62 | 7~58 | 0~52 | 0~45 | 6~41 |
| 4회차 전 | 62~62 | 49~62 | 36~62 | 23~62 | 10~52 | 4~45 | 4~45 | 10~52 |

- 바깥변이 **x56 에서 시작하는 평평한 윗변**(폭 7px)이다 — `x = 56 − 1.037y`. 우리 것은 `63 − 1.086y` 라 같은 y 에서 6px 우측.
- 안쪽변은 `(62,y42) → (41,y66)` 기울기 **−0.875**. 대칭화하면 꼭짓점 x48.5 · 절개 반높이 15.7.
- → `polygon(89% 0, 0 50%, 89% 100%, 100% 100%, 100% 63.5%, 77% 50%, 100% 36.5%, 100% 0)`

> **교훈: «면적이 −12% 다» 라는 지적을 `clip-path` 숫자 하나(안쪽 꼭짓점)로 밀어붙이지 마라.**
> 3회차가 그렇게 37.5% → 50.4% → 68.7% 로 세 번 밀었고 면적은 2172 → 3264 로 올랐지만 **팔 두께는 그대로였다.**
> 부족한 것은 «절개 깊이» 가 아니라 **«바깥변의 시작점»** 이었고, 그건 행 프로파일을 떠야만 보인다.
> 실제로 절개를 더 깊게(65%) 하니 면적은 늘고 정점 가로런은 43 → 41 로 **오히려 나빠졌다.**

> **교훈 2: ref 의 «잉크 면적» 은 상한으로만 써라.** ref 는 JPEG 라 어두운 배경 위 흰 글리프가 번져
> 마스크가 실제 도형보다 크게 잡힌다(ref 3718 은 63×116 삼각형의 최대 면적 3654 보다도 크다 — 기하학적으로 불가능).
> 면적을 목표로 맞추면 과보정한다. **모서리 좌표(행 프로파일)를 맞춰라.**
