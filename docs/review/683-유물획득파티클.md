# 683 — 유물 소환 시 «획득한 그 유물» 자리에 해당 유물 파티클 (연속 소환 포함)

> 주인 지시(2026-09-02 00:33) 원문: **«유물소환했을때 해당 유물쪽에 해당 유물 파티클떠야함. 그리고 연속소환일때도 그런식으로 되게»**
>
> 착수: 2026-09-01, `sess-1622-4733` 루틴 워커 B · **5회차(상한 소진) → ⏸ 보류(최고 6점)** ·
> 지시서 [3]-**(다) 연출 작업**(연속 프레임 캡처 + 비평가 2인 독립 · 최저 8 통과)

---

## 0. 한 줄

**제품 `index.html` 3자리**(CSS 부품 `.fx-rlic` · JS `rwGainFx()`/`rwCardShown()` · 호출 1 = 플래시 길이 포함) ·
**레이아웃 Δ0px** · **밸런스·판정 0줄** · 세이브 이관 **없음**(상태를 안 늘렸다).
자: `probe683` **12/12 신설** · `verify683` **35/35 신설** · `probe683b` 신설 · `cap683` 신설 ·
이관 `verify666` **30/30** · `verify682` **22/22** · SMOKE PASS.

⚑ **주인 지시 자체는 충족됐다** — 획득한 그 유물 카드에서 그 유물 문양이 터지고, 연속 소환도 같다
(`verify683` [C1] 이 **한 홀드에서 16/16 버스트**를 매 실행 확인). **막은 것은 ④ 가독성 한 축**이고,
5회차에 비평가 둘이 «단 하나» 를 같은 자리(당첨 칸 «Lv.n»)로 짚은 채 **상한(5회)이 소진됐다** → §5-5.

---

## 1. 재현이 먼저다 (338·341·350·363·372·429·654·655 규칙)

`tools/probe683.js` 를 신설해 **처방을 따르기 전에 제품에게 직접 물었다.**
등재문의 주장 둘(ⓐ 지금은 획득 카드에 파티클이 없다 · ⓑ 연속 소환도 마찬가지)은 **그대로 참**이었다.

| 절 | 물음 | 수리 전 | 수리 후 |
|---|---|---|---|
| [1-a] | 첫 발 — 획득 카드 원점 알 ≥ 1 | **0** (그 버스트의 알 10개가 전부 버튼 발) | **6** |
| [2-a] | 홀드 — 버스트마다 획득 카드 알 ≥ 1 | **0/16** (당첨 9종 전부) | **16/16** |
| [3-a] | 미획득 카드에서 태어난 알 0건 | 0건 | 0건 (구조 축 — 같은 답) |
| [4-a] | 카드 밖 알은 전부 버튼 대역 | 80/80 | 80/80 (구조 축 — 같은 답) |
| [5-a] | 텍스트 플로터 0장 | 0 | 0 (구조 축 — 같은 답) |

**왜 0 이었나** — 666(주인 지시 «유물소환 버튼에서 유물화폐 아이콘 파티클»)이 회당 연출을 **통째로 버튼으로** 옮기면서
`summonRelic()` 본체의 한 줄(`fxUpOk(el, el, …)`)을 걷어냈다. 그 한 줄이 셋을 같이 하고 있었고(칸 플래시 · 칸에서 터지는 스파크 ·
«이름 Lv.n» 텍스트), 주인이 뺀 것은 **텍스트**였는데 «자리» 지시가 버튼이라 **칸 스파크도 같이** 사라졌다.
즉 이 작업은 «666 을 되돌리는 것» 이 아니라 **666 이 옮기지 못한 채널(«어느 유물을 얻었는가»)을 제자리에 다시 세우는 것**이다.
그래서 지불 버스트는 한 줄도 안 건드렸다([4-a]·`verify666` [C1] 이 그것을 매 실행 못박는다).

---

## 2. 처방 — «별도 이미터» 한 벌

`rwGainFx(it, el, first, iv)` 신설. 원점은 **획득한 유물 카드**, 알갱이는 **그 유물의 글리프**(`RELICS[].ic`),
색은 **그 칸의 글로우 색**(`RW_GLOW[i][1]`)이다. `rwSummonFx` 의 첫 줄(이미 그 칸에 플래시를 걸던 자리)에서 부른다.

### 2-1. ⚑ 이 작업의 본체는 «어느 경로로 부르는가» 였다

처음의 자연스러운 안 — `fxBurst(el, col, n, true, iv, null)`(아이콘 없이 구슬로) — 은 **구조적으로 굶는다.**

`.rw-c` 는 `background:radial-gradient(circle at 50% 46%, var(--cg), var(--ce))` 를 가진 칸이다.
`fxbTextHoles()` 의 43회차 «그림 잉크» 판정(`cs.backgroundImage !== 'none'`)은 **호스트 자신**(`els = [t, ...t.querySelectorAll('*')]`)도 보므로
**카드 전체가 keep-out 구멍**이 되고, `strict` 면 거기에 여유 `FXB_KOS`(19px)까지 얹힌다.
그러면 619 4회차의 «끝까지 글자 위인 입자는 버린다» 가 전부를 버리고 6회차의 **«버스트당 보장 1알»만** 남는다 —
«터진다» 가 아니라 **«한 알씩 튄다»** 다.

**660 이 단련 버튼(340×74)에서 실측으로 겪은 바로 그 자리**이고, 그때 세운 처방이
**«아이콘 버스트는 글자 구멍을 안 판다»**(`kh = (r && !IC) ? … : []`)였다. ⇒ 그 길을 그대로 탄다:

```js
fxBurst(el, col, n, true, iv, PAY_CUR.relic);   // ic 를 넘겨 IC 경로로
```

부수 이득 둘이 **덤이 아니라 이 선택의 근거**다:
- 크기가 `FX_CIC_SC`(1.6)로 커지고 그 값이 `fxBurst` **안에서** `sz` 에 되먹여져 **가둠(`inM`)이 저절로 따라온다.**
  밖에서 키우면 잉크만 액자를 넘는다(619 13·14회차가 «잉크가 액자 안에서 끝난다» 로 닫은 축).
- `--fxgs` 잉크 등가 보정(543)이 같이 걸린다.

### 2-2. `strict = true` 인 이유 셋

① **682 규약** — 버스트마다 황금비 저불일치 위상이 돈다(연속 소환이 «같은 그림» 이 되지 않는다) ·
② 융합 회피(`ks`) — 한 칸 안에서 알이 겹쳐 «판때기» 가 되지 않는다 ·
③ 틱 수명(`fxTickLife`) — 한 세대가 다음 틱까지 안 산다.
셋 다 619 가 «회당 발화» 를 위해 세운 축이고, **연속 소환이 정확히 그 상황**이다.
keep-out 만 §2-1 때문에 안 타는데, `IC` 가 있으면 `kh` 가 비어 **버리기 규칙 자체가 안 걸린다**(`if(strict && kh.length && …)`) —
알이 조용히 사라지는 일이 없다(`verify683` [C1] 15/15 가 그것이다).

### 2-3. 마지막 한 걸음 — 그린 것만 바꾼다

`fxBurst` 가 `.fx-cic`(재화 `<img>`)로 낳은 알을 그 자리에서 `.fx-rlic`(그 유물 글리프)로 갈아 끼운다.
660 이 «몸을 새로 만들지 않는다» 로 세운 규약과 **같은 방식**이다.
글리프는 알 상자 «안에» 앉힌다(`RW_GAIN_GS` 0.86 · `verify683` [D3] 이 «font-size ≤ 상자» 로 못박는다).

### 2-4. 화면 밖 가드 (518 재발 방지)

`rwCardShown(r)` — 페이지가 닫혀 있거나(`#relw.on` 아님), 칸이 프레임/89 페이지 상자 밖으로 나가면 **한 알도 안 쏜다.**
`#relw` 는 `overflow:visible` 이라 짧은 프레임에서 격자가 페이지 밖으로 나갈 수 있다.
안 막으면 «쌩뚱맞은 곳에서 이펙트»(518)가 그대로 재발한다. `verify683` [E1] 이 닫힌 페이지에서 **0알 · ret false** 를 단언한다.

### 2-5. 홀드 틱이 자기 간격을 넘긴다

`rwHoldTick()` 이 다음 틱까지의 간격을 **먼저** 정해 `rwSummonFx(it, false, h.iv)` 로 넘긴다(값·타이머는 종전과 같다).
**첫 발에는 안 넘긴다** — 단발이라 «다음 틱» 이 없고, 넘기면 수명이 380 → 193ms 로 반토막 난다.

---

### 2-6. ⚑⚑ 2회차 — 자리·이동·크기를 이 자리에서 다시 적는다

1회차는 «상자를 한 픽셀도 안 바꾸고 `fxBurst` 의 기하를 그대로 쓴다» 였고, **그것이 3점의 뿌리였다**(§5-1).
`fxBurst` 의 요소 갈래는 **큰 카드**(훈련 326×510)에서 온 기하라 151×151 에서는

    탄생 타원 반축  r.w/2 + FXB_M = 81   >   가둠 상자 반폭  r.w/2 − inM = 49

가 되어 **링의 모든 각이 정사각 상자 네 모서리로 clamp** 된다 ⇒ 6알이 **4갈래**로 접히고
이동은 `FXB_K`(0.20) × 중심거리 = **12px** 에 그친다. 비평가 둘이 독립으로 «41/136/222/315° 네 대각 고정 ·
총 이동 14px · 최대 반경 65 < 카드 반폭 75 = 카드에 붙은 데칼» 로 잡은 값이 정확히 이 산수다.
**LESSONS 666-⑥ 이 이미 적어 둔 함정**이다(«버스트가 안 터진다 의 뿌리는 대개 상수가 아니라 요소 대상의 가둠»).

⇒ **666 3·4회차가 지불 버스트에서 쓴 처방 그대로** — `fxBurst` 에게는 «알을 낳는 일» 만 맡기고
자리·이동·크기는 이 자리에서 다시 적는다(같은 파일 `rwSummonFx` 가 이미 그 꼴이고, 공용 상수는 0줄 변경):

| 손잡이 | 2회차 | 3회차 | 4회차 | **5회차** | 왜 그 값인가 |
|---|---|---|---|---|---|
| `RW_GAIN_SC` | .52 | .52 | **.60** | .60 | 상자를 **줄이기만** 한다 — 줄인 상자는 619 13·14회차 가둠 상자의 **부분집합**이라 «잉크가 액자 안» 이 그대로 산다 |
| `RW_GAIN_GS` | .86 | **.62** | **.74** | .74 | 글리프 잉크 ≈ **21px**(카드 151 의 14%) |
| `RW_GAIN_R0` | 16 | **30** | **50** | **38** | 분리 조건 `2·R0·sin(π/n) ≥ 1.3 × 글리프` — n6 → 38/21 = **1.81** |
| `RW_GAIN_R1` | 70 | 80 | 85 | **86** | 끝 잉크 = 86 + 잉크 반폭 ≈ **96 < 100.5**(이웃 칸 가까운 변) · 주행 48px = 잉크의 **2.00배** |
| `RW_GAIN_N` | 3→5 | 5 | 5 | 5 | 홀드 밀도 반토막(1회차 ⓒ). 분리는 `R0` 가 맡는다 |
| `RW_FLASH_MS` | — | — | — | **120** | §2-8 |

⚠ **2회차의 `R1` 70 은 «선언과 실측이 어긋나는» 값이었다**(비평가 지적) — 머리말은 «카드 테를 넘는다» 인데
70 < 75.5 라 중심은 산술적으로 못 넘었다. 침범의 경계도 «이웃 칸 **중심** 88» 이 아니라
**«이웃 칸의 가까운 변» 100.5px**(세로 피치 176 − 카드 151 = 틈 25). 자([C6])도 그 자로 갈아 끼웠다 —
**무르게 푼 것이 아니라 재는 양을 바로잡은 것**이다(침범의 경계는 이웃의 «변» 이지 «중심» 이 아니다).

### 2-7. ⚑⚑ 5회차 — 알이 **양끝**을 갖는다 (검은 채움 + 여덟 방향 흰 테)

3·4회차 ④ 를 막은 것이 이 한 줄이다. 4회차 실측: **획득 알 34개 중 주변 3:1 을 넘는 것 0개**(최악 1.24:1) ·
흰 채움이 **흰 플래시와 한 연결성분**으로 붙는다(카드 내부 22~24% 가 이미 휘도 ≥238).

⚑ **뿌리는 «한 가지 밝기로는 세 바탕을 다 못 이긴다» 이다.** 이 알이 앉는 바탕은 셋이다 —
어두운 카드 배경(≈40) · 컬러 아이콘(≈140~200) · 흰 플래시(≈238~254). 단색 채움 하나로는
그중 하나에서 **반드시** 진다(3회차 = 컬러 복제본이라 아이콘에 짐 · 4회차 = 흰 채움이라 플래시에 짐).

⇒ **알이 양끝을 다 갖게 한다**: 채움은 검정(`brightness(0)`), 테는 **여덟 방향 흰색**.
흰 플래시 위 → **검은 채움**이 오려 내고, 어두운 배경 위 → **흰 테**가 오려 내고, 컬러 아이콘 위 → 둘 다 선다.
대각 4방향을 1.4px 로 더한 것은 «2px 은 실기기 스케일에서 앨리어싱으로 날아간다» 지적을 같이 닫기 위해서다
(**상자는 한 픽셀도 안 키웠다**). ⚠ 컬러 이모지는 `-webkit-text-stroke` 가 **안 먹는다**(글자가 아니라 그림) —
`drop-shadow` 스택이 유일한 외곽선 수단이고, 같은 이유로 `.ol1~.ol5` 유틸도 여기선 못 쓴다.
⚠ **문양(모양)은 그대로 남는다** — 주인 지시의 «해당 유물» 은 모양이 지고, 색 정체성은 마지막 글로우가 진다.

### 2-8. ⚑ 5회차 — 플래시를 340 → 120ms (`RW_FLASH_MS`)

4회차 H 의 «단 하나». **폭·알파는 안 건드렸다** — 그 곡선은 09·12·17·코스튬·장비가 같이 쓰고
LESSONS **666-⑨** 가 «남의 부품 곡선이 «단 하나» 로 남으면 그 행에서 고치지 말고 등재하라» 로 못박은 자리다.
바꾼 것은 **길이 하나**이고, 그것도 새 API 없이 619 14회차의 `fxTickLife(iv, full) = iv × duty` 에
**«원하는 길이 ÷ duty»** 를 넘겨서 얻는다. 결과: 씻기는 구간이 **71% → 35%**.

`verify683` 이 그것들을 매 실행 센다 — [C4](주행 ÷ 자기 잉크 ≥ 1.4) · [C5](잉크가 카드 테를 넘는다) ·
[C6](이웃 칸 가까운 변에 안 닿는다) · [D5](알이 카드 폭의 22% 이하) · [D6](최소 각 간격 ≥ 15°) ·
[D7](탄생 순간 이웃 간격 ≥ 1.3 × 글리프) · **[G1](알 잉크가 검은 채움 ~ 흰 테 양끝을 다 갖는다)** ·
**[H1](연출 봉투 전 구간에서 «Lv.n» ≥ 3:1)**. 문턱 1.3·3:1 은 비평가 처방문의 값 그대로다.
⚠ [C4] 는 4회차에 «절대 px» → **«자기 잉크 대비»** 로 옮겼다 — `R0` 를 밀면 절대 주행은 줄지만
«데칼이 아니다» 라는 뜻은 안 변한다. **무르게 푼 것이 아님은 1회차 값이 새 자에서도 빨간 것**으로 선다
(주행 14 / 잉크 21 = **0.67배** < 1.4).
⚠ [C5] 를 처음엔 «알 **중심**이 카드 밖» 으로 적었다가 `RW_GAIN_R1`(70) < 카드 반폭(75.5) 이라
**내 상수가 내 단언을 만족 불가능하게 만드는** 것을 자가 잡았다 — 619 13회차와 같은 결론으로
자를 **«잉크»** 로 옮겼다([C6] 의 이웃 칸 산수도 같은 자여야 짝이 맞는다).

## 3. 게이트 이관 — «이 화면에는 이미터가 둘이다»

이 작업이 만든 진짜 파장은 제품이 아니라 **자** 쪽이다. 666·682 는 «이 화면의 모든 `.fx-spark`» 를 한 덩어리로 세고 있었고,
둘째 이미터가 서자 **일부러 다르게 행동하는 알**이 그 표에 섞여 들어왔다.

| 자 | 이관 전 | 무엇이 빨갰나 | 이관 후 |
|---|---|---|---|
| `verify666` | 26/30 | [C1] 밖 30/72 · [C2] 격자 발 30알 · [A2]·[A6] 서명 슬라이스 | **30/30** |
| `verify682` | 9/22 → 15/22 | [C1][C2] 갈래·간격 · [D1][D2] 아래로 16알 · [E1] 밖 33/79 · [A0~A6] 전제 실패 | **22/22** |

**333 처방대로 항을 하나도 안 지웠다 — 방향을 좁혔다:**
- `verify666` [C1][C2] 는 이제 **«지불 이미터의 알»** 만 묻는다. 지불 알이 한 알이라도 버튼 밖에서 나면 여전히 빨갛고,
  그것이 666 이 주인에게서 받은 지시의 전부다. **공허한 음성항이 아님**은 [R2] 되돌림이 매 실행 «격자 발 30알» 로 실제로 빨갛게 만들어 보인다.
- `verify682` 도 지불 이미터만 본다. 획득 이미터는 카드를 **둘러싸는 링**이라 상향 원뿔([D])도 버튼 상자([E1])도 **일부러** 안 따른다.

### ⚑ 이관에서 밟은 함정 둘 (다음 세션이 볼 것)

**ⓐ «append 시점에 읽지 마라» 가 클래스에도 걸린다.**
`verify666` 의 `stamp()` 는 `appendChild` 훅이라 **동기**로 돈다 — 그 순간 획득 알은 아직 `.fx-cic` 다(§2-3 의 갈아 끼우기는
`fxBurst` 가 돌아온 **뒤**에 일어난다). 갈래를 append 에서만 읽으면 **획득 알이 지불 알로 잡혀** [C1] 이 «밖 30/72» 로 빨개진다.
666 3회차가 **이동값**에서 이미 배운 교훈(«마이크로태스크로 미뤄 그 프레임의 최종값을 읽는다»)이 **클래스에도 그대로** 적용된다 →
그 microtask 에서 `k`·`img`·`cur`·`txt` 를 같이 최종값으로 갱신했다.
⚠ 갈래를 안 고치고 [C1] 의 문턱만 손댔으면 **«지불 이미터가 버튼 밖에서 나도 초록»** 인 헛초록이 됐을 것이다.

**ⓑ `verify682` 의 «raw» 버킷은 클래스로 못 가른다.**
그 버킷은 `fxBurst` 를 감싸 **후처리 전** 값을 잡는 자리라, 그 시점에는 획득 알이 아직 `.fx-cic` 다.
⇒ `rwGainFx` 가 도는 동안만 깃발(`inGain`)을 세워 그 버스트를 raw 에서 뺐다.

**ⓒ 서명 한 글자에 절 전체가 죽는다.**
`rwSummonFx` 에 셋째 인자 `iv` 가 붙자 `verify682` 가 본문을 «머리 문자열» 로 자르던 133행이 못 찾아
**[A0~A6] 일곱 항이 통째로 전제 실패**로 죽었다(«본문 못 찾았다»). 자르는 자를 정규식으로 넓혔다 —
묻는 것(«이 함수의 본문»)은 한 글자도 안 바뀌었다.

---

## 4. 캡처·정답표 (`node tools/cap683.js r5`)

`cap666.js` 를 물려받아 `cap683.js` 신설(시드 고정 · 트리거는 실제 사용자 경로 · 시간은 `currentTime` 으로 감는다).
바뀐 것은 **무엇을 세는가** 하나 — 두 이미터를 **갈라서** 적는다.
씬 A = 단발(한 궤적의 여덟 시각) · 씬 B = 연속 홀드(실시간 · 세대가 섞이므로 `currentTime` 을 안 감는다).

**«찍힌 잉크 px» 는 2회차에 신설한 자기검산 열**이다 — 방금 찍은 PNG 를 페이지로 되돌려
당첨 카드 상자 안에서 **실제로 칠해진** 밝은 픽셀을 센다(350 처방). DOM 을 세는 열과 이 열이
어긋나면 표가 거짓이라는 뜻이고, 1회차가 정확히 그래서 통째로 날아갔다(§5-1).

| # | t(ms) | 당첨 유물 | 획득 알 | 그 카드 위 | 다른 칸 침범 | 글리프 일치 | 평균 알 상자 | 최대 반경 | **잉크 Δpx** | 지불 알 | 글자 | 플래시 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A1 | 0 | 폭풍의 인장 🌪️ | 6 | 6 | 0 | 6 | 28px | 43px | 10657 | 10 | 0 | 1 |
| A2 | 40 | 〃 | 6 | 6 | 0 | 6 | 28px | 53px | 12488 | 10 | 0 | 1 |
| A3 | 80 | 〃 | 6 | 6 | 0 | 6 | 28px | 63px | 9178 | 10 | 0 | 1 |
| A4 | 130 | 〃 | 6 | 6 | 0 | 6 | 28px | 72px | 9732 | 10 | 0 | **0** |
| A5 | 180 | 〃 | 6 | 6 | 0 | 6 | 28px | 76px | 10059 | 10 | 0 | 0 |
| A6 | 240 | 〃 | 6 | 6 | 0 | 6 | 24px | 81px | 8274 | 10 | 0 | 0 |
| A7 | 290 | 〃 | 6 | 6 | 0 | 6 | 21px | 86px | 7948 | 10 | 0 | 0 |
| A8 | 340 | 〃 | 0 | 0 | 0 | 0 | 0px | 0px | 7883 | 0 | 0 | 0 |
| B1 | 560 | 용의 심장 🫀 | 6 | 6 | 0 | 6 | 28px | 38px | 8290 | 10 | 0 | 1 |
| B2 | 760 | 폭풍의 인장 🌪️ | 5 | 5 | 0 | 5 | 28px | 38px | 11103 | 4 | 0 | 1 |
| B3 | 980 | 황금 왕관 👑 | 5 | 5 | 0 | 5 | 27px | 38px | 8660 | 4 | 0 | 1 |
| B4 | 1240 | 낡은 부적 🪬 | 5 | 5 | 0 | 5 | 30px | 38px | 6845 | 4 | 0 | 1 |

카드 151×151 · 콘솔 에러 **0건**. 단발은 반경 **43 → 86px** 로 날아가 카드 테(75.5)를 넘고,
크기는 28 → 21px 로 사그라든다. **A4(130ms)부터 플래시 0** — 5회차에 120ms 로 줄인 결과다(§2-8).
연속(B)에서는 틱마다 **그때 얻은 유물**의 글리프로 갈린다 — 🫀 · 🌪️ · 👑 · 🪬.

⚠ **«잉크 Δpx» 읽는 법** — «카드 + 24px» 상자의 밝은 픽셀에서 **연출 0 인 정착 화면**의 같은 상자를 뺀 값.
소환하면 그 칸이 «미보유(회색) → 보유(컬러)» 로 바뀌므로 Δ 에 «카드가 밝아진 몫» 이 같이 들어 있고,
**A8(알 0개 · Δ 7883)이 그 몫만 남은 값**이다 ⇒ 파티클 몫 ≈ Δ − 7883.
⚠ B 프레임의 «최대 반경 38px» 은 결함이 아니라 **619 14회차 규약**이다 — 홀드 틱이 빨라지면
(간격 최소 60ms) 알 수명을 그 틱 안으로 자르므로 홀드에서는 «갓 태어난» 순간만 찍힌다.
38px 는 **탄생 링 반경**이고, 그 값이 홀드가 평생 보는 유일한 자리라 §2-6 의 분리 조건이
곧 홀드의 그림을 정한다.

## 5. 비평 (지시서 [3]-(다) · 비평가 2인 독립 · 최저 8 통과 · 상한 5회차)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 1 | A | 3 | 5 | 3 | 4 | **3** | 빈 프레임(=자가 거짓말) |
| 1 | B | 3 | 4 | 4 | 4 | **3** | 〃 |
| 2 | C | 7 | 6 | 6 | 5 | **5** | 탄생 반경 < 글리프(뭉침) |
| 2 | D | 7 | 7 | 8 | 6 | **6** | 〃 |
| 3 | E | 8 | 7 | 8 | 4 | **4** | 알이 숙주 아이콘의 축소 복제본 |
| 3 | F | 8 | 7 | 9 | 5 | **5** | 〃 |
| 4 | G | 8 | 8 | 7 | 5 | **5** | 34알 중 3:1 넘는 것 0개 |
| 4 | H | 8 | 7 | 7 | 6 | **6** | 플래시가 «Lv.n» 을 240ms 덮는다 |
| 5 | I · J | — | — | — | — | *(§5-5)* | |

**추이가 말하는 것** — 기하 축(①②③)은 3회차에 이미 7~9 로 닫혔고, **④ 하나가 세 회차를 끌었다.**
그 셋이 서로 다른 층위였다: «같은 그림»(3회차) → «같은 밝기»(4회차 G) → «밑바탕이 씻긴다»(4회차 H).

### 5-1. 1회차 — 3 / 3 · **«계측기부터 고쳐라»**

넷이 공통: ⓐ 알이 너무 크다(잉크 37~55px = 카드의 27~39% · 여섯 알 bbox 합이 카드 면의 **67~68%**) ·
ⓑ 안 퍼진다(각이 41/136/222/315° **네 대각 고정** · 총 이동 **14px** · 최대 반경 65 < 카드 반폭 75) · ⓒ 홀드 밀도 반토막.

⚑ **그리고 둘 다 «B1 프레임에는 아무것도 없다» 고 했는데 내 정답표는 «획득 6알» 이라고 적고 있었다.**
`probe683b` 신설(350 처방)로 가르니 **당첨 칸 258px vs 정상 발화 8285px** ⇒ **비평가가 옳았다.**
뿌리 둘: 가시성 문턱 `opacity > 0.06` 이 «거의 투명한 잔해» 를 셌고, 표본 420ms 가 하필
«첫 발(380ms)이 죽는 시각 ↔ 첫 틱(350ms)이 사는 시각» 의 **이음매**였다.
LESSONS **666-⑧**(«표와 그림 중 하나가 거짓이면 표부터 의심하라»)이 같은 화면에서 **두 번째로** 맞았다.

### 5-2. 2회차 — 5 / 6 · **«탄생 반경이 글리프보다 작다»**

둘이 독립으로 같은 «단 하나» 를 짚었다: 탄생 링 `R0`(16~18px) < 글리프(22~24px) ⇒ 링 위 이웃 간격
`2·R·sin(π/n)` 이 **16px** 인데 글리프가 24px 라 **겹치지 않게 놓는 것이 기하학적으로 불가능**했다.
실측 — A1 여섯 알이 **연결성분 1개**(57×49px) · 원반 채움 **122%** · 홀드 대비 **1.2~9.9%**.
그리고 비평가가 **내 머리말의 산수 오류**를 잡았다(«카드 테 75.5 를 넘는다» 인데 `R1` 이 70 < 75.5).
침범 경계도 «이웃 **중심** 88» 이 아니라 **«이웃 칸의 가까운 변» 100.5px** 이었다.

⚑⚑ **그리고 이번에도 «자» 가 점수를 깎아먹고 있었다 — 두 사람 몫을 같이.**
둘 다 «버스트가 제 숙주보다 130ms 먼저 켜진다» 를 감점했는데 **제품이 아니라 하네스**다:
`document.getAnimations()` **전부**에 `currentTime = T` 를 걸어 카드의 «등장» 애니까지 되감았다.
⇒ `#fxl` 안만 T 로 감고 나머지는 `finish()`. 같이 잡힌 것 하나 더 — «찍힌 잉크 px» 가 카드 자기
아이콘까지 세고 있었다(«A8 은 0알인데 7,030px») ⇒ **기준선을 찍어 Δ 로**, 재는 상자는 카드 + 24px.

### 5-3. 3회차 — 4 / 5 · **«알이 숙주 아이콘의 축소 복제본이다»**

기하 축은 닫혔다(①8·8 ②7·7 ③8·9). ④ 만 4·5 였고 둘이 같은 것을 실측으로 짚었다:
알이 **자기가 올라탄 아이콘과 같은 그림·같은 팔레트**라 도형-바탕이 안 갈린다 —
🐂 위 🐂 = **2.4%** · 🫀 위 🫀 = **0.7%** · 🎖️ 위 🎖️ = 4.6% ⇒ «소 다섯 마리» 가 아니라 «흰 가로줄 다섯».
게다가 탄생 링이 ⌀60 인데 숙주 실루엣이 ⌀95~132 라 **탄생 위치 100% 가 아이콘 «안»** 이었다.

### 5-4. 4회차 — 5 / 6 · **둘의 «단 하나» 가 갈렸고 둘 다 맞았다**

- **G — 알 대비**: 획득 알 **34개 중 주변 3:1(WCAG 비텍스트 최소)을 넘는 것 0개**(최악 1.24:1).
  흰 채움이 **흰 플래시와 한 연결성분**으로 붙는다(카드 내부 22~24% 가 이미 휘도 ≥238).
- **H — 플래시**: «Lv.n» 대비가 정착 **9.17:1 → 1.48:1**(84% 손실)로 **연출의 71%(240ms) 동안** 3:1 미달.
  아이콘도 4.98 → **1.60:1**. ⇒ «보상이 떨어지는 바로 그 순간, «어느 유물인가» 가 카드에서
  **가장 대비 낮은 요소**가 되고 알갱이가 가장 높다».
  ⚑ H 는 동시에 «알 테/속 Δ246(96%) · 글리프 일치 12/12» 로 **3회차 지적이 해소됐음**을 확인했다.

⇒ 5회차가 둘을 같이 받았다: 알은 **양끝**을 갖게(§2-7), 플래시는 **짧게**(§2-8).

### 5-5. 5회차 — 6 / 6 · **여기서 상한(5회)이 소진됐다 → ⏸ 보류**

**고친 축은 닫혔다** — 3·4회차를 막던 «밑바탕 분리» 가 두 비평가 모두에게서 해소로 확인됐다:
I «23알 **전부** 가 {검은 코어:바탕, 흰 테:바탕} 중 하나에서 **3:1 이상** — 직전 회차 «34알 중 0개» → **23/23**» ·
J «어느 바탕에서도 양끝 중 하나가 최소 **4.3:1** · 내부 코어:테 상시 **21:1**». 문양 식별도 **12/12**.

**그런데 둘 다 «단 하나» 를 같은 자리로 옮겨 짚었다 — 당첨 칸 «Lv.n».**
340ms 중 **240ms** 동안 안 읽히고, **원인이 둘로 갈린다**:

| 구간 | 범인 | 실측 |
|---|---|---|
| 0~120ms | **플래시** | 국소대비 17.49:1 → **2.27:1**(A2) · 글자 검은 테 **27~36% 표백** |
| 130~240ms | **알갱이** | 플래시가 이미 **0** 인데 3.07~3.34:1 · 알 하나가 «L» 글리프의 **52~56%** 를 덮는다 |

⚑ **우연이 아니라 기하다**(I 의 진단) — 라벨은 카드중심에서 아래로 **59~94px** 인데 링 반경이
t=130~290ms 에 **70~86px** 이라 **링 하단 호가 매번 라벨을 훑는다**. 그래서 5회차의 플래시 단축(§2-8)이
ⓐ 는 줄였지만 ⓑ 는 **원리상 안 줄어든다** — 반경으로는 못 푼다(라벨 상단이 중심에서 59px, 알 반지름 13px ⇒
캡 46px 이하가 필요해 링이 카드 안에 갇힌다). **처방은 «자리» 다**: 아래쪽 섹터 배제 또는 라벨 띠 클램프 +
«Lv.n» 을 연출 레이어 위로. 상세는 PROGRESS 683 행 «남은 문제».

**그리고 다섯 번째 자 결함을 I 가 잡았다** — 씬 B 네 장이 `open(SEED + idx)` 로 **시드까지 바꿔** 찍혀
«한 홀드의 연속 네 틱» 이 아니었다(비당첨 칸 레벨이 **0→0→2→0** 으로 오르내려 들켰다).
그 캡처로는 주인 지시의 후반도 «한 세대가 다음 틱까지 안 산다» 도 **증명할 수 없다**.
⇒ 시드를 고정하고 **0 → 그 시각까지 끊지 않고 누른 뒤 얼려서** 찍도록 고쳤다(정답표에 «실측 t» 와
«보유 레벨 합» 을 같이 찍는다). ⚠ 러너 부하로 틱 수가 흔들려 레벨 합이 **완전한 단조는 아니다** —
그 숫자를 그대로 드러낸다(58 38회차 «흐리게 잴 바에는 흐린 값을 밝혀라»).
**연속성의 진짜 증거는 캡처가 아니라 자에 있다** — `verify683` [C1] 이 **한 홀드에서 16/16 버스트**를,
`probe683` [2-a] 가 같은 것을 매 실행 확인한다.

## 6. 게이트·회귀

| 자 | 결과 | 비고 |
|---|---|---|
| `tools/verify683.js` | **35/35 신설** | 등재문 요구 4개 + [C4~C6] 기하 · [D5~D7] 크기·갈래·탄생분리 · **[G] 잉크 양끝** · **[H] 라벨 대비** · 되돌림 [R1][R2][G2][H3] |
| `tools/probe683.js` | **12/12 신설** | 재현(수리 전 [1-a]·[2-a] 빨강이 정상) |
| `tools/probe683b.js` | 신설 | 350 «찍힌 픽셀» — 1회차의 «표가 거짓» 을 가른 자 |
| `tools/cap683.js` | 신설 | 연속 프레임 A 8장 + 홀드 B 4장 · 기준선 Δ · 실측 t |
| `tools/verify666.js` | **30/30** | 이관(«전부» → «지불 이미터» · 갈래 최종 재독) |
| `tools/verify682.js` | **22/22** | 이관(지불 이미터만 · 서명 슬라이스) |
| `verify660` · `verify619` · `verify625` | 55/55 · 53/53 · 24/24 | 무수정 |
| `verify488` · `verify518` · `verify583` | 80/80 · 42/42 · 37/37 | 무수정 |
| `verify191` · `verify330` · `verify100` | 50/50 · 20/20 · 44/44 | 무수정(89 화면 회귀) |
| `node tools/smoke.js` | **SMOKE PASS** | |
| 화면비 5종(1600·1842·1920·2280·2600) | 가드 5/5 발화 | `rwCardShown` 가 짧은 프레임에서 연출을 조용히 죽이지 않는다 |

### ⚑ 되돌림이 이 작업의 보증이다

| 항 | 되돌리면 | 지금 |
|---|---|---|
| [R1]/[R2] | `rwGainFx` 를 빼면 획득 알 **0** · 원복하면 다시 난다 | 6알 |
| [G2] | 실루엣·림을 걷으면 잉크 휘도 폭 **155** | **255**(0~255 양끝) |
| [H3] | 옛 340ms 플래시면 같은 시각 라벨 **2.32:1** | **9.64:1** |

[H3] 의 2.32:1 이 비평가가 눈으로 재던 1.48~1.73 대역과 같은 자리라는 것이,
이 자가 «원래 참인 것» 을 굳힌 항이 아님을 못박는다(338 이 등재문 처방에서 배운 함정).

### 곁다리 등재 2건
- **728** — `tools/verify664.js` 40/43. **착수 전 트리(`git stash`)에서도 같은 40/43·같은 3건**(338·344·369·372 규약).
  뿌리는 **662 가 완료되어 «미보유 스킬 세부» 를 열었는데** 664 가 아직 «닫혀 있어야 한다» 를 음성항으로 든 것.
- **733** — `docs/LESSONS.md` 가 **2.5MB · 25,424줄 → 0** 으로 비워져 있었다(`2e8c90a` 가 자기 주제와 무관하게 지웠다).
  마지막 정상판에서 복구해 push 했고, `verifyProgress` 네 절이 전부 `PROGRESS.md` 만 보느라 **아무 자도 안 짖은 것**을 재발 방지로 등재했다.
  ⚑ 이 행을 쓰는 시점에 그 자에 **§5 «추적 파일 급감»** 절이 실제로 생겼다.

## 7. 되돌림

`rwSummonFx` 첫 줄의 `rwGainFx(...)` 호출 한 줄을 지우면 **666 상태와 픽셀 동일**하다.
그 자리는 `verify683` [R1] 이 매 실행 실제로 되돌려 «획득 알 0» 을 확인하고, [R2] 가 원복해 «다시 난다» 를 확인한다
(= [R1] 이 «항상 0» 을 재는 헛초록이 아니다).

---

# 9. 9회차 — 두 비평가의 «단 하나» 를 재현이 갈랐다 (2026-09-03, `sess-0758-20031` 루틴 워커 D)

> **제품 `index.html` 0줄** · CSS 0줄 · 레이아웃 Δ0px · 게이트·회귀 **전부 무수정**.
> 신설 `tools/probe683c.js` **9/9** · 브리핑 `docs/review/683-비평브리핑-r9.md`.
> **결과: ①~④ 최저 5 (CL 6/6/8/3 · CM 6/6/7/5) → ⏸ 보류(9/10) · 남은 축은 862 로 등재.**

## 9-1. 왜 9회차가 돌았나

8회차는 «④ 의 주범이 알갱이 밖으로 나갔다»(P·Q 공통 — 카드 전면 흰 판)로 끝났고,
그 축은 **788**(자가 헛초록) → **795**(라벨을 플래시 위에 되그린다)로 옮겨 **둘 다 완료**됐다.
착수 시점 `verify683` **40/40 PASS** 이고 [H] 의 ⏸ 대기 항까지 **21:1 = 정착의 100%** 로 읽힌다.
⇒ «자는 닫혔다고 한다. 눈도 그런가» 를 묻는 회차다.

## 9-2. 채점 (지시서 [3]-(다) · 2인 독립 · 8회차에서 이어 센다)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 8 | P | 7 | 8 | 8 | 4 | **4** | 라벨 대비 2.25~3.41:1 |
| 8 | Q | 6 | 8 | 7 | 4 | **4** | 주범은 알갱이가 아니라 **카드 전면 흰 판** |
| **9** | **CL** | 6 | 6 | **8** | **3** | **3** | «Lv.n» 획↔국소배경 **4.29:1**(정착 20.67) |
| **9** | **CM** | 6 | 6 | 7 | **5** | **5** | 흰 판이 **등급 테를 100% 덮는다**(씬 B 잔존 0px) |

**추이 — 3·4 → 4·4 → 3·5.** ①②③ 은 8회차와 같은 대역(6~8)에서 안정됐고 **④ 하나가 남았다.**
두 사람이 **처음으로 서로 다른 자리**를 짚었다(8회차까지는 늘 같은 자리였다) — 그래서 이 회차는
«수리» 가 아니라 **«둘 중 무엇이 참인가» 를 가르는 회차**가 됐다(338 규약).

## 9-3. 재현 — `tools/probe683c.js` (처방 전에 제품에게 직접 물었다)

⚠ 먼저 **자기검산**부터 세웠다. 1회차의 사고(자가 «연출이 없는 프레임» 을 재고 정답표가 거짓이 됨)를
반복하지 않으려면, 이 자가 **비평가가 본 것과 같은 프레임**을 재고 있음을 먼저 못박아야 한다.

| 자기검산 | 이 자(rl0) | 실캡처 `683-r9-A1/A8.png`(폭풍의 인장) |
|---|---|---|
| 카드 좌하 코너 10×10 평균 휘도 — t0 ↔ 정착 | **0.85 ↔ 0.06** | **0.943 ↔ 0.010** |

**같은 그림이다.** ⇒ 아래 값들의 차이는 «프레임» 이 아니라 **«축»** 이다.

| 물음 | 비평가가 적은 값 | `probe683c` 실측 | 판정 |
|---|---|---|---|
| 「Lv.n」 획 ↔ **국소 배경**(3~4px 중앙값) t0 | CL **4.29:1**(정착 20.67) | **15.26:1**(정착 17.71) · 4.5:1 미만 **0%** | **재현 안 됨** |
| 라벨 채움 화소 중 **알에 눌린** 비율 t0 | CM **52.7%**(347/658) | **0.5%**(2/370) | **재현 안 됨** |
| 카드 **액자선(등급 테)** 열의 정착색 잔존 t0 | CM «씬 B 0px» | **3.1%**(56/1812) | **확인** |

⚑ **795 는 실제로 듣고 있다** — 라벨 축 둘이 다 정착 대역이다. 자([H1] 21:1)와 이 자(15.26:1)가
같은 방향을 가리키고, CL 의 4.29:1 은 **획 주변을 어디까지 «배경» 으로 세는가**에서 갈린 값이다
(CL 이 근거로 든 코너 휘도 0.943 자체는 이 자도 0.85 로 같이 찍었다 — 어긋난 것은 관측이 아니라 마스크다).
⇒ **CM ⓐ 하나만 남는다.**

## 9-4. ⚑⚑ 그 하나의 손잡이를 자가 **기각**했다

`fxFlash` 의 셋째 인자 `inset` 은 619 18회차가 **바로 이 병**(«흰 테가 호스트 액자선을 먹는다»)을
풀려고 만든 opt-in 이다 — «들이는 양 = 호스트가 자기 액자를 그리는 inset 그림자의 spread»(`fxRingIn`).
683 의 호출은 지금 `inset` 을 **안 준다**. 켜 보면:

| A/B (t0) | 등급 테 잔존 | 라벨 획↔주변 중앙값 | 4.5:1 미만 |
|---|---|---|---|
| 현행 | **3.1%** | **15.26:1** | 0% |
| `inset` 켬 | **38.3%** | **3.77:1** | **75%** |
| 패치를 알 위로 | 3.1% | 15.26:1 | 7% |
| `inset` + 패치 올림 | 38.3% | 3.77:1 | 62% |

**등급 테를 사면 라벨을 잃는다.** 뿌리는 **두께 비** 하나다:

- 유물 카드의 액자 링 = `box-shadow: rgb(138,103,50) 0 0 0 **2px** inset` ⇒ `fxRingIn()` = **2px**
- `.fx-flash` 의 흰 테 = **9px** (index.html 4404) = 링의 **4.5배**

619 의 호스트 셋은 링이 **8px** 이라 «링만큼 들이면 흰 테가 [8,17] 에 앉아 액자선(0~8)이 산다» 가
성립했다. 링이 **2px** 인 호스트에서는 들여도 흰 테가 **[2,11]** 을 먹어 **액자선을 못 살리고**
상자만 라벨 쪽으로 2px 밀려 **라벨까지 잃는다**. ⇒ 619 18회차의 처방은
**«흰 테 ≤ 링» 일 때만 성립하는 처방**이었다.

그 흰 테는 `.fx-flash` **공용 CSS**(09·12·17·코스튬·장비·훈련·단련·룬)라 이 행에서 안 고친다
(LESSONS 666-⑨ · 788·795 가 같은 이유로 각자 등재된 자리다) ⇒ **862 등재.**

## 9-5. 남은 ④ 감점의 나머지는 **주인 지시 그 자체**다

실캡처에서 아이콘 면(600,830 120×80)의 평균 휘도가 **정착 0.558 → t0 0.187** 로 내려간다.
CL «아이콘 식별 상관계수 −0.159» · CM «7.58:1 → 3.46:1» 이 같은 것을 봤다.
이것은 **알**이다 — «그 유물의 글리프 · 아이콘과 같은 크기 · 아이콘 중심에서 · 1개»(683 원문 + 753 ②③)를
지키면 태어나는 순간 아이콘 위에 자기 자신이 겹친다. 7회차가 남은 손잡이(α)를 이미 1.00 → **0.55** 로 썼다.
**753 표의 둘째 줄이 이 자리를 «주인 지시 그 자체» 로 적어 둔 그대로다** — 이 행에서 더 갈 데가 없다.

## 9-6. 그래서 ⏸ 보류(9/10)로 이어 둔다

**남은 1회차는 862 뒤에 쓴다.** 862 가 흰 테를 호스트 링에서 파생시키면 등급 테가 살아나고,
그때 라벨 축이 안 흔들리는지는 이 자([3] A/B)와 `verify683` [H1][H4] 가 같이 답한다.

⚠ **다음 세션이 볼 것** — 비평가에게 브리핑을 줄 때 §3 «주인 지시로 고정된 넷» 을 반드시 같이 줘라.
9회차에도 CL 이 «알이 숙주를 안 벗어난다(총 이탈 27px = 아이콘의 21%)» 를 ③ 감점으로 적었는데,
그 이탈 상한은 `RW_GAIN_R1` = 48 이고 그 값은 **이웃 칸(100.5px)이 정한 것**이다(753 §2-2).

## 9-7. 게이트·회귀 (제품 0줄이므로 전부 무수정)

`verify683` **40/40** · `verify753` **32/32** · `verify666` **32/32** · `verify682` **25/25** ·
`verify681` **39/39** · `probe683c` **9/9 신설** · **SMOKE PASS**.

---

# 10. 10회차 — 862 가 닫힌 것을 눈으로 확인했고, **자기 자를 고쳤다** (2026-09-06, `sess-0443-8321` 루틴 워커 C)

> **제품 `index.html` 0줄** · CSS 0줄 · 레이아웃 Δ0px.
> `probe683c` **9/9 PASS**(자 3항 이관 + 마스크 결함 수리) · 브리핑 `docs/review/683-비평브리핑-r10.md`.
> **결과: ①~④ 최저 4 (CN 6/6/8/5 · CO 6/7/7/4) → ⏸ 보류(10/10 소진 · 라운드 마감).**

## 10-1. 착수 — 9회차가 남긴 «단 하나» 는 실제로 닫혔다

9회차는 CM 의 «흰 판이 등급 테를 100% 덮는다» 하나만 남기고 나머지를 재현으로 기각했고,
그 뿌리(공용 `.fx-flash` 의 흰 테 9px ↔ 유물 카드 액자 링 2px)를 **862** 로 등재했다.
**862 완료(2026-09-03)** 뒤 실측:

| 축 | 9회차 | 10회차 | 잰 자 |
|---|---|---|---|
| 카드 등급 테 열 정착색 잔존(t0) | **3.1%** | **100%** | `probe683c` [2] |
| `inset` A/B 차이 | 3.1 → 38.3% | **차이 0**(들이기가 기본) | `probe683c` [2] |
| `inset` 의 라벨 대가 | 15.26 → **3.77:1** | **대가 0**(11.07 ↔ 11.07) | `probe683c` [2-c] |

비평가도 독립으로 같은 것을 봤다 — **CN**: 액자 띠 1086px 색 잔존 **A1 95.0% · A2 98.3% · A3 98.8%**,
«9회차 CM 의 100% 덮음은 재현되지 않는다». ⇒ **862 는 눈으로도 닫혔다.**

## 10-2. 채점 (지시서 [3]-(다) · 2인 독립 · 9회차에서 이어 센다)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 8 | P | 7 | 8 | 8 | 4 | **4** | 라벨 대비 2.25~3.41:1 |
| 8 | Q | 6 | 8 | 7 | 4 | **4** | 주범은 알갱이가 아니라 카드 전면 흰 판 |
| 9 | CL | 6 | 6 | 8 | 3 | **3** | «Lv.n» 획↔국소배경 4.29:1 |
| 9 | CM | 6 | 6 | 7 | 5 | **5** | 흰 판이 등급 테를 100% 덮는다 |
| **10** | **CN** | 6 | 6 | **8** | **5** | **5** | 봉투 380ms 중 **140ms(37%)가 죽은 고원** |
| **10** | **CO** | 6 | 7 | 7 | **4** | **4** | 「Lv.n」 **카드 위 획 71% 가 1.19:1** |

**추이 — 3·4 → 4·4 → 3·5 → 4·5.** ③ 은 처음으로 **8** 이 나왔고(CN — 이웃 9칸 `max|ΔRGB| ≤ 1.0`,
임계 10 초과 픽셀 **0개**), ④ 가 여전히 최저다. ⚑ **9회차와 달리 두 사람이 다시 같은 자리를 짚었다** —
CN [④-3] 「중앙값 11.82 → 4.14/3.49 · «4.5:1 미만» 18.5% → 53.5%/64.0%」 ↔
CO [2] 「카드 위 획 531/747px(71.1%)이 9.19 → **1.23:1**」. **부호도 축도 같다.**

## 10-3. ⚑⚑ 이 회차의 본체 — **비평가가 내 자의 결함을 잡았다**

CN 이 브리핑 §5 를 읽고 이렇게 적었다:

> 「중앙값 5.16:1 인데 «4.5:1 미만» 이 2%」 는 픽셀 모집단에서는 성립하기 어렵다.
> **자와 눈이 갈리는 게 아니라, 두 자의 모집단이 다르다.**

맞았다. `probe683c` 의 [3] 은 잉크 마스크를 **프레임마다 새로** 떴다
(`|프레임 − 라벨 지운 사본| ≥ 24`). 그러면 **씻겨서 배경과 같아진 획은 그 순간 표본에서 빠진다** —
남은(=아직 잘 보이는) 획만 세는 **생존자 편향**이다. 두 비평가는 각자 독립으로
**정착(A8)에서 뜬 고정 마스크**를 전 프레임에 얹었고(CN 889px · CO 747px), 그 방법이 옳다.

마스크를 정착에서 한 번만 뜨게 고치자 **세 가지가 한꺼번에 제자리를 찾았다**:

| 축(t0 · 알 켬) | 움직이는 마스크(옛 자) | **고정 마스크(고친 자)** |
|---|---|---|
| 중앙값 | 5.16:1 | **11.07:1** |
| «4.5:1 미만» | 2% | **31%**(정착 14%) |
| 패치 걷기 되돌림 | 5.16:1 (**배수 1.00 — 안 듣는다**) | **5.15:1 (배수 2.15 — 듣는다)** |
| 꼬리 t20·t40 «4.5:1 미만» | **62%**(→ «꼬리가 최악») | **28 · 26%**(→ **봉우리가 최악**) |

⚑ **9회차가 «CL 의 4.29:1 은 재현 안 됨» 으로 닫은 것이 이 편향 때문이었다.** 고정 마스크로 재면
t0 중앙값 11.07 · «4.5:1 미만» 31% 로, CL(4.29)·CN(4.14)·CO(1.23, 카드 위만) 이 짚은 **방향이 참**이다
— 세 사람이 세 회차에 걸쳐 같은 것을 봤고, 자만 못 보고 있었다.

⚑ **그리고 내가 이 회차에 그 편향 위에 자를 한 번 더 세울 뻔했다.** [3-a] 를 두 번 옮겼는데
첫 시도(되돌림 배수 3)를 «이 축은 패치에 반응 안 한다(배수 1.00)» 로 **스스로 기각**했었다.
반응 안 하던 이유가 축이 아니라 **마스크**였다 — 마스크를 고치자 같은 되돌림이 배수 **2.15** 로 듣는다.
⇒ **«자가 안 움직인다» 는 축을 버릴 근거가 아니라 자를 의심할 근거다.**

## 10-4. 자 이관 (제품 0줄 · `probe683c` 9/9)

| 항 | 9회차 | 10회차 |
|---|---|---|
| [2-a] | «CM ⓐ 는 실재한다»(잔존 &lt;10%) | **«862 로 닫혔다»**(잔존 ≥90% · 실측 100%) |
| [2-b] | «`inset` 은 되살리기는 한다» | **«`inset` 은 이제 아무 일도 안 한다»**(차이 ≤5%p) |
| [2-c] | «그 대가로 라벨을 잃는다» | **«그 대가가 사라졌다»**(상대 5% 안) |
| [3-a] | «t0 중앙값 ≥ 정착의 절반» | **«패치를 걷으면 2/3 아래로 내려간다»**(배수 1.5 · 고정 마스크) |
| [3] 마스크 | 프레임마다 재계산(**생존자 편향**) | **정착에서 한 번만**(CN·CO 방법) |

⚠ **셋 다 지우지 않고 방향만 뒤집었다**(333 처방). 9회차 주석이 «862 가 흰 테를 링에서 파생시키면
이 항은 뒤집힌다 — 그때 지우지 말고 방향만 뒤집어라» 고 적어 둔 그대로다. 지웠으면
«대가가 있었다» 는 사실이 사라져 누가 흰 테를 다시 손 상수로 되돌려도 아무 자도 안 짖는다.

## 10-5. 남은 ④ — **패치가 못 덮는 잔량 +17%p**

795 의 라벨 패치는 **듣고 있다**(«4.5:1 미만» 걷으면 40% → 지금 **31%** · 중앙값 5.15 → **11.07**).
그런데 정착은 **14%** 다 ⇒ 잔량 **+17%p**. 이것이 이 행에 남은 ④ 감점이고,
`probe683c` 가 매 실행 **⏸ 로 찍는다**(실패로 안 센다 — `verify683` [H1] 의 ⏸ 규약과 같은 꼴).

⚠ **CO 의 처방(«`fxFlashKeep` 을 `.rw-c>u` 에도 적용하라»)은 이미 되어 있다** —
`FXKEEP_TXT = '.rw-c>u'`(index.html 44911, 795 가 넣었다). 재현이 그것을 확인한다(배수 2.15).
즉 남은 것은 «패치가 없다» 가 아니라 **«패치가 덮는 범위가 모자란다»** 이고,
그 범위는 **호스트 패딩 상자**가 잡는다(44990 «그릇을 재현한다») — 라벨은 카드 하변 **밖으로**
걸쳐 있어 그 그릇이 아랫부분을 안 담는다. 다음 라운드가 볼 첫 자리다.

## 10-6. 두 비평가가 **독립으로 같이 짚은** 나머지 (다음 라운드 입력)

1. **봉투가 «이징» 이 아니라 «유지 후 절단»** — CN 「|ΔL|&gt;40 픽셀 A5 2905 → A6 **321**(60ms 에 −89%)」
   ↔ CO 「max|ΔL| 116.0 → 40.8(남은 진폭의 **65%** 를 한 칸에 버린다) · 알 상자 −0.14px/ms → **−1.68px/ms(12.0배)**」.
   선언 수명 380ms 중 **보이는 것은 243ms(64%)** 뿐이다. 손잡이는 **`@keyframes fxRlic` 의 α 재분배**
   (683 전용 곡선 — 공용 아님) · 868 이 `fxFlash` 에 쓴 처방과 같은 꼴.
2. **축소 곡선이 그림에 없다** — CN 「알 발자국이 A1~A5 내내 140~144×120px 로 **한 번도 안 준다**」
   ↔ CO 「탄생 상자의 **64%(101/158px)인 채로 잘린다**」. 저작된 1.00→0.45 중 **0.60~0.45 구간(33%)이
   아예 렌더 안 된다**(α 하한 0.25 가 t≈243ms 에 온다).
3. **씬 B 가 ① 을 판정할 수 없는 자료다** — 아래 §10-7 (**975 등재**).
4. **연출이 상품을 어둡게 만든다**(CN [④-1]) — 130ms 에 밝아진 1452px(+61) ↔ 어두워진 **4632px(−78)**,
   어둠:빛 **3.2:1**. «획득» 어휘와 반대다. 손잡이는 알 레이어의 합성 모드(§3 고정 넷과 충돌 없음).
5. **주행이 좌하 한 방향이고 그 끝이 라벨 위다**(CN [③-3] · CO [3]) — 방향 **170.4°**,
   A6 알 덩어리가 «Lv.1» 글자띠(y≥1190) 코앞 y1189 까지 내려온다. ⇒ **④ 와 ③ 이 같은 손잡이**를 공유한다.
   ⚠ 두 사람이 753 §2-2 의 «이웃 중심 간 100.5px» 이 이 캡처와 안 맞는다고 **각자** 적었다
   (CN: 열 피치 **248px** · 빈 복도 좌 96 / 우 98px · CO: 최근접 변 간 세로 26 / 가로 97px).
   **48px 상한의 근거 수치부터 다시 재야 한다.**

## 10-7. 곁다리 등재 1건 — **975**(씬 B 하네스)

두 비평가가 **각자 독립으로** 같은 것을 찍었다:
**CN** 「용의 심장 카드 상자 22,801px 에서 **B1 ↔ B3 ↔ B4 차이 0픽셀(0.00%)** · 평균 |ΔL| 0.00 ·
네 프레임의 흰 판 모서리 L 이 **200/196/200/200 으로 전부 같은 40ms 정점 위상**」 ·
**CO** 「같은 세 프레임 `max|Δ| = 0` · 플래시 링 값 **1713/1713/1713** 동일. 그런데 같은 프레임쌍에서
지불 알(4→8)·재화·전투력은 움직이므로 **앱 시계는 돈다**」.

원인을 소스에서 확인했다 — `tools/cap683.js` `shotB()` 가 프레임마다 **`open(SEED)` 로 브라우저를
새로 연다**(A 씬도 같다). 즉 **B1~B4 는 한 홀드의 네 시각이 아니라 네 번의 독립 실행**이고,
매 실행이 «홀드 → T ms 대기 → 얼림» 이라 **얼는 위상이 매번 같은 자리(갓 태어난 틱)** 에 떨어진다.
그래서 씬 B 는 ① 이 묻는 «홀드에서 틱마다 새로 터지는가» 를 **원리적으로 못 보여 준다**
(캔슬 금지 사양이 약속한 «앞 틱의 알이 수명 끝까지 산다» 의 겹침도 한 프레임도 안 찍힌다).

⚠ **이 자의 자기검산이 그것을 못 잡았다** — 표 꼬리의 «씬 B 보유 유물 레벨 합(한 홀드 = 단조
증가여야 한다): 15 → 15 → 15 → 16» 은 **서로 다른 실행의 합을 견주는** 줄이라 언제나 그럴듯하다.
CO 는 칸별로 갈라 **10칸 중 6칸이 최소 1회 감소**(심장 1→0→1→1 등)하는 것을 찍어 이 줄을 무너뜨렸다.
⇒ **975 등재**(하네스 · 자기검산 부패).

## 10-8. 게이트·회귀 (제품 0줄이므로 전부 무수정)

`probe683c` **9/9 PASS**(자 이관) · `verify683` **40/40** · `verify753` **32/32** ·
`verify862` **19/19** · `verify666` **32/32** · `verify682` **25/25** · **SMOKE PASS** · 콘솔 에러 0.

## 10-9. ⏸ 보류(10/10) — 라운드 마감

지시서 [3]-(다)·(나) 상한대로 **화면당 누적 10회를 소진**했다. ①~④ 최저 **4** 로 8 미달이므로
«완료» 가 아니라 **⏸ 보류(라운드 마감)** 다. §10-5·§10-6 이 2차 라운드의 입력이고,
**첫 자리는 §10-5(패치 그릇이 카드 하변 밖을 안 담는다)** 다 — 두 회차 연속으로 ④ 를 막은 축이다.

---

# 11. 11회차 — **2차 폴리시 라운드 첫 회차.** §10-5 의 진단을 재현이 기각했고, 손잡이는 «그리는 순서» 한 칸이었다 (2026-09-06, `sess-0643-26400` 루틴 워커 C)

> 제품 `index.html` **CSS 1줄 + JS 2줄** · 자 **1벌 신설**(`probe683d` 11/11) · **이관 2벌**(`verify753` [B2] 분할 · `verify795` [H1] 방향 전환)

## 11-1. 착수 — 10회차가 지목한 «다음 라운드 첫 자리» 를 처방보다 먼저 쟀다

10회차는 라운드를 닫으면서 «첫 자리는 §10-5(패치 그릇이 카드 하변 밖을 안 담는다)» 라고 적었다.
338 규칙대로 그 문장부터 쟀다(`tools/probe683d.js` 신설). **셋 다 진단과 반대였다:**

| 물음 | 실측 | 자 |
|---|---|---|
| 그릇이 사본을 자르는가 | `.fx-keep` 은 `overflow:visible` · 사본 하변 **1061** = 원본 하변 **1061** | [1-c] |
| 카드 하변 «밖» 20px 이 상해 있는가 | 패치를 통째로 걷어도 «4.5:1 미만» **3%**(정착 **5%**) | [2-a] |
| 그럼 어디가 상했나 | 패치를 걷으면 **안 74% ↔ 밖 3%** | [2-b] |

⇒ **§10-5 는 거짓이다.** 그릇은 라벨을 한 픽셀도 안 자르고, «걸친 아랫부분» 에는 애초에
고칠 것이 없다(플래시가 거기까지 안 온다). 상한 것은 **전부 카드 «안»** 이고, 그 안에서
795 의 패치는 **이미 듣고 있었다**(되돌림 배수 ×2.03 · [2-c]).

⚑ **결손의 진짜 이름은 «패치가 못 덮는다» 가 아니라 «패치 위를 알이 다시 덮는다» 였다.**
레이어는 «나중에 붙는 것이 위» 인데 알(`.fx-spark.fx-rlic`)은 플래시 **뒤에** 붙는다 —
실측 DOM 순서가 `0:fx-flash · 1:fx-keep(라벨) · 2:fx-rlic · 3~:fx-cic` 라, 795 가 되살린 글자를
그 다음 줄의 알이 도로 씻고 있었다. 10회차 [3] 표의 «패치를 알 위로(RAISE) 31% → 21%» 가
이미 그 답을 적어 두고 있었는데, 그 표는 «[4] 채움 눌림» 축에서 «여지 0.5%p 뿐 = 답이 아니다» 로
읽혀 넘어갔다. **같은 사본이 다른 축에서는 −10%p 였다.**

## 11-2. 제품 — CSS 1줄 + JS 2줄

```
.fx-keep.fx-keep-top{z-index:1}
```
`fxFlashKeep` 의 **글자 라벨 갈래**(`FXKEEP_TXT` = `.rw-c>u`)에만 그 클래스를 붙인다
(`still`=840 뱃지 · `live`=814 신고 잉크는 **제외** — 그 둘은 불투명한 그림이라 `.fx-keep` 의
«z 금지» 가 그대로 옳다).

| 축(t0 · 알 켬 · **정착에서 뜬 고정 마스크**) | 되돌린 사본 | **현행** | 알을 아예 숨긴 사본 | 정착 |
|---|---|---|---|---|
| 라벨 획↔국소배경 «4.5:1 미만» | 31% | **21%** | 25% | 14% |
| 중앙값 | 11.07:1 | **12.45:1** | 12.45:1 | 17.71:1 |
| 카드 «안» 구역만 | 57% | **36%** | 44% | 23% |
| 카드 «밖» 구역(대가 0) | 3% | **5%** | 5% | 5% |

⚑ **알을 지우지 않고 알을 지운 것보다 더 회수했다**(21% < 25%) — 753 «1회당 정확 1개 ·
아이콘 중심에서» 는 한 글자도 안 건드렸다. 자기검산으로 «z 한 칸» 과 «DOM 맨 끝»(RAISE)이
같은 그림임을 못박았다(21% ↔ 21% · `probe683d` [3-c]).

**남은 +7%p(21% ↔ 정착 14%)는 공용 `.fx-flash` 판 몫**이고 이 행이 못 고친다(788·862 계열).
`probe683d` 가 그것을 **⏸ 로 찍는다**(실패로 안 센다 — [H1] ⏸ 규약과 같은 꼴).

> ⚠⚠ **정정(같은 회차 채점 뒤) — 바로 위 «플래시 판 몫뿐» 은 아직 증명되지 않았다.**
> 비평가 둘이 그 문장을 **정면으로 반대로** 판정했다(CQ 는 확인 · CP 는 «알이 아직 14.6% 를 깎는다»).
> 상세·다음 자는 **§11-7** 을 보라. **그 갈림이 닫히기 전에는 이 문장을 근거로 쓰지 마라.**

## 11-3. 대가는 하나였고, 그것을 자에 래칫으로 박았다 — `verify753` [B2] 분할

착수 뒤 `verify753` [B2](«그려진 알도 중심이 같다» · ≤2.0px)가 **0.50 → 2.50 으로 빨개졌다.**
수리 전 트리에서 3회 연속 0.50 인 것을 대조로 확인했으므로(338·344 규칙) **내 몫이 맞다** —
라벨 글리프가 알의 아래 테를 가려 그려진 알의 상자 중심이 위로 간다.

⚑ **문턱을 넓히지 않고 항을 갈랐다(333·726 처방).** [B2] 는 «후광이 대칭이라는 뜻» 이라고
자기 라벨에 적어 두었는데 재는 것은 «화면에 그려진 알» 이었다 — **둘은 전경이 없을 때만 같은 말**이다.
전경이 생긴 순간 한 숫자가 두 물음을 섞는다.

| 항 | 10회차까지 | 11회차 |
|---|---|---|
| [B2] | «그려진 알 중심 = 아이콘 중심»(≤2.0) | **«후광 자체가 대칭»** — 11회차 선언 한 줄을 되돌린 사본에서 잰다(**0.50px** · 문턱 2.0 **한 칸도 안 넓혔다**) |
| [B2b] | — | **신설 래칫** — «전경이 먹는 몫». 그려진 그대로 **2.50px** ≤ 3.0(조이는 쪽으로만 다시 적어라) |
| [B2c] | — | **신설 되돌림 시험** — 선언을 되돌리면 [B2b] 가 **2.00px** 움직인다(= «언제나 초록» 이 아니다) |

⚠ **첫 시안 둘을 자가 스스로 기각했다.** ① 패치를 `visibility:hidden` 으로 끄려 했는데
**안 꺼졌다** — 패치 사본은 `getComputedStyle` 전 항을 인라인으로 물려받아 **`visibility:visible` 을
자기가 들고 있다**(그릇만 숨겨도 안 숨는다. «전경 몫 0.00px» 이 나오고서야 보였다).
② `display:none` 으로 바꿔 **살아 있는 프레임에서** 재니 «후광 자체 7.50px · 전경 몫 −6.00px» 라는
없는 수가 나왔다 — 그 사이의 페인트 왕복에 알이 움직인 것이다. ⇒ §R 은 **«같은 절차로 다시 띄운
사본»** 으로만 잰다. **자를 값에 맞춘 것이 아니라, 자가 재던 자리가 틀렸던 것을 고쳤다.**

## 11-4. `verify795` [H1] 방향 전환 — 금지의 «명시된 예외» 와 그 짝

795 는 [H1] 로 «그릇·복제본 둘 다 `z-index:auto`» 를 못 박았고 근거는 `.fx-keep` 규약
(«z 를 주면 뒤따라 붙는 스파크가 이 판때기 밑으로 깔린다»)이었다. 그 규약이 막으려던 것은
**판때기**다 — 배지 패치는 불투명한 원반이라 z 를 주면 사각형이 스파크를 통째로 가린다.
**글자 패치는 판때기가 아니다**: 그릇이 투명해 가리는 것은 «글자 위» 뿐이다.

⇒ 지우지 않고 **셋으로** 적었다 — [H1] 복제본은 여전히 `auto`(그림은 한 항도 안 바뀐다) ·
**[H1b]** 그릇만 한 칸 · **[H1c] 스코프의 짝**(z 를 받은 그릇이 «글자 라벨» 패치뿐인가).
[H1c] 가 없으면 840 뱃지·814 잉크가 조용히 같이 올라가도 **아무 자도 안 짖는다**.
`verify795` **14/15 → 17/17**.

## 11-5. 게이트·회귀

`probe683d` **11/11 신설** · `probe683c` **9/9**(무수정 — ⏸ 값이 31% → **21%** 로 움직였다) ·
`verify683` **40/40** · `verify753` **32/32 → 34/34 이관** · `verify795` **14/15 → 17/17 이관** ·
`verify862` 19/19 · `verify840` 24/24 · `verify814` 30/30 · `verify666` 32/32 · `verify682` 25/25 ·
`verify625` 24/24 · `verify619` 71/71 · `verify975` 29/29 · **SMOKE PASS** · `verifyProgress` OK · 콘솔 에러 0.

## 11-6. 채점 (지시서 [3]-(다) · 2인 독립 · 10회차에서 이어 센다)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 11 | **CP** | 6 | 5 | 5 | **4** | **4** | A1 에서 라벨 **극성이 뒤집힌다**(글리프 L 0.622→0.242 ↔ 국소배경 0.017→0.396 ×23) |
| 11 | **CQ** | 6 | 4 | 6 | **3** | **3** | B1 «Lv.2» 카드 «안» 이 «4.5:1 미만» **2.2% → 93.9%**(중앙값 13.73 → 3.72) |

**추이 — 3·5 → 5·4(10회차) → 4·3.** ⚠ **점수가 내려간 것을 «11회차가 나빠졌다» 로 읽지 마라** —
CP·CQ 는 이 화면을 처음 보는 두 사람이고, 둘 다 10회차 CN·CO 보다 **깐깐한 자**를 들고 왔다
(CQ 는 씬 B 를 A8 이 아니라 **같은 카드·같은 레벨의 깨끗한 B 프레임**과 짝지어 재서, 10회차가
한 번도 못 보던 씬 B 의 라벨 손실을 처음 찍었다). **11회차가 고친 축은 둘 다 회수를 확인했다**(아래 §11-7).

## 11-7. ⚑⚑ 두 비평가가 **정면으로 갈린 자리** — 12회차가 자로 갈라야 한다

11회차 §11-2 는 «남은 +7%p 는 공용 `.fx-flash` 판 몫뿐» 이라고 적었다. **그 문장을 둘이 반대로 판정했다:**

| | CP ④-4 | CQ ④-2 |
|---|---|---|
| 잰 프레임 | A3(80ms) ↔ **A4(130ms)** | **A4(130ms)·A5(180ms)** ↔ A8 |
| 결과 | 카드 «안» 9.8% → **14.6%** — 플래시가 죽은 뒤가 더 나쁘다 ⇒ **알이 아직 14.6% 를 깎는다** | 카드 «안» **41.5% · 41.1%** ↔ 정착 42.3% = **−0.8%p / −1.2%p** ⇒ **알 몫은 100% 회수됐다** |
| 마스크 | A8 `L>0.50` **1160px**(유효 914) · 배경 = 반경 **2–9px 링 중앙값** | A8 `L>0.55` **556px** · 배경 = 3px 팽창 제외 후 **11×11 상자 평균** |

**둘 다 «A8 고정 마스크» 규약을 지켰는데 정착 기준선이 0.0% ↔ 42.3% 로 갈렸다** — A1 10회차 교훈
(«계측 정의가 다르면 일치해도 틀린다»)이 이 행에서 다시 났다. 어느 쪽이 참인지는 **12회차가
`probe683d` [2] 의 구역 분할에 A3/A4/A5 를 넣어** 답해야 한다(내 자의 정착 «안» 은 23% 다 —
세 자가 셋 다 다르다). ⚠ **그 답이 나오기 전에는 §11-2 의 «플래시 판 몫뿐» 을 근거로 쓰지 마라.**

⚑ 다만 **11회차의 회수 자체는 둘 다 독립으로 확인했다** — CP ④-4 「카드 «밖» 은 전 프레임 0.0~0.4%,
결손은 전부 카드 «안»」(내 `probe683d` [2] 를 다른 마스크로 재확인) · CQ ④-2 「z 한 칸이 알 몫을
100% 회수했다 … 10회차의 «주행 끝이 라벨 위» 항은 알에 관해서는 **완전 해소**」.

## 11-8. 12회차 입력 — 두 사람이 **독립으로 같이 짚은** 것부터

1. **④ 플래시 판이 라벨 «밑판» 을 밝힌다**(둘 다 1순위 · 두 회차 연속). CQ 「라벨 밴드 평균 휘도
   **A2 +44.5% · B2 +65.8% · B1 +112.2%**」 ↔ CP 「A1 에서 국소배경 L 0.017 → **0.396(×23)** 이라
   흰 글자가 밝은 바닥 위의 **어두운 형상**으로 극성이 뒤집힌다」. ⇒ 손잡이는 **세기가 아니라
   그 띠에서의 α**(862 가 «액자가 있는가» 로 흰 테를 눌렀듯). ⚠ `.fx-flash` 는 09·12·17·코스튬·장비
   공용이라 **유물 경로에서만 세기를 빼면 그 화면만 다른 연출이 된다**(LESSONS 666-⑨) — 862 처럼
   **호스트가 신고하는 축**으로 짜야 이 행에서 닫을 수 있다.
2. **② 축소 곡선이 그림에 없다**(둘 다 · 10회차에 이어 **세 회차 연속**). CQ 「실루엣 134×135 → 136×134
   = **+1.5%** 인데 래칫 상자는 158 → 101(−36%) · 잉크 코어 종횡비가 0.89 → 1.27 로 **31% 뒤집힌다**
   ⇒ 보이는 «줄어듦» 은 축소가 아니라 **α 침식**이다」 ↔ CP 「저작 축소 폭의 **34.4%** 가 미렌더」.
   CQ 의 처방이 구체적이다 — **실루엣을 붙잡는 것은 알의 외곽 후광 스트로크**이니 후광을 `transform`
   대상 안에 넣어 같이 줄여라.
3. **① 봉투가 «버티다 절단»**(둘 다 · 세 회차 연속). CQ 「ln 감쇠율 −6.7/s → **−28.2/s(4.2배 가속)** ·
   A7 이 아직 최대 ΔL **0.208** 인데 A8 에서 0 ⇒ 선언 380ms 의 마지막 40ms 가 **보이는 세기에서 잘린다**」.
4. **④ 연출이 상품을 어둡게 한다**(둘 다 · 10회차 3항 재현·악화). CQ 「아이콘 휘도 **−42.3%(t0)** ·
   6% 안으로 돌아오는 데 **240ms** · 채도 −32.7% · B2 폭풍 **−56.8%**」 ↔ CP 「카드 내부만 보면
   어둠:빛 **7.33:1**(10회차 CN 의 3.2:1 보다 나쁘다)」. 처방은 둘 다 같다 — **알을 `screen`/가산으로**.
5. **③ 48px 이탈 예산이 «0px 탈출» 을 만든다**(둘 다 · 브리핑이 요구한 «여지 px» 를 둘 다 냈다).
   CP 「종단 좌변 x586.15 = 카드 좌변을 **0.85px** 통과. 이웃에 12px 남기는 상한은 **gain 132px**,
   권장 **87px(+39)**」 ↔ CQ 「알 잉크가 카드 밖으로 **0px**. 가로 여유 **103px** · 세로 **31px** ⇒
   등방 캡을 **타원(x반경 143 · y반경 75)** 으로」. ⚑ **CQ 가 «100.5px» 논거도 정리했다** — 그 값은
   «이웃 중심 간» 이 아니라 **아이콘 중심에서 위 이웃 카드 하변까지의 세로 여유**다(753 §2-2 정오).

## 11-9. 곁다리 등재 1건 — **980**(A8 이 «정착» 이 아니다)

CQ 가 [보조] 로 찍었다: `A8 ↔ B3`(같은 좌표·같은 Lv.1 폭풍 카드)를 직접 차분하니 `|ΔL|>0.05` 가
**9,728px**(그 중 테두리 밴드 5,832 = 밴드의 48%)이고 라벨 밴드 평균 휘도도 A8 이 **+7.4%** 밝다 —
**A8 은 «막 획득» 선택 테가 아직 켜진 프레임**이다. 브리핑이 «A8 을 기준 프레임으로 써라» 라고
못 박고 있어 **A 씬의 모든 Δ 가 실제보다 작게 나온다**(라벨 «안» 기준선 A8 42.3% ↔ B3 26.1%).
`verify753`·`probe683c`·`probe683d` 의 «정착» 도 같은 프레임에서 뜬다 ⇒ **하네스·자 공통 축**이라
이 행의 lock 으로는 안 잡고 **980 으로 등재**한다(975 와 같은 꼴).

## 11-10. ⏸ 보류(11/20) — 2차 라운드 첫 회차 종료

①~④ 최저 **3**(CP 4 / CQ 3)으로 8 미달이다. 제품은 **z 한 칸**이 들어갔고 그 회수는 두 비평가가
독립으로 확인했으나, **막는 축이 ④ «플래시 판이 라벨 밑판을 밝힌다» 하나로 좁혀졌다**(둘 다 1순위).
12회차의 첫 자리는 §11-8 의 **1번**이고, **그 전에 §11-7 의 갈림을 자로 갈라야 한다**(세 자가 셋 다 다른 정착값을 낸다).

---

# 12회차 (2026-09-06, sess-1027-10971 루틴 워커 B) — 갈림을 자로 갈랐고, 그 답이 가리킨 자리를 고쳤다

## 12-1. 착수 — §11-7 의 갈림부터 (338 규칙: 처방보다 재현이 먼저다)

11회차는 «12회차가 `probe683d` [2] 의 구역 분할에 A3/A4/A5 를 넣어 답하라» 는 숙제를 남겼다.
두 비평가가 **정면으로 갈렸기 때문**이다 — CP 「플래시가 죽은 뒤(A4 130ms)에도 알이 아직
14.6%p 를 깎는다」 ↔ CQ 「알 몫은 100% 회수됐다」. 셋(둘의 자 + 내 자)이 정착 기준선을
0.0% ↔ 42.3% ↔ 23% 로 각자 다르게 잡고 있었다.

`probe683d` 에 **[4] 시간 축**을 신설했다. 조건은 한 벌로 고정한다 — **되돌린 사본**(12회차 제품을
`NOFADE` 로 뗀다 · 그래야 «11회차 판에서 알이 남긴 몫» 이라는 물음 그대로다) · **정착에서 한 번 뜬
고정 마스크**(10회차 처방) · 카드 하변으로 «안/밖» 을 가른 구역 분할(11회차 [2]).

| t(ms) | 0 | 40 | 80 | 130 | 180 | 240 | 300 | 360 |
|---|---|---|---|---|---|---|---|---|
| 카드 «안» «4.5:1 미만»(알 켬) | 36% | 38% | 34% | **23%** | 23% | 23% | 23% | 23% |
| 알 몫(알 켬 − 알 숨김) | −8%p | −4%p | 0 | 0 | 0 | 0 | 0 | 0 |

⇒ **CQ 가 맞았다**([4-a] · 알 몫 ≤2%p · 실제로는 0 이거나 음수다 — 검은 알이 흰 글자 «뒤» 에서
대비를 되레 돕는다). 그리고 **[4-b]** 가 그 답을 시간으로 다시 못박는다: **플래시 창(0~120ms) 밖은
다섯 프레임 전부 정착과 같다**. 남은 결손은 «어느 층» 으로도 «어느 시각» 으로도 **플래시 하나**였다.

## 12-2. 성분 분해 — 임자는 «어느 성분» 이 아니라 «상자 바닥이 글자 위에 앉는 것» 이었다

처방을 고르기 전에 플래시를 성분별로 껐다(같은 자·같은 마스크 · 카드 «안» 기준):

| 사본 | t0 | t40 | t80 |
|---|---|---|---|
| 현행(11회차) | 36% | 39% | 32% |
| 워시 0(`background:transparent`) | 36% | 39% | 32% |
| **`--flash-k` = .70**(814 의 손잡이) | 36% | **41%** | 32% |
| 흰 테 0(`border-width:0`) | 33% | 31% | 32% |
| 안쪽 글로우 0(`box-shadow:none`) | 34% | 33% | 31% |
| **플래시 통째 없음** | **24%** | **23%** | **23%** |

⚑ **워시는 임자가 아니다** — 통째로 지워도 Δ0%p 이고, 814 가 코스튬에서 쓴 `--flash-k` 는 여기서
**아무 일도 안 한다**(t40 에서는 되레 +2%p). 11회차 채점 CQ 가 «세기가 아니라 그 띠에서의 α» 라고
적은 것이 정확했다. 성분을 하나씩 빼는 길이 닫혀 있는 이유도 자에 적혀 있다 —
`box-sizing:border-box` 라 **흰 테를 지우면 그 자리를 워시·안쪽 글로우가 그대로 물려받는다.**

## 12-3. 제품 — `fxKeepTxtTop` + `fxFlashFade` (index.html · 함수 둘 + 호출 두 줄)

**플래시는 «되그려야 하는 글자» 띠에서 스스로 α 를 뺀다.**

- 신고 목록은 **795 의 `FXKEEP_TXT` 를 되읽는다**(새 신고 축을 안 만들었다 — 목록이 둘로 갈리면
  한쪽만 늘어나는 날 조용히 어긋난다).
- α 는 **신고 잉크의 윗변에서 0** 이 되고, 거기까지 **흰 테 두께의 두 배**(실측 12px)에 걸쳐 빠진다.
  손 상수를 새로 안 적었다 — 길이는 `.fx-flash` 자신의 `border-width` 에서 읽고(795·862 처방),
  봉우리 보정은 이미 있는 `FXFLASH_PEAK` 를 쓴다(마스크는 요소 좌표라 `scale(1.06)` 을 같이 탄다).
- **하드 컷이 아니라 페이드**다: 컷은 흰 링을 한 줄에서 끊어 «잘린 액자» 로 읽힌다.
- 그 잉크를 신고하지 않은 호스트는 **마스크 0건**(09·12·17·코스튬·장비·훈련·단련·룬 — `verify683` [J6]).

| 축(카드 «안» · 고정 마스크) | t0 | t40 | t80 | 정착 |
|---|---|---|---|---|
| 되돌린 사본(11회차) | 36% | 43% | 34% | 23% |
| **12회차 현행** | **24%** | **23%** | **23%** | 23% |

⚑ **상자를 줄이는 길(높이 −14)도 같은 값을 냈지만 안 골랐다** — 862 가 사방을 액자 띠(6px)만큼
들여 둔 위에 바닥만 24px 로 벌어져 하이라이트가 카드에서 떠 보인다.

## 12-4. 곁가지 — 11회차가 치른 대가가 사라졌다

11회차는 «라벨 글리프가 알의 아래 테를 가려 그려진 알 중심이 2.00px 위로 간다» 를 대가로 치르고
`verify753` [B2b] 래칫에 박아 두었다. 12회차 뒤 그 값이 **0.00px** 이다 — 플래시가 라벨 띠를
안 밝히니 라벨 글리프가 알 잉크를 자르지 않는다([B2d] 신설).

## 12-5. 자 이관 — 다섯 벌 (전부 333 처방: 지우지 않고 방향·사본·축만 옮겼다)

12회차가 «플래시를 라벨 띠에서 뺀» 순간, **그 경로를 전제로 서 있던 되돌림 항들이 한꺼번에
전제를 잃었다**(862 가 겪은 «헛빨강» 의 재판). 문턱은 한 칸도 안 내렸다(334).

| 자 | 무엇이 무너졌나 | 어떻게 옮겼나 |
|---|---|---|
| `verify683` [H3][H4] | 패치·길이 축 되돌림이 배수 1 로 붙었다 | **재는 사본을 12회차 이전으로**(`NOFADE`) — 축 보존 · 짝 **[H5][H6] 신설**(«12회차가 흡수했다») |
| `verify683` [H1] | — | **⏸ 알갱이 축을 합쳤다**(788 이 «3:1 을 넘으면 그때가 마감» 이라고 적어 둔 조건 — 실측 4.12 → **21:1 = 정착의 100%**). 문턱을 내린 게 아니라 **재는 사본을 «알갱이 숨김» → «전부 켬» 으로 조였다** |
| `verify862` [C2][C3] | 「연출 6% < 정착 14%」라는 **불가능한 답** | 자의 **생존자 편향**을 고쳤다(마스크를 정착에서 한 번만 — 10회차 처방). 그러자 12회차 전이 정착 **+7%p**, 후가 **+1%p** ⇒ 문턱을 «절대 10%» 에서 **«정착 +2%p»** 로. 짝 **[C5] 신설** |
| `verify862` [R2][R3] | 들이기 0 사본의 액자선 잔존 3.1% → 21.7% | 되돌림 사본에 **페이드도 같이 되돌린다**(`NOFADE`) |
| `verify753` [B2c] | 11회차 선언 되돌림이 0.00px | **되돌릴 대상을 12회차 쪽으로**(페이드를 되돌리면 전경 몫 2.00px 이 되살아난다) + **[B2d] 신설** |
| `probe683c` [3-전제] | 카드 **좌하** 코너로 «흰 판이 떠 있는가» 를 묻고 있었다 | 좌하는 이제 판이 **정당하게** 안 뜨는 자리다 ⇒ 묻는 문장은 그대로, **묻는 자리만 좌상으로**(t0 0.58 ↔ 정착 0.06) |
| `probe683c` [3-a] | 중앙값 배수 1.5 → 1.33 | 이 자 자신의 **1차 축**(«4.5:1 미만» +10%p)으로 판정을 옮기고 중앙값은 꼬리표로 |

## 12-6. 게이트·회귀

`probe683d` **11/11 → 20/20**([4] 시간 축 3항 · [5] 회수·되돌림·대가·스코프·자리 6항 신설) ·
`verify683` **40/40 → 50/50**([J] 8항 신설 · [H1] 조임 · [H5][H6] 신설) ·
`verify753` 34/34 → **35/35** · `verify862` 19/19 → **20/20** · `probe683c` **9/9** ·
무수정 `verify795` 17/17 · `verify840` 24/24 · `verify814` 30/30 · `verify666` 32/32 ·
`verify682` 25/25 · `verify625` 24/24 · `verify619` 71/71 · `verify681` 41/41 · `verify890` 16/16 ·
`verify975` 29/29 · **SMOKE PASS** · `verifyProgress` OK · 콘솔 에러 0.

⚠ **관측 1건(등재 안 함 · 기존 자가 이미 잡는다)** — `verify862` [C1]/[C4] 가 실행마다 흔들린다
(같은 트리에서 100% ↔ 42.3%). 876 이 그 사고(1↔16.5↔100)를 알고 [C4] 를 위상 게이트로 심어
두었고, 이 회차의 흔들림도 그 자가 같이 빨개지며 잡았다(재실행 20/20). **수리 전 트리에서도 같은
얼굴**임을 대조로 확인했다(338·344 규칙).

## 12-7. 채점 (지시서 [3]-(다) · 2인 독립 · 11회차에서 이어 센다)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 12 | **CR** | 6 | 6 | 7 | **5** | **5** | 상품이 여전히 어둡다 — 잉크 Δpx −4,467(정착 밝은 화소의 −61.2%) · 유물 글리프 휘도 −77.6% · 채도 +141.1% · 5% 복귀 240ms |
| 12 | **CS** | 5 | 4 | 7 | **3** | **3** | 앞 세대 알이 앉은 «다른» 카드의 라벨이 17.45:1 → **2.75:1**(−84.3% · 국소배경 ×21.7) |

**추이 — 4·3(11회차) → 5·3.**

### 12-7-1. 두 사람이 **각자** 확인한 것 — 12회차가 고친 축은 실제로 닫혔다

| 자 | 11회차 신고 | 12회차 실측 |
|---|---|---|
| 국소배경 L(CP 식 고리) | A1 0.017 → 0.396 = **×23** | 정착 0.0606 → A1 0.0425 = **×0.70**(CR) |
| 밴드 평균 휘도(CQ 식) | A2 **+44.5%** | A2 **−4.5%**(CR) · **+17%**(CS 넓은 자) |
| 글자↔국소배경 대비 | 극성 뒤집힘 | 전 프레임 4.5:1 위 · **최저 8.89:1**(CR) · 극성 뒤집힘 **0회**(CS) |

CR 은 «되레 초과회수»(A1 이 정착보다 1.8단 높다)라고 적었고, CS 는 승자 카드에서 «닫힘» 으로 적었다.

### 12-7-2. ⚑⚑ 그런데 **그 처방이 새 결손을 만들었다** — 2인 독립 일치

| | CR [R2] | CS [6] |
|---|---|---|
| 결과 | 좌·우·상 레일은 있는데 **아래 레일이 0장** · 종점이 두 씬 다 카드 **+124(82.1%)** · 페이드/획 **1.3** ⇒ «비네트» 가 아니라 «찢긴 끝» | 세로획이 12행 만에 0 · 그 아래 **43px = 카드 높이의 28.5%** 가 ΔL **정확히 0** · «아랫변이 아예 없는 ㄷ 자» |
| 처방 | 액자를 닫아라 | 테를 잉크 윗변에서 **안쪽으로 접어** 닫힌 사각 유지 |

⇒ **13회차가 그 자리를 바로 고쳤다**(아래 §13). 브리핑이 «그 대가가 «잘린 액자» 로 읽히는지도
판정하라» 고 물었고 **둘 다 «읽힌다» 로 답한 것**이라, 다음 회차로 미루지 않았다.

### 12-7-3. 다음 회차 입력 — 2인이 **각자** 다시 짚은 열린 항 (12회차가 안 건드린 자리)

1. **④ 연출이 상품을 어둡게 한다**(3회차 연속 2인 · CR 의 «8점을 막는 단 하나») — CR 「잉크 Δpx
   **−4,467 = −61.2%** · 글리프 휘도 **−77.6%** · 채도 **+141.1%** · 5% 복귀 **240ms**」 ↔
   CS 「−77.0% · 잉크:판 대비 **9.95:1 → 1.64:1** 로 극성 반전」. **처방 2인 일치 = 알을 `screen`/가산 합성으로.**
   ⚑ §3 고정 사양은 «글리프·크기·1개·캔슬 금지» 뿐이고 **합성 모드는 안 고정한다**(둘 다 명시).
2. **④ 앞 세대 알이 «다른» 카드의 라벨을 덮는다**(CS 의 최대 감점 · 11회차 CP 의 «×23» 이 살아 있는 자리)
   — B2 눈 카드 Lv.4: 글리프 L 0.998 → 0.684 · 국소배경 **×21.7** · CR **17.45:1 → 2.75:1**.
   승자 카드는 멀쩡하다(15.11 → 15.25) ⇒ **13회차 처방은 플래시에만 걸렸고 알 자신에는 안 걸렸다.**
   ⚠ CR 은 같은 자리에서 다른 것을 봤다 — 「라벨 **패치**가 자기 카드가 아닌 칸에서 4~7% 커졌다
   내려앉는다」(`.fx-keep` 의 `FXFLASH_PEAK` 보정이 «플래시가 없는 카드» 에도 걸린다). **둘은 다른 축이다.**
3. **② 축소 곡선이 그림에 없다**(4회차 연속 2인) — CS 「자기정규화 폭 138→141px = **+2.2%**(선언대로면
   −16.9%)」 ↔ CR 「136→138 = +1.5% 로 안 줄다가 80→130ms 한 칸에 **−29.7%**(플래시 소등에 얹힌 계단)」.
4. **① 봉투가 «버티다 절단»**(4회차 연속 2인) — CR 「ln 감쇠율 −6.9 → **−38.0/s(5.5배)** · 0~120ms 가
   수명의 31.6% 인데 총 에너지의 **77.5%**」 ↔ CS 「−2.6 → −30.1/s(11.6배) · 80→130ms 에 **50ms 죽은 고원**」.
5. **③ 예산이 등방이라 낭비다**(2인 각자 px 로 근거를 냈다 — 브리핑이 요구한 형식) — 가로 홈통 **97px**
   · 세로 홈통 **24~25px** ⇒ CR 「타원 **110×36px**」 · CS 「타원 **±90×±22px**」. 지금의 등방 48px 은
   가로 여유의 **44%만** 쓰고 세로로는 t0 에 **31% 초과**한다. 침범은 두 사람 다 **0칸**으로 확인.

---

# 13회차 (2026-09-06, sess-1027-10971 루틴 워커 B · 12회차와 한 세션) — 내가 만든 «ㄷ 자 액자» 를 닫았다

## 13-1. 왜 같은 세션에서 바로 돌았나

12회차 채점은 **회수를 확인하면서 동시에 새 결손을 냈다**(§12-7-2). 브리핑이 «그 처방의 대가가
«잘린 액자» 로 읽히는지 판정하라» 고 물었고 **둘 다 «읽힌다» 로 답했으며**, 처방까지 같은 곳을
가리켰다(CR 「액자를 닫아라」 · CS 「테를 잉크 윗변에서 안쪽으로 접어 닫힌 사각 유지」).
**내가 만든 결손이고, 답이 두 사람에게서 같은 문장으로 왔으므로 다음 회차로 미루지 않았다.**

## 13-2. 제품 — 마스크를 버리고 상자를 끝낸다 (`fxFlashFade` → `fxFlashClampH`)

```
h ≤ 2·(신고 잉크 윗변 − 상자 윗변) / (1 + FXFLASH_PEAK)
```

`@keyframes fxFlash` 가 상자를 **중심 기준**으로 1.06 배 키우므로, 이 식이면 **봉우리에서 아래변이
신고 잉크 윗변에 정확히 닿는다**(실측 Δ **0px** · `probe683d` [5-e] · `verify683` [J7]).

| | 12회차(마스크) | **13회차(상자 클램프)** |
|---|---|---|
| 라벨 회수(카드 «안» <4.5 · t0/40/80) | 24 / 23 / 23% | **24 / 23 / 23%**(정착 23%) |
| 액자 | **아래 레일 0장**(ㄷ 자) | **네 변 다 있음**(마스크 0건) |
| 상자 높이 | 139(마스크로 아래 18px 가림) | **117.48** |
| 스코프 | 신고 없는 호스트 0건 | 〃 (`verify683` [J6] · `verify862` [B1b]) |

⚑ **되돌림 손잡이가 하나로 모였다** — `fxKeepTxtTop` 이 `null` 을 주면 12·13회차가 통째로 닫힌다.
자 넷(`probe683d`·`verify683`·`verify753`·`verify862`)이 **같은 스텁 한 줄**로 되돌린 사본을 만든다.

## 13-3. 자 이관 — `verify862` [B1] 의 «네 변» 에 명시된 예외 하나

[B1] 은 «상자가 네 변 모두 액자 띠(6px)만큼 들어간다» 였다. 아래변이 잉크 앞에서 멈추면 그 항은
높이 −33.53 을 보고 빨개진다. **문턱을 넓히지 않고 갈랐다**(333):

| 항 | 12회차까지 | 13회차 |
|---|---|---|
| [B1] | 네 변 모두 띠 | **좌·우·상 세 변**(문턱·값 그대로 Δ6·Δ6·−12) |
| [B1b] | — | **신설 · 아래변의 두 갈래** — 신고 없으면 띠(사본으로 −12 확인) · 있으면 봉우리에서 잉크 윗변(Δ0px) |

## 13-4. 게이트·회귀

`probe683d` 20/20 → **21/21**([5-f] «닫힌 액자» 회귀 게이트 신설 — 마스크가 서면 빨강) ·
`verify683` **50/50**([J] 를 상자 축으로 · [J4] «마스크를 안 쓴다» 신설) ·
`verify862` 20/20 → **21/21**([B1] 이관 + [B1b] 신설) · `verify753` **35/35** · `probe683c` **9/9** ·
무수정 `verify795` 17/17 · `verify840` 24/24 · `verify814` 30/30 · `verify666` 32/32 · `verify682` 25/25 ·
`verify625` 24/24 · `verify619` 71/71 · `verify681` 41/41 · `verify890` 16/16 · `verify975` 29/29 ·
`probe980` 17/17 · **SMOKE PASS** · `verifyProgress` OK · 콘솔 에러 0.

⚠ **관측(등재 안 함 · 876 이 이미 잡는 자리)** — `verify862` [C1][C4][R1][R5] 는 **동시 실행 부하에서만**
흔들린다(다른 자를 같이 돌리면 42.3%, **단독 실행은 다섯 판 연속 100% · 21/21**). 876 이 그 위상
사고를 알고 [C4] 를 심어 두었고 이번에도 그 자가 같이 빨개지며 잡았다. 수리 전 트리에서도 같은
얼굴이다(338·344 대조). ⇒ **이 자는 단독으로 돌려라.**

## 13-5. 채점 (지시서 [3]-(다) · 2인 독립 · 12회차에서 이어 센다)

| 회차 | 비평가 | ①타이밍 | ②크기 | ③위치·경로 | ④가독성 | **최저** | 그 회차의 «단 하나» |
|---|---|---|---|---|---|---|---|
| 13 | **CT** | 5 | 6 | 6 | **4** | **4** | 상품이 당첨 순간 글리프 휘도 −84.4% · 채도 +153.2% 로 회색 덩어리가 되고 5% 복귀에 290ms(수명의 76.3%) |
| 13 | **CU** | 5 | 5 | 6 | **4** | **4** | A1 에서 글리프가 자기 판보다 **2.43배 어두워 극성이 뒤집힌다**(정착은 33배 밝다) · 잉크:판 10.66:1 → **2.07:1** · 4.5:1 복귀 130ms |

**추이 — 4·3(11회차) → 5·3(12회차) → 4·4.** ①②③ 이 12회차보다 고르게 올라왔고(CS 4·5·7 → CU 5·5·6 ·
CR 6·6·7 → CT 5·6·6) **④ 만 4 에서 갈리지 않는다** — 네 사람이 두 회차에 걸쳐 **같은 한 줄**을 처방한다.

### 13-5-1. 두 사람이 **각자** 확인한 것 — 13회차가 고친 축은 닫혔다

| 물음 | CT | CU |
|---|---|---|
| 아래 레일이 그려지는가 | 피복 **89.2%**(A2) · 평균 +58.1 L\*(위 레일 +56.8 과 같은 잉크) | 연속률 **84%** · 상자 138×112 로 네 변 확정 |
| 라벨 잉크를 밟는가 | 잉크 429px **전부 \|ΔL\*\| = 0.00** · 4.5:1 미만 **0.0~0.2%**(정착 자신이 0.2%) | 획 Y **±0.0%** · 국소배경 ×0.89~×1.10 · 아래 레일↔잉크 여유 **21px** |
| 12회차의 «ㄷ 자» | **사라졌다** | **사라졌다** |

⚑ **CU 가 12회차 CS 의 [9]까지 닫았다** — «앞 세대 알이 «다른» 카드 라벨을 2.75:1 로 덮는다» 는
r13 에서 **재현되지 않는다**(bull @B1 · heart @B2 둘 다 국소배경 **×1.00** · 획 Y ±0.0%).
⚠⚠ **그리고 그 자리의 오판을 가려 냈다(다음 회차가 반드시 볼 것)** — 씬 B 에는 쌍둥이가 없어
«같은 카드의 다른 프레임» 을 쌍으로 쓰는데, **레벨이 바뀐 쌍을 쓰면 «연출» 이 아니라 «레벨업 팝»
을 재게 된다**(sparkle @B2 는 Lv.0 → Lv.1 로 방금 오른 팝 중이라 **카드 테두리째 3px 내려가 있다** ·
scales 는 B3 Lv.1 ↔ B4 Lv.3 로 글자 자체가 다르다). CR 12회차 [R3]«라벨 패치가 4~7% 커진다» 도
같은 함정이다. ⇒ **레벨이 안 바뀐 쌍(bull · heart · tornado)만 써라.**

### 13-5-2. ⚑ 13회차가 만든 새 대가 — 2인 독립 일치(또)

| | CT [1] | CU [④-4] |
|---|---|---|
| 상자 | 139×116(종횡비 1.198) · 카드 면적의 70.7% | 138×112(1.232) · 정사각 숙주 위에서 19~23% 납작 |
| 여백 | 좌6/우6/**위8**/**아래27** · 중심 **9.5px 위** | 좌6/우7/**위10**/**아래29** · 중심 **10.0px 위** |
| 판정 | «떠 보인다» 가 **실제로 읽힌다** | 액자가 카드에서 **10px 떠 있다** |
| 처방 | 네 변에 같은 비율로 걸어 정사각 유지 · 또는 위 여백을 아래와 같이 | 목표를 «잉크 윗변 −4px» 로 두고 **남는 25px 만큼 상자 상변도 같이 내려** 대칭(각 19~20px) |

⚑ **뿌리는 «과보정 21px» 이고 이유가 자에 있다** — 클램프는 신고 잉크의 **노드 상자 윗변**에 서는데
글리프 **잉크**는 그보다 21px 아래에서 시작한다(줄상자 여백). CU 가 그 21px 을 정확히 짚었다
(「설계 의도 Δ0px 에 대해 실측 **Δ+21px**」). ⇒ 14회차의 손잡이는 **«얼마나 물러설 것인가» 하나**다.
⚠ 다만 상변을 같이 내리면 `verify862` [B1](«좌·우·상 세 변은 액자 띠 6px»)과 정면으로 부딪힌다 —
862 가 그 6px 을 «흰 테가 액자선을 먹지 않게» 로 못박아 둔 값이라, **[B1] 을 다시 갈래로 가르든지
CT 안(네 변 등비 축소)을 고르든지 먼저 정하고 들어가라.**

### 13-5-3. 14회차 입력 — **네 사람이 두 회차에 걸쳐 같은 한 줄을 처방한다**

1. **④ 알을 `screen`/가산 합성으로**(CR·CS·CT·CU **4인 일치** · 두 회차 «8점을 막는 단 하나» 가 이것) —
   CU 「글리프 Y −79.2% · 채도 +159.8% · 잉크:판 **10.66:1 → 2.07:1 극성 반전**(정착은 33배 밝다) ·
   4.5:1 복귀 130ms · 95% 복귀 240ms」 ↔ CT 「−84.4% · +153.2% · 5% 복귀 **290ms = 수명의 76.3%**」.
   ⚑ **§3 고정 사양과 안 부딪힌다** — 주인 지시는 «글리프·크기·1개·발생점·캔슬 금지» 이고
   **합성 모드는 고정하지 않았다**(네 사람이 각자 그렇게 적었다). CT 는 덤으로 「가산이면 알이 레일을
   덮어도 안 어두워지므로 [5](A1 위 레일 43px 구멍)도 같이 닫힌다」 고 짚었다.
2. **④ 액자 대칭**(§13-5-2 · 2인 일치) — 과보정 21px 을 «잉크 −4px» 로 줄이고 남는 만큼 상변도 내린다.
   ⚠ `verify862` [B1] 과의 충돌을 먼저 정리할 것.
3. **① 봉투 «버티다 절단»**(5회차 연속 2인) — CU 「0~80ms = 수명의 21.1% 가 에너지의 **83.3%** ·
   ln 감쇠 −5.77 → −32.95/s(**5.7배**)」 ↔ CT 「−5.3 → −29.6/s(5.6배) · 80→130ms 에 **50ms 죽은 고원**」.
   ⚑ CU 가 알 α 고원을 따로 재서 **−1.55/s**(앞뒤 −9.8·−8.53 의 1/5.5)로 12회차 CS 의 −1.6/s 와
   **소수 둘째자리까지 일치**시켰다 — 한 톨도 안 움직인 자리다.
4. **② 축소 곡선**(5회차 연속) — ⚠ **여기서 두 자가 갈렸다.** CU 는 자기정규화 실루엣으로 12회차와
   같은 값(0→80ms **+1.4%** · 80→130ms **−30.5%**)을 냈고, CT 는 **문턱 없는 2σ RMS 폭**으로 재서
   「−3.9%p 이내로 선언 봉투를 따른다 = 재현 안 됨」 이라고 냈다. **14회차가 이 갈림을 자로 갈라라**
   (11회차 §11-7 과 같은 꼴이다 — 그때도 «시간 축» 을 깔아서 닫았다).
   ⚑ CU 가 원인 후보를 하나 더 냈다: 「0~120ms 에 화면이 «연출의 크기» 로 읽는 것은 알이 아니라
   **채워진 플래시 판**이다(맑은 판 화소의 ΔL>0.10 비율 A1 47% · A2 65% · A3 62% ↔ 플래시가 죽은
   A4 **9%**) — 알의 축소가 그 밑에 가려 있다가 판이 잘리는 순간 한 칸에 −30.5% 로 튄다」.
5. **③ 주행이 «안 보이는 구간» 에서 일어난다**(2인 일치 · 새 프레임) — CU 「총 46.2px 중 0~80ms 에
   **5.4px(11.7%)** 인데 그 구간이 에너지의 **83.3%**」 ↔ CT 「에너지가중 평균 주행 **12.2px** =
   48px 상한의 25.5%」. ⇒ **상한을 늘리는 문제가 아니라 배분 문제**다(둘 다 명시).
   여지는 여전히 가로로 넓다(CU 92px · CT 99×25px 타원 · 침범은 두 사람 다 0칸).
