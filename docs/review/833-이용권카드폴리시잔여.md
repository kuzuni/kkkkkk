# 833 — 10 이용권 카드 · 667 이 남긴 «2인 일치» 3건

> 등재: 2026-09-02(sess-1658-30860 워커 D — 667 10회차 비평 BF·BG 독립 일치)
> 티어 T4 «폴리시(레퍼런스 대조 잔여)» · 통과선 **9**(667 과 같은 화면·같은 등재문)
> 자: `tools/verify833.js`(신설) · `tools/scan833.py`(신설) · 재사용 `tools/scan667b.py`·`tools/probe667b.js`·`tools/cap151.js`

---

## 1회차 (2026-09-03, sess-0528-18953 루틴 워커 B)

등재문의 셋을 **자부터 세우고** 하나씩 열었다(338 규칙 — 처방을 따르기 전에 재현한다).
결과가 셋 다 등재문과 달랐다: **①은 «크기» 가 아니라 «종횡» · ②는 목표가 안 서는 축 · ③은 유령**.

### ⓐ 자 — ref 카드 상변에는 **장식이 걸쳐 있다**(③ 이 유령이 된 뿌리)

머리띠를 재려면 «바탕 → 검정 테 → 밴드 채움» 세 층의 모서리가 필요한데,
667 10회차의 `scan667c.py`(검정 «질량»)는 **양옆이 밝은 고원**인 자리에서만 성립한다 —
머리띠 **위**는 카드 바깥(어두운 바탕 + 드롭섀도)이라 그 자를 그대로는 못 댄다.
`tools/scan833.py` 는 대신 **50% 교차 모서리**로 잰다(AA 가 있는 ref 와 경계가 딱 떨어지는
우리 캡처에 같은 뜻을 준다 — AA 가 없으면 교차는 정확히 화소 경계 −0.5 다).

⚑ **1차 작성이 ref 검정 테를 4.9 가 아니라 2.8 로 읽었고, 그것이 이 작업의 교훈이다.**
ref 카드 상변에는 **보라 리본·상태 탭·가치 배지**가 걸쳐 있어 그 열에서는 검정 테가
1~2행으로 얇게 읽힌다. 열별 값이 **두 봉우리**(2.77 × 62열 ↔ 4.91 × 13열)로 갈리는데
중앙값이 «장식 쪽» 을 고른다. ⇒ 자에 두 조항을 넣었다:
**① 검정 바깥 8행이 전부 바탕일 것 · ② 그런 열들의 «가장 긴 연속 구간» 만 쓸 것.**
검산: 우리 카드2·카드3(같은 CSS)이 그때부터 **10.00 / 102.00 으로 소수점까지 같아졌다.**

### ⓑ ③ 헤더 밴드 세로 — **기각(유령). 제품 0줄**

| 자리 (환산 px) | ref | 우리 | Δ |
|---|---|---|---|
| 배너형 위 검정 테 | **10.15** | 10.00 | −0.15 |
| 배너형 채움 | **96.12** | 96.00 | −0.12 |
| 배너형 머리띠 총 | **107.03** | 106.00 | −1.03 |
| 불릿형 위 검정 테 | **9.00** | 10.00 | +1.00 |
| 불릿형 채움 | **102.28** | 102.00 | −0.28 |
| 불릿형 머리띠 총 | **111.29** | 112.00 | +0.71 |

등재문의 «−3.0 ~ −7.7% · 그중 위 검정 테 −27%» 는 **어느 축에서도 안 나온다**(최대 Δ 1.03px = −1.0%).
BG 의 «위 검정 테 8.3 ↔ 6» 은 위 ⓐ 의 장식 열(ref 2.8 봉우리 = 환산 5.7)을 잰 값과 같은 얼굴이고,
«우리 6» 은 우리 카드 어디에서도 안 나오는 값이다(우리 검정 테는 `.fr{border:10px}` 하나로 **전 카드 10.00**).
⇒ **자리를 비우지 않고 게이트로 굳혔다** — `verify833` §4 가 다음 회차에 같은 지적이 오면 먼저 대답한다.

### ⓒ ① «2000% 가치» 배지 — **크기가 아니라 종횡이었다. 제품 3자리**

`scan833.py` 로 **분홍 잉크 bbox** 를 재면 등재문의 «−3.6%» 는 **높이 한 축**이고, 폭은 반대로 **+4.5%** 다:

| (환산 px) | ref 배너 | ref 불릿 | 수리 전 | 수리 후 |
|---|---|---|---|---|
| 분홍 폭 | 160.90 | 158.84 | 166.0 (+4.5%) | **160.0** (−0.6% / +0.7%) |
| 분홍 높이 | 169.15 | 171.21 | 165.0 (−3.6%) | **170.0** (+0.5% / −0.7%) |
| **종횡** | 0.951 | 0.928 | **1.006** | **0.941** |
| 잉크 윗변(카드 상변 기준) | −29.75 | −32.49 | −32.0 | −32.0 (무변경) |
| 잉크 세로 중심 | +54.82 | +53.11 | +50.5 | **+53.0** |
| 잉크 우단 ← 카드 우변 | −15.37 | −9.15 | −18.0 | **−12.0** |

⇒ `.pvc>.bdg{width:178→**172**px;height:178→**184**px}` (별은 `clip-path` 가 % 라 상자를 그대로 따른다) +
두 줄 `top:45→48` · `95→98`(상자 높이 +6 이므로 별 잉크 중심이 상자-로컬 89 → 92 로 내려간다 — 5회차 규약의 부호만 반대).

⚑ **«중심이 6.5~7.5px 위» 는 «윗변이 틀렸다» 가 아니었다.** 잉크 윗변은 수리 전에도 ref 였고(−32.0 ↔ −32.49),
**아랫변만 5.2px 모자랐다**. 그래서 `top` 을 한 픽셀도 안 건드리고 **높이만** 늘려 중심이 따라오게 했다.
BG 의 «윗변 −32.0 기대 ↔ −38.5» 는 ref **잉크**를 우리 **상자**에 댄 것이다(−38.5 는 상자 윗변이고
잉크는 검정 테 6.5 안쪽에서 시작한다) — 같은 축을 잰 게 아니다.
⚠ 356 «확대·축소는 등방» 과 부딪히지 않는다: 여기서 바꾼 것은 **아트의 배율이 아니라 ref 의 종횡**이고,
별은 이미지가 아니라 % 폴리곤이다. 등방으로는 폭(+4.5%)과 높이(−3.6%)를 **동시에** 못 맞춘다.

### ⓓ ② 수량 숫자 세로 — **목표가 안 서는 축. 3px 만 옮겼다**

등재문이 «먼저 ref 표본을 문자열 길이로 갈라서 다시 재라» 고 한 대로 갈라 쟀다(`scan667b.py`).
**갈라도 안 모인다** — 뿌리는 문자열 길이가 아니라 **ref 의 수량 잉크 «높이» 자체**다:

| ref 표본 | 잉크 높이(원시 → 환산) | 상변 − 띠하변 | 중심 − 띠하변 | 하변 − 띠하변 |
|---|---|---|---|---|
| 배너 rb1 | 12 → 24.8 | +0.0 | **+11.3** | +22.7 |
| 배너 rb2 | 8 → 16.5 | +4.1 | **+11.3** | +18.6 |
| 불릿 rb1 | 10 → 20.6 | +8.3 | +17.5 | +26.8 |
| 불릿 rb2 | 8 → 16.5 | +14.4 | +21.7 | +28.9 |
| **우리(수리 전)** 배너 | 27 | +4.0 | +17.0 | +30.0 |
| **우리** 불릿 | 27 | +2.0 | +15.0 | +28.0 |

ref 잉크 높이가 같은 화면에서 **16.5 ↔ 24.8(+50%)** 로 흔들린다(8~12 **원시** px 라 ±1px = ±2 환산).
높이가 안 맞으면 상변·하변은 **동시에 못 맞춘다** — 실제로 두 축이 서로 반대 결론을 낸다
(상변으로 보면 우리 불릿이 6~12 **높고**, 하변으로 보면 우리 배너가 7~11 **낮다**).
⇒ 남는 축은 **중심**이고, 거기서만 ref 배너형이 **두 리본 모두 +11.3 으로 소수점까지 같다**(우리 +17.0/+17.0).

**처방(위임 규약 채택)**: 그 5.7 을 통째로 옮기면 상변이 ref 범위(0.0~4.1) 밖 **−2** 가 되어
숫자가 리본을 밟는다 ⇒ **3 만** 옮겼다 — `.pvc.ban1>.rb>u{top:52→49}` (제품 1줄).
결과: 중심 **+14.0**(Δ +5.7 → +2.7) · 상변 **+1.0**(ref 0.0~4.1 **안**) · 하변 **27**(30 → 27).
**세 축이 다 좋아지고 어느 축도 ref 범위 밖으로 안 나간다.** 되돌리려면 `52` 로 되돌리면 된다.
⚠ **불릿형은 안 건드렸다** — ref 자신이 +17.5 ↔ +21.7 로 갈려(4.2px) 목표가 안 선다(A3-ⓑ).
`verify833` [3-c] 가 그 «안 건드림» 을 못박는다(무르게 안 푼 증거).

### 게이트 (1회차)

`verify833` **48/48 신설** — §1 배지 종횡(잉크 = 상자 − 검정 테 산수) · §2 두 줄이 별 중심을 따라감 ·
§3 배너형만 옮김 + **[3-c] 불릿형 무변경** · §4 **머리띠 기각을 굳힘** · §5 9:13.3 서명 동일 ·
**§R 되돌림 시험 3항**(상자를 178×178 로 · 수량을 52 로 되돌리면 빨개진다).

회귀 **무수정** — `verify667` 83/83 · `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 ·
`verify164` 31/31 · `verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

⚠ **곁다리 관측 1건 — `verify588` [E3] 1건 실패(28/29)**: 「구 세이브의 offPlus 권한이 새 상품
(오프라인 ×배율)으로 이어진다 — ×1.381」. **수리 전 트리(`git stash`)에서도 같은 28/29** 임을
직접 대조로 확인했다(338·344 규칙) — 내 변경과 무관하고 내 작업 단위가 아니라 안 고쳤다. **858 로 등재**(등재 중 856·857 이 다른 워커에게 먼저 갔다).

캡처: `151-r18`(수리 전) · `151-r19` · `151-r19-h1600`(수리 후 · 비평 채점본). 캡처 PNG 는 커밋하지 않는다(이력 정리 규약).

### 비평 CX · CY (캡처 `151-r19` + `151-r19-h1600` · 9:19 · 9:13.3) — ①~④ 최저 **7 / 8 = 불통과**

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| CX | 8 | **7** | **7** | 8 | 8 | 6 | **7** |
| CY | 8 | 8 | 8 | 9 | 6 | 6 | **8** |

⇒ **1회차 불통과**(통과선 9). 회차 **1/5**. 두 사람은 서로의 보고를 못 봤다.

#### 이번 회차가 고친 셋은 **둘 다 다시 안 짚었다** — 오히려 «맞은 자리» 로 올라왔다
CY 는 «2000% 배지 중심 (949.1,133.8)/(945.8,131.0) ↔ (949.5,132.5) Δ≤3.7» ·
«수량 «1,500» 좌단·상단 346.5/434.0 ↔ 349/435 Δ≤2.5» · «카드1 헤더 채움 96행 · 카드2 102행» 을,
CX 는 «카드 검정 테 기대 10.3 ↔ 10» · «헤더 채움 96/102» · «2000% 배지 좌단 872.6 ↔ 870» 을
각각 «자로 재서 문제 없음» 목록에 넣었다. **머리띠·배지·수량 세로 지적은 두 사람 다 0건이다.**

#### 2인 일치 — 셋

1. **마일리지 알약 높이가 위로만 넘친다** — CX [8] «h 66.0 기대 ↔ 68 (+3.0%) · 상단이 2.3~3.1 더 위» ·
   CY [3] «65.0 기대 ↔ 68 (+4.6%) · **아래끝은 93.5 ↔ 93.6 으로 맞고 위로만 넘침**».
   ⇒ **2회차가 고쳤다**(아래 ⓔ).
2. **리본2 우단(제비꼬리 끝)이 4~10px 안쪽** — CX [5] «기대 568.3 ↔ 558(c1) / 564(c2)» ·
   CY [2] «기대 568.4 ↔ 559.5(c1) / 564.5(c2)». **두 사람의 기대값이 소수점까지 같다.**
   ⚠ 그런데 **리본1 우단은 CX 가 «맞음»(기대 463.1 ↔ 463.5)으로 올렸다** — 같은 CSS·같은 부품인데
   한 줄만 어긋난다면 손잡이는 상수가 아니라 **라벨 문자열의 advance** 다(리본은 `display:flex` 로
   내용에 맞춰 늘어난다). ref 와 우리의 문구가 달라서 생기는 차이라 **레이아웃으로는 못 닫는다** —
   3회차가 이 자리를 잡으려면 «리본 폭을 상수로 박는다» 를 먼저 정해야 하고, 그러면 리본1 이 깨진다.
3. **탭 «비활성화» 자리** — CY [1] «잉크가 탭 안에서 7.7px 위 · 신뢰도 높음» · CX [7] «탭이 카드 위로
   솟은 양 +3.8~5.7». ⚠ **이 둘은 같은 방향이 아니다** — CY 는 같은 보고의 «맞은 자리» 목록에
   «탭 카드 위로 솟은 높이 기대 57.6 ↔ 56(−1.6)» 을 적었다. **CX 의 기대 50.3~52.2 와 CY 의 기대 57.6 이
   7px 어긋난다** ⇒ ref 목표가 두 사람 사이에서 안 서는 축이라 **2회차는 안 건드렸다**(A3-ⓑ).
   3회차가 잡으려면 **탭 판의 ref 상변을 화소로 먼저 재라**(`scan833.py` 의 50% 교차 자를 그대로 쓸 수 있다).

#### 1인 지적 (다음 회차 참고)
CX: 카드1 리본 높이 −4.4%(⚠ 667 10회차 자는 −2.7% 로 쟀고 «안 건드림» 으로 닫았다) · 노치 세로 스팬 +5.9% ·
금색 판 모따기 깊이 −12% · 리본 안 아이콘 폭 −4.2%(신뢰도 낮음 — 형태가 다르다).
CY: 탭 글자 잉크 폭 −6.5%(⑥ 서체 축) · 일러스트 배경판 가로 +11.6%(신뢰도 낮음 — ref 는 45° 마름모) ·
마지막 불릿 → 리본1 간격 +7.9%(신뢰도 낮음 — ref 불릿 4줄 ↔ 우리 3줄).

#### 9:13.3 — 두 사람 다 «없음»
CX «카드 상자 안 레이아웃 차이 0px(임계 12 로 낮춰도 테두리 그림자 1~2px 뿐)» ·
CY «차이 60 초과 화소 0개 · 평균 절대차 0.35~0.47, 전부 카드 밖 배경 그러데이션».

---

## 2회차 (2026-09-03, 같은 세션) — 2인 일치 1건 수리 · 제품 1자리

### ⓔ 마일리지 알약 — **아래끝을 붙박고 위에서만 줄였다**

두 비평가가 같은 것을 봤고 **CY 가 손잡이까지 지목했다**(«아래끝은 맞고 위로만 넘침»).

| (카드-로컬 px) | ref (CX / CY 환산) | 수리 전 | 수리 후 |
|---|---|---|---|
| 알약 높이 | 66.0 / 65.0 | 68.0 (+3.0% / +4.6%) | **65.5** (−0.8% / +0.8%) |
| 하변 (카드 상변 기준) | — | +14.0 | **+14.0 (불변)** |
| 카드 위로 솟는 양 | 51.4~52.2 | 54.0 | **51.5** |

⇒ `.pvc>.pil{top:−54 → **−51.5**px; height:68 → **65.5**px}` (제품 1자리).
배경 그러데이션 눈금 넷도 같은 비(65.5/68)로 줄였다 — 안 줄이면 띠 세 개가 알약 안에서 1~2px 밀린다
(⚠ 마지막 밴드 `62..68` 은 원래 **패딩 상자 밖**이라 화면에 안 나온다 · `box-sizing:border-box`).

### 게이트 (2회차)

`verify833` **48 → 59/59** — **§6 신설**(높이 · **하변 불변** · 솟는 양) · **§R 확장**
([R4] 68/−54 로 되돌리면 §6 이 빨개진다 · **[R4b] 그때도 하변은 14 로 같다** = «아래를 안 건드렸다» 의 증거) ·
§5 프레임 서명에 알약 두 값 편입.
회귀 **무수정** — `verify667` 83/83 · `verify151` 84/84 · `verify124` 76/76 · **SMOKE PASS**.

캡처 `151-r20` · `151-r20-h1600`(2회차 채점본 — **3회차가 이 위에서 채점하면 된다. 다시 찍지 마라**).

⚠ **`verify356` 은 이 환경에서 끝까지 못 돌았다** — 두 번 돌려 각각 러너 시간 초과로 즉사했다(`probe418.js:372 sweep` 의 `browser.newContext` 대기 · 20분·15분). **판정을 못 받은 것이지 빨간 것이 아니다.** 이번 변경은 356 의 축(아이콘 **등방** 비율)에 닿지 않고(`.pil>em>.cic{40×40}` 무변경 · 배지 별은 이미지가 아니라 % 폴리곤), 667 10회차의 회귀 9종 목록에도 356 은 없다. 러너가 빠른 세션이 한 번 돌려 주면 좋겠다 — **857·855 계열의 «환경 의존 문턱»** 이다.

---

## 3회차 (2026-09-03, sess-0728-20833 루틴 워커 B) — 남은 2인 일치 **둘 다 닫았다** · 제품 2자리

2회차가 넘긴 순서대로 했다: **① 캡처 재생성 → ② 비평 2인 → ③ ref 를 화소로 먼저 재고 손대기.**
(⚠ `151-r20` 은 이력 정리 규약대로 커밋 안 된 파일이라 **새 클론에는 없다** — 같은 하네스
`tools/cap151.js` 로 다시 찍었다. 제품이 2회차 그대로였으므로 채점본으로 동등하다.)

### ⓕ 자 `tools/scan833b.py` 신설 — **채움 색으로 리본 우단을 재면 안 된다**

리본 채움은 ref·우리 둘 다 세로 그러데이션(밝은 띠 → 몸통 → 어두운 띠)이라
«가운데 색»(#FF565D)이 잡히는 행수가 **ref 49.5 ↔ 우리 37**(환산)로 다르다.
제비꼬리 **끝은 리본의 위·아래 끝 행에만** 있으므로, 넓게 잡히는 쪽이 저절로 더 바깥을 찍는다 —
1차 작성이 그래서 ref 를 +7~9px 바깥(536~538)으로 읽었다.
⇒ 자를 **«리본 왼끝에서 걸어 나가 처음 카드 몸통색이 되는 자리»** 로 바꿨다(ref·우리 같은 뜻).
검산: 우리 리본2 가 **520.0 / 525.0** 으로 읽히는데 DOM 상자 우단이 521 / 526 이다
(제비꼬리가 마지막 열을 덮으므로 보이는 우단 = 상자 − 1) — 화소와 DOM 이 산수로 맞는다.

### ⓖ 2인 일치 2번 «리본2 우단» — **ref 는 두 카드가 529.2 로 소수점까지 같다**

| 우단 (환산 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 배너형 리본1 | 430.2 | 424.0 | 424.0 (무변경) |
| **배너형 리본2** | **529.2** | 520.0 (−9.2) | **529.0** (−0.2) |
| 불릿형 리본1 | 446.7 | 445.0 | 445.0 (무변경) |
| **불릿형 리본2** | **529.2** | 525.0 (−4.2) | **529.0** (−0.2) |

⚑ **1회차가 «못 닫는다» 로 넘긴 근거가 사실이 아니었다.** 1회차는 «리본 폭이 라벨 advance 를
따라가니 상수로 박으면 리본1 이 깨진다» 고 적었는데, 폭은 **`renderPassCards()` 에서 카드 종류마다
인라인으로 박히고**(`RB = {w1, g1, w2, g2}`) **w1·w2 가 따로**다 — 리본2 를 옮겨도 리본1 은
한 픽셀도 안 움직인다. 667 9회차 주석이 이미 «띠 폭은 인라인으로 박힌다(shrink-to-fit 이 아니다)»
라고 적어 두고 있었다. **ref 쪽 증거도 같은 말을 한다**: 리본1 은 ref 자신이 430.2 ↔ 446.7 로
갈리는데(라벨이 다르다) **리본2 만 두 카드가 같은 값**이다 = 그 줄은 내용이 아니라 자리로 정해진 상수다.
⇒ 두 `w2` 를 **530** 하나로 모았다(보이는 우단 529).

⚠ **`--gx` 는 리본 오른끝 기준**이라 w2 만 늘리면 금색 판·수량이 같이 밀려
**닫힌 자리 2·6**(667 8·9회차가 맞춘 판 중심 · 수량 가로 중심)이 그만큼 깨진다 ⇒ `g2` 를 **같은 Δ**
만큼 키웠다(배너 34→43 · 불릿 23→27). 증거는 무수정 회귀다 — `verify667` **[F3]·[F4](판 중심)
그대로 초록**이고, 되돌림 [R5b] 가 «폭을 되돌려도 판 중심은 같다» 를 반대편에서 못박는다.

### ⓗ 2인 일치 3번 «탭 자리» — **CY 가 맞고 CX 는 기각. 판은 안 건드렸다**

두 비평가의 기대값이 7px 어긋나 1·2회차가 손을 못 댄 자리다(CX 50.3~52.2 ↔ CY 57.6).
화소로 재니 **참값이 둘 사이**였다:

| (카드 상변 기준 · 환산 px) | ref 배너 | ref 불릿 | 수리 전 | 수리 후 |
|---|---|---|---|---|
| 탭 판이 솟은 양 | 54.5 | 55.2 | 56.0 | **56.0 (무변경)** |
| 라벨 잉크 상변 | −29.8 | −32.5 | −39.0 | **−32.0** |
| 라벨 잉크 하변 | −0.9 | −3.6 | −8.0 | **−1.0** |
| 라벨 잉크 중심 | −15.4 | −18.1 | **−23.5** | **−16.5** |

⇒ CX [7]«탭이 카드 위로 +3.8~5.7 솟았다» 는 **기각**(Δ +1.2 = +2.2%) · CY [1]«잉크가 7.7px 위» 는
**확인**(실측 6.8~8.2). 판을 건드리면 CX 의 유령을 좇아 ref 에서 멀어지므로 **손잡이는 `.stt>i{top}` 하나**
— `9 → 16`(+7). 잉크 하변 −1.0 이 ref 배너(−0.9)와 같아 «글자 밑선이 카드 윗선에 앉는다» 는
ref 의 짜임이 그대로 나온다. **판·카드 기하 Δ0px.**

### 게이트 (3회차)

`verify833` **59 → 87/87** — **§7 신설**(리본2 우단 530 · **리본1 무변경** · 판 중심 Δ0 · 수량 중심 = 판 중심) ·
**§8 신설**(라벨 top 16 · 서체 상수 불변 · 줄상자가 판 안 · **판 무변경** · 세 카드 한 값) ·
§5 프레임 서명에 리본 우단·라벨 top 편입 · **§R 확장**([R5] 폭 되돌림 · **[R5b] 그때도 판 중심은 같다** ·
[R6] 라벨 되돌림).
**이관 1항** — `verify667` **[G2]**: «리본 우단은 8회차 자리 그대로(424/521)» 를 333 처방대로
**방향만 갈아 끼웠다**(424 는 그대로 · 리본2 는 833 3회차 자리 530). 이 항이 못박던 «판·꼬리 Δ0» 는
[F3]·[F8] 이 직접 재고 있어 **한 항도 무르게 풀리지 않았다**. `verify667` **83/83**.

회귀 **무수정** — `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 · `verify164` 31/31 ·
`verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

캡처: `151-r20`·`151-r20-h1600`(3회차 채점본 = 2회차 제품) · `151-r21`(수리 후 실측본).

---

## 3회차 채점 — 비평 CZ · CY→DA (캡처 `151-r20` + `151-r20-h1600`) — ①~④ 최저 **8 / 8 = 불통과**

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| CZ | 9 | **8** | 9 | 9 | 8 | 8 | **8** |
| DA | 9 | **8** | 9 | 9 | 9 | 7 | **8** |

⇒ 불통과(통과선 9). 회차 **3/5**. **두 사람 다 ② 하나만으로 8 이 됐고, 그 ② 는 제목 글자 하나다.**
1·2회차와 견주면 **CX 7 → 9 / CY 8 → 9** 로 ①③④ 가 전부 올라왔다(2회차 알약 수리 이후 재채점).

### 이번 회차가 고친 둘을 **두 사람이 독립으로 재확인**했다(수리 **전** 캡처를 봤다)

| 자리 | CZ | DA | 내 자(scan833b) | 수리 후 |
|---|---|---|---|---|
| 카드1 리본2 우단 | −9.0px | −7.4px | −9.2px | **+0.0~+3.0** (ref 526.0~529.2 ↔ 우리 529) |
| 카드2 리본2 우단 | −4.0px | −3.5px | −4.2px | 〃 |
| 탭 라벨 잉크 세로 | −4.1px(중심) | −6.2px(중심) | −6.8px | **−16.5 ↔ ref −15.4/−18.1** |
| 탭 판이 솟은 양 | «문제 없음»(+0.3) | «문제 없음»(+0.5~1.1) | +1.2 | 무변경 |

⚑ **세 자(비평가 둘 + 내 화소 자)가 같은 값을 냈고, 셋 다 «탭 판은 맞고 잉크만 위» 로 일치했다** —
1회차 CX 의 «판이 3.8~5.7 높다» 는 이번 회차에 **세 번 독립으로 기각**됐다. 3회차가 판을 안 건드린 것이 옳았다.

### 남은 2인 일치 — **하나뿐이고, 그것이 통과를 막는 전부다**

**제목 글자 잉크가 크다** — CZ «카드1 +9.3% · 카드2 +10.3% · 글리프 피치 +7.0%» ·
DA «카드1 +9.3% · 카드2 +10.3% · 낱말 «광고» +13.4%». **두 사람의 수치가 소수점까지 같다**(신뢰도 «높음»).
⚑ CZ 가 **뿌리를 좁혀 줬다** — 같은 자로 잰 이웃은 맞는다(불릿 본문 피치 −1% · 리본 라벨 −5.5% ·
가격 글자 +0.6%) ⇒ **제목만** 크다. ⇒ 4회차에서 닫았다(아래).

### 1인 지적 (다음 회차 참고)
CZ: 탭 라벨에 검정 외곽선이 없다(ref 글자당 4.1px ↔ 우리 0 · ⑤/② · 신뢰도 높음) · 탭 라벨 잉크 폭 −6.5% ·
배너 검정 테 −15%(신뢰도 낮음).
DA: 리본2 «파임 깊이» 가 ref 보다 33~61% 깊다(6.2/8.3 ↔ 10/11) · 리본 라벨 잉크 폭 −3.5% ·
배지 글자 −7.6% · 제목 낱말 사이 공백 −27% · 배너 상자 좌단 +3.4px.
⚠ **⑥ 이 7 인 이유(DA)**: «크기 오차의 **방향이 자리마다 뒤집힌다**» — 제목 +10% · 버튼 글자 +5~10% 인데
탭 글자 −6.5% · 리본 라벨 −3.5%. 한 배율이 아니라 여러 배율이 섞여 있다는 뜻이다(⑥ 서체 축).

### 9:13.3 — 두 사람 다 «없음»
CZ «카드 본체 영역 완전 일치 · 차이 화소는 전부 카드 바깥» · DA «채널합 최대 44(>120 인 화소 0개) —
1px 라도 옮겼으면 검정/흰 경계에서 ~765 가 나와야 한다».

---

## 4회차 (2026-09-03, 같은 세션) — 통과를 막던 **제목 글자** 하나 · 제품 2자리

### ⓘ 667 10회차가 «⑥ 서체» 로 넘긴 판정을 **자를 바꿔 뒤집었다**

667 10회차는 이 축을 «검정 획이 ref 자신에게서 ±30% 흔들린다» 며 아트 대기로 넘겼다.
그 근거는 **획**이고, 지금 문제는 **글자 크기**다 ⇒ **획이 안 섞이는 자**로 다시 쟀다 —
`tools/ink151.py` 의 **흰 채움만**(min>200) bbox(이 자는 처음부터 저장소 안에 있었다):

| 흰 잉크 (환산 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 제목 1장 «광고 제거» | 185.7 × 43.3 | 200 × 46 (**+7.7% / +6.2%**) | **184 × 44** (−0.9% / +1.6%) |
| 제목 2장 «전투 효율 증가» | 278.5 × 47.5 | 307 × 49 (**+10.2% / +3.2%**) | **283 × 47** (+1.6% / −1.1%) |
| (획 포함 잉크 폭 · scan833b) | 196.0 / 292.9 | 212 / 321 | **196.0 / 295.0** |

획을 빼도 남으므로 **서체 «모양» 이 아니라 크기 축**이고, 지시서 [3]-(나)가 «글자 잉크 높이·폭은
②·⑥ 비율 항목이므로 반영한다» 고 못박은 자리다.

**손잡이 둘 · 뜻도 둘**(126 서체 전환 때 «세로만 보정하고 가로를 안 되돌렸다» 던 그 처방):
① `font-size 57 → 54.5`(×0.956) = **em 을 줄여 세로를 맞춘다** · ② `scaleX(.96)` = 대체 서체의
**가로 advance 만** 되돌린다. 한 손잡이로는 못 닫는다 — 폭 +9% 와 높이 +4.7% 의 비가 ref 와 다르다
(등방으로 줄이면 높이가 −5% 로 반대편에 떨어진다). 네 값이 전부 **±1.6%** 안에 들어왔다.

### ⓙ ⚠⚠ 이 회차의 본체 — **특이성 함정이 조용히 이겼다** (207·325 재발)

`transform-origin:left center` 를 적었는데 **computed 는 «98.54px 30px»(중심)** 이었다.
4356행 `#shopw i,#shopw em,#shopw b,#shopw u,#shopw s{… transform-origin:50% 50%}` 의
**ID 특이도(1,0,1)** 가 클래스 선택자 `.pvc>.pvt>i`(0,3,1)를 이긴다 — **선언은 남아 있는데 안 먹는다.**
그 결과 scaleX 가 중앙 기준으로 걸려 **제목 잉크 좌단이 74 → 78 로 밀렸다**(카드2 는 73 → 79).
그 자리는 **두 비평가가 «맞음» 으로 올린 곳**이라(ref 75.4/73.4), 못 봤으면 이번 수리가
«하나 고치고 하나 깨는» 회차가 될 뻔했다. ⇒ 같은 스코프의 짝 `#shopw .pvc>.pvt>i{transform-origin:0 50%}`
를 신설(325 가 `#blsw s` 에서 쓴 처방) ⇒ 좌단 **74 / 73 으로 복귀**.
⚑ 세로도 안 건드려졌음을 확인했다 — 잉크 세로 중심 배너 58.0 → **58.0**(무변경) · 불릿 60.0 → **60.5**
(ref 61.4 에 0.5 더 가까워졌다).

### 게이트 (4회차)

`verify833` **87 → 104/104** — **§9 신설**(font-size 54.5 · scaleX .96 · **[9-c] transform-origin 의 x = 0** ·
line-height 60 불변 · 상자 좌단 78 불변) · **§R 확장** — **[R7]·[R7b] 가 이 회차의 교훈을 못박는다**:
«값을 되돌리면 빨개진다» 가 아니라 **«스코프 짝을 빼면 조용히 진다»** 를 잰다(원점이 중앙으로 돌아가고
상자가 81.94 / 83.92 / 85.75 로 실제로 밀리는 것을 본다).

회귀 **무수정** — `verify667` 83/83 · `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 ·
`verify164` 31/31 · `verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

⚠ **곁다리 관측 1건 — `tools/probe679.js` 5/10**: **수리 전 트리(`git stash`)에서도 같은 5/10** 임을
직접 대조로 확인했다(338·344 규칙) — 내 변경과 무관하고 내 작업 단위가 아니라 안 고쳤다.

캡처: `151-r22`(원점 함정이 살아 있던 판) · **`151-r23`(4회차 최종 · 다음 회차는 이 위에서 채점하면 된다)**.

### ⚑ 3회차가 할 것 (순서대로 — 2회차가 남긴 지시, 전부 이행)

1. **위 캡처 `151-r20`·`151-r20-h1600` 으로 비평 2인부터** — 2회차 수리는 채점을 안 받았다.
2. 비평 브리핑의 «닫힌 자리» 에 **9·10·11 에 더해 12(마일리지 알약 높이)** 를 넣어라.
3. 2인 일치 2·3번(리본2 우단 · 탭 자리)은 위에 적은 대로 **먼저 ref 를 화소로 재고** 손대라 —
   둘 다 «두 사람의 기대값이 서로 다르거나 형제 부품과 모순» 인 자리다.

---

## 4회차 채점 — 비평 DB · DC (캡처 `151-r23` + `151-r23-h1600`) — ①~④ 최저 **6 / 8.5 = 불통과**

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| DB | 9 | **6** | 9 | 9 | 6 | 6 | **6** |
| DC | 9.5 | **8.5** | 9 | 9.5 | 8 | 8 | **8.5** |

⇒ 불통과(통과선 9). 회차 **4/5**. ⚠ 9:13.3 대조본이 브리핑에서 낡은 판(`151-r20-h1600`)을 가리키고 있어
**회차 중에 `151-r23-h1600` 을 새로 찍어 두 사람에게 정정 지시를 보냈다** — 안 했으면 두 사람 다
«수리 사이의 차이» 를 «프레임 차이» 로 보고했을 것이다(둘 다 정정본으로 다시 대조해 **0px** 을 보고했다).

### ⚑ 3·4회차가 손댄 **넷 전부**를 두 사람이 «문제 없음» 으로 올렸다

| 자리 | DB | DC |
|---|---|---|
| 제목 잉크 폭 | c1 −0.9% · c2 +1.6% | c1 +0.2% (c2 만 +2.4% 로 지적) |
| **제목 잉크 좌단**(안 밀렸는가) | **Δ +1.1 — «안 밀렸다»** | **Δ +1.6 / −1.4 — «안 밀렸다»** |
| 제목 세로 중심 | Δ −0.4 | Δ −1.3 / −0.3 |
| 리본2 우단 | Δ +1.7 / +0.7 | Δ +2.2 |
| 탭 라벨 잉크 세로 | Δ +0.9~+4.1 | Δ ≤ +2.2 |
| 탭 판이 솟은 양 | Δ +0.4 | Δ +1.6 |
⇒ 4회차 ⓙ 의 «특이성 함정을 못 봤으면 좌단이 4px 밀렸을 것» 이 **두 사람의 좌단 측정으로 확인**됐다.

### 2인 일치 — **탭 라벨 «폭»** 하나 (⇒ 5회차가 닫았다)
DB «−7.5px / −6.5% · 좌단 +3.5 / 우단 −2.9 = 가운데는 맞고 폭만» · DC «−6.5px / −5.6%».
⚑ **이 자리는 이제 비평가 넷이 독립으로 같은 값을 냈다**(3회차 CZ −6.5% · DA −6.5%). 전원 «높이는 맞다».

### 1인 지적 (5회차 이후가 볼 것)
- **DB [A] 배너 라벨 낱자가 가로로 +19%**(총 폭 +11.0% · 높이 Δ+0.1 · 낱자 피치는 +5% ⇒ «자간이 아니라
  글자 자체») — ⚑ **`ink151.py` 도 «배너 글자 +11.0% / 높이 −3.1%» 로 같은 값을 이미 찍고 있다.**
  제목과 **같은 꼴**이라 4회차 ⓘ 의 처방(em 과 scaleX 를 갈라 쓴다)이 그대로 듣는 자리로 보인다. **5회차 뒤 1순위.**
- DB [C] 수량 «10,000/16,000» 6자리가 +19.6%/+7.7%(5자리 «1,500» 은 −2% 로 맞다) — ref 는 자릿수가
  늘면 글자를 줄이는데 우리는 안 줄인다. ⚠ 667 [G3] 이 «advance ≤ 상자폭» 을 이미 못박고 있으니
  그 상자 안에서 줄이는 축(자릿수별 배율)을 새로 세워야 한다. 신뢰도 보통.
- DB [D] 가격 알약 높이 +5.2% · 가격 낱자 높이 +6.0%(폭은 맞다).
- DC [1] 배지 가로 −5.4%(ref 169.1 ↔ 우리 160) — ⚠ **833 1회차가 `scan833.py` 로 잰 ref 폭은 158.84~160.90**
  이라 **정면으로 어긋난다**(닫힌 자리 9). 둘 중 하나가 다른 것을 재고 있다(별 톱니 끝점 검출 ±3px).
  손대기 전에 **세 번째 자**를 세워라(3회차 교훈 ③).
- DC [2]·DB [H] 리본 라벨 잉크 폭 −3.5%(2인) · DC [6] 배너 크림/금색 분할선 +7.1px · DC [7] c2 리본2
  아래 여백 +4.1px · DB [E]·[F] 금판·리본 내부 명암 띠(⑤ 기록).
⚠ **DB 의 ② 가 6 인 이유는 «도형은 전부 ±3% 안인데 글자 잉크만 −6.5%~+19% 로 벗어난다»** 이다 —
남은 감점이 **글자 크기 한 축**에 몰려 있다는 뜻이고, 그 축은 4회차가 제목에서 이미 닫는 법을 찾았다.

---

## 5회차 (2026-09-03, 같은 세션) — 4회차 2인 일치 하나 · 제품 1자리

### ⓚ 탭 라벨 — **범인은 `scaleX(.94)` 하나였다**

비평가 **넷**이 «폭만 −5.6~−6.5%, 높이는 맞다, 가운데도 맞다» 로 일치했다. 세로·중심이 맞고 폭만
좁으면 손잡이는 배율 하나뿐이고, 되돌릴 값은 **산수로 나온다** — 잉크 108 ÷ .94 = **114.9** ↔ ref 115.5.

| `ink151.py` (환산 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 탭 글자 잉크 | 115.5 × 30.9 | 108 × 31 (**−6.5%** / +0.3%) | **115 × 31** (**−0.4%** / +0.3%) |

⇒ `.pvc>.stt>i` 에서 `transform:scaleX(.94)` **삭제**(제품 1자리). 3회차가 맞춘 `top:16`(세로)은 그대로 뒀다 —
그 축은 넷 다 «맞음» 으로 올린 자리다(수리 후 잉크 상/하변 −32.0 / −1.0 로 **무변경** 확인).
⚑ **여기서는 4회차를 괴롭힌 `#shopw` 원점 함정이 오히려 옳았다** — 라벨은 `left:0;right:0;text-align:center`
라 중심 원점이 맞는 원점이고(그래서 DB 가 «좌단 +3.5 / 우단 −2.9» 로 대칭을 봤다), 스코프 짝을 새로
만들 것 없이 **transform 자체를 안 쓰는 것**이 답이었다.

### 게이트 (5회차)
`verify833` **104 → 108/108** — **[8-f] 신설**(세 카드 다 `transform:none`) · **[R8] 신설**(scaleX(.94) 를
되돌리면 빨개진다). 회귀 **무수정** — `verify667` 83/83 · `verify151` 84/84 · `verify124` 76/76 ·
`verify152` 44/44 · `verify164` 31/31 · `verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 ·
`verify267` 67/67 · **SMOKE PASS**.

캡처 `151-r24`(5회차 최종 · **다음 회차는 이 위에서 채점하면 된다**).

---

## ⚑ 6회차가 할 것 (순서대로)

1. **캡처부터 다시 찍어라** — 캡처 PNG 는 커밋 안 하는 규약이라 **새 클론에는 없다**.
   `node tools/cap151.js <경로>/151-r25.png --crop` 와 `--crop --h 1600` **둘 다** 찍고,
   ⚠ **9:19 과 9:13.3 을 반드시 같은 회차 판으로** 비평가에게 줘라(4회차가 여기서 낡은 판을 줄 뻔했다).
2. **채점부터** — 5회차 수리(탭 라벨 폭)는 아직 채점을 안 받았다.
   브리핑의 «닫힌 자리» 에 **17(탭 라벨 폭 — scaleX 제거)** 을 더해라.
3. **1순위는 «배너 라벨 낱자 가로 +19%»** (DB [A] · `ink151.py` 가 «배너 글자 +11.0%» 로 같은 값을 이미 찍는다).
   **4회차 ⓘ 의 처방이 그대로 듣는 자리로 보인다** — em(세로)과 scaleX(가로)를 갈라 쓰고,
   ⚠ `.pvc>.ban>i` 는 `text-align:center` 인지 먼저 보고 **원점을 정하라**(가운데 정렬이면 중심 원점이
   맞고, 좌측 정렬이면 4회차처럼 `#shopw` 스코프 짝이 필요하다 — 그 함정은 이 페이지에 **세 번** 나왔다).
4. **배지 가로**(DC [1])는 손대기 전에 **세 번째 자**를 세워라 — `scan833.py`(ref 158.84~160.90)와
   DC(ref 169.1)가 정면으로 어긋난다. 둘 중 하나가 다른 것을 재고 있다.

---

## 6회차 (2026-09-03, sess-0958-17271 루틴 워커 D) — 5회차 지시 1순위 «배너 라벨 가로» · 제품 1자리

캡처 `151-r25` · `151-r25-h1600`(5회차 최종 상태 = 채점 대상) → 수리 후 `151-r26` · `151-r26-h1600`.

### ⓛ 배너 라벨 — **가로만 되돌렸다. 세로는 손대면 안 되는 자리였다**

4회차 ⓘ 가 제목에서 쓴 «em 은 세로 · scaleX 는 가로» 를 그대로 대려다 **세로 손잡이가 막혀 있는 것**을
자가 먼저 말했다.

| `ink151.py` (흰 채움 min>246 · 환산 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 배너 글자 잉크 | 235.2 × 28.9 | 261 × 28 (**+11.0%** / −3.1%) | **235 × 28** (**−0.1%** / −3.1%) |
| 잉크 중심 x (카드-로컬) | 367.7 | 369.0 | **369.0** (무변경) |
| 잉크 상·하변 | — | 185 / 212 | **185 / 212** (무변경) |

⇒ `.pvc>.ban>i` 에 `transform:scaleX(.9)` **한 값**(제품 1자리). 비평가 DB [A]가 «낱자 가로 +19% ·
총 폭 +11.0% · 높이 Δ+0.1» 로 찍은 것과 `ink151.py` 의 «+11.0% / −3.1%» 가 같은 축이고, 둘 다 **가로**다.

### ⚠⚠ 이 회차가 자를 대고서야 안 것 — **이 라벨은 이미 상자를 9.6px 넘고 있었다**

세로를 같이 맞추려고(−3.1%) `font-size 36 → 37` 을 대 보기 전에 상자를 쟀더니
**advance 264.64 ↔ 상자 폭 255**(`.ban{border:7px}` + `box-sizing:border-box` 라 상자는 269 가 아니라 255)였다.
즉 `text-align:center` 는 이 자리에서 **이미 죽어 있고**(넘치면 왼끝에서 시작해 오른쪽으로만 넘친다 —
667 [G3] 이 수량에서 겪은 그 사고), 667 은 그것을 알고 **넘침의 절반 4.8px 을 `left` 로 미리 갚아 놨다**
(CSS 주석 «그 4 를 빼고 적는다 ⇒ left = 2×(317.7−4.0) − 450»).

⇒ **fs 를 올리는 길은 막혀 있다** — 넘침이 커지는 만큼 잉크 중심이 오른쪽으로 밀려 667 의 보정이 어긋난다.
높이 −3.1% 는 **28 ↔ 28.9 = 0.9px** 이고, 그 0.9px 을 얻으려고 중심을 밀 값이 아니다. **세로 0줄**.
게이트는 «안 넘치는가» 가 아니라 **«넘침이 그대로인가»**([10-d])와 **«변환 뒤 잉크 중심이 ref 367.7 인가»**([10-h])를 묻는다.

### ⓜ 원점 — 여기서는 `#shopw` 특이성 함정이 **이기는 것이 옳다**(4회차와 반대)

4회차 ⓙ 는 `#shopw i{transform-origin:50% 50%}`(ID 1,0,1)가 클래스 선택자를 이겨 제목 잉크가 4px 밀린 것을
스코프 짝으로 되돌렸다. 배너 라벨은 **반대**다 — `left/right + text-align:center` 라 잉크가 상자 중심
언저리에 앉으므로 **중심 원점이 맞는 원점**이고, 제목처럼 `transform-origin:0 50%` 짝을 만들면 이번엔
왼쪽으로 밀린다. 실측이 그것을 확인했다: 수리 뒤 잉크 중심 **369.0 (무변경)** · 산수 값 369.84 ↔ ref 367.7.
⇒ **스코프 짝 0줄**. [10-c] 가 «중심 원점이 의도» 임을 못박는다(다음 세션이 «또 함정이다» 로 짝을 만들지 않게).

### 게이트 (6회차)

`verify833` **108 → 119/119** — **§10 신설 8항**(배율 · 세로 무변경 · 원점 = 상자 중심 · 넘침 9.64 ·
변환 뒤 advance ≈ ref · 상자 181/12 불변 · 획 8 불변 · 잉크 중심 367.7) · **§R 확장 [R9]·[R9b]**
(배율을 지우면 폭이 ref +12.5% 로 나가고 **넘침은 같다** = 6회차가 고친 것이 «넘침» 이 아니라 «넓음» 이라는 증거).

**이관 1종 — `verify667` R4**(333 처방 · 지우지 않고 방향 전환): «left:150 으로 되돌리면 [F1](잉크가 크림 칸을
문다)이 빨개진다» 가 **더 이상 참이 아니다** — 잉크가 26px 좁아져 left:150 에서도 좌단 230.9 로 분할선 216.3
오른쪽에 남는다. 8회차 수리가 **지금도** 지키는 축은 **자리**이므로 [F2](상자 중심 365.5 → 350) 로 옮기고
분할선 값은 «참고» 로 계속 찍는다. ⇒ `verify667` 82/83 → **83/83**.

회귀 **무수정** — `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 · `verify164` 31/31 ·
`verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

## 6회차 채점 — 비평 DD · DE (캡처 `151-r25` + `151-r25-h1600`) — ①~④ 최저 **7 / 7 = 불통과**

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| DD | 9 | **7** | 9 | 9 | 8 | 7 | **7** |
| DE | 8 | **7** | 8 | 8 | 8 | 7 | **7** |

⇒ 불통과(통과선 9). 회차 **6/10**. 두 사람 다 **② 하나로 7 이 됐고, 그 ② 의 1순위가 같다.**

### ⚑ 5회차 수리(탭 라벨 폭)는 **둘 다 «닫아도 된다» 로 확인**했다
DD «잉크 115×31 ↔ 115.5×30.9 = −0.4%/+0.3% · 좌상단 Δ(+0.5,+0.5)» · DE «−0.5%, 임계 90/110/150 전부 같은 값».
⇒ 브리핑 «닫힌 자리» 17번은 **확정**이다(비평가 여섯이 이 자리를 봤다).

### 2인 일치 — 둘

1. **수량 숫자가 자릿수에 따라 안 줄어든다**(⇒ 7회차가 닫았다) — DD «10,000 +19.6% · 16,000 +8.5%» ·
   DE «+21.2% / +9.8%». **4회차 DB [C]에 이어 세 번째 회차**이고, 이번엔 두 사람이 **ref 가 줄인다**는
   근거까지 같이 냈다(ref 「1,500」 44×13 ↔ 「10,000」 44×**11** — 폭은 같고 높이가 준다).
2. **우변 티켓 노치가 얕다** — DD «깊이 31.0 ↔ 33.0 = −6.1%(신뢰 중하)» · DE «30 ↔ 33.0 = −9.1%(중)».
   두 사람의 마스크가 «배경색이 아닌 최우측 화소» 로 같다. 중심 위치는 둘 다 «정상»(Δ≤2.4). **7회차 뒤 1순위.**

### 1인 지적 (다음 회차가 볼 것)
- **DE [A] 카드1 아트 패널이 헤더 밴드를 덮는다**(패널 상변이 카드2·3 은 로컬 135, 카드1 만 **10** ⇒
  제목 띠가 카드 폭의 55.8% 에서 끊긴다 · ref 헤더는 폭 100% 단색). 신뢰도 중 — «아트 내용» 으로 볼 여지가
  있지만 **우리 카드끼리도 안 맞는다**는 것이 근거라 자리(①) 축이다.
- **리본 라벨 잉크 폭 −3.5%** — DD ⑥1(−3.6%/−3.5%) · DE G(−3.5%). ⚑ 4회차 DC [2]·DB [H] 도 같은 값이라
  **비평가 넷이 독립으로 −3.5%** 다. DD 가 «문자열 동일» 임을 밝혔으므로 폭 비교가 성립하는 자리다.
- DE [D] 리본 라벨 잉크가 리본 안에서 **위로 2.8~3.8px**(리본 높이의 5.7%, 신뢰 상).
- DD A3 «배지 종횡 +4.9%(높이만 +4.3%)» ↔ **DE 는 같은 자리를 «Δ+0.5% 문제 없음» 으로 올렸다** —
  마스크 민감도(분홍 ±30 ↔ 색조)라 **2인 일치가 아니다**. DC [1](ref 169.1)까지 셋이 갈리므로
  5회차 지시대로 **세 번째 자를 세우기 전에는 손대지 마라**. ⚠ 다만 DE 가 그 세 번째 자를 실제로 세웠다 —
  «배지 세로중심 행의 분홍 span 77×k = 158.9» 로 `scan833.py` 값을 재현하고 «bbox 자는 톱니 위상 때문에
  부푼다» 로 DC 를 설명했다(닫힌 자리 9 유지).
- DD A4/A8 노치 중심 −5.6px(카드1 노치1만 · 나머지 둘은 Δ≤2.4) · DD ⑥3·DE F 가격 숫자 높이 −3~−6%.

### 9:13.3 — 두 사람 다 «없음»
DD «카드1 채널 최대차 10 · 카드 안(x≥51) 완전 동일 · 크롭 3쌍 최대차 15/15/12 전부 배경 그러데이션» ·
DE «임계 30 초과 화소 0개». ⚠ 두 사람이 같은 관측을 하나 더 냈다 — **1600 판은 카드2 아래가 뷰포트에
잘린다**(스크롤 밖). 레이아웃 결함이 아니라 프레임 높이 차이이고, 카드2·3 의 온전한 대조는
`-c2/-c3` 크롭이 받는다.

---

## 7회차 (2026-09-03, 같은 세션) — 6회차 2인 일치 1번 «수량 폭 맞춤» · 제품 3자리

### ⓝ ref 는 **자릿수가 늘면 글자를 줄여 폭을 붙박는다** — 자 셋이 같은 값을 냈다

| 수량 잉크 폭 (환산 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 카드1 «1,500» | 88.7 | 88 (−0.8%) | **88** (무변경) |
| 카드1 «10,000» | 86.6 | 110 (**+27.0%**) | **88** (+1.6%) |
| 카드2 «1,500» | 86.6 | 88 (+1.6%) | **88** (무변경) |
| 카드2 «16,000» | 96.9 | 111 (**+14.6%**) | **88** (−9.2%) |

ref 실측은 **저장소 안에 있던 자**(`tools/scan667b.py` 흰 잉크)이고 비평가 둘의 값과 방향·크기가 같다.
같은 카드에서 «1,500» 43×12 ↔ «10,000» 42×**8** — **폭은 같고 높이가 준다** ⇒ ref 의 규칙은
«폭을 붙박고 **등방으로** 줄인다» 다(6회차 배너 라벨의 scaleX 와는 **다른 축**이다).

### ⓞ 처방 — 손 상수가 아니라 **그린 뒤 재서** 줄인다(shrink-to-fit)

`pvFitQty()` 신설(제품 3자리 — 함수 · `renderPass()` 꼬리 호출 · `openShopPage()` 재호출) +
CSS 한 값 `transform:scale(var(--uq,1))`. 목표 `PV_QTY_ADV = 90` 은 **ref 네 표본의 중앙값**
(86.6·86.6·88.7·96.9 → 87.65)에 side bearing 몫을 더한 값이고, 우리 «1,500» 의 advance 89.91 이
그 안이라 **다섯 자리는 배율 1 = 한 픽셀도 안 움직인다**.

⚠ **«여섯 자리면 0.8» 같은 손 상수를 안 쓴 이유가 게이트에서 바로 드러났다** — 「5,000」 은 **네 자리인데도**
advance 97.14 로 목표를 넘고(글리프 폭이 자마다 다르다) 「750」 은 64.59 로 안 넘는다. 자릿수로 가른
[11-b] 가 그 자리에서 빨개져 **advance 로** 갈아 끼웠다.

### ⚠⚠ 이 회차의 함정 — **자가 «안 보이는 동안» 재면 0 이 나온다**

`renderShopPage()` 는 `$('shopw').classList.add('on')` **전에** 돈다 ⇒ 그 시점 창은 `display:none` 이고
Range 폭이 **0** 이라 배율이 조용히 1 로 남는다(첫 실행에서 실제로 그랬다 — 화면은 수리 전 그대로였고
`--uq` 만 «1» 로 찍혀 있었다). ⇒ **보인 뒤 한 번 더** 부른다(`openShopPage()`). 열려 있는 동안의
재그리기는 `renderPass()` 안의 호출이 받는다. 폴백이 1 이라 자가 못 돌아도 «수리 전» 으로 얌전히 돌아간다.

### 게이트 (7회차)

`verify833` **119 → 145/145** — **§11 신설 21항**(변환 뒤 advance ≤ 90 · 목표 안이면 배율 1 무변경 ·
넘으면 줄었다 · 상자 139 안 · 잉크 중심 = 판 중심 · **[11-f] «999,999» 로 갈아도 자가 다시 재서 맞춘다**
= 배율이 손 상수가 아니라는 증거) · **§R [R10]**(배율을 1 로 되돌리면 여섯 자리가 113 으로 다시 나간다).

**이관 1종 — `verify667` [G5]·R8**(측정 기준 이관): 상자 폭을 `getBoundingClientRect` 로 재고 있어
등방 배율이 걸리자 **139 가 110.5 로 읽혔다** — 상자가 좁아진 게 아니라 자가 다른 것을 본 것이다
⇒ `offsetWidth`(레이아웃 값)로 갈고, advance 는 반대로 **변환 뒤** 값을 그대로 둔다([G3]«넘치는가» 는
눈에 보이는 것을 물어야 한다). ⇒ `verify667` 82/83 → **83/83**.
⚑ 부수 효과 — shrink-to-fit 이 advance 를 90 으로 붙박으므로 [G3]«넘침» 은 이제 **구조적으로** 못 생긴다.

회귀 **무수정** — `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 · `verify164` 31/31 ·
`verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

캡처 `151-r27` · `151-r27-h1600`(7회차 최종 · **다음 회차는 이 위에서 채점하면 된다**).

---

## ⚑ 8회차가 할 것 (순서대로)

1. **캡처부터** — `node tools/cap151.js <경로>/151-r28.png --crop` 와 `--crop --h 1600` 둘 다(캡처 PNG 는
   커밋 안 하므로 새 클론에는 없다). 두 판을 **같은 회차 것으로** 줘라.
2. **채점** — 6·7회차 수리 둘(배너 라벨 폭 · 수량 폭 맞춤)이 아직 채점을 안 받았다.
   브리핑 «닫힌 자리» 에 **18(배너 라벨 폭 — scaleX .9)** · **19(수량 폭 맞춤 — shrink-to-fit)** 을 더해라.
3. **1순위 = 우변 노치 깊이 −6~9%**(6회차 2인 일치 2번 · DD 31.0 / DE 30 ↔ ref 33.0). 손잡이는
   `pvNotches()`·`--ntc-fr/--ntc-bg` 마스크다. ⚠ 노치 **중심**은 둘 다 «맞음» 으로 올렸으니 깊이만 건드려라.
4. **2순위 = 리본 라벨 잉크 폭 −3.5%** — **비평가 넷**(DB·DC·DD·DE)이 독립으로 같은 값이고 문자열도 같다.
   6회차 배너 라벨과 **같은 꼴**이라 `scaleX` 한 값으로 닫힐 가능성이 크다(부호만 반대 = 넓혀야 한다).
   ⚠ 리본 라벨은 `.rb>i` 이고 `#shopw i` 원점 함정의 사정권이다 — **원점을 먼저 물어라**(6회차 ⓜ).
5. **DE [A] 카드1 아트 패널이 헤더 밴드를 덮는 것**은 «아트 내용» 이 아니라 **자리**로 다뤄라 —
   카드2·3 은 패널 상변이 로컬 135 인데 카드1 만 10 이다(우리 카드끼리 안 맞는다).

---

## 8회차 (2026-09-03, sess-1613-3772 루틴 워커 A) — 7회차가 넘긴 1·2순위 **둘 다** 닫았다 · 제품 3자리

7회차 지시 순서대로 했다: **① 캡처 두 판 → ② 비평 2인 → ③ 노치 깊이 → ④ 리본 라벨 폭.**
캡처 `151-r28` · `151-r28-h1600`(8회차 채점본 = 7회차 제품) · `151-r29c`(수리 후 실측본).

### ⓟ 1순위 «노치 깊이» — **틀린 것은 두 형 중 한 형뿐이었다**

6회차 2인 일치(DD «31.0 ↔ ref 33.0 = −6.1%» · DE «30 ↔ 33.0 = −9.1%»)를 따르기 전에 재현부터 했다
(338 규칙). 두 사람의 마스크가 «배경색이 아닌 최우측 화소» 로 같았고 **저장소 안에 같은 정의의 자가
이미 있었다** — `tools/scan667.py`(4회차가 «눈으로 세지 마라» 로 세운 우변 실루엣 전용 자).

| 노치 깊이 (환산 px · 바탕이 파고든 만큼) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 배너형(파랑) | 28.9 / 30.9 / 30.9 (평균 **30.2**) | 30 (−0.2) | **30 (무변경)** |
| 불릿형(초록) | 33.0 / 33.0 / 33.0 | 30 (**−3.0 = −9.1%**) | **33 (Δ0.0)** |

⇒ **ref 자신이 형마다 두 값**이고 배너형은 이미 맞아 있었다. 비평가 둘이 «−6~9%» 로 갈린 것도
이것으로 설명된다 — 한 사람은 초록만, 한 사람은 둘을 섞어 봤다.

⚑ **근거는 새로 판 것이 아니라 7회차 주석 안에 이미 있었다.** 거기서 «ref 배너 깊이 30.9 ↔ 불릿 33.0»
이라고 적어 두고도 그 회차는 **길이만** 갈랐다(«두 형의 스캘럽 모양이 다르다» 의 나머지 절반).
피치·앵커·길이에 이어 **깊이가 네 번째 «ref 자신이 둘» 인 축**이다.

**처방** — 보이는 깊이 = 링 `s` 반지름 − 링 두께 10 이므로 불릿 33 ⇒ 반지름 **43**(배너 40 그대로).
⚠ 값을 **세 곳에 따로 적지 않았다**(402 «사본을 지운다») — `--ntc-d` 하나를 카드에 인라인으로 싣고
상자 폭(= d+12) · 링 `s`(= 2d) · 밝은 림 `u`(= 2(d+12)) 셋이 CSS 에서 그 값을 읽는다. 실제로 이 자리는
**주석이 이미 한 번 낡아 있었다**(«반지름 32 · 상자 44» — 5회차가 32→40 으로 올리며 안 고쳤다).

### ⓠ 2순위 «리본 라벨 폭» — 비평가 넷이 맞았고, **손잡이는 자가 정했다**

DB·DC·DD·DE 넷이 독립으로 −3.5% 를 냈는데 저장소 안에 **그것을 재는 자가 없었다** ⇒
`scan833b.py` 에 `riblabel()` 신설(연노랑 채움 마스크 · 금색 판은 창에서 잘라 낸다 —
판의 밝은 금 테 #FDC532 가 라벨과 같은 «밝고 노란» 마스크에 걸린다).

⚠⚠ **1차 문턱(170,170,190)은 −8.8% ~ −38.6% 로 넷과 안 맞았고, 우리 두 리본끼리도 갈렸다.**
문턱을 흔들자(150 → 205) 값이 **수렴**했다 — (190,190,170)·(205,205,150)이 같은 값을 낸다:

| 라벨 잉크 (환산 px · 문턱 205,205,150) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 배너 리본1 폭 | 187.7 | 181.0 (**−3.6%**) | **187.0** (−0.4%) |
| 배너 리본2 폭 | 251.7 | 243.0 (**−3.4%**) | **251.0** (−0.3%) |
| 불릿 리본2 폭 | 251.7 | 243.0 (**−3.4%**) | **251.0** (−0.3%) |
| 네 줄 잉크 **높이** | 26.8 | 27.0 (+0.7%) | **27.0 (무변경)** |
| 배너 리본1 잉크 좌단 | 42.4 | 46.0 | **46.0 (무변경)** |
| 배너 리본2 잉크 좌단 | 44.5 | 45.0 | **45.0 (무변경)** |

세 짝이 전부 −3.4~−3.6% = **넷이 낸 값과 같다.** ⚠ **불릿 리본1 의 −18.0% 는 결함이 아니라
«다른 문자열» 이다** — ref 자신이 리본1 에서 187.7(파랑) ↔ 220.7(초록)로 갈리는데 리본2 는 두 장이
251.7 로 **같다**(3회차가 리본 우단에서 이미 «리본1 은 라벨이 달라 갈린다» 를 확인한 그 자리다).
우리 두 장은 같은 문자열이라 둘 다 181 이다 ⇒ **비교가 성립하는 짝만 셌다.**

⚑ **font-size 가 아니라 scaleX 인 것은 취향이 아니라 자가 정했다** — 같은 자로 잰 잉크 **높이**가
ref 26.8 ↔ 우리 27.0 = **Δ+0.2(+0.7%)** 로 이미 맞다. 크기를 올리면 맞아 있던 높이가 깨진다
(6회차 배너 라벨과 같은 꼴 · **부호만 반대**). 검산 — 181×1.035 = 187.3(ref 187.7) · 243×1.035 = 251.5(ref 251.7).

### ⓡ ⚠⚠ 이 회차의 함정 — **7회차가 «원점을 먼저 물어라» 고 적어 뒀는데 그대로 졌다** (207·325·4회차 ⓙ 네 번째 재발)

`.pvc>.rb>i{transform-origin:0 50%}`(0,3,1)를 적었는데 **5873행 `#shopw i{transform-origin:50% 50%}`**
(ID 급 1,0,1)가 이겼다. 선언은 남아 있고 **computed 만** 가운데였다(`122.438px 19px` = 상자 폭의 절반) —
그래서 «안 걸렸다» 가 아니라 **조용히 다른 것이 됐다**. 화소로 잡혔다: 폭은 맞았는데
**이미 맞아 있던 리본2 좌단이 45.0 → 41.0(ref 44.5 대비 −3.5)** 으로 밀렸다.
⇒ 선례 그대로 스코프 짝 `#shopw .pvc>.rb>i{transform-origin:0 50%}` 를 신설(4회차의 `.pvt>i` 와 같은 꼴).
왼쪽 원점이 옳은 이유도 자에 있다 — `.rb` 는 `display:flex; padding-left:38` 로 라벨을 **왼쪽에 앉히는**
자리라, 늘리는 방향이 곧 그 앵커를 지키는 방향이다(수리 후 좌단 두 줄 다 **Δ0 = 무변경**).

⚑ **판정은 «어느 쪽이 나은가» 가 아니라 실측으로 했다** — 가운데 원점은 리본1 좌단을 46.0 → 43.0
(ref 42.4)으로 **좋게** 만들지만 리본2 를 45.0 → 41.0(ref 44.5)으로 **나쁘게** 만든다(|Δ| 합은 둘 다 4.1 로 같다).
같은 값이면 **보고된 적 없는 자리를 안 움직이는 쪽**이 옳다.

### 게이트 (8회차)

`verify833` **161 → 183/183** — **§12 신설 14항**(형별 `--ntc-d` · 링 실측 폭 · **상자·`s`·`u` 셋이 한 값 파생** ·
링 두께 불변 · **[12-e] 두 형이 한 값으로 접히면 빨강** · [12-f] 같은 형끼리는 한 값) ·
**§13 신설 20항**(가로만 1.035 · 세로 1 · **[13-b] computed 원점** · font-size 34 불변 · 한 배율 · 라벨 앵커 불변) ·
**§R 확장 4항**([R11] 불릿 깊이를 40 으로 되돌리면 §12 가 빨개진다 · **[R11b] 그때 상자·림도 같이 되돌아간다**
= 세 폭이 한 값 파생이라는 증거 · [R12] 스코프 짝을 빼면 원점이 가운데로 · **[R12b] 그때 잉크가 실제로 3~4px 밀린다**).
⚠ [R12b] 는 처음에 문턱을 39.5 로 잡아 **자가 빨개졌다** — «밀린다» 는 참인데 «얼마나» 를 틀리게 적은 것이라
**자를 고쳤다(제품이 아니다)**. 왼쪽 원점 44.0 ↔ 가운데 39.7~40.8.

**이관 1종 — `verify667` [A4]**(333 처방 · 방향만 뒤집었다): «두 형 다 링 가로 80» 을 **형별 값**으로 갈았다.
그 항이 못박던 «중심이 카드 우변 위» 는 그대로이고, **짝 항 [A4b] 를 신설**해 상자·`u` 도 같은 한 값에서
파생되는지 같이 본다(자리를 비우지 않았다). `spanAt()` 도 `dep` 를 인자로 받게 고쳤다 —
한 값으로 굳어 있으면 [A9] 스캘럽 비가 불릿에서 3.07 로 부푼다(고친 뒤 **2.80**, ref 2.88 에 더 가깝다).
`verify667` 83 → **86/86**.

회귀 **무수정** — `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 · `verify164` 31/31 ·
`verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.

**레이아웃 그 밖 Δ0px** — 카드 기하·리본 상자·금색 판·수량은 한 픽셀도 안 움직였다(회귀 8종이 그 증거다).

### ⓢ 6회차 1인 지적 «카드1 아트 패널이 헤더 밴드를 덮는다»(DE [A]) — **유령. 제품 0줄**

DE 의 근거는 «우리 카드끼리 안 맞는다»(카드2·3 은 패널 상변 135 인데 카드1 만 10)였는데,
**이 화면은 ref 자신이 형마다 두 값인 축이 넷**이다(피치·앵커·길이 · 이번 회차의 깊이 — §12).
일러스트도 그중 하나이고, **667 6회차가 이미 ref 로 정한 자리**다: 비평 AX·AY **2인 일치**로
«ref 파랑의 대응물은 가로 라운드 사각이 아니라 **45° 마름모 글로우**이고 헤더 밴드까지 덮고 올라간다» 를
확인했고 세로 하변을 자 셋이 겹쳐 쟀다(AX 369.5 · AY 367.2 · 자체 프로파일 366) ⇒ `--art-t 78 → 10` ·
`height 239 → 357`. 되돌리면 6회차가 고친 «세로가 ref 의 60%»(AX −39.6% · AY −37.7%)로 돌아간다.
⇒ 고치지 않고 **게이트로 굳혔다**(§14 신설 6항 · §4 «머리띠 유령» 과 같은 꼴) — 다음 회차가
«맞은 것을 고치는» 일을 이 절이 먼저 막는다.


---

## 8회차 채점 — 비평 DF · DG (캡처 `151-r28` + `151-r28-h1600` = 7회차 제품) — ①~④ 최저 **8 / 8 = 불통과**

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| DF | 8 | 8 | 8 | 8 | 8 | 8 | **8** |
| DG | 8 | 8 | 8 | 8 | 7 | 7 | **8** |

⇒ 불통과(통과선 9). 회차 **8/10**. 두 사람이 **환산 계수를 각자 재검증**했다(DF k=2.0594 · DG k=2.0633 ↔ 지정 2.0628)
— 0.02~0.17% 차라 아래 지적의 부호를 바꾸지 않는다.

### ⚑ 이번 회차가 고친 것 중 하나를 두 사람이 **수리 전 캡처에서 독립으로 재확인**했다
**리본 라벨 폭** — DF 3순위 «−3.3%(카드1·카드2 리본2 둘 다 −8.2px · 같은 문자열 두 표본이 같은 값)» ·
DG 3위 «카드1 −3.6~−3.7% · **카드2 리본2 는 +0.2% 정상**». ⚑ **DG 가 «카드2 는 정상» 이라고 적은 것이
이 회차 ⓠ 의 «불릿 리본1 은 ref 문자열이 달라 비교가 안 선다» 와 같은 관측이다**(둘 다 «같은 문구인 짝만
비교가 성립한다» 로 갔다). 둘 다 **잉크 높이는 정상**이라고 따로 적었다(DF «ref 26.8 ↔ cap 27» ·
DG «−1%») — **손잡이가 scaleX 이지 font-size 가 아니라는 판정을 비평가 둘이 독립으로 뒷받침한다.**
⇒ 8회차 수리(scaleX 1.035)는 **채점자 둘이 지목한 그 자리를 그 방향으로** 닫은 것이다.

### ⚑ 노치 깊이는 두 사람 다 **지적하지 않았고**, DF 는 «맞은 자리» 로 적었다
DF «우변 노치 3곳 위상·깊이 일치(최심 ref 985 ↔ cap 987–989)» — 이것은 **카드1(배너형)** 이고,
이 회차 ⓟ 가 «배너형은 이미 Δ−0.2 로 맞았다» 로 판정한 그 형이다. **두 판정이 어긋나지 않는다** —
6회차 DD·DE 가 −6~9% 를 낸 것은 초록(불릿형)이었고 그쪽만 −9.1% 였다.

### 2인 일치 — 셋 (다음 회차가 이 순서로)

1. **배너 라벨이 노란 필드 안에서 오른쪽으로 +9~13px 밀렸다** — DF 1순위 «잉크 중심 ref 397.4 ↔ cap 409.0 =
   **Δ +11.6**(카드폭의 1.19%) · 금색면 자체는 일치(경계 Δ −0.6 / +0.6 · 폭 285.8 ↔ 287.0)» ·
   DG 1위 «좌여백 185.7 → **197.4 (+11.7)** · 우여백 27.6 → 19.4 (−8.2) · 필드 중심 기준 ref −0.9 ↔ 우리 +8.5».
   **둘 다 신뢰 «상» 이고 둘 다 «폭은 닫혔다» 를 같이 적었다**(DF +0.1% · DG −0.6%) ⇒ **폭이 아니라 좌우 여백 배분**이다.
   ⚑ **6회차 수리(scaleX .9)의 잔여가 아니다** — 가운데 원점이라 폭을 줄여도 중심은 안 움직인다.
   손잡이는 `.ban>i` 의 `left`/`right`(667 8회차가 상자 중심을 350 → **365.5** 로 옮긴 그 축 · `verify667` [F2]·[R4]).
   ⚠ 그 항이 지키는 값을 건드리는 것이므로 **667 8회차의 근거를 먼저 읽고** 이관할지 정할 것.
2. **수량 숫자 총폭 상한이 ref 자연폭보다 낮다** — DF 2순위 «ref 「16,000」 98.9~100.9 ↔ cap 87~88 = **−12%** ·
   대조군 셋은 −2% 로 맞다 · 잉크 **높이·중심은 일치** ⇒ 가로만 눌렸다» · DG 2위 «네 표본 **부호 전부 음수**
   93.0/95.2/92.6/**105.6** ↔ 88.8 고정 = −4.5/−6.7/−4.1/**−15.9%** · 개별 글리프 크기는 +1.3~1.9% 로 맞고
   **자간이 −7%** 다».
   ⚑⚑ **이것은 7회차 설계의 전제를 정면으로 친다.** 7회차는 «ref 는 자릿수가 늘면 글자를 줄여 폭을 **붙박는다**» 로
   보고 `PV_QTY_ADV = 90`(ref 네 표본의 중앙값 87.65)에 모두 맞췄는데, **ref 는 붙박지 않는다** —
   저장소 안의 자 `tools/scan667b.py`(흰 잉크)를 이 회차에 다시 돌려 확인했다:
   **ref 43 / 42 / 42 / 47 ref px = 환산 88.7 / 86.6 / 86.6 / 96.9** 로 넷째만 +11~12% 넓다.
   **7회차 자신의 표에도 96.9 가 적혀 있고 «88 (−9.2%)» 라고 스스로 적어 두었다** — 알고 고른 대가였는데
   채점자 둘이 그 대가를 −12~16% 로 되돌려 줬다. ⇒ 9회차는 «중앙값에 붙박기» 가 아니라
   **«상한을 ref 최대(≈97~106)까지 열고 넘칠 때만 줄인다»** 를 검토하라(상자 폭 139 · [G3] «넘침 0» 은 유지).
   ⚠ 되돌리는 것이 아니다 — 7회차가 고친 «10,000 이 1,500 보다 넓다» 는 **두 사람 다 «해소됐다» 로 확인**했다
   (DF «둘 다 cap 87–88» · DG «네 표본 88.8±0.1»). 고칠 것은 **상한값 하나**다.
3. **카드1 리본1 꼬리 우단이 −5.9px 짧다** — DF 4순위 «ref 469.4 ↔ cap 463.5 · 게다가 ref 에는 «피시테일»
   오목 노치(깊이 10.3)가 있는데 우리는 378~435행 전부 x=463 고정 = **깊이 0**» ·
   DG 4위 «ref 461.9 ↔ 우리 456 = **Δ −5.9(−1.4%)** · 대조 셋은 +1.1~+2.6 로 **넷 중 이 줄만 음수**».
   ⚑ **두 사람이 소수점까지 같은 −5.9 를 냈다.** 리본1 은 형마다 라벨이 달라 폭이 `RB.w1` 인라인 상수로
   박히는 자리다(3회차 ⓖ) ⇒ 배너 `w1 424` 한 값이 손잡이다. DF 의 «노치 깊이 0» 은 리본2 의
   `::after` 제비꼬리(폭 12)가 **리본1 에는 없다**는 뜻이라 같이 볼 것.

### 1인 지적 (다음 회차 참고)
- DG 5위 «카드1 제목 잉크 **높이** −5.9%(폭은 −1.4% 로 정상)» — 신뢰 중~하(ref 압축 번짐). 카드2 는 −2.4%.
- DF 5순위 «리본 좌측 크림 꼬리 하변·높이» — **DF 자신이 신뢰 «하» 로 내렸다**(임계 200 ↔ 225 에서 높이 차가
  −4.7 ↔ −17 로 3배 흔들린다). A3-ⓑ 그대로의 자리다 — 자를 먼저 세우기 전에는 손대지 마라.
- DF 6순위(⑤ 기록) «배너 알약 잘린 모서리 대각선 4곳에 검정 테가 없다» · DF 7순위 «제목 앞 스파클 +15%»(신뢰 중).
- DG 6위(⑤ 기록) «아트 자리 반투명 판이 ref 에는 없다 · ref 아트는 불릿 알약 2·3행 우단을 덮는다» —
  ⚠ **§14 와 같은 자리를 반대편에서 본 것**이다. §14 는 «배너 카드의 아트 **세로 자리**» 를 굳힌 것이고
  DG 가 본 것은 «불릿 카드의 아트 **가로 자리**» 이므로 서로 안 부딪힌다. 규약상 «아트 내용» 이라 감점 밖.

### 9:13.3 — 두 사람 다 «없음» (이번에도)
DF «카드 안쪽 화소가 하나도 안 어긋난다 — Δ>30 인 화소는 전부 배경 스크림 회색» ·
DG «카드 크롭 3장 모두 Δmax ≤ 15/255 · Δ>20 인 화소 0개». 카드2 하단 잘림은 둘 다 프레임 높이 차이로 보고 감점 안 했다.

## ⚑ 9회차가 할 것 (순서대로)

1. **캡처부터** — `node tools/cap151.js <경로>/151-r30.png --crop` 와 `--crop --h 1600` **둘 다**
   (캡처 PNG 는 커밋 안 하므로 새 클론에는 없다). 두 판을 **같은 회차 것으로** 줘라.
2. **채점** — 8회차 수리 둘(노치 깊이 · 리본 라벨 폭)이 아직 채점을 안 받았다.
   브리핑 «닫힌 자리» 에 **20(노치 깊이 — 형별 43/40)** · **21(리본 라벨 폭 — scaleX 1.035)** ·
   **22(배너 일러스트가 머리띠를 덮는 것 = ref 대로 · §14)** 를 더해라.
3. **1순위 = 배너 라벨 좌우 여백 배분 +9~13px**(위 2인 일치 1번 · 둘 다 신뢰 상 · 둘 다 «폭은 닫혔다» 를 같이 적었다).
4. **2순위 = 수량 폭 상한**(위 2인 일치 2번 — 7회차 전제의 수정이지 되돌림이 아니다. 고칠 것은 `PV_QTY_ADV` 한 값).
5. **3순위 = 카드1 리본1 꼬리 우단 −5.9 + 제비꼬리 노치**(두 사람이 같은 값을 냈다 · 손잡이는 배너 `RB.w1`).
6. **자를 먼저 줘라** — 이 화면은 **ref 자신이 형마다 두 값인 축이 다섯**이다
   (피치 · 앵커 · 노치 길이 · 노치 깊이 · 일러스트 자리). 브리핑에 그 표를 넣지 않으면
   «우리 두 카드가 안 맞는다» 가 계속 유령 지적으로 올라온다(335 «앵커가 둘» 표와 같은 처방).
   **8회차에 실제로 그런 지적이 하나 올라왔다**(6회차 DE [A] → §14 로 기각).
7. ⚠ **`scan833b.py riblabel()` 은 문턱을 반드시 흔들어라** — 1차 문턱(170,170,190)이 −8.8%/−38.6% 라는
   **틀린 값**을 냈고 (190,190,170)·(205,205,150)에서 수렴했다. 기본값은 수렴한 쪽으로 안 바꿨다
   (`--thr` 로 주게 해 두었다) — 스윕이 판정의 일부이기 때문이다(A3-ⓑ).

---

## 9회차 (2026-09-03, sess-1743-20040 루틴 워커 C) — 8회차 2인 일치 **셋 다** 닫았다 · 제품 3자리

캡처 `151-r30`(9:19 · h1600 둘 다 = 8회차 제품) → 채점 **DH·DI** 는 아래 «9회차 채점» 절.
수리는 8회차가 순서까지 적어 넘긴 셋을 그 순서대로 했고, **셋 다 손대기 전에 자를 먼저 댔다**(338 규칙).

### ⓣ 1순위 «배너 라벨 좌우 여백» — **뿌리는 «자리» 가 아니라 «넘침» 이었다** (제품 1자리)

세 사람의 ref 기대값이 서로 어긋나 있어서(667 8회차 367.7 · DF 357.4 · DG ≈363.3) **한 정의로
ref·우리를 같이 재는 자**를 신설했다 — `tools/scan833c.py`(칸 = 노랑 채움 · 잉크 = 순백 코어).

| (카드-로컬 우리 px) | ref | 수리 전 | 수리 후 |
|---|---|---|---|
| 노랑 칸 | 217.7..502.4 | 218.0..505.0 | 무변경 |
| 잉크 폭 | 235.2 | 235.0 | 234.0 |
| 좌여백 / 우여백 | 22.7 / 26.8 | **34.0 / 18.0** | **23.9 / 28.1** |
| **잉크중심 − 칸중심** | **−2.1** | **+8.0** | **−2.5** (Δ **+10.1 → −0.4**) |

문턱 **235 / 246 / 250** 셋에서 값·부호가 **안 흔들렸다**(A3-ⓑ 판정법).
⚑ **그 10.1 의 정체가 «넘침» 이다** — 상자는 `.ban{border:7}` + `box-sizing:border-box` 탓에 실폭
**255** 인데 이 문자열 advance 가 **264.7** 이라 9.7px 넘쳤고, 넘치는 줄은 `text-align:center` 가 죽어
**왼끝에서 시작한다**(667 [G3] 이 수량에서 겪은 사고). 667 8회차는 그것을 알고 «넘침의 절반» 을
`left` 로 갚아 놨는데(6회차 [10-d] 가 그 9.6 을 굳혔다) **갚은 자리가 ref 가 아니었다.**
⇒ 갚지 않고 **없앴다** — `left:181→161 · right:12→4`(실폭 283 > 264.7 · 여유 18.3)로 가운데 정렬이
실제로 서게 하고 상자 중심을 **359.5**(= 칸 중심 361.5 + ref 편차 −2.1)에 두었다. 이제 상자 중심 = 잉크 중심.
⚠ 잉크 좌단은 여전히 크림 칸을 안 밟는다(분할선 216.3 ↔ 잉크 좌단 242.4 · 667 [F1] 초록).
⚠ **여유를 2.3px 로 두는 안(`left:169;right:12`)은 버렸다** — 서체가 조금만 달라져도 넘침이 되살아나
[10-d] 가 환경마다 갈린다(플레이키 게이트 계보 574·709·825).

### ⓤ 2순위 «수량 폭 상한» — **한 값이 아니라 «형마다 두 값»** (제품 1자리)

`scan667b.py`(흰 잉크)를 다시 돌려 넷을 형별로 갈랐다:

| 표본 | ref | 수리 전(상한 90) | 수리 후 |
|---|---|---|---|
| 배너형 rb1 «1,500» | 88.7 | 87 | 87 (무변경) |
| 배너형 rb2 «10,000» | 86.6 | 88 | 88 (무변경) |
| 불릿형 rb1 «1,500» | 86.6 | 87 | 87 (무변경) |
| **불릿형 rb2 «16,000»** | **96.9** | **88 (−9.2%)** | **96 (−0.9%)** |

⚑ **빨간 것은 한 자리뿐이었다** — 배너형 둘은 Δ−1.7~+1.4 로 이미 맞아 있었고(DF 도 «대조군 셋은
−2% 로 맞다» 로 적었다), 8회차가 권한 «상한을 ref 최대 97 로 연다» 를 그대로 하면 **맞아 있던
배너형 «10,000» 이 86.6 → 97 = +12% 로 틀어진다.** ⇒ 이 화면이 이미 다섯 번 증명한 «형마다 두 값»
법칙(피치·앵커·노치 길이·노치 깊이·일러스트 자리)을 여기에도 적용해 `PV_QTY_ADV` 를
**{ban:90, bl:99}** 로 갈랐다(99 = 90 × 96.9 ÷ 88). 다섯 자리는 두 형 다 상한 아래라 **배율 1 = 무변경**.
⚠ 되돌리는 것이 아니다 — 7회차가 고친 «자릿수가 늘어도 안 줄어든다» 는 그대로 산다.

### ⓥ 3순위 «배너 리본1 꼬리 우단» — **«ref 도 갈린다» 는 «맞춰 놨다» 가 아니었다** (제품 1자리)

3회차는 «리본1 은 ref 도 430.2 ↔ 446.7 로 갈리니 w1 은 안 건드린다» 로 넘겼는데,
`scan833b.py` 로 형별로 짝지어 보면 **불릿형은 ref 446.7 ↔ 우리 445.0(Δ−1.7 = 맞다)** 이고
**배너형만 ref 430.2 ↔ 424.0 = −6.2** 였다(DF·DG 가 각자 −5.9 로 2인 일치).
⇒ 배너 `RB.w1 424 → 430` · `--gx` 는 리본 오른끝 기준이라 **g1 26 → 32**(3회차가 w2 에서 한 그대로)
⇒ 금색 판 중심 **352.50 · 441.50 무변경**(667 [F3] 무수정 초록)으로 판·수량은 한 픽셀도 안 움직였다.
수리 후 실측 **430.0**(ref 430.2 · Δ−0.2).
⚠ DF 4순위의 **«리본1 에는 제비꼬리 노치가 없다(ref 깊이 10.3)»** 는 **1인 지적이라 이번엔 안 건드렸다** —
다음 회차가 DH·DI 의 판정을 보고 잡을 것(리본2 의 `::after` 는 ref 3 ref px 라 리본1 과 값이 다르다).

### 게이트 (9회차)

`verify833` **189/189 → 194/194**(§7 [7-b] · §10 [10-d]·[10-f]·[10-h] · §11 [11-a]·[11-f] **이관** ·
[11-g] 신설(«같은 문자열이 형마다 다른 상한에 맞는가») · §R **[R13]·[R13b]·[R14]·[R15] 신설**) ·
`verify667` **86/86 이관**([F2] 365.5 → **359.5** · [G2] 리본1 424 → **430** · [R4] 되돌림 목표 동반 이동 —
셋 다 **333 처방으로 방향만 갈아 끼웠고 지운 항은 0** 이다) ·
`scan833c.py` **신설**(문턱 스윕 `--thr`) ·
회귀 무수정 — `verify151` 84/84 · `verify124` 76/76 · `verify152` 44/44 · `verify164` 31/31 ·
`verify477` 35/35 · `verify480` 22/22 · `verify45` 39/39 · `verify267` 67/67 · **SMOKE PASS**.
