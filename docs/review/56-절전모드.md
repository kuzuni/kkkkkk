# 56 — 절전 모드 (진행 기록)

- 스펙: `docs/UI-REFERENCE.md` 56 · 측정표: `docs/measure/56-절전모드.md` · 레퍼런스: `docs/ref/56-절전모드.jpg`
- 구현: `#svw`(신설) · `#app.sv` 상태 클래스 · `openSaver()`/`closeSaver()`/`renderSaver()` · `loop()` 의 saverOn 분기
- 게이트: `node tools/verify56.js` (실동작) · `node tools/smoke.js` · `python3 tools/scan56.py cmp <캡처>` (잉크 bbox 대조)
- 캡처: `node tools/cap56.js docs/review/56-rN.png [--geo]`

---

## 0. 설계 — 레퍼런스 실측이 지시서 전제를 3군데 뒤집었다

측정표 §0 참조. 요약:

| 지시서 전제 | 레퍼런스 실측 | 이번 구현 |
|---|---|---|
| «화면을 어둡게»(딤) | 딤이 아니라 **완전 불투명 검정**(α=1.00, 6개 잉크 밴드 밖 `max(RGB)>3` 픽셀 **0개**) | `background:#000` + 하위 레이어 `visibility:hidden` |
| «전투는 계속»(보인다) | 전투가 **1픽셀도 안 보인다**. 로직만 돈다(방치 시간 `00:00:02` 진행 중) | `step(dt)` 만 돌리고 `draw/drawHud/drawTuto/renderUI` 전부 건너뜀 |
| «터치로 해제» | 화면 문구는 **«밀어서 잠금 해제»** | 문구는 레퍼런스대로, 동작은 **탭 1회 또는 120px 스와이프 둘 다** 수용 |

> 문구·동작이 어긋나는 지점은 저장소 주인 확인이 필요하다. UI-REFERENCE 는 «터치로 해제», 레퍼런스 이미지는
> «밀어서 잠금 해제» 다. 지금은 **레이아웃을 레퍼런스에 맞추고 동작은 둘 다 받는** 쪽으로 뒀다.

«전투는 계속, 렌더는 정지» 는 말이 아니라 숫자로 확인한다 — `verify56.js` 가 `CanvasRenderingContext2D.fillRect`
호출을 세서 **절전 1.5초 동안 0회 / 해제 직후 0.6초에 168회**를 확인한다. 같은 구간에서 `S.playtime` 은 1.52초 흐른다.

---

## 1회차 (2026-08-25, sess-0805-18245 워커 A) — 구현 + 자체 잉크 대조

죽은 lock(05:32, 153분) 회수. 앞 세션이 커밋한 산출물은 없었다(측정표·리뷰·`git log '(56)'` 전부 0건) → 처음부터.

### 한 일

1. 측정표 신설(`docs/measure/56-절전모드.md`) — 서브에이전트가 레퍼런스를 Pillow 로 직접 스캔.
2. `#svw` 오버레이 신설(CSS `<style>` 맨 끝 «56» 절 + `#app` 마지막 자식) · `openSaver/closeSaver/renderSaver`
   · ▦ 메뉴 `saver` 분기를 placeholder 팝업 → `openSaver()` 로 교체.
3. `loop()` 에 saverOn 분기 — 렌더만 끊고 `step`·`save`·`dailyCheck` 는 유지.
4. 게이트 3종 신설: `tools/cap56.js`(캡처+기하), `tools/scan56.py`(잉크 bbox 스캐너), `tools/verify56.js`(실동작).

### 자체 잉크 대조 (`scan56.py cmp`) — 레퍼런스와 **같은 스캐너**로 잰다

레퍼런스도 캡처도 «검정 바탕 + 흰 잉크» 라 스캐너 하나로 둘 다 잰다.
먼저 **레퍼런스를 스캐너로 다시 재서 측정표를 검산**했다(13항목 중 최대 오차 2px) → 측정표 신뢰 확보.

| 요소 | 목표(측정표) | 4회 조정 후 실측 | Δ |
|---|---|---|---|
| 배터리 그룹 | 967.5, 28.5, 84×46 | 968, 29, 84×46 | ≤1 |
| 시계 | 347, 624, 386×115 | 346, 624, 387×114 | ≤1 |
| 날짜 | 410, 764, 260×38 | 411, 764, 259×39 | ≤1 |
| 스컬 배지 | 368, 1031, 58×56 | 368, 1031, 57×56 | ≤1 |
| `STAGE N` | 438, 1041, 204×34 | 436, 1041, 207×34 | ≤3 |
| 패널 | 239.5, 1128, 601×351 | 240, 1128, 601×351 | ≤0.5 |
| pill 라벨 3종 | 360 / 1196·1290·1384 | 360~362 / +1 | ≤2 |
| pill 값 3종 | 우단 759 | 759 (E3 −3) | ≤3 |
| 해제 안내 | 385, 2040, 312×43 | 383, 2039, 312×43 | ≤2 |

**허용(±3px) 밖 = 1건뿐이고, 그것도 레이아웃이 아니라 «값» 이다** — 배터리 텍스트가 레퍼런스는 `10%`(잉크 47),
우리 헤드리스 기기는 `100%`(잉크 65)라 18px 넓다. 측정표 §6-5 «가짜 값을 넣지 말 것» 을 따라 실제 잔량을 쓴다
(API 가 없는 브라우저면 배지를 통째로 숨긴다).

### 타이포 보정 근거 (숫자만)

컨테이너(패널·pill·배터리 몸통·스컬 원판)는 측정표 값을 그대로 적으면 1px 안에 떨어졌다. 어긋난 건 전부 **글자 잉크**다 —
컨테이너 폰트가 레퍼런스 서체와 달라서다. 잉크 높이로 font-size 를, 잉크 폭으로 `scaleX` 를 역산했다:

| 요소 | 1차 | 실측 잉크 | 보정 | 결과 |
|---|---|---|---|---|
| 시계 | fs157 | 113×381 | fs**160** | 114×387 (목표 115×386) |
| 날짜 | fs46 `.` 구분 | 32×227 | fs**51** + `/` 구분 + scaleX **1.03** | 39×259 (목표 38×260) |
| `STAGE N` | fs47 | 32×196 | fs**48.5** + scaleX **.903** | 34×207 (목표 34×204) |
| 라벨 3종 | fs35 | 33×149/261/182 | fs**28** + scaleX **.87**(좌측 기준) | 27×103/184/127 (목표 27×108/183/125) |
| 값 3종 | fs30 | 20×120 | fs**32** + scaleX **1.086**(우측 기준) | 22×137 (목표 21×139) |
| 해제 안내 | fs57 | 52×416 | fs**47** + scaleX **.907** | 43×312 (목표 43×312) |
| pill 아이콘 | fs42 | 53×47 | fs**37.5**, 칸별 scaleX .74/1/.89 | ref 35×42 / 47×42 / 44×44 |

날짜는 구분자를 `.` → `/` 로 바꿨다. 레퍼런스가 **MM/DD/YYYY** 인데 우리는 한국식 **YYYY/MM/DD** 로 순서만 바꿔
같은 글리프 집합을 쓴다 — 잉크 폭·높이가 자동으로 레퍼런스와 같아진다(측정표 §6-4-2 의 «로케일 확인 필요» 처리).

### 정렬 함정 — 측정표 §3-4 가 짚은 것을 그대로 맞았다

`☠ STAGE 80` 은 **텍스트만** cx539.5 정중앙이고, 아이콘 포함 묶음 중심은 504.5 다.
flex row + `justify-content:center` 로 묶었으면 35px 어긋난다 → 텍스트를 화면 중앙 정렬하고 스컬은 `left:368px` 로 덧붙였다.

### 게이트

- `node tools/verify56.js` → **VERIFY56 PASS 20/20**
- `node tools/smoke.js` → **SMOKE PASS** (`menu:saver` 오프너 자동 수집 + 화면비 6종에 «절전 모드 잘림 없음» 신설)
- 캡처: `docs/review/56-r1.png`(초안) → `56-r4.png`(보정 후, 채점용)

### 기능 체크 표 (T2 «기능 완성 규칙» 형식)

| 조작 | 기대 | 실제 (verify56) |
|---|---|---|
| ▦ → 🔋 절전 | 전면 검정 + 하위 레이어 차폐 | `#svw.on` · `#app.sv` · `#top`/`#tabbar` `visibility:hidden` ✓ |
| 절전 유지 1.5초 | 캔버스 드로우 0 | `fillRect` **0회** ✓ |
| 절전 유지 1.5초 | 전투 로직은 계속 | `S.playtime` **+1.52s** ✓ |
| 초 경과 | 방치 시간 라이브 | `00:00:00 → 00:00:01` ✓ |
| 처치·골드 발생 | 3행이 «진입 이후 증분» | 처치 +7 → 표시 Δ7 · 골드 +12345 → `+12.3K` ✓ |
| 30px 드래그 | 해제 안 됨 | 유지 ✓ |
| 200px 스와이프 | 해제 | 해제 + HUD 복귀 + 렌더 재개(0.6초 168 드로우) ✓ |
| 탭 1회 | 해제(UI-REFERENCE) | 해제 ✓ |
| 재진입 | 방치 시간 0 부터 | `00:00:00` ✓ |
| 배터리 | 실제 잔량, 없으면 숨김 | `100%`(헤드리스 실제값) ✓ |

### 한 번 틀렸다가 잡은 것 (다음 세션용)

**«초당 1회 갱신» 을 dt 누적으로 세면 한 칸씩 늦게 보인다.** `svTick += dt; if(svTick>1)` 로 짰더니
`dt` 가 0.1 로 클램프돼 있어 누적이 실경과보다 뒤처지고, 갱신이 **초 경계 직전**에 걸려
1.9초가 지나도 화면은 `00:00:00` 이었다(verify56 이 잡았다). `Math.floor((now−svT0)/1000)` 이 바뀌는 순간에만
그리도록 바꿔서 드리프트를 0 으로 만들었다.


---

## 2회차 (2026-08-25, sess-0805-18245 워커 A) — 비평 A·B 반영

### 1회차 채점 (캡처 `docs/review/56-r5.png`)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| A | 9 | 8 | 8 | 9 | 7 | 5 | **8** |
| B | 9 | 7 | 8 | 8 | 9 | 6 | **7** |

두 비평가 모두 «신뢰도 검산 3건» 을 먼저 재게 했고(LESSONS 30 덤), 6건 전부 우리 기대치와 ≤1px 로 일치했다 —
즉 이후 지적은 좌표계 오독이 아니다. 두 사람이 **독립적으로 1순위를 같은 것**으로 짚었다: **획이 40~67% 얇다.**

### 진짜 원인은 서체가 아니라 우리 버그였다

두 비평가 다 «헤비 게임 서체 없이는 회수 불가» 로 분류했지만, 실제 원인은 **`font-weight` 가 400 이었던 것**이다.
`#svw i,#svw b,#svw s,#svw u,#svw em{...font-weight:900}` 리셋이 **태그 선택자**라서
`div` 인 `.sv-clk`·`.sv-dt`·`.sv-hint` 를 못 잡았다(LESSONS 21-① «#id 태그 리셋» 함정의 자매판 —
그 교훈은 «특이성» 얘기였고 이건 «선택 범위» 얘기다). `#svw>div{font-weight:900}` 한 줄로:

| 잉크 화소 수 | 1회차 | 2회차 | 레퍼런스 |
|---|---|---|---|
| 시계 | 14,171 | **22,493** | 20,921 |
| 해제 안내 | 3,912 | **5,686** | 7,450 |
| 날짜 | 3,045 | **4,800** | 5,095 |

**«서체 한계» 로 접기 전에 weight 가 실제로 걸렸는지부터 확인할 것** — 이번엔 접었으면 회차를 통째로 날릴 뻔했다.

### 반영표

| 비평 | 지적 | 조치 | 결과(잉크 실측) |
|---|---|---|---|
| A①·B① | 안내 문구 잉크 좌단 383 → 385 | `translateX(1.5px)` + scaleX .907→.919 | **385** (ref 385) |
| A② | 스컬 붉은 원반 51×50 → 48×49 | inset 링 3 → **4.5px** (bbox 57×56 유지) | 원반 48×50 |
| A②·B⑥ | 날짜 잉크 높이 39 → 36 | fs 51 → **48.7**, scaleX 1.03 → **1.08** | **260×36** (ref 260×37) |
| B② | 시계 글리프가 좁고 획이 얇다 | weight 900 + fs160 + scaleX **.97** | **385×114** (ref 386×115), 획 15 → 23 |
| B④·A④ | pill 라벨 좌단 362/361/361 (기준 360) | 라벨 left 70.5 → **67.5px** | 360 / 358 / 359 |
| B④ | 값 우단 757 (기준 758~759) | 값 right 30.5 → **29.5px** | 우단 **759** |
| A③ | 패널 좌/우 패딩 50/51 비대칭 | pill left 50 → **50.5px** | 50.5 / 50.5 대칭 |
| B② | pill 높이 64 vs ref 64.6 · 행 간격 30 vs 29 | pill height 64 → **64.5px** | 간격 29.5 |
| A③ | STAGE 아이콘↔텍스트 간격 15 → 17 | 텍스트 scaleX .903 → **.89** (좌단 436 → 438) | 간격 17 |
| A④ | 라벨·값 세로 중심 +1~2px | `--rlty` 17 → 16 · `--rvty` 20 → 19 | 라벨 y1196 · 값 y1199 (ref 1196 / 1199) |

### 2회차 자체 대조 (`scan56.py cmp docs/review/56-r8.png`)

허용(±3px) 밖 **3항목뿐이고 전부 «레이아웃이 아닌» 것**이다:
- 배터리 글자 `100%`(실제 잔량) vs ref `10%` — 자릿수 차이(폭 +18)
- 라벨1 «방치 시간» 폭 103 vs 108 — 같은 scaleX 에서 라벨 2·3 은 ±2px 로 맞는다(글리프 어드밴스 차)
- 값3 `+0` 폭 39 vs 37

게이트: `VERIFY56 PASS 20/20` · `SMOKE PASS`. 채점 캡처는 `docs/review/56-r8.png`.


---

## 3회차 (2026-08-25, sess-0805-18245 워커 A) — 비평 C·D 반영

### 2회차 채점 (캡처 `docs/review/56-r9.png`)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| C | 9 | 8 | 8 | 9 | 9 | 6 | **8** |
| D | 9 | 7 | 9 | 9 | 8 | 6 | **7** |

1회차 대비: ① 9→9 · ② 7~8→7~8 · ③ 8→8~9 · ④ 8~9→9. **획 두께 지적(1회차 1순위)은 회수됐다** —
시계 획 15 → 22~23px(ref 24~25), 안내 3 → 5px. 두 비평가 다 «pill 값 우단 3행 플러시 해결» 을 확인했다.

### 3회차 반영표 — «잰 사람이 셋 다 같은 값을 말하면 그게 맞다»

| 비평 | 지적 | 판정 근거 | 조치 | 결과 |
|---|---|---|---|---|
| C②·D④ | 패널이 601×351 @(239.5,1128) 인데 ref 는 **600×350 @(240,1129)** | 비평가 2명 + 자체 스캐너 **3자 일치**(측정표 초판이 틀렸다) | 패널 규격 교체 · pill left 50.5→50 | 캡처 **240/1129/600×350 = ref 완전 일치**, 좌우 여백 240/240 대칭 회복 |
| D①-2 | 시계·날짜·STAGE 텍스트가 **일괄 1px 위** | 계통 오차라 3개 같이 이동 | `--clky` 621→622 · `--dty` 755.5→756.5 · `--stty` 2→3 | 625 / 765 / 1042 (ref 625 / 765 / 1042) |
| D④ | 시계 잉크 cx 540.5 (ref 539.5) | | `translateX(-1px)` | cx **539.5** |
| C③·D③ | 시계↔날짜 간격 26 (ref 27~28) | | 날짜 +1px 로 동시 해결 | 27 |
| **D②-1 (최저 항목)** | pill 아이콘 **폭은 0px 인데 높이만 −4.5~6.7%** | | fs 를 올리고 **올린 만큼 scaleX 로 폭을 되돌림** ⏱️ 37.5→39.3/.706 · 💀 →40.2/.862 · 🪙 →42/.833 | 잉크 **35×44 / 50×45 / 44×47** (ref 36×44 / 48×45 / 45×47) |
| D②-5 | 스컬 배지 안 흰 해골 잉크 **27×33** (ref 44×45) — «아트 없이는 불가» 분류 | 실루엣은 불가지만 **크기는 회수 가능**했다 | `--skfs` 30→40.5 · `--sksx` 1.25→1.19 · 박스 44 | **43×44 @ (375,1035)** (ref 44×45 @ 375,1036) |
| D②-2·C② | 배터리 테두리 5px (ref 4.3~4.75) | 측정표 초판 «5» 가 과대 | `border` 5 → **4.5px** | 4.5 |
| C①·D④ | 🪙 아이콘 세로 중심 +1~2.5px | | 3행 아이콘에 `--icdy:-2px` | pill 중심과 정합 |
| D③-3 | STAGE 배지 하단↔패널 상단 41 (ref 43) | 패널 top 1128→1129 로 자동 해결 | — | 42 |

### 3회차 자체 대조 (`scan56.py cmp docs/review/56-r12.png`)

허용(±3px) 밖 **3항목**, 2회차와 동일하게 **전부 «레이아웃이 아닌» 잔차**다:
배터리 글자 자릿수(`100%` vs `10%`) · 라벨1 폭 103 vs 108(글리프 어드밴스) · 값3 `+0` 폭 39 vs 37.

게이트: `VERIFY56 PASS 20/20` · `SMOKE PASS`. 채점 캡처는 `docs/review/56-r12.png`.

> **교훈 후보** — 비평가가 «아트/서체 없이는 회수 불가» 로 분류한 항목도 **크기·bbox 는 대개 회수 가능**하다.
> 이번 회차의 최저 항목(② 7점)을 만든 3건(pill 아이콘 높이 · 흰 해골 잉크 · 배터리 테두리)이 전부 그랬다.
> «불가» 딱지는 **실루엣·획 두께·자형**에만 붙여야 한다.


---

## 4회차 (2026-08-25, sess-0805-18245 워커 A) — **통과** (E 8 / F 8)

### 3회차 채점 (캡처 `docs/review/56-r13.png`)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** | 판정 |
|---|---|---|---|---|---|---|---|---|
| E | 9 | 8 | 8 | 8 | 8 | 7 | **8** | 통과 |
| F | 9 | 8 | 9 | 9 | 8 | 7 | **8** | 통과 |

두 비평가 모두 신뢰도 검산 3건을 먼저 통과했고(패널 600×350@(240,1129) 대칭 확인 · pill 피치 94 · 하단 문구 312×43 cx541),
**①~④ 최저 8 이상 = 통과**다. 회차 누적 **4/10**.

점수 추이: 1회차 A 8 / B 7 → 2회차 C 8 / D 7 → **3회차 E 8 / F 8**.

### 비평가 엇갈림 1건 — 임계값 스윕으로 판정 (LESSONS 52-①)

E·F 둘 다 «배지 안 흰 해골 잉크가 ref 41×37 인데 우리는 43×44 = +18.9% 과대» 라고 **독립적으로 같은 지적**을 했다.
그대로 믿고 줄였으면 **반대 방향으로 틀렸다.** 같은 창(x366..430, y1028..1092)에 **임계값을 쓸어서** 양쪽을 같은 자로 쟀다:

| 임계 | 레퍼런스 | 우리 캡처 |
|---|---|---|
| 140 | 46×47 | 44×45 |
| 170 | 46×47 | 44×45 |
| 200 | 44×45 | 43×44 |
| 230 | 40×36 | 42×41 |

**임계 140~200 에서는 우리 쪽이 1~2px 작다.** 230(순백만)에서만 뒤집히는데, 레퍼런스 해골은 크림색 음영(246,225,223)이라
순백 임계에서 잉크가 급격히 깎이고 우리 이모지는 흰 면이 넓어 덜 깎이기 때문이다.
→ **«서로 다른 임계로 잰 두 수를 비교한 것»** 이 원인이고, 3회차 확대는 옳았다. 크기 변경 없음.

같은 이유로 F 의 «캡처에만 있는 여분 외곽 링» 도 감점을 반영하지 않았다 — 그 링은 측정표 §3-4 가 레퍼런스에서
실측한 `#101010` 외곽 링(16,16,16)이고, 우리 `box-shadow:inset 0 0 0 4.5px #101010` 이 그것을 재현한 것이다.
배지 bbox 는 57×56 으로 ref 58×56 과 일치한다.

### 마감 보정 3건 (E·F 가 같은 방향으로 짚은 «1px 과교정» 만)

| 비평 | 지적 | 조치 | 결과 |
|---|---|---|---|
| E④·F④ | 시계 잉크 cx 538.5~539 (ref 539.5~540) — 3회차 −1px 이 과했다 | `translateX` −1 → **−0.5px** | 잉크 x347 w386 → **cx 540 = ref 완전 일치** |
| E③ | 패널 내부 상/하 패딩 49/48 비대칭 (pill 이 0.9px 두껍다) | pill height 64.5 → **64.4px** | 상/하 49.0 / 48.6 (ref 48.9 / 49.0) |
| E①·F① | 🪙 아이콘이 pill 중심보다 2.5~3px 위 — 3회차 `--icdy:-2px` 가 과했다 | `--icdy` −2 → **−0.5px** | 아이콘 중심 −0.7px (ref −0.6px) |

최종 캡처 `docs/review/56-r14.png` · `scan56.py cmp` 허용(±3px) 밖 **3건**(전부 레이아웃 아님: 배터리 자릿수 · 라벨1 폭 103/108 · 값3 폭 39/37).
게이트: `VERIFY56 PASS 20/20` · `SMOKE PASS`.

### 남은 감점 — «아트 리소스/게임 서체 없이는 회수 불가» (4명 중 4명 동의)

- **디스플레이 서체**: 시계 «0» 글리프 폭 −17~18%, 획 두께 −13~16%, 숫자 자간 −28%, 한글 라벨 잉크 높이 +4~8%.
  잉크 **블록** 폭·높이는 이미 0~1% 로 맞췄다 — 남은 건 글리프 자형이라 서체 교체 전에는 회수 불가.
- **아트 스프라이트**: 스컬 배지(뿔 달린 전용 해골 + 입체 음영 원판), pill 아이콘 3종(⏱️/💀/🪙)의 종횡비.
  전부 «아트 자리 규칙» 대로 **레퍼런스 bbox 를 정확히 차지**하고 있어 이미지가 들어오면 교체만 하면 된다(측정표 §5 «아트 필요» 표).
