# 442 — 적 머리 위 체력바 앵커가 «그려진 스프라이트» 와 안 맞는다 (T1 «버그»)

> 세션 `sess-0816-5907` 워커 A · 2026-08-30 · **1회차로 닫음** · 제품 `index.html` **5자리**
> (`frameInk` 1줄 + `eHeadTop` 신설 + `measureHudBox`/`hudBoxSync`/`fxClampY` 신설 + `eBarBox` 신설 + 그리기 블록 4줄 치환)
> 레이아웃(DOM) **Δ0px** · 밸런스 계수 **0줄** · 판정·히트박스 **0줄** · 세이브 이관 **없음**
> 게이트 `verify442` **47/47 신설** · 재현 `probe442` 신설 · 이관 `verify348` **41/41**(하네스 2자리) ·
> 회귀 12종 무수정 · **SMOKE PASS**

---

## 1. 등재문과 재현

등재문(2026-08-30, sess-0537-19157 워커 B — 425 비평 1·3·4회차에서 비평가 **다섯 명(CQ·CR·CU·CV·CX)이
독립으로** 잡았다)은 두 가지를 말했다.

- ⓐ 던전 보스에서 바가 머리 위 **131px** 에 떠 있다(빈 배경).
- ⓑ 적이 화면 위쪽에 서면 그 오프셋이 바를 HUD 띠로 밀어 넣어 «간격 0px»(CQ) · «8행 중 6행 = 75% 가림»(CV).

338 규칙대로 처방을 따르기 전에 **재현**했다 — `tools/probe442.js` 신설. 스테이지 몹 3종 + 스테이지 보스 +
던전·탑 보스 8종(표본 18개)을 실제 진입점으로 세우고, 아틀라스 픽셀에서 **그려진 잉크 윗변**을 직접 읽어
바 밑변과의 거리를 쟀다.

**등재문의 ⓐ 는 그대로 확인됐고(131.8px — 등재 실측 131 과 0.8px 차이), ⓑ 는 등재문보다 더 컸다**
(화면 16% 지점에서 **135.4px** 파고듦). 그리고 재현이 **등재문에 없던 절반을 찾았다** — 아래 §2.

## 2. 뿌리 — «떠 있다» 는 증상의 절반일 뿐이었다

바 자리는 `index.html` 한 줄이었다.

```js
const w = Math.max(22, e.r*2.2), h = 4, by = e.y - e.r*3.1 - 6;
```

`e.r` 은 **히트박스 반경**(충돌·스폰 간격·AI 가 쓰는 값)이고, 그림 크기는 `ETYPE[].scale` 이 따로 정한다.
둘은 아트마다 다른 비율로 붙어 있어서 `e.r*3.1` 이 «머리 높이» 와 맞을 이유가 애초에 없다.
실측(수리 전, 표본 18개 · 게임px · 양수 = 빈 배경 · 음수 = 바가 그림 안으로 파고듦):

| 표본 | r | scale | 그려진 잉크 윗변 | 바 y | 빈 배경 |
|---|---|---|---|---|---|
| 좀비 | 17 | 0.150 | −35.1 | −58.7 | **+19.6** |
| 고블린 | 13 | 0.32 | −41.9 | −46.3 | +0.4 |
| 다크엘프 | 21 | 0.44 | −59.8 | −71.1 | +7.3 |
| 군주(스테이지 보스) | 54 | 1.65 | −222.8 | −173.4 | **−53.3** |
| gold·stone(용) | 90 | 3.48 | −149.2 | −285 | **+131.8** |
| relic3 | 40 | 1.026 | −232.5 | −130 | **−106.5** |
| … 나머지 12종 | | | | | −59.7 ~ +131.8 |

- **떠 있는 자리는 3개**(gold·stone 두 던전이 같은 용 아틀라스를 쓴다 — 등재문이 본 자리).
- **바가 그림 «안» 으로 파고든 자리가 12개**다. 등재문은 이쪽을 안 봤다 — 보스 열두 자리에서 바는
  머리 위가 아니라 **몸통 위**에 그려지고 있었고, relic3 은 106.5px = 그림 높이의 45% 만큼 내려와 있었다.
  즉 결손은 «한 방향으로 어긋남» 이 아니라 **«앵커가 아트와 아무 관계가 없음»** 이었다.
- 박스 불일치 배수(바 앵커 ÷ 잉크 윗변)는 **0.557 ~ 1.91** 로 흩어져 있었다(등재문의 «1.59배» 는 그중 한 자리).

## 3. 처방 — 등재문의 ⓐ·ⓑ 둘 다, 그리고 새 상수 0개

### 3-1. 앵커: `e.r*3.1` → **그려진 잉크 윗변**(425 `frameInk` 의 세로 짝)

425 가 카메라 연출을 위해 아틀라스 픽셀에서 «잉크 행/열의 중앙값»(`my`/`mx`)을 뽑아 두었다.
**같은 픽셀 패스에 한 줄을 얹어 «첫 잉크 행»(`ty`)도 같이 돌려준다** — 픽셀을 두 번 읽지 않으므로 비용 0.

```js
let ty = 0; while(ty < fr[3] && row[ty] === 0) ty++;
m[nm] = n === 0 ? null : { my: mid(row), mx: mid(col), ty };
```

그 위에 `eHeadTop(e)` 를 세웠다. **식의 꼴은 한 글자도 안 바꿨다** — 옛 식이 이미 «머리 위 6px» 이라고
적혀 있었고, 갈아 끼운 것은 «머리가 어디냐» 하나다. ⇒ **새 상수 0개.**

### 3-2. 자를 «어느 프레임으로 잡는가» — 여기가 이 작업의 판단이다

세 후보를 실측으로 견줬다(용 `fly` 6장의 잉크 윗변 = −64 · −50 · −46 · −42 · −42 · −42 아틀라스px,
배율 3.48 ⇒ 세로로 **76.6px** 폭):

| 후보 | 최악 빈 배경 | 프레임마다 흔들리나 | 모션 갈아탈 때 튀나 |
|---|---|---|---|
| 매 프레임 다시 잰다 | 0 | **76.6px 떤다** | — |
| 애니 전 프레임의 **최소**(가장 위 포즈) | **58.8px** | 안 떤다 | 튄다 |
| 애니 전 프레임의 **평균** | 38px + 38px 덮음 | 안 떤다 | 튄다 |
| ✅ **쉬는 자세(`T.walk`)의 중앙값** | **3.7px** | 안 떤다 | **안 튄다** |

- **중앙값을 고른 근거는 425 안에 이미 있다** — 425 5회차가 같은 아틀라스에서 «bbox 는 치켜든 날개 끝과
  꼬리에 끌려다녀 포즈마다 크게 튀고, 중앙값은 몸통을 따라간다» 를 실측으로 못박았다. 용 6장 중 **4장이
  −42 인데 한 장이 −64** 라, 최소·평균은 그 한 장이 통째로 끌고 간다.
- **`e.anim`(지금 모션)이 아니라 `T.walk`(쉬는 자세)** 를 본다. 현재 모션으로 재면 공격 모션으로 갈아타는
  순간 바가 튄다 — knight `idle` −45 ↔ `attack_A` −63 은 승급 수호자 배율 4.84 에서 **87px** 이다.
  ⇒ 적 종류마다 상수 하나 · 캐시 키 `akey|anim` · 값은 아틀라스px 라 배율·`yo` 와 독립이다
  (178 던전 보스처럼 `ETYPE.dunboss` 칸을 갈아 끼워도 캐시가 안 상한다).
- 대가는 **용 한 아틀라스의 날개 최고점 한 장**이다 — 그 프레임에서만 날개 끝이 4px 짜리 바를 74.6px
  넘어선다. 바는 스프라이트 **뒤에 안 깔리고 위에 그려지므로** 가려지지 않는다. `verify442` [2-c] 가
  이 값을 표본마다 **기록**한다(감점이 아니라 설계의 값표다).

### 3-3. 세로 클램프: 상단 HUD 띠 아래로 (등재문 ⓑ)

가로에는 이미 짝이 있다 — 13회차 AQ①·AR#10 이 «좌측 사이드 아이콘 열 위에 바가 그려진다» 를 잡아
`fxClampX` + `sideBox`(DOM 에서 잰 띠)를 세워 뒀다. **세로도 같은 처방·같은 여유(6px)로** 짰다.

```js
const HUD_TOP_IDS = ['dunHud', 'stinfo', 'bossTm', 'bossHp'];
function fxClampY(wy){ const hb = hudBox; return hb ? Math.max(wy, hb.y2 + 6 - camOy) : wy; }
```

- **상수를 안 박은 이유**는 368 이 verify348 에서 겪은 것 그대로다 — 띠 높이는 모드마다 다르고
  (`#dunHud` ↔ 28 규격 `#stinfo`·`#bossTm`·`#bossHp`), `#stinfo` 의 자리는 프레임 높이·`#top` 유무를 탄다.
  «그때의 화면비에서만 맞는 값» 을 박으면 다음 레이아웃 작업이 그것을 조용히 낡게 만든다.
- **다시 재는 순간**은 둘 — 레이아웃 변경(`layoutSide()`, `measureSideBox()` 와 같은 트리거) ·
  띠가 갈리는 순간(`hudBoxSync()` — 키가 **className 읽기**라 리플로우를 안 일으킨다).
- **`.bbtn`([포기하기]·[재도전])은 일부러 뺐다.** 그건 띠가 아니라 화면 한복판의 단추라, 넣으면 클램프가
  화면 절반까지 내려온다.

### 3-4. 자리를 **한 곳에서만** 정한다 (`eBarBox`)

그리기 블록과 `verify348` 하네스가 같은 식을 각각 적고 있었다. 앵커를 바꾸자 게이트 쪽 사본만 낡아
[3-b] 표본이 자리를 잃었다 — **368 이 잡았던 병이 그대로 재발했다.** ⇒ 자리를 `eBarBox(e)` 하나로 모으고
게이트는 **제품에게 묻는다**(368 처방). 그리기 블록은 4줄로 줄었다.

## 4. 결과 (수리 전 → 수리 후, 같은 자 `probe442`)

| 축 | 수리 전 | 수리 후 |
|---|---|---|
| 빈 배경(18표본) | **−107.5 ~ +131.8px** | **−3.7 ~ +3.7px** |
| 24px 넘게 떠 있는 표본 | 3 / 18 | **0 / 18** |
| 쉬는 자세 그림 안으로 파고든 표본 | **12 / 18** (최악 −107.5) | 4 / 18 (최악 **−3.7**) |
| 박스 불일치 배수 | 0.557 ~ 1.91 | **1.001 ~ 1.204** |
| 던전 HUD 파고듦(적 화면 16%) | **135.4px** | **0** (여유 10.5px) |
| 던전 HUD 파고듦(적 화면 24%) | 44.2px | **0** (여유 10.5px) |
| 바가 애니메이션을 따라 떠는 폭 | — | **0px**(40프레임) |
| 모션 갈아탈 때 튀는 양 | — | **0px** |

DOM 레이아웃은 한 픽셀도 안 움직였다(캔버스 오버레이만 바뀐다) · `e.y`·`e.r`·히트박스·깊이 정렬·AI 는
한 줄도 안 건드렸다 ⇒ **밸런스·판정 0줄.**

## 5. 게이트

- **`verify442` 47/47 신설**
  - `[1]` **자 자신의 검산** — `frameInk().ty` 를 «프레임을 따로 그려 읽은 값» 과 대조한다
    (제품과 **다른 코드 경로**다). zombie 15 · elves 33 · knight 92 · dragon 12 = **152장 전부 일치**.
  - `[2]` 표본 14종의 빈 배경이 규약 밴드(−6 ~ 24) 안. `[2-b]` 는 중앙값이 아니라 **지금 그려지는 그 한 장**
    으로 다시 묻는다(포즈가 다르면 값이 달라지므로 헛초록이 아니다). `[2-c]` 는 스침 값을 기록한다.
  - `[3]` 40프레임 애니메이션 · 공격 모션 전환에서 Δ0.
  - `[4]` 던전 HUD(하변 160) · 28 규격 보스 HUD(하변 114) 둘 다에서 바가 띠 아래.
  - `[5]` **폴백** — 아틀라스를 못 읽는 적은 옛 근사로 떨어지고 **바가 사라지지 않는다**.
  - `[6]` 348 규약(화면 밖 0 · 사이드 열 클램프)이 같은 한 줄 위에서 그대로.
  - `[R]` **되돌림 시험 2겹** — ① 앵커를 옛 식으로 되돌린 사본에서 gold 빈 배경이 **131.8px**
    (= 등재문 실측) 로 빨개진다 ② 세로 클램프만 뺀 사본에서 바 화면y 가 **−41.2** 로 띠를 파고든다.
- **이관 `verify348` 41/41** — 하네스 두 자리를 «상수» 에서 «제품에게 묻기» 로 갈았다(`band()`·`shotBand()`),
  `CLAMP`/`[6-c]` 의 소스 앵커를 `eBarBox` 안으로 옮겼다. **허용 오차는 한 칸도 안 넓혔다.**
- **회귀 무수정 12종** — `verify350` 37/37 · `verify338` 179/179 · `verify425` 61 · `verify331` 79 ·
  `verify255` 143 · `verify257` 110 · `verify46` 70/70 · `verify123` 62/62 · `verify208` 39/39 ·
  `verify172` PASS · `verify66` PASS · **SMOKE PASS**.

## 6. 다음 세션이 볼 것

- ⚑ **적 머리 위에 새 표시를 얹는 세션은 `eBarBox(e)` 를 기준으로 쌓아라.** 348 이 «`eOnScreen(e)` 를 같이
  태워라» 를 남긴 것과 같은 이유다 — 자리 식을 다시 적으면 442 가 통째로 되살아난다.
- ⚑ **`frameInk` 는 이제 세 값을 돌려준다**(`my`·`mx`·`ty`). «몸통이 어디냐» 는 `my`(425 카메라),
  «그림이 어디서 시작하나» 는 `ty`(442 오버헤드). 둘을 바꿔 쓰면 용 아틀라스에서 76.6px 어긋난다.
- ⚑ **캔버스 오버레이가 DOM 을 피해야 하면 `hudBox`(세로) · `sideBox`(가로) 를 쓰고 상수를 박지 마라.**
  둘 다 `layoutSide()` 가 다시 재고, `hudBox` 는 모드가 갈릴 때 `hudBoxSync()` 가 한 번 더 잰다.
- ⏸ **199 로 넘길 것 없음** — 밸런스 계수·판정·수치는 한 줄도 안 건드렸다.
