# 420 — 33 재화 정보 팝업(`#ciw`)이 89 유물 페이지의 주 CTA「유물 소환」을 문다

- **작업 단위** 420 (T4 폴리시 · 351 10회차가 낸 기등재분)
- **세션** sess-0659-28089 워커 D · **1회차** (2026-08-30)
- **제품** `index.html` **1자리**(CSS 선언 한 줄) · 레이아웃 **2280 Δ0px** · 판정·밸런스 **0줄**
- **게이트** `tools/verify420.js` **108/108 신설** · `tools/probe420.js` **신설**

---

## 1. 재현 먼저 (338 규칙) — **등재문의 실측이 낡아 있었다**

등재문(2026-08-29, 351 10회차 · 비평가 CQ·CR·CS 3인 독립 일치)이 적은 값은 이렇다:

> 버튼 높이 **214~220px** 중 **128 / 129 / 134px 가림(60~62%)** ·
> 「유물 소환」 라벨 밴드의 1600 환산 자리가 팝업 하단(**1169**) 아래라 가시 픽셀 **셋 다 0**

`tools/probe420.js` 를 신설해 **지금 트리**를 다시 재니 값이 이렇게 나왔다:

| 프레임 | 팝업 하변 | 버튼 상변 | 세로 겹침 | 라벨 잉크 덮임 |
|---|---|---|---|---|
| 2280 | 1509.5 | 1654 | — | 0% |
| 1924 | 1328 | 1331.9 | — | 0% |
| 1900 | 1319.5 | 1311 | **8.5** | 0% |
| **1800** | 1269 | 1224.3 | **44.7 ← 최악** | 0% |
| 1741 | 1210 | 1173.1 | 36.9 | 0% |
| 1700 | 1169 | 1134 | 35 | 0% |
| **1600** | 1069 | 1034 | **35** | **0%** |

**뿌리는 사이에 들어온 407 이다.** 등재문이 적은 «팝업 하단 1169» 는 클램프 **없을 때의** 값
(`369.5 + H/2` = 1169.5 @1600)이고, 407 이 `#ciw` 에 하단 여백 클램프를 얹으면서 팝업은
**1600 에서 100px 위로**(1169 → 1069) 이미 올라가 있었다. 그래서

- **«라벨 100% 가림» 은 지금 거짓이다** — 라벨 잉크는 y**1136..1176**(버튼 안 y97 자리)이라
  팝업 하변 1069 **아래**로 통째로 빠져 있다. 코스트 알약 `#rwCost`(y1183..1236)도 0% 다.
- 남은 결손은 **수반의 림 + 시안 수면**(SVG viewBox y4..56 = 화면 y1038..1090)이 위에서
  잘려 나가는 것 하나다. 버튼 상자 기준 **16.2%**(1600) / **20.7%**(1800).

⚑ **338·341 과 같은 자리다** — 등재문 수치를 그대로 게이트로 굳혔으면 **이미 참인 것**(라벨 0%)을
못 박고 실제로 남은 35px 는 «60~62% 가림» 이라는 틀린 기대 때문에 영영 초록으로 못 돌아왔을 것이다.

⚠ **비단조** — 겹침은 프레임이 낮을수록 커지지 않는다. 1801 위에서는 팝업 하변이 `H/2` 로 느리게
내려오고 버튼은 `H − f(H)` 로 빠르게 내려와 겹침이 **커지다가**, 1801 아래에서는 팝업도 하단 앵커가
되어 **35 로 굳는다**. 그래서 최악은 1600 이 아니라 **1800(44.7px)** 이다.

## 2. 처방 — 407 과 **같은 산식·같은 여백**, 다른 것은 f 하나

팝업 하변 `= H − X/2 + 369.5` · 피해자 상변 `= H − f` ⇒ 여백 30 을 얹으면 **X = 2f + 799**

| | 피해자 | f (하단에서의 거리) | X |
|---|---|---|---|
| 407 | 미션 배너 `#tuto` | 501 (`bottom:171` + 탭바 180 + h150) | 1801 |
| **420** | 유물 CTA `#rwBasin` | **566**(실측) | **1931** |

```css
#relw.on ~ #ciw{padding-bottom:calc(234px + max(0px, 1931px - var(--frameh, 2280px)))}
```

- **여백 30 은 새 상수가 아니다** — 56 절전 `.sv-hint` → 407 이 쓴 값 그대로다. **새 상수 0개.**
- **f = 566 은 «바닥 구간» 값이다.** `#relw` 패널은 남는 세로를 구획 사이에 나눠 주므로 버튼이
  프레임보다 **느리게** 내려온다: f 는 1600·1700 에서 **566** → 1800 575.7 → 1924 592.1 → 2280 626.
  긴 쪽에서 버튼이 팝업보다 빨리 멀어지므로 **짧은 쪽 값으로 잡은 1931 이 전 구간을 덮는다.**

### 수리 후 (전수 13프레임, 겹침 0건)

| 프레임 | 1600 | 1650 | 1700 | 1741 | 1800 | 1850 | 1900 | 1924 | **1931** | 1950 | 2000 | 2100 | 2280 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 여백 | **30.0** | **30.0** | **30.0** | 28.2 | 36.3 | 13.7 | 7.1 | 11.3 | **2.9** | 9.9 | 28.3 | 81 | 139.6 |

- **1600·1650·1700 은 정확히 30.0** — 407 과 같은 값으로 굳는다.
- 최솟값 **2.9px** 는 교차점 1931 부근인데, 그 자리는 «클램프가 막 붙는» 지점이라 **원래도 겹침이 0**
  이던 곳이다. 층 없이 이어진다(1929→1933 하변 단조 증가 · 한 칸 걸음 < 1.2px).
- **9:19(2280) Δ0px** — `max()` 가 0 을 골라 여백 234px 그대로, 팝업 696.5..1509.5 불변.

### ⚑ 전역으로 1801 을 올리지 않고 `#relw` 스코프로 가른 이유

전역이면 **메인 화면(골드·다이아)에서 배너 여백이 30 → 95 로 벌어져** 407 이 확정한 «하단 앵커» 가
무너진다(`verify407` §3 이 그 값을 못 박는다). 피해자가 있는 화면에서만 그 피해자에 맞춘다 —
`#modal.sk8`·`#modal.at70` 이 이미 같은 꼴의 선례다. `verify420` **§4** 가 메인 화면 6표본에서
407 의 자리(`H − 531`)가 **한 칸도 안 움직였음**을 못 박는다.

⚠ **규칙은 반드시 `#ciw{…}` 뒤에 둔다** — `verify407` §0 이 `/#ciw\{[^}]*\}/` 로 **첫** 규칙을 읽어
클램프 문자열을 확인한다. 앞에 두면 **남의 게이트가 내 선언을 읽는다.** `verify420` §0 이 그 순서 자체를 단언한다.

## 3. 처방이 «위쪽에 새 피해자» 를 만들지 않았나 — 음성 대조로 확인

팝업이 65px 올라가므로 위쪽을 같이 봤다(`probe420` **[E]** — 1600 에서 `#relw` 자손 전수 덮임 %).

| 부품 | 되돌림 → 지금 | |
|---|---|---|
| `.rw-lintel`(상인방 **장식**, `pointer-events:none`) | 0% → **2.5%** | ↑ 3px 슬라이버 |
| `#rwBasin` · `.rw-stone` · `.rw-mid` (**주 CTA**) | 16.2% → **0%** | ↓ 회수 |
| `.rw-floor` / `.rw-ground` (바닥 **장식**) | 13.7 / 9.7% → 5.9 / 1.3% | ↓ 회수 |
| `#rwGrid`·`.rw-c`·`.rw-frame`·`.rw-panel` | **불변**(55.4 / 83.4 / 34.5 / 34.3%) | = |

새로 덮인 것은 **장식 그라디언트 띠의 3px** 하나뿐이고, 조작·정보를 가진 자리
(`.pcb` 재화 바 · 429 의 `[?]` 도움말 · 격자 슬롯)는 **한 곳도 새로 안 덮였다**.
격자가 «불변» 인 이유는 두 판 모두 팝업 세로 대역 **안**에 통째로 들어 있어 가로 지분(598/1080 = 55.4%)만
남기 때문이다 — 중앙 다이얼로그의 구조적 지분이지 이 처방이 만든 것이 아니다.

## 4. 게이트

`node tools/verify420.js` → **108/108 PASS**

- **§0 전제** — 스코프 규칙이 있고 · **`#ciw{…}` 뒤**에 있고(407 정규식 보호) · 사본 없음 ·
  407 기본 클램프 무수정 · `#relw` 가 `#ciw` 보다 앞선 형제(`~` 결합자의 전제)
- **§1 겹침 0** — 13프레임 전수에서 진입 서명(`#ciw@on` **와** `#relw@on`) · 겹침 0 · 버튼 부품 덮임 0%
- **§2 여백** — 바닥 구간 **정확히 30.0** · 전 구간 음수 없음 · 최솟값 ≥ 2
- **§3 연속·앵커** — 1931 에서 여백 234px · 1929~1933 단조·무층 · **2280 Δ0** · 상자 813 불변 · 상변 191 > 126
- **§4 스코프** — 메인 화면 골드·다이아 × 1600/1700/1800 에서 407 의 `H − 531` 이 **한 칸도 안 움직인다**
- **§R 되돌림 시험** — 스코프 클램프를 407 값으로 되돌린 사본에서 **겹침 35(1600)·44.7(1800)** 과
  `.rw-stone` 덮임 **16.2%** 가 되살아난다. ⚑ **무르게 푼 수리가 아님을 한 항이 더 못박는다** —
  음성 판에서도 **라벨은 0%** 다(「라벨 100% 가림」은 407 이전에만 참이었다).
- **§H** 콘솔·페이지 에러 0건

⚠ **1회차에 자가 흔들렸다** — 60 쥬시 개봉 연출이 도는 중에 재면 상자가 813 을 **넘겨** 잡혀
2280 하변이 1509.5 대신 1514.4 로, 교차점 걸음이 «1317 → 1333.9 → 1339.9 → **1335.5**» 로 단조도
아니었다. `probe420` 이 쓰는 정착기(`probe351lib.settle`)를 **자도 그대로 쓰게** 해서 닫았다 —
두 자가 «다른 순간» 을 재면 숫자를 대조할 수 없다(385 «자매 자 드리프트»).

### 회귀 (전부 무수정)

`verify407` **91/91** · `verify429` 50/50 · `verify330` 20/20 · `verify125` 38/38 · `verify166` 73/73 ·
`verify299` 27/27 · `verify120` 157/157 · `verify119` 38/38 · `verify351` 86/86 · `verify400` 156/156 ·
`verify414` 31/31 · `verify415` 115/115 · **SMOKE PASS**

## 5. 곁다리 등재 — **439** (⚠ 438 로 등재하려다 같은 순간 다른 워커에게 번호를 비켜 줬다 — 340·412·418·437 선례)

등재문이 «자를 먼저 세울 것» 이라며 지목한 자리를 직접 읽어 확인했다: `probe351` **D7** 의 `navs`
목록은 지금 **`#tabbar` · `.pedge` · `#tuto` 셋뿐**(`tools/probe351.js` 213~227행)이고
**«페이지가 소유한 주 행동 버튼» 은 후보에 아예 없다.** 407 이 `#tuto` 를 넣어 배너를 잡았듯,
`#rwBasin` 계열을 넣으면 390·400·407·414·420 계열을 한 번에 훑을 수 있다.

**이 세션이 D7 을 안 건드린 이유**: `probe351.js` 는 지금 **351 lock(sess-0651-25855)이 잡고 도는
파일**이고, 같은 함수의 다른 축이 **424·435·436** 으로 이미 등재돼 있다(각각 406 규약 · 자기무효
셀프테스트 · 여유 0.0px 축). 셋 중 어느 쪽이 먼저 손대든 같은 블록을 다시 쓰게 되므로
**한 워커가 D7 을 한 번에 손보는 편이 옳다** — ROUTINE [1] «작업 단위 1개» 규칙 그대로.

---

## 기능 체크 (T4 라 레이아웃만 — 실동작 회귀 확인용)

| 확인 | 결과 |
|---|---|
| 89 유물 페이지 진입 → `.pcb` 「유물조각」 알약 클릭 | 33 팝업이 뜬다(`#ciw.on` + `#relw.on` 둘 다) |
| 1600 에서 「유물 소환」 버튼 | 림·수면·라벨·코스트 알약 **전부 안 가림**(덮임 0%) |
| 팝업 딤 클릭 | 닫힘 정상(`closeCurInfo`) · 콘솔 0 |
| 2280 | 팝업 좌표 696.5..1509.5 — 수리 전과 **한 칸도 안 다름** |
| 메인 화면 골드·다이아 팝업 | 407 자리 그대로(`H − 531`) |
