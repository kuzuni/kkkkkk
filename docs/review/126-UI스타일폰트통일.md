# 126 — UI 스타일·폰트 통일 (디자인 토큰 1벌 + 게임용 한글 웹폰트 1종)

> 저장소 주인 지시(2026-08-26): **UI 스타일도 통일감 있게, 폰트도 통일감 있게.**
> 추가 지시: **그림체도 레퍼런스 이미지들 느낌으로.**
> 규모가 커서 지시서가 회차를 **①② 폰트 → ③④ 토큰 → ⑤⑥ 정리** 3단계로 나눠 놓았다.
> 각 단계 끝에 커밋 + 게이트.

**진행 현황 — 다음 세션은 여기부터 본다**

| 단계 | 내용 | 상태 |
|---|---|---|
| ① 폰트 동봉 | 후보 비교 → 1종 확정 → `@font-face` 1벌 · `font-family` 1곳 | **완료 (1회차)** |
| ② scaleX·stroke 재검토 | `scaleX` 288곳 전수 재검토 · `-webkit-text-stroke` 4단 토큰화 | **②-1 스트로크 토큰 = 반영 완료(4회차, §6)** · ②-2 scaleX 재보정 = 실측만 끝남(§2) → 다음 세션은 여기부터 |
| ③ 디자인 토큰 | `:root` 1벌(색·모서리 4단·테두리·그림자·텍스트 6종) · `font-size` 8단 스냅 | 미착수 |
| ④ 공용 부품 | 버튼 3종·알약·탭·카드 프레임·팝업 껍데기 클래스 1벌 | 미착수 |
| ⑤ 측정표 정합 | bbox ±3px 초과 자리는 «측정표 값 우선» 인라인 px | 미착수 |
| ⑥ 문서 | `docs/UI-TOKENS.md` 신설 · LESSONS · «결정 사항» | 미착수 |
| ⑦ 스타일 보드 | `docs/review/126-스타일보드.png` — ref 8~10장에서 껍데기·버튼·프레임·텍스트 샘플 | 미착수 |

---

## §1. ① 서체 확정 — «Jua» (1회차, 2026-08-26, sess-2349-9898 워커 D)

### 후보와 비교 방법

지시서가 지정한 OFL 후보 4종을 **같은 조건으로 나란히 렌더**해 비교했다
(`tools/cap126font.js` → `docs/review/126-font-candidates.png`).
블록마다 같은 문자열 6줄, 같은 `font-size`, 같은 흰 글자 + 검정 스트로크:
HUD 숫자 44px · 팝업 타이틀 56px · 카드 이름 34px · 본문 30px · 버튼 40px · 캡션 24px.
맨 위 블록은 **현재 시스템 폰트**(Malgun Gothic 계열)라 «무엇이 좋아지는지» 가 같은 그림에 들어온다.

기계 실측(`tools/cap126font.js` 가 같이 찍는다. 타이틀 56px «보물상자»):

| 후보 | advance(4자) | 잉크 높이 | 자폭:자고(글자당) |
|---|---|---|---|
| 현재 Malgun Gothic | 224.0 | 52 | 1.08 |
| Black Han Sans | 187.8 | 43 | **1.09** |
| Do Hyeon | 172.0 | 44 | 0.98 |
| **Jua** | 184.9 | 47 | **0.98** |
| Gothic A1 Black | 228.4 | 55 | 1.04 |

### 비평가 2인 독립 채점

축 4개(①자폭/자고 비율 ②획 굵기 ③글자 형태 ④숫자·영문 조화), 각 10점, **최저 항목 = 그 후보의 점수**.
색·배경·아트 내용은 감점 사유에서 제외(글자 모양·굵기·비율만).

| 후보 | 비평가 E | 비평가 F | 두 채점의 최저 |
|---|---|---|---|
| Black Han Sans | 4 (①5 ②5 ③4 ④5) | 6 (①8 ②9 ③6 ④7) | 4 |
| Do Hyeon | 3 (①7 ②3 ③6 ④6) | 3 (①9 ②4 ③5 ④3) | 3 |
| **Jua** | **7** (①7 ②7 ③8 ④7) | 5 (①9 ②5 ③8 ④8) | **5** |
| Gothic A1 Black | 4 (①6 ②5 ③4 ④5) | 4 (①5 ②7 ③4 ④6) | 4 |

**«추천» 은 갈렸다** — E 는 Jua, F 는 Black Han Sans. 그런데 두 비평가의 **실측은 일치**했고
(Jua 획 15%, BHS 획 22%, 자폭비 Jua ≈1.0 / BHS ≈1.05~1.09), 갈린 건 **레퍼런스의 획 굵기 목표치**
뿐이다(E 18% · F 20%). 지시서 [3]-(나) 의 «2명 중 최저 점수» 규칙을 그대로 적용하면
**Jua 5 > BHS 4** 라 Jua 로 확정했다.

확정 근거를 규칙 밖에서도 한 번 더 확인했다:

- **자폭:자고 0.98** — 4후보 중 레퍼런스(E 실측 0.87 · F 실측 ≈1.0)에 가장 가깝다. BHS 는
  «압축 서체» 라는 인상과 달리 **잉크 기준으로는 시스템 폰트만큼 넓다**(1.09 vs 1.08) —
  advance 가 좁을 뿐 글자가 작게 그려져서다. 이 게임이 필요로 한 «좁고 높은» 은 BHS 가 아니다.
- **획이 얇은 것(15%)은 이 게임에서 약점이 아니다.** 전 화면이 글자마다 `-webkit-text-stroke`
  4~10px 를 이미 얹는다(223곳). 스트로크가 굵기를 만들어 주므로 서체는 속공간이 살아 있어야 하고,
  반대로 획 22% 인 BHS 에 스트로크를 얹으면 **속공간이 막혀 4글자가 한 덩어리로 뭉갠다**(E ③4).
- 종성·모음의 둥근 마감이 레퍼런스의 «두껍고 통통한» 카툰 UI 계열과 같다(E ③8 · F ③8, 두 비평가 일치).

### 넣은 것

- `assets/fonts/Jua-subset.woff2` **180KB** (상한 400KB) — ASCII + KS X 1001 한글 2,350자 + UI 기호.
  서브셋 재현 명령·라이선스는 `assets/CREDITS.md`, 원문은 `assets/fonts/OFL.txt`(SIL OFL 1.1).
- `index.html` `<style>` 맨 앞 `@font-face{font-family:'GameKR'}` **1개**,
  `html,body{font-family:'GameKR',…}` **1곳**. 나머지 15곳은 전부 `font-family:inherit` 라 손댈 데가 없었다.
- **CDN 금지·`file://` 유지** 지시대로 상대경로 1개. Chromium 에서 `file://` 로 열어
  `document.fonts` 가 `GameKR:loaded` 를 보고하는 것까지 확인했다(폰트가 막히는 브라우저에서는
  `font-display:swap` + 기존 폴백 스택으로 오늘과 같은 모습으로 떨어진다).

### 신설한 도구

| 도구 | 하는 일 |
|---|---|
| `tools/cap126font.js` | 후보 비교 캡처 + 후보별 advance·잉크 높이 실측 |
| `tools/scan126.js` | 9화면 **1,728요소**의 bbox 회귀 스캐너 (`--diff a.json b.json [허용px]`) |
| `tools/m126sx.js` | `scaleX≠1` 인 텍스트 자리를 모아 «압축을 꺼도 안 넘치는가» 실측 |

`scan126.js` 는 같은 빌드를 두 번 떠서 **잡음 바닥을 먼저 쟀다** — 2,013요소 중 34개가
상시 애니메이션(스킬 쿨다운 `.cdv` · 진행바 `#prF` · 유물 격자 아이들 · 썸네일 들썩)이라
그 키를 `ANIM` 으로 빼면 **1,728 중 6건(±3px)** 이 바닥이다. 이 바닥 위에서만 회귀를 읽는다.

### 서체 교체가 만든 이동

`scan126.js --diff` (±3px): **유지 1,461 · 이동 267 · 사라짐 0.**
이동분은 **전부 auto 폭 텍스트 잉크 상자**(`i`/`b`/`em`/`u` 같은 인라인 칸)다 —
고정 px 로 잡아 둔 레이아웃 상자는 하나도 안 움직였다. 서체를 바꾸면 글자 폭이 바뀌므로
**이건 회귀가 아니라 ② 가 처리할 목록**이다.

- `node tools/smoke.js` → **SMOKE PASS** (자동 플레이 · 팝업 전부 열기 · NaN/undefined 0 ·
  화면비 6종 프레임/시트/절전 잘림 0). 즉 **넘침·잘림은 한 곳도 생기지 않았다.**

---

## §2. ② `scaleX` 288곳 전수 재검토 — 실측 (2회차)

`tools/m126sx.js` 로 9화면을 돌아 «계산된 transform 에 scaleX≠1 이 있고 한글/숫자 텍스트를 가진 칸»
을 전부 모았다: **355건 = 67종**(같은 CSS 규칙이 화면마다 반복되므로 종으로 묶는다).

`need` = (압축을 껐을 때의 자연 폭) / (담을 상자 폭). `need ≤ 1` 이면 **압축이 없어도 안 넘친다.**

| 구분 | 종수 |
|---|---|
| 압축 없이도 안 넘침 (삭제 후보) | **55** |
| 압축이 실제로 필요 | **12** |

압축이 실제로 필요한 12종 (need 큰 순):

| need | 현재 sx | 자리 | 예시 글자 | 자연폭/상자 |
|---|---|---|---|---|
| 1.90 | .89~1.006 | `52-메뉴 .mn-b>.mn-l` (7칸 전부) | 우편·랭킹·길라잡이·가방·절전·설정·패스 | 190/100 |
| 1.71 | .5 | `02 #tutoName>em>u` · `#tutoPg>em>u` | 숫자 1 | 12/7 |
| 1.56 | 1.1 | `14 #rwGrid .rw-c>u` | Lv.0 | 223/143 |
| 1.42 | 1.18 | `22 .qs-i>.ifq` | 100 | 136/96 |
| 1.38 | .5 | `02 #tutoBtn>em>u` | 숫자 1 | 18/13 |

> 주의 — `need ≤ 1` 은 «상자를 안 넘친다» 는 뜻이지 «측정표 폭과 같다» 는 뜻이 아니다.
> 55종을 한 번에 지우면 글자 잉크 폭이 레퍼런스 측정치에서 벗어날 수 있다. **삭제는
> «측정표에 잉크 폭이 적힌 자리» 와 «안 적힌 자리» 를 갈라서** 진행한다 —
> 적힌 자리는 새 서체의 자연 폭으로 `sx = 목표폭 / 자연폭` 을 다시 계산해 토큰
> (`--sx-tight` / `--sx-wide`)으로만 남기고, 안 적힌 자리는 지운다. **반영은 다음 세션 몫이다 — §5 참고.**

---

## §3. 적용 결과 비평 (3회차) — 비평가 G · H

캡처 5장(`docs/review/126-s02-메인.png` · `-s23-훈련` · `-s10-상점` · `-s22-퀘스트` · `-s19-프로필`,
`tools/cap126.js`)과 레퍼런스를 같이 읽히고 축 3개로 채점했다:
①넘침·잘림 ②글자 폭·크기 비율 ③레퍼런스 그림체와의 거리. **최저 항목이 그 비평가의 점수.**

| | ① 넘침·잘림 | ② 폭·크기 비율 | ③ 그림체 | 최저 | 유지? |
|---|---|---|---|---|---|
| G | 3 | 6 | 8 | 3 | 아니오 |
| H | **10** | 6 | 5 | 5 | **예** |

**G 의 ①3 은 서체 회귀가 아니다.** 근거는 «19 프로필 잠금 칭호 7칸에서 자물쇠가 이름
가운데 1~2자를 덮는다» 인데, CSS 를 보면 `.pf-lk{left:50%;margin-left:-14px}` 의 중심이
카드 350 기준 **175**, `.pf-bn`(left 26 · width 298) 안에서 가운데 정렬된 이름의 중심도
26+149 = **175** 로 **글자 폭과 무관하게 항상 같은 점**이다. 즉 서체를 바꾸기 전에도 겹쳐 있었다.
같은 화면을 본 H 는 «레퍼런스와 크기·폭 거의 1:1, 넘침·잘림 없음» 으로 채점했다.
→ 기존 결함으로 **작업 137** 에 등재(자물쇠 앵커만 손보면 된다).

두 비평가가 **공통으로 남긴 숙제는 정확히 ② 다**:

1. (H 1순위) **작은 라벨(24~30px)에서 고정 px 스트로크가 «ㅇ» 속공간을 막는다** — 하단 탭 캡션 등.
   스트로크가 `-webkit-text-stroke:6~9px` 고정이라 글자가 작을수록 잉크를 잡아먹는다.
   → ② 의 **역할별 4단 토큰**(`--stroke-hud 9 · --stroke-title 7 · --stroke-body 5 · --stroke-small 4`)이
   이 자리를 위한 것이다. 지금 스트로크는 **50종**(6px 31곳 · 5px 31 · 8px 26 · 4px 21 · 7px 18 · 10px 16 …).
2. (G 2·3순위) **타이틀류 자폭이 레퍼런스보다 10~19% 좁다** — 「무기 상자」 146 vs 181(−19%),
   「10회 소환」 93 vs 111(−16%), 「훈련」 리본 78 vs 87(−10%, 높이는 +7%).
   → ② 의 scaleX 재보정(§2)이 처리할 자리. **지금 scaleX 값은 140종**이라 이대로는 못 맞춘다.
3. (H 3순위) 리스트 본문이 레퍼런스보다 12~16% 작다 → ③ 의 `font-size` 8단 스냅에서 같이 본다.

**③ 그림체 점수가 G 8 · H 5 로 갈린 것**은 «흰 글자 + 검정 외곽선» 을 어느 크기에서 봤느냐 차이다 —
H 는 작은 라벨(막힘)을, G 는 중·대 글자(막힘 없음)를 근거로 들었고 서로 모순되지 않는다.
**두 사람 다 «되돌리지 말고 고쳐라» 로 결론냈다.**

---

## §4. 게이트 · 남긴 문제

**전 게이트 70종을 돌리고, FAIL 24건을 부모 커밋 워크트리에서 같은 방식으로 다시 돌려
«내가 만든 것 / 원래 깨져 있던 것» 을 갈랐다.** 내 것은 3건뿐이었고 전부 회수했다.

| 게이트 | 부모 커밋 | 서체 교체 직후 | 회수 후 | 원인 · 고친 것 |
|---|---|---|---|---|
| verify116 | 34/34 PASS | 33/34 | **34/34 PASS** | C4 «다이아 5칸 라벨 폭 편차 ≤ 6px» 이 0.6 → 14.6px. 구 서체는 이 숫자열 5개의 자연 폭이 전부 131.7px 로 같아 `qx` 가 한 값(0.90)이면 됐지만 Jua 는 «1» 이 좁아 122.6~135.3px 로 갈라진다. 목표 폭을 교체 전과 같은 **118.5px** 로 두고 칸별 `qx = 118.5/자연폭` 재계산 → 편차 **0.1px** |
| verify84 | 30/30 PASS | 26/30 | **30/30 PASS** | 12 «터치하여 닫기» **잉크**가 측정표 자리(2064~2104)에서 5px 떠올랐다 — 같은 `line-height` 안에서 새 서체의 베이스라인이 5px 높다. 지시서 ⑤ 대로 `.sm-close` 앵커 **175 → 170**(짧은 프레임 보호항 1480 → 1475 동반). 게이트가 «175px» 를 리터럴로 물고 있어 같이 갱신 — 이 항목이 지키려는 건 숫자가 아니라 위 4항목이 재는 잉크 자리다 |
| verify69 | 99/99 PASS | 98/99 | **99/99 PASS** | 우편 안내 1줄 폭 .904 PW → .773(하한 .80). 상자(862×50, 25px 줄 2개)는 그대로 두고 글자만 **22 → 25px** → .879 PW |

- **폼 컨트롤 상속 누락 4칸**(비평가 H 가 «일부만 구서체» 라고 지적 → 기계로 전 화면 훑어 확정):
  UA 가 `button`/`input` 의 서체를 쥐고 있어 body 상속이 끊긴다. 32 튜토 배너 «미션·1·받기» 와
  12 `.sm-fat «10회 소환»` 이 Arial 로 남아 있었다. 리셋에 `button,input,select,textarea{font-family:inherit}`
  한 줄 → 3화면 재스캔 **비-GameKR 텍스트 요소 0**.
- `node tools/smoke.js` → **SMOKE PASS**(수정 후 재실행 포함 2회).
- 나머지 21건은 **부모 커밋에서도 똑같이 FAIL** 이라 손대지 않았다(지시서 [6] «남의 구간»):
  102 · 32 · 46(2~3/66, 123 이 던전 카드 구성을 바꾸는 중이라 [1]에서 연쇄 중단) · 47 · 48 · 49 ·
  54 · 59 · 61(실행 즉시 TypeError) · 71 · 74 · 76 · 81 · 82 · 83 · 94 · 114 · 117 · 121 · A2.
  **verify64 는 오히려 39/41 → 40/41 로 좋아졌다.**
- `tools/verify102.js` 는 **1회차 이전부터 이미 FAIL(77/80)** 이다 — 내 서체 교체 때문이 아니다.
  같은 3항목(`10 기하 불변 · 10회/30회/무료 버튼`)이 부모 커밋(`HEAD~1`)의 워크트리에서도
  똑같이 깨진다(`526,425,208,127` vs `526,425.1,208.4,127.2`). 원인은 **작업 122 가 붙인
  `.shp-card .cbtn.b1:not(.lack){animation:jz122Ring…}`** 로 보인다 — 링 펄스가 `.lack` 아닌
  버튼에만 걸려서 «부족 상태 vs 충분 상태의 rect 가 같아야 한다» 는 102 의 불변식을
  **애니메이션 위상만큼** 깬다. 지시서 [6] 대로 **남의 구간이라 손대지 않았다.**
  → PROGRESS 에 별개 작업으로 등재.

---

## §5. 다음 세션에게 (② 부터 이어서 — 재측정 금지)

1. **§2 의 실측표를 다시 재지 마라.** `tools/m126sx.js` 를 한 번 돌리면 같은 표가 나오지만,
   회차 예산을 거기 쓰지 말고 **반영**부터 한다.
2. **② 는 두 갈래다.**
   - **스트로크 4단 토큰** — `:root` 에 `--stroke-hud 9 · --stroke-title 7 · --stroke-body 5 · --stroke-small 4`
     를 두고 현재 **50종 223곳**을 역할별로 흡수한다. 비평가 H 의 1순위(작은 라벨 속공간 막힘)가
     여기서 풀린다. **작은 글자일수록 스트로크를 얇게** 가는 방향이 맞다.
   - **scaleX 재보정** — 삭제 후보 55종을 그냥 지우면 잉크 폭이 측정표에서 벗어난다.
     `docs/measure/<화면>.md` 에 **잉크 폭이 적힌 자리**는 `sx = 목표폭 / 새 서체 자연폭` 으로 다시 계산해
     `--sx-tight` / `--sx-wide` 토큰으로만 남기고, 안 적힌 자리는 지운다.
3. **회귀는 `tools/scan126.js` 로 본다.** `node tools/scan126.js a.json` → 고치고 → `b.json` →
   `node tools/scan126.js --diff a.json b.json 3`. 잡음 바닥은 6건이니 그 위로 뜨는 것만 보면 된다.
4. **게이트는 70종을 다 돌리지 말고**(순차로 1시간 넘는다) `xargs -P 3` 으로 돌린 뒤,
   FAIL 이 나오면 **부모 커밋 워크트리에서 같은 게이트를 다시 돌려** 내 것인지부터 가른다.
   이 세션은 그렇게 해서 FAIL 24건 중 21건이 남의 것임을 확인했다(§4).
5. **③④ 는 ② 가 끝난 뒤**에 잡는다 — `font-size` 107종을 8단으로 스냅하는 순간 bbox 가
   전 화면에서 움직이므로, 그때 지시서 ⑤(«±3px 넘으면 측정표 값 우선»)를 화면마다 적용해야 한다.
   ② 를 건너뛰고 ③ 을 먼저 하면 어느 이동이 스냅 탓이고 어느 것이 스트로크 탓인지 못 가른다.
6. **⑦ 스타일 보드**(`docs/review/126-스타일보드.png`)는 아직 안 만들었다. ③ 의 색·형태 토큰 값을
   정하기 **전에** 만들어야 한다 — 보드 없이 토큰을 정하면 «임의의 값» 이 되어 주인 지시(⑦)를 어긴다.

---

## §6. ②-1 스트로크 4단 토큰 — 반영 (4회차, 2026-08-26, sess-0113-24608 워커 A)

3회차가 남긴 두 숙제 중 **H 의 1순위**(«작은 라벨에서 고정 px 스트로크가 ㅇ 속공간을 막는다»)를
기계로 가르고 고쳤다. G 의 2순위(타이틀 자폭 −10~19%)는 ②-2 라 이번 회차에는 손대지 않았다.

### 6-1. 원인 — 비율이 «정확히 거꾸로» 서 있었다

신설 `tools/m126st.js` 로 9화면의 «스트로크가 걸린 글자 칸» 386곳(94종)을 모아
`r = 스트로크 ÷ font-size` 를 냈다. 스트로크는 고정 px 인데 글자 크기는 18.7~64px 로 흩어져 있으니
**같은 6px 이 64px 글자에서는 9%, 24px 글자에서는 25%** 를 먹는다. 결과:

| 글자 크기대 | 종/곳 | r 중앙값 (전) | r 중앙값 (후) | 최대 r (전 → 후) |
|---|---|---|---|---|
| 작은 <25px | 13종 56곳 | **0.217** | **0.130** | 0.374 → 0.217 |
| 본문 25~33 | 31종 181곳 | 0.192 | 0.154 | 0.276 → 0.194 |
| 타이틀 33~45 | 36종 118곳 | 0.145 | 0.145 | 0.263 → 0.205 |
| 대형 45+ | 13종 31곳 | 0.162 | 0.162 | 0.208 → 0.208 |

즉 **가장 작은 글자가 가장 두꺼운 외곽선을 쓰고 있었다.** 눈으로는 «서체 탓» 으로 보이지만
서체와 무관한 기계적 결과다 — 고정 px 를 흩어진 크기에 얹으면 반드시 이렇게 된다.

### 6-2. «막힘» 은 절대 px 가 아니라 비율로 결정된다 (근거)

386곳 중 **385곳이 `paint-order:stroke fill`** 이다(`tools/m126st.js` 가 같이 잰다. 예외는
`#nickN` 1종 9곳, r .026 이라 무해). 그래서 스트로크의 안쪽 절반은 흰 채움이 덮는다 — 여기까지는
기존 주석(`#tuto.todo` 절)이 이미 적어 둔 대로다. **그런데 속공간(카운터)은 채움이 아니라 «구멍»
이라 덮이지 않는다.** 카운터를 두른 안쪽 윤곽에도 스트로크가 그려지고, 그쪽은 구멍 안으로 자란다.
결과적으로 **카운터 지름이 사방에서 sw/2 씩, 즉 총 sw 만큼 줄어든다.**

신설 `tools/m126ct.js` 로 GameKR(Jua)을 실측했다(«우편·보상·소환·길라잡이·아무거나» 5단어,
`paint-order:stroke fill` 과 같은 순서로 캔버스에 그려 남은 카운터 면적을 센다):

| r | .08 | .12 | .16 | .18 | .20 | .22 |
|---|---|---|---|---|---|---|
| 살아남은 카운터 면적 (스트로크 0 대비) | ~80% | ~41% | ~23% | ~15% | ~12% | ~10% |

**fs 22~56 에서 값이 거의 같다** — 막힘은 글자 크기가 아니라 오직 r 의 함수다. 이것이
«토큰을 px 가 아니라 비율로 두어야 하는» 이유다.

이 모형은 캔버스 에뮬레이션이라 그대로 믿지 않고 **제품의 계산된 스타일로 격리 렌더해 교차
확인**했다(6-9). fs 24 «우편» 에서 스트로크 0 → 42.6px² · 옛 7px → **2.1px²(5%)** · 새 3.12px →
8.8px²(21%). 위 표의 r .29 ≈ 10% · r .13 ≈ 35% 와 같은 방향·같은 자릿수다.

### 6-3. 넣은 것 — `:root` 4단 **비율** 토큰

```css
:root{ --st-hud:.17; --st-title:.15; --st-body:.15; --st-small:.13; }
```

각 자리는 `-webkit-text-stroke: calc(var(--st-*) * 1em)` 로 쓴다. `em` 은 그 칸의 font-size 로
풀리므로 **글자 크기가 바뀌면 외곽선이 저절로 따라간다.** ③ 의 `font-size` 8단 스냅이 들어와도
스트로크를 다시 손볼 필요가 없다.

값의 근거는 «레퍼런스 바깥 두께»(= 스트로크 값의 절반. 본문 2~3px · 수량 4.5px — 3회차 G·H 독립
실측)와 «카운터 생존»(6-2) 이 만나는 지점이다. 본문 fs 26 에서 `.15em` = 3.9px → 바깥 1.95px 로
레퍼런스 하한과 맞고, 카운터는 ~25% 가 살아남는다.

### 6-4. 흡수한 자리 — r ≥ .22 였던 **20종 53곳** (→ 0종 0곳)

| 자리 | 화면 | 전 (fs / sw / r) | 후 (r) | 토큰 |
|---|---|---|---|---|
| `#mnw .mn-l` (7칸) | 52 | 24 또는 **18.7** / 7 / .292·**.374** | .130 | `--st-small` |
| `.qs-t` + `em` | 22 | 29 / 8 / .276 | .150 | `--st-body` |
| `.mhead h2` (`#mtitle`) | 공용 모달 | 38 / 10 / .263 | .150 | `--st-title` |
| `.shp-card .b1>.sub` | 10 | 23 / 6 / .261 | .130 | `--st-small` |
| `.qs-b b` | 22 | 30 / 7 / .233 | .150 | `--st-body` |
| `.pcb-p>b` | 03·10·14 | 34.5 / 8 / .232 | .150 | `--st-title` |
| `.shp-card .cbtn>u` → `.lab`·`.cost`·`.sub` | 10 | 29·26·23 / 6 / .207·.231·.261 | .150 | `--st-body` |
| `.ifr>.ifq` (수량 배지) | 05·12·22·53·69·70 | 24.9 / 5.72 / .230 | .150 | `--st-body` |
| `#tuto.todo .tsub` | 02·32·61 | 36 / 8 / .222 | .170 | `--st-hud` |
| `.shp-card .cbtn.lack>.cost` | 10 | 26 / 5.5 / .212 | .150 | `--st-body` |

`.cbtn>u` 는 한 규칙이 세 자식(`.lab` 29px · `.cost` 26px · `.sub` 23px)에 상속되는데
**고정 6px 이라 자식마다 r 이 .207~.261 로 갈라져 있었다** — `em` 기준이 되면서 셋이 같은 비율로 붙었다.
이런 «한 규칙, 여러 글자 크기» 자리가 토큰화의 가장 큰 수확이다.

### 6-5. 남긴 한계 (되돌리지 말 것 — 근거 있음)

- **«레퍼런스만큼 두껍게 두르면 반드시 막힌다» 는 것은 사실이 아니다** — 6-8 을 쓰기 전까지 그렇게
  적어 두었으나, 레퍼런스 35곳을 직접 재 보니 레퍼런스 자체가 **작은 글자에서 외곽선을 얇게**
  쓴다(환산 r .131). 우리 `--st-small`(.13)이 그 값과 사실상 같으므로 이 자리는 «회수 불가» 가
  아니라 **이미 회수된 것**이다. 남는 것은 `#tuto.todo .tsub` 한 자리뿐이고, 그것도 «레퍼런스가
  정말 4.5px 인가» 가 두 측정 사이에서 갈린다(6-8 각주). 서체 교체가 필요한 자리로 분류하지 마라.
- **r .17~.22 구간 19종 114곳은 이번에 안 건드렸다.** 6-2 기준으로 «막힘 확실» 이 아니고
  (카운터 15~23% 생존), 대부분 32·61 튜토 배너처럼 **레퍼런스 실측으로 개별 튜닝된 자리**라
  한꺼번에 옮기면 어느 이동이 토큰 탓인지 못 가른다. ③ 의 `font-size` 스냅과 같이 처리하는 것이 맞다.

### 6-6. 회귀 · 게이트

- `tools/scan126.js --diff` (±3px): **유지 1775 · 이동 18 · 사라짐 1.** 이동분은 전부 내가 안 건드린
  자리다 — 무작위 닉네임 `#nickN`(문자열 자체가 로드마다 바뀐다) · 애니메이션 배지·카운터·NEW 리본.
  스트로크는 **레이아웃 상자를 바꾸지 않으므로** bbox 이동 0 이 정상이고, 실제로 그렇게 나왔다.
- `node tools/smoke.js` → **SMOKE PASS**.
- 게이트: VERIFY22 65/65 · 52 79/79 · 45 36/36 · 84 30/30 · 102 85/85 · 116 34/34 · 125 24/24 ·
  **104 68/68**.
- **내가 깬 게이트는 104 하나뿐이었고 회수했다.** 배지 스트로크 기준이 «프레임 폭 × .054» 였는데,
  그 .054 는 05·53 에서 뽑은 **통일 상수이지 레퍼런스 실측이 아니다**(`docs/review/104-아이템프레임통일.md` §2).
  글자 기준으로는 r = .054/.235 = **.230** 으로, 126 ② 가 «막힘 확실» 로 가른 구간이었다.
  기대값을 `--st-body`(.15) × 글자비율(.235) = **.03525** 로 갱신했다. «프레임마다 같다» 는 104 의
  불변식 자체는 그대로다(6화면 전부 .0352).
- 32(32/33) · 76(22/23) · 124(52/53) · 61·81(실행 즉시 TypeError) 은 **부모 커밋 워크트리에서 같은
  게이트를 다시 돌려** 똑같이 FAIL 하는 것을 확인했다 — 남의 구간이라 손대지 않았다(지시서 [6]).

### 6-9. 회수 확인 게이트 — `tools/m126counter.js` (COUNTER126 **4/4 PASS**)

«막힘이 풀렸다» 를 눈이 아니라 픽셀로 붙들어 둔다. 자리마다 계산된 타이포그래피(글꼴·크기·자간·
스트로크·`paint-order`)만 뽑아 **격리 렌더**한 뒤, 검정에 갇힌 «바탕색 섬» 중 **가장 큰 것**
(= ㅇ 의 속공간) 면적을 «스트로크 0 / 4회차 이전 고정 px / 지금» 세 조건으로 잰다.

| 자리 | fs | 옛 sw → 새 sw | 스트로크 0 | 옛 값 | 새 값 | 회복 |
|---|---|---|---|---|---|---|
| 52 메뉴 «우편» | 24 | 7 → 3.12 | 42.6 | **2.1** | **8.8** | 4.2× |
| 52 메뉴 «길라잡이» | 18.7 | 7 → 2.43 | 8.5 | 2.2 | 5.1 | 2.3× |
| 52 메뉴 «설정» | 24 | 7 → 3.12 | 0 | 2.4 | 3.1 | 1.3× |
| 10 상점 «10회 소환» | 29 | 6 → 4.35 | 70.6 | **4.5** | **24.1** | 5.4× |

«우편» 은 스트로크가 없을 때 42.6 인 속공간이 옛 값에서 **2.1(5%)** 까지 메워져 있었다 —
H 의 지적이 과장이 아니었다는 확인이다. 지금은 8.8(21%)이다.

> ⚠ **이 게이트를 «제품 요소를 그대로 스크린샷 하는» 방식으로 다시 짜지 마라.** 처음에 그렇게
> 만들었다가 «옛 값이 더 열려 있다» 는 **정반대 결과**를 얻어 30분을 태웠다. 두 가지가 낀다:
> ① `.mn-l::after` 처럼 같은 글자를 **흰 스트로크로 한 번 더 얹는 오버레이**가 있으면 그 흰
> 테두리가 속공간을 바깥에서 다시 메운다(스트로크 폭과 무관한 제3의 변수). ② 외곽선이 두꺼우면
> 이웃 글자끼리 검정이 붙어 «글자 사이·ㅅ/ㄹ 의 홈» 에 가짜 주머니가 생기므로 섬 **개수**를 세면
> 두꺼울수록 늘어나 판정이 뒤집힌다(실측: «설정» 옛값 12개 / 새값 2개). **격리 렌더 + 가장 큰 섬
> 하나**가 이 둘을 모두 피한다.

### 6-8. 레퍼런스 실측 — «고정 px 외곽선» 은 레퍼런스에 존재하지 않는다

토큰 값을 «우리 분포의 중앙» 이 아니라 레퍼런스에 붙이기 위해, 별도 측정자에게 레퍼런스 6장
(`02` · `23` · `10` · `22` · `52-메뉴-버튼-클릭시` · `13`)에서 **크기가 다른 자리 35곳**의
«검정 외곽선 한쪽 두께 ÷ 글자 잉크 높이» 를 재게 했다. 눈대중이 아니라 Pillow 로 휘도
(0.299R+0.587G+0.114B ≤ 60)를 직접 읽고, **오른쪽·아래로 오프셋된 드롭섀도우는 제외**하고
위·왼쪽 값만 채택한 수치다(모든 샘플에 외곽선과 별개로 우·하 1~3px 그림자가 있었다).

| 글자 크기대 | 표본 | 외곽선÷잉크높이 | 절대 두께 |
|---|---|---|---|
| 큰 글자 (잉크 40px+) | 6 | 평균 **.099** (.077~.122) | 4~6px |
| 중간 (잉크 25~40) | 17 | 평균 **.114** (.056~.154) | 1.5~5px |
| 작은 (잉크 <25) | 11 | 평균 **.087** (.04~.119) | 0~2.5px |

**결론(측정자 원문): «작은 글자일수록 비율이 커지지 않고 오히려 살짝 작아진다 — 외곽선은 글자
크기에 거의 비례해 함께 줄어들며, 레퍼런스에는 ‹작은 글자에 두꺼운 고정 px 외곽선› 이 존재하지 않는다.»**

이것이 ②-1 의 결정적 근거다. 우리 쪽 실측(6-1)이 **정확히 반대**(작은 글자 r 중앙값 .217 로 최대)
였으므로, 고정 px 스킴 자체가 레퍼런스와 어긋나 있었던 것이다.

우리 토큰과 대조하려면 단위를 맞춰야 한다. `-webkit-text-stroke` 는 윤곽 중심선이라 **바깥 띠 =
값의 절반**이고, 한글 음절 블록의 잉크 높이는 대략 `font-size × 0.75` 다. 따라서

    r(우리 토큰) = 2 × (외곽선÷잉크높이) × 0.75 = 1.5 × (레퍼런스 비율)

| 구간 | 레퍼런스 비율 | 환산 r | 이번에 넣은 토큰 |
|---|---|---|---|
| 큰 글자 | .099 | **.149** | `--st-title .15` · `--st-hud .17` |
| 중간 | .114 | **.171** | `--st-body .15` · `--st-hud .17` |
| 작은 | .087 | **.131** | `--st-small .13` |

`--st-small`(.13 ↔ 환산 .131)과 `--st-title`(.15 ↔ .149)은 **레퍼런스와 사실상 일치**한다.
`--st-body`(.15)만 레퍼런스 중간 구간(.171)보다 약간 얇다 — 6-2 의 카운터 생존(.15 에서 ~25%,
.17 에서 ~15%)을 감안해 얇은 쪽을 골랐다. **비평가가 «외곽선이 레퍼런스보다 얇다» 고 하면
`--st-body` 를 .17 까지 올릴 여지가 있다 — 토큰 한 줄이므로 ③ 에서 조정하면 된다.**

> 어긋난 자리 하나: 코드 주석(`#tuto.todo` 절)의 «수량 `200` 은 ref 바깥 4.5px» 는 fs 36 기준이라
> 환산 r 이 **.25** 로, 이번 35곳 조사의 어떤 구간보다도 두껍다. 같은 계열인 22 퀘스트 「100」
> 수량 배지는 이번 조사에서 잉크 25 / 바깥 3.5 = 환산 **.21** 로 나왔다. 두 측정이 갈리므로
> «레퍼런스가 수량 표기만은 다른 자리보다 두껍게 두른다» 까지는 말할 수 있어도 4.5px 자체는
> 재확인이 필요하다. 지금은 `--st-hud`(.17)로 두었다.

### 6-7. 신설한 도구

| 도구 | 하는 일 |
|---|---|
| `tools/m126st.js` | 9화면의 스트로크 자리를 모아 `r = sw/fs` 로 줄 세운다. `--all` 전건 · `--json` 저장. `paint-order` 도 같이 찍어 «파먹는 자리» 를 가른다 |
| `tools/m126ct.js` | GameKR 에서 «r 이 얼마일 때 카운터가 막히는가» 를 캔버스 픽셀로 잰다. `paint-order:stroke fill` 과 같은 순서로 그린다 |

> ⚠ `m126ct.js` 를 고칠 일이 있으면 **그리는 순서를 건드리지 마라.** 처음에 «스트로크 → 채움» 이
> 아니라 «채움 → 스트로크» 로 짰다가 «r .12 에서 흰 잉크가 1% 만 남는다» 는 엉뚱한 결론이 나왔다.
> Chromium 의 기본 `paint-order`(normal)는 채움 위에 스트로크를 얹지만, 이 저장소는 385/386 곳이
> `paint-order:stroke fill` 이라 **반대**다.

---

## §7. ②-2 `scaleX` 재보정 — 다음 세션의 작업 목록 (4회차에 재측정해 둔 것)

`tools/m126sx.js` 를 스트로크 토큰 반영 **뒤에** 다시 돌렸다(§2 는 반영 전 수치다. 종수가
67 → **68종 359건**, 삭제 후보 55 → **56종** 으로 미세하게 움직였다). 아래가 지금 기준이다.

### 7-1. 압축이 실제로 필요한 12종 — 지우면 넘친다

| need | 현재 sx | 자리 | 예시 | 자연폭/상자 |
|---|---|---|---|---|
| 1.90 | .89~1.006 | `52 #mnw .mn-b>.mn-l` (7칸 전부) | 우편·랭킹·길라잡이·가방·절전·설정·패스 | 190/100 |
| 1.71 | .5 | `02 #tutoName>em>u` · `#tutoPg>em>u` | 숫자 1 | 12/7 |
| 1.56 | 1.1 | `14 #rwGrid .rw-c>u` | Lv.0 | 223/143 |
| 1.56 | .5 | `02 #tutoBtn>em>u` | 숫자 1 | 14/9 |
| 1.42 | 1.18 | `22 .qs-i>.ifq` | 100 | 136/96 |

### 7-2. 삭제 후보 56종 — **그냥 지우지 마라**

§2 의 경고가 그대로 유효하다. `need ≤ 1` 은 «상자를 안 넘친다» 는 뜻일 뿐 «측정표 폭과 같다» 가
아니다. 3회차 비평가 G 의 2순위가 바로 이 함정의 반대편이다 — **타이틀류가 레퍼런스보다 10~19%
«좁다»**. 즉 이 자리들은 sx 를 **지울(=1로 되돌릴) 게 아니라 1 보다 크게 올려야** 한다:

| 자리 | 현재 sx | G 실측 (우리 → ref) | 차이 | 지우면 | 맞추려면 |
|---|---|---|---|---|---|
| `10 .shp-card>.chd>i` «무기 상자» | .90 | 146 → 181 | **−19%** | 162 (아직 −10%) | sx ≈ **1.115** |
| `10 .cbtn>.lab` «10회 소환» | .86 | 93 → 111 | −16% | 108 (−3%) | sx ≈ **1.027** |
| `23 #trRib` «훈련» 리본 | .93 | 78 → 87 | −10% (높이는 +7%) | 84 (−3%) | sx ≈ **1.038** |

**절차(§2 가 정한 대로)**: `docs/measure/<화면>.md` 에 **잉크 폭이 적힌 자리**는
`sx = 목표폭 / 새 서체 자연폭` 으로 다시 계산해 `--sx-tight` / `--sx-wide` 토큰으로만 남기고,
**안 적힌 자리만** 지운다. 위 3건은 측정표에 폭이 있으므로 **재계산 대상**이다.

> ⚠ 잉크 폭을 잴 때 **외곽선이 폭에 포함된다.** 4회차에서 스트로크를 얇게 했으므로
> 같은 자리의 잉크 폭이 그만큼 **줄었다** — G 의 −10~19% 는 4회차 이전 수치다.
> ②-2 를 시작하기 전에 `tools/ink05.js` 계열로 **해당 자리만** 다시 재고 시작해라
> (전 화면 재측정은 하지 말 것 — 위 3자리 + 12종만).
