# 559 — `tools/verify341.js` [D] 「던전 카드 닷 세로 자리가 ref 봉투 안」 1건 실패

**세션** sess-0135-14085 (워커 G) · **1회차** · 2026-08-31
**결과** `verify341` **31/32 → 37/37 PASS** · **제품 `index.html` 0줄** · 레이아웃 Δ0px · 판정·밸런스 0줄

---

## §1 재현 — 등재문 두 갈래 중 **ⓐ 가 맞았다**

등재문이 갈래를 둘로 열어 놨다.

- **ⓐ** 봉투(−1~+29)가 «닷 외경 42 를 모르는 시절 값» 이다 — 471 이 코너 규약으로 자리를 통일하면서
  중심이 올라갔고 봉투만 안 따라왔다 ⇒ **제품 0줄**
- **ⓑ** 정말로 1.5px 삐져나온다 ⇒ 제품 한 줄

338 규칙대로 고치기 전에 먼저 물었다. 같은 트리에서 4회 돌려 **결정적으로 같은 값**이다.

```
[D] 세로 자리가 ref 봉투(카드상변 −1~+29) 안 — +-2.5..+24.5      FAIL
    현행 코어 실측 : x1005.5..1032.5 · 27 × 27 · 카드상변 +-2.5 (중심 +11)
```

⚠ **등재문의 수치 한 칸을 정정한다** — «상단이 **1.5px** 넘는다» 가 아니라 **0.5px** 이다
(항의 식은 `d0.dy >= -2`, 실측 −2.5). ref 봉투의 하단(+29)에는 4.5px 여유가 남아 있으므로
«봉투가 좁다» 가 아니라 **봉투 전체가 3px 아래에 있다**.

### 뿌리 — 두 자가 서로 반대를 단언하고 있었다(333 과 같은 꼴)

| 자 | 이 닷에 대해 무엇을 단언하나 | 상태 |
|---|---|---|
| `verify341` [D] | 잉크가 카드상변 **−1~+29** 안 (= 중심 ≈ **+14**) | 🔴 |
| `verify471` [A] | 중심이 호스트 코너 안쪽 **`--dot-in` 11px ±2** (= 중심 **+11**) | 🟢 34/34 |

두 항은 **양립할 수 없다**(14 는 11±2 밖이다). 어느 쪽이 옳은지는 이력이 답한다 —
`docs/review/471-레드닷코너통일.md` §3 «지운 좌표 상수 20개» 목록에 **`.dnc .dot` −3/0** 가 그대로
적혀 있다. 즉 471(주인 지시 «레드닷 통일»)이 이 닷의 좌표를 **일부러** 지우고 공용 규약에 묶었고,
그때 중심이 ref +14 → +11 로 **3px 올라간 것**이다. 나중 지시가 옳은 쪽이다.

### 그것을 «주장» 이 아니라 **시험**으로 못박았다 — §R3

471 이 지운 그 값(`top:0; right:-3`)을 되씌워 다시 쟀다:

| | 중심(카드상변에서) | 새 규약 항 | 옛 ref 봉투 |
|---|---|---|---|
| 현행(471 이후) | **+11** | 🟢 | 🔴 (−2.5) |
| `top:0` 되씌움(471 이전) | **+13.5** | 🔴 (어긋남 2.5px) | 🟢 (+0..+27) |

⇒ 두 자는 «맞다/틀리다» 가 아니라 **«전/후»** 였다. 옛 봉투는 쓰레기가 아니라 471 이전 자리를
정확히 재고 있던 자이고, 그래서 **지우지 않고 [D-ref] 로 남겼다.**

---

## §2 처방 — 자리를 상수에서 빼고 **제품에게 묻는다**(335·368 선례)

고친 파일은 `tools/verify341.js` **하나**다.

| 항 | 전 | 후 |
|---|---|---|
| `[D]` 세로 | `dy >= -2 && dy+h <= 31` (봉투 폭 **30**) | `\|중심 − --dot-in\| <= 0.5` (**한 점**) |
| — | (없음) | `[D] 전제` — 제품에서 `--dot-in` 을 실제로 읽었나 |
| — | (없음) | `[D-ref]` — 규약이 측정표 §3-6(+14)에서 벗어난 폭 = **정확히 3px** |
| — | (없음) | `[R3]` 되돌림 3항 (위 표) |

⚠ **허용 오차를 한 칸도 안 넓혔다 — 오히려 좁혔다.** 이 계열의 상습 실패는 «빨간 자를 무르게 푸는 것»
이고(334 처방 ①/②), 봉투를 «−4~+29» 로 늘리는 길이 실제로 열려 있었다. 그 길을 안 간 이유는
그렇게 하면 **닷이 또 옮겨져도 초록**이기 때문이다. 폭 30 짜리 봉투를 ±0.5 한 점으로 바꾼 것이
이 수리의 본체이고, 무르지 않다는 증거가 §R3 다.

⚑ **`--dot-in` 을 다시 건드리는 세션에게** — [D-ref] 가 그 순간 빨개진다. 그것은 «고장» 이 아니라
«측정표 03 §3-6 정오표를 다시 보라» 는 신호다. 이탈 폭을 자가 들고 있어야 레퍼런스와의 거리가
소리 없이 벌어지지 않는다.

---

## §3 곁다리로 같이 잡은 것 — [E] 플레이키(같은 파일 · 같은 게이트)

착수 직후 첫 실행에서 등재문에 없는 **[E] 서브탭 «던전» 배지가 카드와 함께 켜진다** 가 같이 빨갰다
(30/32). 4회 중 **1회 빨강** — 플레이키다.

**뿌리**: 이 하네스는 게임 루프를 얼려 둔다(`window.step = () => {}`). 그러면 `markDirty()` 가 세운
깃발을 걷어 갈 주체가 없어서 `[D]` 의 setup(`__arm(); renderDunPage()`)만으로는 **서브탭 배지가
안 따라온다.** [E] 는 그 setup 이 남긴 상태를 읽는데, 그 값이 «누가 renderUI 를 대신 돌려 줬는가»
라는 경합에 걸려 있었다 — **555·546 이 말하는 «얼리지 않은 주차» 와 같은 병**이다.

**처방**: [E] 자신이 두 줄 아래에서 이미 쓰고 있는 **같은 주문**(`uiDirty = true; renderUI()`)을
[D] setup 에도 건다. 한 줄이고, 같은 파일 안에 이미 있던 관용구다(새 주문 0개).

> 별도 번호로 안 뺀 이유: **내가 지금 들고 있는 바로 그 게이트의 항**이고, 남겨 두면 559 를 닫아도
> `verify341` 이 계속 30/32 ↔ 31/32 를 오간다 = 다음 워커가 같은 자리를 또 연다. 344·372 가 별도
> 등재한 것들은 **다른 게이트**의 플레이키였다.

수리 후 **4회 연속 37/37**(플레이키 0).

---

## §4 게이트

| 자 | 결과 |
|---|---|
| `tools/verify341.js` | **37/37 PASS** (31/32 → · 4회 연속 결정적) |
| `tools/verify471.js` | **34/34 PASS** — 규약 쪽 자. 이 작업이 «471 이 옳다» 로 판정한 근거이고 무수정이다 |
| `tools/verify299.js` | **27/27 PASS** |
| `node tools/smoke.js` | **SMOKE PASS** |

**제품 `index.html` 0줄** — `git diff` 로 확인했다. 그래서 레이아웃·판정·밸런스 회귀는 구조적으로 없다.

---

## §5 문서

- `docs/measure/03-던전팝업.md` **§3-6 정오표 신설** — «세로 중심 +14 는 471 이전 값이다» + 축별 차 표.
  ROUTINE 규칙(«주인 지시로 레퍼런스를 이탈하면 측정표 정오표 필수» · 354 선례).
- `docs/LESSONS.md` — 교훈 3개.
