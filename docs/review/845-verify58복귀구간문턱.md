# 845 — `verify58` [19] 「씬 A 알약 팝 — 복귀 실효 구간」이 문턱에 붙어 흔들린다

- 세션: `sess-0213-16882` (루틴 워커 A) · 2026-09-03
- 티어: T1 급 «잠복(문턱 플레이키 · 825·836·574·344 계열)»
- **제품 `index.html` 0줄** · 고친 것은 `tools/verify58.js` 의 [19] 한 절 · 신설 `tools/verify845.js`
- 지시서 [3]-**(가)** 기계적/게이트 작업 — **비평가 없음**

---

## 1. 재현 — 등재문이 맞았고, 부하를 걸면 바로 나온다

등재문(2026-09-02, sess-2228-4369 워커 B)은 «7회 실행 중 1회 73ms 로 빨강(나머지 103·104·141·143·172ms)»
이라고 적었다. 착수해서 **순차 3회**를 돌리니 104 · 110 · 124ms 로 전부 초록이라 재현이 안 됐다.
그래서 **같은 자를 동시에 4개** 돌렸다 — 845 를 낳은 것이 «러너 부하» 이므로 부하를 만들어 준 것이다.

| 실행 | 복귀 실효 구간 | tick 표본 | 판정 |
|---|---|---|---|
| 동시 #1 | 149ms | 27장 | ✓ |
| 동시 #2 | **84ms** | 21장 | **✗** |
| 동시 #3 | 137ms | 23장 | ✓ |
| 동시 #4 | 108ms | 31장 | ✓ |

**한 번에 재현됐다.** 표본 수가 49장(한가할 때) → 21장(붐빌 때)으로 반토막 나고, 값이 84~149ms 로 널뛴다.
등재문의 «표본 격자로 재는 것이 범인» 이 그대로 확인됐다.

## 2. 뿌리 — 하나가 아니라 셋이었다

등재문은 «봉우리→바닥 복귀를 샘플러 격자로 재는 것» 하나를 지목했다. 실제로는 **셋**이고, 하나만 고치면
나머지 둘이 산포를 그대로 남긴다.

| ⓐ | **격자 양자화** | 39회차 자는 문턱 통과 시각을 «넘은 **첫 표본**» 의 `t` 로 썼다 ⇒ 값의 오차가 통째로 표본 간격이다. 공용 `tick` 은 표본마다 selector 를 수십 개 도느라 실측 간격이 **지시 8ms 에도 50~80ms** 다. |
| ⓑ | **봉우리 표본 결손** | 진폭을 표본의 `max−min` 으로 잡는다 ⇒ 붐빌 때 봉우리 표본이 빠져 진폭이 깎이고(84ms 실행의 봉우리 **242.6px** = 선언 246.9 의 98.3%), 진폭이 깎이면 문턱 **둘이 같이** 움직여 값이 또 흔들린다. 바닥도 «표본 최소» = 되튐 `.985` 의 밑점이라 정지 폭이 아니다. |
| ⓒ | **성긴 표본** | 복귀 구간이 150ms 안팎인데 그 안에 표본이 2~3장뿐이면 어떤 자를 써도 못 잰다. |

## 3. 처방 — 등재문의 갈래 ⓐ(«시각 기반으로 다시 잰다»), 셋을 다 막았다

1. **문턱 통과 시각을 앞뒤 두 표본 사이 선형 보간으로.** 복귀 구간은 선언이 이미 «거의 선형»
   (`cubic-bezier(.3,.05,.7,.95)`)이라 보간이 바로 그 구간의 모양이다.
2. **봉우리·정지폭·기대 구간을 «선언» 에서 읽는다.** `@keyframes fxPunch` 를 **CSSOM** 으로 캐낸다
   (게이트가 자기 사본을 들면 부패한다 — 211·289). 제품이 봉우리·길이를 바꾸면 자가 따라온다.
3. **폭 한 줄만 읽는 rAF 기록기**(`pillRec`)를 따로 돌린다. 노드를 미리 잡아 두므로 프레임당 비용은
   `getBoundingClientRect` 하나뿐이고, 공용 `tick` 에는 **한 줄도 더 안 얹었다**(씬 A 의 타이밍 축에
   부하를 얹지 말라는 570 규약).

⚠ **4번째가 회차 중에 하나 더 나왔다 — 씬 A 는 3박자라 «팝» 이 여러 번 난다.** 첫 시안은 25% 문턱의
«첫» 하강 교차를 썼는데, 앞 팝의 복귀는 **다음 팝이 중간에 끊는다** ⇒ «끊긴 복귀» 와 «온전한 복귀» 가
실행마다 섞여 동시 4실행 산포가 **25ms**(130~155)로 남았다. 25% 는 **마지막** 하강 교차(= 뒤에 아무것도
안 오는 팝)를 잡고 10% 는 그 뒤 첫 교차로 바꾸자 산포가 **11ms** 로 내려갔다.

## 4. 문턱 — 갈래 ⓑ(«내린다»)는 안 골랐다. 오히려 **엄해졌다**

```
lim = max( FLOOR 105 , 0.70 × 기대 실효 구간(선언 파생 153.6ms) = 107.5 )
```

- **상대 문턱** — 선언 곡선(구간별 선형)에 같은 두 문턱을 대고 «기대 실효 구간» 을 계산해 그 70% 를
  요구한다. 눈대중 상수가 아니고, 제품이 선언을 고치면 따라 움직인다.
- **절대 하한 105ms** — 상대 문턱만 두면 **선언 자신이 나빠진 빌드**가 헛초록이 된다(39회차 이전 빌드는
  기대가 96ms 라 70% = 67ms 로 문턱이 같이 내려간다). 105 는 37회차 처방 «복귀 구간을 180ms 로 펼 것» 의
  실효 몫(180 × 153.6/201.6 = **137ms**)의 **3/4**.
  ⚠ 종전 90ms 을 그대로 안 쓴 이유 — **그 값은 옛 눈금의 것**이다(§5 참조).

## 5. ★ 새 자가 옳다는 것은 **39회차 자신의 산수**가 증언한다

`index.html` 의 `@keyframes fxPunch` 바로 위 주석(39회차)이 이렇게 적어 두었다 —
> 복귀 구간이 101ms(**실효 93ms**) → **202ms** 로 펴진다.

| | 39회차가 손으로 적은 값 | **새 자** 실측 | 옛 자 실측 |
|---|---|---|---|
| 39회차 **이전** 빌드(고원 60% + 전 구간 ease-out) | 실효 **93ms** | **78 · 90 · 94ms** | **34ms**(주석: «두 번 연속 34·34») |
| 현행 빌드 | 실효 몫 = 이 자의 `exp` **153.6ms** | **128~140ms** | 73~173ms |

**새 자는 39회차 저자가 종이 위에서 계산한 값을 그대로 되읽는다.** 옛 자는 흔들리기만 한 게 아니라
값 자체가 실효의 **1/3** 이었다 — 그래서 «90ms» 라는 옛 문턱은 새 눈금에서는 «이전 빌드에 딱 붙는»
자리이고, 그대로 두면 되돌림 시험이 흔들린다. 문턱을 새 눈금으로 다시 적은 근거가 이것이다.

## 6. 게이트 — `tools/verify845.js` **9/9 신설**

`verify732`(같은 짝 `verify58`)의 틀을 그대로 썼다. 주입 사본은 저장소 루트 · 이름에 pid(646·648).

| 축 | 무엇을 주입하나 | 기대 | 실측 |
|---|---|---|---|
| §0 | 없음(청정 트리) | [19-a]·[19-b] **초록** | 132·134·140ms |
| §R-a | 39회차 **이전** 빌드 재현(고원 60% + 전 구간 ease-out) | [19-a] **빨강** | 78·90·94ms (< 105) |
| §R-b | 복귀를 한 프레임으로(90% 키프레임 → 44%) | [19-a] **빨강** | 22·24·25ms |
| §R-c | 기록기 결손(`.cGold` → `.cGoldX`) | [19-a] **빨강** «잴 대상이 없다» | rAF 표본 0장 |
| §R-d | 선언 결손(`@keyframes fxPunch` 개명) | [19-a] **빨강** «CSSOM 에서 못 읽었다» | — |
| §S | 없음 — **동시 4실행**(845 를 낳은 그 부하) | 산포 ≤30ms · 넷 다 초록 | **산포 11ms**(128·130·139·133) |

⚑ **§S 가 이 작업의 본체다.** 같은 네 실행에서 **옛 자**를 나란히 찍으면 산포 **47ms**(81 · 128 · 93 · 84)
이고 **넷 중 셋이 종전 문턱 90 아래**다 — 즉 이 부하에서 옛 자는 3/4 이 빨갛다. 새 자는 4/4 초록이다.

⚑ **§R-c·§R-d 는 «못 쟀다» 가 «괜찮다» 로 읽히지 않게 한다.** 갈래가 둘(기록기·선언) 늘었으므로
각각 빨개지는지 물어야 한다(333 «자리를 비우지 마라»). 자 안에도 전제항 **[19-b]**(«문턱 둘이 실제로
두 표본 **사이**에서 보간됐는가»)를 새로 세웠다 — 표본이 성겨 문턱이 구간 밖으로 나가면
«복귀가 없다» 와 «못 쟀다» 가 구별되지 않는다.

## 7. 검증

- `node tools/verify845.js` **9/9 PASS** (신설)
- `node tools/verify58.js` **33/33 PASS** — 항 하나 늘었다([19-b] 전제항 신설). 동시 4실행에서도 4/4 초록.
- 회귀(무수정): `verify732` **15/15 PASS**(같은 짝 `verify58` 의 되돌림 시험 — 주입 8축) ·
  `probe844` **17/17 PASS** · `verify93` **33/33 PASS**
- `node tools/verifyProgress.js` **PROGRESS OK**(§1~§5 빨강 0건)
- `node tools/smoke.js` **SMOKE PASS**
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄

## 8. 곁다리 관측 — **등재 0건**(내 변경과 무관함을 대조로 확인)

동시 4실행에서 [19] 말고도 빨간 항이 나왔다(29~31/32). **수리 전 트리에서도 같은 부하에서 같은 항이
빨갛다** — 씬 A·B 의 타이밍 축이 러너 부하에 물린다는 것은 570 이 이미 적어 둔 사실이고, 그 항들은
**내 작업 단위 밖**이라 손대지 않았다(병렬 세션 규칙 3). 순차 실행에서는 **33/33** 이다.
문턱에 붙은 것이 아니라 부하에서만 뒤집히는 축이라 «잠복» 으로도 등재하지 않았다 —
같은 종의 등재가 이미 **846**(`verify818` [R1]·[R2] 표본 수)에 살아 있고, 그쪽 처방(«표본기에서
화소 주사를 떼어 개수와 덮음을 따로»)에 이 작업의 처방 셋이 그대로 쓰인다. **846 이 이 파일을 읽어라.**
