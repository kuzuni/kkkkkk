# 427 — 탑도 «층» 이 아니라 «레벨» (저장소 주인 지시 2026-08-30)

> 세션 `sess-0546-19844` 워커 C · 1회차 · **제품 `index.html` 9자리(문자열만)** ·
> 레이아웃 Δ0px · 데이터·판정·밸런스·세이브 **0줄** · 지시서 [3]-**(가)** (기계적 작업 — 비평가 없음)

주인 원문: «탑들도 층 말고 레벨로 바꾸기».
209 가 2026-08-2x 에 «탑은 «레벨» 이 아니라 «층» 이다(03 카드·04 세부와 같은 낱말)» 로 못박아 둔
**낱말 규약을 주인이 뒤집었다.** 규약이지 자리 하나가 아니므로, 이 작업의 크기는 «라벨 한 칸» 이
아니라 **그 규약이 흩어져 있던 자리 전부**다.

---

## 1. 무엇이 실제로 어긋나 있었나 — 자리는 아홉이고 그중 넷이 «던전·탑 공용» 이다

등재문이 여섯 자리를 짚었는데 실측은 **아홉**이었고, 결정적인 것은 그중 **넷이 던전과 공용 한 줄**이라는
사실이다 — 즉 이 지시는 «탑만» 고칠 수가 없다. 공용 줄에서 «층» 을 «레벨» 로 바꾸면 던전 쪽 문구도 같이
바뀌는데, **던전은 원래 «레벨» 이라 오히려 그쪽이 정합이 된다**(던전 런 타이틀이 이미 `' - 레벨 ' + f` 였다).

| # | 자리 | 옛 문자열 | 새 문자열 | 탑 전용? |
|---|---|---|---|---|
| ① | 런 타이틀 `dunTtlBase` (~23543) | `d.n + (isTower(d) ? ' - ' + f + '층' : ' - 레벨 ' + f)` | `d.n + ' - 레벨 ' + f` | **분기째 소멸** |
| ② | 실패 통보 (~23589) | `' ' + f + '층 실패 — '` | `' 레벨 ' + f + ' 실패 — '` | 공용 |
| ③ | 소탕 안내 (~23668) | `먼저 <b>1층</b>을 클리어해야` | `먼저 <b>레벨 1</b>을 클리어해야` | 던전 |
| ④ | 339 카운트다운 (~23768) | `다음 층 자동 도전` | `다음 레벨 자동 도전` | 공용 |
| ⑤ | 던전 탭 안내문 (~24565) | `다음 층이 열립니다 · 이전 층 보상` | `다음 레벨이 …· 이전 레벨 보상` | 던전 |
| ⑥ | 던전 탭 행 캡슐 (~24572) | `f + '층'` | `'레벨 ' + f` | 던전 |
| ⑦ | 04 세부 라벨 `#dgdLvL` (~24721) | `'층'` | `'레벨'` | 탑 |
| ⑧ | 던전 잠금 사유 `dunLockTxt` (~25587) | `' <b>' + pre.f + '</b>층 클리어'` | `' 레벨 <b>' + pre.f + '</b> 클리어'` | 던전 |
| ⑨ | 03 탑 카드 좌 캡슐 (~25729) | `<i>층</i>` | `<i>레벨</i>` | 탑 |

⚑ **⑧ 은 등재문이 «같이 갈지 확인» 으로 남겨 둔 자리다 — 같이 갔다.** 이유는 취향이 아니라 게이트의
요구다: 이 작업의 통과 기준이 «**제품 문자열에 «층» 0건**» 인데, ⑧ 은 던전 문구이면서 «층» 을 쓰던
마지막 자리였다. 남겼으면 «유물석 던전만 층» 이라는 **새 예외**가 생긴다(209 가 만든 예외를 지우면서
다른 예외를 세우는 꼴).

## 2. 안 건드린 것 — 지시는 «표기만» 이다

- 진행 키 `S.tower` / `S.tower2` · `TOWERS[].fk` · `towerSetFloor` / `towerFloor` **이름 그대로**
  (리네임은 이관 비용만 낳는다 — 등재문 지시).
- 규칙 그대로: «현재 레벨만 도전»(◀▶ 둘 다 잠금) · «입장권 0 차감» · «소탕 없음» · «이전 단계 재도전 불가».
- **세이브 이관 없음**(KEY 안 올림) — 저장되는 것은 숫자뿐이고 낱말은 어디에도 안 들어간다.
- 측정표 **정오표 없음** — 라벨 상자(`.dgd-lv` · `.lb.a`)의 기하는 그대로이고 글자만 바뀐다.
  두 자리 다 **던전이 이미 «레벨» 로 쓰고 있던 같은 상자**라 폭이 이미 증명돼 있다(03 카드 `.lb a` ·
  04 `#dgdLvL` 는 던전 모드에서 내내 «레벨» 이었다).
- 밸런스·판정 0줄 · 레이아웃 Δ0px.

⚠ 한 자리만 길어졌다 — **실패 통보**가 «황금 동굴 999층 실패» → «황금 동굴 레벨 999 실패» 로 **3글자** 늘었다.
`verify149` §3(워스트케이스 토스트가 프레임 1080 안에 드는가)의 표본을 그 새 문구로 갈아 끼웠고
**285/285 초록** — 폭 예산 안이다.

## 3. 게이트 — 이관 6종 + 신설 1종

**신설 `tools/verify427.js` 36/36 PASS.** 자리마다 따로 묻는다([A] 소스 · [B] 런 타이틀 · [C] 04 라벨 ·
[D] 03 카드 · [E] 실패 통보 · [F] 소탕 · [G] 카운트다운 · [H] 던전 탭 · [I] 잠금 사유 · [K] 규칙 0줄 · [R] 되돌림).

⚑ **[A] «제품 문자열에 «층» 0건» 하나로 닫지 않았다.** 소스 grep 은 «그렸는가» 를 못 본다(350·368 규약) —
그래서 아홉 자리를 **전부 런타임에서** 다시 물었다. 반대로 런타임만 보면 «안 열어 본 화면» 이 샌다.
두 겹이 서로의 구멍을 막는다.

⚑ **[K] 가 이 자의 절반이다.** 낱말을 바꾸다 규칙을 같이 건드리는 것이 이 작업의 진짜 위험이고
(«층 선택» 이라는 말이 코드 곳곳에 개념어로도 쓰인다), [K] 는 진행 키·`towerSetFloor`·◀▶ 잠금·
입장권 불변·클리어 +1 을 되물어 **«바뀐 것은 낱말뿐»** 을 못박는다.

**§R 되돌림 시험** — 세 자리(런 타이틀·카드 라벨·카운트다운)를 옛 낱말로 되돌린 **사본**에서 [A] 가
실제로 빨개진다. 음성항 둘도 같이 세웠다: 주석 안 «층» 은 안 세고(안 그러면 209·210 설계 주석 때문에
영원히 빨간 게이트가 된다), 주석 밖 «층» 은 반드시 센다.

### 이관 (전부 초록으로 되돌림)

| 게이트 | 무엇을 옮겼나 | 결과 |
|---|---|---|
| `verify209` | 카드 좌 캡슐 `'층'` → `'레벨'` · `#dgdLvL` · HUD «시련의 탑 - 1층» → «- 레벨 1» · 실패 토스트 | **61/61 PASS** |
| `verify149` | §1 조각 `"층 실패 — '"` → `"' 레벨 ' + f + ' 실패 — '"` · §3 폭 워스트케이스 · §255/§264 소스 regex | **285/285 PASS** |
| `verify339` | 카운트다운 기대 문자열 7곳 | **113/113 PASS** |
| `verify90` | 잠금 문구 `/5<\/b>층 클리어/` → `/레벨 <b>5<\/b> 클리어/` · «용의 무덤 레벨 5 클리어» | **107/107 PASS** |
| `verify61` | §10 잠금 라벨 «N층 클리어» → «레벨 N 클리어» | **109/109 PASS** |
| `verify264` | 문자열 단언 없음(개념어만) — 무수정 | **85/85 PASS** |

⚑ **`verify149` 이관에 항을 하나 **더했다**(328-330 교훈).** 조각 한 줄만 새 문구로 갈아 끼우면
«427 이 통째로 되돌아가도 초록인 게이트» 가 된다 — 그래서 `§427 던전·탑 실패 통보에 «층» 0건`
(옛 `' + f + '층 실패` 형태를 소스에서 되묻는 음성항)을 같은 절에 붙였다.

### 회귀 (무수정)

`verify30` 49/49 · `verify257` 110/110(«보스 1마리 던전 타이틀 = 옛 문자열 그대로» 가 `황금 동굴 - 레벨 1`
이라 **던전 쪽 형식이 안 바뀌었음의 가장 짧은 증거**다) · `verify188` 79/79 · `verify46` 70/70 · **SMOKE PASS**.

## 4. 곁다리 관측 — 434 로 등재

`verify210` 이 **92/93** 으로 빨갛다: `✗ 단련석은 던전 계열이 아니라 입장권을 안 만들었다(125 [H] 5종 유지)`.
**338·344 규칙대로 대조했다** — 내 변경 전부를 `git stash` 로 들어낸 트리에서 같은 자를 돌려
**같은 92/93 · 같은 한 항**이었다. 427 과 무관하고, 402(입장권 8종 분리)·430(8색 확정)이 «5종뿐» 이라는
125 [H] 의 옛 상수를 갱신하면서 210 쪽 짝이 안 따라온 자리로 보인다. **내 작업 단위가 아니라 안 고쳤다.**
