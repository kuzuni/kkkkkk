# 763 — `tools/verify268.js` 「미보유 장비 제목 = ???」 (게이트 부패 · T1)

> **완료(2026-09-01, sess-1942-6727 루틴 워커 C · 1회차) · 제품 `index.html` 0줄 · 자 `tools/verify268.js` 한 자리(+ 부품 1개).**
> `verify268` **148/149 FAIL → 152/152 PASS** · 되돌림 시험으로 **150/152 빨강** 확인 ·
> 회귀 `verify664` 53/53 · `verify662` 30/30 · **SMOKE PASS**.

## §1 재현부터 (338 규칙)

등재문: «`verify268` 1건 실패(148/149) — 664 가 뒤집은 «미개방 정보 공개» 규약을 이 항이 아직 옛 방향(«???»)으로 묻는다».

```
$ node tools/verify268.js
  ✗ [미보유 장비] 제목 (기대 ??? · 실제 투박한 곤봉)
VERIFY268 148/149 FAIL
```

**등재문 그대로 재현됐다.** 빨간 항은 하나뿐이고, 그 한 항이 요구하는 «???» 는
664(주인 지시 2026-09-02 00:27 «미개방장비들이 스펙이랑 아이콘이 뭔지 정보 볼수있게 해줘야함»)가
**주인 지시로 걷어낸 바로 그 은닉**이다. 등재 세션(703·704 워커 C)이 수리 전 트리(`71e0a2e^`)에서도
같은 148/149 임을 대조로 못박아 뒀으므로 이 회차는 귀속을 다시 파지 않았다.

**제품이 옳고 자가 옛 방향을 묻는다** — 게이트 부패다. 제품 `index.html` 은 한 줄도 안 건드렸다.

## §2 자리 — 제품은 이미 «실이름» 을 쓰고 있다

`index.html` **28950~28954**(664 가 적은 주석과 그 다음 줄):

```js
/* 664 — 미보유라고 이름을 «???» 로 가리지 않는다(주인 문면 «정보 볼수있게»). ... */
$('mtitle').textContent = it.n;
```

버튼 쪽도 같은 회차가 «막는 것은 종전 그대로 `disabled` · 바뀐 것은 라벨이 이유를 말한다» 로 정해 뒀다:

```js
acts = '<button class="sk-e" id="mEq"' + (own ? '' : ' disabled') + '><b>'
     + (own ? (eq ? '해제' : '장착') : '미보유') + '</b></button>' + ...
```

실측(미보유 표본 `weapon0_1`): 제목 «낡은 손도끼» · 버튼 2개 `mEq`(라벨 «미보유» · disabled) ·
`mLv`(«강화» · disabled) · 아이콘 🪓 · 등급 «일반» · 표 «부위/효과» = «무기/공격력».
⇒ **정보는 전부 열려 있고 상태(못 가졌다)도 안 감춰져 있다.** 자만 옛 세계에 있었다.

## §3 처방 — 333 처방(방향만 뒤집되 **자리를 비우지 않는다**)

옛 항 한 줄을 지우고 끝내면 **«664 가 통째로 되돌아와도 초록인 게이트»** 가 된다.
등재문 처방대로 **짝 항**으로 갈아 끼웠다 — 한 항이 «열렸다» 를, 다른 항이 «그래도 막혀 있다» 를 든다.

| | 옛 항 (1건) | 새 항 (4건) |
|---|---|---|
| 제목 | `제목 === '???'` | `제목 === it.n`(**데이터 파생** — 표본의 `x.n` 을 같은 evaluate 안에서 같이 걷어 온다) |
| 버튼 자리 | — | `[장착] 자리 id === 'mEq'` |
| 반려 문면 | — | `[장착] 라벨 === '미보유'`(664 ③ «라벨이 이유를 말한다») |
| 소스 | — | `showItem` 본문에 `$('mtitle').textContent = it.n;` 이 그대로 있다(가림 삼항 부활 0) |

기존 `[미보유 장비] 두 버튼 다 비활성` 은 **그대로 두었다** — 짝의 «막혀 있다» 쪽을 이미 그 항이 들고 있다.

⚠ **기대값을 손으로 적지 않았다.** 제목 기대값은 표본 자신의 `x.n` 이라
장비 표가 바뀌어도 자가 따라온다(이름을 상수로 박으면 그것이 다음 번 부패다).

⚠ **소스 항의 앵커는 `showItem` 본문으로 좁혔다.** `$('mtitle').textContent = it.n;` 은
파일에 **두 번** 나오고(28783 = `showSkillDetail`, 662 의 구간 · 28954 = `showItem`, 664 의 구간),
파일 전체로 물으면 **662 쪽이 대신 초록을 준다.** §3 이 인라인으로 쓰던 본문 잘라내기를
부품 `ITEM_FN` 으로 뽑아 §5 가 같은 구간을 보게 했고, 앵커가 빗나가면 빈 문자열이 아니라
`null` 을 돌려 부정 단언이 헛통과하지 않게 했다(315 · 212-① 교훈).

## §4 되돌림 시험 — 무르게 푼 수리가 아님의 증거

`showItem` 의 제목 줄만 664 **이전 꼴**(`own ? it.n : '???'`)로 되돌린 사본에서 재실행:

| 항 | 정상 트리 | 664 되돌린 사본 |
|---|---|---|
| `제목 = 데이터의 실이름` | ✓ (투박한 곤봉) | **✗ (실제 ???)** |
| `showItem 이 제목을 데이터에서 그대로 쓴다` | ✓ | **✗** |
| `[장착] 라벨 = 미보유` | ✓ | ✓ (664 의 다른 자리라 안 딸려 간다) |
| 합계 | **152/152 PASS** | **150/152 FAIL** |

⇒ 새 항 둘이 **664 의 회귀를 실제로 잡는다.** (사본은 시험 뒤 원본으로 되돌렸다 — `git status` 에 `index.html` 없음.)

⚑ **1회차에 이 시험을 한 번 헛돌렸다.** 첫 시도는 파일 전체에서 첫 번째 일치를 갈았는데
그것이 **28783 = 스킬 쪽(662 의 구간)** 이라 `verify268` 이 152/152 초록 그대로였다.
그 헛시험이 없었으면 «파일 전체로 물어도 된다» 고 믿고 §3 의 앵커를 넓게 적었을 것이다 —
**되돌림 시험이 자기 자신의 앵커 결함을 먼저 잡은 자리**다(LESSONS 763-① 로 남긴다).
겸사로 확인한 것 하나: 그렇게 되돌린 **스킬** 제목은 `verify268` 이 안 잡는다(§8 은 «스킬 세부 불변»
을 표 헤더·버튼 규격으로만 본다). 그 자리는 662 의 구간이고 `verify664` **§5-b**(«미보유 스킬 제목은 실명이다»)
가 이미 지키고 있어 **구멍이 아니다** — 곁다리 등재 없음.

## §5 게이트·회귀

| 자 | 결과 |
|---|---|
| `node tools/verify268.js` | **152/152 PASS** (수리 전 148/149) |
| `node tools/verify664.js` | 53/53 PASS |
| `node tools/verify662.js` | 30/30 PASS |
| `node tools/smoke.js` | **SMOKE PASS** |

**항 수 149 → 152**(1건을 4건으로 갈아 끼움 · 지운 항 없음).
제품 `index.html` **0줄** · 레이아웃 0px · 세이브 이관 없음(KEY 불변).

## §6 남은 것

없음. 같은 꼴의 부패를 전수로 훑어 보니 `verify268` 안에서 «미보유/미개방» 을 옛 방향으로 묻는 항은
이 하나뿐이었고(다른 «???» 단언 0건), 664 규약의 다른 시트(펫·코스튬)는 `verify664` 가 이미 새 방향으로 지킨다.
