# 587 — 플레이어·몬스터 스프라이트가 이동 방향과 반대로 뒤집힌다 「T1 버그」

주인 원문: «플레이어나 몬스터나 **이동하는 방향으로** 스프라이트 되잇기. **현재 반대인상황이 많음**»

세션 `sess-0629-25862` (루틴 워커 A) · 2026-08-31

---

## 1회차 — 재현 (338 규칙: 처방 전에 재현)

### §1 자 두 벌을 먼저 세웠다

* `tools/atlasface587.js` — «아틀라스 원본이 어느 쪽을 보는가» 를 픽셀로 재는 자.
  `probe587` 과 `verify587` 이 **같은 함수**를 쓴다(385 규약 — 자는 한 벌).
* `tools/probe587.js` — «찍히는 방향 ↔ 실제 이동 방향» 을 제품에게 직접 묻는 자.

판정 규약은 하나다. `drawFrame(…, flip, …)` 은 `flip` 이 참이면 `ctx.scale(-1,1)` 로 좌우를 뒤집으므로
(index.html 17581),

> **drawnRight = (원본이 오른쪽을 본다) !== flip**

이고 이 자는 그 `drawnRight` 를 **좌표 변화의 부호**와 견준다.

### §2 ⚑ 등재문의 ⓑ 를 재려다 «표가 없다» 가 아니라 **«주석이 거짓말이다»** 를 잡았다

등재문은 «아틀라스별 원본 방향 표가 펫에만 있다» 고 적었다. 맞다. 그런데 **없는 표를 채우려고 실측했더니
제품이 하드코딩으로 들고 있던 값 자체가 틀려 있었다.**

`index.html` 22136 은 이렇게 적혀 있다:

```
player.flip = Math.cos(player.aim) > 0;   /* 기사 원본은 왼쪽을 봄 */
```

**그 주석이 틀렸다 — knight 아틀라스는 오른쪽을 본다.** 출처가 셋이고 셋이 일치한다:

| 출처 | knight | elves 초록 | elves 파랑 | zombie | bird·robo·dragon |
|---|---|---|---|---|---|
| ① 공격 뻗음 눈금(기계적) | **오른쪽** (+29 / −4, 여유 **25**) | **왼쪽** (여유 **−142**) | **오른쪽** (여유 **+128**) | 잴 수 없음(공격 애니 = walk) | — |
| ② 비평가 A(독립) | 오른쪽 | 판단 보류 | 판단 보류 | 판단 보류 | 오른쪽 ×3 |
| ③ 비평가 B(독립) | 오른쪽 | 판단 보류 | 판단 보류 | 판단 보류 | 오른쪽 ×3 |
| ④ 비평가 C(애니 스트립, 독립) | 오른쪽 | **왼쪽**(쏜 화살촉이 왼쪽) | **오른쪽**(구슬이 오른쪽) | 판단 보류 | — |
| ⑤ 제품의 기존 선언 `PET_SP.faceRight` | — | — | — | — | 오른쪽 ×3 |

①은 **기계적**이다 — 무기는 보는 쪽으로 나가므로, 정지 프레임의 잉크 상자와 공격 프레임의 잉크 상자를
논리 프레임 좌표에서 견주면 뻗은 쪽이 곧 보는 쪽이다. 비평가 C 가 «화살촉이 왼쪽·구슬이 오른쪽» 을
프레임 안 bbox 좌표까지 짚어 같은 답을 냈다.

⚠ **머리 눈금(위 1/3 잉크 중심)은 버렸다.** 처음 세운 자가 그것이었는데 **교정에 떨어졌다** —
이미 `faceRight:true` 로 선언돼 있는 펫 3종을 **1/3 만** 맞힌다(robo −0.003 · dragon −0.002).
머리가 위가 아니라 «앞» 에 달린 몸에서는 위 1/3 이 머리가 아니기 때문이다.
**무른 자를 통과선으로 쓰지 않는다** — `probe587` 은 이 눈금을 기록만 하고 판정에는 안 쓴다.

### §3 ⚑ «아틀라스 하나 = 방향 하나» 가 거짓이다 — elves 시트는 둘이 서로 반대다

등재문 처방 ①은 «`ATLAS` 옆에 아틀라스별 원본 방향을 선언» 하라고 적었다. **그 단위로는 못 적는다.**
`elves` 한 장에 고블린(초록)과 다크엘프(파랑)가 같이 들어 있고, **둘이 서로 반대를 본다**:

* 초록 — 활이 **왼쪽**으로 뻗고 쏜 화살촉도 왼쪽(여유 −142)
* 파랑 — 지팡이가 **오른쪽**으로 뻗고 구슬도 오른쪽(여유 +128)

⇒ 표의 키는 **아틀라스 + 애니 계열**이어야 한다.

### §4 수리 전 기준선 — `node tools/probe587.js`

```
 ✗ [B] 플레이어             38/258  = 14.7%   (정지 표본 220 — 아래 ⚑)
 ✗ [C] zombie              527/5987 =  8.8%
 ✗ [C] goblin              363/372  = 97.6%   ← 가장 크고 가장 잘 보인다
 ✓ [C] dark                  0/373  =  0%
 ✓ [D] promo(knight)         0/53   =  0%
 ✓ [D] arena(knight)         0/37   =  0%
 ✓ [E] 시체 — flip 승계       1/1
 ✗ [G] 조이스틱 오른쪽       41/56   = 73.2%
 ✗ [G] 조이스틱 왼쪽         41/52   = 78.8%
```

**주인이 본 «반대인 상황이 많음» 의 실체는 셋이고, 등재문이 짚은 것은 그중 하나 반이다.**

1. **고블린이 통째로 반대다(97.6%)** — 등재문 ⓑ 가 맞았다. 다만 뿌리는 «적 아틀라스가 셋» 이 아니라
   **«한 아틀라스 안에서 둘이 반대»** 다. `e.flip = dx < 0`(22427)은 «원본이 오른쪽» 을 전제한 식이라
   파랑·knight 계열(promo·arena)에는 **이미 맞고**(전부 0%), 초록에만 정확히 반대로 먹는다.
   고블린은 스테이지 3부터 스폰의 **44%** 다 — 주인이 «많음» 이라고 한 것의 큰 갈래가 이것이다.
2. **플레이어가 «조준» 을 본다(등재문 ⓐ)** — 그런데 §2 때문에 증상이 등재문과 다르다.
   knight 원본이 오른쪽인데 `flip = cos(aim) > 0` 이라 **플레이어는 언제나 표적에 등을 돌린다.**
   ⇒ ⓐ 는 «카이팅 구간에서만 반대» 가 아니라 **표적에 붙어 갈 때 반대(14.7%)이고, 멈춰 서서 쏠 때는
   상시 등을 보인다**(위 «정지 표본 220» 이 전부 그 자리다 — 이동 축이 없어 [B] 의 분모에도 안 들어간다).
   등재문에 없던 갈래이고, 표를 고치면 같이 낫는다.
3. **조이스틱 수동 이동이 양쪽 다 반대다(73~79%)** — 등재문 ③ 이 «자동/수동이 같은 규칙을 쓰게 하라» 고
   적은 자리인데, 실제로는 수동 쪽이 **자동보다 더 나쁘다**(손가락으로 왼쪽을 밀어도 오른쪽 적을 본다).

⚠ **zombie 8.8% 는 눈에 안 보이는 숫자다.** 좀비 아트는 위에서 내려다본 기는 자세라 **좌우 축이 아예
없다**(비평가 3인 전원 «판단 보류» — 머리가 아래를 보고 팔이 좌우 대칭이다). 8.8% 는 `e.flip` 이
«실제 이동» 이 아니라 «플레이어 쪽» 을 보기 때문에 생기는 잔차(스월·분리 힘)이고, 어느 쪽으로 뒤집어도
찍히는 픽셀이 같다. **이 숫자를 결함으로 세지 않는다.**

---

## 2회차 — 수리

### §5 제품 변경 — `index.html` **4자리**

| 자리 | 무엇을 |
|---|---|
| 17411 (신설) | **`SPRITE_FACE` 표 + `srcFaceRight()` + `faceFlip()` + `MOVE_EPS`** — 원본 방향을 선언하는 단 하나의 자리 |
| 18260 `PET_SP` | `faceRight:true` **세 줄 삭제**(두 벌 금지 — 값은 표로 옮겼고 셋 다 `true` 라 펫 거동 Δ0) |
| 22136 → 22245 | 플레이어 — 조준 기준 한 줄을 **걷어내고**, 이동이 정해진 뒤로 내려 «이동 방향» 으로 |
| 22427 | 적 — `e.flip = dx < 0` → 실제 이동 성분 `(ax/al)×spd` 기준 |
| 22341 | 펫 — 같은 표를 지나게만 함(옛 삼항과 **항등**) |

표의 키가 «아틀라스» 가 아니라 **«아틀라스 + 애니 계열»** 인 것이 이 수리의 뼈대다(§3).

### §6 ⚑ 이 작업의 본체는 «방향» 이 아니라 **«방향을 맞히면서 떨지 않기»** 였다

이동 방향으로 돌리는 것 자체는 한 줄이다. 그런데 그 한 줄을 넣자마자 **필드가 깜빡였다** —
잡몹은 서로 밀어내는 분리 힘(`ax += ox/od*w*2.9`)이 붙어 있어 가로 속도가 0 언저리에서 쉬지 않고
부호를 바꾼다. 실측(`scratchpad/t8.js` 꼴, 수리 전 사본과 나란히):

| | 잡몹 뒤집힘(개체당 초당) | 플레이어 뒤집힘(초당) |
|---|---|---|
| 수리 전 | **0.10회** | 2.6~2.9회 |
| 임계 하나만 쓴 1차 안 | **0.96회** (7.4배 · 46마리면 초당 44번) | **4.15회** |
| 히스테리시스를 넣은 최종 | **0.26회** | **2.16회**(수리 전보다 **낮다**) |

⇒ 두 자리 모두 «지금 보고 있는 쪽과 **반대로 뚜렷하게** 갈 때만 돈다» 로 바꿨다.
**새 임계를 만들지 않았다** — 이미 있는 `MOVE_EPS`(= 460 의 run/idle 갈림값 25)를 «돌 이유» 로만 쓴다.
플레이어 쪽 함정은 따로였다: 카이팅 중 `vx` 가 임계를 들락거릴 때마다 «이동 방향(뒤)» 과
«조준 방향(적)» 이 **서로 반대**라 그때마다 홱 돌았다 ⇒ «세로로만 갈 때는 지금 보던 쪽 유지» 를 넣었다.

### §7 ⚑ 자를 세 번 고쳤다 — 셋 다 «제품이 아니라 자» 가 틀린 자리였다

1. **머리 눈금 폐기** — 펫 3종 교정에 1/3 로 떨어져 판정에서 뺐다(§2).
2. **짝을 앞으로** — 제품 코드만 보면 `flip` 은 좌표를 옮긴 **뒤** 정해지니 뒤 차분이 맞아 보인다.
   **실측이 그 짐작을 기각했다**: 이 자의 rAF 콜백이 게임 스텝보다 **먼저** 돌아, 표본 i 의 flip 은
   직전 스텝 값이고 그 값이 미는 것은 i→i+1 구간이다. 뒤 차분으로 재면 단독 좀비가 **24%**(앞 차분 0%).
3. **표본을 깨끗하게** — ⓐ 체력을 안 올리면 플레이어가 곧바로 죽여 표본이 대여섯 개 ·
   ⓑ 코앞에서는 몹이 플레이어를 돌며 계속 방향을 틀어 «전환 창» 을 재게 된다 ·
   ⓒ 되돌려 놓을 때 **플레이어와 같은 높이**에 놓아야 접근이 가로 이동이 된다(대각으로 놓으면
   느린 다크엘프 46px/s 의 x 성분이 임계 아래로 깔려 **표본이 5개**였다).
   ⓓ `e.hp = -1` 로는 **안 죽는다** — 죽음은 step 이 아니라 `hitEnemy()` 안에서만 난다(21509).
   1회차의 «시체 0구» 가 그것이었고, 가끔 1구가 나온 것은 마침 플레이어가 때렸기 때문이다.

### §8 결과 — `node tools/probe587.js` (같은 자로 수리 전/후)

| 자리 | 수리 전 | 수리 후 |
|---|---|---|
| [C1] **고블린 단독 직진** | **98.7%** | **0%** |
| [C2] 좀비 무리(47마리, 곧게 가는 구간) | **10.5%** | 0.2% |
| [G] 조이스틱 오른쪽 / 왼쪽 | **100% / 85.7%** | **0% / 0%** |
| [B] 플레이어(곧게 가는 구간) | 14.7%(+ 정지 중 **상시** 등을 보임) | 0.4% |
| [C1] 다크엘프 단독 · [D] 승급 수호자 · 아레나 | 0% | 0% (불변) |
| [E] 시체 방향 승계 | 1/1 | 1/1 |

**23/23 PASS.** 남은 0.2~0.4% 는 결함이 아니라 **자의 창 폭**이다 — 33ms 창 하나가 게임 스텝 둘을
품는 일이 있고(이 환경 ~38fps), 그때 창 평균 방향과 창 끝 flip 이 갈릴 수 있다. 근거는 단독·직진
표본이 프레임 단위로 **0%** 라는 것이고, 틀린 프레임이 예외 없이 «방향 전환 ±4칸» 안에 있다는 것이다.

### §9 게이트 · 회귀

`tools/verify587.js` **28/28 신설** — 절 여섯:

* **§1 표 = 실측** — 선언이 공격 뻗음 눈금과 같다 · **elves 초록·파랑이 서로 반대**로 선언돼 있다
  (한 값으로 접으면 즉시 빨강) · 아틀라스 **잉크 지문**(잉크 화소 수·중심·크기)이 그대로다
  ⇒ 그림이 바뀌면 빨개져서 «표를 다시 재라» 고 말한다.
* **§2 두 벌 금지** — 옛 하드코딩 두 줄이 없고, `flip` 을 넣는 자리가 **3/3 전부** `faceFlip()` 을 지난다.
  ⚠ 이 절을 처음 짤 때 **주석에 남긴 옛 식**을 세어 스스로 빨개졌다 — 자는 «문자열이 파일에 있나» 가
  아니라 «그 식이 도나» 를 물어야 하므로 주석을 걷어내고 본다.
* **§3 실동작** — 단독 직진 3종 · 무리 · 조이스틱 좌우.
  ⚠ **바를 세 번 다시 잡았다.** 1% 로 조였더니 자가 3회 중 1회씩, 그것도 **매번 다른 행**에서
  빨개졌다 — 느린 다크엘프 5/440(1.1%) · 조이스틱은 표본이 62개뿐이라 **한 프레임이 곧 1.6%** ·
  아레나 5/233(2.1%). 행마다 바를 깎는 것은 두더지잡기라, 대신 **바를 하나로 합치고 4% 에 뒀다**:
  이 자가 갈라야 하는 두 무리가 멀찍이 떨어져 있기 때문이다 — 규칙이 반대인 상태는 **85~100%**,
  맞는 상태는 **0~2.1%**. 4% 는 그 사이고 양쪽으로 여유가 있다. 판별력은 임계가 아니라
  **§R-a 되돌림**(표를 접으면 100%)이 낸다. 조이스틱은 그와 별개로 손가락을 꺾은 직후 0.5초
  (가감속 과도 구간)를 버리고 표집을 4.5초로 늘려 분모를 키웠다. **이 상태로 4회 연속 28/28.**
* **§4 전수** — 승급 수호자·아레나 · 시체 승계 · **펫은 옛 삼항과 항등**(대수로 못박아 경합 제거).
* **§5 깜빡임** — 잡몹·플레이어 뒤집힘 빈도를 **기록만 한다(판정 안 함)**.
  ⚠ **여기서 한 번 틀렸다.** §6 의 결손을 그대로 게이트로 굳히려 했는데, 그 값을 **통과선**으로 쓰려
  하자 회차마다 0.26~0.89회/s 로 흔들렸고 인구를 24마리로 고정한 뒤에도 사본과 **부호가 두 번
  뒤집혔다**(고친 쪽 0.885 ↔ 뺀 쪽 0.852 · 0.621 ↔ 0.36). 히스테리시스가 필요하다는 **근거 자체는
  단단하다** — 수리 직후 같은 스크립트로 두 트리를 나란히 재서 0.13 → 0.96 → 0.26 을 얻었다 — 하지만
  그 한 번의 대조를 «게이트» 로 승격할 만큼 축이 재현성 있지는 않다. 무엇이 값을 흔드는지 모르는 채
  임계를 박으면 **플레이키 게이트가 하나 더 생긴다**(344·372·591 이 그 부류다). ⇒ 판정에서 뺐고,
  축을 제대로 세우는 일을 **598** 로 등재했다.
* **§R 되돌림** — ⓐ 표를 한 값으로 접으면 고블린이 **100%** 로 돌아온다(판별력은 이 항이 낸다) ·
  ⓑ 히스테리시스를 뺀 사본의 뒤집힘도 나란히 **기록**한다(판정 안 함 — 위 §5 와 같은 이유).

**회귀 — 무수정 통과**: `verify66` PASS · `verify123` 62/62 · `verify208` 39/39 · `verify178` 75/75 ·
`verify541` 68/68 · `verify441` 28/28 · `verify358` 65/65 · **SMOKE PASS**.

⚠ **곁다리 등재 593 — `verify460` [E2] 가 빨갛다. 내 작업과 무관함을 직접 대조로 못박았다**(338·344 규칙):
같은 자가 **내 변경이 없는 현재 main(`0e0e4fd`)에서도 똑같이 25/26** 이다. 뿌리는 **580**(이동 속도 ×2,
`SPD_SC`)이다 — E2 는 «속도를 0 으로 두고 세 프레임 굴리면 idle» 을 단언하는데, 그 세 프레임에 AI 가
되돌려 놓는 잔여 속도가 `stat.speed` 에 비례한다: 115일 때 **17.2**(임계 25 아래 = idle) → 230일 때
**34.5**(임계 위 = run). **460 은 잘못한 게 없고 580 도 지시대로 했다** — 상수를 표본에 박아 둔 쪽이 자다.
내 작업 단위가 아니라 고치지 않았다. 상세는 PROGRESS 593 행.
