# 140 — `tools/fnchk97.js` 가 72 의 스프라이트 썸네일 교체 이후 FAIL(19/20)

- **티어**: T1 버그 · **세션**: sess-0220-12104 (워커 B, 2026-08-26) · **1회차에 종료**
- **검증 방식**: 지시서 [3]-**(가) 기계적 작업** — 게이트 판정식만 고치는 작업이라 **비평가를 띄우지 않았다.**
- **범위**: `tools/fnchk97.js` 한 파일. **`index.html` diff 0줄.**

## 1. 재현

```
$ node tools/fnchk97.js
[8] 서브탭 «던전» 복귀 — 던전 카드가 그대로 돌아온다
  ✗ 던전 카드 6장 · 이모지 썸네일 0 · 레이드 캔버스 6
FNCHK97 19/20 FAIL
```

등재(`d55ea4d` 갈래, 워커 D)가 특정한 원인 그대로다 — 72 가 `6efe9e8` 에서
`.dnc>.th>em`(이모지) → `.dnc>.th>canvas.thcv`(스프라이트)로 갈았는데
`fnchk97.js:124~127` 은 **없어진 `<em>` 이 6개일 것**과 **캔버스가 0개일 것**을 동시에 단언하고 있었다.

```js
em: document.querySelectorAll('#dunList .dnc>.th>em').length,
cv: document.querySelectorAll('#dunList canvas.thcv').length }));
ok(dn.n === 6 && dn.rd === 0 && dn.em === 6 && dn.cv === 0, …);
```

**판정이 정확히 뒤집혔다**: 실측은 `em 0 · cv 6`. 나머지 19 항목은 전부 PASS 였다.
`cv` 쪽이 특히 무른 판정이었다 — 셀렉터가 `#dunList canvas.thcv` (전역)이라
«레이드 카드가 안 남았나» 를 물으려던 것이 **던전 카드 자신의 썸네일까지 세고 있었다.**
72 가 던전 카드에 캔버스를 붙인 순간 이 항목은 원리적으로 통과할 수 없다.

## 2. 139 와 같은 원인 · 다른 파일 — 두 번째 층(캔버스 오염)은 이미 막혀 있었다

139 는 `verify90` 에서 판정식을 고쳐도 `--allow-file-access-from-files` 가 없어
`getImageData` 가 `SecurityError` 로 막히는 두 번째 층을 만났다.
`fnchk97` 은 [2] 항목이 이미 잉크를 읽고 있어서 **`launch` 에 그 플래그를 처음부터 주고 있었다**(`fnchk97.js:16`).
그래서 이번에 고칠 것은 **판정식 한 곳뿐**이다. 플래그 유무 대조로 확인:

| launch args | `#dunList .dnc` 6장의 썸네일 잉크 |
|---|---|
| (없음) | `SecurityError` × 6 → 읽기 불가 |
| `--allow-file-access-from-files` (현행) | 16349 / 24141 / 35569 / 30845 / 29944 / 35479 px |

즉 플래그가 빠지면 이 항목은 **헛통과가 아니라 FAIL** 로 떨어진다(오염 = 못 읽음 = 통과 아님).

## 3. 고친 것 (1곳, `tools/fnchk97.js:121~140`)

판정을 «마크업 모양»(이모지 6 · 캔버스 0)이 아니라 **«칸이 채워졌나»** 로 좁혔다 —
139 가 `verify90` 에, 72 가 `verify72` §1-2 에 쓴 것과 같은 기준이다:
`.th>canvas.thcv` 가 있고, 알파>8 픽셀이 하나라도 있으면 채워진 것. `getImageData` 가 던지면 통과시키지 않는다.

```js
ok(dn.n === 6 && dn.rd === 0 && dn.drawn === 6,
   `던전 카드 ${dn.n}장 · 레이드 카드 ${dn.rd}장 · 썸네일(.th>canvas.thcv) ${dn.drawn}장 실제로 그려짐`);
```

**«레이드 캔버스 0» 은 버린 것이 아니라 옮긴 것이다** — 서브탭 복귀 후 레이드가 안 남았음은
`.dnc.rd` 0장(`dn.rd`)이 이미 담보한다. 전역 `canvas.thcv` 개수는 그 사실을 대신 물을 수 없는
(그리고 72 이후로는 물을 수 없게 된) 대리 지표였다.

[8] 이 원래 보려던 것 — «레이드 갔다 돌아오면 던전 카드가 **그대로** 돌아온다» — 은 셋 다 유지된다:
카드 수 6 · 레이드 잔존 0 · **썸네일이 재렌더에서 죽지 않음**(옛 판정은 마지막 항목을 마크업 모양으로만 대신 보고 있었다).

## 4. 결과 · 게이트

| 게이트 | 결과 |
|---|---|
| `node tools/fnchk97.js` | **20/20 PASS** |
| `node tools/verify90.js` | **91/91 PASS** (139 가 고친 형제 게이트 — 무회귀) |
| `node tools/verify72.js` | **165/165 PASS** (같은 것을 세는 원본 게이트 — 무회귀) |
| `node tools/smoke.js` | **SMOKE PASS** |
| `index.html` | **무변경** |

## 5. 음성 대조 — 새 판정이 헛통과가 아님을 확인

같은 페이지에서 던전 카드 썸네일 캔버스만 건드려 판정값을 다시 읽었다(`scratchpad/neg140.js`):

| 상태 | 실측 `drawn` |
|---|---|
| 그대로 | **6** |
| 캔버스 `clearRect` (칸은 있는데 그림이 없음) | **0** |
| 캔버스 제거 | **0** |

«캔버스가 있다» 가 아니라 «그려져 있다» 를 실제로 잡는다.

## 6. 남은 것 · 손대지 않은 것

139 §7 의 사각지대 표를 다시 훑었다(`grep -rn '\.th *>\? *em\|th em' tools/`):

| 파일 | 상태 |
|---|---|
| `tools/verify90.js` | 139 가 고침 (91/91) |
| `tools/fnchk97.js` | **이번에 고침 (20/20)** |
| `tools/verify72.js:300` | `em: !!c.querySelector('.th>em')` — «이모지가 **아님**» 을 확인하는 쪽이라 정상 |
| `tools/verify121.js` | `':scope>.th>em, :scope>.th>canvas'` 로 둘 다 받는다 — 무해 |
| `tools/cap72.js:68` · `tools/probe121.js` | 게이트가 아니라 캡처·진단 보조 도구. **72·121 lock 생존 중이라 손대지 않았다.**<br>`cap72.js:68` 은 `.th>em` 을 찍어 빈 rect 가 나오지만 캡처 보조라 게이트를 깨지 않는다 — 72 구간에서 처리할 일. |

## 7. 병렬 충돌 — 72 워커가 같은 시각 곁가지로 같은 자리를 고쳤다 (양쪽 다 살림)

push 직전 rebase 에서 `tools/fnchk97.js` [8] 이 충돌했다. 72 워커가 `343ff0c` 로
같은 자리를 **«이모지 0 · 캔버스 6»** 으로 옮겨 놓았다(자기 변경이 이 게이트를 깨서 곁가지로 처리).

지시서 [4] «양쪽 변경을 모두 살린다» 대로 **두 단언을 합쳤다** — 잃은 것 없음:

| 단언 | 출처 | 남았나 |
|---|---|---|
| `dn.n === 6` (카드 6장) | 원본 | ✓ |
| `dn.rd === 0` (레이드 잔존 0) | 원본 | ✓ |
| `dn.em === 0` (이모지 0) | **72 곁가지** | ✓ 그대로 |
| `dn.drawn === 6` (썸네일이 **실제로 그려짐**) | **140(이번)** | ✓ 추가 |
| ~~`dn.cv === 0`~~ (전역 캔버스 0) | 원본 | 폐기 — 72 이후 원리적으로 통과 불가인 대리 지표 |

**두 수정은 겹치지만 같지 않다.** 72 의 «캔버스 6» 은 «칸이 **있다**» 이고,
`verify72` [5-2] 가 이미 같은 장면에서 같은 것을 단언한다(`back.every(c => c.th && !c.rd && c.cv && !c.em)`).
이번에 더한 «알파>8 픽셀 ≥ 1» 은 «칸이 **그려졌다**» 로, **두 게이트 어느 쪽도 아직 안 보던 것**이다 —
[8] 이 원래 보려던 «재렌더 뒤에도 썸네일이 죽지 않았나» 는 여기에 있다.
음성 대조(§5)가 그 차이를 보여준다: `clearRect` 하면 캔버스는 6개 그대로인데 `drawn` 만 0 이 된다.

합친 뒤 재실행: **FNCHK97 20/20 · VERIFY72 165/165 · VERIFY90 91/91 · SMOKE PASS.**

**«사라진 `<em>` 을 아직 세는» 게이트는 이제 없다.** 두 파일 모두 «칸이 채워졌나» 만 묻는 판정이라
72 가 아트를 또 바꿔도 다시 뒤집히지 않는다.
