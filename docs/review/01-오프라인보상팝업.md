# 01 오프라인보상 팝업 — 비평 기록

- 작업 세션: `sess-2321-27908` (2026-08-23)
- 측정표: `docs/measure/01-오프라인보상팝업.md` (이번 회차에 신규 측정)
- 레퍼런스: `docs/ref/01-오프라인보상-팝업.jpg` (1080x2340)
- 캡처: 1080x1920 헤드리스(playwright, chromium-1194)

## 무엇을 만들었나

기존 구현은 공용 `popup()` 에 줄글을 넣은 텍스트 안내(`🌙 방치 보상 / 자리를 비운 N시간 M분 동안…`)였다.
레퍼런스대로 **전용 오버레이 `#offw`** 를 새로 짰다.

- 모달 아래 **모달 바깥에 상품 배너**가 붙는 구조라 공용 `#modal` 껍데기로는 표현이 안 된다.
  → `#offw` 안에 `1080x972` 스테이지를 두고, 레퍼런스 절대좌표에서 그룹 상단(y=772)만 뺀 값으로 전 요소를 절대 배치했다.
- 병렬로 A5(모달 규격)가 `.modal`/`.mbox`/`.mhead`/`.mbody` 를 고치는 중이라 **공용 모달 CSS는 일절 건드리지 않았다.**
- 보상 지급 방식도 바꿨다: 기존은 팝업 뜨기 전에 이미 지급됐는데, 이제 `받기`/`1.5배 받기` 로 **수령**해야 들어온다.

## 1회차 — 최저 4/10

비평가 A 채점: ① 5 ② 5 ③ 4 ④ 4 ⑤ 6 ⑥ 4 → **최저 4**

지적 중 **실제로 재현된 치명적 결함 1개**:

> ④ 정렬·그리드 — 모든 텍스트에서 어절 공백이 삭제됨: '오프라인 보상'→'오프라인보상',
> '자리를 비우신 동안 사냥했어요!'→'자리를비우신동안사냥했어요!', **'1.5배 받기'→'15배받기'(소수점까지 소실)**

원인을 추적했다. 측정표 부록 B가 `font-size ≈ 잉크높이 ÷ 0.75` 로 추정했는데,
**이 프로젝트가 쓰는 폰트의 실제 잉크비는 0.93** 이었다(헤드리스에서 `measureText` 로 실측).
그래서 font-size 가 15~25% 과대해졌고, 폭을 실측값에 맞추려고 넣은 음수 `letter-spacing`(-9 ~ -18px)이
**공백 글리프와 마침표의 advance 를 통째로 먹었다.**

비평가 A의 나머지 절대 px 지적(모달↔배너 간격 +25px 등)은 **채택하지 않았다.**
비평가가 레퍼런스를 923x2000 으로 축소해 읽고 1.17 을 곱해 환산했는데, 캡처는 다른 배율로 읽어서
두 이미지의 축척이 어긋났다. 헤드리스 `getBoundingClientRect` 실측으로는 모달 하단→배너 상단이
17px(실측 목표 18px)로 이미 맞다. → 2회차부터는 비평가에게 **"모달 폭을 1.0 으로 놓고 상대 비율로 비교하라"** 고 지시를 바꿨다.

## 2회차 — 폰트 산정 방식 교체

- `letter-spacing` **전면 제거**. font-size 는 `실측 잉크높이 ÷ 0.93` 으로 다시 잡고,
  가로 폭은 텍스트를 감싼 `<i>` 의 **`transform: scaleX()`** 로 눌러 맞췄다.
- 세로는 **실측 잉크 중심**에 줄상자 중심을 맞추는 방식으로 통일(`height:60;line-height:60;top = 잉크중심 - 30`).
- 타이틀바가 갈색 안쪽 테두리 위를 덮어 검정 테두리 안쪽 폭을 꽉 채우도록 고침(실측 x58~1021).

헤드리스 실측 대조 결과 **텍스트 13개 전부 실측 bbox 와 오차 1px 이내**:

| 텍스트 | 실측 x / w / 잉크중심y | 구현 x / w / 중심y |
|---|---|---|
| 오프라인 보상 | 404 / 273 / 54 | 403 / 273 / 54 |
| 최대 6시간 0분 | 525 / 161 / 137.5 | 524 / 161 / 137 |
| 0시간 6분 | 484 / 112 / 177.5 | 485 / 112 / 178 |
| 자리를 비우신 동안 사냥했어요! | 307 / 467 / 263.5 | 306 / 467 / 263 |
| 6.49A | 492 / 97 / 423 | 491 / 97 / 423 |
| 받기 | 330 / 69 / 535.5 | 330 / 69 / 536 |
| 1.5배 받기 | 601 / 176 / 535.5 | 602 / 176 / 536 |
| 오프라인 보상 최대 시간 | 472 / 423 / 721.5 | 471 / 423 / 721 |
| 4시간 증가 | 704 / 260 / 793 | 703 / 260 / 793 |
| 이동 | 762 / 81 / 889.5 | 763 / 81 / 890 |
| 구매 즉시 반영! | 209 / 164 / 935.5 | 208 / 164 / 935 |

(y 는 스테이지 좌표 = 레퍼런스 y − 772)

박스류도 헤드리스 실측이 측정표와 일치한다:
모달 978x657 / 알약 280x50(좌변 411) / 보상박스 850x150 / 프레임 126x116 /
버튼 249x148 · 300x148(gap 51) / 배너 948x298 / 모달하단→배너상단 17px.

콘솔 에러 0건.

## 3회차 — 배너 일러스트 좌표 오류 + 그라데이션 글자

비평가 A 2회차: ① 6 ② 5 ③ 5 ④ 5 ⑤ 4 ⑥ 7 → **최저 4**

재현된 결함 2건만 채택해 고쳤다.

1. **배너 일러스트 y 좌표 산술 오류.** 실측 1457 → 스테이지 685 여야 하는데 **783** 으로 적었다.
   그 결과 일러스트가 배너 하단을 **87px 뚫고 나가** `4시간 증가` 와 `구매 즉시 반영!` 라벨을 덮고 있었다.
   비평가 3명이 전부 이걸 지적했다(“배너 아트 미클립”). `.ofr-bn` 안으로 옮겨 `overflow:hidden` 으로
   배너 라운드에 잘리게 했다. → 헤드리스 실측 78,685 로 측정표와 일치.
2. **`4시간 증가` 가 통째로 검정으로 렌더됐다.**
   `background-clip:text` 와 `-webkit-text-stroke` 는 같이 못 쓴다 — 스트로크가 그라데이션을 덮는다.
   외곽선 레이어(`.ofr-t2.s`)를 먼저 깔고 그 위에 그라데이션을 얹되, **그라데이션은 글자를 직접 담은
   `<i>` 에 걸어야 한다**(부모 div 에 걸면 안 나온다). 픽셀 실측으로 (255,233,164)→(254,208,118) 확인.

## 4회차 — 명암·질감

비평가 A 4회차 **최저 5**(2회차 4 → 5), 비평가 C 2회차 **최저 4**.
비평가 C 는 PIL 픽셀 실측을 근거로 제시해 신뢰도가 높아 그 지적 위주로 채택했다.

- 모달 바탕 패턴을 **점 → 5각별**로 교체. A5 가 공용 모달(`.mbody`)에 쓴 것과 **같은 SVG 타일**을 썼다.
- 타이틀바에 **상단 하이라이트 밴드**(`#5B4643` 9px) 추가. 기존엔 `#52403D` 단색 평면이었다.
- 버튼 3개 본체를 **평면 단색 → 세로 그라데이션**으로. 기존엔 상/하 8px 띠만 있고 가운데가 평면이었다.
  실측 근거: 초록 `#77EE44`→`#76DB27`, 파랑 `#35CFE7`→`#19B2D4`.
- 초록 아이콘 프레임 세로 **116 → 128**. 측정표는 116(상/하 여백 12/22)이라 했지만
  비평가 C 의 PIL 실측은 128(여백 12/11, 종횡비 0.98)이었다. 보상박스 세로 중심에 맞는 쪽은 후자라
  **측정표보다 비평가 실측을 택했다.** (측정표에 반영 필요)
- 배너 티켓 노치 깊이 17 → **20**(측정표 값).

## 채택하지 않은 지적과 그 이유

비평가들이 **레퍼런스와 캡처를 서로 다른 배율로 축소해 읽어** 절대 px 지적이 자주 어긋났다.
(1회차 비평가 A 는 레퍼런스를 923x2000 으로 읽고 1.17 을 곱해 환산했다.)

- “받기 버튼이 30px 높다 / 이동 버튼이 29px 높다 / 라벨이 58px 넓다” → 헤드리스
  `getBoundingClientRect` 실측으로 249x148 · 299x118 · 350x46 이며 **측정표와 정확히 일치**한다.
- “버튼에 베벨이 없다” → 픽셀 실측 `#ABFF7C`/`#77E536`/`#40BC2A` 로 **띠는 있었다.**
  다만 가운데가 평면인 건 맞아서 4회차에 그라데이션을 넣었다.
- “AD 뱃지가 65x54 여야 하는데 71x62 다” → `rotate(-8deg)` 의 **회전 후 bounding box** 를 잰 것이다.
  회전 전 실제 크기는 65x54 로 정확하다.
- “시계가 알약에 겹치면 안 된다” → 측정표는 반대로 **“알약 좌변 411보다 55px 왼쪽으로 튀어나감”** 이다.

→ 2회차부터 비평가에게 **“모달 폭을 1.0 으로 정규화한 상대 비율로만 비교하라”** 고 지시를 바꿨고,
   그 뒤 지적의 정확도가 올라갔다(4회차 A 는 배치·정렬을 7점으로 올렸다).

## 남은 문제 (통과 기준 9 미달)

1. **아이콘·일러스트가 전부 이모지다.** A1 과 같은 한계다.
   - 배너의 개구리 캐릭터: 레퍼런스는 항아리·지팡이를 든 **전신 일러스트 490x250**(가로가 세로의 2배).
     이모지는 가로세로가 같이 커져서 이 비율을 못 만든다. 현재 250px 정사각 근사.
   - 배너 안 **금화 3개**(지름 95/90/80)는 아예 못 넣었다.
   - 스톱워치·골드 코인도 이모지 근사(코인은 76px 로 크기만 맞춤).
   → **아트 리소스가 들어와야 ②상대 크기 비율·⑤색·명암이 9점에 도달한다.**
2. **모달 배경 별무늬가 점(dot) 패턴**이다. 레퍼런스는 5각 별. A5 가 공용 모달에 5각별을 넣었으므로
   그 방식이 확정되면 여기도 맞추면 된다(공용 CSS 충돌을 피하려 이번엔 안 건드렸다).
3. **`4시간 증가` 그라데이션 글자**는 `background-clip:text` + `-webkit-text-stroke` 조합이라
   브라우저에 따라 외곽선이 그라데이션 위로 덮일 수 있다. 실기기 확인 필요.
4. **금화 3개**(배너 안, 지름 95/90/80)를 못 넣었다. 캐릭터 아트가 들어올 때 같이 넣어야 한다.
5. 비평가 최고 점수는 **5/10**(4회차 A)이다. 통과 기준 9 미달이며, 남은 감점은 대부분
   ①배너 아트 부재 ②이모지 대체 ③레퍼런스 게임 서체 부재 세 가지로 수렴한다.
   **박스·좌표·폰트 크기는 헤드리스 실측으로 측정표와 1px 이내로 일치**하므로,
   아트 리소스 없이 더 올릴 여지는 크지 않다고 판단해 4회차에서 종료했다.

## 5회차 — 버튼 내부 밝은 림

4회차 두 번째 비평가(C계열, 픽셀 마스크·컬럼 스캔) **최저 5** — ① 8 ② 7 ③ 9 ④ 9 ⑤ 5 ⑥ 8.
배치·여백·정렬·폰트가 8~9점까지 올라왔고, 감점은 ⑤ 색·명암에 몰려 있다.
이 비평가가 4회차 수정 **이전** 캡처를 봤는데도 지적한 '버튼 본체 평면'은 이미 고친 뒤였고,
아직 안 고친 항목 1개만 채택했다.

- 3개 버튼 모두 **하단 어두운 띠 바로 위에 내부 밝은 림 8px** 추가
  (초록 `#ABFF7C`, 파랑 `#52E7FD`). 이동 버튼은 상단 밝은 띠도 6 → 8px.
- 픽셀 단면 검증: 받기 버튼 y480 `#77EE44`(본체 상단) → y594 `#ABFF7C`(내부 림) → y600 `#40BC2A`(하단 띠).

## 미해결 — 측정표와 비평가 실측이 엇갈리는 2건

다음 세션이 재측정해서 확정할 것. **이번엔 측정표를 따랐다.**

1. **배너 좌측 일러스트 패널 색.** 측정표 `#91DEBE (145,222,190)` vs 비평가 `(46,164,116)`.
   비평가는 "명도가 +24 뜬다"고 했지만, 같은 비평가가 배너 우측 `(57,194,140)` 은 정확히 일치한다고 했다.
   좌측만 틀렸다면 측정표 쪽이 잘못 읽었을 가능성이 있다.
2. **시계와 알약의 겹침량.** 측정표는 시계 bbox 356~419 · 알약 좌변 411 → **겹침 8px**.
   비평가 2명이 독립적으로 **24~25px** 이라고 했다(서로 다른 회차, 서로 다른 캡처).
   2:1 로 비평가 쪽이 많지만 측정표가 원본 픽셀 직접 스캔이라 이번엔 측정표를 유지했다.
   겹침을 25px 로 바꾸려면 시계를 좌측으로 약 17px 옮기고 지름을 61 → 69 로 키워야 한다.

## 최종 헤드리스 실측 (측정표 대조)

| 요소 | 측정표 | 구현 |
|---|---|---|
| 모달 | 51, 0, 978x657 | 51, 0, 978x657 |
| 타이틀바 | 58, 7, 963x90 | 58, 7, 964x90 |
| 시간 알약 | 411, 153, 280x50 | 411, 153, 280x50 |
| 시계 | 356, 133, 64x73 | 356, 133, 64x73 |
| 보상 박스 | 115, 288, 850x150 | 115, 288, 850x150 |
| 초록 프레임 | 477, 300, 126x128\* | 477, 300, 126x128 |
| 받기 | 240, 464, 249x148 | 240, 464, 249x148 |
| 1.5배 받기 | 540, 464, 300x148 | 540, 464, 300x148 |
| 배너 | 66, 674, 948x298 | 66, 674, 948x298 |
| 배너 아트 | 78, 685, 605x276 | 78, 685, 605x276 |
| 이동 | 654, 834, 299x118 | 654, 834, 299x118 |
| 라벨 | 115, 912, 350x46 | 115, 912, 350x46 |

\* 프레임 세로만 비평가 C 의 PIL 실측(128)을 따랐다. 콘솔 에러 0건.

---

## 5회차 (2026-08-25, sess-0900-6158 워커 D) — 2차 폴리시 라운드 · 캡처 `docs/review/01-r5.png` → `01-r6.png`

**기준이 9:16(1080×1920) → 9:19(1080×2280) 로 바뀐 뒤 첫 회차다.** 캡처 하네스 `tools/cap01.js`(신설),
같은 창·같은 임계값으로 ref/캡처를 대조하는 스캐너 `tools/scan01.py`(신설). 좌표는 `.ofrs`(1080×972)
스테이지가 ref 원점 y772 이므로 **ref y = stage y + 772**, 프레임 y = stage y + 688.

### 채점 (r5 기준)

| 비평가 | ① | ② | ③ | ④ | ⑤ | ⑥ | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| G | 8 | 8 | 9 | 9 | 8 | 6 | **8** |
| H | 7 | 6 | 7 | 7 | 9 | 6 | **7** |

→ 최저 **7** (8 미달) 이므로 수정 후 6회차로 넘긴다.

### ★ 이번 회차의 핵심 — 두 비평가가 **같은 오진**을 했다

G·H 가 독립적으로 «「자리를 비우신 동안 사냥했어요!」·「4시간 증가」·버튼 라벨의 **잉크 높이가 −12~18%**
→ **font-size 를 +14~18% 올려라**» 라고 했다. **그대로 했으면 화면이 멀어졌다.**

같은 창·같은 임계값으로 **흰 코어**와 **검정 포함**을 따로 재니 원인이 갈렸다:

| 요소 | 흰 코어 높이 REF/CAP | 검정 포함 높이 REF/CAP | 검정 한쪽 REF/CAP |
|---|---|---|---|
| 타이틀 | 45 / **45** | 59 / 51 | 7.0 / **3.0** |
| 안내 문구 | 32 / **32** | 42 / 35 | 5.0 / **1.5** |
| 받기 | 36 / **36** | 46 / 40 | 5.0 / **2.0** |
| 1.5배 받기 | 36 / **36** | 46 / 40 | 5.0 / **2.0** |
| 이동 | 46 / **45** | 57 / 50 | 5.5 / **2.5** |

**흰 코어(=글리프 그 자체)는 전부 1px 이내로 이미 일치**한다. 어긋난 것은 **검정 외곽선 두께 하나**이고,
한쪽 3~4px 씩 얇았다. font-size 를 올렸다면 이미 맞던 흰 코어가 통째로 +14~18% 오버슈트했을 것이다.
→ **원인은 하나(외곽선), 고치는 곳도 하나.** `-webkit-text-stroke` 를 A5 교훈대로 «한쪽 N px = 2N px» 로 올렸다.

### 수정 목록

| # | 대상 | 전 → 후 | 근거 |
|---|---|---|---|
| 1 | 타이틀 외곽선 | `5px` → **`11px`** | 검정 한쪽 3.0 → **6.0** (ref 7.0) |
| 2 | 안내 문구 외곽선 | `4px` → **`10px`** | 한쪽 1.5 → **4.5** (ref 5.0). `.ofrs .ot`(0,2,0) 가 4px 로 잡고 있어 `.ofr-lead`(0,1,0) 로는 못 이겨 **`.ofrs .ofr-lead`** 로 특정도를 맞췄다 |
| 3 | 버튼 라벨 외곽선 | `4px` → **`8px`** | 한쪽 2.0 → **4.0** (ref 5.0) |
| 4 | 「이동」 외곽선 | `5px` → **`9px`** | 한쪽 2.5 → **4.5** (ref 5.5) |
| 5 | 「받기」 잉크 폭 | `scaleX(.885)` → **`1.018`** | 흰 코어 폭 60 → **70** = ref 70, **Δ0.0%** |
| 6 | 「1.5배 받기」 폭 | `scaleX(.967)` → **`1.019`** | 167 → **177** (ref 176, +0.6%) |
| 7 | 「이동」 폭 | `scaleX(.827)` → **`.904`** | 75 → **81** (ref 82, −1.2%) |
| 8 | 라벨 3개 세로 | `translateY -2/-2/-3` → **`-4/-4/-5.5`** | 자체 스캔 Δcy +2.0/+2.0/+2.5 |
| 9 | **코인 🪙** | `font-size 55` → **`72`** | 행 단면 실측: ref 코인 **x503..576(74~76)**, 우리 **58** = **−22%**. 수정 후 **76 = 76 정확 일치** |
| 10 | **시계 ⏱️** | `left356 w64 h73 fs62` → **`left330 w120 h81 fs72`** | ref 잉크 x357..424 y900..979. 우리 잉크 좌단 **368 → 356**(ref 357), 높이 **68 → 79**(ref 80). 박스를 글리프 advance 보다 **넓게**(65→120) 잡아야 `text-align:center` 가 먹는다 — A1 교훈 재현 |
| 11 | **AD 뱃지** | `528,567 65×54` → **`529,570 60×48`** | 회전(−8°) 후 오렌지 bbox ref 56×46, 우리 61×52 → 역산해 비회전 52×40+테두리4. 수정 후 **56×46 = 56×46 정확 일치** |
| 12 | **티켓 노치 3개** | 칠한 원 → **`mask` 실제 절개** | 칠하면 배너 초록 위에 얹혀 (14,48,35) 로 보이고 뒤가 안 비친다. 중심도 **6px 낮았다**(ref 1519.5/1592.5/1665). 반경은 ref 절개 프로파일 역산(최심 22 · \|t\|=20 에서 7) → **r21 · 우변 안쪽 1px**. 밴드 35/35/36 vs ref 34/34/35 |
| 13 | 배너 헤드라인 폭 | `scaleX(1)` `w894` → **`.9785`** `w896` | 흰 코어 419 → **411**(ref 410, +0.2%), 중심 Δ −6.5 → **−0.5** |

### 반증하고 **미채택**한 지적

| 지적 | 반증 근거 |
|---|---|
| **H [2][3] «시계↔알약 겹침이 30px 이고 알약은 x391~689(299 폭) 다»** (측정표 411/280 을 뒤집으라는 요구) | **알약 좌변은 시계에 완전히 가려져 픽셀로는 직접 못 잰다.** H 는 «텍스트가 알약 중앙» 을 가정해 391 을 역산했는데, 측정표 §4-2 는 «텍스트는 **화면 중심 540** 기준이고 알약 자체 중심(550)과 다르다» 를 이미 명시했다. H 가 근거로 든 y973 의 검정(x399~402)은 **시계 바닥 외곽선**이다(y972~976 에서 x380~429 가 통째로 검정 = 알약 캡이 아니다). **결정적 반증**: y958 행에서 x400..404 가 **모달 베이지 배경**(240,217,186)이다 — 알약이 391 까지 왔다면 그 자리는 알약 내부여야 한다. 같은 행에서 검정 5px(x415~419) 뒤 채움(x420~)이 오고, r≈15 코너 보정(+2.6)을 빼면 좌변 ≈**412** 로 측정표의 411 과 일치. **측정표 유지.** (LESSONS 04-② «가려진 도형은 전체 bbox 로 재지 마라» 의 재현) |
| G [7] 버튼 테두리 8→7px · 알약 5→4px | 단일 비평가 + **두께는 JPEG AA 오독이 절반**(LESSONS 70-④). G 자신의 신뢰도 체크가 받기 버튼 바깥을 **249×148 = 우리와 동일**로 읽었다 — 테두리가 1px 얇다면 바깥 bbox 가 같을 수 없다. 미채택 |
| G [9] 모달↔배너 간격 17→15 · 배너 높이 298→300 | G 자신이 «모달 하단→배너 하단 총합은 REF 315 / 우리 315 로 **동일**» 이라고 적었다 = 순수 AA 배분 차이. 미채택 |
| G [8] 배너 아트 분할선을 «x≈545 수직» 으로 | 측정표 §10-2 는 ref 우측 경계가 **대각선**(y1470→647 / y1590→680 / y1695→628)이라고 실측해 뒀다. G 가 잰 것은 개구리 이모지 실루엣 경계로 보인다. 미채택 |
| H [4] 배너 캐릭터 일러스트 bbox −29% | 아트 대기 — 이모지로 382×230 을 자연스럽게 채울 수 없다(LESSONS 72-③). ⑤ 계열이라 통과 비차단 |

### 검증

- `node tools/cap01.js` 콘솔 에러 **0건**
- `python3 tools/scan01.py docs/review/01-r6.png` — 「받기」 **Δ0.0%**, 1.5배 +0.6%, 이동 −1.2%,
  시계 +1.9%/−1.2%, 코인 76=76, AD 56×46=56×46, 배너 헤드라인 +0.2%, 타이틀 −1.1%, 안내 문구 −1.3%
- `node tools/smoke.js` → **SMOKE PASS**

### 6회차로 넘기는 것

- 재채점 필요(이번 수정은 비평가 재채점 전이다). ①~④ 8 미달이면 계속.
- 남은 ⑤⑥ 은 «아트 대기» — 배너 캐릭터(382×230), 개구리/코인/시계 이모지의 종횡비 한계.
