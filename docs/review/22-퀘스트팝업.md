# 22 퀘스트 팝업 — 비평 기록

- 측정표: `docs/measure/22-퀘스트팝업.md` (2026-08-24 PIL 실측, **재측정 금지**)
- 레퍼런스: `docs/ref/22-퀘스트-팝업.jpg` (1080×2340, 상단 84px 상태바)
- 캡처: `node tools/cap22.js` → `docs/review/22-r{n}.png` (**1080×2280**)
- 회귀 게이트: `node tools/verify22.js` (기하 45항목 + 기능 14항목 + 화면비 5종)
- 구현 구간: `index.html` — CSS `.q22`/`.qs-*` 블록 · JS `openQuest()` · 데이터 `QUESTS`/`DQUESTS`

## 인계 상황 (2026-08-25, sess-0313-29422 워커 A)

`docs/claims/22.lock` 이 **2026-08-24T07:58 로 28시간 묵어** 죽은 lock 이었다(50분 규칙). 뺏어서 이어받았다.
`docs/review/22-*.md` 가 없어 T3(한 번도 구현 안 된 화면)로 잡았으나, **실제로는 구현이 이미 올라와 있었다**
(21 과 같은 상황 — 앞 세션이 구현만 하고 기록·PROGRESS 갱신 전에 죽었다). 그래서 **1회차는 «인계 확인»**
으로 쓰고, 회차는 1부터 센다(앞 세션의 비평 기록이 없어 이어 셀 근거가 없다).

## 1회차 (2026-08-25, sess-0313-29422)

### 한 일

1. **채점용 캡처 도구 신설** `tools/cap22.js` — 레퍼런스와 **같은 상태**로 맞춘다(LESSONS 04-①):
   탭 «반복» · 5행 전부 진행 중(회색 비활성) · [모두 받기] 비활성. 상태가 다르면 스스로 죽는다.
   캔버스는 캡처 직전 `visibility:hidden`(28-③), `window.step` 을 비워 자동 전투를 멈춘다(58-②).
2. **회귀 게이트 신설** `tools/verify22.js` — 프레임 절대 px 로 대조. `--probe` 로 좌표 덤프.
3. **smoke 오프너 추가** — 22 팝업 하단 토글(`qtab:daily` · `qtab:rep`)을 2단계 오프너로 등록.
   «반복» 경로는 일일로 갔다가 되돌아오는 왕복까지 본다(ROUTINE [6] «새 탭을 만들면 smoke 에 추가»).

### 기하 실측 — 측정표 대조 (전 항목 Δ≤2px)

| 항목 | 목표(ref−84) | 실측 | Δ |
|---|---|---|---|
| 모달 박스 x / width | 91 / 898 | 91 / 898 | 0 / 0 |
| 모달 박스 top / height (검정 코어) | 380 / 1497 | 379.5 / 1497 | −0.5 / 0 |
| 타이틀바 top / height | 387 / 91 | 387.5 / 91 | +0.5 / 0 |
| 본문 top | 479 | 478.5 | −0.5 |
| 본문 패딩박스 좌/우단 | 111 / 968 | 109 / 971 | −2 / +3 |
| 리스트 패널 x/y/w/h | 118 / 544 / 844 / 999 | 118 / 543.5 / 844 / 999 | ≤0.5 |
| 1행 x/y/w/h | 131 / 577 / 818 / 179 | 131 / 576.5 / 818 / 179 | ≤0.5 |
| 행 pitch / gap | 200 / 21 | 200 / 21 | 0 / 0 |
| 보상 프레임 x/y/w/h | 167 / 612 / 106 / 105 | 167 / 611.5 / 106 / 105 | ≤0.5 |
| 진행바 x/y/w/h | 299 / 678 / 402 / 38 | 299 / 677.5 / 402 / 38 | ≤0.5 |
| [보상 받기] x/y/w/h | 722 / 608 / 195 / 118 | 722 / 607.5 / 195 / 118 | ≤0.5 |
| [모두 받기] x/y/w/h | 404 / 1563 / 272 / 130 | 404 / 1562.5 / 272 / 130 | ≤0.5 |
| 토글 바 x/y/w/h | 151 / 1717 / 797 / 99 | 151 / 1716.5 / 797 / 99 | ≤0.5 |
| 선택 알약 x / width | 522 / 425 | 522 / 425 | 0 / 0 |
| 패널→모두받기 / 모두받기→토글 간격 | 21 / 25 | 21 / 25 | 0 / 0 |
| 5행 하단 잘림(스크롤 어포던스) | ~13 | 13 | 0 |

> **주의 — «본문 패딩박스» 의 −2/+3 은 A5 공용 껍데기 소속이다.** `.mbody` 는 갈색 프레임을 자기
> `border` 로 그리므로 **border box 를 측정표의 «콘텐츠 영역 111..968» 과 대조하면 안 된다**(43 교훈 1).
> 처음 쓴 assert 가 그 오류로 3건 FAIL 을 냈고, 틀린 것은 구현이 아니라 assert 였다.

### 기능 체크 (헤드리스 실동작 — `verify22.js` §2)

| 조작 | 기대 | 결과 |
|---|---|---|
| 사이드 «퀘스트» 클릭 | 팝업 열림 · 반복 탭 5행 | PASS |
| 기본 선택 탭 | «반복»(레퍼런스와 동일) | PASS |
| 토글 «일일» | 일일 퀘스트 5행으로 교체 · 선택 알약 좌측(-5px) | PASS |
| 토글 «반복» 복귀 | 반복 5행 복귀 | PASS |
| 완료 퀘스트 [보상 받기] | 버튼 활성화 | PASS |
| [보상 받기] 클릭 | 다이아 증가 | PASS |
| 〃 | 퀘스트 단계 +1 (다음 목표로 갱신) | PASS |
| 〃 | 진행 기준선 재설정(`base`) | PASS |
| 〃 | `localStorage` 에 저장 반영 | PASS |
| 비활성 [보상 받기] 클릭 | 변화 0 | PASS |
| 사이드 아이콘 뱃지 | 요소 존재(`sideAlert('quest', …)`) | PASS |
| 행 자식 잉크 | 행 밖으로 안 나감(46-① · 61-③) | PASS |

**`node tools/verify22.js` → VERIFY22 PASS 64/64** · **`node tools/smoke.js` → SMOKE PASS**

### 우리 설계와 레퍼런스가 다른 점 (감점 사유 아님 — 비평 브리핑에 명시)

- 레퍼런스는 캐릭터 2명(햄지·매옹이) 구조라 «햄지 공격력 훈련 / 매옹이 공격력 훈련» 이 갈려 있다.
  우리 게임은 **«용사» 1명**(UI-REFERENCE 설계확정 §1)이라 반복 퀘스트를
  `아무거나 소환 / 아무 강화 / 적 처치 / 스테이지 도달 / 도감 수집` 5종으로 잡았다.
- 보상 아이콘은 아트 대기 중이라 이모지 💎 로 bbox(106×105)만 정확히 채워 둔다.

---

## 2회차 (2026-08-25) — 비평 P(1회차 캡처 `22-r1.png`) + 자체 픽셀 검산

### 비평가 P 점수
| ① 배치 | ② 크기 비율 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|
| 8 | **6** | 8 | 8 | 9 | 7 | **6** |

P 의 1~3순위 지적은 «보상 프레임 세로가 15px 부족 / 수량 숫자가 프레임과 안 겹침 / 프레임 중심이 10px 위» 였다.
**전부 «가림 오독» 이다**(04 교훈 2 재현) — 수량 숫자가 프레임 하단 20px 를 덮고 있어 프레임의 검정 bbox 를
그대로 재면 짧아 보인다. 텍스트에 안 가리는 열(x171·175·179·266·270)로 다시 재니 **세로 연속 높이가
ref 76/87/93/82/68 vs 우리 67/85/94/74/59** 로, 문제는 높이가 아니라 **코너 반경**이었다.

### 자체 검산으로 잡은 것 5건

1. **본문 별 무늬의 절반이 비어 있었다.** 레퍼런스 별 행 중심은 ref y **587.5 / 1647 / 1762.5**(피치 117.5,
   체커보드)인데 우리는 587.5 / 1762.5 둘뿐이었다 — A5 공용 `.mbody` 타일에 **별이 1개**(235×235)뿐이라
   235px 마다 한 줄씩만 찍힌 것이다. 33 재화정보(`.ci-body`)·20 스펙정보(`.spc-body`)가 쓰는 **같은 게임 자산**
   (별 2개 반칸 오프셋)을 `.q22 .mbody` 에서만 override 해 복원했다(LESSONS 20-② 재현).
   위상은 22 실측(별 중심 ref x171 / y587.5)에 맞춰 `36.5px -1px/235.5px 235px`.
   → 재캡처 실측: 행 587.5 · 열 171/407/642/878 (ref 587.5 · 171.5/407/642/877.5).
   **A5 공용 타일 자체는 건드리지 않았다** — 다른 11개 다이얼로그가 같이 쓴다(남의 구간).
2. **보상 프레임 radius 는 36 이 아니라 31 이다 — 측정표 §5-1 오기.**
   레퍼런스 좌·우 14개 열의 «세로 연속 높이» 로 (h, r) 최소자승 피팅 → **h 105.8 · r 30.9 (MSE 0.27)**,
   측정표 값(h105 · r36)은 같은 표본에서 **MSE 41**. 상단도 696 이 아니라 698 이고,
   불변식은 «프레임 세로중심 = 행 세로중심»(ref 750.75 vs 750.5)이다.
   → `top:35→37 · height:105→106 · radius:36→31`. 수정 후 비가림 열 5곳이 ref 와 **완전 일치**.
3. **수량 숫자 자폭 +18%** — ref 는 `50` w44 / `100` w65 로 **자당 22px(탭폭)** 인데 `scaleX(1.4)` 로는 26px.
   1.4 × 22/26 = **1.18**. 수정 후 `235` w65 = 자당 21.7.
4. **진행바 중앙 텍스트 폭 −24%** — 같은 6글자로 대조(ref `45/200` 총잉크 w92 vs 우리 `50/224` w70).
   코어 높이는 ref 15~16 / 우리 16 으로 맞으므로 **font-size 가 아니라 폭**이다(05 «숫자 폭» 교훈).
   `<i>` 는 채움 바가 이미 쓰고 있어 **`<em>` 에 `scaleX(1.31)`**. 수정 후 w92 = Δ0.
5. **토글 라벨 코어 −5~6%** — 외곽선 두께는 ref 와 같고 **코어만** 작았다(20 교훈 3) → `fs 39→41`.
   수정 후 `일일` 68×36 (ref 69×36) · `반복` 70×37 (ref 68×36).

**미채택**: P 의 «타이틀 밴드 높이 92 vs 90» — 밴드색 마스크로 직접 재면 ref 472~561 / 우리(환산) 472~561 로 **Δ0**.

## 3회차 (2026-08-25) — 비평 Q(1회차 캡처) 반영

### 비평가 Q 점수
| ① 배치 | ② 크기 비율 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|
| 9 | **8** | 9 | 9 | 7 | 8 | **8 (통과선)** |

Q 는 P 와 달리 «가림 없는 열» 규칙을 지켜 보상 프레임을 **106×106 정사각**으로 정확히 읽었고(측정표 105 정정),
같은 결론에 독립적으로 도달했다.

### 반영 4건

| # | 지적 | 처방 | 결과 |
|---|---|---|---|
| 1 | 타이틀 잉크 좌단이 진행바 기준 ref +3 인데 우리는 +5 | `.qs-t left 170→168` | 코어 좌단 302 = ref 302 |
| 2 | 한글 자간 −4.8% (공통 접두 «아무거나 소환» ref 168 / 우리 160) | `scaleX .9→.945` + `word-spacing 2.5→1.5` | 169 (ref 168) |
| 3 | [모두 받기] 바닥 그림자 밴드 6→14px (+133%) | 그라디언트 스톱 102→109 | 재HL 1673~1678 · 그림자 1679~1685 = **ref 완전 일치** |
| 4 | 진행바 폭 (측정표 402, ref 검정 마스크 **300~699 = 400**) | `left 168→169 · width 402→400` | ref 와 Δ0 |

**부분 채택** — [보상 받기] 그림자(ref 8 vs 우리 10)는 되돌렸다. 스톱을 내리면 그림자는 맞지만
**재하이라이트가 ref(내부 local 90~96 = ref 788~794)보다 3px 내려간다.** 둘을 동시에 못 맞춰 **위치 쪽**을 택했다.

**미채택 2건**
- «행 높이 179→178 · gap 21→22» — P 는 정반대로 «180 / 20» 이라 했다. 두 비평가가 **우리 값(179/21)을
  사이에 두고 엇갈리므로** 움직이지 않는다. 하류 좌표(패널 하단·모두받기·토글·5행 잘림)는 전부 Δ0 이다.
- «모달 검정 테두리 8→7px», «모달 타이틀 외곽선이 코어를 절반 먹는다» — 둘 다 **A5 공용 껍데기**
  (`.mbox` / `.mhead h2`) 소속이라 11개 다이얼로그가 같이 쓴다. **내 작업 구간이 아니다**(병렬 규칙 3).
  A5 를 다시 잡는 세션이 처리할 것 — 아래 «다음 세션에 넘기는 것» 참고.
