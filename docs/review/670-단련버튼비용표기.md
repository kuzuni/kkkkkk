# 670 — 단련 버튼 비용 표기 간소화 (주인 지시 2026-09-02 01:08)

> 주인 원문: «단련석에 단련 (단련아이콘) 3 이런거 말고 (단련아이콘) 3 이런식으로 해주면됨. 버튼에 그거 표시된거»

- 세션 `sess-1429-8910` (루틴 워커 B) · 1회차 · 2026-09-01
- 분류 **T2(주인 지시)** · 지시서 [3]-(가) — 채점 축이 «라벨에 낱말이 있나» 라 **자가 곧 눈**이다(비평가 없음).
- **제품 `index.html` 1자리** · 레이아웃 **Δ0px** · 밸런스·판정·세이브 **0줄**.

## 1. 재현이 먼저다 (338 규칙) — `tools/probe670.js` **7/7 신설**

등재문의 읽기 셋을 각각 물었고 **셋 다 확인**됐다.

| | 물음 | 실측(수리 전) |
|---|---|---|
| ⓐ | 주인이 지목한 «단련» 이 그 버튼에 있나 | 세 행 전부 «**단련 1**»(아이콘 1장 · 수 = `temperCost()`) |
| ⓑ | 그 말을 빼도 «무슨 버튼인지» 가 남나 | 같은 행 제목 `.tn` = «**공격력 단련**»·«체력 단련»·«체력회복 단련» · 서브탭 이름도 «단련» ⇒ 버튼 안의 «단련» 은 **한 화면에서 세 번째 나오는 같은 말**이었다 |
| ⓒ | 상자(584 가 300 → 340 으로 넓힌 것)를 다시 좁힐 이유가 생기나 | 자릿수 스윕 — 10자리 안쪽 여유 **1.13px**(수리 전) → «단련 » 을 빼면 **61.89px**. 넘침 근거는 **안 생긴다** |

⚑ **ⓒ 가 이 회차의 결정 하나를 대신 지었다.** 584 가 340 을 고른 근거는 «자릿수 최악에서 넘친다» 였고,
라벨이 짧아지면 그 여유는 **늘기만 한다**. 그래서 상자를 되좁히지 않았다 —
좁히면 584 가 사 놓은 여유를 도로 반납하는 것이고, 210 의 세로/가로 예산(`.td` 우변 616 ↔ 버튼 좌변 632)
과 351(9:13.3) 축을 이유 없이 다시 흔든다. **레이아웃 Δ0px 가 정답이다.**
(되돌림: `.tr-tp>.tb{width:340px}` 한 값 · 되좁히려면 `probe670` ⓒ 를 다시 돌려 여유부터 확인할 것.)

## 2. 제품 — 한 줄

```js
-    btn: '단련 ' + curIc('tstone', TR_CUR_PX) + fmt(c),
+    btn: curIc('tstone', TR_CUR_PX) + fmt(c),
```

`temperRowTxt()`(`index.html` ~33808) 하나만 고치면 **통짜 렌더(`renderTemper`)와 홀드 갱신(`liveTemper`)이
둘 다** 따라온다 — 297 규약대로 두 경로가 같은 함수를 읽기 때문이다(그래서 [6-a] 가 초록).

## 3. 게이트

- **`tools/verify670.js` 26/26 신설** — [1] 전제 · [2] ★ 라벨이 «(아이콘) n» 뿐(낱말 0건 · tstone 1장 ·
  소스에 옛 접두식 0건) · [3] 뺀 말이 화면에 **남아 있다**(행 제목·서브탭) · [4] 그 수가 `temperCost()`
  **파생**(레벨을 올리면 통짜·홀드 두 경로가 따라 바뀐다) · [5] 584 상자·정렬 **Δ0**(340×74 · right 26 ·
  top 128 · 화폐 세로 중앙 10.5/10.5 · 라벨 가로 중앙 134.1/134.1 · 10자리 여유 1.13 → 61.89) ·
  [6] 297 두 경로 동일 + **홀드 토스트 «단련 n회» 는 살아 있다** · §R 되돌림 3항.
- ⚑ **[6-c] 가 이 자의 «무르지 않음» 이다** — «단련» 을 소스에서 통째로 지우는 수리였다면 그 항이 빨개진다.
  주인이 지목한 것은 **버튼 라벨 한 자리**이지 낱말 자체가 아니다.
- ⚠ **1회차에 자가 자기 하네스에 걸렸다** — [4-b] 가 «올린 뒤 또 올리고» 기대값을 읽어 «15 vs 45» 로 빨갰다.
  제품이 아니라 **읽는 순서**가 틀린 것이고(LESSONS 199-⑤ 와 같은 함정), 기대값을 각 단계 직후로 옮겨 닫았다.

## 4. 이관 2건 — 방향을 뒤집었지 지우지 않았다 (333 처방)

| 자 | 옛 항 | 갈아 끼운 뒤 |
|---|---|---|
| `verify584` | **[5-a]** «라벨이 «단련» 으로 시작한다» | **[5-a]** «라벨은 «(아이콘) n» 뿐(낱말 0건)» + **[5-a2] 신설** «뺀 말은 같은 행 제목이 말한다» · §R [R-b] 원복 술어도 같이 |
| `verify210` | «단련 버튼에도 같은 비용이 적힌다» 를 **`/단련\s*3/`** 으로 물었다 | 같은 뜻을 **수의 일치**로 — 버튼 텍스트 = `temperCost()` = 비용 열의 수 |

⚑ **둘 다 «항을 지우면 안 되는» 자리였다.** 584 ④ 의 뜻은 «버튼이 죽은 말([투자]·pt)을 하지 않는다» 였고
210 의 뜻은 «표기층이 두 벌이 되지 않는다» 였다 — **둘 다 지금도 참**이고, 달라진 것은 버튼이 말하는
**방법**(낱말 → 아이콘)뿐이다. 그래서 술어만 갈아 끼웠고, 584 쪽은 «그 말이 어디로 갔는지» 를 묻는
항([5-a2])을 **한 줄 더 넣어** 옛 항이 지키던 뜻이 통째로 사라지지 않게 했다(328 교훈).

## 5. 회귀

`verify670` 26/26 신설 · `probe670` 7/7 신설 · `verify584` **54/54**(이관 뒤) · `verify210` **96/96**(이관 뒤) ·
`verify613` 15/15 · `verify614` 10/10 · `verify519` 27/27 · `verify166` 73/73 · **SMOKE PASS**.

⚠ **`verify583` 은 19/33 으로 빨갛고 내 변경과 무관하다** — `HEAD~1`(수리 전) 트리에서 **같은 19/33** 임을
별도 worktree 대조로 확인했다(338·344·372 규칙). 빨간 축은 «강화 소모 알갱이 0개» 로,
지금 **658·659·660(lock 살아 있음 — `sess-1348-13644`)이 «알갱이 파티클 폐지» 로 걷어내는 중인 바로 그 축**이다.
그 세션의 작업 범위(«게이트 동시 철거» — 658 등재문)에 이미 들어 있으므로 **새 번호로 등재하지 않았다**(중복 방지).

## 6. 곁다리 등재

**0건.**
