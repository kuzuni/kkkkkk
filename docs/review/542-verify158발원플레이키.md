# 542 — `verify158` 「발원 좌표 = 킬 자리」 플레이키

* 세션 `sess-2137-13867`(루틴 워커 B) · 2026-08-30 · **1회차**
* 분류 **T1 «버그(자 플레이키)»** · 지시서 [3]-**(가)** 기계적/자 작업 — **비평가 없음**
* **제품 `index.html` 0줄** · 레이아웃 Δ0px · 판정·밸런스·세이브 0줄. 고친 파일은 `tools/verify158.js` 하나 + 재현기 신설.

## 1. 재현 먼저 (338 규칙)

착수 트리에서 `node tools/verify158.js` 5회: **PASS 3 / FAIL 2**. 빨간 항은 두 자리인데 **문구가 같다**
— [A~C] 141행·[F] 269행의 «발원 좌표 = 킬 자리».

```
run 3:  ✗ 발원이 «누른 탭» 쪽이다 — 킬까지 414px / 탭까지 1133px
run 4:  ✗ 발원이 «누른 버튼» 쪽이다 — 킬까지 809px / 탭까지 1477px
```

⚑ **실패 문구가 이미 자기모순이었다** — «탭 쪽이다» 라면서 **탭까지가 킬까지보다 2.7배 멀다.**
발원은 탭도 킬 자리도 아닌 **제3의 자리**였다.

## 2. `tools/probe542.js` 신설 — 뿌리는 둘이다

씬을 8회 돌리며 ① 창 앞/안에 들어온 **남의 킬**(killEnemy 가 부르는 `fxAt(…, 'combat')`) 수 ②
`fxFly(from,…)` 인자(= 묶음이 실제로 쓴 발원) ③ 킬 자리·탭까지의 거리를 같이 찍는다.

**[1] 격리 없음(현행 자와 같은 씬) 8회 — 어긋남 5/8**

| # | 창 앞 킬 | 창 안 킬 | gold 묶음 | combat | 킬까지 | (마지막 묶음) | 탭까지 |
|---|---|---|---|---|---|---|---|
| 1 | 1 | 0 | 2 | true | **129** | 0 | 799 |
| 2 | 0 | 0 | 1 | true | 0 | 0 | 716 |
| 3 | 0 | 1 | 1 | true | 0 | 0 | 716 |
| 4 | 0 | 1 | 2 | true | 0 | 662 | 716 |
| 5 | 0 | 1 | 1 | true | **598** | 598 | 1304 |
| 6 | 0 | 1 | 1 | true | **539** | 539 | 1255 |
| 7 | 0 | 1 | 1 | true | **563** | 563 | 1279 |
| 8 | 1 | 0 | 2 | true | **593** | 0 | 1308 |

⇒ **구멍이 둘**이고 **어긋난 8시행 전부 발원 태그는 `combat` 이다**(= 탭 가로채기는 한 번도 없었다):

* **ⓐ 창 «안» 의 남의 킬**(#5·6·7) — 제품이 규약대로 `fxAccSrc` 를 **마지막 킬 자리**로 갱신한다
  (`index.html` 35545 «매 증가마다 갱신하므로 연속 킬에서는 마지막 킬 자리가 남고»). **제품이 옳다.**
* **ⓑ 창 «앞» 의 남의 킬**(#1·8) — 그 킬이 자기 묶음을 먼저 쏘는데 자가 `fxFly` 로그의 **첫** gold 를
  골랐다. 같은 시행을 **마지막 묶음**으로 재면 오차 **0px** 이다 — **자가 남의 묶음을 봤다.**

**[2] 격리(창 내내 `enemies`·`spawnQ` 를 비운다) 8회 — 8/8 끼어듦 0 · 오차 0px**
**[3] 되돌림 — 격리한 채 창 한복판에 남의 킬을 일부러 하나: 다시 1253px 어긋난다.**

⚠ **한 번만 비우면 모자란다** — 창(320~700ms) 안에 새로 스폰된 적이 다시 죽는다. 16ms 주기로 창 내내 비운다.
(등재문의 후보 ⓑ «`S.bossFarm = true`» 는 보스전만 막고 잡몹 킬은 계속 나므로 이 자에는 실제로 부족하다.)

## 3. 처방 — 자만 고쳤다(제품 0줄)

1. **씬 격리** `h.quiet()` — 창 내내 `enemies`·`spawnQ` 를 비우고 창이 끝나면 되돌린다.
2. **묶음을 금액으로 고른다** `h.pick(cur, 최소액)` — «첫 gold» 가 아니라 «이 씬이 넣은 금액(777/500)을
   나르는 묶음». ⓑ 를 닫는다.
3. **음성항 [A0]·[F0] 신설** — «창 동안 남의 킬 0건». 끼면 그 시행을 **버리고 다시 돌리되(상한 6)**
   6시행 전부 끼면 **빨강**이다. 「끼면 넘어간다」로 두면 이 절이 통째로 사라져도 초록인 게이트가 된다.
4. **[G] 되돌림 시험 신설** — 격리가 뚫린 시행을 합성(남의 킬 정확히 1건)하면 위 초록이 실제로 **빨개진다**
   (1253px). 어긋난 자리가 **남의 킬 자리**(오차 <4px)이고 태그는 여전히 `combat` 임까지 단언한다.
5. **실패 문구 정정** — 「발원이 «누른 탭» 쪽이다」 → 「발원이 킬 자리가 아니다 — 킬까지 n / 탭까지 n
   (combat=… · 남의 킬 n건)」. 다음 세션이 «탭이 이겼다» 로 오독하지 않게 두 거리를 같이 찍는다.

⚠ **허용 오차는 한 칸도 안 넓혔다**(<4px 그대로) · **제품 `fxSrc` 의 600/1200ms 창은 한 줄도 안 건드렸다**
(등재문 경고 — 77·158·227 이 그 위에 서 있다).

## 4. 결과

| 자 | 착수 | 지금 |
|---|---|---|
| `tools/verify158.js` | 5회 중 **PASS 3 / FAIL 2** (18항) | **10회 연속 PASS · 24/24** |
| `tools/probe542.js` | — | **신설 · PASS** |

* `verify158` **10/10 연속 PASS**(항 18 → **24**: [A0]·[F0] 음성항 2 + [G] 되돌림 4).
* [E](자동 전투 8초 통합)는 **일부러 격리하지 않았다** — 그 절이 재는 것이 «진짜 배경 전투 중» 이다.

## 5. 회귀 (전부 무수정)

| 자 | 결과 |
|---|---|
| `tools/verify158.js` | **10/10 연속 PASS · 24/24** (착수 5회 중 FAIL 2) |
| `tools/probe542.js` | **신설 PASS** |
| `tools/verify93.js` | 21/21 PASS |
| `tools/verify518.js` | 32/32 PASS |
| `tools/verify58.js` | 26/26 PASS |
| `tools/verify98.js` · `tools/verify99.js` | PASS |
| `tools/verify184.js` | 13/13 PASS |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verifyProgress.js` | **PROGRESS OK** (종료 코드 0) |

`docs/UI-REFERENCE.md` 는 **안 건드렸다** — 제품 0줄 · 레이아웃 Δ0px 라 갱신할 체크리스트 항목이 없다.

## 6. 곁다리 등재 — 552 (`verify77` [B])

회귀로 `node tools/verify77.js` 를 돌리다 `✗ combat 묶음의 +n 이 #fxlc 에 없다` **1건**(FAIL(1))을 봤다.
**플레이키가 아니라 결정적**이고(3회 연속 같은 자리), 같은 절의 형제 세 항은 전부 초록이라 **갈리는 것은 그 한 자리뿐**이다.

⚑ **내 변경과 무관함을 대조로 못박았다**(338·344·369·372 규칙) — 착수 커밋 `c62e86e` 의 트리를 통째로 꺼내
(`git archive` → 별도 디렉터리) 2회 돌렸더니 **같은 자리에서 같은 FAIL(1)** 이다. 542 의 변경은
`tools/verify158.js` 한 파일 + 재현기 신설이 전부이고 제품은 0줄이다. **내 작업 단위가 아니라 안 고쳤다** —
갈래(ⓐ 자 쪽 도착 판정 · ⓑ 제품 `fxPlus` 의 층 선택)와 재현 처방은 PROGRESS **552** 행에 적었다.

## 7. 다음 세션이 볼 것

* ⚑ **«전투 발원» 을 재는 씬은 배경 자동 전투를 반드시 잠재워라.** 제품은 규약대로 «마지막 킬 자리» 를 남기므로
  격리하지 않은 씬은 **구조적으로** 플레이키다. 한 번 비우는 것으로는 모자라고 **창 내내** 비워야 한다.
* ⚑ **`fxFly` 로그에서 묶음을 «첫 것» 으로 고르지 마라.** 창 앞의 킬이 자기 묶음을 먼저 쏜다 —
  **금액**으로 고르면 남의 묶음을 안 본다(`h.pick(cur, 최소액)`).
* ⚑ **실패 문구가 자기모순이면 문구부터 의심하라.** 이 자는 3회차 동안 «탭이 가로챘다» 고 말했지만
  어긋난 시행의 발원 태그는 **한 번도 빠짐없이 `combat`** 이었다 — 탭은 용의자조차 아니었다.
