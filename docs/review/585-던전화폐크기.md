# 585 — 03 던전의 화폐·입장권 아이콘 크기

> 저장소 주인 지시(2026-08-31): **«던전행에서도 화폐들 크기좀 키우기»**
> 세션 `sess-0721-11840` (루틴 워커 A) · 지시서 [3]-**(가)** 계열(레퍼런스 대조는 측정표 수치로,
> 비평 루프 없음 — 바꾸는 것이 «아이콘 잉크 크기» 하나이고 측정표가 목표값을 이미 적어 두고 있다)

---

## 1회차 (2026-08-31)

### §1 재현 — `tools/probe585.js` 신설

338 규칙대로 **처방 전에 쟀다.** 등재문이 지목한 기준선이 «훈련 팝업에서 처럼» 이므로
자는 먼저 23 훈련 팝업의 화폐 아이콘을 재고, 03 던전의 자리들을 그 값에 견준다.

⚠ **재는 것은 상자가 아니라 잉크다**(340 교훈). `.cic` 는 `object-fit:contain` 인 정사각 SVG 라

    잉크 = 렌더 상자 × (그 SVG 의 알파 bbox ÷ viewBox)

이고, 알파 bbox 는 자산을 512px 로 크게 그려 한 번만 잰다(412 방식).
**눈금은 √잉크면적(«시각 덩치»)으로 정해 적었다**(394 규약) — 동전은 정사각이고 입장권은
가로로 긴 티켓이라 «높이» 로만 견주면 **모양 차이가 크기 차이로 읽힌다.**

#### 자산별 잉크비 (알파 bbox ÷ viewBox · `probe585` 실측)

| 자산 | 가로 | 세로 |
|---|---|---|
| `cur-gold` | 1.00000 | 1.00000 |
| `cur-dia` | .93750 | .93750 |
| `cur-relic` | .84375 | .97266 |
| `cur-stone` | .75000 | .81641 |
| `cur-rstone` | .62500 | 1.00000 |
| `cur-tstone` | .75000 | .75000 |
| `cur-mile` | .90625 | .65625 |
| `cur-ticket-*` (8장 전부) | **.93750** | **.53125** |

⚑ **입장권 8장은 잉크비가 완전히 같다** — 412·430 이 «껍데기 기하(path 2줄)는 8장 픽셀 동일»
이라고 적어 둔 것이 이 표로 확인된다. 그래서 크기 처방은 **8종에 한 줄**이면 된다.

### §2 재현이 밝힌 것 — 등재문의 ①(`.pcb`)은 **기각**, 뿌리는 다른 데 있었다

| 자리 | 수리 전 덩치 | 기준선(23 훈련 골드 **52.97**) 대비 |
|---|---|---|
| 03 `.pcb` 골드 | 65.30 | **+23.3%** |
| 03 `.pcb` 다이아 | 59.06 | **+11.5%** |
| 03 카드 보상 알약 코인 8칸 | 51.51 / 48.29 / 46.67 ×4 / 38.64 / 32.20 | −2.8% ~ **−39.2%** |
| 03 카드 입장권 8종 | **35.01** | **−33.9%** |
| 04 세부 입장권 | **30.35** | **−42.7%** |

⚑ **등재문 처방 ①·②(«`.pcb` 를 세 화면 같이 올린다»)는 안 골랐다 — 재현이 그 전제를 뒤집었다.**
`.pcb` 의 두 아이콘은 이 게임에서 **가장 큰 화폐 아이콘**이고 기준선보다 이미 11~23% 크다.
340 이 HUD 값(65.3 / 63)으로 이미 맞춰 둔 자리다. 여기를 더 키우면
**89 유물·10 상점까지 같이 커지고**(공용 부품) 340·A3 가 세운 ref 정합이 깨진다.
⇒ **`.pcb` 는 0줄 변경**이고, 그 근거를 측정표 41 정오표에 적었다(등재문이 요구한 «던전만
   올려야 할 근거» 대신 «`.pcb` 는 안 올린다는 근거» 가 됐다).

⚑ **진짜 뿌리는 하나다 — 세 자리 전부 «상자를 재고 잉크를 안 봤다».**
`.dnc .pill>em{transform:scale(1.0732)}` 은 356 5회차가 **금화 하나**(자연 잉크 47.52 =
viewBox 를 꽉 채우는 유일한 아트)를 재서 정한 값이고, `.dnc .sp.tk>em{scale(.8269)}` 의
«자연 잉크 60.47» 은 잉크가 아니라 `.cic` 의 **선언 상자**(1.08em × 56)다.
상자는 여덟 칸 다 같은데 아트가 viewBox 를 덜 채우는 만큼 **그려진 잉크만** 제각각 작아졌다.
340 이 HUD 에서 잡고 `.pcb` 에서 다시 잡은 **바로 그 계열**이 03·04 에 세 자리 더 남아 있었던 것이다.

### §3 처방 — «ref 잉크 상자에 등방 contain» (제품 `index.html` 3자리)

새 상수를 만들지 않고 측정표가 이미 적어 둔 **ref 잉크 상자**를 목표로 삼는다.
356 규약대로 **등방**만 쓴다(비균등 scaleX/scaleY 금지) — 한 축이 남는 것은 아트 몫이다.

| 자리 | ref 잉크 상자 | 배율 | 수리 후 그려진 잉크 |
|---|---|---|---|
| `.dnc .pill>em` dia | 51 × 53 (§3-4-1) | `scale(1.14478)` | 50.99 × 50.99 |
| `.dnc .pill>em` relic | 〃 | `scale(1.14664)` | 45.97 × 52.99 |
| `.dnc .pill>em` stone | 〃 | `scale(1.36613)` | 48.68 × 52.99 |
| `.dnc .pill>em` rstone | 〃 | `scale(1.11532)` | 33.12 × 53.00 |
| `.dnc .sp.tk>em` 입장권 8종 | 64 × 50 (§3-5-3) | `.8269` → **`1.12875`** | **63.99 × 36.26** |
| `.dgd-tki` 입장권 | 69 × 54 (04 §6) | `rotate(-18deg) scale(1.51276)` | **68.98 × 51.94** (∠18° bbox) |

**묶는 축(=배율을 정한 축)은 전부 Δ0 이다** — 입장권 폭 −0.02% · 04 폭 −0.03% ·
보상 코인 gold/dia 폭 −0.01% · relic/stone/rstone 높이 −0.01%.

⚠ **배율을 `<em>` 에 그대로 뒀다**(자식 `<img>` 로 안 옮겼다) — 원점이 em 중심이라
   356·342 가 실측해 둔 **잉크 중심이 한 칸도 안 움직인다.** `<img>` 는 인라인이라
   세로로는 baseline(`vertical-align:-0.2em`) 위에 놓여 em 중심과 다르다.

⚠ **04 슬롯은 «입장권일 때만»** 건다(`:has(>[data-cur-ic^="tk"])`). 209 탑이 같은 슬롯에
   `curIcEl($('dgdTki'), ck)` 로 **보상 재화**(정사각)를 그리는데, 정사각 아트에 1.51 을 걸면
   65.4 정사각이 되어 상자(69×54)를 넘는다. 402 규약 덕에 스코프가 한 줄로 잡힌다 —
   **입장권 키는 예외 없이 `tk` 로 시작한다.**

⚠ 회전을 먼저·확대를 나중에 적는다(`rotate() scale()`). 순서를 바꾸면 원점은 같아도
   회전 **전** 좌표계에서 늘어나 «기울어진 방향으로만» 커진다.

### §4 남은 어긋남은 배율 몫이 아니다 — 두 가지, 둘 다 등재로 넘긴다

**ⓐ 카드 입장권 세로가 ref 50 에 대해 36.26(−27.5%)** — **각도** 몫이다.
측정표 §3-5-3 은 ref 를 «**기울어진** 티켓» 이라 적어 뒀고 04 세부의 우리 슬롯도
`rotate(-18deg)` 인데 **03 카드 티켓만 눕지 않았다.** 산수가 그것을 말한다:
우리 티켓 비율 1.765 를 18° 눕히면 bbox 종횡이 **1.33**, ref 64/50 = **1.28** 로 맞는다.
평평한 채로는 등방 배율이 무엇이든 1.765 를 못 벗어난다.
⇒ 이 행은 «크기» 지시이므로 각도는 안 건드리고 **596 으로 등재**한다(§6).

**ⓑ 보상 알약 rstone 폭이 33.12(ref 51 대비 −35%)** — **아트** 몫이다.
`cur-rstone.svg` 자체가 .625 × 1.000 으로 세로로 긴 그림이라 등방으로는 두 축을 같이 못 채운다.
356 정오표의 🔥 와 **같은 자리**이고 처방도 같다(371 «답은 배율이 아니라 글리프 교체»).
발주 시 이 자리의 ref 종횡비는 51/53 = **0.962** 다.

### §5 결과 — 덩치 고르기

여덟 칸 보상 코인의 «최대÷최소» 가 **1.600 → 1.217** 로 좁아졌고,
rstone 한 칸(아트 몫)을 빼면 **1.033** 이다. 입장권은 8종이 원래도 지금도 **1.000**.

| 자리 | 수리 전 덩치 | 수리 후 | 증가 |
|---|---|---|---|
| 03 카드 입장권 8종 | 35.01 | **48.17** | **+37.6%** |
| 04 세부 입장권 | 30.35 | **46.11** | **+51.9%** |
| 보상 코인 dia | 48.29 | **51.00** | +5.6% |
| 보상 코인 relic ×4 | 46.67 | **49.36** | +5.8% |
| 보상 코인 stone | 38.64 | **50.79** | **+31.4%** |
| 보상 코인 rstone | 32.20 | **41.90** | **+30.1%** |
| 보상 코인 gold | 51.51 | 50.99 | −1.0% (ref 51 로 정렬) |
| `.pcb` 골드·다이아 | 65.30 / 59.06 | **불변** | 0 |

### §6 곁다리 등재

- **596**(T3) — 03 던전 카드 입장권 아이콘이 **눕지 않았다**. ref 는 기울어진 티켓(§4-ⓐ).
  04 세부(`rotate(-18deg)`)에 이미 선례가 있고, 눕히면 카드 티켓 bbox 종횡이 ref 와 맞는다.
  ⚠ 눕히면 bbox 가 커지므로 **`.lb` 라벨(카드+243..281)과의 간격을 다시 재야 한다** —
    지금 잉크 상변이 카드+283.9 라 여유가 2.9px 뿐이다.
