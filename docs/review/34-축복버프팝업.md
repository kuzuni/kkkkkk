# 34 축복(버프) 팝업 — 작업 기록

- 작업 단위 **34** · 워커 **C** · 세션 `sess-0344-11735` · 2026-08-25
- 측정표: `docs/measure/34-축복버프팝업.md` (이 세션이 **신설**. 재측정 금지)
- 캡처: `docs/review/34-r1.png` (1080×2280)
- 스크립트: **`scan34.js`**(레퍼런스 픽셀 스캐너) · **`cap34.js`**(캡처+좌표 덤프) · **`verify34.js`**(기능·기하 검증기)
- 게이트: `node verify34.js` → **VERIFY34 PASS 51/51** · `node tools/smoke.js` → **SMOKE PASS** · 콘솔 에러 0

---

## 0. 이 화면이 무엇인가

레퍼런스 `docs/ref/34-축복-버프팝업.jpg` 는 **시간제 버프 3종을 무료로 켜는 팝업**이다.
껍데기는 **딤(.54) 위 가운데 다이얼로그 998×1157** — A5 `.mbox` 와 겹 구성이 같지만(검정 8 + 갈색 링 11 + 헤더 바 90 + 크림)
**폭이 897 이 아니라 998** 이고, **팝업 «바깥»에 초록 프로모 스트립과 빨간 닫기 X 가 형제로 떠 있다.**
그래서 공용 `#modal` 을 쓰지 않고 자체 오버레이 `#blsw` 로 만들었다(33 재화정보 팝업과 같은 판단).

## 1. 만든 것 (실동작)

레퍼런스는 «광고를 보고 켜는» 구조지만 이 게임엔 광고 SDK 가 없다. 목업(«준비 중»)을 만들지 않고
**실제로 동작하는 규칙**으로 바꿨다 — 측정표 §10 에 레퍼런스 ↔ 우리 게임 대응표가 있다.

- **축복 3종** `공격력 / 체력 / 공격 속도` 각 **+20%** — 레퍼런스의 `햄지 공격력 / 매옹이 공격력 / 체력` 은
  캐릭터 2명 구조라 «설계 확정» 1번(캐릭터는 «용사» 1명)에 따라 항목만 바꿨다. **3칸 그리드·좌표는 그대로.**
- 카드를 누르면 **무료로 활성화**, 기본 **30분** 지속. 남은 시간이 `⏱ HH:MM:SS` 로 1초마다 갱신된다.
- **축복 레벨** — 1개 켤 때마다 진행 +1, **4를 채우면 레벨 +1**(그 활성화부터 즉시) 이고 **레벨당 지속시간 +5분**.
  진행바 채움은 `prog/4` 비례(레퍼런스의 `3/4` = 74% 와 같은 규칙).
- **보너스 축복** — 3종이 **동시에** 켜져 있는 동안만 `골드 획득량 +50%`. 하나라도 만료되면 즉시 꺼진다.
- 팝업 밖 초록 스트립 `모든 축복 받기!` + `[받기]` — 비활성 축복을 한 번에 켠다(레퍼런스의 «축복 자동 갱신! / 이동»
  은 유료 상품 크로스 프로모라 갈 곳이 없다. **자수 8자 / 2자를 맞춰 bbox 는 유지**했다).
- 효과는 `bonus()` 에 곱한다(`b.atk`·`b.hp`·`b.rate`, 보너스는 `b.gold`). `bonus()` 는 캐시라
  **만료 순간에 `markDirty()` 로 캐시를 깨는 1초 tick** 을 붙였다 — 이게 없으면 만료돼도 배수가 안 내려간다.
- **진입점**: 좌측 사이드 아이콘 «축복»(`data-pop="bless"`). 작업 48 이 «던전» 을 지우면서 비워 둔
  **예약된 5번째 라벨 행**에 넣었다 — `layoutSide()` 가 `SIDE.N=5` 로 이미 5칸을 잡아 두고 있어서
  `--ih/--igap/--itop` 이 안 바뀌고 **기존 4칸 아이콘 좌표 이동 0** 이다(A2 측정 규격 불변).
  켤 수 있는 축복이 있으면 아이콘에 레드닷(`sideAlert('bless', …)`)이 붙는다.
- **저장 구조**: `S.bless = {lv, prog, exp:{atk,hp,rate}}` 신설. **`KEY` 는 올리지 않았고**(올리면 전 진행도 소멸)
  44 교훈 2 대로 «없으면 기본값» 마이그레이션으로 흡수한다 — 옛 세이브가 없거나 null·문자열이어도 NaN 이 안 샌다.

## 2. 회차 기록

| 회차 | 한 일 | 검증 |
|---|---|---|
| 1 | **`scan34.js` 신설** — 이 컨테이너엔 PIL 이 없어 playwright chromium 의 `canvas.getImageData` 로 JPEG 를 디코드하는 스캐너를 만들었다(`map`/`row`/`col`/`raw`/`mask`/`rect`, `SCAN_IMG` 로 아무 레퍼런스에나 재사용). 껍데기·카드 그리드·리본·배너·진행바·보너스 바 전량 실측 → 측정표 작성 | — |
| 2 | 팝업 구현(#blsw). DOM 좌표 덤프 대 측정표 **13항목 Δ≤2** | 콘솔 0 |
| 3 | 서체 실측 보정 8건(아래 §3) | **VERIFY34 51/51** · **SMOKE PASS** |
| 4 | 아트 자리 규칙 — 카드 아이콘 잉크를 ref bbox 에 맞춤, 보너스 바에서 **통째로 빠져 있던 초록 셰브론** 복원 | VERIFY34 51/51 |
| 5 | **«노랑 사선 스트라이프» 자체 반증** — 픽셀 스캔상 평면이었다(§4 교훈 1) | VERIFY34 51/51 |

## 3. 3회차 서체 보정 — 「현재 → 목표(ref)」

| 항목 | 마스크 | 3회차 전 | 목표(ref) | 처방 | 결과 |
|---|---|---|---|---|---|
| 타이틀 «축복» 코어 | pale | 94×47 | **78×47** | `scaleX(.83)` | **78×47 Δ0** |
| 안내 배너 흰 코어 | bright | 669×33 | 597×33 (24자) | 문구 24자로 + `scaleX(.856)` | 604×33 (Δ7) |
| 카드 헤더 잉크 | dark | h51 · 자폭 43.3 | h42 · 자폭 33.8 | fs 42→35 · `scaleX(.936)` | h43 · 자폭 34.0 |
| 카드 `+20%` 초록 | green | 105×29 | 119×32 | fs 42→46 | 116×32 |
| 타이머 흰 코어 | bright | 153×25 | **169×24** | `scaleX(1.1)` | **169×25 Δ1** |
| 보너스 탭 잉크 | dark | 184×41 | **161×42** | `scaleX(.875)` | **161×41 Δ1** |
| 보너스 바 흰 잉크 | bright | 218×39 | **181×38** | `scaleX(.83)` + left 315→309 | **181×39, x386..566 Δ0** |
| 하단 문구 잉크 | dark | 776×31 (색이 밝아 마스크에 안 걸렸다) | 673×36 | 색 `#6A5033`→`#241A0E` · fs 33→38 · `scaleX(.75)` | 669×36 (Δ4) |

## 4. 교훈 (다른 화면에서도 재발한다)

> **1. 「단면이 한 덩어리다」로 「패턴이 없다」를 단정하지 마라 — 세그먼터가 부드러운 전이를 삼킨다.**
> 시각 판독은 카드·보너스 바를 «노랑 사선 스트라이프» 로 읽었는데, 5회차에 `row 1120 76 372` 가
> `76..372 (297) #fdce5a` **한 덩어리**, `col 95 878 1246` 이 `879..1237 (359)` **한 덩어리**로 나와
> «평면» 으로 판정하고 스트라이프를 **지웠다. 이게 오판이었다.**
> `row`/`col` 세그먼터는 **«바로 앞 픽셀과의 차이 > 26»** 으로만 끊는다. 두 톤이 여러 픽셀에 걸쳐
> 부드럽게 넘어가면 걸음마다 26 미만이라 **한 덩어리로 보고**된다.
> **면적 히스토그램**으로 다시 재니 `rect 90 1105 360 1125` → **(240,176,72) 44.3% / (248,200,80) 39.3%** —
> 명백한 2톤이었다(비평가 A 실측: 45° · 수평주기 92px · 어두운 띠 43px).
> → **주기 패턴·워터마크·틴트는 «세그먼트 개수» 가 아니라 «면적 히스토그램(`rect`)» 으로 판정하라.**
> 단면 스캔은 «경계가 뚜렷한 층»(테두리·밴드)에만 믿을 수 있다.
> 부록: 45° 스트라이프를 CSS 로 옮길 때는 **수평 실측치 ÷ √2** 가 그라디언트 축 값이다(92→65.05, 43→30.4).
>
> **2. 이모지 아트는 «배경과 같은 색군» 이면 스캔에도 사람 눈에도 사라진다.**
> 공격 속도 카드에 `⚡` 를 넣었더니 노랑 카드 위에서 잉크가 배경과 섞여 **noty 마스크 bbox 가 아예 안 잡혔다**
> (다른 두 카드는 잡혔다 = 우리 쪽만 아이콘이 «없는» 그림). `🌀` 로 바꾸니 152×155 로 ref(132×156)와 맞았다.
> → 아트 자리를 이모지로 대체할 때는 **그 칸 배경색과 색군이 겹치지 않는 것**을 고르고,
> 고른 뒤 **배경을 제외하는 마스크로 bbox 가 실제로 잡히는지** 확인하라.
>
> **3. 시간제 버프를 캐시된 `bonus()` 에 넣으면 «만료돼도 안 꺼진다».**
> `bonus()` 는 `bonusDirty` 캐시라 축복을 곱하기만 하면 켤 때는 맞고 **만료는 영원히 반영되지 않는다**.
> 1초 tick 에서 «3종의 on/off 조합 문자열» 이 바뀔 때만 `markDirty()` 를 부르게 했다(매 초 캐시를 깨면
> 유휴 루프가 매 프레임 전체 보너스를 재계산한다). 검증도 «만료 전 배수 ÷ 만료 후 배수 = 1.2» 로 못박았다(I2).
>
> **4. `#id 태그`(1-0-1) 리셋은 `.클래스 .클래스`(0-2-0) 를 이긴다.**
> `#blsw s{display:inline-block}` 때문에 `.bls-c .tm{display:flex}` 가 **조용히 죽어** 타이머 글자가
> 세로 중앙에 안 왔다. `position:absolute` 인 자식들은 blockify 돼서 티가 안 나 더 늦게 발견된다.
> → 오버레이 전체에 태그 리셋을 걸었으면, 그 안에서 `display` 를 바꾸는 규칙은 **특이도를 확인**하거나
> 아예 `line-height`/`text-align` 으로 정렬하라.
>
> **5. 헤드리스 검증기의 `addInitScript` 는 `reload()` 마다 다시 돈다.**
> «새로고침 후에도 유지» 검사(G2)가 계속 실패했는데 원인은 코드가 아니라 **검증기가 reload 때 옛 세이브를
> 다시 깔아 버린 것**이었다. `if(!localStorage.getItem(KEY))` 가드를 넣으니 통과했다.
> 같은 맥락으로 «배수 비교» 검사는 `autoBuy`·`spAuto` 를 꺼야 한다 — 유휴 루프가 사이에 레벨을 올려
> `stat.dmg` 비가 1.20 이 아니라 1.344 로 나왔다(51 교훈 ③ «유휴 루프가 굴리는 값을 빼라» 의 재현).

## 5. 기능 체크 표 (`node verify34.js` — 51/51 PASS)

| 그룹 | 무엇을 눌렀나 | 무엇이 바뀌는가 |
|---|---|---|
| A1~A3 | (구버전 세이브 로드) | `bless` 없던 세이브 → `lv1/prog0/exp 3키`, 전부 비활성 |
| B1~B3 | 사이드 «축복» 아이콘 | 팝업 열림 · 카드 3장 렌더 |
| C1~C9 | 공격력 카드 | 활성 · `mulAtk()` **×1.20** · `stat.dmg` **×1.20** · 진행 +1 · 남은시간 30분 · 타이머 `HH:MM:SS` · **재클릭해도 누적 없음** · 체력/공속은 불변 |
| D1~D7 | 체력 · 공속 카드 | 2종만이면 보너스 없음 → 3종째에 `blessAll()` true · `mulGold()` **×1.50** · 보너스 바 활성 표시 |
| E1~E5 | 4번째 활성화 | 레벨 1→2 · 진행 0/4 되감기 · 지속 **35분** · `Lv.2` 표시 · 채움 0% |
| F1~F3 | `[받기]` | 3종 한 번에 활성 · 진행 +3 · **켤 게 없으면 버튼 숨김** |
| G1~G3 | (저장·새로고침) | `localStorage` 에 `bless` 저장 · 새로고침 후 유지 · **KEY 미변경** |
| H1~H2 | `X` · 딤 | 둘 다 닫힘 |
| I1~I3 | (만료 대기) | 비활성 전환 · **배수 즉시 원복(캐시 무효화)** · 카드 `.off` + «받기» 표시 |
| J1~J12 | (기하) | 측정표 12항목 Δ≤2 · 프레임 밖 이탈 0 |
| K1 | — | 콘솔 에러 0 |

## 6. 비평 회차

### 1차 (8회차 캡처 기준) — **A 4 / B 6** (통과 기준 8, 미달)

두 비평가 모두 **팝업 본체는 Δ0~2 로 정확하다**고 했고(타이틀 잉크·카드 그리드·보너스 라벨은 완전 일치),
감점은 **팝업 바깥 초록 스트립**과 **면·패턴 레이어**에 몰렸다.

| # | 지적 | 누가 | 반영 |
|---|---|---|---|
| 1 | **초록 스트립 CTA 버튼이 통째로 없다**(스트립 하단 54px 공백 · 우측 정렬 97px 붕괴 · 헤드라인 24% 축소를 «동시에» 만든 단일 원인) | A·B | ✔ `renderBless()` 가 «켤 축복이 없으면 `visibility:hidden`» 이었다 — 캡처 상태가 3종 전부 활성이라 버튼이 사라졌다. **숨김 → 비활성(`.off`)** 으로 바꿨다 |
| 2 | 카드·보너스바 **45° 사선 스트라이프 누락**(ref 수평주기 92 · 어두운 띠 43) | A | ✔ 복원(§4 교훈 1 정정) |
| 3 | 스트립 헤드라인 잉크 296×42 → **379×55** | A·B | ✔ fs 44→56 + 우측 정렬(우인셋 52) |
| 4 | 닫기 X 원 지름 105~106 → **117~118** | A·B | ✔ 상자 120→132 (블록 +12 만큼 padding-bottom 142→136 으로 상쇄해 팝업 top 345 유지) |
| 5 | 안내 배너 폭 866~868 → **892~896** | A·B | ✔ 879→900, 좌우 인셋 29 |
| 6 | 타이머 알약 폭 205 → **213~217** | A·B | ✔ 214 (카드 로컬 left 42) |
| 7 | 보너스 문구 중심 x 529.5 → **540** | B | ✔ 좌앵커 → 가운데 정렬 |
| 8 | 보너스 아이콘 101×96 @좌인셋 54 → **114×117 @63** | B | ✔ ref 잉크 x136..246·y1356..1470 실측으로 재배치 |
| 9 | 스트립 아이콘 뭉치 256×109 → **308×197** | B | ✔ fs 112→138 + `scaleY(1.35)`(아트 자리) |
| 10 | 크림 별 워터마크 부톤이 밝다 | A | ✔ `#ECD3B4`→`#E0C8A8` (ref 실측 224,200,168). ⚠ A5 `.mbody` 의 같은 문자열을 건드리지 않도록 **줄 번호로** 바꿨다 |
| 11 | 카드 하단 하이라이트 19px → **9px** | A | ✔ 2단 그라디언트 → 9px 단색 `#FFFF78` |

**반영하지 않은 지적** (두 비평가가 엇갈리거나 내 실측과 어긋난 것 — LESSONS 22 «엇갈리면 움직이지 마라»):

- A «카드 헤더 타일 폭 254 · 3열 간격 61»: 내 실측 `row 860` 은 타일이 카드 폭 전체(76..372)를 채운다.
  B 도 «카드 간격 18/17 (Δ1)» 로 내 값을 지지한다. **A 가 라운드 코너 구간에서 잰 것으로 보인다.**
- A «카드1 아이콘 폭 151 → 99»: 내 `noty` 마스크 실측은 ref 150×153. A 는 파란 물약 본체만 잰 듯하다.
- A «타이머 필 좌우 인셋 39 → 21»: ref 채움 실측이 x118..331 이라 인셋 42 다.
- A «별 워터마크 통째 누락»: `rect 100 1250 900 1278` 히스토그램에서 **우리도 부톤이 20.1% 있다**(ref 13.8%). 톤만 고쳤다.
- A «리본 기울기 12.5° → 15.0°»: 7회차에 칠해진 마젠타 bbox 를 314×150 @x32..345(ref 317×146 @x32..348)로 맞춰 둔 상태다.
  각도만 올리면 bbox 가 다시 어긋나므로 2차 비평이 같은 지적을 하면 그때 함께 재조정한다.

### 2차 (8회차 캡처 기준) — **C 5 / D 4** (1차 A4/B6 → 개선했으나 여전히 8 미달)

C 가 «1차 수정 7건 합격»(CTA 표시 · 헤드라인 · 닫기 X · 배너 폭 · 타이머 알약 · 하단 하이라이트 · 워터마크 톤)을
확인했고, 남은 감점은 **보너스 행 정렬**·**아트 자리 bbox**·**리본 두께** 3덩어리였다.

| # | 지적 | 누가 | 9회차 반영 · 실측 결과 |
|---|---|---|---|
| 1 | **보너스 문구가 «아이콘 우측 잔여폭 중앙»(x629)에 놓여 센터라인 90px 이탈** | C·D | ✔ `left:200`→`left:0;right:0`. 흰 잉크 **182×39 @x383..564**(ref 181×38 @386..566) |
| 2 | 보너스 «골드 획득량» 자당 잉크폭 +20% | C | ✔ `scaleX(.83)` 를 안쪽 `<b>` 가 아니라 **상자 전체**에 걸었다(안쪽에 걸면 중심이 안 움직여 2차에서 잡히지 않았다) |
| 3 | 초록 «+50%» 잉크 87×29 → **113×32** | 자체 | ✔ fs 42→45 + `letter-spacing 5` → **106×31 @x586..691**(ref 113×32 @581..693) |
| 4 | **리본이 «쐐기»** — ref 는 수직두께 67 로 일정한 평행사변형인데 우리는 85→111 로 두꺼워지며 배너 문구 선두를 덮었다 | C·D | ✔ clip-path 좌변을 18%~82% → 0~100% 로 펴고 상자를 114→**66** 으로 얇게. 마젠타 화소 **17849 → 15066**(ref ~15300) |
| 5 | 리본 기울기 12.5~13° → **15.0°** | A·D | ✔ `rotate:-15deg` → bbox **311×134 @x32..342**(ref 317×146 @x32..348) |
| 6 | **CTA 버튼 외곽 스트로크가 통째로 없다**(ref 검정 6px + 내측 상단 하이라이트 8px) | D | ✔ `border:6px solid #000` + `inset 0 8px` 하이라이트 |
| 7 | 비활성 CTA 면이 회녹(143,169,138)이라 «죽은 버튼» 으로 읽힌다 | D | ✔ `.off` 를 회색이 아니라 **어두운 초록 램프**로 (ref 면 119,229,54 대비 유지) |
| 8 | 보너스 아이콘 83×85 @x178 → **116×120 @x138** | C·D | ✔ 상자 114→150 · fs 104→140, 좌인셋 61→40 → 우단 **252**(ref 253) |
| 9 | 초록 스트립 아트 폭 −54% | C·D | ✔ `scaleY` 만으로는 폭이 안 늘어 **자간 14px** 로 3개를 벌리고 상자 340×210 |
| 10 | 보너스 바 상 9px / 하 14px 하이라이트 띠 누락 | C | ✔ `::before/::after` 로 신설 |

**반영하지 않은 지적 (두 비평가가 엇갈림 — LESSONS 22 «엇갈리면 움직이지 마라»)**

- **45° 스트라이프 주기**: C «ref 73px(밝 35/암 38), 우리 92 → 줄여라» vs D «ref 90.5(밝 47/암 41), 우리 92 → 맞다».
  정반대 방향이라 **손대지 않았다.** 3차에서 한 명이라도 재확인하면 그때 움직인다.
- **카드 간격**: C «ref 21» vs D «ref 18, 우리 17 (Δ1)» — 내 실측(크림 61→카드 76, gap 17)은 D 쪽이다. 유지.
- D «카드 헤더 상단 코너 반경 18 → 36»: 1명만 지적. 3차 확인 대기.
- D «Lv 진행바 높이 51 → 48»: C 는 «Lv 알약 137×68 · 진행바 Δ≤2» 로 «손대지 말 것» 에 넣었다. 유지.

### 회차 소진 — **9/10** (다음 세션이 마지막 1회차)

3차 비평은 **9회차 결과물**(현재 `docs/review/34-r1.png`)로 돌려야 한다. 위 «반영하지 않은 지적» 4건이
3차 판정 대상이고, 그 밖에는 두 비평가가 «손대지 말 것» 으로 확인해 준 항목이 매우 길다(아래).

**두 비평가가 공통으로 «Δ≤2, 손대지 말 것» 으로 확인한 항목** — 3차에서 재조정 금지:
팝업 외곽 x41..1038(998) · 상단 y429 · 하단 테두리 · 헤더 밴드 90 · **타이틀 «축복» 잉크 x501..578 × y458..504(픽셀 일치)** ·
배너 박스 y568..668(h101)·우단 x989 · Lv 알약 137×68 @x111 · 진행바 우단 923~925·채움비 0.74 ·
카드 3열 x76..372 / 391..688 / 707..1003 · 카드 y799..1246 · 헤더 밴드 78 · 헤더 잉크 높이 32 ·
`+20%` 잉크 116×32 · 타이머 알약 214×97 · 타이머 잉크 169×25 · 보너스 탭 442 @x318 · 보너스 바 x75..1002 ·
하단 안내문 674×37 @x202 · 초록 스트립 952×252 @x64 · 헤드라인 잉크 376×53 우단 952 · CTA 버튼 267 @x694..960 ·
**닫기 X 원 지름 132 @중심(539.5,1858)** · 카드 하단 하이라이트 9px · 크림 별 워터마크 톤 224,200,168 · 전 요소 수평 중심 539.5±1.5

## 7. 남은 문제 / 넘기는 것

- **아트 대기** — 측정표 §11 «아트 필요» 표 5건(핑크 캠페인 리본 · 카드 아이콘 3종 · 보너스 바 금화+셰브론 ·
  초록 스트립 일러스트). 전부 이모지·CSS 로 **ref bbox 를 차지하게만** 만들어 뒀다.
- 초록 스트립 좌측 일러스트는 이모지 3개 나열이라 ref 의 «날개 달린 3원형 뭉치»(≈325×200)와 형태가 다르다 — 아트 교체 대상.
- `S.bless` 는 «축복 레벨» 상한이 없다. 지속시간이 무한정 늘어나므로 밸런스 단계에서 상한(예: Lv.10 = 75분)을 정할 것.

---

## 8. 3차 비평 (10회차 = 라운드 상한) — **E 6 / F 5** · 세션 `sess-0828-16672` (워커 B, 2026-08-25)

캡처 `34-r10.png`(비평 대상) → 수정 → `34-r10c.png`(최종). 게이트 **VERIFY34 51/51 · SMOKE PASS · 콘솔 0**.

| 비평가 | ① 배치 | ② 크기비율 | ③ 여백 | ④ 정렬 | ①~④ 최저 | (⑤/⑥) |
|---|---|---|---|---|---|---|
| E | 8 | **6** | 7 | 8 | **6** | 7 / 7 |
| F | 8 | **5** | 7 | **5** | **5** | 8 / 8 |

### 8-1. 2차에서 «엇갈려 보류»한 4건 — 전부 결론이 났다

이번 회차의 핵심 성과다. 2차의 엇갈림은 **두 비평가가 서로 다른 요소를 재고 있었기 때문**이었다.

| # | 2차 상태 | 3차 + 자체 실측 결론 | 처리 |
|---|---|---|---|
| 1 | 스트라이프 주기 C «73» vs D «90.5» | **둘 다 맞았다.** ref 는 카드와 보너스바에 **서로 다른 주기**를 쓴다. 자체 raw 덤프: 카드 `row 890` 밝은 띠 시작 x=126·217·307 → **주기 90.5**(밝 48/암 43). 보너스바 `row 1460` → **주기 72**(밝 34~35/암 36~38). C 는 보너스바를, D 는 카드를 재고 있었다 | ✔ 카드 90.5 유지 · **보너스바만 92→72** 로 분리 |
| 2 | 카드 열 간격 C «21» vs D «18» | **둘 다 아니다. E·F 독립 일치로 ref 15.** F 가 엇갈림의 원인까지 설명 — ref 헤더 밴드가 본체보다 좌3/우2 인셋이라 **헤더 행에서 재면 20~21 로 읽힌다**. 피치 315 는 3자 모두 일치하므로 간격 15 ⇒ 카드 폭 300 | ✔ 간격 17→**15** · 카드 폭 298→**300** |
| 3 | 헤더 상단 반경 D «18→36» (1명뿐) | **D 방향이 맞았고 폭은 더 컸다.** 자체 아크피팅(좌상단 4점 + 좌하단 3점) R≈45~48 · E 42 · F 41.1(헤더)/44.5(본체) | ✔ 24→**44** (3자 합의값) |
| 4 | Lv 진행바 높이 D «48» vs C «Δ≤2» | **E 48 · F 48~49 로 독립 일치** | ✔ h51→**48** · top 634→636 · 테두리 5→6(코어 36) |

### 8-2. 3차에서 새로 잡힌 최대 결함 — **사선 스트라이프가 좌우 반전(거울상)이었다**

F ④-1 이 지적했고 **자체 raw 덤프로 확증**했다:

| | y+4 만큼 내려갈 때 경계 x | 방향 |
|---|---|---|
| ref (y886→894) | 177 → 173 → 169 | **x+y=const («/»)** |
| 우리 10회차 전 (y802→810) | 211 → 215 → 219 | x−y=const («\\») |
| 우리 수정 후 | 239 → 235 → 231 | **x+y=const («/») — 일치** |

각도는 양쪽 다 정확히 45° 라 «각도만» 보면 통과처럼 보이는데, **카드 3장 + 보너스바 ≈930×600px 전면이 거울상**이었다.
`repeating-linear-gradient(45deg …)` → **`-45deg`**. 1줄 수정으로 ④ 최대 감점 요인이 사라진다.

### 8-3. 10회차에 반영한 것

| # | 지적 | 누가 | 반영 · 검증 |
|---|---|---|---|
| 1 | 스트라이프 거울상 | F | ✔ `45deg`→`-45deg` (4개 그라디언트). 캡처 재측정으로 ref 와 동일 방향 확인 |
| 2 | 보너스바 주기 92→72 | F(+2차 C) | ✔ 축 26.2/50.9. ref row1460 실측(밝 34~35/암 36~38) |
| 3 | 카드 반경 −46~50% | E·F | ✔ 24→44 |
| 4 | 보너스바 반경 25.9→51 | F | ✔ 24→**52**. 자체 아크피팅 3점 R=52.5/52.9/55.2 로 F 지지 |
| 5 | 카드 폭·열간격·우측 패딩 | E·F | ✔ 폭 300 · 간격 15 · 컨테이너 928→930(우단 1002→1004, 좌우 인셋 15 대칭) |
| 6 | Lv 진행바 h51→48 | E·F | ✔ top 636 · 테두리 6 · 코어 36 |
| 7 | 닫기 X 가 5px 위 | E·F | ✔ ref 원 중심 y1863(양쪽 독립 일치), 우리 1858.5 → `position:relative;top:5px` 로 **레이아웃을 건드리지 않고** 그리기만 이동(팝업 top 345 유지) |
| 8 | 보너스바 아이콘 잉크가 좌측으로 34px 모자람 | E | ✔ 같은 마스크로 중간 밴드 실측: ref x139..246(108) vs 우리 x180..250(71) → 코인 fs 140→150 · 상자 좌 40→2 · 셰브론 좌 132→125 → **147..237**(좌단 Δ41→**Δ8**) |

### 8-4. 반영하지 않은 것 (E·F 가 엇갈림 — «엇갈리면 움직이지 마라»)

- **핑크 리본 높이**: E «ref 317×138 @y436..573» vs F «316×146 @y428..573». 하단 573 은 일치하나 **상단이 436 vs 428 로 8px 엇갈린다**.
  게다가 자체 실측(ref x=200 수직두께 **66**)은 «두께를 키우는» 처방과 충돌한다 — 두께를 74 로 키우면 수직 76.6 이 돼 실측을 벗어난다.
  bbox 를 키우려면 길이·각도를 건드려야 하는데 2차에서 겨우 맞춰 둔 마젠타 화소수(15066 vs ref ~15300)가 다시 어긋난다. **다음 라운드로 넘긴다.**
- **안내 배너 높이**: F «91→98» vs E «ref 900×99 / 우리 898×100 = 양호». 정반대. 유지.
- **보너스 탭**: E «h55, top +5» vs F «h59, top +2». 높이·위치 둘 다 엇갈림. 유지.
- **카드 헤더 밴드 좌3/우2 인셋**: F 만 지적. E 는 «헤더 폭 297 = 본체 297» 로 사실상 반대. 유지 — 3차 확인 대기.
- **닫기 X 지름**: E «132→139» vs F «118×117 = 우리와 동일 크기». 위치(+5)만 양쪽이 일치해 위치만 옮겼다.

### 8-5. 회차 소진 — **10/10 → ⏸ 보류(최고 6점)**

①~④ 최저가 5~6 으로 8 에 미달한 채 화면당 누적 상한 10회를 채웠다. 2차 폴리시 라운드(+10회 리셋)에서 이어받을 것.

**다음 라운드가 가장 먼저 볼 것** (이번에 «엇갈림» 으로 남긴 것 = 8-4 전부, 그리고):
1. F ④-2 헤더 밴드 인셋 · F ② 다이얼로그 외곽 반경 39→44 · 보너스 탭 반경 24→34 — **반경 계열이 아직 남아 있다.**
2. E ② 초록 스트립 좌측 아트 폭 305→398(−23%) · 스트립 반경 30→16 — 아트 자리 계열.
3. E ⑤ **딤 휘도가 ref 대비 2배 이상 어둡다**(ref 55~65 vs 우리 0~30) — 통과 항목은 아니나 배경 아트가 전멸해 ①③ 판독을 방해한다.

---

## 9. 2차 폴리시 라운드 — 11회차 비평 **G 6 / H 7** · 세션 `sess-0947-6206` (워커 D, 2026-08-25)

10회차에서 «상한 10회 소진 → ⏸ 보류» 로 넘어온 화면을 2차 라운드(+10회 리셋)로 이어받았다.
**회차는 이어서 센다 — 11회차부터.** 캡처 `34-r11.png`(채점 대상) → 수정 → `34-r12.png`(12회차 채점용).

> ⚠ 인수인계 시점의 함정 하나 — **10회차의 수정분은 한 번도 채점된 적이 없었다.**
> `34-r10c.png` 는 «10회차 수정 뒤 최종 캡처» 일 뿐 비평가에게 준 적이 없다(E·F 는 `34-r10.png` 를 봤다).
> 그래서 11회차는 «새 수정» 이 아니라 **«10회차 수정분의 첫 채점»** 으로 시작했다.

| 비평가 | ① 배치 | ② 크기비율 | ③ 여백 | ④ 정렬 | ①~④ 최저 | (⑤/⑥) |
|---|---|---|---|---|---|---|
| G | 8 | **6** | **6** | 7 | **6** | 7 / 8 |
| H | 8 | 7 | 8 | 8 | **7** | 7 / 8 |

10회차(E 6 / F 5) 대비 **F→H 쪽이 5→7 로 올라왔다.** 남은 감점은 아래 9-2 로 수렴한다.

### 9-1. 신설한 도구 — `tools/m34.py`

ref(1080×2340)와 캡처(1080×2280)를 **«cap y = ref y − 84»** 로 같은 좌표계에 놓고
① 마스크 bbox ② 행/열 색 전이점 ③ 모서리 원 최소자승을 한 번에 뽑는다.
`python3 tools/m34.py all` 이 주요 항목 ref↔cap 대조표를 찍고, `row/col/mask/arc` 로 개별 실측을 한다.
**비평가 엇갈림은 이걸로 판정한다** — 이번 회차에 4건을 결론냈다.

### 9-2. 엇갈림 4건 — 자체 raw 덤프로 전부 판정

| # | G | H | 자체 실측 | 결론 |
|---|---|---|---|---|
| 1 | 닫기 X 검정 테두리 «ref 9~10px, 우리 7px» | «ref 는 검정 링이 **없다**, 실루엣 Ø≈120» | ref `row 1863`: 좌 x479(48,23,26)→488(223,77,88) 로 **부드러운 붉은 가장자리**, 우 599(106,34,38)→601(32,32,32) **1~2px**. 순검정 링 없음 | **H 확정.** 링 제거(132→**120**, 1px 테) |
| 2 | 타이머 알약 «ref 81 vs 우리 87 (+7%)» | «ref 219×99 vs 우리 214×98 — 합격» | 정반대라 판정 불가 | **유지** (엇갈리면 움직이지 마라) |
| 3 | 카드 헤더 밴드 인셋 | 인셋 «좌 0 / 우 0» | 카드2 헤더 x392..687(296) vs 본체 391..689(299) = **1~2px** | **G·H 모두 «인셋 아님»** → 10회차 F 의 «좌3/우2» 는 **코너 반경 구간 오독**으로 폐기 |
| 4 | (언급 없음) | Lv 알약↔진행바에 «없어야 할 9px 크림 갭» | `row 660`: ref 는 알약테 244 → 바 249 로 갭 0, **우리도** 알약테 239..248 → 바 249 로 갭 0 | **반영 안 함** (해당 행에서 재현 안 됨) |

### 9-3. 11회차에 반영한 것

| # | 지적 | 누가 | 반영 · 검증 |
|---|---|---|---|
| 1 | **핑크 리본 bbox 가 −13~14px 짧다** (10회차가 «엇갈림» 으로 미뤄 둔 1순위) | G·H·10회차 F | ✔ 아래 9-4. **308×132 → 315×144** (ref 316×144, Δw1·Δh0·Δx0·Δy0) |
| 2 | **초록 스트립 좌측 일러스트가 절반 이하** (양쪽 1순위) | G(323×201) · H(292×190) | ✔ 아래 9-5. 이모지 → CSS 작화. **145×145 → 319×221** (ref 323×224) |
| 3 | 보너스 탭 상단 반경이 절반 이하 | G(ref≈33) · H(ref≈36) | ✔ 20→**35**. 자체 아크 최소자승 37.6(30px 창 편향 −2.5 보정) |
| 4 | 보너스 탭 폭·노출 립 | G(446/60) · H(448/59) | ✔ 폭 442→**446** · top 752→750 · h 58→**60**, 바 1252→**1254**(h 152→150) ⇒ 노출 립 55→**59** |
| 5 | 다이얼로그 외곽 모서리 반경 | G(ref≈42, 우리 36) · H(ref≈42, 우리 37) **독립 일치** | ✔ `.bls` 44→**49** · `.bls-head` 36→**41** |
| 6 | 카드 헤더 밴드가 2~3px 얕다 | G(Δ3) · H(Δ2) 방향 일치 | ✔ 78→**80** + 카드 내용물 `top` −2 로 제자리 유지 |
| 7 | CTA 헤드라인 잉크 h 53 (ref 59) | G ⑥ 최대 오차 | ✔ fs 56→**62** · top 24→19 |
| 8 | CTA 버튼 라벨 h 42 (ref 45) · 버튼 테두리 7 (ref 9~10) | H ⑥ · G ② | ✔ fs 46→**49** · border 6→**9** |
| 9 | 프로모 좌측 아트가 7px 우/하로 밀림 | 자체 실측 | ✔ `.ic` left 73→68 · top 13→8 |

### 9-4. 핑크 리본 — «두께를 키워라» 는 처방이 틀렸다

E·F(10회차)·G·H 넷 다 «리본 높이가 13~14px 모자라다» 고 했고, 처방은 전부 «두껍게» 였다.
그런데 **열 두께 프로파일**을 뜨면 정반대의 그림이 나온다:

| x | 60 | 100 | 150 | 200 | 250 | 300 | 330 |
|---|---|---|---|---|---|---|---|
| ref | **81** | 67 | 67 | 65 | 66 | **77** | **57** |
| 우리(11회차 전) | 67 | 68 | 67 | 68 | 68 | 64 | 42 |

**중앙 밴드(x=100~250)는 이미 일치**한다. 모자란 것은 **양 끝** — 좌단의 접힘(fold)과 우단의 제비꼬리다.
두께를 74~77 로 키웠다면 중앙이 ref 를 벗어나 ② 가 더 나빠졌을 것이다.

처방: 밴드 두께 66 은 그대로 두고 **박스를 310×66 → 318×76 으로 키운 뒤 밴드를 상단 86.8% 로 고정**,
남는 아래 13.2% 를 좌단 플레어 꼭짓점(`12% 86.8% → 0 100%`)으로 쓴다.
배치는 회전 후 bbox 를 해석적으로 풀었다 — `rotate(-15deg)` 에서 잉크중심이 박스중심에서 `(-3.5,+5.3)` 이므로
박스중심을 frame(194.0,495.2), 즉 `.bls` 기준 `left:-13 / top:105` 에 놓으면 잉크중심이 ref(190.5,500.5)에 맞는다.
문구는 밴드가 박스 상단 66/76 이라 `translateY(-5px)` 로 밴드 중심에 재정렬했다.

결과 — 좌단 67→**75**(ref 81) · 우단 64→68(ref 77) · **bbox 315×144 @ x33..347 · y429..572 (ref 316×144 @ x33..348 · y429..572)**.

### 9-5. 초록 스트립 좌측 아트 — 이모지는 «아트 자리 규칙» 을 지킬 수 없다

«아트 자리 규칙» 은 **레퍼런스 bbox 를 정확히 차지**하라는 것인데, `⚔️❤️⚡` 3자는 헤드리스에서
**단일 145×145 덩어리 하나로만** 잡혔다(열 덩어리 분석: ref `[(137,459,323)]` vs 우리 `[(231,375,145)]`).
이모지 폰트 가용성에 잉크 bbox 가 좌우되므로 «정확히 차지» 를 **보장할 수 없다**.

→ 폰트 의존을 끊고 `radial-gradient` 5겹으로 직접 그렸다(측정표 §11 의 «날개 달린 3원형 뭉치» 형태 그대로).
좌우 날개 타원 2개 + 하단 원 2개 + 상단 원 1개로 **323×224 를 채운다**. 색은 축복 3종(공격/체력/속도)에 맞춰 뒀고,
아트가 오면 `background` 한 줄만 갈아끼우면 된다.

> **교훈(다른 화면에도 재발한다)**: 이모지를 «아트 자리» 로 쓰면 bbox 가 폰트에 좌우된다.
> ref bbox 를 반드시 차지해야 하는 자리는 **CSS 도형(gradient/clip-path)으로 그려라.**
> 잉크가 몇 덩어리로 잡히는지는 «열 덩어리 분석»(마스크 열합 → 연속 구간)으로 확인한다.

### 9-6. 게이트 갱신

`verify34.js` 의 J8·J9·J11 기대값이 **낡은 쪽**이었다 — 구현이 아니라 검사를 고쳤다(32번 교훈).
- J8 보너스탭 `318,1197,442x58` → **`317,1195,446x60`**
- J9 보너스바 `75,1252,930x152` → **`75,1254,930x150`**
- J11 닫기 X `중심 540,1864 Ø132` → **`중심 540,1863.5 Ø120`**

**VERIFY34 51/51 PASS · SMOKE PASS · 콘솔 0.**
(G2 «새로고침 후에도 축복 유지» 는 실행이 느리면 축복 타이머가 만료돼 **간헐 실패**한다 — 재실행하면 통과.
 다음 세션이 만나면 검증기 결함이지 구현 결함이 아니다.)

---

## 10. 12회차 비평 **I 7 / J 7** · 같은 세션 `sess-0947-6206`

캡처 `34-r12.png`(11회차 수정분) → 수정 → `34-r13.png`(13회차 채점용).

| 비평가 | ① 배치 | ② 크기비율 | ③ 여백 | ④ 정렬 | ①~④ 최저 | (⑤/⑥) |
|---|---|---|---|---|---|---|
| I | 8 | **7** | 8 | 8 | **7** | 8 / 8 |
| J | 8 | **7** | 8 | 9 | **7** | 8 / 7 |

추이: 10회차 E6/F5 → 11회차 G6/H7 → **12회차 I7/J7**. 두 비평가 다 ② 가 단독 최저다.
①③④ 는 8~9 로 올라왔으므로 **8점을 막는 건 ② 하나**다.

### 10-1. 11회차의 오판 2건이 드러났다 — «비평가 1명만의 수치» 를 반영하면 이렇게 된다

| # | 11회차에 한 것 | 12회차 판정 | 조치 |
|---|---|---|---|
| 1 | CTA 버튼 테두리 6→**9** (G 단독 «ref 9~10px») | **I «ref 좌6·상6·하6» · J «ref 6 vs 우리 9» 독립 일치** — G 가 틀렸다 | ✔ **6 복귀**. 자체 row1700: ref 7px / 우리 6px |
| 2 | 닫기 X 링 제거 (H 확정, Ø132→120) | 방향은 맞았으나 **살짝 작았다** — I «ref 실루엣 Ø120~121» | ✔ Ø120→**122**, 림 1px→2px(어두운 적색) |

> **교훈**: 한 비평가만 낸 «두께/테두리» 수치는 반영 전에 **반드시 자체 row 덤프로 확인**한다(34-⑧).
> 11회차에 G 만 보고 9px 로 키운 탓에 12회차를 한 번 태웠다.

### 10-2. 12회차에 반영한 것 (전부 «두 비평가 이상 일치» 만)

| # | 지적 | 누가 | 반영 · 검증 |
|---|---|---|---|
| 1 | CTA 버튼 테두리 9→6 · 높이 118→115 · top 112→113 | I(114) · J(115) | ✔ 자체 row1700 검정 두께 ref 7 / 우리 **6** (수정 전 9) |
| 2 | CTA 라벨이 버튼 안에서 5px 아래 | I(−3.5 vs +1) · J(ref 1673..1717 / 우리 1678..1722) | ✔ `padding-bottom:10px` 로 5px 올림 |
| 3 | 초록 스트립 모서리 반경이 크다 | **G(ref≈17) · H(ref≈20) · I(ref≈18~19) 3인 수렴** | ✔ 30→**20** (LESSONS 52-④ «같은 쪽으로 몰리면 움직인다») |
| 4 | 진행바 트랙이 Lv 알약 아래로 안 파고든다 | I(트랙 좌단 역산 **233** vs 우리 250) · J(«채움 종료 ref **748** / 우리 752») | ✔ `left 183→167 · w 682→698`(우단 925 불변). 자체 row660 채움 종료 **ref 747 / 우리 747**(수정 전 752), «3/4» 중심 584→576(ref 577) |
| 5 | 닫기 X 실루엣이 3px 작다 | I | ✔ Ø120→122 · 림 2px |

### 10-3. 반영하지 않은 것 (엇갈림 — «엇갈리면 움직이지 마라»)

| 항목 | I | J | 자체 실측 | 결론 |
|---|---|---|---|---|
| 핑크 리본 수직 두께 | «ref x100=67 · 우리 68 (Δ1)» | «ref 54 · 우리 68 (+26%)» | ref x100 **67** · x150 67 · x200 65 | **I 확정, J 이상치.** 유지 (J 의 1순위는 오탐) |
| 초록 스트립 좌측 아트 bbox | «ref 323×224 · 우리 319×220» | «ref 300×184 · 우리 319×220 (+20%)» | ref **323×224** | **I 확정, J 과소.** 유지 |
| 보너스 탭 상단 반경 | «ref 37 · 우리 26 → 키워라» | «ref ≈33 · 우리 ≈35 → 이미 충분» | 창 편향이 커서 판정 보류 | **정반대 → 유지** |
| 다이얼로그 외곽 반경 | «ref 34 · 우리 27» | «ref ≈34 · 우리 ≈35» | 측정법이 서로 달라 절대값 비교 불가 | **정반대 → 유지**(11회차에 44→49 로 이미 한 번 키웠다) |
| 보너스바 아이콘 23px 좌편향 | 지적 | 언급 없음 | — | 단독 → 유지 (10회차에 실측으로 맞춘 자리) |
| 타이머 시계 아이콘 높이 −17% | 지적 | 언급 없음 | 이모지라 폰트 의존 | **아트 대기** (34-⑦ 대상) |
| 카드 헤더 밴드 79~78 vs 우리 80 | ref 79 (Δ1) | ref 78 (Δ2) | — | 1~2px → 유지 |

### 10-4. 다음 라운드(13회차)가 먼저 볼 것

**①③④ 는 8~9 로 통과권이다. ② 만 남았다.** ② 를 8 로 올릴 후보 (전부 «반경·두께» 계열):
1. **보너스 탭 상단 반경** — I «ref 37 / 우리 26» vs J «ref 33 / 우리 35». 제3 비평가로 결선을 내야 한다.
   판정법: 마스크 bbox 가 아니라 **행별 인셋 프로파일에 원 피팅**(LESSONS 20-2) 으로 ref·우리를 **같은 창 크기**에서 뽑아 비교.
2. **다이얼로그 외곽 반경** — 위와 같은 이유로 엇갈림. 같은 방법으로 결선.
3. **타이머 칩 내부**(시계 아이콘 높이·숫자 잉크) — 이모지 의존이라 **CSS 작화 전환**이 답이다(34-⑦ 을 이 자리에도 적용).
4. **카드 제목:타이머 위계** — J «ref 33:22 = 1.50 → 우리 31:25 = 1.24». ⑥ 이지만 ② 체감의 원인이라고 J 가 지목했다.

---

## 11. 13회차 비평 **K 6 / L 6** · 세션 `sess-0012-13161` (워커 A, 2026-08-26)

캡처 `34-r13b.png`(12회차 수정분을 **현재 main 기준으로 재캡처**한 것) → 수정 → `34-r14.png` → 추가 수정 → `34-r15.png`(14회차 채점용).

| 비평가 | ① 배치 | ② 크기비율 | ③ 여백 | ④ 정렬 | ①~④ 최저 | (⑤/⑥) |
|---|---|---|---|---|---|---|
| K | 8 | **6** | 7 | 8 | **6** | 7 / 6 |
| L | 8 | **6** | 7 | 8 | **6** | 6 / 6 |

추이: 10회차 E6/F5 → 11회차 G6/H7 → 12회차 I7/J7 → **13회차 K6/L6**.
12회차의 «7» 에서 내려온 것이 아니라, **12회차 뒤에 다른 세션이 올린 변경(117 축복 레벨 등)이 섞인 현재 main 을
새 비평가 2명이 처음 본 값**이다. K·L 둘 다 ② 가 단독 최저이고 ①④ 는 8 로, 12회차와 같은 그림이다.

> ⚠ `34-r13.png`(12회차 세션이 남긴 파일)는 그 뒤 main 이 움직여 **현재 코드와 다르다.** 13회차는 재캡처본
> `34-r13b.png` 로 채점했다. 다음 세션도 «앞 세션이 남긴 채점용 캡처» 를 그대로 쓰지 말고 **한 번 재캡처해서
> 차이를 확인**할 것(팝업 안쪽만 diff 하면 30초면 된다).

### 11-1. 12회차가 «엇갈림» 으로 미뤄 둔 반경 2건 — 결선 도구를 만들어 끝냈다

11·12회차가 이 두 항목에서 계속 갈린 진짜 원인은 비평가가 아니라 **우리 측정기**였다.
`m34.py arc` 는 모서리 창 안의 경계점 **전체**에 원을 최소자승하므로, 창에 직선변이 섞이는 비율만큼 반경이 밀린다.
11회차는 이걸 «창 편향 +2.5» 라는 **상수**로 빼려 했는데(그래서 ref 37.6 → 35 로 적었다), 편향은 상수가 아니다.

신설 `tools/r34.py` — LESSONS 20-2 처방대로 **행별 인셋 프로파일에 원을 피팅**한다.
모서리에서 j번째 행의 인셋 `e(j)` 를 재고 `e(j) = r − √(r²−(r−j)²)` 모델에 r 을 0.5px 격자로 맞춘다.
**검증**: 우리 CSS 35 를 넣으면 35.0 이 나온다(편향 0). 이 상태로 ref 를 재면:

| 항목 | ref | 우리(수정 전) | 판정 | 조치 |
|---|---|---|---|---|
| 보너스 탭 상단 반경 | **42.5 / 42.5**(좌/우, rms 1.0) | 35.0 / 35.0 (CSS 35) | 우리가 **−18%** | 35 → **42** |
| 다이얼로그 외곽 반경 | **44.0 / 44.5**(아래 두 모서리, rms ≤0.94) | 47.0 / 47.0 (CSS 49, 편향 −2) | 우리가 **+6%** | 49 → **46**(헤더 41 → 38) |

- 탭은 **K «ref 42 / 우리 34»· L «ref 42 / 우리 33» 이 독립적으로 같은 값**을 냈다. 3자 일치.
- 외곽은 K «우리가 +2.7 크다, 손대지 마라» · L «우리가 2 작다, 감점 대상 아님» 으로 여전히 갈렸지만,
  자체 측정이 K 와 같은 방향이고 수정 뒤 **ref·cap 이 같은 코드에서 둘 다 44.25 로 Δ0** 이 됐다.
- 다이얼로그 **위쪽** 두 모서리는 헤더 밴드·핑크 리본·딤 뒤 전투 씬이 창을 오염시켜 rms 가 3 이상으로 뛴다.
  **아래 두 모서리만 쓴다** — `r34.py run_all` 에 그렇게 박아 뒀다.

### 11-2. 하네스 결함 — 타이머 문자열이 회차마다 달랐다 (거짓 지적의 근원)

`cap34.js` 는 `addInitScript` 에서 `Date.now()+82000` 으로 축복 만료를 심었다. 표시되는 값은
**페이지 로드·클릭에 걸린 가변 시간을 뺀 나머지**라 회차마다 `00:01:20` 이 나오다 `00:01:21` 이 나오다 했다.
ref 카드 3장은 **00:01:20 / 00:01:21 / 00:01:22** 다. 마지막 글자가 `0` 이냐 `1` 이냐로 숫자 잉크 폭이 **7px** 흔들리고,
그래서 K·L 이 둘 다 «숫자열 폭 ref 142 / 우리 124 = −12.7%» 라고 적었다 — **우리 쪽이 `…:21` 이었을 뿐이다.**

→ 렌더 시점 기준으로 `S.bless.exp` 를 다시 찍어 문자열을 고정했다(`hms()` 가 `Math.ceil` 이라 79.5/80.5/81.5초를 준다).
같은 문자열끼리 다시 재니 **ref 142 / 우리 139 (−2%)** 로, 애초에 거의 맞아 있었다.
**교훈: 시간·랜덤이 들어간 하네스는 «렌더 시점 기준으로 다시 심어» 고정하라.** 안 그러면 비평가 2명이
같은 거짓 수치에 독립적으로 도달해서 «2인 일치» 라는 가장 강한 채택 근거를 만들어 낸다.

### 11-3. 타이머 칩을 «시계» 와 «숫자» 로 분해했다

12회차까지 `<i>` 하나에 «⏱ 00:01:20» 이 통째로 들어 있어 **시계만 키울 수가 없었다**
(그래서 12회차 §10-3 에서 «시계 아이콘 높이 −17%» 가 «아트 대기» 로 밀렸다).
`<b class="ck">⏱</b>` + `<i>숫자</i>` 로 갈랐다.

| 항목 | ref | 12회차 | 13회차 |
|---|---|---|---|
| 시계 잉크 bbox | 33×39 | 30×31 | **34×40** |
| 시계 세로 중심 | 1093 | 1088 | **1092.5** |
| 칩 좌변→시계 | 14 | 25 | **13** |
| 시계↔숫자 간격 | 6 | 12 | **6** |
| 숫자 콜론 중심거리 | 44.0 | 41.5 | **44.0** |
| 칩 내용 전체 폭 | 181 | 165 | **179** |

- 시계를 `<i>` 밖으로 빼면 흰 채움·검정 외곽선을 **못 물려받아 «윤곽선만 있는 유령 글리프»** 가 된다(잉크 마스크에 아예 안 잡힌다). 같은 `color`·`-webkit-text-stroke` 를 다시 줘야 한다.
- 숫자는 **크기가 아니라 자간** 문제였다 — 글리프 폭은 ref 18 / 우리 17 로 같고 피치만 23 vs 20.
  `scaleX` 를 키우면 글리프가 뚱뚱해진다. `letter-spacing:.8px`(+ 마지막 글자 뒤 자간을 빼는 `margin-right:-.8px`)로 콜론 거리 Δ0.
- ref 는 칩 내용이 칩 중심(225)에서 **5px 왼쪽**(중심 220)에 앉는다 → `.tm{padding-right:20px}`.

### 11-4. 13회차에 반영한 나머지 (전부 «K·L 2인 일치 + 자체 실측 확인»)

| # | 요소 | ref | 수정 전 | 수정 후 | 근거 |
|---|---|---|---|---|---|
| 1 | 안내 배너 우측 반경 | 우상 30.5 · 우하 40.5 | 47.0 (CSS 48, 알약) | **32 / 42** | K ②1순위 «ref≈36 / 우리 50» + 자체 r34.py. 좌측 두 모서리는 ref 에서 **핑크 리본에 가려** 측정 불가라 48 유지 |
| 2 | 타이머 칩 반경 | K 24 · L 25 · 자체 28 | 22 | **26** | 셋 다 «우리가 작다», 값만 갈려 가운데 |
| 3 | 각주 문구 | 674×36 | 555×35 | **674×35** | K·L 이 «같은 문자열인데 −17.7%» 로 일치 + 자체 검정 bbox 동일. 높이는 이미 맞아 `scaleX .887→1.077` 만 |
| 4 | 헤더 타이틀 «축복» | 76×45 · 중심 y397 | 63×42 · y391.5 | **76×45 · y396** | K «7.5px 위·폭 −16.7%» · L «6px 위·폭 −16%» |
| 5 | 보너스 탭 라벨 | 152×31 · 중심 y1228 | 125×28 · y1221.5 | **149×30 · y1227.5** | K·L 둘 다 «152~153×32 vs 125×28» |
| 6 | 카드 «+20%» | 119×32 | 112×35 | **118×32** | K «h +12.5%» · L «h +9%» — 이 줄만 혼자 커서 위계가 깨져 보인다고 둘 다 지목 |

### 11-5. 반영하지 않은 것

| 항목 | K | L | 결론 |
|---|---|---|---|
| CTA 버튼 높이 | «ref 108 / 우리 114 → 줄여라» | «ref 118 / 우리 115 → 키워라» | **정반대 → 유지**(12회차에 I114·J115 로 이미 115 로 내렸다) |
| CTA 버튼 테두리 | 언급 없음 | «ref 8 / 우리 6» | 단독. 12회차에 I·J 2인이 «ref 6» 으로 일치해 9→6 으로 내린 자리다 → 유지 |
| 칩 폭 | 언급 없음 | «ref 219 / 우리 214» | 자체 실측도 ref 116..334(219). Δ5(2.3%)라 **다음 회차 후보**로만 적어 둔다 |
| 카드 높이·반경·거터, Lv 알약 폭 | 지적 | 언급 없음 | 단독 → 유지(14회차에 N 이 독립 재판정 중) |
| 한글 잉크 폭 −12~18% 전반 | 지적 | 지적 | 각주·탭 라벨·타이틀은 **위에서 실제로 맞췄다.** 남은 것(카드 제목 «체력» 등)은 ⑥ 이라 통과를 막지 않는다 |

### 11-6. 게이트 · 신설 도구

- `node verify34.js` → **VERIFY34 PASS 51/51**. 칩 분해에 맞춰 C6·I3 의 선택자를 `.tm>i` → `.tm` 로 바꿨다(시계까지 같이 본다). `cap34.js` 의 `_txt.tm` 도 같이.
- `node tools/smoke.js` → **SMOKE PASS** · 콘솔 에러 0.
- 신설 `tools/r34.py`(모서리 반경 결선 — 행별 인셋 프로파일 원 피팅) · `tools/tm34.py`(타이머 칩 시계/숫자 분리 실측).
- ⚠ `verify34.js` 는 **다른 브라우저(캡처·smoke)가 동시에 돌면 간헐 FAIL 1/51** 이 난다. 연속 3회 PASS 로 확인했다. 게이트를 돌릴 때는 캡처와 겹치지 마라.
