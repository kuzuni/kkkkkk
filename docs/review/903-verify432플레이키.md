# 903 — `tools/verify432.js` [R2-1600] 플레이키 (자 플레이키 · T1)

> 1회차 · 2026-09-05 · sess-0013-31244 루틴 워커 A
> **제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정식·문턱·재는 창 0칸.** 고친 것은 `tools/verify432.js` 한 파일이다.

## §0 등재문과 결론

등재문(2026-09-04, sess-2227-15725 워커 B — 813 10회차 회귀 스윕의 곁다리):

> `[R2-1600]` 이 플레이키 — 같은 트리에서 잉크 높이 **348 ↔ 419** 로 흔들린다.
> 판정식은 `looseInk.h < glowInk.h - 20` 이고 `background-size` 를 풀면 `closest-side` 의 ry 가
> 275 → 245 로 줄어 **작아지는 것이 정답**인데 실행에 따라 되레 큰 값이 잡힌다 ⇒
> 측정 시점(레이아웃·폰트 안정 전)이나 `background` 재계산 경쟁을 의심한다.
> 먼저 `probe` 로 «몇 번에 한 번» 인지부터 세라(873 처방).

**결론**: 등재문의 «흔들린다» 는 사실이고(무보정 **20회 중 9회 빨강**), **의심 두 갈래는 둘 다 기각됐다.**
레이아웃·폰트도, `background` 재계산도 아니다 — **글로우는 20회 전부 한 픽셀도 안 흔들렸다.**
흔들린 것은 **차분 bbox 에 섞여 든 남의 띠 하나**이고, 그 띠를 두 얼굴로 만든 것은
**Chromium 의 부분 리라스터(타일 재사용)** 다. 처방은 **브라우저 깃발 한 줄**이고 창·문턱·판정식은 한 칸도 안 건드렸다.

## §1 재현 — 몇 번에 한 번인가 (`tools/probe903.js` 신설)

873 처방대로 «세는 것» 부터 했다. `probe903` 은 **판정하지 않는다** — `verify432` 와 **같은 순서로**
(A · A/A · N(글로우 끔) · L(size 풀기) · L2 · 끝판) 찍고 그 차분의 **y 띠**(연속 행 묶음)를 전부 찍는다.

| # | 조건 | 빨강 | `glowInk.h` | `looseInk.h` |
|---|---|---|---|---|
| ① | 무보정(현행 자와 같은 조건) 6회 | **4/6** | 391 (전부) | 420 / 349 |
| ② | 워밍판 2장 — **칠이 안 바뀌는 태그**(`outline:0`) 6회 = 실질 무보정 | **4/6** | 391 (전부) | 420 / 349 |
| ③ | 무보정 8회 | **1/8** | 391 (전부) | 349 / 420 |
| ④ | 워밍판 2장 — **칠을 바꾸는 태그**(`display:none`) 6회 | **5/6** | 391 (전부) | 420 / 349 |
| ⑤ | **`--disable-partial-raster` 6회** | **0/6** | 391 (전부) | **349 (전부)** |

⇒ **무보정 계열(①②③) 20회 중 9회**가 빨갛다(45%). «5회 돌려 보니 같더라» 로는 반증이 안 되는 딱 그 폭이다(873).
④ 는 처방 후보였고 **5/6 으로 더 나빴다**(§3 ⓑ). ⑤ 만 축을 죽였다.
⚠ ② 를 «워밍» 으로 세지 않는 이유 — `outline:0` 은 **그림이 그대로**라 리라스터를 아예 안 일으킨다(판 한 장도 못 소진한다).

## §2 흔들린 것은 글로우가 아니다 — 띠 분해가 한 번에 갈랐다

`looseInk`(= L ↔ N 차분)의 띠를 전부 찍으면 **본체는 20회 전부 같은 자리**다:

```
y1000(1행 35px) · y1002~1346(h345 · 93,790px) · y1348(1행 2px)      ← 초록 회차 (bbox h=349)
y929~934(h6 · 53px · x248~829 · Δ19) · y964(1px) · y977(202px · Δ1)  ← 빨간 회차에만 더 붙는 띠 (+256px)
+ y1000 · y1002~1346 · y1348                                          ← 본체는 **똑같다** (bbox h=420)
```

- `glowInk.h` 는 **20회 전부 391** · `looseInk` 본체 띠도 **20회 전부 `y1002~1346`**.
- 349 → 420 의 **71px 은 전부 «위쪽 남의 띠»** 다. 차분 문턱이 `d > 0`(1단위)이라 **한 띠가 bbox 를 71px 늘린다.**
- 그 자리는 배수 토글 바 **`.stabs.sp4`**(1600 프레임에서 y896~984 · x248.7~829.3 — 700·701·713 부품)다.
  글로우 축과 아무 상관이 없다.

## §3 처방 후보를 **실측으로** 둘 기각했다

- **ⓐ «재는 창을 글로우 상자로 좁힌다»** — `::before` 상자는 y**929**~1419(864×490)라
  그 띠가 **상자 안**이다. 창을 상자로 좁혀 다시 재도 8회 중 1회 여전히 빨갛다(`probe903` [5] 열).
  ⇒ **기하로는 못 가른다.**
- **ⓑ «재기 전에 라스터 세대를 소진한다(워밍판)»** — 칠을 바꾸는 태그로 버리는 판 2장을 먼저 찍어도
  **5/6 빨강**. 이때 `A↔끝판` 은 0(태그 없는 판끼리는 늘 같다)인데도 L 과 L2 가 서로 달랐다
  ⇒ 뒤집힘은 «한 번 일어나고 굳는» 것이 아니라 **태그가 붙은 판마다 다시 던지는 주사위**다.
- **ⓒ «시간·애니메이션»** — 태그를 **한 번도 안 붙이고** 6.5초 동안 10판을 찍으면 판 사이 차분이
  **전부 0**이다(freeze 뒤 rAF·타이머는 이미 끊겨 있고 CSS 애니메이션도 이 자리엔 없다).
  ⇒ 등재문이 의심한 «측정 시점(레이아웃·폰트 안정 전)» 은 아니다.

## §4 뿌리 — 부분 리라스터(타일 재사용)

스타일 태그를 붙였다 떼면 레이어가 더러워진다. 그때 그 띠의 타일이 **전체 재라스터되는지 부분 재사용되는지가
판마다 갈려** 같은 화면이 **±1~19 단위 두 얼굴**을 갖는다(같은 띠의 서명 `10555547 ↔ 10555465`).
태그가 안 붙은 판은 늘 한 얼굴이라 **789 가 세운 A/A 항은 초록인 채**로 §R2 만 조용히 흔들렸다.

**처방 = `launch(chromium, { args: ['--disable-partial-raster'] })` 한 줄.**
켜고 6회 재현 — **빨강 0/6 · `looseInk.h` 전부 349 · 판 사이 차분 0 · L↔L2 0.**

## §5 무르게 푼 수리가 아님을 두 겹으로 못박았다

1. **아무것도 안 넓혔다** — 재는 창(789 `roi`)·문턱(`d > 0`)·판정식(`loose.h < glow.h − 20`)·
   `[3-a]` 진폭 5·`[3-b]` 388~400 **전부 그대로**다. 제품 `index.html` 은 **0줄**.
2. **[0-g] 신설 — 결정성을 이름으로 지킨다.** 네 상태를 다 찍은 **뒤** 처음 상태로 돌아와 한 장 더 찍어
   첫 판과 비교한다(A/A 는 처음 두 장만 본다). 깃발이 빠지면 **이 항이 먼저** 빨개진다 —
   무보정 실측에서 이 차분은 **8회 중 8회 비영**(256px · `y929~984`)이었고 깃발을 켜면 **6/6 정확히 0** 이다.
   ⇒ §2·§3·§R2 가 «글로우가 흔들렸다» 는 거짓말을 하는 대신 여기서 멈춘다(789 의 A/A 를 **판 전체**로 늘린 것).

## §6 결과

- `node tools/verify432.js` → **VERIFY432 45/45 PASS**(44 → 45 · `[0-g]` 신설) · **5회 연속 초록**
  (같은 자가 수리 전에는 무보정으로 20회 중 9회 빨갰다).
- `node tools/probe903.js` **신설**(판정 없음 · `--warm`·`--nopr` 로 처방 후보를 켜고 끄며 센다).
- 회귀 — 제품이 0줄이라 대상은 «같은 화면(89 유물 소환)» 계열이다:
  `verify866` **16/16 PASS** · `verify813` **24/24 PASS** · `node tools/smoke.js` **SMOKE PASS**.

## §7 다음 세션에게 (곁다리)

같은 병이 **다른 자에도 있을 수 있다.** 조건은 셋 다 갖춘 자다 —
① 한 페이지에서 **스타일 태그를 갈아 끼우며** 여러 판을 찍는다 ② 판끼리 **화소 차분**을 센다
③ 문턱이 «몇 단위» 로 작다.
⚠ **«자 플레이키 계열» 이 한 뿌리라고 생각하지 마라** — 바로 옆 902(`verify583` `[C-big]`)는
같은 날 **다른 뿌리**(자의 정수 양자화 · `Math.round` 잔차)로 닫혔다(sess-2358-17704 워커 D).
셋을 다 갖춘 자만 이 눈으로 다시 본다. 344·574·709·825·854·855·870·871·872·873·876·884 계열 중
**«한 페이지에서 태그를 갈아 끼우며 찍는»** 자가 그 후보다.
**확인은 한 줄이면 된다** — 그 자의 `launch(...)` 에 `{ args: ['--disable-partial-raster'] }` 를 넣고
같은 횟수를 다시 세어 본다. 등재는 PROGRESS **907**.
