# 710 — 스킬 이펙트 중복 제거 (주인 지시 2026-09-02 02:40)

> 주인 원문: «스킬 이펙트 중복 된거 있으면 중복 안되게 하기»
> 지시서 [3]-**(가)+(다)** — 레퍼런스 대조가 아니라 «전수 표 + 자» 작업이라 회차 루프의 본체는
> 비평이 아니라 **측정**이다. 채점(대조 캡처 비평 2인)은 2회차 몫으로 남긴다.

## 1회차 (2026-09-02, sess-0213-9148 루틴 워커 A)

### ① 재현부터 했다 (338 규칙) — 등재문 ⓐ 는 사실이고 ⓑ 는 **이미 0** 이었다

`tools/probe710.js` 신설. 재는 법은 «찍힌 픽셀 diff»(350·752 선례) —
같은 프레임을 «투사체 있음 / 없음» 으로 두 번 `draw()` 해 달라진 화소만 그 스킬의 잉크로 세고,
**자리·각도·스핀을 27종에 대해 같은 값으로 고정**해 마스크를 서로 정렬시킨 뒤 쌍별 IoU 를 잰다.

⚑ **이 자는 색을 안 본다.** 412 교훈(«색만 가르면 회색조에서 못 갈린다»)을 자에 새긴 것이다 —
마스크는 «배경과 달라진 화소» 라 색을 바꿔도 안 바뀐다. 그래서 IoU 1.000 은
«회색조에서 완전히 같은 그림» 이라는 뜻이다.

**수리 전 실측 (PROBE710 4/6 FAIL 2)**

| 절 | 실측 |
|---|---|
| [1-b] 가족 서명 중복 묶음 | **6개 · 그 안의 종 17종** — `slash=multi=whirl=gale` / `shuri=stone` / `curve=ice=arrow=lance` / `bolt=drain=frost` / `rico=bounce` / (`orbit=aura` = 관측 artifact, 아래 ③) |
| [2] 실루엣 IoU ≥ 0.90 쌍 | **18쌍** — 그 중 16쌍이 **IoU 1.000**(완전 동일), `shuri↔stone`·`stone↔boomer` 만 0.994 |
| [4] 한 시전 겹침 스폰 | **0** ← ⓑ 는 수리 전부터 참이었다 |

뿌리는 한 줄로 적힌다 — **`shotBody()` 의 분기가 다섯 개뿐이었다**:
`slash` · `shuri|boomer` · `ice` · `boom|meteor|flask` · `else`(공용 구슬).
17종이 그 다섯 그림을 나눠 쓰고 색만 달랐다.

⚠ **[4] 의 첫 판정식은 틀렸었다** — «같은 자리» 만 보면 다발형(표창 8발·폭풍 12발)이
총구 한 점에서 나므로 `shuri:28`·`gale:66` 이 잡힌다. 겹침은 «분간할 수 없는 두 장» 이므로
**자리 + 속도**를 같이 봐야 한다. 고친 뒤 27종 전부 0 이고, 그것이 정답이다(338 — 이미 참인 것).

### ② 처방 — 표 하나, 읽는 자리 셋 (제품 `index.html`)

**`SK_FX`** 신설 — 710 이 지시한 «24종 발동 이펙트 전수 표» 그 자체다.
한 스킬 = 한 서명(`fam` 렌더러 가족 · `sh` 실루엣 키)이고, 같은 서명을 둘이 쓰면 그것이 중복이다.

배선은 **한 자리**다 — `castSkill()` 의 114 등급 태깅 바로 옆에서 이번 시전이 만든
투사체에 `sh`, 번개에 `bk` 를 찍는다. ⚑ **기존 13분기와 `castGeneric` 을 한 글자도 안 건드렸다**
(402 의 «사본을 지운다» 와 같은 원칙 — 표는 하나고 그리기는 그 키만 읽는다).

읽는 자리는 셋뿐이다: `shotBody()`(투사체·접촉 잔상) · 번개 폴리라인(`boltAlt`) · `skAreaFx()`(광역 2종).

**갈라 놓은 그림 (형상 우선 · 색은 보조 — 412 방식)**

| 계열 | 기준형(그대로) | 새로 그린 것 |
|---|---|---|
| 검격(호) | `arc` 검기 | `rake` 멀티 검기(호 세 겹) · `cross` 주변 참격(X 교차) · `wing` 폭풍의 칼날(호+뒷깃 3줄) |
| 투척 | `star` 표창 | `rock` 돌팔매(울퉁불퉁한 육각 바위) · `bmr` 부메랑(굽은 막대) |
| 찌르기 | `spear` 얼음창 | `moon` 곡선탄(초승달) · `arrow` 화살(대+촉+깃) · `lance` 천벌의 창(창날+가로 가드) |
| 불 | `fire` 화염구 | `rockfall` 운석(바위 코어+불꼬리) · `bottle` 화염병(병 실루엣+심지 불) |
| 구슬 | (공용 구슬 = 드론 탄) | `gem` 반사탄(마름모 결정) · `ball` 도약탄(공+이음매) · `swirl` 나선탄(소용돌이) |
| 번개 | `zap` 연쇄 번개 | `tether` 흡혈의 검(떨지 않는 굵은 흡수관+흐르는 구슬) · `shard` 서리 연쇄(곧은 마디+얼음 결정) |
| 광역 | — | `ray` 심판의 빛(여덟 갈래 빛살) · `shock` 창세의 폭발(3겹 동심 충격파) |

⚑ **광역 2종은 스프라이트를 못 바꾼다** — 아틀라스가 `explosion.png` **한 장뿐**이라
새 폭발 그림이 없다. 그래서 412 방식대로 **형상 층을 하나 더 얹어** 갈랐다(`skAreaFx`).
스프라이트·피해 반경·색·판정은 한 글자도 안 건드렸다.
⚠ 링 억제(`fxRing` 4회차 «같은 자리·같은 크기»)를 반드시 피해야 한다 — 빛살 8개는 자리·반경이
같아 억제에 걸리면 **한 개만 남는다**. `nd`(억제 끄기)를 켜는 것이 그 이유다.

⚑ **번개 기준형 `zap` 은 한 글자도 안 건드렸다.** 15~17회차가 손수 맞춘 감쇠·지터 곡선이라
새 두 종만 `boltAlt()` 로 갈라 보내는 **분기 한 줄**만 얹었다.

⚑ **잔상(ghosts)에 키를 물려주는 것이 빠지면 안 된다** — 안 주면 «닿는 순간» 만 옛 공용 그림으로
튄다(114 ④ 일관성 붕괴). `verify710` [D1] 이 실제로 맞혀 보고 그것을 못박는다.

**밸런스·판정 0줄** — 자리·속도·수명·관통·피해를 읽지도 쓰지도 않는다(114·541 «그리기만» 문법).
세이브 이관 없음(KEY 불변 — 표는 런타임 상수다).

### ③ 갈래 하나를 재현이 기각했다 — `orbit ↔ aura` 는 중복이 아니다

가족 서명으로만 보면 회전검·신성 오라가 «같은 빈 서명» 으로 묶인다. 그런데 그것은
**중복이 아니라 관측이 없는 것**이다 — 둘은 `cd 0` 지속형이라 **시전 경로 자체가 없다**.

그래서 [2-b] 를 신설해 **같은 방법(찍힌 픽셀)** 으로 다시 쟀다: 그 하나만 장착하고 8프레임 굴린 뒤
마스크를 비교한다. 결과 **회전검 28,728px · 신성 오라 44,071px · 서로의 IoU 0.263** —
원래부터 다른 그림이다(칼날 공전 ↔ 불길 링). **그 둘에 대한 제품 변경은 0줄**이고,
고친 것은 자의 스코프뿐이다(338 — 이미 참인 것을 게이트로 굳히지 않는다).

⚠ 자를 만들다 두 번 헛디뎠고 둘 다 «환경/자료구조» 문제였다(제품 결함이 아니다):
- 이 저장소의 이펙트 배열은 최상위 `const` 라 **`window` 의 속성이 아니다** — `window['shots']` 는 undefined 다.
- `S.own[id]` 는 «수» 가 아니라 `{n, l}` 이다 — 수로 넣으면 `oLv()` 가 undefined 를 돌려주고
  오라 반경이 NaN 이 되어 `createRadialGradient` 가 즉사한다.

### ④ 곁다리 수리 1건 — 초승달의 «무게중심»

`verify752`([A4]·[E2] — 발 자리 **중심 ±44 캔버스px** 의 잉크)가 `curve` 하나에서 빨개졌다.
제품 회귀가 아니라 **새 그림의 자리** 문제다: 두 호가 모두 `+x` 쪽이라 잉크가 통째로
원점 밖(실측 local **+10..+26** · 캔버스 dx **66..127**)에 앉는다.

⚠ **부호를 눈대중으로 정하지 마라** — 1회차에 `+6` 으로 밀어 **오히려 더 밖으로** 내보냈다.
`ctx.arc(…, true)` 는 «긴 쪽» 이 아니라 각도가 줄어드는 **짧은 쪽**을 그린다.
bbox 를 직접 찍어 무게중심 `+18` 을 확인하고 `translate(-18, 0)` 으로 옮겼다
⇒ dx **−30..+31** · 중심 ±44 안 잉크 **0 → 4,153**. 형상은 한 획도 안 바뀌었다.

### ⑤ 「한 밴드」 — 갈라도 한 세트로 읽혀야 한다 (412)

가르는 대가로 «제각각» 이 되면 안 되므로, 새 실루엣도 잉크 지름 22~30 게임px ·
같은 선 굵기 층(굵은 흐린 밑선 + 가는 밝은 코어)을 지켰다. 두 종은 형상 탓에 덩치가 빠져
같은 형상의 흐린 겹을 뒤에 깔아 맞췄다(초승달 1,901 → 3,793 · 화살 3,829 → 5,423).

**종별 잉크 최대÷최소 = 7.13** (`verify710` [E1] 문턱 12).

### ⑥ 게이트

| 자 | 결과 |
|---|---|
| `tools/probe710.js` **신설** | **7/7 PASS** (수리 전 4/6 — [1-b] 6묶음 · [2] 18쌍) |
| `tools/verify710.js` **신설** | **14/14 PASS** — [A] 표 · [B] 배선 · [C] 그리기 · [D] 잔상 · [E] 밴드 · [F] 겹침 · **[R] 되돌림 3항** |
| 되돌림 시험 | 태깅 블록만 지운 사본에서 [B1] **20종 어긋남** · [C1] **IoU 1.000**(slash↔multi) · [D1] 잔상 키 없음 ⇒ 자가 실제로 옛 결함을 잡는다 |

**회귀** — `verify99` 52/52 · `verify114` 46/46 · `verify541` 68/68 · `verify698` 19/19 ·
`verify625` 24/24 · `verify752` **17/17**(위 ④ 수리 후) · **SMOKE PASS**.

### ⑦ 채점 — 2~9회차 (대조 캡처 + 비평 2인 독립, 같은 세션)

`tools/cap710.js` 신설 — «수리 전에 한 그림이던 무리» 를 **한 줄씩** 모아 한 장에 놓는다
(411 규약: **나란히 안 놓으면 어긋남이 안 보인다**). 실제 게임 캔버스에 실제 `shotBody()` 로
그리므로 배율·알파 층·바닥이 화면과 같다.

| 회차 | 비평가 | ① 분간 | ② 통일 | ③ 덩치 | ④ 의미 | 최저 | 그 회차에 고친 것 |
|---|---|---|---|---|---|---|---|
| 2 | A·B | 6·7 | 7·6 | 5·5 | 6·7 | **5** | 캡처가 가로로 잘려 8종이 안 보였다 |
| 3 | C·D | 7·9 | 5·7 | 4·6 | 6·6 | **4** | 5번째 줄이 하단 DOM 위젯에 가려 3종이 안 보였다 |
| 5 | E·F | 7·8 | 5·6 | 5·6 | 6·7 | **5** | 세로를 상수로 적어 또 잘렸다 |
| 6 | G·H | –·7 | –·7 | –·7 | –·7 | **7** | 캔버스에서 **재서** 나누자 처음으로 17종이 다 보였다 |
| 8 | J·K | 8·8 | 7·7 | 8·6 | 7·8 | **6** | 외곽선 규격·덩치 정렬 |
| 9 | L·M | 8·7 | 6·7 | 6·6 | 6·8 | **6** | `bounce` 이음매 |

⚑ **다섯 회차를 통째로 태운 것은 그림이 아니라 캡처였다.** 세 번 연속 «어떤 종이 안 보인다» 가
1순위 지적이었고, 그 세 번이 각각 다른 이유(가로 잘림 → DOM 위젯 가림 → 세로 상수)였다.
뿌리는 하나다 — **캔버스 크기를 상수로 적었다.** 이 저장소의 게임 세로는 `#app` 높이 클램프에
따라 프레임마다 달라(실측 1140 이 아니라 **998**) 상수는 반드시 틀린다. 9회차의 자는
`cvs.height / SC` 로 **재서** 나누고 DOM 위젯을 통째로 숨긴다 — 줄 수를 늘려도 저절로 맞는다.

**닫은 지적 (수치가 근거를 준 것)**
- ③ 줄 안 덩치 — 표창 대비 돌 +85% · 얼음창 대비 초승달 −51% · 화염구 대비 화염병 −52% 였다.
  형상은 한 획도 안 바꾸고 **반지름·후광·획 굵기만** 손봐 줄 안 편차를 ±15~20% 로 좁혔다.
- ④ 화살이 «양방향 화살(↔)» 로 읽혔다 → 꼬리 깃을 **뒤로 눕혀** 방향이 한쪽으로만 서게 했다.
- ④ 천벌의 창이 «십자(+)» 로 읽혔다 → 가로 가드를 버리고 **길고 가는 대못**으로 다시 뽑았다.
- ④ 부메랑이 «ㄱ자 갈고리» 였다 → 같은 세 점을 **곡선**으로 이어 바나나꼴로 바꿨다.
- ④ 주변 참격이 «정지한 십자» 였다 → 굵기가 고른 두 선을 **뿌리에서 두껍고 끝으로 가는 날**로.
- ②④ 도약 연쇄탄이 «여는/닫는 괄호» 로 읽혔다(L·M 공통, 9회차 1순위) — **내 잘못이었다**:
  이음매 호를 중심에서 ±13 인 원으로 그렸는데 몸통 반지름이 11 이라 **호가 몸통 밖으로 나갔다**.
  몸통을 채운 원으로 세우고 이음매를 그 **안쪽**으로 클립해 넣었다.

### ⑧ 닫지 못한 것 — 회차 마감(주인 지시 없이 더 밀지 않는다)

**①(분간 = 이 작업이 지시받은 축)은 8·8·8·7 로 회차마다 안정적으로 8 언저리**인데,
②(통일감)·③(덩치)이 6~7 에서 안 올라가 **최저 8 을 못 채웠다.** 그대로 적는다.

⚠ **다만 ②③ 지적은 회차마다 서로 어긋나고, 자와도 어긋난다** — 9회차 M 은 «파생형이 기준형보다
얇다(멀티 검기가 검기보다 −45%)» 고 적었는데 자로 재면 **멀티 검기 7,127 > 검기 6,043** 으로
오히려 크다. 같은 줄을 두고 J 는 ③ 8, K 는 ③ 6 을 줬다. LESSONS 의 «유령» 패턴 그대로라
**눈의 지적을 자로 검산하지 않은 채 값을 흔들면 두 회차가 서로를 되감는다**(114 12·13회차 선례).

남은 실체는 하나로 좁혀진다 — **17종이 네 가지 «렌더링 문법»(가는 선화 / 단색 평면 /
파스텔 후광+본체 / 방사 발광)을 섞어 쓴다.** 그런데 그 넷을 정한 것은 이번 작업이 아니라
**안 건드린 기준형 다섯**(검기=선화 · 표창=평면 · 얼음창=후광 · 화염구=발광 · 반사탄=평면)이다.
여기를 통일하려면 기준형을 다시 그려야 하고, 그것은 «중복 제거» 가 아니라 **연출 규격 재설계**라
710 의 밖이다 ⇒ **792 로 등재**(T4 · 상세는 PROGRESS 행 · 788 에서 등재 경쟁으로 번호 개정).

`stone ↔ bounce` 는 IoU 0.785 로 남은 최악 쌍이다(문턱 0.90 안 · 둘 다 «덩어리» 계열).
