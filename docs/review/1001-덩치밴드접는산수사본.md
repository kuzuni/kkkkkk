# 1001 — `verify982` [C1] 이 792 [E1] 덩치 밴드의 접는 법을 인라인으로 다시 적는다 (네 번째 사본)

**T1 «버그(잠복 · 자)» · 제품 `index.html` 0줄 · 자 3벌(982 이관 · 995 이관 · 1001 신설)**
세션 `sess-1643-4006` (루틴 워커 C · 2026-09-06) · 1회차 · 지시서 [3]-**(가)** — 레퍼런스 대조가 아니라
«접는 법이 한 벌인가» 라 **비평가 없음**.

---

## 1회차 — 사본을 지웠다

### 착수 시점 실측

| 자 | 착수 | 마감 |
|---|---|---|
| `verify982` | 14/14 PASS | **14/14 PASS** (값 한 자리도 안 바뀜) |
| `verify995` | 11/11 PASS | **11/11 PASS** ([B4] 이관) |
| `verify1001` | 없음 | **10/10 PASS 신설** |
| `verify989` | **9/10 FAIL** | 9/10 FAIL — **내 몫이 아니다**(아래 §곁다리, 1003 등재) |

### 무엇이 결손이었나 — «값» 이 아니라 «자리»

792 [E1] 이 묻는 «투사체 덩치가 한 밴드 안인가» 의 **접는 법**(중앙값 ±25%)과 **견주는 법**
(되돌림 판과 대 봐서 «내 처방이 안에 있던 종을 밖으로 냈는가»)이 자마다 인라인으로 다시 적혀 있었고,
**원본이 눈금을 갈 때마다 사본만 뒤처져 «792 가 버린 자» 를 지켰다.**

* **989** — 눈금이 «본체» → «본체 + 종이 제 손으로 깐 반투명 부품» 으로.
  뒤처진 사본이 밖 4종(whirl·arrow·bounce·gale)으로 빨개졌다 — 넷 다 989 가 한 획도 안 건드린 종이다.
* **995** — 눈금이 «대각» → **«최대 변»(`bulk`)** 으로.
  대각 = 최대변 × √(1+(최소변/최대변)²) 이라 **정사각형 종을 +41.4% 부풀려** 밴드를 인위적으로 좁힌다
  (같은 17종: 대각으로는 밖 **0종**, 최대 변으로는 밖 **5종**).
* **998** — 그 산수를 부품 `tools/bulk998.js` 한 벌로 빼고 `verify981` [D2] 를 갈아 끼웠다.
* **1001(이 작업)** — 남아 있던 **마지막(네 번째) 사본이 `verify982` [C1]**(173~186행)이다.

**지금은 안 틀렸다** — 995 가 이 자리를 `bulk` 로 이관해 뒀다. 그래서 이 작업의 값 변화는 **0** 이다
(수리 전후 [C1] 출력이 «밖 [shuri·stone·arrow·meteor·lance] · 스프레드 2.04→2.1배 · 중앙값 112px» 로 동일).
**고친 것은 자리다** — 792 가 눈금을 또 갈면 982 만 옛 눈금을 지키게 되고, 그 사고를 이미 세 번 겪었다.

### 처방 — 한 줄 갈아 끼우기 (등재문 그대로)

```
- const bulkBand = (rs) => { …중앙값·밴드·스프레드 산수 6줄… };
- const cur = bulkBand(rows);  const nRows = …;  const nb = bulkBand(nRows);
- const newOut = cur.out.filter(i => nb.out.indexOf(i) < 0);
+ const B998 = require('./bulk998');
+ const bulkBand = (rs) => B998.band(rs, BULK_TOL, 'bulk');
+ const cur = bulkBand(out.rows);  const nb = bulkBand(neg.out.rows);
+ const newOut = B998.newOut(cur, [nb]);
```

* 되돌림 판이 하나뿐이라 `newOut` 의 «판 **전부**에서 안» 은 «이 판에서 안» 과 같다 — **판정 동치**다.
* 밴드 경계(`cur.lo`~`cur.hi`)를 메시지에 한 줄 더 찍는다(부품이 이미 계산해 둔 값 — 새 산수 0줄).
* **눈금(`bulk`)과 밴드 반폭(`BULK_TOL`)은 그대로 792 가 고른다.** 부품은 픽셀도 문턱도 안 만든다.
  `BULK_TOL` 상수는 981 선례대로 자에 남긴다(998 이 981 에서 그렇게 뒀다 — 이 작업의 범위가 아니다).

### ⚑ 이관이 이 작업의 본체다 — `verify995` [B4] 가 **사본이 있어야 초록**이었다

`verify995` [B4] 는 982 의 인라인 산수를 **문자열로** 물었다:

```js
ok(/r\.bulk < m \* \(1 - BULK_TOL\)/.test(s982) && /\[C1\] 대가/.test(s982), '[B4] …')
```

즉 **사본을 지우는 순간 995 가 «남의 정리» 때문에 빨개진다.** 333 처방대로 **자리를 비우지 않고
방향만 옮겼다** — 묻는 문장(«982 도 같은 눈금을 읽는가»)은 그대로 두고, 묻는 대상을 부품 호출로 바꿨다:

```js
ok(/B998\.band\(\s*rs\s*,\s*BULK_TOL\s*,\s*'bulk'\s*\)/.test(s982) && /\[C1\] 대가/.test(s982) &&
   /rows\[i\]\[k\]\s*<\s*lo/.test(sB998) && /const k = key \|\| 'bulk';/.test(sB998), '[B4] …')
```

⚠ **부품 소스까지 같이 보는 것이 핵심이다.** 호출 이름만 물으면 `band()` 가 빈 껍데기여도 초록이다 —
그래서 «부품이 실제로 그 열쇠로 접는가» 를 같은 항에 넣었다.

### ⚠ `verify995` 의 인라인 접기는 사본이 아니다 — 안 건드렸다

995 의 `fold()` 는 **최대변·대각·기하평균·잉크√ 를 나란히 견주는 것**이 그 자의 물음이라,
부품 하나로 묶으면 물음 자체를 잃는다. 등재문의 ⚠ 그대로 두었고, `verify1001` [B5] 가
**«남아 있는지»** 를 반대로 못박는다(다음 워커가 «정리» 라며 지우지 못하게).

### 새 자 — `tools/verify1001.js` (10/10 PASS · 브라우저 없이 도는 절 + [R3])

| 항 | 무엇을 묻나 |
|---|---|
| [B1] | 982 [C1] 이 부품을 쓴다 — `B998.band` 1곳 · `B998.newOut` 1곳 · **인라인 접기 0곳** |
| [B2] | [C1] 이 최대 변(`bulk`)으로 접고 옛 눈금(`.own`/`.diag`)을 판정에 안 읽는다 |
| [B3] | **부품이 실제로 접고 견준다** — 껍데기면 [B1] 이 헛초록이 된다 |
| [B4] | 995 [B4] 가 **옮긴 물음**을 묻는다(옛 인라인 앵커 0곳) |
| [B5] | ⚠ 995 의 인라인 접기는 **남아 있다** |
| [C1] | **인구조사** — 이 축을 인라인으로 접는 자가 허용 목록과 정확히 같다(실측 5자) |
| [R0] | 되돌림 앵커가 실제로 있다(998 [B4] 교훈 — 없으면 아래 셋은 빈 시험이다) |
| [R1] | 되돌린 사본에서 [B1] 이 **실제로** 빨개진다 |
| [R2] | 그 사본이 있으면 인구조사가 5자 → **6자**로 늘어난다 |
| [R3] | **995 에게 그 사본을 직접 물렸다** — 995 [B4] FAIL · 종료 코드 1 |

[R3] 을 위해 `verify995` 에 **인자 손잡이**를 하나 달았다(`node tools/verify995.js <982사본>`) —
998 이 `verify998` 에 준 것과 같은 꼴이다. 인자가 없으면 종전대로 `tools/verify982.js` 를 읽는다.

**인구조사 허용 목록(이유까지 같이 적는다 — 이유 없이 늘리면 다섯 번째 사본이다)**

| 자 | 왜 인라인이어도 되나 |
|---|---|
| `bulk998.js` | 부품 그 자신 |
| `verify792.js` | 원본 — [E1] 판정과 [E1n] «네 접는 법» 표를 여기서 만든다 |
| `verify995.js` | 나란히 견줌 — 묶으면 물음을 잃는다 |
| `verify981.js` | (기록) 한 줄 — 판정은 부품이 하고 옛 눈금은 기록으로만 찍는다(998 규약) |
| `verify989.js` | **다른 축** · 옛 이름 `DIAG_TOL` — 989 자신의 «상자» 축 |

---

## 검증 (지시서 [3]-(가))

* `verify982` **14/14 PASS** — [C1] 출력값이 수리 전과 **한 자리도 안 다르다**(판정 동치의 가장 짧은 증거)
* `verify995` **11/11 PASS** (이관 [B4])
* `verify1001` **10/10 PASS 신설**
* `verify981` · `verify998` · `smoke` — 아래 «회귀» 절
* 문법 검사 `node --check` 3파일 통과

## 곁다리 관측 1건 — **1003 등재** (내 몫이 아님을 대조로 확인)

`tools/verify989.js` 가 **9/10 FAIL**([R2])이다. **내 변경 전 트리에서도 같은 9/10 · 같은 수**임을
`git stash` 대조로 직접 확인했다(338·344 규칙):

```
FAIL [R2] 989 직전 트리에서 옛 눈금(본체만)은 «밖 1종» 으로 초록인데
          새 눈금은 «밖 4종» 으로 빨갛다 (boom:229.1 · bounce:218.7 · meteor:228.6 · flask:229 · 중앙값 145)
```

`verify989` 는 `verify982`·`verify995` 를 **한 줄도 안 읽는다**(`require` 는 `path`·`fs`·`pwlaunch` 셋뿐).
[R2] 는 «989 직전 트리» 사본을 구워 견주는 항인데, 그 뒤 802·981·982 가 크기를 바꾸면서 그 사본의
**옛 눈금 쪽도 이제 빨갛다** — 되돌림이 «축 하나씩» 이라야 뜻이 있다는 376-④ 자리다.
지시서 [1] «딱 1개만» 대로 손대지 않고 **1003 으로 등재만** 했다.
