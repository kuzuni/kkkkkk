# 804 — `tools/verify654.js` `[B3]` 1건 실패(23/24): `grantNow` 의 `S.gold +=` 가 미분류

**1회차 (2026-09-02, sess-0814-28698 루틴 워커 A) — 판정 ⓑ(게이트 부패) · 제품 `index.html` 0줄 · `VERIFY654` 23/24 FAIL → 28/28 PASS**

지시서 [3]-**(가)** 기계+자 — 비평가 없음.

---

## 1. 재현 (338·344 규칙 — 처방 전에 먼저 돌린다)

```
FAIL [B3] `S.gold +=` 자리가 **전부 분류돼 있다**
     — ["claimAllMail","claimMail","claimOffline","giveReward","grantNow","killEnemy","renderCoinPage","step"]
     · 미분류 ["grantNow@28131"]
```

등재문의 값(28044)과 줄 번호만 다르고(그 사이 619·740·754 가 위쪽을 밀었다) **같은 한 항**이다.

**내 것이 아님도 같이 확인했다** — 이 회차의 변경은 `tools/verify654.js` 한 파일뿐이고
`tools/verify512.js`·`index.html` 은 한 줄도 안 건드렸다(`git show --name-only`).

## 2. 갈래 판정 — ⓐ(실재) 인가 ⓑ(자) 인가

등재문이 갈래 둘을 줬다: **ⓐ** 697 이 무음이어야 할 자리를 만들었다(= 제품) · **ⓑ** 자가 새 자리를 못 읽는다.

**ⓑ 확정.** `grantNow` 는 697(«상점 구매 전부 즉시 지급», 주인 지시 2026-09-02 00:25)이 세운
**비전투 지급** 경로다. 654 본문이 그은 경계는 «전투·보스·클리어 계열은 무음, **비전투 512 연출은 유지**» 이고,
`grantNow` 안에는 그 유지가 코드로 적혀 있다:

```js
const kinds = [g && 'gold', c && 'dia', r && 'relic', mi && 'mile'].filter(Boolean);
if(o.at) fxAt(o.at);
if(kinds.length) fxReward(null, kinds);
```

⇒ 이 자리는 **유음이 정답**이고, 제품은 처음부터 옳았다. 빨간 것은 자의 분류표뿐이다.

## 3. 처방 — «이름» 이 아니라 «방향» 을 등재한다

등재문 처방 ⓐ(«분류표에 «무음이어야 하는가» 와 함께 등재») 를 골랐다. ⓑ(«함수 단위로 무르게 풀기»)는
등재문이 금지한 길이고, 654 가 잡은 «던전 클리어 보상은 그 표를 한 번도 안 지난다» 를 다시 못 보게 된다.

⚠ **표에 이름 한 줄만 적는 것은 «누른 항» 이다**(328~330 이관 교훈). 그러면 나중에 누가
`grantNow` 를 무음으로 바꿔도 표는 그대로 초록이라 654 의 경계가 조용히 죽는다.
그래서 분류표를 `{ mute, why }` 로 바꾸고 **묻는 항을 세 겹으로 더 넣었다**:

| 항 | 무엇을 묻나 | 값 |
|---|---|---|
| `[B3]` (기존) | 모든 `S.gold +=` 자리가 표에 있다 | 9자리 · 미분류 0 |
| **`[B3m]` 신설** | 표의 **방향**이 소스와 맞는다(무음 자리엔 앞 3줄에 `fxSilent('gold'`, 유음 자리엔 없다) | 무음 3 · 유음 5 · 어긋남 0 |
| **`[8]` 신설** | 소스가 아니라 **찍힌 코인** — `grantNow({g})` 가 512 연출을 그대로 낸다 | 코인 6개 `["fxl"]` · 버스트 O |
| **`[R5]`·`[R6]` 신설** | 양방향 되돌림 | 아래 §4 |

`giveReward` 만 `mute:'경로'` 다 — 무음 여부를 부르는 쪽이 정하고 그건 `[B1]`·`[B2]` 가 이미 본다.

**`[8]` 이 따로 필요한 이유**: `grantNow` 는 `giveReward` 를 **한 번도 안 지난다**.
기존 경계 표본 `[6]`(우편)은 `giveReward({gold})` 라서, 초록이어도 상점 구매 자리가 무음일 수 있었다.

## 4. 무르게 풀지 않았다는 증거 — 되돌림 두 방향

제품 파일은 안 건드리고 **소스 사본**에 한 줄을 넣거나 빼서 각각 뒤집는다.

```
ok [R5] `grantNow` 를 **무음으로 바꾸면** [B3m] 이 즉시 빨개진다 — ["grantNow@28132(무음)"]
ok [R6] 반대로 전투 자리(`killEnemy`)의 `fxSilent` 를 **빼면** 그것도 빨개진다 — ["killEnemy@23602(유음)"]
```

옛 `[B3]`(이름만 등재)으로는 **둘 다 초록**이었다 — 그것이 이 두 항의 존재 이유다.

## 5. 게이트·회귀

| 자 | 결과 |
|---|---|
| `verify654` | **23/24 FAIL → 28/28 PASS** (신설 4항: `[B3m]`·`[8]`·`[R5]`·`[R6]`) |
| `verify592` | 35/35 PASS (무수정 — 654 의 짝) |
| `verify697` | 44/44 PASS (무수정 — `grantNow` 본체) |
| `verify589` | 56/56 PASS (무수정 — 우편 경계) |
| `smoke` | **SMOKE PASS** |

## 6. 곁다리 관측 1건 — `verify512` `[R1]` 플레이키 (807 로 등재)

회귀 스윕에서 `verify512` 가 **30/31**로 한 번 빨갰다:

```
FAIL [R1] 되돌리면 «수리 전 크림» 이 화면에 실제로 찍힌다 — 되돌림 128 ↔ 정상 0   (문턱: cream > 500)
```

**내 것이 아니다** — 이 회차의 변경은 `tools/verify654.js` 한 파일이고 `verify512.js` 에는 `654` 문자열이
**0건**이다(참조 경로가 없다). 같은 트리에서 **두 번 더 돌려 31/31 PASS × 2**(크림 2,647 · 2,721)
⇒ 값이 문턱의 5배와 1/4 사이를 오간다 = **플레이키**(759·783·784·791·794 가족).
축은 «되돌린 프레임에 크림이 찍혔는가» 로 옳고, 흔들리는 것은 **언제 찍는가**로 보인다. 807 에 등재했다.
