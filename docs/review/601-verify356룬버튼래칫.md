# 601 — `tools/verify356.js` [S3] ③ 래칫·이름표 3건 빨강 «버그(게이트 부패)»

**세션** sess-0837-10287 (루틴 워커 B) · **1~2회차** · 2026-08-31

> # ⚠ 선점 인계 — **10:37Z 에 `601.lock` 이 `sess-1035-19169` 로 넘어갔다(90분 규칙).**
> 뺏긴 것이 맞다. 내 `--beat` 는 **파일만 갱신**하는데(지시서 [1] · claim.js 규약) 09:06 의 회차 커밋 이후
> **beat 를 커밋에 안 담아서** origin 의 lock 시각이 09:06 에 굳어 있었다(09:43·10:15 의 beat 는 로컬에만 남았다).
> ⇒ **다음 워커에게: 이 파일이 인계서다. 처음부터 다시 하지 마라 — 아래 §1~§6 이 이미 끝난 일이다.**
> **남은 일은 §6-4 «다음 세션 몫» 한 줄뿐**이고, 그 밖의 [S3] ③ 은 이 트리에서 **전부 초록**이다.
> 나는 **남의 lock 을 건드리지 않았다**(지시서 [4] — 601.lock 은 `sess-1035-19169` 판 그대로 두고 push 했다).
**제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정·밸런스 0줄.** 고친 것은 `tools/verify356.js` 한 파일이고,
새로 만든 것은 재현·시험기 `tools/probe601.js` 하나다.

---

## §1 재현 — 등재문 그대로 3건이 빨갛다

`node tools/verify356.js` (수리 전, `9c8565b` 나무):

```
✗ [S3] ③ 래칫 — 편차 >0.5% 인 칸이 57개(래칫 55): 새 자리가 생겼다
✗ [S3] ③ 래칫 — 자리가 16개(래칫 14): …
✗ [S3] ③ 이름표 — 등재 안 된 자리 2개:
      div#trRunes>div.tr-rn>span.rbt.b1.no>i>img.cic
      div#dunList>div.dnc.bgm-rel>div.pill>em>img.cic
```

**귀속은 산수로 딱 떨어진다.** 그 실행이 뱉은 16자리의 칸 수를 더하면 57이고,
새 두 자리(각 **1칸**)를 빼면 **정확히 55 = 옛 래칫**이다. 즉 옛 15자리는 한 칸도 안 늘었고
늘어난 것은 **새로 판정 안에 들어온 두 자리뿐**이다.

| 자리 | 칸 | 편차 | 잉크 | 그려지는 상자 |
|---|---|---|---|---|
| **A** `div#trRunes>div.tr-rn>span.rbt.b1.no>i>img.cic` | 1 | **+2.64%** | 68×106 | **53.0000**×53.0000 @ 491.5625, 1696.5000 |
| **B** `div#dunList>div.dnc.bgm-rel>div.pill>em>img.cic` | 1 | **+1.27%** | 94×107 | 54.4833×54.4833 @ 105.7494, 272.5099 |

⚠ **등재문의 «자리» 한 칸을 정정한다** — B 는 «03 던전» 이 아니라 **«03 탑»** 에 있다.
`#dunList` 를 세 서브탭(던전·레이드·탑)이 나눠 쓰기 때문에, 03 던전만 열고 재면 이 셀렉터는 **0개**이고
자는 «문턱 밖(≤0.5%)» 이라고 답한다. 1회차의 첫 시험이 실제로 그렇게 나와 «편차가 없다» 로 읽힐 뻔했다
(`probe601` §SITES 주석에 그 오측을 남겨 뒀다). 화면 귀속은 `node tools/probe418.js --screen "03"` 이 찍는다.

---

## §2 처방 전에 재현 — `tools/probe601.js` 신설 (338 규칙)

548 이 놓은 자(`probe548`)를 그대로 이어 쓰되 **축을 하나 더했다**.

* **Δw(device px)** — 편차 %는 잉크가 좁을수록 커진다. `dev` 를 원본 종횡비로 되돌려 «몇 px 인가» 를 찍는다.
* **DSF 수렴**(356 9회차) — 배율을 올려 편차가 0 으로 가면 자의 바닥, 안 줄면 기하다.
* **418 §5** — 상자를 다른 정수로 바꾸면 편차가 사라지는가.
* **⚑ 새 축: «좌표»** — 418 이 «소수 상자» 를 닫은 뒤 남는 갈래는 **소수 좌표**다(`.ps-bx` 가 그 선례 —
  상자는 정수 88 인데 행 y 가 1003.5). 548 의 `CASES` 에는 그 축을 묻는 시험이 **없었다**.
  A 는 버튼 안 `text-align:center` 로 놓인 인라인 이미지라 x 가 **491.5625** 에 앉는다 ⇒
  `position:relative;left` 로 **크기는 그대로 두고 x 만** 정수로 밀어 가른다.

### §2-1 자리 A — 23 룬 [강화] 버튼 안 화폐 (584 `TR_CUR_PX = 53`)

| 시험 | DSF | 편차 | Δw(device px) | 잉크 | 상자 |
|---|---|---|---|---|---|
| `A-base2` 바닥값 | 2 | **+2.64%** | +1.75 | 68×106 | 53.0000 |
| `A-base3` DSF 수렴 | 3 | **+1.64%** | +1.63 | 101×159 | 53.0000 |
| `A-base4` DSF 수렴 | 4 | **+1.13%** | +1.50 | 134×212 | 53.0000 |
| `A-52` 상자 53 → 52 | 2 | +1.54% | +1.00 | 66×104 | 52.0000 |
| `A-54` 상자 53 → 54 | 2 | +0.74% | +0.50 | 68×108 | 54.0000 |
| `A-xint` x 491.5625 → **492** | 2 | **+2.64%** | +1.75 | 68×106 | 53.0000 |
| `A-xint2` x 491.5625 → **491** | 2 | **+2.64%** | +1.75 | 68×106 | 53.0000 |

**읽는 법 셋:**

1. **상자는 이미 정수다.** 584 는 `curIc('rstone', TR_CUR_PX)` 로 **인라인 `width:53px`** 을 박는다 —
   418 이 닫은 «`.cic{width:1.08em}` × 소수 fs = 소수 상자» 가 **아니다**(오히려 584 가 그 자리를 정수로 만들었다).
   ⇒ 등재문이 첫 갈래로 지목한 «소수 상자» 는 **여기서 쓸 것이 없다.**
2. **좌표 몫도 아니다.** x 를 양쪽 정수로 밀어도 편차가 **소수점 둘째 자리까지 안 움직인다.**
   한쪽만 재면 «우연히 좋아진 자리» 를 못 가르므로 491·492 **둘 다** 쟀다 — 둘 다 +2.64%.
   ⇒ `.ps-bx`(정수 상자 + 소수 행 y) 계열이 아니다. **이 축을 닫은 것이 이 회차의 값이다.**
3. **DSF 가 답을 준다.** 2.64 → 1.64 → 1.13% 로 줄고, Δw 는 device px 로 1.75 → 1.63 → 1.50 이지만
   **CSS px 로는 0.88 → 0.54 → 0.38** 로 0 을 향한다. 잉크 폭에 **상수로 얹힌 AA 테** 라는 뜻이고,
   356 9회차 규칙이 «자의 바닥» 이라고 부르는 바로 그것이다.

**%가 유독 큰 이유는 결함이 커서가 아니다** — 이 잉크(68px)가 표 전체에서 **제일 좁기** 때문이다.
`cur-rstone.svg` 의 아트가 viewBox 를 `.625 × 1.000` 으로 채워(585 실측표) 53 상자 안에서 폭이 33 CSS px 밖에 안 된다.
같은 AA 한 픽셀이 넓은 아이콘에서는 0.6%, 여기서는 2.6% 로 읽힌다.

**«상자를 바꿔 초록으로 만드는 길» 은 막혀 있다.** 이웃 정수 둘 다 문턱(0.5%) 위이고(52 → +1.54 · 54 → +0.74),
게다가 상자를 건드리면 584 가 `verify584` [2-h]·[2-i] 로 못박은 **버튼 안 여백 대칭**(53px 그림 + 검정 링 5 ⇒ 안쪽 예산 64,
`vertical-align:-15.5px` 에서 상하 29.5)이 깨진다. PROGRESS 601 이 «크기는 그대로 두고 상자의 소수만 다뤄라» 라고
못박은 자리가 정확히 여기다.

### §2-2 자리 B — 03 **탑** 유물 계열 재화 알약 (585 `scale(1.14664)`)

| 시험 | DSF | 편차 | Δw(device px) | 잉크 | 상자 |
|---|---|---|---|---|---|
| `B-base2` 바닥값 | 2 | **+1.27%** | +1.18 | 94×107 | 54.4833 |
| `B-base3` DSF 수렴 | 3 | −0.85% | −0.99 | 116×117 | 50.9938 |
| `B-base4` DSF 수렴 | 4 | +0.65% | +1.01 | 156×155 | 50.9938 |
| `B-r54` 그려지는 상자 → 54 | 2 | +2.63% | +2.00 | 78×76 | 50.5410 |
| `B-r55` 그려지는 상자 → 55 | 2 | +1.27% | +1.18 | 94×107 | 55.0029 |
| `B-pre585` 585 이전 배율(1.0732) | 2 | **문턱 아래(칸 0)** | — | — | — |

1. **이미 등재된 자리와 «같은 한 부품» 이다.** `div#dunList>div.dnc.bgm-rel.lkd>div.pill>em>img.cic`(03 던전)와
   **상자 54.4833 · 잉크 94×107 · +1.27%** 가 전부 같다. 다른 것은 카드가 **잠겨 있는가**뿐이고,
   그 클래스 하나가 셀렉터 경로에 들어가 «다른 이름» 이 됐다.
2. **한 래스터 픽셀에 붙박이다.** Δw 가 DSF2·3·4 에서 +1.18 · −0.99 · +1.01 로 **부호까지 뒤집히며 1px 안**에 머문다.
   548 §4-B(`cn-cd.alert>.pn` — «래스터 1px, 상자는 이미 정수 120») 와 같은 자리다.
   그려지는 상자를 54·55 로 맞춰도 안 낫는다(54 는 **악화** — 548 §4-C 가 겪은 «선언 상자 ≠ 그려지는 상자» 함정 그대로).
3. **귀속을 되돌림으로 못박았다(338·344 규칙).** 585 이전 배율 `scale(1.0732)` 을 도로 심으면
   이 자리가 **판정 목록에서 통째로 사라진다**(칸 0 · 자리 0) ⇒ 585 가 relic 배율을 올리면서
   그려지는 상자가 50.9938 → 54.4833 이 된 것이 이 자리를 판정 안으로 데려왔다.
   585 는 저장소 주인 지시(«던전행에서도 화폐들 크기좀 키우기»)이므로 **되돌리지 않는다.**

---

## §3 수리 — 게이트 한 파일, 세 자리

### ① 래칫 두 수: 칸 55 → **57** · 자리 14 → **16**

11회차가 정한 규약(«표본이 고정이면 줄이는 쪽으로만 · 넓히려면 **자리별 귀속**을 찍어서») 을 그대로 따랐다.
**`SCREENS` 는 한 줄도 안 넓혔다** — 늘어난 둘은 그 사이에 들어온 **제품 변경**(584·585)이 데려온 자리이고,
각각 **1칸**씩이며 **새 «부품» 은 0개**다(둘 다 이미 등재된 부품의 다른 인스턴스).

### ② `KNOWN_SITES` 두 항 신설 — **이름표 + 눈금(cap)**

늘린 것이 «숨긴 것» 이 아니라는 증거를 같은 커밋 안에 넣는다:

* **이름표는 한 칸도 안 넓혔다** — 등재는 16자리이고 **17번째 자리는 여전히 빨갛다.**
* 두 항 모두 `cap: null` 이 아니라 **실측 상한**을 달았다. 548 의 경고(«안 잰 값을 적으면 그것이 헛초록») 때문에
  cap 은 잰 자리에만 적는 규약인데, 이 회차는 두 자리를 **직접 쟀으므로** 적는 것이 맞다.

**눈금은 «실측 + 래스터 반 픽셀» 로 통일해 적었다.** 여유의 근거를 느낌이 아니라 그 자리의 산수로 둔다 —
`한 device px 이 그 잉크에서 만드는 %` ÷ 2:

| 자리 | 실측(DSF2) | 1 래스터 px | 눈금 | 한 픽셀이 튀면 |
|---|---|---|---|---|
| A 룬 버튼 화폐 | +2.64% (3회 폭 **0**) | 1.51% | **3.4** | 4.15% → **빨강** |
| B 03 탑 알약 | +1.27% (4회 폭 **0**) | 1.08% | **1.8** | 2.35% → **빨강** |

⇒ 상한이 «초록으로 만드는 값» 이 아니라는 것을 **그 수 자신이 증명한다.**

---

## §4 게이트 결과

| 자 | 결과 |
|---|---|
| `node tools/verify356.js` | **114/114 PASS** (수리 전 111/114 — ✗ 3건이 전부 [S3] ③) |
| `node tools/verify584.js` (이관) | **52/52 PASS** — 584 의 크기·정합을 한 칸도 안 건드렸다는 증거 |
| `node tools/verify411.js` (이관) | **34/34 PASS** |
| `node tools/smoke.js` | **SMOKE PASS** |
| `tools/probe601.js` | **신설** — A 7시험 · B 6시험 |

⚠ **PROGRESS 601 행의 이관 목록에 있던 `verify418` 은 «파일로는 없다».**
418 의 자는 **`verify356.js` [S3] 절 자신**이고(그 절 서두가 «418 이 옛 [S3]+[S4] 를 스윕 한 벌로 합쳤다» 고 적어 뒀다),
실행체는 `tools/probe418.js` · `tools/scan418.js` 다. 이 회차는 그 스윕을 **직접** 돌렸으므로(`probe418 --screen`)
418 축의 회귀는 `verify356` [S3] ①~④ 가 그대로 대신한다 — **[S3] ① 정수 상자 5자리 · ④ 되돌림 전부 무수정 초록.**

---

## §5 남긴 것 · 다음 세션에게

1. **`probe601` 의 «좌표» 축은 다음 자리에도 그대로 쓸 수 있다.** 418 이 «소수 상자» 를 닫은 뒤
   남는 갈래는 «좌표» 와 «AA» 둘인데, 548 의 `CASES` 는 앞의 것을 안 묻는다.
   `position:relative;left` 로 **양쪽 정수**를 다 밀어 보는 것이 요령이다 — 한쪽만 재면
   «우연히 좋아진 자리» 와 «축이 무관한 자리» 를 못 가른다.
2. **자리 이름에 «상태 클래스» 가 들어가면 같은 부품이 두 이름으로 등재된다.**
   B 는 `.dnc.bgm-rel.lkd`(잠김)와 `.dnc.bgm-rel`(해금)이 **같은 한 부품**인데 이름표에는 두 줄로 앉는다.
   이름표의 값은 «수가 아니라 이름» 이라는 데 있으므로 지금은 두 줄이 맞지만,
   상태가 하나 더 붙으면(예: `.dnc.bgm-rel.on`) **또 새 이름**이 나온다는 것을 알고 있어라.
   (602 가 같은 날 [A] SCOPE 키에서 상태 클래스를 뺀 것과 **같은 병의 다른 절**이다.)
3. **화면 귀속을 먼저 찍어라.** `#dunList` 처럼 한 컨테이너를 서브탭 셋이 나눠 쓰면
   «그 화면만 열고 재는» 시험이 조용히 0개를 답한다. `node tools/probe418.js --screen "<말>"` 은
   부분 일치라 `"03"` 하나로 던전·레이드·탑을 한 번에 찍는다 — 시험 CASES 를 짜기 전에 그것부터 돌려라.

---

## §6 2회차 — 병합 뒤 트리(56화면 · 356 12회차 + 596 이후)에서 다시 닫았다

1회차는 **48화면 트리**에서 `verify356` **111/114 → 114/114** 로 닫았다. push 직전 rebase 에서
`tools/verify356.js`·`PROGRESS`·`LESSONS` 세 파일이 충돌했고, 지시서 [4] 대로 **양쪽을 모두 살렸다.**
그 사이 상류에 들어온 것이 둘이다.

* **356 12회차** — `SCREENS` **48 → 56화면**, 래칫 칸 55 → 67 · 자리 14 → 16, `KNOWN_SITES` 2자리 신설.
  ⚑ 그 회차는 **미등재 4자리를 «일부러 안 덮고» 601 로 넘겼다**(주석에 그렇게 적혀 있다) —
  그중 둘이 내가 이미 닫은 A·B 다.
* **596** — 03 카드 입장권을 **−18° 로 눕혔다**(`.dnc .sp.tk>em`).

### §6-1 병합 결과 — [S3] ③ 네 항이 전부 초록, 새 빨강 하나

병합 트리에서 처음 돌린 값: **판정 245 · 칸 62 ≤ 67 · 자리 15 ≤ 19 · 이름표 15자리 전부 등재.**
12회차가 넘긴 나머지 두 자리(`#mbox .mwell p` · `#rwCost>i`)는 **이 트리에서는 아예 안 나온다** —
그 관측은 596 이전 세계의 것이다(아래 §6-2 가 이유를 준다).

대신 **[S3] ③ 눈금 [전제]** 하나가 빨갛게 남았다:

```
✗ [S3] ③ 눈금 [전제] — 상한이 붙은 7자리 중 6자리만 이번 실행이 쟀다:
      div#dunList>div.dnc.bgm-dia>div.sp.tk>em>img.cic
```

### §6-2 뿌리 — 596 의 이관이 **주석만 들어오고 자리는 안 지워졌다**

상류 `verify356.js` 에는 이미 이런 주석이 있었다: 「↓ **596 이 뺀** `bgm-dia>.sp.tk`(cap 1.4)의 근거 —
기록으로 남긴다(**자리는 위 블록이 뺐다**)」. 그런데 **`{ sel: … bgm-dia … }` 항 자신은 그대로 남아 있었다.**
형제 두 자리(`.bgm-rel.lkd>.sp.tk` · `.bgm-gold>.sp.tk`)는 실제로 빠졌는데 **cap 이 붙은 이것 하나만** 남은 것이다.
cap 이 붙은 자리는 **[전제] 가 «이번 실행이 실제로 쟀는가» 를 묻기 때문에**, 판정에 안 들어오는 순간 곧바로 빨개진다.

**왜 판정에 안 들어오는가 — 닫힌 것이 아니라 «이 자의 스코프 밖» 이 된 것이다.**
[S3] 은 «회전 안 한 원본»의 잉크 종횡비를 기준표로 쓰는 자다. 596 이 입장권을 눕히자 회전한 그림의
잉크 bbox 가 기준표의 «상자를 얼마나 채우는가»(fw·fh)와 3% 넘게 어긋나 **«가려짐» 문턱에 걸린다.**
`probe418 --screen "03"` 실측이 그 산수를 그대로 보여 준다:

| | 판정 | 가려짐 | 입장권 자리 |
|---|---|---|---|
| 596 **전** | 18 | 0 | 3자리(−1.42 · −0.64 · −0.64%) |
| 596 **후** | 13 | 5 | **0자리** |

⇒ **정확히 그 다섯 칸이 판정에서 가려짐으로 옮겨 갔다.** 이 자리의 종횡 소유권은 **`tools/verify596.js`**
(회전각·등방 contain·bbox 를 직접 잰다 · 39/39)로 넘어갔고, [S3] 에 남겨 두면 헛초록이 아니라 **영원한 빨강**이다.
⇒ 항을 **근거와 함께 뺐다**(지시서·548 규약 «자리를 뺄 때는 실제로 닫았다는 근거를 같이 적는다»).

### §6-3 래칫 두 수 재정리

12회차가 잰 «칸 63~67 · 자리 18~19» 는 **596 이전 세계의 값**이다(입장권 다섯 칸이 아직 판정 안에 있던 때).
596 이후 트리에서 전 화면을 **두 번** 돌린 값은 두 번 다 **칸 62 · 자리 15**(등재 15자리와 정확히 일치)다.

* **자리 19 → 16 으로 도로 조였다.** 1회차 병합에서 내가 19 로 올린 것은 12회차의 596 이전 관측을
  근거로 삼은 값이라 **무효**다. 16 은 12회차가 정한 수이고 관측 15 를 여유 1 로 덮는다.
* **칸 67 은 안 건드렸다.** 12회차가 같은 트리에서 63↔67 로 **4칸 흔들리는 것**을 봤고,
  596 이후 관측은 아직 두 번뿐이다 — 조이려면 세 번째 실행이 먼저다.

### §6-4 결과 · 남은 한 줄

| 자 | 결과 |
|---|---|
| `node tools/verify356.js` (병합 트리) | **124/124 PASS** — [S3] ③ 래칫·이름표·눈금·[전제]·§R **전부 초록** |
| `node tools/verify356.js` (48화면 트리, 1회차) | 111/114 → **114/114 PASS** |
| `node tools/verify584.js` · `verify411.js` (이관) | 52/52 · 34/34 |
| `node tools/smoke.js` | SMOKE PASS |

**⚑ 다음 세션 몫 — 딱 한 줄이다.** 596 이후 전 화면 스윕을 **한 번 더** 돌려 세 번째 관측을 얻고,
셋이 다 «칸 62» 면 `RATCHET_CELLS` 를 **67 → 64**(관측 62 + 여유 2)로 **조여라.**
수만 내리지 말고 위 §6-3 처럼 **자리별 귀속**을 다시 찍어서 적을 것.

---

## §7 3회차 — 인계된 «한 줄» 을 닫았다 (칸 래칫 67 → 64)

**세션** sess-1035-19169 (루틴 워커 G) · 2026-08-31 · **제품 `index.html` 0줄 · 자 2파일**

### §7-0 선점 사고 — 90분 규칙이 **살아 있는 세션**을 뺏었다 (기록으로 남긴다)

`601.lock` 은 origin 에서 **09:06:55Z 에 굳어 있었고**, 같은 번호의 마지막 `wip(601)` 커밋도
같은 시각이었다. `node tools/claim.js --check` 가 «lock 91분 · 마지막 wip 91분» 으로 **DEAD** 를 냈고
(지시서 [-1] 2 의 두 조건이 **둘 다** 참이다) 10:37Z 에 회수했다.
그런데 워커 B 는 살아 있었다 — 09:43·10:15 의 `--beat` 를 **회차 커밋에 안 담아** 로컬에만 남겼고,
`done(601)` 커밋(작성 10:15)은 **11:13 에야 push** 됐다.

⇒ **90분 규칙은 «origin 에 보이는 것» 으로만 판정한다.** 지시서 [1] 이 «같은 커밋에서 lock 시각을
갱신하라» 고 못박은 이유가 이것이고, 이번이 그 조항의 실제 사례다(LESSONS 601 에 적었다).
3회차는 **처음부터 다시 하지 않았다** — 이 파일 서두의 인계 지시대로 §6-4 의 «남은 한 줄» 만 했다.

### §7-1 세 번째·네 번째 관측 — 네 실행이 글자까지 같다

`node tools/probe418.js`(DSF2 · 56화면)를 두 번 더 돌렸다. 596 이후 관측은 이제 **넷**이고 전부 같다:

| 실행 | 판정 | 원본비 없음 | 가려짐 | 칸 | 자리 |
|---|---|---|---|---|---|
| 2회차 ①② (워커 B) | 245 | 173 | 31 | **62** | **15** |
| 3회차 ③④ (이 세션) | 245 | 173 | 31 | **62** | **15** |

③④ 는 **자리 목록도 편차 %도 `diff` 로 대조해 차이 0줄**이다.

### §7-2 자리별 귀속 — «수만 내린 것이 아니다»

62칸의 임자는 등재된 15자리가 전부다(미등재 칸 **0**):

| 칸 | 자리 |
|---|---|
| 36 | `div#top>…>div.cbox.cDia>i>img.cic` |
| 5 | `div#psTk>…>div.ps-bx.c0>i>img.cic` |
| 4 | `div#dunList>div.dnc.bgm-rel.lkd>div.pill>em>img.cic` |
| 3 | `div#bCos>…>div.sk-btn.sk-b2.no>i.ol3>img.cic` |
| 3 | `div#shopList>…>div.cn-bn>div.gem>img.cic` |
| 2 | `div#shopList>…>div.cn-cd.alert>div.pn>em>img.cic` |
| 1 ×9 | `.rbt.b1.no` · `.cn-a2>.gm` · `#dgdIcon` · `.bgm-rel>.pill` · `.pvc.pb.ban1>.pil` · `.pvc.pg>.pil` · `.bgm-gold>.pill` · `.skd…>.nt>b` · `#ciIcon` |
| **62** | 합 |

⇒ `RATCHET_CELLS` **67 → 64**(관측 62 + 여유 2). **`RATCHET_SITES` 16 은 안 건드렸다** —
12회차가 정한 수이고, 여유 1 이 남아도 «새 자리» 는 **이름표** 항이 이름으로 잡는다.

### §7-3 12회차가 남긴 «흔들리는 자리» 는 596 이후 안 흔들린다

12회차는 같은 트리 독립 두 실행이 **칸 63 ↔ 67 · 자리 18 ↔ 19** 로 갈렸고 갈린 항이
`em#dgdIcon>img.cic` 하나라고 적었다(그래서 큰 쪽으로 적고 흔들림을 이 행에 넘겼다).
596 **이후** 네 실행에서는 그 자리가 **빠짐없이 나오고**(−1.49% · 1칸) 가려짐 수도 **31 로 고정**이다.
입장권 다섯 칸이 −18° 회전으로 판정 밖으로 나가면서 그 자리의 «판정 ↔ 가려짐» 경계도 같이 닫힌 것으로 읽는다.
⚠ 그래서 여유를 **0 이 아니라 2** 로 뒀다 — 그 한 자리가 다시 흔들려도(±1칸) 덮는다.

### §7-4 `probe601` 에 [전제] 를 세웠다 (자가 자기 사고를 다시 안 내게)

1회차가 B 를 **없는 화면**(`03 던전`)에서 재고 «판정 밖/문턱 아래» 다섯 줄을 결과로 남긴 것이
이 작업의 가장 큰 함정이었다 — 그 다섯 줄은 «편차가 작다» 가 아니라 **«아무것도 안 쟀다»** 인데
**표에서는 둘이 똑같이 생겼다.** 2회차가 화면 이름은 고쳤지만 **자에는 그 사고를 막는 항이 없어서**,
화면 이름이 또 낡으면(428 이 패스 탭을 개명했듯) 표는 다시 조용히 «괜찮다» 로 읽힌다.
⇒ 바닥값(`*-base2`)에서 그 자리가 판정 안에 없으면 그 자리의 시험 전체를 **«[전제] 무효»** 로 크게 찍는다.
⚠ 이 항은 «편차가 작은 것» 을 무효라 하지 않는다 — `base2` **하나만** 본다.

### §7-5 자

| 자 | 결과 |
|---|---|
| `node tools/verify356.js` | **134/134 PASS** — `[S3] ③ 래칫 칸 62개 ≤ 64` · `자리 15개 ≤ 16` · `이름표 15/15 등재` · `눈금 6자리` · `[전제]` · `§R 되돌림` 전부 초록 |
| `node tools/verify584.js` (이관) | **52/52 PASS** |
| `node tools/verify411.js` (이관) | **34/34 PASS** |
| `node tools/verify418.js` (이관) | **자가 없다** — 418 의 판정은 `verify356` **[S3]** 이 갖고 있고(`probe418.js` 는 측정 전용), 등재문의 이 줄은 있지도 않은 파일을 가리킨다 |
| `node tools/verify585.js` (이관) | **38/41 FAIL — 내 것이 아니다.** `git stash` 로 내 변경을 **뺀 같은 나무**에서 돌려도 **똑같이 38/41**(338·344 규칙 대조). 내 변경은 `tools/verify356.js` 주석·상수 하나와 `tools/probe601.js` 뿐이라 `.pcb` 잉크를 못 움직인다 ⇒ **609 로 등재**(아래 §7-6) |
| `node tools/smoke.js` | **SMOKE PASS** |

### §7-6 곁다리 등재 — 609 (`verify585` §5 3건)

| 항 | 기대 | 실행 ① | 실행 ② |
|---|---|---|---|
| 03 `.pcb` 골드 잉크 | 65.3 (±0.5) | **66.03** | **66.03** |
| 03 `.pcb` 다이아 잉크 | 59.06 (±0.5) | **72.06** | **59.73** |
| 03 던전 ↔ 10 상점 같은 값 | 같다 | 65.3 / 59.06 (상점) ≠ 던전 | 〃 |

⇒ 성질이 **둘로 갈린다**: 골드는 두 실행 다 **+0.73 으로 결정적**이고, 다이아만 **59.73 ↔ 72.06 으로 흔들린다.**
셋째 항은 앞 둘의 파생이다(상점 쪽 값 65.3/59.06 은 두 실행 다 정확하다).

⚑ **뿌리의 1순위 후보는 자 자신이 이미 적어 뒀다** — `tools/verify585.js` 152~159행:
「`.pcb-p.fx-punch` 연출이 알약 조상에 한시적 배율을 얹어 그 프레임에 재면 «`.pcb` 가 커졌다» 는
거짓 신호가 난다(이 자의 실행마다 골드가 **65.3 / 66.85 / 70.01** 로 흔들렸다)」.
내 두 실행의 다이아 값 **72.06** 이 정확히 그 대역이다 ⇒ 그때 넣은 무력화
(`.pcb-p.fx-punch{animation:none!important;transform:none!important}`)가 **새고 있다**.
⚠ 다만 **골드의 +0.73 은 그 설명으로 안 닫힌다**(두 실행이 같은 값이다) — 609 는 **두 갈래**를 따로 봐야 한다.
⚠ `.pcb` CSS 자체는 안 바뀌었다(`git log -S` — `.pcb-g>i>.cic{width:65.3px}` · `.pcb-d>i>.cic{width:63px}` 그대로).
