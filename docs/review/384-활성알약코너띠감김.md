# 384 — 활성 알약의 «가로 띠» 가 코너를 감고 올라간다

> 등재: 352 §20-1 (비평가 **AX**·**AZ** 2인 독립 일치, 각도만 다르다)
> 작업: 2026-08-29 · `sess-0647-15927` 워커 C · 1회차
> 제품 `index.html` **1자리**(의사요소 한 겹 · 3줄) · 알약 상자·반경·옆띠·그라데이션 **0줄** · 밸런스·판정 0줄

---

## 0. 이 회차가 한 일 한 줄

바닥 어두운 띠가 코너에서 **0px** 이던 것을 ref 와 같은 호(弧)로 돌렸다 —
그리고 그 과정에서 **등재문의 뿌리 진단이 반쯤 틀렸다는 것**을 재현으로 확인해 처방을 바꿨다.

## 1. 새 도구 2개

| 도구 | 하는 일 |
|---|---|
| `tools/probe384.py` | ref·cap 의 알약을 **같은 팔레트로 분류**해 행별 «클래스 런»(K 검정 · B 베벨 · F 면 · D 바닥띠)을 찍는다. 코너에서 무엇이 무엇을 덮는지가 한 줄로 보인다. 채움면 좌 경계의 **자유 원 적합**(AZ 의 자)도 같이 낸다. |
| `tools/crop384.py` | 비평가용 1:1 창(하단 앵커 −60 **적용 후** 자르기 — 337 교훈 2) + **좌하단 코너 4배 확대** 짝. 30px 짜리 호는 1:1 창으로는 두 그림 다 «그냥 알약» 으로 보인다. |

게이트는 `tools/verify384.js` **74/74 신설**.

## 2. 재현 — 등재문의 뿌리 진단은 **반쯤 기각**됐다

등재문(352 §20-1)의 진단은 «`inset ±7px 0 0` 은 **가로로만** 미는 그림자라 알약 코너 구간에서도
**세로 직선**으로 내려온다» 였다. 픽셀은 다르게 말했다.

`inset` 그림자는 **패딩 상자에 클리핑**되므로 실제로 그려지는 것은 «세로 직선» 이 아니라
**반경을 따라가는 평행이동 복사본**이다. 검산은 한 줄로 끝난다 — 수리 전 우리 알약의
`rel 78` 행에서 검정이 **12..18** 인데, 반경 30 짜리 윤곽의 그 행 위치가 `x1(78) = 30 − √(900−529) = 10.74`
이고 여기에 밴드 7 을 더하면 정확히 그 자리다. **옆띠는 감기고 있었다.**

안 감기는 것은 **그라데이션이 그린 가로 띠 셋**이었다(`linear-gradient` 는 수평 줄이라 코너에서 잘린다).
그리고 그 잘린 자리를 **감긴 옆띠가 덮는다**. 즉 결손은 «옆띠가 안 감긴다» 가 아니라
**«가로 띠만 안 감긴다»** 였다.

`probe384.py` 실측 (07 활성 «스킬» 알약 **좌** 코너 · rel = 알약 상변 기준 행):

| rel | ref | 수리 전 cap |
|---|---|---|
| 60 | `K7` **`D2`** `B8` | `K6 B6 F12` — D **0** |
| 68 | `K7` **`D5`** `B10` | `K6 B6 F9` — D **0** |
| 74 | `K8` **`D8`** `F1 B1` | `K6 B11` — D **0** |

AX 가 잰 창(07-ref y2087..2104 = rel 60..77 × x rel 10..16)에서 **ref 34px ↔ 우리 3px**.

## 3. «어느 상자의 코너를 도는가» 가 이 처방의 전부다

가장 먼저 떠오르는 것(알약 자신에 `inset 0 -7px 0 #413122`)은 **한 픽셀도 안 보인다.**
그 띠는 행 y 에서 `x1(y)..x1(y+7)` 을 채우는데 그 구간이 통째로 **검정 옆띠 밑**이다
(rel 74 에서 띠 6.8..15.0 ↔ 검정 6.8..13.8).

ref 의 자리는 그것을 **오른쪽으로 7 민 값**이다 — rel 74 실측 **16..23** ↔ 평행이동 모델 **13.8..22.0**.
즉 ref 가 도는 것은 알약이 아니라 **검정 «안쪽» 상자**(알약을 좌·우 7 인셋한 것 = 옆띠의 내변과 같은 윤곽)다.
그 상자는 자기 자신에 못 만들므로 **의사요소 한 겹**으로 세운다(평행이동은 반경을 안 바꾸므로 반경은 30 그대로).

## 4. 띠는 하나가 아니라 **셋**이다

«바닥 어두운 띠 하나» 로 끝내려다 재현이 말렸다. ref 는 같은 코너에서 **바닥 베벨(71..78)과
상단 베벨(0..7)도 같이 감긴다**:

* `ref rel 68` = `K7 D5` **`B10`** — 바닥 베벨을 안 감으면 우리는 `B2` 에서 곧장 면이 시작한다.
* `ref rel 9` = `K8` **`B9`** `F…` — 상단을 안 감으면 우리 **면 경계가 22**(ref **29**)에서 시작한다.

⚑ **AZ 의 «채움면 코너 반경 −10%» 는 반경이 아니라 이 세 띠가 코너에서 빠진 만큼이었다.**
반경(30)은 다섯 비평가가 모아 둔 값이고 이번에도 **한 줄도 안 건드렸다**.

## 5. 제품 변경 (index.html 1자리)

```css
.stab.on::after{content:'';position:absolute;left:7px;right:7px;top:0;bottom:0;
  border-radius:30px;pointer-events:none;
  box-shadow:inset 0 -7px 0 #413122,inset 0 -14px 0 #634F37,inset 0 7px 0 #634F37}
```

* 색·두께가 그라데이션과 **같은 값**이라 **세로 한복판 단면은 한 픽셀도 안 바뀐다**
  (가운데에서는 같은 자리에 같은 색을 다시 칠할 뿐이고, 바뀌는 것은 **코너뿐**이다).
* **등재문 처방 ⓐ(`border`+스프레드로 다시 그리기)는 안 골랐다.** 그 길은 검정 링을 상·하변에도
  둘러 «위·아래 테두리는 바 테두리와 공유»(측정표 07 §9 · 378)를 깨고, 알약 상자 크기까지 흔든다.
* 칠 순서 — 의사요소는 부모의 inset 그림자 **위**, 그리고 라벨 글자 **위**에 온다.
  라벨 잉크는 rel **20~57**(실측)이고 이 띠는 rel 78~85 + 코너(x 7~30)라 **겹치는 픽셀이 0** 이다.
  `verify384` **§R3** 이 그것을 «띠를 끄고 켠 잉크 픽셀 수 Δ0» 으로 못박는다(1061 ↔ 1061).
* `pointer-events:none` — 클릭은 그대로 칸이 받는다(§6 이 히트 테스트와 진짜 마우스 클릭 둘 다 문다).

## 6. 수리 후 실측

| rel | ref | 수리 후 cap |
|---|---|---|
| 60 | `K7 D2 B8` | `K6 D2 B4 F12` |
| 68 | `K7 D5 B10` | `K6 D5 B10 F1` |
| 74 | `K8 D8 F1 B1` | `K6 D9 F1 B2` |

채움면 좌 경계 인셋(아래 코너, rel 56→70): ref `16 16 17 18 19 19 20 22 22 24 25 26 27 31 32`
↔ 수리 전 `14 14 14 14 14 15 15 15 16 16 16 17 17 18 17`(평균 Δ **6.8px**)
→ 수리 후 `14 14 14 14 15 15 16 17 18 20 21 23 25 28 31`(평균 Δ **3.3px** — **절반**).
위 코너 rel 9 의 면 경계도 **22 → 25**(ref 29)로 올라왔다.

**남는 것은 «링 vs 밴드» 한 가지다** — ref 의 검정 옆띠는 코너에서 **균일 법선 두께**로 두꺼워지는데
(`probe384.py` **ⓗ** — 검정 런 폭 ref rel 68/74/80/84 = **9/10/13/18**) 우리 평행이동 밴드는 **전부 7** 이다.
1회차 채점의 감점이 **전부 이것 하나**였고 → **409 로 등재**했다(등재 당시 번호는 405 였으나 `fnchk125` T1 이 같은 번호를 먼저 잡고 있어 2026-08-29 에 409 로 옮겼다 — 340 선례. 408 도 같은 순간 다른 워커가 잡아 한 칸 더 비켰다).

### 6-1. 2회차에 시도하고 **되돌린** 것 (다음 세션이 같은 길을 두 번 안 가게)

* **ⓐ `inset 0 0 0 7px #000`(스프레드 링)을 `var(--pill-*)` «뒤» 에 깔기** — 실행·측정했다.
  코너에서 두꺼워지는 몫이 정확히 `--pill-l` 의 **베벨 밴드(`inset 14px`) 밑**으로 들어가
  바닥 코너 7행 검정 합이 **41 ↔ 39, Δ +2px** 에 그쳤다. 그걸 지키자고 쓴 §R5 되돌림 항이
  **Δ2 로 빨개져** 스스로 «무른 항» 임을 드러냈고, **항을 무르게 두느니 링을 물렸다.**
* **ⓑ 링을 «앞» 에 두기** — 두께는 나오지만 **378 이 깨진다**: 끝 칸의 «셸에 닿는 면» 을 베벨로 바꾼
  override 가 링 **밑**으로 들어가 그 면에 검정이 되살아난다.
* ⇒ 제대로 하려면 **면별 손잡이를 넷으로 쪼개고**(검정/베벨 각각) `verify378` [1] 선언 항을 같이 이관해야 한다.
  이 작업 단위 밖이라 **409**(구 405) 로 넘겼다. **지금 커밋된 것은 1회차 상태이고, 그것이 정답이다.**
* **세로 평행이동 밴드 두 줄**(`inset 0 ±7px 0 #000`)도 재 봤다 — 바닥 코너에서 rel 84 `K3 → K6` 로 오르지만
  링(ⓐ)보다 못하고 같은 이유로 무르다. 셋 다 되돌렸다.

## 7. 게이트

| 게이트 | 결과 |
|---|---|
| `tools/verify384.js` | **74/74 신설** — [1] 선언 · [2] 코너 감김(5호스트 × 좌우 10면) · [3] 세로 한복판엔 없다 · [4] 위 코너엔 없다 · [5] 아래로 갈수록 두껍다(=호) · [R] 되돌림 4항 · [6] 조작 2항 |
| `verify47` | 161/161 무수정 |
| `verify352` | 62/62 무수정 |
| `verify378` | 47/47 무수정 |
| `verify379` | 100/100 무수정 |
| `verify337` | 91/91 무수정 |
| `verify96` | 69/69 무수정 |
| `verify209` | 61/61 무수정 |
| `verify351` | 38/38 무수정 |
| `node tools/smoke.js` | **SMOKE PASS** |

⚑ **게이트가 무르지 않다는 증거 셋** — ① §R2 «띠를 끄면 코너 D 가 **0px** 로 돌아간다»
② [3]·[4] 음성항(«한복판·위 코너에는 D 가 **없어야** 한다») — 없으면 «띠를 옆면 전체로 번지게 해도 초록» 이 된다
③ [5] «rel 74 가 rel 68 보다 두껍다» — 폭이 일정하면 그건 코너를 도는 게 아니라 또 하나의 수평 줄이다.

⚠ **게이트를 쓰다 잡은 함정 하나** — 옆띠를 «처음 나오는 검정» 으로 찾으면 **378 이 그 면을 셸
테두리에 넘긴 끝 칸**(10 상점 좌 등)에서 스캔이 셸 검정 6 을 먼저 만난다(`K4 B6 D4 B10`).
그래서 자는 **어두운 띠를 먼저 찾고 그 앞 런이 옆띠인지**를 묻는다 — 검정 옆띠·베벨 옆띠 두 갈래가 한 자로 읽힌다.

## 7-1. 곁다리 관측 — 새로 등재할 것 **없음**

회귀를 돌다 `verify149` **272/274**(§1 토스트 «룰렛 무료 소진 — 조각을 못 찾음»)를 만났다.
**내 변경과 무관하고 이미 등재돼 있다** — 367 이 문구를 «오늘 룰렛 소진» 으로 바꾼 게이트 부패로
같은 시각 다른 워커가 **401** 로 등재했다(수리 전 커밋 대조 확인 포함). 손대지 않았다.

## 8. 환경 메모 (다음 세션 시간 절약)

이 컨테이너의 `npm i playwright` 는 최신판(chromium v1234)을 끌어오는데 **다운로드가 막혀 있다**.
`/opt/pw-browsers` 에 이미 있는 것은 **chromium-1194 = Chromium 141** 이므로
`npm i --no-save playwright@1.56.0` 로 맞추면 그대로 뜬다(`npx playwright install` 은 실패한다).

---

## 9. 1회차 채점 (비평가 BB · BC 2인 독립 — «확정 사실표» F0~F5 를 미리 줬다)

| 축 | BB | BC |
|---|---|---|
| ① 요소 배치 | 5 | 6 |
| ② 상대 크기 비율 | **4** | 5 |
| ③ 여백·간격 | 5 | 6 |
| ④ 정렬·그리드 | **4** | 5 |
| ⑤ 색·명암 *(기록만)* | 9 | 10 |
| ⑥ 폰트 크기 비율 *(기록만)* | 9 | 10 |
| **①~④ 최저** | **4** | **5** |

⚑ **두 사람의 감점이 한 항으로 모인다 — 그리고 그것은 384 의 항목이 아니다.**
384 가 등재문에서 받은 것(«바닥 어두운 띠가 코너를 안 감는다»)은 **둘 다 [달성]** 으로 적었다:

* **BB** — «바닥 어두운 띠는 이제 코너를 돈다. 대각 두께 ref **4.90** ↔ 우리 **4.69**(Δ0.21px, 4%).
  행별 쐐기 폭도 ref 를 ~0.5px 이내로 추종. 1회차 커밋의 「가로 띠 셋을 코너에 돌린다」는 어두운 띠에 한해 성립.»
* **BC** — «바닥 어두운 띠(D)의 코너 폭은 맞다. y=126 에서 ref **9.0px** ↔ cap **9.0px**(Δ0).
  무너진 건 검정 링뿐이고 D 띠 자체는 정확.»

남은 감점은 **전부 «옆띠가 등폭 링이 아니다»** 다(BB ①②③④ 지적 5건 중 5건 · BC 4건 중 3건) →
**409 로 등재**했다(구 405 — 번호 충돌로 이동). 나머지는 둘 다 [달성]:

* 직선부 전 구간 정합 — BC «검정 7.02/7.00 · 베벨 6.99/7.00, Δ≤0.4px» · BB «좌·하 점근선 4개 전부 ≤0.80px»
* 상자 Δ0 — 셸 950 · 알약 260, 양쪽 동일
* 밝은 띠 **바깥** 모서리 궤적 — BB «최대 1.23px · 평균 0.87px»
* ⑤ 색 — 최대 ΔRGB 3 · ⑥ 라벨 bbox — **67×38 @(367,72) 완전 동일**

⚠ **BC 의 지적 2·3(«코너 호의 바닥 접선이 6.4px 위» · «셸 배경이 7px 파고든다»)은 409 와 같은 뿌리**이고,
그 «ref R=34.8» 은 **352 §10 이 이미 기각한 읽기**(알약 실루엣에 셸 검정 테두리를 붙여 재면 코너가 부푼다 — AU 선례)다.
F2 대로 바깥 반경 30 은 건드리지 않았다.

**범위 밖 참고(BB, 채점 미반영)**: 셸 바 **자체의 상단 검정 외곽선**이 열 200·600·900 단면에서
ref **7.9px** ↔ 우리 **6.0px**(−24%). 알약이 아니라 셸(`.stabs{border:6px}`)이라 이번 단위가 아니다 —
352 §2 가 «ref 의 순검정은 6, 바깥 AA 한 줄» 로 **이미 기각한 항**과 같은 자리이므로 새로 등재하지 않았다.
