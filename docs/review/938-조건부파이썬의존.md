# 938 — `scipy`·`soundfile` 두 자를 937 사슬 안으로 (조건부 의존의 «할 일» 은 모듈마다 다르다)

- 세션 `sess-1128-18850` (루틴 워커 B) · 2026-09-05 · **1회차**
- 티어 **T1 «버그(자)»** · 지시서 [3]-**(가)** 기계+자 — **비평가 없음**(레퍼런스 대조가 아니다)
- 제품 `index.html` **0줄** · 자 4파일(`pydep937.py` · `scan885e.py` · `synth99.py` · `verify937.js`) · 지시서 [6] «준비» 절

## 1. 재현 — 등재문 그대로였다

이 컨테이너는 파이썬 의존이 **하나도 없는** 상태로 뜬다. 지시서 [6] «준비» 줄대로
`pip3 install pillow numpy` 를 깔고 나서 두 자를 돌린 것이 재현이다(그 줄이 답이라고 적힌 상태).

| 자 | 수리 전 종료 코드 | 수리 전 stderr | 점수 줄 |
|---|---|---|---|
| `tools/scan885e.py` | **1** | `Traceback … ModuleNotFoundError: No module named 'scipy'` (44행) | **0줄** |
| `tools/synth99.py` | **1** | `Traceback … ModuleNotFoundError: No module named 'soundfile'` (15행) | **0줄** |

913·937 이 두 번 찍은 바로 그 얼굴이다 — **빨강이 아니라 «없는 자»**.

## 2. 스코프 — 사슬 안의 여집합은 정확히 이 둘뿐이었다

`ast` 로 저장소 전체 `*.py` 의 최상위 import 를 세어(표준 라이브러리·같은 폴더의 로컬 모듈 제외) 서드파티 하드 import **146건**을 얻었고,
**`tools/` 안에서는 `scipy` 1건 + `soundfile` 1건이 전부**였다(나머지 144건은 저장소 루트의 옛 일회용 자
`s2~s38.py`·`crit*33.py`·`p58aj.py`·`pxdiff58.py` 등 — 이들을 부르는 노드 자를 전수로 찾으니 **`pxdiff41.py` 하나뿐**이고
그 하나는 937 이 이미 사슬에 넣어 뒀다). ⇒ **여집합을 다 닫았다**(939 로 이어질 «세 번째 반쪽» 은 없다).

## 3. 실제 판단 — 준비 줄에 상시로 올릴 것인가 (등재문이 지목한 자리 · 위임 규약 채택)

| 안 | 대가 | 판정 |
|---|---|---|
| ⓐ 상시 줄에 `scipy soundfile` 을 더한다 | 워커 **전원**이 매 세션 40MB+ 를 내려받는다. 쓰는 자는 **각각 한 자뿐**(scan885e · synth99)이고 둘 다 노드 게이트가 안 부른다(회귀 스윕 밖 — `verify833` 은 주석으로만 언급한다) | ✗ |
| ⓑ **상시 줄은 그대로 두고, 부딪히는 자리에서 그 자가 명령을 알려 준다** | 그 둘을 쓰는 워커만 한 줄을 더 친다 | **✔ 채택** |
| ⓒ 아무것도 안 적고 부트스트랩만 건다 | 안내문이 상시 줄(`pip3 install pillow numpy`)을 답으로 주면 **적힌 대로 해도 안 낫는다** | ✗ |

⇒ ⓑ 를 고르면 **«할 일» 이 모듈마다 달라야 한다**는 것이 딸려 온다 — 이것이 이 작업의 본체다.
`pydep937._SPEC` 은 지금까지 상수 `PIP` 하나만 물려 줬는데, 그 줄은 scipy·soundfile 에는 **거짓 처방**이다.
`SCIPY = 'pip3 install scipy'` · `SNDF = 'pip3 install soundfile'` 두 상수를 세우고 네 이름
(`ndimage`·`scipy`·`sf`·`soundfile`)을 각자에 물렸다. 937-② 는 «사람이 읽을 문장과 기계가 읽을 코드를 같이 고쳐라» 였는데,
**세 번째 축이 «할 일» 이다**(문장·코드가 옳아도 처방이 틀리면 워커는 제자리에 선다).

되돌림 방법(위임 규약): `_SPEC` 의 두 상수를 `PIP` 로 되돌리고 지시서 [6] 의 ⚑ 문단을 지우면 ⓐ 로 간다.

## 4. 수리 (제품 0줄)

| 파일 | 바꾼 것 |
|---|---|
| `tools/pydep937.py` | `SCIPY`·`SNDF` 상수 + `_SPEC` 네 줄(`ndimage`·`scipy`·`sf`·`soundfile`) · 쓰는 법·⚠ 문단 |
| `tools/scan885e.py` | 44행 `from scipy import ndimage` → `from pydep937 import ndimage` |
| `tools/synth99.py` | 15행 `import soundfile as sf` → `from pydep937 import sf` |
| `docs/ROUTINE.md` [6] | «준비» 절에 ⚑ 조건부 의존 문단(모듈 · 그 모듈을 쓰는 자 · 설치 명령 · «모듈마다 할 일이 다르다» 경고) |
| `tools/verify937.js` | `HARD` 정규식 + 항 11개 신설 — 아래 |

**수리 뒤 응답** — 둘 다 «한 줄 + 코드 2», 각자 **자기 명령**을 짚는다:

```
$ python3 tools/scan885e.py --cap …     → scipy 없음 — pip3 install scipy          (코드 2)
$ python3 tools/synth99.py              → soundfile 없음 — pip3 install soundfile  (코드 2)
```

**처방이 참인지도 실제로 확인했다** — `pip3 install scipy` 뒤 `scan885e` 는 ref 문턱 사다리 16행을 끝까지 찍고
자기 판정으로 넘어갔고(§5 곁다리), `pip3 install soundfile` 뒤 `synth99` 는 import 를 통과한다.
⚠ 안내문이 문법만 맞고 실제로는 안 낫는 자리가 913·937 이 경계한 그 자리다.

## 5. 게이트 — `verify937` **26/26 → 37/37 PASS**(신설 11항, 새 자 불요 = 등재문 처방)

| 항 | 재는 것 |
|---|---|
| [A7]·[A8] | scipy·soundfile 안내문이 **자기 설치 명령**을 적는다 |
| [A9] | 조건부 의존에 상시 준비 줄을 답으로 주지 않는다(«적힌 대로 해도 안 낫는 처방» 0건) |
| [B1] | 정규식에 `scipy`·`soundfile` 을 더했다(하드 import 0건) |
| [B6]·[B7] | 등재가 지목한 두 자가 사슬 안이다 |
| [C5]·[C6] | 지시서가 두 명령을 적고 **어느 자가 쓰는지**까지 적는다(상시 줄에서 뺀 근거가 그 자리에 있다) |
| [R8] | numpy·PIL 이 실제로 있다 — 없으면 조건부 둘이 «먼저 죽는 것» 에 가려진다 |
| [R9]·[R10] | ★ **되돌림 시험** — 그 둘«만» 감춘 세상에서 각 자가 한 줄 + 코드 2 로 답하는가 |
| [R11] | 조건부를 감춰도 numpy·PIL 만 쓰는 자는 멀쩡하다(지연 해석이 안 깨졌다 = 조건부가 «상시» 가 되지 않았다) |

⚠ **감춘 세상을 둘로 나눈 것이 [R9]·[R10] 의 전제다** — 기존 `sand`(numpy·PIL 을 감춘다)를 그대로 쓰면
자의 import 순서상 **언제나 «numpy 없음» 이 먼저** 나와 scipy·soundfile 쪽을 한 항도 못 잰다.

**헛초록이 아님을 못박았다** — `scan885e` 의 import 한 줄을 수리 전으로 되돌려 돌리니
**[B1] 빨강 · [R9] 빨강(실측 코드 1 · `Traceback …`) · 35/37 FAIL**, 원복하면 37/37 PASS.

## 6. 회귀

| 자 | 결과 |
|---|---|
| `node tools/verify937.js` | **37/37 PASS** (26/26 → 신설 11) |
| `node tools/verify913.js` | 16/16 PASS (npm 쪽 절반을 안 건드렸다) |
| `node tools/verify895.js` | 18/18 PASS (937 이 살린 그 자) |
| `node tools/verify833.js` | 280/280 PASS (`scan885e` 를 주석으로 언급하는 자) |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verifyProgress.js` | 종료 코드 0 |

## 7. 곁다리 관측 — **939 로 등재**(고치지 않았다)

`tools/scan885e.py` 는 **자기 측정 실패**(«★ 를 n 개밖에 못 찾았다 — 창 자동 탐색 실패»)에도 `sys.exit(2)` 를 쓴다(222·232행).
937 이 코드 2 를 «환경에 없음» 으로 못 박았으므로 이 자 안에서는 그 신호가 **둘을 가리킨다**.
지금은 이 자를 부르는 노드 게이트가 없어 스윕이 오독할 자리는 없지만, 누군가 `py()` 로 부르는 순간
«측정이 안 됐다» 가 «환경에 없음» 으로 읽힌다. **수리 전 트리에서도 같은 두 줄**임을 확인했다(내 변경과 무관 ·
내 작업 단위가 아니라 안 고쳤다 — 그 exit 은 885 의 판정 소유다).
