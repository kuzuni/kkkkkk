# 499 — `tools/verify124.js` 축복 6건 실패 (등재 중복 · 495 가 같은 뿌리를 먼저 닫았다)

**세션** sess-1551-22038 (워커 H) · 2026-08-30 · **1회차** · **제품 0줄 · 게이트 0줄 · 레이아웃 Δ0px**

## 결론

**499 는 495 와 같은 결손의 두 번째 등재다.** 두 워커가 15분 간격으로 같은 빨강을 따로 봤다 —
495 는 sess-1459-28514(워커 D, 477 회귀 곁다리), 499 는 sess-1436-12388(워커 G, 471 회귀 곁다리).
495 가 `c3501c7` 로 먼저 닫았고(**55/61 → 66/66**), 그 수리가 499 의 처방과 **같은 방향**이라
499 에 남은 작업이 없다. 표에서 «미착수» 로 남겨 두면 다음 워커가 통째로 다시 선점한다
(ROUTINE [4] 가 경고하는 371·369 사고와 같은 꼴)이라 **행만 마감**한다.

## 같은 결손임을 무엇으로 확인했나

등재문 499 가 적은 빨강 6건과, 495 가 남긴 되돌림 시험 `[R1]`·`[R2]` 가 내는 값이 **정확히 서로의 거울**이다.

| 499 등재문이 본 값 | 495 `[R1]`(옛 곡선 재주입) | 지금 초록값 |
|---|---|---|
| 게임 Lv19 / 기대 Lv11 | Lv11 · 42회 | Lv19 · 75회 |
| prog 3/4 / 기대 2/4 | (같은 뿌리) | 3/4 |
| 발동 75회 / 기대 42 | 42회 | 75회 |
| 만료 −300000 / −1200000 / −2100000 ms | 3종 만료가 서로 −30분 벌어짐 | 0 / 0 / 0 ms 차 |

−5분·−20분·−35분 = **레벨당 5분** 가산의 흔적이고, 495 가 지운 것이 바로 그 상수(`B_PERLV`)다.

## 499 가 «먼저 보라» 고 지정한 것 — 뿌리가 제품이 아님

등재문 ⚠ 조항: «먼저 `verify456`·`verify476` 이 초록인지부터 볼 것 — 그 둘이 빨갛다면 뿌리가 제품이다.»

- `node tools/verify456.js` → **24/24 PASS**
- `node tools/verify476.js` → **34/34 PASS**
- `node tools/verify124.js` → **66/66 PASS**

⇒ 제품은 내내 옳았고 뿌리는 자(gate) 하나였다. **제품 0줄**이 정답이었음이 확인된다.

## 무르게 푼 수리가 아님을 확인했다

499 등재문은 «기대값을 무르게 푸는 것이 아니라 시뮬을 새 규칙으로 갈아 끼우고 되돌림 시험을
같이 세울 것»(334 처방)을 요구했다. `c3501c7` 의 diff 를 직접 읽어 셋을 확인했다:

1. **이름째 삭제** — `B_PERLV` 가 사라졌고 `durAt()` 은 **인자도 분기도 없다**(레벨을 읽는 지속시간이
   되살아날 자리를 자에도 안 남겼다).
2. **값 자체를 따로 단언** — `[6-e]` «만료 = 마지막 발동 + 30분» · `[6-f]` «3종 만료가 서로 같다» 신설.
   시뮬 대조만이면 «둘이 같이 틀리면 초록» 이 되는데, 그 구멍을 게임이 낸 만료 시각에서 30분을
   **직접 읽어** 막았다.
3. **되돌림 시험 §R 3항** — 옛 곡선을 다시 깔면 빨개지고(`[R1]`·`[R2]`), 걷으면 초록(`[R3]`)이며
   시험이 상태를 안 남긴다.

## 곁다리 등재 — 500 (주인 지시가 표에서 사라져 있었다)

499 등재문이 짝으로 지목한 «476(`BLESS_NEED` 표)» 을 확인하다 **번호 충돌**을 찾았다.

- `docs/ROUTINE.md` 239행: «신규 주인 지시(2026-08-30 22:10): **476** 축복 레벨업 필요 경험치 표
  4·10·15·…·90·100 …» 
- 그런데 `docs/PROGRESS.md` 의 **476 행은 전혀 다른 작업**(`probe351` D7 유령 겹침)이고
  이미 완료됐다(sess-1438-17286). `verify476.js` 도 그쪽이다.
- ⇒ 주인의 축복 경험치 표 지시에는 **PROGRESS 행이 하나도 없다.** 워커의 티어 스캔은 표를 읽으므로
  **아무도 잡을 수 없는 상태**로 누워 있었다.

제품 실측으로 미구현임을 못박았다 — `BLESS_STEP = 4` 상수가 그대로고(18759), 레벨업 식이
**두 벌**(28778 `autoBlessSettle` · 29166 `activateBless`)이며 진행바는 `n/4` 고정(29184·29186)이다.
= 주인 지시문의 «`BLESS_STEP` 상수 → `BLESS_NEED` 표 + 접근자, 두 벌 식 통합, 진행바 n/need» 가
한 줄도 안 들어갔다. **500 으로 등재**했고(가장 큰 번호 + 1), ROUTINE 239행에 번호 이동을 적었다.

⚑ **500 을 잡는 워커에게**: 그 작업은 `verify124` §6·§7 을 **반드시 다시 빨갛게 만든다** —
지금 자의 시뮬이 `B_STEP = 4` 를 손으로 적어 두고 있다(495 가 갈아 끼운 건 «지속» 축뿐이다).
이관을 같이 하지 않으면 **같은 빨강이 세 번째로 등재된다.**

## 회귀

`verify124` 66/66 · `verify456` 24/24 · `verify476` 34/34 · `node tools/smoke.js` **SMOKE PASS**
(제품·게이트 0줄이라 전부 무수정 확인용).
