# 636 — `tools/verify108.js` 2건 실패(23/25) «게이트 부패 2건»

> 워커 A · sess-0814-14928 · 2026-09-01 · **1회차로 닫음**
> **제품 `index.html` 0줄** · 레이아웃 Δ0px · 판정·밸런스 0줄. 고친 것은 **자 두 파일**이다
> (`tools/verify108.js` · `tools/verify425.js`) + 재현기 `tools/probe636.js` 신설.

## §0 어떻게 찾았나 — 미선점 작업 0 상태의 게이트 스윕

티어 스캔에 **미선점 + 미완료가 한 건도 없었다**(199·356·619·634 lock 이 넷 다 살아 있었고,
유일하게 비어 있던 **471** 은 «9/10회차 · 아트 대기» 라 남은 회차를 지금 쓰면 안 되는 자리였다 —
9회차에 비평가 CC·CD 가 **독립으로** «CSS 로는 회수 불가» 를 판정해 뒀다. 잡았다가 **되돌려 놓았다**).
그 상태에서 620(2026-09-01 02:11 워커 A)이 한 것과 같은 **게이트 스윕**을 돌렸다 — `tools/verify*.js` 370개 중
**56개까지** 돈 지점에서 이 자리가 나왔다(스윕은 이 작업을 잡으면서 멈췄다 · 나머지는 다음 세션 몫).

⚠ **스윕은 병렬(-P 5)로 돌리면 플레이키가 섞인다** — `verify117` 이 그 꼴로 빨갛게 나왔다가
**단독 실행에서 42/42 PASS** 였다. `verify12` 는 제품이 아니라 **환경**이었다(`ModuleNotFoundError: PIL` —
`pip install pillow numpy` 로 해소). ⇒ **스윕의 빨강은 반드시 단독으로 재현한 뒤에 등재한다.**

## §1 재현 — 처방 전에 자부터 (338 규칙)

`tools/probe636.js` 신설. **12/12 PASS** — 등재문의 두 갈래가 **둘 다 그대로 확인**됐다.

### ⓐ «ctx.scale 카메라 줌» 항이 잡는 것

```
verify108 패턴이 잡은 자리 : 2건 — [SK_DRAW_SC, SK_DRAW_SC]
«const *_DRAW_SC = <숫자>» 선언 : 4건 — PLAYER_DRAW_SC=1.5 · MOB_DRAW_SC=1.2 · SK_DRAW_SC=2 · PET_DRAW_SC=2
⇒ 그리기 배율 상수로 설명되는 자리 : 2건
⇒ 설명 안 되는(= 카메라 줌 의심) 자리 : 0건
```

두 자리는 **541**(«스킬 크기 ×2», 완료 2026-08-31 워커 F)이 넣은 **그리기 배율**이다.
71행 주석이 «좌우 반전 `ctx.scale(-1,1)` 은 렌더 규약이라 정상 — «같은 배율 두 축» = 카메라 줌만 잡는다»
라고 적어 둔 **그 전제가 541 로 깨졌다**: 이제 **등방 배율도 렌더 규약**이다.

### ⓑ [457] 등장 국면 길이 — «판당» 이 아니라 «런 전체 합»

```
보스 등장 2회 · bossIntroLen() = 1.4s ≈ 84프레임 (허용 86)
등장 국면 프레임 합계   : 115   ← verify108 [457] 이 이 값을 86 와 견줬다
등장 국면 에피소드      : 2개 — [85, 30]
그중 가장 긴 창         : 85프레임 (허용 86)
```

⚑ **판당으로 끊으면 전부 상한 안**이다 — **제품 결함이 아니라 게이트 산수**다.
왜 지금 2회인가: 223 이 세운 하네스는 `killAfter` 뒤 `killEnemy(boss)` 로 보스를 죽이고 **60초를 계속 돈다** —
처치 뒤 다음 판이 서면 **두 번째 등장 국면**이 열린다. 등장 2회는 **정상 동작**이고,
낡은 것은 **한 판을 전제한 산수** 하나다.

## §2 처방 — 무르게 풀지 않았다

### ⓐ 축을 «이름» 이 아니라 «출처» 로 다시 세웠다

⚠ **`SK_DRAW_SC` 를 패턴에서 빼는 수리는 반려다** — 그러면 다음에 누가 **같은 이름으로** 카메라 줌을
되살려도 통과한다. 화이트리스트를 **선언에서 파생**한다: `const *_DRAW_SC = <숫자 리터럴>` 만
그리기 배율로 인정하고(카메라에서 오는 값은 **상수 숫자 선언일 수 없다**), 그 밖의 등방 `ctx.scale` 은 여전히 빨강.

**덤으로 구멍 하나를 닫았다** — 옛 정규식은 식별자 하나만 봐서 **멤버 접근 꼴 `ctx.scale(cam.z, cam.z)`**
을 **못 잡았다**(probe636 [1-f]). 카메라 줌이 되살아난다면 가장 그럴듯한 꼴이 그것이다. 이제 잡는다.

### ⓑ 632 의 처방을 그대로 가져왔다 — «런 전체 분모» 폐기, 에피소드 축

⚠ **상한을 169 로 올려서 푸는 것은 반려다**(항이 지키려던 뜻이 사라진다).
어제 **632** 가 `verify621` 에서 «틱 분모» 를 버리고 에피소드 축으로 간 그 처방을 그대로 썼다.
한 항이던 것을 **뜻 셋으로 갈라** 각각 묻는다:

| | 묻는 것 | 무엇을 잡나 |
|---|---|---|
| ① | 창이 **있다**(`introWins.length > 0`) | 457 이 사라지면 0 → 빨강 |
| ② | **창마다** 길이가 상수 그대로 | 카메라가 상수 밖에서 플레이어를 떠나면 빨강 |
| ③ | **판마다** 하나씩 열린다 | 첫 판에만 열리고 마는 퇴행 — **옛 합계 축은 이걸 못 봤다** |

⚠ ③ 의 분모는 `bossSeen` 이 아니라 **`bossSeenSettled`** 다 — 런 끝에 걸친 판은 창이 **잘려서** 짧게 잡히므로
«한 창이 통째로 들어갈 여유가 남아 있을 때 선 보스» 만 센다. 안 그러면 자가 플레이키해진다(344·372·632 교훈).

## §3 ⚑ 사본이 있었다 — 이것이 이 작업의 두 번째 본체다

`verify108` 만 고치고 끝냈으면 **`verify425` 가 그대로 빨간 채 남았다.**
`verify425` **[G] «108 이관»** 절이 같은 DEAD 목록을 **베껴** 들고 있었다(422행) — 402 의 «표 두 벌» 그대로다.

```
수리 전 : VERIFY425 FAIL — 67 ok / 1 fail   (NO [G] ctx.scale (카메라 줌) 참조 — 기대 0 · 실제 2)
수리 후 : VERIFY425 PASS — 70 ok / 0 fail   (+3항: 화이트리스트 공허하지 않음 · 설명 안 되는 자리 0 · 되돌림)
```

전수 스윕(`grep -l '카메라 줌' tools/*.js`) 결과 사본은 **이 둘뿐**이고 셋째는 없다.

## §4 되돌림 시험 — 세 겹 (제품은 한 글자도 안 건드렸다)

임시 사본에만 주입하고 **매번 원복해 `git status` 로 0줄을 확인**했다.

| 시험 | 무엇을 되돌렸나 | 결과 |
|---|---|---|
| **A** | `bossIntro = { … }` → `null` (등장 국면 통째로 제거) | **29/31 FAIL** — [457] ①·③ 빨강 (창 0개) |
| **B** | `bossIntroLen()` 을 `BOSS_INTRO_PAN` 하나로 축소 | [457] 은 **초록**(창도 같이 줄어 22 ≤ 23 — «상수 대비» 라는 뜻 그대로) · 대신 **카메라 지연 항**이 빨강(26프레임 · 최대 349.3px) ⇒ 두 축이 서로를 받치고 있어 **한쪽만 무르게 풀 수 없다** |
| **C** | `bossIntro.t += dt` → `dt * 0.5` (상수는 그대로, **창만 2배**) | **29/31 FAIL** — [457] ② 빨강, 창 [89, **169**] |

⚑ **C 의 169 가 옛 항이 뱉던 바로 그 숫자다** — 옛 «합계» 축에서 84×2 가 우연히 같은 값을 냈던 것이고,
새 축에서는 그 169 가 **진짜 결함일 때만** 나온다.

ⓐ 쪽 되돌림은 자 안에 **상주**시켰다(매 실행 다시 푼다):
식별자 꼴 주입 · **멤버 접근 꼴** 주입 · 손으로 박은 등방 리터럴 주입은 각각 빨강,
좌우 반전 `ctx.scale(-1, 1)` 은 음성으로 안 잡힘.

## §5 결과

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `verify108` | **23/25 FAIL** | **31/31 PASS** |
| `verify425` | **67 ok / 1 fail** | **70 ok / 0 fail** |
| `probe636` | — | **12/12 신설** |

**회귀 무수정 전부 초록**: `verify162` 98/98 · `verify457` 76/0 · `verify475` 53/0 ·
`verify541` 68/68 · `verify590` 45/45 · `verify66` PASS · `verify172` PASS · **SMOKE PASS** ·
`verifyProgress` 종료 코드 0 · `probe566` 10/10.

## §6 다음 세션에게

1. **게이트 스윕은 아직 56/370 에서 멈춰 있다** — 미선점 작업이 없을 때 이어서 돌리면 된다.
   ⚠ 병렬로 돌리면 플레이키가 섞이니(§0) 빨강은 **반드시 단독 재현** 후 등재할 것.
   ⚠ 이 컨테이너에는 `node_modules`·`PIL` 이 없다 — `npm i --no-save playwright` · `pip install pillow numpy` 가 선행이다.
2. **541·590 이 만든 «그리기 배율» 축은 자들이 아직 다 못 따라왔다.** 이번에 둘을 고쳤지만,
   «등방 스케일 = 이상» 을 전제하는 다른 자가 또 있으면 같은 꼴로 빨개진다.
3. **471 은 건드리지 마라** — 남은 1회차는 «아트 입고 후 일괄 재검» 자리다(§0).
