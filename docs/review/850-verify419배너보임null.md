# 850 — `tools/verify419.js` 74건 빨강: «배너 보임 null»

> T1 «버그(게이트 부패 · 자 표본)» · 지시서 [3]-**(가)**(기계 + 자 · 비평가 없음)
> 선점 `docs/claims/850.lock` — sess-0043-6582 (루틴 워커 C)

---

## 1회차 (2026-09-03, sess-0043-6582)

### 재현 — `tools/probe850.js` 신설 (338 규칙: 처방 전에 먼저 재현)

등재문의 두 갈래를 **한 번에** 가르는 자를 세웠다. 같은 오프너를 **네 트리**에서 열어
`#tuto` 의 존재 · 상자 · `display` · `visibility` · 보임% 를 나란히 찍는다.

| 트리 | 무엇을 뺐나 |
|---|---|
| T0 | (현재 그대로) |
| T1 | 419 선언 `#app:has(:is(#modal,#collw,#blsw,#bagw,#pfw,#specw,#cfw).on) #tuto{display:none}` |
| T2 | 811 의 HUD 숨김 목록에서 **`#tuto` 만** (`:is(#top,#tuto,#slots)` → `:is(#top,#slots)`) |
| T3 | 둘 다 |

실측(`node tools/probe850.js` · disp/vis · 보임%):

| 오프너 | 프레임 | T0 | T1 (−419) | T2 (−811#tuto) | T3 (−둘 다) |
|---|---|---|---|---|---|
| main | 2280·1600 | block/visible 100% | 같음 | 같음 | 같음 |
| side:attend | 2280 | none/**hidden** — | block/**hidden** 100% | none/visible — | block/visible **100%** |
| side:attend | 1600 | none/**hidden** — | block/**hidden** 15.2% | none/visible — | block/visible **15.2%** |
| side:bless | 1600 | none/**hidden** — | block/**hidden** 8.9% | none/visible — | block/visible **8.9%** |
| menu:bag | 1600 | none/**hidden** — | block/**hidden** 14.3% | none/visible — | block/visible **14.3%** |
| prof:19 | 2280 / 1600 | none/**hidden** — | block/**hidden** 74.4% / 20% | none/visible — | block/visible **74.4% / 20%** |
| **cur:gold** | 2280·1600 | **block/hidden** 100% | block/**hidden** 100% | block/**visible 100%** | block/visible 100% |
| tab:adv | 2280·1600 | block/visible 0% | 같음 | 같음 | 같음 |

### 갈래 판정 — **ⓐ·ⓑ 둘 다 기각**

* **ⓐ(노드가 이름·구조째 바뀌었다) 기각** — `#tuto` 는 네 트리 · 전 표본에서 살아 있고,
  `display` 가 살아 있는 표본에서 껍데기 상자도 **460×150 그대로**다.
  (`visibility:hidden` 은 상자를 안 접는다 — 그래서 `#ciw` 자리는 T0 에서도 460×150 이다.)
* **ⓑ(오프너 클릭이 조용히 실패했다) 기각** — 호스트 상자(`#modal>.mbox` 940×1077 ·
  `#blsw>.bls` 998×1157 · `#bagw>.bg53` 948×967 · `#pfw>.pf` 896×1396/1296 · `#ciw>.ci` 598×813)가
  전부 눈에 보이는 크기로 잡힌다 = 화면은 실제로 열렸다.

⇒ 남는 답은 하나 — **제품이 «한 겹 더» 숨기게 됐고 자가 그 겹을 모른다.**

### 실재 — **811 이 419 를 덮었다**(제품은 옳고 자가 낡았다)

`index.html` 286~287(작업 **811**, 2026-09-02 완료 — 주인 지시 «팝업이 떠 있는데 그 밖의
HUD(**미션 트래커** · 재화 표시 · 스킬 슬롯줄)가 딤 너머로 읽힌다»):

```css
#app:has(:is(#modal,#offw,#dgdw,#wpnw,#prbw,#collw,#ciw,#pfw,#specw,#upw,#sumw,#dclw,
             #defw,#blsw,#bagw,#cfw,#statw).on) :is(#top,#tuto,#slots){visibility:hidden}
```

* 811 의 **17종 ⊃ 419 의 일곱**(`#modal`·`#collw`·`#blsw`·`#bagw`·`#pfw`·`#specw`·`#cfw` 전부 포함).
* `verify419` 의 `vis()` 는 `visibility:hidden` 을 «안 보인다» 로 읽어 `visPct` 를 **`null`** 로 낸다.
  74건의 «기대 100 · 실제 null» 은 값이 틀린 게 아니라 **잴 것을 못 찾았다** 는 뜻이고,
  못 찾은 이유는 노드가 사라져서가 아니라 **잉크가 꺼져서**다.
* 그래서 §1(«토막» 0건)은 **전부 초록**이다 — 숨은 배너는 토막이 아니기 때문이다.
  죽은 것은 «배너를 찾아 %로 재는» 축 하나뿐이라는 등재문의 관측이 그대로 맞았다.

### ⚑ 이 작업의 본체 — **`#ciw` 한 자리의 방향을 누가 정하는가**

419 는 `#ciw`(33 재화 정보 팝업)를 숨김 목록에서 **일부러 뺐다** — 407 이 하단 여백으로
자리를 만들어 배너를 살려 둔 자리라, `verify419` §2-c 가 «배너가 **안 숨는다** · **100% 보임**»
을 네 항으로 못박고 있었다. 811 은 그 자리를 목록에 넣었다.

**811 이 나중 지시이고, 주인 원문이 «미션 트래커» 를 지목한다** ⇒ 333 처방대로 **나중 지시가
옳은 쪽으로 게이트를 돌린다.** 407 이 만든 자리(하단 여백)는 제품에 그대로 남아 있고,
그것이 살아 있다는 증거는 §R2 의 T2 항(811 에서 `#tuto` 만 빼면 **보임 100% 가 그대로 돌아온다**)이
매 실행 다시 잰다 — 자리를 비우지 않고 **한 칸 옮겼다**(310·333 선례).

### 처방 — 제품 `index.html` **0줄**, 고치는 것은 `tools/verify419.js` 한 파일

1. **§0 에 «두 번째 겹» 을 등재** — 811 선언이 있고, `#tuto` 를 같이 숨기고, 그 목록이
   419 의 일곱을 **전부 품는다**(⊇). 811 이 좁아지면 여기가 먼저 빨개진다.
2. **§2-c 방향 전환** — «안 숨는다 / 100% 보임» → «811 이 덮어 **숨는다**(`visibility:hidden`) ·
   그래서 **토막이 아니다**». 407 의 자리는 §R2 가 대신 잰다.
3. **§R 의 사본을 «두 겹 다 뺀 것»(T3)으로** — 그래야 `BEFORE` 표(15.2 · 8.9 · 14.3 · 20 · 74.4 …)를
   다시 잴 수 있다. 재현이 **그 값이 한 칸도 안 바뀌었음**을 이미 확인했다(위 표 T3 열).
4. **§R2 신설 — 겹 분리 시험**: 419 만 빼도(T1) 일곱은 숨고, 811 의 `#tuto` 만 빼도(T2) 일곱은 숨는다
   = **두 겹이 각각 혼자서도 그 일곱을 지킨다.** 무르게 푼 수리가 아님을 이 짝이 못박는다
   (334 처방 — «접두어만 빼는» 길은 선언이 통째로 사라져도 초록이다).

### 반려한 길

* ⓧ **419 선언을 죽은 코드로 철거**(678 선례) — T1 이 «419 만 빼도 여전히 숨는다» 를 찍었으니
  지금은 실제로 덮여 있다. 그래도 **안 지웠다**: 두 선언은 목록이 다르고(419 의 일곱 ⊂ 811 의 17),
  811 이 언젠가 좁아지면 419 의 일곱이 통째로 무방비가 된다. 겹은 공짜고, 이 작업 단위는
  «게이트 부패» 라 제품 0줄이 정답이다. §R2 가 그 겹의 값을 매 실행 증명한다.
* ⓧ **§2-c 를 «숨든 안 숨든 통과» 로 무르게 풀기** — 그 순간 811 선언이 통째로 사라져도 초록이다(334).

### 게이트

* `probe850` — 신설(재현자 · 네 트리 대조)
* `verify419` — §0 +3항 · §2-c 방향 전환 · §R 사본 교체 · §R2 신설
* 회귀 — `verify811` · `verify407` · `verify351`(있으면) · SMOKE

---

## 2회차 (2026-09-03, sess-0043-6582) — 자 이관

### 기준선 — 착수 트리에서 재확인

`node tools/verify419.js` → **`VERIFY419 162/236 FAIL`** (등재문 수치와 한 칸도 안 다르다).
빨강 74건의 절 분포:

| 절 | 건수 | 문장 |
|---|---|---|
| `[R]` | **58** | «두 겹을 뺀 사본에서는 배너가 안 숨는다» + 보임 % |
| `[R-음성]` | **8** | «사본에서도 온전히 보인다 / 보임 0%» (기대 100·0 · 실제 **null**) |
| `[2-c]` | **8** | «배너가 안 숨는다 · 100% 보임» (기대 100 · 실제 **null**) |

§0·§1·§2-a·§2-d·§2-e·§3 은 **전부 초록** — 죽은 것은 «배너를 찾아 %로 재는» 축 하나뿐이라는
등재문의 관측이 그대로 맞았다.

### 고친 것 — `tools/verify419.js` 한 파일 (제품 `index.html` **0줄**)

| 자리 | 전 | 후 |
|---|---|---|
| §0 | 419 선언만 묻는다 | **+4항** — 811 선언이 한 번 있다 · `#tuto` 를 같이 숨긴다 · 811 의 17종 ⊇ 419 의 일곱 · `#ciw` 는 419 밖·811 안 |
| §2-c | «배너가 **안 숨는다** · 100% 보임» ×4 | «**811 이 껐다**(`visibility:hidden`) · 419 는 여전히 안 건드린다(`display` ≠ none) · 그래서 토막이 아니다» ×12 |
| §3·§R 사본 | 419 선언만 뺀 사본 | **두 겹을 다 뺀 사본**(419 선언 + 811 목록의 `#tuto`) |
| §R2 | (없음) | **신설** — 겹 분리 시험 12항 |
| `MEAS` | `display` 만 찍는다 | `visibility` 도 찍는다 (어느 겹이 껐는지 말하려면 두 낱말이 다 필요하다) |

⚑ **§2-c 를 «세 항» 으로 늘린 것이 방향 전환의 핵심이다.** 「숨는다」 하나만 물으면
누군가 `#ciw` 를 419 목록에 넣어도 초록이다 — 그러면 407 이 값을 치른 자리가 조용히 사라진다.
그래서 **`display` 가 `none` 이 아님**을 짝으로 물어(§0 `RULE_NEVER` 의 실행판) «419 는 이 자리를
안 건드린다» 를 매 실행 다시 잰다.

### §R2 — 이 수리가 무르지 않다는 증거

§R 이 두 겹을 **한꺼번에** 빼기 때문에, 그것만 두면 «419 선언이 통째로 사라져도 §R 은 초록» 이 된다
(334 가 잡은 바로 그 함정 — 「접두어만 빼면 시계가 없어져도 초록」). ⇒ 겹을 **하나씩만** 뺀 사본 둘로:

* `[R2-a]` 419 만 빼도 일곱은 `visibility:hidden` — **811 이 혼자 지킨다**
* `[R2-b]` 811 의 `#tuto` 만 빼도 일곱은 `display:none` — **419 가 혼자 지킨다**
* `[R2-c]` 811 을 걷으면 `#ciw` 의 배너가 **온전한 100%** 로 돌아온다 — **407 의 하단 여백이 제품에 살아 있다**

`[R2-c]` 가 §2-c 를 뒤집으며 비운 자리를 그대로 채운다(333·310 «자리를 비우지 않는다»).

### 반려한 길(2회차에 다시 확인)

* ⓧ 419 선언 철거 — `[R2-b]` 가 «419 가 혼자서도 지킨다» 를 매 실행 찍는다. 811 목록이 언젠가
  좁아지면 그 겹이 유일한 방어가 된다. 겹은 공짜다.
* ⓧ §2-c 를 «숨든 안 숨든 통과» 로 무르게 풀기 — 811 선언이 사라져도 초록이 된다.
