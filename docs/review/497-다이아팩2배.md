# 497 — 다이아 팩 5종 «×2» · 마일리지 교환 «×2» (13 재화 탭)

> **완료 (2026-08-31, sess-1751-6387 워커 H · 1회차)** · T2(주인 확정 · 상수 6개) ·
> 지시서 [3]-**(가)** 기계적/상수 작업이라 **비평가는 안 띄웠다**(레퍼런스 대조가 아니다).
> 제품 `index.html` **6자리**(상수 5팩 + `MILE_DIA` + 파생 문구 1줄) · **레이아웃 Δ0px** · 판정 0줄 · 세이브 이관 없음.

주인 확정(2026-08-31, ROUTINE «신규 주인 확정(2026-08-31 01:10)» · PROGRESS 497 등재문):

> «다이아 보급(하루 27만)이 많아 과금할 이유가 없어 보임 → 상점 팩 2배 · 마일리지도 2배 ·
> 500만 개 얻으면 괜찮은 느낌» — 선택지 1(팩 ×2 · 과금 한 벌 ₩150만) 채택.

---

## 1. 재현이 먼저다(338 규칙) — `tools/probe497.js` **7/7 신설**

등재문이 처방으로 적어 둔 것은 «카드 숫자 폭이 늘어 잘리지 않는지(470/477 규약) 확인» 한 줄뿐이라,
**바꾸기 전에 지금 폭과 바꾼 뒤 폭을 실제로 찍었다**(자릿수 × 상수로 지어내지 않았다 — 이 서체는
글리프마다 폭이 다르다).

| 칸 | 지금(116 «÷2») | 자연폭 | 렌더폭 | 좌여백 | → «×2» | 자연폭 | 렌더폭 | 좌여백 |
|---|---|---|---|---|---|---|---|---|
| d1 | ×5,000 | 100.2 | 110.2 | 143.0 | ×10,000 | 114.6 | 126.0 | 128.8 |
| d2 | ×35,000 | 117.0 | 131.1 | 122.0 | ×70,000 | 120.5 | 135.1 | 119.7 |
| d3 | ×75,000 | 118.7 | 130.8 | 122.4 | ×150,000 | 132.3 | 145.8 | 109.0 |
| d4 | ×450,000 | 138.1 | 160.8 | 92.3 | ×900,000 | 138.8 | 161.7 | 93.1 |
| d5 | ×1,000,000 | 159.9 | 193.9 | 59.3 | ×2,000,000 | 166.3 | 201.6 | **53.2** |

**결론: 넘침 0칸이고 `qx` 는 한 칸도 안 건드려도 된다.** 카드 안쪽(`.bg` 260px) 기준 최악 칸(d5)의
좌여백이 59.3 → **53.2px** 로 줄 뿐이고, 우여백은 `.cn-cd.dia>.qt{right:14px}` 우측 앵커라
**다섯 칸 전부 6.9px 고정**(자릿수가 늘면 왼쪽으로 자란다 — 6415행 주석 «×200만까지 길어지므로
완료 카드와 같은 우측 앵커» 가 이 경우를 이미 예상해 두고 있었다).

마일리지 패널 문구(`.rw`)는 «2,500,000 → 5,000,000» 으로 잉크가 **394.9 → 395.7px**(+0.8) 뿐이다
(같은 자릿수라 글리프만 바뀐다). 33 재화 정보 팝업의 획득처 줄도 잉크 402.9 / 호스트 480 ⇒ 우여백 37.1px.

### ⚑ 재현이 찾아낸 것 — **하드코딩 잔재 1건**(probe497 [4])

`CURINFO.mile.ways[0]` 이 «다이아 **90만 · 200만** 팩 구매» 로 **손으로 적혀** 있었다.
116 이 2026-08-26 에 값을 절반으로 내린 뒤 실제 쿠폰 팩은 45만·100만이었으므로,
이 줄은 **닷새 동안 실제와 어긋난 채** 33 팝업에 떠 있었다. 497 이 값을 되돌리면 «우연히» 다시 맞지만,
같은 일이 또 나므로 **팩 표에서 파생**시켰다(`DIA_PACKS.filter(p => p.cp)` = 쿠폰이 나오는 팩).
`verify497` **B7·B8** 이 그 파생을 못박는다.

---

## 2. 제품 변경 — `index.html` 6자리

```
DIA_PACKS  d1 5,000 → 10,000 · d2 35,000 → 70,000 · d3 75,000 → 150,000
           d4 450,000 → 900,000 · d5 1,000,000 → 2,000,000     (won·cp·id 불변)
MILE_DIA   2,500,000 → 5,000,000                                (MILE_NEED 10 불변)
CURINFO.mile.ways[0]  손으로 적은 «90만 · 200만» → 팩 표 파생
```

* **라벨은 안 적었다** — `q: '×' + fmt(p.dia)` 파생이 그대로라 값 한 곳만 바꾸면 카드·구매 팝업 상품명
  (`diaPackName`)·우편 제목이 같이 움직인다(LESSONS 111-1). `qx` 는 **0줄**.
* **안 건드린 것(지시 ③)** — 이용권 대체가 `PASS_OFFPLUS_DIA` 50,000 · 룰렛 · 13 광고 상품 보석 ×100.
  주인 원문은 «상점 **재화로 파는 것**» 이다. `verify497` [D] 가 그 셋의 불변을 단언한다.
* **세이브 이관 «없음» 이 정답**(지시 ④) — `S.dia`·`S.mileage` 는 «이미 받은 수량» 이고 팩은 id 로만
  기억된다. KEY 를 안 올렸고 `verify497` [E] 가 구 세이브 실로드로 못박는다.

---

## 3. 게이트 — **116 과 정면으로 부딪히는 것이 이 작업의 본체다**

`tools/verify116.js` 는 이름 그대로 «전부 ÷2 인가» 를 지키는 자였다. 497 은 그 결정을 **되돌리는** 지시라,
두 게이트가 서로 반대를 단언하는 자리다 — **295 가 149 를**, **402 가 `verify125` H2 를** 갈아 끼운 것과
같은 꼴이고, 규칙도 같다: **나중 지시가 옳은 쪽이고, 게이트는 자리를 비우지 않고 이사한다.**

* `verify116` **41/41**(수 그대로) — [A1][A2][E2][E4] 기대값을 새 값으로, **A5 의 방향을 뒤집었다**
  («옛 값 = 1만·7만·… 부재» → «116 «÷2» 값 = 5천·3.5만·… 부재»). 116 이 지키던 나머지
  (가격·쿠폰·`MILE_NEED` 불변 · 라벨 파생 · 우편 경유 · 구 세이브 무변경 · 넘침 0)는 **한 항도 안 지웠고
  허용 오차도 한 칸 안 넓혔다.**
* `verify153` **67/67** — «÷2» 시절 팩 값이 **두 자리에 리터럴로** 박혀 있었다([D] 클릭 수령 450,000 ·
  [C] `load()` 뒤 우편 보상 35,000). 값만 ×2 로 옮겼다 — 이 자가 보는 것은 «경로(우편 경유)» 지 수량이 아니다.
* `tools/verify497.js` **37/37 신설** — [A] 상수(«정확히 2배» 포함) · [B] 표기·넘침·파생 문구 ·
  [C] 지급 경로(구매 직후 Δ0 · 우편 1통 · 수령 시 새 값) · [D] 안 건드린 것 · [E] 구 세이브 ·
  **[R] 되돌림 시험**(116 «÷2» 사본을 만들어 [A]·[C] 가 실제로 빨개지는지 확인 — R1·R2·R3).

**회귀(무수정)**: `verify398` 24/24 · `verify365` 36/36 · `fnchk125` 17/17 · **SMOKE PASS**.

⚠ **[D] 의 한 항은 병행 세션 때문에 갈아 끼웠다** — 처음엔 «룬 대체가 `RUNE_DIA` 존재» 로 «안 건드렸다» 를
쟀는데, rebase 로 들어온 **490(진행 중 · 주인 지시 «룬 강화는 룬강화석만, 다이아 폐지»)이 그 상수를 지웠다.**
없어진 것이 정답인 상수를 «있어야 한다» 로 세우면 **남의 정당한 작업이 내 게이트를 빨갛게** 만든다 ⇒
같은 뜻을 **497 이 실제로 안 건드린 축**(180 신규 `NEW_DIA` 100만 · `MONTHLY_DIA` 10만)으로 옮겼다.

---

## 4. 199 에게 넘기는 것(등재문 ④ — 계수 확정은 199 몫)

| 축 | 값 |
|---|---|
| 최대 팩 단가 | 200만 다이아 / ₩110,000 = **18.2 다이아/원** |
| 최대 팩이 무과금 며칠치인가 | 200만 ÷ 하루 27만 = **7.4일치**(116 이전 근거인 «관례 1~2주» 안) |
| 과금 한 벌(2,730만 다이아) — 팩만 | 2,730만 ÷ 200만 = 13.65팩 ⇒ **₩1,501,500 ≈ ₩150만** |
| 마일리지를 세면 | d5 5팩 = 1,000만 + 쿠폰 10 → 500만 ⇒ 5팩당 **1,500만**. 10팩(₩110만)에 **3,000만** ⇒ 한 벌이 **₩110만 안팎**으로 내려간다 |

⚑ **199 가 판단할 것**: 주인 목표는 «과금 한 벌 ≈ ₩150만» 인데 **마일리지 보너스를 세면 ₩110만**이다
(등재문은 «7팩마다 500만 → 실효 ₩130만» 으로 적었지만, d5 는 팩당 쿠폰 **2개**라 **5팩마다** 500만이다 —
이 세션이 다시 센 값이다). 손잡이는 셋 — ⓐ `MILE_NEED` 를 올린다(10 → 14) · ⓑ `MILE_DIA` 를 내린다 ·
ⓒ 한 벌 값(2,730만)을 496 쪽에서 다시 잡는다. **497 은 주인이 못박은 «팩 ×2 · 마일리지 ×2 · 쿠폰 수 불변»
만 했고 어느 손잡이도 안 당겼다.**

---

## 5. 곁다리 관측 1건 — **511 등재**(`verify149` 2건 실패)

착수 시점 트리(`44f93a2`)에서 `verify149` 가 **284/286** 이었다 —
§1 «팝업 유지 — 승급 성공(코스튬 그리드)» 1건 + 그 묶음 집계 1건.
338·344 규칙대로 **내 수리 전 트리에서도 같은 284/286** 임을 직접 대조 실행으로 확인했다
(`git show HEAD:index.html` 로 되돌려 실행 → 같은 항목이 같은 문구로 실패).
내 변경과 무관하고 내 작업 단위가 아니라 안 고쳤다 — PROGRESS **511** 행에 등재했다.

⚠ **rebase 뒤에는 283/288 로 더 빨개진다** — 늘어난 두 건(§1 토스트 «유물조각 교환 — 다이아 부족» ·
«유물조각 교환 완료»)은 내 작업이 아니라 **`d2875a0` = 진행 중인 490**(주인 지시 «상점 다이아 →
유물조각/룬강화석 1:1»)이 그 토스트 문구를 바꾸면서 생긴 것이다. **490 은 지금 lock 이 살아 있는
작업이라 등재하지 않았다**(그 세션이 자기 회차에서 볼 몫이다). 511 은 **승급 계열 1건만** 가리킨다.
