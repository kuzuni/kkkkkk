# 54 랭킹 팝업 — 회차 기록

> 측정표 `docs/measure/54-랭킹팝업.md` · 캡처 `docs/review/54-r*.png`
> 스크립트 `tools/scan54.py`(레퍼런스 스캐너) · `tools/cap54.js`(캡처) · `tools/verify54.js`(기하 + 기능 게이트)
> 담당 세션: **sess-0748-7696 (워커 D, 2026-08-25)** — T3, 이 화면은 이 세션이 처음 구현했다(이전 기록 없음).
> 죽은 lock(05:25 sess-0500-22861, 143분) 회수. 앞 세션의 산출물은 없었다(review·measure 파일 모두 부재).

## 껍데기 판정 — **(다) 딤·테두리 없는 전체화면 페이지**

03 던전 · 14 유물 · 35 패스와 같은 계열이다. 모달 껍데기가 아니다.

- **딤 반증**: `scan54.py bbox 0 0 1079 775 near:FFFFFF:2` → **MATCH 3644**. 상단 씬에 순백 화소가 남아 있으므로
  알파 검정 오버레이가 깔렸을 수 없다.
- **상태바 반증**: `bbox 0 0 1079 84 light>90` → **MATCH 0**. 상단 84px 은 OS 바가 아니라 씬 배경 그대로다.
- 닫기 ✕ 가 없다 — 하단 내비바의 **← 화살표**가 그 역할을 한다.

## 세로 밴드 7장 (프레임 = ref y − 84)

| 밴드 | 프레임 y | h | 구현 |
|---|---|---|---|
| ① 포디움 씬 | 0..691 | 692 | `.rk-scene` (top 앵커) |
| ② 검정선 | 692..701 | 10 | `.rk-div1` |
| ③ 안내문 밴드 | 702..800 | 99 | `.rk-tip` |
| ④ 리스트 패널 | 802..1884 | 1083 | `.rk-panel` — **top + bottom 양쪽 앵커(가변)** |
| ⑤ 내 순위 바 | 1905..2076 | 172 | `.rk-me` (bottom 203) |
| ⑥ 검정선 | 2077..2084 | 8 | `.rk-div2` (bottom 195) |
| ⑦ 내비바 | 2086..2249 | 164 | `.rk-nav` (bottom 30) |

**남는 높이 흡수는 리스트 패널 하나가 맡는다**(LESSONS 22-④). 하단 3장을 전부 `bottom:` 앵커로 두었기 때문에
짧은 프레임(1600)에서도 내 순위 바·내비바가 삼켜지지 않고 패널만 줄어든다 — smoke [3] 6종 화면비 전부 통과.

레퍼런스 캡처는 ref 2340 − 84 = **2256** 까지라 프레임 2280 의 **하단 30px 은 캡처 밖**이다.
그 띠는 `#rkw` 배경을 내비바와 같은 `#3C2D28` 로 칠해 «내비바가 이어진» 것처럼 보이게 했다.

## 1회차 (sess-0748-7696)

측정표를 PIL 스캔으로 새로 만들고(310줄) 화면 전체를 한 번에 올렸다.

**구현한 것**
- 씬: 타이틀 육각 명판(486×114 @ top 20) + 시상대 3개 + 단상 위 텍스트 4종(방패·칭호 리본·닉네임·점수 알약)
- 안내문 밴드: 한글 46자를 ref 잉크폭 894 에 맞추기 위해 `transform:scaleX(.67)` (LESSONS 01 — 음수 letter-spacing 금지)
- 리스트: 패널 998×1083(테두리 9px 1겹, **검정 겹 없음**) + 행 950×149 · pitch 165 · 립 5 `#F7EBD9`
- 행 부품: 좌 탠 칸 148×138 · 배지(1~3위 메달 102×80 / 4위↓ 플랫 96×96) · 아바타 111×113 · **리본 256 고정폭** ·
  닉네임 · «최고 스테이지» 라벨 · 점수 알약 179×50 r25 · 우측 탠 **평행사변형(좌변 Δ29)**
- 내 순위 바: **행과 같은 부품 클래스를 전폭 1080 으로 재사용**(측정표 주의점 2)
- 내비바: ← + 탭 3개(활성 288 / 비활성 188·190, 활성 표시는 밝은 fill + 폭 +100 두 가지뿐)

**기능** — 서버가 없으므로 UI-REFERENCE 54 지시대로 «고정 더미 사다리 + 내 실기록».
`RANK_NPC` 40명은 상수이고 내 행만 `S.best`·`cp()` 로 만들어 끼워 넣는다. 사다리가 고정이라
**내가 스테이지를 올리면 내 순위가 실제로 올라간다**(목업이 아니다).

### 기능 체크 (헤드리스 실측 — `tools/verify54.js`)

| 조작 | 확인된 변화 |
|---|---|
| ▦ 메뉴 → «랭킹» | `#rkw.on` — 기존 «준비 중» 팝업 분기를 `openRank()` 로 교체 |
| 리스트 렌더 | 41행(더미 40 + 나), 1~3위 메달 배지 · 4위 이하 숫자 배지, 리본 폭 6행 전부 256 |
| 내 순위 바 | `S.best 50` → **27위**, 닉네임·최고 스테이지가 내 실제 값 |
| `S.best` 50 → 300 | 순위 27 → **6위로 상승**, 알약 값도 300 으로 갱신 |
| 포디움 | 1·2·3위 닉네임·점수·칭호가 리스트 상위 3행과 같은 데이터로 그려짐(내림차순 검증) |
| 행 클릭 | 그 유저의 칭호·최고 스테이지·전투력 상세 팝업 |
| 탭 «시련의 탑»/«몬스터 농장» | 콘텐츠 미존재 안내 팝업 + **리스트는 바뀌지 않음**(35 패스 잠금 탭과 같은 처리) |
| ← 뒤로가기 | 페이지 닫힘 |

**자체 실측**: `VERIFY54 57/57` — 기하 38항목이 전부 측정표와 **Δ≤1.0px**, 기능 12항목 통과,
NaN/undefined 0건, 콘솔 에러 0건. `node tools/smoke.js` **SMOKE PASS**(69 ✓).

smoke 확장: `#rkw .rk-panel/.rk-me/.rk-nav` 를 화면비 잘림 후보에 추가하고 오프너로 `openRank()` 를 넣었다
(새 오버레이를 만들면 smoke 목록에 넣는 것까지가 작업 범위 — ROUTINE [6]).

## 1회차 채점 (비평가 A · B, 독립)

| 항목 | A | B |
|---|---|---|
| ① 배치 | 5 | 6 |
| ② 상대 크기 | 4 | 4 |
| ③ 여백·간격 | 5 | 4 |
| ④ 정렬·그리드 | 5 | 5 |
| ⑤ 색·명암 | 6 | 6 |
| ⑥ 폰트 | 4 | 4 |
| **①~④ 최저** | **4** | **4** |

신뢰도 체크는 둘 다 통과(행 pitch 165 · 패널 bbox · 점수 알약 179×50 · 내 순위 바 172 를 우리 측정표와 1~2px 이내로 맞혔다).

### 두 비평가가 «일치»한 지적 (그대로 반영)

| 지적 | ref | 1회차 | 원인 |
|---|---|---|---|
| 내비 탭이 한 덩어리로 보임 | 탭 사이 검정 세퍼레이터 4~5px (488·680·875) | **세퍼레이터 0개** | 측정표 §9 에 있는데 구현에서 통째로 빠졌다. 비활성 탭 색이 내비 배경과 거의 같아 «경계는 세퍼레이터가 만든다» |
| 타이틀 잉크가 배너 중심에서 아래로 | cy 78.5 | cy 99.5 (Δ21) | line-box 가 21px 아래로 밀려 있었다 |
| 타이틀 잉크 폭 | 220 | 248 (+13%) | scaleX 없이 그대로 |
| 행 순위 숫자 | 28×35 | 19×27 (−30%) | **«잉크비 0.93» 은 한글 받침 기준이라 숫자엔 안 통한다**(숫자 잉크비 ≈0.70) |
| 점수 알약 숫자 | h25 | h19 (−24%) | 같은 원인 |
| «최고 스테이지» 라벨 | 161×23 | 145×21 | 같은 원인 |
| 행 우측 사선 웨지 폭 | 211 | 144 (−32%) | `clip-path` 좌표를 요소 로컬이 아니라 행 로컬로 써서 65px 을 더 깎았다 |
| 4위↓ 순위 도형 높이 | 96 | 84 (−12%) | `clip-path` 를 `0 12%` 에서 시작해 bbox 위쪽 12% 가 잘렸다 |
| 안내문 잉크 폭 | 896 | 708 (−21%) | scaleX 값 과소 |
| 내비 탭 라벨 | 129×28 / 153×30 | 143×31 / 176×31 (+11~16%) | fs 과대 |
| 시상대 밑변 폭 · 사이 간격 | 밑변 310/306/312, 간격 13~14 | 287/288, 간격 38 | **사다리꼴을 직사각형으로 만들었다** |
| 패널 바깥 배경 | `#634032` (밝기 70) | `#3C2D28` (48) | 본문 갈색 밴드를 안 깔아 패널 경계가 프레임 선에만 의존 |

### 비평가가 «엇갈린» 지적 — 제3의 독립 검산으로 판정 (LESSONS 52-①)

| 항목 | A | B | 자체 재검산 | 판정 |
|---|---|---|---|---|
| 행 높이·간격 | 행 137 / 간격 28 | 본문 138 / 간격 27 | `col 700 1380..1580`: 크림 **1406..1553 = 148**, 위 갭 13, 아래 갭 14 | **우리 값(149/16) 유지.** 둘 다 «탠 칸» 을 행으로 읽었다. 검산식 149+16 = 165 = pitch ✓ |
| 닉네임 잉크 높이 | ref 34 (우리가 작다) | ref 26 (우리가 크다) | ref 행1·2·4 = **22 / 25 / 26**, 우리 32 | **B 채택** → fs 35 → 28 |
| 아바타 폭 | ref 112 / 우리 95 | ref 112 / 우리 110 | 우리 요소 111 (verify54) | **B 채택, 수정 없음** |
| 3위 시상대 상단 | 언급 없음 | ref 552 (우리 −24) | `col 740`: 검정 555..557 → 윗면 **555** | **B 채택** → 프레임 471, 높이 221 (측정표 §12 의 «576» 은 캐릭터에 가려 잘못 읽은 값이라 정정) |
| 시상대 폭·인셋 | 밑변 323/336/323, 인셋 49 | 폭 312, 간격 13, 인셋 59 | `row 770`: 채움 **60..369 / 387..692 / 709..1020** | 둘 다 «밑변» 을 본 것이 맞다 — 사다리꼴로 구현 |

## 2회차 — 위 지적 일괄 반영

`.rk-mid`(본문 갈색 밴드) · `.rk-sep`(내비 세퍼레이터 3개) 신설, 시상대 3개를 사다리꼴로 재작도,
`clip-path` 버그 2건(웨지·배지) 수정, 글자 6종을 **캡처 잉크 실측 역산**으로 재계산.

### 잉크 실측 대조 (프레임 좌표, `scan54.py bbox`)

| 텍스트 | ref | 1회차 | 2회차 |
|---|---|---|---|
| 타이틀 | 220×36 · cy 78.5 | 248×36 · cy 99.5 | **214×36 · cy 78.5** |
| 안내문 | 896×27 · cy 742 | 708×26 | **896×27 · cy 743** |
| 행 순위 숫자 | 28×35 · cy 1394 | 19×27 | **30×36 · cy 1394.5** |
| 점수 알약 숫자 | h25 · cy 915 | h19 | **h26 · cy 914.5** |
| 닉네임(행4) | h26 · 잉크top T+89 | h32 · T+83 | **h25 · T+89** |
| «최고 스테이지» | 161×23 · cy 869 | 145×21 | **162×23 · cy 870** |
| 내비 탭2 라벨 | 129×28 · cy 2215.5 | 143×31 · cy 2219 | **131×29 · cy 2216** |

### 도형 실측 대조

| 요소 | ref | 1회차 | 2회차 |
|---|---|---|---|
| 4위↓ 순위 도형 | 98×96 | 96×84 | **96×95** |
| 행 우측 웨지(행top+19) | x799..1009 (211) | x864..1008 (144) | **x799..1008 (210)** |
| 시상대 밑변(프레임 y686) | 310 / 306 / 312 | 288 / 287 / 287 | **311 / 307 / 324** |
| 시상대 사이 간격(밑변) | 13~14 | 38 | **14 / 13** |

`VERIFY54 61/61`(기하 42 + 기능 12 + 잡음 검사) · `VERIFY55 58/58`(다른 워커 화면 회귀 없음) · `SMOKE PASS`.

## 2회차 채점 (비평가 D) — ①5 ②3 ③5 ④8 → **최저 3**

신뢰도 체크 통과(시상대 밑변 60..370/387..692/709..1020 · 타이틀 잉크 222×37 · 사선 패널 기울기 · 고정 바 알약 bbox 를 전부 우리 값과 Δ≤3 으로 맞혔다).
④ 정렬은 **8/10** 으로 올라왔다 — 「행 pitch 165 / 틈 16 / 높이 149, 알약·아바타·리본·내비 버튼 x 가 Δ≤4px」.

**감점이 한 곳에 몰렸다: 시상대 위 캐릭터 «아트 자리»**
| | ref | 2회차 | Δ |
|---|---|---|---|
| 1위 아트 bbox | x386..694 · y230..530 (309×301) | 290×300 @ (396,148) | 위치 −80px, 폭 −6% |
| 2위 아트 bbox | x24..380 · y239..564 (357×326) | 290×300 @ (71,182) | **세로 −8%, 가로 −19%** |
| 3위 아트 bbox | x700..1041 · y232..552 (342×321) | 290×300 @ (722,192) | 가로 −15% |

이모지 잉크는 상자의 60% 밖에 안 채워서 실제로는 «세로 −50% · 가로 −33~46%» 로 읽혔고,
그 결과 타이틀 판 아래 143px · 아트 사이 245px 의 죽은 여백이 생겨 ①·③ 이 같이 깎였다.

### 반려한 지적 (자체 검산으로 기각)

| D 지적 | 자체 검산 | 판정 |
|---|---|---|
| «타이틀 판이 14% 두껍다(ref h100 vs 114)» | ref **외곽** 104..217 = 114 = 우리 114. D 는 ref 는 *fill*(112..211), 우리는 *outer* 를 쟀다 | 기각 |
| «안내띠~패널 간격 36 vs 52, 패널 프레임 23 vs 10» | 우리 `.rk-tip` 702..800 = ref 786..884 ✓, 패널 top 802 = ref 886 ✓ (verify54 Δ0) | 기각 — 색 전이를 경계로 읽은 것 |
| «본문 폰트가 일괄 8~12% 크다» | 자체 잉크 스캔은 ref 이름 22~26 / 점수 25 / 라벨 23 / 순위 35 로 우리와 Δ≤1 | 보류(⑥ 은 통과 무관, 비평가 3인이 서로 다른 임계값을 쓴다) |
| «시상대 전면 기울기가 ref 의 38%» | 우리 사다리꼴은 **외곽선**(윗면 72..359 / 밑변 60..369)을 그대로 옮긴 것. D 는 «윗면이 전면보다 튀어나온» 3D 단차의 전면만 쟀다 | 보류 — 슬랩/전면 2겹 분리는 아트 교체 시 무의미해 미적용 |

## 3회차 — 아트 자리 재작도 + 타이틀 테이퍼 + 내비 좌측 세퍼레이터

| 수정 | 근거 | 결과 |
|---|---|---|
| 아트 자리 3개를 ref 실측 bbox 로 (`bbox … 'light>78'`) | 1위 309×301@(386,146) · 2위 357×326@(24,155) · 3위 342×321@(700,148) | 이모지 잉크가 상자를 채우도록 fs 354/384/378 + scaleX 1.03/1.10/1.07 역산. 중앙 실측 잉크 **309×302** = 상자와 Δ1 |
| 타이틀 육각 테이퍼 6.2% → 12.3% | ref 폭 @y120 415 / @y190 436 vs 2회차 435/454 | 3회차 400/424 → Δ −15/−12 (2회차 +20/+18 에서 축소) |
| 내비 **좌측** 세퍼레이터 신설 (`.rk-sep.e0` x195) | `row 2250` 실측 ref x195..197 검정 | 세퍼레이터 4개(195·488·680·875)로 ref 와 개수 일치 |

`VERIFY54 65/65` · `VERIFY55 63/63` · `SMOKE PASS`.
`tools/cap54.js` 에 `#fxl{display:none}` 주입 추가 — 58 연출 파티클이 전체화면 페이지 위를 지나 채점을 오염시키는 문제(53·72 가 먼저 겪은 것)를 선제 차단.

## 2회차 채점 (비평가 C) — ①7 ②4 ③7 ④8 → **최저 4**

C 는 r2 를 봤다(세퍼레이터 지적은 3회차에서 이미 해소). 신뢰도 체크 4문항 전부 우리 값과 Δ≤2.
①③④ 가 7·7·8 로 올라왔고 **감점은 ② «아이콘·도형의 잉크 크기» 한 곳에 몰렸다**.

| 요소 | ref 잉크 | r2/r3 | 원인 |
|---|---|---|---|
| 1~3위 메달 엠블럼 | **102×82** | 51×84 (**폭 −50%**) | 이모지 «잉크» 는 em 박스보다 훨씬 좁다. 박스만 102×80 으로 맞추고 잉크는 안 쟀다 |
| 시상대 방패 | ≈100 | ≈47 (−53%) | 같은 원인 |
| 시상대 윗면 슬래브 | 1위 43 · 2위 31 | 14 (−55~67%) | 그라디언트 스톱을 임의값으로 |
| 시상대 검정 외곽 | 8~9 (좌 50..57 · 우 1021..1030) | **0** | 외곽선을 아예 안 그렸다 |
| 뒤로가기 화살표 | 94×82 | 81×64 (−14%/−22%) | 채움만 그리고 검정 외곽 누락 |
| 내비 아이콘 1·3 | 162×102 / 151×106 | 145×97 / 116×91 | 이모지 잉크 부족 |
| 패널 하단 안쪽 어두운 띠 | 8px | 0 | 7행 잘린 조각이 ref 52 vs 우리 59 |
| 내 순위 바 닉네임 잉크 top | ref +14px 아래 | — | 뱃지↔이름 간격 14 → 5 |

## 4회차 — ② «잉크 크기» 일괄 교정

| 수정 | 4회차 실측 | ref |
|---|---|---|
| 메달 엠블럼: fs 74→72 + **scaleX 2.0** (`<em>` 래핑) | **99×70** | 104×72 |
| 시상대 방패: `<em>` + scaleX 2.1 | — | — |
| 시상대 검정 외곽 8/6px 복원 + 상자 재정의 | 밑단 `50..57 검정 / 59..368 / 370..385 검정 / 387..692 / 694..709 검정 / 711..1021 / 1023..1030 검정` | `50..57 / 60..369 / 372..384 / 387..692 / 695..708 / 709..1020 / 1021..1030` |
| 윗면 슬래브 14 → 1위 43 · 2위 31 · 3위 35 | — | — |
| 뒤로가기 화살표에 검정 외곽 | **94×78** | 93×80 |
| 내비 아이콘 1·3 확대 (fs 105→110·99→115, scaleX 1.30·1.36) | — | 162×102 / 151×106 |
| 패널 `padding-bottom:8px` | — | 안쪽 어두운 띠 8 |
| 내 순위 바 닉네임 top 80 → 94 | — | 뱃지↔이름 14 |
| 타이틀 검정 외곽 9 → 7 | — | 6~7 |

`VERIFY54 65/65` · `VERIFY55 63/63` · `SMOKE PASS`.

## 3회차 채점 (비평가 E · F, 독립)

| 항목 | E | F |
|---|---|---|
| ① 배치 | 7 | 6 |
| ② 상대 크기 | **4** | **5** |
| ③ 여백·간격 | 8 | 7 |
| ④ 정렬·그리드 | 8 | 8 |
| ①~④ 최저 | **4** | **5** |

①③④ 는 6~8 로 안정됐고 감점은 다시 ② 한 곳이다. E·F 의 ② 지적 중 **슬래브 두께·메달 폭·뒤로가기 화살표는
4회차에서 이미 고친 항목**(둘은 r3 를 봤다). 새로 남은 것은 아래 3가지였다.

| 지적 | ref | r3 | 반영 |
|---|---|---|---|
| 캐릭터 아트가 화면 밖으로 잘리고 3개가 한 덩어리로 붙음 | 2위 좌단 x24 · 3위 우단 x1041, 서로 6px 간격 | 좌단 **x0** · 우단 **x1079**, y300 행에서 x44..776 연속 | 상자에 `overflow:hidden` + fs 재역산(384→350 · 378→335). **x0..23 / x1042..1079 밝은 화소 0개**로 확인 |
| 시상대 점수 알약이 «숫자만» | 잉크 166×20 = «N 스테이지» | 34×15 (숫자만) | 단위 복원 + fs·scaleX 역산 → **166×21 · cx 538.5**(ref 166×20 · 538.5) |
| 순위 숫자 «검정 bbox» 가 작다 | 40×48 (흰 코어 29×36) | 38×42 | 코어는 이미 맞았고 **외곽선이 편측 3 vs ref 6.5** 였다 → `text-stroke` 6 → 13px. 결과 **42×48** |

### 반려/보류한 지적

| 지적 | 자체 검산 | 판정 |
|---|---|---|
| F: 내비 탭 폭 281/184/184 · 간격 11 | 우리 288/188/190 · 세퍼 4~5. 비평가 A(286/188/187) · C(288/188/190) · 측정표(288/188/190) 가 우리와 일치 | **기각** (F 단독 이견) |
| F: 시상대 바닥 폭 −13px | `row 770` 직접 스캔: ref 채움 387..692 = 306 = 우리 값 | **기각** |
| E: 2위 시상대 윗면 ref y555 (3위와 동일선) | `col 90`: 검정 566..570 → 윗면 **566**. 비평가 D 도 «청 572» 로 읽었다 | **기각** (E 단독 이견) |
| E: 몬스터농장 아이콘 중심 +10.5px | 4회차에서 아이콘 크기를 바꿔 재측정 필요 | 보류 |

## 5회차 — 아트 클리핑 · 알약 단위 · 외곽선 + 리본 등급색

- `.rk-ch{overflow:hidden}` 으로 **아트 bbox 를 강제**(이모지 잉크비가 글리프마다 달라 fs 만으론 못 맞춘다)
- 시상대 알약 «N 스테이지» 복원 (fs 23 + scaleX 1.25 → 잉크 166×21)
- 순위 숫자 `-webkit-text-stroke` 6 → **13px** (검정 bbox 42×48, ref 40×48)
- 칭호 리본을 **등급색 8종**으로 (ref 6행이 전부 다른 색 — 측정표 §6-3. 단색은 정보 계층 손실)

`VERIFY54 65/65` · `VERIFY55 63/63` · `SMOKE PASS`.

## 5회차 채점 (비평가 G · H, 독립)

| 항목 | G | H |
|---|---|---|
| ① 배치 | 8 | 8 |
| ② 상대 크기 | **6** | **5** |
| ③ 여백·간격 | 7 | 8 |
| ④ 정렬·그리드 | 9 | 8 |
| ①~④ 최저 | **6** | **5** |

**회차별 ①~④ 최저 추이: 4 → 4 → (E4/F5) → 5 → **6****. ①③④ 는 7~9 로 안정됐고 남은 감점은 전부 ②다.

두 비평가가 «Δ≤2px 로 정확» 이라고 확인한 것: 리스트 패널·6개 행 내부 요소 전부 · 칭호 리본 · 아바타 액자 ·
점수 알약 · 내 순위 고정 바 · 내비 세퍼레이터 4곳 · 탭 폭 · 시상대 밑변 · 행 pitch/크림/간격 · 안내문 · 타이틀 잉크.

### 6회차 반영 (G 지적)

| 지적 | ref | 5회차 | 6회차 |
|---|---|---|---|
| 리스트 메달 엠블럼 잉크 | 104×96 · cx 144.5 | 99×81 · cx 142 | **103×96 · cx 145** |
| 시상대 순위 방패 잉크 | 101×81 | 92×65 | fs 62→77 · scaleX 2.1→1.86 (산식상 101×81) |
| 행 «최고 스테이지» 라벨 폭 | 168~170 (A·C·D) | 162 | scaleX 1.03→1.08 → **170** (H 재확인: ref 169 / 우리 170) |
| 뒤로가기 채움 상단 | 프레임 2133 | 2140 | top 9→6 |

### 7회차 반영 (G·H 공통 지적 — 시상대 윗면 슬래브)

ref 슬래브는 **원근 때문에 좌우 두께가 다르다**(2위 x70 에서 40px · x340 에서 50px, 3위 49px).
4회차에 내 `col x=90` 표본 하나(30px)만 보고 2위 31 · 3위 35 로 잡은 것이 원인이었다 —
**LESSONS 52-① «자체 스캔도 표본 위치가 틀릴 수 있다»** 가 그대로 재발했다. 평균값으로 정정:
2위 31 → **45**, 3위 35 → **49**, 3위 중간 띠 10 → **22**.

### 반려한 지적

| 지적 | 자체 검산 | 판정 |
|---|---|---|
| G: 패널 외곽 하단 ref 1960(→우리 8px 길다) · 패널↔바 간격 28 | `bbox … near:413028` → 패널 안쪽 895..1959, 외곽 886..**1968**. 측정표 §1-1 «④⑤ 사이 여백 1969..1988 = 20px» 과 일치하고 H 도 «간격 21/21 ✓» 로 확인 | **기각** |
| G: 내비 아이콘 폭 −11~16% | ref 값이 비평가마다 143×84(G)·143×91(A)·160×103(F)·162×102(C) 로 갈린다. 우리 159×90 은 그 한가운데 | **보류**(LESSONS 22-⑤ — 값을 사이에 두고 엇갈리면 움직이지 않는다) |
| H: 캐릭터 아트 폭 +26%, 시상대 침범 35px | 아트 **자리(bbox)** 는 ref 실측 x24..380 그대로다. ref 캐릭터는 아래로 갈수록 좁아지는데 이모지는 상자를 균일하게 채워서 생기는 차이 | **아트 대기** — 이미지로 교체하면 자동 해소 |

## 남은 문제 (다음 세션이 8회차부터 이어서 센다)

1. **시상대 윗면 슬래브의 «원근 기울기»** — ref 는 좌 40 → 우 50 의 평행사변형인데 우리는 균일 45다.
   재현하려면 슬래브를 `<u>` 그라디언트에서 떼어내 **별도 평행사변형 요소**로 만들어야 한다(2·3위 각 1개).
2. **시상대 캐릭터 아트의 «테이퍼»** — 위 반려표 참조. **아트 대기** 항목이다.
3. 1위 시상대 리본 cx +4px / 알약 텍스트 cx −4.5px (H 단독 지적, 미검증).
4. 내비 아이콘 잉크 크기 — 비평가 4명의 ref 값이 143~162 로 갈려 판정 보류.

**현재 상태: 7회차 · ①~④ 최고 6점** (통과 기준 8 미달). ROUTINE [3]-(나) «세션당 최대 5회» 를 넘겨 진행했으므로
여기서 lock 을 풀고 종료한다. 화면당 누적 상한은 10회이므로 **다음 세션은 8회차부터** 이어서 돌린다.

---

## 8회차 (sess-1028-25517, 워커 B) — «남은 문제 1» 시상대 원근 슬래브 회수

7회차가 남긴 1번(윗면 원근 기울기)을 실제로 고쳤다. 캡처 `docs/review/54-r8.png`.

### 먼저 재측정했다 (측정표 §3-1a 신설)

7회차는 «2위 x70 에서 40px · x340 에서 50px» 라는 **표본 2개**로 «평균 45 균일» 을 택했다.
x70 은 둥근 모서리라 표본으로 못 쓴다. 이번엔 가림 없는 열 전체를 훑었다:

| | ref 먼 모서리 | ref 가까운 모서리 | 7회차 구현 | 8회차 구현 |
|---|---|---|---|---|
| 2위 | x80..162 **573** → x303..347 **553** | **603 평평** | 균일 45px (윗면 572..617) | 평행사변형 (윗면 571→552 · 가까운 모서리 603) |
| 1위 | **540 평평** | **577** | 43px (539..582) | 38px (539..577) |
| 3위 | 560→553 | 639 (좌측은 경계 자체가 없음) | 밝은 슬래브 `#8A6A44` 49px | **슬래브 폐기** (ref 는 윗면·앞면 동색) |

**7회차의 «균일 45» 는 두께만 틀린 게 아니었다** — 가까운 모서리를 ref 603 → 617 로 **14px 아래**에 그렸다.
전이 구간 x163..265 가 순위 방패에 가려 «왼쪽 573 이 끝까지 간다» 고 읽힌 것이 원인이다(LESSONS 22-① 재발).

### 고친 것

| # | 고침 | 방법 |
|---|---|---|
| 1 | 2위 윗면 = 원근 평행사변형 | `.rk-pod.p2` 상자를 위로 19px 확장(482→463)하고 **검정 실루엣 자체에 clip-path** 를 걸어 윗변을 기울였다. 윗면은 `<b>` 로 떼어 별도 clip-path. 윗면 위 검정 6px 띠는 `.rk-pod` 자기 배경이 드러나 자동으로 생긴다(요소 추가 없음) |
| 2 | 1위 금색 슬래브 43 → 38px | 가까운 모서리를 ref 577 에 맞춤 |
| 3 | 앞면 가짜 가로선 3개 제거 | ref 열스캔에서 2위 앞면은 604..774 가 `#4A5978` **한 덩어리**(171px)다. «상단테 10px / 굽 76%» 하드 스톱은 ref 에 없는 선이라 ③④ 의 격자 읽기를 깨뜨린다 → 단일 톤·부드러운 그라디언트로 교체(음영은 남기고 경계만 지움) |
| 4 | 3위 밝은 슬래브 폐기 | ref 3위는 윗면도 앞면도 `#432D15`/`#43311B` 짙은 갈색이라 밝은 띠가 아예 없다 |

### 게이트를 «실효 렌더값» 으로 바꿨다

`clip-path` 는 `getBoundingClientRect` 에 안 잡힌다 — 상자 기하만 재는 게이트로는 평행사변형을 **전혀 검증할 수 없다**
(60번이 겪은 «선언값 순환 논증» 과 같은 함정). `verify54.js` 에 **실제 캡처를 찍어 열마다 윗면 색 구간을 세는**
픽셀 게이트 16항목을 추가했다. PNG 디코드는 크로미움 자신에게 시킨다(`data:` URL → canvas `getImageData`) —
`pngjs` 가 이 환경에 없어서 npm 의존성 0으로 짰다.

`VERIFY54 65/65 → **81/81**` · `SMOKE PASS`.

## 8회차 채점 (비평가 K · L, 독립 — sess-1149-14964 워커 D가 이어받아 진행)

앞 세션(sess-1028-25517)이 수정까지 커밋하고 채점 전에 죽었다(lock 63분 경과 회수). 현재 main 재캡처(r8)로 채점.

| 항목 | K | L |
|---|---|---|
| ① 배치 | 9 | 8 |
| ② 상대 크기 | 8 | **7** |
| ③ 여백·간격 | 8 | 9 |
| ④ 정렬·그리드 | 9 | 9 |
| ①~④ 최저 | **8** | **7** |

신뢰도 체크 둘 다 통과(행 pitch 165 · 패널 998×1082~1084 · 알약 179×50 · 내 순위 바 171~172 · **2위 슬래브 원근 좌 32~35/우 49~52 재현 확인** — 8회차의 원근 평행사변형이 잡혔다).

감점은 두 사람 모두 **시상대 씬의 캐릭터·전면 폭**에 몰렸다:

| 공통 지적 | ref | r8 | 원인 |
|---|---|---|---|
| 3위 캐릭터가 부유 장식 구역까지 한 덩어리 (L①) | 캐릭터 y262..466 h205, 부유물 y147..260 별도 | y148..468 h321 (+58%) | 3회차 bbox 스캔이 부유 장식을 캐릭터로 합산 |
| 1위 캐릭터 폭 (K −21% / L −31%) | w≈318~325 | 잉크 252 | 이모지 잉크비 미보정 |
| 2위 캐릭터 부양 +폭 (K) / top −36 (L) | 접지·w314·c181 | 잉크 바닥 436 (슬래브 470 위 34px 부양), w354 c201 | 상자 하단이 잉크 바닥과 불일치 |
| 전면 폭 과대 (K 갭 소멸 / L 전면 +15%) | **전면은 수직 변 직사각형** 2위 80..350 · 1위 398..684 · 3위 716..1009, 슬래브 립(2위 66..363, ~y542)과 바닥 받침(밑변 폭, y651~)이 별도 | 슬래브→밑변 연속 사다리꼴 (중간 높이 편측 +11~20px) | 7회차까지의 «사다리꼴» 해석이 틀림 — 자체 모서리 프로파일로 확정(ref 좌변 x80 이 raw627..735 수직, raw736 부터 59 로 플레어) |

### 반려한 지적 (자체 검산)

| 지적 | 검산 | 판정 |
|---|---|---|
| K: 타이틀 폭 ref ~450 vs 471 | ref 안쪽 립(near:6C584D) x306..773=468 → 외곽 ≈482 ≈ 우리 486 | 기각 |
| L: 타이틀 높이 ref 96 vs 114 | 2회차 판정 재인용 — ref **외곽** 104..217=114. L 은 fill 을 쟀다 | 기각 |
| L: 뒤로가기 80×63 vs 93×81 | 비평가 C(94×82)·G(93 폭)·4회차 실측(93×80)과 3:1 — 52-④ | 기각 |
| L: 1위 배지 139×144 vs ref 113×109 | 우리 선언 102×88, G(6회차) 잉크 101×81 로 정합. L 측정치가 우리 어떤 상자와도 불일치(외곽/글로우 합산 추정) | 보류(단독) |
| K: 7행 노출 +13px | ref 1821..1875=54 vs 우리 1817..1876=59 — Δ5, K 의 67 은 과대 | 기각 |
| H(7회차) 1위 리본 cx +4 / 알약 텍스트 cx −4.5 | 알약 잉크 ref 167×21 cx539 vs 우리 166×21 cx538.5 · 리본 cx ref 538 vs 539.5 | 기각 |

## 9회차 — 시상대 씬 재작도 (캐릭터 3 + 전면 3단 + 부유물 자리)

1. **캐릭터 상자**: c1 (345,186) 320×269 scaleX1.31 · c2 (24,196) 314×274(잉크 바닥 470 접지) · c3 (700,262) 342×209. fs 유지 — 잉크가 상자를 넘치게 두고 overflow:hidden 클립으로 bbox 보장.
2. **부유 장식 자리 신설** `.rk-fl` (728,147) 286×114 — 이모지 2개, 아트 대기 표에 추가.
3. **전면 3단 재작도**: 실루엣·채움 clip-path 를 «립(2위만)→수직 직사각형 전면→받침(y651..692 밑변 폭)» 스텝 폴리곤으로. 밑변·슬래브·원근 파라렐로그램은 8회차 값 그대로(게이트 top2/rim2/front2/gold1 무변).
4. verify54 상자 표 갱신 + `.rk-fl` 추가 → `VERIFY54 82/82`.

캡처 r9 자체 스캔: 전면 80..349 / 398.. / 밑변 59..369·387..691 ✓ · c1 잉크 345..665(중심 505) · c2 바닥 468~470 접지 · c3 y262 시작 · 부유물 y150..261 ✓.

---

## 10회차 채점 (비평가 M · N, 독립 — sess-0025-5382 워커 B)

9회차(sess-1149-14964)가 **수정까지 커밋하고 채점 없이 죽었다.** lock 12:34Z 로 12시간 경과·`wip(54)` 커밋도
없어 90분 규칙으로 회수했다. 그 사이 **작업 101**(주인 지시)이 `.rk-fl` 3군집과 `.rk-ch.c3a`(⛵)를 폐기하고
**작업 80**이 단상 위 이모지를 스프라이트 캔버스 3장으로 바꿨으므로, r9 가 아니라 **현재 main 을 r10 으로 재캡처**해 채점했다.

| 항목 | M | N |
|---|---|---|
| ① 배치 | 8 | 8 |
| ② 상대 크기 | **5** | **6** |
| ③ 여백·간격 | 7 | 7 |
| ④ 정렬·그리드 | 7 | 8 |
| ⑤ 색·명암 | 8 | 7 |
| ⑥ 폰트 | 5 | 4 |
| **①~④ 최저** | **5** | **6** |

두 사람의 신뢰도 체크는 둘 다 통과했다(검정 구분선 Δ0 · 행 pitch 165 Δ0 · 패널/카드/셀 경계 0~1px ·
단상 상단 모서리 448/482/473 vs 449/482/471).

### 채택 — 두 비평가가 **독립적으로 같이** 짚은 것 2건

| # | 지적 | 실측 | 처방 |
|---|---|---|---|
| 1 | **단상 캐릭터가 절반 크기** (M ② −49~51% · N ② −33%) | `tools/probe54.js`(신설, 캔버스 안에서 alpha bbox 직독) — c1 잉크 **215×225** / 상자 395×315(**폭 54%**) · c2·c3 **172×180** / 316×252. M 이 캡처에서 잰 «215» 와 **정확히 일치**해 지적이 확증됐다 | 아래 §처방1 |
| 2 | 한글 텍스트 −15~20% (M·N 공통 ⑥) | 같은 문자열 잉크 폭 — 안내문 ref 897×28 vs 748×25(**−16.6%**) · 타이틀 ref 222×37 vs 178×32(−19.8%) · 탭 라벨 −15~17%. 폭 −149px 는 외곽선 두께로 설명이 안 되므로 **01·16 의 «stroke 오진» 사례가 아니라 실제 글자 크기 부족**이다 | **보류 — 작업 126 소관.** 126(«UI 스타일·폰트 통일 — 게임용 한글 웹폰트 1종») 이 `docs/claims/126.lock` 으로 선점 중이라 손대지 않았다. ⑤⑥ 은 통과를 막지 않는다(2026-08-25 규칙) |

#### 처방1 — 배율은 «상자» 가 아니라 «스프라이트 프레임» 기준이었다

`drawHeroTo(cv,{scale})` 의 `scale` 은 아틀라스 프레임 배율이라 **상자를 채우는 값이 아니다.**
sc5/sc4 는 잉크를 상자의 절반(폭 54%)까지밖에 못 키웠다. 72 «아트 자리 규칙»은 대체물이
**레퍼런스 bbox 를 정확히 차지**할 것을 요구하고, 이 상자들이 곧 ref 캐릭터 bbox 다.

잉크는 sc 에 선형이고 **발밑이 상자 바닥에 앵커**되므로 «높이가 상자를 꽉 채우는 최대 정수 배율»이 답이다:

| 캔버스 | 상자 | 잉크(전) | 배율 | 잉크(후) | 비고 |
|---|---|---|---|---|---|
| c1 (1위) | 395×315 | 215×225 | 5 → **7** | **301×315** | 315/45 = 7.0 — 높이 100% 충족 |
| c2 (2위) | 316×252 | 172×180 | 4 → **5** | **215×225** | 252/45 = 5.6 → sc6(270)은 머리가 잘린다 |
| c3b (3위) | 316×252 | 172×180 | 4 → **5** | **215×225** | 〃 |

정수 배율을 지킨 것은 `image-rendering:pixelated` 의 전제다(비정수면 픽셀 크기가 들쭉날쭉해진다).
발밑 앵커라 `verify80` B2 발 위치 **448/482/492 는 불변**이다.

#### 처방2 — 3위 캐릭터 편심 (M ① 단독이지만 CSS 기하로 자명)

`c3b{left:785}` 는 **⛵(c3a)가 왼쪽에 있던 시절의 «ref 캐릭터 자리»** 값이다. 101 이 탈것을 지우자
그 값은 캐릭터를 자기 단상 중심에서 **+76.5px 우측**으로 밀어 놓은 꼴이 됐다(상자 중심 943 vs 단상 p3 중심 866.5)
— M 이 «단상 좌측 163px 이 통째로 빈다»고 읽은 그대로다. **1위(540.5 / 540)·2위(215 / 214)는 이미 단상 중심**이라
3위만 어긋난 것이므로, 주인 지시 «담백하게 플레이어 캐릭터만» 의 뜻대로 **단상 중심에 맞췄다**: `left 785 → 708`.
기대값 소유는 `tools/verify80.js` A4 이므로 그 `spec` 도 같은 커밋에서 고쳤다.

### 반증해 미채택 (전부 자체 열스캔으로 검산)

| 지적 | 검산 | 판정 |
|---|---|---|
| **N ②: «ref 1위 윗면 슬래브 75px vs 우리 44px = −41%»** | `scan54.py <ref> col 420 500 640` · `col 620 500 640` **둘 다 `#FFDC62` y540..576 = 37px**. 측정표 §3-1a(8회차 다열 스캔)와 일치. 우리 38px | **기각** — N 은 슬래브 위 검정 외곽·그림자까지 합산했다 |
| N ②: «앞면 높이 +15~16%» | 위 슬래브 오측에서 파생된 값 | 기각 |
| **M ②: «내 순위 바 ref 190px, 패널과 간격 ref 0px vs 우리 20px»** | `col 20`: ref 바 **1990..2160 = 프레임 171px** vs 우리 **1905..2076 = 172px (Δ1)**. 패널↔바 브라운 간극도 ref **1961..1987 = 27px** vs 우리 **1876..1904 = 29px (Δ2)** | **기각** — M 은 브라운 간극 안(1970)에서 바를 시작해 쟀다. N 은 같은 요소를 «완전 일치» 로 읽어 **두 비평가가 정반대**였다 |
| **M ②: «바 아바타 프레임 높이 −12%»** | `col 266`: ref **2010..2131 = 122px** vs 우리 **1926..2043 = 118px = −3.3%** | **대부분 기각**(실차 4px). M 은 ref 는 아래 테두리를, 우리는 위 테두리를 빼고 셌다 |
| **N ①: «2·3위 단상이 12~17px 낮다»** | M 의 신뢰도 체크가 **독립적으로** 448/482/473(ref) vs 449/482/471(캡처)로 반박. CSS 선언값과도 일치 | **기각** (LESSONS 52-④ 다수결) |
| N ③④: 단상 간격 −31~36%(ref 13~14 vs 9) | 두 비평가가 갈렸고(M 은 단상 폭 310/310/311 vs 305/311/311 로 «일치» 분류) 절대 오차 4~5px | 보류 — 다음 회차 재확인 |
| M ⑤: «탭바 최하단 5px 검정 라인 누락» | 탭바는 **A1 구간**이라 54 가 손대지 않는다 | 이관 — PROGRESS 비고 |

### 교훈 (다른 화면에서도 재발한다)

1. **«scale» 이라는 이름의 인자가 «상자를 채우는 배율» 이라는 보장은 없다.** `drawHeroTo` 의 `scale` 은
   스프라이트 프레임 배율이라, 상자만 ref bbox 로 맞춰 놓고 배율을 그대로 두면 **아트 자리 규칙이 조용히 깨진다**
   (상자는 정확한데 잉크가 54%). 아트 자리를 만든 뒤에는 **잉크 bbox 를 따로 실측**하라 — `tools/probe54.js` 처럼
   캔버스 안에서 alpha bbox 를 직독하면 PIL·캡처 없이 즉시 나온다(단, `--allow-file-access-from-files` 없이는
   `getImageData` 가 tainted canvas 로 죽는다).
2. **요소를 폐기하면 «그 옆에 있던 것» 의 좌표도 같이 재라.** 101 이 ⛵ 를 지웠을 때 c3b 의 `left:785` 는
   «탈것과 무관한 값» 으로 보였지만, 탈것이 사라진 순간 그 값은 **단상 중심에서 76px 벗어난 값**이 됐다.
   좌표의 «근거» 는 이웃 요소와의 관계인 경우가 많다.
3. **비평가 둘이 같은 요소를 «−9.5%» 와 «완전 일치» 로 정반대로 읽으면 경계 정의가 다른 것이다.**
   이번엔 «패널↔바 사이 브라운 간극» 을 바에 포함시켰느냐가 갈랐다. 임계값 문제(LESSONS 16-④)의 자매편으로,
   **판정은 열스캔의 색 경계로** 한다.

## 11회차 — 채점 대기

`54-r11.png` 재캡처(위 처방 1·2 반영). 게이트 **VERIFY54 82/82 · VERIFY80 26/26 · VERIFY87 45/45 · SMOKE PASS** ·
콘솔 에러 0. 비평가 **O·P** 채점 대기.

## 11회차 채점 (비평가 O · P, 독립)

| 항목 | O | P |
|---|---|---|
| ① 배치 | 8 | 9 |
| ② 상대 크기 | 8 | 8 |
| ③ 여백·간격 | **8** | **7** |
| ④ 정렬·그리드 | 8 | 9 |
| ⑤ 색·명암 | 7 | 7 |
| ⑥ 폰트 | 6 | 5 |
| **①~④ 최저** | **8 ✅** | **7** |

10회차 처방 2건은 **둘 다 회수 확인**됐다 — ② 5·6 → **8·8**(캐릭터 잉크), 3위 캐릭터 중심 정렬은
O 가 «무게중심 x 855.2 / 기둥 864, 전부 ±9px 이내», P 가 «bbox 중심 870.5 / 기둥 864 — 정답 처리» 로 각각 승인했다.
**O 는 통과선(8)을 넘었고 P 만 ③ 에서 7** 이라, 12회차는 **③ 에만** 손댔다.

## 12회차 — O·P 가 «독립적으로 같이» 짚은 것만 반영

| # | 지적 | 자체 검산 | 처방 |
|---|---|---|---|
| 1 | 패널 하단 여백 부족 (**O ④ −29% · P ② «9px 링»**) | `col 500` — ref 는 위 `…894 / 895 안쪽`, 아래 `안쪽 …1959 / 1960 전이 1px / 1961 페이지배경 #603F36`. **테두리 띠가 없다.** 우리는 `802..810`·`1876..1884` 가 `#785341` **9px** | 테두리 **9px → 1px**, 상자를 되짚어 **안쪽(50..1029 / 811..1875)은 불변** |
| 2 | 1위 칭호 리본이 기둥 중심에서 +4.5px (**O ⑥ · P ⑧**) | ref 리본판 `bbox … near:AF451B:35` → x435..644 **cx539.5** = 기둥 면 중심과 일치. 우리 cx543.5 | `left 439 → 435` → 실측 x435..644 cx539.5 **Δ0** |
| 3 | 내 순위 바 칭호판 −8% (**M(10회차) · P ⑨**) | `col 480` ref 검정 테두리 **2028..2075 = 48px** vs 우리 44 | `height 44 → 48` → 실측 1943..1990 = **48** ✓ |

### 측정표 §11 정오표 — «패널 테두리 9px» 은 오기다

§11 은 패널을 «외곽 41..1038 / 886..1968 (998×1083) · 테두리 9px `#785341`» 로 적었지만,
그 **9 는 잰 값이 아니라 «안쪽 50..1029 / 895..1959» 에서 역산한 값**이다. 실제 ref 에는 테두리 띠가 없다(위 검산).
**왜 8회차까지 아무도 못 잡았나** — 좌우에서는 ref 페이지 배경(`#755548`)이 우리 테두리색(`#785341`)과
**사실상 같은 색**(채널차 3/2/7)이라 경계가 보이지 않는다. 갈리는 곳은 **위·아래뿐**이고, 거기서 우리 패널이
상하 각 8px 씩 컸다. 이번 회차의 새 값: **외곽 49..1030 / 810..1876 (982×1067) · 테두리 1px**.

### 반증해 미채택

| 지적 | 검산 | 판정 |
|---|---|---|
| **P ①: 리스트 칭호 pill +6px(47 vs 41), 이름과의 여백 −60~71%** | P 는 `col 395` = **모따기 구간**에서 쟀다. 가운데(`col 510`)로 다시 재면 ref **946..990 = 45px** vs 우리 **861..907 = 47px** → **Δ2 = JPEG AA 한계**. 리본 하단도 ref 906 vs 우리 907 로 Δ1 이라 «이름과의 여백» 파생 지적도 성립하지 않는다. 실제 차이는 두께가 아니라 **모따기 폭**이다 | **기각** |
| P ③: «최고 스테이지» 라벨이 알약에서 +11px | 라벨 **상자** 는 ref 856 vs 우리 854(Δ2). 벌어진 것은 **한글 잉크 높이**(ref 36 vs 28 = −22%) 탓이다 — 상자를 내려 보정하면 126 이 서체를 넣는 순간 도로 깨진다(01 «stroke 오진» 의 자매 사례) | 이관 — 126 |
| P ④: 타이틀 배너 +9.7% | 8회차가 이미 같은 지적(L)을 기각했다 — ref **외곽** 104..217 = 114 로 우리와 일치. P 는 fill 을 쟀다. 독립 3인차 확인 | 기각 |
| P ⑦ / O ⑧: 탭바 아래 여백 +23~26px | 지시서가 **감점 금지**한 «24px 캔버스 흡수» 항목이고, 탭바는 **A1 구간** | 대상 외 |
| O ③ / O ⑪: 3위 점수 알약 대비, 내 순위 바 그라데이션 소실, 씬·기둥 밝기 ±13~15% | 전부 ⑤ 색 — 통과 기준 아님(2026-08-25) | 기록만 |

### 넘긴 것 — **다음 회차 1순위: 3위 시상대 윗면(top slab) 재작도** (O ① · P ⑤)

**지적이 맞다.** 자체 검산: `scan54.py <ref> col 995 540 660 10` → `#725334`(휘도 82) **553..603 = 51px** 이
`#453118`(휘도 52) 위에 얹혀 있다. 우리는 `col 995` 가 **477..691 단색 `#452F17` 215px** 로 윗면이 아예 없다.
→ **측정표 §3-1a 의 «3위는 윗면·앞면이 동색이라 밝은 슬래브가 없다» 가 틀렸고, 8회차가 그 근거로 슬래브를 지웠다.**
되살릴 때 필요한 값(이번에 실측):

| 항목 | ref y | 프레임 y |
|---|---|---|
| 윗면 먼 모서리 | x760 **560** → x995 **553** (−7px / 235px) | 476 → 469 |
| 윗면 가까운 모서리 | **603 평평**(x760·x995 공통) | **519** |
| 실루엣(검정) 상단 | x995 **542..550** | 458..466 |

즉 **평행사변형이고 실루엣 상단도 우측이 높다**(x995 에서 458 — 우리 평평한 471보다 13px 위).
단순히 `>b` 를 얹는 것으로 끝나지 않고 p3 실루엣·채움 clip-path 까지 다시 그려야 해서 이번 회차 범위를 넘겼다.
**② 는 O·P 둘 다 8** 이라 통과를 막지는 않는다.

### 교훈 (10회차 3개에 이어)

4. **«역산해서 적은 값» 은 측정값이 아니다.** §11 의 «테두리 9px» 은 안쪽·바깥 좌표의 차이로 만든 값이었고,
   바깥 좌표 자체가 «테두리가 9px 이라면» 이라는 가정에서 나왔다(순환). 측정표에는 **잰 것과 계산한 것을 구분해 적어라.**
5. **두 색이 «거의 같은 색» 이면 그 경계는 재지 못한다.** 좌우에서 `#755548` vs `#785341`(채널차 3/2/7)은
   구분이 안 돼 9px 이 12회차까지 살아남았다. **경계는 색이 확실히 갈리는 변에서 재라**(여기서는 위·아래).
6. **모따기·라운드가 있는 도형의 «두께» 는 반드시 가운데에서 재라.** P ① 은 끝단에서 재 ref 만 테이퍼가 걸려
   +6px 의 없는 차이를 만들었다(8회차 «2위 윗면» 이 가림 때문에 균일로 읽힌 것의 자매편).

## 13회차 — 채점 대기

`54-r12.png` 재캡처. 게이트 **VERIFY54 82/82 · VERIFY80 26/26 · SMOKE PASS** · 콘솔 에러 0.
비평가 **Q·R** 채점 대기.

## 13회차 채점 (비평가 Q · R, 독립)

| 항목 | Q | R |
|---|---|---|
| ① 배치 | 8 | 9 |
| ② 상대 크기 | **7** | 8 |
| ③ 여백·간격 | 8 | 9 |
| ④ 정렬·그리드 | 8 | 9 |
| ⑤ 색·명암 | 6 | 7 |
| ⑥ 폰트 | 7 | 6 |
| **①~④ 최저** | **7** | **8 ✅** |

12회차 처방 3건은 **R 이 전부 «완전 일치» 로 승인**했다 — 내 순위 칭호판 x343..619(277) vs x343..620(278) Δ1 ·
패널 좌우 50..1029 동일 · 1위 칭호 리본 548..584 완전 일치. **R 8 통과 · Q 만 7.**

## 14회차 — Q·R 이 «독립적으로 같이» 짚은 것 + 4인 누적 지적

| # | 지적 | 자체 검산 | 처방 |
|---|---|---|---|
| 1 | **패널 바닥 림 6px 누락** (**R ① · Q ②**) | `col 690` — ref 는 카드가 **1952** 에서 잘리고 **1954..1959 = 6px `#3D2A23`** 림이 7위 행을 닫은 뒤 1960 에서 바깥으로. 우리는 카드가 안쪽 바닥 **1875** 까지 내려가 림 **0px** | `.rk-list{bottom:0 → 6px}` → 실측 **카드 …1869 / 림 1870..1875 / 테두리 1876 = ref 와 완전 일치**. 7위 노출량 59 → **53px**(ref 52~53) |
| 2 | **3위 시상대 윗면 부재** (**O ① · P ⑤ · R ② · Q ③ — 두 회차에 걸쳐 4명**) | ref `col 950` 윗면 **555..603(49px) `#735435`**(휘도 82) · `col 995` **553..603(51px) `#725334`**, 그 아래 `#453118`(52). 우리는 `col 995` 가 **477..691 단색 `#452F17` 215px** | `.rk-pod.p3>b` 신설(파라렐로그램) → 실측 `col 950`·`col 995` 모두 **475..518 `#6D5033`/`#715335`** — **가까운 모서리 Δ0~1 · 색 Δ1** |

### 측정표 §3-1a 정오표 — «3위는 밝은 슬래브가 없다» 는 오기다

§3-1a 는 3위를 «윗면 `#432D15` — 앞면과 같은 짙은 갈색이라 밝은 슬래브가 아예 없다» 로 적었고,
**8회차는 그 근거로 3위 슬래브를 삭제**했다. 그런데 그 절이 본 열은 `col 990` 과 `col 735` 둘뿐이다 —
`x735` 는 **캐릭터에 가려 검정**이고, `x990` 은 밴드 경계를 앞면 쪽(639)에서 끊었다.
**가림이 없는 `x860` 오른쪽을 훑으면 윗면이 또렷하다**(위 검산). 새 값:

| 항목 | ref y | 프레임 y |
|---|---|---|
| 윗면 먼(위) 모서리 | x760 **560** → x995 **553** (−7px / 235px) | 476 → 469 |
| 윗면 가까운(아래) 모서리 | **603 평평** | **519** |
| 실루엣(검정) 상단 | x880 545 · x950 546 · x995 542 | 461 · 462 · 458 |

### 반증해 미채택

| 지적 | 검산 | 판정 |
|---|---|---|
| **Q ①: «아바타 프레임 세로 −7.6%»**(Q 의 ② 감점 주원인) | **N**(ref 113×114 vs 우리 111×113) · **O**(ref 112×114 vs 111×113) · **R**(«x257..367, 높이 136 동일») 세 명이 독립적으로 **Δ1~2**. Q 만 `th 20` 으로 **안쪽 판**을 재 −7.6% 가 나왔다 — 프레임 상단은 ref·캡처 둘 다 **844**(`col 300`)로 Δ0 | **기각** (LESSONS 16-④ 임계값 · 52-④ 다수결) |
| Q ⑤: 안내문 띠에 ref 에 없는 어두운 배경 | ⑤ 색 — 통과 기준 아님 | 기록만 |
| Q ④ · R ⑥: 시상대 명암 서열·전체 색온도 | 〃 | 기록만 |
| R ③: 시상대 상단 외곽선 13~14px vs 5~6px | 단독 지적이고, ref 의 «두꺼운 상단 테» 는 **윗면 위 검정**이라 위 §정오표의 실루엣 상단(458~462)과 같은 항목이다 — 아래 «남은 것» 으로 합친다 | 이관 |

### 남은 것 — 다음 회차 1순위: **3위 실루엣 상단 7~10px 상향**

ref 3위 실루엣(검정) 상단은 x880~1010 에서 **458~464** 인데 우리는 **471** 이라 7~10px 낮다
(**R ② «외곽 높이 −9%» · Q ③ «3위만 −9px, 2위보다 6px 낮은 계단이 새로 생겼다»** — 2인 공통).
윗면을 얹어 «면» 은 생겼지만 **기둥 자체의 키**는 아직 낮다. 맞추려면 `.rk-pod.p3` 상자(top 471 · height 221)와
실루엣·채움 clip-path 의 **백분율을 전부 다시 풀어야** 한다(77.83%·80.93% 가 현재 높이에 묶여 있다) — 별도 회차 몫이다.

### 교훈 7

7. **«두 열만 보고 내린 부재 판정» 이 한 요소를 6회차 동안 지웠다.** §3-1a 는 `x990`·`x735` 두 열로
   «3위는 윗면이 없다» 고 결론냈고 8회차가 그 근거로 슬래브를 삭제했는데, `x735` 는 캐릭터에 가려 검정이었다.
   **«없다» 를 증명하려면 가림이 없는 열을 여러 개 훑어야 한다** — 있다는 증거는 한 열로 충분하지만
   없다는 증거는 그렇지 않다(LESSONS 22-① «가림 없는 열» 의 역방향 사례).

## 15회차 — 채점 대기

`54-r13.png` 재캡처. 게이트 **VERIFY54 82/82 · VERIFY80 26/26 · SMOKE PASS** · 콘솔 에러 0.
비평가 **S·T** 채점 대기.

## 15회차 채점 (비평가 S · T, 독립)

| 항목 | S | T |
|---|---|---|
| ① 배치 | 8 | 9 |
| ② 상대 크기 | **7** | **7** |
| ③ 여백·간격 | 7 | 8 |
| ④ 정렬·그리드 | 8 | 8 |
| ⑤ 색·명암 | 6 | 8 |
| ⑥ 폰트 | 5 | 5 |
| **①~④ 최저** | **7** | **7** |

14회차 처방 2건은 **T 가 실측으로 승인**했다 — «패널 하단→내 순위 바 간격 **29px = 29px (Δ0)**,
7위 잘린 노출 높이 **53px = 53px**». 다만 **3위 윗면을 절반만 그린 것**이 새로 드러났다.

## 16회차 — S·T 공통 지적 반영 (+ 내가 10회차에 넣은 과잉 되돌림)

| # | 지적 | 자체 검산 | 처방 |
|---|---|---|---|
| 1 | **3위 윗면이 «밝은 띠» 까지만 있고 «슬래브 립» 이 없다** (S ①② «앞면 시작 23px 높다» · T ①② ) | x 를 훑으니 3위에도 2위와 같은 립이 있다. ref y, `col 775·950·995` **셋 다 동일** — 밝은 윗면 …**602** → **604..625 `#60462D`(립)** → **627..639 `#453118`** → 641.. 앞면. 경계 603·626 은 **x 무관 평평** | `>b` 48px → **71px**, «밝은 띠 48 + 립 23» 2톤 |
| 2 | **3위 상판이 몸통에 붙어 인셋이 0** (T ③, ②의 «3위만 폭 +11px» 과 같은 원인) | ref 상판 인셋 1위 6/6 · 2위 8/7 · **3위 8/7**. 14회차는 채움 폭(709..1020)에 그대로 맞췄다 | `left 7→15 · width 311→297` (= **717..1013**) |
| 3 | **1위 캐릭터가 타이틀 명판을 6px 까지 밀고 올라온다** (**Q ⑧ · S ③** — 10회차에 내가 넣은 과잉) | 명판 하단 133 → 검 끝 **140**(ref 같은 열 여백 **145px**). 세로도 ref ≈275 vs 우리 **315(+15%)** | c1 **sc7 → sc6**(잉크 301×315 → **258×270**, 머리 상단 133 → **178**) |

**구현 실측(r14)**: `col 980` → 밝은 **472..518** · 립 **519..541** · 앞면 머리 **542..**
(ref 프레임 469..518 / 520..541 / 543..555 — **Δ0~3**) · 상판 좌단 **717 = ref 717 (Δ0)**.

### 게이트가 낡아 있었다 — `verify54` 의 «3위엔 밝은 슬래브가 없어야 한다» 를 뒤집었다

8회차가 넣은 `ck(probe.bright3 === 0, '3위에 ref 에 없는 밝은 슬래브가 …')` 는 **틀린 전제를 고정한 검사**였다
(LESSONS 52-⑤ «구현이 아니라 검사가 낡은 것»). 근거인 측정표 §3-1a 가 `x735`(캐릭터에 가림)를 «윗면 없음» 의
증거로 썼기 때문이다. 이 검사가 있는 한 **올바른 구현이 영구 FAIL** 이 된다 — 실제로 이번에 47px 로 걸렸다.
→ «없어야 한다» 를 **«있어야 한다 + 경계가 어디여야 한다»** 로 바꿨다(2위 `top2` 와 같은 형식):
먼 모서리 471 · 가까운 모서리 518 · 립 520..541, 각 ±3. **VERIFY54 82/82 → 85/85.**

### 반증해 미채택

| 지적 | 검산 | 판정 |
|---|---|---|
| T ④: 1위 상판 두께 ref 42 vs 우리 38 | `col 420`·`col 620` 둘 다 ref 금색 `#FFDC62` **540..576 = 37px**(533..538 은 검정·전이). T 는 `x650` 에서 검정 전이(535~539)를 포함해 쟀다 | 기각 |
| S ⑤: 칩 높이 +1~2px · T ⑦ 알약→이름 갭 −5px | 알약 자체는 S 도 «Δ0~2 · 문제없다» 로 적었고, 갭 차이는 한글 잉크 높이(−12~20%) 탓 | 이관 — 126 |
| Q ①(−7.6%) · T ⑤(−5%) · S ⑥(−2.6%) 아바타 프레임 세로 | 같은 요소인데 **임계값에 따라 −2.6 ~ −7.6% 로 갈린다**. N·O·R 은 Δ1~2 로 «일치» 로 읽었다. S 가 원인을 밝혔다 — «ref bbox 의 h126 은 왕관·보석 돌출분» | 보류 — 임계값 합의 전에는 손대지 않는다(LESSONS 16-④) |
| T ⑧: 헤더 명판 +6px | 8회차(L)·13회차(P)에 이어 **세 번째 재발**이고 그때마다 ref **외곽** 104..217 = 114 로 우리와 일치함이 확인됐다 | 기각(3회 확정) |

### 교훈 8·9

8. **«없어야 한다» 를 거는 게이트는 그 «없음» 이 가림 때문일 수 있는지 먼저 따져라.** `bright3 === 0` 은
   두 열짜리 오측을 3회차 동안 코드로 못박아, 올바른 구현이 들어오는 순간 FAIL 을 냈다.
   부재 단언은 **가림 없는 열 여러 개**로만 세우고, 가능하면 «있어야 한다 + 경계» 의 양성 형식으로 적어라.
9. **아트 자리를 키울 때는 «상자» 뿐 아니라 «이웃 요소와의 여백» 도 같이 봐야 한다.** 10회차에 c1 잉크를
   상자에 꽉 채웠더니(sc7) 상자 자체가 이모지 시절 값이라 캐릭터가 타이틀 명판을 6px 까지 침범했다.
   상자는 «잉크가 상자를 못 채운다» 는 전제로 잡혀 있을 수 있다 — **키운 뒤 위아래 여백을 다시 재라.**

---

# 현재 상태 (2026-08-26, sess-0025-5382 워커 B 종료 시점)

- **누적 16회차** · **①~④ 개인 최고 8**(O 11회차 · R 13회차) — 다만 **두 비평가가 동시에 8 이 된 적은 없다**(통과 조건).
- 회차별 ①~④ 최저: M5/N6 → **O8**/P7 → Q7/**R8** → S7/T7 → (16회차 미채점)
- 게이트 **VERIFY54 85/85 · VERIFY80 26/26 · SMOKE PASS** · 콘솔 에러 0.

## 다음 세션이 이어서 할 일 (수치까지 정리돼 있다)

1. **3위 실루엣 상단 7~10px 상향** — **R ② · Q ③ 2인 공통.** ref 검정 실루엣 상단이 `x880` 461 · `x950` 462 ·
   `x995` 458 인데 우리는 **471** 이다. 윗면·립은 16회차에 맞췄으므로 남은 것은 **기둥의 키**뿐이다.
   `.rk-pod.p3`(top 471 · height 221)와 실루엣·채움 clip-path 백분율(77.83% · 80.93%)을 **함께** 다시 풀어야 한다.
2. **랭크 숫자가 셀 세로 중심에서 4px 위** — **S ⑦ · T ⑥ 2인 공통**(ref cy 오차 0.5 vs 우리 −4).
3. **아바타 프레임 세로** — 먼저 **임계값을 합의**하라(위 표). ref 는 왕관·보석이 bbox 를 부풀린다.
4. **한글 잉크 −12~25%** — 비평가 8명 전원이 지적했으나 **작업 126**(게임용 한글 웹폰트 통일) 소관이다.
   126 이 들어온 뒤 **일괄 재검**하면 ⑥ 과 «라벨↔알약 갭» 계열 지적이 같이 회수된다.

---

## 17회차 (sess-0327-26490 워커 B) — 16회차가 넘긴 1순위 2건 처리

### ① «3위 실루엣 상단 7~10px 상향»(R ② · Q ③) — **방향은 채택, 근거 좌표는 정정**

16회차는 이 항목을 «p3 상자와 실루엣·채움 clip 백분율을 전부 다시 풀어야 한다» 며 넘겼다.
이번에 **가림 없는 열을 다시 골라** 재니 두 비평가의 «올려라» 는 맞았고, 그들이 근거로 든
«ref 458~462» 는 단상이 아니었다.

신설 도구 **`tools/p3prof.py`** — 열마다 «상판 L80 `#694C2E` → 하이라이트 L95 `#7B5A39` →
립 L72 `#5E4429` → 어두운 테 L50» 네 경계를 한 번에 뽑는다(회차마다 손으로 열을 고르다 미끄러졌다).

| 구간(프레임 x) | ref 갈색 시작 | 판정 |
|---|---|---|
| 710~850 | 474~477 | **가림** — 3위 캐릭터 몸통. 캐릭터 외곽선 아래라 실제보다 낮게 읽힌다 |
| **856~877** | **467.8** | 깨끗 |
| 878~950 | 468→482→468(사선) | **가림** — 캐릭터의 청회색 덩어리(RGB≈63,71,84 · x881..1031 · y420..475)가 상판을 **앞에서** 덮는다. 하단이 (920,481.5)→(952,468) 우상향 사선 |
| **952~1000** | **467.8** | 깨끗 |

⇒ **먼 모서리는 완전히 평평(467.8)** 하고 검정 외곽선은 전 구간 **≈3.5px 균일**(바깥 464.5)이다.
14회차의 «x760 476 → x995 469 로 −7px 기운다» 와 16회차의 «x990 471» 은 둘 다 위 표의 «가림» 구간을
표본으로 쓴 것이다. 우리 16회차 구현은 윗변 **477(좌)→471(우)** 기울기 + 검정 두께 **6px(좌)/0px(우)** 라
좌측 끝에서 **Δ9px** 였다.

**처방** — `.rk-pod.p3` top 471→**464** · height 221→**228**(바닥 692 고정, 받침 77.83%→78.51%) ·
`>b` 윗변을 **프레임 468 에서 평평**(clip `0 8.45%,100% 0` → `0 5.13%,100% 5.13%`) · height 71→78 ·
`>u` 는 절대 기하 보존(위 470·아래 692·받침 651), 백분율만 80.93%→81.53%.

**부수 발견 — ref 상판은 «1톤» 이 아니라 2톤이다.** 열스캔에서
`#694C2E`(L80) 468..505 → `#7B5A39`(L95) **506..518** → 립 519..541 로 나뉘고,
하이라이트 506 은 **x720~1005 전부 평평**하다. 우리는 48px 전체를 L96 한 톤으로 칠해
«상판이 통째로 밝은 판» 으로 읽혔다(② 두께 오독의 원인) → 2톤 그라디언트로 교체.

**구현 실측(r15, `tools/p3prof.py`)**: 먼 모서리 x720..775 **468** · x955..1010 **468**(평평) ·
하이라이트 506 · 립 519 · 어두운 테 542 — ref 467.8 / 507 / 520 / 543 대비 **Δ0~1**.

### ② «랭크 숫자가 셀 세로 중심에서 4px 위»(S ⑦ · T ⑥) — 채택

| 요소 | 우리(수정 전) | ref | 처방 | 수정 후 |
|---|---|---|---|---|
| `.rk-bd.f>b` 행 순위 숫자 | 잉크 cy 1391.5 / 셀 cy 1396 = **Δ−4.5** (3행 표본 −4.0~−4.5) | 잉크 cy 1480.0 / 셀 cy 1479 = **Δ+1.0** | `transform` 에 `translateY(5px)` | **Δ−0.5 · +1.0** |
| `.rk-mn` 내 순위 바 숫자 | 잉크 cy 1990 / 플레이트 cy 1992 = **Δ−2.0** | Δ**0.0** | `translateY(2px)` | **Δ0.0** |

원인은 폰트 메트릭이다 — `line-height` 로 세운 줄상자는 «상자» 를 가운데 맞출 뿐이고 숫자 글리프의
잉크는 베이스라인 위로 몰려 앉는다. 여백이 큰 쪽(fs 51 / 줄상자 96)이 더 크게 어긋났다.

### 게이트 (VERIFY54 85 → **88/88**)

«3위가 기울었다/낮다» 는 오독이 14·16회차에 이어 세 번째라 **양성 형식 3항**을 추가했다(LESSONS 54-8):
`top3`(x990) · `top3b`(x760) 둘 다 갈색 시작 **468 ±3**, 그리고 **|x760 − x990| ≤ 2**(평평함).
윗면 «먼 모서리» 는 하이라이트가 아니라 **L80 톤**으로 잰다 — 2톤이 된 뒤로 `#7B5A39` 로 재면
하이라이트 상단(506)이 잡혀 9회차째 같은 오독을 되풀이하게 된다.
`hi3` 로 하이라이트 506..518 을 따로 못박았다.

**VERIFY54 88/88 · VERIFY80 26/26 · VERIFY87 45/45 · SMOKE PASS · 콘솔 에러 0.** 캡처 `54-r15.png`.
비평가 채점 대기.

### 교훈 10·11

10. **«가림 없는 열» 은 회차마다 다시 골라야 한다 — 앞 회차가 고른 열을 물려받지 마라.**
    이 화면에서 x760·x990·x995 는 세 회차 동안 «깨끗한 열» 로 통용됐는데 셋 다 캐릭터 아트
    (몸통 / 청회색 덩어리) 안이었다. 열 하나가 아니라 **열 전체의 프로파일을 찍어 «값이 평평한 구간»**
    을 먼저 찾고, 그 구간에서만 값을 읽어라. 그래서 도구(`p3prof.py`)로 고정했다.
11. **가림물의 «사선 윤곽» 은 원근 기울기로 위장한다.** x878~950 의 (920,481.5)→(952,468) 사선은
    2위 윗면의 진짜 기울기(−20px)와 부호·크기가 그럴듯해서 두 회차가 «3위도 평행사변형» 이라고 믿었다.
    기울기를 주장하려면 **가림 없는 구간이 둘 이상**이고 그 둘이 서로 다른 값을 줘야 한다 —
    여기서는 둘 다 467.8 로 같았고, 그것이 «평평» 의 증거였다.
