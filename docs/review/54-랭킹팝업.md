# 54 랭킹 팝업 — 회차 기록

> 측정표 `docs/measure/54-랭킹팝업.md` · 캡처 `docs/review/54-r*.png`
> 스크립트 `tools/scan54.py`(레퍼런스 스캐너) · `tools/cap54.js`(캡처) · `tools/verify54.js`(기하 + 기능 게이트)
> 담당 세션: **sess-0748-7696 (워커 D, 2026-08-25)** — T3, 이 화면은 이 세션이 처음 구현했다(이전 기록 없음).
> 죽은 lock(05:25 sess-0500-22861, 143분) 회수. 앞 세션의 산출물은 없었다(review·measure 파일 모두 부재).

## 껍데기 판정 — **(다) 딤·테두리 없는 전체화면 페이지**

03 던전 · 14 유물 · 35 패스와 같은 계열이다. 모달 껍데기가 아니다.

- **딤 반증**: `scan54.py bbox 0 0 1079 775 near:FFFFFF:2` → **MATCH 3644**. 상단 씬에 순백 화소가 남아 있으므로
  알파 검정 오버레이가 깔렸을 수 없다.
- **상태바 반증**: `bbox 0 0 1079 84 light>90` → **MATCH 0**. 상단 84px 은 OS 바가 아니라 씬 배경 그대로다.
- 닫기 ✕ 가 없다 — 하단 내비바의 **← 화살표**가 그 역할을 한다.

## 세로 밴드 7장 (프레임 = ref y − 84)

| 밴드 | 프레임 y | h | 구현 |
|---|---|---|---|
| ① 포디움 씬 | 0..691 | 692 | `.rk-scene` (top 앵커) |
| ② 검정선 | 692..701 | 10 | `.rk-div1` |
| ③ 안내문 밴드 | 702..800 | 99 | `.rk-tip` |
| ④ 리스트 패널 | 802..1884 | 1083 | `.rk-panel` — **top + bottom 양쪽 앵커(가변)** |
| ⑤ 내 순위 바 | 1905..2076 | 172 | `.rk-me` (bottom 203) |
| ⑥ 검정선 | 2077..2084 | 8 | `.rk-div2` (bottom 195) |
| ⑦ 내비바 | 2086..2249 | 164 | `.rk-nav` (bottom 30) |

**남는 높이 흡수는 리스트 패널 하나가 맡는다**(LESSONS 22-④). 하단 3장을 전부 `bottom:` 앵커로 두었기 때문에
짧은 프레임(1600)에서도 내 순위 바·내비바가 삼켜지지 않고 패널만 줄어든다 — smoke [3] 6종 화면비 전부 통과.

레퍼런스 캡처는 ref 2340 − 84 = **2256** 까지라 프레임 2280 의 **하단 30px 은 캡처 밖**이다.
그 띠는 `#rkw` 배경을 내비바와 같은 `#3C2D28` 로 칠해 «내비바가 이어진» 것처럼 보이게 했다.

## 1회차 (sess-0748-7696)

측정표를 PIL 스캔으로 새로 만들고(310줄) 화면 전체를 한 번에 올렸다.

**구현한 것**
- 씬: 타이틀 육각 명판(486×114 @ top 20) + 시상대 3개 + 단상 위 텍스트 4종(방패·칭호 리본·닉네임·점수 알약)
- 안내문 밴드: 한글 46자를 ref 잉크폭 894 에 맞추기 위해 `transform:scaleX(.67)` (LESSONS 01 — 음수 letter-spacing 금지)
- 리스트: 패널 998×1083(테두리 9px 1겹, **검정 겹 없음**) + 행 950×149 · pitch 165 · 립 5 `#F7EBD9`
- 행 부품: 좌 탠 칸 148×138 · 배지(1~3위 메달 102×80 / 4위↓ 플랫 96×96) · 아바타 111×113 · **리본 256 고정폭** ·
  닉네임 · «최고 스테이지» 라벨 · 점수 알약 179×50 r25 · 우측 탠 **평행사변형(좌변 Δ29)**
- 내 순위 바: **행과 같은 부품 클래스를 전폭 1080 으로 재사용**(측정표 주의점 2)
- 내비바: ← + 탭 3개(활성 288 / 비활성 188·190, 활성 표시는 밝은 fill + 폭 +100 두 가지뿐)

**기능** — 서버가 없으므로 UI-REFERENCE 54 지시대로 «고정 더미 사다리 + 내 실기록».
`RANK_NPC` 40명은 상수이고 내 행만 `S.best`·`cp()` 로 만들어 끼워 넣는다. 사다리가 고정이라
**내가 스테이지를 올리면 내 순위가 실제로 올라간다**(목업이 아니다).

### 기능 체크 (헤드리스 실측 — `tools/verify54.js`)

| 조작 | 확인된 변화 |
|---|---|
| ▦ 메뉴 → «랭킹» | `#rkw.on` — 기존 «준비 중» 팝업 분기를 `openRank()` 로 교체 |
| 리스트 렌더 | 41행(더미 40 + 나), 1~3위 메달 배지 · 4위 이하 숫자 배지, 리본 폭 6행 전부 256 |
| 내 순위 바 | `S.best 50` → **27위**, 닉네임·최고 스테이지가 내 실제 값 |
| `S.best` 50 → 300 | 순위 27 → **6위로 상승**, 알약 값도 300 으로 갱신 |
| 포디움 | 1·2·3위 닉네임·점수·칭호가 리스트 상위 3행과 같은 데이터로 그려짐(내림차순 검증) |
| 행 클릭 | 그 유저의 칭호·최고 스테이지·전투력 상세 팝업 |
| 탭 «시련의 탑»/«몬스터 농장» | 콘텐츠 미존재 안내 팝업 + **리스트는 바뀌지 않음**(35 패스 잠금 탭과 같은 처리) |
| ← 뒤로가기 | 페이지 닫힘 |

**자체 실측**: `VERIFY54 57/57` — 기하 38항목이 전부 측정표와 **Δ≤1.0px**, 기능 12항목 통과,
NaN/undefined 0건, 콘솔 에러 0건. `node tools/smoke.js` **SMOKE PASS**(69 ✓).

smoke 확장: `#rkw .rk-panel/.rk-me/.rk-nav` 를 화면비 잘림 후보에 추가하고 오프너로 `openRank()` 를 넣었다
(새 오버레이를 만들면 smoke 목록에 넣는 것까지가 작업 범위 — ROUTINE [6]).
