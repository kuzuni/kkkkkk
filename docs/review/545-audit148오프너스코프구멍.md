# 545 — `tools/audit148.js` 오프너 스코프 구멍 (죽은 이름 `openRelicPage()` 를 `catch (_) {}` 가 삼켰다)

**세션** sess-0038-4394 (워커 B) · **1회차** · 2026-08-31
**티어** T4 「게이트 위생」 · 등재 sess-2036-11381 워커 G(534 곁다리 관측)
**제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 파일은 `tools/audit148.js` 하나다.

---

## §1 재현 먼저 (338 규칙) — `tools/probe545.js` 신설, 13/13

등재문은 «안 보이던 자리» 라고만 적었으므로, 이 자가 할 일은 «정말 한 번도 안 열렸나» 와
«고치면 실제로 열리나» 를 **찍힌 상태로** 가르는 것이었다. 결과는 **등재문 그대로 확인**이다
(338 은 가설 기각 · 341 은 유령이었던 자리이므로 매번 물어야 한다).

| 항 | 물은 것 | 실측 |
|---|---|---|
| [A1] | 옛 이름이 제품에 있는가 | `typeof openRelicPage = undefined` |
| [A2] | 현재 이름 | `typeof openRelw = function` |
| [A3] | 열 대상 노드 | `#relw` 문서에 있음 |
| [B1] | **옛 목록**으로 한 바퀴 돌 때 `#relw.on` 인 scan | **0회** |
| [B2] | 던진 오프너 | `openRelicPage()` **하나** (`ReferenceError: openRelicPage is not defined`) |
| [C1] | 이름만 고친 목록으로 같은 바퀴 | `#relw.on` 인 scan **1회** |
| [C2] | 고친 목록에서 던지는 오프너 | **0개** |
| [D] | 침묵으로 삼켜지던 실패 | 옛 **1건** → 새 **0건** |

⚑ **«0건» 이 두 가지 뜻이었다.** 수리 전 `audit148` 의 출력은 내내 `없음 ✅ / 총 0건` 이었고
그것이 «전수 감사 결과 0건» 으로 읽혀 왔다(`docs/UI-REFERENCE.md` 4298행 체크리스트가 그 문장이다).
실제로는 «오프너 8개 중 1개가 조용히 죽은 채로 얻은 0건» 이었다.
397(스캐너 SCREENS 가 출석 패스 탭을 한 번도 안 열었다) · 133(`aspect63` 의 죽은 셀렉터가
`missing:true` 로 조용히 빠져 두 화면이 통째로 누락)과 **같은 계열**이다.

## §2 헛초록이 아님을 먼저 못박았다 — [E0]

유물 페이지를 열고 나서도 위험 자리가 **0건**이었다. 그런데 «볼 것이 아예 없어서 0» 이면
이 작업은 아무것도 안 한 것이 된다(534 가 겪은 «잴 게 0종인 회귀 = 헛초록»). 그래서 먼저 셌다:

> `#relw` 안에 **보이는 `s`/`u`/`strike` 10개 · 그중 글자 든 것 10개 · 그중 `deco≠none` 0개**

⇒ **봤는데 전부 안전**이다. 유물 페이지는 `<u>` 를 아이콘·라벨 슬롯으로 열 개나 쓰면서
기본 밑줄을 전부 꺼 두고 있었다. **등재문 처방 ③(새로 잡히는 자리를 그 화면 담당 구간으로 등재)은
유물 페이지에 관해서는 등재할 것이 없다.**

## §3 고친 것 — `tools/audit148.js` (2자리)

1. **오프너 이름** `openRelicPage()` → `openRelw()` (429·432·534 가 옮긴 현재 이름).
2. **침묵을 끝냈다 — 이게 본체다.** 오프너 호출만 `try` 로 감싸고 실패를 `dead[]` 에 모아
   끝에 한 줄로 찍는다. 실패가 있으면 **종료 코드 1**.

```
열지 못한 오프너 0건 / 8건 중 ✅ (감사 범위 = 오프너 전부)
```

실패가 있으면 이름·이유와 함께 «⚠ 이 오프너가 여는 화면은 위 «총 n건» 에 들어 있지 않다 — 전수가 아니다» 를 같이 찍는다.
⚠ 닫기(`closeModal(); gmCloseAll()`)의 `catch` 는 그대로 뒀다 — 그것은 스코프 구멍이 아니라
«이미 닫혀 있음» 이 정상인 자리다.

## §4 게이트 — `tools/verify545.js` 신설 **10/10**

이름을 문자열로 비교하지 않는다(그러면 **다음 개명 때 똑같이 굳는다**). **목록을 소스에서 파싱해
제품에게 직접 물어본다** — 402 «표 두 벌» 부패 방지와 같은 방향이다.

| 항 | 단언 |
|---|---|
| [A1] | `audit148.js` 에서 오프너 목록을 읽었다(8개) |
| [A2] | **목록에** 죽은 이름 `openRelicPage` 가 없다 (⚠ 소스 전체가 아니라 목록만 본다 — 왜 이 자가 생겼는지 적은 주석에는 옛 이름이 나오고, 그 이력을 지우면 다음 세션이 같은 함정을 다시 판다) |
| [A3] | 실패를 세어 보고하는 코드가 있다(침묵 금지) |
| [B] | 목록의 오프너가 **전부 제품에서 살아 있다** — 개명이 생기면 여기가 빨개진다 |
| [C] | 그 목록이 **89 유물 페이지(`#relw`)를 실제로 연다** = 이름이 아니라 «열리는가» 로 묻는다 |
| [D1][D2] | 실제 실행이 «열지 못한 오프너 0건» 을 찍고 종료 코드 0 |
| [R1][R2][R3] | **되돌림 시험** — 오프너 하나를 죽은 이름으로 바꾼 **사본**은 «1건» 을 찍고 종료 코드 **1** 로 끝나며 «전수가 아니다» 를 같이 찍는다 |

§R 이 있어야 «세는 코드가 장식이 아님» 이 증명된다. **허용 오차는 한 칸도 안 넓혔고, 항을 지워서 닫은 곳도 없다.**

## §5 곁다리 — 손 목록 자체가 좁다(등재 561)

`audit148` 의 오프너는 **손으로 적은 8개**이고, `smoke.js` 는 같은 일을 **DOM 에서 파생**한다
(`.tab[data-t]` · `.side .ibtn[data-pop]` · `[data-mn]` …). 손 목록은 개명에 썩고 신설에 뒤처진다
— 545 가 바로 그 사고다. 그래서 probe545 **[F]** 에 «DOM 파생 넓은 스윕»(읽기 전용)을 넣어
**18개 입구**를 열어 봤더니 손 목록이 못 보던 **위험 2건**이 나왔다:

```
· #svw u  «⏱️»  deco=underline stroke=0   [mn:saver]
· #svw u  «💀»  deco=underline stroke=0   [mn:saver]
```

**찍힌 픽셀로 확인했다** — `#svw .sv-r` 첫 행(500×65)에서 아이콘 구간(x 0..70)만 잘라
`text-decoration:none` 을 건 사본과 대조하니 **y 45·46·47 세 줄에서 57px** 이 달라진다
(같은 상태 두 장의 잡음 **0px**). 즉 `#0E0E0E` 행 위에 **흰 밑줄 3px × 약 20px** 이
이모지 밑에 실제로 그려지고 있다. 148 의 원인 ② 와 **같은 함정**이되 `-webkit-text-stroke` 가 0 이라
검정 막대가 아니라 «선만 보이는» 쪽이다.

⚠ **고치지 않았다 — 56 절전 모드 화면 구간이다**(병렬 세션 규칙 3 · 148 처방 «목록만 남긴다»).
**561** 로 등재했다.

## §6 실행 결과

| 자 | 결과 |
|---|---|
| `node tools/probe545.js` | **13/13 신설** |
| `node tools/verify545.js` | **10/10 신설** |
| `node tools/audit148.js` | 총 0건 · **열지 못한 오프너 0건 / 8건 중** · 종료 코드 0 |
| `node tools/verify148.js` | **16/16 PASS**(회귀 · 무수정) |
| `node tools/smoke.js` | **SMOKE PASS** |

## §7 다음 세션이 볼 것

- **스캐너·감사의 «전수» 는 목록이 정직할 때만 참이다.** 손으로 적은 오프너 목록을 쓰는 자는
  실패를 **세어서 찍고 종료 코드로 내야** 한다 — 안 그러면 «0건» 이 «못 본 화면이 있는 0건» 과
  구별되지 않는다(397·133·545 가 같은 사고다).
- **개명에 굳지 않는 자를 쓰는 법**: 이름을 비교하지 말고 **목록을 파싱해 제품에게 물어라**.
  `verify545` [B]·[C] 가 그 꼴이다 — `openRelw` 가 또 개명돼도 [B] 는 빨개지고 [C] 는 뜻이 안 변한다.
