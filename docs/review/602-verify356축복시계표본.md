# 602 — `tools/verify356.js` [A] 3건 + [R5] 빨강 (34 축복 ⏱ 시계 자리가 감시 밖)

T1 «버그(게이트 부패)» · 착수 2026-08-31 09:00Z · 워커 C(`sess-0843-32631`) · **제품 `index.html` 0줄**

---

## 1회차 — 재현(338 규칙): 등재문 가설을 **기각**했다

등재문은 이렇게 적혀 있었다:

> 요구된 상태가 `s.tm.**alert**` 라 **축복이 실제로 돌고 있어야** 시계가 렌더된다.
> 자가 34 를 열기만 하고 «켜진 축복» 을 안 만들어서 0개가 나오는 것이다.
> **처방**: 자의 34 표본 만드는 자리에서 **축복을 하나 켜고** … 시계가 뜬 프레임에서 재게 한다.

`tools/probe602.js` 를 신설해 제품에게 직접 물었다(현재 트리 · 부팅 세이브 · 34 축복만 연다).

| 물음 | 찍힌 것 |
|---|---|
| ① 부팅 상태(축복 0개)에 시계가 있는가 | **3개 다 있다** — `blsC_atk·hp·rate` 전부 `⏱` 36.7×88.9 |
| ① 그때 `.tm` 의 클래스는 | `[tm **alert** ifbtn]` — `.alert` 는 **꺼져 있을 때** 붙는다 |
| ② 수집기(`COLLECT`)가 그 노드를 보는가 | **본다** — `div#blsC_atk>div.b>s.tm.alert.**ifbtn**>b.ck` 외 2개 |
| ③ `verify356` 의 세 키가 그 경로 안에 있는가 | **0/3** — 키는 `…>s.tm.alert>b.ck` 라 부분 일치가 안 된다 |

⇒ **가설과 정반대다.** 시계는 «축복이 돌 때» 가 아니라 **꺼져 있을 때**(= 부팅 상태) 그려져 있고,
자는 그것을 **이미 보고 있었다**. 회색인 것은 화면도 상태도 아니라 **키 문자열 한 조각**이다.

### 뿌리 — 581 이 얹은 부품 클래스 하나

`renderBless()` 는 581(주인 지시 «축복의 받기 버튼도 일괄 받기 버튼 같은 디자인»)부터
같은 국면에 **클래스를 하나 더** 얹는다:

```
c.querySelector('.tm').classList.toggle('alert', !on);
c.querySelector('.tm').classList.toggle('ifbtn', !on);   // ← 581 이 추가
```

`scan356.js` 의 `pathOf()` 는 «태그 + 클래스 앞 3개» 를 이어 붙인 문자열을 돌려주고,
`verify356` 의 SCOPE 는 그 문자열의 **부분 일치**로만 자리를 찾는다.
클래스가 하나 늘자 경로가 `s.tm.alert>b.ck` → `s.tm.alert.ifbtn>b.ck` 로 갈렸고,
6회차에 박아 둔 세 키는 한꺼번에 **«노드를 한 개도 못 봤다»** 가 됐다.

**대조 실험으로 못박았다**(`probe602` ④) — 살아 있는 페이지에서 `ifbtn` **한 개만** 떼면
(= 581 이전의 DOM) 세 키가 **즉시 3/3 으로 일치**한다. 얕은 클론이라 581 커밋이 이력 밖이지만,
이 대조는 이력 없이도 «클래스 한 개» 라는 것을 직접 보여 준다.

### 등재문 처방을 그대로 따랐으면 — 더 나빠졌다

`probe602` ⑤ 가 그 처방을 실제로 굴렸다. 325·117 이 쓰던 상태 주입으로 축복 3개를 켜면
`.alert` 는 `!blessOn(k)` 이므로 **오히려 빠지고**(`.tm class=[tm]`), 세 키는 **여전히 0/3** 이다.
즉 «축복을 켜고 재라» 는 처방은 이 자리를 **한 칸도 못 살린다.**
(시계 노드 자체는 켠 국면에서도 3개 그대로다 — 문제는 처음부터 국면이 아니었다.)

## 처방 — 키에서 **상태 클래스**를 뺀다 (저장소가 이미 적어 둔 규칙)

같은 파일 **7회차 주석**이 이 규칙을 이미 쓰고 있었다:

> ⚠ 키에 **상태 클래스를 넣지 않는다** — 스캐너 경로는 `div.tr-card.no` 처럼 그때그때의 상태를
> 달고 나오므로 … 상태가 안 끼는 조각으로 문다.

6회차 키(34 축복)만 그 규칙 밖에 있었다. ⇒ `…>s.tm.alert>b.ck` → **`…>s.tm`**.
두 국면(«받기» = `.tm.alert.ifbtn` · «시간이 남았다» = `.tm`)에서 모두 물린다.

**느슨하게 푼 것이 아니다** — `.tm` 아래에서 수집기가 «아이콘» 으로 세는 노드는 시계(`b.ck`)
하나뿐이다(`>i` 는 «받기»/«00:09:59» 라 라벨로 탈락, `s.updot` 은 빈 상자). 시계가 사라지면
키는 그대로 «노드 0개» = **빨강**이다(334 가 이 화면에서 잡은 «시계가 통째로 사라져도 초록» 을
되살리지 않는다).

## 무르게 풀지 않았음 — [R5] 에 항 둘을 **더 세웠다**

`[R5]` 는 «옛 scaleX 를 도로 심으면 ≥7 노드가 빨개지는가» 하나로 세고 있었는데,
**합계만 세면 시계 3자리가 통째로 빠져도 7 이 찬다**(실제로 581 이후 «5건뿐» 이었지만
그 숫자는 «어느 자리가 빠졌는지» 를 말하지 못한다). 그래서 갈래별로 나눠 묻는다.

| 신설 항 | 묻는 것 |
|---|---|
| `[R5] … 그 중 ⏱ 시계가 n노드` | 심은 `scaleX(.97)` 이 **시계 3자리**에서 실제로 빨개지는가 |
| `[R5] … 국면을 갈아도 ⏱ 3노드` | 축복을 켜 국면을 바꿔도 스코프가 시계를 놓치지 않는가(= 키가 상태 클래스에 안 물린다) |

두 번째 항이 이 결함의 **재발 자**다. 602 이전에는 국면이 갈리면 조용히 0개가 됐고
그것을 말해 주는 항이 하나도 없었다.

## 게이트 (1회차 실측)

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `tools/probe602.js` **신설** | — | **12/12** (③ 자의 키 3항이 회색 → 초록으로 뒤집힌다) |
| `tools/verify356.js` [A] 34 축복 시계 3항 | ✗✗✗ «노드를 한 개도 못 봤다» | ✓✓✓ «1노드 전부 등방» |
| `tools/verify356.js` [R5] | 3/4 — «심어도 **5건뿐**(≥7 이어야 한다)» | **5/5** — 8노드 · ⏱ 3노드 · 국면 무관(신설 2항) |
| `verify117` | — | **42/42 PASS** |
| `verify325` | — | **69/69 PASS** |
| `verify394` | — | **20/20** |
| `verify581` | — | **22/22 PASS** |
| `tools/smoke.js` | — | **SMOKE PASS** |

⚠ **[S3] ③ 래칫·이름표 3건은 601 의 것이다**(같은 파일 다른 절 · 다른 워커가 선점 중) — 손대지 않았다.
수리 전 실행도 수리 후 실행도 **같은 3건**이 같은 문장으로 나온다(내 변경과 무관하다는 대조).
수리 전 **`VERIFY356 105/112`**(빨강 7 = 602 의 4 + 601 의 3) → 수리 후 **`VERIFY356 111/114`**
(항이 112 → 114 로 는 것은 [R5] 에 신설한 2항이고, 빨강은 **3건 = 전부 601 것**이다).

⚠ **[S3] ③ 래칫·이름표 3건은 601 의 것이다**(같은 파일 다른 절 · 다른 워커가 선점 중) — 손대지 않았다.
