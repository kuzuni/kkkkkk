# 352 — 공용 서브탭 부품 «337 이 남긴 넷»

> 등재: 2026-08-28, sess-1700-10797 워커 D (337 §9-b · 비평가 AR·AS 2인 + 픽셀 검산)
> 착수: 2026-08-29, **sess-0059-2155 워커 D**
> 호스트 **7곳** — 03 던전 · 06 장비 · 07 스킬 · 10 상점 · 13 재화 · 23 훈련 · 47 룬

---

## 1회차 (2026-08-29, sess-0059-2155) — 제품 `index.html` **3자리**

### 0. 이 회차가 한 일 한 줄

**넷 중 셋을 픽셀로 확정해 고쳤고, 하나(ⓑ 테두리 6 → 8)는 «자를 대기 전에» 기각했다** —
등재문이 «어느 면이 진짜인지부터 확정하라» 고 남긴 그 항이다.

### 1. 새 도구 3개

| 도구 | 하는 일 |
|---|---|
| `tools/probe352.js` | **제품에게 직접** 묻는다 — 6 호스트의 셸·활성 알약·구분선 실상자와 계산된 값 |
| `tools/probe352.py` | ref/cap 을 **같은 마스크로 동시에** 잰다 — 테두리(두 문턱)·림·코너 반경(원호 역산)·구분선 |
| `tools/crop352.py` | 비평가용 **앵커 적용 1:1 비교판** `docs/review/352-{03,07}-{ref,cap}.png` (337 교훈 2) |

게이트는 `tools/verify352.js` **55/55 신설**(§R 되돌림 4항 포함).

### 2. ⓑ 기각 — «ref 8 ↔ 우리 6» 의 8 은 **JPEG AA 한 줄**이었다

등재문이 «실재하나 위험하다 · 어느 면이 진짜인지부터 확정하라» 고 남긴 항이다.
두 비평가(AR·AS)와 337 워커의 검산이 **셋 다 8** 로 일치했는데, 셋 다 같은 것에 속았다.

`python3 tools/probe352.py` 는 검정 문턱을 **둘** 쓴다 — 느슨(≤24)은 «어디가 테두리인가» 를
찾는 데, 순검정(≤4)은 «몇 px 인가» 를 세는 데.

| 07 셸 테두리 | 느슨 ≤24 | 순검정 ≤4 | 단면 (좌, 바깥 → 안) |
|---|---|---|---|
| ref | 좌 8 · 우 8 · 상 8 · 하 6 | 좌 7 · 우 7 · 상 6 · 하 5 | `#0C0500` `#010000` **`#000000` ×5** `#030000` `#4D4135` … |
| 우리 | 좌 6 · 우 6 · 상 6 · 하 6 | 좌 5 · 우 5 · 상 5 · 하 5 | **`#000000` ×6** `#6F5E49` … |

ref 의 검정 바깥에는 `#0C0500`(값 12) 이, 안쪽에는 `#030000`(값 3) 이 **한 줄씩** 붙는다.
느슨 문턱은 그 둘을 같이 세어 **6 → 8** 로 부풀린다. ref 자신이 «면마다 다르게»(07 좌·우 8 /
상 8 / 하 6 · 03 좌 9 / 우 7 / 상 6~8 / 하 7) 읽히던 것도 이것으로 설명된다 —
**실제 테두리는 네 면 다 6 이고, 면마다 다른 것은 이웃 픽셀의 밝기(= AA 의 세기)** 다.

**그리고 이 항은 자를 안 대도 확정된다.** 337 이 셸 높이 **97**·알약 높이 **85** 를 둘 다
ref 와 Δ0 으로 못박아 뒀으므로 상·하 테두리는 `(97 − 85) / 2 = 6` 이고, 네 면은 `border` 한
줄이 정하므로 좌·우도 6 이다. 8 로 밀면 알약이 85 를 유지할 수 없다.
가로도 같은 말을 한다 — ref 셸 바깥 폭 **948**, ref 자신의 칸 경계 기록 **73…1006** ⇒
콘텐츠 933~934 ⇒ 테두리 ≈ **7**(6 + AA 반 줄). 8 이면 932 로 한 칸 더 지나친다.

⇒ **0줄 변경.** 대신 `verify352` [2] 가 이 항등식을 **6 호스트 전부에서** 단언해,
다음 세션이 같은 AA 에 속아 8 로 미는 것을 막는다.

### 3. ⓐ 활성 알약 코너 반경 36 → **32** — 원호 역산기를 «우리 값으로 먼저 검산»했다

AR «ref ≈28 ↔ 우리 ≈34(+21%)» · AS «ref ≈40 ↔ 우리 48.5(+21%)» — 부호와 %는 일치하는데
절대값이 12 나 갈렸다(AS 는 셸 값을 읽었다). 원호로 역산하면 갈릴 데가 없다:

> 상변에서 `d` 행 내려온 좌변 인셋이 `ins` 이면 `(r−d)² + (r−ins)² = r²` ⇒
> **`r = (d + ins) + √(2·d·ins)`**

**역산기를 우리 캡처에 먼저 걸어 검산했다 — CSS 36 ↔ 역산 35.8**(편향 −0.2).
같은 마스크로 ref 를 재면:

| 07 «스킬» 알약 (ref·cap 둘 다 **가운데 칸이 활성**이라 1:1) | 좌 코너 | 우 코너 | 평균 |
|---|---|---|---|
| ref | 30.1 | 33.9 | **32.0** |
| 우리(수리 전) | 35.8 | 35.8 | 35.8 |
| 우리(수리 후) | **32.0** | **32.0** | **32.0 = Δ0** |

좌·우가 3.8 갈리는 것은 JPEG 가 알약 좌 밴드를 6px, 우 밴드를 8px 로 번지게 찍어
검출 시작점이 **양쪽 다 같은 방향(+0.5px)** 으로 밀리기 때문이라 **평균이 그 편향을 지운다.**
⇒ −10.6% = **32**. 등재문 범위(28~32) 안이면서 **픽셀이 받치는 가장 얕은 값**이다
(A3 교훈 2 — 과교정은 되돌리는 데 두 회차가 든다).
⚠ `verify47` 의 `PILL_LIP 14`(좌우 «검정 7 + 림 7»)는 **세로 한복판**의 두께라 반경과 무관하게 불변이고, 실제로 151/151 중 그 6항은 한 항도 안 움직였다.

### 4. ⓒ `.stab-sep` 구분선 top 18 → **16** · height 54 → **55**

AR 단독 지적이었지만 값이 측정표와 정합하고 픽셀이 그대로 받친다(같은 열 세로 프로파일):

| | y 구간 | 높이 | 셸 바깥 상변에서 |
|---|---|---|---|
| ref (x777) | 2043~2097 | **55** | **+22** |
| 우리 수리 전 (x775) | 1985~2038 | 54 | +24 |
| 우리 수리 후 (x775) | 1983~2037 | **55** | **+22 = Δ0** |

`.stab-sep` 은 `.stabs` **패딩박스** 기준이라 `top:16` = 바깥 +22 다(테두리 6).

⚑ **여기가 이 회차의 게이트 교훈이다.** `verify47` 은 구분선을 **폭·높이·left 만** 보고 있어서
«세로로 2px 내려앉은 것» 을 한 번도 못 봤다. 54 → 55 로 숫자만 갈아 끼웠으면 같은 눈먼 자리가
남는다 — **묻는 항을 하나 더 넣었다**(`구분선 상변 = 콘텐츠 상변 + 16`, LESSONS 328).

### 5. ⓓ 셸 좌·우 «안쪽 밝은 림» 7px — **미구현이었다**

`.stabs{background}` 는 세로 `linear-gradient` 뿐이라 상(`#6F6251` 0~8)·하(`#706049` 79~85)
만 있고 **좌·우에는 아무것도 없었다.** ref 는 네 면 전부에 같은 림을 두른다:

| 바 세로 한복판 행, 검정 안쪽 | ref | 우리 수리 전 | 우리 수리 후 |
|---|---|---|---|
| 07 좌 | `#4D4135` `#6F604D` **`#705F4B` ×5** `#71604C` … → 면 `#62513D` | 면 `#61523D` **즉시** | **`#705F4B` ×6** → 면 `#61523D` |
| 07 우 | `#4D4135` `#6E5E4E` **`#705F4B` ×5** … → 면 | 면 즉시 | **`#705F4B` ×6** → 면 |
| 03 좌 | `#2A231B` `#6D6050` **`#705F4B` ×4** … → 면 | 면 즉시 | **`#705F4B` ×6** → 면 |

알약(`.stab.on`)은 `inset ±7 #000` + `inset ±14 #604F3D` 로 **이미 같은 구조**(검정 7 + 림 7)를
갖고 있었고 **셸만 빠져 있었다** → 같은 손잡이로 채웠다:
`.stabs{box-shadow:inset 7px 0 0 #705F4B,inset -7px 0 0 #705F4B}`.

⚠ **선언이 아니라 그림으로 단언한다**(350 처방). inset 그림자는 배경 위·**자식 아래**에 깔리므로
`box-shadow` 가 있어도 자식이 덮으면 안 보인다. `verify352` [4] 는 캡처를 data URL 로 페이지에
되돌려 **찍힌 픽셀**을 읽는다(검정 6 → 림 7 → 바 면 `#61523D` 순서와 길이).
비활성 칸은 배경이 없어 림이 비치고, **끝 칸이 활성이면 알약이 덮는다 — 그것이 ref 와 같은 그림**이다
(03 ref 우 = 활성 칸이라 검정 뒤가 림이 아니라 알약 베벨 `#604F3D` 다).

### 6. 제품 변경 (index.html 3자리)

| 자리 | 전 | 후 | 근거 |
|---|---|---|---|
| `.stab.on{border-radius}` | 36px | **32px** | ⓐ 원호 역산 ref 32.0 |
| `.stab-sep{top,height}` | 18px, 54px | **16px, 55px** | ⓒ ref +22 · h55 |
| `.stabs{box-shadow}` | (없음) | **`inset ±7px #705F4B`** | ⓓ ref 네 면 림 |
| `.stabs{border}` | 6px | **6px (불변)** | ⓑ **기각** |

**레이아웃 이동 0px**(반경·림·구분선은 전부 상자 안쪽 그림이다) · **판정·밸런스 0줄** ·
칸 격자(`.stab-c1~c4` 224/261/223/229)·셸 높이 97·알약 85 **전부 Δ0**.

### 7. 게이트

| 게이트 | 결과 |
|---|---|
| `node tools/verify352.js` | **55/55 신설** ([1] 6 호스트 동시성 18 · [2] ⓑ 항등식 12 · [3] ⓒ 14 · [4] ⓓ 찍힌 픽셀 6 · [R] 되돌림 4 · [5] 콘솔 1) |
| `node tools/verify47.js` | 151 → **153** — 이관 2항(반경 32 · 구분선 5x55) + **신설 1항**(구분선 top 16) |
| `node tools/verify337.js` | **91/91 무수정** ← «부품 재측정의 값은 안 건드렸다» 의 가장 짧은 증거 |
| `node tools/verify96.js` | 64/64 무수정 |
| `node tools/verify335.js` | 24/24 무수정 |
| `node tools/verify45.js` | 36/36 무수정 |
| `node tools/verify271.js` | 62/62 무수정 |
| 회귀 `verify209·210·203·166·299·202·88` | 61·93·85·73·27·42·25 **전부 무수정 초록** |
| `node tools/smoke.js` | **SMOKE PASS** |

### 8. 곁다리 관측 1건 — **378 로 등재** (338·344 규칙대로 **대조 확인 완료**)

**끝 칸이 활성일 때 알약의 «검정 7px» 이 셸 테두리에 겹쳐 검정이 13px 이 된다.**
ref 는 그 자리에 검정을 겹치지 않는다:

| 바 좌변 바깥 → 안 | 우리 10 상점(활성 = 맨 왼쪽 «소환») | ref 03(활성 = 오른쪽 칸) |
|---|---|---|
| | `#000000` **×13** → `#604F3D` ×7 → 면 `#4B3E2D` | `#000000` ×6(+AA) → `#604F3D` → `#634F37` ×5 → 면 `#4B3E2D` |

즉 우리는 그 면에서 **검정이 7px 두껍고 알약 면이 7px 안으로 밀린다.**
**수리 전 커밋에서도 같은 13px** 임을 `git stash` 대조 실행으로 확인했다 — 내 변경과 무관하고,
352 의 네 항 어디에도 없는 **다섯째 자리**라 안 고쳤다. 상세는 PROGRESS 378 행(§17 — 379 가 이것을 설명한다).

### 9. 채점 (비평가 AT · AU 2인 독립)

**비평가에게 준 것** — `docs/review/352-{03,07}-{ref,cap}.png` 네 장. 337 교훈 2 그대로
**하단 앵커 −60 을 미리 적용해 같은 창으로 잘라** 줬다.

| 축 | AT | AU |
|---|---|---|
| ① 요소 배치 | 9 | 9 |
| ② 상대 크기 비율 | **6** | **6** |
| ③ 여백·간격 | 8 | 7 |
| ④ 정렬·그리드 | 9 | 8 |
| ⑤ 색·명암 *(기록만)* | 9 | 10 |
| ⑥ 폰트 크기 비율 *(기록만)* | 9 | 9 |
| **①~④ 최저** | **6** | **6** |

⇒ **미달.** 넷 중 **ⓒ·ⓓ 는 둘 다 «달성»**(구분선 Δ0~0.5px · 림 «좌·우 모두 존재»),
ⓐ·ⓑ 가 남았고 **둘의 ② 감점이 사실상 같은 갈래**다 — «두께계가 얇다».

| 등재문 항 | AT 실측 | AU 실측 |
|---|---|---|
| ⓐ 알약 코너 반경 | 등가 R ref ≈38 ↔ 우리 ≈48 (**+26%**) | 최소자승 원 적합 ref 37.4 ↔ 우리 31.4 (**−16%**) |
| ⓑ 셸 테두리 | ref 좌 8.3~8.6 / 우 8.6 / 상 8.6 / 하 7.0 ↔ 우리 6.0 | ref 8.4/8.4/8.4/7.0 ↔ 우리 6.0 (순검정 8/8/8/6) |
| ⓒ 구분선 | 폭 0 · 길이 0 · 중심 0 → **달성** | 길이 +0.5px(+0.9%) · 중심 +0.06px → **달성** |
| ⓓ 셸 좌·우 림 | 존재 0 · 두께 −1.3px(−16%) → **존재 달성** | 좌·우 모두 존재 · 두께 −1.0~1.5px → **달성** |

---

## 2회차 (2026-08-29, 같은 세션) — AT·AU 반영, 제품 2자리 추가

### 10. ⓐ 가 **정반대로 갈렸다** — 가정이 가장 적은 읽기로 끊었다

AT «아직 +26%(우리가 크다)» ↔ AU «−16%(우리가 작다)». 같은 그림 같은 자리인데 부호가 반대다.
**AU 의 방법이 알약 실루엣에 셸 검정 테두리를 붙여 읽은 것**이다 — AU 자신이
«ref 알약 외곽 h ≈ **95.3**, 바 검정 테두리를 위아래 각 6.3px 파고듦» 이라고 적었는데,
ref 알약은 측정표 07 §9 에 **85**(2027..2111)로 확정돼 있다. 알약의 좌·우 검정 밴드가 셸
테두리와 **맞닿아 이어져** 보이므로 실루엣을 통째로 원 적합하면 위아래 6px 씩을 먹고,
그만큼 코너도 부풀려진다.

타이브레이커는 **가정이 가장 적은 읽기 — «인셋이 0 이 되는 행»** 이다(그 `d` 가 곧 `r`):

| | d=25 행의 좌변 인셋 | ⇒ r |
|---|---|---|
| ref | 아직 **1** | **≈ 28~30** |
| 우리(반경 32) | 0 이 되는 행이 d=32 | 32 |

AR·AS 의 **비(比)** 를 당시 CSS 36 에 되돌려도 같은 데로 온다 — AR 36×28/34 = **29.6** ·
AS 36×40/48.5 = **29.7**. ⇒ 셋이 가리키는 **30**.
수리 후 `probe352.py`: **ref 좌 코너 30.1 ↔ 우리 30.1 = Δ0**(우 코너 33.9 는 JPEG 편향).

### 11. 셸 상·하 밝은 띠 8 / 6 → **7 / 7** — 부품이 자기 규약을 자기 안에서 깨고 있었다

AT «ref 좌·우 8.3 · 상 7.7 · 하 6.9» · AU «ref 7.0 / 7.1 **대칭** ↔ 우리 8.0 / 6.0 **자기모순**»
2인 독립 일치다. 그리고 **측정표 07 §9 의 행 구간 자체가 7 을 가리킨다** —
«상단 하이라이트 **8px**» 이라 적혀 있지만 구간은 `y2028~2034` = **7행**이고, 하단은
`y2098~2104` = 7행이다(라벨과 구간이 어긋난 것은 ⓑ 의 «6px 인데 66~73» 과 같은 병이다).

⇒ `.stabs{background}` 의 첫 스톱 `0 8px` → **`0 7px`** · 마지막 밝은 띠 `79px 85px` → **`78px 85px`**.
좌·우 `inset 7px` 과 같은 값이 되어 **네 면이 한 규약**이 됐다. 셸 97 · 알약 85 는 불변.
`verify352` 에 그 규약을 **세로 픽셀로 묻는 2항**을 신설했다(55 → **59/59**).

### 12. ⓑ 는 2회차에도 **기각 유지** — 그리고 이것이 이 화면의 «회수 불가» 자리다

비평가 **넷이 전부**(AR·AS·AT·AU) «ref 8 ↔ 우리 6» 으로 일치했고, AU 는 순검정 픽셀 수까지
**8/8/8/6** 으로 셌다. 그래도 기각한다 — 넷 다 같은 AA 를 센 것이기 때문이다:

- **자를 안 대도 성립하는 항등식**: ref 셸 97 · ref 알약 85(둘 다 337 이 Δ0 로 확정) ⇒ 상·하 = 6.
  네 면은 CSS `border` **한 줄**이 정하므로 좌·우도 6 이다. 8 로 밀면 알약이 85 를 못 지킨다.
- **ref 자신이 면마다 다르게 읽힌다**(AU: 상 8.4 / 하 7.0 · 03 좌 8.5 / 우 7.1). **실제 테두리는
  면마다 다를 수 없다.** 다른 것은 테두리가 아니라 이웃 픽셀의 밝기 = AA 의 세기다.
- **비용**: 6 → 8 이면 패딩박스가 938 → 934 로 좁아져 4칸 격자(`.stab-c1~c4` 224/261/223/229,
  합 938)가 통째로 다시 잡히고 `verify47` 의 격자·`BAR_BORDER` 6항이 같이 움직인다.
  337 의 AR 도 «손해가 크니 6 유지를 권한다» 고 적었다.

⇒ **0줄.** 대신 `verify352` [2] 가 그 항등식을 **6 호스트 전부**에서 단언해 다음 세션이 같은
AA 에 속아 8 로 미는 것을 막는다. 3회차 비평가에게는 이 결론을 **«확정 사실표»(F1·F2)** 로
미리 줬다 — 335 가 «앵커가 둘» 을 말로 줘서 세 회차를 유령에 태운 그 교훈이다.

### 13. 2회차 채점 (비평가 AV · AW 2인 독립 — **«확정 사실표» F1·F2 를 미리 준 첫 회차**)

| 축 | AV | AW |
|---|---|---|
| ① 요소 배치 | 6 | 9 |
| ② 상대 크기 비율 | **6** | **7** |
| ③ 여백·간격 | 7 | 8 |
| ④ 정렬·그리드 | 7 | 9 |
| ⑤ 색·명암 *(기록만)* | 9 | 9 |
| ⑥ 폰트 크기 비율 *(기록만)* | 10 | 9 |
| **①~④ 최저** | **6** | **7** |

⚑ **사실표가 먹혔다** — 둘 다 «테두리가 −2px 얇다» 를 **감점에서 뺐고**(AW 는 «참고로 ref 는 상 8.57 /
하 6.67 로 자기 안에서 1.9px 어긋나게 읽힌다» 고 F1 을 스스로 재확인했다), 알약 상자도 85 로 고정해
쟀다. 그러자 1회차에 **정반대로 갈렸던 ⓐ 가 둘 다 «달성»** 으로 모였다:

| 항 | AV | AW |
|---|---|---|
| ⓐ 알약 코너 반경 | ref 28.86 ↔ 우리 29.90 (**+3.5%**) → **달성** | ref 30.83±2.68 ↔ 우리 30.15 (**−2.2%**) → **달성** |
| ⓒ 구분선 | 길이 +0.61px · 중심 +0.07px → **달성** | 길이 +0.60px · 중심 +0.06px → **달성** |
| ⓓ 셸 안쪽 림 네 면 | 상 6.95 / 하 6.91 / 좌 8.30 / 우 8.23 ↔ 우리 7.00 → **네 면 달성** | «ref 는 같은 부품을 면마다 6.72~8.24 로 읽는다(07 좌 8.24 vs 03 좌 6.98)» → **7.0 ± 1.3 로 달성** |

**남은 감점이 셋으로 좁혀졌고 셋 다 새 자리다** — 352 가 잡겠다고 한 넷은 전부 닫혔다.

### 14. 3회차 — 셸 «바깥» 코너 반경 50 → **43** (AV ⓔ-1 · 337 이 기각했던 항이 실재였다)

337 §9-b 는 이 항을 «두 추정은 원호 피팅 편의다» 로 **기각**했다. 그런데 AV 가 **상자를 가정하지 않는
자유 3-파라미터 원 적합**(rms 0.12~0.36)에 45° 대각 코너컷·03 두 가지 교차검증까지 붙여 다시 냈고,
같은 방법을 내가 독립으로 돌리자 **소수점까지 같은 데로 왔다**:

| | ref 적합 | 우리 적합 | 우리 실효값 | ⇒ ref 환산 |
|---|---|---|---|---|
| AV | 42.16 | 47.54 | **48.5** | **43.1** |
| 나(`probe352.py` 계열) | 41.21 (rms 0.51) | 46.59 (rms 0.27) | **48.5** | **43.1** |
| 337 AR / AS | ≈44 / ≈44 | — | — | ≈44 |

⚑ **열쇠는 «우리 값이 50 이 아니었다» 는 것**이다 — `border-radius:50px` 는 높이 97 에 걸려
**48.5 로 클램프**된다. 337 이 편향을 50 으로 잡아 «우리 47~48.5 = ref 44 와 3~4 차이 = 피팅 편의»
로 읽은 그 3~4 가, 실효값 48.5 로 잡으면 **편향 −1~−2 로 줄고 차이는 그대로 남는다.**
⇒ **43**. 이 바는 «완전 캡슐»(48.5)이 아니라 «많이 둥근 사각형» 이다(측정표 07 §9 «radius 50 (= h/2,
완전 캡슐) (추정)» 은 그래서 틀렸다). 수리 후 **ref 41.21 ↔ 우리 41.90 = Δ0.69 (+1.7%)**.

### 15. 4회차 — 활성 알약 «내부 3띠» 전부 **7px** · 구분선 길이 55 → **54**

AW 의 유일한 ② 감점이 이것이었다(«세 띠를 전부 7px 로 맞추면 ②→9»). 커버리지 적분으로
07·03 두 그림 · 각 3열에서 재면:

| 알약 내부 띠 | ref | 우리(수리 전) | 우리(수리 후) |
|---|---|---|---|
| 상단 하이라이트 | **6.75** | 8.52 (+26.2%) | **7** |
| 하단 밝은 띠 | **6.86** | 5.00 (−27.1%) | **7** |
| 하단 어두운 띠 | **7.03** | 8.00 (+13.8%) | **7** |

⇒ 알약 국소좌표 **0..7 · 7..71 · 71..78 · 78..85**.
⚑ **이 7 이 이 부품의 «한 규약» 이다** — 셸 네 면 림 7(§11) · 알약 좌·우 «검정 7 + 림 7»
(`verify47 PILL_LIP 14`) 과 같은 7 이다. 337 이 «아래 베벨 rel 72~76»(5px)로 읽은 것은
**단일 열 단면**이었고, 커버리지 적분이 두 그림·여섯 열에서 7 로 모인다(측정표 정오 대상).

**구분선은 «자리만» 틀렸었다** — 1회차에 `top 18→16`(옳다, ref +22 와 Δ0.08) 과 함께
`height 54→55` 도 올렸는데, AV·AW 가 커버리지 적분으로 잰 ref 길이는 **54.39 / 54.40** 이고
55 는 알약 안에서 **아래 여백만 1.54px 좁힌다**(ref 위 16.1 / 아래 15.5 ↔ 우리 16 / 14).
⇒ **54 로 되돌렸다.** 게이트에는 1회차가 신설한 «상변 = 콘텐츠 상변 + 16» 항이 그대로 남는다.

### 16. 제품 변경 누계 (index.html **6자리**)

| 자리 | 착수 전 | 최종 | 근거 |
|---|---|---|---|
| `.stab.on{border-radius}` | 36px | **30px** | ⓐ · AV 28.86 / AW 30.83 / 원호 역산 28~30 |
| `.stab-sep{top}` | 18px | **16px** | ⓒ · ref 셸 바깥 +22 |
| `.stab-sep{height}` | 54px | **54px (불변)** | ⓒ · 1회차 55 를 4회차에 되돌림 |
| `.stabs{box-shadow}` | (없음) | **`inset ±7px #705F4B`** | ⓓ · ref 네 면 림 |
| `.stabs{background}` 상·하 띠 | 8px / 6px | **7px / 7px** | AT·AW · 네 면 한 규약 |
| `.stabs{border-radius}` | 50px(실효 48.5) | **43px** | AV ⓔ-1 · 자유 원 적합 2인 43.1 |
| `.stab.on{background}` 3띠 | 8.5 / 5 / 8 | **7 / 7 / 7** | AW · 커버리지 적분 6.75/6.86/7.03 |
| `.stabs{border}` | 6px | **6px (불변)** | ⓑ **기각** |

**레이아웃 이동 0px** · **판정·밸런스·세이브 0줄** · 칸 격자(224/261/223/229)·셸 97·알약 85 **전부 Δ0**.

### 17. 곁다리 등재 2건 (338·344 규칙대로 **대조 확인 완료**)

- **378** — 끝 칸이 활성일 때 알약의 «검정 7px» 이 셸 테두리에 겹쳐 검정이 **13px** 이 된다(§8).
- **379** — 균등분할 바(`.sp2`/`.sp3`)의 칸 격자 **기준 상자**가 4칸 격자와 다르고(패딩박스 ↔ 바깥 상자),
  활성 알약의 «칸 대비 오버행»(ref ~11.5~13.2px/변) 규칙이 없다. AT·AV **2인 독립 일치**.
  ⚑ **379 가 378 을 설명한다** — 오버행이 있으면 끝 칸 알약은 셸 안쪽 변에 «맞닿는» 게 아니라
  **파고들어** 그 변의 검정을 셸 테두리와 공유한다. 378 의 13px 은 오버행이 없어서 두 검정이
  **겹치지 않고 더해진** 결과다. ⇒ **379 를 먼저 잡으면 378 이 같이 닫힐 수 있다.**

### 18. 3회차 채점 (비평가 AX · AY 2인 독립 — 사실표에 **F4**(379 이관) 추가)

| 축 | AX | AY |
|---|---|---|
| ① 요소 배치 | 8 | 9 |
| ② 상대 크기 비율 | **8** | **7** |
| ③ 여백·간격 | 9 | 7 |
| ④ 정렬·그리드 | 9 | 9 |
| ⑤ 색·명암 *(기록만)* | 9 | 9 |
| ⑥ 폰트 크기 비율 *(기록만)* | 10 | 8 |
| **①~④ 최저** | **8 (통과)** | **7** |

⇒ **한 명 통과.** 3·4회차가 손댄 것이 전부 «달성» 으로 확인됐다:

| 항 | AX | AY |
|---|---|---|
| ⓐ 알약 코너 반경 | ref 30.55 ↔ 우리 31.04 (**+1.6%**) → 달성 | ref 29.10 ↔ 우리 30.05 (**+3.3%**) → 달성 |
| ⓑ 셸 바깥 코너 반경 | ref 42.82 ↔ 우리 42.88 (**+0.14%**) → 달성 | ref 43.35 ↔ 우리 42.65 (**−1.6%**) → 달성 |
| ⓓ 셸 림 네 면 | 상 7.00 ↔ 7.00 · 하 6.80 ↔ 7.00 → 달성 | (좌·우만 −1.3) → 미달 |
| ⓔ 알약 내부 3띠 | 6.90 / 6.75 / 7.06 ↔ 7 / 7 / 7 → **셋 다 달성** | 6.97 / 6.89 / (7 범위) → **셋 다 달성** |

⚑ **AX 가 F1 을 «림» 으로 확장해 줬다** — ref 의 검정 테두리가 좌·우면에서만 +2.5px 부푸는데,
**같은 좌·우면에서 림도 +1.3 부푼다**(상·하면은 ref 7.00 / 6.80 으로 우리와 Δ≤0.2). 오염원이
하나라는 뜻이라, AY 가 ⓓ 를 미달로 본 근거(좌·우 −1.3)는 F1 오염의 그림자다. ⇒ 4회차 사실표에 넣었다.

### 19. 5회차 — 구분선 폭 5 → **6** · 셸 림 네 면 **색까지** 통일

**ⓒ 폭** — 세 비평가가 **커버리지 적분**으로 ref 를 **5.60(AW) · 5.74(AV) · 5.78(AY) · 5.67(AX)** 로 읽었다.
적분은 **에너지 보존**이라 번짐이 폭을 부풀리지 않는다 — 즉 이 값은 AA 로 설명되지 않는다.
내 적분도 같은 말을 한다: **ref 5.38 ↔ 우리 4.83**(참값 5 · 편향 −0.17) ⇒ **ref ≈ 5.55**.
5 는 −11% · 6 은 +7% ⇒ **6**. 중심은 지킨다(ref 중심 = 콘텐츠 좌변 + **706.43** ↔ 우리 706.0).
⚑ **게이트도 같이 바꿨다 — `left` 가 아니라 «중심» 을 묻는다.** 폭이 바뀌면 `left` 는 따라 움직여야
하는데 `left` 만 묻는 항은 «중심이 밀린 것» 을 못 본다(LESSONS 328 계열).

**림 색** — AX ⑤ 기록: «ref 상 94.2 / 하 94.2 / 좌·우 94.5 로 **균일** ↔ 우리 상 96.7 / 하 93.7».
우리 셸 배경 그라데이션이 상 `#6F6251` · 하 `#706049` 로 갈라져 **ref 에 없는 3.0 레벨 상·하
그라데이션**을 만들고 있었다 ⇒ 좌·우 `inset` 과 같은 **`#705F4B`(94.0)** 로 네 면을 통일했다.
이로써 이 부품의 «7px 한 규약» 이 **두께에 이어 색까지** 한 값이 됐다.

### 20. 회수 못 한 것 — 다음 세션에게

1. **활성 알약 «바닥 코너 띠 감김»**(AX ①, −100%) — ref 는 알약 하단의 어두운 띠·밝은 띠가
   **코너 반경을 따라 안쪽으로 감겨 올라간다**(07-ref y112~129, x282~288 에 폭 2~5.1px).
   우리는 세로 옆띠(`inset ±14px #604F3D`)가 **직선으로 내려와** 그 자리를 덮는다 —
   `box-shadow: inset` 은 배경 그라데이션 **위**에 그려지기 때문이다. 고치려면 옆띠를
   그림자가 아니라 **radial/conic 로 코너를 감싸는 그림**으로 다시 그려야 한다(구조 변경).
   AX 는 이것을 안고도 ① 8 을 줬다.
2. **셸 림 좌·우 −1.3px**(AY ②③) — AX 가 F1 오염으로 분류했다(위 §18). 3인(AV·AW·AX)이 달성.
3. **구분선 위/아래 비대칭** ref 1.78px ↔ 우리 1.00px (AX #4, Δ0.78px) — 0.5px 급이라 정수 CSS 로
   회수 불가에 가깝다(`top:16` 을 16.5 로 하면 서브픽셀 렌더로 선이 물러진다).
4. **「장비」 라벨 1.0px 좌측**(AX #5) — bbox 폭은 ref 71 ↔ 우리 71 로 Δ0 이라 서체 문제가 아니라
   **칸 중심 정렬** 문제다. `.stab-c1` 격자(47 소관)라 이 작업 단위가 아니다.
