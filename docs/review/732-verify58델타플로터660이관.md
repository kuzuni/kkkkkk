# 732 — `tools/verify58.js` 2건 실패(25/27) → **게이트 부패**: [7]·[8] 의 659·660 이관

**세션** sess-1726-22911 (루틴 워커 B) · **1회차** · **2026-09-01**
**제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/verify58.js` 두 절이다.

---

## 1. 재현 — 등재문 그대로다

```
node tools/verify58.js  →  VERIFY58 25/27 FAIL
  [7] ✗ 델타 «+n» 플로터가 난다
  [8] ✗ 씬 A 34 · 씬 B 34 · 씬 C null px
```

등재문(sess-1625-22108, 684·685 회귀 스윕의 곁다리)이 `git stash` 대조로 «수리 전에도 같은 25/27» 을
이미 확인해 두었고, 이 회차 첫 실행도 **같은 두 항**이다. 갈래는 등재문이 열어 둔 그대로 둘이었다:

* ⓐ **게이트 부패** — 659(단련 숫자 플로터 폐지)·660(훈련·단련·룬 확정 스펙 — 숫자 플로터 폐지,
  아이콘 버스트로 교체)이 호스트를 없앤 뒤 자만 옛 표본을 아직 찾는다
* ⓑ **실재 회귀** — 아직 플로터를 쓰는 자리가 있는데 그것까지 같이 죽었다

---

## 2. 판정 — `tools/probe732.js` **12/12 PASS** (338 규칙: 처방 전에 제품에게 묻는다)

| 물음 | 실측 |
|---|---|
| 씬 C(23 훈련 카드)는 났는가 | **났다** — 골드 **45 지출**(연출이 아니라 «판정» 에서 읽었다) · 흰 플래시 1장 |
| 씬 C 의 «+n» 플로터 | **0장** |
| 그 자리에 무엇이 뜨는가 | **아이콘 버스트 14알**(`.fx-spark.fx-cic` · 화폐 신원 **gold** 하나) |
| 옛 앰버 방사형 불꽃 | **0개** — 겹쳐 쏘지 않는다(583 «되게» 가 «더하기» 가 아니라는 것) |
| 부품 `.fx-plus` 는 죽었는가 | **아니다** — 씬 A 1장 34px · 씬 B 1장 34px |
| 델타 부품(`.fx-plus.fx-delta`)은 | **살아 있다** — 50 코스튬 [강화]에서 1장 34px(강화석 2 지출) |
| 소스 — 훈련 두 호출부 | 셋째 인자가 **`null`**(35063·35123) — 660 주석 «숫자 플로터 폐지» 와 정확히 일치 |
| 소스 — 텍스트를 넘기는 곳 | **코스튬뿐**(34076·34202) |

⇒ **ⓐ 게이트 부패.** 제품은 660 대로 옳고, 종전 [7] 은 «주인이 없애라고 한 것이 **있어야**
통과하는 자» 가 돼 있었다. 694 가 `verify93` [7] 에서 닫은 것과 **같은 뿌리·같은 처방**이다
(등재문이 «702 와 한 벌로 본다» 고 적어 둔 그 짝).

### ⚠ 1회차 함정 — 주석을 안 걷으면 답이 뒤집힌다

첫 실행에서 `probe732` 가 **10/12** 로 빨갰는데, 빨간 둘은 **제품이 아니라 내 스캐너** 몫이었다.
`fxUpOk(` 를 그냥 세면 **주석의 인용까지** 호출부로 잡힌다 — 31036(«종전 한 줄은 …» 인용) ·
35138 · 35268 이 그렇다. 걷기 전 **11곳**, 걷은 뒤 **8곳**이고 «텍스트를 넘기는 곳» 은 4 → **2** 로 준다.
그 상태로는 «훈련 호출부가 셋» 으로 읽혀 판정이 뒤집힌다. 주석 자리를 **같은 길이의 공백**으로
덮어(줄 번호 보존) 다시 셌다. ⇒ **LESSONS 732-①.**

---

## 3. 처방 — 자리를 비우지 않는다 (333 · 694 가 짝 게이트에 쓴 규약 그대로)

| 항 | 종전 | 732 이관 |
|---|---|---|
| [7] 셋째 층 | 씬 C 에 델타 «+n» 플로터가 **난다** | — (호스트가 사라졌다) |
| [7-a] | — | **전제 신설** — 씬 C 는 났다(훈련 강화로 **골드가 나갔다**). 없으면 «0장» 이 «씬이 안 났다» 와 구별되지 않는다 |
| [7-b] | — | **방향을 뒤집었다** — 훈련 «+n» **0장**(660 이 되돌아가면 빨개진다) |
| [7-c] | — | **양성항 신설** — 그 자리를 **재화 아이콘 버스트**가 대신한다(≥3알 · 화폐 신원 `gold`). 없으면 «660 이 통째로 사라져도 초록» 이다 |
| [8] | 씬 A · 씬 B · **씬 C** 의 «+n» 글자 크기가 공용 토큰 하나 | 표본 셋째 자리를 **씬 D(50 코스튬 [강화])** 로 이관 — 델타가 아직 사는 유일한 자리 |
| [8-a] | — | **전제 신설** — 씬 D 는 났다(강화석 지출). §R-d 의 씬 D 판 |

⚑ **새 임계는 하나도 안 만들었다.** [7-c] 의 «≥3» 은 [7] 의 알갱이 층 항이 이미 쓰던 수이고,
화폐 신원 `gold` 는 **660 의 스펙 문장**(«훈련 = 골드») 그대로다. [8] 의 0.6px 창은 종전 값 무변경.

⚑ **위치 축은 여기서 다시 안 잰다.** 702 가 `verify93` 에 요구했던 «버스트가 660 이 금지한 자리에서
터져도 초록이면 안 된다» 는 **`verify660` [C1]·[C2] 가 세 탭 전부** 이미 단언한다(스폰 중심이 강화 버튼
상자 안 · 버튼이 호스트보다 작다 = C1 이 헛초록이 아니다). 여기 또 적으면 **값이 두 벌**이 된다(402 규약).
`verify58` [7] 이 지키는 것은 위치가 아니라 «한 사건에 세 층이 같이 난다» 다.

⚑ **왜 씬을 둘로 줄이지 않았나** — [8] 이 지키는 것은 «회당 플로터가 자기 자리에 크기를 **손으로 적지
않는다**» 이고, 그것은 491 2회차가 488 의 실제 사고를 되돌린 자리다. 씬 C 를 그냥 빼면 델타 계열이
자에서 통째로 빠져 **토큰이 풀려도 초록**이 된다 — 아래 §R-e 가 그것을 실측으로 못박는다.

씬이 하나(**씬 D**) 늘었다. 버스트·델타 수집기는 **씬 C·D 에서만** 돈다 — 씬 A·B 의 타이밍 축은
샘플러 부하에 물리므로(570) 거기서는 한 줄도 더 안 돌게 했다.

---

## 4. 무르게 풀지 않았음 — `tools/verify732.js` **11/11 PASS** (되돌림 시험 신설)

334 규약대로 결함을 하나씩 주입한 사본으로 **그 항이 실제로 빨개지는가** 를 물었다
(694 가 같은 짝에 세운 틀 · 사본 이름에 pid = 646·648 처방 · `V58_SRC` 손잡이 신설).

| 주입 | 빨개져야 하는 항 | 결과 |
|---|---|---|
| §R-a **660 되돌림** — 훈련 첫 발이 다시 «+1» 을 띄운다 | [7-b] | ✗ (초록 → 빨강) |
| §R-b **버스트 폐지** — 알갱이가 `.fx-cic` 를 안 단다 | [7-c] | ✗ **0알 []** |
| §R-c **부품 사망** — `fxDelta` 가 아무것도 안 만든다 | [8-b] | ✗ 씬 D **null px** |
| §R-d **씬 C 판정 무력화** — 훈련이 골드를 안 쓴다 | [7-a] | ✗ **골드 0 지출** |
| §R-e **토큰 풀림** — 코스튬 델타«만» 크기를 손으로 적는다 | [8-b] | ✗ 씬 D **30 px**(A·B 34) |
| §R-f **씬 D 판정 무력화** — 코스튬 강화가 안 난다 | [8-a] | ✗ **강화석 0 지출** |

**§R-d·§R-f 가 이 이관의 핵심을 지킨다** — 그 둘이 없으면 «씬이 안 났다» 를 «폐지됐다» 로 읽는
**헛초록**이 정확히 종전 실패문의 모양으로 되살아난다.
**§R-e 는 [8] 이 왜 씬을 셋 유지해야 하는지**를 못박는다 — 488 이 실제로 낸 사고(«이 자리에만 크기를
손으로»)를 그대로 재현하고, 씬 C 를 빼고 둘로 줄인 자에서는 **이 주입이 안 잡힌다.**

---

## 5. 게이트 · 회귀

* `node tools/verify58.js` → **VERIFY58 30/30 PASS** (종전 25/27 · 항수 27 → 30)
* `node tools/verify732.js` → **VERIFY732 11/11 PASS** (신설 · §0 5줄 + 주입 6축)
* `node tools/probe732.js` → **PROBE732 12/12 PASS** (신설)
* 회귀 — §6 표

---

## 6. 곁다리 관측 (등재 판단)

**[7] 의 «알갱이 층» 항은 지금 OR 의 오른쪽 가지로만 초록이다.** 583 이 그 항에
«화폐 신원까지 같이 못박는다» 며 넣은 왼쪽 가지(`.fx-spd` 화폐 알갱이 ≥3·gold)는 **0개**다 —
658·660 이 «알약 → 버튼» 비행(`fxSpend`)을 폐지했기 때문이고, 그 죽은 부품의 **선언째 철거는
이미 678 로 등재돼 있다**. 오른쪽 가지(`.fx-spark` ≥10)는 660 의 버스트가 `.fx-spark.fx-cic` 를
달고 있어 초록이다.

**그 자리를 이번에 안 지운 이유**는 판단이 아니라 범위다 — ⓐ 678 이 그 부품의 마감을 들고 있고,
ⓑ 이번 회차가 세운 **[7-c] 가 그 항의 뜻을 이미 더 강하게 다시 세웠다**(버스트를 **이름과 화폐
신원으로** 단언하므로 «660 이 사라져도 초록» 이 아니다). 678 이 `fxSpend` 계열을 선언째 걷을 때
그 OR 의 왼쪽 가지도 같이 걷으면 된다 — **새 번호를 뗄 결함이 아니다.**
