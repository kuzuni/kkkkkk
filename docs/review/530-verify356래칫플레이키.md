# 작업 530 — `verify356` [S3] ③ 래칫이 실행마다 흔들린다 (자 문제 · 제품 `index.html` 0줄)

> 티어 **T1 «버그(자 플레이키)»** · 지시서 [3]-**(가)**(기계적 작업 — 레퍼런스 대조가 아니라
> «자가 같은 트리에서 같은 답을 내는가» 다. **비평가 없음**).
> 세션 `sess-1959-16876` 워커 D · 2026-08-30 · 1회차.

## 0. 한 줄

같은 트리에서 `[S3] ③` 이 **칸 47·49·51 / 자리 10·11** 로 흔들렸고, 뿌리는 스윕이
**«상시» 가 아닌 화면을 재고 있던 것**이다 — ⓐ 찰나의 연출 노드가 섞이고 ⓑ 상시 쥬시(122)가
차분 두 장 사이에도 계속 움직였다. **제품은 한 줄도 안 고쳤다.**

## 1. 재현이 먼저 (338 규칙)

등재문은 처방 후보 둘(① 연출 클래스 노드를 판정에서 뺀다 ② 연출을 멈추고 스윕한다)을 적어 뒀지만,
**먼저 흔들림을 눈으로 봐야** 어느 쪽이 뿌리인지 갈린다. 그래서 두 갈래로 쟀다.

### ⓐ 전 화면 스윕 3회 — 같은 트리, 다른 답 (수리 «전»)

| 실행 | 잰 노드 | 판정 | 칸(편차 >0.5%) | 자리 | 그중 `#fxlc>b.fx-fly>img.cic` |
|---|---|---|---|---|---|
| base1 | 404 | 201 | **49** | **11** | 5칸 (+1.59%) |
| base2 | 404 | 204 | **47** | **10** | 4칸 (−1.43%) |
| base3 | 414 | 211 | **51** | **10** | 8칸 (−1.59%) |

* **비행 코인을 빼면** 칸은 **44 · 43 · 43** 으로 거의 붙는다 ⇒ 흔들림의 **본체가 그 한 그룹**이다.
* 같은 그룹의 **잉크가 통째로 다르다**(64×63 · 69×70 · 62×63) — «날아가는 중» 의 어느 프레임에서
  굳었느냐로 크기가 바뀐다. 이것은 «상시 아이콘의 종횡» 이 아니다.
* 래칫 `RATCHET_CELLS = 50` 을 **base3(51)이 넘었다** — 등재문의 «임의로 넘는다» 가 그대로 재현됐다.

### ⓑ `tools/probe530.js` 신설 — 흔들림의 **입력 집합**만 싸게 잰다

전 화면 스윕은 한 번에 10분이라 «네 번 돌려 보기» 가 한 시간짜리 실험이 된다. 그런데 뿌리를 보는 데
**판정까지 갈 필요가 없다** — 스윕이 **무엇을 재게 되는가**(그 순간 화면에 있던 연출 노드 수)만
보면 된다. 5화면 × 6회:

```
[정규화 전] 02 메인            — 0 · 0 · 0 · 0 · 1 · 1   (0~1)
[정규화 전] 52 메뉴            — 0 · 1 · 2 · 0 · 2 · 4   (0~4)
[정규화 전] 29 룰렛            — 0 · 0 · 0 · 2 · 3 · 4   (0~4)
[정규화 전] 22 퀘스트          — 3 · 1 · 2 · 0 · 2 · 0   (0~3)
[정규화 전] 33 재화 정보(유물)  — 2 · 2 · 3 · 1 · 2 · 3   (1~3)
① 실행마다 수가 달라진 화면: 5/5
```

**다섯 화면 전부**가 실행마다 다른 입력을 준다. 등재문이 지목한 세 그룹은 여기서 두 갈래로 갈렸다 —
`#fxlc` 안의 비행 코인(레이어)과 `.cbox.cGold.fx-punch` 계열(클래스).

## 2. ⚑ 이 작업의 본체 — **뿌리가 하나가 아니라 셋이었다**

등재문은 ⓐ(연출 노드)만 지목했다. 그것을 닫자 칸이 **45 · 43** 으로 좁아졌지만 **±2 가 남았고**,
남은 것을 파고들어 두 가지가 더 나왔다.

### ⓐ 찰나의 연출 노드가 판정에 섞인다 — «상시» 가 아니다

356 [A] 가 지키려는 성질은 자기 문구 그대로 «**상시** 크롬 아이콘의 종횡» 이다.
그런데 스윕은 재화 비행(`#fxlc > .fx-fly > img.cic`)·알약 펀치(`.cbox.cGold.fx-punch > i > img.cic`)를
같이 쟀다. ⇒ **재기 전에 페이지를 «상시» 상태로 되돌린다**(연출 레이어 `#fxlc`·`#fxl` 비우기 +
`fx-*` 클래스 걷기).

⚠ **«판정에서만 뺀다»(등재문 후보 ①)를 안 고른 이유**: 무엇을 뺐는지가 결과에 안 남아 스코프가
조용히 줄고(397 «무음 실패» 사고), 이웃 아이콘은 **여전히 그 연출에 가려진** 채 판정된다.
걷어 내면 둘 다 사라진다. 그리고 걷은 수(`fx`·`fxCls`)를 **결과에 실어 돌려준다**.

### ⓑ **비우기만으로는 모자랐다** — 정지 뒤에도 연출 노드가 «또» 태어난다

레이어를 비우고 180ms 뒤에 재니 5화면 25회에서 **12건**이 도로 잡혔다.
`clearInterval/clearTimeout(1..20000)` 은 **그 순간** 걸려 있던 타이머만 지우므로, 게임 틱이
그 뒤에 새로 잡은 타이머는 계속 돈다. ⇒ 레이어를 **`display:none` 으로 내린다** — 그 뒤에 무엇이
생겨도 rect 가 0 이라 COLLECT 의 «8px 미만은 건너뛴다» 에 걸려 판정에 절대 안 섞인다.

### ⓒ ⚑ **정지 절차 자신이 «무한 반복» 을 한 번도 못 세우고 있었다**

8회차가 놓은 정지는 `for (const a of document.getAnimations()) { try { a.finish(); } catch (e) {} }` 다.
**무한 반복 애니에서 `finish()` 는 던진다**(`Cannot finish Animation with an infinite target effect`) —
그 예외를 `try/catch` 가 삼켰고, `clearInterval`·rAF 무력화는 **CSS 애니를 안 멈춘다**(합성기에서 돈다).
⇒ 122 가 넣은 «가만히 있어도 살아 있는» 상시 쥬시가 **차분 두 장 사이에도 계속 돌고 있었다.**

실측(13 재화 탭 · 정지·정규화를 다 마친 뒤):

```
DIV.gem  animation-name = jz122Float      top = 360.736 / 360.212 / 360.654 …
document.getAnimations() → 183~186개 중 jz122Float·jzDotPulse·jzUpN 이 running
같은 페이지에서 500ms 더 기다리면 top 이 또 달라진다 (360.736 → 360.6719)
```

`pause()+currentTime=0` 으로는 **안 닫혔다** — 재렌더가 **새 CSSAnimation 을 계속 만든다.**
⇒ 판정 직전에 «애니는 없는 것» 이라고 **선언**한다:
`*,*::before,*::after{animation:none!important;transition:none!important}`.
그러면 요소가 자기 **기본 자리**에 선다 — 그것이 356 이 재려는 «상시» 기하다.

⚠ **선언이 스코프를 줄일 수 있다**(`fill:forwards` 로만 보이던 노드). 그래서 선언 **전후로**
«잴 수 있는 노드» 수를 세어(`COUNT_VIS`) 줄었으면 `stillLost` 로 신고한다 — 게이트가 0 을 단언한다.
**조용히 줄지 않는다.**

## 3. 고친 것 (제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄)

| 파일 | 무엇 |
|---|---|
| `tools/probe418.js` | `FREEZE`(유한은 finish · 무한은 주기 0) · `SETTLE_FX`(레이어 비우기 + `display:none` + `fx-` 클래스 걷기) · `STILL_CSS`(`animation:none`) · `COUNT_FXIN`(잰 뒤 잔여) · `COUNT_VIS`(스코프 손실) 신설. 결과에 `fx`·`fxCls`·`fxLeft`·`stillLost`·`anFin`·`anHeld` 를 실어 돌려준다 |
| `tools/verify356.js` | `[S3] ②` 에 «연출 정규화» 항 1개 신설 · `[S3] ③` 래칫을 실측대로 **조인다** |
| `tools/probe530.js` | **신설** — 재현자. §1 입력 집합 흔들림 · §2 정규화 뒤 잔여 · §3 «옛 정지 ↔ 새 정지» y 드리프트 대조 |

⚑ **정지·정규화는 `probe418` 한 곳에만 적는다.** `probe530` 은 그 함수를 **가져다 쓴다**
(402 교훈 — 두 벌로 적으면 한쪽만 늙는다).

## 4. 수리 «후» 실측

(§6 에 표)

## 5. 무르게 안 풀었다는 증거

- **래칫을 올려서 초록을 만들지 않았다.** 자 안 주석이 직접 경고하는 자리이고, 등재문도 못박았다 —
  실제로 한 것은 **내리는 것**이다(칸 50 → 46 · 자리 13 → 11).
- **스코프를 조용히 안 줄였다** — 걷은 것(`fx`·`fxCls`)·잔여(`fxLeft`)·선언이 앗아간 노드(`stillLost`)를
  전부 결과에 실어 `[S3] ②` 가 단언한다. `JUDGE_MIN` 은 **안 건드렸다**(바닥이지 결함 래칫이 아니다).
- **되돌림 대조** — `probe530` §3 이 «옛 정지» 를 그대로 다시 돌려 드리프트를 찍는다
  (옛 0.64~1.67px ↔ 새 **0.0000px**). 새 정지가 무르게 푼 것이 아니라 **축을 없앤 것**임을 이 대조가 못박는다.
- **레이어 이름이 바뀌면 던진다** — `SETTLE_FX` 는 `#fxlc`·`#fxl` 를 못 찾으면 조용히 넘어가지 않고
  예외를 낸다(443 «무음 실패를 다른 무음으로 갈아 끼우지 마라»).

## 6. 게이트·회귀 실행 결과

(채워 넣는다)

## 7. 다음 세션이 볼 것

- **`page.screenshot` 두 장의 차분으로 재는 자를 새로 쓰는 세션에게**: `getAnimations().finish()` 는
  **무한 반복에서 던진다.** 그 한 줄로 «정지했다» 고 믿으면 122 계열 상시 쥬시가 그대로 돈다.
  판정 직전 `animation:none` 선언이 가장 싸고 결정적이며, **선언 전후 노드 수를 세어** 스코프 손실을
  같이 신고해라.
- **`clearInterval(1..20000)` 은 «그 순간» 만 지운다.** 그 뒤 태어나는 노드까지 막으려면
  자리(레이어)를 `display:none` 으로 내려라.
- 남은 잔여 자리는 **상자·좌표·아트 몫**이라 이 작업 밖이다(418 §5·356 17회차 판정 그대로).
