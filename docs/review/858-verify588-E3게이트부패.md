# 858 — `tools/verify588.js` [E3] 게이트 부패 (구 세이브 offPlus → 오프라인 ×배율)

**세션** sess-0643-18889 (루틴 워커 C) · **1회차** · 2026-09-03
**티어** T1 «버그(게이트 부패)» · 지시서 [3]-**(가)** (자·재현기 — 비평가 없음)
**제품 `index.html` 0줄.** 고친 것은 `tools/verify588.js` 한 파일이다.

---

## 1. 재현 (338 규칙 — 처방보다 먼저)

```
node tools/verify588.js
FAIL E3 구 세이브의 offPlus 권한이 새 상품(오프라인 ×배율)으로 이어진다 — ×1.381
VERIFY588 28/29 FAIL
```

등재문(sess-0528-18953 워커 B)이 갈래를 둘로 열어 두었다 —
**ⓐ 게이트가 옛 축을 재고 있다(게이트 부패 · 제품 0줄)** / **ⓑ 제품이 구 세이브의 권한을 배율로 못 옮긴다(세이브 이관 버그)**.

**ⓐ 가 맞았다.** 갈라 낸 근거는 세 줄이다:

| 자리 | 값 | 누가 정하는가 |
|---|---|---|
| `index.html:35087` | `const PASS_OFF_MUL = 1.381;` — 주석에 «199 26회차(구 1.2)» | **199** (`verify758` [P1]·[P2] 가 값을 정한다) |
| `tools/verify151.js:49` | `OFF_MUL_ON = 1.381` — 주석에 «26회차 1.2 → 1.381» | 199 26회차가 **같이 갱신했다** |
| `tools/verify588.js:187` | `Math.abs(E.offMul - 1.2) < 1e-9` | 199 26회차가 **못 보고 지나갔다** |

즉 제품·151·758 은 한 세대(1.381)로 같이 움직였고 **588 만 옛 세대(1.2)에 굳어 있었다.**
구 세이브의 권한 자체는 내내 정상이다 — 같은 실행의 [E1] 이 `offPlus=true` 로 초록이고,
`offMul()` 도 그 권한을 보고 1.381 을 내주고 있었다. **결손은 «틀린 값» 이 아니라 «틀린 주인» 이다.**

## 2. 처방 — 값을 옮겨 적지 않고, 축을 588 의 것으로 되돌린다

가장 짧은 수리는 `1.2` → `1.381` 한 글자다. **안 골랐다.** 그건 199 가 계수를 또 흔드는
다음 회차에 **똑같은 얼굴로 다시 빨개지는 자리**를 남긴다(이번이 그 두 번째다).

배율의 **값**은 588 의 주장이 아니다 — 값을 정하는 항은 `verify758` [P1]·[P2] 이고,
세대 상수 대조는 `verify151` [C2] 다. 588 이 묻는 것은 **«구 세이브의 권한이 배율로 전달되는가»** 하나다.
⇒ 자리를 **제품 선언 `PASS_OFF_MUL`** 에 맞추고, 그것이 헛초록이 되지 않게 **짝 항 둘**로 못박았다.

```js
ok(Math.abs(E.offMul - E.passOffMul) < 1e-9 && E.passOffMul > 1,
   'E3 … 제품 선언 `PASS_OFF_MUL` 과 일치 · 1 아님', …);
ok(E.offMulNoPerm === 1,
   'E3b 짝 — 같은 트리에서 권한을 끄면 배율이 정확히 1', …);
```

`E3b` 는 같은 페이지에서 `S.pass.offPlus` 를 한 번 껐다 **즉시 원복**하고 읽는다(뒤 항 [E4]·[E5] 가
구 세이브 상태를 그대로 보게 하려고). 제품은 한 줄도 안 건드렸다.

## 3. 되돌림 시험 — 무르게 푼 수리가 아니다 (334 처방)

사본에 결함을 심어 **셋 다 실제로 빨개지는지** 확인했다(전부 확인 후 `index.html` 원복 · `git diff` 0줄).

| 심은 결함 | 기대 | 실측 |
|---|---|---|
| `offMul = () => PASS_OFF_MUL` (권한을 안 본다) | E3b 빨강 | **FAIL E3b — ×1.381** (29/30) |
| `PASS_OFF_MUL = 1` (상품이 무의미) | E3 빨강 | **FAIL E3 — ×1 (PASS_OFF_MUL ×1)** |
| `passOffPlus = () => false` (권한을 안 잇는다) | E3 빨강 | **FAIL E3 — ×1 (PASS_OFF_MUL ×1.381)** |

⚑ 셋째가 이 항의 존재 이유를 그대로 보여 준다 — **[E1] 은 초록인 채로 E3 만 빨개졌다.**
[E1] 은 `S.pass.offPlus` 필드를 직접 읽으므로 «필드는 살아 있는데 아무 데도 안 쓰인다» 를 못 본다.
E3 이 «권한 → 배율» 전달을 보는 유일한 항이다.

## 4. 결과

- `node tools/verify588.js` **30/30 PASS**(28/29 → 항 하나 신설, 29 → 30).
- 회귀(전부 **무수정**): `verify151` **84/84** · `verify758` **55/55** · `verify712` **36/36** · `verify164` **31/31**.
- **SMOKE PASS**(화면비 6종) · 콘솔 에러 0 · 저장 구조 무변경(KEY v4).
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 계수 0줄.

## 5. 전수 스윕 — 같은 병이 더 있는가

`grep -rn "1\.2\b" tools/*.js | grep -iE "off|pass|mul"` 로 훑었다. 남은 `1.2` 는 셋 다 **다른 것**이다:
`verify151:77`·`sim112:114` 는 오프라인 **골드 공식**의 `×1.2×0.5`(이용권 배율이 아니다) ·
`verify758:240,243` 은 «옛 값 1.2 는 이 세대 과녁을 못 넘는다» 를 **일부러** 적어 둔 [P3] 전제 주석이다.
`1.381` 을 손으로 든 자는 `verify151`(세대 상수 `OFF_MUL_ON` — 151 규약상 **제품에서 안 읽는 것이 설계**)와
`verify758`(값을 정하는 자) 둘뿐이고, **둘 다 정당하다.** 588 이 유일한 무임승차였다.

## 6. 다음 세션에게

- 199 가 `PASS_OFF_MUL` 을 또 흔들어도 **588 은 이제 안 빨개진다.** 흔들면 빨개져야 하는 자는
  `verify758` [P1]·[P2] 와 `verify151` [C2] 이고, 그건 **의도된 자리**다(값의 주인).
- 곁다리 등재 **0건** — 이 실행에서 돌린 자 다섯이 전부 초록이었다.
