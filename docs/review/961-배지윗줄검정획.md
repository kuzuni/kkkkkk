# 961 — 10 이용권 배지 **윗줄** 검정 획 선언 8 → 10

> 지시서 [3]-**(가)** — 레퍼런스 대조가 아니라 «한 선언이 몇 px 인가» 라 채점할 것이 없다. **비평가 없음.**
> 제품 `index.html` **1자리**(선언 한 값) · 레이아웃 Δ0px · 밸런스·판정 0줄.

## 1회차 (2026-09-06, sess-0213-9050 루틴 워커 A) — **완료**

### ⓐ 무엇을 물려받았는가

895 1회차가 배지 두 줄의 잉크를 자로 재기 시작했고 **아랫줄은 2회차에 닫혔다**(fs 31 · 획 7).
**윗줄 획만** 열린 채였는데, 열려 있던 이유가 «결손이 없어서» 가 아니라 **«자가 갈려서»** 였다:

| 자 | 계열 | ref ↔ 우리 | 잔차 |
|---|---|---|---|
| GH(비평) | 교차점(서브픽셀 옆면) | 6.54 ↔ 6.58 | **0% «맞다»** |
| GI(비평) | 교차점(g=126 교차) | 4.7~5.2 ↔ 4.0~4.2 | −15% |
| `scan895.stroke_thk`(ⓐ) | 교차점(R_MID 보간) | 5.393 ↔ 4.299 | **−20.3%** |
| `scan932.mass_on`(ⓑ) | **질량 적분** | 5.197 ↔ 4.299 | **−17.3%** |

앞 셋이 전부 «교차점» 계열이라 서로를 못 가렸고(932 2회차 ⓕ-2), 932 8회차가 **계열이 다른 넷째 자**를
**같은 광선·같은 표본** 위에 얹어 GH 의 «맞다» 를 기각했다. [1-k] 의 환산비 **0.534**(아랫줄 3.73 ⇒ 선언 6.99 ≈ 7)로
옮기면 ⓐ **10.10** · ⓑ **9.73** — **둘 다 10**(현행 8). 932 는 «선언은 못 옮긴다»(`verify833` [1-k] 이관이 붙는다)로
자기 회차의 몫을 갈라 이 번호로 넘겼다.

### ⓑ ⚑⚑ 계산값 10 을 그대로 안 썼다 — 선언을 굴려 화소로 쟀다 (338 규칙)

인계된 10 은 «환산비를 곱한 수» 다. 338 규칙(«처방을 따르기 전에 재현한다»)대로
`tools/probe961.js` 를 신설해 선언 **8·9·10·11·12** 를 실제로 그려 같은 두 자로 쟀다:

| 선언 | ⓐ 교차점 Δ | ⓑ 질량 Δ | 두 줄 빈 띠 Δ | 노랑 AABB Δ | 아랫줄 획 Δ |
|---|---|---|---|---|---|
| 8 (현행) | −20.3% | −17.3% | −3.0% | +1.4% | −1.3% |
| 9 | −10.2% | −6.8% | −3.0% | +1.4% | −1.3% |
| **10** | **−0.3%** | **+2.8%** | **−3.0%** | **+1.4%** | **−1.3%** |
| 11 | +9.1% | +12.3% | −3.0% | +1.4% | −1.3% |
| 12 | +18.4% | +22.6% | −3.0% | +1.4% | −1.3% |

⇒ **두 자가 같은 칸(10)을 골랐다.** 계산과 그림이 같은 답을 냈고, 한 칸 이웃(9·11)은 양쪽 다 두 자리수 잔차다 —
**«10» 은 반올림의 결과가 아니라 봉우리 자체**다. 스윕은 ref 값을 다섯 칸 내내 고정으로 냈다(`probe961` [1]) —
자가 우리 그림에 안 물든다는 뜻이고, 그게 없으면 위 표는 비교가 아니다.

### ⓒ ⚑⚑ 등재문의 걱정 ②(§C 가 따라 움직이는가)의 답 — **«작다» 가 아니라 «구조적으로 없다»**

등재문은 «획이 두꺼워지면 두 줄 «빈 띠»(§C1 ±8%)·노랑 AABB 높이(§C2 ±4%)가 따라 움직이는지 다시 재야 한다» 로
이 작업의 두 번째 몫을 적어 뒀다. 실측은 **다섯 칸에서 값이 한 자도 안 움직인다**(빈 띠 9.000 · AABB 92.000 고정).

뿌리는 둘이 맞물려 있다 — ① `paint-order:stroke fill` 이라 획은 노랑 채움 **뒤**에 깔리고,
② §C 의 두 축은 `scan895` 의 **노랑 마스크**로 잰다. ⇒ 선언이 무엇이든 같은 값이 나온다.

⚠ 그래서 `verify961` §3 은 «대역 안» 만 묻지 않는다 — **[3-d] 가 «선언 8 주입본과 한 자도 안 다르다»** 를 같이 묻고,
**[R4] 가 11 에서도 같은 값임**을 확인한다. 대역만 물었으면 «획을 키웠는데 마침 대역 안이었다» 와
«이 축에 애초에 닿지 않는다» 가 안 갈린다.

### ⓓ ⚑ 그러나 §C 가 **못 보는** 대가가 하나 있다 — 검정이 빈 띠를 채우는 몫

노랑 자는 두 줄 사이의 **검정**을 못 본다. 획을 키우면 두 줄의 바깥 획이 그 띠를 더 채우고,
합이 띠를 넘으면 두 줄의 검정이 **붙는다**. 그래서 그 몫을 ref 와 나란히 쟀다(`verify961` [3-c]):

| | 윗줄 바깥 획 | 아랫줄 바깥 획 | 합 | 빈 띠 | 채우는 몫 |
|---|---|---|---|---|---|
| ref | 5.393 | 3.729 | 9.122 | 9.283 | **0.98** |
| 우리(선언 8) | 4.299 | 3.680 | 7.979 | 9.000 | 0.89 |
| **우리(선언 10)** | 5.374 | 3.680 | 9.054 | 9.000 | **1.01** |

⇒ **ref 도 거의 붙어 있다**(0.98). 목표는 «0 으로 만들기» 가 아니라 **ref 와 같은 정도**이고,
수리 뒤 차는 **0.03** 이다(수리 전 0.09 — 즉 이 축도 ref 쪽으로 갔다). 대역은 ±0.10 으로 잡았다.

### ⓔ 이관 (333 처방 — 자리를 비우지 않고 값·방향을 갈아 끼웠다)

* **`verify833` [1-k]** — «검정 획 선언 **8**/7 불변» → «**10**/7».
  ⚠ 이 항의 주석이 말하던 «8/7 그대로가 맞다» 는 **아랫줄** 판정이었다(1회차의 7 → 5 과교정을 되돌린 것).
  **윗줄 8 은 «자 셋이 갈려서 아직 못 잰 값»** 이었고, 이번에 잰 값으로 갈아 끼웠다(885 4회차 [1-l] 선례 —
  «자리 잡기 전의 임시값» 을 «잰 값» 으로 바꾸는 것은 방향을 뒤집는 것이 아니다). **281/281 PASS.**
* **`verify895` [B3]** — «윗줄 획 잔차가 **−8~−30% 에 그대로 있다**» → «**ref ±5% 안**».
  이 항은 895 2회차가 **양성으로** 등재한 것이다(«누가 조용히 옮기면 짖는다»). 961 이 밝히고 옮겼으므로
  이제 **닫힌 축을 지킨다** — 8 로 되돌리면 −20.3%, 9 면 −10.2%, 11 이면 +9.1% 로 대역 밖이다.
* **`verify895` [B5]** — ⓑ 대역 «−14~−21%» → «**−4~+8%**».
  ⚠ **−17.3% 는 961 의 «근거» 였지 «지켜야 할 수» 가 아니다** — 대역을 그대로 뒀으면 이 항이
  **수리 자체를 되돌리라고 짖는다**. 짝 항(«ⓑ 가 ⓐ 를 되뇌지 않는다» = ref 에서 둘이 0.1 이상 갈린다)은
  선언과 무관하므로 **한 글자도 안 건드렸다**.
* **`verify895` [B6]** — **무수정.** 이 항은 **ref 만 읽어** 두 자의 환산이 한 칸(10)에 모이는지를 묻는다.
  선언이 움직여도 초록이어야 하고 실제로 초록이다(ⓐ 10.10 · ⓑ 9.73) — 961 등재문이 미리 적어 둔 그대로다.
  말꼬리(«현행 8 — 961 등재»)만 사실에 맞게 고쳤다.

### ⓕ 게이트

* `tools/probe961.js` **9/9 신설** — 선언 5칸 스윕(위 표) · ref 고정점 · 단조성 · 두 자의 최선 칸 · §C 불변 · 아랫줄 독립.
* `tools/verify961.js` **20/20 신설** — §1 선언(소스 + **계산값** — 이 배지는 ID 급 특이성에 선언을 두 번 삼킨 자리다) ·
  §2 그려진 획(두 자를 **같은 광선**에서) · §3 대가(«구조적으로 없다» + 노랑 자가 못 보는 [3-c]) ·
  §4 아랫줄 독립 · **§R 되돌림 시험 4항**(8 · 이웃 9 · 반대편 11 · [3-d] 의 되돌림).
* 이관 회귀: `verify833` **281/281** · `verify895` **21/21** · `verify932` **63/63**.
* 회귀 무수정: `verify887` 7/7 · `verify923` 121/121 · `verify920` 6/6 · **SMOKE PASS**.
* ⚑⚑ **회귀 목록에 `verify934` 가 빠져 있었다(972, 2026-09-06 추가).** 이 회차 뒤 그 자가
  **10/18 로 빨갰고** 아무도 몰랐다 — 934 가 «획 8» 시절의 지문을 결함 근거로 굳혀 두었기 때문이다.
  **961 은 잘못한 게 없다**(972 가 확인: 획 10 이 가둠 정도 19.2% 로 ref 20.7% 에 **더 가깝다** —
  획 8 은 68.0% 로 ref 체제 밖이었다). 부패의 경로는 «회귀 목록에 그 자가 없었다» 한 줄이다.
  ⇒ **이 배지의 두 줄(`.pvc>.bdg>i`·`>b`)을 만지는 회차는 `verify934` 를 반드시 같이 돌린다.**
  갈아 끼운 자는 `verify934` **22/22**(§S 가 획 8 을 그 페이지에만 되돌려 이 회차의 값을 재현한다).

### ⓖ 남은 것

**없다 — 이 번호는 마감이다.** 곁다리 등재 **0건**.

⚑ **다음 세션에게** — 932 8회차가 곁다리로 적어 둔 «아랫줄에서도 ⓐ −1.3% ↔ ⓑ +5.0% 로 **6.3%p 자 차이**가 깔려 있다»
는 이 회차에서도 그대로다(선언 7 은 두 자의 대역 안이라 안 건드렸다). 아랫줄을 다시 잡는 세션은
**선언을 옮기기 전에 그 6.3%p 부터** 봐라 — 윗줄에서 그 갈림을 닫은 것이 이 번호의 방법이다(계열이 다른 자를 같은 광선에).
