# 576 — 팝업·시트 스크림(딤) α 규격

> 등재: 2026-08-31 (번호 개정 573 → 576, sess-0322-17083 워커 F — 518 5회차 비평가 CZ·DA 2인 공통 지적)
> 작업: 2026-08-31, `sess-0421-14046` 루틴 워커 F · **1회차**
> 등재 티어: T3 「규격 불일치」 · 실제 결과: **제품 0줄(동작) · 결함은 «문서» 였다**

## 0. 한 줄

**등재문의 가설이 재현으로 기각됐다** — α 가 화면마다 다른 것은 규격 불일치가 아니라
**껍데기가 셋**인 것이고, 22자리 전부 «그 화면 자기 측정표» 의 값이다(Δ 0.000).
실재한 결함은 그 옆에 있었다: **A5 측정표만 `.65` 에 머물러**, 제품(`.54`)을 «틀린 값» 으로
읽게 만드는 **오검출의 씨앗**이 세 문서 · 한 주석에 남아 있었다.

## 1. 등재문이 세운 것과, 재현이 돌려준 것

등재문의 처방 후보는 둘이었다.

- ⓐ 측정표에서 ref 딤을 재확인해 **한 상수**로 모으고 예외를 이름으로 적는다
- ⓑ 시트만 예외라면 그 이유를 규약으로 못박고 게이트에 음성항을 세운다

둘 다 «흩어진 값 = 결함» 을 전제한다. 338 규칙대로 **처방 전에 그 전제부터 쟀다**
(`tools/probe576.js` 신설 — 판정 없음, 측정 전용).

| 축 | 무엇을 쟀나 | 결과 |
| --- | --- | --- |
| ⓢ 정적 | 제품에 선언된 전면 스크림 α 전수 | 18줄 · 22자리 |
| ⓜ 문서 | 그 화면 **자기** 측정표가 적은 α | 22자리 전부 존재 |
| ⓟ 픽셀 | 딤 끄고/켜고 같은 지점의 투과배율 → α = 1 − after/before | 22자리 |
| ⓒ 비평 | 등재문이 쓴 자 «스크림 밖 최대 채널값» 재현 | 4/4 **±0** |

**ⓢ = ⓜ 가 22/22, Δ 0.000.** ⓟ 도 같은 값을 돌려준다(오차 ≤ 0.006).

```
스크림           종류  선언    측정표   픽셀     Δ(선언−측정표)
#modal          가    0.540  0.540  0.544   +0.000 ✓
#modal.sk8      가    0.550  0.550  0.552   +0.000 ✓
#modal.q22      가    0.560  0.560  0.566   +0.000 ✓
#modal.ml69     가    0.560  0.560  0.565   +0.000 ✓
#modal.at70     가    0.650  0.650  0.655   +0.000 ✓
#dgdw           가    0.500  0.500  0.500   +0.000 ✓
#wpnw           가    0.550  0.550  0.552   +0.000 ✓
#prbw           가    0.550  0.550  0.552   +0.000 ✓
#collw          가    0.530  0.530  0.533   +0.000 ✓
#ciw            가    0.540  0.540  0.544   +0.000 ✓
#specw          가    0.550  0.550  0.552   +0.000 ✓
#blsw           가    0.540  0.540  0.545   +0.000 ✓
#bagw           가    0.545  0.545  0.547   +0.000 ✓
#cfw            가    0.550  0.550  0.552   +0.000 ✓
#eqw>.dim       나    0.280  0.280  0.283   +0.000 ✓
#panel::before  나    0.280  0.280  0.283   +0.000 ✓
.tr-dim         나    0.340  0.340  0.345   +0.000 ✓
#offw           연출  0.800  0.800  0.802   +0.000 ✓
#upw            연출  0.800  0.800  0.802   +0.000 ✓
#sumw           연출  0.800  0.800  0.802   +0.000 ✓
#dclw           연출  0.780  0.780  0.783   +0.000 ✓
#defw           연출  0.620  0.620  0.622   +0.000 ✓
→ 선언 α 가 «자기 측정표» 와 어긋나는 자리: 0 / 22
```

⇒ **처방 ⓐ 는 오답이다.** 한 값으로 모으면 **16자리의 레퍼런스 실측을 한꺼번에 버린다.**
⇒ **처방 ⓑ 는 반만 맞다.** 예외는 «시트» 하나가 아니라 **껍데기가 셋**이다
(A5 측정표가 이미 (가)·(나)·(다) 로 갈라 적어 뒀고, 여기에 **결과 연출** 계열이 하나 더 있다).

## 2. 비평가의 네 수치는 옳았다 — 옳은 자로 잰 정상값이었다

518 5회차 비평가 CZ·DA 가 2인 독립으로 일치해 적은 «스크림 밖 최대 채널값»
**51 / 51 / 120 / 184** 를 `probe576` ⓒ 가 **네 개 다 ±0 으로** 재현했다.

| 자리 | α | 등재문 | 재현 | 차 |
| --- | --- | --- | --- | --- |
| 12 소환 결과 `#sumw` | 0.80 | 51 | 51 | +0 |
| 09 일괄 강화 `#upw` | 0.80 | 51 | 51 | +0 |
| 21 도감 `#collw` | 0.53 | 120 | 120 | +0 |
| 07 스킬 시트 `#panel::before` | 0.28 | 184 | 184 | +0 |

그리고 **같은 α 를 쓰는 자리끼리는 그 값이 폭 0 으로 같다**(같은 배경 위에서 잰다면):

```
α=0.280 인 2자리 → 184 / 184  (폭 0)   #eqw>.dim, #panel::before
α=0.540 인 3자리 → 117 / 117 / 117  (폭 0)   #modal, #ciw, #blsw
α=0.550 인 5자리 → 115 ×5  (폭 0)   #modal.sk8, #wpnw, #prbw, #specw, #cfw
α=0.560 인 2자리 → 112 / 112  (폭 0)   #modal.q22, #modal.ml69
α=0.800 인 3자리 → 51 / 51 / 51  (폭 0)   #offw, #upw, #sumw
```

⇒ 비평가의 자는 정확했고, 그 자가 읽은 «3.6배 차이» 는 **결함이 아니라 껍데기 차이**다.
07 시트가 밝은 것은 06 장비 시트와 **같은 값**이고, 그 값은 07 측정표가 «투과배율 0.721» 로 잰 것이다.

## 3. 그런데 결함은 실재했다 — 자리가 제품이 아니라 문서였다

`#modal` = `.54` 인데 **A5 측정표는 `65% (±4)`** 라고 적고 있었다. 어느 쪽이 옳은지는
**A5 자신의 1회차 채점 기록**이 이미 판정해 놓았다(`docs/review/A5-모달.md` 끝):

> **딤 불투명도**: 측정 에이전트는 네비바 픽셀 비교로 .65, 비평가 2명은 흰 텍스트 관통값으로 .54.
> 2:1 로 **.54** 채택 (흰색 관통 쪽이 감마 영향이 적다).

`docs/measure/22-퀘스트팝업.md`(A5 의 **원본 레퍼런스**)도 같은 자리를 «0.56 … 우리 ~0.54 ✔ 일치» 로
교차 확인해 뒀다. **제품은 내내 옳았고 측정표만 안 따라온 것**이다(368 과 같은 꼴).

그 유령이 남긴 자국이 셋 더 있었다 — 전부 «A5 는 65%» 를 인용한 자리다.

| 자리 | 있던 글 | 고친 글 |
| --- | --- | --- |
| `docs/measure/A5-모달.md` 딤 표 | 불투명도 **65% (±4)** | **54%** + §정오표 |
| `docs/measure/A5-모달.md` 구현 매핑 | `#modal{…rgba(0,0,0,.65)}` | `…,.54)` |
| `index.html` `#modal.sk8` 주석 | 딤 **65%→55%** | 딤 **54%→55%** + 정오 5줄 |
| `docs/measure/08-스킬세부팝업.md` ×3 | 딤 \| 65% \| 55% \| ❌ 차이 / «A5 는 65%» / «A5 기본 .65» | 54% (A5) · 🔸 −1%p + 정오 |
| `docs/measure/09-일괄강화결과팝업.md` | 08(55%)·**A5(65%)**·05(55%) | A5(**54%**) + 정오 |

⚑ **여기서 값 판단이 한 칸 바뀐다** — 08 이 A5 와 «다른 값을 쓴다» 는 판단은 그대로 옳지만,
**차이는 −10%p 가 아니라 −1%p** 다. 08 이 그것을 «❌ **차이**» 로 적어 둔 것은 과했다(🔸 로 내렸다).

## 4. 제품 변경

**동작 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** `index.html` 에서 바뀐 것은 **주석 한 블록**이다
(`#modal.sk8` 머리 주석의 «65%» → «54%» + 정오 5줄). 선언값은 **한 자리도 안 건드렸다** —
22자리 전부 자기 레퍼런스와 맞기 때문이다.

## 5. 게이트 — `tools/verify576.js` **49/49 PASS** (신설)

| 절 | 무엇을 지키나 |
| --- | --- |
| [A] α 표 (22항) | 자리마다 **자기 측정표** 값을 그대로 쓰는가. 근거 칸에 그 측정표 경로를 적었다 — 값을 바꾸려면 측정표를 먼저 고쳐라 |
| [B] 껍데기 띠 (5항) | (나) 최대 < (가) 최소 · (연출) 최소 > (가) 본류 최대 · 계열 안 폭 |
| [C] 음성항 (4항) | 시트가 다이얼로그 깊이로 올라오지 않는가 · 다이얼로그가 시트 깊이로 내려가지 않는가 · **α 가 한 상수로 뭉개지지 않았는가(12종 ≥ 8)** |
| [D] 문서 동기 (7항) | **측정표가 제품과 같은 값을 적는가** — 576 이 고친 결함이 되살아나면 여기가 빨개진다 |
| [E] (다) 전체화면 (7항) | `#dunw`·`#relw`·`#shopw`·`#rkw`·`#psw`·`#chw`·`#svw` 에 검정 스크림 선언이 **없는가** |
| §R 되돌림 (4항) | 등재문의 처방 ⓐ 를 **실제로 먹인 사본**이 빨개지는가 |

**§R 이 이 자의 값을 못박는다** — 세 가지 변형을 사본에 먹여 판정이 실제로 빨개지는지 본다:

- ⓐ-1 **전 자리를 `.54` 로 통일** → 3항 실패
- ⓐ-2 `#modal` 을 **유령값 `.65`** 로 되돌림 → 1항 실패
- ⓐ-3 07·26·50 **시트 딤을 `.54`** 로 → 2항 실패
- §R-n **원본은 세 판정 모두 초록** — 되돌림 시험이 «항상 빨간 자» 가 아님을 같이 못박는다

## 6. 회귀

`verify413` 13/13 · `verify518` 38/38 · `verify363` 80/80 · `verify350` 44/44 ·
`verify299` 27/27 · `verify351` 86/86 — **전부 무수정 초록** · **SMOKE PASS**.

## 7. 프로브를 짜며 접은 함정 넷 (다음 세션이 그대로 밟을 자리)

1. **켜기 전에 표본 자리를 고르면 안 된다.** 스크림은 `display:none` 이라 rect 가 0 이고,
   대화상자가 어디 놓이는지도 모른다 — 1회차에 21자리가 «표본 자리 없음» 이었다.
2. **60 쥬시 열림 연출이 끝나기 전에 찍으면 α 가 반만 나온다.** 60ms 뒤에 찍었더니
   `#modal` 이 **0.056**(참값 0.541)으로 읽혔다. 420ms 로 올려야 값이 선다.
3. **격자를 촘촘히 깔면 상한에 걸려 표본이 맨 위 24px 띠에 몰린다.** 성글게(40px) **전 구역**을 훑어라.
4. **자기 자손만 빼면 안 된다** — `#eqw>.dim` 은 **형제**(`.eqp` 크림 시트)가 자기 위에 얹혀
   α 가 **−0.793** 으로 나왔다. 스코프를 껍데기 전체로 올려야 한다.

그리고 `#panel` 은 **inline `display:none`** 으로 닫혀 있다(탭 스크립트가 켠다) —
클래스만 얹어서는 07·26·50 시트를 헤드리스로 못 연다.
