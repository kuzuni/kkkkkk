# 55 설정 팝업 — 회차 기록

> 측정표 `docs/measure/55-설정팝업.md` · 캡처 `docs/review/55-r*.png`
> 스크립트 `tools/scan55.py`(레퍼런스·캡처 공용 스캐너, `IMG=` 로 대상 지정) · `tools/cap55.js`(캡처) ·
> `tools/verify55.js`(기하 + 기능 게이트 → `VERIFY55 58/58 PASS`)
> 담당 세션: **sess-0758-2927 (워커 D, 2026-08-25)** — T3, 이전 기록 없음(이 세션이 처음 구현).

## 껍데기 판정

**(가) 딤(α .55) 위 가운데 다이얼로그 798×1347.** 03·10·14 식 전체화면 페이지도, 06·07 식 바닥 시트도 아니다.
53 가방과 **껍데기 규격이 같다**(검정 8 + 갈색 림 12 + r44 + 헤더 91 + 크림 본문) — 폭·높이만 다르다.
공용 `#modal` 은 폭 898 고정이라 798 이 안 나와서 **자체 오버레이 `#cfw`** 로 갔다(19 `#pfw` · 33 `#ciw` · 53 `#bagw` 와 같은 판단).

**레퍼런스에 ✕ 가 없다.** 캡처 우상단의 ✕(ref x950..1022 y278..345)는 **팝업 뒤 «메뉴 열림» 배경의 것**이고
딤 «아래» 라 흰 픽셀이 255 가 아니라 116~119 로 찍힌다. 닫기는 **딤 탭**이다(70 출석 팝업과 같은 판단).

## 1회차 (sess-0758-2927)

레퍼런스를 PIL 로 직접 스캔해 측정표를 만들고, 6겹(딤 / 팝업 / 구분선 / 옵션 리스트 / 계정 패널 / 버튼·푸터)을 올렸다.

**구현**
- `#cfw`(딤 .55) → `.cf55` 798×1347(검정 8 + 갈색 림 12 + r44) → `.cf55-head` 91 → `.cf55-body` 크림 758×1228
- 볼륨 그룹: 소제목 · 슬라이더(트랙 679×50 + 노브 60×50) · `0` / `MAX` 끝 라벨 · 구분선 699×4
- 옵션 리스트 699×509, **교대 틴트 6행**(홀 `#D0BCA1` / 짝 `#E2CDAF`, 모서리 각) — 언어 · BGM · SFX · 화면 흔들림 · 푸시 알림 · 야간 알림 수신 동의
- 계정 패널 699×198 r45 + 「계정 연동」 + 흰 카드 248×78(구글 배지)
- 하단 버튼 3칸 217×99(청록 1 + 금색 2) · 「계정 삭제」 밑줄 링크 · UID/Server · Gamer Id

**자체 실측 결과(2차 보정 후)** — 아래 «캡처 실측 대조» 표. 잉크 bbox 기준 대부분 **Δ≤3px**.

### 이 회차에 잡은 함정 4개

1. **`#cfw i` 리셋(1-0-1)이 `.cf55-lb>i`(0-1-1)의 `transform-origin` 을 이긴다**(LESSONS 21-①의 재발).
   좌단 고정 라벨의 origin 이 `50% 50%` 로 남아 scaleX 를 걸 때마다 잉크가 **+6.5px 오른쪽**으로 밀렸다.
   `#cfw .cf55-lb>i` 로 특이성을 맞춰 x300 이 고정됐다.
2. **토글 노브의 기준은 트랙의 border box 가 아니라 padding box 다**(LESSONS 03).
   트랙 `border:5px` 이라 `left:0` 은 685 가 아니라 **690** 이었다 — OFF 노브는 `left:-5px`, ON 노브는 `left:65px`.
3. **크림 본문의 `border-top` 두께가 곧 본문 좌표계의 원점**이다. 53 을 그대로 베껴 `2.5px` 로 뒀더니
   본문 절대배치 자식이 전부 **+1.5px** 내려갔다. `1px` 로 바꾸니 원점이 ref 639 와 정확히 일치한다.
4. **자체 스캔의 «표본 구간» 이 틀리면 자체 스캔도 틀린다**(LESSONS 52-①).
   「볼륨」 잉크를 재는 구간 아래끝이 슬라이더 트랙의 검정 테두리를 물어 **w52 를 w78 로** 읽었고,
   「한국어」는 값 버튼의 **r35 둥근 모서리 테두리**를 물어 w75 를 w141 로 읽었다. 둘 다 구간을 좁혀 해결.

### 캡처 실측 대조 (2차 보정 후, 잉크 bbox · ref 좌표)

| 요소 | 캡처 | 목표 | Δ |
|---|---|---|---|
| 타이틀 「설정」 | x493..588 (96) y567..626 | x491..587 (97) y566..624 | x+2 w−1 y+1 |
| 「볼륨」 | x239..289 (51) y659..690 | x234..285 (52) y657..689 | x+5→보정 w−1 |
| 「0」 | x216..233 (18) y761..784 | x216..234 (19) y762..784 | w−1 |
| 언어 라벨 | x300..349 (50) y850..882 | x300..354 (55) y850..881 | w−5 |
| 「한국어」 | x734..803 (70) y850..875 | x732..806 (75) y849..874 | w−5→보정 |
| BGM | x300..369 (70) y939..963 | x300..371 (72) y937..964 | w−2 |
| SFX | x298..359 (62) y1023..1047 | x300..359 (60) y1021..1048 | w+2 |
| 화면 흔들림 | x297..438 (142) y1105..1137 | x299..439 (141) y1104..1136 | w+1 |
| 푸시 알림 | x299..410 (112) y1190..1222 | x300..414 (115) y1190..1221 | w−3 |
| 야간 알림 수신 동의 | x300..538 (239) y1275..1307 | x300..538 (239) y1276..1308 | **Δ0** |
| 「계정 연동」 | x472..606 (135) y1388..1426 | x472..608 (137) y1385..1423 | w−2 y+3→보정 |
| 버튼1 잉크 | x221..395 (175) y1624..1644 | x222..396 (175) y1624..1644 | **Δ≤1** |
| ON / OFF 글자 | x788..820 / x712..756 | x789..820 / x714..755 | Δ≤2 |
| Gamer Id | x211..868 (658) y1826..1848 | x212..869 (658) y1828..1848 | **Δ≤2** |

## 기능 체크 표 (`node tools/verify55.js` → 58/58 PASS)

> 이 게임엔 아직 **오디오·푸시가 없다.** 그래서 BGM·SFX·푸시·야간은 «저장되는 사용자 설정» 까지가 실동작이고,
> 실제로 무언가를 끄는 것은 **화면 흔들림(67 카메라)** 과 **볼륨 값(오디오 게이트 `sndOn()`)** 이다.
> 소리가 들어오면 `sndOn(k)` 한 곳만 보면 되도록 게이트를 미리 뚫어 뒀다.

| 누르면 | 무엇이 바뀌나 | 확인 |
|---|---|---|
| ▦ 메뉴 → 설정 | `openConf()` — 팝업이 열린다(52 주석의 «분기 한 줄 교체» 지시대로 placeholder 제거) | ✅ |
| 토글(BGM/SFX/흔들림/푸시/야간) | `S.opt[k]` 반전 → 노브가 좌↔우로 이동 · 라벨 ON↔OFF · 트랙색 초록↔청회색 · **즉시 저장** | ✅ |
| 화면 흔들림 OFF | 진행 중인 `cam.shake` 까지 **그 프레임에 0** (호출부 8곳을 안 건드리는 단일 게이트) | ✅ |
| 볼륨 트랙 드래그 | `S.opt.vol` 0~100 · 채움 폭 · 노브 위치 추종 · 저장 (포인터 캡처로 트랙 밖까지) | ✅ |
| 볼륨 0 | `sndOn('bgm')` 이 false — 오디오가 들어오면 여기서 꺼진다 | ✅ |
| 언어 「한국어」 | 값이 바뀌지 않고 «한국어만 지원» 안내(지원 언어가 1개다) | ✅ |
| 개인정보 처리 방침 | 실제 저장 항목·전송 없음 안내 | ✅ |
| 쿠폰 | 코드 3종 → 💎 지급 + `S.opt.cp` 에 사용 이력 · **재사용 차단** · 잘못된 코드 차단 | ✅ |
| 고객 지원 | UID · Gamer Id · Server · 플레이 시간 표시 | ✅ |
| 계정 삭제 | confirm 후 `localStorage.removeItem(KEY)` + reload | ✅ |
| 딤 탭 | 닫힘(레퍼런스에 ✕ 가 없다) | ✅ |
| UID / Gamer Id | 닉네임에서 결정적으로 유도 — NaN·undefined 없음 | ✅ |

## 게이트

- `node tools/verify55.js` → **VERIFY55 58/58 PASS**
- `node tools/smoke.js` → **SMOKE PASS** (화면비 6종 전부 «시트 잘림 없음». `#cfw .cf55` 를 잘림 검사 목록에,
  `openConf()` 를 오프너에 추가했다 — 798×1347 은 지금까지 중 가장 키가 큰 가운데 다이얼로그다)

## 2회차 — 비평 대기
