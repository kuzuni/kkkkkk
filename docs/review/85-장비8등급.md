# 85 — 장비 8등급 체계 (마지막 등급 1종 · 그 외 등급당 5종)

- 2026-08-25, sess-1750-16579 (워커 D) · T2 기능(주인 지시 2026-08-26) · 1회차 완료
- 게이트: `node tools/verify85.js` → **VERIFY85 PASS 33/33** · `node tools/smoke.js` → **SMOKE PASS** · 콘솔 에러 0
- 검증 종류: [3]-(가) 기계적/기능 — 비평가 루프 없음(레퍼런스 대조 없는 데이터·시스템 작업)

## 무엇을 바꿨나 (지시 ①~⑦ 전부)

| 지시 | 구현 |
|---|---|
| ① GRADE 8단계 | 7 «초월»(c `#4fe3c4` · mul **16**, ic 🌀) · 8 «불멸»(c `#f0edd2` · mul **26**, ic ♾️) 신설. `isTopGrade` 를 **`topG(cat)`**(equip 7 / 그 외 5) 기준으로 교체 — 신화 스킬·펫·유물의 MAX 표기·합성 차단 유지 |
| ② EQ_NAMES 8×(5,5,5,5,5,5,5,1) | 기존 54종 id·이름·v 전부 보존(티어 배열 앞쪽 그대로), 부족분 뒤에 j 이어 붙임. 신설 54종(부위당 18) — 등급 테마명(초월 = 차원·시간·공허·무한·초신성, 불멸 = «만물의 종언/수호/근원»). v 0.90~1.15 분산 · ic 개별 이모지 |
| ③ 소환 확률 8행 | `GRADE_ROLL_EQ = GRADE_ROLL + [{unlock:55,p1:0.06},{unlock:75,p1:0.03}]`, `rollOf(b)` 가 **coll==='equip' 배너만** 8행 표 선택(스킬·펫 6행 유지). `gradeProbs`/`gradeProbsAt` 는 결과를 GRADE.length 로 0-패딩(11 팝업 «빈 등급 흘려보내기» 루프의 NaN 방지). 11 팝업 이정표 `PRB_STEPS_EQ = [1,5,15,30,40,55,75,100]` (배너별 `prbSteps()`) |
| ④ SUM_CARD 8색 | 초월 face `#1EA98E`/rim `#40DCC0` · 불멸 `#8F90AE`/`#D9DAEE` — 같은 «면+밝은 림» 규칙. 참조처(12 결과·69 우편 `SUM_CARD[top.g]`·05 `WGRADE`·53 `BAG_G`)도 8행으로 |
| ⑤ 합성 | `nextGradeItem` = **다음 등급 풀에서 랜덤**(7→8 은 풀 1종이라 자연 고정). `canCraft` 는 존재 여부(`!!`)만 보므로 판정↔지급 불일치 없음 — 75 의 «결정적 j 유지» 규칙은 주인 지시로 폐기 |
| ⑥ 도감 | `COLL.equip.tiers` need 12/24/33/42/48/54 → **24/48/66/84/96/108**(마지막 = 전 종 수집 유지). `S.coll.equip` 은 «받은 단계 수» 카운터라 구 세이브의 기수령 보너스 그대로 유효 |
| ⑦ README | 도감 표(각 36종·108종) · 등급 행(장비만 초월→불멸) · 확률 표 8행 · 무한 강화/무한 소모처 «신화→불멸» · 합성 규칙 갱신 |

부수: `REFUND` 8행(…1400, **3400, 8000**) — `refundAmount` 의 `REFUND[it.g]` NaN 방지.

## 기능 체크 표 (버튼별 «눌렀을 때 무엇이 바뀌는지» — verify85.js 가 헤드리스로 확인)

| 조작 | 결과 | 확인 |
|---|---|---|
| 무기 소환(Lv100) 3,000연 | g0~g7 전부 등장(불멸 81회 ≈2.7%) · 아이템 불량 0 · `S.own` 반영 | E1~E3 |
| 스킬 소환(Lv100) | 초월·불멸 확률 0(6행 표) — 신화까지의 기존 확률 유지 | D3 |
| weapon5(신화 Lv100+5개) 합성 버튼 | 초월 5종 중 랜덤 1개 획득(60회 표본 5종 전부 관측) | F1 |
| weapon6(초월 Lv100+5개) 합성 | `weapon7`(불멸) 고정 획득 | F2 |
| weapon7 상세 | 합성 버튼 없음(top) · **무한 강화**(maxLv ∞) | F3·C3 |
| 신화 스킬(holy) 상세 | «MAX» 유지 · 합성 불가(비장비 top=5) | C2·F3 |
| 11 확률 팝업 ◀▶ (무기, MAX) | 초월·불멸 행 표시 · NaN/undefined 0 · 스킬 팝업엔 없음 | G1~G3 |
| 05 부위 팝업 ▶ (2페이지) | 전설~불멸 4행 렌더(불멸 1칸+잠금 4칸) · NaN 0 | H1·H2 |
| 21 도감(장비 탭) | 마지막 티어 108종(전 종) · 렌더 무에러 | I1·I2 |
| 구 세이브(신화 lv120 무한강화분) 로드 | 판정·렌더 무에러 · MAX 취급 · 5개 모으면 초월 합성 가능 | J1·J2 |

## 설계 결정 기록 (지시가 «담당이 정하고 기록» 한 것)

- 등급명 **초월(🌀)·불멸(♾️)**, mul **16·26**(6→10 의 ×1.67 비율 유지), 소환 해금 **Lv55·75**, p1 **6%·3%**(재정규화 전 가중치 — Lv100 실측 5.5%/2.75%).
- REFUND 3400·8000(×2.4~2.5 기하급수 유지).
- 색: 초월 = 청록 계열(기존 6색과 색상환 겹침 없음), 불멸 = 연한 은백 계열. GRADE.c/bg · SUM_CARD · WGRADE(05 c/r/e/p 4톤) · BAG_G(53) 전부 같은 톤 패밀리로 파생.

## 다음 세션 참고

- 신설 54종 ic 는 임시 이모지 — 아트 입고 시 EQ_NAMES 의 `ic` 만 교체하면 된다(별도 «아트 필요» 표는 화면 작업이 아니라 생략, 목록 = EQ_NAMES 신설분).
- 86(스킬 24종)이 도감 `COLL.skill.tiers` 를 재배분할 때 이 회차의 equip 재배분(마지막 = 전 종)과 같은 규칙을 쓰면 된다.
- 91(도감 시스템 교체)이 들어오면 ⑥ 의 need 재배분은 통째로 대체된다.
