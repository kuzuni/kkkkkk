# 277 — `tools/verify88.js` [A][B] 2건 FAIL 「폐기 식별자 `trSub`·`data-trsub` 부활」 게이트 부패

> T1 버그(게이트) · 【⚑ 우선】 · 지시서 [3]-**(가) 기계적 작업 — 비평가 없음**
> 워커 B · sess-2340-5535 · 2026-08-27 · **1회차에 종결**
> `index.html` **0줄** — 제품이 옳고 게이트가 낡았다(242·245~247·275·276 계열).

---

## 1회차 — 진단

### 재현

손대기 전 트리에서 그대로 재현. 등재문의 «18/20, [A][B] 2건» 과 일치한다.

```
FAIL [A] 폐기 식별자 0건 (주석 제외) — 잔존: trSub, data-trsub
FAIL [B] 서브탭(스탯 훈련) 요소 없음
VERIFY88 FAIL — 18/20
```

### 등재문의 두 갈래 중 **②** 가 맞았다

등재문은 착수 전 확인 항목으로 «① `trSub` 가 스탯 훈련의 잔재인가 ② 203/210 이 새로 쓰는 이름인가»
를 걸어 뒀다. 소스를 그대로 읽어 **②** 로 판정했다.

| 자리 | 지금 무엇인가 | 누가 |
|---|---|---|
| `index.html:12756` `<div class="stabs sp3 tr-subs" id="trSubs">` | 23 훈련 팝업 바닥의 **«훈련 · 룬 · 단련» 3칸 바** | 203(훈련·룬) → 210(+단련) |
| `data-trsub="train"/"rune"/"temper"` | 그 3칸의 키 | 〃 |
| `let trSub = 'train'` · `const TRSUBS = ['train','rune','temper']` · `setTrSub()` | 그 바의 상태·전환 | 〃 |

`docs/review/203-룬시스템.md` §51 «`#trSubs`·`.tr-subs` CSS + `setTrSub` 신설» ·
`docs/review/210-단련시스템.md` §56 «`.sp2` → **`.sp3`** + 3번째 칸 «단련»» 이 둘 다 **저장소 주인 지시**로
등재돼 있다. 88 이 지운 «훈련 | 스탯» 두 칸짜리 바(`#trSub` · `.tr-sub` · `trIsTrain`)는 **지금도 없다**.

> **즉 88 이 막으려던 것과 다른 물건이 같은 이름을 쓰고 있었다 — 게이트가 틀렸다**(185-①: 손으로 박은
> 금지 목록은 «기능» 이 아니라 «그때의 이름» 을 감시한다).

### 기계적 원인 — 부분문자열 대조

`[A]` 는 `code.includes(t)` 였다. `trSub` 은 `trSubs`·`setTrSub` 안에, `data-trsub` 은 마크업 3칸에
그대로 들어 있으므로, **203 이 올라간 순간부터 원리적으로 영영 빨간** 단언이었다.
`[B]` 의 `document.querySelector('#trSub, [data-trsub], .tr-sub')` 도 마찬가지로 **항상 참**이다.

---

## 2. 고친 것 — `tools/verify88.js` 만, `index.html` 0줄

### ★ 빨간 단언을 지우지 않았다 — 물음을 **이사**시켰다 (185-④)

가장 쉬운 «고침» 은 두 단언을 빼는 것이다. 그러면 초록은 되지만 **88 이 세운 감시가 통째로 사라진다** —
나중에 «스탯 훈련» 이 되살아나도 아무도 못 본다. 그래서 «이름이 있나» → «**스탯 칸**이 있나» 로 옮겼다.

| | 옛 물음 | 새 물음 |
|---|---|---|
| **[A]** | 금지어 26개(= `trSub`·`data-trsub` 포함) 부분문자열 0건 | 금지어 **24개**를 **식별자 경계**로 0건 (203/210 의 이름 2개 제외) |
| **[A2]** | — (신설) | 88 의 바(id `trSub` / class `tr-sub`)도 `stat` 칸도 0건 · **전제**: 마크업 칸 ⊆ `TRSUBS` |
| **[A3]** | — (신설) | 매처 자가검사 — 금지어 24개를 **실제로 잡고**, 203/210 의 5줄은 **안 잡는다** |
| **[B]** | `[data-trsub]` 요소 0개 | **스탯 칸·스탯 분배 UI** 0개 · **전제**: 화면 칸 == `TRSUBS` |

세부 세 가지:

1. **경계 대조.** `(?<![A-Za-z0-9_$])토큰(?![A-Za-z0-9_$])` — 앞 경계는 `.` 을 **허용**한다
   (`S.pexp` 의 `pexp`, `S.upTab` 의 `upTab` 을 놓치면 안 된다). 이러면 `setTrSub`·`trSubs` 같은
   남의 이름이 금지어를 부분문자열로 품어도 안 걸린다 — 이번 오진의 기계적 원인이 닫힌다.
2. **«0건» 은 매처가 죽어도 초록이다**(185-② 의 반대 방향). 그래서 [A3] 을 신설해 **양방향**으로 건다 —
   금지어 자신을 넣으면 잡히는가 · 203/210 의 실제 소스 5줄을 넣으면 안 잡히는가.
   두 번째가 곧 **277 회귀 방지**다.
3. **전제를 결론과 갈랐다**(185-③). `TRSUBS` 파싱이 빗나가면 `[A2]` 의 결론(«stat 칸 0건»)은
   빈 배열로 **조용히 초록**이 된다 → «마크업 칸 ⊆ TRSUBS»·«화면 칸 == TRSUBS» 를 전제로 따로 박아,
   바 자체가 딴것으로 바뀌면 **전제 쪽이** 빨개진다.

### 함정 1건 — 소스 검사가 **속성 표기**를 놓쳤다

[A2] 를 처음에는 선택자 표기(`#trSub` · `.tr-sub`)로만 짰다. 반증 시험 ③(88 의 바 부활)을 돌리자
**[B] 만 빨개지고 [A2] 는 초록**이었다 — 마크업에는 `id="trSub"` · `class="tr-sub"` 로 적히지
`#trSub` 로 적히지 않기 때문이다. id 값 전체 대조 + class **토큰** 대조를 같이 넣어 닫았다
(`tr-subs` 가 `tr-sub` 를 부분문자열로 품으므로 토큰으로 갈라야 한다).
**반증 시험이 없었으면 «반쪽만 무는 게이트» 로 굳었을 자리다.**

---

## 3. 검증

### 되돌림 시험 — `tools/neg277.js` **7/7 PASS** (신설)

«단언을 지워서 초록이 된 것 아닌가» 를 스스로 반증한다. `index.html` 을 일부러 되돌려 놓고
**진짜 게이트(`node tools/verify88.js`)를 6번 구동**해 해당 줄이 FAIL 로 바뀌는지 본다(로직 복제가 아니다).

| 케이스 | [A] | [A2] | [B] |
|---|---|---|---|
| ① 무변경 (현재 제품 — 203/210 의 3칸 바가 그대로) | ✓ | ✓ | ✓ |
| ② «스탯» 칸 부활 — `data-trsub="stat"` + `TRSUBS` 에 `stat` | ✓ | **✗** | **✗** |
| ③ 88 의 바 부활 — `id="trSub"` · `class="tr-sub"` (속성 표기) | ✓ | **✗** | **✗** |
| ④ 훈련 팝업에 스탯 분배 UI 부활 — `[data-sp]` | ✓ | ✓ | **✗** |
| ⑤ 폐기 식별자 부활 — `S.spAtk` · `S.pexp` · `S.upTab` | **✗** | ✓ | ✓ |
| ⑥ 203/210 계열 칸을 하나 더(`data-trsub="rune2"`) | ✓ | ✓ | ✓ |
| ⑦ `index.html` 원상 복구 — `git diff` 비어 있음 | — | — | — |

**세 축이 서로 안 샌다**: 식별자 회귀는 [A] 만, 스탯 칸은 [A2]+[B], 분배 UI 는 [B] 만 빨개진다.
**⑥ 이 이번 버그 자체의 회귀 시험**이다 — 옛 게이트라면 203/210 계열 칸이 늘어날 때마다 또 빨개졌다.
⑦ 은 복구를 게이트가 아니라 **git 에게 물어** 검산한다(`try/finally` + `git diff --stat`).

### 게이트

- **`node tools/verify88.js` → VERIFY88 PASS 25/25** (18/20 → 25/25 · 항목 20 → 25)
  — **3연속 PASS** 로 flake 확인(브라우저를 띄우는 절이라 1회 PASS 는 근거가 아니다, 185-⑥).
- `node tools/neg277.js` → **NEG277 7/7 ✓ PASS** (신설)
- **`node tools/smoke.js` → SMOKE PASS**
- `[M] 콘솔 에러 0건` 그대로 통과.

---

## 4. 남은 것 · 곁가지 등재

`index.html` 을 한 줄도 안 고쳤으므로 제품 회귀 위험 0.

**곁가지로 같은 부패를 하나 더 찾아 등재했다 → 작업 279.**
`tools/verify47.js` §[0] «옛 대상(23 훈련 서브탭) 폐기 확인» 이 **정확히 같은 낡은 전제**를 들고 있다:

```
FAIL [data-trsub] 0개 — 3개
FAIL .tr-sub CSS 규칙 0건 — true      ← 정규식 /\.tr-sub/ 가 203 의 `.tr-subs` 를 문다
```

여기서 고치지 않았다 — 지시서 [1] «잡은 작업 단위 딱 1개». 다만 185-② («빨간 게이트는 아무도 안 본다»)
대로 **그 자리에서 등재**한다. 같은 파일이 **총 7건 FAIL(132/139)** 이고 나머지 5건은 축이 다르다
(«칸 2개» 기대가 3칸이 된 바를 보고 있다 — 폭·경계·오른끝이 전부 ÷2 스냅샷이라 연쇄로 빨갛다).
279 가 두 갈래를 갈라서 볼 것.

**다음에 훈련 팝업에 칸이 또 늘면**: `TRSUBS` 에 키를 더하기만 하면 [A2]·[B] 는 그대로 통과한다.
금지 목록(`dead`)에는 **손대지 마라** — 거기 이름을 더하는 순간 다시 «이름» 을 감시하게 된다.
88 이 지키는 것은 «**스탯** 훈련이 안 돌아온다» 하나다.
