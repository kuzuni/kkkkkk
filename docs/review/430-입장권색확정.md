# 430 — 던전 입장권 8색을 «이름대로» 확정

> 저장소 주인 재재지시(2026-08-30): «던전 입장권 색깔 여전히 비슷함. 노랑(황금) 초록(수정) 갈색(고대)
> 회색(잊힌) 빨강(용) 흰색(창세) 주황(각성) 파랑(룬) 으로 해야할거 같다»
> 같은 결함을 **세 번째** 지시받은 자리다 — 402(매핑 8종 분리) → 412(색상환 배분) → 430(이름이 색을 정한다).
>
> 세션 `sess-1935-8337` 워커 B · 2회차 · **제품 `index.html` 1자리(필터 한 줄 삭제) · 자산 SVG 8장 · 게이트 4자리**
> 지시서 [3]-(가) 기계적/자산 작업 — 비평 루프 없음(캡처 확인 2회차).

## 1. 재현이 먼저 (338 규칙)

`tools/probe430.js` 신설. 412 팔레트(수리 전)와 지금 파일을 **같은 캔버스에 그려** 찍힌 픽셀을 읽고,
그 색이 그 던전 «이름의 색 구간» 에 드는지 센다(색상각은 HSL · 밝기·채도는 CIE-Lab).

| | 412(수리 전) | 430(지금) |
|---|---|---|
| 이름 일치 | **0/8** | **8/8** |
| 회색조(L\* 만) 최소 인접차 | **0.0** (여덟이 전부 L\*64) | **8.2** |
| 쌍별 최소 ΔE | 39.6 | **44.4** |

**등재문의 가설이 재현으로 그대로 확인됐다** — 412 는 «갈리기» 는 했지만(ΔE 39.6) 이름과는 하나도 안 맞았다.
찍힌 픽셀로 보면 황금이 **황동**(h40°) · 수정이 **청록**(167°) · 고대가 **연두**(86°) · 잊힌이 **하늘**(204°) ·
용이 **보라**(272°) · 창세가 **자홍**(335°) · 각성이 **연어**(10°) · 룬이 **청록2**(188°)였다.
주인이 «여전히 비슷함» 이라고 한 이유가 여기 있다 — **색상환 이웃 넷(86·167·188·204°)이 «파랑 계열 넷» 으로 읽힌다.**

## 2. 뿌리는 색이 아니라 **규칙**이었다

412 는 «통일감» 을 **«L\*64 한 밴드 · C\* 한 밴드 · 색상만 돌린다»** 로 풀었다(그 자리의 게이트가
`verify402` B6 «L\* 폭 ≤ 4 · 최소 C\* ≥ 30» 이다). 그 규칙 아래에서는

* **명도 축이 통째로 죽는다** → 회색조에서 8장이 완전히 같다(0.0). 색각 이상·저해상도에서는 문양이 유일한 축.
* **«회색·흰색·갈색» 을 아예 못 만든다** — 셋 다 C\* 또는 L\* 가 밴드 밖이다.

주인이 준 8색은 **색상·명도·채도 세 축을 다 쓰는 팔레트**다. ⇒ **412 의 밴드 규칙을 폐기**하고
그 자리에 정반대 축(«회색조 사다리»)을 세웠다. 자리를 비우지 않은 것이 333 처방이다.

## 3. 무엇을 했나 — 팔레트

껍데기·속띠 `path d` 와 문양 실루엣 8종·bbox 21×21·중심 (32,35.5) 은 **한 글자도 안 건드렸다**(bbox Δ0).
바뀐 것은 **색 세 자리(테·채움·문양 잉크)뿐**이다.

| 던전 | id | 이름색 | 채움 | L\* | C\* | HSL h | 테 | 문양 잉크 |
|---|---|---|---|---|---|---|---|---|
| 황금 동굴 | `gold` | 노랑 | `#E8DA33` | 85.8 | 77.8 | 55° | `#696700` | 테색(어두움) |
| 수정 광산 | `dia` | 초록 | `#2FB43F` | 64.6 | 75.7 | 127° | `#006B0B` | 흰색 |
| 고대 유적 | `relic1` | 갈색 | `#6C380D` | 29.5 | 40.0 | 27° | `#3B1C00` | 흰색 |
| 잊힌 신전 | `relic2` | 회색 | `#85858B` | 55.7 | 3.4 | – | `#3A3A40` | 흰색 |
| 용의 무덤 | `relic3` | 빨강 | `#D22A2A` | 46.4 | 76.3 | 0° | `#6B0000` | 흰색 |
| 창세의 제단 | `relic4` | 흰색 | `#FFFFFF` | 100.0 | 0.0 | – | `#5A5A60` | 테색 |
| 각성의 동굴 | `stone` | 주황 | `#FF9F55` | 74.0 | 60.2 | 26° | `#9F4B01` | 테색 |
| 룬의 제단 | `rstone` | 파랑 | `#1352C4` | 38.1 | 69.5 | 219° | `#001E5E` | 흰색 |

### 3-1. 명도 사다리가 «남는 자유도» 를 다 먹었다 (다음 세션이 볼 것)

이 팔레트는 취향이 아니라 **부등식 넷의 해**다. 값을 흔들려면 넷을 같이 봐야 한다.

1. **회색↔흰색이 최악 쌍이다.** 둘 다 무채색이라 ΔE 가 곧 ΔL 이다 ⇒ «최소 ΔE ≥ 39.6» 을 지키려면
   흰색이 L\*100 일 때 **회색은 L\* ≤ 60.4** 다. 회색을 «중간 회색» 으로 두는 순간 게이트가 빨개진다.
2. **회색조 최소 인접차 ≥ 8** 이면 여덟 값의 **span ≥ 56** 이 필요하다. 위가 100(흰색)이므로 아래가 ≤ 44 —
   갈색(29.5)·파랑(38.1)이 그 아래를 채운다.
3. **갈색과 주황은 색상각이 같은 구간**(HSL 18~42°)이라 **L\* 로만 갈린다**(갈색 ≤ 50 · 주황 ≥ 62).
   갈색을 밝게 하면 주황과 붙고, 주황을 어둡게 하면 갈색이 된다.
4. **노랑↔주황**은 색상각을 벌려야 한다. 노랑을 55°(레몬 쪽) · 주황을 26°(빨강 쪽)로 밀어 ΔE 49.6 을 얻었다.
   노랑을 50° 아래로 내리면 그 쌍이 ΔE 40 밑으로 떨어진다(1회차 중간 팔레트에서 실제로 32.9 를 봤다).

⚠ **주황이 L\*74 로 «조금 밝은 주황»** 인 것은 그 사다리의 대가다 — 초록(64.6)과 노랑(85.8) 사이 8 간격을
지키면서 색상각 26° 를 유지할 수 있는 자리가 거기뿐이다.

### 3-2. 문양 잉크가 «흰색 고정» 에서 «대비가 큰 쪽» 으로 바뀌었다

412 는 8장이 전부 L\*64 라 흰 문양(대비 2.8:1)이 어디서나 통했다. 430 은 채움이 L\*29.5~100 이라
**밝은 장 셋(노랑 1.45:1 · 흰색 1.00:1 · 주황 1.93:1)에서 흰 문양이 통째로 사라진다.**
⇒ 규칙을 «**흰색 ↔ 그 장의 테색 중 대비가 큰 쪽**» 으로 바꿨다(노랑·흰색·주황이 테색으로 넘어간다).
획 규격(opacity .92 · stroke-width 1.6)은 **그대로**이고, 획 **색**만 «잉크의 반대쪽» 으로 대칭화했다
(흰 잉크 → 테색 · 테색 잉크 → 채움색). 자세한 이유는 §7-2 — 2회차에 바뀐 자리다.

### 3-3. 테 L\* 상한 45 — 밝은 장의 «어두운 짝» 은 −26 으로는 모자란다

412 규격은 «테 = 채움 L\* −26» 이었다. 흰색(100)·노랑(85.8)에 그대로 쓰면 테가 L\*74·60 이라
**배경에서도 문양에서도 안 잡힌다.** ⇒ 테 규칙을 «더 어두운 짝(ΔL ≥ 15) **이면서** L\* ≤ 45» 로 바꿨다.
무채색 2장(회색·흰색)은 색상각이 없으므로 «중성 회색 테»(C\* ≤ 8)로 짝을 맞춘다.

## 4. 게이트

| 자 | 결과 | 무엇을 하나 |
|---|---|---|
| `tools/verify430.js` | **38/38 신설** | [A] 8장이 이름 구간 안(찍힌 픽셀) · [B] ΔE ≥ 39.6 · 회색조 ≥ 8 · [C] 기하·문양 세트 · [D] 테·문양 잉크 · [E] 세 화면 + «배경에 안 녹는가» + **[E5]·[E6] 화면에 찍힌 픽셀** · [F] 제품 리터럴 0건 · §R 되돌림 7항 |
| `tools/probe430.js` | **6/6 신설** | 412 팔레트 ↔ 지금을 같은 캔버스에서 대조(이름 일치 0/8 → 8/8) |
| `tools/verify402.js` | **39/39** (B5·B6·B7·B9·§R5 이관) | 아래 §4-1 |
| `tools/probe412.js` | **8/8** (②-b·②-c 이관) | «한 밴드» 를 «회색조 사다리» 로 |
| `tools/verify125.js` | **38/38 무수정** | 125 단일 출처·H1~H3 은 매핑 층이라 색과 무관하다 |
| `tools/verify356.js` | 무수정 | 비율 정책 — bbox Δ0 이라 스코프 밖 |
| `node tools/smoke.js` | **SMOKE PASS** | |

### 4-1. `verify402` [B] 이관 — 412 규칙 둘을 폐기한 자리

* **B5** — 축이 둘(색상·명도)에서 **셋**(+채도)이 됐다. 430 의 무채색 2장은 «채도가 0 이라는 것» 자체가 축이다
  (회색 ↔ 파랑은 색상각 1°·ΔL 17.6 이지만 ΔC 66 이라 사람 눈에 절대 안 헷갈린다).
* **B6** — «L\* 폭 ≤ 4 한 밴드» **폐기** → «회색조 최소 인접차 ≥ 8». 같은 자리에 **정반대 규칙**이 섰다.
* **B7** — «Δh ≤ 6 · ΔL 26±3» → «유채색 Δh ≤ 6 · 무채색 C\* ≤ 8 · ΔL ≥ 15 · **테 L\* ≤ 45**».
* **B9** — 문양 잉크가 «흰색 고정» → «흰색 또는 그 장의 테색»(획 규격은 그대로).
* **§R5** — 되돌림 **표본**을 갈았다. 412 시절 표본(`rstone ← #2FD4C4`)은 430 팔레트에서는 아무 쌍과도 안 가까워
  **«되돌려도 초록»** 이 됐다 — 표본을 «이웃 계열로 접힌 한 장»(`relic3 ← #8C4A18`, 갈색 옆 밝은 갈색)으로 바꿔
  ΔE 44.4 → **11.6** 으로 빨개지는 것을 다시 못박았다.
  ⚑ **교훈: 되돌림 시험은 팔레트를 갈면 같이 갈아야 한다.** 안 갈면 «항상 초록인 §R» 이 남는다(368·422 계열).

### 4-2. 게이트가 실제로 한 건 잡았다 — «검은 테가 받아 준다» 는 거짓이었다

[E4] 를 처음에 «검은 테(4px) 또는 채움이 배경과 ≥ 3:1» 로 썼더니 **갈색이 2.21:1 로 빨개졌다.**
03 카드의 `.sp.tk` 알약은 `rgba(0,0,0,.75)` 라 **거의 검정**이고, 그 위에서는 검은 테가 아무것도 안 해 준다.
어두운 장(갈색 2.2 · 파랑 3.0)을 실제로 보이게 하는 것은 **흰 문양**이다(21:1).
⇒ 판정을 «세 잉크(테·채움·문양) 중 하나가 ≥ 3:1» 로 고쳤고, **무르게 푼 게 아님을 §R6 이 못박는다** —
갈색 장의 문양을 테색으로 바꾼 사본은 최선이 2.21:1 이라 그 항이 빨개진다.
(잉크 고르기 규칙이 «어두운 장 = 흰 문양 / 밝은 장 = 어두운 문양» 이라 **어느 장이든 배경에서 먼 잉크가 하나 있다**.)

## 5. 안 건드린 것

* **매핑·마크업 0줄** — 402 가 세운 `dunTk()` **id 파생** 이 그대로라 파일 이름만 같으면 된다.
  `verify430` [F1] 이 «팔레트 고유색 리터럴 0건»(순백·순검 제외 채움 7 + 테 8)으로 못박는다.
  제품에서 바뀐 것은 **`.dgd-tki` 의 `filter` 한 줄 삭제뿐**이다(§7).
* **세이브 이관 없음** — `S.dunTk` 는 던전 id 별 수량이고 색은 자산이다(KEY 안 올림).
* **`DUN_UI[].s/i/r`(카드 두 톤)** — 412 가 «카드 두 톤 = 입장권 색» 규칙을 이미 폐기했고, 430 도 안 되살렸다.
* **레이아웃 0px · 밸런스 0줄 · 판정 0줄.**

## 6. 캡처

`node tools/cap430.js` → `docs/shots/cap430-*.png` 6장
(03 던전 카드 상단 4장 · 하단 4장 · 04 세부 팝업 흰색/갈색 · 13 교환 카드 · **8장 나란히 대조판**).
⚠ **스크롤을 안 하면 증거가 안 담긴다** — 1회차 13 캡처에는 교환 카드가 한 장도 안 찍혀
«입장권이 없다» 로 읽혔다. 두 자리 다 `scrollIntoView` 로 올려 찍는다.
⚑ **나란히 안 놓으면 세트로 읽히는지가 안 보인다**(411 이 세운 규칙) — 대조판을 같이 찍는 것을 규약으로 둔다.


---

## 7. 2회차 — 캡처 확인이 «선언은 맞는데 화면은 틀린» 자리를 하나 꺼냈다

1회차 캡처를 본 확인자가 «04 세부 팝업의 입장권이 **초록**» 이라고 보고했다. 확인자는 그 팝업의
**보상 아이콘 `cur-relic.svg`(유물조각, 초록)** 을 입장권으로 오독했지만 — 팝업의 입장권 `src` 는
내내 `cur-ticket-relic1.svg` 였고 `verify402` [C2]·`verify430` [E2] 가 그것을 이미 초록으로 통과시키고 있었다 —
**찍힌 픽셀을 재 보니 그 지적 자체는 맞았다.**

```
#dgdTki { … filter: hue-rotate(108deg) saturate(.7) brightness(1.7) }   ← index.html 7845
```

04 세부 팝업의 입장권 호스트가 **아이콘의 색을 통째로 되접고 있었다.** 갈색 `#6C380D` 이 `#23864E` 로,
주황 `#FF9F55` 가 `#2D9350` 으로 — **8장이 전부 초록 한 색**이었다. 이 필터는 «입장권이 한 장뿐이던» 시절
ref(측정표 04 §6: 티켓 아이콘 **초록 `#57A888`**)에 맞추려고 색을 돌려 둔 값이고, 402(던전마다 한 장)
이후로는 **주인 지시를 정면으로 무효화하는 한 줄**이 됐다. 주인 지시가 레퍼런스보다 우선이므로 걷어냈다
(측정표 04 §6 정오표 — 의도적 이탈. 기울기 −18°·자리·크기는 ref 그대로).

⚑ **왜 게이트가 못 잡았나 — [E1]~[E3] 이 «선언(src)» 만 봤기 때문이다.** 350·368 이 이미
«선언이 아니라 찍힌 픽셀을 세라» 를 남겼는데 이 자의 [E] 절만 그 규약 밖에 있었다. ⇒ 2회차에 **[E5]·[E6]**
(03 카드 · 04 세부에서 캡처를 되읽어 이름 창을 **다시** 묻는다)과 **§R7**(그 필터를 도로 켜면 빨개진다)을 신설했다.
⚠ **회전한 호스트를 rect 로 찍으면 표본이 빗나간다** — `.dgd-tki` 는 `rotate(-18deg)` 라
`getBoundingClientRect` 가 **회전 전 상자보다 큰 축정렬 상자**다(첫 판이 검정·크림색을 읽고 «8장 다 실패» 로 나왔다).
조상 transform 의 회전·배율만 곱해 중심 기준으로 되민 뒤 찍는다.

### 7-2. 문양 외곽선을 «잉크의 반대쪽» 으로 (밝은 장 3종)

같은 확인에서 «창세(흰색)·각성(주황)의 문양이 테두리에 붙어 밑변이 잘려 보인다» 가 나왔다 — 실재한다.
밝은 장은 잉크가 **테와 같은 어두운 색**이라, 외곽선까지 테색이면 문양 밑변(y 46)과 테(y 47~)가 한 덩어리로 붙는다.
⇒ 획 규칙을 **«획 = 잉크의 반대쪽 색»** 으로 대칭화했다(흰 잉크 → 테색 어두운 테두리 · 테색 잉크 → 채움색 밝은 후광).
`stroke-width 1.6` · `opacity .92` · path 는 그대로라 **bbox Δ0** 은 유지된다.
곁다리 정정: `stone`·`rstone` 의 **주석 문양 이름이 서로 바뀌어 있었다**(그림은 처음부터 맞았다) — 각성 = 원석(칠각) · 룬 = 룬 방패.
