# 791 — `probe695` [2] «제품 드리프트 기각» 이 실행마다 갈린다 (T1 «게이트 플레이키»)

- **세션** sess-0328-12297 · 루틴 워커 B · 1회차 (2026-09-02)
- **지시서 [3]-(가)** — 자 수리라 비평가 없음. **제품 `index.html` 0줄 · 레이아웃 Δ0px · 문턱 0칸**
- **바뀐 파일** `tools/probe695.js`([2] 절) · `tools/rul504.js`(`treeSep` 신설) · `tools/probe791.js`(신설)

## §1 재현 — 등재문보다 세게 났다

착수 첫 실행이 **`FAIL 16/18`** 이었다. 등재문은 `[2]` 하나가 18회 중 2회 빨갛다고 적었는데,
그 실행은 `[2]` 와 `[2-b]` 가 **같이** 빨갰다.

| 항 | 옛 판정식 | 그 실행의 값 |
|---|---|---|
| `[2]` | `old.every(off > tol)` | `aura` 이탈 **35%** < 허용 40% ⇒ 빨강 |
| `[2-b]` | 두 트리 평균비 `< 3` | `aura` 6.113 / 2.007 = **×3.05** ⇒ 빨강 |

수집 10회를 더 돌려 분포를 잡았다(등재문 «착수 세션은 먼저 8회 이상»):
504 트리 `aura` 평균 **2.533 / 2.733 / 3.26 / 3.587 / 3.827 / 3.947 / 4.033 / 4.28 / 4.293 / 4.827**
↔ 그 빨간 실행 **6.113**. 밴드 경계는 `9.4 × (1 − 0.40)` = **5.64** 다.
⇒ **갈림의 자리는 문턱이 아니라 평균이다** — 빨간 실행에서도 허용은 바닥값 `TOL_FLOOR` 0.40 그대로였고
`aura` 의 K회 평균만 밴드 안으로 올라왔다(`probe791` [1-c] 가 이 칸을 못박는다).

## §2 뿌리 — 759-① 이 금지한 자리를 [2] 가 그대로 쓰고 있었다

`orbit`·`aura`·`whirl` 은 695 가 **«이 눈금으로 못 잰다»(⏸접촉)로 등재한 바로 그 셋**이다
(`RUL.held695`). 그런데 [2] 는 그 셋에게 **밴드 멤버십**(`off > tol`)을 다시 물었다 —
759-① 이 «판정 불가라고 적어 둔 종은 어느 항에서도 그 눈금으로 묻지 마라» 로 닫은 자리이고,
[3] 이 바로 아래에서 «같은 자·같은 트리인데 평균이 갈린다» 를 **초록으로** 단언하고 있었다.
자가 자기 말을 뒤집고 있었던 것이다(759 와 같은 모양, 다른 절).

## §3 ⚑ 이 회차의 본체 — [2-b] 는 흔들리기만 한 게 아니라 **거짓을 단언하고 있었다**

옛 [2-b] 의 문장은 «두 트리의 실측이 같은 자리 — **제품이 옮긴 것이 아니다**» 였다.
695 자신이 열어 둔 **고정 장면**(`freeze` 손잡이 — 카이팅을 끄고 재는 장면)에서 두 트리를 재 보니:

| 종 | 504 트리(고정) | 오늘 트리(고정) | 이동 | 자유 장면 Δ |
|---|---|---|---|---|
| `orbit` | 10.0 ~ 10.8 | 7.07 ~ 7.41 | **×1.4** | 0.72 이하 |
| `aura` | 24.8 ~ 25.4 | 9.15 ~ 9.50 | **×2.7** | 2.95 이하 |
| `whirl` | 25.9 ~ 26.9 | 17.7 ~ 18.4 | **×1.5** | 3.02 이하 |

**제품은 그 사이 실제로 움직였다.** 자유 장면의 잡음(K회 폭 76~283%)이 그 이동을 통째로 덮고 있었고,
[2-b] 는 그 잡음을 «같은 자리» 로 읽어 **틀린 것을 초록으로 지고 있었다**(788 이 말한 헛초록 부류 —
«흔들린다» 보다 나쁘다). 문턱을 다시 뽑는 처방(등재문 후보 ⓑ)이었으면 **거짓말을 더 안정적으로**
지게 만들었을 것이다.

## §4 처방 — 문턱을 고치지 말고 **장면을 옮겼다**

후보 ⓐ(«선언이 낡았다»)를 기각하는 일은 그대로 하되, 물음을 **잴 수 있는 장면**에서 던진다.
고정 장면은 K회 폭이 **3~13%**(자유 장면은 76~283%)라 같은 물음이 실제로 답을 갖는다.

| 항 | 지금 묻는 것 | 실측 여유 |
|---|---|---|
| `[2]` | (자유) 504 트리에서도 K회 **평균이 선언에 못 미친다** — 방향만, 문턱 0 | 11회 × 3종 **33/33**, 가장 가까운 표본도 선언의 65% |
| `[2-b]` | (대조) 자유 장면의 두 트리 Δ가 **고정 장면의 Δ보다 작다** ⇒ 그 장면의 판정은 잡음이 정한다 | ×0.19 ~ 0.37 (여유 ×2.7~5.3) |
| `[2-c]` | (고정) 두 트리가 **갈린다** ⇒ 옛 [2-b] «안 옮겼다» 는 거짓이었다 | ×1.76 ~ 28.9 (단위 1) |
| `[2-d]` | (고정) 그 장면에서 **오늘** 트리만 자기 선언과 맞는다 ⇒ ⓐ 기각은 유지 | 오늘 이탈 **0~11%** ↔ 504 트리 **47~168%**(밴드 40%) |
| `[2-e]` | 되돌림 — `RUL.treeSep` 이 «다 통과» 가 아니다 | 같은 구름 ×0 · ¼폭 ×0.25 · 3폭 ×3.00 |

⚑ **[2-d] 가 이 절의 결론을 오히려 더 세게 만들었다** — 고정 장면에서 오늘 트리의 `aura` 는
**9.4 = 선언 그 값**(이탈 0%), `orbit` 7%, `whirl` 1% 다. 즉 «선언이 낡았다» 는 오늘 트리에서
반증되고, **낡은 쪽은 504 트리**(자기 선언에서 47~168% 밖)다.

**새 눈금 `RUL.treeSep`** — 손 상수도 새 문턱도 없다(784 의 모양 그대로):
`ratio = |평균A − 평균B| ÷ ((폭A + 폭B) / 2)`, 단위는 기존 `SHAKE_UNIT`(=1 · «폭 한 벌»).
`TOL_FLOOR` 0.40 · `K` 6 · `SHAKE_UNIT` 1 · `HOLD695` 표는 **한 칸도 안 건드렸다.**

## §5 ⚠ 내가 심을 뻔한 동전 둘 — 둘 다 값이 잡았다(784-②)

「옮겨온 처방도 새 항이다」를 이 회차가 두 번 값으로 확인했다.

1. **첫 꼴 = 양방향 `shakeSep` 의 min.** 12표본에서 자유 ≤0.26 · 고정 ≥1.71 로 깨끗해 보였는데,
   13번째 실행에서 504 트리 `orbit` 구름만 넓게 나오자(9.44~12.52) **0.74** 로 내려앉아 고정 장면이
   빨개졌다 — 널을 «한쪽 구름의 폭» 으로 잡으면 **넓게 나온 쪽이 판정을 쥔다.**
   두 폭의 **평균**을 널로 쓰면 같은 실행이 **1.76** 이다(`probe791` [6] 이 그 표본을 들고 있다).
2. **둘째 꼴 = «자유 장면에서는 못 가른다»(`ratio < 1`) 를 단언한 항.** 30표본 최대 0.70 이라
   안전해 보였으나 `probe791` [4] 의 라이브 실행이 **1.17** 을 냈다 — 단위를 넘었다.
   ⇒ 그 항을 **단언에서 내리고**(값은 참고로 찍는다) 같은 실행 안의 두 값 비교(§4 `[2-b]`)로 갈았다.

## §6 게이트 — `tools/probe791.js` 11/11 신설

«몇 번에 한 번 빨간가» 를 세지 않는다(784 규약). 확률을 확정으로 바꾸는 손잡이는 둘이다 —
**① 실측된 두 실행**(빨강 실행 · 초록 실행)을 표본으로 박아 두면 «같은 제품·같은 자인데 판정이 반대»
가 합성 강짜 없이 선다. **② 장면을 바꾸면 그 갈림이 사라진다.**

- `[0]` 소스 — [2] 절에 옛 두 축이 0건이고 판정은 `RUL.treeSep` 하나(779-③)
- `[1]`·`[1-b]`·`[1-c]` 재현 — 옛 [2]·[2-b] 가 두 실측 실행에서 서로 반대 · 갈림의 자리는 평균
- `[2]` 새 축은 그 두 실행에서 같은 판정
- `[3]` **축 시험(784-①)** — 표본은 그대로 두고 **선언만** 반으로 옮기면 옛 [2] 는 뒤집히는데
  두 트리의 관계는 **Δ0** ⇒ 옛 축은 «제품 드리프트» 를 아예 안 보고 있었다
- `[4]`·`[4-b]` 라이브(기본 2회) · `[5]`·`[5-b]` 되돌림 · `[6]` 첫 처방 기각 표본

## §7 검증

- `probe695` **21/21 × 6회 연속**(수리 전 같은 자리에서 1회차부터 빨강이 났다)
- `probe791` **11/11 신설**
- 회귀 — `verify504` 15/15(⏸1 199 · ⏸3 접촉) · `probe504` 15/15 · `probe680` 19/19 ·
  `probe722` 23/23 · `probe766` 13/13 · `probe783` 12/12 · `probe784` 18/18 — **전부 무수정**
- **SMOKE PASS** · 제품 `index.html` **0줄**

## §8 다음 세션이 볼 것

- ⚑ **플레이키를 만나면 «문턱 → 모수 → 축» 다음에 «장면» 이 있다.** 759 는 면제를, 783 은 모수를,
  784 는 축을 갈았다. 791 은 축을 갈아도 안 닫혔다 — 같은 물음을 **답이 있는 장면**으로 옮겨야 했다.
  그 장면은 새로 만든 것이 아니라 695 가 이미 열어 둔 `freeze` 손잡이다.
- ⚠ **`probe695` `[1]` 은 아직 같은 가족이다** — 자유 장면에서 `off > tol` 을 묻는다.
  지금은 여유가 크지만(이탈 70~84% ↔ 허용 40~56%) 폭이 343% 를 넘는 실행이면 뒤집힌다.
  실측 최대 폭 283% 라 **아직 안 넘었고**, 이번 작업 단위 밖이라 손대지 않았다 → **794** 로 등재.
- ⚠ 고정 장면이 보여 준 **제품 이동(×1.4~2.7)** 은 이 자의 결론(⏸접촉 = 눈금 미적용)을 안 바꾸지만,
  **199 가 볼 값**이다 — 504 이후 접촉형 3종의 서서 맞는 DPS 가 `orbit` −32% · `aura` −63% ·
  `whirl` −32% 로 내려왔다(724 곱→합 모델 등이 지난 자리). 계수 판단은 199 몫이다.
