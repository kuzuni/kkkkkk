# 768 — 714 가 폐지한 `S.sumLv`·`S.sumExp` 를 아직 읽는 자 (게이트 부패 · 714 이관 누락)

**완료(2026-09-01, sess-2113-30232 루틴 워커 A · 1회차) · 제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.**
고친 것은 `tools/probe528.js` **한 파일 3자리**이고, 새로 놓은 것은 게이트 `tools/verify768.js` **19/19** 다.

---

## 1. 재현 — 등재문의 «20종» 은 착수 시점에 이미 19종이 닫혀 있었다

등재(2026-09-01 18:48Z, sess-1848-1709 · 757 회귀 스윕의 곁다리)는 두 자를 이름으로 지목했다 —
**`verify195` 즉사** · **`verify196` 22/39**. 338 규칙대로 처방 전에 **둘을 그대로 돌렸다**:

| 자 | 등재문(18:48Z) | 착수 시 실측(21:2x Z) |
|---|---|---|
| `verify195` | 즉사(`undefined.toLocaleString()`) | **36/36 PASS** |
| `verify196` | 22/39 | **39/39 PASS** |

누가 닫았는지는 이력이 못박는다 — **`c7998a0` «wip(714): 2회차 — 소환 레벨 축 게이트 19종 이관(333 처방)»**(2026-09-01 **19:27:33Z**),
등재 **39분 뒤**다. 그 커밋이 만진 19 파일에 `verify195`·`verify196`·`func196`·`bot199`·`probe568`·`probe655`·`probe668`·
`probe675`·`fnchk115`·`verify655`·`verify668`·`verify669`·`verify675`·`verify76`·`verify106`·`probe522`·`cap669` 가 들어 있다.

⚑ **그래서 이 작업의 본체는 «남은 자리를 찾는 것»** 이었다. 등재문의 목록을 그대로 믿고 20종을 다시 갈아 끼웠으면
**이미 옳은 것을 두 번 고치고** 진짜 남은 한 자리는 그대로 뒀을 것이다.

## 2. 남은 한 자리 — `probe528` 의 «저장·복원 짝»

전수 스윕(`tools/**.js` 1,144 파일, 주석 제거 뒤)에서 살아 있는 위반은 **한 파일 3자리**뿐이었다.

```js
/* 수리 전 — 쓰기만 배너 칸으로 옮기고 저장·복원은 496 의 공용 스칼라에 남았다 */
const probsAt = v => { const o = S.sumLv; S.sum.weapon.lv = v; const p = gradeProbs('weapon'); S.sumLv = o; return p; };
```

* 읽기 `const o = S.sumLv` → **`undefined`**(NaN 이 아니다).
* 되돌리기 `S.sumLv = o` → **아무 데도 안 닿는 새 전역**을 하나 만들 뿐이라 **원복이 조용히 사라진다.**

세 자리(`probsAt` · `sim` · `live`)를 전부 배너 칸(`S.sum.weapon.lv`)으로 저장·복원하게 바꿨다.

**결손의 크기는 «지금 빨강» 이 아니라 «잠복»** 이다 — `measure()` 의 모든 측정이 읽기 **전에** 값을 직접 세우므로
현재 A/B 판정값은 안 바뀐다(수리 전 10/12 ↔ 수리 후 10/12, 실패 2건은 §4 의 다른 뿌리다).
바뀌는 것은 **측정이 끝난 뒤의 상태**다 — `verify768` [D2] 가 그 자리를 못박는다:
수리 전 모양으로 «저장·복원» 하면 배너 칸이 **원값 1 → 50 으로 바뀐 채 남고**, 수리 후 모양은 **1 → 1** 로 돌아온다.
측정을 하나 더 붙이는 다음 세션이 그 오염을 먹는다.

## 3. 자 — `tools/verify768.js` 19/19 (재발 방지)

이 자가 잡는 것은 «지금 빨간 게이트» 가 아니라 **재발**이다(731 계열 «없는 전역을 읽으면 빨강»).

* **[A] 소스 스윕** — `tools/**.js` 전수. 주석을 «자리는 남기고 내용만» 지운 뒤 남은 `S.sumLv`/`S.sumExp` 는
  **같은 줄에 `typeof` 가드**가 있어야 한다. 가드는 «수리 전 트리도 함께 재는 자»(`probe714`·`probe496`·
  `verify496`·`verify714`)의 갈래 표식이라 헛빨강을 막는다. **A1 이 스코프가 비지 않았음을 먼저 못박는다**(1,144 파일).
* **[A3]** 등재문이 이름으로 적은 19종을 **이름으로도** 다시 센다(스코프 구멍 방지).
* **[A4]** 768 이 고친 짝 3곳이 실제로 배너 칸으로 원복하는지 소스로 센다.
* **[B]** 지목당한 자들이 배너 접근자(`S.sum[b]`·`sumLv(b)`·`sumOf(b)`)를 쓴다.
* **[C] 런타임 — 실패 기계 자체를 못박았다**: 폐지된 이름에 값을 써도 **배너 다섯 칸이 한 칸도 안 움직인다**(C3).
  «조용한 빨강» 의 정체가 그것이고, 세이브에도 스칼라 둘이 안 담긴다(C4 — `verify714` [B1] 과 같은 축).
* **[R] 되돌림 시험** — 스트레이 한 줄은 **빨강**(R1) · typeof 가드 줄은 초록(R2) · 주석은 초록(R3) ·
  등재문이 적은 «즉사» 두 줄도 빨강(R4) · **예외 목록이 이 자 하나뿐임**(R5).

⚠ **자를 놓다가 다른 자를 빨갛게 만들 뻔했다** — [C] 가 실패 기계를 재현하려고 `S.sumLv = 99` 라고 적자
`verify528` [A2](«만렙 이상의 수를 손으로 적은 자리 0건»)가 **내 새 파일을 잡았다**. 값을 만렙 미만(7)으로 내려
뜻은 그대로 두고 초록으로 되돌렸다. **회귀는 새로 놓은 자에게도 대야 한다.**

## 4. 곁다리 관측 2건 — 등재만(내 변경과 무관함을 대조로 확인)

* **773** — `probe528` [A2]·[B3] 와 `verify528` [B1]·[R3] 이 «펫 **8등급 / 36행**» 을 단언하는데 실측은 **7등급 / 35행**이다.
  뿌리는 **757**(`rollOf` = «그 배너가 파는 종 목록까지 자른 표» — 주석이 «스킬 6행 · **펫 7행** · 장비 8행» 이라고 적어 두었다).
  **수리 전에 돌린 `probe528` 도 같은 10/12** 였고(내 편집 전 실행), `verify528`·`probe496` 은 내가 만진 파일을 읽지도 않는다.
* **774** — `probe496` 11/16. ⓑ·ⓒ 가 `SUM_MAXLV = 25` · 해금 사다리 `1,1,5,8,12,16` 을 기대하는데 현행은 **50** · `1,1,10,16,24,32` 다.
  더해서 이 자의 **갈래 키가 뜻을 잃었다** — `typeof S.sumLv === 'number'` 하나로 «수리 전/후» 를 가르는데
  714 가 다시 배너별로 돌려놓아 **오늘 트리를 «496 수리 전» 으로 읽는다**(`probe568` 이 «갈래 키가 둘이 됐다: `sumVer`(714)·`sumLv`(496)» 로
  이미 적어 둔 그 자리다).

## 5. 회귀

| 자 | 결과 |
|---|---|
| `verify768` | **19/19 신설 PASS** |
| `verify195` | 36/36 PASS |
| `verify196` | 39/39 PASS |
| `verify714` | 32/32 PASS |
| `verify496` | 41/41 PASS |
| `probe714` | 11/11 PASS |
| `verify528` | 11/14 — **773**(펫 7행) · [A2] 는 내 파일 수정 뒤 초록 복귀 |
| `probe528` | 10/12 — **773**(수리 전과 같은 점수) |
| `probe496` | 11/16 — **774** |
| `node tools/smoke.js` | **SMOKE PASS** |

## 6. 교훈

1. **등재문의 «N종» 은 착수 시점의 수가 아니다.** 4워커가 동시에 도는 저장소에서 등재와 착수 사이 39분이면
   다른 세션이 19종을 닫아 놓는다 — **처방 전에 지목당한 자를 직접 돌려라**(338 규칙의 값어치가 여기서 나온다).
2. **반쯤 이관된 자리가 가장 오래 산다.** `probe528` 은 «쓰기» 만 새 API 로 옮겨 게이트가 초록이었고,
   죽은 것은 **저장·복원 짝**이라 아무 자도 안 짖었다. 이관은 «쓰는 자리» 가 아니라 **이름 단위**로 센다.
3. **폐지한 전역은 지운 것으로 끝나지 않는다** — 읽으면 `undefined`, 쓰면 새 전역이라 **두 얼굴(즉사·조용한 Lv1)** 로만 드러난다.
   그래서 자를 «값이 맞나» 가 아니라 **«그 이름이 코드에 있나»** 로 세웠다(731 계열).
4. **새로 놓은 자도 회귀 대상이다** — `verify528` [A2] 가 내 [C] 절의 리터럴을 잡았다.
