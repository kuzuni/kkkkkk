# 728 — `tools/verify664.js` 축 뒤집힘 (T1 «버그(게이트 부패)»)

**제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정·밸런스 0줄.** 고친 것은 `tools/verify664.js` 한 파일이고,
`tools/probe728.js` 를 신설했다. 지시서 [3]-**(가)** — 기계적/게이트 작업이라 **비평가를 안 띄웠다.**

| 회차 | 세션 | 결과 |
|---|---|---|
| 1 | sess-1712-27950 (루틴 워커 A) | `verify664` **39/43 FAIL → 53/53 PASS** · `probe728` **16/16 신설** |

---

## §1 재현 — 등재문의 가설을 확인하고, **[R3] 의 뿌리는 정정했다**

338 규칙대로 처방 전에 `tools/probe728.js` 를 신설해 제품에게 직접 물었다(16/16).

**착수 시점의 실측은 등재문과 달랐다 — 40/43 이 아니라 `VERIFY664` 39/43** 이다.
등재(2026-09-01, sess-1622-4733) 이후 **§4-c 가 하나 더 빨개졌다**(아래 §3). 내 변경 이전의
깨끗한 트리에서 잰 값이므로 내 작업과 무관하다.

| 항 | 등재문 가설 | 재현 결과 |
|---|---|---|
| §5-a | 662 가 «미보유 대체문» 을 지웠는데 자는 아직 그것을 찾는다 | **확인** — `probe728` [1-a]~[1-c]: 대체문 **0건**, 자리는 `skillDescText(it)` 파생이 대신한다 |
| §5-b | 미보유 스킬이 실명으로 열리는데 자는 «???» 를 요구한다 | **확인** — [2-a] `"표창 난사"` ↔ `us.n` 일치 · [2-c] 그러면서 `.sk-un` 🔒 상태 딱지는 살아 있다 |
| [R3] | «되돌림 표본이 662 이후 죽었다» → 표본을 갈아라 | ⚑ **정정** — 죽은 것은 표본이 아니라 **치환이 딴 자리를 맞았다** |
| §4-c | (등재문에 없음) | **신규** — 보유 칸 [장착] 뒤 토스트 문면이 `"⚔️ 전투력 +22"` 다 |

### ⚑ [R3] 의 진짜 뿌리 — `String.replace` 는 첫 자리 하나만 바꾼다

`§R` 되돌림 사본은 네 자리를 치환해 «664 이전» 을 복원한다. 그중 R3 은 펫 제목을 가리는 치환인데,
겨눈 문자열이 `  $('mtitle').textContent = it.n;` 한 줄이다. **662 가 `showSkillDetail` 의
`own ? it.n : '???'` 가드를 걷어내면서 똑같은 줄이 스킬 함수에 «먼저» 생겼다.**

```
· R1 칸 클릭      : 출현 1회 · 첫 자리 32286행 (showItem — 664)
· R2 보유 효과    : 출현 1회 · 첫 자리 32257행 (showItem — 664)
· R3 제목         : 출현 2회 · 첫 자리 28272행 (showSkillDetail — 662)   ← ⚑
· R4 버튼 라벨    : 출현 1회 · 첫 자리 28431행 (showItem — 664)
```

`probe728` [3-d]·[3-e] 가 이것을 **실증**한다 — 자와 같은 치환표로 만든 사본에서
**펫 제목은 `"나무 인형"` 실명 그대로**이고 대신 **스킬 제목이 `"???"` 로 되돌아간다.**
즉 [R3] 은 664 를 되돌린 것이 아니라 **662 를 되돌리고 있었다.**

⚠ **등재문 처방 ③(«표본을 662 이후에도 살아 있는 축으로 바꾼다»)을 그대로 따랐으면
치환은 여전히 662 를 맞고 [R3] 은 «되돌려도 안 빨개지는» 헛초록이 됐을 것이다.**
축을 바꿀 자리가 아니라 **겨냥을 고칠 자리**였다.

---

## §2 처방 — 「축을 갈라 적는다」(333 처방 · 항을 지우지 않는다)

### ① §5 — «스킬은 아직 «???» 다» → «대상 축이 갈려 있다»

옛 §5 는 «664 가 남의 구간을 대신 열지 않았다» 를 **«스킬은 아직 가려져 있다»** 로 재고 있었다.
662(주인 지시 2026-09-02 00:20)가 완료되자 그 표현은 **«662 를 되돌려라» 와 같은 말**이 됐다 —
333 이 다룬 «두 게이트가 서로 반대를 단언» 의 같은 꼴이고, 나중 지시(662)가 옳은 쪽이다.

**항을 지우지 않고 방향만 뒤집어, 뜻을 «대상 축» 으로 다시 적었다** — «미개방 정보 공개» 규약은
한 벌이고 **스킬은 662 가 · 장비·펫·코스튬은 664 가** 연다:

| 항 | 새 단언 |
|---|---|
| §5-a | 662 가 지운 미보유 대체문이 **되살아나지 않았다** + 그 자리를 `skillDescText(it)` 파생이 쓴다 |
| §5-b | 미보유 **스킬** 제목이 **실명**이다 — 「«?» 0건」 자는 **`verify662` [A] 에서 빌려 왔다** |
| §5-c | 열되 **상태는 안 잃었다** — 스킬 설명에 `.sk-un` 🔒 딱지가 남아 있다 |
| §5-d | 두 자리는 **다른 함수**다(`showSkillDetail` 28234행 · `showItem` 28327행 · 제목 줄 2자리) |

⚑ **§5-b 가 `verify662` 의 자를 빌린 것이 등재문의 마지막 경고에 대한 답이다** —
«두 자가 같은 칸을 각자 세면 다음 번복에서 또 갈라진다». 이제 662 의 표기 규약이 바뀌면
두 자가 **같이** 움직인다.

### ② §R — 치환을 `showItem` **뒤 구간에만** 건다 + [R0] 을 «개수» 로

```js
const head = RAW.slice(0, ITEM_AT);          //  662 구간 — 손대지 않는다
let  body  = RAW.slice(ITEM_AT);             //  664 구간 — 여기서만 치환
const hits = subs.map(([a,b]) => { const n = body.split(a).length-1; if(n===1) body = body.split(a).join(b); return n; });
```

⚑ **[R0] 을 «`old !== RAW`» 에서 «네 자리가 각자 꼭 한 번씩» 으로 바꾼 것이 재발 방지의 본체다.**
옛 [R0] 은 **넷 중 셋만 걸려도 초록**이었고, 그래서 이번 부패가 조용히 통과했다
(334·537 «개수가 아니라 목록» 과 같은 꼴). 여기에 [R0-b](사본에서도 `showSkillDetail` 구간은
안 건드려졌다)와 [R6](사본에서도 스킬은 실명)을 **음성항**으로 붙여 «사본이 통째로 딴 것» 을 기각한다.

### ③ §R2 신설 — 새 방향이 **무르지 않음**을 못박는다

§5 를 뒤집었으니 «뒤집은 쪽이 실제로 빨개지는가» 를 자가 스스로 증명해야 한다(334·368 규약).
`verify662` 의 `revert()` 표를 **그대로 빌려** 스킬 쪽을 도로 닫은 사본에서:

- [R2-a] 옛 대체문이 되살아난다(§5-a 빨강) · [R2-b] 제목이 다시 `"???"`(§5-b 빨강)
- [R2-c] `.sk-un` 딱지가 사라진다(§5-c 빨강)
- [R2-d] **그런데 펫은 그 사본에서도 실명**이다 — 두 구간이 정말 갈려 있다는 음성항

⚠ **1회차에 [R2-c] 가 빨갰다** — 662 되돌림을 두 자리(제목·desc)만 걸었더니 **`.sk-un` 딱지를
덧붙이는 줄이 살아남아** «되돌렸는데 §5-c 는 초록» 이 됐다. 옛 세계에는 그 줄이 없었으므로
되돌림은 거기까지 가야 참이다 → 셋째 치환(딱지 제거)을 더해 [R2-0] 이 **세 자리**를 센다.

---

## §3 곁다리로 같이 잡은 것 — §4-c (등재문에 없던 4번째 실패)

**등재 시점(40/43)에는 초록이던 항이 착수 시점(39/43)에 빨갛다.** 재현이 문면을 그대로 찍었다:

```
· 6프레임 뒤 : ["⚔️ 전투력 +22"]
· 36프레임 뒤: ["⚔️ 전투력 +22"]
```

§4-c 의 뜻은 «**반려**가 «항상» 뜨면 그건 새 결함이다» 인데, 구현은 `toasts() === 0` —
**모든 토스트를 세고 있었다.** 장착으로 `cp()` 가 오르니 324/684/685 가 세운 **산 연출**이
그 자리에 뜨는 것이 정상이고, 자가 그것을 반려로 오독한 것이다. 제품은 정상이다(`probe728` [4-a]).

⇒ 개수가 아니라 **문면**을 세도록 좁혔다. **무르게 푼 것이 아님을 두 겹으로 못박았다**:

- **한 자로 묶었다** — 새 문면 자 `REJECT` 를 §3-b(미보유에서 **뜬다**)·§3-g(코스튬)·§4-c(보유에서 **안 뜬다**)가
  **같이** 쓴다. 세 항이 각자 다른 눈으로 세면 한쪽이 부패해도 다른 쪽이 안 알려 준다.
- **§4-f 신설** — 그 자가 실제로 **가르는지** 를 산 표본으로 잰다:
  산 반려문 `"🔒 아직 미보유 장비입니다 …"` 는 잡고, 산 연출문 `"⚔️ 전투력 +22"` 는 안 잡는다.
  이 항이 초록이면 «반려가 보유 칸에 새면 §4-c 는 그대로 빨개진다» 가 성립한다.

⚠ 허용 오차는 한 칸도 안 넓혔다 — §4-c 는 여전히 «보유 칸에 반려 0장» 을 요구한다.

---

## §4 결과

| 자 | 착수 시점 | 지금 |
|---|---|---|
| `tools/verify664.js` | **39/43 FAIL** | **53/53 PASS** |
| `tools/probe728.js` | — | **16/16 신설** |
| `tools/verify662.js` | (아래 회귀표) | 무수정 |

**제품 `index.html` 은 한 글자도 안 건드렸다** — 319·333·334·343 계열(자만 고치는 T1 게이트 부패).
