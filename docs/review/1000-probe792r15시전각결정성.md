# 1000 — `probe792r15` 의 «(기록) 종:시전각» 줄이 판마다 갈린다 (잠복 · 자 · 기록값)

**완료(2026-09-06, sess-1727-3239 루틴 워커 B · 1회차) — 제품 `index.html` 0줄 · 자 `tools/probe792r15.js` 3자리 · `verify1000` 14/14 신설.**
지시서 [3]-**(가)** 로 닫았다(레퍼런스 대조가 아니라 «내가 적는 수가 두 판에서 같은가» — 비평가 없음).

## 1. 재현 (338 — 처방 전에 잰다)

손 안 댄 트리에서 `node tools/probe792r15.js` 를 **4판** 돌려 stdout 을 `diff` 했다.

| 판 | «종:시전각» 줄 |
|---|---|
| r1 | `ice:-1.399 · arrow:-1.464 · lance:-1.699 · gale:0 · slash:-1.399 · curve:-1.569` |
| r2 | `ice:-1.401 · arrow:-1.466 · lance:-1.701 · gale:0 · slash:-1.401 · curve:-1.571` |
| r3 | `ice:-1.376 · arrow:-1.441 · lance:-1.676 · gale:0 · slash:-1.376 · curve:-1.546` |
| r4 | `ice:-1.344 · arrow:-1.409 · lance:-1.644 · gale:0 · slash:-1.344 · curve:-1.514` |

**갈린 줄은 그 한 줄뿐**이고 나머지 출력은 4판 **바이트 동일** · 점수도 **20/20 PASS** 로 같다 ⇒
등재문대로 **«버그» 가 아니라 잠복**이다. 여섯 종 중 `gale:0` 만 안 흔들린다(제품 상수각).
흔들림 폭은 판별로 **0.002 ~ 0.055 rad**(0.1° ~ 3.2°).

## 2. 뿌리 — 등재문이 짚은 «용사 자리» 가 맞다, 다만 **한 겹 아래**다

등재문은 «이미 `player.x = WORLD.w/2` 로 세우고 있으므로 착수 전에 어디서 밀리는지부터 재라» 고 적었다.
재 보니 **그 줄(128행)은 §3 화소 자 앞에만 있었고**, 각을 만드는 §4·§5 의 시전은 **그 앞**에서 끝난다.
그런데 그 앞자리를 재 보니 **자리 자체는 이미 못박혀 있었다** — `spawnStage()` 직후 용사는 세 판 모두
정확히 **(960, 1536) = WORLD 중심**이다. 새는 것은 **속도**다:

| 판 | 부팅 직후 vx · vy | `spawnStage()` 직후 자리 | 첫 `step()` 뒤 자리 | 준비 루프 5틱 뒤 |
|---|---|---|---|---|
| 1 | 49.99 · 189.6 | 960 · 1536 | 961.207 · 1538.871 | 965.676 · 1549.505 |
| 2 | 141.86 · −180.29 | 960 · 1536 | 962.205 · 1533.348 | 970.373 · 1523.525 |
| 3 | 91.33 · −207.31 | 960 · 1536 | 959.999 · 1534.207 | 959.996 · 1527.568 |

`spawnStage()` 는 x·y 만 되돌리고 **vx·vy 는 안 지운다.** 남은 속도가 바로 다음 `step()` 에서 용사를 옮기고,
준비 루프(`while (enemies.length === 0) step(1/60)` — 세 판 다 **5틱**으로 결정적)가 그것을 **약 5배로 키운다**.
조준각은 `atan2(표적 − 용사)` 이므로 그 자리 차이가 그대로 각으로 나온다.
그 vx·vy 의 임자는 **부팅 프레임 수**다 — `goto` 뒤 `waitForTimeout(1100)` 동안 제품의 제 루프가
판마다 다른 프레임 수로 돌고, `requestAnimationFrame` 을 눕히는 것은 그 **뒤**다(**928-②** 와 같은 기계).

⚑ **등재문의 경고 — «996 의 처방을 그대로 옮기지 마라» 는 지켰다.** 996 쪽(`orbitAng`)은 일부러 `a = 0`
을 유지해야 하는 자리이고(`cap710` 머리말 «둘은 서로 반대가 맞다»), 여기서 고친 것은 **각을 싣는 방식이
아니라 각이 매달린 자리**다. `probe792r15` §1 의 하네스 계약 3항(`a: sp.a` · `a:0` 유지)은 **한 자도 안 건드렸다.**

## 3. 처방 — 못박기를 **한 곳**으로 모으고 발을 만드는 길이 그 곳을 지나게 한다

```js
const seatPlayer = () => { player.x = WORLD.w/2; player.y = WORLD.h/2; player.vx = 0; player.vy = 0; };
```

- `putFoe()` 가 표적을 세운 **뒤** 이것을 부른다 — 준비 루프의 `step` 이 옮긴 것까지 되돌려야 하므로 순서가 뒤다.
- 네 값은 **928 선례와 같은 네 값**이다(`verify856` 300행 · `probe928` 95행). 다른 값을 쓰면 두 자를 못 견준다.
- 128행의 사본은 **지웠다** — `putFoe()` 바로 뒤라 정확히 같은 일을 두 번 한다(402 «사본을 지운다»).
  `CX`·`CY` 는 그 자리에서 그대로 나오므로 §3 화소 축은 **한 화소도 안 움직인다**(수리 전후 출력 대조로 확인).

**결과**: 4판 stdout **바이트 동일** · 20/20 PASS 불변 · 수리 전 대비 갈린 줄은 **그 한 줄뿐**이다
(`ice:-1.42 · arrow:-1.485 · lance:-1.72 · gale:0 · slash:-1.42 · curve:-1.59`).
새 값은 이제 **기하에서 나온다** — 못박힌 자리 (960,1536), 표적 (990,1337) ⇒ `atan2(−199, 30) = −1.4213`.

## 4. 게이트 `tools/verify1000.js` — 14/14 신설

| 절 | 무엇을 묻나 |
|---|---|
| §1 소스 계약 6항 | `seatPlayer` 가 **네 값을 다** 못박는가(속도가 빠지면 병이 그대로다) · `putFoe` 가 그것을 부르는가 · **사본 1벌**인가 · 손잡이 둘이 있는가 |
| §2 결정성 3항 | 손 안 댄 **프로세스 3판**의 stdout 이 바이트 동일 · 시전각 줄의 값 갈래 **1개**(928-① «프로세스 N판이 판을 묻는다») |
| §3 흡수 3항 | 서로 반대인 두 «부팅 속도»(`600,-400` ↔ `-500,700`)에서 시전각이 같고, 그 값이 손 안 댄 판의 값과도 같다 |
| §R 되돌림 2항 | 못박기를 빼면 그 두 판이 **갈린다**(−1.615 ↔ −1.322) · 그래도 점수는 PASS ⇒ 이 병이 «판정» 이 아니라 «기록값» 임을 자기 안에서 못박는다 |

⚠ **§R 의 재료로 자연 표류를 쓰지 않았다(928-④).** 무보정 두 판을 견주면 두 판이 우연히 가까이 서서
**되돌림 자신이 플레이키**가 된다. `P1000_KICK=<vx>,<vy>` 로 **같은 크기를 결정적으로** 밀고,
`P1000_NOPIN=1` 로 못박기만 뺀다. 두 손잡이는 평소에 꺼져 있고 **기본 출력은 손잡이 추가 전과 바이트 동일**이다(대조 확인).

## 5. 회귀

`verify1000` **14/14 신설 PASS** · `verify995` 11/11 · `verify996` 15/15 · `verify999` 14/14 ·
`verify998` PASS · `verify856` 23/23 · `verify928` 10/10 · `verify981` PASS · **SMOKE PASS**.

`verify792` 는 **31/32** 로 빨갛다 — 실패 항은 **[E1] ③ 덩치**(`shuri:78 · stone:77 · arrow:162 · meteor:155 · lance:152`)
하나뿐이고, **수리 전 트리에서도 같은 31/32 · 같은 항**임을 `git stash` 대조로 확인했다(338·344 규칙).
792 는 살아 있는 2차 라운드 작업(15/20)이고 [E1] 은 **995 가 «그것이 결과물이다» 로 남겨 둔 그 항**이다 —
내 변경(자 3자리)과 무관하고 `verify792` 는 `probe792r15` 를 실행하지도 않는다(주석 참조 2건뿐).

## 6. 곁다리 등재 — 0건
