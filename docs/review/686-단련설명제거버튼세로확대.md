# 686 — 단련 «구간당 1레벨당 (단련석)n» 텍스트 제거 + 강화 버튼 세로 확대

> 주인 지시(2026-09-02 00:50) 원문: «훈련에 구간당 1레벨당 (단련석)1 이거 텍스트 없애주고
> 대신 그 단련석 강화버튼을 세로로 키워줘야함»
>
> 세션 `sess-1804-10088` 루틴 워커 A · 1회차

---

## §1 재현 먼저 (338 규칙) — `tools/probe686.js` 신설

처방 전에 «주인이 지목한 것이 **어느 노드**이고, 지우면 무엇을 잃는가» 를 물었다. **11/11**.

| 물음 | 실측 |
|---|---|
| ⓐ 지목된 텍스트의 자리 | **`.tc` 한 상자**다 — `<i>` = «(단련석)n» · `<s>` = «n~n 구간 · 1레벨당». 흩어져 있지 않다 |
| ⓑ 정말 중복인가 | **그렇다.** 세 구간(Lv50·150·250)에서 `.tc` 값 = `.tb`(버튼) 값 = `temperCost` = **1 / 3 / 6**. 화폐 아이콘도 두 자리 다 있었다 |
| ⓑ-3 `.tc` 만 아는 정보 | **«구간 경계» 문구 하나뿐**(«100~199 구간 · 1레벨당»). 값이 아니라 **비용식 설명**이다 |
| ⓒ 비는 세로 예산 | `.tc` 상변 **14** → 버튼 상변 **128** = **114px**. 버튼 74 → 최대 **188** 까지 가능 |
| ⓒ-3 목표치의 근거 | 같은 행 아이콘 액자 `.ti` 가 이미 **22..200 (178)** = «행 222 − 위 22 − 아래 22» 로 서 있다 |
| ⓒ-1 두 프레임 | 9:19·9:13.3 에서 행 기하가 **완전히 같다** — 404 의 «좁은 프레임에서 밀림» 이 여기엔 없다 |
| ⓓ 가로 여지(자릿수 스윕) | fs 31 여유 **61.9** · 34 → 41.7 · 38 → 14.7 · **42 에서 −10(넘침)** |

⚑ **재현이 «제거 범위» 를 정했다.** 주인 문구 «구간당 1레벨당 (단련석)1» 은 한 문장처럼 읽히지만
화면에서는 **위아래 두 줄**이고, 둘이 같은 상자(`.tc`)에 산다. 그래서 «설명 줄만 지우고 비용은 남긴다» 는
갈래가 성립하지 않는다 — 비용 숫자는 **버튼이 이미 말하고 있으므로**(ⓑ) 상자째 지우는 것이 정답이다.

---

## §2 무엇을 고쳤나 — 제품 `index.html` **5자리**

| # | 자리 | 변경 |
|---|---|---|
| 1 | CSS `.tr-tp>.tc` 계열 4줄 | **선언째 삭제**(614 «죽은 코드 금지» 규약과 같은 꼴) |
| 2 | CSS `.tr-tp>.tb` | `top:128 → 22` · `height:74 → 178` · `line-height:74 → 178`. **가로(`right:26` · `width:340`)는 한 픽셀도 안 건드렸다** |
| 3 | `temperRowTxt()` | 죽은 필드 `cost`·`seg` 를 **선언째** 제거(402 «사본을 지운다») · 쓰이지 않게 된 지역변수 `seg` 도 |
| 4 | `renderTemper()` | `.tc` 마크업 제거 — **자리를 새 부품으로 메우지 않는다**(614 규칙) |
| 5 | `liveTemper()` | `.tc i`·`.tc s` 갱신 두 줄 제거 — **297 «두 경로가 같은 식을 본다»** 를 지키는 자리 |

### 높이 178 은 발명이 아니라 **뺄셈**이다
비는 예산은 114px 이라 버튼을 **최대 188**(14..202)까지 키울 수 있었다. 그러나 그 수는 «남는 만큼» 일 뿐
근거가 없다. 같은 행의 아이콘 액자가 이미 **«행 222 − 위 22 − 아래 22 = 178»** 로 서 있고(612 가 그렇게 적어 두었다),
그 띠를 그대로 쓰면 **한 행의 두 덩어리(왼쪽 그림 · 오른쪽 버튼)가 같은 세로 밴드를 공유**하고 행 여백이
위아래 대칭(22/22)이 된다. ⇒ **22..200 · 178** 채택. `verify686` [2-c] 가 «아이콘과 같은 밴드» 로 이 근거를 못박는다.

### `.cic` 세로 정렬은 **재계산이 필요 없었다**
584 는 버튼 안 화폐 아이콘을 `vertical-align:-17.5px` 로 «잉크 중심 = 버튼 중심» 에 맞춰 두었다(위 10.5 / 아래 10.5).
높이와 함께 `line-height` 를 올리면 반여백이 위아래로 **같이** 늘어 baseline 이 정확히 `(178−74)/2 = 52` 내려가므로
**상대 관계가 보존된다.** 실측이 그대로 확인했다 — 아이콘 위 **62.5** / 아래 **62.5**(`verify686` [4-a]).
라벨 가로도 좌 131.97 / 우 131.97 로 대칭(670 [5-d] 회귀).

---

## §3 위임 규약 채택 — 라벨 잉크는 **안 키웠다**

등재문이 «잉크 크기 자체를 키울지는 위임 규약으로 판단·기록» 이라고 남긴 자리다.

| 안 | 대가 | 판정 |
|---|---|---|
| **ⓐ fs 31 유지(채택)** | 178 짜리 면에 31px 라벨 — 잉크가 작아 보일 수 있다 | ✅ 주인 지시는 «세로로 키워라» 였고, 584 의 **자릿수 예산이 살아 있는 제약**이다(여유 61.9 유지) |
| ⓑ fs 34 | 여유 61.9 → **41.7** | 잉크 +9.7% 뿐 — 대가에 비해 얻는 게 적다 |
| ⓒ fs 38 | 여유 61.9 → **14.7** | 10자리 최악에서 14.7px 은 584 가 340 으로 넓힌 이유를 거의 반납한다 |
| ⓓ fs 42 | **−10 = 넘침** | 기각(스윕이 직접 찍었다) |

**되돌림 방법**: `.tr-tp>.tb` 의 `font-size:31px` **한 값**만 바꾸면 된다(그때 `verify686` [4-c] 의 기대값도 같이).
주인이 «글씨도 키워라» 로 뒤집으면 ⓑ→ⓒ 순으로 올리고 `probe686` ⓓ 스윕을 다시 찍으면 된다.

---

## §4 게이트

### 신설
- **`tools/verify686.js` 25/25 PASS** — [1] 설명 텍스트 0건(**통짜·홀드 두 경로 다**) · [2] 버튼 기하(세로 686 · 가로 584 Δ0 · 아이콘과 같은 밴드) · [3] 이웃 침범 0(형제 전수 × 두 프레임 × 행 베벨 × 시트 안) · [4] 라벨 중앙 · [5] 값이 구간을 따라간다 · **[R] 되돌림 시험 4항**
- **`tools/probe686.js` 11/11 PASS** — 수리 전·후 **둘 다 초록**으로 끝나게 짰다(`.tc` 유무로 갈래를 고른다). 재현자를 «수리 전에만 초록» 으로 두면 다음 세션이 그 빨강을 **게이트 부패로 읽는다**(702·730·732 가 그 자리였다)
- `tools/cap686.js` — 채점 캡처 하네스(두 프레임 · 세 축을 서로 다른 비용 구간 1/3/6 에 세운다)

### 이관 5종 (333 처방 — 항을 지우지 않고 **과녁·방향**을 고쳤다)
| 자 | 무엇이 빨갰나 | 어떻게 이관했나 | 결과 |
|---|---|---|---|
| `verify612` | [D3]·[D5] 가 `.tc` 를 읽다 **즉사**(TypeError) | [D3] 과녁을 «오른쪽에 남은 상자» = 버튼으로 · [D4] 세로만 686 값으로 · [D5] **방향 반전**(«있다» → «없다») · **[D5b] 신설**(버튼 밴드 = 아이콘 밴드) | **26/26** |
| `verify584` | [7-c] 가 `.tc` rect 를 읽다 즉사 | 과녁을 `.ti` 로 옮기고 «비용 줄은 사라졌고 버튼이 그 세로를 먹어 아이콘과 같은 밴드에 선다» 로 | **54/54** |
| `verify210` | 「UI 가 현재 구간 비용을 적는다(주인 ⓑ-2)」가 `.tc` 를 읽다 예외 · 262 교훈 2ⓑ 의 «두 경로 같은 값» 도 | 과녁을 버튼으로. ⚑ **문구 한 줄이 하던 증언을 «세 구간을 실제로 걸어 보기»(Lv50/150/250 → 1/3/6)로 갈아 끼웠다 — 더 세다**: 옛 항은 문구를 지워도 초록이었지만 이 항은 값이 구간을 안 따라가면 빨개진다 | **97/97** |
| `verify613` | [2-c] 「비용 열·버튼이 아이콘으로 화폐를 말한다」 | 버튼만 보게 하고 **[2-c2] 신설**(«.tc 없음» — 되살아나면 빨강) | **16/16** |
| `verify670` | [1-c]·[5-a]·[5-b] 가 `.tc` 존재와 옛 상자 340×74·top 128 을 단언 | [1-c] 분리 + **[1-c2] 신설**(방향 반전) · [5-a] «가로 340 불변(584) · 세로 178(686)» · [5-b] «right 26 불변 · top 22» | **27/27** |

⚑ **이관에서 한 번 헛초록을 만들 뻔했다** — `verify210` 의 262-2ⓑ 항(홀드 경로 ↔ 통짜 경로 문자열 동일)은
읽는 목록에 `.tc i`·`.tc s` 가 있었다. 그것을 **그대로 두면** 두 경로가 **둘 다 null 이라서** 같아진다 =
«두 경로가 같은 것을 그린다» 가 아무것도 안 묻는 항이 된다. 살아 있는 노드만 읽게 줄이고, `.tc` 부재 자체는
[C] 절이 따로 못박게 갈랐다.

### 회귀 (무수정 전부 초록)
`verify660` **55/55**(★ **버스트 스폰 = 강화 버튼 상자 안** — 버튼 기하가 바뀌었으므로 이 자가 이번 작업의 필수 재실행이다. [C1] 은 버튼 rect 를 **실측으로** 읽어 상자 밖 스폰 0건) ·
`verify621` **42/42**(★ 눌림 왕복 — 틱당 원래 크기↔눌린 크기 · duty temper 30/51 = 58.8%) ·
`verify619` 53/53 · `verify491` 83/83 · `verify58` 30/30 · **SMOKE PASS**

대조 1건 — `probe670` ⓐ 가 6/7 로 빨갛지만 **수리 전 트리(`git stash`)에서도 같은 6/7** 이다.
그 항은 «버튼 라벨에 «단련» 이 있다» 는 **670 의 수리 전 재현 항**이라 670 완료 시점부터 빨간 것이고,
내 변경과 무관하다(338·344 규칙에 따른 직접 대조).

---

## §5 실측표 (행 기준 좌표 · 두 프레임 동일)

| 요소 | 수리 전 | 수리 후 |
|---|---|---|
| 행 `.tr-tp` | 998×222 · pitch 236 | **Δ0** |
| 아이콘 `.ti` | 22..200 (178) | **Δ0** |
| 이름 `.tn` / 레벨 `.tl` / 효과 `.td` | 18..68 / 72..112 / 118..186 | **Δ0** |
| 비용 열 `.tc` | 14..98 (x 552..972) | **없음** |
| 버튼 `.tb` | **128..202 (74)** · x 632..972 | **22..200 (178)** · x 632..972 (**가로 Δ0**) |
| 버튼 안 화폐 아이콘 여백 | 위 10.5 / 아래 10.5 | **위 62.5 / 아래 62.5** (대칭 유지) |
| 라벨 좌우 여백 | 대칭 | **131.97 / 131.97** |

레이아웃 변경은 **버튼 한 상자의 세로뿐**이고 밸런스·판정은 **0줄**이다.

---

## §6 채점 (지시서 [3]-(나) · 비평가 2인 독립)

1회차 — 캡처 `cap686.js` 두 프레임(1080×2280 · 1080×1600). 결과는 아래 «회차 기록» 에.

### 회차 기록
- **1회차(2026-09-01, sess-1804-10088)** — 제품 5자리 · 이관 5종 · 신설 자 2 + 하네스 1. 게이트 전부 초록 · SMOKE PASS.
