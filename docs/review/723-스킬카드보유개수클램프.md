# 723 — 스킬 카드 보유 개수가 «3/3» 으로만 뜬다 (주인 보고 2026-09-02 04:00)

> 주인 원문: «스킬부분은 개수가 분명히 존나 많이 뽑았는데도 3/3 이런식으로 현재 개수가 안뜸.
> 분명히 1002/3 이런식으로 존나 많을 상황인데 그거 수정해줘야함»

세션 sess-1648-12785 (루틴 워커 D) · 1회차 · 2026-09-01(UTC)

---

## 1. 재현 먼저 (338 규칙)

`tools/probe723.js` 를 신설해 **제품을 한 줄도 안 고친 상태에서** 등재문의 갈래 둘을 갈랐다.

| 갈래 | 뜻 | 처방의 크기 |
|---|---|---|
| ⓐ **표시만** 잘린다 | 저장값은 실보유인데 그리는 자리가 눌렀다 | 그리는 줄 하나 · 데이터 손실 0 |
| ⓑ **저장까지** 잘린다 | `save()`/`load()` 왕복에서 초과분 소실 | 데이터 버그 승격 · 719 합성 재료·환급 직결 · 세이브 이관 검토 |

**수리 전 실측(probe723)**

```
[2] 화면  — slash n=1002 · Lv.1 · 요구 2  ⇒ 카드 잉크 "2/2"        ← 주인이 본 그림
[3] 저장  — localStorage n = 1002 · load() 뒤 frag() = 1002        ← 안 잘린다
[5] 26 펫 — 같은 표본에서 "2/2"                                     ← 같은 결함이 복제돼 있다
⇒ 갈래 판정: **ⓐ 표시만**
```

⚑ **ⓑ 는 기각됐다.** 저장 경로에 `n` 상한 클램프는 소스에도 없고(`[1e]`), 왕복 실측도 1002 를
그대로 돌려준다. **모은 조각은 한 개도 안 사라지고 있었다** — 화면이 그것을 안 적었을 뿐이다.
⇒ 719(장비 합성 재료)·환급 계산에 **영향 없음**, 세이브 이관 **없음이 정답**(KEY 안 올림).

## 2. 뿌리 — 같은 값을 세 자리가 다르게 적고 있었다

| 자리 | 식 | 결과 |
|---|---|---|
| 07 스킬 격자 `renderSkill` | `have = own ? Math.min(frag(it.id), need) : 0` | **눌린다** |
| 26 펫 격자 `renderPet` | 같은 줄(07 구조의 복제) | **눌린다** |
| 05 장비 카드 `renderWpn` | `$('wpnBarT') = got + '/' + need` | 초과를 그대로 적는다 |

주인이 «장비는 17/16 처럼 뜬다» 고 대조군을 직접 지목했고, 실제로 **규약은 장비 쪽이 맞았다.**
07·26 두 자리만 어긋나 있었으므로 **장비에 맞추는 것**이 처방이다(새 규칙을 만들지 않는다).

⚠ 이 클램프는 «게이지 폭» 과 «숫자» 를 **한 값으로 겸하고 있었다.** 폭은 눌러야 맞고(넘치면
그릇 밖으로 나간다) 숫자는 누르면 안 된다 — 05 가 이미 그 둘을 갈라 두고 있었다
(`wpnBarF` 만 `Math.min(100, …)`). 그래서 처방도 **둘로 가른다.**

## 3. 처방 (`index.html` 5자리)

1. `renderSkill` — `have = own ? frag(it.id) : 0` (클램프 폐지)
2. `renderSkill` — 게이지 폭만 `Math.min(100, have/need*100)` (05 `wpnBarF` 와 같은 식)
3. `renderPet` — 위 둘과 같은 두 줄 (같은 부품이라 같이 편다)
4. `skBarFit(root)` 신설 — `.sk-bar>b` 잉크가 그릇(157px · `overflow:hidden`)을 넘으면
   **한 글자도 안 버리고 폭에만 맞춘다**(150 규약의 `fitNum` 을 그대로 부른다 · 53 가방 선례)
5. `renderSkill`/`renderPet` 의 `setBody` 반환값으로 4 를 부른다

⚑ **왜 `fmt()`(쉼표)를 안 붙였나** — 등재문 게이트가 «장비 카드와 같은 규약» 을 요구한다.
05 는 날 숫자를 적으므로 쉼표를 붙이면 두 화면의 같은 값이 다른 글자가 된다. 150 의 «개수는
숫자 그대로» 도 만족한다. 되돌리려면 두 자리의 `have + '/'` 를 `fmt(have) + '/'` 로 바꾸고
05 도 같이 바꾸면 된다(**위임 규약 채택** — 주인이 뒤집으면 그 한 줄로 되돌아간다).

⚠ **성능** — `renderSkill` 은 시트가 열려 있는 동안 0.35s 마다 돈다. `skBarFit` 은 ①`setBody` 가
내용 변경 시에만 노드를 돌려주고 ②9자 미만이면 `fitNum` 을 아예 안 부른다(fs21 숫자 한 글자
≈ 11.7px ⇒ 13자에서 157 을 넘으므로 9자는 실측 여유). 평범한 «1/2» 자리는 **잰 적도 없다.**

## 4. 게이트

`tools/verify723.js` **27/27 신설** — [S] 선언 4 · [A] 표기 2 · [B] 게이지 폭 3 · [C] 05 대조 2 ·
[D] 자릿수 9 · [E] 회귀 3 · [R] **되돌림 4**.

```
[D] 4자 "1002/2"            잉크  68.97 ≤ 157.00 · fs 21px
    10자 "1234567890/2"     잉크 143.23 ≤ 157.00 · fs 21px      ← 아직 안 눌린다
    13자 "1234567890123/2"  잉크 157.00 ≤ 157.00 · fs 18.77px   ← 여기서 fitNum 이 일한다
    17자                    잉크 157.02 ≤ 157.00 · fs 14.58px
[R] 옛 `Math.min(frag, need)` 를 되살린 사본 ⇒ "2/2" (= [A1] 이 실제로 빨개진다)
    원본은 같은 자로 다시 "1002/2"                              ← 무르게 잡은 게 아니다
```

⚠ **자를 짤 때 밟은 함정** — `.sk-bar>b` 는 `inset:0` 전폭 상자라 **그 bbox 로는 «잘렸는지» 를
영원히 못 잰다**(늘 그릇과 같은 157 이 나온다). 잉크는 `fitNum` 이 쓰는 눈(Range)으로 재야 한다.
1회차 초안이 실제로 그 값으로 «잘림 0» 을 초록으로 읽고 있었다.

`tools/probe723.js` **24/24 신설**(재현기 — 수리 후에도 PASS 하도록 [1b] 를 «클램프 0건» 으로 뒤집었다.
수리 전 값은 클램프 2건 · 실보유 0건이고 그것이 등재문의 재현이다).

## 5. 회귀

(§6 에 실행 결과)

## 6. 남긴 것 · 다음 세션에게

- **726**(일괄 강화 결과 팝업 6개 상한)은 **다른 자리**다 — 이 행은 «카드의 보유/요구 잉크» 뿐이다.
- **719**(장비 합성)에게: 조각 초과분은 **처음부터 안 사라지고 있었다**(probe723 [3]). 재료 수량을
  `frag()` 로 읽으면 그대로 맞다 — 표시 클램프를 데이터로 착각하지 말 것.
- **50 코스튬**(`#bCos`)은 같은 `.sk-bar` 부품을 쓰지만 «보유/요구» 축이 아니라(`.sk-bar.rq`·`.up`
  강화 진행도) 이 결함이 없다 — `Math.min(frag(...` 전수 grep 이 07·26 두 자리뿐이었다.
