# 760 — `tools/verify188.js` 즉사(`ReferenceError: trDeltaTxt is not defined`)

**T1 «버그(게이트 부패)» · 지시서 [3]-(가) 기계+자 · 비평가 없음 · 제품 `index.html` 0줄.**
세션 `sess-1900-32171` 루틴 워커 D · 1회차(2026-09-01).

---

## §1 착수 전 — 재현이 등재문을 그대로 확인했다(338 규칙)

등재문: 「`tools/verify188.js` 172·322행이 `trDeltaTxt()` 를 부르는데 660(주인 지시 «훈련·단련 숫자
플로터 폐지»)이 그 함수를 **선언째** 지웠다. 예외가 `page.evaluate` 밖으로 나가 게이트가 통째로 죽는다.」

수리 전 실행:

```
⚠ evaluate 예외 [verify188.js:136] page.evaluate: ReferenceError: trDeltaTxt is not defined
⚠ EVGUARD 731 — 삼켜진 evaluate 예외 1건 (1종)
page.evaluate: ReferenceError: trDeltaTxt is not defined
    at /home/user/kkkkkk/tools/verify188.js:136:23 { log: [] }
EXIT=1
```

행 번호만 등재문(172·322)과 다르다 — 그 둘은 **부르는 줄**이고, 죽는 자리는 그 줄을 감싼
`page.evaluate` 의 시작(136)이다. 나머지는 전부 그대로였다.

⚑ **«즉사» 의 대가가 등재문이 적은 것보다 한 칸 컸다.** 이 자는 보고서(`R.forEach`)가 **파일 맨 끝**이라,
③ 에서 죽으면 «③ 뒤가 안 돈다» 로 끝나지 않고 **이미 판정을 마친 ①·② 의 결과까지 같이 사라진다**
(`VERIFY188 n/m` 이 한 줄도 안 나온다). 그래서 ① 에 **이미 빨간 항이 하나 더 있었는데 아무도 못 봤다**
— §3 이 그 이야기다.

---

## §2 처방 ⓐ — `ev()`/`blk()` 로 «즉사» 를 «그 블록만 빨강» 으로

278·228 처방 · 319·708 선례. 부품은 **새로 짜지 않고** `tools/verify486.js` 머리말의 것을 그대로 옮겼다
(486 이 **똑같은 함수·똑같은 사고**로 이미 한 번 당한 자리다 — 708 등재문).

* `ev(page, fn, arg)` — 예외를 `{ __err }` 로 잡는다.
* `blk(r, 이름)` — 죽었으면 **FAIL 1건**으로 세고 `false` 를 준다. 호출부는 `if (blk(…)) { … }`.

감싼 곳 넷: ③ 런타임 표시면 · ③-b 08 세부 팝업 · §R3 표기층 센티넬 · ⑤ 기능 체크.
§R3 은 ③-b 가 집어 온 id·기대값 위에 서 있어 **③-b 가 죽으면 같이 건너뛴다**(거짓 빨강 5건 방지).

⚠ **② 표기층의 짧은 `p.evaluate` 는 안 감쌌다** — 그 자리가 죽으면 `fmtB` 자체가 없다는 뜻이라 블록을
이어 봐야 뒤 항목이 전부 거짓 빨강이 된다(486 이 `open()` 초기화 evaluate 에 내린 것과 같은 판단).

---

## §3 ⚑ 즉사 뒤에 **숨어 있던 빨강 1건** — 664 가 낸 두 번째 게이트 부패

수리하고 처음 끝까지 돌린 순간 **89/90** 이 나왔다. 빨간 항:

```
FAIL ① 싱크 — 07 펫 피해 — 세부 표 «피해량» 칸(ct3Now)  →  false (want true)
```

* 자가 기대하던 것: `ct3Now = () => cat === 'pet' ? (own ? fmtB(petDmg(it)) : '—')`
* 지금 제품(`index.html` 28635): `ct3Now = () => cat === 'pet' ? fmtB(petDmg(it))`

**제품이 옳고 자가 뒤처진 쪽이다** — 664(주인 지시 «미개방 정보 공개»)가 미보유 «—» 분기를 걷어 냈고,
바로 위 세 줄이 그 판단을 주석으로 적어 두고 있다. 제품은 그 사이에도 내내 `fmtB` 를 지났다.
같은 파일의 바로 위 항(343·662 이관)이 **글자 그대로 같은 사고**를 두 번 겪은 자리다.

⇒ 343·662 와 같은 처방: **자리만 지금 그 줄로 옮기고 묻는 것은 그대로 둔다**(fmtB 를 지나는가).
그리고 «옛 삼항이 없어졌다» 를 눌러 통과시키는 것이 아님을 짝 항으로 못박았다 —
**`①-664 펫 미보유 피해량이 «—» 로 되돌아가 있지 않다`** 신설(662 가 세운 짝과 같은 꼴 · 328-330 교훈).
안 세우면 «664 가 통째로 되돌아가도 초록인 게이트» 가 된다.

⚑ **이 1건이 이 작업의 두 번째 교훈이다.** 즉사가 가리고 있던 것은 «안 돈 절» 만이 아니라
**이미 빨갛던 판정**이었다. 자의 보고서가 파일 맨 끝에 있으면 즉사는 앞쪽 절까지 소급해서 삼킨다.

---

## §4 처방 ⓑ — 폐지된 «+n» 입, 333 방향 반전

`trDelta` 축은 486 이 «알약 = 최종값 / 플로터 = 증가분» 으로 카드를 두 입으로 가르면서 생긴 둘째 칸이고,
그 입을 660 이 닫았다. **자리를 비우지 않고 방향을 뒤집었다**(333):

| | 수리 전 | 수리 후 |
|---|---|---|
| ③ | `trDeltaTxt(카드)` 가 «+n» 을 알파벳 단위로 말한다 | **카드에 찍힌 글자에 «+n» 이 0건**(660 · 58 플로터 은퇴) |
| ⑤ [F1] | 같은 함수로 강화 전 값을 잰다 | **강화 직후에도 «+n» 이 0건** |

재는 것은 함수가 아니라 **카드에 실제로 찍힌 글자**다(`/\+\s*\d/`).

⚠ **살아 있는 후속 자리(`trainGainTxt`)로 옮기는 길은 안 골랐다.** 749 가 `probe486` 에서 그렇게 했고
그 선택은 그 자리에서는 옳았지만, `probe707` 이 이미 재 놓았듯 **`trainGainTxt` 는 호출부가 0**이다
(등장 1회 = 선언뿐). 188 이 묻는 것은 «표기층» 이고, 화면에 안 나가는 문자열의 표기층을 묻는 항은
678(죽은 코드 청소)이 그 함수를 걷는 날 **똑같이 다시 죽는다**. 지금 이 자를 고치는 이유가 그것이다.

⚠ **«훈련 플로터 0장» 자체는 겹쳐 세지 않았다** — `verify660` [D3] 이 계측기까지 세워 지키고,
`trDeltaTxt` 선언 0건은 `verify486` [F1] 이 지킨다. 188 이 이 자리에서 묻는 것은 표기층뿐이다.

**곁다리 강화 1건** — `③ 23 훈련 체력 표본이 접히는 값이다(stat.maxHp ≥ 10,000)` 신설.
`UNIT` 은 1000 미만이면 접미사 없이도 통과하므로, 표본 값이 언젠가 작아지면 «알파벳 단위» 항이
조용히 무뎌진다. 실측 195,395 — 여유는 크지만 **그 여유가 있다는 사실 자체를 항으로 적었다**.

**헛초록 방지(자 안)** — §R4·§R5 미끼: 같은 대조기에 «+12.3B» 를 흘려 **대조기가 살아 있는지**를 본다.
안 세우면 «카드 글자를 못 읽어도(빈 문자열) 초록» 이 된다(328-330 이관 교훈).

---

## §5 되돌림 시험 — `tools/probe760.js` **20/20 신설**

자 안의 미끼(§R4·§R5)는 «대조기가 살아 있는가» 만 본다. 「**제품이 되살아나면 빨개지는가**」와
「**ⓐ 처방이 실제로 즉사를 막는가**」는 자 밖에서 재야 해서 재현기를 신설했다.

| 절 | 묻는 것 | 결과 |
|---|---|---|
| [1] | 수리 전 판(`4075c67:tools/verify188.js`)이 죽고 **보고서를 한 줄도 못 찍는가** | ✓ 5/5 |
| [2] | 지금 판이 끝까지 돌아 PASS 하는가 | ✓ 3/3 |
| [3] | ③ 안에 죽은 입(`__dead760()`)을 심으면 — 프로세스는 살고 · **FAIL 1건**으로만 세고 · 원인 문자열을 들고 · **뒤 절은 계속 도는가** | ✓ 7/7 |
| [4] | 660 이 닫은 «+n» 을 카드에 되살린 사본에서 ③·⑤ 방향 반전 항이 **빨개지고**, 미끼 항은 **초록으로 남는가** | ✓ 5/5 |

[3-e]·[3-f] 가 «무르게 안 풀었다» 의 핵심이다 — 죽은 블록의 대가는 **FAIL 1건뿐**이고(③ 항 전체가
거짓 빨강으로 번지지 않는다) **총 항 수가 건너뛴 만큼 줄어 있다**(초록으로 메우지 않는다).

⚠ [1] 은 `git show <BASE>` 를 쓴다 — **756(얕은 클론에서 거짓 빨강)** 을 피해 그 SHA 를 못 읽으면
빨강이 아니라 **건너뜀**으로 세고 [2]~[4] 만 판정한다(`PROBE760_BASE` 로 갈아 끼울 수 있다).

---

## §6 결과

* `node tools/verify188.js` — **수리 전 즉사(보고서 0줄) → 91/91 PASS**
* `node tools/probe760.js` — **20/20 PASS 신설**
* **제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/verify188.js` 한 파일이다.
* 회귀는 §7.

## §7 회귀 — 9종 **무수정** 전부 초록

`pull --rebase` **뒤에** 돌렸다(654 교훈 — rebase 전의 낡은 자를 돌리면 남의 진행분을 내 빨강으로 오인한다).

| 자 | 결과 | 왜 이것을 골랐나 |
|---|---|---|
| `verify486` | **79/79** | 이 사고를 **먼저 당한 자** — `ev()`/`blk()` 의 원산지이고 [F1] 이 `trDeltaTxt` 선언 0건을 지킨다 |
| `verify660` | **55/55** | 함수를 지운 주인 지시 본인 — [D3] 이 «훈련 플로터 0장» 을 계측기까지 세워 지킨다(겹쳐 세지 않았다) |
| `verify664` | **53/53** | §3 에서 자리를 옮긴 `ct3Now` 의 주인 |
| `verify481` | **33/33** | 같은 줄의 `petDmg` 축 |
| `verify485` | **27/27** | 같은 설명문의 «장착 효과» 축 |
| `verify628` | **118/118** | 폐지된 «정산은 방금 오른 양» 축의 주인(707 이 같은 즉사를 겪은 자) |
| `verify731` | **36/36** | 삼켜진 evaluate 예외를 신고하는 `evguard731` 본체 — `ev()` 가 그 신고를 안 가리는지 |
| `probe707` | **20/20** | `trainGainTxt` 호출부 0 을 재 놓은 자(§4 의 판단 근거) |
| `probe486` | **13/13** | 749 가 «폐지된 입 → 살아 있는 자리» 로 옮겨 놓은 짝 |

* `node tools/smoke.js` — **SMOKE PASS**
* `node tools/verifyProgress.js` — 종료 코드 0

## §8 곁다리 관측 — **등재 0건**

`①-664`(§3)는 곁다리로 넘기지 않고 **여기서 고쳤다**. 판단 근거 둘:

1. **같은 파일·같은 병**이다 — 「660 이 지운 것을 자가 아직 찾는다」와 「664 가 지운 것을 자가 아직
   찾는다」는 같은 종류의 게이트 부패이고, 둘 다 **이 자를 초록으로 만들기 전에는 아무도 못 본다**.
2. 남겨 두면 `verify188` 이 89/90 으로 **빨간 채 마감**된다 — 760 의 완료 조건은 «자가 끝까지 돈다» 가
   아니라 «자가 제 일을 한다» 이므로 그 상태로는 닫을 수 없다.

작업 단위 1개 규칙을 넘지 않았음은 **제품 `index.html` 0줄**이 못박는다(고친 파일은 `tools/verify188.js`
하나 + 재현기 신설 하나).

## §9 다음 세션에게

* ⚑ **보고서를 파일 맨 끝에 모아 찍는 자는 «즉사» 에 두 배로 약하다.** 안 돈 절뿐 아니라 **이미 판정한
  절까지** 사라진다. 이 저장소의 자 상당수가 그 꼴이므로, `ev()`/`blk()` 를 넣을 때 «뒤 절이 도는가» 만
  보지 말고 **앞 절의 빨강이 새로 드러나는지**도 같이 봐라 — 여기서 실제로 1건 나왔다.
* ⚑ **폐지된 입을 «살아 있는 후속 자리» 로 옮길 때는 그 자리의 호출부 수를 먼저 세라.** 749 가 고른
  `trainGainTxt` 는 호출부 0 이고, 678(죽은 코드 청소)이 등재돼 있다 — 그날 `probe486` 은 또 죽는다.
  화면에 안 나가는 문자열은 «표기층» 을 물을 자리가 아니다.
* `docs/PROGRESS.md` 의 760 행은 칸이 **8개**였다(주 편집 구간이 따로 잘려 있었다) — 566 이 «7칸이 아닌
  행은 판정 불가로 센다» 고 적어 둔 그 모양이다. 759 와 같은 꼴로 앞 칸에 접어 7칸으로 맞췄다
  (**내용은 한 글자도 안 바꿨다** — 691 규약).
