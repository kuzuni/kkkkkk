# 859 — 89 유물 소환 팝업의 «그릇» 을 레퍼런스 비례로

> **등재**: 2026-09-03, sess-0628-8392 워커 B (813 4회차 채점 2인 CJ·CK **독립 일치** · 둘 다 1순위)
> **T4 «폴리시» · 813 의 선행.** 813 은 «여백 배분» 이 권한이고, 이 행은 «그 여백이 담긴 그릇» 이다.

---

## 1회차 (2026-09-03, sess-0716-30731 루틴 워커 A) — 제품 `index.html` 3자리 · 자 5파일

### 결론 먼저

- **등재문의 두 처방 중 ⓐ(CK — 부품을 그릇에 비례시킨다)는 실측으로 기각했다.** 가로가 안 맞는다.
- **ⓑ(CJ — 그릇을 캡한다)를 채택하되, 120 의 주인 지시(«검은 영역이 없게»)를 깨지 않도록
  «비는 자리를 비워 두지 않는» 형태로 구현했다** — 장면·금테·내용은 ref 비례로 캡된
  `.rw-bowl` 안에 들고, `.rw-panel` 자신은 같은 석벽/바닥 톤으로 위·아래를 이어받는다.
  ⇒ **검은 띠 0px**(`verify120` ③ 센티넬 그대로 초록) · **그릇 1:1.4139 = ref** · **부품 절대값 불변**.
- 그 결과 **813 이 두 달째 못 닫던 «간극이 프레임마다 몇 배로 변한다» 가 구조적으로 사라졌다** —
  E(격자↔수반)가 1841·1920·2280·2600 에서 **같은 값**(진폭 0.00px)이다. 859 전에는
  330.7 / 366.3 / 535.6 / 702.0 = **2.12배**(813 4회차 Q1 이 «프레임 간 3.72배» 로 1순위로 짚은 자리).
- **1600 은 픽셀 하나 안 움직인다**(패널 1292 < 캡 1527 이라 캡이 안 걸린다). `verify700` [C] 의
  1600 칸이 **빈 표**인 것이 그 증거다.
- 곁다리로 **16회차가 고쳤던 «계단 0단» 이 재발할 뻔한 것을 같은 회차에서 잡았다**(아래 §5).

### §1 재현 — 등재문 수치가 그대로 나온다 (338 규칙)

`node tools/probe813.js` (수리 전):

| | 1600 | 1841 | 1920 | 2280 | 2600 |
|---|---|---|---|---|---|
| 패널 높이 | 1292.3 | 1529.7 | 1607.5 | **1962.1** | 2277.3 |
| gt(격자 상변) | 228.5 | 300.6 | 333 | 487 | 638.3 |
| E = 격자↓수반 | 175.3 | 325.7 | 360.8 | **527.6** | **691.5** |
| 안내문 위:아래 | 0.6 | 0.6 | 0.6 | 0.6 | 0.6 |

⇒ 등재문의 «2280 패널 1:1.848 · E 가 프레임 따라 3.7배» 가 그대로 재현된다.
ref 환산 그릇은 **1080×1527**(측정표 서두 — ref 486×687 ×2.2222)이므로 2280 에서 **+435px**.

### §2 ⓐ(CK — 부품을 그릇에 비례) 를 **기각**한다 — 가로가 안 맞는다

CK 의 처방은 «슬롯 한 변 = 패널 안쪽 높이 × 0.0996» 이다(2280 → **198px** · 2600 → **230px**).
세로만 보면 닫히지만, **격자는 가로가 이미 꽉 차 있다**:

| | 슬롯 | 열 피치 | 격자 폭(4열) | 패널 폭 | 좌우 여백 |
|---|---|---|---|---|---|
| ref / 현행 | 151 | 247.8 | **896** | 1080 | 91 / 93 |
| CK 안 2280 | 198 | 324.9 | **1174** | 1080 | **−47** |
| CK 안 2600 | 230 | 377.4 | **1359** | 1080 | **−139** |

⇒ 2280 에서 **94px**, 2600 에서 **279px** 넘친다. 즉 ⓐ 는 «세로 비례» 를 사려고
**모두가 합의한 유일한 축인 «가로 1:1»**(브리핑 규칙 2 · 측정표 120 정오표 «대조가 유효한 축은
가로 1:1 과 요소 크기·행 피치뿐»)을 판다. 슬롯 151 은 ref 실측 151.1(**−0.1%**)이고
`verify100` ③ 이 «슬롯 151×151 ×10 · 행 피치 176» 을 89 측정 규격으로 못박고 있다.
**부품을 키우는 길은 닫혀 있다.**

### §3 ⓑ(CJ — 그릇을 캡) 를 채택하되 «남는 자리를 비우지 않는다»

CJ 안을 문자 그대로 쓰면(패널 자체를 1527 로 줄이면) `#relw` 바탕이 드러나
**120 이 주인 지시로 없앤 «검은 띠» 가 그대로 돌아온다**(2280 에서 위·아래 217px 씩).
`verify120` ③ 은 그 순간 빨개진다(센티넬 `#FF00FF` 가 영역 안에 보이면 FAIL).

⇒ **그릇과 채움을 분리한다.**

| | 859 전 | 859 후 |
|---|---|---|
| `.rw-panel` | 영역 전체 + 장면·금테·내용 | 영역 전체 + **이어받는 벽면**(#090C10 → #12130D) |
| `.rw-bowl`(신설) | — | `top:--rw-oy` · `height:--rw-ph` · `overflow:hidden` |
| `--rw-ph` | — | `min(100%, 1527px × --rwc)` |
| `--rw-oy` | — | `round(down, (100% − --rw-ph) / 2, 1px)` |
| 예산 변수 8종 | `.rw-panel` 기준 | **한 글자도 안 고쳤다** |

⚑ **예산 변수를 한 줄도 안 고친 것이 이 처방의 핵심이다.** `--rw-sp` 이하 여덟 변수는 전부
`100%` 를 품고 있고, 커스텀 속성의 `%` 는 **쓰는 자리에서** 풀린다. 내용이 `.rw-panel` 직속에서
`.rw-bowl` 직속으로 한 겹 내려가는 것만으로 그 `100%` 가 **그릇 높이**가 된다 —
10·13회차 주석이 «손자에서 쓰면 100% 가 그 부모 높이로 풀린다» 며 함정으로 적어 둔 바로 그 성질을
**의도적으로** 쓴 자리다. 그래서 제품 변경이 세 자리로 끝났다.

### §4 결과 — E 가 프레임의 함수이기를 그만두었다

`node tools/probe813.js` (수리 후 · 그릇 기준):

| | 1600 | 1841 | 1920 | 2280 | 2600 |
|---|---|---|---|---|---|
| 그릇 높이 | 1292.3 | 1504.1 | 1504.1 | 1504.1 | 1504.1 |
| gt | 228.5 | 284 | 284 | 284 | 284 |
| E = 격자↓수반 | 175.3 | **320.1** | **320.1** | **320.1** | **320.1** |
| 안내문 위:아래 | 0.6 | 0.6 | 0.6 | 0.6 | 0.6 |
| 붕괴·겹침 쌍 | **0건** | | | | |
| 120 최소 요구 위반 | **0건** | | | | |

`verify813` 눈금으로는 E = **323**(네 프레임 동일 · 진폭 0.00px)이고 **ref 342 대비 −5.6%** 다.
⚠ 두 자의 절대값을 섞어 쓰지 마라 — 팝업을 여는 방법이 달라 표본이 다르다(verify813 §머리말 규약).

**남는 어긋남은 813 의 몫으로 되돌려 준다** — 그릇 기준 gt 297.5 vs ref 320 · E 320 vs 342 ·
수반↓안내문 57.7 vs ref 29(**약 2배 — 이것이 813 5회차의 1순위 후보**) · 안내문↓그릇하변 34.6 vs 34.
813 4회차가 «세 요구가 서로의 목적지» 라고 적은 삼각은 **꼭짓점(470px)이 사라져 풀렸다.**

### §5 곁다리 — «계단 0단» 재발을 같은 회차에서 잡았다 (제품 1자리)

캡을 넣자 **다섯 프레임 전부 계단이 0단**이 됐다(16회차가 «1920 이 1단 → 0단» 으로 되돌린 결함의 재발).
뿌리는 **1px** 이다:

- 아래 예약 상수 **137** = 받침 40 + 접합선 띠 13 + 계단 1단 84.
- 그런데 계단 구간 식은 `sh − gd − 14 ≥ 84`, 즉 **sh ≥ 138** 을 요구한다.
- 859 전에는 긴 프레임이 이 상한을 안 골랐다(비 규칙이 이겼다) ⇒ 1px 모자람이 **드러날 자리가 없었다**.
  캡이 걸리자 네 프레임 전부 이 상한을 골라 sh 가 **정확히 137** 이 됐다.

⇒ 137 → **139**. `sh` 139 ⇒ 계단 **1단 복귀**(1841~2600 전부 · 1600 은 원래 0단).
⚠ 올리는 방향은 안전하다 — 16회차의 경고는 «**낮추면** 바닥을 빼앗긴다» 였다.

### §6 게이트 이관 — 5파일 (333 처방: 자리를 비우지 않는다)

| 자 | 전 | 후 |
|---|---|---|
| `verify120` | 140/140 | **169/169** — ② 세 항(`bg`·`frame`·`fc`)의 «패널» 을 «그릇» 으로 · **[859-a] 그릇 = ref 비례** · **[859-b] 그릇이 패널 세로 중앙** 신설 · ⑥ 예산을 그릇 위에서 · ⑥ [L1] 에 **세 번째 경우**(상한이 이김) 추가 |
| `verify813` | 15/15 | **16/16** — 눈금을 그릇으로 · **[6a] 축을 «값» → «상수성»**(네 프레임 한 값 + ref 342 ±10%) · **[6b] 를 «네 여백의 합 = 그릇 예산»** 으로 · **[R3] 을 859 캡 해제 되돌림**으로 갈아 끼움 · **[R4] 신설** |
| `verify700` | 25/26 | **26/26** — [C] `ALLOW813` 표를 859 이동 **36건**으로 갈아 끼움(이름과 값으로. 1600 은 **빈 표** = 안 건드렸다는 증거) |
| `verify432` | 39/44 | **44/44** — 넘침을 담는 상자를 그릇으로 |
| `probe813` | — | 재는 상자를 그릇으로(그릇이 없으면 패널로 떨어진다 — 되돌림 사본 대비) |

**무수정 초록**: `verify100` 44/44 · `verify754` 60/60.

⚑ **[L1] 의 세 번째 경우**가 이 이관에서 가장 조심한 자리다. 859 전에는 «규칙이 안 먹는» 경우가
1600 하나뿐이었고 그때는 **하한**이 이겨 gt 가 규칙보다 **위로** 갔다 — 그래서 옛 else 는
`gt >= gtRule` 한 줄이면 됐다. 캡이 걸리자 긴 프레임은 **상한**이 이겨 gt 가 규칙보다
**아래로** 4.1px 내려온다(방향이 반대다). 예약 139 를 낮춰 규칙을 억지로 먹이는 길은
계단 1단을 다시 죽이므로, «비가 ref 대역(48±1%) 안이고 세 프레임이 같은 값인가» 를 묻는 항으로 세웠다.

⚑ **[R4] — 죽은 지렛대를 죽었다고 적었다.** 캡 뒤로 `--rw-lt` 는 `clamp()` 하한(20)이 이겨
813 2회차 [E3] 의 «벽 폭 294» 항이 **한 번도 안 골라진다**. 그 자리에 옛 되돌림 시험을
그대로 두면 «사본에서 안 빨개진다» 로 영원히 빨간 항이 되므로, R3 은 **859 자신의 되돌림**
(캡 해제 ⇒ E 진폭 0 → 371px)으로 갈고 [E3] 이 무력해진 사실은 R4 가 «되돌려도 0.00px 변한다» 로 못박았다.

### §7 되돌리는 법

`--rw-ph` 를 `100%` 로 되돌리면 `--rw-oy` 가 0 이 되어 **859 이전과 항등**이다(`.rw-bowl` 이
패널과 같은 상자가 된다). 예약 139 → 137 은 그와 별개로 되돌릴 수 있다.

### §8 2회차가 할 일

1. **채점**(지시서 [3]-(나)) — `tools/cap754.js --only 89 --set r6` 로 다섯 프레임을 찍고
   비평 2인. 브리핑은 `docs/review/813-비평브리핑-r4.md` 를 그대로 쓰되 **규칙 2 를 고쳐야 한다**
   («세로는 프레임마다 다르다» 가 이제 거짓이다 — 1841 이상은 그릇이 **같은 크기**다).
2. 물어야 할 것 — **«그릇 밖 벽면이 «이어지는 벽» 으로 읽히는가, «팝업이 영역을 안 채운다» 로
   읽히는가»**. 이것이 120 주인 지시와의 유일한 접점이고, 만약 후자면 되돌림 방법은 §7 에 있다.
   (위임 규약 2026-09-01 채택 — 주인 상신으로 회차를 세우지 않고 권고안을 채택해 진행했다.)
3. 남은 어긋남 셋(§4)은 **813 5회차의 재료**다 — 859 가 그릇을 잡았으므로 813 의 삼각이 풀린다.

---

## 2회차 (2026-09-03, 같은 세션 sess-0716-30731) — 제품 2자리 · **채점 2인 CL·CM · ①~④ 최저 4 / 4**

### 결론 먼저

- **두 채점자가 독립으로 같은 1순위를 냈다** — «금테 바깥의 이어받는 면이 **벽이 아니라 무텍스처
  단색 매트**다». 1회차는 그 자리를 `.rw-bg` 의 **재료 톤**(#090C10 / #12130D)으로 깔았는데,
  합성 **결과 톤**(내부 벽 평균 #362F24)보다 **4.07배 어두웠고** 행 표준편차가 **0.00~0.04**였다.
  ⇒ 「같은 벽이 이어진다」 가 아니라 「검은 바탕에 뜬 팝업」 으로 읽혔다.
- **1회차 처방의 뼈대(그릇을 ref 비례로 캡)는 둘 다 유지하라고 했다** — CM 은 처방까지
  «장면만 가용영역으로 늘리고 금테+내용은 1527 고정» 으로 적었고, 비례 쪽 실측은
  «슬롯 +0.07% · 격자 −0.04% · 수반 −0.7% — 잘 지켜졌다» 로 확인했다. 깨진 것은 **채움** 뿐이다.
- 2회차가 한 것 둘:
  1. **채움면을 «결 있는 벽» 으로** — 켜 줄눈·세로 이음매를 `.rw-bg::before` 와 **같은 주기·같은
     위상(132px)** 으로 깔고, **좌·우 벽기둥(`.rw-pil` 무늬)을 띠에도 이어** 붙였다.
     행 표준편차 **0.0 → 12.2**(내부 13.0) · 경계 휘도비 **3.00배 → 1.09배** · 단일색 행 **0행**.
  2. **중앙 정렬 → 하단 앵커** — 아래는 §9.
- ⚑ **채점 중에 «859 가 420 을 깼다» 를 회귀가 먼저 잡았다**(아래 §9). 그 수리가 곧 하단 앵커다.

### §9 중앙 정렬을 버린 이유 — **남의 게이트가 먼저 말했다**

1회차는 그릇을 세로 중앙에 뒀다. 그러자 수반이 2280 에서 **184.7px 위로** 올라가
33 재화 팝업(`#ciw`)이 「유물 소환」 버튼을 **40.2px** 물었다 — 420 이 닫아 둔 자리다
(`verify420` **22항 빨강** · 1900 −24.7 · 1924 −36.7 · 1931 −39.7 · 1950 이상 −40.2 로 포화).

**팝업 쪽에서 푸는 길을 먼저 시도했고, 그 길이 막혀 있음을 자가 말했다** — `#relw.on ~ #ciw` 의
`max(0px, 1931px − H)` 에 하한 74px 을 넣으면 겹침은 −3.2 까지 줄지만
**«[2280] 팝업 하변 1509.5 — 9:19 기준은 한 칸도 안 움직였다»**(63 검산값)와
«교차점에서 정확히 234px» 이 대신 깨진다(80/108). 남의 게이트가 못박은 값이라 그쪽은 길이 아니다.

⇒ **그릇을 하단 앵커로 바꾼다.** 하변이 패널 바닥(= 탭바 상변)에 붙으면 수반이 탭바에서
떨어진 거리가 859 전과 같은 축에 남고, 오히려 **41px 더 내려간다**(패널 하변↔수반 하변
223 → **181.7**, 캡이 걸린 네 프레임 전부 같은 값) — `#relw.on ~ #ciw` 선언을 **한 글자도 안 고치고**
420 이 초록으로 돌아온다.

부수 효과로 **채점자 둘의 다른 지적도 같이 닫힌다**: 1회차의 위·아래 두 띠는 색이 서로 달라
(위 L 11.7 한색 ↔ 아래 L 18.4 난색) «하나의 벽으로도 안 읽힌다» 는 지적을 받았는데,
하단 앵커에서는 **이어받는 면이 위 하나뿐**이라 톤도 하나다. 그리고 레퍼런스 장면이
«위가 벽 · 아래가 바닥» 이므로 남는 세로가 **위로** 가는 것이 물리적으로도 맞다.

| 프레임 | 패널 | 그릇 | 위 띠 | 아래 띠 |
|---|---|---|---|---|
| 1600 | 1312 | 1312 | **0** | 0 |
| 1841 | 1553 | 1527 | 26 | 0 |
| 1920 | 1632 | 1527 | 105 | 0 |
| 2280 | 1992 | 1527 | 465 | 0 |
| 2600 | 2312 | 1527 | 785 | 0 |

### §10 채점 결과 — 두 사람의 수치 (①~④ 최저 **CL 4 / CM 4**)

| 축 | CL | CM |
|---|---|---|
| ① 배치 | 6 | 5 |
| ② 크기 비율 | 7 | 8 |
| **③ 여백·간격** | **4** | **4** |
| ④ 정렬·그리드 | 8 | 7 |
| ⑤ 색·명암(비차단) | 3 | 3 |
| ⑥ 폰트(비차단) | 7 | 7 |

**⚑ 규칙 2 는 둘 다 «사실» 로 확인했다** — CM: 네 프레임을 금테 외곽 기준 1527행 정렬해 화소를 차분하니
**1,649,160px 중 460px(0.028%)** 만 달랐고 그것도 수반 물결 애니메이션과 탭 글로우뿐이다.
CL: 랜드마크 상대좌표가 **1px 오차도 없이** 일치.

**2인 독립 일치 — 남은 결함은 «하단 스택이 통째로 위로 밀렸다» 하나다**

| 자리 | ref 환산 | 현행 | CL | CM |
|---|---|---|---|---|
| 격자 상변 | 317.8~320.0 | 284~288 | −33.8 | −32.0 |
| 수반 상변 | 1173.3 | 1131 | −42.3 | −42.3 |
| 수반 하변 → 안내문 위 | 28.9~33.4 | 64~67 | **+100.6%** | **+121%** |
| 안내문 아래 → 금테 하변 | 22.2~24.4 | 37~40 | +63.9% | +67% |
| 격자 하변 → 수반 상변 | 351.1~355.6 | **341~346** | −2.7% | −2.9% |

⇒ **Q2(격자↔수반)는 둘 다 «결함 아님»** 으로 닫혔다(−2.7~−2.9%, 네 프레임 편차 **0px**).
813 이 두 달 동안 1순위로 붙잡고 있던 그 칸이다. 남은 것은 **하단 두 여백**이고, 둘 다
«원인은 여백이 아니라 스택 오프셋 하나» 라고 적었다(CL «+40px 아래로» · CM «격자 +32 · 수반 +42»).
**이것이 813 5회차의 재료다** — 859 의 권한(그릇)이 아니라 813 의 권한(배분)이다.

### §11 2인 일치이나 이 작업 단위 밖 — 등재만

- **「유물 소환」 라벨 폭만 −12.0%**(CL −12.0 / CM −12.0 · 높이는 +2.9%/−0.4% 로 정상 · 안내문·Lv 는 ±1%).
  «높이는 맞는데 폭만 12% 좁다» 로 둘의 수치가 소수점까지 같다 ⇒ **860 으로 등재**.
- 3열 행 피치 246/249 비대칭 3px(CM ⑦ · ref 는 247.8/247.8 대칭) — 같은 등재문에 붙였다.
- 금테 획 두께 — CL «+58%» / CM «+13% ~ +125%, 정의에 따라 갈린다» 로 **두 사람의 마스크가 갈렸다.**
  CM 이 «ref 는 [검정1·밝은금1·어두운금1·검정1] 4겹» 이라고 갈라 적었으므로 **정의부터 정하고**
  잡아야 한다(A1 10회차 «계측 정의가 다르면 일치해도 틀린다»). 등재 보류.

### §12 CM 의 «σ_L ≥ 15» 는 채택하지 않았다 — **내부보다 엄격하다**

CM 의 통과 기준 셋 중 둘은 그대로 받았다(경계 휘도비 ≤ 1.25 → **1.09** · 단일 RGB 100행 연속 0행 → **0행**).
셋째 «σ_L ≥ 15» 는 **금테 안쪽 벽 자신이 12.6~13.0** 이라 그 기준을 대면 **내부가 먼저 빨개진다.**
⇒ CL 의 «≥ 12» 를 받아 **12.2**(2280) / 11.3(2600)에서 멈췄다 — 내부와 **같은 급**이 목표이지
내부보다 결이 굵은 벽은 오히려 «다른 벽» 이다.

### §13 3회차가 할 일

1. **재채점**(캡처를 다시 찍어라 — 2회차 제품이 1회차 캡처와 다르다). Q1 이 닫혔는지가 전부다.
2. Q1 이 닫혔으면 **③ 의 남은 결함은 813 의 몫**이므로 859 는 마감하고 813 5회차로 넘긴다.
   Q1 이 안 닫혔으면 **되돌림은 `--rw-ph` → `100%` 한 줄**이고(§7), 그때는 «레퍼런스 비례를
   포기하고 120 주인 지시를 지킨다» 가 답이라는 뜻이다 — 그 판정을 review 에 명시로 남겨라.

---

## 3회차 (2026-09-03, 같은 세션 sess-0716-30731) — 제품 1자리 · **재채점 2인 CN·CO · ①~④ 최저 4 / 4**

### 결론 먼저 — **Q1 의 등재 결함은 닫혔다. 남은 ③ 은 859 의 권한 밖이다.**

두 사람이 **각자** 2회차 제품을 재고 같은 결론에 도달했다:

- **«팝업이 떠 있다» 로 읽히는 프레임은 하나도 없다**(CN 원문). 1회차의 단색 매트가 만든
  결함은 **완전히 사라졌다** — 「직전 행과 완전히 동일한 RGB 행」이 2600 의 785px 벽면에서
  **0개**(1회차엔 위 388 + 아래 388행) · 행당 고유색 중앙값 **132색** · 경계 휘도비
  **3.00배 → 1.03배**(CN) / **3.14배 → 0.96~1.14**(CO) · 좌·우 벽기둥이 x 71–83 / 995–1007 로
  **금테를 정확히 같은 x 로 관통해 이어진다**(CO «이건 확실히 성공»).
- **2280 은 CN 이 «같은 벽이 위로 이어지는 면으로 읽힌다 — 결함 아님» 으로 닫았다.**
- 남은 ③ 4점의 내용물은 **하단 스택**이고, 그것은 **813 의 권한(여백 배분)**이다 — 아래 §15.

### §14 3회차가 고친 것 — 두 사람이 각자 찾은 «같은 벽» 의 잔여 결함 둘 (제품 1자리)

**⓵ 켜 줄눈의 «위상 고정점» 이 틀렸다 — 주기는 맞췄는데 기준이 달랐다.**
2회차는 주기(47px)와 세로 이음매 위상(132px)은 맞췄지만 **가로 켜 줄눈의 세로 위상**은 안 맞췄다.
바깥 줄눈은 **화면 상단**에 고정(y ≡ 14 mod 47)이고 안쪽 줄눈은 **금테 상단**에 고정이라,
금테를 건널 때 위상이 **프레임마다 다르게** 어긋났다:

| | 1841 | 1920 | 2280 | 2600 |
|---|---|---|---|---|
| 3회차 전 위상차(CN 실측) | **21px (45%)** | 11px (23%) | 5px (11%) | 14px (30%) |
| **3회차 후(자체 실측)** | — | — | **0** | **0** |

⚑ **«프레임마다 어긋남이 달라진다» 가 진단이다**(CN) — 두 벽이 서로 다른 기준에 붙어 있다는 뜻이다.
⇒ 켜 층에 `background-size:100% 47px` 를 주어 **47px 타일**로 만들고
`background-position-y` 를 **`--rw-oy`(그릇 상변)** 로 준다. 남은 상수 −5px 는 «안쪽 첫 켜선이
그릇 상변에서 5px 아래» 라서 붙는 값이고, **그 어긋남이 프레임 무관 상수로 굳은 것 자체가**
두 벽이 같은 기준에 붙었다는 증거다(전에는 5/11/14/21 로 갈렸다).

**⓶ 바깥에만 있는 세로 이음매를 뺐다.**
2회차가 σ 를 올리려고 얹은 «주기 97» 한 겹이 x≈30~53 · 1018~1023 에 이음매를 만들었는데
**그 자리는 안쪽에 대응이 없어 금테에서 뚝 끊겼다** — CN «바깥 이음매 28개 중 안쪽 대응이 12개(43%)»,
CO 도 같은 자리를 독립으로 짚었다. ⇒ **뺐다.** 행 표준편차는 **얼룩**이 받는다(안팎에 다 있는 부품).
같이 켜 줄눈 대비도 안쪽에 맞춰 낮췄다(밝은 .55 → .42 · 어두운 .50 → .34).

**결과** — 행 표준편차 **11.9**(2280) / 10.2(2600) vs 내부 12.9 / 12.6 · 채움면 평균 46.0 vs 내부 42.2(비 **1.09**).

### §15 남은 ③ 은 **813 의 몫**이다 — 두 사람의 수치가 그것을 가리킨다

| 구간 | ref 환산 | 현행(네 프레임) | CN | CO |
|---|---|---|---|---|
| 금테상변 → 격자상변 | 311.1~316.7 | 279~286 | −10.3% | −9.5% |
| 격자하변 → 수반상변 | 348.9~346.7 | **346 / 343.5** | **−0.8% (결함 아님)** | **−0.9% (결함 아님)** |
| **수반하변 → 안내문 위** | 26.7 | **63~64** | **+140%** | **+136%** |
| **안내문 아래 → 금테하변** | 17.8~23.3 | **35~40** | **+97%** | **+72%** |

⇒ **Q2 는 두 사람 모두 «결함 아님»** 이고 네 프레임 편차 **0px** 이다 — 813 이 두 달 1순위로
붙잡던 칸이 닫혔다. 남은 것은 **수반 아래에 고인 53~54px** 이고, 둘 다 처방을 «그 여백을 줄여
회수한 세로를 상단으로» 로 **같은 값까지 계산해 놓았다**(CN 64 → 27 · 35 → 18 · 상단 279 → 311 ·
CO 63 → 27 · 40 → 23 · 상단 286 → 317). **그것은 여백 배분 = 813 의 권한이다.**

⚠ **1600 은 859 가 손대지 않는다** — 두 사람 다 «격자하변→수반상변 −43%» 를 1순위 급으로 짚었지만,
그 프레임은 **캡이 안 걸려 859 이전과 픽셀 동일**하다(패널 1312 < 1527). 즉 859 가 만든 결함이 아니라
**813·120 이 이미 알고 있던 «1600 은 예산이 없다»** 자리다. CN 의 처방(«내부를 0.861배 균일 축소»)은
ROUTINE [2] 의 «절대 전체를 비율로 축소하지 마라» 와 정면으로 부딪히므로 **그대로 채택하면 안 된다** —
813 이 그 자리를 잡을 때 이 충돌을 먼저 판정해야 한다.

### §16 2인 일치이나 이 작업 단위 밖 — 등재 갱신

- **860 에 항이 늘었다**: Lv 라벨 잉크가 슬롯 밖으로 **9px(잉크 높이의 35%)** 나온다(CN ⑩ — ref 는 100% 안쪽) ·
  코스트 필 **−10.0%**(CN) / −13.9%×−17.7%(CO) · 계단 두 단의 **폭이 완전히 같다**(618/618 — ref 는
  112 → 152px 로 +36% 벌어져 원근을 만든다, CO ⓹).
- **들보가 금테에 너무 붙었다**(CN Q4 «금테 안쪽 상변 + 11px = 0.73%» vs ref 는 같은 자리에 규칙선이
  **0개**이고 20.4% 를 비워 둔다 · CO 도 «18px = 1.18% vs ref 6.45%»). 두 사람 다 결함으로 판정했고
  수치도 같은 방향이다 — 다만 이것은 **상단 여백 배분(813)과 한 몸**이라 813 5회차가 같이 봐야 한다.

### §17 4회차가 할 일

**859 는 여기서 마감 후보다.** 등재 결함(«그릇이 ref 비례보다 +31% 길다»)은 닫혔고
(그릇 1:1.4139 · 네 프레임 픽셀 동일 · Q2 −0.8%), 1회차가 만든 부작용(단색 매트)도 닫혔다.
남은 ③ 4점은 **전부 813 의 권한**이고 두 사람이 처방을 숫자까지 남겼다.
⇒ **4회차는 «859 마감 + 813 5회차 인계» 로 잡는 것을 권한다.** 재채점을 한 번 더 돌리려면
813 의 하단 재배분을 **먼저** 넣고 같이 찍어라 — 안 그러면 같은 ③ 4점이 세 번째로 나온다.
