# 669 — 소환 레벨·소환 경험치 글씨 확대 (주인 지시 2026-09-02 01:02)

> 주인 원문: «소환레벨, 소환경험치 글씨 크기 너무작음. 좀더 키워줘 가독성 좋게»
> 자리 = 10 상점 «소환» 탭 배너 카드의 **레벨 알약 `.clv>i`** 와 **경험치 게이지 숫자 `.cbar>b`**.
> 655(«덮여서 안 보임»)가 닫힌 바로 그 자리의 **다른 축**(«작아서 안 보임»)이다.

세션 `sess-1443-3674` 루틴 워커 C · **2회차** · 제품 `index.html` **4값**(fs 2 · 바 높이/top · line-height·transform).

---

## 1. 자(尺) — `tools/probe669.js` 신설, 잉크는 «찍힌 픽셀» 로 잰다

레이아웃 상자가 아니라 **글리프 잉크**를 잰다(340·350 처방). 그 노드만 `visibility` 로 껐다 켠
두 장의 차분이 잉크 마스크다. Range bbox 는 **줄 상자**라 `line-height` 를 그대로 먹으므로
«글씨 크기» 의 자가 못 된다(알약 line-height 44 → Range 높이가 늘 44).

⚠ **잡음 함정을 하나 다시 밟았다.** 첫 실행의 표가 «Lv.1 잉크 72×60» 처럼 말이 안 됐다 —
`.shp-card{filter:drop-shadow}` 때문에 **자식 하나를 감추면 카드 그림자 실루엣이 바뀌어 레이어가
통째로 다시 래스터**된다(probe655 §3 이 이미 적어 둔 함정). 재는 대상은 글리프이지 카드 그림자가
아니므로 **잴 동안만 그 필터와 상시 광택 레이어를 끈다**(좌표·글꼴은 한 값도 안 건드린다).
그 뒤 **널 대조(같은 상태 두 장) 0픽셀** — 아래 수치는 전부 그 위에서 잰 것이다.

⚠ **알약은 rect 가 아니라 stadium(r=22)이다.** 모서리에서 그릇이 좁아지므로 bbox 로 재면
«rect 안» 인데 곡선 밖인 글리프를 통과시킨다. ⇒ [B1b] **캡슐 여유**를 행마다 잰다.

### 수리 전 실측 (두 프레임 **완전 동일** — 이 카드는 절대배치라 프레임과 무관하다)

| 표본 | Lv 잉크 w×h | 알약 여유 L/R/T/B | 경험치 잉크 w×h | 트랙 여유 L/R/T/B |
|---|---|---|---|---|
| Lv.1 · 409/410 | 40×**17** | 24/26/14/14 | 62×**15** | 123/116/6/7 |
| Lv.10 · 655/2300 | 58×17 | 16/16/14/14 | 72×15 | 118/111/6/7 |
| Lv.31 · 655/6710 (주인 표본) | 54×17 | 17/19/14/14 | 70×15 | 119/112/6/7 |
| Lv.49 · **10489/10490**(최악 문자열) | 63×17 | 13/14/14/14 | 96×15 | 106/99/6/7 |
| Lv.50 · MAX | 63×17 | 14/13/14/14 | 32×15 | 138/131/6/7 |

⚑ **막는 축이 둘 사이에 정반대다** — 이것이 이 작업의 뼈대다.
- **레벨 알약**: 그릇 89×44 인데 가장 긴 «Lv.49/Lv.50» 잉크가 **63px** ⇒ 배율은 **가로가 먼저 막는다**
  (k ≤ 77/63 = 1.22). 세로는 44 안에 17 이라 한참 남는다.
- **경험치**: 그릇(트랙) 300×27 인데 최악 문자열조차 잉크 **96px** ⇒ 좌우로 **99px 이상**이 남고
  **세로가 막는다**(27 안에 15).

그리고 레퍼런스 대조(측정표 10 §2): #6 `Lv.2` ref 잉크 46×18~19 vs 우리 40×17 (**ref 보다 작다**) ·
#11 `100/130` ref 잉크 75×14 vs 우리 같은 자릿수 70×15 (**ref 와 같다**).
⇒ 주인이 «작다» 고 한 것은 **레퍼런스 자체가 작은 자리**다. 지시가 레퍼런스보다 우선이므로
**의도적 이탈 + 측정표 정오표**로 처리한다(360 선례).

---

## 2. 1회차 — «글자만» 키웠다 (fs 23→28 · 20→27 + 가로 압축 해제)

- `.clv>i` **fs 23 → 28**. `scaleX(1.216)`·`letter-spacing:-1` 은 5회차가 ref 글리프 폭에 맞춘 값이라
  **크기 한 축만** 올렸다. 잉크 17 → **23**(+35%), 캡슐 여유 3.6px.
- `.cbar>b` **fs 20 → 27** + **`scaleX(.85)` 해제**. 트랙 좌우에 99px 이 남는데 가로로 눌러 두면
  «작아서 안 읽힌다» 를 두 축에서 거들 뿐이라 **폭 잉크를 대가 0 으로 회수**했다. 잉크 15 → **20**(+33%).

`probe669` 16/16 · `verify669` 24/24 신설 · **SMOKE PASS**.

### ⚠ 곁다리 하나 — `verify655` 가 빨개졌고, **자를 고쳐야 하는 자리였다**

fs 를 올리자 `verify655` [B1] 이 «Lv.17(3px) · Lv.20(16px)» 로 빨개졌다(수리 전 트리는 14/14 초록).
그런데 **회차마다 빨간 레벨이 달랐다**(2회차 실행은 Lv.16(2px)) — 흔들린다는 뜻이다.
`diag669` 로 그 픽셀의 좌표를 직접 세어 보니 **예외 없이 캡슐 «밖»** 이었다:

| 표본 | killed 캡슐 안 | killed 캡슐 밖(모서리) |
|---|---|---|
| Lv.14 ~ Lv.22 9표본 | **0** (전부) | 0~2 (Lv.15 · Lv.16 에서만) |

뿌리는 그 자의 상자 정의다 — `상자 = 알약 rect ∩ 잉크 bbox(+2)`. 글씨가 커져 잉크 bbox 가
**캡슐의 곧은 구간보다 넓어지는 순간** 상자가 둥근 모서리 «밖»(= 카드 본문)을 도로 삼키고,
거기가 바로 그 자의 주석이 이미 «흔들린다» 고 적어 둔 drop-shadow 재래스터 구간이다.

⇒ 처방은 **상자를 좁히는 것이 아니라 «세는 마스크를 캡슐 모양으로» 깎는 것**이다.
**무르게 푼 것이 아님을 [R1] 이 못박는다** — 알약 z 를 2 로 되돌리면 여전히 **46px 빨강**이다
(허용 오차는 한 칸도 안 넓혔고, 캡슐 밖은 «알약 잉크» 가 아니다). `verify655` **14/14 복귀**.

### 1회차 채점 — **DA 7 / DB 6** (통과선 8 미달)

두 비평가가 **독립으로 같은 곳 세 군데**를 짚었다:

| 지적 | DA | DB | 처리 |
|---|---|---|---|
| 경험치 숫자가 아직 화면 최소 글자 — 23px 까지 올릴 것 | ① 7 | ① 7 | **채택**(2회차) |
| 경험치 상하 여유 2~4px 로 답답 · 트랙 안쪽 27→34 로 키울 것 | ② 8 | ② 7 | **채택**(2회차) |
| 레벨 23 ↔ 경험치 19~20 = **21% 격차**(수리 전 13% 보다 벌어졌다) | ④ 7 | ④ 6 | **채택**(2회차) |
| 경험치 숫자가 «홈» 중앙에서 11px 왼쪽 | ④ | ④ | **기각 — 레퍼런스 값이다**(아래) |

⚑ **네 번째 지적은 «유령» 이다 — 그리고 유령인 이유가 335 와 똑같다.**
두 사람 다 **보이는 홈**(알약이 왼쪽 31px 를 덮은 뒤 남는 194..466)의 중앙으로 쟀다. 그런데
측정표 10 §5 정오표 #11 은 «트랙 중앙(267) + 2.5 = **269.5**» 를 ref 잉크 중심 실측으로 못박아 뒀고
(4회차에 트랙 중앙으로 맞췄다가 5·7회차 비평가 **4명**이 일관되게 지적해 되돌린 값이다),
계측기 실측도 잉크 중심이 트랙 중심 **+4px**(= 그 값)이다. 레퍼런스 원본도 같은 알약이 같은 자리를
덮으므로 **ref 자신이 «보이는 홈 기준으로는 왼쪽»** 이다. ⇒ 2회차 브리핑에 그 사실을 넣었다
(335 «앵커가 둘» 표를 비평가에게 주는 것과 같은 처방 — 안 주면 같은 유령이 그대로 다시 나온다).

---

## 3. 2회차 — **그릇을 같이 키웠다** (바 33 → 40 · 경험치 fs 27 → 31)

1회차의 막다른 골목은 산수다: 같은 27px 트랙 안에서 글자를 더 키우면 ① 이 오르는 만큼 ② 가 내려간다.
**그릇을 키우는 것이 유일한 출구**이고, PROGRESS 669 ② 가 그 경우를 미리 허용해 뒀다
(«커진 글씨가 겹침을 악화시키면 그릇도 같이»).

- `.cbar,.stkbar` — **height 33 → 40 · top 369 → 365**. 폭·좌변·radius **Δ0**, **세로 중심 387 Δ0**.
  40 을 고른 근거 셋:
  ① 안쪽 트랙이 **34** 가 되어 잉크 23 을 넣고도 알약과 **같은 비율의 여유**가 남는다(5px).
  ② 바가 여전히 **알약 span(363..407) 안에** 들어간다 ⇒ 655 의 «알약이 게이지 계열 **전부**의 위» 가 불변.
  ③ 이웃 침범 0 — 위로는 카드 아트 하변 362(3px 여유), 아래로는 카드 본문 하단 띠(438)까지 33px,
     가로로는 버튼 `.b2`(476~)와 애초에 안 만난다.
- `.cbar>b` — **fs 27 → 31 · line-height 33 → 40 · `translateY(2px)` → `3px`**
  (트랙 34 안에서 상하 여유가 5/7 로 1px 기울어 있었다 → 6/6).
- `.clv>i` 는 **1회차 그대로**(fs 28) — 알약은 가로가 막고 있어 더 못 키운다. 비평가 둘 다
  «알약을 더 키우는 방향은 금지(꺾쇠까지 잔여 11px)» 로 같은 판정을 냈다.

### 2회차 실측 (`probe669` · 두 프레임 동일)

| 표본 | Lv 잉크 | 캡슐 여유 | 경험치 잉크 | 트랙 여유 L/R/T/B | 알약 침범 |
|---|---|---|---|---|---|
| Lv.1 · 409/410 | 50×24 | 17.1 | 115×**23** | 97/89/5/7 | 0 |
| Lv.31 · 655/6710 | 67×24 | 8.6 | 130×24 | 89/82/5/6 | 0 |
| Lv.49 · **10489/10490** | 78×**23** | **3.6** | 180×23 | 64/57/5/7 | 0 |
| Lv.50 · MAX | 77×23 | 4.6 | 58×23 | 125/118/6/6 | 0 |

**Lv 23 ↔ 경험치 23 — 격차 0%**(1회차 21% · 수리 전 13%). 수리 전 대비 **+35% / +53%**.

---

## 4. 게이트

- **`tools/verify669.js` 24/24 신설** — 한 방향만 보는 자는 «크게만 하면 초록» 이라, **세 축을 동시에** 건다:
  `[B]` 잉크 높이 하한(Lv 22 · 경험치 22) · `[C]` 캡슐 곡선·트랙·이웃 침범 0 · `[R]` **양방향 되돌림**.
  - `[R1]` 옛 값(fs 23/20 + `scaleX(.85)`)으로 되돌리면 [B] 가 빨강 — Lv 17 · 경험치 15
  - `[R2]` 알약 글자를 과하게(fs 36) 키우면 [C1] 이 빨강 — 캡슐 여유 **−6.2px**
  - `[R3]` 경험치를 과하게(fs 52) 키우면 [C2] 가 빨강 — 트랙 여유 **−10.0px**
  - `[A3]` `.clv{z-index:3}` — 655 규약을 되살리지 않았음을 이 자도 같이 지킨다
- **`tools/probe669.js` 16/16 신설**(재현·판정 없는 측정자 · 널 대조 포함)
- **이관 `tools/verify655.js` 14/14**
  ① 세는 마스크를 rect → **캡슐**(위 §2) ② `[C2]` 바 기하 `(113,369,306×33)` → `(113,365,306×40)`.
  ⚠ 이 자가 지키던 뜻은 «치수 상수» 가 아니라 **«알약과의 상하 관계»** 라, 값만 갈아 끼우고
  `[A2]` 겹침 31px · `[A3]` z 순서 · `[C1]` 알약 기하는 **한 칸도 안 건드렸다**.
- 회귀: `verify470` **16/16 무수정** · (아래 «회귀» 절)
- **SMOKE PASS**

---

## 5. 안 한 것 · 남긴 것

- **레벨 알약을 더 키우지 않았다.** 가로가 막는다(가장 긴 «Lv.49» 에서 캡슐 여유 3.6px).
  더 키우려면 알약 89×44(측정표 #6 좌표·크기)를 벌려야 하는데, 그건 주인이 지시한 «글씨» 밖이고
  왼쪽으로 벌리면 카드 아트, 오른쪽으로 벌리면 **게이지 채움의 왼쪽 끝을 더 가린다**(정보 손실).
- **경험치 숫자의 좌우 자리는 안 옮겼다** — ref 실측값이다(위 §2 유령 절). 옮기면 5·7회차 4명이
  되돌린 그 자리로 되돌아간다.
- **668(소환 배수 토글)과 같은 화면**이지만 만진 구간이 다르다(668 = 버튼 라벨·가격 배선 ·
  669 = 알약/게이지 글자와 그 그릇). 같은 세션 권장이었으나 668 은 선점돼 있었다.

---

## 6. 2회차 채점 — **DC 8 / DD 8 → 통과**(지시서 [3]-(나)-3·4, ①~④ 최저 8)

| 축 | DC | DD |
|---|---|---|
| ① 가독성(크기) | 9 | 9 |
| ② 그릇 여유 | **8** | **8** |
| ③ 침범·겹침·잘림 | 10 | 9 |
| ④ 정렬·균형 | 9 | 9 |
| **최저** | **8** | **8** |

두 사람이 독립으로 같은 것을 확인했다 — 잉크 **17→23**(+35%) · **15→23**(+53%), 트랙 안쪽 27→34,
알약 좌표·크기 불변, **두 글자 높이 격차 13% → 0%**, 잘림·이웃 침범 **0건**,
9:19 ↔ 9:13.3 두 화면비 좌표 **1px 오차 없이 동일**.

⚑ **1회차의 «11px 왼쪽» 유령이 사라졌다.** 브리핑에 «가려진 부분을 포함한 트랙의 중앙이 기준» 을
넣자 DD 는 «전제 확인 — 기록만 하고 감점하지 않음», DC 는 «측정오차 범위와 겹쳐 감점 근거로 약하다»
라고 **스스로 갈라 적었다**. 335 의 «앵커가 둘» 표와 같은 처방이 그대로 들었다.

### 남은 지적(다음 라운드 몫 — 통과를 막지 않은 것들)

1. **트랙 채움률 68% ↔ 알약 채움률 52% = 16%p 불일치**(두 사람 공통 1순위). 같은 23px 글자인데
   경험치 쪽만 빡빡해 보인다. 처방은 **트랙 안쪽 34 → 38**(바 40 → 44). 이번 회차에 안 한 이유는
   이미 통과선을 넘겼고, 바를 44 로 키우면 **알약 span(363..407) 을 처음으로 넘어서** 655 가 지키는
   «알약이 게이지 계열 전부의 위» 관계가 알약 밖으로 새기 때문이다 — 그 관계를 다시 설계해야 하는
   작업이라 한 회차를 따로 써야 한다.
2. **트랙 안 숫자 상하 여백 6/5 (1px 비대칭)**. 잉크 23 · 트랙 34 라 **여유 11 이 홀수**여서
   정수 px 로는 대칭이 안 된다(2회차에 5/7 → 6/5 로 이미 한 번 당겼다). `translateY(2.5px)` 로
   맞출 수 있으나 **서브픽셀 이동이 글자를 흐리게 만든다** — 가독성이 과녁인 작업에서 대가가 이득보다
   크다고 보아 안 했다. 1번(트랙 38)을 하면 여유가 15 로 홀수인 채라 같은 문제가 남고, 40 이면 풀린다.
3. DC 가 «계측기 3.6px vs 내 실측 7.8px» 로 적은 것은 **표본이 다르다** — 3.6 은 가장 긴
   «Lv.49/Lv.50» 의 값이고, DC 가 잰 «Lv.31» 은 계측기도 **8.6px** 로 찍는다(§3 표). 어긋남 아님.

## 7. 회귀 · 게이트 (2회차 최종)

`verify669` **24/24 신설** · `probe669` **16/16 신설** · `verify655` **14/14 이관**(마스크 캡슐화 + [C2] 바 기하) ·
`verify470` **16/16 무수정** · `verify496` 41/41 · `verify195` 36/36 · `verify328` 19/19 ·
`verify10` **64/64 이관**(바 기하 `(113,369,306×32)` → `(113,365,306×40)`. ⚠ 값만 갈아 끼우면
«바가 어디로든 자라도 초록» 이 되므로 **[669] 3항을 같이 신설**했다 — 바 세로 중심 = 알약 세로 중심 ·
바 상변/하변이 알약 안. 그 셋이 655 의 상하 관계가 성립하는 전제다) · **SMOKE PASS**.

⚠ **`verify122` 는 첫 실행이 169/170 이었고 «내 것이 아님» 을 재실행으로 확인했다.**
빨간 항은 [8] «소환 탭 스크롤 비용 증가 없음»(122 자신의 쥬시 CSS ON/OFF **fps 비**)이고
값이 **0.890 vs 문턱 0.9** — 1% 차이였다. 그 자는 글자 크기·상자 높이를 안 보고 **fps 만** 본다.
첫 실행은 `smoke.js` 와 **동시에** 돌던 회차였고, **단독으로 다시 돌리자 170/170 PASS**
(재화 비 1.010 · 소환 비 1.013). 651·648 이 등재한 «부하 플레이키» 계열과 같은 꼴이다 —
**이 자는 단독으로 돌려라.**
