# 750 — `verify356` [S3] ③ 5건 실패: 래칫(칸 91>79 · 자리 19>18) · 이름표 8자리 · 눈금 2자리 · [전제] 2자리

(2026-09-01, sess-1818-13220 루틴 워커 B · 1회차)

## 0. 등재문이 물은 것 — «제품 드리프트인가, 자가 뒤처진 것인가» (미판정)

등재문(PROGRESS 750)은 빨강 5건을 **미판정**으로 남겼다. 338 규칙대로 **처방 전에 재현**한다.
재현 도구는 새로 만들지 않는다 — `verify356` [S3] 의 스윕은 `require('./probe418').sweep` 그 자체이므로
(`tools/verify356.js:991`), `node tools/probe418.js --json` 이 **게이트가 보는 것과 같은 수**를 준다.

빨강 5건:

| 항 | 등재문 실측 | 성격 |
|---|---|---|
| ⓐ 래칫 칸 | 91 > 래칫 79 | 새 칸 12개 |
| ⓑ 래칫 자리 | 19 > 래칫 18 | 새 자리 1개(순증) |
| ⓒ 이름표 | 등재 안 된 자리 **8개** | 이름이 갈렸다 |
| ⓓ 눈금 | `#tutoRew` −2.75% > 1.2% · `.skd .vl .nt>b` +1.79% > 1.4% | 등재 자리가 «커졌다» |
| ⓔ 눈금 [전제] | 비 band 7자리 중 **5자리만** 쟀다 | 안 잰 자리 2개 |

⚑ **ⓑ 와 ⓒ 가 같이 빨간 것이 이 행의 열쇠다.** 자리는 18 → 19 로 **하나만** 늘었는데 이름표는
**여덟 개**가 미등재다 ⇒ 등재된 자리 중 **일곱**이 이번 실행의 판정에서 빠졌다는 뜻이다.
«새 결함 8건» 이 아니라 **자리의 이름이 갈린 것**이 먼저 의심된다(ⓔ 가 그 방증 — 안 잰 2자리가 전부 `#shopList`).

(§1 이하 실측으로 채운다)

## 1. 재현 — 착수 기준선

(채움 예정)

## 2. 새 자리 8개의 귀속

(채움 예정)

## 3. 수리

(채움 예정)

## 4. 검증

(채움 예정)
