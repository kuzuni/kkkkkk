# 750 — `verify356` [S3] ③ 5건 실패: 래칫(칸 91>79 · 자리 19>18) · 이름표 8자리 · 눈금 2자리 · [전제] 2자리

**1회차 = 재현·귀속만. 제품 `index.html` 0줄 · `tools/verify356.js` 0줄 · 신설 `tools/probe750.js` 1개**
(2026-09-01, sess-1818-13220 루틴 워커 B)

> **미판정을 판정한 회차다.** 등재문은 «제품 드리프트인지 래칫 부패인지» 를 열어 뒀고(338 규칙),
> 이 회차는 그 갈래를 **측정으로 닫았다**. 수리는 2회차 몫이고, 무엇을 고칠지는 §5 에 적었다.

## 1. 재현 — 착수 기준선 (등재문 5건이 그대로 나온다)

재현 도구를 새로 만들지 않았다. `verify356.js:991` 이 `require('./probe418').sweep` 을 그대로 쓰므로
**`node tools/probe418.js --json` 이 게이트가 보는 바로 그 수**다(40분짜리 완주를 재현에 다시 쓸 이유가 없다).

```
screens 71 · measured 570 · judged 306 · outside 223 · clipped 41
cells 91 (래칫 79)  ·  groups 19 (래칫 18)  ·  errs [] · fxLeft 0
```

| 등재문 | 이번 실측 | 일치 |
|---|---|---|
| ⓐ 래칫 칸 91 > 79 | **91** | ✔ |
| ⓑ 래칫 자리 19 > 18 | **19** | ✔ |
| ⓒ 이름표 미등재 8자리 | **8** (아래 §2 표) | ✔ |
| ⓓ 눈금 초과 2자리 | `#tutoRew` **−2.75%**>1.2 · `.skd .vl .nt>b` **+1.79%**>1.4 | ✔ |
| ⓔ 눈금 [전제] 비 band 7 중 5만 측정 | 안 잰 둘 = `.cn-a2>.gm` · `.cn-cd.alert>.pn>em`(둘 다 `#shopList`) | ✔ |

⚑ **ⓑ 와 ⓒ 가 같이 빨간 것이 열쇠였다.** 자리는 18 → 19 로 **하나만** 늘었는데 미등재 이름은 **여덟**이다
⇒ «새 결함 8건» 이 아니라 **등재된 자리 일곱이 빠지고 새 이름 여덟이 들어온 교체**다.

## 2. 귀속 ① — 자리 → 아트 (`node tools/probe750.js` §1)

여덟 자리 + 눈금 초과 둘, 열 자리의 `currentSrc` 를 화면에서 직접 읽었다:

| 자리 | 상자(정사각) | 아트 | 상태 |
|---|---|---|---|
| `#psTk .ps-bx.c0>i` | 88 | **cur-dia.svg** | 등재됨(cap null) |
| `#psTk .ps-bx.c1>i` | 88 | **cur-dia.svg** | **미등재** |
| `#psTk .ps-bx.c2>i` | 88 | **cur-dia.svg** | **미등재** |
| `#psArt` | 142.5469 | **cur-dia.svg** | **미등재** |
| `#dunw .pcb-p.pcb-d>i` | 65.2969 | **cur-dia.svg** | **미등재** |
| `#relw .pcb-p.pcb-d>i` | 64.3174 | **cur-dia.svg** | **미등재** |
| `#shopw .pcb-p.pcb-d>i` | 65.2969 | **cur-dia.svg** | **미등재** |
| `#dunList .dnc.bgm-dia>.pill>em` | 50.9938 | **cur-dia.svg** | **미등재** |
| `#relw .pcb-p.pcb-r>i` | 56.145 | cur-relic.svg | **미등재** |
| `#tutoRew` | 64.856 | **cur-dia.svg** | 등재·눈금 초과 |
| `.skd .sk-ct .vl .nt>b` | 26.0001 | cur-stone.svg | 등재·눈금 초과 |

⇒ **열 자리 중 여덟이 한 장의 그림(`cur-dia.svg`)이다.** 「한 아트가 움직이면 그 아트를 쓰는
**모든 자리**가 한꺼번에 움직여 «새 자리가 여덟 개» 처럼 보인다」 — 등재문이 본 모양이 정확히 이것이다.
그리고 바로 앞 회차에 **671(2026-09-01)이 `cur-dia.svg` 를 재작도**했다(«테 규격 재작도 · .875 등방»).
⇒ **가설: 671 이 뿌리다.**

## 3. 귀속 ② — **그 가설을 기각했다** (`probe750` §2)

⚠ 가설을 처방으로 옮기기 전에 **아트 자신에게 직접 물었다**(338 규칙). 각 그림을 **한 장씩**
흰 바탕·정수 좌표·정사각 `object-fit:contain` 상자에 넣고, **화면에서 실제로 쓰이는 여덟 크기**에서
잉크 bbox 를 잰다. 상자가 정수·정사각·무변환이라 **화면 쪽 뿌리가 구조적으로 0** 이다 — 남는 편차는 전부 아트 몫이다.

| 아트 | 256 | 142.55 | 88 | 65.30 | 57 | 52.93 | 50.99 | 26 |
|---|---|---|---|---|---|---|---|---|
| **cur-dia.svg** | 0% | **0%** | **0%** | **0%** | **0%** | **0%** | **0%** | **0%** |
| | 512×512 | 286×286 | 176×176 | 130×130 | 114×114 | 106×106 | 102×102 | 52×52 |
| cur-gold.svg | 0% | 0% | 0% | 0% | 0% | 0% | 0% | 0% |
| cur-relic.svg | 0% | +0.35% | +0.45% | +0.67% | +0.70% | +1.80% | +1.29% | +1.55% |
| cur-stone.svg | 0% | −0.29% | +0.65% | −0.29% | +0.44% | −0.14% | +0.60% | −0.29% |

⇒ **`cur-dia.svg` 는 여덟 크기 전부에서 정확히 정사각으로 그려진다(편차 0%, 예외 없음).**
671 의 아트는 깨끗하고 **크기를 타지도 않는다**. ⇒ **671 귀속 기각.** 「같은 그림을 쓰니 같이 움직였다」는
관측은 맞았지만 **움직인 것이 그림은 아니었다.**

⚑ 부수 소득 — `cur-relic.svg` 는 작아질수록 +0.35 → +1.8% 로 **크기를 탄다**(획이 가늘어 AA 문턱을
못 넘는 축이 생긴다). `#relw .pcb-r`(+0.70% @ 56.145)는 §2 표의 57 칸 **+0.70% 와 소수점까지 같다** ⇒
**그 자리는 아트가 크기를 타는 것이 전부**이고 화면에 고칠 것이 없다(2회차에 이름표+눈금으로 등재할 자리).

## 4. 귀속 ③ — 그러면 무엇이 움직였나: **화면 쪽이다**

§3 이 «같은 상자 크기에서 그 그림이 내는 잉크» 를 줬으므로, 화면 실측과 **같은 상자 크기에서 나란히** 놓는다:

| 자리 | 상자 | 홀로 그린 잉크 | **화면 잉크** | Δw | Δh | [S3] 편차 |
|---|---|---|---|---|---|---|
| `.ps-bx.c0>i` | 88 | 176×176 | 176×**171** | 0 | **−5** | +2.92% |
| `.ps-bx.c1>i` | 88 | 176×176 | 176×**172** | 0 | **−4** | +2.33% |
| `.ps-bx.c2>i` | 88 | 176×176 | 176×**172** | 0 | **−4** | +2.33% |
| `#psArt` | 142.5469 | 286×286 | **282**×286 | **−4** | 0 | −1.40% |
| `.pcb-p.pcb-d>i` ×3 | 65.2969 | 130×130 | **129**×**132** | −1 | +2 | −2.27% |
| `.dnc.bgm-dia>.pill>em` | 50.9938 | 102×102 | **104**×**103** | +2 | +1 | +0.97% |

**한 축만 어긋난다** — 찌그러짐(두 축이 반대로 벌어지는 모양)이 아니라 **한쪽 끝을 잃거나 얻는** 모양이다.

### 4-1. 가장 큰 덩어리(칸 91 중 **44** = c0 20 + c1 15 + c2 9)의 정체 — **문턱 아래 가림**

`.ps-bx` 세 칸을 화면에서 직접 물었다(`elementFromPoint` · 형제 실측):

```
c0/c1/c2 : rect 88×88 @정수좌표 · .ps-bx overflow:visible · <i> overflow:visible
           topHit i.   leftHit i.   rightHit i.
           botHit **em.**          ← 아이콘 하변을 형제가 덮고 있다
형제      : b. @y866 h31 (수량 라벨) · s.lk @y728 (자물쇠 뱃지)
```

아이콘은 776..864 인데 그 **하변에서 잡히는 것이 `<i>` 가 아니라 `<em>`** 이다(수량 라벨 `.ps-bx>b>em`
의 줄상자가 위로 올라와 아이콘 하변을 덮는다). `probe418` 의 잉크는 «대상 노드 opacity 0» 두 장의
**차분**이라, 덮인 줄은 아이콘을 껐을 때도 안 바뀌므로 **bbox 가 그만큼 짧아진다.**

⚠ **`probe418` 은 이 경우를 막으려고 `occ`(가려짐) 가드를 이미 갖고 있다 — 문턱이 3% 다**
(`PROBE418_OCC`, 396행 주석: «래스터가 만드는 결함은 1px 이라 이 문턱을 절대 못 넘는다»).
이번 손실은 **5 ÷ 176 = 2.84%** 로 **문턱 바로 아래**다 ⇒ 가드를 통과해 «판정» 으로 남고,
그 축의 손실이 그대로 «종횡 편차» 로 읽힌다.

### 4-2. 되돌림 시험 — **가설이 아니라 증명이다**

«형제가 덮어서 짧아진 것» 이라면 **그 형제를 치우면 돌아와야 한다.** 같은 화면·같은 차분법으로,
수량 라벨 `.ps-bx>b` 에만 `visibility:hidden` 을 주고 다시 쟀다(레이아웃은 그대로 — `visibility` 는
자리를 안 뺀다. `display:none` 으로 하면 «라벨을 없앤 다른 화면» 을 재게 된다):

```
현행       → c0 176×172 dev +2.33%  |  c1 176×172 +2.33%  |  c2 176×172 +2.33%
라벨 숨김  → c0 176×176 dev  0.00%  |  c1 176×176  0.00%  |  c2 176×176  0.00%
```

⇒ **세 자리 전부 정확히 176×176 · 편차 0.00% 로 돌아온다.** 편차의 **100%** 가 형제 라벨 몫이고
아이콘 기하의 몫은 **0** 이다.

⇒ **`.ps-bx` 44칸은 제품의 찌그러짐이 아니다.** 상자는 정수 88 정사각 · 좌표 정수 · 변환 없음 ·
그림은 §3 이 같은 크기에서 176×176 을 내고, §4-2 가 화면에서도 176×176 을 **직접 보여 준다**.
움직인 것은 **아이콘이 아니라 그 옆 라벨**이다.

## 5. 1회차 결론과 2회차가 할 일

**판정: «제품 드리프트» 도 «래칫 부패» 도 아니다 — 셋째 갈래다.**
`[S3]` 의 편차는 「화면 잉크 종횡 ÷ **그 그림을 256px 상자에 한 번 그려 잰 종횡**」인데,
그 기준선이 두 곳에서 새는 것이 이번 다섯 빨강의 전부다:

- **ⓐ 문턱 아래 가림**(`.ps-bx` ×3 = **44칸** · `#psArt` 로 보이는 −4px 도 같은 모양)
  → `occ` 3% 가 못 잡는 2~3% 대 가림. **자를 고칠 자리**(제품 0줄).
- **ⓑ 아트가 크기를 탄다**(`cur-relic`·`cur-stone` 계열 — `#relw .pcb-r` 는 §2 표와 소수점까지 일치)
  → 기준을 256px 한 점에서만 잡는 한 구조적으로 남는다. **이름표+눈금으로 등재할 자리.**
- **ⓒ 합성 배율 자리**(`.dnc .pill>em` 은 `transform:scale(1.0732)` 라 47.52 로 래스터한 뒤 확대 —
  잉크가 **커진다**: 102 → 104) → 601 이 `bgm-rel` 에서 이미 같은 성질을 재고 눈금 1.8 로 등재해 둔 자리.

**2회차 순서(권장)**
1. **ⓐ 를 먼저 닫는다** — 91칸 중 44칸이 여기다. `occ` 문턱을 함부로 내리지 말 것(내리면 정상 자리가
   스코프 밖으로 나가 **판정 노드가 줄고** `[S3] ②`(`judged ≥ 180`)가 그것을 «스코프가 줄었다» 로 잡는다).
   1순위 후보는 **가림을 «자리 밖» 이 아니라 «축별» 로 보는 것** — 손실이 한 축에만 있고 그 축의
   손실률이 다른 축보다 크면 그 자리는 «가려짐» 이다. 되돌림 시험은 «라벨을 지우면 176×176 이 나온다».
2. ⓑ·ⓒ 남는 자리만 **`KNOWN_SITES` 에 이름표 + 눈금**으로 등재(601 산수: 실측 + 한 device px 이 그
   잉크에서 만드는 % ÷ 2). **cap 은 반드시 잰 값으로만** — 안 잰 자리에 숫자를 적는 것이 헛초록이다(548).
3. 그러고 **남은 값으로** `RATCHET_CELLS`·`RATCHET_SITES` 를 다시 적는다. ⓐ 를 닫으면 44칸이 빠지므로
   **래칫은 내려 적힐 가능성이 크다**(«줄이는 쪽으로만» 규약과 같은 방향이다).
4. ⓔ 의 안 잰 두 자리(`#shopList` 둘)는 그 화면 상태가 바뀐 것인지부터 볼 것 — **743 이 같은 자의
   [S3] ① 을 잡고 있으므로** `tools/verify356.js` 를 만질 때 그 구간과 겹치지 않게 한다(현재 743 은
   [S3] ① 실패 문구만 만졌고 래칫은 «안 건드렸다» 고 기록에 적어 뒀다).

⚠ **`RATCHET_CELLS` 를 91 로 올려 덮지 마라** — 등재문이 못박은 그대로이고, §4 가 그 이유를 실측으로
보탠다: 44칸은 결함이 아니라 **자가 잘못 센 칸**이라 올려 적으면 «없는 결함» 을 영구히 래칫에 새긴다.

## 5-2. ⛔ 래칫 수를 다시 적기 전에 풀어야 할 것 — **환경 간 편차**(2회차 착수 조건)

743 워커 D 가 같은 날 같은 자를 완주해 PROGRESS 750 행에 덧붙인 실측:

| | 칸 | 자리 | 이름표 | 눈금 | [전제] |
|---|---|---|---|---|---|
| 등재 세션 | 91 | 19 | 8 | `#tutoRew` −2.75 · `.nt>b` +1.79 | 2 |
| **이 회차(sess-1818-13220)** | **91** | **19** | **8** | **−2.75 / +1.79** | **2** |
| 743 회차(sess-1800-24740) 완주 ×2 | **103** | **20** | 8 | −2.75 / +1.79 | 2 |

⇒ **한 환경 안에서는 결정적인데(각자 두 번 이상 같은 값) 환경이 바뀌면 칸/자리 «수» 가 갈린다.**
편차 «값»(−2.75 · +1.79)과 이름표 8자리는 **세 실행 전부 같다** — 갈리는 것은 **수** 뿐이다.
이는 §4 의 결론과 같은 방향이다: 수를 흔드는 것은 결함이 아니라 **가림·합성처럼 컨테이너를 타는 항**이다.

⇒ **래칫을 어느 한 실행값으로 다시 적는 것은 지금 불가능하다.** ⓐ 를 닫아 그 항을 걷어낸 **뒤에**
두 환경에서 다시 재서 값이 모이는지부터 봐야 한다(안 모이면 `#dgdIcon`·`#rwCost` 처럼 `band` 로
성질을 적는 것이 답이지 수를 올리는 것이 답이 아니다 — 601·17회차 선례).

## 6. 이 회차에 만든 것

- `tools/probe750.js` **신설** — §1 자리→아트 매핑 · §2 아트별 «상자 크기 ↔ 잉크 종횡» 곡선.
  ⚠ 1회차에 §2 를 «여러 장을 나란히 놓고 x 를 등분» 으로 짰다가 큰 상자에서 이웃과 겹쳐
  620×512 같은 **합본 bbox** 를 읽었다(cur-dia 가 «+884%» 로 나왔다). **한 장씩 재도록 고쳤고**,
  그 함정을 코드 주석에 남겼다 — 같은 자를 다음에 쓰는 세션이 다시 밟지 않게.
- `index.html` **0줄** · `tools/verify356.js` **0줄** · 래칫·이름표·눈금 **한 칸도 안 건드렸다**.

## 7. 실행 기록

| 자 | 결과 |
|---|---|
| `node tools/probe418.js --json` (전 화면 · DSF2) | 칸 **91** · 자리 **19** · errs 0 · fxLeft 0 — 등재문과 일치 |
| `node tools/probe418.js --screen 패스 --dsf 2/3/4` | `.ps-bx` +2.92/+2.33/+2.33 → +2.72/+1.93/+2.33 → +2.03/+2.03/+2.33 (**0 으로 안 수렴**) · `#psArt` **−1.40% 세 배율 전부 같음** |
| `node tools/probe750.js` | §1 열 자리 중 **여덟이 `cur-dia.svg`** · §2 `cur-dia` 여덟 크기 전부 **0%** |
| 형제 가림 실측(`elementFromPoint`) | `.ps-bx` c0·c1·c2 **하변 `botHit = em.`** · `.ps-bx`·`<i>` 둘 다 `overflow:visible` |
| **되돌림 시험**(`.ps-bx>b` 만 `visibility:hidden`) | 176×172(+2.33%) → **176×176(0.00%)** · 세 자리 전부 |
| `node tools/verifyProgress.js --no-gate` | **0** (§1~§5 빨강 0) |
| `node tools/smoke.js` | **SMOKE PASS** |

⚠ 이 회차는 `verify356` 완주를 **안 돌렸다** — 제품·자를 **한 줄도 안 고쳤으므로** 완주의 답은
착수 기준선(= 등재문 5건)과 같고, 그 기준선은 §1 이 `probe418`(= 그 자가 쓰는 바로 그 `sweep`)로
이미 재현해 뒀다. 완주가 필요한 것은 **수리가 들어가는 2회차**다.

---

# 3회차 — ⓐ 를 닫았다: «가림» 을 문턱이 아니라 **되돌림**으로 가른다
**제품 `index.html` 0줄 · 고친 것은 `tools/probe418.js`(자) 한 파일**
(2026-09-01, sess-1913-3830 루틴 워커 A)

> 1·2회차가 «셋째 갈래(자가 잘못 센 칸)» 를 측정으로 못박았고, 이 회차는 그 갈래를 **자에서 닫는다.**
> review §5 의 2회차 순서 그대로 — ① ⓐ 를 먼저 닫고 ② 남는 자리를 이름표·눈금으로 ③ 그 뒤 래칫.

## 8. 무엇이 잘못 세고 있었나 (1·2회차 결론의 기계)

이 자의 잉크는 «대상 노드 `opacity:0`» 두 장의 **차분**이다. 그래서 아이콘 **앞**에서 무언가를
칠하고 있으면 그 줄은 두 장이 똑같아 bbox 가 그만큼 짧아진다 — 아이콘 기하는 멀쩡한데 **종횡만**
어긋난 값이 나온다. `probe418` 은 이걸 막으려고 `occ` 가드(예상 채움과 **3%** 넘게 어긋나면 판정 밖)를
이미 갖고 있었는데, 35 패스 알약의 손실이 **5 ÷ 176 = 2.84%** 로 **문턱 바로 아래**라 통과했다.

**문턱을 내리는 것은 답이 아니다** — 내리면 정상 자리가 스코프 밖으로 나가 `judged` 가 줄고
`[S3] ②`(판정 ≥180)가 그것을 «스코프가 줄었다» 로 잡는다(§5 1항). ⇒ **가리는 것을 치우고 다시 잰다.**
가림이면 값이 돌아오고, 기하면 그대로다. 2회차 되돌림 시험을 **자 안으로 들여놓은 것**이 이 회차다.

## 9. 처방 — `SCAN_COVER` / `HIDE_COVER` (probe418)

1. 판정 스코프가 될 수 있는 노드(`img` + 늘리지 않는 `object-fit`)마다 상자 위 **12×12 격자**를
   `elementFromPoint` 로 물어 **앞에 있는 남**을 찾는다. 하나도 없으면 **다시 재지 않는다**.
2. 있으면 그것들만 `visibility:hidden` 으로 치우고 **같은 차분법으로 다시 재서** 그 값을 쓴다.

### 9-1. 세 번 틀리고 얻은 규칙 (같은 자를 다음에 고칠 세션에게)

- ⛔ **«겹치는 것» 을 다 숨기면 안 된다.** 뒤에 깔린 형제를 숨기면 아이콘 테두리 AA 가 얹히는
  바탕색이 바뀌어 경계 칸의 diff 가 문턱(THR 12)을 들락거린다. 실제로 03 던전 `.pcb-d` 가
  그 방식에서 **129 → 130** 으로 «고쳐졌다» — 고쳐진 것이 아니라 **자가 다른 것을 잰** 것이다.
  ⇒ 숨기는 것은 **hit-test 로 «앞» 임이 확인된 것**뿐이다(LESSONS 718-③ 의 «가림은 재려는 것이
  아닌 층에만» 을 이 자리에서 지키는 방법이다).
- ⛔ **맞은 노드만 숨기면 모자란다.** 패스 알약의 hit 은 수량 라벨 `em` 인데 아이콘 하변을 실제로
  덮는 것은 그 **부모 `b` 의 배경**이라 `em` 만 숨기면 176×172 **그대로**다.
  ⇒ 맞은 노드에서 **형제 가지의 뿌리**(= 대상의 조상 바로 아래 칸)까지 올라가 통째로 숨긴다.
- ⛔ **표본이 상자 끝줄을 포함해야 한다.** 덮인 것이 마지막 2~3px 인데 `(i+0.5)/N` 격자는 그 줄을
  통째로 건너뛴다 — 그 판으로 재면 «숨길 것이 없다» 가 나와 가림이 통째로 안 보인다.
  ⇒ 모서리 안쪽 **0.3px** 부터 `i/(N−1)` 로 끝까지 찍는다.
- `pointer-events:none` 인 노드도 화면에는 칠하므로 hit-test 동안만 `*{pointer-events:auto!important}` 를 얹는다.

### 9-2. 이 처방이 «무르게 푼 것» 이 아니라는 증거 — 살아남는 자리들

| 자리 | 그대로 | 가림 치움 | 판정 |
|---|---|---|---|
| `#psTk .ps-bx.c0/c1/c2>i` (88 정사각 상자) | 176×**172** (+2.33%) | **176×176 (0.00%)** | 가림이었다 ⇒ 닫힘 |
| `#psArt` (142.5469) | **282**×286 (−1.40%) | **286×286 (0.00%)** | 가림이었다 ⇒ 닫힘 |
| `#dunw .pcb-p.pcb-d>i` (65.2969) | 129×132 (−2.27%) | **129×132 (−2.27%)** | 앞을 덮는 것 **0개** ⇒ 그대로 남는다 |
| `#dunw .pcb-p.pcb-g>i` (같은 상자·다른 아트) | 132×132 (0.00%) | 132×132 (0.00%) | 대조 — 0 은 0 그대로 |

## 10. 전 화면 완주 — 수리 전후 (같은 환경 · DSF2 · `SCREENS` 71화면 · 표본 0줄 안 넓혔다)

| | 판정 | 가려짐·잘림 | 칸 | 자리 | errs | fxLeft |
|---|---|---|---|---|---|---|
| 수리 전(1·2회차 · 이 환경) | 306 | 41 | **91** | **19** | 0 | 0 |
| **3회차(가림 보정)** | **318** | **27** | **44** | **14** | 0 | 0 |

⚑ **판정이 306 → 318 로 «늘었다» 는 것이 이 회차의 핵심 증거다.** 문턱을 내려 푸는 길은 정상 자리를
스코프 밖으로 밀어 판정을 **줄인다** — `[S3] ②`(판정 ≥180)가 그것을 잡으라고 있는 항이다.
가림 보정은 반대로 **문턱 3% 를 넘던 가림 자리를 제 값으로 돌려** 판정 안으로 데려온다(가려짐 41 → 27).
가림 보정이 다시 잰 노드는 258개, 그중 값이 실제로 달라진 노드가 **79개**다.

### 10-1. 등재문 다섯 항의 처리

| 등재문 | 3회차 결과 |
|---|---|
| ⓐ 래칫 칸 91 > 79 | **44** — 44칸이 «자가 잘못 센 칸» 이었다(래칫 48 로 **내려** 적었다) |
| ⓑ 래칫 자리 19 > 18 | **14** (래칫 16 으로 내려 적었다) |
| ⓒ 이름표 미등재 8자리 | 셋(`ps-bx.c1`·`ps-bx.c2`·`#psArt`)은 **가림이라 닫혔고**, 다섯은 이름표+눈금으로 등재 |
| ⓓ 눈금 초과 2자리 | `#tutoRew` −2.75% → **0칸(닫힘)** · `.skd .nt>b` +1.79% → **눈금 1.4 → 2.8**(DSF 수렴 근거) |
| ⓔ 눈금 [전제] 안 잰 2자리 | `.cn-cd.alert>.pn>em` → **재고 나서 문턱 아래**(−0.41%) · `.cn-a2>.gm` → **스크롤 아래**(곁다리 772) |

## 11. 게이트 이관 (`tools/verify356.js`)

- **래칫 칸 79 → 48 · 자리 18 → 16.** 실측 44·14 + 여유 4·2(근거: `band` 2자리 재등장 + `#dgdIcon` 흔들림 1칸).
- **뺀 자리 셋** — 전부 «왜 뺐는가» 를 근거와 함께 적었다. 뺀 것 자체가 감시자다(다시 나오면 이름표가 빨개진다).
  - `span#tutoRew>img.cic` — 닫힘. 전 화면 0칸 · 홈 화면(31 던전 클리어) `TOL=0` 에서도 **0칸**(판정 3개가 전부 0.00%).
  - `.cn-cd.alert>.pn>em` — 닫힘. 13 재화 탭 `TOL=0` 실측 **판정 12/12 · −0.41%**(문턱 아래).
  - `.cn-a2>.gm` — **닫힌 것이 아니다.** `top:1374px` 이라 스윕 뷰포트 밖이고 `COLLECT` 이 건너뛴다 ⇒ 곁다리 **772**.
- **더한 자리 다섯** — `.pcb-d` ×3(cap **2.7**) · `#relw .pcb-r`(**1.2**) · `.dnc.bgm-dia>.pill>em`(**1.5**).
  눈금은 601 산수 그대로(실측 + 한 device px 이 그 잉크에서 만드는 % ÷ 2).
- **올린 눈금 하나** — `.skd .nt>b` 1.4 → **2.8**. 12회차 등재는 08 **스킬** 세부(잉크 40×43 · +0.74%)였고
  750 이 잡은 +1.79% 는 08 **코스튬** 세부의 같은 셀렉터(잉크 **49×52**)다. 한 device px 이 **2.04%** 라
  1.79% 는 한 픽셀도 안 되는 값이고, **DSF2·3·4 = +1.79 → +1.09 → +0.75%** 수렴이 «자의 바닥» 을 못박는다.
- **[S3] ⑤ 신설 — 가림 보정의 되돌림.** ③ 의 래칫을 내린 근거가 이 보정이므로, 보정이 조용히 죽으면
  래칫 48 은 **닿을 수 없는 수**가 되어 이 절이 영원한 빨강이 된다. 35 패스 네 화면만 `nocov` 로 다시 돌아
  «끄면 ≥20칸(실측 46) · 켜면 그 자리 0칸 · covFix ≥1» 을 단언한다(전 화면을 두 번 도는 값을 안 치른다).

### 11-1. 남긴 것 — 다음 세션에게

- **`.pcb-d` 는 값을 회수할 여지가 있다.** 처방을 그 자리에서 시험해 뒀다 —
  `.cic` 를 정수 65 로 심으면 **−2.27% → −0.77%**(잉크 129×130). 값의 3분의 2가 소수 상자(65.2969) 몫이다.
  제품 CSS 라 750(자 수리)의 단위 밖이라 **772** 에 적어 넘긴다.
- **환경 간 편차(§5-2)는 이 회차가 «닫았을 것» 이다.** 갈리던 것은 «수»(칸 91 ↔ 103 · 자리 19 ↔ 20)뿐이고
  §4 가 그 흔들림의 정체를 «가림·합성처럼 컨테이너를 타는 항» 으로 지목했는데, 이 회차가 그 항을 걷었다.
  ⚠ 다만 **다른 환경에서 한 번 더 완주해 확인한 것은 아니다** — 다음 세션이 다른 컨테이너에서 완주해
  칸 44·자리 14 가 재현되면 여유 4·2 를 **더 조일 수 있다**(줄이는 쪽으로만).

## 12. 마감 직전 rebase — **래칫의 여유가 실제로 일을 했다** (같은 회차)

완주(칸 44 · 자리 14)와 push 사이에 상류가 `index.html` **392줄**을 올렸다(686 단련 버튼 74 → 178 ·
687 룬 헤더 신설 등). 그래서 **rebase 뒤 같은 자를 한 번 더 완주**시켰다(LESSONS 654 «회귀는 pull 뒤에 돌려라»):

| | 판정 | 가려짐 | 칸 | 자리 | 결과 |
|---|---|---|---|---|---|
| 내 완주(수리 전 트리) | 318 | 27 | 44 | 14 | **256/256 PASS** |
| **rebase 뒤(병합 트리)** | 317 | 28 | **45** | **15** | **255/256** — 이름표 1건 |

빨강은 **`div#rnHd>span.pv>i>img.cic` 한 자리**이고, `git log -S'rnHd'` 가 범인을 바로 짚는다 —
`e671ab3 wip(687)`(룬 탭 재화 잔량 헤더 신설)이 **같은 재화 아이콘을 새 호스트에 하나 더** 놓았다.

⚑ **이것이 래칫을 «수» 가 아니라 «이름표» 로도 묻는 이유의 실물 예다.** 칸 45 · 자리 15 는 내가 적은
여유(48 · 16) **안**이라 ③ 은 초록인데, 이름표는 그 자리를 **정확히 집어** 빨개졌다. 수만 세는 게이트였다면
«새 부품이 하나 들어왔다» 는 사실이 여유 안에 조용히 묻혔을 것이다(530 이 «수 → 이름» 으로 올린 그 칸).

⚑ **처방은 «수를 올리는 것» 이 아니다**(11·12회차·601 규약). `probe418 --screen "23 룬"` 이 두 자리를
**나란히** 찍는데 **잉크 68×106 · 상자 정수 53.0000 · +2.64%** 가 이미 등재된
`#trRunes .rbt.b1.no`(601 · cap 3.4)와 **소수점까지 같다** ⇒ 같은 부품의 **추가 목격**이지 새 결함이 아니다.
그래서 래칫은 한 칸도 안 건드리고 **이름표 + 그 자리의 눈금(3.4)** 으로만 등재했다.
