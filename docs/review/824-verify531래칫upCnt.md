# 824 — `verify531` [B1]·[B2] 래칫 0 → 1자리 (`#upCnt`)

**세션** sess-1528-147 (루틴 워커 B) · 2026-09-02 · **1회차**
**티어** T1(게이트 빨강 — 「레드닷 상시 점등」 예방 짝의 래칫) · 지시서 [3]-**(가)** 기계+자 ⇒ **비평가 없음**
**제품** `index.html` **1자리**(짝 두 줄에 id 하나씩 · 주석) · **레이아웃 Δ0px · 밸런스·판정 0줄**

---

## 1. 착수 시점의 빨강 (재현 = 등재문 그대로)

```
FAIL  [B1] `#app` 안 id 요소 전수 — 닷을 심어도 «축 없이 켜지는» 자리 0 — #upCnt(updot off:block)
FAIL  [B2] 그중 root 0자리 (래칫 — 새 화면이 생겨도 여기가 늘면 빨강) — 1자리
VERIFY531 16/18 FAIL
```

`node tools/probe531.js`(338 규칙 — 처방 전에 재현부터)도 같은 것을 찍었다:

```
[1] 빨간 id 1개 · 그중 **root** 1개
    ⚠ #upCnt   .updot(축없음→block / .alert→block)
    ▸ 모양은 «짝 있음» 인데 실제로는 함정인 스코프 1개: #upCnt
[3] A/B — 함정 root 1곳에만 짝을 깔았을 때 : 꺼진 닷 0 · 새로 켜진 닷 0 · 남은 함정 root 0
```

즉 **등재문의 진단·처방이 그대로 맞았다**(338·341 처럼 기각되지 않았다).
재료는 726 이 09 일괄 강화 결과에 «총 N건 강화» 헤더를 만들며 같이 깐 한 줄이다:

```css
.upr-cnt s{display:inline-block;text-decoration:none}   /* index.html 13298 */
```

**클래스 급(0,1,1)이라 공용 `.updot{display:none}`(0,1,0)을 이긴다** — 이 자리에 레드닷을 놓는
다음 워커는 «강화 가능 여부와 무관하게 상시 점등» 을 그대로 밟는다. 166 ⓔ · 202 §3 · 283 · 294 ·
325 · 516 · 519 로 **일곱 번** 난 계열이고, 531 은 사고가 난 자리가 아니라 **재료가 남은 자리**를
래칫으로 지키게 해 뒀다. 그 래칫이 처음으로 «자라야 할 때» 를 말한 것이 이 행이다.

## 2. 처방 — 짝 목록에 `#upCnt` 편입 (23 → 24자리)

```
:is(#bSk,…,#svw,#upCnt) :is(s.updot,s.bdg,s.dot){display:none}
:is(#bSk,…,#svw,#upCnt) .alert>:is(s.updot,s.bdg,s.dot){display:block}
```

**두 줄 다** 더했다 — 한 줄만 더하면 반대로 그 자리의 닷이 통째로 꺼진다(`#dunw` 를 일부러
뺀 이유가 그것이다). 특이성은 `#upCnt s.updot`(1,1,1) > `.upr-cnt s`(0,1,1) 이라 짝이 이긴다.

## 3. ⚑ 이 자리에는 **살아 있는 `<s>`** 가 있다 — 넓게 적었으면 사고였다

531 교훈 ②(«예방 규칙을 넓게 깔기 전에 그 스코프에 지금 무엇이 살아 있는지 오프너로 세어라»)를
그대로 밟았더니, `#upCnt` 안에는 782 가 넣은 **«밀어서 더 보기»** 안내문이 있었다:

```js
$('upCnt').innerHTML = '<i>총</i><em>' + n + '</em><i>' + tail + '</i>'
  + (over ? '<s>밀어서 더 보기</s>' : '');            /* index.html 27574 */
```

짝을 `#upCnt s` 처럼 **태그 꼴로** 적었으면 그 안내문이 통째로 꺼졌다 — `.bdg` 를 클래스 꼴로
적었을 때 10 상점 배너 리본 3장이 같이 꺼진 것과 **같은 사고**다. 짝이 `s.updot,s.bdg,s.dot`
**세 클래스만** 집기 때문에 그 `<s>`(클래스 없음)는 그대로 산다. 실측 — `display:inline-block`.

## 4. 자 이관 (`tools/verify531.js` 18항 → **21항**)

| 항 | 내용 |
|---|---|
| [A3] | 스코프 **23 → 24**자리 (래칫 동기화 · 「자라도록 설계된 값」임을 주석에 명시) |
| **[A7] 신설** | `#upCnt` 가 목록에 **있다** — [A6](`#dunw` 는 **없다**)의 반대 짝 |
| **[C4] 신설** | `#upCnt` 의 «밀어서 더 보기» `<s>` 는 그대로 보인다 — [C2] 리본 항과 같은 꼴의 «무르게 안 풀었다» |
| **[R5] 신설** | 짝을 **태그 꼴**(`#upCnt s`)로 넓힌 사본에서는 그 안내문이 **꺼진다** — [C4] 가 허수가 아님을 못박는다 |

⚠ **[C4] 의 표본은 «넉넉히» 가 아니라 «넘치는 판» 이어야 한다.** 1차 실행은 스킬 전종 27칸으로
열었는데 2280 프레임에서는 5행이 통째로 보여 `over` 가 거짓 ⇒ 안내문이 **아예 안 그려져** 빨갰다
(«노드 없음»). 세 목록(SKILLS·PETS·EQUIPS)을 이어 붙여 170칸 · `sh 5211 > ch 1614` 로 세웠다.

## 5. 결과

| 자 | 결과 |
|---|---|
| `node tools/verify531.js` | **16/18 FAIL → 21/21 PASS** |
| `node tools/probe531.js` | 함정 root **1 → 0** · A/B «꺼진 닷 0 · 새로 켜진 닷 0» |
| `node tools/verify283.js` | 42/42 (무수정) |
| `node tools/verify325.js` | 75/75 (무수정) |
| `node tools/verify516.js` | 32/32 (무수정) |
| `node tools/verify519.js` | 27/27 (무수정) |
| `node tools/verify726.js` | 52/52 (무수정 — 726 이 만든 호스트가 대상이라 1순위 회귀) |
| `node tools/probe782.js` | 9/9 (무수정 — «밀어서 더 보기» 가 실제 흐름에서도 그대로 뜬다) |
| `node tools/smoke.js` | **SMOKE PASS** |

283·325·516·519 는 531 의 짝을 `tools/dot531.js` 한 곳에서 걷어 되돌림을 재는 자들이다 —
그 식(`SRC_RE` = `:is(#bSk[^)]*\)…`)이 **목록 길이와 무관**해 이관이 0줄이었다(531 교훈 ⑤가
«걷는 식을 한 곳에 둬라» 고 한 값을 여기서 받았다).

## 6. 곁다리 관측 — 등재 0건

- 798(«안 넘치는 판에서도 «밀어서 더 보기» 가 붙는다»)은 **이미 등재돼 있고 내 단위가 아니다** —
  [C4] 는 «넘치는 판에서 보이는가» 만 묻는다(그 행의 판정을 굳히지 않는다).
- `.upr-cnt s` 두 줄(13298·13300)은 **죽은 CSS 가 아니다** — 782 안내문이 그 규칙으로 그려진다.
  지웠으면 안내문 서식이 통째로 날아갔다.
