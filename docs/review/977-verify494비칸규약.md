# 977 — `verify494` [9]ⓑ 1건 실패: [G] «비» 칸 규약과 md 생성부가 갈렸다

> 담당: sess-0713-27624 (루틴 워커 A) · 2026-09-06 · **1회차 · 제품 `index.html` 0줄**
> 지시서 [3]-**(가)** (기계/게이트 — 레퍼런스 대조 없음 · 비평가 없음)

## 1. 재현 — 등재문 그대로 98/99

착수 직후 `node tools/verify494.js` = **98/99 FAIL**, 빨간 항은 등재문이 지목한 그 한 줄이다:

```
FAIL 단일 정책 [G] 의 «비» 칸이 전부 «못 잰다» 이거나 **출처를 밝힌 대조 값**이다 (행 25 · 어긴 행 1건)
```

등재문이 «먼저 가르라» 고 한 두 갈래(ⓐ 출처 표식을 잃은 대조 값 / ⓑ «못 잰다» 로 적어야 할 자리에
수가 앉았다)는 **둘 다 아니었다.** 어긴 행을 실제로 찍어 보니 **13행 «창 역량»** 이고, 그 «비» 칸에는
수도 표식도 없이 **맨 `—`** 하나가 들어 있었다:

```
| **⚑ 창 역량 — 말미 축(…) 〔§0-2 판정 창 규약〕** | ⚠ **판정 불가 — …** | — |
```

⇒ 진단은 **ⓒ = 규약이 생기기 전 모양의 빈칸**이다. 33회차가 이 줄을 판정 표 맨 위에 세우면서
20회차 정정4 의 규약(«빈칸도 «-» 도 아니다 · 못 재는 칸은 못 잰다고 자가 말한다»)을 지나쳤다.
등재문의 «표식 규약과 현재 md 생성부가 갈렸다» 는 맞고, **갈린 자리가 33회차의 새 줄**이었다.

⚠ **등재문을 진단으로 믿지 않은 것이 이 회차의 첫 갈림이다**(LESSONS 232-②). ⓐ 로 읽었으면
«출처 표식만 달아 준다», ⓑ 로 읽었으면 «수를 지운다» 인데 둘 다 이 자리에서는 **없는 병의 처방**이다.

## 2. 처방 — 게이트가 아니라 md 생성부를 고쳤다 (333)

항을 눌러 초록으로 만들지 않았다. 규약은 20회차가 세운 그대로 옳고, **어긴 것은 표**다.

* `tools/bot199.js` — «창 역량» 줄의 비 칸을 자기 이유를 적는 문장으로:
  `— ⚠ **창 이름은 나눌 수 없다 — 못 잰다**(비가 아니라 창 역량 표시 · 33회차)`
* ⚠ `NORATIO`(«정책 1개 — 못 잰다»)를 **재사용하지 않았다.** 이 줄이 비를 못 내는 이유는
  «정책이 하나라서» 가 아니다 — 정책이 둘이어도 **창 이름은 나눌 수 없다**. 같은 문장을 돌려쓰면
  표가 두 정책 실행에서 거짓말을 한다(이유마다 다른 문장 = `NODIV`/`NOWIN` 두 벌).

## 3. ⚑ 이 회차의 본체 — 빨강 옆의 **헛초록**

빨간 항을 고치고 «같은 규약을 묻는 자리» 를 훑자(LESSONS 232-① · 214-④ 스윕 규칙),
**정책이 둘인 표에는 아무 자도 안 걸려 있었다.** 그 표의 «비» 칸은 분모가 0 이면
`'-'` 를 찍는다 — 20회차 정정4 가 **이름까지 대며 금지한 바로 그 문자**다. 3일·2시드
표본에서 그런 칸이 **8개**(순 이동 · §0 소환 예산 · ① 다섯 줄 · ② 말미 정상 기울기).

| | 단일 정책 표 | 두 정책 표 |
|---|---|---|
| 수리 전 | «창 역량» 1건 **빨강**(등재 977) | 맨 `'-'` 8건 — **아무 자도 안 짖음** |
| 수리 후 | 25/25 자기설명 | 수 16 · 자기설명 9 |

그래서 처방을 «창 역량 한 줄» 에서 멈추지 않고 **분모 0 자리 전부**(`bot199.js` 6자리)를
이유를 적는 칸으로 바꿨다: `— ⚠ **분모(대충 유저)가 0 이거나 없다 — 못 잰다**`
(분모가 어느 정책인지 `POLICIES[].name` 에서 파생 — 손으로 적은 이름 금지).

## 4. 게이트 — 한 벌로 적고 두 표에 건다

`tools/verify494.js` [9]ⓑ 를 다시 적었다(항을 지우지 않았다 — 328-330 교훈):

* 판정을 두 술어로 분리: `DECL`(«못 잰다» / «이 실행의 수가 아니다») · `NUM`(수 · `∞`).
* **단일 정책** — 비는 존재할 수 없다 ⇒ 모든 칸이 `DECL`(수가 앉으면 빨강 = 정정4 유지).
* **두 정책**(신설) — 비는 존재한다 ⇒ `NUM` **이거나** `DECL`. 맨 `-`·맨 `—` 는 빨강.
* ⚠ 두 표를 한 항으로 묶지 않았다 — 묶으면 «수» 를 허용해야 해서 단일 정책 쪽이
  «다른 실행의 수가 앉아도 초록» 으로 되돌아간다(정정4 가 막은 그 자리).
* **§R977 되돌림 시험**(신설) — 픽스처 표(`| … | — |`·`| … | - |`·`| … | 1.275 |`·자기설명 1줄)에
  두 판정을 걸어 **두 정책 판정 2건 · 단일 판정 3건**이 빨간지 센다. 판정을 `/./` 처럼 무르게
  풀면 이 항이 즉시 빨개진다 — 없으면 위 두 항은 «아무 문자열이나 통과시키는 자» 여도 초록이다.

## 5. 결과

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `tools/verify494.js` | **98/99 FAIL** | **101/101 PASS** (신설 2항 + 되돌림 1항) |
| `tools/verify199win.js` | 32/32 | 32/32 (창 역량 줄 정규식 무손상) |
| `verify758` · `verify801` · `verify842` · `verify955` | — | 66/66 · 38/38 · 27/27 · 21/21 |
| `verify498` · `verify614` · `verify615` · `verify650` · `verify712` · `verify768` | — | 60/60 · 10/10 · 40/40 · 26/26 · 36/36 · 19/19 |
| `node tools/smoke.js` | — | **SMOKE PASS** |

* 제품 `index.html` **0줄** · 밸런스 계수 **0줄** · 세이브 이관 없음.
* 199 판정 수치는 **한 자도 안 바뀐다** — 바뀐 것은 «비» 칸의 **문장**뿐이고, 수가 있던 칸은
  그대로 수다(`verify758` 이 비 칸을 안 읽고 두 값에서 다시 나누는 것도 무영향).
* 곁다리 등재 **0건**(회귀 스윕 전부 초록).
