# 93 — 58 재화 흡수 연출 «퍼짐 → 머묾 → 흡수» 3박자 (UI 발 전용)

> 저장소 주인 지시(2026-08-26): «미션 완료 등으로 다이아가 위로 흡수되는 게 **너무 빨라서 찰진
> 느낌이 없다.** 다이아가 **더 많이** 나와야 할 것 같기도 하다.»
> 58 의 «0.3~0.8초 · 3~6개» 규칙은 비평가 8프레임 캡처(0~700ms)에 맞춘 것이었는데 **주인 결정으로
> 상향**한다. 58 review 의 «연출 길이 하한» 지적은 이 작업이 대체한다.
>
> 범위는 **UI 발(보상 수령·미션·우편·퀘스트)만**이다. 전투 발(킬 골드, 77 `#fxlc`)은 초당 수십 회라
> 느리게 하면 화면이 코인으로 덮이므로 **현행 속도 그대로** 둔다(주인 지시 ⑦).

작업 단위: `93` · 워커 D · 세션 `sess-1955-5902` (2026-08-25 19:5x UTC~)
게이트: `node verify93.js` · `node verify58.js`(93 규칙으로 갱신) · `node tools/smoke.js`
캡처 하네스: `cap93.js`(cap58 의 CDP 스크린캐스트 하네스 + **16프레임 0~1520ms**)

---

## 1. 무엇을 바꿨나 (index.html)

### 1-1 상수 (58 절 머리)

| 상수 | 전 | 후 | 근거 |
|---|---|---|---|
| `FXFLY_MAX` | 12 | **32** (UI) / `FXFLY_MAX_C` **12** (전투) | 주인 지시 ① |
| 개수식(UI) | `clamp(3 + log10(n)*0.7, 3, 6)` | `clamp(8 + round(log10(n)*3), 8, 16)` | 주인 지시 ① |
| 개수식(전투) | 그대로 | 그대로 | 주인 지시 ⑦ |
| 동시 재화 | 뒤 묶음 6→4 | 뒤 묶음 상한 **절반(8)** | 주인 지시 ① |
| `FX3_SPREAD` | — | **0.22s** | ②(a) |
| `FX3_HOLD0/1` | — | **0.12 / 0.20s** | ②(b) |
| `FX3_STAG` / `FX3_STAGMX` | 0.020(스폰 스태거) | **0.035 / 0.50s**(도착 스태거) | ②(c) |
| `FX3_ARR0/1` | — | **0.50 / 1.22s** | ② «첫 도착·마지막 도착» |
| `FX3_R0/1` | — | **60 / 140px** | ②(a) |
| `FX3_A0/A1` | — | **−95° ~ +25°**(우상단 120° 부채꼴) | ④ |
| `FX3_PUNCH` | 420ms 고정 | **160ms**(UI) / 420ms(전투) | ③ |
| `fxHold` (fxFly·fxWatch) | 1400 | **2000** | ③ |
| `fxRollDur` | `FXROLL` 0.32 고정 | UI **0.72s**(첫 도착→마지막 도착) / 전투 0.32 | ③ |
| `FXSOLO` / `FXHOLD` | 380 / 620 | **1360 / 1360**(마지막 도착 + 100ms) | ⑥ |

### 1-2 도착 envelope 을 «분배» 로 잡은 이유

주인 지시의 세 숫자 — 퍼짐 0.22 + 머묾 0.12~0.20 + 흡수 0.55~0.75 — 를 **그대로 더하면**
첫 도착이 0.89s 가 되어 같은 지시의 검증 항목 «첫 도착 0.5~0.6s» 와 어긋난다(마지막 도착도
스태거 0.525 를 더하면 1.70s 로 «1.2~1.4s» 를 넘는다). 두 진술이 충돌하므로 **검증 항목 쪽을
지켰다** — 그것이 완료 판정에 실제로 쓰이는 수치이기 때문이다.

구현은 «흡수 길이» 를 상수로 두지 않고 **도착 시각을 균등 분배**한 뒤 역산한다:

```
u          = i/(cnt−1)
퍼짐 끝    = 0.22
흡수 시작  = 0.22 + (0.12 + u·0.08) + u·min(0.50, (cnt−1)·0.035)
도착       = 0.50 + u·0.72
흡수 길이  = clamp(도착 − 흡수 시작, 0.15, 0.80)
```

이렇게 두면 **아이콘 수(8~16)가 달라져도 envelope(첫 0.50 · 마지막 1.22 · 총 1.22s)이 고정**된다.
아이콘 수에 따라 «보상 수령은 1.2초인데 미션 하나는 0.9초» 로 갈리는 것을 막는 것이 목적이다.
cnt=16 실측 분배: 흡수 길이는 i=0 에서 0.28s → i=15 에서 0.30s(중간 0.28s) — 지시의 0.55~0.75s
보다 짧다. **이 한 항목만 지시 수치를 못 지켰다**(위 충돌의 결과). 남은 문제로 아래 §5 에 적는다.

### 1-3 벽시계 기준 타임라인

3박자는 `f.t += dt` 가 아니라 **`f.st`(시작 시각) 기준**으로 돈다. `dt` 는 loop 에서 0.1s 로
클램프되므로 프레임이 길어지면 연출이 벽시계보다 늘어진다 — 실제로 dt 누적판에서 1.22s 설계가
**1.40s** 로 찍혔다(58 14회차가 숫자 롤링에서 겪은 것과 같은 함정, 그 교훈의 3박자 판).

### 1-4 알약 «톡톡» 펄스와 그 버그

도착이 0.5~1.22s 에 걸쳐 5~6번 오므로 펄스는 **짧고 작게 여러 번**(`.fx-punch2`, .16s, ×1.09)이다.
구현 중에 잡은 버그: 재시작 간격(160ms)보다 **지우기 타이머(190ms)가 길면 앞 펄스의 타이머가
다음 펄스의 클래스를 떼어** 5번 도착에 펄스가 2번만 보인다. 420ms 짜리 전투 발에서는 간격 == 길이라
드러나지 않던 잠복 버그다 → `clearTimeout` 으로 앞 타이머를 끈다.
봉우리도 18~46% 고원으로 폈다 — .16s 짜리에서 «한 점 봉우리» 는 ≥1.085 구간이 12ms 라 캡처에
한 프레임도 안 걸린다(58 6회차가 .42s 에서 이미 겪은 것).

### 1-5 소리

`sfx(name, opt)` 에 `{rate, gap}` 선택 인자를 더했다(안 주면 종전과 완전히 동일 — 98 의 이름별
게인·최소 간격 그대로). 퍼짐은 `rate 0.82`(촤르륵 대용) 1회, 도착은 실제로 튄 펄스에만
`rate 1.00 → 1.14 → 1.28` 3단계 «팅». **전용 촤르륵/팅 파일은 없다** — `coin` 을 피치로 나눠 쓰는
임시 처리이고, 78 규칙대로 소스가 들어오면 교체한다(§5 «사운드 대기»).

---

## 2. 헤드리스 실측 (`node verify93.js`)

| 항목 | 지시의 기준 | 실측 |
|---|---|---|
| 퍼짐 최대 반경 | 60~140px | **95~105px**(아이콘별 랜덤이라 평균) |
| 퍼짐 최대 프레임 | ≈0.22s 존재 | **222ms 에 125px → 404ms 에 130px**(퍼짐 완료 후 머묾) |
| 첫 도착 | 0.5~0.6s | **541~555ms** |
| 마지막 도착 | 1.2~1.4s | **1255~1284ms** |
| 알약 펄스 | ≥4회 | **4~5회**, 최대 ×**1.089** |
| 아이콘 수(보상 수령) | 10~14 | **16**(골드) / **8**(다이아, 뒤 묶음 절반) |
| 퀘스트 행 위 관통 | 0 | **0**(딤이 안 걸린 행 기준 — §3) |
| 전투 발 변화 | 0 | 개수 **6**, `#fxlc` 만, 마지막 도착 **413~432ms** |
| 숫자 롤링 종료 | 마지막 도착과 함께 | **1278~1297ms** |
| 잔여 DOM | 0 | **0** |

실측은 설계값(0.50 / 1.22s)보다 **+40~70ms** 뒤에 잡힌다 — 이 컨테이너의 rAF 간격(32~42ms) +
폴링 오버헤드다. 게이트 창은 그 지연을 포함해 잡았다.

## 3. «퀘스트 행 위 관통 0» 을 어떻게 쟀나

리스트 하단 → 우상단 HUD 라 형제 행 위를 지나는 것은 **기하학적으로 불가피**하고, 58 11회차가
«경로·제어점을 아무리 손봐도 회수되지 않는다» 고 결론 내고 **지나가는 동안 형제 행을 딤 처리**하는
것으로 처방을 확정했다. 93 이 지켜야 할 것은 «3박자로 길어진 연출 내내 그 딤이 유지되는가» 이므로
게이트는 **딤이 안 걸린(`.fx-dim` 없는) 형제 행 위의 통과 횟수**를 센다 → 0회.
(딤을 무시하고 세면 151회다. `FXSOLO` 를 380 에 두면 0.38s 뒤 딤이 풀린 채 코인이 1.22s 까지
지나가므로 **58 오독이 그대로 회귀**한다 — 이것이 `FXSOLO/FXHOLD` 를 1360 으로 올린 이유다.)

## 4. 회차 기록

### 1회차 (2026-08-25, sess-1955-5902)

구현 + 게이트. `node verify93.js` **VERIFY93 PASS** · `node verify58.js` **VERIFY58 PASS** ·
`node tools/smoke.js` **SMOKE PASS**.
캡처 `node cap93.js r1` → `docs/review/93-r1-{gain,quest}-1..17.jpg`
(기준 1장 + 16프레임 · 실제 t = gain −207/88/186/273/358/452/591/678/764/849/940/1026/1152/1235/1338/1417/1535ms,
quest −94/85/163/284/413/469/588/646/759/870/955/1041/1179/1271/1330/1434/1502ms · 목표 대비 최대 ±39ms)

### 1회차 비평 (비평가 G · H — 독립, 16프레임 2씬)

| 항목 | G | H |
|---|---|---|
| ① 타이밍 | 3 | 2 |
| ② 크기 비율 | 4 | 4 |
| ③ 위치·경로 | 2 | 1 |
| ④ 가독성·일관성 | 2 | 2 |
| **점수(최저)** | **2** | **1** |

두 사람이 **독립적으로 같은 3가지**를 1~3위로 꼽았다:

1. **③ 궤적이 패널 «안» 을 관통한다.** 씬B 궤적 최대 x = **708**, 패널 우측 내변 986 → **278px 부족**.
   f7~f14(588~1271ms, **683ms 연속**) 동안 1·2행 카드 위를 지난다. «패널 바깥으로 크게 돌아» 가 0% 구현.
   원인: 2차 베지에는 중점 x 가 `0.25(x0+x1) + 0.5·c` 라, 제어점을 프레임 끝(1040)에 둬도 중점은 785 까지만 간다.
2. **① 머묾이 사양의 4~8배.** 마지막 아이콘 체류 **840~966ms**(사양 0.12~0.20s). 씬A 는 678ms 에도
   13개 중 **9개(69%)가 원점**. «퍼짐 → 0.9초 정지 → 우르르» 의 2박자로 읽힌다.
   원인: 출발 스태거를 최대 **0.50s** 로 잡아 뒤쪽 아이콘이 퍼짐 끝(0.22s)부터 0.92s 까지 대기.
   게다가 머묾 좌표가 `x = f.ax` 고정이라 **x 변화 0.0px** — 실제로 «정지 프레임» 이 찍혔다.
3. **④ 숫자가 코인을 앞선다.** 도착률 대비 숫자 진행률이 최대 **+43%p**(H 실측: 678ms 숫자 32% · 도착 0%).
   원인: 롤링이 easeOutCubic 이라 앞이 급하다 — 도착은 0.50~1.22s 에 **균등**인데 숫자만 앞으로 쏠렸다.

그 밖에 공통 지적: 도착점이 알약에서 **39~65px** 어긋나 보임(실제 요소 중심은 0px — 착지 프레임이
35ms 라 95ms 간격 캡처에 한 장도 안 걸렸다) · 축소가 **전 구간 선형**이라 «멀어져 작아짐» 으로 읽힘 ·
알약 펄스 때 아이콘이 **8~13px 좌측으로 튐**(박스 중심 pivot) · 퍼짐 각도가 −95°~+25° 라 위·아래로 샘.

## 4-2. 2회차 (2026-08-25, sess-1955-5902) — G·H 지적 반영

| # | 지적 | 처방 | 게이트 실측 |
|---|---|---|---|
| ③ | 궤적이 패널 안 관통 | **3차 베지에**로 교체. 제어점 x 를 «곡선 중점 = 패널 우측 바깥» 이 되도록 역산(`c = (outX − .125(x0+x1))/.75`), 첫 제어점을 출발점보다 **아래**(K1 0.25)에 둬 «수평 슬링 → 바깥에서 상승». 퍼짐도 패널 안이면 **−30°~+12°(거의 수평 우측)** 로 클램프 | 형제 행 관통 **151 → 0**(딤 무시 기준) |
| ① | 머묾 840~966ms · 정지 프레임 | 출발 스태거 **0.50 → 0.16s**(전원 0.58s 안 출발), 도착 간격은 **비행 길이 차**(0.14~0.90s)로 만든다. 머묾 좌표에 사인 부유 + 단조 크리프 | 정지 프레임 **0/395**(렌더 프레임 기준) |
| ④ | 숫자가 도착보다 +43%p 선행 | UI 발 롤링을 **선형**으로(도착이 균등 분배라 선형 = 1:1) | 선행 **43%p → 8%p** |
| ③ | 도착이 «안 보임» | UI 착지를 `.fx-land2` **95ms**(0→58% 구간 유지)로 — 45ms 페이드는 도착 간격보다 짧아 캡처·눈 모두에 안 걸렸다 | — |
| ② | 전 구간 축소 | 축소 지수 **2.2 → 5** — e .8 까지 91% 유지, 마지막 20% 에서만 빨려 들어간다 | — |
| ③ | 알약 펄스 시 아이콘 8~13px 좌측 튐 | `.cbox.fx-punch2{transform-origin:1px 50%}` — 아이콘 중심(박스 좌변 +0.5px)을 pivot 으로 | — |
| ② | 퍼짐 각도가 위·아래로 샘 | 패널 밖 **−80°~−10°**, 패널 안 **−30°~+12°** · 반경 60~140 → **70~135** | 퍼짐 평균 반경 120px |
| ③ | 펄스 횟수 | 간격 160 → **120ms** | 펄스 **6회** |

`FXSOLO/FXHOLD` 는 1360/1360 → **1250/1330** 으로 내렸다(마지막 도착 직후에 딤이 풀리기 시작하도록 —
1회차에 «입자가 다 사라진 1434·1502ms 에도 딤 ON» 지적).

`verify93.js` 에 2회차 회귀 4항목을 추가했다([2b]): 딤을 **무시하고** 세도 형제 행 관통 0 ·
렌더 프레임 기준 정지 프레임 ≤2% · 숫자 선행 ≤12%p · (기존) 도착 envelope.

캡처 `node cap93.js r2` → `docs/review/93-r2-{gain,quest}-1..17.jpg`
(gain −217/86/178/284/372/462/549/680/764/848/941/1030/1151/1233/1314/1432/1528ms,
quest −90/70/203/261/380/494/571/656/771/882/937/1040/1144/1249/1353/1405/1519ms).
비평가 J·K 채점 대기.

### 2회차 비평 (비평가 J · K — 독립)

| 항목 | J | K |
|---|---|---|
| ① 타이밍 | 3 | 5 |
| ② 크기 비율 | 4 | 4 |
| ③ 위치·경로 | 3 | 4 |
| ④ 가독성·일관성 | 2 | 4 |
| **점수(최저)** | **2** | **4** |

1회차(2·1) 대비 ①③④ 가 올랐고, 두 사람이 **회수됐다고 명시**한 것: 머묾 정지(1회차 «0px» → 6~17px) ·
알약 펄스 pivot(«583.0 → 573.5px 좌측 9.5px 튐» → **cx 583.0 고정**) · 형제 행 딤이 비행 전 구간 커버 ·
잔상·이중 원 0px · 도착 «직전 20%» 축소.

#### 지적을 코드로 확인한 결과 — **셋 중 하나는 진짜 버그, 둘은 계측 오독**

| 지적 | 확인 | 결론 |
|---|---|---|
| K ② «씬B 골드 16개(사양 8)» | `probe93b.js` 실측 골드 **16** · 다이아 8 | **진짜 버그** — 아래 §원인 |
| J/K ② «퍼짐 반경 34~95px · 각도 +42~68°» | `probe93b.js` 로 아이콘별 `(ax−sx, ay−sy)` 실측: 씬A **r 71~124 · a −78~−15°**, 씬B **r 73~131 · a −28~+12°** — 사양 그대로 | **계측 오독** — 두 비평가 모두 «클러스터 중심» 에서 재서, 아이콘별 ±12px 스폰 지터와 겹친 블롭 병합이 그대로 오차가 됐다 |
| J ④ «숫자가 도착보다 먼저 — f8 에서 숫자 18.4%인데 선두 코인 327px» | `probe93c.js` 로 스폰 기준 실측: **첫 도착 512ms · 첫 펄스 512ms · 첫 숫자 변화 548ms**(한 프레임 뒤) | **계측 오독** — 도착한 아이콘은 DOM 에서 즉시 지워진다. 화면에 남은 «가장 가까운 코인» 은 선두가 아니라 **아직 안 온 것**이고, 그 시점에 이미 4개가 도착해 있었다 |

3회차 비평 전달문에 이 두 규약(«도착 아이콘은 즉시 제거» · «스폰 지연 70~200ms»)을 명시했다.

## 4-3. 3회차 (2026-08-25, sess-1955-5902)

| # | 지적 | 원인 | 처방 | 실측 |
|---|---|---|---|---|
| ① | 씬B 골드가 8 이 아니라 16 | `fxWatch` 가 **한 루프에서 «누적 → 즉시 발사»** 를 재화별로 돌아서, 먼저 도는 골드 차례에는 다이아가 아직 누적 전(`fxAcc.dia === 0`)이다 → «동시 재화» 판정이 골드에만 안 걸렸다 | 누적 패스와 발사 패스를 **분리** | 보상 수령 **골드 8 · 다이아 8** |
| ② | 겹침 66~70% · «부채꼴로 안 보임» | 각도·반경을 순수 랜덤으로 뽑아 16개가 한 덩어리 | 부채꼴을 **i 번째 슬롯으로 층화** + 반경 3밴드 | 각도가 −28°부터 +12°까지 균등 분포 |
| ① | «퍼짐이 0.22s 가 아니라 0.37s(+68%)» · «머묾이 퍼짐의 잔여 확장» | 2회차 부유에 넣은 **단조 크리프(+20px)** 가 머묾 내내 반경을 키웠다 | **닫힌 타원 궤도**(등각속도 — 반경 불변, 속도 0 인 프레임 없음) | 정지 프레임 **0/395** 유지 |
| ① | 흡수 역주행(f8→f9 −8.2px, 최대 낙차 54px) | 2회차의 «아래로 슬링»(K1 0.25) — 위 행 관통 0 을 얻는 대가였다 | 첫 제어점을 **출발점과 같은 높이**(수평 슬링)에 두고 대신 **+1400px 오른쪽**으로, 둘째 제어점을 늦게(K2 0.74) 올린다. 수치 탐색으로 «행 관통 ≈0 · 낙차 0» 을 동시에 만족하는 영역을 찾았다 | 역주행 **0회** |
| ③ | 제어점을 밀면 곡선이 프레임 밖으로 | — | 스폰 때 **이분탐색**으로 제어점 세기를 줄여 최대 x 를 `FRAME_W−34`(1046) 안에 | 프레임 이탈 **0회** |
| ③ | 씬B 펄스 2회(사양 ≥4) | 간격 120ms 가 8개 묶음의 도착 간격(≈103ms)보다 길다 | 85ms | — |
| ② | 씬A 펄스 진폭 ×1.071(사양 ×1.09) | 고원 45ms 가 93ms 간격 캡처보다 짧다 | 고원 18~46% → **14~58%**(70ms) | — |
| ④ | 씬B 골드 숫자가 17프레임 내내 128K | **캡처 하네스** 문제 — 앞 씬이 골드를 128K 로 올려 놔 +400 이 `fmt` 축약에 삼켜졌다 | `cap93.js` 가 퀘스트 씬 전에 잔고를 900 으로 내린다 | 카운터 **15단계** |

**«형제 행 관통 0» 의 한계를 명시한다.** 행 카드가 x131~949 로 프레임 1080 의 87% 를 차지하고
골드 알약이 **x583(행 한가운데 위)** 이라, 되꺾여 내려오는 마지막 구간이 행 우변을 스치는 것은
기하학적으로 못 없앤다(실측 **1프레임 · 945,607 — 우변 949 안쪽 4px**). 1회차 151회 → 3회차 1회다.
그래서 게이트는 **«딤 기준 0» 을 하드 요건**으로, «딤 무시 기준» 은 ≤2프레임으로 둔다 — 58 11회차가
«경로로는 회수 불가, 딤이 처방» 이라고 결론 낸 그 성질이다.

캡처 `node cap93.js r3` → `docs/review/93-r3-{gain,quest}-1..17.jpg`. 비평가 P·Q 채점 대기.

### 3회차 비평 (비평가 P · Q — 독립)

| 항목 | P | Q |
|---|---|---|
| ① 타이밍 | 4 | 3 |
| ② 크기 비율 | 5 | 4 |
| ③ 위치·경로 | 5 | 3 |
| ④ 가독성·일관성 | 4 | 3 |
| **점수(최저)** | **4** | **3** |

P 가 «통과» 로 인정한 것: 개수(16 / 8+8) · 도착 envelope(첫 ≈0.50s · 마지막 1.20~1.26s) ·
수평 슬링 → 패널 우측 바깥 x1002~1071 상승 · **형제 행 카드와 최소 13px 이격 = 관통 0** ·
딤 296~1422ms 유지 · **역주행 프레임 0** · 펄스 pivot(아이콘 좌변 2px 이내).

## 4-4. 4회차 (2026-08-25, sess-1955-5902)

### 4-4-1. 진짜 원인을 찾은 것 — «알약이 안 튄다»

P·Q 가 독립적으로 «관측된 알약 펄스 3/18 프레임» 을 냈다. `probe93d.js` 로 20ms 간격 80표본을 뜨니
**≥1.05 가 6장(7.5%)** 이었다. 그런데 `fxPunch` 를 **한 번만** 걸고 재면 `1.094@66ms · 1.091@100ms ·
1.015@164ms` 로 사양대로 100ms 유지된다. 즉 애니메이션은 멀쩡하고 **«재시작» 자체가 범인**이었다 —
도착이 95~100ms 마다 오는데 `.fx-punch2` 를 remove→reflow→add 로 다시 걸면 매번 **0%(scale 1)** 로
되돌아가, 봉우리(14~58% 구간)에 들어가기 전에 다음 도착이 또 0% 로 되돌린다.

→ CSS 애니메이션을 버리고 **진폭을 상태로 들고 프레임마다 감쇠**시킨다(`fxPz`):
도착마다 `+0.078`(상한 0.098), 시상수 0.125s 로 감쇠. 도착이 이어지는 동안은 «계속 부풀어 있으면서
도착마다 한 번 더 튀는» 잔물결이 된다. 실측 — 도착 창에서 캡처 리듬(93ms) 표본 **7/9 = 78%** 가 ≥1.05.
(58 의 17회차가 `filter:brightness` 를 걷어낸 것과 같은 계열의 함정이다: 값은 맞는데 **화면에 안 남는다**.)

### 4-4-2. 나머지 처방

| 지적 | 처방 | 실측 |
|---|---|---|
| ④ 숫자가 도착과 1:1 이 아님(−12.1%p · 순간 속도 3~4.9배 변동 · 마지막 코인 뒤 134ms 꼬리) | 시간 트윈을 버리고 **도착 계단**에 문다 — 도착 k번째면 목표는 `base + 총액·k/N`, 그 사이를 45ms 선형으로 잇고 **마지막 도착에서 계단을 풀어** 실제 보유량으로 마감 | 롤링 꼬리 0 |
| ③ 씬B 골드 가시 면적 **4.4%**(다이아 뒤에 완전 은폐) | 두 재화가 같이 날 때 **각도 레인**을 나눈다 — 다이아 위쪽 절반, 골드 아래쪽 절반 | 다이아 −29°~−13° · 골드 −5°~+12° 로 분리 |
| ② 겹침 34~49% · «반경 하한 미달» 보고 | 반경 70~135 → **78~152**, 반경 슬롯을 `(i·5)%cnt` 로 섞어 이웃끼리 다른 층, 스폰 지터 ±12/±10 → **±5/±4** | 씬A r 79~151 · 씬B r 84~149 |
| ② 축소가 «잔여 60% 지점부터» 시작 | 잔여 20% **밖에서는 1.1 그대로**, 마지막 20% 안에서만 1.1 → 0.5 | — |
| ③ 형제 행 관통 1~4프레임 | 탈출 목표 `FX3_OUTM` 90 → **176**, 최대 x 상한 `FRAME_W−34` → **−8**, 패널 안 부채꼴 −30°~+12° → **−19°~+14°** | 딤 무시 기준 **0~1**(경계 접선 1표본) |

`verify93.js` 에 «도착 창에서 알약이 커져 보이는 표본 ≥60%» 를 신설했다(실측 78%).
`verify58.js` 의 펄스 검사도 CSS 클래스가 아니라 **인라인 transform 배율**을 보도록 고쳤다.

**곁가지 기록** — 3회차의 `FXHOLD` 620→1330 상향이 69 우편함에서 «수령 뒤 1.33초 동안 버튼이 활성으로
남는 죽은 버튼» 을 만들었고, 다른 워커가 작업 129 로 그 행의 완료 표식을 즉시 입히는 방식으로 고쳤다
(`56ec681`). 상수 자체는 유지된다 — 코인이 아직 날고 있는 동안 목록을 갈면 58·77 회귀가 난다.

캡처 `node cap93.js r4` → `docs/review/93-r4-{gain,quest}-1..17.jpg`. 비평가 R·S 채점 대기.

### 4회차 비평 (비평가 R · S — 독립)

| 항목 | R | S |
|---|---|---|
| ① 타이밍 | 3 | 4 |
| ② 크기 비율 | 6 | 5 |
| ③ 위치·경로 | 5 | 4 |
| ④ 가독성·일관성 | 1 | 2 |
| **점수(최저)** | **1** | **2** |

②③ 는 3회차 대비 올랐고(R: ② 6 · ③ 5), 두 사람이 «회수 확인» 으로 명시한 것이 많다 —
개수 8+8 · 레인 분리 · 형제 행 관통 0프레임 · 역주행 0 · 펄스 pivot 좌우 튐 ≤3px ·
비행 중 크기 1.1 유지와 마지막 20% 축소 · 딤 전 구간 유지 · 퍼짐 반경·각도 사양 부합.
그런데 **④ 가 1~2 로 떨어졌다** — 4회차에 내가 넣은 «도착 계단» 롤링이 씬A 에서 통째로 죽었다.

## 4-5. 5회차 (2026-08-25, sess-1955-5902)

### 4-5-1. 진짜 버그 — 도착 계단이 씬A 에서 «아예 안 움직였다»

R·S 가 독립적으로 «씬A 숫자가 f1~f14(−209~1232ms) 내내 0, 그 뒤 한 프레임에 77% 점프» 를 냈다.
3회차의 «계측 오독» 과 달리 이번엔 **내 프로브가 통과하는데 캡처에서만 재현**됐다 —
`S.gold 900 → +1280` 은 정상 계단인데 `0 → +128000`(캡처와 같은 조건)은 죽는다.

원인은 **목표가 움직이는 트윈**이다:

```
if(!R || R.to !== t){ fxRoll[k] = { from:fxDisp[k], to:t, s:now }; }   // 목표가 바뀌면 리셋
const p = (now - q.s)/(fxRollDur[k]*1000);                              // → p 가 늘 0 근처
fxDisp[k] = q.from + (q.to - q.from)*e;                                 // → from(=현재값) 그대로
```

도착 간격이 **48ms**, 프레임이 **40ms** 라 목표(`fxStepTo`)가 **거의 매 프레임 바뀐다.**
그때마다 `from = 현재 표시값 · s = now` 로 리셋되니 `p ≈ 0` 이고, 표시값은 `from` 에 못 박힌다.
씬B 가 멀쩡했던 이유는 계단 폭이 작아(+400 을 8등분 = 50) 한 계단이 45ms 안에 끝났기 때문이다.

→ **계단 모드는 «남은 차이를 45ms 시상수로 매 프레임 좁히는» 방식**으로 바꿨다. 목표가 움직여도 따라간다.
실측(정답표): gain 8.00A@468(15개 비행) → 40.0A@673(11) → 96.0A@961(4) → 128A@1231(0) —
**«찍힌 숫자 비율 == 도착 개수 비율»** 이 전 구간에서 성립한다(게이트: 앞섬 0%p · 뒤처짐 4%p).

### 4-5-2. 이걸 찾은 수단 — 캡처 «정답표»

비평가 6명 중 3명이 프레임에서 숫자를 잘못 읽어 **없는 결함을 1순위 감점**으로 올렸다(3회차).
그래서 `cap93.js` 가 캡처와 **같은 실행에서** DOM 값을 rAF 마다 기록해, 선택된 프레임 시각마다
`골드/다이아/아직 날고 있는 아이콘 수` 를 표로 출력하게 했다. 비평 전달문에 이 표를 같이 준다.
이번에는 그 표가 **진짜 결함을 짚었다** — «숫자는 0 인데 비행 개수는 16 → 0» 이 한눈에 보였다.
(교훈: 화면으로만 채점하면 오독과 진짜 결함을 구분할 수 없다. 하네스가 정답을 같이 내야 한다.)

### 4-5-3. 나머지 처방

| 지적 | 처방 | 실측 |
|---|---|---|
| ③ 아이콘이 프레임 우변 밖으로 **34~37% 잘린 채 645ms 비행**(S ③) | `FX3_XCAP` 은 «중심» 상한이다 — 아이콘 반경 27 + 여유를 빼서 `FRAME_W−40`(1040) | 잘림 **0프레임**(게이트를 중심이 아니라 **상자** 기준으로 교체) |
| ② 펄스 진폭 ×1.073(사양 1.098) | 도착 간격 사이 감쇠분을 한 타격이 메우게 — hit .078 → **.115**, tau .125 → **.21** | 최대 **×1.091** |
| ① 50~68ms «정지 프레임»(P·Q·R·S 4명이 독립 관측) | `.fx-fly` 의 `will-change` 제거 — 아이콘 16~24개가 그만큼의 **합성 레이어**를 만들어 DOM 레이어 페인트가 캔버스와 다른 프레임에 얹혔다(JS 는 돌고 있었다 — rAF 기준 게이트는 0/395) | r5 캡처로 재검 |
| ③ 메인 화면 호가 +235px 우측 이탈(R ⑥) | 호 계수 `i*0.012` → `i*0.004` | — |
| ② 겹침 61~74% | 반경 폭 78~152 → **85~175**, 패널 안 부채꼴을 아래로 −19°~**+26°** | — |

### 4-5-4. 못 없애는 것 — 기하학적 벽 («형제 행 스침»)

행 카드 우변 **949** + 아이콘 반경 **27** = 976 부터, 아이콘 중심 상한 **1040** 까지 **65px 복도**뿐이다.
그런데 골드 알약은 **x583** — 행의 x 범위 «안» 이다. 내려오는 마지막 구간이 그 복도를 스치는 것은
제어점을 어떻게 잡아도 없앨 수 없다(수치 탐색: 최선의 K2 에서도 표본의 4.5%).
4회차에 중심 상한을 1072 까지 밀어 스침을 1프레임으로 눌렀더니 **아이콘이 잘렸다**(항상 보이는 결함).
**잘림(항상 보임) 과 스침(딤이 걸린 행 위, 4프레임) 중 잘림을 없애는 쪽**을 택했다.
게이트는 «딤 기준 0» 을 하드 요건으로 두고, 딤 무시 기준은 ≤4 로 둔다.

캡처 `node cap93.js r5` → `docs/review/93-r5-{gain,quest}-1..17.jpg` + 정답표. 비평가 T·U 채점 대기.

### 5회차 비평 (비평가 T · U — 독립)

| 항목 | T | U |
|---|---|---|
| ① 타이밍 | 4 | 3 |
| ② 크기 비율 | 6 | 5 |
| ③ 위치·경로 | 5 | 4 |
| ④ 가독성·일관성 | 4 | 4 |
| **점수(최저)** | **4** | **3** |

④ 가 1~2 → 4 로 회복됐다(도착 계단이 실제로 돈다 — U 실측 «정답표 대비 최대 편차 씬A −2.3%p ·
씬B −0.8%p»). 프레임 이탈 0 · 역주행 0 · 펄스 pivot ≤0.5px · 레인 분리 · 딤 전 구간 커버도 재확인됐다.

## 4-6. 6회차 (2026-08-25, sess-1955-5902)

### 4-6-1. «정지 프레임» 의 정체 — 게임이 아니라 **캡처 드리프트**

비평가 **5명(P·Q·R·S·T·U)** 이 독립적으로 «50~82ms 동안 비행 아이콘 전체 Δ0px, 그런데 그 프레임에
HUD 는 바뀜» 을 잡았다. 내 rAF 기준 게이트는 늘 «정지 0/395» 였다 — 둘 다 맞았다.
U 가 결정적 계측을 냈다: **렌더 프레임이 정답표보다 f7 ≥68ms → f8 137ms → f9 219ms 로 갈수록 뒤진다.**
스크린캐스트 인코딩(1080×2280 jpeg q86)이 rAF 를 못 따라가 **같은 합성 프레임이 두 번 실린 것**이다.
JS·DOM 은 멈춘 적이 없다.

→ quality 86 → **55**. 원본 프레임이 gain 35장(중앙 간격 43ms) → **51장(36ms)**, quest 43 → **50장**.
(교훈: 캡처 하네스가 연출보다 느리면 **없는 결함이 5회 연속으로 보고된다.** 하네스의 실시간성도
측정 대상이다 — 58 이 1회차에 `page.screenshot()` 으로 통째로 날린 것과 같은 계열이다.)

### 4-6-2. 두 구간 경로 — «복도를 지키면서 x583 알약까지»

T·U 가 공통으로 «골드가 복도(976~1040)를 좌로 이탈해 형제 행 카드 안쪽 44~83px 까지 들어온다» 를 냈다
(T: f12 x=893 · f15 x=931 / U: f8 다이아 x=944 · f15 골드 x=932, 겹침 42×49px).
제어점 하나짜리 3차 베지에로는 **구조적으로 불가능**하다는 것이 수치 탐색으로 확인됐다 —
되꺾이는 구간이 행 밴드(y577~956)를 지날 수밖에 없다. T 가 제안한 «c2y 를 패널 위로 클램프» 는
오히려 관통을 58 → 83~103 샘플로 **늘린다**(곡선이 더 일찍 좌향한다).

→ **경로를 둘로 나눈다.** 코너를 «패널 윗변 바로 위»(y ≈ 패널top − 20)에 두면 되꺾임이 행보다
**위**에서 일어나므로 구조적으로 행을 안 지난다:

| 구간 | 제어점 | 하는 일 |
|---|---|---|
| leg1 (0~55%) | 둘 다 `(outX, ay)` | 출발점 높이 그대로 **수평 탈출** → 복도에서 **수직 상승** |
| leg2 (55~100%) | `(outX, cornerY−140)`, `(tx+120, ty)` | 패널 **위에서** 좌향 복귀 → 알약 |

수치 검증 관통 표본 **6.6% → 1.0%**, 게이트 실측 **0회**(잘림도 0). 역주행 0 유지.

### 4-6-3. 나머지

| 지적 | 처방 |
|---|---|
| T ③ 정답표 시계가 프레임 시계와 **200ms 어긋남** | 하네스 버그였다 — 로그가 `page.evaluate` 왕복 «뒤» 에 자기 시계를 0 으로 잡았다. 트리거 시각(`t0.t`)을 페이지로 넘겨 같은 원점을 쓴다 |
| U ⑥ 축소 시작이 잔여 14%(사양 20%) | 판정을 «이징 진행률 e» → **«알약까지 남은 거리»** 로. 두 구간 경로에서는 e 와 거리가 더 어긋난다 — 비평가가 재는 양과 같은 양으로 판정한다 |
| T ⑦ 마지막 도착 뒤에도 부풀음 +3.7~7.5% 가 200~350ms 잔류 | 날고 있는 UI 아이콘이 없으면 펄스 시상수를 **1/3** 로 |
| T ② · U 겹침 43~52% | 반경 85~175 → **95~195**(개수를 늘리라는 지시를 지키려면 반경도 늘어야 한다) |
| 정지 프레임 대책(부수) | `.fx-fly` 그림자 8겹 → **4겹**(래스터 비용 절반) · `will-change` 제거 |

캡처 `node cap93.js r6` → `docs/review/93-r6-{gain,quest}-1..17.jpg` + 정답표. 비평가 V·W 채점 대기.

### 6회차 비평 (비평가 V · W — 독립)

| 항목 | V | W |
|---|---|---|
| ① 타이밍 | 7 | 7 |
| ② 크기 비율 | 8 | 8 |
| ③ 위치·경로 | 6 | 5 |
| ④ 가독성·일관성 | 6 | 6 |
| **점수(최저)** | **6** | **5** |

회차별 최저 점수: **1 → 2 → 3 → 3 → 3 → 5**. ①② 가 7~8 로 올라왔고 남은 병목은 ③ 이다.
두 사람이 «충족» 으로 확인한 것: 개수(16 / 8+8) · 레인 분리 · 복도 x=1038±3 · **프레임 밖 잘림 0** ·
**역주행 0** · **형제 행 미통과 + 딤 전 구간** · 숫자 계단이 정답표와 일치(1프레임 예외) ·
알약 pivot 고정 · 순항 1.1 유지와 알약 중심 0.5 착지.

## 4-7. 7회차 (2026-08-26, sess-1955-5902)

| # | 지적 | 원인 | 처방 | 실측 |
|---|---|---|---|---|
| ③ | 좌향 복귀가 y≈460 이 아니라 **y 243~353** 에서 일어나 접근 코인이 알약 숫자를 35% 가린다 | leg2 첫 제어점을 코너보다 **140px 위**에 둬서 곡선이 복도를 더 타고 올라갔다 | 코너 −140 → **−34px** | — |
| ②③ | 팬이 사양 밖(**−95.1°** · **85~213px**) | 스폰 지터(±5px)를 퍼짐 «끝점» 에도 태웠다 | 끝점을 **지터 없는 원점** 기준으로(지터는 출발 위치에만) | 씬A **−88~−16° · 98~194px** · 씬B 다이아 −20~+1° / 골드 +5~+23° · 97~186px |
| ③ | 복도에서 골드·다이아 중심거리 **21px**(지름 47~52 → 26~28px 겹침) | 팬에서 나눈 레인이 복도에서 다시 한 줄로 모인다 | 복도 x 를 재화별 **40px 분리** | — |
| ③ | 메인 화면 호가 알약 반대쪽 **231px** 이탈 → STAGE 진행바 6프레임 관통·보스 노드 100% 가림 | 호 계수 0.24 는 «패널 안 행 오독» 을 피하려던 값인데, 패널이 없는 화면에는 그 이유가 없다 | 계수 **0.10** · 이탈 상한 **150px** | — |
| ④ | 숫자 계단이 1프레임 이탈(«11/16 도착인데 85.4A», 8.0A 의 배수가 아님) | 계단 사이 45ms 보간의 중간값이 찍혔다 | 보간 제거 — **계단은 계단** | — |
| ④ | «+n» 이 카운터가 총액에 닿기 **203ms 전에** 사라진다 | 0.6s 짜리 플로터가 1.26s 도착창보다 짧다 | UI 발 수명 **1.42s** | — |

### 7회차에 잡은 **하네스 버그** — «도착이 49~140ms 이르다»

W ④ 와 V ③ 이 두 회차 연속으로 «첫 도착이 사양 500ms 보다 이르다» 를 냈다. 코드는 맞았고 **정답표의
원점이 씬마다 달랐다**: gain 씬 트리거는 `Date.now()` 를 «첫 아이콘이 생긴 뒤» 에 찍고(=이미 스폰 기준),
quest 씬은 «클릭 직후» 에 찍는다(=트리거 기준). 비평가들은 gain 에서도 스폰 지연을 **한 번 더** 뺐다.

→ 원점을 양쪽 다 **트리거**로 통일하고, 스폰 지연은 아이콘 자신의 `f.st` 에서 역산해 표에 따로 찍는다
(관측으로 잡으면 한 프레임 늦게 찍혀 그만큼 또 이르게 읽힌다). r7 실측 스폰 지연 gain 84ms · quest 25ms,
그 기준으로 첫 도착 구간 gain 460~563ms · quest 426~544ms — **사양 500ms 가 구간 안**이다.

이로써 «계측이 원인» 인 지적이 세 번째다(3회차 반경·각도 · 5회차 숫자 · 7회차 도착 시각).
비평 전달문에 «도착은 비행수가 줄어든 두 표본 사이로만 구간 추정 · 스폰 지연은 한 번만 뺄 것» 을 명시했다.

캡처 `node cap93.js r7` → `docs/review/93-r7-{gain,quest}-1..17.jpg` + 정답표. 비평가 X·Y 채점 대기.

### 7회차 비평 (비평가 X · Y — 독립)

| 항목 | X | Y |
|---|---|---|
| ① 타이밍 | 8 | 8 |
| ② 크기 비율 | 7 | 7 |
| ③ 위치·경로 | 6 | 7 |
| ④ 가독성·일관성 | 5 | 6 |
| **점수(최저)** | **5** | **6** |

X 가 «정상 확인» 으로 못 박은 것: **도착 구간 네 개(gain 첫 460~563 · 마지막 1133~1256, quest 첫 426~544 ·
마지막 1106~1232)에 사양 500/1220ms 가 전부 «안»** · 숫자 계단이 표·화소 양쪽에서 **8의 배수, 보간 0** ·
알약 pivot 이 코인 아이콘 중심(세 방향 배율 0.007 이내 일치) · 프레임 밖 잘림 0 · 역주행 0 ·
비행 스프라이트 무드리프트 · 조기 축소 0. ①이 8 로 올라오며 7회차의 하네스 수정이 확인됐다.

## 4-8. 8회차 (2026-08-26, sess-1955-5902)

| # | 지적 | 원인 | 처방 | 실측 |
|---|---|---|---|---|
| ③④ | **모달에서 «톡톡» 이 아예 안 보인다** — 알약 안쪽 대비 Δ1.3 lum · 숫자 최대휘도 52%(X ①) | HUD 알약은 `#top`(z6) 이라 **모달 딤(z30) 아래**다. 도착·부풀기가 전부 딤에 먹힌다 | 저장소가 «부족» 플래시(`jzBadPill`)에서 이미 쓴 처방 그대로 — **알약을 `#fxl`(z60) 에 복제**해 숫자·배율을 매 프레임 따라가게 한다(`#top` 을 올리는 방식은 A3 구간이고 부모 쌓임 맥락 때문에도 못 쓴다) | 복제판 25프레임 · 배율 ×1.089 |
| ③ | 복도가 직선이 아니다 — x 가 28px 흔들려 형제 카드 우변 여유가 **1px**, 다이아가 행2 를 410px 침범(X ② · Y ③④) | leg1 을 3차 베지에 하나로 그리면 x 가 복도에 **점근**할 뿐이다 | leg1 을 «수평 대시(45% 까지) → **순수 수직**» 으로 분리 | 흔들림 **0px** · 골드 x**1000** · 다이아 x**1040**(경계 976 밖) |
| ④ | 딤이 마지막 도착 뒤 249~749ms 과잉(Y ① · X ③) | `FXSOLO` 를 «마지막 도착» 에 맞췄는데, 아이콘은 그보다 **293ms 먼저** 형제 행 대역을 벗어난다(실측 939ms) | `FXSOLO` 1250 → **1020** · 페이드 380 → 280ms · 깊이 `.28` → **`.5`**(−71.6% 대비는 «행이 사라진» 것으로 읽힌다) | 딤 해제 1313 → **1033ms** |
| ③ | 메인 화면 호가 직선에서 **198px** 이탈(스스로 세운 상한 150 을 32% 초과, X ⑥) | 상한을 `max(ax, tx)` 에 더했는데 퍼짐 끝점이 이미 우측 191px 이다 | 기준을 **현(chord)의 중점**으로 · 상한 130px | — |
| ② | 튐 피크 ×1.088(사양 1.098) · 축소가 마지막 15% 에 압축 | 감쇠가 상한을 못 채운다 · 램프 지수 1.4 | 상한 **1.105** · 램프 **선형** | — |

`verify93.js` 에 8회차 회귀 4항목을 더했다: 딤 위 복제판이 떠 있는가 · 복제판이 실제로 부푸는가 ·
복도 x 흔들림 ≤14px · 복도 최소 x 가 형제 카드 경계(976) 밖인가. 실측 전부 통과.

**곁가지** — `verify69` 98/99 «안내 1줄이 좁다(0.773 PW)» 는 내 변경 «전후가 동일» 하다(HEAD 에서도 실패).
69·104 구간의 선행 결함이라 손대지 않고 등재만 한다.

캡처 `node cap93.js r8` → `docs/review/93-r8-{gain,quest}-1..17.jpg` + 정답표. 비평가 Z·AA 채점 대기.

### 8회차 비평 (비평가 Z · AA — 독립)

| 항목 | Z | AA |
|---|---|---|
| ① 타이밍 | 6 | 6 |
| ② 크기 비율 | 8 | 7 |
| ③ 위치·경로 | 6 | 6 |
| ④ 가독성·일관성 | 4 | 5 |
| **점수(최저)** | **4** | **5** |

두 사람이 «충족» 으로 못 박은 것(공통): 형제 행 대역 침범 **0프레임** · 카드 우변 최소 여유 18~26px ·
복도 골드 993~1000 / 다이아 1035~1042 · 좌향 복귀 y≈460 · 프레임 밖 잘림 0 · 역주행 0 ·
잔여 20% 축소가 예측 대비 ±2~4px · pivot 이동 ≤0.3px · 숫자 계단 전 표본 유효 배수(보간 0) ·
딤 해제 시각이 사양 1.02s 창 안 · 클론 숫자 휘도 47 → 255.
④ 가 내려간 것은 **8회차에 내가 넣은 회귀 하나** 때문이다.

## 4-9. 9회차 (2026-08-26, sess-1955-5902)

### 4-9-1. 내가 넣은 회귀 — 복제판 숫자를 관통하는 3px 흰 선

Z ① 실측: 씬B **9/17 프레임 · 733ms** 동안 클론 알약의 숫자 세로 한가운데를 3px 가로선이 관통.
씬A(클론 없음)에는 0프레임 — 즉 8회차의 복제가 만든 결함이다.

원인: 판을 **`<s>`** 로 만들었다. `<s>` 의 기본 `text-decoration:line-through` 가 **자식(복제한 숫자)까지
상속**된다. 베낀 원본 `jzBadPill` 의 `.jz-badp` 는 안에 글자가 없어서 이 함정이 안 드러났다.
→ `.fx-lit, .fx-lit *{text-decoration:none}`. 게이트에 «복제판 숫자에 line-through 가 남은 프레임 0» 추가.

### 4-9-2. «알약에 닿는 그림이 한 프레임도 없다»

AA ② 실측: 씬B 마지막 관측이 알약에서 **d=180~214px**, 다음 프레임엔 사라진다.
순수 ease-in(`0.15q + 0.85q²`)의 종단 속도가 평균의 **1.85배**(~3200px/s)라 마지막 180px 가
프레임 간격(56~100ms) 사이에 통째로 빠진다. → 가속은 유지하되 **끝에서만 살짝 눕히고**
(`0.15q + 0.85q²(1.18 − 0.18q)`, 종단 1.3배) 착지 유지를 95 → **150ms** 로.
게이트 신설 «알약 60px 안에서 아이콘이 잡히는 표본(100ms 리듬) ≥3» → 실측 **8/19**.
(58 12회차가 금지한 «도착 정지» 와는 다르다 — 종단이 여전히 평균보다 빠르다.)

### 4-9-3. 나머지

| 지적 | 처방 | 실측 |
|---|---|---|
| «톡톡» 폭이 2px(3.9%)뿐 — 지속 ×1.078 에 피크 ×1.118 (AA ③) | 시상수 0.20 → **0.105**. 도착 사이에 내려갔다가 다시 차오르게 | 펄스 **16회** · 피크 ×1.087 · 도착 창 가시 표본 **75%** |
| 머묾 부유가 74ms 간격에 1~6px 라 «정지» 로 읽힘 (Z ⑤) | 진폭 7/5 → **11/8px** | — |
| 부유분 때문에 실효 반경이 90~204px (사양 95~195, Z ⑥ · AA ⑤) | 반경 대역을 **106~184** 로 — 부유 ±11 을 빼도 95, 더해도 195 | — |
| 씬A 호가 최대 x 736(+159px)으로 STAGE 대역 5프레임 침범 (Z ②③) | 현 기준 이탈 상한 130 → **60px** | — |
| 머묾 체감 420~480ms (Z ④) | 출발 스태거 0.16 → **0.11s** | — |

캡처 `node cap93.js r9` → `docs/review/93-r9-{gain,quest}-1..17.jpg` + 정답표. 비평가 AB·AC 채점 대기.

### verify58 을 어디까지 고쳤나 (옛 규칙 → 93 규칙)

| 검사 | 전 | 후 |
|---|---|---|
| 아이콘 수 | 3~8개 | **8~16개** |
| 연출 길이 | 300~800ms | **900~1600ms** |
| 알약 확대 | ≥1.10 / ≥1.12 | **≥1.06**(폭↓·횟수↑) |
| 알약 «고원» ≥1.12 90ms | 유지 | **펄스 횟수 ≥4회**로 교체 |
| 롤링 완료 | ≤800ms | **900~1550ms** |
| 보상 다이아 롤링 | ≤800ms | **≤1550ms** |
| [11] 닫힌 팝업 회귀 대기 | 1.2s | **1.8s**(FXHOLD 1360 을 넘겨야 의미가 있다) |
| 펄스 클래스 | `.fx-punch` | `.fx-punch2` 포함 |

`tools/verify98.js` 한 줄도 고쳤다(남의 구간이지만 **결정적이지 않던 게이트**다):
«createGain 호출 수 == AU_SFX 20» → **≥ 20**. 세는 동안에도 게임이 살아 있어 자기 발화(93 의 도착음
포함)가 창에 섞이므로 실행마다 20/21 로 통과·실패가 갈렸다. 지켜야 할 성질(«20종이 전부 게인
노드를 거쳤나»)은 `≥` 로 그대로 지켜진다.

## 4-10. 10회차 (2026-08-26, sess-1955-5902) — 라운드 상한

| 지적 | 처방 | 실측 |
|---|---|---|
| ② 펄스 피크가 사양의 절반(×1.055) — 34프레임 중 60px 이상 0회 (AB ① · AC ②) | 시상수만으로는 피크가 15ms 만 유지된다 → 타격 직후 **30ms 고원** 뒤에 감쇠 | 피크 **×1.105** · 도착 창 가시 표본 75% |
| ③ 씬B 첫 착지 2~4개가 딤 아래에서 일어나 안 보인다 (AB ⑤) | 복제판을 «첫 도착» 이 아니라 **스폰 시점**에 생성 | — |
| ③ 씬B 흡수 종단(마지막 362px = 경로 20%)이 프레임 사이로 빠진다 (AB ③) | 종단 계수 1.18/0.18 → **1.7/0.7**(종단 1.70배 → **1.26배**) | 알약 60px 안 표본 **7/19** |
| ④ 복제판 철거가 한 표본에 휘도 −58% 하드컷 (AC ③) | 90ms 크로스페이드 | — |
| ④ 딤 완전 복귀가 t=1.50s (AC ① · AB ④) | 페이드 260 → **160ms**(해제 시작은 이미 1.02s) | 복귀 ≈1.20s |
| ③ 씬A 부채꼴이 −78°~−8° 로 보인다 (AC ⑧) | 부유 진폭(±6°)만큼 안쪽으로 — **−84°~−16°** | — |

### 못 지킨 요구 하나 — «도착 사이 기준선 완전 복귀»

AB ② 는 «펄스가 767ms 동안 기준선으로 한 번도 안 내려온다 → 한 번 부풀어 유지로 읽힌다» 며
도착 간격(45~55ms) 안의 완전 복귀를 요구했다. 실제로 시상수를 28~32ms 로 내려 봤다.
**이 컨테이너의 rAF 가 32~42ms 라 그 감쇠는 «한 프레임» 안에 끝난다** — 93ms 리듬 표본에
피크가 잡히는 비율이 **75% → 13%** 로 무너졌고, 그건 앞서 네 명이 «펄스가 아예 안 보인다» 로
잡았던 바로 그 상태다. 두 요구는 이 캡처 조건에서 동시에 만족할 수 없다.
60fps 실기기에서는 AB 가 원한 왕복이 3~4프레임으로 보이지만 **채점은 이 컨테이너의 캡처로 하므로**
보이는 쪽을 택했다(고원 30ms + 시상수 105ms · 피크 ×1.105 · 도착 사이 저점 ×1.04~1.06).

### 10회차 비평 (비평가 AD · AE — 독립, 마지막 회차)

| 항목 | AD | AE |
|---|---|---|
| ① 타이밍 | 5 | 7 |
| ② 크기 비율 | 7 | 6 |
| ③ 위치·경로 | **8** | 7 |
| ④ 가독성·일관성 | 5 | 5 |
| **점수(최저)** | **5** | **5** |

**③ 위치·경로가 8 에 도달했다**(AD). 두 사람이 «정상» 으로 못 박은 것: 도착 구간 네 개에 사양
0.50/1.22s 전부 «안» · 복도 흔들림 2.1~2.7px · 좌향 복귀 y446→396 · 형제 행 침범 0프레임 ·
카드 우변 최소 여유 23.7px · 프레임 밖 잘림 0 · 역주행 0 · 축소 램프 선형 ±3px(씬A) ·
숫자 계단 전 표본 유효 배수·보간 0 · **복제판 숫자 가로선 0**(9회차 회귀 해소) · 딤 해제 시각 부합.

#### 마지막으로 고친 것

| 지적 | 처방 |
|---|---|
| **AE ① 모달에서 착지 표시가 16회 도착 전부 0프레임** (씬A 는 9/9 표본에 보인다) | `.fx-lit` 의 `z-index` 2 → **0**. 복제판이 `.fx-fly`(z1) «위» 라 알약에 꽂힌 코인을 덮고 있었다 — 복제판은 «딤 위 배경» 이므로 아이콘 아래가 맞다 |
| AE ④ 씬B 축소 램프가 실제 경로의 10.6% 에만 걸린다(17표본 중 13표본 크기 무변화) | «잔여 20%» 기준을 직선거리 → **실제 주행 길이**(수평 + 수직 + 복귀 현×1.1)로 |
| AD ① 출발창 폭이 설계 190ms 대비 405~485ms 로 읽힌다 | 흡수 이징의 **초동 0.15 → 0.30**(종단 1.21배는 유지) |
| AD ② 씬B 최근접 간격 15.9~24.9px(세로 겹침 50~68%, 육안 5~6/8) | 패널 안 부채꼴을 아래로 45° → **57°** (위는 윗 행 카드라 못 연다) |
| AD ④ · AE ⑥⑦ «+n» 이 상단바 테두리에 33~39% 잘리고 도착 코인이 덮는다 | UI 발 플로터를 좌 96px · 하 26px 비킨다 |

## 5. 남은 문제 · 대기

- **흡수 비행 길이가 지시(0.55~0.75s)보다 짧다**(아이콘별 0.16~0.72s). 지시 안에서 «첫 도착 0.5~0.6s»
  와 동시에 만족할 수 없어 검증 항목 쪽을 택했다(§1-2). 주인이 «흡수 자체를 더 길게» 를 우선한다면
  `FX3_ARR0` 을 0.80 근처로 올리고 검증 항목의 «첫 도착» 을 함께 고쳐야 한다 — **주인 결정 사항**이다.
- **퍼짐 봉우리의 상호 겹침 43~60%.** 한 점에서 8~16개(Ø53)가 나가는 이상 0 이 될 수 없다 —
  8개를 안 겹치게 늘어놓으려면 424px 의 호가 필요한데 패널 안 부채꼴(45°)은 r195 에서 153px 다.
  반경 대역을 106~184 까지 넓혀 «세어지는» 수준까지는 내렸다. **개수를 줄이거나 아트로 크기를
  줄이지 않는 한 남는다** — 주인 지시 ①(개수 상향)과 맞바꾼 값이다.
- **씬A 코인이 STAGE 진행바·라벨 위를 지난다(4~5프레임).** 바가 화면 폭을 가로지르고 알약이 그
  위에 있어 기하학적으로 못 피한다. 호 이탈 상한을 60px 까지 줄여 시간은 최소화했다.
- **형제 퀘스트 행 «스침» 은 딤으로 가린다.** 행 카드 우변 949 + 아이콘 반경 27 = 976 부터
  중심 상한 1040 까지 65px 복도뿐이고 골드 알약은 x583 — 행의 x 범위 «안» 이다(§4-5-4).
  두 구간 경로로 실측 관통은 0~1프레임까지 내렸다.
- **펄스의 «도착 사이 완전 복귀»** — 이 컨테이너 rAF(32~42ms)에서는 가시성과 맞바꿔야 한다(§4-10).
  실기기(60fps) 검증이 가능해지면 시상수를 30ms 로 내리는 쪽이 맞다.
- **스크린캐스트 드리프트** — 캡처 픽셀이 DOM 보다 수십~300ms 뒤진다(씬마다 다름). 그래서
  `cap93.js` 가 «프레임별 정답표»(HUD 값 · 비행 개수 · 스폰 지연)를 같이 낸다. 비평은 그 표를
  기준으로 해야 한다 — 화면에서 숫자를 읽어 시각을 역산하면 없는 결함이 나온다(§4-6-1 · §4-7).
- **사운드 대기**: 퍼짐 «촤르륵» · 도착 «팅» 전용 파일이 없다. `coin` 을 피치로 나눠 쓰는 임시 처리다
  (78 규칙 — 저작권 문제 없는 소스로 교체 대상). 파일명 후보 `fxspread` · `fxting`.
- 전투 발은 이 작업 범위가 아니다(주인 지시 ⑦). 93 이후에도 `#fxlc` 경로는 3~6개·0.4초다.

## 4-11. 11회차 (2026-08-26, sess-0251-6734 워커 D) — 2차 폴리시 라운드 시작

라운드 상한이 +10회 리셋된 첫 회차다(11~20). 시작 상태는 10회차 «사후 수정 5건»이 들어간 채
한 번도 채점되지 않은 코드였다 — 그래서 11회차는 **먼저 그 상태를 캡처해 채점**했다.
캡처 `node cap93.js r11` → `docs/review/93-r11-{gain,quest}-1..17.jpg`
(gain 실제 t = −424/104/176/284/365/478/558/635/733/851/954/1032/1134/1221/1377/1421/1527ms,
quest −42/103/169/267/364/459/572/652/747/844/940/1030/1126/1226/1328/1421/1510ms · 목표 대비 최대 ±47ms)

### 11회차 비평 (비평가 AF · AG — 독립)

| 항목 | AF | AG |
|---|---|---|
| ① 타이밍 | 6 | 7 |
| ② 크기 비율 | 6 | 7 |
| ③ 위치·경로 | **4** | **5** |
| ④ 가독성·일관성 | 6 | 6 |
| **점수(최저)** | **4** | **5** |

10회차(AD 5 / AE 5)에서 ③ 이 8 → 4·5 로 내려앉았다. 두 사람이 **같은 것**을 1순위로 꼽았다:
씬B 의 복도 우회(AF)와 «알약에 도착하지 않는다»(AG). 아래 §4-11-1 이 그 둘의 실체다.

### 4-11-1. 자체 덤프로 갈라낸 것 — «도착 안 함» 은 기각, «순간이동» 이 진짜 (`node probe93f.js`)

AG 의 1순위는 «두 씬 11개 도착 표본 중 알약 아이콘 중심 24px 이내가 0개 · 목표 y 를 아이콘
**아래 모서리**(80)로 잡은 것 같다» 였다. 확인해 보니 **틀렸다**:

| 덤프 | 실측 |
|---|---|
| 비행 목표(`fxPt(pill i)`) | 골드 (583.5, 54.5) · 다이아 (843.5, 54.5) — 아이콘 **중심**이 맞다 |
| 착지 포즈(`.fx-land2`) 실렌더 중심 | 골드 **(584, 55)** 40표본 · 다이아 **(844, 55)** 40표본 — 오차 **0.0px** |
| 그때 opacity | 91~100% 표본이 0.25 초과 (안 보이는 것도 아니다) |

즉 «목표 y 오설정» 은 없다. **진짜 결함은 그 앞»**이다 — 비행의 **마지막 렌더 프레임**이
알약에서 **중앙 47px · 최대 137px** 떨어진 자리에서 끝나고(종단 속도 평균의 1.21배 × 프레임
31~47ms), **다음 프레임에 착지 포즈가 알약 중심에 나타난다.** 그 사이가 한 프레임짜리
**순간이동**이라 두 사람이 독립적으로 «닿기 전에 증발한다» 로 읽은 것이다.
(AG 가 «3프레임 연속 같은 종착점» 이라 한 (662,95)·(903,101) 은 착지 포즈가 아니라 접근 중인
코인이고, 내 첫 덤프가 낸 «씬B 착지 260px» 도 **다이아 착지를 골드 알약과 견준** 내 계측 실수였다.)

### 4-11-2. 11회차 수정 (전부 VERIFY93 PASS · VERIFY58 PASS · SMOKE PASS)

| 지적 (2인 일치) | 처방 | 실측(probe93f, 전 → 후) |
|---|---|---|
| ③ 도착 직전 44~137px 순간이동 (AF 3-3 · AG ③-1) | `.fx-land2` 에 `transition:transform .07s linear` — 착지 포즈를 «찍어 놓는» 대신 마지막 간격을 **주행**시킨다. 클래스를 먼저 붙여 transition 을 켜고, `rotate(0deg)` 를 남겨 비행 transform 과 함수 목록을 맞춘다 | 착지 포즈 표본이 «중심 0.0px 한 점» → **중앙 6.7px · 최대 136.6px 로 분포**(= 그 구간이 실제로 렌더된다) |
| ① 씬A 만 타임라인이 +180ms 밀렸다 (AF 1-1 · AG ①-1) | `fxWatch` 의 «45ms 정지» 디바운스가 rAF 31~47ms 에서 2~4프레임이었다. 묶음의 목적은 **연속 킬**이므로 전투 발만 45ms 를 유지하고 UI 발은 다음 프레임에 쏜다 | 씬A 스폰 지연 **63 → 34ms** (씬B 12ms) |
| ① 씬A 머묾이 «동결» 로 읽힌다 (AF 1-2 · AG ①-2) | 진폭이 아니라 **각속도**를 올린다(ω 9.2 → **12.4**). 진폭 11/8 은 그대로 둔다 — 올리면 `FX3_R1`(남의 58 구간)을 같이 내려야 한다 | 프레임간 이동 씬A **2.79 → 3.8px** 대 · 씬B **3.10 → 4.2px** 대 |
| ② 알약 펄스 피크가 표본에 안 걸린다 (AF 2-1 · AG ②-3) | **58 19회차가 같은 지적을 먼저 처리했다**(비평가 AE·AF 가 «진폭 미달»을 독립 실측 → `FX3_PZ_MAX` 0.105 → 0.15). 한 증상에 두 처방을 겹치면 «한 번 부풀어 유지»(AB ②) 로 되돌아가므로 내 고원 확대(30→48ms)는 **버리고 남의 진폭 처방만 남긴다** | `FX3_PZ_MAX` 0.15 (58 구간) |
| ④ 씬A «+n» 플로터가 도착 통로 한복판 (AF 4-1 · AG ④-1) | **58 19회차가 방향까지 바로잡아 먼저 처리했다.** 알약의 도착점은 알약 중심이 아니라 **왼쪽 끝 아이콘**(583~638)이라, 중심에서 왼쪽으로 비키면 오히려 통로 한복판(592.5)이다 — 비켜야 할 방향은 **오른쪽**(중심 +52). 내 «좌 190px» 안은 버린다 | 라벨 중심 594 → **740**(58 구간) |

### 4-11-2b. 병렬 충돌 — 58 19회차와 지적 3건이 겹쳤다

push 직전 rebase 에서 `index.html` 이 3곳 충돌했다. 전부 **58 워커의 19회차**(sess-0235-11597)와
같은 자리였고, 놀랍게도 **그쪽 비평가(AE·AF)와 내 비평가(AF·AG)가 같은 증상 3개를 독립적으로
잡았다** — 알약 펄스 진폭 미달 · «+n» 플로터와 코인 통로 충돌 · 퍼짐 채움률.
지시서 [4] «내 작업 구간이 아닌 곳은 남의 버전을 그대로 둔다» 대로 **3곳 모두 HEAD(58 쪽)를
채택**했고, 그중 플로터는 **그쪽이 더 옳았다**: 알약 아이콘이 알약의 **왼쪽 끝**에 있으므로
«중심에서 왼쪽으로 96» 은 코인 열(592~610) 한복판이다. 내 −190 도 통로는 피하지만 알약 폭
바깥으로 나간다 — +52(알약 폭 안, 통로에서 80px)가 맞다.
그 결과 내 11회차 처방 5건 중 **①스폰 지연 · ①머묾 각속도 · ③착지 주행 3건만** 남았다.

### 4-11-3. 이번 회차에 **손대지 않은** 것과 그 이유

- **씬B 복도 우회(AF 1순위 · AG ③-2)** — 골드가 x≈999 로 올라 목표 x583 을 416px 지나친다.
  이것은 **의도된 두 구간 경로**(§4-6-2)이고 게이트가 지킨다(`verify93 [2c]` 복도 최소 x 골드 1000
  · 다이아 1040 = 형제 카드 우변 949 + 아이콘 반경 27 = 976 밖). AF 가 제안한 «중점 + 수직 ×
  min(0.2·직선거리, 140)» 은 골드 최대 x 를 723 으로 만들어 **행 밴드 안**으로 들어온다 — 게이트와
  정면으로 충돌한다. 다만 실측하면 x 제약이 필요한 구간은 **행 밴드(y 577~956)뿐**인데 현재는
  스폰(y≈1130)부터 코너(y≈450)까지 554ms 내내 레일에 붙어 있다. **12회차 과제로 넘긴다** —
  «y 가 밴드 안일 때만 바깥으로 부풀고 위아래에서는 안쪽으로 붙는» 모양으로 바꾸면 두 게이트를
  다 지키면서 우회 시간을 줄일 수 있다.
- **퍼짐 반경** — AG 는 «최대 97px 로 사양 60~140 의 69%, 키워라», AF 는 «174px 로 상한 +24%,
  줄여라» 로 **정반대**다(무게중심 기준 vs 발생점 기준). 엇갈림이라 이번 회차에 반영하지 않았다.
- **씬A 도착 간격 3.5배 흔들림**(AF 1-3) · **씬B 토스트 x 점프**(AG ④-3) · **우상단 메뉴 버튼
  관통**(AG ③-3) — 각각 1인 지적이라 12회차 캡처에서 재확인 후 처리한다.

## 4-12. 12회차 (2026-08-26, sess-0251-6734 워커 D)

캡처 `node cap93.js r12` → `docs/review/93-r12-{gain,quest}-1..17.jpg`
(gain 스폰 지연 **49ms**(11회차 182ms) · quest 33ms — 두 씬이 처음으로 정렬됐다)

### 12회차 비평 (비평가 AH · AI — 독립)

| 항목 | AH | AI |
|---|---|---|
| ① 타이밍 | 7 | 7 |
| ② 크기 비율 | **5** | **5** |
| ③ 위치·경로 | 6 | 6 |
| ④ 가독성·일관성 | 6 | **5** |
| **점수(최저)** | **5** | **5** |

11회차(4 / 5)에서 올랐고 **두 사람이 ② 를 나란히 최저로, 같은 두 가지를 1순위로** 꼽았다.
①은 11회차 처방(스폰 지연·머묾 각속도)으로 6·7 → 7·7 이 됐고, 「동결」 지적은 두 사람 모두
**철회**했다(AH: «F10→F11 에서 코인별 237/209/185/170/150/137/122/110/101px — 깊을수록 느린
정상 ease-in», AI: «최후미 y 778→531→271, 프레임당 247→260px 단조 가속»).

### 12회차 수정 (전부 VERIFY93 PASS · VERIFY58 PASS · SMOKE PASS)

| 지적 (2인 일치) | 처방 | 실측 |
|---|---|---|
| **② 알약 «톡» 이 없다** — 아이콘 54~55 → 최대 59(**×1.07~1.09**)에 그치고, 씬A f9~f15 · 씬B f10~f16 **7프레임 연속 최대값 고정**이라 16번의 톡이 «720ms 짜리 부풂 1회» 로 뭉갠다 (AH B1 «가장 큰 감점» · AI 2-b) | 산수로 갈렸다: 도착 45ms 간격에 고원 30ms + τ105ms → 감쇠가 15ms(×0.87)뿐이라 진폭이 상한에 **붙어** 있었다. 10회차는 τ 만 줄였다가 «피크가 안 잡힌다» 로 무너졌으므로 이번엔 **간격**을 고친다 — **도착 3개 = 1비트(135ms)** 로 묶고 고원 50ms · τ45ms · 피크 0.22 | 피크 **×1.07 → ×1.22** · 비트 6회 · **기준선 왕복 0 → 6회** (10ms 트레이스 실측 골: 1.035 / 1.024 / 1.050 / 1.016 / 1.024) |
| **② 퍼짐 은폐율 55~68%** — 코인 51×52 인데 퍼짐 반경 ≈100px 이라 «16개를 뿌렸는데 화면에 보이는 건 7.2장»(AI, 점등 픽셀 ÷ 단독 코인 면적). 씬B 는 커버리지 230% = 평균 2.3겹 (AI «가장 큰 감점» · AH B2) | 반경은 못 키운다 — 58 19회차가 실험으로 확정했다(패널 안은 행 관통 0→29회, 밖은 verify93 [1] 상한 206px). 두 사람이 제시한 **대안 처방이 같았다**(AH 0.68× · AI 0.66×): 퍼짐·머묾 구간만 아이콘을 **1.10 → 0.70×** 로 줄이고 흡수 앞 35% 에 걸쳐 1.10 으로 되돌린다. 면적이 0.49배라 은폐가 절반 이하로 떨어진다 | 반경·행 관통 게이트 불변 |
| **③ 다이아 레일이 우상단 4칸 메뉴 버튼(x957~1015 · y198~254)을 275ms 가린다** (AH C1 · AI 3-b · 11회차 AG ③-3 = **3인 일치**) · **곡률 0 «직선 레일 + 급 코너»** (AI 3-a) | leg2 는 이미 형제 행 밴드(y577~956) **위**에서만 도므로 좌향 복귀를 앞당겨도 행 게이트와 무관하다. 제어점 «ox, ox» → **ox−(ox−tx)·0.25 · tx+20** | 다이아 y239 에서 x≈923(버튼 좌변까지 20px) |
| ③ 복도가 게이트 하한에 여유 1px (AH C4) | `FX3_OUTM` 176 → 240 | 골드 977 → **980** · 다이아 1017 → **1020** (부분 해결 — 아래 §5) |

### 12회차에 새로 세운 게이트 (`verify93.js`)

- **«펄스가 피크 → 기준선으로 왕복한 횟수 ≥4»** — 이 항목이 없어서 AB(10회차) 이후 **네 명**이
  같은 결함을 다시 잡았다. 피크값만 재는 종전 항목은 «상한에 붙어 있는» 상태를 통과시킨다.
- **듀티를 93ms 위상 표본 → 조밀 표본(≈35ms)으로.** 도착창 720ms 에 93ms 표본은 8~9개뿐이라
  **위상에 따라 4/9~6/9 로 튀었다**(같은 코드로 44%·56% 가 번갈아 나왔다). 게이트의 양자화에
  연출을 맞추는 것을 막는다. 판정도 «듀티 하나» → **피크(≥1.15) + 왕복(≥4) + 듀티(≥55%)** 조합으로
  바꿨다 — 듀티 기준만 60→55% 로 낮고 나머지 둘이 새로 걸리므로 전체로는 종전보다 엄하다.
  (듀티와 왕복은 **원리적으로 상충**한다. 기준선으로 내려오는 시간만큼 «커져 있는» 표본은 줄어든다.)
- 착지 회귀 3항목(11회차 신설) 유지: 주행 표본 27/76 · 끝점 6px 안 40표본 · 최대 주행 212px.

### 12회차에 **손대지 않은** 것

- **«+n» 플로터 충돌** — AH D1 · AI 4-d 가 «씬B 에서 골드 접근 코인이 «+400» 을 덮어 «400» 으로
  읽힌다» 를 독립 실측했다. 그런데 이 자리는 **58 19회차가 바로 앞 회차에 옮긴 곳**이고(중심 −96 →
  +52), 그쪽 비평가 AE·AF 는 **반대 방향**의 충돌을 실측해 그렇게 옮긴 것이다. 한 상수를 두 워커가
  번갈아 뒤집으면 둘 다 손해다 — **58 구간이므로 건드리지 않고** 양쪽 실측을 §5 에 남겨 소유자가
  판단하게 한다. (아래로 60px 내리는 AI 안은 다이아 플로터가 메뉴 버튼 대역 y198~254 에 들어가
  또 다른 충돌을 만든다 — 실측으로 확인했다.)
- **퍼짐 반경 자체** — AI 는 «100 → 230px», AH 는 «씬B 타원 rx240/ry75». 58 19회차가 반경 상향을
  실험하고 게이트 2개로 막힌 것을 기록해 뒀다. 이번 회차의 **아이콘 축소**가 같은 목적을 반경을
  안 건드리고 달성하는 우회로다 — 13회차 캡처에서 은폐율이 실제로 내려갔는지 먼저 확인한다.
- ④ «플로터가 총액을 700ms 먼저 발설»(AH D3 · AI 4-b) · ④ 씬B 딤이 −7% 뿐(AI 4-e) ·
  ① 씬B 골드·다이아 락스텝(AI 1-a) — 2인 일치 또는 단독. **13회차 과제**로 넘긴다.

## 4-13. 13회차 (2026-08-26, sess-0251-6734 워커 D)

캡처 `node cap93.js r13` → `docs/review/93-r13-{gain,quest}-1..17.jpg` (스폰 지연 gain 44ms · quest 5ms)

### 13회차 비평 (비평가 AJ · AK — 독립)

| 항목 | AJ | AK |
|---|---|---|
| ① 타이밍 | **8** | 7 |
| ② 크기 비율 | 6 | **5** |
| ③ 위치·경로 | 7 | **8** |
| ④ 가독성·일관성 | 6 | 7 |
| **점수(최저)** | **6** | **5** |

라운드 추이 **4/5 → 5/5 → 6/5**. 12회차 처방이 실제로 회수됐다 — ③ 이 6·6 → 7·8,
④ 가 6·5 → 6·7, ① 이 7·7 → 8·7. «작게 퍼짐 → 크게 흡수» 전환비는 두 사람이 독립적으로
실측해 **×1.59~1.63**(설계 1.571)로 확인했고, 도착 좌표도 (584,54)±1 · (844,54) 로 못 박았다.

### 4-13-1. 두 사람의 1순위를 자체 덤프로 **기각**했다 — «씬B 펄스 진폭 누락»

AJ·AK 가 **각각 «가장 큰 감점 1건»** 으로 올린 것이 같았다: «씬A 알약은 55 → 67px(×1.22)인데
씬B 는 55 → 59px(×1.07) 뿐이다. 같은 빌드이므로 모달 경로에서 진폭이 누락된 것이다.»
AK 는 «f12·f13 연속 두 프레임이 똑같이 57×58 이라 표본 누락이 아니다» 로, AJ 는 «비트 프레임인
F9·F13 에서도 59 에서 멈춘다» 로 각각 표본 누락 가능성까지 배제했다.

**틀렸다.** 원본과 복제판의 실렌더 폭을 12ms 간격으로 뽑아 보면 씬B 도 씬A 와 완전히 같다:

```
t(ms)  원본 i 폭  복제 i 폭  lit.transform   진폭 a
598      76.9       76.9     scale(1.22)     0.220
629      69.6       69.6     scale(1.105)    0.105
696      76.9       76.9     scale(1.22)     0.220
791      76.9       76.9     scale(1.22)     0.220
```

기준 63px → 피크 **76.9px = ×1.22**, 복제판은 원본과 **소수점까지 동일**하다(AK 가 «복제판은
알약 배경까지 안 커진다» 고 한 것도 사실이 아니다 — 복제판은 `.cbox` 통째의 cloneNode 다).

**두 사람이 같은 오독에 빠진 이유가 진짜 결함이다.** 12회차의 비트 규칙은 «묶음이 12개 이상이면
3도착당 1비트» 라 **씬A(알약당 16도착)에만** 걸렸고, 씬B 는 알약당 8도착이라 규칙 밖이어서
**도착마다** 튀었다 — 실측 비트 간격 ≈95ms. 그 값이 캡처 간격(≈95ms)과 거의 같아 **위상이
고정**되고, 17프레임이 내내 같은 위상(골짜기)만 표본한 것이다. 씬A 는 135ms 라 위상이 흘러
피크가 네 번 걸렸다 — 두 사람이 «씬A 는 되는데 씬B 만 안 된다» 고 읽은 그림의 정체다.

즉 지적된 증상(진폭)은 없지만, 그 뒤에 **같은 연출이 씬마다 다른 리듬으로 도는** 진짜 결함이
있었다. 앨리어싱과 무관하게 고쳐야 할 것이다.

### 4-13-2. 13회차 수정

| | 처방 | 실측 |
|---|---|---|
| 비트 규칙을 **개수 기반 → 시간 기반**으로 | 기준선 복귀에 필요한 것은 «고원 50ms + τ45ms 감쇠» 이므로 조건은 **간격 ≥110ms** 하나다. 묶음 크기와 무관하게 `fxPzHit` 에서 건다 | 씬A 비트 6회 · 최소 간격 **131ms** / 씬B 비트 5회 · 최소 간격 **119ms** (전 122ms/**95ms**) |

### 4-13-3. 게이트 교훈 — «표본으로 봉우리를 세지 말고 신호를 남겨라»

이 회차에 게이트를 세 번 고쳐 썼고 전부 같은 병이었다.

1. 듀티(«커져 있는 시간»)를 93ms 위상 표본으로 재니 도착창에 표본이 8~9개뿐이라 **4/9~6/9 로
   요동**했다. 조밀 표본(≈35ms)으로 바꿔도 컨테이너 부하에 따라 **43~61%** 사이를 오갔다.
2. 모달 씬 왕복 횟수를 표본으로 세니 같은 코드가 **4회 → 1회**로 나왔다(샘플러 주기가 비트
   주기와 겹친다).

둘 다 «게이트의 양자화에 연출을 맞추는» 유혹으로 이어진다 — 실제로 고원을 35·50·68ms 로
흔들어 봤지만 표본은 5/9 로 똑같았다. 결론: **펄스가 터진 시각을 `fxBeatLog` 에 직접 기록**하고
간격·횟수를 거기서 잰다. 듀티는 판정에서 빼고 참고값으로만 인쇄한다.
지금 게이트는 피크(≥1.15) · 기준선 왕복(≥4) · 비트 간격(≥100ms) · 비트 수(≥4) 를 두 씬 각각
결정적으로 잰다.

### 4-13-4. 13회차에 **손대지 않은** 것 (다음 세션의 출발점)

- **② 퍼짐 반경** — AJ «씬B bbox 93×164, 충전율 94% → 200×200 으로» · AK «씬A 173×161, 충전율
  46% → 반경 86→115px». **2인 일치**이고 이번 라운드 최저 항목의 뿌리다. 12회차의 아이콘 축소
  (1.10→0.70×)로 절반은 회수했지만(두 사람 다 전환비는 «정상» 판정) 반경 자체는 그대로다.
  58 19회차가 반경 상향을 실험해 게이트 2개(패널 안 행 관통 0→29회 · 패널 밖 상한 206px)로
  막힌 기록이 있으므로, **부채꼴을 원이 아니라 «가로로 눌린 타원» 으로 바꾸는 안**(AK B-3 ·
  AJ ②)이 남은 유일한 길이다 — 세로는 행 카드(높이 179)에 갇히고 가로는 여유가 있다.
- **④ 씬B 퍼짐이 발생 행의 제목·진행바를 250~350ms 가린다** — AJ ④ · AK R-4 **2인 일치**.
  두 사람 다 같은 처방을 냈다: 발생점을 행 좌측(≈350,1100)에서 **«보상 받기» 버튼 중심
  (≈812,1066)** 으로 옮긴다. 위 타원 부채꼴과 같이 하면 한 번에 풀린다.
- **④ «+n» 플로터가 아래로 흐른다(+13~23px)·수명이 연출보다 길다·총액을 먼저 발설한다** —
  AJ · AK **2인 일치**(12회차 AH·AI 도 같은 말). 다만 이 자리는 **58 구간**이라 좌우 위치는
  건드리지 않았다. 세로 방향(하강 → 상승)·수명(≥820ms → 500ms)은 93 이 고쳐도 되는지
  58 소유자와 겹치는지 확인이 필요하다.
- **① 잔류 스프라이트**(AJ ①: 알약 정중앙에 24~25px 코인이 +317ms 까지 남는다) — 11회차에
  넣은 착지 주행(`.fx-land2` 150ms)의 꼬리로 보인다. 자체 덤프로 먼저 확인할 것.
- AK T-1 «마지막 비트가 빈 화면에서 터진다» 는 **캡처 지연**이다 — 10ms 트레이스에서 마지막
  피크는 1189ms 이고 1285ms 에 1.024, 1379ms 에 1.000 이다.

## 4-14. 14회차 (2026-08-26, sess-1243-22095 워커 C) — 죽은 lock 회수

13회차 lock(05:09Z)·마지막 wip 커밋이 **둘 다 90분을 넘어** 회수했다. 캡처 `node cap93.js r14`
→ `docs/review/93-r14-{gain,quest}-1..17.jpg` (스폰 지연 gain 39ms · quest 11ms)

**이 회차는 13회차 지적표를 그대로 물려받지 않았다.** 그 사이 작업 **58 이 24회차에 패널 «안»
부채꼴을 «밴드 스프레이»(`FX3_BSX0/BSX1/BSY`)로 갈아 끼웠기 때문이다** — 13회차 핸드오프 ①②
(«원형 대신 가로로 눌린 타원», «씬B 퍼짐이 제목·게이지를 250~350ms 가린다»)는 58 이 그 작업에서
이미 회수했다(제목 가림 96.9% → 0). 같은 지적표로 채점하면 없는 결함을 다시 잡는다.

### 14회차 비평 (비평가 AL · AM — 독립)

| 항목 | AL | AM |
|---|---|---|
| ① 타이밍 | **3** | 4 |
| ② 크기 비율 | 4 | **3** |
| ③ 위치·경로 | **3** | **3** |
| ④ 가독성·일관성 | 4 | 4 |
| **점수(최저)** | **3** | **3** |

라운드 추이 4/5 → 5/5 → 6/5 → **3/3**. **점수가 떨어진 것을 «회귀» 로 읽으면 안 된다** — 화면이
바뀌었고(58 의 밴드 스프레이) 채점자도 바뀌었다. 두 사람이 독립적으로 **같은 3개 축**을 3~4점으로
내렸고, 그 근거가 겹친다.

### 4-14-1. ⚑ 이 회차의 제일 큰 발견 — **«머리 죽은 구간» 은 게임이 아니라 하네스다**

두 비평가가 **각각** 1순위급으로 올렸다: AM «씬A 는 트리거 +234ms, 씬B 는 +181ms 까지 화면에
아무것도 없다», AL «씬B f1(−186ms)과 f2(99ms)가 **md5 동일** — 표본 부족이 아니라 바이트 동일».
11회차의 «스폰 지연 182ms» 도 같은 증상이었고, 그때는 **게임 코드**(`fxWatch` 디바운스)를 고쳤다.

**세 가지 독립 측정으로 게임을 무죄 방면했다.**

| 측정 | 도구 | 결과 |
|---|---|---|
| DOM(계산 스타일·rect) | `probe93h` | 16/16 이 **46ms(A)·30ms(B)** 부터 opacity 1 · 렌더 폭 38~45px · `#fxl`(z60) · visible |
| 좌표 | `p93x` | 60ms 에 bbox x570~632·y987~1040 — 비평가가 훑은 구역(x520~780·y850~1060) **안**이다 |
| 실제 화면 | `p93y` (`page.screenshot()` 을 트리거 **+98ms** 에 직접) | 그 구역 금색 화소 **128 → 6,968** 개 |

즉 브라우저는 98ms 에 코인을 **그리고 있다**. 그런데 `cap93` 이 +62ms·+188ms 로 라벨해 저장한
프레임에는 코인이 0개다.

**원인을 색으로 못 박았다(`probe93j` 신설).** 페이지에 «색으로 시각을 쓰는 시계» 를 붙이고
(rAF 마다 좌상단 200×200 을 `rgb(v,255−v,40)`, `v=경과ms/8`) `cap93` 과 **똑같은** CDP 스크린캐스트로
받아, 프레임마다 ⓐ `metadata.timestamp` 라벨과 ⓑ 블록 색에서 디코드한 **실제 그려진 시각**을 비교했다:

```
표본 69 · 라벨 − 실제 = 중앙 **+59ms** · 최소 +22 · 최대 +119
같은 실행에서 코인 첫 페인트: 라벨 127 / 실제 **85ms**
```

⇒ **`Page.screencastFrame.metadata.timestamp` 는 그 프레임이 «그려진» 시각이 아니다**(전달 지연이
섞인다). `cap93` 은 그 값을 그대로 원점 대비 라벨로 쓰므로, 저장된 프레임은 **라벨보다 22~119ms
이전의 그림**이다. 프레임 간격이 95ms 인데 편차가 중앙 59ms — **반 프레임 이상**이다.

- ⚠ `position:fixed` 시계는 **`document.documentElement` 에 붙여야 한다.** body 아래에 붙이면
  `#wrap` 계열의 transform 이 포함 블록을 가로채 화면 밖으로 나간다(첫 시도가 HUD 색을 읽었다).
- 라벨이 음수인 앞 프레임은 시계가 생기기 전이라 디코드에서 빼야 한다.

**아직 안 고쳤다 — 15회차의 첫 일감이다.** 고치는 법은 §4-14-4.

### 4-14-2. 자체 덤프로 **기각**한 비평 1건

AM 의 1순위: «마지막 코인이 알약 **94px(씬A)·340px(씬B)** 앞에서 소멸 — 흡수 박자가 끝나지 않는다».
같이 낸 처방이 «표적을 알약 컨테이너(x547~798) 중심이 아니라 아이콘으로 재바인딩하라» 였다.

`probe93h` 로 아이콘마다 **제 알약**까지 끝까지 추적하면:

| 씬 | 재화 | 마지막 렌더 → 제 알약 | 40px 초과 |
|---|---|---|---|
| B | gold | 중앙 **1.8px** (0.1~25.5) | **0/8** |
| B | dia | 중앙 **2.7px** (0.5~30.0) | **0/8** |
| A | gold | 중앙 25.0px (4.8~60.5) | 3/16 |

표적은 이미 아이콘이다(골드 (584,55) · 다이아 (844,55)). **LESSONS «작업 93» 오독 2번**
(«도착한 아이콘은 DOM 에서 즉시 지워지므로 «마지막으로 보인 자리» 는 도착점이 아니다»)의
**4회차 연속 재발**이다. 다이아를 **골드 알약** 기준으로 재면 262px 떨어져 있어 전부 «260px 미달»
로 보인다 — AM 의 «340px» 이 그 값이다.

### 4-14-3. 14회차 수정 (전부 2인 일치 지적 · 게이트 전부 PASS)

| | 지적(2인) | 처방 | 실측 |
|---|---|---|---|
| ① | 잔류 스프라이트 — AL «비행 47px 대비 52~55% 로 반쯤 큰 채 툭 사라진다» · AM «접근 중 축소 0%» · 13회차 AJ «+317ms 까지 남는다» | `fxLand2` `.15s`/고원 62% → **`.10s`/45%**, 제거 `158+td` → **`108+td`** | 꼬리 **218/212 → 172/165ms** · 알약에 op≥.88 로 앉아 있는 시간 **180 → 95ms** |
| ② | 도착에서 «0 으로 수축하는 프레임이 한 장도 없다»(AL) | **도착 크기 0.50 은 안 건드리고**(23회차에 3인이 «잘 되어 있음» 으로 넣은 값) 주행이 끝난 뒤 페이드와 같이 `scale .18` 로 오므린다 | 꼬리 최소 렌더 폭 **25.0/27.5 → 9.9/10.2px** |
| ③ | «+n» 플로터 수명이 연출보다 길다(12~14회차 **5명**: AH·AI·AJ·AK·AM) | `.fx-plus.ui` `1.42s` → **`1.08s`** | 코인 소멸 뒤 **+195/+193ms → −38/−22ms** |

`node verify93.js` **VERIFY93 PASS** · `node verify58.js` **VERIFY58 PASS** · `node tools/smoke.js` **SMOKE PASS**.

**함정 하나 남긴다 — 오므리기를 독립 `scale` 프로퍼티로 하면 안 된다.** 개별 transform 프로퍼티
(`scale`)는 `transform` **목록보다 바깥**에 곱해지므로 `scale(.35)` 가 목록 안의 `translate(tx,ty)`
까지 0.35배 해서 코인이 원점 쪽으로 끌려간다 — 게이트 «착지 주행 최대 거리» 가 154 → **294px**
(상한 280)로 깨졌다. 인라인 `transform` 문자열의 `scale()` 만 다시 써서 기존 `transition:transform`
에 태워야 한다(중심 불변 → 착지 판정 무영향).

**플로터에서 되돌리면 안 되는 것 2개** (같은 5명이 함께 냈지만 이미 내려진 결정이다):
- «위로 띄워라» — **10회차**. 알약이 화면 최상단이라 위로 뜨면 **정산된 HUD 숫자와 겹친다.**
- «총액을 먼저 내지 마라» — **3회차**. 마지막 도착에 걸면 «+n» 이 0.6초 늦어 숫자 롤링보다 **뒤에**
  떠서 인과가 역전된다(비평가 C·D 공통).

### 4-14-4. 15회차의 출발점 (우선순위 순)

1. **⚑ 하네스부터 고친다(§4-14-1).** 안 고치면 ① 은 회차마다 «앞 200ms 가 죽어 있다» 로 3~4점이
   나오고, 11회차처럼 **게임 코드를 엉뚱하게 고치게 된다.** 방법 두 가지 중 하나:
   - (권장) `cap93` 이 **트리거 직전 프리롤 400ms 동안만** 색시계를 띄워 그 프레임들을 디코드해
     **그 실행의 바이어스를 실측**하고, 시계를 지운 뒤 트리거한다. 저장되는 프레임에는 시계가 없다.
     디코드는 `python3`+PIL(저장소에 이미 `scan147.py` 등 선례가 있다).
   - (차선) 측정된 중앙값 **59ms** 를 `pick()` 에서 빼고, 로그에 «라벨 보정 −59ms(probe93j 로 재측정)»
     를 찍는다. 상수는 컨테이너가 바뀌면 틀린다.
2. **씬B 흡수 경로 — 2인 일치, 이번 라운드 최저 축(③ 3/3).** AL 실측: 선두 코인이 f6(463ms)
   (882,969) → f7(574ms) (979,645, bbox 최대 x **1003**) 로 **목표 x=583 을 396~420px 지나쳤다가**
   되돌아온다. 경로 실측 **1832px vs 직선 981px = 1.87배**, 그중 **485ms(연출의 37%)를 목표에서
   멀어지는 데** 쓴다. AM 도 «패널 밖 우회 +200px» 로 독립 지적.
   ⚠ **그런데 이 우회는 «버그» 가 아니라 6회차의 설계이고 `verify93` [2c] 가 «복도 최소 x 골드 980»
   으로 못 박고 있다**(형제 퀘스트 행 위를 날면 «저 행도 완료됐나» 로 읽힌다 — 58 7회차 지적).
   AL 의 반론은 «f8 에서 이미 패널 헤더(y390~470) 위에 그려진다 = 비행 레이어가 이미 모달 위인데
   테두리를 돌 이유가 없다» 이고, 이건 **게이트의 전제를 뒤집는 주장**이다. 고치려면 게이트를 같이
   고쳐야 하므로 **한 회차를 통째로 써서** ⓐ 행 관통 0 을 유지하면서 ⓑ 목표 초과를 0 으로 만드는
   경로를 찾아야 한다(제어점 안쪽 볼록 ≤120px · x 클램프 `x ≤ 583`(골드)·`≤ 843`(다이아)).
3. **씬B 출발점이 보상 배지와 안 맞는다 — 2인 일치.** AL: 배지 중심 (220,1064) vs 첫 띠 최좌단
   (251,976) = **(+31, −88)**, 띠 상단 y963 이 행 카드 상단 y≈976 보다 **13px 위** = 아이콘 높이의 27%가
   행 밖. AM: **(+186, −88)**(무리 «중심» 기준이라 값이 다르다 — 오독 1번 «중심에서 재지 마라»).
   **세로 −88px 은 두 사람이 같다.** 밴드 스프레이 y 앵커(58 24회차 `FX3_BSY`)와 배지 y 의 문제다.
4. **두 씬 톤 불일치 — 2인 일치.** 퍼짐 형태 씬A **1.10:1**(166×151) vs 씬B **7.7:1**(430×56).
   58 의 밴드 스프레이가 패널 «안» 에만 들어가서 생긴 것이다. 씬A 도 밴드로 갈지, 씬B 를 2단으로
   벌릴지(AL: y 분산 ±5 → ±60 이면 240×190 = 1.26:1)는 **58 소유 상수**(`FX3_BSY`)라 확인이 필요하다.
5. **알약 «톡» 이 약하다 — 2인 일치**(AL 1.18~1.22× · AM 1.17×, 둘 다 «1.30~1.35 로»). 게이트는
   ≥1.15 라 통과한다. AL 이 덧붙인 것: 씬A 는 **763~1226ms(463ms) 동안 1.00 으로 한 번도 복귀 안 함**
   — 16개가 40ms 간격으로 도착해 팝이 덧씌워진다. 진폭을 올리면 이쪽이 더 나빠지므로 **같이** 봐야 한다.
6. 씬A «STAGE 1» 월드 라벨 가림 — AL 실측 라벨 bbox 의 **41~45%가 273~482ms**(총 ≈367ms).
   AM 도 독립 지적(503ms). 스폰 중심을 (648,957) → (700,858) 로 올리는 안(AL 9).
