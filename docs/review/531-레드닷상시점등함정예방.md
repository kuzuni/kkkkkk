# 531 — 레드닷 «상시 점등» 함정의 재료를 미리 덮는다 (잠복·예방)

> 등재(2026-08-31, sess-1859-3461 워커 D — 519 ⑤ 전수 스윕 곁다리 관측):
> «`#X … s{display:…}` 로 `<s>` 를 통째로 켜는 32 스코프 중 «같은 급 짝» 이 있는 것은 8개뿐이다.
> 나머지 23곳은 그 화면에 레드닷을 **놓는 순간** `.alert` 와 무관하게 상시 점등이 된다.»

세션 `sess-2317-22522` 워커 A · **1회차**(완료)

제품 `index.html` **2줄**(CSS 짝 한 벌) · 자 신설 2개 · 이관 4개 · 레이아웃 Δ0px · 판정·밸런스 0줄.

---

## 1회차 (2026-08-31)

### ⓪ 338 규칙 — 처방 전에 재현부터. **그리고 재현이 등재문을 두 군데 뒤집었다**

`tools/probe531.js` 신설. 등재문은 처방 ⓐ 를 «23 스코프에 짝을 미리 다 깔아 둔다
(제품 46줄 · **노드가 없어도 무해** · 가장 짧다)» 로 적었다. 그 문장에는 **검사할 수 있는 주장이 둘**
들어 있다 — ① 자리가 23곳이다 ② 무해하다. 둘 다 찍어 봤고 **둘 다 틀렸다.**

#### ① 자리 목록이 틀렸다 — «셀렉터 모양» 으로는 함정을 못 센다

등재문의 근거인 `scan519 [2]` 는 `#X … (s|i|b|em|u)` 라는 **모양**으로 스코프를 고르는데,
콤마 그룹의 **마지막 항**만 본다. 그래서 두 쪽으로 틀린다:

| | 자리 | 왜 |
|---|---|---|
| **허수 12** | `#stinfo` `#bossTm` `#dunTm` `#arnTm` `#hpwrap` `#tuto` `#ldBar` `#dunw` `#eqw` `#upw` `#statw` `#defw` | `#eqw i,#eqw em,#eqw b,#eqw u` 처럼 **`<s>` 를 안 켜는** 스코프인데 마지막 항(`u`)에 걸렸다 |
| **누락 12** | `#bSk` `#bPet` `#bCos` `#shopw` `#relw` `#trw` `#wpnw` `#mnw` `#blsw` `#collw` `#psw` `#stDesc` | 짝이 **자리별로만** 있어서(`#mnw .mn-b>.bdg` · `#psw .ps-bx.alert>s.updot`) «짝 있음» 으로 세어지지만, 그 화면의 **다른 자리**에 닷을 놓으면 그대로 켜진다 |

⚑ **모양 23 ↔ 실측 23 — 개수만 우연히 같고 양쪽으로 12자리씩 다르다.** 등재문 목록을 그대로
믿었으면 허수 12곳에 죽은 CSS 를 깔고, 진짜 함정 12곳은 그대로 열어 둔 채 «완료» 로 닫았을 것이다.

⇒ 자를 **모양이 아니라 결과**로 바꿨다. `probe531 [1]` 은 `#app` 안 id 요소마다 **진짜 닷**
(`<i><s class="updot"></s></i>`)을 심어 보고 `display` 를 읽는다. 심는 자리가 곧 «다음 워커가
닷을 놓을 자리» 이므로 이것이 함정의 정의 그대로다.

**자 자신의 함정 셋을 잡느라 네 번 고쳐 돌렸다**(전부 자 쪽 · 제품 아님):
1. **맨몸으로 심으면 저장소 전체가 빨개진다** — 전역 기본값 `display:none` 을 가진 것은 `.updot`
   하나뿐이고(11081), `.bdg`·`s.dot` 의 «꺼짐» 은 구조 규칙(`.stab>.bdg` · `.cltab>s.dot`)이 준다.
   맨몸 `.bdg` 는 어디에 놓든 `<s>` 기본값 `inline` 이라 root 34 중 20 이 허수였다 ⇒ **실제로 쓰이는
   host 꼴**(`.stab` · `.cltab`)에 담아 심는다.
2. **조상의 살아 있는 축을 먼저 떼야 한다** — `#menub` 은 부팅 때 «안 읽은 우편» 으로 `.alert` 가
   켜져 있어서 `#menub.alert .bdg{display:block}`(**제 몫을 하는 정상 규칙**)이 심은 닷까지 켰다
   ⇒ 함정으로 오독. `scan519 [1]` 과 같은 규율로 조상 축을 떼고 잰다.
3. **«켜짐» 은 축이 두 자리에 온다** — 상자에 오는 꼴(`.alert>.updot`)과 **화면 자신**에 오는 꼴
   (`#menub.alert .bdg`)이 둘 다 있다. 상자만 보면 `#menub` 이 이번엔 «`.alert` 를 붙여도 안 켜진다»
   로 빨개진다(같은 자리가 두 회차 연속 서로 **반대 이유**로 허수였다).
4. **자식을 못 받는 요소는 건너뛴다** — `#chIn` 은 `<input>` 이라 `appendChild` 는 통해도 그 자식이
   렌더 트리에 안 들어가 `getComputedStyle` 이 늘 `none` 을 준다.

#### ② «무해» 가 아니었다 — 살아 있는 레드닷 2개가 꺼진다

`probe531 [2]` 는 오프너 34개를 돌며 **화면을 열어야 태어나는** 닷까지 모은다(`scan519` 는 부팅 시점
CSSOM 만 봐서 이것을 한 개도 못 본다 — 살아 있는 닷 98개 중 부팅 시점은 22개뿐이다).
`[3]` 이 그 목록을 짝 주입 전/후로 대조한다.

등재문 목록(`#dunw` 포함)에 짝을 깔면 **03 던전 카드 «입장 가능» 닷 2장이 꺼진다.**
341 이 놓은 그 닷(`.dnc .dot`)은 **`display` 선언이 아예 없고** «노드를 만들거나 말거나» 로 켜는
자리라, 짝의 `display:none` 이 그것을 덮는다. ⇒ **`#dunw` 는 목록에서 뺐다**(애초에 함정도 아니다 —
`#dunw i,#dunw em,#dunw b` 에 `s` 가 없다).

### ① 처방 — 제품 2줄

```css
:is(#bSk,#bPet,#bCos,#shopw,#relw,#trw,#wpnw,#prbw,#mnw,#ciw,#blsw,#collw,#psw,#pfw,#sumw,
    #stDesc,#dclw,#specw,#bagw,#cfw,#rkw,#chw,#svw) :is(s.updot,s.bdg,s.dot){display:none}
:is(<같은 23>) .alert>:is(s.updot,s.bdg,s.dot){display:block}
```

특이성 (1,1,1) / (1,2,1) — ID 급 타이포 리셋 `#X … s`(1,0,1) 을 이기고, 자리별 점등 규칙
(`.stab.alert>.bdg` 1,3,0 · `#psw .ps-bx.alert>s.updot` 1,3,1 …)에는 진다. **그래서 기존 자리는
한 값도 안 바뀐다**(probe531 [3]: 꺼진 닷 0 · 새로 켜진 닷 0).

⚠ **`<s>` 꼴로만 적는다.** `.bdg` 는 레드닷 배지 말고 **10 상점 배너 리본**(`<div class="bdg">`)에도
쓰이는 이름이다 — 클래스 꼴로 적었더니 그 리본 **3장이 같이 꺼졌다**(`[3]` 1차 실행).
살아 있는 «닷» 98개의 태그는 `<s>` 61 · `<i>` 22 · `<span>` 12 · `<div>` 3 으로, 레드닷은 예외 없이 `<s>` 다.

⚠ **`.on` 을 점등 축에 넣지 마라.** `:is(.alert,.on)>…` 로 적었더니 «지금 고른 서브탭» 배지가 켜졌다.
`.stab.on` 은 «선택» 이지 «알림» 이 아니다.

### ② 게이트 — `tools/verify531.js` **18/18 신설**

[A] 정적 6항(짝의 두 줄 · 23자리 · `<s>` 꼴 · `.on` 아님 · `#dunw` 제외) ·
[B] **심어서 잰 함정 0자리** 3항(본체·래칫 · `scan519 [1]` 을 게이트로 승격) ·
[C] 무르게 안 풀었다 3항(341 던전 닷 2/2 보임 · 상점 리본 3/3 보임 · 고른 탭 0자리) ·
[X] 콘솔 · **[R] 되돌림 4항**.

⚑ **되돌림을 넷으로 가른 것이 이 자의 값이다** — [R] 하나로는 «어차피 아무 일도 안 일어났을 것» 과
구별이 안 된다.

| 항 | 무엇을 되돌리나 | 무엇을 못박나 |
|---|---|---|
| [R1] | 짝 두 줄을 CSSOM 에서 삭제 | 함정 **23자리 재발** = 이 짝이 실제로 일한다 |
| [R2] | 짝을 **클래스 꼴**로 다시 씀 | 상점 배너 리본 **0/3 만 보임** = `<s>` 꼴이 왜 필요한지 |
| [R3] | `.on` 을 점등 축에 넣음 | 고른 탭에 심은 닷 **151자리 점등** = `.alert` 만 축인 이유 |
| [R4] | **등재문 처방 ⓐ 를 적힌 그대로**(`#dunw` 포함) | 341 던전 닷 **0/2 만 보임** = 목록에서 뺀 이유 |

⚠ [R3] 은 **특이성 한 칸**에 걸린다 — `:is(.alert,.on)>…`(1,2,1) 이라야 `#trw .stab>.bdg`(1,2,0) 를
이긴다. `:is(.on)>…`(1,1,1) 로 적으면 아무 일도 안 일어나 이 되돌림이 헛초록이 된다(1차 실행에서 잡았다).

### ③ 이관 — **앞선 넷의 음성항이 동시에 빨개졌고, 그게 옳은 신호였다**

`verify283`(41→39) · `verify325`(69→68) · `verify516`(32→31) · `verify519`(31→30) 가 같이 빨개졌다.
넷 다 **같은 항**이다 — «화면별 짝을 걷으면 상시 점등으로 되돌아간다». 531 이 그 뒤를 한 칸 높은
특이성으로 받치고 있으니, 화면별 짝만 걷어서는 이제 안 켜진다.

**제품 동작은 한 값도 안 바뀌었다**(닷은 전부 종전대로 켜지고 꺼진다). 빨간 것은 **되돌림의 사거리**다.
그 항들의 뜻(«이 두 줄이 실제로 일한다»)은 여전히 옳으므로, 되돌릴 때 **531 의 짝도 같이** 걷도록 이관했다:

* `tools/dot531.js` **신설** — 걷는 식을 **한 곳**에 둔다(`CSSOM_RE` · `SRC_RE` · `STRIP_JS`).
  넷이 각자 적으면 531 의 셀렉터가 바뀌는 날 **넷이 조용히 헛초록**이 된다(그때 «걷을 규칙 0줄» 은
  예외가 아니라 그냥 0이다). 553 이 `dpsk.js` 로 «선언 한 곳» 을 세운 것과 같은 길.
* `verify519` [R-a] 2줄 → **4줄**(519 짝 2 + 531 짝 2) · `verify516` R1 같은 꼴 ·
  `verify283` 은 소스 사본을 만드는 자라 `SRC_RE` 로 두 줄을 빼고 **[N0] 전제항을 신설**해
  «사본에서 실제로 뺐다» 를 먼저 못박았다(41 → **42**) ·
  `verify325` 는 주입 셀렉터를 `#blsw .updot`(1,1,0) → `#blsw .updot,#blsw s.updot`(1,1,1) 로 올렸다.

**결과**: `verify283` **42/42** · `verify325` 69/69 · `verify516` 32/32 · `verify519` 31/31.

### ④ 결과

`verify531` **18/18 신설** · `probe531` 신설 · **SMOKE PASS** ·
이관 4종(`verify283` 42/42 · `verify325` 69/69 · `verify516` 32/32 · `verify519` 31/31) ·
회귀 무수정 6종 — `verify166` 73/73 · `verify210` 101/101 · `verify299` 27/27 · `verify322` 48/48 ·
`verify364` 18/18 · `verify471` 33/33.

⚠ **곁다리 등재 559** — `verify341` [D](던전 카드 닷 세로 자리 봉투)가 **31/32** 로 빨갛다.
338·344·369·372 규칙대로 **수리 전 커밋에서도 같은 항이 같은 수치**(`+-2.5..+24.5`)로 빨간지
직접 대조해 확인했다 — 내 변경과 무관하다(`#dunw` 는 짝 목록에 없고, `verify531` [C1] 이 그 닷
2/2 가 그대로 보임을 단언한다). 상세는 PROGRESS 559 행.

### ⑤ 남은 것 — 이 행에서는 없다

1. **화면별 짝은 그대로 뒀다.** 531 이 덮긴 하지만 더 좁고, 531 의 목록이 바뀌는 날 마지막 안전망이
   된다. 지우는 것은 이 작업 단위 밖이고 diff 만 넓힌다.
2. **`scan519 [2]` 는 안 고쳤다** — 그 자는 519 의 기록이고, 이 행이 «모양으로 세면 틀린다» 를
   `probe531`·`verify531` 로 대체했다. 다음에 그 절을 근거로 쓰려는 세션은 **이 파일 §⓪ ① 을 먼저 볼 것.**
3. 새 화면을 만들면 그 id 를 짝 목록에 더한다 — 안 더하면 `verify531` [B2] 래칫이 그 순간 빨개진다.
