# 640 — `verify580` §4 터널링 1건 실패(38/39) · dt 상한 프레임의 «놓친 접촉»

> 선점 `sess-1144-14134`(루틴 워커 C) · 2026-09-01 · 1회차
> 등재 `sess-1028-23195`(워커 B · 게이트 스윕 곁다리 · 등재만)

## 0. 한 줄

등재문이 열어 둔 갈래 둘(ⓐ 제품 터널링 잔존 / ⓑ 자의 헛초록)은 **둘 다 참**이었다.
제품은 접촉 판정이 **프레임을 안 따라가고**(`d` 가 개체가 움직이기 전 자리로 잰 거리다),
자는 그 결과로 생기는 **드문 사건을 `== 0` 으로 물어** 실행마다 뒤집혔다.
제품 `index.html` **3자리** · `verify580` §4 판정 갈아 끼움 + **§R3 신설** · 속도 계수 **0줄**.

## 1. 재현 (338 규칙 — 처방 전에 재현)

`tools/probe640.js` 신설. 세는 법은 `probe580` [3]·`verify580` §4 와 **같은 상대 운동 선분**이고
(새 자를 만들지 않았다), 다른 것은 둘이다: **시드 고정**(mulberry32 — 판을 다시 굴릴 수 있게)과
**멈추는 조건**(프레임 수가 아니라 «임계 안» 프레임 수 = 노출량).

### [1] 플레이키 재현 — 같은 자·같은 표본 크기에서 계수가 뒤집힌다

dt 0.1 · 600프레임(= `verify580` §4 lag 갈래와 **같은 표본**):

| 시드 | 표본(적·프레임) | «임계 안» | 통과(준비됨) |
|---|---|---|---|
| 1000 | 29,674 | 127 | **0건** |
| 8717 | 29,656 | 137 | **1건** |

⇒ 등재문의 «실행마다 다른 항이 빨개진다» 가 **재현됐고 이제 결정적**이다(시드로 다시 굴릴 수 있다).
⚑ **부하 인공물은 여기서 완전히 죽는다** — 하네스가 가상 시계(`__v640tick`)라 CPU 경합이 이 숫자를
못 움직인다. 등재문이 «단독 실행이었다» 로 배제한 것을 구조로 배제한다.

### [2] ⚠ 내 첫 모형이 틀렸다 — 흔드는 것은 «노출량» 이 아니다

등재문의 처방(«노출량을 고정해 ⓐⓑ 를 가르라»)을 그대로 따라 **노출량을 4.4~4.7배로 키웠다.**
내가 건 판정은 «[1] 에서 0건이던 시드가 노출량만 키우면 계수가 선다»(within-seed 증명)였고 —
**빨갰다**:

| 시드 | 노출 127~137 | 노출 600~602 (4.4~4.7배) |
|---|---|---|
| 1000 | 0건 | **여전히 0건** (3,213프레임 · 표본 160,324) |
| 8717 | 1건 | **여전히 1건** (3,926프레임 · 표본 195,956) |

⇒ 통과는 **노출량에 비례하는 상수율 과정이 아니다.** 보는 시간을 4.7배로 늘려도 계수가 안 는다 =
사건은 **드물고 «배치» 에 묶여** 있다. 등재문이 본 4배 노출 편차(353 vs 89)는 시드 미고정 실행끼리의
것이고, 시드를 고정하면 노출량은 오히려 1.08배로 잘 붙는다(127 vs 137).
**ⓑ 는 그대로 서고 뿌리 설명만 바뀐다** — `verify580` §4 가 뒤집히는 이유는 «노출량이 갈려서» 가
아니라 **«사건이 드물어서»** 다(수백 노출프레임당 1건 ⇒ 600프레임 표본은 대개 0, 가끔 1~3).

⚑ **이 절이 이 작업에서 제일 값진 자리다.** 등재문의 가설을 그대로 믿고 «노출량 고정» 을 게이트로
굳혔으면, 노출량은 **원래 잘 붙는 축**이라 아무것도 안 잡는 항이 하나 늘었을 것이다(338·341 전례).
자기 판정이 빨개진 것을 «자를 무르게» 풀지 않고 **모형을 버리는 쪽**으로 읽은 것이 답이었다.

### [3] 정상 프레임 대조 — 같은 노출량에서 0건

| 시드 | dt | 프레임 | 표본 | «임계 안» | 통과(준비됨) |
|---|---|---|---|---|---|
| 1000 | 1/60 | 6,090 | 302,667 | 603 | **0건** |
| 8717 | 1/60 | 1,658 | 80,950 | 600 | **0건** |

⇒ 터널링은 **dt 축의 것**이다. 이 표가 아래 수리의 «정상 프레임 Δ0» 을 보증한다.

### [4]·[5] 기하와 산술 — 스침 1건, 그러나 상한은 얕지 않다

* 잡은 통과 1건: 상대 이동 **27.32px** · 침투 깊이(reach − md) **0.39px** — 프레임 크기의 실제
  이동이다(재배치·리스폰 점프가 아니다 = **측정 인공물 기각**).
* 플레이어 230 px/s · 잡몹 천장 195.5 px/s ⇒ 상대 속도 상한 **425.5 px/s**
  · 프레임당 상대 이동 상한 **dt 0.1 → 42.6px** vs **dt 1/60 → 7.09px**, 임계 reach **45.6~55.2px**.
  (7.09 는 임계의 1/6 — LESSONS 580-① 이 세운 «1/3 아래면 통과 0건» 눈금 안이다.)
* ⚑ **표본 전체 최대 상대 이동은 82.8px(돌진)** 이고, 두 끝이 다 밖인 채 파고들 수 있는 깊이는
  `R − √(R² − (L/2)²)` = **26.5px**(R 45.6 · L 82.8). ⇒ 잡힌 표본은 0.39px 스침이지만
  **돌진이 걸리면 얕지 않다.** 수리를 «있어도 그만» 으로 볼 수 없는 근거가 이 산수다.

## 2. 뿌리 — 접촉 판정이 프레임을 안 따라간다

`index.html` 적 루프는 머리에서 `const d = Math.hypot(dx, dy)` 를 잡고(개체가 **움직이기 전**),
한참 아래에서 개체를 움직인 뒤(`e.x += …`), **그 낡은 `d`** 로 접촉을 판정한다.
정상 프레임에서는 한 프레임 상대 이동이 7.09px 라 그 낡음이 안 드러나지만, `loop` 는 dt 를
**0.1(10fps)로 클램프**하므로 42.6px(걸음)~82.8px(돌진)이 되어 **임계 원을 두 끝점이 다 밖인 채로
스쳐 지나갈 수 있다.** = 등재문이 물은 «접촉 판정이 상한 프레임을 따라가는가» 의 답은 **아니오**였다.

## 3. 처방 (제품 3자리)

1. `step()` 머리 — `player.px0/py0` = **이 프레임이 시작될 때의 플레이어 자리**.
   (적 루프가 도는 시점의 `player.x` 는 이미 이번 프레임 이동이 끝난 자리다.)
   ⚠ `step()` 이 중간에 `return` 하는 프레임(보스 등장 국면)에는 적도 안 움직이므로 낡을 일이 없다 —
   적 루프와 **같은 호출** 안에서만 읽힌다.
2. 적 이동 직전 — `const ex0 = e.x, ey0 = e.y`.
3. 접촉 판정 — **스윕 가지** 하나를 더한다.

```
const reach = e.r + player.r + 6;
let touch = d < reach;                      /* 기존 판정 — 한 글자도 안 바꾼다 */
if(!touch){                                 /* 그것이 «구조적으로 못 보는» 경우만 줍는다 */
  두 끝(프레임 시작·끝의 상대 자리)이 다 원 밖인데 선분이 원을 지나는가
}
```

⚑ **왜 `md < reach` 로 통째로 갈지 않았나** — 그러면 원 «안에서» 끝나는 흔한 접근이 지금보다
**한 프레임 일찍** 잡혀 60fps 피격 타이밍이 통째로 움직인다(쿨다운 0.9초 위에서 회당 ±1프레임).
그 경우는 **놓치는 게 아니라 다음 프레임의 `d` 가 잡는다** — 즉 결함이 아니다.
새 가지를 «두 끝이 다 밖» 으로 좁히면 정상 프레임 발화가 **0건**이고([3]), 그래서
**60fps 체감·피격률 Δ0** 이 실측으로 보장된다. 501·502·sim 계열에 파급이 없는 이유가 이것이다.
⚠ 속도 계수는 **0줄** — 배수 확정은 199·541 몫이라는 등재문 경고 그대로다.

## 4. 게이트 — §4 를 갈아 끼우고 §R3 를 세웠다

* **판정 축 교체**: `tunReady`(기하 = «지나갔다») → **`tunMiss`(기하 + «접촉이 안 찍혔다»)**.
  옛 항은 제품이 그 프레임에 **제대로 때려도 계속 세므로**, 640 의 수리가 들어가면
  «고쳤는데 빨강» 이 된다(헛빨강). 적의 공격은 `e.cd = 0.9` 로 표가 나므로
  틱 전 `cd ≤ 0` → 틱 뒤 `cd > 0` 이면 이 프레임에 때린 것이다(`e.cd -= dt` 로는 양수가 못 된다).
* **판 고정**: §4·§R3 만 `TUN_SEED = 8717` 로 `Math.random` 을 고정한다.
  8717 은 **수리 전 통과가 실제로 찍히는 판**으로 골랐다([1]·[2]) — §R3 가 우연히 초록이 될 수 없다.
  ⚠ §2·§3·§5 에는 시드를 **안** 준다. 그 절들은 무작위 표본 위에서 «전부/서열» 을 묻는 절이라
  판을 고정하면 묻는 것이 좁아진다. 결정적이어야 하는 것은 §4 하나다.
* **§R3 신설(되돌림)**: 스윕 가지만 `if(false)` 로 죽인 사본에서 **«놓친 접촉» 이 되살아난다.**
  R2(배수 8 → 통과가 찍힌다)는 **기하**만 보이므로 640 의 수리가 통째로 사라져도 초록이다 —
  그것만 두면 «수리가 있으나 없으나 같은 게이트» 가 된다(334 가 잡은 꼴). R3 가 그 자리를 막는다.

## 5. 결과

### 5-1. `verify580` — 38/39 ↔ 39/39 (2/4 빨강) → **41/41**

| 실행 | 등재문(수리 전) | 이번(수리 후) |
|---|---|---|
| 1 | **38/39 FAIL** | **41/41 PASS** |
| 2 | 39/39 PASS | **41/41 PASS** |
| 3 | **38/39 FAIL** | **41/41 PASS** |
| 4 | 39/39 PASS | — |

항 수가 39 → 41 인 것은 §R3 두 항(되돌릴 자리 확인 + 되돌림 실측)이 늘어서다.

### 5-2. 전·후 대조는 **같은 시드 위**에서 났다 (§4 ↔ §R3)

| | 지나감(기하) | 그 중 준비됨 | **놓친 접촉** |
|---|---|---|---|
| §4 수리 후 (dt 0.1 · seed 8717) | **1건** | **1건** | **0건** |
| §R3 스윕 가지만 뗀 사본 (같은 시드) | 1건 | 1건 | **1건** |
| §4 정상 프레임 (dt 1/60) | **0건** | 0건 | 0건 |
| §R2 배수 8 사본 | 186건 | 152건 | (판정 안 함) |

⚑ **이 표의 첫 두 줄이 이 작업의 증거 전부다.** 판을 안 바꾸고 **스윕 가지 한 개만** 넣고 뺐는데
«놓친 접촉» 이 1 ↔ 0 으로 갈린다. 그리고 «지나감 1건» 은 **양쪽 다 그대로** — 즉
기하는 안 바뀌었고 **접촉을 찍느냐만** 바뀌었다.
⚑ 그 «지나감 1건» 이 곧 **옛 판정(`tunReady === 0`)이었으면 FAIL 이었을 값**이다.
제품만 고치고 게이트를 안 갈아 끼웠으면 **고쳤기 때문에 빨간** 게이트가 됐다(LESSONS 640-③).

### 5-3. 정상 프레임 Δ0 (60fps 파급 없음)

§4 fast 갈래가 **지나감 0건**(표본 178,050 적·프레임 · «임계 안» 810프레임) —
새 가지는 60fps 에서 **발화할 기회 자체가 0**이다. probe640 [3] 이 같은 것을 두 시드에서
노출량 600 으로 맞춰 다시 보였다. ⇒ 피격 타이밍·피격률·밸런스 파급 없음.
§5 체감 표도 보스 30초 **29회**로 LESSONS 580-④ 의 «전·후 동수 29회» 와 같다.

### 5-4. 회귀 · SMOKE

* `SMOKE PASS` (제품 수정 직후 · 마감 전 재확인).
* 회귀 게이트: `verify501` · `verify502` · `verify66` · `verify172` · `verify358` — 결과는 아래 표.
* `probe640` **11/12 신설** — 빨간 1항(2-c)은 **버그가 아니라 기록**이다(§1 [2] · LESSONS 640-①).
  내 첫 모형(«노출량이 흔든다»)이 틀렸다는 것을 그 항이 잡았고, 그 사실 자체가 이 행의 핵심 발견이라
  **항을 지우지 않고 남겼다**. 지우면 «등재문의 처방이 왜 안 통했는지» 가 저장소에서 사라진다.

## 6. 다음 세션에게

* ⚠ **199 에게** — §4·§R3 만 시드를 고정했으므로 **§5 체감 표가 이제 «고정된 판» 의 값**이다
  (잡몹 피격 0.183/초 · 보스 29회/30초). 재현은 되지만 무작위 draw 가 아니다. 계수 비교에 쓸 때
  **판이 같은지**를 먼저 봐라. §2·§3·§5 자체에는 시드를 안 줬다(무작위 표본 위에서 «전부/서열» 을
  묻는 절이라 판을 고정하면 묻는 것이 좁아진다).
* ⚑ **곁다리로 남는 자리 하나(등재 안 함 — 이 행의 단위가 아니다)**: 제품의 `d` 는
  **적은 이동 전 · 플레이어는 이동 후**를 섞어 잰다. 640 이 닫은 것은 «두 끝이 다 밖» 인 통과뿐이고,
  «프레임 시작엔 임계 안이었는데 `d` 로는 밖» 인 경우는 여전히 **다음 프레임**으로 밀린다.
  그건 놓침이 아니라 한 프레임 지연이라 결함으로 안 봤지만, dt 상한에서 그 지연은 **100ms** 다.
  누가 «피격이 한 박자 늦다» 를 보고하면 먼저 여기를 봐라.
