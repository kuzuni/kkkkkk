# 466 — `tools/verify338.js` §2 «던전 입장 실패» 10건 (T1 «버그(게이트 부패)»)

- 세션 `sess-1238-14836` (클라우드 루틴 워커 B) · 2026-08-30 · **1회차로 종결**
- 지시서 [3]-**(가) 기계적 작업** — 레퍼런스 대조가 아니라 «게이트의 전제가 낡았는가» 판정이라 **비평가를 안 띄웠다.**
- **제품 `index.html` 0줄** · 레이아웃 Δ0px · 판정·밸런스·세이브 0줄. 고친 것은 `tools/verify338.js` 한 파일이다.

## 1. 등재문 (PROGRESS 466, sess-1138-24523 워커 B — 457 회귀 곁다리 관측)

> `verify338` §1 이 스테이지 보스전을 **켜 둔 채** 끝내는데(`spawnStage(); killed = ENEMY_COUNT; step()`),
> 453 이후 `battleBusy()` 가 그 상태에서 던전 입장을 막는다(`startDunRun` 첫 줄 가드).
> §2 `enter()` 는 `endDunRun` 만 하고 `bossOn` 은 안 내린다 ⇒ 10종이 전부 «입장 실패».
> **제품 0줄이 예상된다** — 453 이 제품에 세운 것이 옳고(주인 지시), 낡은 것은 게이트 쪽 전제다.

수리 전 실행값: **`VERIFY338 3/13 FAIL`** (§2 의 던전 8 + 탑 2 가 전부 «입장 실패»).

## 2. 재현 — `tools/probe466.js` 신설 (338·341·350 규칙: 처방 전에 제품에게 직접 묻는다)

**등재문은 기각이 아니라 확인**이다(338·341 은 여기서 기각됐던 자리다). 수리 전·후 같은 값:

| 축 | 물은 것 | 값 |
|---|---|---|
| [1] | **중립 상태**에서 던전 8 + 탑 2 입장 성공 수 | **10 / 10** ⇒ **제품은 멀쩡하다** |
| [2] | §1 의 코드를 글자 그대로 굴린 뒤 남는 상태 | 보스 섬 · `bossOn` **true** · `bossT` **15s** · `bossMode()` **"stage"** · `battleBusy()` **true** · `dunRun` **false** |
| [3] | 그 상태 위에서 입장 | **10 / 10 실패** = 등재문의 «10건» |
| [4] | **음성항** — 현행 준비(`endDunRun` 만) 뒤 | `battleBusy()` 여전히 true · gold 입장 **false** |
| [5] | `verify457` 꼴 `reset()`(전 모드 중립화) 뒤 | **10 / 10 성공** = 처방 확인 |
| [6] | 입장 실패를 **한 줄도 안 찍고** 지나가는 자리 | 수리 전 **2곳** → 수리 후 **0곳** (트리에 따라 갈리는 유일한 축) |

⇒ **[2]+[4] 가 이 작업의 본체다.** 막고 있던 것은 «던전» 이 아니라 **스테이지 보스전**이라,
게이트가 부르던 `endDunRun(false, true)` 은 **아무 일도 안 하고 있었다**(끌 것이 애초에 던전이 아니었다).

⚑ **곁다리로 더 큰 것이 나왔다 — 13항이 애초에 «전부» 가 아니었다.**
§1 의 던전 방향 절과 §R 되돌림 절이 `if (!blk(…) && !p.err)` 로 감싸여 있어, 입장이 실패하면
**한 줄도 안 찍고 절 전체를 건너뛰었다.** 그래서 «10건 실패» 회차의 총항이 13 이었던 것이고
(빨간 10 + 초록 3), 두 절은 «없는 것» 으로 조용히 지나갔다. 소리를 내게 하자 같은 고장이
**3/17** 로 읽힌다 — 같은 트리, 같은 결함, 네 항 더. 이 종류의 헛초록은 «게이트가 초록이라
안심» 이 아니라 **«게이트가 그 자리를 아예 안 본다»** 라서 328·319 계열보다 조용하다.

## 3. 수리 — `tools/verify338.js` 4자리 (제품 0줄)

| 자리 | 전 → 후 |
|---|---|
| ~72 (신설) | 페이지 안에 **`window.__v338neutral()` 한 곳** 을 심는다 — `verify457` §HARNESS 의 `reset()` 과 같은 꼴(던전·아레나·레이드·승급전·`bossOn`/`bossT`/`bossIntro`/`stageWin`·`enemies`/`spawnQ`/`shots`/`nums`/`corpses`·플레이어·카메라) |
| ~89 `enter()` | `if (dunRun) endDunRun(false, true);` → `window.__v338neutral();` · 실패 문구에 원인을 붙인다 → `입장 실패 (battleBusy=… · bossMode="…")` |
| ~112 `cleanup()` | `if (dunRun) endDunRun(…)` → `window.__v338neutral();` |
| ~122 §1 준비 · ~146 §1 뒷정리 | 둘 다 중립화를 읽는다. 뒷정리는 **자기가 세운 보스전을 자기가 껐는지** 를 두 항으로 단언한다(`battleBusy()` false · `bossMode()` `''`) |
| ~150 · ~300 | 조용한 건너뜀 2곳을 `if (blk(…) \|\| p.err) { if (p.err) no(…) } else {` 로 — **절이 죽으면 죽었다고 말한다** |

**처방을 등재문 그대로 따르되 «더 나은 쪽» 을 골랐다** — 등재문의 1안(`failBoss('시간 초과')`)은
§1 이 세운 보스전만 치우고 다음에 모드가 늘면 또 새 줄이 필요하다. 2안(`reset()` 꼴 통일)을 골라
**전장을 되돌리는 정의가 파일에 한 곳**뿐이게 했다(LESSONS 58-① · 453 이 제품에서 한 것과 같은 꼴).

⚠ **항을 눌러 «입장 실패해도 초록» 으로 만들지 않았다**(등재문 경고 · LESSONS 328).
§2 의 단언은 한 항도 안 물러졌고, 총항은 **13 → 181** 로 늘었다(조용하던 두 절 + §1 뒷정리 2항).

## 4. 게이트

- **`node tools/verify338.js` 3/13 FAIL → 181/181 PASS** (338 완료 당시 179/179 → +2 = §1 뒷정리)
- **`tools/verify466.js` 27/27 신설** — 잠그는 것은 제품이 아니라 **«게이트가 자기가 세운 전투를 자기가 치우는가»** 다
  - **[A] 모양** — 중립화 선언 **1곳**(값을 두 곳에 안 적는다) · 그것을 읽는 자리 **4곳** ·
    끄는 모드 6종 전수 · **[A4] 중립화 밖에 남은 `endDunRun(` 0건** · [A5] 실패 문구가 원인을 댄다
  - **[B] 헛초록** — 조용한 건너뜀 **0곳** · 실패를 찍는 자리 ≥3
  - **[C] 실행** — `verify338` 판정 PASS · 초록 항 **래칫 181** · «입장 실패» **0건** ·
    던전 8 + 탑 2 태그 전수 · **[C7]·[C8] 조용하던 두 절이 실제로 출력에 있다** · [C9] §1 뒷정리
  - **[R] 되돌림 시험** — 중립화에서 **지렛대 한 줄**(`bossOn = false; bossT = 0; …`)만 뺀 사본을
    실제로 굴린다 ⇒ **FAIL · 종료 코드 1 · 던전 8 + 탑 2 전부 «입장 실패» 로 되돌아옴 ·
    실패 문구가 `bossMode="stage"` 를 댄다.** 음성항 둘로 «사본이 다른 이유로 빨간 게 아님» 을 못박았다:
    **[R5]** 사본도 콘솔 에러 0건 · **[R6]** 사본에서도 §1 스테이지 방향은 초록(지렛대가 이 한 자리다).
    실측 **사본 3/17 ↔ 현재 181/181**.
- **`tools/probe466.js` 14/14 신설** (재현 기록 — 수리 전·후 같은 뜻)
- 회귀 **무수정 전부 초록**: `verify457` 76/76 · `verify425` 68/68 · `verify453` 82/82 ·
  `verify331` 79/79 · `verify332` 80/80 · **SMOKE PASS**

## 5. 다음 세션에게

⚑ **453 이후로 «게이트가 전투를 켜 둔 채 끝내면» 그 뒤의 모든 입장이 막힌다.**
`verify457` 이 1회차에 같은 것에 걸렸고(«국면 0회» 가 조용히 초록), 338 이 두 번째다.
전투 모드를 세우는 자를 새로 쓰거나 고칠 때는 **끝에 전 모드 중립화를 걸어라** —
`verify457` §HARNESS `reset()` · `verify338` `__v338neutral()` 둘 중 아무 꼴이나 그대로 베끼면 된다.
`endDunRun` 만 부르는 뒷정리는 **던전이 아닌 모드에서는 아무 일도 안 한다.**

⚑ **`if (!blk(…) && !p.err) { … }` 는 게이트에서 금지형이다.** 준비가 실패하면 그 절이
**한 줄도 안 남기고** 사라져 총항이 조용히 줄어든다(빨강이 아니라 «없는 것» 이 된다).
`if (blk(…) || p.err) { if (p.err) no(…) } else { … }` 로 적어라 — §2 루프는 원래부터 이 꼴이었고,
그래서 §2 만 빨갛고 §1·§R 은 조용했다. 같은 파일 안에서 두 관용구가 섞여 있던 것이 뿌리다.
