# 809 — `fix572` too-few 무복구 · `verifyProgress` 축 ⓓ 사각지대

> **T1 «버그(자 · 사각지대)» · 제품(`index.html`) 0줄 · 1회차 완료 (2026-09-02, sess-0928-8652 루틴 워커 B)**
> 지시서 [3]-**(가)** — 레퍼런스 대조가 없는 도구 작업이라 **비평가를 안 띄웠다.**

## 1. 등재문이 말한 것과 실제

등재(sess-0858-29665 워커 D, 807 재현의 곁다리)는 결손 둘을 지목했다. **둘 다 그대로 사실이었고**,
재현기 `probe807` 이 이미 두 자리를 찍고 있어서 이 회차는 «재현부터» 를 건너뛸 수 있었다(27/27 PASS).

| | 등재문 | 착수 시 실측 |
|---|---|---|
| ⓐ | `fix572.fixRow` 114행이 `n < COLS` 를 `'too-few(n)'` 로 즉시 반환 | `probe807` [3-799]·[3-800] 이 «못 고친다» 를 **못박고 있었다**(= 옛 동작이 게이트로 굳어 있었다) |
| ⓑ | 축 ⓓ 범위가 «완료 표지가 있는 행» 이라 표지가 밀린 행일수록 조용하다 | `probe807` [4-b] 가 «⏹ 종료 표지가 밀려난 5칸 행에 자는 **조용하다**(종료 코드 0)» 로 찍고 있었다 |

⚑ 이 작업의 성격은 «없는 것을 만든다» 가 아니라 **«재현기가 이미 빨강으로 찍어 둔 두 자리의 방향을 뒤집는다»** 였다.
그래서 `probe807` [3]·[4-b] 는 **이관 대상**이다(333 처방 — 관측점은 그대로, 묻는 방향만 반대로).

## 2. ⓐ — too-few 를 «같은 자리 점수 탐색» 으로 폈다

### 2-1. 왜 «뒤에 빈 칸 붙이기» 가 오답인가 (등재문 ⚠ 의 확인)

799 의 5칸은 `[ID][작업][주인원문][주 편집 구간][구현]` 이다. 뒤에 두 칸을 붙이면

```
[0]ID [1]작업 [2]주인원문 [3]주 편집 구간 [4]구현 [5]빈 [6]빈
                          ^^^^^^^^^^^^^^  ^^^^^^
                          §2 가 «구현» 으로 읽는 칸   진짜 구현 칸이 «최고 점수» 자리로 밀렸다
```

가 되어 **판정 불가는 사라지는데 판정은 틀린다** — 자를 조용하게 만들 뿐이라 결손보다 나쁘다.

### 2-2. 고른 방법 — too-many 와 한 물음으로 합쳤다

too-many 는 «어디를 합치나»(p·q), too-few 는 «어디에 빈 칸을 끼우나» 인데,
**7칸의 자리 [2]~[6] 에 남은 칸 `c[2..n-1]` 을 차례를 지켜 나눠 담되 «빈 자리» 를 허용하면**
둘이 한 탐색에 들어온다. 자리 수는 `C(rest+4, 4)` 라 5칸 행이 35가지, 6칸 행이 70가지다(전수).
점수는 too-many 가 쓰던 `score()` 그대로 — 고른 자리에서 **구현 칸·루프 칸이 그 칸처럼 읽혀야** 한다.

`fixRow` 의 too-many 경로는 **한 줄도 안 건드렸다**(`probe807` [3-720] 이 `p=2 q=4 score=13` 으로 불변).

### 2-3. 보정 둘 — 실측이 시킨 것이지 취향이 아니다

**① 빈 칸이 받은 점수를 되뺀다 (`blankCredit`)**

`score()` 는 «있는 표» 를 읽으라고 만든 자라 빈 칸을 후하게 본다 — `BARE_IMPL` 이 빈 구현 칸을
«–» 와 같이 **3점**, `LOOP` 가 빈 루프 칸을 **2점**, 빈 측정표가 **1점**이다.
too-many 에서 그 빈 칸은 **표에 원래 있던 것**이라 증거지만, too-few 에서 빈 칸은 **우리가 방금 끼운 것**이라
증거가 아니다. 보정 전 실측(681 옛 5칸):

| 자리 | 뜻 | 점수(보정 전) | 점수(보정 후) |
|---|---|---|---|
| `cut=0,0,0,0,0,3` | **전부 비고에 몰고 앞칸을 다 비운다** | **8** ← 이겼다 | 2 |
| `cut=0,2,3,3,3,3` | 구간을 측정표로, «미착수» 를 구현 칸으로 | 6 | **4** ← 이긴다 |

퇴화 자리가 «빈 칸 3+2+1» 과 «긴 비고 +2» 만으로 정답을 눌렀다. 되뺀 것은 **비어서 받은 점수뿐**이고
채워진 칸의 점수는 손대지 않는다.

**② 동점이면 «덜 합치는» 쪽 (`-merges` 를 2순위 키로)**

too-many 의 동점 규칙은 «앞칸으로 더 미는 쪽»(p·q 큰 쪽)이다. too-few 에 그것만 쓰면 실측(681 옛 6칸)에서

| 자리 | 뜻 | 점수 | 합침 |
|---|---|---|---|
| `cut=0,2,…` | 구간+**미착수**를 측정표로 뭉치고 구현 칸에 «–» 를 남긴다 | 8 | 1 |
| `cut=0,1,2,3,4,4` | 구간·미착수·–·0/5 가 각자 제 칸 | 8 | **0** |

가 **둘 다 8점**인데 앞칸 규칙만으로는 위가 이겼다(`verify809` [2-681@6] 이 그 순간 빨갰다).
빈 칸을 끼우는 것은 자리를 **비우는** 일이라 글이 제 칸에 남지만, 합치는 것은 두 칸의 뜻을
**한 칸으로 뭉개는** 일이다 ⇒ 합침 수를 먼저 보고, 그래도 같으면 앞칸 규칙을 쓴다.

### 2-4. 정답 대조 — 807 이 **손으로** 편 두 행과 같은 자리에 선다

807 이 손으로 편 657·681 을 표본으로 되만들어(뒤 칸을 없앤 4·5·6칸) 자동 복구를 대 봤다:

| 표본 | 자동 복구가 세운 구현 칸 | 807 이 손으로 세운 구현 칸 |
|---|---|---|
| 657 (4칸·5칸) | `⏹ **종료(2026-09-02 00:02, 흡수)** …` | 같음 |
| 681 (4칸·6칸) | `미착수` | 같음 |

즉 **사람이 고른 자리를 자가 스스로 고른다.** (807 이 추가로 쓴 «칸 수 5 → 7 은 807 이 폈다» 같은
새 산문은 **집필**이라 기계 복구의 몫이 아니다 — 복구는 무손실이고 글자를 늘리지 않는다.)

## 3. ⓑ — 축 ⓓ 의 범위를 넓혔다 (이 작업의 본체)

옛 코드에서 칸 수 검사는 `if (!d) continue;`(= 완료 표지가 없으면 건너뜀) **아래**에 있었다:

```
    표지가 밀리면 →  §2 가 그 행을 «미착수» 로 본다 → 첫 continue → 축 ⓓ 가 **보기 전에** 놓친다
```

**밀린 행일수록 자가 더 조용하다 — 정확히 거꾸로다.** 657 이 실제로 ⏹ 종료행인데 티어 스캔에
«미착수» 로 보였고, 그 순간 `verify572` 만 빨갛고 `verifyProgress` 는 초록이었다.
지시서 [4] 는 push 게이트로 **`verifyProgress` 만** 지목하므로 그 모양은 **규칙을 지킨 워커에게도 안 짖었다**.

⇒ 칸 수 검사를 표지 검사 **위로** 올리고 범위를 «칸 수가 7이 아닌 행 전부»(위쪽 4칸 표 제외)로 넓혔다.

### 3-1. 등재문의 ⚠ («넓히면 등재 중인 워커의 push 를 막는다») 를 어떻게 닫았나

두 갈래로 닫았다.

1. **문턱이 «칸 수» 하나뿐이라 정상 등재행은 안 걸린다.** 제대로 쓴 등재행은 7칸이다 —
   걸리는 것은 **GitHub 이 이미 칸을 버리고 있는 행**뿐이다. 못박는 자가 `verify809` **§R** 이다:
   새 7칸 등재행(`| 999 | … | – | – | **0/5** | **미착수 · 등재만** |`)을 한 줄 끼워 넣어도 **초록**이고([R-1]),
   같은 행의 뒤 세 칸을 없애면 그때 이름을 부른다([R-2]), 되돌리면 다시 초록이다([R-3]).
2. **걸려도 값이 싸졌다.** 옛 범위를 좁게 잡은 진짜 이유는 «걸리면 손으로 풀어야 한다» 였는데,
   ⓐ 가 **too-few 를 자동 복구로 만들었으므로** 이제 처방은 `node tools/fix572.js --write` 한 줄이다.
   ⓐ 와 ⓑ 를 한 세션에 넣어야 했던 이유가 이것이다 — **ⓐ 없이 ⓑ 만 넓히면 워커를 손수리에 묶는다.**

자가 찍는 사유도 갈라 준다 — 표지가 있는 행이면 예전 문구, 없는 행이면
«완료 표지가 **구현 칸 밖으로 밀렸을 수도** 있다(809 — 표지 유무로 거르지 않는다)».

## 4. 게이트

| 자 | 결과 | 비고 |
|---|---|---|
| `node tools/verify809.js` | **28/28 PASS** | **신설.** [1] ⓐ 복구 · [2] ⓐ 자리 · [3] ⓑ 범위 · [4] 수렴 · **[R] 되돌림 5항** |
| `node tools/probe807.js` | **29/29 PASS** | **이관**(27 → 29). [3] «못 고친다» → «편다» · [4-b] «조용하다» → «657 을 이름으로 부른다» |
| `node tools/verify572.js` | 17/17 PASS | too-many 경로·무손실·수렴 불변 |
| `node tools/verify388.js` | 157/157 PASS | §2 자기모순 축 불변 |
| `node tools/verifyProgress.js` | **종료 코드 0** | 넓힌 범위에서도 작업 트리 초록(헛빨강 0) |
| `node tools/verify571.js` | 종료 코드 0 | §4 병합 표시 |
| `node tools/verify733.js` | 종료 코드 0 | §5 추적 파일 급감 |
| `node tools/probe571.js` | 종료 코드 0 | |
| `node tools/smoke.js` | **SMOKE PASS** | 제품 0줄이라 형식 확인 |

## 5. 곁다리 — **오등재 하나와 그 정정** (→ 810)

### 5-1. 처음 적은 것 (틀렸다)

회귀 스윕에서 `verify557` 이 **35/39**([B-a]~[B-d]) · `probe557` 이 «재현 실패 1건» 으로 빨갰다.
두 단계로 «내 것이 아님» 을 갈랐다고 적었다 — ⓐ 내 세 파일을 `git stash` 로 뺀 트리에서도 같은 값,
ⓑ ROUTINE [-2] 의 `git fetch --deepen=640` 뒤에도 그대로 ⇒ «환경이 아니라 실재하는 자 결손» 이라고
810 으로 등재했다.

### 5-2. 실제 (같은 세션이 정정)

**깨끗한 트리에서 `verify557` 은 39/39 PASS 다**(실측 2회 · `probe557` 도 초록).
빨강의 원인은 **이 세션이 남긴 쓰레기**였다:

`verify557` [E] 는 «자산 없는 미착수 행» 에 임시 자 `tools/verify<eid>.js` 를 심었다가 `finally` 로 지운다.
그런데 ⓑ 를 확인하려고 돌린 `git fetch --deepen=640 && node tools/verify557.js` 가 **2분 타임아웃에 걸려
[E] 한복판에서 죽었고**, `tools/verify678.js`(`process.exit(1)` 한 줄)가 트리에 남았다.
그 파일이 있으면 678 행이 «자산 있음 + 자가 빨감» 이 되어 §3 이 «마감 누락» 대신 «관찰» 로 답하고,
[B]·[E] 가 표본을 그 행으로 고르면 4항이 줄줄이 빨개진다.

**즉 ⓐ·ⓑ 두 단계를 다 밀고도 틀렸다** — 두 단계가 «내 *변경*이 원인인가» 와 «환경이 원인인가» 만 물었고
«내 *실행*이 트리에 뭘 남겼는가» 는 안 물었기 때문이다. `git status` 에 낯선 untracked 파일이 있었는데
«남의 진행분» 으로 넘겨짚은 것이 갈림길이었다(실제로는 **내 자가 만든 파일**이었다 — mtime 이 내 실행 시각).

⚠ 이 세션의 stop 훅이 그 파일을 **«커밋하라» 고 권했다.** 담았으면 `process.exit(1)` 한 줄이 678 의
«게이트» 로 굳어 §3 이 그 행을 영원히 «자가 빨갛다» 로 읽었을 것이다 —
지시서 [4]·[6](«내가 만진 파일만 이름으로 add») 가 그 사고를 막았다.

### 5-3. 그래서 810 은 무엇인가

**«임시 표본이 크래시에 안 지워진다»** 로 다시 등재했다 — 이쪽은 실재하는 결손이다
(처방 후보: 표본을 `tools/` 말고 임시 디렉터리에 · `exit`/`SIGTERM` 뒷정리 · 시작 때 쓸어내기).
⚠ 810 은 **809 의 것이 아니다** — 809 는 축 ⓓ(칸 수)의 사각지대다.

## 6. 다음 세션이 알아야 할 것

- `fix572` 는 이제 **양방향**이다. `verifyProgress` 가 UNJUDGEABLE 을 내면 칸이 많든 적든
  `node tools/fix572.js --write` 한 줄이 답이다 — 손으로 칸을 옮기지 마라(자리를 틀리게 고르면
  판정 불가만 사라지고 판정이 틀린다 · §2-1).
- `fix572` 의 too-few 경로에만 있는 보정 둘(`blankCredit` · `-merges`)은 **too-many 에는 없다.**
  too-many 의 빈 칸은 표에 원래 있던 증거라서다 — 한쪽 규칙을 다른 쪽에 옮겨 붙이지 마라.
- 축 ⓓ 가 이제 **미착수 행도 잰다.** 등재만 하고 가는 워커도 **7칸으로 써야** push 가 통과한다
  (`verify809` [R-1] 이 «제대로 쓴 등재는 안 막힌다» 를 못박고 있으므로, 막혔다면 정말 칸이 틀린 것이다).
