# 35 패스 — 스테이지 패스 (작업 기록)

- 측정표: `docs/measure/35-패스.md` (2026-08-25 초판, 픽셀 스캔 75개)
- 캡처 스크립트: `node tools/cap35.js [이름]` → `docs/review/35-r{n}.png`
- 회귀·기능 하니스: `node tools/verify35.js` (**35 를 다시 손대는 세션은 손대기 전/후로 한 번씩 돌릴 것** — LESSONS 25-⑦)

---

## 1회차 (2026-08-25, sess-0358-27977, 워커 D) — 화면 신설

### 껍데기 판정

측정표 §0 대로 **딤 없는 전체화면 페이지**다(03 던전 · 14 유물과 동류). 공용 `#modal` 을 쓰지 않고 `#psw` 를 신설했다.
판정 근거는 픽셀이다 — 순백 `#FFFFFF` 42,381px · 순흑 `#000000` 129,410px 이 살아 있어 곱연산 딤이 없고(33 재화팝업은
딤 0.46 배로 최고광이 116 이었다), 공용 하단 탭바(02 는 검정 3px + `#42362A`)가 **페이지 전용 바**(검정 8px + `#49322C`)로
통째로 교체돼 있다. A5 교훈 «파일명에 팝업이 붙었다고 모달이 아니다» 의 재확인이다.

### 세로 앵커 (frame y = ref y − 84)

| 구간 | ref y | frame y | 구현 |
|---|---|---|---|
| 히어로 | 84..618 | 0..534 | `.ps-hero{top:0;height:535}` |
| 골드 구분바 | 619..647 | 535..563 | `.ps-gold{top:535;height:29}` (아래 검정 4px 은 헤더 밴드가 소유 — 이중 도색 방지) |
| 무료/프리미엄 헤더 | 648..795 | 564..711 | `.ps-hdr{top:564;height:148}` |
| 리스트 | 796..2160 | 712.. | `.ps-list{top:712;bottom:179}` |
| 하단 패스탭바 | 2161..2339 | 바닥 앵커 | `.ps-bar{bottom:0;height:179}` |

> **24px 잔차 처리** — 프레임 2280 − ref 콘텐츠 2256 = 24. 하단바는 **bottom 앵커**, 상단 3층은 **top 절대**,
> 남는 24px 은 **리스트(스크롤 영역)가 흡수**한다. 0번 «남는 높이는 가변 요소가 흡수» 와 같은 처방이고,
> 22 교훈 4(«본문 높이를 절대값으로 박으면 짧은 프레임에서 하단이 삼켜진다»)를 처음부터 피한 구조다.
> 화면비 3종(1920/2280/2520)에서 상단 앵커 Δ0 · 하단바 bottom = 프레임 높이 · 리스트만 1029/1389/1629 로 변한다.

### 리스트 구조

- 3분할: 무료 `0..518` / 스파인 `520..559` / 프리미엄 `561..1079`. 바깥 하이라이트 줄(무료 x4..9 6px · 프리미엄 x1070..1074 5px).
- 행 pitch **229.85**(정수가 아니다 — 측정표 §5-2 최소제곱). 행 셀은 «상 5px 베벨 / 하 6px 베벨 + 6px 어두운 홈» 을
  **하드 스톱 그라디언트 한 줄**로 만든다(요소를 더 만들지 않는다).
- 스파인은 리스트 전체에 **하나만** 깔고(행마다 만들면 이음선이 생긴다), 초록 달성 구간은 자식 `<u>` 의 height 로만 표현한다.
  끝나는 y 는 **마지막 해금 단계의 육각 top**(= `last*229.85 + 59.5`) 이다 — ref 가 «3번째 육각의 위에서 끊기고 아래에서 검정» 이다.
- 육각 배지는 실측이 **45° 챔퍼 8각**(좌우 수직변 31px · 위아래 챔퍼 38px)이라 `clip-path:polygon()` 8점으로 만들었다.
  «육각» 이라는 이름에 끌려 pointy hex 로 만들면 최대폭 구간이 한 줄이 돼 31px 이 안 나온다.
- 별 워터마크는 **이 화면 전용 타일**이다. 20 교훈 2(«워터마크는 이미 넣은 화면에서 grep 해 재사용하라»)대로 기존
  `.ci-body`/`.spc-body` 자산과 대조했더니 **별 38×37 vs 53×51 · pitch 153.75 vs 236 으로 불일치** — 재사용하면 22 교훈 3
  («타일이 반쪽이라 행의 절반이 비어 있었다»)이 그대로 재발한다. 그래서 새 SVG 타일을 만들고 pitch·오프셋을 실측대로 넣었다.

### 기능 (T2 «기능 완성 규칙» 수준으로 실동작 확인)

| 조작 | 기대 | 결과 |
|---|---|---|
| 최고 스테이지 79 | 단계 0..14(스테이지 5..75) 해금, 15(80) 잠금 | ✅ `last=14` |
| 무료 칸 클릭 | 재화가 **실제로** 증가 + 칸이 «수령완료» + ✓ | ✅ relic +17, `dn`·✓ 부착 |
| 같은 칸 재클릭 | Δ0 | ✅ |
| 미해금 단계 클릭 | Δ0 | ✅ |
| 프리미엄 미활성 + 프리미엄 칸 | 🔒 표시 + Δ0 + 안내 팝업 | ✅ |
| 프리미엄 활성화 | 구매 버튼 숨김 + 🔒 해제 | ✅ |
| 활성화 후 프리미엄 칸 | 재화 증가 | ✅ gold +13,500,000 |
| reload | 프리미엄·수령 기록 유지 | ✅ |
| 구버전 세이브 5변종(`없음/null/"x"/7/{prem:"y"}`) | 기본값 흡수, NaN·예외 0 | ✅ |
| 하단바 탭 전환 | 활성 칸 289 / 비활성 190 재분배 | ✅ |
| 뒤로가기 ← | 페이지 닫힘 | ✅ |

- 저장은 `S.pass = { prem, got }` **신설, KEY 는 올리지 않았다**(44 교훈 2 — 스칼라/객체 추가는 `Object.assign(DEF(), d)` 가
  이미 흡수한다). 옛 세이브 변종은 `load()` 에서 항목별로 못박았다.
- **결제 연동은 없다.** 44 와 같이 지급을 `grantPassPrem()` **한 함수**로 모으고 구매 버튼은 «준비 중» 팝업이다
  (지시서 [3] «목업·준비 중은 결제처럼 문서가 명시한 경우만 허용»에 해당). 결제를 붙일 때 버튼 핸들러 한 줄만 바꾸면 된다.
  디버그 경로는 `window.devPassPrem()` 로 그 함수를 부른다.

### 진입점 — ⚠ 임시다 (52 가 인계할 것)

레퍼런스는 **좌측 사이드 아이콘 «패스»** 다. 그런데 작업 48 이 «던전» 을 지우며 비워 둔 **5번째 라벨 행을
작업 34(축복)가 이미 가져갔고**, `SIDE.N` 을 6 으로 올리면 `layoutSide()` 가 `--ih/--igap/--itop` 을 N 으로 역산하므로
**짧은 화면비에서 좌측 6칸의 pitch 가 전부 달라진다**(48 주석). `#sideL` 은 내 작업 구간도 아니라 **손대지 않았다.**

→ 진입점은 **▦ 메뉴 placeholder 안의 임시 버튼**(`#psGo`)에 뒀다. **52(메뉴 팝업) 세션이 이 버튼을 정식 메뉴 항목으로
가져갈 것.** 옮길 때는 LESSONS 57-① 대로 **이 임시 경로를 그때 지운다**(남겨 두면 «두 개의 진입점» 이 된다).
05→06 진입점 인계와 같은 패턴이고, 그래서 이 판단을 여기 남긴다(LESSONS 50-③ — 다음 세션이 «지시 위반» 으로 되돌리지 않게).

### smoke

`.side .ibtn[data-pop]` 자동 수집에 안 걸리는 2단계 진입이라 `tools/smoke.js` 에 오프너 6종을 명시 추가했다 —
`pass:35` · `ptab:stage/box/tower/att` · `pass:back`. **SMOKE PASS.**

---

## 2회차 (2026-08-25, 같은 세션) — 캡처 오염 3건 차단 + 회귀 하니스

`verify35.js` 를 만들어 좌표를 재 보니 **1회차 캡처가 통째로 오염돼 있었다**(LESSONS 04-① 의 연출 판).
비평가에게 보내기 전에 잡아서 회차 하나를 버리지 않았다.

| # | 오염 | 증상 | 처방 |
|---|---|---|---|
| ① | 60 쥬시의 «열기 팝»(scale)이 도는 중에 촬영 | 페이지 전체가 **3~4% 작게** — 구매 버튼이 419×149 대신 **405×144**, 중심은 그대로라 «작게 그렸다» 로 오독된다 | 연출이 끝난 뒤 촬영 |
| ② | `#psw` 불투명도 **0.38** 프레임에 촬영 | 전 화면이 균일하게 어두워지고(골드바 253,208,81 → 96,79,31 = ×0.38), 그 아래 **«닫히는 중» 인 메뉴 모달의 크림**이 비쳐 들어옴 | 위와 같음 |
| ③ | «끝났나» 를 `getAnimations()`·`setTimeout` 으로 판정 | **«아직 시작 안 함» 을 «끝남» 으로 읽는다** — 그래서 ①②가 통과했다 | **상태로 판정**: `opacity === '1'` **그리고** 메뉴 모달 `display === 'none'` 일 때만 촬영 |

추가로 «캡처 위에 z≥34 오버레이가 떠 있으면 즉시 throw» 하는 가드를 넣었다(`fxl` 은 투명한 연출 레이어라 제외).
재캡처 후 픽셀 대조: 골드바 `(253,208,81)` · 헤더 무료 `(137,146,187)` · 헤더 프리미엄 `(126,27,206)` ·
하단바 활성 `(125,87,76)` — **전부 측정표 팔레트와 정확히 일치**.

### 내가 쓴 assert 가 틀린 경우 2건 (LESSONS 43-① 의 재현)

1. **«프리미엄 수령 gold 증가량이 30 모자라다»** — 구현이 아니라 검사가 틀렸다. `S.autoBuy` 가 기본 ON 이라
   **자동 강화가 두 번의 읽기 사이에 골드를 쓴다.** 재화 Δ 를 재는 검사는 `S.autoBuy = false` + `window.step = () => {}` 로
   묶어 놓고 재야 결정적이다. (25 교훈 6-② «autoEquip 이 기본 ON» 의 골드 판)
2. **«마이그레이션 실패»** — `load()` 는 세이브 객체가 아니라 **`d.time` 을 돌려주고 전역 `S` 를 세운다.**
   반환값을 세이브로 착각해 `b.pass.prem` 을 읽다가 죽었다. 마이그레이션 자체는 처음부터 멀쩡했다.
   그리고 영속성 검사 섹션에서 `addInitScript` 를 쓰고 있었다 — **reload 때 원본이 재주입돼** 저장 결과를 덮는다(LESSONS 50-②).

`VERIFY35 PASS` (46항목: 껍데기 앵커 18 · 하단바 칸 폭 10 · 기능 14 · 화면비 3종 12) · `SMOKE PASS`.

---

## 3·4회차 — 비평 1차 (E·F) 와 반영

깨끗한 캡처 `35-r1.png` 을 비평가 2명에게 독립으로 채점시켰다.

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| E | 4 | 4 | 7 | 6 | 5 | 7 | **4** |
| F | 5 | 6 | 6 | 7 | 5 | 5 | **5** |

통과선(①~④ 최저 8)에 한참 못 미친다. 다만 **두 비평가 모두 «프레임·밴드·열·행 그리드는 픽셀 단위로 정확»** 이라고
독립적으로 확인했다(검산표에서 골드바·헤더밴드 148·보상칸 158×157·행 pitch 229.85·구매버튼 419×149·스파인 40 전부 일치).
점수를 끌어내린 것은 전부 **«밴드 안에 들어가는 부품»** 이었고, 그중 **렌더 버그가 3건**이었다.

### 렌더 버그 3건 (캡처로는 «디자인이 이상하다» 로만 보인다)

1. **`.ps-eg` 에 `position` 이 없어 컬럼 바깥 하이라이트·외곽 테 4줄이 통째로 안 그려졌다.**
   `.ps-tk` 의 자식이라 `.ps-r>*{position:absolute}` 가 안 걸렸다. F 의 **1순위 지적**(«4개 줄 전부 100% 누락»)이 이것이다.
   요소를 만들어 넣었다고 그려지는 게 아니다 — **부모가 다르면 position 규칙도 다시 봐야 한다.**
2. **수령완료 칸에 인라인 `--fill/--rim` 이 남아 «인라인 > 클래스» 로 `.dn` 의 수령완료 색이 죽었다.**
   E·F 가 각각 «명암 2.64배 밝음» / «3.2배 밝음» 으로 독립 검출했다. 처방은 CSS 특이도 싸움이 아니라
   **renderPass() 가 수령완료면 인라인을 아예 안 붙이는 것**이다.
3. **`<s>` 는 취소선 요소다.** 자물쇠·체크처럼 «글자» 를 담은 `<s>` 에 **순백 72×5 막대**가 그려져
   **프리미엄 칸 12개 전부에서 칸 우측 경계를 24px 침범**했다. E 가 3순위로 정확히 짚었고(F 는 «72×5 밝은 판» 으로 목격만 했다),
   원인이 «자물쇠 아트» 가 아니라 **`text-decoration` 기본값**이라 아트 교체로는 영원히 안 없어졌을 결함이다.
   → `#psw s{text-decoration:none}`. **의미 없는 태그로 도형을 만들 때는 그 태그의 기본 스타일을 먼저 지워라.**

### 좌표 오기 3건

4. **헤더 배지 `top` 에 ref 절대 y 를 넣었다** — 패딩박스 원점(frame 569) 기준이어야 하는데 84px 아래로 내려가
   헤더 하단 검정에 잘렸다(E 1·2순위: «보이는 높이 −72%»). 03 규칙(«절대 배치 자식의 기준은 padding box»)의 재현이다.
5. **육각이 108×107 평탄 8각이었다** — ref 중심열은 **106×114 돔**이다(E·F 가 −6.2% / −7% 로 일치).
   그리고 내부의 어두운 코어 링은 ref 에 **없다**(세로 그라디언트 한 겹뿐).
6. **보상칸이 ref 대비 +2px**, 아이콘이 **15px 떠 있고** 잉크가 7~10% 작았다.

### 측정표 §11-2 의 경고가 그대로 현실이 됐다

측정표가 «숫자는 ÷0.69 규약을 쓰면 30~40% 크게 나온다 — 구현 후 캡처 대조 필수» 라고 적어 둔 그 항목이다.
그 규약으로 뽑은 `scaleX .63` 을 그대로 넣었더니 **자폭이 ref 의 절반**이 됐다(ref 자당 22~25 vs 우리 11~13).
F 가 «폭 −50%» 로 잡았다. **실측으로 다시 뽑아 1.25 로 정정**했다.
→ 교훈: **측정표가 «신뢰도 낮음» 이라고 표시한 값은 구현에 넣기 전에 한 번 더 재라.** 표에 있다고 확정값이 아니다.

### 4회차에서 같이 고친 것

- 하단 탭 아이콘 잉크 폭 65~72 → ref 133~137 (E: −47~51%)
- 탭바 하단 립의 **명암 방향이 반전**돼 있었다(ref 는 «더 어두운» 60px, 우리는 «더 밝은» 18px)
- 히어로 아트 슬롯 면적 39% → ref 82.7% (아트 «내용» 은 교체 대상이지만 **면적·bbox 는 지금 맞춰야 한다**)
- 구매버튼 리본 클러스터가 메달과 67px 벌어져 총높이 +89% → 붙여서 측정표 bbox(117×120) 안으로
- 「79」 흰 코어 36×27 → ref 50×31 · 별 워터마크 −21% · x560 파란 1px 시임 제거 · 헤더 프리미엄 라벨 폭 +7%

캡처 `35-r2.png`(3회차) · `35-r3.png`(4회차). `VERIFY35 PASS` · `SMOKE PASS`.

### ⚠ 두 비평가가 엇갈려 «움직이지 않은» 항목 (LESSONS 22-⑤)

| 항목 | E | F | 조치 |
|---|---|---|---|
| 수령완료 체크 세로 위치 | 우리가 **8.5px 아래** | 우리가 **15px 위** | 우리 값을 사이에 두고 갈렸다 — **움직이지 않았다.** 다음 회차가 ref 를 직접 재서 판정할 것 |
| 하단바 비활성 탭 색 | ref = `#49322C`(뒤로가기 칸과 같음) | — | 측정표는 `#3F2D29`. ⑤ 항목이라 통과를 막지 않으므로 **보류**. 재측정 필요 |
| 육각 폭 | ref **106** | ref **108** | 측정표도 108 → **108 유지**(2:1) |

### 남은 문제 (다음 세션이 이어받을 것 — 회차는 **5회차부터**)

- **①~④ 를 8 로 올리려면 재채점이 먼저다.** 4회차 수정이 반영된 `35-r3.png` 은 아직 채점되지 않았다
  (위 점수는 전부 **수정 전** `35-r1.png` 기준이다). 5회차는 **재채점부터** 시작한다.
- 히어로 일러스트·헤더 메달 배지·탭 아이콘·자물쇠는 **아트 리소스 대기**다. 두 비평가 모두
  «그림은 아트, **슬롯 치수는 지금 CSS 로 회수 가능**» 으로 분류했다 — 치수는 4회차에서 맞췄으니
  남은 감점은 아트가 들어와야 회수된다.
- **서체 한계(회수 불가)**: 폰트 «크기» 는 맞고 «자폭» 만 다르다(E 측정: 「₩14,900」 폭 −32%, 타일 숫자 −37%,
  부제 −7.7%, 제목 높이는 **완전 일치**). 05 교훈의 `scaleX` 로 부분 회수만 가능하다.



---

## 5회차 (2026-08-25, sess-0834-17264, 워커 C) — 재채점(G·H) + 아이콘 bbox 회수

죽은 lock(05:31, 183분)을 회수해 이어받았다. 4회차 수정본을 새로 캡처(`35-r4.png`)해 비평가 2명(G·H)에게
독립 채점시켰다. **회차는 이어서 센다 — 4회차 다음이 5회차다.**

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| G | 8 | 6 | 8 | 9 | 7 | 6 | **6** |
| H | 8 | 5 | 6 | 8 | 6 | 6 | **5** |

3·4회차의 4·5 에서 6·5 로 올랐고 **①④ 는 8~9 로 통과선에 들어왔다.** 남은 블로커는 **② 상대 크기 비율**
하나이며, 두 비평가가 독립적으로 같은 목록을 냈다 — **아이콘·오버레이 bbox** 다(밴드·행·칸 그리드는
둘 다 «±2px 정확» 으로 확인했다).

### 엇갈린 수치 4건을 «제3의 스캔» 으로 판정했다 — `tools/scan35.py` 신설

G 와 H 가 같은 요소에 **반대 방향** 수치를 준 것이 있어서, 두 이미지를 **같은 창·같은 마스크**로 훑는
스캐너를 만들어 결정했다(LESSONS 05-③ · 70-③).

| 항목 | G | H | 자체 스캔 결론 |
|---|---|---|---|
| 자물쇠 | 우리 «64×78 — 너무 길다» | 우리 «52×38 — 너무 납작» | **H 가 맞다.** ref 50×60 vs 우리 **41×35**(세로 −42%) |
| 육각 | ref 91~93 / 우리 96 | ref 104 outer / 우리 108 | **둘 다 같은 방향** — 우리가 +4% 넓다 → 4px 축소 |
| 탭 배지 폭 | 우리 무료 96 | 우리 무료 64 | ref 125 기준 **둘 다 부족** — 125×89 / 125×108 로 확정 |
| 하단 탭 아이콘 | «폭 −22~29%» | «높이 +40%» | **서로 보완**이었다 — ref 는 «넓고 낮은» 아이콘, 우리는 «좁고 높은» |

> **스캐너가 스스로 틀린 건 1건.** 자물쇠 창을 칸 안쪽까지 열었더니 **칸 아이템 아이콘의 밝은 픽셀**이
> 자물쇠로 잡혀 «우리 67×73(너무 크다)» 이 나왔다 — 방향이 통째로 뒤집힌다. 창을 `x≥968` 로 좁히자
> 41×35 가 나왔고 H 의 지적과 일치했다. **마스크 창이 이웃 요소를 물면 비평가보다 더 크게 틀린다.**

### 반영 (전부 실측 → 수정 → 재캡처 → 재측정으로 닫았다)

| 요소 | ref | r4(수정 전) | r6(수정 후) | 처방 |
|---|---|---|---|---|
| 수령완료 ✓ | 82×67 @frame 783 · cx259.5 | 73×45 @792 · cx257 | **82×66 @783 · cx259.5** | `scaleY(.82)` 제거 + font 76→93 + `scaleX(.92)` |
| 자물쇠 | 50×60 @724 · cx989.5 | 41×35 @733 · cx988.5 | **45×62 @722 · cx992** | font 74→127(높이) + `scaleX(1.32→.8)`(폭 되밀기) |
| 무료 헤더 배지 | 125×89 | 97×97 | **125×89** | font 86→79 · scaleX 1.3→1.82 |
| 프리미엄 헤더 배지 | 125×108 | 78×117 | **125×108** | font 104→96 · scaleX 1.12→1.94 |
| 하단 탭 아이콘 | 본체+23, h92, w145~167 | 본체+1, w104~134 | **본체+21~26, w146~162** | top 2→23 · scaleX 1.15→1.6 |
| 하단 탭 라벨 | 본체+117, 코어 h25 | +110, 코어 h35 | **+116, 코어 h25** | font 38→27 · stroke 9→7 · scaleX .9→1.15 |
| 육각 | 채움 92 | 96 (중심 2px 아래) | **92** | 폭 108→104 · top 56.5→54.5 |
| 리본 보라꼬리 | 잉크 406..444 | 427..475 | 21px 상승 | top 58→37 · h 62→51 |

- **하단 탭바는 «바 본체 상단 기준 상대값»으로만 맞춘다.** 바닥 앵커라 프레임 24px 잔차가 그대로 들어가서
  절대 y 로 맞추면 화면비마다 어긋난다. 스캐너도 그렇게 비교한다(ref 2169 / 우리 2109 를 각각 0 으로).
- 히어로 배경 아트(금화더미 지면 밴드 433px)·탭 배지 «날개» 실루엣·체크/자물쇠 글리프 비율은 두 비평가가
  모두 **«아트 리소스 없이는 회수 불가»** 로 분류했다. **bbox 는 이번에 전부 맞췄으니** 이미지가 들어오면
  교체만 하면 된다(측정표 §10 «아트 필요» 표 참조).
- 텍스트 잉크 폭(₩14,900 −31.8% 등)은 **서체 한계**로 5회차에서도 그대로 둔다 — ⑤⑥ 는 통과를 막지 않는다.

`VERIFY35 PASS` · `SMOKE PASS` · 캡처 `35-r6.png`.



---

## 6회차 (2026-08-25, 같은 세션) — 비평 I·J + «밝기 마스크의 함정»

5회차 결과물(`35-r7.png`)을 비평가 2명에게 다시 독립 채점시켰다.

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| I | 8 | 5 | 7 | 8 | 7 | 8 | **5** |
| J | 7 | 6 | 7 | 8 | 7 | 8 | **6** |

①③④⑥ 는 7~8 로 올라왔고 **둘 다 «②의 단일 원인은 프리미엄 자물쇠» 라고 못박았다.**

### ★ 이번 회차의 핵심 교훈 — «밝기 마스크는 글리프의 일부만 잰다»

5회차에 내가 «자물쇠 45×62 로 ref(50×60)와 일치» 라고 닫았던 항목이 **완전히 틀렸다.**
`min(RGB)>150` 마스크는 ref 자물쇠(흰 배지)는 거의 전부 잡지만 **🔒 이모지는 금색 몸통만 잡고
어두운 고리를 통째로 버린다.** 실제 글리프는 **75×134** 로 칸 높이의 88% 를 덮고 있었다 —
비평가 둘이 «+50% / +160%» 로 같은 것을 가리켰다.

→ **대체물과 원본의 «색 구성» 이 다르면 같은 마스크로 잰 값은 비교 가능한 값이 아니다.**
   판정은 «배경과 다른 모든 픽셀»(타일 우단 밖 돌출 프로파일)로 다시 했고,
   글리프 자체 치수는 **빈 페이지에 같은 글리프를 찍어** 뽑았다(font 56 + scaleX 1.21 → 잉크 50×60).
   ref 돌출 y723..795 · 최대 x1019 ↔ 우리 y720..776 · 최대 x1019 로 닫았다.

### 반영

| 요소 | ref | 수정 전 | 수정 후 | 처방 |
|---|---|---|---|---|
| 프리미엄 자물쇠 | 잉크 50×60 · 돌출 16px | **75×134**(칸의 88% 덮음) | 50×60 · 돌출 16px | font 127→56 · scaleX .8→1.21 · top −24 |
| 뒤로가기 화살표 | 잉크 95×82 @본체+42 (코어 82×64) | 82×64 @+48 (**외곽선 0px**) | **95×82 @+42** | `filter:drop-shadow` + `clip-path` 를 같은 요소에 주면 **클립이 필터 결과를 잘라 외곽선이 사라진다** → 검정 판 + 6px 인셋 코어 2겹 구조로 교체 |
| 활성 탭 아이콘 | h104 @본체+4 · 폭 241 | h82 @+26 · 폭 187 | **h103 @+5 · 폭 242** | 활성 칸은 아이콘도 크다 → `.pt.on>i` 규칙 신설(36 출석 활성에도 같이 걸린다) |
| 시련의탑 아이콘 | 105×83 @+25 | 122×70 @+38 | **105×83 @+25** | 칸별 scaleX·top |
| 보물상자 아이콘 | 167 | 151 | **167** | 칸별 scaleX 1.77 |
| 하단바 자물쇠 | 본체+96..158 | +82..142 | +96 | top 78→92 |
| 비활성 탭 배경 | **#49322C**(72~73,50,44) = 뒤로가기 칸과 같다 | #3F2D29 | #49322C | 측정표 값이 셀 하단 그림자 구간이었다 — I·J 가 독립 지적, 자체 스캔으로 확인 |
| 보상 수량 라벨 | 칸(158) 안 | «3,380,000» 잉크 **217px** 로 칸 밖 45px 돌출 | 칸 안 (73/47px) | 자릿수별 축소 클래스 `.ln1/.ln2` (표기 쉼표는 유지) |
| 무료 컬럼 좌측 하이라이트 | x5..9 (5px) | x4..9 (6px) | 5px | — |
| 히어로 지면 밴드 | 지평선 frame **362**, 아래는 하늘 0% | 하늘이 534 까지 (지면 0px) | 366 부터 하늘 0% | `.ps-gr` 신설(36 히어로는 `display:none` 으로 보호) |

### 미반영 — 근거를 대조해 «움직이지 않은» 것 (LESSONS 22-⑤)

| 지적 | 판정 |
|---|---|
| 헤더 밴드 하단 검정 «10px → 4px(H) / 6px(J)» | **자체 스캔 결과 ref 순검정 787..795 = 9px, 우리 10px.** 두 비평가가 본 4~6px 은 JPEG AA 를 뺀 코어다(LESSONS 70-④). 그대로 둔다 |
| 리스트 1행 상단 «−34px»(J-6) | 육각·칸·구분홈을 ref 절대좌표로 재면 Δ≤2px 로 맞다. 스크롤 잔차 범위 |
| 구매 버튼 «x617..1018»(J-5) | 비평가 스스로 «눈금 판독 ±10px» 로 표시했고 측정표 실측(605..1023)·앞 회차 비평과 어긋난다 |
| 자물쇠 «칸 세로 중앙에 앵커»(J-3) | 돌출 프로파일로 재면 우리 top 720 / ref 723 — 이미 칸 상단 앵커다 |

### ⚠ 캡처를 지우지 마라 — 비평가가 읽는 중이었다

`35-r7.png` 을 다음 회차 캡처로 교체하며 지웠는데, **그 시점에 비평가 2명이 그 파일을 채점하고 있었다.**
둘 다 «파일이 사라졌다» 를 보고서 첫 줄에 적었고, 한 명은 r8 로 갈아타 «히어로 말고는 바이트 동일» 을
직접 확인한 뒤 진행했다. 다행히 채점은 살았지만 **회차 하나를 통째로 날릴 뻔했다** — 캡처는 회차마다
새 이름으로 남기고, 지우려면 그 회차 채점이 끝난 뒤에 지운다.

`VERIFY35 PASS` · `VERIFY36 PASS` · `SMOKE PASS` · 캡처 `35-r9.png`.



---

## 7회차 (2026-08-25, 같은 세션) — 비평 K·L + «임계값이 만든 정반대 지적»

6회차 결과물(`35-r9.png`)을 새 비평가 2명에게 채점시켰다.

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| K | **9** | 6 | 8 | **9** | 7 | 7 | **6** |
| L | 6 | 5 | 7 | 8 | 7 | 6 | **5** |

K 는 ①④ 를 **9** 로 올렸다(«18개 타일 bbox 전부 0~1px», «열 중심·레일·탭 구분선 전부 ≤1px»).
L 의 ① 6 은 전부 **히어로(화면의 23%) 아트** 때문이다 — 지평선·집·전경 코인.

### L 이 «닫은 항목 6개» 를 재검증했다 — 4 ✅ / 2 ❌

| 항목 | 결과 |
|---|---|
| 수령완료 ✓ 82×67 @칸+43 | ✅ (ref 82×67 @+44 / 우리 83×66 @+43) |
| 자물쇠 잉크 50×60 · 돌출 16px | ✅ (ref 돌출 15 / 우리 17) |
| 헤더 배지 125×89 · 125×108 | ✅ |
| 뒤로가기 화살표 95×82 @바+42 | ✅ (ref 95×81 @+42 / 우리 95×78 @+44) |
| **보물상자 아이콘 폭 167** | ❌ — «167 은 우리 값이지 ref 값이 아니다. ref 는 **126**» |
| **히어로 하늘 종료선** | ❌ — 더 위여야 한다 |

### ★ 교훈 — «비평가 셋이 같은 요소를 111 / 167 / 179 로 읽은 이유»

ref 탭 아이콘은 **불투명 본체 둘레에 «코인 흩뿌림» 이 옅게 깔려 있다.** 그래서 임계값에 따라 폭이

| 임계값 | 80 | 120 | 150 | 200 |
|---|---|---|---|---|
| ref 보물상자 폭 | 190 | 182 | **167** | **111** |

으로 흔들린다 — 5·6회차 비평가는 흩뿌림까지(167~179), 7회차 K·L 은 본체만(111~126) 읽은 것이다.
**우리 이모지는 전부 불투명이라 흩뿌림 폭까지 채우면 실제보다 훨씬 무거워 보인다.**
→ 판정 기준을 **«ref 의 조밀부(열당 8px 이상 잉크)»** 로 고정했다: 스테이지 169 · 보물상자 123 · 시련의탑 105 · 출석 137.
   (측정표 §10 의 폭 값도 이 기준으로 다시 적었다)

### 반영 (r11)

| 요소 | ref | 수정 전 | 수정 후 |
|---|---|---|---|
| 탭 아이콘 폭(조밀부) | 169 / 123 / 105 / 137 | 232 / 167 / 102 / 156 | **169 / 123 / 103 / 159** |
| 시련의탑 아이콘 | 105×83 @바+25 | 105×70 @+38 (깃대가 위로 삐져나옴) | **103×81 @+27** |
| 히어로 하늘 면적비 | 25.6% | 37.8% | **30.4%** (5회차 60.7% → 지면 밴드 +46px 상향) |
| 전경 코인 | 텍스트를 가리는 오브젝트 없음 | Ø200 코인이 «최고 스테이지 : 79»·CTA 를 정면으로 가림 | 이모지 205px→132px 로 낮춰 더미 디테일 크기로 |
| 가격 «₩14,900» 코어 폭 | 157 | 107 (−32%) | **156** (scaleX .75→1.1) |
| 긴 수량 라벨 | 자릿수와 무관하게 h25~28 | 8자 이상에서 h20 으로 축소(높이 2종 공존) | **높이 유지 + 가로 압축**(.ln1 1.05 / .ln2 .84) |
| 프리미엄 자물쇠 | 실루엣 56×63 | 49×59 | font 56→60 |

### 미반영

- **히어로 일러스트·집·탭 아이콘 실루엣·보상 아이템 아트** — 세 비평가(K·L 및 앞 회차)가 모두 «아트 없이는 회수 불가» 로 분류.
  bbox·면적·경계는 CSS 로 맞춰 뒀으니 이미지가 들어오면 교체만 하면 된다(측정표 §10).
- **행 구분선 «17px → 4px»(K-10)** — 측정표 §5-2 가 «상 5px 베벨 / 6px 홈 / 하 6px 베벨» 을 행별로 실측해 둔 값이라 그대로 둔다.
- **출석 📅 폭** — scaleX 를 1.35→1.19→0.96 으로 내려도 측정 폭이 169→159 로만 줄어(글리프 안 흰 판 때문) 기준을 못 맞춘다. 아트 교체분.

`VERIFY35 PASS` · `VERIFY36 PASS` · `SMOKE PASS` · 캡처 `35-r11.png`.




---

## 8회차 (2026-08-25, sess-1017-30145 워커 B) — 비평 M·N + «없던 덩어리를 찾아냈다»

7회차 반영분(r11)을 새로 캡처(`35-r12.png`)해 비평가 2명에게 독립 채점시켰다.

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| M | 7 | 6 | 8 | 7 | 7 | 7 | **6** |
| N | 6 | 5 | 6 | 7 | 6 | 6 | **5** |

### ★ 이번 회차의 핵심 — 두 비평가가 **독립적으로 같은 1순위**를 짚었다: «우상단 집이 통째로 없다»

| 비평가 | 표현 |
|---|---|
| M | 집 프롭 bbox ref 390×385 @cx885 cy190 ↔ 우리 160×143 @cx480 cy461 — «면적 −85%, 중심 405px·271px 이동» |
| N | 집 bbox ref 386×375 @center(883,173) ↔ 우리 108×108 @center(539,459) — «면적비 8%» |

자체 검산(열별 «하늘 종료 y» 스캔)이 둘을 확정했다. ref 는 x720~1020 에서 하늘이
**170 · 111 · 55 · −13 · 64 · 121** 로 잘리는데 — 우상단을 큰 집 덩어리가 먹고 있다는 뜻이다 —
**우리는 18개 열 전부가 287 로 평평했다.** 히어로에 «덩어리» 자체가 없었고, 있던 🏠 이모지는
화면 **중앙 하단**에 코인과 나란히 박혀 있었다(= 위치도 크기도 다른 곳에 있었다).

### 반영 (r12 → r19/r20)

| 요소 | ref | 수정 전(r12) | 수정 후(r19/r20) | 처방 |
|---|---|---|---|---|
| 우상단 집 | x690..1079(w390) · frame y0..380(h380) · 지붕 꼭짓점 x884, 밑변 도달 y194 | **없음**(전 열 하늘끝 287) | x690..1079 · y0..379 · 꼭짓점 x884 @y0 · 전폭 도달 y200 | `.ps-hs` 신설(검정 판 + 인셋 면 2겹) |
| 지면 윗변 | 평평하지 않다 — 골 516~533 / 마루 274 (좌측 평균 388) | 전 열 평평한 287 | 12열 중 8열이 Δ≤4px | ref 프로파일을 30px 간격으로 떠서 `clip-path` 폴리곤으로 |
| 하늘 면적비 | 45.6% | 46.5%(집이 없어서 «우연히» 맞았다) | **44.5%** | 위 둘의 합 |
| 열별 하늘끝 평균 \|Δ\| | — | **109px** | **6px** | — |
| 구름(left:812) | 그 자리는 «지붕» 이라 구름 없음 | 집 위에 얹혀 x810·x990 을 32·18 로 끌어올림 | 제거 → 73 · 104 (ref 83 · 93) | — |
| 코인 더미 | x450 은 «골»(하늘 521), 마루는 x540~660 | 가운데 정렬이라 x415~665 를 덮어 골을 메움 | `left:0→120` (+60px) → x450 Δ+4 | — |
| 수량 라벨 이탈 | 칸 안 | R3 프리1 **칸 우변 +11px · 중심 +16.5px** · R3 프리2 이탈 45px | **전 행 이탈 0 · 중심 Δ≤1px** | 박스를 칸보다 넓게(−40/−40) + `.ln3`(10자↑ scaleX .72) |

### ★ 교훈 2건

> **1. `inset:0` 로 깐 자식에 `clip-path` 의 % 를 쓰지 마라 — 상자가 기대와 다르게 풀린다.**
> 집을 처음 올렸을 때 지붕 기울기가 1:1 이 아니라 **3.4:1** 로 뭉개졌다(y20 에서 폭 136, y80 에 벌써 전폭 390).
> 역산하니 % 가 **113px 짜리 상자** 기준으로 풀리고 있었다 — `#psw s` 태그 리셋이 걸린 오버레이라
> `inset:0` 만으로는 380px 을 못 잡는다. `width:390px;height:380px` 로 **명시**하자 즉시 1:1 이 됐다.
> (LESSONS 56-① «태그 리셋의 선택 범위» · A1 «의사요소는 box-sizing 리셋을 못 받는다» 와 같은 계열)
>
> **2. «면적비가 맞다» 는 «덩어리가 맞다» 가 아니다.**
> 7회차는 하늘 면적비를 30.4%(ref 25.6%)까지 좁혀 놓고 닫았고, r12 의 면적비는 46.5% 로 ref 45.6% 와
> **1%p 차이**였다 — 그런데 그 화면에는 **집이 아예 없었다.** 없는 덩어리(집)와 남는 하늘이
> 서로를 상쇄해 총량만 맞은 것이다. → 면적·비율 지표는 **열별(또는 행별) 프로파일과 같이** 봐야 한다.
> 집을 올리자 면적비가 33.9% 로 «나빠졌고», 지면 윗변까지 ref 프로파일로 떠서야 44.5% 로 돌아왔다.

### 미반영 — 근거를 대조해 «움직이지 않은» 것

| 지적 | 판정 |
|---|---|
| N-2 «하단 탭바 높이 +25%(+39px) · top −19px» | **반증.** 순검정 밴드 첫 행이 ref 2160(바닥에서 179px) / 우리 2101(바닥에서 178px) — **Δ1px** 이다. M 은 같은 것을 «바 본체 높이 165→166, 구분선 4개 전부 일치» 로 읽었다 |
| N-5 «보상 타일 152×152 인데 우리 142» · N-6 «육각 84→77» | **반증.** M 이 «타일 158×158 vs 158×157, 육각 bbox 104×112 vs 104×114» 로 정반대. 자체 스캔도 육각 채움 92×66 **Δ0**. N 이 다른 임계값으로 읽은 것(7회차 «임계값이 만든 정반대 지적» 의 재발) |
| M-6 «내비 아이콘 폭 −27~38%» | **7회차 결정 유지.** ref 탭 아이콘은 흩뿌림 때문에 임계값별로 111~190 으로 읽힌다. 7회차가 «조밀부(열당 8px↑)» 기준으로 169/123/105/137 로 고정했고 M 은 흩뿌림까지 읽은 값(171/146)이다. 기준을 회차마다 바꾸면 영원히 수렴하지 않는다 |
| M-5 «출석 아이콘 cx −19px» / 자체 스캔 «+16px» | **방향이 반대라 보류.** 같은 자로 재기 전에는 반영하지 않는다(LESSONS 56-②). 다음 회차에서 임계값 스윕으로 가른다 |
| M-2 «지면 경계선 1080×5px @y378» | 부분 반영. 전폭 점유는 폴리곤으로 해결했으나 «5px 실선» 은 자체 프로파일상 ref 지면 윗변이 274~533 으로 **평평하지 않아** 단일 y 의 선으로 볼 수 없다 |

`VERIFY35 PASS` · `VERIFY36 PASS` · `SMOKE PASS` · 캡처 `35-r20.png`.

### 8회차 추가 — 보류했던 2건을 «같은 자로» 갈랐다 (캡처 `35-r21.png`)

**(1) M-5 «출석 아이콘 cx −19px» → 반영했다. M 이 맞았다.**
임계값 6개(80/110/140/170/200/230)로 ref·캡처를 **같은 창·같은 자**로 스윕한 결과:

| 임계 | 80 | 110 | 140 | 170 | 200 | 230 |
|---|---|---|---|---|---|---|
| ref cx | 980.0 | 979.5 | 979.0 | 979.0 | 980.5 | 980.5 |
| 우리 cx | 959.5 | 959.5 | 960.0 | 959.5 | 960.0 | 959.5 |
| Δ | −20.5 | −20.0 | −19.0 | −19.5 | −20.5 | −21.0 |

**임계값과 무관하게 −19~−21px 로 일정** = 흩뿌림 함정이 아니라 진짜 어긋남이다.
원인은 `.ps-bar>div[data-ptab="att"]>i{right:40px}` — 상자를 우측에서 40 깎아 중심을 정확히 −20px 민다.
앞 회차 자체 스캔이 «+16px» 로 정반대를 읽었던 것은 **창이 달랐기 때문**이고, 이번 스윕이 그걸 뒤집었다.
→ `:not(.on)` 으로 좁혀 `right:0` (36 출석 패스에서는 이 칸이 «활성» 이고 36 담당 세션이 8회차에 활성
   아이콘을 Δ−2 까지 맞춰 뒀다 — 그 구간은 건드리지 않는다). 결과 **Δcx ≤1px (전 임계값)**.
덧붙여 임계 200·230(조밀부)에서 ref 폭 86 ↔ 우리 83 으로 **폭은 이미 맞다** — 7회차의 «조밀부 기준» 결정이 옳았다.

**(2) M-8 «행 구분선 체감 17px(ref 6px)» → 반영하지 않았다. 실측이 반증한다.**
M 은 «ref 는 양옆 밝은 띠가 값차 ±5 라 사실상 6px 선인데, 우리는 ±61 이라 체감 17px» 이라고 했다.
무료 컬럼 빈 구간(x60..170)의 세로 단면을 ref·캡처 같은 방식으로 뽑으니 **구조가 거의 동일**했다:

| | 위 밝은 띠 | 어두운 코어 | 아래 밝은 띠 | 총 |
|---|---|---|---|---|
| ref | y925..930 (6px, Δ**+13.5**) | y931..936 (6px, Δ−18) | y937..940 (4px, Δ+13.2) | 16px |
| 우리 | y924..929 (6px, Δ**+13.0**) | y930..935 (6px, Δ−16) | y936..940 (5px, Δ+12.7) | 17px |

ref 의 양옆 띠는 ±5 가 아니라 **±13** 이다 — 우리와 사실상 같다. 차이는 **전체 1px 상향**뿐이다.
7회차 K-10 도 같은 지적(«17px → 4px»)을 냈고 같은 이유로 미반영했다 — **비평가 2명이 같은 오독을 공유**한
사례다(LESSONS 56-② «둘이 같은 말을 해도 같은 자로 잰 것이 아니면 반영하면 안 된다»). ref 의 JPEG 평활이
띠를 흐리게 보이게 하는 것이 원인으로 보인다. 측정표 §5-2(상 5px 베벨 / 6px 홈 / 하 6px 베벨)는 **유효하다**.

<!-- 다음 세션에게: 8회차까지 돌았으므로 다음은 9회차다(상한 10).
     9회차는 «재채점부터» 시작한다 — 위 반영(r19/r20)은 아직 채점되지 않았다. 점수는 전부 수정 전 r12 기준이다.
     남은 1순위 후보: ① M-5 출석 아이콘 cx (임계값 스윕으로 방향부터 가를 것)
     ② M-8 행 구분선 «체감 17px»(하이라이트 띠 명도차 ±61→±15) ③ M-9~11 텍스트 잉크 높이 ±8%. -->

### 8회차 추가 2 — 히어로 텍스트 잉크 (캡처 `35-r23.png`)

비평가 M 의 9·10번(«타이틀 +8% 높이», «부제 폭 −7.7%»)을 임계값을 명시해 재실측하고 반영했다.
자체 스캔([6] 타이틀 «+23.6%»)과 **같은 방향**이라 두 자가 일치한 항목이다.

| 요소 | ref (임계) | 수정 전 | 수정 후 | 처방 |
|---|---|---|---|---|
| 타이틀 흰 코어 | w466 × h**55** @y107 (임계 225·240 동일) | w473 × h**68** @y103 — **높이만 +24%**, 폭은 이미 +1.5% | **w466 × h55 @y108** (Δw0 Δh0 Δtop+1) | font 74→**60**(높이를 내리고) · scaleX .91→**1.107**(줄어든 폭을 되민다) |
| 부제 L1 | w456 h31 @y210 (임계 215) | w422 h30 @y204 | **w459 h33 @y210** | font 34→**37** · top 200→**207** |
| 부제 L2 | w244 h33 @y247 | w231 h30 @y241 | **w250 h34 @y247** | (위와 같은 규칙 — line-height 37 은 그대로. 줄 pitch 는 양쪽 37 로 이미 일치했다) |

- **높이만 틀리고 폭은 맞은 경우의 정석**: font-size 로 높이를 잡고 scaleX 로 폭을 되민다(7회차 21번 교훈과 같은 수법).
- **⚠ 두 규칙 모두 `#psw:not(.att)` 로 좁혔다.** 36 출석 패스가 같은 슬롯을 쓰면서 `>i` scaleX 를
  **현재 font-size 기준으로** 맞춰 뒀기 때문이다(6069·6071·6077 행). 밑값을 바꾸면 36 이 조용히 틀어진다.
  → **36 담당 세션에게**: 36 의 타이틀·부제도 같은 방식으로 재실측할 값이 있을 수 있다. 35 는 건드리지 않았다.

`VERIFY35 PASS` · `VERIFY36 PASS` · `SMOKE PASS` · 캡처 `35-r23.png`.

---

## 9회차 (2026-08-25, 같은 세션 sess-1017-30145) — 비평 P·Q + «보석을 하늘로 읽었다»

8회차 반영분(r20)을 새 비평가 2명에게 채점시켰다. **두 명 다 ①~④ 최저 7** 로, 8회차 시작(M 6 / N 5)에서 올라왔다.

| 비평가 | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | **①~④ 최저** |
|---|---|---|---|---|---|---|---|
| P | 8 | 8 | 8 | 7 | 7 | 7 | **7** |
| Q | 8 | 7 | 8 | **9** | 6 | 7 | **7** |

P 는 «통과를 막는 실질 결함은 #1 내비 4번칸 19.5px 편심, 그 다음 #3 부제 6px 상승, #5 «79» 행간 −4px» 라고
못박았다. **이 중 #1·#3 은 r20 채점 중에 이미 고쳐져 있었다**(8회차 추가 1·2 — 채점은 r20, 반영은 r23).
P·Q 가 독립적으로 «출석 아이콘 −19.5px / −20px», «타이틀 +8.1% / +9.6%», «부제 −7.3% / −7.5%» 로
**앞서 자체 스윕이 낸 값과 소수점까지 같은 수치**를 냈다 — 스윕 판정이 옳았다는 교차 검증이다.

### ★ 이번 회차의 핵심 — «열 프로파일이 행 프로파일과 어긋나면, 마스크가 다른 것을 잡고 있다»

Q-9 가 «하늘·지면 경계가 30px 아래고, ref 는 y386 이후 하늘 5% 이하인데 우리는 y501 까지 15% 남는다» 고 했다.
8회차에서 내가 **열별 «하늘 종료 y»** 로 떠서 폴리곤을 만든 그 지면이다. 자체 재측정 결과 **Q 가 맞았다**:

| 구간 | ref | r23(8회차 결과) | Δ |
|---|---|---|---|
| y285..385 | 37.2% | 48.3% | **+11.1%p** |
| y385..535 | 4.4% | 8.7% | +4.3%p |

원인은 **열 프로파일과 행 프로파일이 서로 다른 답을 준 것**이었다. 열별로 재면 x20~70·x160~180·x400~450 이
«하늘이 y476~534 까지» 로 나오는데, 행별로 재면 ref 는 y385 에서 이미 하늘이 5% 다. 둘 다 맞을 수는 없다.
→ 정체는 **금화더미 속 파란 보석**이었다. `b>r+25 & b>140` 마스크에 걸려 «하늘» 로 잡힌 것이다.
열 프로파일의 «마지막 하늘 행» 은 이런 **고립된 밝은 화소 하나에 통째로 끌려간다**(최댓값 통계라서).
8회차가 그 값을 그대로 떠서 폴리곤에 골을 팠고, 그래서 지면이 화면 절반까지 내려가 있었다.

**처방**: 열 프로파일에서 **>420 인 값(=보석 오염)을 382 로 치환**하고 3점 이동평균으로 계단을 없앤 뒤,
분포를 ref 의 **행별 커버리지**와 대조해 검산했다(<295 6% / <325 24% / <355 49% / <385 93% ↔ ref 14/38/57/**92**).

| 지표 | ref | r23 | **r26** |
|---|---|---|---|
| y285..385 하늘 | 37.2% | 48.3% (+11.1%p) | **42.5% (+5.2%p)** |
| y385..535 하늘 | 4.4% | 8.7% | **0.4%** |
| 하늘 0.15 교차 | y381 | y428 | **y381 (Δ0)** |
| 하늘 0.35 교차 | y334 | y364 | **y348 (Δ14)** |

> 남은 y385~535 의 ref 4.4% 는 **더미 속 파란 보석**이다 — 아트 교체분이다(측정표 §10).

### 반영 (r23 → r26)

| 요소 | ref | 수정 전 | 수정 후 | 처방 |
|---|---|---|---|---|
| 지면 윗변 | 위 표 | 골이 y517~525 까지 | 위 표 | 보석 오염값 치환 + 분포 검산 |
| 스탯 라벨 «최고 스테이지 :» | w252 **h32** | w256 **h35**(+9.4%) | **w255 h32** | font 39→36 · scaleX .98→1.055 |
| 스탯 값 «79» | **w49** h30 | **w44** h31(−10%) | **w50 h31** | scaleX .98→1.09 |
| 두 줄 행간 | **18px** | **14px** | **17px** | `.ps-st.v` top 444→445 |
| CTA 1행 «프리미엄 활성화» | 폭 기준 −8% 필요 (M +7.7% · Q +8.5% 독립 일치) | +8% | 같은 창에서 ref 291 ↔ 우리 287 | font 44→42 · scaleX .9→.873 |

**⚠ 이번 회차 CSS 5건은 전부 `#psw:not(.att)` 로 좁혔다.** 36 출석 패스가 같은 슬롯에 자기 scaleX 를
**현재 font-size 기준으로** 걸어 뒀기 때문이다(6114·6119·6127행). 밑값을 바꾸면 36 이 조용히 틀어진다.

### 미반영 — 근거를 대조해 «움직이지 않은» 것

| 지적 | 판정 |
|---|---|
| Q-2·Q-1 폭 · P «내비 아이콘 −31%/−42%» | **7회차 «조밀부» 기준 유지.** Q 는 갈색 배경 대비 th=45 로 «흩뿌림»까지 읽은 값(ref 175·148)이고 K·L 은 본체만 읽어 111~126 이었다. 기준을 회차마다 갈아타면 영원히 수렴하지 않는다. P 스스로 이 항목을 «아트 없이는 회수 불가, 감점 미반영» 으로 분류했다 |
| Q-4 «자물쇠 h +33%» | **자를 다르게 썼다** — Q 는 ref 를 «흰색>200», 우리를 «금색 마스크» 로 쟀다. 이것이 LESSONS 35-① 이 경고한 바로 그 함정이다. 같은 창·같은 임계로 스윕(110/140/170/200/230)하니 비교 가능한 구간(110~140)에서 ref 105×76 ↔ 우리 100×81 로 **Δ≤5px** 였다 |
| P-6 «헥사 숫자 r1~r3 만 +10%» | P 스스로 «r4~r6 은 46×26 ↔ 46×26 으로 일치» 라고 적었다. 같은 렌더 경로에서 상단 3행만 다를 수 없다 — ref 쪽 r1~r3 판독(41.7×24.3)이 흔들린 것으로 본다 |
| Q-3 vs P-10 리본 배지 | **두 비평가가 정반대다.** Q «날개 h +46% 과대», P «배지 h −10% 과소». 방향이 반대라 보류 — 다음 회차에서 임계값 스윕으로 가른다 |
| Q-8 «보상 아이콘 검정 외곽선 5px 없음» | ⑤ 항목이고 Q 스스로 «(⑤ 항목)» 으로 표시했다. 통과를 막지 않는다 |

`VERIFY35 PASS` · `VERIFY36 PASS` · `SMOKE PASS` · 캡처 `35-r26.png`.

<!-- 다음 세션에게: 9회차까지 돌았으므로 다음은 10회차다(상한 10 — 마지막 회차다).
     10회차는 «재채점부터» 시작한다 — 위 반영(r24~r26)은 아직 채점되지 않았다. P·Q 의 7점은 전부 수정 전 r20 기준이고,
     그들이 «통과를 막는다» 고 지목한 3건(출석 아이콘·부제·79 행간)은 전부 닫혔다.
     남은 후보: ① Q-3 vs P-10 리본 배지(방향이 반대 — 임계값 스윕으로 먼저 가를 것)
     ② 탭 라벨 폭 편차(보물상자 −8px / 시련의탑 +8px, Q-12) ③ 헤더 «무료» 라벨 폭 +7.6%(Q-11·P-9 일치). -->
