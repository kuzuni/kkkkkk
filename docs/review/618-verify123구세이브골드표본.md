# 618 — `tools/verify123.js` §10 「구 세이브 값(골드)은 그대로 = 12345」 1건 실패 (게이트 표본 오염)

- 등재: 2026-09-01, sess-0057-25538 루틴 워커 D (616 회귀 실행 곁다리 관측)
- 처리: 2026-09-01, sess-0144-13148 루틴 워커 C · 1회차
- 제품 `index.html` **0줄** · 게이트 `tools/verify123.js` 한 파일 + `tools/probe618.js` 신설

## 1회차 — 재현(338 규칙)이 등재문 1순위 용의자를 기각했다

`tools/probe618.js` 신설. 표본 세이브(시각 없음 · gold 12345)로 부팅해 100ms 간격 시계열을 찍었다:

| t | gold | totalKills | offPend | 오프라인 팝업 |
|---|---|---|---|---|
| 0~800ms | **12345** (불변) | 0 | null | 닫힘 |
| 900ms~ | **12783.286702740234** | **1** | null | 닫힘 |

- **주입→load() 구간은 결백** — load 직후 골드가 정확히 12345 다(load() 는 골드를 안 만진다).
- **오프라인 축(등재문 1순위 용의자)은 기각** — 표본에 `time` 이 없어 `load()` 가 null 을 돌려주고
  `offlineReward(null)` 은 `offPend` 자체를 안 만든다. 전 구간 offPend=null · `#offw.on` 0회.
  게다가 오프라인 보상은 **수령 클릭**(`claimOffline`) 전에는 `S.gold` 를 안 건드린다(34655).
- **실제 오염원 = 부팅 즉시 도는 자동 전투의 첫 킬 드랍**(`killEnemy` 21982 `S.gold += g`).
  스테이지 30 의 첫 몹이 ~850ms 에 죽으며 g≈438(소수)이 들어온다 — 등재문의 두 실측값
  12739.458…·12783.286… 이 정확히 «12345 + 몹 한 마리의 g»(몹 종류에 따라 다름)다.
  «실행마다 다른 소수» 의 정체는 900ms 창 끝자락에 걸리는 첫 킬의 타이밍·몹 종류다.
  최근에 빨개진 이유도 같은 축으로 설명된다 — 580(이동 속도 ×2)·541(잡몹 ×1.2 = 접촉 거리 +32%)
  이후 첫 킬이 900ms 창 안으로 들어왔다.

## 처방 — 시간을 고정한다(등재문 권장 ①), 범위 비교 반려 준수

§10 의 c2 컨텍스트에만 init script 로 rAF 타임스탬프를 0 으로 고정:

```js
const raf = window.requestAnimationFrame.bind(window);
window.requestAnimationFrame = (cb) => raf(() => cb(0));
```

제품 `loop(now)`(37871)의 `dt = (now-last)/1000` 이 0 이 되어 `step(0)` = 전투 정지, 렌더는 그대로다.
- `=== 12345` 엄격 비교는 **한 칸도 안 풀었다**(범위 비교 = 등재문 반려 사유).
- §10 의 다른 축(r60 이월 · arena 채움 · NaN/에러)은 시계 고정과 무관하게 그대로 성립(probe618 [3] 대조).
- **고정 자체를 지키는 항 신설**: «골드 표본 창에 전투 수입이 안 섞였다(킬 0)» — 누가 고정을 걷어내면
  플레이키한 골드 항보다 먼저, 뜻이 보이는 이름으로 빨개진다.
- 되돌림 방향은 probe618 [1] 이 물었다 — 고정 없는 부팅은 900ms 창에서 kills 0→1 · gold 12345→12783.28.

## 결과

- `verify123` **61/62 → 63/63**(2연속 실행 PASS — 플레이키 축 제거 확인) · `probe618` **9/9 신설** · **SMOKE PASS**
- 제품 0줄 · 레이아웃 Δ0px · 밸런스 0줄

## 교훈

- 자동 전투가 즉시 도는 게임에서 «부팅 후 n ms 대기 → 재화 상수 단언» 은 구조적으로 플레이키다 —
  대기 창이 첫 킬보다 길어지는 순간(이속·크기 밸런스 변경으로도 당겨진다) 오염된다.
  재화 표본을 묻는 절은 프레임 시계를 고정하고, 고정이 살아 있음을 묻는 항(킬 0)을 짝으로 둔다.
- «실행마다 다른 소수» 는 상수 어긋남이 아니라 시간에 물린 수입이 창에 섞인 신호다 — 등재문(618)의
  1순위 용의자(오프라인)를 시계열 한 장이 기각하고 진범(킬 드랍)을 같은 장에서 확정했다(338 규칙의 값).
