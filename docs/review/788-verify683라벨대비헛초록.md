# 788 — `verify683` [H] 가 «다른 카드» 의 라벨을 잰다 (헛초록 + 플레이키)

**세션** sess-0413-6908 (루틴 워커 A) · **등재** 2026-09-02 (sess-0113-28360 워커 A, 753 7회차 관측)
**티어** T1 «버그(게이트 헛초록 + 플레이키)» · **지시서 [3]-(가)** — 기계+자, 비평가 없음
**제품 `index.html` 0줄** · 고친 것은 `tools/verify683.js` [H] 절 한 곳이다.

---

## 1. 재현 (338 규약 — 처방을 따르기 전에 먼저 물었다)

`tools/probe788.js` 신설. 같은 페이지·같은 라운드에서 두 경로를 번갈아 잰다 —
**RND**(지금의 [H] 와 같은 코드 경로: 표본마다 `summonRelic(true)`) ↔ **FIX**(대상 칸 고정 + 실경로 보유).
알갱이는 둘 다 숨긴다(= [H1]·[H2] 와 같은 조건).

| 라운드 | RND 정착 칸 | 같은 칸 표본 | RND [H2] % | FIX [H2] % |
|---|---|---|---|---|
| 1 | rl7 | **0/8** | 100 | 100 |
| 2 | rl3 | **1/8** | **36** | 100 |
| 3 | rl4 | **0/8** | 100 | 100 |
| 4 | rl1 | **0/8** | 100 | 100 |

**등재문 ⓐⓑⓒ 가 셋 다 확인됐다.**
- ⓐ 정착 프레임과 표본 프레임이 **다른 칸**이다(같은 칸 표본 0~2/8).
- ⓑ 그래서 [H2] 백분율이 실행마다 **21 · 30 · 36 · 100%** 로 갈린다(폭 **64~79%p**).
  칸을 고정하면 같은 제품에서 폭이 **0%p** 다 ⇒ **흔들림은 «제품» 이 아니라 «자» 의 것이다.**
- ⓒ 칸을 고정하면 [H1] 문턱(≥3:1)이 **일관되게 미달**이다(2.75 · 2.78 · 2.79 · 2.81 · 2.82:1).

## 2. 뿌리가 하나가 아니었다 — 두 번째는 «상자» 다

재현이 등재문에 **한 칸을 더 얹었다.** 봉우리(t20)에서 워시를 만드는 부품을 하나씩 꺼 보면:

| t20 대비 | 값 |
|---|---|
| 전부(현행) | **2.75:1** |
| `.fx-flash` **채움 워시**만 끔 | 2.84:1 ← 거의 안 움직인다 |
| `.fx-flash` **흰 테**만 끔 | **4.75:1** |
| `.fx-flash` **바깥 글로우**만 끔 | 3.55:1 |
| 플래시 통째로 끔 | 11.45:1 |

주범이 «카드 **면**을 덮는 워시» 가 아니라 «카드 **가장자리·바깥**» 이라는 것이 상자를 의심하게 했다 —
`.rw-c>u` 는 `left:-40px;right:-40px;top:123px`(카드 151×151)이라 **상자 223×~40 의 좌우 40px 기둥과
아래 ~10px 이 카드 밖**이다. 옛 마스크는 그 상자의 휘도 상·하위 12% 를 골랐으므로 **어두운 쪽에 카드 밖
배경이 섞였고**, 플래시의 흰 테·글로우가 그 배경을 밝히면 «테↔채움» 이 아니라 «배경↔채움» 이 좁아진다.

⇒ 자를 **글리프 화소만** 고르게 바꿨다: 같은 프레임에서 **라벨 글자만 지운 사본**을 한 장 더 찍어
**차분**으로 화소 집합을 만든다(제품에게 «어디가 글자인가» 를 직접 묻는다 — [G] 가 알 잉크에 쓰는 방법과
같다). **손 상수 0개**이고 라벨 서체·색이 아트 교체로 바뀌어도 따라온다.

## 3. 고치고 나니 남은 빨강이 진짜였다

글리프 화소만 골라도 **정착 21:1 → 봉우리 2.16~2.46:1**. 상자를 좁혀도 남는다 ⇒ **제품 결함**이다.
뿌리는 **자리**다 — 라벨이 카드 하변 **밖으로** 걸쳐 있어(`top:123` + 글자 높이 ↔ 카드 151)
카드 모양 플래시의 **흰 테 9px + 바깥 글로우**가 라벨 띠를 정면으로 지난다.

⚠ 그 둘은 `.fx-flash` **공용 CSS**(09·12·17·코스튬·장비가 같이 쓴다)라 **이 행에서 안 고쳤다**
(LESSONS 666-⑨ · 788 등재문 자신의 ⚠) — **795 로 등재**했다.

## 4. 그래서 축을 셋으로 갈랐다 (문턱은 한 칸도 안 내렸다)

셋을 한 항으로 묶으면 이 자는 **영원히 빨간 게이트**가 된다(680). 그렇다고 문턱을 내리면
**334 가 금지한 «무르게 푼 수리»** 다. ⇒ **문턱은 그대로 두고 축을 갈랐다.**

| 축 | 누구 몫 | 항 |
|---|---|---|
| ⓐ **길이** — 683 5회차가 340 → 120ms 로 고친 축 | 683(닫힘) | **[H1]·[H2] 하드** |
| ⓑ **알갱이** — 링 하단 호가 라벨을 훑는다 | 683 ⏸ 보류 사유 | ⏸ 대기 항(종전 그대로) |
| ⓒ **세기** — 흰 테 + 글로우가 라벨 띠를 지난다 | **795**(공용 `.fx-flash`) | **[H4] 래칫** + ⏸ 목표 3:1 |

- **[H1]** 은 «봉투 전 구간 ≥3:1»(세기와 길이가 섞여 있던 형태)에서 **«플래시 창(`RW_FLASH_MS`) 밖
  (t ≥ 130ms)에서는 한 번도 3:1 아래로 안 내려간다»** 로 **방향을 유지한 채 축만** 갈아 끼웠다(333 처방).
- **[H2]** 는 한 글자도 안 바꿨다(정착의 80% 회복). 이제 **100% 로 안정**이다.
- **[H4]** 는 **래칫**이다 — «더 나빠지지 않는다»(≥ 2.0:1)만 지키고, **목표 3:1 미달분은 매 실행 ⏸ 로 찍는다.**
  795 가 닫으면 문턱을 3 으로 올리고 [H4] 를 [H1] 에 합치는 것이 마감이다.
- **[H3] 되돌림**도 «길이» 축으로 옮겼다 — 옛 340ms 플래시는 t≥130 에서도 **아직 살아 있어**
  [H1] 이 지키는 «창 밖은 깨끗하다» 를 정면으로 깬다(**옛 2.46:1 ↔ 지금 21:1**).
  옛 자는 t=40 **한 점**만 봐서 세기와 섞여 있었다.
- **[H0] 전제** 신설 — 대상 칸을 **실경로(`summonRelic`)로** 보유시킨다. `S.own` 을 직접 쓰면
  «세이브가 만들 수 없는 상태» 를 재게 된다(336 규약).

## 5. 결과

- `verify683` **37/38 FAIL → 40/40 PASS**, 그리고 **연속 3회 실행이 한 글자도 안 다르다**
  (정착 21:1 · t0 2.21 · t20 2.29 · t40 2.46 · t60 2.46 · t90 5.61 · t130~260 21 · 글리프 화소 998).
  종전은 같은 트리에서 [H2] 가 20~100% 로 갈렸다.
- `probe788` **8/8 신설**(재현 + 부품 갈래 + 글리프-화소 대조).
- **제품 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.**

## 6. 되돌림

- 전체 되돌림: `verify683` [H] 절에서 `RW_H_ID` 고정과 `INK` 차분 마스크를 걷고 표본마다
  `summonRelic(true)` 로 되돌리면 **옛 헛초록·플레이키가 그대로 재현된다**(`probe788` RND 경로가 그 사본이다).
- [H4] 를 걷으려면 그 한 항을 지우고 [H1] 의 판정을 `LR.worst`(봉투 전체)로 되돌린다 —
  그 순간 이 자는 **영원히 빨간 게이트**가 된다(그것이 갈라 둔 이유다).

## 7. 곁다리 등재 1건

- **795** — `.fx-flash` 의 **흰 테 9px + 바깥 글로우**가 카드 하변 밖으로 걸친 라벨을 지운다.
  89 유물 카드에서 **정착 21:1 → 봉우리 2.16~2.46:1**(목표 3:1). 공용 CSS(09·12·17·코스튬·장비)라
  이 행에서 안 고쳤다. 갈래·실측은 `probe788` [6] 에 그대로 있다.
