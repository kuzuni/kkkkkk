# 813 비평 브리핑 (5회차) — 89 유물 소환 팝업 · 프레임 5종

> 1~4회차 브리핑과 **같은 규칙·같은 질문**이다(점수를 회차 간에 비교할 수 있어야 한다).
> 캡처만 5회차 것으로 바꿨고, 4회차의 **Q5(A/B 판정)는 뺐다** — 두 채점자가 만장일치로 A 를 골라 닫힌 갈래다.

## 무엇을 보는가

레퍼런스 한 장과 **같은 화면의 프레임 5종 캡처**를 나란히 놓고 채점한다.
이 화면은 세로 길이가 다른 기기에서 **패널 높이만 달라지고 부품 크기는 고정**이다.
따라서 이번 채점의 핵심은 «늘어난 세로를 어디에 나눠 줬는가»(여백 배분)다.

## 파일

- 레퍼런스: `docs/ref/89-유물-팝업.png` — **486×687 크롭**. 화면 전체가 아니라 **팝업 패널만** 잘린 그림이다.
  패널 4변에 금색 장식 테두리가 있고 그 바깥은 없다. 환산 배율 **×2.2222**(= 1080/486) ⇒ 패널 환산 **1080×1527**.
- 캡처 5장(전부 폭 1080):
  - `docs/shots/754-813r5-89-1600.png` (프레임 높이 1600 — 9:13.3 및 그보다 넓은 기기 전부)
  - `docs/shots/754-813r5-89-1841.png`
  - `docs/shots/754-813r5-89-1920.png` (9:16)
  - `docs/shots/754-813r5-89-2280.png` — **기준 프레임(9:19)**
  - `docs/shots/754-813r5-89-2600.png`
  - 5장을 한 장에 붙인 스트립: `docs/shots/754-813r5-89-strip.png`

## 비교 규칙 (반드시 지킬 것)

1. **캡처는 화면 전체**이고 레퍼런스는 **패널만**이다. 캡처 안에서 금색 테두리로 둘러싸인 팝업 패널을 먼저 찾고,
   그 패널의 좌상단을 원점으로 삼아 레퍼런스와 비교하라. 패널 밖(뒤 배경·HUD)은 채점 대상이 아니다.
2. 가로는 1:1(둘 다 폭 1080 환산)이다. 세로는 **패널 높이가 프레임마다 다르다** —
   ⇒ **세로 절대 좌표로 «몇 px 어긋남» 을 재지 마라.** 세로는 «부품과 부품 사이 간격», «그 간격이 프레임마다 몇 배로
   변하는가», «부품 크기 대비 여백의 비» 로만 재라.
3. **색·아트 내용·서체 모양은 감점 사유가 아니다.** 아트는 나중에 교체된다.
   단 **크기·bbox·비율에 관한 것**(아이콘 상자 w×h, 글자 잉크 높이·폭, 선 두께)은 ②·⑥ 축이므로 지적하라.
4. 지적은 **수치로만**. «몇 px», «몇 %», «몇 배». 두루뭉술한 총평 금지. **후하게 주지 마라.**

### ⚠ 잴 자리의 정의 (3회차에 두 채점자의 값이 갈린 자리 — 못박는다)

- **수반 하변** = 돌 수반(소환 버튼) 아래 **받침 밑판 외곽선의 최하단**이다(사발 바닥이 아니다).
- **들보(상인방) 상변** = 들보 띠의 **최상단 규칙선**이다(그 위의 장식 그림자·글로우가 아니다).
- **격자 하변** = 유물 슬롯 묶음(3·4·3)의 마지막 줄 **슬롯 테두리 하단**이다.
- **안내문** = 패널 맨 아래 크림/탠골드 2줄 글자. 잴 때는 **잉크 bbox**(칠해지는 자리)를 쓰고,
  «상자» 를 쓸 거면 그렇다고 밝혀라(3·4회차에 두 채점자의 값이 이것 때문에 최대 7px 갈렸다).

## 채점 (각 10점 · 최저 항목 점수가 당신의 점수)

① 요소 배치 · ② 상대 크기 비율 · ③ 여백·간격 · ④ 정렬·그리드
(⑤ 색·명암 · ⑥ 폰트 크기 비율도 채점해 기록은 남기되 통과를 막지 않는다)

## 답에 반드시 포함할 것

1. **①~⑥ 점수와 최저 항목**
2. **감점 사유 목록 — 심한 순으로. 각 항목에 실측 수치와 «어느 프레임에서»**
3. 아래 자리에 대한 **당신의 독립 판정**(유도하지 않는다 — 결함이 아니면 «결함 아님» 이라고 답하라):
   - **Q1** 유물 격자 하변 ↔ 돌 수반 상변 사이 간격. 프레임마다 재고, 그 변화가 결함인지 판정하라.
     결함이라면 «늘어난 세로가 대신 어디로 가야 하는가» 를 구체적으로(어느 부품 사이) 적고
     **다음 회차가 쓸 비를 숫자로 처방하라**(예: «패널 상변→격자 상변 : 격자 하변→수반 상변 = n : m»).
   - **Q2** 들보(상인방) 아래 **×1/×10/×100/×1000 배수 토글 바**의 자리.
     들보 하변↔바 상변, 바 하변↔격자 상변 두 간격을 프레임마다 재고, 어느 배분이 옳은지 판정하라.
   - **Q3** 안내문 2줄의 **아래 여백**(안내문 ↔ 패널 하단 금색 테두리)을 프레임마다 재고,
     안내문 **위** 여백(수반 하변 ↔ 안내문)과 비교해 역전·불균형이 있는지 판정하라.
     레퍼런스의 같은 두 값도 재서 같이 적어라.
   - **Q4** 들보 **위**의 띠(패널 상단 금색 테두리 ↔ 들보 상변)를 프레임마다 재라.
     **«죽은 검은 여백» 인지, 벽의 결이 이어지는 면인지** 판정하고,
     레퍼런스의 같은 자리가 패널 높이의 몇 %인지 재서 같이 적어라.
4. **당신이 1순위로 고치라고 하는 것 한 가지**와 그 처방(어느 값을 얼마로).

## 이 회차에 바뀐 것 (사후 확인용 — 채점에 쓰지 말고, 당신 실측이 이것과 어긋나면 그 사실을 적어라)

패널 하단 «안내문 블록»(수반↓안내문 + 안내문↓패널하변)의 **총량을 90.6px → 36px 으로 줄이고**,
회수한 세로를 «패널 상변→격자 상변»(+33.8px)과 «격자 하변→수반 상변»(+28.8px)으로 나눠 줬다.
그 밖의 부품 크기·가로 좌표는 안 건드렸다.
