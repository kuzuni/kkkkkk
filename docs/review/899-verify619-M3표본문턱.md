# 899 — `verify619` [M3] 의 «빈 프레임 0/17» 은 **축이 아니라 표본**이 빨간 것이었다

> T1 «버그(자 · 환경 의존 문턱 · 574·709·825·855·857·869·870~872·876 계열)» · 지시서 [3]-**(가)**
> (자 문턱은 레퍼런스 대조가 아니다 — **비평가 없음**) + 되돌림 시험
> 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄 · 판정 0줄
> 착수 세션 sess-2258-27446(루틴 워커 D · 2026-09-04 · 1회차)

## 1. 재현이 먼저 — `probe899` (338 규칙)

등재문이 남긴 갈래는 둘이고 **처방이 정반대**다.

| 갈래 | 주장 | 고칠 곳 |
|---|---|---|
| ⓐ | 자가 «축(빈 프레임)» 과 «표본(프레임 수)» 을 **한 항에 묶었고**, 표본 쪽이 러너 속도다 | `tools/verify619.js` |
| ⓑ | 제품이 실제로 끊겨 빈 프레임이 늘었다 | `index.html` |

⚑ **재현을 러너의 기분에 맡기지 않았다**(785 처방) — 이 컨테이너는 지금 한가해서 그냥 돌리면
«오늘은 초록» 이 나온다(실측 33.6프레임/초). ⇒ CDP `Emulation.setCPUThrottlingRate` 로 **느린
기계를 만들어** A/B 를 갈랐다.

| 항 | 물음 | 실측 |
|---|---|---|
| [0-b] | 옛 항의 통과 조건은 무엇을 요구하나 | `G.n > 20` = «1100ms 에 21프레임» = **19.1프레임/초 이상** |
| [0-c] | 무부하에서 축은 | **빈 프레임 0/38** (초록) |
| [1-a] | 부하를 걸면 옛 자가 빨개지나 | **×4 스로틀 → 8프레임** (< 21) ⇒ FAIL |
| [2-a] | 그 사본에서 축은 | **빈 프레임 0/8** — 축은 멀쩡하다 ⇒ **ⓑ 기각** |
| [2-b] | 그래서 실패 문구가 | «빈 프레임 0/8» — **빨강이 초록으로 읽힌다**(등재문의 본체 · 653 의 거울) |
| [3] | 표본 구동 홀드는 같은 부하에서 | 훈련·룬·단련 **20/20 프레임 · 빈 프레임 0**(2.7~3.0초 눌렀다) |
| [4-a] | 발화·홀드 링을 죽인 사본에서 새 축은 | **빈 프레임 26/42** (짖는다) |
| [4-b] | 원복하면 | **0/38** (초록) |

등재문의 실측(훈련 0/17 · 룬 0/15 · 단련 0/16)은 ×4 스로틀의 8프레임과 **같은 얼굴**이다 —
그 기계가 1100ms 에 15~17프레임밖에 못 준 것이지 연출이 끊긴 것이 아니다.

## 2. 처방 — 문턱(20 · 빈 프레임 0)은 그대로 두고 **누르는 법과 항 구성**을 갈랐다

바뀐 파일은 **셋 다 `tools/`** 다(`index.html` 0줄).

* **신설 `tools/frameburst.js`** — 785 `holdburst`(node 쪽 폴링)의 **프레임 판**.
  버튼을 누른 채 rAF 프레임마다 «보이는 fx · 홀드 링» 을 세고, **표본이 찰 때까지** 누른다.
  1100ms 는 이제 «바닥»(빠른 기계에서 종전과 같은 시간)이고 30초는 «상한»(판정 문턱이 아니다).
  ⚑ **기다림의 끝은 절대 초가 아니라 «비» 다**(857 ③ · 869) — 마지막 프레임 뒤
  «평균 프레임 간격 × 6»(바닥 3초) 동안 프레임이 안 오면 «정체» 로 끊고, 그 사실을 **표본 항**이 말한다.
* **`verify619` [M3] 을 두 항으로 갈랐다** — `M3s`(표본이 있다: 프레임 ≥ 20 · 정체 아님) ·
  `M3`(축: 빈 프레임 0). 한 항이면 899 가 등재한 그 병(«빨강인데 문구는 초록»)이 그대로 돌아온다.
* **문턱은 한 칸도 안 건드렸다**(334 규약). 문턱 20 을 12 로 내리는 길은 **무른 수리**다 —
  «끊김을 볼 눈» 이 그만큼 성겨져 축 자체가 물러진다.

### ⚑ 축도 좁혔다 — 되돌림 시험이 먼저 기각한 것

처음 짠 되돌림(발화 `upFx` + 홀드 링 `fxHoldMark` 를 **둘 다** 죽인 사본)이 **초록**이었다.
이유를 프로브가 이름으로 찍었다 — `#fxl` 은 화면 전체가 같이 쓰는 층이라 돌아가는 게임의
`fx-toast`·HUD 재화 알갱이가 **늘 몇 장 떠 있고**, 옛 축은 그것까지 «보이는 fx» 로 셌다.
즉 **홀드 연출이 통째로 사라져도 초록인 축**이었다(헛초록).

⇒ 660 이 «스폰 위치는 **강화 버튼뿐**» 이라고 못 박았으므로 **호스트 상자 ±160px** 과 겹치는
노드만 이 홀드의 발화로 센다. 좁힌 뒤 되돌림이 **26/42 로 빨개졌고**, 건강한 트리는
훈련 0/36 · 룬 0/44 · 단련 0/43 으로 **그대로 초록**이다(층 전체 기준으로도 0 — 좁힌 것이
초록을 산 게 아니다). 진단 문구에 «층 전체 기준이면 n» 을 같이 찍어 두 자의 값을 나란히 남긴다.

## 3. 결과

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `verify619` | 65/65 (이 기계) · **62/65**(느린 기계 — 등재문) | **71/71** · ×4 스로틀에서도 축 초록(`probe899` [3]) |
| `probe899` | — | **15/15 신설** |
| `smoke` | — | **SMOKE PASS** |

* 늘어난 6항 = `M3s` ×3(표본) + `R-M3s`·`R-M3`·`R-M3b`(§R-M3 되돌림 신설).
* 제품 `index.html` **0줄** — 등재문이 «수리 전 트리에서도 같은 3건» 으로 894 와 무관함을 이미
  못박아 두었고, `probe899` [2] 가 «축은 멀쩡하다» 로 제품 갈래(ⓑ)를 다시 기각했다.

## 4. 곁다리 — 같은 병의 다른 입구 (857 ④)

「한 항에서 찾은 병은 그 자의 **모든 입구**에서 세어 보라」. `verify619` 안의 홀드 입구는 넷이다 —
[B]·[K]·[L] 은 이미 785 `holdUntil`(표본 구동)이거나 **비율**로 판정하고(K3 «프레임의 2/3 이상»),
절대 프레임 수에 매인 곳은 [M3] 하나뿐이었다.

⚠ 다만 **[E1] 에 같은 병의 씨앗**이 있다 — «120ms × 12회 고정 창에서 스파크 ≥ 20개». 이번 회차에
무부하(58개)와 **배경 부하 6프로세스**(58개) 둘 다 초록이라 **지금 빨간 자리가 아니고, 등재하지
않았다**(869 §5 와 같은 판단 · 지시서 [1] «작업 단위 하나»). 부하로 이 러너의 렌더 프레임률은
33→32.8프레임/초로만 내려간다 — 이 환경에서 «느린 러너» 를 만드는 확실한 손잡이는 배경 부하가
아니라 **CDP 스로틀**이다(그래서 프로브가 그것을 쓴다).
