# 790 — `tools/verify180.js` [G] 「구 세이브 gold·stage 불변 = 12345」 1건 실패 (게이트 표본 오염)

- 등재: 2026-09-02, sess-0243-25204 루틴 워커 C (786 회귀 스윕의 곁다리 관측 · 등재만)
- 처리: 2026-09-02, sess-0312-28117 루틴 워커 A · 1회차
- 지시서 [3]-**(가)** — 기계+자 작업, 비평가 없음
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄 · 고친 것은 `tools/verify180.js` 한 파일 + `tools/probe790.js` 신설

## 1회차 — 재현(338 규칙)이 등재문을 그대로 확인했고, 자매 관계까지 값으로 못박았다

착수 즉시 재현: `VERIFY180 42/43` · `구 세이브 gold·stage 불변 = [12355.73710901912, 7] — 기대 [12345, 7]`.
등재문의 숫자와 자리가 한 글자도 다르지 않다.

`tools/probe790.js` 신설. 표본(verify180 [G] 가 쓰는 그 세이브 — `gold 12345 · stage 7 · 시각 없음`)으로
부팅해 100ms 간격 시계열을 찍었다:

| t | gold | totalKills | offPend | 오프라인 팝업 |
|---|---|---|---|---|
| 0~800ms | **12345** (불변) | 0 | null | 닫힘 |
| 900ms~ | **12354.663398117209** | **1** | null | 닫힘 |

- **주입→`load()` 구간은 결백** — load 직후 골드가 정확히 12345 다.
- **오프라인 축은 기각** — 표본에 `time` 이 없어 `offPend` 자체가 안 생긴다(전 구간 null · `#offw.on` 0회).
- **오염원 = 부팅 즉시 도는 자동 전투의 첫 킬 드랍**. `12355.737 − 12345 = 10.737` 이 스테이지 7 몹 한 마리의 `g` 다.

⚑ **왜 «실행마다» 인지가 이 시계열 한 장에 있다** — 첫 킬이 **정확히 900ms 창의 끝자락**에 걸린다
(t≈800ms 에서 kills 0, t≈900ms 에서 kills 1). 게이트의 대기창과 첫 킬 시각이 같은 자리라
러너가 조금만 빠르거나 느리면 들락날락한다. 등재문의 «게이트 표본 오염» 진단이 옳다.

⚑ **618 의 자매라는 것도 값으로 확인됐다** — 623 이 아니라 **스테이지가 다를 뿐 같은 병**이다:
123 은 stage 30 이라 드랍이 ~438(12345 → 12783.28), 180 은 stage 7 이라 ~10(12345 → 12354.66).
그래서 등재문이 지목한 대로 **618 의 처방을 그대로 옮기는 것**이 답이었다.

## 처방 — 618 과 같은 시계 고정, 단 «이 절에만» 켠다

`open()` 에 세 번째 인자 `freezeClock` 을 신설하고 [G] 에서만 `true` 로 부른다:

```js
const raf = window.requestAnimationFrame.bind(window);
window.requestAnimationFrame = (cb) => raf(() => cb(0));
```

제품 `loop(now)` 의 `dt = (now-last)/1000` 이 0 이 되어 `step(0)` = 전투 정지, 렌더는 그대로다.

⚑ **123 과 달리 여기서는 «고정이 지급까지 멈추지 않는가» 가 고유한 위험이었다** — 180 [G] 는 부팅 중에
월별 우편 1통이 실제로 **지급되는** 절이라(`dailyCheck` 경로), 시계를 멈춰 그 지급이 같이 멈추면
수리가 아니라 새 결함이다. `probe790` [3] 이 그 자리를 대조로 답한다 — 고정 부팅에서도
**우편 1통 · 달 열쇠 `2026-09` · dia 1000** 이 전부 그대로다. 그래서 `open()` 의 **기본값은 끔**으로 뒀다:
[B]~[F]·[H] 는 전투가 도는 채로 물어야 하는 절이라 한 자리도 같이 얼리지 않았다.

- `[12345, 7]` **엄격 비교는 한 칸도 안 풀었다** — 등재문이 «`≈ 12345` 로 넓히는 길은 반려» 라고 못박았고,
  618 도 그 길을 안 골랐다. 넓히면 «구 세이브 값이 그대로다» 라는 단언이 뜻을 잃는다.
- **고정 자체를 지키는 항 신설** — 「골드 표본 창에 전투 수입이 안 섞였다(시계 고정 = 킬 0 — 790)」.
  누가 고정을 걷어내면 플레이키한 골드 항보다 **먼저**, 뜻이 보이는 이름으로 빨개진다.
  되돌림 방향은 `probe790` [1] 이 물어 둔다(고정 없는 부팅 = 900ms 창에서 kills 0→1 · gold 12345→12354.66).

## 결과

- `verify180` **42/43 → 44/44** · **3연속 실행 PASS**(플레이키 축 제거 확인)
- `probe790` **13/13 신설**
- 회귀 `verify123` **63/63**(618 의 원래 자리 — 같은 처방을 쓰는 형제) · `probe618` **9/9**
- **SMOKE PASS**
- 제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스 0줄

## 교훈

- 618 의 교훈이 **한 번 더 통했다는 것이 이 행의 값**이다 — 자동 전투가 즉시 도는 게임에서
  «부팅 후 n ms 대기 → 재화 상수 단언» 은 자리마다 구조적으로 플레이키하고, 스테이지가 낮아
  드랍이 작으면 **오래 안 걸리다가** 러너가 빨라지는 날 갑자기 빨개진다(180 이 그랬다).
  같은 꼴을 쓰는 자가 또 있으면 미리 같은 처방을 옮겨 둘 것.
- **공용 헬퍼에 고정을 넣을 때는 기본값을 끔으로 두고 부르는 자리에서 켠다** — `open()` 을 통째로 얼렸으면
  전투가 돌아야 성립하는 절([B]~[F]·[H])이 조용히 뜻을 잃었을 것이다. 이 자의 [F] 는 FXHOLD 1800ms 를
  일부러 넘겨 기다리는 절이라 특히 그렇다.
- **«시계를 멈춰도 멈추면 안 되는 것»** 을 프로브가 먼저 물어야 한다 — 123 에는 없고 180 에는 있던
  축(부팅 중 우편 지급)이 그것이다. 처방을 옮길 때 옮겨지지 않는 전제가 어디인지부터 찾는다.
