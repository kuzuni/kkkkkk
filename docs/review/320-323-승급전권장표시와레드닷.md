# 320 · 323 — 승급전 «권장 스테이지» 한 줄 + [승급전 시작]·[승급전 도전] 버튼 레드닷 (저장소 주인 지시 2026-08-28)

> 워커 B · sess-1236-12003 · 2026-08-28 · **T2 주인 지시 · 1회차**
> 지시서 [3]-**(가)** 기계적/기능 검증 — «상태 → DOM» 판정이라 **비평가를 띄우지 않았다.**
> (캡처 2장은 «잘림·겹침 0건» 확인용으로만 서브에이전트에게 읽혔다. 채점 루프가 아니다.)
>
> 주인 원문
> · 320 — «승급전에 걍 권장 스테이지까지만 해주고 그래라. 최고스테이지 전투력 이거는 별로임»
> · 323 — «승급전도 권장 스테이지 도달할시에 승급전 시작에 빨간점 알림»
>
> **두 지시가 같은 팝업(`openPromo()`)이라 PROGRESS 323 행의 권고대로 한 세션에서 같이 처리했다.**

---

## 1. 착수 전 실측 — 무엇이 있었나

| 재는 것 | 실측 |
|---|---|
| 조건 상자 `.pr-cond` | `<p>` **2개** — «최고 스테이지 `S.best` / `r.stage`»(okS 색분기) · «전투력 `fmtB(cp())` / `fmtB(r.cp)`»(okC 색분기) |
| 색분기 | `okS`/`okC` 각각 `#8ff0a8`(충족) / `#ff6b7f`(미달) **인라인** |
| 판정 함수 | `promoReady()` = `S.best >= r.stage && cp() >= r.cp` (index.html ~17344) — **295 가 «권장 기준» 으로 남겨 둔 것** |
| 그 판정의 표시처 | `sideAlert('promo', promoReady())` — **좌측 사이드 `promo` 아이콘 하나뿐** |
| 대상 버튼 2곳 | 팝업 `#pgo` · 「내 정보」 탭 `#promoBtn` — 둘 다 `.ifbtn.pbtn`, **배지 노드 0개** |

⇒ 320 은 «대비 표기가 거슬린다»(주인 스크린샷: 246/45 처럼 한참 넘긴 값이 나란히 선다),
323 은 **318(출석)·321·322 와 같은 «경로가 한 칸 짧다»** 다 — 판정은 이미 있는데 신호가 사이드
아이콘까지만 가고, 팝업 딤(`#modal` z30)이 사이드(`.side` z3)를 덮는 순간 화면에서 사라진다.

## 2. 고친 것 — 제품 코드 4곳 · 순증 함수 0개

판정식을 **한 줄도 새로 만들지 않았다.** 320 은 표기만, 323 은 이미 있는 `promoReady()` 를 그대로 읽는다.

| # | 자리 | 변경 |
|---|---|---|
| ① | `openPromo()` 조건 상자 | 두 `<p>` → **`<p>권장 스테이지 <b>r.stage</b></p>` 한 줄**. 지역값 `okS`/`okC` 는 이 표기 전용이라 같이 삭제 |
| ② | CSS `.mbody .pr179 .pr-cond p b` | `color:#EADCC6` 신설 — 인라인 색이 사라지면 `.mbody p b{color:#B4571E}`(갈색)가 **남색 상자(#0e1428) 위**에 깔려 179 가 고쳤던 «있는데 안 보인다» 가 재발한다 |
| ③ | `openPromo()` `#pgo` | `pready = promoReady()` 일 때 호스트에 `.alert` + 공용 **`<s class="updot">`** 1개 |
| ④ | `renderSt()` `#promoBtn` | 같은 조건으로 `.alert` + `<s class="updot">` |
| ⑤ | CSS `.ifbtn.pbtn>.updot` | `right:24px;top:24px` 신설 (아래 §3) |

**색분기를 되살리지 않은 이유**: 295 로 입장 제한이 폐지돼 «미달 = 빨강» 은 **거짓 신호**이고,
«충족» 신호는 323 이 버튼 레드닷으로 준다. 한 상태를 두 곳에서 다르게 말하지 않도록 **신호를 한 곳으로 모았다.**

## 3. 배지 자리 — 299 «우상단 사분면» 계산과 실측

`.ifbtn` 은 절대배치 부품이지만 `.ifbtn.pbtn` 은 `position:relative`(index.html 1708) 라 기준상자가 된다.
`overflow` 는 없고 라벨 `<b>` 는 가운데 한 줄이라 **안쪽 우상단이 통째로 비어 있다.**

| 값 | 계산 | 실측(캡처) |
|---|---|---|
| 오프셋 기준 | 절대배치는 padding box 기준 → `right/top:24` = 검정 테두리 7 + 24 | – |
| 닷 27 + 바깥 링 7.5 | 버튼 안쪽 여백 **16.5px** — 베벨 림을 안 밟는다 | 링 포함 우단에서 24px 안쪽 |
| 중심 (우/상) | **44.5 / 44.5** · 버튼 높이 131 의 절반 65 위 | 우 43 · 하 36 (버튼 우상단 모서리 기준) |
| 299 판정 | cx 784.4 / 829 · cy 45.0 / 131 → **우상단 사분면 충족** | `verify299` · `verify320` 둘 다 초록 |
| 라벨 충돌 | – | 흰 글자 우단 555 ↔ 닷 좌단 805 = **250px 여백**, 접촉 0 |

## 4. 기능 체크 표 (ROUTINE «기능 완성 규칙»)

전부 **헤드리스 실동작**(`tools/verify320.js`). «논리(class·computed display)» 와 «화소(빨강 수)» 를 **같이** 본다
(292 «열렸는가 ≠ 보이는가» · 189-③ «헛초록» 처방).

| 상태 | 조건 상자 | `#pgo` | `#promoBtn` |
|---|---|---|---|
| 권장 미달(계급 1~7 전부) | «권장 스테이지 n» **1줄** · «최고 스테이지»·«전투력» **0건** | `.alert` 없음 · 닷 노드 **0개** | 닷 **0개** |
| 권장 충족(계급 1~7 전부) | 같은 한 줄 그대로 | `.alert` + 닷 **1개** · 빨강 화소 **>100** | 닷 **1개** · 빨강 화소 **>100** |
| `.alert` 를 손으로 뗌 | – | computed display `none` → 붙이면 `block` | – |
| **승급 성공 직후**(`endPromo(true)`) | – | **닷 0개**(다음 계급 기준 미달) | **닷 0개** |
| 남색 상자 잉크 | 밝은 화소 1326/4214 — 179 결함 재발 없음 | – | – |

캡처 2장(`docs/review/320-promo-{ready,short}.png`, 918×1283) 독립 판독 결과:
남색 상자 안 글자 밴드는 **y353~391 한 줄뿐**, «최고 스테이지»·«전투력»·«n / n» **0건**,
두 장의 유일한 유의미 차이는 **버튼 배지 유무**. 잘림·오버플로·요소 충돌 0건.

## 5. 게이트

| 게이트 | 결과 |
|---|---|
| **`tools/verify320.js` 신설** | **102/102 PASS** — 320 표기(계급 7단 전부·되돌림 감시) + 323 점등/소등/사분면/화소/음성 |
| `tools/verify299.js` | SITES 18종 → **20종 · 21/21 PASS** (`#pgo` · `#promoBtn` 추가) |
| `verify267` | **66/66 PASS** — `spec()` D-승급전·D-내 정보 규격(`.ifbtn.pbtn` · 라벨 `<b>` · 잉크 흰색 + 검정 스트로크 · 높이·radius·테두리·fs) 불변, B7 «팝업 버튼 1개» 불변 |
| `verify295` | **25/25 PASS** — 버튼 2줄에 조건부 `disabled` 없음 · `sideAlert('promo', promoReady())` 불변 |
| `verify179` | **135/135 PASS** — 조건 상자가 한 줄이 돼도 본문 fs 48·줄 상자 81.6 · `.mbox` ≤ 1497 · 겹침/잘림 0 |
| `verify182` · `verify149`(승급 미리보기) · `verify208` · `verify284` · `verify318` | **92/92 · 39/39 · 59/59 · 21/21 PASS** |
| `tools/smoke.js` | **SMOKE PASS** (화면비 6종) · 콘솔 에러 0 |

### 게이트 하네스 메모 — «강화 레벨로는 상위 계급 전투력을 못 만든다»

`verify320` 첫 실행에서 계급 5~7 의 «충족» 상태를 못 만들었다. 원인은 제품 결함이 아니라
**`lv()` 가 `S.lv[id] | 0`(32비트)** 라 레벨이 2^31 에서 잘리고 `cp()` 가 **≈3.5e12 에서 멈추는데**
마스터~챌린저 기준은 **8e14 ~ 2e21** 이라는 것이다(cp 는 레벨당 ≈1639 로 거의 선형).
그 계급에서는 **기준치 쪽을 잠깐 0 으로 내려** 충족 상태를 만들고 회차마다 원복한다
(`window.__CP0` 스냅숏). 320·323 이 지키는 것은 «판정 결과 → 표시» 이지 «전투력 곡선» 이 아니다.
완화가 걸린 회차는 로그에 «기준 완화(32비트 레벨 상한)» 로 찍힌다 — **조용히 넘어가지 않는다.**

## 6. 범위 밖 · 곁가지

- **`promoReady()` · `canPromote()` · `sideAlert('promo', …)` 는 한 줄도 안 건드렸다.** 320 은 표시만이다.
- 「내 정보」 탭의 «다음 계급 — 스테이지 n · 전투력 n» 안내 줄(renderSt, ~22993)은 **대상이 아니다**.
  주인 지시는 «승급전 팝업» 의 대비 두 줄이고, 그 줄은 대비가 아니라 **다음 계급 소개**다(현재값을 안 적는다).
- ⚠ **곁가지 — `tools/verify149.js` 게이트 부패 1건(내 구간 아님).** `§1 토스트 — 승급 조건 미달` ·
  `§2 승급 조건 미달` 두 케이스가 **295 가 폐기한** `startPromo()` 의 «승급 조건 미달» 반려를 아직 찾는다
  (`tools/verify149.js` 62 · 162 · 244행). 착수 전 기준선(`git stash`)에서도 **272/278 로 똑같이 실패**해
  내 변경과 무관함을 가름했다 → PROGRESS 비고에 등재만 하고 손대지 않았다(185-② «부패는 그 자리에서 등재»).
