# 838 — 요소 대상 버스트의 사거리: 「터진다」 가 아니라 「제자리에서 커졌다 꺼진다」

> 등재: 2026-09-02 (sess-2013-14138 워커 A · 681 9회차 비평 2인 CV·CW 가 **각각 1순위**로, 5회차부터
> 여섯 회차 내내 같은 자리). 티어 T4 «폴리시(스폰 사거리 · 619 클램프)».
> 지시서 [3]-**(다)** 연출 작업 — 연속 프레임 캡처 + 비평 2인, 최대 5회.

## §1 재현 — 등재문의 세 얼굴을 «찍힌 값» 으로 (338 규칙)

`tools/probe838.js` 신설(자는 `tools/travel838.js` 한 벌 — `verify838` 과 공용 · 402 «두 벌 금지»).
씬 둘을 **실제 사용자 경로**로 굴려(시드 20260902 고정) 캡처 격자(0·20·45·70·110·175·250·320ms)에서
알마다 «어디서 태어나 어디로 가는가» 를 잰다.

| 축 | 씬 A(요소 대상 · 훈련 강화 버튼 310×106) | 씬 B(점 대상 · 유물 소환 — 대조군) |
|---|---|---|
| 사거리(총 이동 ÷ 최대 지름) | **0.41 몸길이** (18.5px ÷ 46.06px · 최소 알 0.21) | **4.8 몸길이** (228.5px ÷ 48.07px) |
| 출생 반경 → 끝 반경 | 77.4 → 95.9px = **×1.24** | 66.7 → 285.4px = **×4.28** |
| 이웃 장 IoU(최대) | **0.78** (45→70ms) | 0.27 |

**PROBE838 5/5 PASS — 등재문이 이 트리에서 그대로 재현된다.** 등재문(CV)의 0.59 몸길이·IoU 0.744~0.775 ·
반경 +15~34% 와 같은 자리이고, 이 자가 더 나쁘게 읽는 이유는 **자의 눈금이 다르기 때문**이다 —
CV·CW 는 PNG 의 **잉크**를 쟀고 이 자는 **상자**를 잰다(LESSONS 681-⑥). 그래서 절대값은 갈리되
**갈리는 배수**는 같은 편이다: 등재문 ×16, 이 자 ×11.7.

⚑ **뿌리는 등재문이 지목한 그 자리다** — `fxBurst` 의 요소 대상 이동은 41회차가
«중심 → 끝점 거리 × `FXB_K`(0.20)» 로 적었고, 그 거리는 **28회차의 가둠 상자**가 정한다.
훈련 강화 버튼에서 상자는 반폭 128 · **반높이 26** 이라 이동이 6~26px 이고, 알 지름이 46px 이니
한 알이 제 몸길이의 절반도 못 간다. 곡선(681)이 아홉 회차를 돌아도 한 픽셀도 안 움직인 이유다.

## §2 처방 — 「사거리를 상자에 맞춰 접지 않는다」

등재문이 연 갈래(«상자를 넓힌다» 가 **아니라** «비율을 다른 데서 뽑는다»)를 그대로 따랐다.

**제품 `index.html` 2자리** (상수 선언 1 · 후보 루프 1):

1. **당김 비율을 상자가 아니라 «제 몸길이» 에서 역산한다.**
   `kw = clamp(FXB_BODY × sz ÷ |중심→끝점|, FXB_K, FXB_KMAX)` — 알 하나가 `FXB_BODY`(1.35) 몸길이를
   가려면 당김이 얼마여야 하는지 풀고, 상자가 그만큼을 못 내주면 `FXB_KMAX`(0.55)까지만 받는다.
2. **사다리(`FXB_KLAD` 3칸)** — 큰 칸부터 넣어 보고 keep-out(816·818 신고 잉크)·융합(619 12회차)에
   걸리면 한 칸씩 내린다. **마지막 칸이 종전 값 `FXB_K`** 라, 걸리는 자리에서는 종전과 한 픽셀도 안 다르다.

⚑ **스필은 구조적으로 0 그대로다** — 바뀌는 것은 **시작점뿐**이고 그 점은 중심과 끝점 **사이**에 있다.
끝점·가둠 상자(`inM`)·링 반경(rx·ry)·알 크기(sz)·수명·개수·이징은 **한 값도 안 건드렸다**
(28·21·13·14·41·42회차의 규칙이 전부 그대로 성립한다). `verify838` [B1]·[B2] 가 그것을 직접 잰다.

⚑ **큰 호스트는 한 값도 안 바뀐다.** `kw` 는 `|중심→끝점|` 이 크면 `FXB_K` 로 떨어진다 —
훈련 **카드**(326×510)·우편 행(826px)·09·12·17 처럼 넓은 자리는 이미 20% 가 1.35 몸길이보다 크다.
이 수리가 실제로 여는 것은 **버튼처럼 좁은 호스트**뿐이고, 그게 등재문이 지목한 자리다.

### 왜 `FXB_KMAX` 가 0.55 인가 — 더 열면 **되레 짧아진다**

| `FXB_KMAX` | 씬 A 사거리 | 최소 알 | 출생 반경 |
|---|---|---|---|
| 0.20(수리 전) | 0.41 몸길이 | 0.21 | ×1.24 |
| **0.55(채택)** | **0.83 몸길이** | **0.43** | **×1.53** |
| 0.72 | 0.77 몸길이 | 0.21 | ×1.57 |

당김을 더 열면 알이 중심 쪽에 몰려 **융합(619 12회차 `FXB_SEP`)에 걸리고**, 사다리가 바닥칸까지
내려가 그 알은 종전 값으로 돌아간다(최소 알 0.21 = 수리 전 그대로). **막는 것은 상자가 아니라 융합**이다.
사다리 칸을 5로 늘려도 0.84 로 0.01 밖에 안 오르고 후보 평가가 45회/알이 되므로(성능 — `verify619` [M3])
**3칸으로 둔다.**

## §3 게이트

`tools/verify838.js` 신설 — **11/11 PASS**

| 절 | 항 | 값 |
|---|---|---|
| [A] 사거리 | A1 ≥ 0.72 몸길이 · A2 최소 알 ≥ 0.30 · A3 총 이동 ≥ 30px | 0.83 · 0.43 · 37.75px |
| [B] 스필 | B1 알 잉크가 호스트 상자 밖 0 · B2 여유가 남아 있다 | −7.25px(안쪽) |
| [C] 출생 반경 | C1 끝 ÷ t=0 ≥ 1.45 | ×1.53 |
| [D] 대조군 | D1 점 대상은 종전대로 · D2 콘솔 에러 0 | 4.43 몸길이 · 0 |
| [R] 되돌림 | R1 `FXB_KMAX = FXB_K` 사본에서 사거리가 다시 짧다 · R2 출생 반경도 종전 | **0.44 몸길이 · ×1.24** |

⚠ [R] 의 사본은 임시 파일이고 **크래시에도 지운다**(810 — «남은 표본이 다음 자를 조용히 바꾼다»).

**회귀(전부 무수정 PASS)**: `verify619` 65/65 · `verify681` 39/39 · `verify660` 55/55 · `verify666` 32/32 ·
`verify816` 24/24 · `verify818` 37/37 · `verify814` 27/27 · `verify753` 32/32 · `verify683` 40/40 ·
`verify621` 42/42 · `verify625` 24/24 · **SMOKE PASS**.

## §4 회차 기록

### 1회차 (2026-09-03, sess-0543-13399 루틴 워커 C) — 제품 2자리 · 자 3벌 신설

위 §1~§3. 캡처는 `node tools/cap681.js r838a`(씬 A·B × 8장 · `docs/shots/` 는 커밋 안 함).

⚠ **하네스 함정 하나를 1회차에 스스로 밟았다** — `travel838.iou` 가 알 상자의 **높이**를 따로 받게
적혀 있어 `undefined` 가 NaN 으로 번져 IoU 가 **전부 0** 이었다(등재문의 0.744~0.775 를 «재현 안 됨» 으로
읽을 뻔했다). 알은 정사각 상자(`width = height = sz`)이므로 한 변만 받게 고쳤고, 그러자 0.78 로
등재문과 같은 자리에 앉았다. 「자가 만든 유령」(619 20회차 계열)의 표본 하나 더.

### 1회차 채점 — 비평 2인(CX·CY) 독립 · **CX 2 / CY 2** (통과선 8)

채점 규약은 681 9회차 것을 그대로 썼다(씬마다 ①②③④ → **두 씬 중 낮은 쪽의 최저 항목**이 최종).

| 축 | CX 씬 A | CY 씬 A | CX 씬 B | CY 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 4 | 4 | 8 | 9 |
| ② 크기 비율 | 6 | 7 | 7 | 8 |
| ③ 위치·경로 | **2** | **2** | 9 | 9 |
| ④ 가독성·일관성 | 3 | 4 | 7 | 8 |

**두 사람이 독립으로 같은 산수를 냈다** — 씬 A 몸길이 **중앙값 0.77(CX) · 0.61(CY)** ·
무리 반경비 **1.39×(CX) · 1.37×(CY)** · 3↔4장 IoU **0.69(CX) · 0.718(CY)**.
이 자(`probe838`)의 평균 0.83 과도 어긋나지 않는다 — **CX 표본 5알의 평균이 0.82** 다.
씬 B(점 대상)는 9.1 몸길이 · 11.3× · IoU 0.24 로 ①②③ 이 전부 통과선 위다.

⚑ **1순위도 한 낱말로 같았다 — «태생 반경을 상자에서 떼라».**
CX «t=0 에 이미 최종 반경의 **72%** 지점에서 태어난다 · 스폰 반경 76.7px → ≤0.4 D» ·
CY «t=0 에 알 11개가 이미 **205px 폭**(카드 폭의 66%)에 흩뿌려져 태어난다 — 발원이 «점» 이 아니라 «비용 줄 전체»».
곁들여 CX 가 ④ 의 산수를 줬다 — 봉우리에서 **최근접 중심거리 ÷ 지름 = 0.97**(서로 겹친다) ·
14알 중 분리 성분 8개 · 발원 코인 아이콘이 **77~79% 매몰**.

**1회차가 못 산 것**: 당김 사다리는 «벽에서 얼마나 물러날지» 만 정한다. 태어나는 자리는 여전히
21회차의 **탄생 타원(bbox 반축 + FXB_M)** 이라, 좁은 버튼에서는 **태어나는 순간 이미 벽**이다.

### 2회차 (2026-09-03, 같은 세션) — 제품 4자리(JS 3 · CSS 3선언) · 자 3벌 이관

**발원과 «날아갈 방» 을 분리했다**(`--burst-from`, 자식 셀렉터 — `--burst-to`(660)·`--burst-keep`(816)·
`--burst-ry`/`--burst-sz`(814)와 **같은 꼴**, 새 기계를 안 만든다). 신고한 호스트는 점 대상과 같은 기계를 탄다:

| | 1회차까지 | 2회차 |
|---|---|---|
| 발원 | 버튼 **중심** | 신고한 자식 = **코인/재화 아이콘**(훈련 `s` · 단련·룬 `.cic`) |
| 탄생 링 | bbox 반축 + `FXB_M`(= **벽 위**) | `FXB_RMIN` **22px 원** |
| 이동 | 중심→끝점 거리 × k(0.20~0.55) | 상자 대각의 **60~100%** 를 바깥으로(끝점은 여전히 상자 안) |
| 방향 | 균등 각 | **호스트 종횡비**를 타는 각(21회차 «링이 종횡비를 유지한다» 의 방향판) |
| 알 크기 | 46px(버튼 높이의 43%) | `FXB_FITS`(0.34) × 짧은 변 = **36px** — 660 «버튼이 허용하는 만큼» 의 산수판 |
| 각도 굴림 | ±0.26rad × 9 | 신고 호스트만 **±0.70rad × 9**(= 한 바퀴 · 아래 ⚠) |

**측정(같은 자 · 실행 3회)**: 사거리 **1.36~1.82 몸길이**(수리 전 0.41~0.46 · 1회차 0.83) ·
중앙값 **2.04** · 무리 반경비 **×3.24~4.09**(수리 전 ×1.24 · CX 처방 «3.5× 이상») ·
이웃 장 최대 IoU **0.61~0.66**(수리 전 0.78) · 스필 여전히 **−8.06px(안쪽)**.

⚠ **굴리는 각을 키운 이유** — 발원이 아이콘(한쪽 끝)이라 숫자(`--burst-keep`)가 바로 옆이다.
±67° 로는 «숫자 없는 쪽» 으로 못 넘어가 룬에서 `verify818` [R3]«수량 덮임 0» 이 **실제로 빨개졌다**(≥5% 표본 6개).
0.70rad 9칸이면 원을 한 바퀴 덮어 후보에 반드시 빈 쪽이 들어온다 — 신고 안 한 호스트는 종전 0.26 그대로다.

⚑ **이관 3벌(333 처방) — 「엉뚱한 잉크를 신고하면 수량이 덮인다」 는 전제를 이 회차가 죽였다.**
`verify816` [R2] · `verify818` [R2]×2 는 «아이콘을 신고하면 **숫자**가 그대로 덮인다» 를 물었는데,
발원이 그 아이콘이 되면서 **구멍이 발원을 덮어 궤적이 통째로 걸러진다** — 숫자가 안 덮이는 것이 이제 정상이다.
자리를 비우지 않고 **묻는 것을 그대로 둔 채 재는 것을 옮겼다**:
**[R2a] «발원 잉크를 신고하면 그 버스트가 죽는다»**(훈련 알 평균 11.3 → **2.4** · 단련 7.0 → 2.1 · 룬 6.6 → 2.1) +
**[R2b] «제품 선언에서는 그 잉크가 덮인다»**(짝 항 — 816 «코인은 안 신고한다» 가 근거).
⚠ 덮임 비는 **판정에서 뺐다** — 발원은 «태어나는 자리» 라 0 이 구조적으로 불가능하고 실행마다 0.48~0.74 배로 흔들린다(574·709·825 «문턱 플레이키»).
⚑ 이 이관이 남긴 규약 한 줄: **`--burst-from` 과 `--burst-keep` 에 같은 잉크를 신고하지 마라 — 그 버스트는 죽는다.**

**게이트**: `verify838` **12/12**(A1 1.15 몸길이 · A2 중앙값 1.00 · C1 ×2.60 · **C2 이웃 장 IoU ≤ 0.70 신설** ·
[R] 은 **두 회차를 다 되돌린다**) · `probe838` **5/5**(재현을 **수리 전 사본**에서 하도록 옮겼다 — probe681 선례).
**회귀**: `verify619` 65/65 · `verify660` 55/55 · `verify681` 39/39 · `verify816` **25/25**(24 → 25, 이관) ·
`verify818` **39/39**(37 → 39, 이관) · `verify666` 32/32 · `verify683` 40/40 · `verify753` 32/32 ·
`verify621` 42/42 · `verify625` 24/24 · **SMOKE PASS**.

### 2회차 채점 — 비평 2인(CZ·DA) 독립 · **CZ 5 / DA 3** (통과선 8 · 1회차 2/2 에서 +3/+1)

| 축 | CZ 씬 A | DA 씬 A | CZ 씬 B | DA 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 7 | 4 | 9 | 7 |
| ② 크기 비율 | 8 | 7 | 9 | 8 |
| ③ 위치·경로 | **5** | **3** | 8 | 6 |
| ④ 가독성·일관성 | 5 | 3 | 8 | 5 |

**1회차 대비 실측(두 사람 각각)**: 몸길이 중앙값 **0.77·0.61 → 2.18·2.54** · 무리 반경비
**1.39×·1.37× → 4.09×·3.13×** · 3↔4장 IoU **0.69·0.718 → 0.551·0.57** · 숫자 가림 **0.0%** 유지 ·
상자 밖 스필 **0.0px** 유지. 발원 아이콘 매몰도 77~79% → **25.7%(45ms 봉우리) 뒤 0%** 로 «아이콘에서 나온다» 가 읽힌다.

⚑⚑ **그런데 두 사람의 1순위가 **같은 새 결함**이었다 — «왼쪽 벽 적재».**
CZ «8알 전부가 **40.8° 부채**(원의 11%) 안에만 있고 f8 에서 5알이 **x=68.0~69.0(폭 1.0px)** 세로 선분에 정렬 ·
E1·E2·E3 의 요청 각도는 22.6° 차인데 끝 x 는 폭 **1.3px** — clamp 가 가로 각도 정보를 100% 지웠다» ·
DA «좌향 **65° 쐐기**(14알÷65° = 4.6°/알) · f8 에 4알이 x=68±0.5 · 발원 오른쪽 잉크 **0px**».
뿌리 둘: ⓐ 요청 이동량(상자 대각의 60~100% = 98~164px)이 **갈 곳**(좌 98·우 33·상 25·하 37)보다 커서
초과분을 clamp 가 **각도째** 지운다 · ⓑ 2회차가 알만 줄이고 keep-out 여유는 54px 짜리 값(30px)을 그대로 써서
가격 숫자의 구멍이 **발원 오른쪽 3px** 까지 밀고 들어와 오른쪽 반원이 통째로 굴려졌다.

### 3회차 (2026-09-03, 같은 세션) — 제품 5자리 · 자 4벌(신설 1 · 이관 3)

두 사람의 처방이 한 낱말로 겹쳤다(CZ ② «이동량을 방향별 남은 거리에서» · DA ① «끝점을 벽이 아니라
안쪽 타원 위에 미리 찍는다» · CZ ① · DA ② «알을 줄여라»). 셋을 다 넣었다:

1. **이동량을 «그 방향으로 남은 거리»에서 뽑는다**(`rayRoom` 신설 — 가둠 벽까지와 **keep-out 구멍까지** 중 짧은 쪽).
   구멍을 만나면 **각도를 굴려 버리는 대신 그 앞에서 멈춘다** — 2회차가 오른쪽 반원을 잃은 뿌리가
   «구멍에 걸리면 후보를 버린다» 였다. 끝점이 벽에 안 붙으므로 **각도 정보가 산다**(끝 x 폭 1.3px → 정렬 4알).
2. **탄생 반경도 남은 방을 따라간다**(`FXB_BR` 0.34) — 좁은 방향에서 22px 링을 고집하면 태어나는 자리가
   이미 구멍 안이라 그 방향이 통째로 죽는다.
3. **알 크기는 «상한 자르기» 가 아니라 «배율»**(`fitK`) — 2회차의 하드 상한이 14알을 같은 크기로 만들어
   두 사람이 «한 장 안 크기 변주 0(±3%)» 으로 적었다(씬 B ±12%). 배율이면 `rnd(24,34)` 변주가 살아 있다.
   `FXB_FITS` 0.34 → **0.26**(알 지름 35.8 → **23.7px** = 버튼 높이의 22% · CZ 21~30px · DA 18px 권고 대역).
4. **keep-out 여유가 그 배율을 같이 탄다** — 619 4회차 «여유는 입자 크기에서 나온다» 를 **더** 지키는 방향이고,
   이 한 줄이 위 ⓑ(오른쪽 반원 봉쇄)를 연다.

**측정(같은 자)**: 사거리 **1.68~1.71 몸길이**(중앙값 1.39) · 무리 반경비 **×3.75~3.8** ·
이웃 장 최대 IoU **0.63** · **가장 큰 빈 각 85~117°**(2회차 채점의 «부채 40.8°» = 빈 각 319°) ·
스필 **−11.6px(안쪽)** · 알 지름 23.7px.
⚠ **대가를 적어 둔다** — 이동 평균은 2회차 56.6 → **39.1px** 로 짧아졌다(끝점이 벽까지 안 가고 방의 82~100%).
몸길이가 그래도 오른 것은 알이 같이 작아졌기 때문이다. **각도를 사고 이동을 조금 판 것**이 이 회차의 거래다.

**게이트**: `verify838` **13/13**(C3 «가장 큰 빈 각 ≤ 140°» 신설 · A1 1.35 · A2 중앙값 1.20 · C1 ×3.00) ·
`probe838` 5/5. **이관 2벌** — `verify816` [A4]·`verify818` [A5] 가 여유 호출을 **한 글자까지** 단언하고 있어
`* fitK` 가 빨갛게 만들었다. 「여유가 입자 크기를 따라가는가」 라는 **묻는 것은 그대로 두고** 배율까지 받도록 넓혔다
(`verify816` 25/25 · `verify818` 39/39).
**회귀**: `verify619` 65/65 · `verify660` 55/55 · `verify681` 39/39 · `verify666` 32/32 · `verify683` 40/40 ·
`verify753` 32/32 · `verify621` 42/42 · `verify625` 24/24 · **SMOKE PASS**.

### 3회차 채점 — 비평 2인(DB·DC) 독립 · **DB 3 / DC 4** (통과선 8)

| 축 | DB 씬 A | DC 씬 A | DB 씬 B | DC 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 5 | 5 | 8 | 8 |
| ② 크기 비율 | 4 | 5 | 6 | 6 |
| ③ 위치·경로 | **3** | **4** | 8 | 7 |
| ④ 가독성·일관성 | 4 | 6 | 6 | 6 |

**닫힌 것(2회차 결함 둘)** — 두 사람 다 확인: **2px 띠 정렬 «최대 1알»(DB) · «정렬 인공물 없음»(DC)** ·
숫자 가림 **0%** · 상자 이탈 **0**. 몸길이 중앙값 **1.73(DB) · 2.04(DC)**, 반경비 ×3.45 · ×2.73.

⚑⚑ **그러나 1순위가 또 같았다 — «오른쪽 반원이 통째로 죽었다».**
DB «f8 에서 실제로 움직인 6알의 끝점각이 **81.2° 부채**뿐(빈 각 **278.8°**) · 오른쪽 3~4알은 전 구간
발원 코인 반경(24.5px) 안에 붙박여 **총 7px** 만 움직인다» · DC «빈 각 f7 160° / f8 **249°** · 3트랙이
**1.1 몸길이 미만**». 즉 3회차의 «구멍 앞에서 멈춘다» 는 오른쪽 알을 **살렸지만 «거의 안 움직이는 알»** 로 살렸다.
⚑ **DB 가 자리의 산수를 줬다** — 「53」의 상자는 44×36 이고 그 **위 26px · 아래 19px 회랑**이
x=329 까지 **165px** 비어 있다. 오른쪽이 죽은 것은 «벽이 35px» 라서가 아니라 **그 회랑을 아무도 안 향해서**다.

### 4회차 (2026-09-03, 같은 세션) — 제품 3자리

1. **각도의 종횡비 편향을 걷어냈다.** 2회차가 그것을 넣은 이유(«짧은 축으로 쏘면 9px 뿐»)는
   3회차가 사거리를 방향별로 뽑으면서 사라졌는데, 편향이 남아 **가파른 각을 눌러 회랑을 못 쓰게** 하고 있었다.
   균등 각이면 회랑을 향하는 표본이 실제로 생기고, 짧은 방향은 «없는 알» 이 아니라 «짧게 가는 알» 이 된다.
2. **굴림을 2단 사다리로**(앞 두 칸 0.26rad · 그 뒤 0.70rad). 3회차까지 신고 호스트는 한 칸이 40° 라
   **융합 하나 때문에 알이 40° 씩 날아갔고**, 그 알들이 왼쪽에 쌓였다(DB «4알이 x=69~81 의 11.8px 기둥»).
   융합 회피는 619 12회차가 0.26 으로 하던 일이고, 큰 칸이 필요한 것은 **구멍을 넘을 때**뿐이다.
3. **알 지름 `FXB_FITS` 0.26 → 0.22**(23.7 → **18.8px** = 버튼 높이의 17.8%) — 회랑(19~26px)을 지나려면
   알이 그보다 작아야 한다(DB 처방 A 의 산수 · 여유도 `fitK` 를 타고 같이 줄어 회랑이 더 열린다).

**측정**: 사거리 **1.85~1.89 몸길이**(중앙값 1.54) · **가장 안 움직인 알 0.63 몸길이**(3회차 0.34~0.46 ·
2회차 0.22 — 「발원에 붙박인 알」 이 사라졌다) · **가장 큰 빈 각 51.6~92.4°**(3회차 85~117° · 2회차 채점 295~319°) ·
이웃 장 최대 IoU **0.60** · 무리 반경비 **×3.37~3.52** · 스필 **−9.0px(안쪽)**.

**게이트**: `verify838` **14/14**(A4 «가장 안 움직인 알 ≥ 0.55 몸길이» 신설 · A1 1.55 · A2 1.35 · C3 100°).
**회귀**: `verify619` 65/65 · `verify660` 55/55 · `verify816` 25/25 · `verify818` 39/39 · `verify681` 39/39 ·
`verify666` 32/32 · `verify683` 40/40 · `verify753` 32/32 · `verify621` 42/42 · `verify625` 24/24 · **SMOKE PASS**.

### 4회차 채점 — 비평 2인(DD·DE) 독립 · **DD 3 / DE 3**

| 축 | DD 씬 A | DE 씬 A | DD 씬 B | DE 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 5 | 4 | 8 | 8 |
| ② 크기 비율 | 6 | 5 | 8 | 7 |
| ③ 위치·경로 | **3** | **3** | 6 | 7 |
| ④ 가독성·일관성 | 5 | 5 | 7 | 7 |

**닫힌 것**: 몸길이 중앙값 **2.6(DD) · 2.94(DE)**(1회차 0.61~0.77) · **가장 큰 빈 각 53.9° · 86.4°**
(2회차 채점 295~319° · 3회차 249~278°) · 숫자 가림 **0.0%** · 상자 이탈 **0px** — 3·4회차의 두 결함이 다 닫혔다.

⚑⚑ **1순위가 또 같았다 — «알이 발원 코인을 못 벗어난다».**
DD «13알 중 3알이 250ms 에도 원점 25px 안(16.6/22.0/23.2px) · 9알이 코인 지름 52 언저리» ·
DE «110ms 에 12알 중 6알이 아직 발원 25px 안 · 최소 몸길이 1.45».
**두 사람의 처방도 같았다** — DD «+x ±40° 섹터를 막고 그 알을 좌·상·하로 재분배» ·
DE «최소 이동 반경 = 발원 반경 25 + 알 반경 7 = 32px 를 하한으로 강제». 원인도 같이 짚었다:
그쪽은 「53」까지 **33~35px** 뿐이라 «발원 원반» 을 벗어날 방이 없다.

### 5회차 (2026-09-03, 같은 세션) — 제품 4자리 · 자 3벌(축 1 신설 · 이관 1)

**«발원 원반을 벗어나야 나온다» 를 규칙으로 세웠다.** 방이 «발원 원반 + 알 반지름» 도 못 내주는
방향은 안 쓰고 각도를 굴린다. 두 번 고쳐 잡았다:

1. **1차 시도(금지만)** — A5(발원 안에서 끝나는 알)는 5 → **0** 이 됐지만 알이 깊은 쪽(왼쪽)으로 몰려
   빈 각이 **185°** 로 튀었다(2회차의 부채가 되돌아왔다). ⇒ 되돌리고 두 가지를 고쳤다:
   ⓐ 발원 «반경» 은 **상자가 아니라 그린 원반**이다(코인 상자 71px ↔ 그림 52px · DD 실측 비 0.73 = `FXB_FOK`).
   ⓑ 차선(`fb`)도 **깊은 방향 중에서** 고른다 — 안 그러면 융합 하나 때문에 얕은 원래 각으로 되돌아가
     그 알이 발원 안에서 끝난다(A5 가 세던 바로 그 알들이다).
2. **2차(금지 대신 크기로 갚기)** — 등재문이 연 갈래를 여기서도 썼다: 얕은 방향은 **버리지 말고
   «그 방향에서 발원을 벗어날 수 있는 가장 큰 알»** 로 줄인다(`roomSz`). 최소 크기(16px)보다도 작아질
   때만 버린다. ⇒ 빈 각 **185° → 116~122°**.

**측정(5회차 최종)**: 사거리 **1.97~2.10 몸길이**(중앙값 1.42~1.68) · **가장 안 움직인 알 0.88~1.10 몸길이** ·
**발원 원반 안에서 끝난 알 0개**(4회차 채점 DD 3알 · DE 6알) · 이웃 장 최대 IoU **0.58~0.59** ·
무리 반경비 **×3.46~3.59** · 빈 각 **110~122°** · 스필 **−8.9px(안쪽)**.

⚑ **이 회차의 거래를 게이트에 적었다** — C3(빈 각) 문턱을 100 → **135°** 로 열었다. 값을 결과에 맞춘 것이
아니라 **A5 와 맞바꾼 것**이다: 「53」 쪽 ±40° 섹터는 방이 35px 뿐이라 «발원 원반(26px)을 벗어나는 알» 이
**산술적으로 못 산다** — A5 를 0 으로 지키면 그 섹터가 비고, 그 섹터를 쓰면 A5 가 5알로 빨개진다(둘 다 실측).
비평 2인이 1순위로 적은 것은 A5 쪽이라 A5 를 지키고 C3 가 대가를 적는다(문턱은 실측 116~122 **위로** 띄웠다 —
574·709·825 «문턱 플레이키»).

**게이트**: `verify838` **15/15**(A5 «발원 원반 안에서 끝나는 알 0» 신설).
**이관 1벌** — `verify816` [R1] 이 «신고를 지우면 숫자가 **25% 이상** 덮인다» 를 물었는데, 838 이 알을
54 → 20px 로 줄이고 궤적을 발원 밖으로 밀면서 **신고 없이도 최대 10~19%** 가 됐다(제품이 옳은데 빨간 자리).
눈금을 818 [R1] 과 같은 **≥5%** 로 내리고 최대값은 계속 찍는다.
**회귀**: `verify619` 65/65 · `verify660` 55/55 · `verify818` 39/39 · `verify681` 39/39 · `verify666` 32/32 ·
`verify683` 40/40 · `verify753` 32/32 · `verify621` 42/42 · `verify625` 24/24 · **SMOKE PASS**.

### 5회차 채점 — 비평 2인(DF·DG) 독립 · **DF 3 / DG 3**

| 축 | DF 씬 A | DG 씬 A | DF 씬 B | DG 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 5 | 4 | 8 | 9 |
| ② 크기 비율 | 6 | 6 | 7 | 9 |
| ③ 위치·경로 | **3** | **3** | 7 | 7 |
| ④ 가독성·일관성 | 6 | 5 | 6 | 7 |

**닫힌 것(5회차가 목표로 한 축)**: **발원 원반 안에서 끝나는 알 0/10**(두 사람 각각 확인 · 4회차 DD 3알 · DE 6알) ·
「53」 가림 **0.0%**(여유 8~16.8px) · 상자 이탈 **0px**(여유 5.4~12px) · 발원 정합 **2.2px**(DG 역투영 — 알이 실제로 코인에서 나온다).

⚑⚑ **두 사람이 «섹터 맞바꿈» 을 각각 계산해 5회차의 결정을 지지했다**(이 회차의 가장 중요한 회신):
DF — 사분면별 «원반 밖 몸길이»: 우(±40°) **0.0** · 상 1.1 · 하 1.4 · 좌 5.0 ⇒ «우측에 2알을 넣으면 원반 밖 이동
중앙값이 −18% 되고 «0 이동» 더미가 17% 생긴다. **비우는 쪽이 정답**» ·
DG — «「53」 섹터 비우기는 유지(오히려 확대)가 정답이고, 지금의 빈 각 126°는 **반경 부족의 증상이지 원인이 아니다**.
**예산은 각도가 아니라 반경에 써야 한다**».
⇒ `verify838` [C3] 의 문턱을 A5 와 맞바꾼 판단이 외부 두 사람에게 각각 확인됐다.

**남은 1순위도 둘이 같았다 — «반경이 모자란다».**
DF «안쪽 7알의 끝점 반경 중앙값 44.7px(원반 반지름 26 의 1.7배) · 원반 밖 실이동 1.10 몸길이 · 3↔4장 IoU 0.75» ·
DG «몸길이 중앙값 1.79 · 10알 중 6알이 수명의 46~84% 를 원반 발자국 안에서 보낸다 · 3↔4장 IoU 0.491».
⚑ **DG 가 손익분기를 계산해 줬다** — 끝반경 R 에서 Ø11 알 하나가 차지하는 각은 `2·asin(5.5/R)`.
현재 R=41 이면 14알에 **324°** 가 필요한데 「53」 가드 뒤 활성 호는 **280°** ⇒ 뭉침(0.30)과 내부 구멍(50.6°)이 **동시에** 난다.
**손익분기는 14알에 R ≥ 47.4px · 10알에 R ≥ 33.9px** — 즉 «알 수» 와 «반경» 중 하나를 풀면 두 축의 상충이 사라진다.

### 6회차 (2026-09-03, sess-1027-19570 루틴 워커 B) — 제품 4자리(JS 3 · CSS 1선언) · 자 4항 신설

**5회차가 남긴 «1순위 = 반경이 모자란다» 를 DG 의 손익분기 산수 그대로 갚았다.** DF·DG 가 각각
«R ≥ 48px 면 각도와 뭉침이 동시에 풀린다» 를 냈지만 **R 은 못 올린다** — 가둠 상자(619 28회차)가
훈련 버튼의 반높이 53px 이고 잉크가 `sz/2 + FXB_INPAD` 를 물러나야 해서 세로 최대 반경이 40px 언저리다.
⇒ 예산의 **다른 쪽 두 항**(알 수 · 알 지름)을 열었다. 둘 다 5회차 §5 가 이 행 안이라고 적어 둔 지렛대다.

1. **알 수 14 → 10**(`--burst-n`, 새 손잡이 넷째). 14 는 «첫 발 `fxUpOk` 10 + 반복 `UPFX_NOW` 4» 다.
   ⚑ **배수가 아니라 «상한» 인 것이 이 항의 전부다** — 배수로 누르면 반복분까지 4 → 3 이 되고 그 순간
   `verify818` [C2](동시 생존 = 발화당 × 수명 ÷ 가장 성긴 틱 = 3×380÷160 = **7.1 < 8**)가 빨개진다.
   상한이면 첫 발만 `min(10,6)` = 6 으로 눌리고 반복분은 `min(4,6)` = **4 그대로**다(첫 발 6 + 반복 4 = 10).
2. **알 지름 Ø20.4 → 16.3**(`--burst-fit`, `FXB_FITS` 를 **호스트가 좁혀 신고**한다 · 상한은 `FXB_FITS`).
   DF Ø11 · DG Ø15.5 요구와 3회차 채점 DC «미세점은 판독 불가» 의 교집합이 0.18 이다(5회차 §5 의 그 값).

**측정(6회차 최종 · `verify838`)**: 사거리 **2.30 몸길이**(5회차 1.97~2.10 · 수리 전 0.41) ·
중앙값 **1.76**(1.42~1.68) · 가장 안 움직인 알 **1.37**(0.88~1.10) · 이웃 장 최대 IoU **0.54**(0.58~0.59) ·
빈 각 **103.8°**(110~122) · 무리 반경비 **×3.20** · 발원 원반 안에서 끝난 알 **0** · 스필 **−8.8px(안쪽)**.
**여섯 축이 같은 회차에 동시에 좋아진 것은 다섯 회차 만에 처음이다** — 앞 회차들이 하나를 올리면
하나가 내려간 이유가 예산 적자였다는 DG 의 진단이 그대로 맞았다.

⚑ **이 회차가 자에 새로 세운 것 — [E] 반경 예산(DG 의 산수를 자로)**: 끝반경 R 에서 지름 d 인 알이
먹는 각은 `2·asin(d/2R)` 이므로 알 n 개가 **요구하는 호**는 `n·2·asin(d/2R)`, **쓸 수 있는 호**는
`360 − 빈 각` 이다. 요구 > 공급이면 뭉침과 내부 구멍이 **동시에** 난다.
실측 — 6회차 **186.1° ≤ 256.8°(흑자)** ↔ 두 신고를 걷은 사본 **314.8° > 250.2°(적자)**(= DG 가 손으로
계산한 324° ↔ 280° 와 같은 자리). ⚠ 이 항은 [C2]·[C3] 과 **다른 것을 묻는다** — 둘은 «지금 그림이
어떤가» 이고 이 항은 «지금 그림이 **산술적으로 가능한가**» 다.

⚠⚠ **범위를 씬 A(훈련 버튼) 하나로 좁힌 것이 이 회차의 유일한 «못 한 일» 이다.** 두 신고를 단련
(`.tr-tp>.tb` 496×173)·룬(`.tr-rn>.rbt.b1` 420×112)에도 걸어 보고 되돌렸다 — 그 상태에서
`verify818` [C1](= 660 [C] «알 중심이 버튼 밖 0»)이 1~2개로 빨개졌기 때문이다. 갈래를 끝까지 갈랐다:
  · **가설 ⓐ «알이 작아져 가둠 상자가 열린 몫을 버튼 자기 눌림이 먹는다»** — 621 눌림이
    `scale(.94) translate(0 8px)` 이라 단련은 가로 몫만 **14.9px** 이다. 여유의 바닥을 그 변위에서
    파생시키는 안(변마다 `0.03w+4` · `0.03h+8+4` · `0.03h−8+4`)을 실제로 넣어 봤지만 **[C1] 이 그대로 1개**였다.
  · **가설 ⓑ «자가 흔들린다»** — 338·344 규칙대로 **수리 전 트리**에서 같은 자를 두 번 돌렸더니
    **1회 빨강 · 1회 초록**(38/39 ↔ 39/39)이다. ⇒ **내 것이 아니다.** 846 이 자기 머리말에 적어 둔
    «알 방향이 발화마다 무작위라 12세대는 제비뽑기다 — 단련 봉우리가 12.5% ↔ 4.0% 로 갈렸다» 가 그 자리다.
  ⇒ **870 으로 등재**(⚠ 등재 push 경쟁으로 868 → 870 개정 · T1 «버그(자 플레이키)» · 574·709·825·854·855 계열)하고, 이 회차는 **채점받은
    호스트만** 신고했다. 단련·룬은 선언·픽셀 **한 값도 안 바뀐다**(신고가 없으면 종전 그대로인 규약).
    870 이 닫히면 같은 두 신고를 그 둘에도 걸어 «세 탭 한 어휘»(660)를 회수하는 것이 다음 회차 몫이다.

**게이트**: `verify838` **19/19**(신설 4 — [E1] 알 10개 이하 · [E2] 반경 예산 흑자 ·
[R3a][R3b] 두 신고를 걷으면 [E2] 가 적자로 돌아간다).
**이관 0벌** — 이번 회차는 눈금을 하나도 안 눌렀다(A1~A5·B·C·D 문턱 전부 5회차 값 그대로이고,
그 위에서 값이 올라갔다).

### 7회차 (2026-09-03, sess-1142-20793 루틴 워커 C) — 캡처 + 채점 2인 · **제품 `index.html` 0줄(처방을 넣어 보고 되돌렸다)**

6회차가 남긴 지시(«7회차 캡처·비평 2인부터 이어서 센다 · 브리핑은 5회차와 같은 것을 쓰되 «반경 예산이
흑자로 돌아섰다» 를 알리지 말 것»)를 그대로 따랐다. 캡처 `node tools/cap681.js r838g`(씬 A·B × 8장).

### 7회차 채점 — 비평 2인(DH·DI) 독립 · **DH 3 / DI 4** (통과선 8)

| 축 | DH 씬 A | DI 씬 A | DH 씬 B | DI 씬 B |
|---|---|---|---|---|
| ① 타이밍 | 4 | 6 | 6 | 7 |
| ② 크기 비율 | 5 | **8** | 5 | 7 |
| ③ 위치·경로 | **3** | **5** | **3** | **4** |
| ④ 가독성·일관성 | 4 | 6 | 5 | 6 |

**6회차의 네 축이 실제로 회수됐다** — 5회차 DF·DG 가 각각 1순위로 적은 «반경이 모자란다» 는
7회차 두 사람 중 **아무도 1순위로 적지 않았다**(6회차가 알 수·크기로 예산을 갚은 그 자리다).
DI 는 ② 를 **8**(이 작업 일곱 회차 중 최고)로 올리며 «알 13.3 CSS = 호스트 높이의 12.5% · 코인의 33% ·
시간 계단 봉우리/끝 1.92배» 를 근거로 적었다.

⚑⚑ **두 사람이 «세 수» 를 독립으로 같이 냈다 — 그리고 셋이 한 뿌리다.**

| 자 | DH | DI |
|---|---|---|
| 최대 빈 각 | **108.2°** | **106.3°** |
| 최소 간격(뭉침) | 봉우리 최근접 ÷ 지름 **0.98** | **9.3°** · 봉우리 최근접 **0.68 지름** |
| 무리 무게중심 ↔ 발원 | 24.1px = 평균반경의 **47.6%** | (−23.4, −10.6) = **0.46배** |
| 끝 반경 산포 | **3.25배** | **2.94배** (CV 0.38) |

DI 가 뿌리에 한 걸음 더 갔다 — «끝점이 원이 아니라 **직사각형**을 그린다»: f8 에서 **9알 중 7알이
가둠 사각형 윤곽에서 ±4.5px 안**에 앉는다(A Δ0.3 · D Δ1.4 · C Δ2.4 · F Δ4.4 · G Δ4.4 · E Δ−4.4 · I Δ−7.4).
그리고 «「53」 잉크가 먹는 각은 **68.5°** 뿐인데 실제로 빈 각은 106.3° = **37.8° 과잉 회피**» 를 셌다.

**두 사람의 처방도 같은 곳을 가리켰다**(DH P1 뒷절 · DI P2): **«쓸 수 있는 호» 위에 씨앗 각을 등간격으로
다시 뿌려라** — 지금은 360° 에 등간격으로 뿌리고 못 쓰는 방향은 굴림(`FXB_KTRY`)으로 옆으로 밀리는데,
밀린 알이 이웃 **위에** 포개지므로 오른쪽은 비고 왼쪽은 두 겹이 된다(= 위 표의 세 수).

#### ⚠⚠ 그 처방을 **실제로 넣었고, 자가 기각했다** — 되돌렸다(제품 0줄). 값을 남긴다(LESSONS 838-④ 규약).

두 판을 다 만들어 재 봤다(`fxbArc` — 버스트당 5° 격자 72칸으로 «쓸 수 있는 호» 를 굽고 그 위에 `cnt` 개를
등간격 배치 · 판정식은 `roomOk` 를 **그대로** 재사용 · 새 `rnd` 호출 0회로 난수 흐름 불변):

| 자 | 무변경(5회 실측) | 1안 `(i+0.5)/cnt` | 2안 «양 끝 앵커» `i/(cnt−1)` |
|---|---|---|---|
| C3 빈 각 | **102.61 · 102.61 · 103.24 · 103.81** (진폭 1.2°) | **121.74** ✗ | **107.02** ✗ |
| A2 중앙값 | 1.74~1.80 | **1.64** ✗ | **1.64** ✗ |
| A1 사거리 | 2.15~2.31 | 2.16 | **1.95** ✗ |
| C1 반경비 | 3.20~3.69 | 3.37 | **3.00**(문턱과 동일) ✗ |
| A4 최소 알 | 1.07~1.41 | 1.27 | 1.41 |

**기각 이유는 «굴림이 결함이 아니라 일을 하고 있었다» 이다.** 막힌 쐐기 안에 떨어진 씨앗은 굴림이
**쐐기 경계 바로 바깥**에 앉힌다 — 그래서 빈 각이 쐐기보다 커지지 않는다. 등간격 재파종은 바깥쪽 알을
경계에서 **반 피치(11.9°) 안쪽**에 앉히므로 빈 각이 그만큼 **넓어진다**(1안 +18°). 두 사람의 산수
(«빈 각 ≤86°» DH · «≤97.7°» DI)는 **쐐기가 68.5° 라는 전제** 위에 있었는데, 실제로 못 쓰는 섹터는
잉크 쐐기가 아니라 **`roomOk`(5회차 A5)가 막는 얕은 방** 이고 그쪽이 더 넓다(자 실측 «쓸 수 있는 호»
238~257°, 즉 막힌 각이 **103~122°**). ⇒ **106.3° 는 과잉 회피가 아니라 A5 의 값이다.**
5회차가 C3 를 100 → 135° 로 연 그 거래(A5 와 맞바꿈)를 7회차가 **바깥에서 다시 확인한 셈**이다.

#### ⚑⚑ 이 회차의 진짜 수확 — **자가 흔들린다(873 등재)**

되돌린 뒤 무변경 트리에서 자를 **5회** 돌려 위 표의 «무변경» 칸을 채우다 발견했다:

* `C1` 끝반경비 **×3.20 ~ ×3.69 = ±15%** · `A4` **1.07 ~ 1.41 = ±14%** · `A1` 2.15~2.31 ·
  `E2` 요구 호 **170.99 ~ 188.44°** — **한 줄도 안 고친 트리에서** 이만큼 흔들린다.
* `C3` 만 예외로 촘촘하다(102.61~103.81 · 진폭 **1.2°**) — 그래서 위 기각 판정이 선다(1안 +18°·2안 +3.8° 는
  둘 다 진폭 밖이고, A2 의 −0.10 도 밖이다. **A1·C1·C2 의 «내려감» 은 진폭 안이라 근거로 안 썼다**).
* 뿌리는 «시드가 없다» 가 아니다 — `travel838.js` 는 `Math.random` 을 고정 시드 PRNG 로 갈아 끼운다(105~112).
  **시드 뒤 버스트에 닿기까지 게임 루프·파티클이 draw 를 몇 번 쓰는지가 러너 속도에 달렸다** ⇒ 버스트가
  수열의 다른 자리에서 시작한다. `waitForTimeout(900)`·`(700)` 이 «시간» 이지 «프레임 수» 가 아닌 자리다.
* ⚑ **처방이 저장소 안에 이미 있다** — `cap681.js` 머리말이 «난수는 **트리거 직전**에 다시 심는다 —
  안 그러면 여덟 장이 서로 다른 버스트가 된다(666 5회차 · LESSONS 666-⑧)» 라고 적어 두고 그렇게 한다.
  `travel838.js` 만 이 규약을 안 따른다.
* ⚠ **1~6회차의 «축이 올랐다» 판정이 전부 단일 실행 대조였다** — C1 의 무변경 진폭(±15%)이 6회차가 보고한
  상승폭과 같은 크기다. **다음 회차는 873 을 먼저 닫고(또는 n회 중앙값으로 읽고) 채점하라.**
  7회차가 그 함정을 실제로 밟았다(처방 1안을 단일 실행끼리 비교해 «네 축이 내려갔다» 고 읽었는데
  진짜 회귀는 **C3·A2 둘뿐**이었다).

**게이트**: `verify838` **19/19**(무변경 · 5회 전부 PASS) · **회귀 무수정 PASS** — `verify619` 65/65 ·
`verify660` 55/55 · `verify818` 39/39 · `verify816` 25/25 · `verify681` 39/39 · `verify683` 40/40 ·
`verify753` 32/32 · `verify621` 42/42 · `verify625` 24/24 · `verify666` 32/32 · **SMOKE PASS** ·
`verifyProgress` 종료 코드 0.

#### 곁다리 관측 1건 — `verify814` [B11] 빨강(**874 등재** · 내 것이 아님을 대조로 못박았다)

회귀 스윕에서 `verify814` **28/29** 가 나왔다. 내 제품 변경은 **0줄**이므로 갈래는 하나뿐이고, 338·344
규칙대로 **대조 실행**으로 자리를 못박았다: 862 의 `59ef749` 가 `fxFlash` 의 들이기를 `inset` 인자
뒤에서 꺼내 모든 호스트에 건 **그 한 줄**(`const ring = inset ? fxRingIn(el) : 0;` → `const ring = fxRingIn(el);`)만
옛 식으로 되돌린 사본에서 **29/29 PASS**, 되돌리지 않으면 **28/29**. ⚠ **862 lock 이 살아 있어
선점하지 않았다** — 그 세션의 회귀가 닫는 것이 1순위다(상세·갈래 둘은 PROGRESS 874 행).

### 8회차 (2026-09-03, sess-1358-25438 루틴 워커 D) — **제품 `index.html` 0줄(처방을 넣어 보고 자가 기각 → 되돌렸다)** · 877 등재

7회차가 «다음 회차 1순위» 로 넘긴 873(자 플레이키)이 그 사이 닫혀서(재시드 · 열한 축 진폭 0),
8회차는 **단일 실행 대조가 정당한 상태**로 시작했다. `verify838` 19/19 · `probe838` 5/5 무수정 재확인.
스크래치 자로 7회차 두 사람이 낸 세 수를 다시 찍었다(재현): **끝 반경 산포 ×2.89**(min 34.2 · max 98.8) ·
**무게중심 어긋남 36.3%**(dx −18.6) · **최소 간격 0.13 지름**(#1·#7 이 **각 58°/58°** 로 포갠다 · 7회차 DH «0.98 지름» 보다 나쁘다).

**7회차가 못 판 갈래를 하나 팠다 — 끝점 융합.** 12회차의 `ks`(융합 회피)는 **태어난 자리** 만 등록·회피하는데,
요소 대상은 births 가 원점↔끝점 사이(당김 사다리)라 births 가 `FXB_SEP` 떨어져도 **끝점이 벽에서 겹칠 수 있다**
(DI «끝점이 직사각형 · 9알 중 7알이 가둠 사각형 윤곽 ±4.5px»). ⇒ ⓐ 1순위 수락에 `!fxbFuseE(ex,ey)` 를 얹고
ⓑ 차선(`fb`)도 **끝점이 이미 앉은 끝점을 안 밟는 쪽**을 먼저 들게 고쳤다(끝점 여유가 없을 때만 종전 차선 `fbAny`
= ≥1 보장). **둘 다 넣어 재 봤고 자가 기각했다**(제품 0줄로 되돌림 · LESSONS 838-④ 규약):

| 자 | 무변경 | ⓐ 수락 끝점융합 | ⓐ+ⓑ 차선 끝점융합 |
|---|---|---|---|
| 최소 간격 | **0.13 지름**(#1·#7) | 0.13(불변) | **0.13(불변)** |
| 각 목록 | `58 58 120 …` | `58 58 …` | `58 58 120 164 199 …`(#4 만 이동) |
| 끝 반경 산포 | ×2.89 | ×2.89 | **×2.97(악화)** |
| 무게중심 | 36.3% | 36.3% | **40.5%(악화)** |

**기각 이유 = #1·#7 은 「쓸 수 있는 호」의 **한쪽 경계(58°)에 굴려 앉는 알**이고, 그 경계가 유일한 출구다.**
7회차가 §5 5번에서 이미 못박았다 — 못 쓰는 섹터는 잉크 쐐기(68.5°)가 아니라 **A5(5회차)가 막는 얕은 방**(103~122°)이고,
막힌 방향에서 굴려 나온 알은 **경계 바로 바깥**에 앉는다. 두 알이 다 막히면 **둘 다 같은 경계**로 온다(= 58°/58°).
끝점 융합으로 둘째 알을 밀어내면 갈 곳이 **경계 안쪽(A5 금지) 아니면 원래 각(발원 원반 안 = A5 가 세던 알)** 뿐이라,
차선이 **더 깊은(먼) 각**을 골라 산포가 되레 커진다(위 표 ×2.97 · 40.5%). ⇒ **경계 클럼프는 A5 를 다시 열지 않는 한 못 푼다**,
그리고 A5 재개방은 5회차가 두 사람 확인 아래 맞바꾼 거래다(§5 5번과 같은 벽 — 재파종과 뿌리가 같다).

⚑⚑ **8회차의 수확 = 남은 최대 결손이 이 행 밖임을 확정했다(877 등재).** §5 2번(속도 이즈아웃)이 네 사람(DA·DE·DF·DG)이
«등속 · 45→70ms 가 같은 그림» 으로 적은 **③ 의 최대 실점**인데, 그 곡선은 공용 `@keyframes fxSpark`(09·12·17·장비·코스튬
공유 · **681 이 아홉 회차 고쳐 ⏸ 보류로 닫은 자리**)라 이 행에서 건드리면 남의 화면 다섯을 같이 바꾼다. ⇒ 7회차 §5 2번의
지시(«새 번호로 등재하는 것이 맞다»)대로 **877 로 등재**했다(자·수치는 이 문서 §4 5회차 절). **839~876 을 훑어 877 이 미사용임을
awk 로 확인**하고 잡았다.

**게이트**: `verify838` **19/19 무수정** · `probe838` 5/5 · **SMOKE PASS** · `verifyProgress` 0. **이관 0벌 · 제품 0줄.**
**채점은 안 돌렸다** — 제품이 0줄이라 7회차의 DH 3 / DI 4 가 그대로 선다(비평 루프로 회차 예산을 태우지 말라는 [3]-(가)/(다) 규약).
**남은 것**: ③ 의 최대 실점(속도 이즈아웃)은 **877** 로 갔다 · 경계 클럼프·박스 추적(산포 ×2.89)은 이 박스(310×106)의
**구조 한계**다(반경 캡은 `C1` 여유 13.67% 를 깬다 — 넣어 보니 mean rE 가 내려가 C1<3.00). **다음 세션(9회차)이 8점을
노리려면 877(속도)이 먼저 닫혀 ③ 의 큰 몫이 빠진 뒤라야 한다.**

### 9회차 (2026-09-03, sess-1558-25897 루틴 워커 D) — 캡처 + 채점 2인 · **제품 `index.html` 0줄(이 회차는 채점 회차다)**

8회차가 남긴 조건(«9회차가 8점을 노리려면 **877(속도)이 먼저 닫혀** ③ 의 큰 몫이 빠진 뒤라야 한다»)이
그 사이 충족됐다 — **877 완료**(`b332091` · 요소 버스트 이즈아웃 `fxSparkE` 를 호스트 신고로) ·
**870·871·873·874 전부 완료**. 그래서 이 회차는 **877 뒤 첫 재촬영**이다.
캡처 `node tools/cap681.js r838h`(씬 A·B × 8장) · 브리핑 `docs/review/838-비평브리핑-r9.md`
(5·7회차와 같은 것을 쓰되 **이 회차가 무엇을 고쳤는지는 알리지 않았다** — 그 회신이 검산이다).
착수 게이트 — `verify838` **19/19** · `verify818` **41/41** · **SMOKE PASS** · `verifyProgress` 0.

### 9회차 채점 — 비평 2인(DJ·DK) 독립 · **DJ 5 / DK 6** (통과선 8 · 7회차 DH 3 / DI 4)

| 축 | DJ 씬 A | DK 씬 A | DJ 씬 B | DK 씬 B |
|---|---|---|---|---|
| ① 타이밍 | **8** | 7 | 6 | 6 |
| ② 크기 비율 | 6 | 7 | 6 | 7 |
| ③ 위치·경로 | 6 | 6 | **5** | 7 |
| ④ 가독성·일관성 | 6 | 6 | **5** | **6** |

**⚑⚑ 877 의 회수가 확인됐다 — ① 이 이 작업 아홉 회차 중 최고다.**
7회차 DH 4 / DI 6 → 9회차 **DJ 8 / DK 7**. 두 사람 다 «감속 곡선이 매끈하다» 를 각자 수치로 적었다 —
DJ «45~110ms 0.52px/ms → 250~320ms **0.064px/ms**»(알 하나 연결요소 추적 · 골드 마스크
R>200,G140–235,B<160,R−B>60) · DK «#3→#6 프레임간 변화량이 점감 — **등속/계단으로는 안 읽힌다**».
5·7회차에 네 사람(DA·DE·DF·DG)이 «등속이다 · 45→70ms 가 같은 그림» 으로 적었던 그 자리다.
**«③ 의 최대 실점» 이라던 항이 실제로 빠졌고, 남은 것이 드러났다.**

**⚑⚑ 그리고 남은 둘이 «독립 3자 일치» 로 닫혔다 — 하나는 이 행 안, 하나는 이 행 밖이다.**

#### ㉮ ③ 방향 비대칭 **≈2.9배** — 자·DJ·DK 셋이 각각 같은 수를 냈고, 그 수는 **상자 종횡비**다

| 잰 사람 | 자 | 값 |
|---|---|---|
| DJ | #6 좌측 부채(120~220°) 최장 90.1px ↔ 우측 부채(260~320°) 30.6px | **×2.94** (#7 에서 99.6:33.8 = **×2.95** 로 재현) |
| DK | #6 기준 알 중심↔원점 거리 min 31 ↔ max 83 | **×2.7** |
| 자(`travel838`) | 끝 반경 min 34.5 ↔ max 99.9 · CV **0.367** | **×2.90** |

⚑ **셋이 맞춘 그 수가 가둠 상자의 종횡비다** — 훈련 버튼 **310×106 = ×2.925**.
우연이 아니라 **항등식**이다: 3회차가 사거리를 «그 방향으로 남은 거리»(`rayRoom`)의 몫으로 뽑았고
(`dv = (room − br) × dFrac`) 그래서 **끝점 궤적이 원이 아니라 상자를 닮는다**(DI 7회차 «9알 중
7알이 가둠 사각형 윤곽 ±4.5px»). 대조군이 그것을 못박는다 — 씬 B(점 대상 · 상자 없음)는
**산포 ×1.65 · CV 0.148** 로 같은 자에서 **2.5배 촘촘하다**.
⇒ **DJ 의 처방(«우측 이동거리를 좌측과 동률로»)은 이 행에서 못 쓴다** — 우측이 짧은 이유가
① 「53」 잉크 쐐기 ② **A5(발원 원반을 벗어나야 한다)가 막는 얕은 방 103~122°** 인데, 둘 다
5회차가 두 사람(DF·DG) 확인 아래 **맞바꾼 거래**이고 7·8회차가 재파종·끝점융합 두 판을
**만들어 보고 자로 기각**한 자리다(§5 5·6번). 남은 지렛대는 **상자 자신**(=23 훈련 카드 기하)이라
이 행 밖이다 — 아래 §5 7번에 지렛대와 산수를 남긴다.

#### ㉯ ② 알이 «판독 하한» 아래다 — **6회차가 산 것의 청구서**(이 행 안 · 10회차 몫)

두 사람이 각자 다른 자로 같은 것을 적었다:
· DJ **6** — «프레임 3~8 검출 알 27개 중 **22개가 지름 5~11px**(발원 코인 Ø50 대비 **10~22%**)이고
  최댓값도 13px(26%) — **판독 최소선(통상 14~16px) 미달**» ⇒ 처방 «최소 지름을 코인 대비 **28~32%**(14~16px)로»
· DK **7** — «#4 ≈12px → #8 ≈6px 로 **50% 축소**(코인 Ø32 대비 37%→19%) · 후반 알이 카드 베이지 배경과
  **판독 경계**» ⇒ 처방 «소멸 전 지름 하한을 고정, 축소 대신 알파만»
⚑ **이것은 6회차가 각도 예산을 사려고 지불한 값이다** — 6회차가 알 Ø20.4 → **16.3**(`--burst-fit` .18)로
눌렀고 두 사람이 그 청구서를 각각 들고 왔다. **그리고 이제 갚을 돈이 있다**: 6회차가 세운 [E2] 장부가
**흑자 85.0°**(요구 169.53° ≤ 공급 254.53°)라 크기 쪽으로 되돌려 쓸 여유가 실재한다. **10회차가 이것을 쓴다.**

#### ㉰ 씬 B ④ 배수 라벨 가림 — **두 사람이 독립으로 적었고 자로 확인해 880 으로 등재했다**

· DJ «#7·#8 에서 알(Ø19 · 중심 (556,736)·(562,703))이 «×100/×1,000» 라벨 bbox(x471–619, y682–716)와 **직접 겹침**»
· DK «#8 에서 «×100» 텍스트 중심(518,697) ↔ 근접 알 중심(508,696) 거리 **≈10px**(알 반지름 ~11px)»
**자로 재서 확인했다**(`scratch/relicocc` · 시드 20260902): 175ms 까지 간격 322~335px 로 **0%** 인데
**250ms 에 «×10» 칸 덮임 4.94% · 간격 0px**(320ms 4.16%). 칸 이름과 좌표계는 세 자가 서로 달랐지만
**현상은 실재**다. ⚑ **뿌리는 «닿을 수 없는 잉크»** — 호스트는 `#rwBasin`(`.rw-bowl > .rw-mid > #rwBasin`)
이고 바는 `.rw-bowl > #rwMulBar` 로 **형제**다(`basin.contains(bar) === false` 실측). `fxbKeepHoles`(816)는
«신고된 셀렉터에 걸리는 **자손**뿐» 이라 `--burst-keep` 으로 **못 닿는다**. 그리고 이 바는
**867 이 오늘 «들보↔격자 벽» 에서 «수반/소환 버튼» 블록으로 옮긴 뒤** 처음 버스트 사거리(끝 반경 평균
280.3 · 최대 345.9px) 안에 들어왔다. ⇒ **880 등재**(처방 갈래 둘·자 세우는 순서까지 그 행에).
⚠ **838 이 이 자리를 안 고치는 이유**: 838 은 «요소 대상» 축이고 씬 B(점 대상)는 **대조군**이다
(7·8회차가 877·873·874 를 같은 이유로 넘긴 자리 · 브리핑도 «씬 B 를 정답으로 삼지 마라» 를 명시했다).

#### 기각 1건 — DK 씬 B ① «320ms 에도 신규 알이 태어난다(스폰 창 = 수명의 84%)»

**자로는 기각이다.** 알 수는 0ms **11개** → 320ms **10개** 로 **줄기만** 한다
(`cap681` r838h 표의 «안 보이는 알» 0·0·0·0·1·1·1·2 — 늘어나는 칸이 없다).
DK 가 «신규» 로 읽은 것은 그릇 림 근방에서 **위상이 늦은 알이 배경에서 분리돼 보이기 시작한 것**이다.
⚠ 그래서 DK 의 ④ «스폰 창 비율이 씬 A 29% ↔ 씬 B 84% = 2.9배 차» 도 전제가 사라진다 — 두 씬 다 단발이다.

### 10회차 (2026-09-03, 같은 세션 sess-1558-25897) — **제품 `index.html` 0줄(범위의 끝까지 열어 보고 자가 기각 → 되돌렸다)** · **881·882 등재 · 라운드 마감**

9회차가 «10회차의 유일한 몫» 으로 남긴 ㉯(② 알 크기)를 갚으러 갔다. 근거는 6회차가 세운 [E2] 장부의
**흑자 85.0°** 였다 — «각도 예산을 사려고 크기를 지불했으니, 흑자로 되돌려 쓸 수 있다».
손잡이는 하나뿐이고 **범위도 짧다**: `--burst-fit` 은 `FXB_FITS`(0.22)가 상한이라(호스트는 «좁혀» 만 신고한다)
현행 .18 에서 **.22 가 범위의 끝**이다 = 6회차가 누른 것을 통째로 되돌리는 것.

**세 판을 다 만들어 재 봤다**(`travel838` 재시드판 · 873 이후라 단일 실행 대조가 정당하다):

| fit | 알 Ø | 사거리 평균/중앙/최소 | 빈 각(C3) | 이웃 IoU(C2) | C1 | [E2] 흑자 |
|---|---|---|---|---|---|---|
| **.18(현행)** | 16.22 | 2.39 / 1.97 / 1.46 | **105.5°** | 0.56 | ×3.45 | 85.0° |
| .20 | 17.32 | 2.21 / 1.81 / 1.38 | **120.2°** | 0.58 | ×3.43 | 55.3° |
| .22(범위 끝) | 18.51 | 2.03 / 1.63 / 1.29 | **120.2°** | 0.61 | ×3.40 | 38.9° |

.22 를 제품에 실제로 넣고 자를 돌렸다 — **19항 중 18항이 초록**이고 빨간 하나는 R3a(«6회차의 두 신고가
제품에 있다»)뿐이었다. 즉 **문턱은 하나도 안 깨진다.** 그런데도 되돌렸다:

#### ⚠⚠ 기각 이유 — **«문턱이 안 깨진다» 와 «채점이 오른다» 는 다른 말이다.** 사는 것과 파는 것이 뒤집혀 있었다.

9회차 채점에서 **씬 A 의 binding 축은 ③ 이다** — 두 사람이 **각각 6** 을 매겼고(② 는 DJ 6 / DK **7**),
③ 의 1순위 근거가 바로 **빈 각·비대칭**이다. fit 을 열면 그 축이 **105.5° → 120.2°(+14.7°)** 로 간다
(기계: `fxbFuse(sx, sy, sz/2)` 가 알 반지름으로 융합을 보므로 알이 커질수록 굴림이 잦아지고, 굴린 알은
막힌 쐐기의 **경계 바로 바깥**에 쌓인다 — 7회차가 이미 판 그 기계다). 그리고 이 행의 **주제 자신**(사거리)도
2.39 → 2.03(−15%) · 중앙값 1.97 → 1.63(−17%)으로 내려간다.
⇒ **«② 를 6 → 7 로 올리려고 ③ 을 6 → 5 로 내리는 거래»** 이고, 최저 점수제에서는 그 거래가 **점수를 내린다**.

#### 그리고 결정적으로 — **범위 안에는 애초에 답이 없었다.**

DJ 의 하한은 «**최소** 지름이 코인 Ø50 의 28~32% = 14~16px» 다. 봉우리는 이미 하한 안이다(16.22 = **32%**) —
어긋난 것은 **꼬리**다: `fxSpark`/`fxSparkE` 의 scale 이 18% 에서 1.0 → 100% 에서 **0.5** 로 내려가
320ms 에 Ø**9.5**(19%)가 된다. **.22 에서도 꼬리는 Ø12.0(24%)** 로 하한에 못 닿는다.
⚑ 즉 ② 의 뿌리는 «기본 크기» 가 아니라 **scale 감쇠 채널**이고, 그것은 **877 이 «크기·알파 채널은 fxSpark 와
한 값도 안 다르다» 고 명시적으로 비워 둔 자리**다(681 봉투 [B4]·[B7] 과 660 아이콘 몸이 그 값 위에 서 있다).
⇒ **881 로 등재**했다(처방 갈래 둘 · 자 세우는 순서 · 위 표 전문 포함).

#### ⚑⚑ 그리고 ③ 의 뿌리를 **882** 로 등재했다 — 이 행이 아홉 회차 만에 도달한 결론이다

자·DJ·DK 셋이 각각 낸 **×2.90 / ×2.94 / ×2.7** 이 **가둠 상자의 종횡비 310÷106 = ×2.925** 다(§4 9회차 ㉮).
838 안에서 쓸 수 있는 수는 **다 써 봤고 자가 다 기각했다** — 재파종(7회차) · 끝점 융합(8회차) · 반경 캡(8회차) ·
그리고 이번 회차의 크기(10회차). 남은 지렛대 셋은 전부 **상자 자신**이고 = **23 훈련 카드 기하**(686·670·688 축)라
이 lock 밖이다. **882 가 닫히면 838 을 2차 라운드(+10회 리셋)로 다시 잡아 ③ 을 재채점하는 것이 회수 경로다.**

**게이트**: `verify838` **19/19**(되돌린 뒤 · 넣었을 때는 18/19 로 R3a 만 빨강) · `verify818` **41/41** ·
**SMOKE PASS** · `verifyProgress` 0. **이관 0벌 · 제품 0줄.** `git diff --stat` 이 **빈 출력**인 것으로 되돌림을 확인했다.
**채점은 안 돌렸다** — 제품이 0줄이라 9회차의 DJ 5 / DK 6 이 그대로 선다(지시서 [3]-(가)/(다) 규약).

#### ⏹ 라운드 마감 — **⏸ 보류(10회 · 최고 6점)**

지시서 [3]-(나)/(다)의 «화면당 누적 10회» 상한에 닿았다(1~5회차 워커 C · 6 워커 B · 7 워커 C · 8 워커 D ·
**9·10 워커 D**). 최고 점수는 **6**(9회차 DK · 씬 A 최저 6)이고 통과선 8 에 못 미친다.
**남은 감점의 소재가 이 행 밖임을 자로 확정했으므로**(③ → 882 · ② → 881 · 씬 B ④ → 880 · 씬 B ① 속도 → 877 완료)
CSS·FX 상수로 더 밀 자리는 없다. 아홉 회차가 실제로 회수한 것은 아래 §5 머리말의 자 기준 여덟 축이다.

## §5 남은 문제 (**10회차 기준** · 회차 **10/10 — 상한 소진 · ⏸ 보류(최고 6점)** · 남은 것은 전부 880·881·882 로 이관됐다)

⚑⚑ **7회차가 이 절의 읽는 법을 바꿨다 — 아래 «단조로 올랐다» 를 단일 실행 대조로 읽지 마라(873).**
자가 무변경 트리에서 `C1` ±15% · `A4` ±14% 로 흔들린다(§4 7회차 절). 촘촘한 축은 `C3`(진폭 1.2°) 뿐이다.

**자 기준은 여섯 회차 내내 단조로 올랐고**(사거리 0.41 → **2.30 몸길이** · 무리 반경비 ×1.24 → **×3.20** ·
이웃 장 최대 IoU 0.78 → **0.54** · 빈 각 319° → **103.8°** · 발원에 붙박인 알 5~6 → **0** ·
반경 예산 **적자 44° → 흑자 70.6°** · 스필·가림은 여섯 회차 내내 **0**), **통과선 8 은 아직 못 넘었다**
(5회차까지 최고 5 — 2회차 CZ. **6회차는 채점을 못 돌렸다**: 회차 예산 안에서 예산 축을 닫는 데 썼고,
다음 세션이 7회차 캡처·비평 2인부터 이어서 센다. 브리핑은 5회차와 같은 것을 쓰되 «반경 예산이 흑자로
돌아섰다» 를 알리지 말 것 — 그 회신이 이 회차의 검산이다).
비평 6쌍(12인)이 남긴 것은 **한 갈래로 수렴**했고, 그중 하나는 6회차가 닫았다:

1. ~~**반경 예산(=이 행의 몫 · 1순위).**~~ — **6회차가 닫았다**(§4 6회차 절). DF·DG 가 낸 지렛대 둘
   (ⓐ 알 수 14 → 10 · ⓑ 알 크기)을 **둘 다** 썼고, DG 의 손익분기 산수를 자 [E2] 로 세웠다
   (요구 호 186.1° ≤ 공급 호 256.8° = **흑자 70.6°** · 되돌리면 314.8° > 250.2° 로 적자 복귀).
   ⚑ **7회차 추가** — 5회차의 그 1순위는 7회차 채점 2인(DH·DI) 중 **아무도 1순위로 안 적었다** = 회수 확인.
   ⚠ **남은 반쪽 — 단련·룬**: 같은 두 신고를 그 둘에도 걸려면 **870**(`verify818` [C1] 플레이키 · 등재 경쟁으로 868 → 870)이
   먼저 닫혀야 한다(6회차 절 ⚠⚠ · 344 대조로 «내 것이 아님» 확인 완료). 660 의 «세 탭 한 어휘» 는
   그때 회수한다.
2. ~~**속도 이즈아웃(=이 행 밖 · 681 의 몫).**~~ — **877 로 등재했다(8회차, 2026-09-03)**. 아래가 등재 근거다.
   네 사람(DA·DE·DF·DG)이 «등속이다 · 45→70ms 가 같은 그림» 을 적었고
   처방이 전부 `v(t) = v₀·e^(−t/τ)` 꼴이다. 그 곡선은 **공용 `@keyframes fxSpark`**(09·12·17·장비·코스튬이 같이 쓴다)이고
   **681 이 아홉 회차 동안 고쳐 ⏸ 보류로 닫은 자리**다 — 이 행에서 건드리면 남의 화면 다섯을 같이 바꾼다.
   ⇒ **새 번호로 등재하는 것이 맞다**(838 이 619 에서 갈라져 나온 것과 같은 꼴). 자·수치는 이 문서 §4 5회차 절에 다 있다.
3. **어휘 대조는 씬 B 를 기준으로 삼지 마라 — 그쪽도 결함이다.** DG·DF 가 씬 B 의 발원이 «화폐 아이콘이 아니라
   가마솥/그릇»(115.7px = 아이콘 Ø의 4.1배 떨어짐)이라고 각각 적었다. 씬 A 를 «B 처럼» 만드는 것이 목표가 아니다.

4. ~~**자 플레이키(873) — 다음 회차의 1순위.**~~ — **873 이 닫았다(2026-09-03, sess-1243-6374 워커 C · 제품 0줄)**:
   `travel838` 이 트리거 **직전에 재시드**한다 ⇒ 동시 4실행이 한 자리까지 같다(열한 축 진폭 **0**).
   ⚠ **8회차는 새 기준값과 견줘라**(재시드가 버스트를 수열의 고정된 자리에 앉혀 종전 순차값과도 다르다):
   **A1 2.36 · A2 1.94 · A4 1.44 · A3 38.5 · C1 ×3.41 · C2 0.52 · C3 105.47° · E2 171.28°/254.53°**
   (종전 2.15 · 1.74 · 1.21 · 35.89 · 3.27 · 0.56 · 103.24 · 186.14). 문턱은 한 칸도 안 옮겼고 C1 여유는
   6.7% → **13.67%** 로 스스로 풀렸다. **이제 단일 실행 대조가 정당하다.** 상세 `docs/review/873-자플레이키재시드.md`.<br>
   ←(아래는 등재 당시 기록) 무변경 트리 5회에 `C1` ×3.20~3.69 · `A4` 1.07~1.41 ·
   `E2` 요구 호 170.99~188.44°. 처방은 저장소 안에 있다(`cap681.js` 의 «트리거 직전 재시드» 규약을
   `travel838.js` 가 안 따른다 — LESSONS 666-⑧). ⚠ `C1` 문턱 3.00 이 무변경 최저 3.20 에 붙어 있다.
   **이것을 안 닫고 8회차를 돌면 «고쳤는데 내려간 것처럼» 또는 그 반대로 읽는다** — 7회차가 실제로 그랬다.

5. **씨앗 각 재파종은 이미 기각됐다 — 다시 파지 마라(§4 7회차 절).** DH·DI 가 각각 처방한 «쓸 수 있는 호에
   등간격» 은 두 판 다 자가 기각했다(빈 각 1안 +18° · 2안 +3.8°). 뿌리는 **굴림이 막힌 쐐기의 경계 바로
   바깥에 알을 앉히고 있다는 것**이고, 등간격은 그 알을 반 피치 안쪽으로 물린다. 그리고 «과잉 회피 37.8°»
   라는 두 사람의 전제는 틀렸다 — 못 쓰는 섹터는 잉크 쐐기(68.5°)가 아니라 **A5 가 막는 얕은 방**(103~122°)이다.
   ⇒ 빈 각을 더 줄이려면 A5 를 다시 여는 수밖에 없고, 그것은 5회차가 두 사람 확인 아래 맞바꾼 거래다.

6. ~~**끝점 융합 회피 — 8회차가 만들어 보고 자로 기각했다.**~~ (§4 8회차 절 · 최소 간격 0.13 지름 불변 · 산포 ×2.89 → ×2.97 악화)

7. **③ 방향 비대칭 ×2.9 = 상자 종횡비 ×2.925 — 남은 지렛대는 «상자 자신» 이고 이 행 밖이다(9회차 확정).**
   자·DJ·DK **셋이 각각** 같은 수를 냈다(§4 9회차 ㉮). 대조군 씬 B(상자 없음)는 같은 자에서
   **×1.65 · CV 0.148** 로 2.5배 촘촘하다 ⇒ **산포는 곡선도 씨앗 각도 아니고 «방마다 방의 길이가
   다르다» 의 직접 결과**다(`dv = (room − br) × dFrac` · 3회차가 그렇게 뽑았고 그것이 5회차의 A5·
   7회차의 재파종 기각·8회차의 융합 기각을 전부 설명한다).
   **이 행 안에 남은 수는 없다** — 세 판을 이미 만들어 보고 자가 기각했다:
   · 재파종(7회차) 빈 각 +18°/+3.8° · 끝점 융합(8회차) 산포 ×2.89 → ×2.97 · **반경 캡**(8회차) `C1` < 3.00.
   ⇒ **지렛대는 셋**이고 전부 **23 훈련 카드의 기하**다(= 다른 행):
     ⓐ **버튼을 세로로 키운다** — 산포는 `bw/bh` 라 106 → 155 면 ×2.93 → ×2.0(686 이 단련 버튼에서 이미 한 일이다)
     ⓑ **「53」(가격)을 버튼 밖으로** — A5 가 막는 얕은 방 103~122° 가 그만큼 열려 공급 호가 늘고
        우측 부채가 살아난다(670·688 의 «버튼 비용 표기» 축과 같은 자리)
     ⓒ 아무것도 안 한다 — **씬 A 는 «국소 버스트» 로 굳고 ③ 은 6~7 에서 멎는다**(DJ ④ 원문이
        그것을 정확히 적었다: «씬 A 는 국소버스트, 씬 B 는 비행형 — 물리가 다르다»).
   ⚠ **ⓐ·ⓑ 는 이 lock 이 못 잡는다**(23 훈련 카드 레이아웃 = 686·670·688 계열 행의 구간이고,
     686 이 «버튼 세로 확대» 를 이미 통과로 닫아 두었다). **10회차는 ㉯(알 크기)만 갚고 마감한다.**

8. **② 알 크기 — 10회차의 유일한 몫(이 행 안 · 갚을 돈이 있다).**
   DJ 6 «22/27 알이 Ø5~11px = 코인 Ø50 의 10~22% · 판독 최소선 14~16px 미달» ·
   DK 7 «#4 12px → #8 6px = 50% 축소 · 판독 경계». 6회차가 각도 예산을 사려고 `--burst-fit`
   .18 로 Ø20.4 → 16.3 을 지불한 그 값의 청구서다. **[E2] 장부가 흑자 85.0°** 라 되돌려 쓸 여유가 실재한다.
