# 53 가방 팝업 — 회차 기록

> 측정표 `docs/measure/53-가방팝업.md` · 캡처 `docs/review/53-r*.png`
> 스크립트 `tools/scan53.py`(레퍼런스 스캐너) · `tools/cap53.js`(캡처) · `tools/verify53.js`(기능·기하 게이트)
> 담당 세션: **sess-0443-19993 (워커 C, 2026-08-25)** — 이 화면은 이 세션이 처음 구현했다(T3, 이전 기록 없음).

## 껍데기 판정

**(가) 딤 위 가운데 다이얼로그 948×967 + 팝업 «바깥» 하단 탭 스트립 792×93.**
A5 공용 `#modal` 은 `padding:0 91px` 로 폭이 **898 고정**이라 948 이 안 나온다 → 19 `#pfw` · 33 `#ciw` 와 같이 **자체 오버레이 `#bagw`** 로 갔다.

## 1회차 (sess-0443-19993)

레퍼런스를 PIL 로 직접 스캔해 측정표를 만들고 껍데기 + 5×4 격자를 올렸다.

**구현**
- `#bagw` (딤) → `.bg53` (검정 8 + 갈색 림 + r44) → `.bg53-head` 91 → `.bg53-body` 크림 908×848
- 격자 5열 × 4행, `.bg53-c` 등급색 칸
- 진입: 처음엔 «▦ 메뉴 placeholder» 안에 임시 버튼(작업 35 가 만든 선례)

**자체 실측 결과** — 전 요소 측정표와 **Δ≤0.5px**.

**그 회차에 잡은 CSS 함정 2개**
1. `#bagw{padding:100px 0 94px}` 의 flex 중앙정렬이 이미 목표 y(660)를 만드는데 거기에 `transform:translateY(3.5px)` 를 또 걸어 **3px 초과**했다. «중심 오프셋» 을 패딩과 transform 양쪽에 넣지 마라.
2. 갈색 림을 10px 로 잡았더니 본문 폭이 **912**(목표 908)가 됐다. 검정 8 + 림 **12** 여야 좌우 인셋 20 이 나온다.

## 2회차 — **누락 레이어 3장** (가장 큰 감점 요인이었다)

1회차 캡처를 다시 재고, 독립 측정 결과와 대조하니 **레퍼런스에 있는 레이어 3장이 통째로 빠져 있었다.**

| 누락 | ref bbox | 왜 놓쳤나 |
|---|---|---|
| **안내 배너** | x99..980 · y863..962 (882×100) r30 | 반투명 토프라 크림과 색차가 작다. y930 단면에서 `#CFBBA2` 를 봤는데 «글자 배경» 으로 넘겼다 |
| **아이템 패널** | x99..980 · y983..1682 (882×700) r24 | 같은 이유. 격자만 보고 «격자를 담는 상자» 를 못 봤다 |
| **하단 탭 스트립** | x151..942 · y1722..1814 (792×93) | **팝업 «바깥»** 이라 팝업 bbox(744..1710)만 스캔하고 끝냈다 |

> **교훈**: 팝업 bbox 를 찾았다고 스캔을 거기서 멈추면 안 된다. **딤 영역 전체**를 한 번 훑어야
> «팝업 밖에 붙은 레이어»(09 리본 · 05 페이지 화살표 · 34 초록 스트립과 같은 계열)를 놓치지 않는다.
> LESSONS 34-② 의 «레퍼런스에 항상 있는 레이어가 빠지면 ①②③④ 가 한꺼번에 깎인다» 가 그대로 적용된다.

### 2회차에 정정한 측정 오독 — 칸 크기

| | 1회차(오독) | 2회차(정정) |
|---|---|---|
| 칸 크기 | 149 × **159** | **148 × 147** |
| 세로 pitch | **171** | **170** |

칸 중앙 열(x=200)에서 쟀더니 **수량 숫자의 검정 외곽선**(y1156..1157)이 칸 아래 테두리로 잡혔다.
수량은 칸 아래 테두리에 **걸쳐서 ~9px 삐져나온다**. 가림 없는 열(x=410)에서 재면 **999..1145 = 147**.
→ LESSONS 22-① / 04-② «가림 없는 열을 골라 재라» 가 세 번째로 재발했다.

### 2회차에 정정한 딤 계수

1회차는 **02 기본 메인 화면**과 비교해 α = .61 을 냈는데, 02 는 **배경 상태가 다르다**(STAGE·배너 구성).
배경이 같은 상태인 **40 (보스 재도전)** 과 비교하면 휘도비 0.457 로 화면 전역에서 균일 → **α = .545**.
→ 딤 계수는 «아무 레퍼런스» 가 아니라 **배경 상태가 같은 레퍼런스**와 비교해야 한다.

### 2회차에 잡은 CSS 함정 — padding box

탭 스트립을 `.bg53` 기준 `left:85px; margin-top:12px` 로 놨는데 **x +8 · y −7.5** 로 어긋났다.
`.bg53` 은 `border:8px` 이고 **절대배치 자식의 기준은 border box 가 아니라 padding box** 다(LESSONS 03).
→ `left = 151 − 74 = **77**`, `margin-top = 12 + 8 = **20**`.

## 3회차 — **첫 비평 채점** (sess-0844-28398, 워커 C · 죽은 lock 193분 회수)

1·2회차는 **자체 실측만** 하고 비평가를 한 번도 띄우지 않은 채 세션이 죽었다(캡처 `53-r1~r3.png` 만 남아 있었다).
그래서 3회차 = **첫 채점**이다. 회차는 이어서 센다(ROUTINE [-1] 2).

### 채점 결과

| 비평가 | ① | ② | ③ | ④ | ⑤ | ⑥ | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| **A** | 8 | 6 | 8 | 8 | 8 | 4 | **6** |
| **B** | 7 | 6 | 7 | 7 | 7 | 5 | **6** |

껍데기·격자·패널은 두 비평가 모두 **Δ0px** 로 확인했다(팝업 948×967 · 패널 882×700 · 행 pitch 170 · 열 좌단 126/296/466/636/806).
감점은 전부 **글자·아이콘의 «크기»** 한 군데로 모였다.

### 이번 회차에 만든 것 — `tools/cmp53.py` (대조 스캐너)

`scan53.py` 는 레퍼런스 경로가 박혀 있어 우리 캡처를 못 읽는다. **레퍼런스와 캡처를 «같은 코드·같은 임계값»으로**
스캔하는 `cmp53.py` 를 만들었다(`row`/`col`/`bbox`/`ink`, ref y 를 넣으면 캡처는 자동으로 −84). LESSONS 36-④ 그대로
— **스캔 스크립트 하나가 비평 회차 하나보다 싸다.** 아래 판정은 전부 이걸로 냈다.

### 3회차 수정 — 자체 실측으로 **재현된 것만** 반영

| 항목 | ref | 3회차 전 | 조치 | 3회차 후 |
|---|---|---|---|---|
| **수량 숫자 잉크 높이** | **24** | **19** (−21%) | `font-size` 28 → **35** | **24** ✓ |
| **헤더 타이틀 «가방» 잉크 폭** | **82**×43 | **91**×42 (+11%) | `scaleX(.9)` | **82**×42 ✓ |
| **탭 «재화» 잉크 중심 x** | **735** | 741.5 (+6.5) | `.t2` 폭 393 → **380** | **734** ✓ |
| **아이콘 잉크 (7칸 평균)** | **82.3 × 83.0** | 93.4 × 81.4 (폭 +13%) | `font` 80→82 · `scaleX` 1.18→**1.01** | **82.0 × 82.9** ✓ |

> **★ 수량 숫자가 −21% 였던 이유 — LESSONS 70-① 을 그대로 밟았다.**
> 측정표 §4-2 가 «코어 25~26 → font-size ≈ 28» 로 적어 뒀는데, 이 추정은 **한글 잉크비 0.93** 을 쓴 것이다.
> **숫자+쉼표는 캡하이트 기준이라 이 폰트의 잉크비가 ≈0.68** 이다. 28px 를 주면 잉크가 19px 밖에 안 나온다.
> 측정표에 «font-size 추정» 이 적혀 있어도 **숫자면 그려 보고 되맞춰야** 한다.

> **★ 아이콘이 +13% 였던 이유 — 2회차가 «칸 안쪽 밝은 림» 을 아이콘으로 세고 있었다.**
> 2회차 주석은 «ref 평균 91×82» 를 목표로 `scaleX(1.18)` 을 걸었는데, 그 91 은 **칸 fill 만 마스킹해서 나온 값**이라
> 등급 림(6px, fill 과 색이 다르다)이 통째로 잉크에 포함돼 있었다. **fill 과 rim 을 둘 다 빼고** 7칸을 재니
> ref 는 **82.3 × 83.0** 이었다. LESSONS 70-③ «색/면적 마스크로 재라» 와 같은 계열이다.

## 4회차 (sess-0844-28398) — 두 비평가가 **독립적으로 같은 자리를 짚은 것만** 반영

LESSONS 70-③ 의 «독립된 둘이 같은 요소를 짚으면 측정표부터 다시 재라» 를 판정 기준으로 썼다.

| 지적 | A | B | 자체 스캔 판정 | 조치 |
|---|---|---|---|---|
| **헤더 밴드 91 → 93** | ③ +2px | ③ +2px | **채택** — 크림 본문의 `border-top:2.5px` 색이 헤더와 **같은 `#523E3D`** 였다. ref 는 842 에서 헤더가 끝나고 843 부터 곧바로 크림 | 테두리 삭제 + 배너 top 18→**20.5** · 패널 138→**140.5** 로 아래 좌표 유지 → 밴드 끝 **842 = ref** ✓ |
| **«소모품» 잉크 폭 +9%** | ⑥ +9.2% | ②⑥ +9% | **채택** — 109 vs ref 100. 좌단은 맞고 폭만 넓어 중심이 +5.5 밀린 것 | `font` 40 + `scaleX(.863)` + 박스폭 389→**377**(중심까지) → **99×37 = ref 100×37** ✓ |
| **골드 파티클이 캡처를 오염** | ⑤ 재촬영 요구 | ①-3 «재화 잉크의 55% 를 가림» | **채택** — 58 `#fxl` 의 비행 재화가 딤 «위» 를 지나간다 | `cap53.js` 가 `#fxl{display:none}` 을 걸고 찍는다(72 의 대책과 동일) |
| **탭 라벨 세로** | 재화 +4 아래 | 재화 +5 아래 / ④ 소모품은 −3 위 | **부분 채택** — 글리프별 편차가 커서 한쪽에 맞추면 반대쪽이 어긋난다(ref 는 재화 1749..1781 · 소모품 1754..1790 로 잉크 상단이 5px 다른데 우리 폰트는 0이다). **두 라벨 평균 중심**으로만 판정 → ref 1768.5 vs 우리 1770 | `top` 0 → **−2px** (평균 중심 Δ0.5) |
| **활성 알약 높이 −21%** | 지적 없음 | ②-2 «91 → 72» | **기각** — B 가 우리 «코어»(69)를 ref «알약 전체»(91)와 비교했다. 같은 겹끼리 재면 ref 알약 1721..1816 · 우리 1723..1815 로 **Δ2** | — |
| **팝업 외곽선 7 vs 8** | ⑤ +1px | — | **기각** — ref 우측 1006..1013 = **8**. JPEG AA 오독(LESSONS 70-④) | — |
| **탭 트랙 폭 −5px** | ②·③ | — | **기각** — 색 마스크로 재면 양쪽 다 **x151..942 = 792** 로 동일 | — |
| **배너 좌우 여백 3px 좁음** | — | ③-2 | **기각** — ref 배너 가장자리가 소프트해 임계값에 따라 99~104 로 흔들린다. y900 직접 프로브로 양쪽 다 **x99..980** | — |
| **칸 프레임 +3px** | ② +2.2% | — | **기각** — 순색 코어로 재면 양쪽 다 칸 **126..273 = 148** | — |

## 4회차 채점 결과

| 비평가 | ① | ② | ③ | ④ | ⑤ | ⑥ | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| **C** | 8 | 8 | 8 | 8 | 7 | 8 | **8 (통과)** |
| **D** | 8 | 6 | 7 | 8 | 7 | 6 | **6** |

C 는 통과를 줬지만 D 가 6 이라 **미통과**(둘 중 최저가 기준). D 의 ② 6 은 «활성 알약» 과 «수량 자간» 두 개였고,
**둘 다 자체 스캔에서 재현됐다** — 즉 D 가 맞고 **측정표가 틀렸다.**

## 5회차 — 측정표 오기 2건을 잡았다

| 지적 | C | D | 자체 스캔 판정 | 조치 |
|---|---|---|---|---|
| **활성 알약이 작다** | — | ②-1 «코어 76→67» | **채택(수치는 D 도 틀림, 자리는 맞음)** — ref x880 세로 실측: 외곽테 1719 · 카키 1723~1731 · **코어 1734~1807** · 카키 1808~1815 · 외곽테 ~1818 = **알약 100px**. **측정표 §5 의 «420×93» 이 오기**였다. 알약은 트랙(93)과 같은 높이가 아니라 **위·아래로 3.5px 씩 돌출**한다 | `height` 93→**100** · `top` −5→−7.5 → 실측 420×100 @(523,1635) ✓ |
| **수량 숫자가 좁다** | ⑥ −4% | ⑥ «자리당 진행폭 19 vs 25.5» | **채택** — 자리당 진행폭 ref **24** vs 우리 **19.5**. 단 **글리프 잉크 폭은 16~17 로 양쪽이 같다** → 원인은 글자 크기가 아니라 **자간**이다 | `letter-spacing:4px` + 같은 값 `text-indent`(LESSONS A5: 자간은 마지막 글자 뒤에도 붙어 중심을 민다) → 피치 **23~24** ✓ |
| **탭 트랙 다크 인셋 링 누락** | ⑤ «라이트림 3 + 안쪽 어두운 인셋 4» | ⑤ «상단 6px 다크 인셋 링 통째 누락» | **채택(독립 일치)** — ref x220 세로: 라이트림 **1723..1726** · 다크 인셋 **1727..1733** · fill 1734~. 좌측도 x156..159 가 어둡다 | 트랙 테두리 5→**4**(ref 151..154) + `box-shadow:inset 0 7px 0 / inset ±4px 0 0 #453B2F` → 3겹 전부 ref 와 일치 ✓ |
| **팝업 드롭섀도가 ref 에 없다** | ①-우선순위 1 | — | **채택** — ref 는 딤이 팝업 경계 바로 앞까지 **휘도 62~63 으로 완전히 평탄**하다(x20~45). 우리는 좌 26·상 32px 에서 43→21 로 떨어지는 램프가 있었다 | `.bg53` 의 `box-shadow:0 0 24px 10px rgba(0,0,0,.55)` 제거 |
| **탭 라벨 세로** | 소모품 −4 / 재화 +3 | 소모품 4px 위 | **채택** — 자체 실측도 소모품 +3 · 재화 −3 으로 **방향이 서로 반대**였다(글리프별 잉크 편차). 공용 규칙 하나로는 못 맞춘다 | `.t1{top:1px}` · `.t2{top:−5px}` 로 분리 → 두 라벨 모두 **Δ0.0** ✓ |
| 수량 잉크 «높이» 26 vs 24 | ⑥ 25 vs 24 | ⑥ 26 vs 24 | **기각** — 5칸을 재니 ref 도 **24**(c1~c4). c0 만 29 인데 그건 접미 글리프가 높은 것이다. 둥근 글리프의 오버슈트를 캡하이트로 읽은 것 | — |
| 2행 그리드 y −2px | ④ | — | **기각** — ref 행 상단 999/1169/1339/1509 로 pitch 170 균일. C 가 읽은 914/1083/1254/1424 는 AA 경계 | — |
| 아이콘 −5% / 채팅버튼 −5% | — | ②-5 | **기각** — D 는 **최대값**을 비교했다. 7칸 평균은 ref 82.3×83.0 vs 우리 82.0×82.9 로 일치. 이모지와 원본 아트는 글리프별 편차가 달라 최대값은 맞출 수 없다 | — |
| 알약 가로 414 vs 406 | ②-2 −1.0% | ②-3 | **기각** — 색 마스크·행 단면 모두 양쪽 **x523..942 = 420** | — |

> **★ 이 회차의 교훈 — «측정표가 성경이 아니다».**
> 3·4회차는 «비평가 지적을 측정표로 반박» 하는 데 썼는데, 5회차에 반박당한 쪽은 **측정표**였다.
> 알약 93(→100)·트랙 테두리 5(→4)·다크 인셋 링(→누락)이 전부 2회차 측정 오류다.
> **독립된 두 비평가가 같은 요소를 짚으면**(B 3회차 + D 4회차가 «알약», C+D 가 «인셋 링»)
> 그건 노이즈가 아니라 **측정표를 다시 재라는 신호**다(LESSONS 70-③ 그대로).

## 기능 (T3 «기능 연결은 가능한 범위에서»)

레퍼런스의 가방은 **«소모품 · 재화» 2탭**이고 활성 탭이 «재화» 다. 2탭 구조를 그대로 가져오고 내용만 우리 데이터로 매핑했다 — **둘 다 실제 게임 상태를 읽는다(«준비 중» 없음).**

| 탭 | 내용 | 소스 |
|---|---|---|
| **재화** (기본 활성) | 골드 · 다이아 · 유물석 · 마일리지 · 스탯 포인트 + 보유 아이템 재료 | `S.gold/dia/relic/mileage/sp` · `S.own` |
| **소모품** | 던전 입장권 5종 · 상자별 무료 소환권 3종 · 룰렛 남은 횟수 | `S.daily.dun` · `S.daily.freeSum` · `S.daily.spins` |

### 기능 체크 표 (`node tools/verify53.js` → **VERIFY53 PASS 31/31**)

| 눌렀을 때 | 무엇이 바뀌는가 | 결과 |
|---|---|---|
| ▦ 메뉴 → «가방» | `#bagw` 가 열리고 메뉴가 닫힌다 | ✓ |
| «소모품» 탭 | 격자가 소모품 목록으로 교체 + 활성 알약이 왼쪽 칸으로 이동 | ✓ |
| «재화» 탭 | 격자가 재화+재료로 교체 + 알약이 오른쪽(372px)으로 복귀 | ✓ |
| 칸 | 그 항목의 이름·보유 수량 팝업 | ✓ |
| 딤 | 닫힌다 | ✓ |
| `S.gold = 1234` | 골드 칸 수량이 `1234` 로 표시 | ✓ |
| `S.relic` 0 → 500 | 유물석 칸이 **새로 생긴다**(보유 0 이면 안 나온다) | ✓ |
| `S.own[스킬] = {n:9}` | 그 재료 칸이 생기고 수량 `9` | ✓ |
| `S.daily.spins = 3` | 소모품 탭 «룰렛 횟수» 가 `3` | ✓ |
| 등급 섞어 주입 | 칸이 **등급 내림차순**으로 재정렬 | ✓ |
| 보유가 적을 때 | 빈 칸으로 20칸을 채운다(플레이스홀더·배경 없음 = 레퍼런스 r3c3·r3c4) | ✓ |

## 진입점

52 ▦ 메뉴가 «가방» 칸에 남겨 둔 «준비 중» 분기를 **`openBag()` 한 줄로 교체**했다.
(52 주석: «그 화면들이 올라오면 아래 분기 한 줄씩만 갈아끼우면 된다» — 그 인계 지시대로.)
1회차에 만들었던 임시 placeholder 버튼은 52 가 정식 메뉴를 올리면서 사라져 **rebase 때 52 쪽을 살리고 내 임시 경로는 버렸다**(ROUTINE [4]).
`tools/smoke.js` 는 52 가 이미 `#mnw [data-mn]` 을 자동 수집하므로 별도 오프너를 넣지 않았고, 짧은 프레임 잘림 검사 목록에 `#bagw .bg53` · `#bagw .bg53-tabs` 만 추가했다.

## 게이트

- `node tools/verify53.js` → **VERIFY53 PASS 31/31**
- `node tools/smoke.js` → **SMOKE PASS** (`menu:bag` 오프너 포함 · 화면비 6종 잘림 없음)
