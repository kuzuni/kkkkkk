# 634 — `verify356` 캔버스 축([G-b]·[G-g])을 대표 4화면에서 **전 화면**으로

> 작업 단위 634 · T4 「게이트 커버리지(잠복)」 · 1회차 · 세션 `sess-0811-22774`(루틴 워커 A, 2026-09-01)
> 지시서 [3]-**(가) 기계적/게이트 작업** — 레퍼런스 대조가 아니라 «자가 무엇을 보는가» 라서 **비평가 없음**.
> **제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/verify356.js` 한 파일과
> 신설한 `tools/probe356r28.js` 다.

---

## §1 무엇이 결함이었나 — «0건» 이 아니라 «안 보는 화면 67개»

23회차가 세운 **캔버스 축** 두 항은 대표 4화면(`03 레이드`·`26 펫`·`12 소환 결과(펫)`·`09 일괄 강화 결과`)
안에서만 돌고 있었다.

| 항 | 무엇을 보는가 |
|---|---|
| `[G-b]` | `ctx.drawImage(…, dw, dh)` 가 **원본 종횡을 어기는** 그리기 |
| `[G-g]` | `ctx.scale/transform/setTransform` 의 **비균등 컨텍스트 변환** |

접은 근거는 26회차 인계문이었다:

> « `[G]`·`[I]` 는 `[A]` 가 이미 도는 축의 «다른 각도» 라 대표 화면으로 접어도 되지만,
>   의사 요소는 `[A]` 가 **구조적으로 못 보는** 노드라 대표 화면으로 접으면 그만큼이 그냥 구멍이다. »

**기준은 옳고 분류가 틀렸다.** `[I]`(시간 축)는 정말로 «`[A]` 가 도는 **그 노드**의 다른 위상» 이다.
그런데 `[G]` 가 보는 것은 캔버스 **안에 구워진 픽셀**이고, 23회차 자신이 그 절의 주석에
«`getComputedStyle` 로는 영영 안 보인다» 고 적어 뒀다 — 즉 **`[A]` 가 구조적으로 못 보는 자리**다.
26회차의 기준을 그대로 대면 이 축이야말로 «접으면 구멍» 이다.

## §2 재현 — 구멍의 크기

`tools/probe356r28.js` 신설(6/6 PASS). 훅은 새로 안 적고 `probe356r23.initHook` 을 그대로 받아 썼다(13회차 [R12]).

| 잰 것 | 값 |
|---|---|
| 대표 4화면(`[G]` 의 현행 범위) `drawImage` | **15,276건** (등재문 실측) |
| 스윕 전 화면 `drawImage` | **274,492건** · **71화면** (등재문 실측) |
| 밖에 놓여 있던 화면 | **67개** · 호출 **×18.0** (02 메인 · A1 탭바 · 06 장비 · 07 스킬 · 50 코스튬 · 23 훈련 … 외 61) |
| 지금 트리의 결함 | 비균등 그리기 **0자리** · 비균등 CTM **0회** ⇒ **결함은 없고 감시만 접혀 있었다** |

**«구멍» 이 말뿐이 아니라는 실측** — `02 메인` 한 화면에서:

| 주입 | `[L]`(캔버스 축) | `[A]`(`scan356.COLLECT`) |
|---|---|---|
| 없음 (지금 트리) | 0자리 (`drawImage` 4,790건) | 0자리 / 29노드 |
| **세로만 ×0.6** | **3자리** (최악 **×1.6667**) | **0자리** ← 구조적으로 못 본다 |
| 등방 ×2 (음성항) | 0자리 (4,464건) | 0자리 |
| `ctx.scale(1, 0.6)` 1회 | **1회**로 잡음 | — |

세 번째 줄이 이 작업의 근거 전부다. 같은 주입을 `[A]` 가 **0자리**로 읽는다 ⇒ `[G]` 는
«`[A]` 가 도는 축의 다른 각도» 가 아니고, 대표 화면으로 접은 만큼이 **그냥 구멍**이었다.

## §3 처방 — 값이 공짜다(스윕을 한 벌 더 안 돈다)

`[J]`(26회차 의사 축)·`[F]`(14회차 프레임 축)와 **같은 손**이다. 화면 진입 비용(컨텍스트 · `goto` ·
단계 클릭)이 이 자의 거의 전부이므로, 이미 여는 페이지에 훅을 얹고 `evaluate` 를 한 번 더 한다.

* `sweep()` 의 `newPage()` 뒤 · **`goto` 앞**에 `page.addInitScript(R23.initHook, TOL)`
  (⚠ `drawImage` 는 프로토타입에 있으므로 컨텍스트가 만들어지기 전에 갈아 끼워야 전수다 — 23회차 주석)
* 화면마다 `page.evaluate(() => window.__r23)` 를 **리사이즈 전**에 한 번 꺼내 `seenG` 에 접는다
* ⇒ 71화면 커버리지가 **evaluate 71번** 값이 된다

**`[G]` 를 지우지 않았다.** 그 절은 616 «직전» 트리 되돌림(`[G-c]`)을 이고 있고, 되돌림은 대표
화면에서 도는 것이 맞다. 넓힌 것은 «지금 트리의 판정» 하나다.

### 항을 넷으로 세웠다

| 항 | 무엇을 묻나 |
|---|---|
| `[L-a]` **전제** | 훅이 정말 돌았는가(`calls > 0` · 훅 예외 0). 0 이면 아래 초록은 전부 헛초록이다 |
| `[L-b]` **커버리지** | «캔버스가 도는 화면» == `SCREENS.length`. **줄면 빨강** |
| `[L-c]`/`[L-c2]` **판정** | 전 화면 비균등 그리기 0자리 · 비균등 CTM 0회 |
| `[L-d]`~`[L-d4]` **되돌림·[A] 대조·음성항** | 세로만 ×0.6 → 잡힌다 / 같은 주입을 `[A]` 는 0자리 / 등방 ×2 → 0자리 / `ctx.scale(1,.6)` → 잡힌다 |

⚑ **`[L-b]` 의 방향은 `[B]`·`[F]` 래칫과 반대다.** 저 둘은 «결함이 늘면 빨강», 이것은
**«커버리지가 줄면 빨강»** 이다. 판정값이 0자리라 «0 이 늘었다» 로는 축이 꺼진 것을 **영영 못 본다** —
356 이 스물여덟 회차 동안 싸운 «못 봐서 0» 의 다섯 번째 모양이다.

⚠ **하한을 손으로 안 적었다.** 전투 캔버스는 어느 화면에서도 살아 있어 실측이 71/71 이므로 기대값은
`SCREENS.length` **그 자체**다(402 «표가 아니라 파생»). 화면 목록이 늘면 기대도 같이 는다.

## §4 남긴 것 — 곁다리 관측(번호 안 태웠다)

1. **1600 프레임의 캔버스는 아직 아무도 안 본다.** `[L]` 의 훅 스냅샷은 `[F]` 리사이즈 **전**에
   꺼낸다 — 이 축을 `[A]`·`[G]` 와 같은 프레임(2280)에서 재야 «`[A]` 가 도는 그 순간에 `[A]` 가 못
   보는 것» 이라는 말이 실측이 되기 때문이다. 1600 에서만 눌리는 캔버스는 `[F]` 의 물음인데 `[F]` 는
   DOM 축만 본다. 스냅샷을 리사이즈 뒤에 한 번 더 꺼내면 그 자리도 공짜로 닫힌다(호출부 한 줄).
2. **`drawFrameTh`(`index.html` 18121~18122) 의 두 축 따로 반올림** — 등재문이 적어 둔 그대로다.
   `dw = round(fr[2]·k)` / `dh = round(fr[3]·k)` 라 작은 프레임의 비가 최대 **1.0159**(+1.59%)까지
   흔들린다. `TOL 0.02` 안이라 지금은 0자리 — **누가 TOL 을 조이려 하면 여기부터 닫아야 한다**
   (비를 먼저 정하고 한 축을 그 비로 파생).

## §5 번호·이름 주의

이 실측을 낸 세션(`sess-0613-16031`)은 그것을 `probe356r27`·`[K]` 로 올리려다 **lock 을 놓쳤고**
(게이트 실행 1시간 45분 동안 push 를 안 해 90분 규칙에 걸렸다 — 지시서 [1] «회차마다 커밋·push»),
그 사이 `sess-0732-5145` 가 **다른 27회차**(의사 «이름» 축)를 같은 이름으로 먼저 올렸다.
⇒ 이 작업은 **`probe356r28`·`[L]`** 이다. 상류 것은 한 글자도 안 건드렸다.

## §6 검증

(§7 에 채운다 — 게이트 실행 결과)
