# 619 — 연속 강화 때 이펙트가 **매 강화마다** 터진다 (훈련 · 단련 · 룬)

> 주인 지시(2026-09-01 11:25 KST): «이펙트같은거 연속 강화할때 계속 터져야함. 훈련이나 단련 룬 전부»
> 세션 `sess-0226-11989` (워커 B) · 지시서 [3]-**(다) 연출 작업**(연속 프레임 캡처 · 비평가 2인 · 최저 8 통과)

## §1 재현 — 등재문이 그대로 맞았다 (`tools/probe619.js` 신설)

338 규칙대로 **처방 전에 제품에게 먼저 물었다.** 자는 «함수를 불렀는가» 가 아니라 **`#fxl` 에 붙은 노드**다
(`fxBurst`·`fxSpend` 는 상한·스로틀에서 조용히 빠지므로 호출 수를 세면 헛초록이 된다 — 350 «찍힌 픽셀로
물어라» 의 DOM 판).

수리 «전» (홀드 2.6초 · `P619_HOLD=2600`):

| 자리 | 강화 N | 발화가 붙은 강화 | 비율 | 첫 발 | 반복 N | 반복 발화 | 반복 비율 | `#fxl` 최대 |
|---|---|---|---|---|---|---|---|---|
| 23 훈련 카드 | 23 | 1 | 0.04 | 1 | 22 | **0** | **0.00** | 7 |
| 룬 [강화] | 22 | 1 | 0.05 | 1 | 21 | **0** | **0.00** | 13 |
| 단련 [단련] | 23 | 1 | 0.04 | 1 | 22 | **0** | **0.00** | 11 |

⇒ 결손은 등재문이 의심한 «스로틀에 먹힌다» 가 **아니었다**. 반복분에는 발화가 **아예 없었다** —
64 가 «반복분 연출은 정지할 때 한 번»(FXMAX 포화 대책)으로 못 박았고, 297·491 이 그 규약을 룬·단련에
그대로 물려받은 자리다. 주인 지시가 그 판단을 뒤집는다.

⚑ **옛 규약의 근거는 여전히 옳다** — 60ms 간격으로 10개짜리 버스트를 쌓으면 `#fxl` 이 FXMAX(120)로 막혀
«손맛이 아니라 잡음» 이 되고, 막히는 순간 발화가 **조용히 빠져** 오히려 «간헐로 터지는» 그림이 된다.
그래서 지시 ② 대로 **틱당 1회 발화 + 풀·동시 상한**으로 풀었다(아래 §2).

## §2 처방 — 부품은 한 곳, 상한은 풀이 지킨다

`index.html` 에 공용 부품 `upFx(key, host, cur, n, noFlash)` 하나를 세우고 세 자리가 그것을 부른다.

* 부품은 **`rtFirstFx`(491 7회차)와 같은 두 조각**이다 — 흰 플래시(`fxFlash`) + 화폐 알갱이(`fxSpend`),
  못 쏘면 종전 앰버 버스트(`fxBurst(el, FXPAL.up, cnt)`). 빠뜨린 셋(`fxHitEl`·델타 문구)의 이유는 491·58 의
  머리말 그대로다(488 `jz-hb` 와 `transform` 을 다툰다 · 같은 수가 두 곳에서 말해진다).
* **풀·동시 상한** — 한 자리(key)가 `#fxl` 에서 동시에 차지하는 노드를 `UPFX_CAP = 36`(FXMAX 의 30%)으로
  묶고, 넘치면 **내가 만든 세대 중 가장 오래된 것부터** 걷는다. 남의 연출(획득 비행·토스트·플로터)은
  한 노드도 안 건드린다. 지시 ② 의 «이펙트를 겹쳐 새로 쏘지 못하는 구조면 재시작 가능한 구조로» 가 이것이다.
* 반복 1틱당 파티클은 **4개**(`UPFX_N` — 1회차 6 → 4회차 4) · 첫 발은 10개 + «타격 순간» 몫 4개(`UPFX_NOW`).
* 첫 발도 **같은 부품·같은 키**로 지난다(`rtFirstFx` 가 개수 10 만 정한다) — 부품이 두 벌이면 첫 발과
  반복분이 다른 그림이 되어 ④ 일관성이 깨지고, 402 가 겪은 «표 두 벌» 부패가 된다.

⚠ **룬만 «강화» 의 뜻이 다르다** — 확률 판정이라 시도 ≠ 강화다. 성공 세트(플래시+파티클)를 실패에 얹으면
«레벨이 올랐다» 는 **거짓 신호**가 되므로 제품은 **성공한 시도에만** 건다. 실패의 회당 피드백은 종전대로
흔들림 + «실패» 플로터다(488).

⚠ **324 는 한 줄도 안 건드렸다**(지시 ③) — 전투력 «+X» 토스트의 홀드 묶음(합계 1장)은 주인 승인 설계다.
게이트 [D1] 이 그것을 지킨다.

## §3 게이트 — `tools/verify619.js` 신설 (28/28)

절이 여섯이다: **[A] 선언**(부품 한 곳 · 세 자리가 부른다 · 첫 발도 같은 부품) · **[B] 실동작**(강화 N ↔ 발화 N) ·
**[C] 상한**(상한 때문에 빈 발화 0 · `#fxl` < FXMAX) · **[D] 324 불변** · **[E] 가독성**(스파크가 글자 위에 앉는 비율 ≤5% · 되돌림 포함) · **[R] 되돌림**.

수리 «후» (같은 자, 홀드 2.4초):

| 자리 | 강화 N | 발화 | 비율 | `#fxl` 동시 최대 | 빈 발화 |
|---|---|---|---|---|---|
| 23 훈련 카드 | 20 | 20 | **1.00** | 45 | 0 |
| 룬 [강화](성공 축) | 9 | 9 | **1.00** | 31 | 0 |
| 단련 [단련] | 19 | 19 | **1.00** | 45 | 0 |

**[R] 되돌림** — `upFx` 를 무력화한 사본에서 같은 자가 0.09 / 0.00 / 0.00 으로 **빨개지고**, 원복하면 20/20 으로
다시 초록이다(R2). 무르게 푼 수리가 아님을 이 두 줄이 못박는다.

## §4 이관 — `verify491` 3항 (72/72 유지)

619 가 부품을 옮기고 «홀드 중 화면» 을 바꾸자 491 의 세 항이 빨개졌다. **묻는 뜻은 그대로 두고 자리만 옮겼다**(333 처방):

* **[7-d0]** — 종전 regex 는 `rtFirstFx` 안의 `fxFlash → fxSpend → fxBurst` 를 글자로 물었다. 그 두 조각이
  `upFx()` 로 모였으므로 ① 첫 발 자리에서 부른다 ② 첫 발은 10개다 ③ 부품 안에 갈아 끼움 규칙이 그대로 있다,
  셋을 이어서 묻는다. 폴백이 사라지면 그대로 빨개진다.
* **[8-a]** — «800ms 에는 첫 발 오버레이가 이미 꺼져 있다(이 항의 전제)» 를 **개수** 로 세던 자리다.
  619 이후에는 회당 발화가 계속 새로 뜨므로 **나이**로 센다(첫 발의 수명은 `fxBye` 500ms — 그보다 늙은
  `fx-flash` 가 하나라도 있으면 전제가 깨진 것이다). 실측 «flash=2 중 500ms 초과 **0**».
* **[R4-a]** — 같은 자(나이). 이 절이 묻는 축은 **호스트 자신의 밝기**(`.jz-hdn`)이고, 619 의 오버레이는
  «호스트 위에 잠깐 뜨는 별개 노드» 라 그 축을 대신하지 못한다. 밝기가 살아 있으면 그대로 빨개진다.

회귀(무수정): `verify64` 44/44 · `verify488` 76/76 · `verify583` 36/36 · `verify518` 42/42 ·
`verify349` 29/29 · `verify592` 35/35 · **SMOKE PASS**.

⚠ **곁다리 관측 1건 — `verify324` [5]**(«단련 temperUp — 전투력이 안 올랐다(재료 준비 실패)» 33/34).
**수리 전 커밋(`257584e^`)에서도 같은 33/34** 임을 직접 대조 실행으로 확인했다(338·344 규칙) — 내 변경과
무관하고 내 작업 단위가 아니라 안 고쳤다. **623** 으로 등재한다(620~622 는 다른 세션이 먼저 썼다).

## §5 회차 기록

### 1회차 (2026-09-01) — 제품 · 게이트
재현 → 처방 → `verify619` 25/25 → `verify491` 이관 72/72 → 회귀 6종 → SMOKE PASS.

### 2회차 — 연속 프레임 캡처 · 비평 2인 (`tools/cap619.js` 신설) — **DJ 3 / DK 5 (미달)**
세 화면 × 8장(f0 = 누르기 전 · f1~f7 = 꾹 누른 채, 실측 간격 91~1,823ms). 캡처는 **커밋하지 않는다**
(`docs/review/*.png` 는 .gitignore — 서두 이력 정리 규약).

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DJ | 6 | **3** | 6 | **3** | 룬 성공 5회 중 4회가 파티클 없이 텍스트만 |
| DK | 6 | **5** | 6 | **5** | 룬 젬 클러스터가 진행 바의 25~30% 를 3프레임 연속 가린다 |

**2인 공통 지적 두 개**(한 사람만 낸 것은 이 회차에서 안 고쳤다):
1. **회당 화폐 알갱이가 쌓여 가린다** — 알갱이 잉크는 50~70px(호스트의 5~7%)이고 «날아와 도착» 하는 그림이라
   60ms 마다 되풀이하면 룬 진행 바를 3프레임 연속 25~30%(DK), 훈련 수치를 30~40%(DK·DJ) 덮는다.
2. **세 화면의 어휘가 갈린다** — 훈련은 궤도형 스파크, 룬·단련은 알갱이 클러스터로 읽힌다(DJ ④ 3 · DK ④ 5~6).

### 3회차 — 반복분은 «스파크 고정» · 첫 발에 «타격 순간» 몫 — **DL 6 / DM 5 (미달)**
2회차 처방: **반복분에서 화폐 알갱이를 뺐다**(`cur` 를 안 넘긴다 = 앰버 스파크 16~20px 로 고정).
583 규약은 **첫 발에 그대로** 남는다 — «무엇으로 샀는가» 는 한 사건에 한 번 말하면 되고, 반복분이 말하는 것은 «또 올랐다» 다.

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DL | 7 | 6 | 7 | **6** | 룬 첫 발(성공)에 플래시·파티클 0개 |
| DM | 5 | 5 | 5 | **5** | 세 화면 모두 f1(첫 강화)에 이펙트가 죽어 있다 |

⇒ 가림 지적은 사라졌다(DM 「단련 f2~f7 전 프레임 0% 겹침」 · DL 「겹침 육안상 0%」). **남은 하나가 두 사람 공통**이고
2회차 DJ·DK 의 「f1 파티클 0개」와 같은 자리다 — **비평가 넷이 두 회차에 걸쳐 같은 것을 냈다.**

**재현으로 뿌리를 못박았다**(호스트 ±60px 안 노드 수, 첫 발 직후):

| 자리 | t≈70ms | t≈130ms | t≈190ms | t≈280ms |
|---|---|---|---|---|
| 훈련 | flash 1 · spark **0** · 알갱이 **0**(전체 3개는 비행 중) | 1 · 0 · 0 | 1 · 0 · 1 | 1 · 6 · 2 |
| 룬 | 1 · **0** · 1 | 1 · 0 · 2 | 1 · 0 · 3 | 1 · 6 · 5 |
| 단련 | 1 · **0** · 0 | 1 · 0 · 1 | 1 · 0 · 2 | 1 · 6 · 4 |

즉 «첫 발에 이펙트가 없다» 가 아니라 **알갱이가 HUD 알약·아이콘에서 날아오느라 타격 순간의 호스트 근처가 비어 있다**
(583 의 구조적 성질 — 619 가 만든 것이 아니다). 처방은 그 빈자리를 **작은 앰버 한 줌**(`UPFX_NOW = 4`)으로 메우는 것이다.
⚠ 583 의 «겹쳐 쏘지 않는다» 는 **값으로 지켰다** — 그 규약의 근거는 «한 사건에 파티클 13개» 였고 여기서는
**4 + 알갱이 ≤6 = 10**(583 이전 앰버 단독과 같은 수)이라 `verify583` [G2]«사건당 3~12개» 도 그대로 통과한다.
수리 후 같은 자: 세 자리 전부 t≈70ms 에 **flash 1 · spark 4**.

**이관** — `verify64` [10]. 종전 항은 «반복 중 `#fxl` 자식 수가 구매수보다 훨씬 적다» 로, **619 가 뒤집은 결정
(«반복분 연출은 정지할 때 한 번»)을 그대로 굳힌 자리**였다. 그 항의 원래 근거는 «폭주 금지» 이므로 자를
**동시 노드 상한**(FXMAX 120 밑 · 70 이하)으로 옮겼다(333 처방). 상한을 걷어내면 1.4초 홀드에서 100개를 넘겨
그대로 빨개진다.

### 4회차 — 「파티클이 글자 위에 앉는다」 (2인 공통 ③) — **DN 4 / DO 6 (미달)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DN | **4** | 6 | 6 | **4** | 훈련에서 파티클이 레벨·코인 숫자를 30~70% 덮는다(f2~f7 다수) |
| DO | 6 | 6 | 7 | **6** | 훈련 held 프레임의 43%(f4·f6·f7)에서 «Lv.» 의 L 을 덮는다 |

3회차의 «첫 발 공백» 은 닫혔다(둘 다 f1 에 파티클 3개를 셌다 · 「성공인데 없다」 0건).
남은 하나가 다시 2인 공통이라 **자를 대 봤다** — 훈련 홀드 1.7초 동안 `#fxl` 에 붙은 노드를 카드의
**글자 상자**(Range 로 잰 텍스트 노드 bbox)와 교차시켰다:

| 종류 | 개수 | 글자와 겹침 | 비율 |
|---|---|---|---|
| 스파크(`fx-spark`) | 100 | **36** | **0.36** |
| 화폐 알갱이(`fx-spd`) | 3 | 0 | 0.00 |
| 플로터(`fx-plus`) | 1 | 0 | 0.00 |

⇒ 비평가가 옳았고, **범인은 619 가 되풀이하는 스파크**다(알갱이는 첫 발뿐이라 0건 — 3회차 처방이 맞았다).

**뿌리는 `fxBurst` 의 두 규약이 부딪히는 자리다**:
* **28회차** — «입자가 형제 UI 로 새지 마라» 로 시작·끝점을 **bbox 안**에 가뒀다(2인 공통 처방).
* **42회차** — 글자 회피는 «**각도만** 굴린다»(9회) — 빈 각이 없으면 **그냥 앉는다**.
글자가 빽빽한 큰 호스트(훈련 카드 326×510)에서는 그 두 규약이 «앉을 수밖에 없다» 를 만든다.
한 번 터지는 자리에서는 그것이 옳았지만 619 는 **초당 6회**라 같은 확률이 «수치가 계속 가려진다» 가 된다.

**처방 — 선택 인자 하나(`strict`)를 더했다. 안 주면 기존 25 자리는 한 값도 안 바뀐다.**
`strict` 를 준 호출에서는 끝까지 자리를 못 찾은 입자를 **버린다**(옮기지 않는다 — 옮기면 링이 무너진다).
⚠ **1차 시도는 실패했고 그것이 이 회차의 교훈이다** — 그대로 걸었더니 훈련 카드에서 입자가 **100% 버려졌다**
(연출이 통째로 사라진다 = ① 0점). 뿌리는 회피 여유 `FX3_KOM` 이 **93 재화 코인의 잉크 반경(61.5 · 543 이 3배로
키웠다)**에서 온 값이라, 26px 스파크에 그대로 쓰면 구멍이 링을 통째로 덮기 때문이다.
⇒ `fxbTextHoles(t, m)` 에 여유 인자를 더해 **입자 크기에서 나온 값**(`FXB_KOS = 14` = 스파크 반경 13 + 1)을 쓴다.
회당 개수도 6 → **4**(`UPFX_N`)로 내려 노출을 줄였다.

**결과(같은 자)**: 스파크 68개 중 겹침 **36 → 1**(0.36 → **0.01**) · 입자 수는 유지(연출이 «없어서» 안 겹치는 게 아니다).
게이트 [E] 신설로 못박았다 — **[E1]** 표본 ≥20 · **[E2]** 겹침 비율 ≤ 0.05(실측 **0.03**) ·
**[E3] 되돌림** `strict` 를 무시하는 사본에서 **0.45** 로 되살아난다.

회귀(무수정): `verify58` 27/27 · `verify64` 44/44 · `verify488` 76/76 · `verify491` 72/72 · `verify518` 42/42 ·
`verify583` 36/36 · `verify592` 35/35 · **SMOKE PASS**.

### 5회차 — 위상 굴림 · 채점 — **DP 7 / DQ 6 (미달 · 이 세션의 회차 예산 소진)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DP | **7** | 8 | 8 | **7** | 훈련 파티클 좌표가 f1~f7 내내 ±20px 고정 — «고정 장식» 으로 읽힌다 |
| DQ | 8 | **6** | 7 | **6** | 룬 파티클이 호스트 폭의 2%(훈련 8%)라 «버스트» 로 체감 안 된다 |

4회차의 ③ 가림 지적은 **두 사람 모두에서 사라졌다**(DQ 「침범 0%」 · DP 「겹침 프레임당 0~1개, 5% 미만」) — [E] 게이트와 일치한다.
DP 의 지적은 **자로 확인됐다**: 스파크 좌표를 12px 격자로 세니 **중복 34%**(64개 → 42칸).
뿌리는 각도가 `i/cnt` 고정이고 지터가 ±0.18 뿐이라, keep-out 이 살려 주는 «빈 각» 이 매 틱 같은 자리라는 것.
⇒ `strict` 호출에서만 **버스트마다 링을 한 칸(2π/cnt) 안에서 굴린다**(등간격은 불변 · 기존 호출은 위상 0).
같은 자로 중복 34% → **29%**(68개 → 48칸). 개선이 작아 **다음 회차가 이어서 볼 자리**로 남긴다.

### 6회차 (2026-09-01, sess-0359-2612 워커 D) — 크기 어휘 통일 · 저불일치 수열 — **DR 4 / DS 7 (미달)**

**사전 실측이 §6 의 두 축을 다 확정했다** — 훈련 좌표 중복 21%(§5 의 34% 는 표본 차) · **룬 스파크
크기 2.9%/폭 vs 훈련 8.9% = 3배 어긋남**(DQ 가 옳았다 — 2인 불일치 지적을 자로 갈랐다).

처방 3개(전부 `strict` 한정 — 기존 25 자리 호출은 한 값도 안 바뀐다):
1. **스파크 크기 = 호스트 √wh 비례**(`hsc`, 기준 410 = 훈련 카드 → 훈련 배율 1 불변). ⚠ 1차 시도
   «대각 비례» 는 좁은 띠에서 과했다 — 단련(998×222)이 1.65 배를 받아 중복이 19→27% 로 되레 올랐다.
   기하평균은 단련 1.15 · 룬 1.9. 결과: 세 화면 잉크가 대각 대비 4.5~4.8% 로 모인다.
2. **위상·반경 저불일치 수열** — 5회차의 무작위 위상(연속 두 버스트가 같은 값을 뽑을 수 있다)을
   황금비 수열로, **링 반경도 같은 수열로 숨쉰다**(rr 0.82~1.00). 반경을 흔드는 이유: 탄생 타원이
   bbox 밖이라 clamp 가 입자를 테두리 같은 자리에 눌러붙였다(중복의 뿌리). 훈련 중복 21→10~19%.
3. **keep-out 여유 14 → 19**(최대 입자 반경 17+2 — 숨쉬기가 입자를 안쪽으로 들이자 상자 모서리가
   글자를 스쳤다, [E2] 0.12) + **버스트당 최소 1개 보장**(여유·숨쉬기로 «4개 전부 버려지는 틱» 이
   생기면 [B] «강화마다 터진다» 가 깨진다 — 실측 B2 0.89~0.93 롤백 확인).

`verify619` 28/28(2연속 · B1 룬 «성공 수 RNG» 플레이크는 수리 전과 동일) · 회귀 8종 · SMOKE PASS.

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DR | ③6 | ③5 | ③**4** | **4** | 단련 «+1»/«−1» 플로터가 f2~f7 내내 (127,268)±4px 고정 — 인쇄된 장식 |
| DS | ③9 | ③7 | ③7 | **7** | 플로터 스폰 경계·레인 규율 부재(룬 한 줄 자기충돌 · 단련 파편 170px 이탈) |

5회차 두 축은 **둘 다 닫혔다** — DP 훈련 고정 좌표(DS ③9 「프레임별 스폰 전부 다름」) · DQ 룬 크기
(DR ② 「3.0~6.2% — 해소 확인」). 남은 것은 전부 **hb 사다리(488)·화폐 알갱이(583)** 축이다:
2인 공통 = ⓐ 플로터 고정/한 줄 겹침 ⓑ 단련 첫 발 파편이 이웃 행 침범 ⓒ 세 화면 플로터 문법 불일치.

### 7회차 — hb 사다리·알갱이의 네 뿌리 (제품 수리 · 게이트 이관 0)

ⓐ·ⓑ 의 뿌리를 재현으로 넷으로 갈랐다(전부 **수리 전 커밋에서도 재현** — 619 가 만든 게 아니라
홀드 연속 발화가 처음 드러낸 것):
1. **패리티 함정** — 전역 hbSeq 가 beat 당 +2(결과·비용 두 줄기)라 짝수 칸은 결과·홀수 칸은 비용에
   고정, `--hb-slots:2` 인 단련은 **두 플로터가 영원히 같은 자리**(DR 의 «인쇄된 장식» 실체).
   ⇒ 줄기별 순번(`hbSeqK`) + 칸 «안» 지터(가로는 칸 여유에 물림 · 세로는 `--hb-jy`, 룬 0).
   실측: 단련 «+1» 11회 → **11곳**.
2. **비용 하강이 룬 라벨을 뚫는다** — 491 2회차가 잉크를 32→43 으로 키운 뒤 띠(224..280=56px)에
   «43+하강» 이 물리적으로 안 들어간다(lone 48 은 315 까지). ⇒ `--hb-fall`(기본 = 종전값) 신설,
   룬만 12px(단련 헤더 `--hb-rise:22` 선례). 실측 라벨 겹침 14/243 → **0**.
   ⚠ x2 를 .62(아래 빈 복도)로 옮기는 길은 `verify488` [I3] «두 줄기 좌우 분리» 가 막았다(되돌림).
3. **«한 발»(lng 1.3s) 오분류** — hbAt 가 WeakMap(노드 키)인데 홀드 live 재렌더가 호스트 노드를
   갈아끼워 **매 beat 가 lone** 이 됐고, lng 가 점유한 칸을 반복분 순환(≈550ms)이 재사용해 겹쳤다
   (`verify488` [H2] 간헐 빨강의 뿌리 — **수리 전 커밋에서 같은 쌍 재현**, h2 probe 대조).
   ⇒ 키를 문자열 셀렉터로(Map · LESSONS 50-① 의 hb 판) + **lng 점유 칸 회피**(`hbLng`).
   실측: 6표본 × 3실행 겹침 쌍 **전부 0**(수리 전엔 1~4쌍 간헐).
4. **파편이 이웃 행에서 출발** — `fxSpendFrom` ② «가장 가까운 밖 아이콘» 이 단련 k0 에서 **바로 아래
   k1 행의 버튼 아이콘**을 집었다. ⇒ 같은 부품 클래스의 다른 개체 안 아이콘 제외(출발 = 헤더 보유
   표시로 복귀) + ③ 폴백 «위 200px»(룬 젬이 서브탭 위에서 태어남) → 상변 안쪽 30px.

`verify488` 76/76 · `verify491` 72/72 · `verify619` 28/28 · 회귀 5종 · SMOKE PASS.

### 7회차 채점 — **DT 4 / DU 5 (미달)** · 8회차 — 2인 공통 4건 수리

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DT | ④5 | ①**4** ③4 | ③5 | **4** | 룬 이펙트가 «홀드 루프» 가 아니라 «시도당 원샷» — f4·f6 글로우·스파크 동시 0 |
| DU | ④7 | ③**5** | ③6 | **5** | 룬 플로터 레인 재설계(실패 앵커 · +1 드랍 · 은폐) |

**2인 공통 4건과 처방(8회차 · 게이트 이관 0):**
1. **룬 홀드 연속감 단절**(DT ① «단 하나» · DU ①) — 강화 이펙트가 «성공만» 인 설계(거짓 신호 금지)라
   저확률 구간에서 이펙트 층이 프레임째 꺼진다. ⇒ **`.fx-holding` 홀드 상시 글로우** 신설(성공 신호가
   아니라 «누르고 있다» 신호 — 실패 틱에도 정당). outline 이라 621 의 눌림 왕복(scale)·60 쥬시와 층이
   안 겹치고, 훈련·룬·단련 세 화면이 같은 클래스를 쓴다. 실측: 홀드 2초 표본 19/19 점등 · 뗀 뒤 소등.
2. **스파크가 정보를 덮는다**(DT ③c·d · DU ③ — «−n 플로터 70% 은폐 · 진행바 착지») — 살아 있는
   `.fx-plus`(#fxl 에 살아 호스트 자손 스캔 밖)와 진행바 `.rb`(단색 배경이라 «그림 잉크» 판정 밖)를
   strict 구멍에 추가.
3. **단련 플로터 아이콘 스택**(DT «f2 6장 · 60% 은폐» · DU «40~60% 상시») — 줄기당 **동시 생존 상한 2**
   (`hbNodes` — 3장째가 뜨면 가장 오래된 것을 걷는다, upFx 세대 풀 규약).
4. **첫 발 알갱이가 보유 라벨 가림**(DT ② · DU ③ «단련석 라벨 100%») — ② 출발 중심을 아이콘
   «하단 아래 44px» 로(스폰 순간 잉크 반경 ≈54 가 가장 클 때라 중심 출발이 라벨을 통째로 덮었다).

**기각 1건 — «실패» 앵커 고정(DT ③a · DU ③)**: 스폰 전수 로그(46건)로 **기각** — «실패» 는 5칸
(x306·376·446·516·586)을 정확히 순환한다. 두 비평가가 본 «x≈330 4회» 는 캡처 간격(~370ms)과 칸
주기(5칸 × ~150ms ≈ 750ms)의 **앨리어싱**이다(621 1회차 «스틸 앨리어싱 헛빨강» 과 같은 병) — 9회차
비평가에게 스폰 순환 표를 전제로 준다. DU 의 «+1 드랍»(f4→f5) 도 같은 병(반복분 수명 300ms < 프레임
간격 323ms — DU 는 lng 의 800ms 를 보고 «만료로 설명 불가» 로 추론했다).

**곁다리 관측 — 훈련 첫 발 델타 플로터 «+20» 이 실제 증분과 어긋난다**(DT·DU 공통 관측): 실측
`trDeltaTxt` «+20» vs `stat.dmg` 증분 **+24.48**. 486(«지금 최종값» 표시) 이후 카드의 «+n» 줄이 배수
미반영 기저 증분을 말하는 것이 뿌리로 보인다 — 58·486 구간이라 619 에서 안 고치고 **628 로 등재**(아래).

게이트: `verify619` 28/28 · `verify488` 76/76 · `verify491` 72/72 · 회귀 5종 + SMOKE PASS
(488·491 은 부하 시 [J2]·[H1] 등이 간헐 — 재실행 초록 · 수리 전에도 같은 축).

### 8회차 채점 — **DV 7 / DW 6 (미달 · 훈련은 둘 다 8+ 돌파)** · 9회차 — 2인 공통 3건

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DV | **35/40(min 8)** | ②③**7** | ③7 | **7** | 첫 발 화폐 비행 경로가 텍스트 라인 관통(룬 Lv/진행바 · 단련 타이틀) |
| DW | **35/40(min 8)** | ①**6** | ③7 | **6** | 홀드 글로우가 실패 연속 구간에서 꺼져 보임(3/7 프레임) |

7회차 공통 4건은 **전부 소멸**(플로터 순환 «고정 아님» 을 둘 다 명시 · 아이콘 스택 «f2 4장 40%» 로 축소 ·
이웃 행 침범 0 · 앨리어싱 전제가 먹혔다). 9회차 처방(2인 공통 3건):
1. **첫 발 비행 경로**(DV «단 하나» · DW 단련③) — `--spend-to`(도착 자식 신고) 신설: 룬 `.ri`·단련 `.ti`
   좌측 아이콘 타일로 도착 + ③ 폴백 출발 x 를 도착 위로(수직 낙하). 실측: 단련 세 행 전부
   (87,1135)→(156,·) 좌측 아이콘 기둥 · 룬 (185,1185)→(185,1295) 수직 — 타이틀·Lv·진행바 관통 0.
2. **홀드 글로우 시인성**(DW «단 하나» · DV ①) — 클래스는 19/19 붙어 있었다(8회차 실측) — 문제는
   5px·α.55 가 «없다» 로 읽힌 것. 9px·α.95 로.
3. **룬 성공 플래시 전면 워시**(DV ② · DW ②④) — `--flash-to` 신설, 룬만 `.ri`(200×200) 국소 플래시.

게이트 이관 2건(리터럴 시그니처): `verify583` [A1](fxSpendFrom toC 인자) · `verify491` [7-d0](fxFlash fel).
`verify583` 36/36 · `verify491` 72/72 · `verify619` 28/28 · `verify488` 76/76 · 회귀 4종 · SMOKE PASS.

### 9회차 채점 — **DX 6 / DY 8 (DY 첫 통과)** · 10회차 — 설계 충돌 확인 · 미세 2건

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DX | ③7 | ③8 | ③**6** | **6** | 단련 «±» 레인을 아이콘 타일 밖으로(x420..730 빈 복도 제안) |
| DY | 38/40 | 39/40 | 36/40 | **8 — 통과** | (없음 · 참고: 같은 «타일 밖으로») |

8회차 공통 3건 전부 소멸(비행 경로 관통 0 · 글로우 «상시 유지 확인» · 전면 워시 지적 소멸).
**두 비평가가 독립으로 낸 동일 처방(단련 레인을 아이콘 밖으로)은 구현해 봤고 게이트가 막았다** —
`verify488` **[I6b] «612 — 두 봉투가 아이콘 액자 안에 든다» 래칫**(612 가 빈 띠를 액자로 옮겨 잠근
설계) · [I6](복도는 잉크만 비었지 `.td`/`.tc` **상자** 는 아니다) · [I7] · [K2]. 룬 레인 +4px(보더
플러시 해소 시도)도 [I1](`.rd` top=280)이 막았다 — **띠 56px 는 어느 방향으로도 여유 0**. 둘 다 원복,
10회차 비평가에게 «612 래칫 · 확정 기하» 전제로 이관(335 «앵커가 둘» 선례). 10회차 반영분은
글로우 오프셋 −3 → −10(코너 알림 배지 세척 해소 — DX·DY 공통 관측) 하나.

### 10회차 채점 — **DZ 9(39/38/39) / EA 7(36/32/38) — 미달 · 회차 소진**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | «8점을 막는 단 하나» |
|---|---|---|---|---|---|
| DZ | 39/40 | 38/40 | 39/40 | **9 — 통과** | 없음(전 축 9+) |
| EA | 36/40 | ④**7** | 38/40 | **7** | 룬 f7(2736ms) 무스파크 — 실패 연타 종반에 활동 신호가 플로터 한 겹 |

⏸ **회차 상한 10/10 소진 — «보류» 로 마감한다**(지시서 [3] — 완료가 아니다). 궤적:
DJ3/DK5 → DL6/DM5 → DN4/DO6 → DP7/DQ6 → **(이 세션)** DR4/DS7 → DV7/DW6 → DX6/**DY8** → **DZ9**/EA7.

### 11회차 (2026-09-01, sess-0720-26273 워커 B · **2차 폴리시 라운드 시작 — 회차 +10 리셋**) — §6 처방 4건 · 채점 **EB 6 / EC 5 (미달)**

§6 이 남긴 후보 넷을 그대로 구현했다(제품 4자리 · 게이트 이관 1건).

| # | 처방 | 자리 | 실측(11회차) |
|---|---|---|---|
| 1 | 룬 **실패** 틱에도 앰버 3알(플래시 없음) | `rtRuneHold` + `UPFX_FAIL_N` | 룬 스파크 홀드당 **30 → 79개** |
| 2 | 플로터 스폰 최소거리 가드 | `hbFloat` + `HB_INK_W` | 룬 최소 Δx **60 → 69.85px** |
| 3 | 스파크 크기 배율 상한 1.9 → 1.6 | `fxBurst` 의 `hsc` | 룬 중앙 46px · 최대 54px |
| 4 | 「타격 순간」 몫만 진한 앰버 | `FXPAL.upNow` 신설 | 대비 **1.28 → 2.60 : 1** |

⚠ **문턱을 «글리프 폭 69» 로 박으면 훈련이 망가진다**(가드 ②) — 훈련 카드는 폭 326 이라 설계 피치가
`hbSlotW` 로 **64**(< 69)다. 그래서 문턱은 «글리프 폭» 과 «그 호스트의 설계상 최소 정상 간격»
(칸 피치 − 지터 진폭 2배) 중 **작은 쪽**으로 잡았다. 이 한 줄이 없었으면 훈련은 정상인 이웃 칸이
매번 위반이 되어 순번만 헛돌았을 것이다.

**채점 — EB 6 / EC 5 (미달)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | 「8점을 막는 단 하나」 |
|---|---|---|---|---|---|
| EB | ①**6** (28/40) | ②③④**6** (26/40) | ③**6** (28/40) | **6** | 훈련 f4~f7 666ms 동안 «+21» 0장 |
| EC | ①**7** (31/40) | ②**5** (26/40) | ②③④**7** (30/40) | **5** | 룬 스파크 융합 블롭 — 상한 1.6 이 안 들었다 |

⚑ **EB 의 「단 하나」 진단은 대조 실행으로 기각했다**(338·344 규칙). EB 는 훈련 플로터가 홀드당 1장뿐인
것을 «이번 회차의 최소거리 가드가 과교정한 자리» 로 읽었는데, **수리 전 커밋(`86da6e8`)에서도 같은 값**이다:

| 트리 | 훈련 플로터 | 룬 | 단련 |
|---|---|---|---|
| 수리 **전** `86da6e8` | **2** | 48 | 46 |
| 수리 **후** (11회차) | **2** | 48 | 46 |

뿌리는 가드가 아니라 **훈련만 원장(ledger)이 없다**는 것이다 — 훈련의 회당 신고는
`hbBeat(cardNow, true, null, null)` 로 **txt·cost 가 둘 다 `null`** 이라 `hbFloat` 를 아예 안 지난다
(룬 `'+1'`/`'−n'` · 단련 `'+1'`/`'−n'` 과 갈리는 자리). 가드는 플로터를 **취소하지 않는다** — 순번만
민다. 즉 EB 의 **관측은 옳고 진단은 틀렸다**: 고칠 자리는 가드가 아니라 훈련의 회당 문구다.

**12회차가 잡을 것 — 2인 공통만**(한 사람 지적은 회차를 태우기 전에 실측으로 가른다 · §6 규약):
1. **스파크 융합**(EB ⑥⑦ · EC ①④ — EC 의 「단 하나」) — 상한 1.6 은 **단일 크기만** 눌렀고 «중첩» 은
   못 막았다(EC 실측 f4 3알이 중심간 35·40px 인데 반지름합 52·55 → 97×64 아메바 1덩이).
   ⇒ ⓐ 절대 상한(두 사람 38·40px) ⓑ **스파크끼리 최소중심거리** 두 가지가 같이 필요하다.
2. **스파크가 호스트 테두리를 넘는다**(EB ④⑪ · EC ⑨⑬) — 단련은 프레임당 8~9알 중 5~6알.
3. **세 화면 크기 어휘**(EB ⑮ · EC ④) — ⚠ **두 사람이 축이 갈렸다**: EB 는 «호스트 짧은 변 대비 6~8%»
   (호스트 상대) · EC 는 «38px 공통 절대 상한». **394 선례대로 눈금을 먼저 정해 적을 것.**
4. **훈련 회당 원장**(EB ① · EC ⑦) — 위 대조표의 실체. ⚠ 58 26회차 «바로 아래 줄이 상시로 말하는 것을
   플로터가 한 번 더 말하지 마라» 와 부딪히므로 **문자열을 먼저 정하고** 넣는다.

게이트: `verify619` **28/28** · `verify491` **72/72**([7-d0] 색 갈래 이관 — 333 처방) · `verify488` 77/77 ·
`verify583` 36/36 · `verify64` 44/44 · `verify518` 42/42 · `verify349` 29/29 · `verify592` 35/35 ·
`verify625` 24/24 · `verify324` 37/37 · **SMOKE PASS**. 자 신설 `tools/probe619b.js`(ⓐ 스폰 최소거리 ·
ⓑ 스파크 실크기 · ⓒ 대비비).

### 12회차 (2026-09-01, sess-0720-26273 워커 B) — 11회차 2인 공통 3건 · 채점 **ED 5 / EE 5 (미달)**

11회차의 **2인 공통만** 잡았다(한 사람 지적은 실측으로 가른다 — §6 규약). 제품 3자리.

| # | 처방 | 자리 | 실측 (11 → 12회차) |
|---|---|---|---|
| 1 | 스파크 눈금을 **«글리프 대비 절대»** 로 | `FXB_DMAX 40`·`FXB_SZMAX 34` | 룬 스파크 최대 **54 → 40px** · 중앙 46 → 34 |
| 2 | 살아 있는 스파크끼리 **최소 중심거리** | `FXB_SEP 1.30` + `ks`/`fxbFuse` | (아래 ⚠ 참조) |
| 3 | 칸이 모자라면 **겹치는 앞 플로터 회수** | `hbFloat` | 단련 최소 Δx **7.55 → 74.33px** |

⚑ **눈금을 골랐다(394 선례 — «무엇을 눈금으로 삼는가» 를 먼저 적는다).** 11회차에 **두 비평가의 축이
갈렸다** — EB «호스트 짧은 변 6~8%»(상대) vs EC «38px 공통 절대 상한». **절대를 골랐고 근거는 EC ②
가 찍은 자리**다: 스파크가 크다고 읽히는 순간의 비교 대상이 호스트 패널이 아니라 **바로 옆 글리프**였다
(«숫자 옆에 같은 밝기의 원반이 붙어 라벨이 글자인지 파티클인지 헷갈린다»). 글리프는 `--fx-plus-fs`
**34px 절대값**이므로 스파크도 절대여야 한 어휘가 된다 ⇒ 지름 상한 40px(= 글리프의 1.18배).

⚑ **11회차 가드가 단련에서 안 들었던 뿌리 — «칸이 2개뿐»**. 페이지에 직접 물어 확정했다:
단련은 `--hb-slots:2`(피치 88 · `--hb-x .1152`)인데 점유자가 **셋**이다(첫 발 `lng` 1.3s + 반복분 2장).
빈 칸이 **구조적으로 없으니** «순번을 민다» 로는 못 푼다 — n 바퀴를 다 돌고 충돌한 자리에 그대로 앉는다.
칸을 늘리는 길은 2회차가(«카드 밖으로 나간다») · 레인 이동은 612 [I6b] 래칫이 막고 있으므로
**겹칠 바에는 앞엣것을 먼저 보낸다**(EC ⑪ 의 처방 그대로 · 상한 2 의 `old.remove()` 와 같은 규약).

**채점 — ED 5 / EE 5 (미달)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | 「8점을 막는 단 하나」 |
|---|---|---|---|---|---|
| ED | ④**5** (25/40) | ②**6** (27/40) | ④**5** (26/40) | **5** | 훈련 회당 «+N» 원장 0장(f3~f7 932ms · Lv +9) |
| EE | ④**5** (27/40) | ②**6** (27/40) | ②④**5** (25/40) | **5** | **같은 자리** — 훈련 «+N» 원장 f2 이후 932ms 소등 |

**두 사람의 「단 하나」가 처음으로 완전히 일치했다** — 남은 결손이 한 자리로 좁혀졌다는 뜻이다.

⚠⚠ **그런데 그 처방의 절반은 «하지 말 것» 이다 — 다음 세션이 반드시 먼저 읽을 것.**
EE ② 는 «훈련에도 비용 «−N» 수치를 넣어라»(룬 −13 · 단련 −1 과 어휘 통일) 인데, 그 자리는
**주인 지시 583 이 직접 걷어낸 자리**다(원문 «그 훈련할때 금액 마이너스로 되는 연출 빼기»).
`verify488` **[F3] «583 — 훈련은 «금액» 대신 골드 알갱이로 말한다(비용 플로터 0)»** 가 그것을 못박고
있어, 넣는 순간 게이트가 빨개지고 **주인 지시를 되돌리는 것**이 된다. ⇒ **«−N» 은 넣지 마라.**
«+N» 쪽만 남는데 그것도 2회차가 이유를 적어 뒀다(카드 `.cv` 가 «다음에 오를 양» 을 상시로 말하고
`fx-cvswap` 이 그 자리를 깜빡이며 손을 떼면 `fxUpOk` 가 델타를 한 장 띄운다 = 같은 수가 세 곳).
**⇒ 13회차의 갈래는 «수치를 한 장 더 띄운다» 가 아니라 «훈련의 회당 신호를 수치 없이 새 사건으로
읽히게 한다» 다.** 후보는 EB ② 가 이미 준 것이 있다 — **홀드 글로우를 틱마다 펌프**(상시 α .45 →
틱에 1.0 → .45, 90ms). 지금 링은 7프레임 내내 같은 밝기라 «상태 표시» 로만 읽힌다.

⚑ **자와 눈이 갈린 자리 둘 — 자를 적어 둔다(338·350 규칙).**
1. **스파크가 호스트 밖으로 샌다**(ED ③ · EE ④⑩ «13~20px»)는 **호스트 bbox 기준으로는 0px 다**
   (세 화면 전부 위·아래·좌·우 **0** · 표본 91/80/85알). 두 사람이 잰 것은 bbox 가 아니라 **금색 액자
   테두리**이고 그것은 bbox 보다 안쪽이다 — **둘 다 옳다.** ⇒ 13회차가 고칠 것은 «클램프가 샌다» 가
   아니라 **«클램프의 기준을 bbox 에서 보이는 액자로 바꾼다»** 이다(자리는 `fxBurst` 의 `bx0..by1`).
2. **단련 «−1»/«+1» 이중 인쇄**(ED ② · EE ⑨ «8~10px 어긋나 겹친다»)는 **재현되지 않는다** —
   동시 생존 플로터 쌍 **534개** 중 최소 중심거리가 **76px**(Δx 74 · Δy 17)이고 그 쌍은 «+1 ↔ +1»
   (같은 줄기)이다. «−1»/«+1» 은 레인이 `--hb-y 116` / `--hb-y2 20` 으로 **96px** 벌어져 있다.
   11회차(7.55px)에서 실제로 닫힌 자리이고, 캡처가 «회수 직전 프레임» 을 물었을 가능성이 크다.
   ⇒ **13회차는 이 두 건에 회차를 태우기 전에 위 두 자를 먼저 돌릴 것.**

**여전히 2인 공통으로 남은 것**(13회차 후보): 룬 스파크 **융합**(ED ④ 121×70 · EE ⑤ 117×66 —
크기 상한은 들었지만 «겹침» 은 남았다 ⇒ `FXB_SEP` 1.30 이 모자라거나 동시 생존 수를 줄여야 한다) ·
룬 «−13» 두 장이 5~10px 간격으로 붙는다(ED ⑧ · EE ⑦ — `hbFloat` 가드는 **같은 줄기** 만 보는데
이 둘은 서로 다른 줄기다) · 룬 스파크가 «−13» 을 15px 덮는다(ED ⑦ · EE ⑥).

게이트: `verify619` 28/28 · `verify488` 77/77 · `verify491` 72/72 · `verify583` 36/36 · `verify64` 44/44 ·
`verify518` 42/42 · `verify592` 35/35 · `verify625` 24/24 · `verify324` 37/37 · `verify349` 29/29 ·
`verify58` · `verify93` · `verify512` · `verify460` 29/29 **전부 무수정** · **SMOKE PASS**.
곁다리 **635** 등재(`verify619` [B1] 룬 항 플레이키 — 확률 축에 절대 문턱. **수리 전 트리에서도 2/5 미달**을
`git stash` 대조로 확인했다 · 632 와 같은 병의 다른 얼굴).

### 13회차 (2026-09-01, sess-0828-32001 루틴 워커 B) — 12회차 2인 공통 3건 · 채점 **EF 3 / EG 3 (미달)**

12회차가 남긴 세 갈래를 그대로 잡았다(제품 3자리 · 자 1개 신설). **자를 먼저 돌렸고**(338 규칙),
그 자가 처방 하나를 **네 번 되잡게** 했다 — 아래 ⚑ 가 이번 회차의 본체다.

| # | 처방 | 자리 | 실측 (12 → 13회차) |
|---|---|---|---|
| 1 | 훈련 회당 신호를 **수치 없이** — 홀드 글로우 **틱 펌프** | `fxHoldMark`/`fxHoldPump` | 링 상시 9px **고정** → 틱마다 **17px 봉우리**(봉우리 실린 프레임 **0% → 22~29%**) |
| 2 | 가둠 기준을 «중심» 에서 **«잉크»** 로 | `fxBurst` 의 `inM` | 액자 밖 잉크 **최대 sz/2(17~20px) → 0px**(표본 72·79·78알 · 밖으로 나온 알 **0개**) |
| 3 | 최소거리 가드가 **줄기를 넘어서** 본다 | `hbFloat` + `HB_INK_H` | 룬 «−13»↔결과 최소 Δx **5~10px(ED⑧·EE⑦) → 84~154px** |

⚑ **①은 «주인 지시가 금지한 처방» 을 피해서 세운 것이다.** 12회차에 두 비평가의 「8점을 막는 단
하나」가 처음으로 완전히 일치했는데(«훈련은 f3~f7 932ms 동안 Lv +9 인데 회당 원장 0장»), 그들이 같이
적은 처방의 절반은 **583 주인 지시가 직접 걷어낸 자리**였다(«그 훈련할때 금액 마이너스로 되는 연출
빼기» · `verify488` [F3]). «+N» 쪽도 2회차가 닫아 뒀다(같은 수가 세 곳에서 말해진다). ⇒ 축을
**«수치» 에서 «상태 변화»** 로 옮겼다 — 호스트 링이 **틱마다 부풀었다 돌아온다**(EB ② 의 축).

⚠ **EB ② 의 값(상시 α.45 → 1.0 → .45)은 안 썼다 — 9회차가 이미 실측으로 닫은 자리다.** 5px·α.55 를
비평가 둘이 각각 «없다»(DW «3/7 프레임 소등»)·«감쇠»(DV «실패 구간 ~4px»)로 읽어 9px·α.92 로 올린
것이 지금 값이라, 상시를 .45 로 내리면 그 2인 공통이 그대로 되살아난다. ⇒ **바닥은 그대로 두고 봉우리만**
올렸다(9px/α.92 → 17px/거의 흰 앰버). «누르고 있다»(상태)는 바닥이, «또 올랐다»(사건)는 봉우리가 말한다.

⚑⚑ **이 회차의 교훈은 «펌프를 네 번 되잡았다» 는 것이고, 되잡게 한 것은 눈이 아니라 자다.**
신설한 `tools/probe619c.js` 가 매 시도를 수치로 찍었다:

| 시도 | 무엇을 했나 | 자가 찍은 것 | 왜 틀렸나 |
|---|---|---|---|
| 1 | WAAPI 90ms · `ease-out` · 틱마다 취소 후 재시작 | 링이 **상시 17px** 로 굳음(봉우리/강화 0.10) | **619 4회차가 이미 적어 둔 함정** — 홀드 중에는 `startTime` 이 다음 스타일 플러시까지 밀린다(평균 86~131ms). 대기 중인 애니메이션이 **0% 값(17px)을 문 채** 다음 틱에 취소된다 |
| 2 | `startTime = document.timeline.currentTime` | 봉우리 **최대 10px**(= 거의 안 섬) | 그 값은 «지금» 이 아니라 **직전 프레임**이다. 틱은 setTimeout 이라 프레임 사이에 돌므로 펌프가 **이미 지난 만큼 진행된 채** 시작한다 |
| 3 | `startTime = performance.now()` · 55ms ease-out | 최대 16~17px 인데 **봉우리 3~6회 ÷ 강화 19~21회** | 순수 ease-out 은 첫 5ms 에 값이 주저앉는다 — 60fps 화면에도 90ms 캡처에도 한 프레임 안 실린다 |
| 4 | 봉우리를 **평평하게**(0%·55% 같은 값) + 길이를 **틱 간격에 비례**(621 이 `h.iv` 를 넘기는 그 길) | 봉우리 17px · 실린 프레임 **22~29%** · 바닥 9px 유지 | ✅ |

⚠ **길이를 상수로 박으면 어느 한쪽 속도에서 반드시 깨진다** — 홀드 틱은 160ms 에서 **60ms 까지**
빨라진다(`TR_HOLD_IV0/IVMIN/ACCEL`). 고정 90ms 는 빠른 구간에서 골이 통째로 잘려 «상시 켜짐» 으로
되돌아가고(= 9회차 상시 글로우와 구분 불가), 고정 40ms 는 느린 구간에서 한 프레임만 스친다.
듀티도 자로 골랐다 — 0.92 는 골이 틱의 8% 라 화면에 안 남고, 0.62 는 봉우리가 14~17% 프레임에만
실린다. **0.80** 이 두 상태를 각각 남기는 자리다.

⚑ **자의 문턱을 «잴 수 없는 것» 에서 «재는 것» 으로 고쳐 세웠다(394 «무엇을 눈금으로 삼는가»).**
처음에 세운 «봉우리 수 ÷ 강화 수 = 1.00» 은 **이 화면에서 원리적으로 잴 수 없다** — 최속 틱 60ms 는
60fps 에서 **3.6 프레임**이고, 봉우리와 골이 각각 2프레임씩 실려야 세지는데 3.6 안에 둘 다 못 넣는다.
어떤 처방을 해도 그 비는 0.3~0.8 에서 표본 오차로 흔들린다(실측 그대로). ⇒ 축을 **«두 상태를
오가는가»** 로 바꿨다: ⑴ 봉우리가 실제로 서는가(최대 17px) ⑵ **바닥이 9px 그대로인가**(9회차 값을
안 내렸다는 증거 — 이 항이 EB ② 를 무르게 따르는 것을 막는다) ⑶ 봉우리가 실린 프레임이 중간대인가.
⚠ 그 중간대의 아래끝은 **20% 로 박으면 안 된다** — 같은 트리에서 16·19·22·25·29% 가 나온다
(635 가 등재한 «확률 축에 절대 문턱» 과 같은 병). 12% 로 두고 흔들리는 이유를 적었다:
**홀드 중 통짜 렌더가 호스트 노드를 갈면 그 위에서 돌던 펌프가 노드째 사라진다**(다음 틱이 새 노드에
다시 단다 — 619 8회차가 `fxHoldMark` 를 틱마다 부르는 그 이유). 설계상 봉우리는 틱의 44% 인데
화면에 실리는 것이 16~29% 인 차이가 통째로 그것이다.

⚑ **②는 «자와 눈이 갈린 자리» 를 자를 옮겨서 닫았다.** 12회차는 ED ③·EE ④⑩(«스파크가 호스트 밖
13~20px»)과 실측(«bbox 기준 사방 0px»)이 정면으로 어긋난 채 넘겨졌는데, **둘 다 옳았다** — 종전
clamp 는 **입자 중심**을 bbox 안에 넣었고 잉크는 거기서 `sz/2` 를 더 나갔다(28회차가 «사방 스필
≤ sz/2 로 균일» 이라고 **의도한** 값이다). 사람이 재는 것은 중심이 아니라 «액자 밖으로 나온 잉크»
이므로 가둠 상자를 `sz/2` 안으로 들였다. 검산: 룬 sz 최대 40 ⇒ 20px · 단련 39 ⇒ 19.5px ·
훈련 34 ⇒ 17px = 두 사람이 적은 «13~20px» 과 같은 크기다.
⚠ **탄생 타원과 이동식은 한 줄도 안 건드렸다** — 21회차(«대상 테두리 바깥에서 태어난다»)와
41회차(«끝점을 가두고 시작점을 FXB_K 만큼 당긴다» = 퍼짐 ±0% 방지)는 둘 다 **가둠 상자와 무관한 식**
이라 그대로 성립한다. 바뀐 것은 상자의 네 변뿐이다.

⚑ **③의 뿌리는 «가드가 약했다» 가 아니라 «룬의 두 띠가 24px 밖에 안 떨어져 있다» 였다**
(`.tr-rn{--hb-y:248;--hb-y2:224;--hb-jy:0}`). 줄기별 목록만 보는 11회차 가드는 **정의상** 그 쌍을 못 본다.
⇒ **미는 쪽만** 두 줄기를 다 보게 하고 겹침 판정에 세로를 넣었다(글리프 높이 `HB_INK_H` 34).
세로로 충분히 갈린 호스트는 종전과 같다(단련 96px · 훈련 70px — 자가 «표본 없음» 으로 그것을 보인다).
⚠ **걷는 쪽(회수)은 같은 줄기로 남겼다** — 12회차의 회수는 칸이 구조적으로 모자란 단련을 위한
규약인데, 줄기를 넘겨 걷으면 결과 줄기가 «무엇을 냈는가»(비용)를 지운다. 정보를 지우는 대신
**자리를 넓히는 방향으로만** 돈다.

**채점 — EF 3 / EG 3 (미달 · 12회차 5·5 에서 **내려갔다**)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | 「8점을 막는 단 하나」 |
|---|---|---|---|---|---|
| EF | ③④**3** | ①③④**3** | ③**3** | **3** | 회당 알갱이 수명이 틱(≈170ms)보다 길어 9~11알이 상시로 떠 있다 |
| EG | ①④**3** | ③**3** | ①③**3** | **3** | 훈련 링·단련 플래시가 홀드 내내 «켜짐» 으로 구워져 있다(룬처럼 틱 사이에 내렸다 올려야 한다) |

⚠⚠ **점수가 내려간 것을 «비평가가 짜다» 로 넘기지 마라 — 두 사람이 각각 다른 자로 «내가 이번 회차에
만든 결함» 을 찍었다.** 그리고 그 지적은 **자로 재현됐다**(아래 ⚑ 회수). 12회차(5·5)와 같은 프레임 수·
같은 규약이므로 하락분은 이번 회차 몫이다.

⚑⚑ **되돌린 것이 아니라 «같은 처방을 옳게 다시 세웠다» — 링을 «바깥으로» 키운 것이 이번 회차의 사고다.**
`outline-offset:-10px` 는 링의 **안쪽 변**을 테두리에서 10px 들이는 것이고 링은 거기서 **바깥으로**
두께만큼 뻗는다 ⇒ 9px 는 «1px 안쪽» 에서 끝나지만 **17px 는 7px 밖으로 나간다.** 채점이 그 대가를
세 자리에서 찍었다:

| 자리 | 비평가 실측 | 뜻 |
|---|---|---|
| 훈련 카드 붉은 알림 배지 | 붉은 픽셀 752 → **336~564**(홀드 **7/7 프레임**, 최악 −55%) | 기존 UI 가 홀드 내내 반달로 잘린다 |
| 옆 «체력» 카드 | 여유 15px → **3px**, 옆 카드 검은 테두리 (20,20,20) → (64,54,32) | «어느 카드가 강화 중인가» 의 경계가 흐려진다 |
| 단련 아래 행 | 링 바깥 하단 377 vs 아래 행 테두리 376 = **2px 덮음** | 틱마다 남의 행 테두리가 깜빡인다 |

⇒ 봉우리의 `outline-offset` 을 **−18px 로 같이 밀어** 두 상태의 **바깥 변을 같은 자리(테두리 1px 안쪽)로
고정**했다. 링은 이제 **안쪽으로만** 자란다. 8회차가 box-shadow 를 버리고 outline 을 고른 이유
(«레이아웃·형제 UI 를 안 건드린다»)가 이 한 줄에 달려 있었다.
**자에 축을 하나 더 세웠다 — `probe619c` ⓓ «링 바깥 변 = offset + width ≤ 0»**. 수리 뒤 세 화면 전부
**−1px**(= 호스트 안). 이 축이 없으면 다음 세션이 봉우리를 더 키우다 같은 사고를 되풀이한다.

⚑ **EG 가 이 회차에서 가장 값진 것을 하나 줬다 — «룬에서는 펌프가 스펙대로 보인다»**(정지 앰버
(248,168,28) **8~9px** ↔ 펌프 창백한 (255,248,185) **17px** · 2/7 프레임 ≈ 29% 듀티). 즉 **부품은 맞고
자도 맞다**(`probe619c` ⓑ 와 같은 값). 훈련·단련에서 «안 보인다» 는 것은 부품이 아니라 **그 화면에서
링보다 큰 다른 신호가 이미 켜져 있다**는 뜻이다(EG ⑪ «단련 아이콘 플래시가 7/7 프레임 상시» ·
EF ① «훈련 링 두께가 최대치의 90% 에 앉아 있다»). **14회차는 그 «상시로 켜진 것» 을 끄는 데서 시작해라
— 펌프를 더 키우는 것이 아니다**(키우면 방금 닫은 바깥 자람 사고가 그대로 돌아온다).

**게이트** `verify619` **32/32** · `verify488` 77/77 · `verify491` **72/72**(⚠ [R-c] 이관 — 아래) ·
`verify583` 36/36 · `verify64` 44/44 · `verify518` 42/42 · `verify592` 35/35 · `verify625` 24/24 ·
`verify324` 37/37 · `verify349` 29/29 · `verify58` 27/27 · `verify93` 26/26 · `verify512` 31/31 ·
`verify460` 29/29 · `verify612` 25/25 · `verify621` 42/42 · **SMOKE PASS**.

⚑ **이관 1건 — `verify491` [R-c](333 처방대로 «문턱» 이 아니라 «재는 방법» 을 고쳤다).**
그 항은 되돌림 사본에서 «누른 그 노드가 반응하지 **않는다**»(픽셀 < 8%)를 단언하는데, 619 가 홀드에
회당 이펙트를 얹으면서 **누른 노드가 죽은 사본에서도 버튼 상자 안이 계속 움직이게** 됐다 — 룬 **32.74%**.
그 자의 머리말은 이미 «주 축은 픽셀이 아니라 노드 생존» 이라고 적어 뒀으므로 뜻은 그대로 두고
**재는 동안만 이펙트 층을 가렸다**(`#fxl` + `.fx-holding`). 가린 뒤 룬 **1.04%**,
대조군 [R-d] 훈련 카드는 **97.99%** 로 그대로 초록 — **자가 «아무거나 빨개지는 자» 가 아님이 그 항으로 남는다.**
⚠ 가림은 `pixelRun` 안에서만 살고 끝에 걷는다(뒤 절이 이펙트 노드를 센다).
⚠ **`page.addStyleTag({id, content})` 로는 안 걸린다** — 옵션이 안 맞아 조용히 건너뛰고 **같은 수치가
그대로 다시 찍힌다**(1차 시도가 32.74% 로 한 자리도 안 움직여 «이펙트가 원인이 아니다» 로 오독할 뻔했다).
`page.evaluate` 로 `<style>` 를 직접 넣어야 한다.

**자** `tools/probe619c.js` **신설**(ⓐ 잉크 스필 · ⓑ 글로우 펌프 두 상태 · ⓒ 줄기 넘는 최소거리) —
세 축 전부 문턱 통과.



**14회차가 볼 것 — 2인 공통만**(한 사람 지적은 회차를 태우기 전에 실측으로 가른다 · §6 규약):
1. ⚑ **«상시로 켜진 것» 을 끄는 것이 1순위다**(EF ① · EG ⑪ — 두 사람이 다른 화면에서 같은 병을 찍었다):
   단련 아이콘 플래시가 **7/7 프레임 상시**(칼날 색 편차 ≤4)이고 훈련 링 두께가 **최대치의 90% 에 앉아**
   있다. 회당 정보를 **0비트** 나르는 표시가 화면에 켜져 있으면 그 위에 무엇을 얹어도 «새 사건» 이
   안 된다. ⚠ **펌프를 더 키우는 것으로 풀지 마라** — 13회차가 방금 그렇게 하다 링을 바깥으로 7px
   내보내 알림 배지를 41~55% 지웠다(`probe619c` ⓓ 가 그 자리를 지킨다).
2. **회당 알갱이 수명이 틱보다 길다**(EF 의 「단 하나」 · EG ⑤⑫ — 2인 공통): 틱 ≈161~175ms 인데
   스파크·플로터가 2~3틱을 살아 한 프레임에 **7~11알 / 토큰 4~5개**가 겹쳐 선다. «세대» 가 안 갈리니
   «3알 = 이번 한 방» 으로 안 세어진다. ⚠ 수명을 줄이는 길은 **41회차 공용 퇴장 규약**과 부딪히므로
   먼저 읽을 것 · 개수를 줄이는 길은 `verify619` [B] 1.00 을 깨면 안 된다(발화가 0 이 되면 반려).
3. **가둠을 «잉크» 로 들인 대가 — 이제 여유가 0px 이라 알갱이가 액자에 붙는다**(EF ② «폭의 21~38% 가
   절단» · EG ⑬ «최상단 y=151 = 링 안쪽 경계 151»). 13회차는 «밖으로 0» 을 얻었지만 **스폰 상자를 안
   줄이고 클램프로 눌렀기** 때문이다 — 다음은 `inM` 에 **여유 한 칸**을 더 주거나(예 sz/2 + 4)
   탄생 타원 반축 자체를 줄이는 쪽이다.
4. **첫 발 화폐 알갱이는 아직 액자를 뚫는다**(EF ⑪ 16px · EG ⑭ 4~11px — 2인 공통): 13회차의 잉크 가둠은
   `fxBurst`(스파크)에만 걸려 있고 `fxFly`/화폐 알갱이에는 안 걸렸다.
5. 룬 전용 2건: **성공 플래시가 시안 아이콘을 탁한 세이지로 물들인다**(EG ⑨ 채도 −33%) ·
   **판독 줄(y382~424) 한복판에 스파크가 끼어든다**(EG ⑧ — «레인을 옮겨라» 가 아니라 «그 띠에 스파크를
   넣지 마라» 다. `fxbTextHoles` 에 그 띠를 구멍으로 추가하는 길이 이미 있다).

⚠ **하지 말 것(13회차에 다시 확인)**: 훈련에 «+N»/«−N» 수치 넣기(583 주인 지시 · `verify488` [F3]) ·
상시 글로우 밝기 내리기(9회차 2인 공통) · 링을 바깥으로 키우기(`probe619c` ⓓ) ·
단련·룬 레인 이동(612 [I6b] 래칫 · 띠 여유 0).

### 14회차 (2026-09-01, sess-0928-11017 루틴 워커 B) — 13회차 2인 공통 4건 중 3건 · 채점 **EH 6 / EI 6 (미달 · 13회차 3·3 에서 두 사람 다 +3)**

⚑⚑ **13회차의 두 「8점을 막는 단 하나」는 서로 다른 말이 아니라 **한 병**이었다.**
EF 는 «회당 알갱이 수명이 틱(≈170ms)보다 길어 9~11알이 상시로 떠 있다» 라고 적었고 EG 는
«훈련 링·단련 플래시가 홀드 내내 «켜짐» 으로 구워져 있다 — **룬처럼 틱 사이에 내렸다 올려야 한다**»
라고 적었다. 둘 다 **«꺼지는 순간이 있는가»** 를 물은 것이다.
⚠ **13회차의 자(`probe619c`)는 세 축 전부 초록이었다**(스필 0 · 펌프 두 상태 · 링 바깥 변 −1px).
자와 눈이 갈린 자리라, 338 규칙대로 **자를 눈이 쓰는 축으로 옮기는 것**부터 했다.

**`tools/probe619d.js` 신설 — 「틱 사이에 내렸다 올라가는가」.** 부품 셋(링·플래시·스파크)의
«지금 켜져 있는가» 를 홀드 내내 rAF 로 찍는다. **수리 전** 실측(틱 중앙 **97ms**):

| 부품 | 소등 프레임 비율 | 최대 «켜짐» 구간 | 동시 최대 |
|---|---|---|---|
| 홀드 글로우 링 | 65~71% | ×0.7~1.2 틱 | — |
| 흰 플래시 | **4%** | **2803ms = ×29 틱** | — |
| 스파크 | **4%** | **2835ms = ×30 틱** | **20알** |

⇒ 두 비평가가 옳았고 **자리도 좁혀졌다** — 링은 이미 틱마다 내렸다 올라가고 있었다(13회차 펌프가
그 일을 한다. EG 가 «룬에서는 펌프가 스펙대로 보인다» 고 적은 그 부품이다). **구워져 있던 것은
나머지 두 조각**이다.

⚑ **뿌리는 부품이 아니라 «손으로 적은 상수» 였다 — 13회차가 펌프에서 이미 배운 것과 같은 함정이다.**
`.fx-spark` **.38s** · `.fx-flash` **.34s** 는 41·42회차가 **단발**(퀘스트 수령·보상·업그레이드 한 번)에
맞춰 고른 값이다. 그런데 홀드 틱은 **60~175ms** 라 어느 화면에서든 그 상수는 **2~4틱**을 산다 ⇒
이번 발화와 지난 발화가 화면에서 안 갈린다. 13회차가 펌프 길이를 상수로 박으려다 네 번 되잡고
얻은 결론(«길이를 상수로 박으면 어느 한쪽 속도에서 반드시 깨진다 — `h.iv` 를 받아라»)이
**나머지 두 조각에는 아직 안 와 있었다.**

**처방 — 회당 연출의 길이는 «틱» 이 정한다(`fxTickLife` 한 곳 · 제품 8자리).**

* `fxTickLife(iv, full)` = `iv` 가 있으면 `clamp(iv × 0.80… → 0.55, 45, full)`, 없으면 **`full` 그대로**.
  듀티 **0.55** 는 자로 골랐다(아래 ⚑) · 하한 **45ms** 는 최속 틱(60ms)에서도 한 프레임은 실리는 자리다.
* `upFx(key, host, cur, n, noFlash, **iv**)` 가 그 값을 받아 `fxFlash(fel, iv)` · `fxBurst(…, true, iv)` 로
  내려보낸다. 세 부품(스파크·플래시·글로우)이 **같은 `iv`** 를 받아 **한 박자**로 뛴다.
* 틱 간격은 세 홀드 경로에서 **«다음 틱까지의 간격»** 으로 넘긴다 — `trHoldTick`·`rtHoldTick` 둘 다
  `h.iv` 갱신을 **앞으로 당겨** `ivNext` 를 만들고 그것을 이펙트·`once(iv)` 에 준다
  (13회차가 `fxHoldMark` 를 `h.iv` 갱신 «뒤» 로 옮긴 것과 같은 이유다).

⚠⚠ **41회차 «공용 퇴장 규약» 과 부딪히지 않는다 — 그 규약이 금지한 것은 다른 것이다.**
41회차가 없앤 것은 «애니 길이와 **따로 손으로 적은 제거 상수**»(`fx-flash` 애니 .34s ↔ 제거 .50s =
138ms 를 투명한 채 세던 뒷꼬리)이고, 여기서 바뀌는 것은 **애니 길이 자신**이라 `fxBye`(→ `fxAnimEnd`)의
제거 시각이 **자동으로 따라온다**. 그리고 **단발 경로는 한 글자도 안 바뀐다** — `iv` 를 안 주면
`full`(380 · 340)이 그대로다. 42회차가 «퇴장 182ms» 로 닫은 그 자리가 바로 단발이다.
⇒ 상수 이름을 코드가 읽게 세웠다(`FXFLASH_MS 340` · `FXSPARK_MS 380`) — CSS 선언과 같은 값이고,
`iv` 없는 호출은 이 값이 그대로 `full` 이 된다.

**수리 후**(같은 자 · 틱 중앙 89~112ms):

| 화면 | 소등 비율 (링 / 플래시 / 스파크) | 동시 스파크 최대 |
|---|---|---|
| 23 훈련 카드 | 72% / **12%** / **15%** | **8알** |
| 룬 [강화] | 69% / **61%** / **19%** | **8알** |
| 단련 [단련] | 73% / **24%** / **20%** | **7알** |

= 동시 스파크 **20 → 7~8알** · 플래시·스파크 소등 **4% → 12~61%**.

⚑ **자의 문턱을 «잴 수 있는 것» 으로 세웠다 — 13회차 교훈의 재적용.**
처음에 세운 «최대 켜짐 ÷ 틱 ≤ 1.0» 은 **이 화면에서 원리적으로 못 잰다**: 설계상 소등은 틱의 45%
(듀티 0.55)인데 rAF 표본이 **26ms** 라 60~90ms 틱의 골을 절반쯤 놓친다 ⇒ 수리 후에도 ×4~19 가 찍힌다
(처방이 나빠서가 아니라 **샘플러의 한계**다 — 13회차가 «봉우리 수 ÷ 강화 수» 에서 겪은 그것).
⇒ ⓕ 는 **참고**로 내리고 문턱은 **ⓔ 소등 비율 ≥ 10%**(수리 전 4% 의 두 배 위)와 **ⓖ 동시 ≤ 8알**에 둔다.
⚠ 설계값(45%)을 문턱으로 박으면 **영원히 빨간 자**가 되고, 0% 로 두면 수리 전(4%)과 후(16%)를 못 가른다.

⚑ **듀티는 «켜짐 몫» 이 아니라 «소등이 남는가» 로 골랐다.** 1차 시도 **0.80**(펌프와 같은 값)은
스파크 소등을 14~17% 까지만 올렸다 — 틱 86ms 에서 골이 **17ms** 라 90ms 격자 캡처에 한 장도 안 걸린다.
링이 두 비평가에게 제대로 읽히는 이유가 **켜짐 30% / 소등 70%** 라는 것을 보고 회당 연출도 그쪽으로
당겼다(0.55). 더 내리는 길은 하한 45ms 가 막는다 — 최속 틱에서 «연출 없음» 이 되면 무조건 0점이다.

**곁다리 1건 — 스파크 가둠에 «여유 한 칸»**(`FXB_INPAD = 4`). 13회차의 «잉크 기준 가둠» 은 스필을
0 으로 만들었지만 잉크가 **액자 «위»** 에서 끝나 두 비평가가 그것을 절단으로 읽었다
(EF ② «폭의 21~38% 가 잘림» · EG ⑬ «최상단 y=151 = 링 안쪽 경계 151» — 2인 공통).
스폰 타원·링 반경·등간격은 한 픽셀도 안 건드리고 **가둠 상자만** 한 칸 더 들였다.

⚠ **13회차 인계문 4번(«첫 발 화폐 알갱이가 액자를 뚫는다» — EF ⑪ 16px · EG ⑭ 4~11px)은 이번에 안 고쳤고,
그 이유가 다음 회차의 재료다.** 그 알갱이는 `fxSpend` 가 **호스트 밖 재화 아이콘에서 출발시키는** 것이라
(583 «무엇으로 샀는가» 축 · `fxSpendFrom` 머리말) **액자를 지나는 것이 설계 자체**다. 스파크처럼
가두면 «출처» 가 통째로 사라진다 ⇒ 손댈 곳은 비행 경로가 아니라 **도착 뒤 잔여 잉크**이고,
그것은 `FX3_FLYS`(도착 스케일) 축이라 543 주인 지시와 같은 상수를 만진다. **재현부터 하고 잡을 것.**

**게이트** `verify619` **32/32** · `verify491` **72/72** · `verify625` **24/24**
(⚑ **셋 다 서명 이관 — 자를 «넓힌» 것이 아니라 «좁혔다»**: 세 자가 `upFx`/`fxFlash` 의 인자 목록을
문자열로 박고 있어 `iv` 를 더하자 빨개졌다. 333 처방대로 **뜻을 그대로 두고 새 축을 서명에 같이 박았다** —
이제 `iv` 가 빠지면 그 세 자가 빨개진다. 그냥 인자 검사를 느슨하게 풀었으면 **«회당 연출이 틱을 넘겨
살아도 초록인 게이트»** 가 됐을 것이다) ·
`verify488` 77/77 · `verify583` 36/36 · `verify324` 37/37 · `verify512` 31/31 · `verify621` 42/42 ·
`verify64` 44/44 · `verify518` 42/42 · `verify592` 35/35 · `verify349` 29/29 · `verify58` 27/27 ·
`verify93` 26/26 · `verify460` 29/29 · `verify612` 25/25 · **SMOKE PASS**.
**자** `tools/probe619d.js` **신설**(ⓔ 소등 비율 · ⓕ 최대 켜짐÷틱[참고] · ⓖ 동시 스파크).
`probe619c` 세 축도 그대로 초록(13회차가 닫은 자리를 안 무너뜨렸다).

**채점 — EH 6 / EI 6 (미달 · 13회차 EF 3 / EG 3 에서 **두 사람 다 세 칸 올랐다**)**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | 「8점을 막는 단 하나」 |
|---|---|---|---|---|---|
| EH | ③**6** | ④7 | ③④**7** | **6** | 훈련 회당 봉우리 층이 **액자를 넘는다** — 흰 헤일로만 안쪽으로 접어라 |
| EI | ③6 ④**6** | ③④7 | ③④**6** | **6** | 버스트 순간의 **흰 밴드를 호스트 f0 footprint 안쪽으로만**(inset) 그려라 |

⚑⚑ **두 사람의 「단 하나」가 문자 그대로 같은 문장이었다 — 15회차는 고를 것이 없다.**
그리고 둘 다 **«상시 앰버 링은 손대지 말라»** 를 스스로 덧붙였다(9회차 값 유지 · §「하지 말 것」).

**13회차의 두 「단 하나」는 실제로 닫혔다 — 두 사람이 프레임 수로 각각 확인했다:**

| 축 | EH | EI |
|---|---|---|
| ⑴ «9~11알 상시» | 동시 최대 **4알**(20→4) · 스파크 0알 프레임 훈련 **4/7** · 단련 **3/7** · 룬 **2/7** | 동시 최대 **정확히 4알** · 0알 프레임 **57% / 43% / 29%** |
| ⑵ «홀드 내내 켜짐» | 훈련 회당 워시 없는 프레임 **4/7** · 단련 **3/7** · 상시 링은 7/7 유지(바닥 안 내려갔다) | 몸통 플래시 **닫혔다**(훈련 소등 4/7 · 단련 3/7) · **단 «반만»** — 아래 ⚠ |

⚑ EI 의 «듀티 43% 는 실측 틱과 맞는다»(훈련 1575ms 에 13계단 = **121ms/틱**, 점등 50ms → **41%**)가
`probe619d` 의 설계값(듀티 0.55)과 독립으로 일치한다 — **자와 눈이 이번엔 같은 것을 보고 있다.**

⚠⚠ **그런데 그 처방이 새 결함을 하나 열었다 — 그것이 곧 15회차의 전부다.**
회당 플래시가 «틱마다 새로 켜지게» 되자 그 순간의 **흰 밴드가 호스트 밖으로 나가는 것**이 프레임마다
반복돼 눈에 띄게 됐다(전에는 상시로 켜져 있어 한 장의 배경처럼 읽혔다). 두 사람의 실측:

| 자리 | EH | EI |
|---|---|---|
| 훈련 알림 배지 | 붉은 픽셀 752 → **477(−36.6%)** f6 · 683(−9.2%) f1 | 검은 외곽선 715 → **251(−65%)** f6 · 322(−55%) f1 |
| 훈련 카드 밖 밴드 | 테두리 x38 → 흰 x30~34(**밖 20~24px**) | f0 테두리 x36~43 ↔ f1·f6 **바깥 x28~35 에 흰 밴드 8~10px** |
| 단련 위 «단련석» 패널 | f1 아래 테두리 (20,20,20) → **(92,77,41)**(휘도 +360%) · 위로 8px | 행↔바 틈 **6px(y131~136)을 100% 채움** · 같은 (92,77,41) |

⇒ 뿌리는 `.fx-flash` 의 **바깥 방향 글로우**(`box-shadow:0 0 0 4px …, 0 0 26px 2px …`)다.
흰 테두리 9px 자신은 `box-sizing:border-box` 라 안쪽에 그려지는데 **글로우만 밖으로 뻗는다.**
⚠ **13회차가 링에서 겪은 것과 정확히 같은 사고**이고(«outline 은 offset 의 바깥으로 자란다»),
`probe619c` ⓓ 는 **outline 축만** 보고 있어 이 자리를 못 봤다 — 15회차는 자를 **«잉크가 호스트
footprint 밖으로 나갔는가»** 로 넓혀야 한다.

**15회차가 볼 것 — 2인 공통만**:
1. ⚑⚑ **회당 흰 플래시를 «안쪽으로만» 그린다**(두 사람의 「단 하나」가 같은 문장 · 위 표).
   `box-shadow` 를 `inset` 으로 돌리는 것이 가장 짧은 길이다. ⚠ **`.fx-flash` 는 09·12·17 이 함께 쓰는
   부품**이라(182 주석 «한 세트») 전역으로 뒤집지 말고 **회당 발화(`upFx`)가 만든 장에만** 스코프를 건다.
   ⚠ 상시 앰버 링(9px·α.92)은 **한 값도 안 건드린다** — 두 비평가가 각각 그렇게 못박았다.
2. **훈련만 회당 «글자» 가 없다**(EI ③ «1575ms 에 13계단인데 «+21» 이 선 것 1회 = 7.7%» ·
   EH ④ «f3~f5 3연속 509ms 동안 Lv +4 인데 회당 잉크 0장»). ⚠⚠ **처방을 고를 때 583 금지선을 먼저 읽어라** —
   «+N»/«−N» 을 훈련에 새로 세우는 길은 주인 지시가 막았다(`verify488` [F3]). 남는 축은 «글자» 가 아니라
   **소등이 세 틱 연속으로 몰리지 않게 하는 것**(EI ⑨ · EH ④ 가 같이 지목한 «0.5초 공백»)이다.
3. **소등이 몰린다**(EI ⑨ 훈련 f3·f4·f5 3연속 509ms · 단련 f6·f7 2연속) — 듀티 0.55 는 «틱당 41%» 인데
   90ms 격자 캡처가 우연히 골에 세 번 연속 걸린다. 위상을 틱마다 조금씩 밀거나(캡처 격자와 안 물리게)
   듀티를 0.55 → 0.65 로 올리는 두 갈래. ⚠ **먼저 재라** — `probe619d` ⓔ 가 그 자다.
4. **훈련 스파크가 «+21» 라벨을 먹는다**(EH ② 60% · EI ④ 44% — 2인 공통) — `strict` keep-out 구멍에
   `.cv` 노드가 안 들어 있다. `fxbTextHoles` 에 한 줄이면 된다(619 8회차가 `.fx-plus`·`.rb` 를 그렇게 넣었다).
5. **코인/룬석 알갱이가 액자를 뚫는다**(EH ③ 훈련 코인 38% 밖 · EI ⑥ 룬석 18px 밖) — 위 ⚠ 의
   `fxSpend` 축이라 재현부터.
6. **룬만 몸통 워시가 0/7**(EH ⑧ · EI ⑧ — 2인 공통 ④): 룬 호스트는 998×700 패널이라 9회차가
   `--flash-to` 로 플래시 자리를 좁혀 놨다(«화면 전면 워시» 감점을 닫은 자리다). 두 사람이 이번엔
   그것을 «어휘 불일치» 로 읽었다 — **되돌리지 말고** 룬의 플래시 자리를 «카드» 급으로 넓히는 쪽을 보라.

### 15회차 (2026-09-01, sess-0928-11017 루틴 워커 B · 14회차와 한 세션) — 14회차 2인 공통 「단 하나」 · 채점 **EJ 6 / EK 3**

14회차 두 비평가의 「8점을 막는 단 하나」가 **문자 그대로 같은 문장**이라 고를 것이 없었다 —
«버스트 순간의 흰 밴드를 호스트 footprint **안쪽으로만** 그려라 · 상시 앰버 링은 손대지 마라».

⚑ **재현이 뿌리를 «그림자» 에서 «확대» 로 옮겼다.** 처음 의심한 것은 `.fx-flash` 의 바깥 글로우
(`box-shadow:0 0 0 4px …, 0 0 26px 2px …`)였는데, `@keyframes fxFlash` 를 읽으니 **`scale(1.06)`** 이 있다.
플래시 상자는 호스트 rect «그대로» 라 6% 확대가 곧 **바깥 3%**다 — 훈련 카드 326×510 에서
좌우 **+9.2px** · 위아래 **+14.4px** 이고, 이것이 EI 가 잰 «바깥 8~10px 흰 밴드» 와 같은 수다.

⚠⚠ **두 비평가가 쓴 낱말(«inset»)을 그대로 코드로 옮기면 안 되는 자리였다** — `box-shadow` 를
`inset` 으로 뒤집는 길은 **12회차가 이미 폐기했다**(«퍼짐 10px + 흐림 34px 이 알파 .92 로 카드 전체를
메워 강화 순간에 카드 내용이 통째로 사라진다» — 2인 독립 ④ 2점, CSS 머리말이 «안쪽 글로우는 쓰지
마라 · 강조는 바깥 림만으로» 로 못박아 뒀다). ⇒ **뜻만 취하고 수단을 바꿨다**: 림·확대·색·길이는
한 값도 안 건드리고 **플래시 «상자» 의 네 변만** 넘치는 만큼 안으로 들인다
(13회차가 스파크 가둠에서 쓴 것과 같은 수). 들이는 양은 상수가 아니라 **식**이다 —
확대가 «비율» 이라 호스트가 크면 넘치는 양도 크다: `pad = size × (1 − 1/PEAK) / 2 + RIM`(축마다 따로).

**자 — `tools/probe619e.js` 신설**(ⓗ 호스트 밖 24px 띠의 변화율 · ⓘ 훈련 알림 배지 붉은 픽셀 보존율).
⚠ **ⓗ 는 문턱으로 못 쓴다** — 621 눌림 왕복과 488 맥박이 **호스트 자신을 확대**하므로 정적 bbox 밖 띠는
플래시가 없어도 매 틱 바뀐다(실측 20~42%). 그래도 **같은 자로 전·후를 비교**하는 데는 쓸 수 있다:

| 축 | 수리 전(14회차 트리) | 수리 후 |
|---|---|---|
| ⓗ 훈련 밖 띠 변화(최악 프레임) | **72.29%** | **28.55%** |
| ⓘ 훈련 배지 붉은 픽셀 보존 | 1.92% | 2.44% |

⚠ **ⓘ 의 red 판정을 한 번 고쳐 세웠다** — «붉다» 를 `r−g`·`r−b` 차로만 물으면 **앰버 글로우**
(255,186,54)가 통째로 걸려 «보존 121%» 라는 헛수가 나온다(1차 시도). 배지 원반은 채도 높은 적색이라
G·B 가 둘 다 낮다(`g<90 && b<90`)로 좁혔다.

**채점 — EJ 6 / EK 3**

| 비평가 | 훈련 | 룬 | 단련 | 최저 | 「8점을 막는 단 하나」 |
|---|---|---|---|---|---|
| EJ | ①**6** ③6 | ②**6** ④6 | ①**6** ③6 | **6** | **룬 몸통에 회당 워시를 세워라**(7/7 프레임 warm 1.0 — ②와 ④를 동시에 묶고 있다) |
| EK | ③**4** | 6 | ③**3** | **3** | **단련 «비용 단련석» 스프라이트의 상승량을 행 footprint 안으로 클램프** |

**⑴ 목표한 축은 닫혔다 — 두 사람이 각각 확인했다.**

| 자리 | EJ | EK |
|---|---|---|
| 훈련 카드 밖 흰 픽셀 | EI 가 잰 그 띠(x28~35)에서 **7/7 프레임 0/4080** | 밖 24px 띠 근백색 **0/42,432** · 흰 밴드가 f0 외곽선보다 **19px 안쪽**에서 시작 |
| 단련 위 패널 아래 틈 | **(92,77,41) 은 7/7 프레임 0px** · 틈이 f0 보다 오히려 밝아졌다 | 플래시 자체 번짐은 «실제로 줄었다»(테두리 ΔR **+14** 뿐 · 14회차 +72) |
| 룬석 알갱이 | top y=137 = 패널 잉크 가장자리 = **밖 0px**(14회차 EI ⑥ «18px 밖» 닫힘) | 룬젬 top y158 = 링보다 **10px 안쪽** |

**⑵ 그런데 «배지» 는 나빠졌다 — 그리고 두 사람이 그 이유까지 같이 적었다.**

| | 14회차 | 15회차 EJ | 15회차 EK |
|---|---|---|---|
| 배지 붉은 픽셀 최악 | −36.6% | **−45.7%**(f7) | **−44.7%**(f7) |
| 배지 검은 외곽선 최악 | −65% | −57.3% | −57.2% |

⚑⚑ **두 사람이 독립으로 같은 진단을 냈다 — «안으로 들이기» 로는 이 자리가 원리적으로 안 닫힌다.**
배지는 카드 **footprint 안쪽**(x335~364 ⊂ x32~365)에 앉아 있어서, 상자를 안으로 들이면 흰 밴드가
**배지 쪽으로 다가온다.** EJ: «점등 프레임 3/3 전부에서 깨지고 소등 프레임 4/7 만 보존된다» ·
EK: «액자 밖으로 새는 게 아니라 앰버림+흰밴드가 배지 위에 그려진다 — 상자를 들여서는 못 닫는 자리».
⇒ **16회차의 처방은 «keep-out»** 이다(둘 다 그렇게 적었다): 배지 원반 bbox + 4px 를 플래시 상자에서 뺀다.

⚠⚠ **이번 회차가 만든 비용 하나 — 카드 자신의 액자선**(EJ ⑤, 이 회차 고유):
버스트 틱에 잉크 폭 **334 → 323px(−3.3%)**, 좌변 최암 픽셀 **(20,20,20) → (132,100,39)**(휘도 **+365%**)
인데 **같은 프레임의 이웃 카드는 (20,20,20) 그대로**다 = 대상 카드만 자기 액자를 잃는다.
⇒ 16회차는 **`FXFLASH_RIM`(8) 을 줄이는 것부터** 보라 — 넘침의 지배항은 확대분(9.2px)이지 림이 아니다.
RIM 을 2 로 내리면 밖 흰 픽셀 0 은 유지하면서 흰 밴드가 6px 덜 들어온다(배지·액자선 둘 다 이득).
⚠ **재측정 없이 고치지 마라** — `probe619e` ⓘ 와 캡처 채점이 그 자다.

**⑶ 13·14회차가 닫은 것은 안 무너졌다**(두 사람 모두 확인):

| 축 | EJ | EK |
|---|---|---|
| 스파크 0알 프레임 | 훈련 3/7 · 룬 3/7 · 단련 4/7 · 동시 최대 **4~5알** | 훈련 3/7 · 룬 3/7 · 단련 4/7 · «구워짐 없음» |
| 몸통 플래시가 틱 사이에 내려가는가 | 훈련 warm 7.2 ↔ **54.7** · 단련 1.0 ↔ **50.1** (소등 4/7) | 훈련 ΔB 진폭 **43** · 단련 **45** |

⚠ **EK ⑤ 가 «583 회귀 아닌가» 를 물었다 — 아니다.** f1·f2 의 «+21» 은 `fxUpOk` 의 넷째 인자로 들어가는
**628 «방금 오른 양»**(홀드가 **멎을 때** 한 장)이고, 583 이 걷어낸 것은 **틱마다 뜨던 «−n 금액»** 이다.
`verify488` **[F3] 77/77 초록**이 그 축을 지키고 있다(이번 회차에도 무수정 통과).

**15회차가 남긴 것 — 16회차가 볼 것(2인 공통 우선)**:
1. ⚑⚑ **훈련 알림 배지를 회당 플래시의 keep-out 으로 등록**(EJ·EK **2인 공통** — 둘 다 «상자를 들여서는
   안 닫힌다» 를 실측으로 못박았다). 배지 원반 bbox + 4px.
2. ⚑ **`FXFLASH_RIM` 을 줄여 카드 자신의 액자선을 되살린다**(EJ ⑤ — 이번 회차가 만든 비용).
3. **단련 «비용 단련석» 스프라이트 상승 클램프**(EK 「단 하나」 — top y108 이 행 검은테보다 **32px 위** ·
   위 패널에 잉크 1,029px · 패널 테두리 검정 **−30.7%** · (20,20,20)→**(224,75,60)**).
   ⚠ 14회차가 «`fxSpend` 는 호스트 밖에서 출발하는 것이 설계» 라고 미뤄 둔 그 축인데, EK 가 **«레인·색·
   개수·출발 x 는 그대로 두고 이동 거리 상한만»** 이라는 길을 줬다 — 출처는 그대로 읽히고 침범만 사라진다.
4. **룬 몸통 워시가 7/7 프레임 평탄**(EJ 「단 하나」 · EH ⑧·EI ⑧ 과 합쳐 **3회차 연속 2인 공통**):
   warm 1.0~1.2 ↔ 훈련 44.6~54.7 · 단련 35.3~50.1. EJ 의 처방은 «9회차 `--flash-to` 를 되돌리지 말고
   워시 대상을 **«룬 카드» 블록**(x85~1010 · y170~370)으로 스코프» 다. ⚠ 룬 오브가 안 보이는 것(EJ ⑥
   «4프레임 중 2프레임이 대비 10/255»)도 이 한 값에 물려 있다 — 흰 오브가 (255,255,254) 위에 있어서다.
5. **소등이 3연속으로 몰린다**(EJ ①·EK ①③ — 14회차에 이어 **2회차 연속 2인 공통**):
   훈련 f4·f5·f6 **447ms**(+4레벨) · 단련 f3·f4·f5 **432ms**(+3레벨). 14회차 509ms → 447ms 로 **12% 만** 줄었다.
   ⚠ **먼저 재라** — `probe619d` ⓔ 가 그 자다. 갈래는 «듀티 0.55 → 0.65» 와 «틱마다 위상 밀기»(90ms 캡처
   격자와 안 물리게) 둘이고, 위상 쪽이 «틱당 점등 비율» 을 안 건드려 ⑴을 안 되돌린다.
6. **훈련 스파크가 «+21» 을 100% 덮는다**(EJ ④ — 14회차 지적 4번이 **한 글자도 안 들어갔다**):
   `fxbTextHoles` 에 `.cv` 한 줄. 619 8회차가 `.fx-plus`·`.rb` 를 그렇게 넣은 선례가 있다.

⚠ **하지 말 것(15회차에 다시 확인)**: 훈련에 «+N»/«−N» **신설**(583 · `verify488` [F3]) ·
상시 앰버 링 밝기·두께 내리기(9회차 2인 공통) · 링을 바깥으로 키우기(`probe619c` ⓓ) ·
플래시에 **inset 글로우**(12회차 ④ 2점) · 단련·룬 플로터 레인 이동(612 [I6b] 래칫 · 띠 여유 0).

**게이트** `verify619` **32/32** · `verify491` **72/72** · `verify625` **24/24**(⚑ 셋 다 서명 이관 —
14회차와 같은 규약으로 **좁혔다**: `fxFlash(el, iv, inset)` · `fxUpOk(…, cur, inset)` · `fxFlash(fel, iv, true)` ·
`fxUpOk(card, card, txt, bi0.cur, true)` 를 그대로 박았으므로 `inset` 축이 빠지면 세 자가 빨개진다) ·
`verify488` 77/77 · `verify583` 36/36 · `verify621` 42/42 · `verify512` 31/31 · `verify324` 37/37 ·
`verify64` 44/44 · `verify518` 42/42 · `verify58` 27/27 · `verify93` 26/26 · **SMOKE PASS** ·
`probe619c` 세 축 · `probe619d` 세 축.

### 16회차 (2026-09-01, sess-1107-2662 루틴 워커 A) — 15회차 잔여 5건 전부 착수 · 채점 **EL 3 / EM 2**

15회차가 남긴 목록을 **다섯 건 다** 잡았다(2인 공통 둘 · 「단 하나」 둘 · 두 회차 연속 무시된 지적 하나).
자 넷 중 **넷이 초록**인데 채점은 **EL 3 / EM 2** 로 15회차(EJ 6 / EK 3)보다 내려갔다 —
그 어긋남 자체가 이 회차의 가장 큰 소득이라 아래 ⑷ 에 따로 적는다.

**⑴ 고친 다섯 자리와 그 자(전부 실측 전·후)**

| # | 자리 | 근거 | 수리 전 | 수리 후 | 자 |
|---|---|---|---|---|---|
| 1 | 훈련 알림 배지가 플래시에 지워진다 | **EJ·EK 2인 공통** | 보존 **4.44%** | **95.82%** | `probe619e` ⓘ |
| 2 | 훈련 «+n» 플로터를 스파크가 덮는다 | EJ ④(14·15 연속 무시) | **100%** | **0%** | `probe619f` ⓛ |
| 3 | 단련 알갱이가 위 패널을 침범한다 | EK 「단 하나」 | **32px** | **0px** | `probe619f` ⓙ |
| 4 | 룬 몸통에 회당 워시가 없다 | EJ 「단 하나」 · **3회차 연속 2인 공통** | 프레임 **0%** | **45%**(패널의 21%) | `probe619f` ⓚ |
| 5 | 카드 자신의 액자선(15회차가 만든 비용) | EJ ⑤ | `FXFLASH_RIM` 8 | **2** | — (아래 ⑷) |

**⑵ 「빼기」 를 「위에 다시 그리기」 로 뒤집었다 — 두 길이 막힌 것을 재현으로 확인하고서다.**
두 비평가가 같이 쓴 낱말은 «배지 원반 bbox + 4px 를 플래시 상자에서 **뺀다**» 였다. 그런데:
- ⓐ **`mask` 로 구멍** — 마스크 칠 영역이 border-box 로 잘린다. `mask-clip:no-clip` 은 이 크로뮴에서
  computed 가 `border-box` 로 되떨어지고(직접 띄워 확인), 그러면 `box-shadow`(하드 림 4px + 흐림 26px)가
  **통째로 사라진다** = 12회차 CSS 머리말 「강조는 바깥 림만으로」 를 깨뜨린다.
- ⓑ **`clip-path` 로 구멍** — 참조 상자를 넘겨 그릴 수 있어 림은 살지만, 좌표가 **transform 전** 로컬
  좌표라 `@keyframes fxFlash` 의 `scale(1.06)` 을 **구멍이 같이 탄다**: 배지는 중심에서 (154, −198)
  떨어져 있어 봉우리에 구멍이 **(9.2, 11.9)px 밀린다** — 정작 점등 프레임에서 빗나간다.
⇒ 15회차가 «inset» 낱말에서 한 것과 같은 처리로 **뜻만 취하고 수단을 바꿨다**: `fxFlashKeep()` 이
배지 원반을 플래시 **위에** 다시 그린다. 그림은 손으로 안 베끼고 `getComputedStyle(배지).background`
를 그대로 읽어 오므로(`.tr-card>.dot` 은 붉은 코어와 검정 링을 radial-gradient **한 장**에 그린다)
배지 규격이 바뀌면 패치가 저절로 따라오고 **새 상수는 0 개**다.

**⑶ 「한 번 찍고 끝」 이 아니었다 — 이 회차가 배운 것 셋(전부 자가 먼저 잡았다).**
- ⓐ **정지 패치는 유령을 만든다.** 홀드 중 배지 중심이 패치에서 **최대 25.49px** 벗어나고 배지 폭도
  39.5~45.6 을 오간다. 뿌리는 **621**(주인 지시 «틱마다 원래 크기 ↔ 눌린 크기 왕복») — 카드가 틱마다
  크기를 오가는데 배지는 우상단 코너라 중심에서 ~198px 떨어져 있어 배율 1% 가 2px 다.
  ⇒ rAF 로 따라가게 했다. **비평가 둘이 이 결함을 독립으로 찍어 왔다**(EL 「f2 에 완전한 원반 **2개**
  (752 + 750px)」 · EM 「bbox 가 y166→188 로 22px 늘어남 = 붉은 잔상 원 1개 추가」) — **캡처가 수리
  전 트리라 그렇다**(캡처 → 채점 사이에 자가 먼저 잡아 같은 회차에 고쳤다). 지금 트리의 중심 어긋남은
  **0.03~9.3px** 이고 `verify619` [K] 가 그것을 묶는다.
- ⓑ **패치가 새 플래시 «밑» 에 깔린다.** 앞 틱 패치가 안 걷히면 DOM 순서가 `keep|flash|keep` 이 되어
  깔린 장은 배지를 못 지킨다 — «패치는 있는데 배지는 지워진» 프레임이 생겨 배지 보존이 실행마다
  **7% ↔ 97%** 로 뒤집혔다. ⇒ `fxFlashKeep` 머리에서 앞 패치를 전부 걷는다(625 가 플래시에 하는 것과
  같은 «한 자리에 한 장»). 수명도 플래시에 묶었다 — 틱 60~175ms 인데 `fxBye` 폴백 500ms 라 다섯 겹까지 쌓였다.
- ⓒ **rect 로 물으면 렌더에 없는 결함을 잡는다.** 「패치가 배지를 품는가」 를 rect 로 물었더니 66 표본 중
  **30 이 17~19px 어긋남** 으로 나왔는데, `getBoundingClientRect` 는 애니메이션 중인 배지를 **지금 시각**
  으로 계산해 돌려주고 패치 좌표는 **직전 rAF** 에 쓴 값이다 — 브라우저는 rAF 를 다 돌린 «뒤» 합성하므로
  **실제로 찍히는 프레임에서는 둘이 같은 시각**이다. ⇒ [K] 를 «찍힌 픽셀» 로 갈아 끼웠다(350 규칙).

**⑷ ⚑⚑ 자 넷이 초록인데 점수는 내려갔다 — 두 비평가가 **같은 뿌리**를 지목했다(2인 공통 「단 하나」).**

| | EL | EM |
|---|---|---|
| 점수 | 훈련 4 · 룬 3 · 단련 3 → **3** | 훈련 5 · 룬 3 · 단련 2 → **2** |
| 「단 하나」 | 플래시 상자의 **하드 에지가 호스트 «안» 콘텐츠 위에 떨어진다** | 링·워시가 행 콘텐츠보다 **위 레이어**라 정보를 덮는다 |

같은 자리를 각자 수치로 쟀다 — 단련 비용 «1» **−48.8%**(EL: f7 밴드 x995~1003 이 «1»(x998~1008)을
100% 덮음 · EM: 오렌지 85px, 명암비 4.69:1 → **1.68:1**) · 룬 «공격력» **−40.2~44.2%** · CTA «단련»
명암비 2.18:1 → **1.34:1**.
그리고 **⑸ 액자선은 되레 나빠졌다**(2인 공통): 훈련 좌변 검정 (20,20,20) → **(203,150,49) 휘도 +570%**
(15회차 +365%), EM 은 **7프레임 중 6프레임에서 0%** 이고 **룬·단련에도 같은 회귀**가 있다고 적었다.
⇒ `FXFLASH_RIM` 8 → 2 는 **틀린 손잡이였다**. 넘침의 지배항은 림이 아니라 **확대분**이고, 림을 줄이면
상자가 «덜 들어와» 오히려 액자선 위에 더 정확히 앉는다.

⚑⚑ **17회차가 볼 것 — 뿌리 가설 하나로 모인다: «회당 플래시가 호스트를 안 따라간다».**
이 회차에 배지 패치를 만들면서 **호스트가 틱마다 최대 25.5px 움직인다**는 것을 처음 쟀다(⑶ⓐ).
그런데 **플래시는 만들 때 호스트 rect 로 한 번 찍고 끝이다** — 즉 홀드 내내 상자가 카드에서 최대 25px
어긋난 채 떠 있다. 두 비평가가 «에지가 엉뚱한 콘텐츠 위에 떨어진다»·«호스트 자신의 액자선이 사라진다»
로 잡은 그림이 **정확히 그 어긋남의 모양**이다(둘 다 «이웃 카드는 7/7 멀쩡» 이라고 같이 적었다 —
움직이는 것은 «눌린 카드» 뿐이다). ⇒ **17회차 1순위는 `fxFlash` 에 패치와 같은 rAF 추적을 붙이는 것**
이고, 그것이 서면 `FXFLASH_RIM`·`pin()` 의 «안으로 들이기» 자체가 필요 없어질 수 있다(먼저 재고 고쳐라).
EM 이 준 **되돌림 가설**도 같이 있다: 「링·워시를 행 콘텐츠보다 **아래 레이어**로」.

**17회차가 볼 것(그 다음 순위)**:
1. **단련 소등 3~4연속 — 악화**(EL #1 479ms · EM #6 479ms · 15회차 432ms 대비 **+10.9%**). EM 이
   «꺼진 게 아니라 **켜진 채 구워져 있다**» 를 못박았다(f4·f5·f6 순백 몸통 103,347/103,536/103,383px
   = 편차 **0.18%** · 968ms 무입자 공백). **2회차 연속 2인 공통**이다.
2. **룬 알갱이가 탭바를 침범한다 — 2인 공통 신규**(EL: 패널 액자보다 **49px** 위 · 탭 버튼 위 715px ·
   EM: **57px** 위 · 582px). ⚑ **16회차가 단련에 쓴 그 클램프를 룬은 안 탄다** — 룬은 화폐 아이콘이
   호스트 밖에 없어 `fxSpendFrom` 의 **③ 폴백**(상변 안쪽 30px)으로 가고, 클램프는 **②** 에만 걸었다.
   같은 산수를 ③ 에도 주면 되는 자리다(상자 반높이 `ics × FX3_FLYS / 2` + `FX_SPEND_JY`).
3. **룬·단련 호스트가 영구 오프셋된 채 복귀 안 한다**(EM #4 — 룬 제목·진행바 **+8~11px** 고정,
   훈련만 f2·f5·f7 에 복귀). 621 왕복이 룬·단련에서 «왕복» 이 아니라 «한쪽으로 눌린 채» 굳는다는 뜻이라
   **621 과 같이 볼 것**.
4. **캡처 간격이 규격을 못 지킨다**(EM #12 — 목표 90ms 인데 실측 **237~328ms**, 훈련 f6→f7 한 칸에
   틱이 **2회** 지나갔다). `cap619.js` 는 `screenshot` 왕복이 곧 간격이라 GAP 이 하한으로만 작동한다 —
   17회차는 **≤60ms** 로 찍어야 「틱마다」 를 원리적으로 검증할 수 있다(CDP 연속 캡처나 clip 축소).
5. **화폐 알갱이가 배지를 스친다**(16회차 자가 관측 — `probe619e` ⓘ 가 3회 중 1회 **11.15%** 로 흔들린다).
   플래시가 아니라 `.fx-spd`(잉크 지름 115.5px)가 지나가는 한 프레임이다. `verify619` [K3] 는 그래서
   «최저» 가 아니라 **«성한 프레임 비율»** 로 묻는다(문턱 2/3). 축이 다르므로 따로 잡아라.

⚠ **하지 말 것(16회차에 다시 확인)**: 훈련에 «+N»/«−N» **신설** · 상시 앰버 링 밝기·두께 내리기 ·
링을 바깥으로 키우기 · 플래시에 **inset 글로우**(12회차 ④ 2점) · 단련·룬 플로터 레인 이동 ·
**그리고 새로: `FXFLASH_RIM` 을 더 내리는 것**(16회차가 8 → 2 로 내려 액자선이 +365% → **+570%** 로
나빠졌다 — 손잡이가 아니다).

**게이트** `verify619` **39/39**(⚑ **[K] 6항 신설** — K3 «성한 프레임 ≥2/3» · **K4 되돌림**(keep-out 을
끄면 패치가 사라지고 붉은 픽셀이 무너진다) · K5 원복 · K6 «`inset` 없는 단발 플래시는 불변» = 09·12·17·
코스튬·장비 회귀 방지. **5회 연속 39/39** 로 흔들림 없음을 확인했다) ·
이관 무수정 `verify625` 24/24 · `verify491` 72/72 · `verify621` 42/42 · `verify488` 77/77 ·
`verify583` 36/36 · `verify512` 31/31 · `verify324` 37/37 · `verify64` 44/44 · `verify518` 42/42 ·
`verify58` 27/27 · `verify93` 26/26 · **SMOKE PASS** ·
`probe619f` **신설 3/3**(ⓙ 단련 침범 · ⓚ 룬 몸통 워시 · ⓛ 플로터 가림) · `probe619e` ⓘ 95.82%.

⚑ **다음 세션에게 — 자를 세울 때 두 번 밟은 함정**(둘 다 이 저장소가 이미 적어 둔 것이다):
① **`localStorage.clear()` 를 리로드 «전» 에 부르면 안 된다** — `beforeunload → save()` 가 떠나는 길에
그 키를 되살린다(363 교훈). `addInitScript` 로 **새 문서가 뜨기 전**에 지워야 게임이 세이브 없이 부팅한다.
② **한 페이지에서 훈련 카드를 여러 번 홀드하면 단계 상한에 닿는다** — 그러면 뒤 절이 «살 것이 없어»
0/0 으로 빨개져 **자가 자를 방해한다**. 되돌림 절이 «0» 을 요구할 때는 «무력화해서 0» 과
«홀드가 안 먹어서 0» 을 반드시 갈라라(K4 가 `dotFrames`·`flashOnly` 를 같이 묻는 이유다).

### 17회차 (2026-09-01, sess-1313-23249 루틴 워커 A) — 16회차 1순위(플래시 추적) · 채점 **EP 3 / EQ 3**

16회차가 남긴 1순위(«회당 플래시가 호스트를 안 따라간다»)를 **닫았다**. 2순위(룬 알갱이 상변)는
걸어 봤다가 **실측으로 되돌렸다** — 되돌린 이유가 이 회차의 가장 큰 소득이라 ⑷ 에 따로 적는다.

**⑴ 제품 — 고친 것은 한 자리다(`fxFlash`, `index.html`)**

16회차까지 플래시 상자는 **스폰 시각의 rect 로 한 번 찍고 끝**이었다. 621(주인 지시 «틱마다
원래 크기 ↔ 눌린 크기 왕복»)이 호스트를 홀드 내내 흔들므로 상자만 그 자리에 굳는다.
`probe619g` **신설**로 재현했고 수리 전·후는 이렇다(축은 «상자 중심 ↔ 자기 호스트 중심»):

| 화면 | 수리 전 평균 | 수리 전 최악 | 수리 후(스폰 프레임 제외) |
|---|---|---|---|
| 훈련 카드 | 4.45px | **8.02px** | **0.02px** |
| 룬 [강화] | 4.87px | **11.59px** | **0.02px** |
| 단련 [단련] | 0.80px | **6.01px** | **0.02px** |

처방은 16회차 배지 패치와 **같은 꼴**이다 — 노드가 살아 있는 동안 rAF 로 호스트 rect 를 다시
읽어 자리를 쓴다. `inset` 을 준 호출(= 619 회당 발화)에서만 돌아 09·12·17·코스튬·장비의 단발
플래시는 한 프레임도 안 바뀐다([K6] 가 그것을 묶는다).

⚑ **이것이 `FXFLASH_RIM` 이 손잡이가 아니었던 이유다.** 16회차가 8 → 2 로 내리자 액자선 훼손이
+365% → **+570%** 로 되레 나빠졌다 — 넘침의 지배항은 림이 아니라 **어긋남**이라, 상자를 더 들이면
«덜 들어온» 상자가 엉뚱한 자리에 더 정확히 앉을 뿐이었다.

**⑵ ⚠⚠ 자를 세우다 밟은 함정 둘 — 둘 다 이 파일이 이미 적어 둔 것이다**

- **ⓐ rAF 등록 순서가 축을 흔든다**(16회차 ⑶ⓒ 와 같은 함정). 추적(`follow`)과 자(`tick`)는 **같은
  프레임의 rAF 큐**에 있고 순서는 등록 순이다 — 자가 먼저면 상자는 아직 **직전 프레임** 자리다.
  브라우저는 rAF 를 다 돌린 «뒤» 합성하므로 **찍히는 프레임에서는 둘이 같다**. 1차 시도가 이것을
  안 갈라 **같은 트리에서** 단련 0.02px · 훈련 8.02px 로 화면마다 딴 답을 냈다(둘 다 추적은 켜져
  있었다). ⇒ 축을 «이번 **또는** 직전 프레임의 호스트와 맞는가» 로 세웠다.
- **ⓑ 스폰 프레임은 추적의 품질이 아니다** — 그 프레임엔 `follow` 가 아직 한 번도 안 돌았다.
  섞으면 한 프레임짜리 값(1.9~4.2px)이 최악값을 통째로 지배한다. 갈라서 센다.

**⑶ ⚑⚑ 캡처 하네스가 «제품이 아니라 창» 을 채점하게 하고 있었다 — 1차 채점을 통째로 버렸다**

16회차 EM #12(«목표 90ms 인데 실측 237~328ms · 한 칸에 틱이 2회»)를 따라 `cap619.js` 를 고쳤다.
`page.screenshot()` 은 **요청-응답 왕복**이라 인코딩·전송이 통째로 간격에 들어가 GAP 이 하한으로만
작동한다(디스크 쓰기를 홀드 밖으로 빼도 220~343ms 로 그대로였다 — 병목은 왕복 자체다).
⇒ **CDP `Page.startScreencast`**(합성마다 밀어 준다) 로 바꾸고 목표 격자에 가장 가까운 **«남은»**
프레임을 고른다(«가장 가까운» 만 쓰면 훈련이 8칸 중 **4칸이 같은 장** 이었고, 그것은 «이 틱엔
아무것도 안 터졌다» 로 **거짓으로** 읽힌다). 실측 간격 **218~343ms → 18~95ms**.

⚠⚠ **그런데 간격을 좁힌 대가로 «창» 이 무너졌고, 1차 비평가 둘이 그것을 채점했다.**
7장 × 60ms = span **420ms** 인데, 홀드 발화 시각을 처음 재 보니:

- 훈련 320,483,633,745,858,968,… · 단련 349,513,650,785,… · **룬 634**,748,948,1025,…

즉 **첫 발화가 320~634ms**(홀드 반복 진입 지연)라 420ms 창에는 발화가 훈련·단련 **1회**, 룬은
**0회** 들어간다. 1차 비평가(EO)는 그래서 «발화는 f3 딱 한 번 · 219~267ms 동안 두 번째 틱이
없다 · 감쇠가 전혀 없다» 로 **①타이밍 3점**을 줬다 — 제품이 아니라 내 창을 잰 것이다.
⇒ **LEAD 300ms + 60×12 = 창 347~1014ms · 창 안 발화 4~5회 · 13장 전부 상이**로 다시 찍고
**EP·EQ 로 다시 채점했다.** 1차 채점(EN·EO)은 **무효**다.
아울러 크롭이 **디코드 실패를 조용히 넘기고** 있었다(`onerror` 도 resolve) — 못 읽은 장을 그대로
그려 «호스트가 통째로 사라진» 프레임을 만들고 있었고, 1차 비평가 둘 다 그것을 «카드 미렌더»
결함으로 보고했다(= 자가 만든 유령). 실패한 장은 버리고 그 수를 찍게 했다.

**⑷ ⚑⚑ 룬 알갱이 상변 클램프 — 걸었다가 «걸면 안 된다» 를 실측하고 되돌렸다**

등재 사유는 진짜다(EL **49px** · EM **57px** 2인 공통 · `probe619g` ⓝ 수리 전 **잉크 25~44px 이탈**).
16회차가 단련에 건 ② 클램프를 룬이 타는 **③ 폴백**에 같은 산수로 주면 이탈은 **0px** 이 된다.
**그런데 그 대가가 설계 파괴였다** — 룬 상자 반높이는 `ics 55 × FX3_FLYS 2.10 / 2` = **57.75** 라
출발이 `hr.y + 73.75` 가 되는데, 룬의 도착지(`--spend-to` 좌측 아이콘 타일, 9회차 결정)는 중심이
`hr.y + 137` 부근이고 **타일 반높이가 ≈97px** 다 ⇒ **출발이 도착 타일 «안»** 이 된다.
그것은 `fxSpendFrom` 머리말의 «목적지 안에서 출발하면 방향이 안 보인다» 를 정면으로 깨고,
`verify583` **[D-rune-0]** 이 그 자다(실측 비행 **107px → 57px**, 문턱 60 에서 5회 중 1회 빨강).
⇒ **되돌렸다.** 룬 행은 «알갱이 상자(115.5px) + 읽히는 비행» 을 둘 다 담기에 세로가 모자라고,
그건 클램프 산수로 못 푸는 **설계 선택**이다. 18회차/주인 판단 몫으로 넘긴다(§6-A).

⚠ **우회로 둘도 막힌 것을 실측으로 확인했다(다음 세션이 다시 시도하지 말 것)**:
- **«산포 뒤로 클램프를 옮긴다»** — 미리 물던 `FX_SPEND_JY` 16px 이 사라지는데, 그 16px 이 단련에서
  «`fxSpend` 가 받는 호스트 rect ↔ 행 `.tr-tp.k0`» 사이의 **13.25px 기저 차이**를 덮고 있었다.
  제거하자 `probe619f` ⓙ 가 0 → **9.67px** 로 **3회 연속** 재현됐다.
- **«반높이에 `fxGrainSc` 를 곱한다»** — 틀렸다. `fxGrainSc` 는 `--fxgs` 로 **안쪽 아이콘**만 키우는
  재화별 잉크 보정이고(`verify583` [C] 가 재는 것이 그 안쪽이다), 상변을 넘는 것은 `scale(FX3_FLYS)`
  만 타는 **바깥 `.fx-spd` 상자**라 크기가 재화와 무관하게 115.5 다. 넣었더니 tstone(0.809)에서
  클램프가 11px 얕아져 `probe619f` ⓙ 가 **9.17px** 로 빨개졌다.

**⑸ 채점 — EP 3 / EQ 3 (16회차 EL 3 / EM 2)**

| | EP | EQ |
|---|---|---|
| ① 타이밍 | 4 | **5** |
| ② 크기 비율 | 4 | 5 |
| ③ 위치·경로 | **3** | **3** |
| ④ 가독성·일관성 | **3** | **3** |
| 점수 | **3** | **3** |
| 화면별 | 훈련 3 · 룬 2 · 단련 3 | 훈련 4 · 룬 3 · 단련 3 |

⚑ **①타이밍이 3 → 4·5 로 올라온 것이 창 교정의 효과다** — 둘 다 «발화 자체는 매 틱 있다» 를
프레임으로 세어 확인했다(EQ: 훈련 4틱 4발화 · 룬 4틱 · 단련 5틱 · EP: 훈련 4/4 · 룬 4/4 · 단련 5/5).
주인 지시 본문(«매 강화마다 터진다»)은 자·눈 양쪽에서 서 있다.

**⑹ ⚑⚑ 「8점을 막는 단 하나」가 2인 완전 일치 — 18회차의 1순위다**

- **EP**: «워시/플래시 사각형을 호스트의 **패딩 박스**가 아니라 그보다 좁게 그린다 — 좌우 테두리가
  «공/체»(테두리 x92~110 vs 글리프 x87..107)와 «다음 +3.0%»(x973~988 vs 글리프 런 x868..994) 획
  위에 떨어진다. 상자가 뜨는 **6프레임 전부**.»
- **EQ**: «워시 상자의 세로 모서리가 라벨 글자 한가운데서 끝난다 — 룬 좌변 x=93~103 이 «공격력»·
  «체력» 을 7~17px 지점에서 쪼개고 우변 x=970~988 이 «%» 를 자른다. **7/13**.»

두 사람이 각자 잰 수치가 겹친다. EP 는 상자 폭이 프레임마다 **867~934px 로 7.2% 흔들리고**,
콘텐츠가 요구하는 폭은 **906px**(«공격력» 좌단 86 ~ «다음 +3.0%» 우단 992)이라고 못박았다 —
즉 상자가 자기 내용보다 **13~47px 좁다**.

⇒ **뿌리는 `pin()` 의 «안으로 들이기» 다.** 15회차가 «액자를 넘는다» 를 막으려 넣은 것인데,
넘침의 진짜 지배항은 **추적 부재**였고 그것을 이번에 닫았다. **16회차 머리말이 예고한 그대로다**:
«추적이 서면 `FXFLASH_RIM`·`pin()` 의 안으로 들이기 자체가 필요 없어질 수 있다 — 먼저 재고 고쳐라.»
18회차는 **`pin()` 을 0 으로 두고 `probe619e` ⓗ(밖 띠 넘침)를 다시 재는 것**부터 하면 된다.
⚠ 다만 ⓗ 는 «참고» 축이다(621 눌림·488 맥박이 호스트를 확대해 띠가 상시로 바뀐다) — 넘침 판정은
**호스트 «액자선»** 기준으로 새로 세워야 한다.

**게이트** `verify619` **50/50**(⚑ **[L] 5항 신설** — L2 «★ 전제: 홀드 중 호스트가 실제로 움직인다»
가 이 절의 본체다(안 움직이면 «따라간다» 는 아무것도 안 묻는 헛초록이다) · L4 **되돌림**(rAF 를
죽인 사본은 **8.02px** = 수리 전 값 그대로 재현) · L5 원복. **5회 연속 50/50**) ·
`verify583` **36/36**(⚑ [D] 궤적을 **«첫 무리»** 로 좁혔다 — 홀드 중 새 무리가 계속 태어나 «갓 태어난
(먼) 알갱이» 와 «도착 직전(가까운) 알갱이» 가 한 중심에 섞였고, 마지막 표본이 어느 쪽에 걸리는지가
**순전히 위상 운**이었다. 수리 전 트리에서도 22~70px 로 흔들려 문턱 20 에 여유가 2px 뿐이었다.
문턱·방향·의미는 한 칸도 안 건드렸고 **5회 연속 36/36**) ·
이관 무수정 `verify625` 24/24 · `verify491` 72/72 · `verify621` 42/42 · `verify488` 77/77 ·
`verify512` 31/31 · `verify324` 37/37 · `verify64` 44/44 · `verify518` 42/42 · `verify58` 27/27 ·
`verify543` 23/23 · **SMOKE PASS** · `probe619e` 2축 통과 · `probe619f` 3축 통과 · `probe619g` **신설**.

⚠ **곁다리 등재 694 — `verify93` 23/26 FAIL 은 내 것이 아니다.** 338·344 규칙대로 **수리 전 커밋에
자를 대 같은 23/26** 임을 직접 대조로 확인했다(내 변경과 무관). 상세는 PROGRESS 694 행.

⚑ **18회차가 볼 것(순서대로)**
1. **`pin()` 제거 — 2인 공통 「단 하나」**(위 ⑹). 추적이 섰으니 먼저 `pin()`=0 으로 재고, 넘침이
   실제로 생기면 그때 «액자선» 기준으로 새 자를 세운다. `FXFLASH_RIM` 을 더 내리는 것은 **금지**
   (16회차가 8→2 로 내려 액자선이 +365% → +570% 로 나빠졌다).
2. ~~**룬 알갱이 상변 — 설계 판단**~~ — **소멸**(658·660 이 그 알갱이를 폐지했다. 위 갱신 절 참조).
3. **단련 소등 3~4연속**(16회차 #1 그대로 남아 있다) · **룬 알갱이 탭바 침범**(2번과 한 뿌리).

⚑⚑ **[리베이스 시점 갱신] 660 이 이미 들어왔다 — 위 ⑷ 와 18회차 2번은 «해소됨» 이다.**
이 회차를 push 하려고 리베이스하니 상류에 **658·660 이 완료돼 있었다**: 훈련·단련의 숫자 플로터와
**«버튼으로 날아가는 비용 알갱이»(`.fx-spd`) 자체가 폐지**되고 버튼에서 터지는 버스트(`.fx-cic`)로
갈렸다. ⇒ ⑷ 가 «설계 판단으로 넘긴다» 고 적은 룬 알갱이 상변 문제는 **그 알갱이가 사라져 소멸**했고,
`verify619` [L4]·[L5] 는 상류가 **333 처방대로 방향을 뒤집어**(«나야 한다» → «한 알도 안 난다»)
살아 있는 표본(`.fx-cic`)으로 갈아 끼웠다 — 리베이스에서 **상류 판을 그대로 받았다**.
⇒ **내가 ⑷ 에서 클램프를 되돌린 판단이 결과적으로 맞았다**(걸어 두었으면 폐지된 연출에 건 클램프가
죽은 코드로 남았을 것이다). 아래 «660 을 먼저 볼 것» 은 그 시점의 판단으로 보존한다.

⚑⚑ **18회차는 660 을 먼저 볼 것 — 619 의 남은 회차가 헛돌 수 있다.**
주인 지시 **660**(2026-09-01 23:47)이 **훈련·단련 강화 연출을 리디자인**한다: 숫자 플로터 **폐지** ·
알갱이 파티클 **폐지** → 지불 재화 아이콘이 버튼에서 터지는 **버스트**로 교체(657 밀도 상향은 **룬만**
잔존). EP·EQ 지적 13건 중 **플로터 정지·«+21» 고착·무플로터 틱·알갱이 패널 침범·배율 어휘 불일치**는
전부 **660 이 지우는 요소**에 붙은 것이라, 지금 고치면 그 작업이 통째로 버린다.
⚠ 반대로 **`pin()`(1번)은 660 이 명시적으로 남긴다** — 660 본문이 «눌림 왕복(621)·**플래시 1장(625)**
위에 얹는다» 라고 적었다. **1번은 지금 해도 안 버려지고, 2·3번은 660 뒤로 미루는 것이 맞다.**

### 18회차 (2026-09-01, sess-2258-25875 루틴 워커 D) — 17회차 1순위(`pin()` 제거) · 채점 **ER2 3 / ES2 2**

17회차가 남긴 1순위 하나만 잡았다(2·3번은 660 이 이미 지웠다). 그런데 **지시대로 껐더니 빨개졌고,
그 빨강이 이 회차의 소득이다** — 17회차 기록이 «넘침이 실제로 생기면 그때 «액자선» 기준으로 새 자를
세운다» 로 넘겨 둔 그 자리를 실제로 밟았다.

**⑴ 지시대로 «0» 으로 두고 재 봤다 — 두 비평가가 다시 완전 일치했고, 이번엔 반대편이었다**

17회차 1순위는 «`pin()` 을 0 으로 두고 다시 재라» 였다. 껐다(`FXFLASH_PIN = 0`). 그 빌드로 캡처(`-r18-`)해
독립 2인(EP·EQ 의 후신 **ER·ES**)에게 줬더니 **점수가 3 / 4** 였고, 「8점을 막는 단 하나」가 **또 2인 완전
일치**했다 — 방향만 정반대다:

> **«대상 호스트 자신의 8px 검은 액자선이 홀드 내내 지워진다 — 세 장면 36개 홀드 프레임 중 36장(100%).»**

판정을 갈라 준 것은 **같은 프레임의 이웃**이다(16회차 EJ ⑤ 가 쓴 그 방법). 둘이 각자 잰 표가 겹친다:

| | 대상 액자선(f0 → 홀드) | 소실 | 같은 프레임 이웃 |
|---|---|---|---|
| 훈련 카드 | 13,599 → 381~2,522 | **92.5%**(12/12장) | 이웃 카드 7,935 → 7,937 = **불변** |
| 룬 패널 | 26,645 → **0**~1,331 | **97.7%**(12/12장) | 상·하단 탭바 27,926 · 23,447~450 **고정** |
| 단련 행 | 13,387 → 590~1,737 | **91.6%**(12/12장) | 이웃 행 9,742 → 9,752 = **불변** |

ES 는 같은 것을 휘도로 다시 쟀다(단련 좌변 x41..48: f0 lum **20.0** → 홀드 12/12 에서 133.1~**255.0**,
f4·f8·f12 는 여덟 칸 전부 255.0 = 완전 소거. 같은 프레임 이웃 행 x41..48 은 **13/13 전부 20.0**).
⇒ «캡처 편차» 도 «전체 톤 변화» 도 아니다. **강화 대상만 화면에서 유일하게 «테두리 없는 물체» 가 된다**
(ER: «강조가 아니라 비활성·유령처럼 보인다» — ④ 톤 일관성이 여기서 깨진다).

**⑵ 자를 먼저 세웠다 — `probe619h` 신설**

눈이 본 것을 자가 먼저 찍고 있었다. `probe619e` ⓗ 는 **문턱으로 못 쓴다**(그 자의 머리말이 스스로
«참고» 라고 적어 뒀다 — 621 눌림·488 맥박이 호스트를 확대해 밖 띠가 상시로 바뀐다. `pin` 0 에서
훈련 30.35 → **71.39%**, 단련 70.11 → **85.81%** 로 뛰었지만 그 수는 «플래시가 넘쳤다» 를 뜻하지 않는다).
⇒ 17회차가 넘긴 대로 **«액자선» 기준**으로 새 자를 세웠다. 축 셋:

- **ⓙ 대상 액자선 잉크 보존율** — 호스트 rect 안쪽 6px 띠의 어두운 픽셀(luma ≤ 60), 대조 대비 홀드 최솟값.
- **ⓚ 이웃 액자선 잉크 보존율** — **같은 프레임의 이웃 카드/행**. 두 비평가가 실제로 쓴 판정법을 자로 옮긴 것이다.
- **ⓛ 워시 상자 ↔ 호스트 «액자 링 안쪽» 상자, 네 변 Δ** — **0 이 정답**. 음수 = 흰 테두리가 액자선을 덮는다(16회차 EJ ⑤) ·
  큰 양수 = 링을 지나 라벨 글리프 위로 들어간다(17회차 EP·EQ). **한 자에 두 실패가 양쪽으로 걸린다.**

⚑ **자를 세우다 잡은 함정 셋**(다음 회차가 같은 데 빠지지 않게 적어 둔다):
- **ⓐ 두 상자를 다른 순간에 읽으면 안 된다.** 상자는 «직전 rAF 의 rect» 로 쓰여 있고
  `getBoundingClientRect()` 는 «지금» 을 준다 — 1차 시도가 단련 Δ **−9.83**, 룬 **−252px** 라는
  **위상 잡음**을 찍었다(자가 재는 것이 상자가 아니게 된다). 771 이 쓴 **「두 프레임 연속 정적」**
  에서만 읽어 없앴다.
- **ⓑ `__fxr` 은 스폰 시각에 굳는다.** 17회차 추적(`follow`)은 `style` 만 갱신하고 `__fxr` 은 안 고친다 —
  그것을 기준으로 삼은 2차 시도가 훈련 Δ −8.05·−20.6 을 찍었다. 기준은 **살아 있는 rect** 여야 한다.
- **ⓒ 호스트를 «마지막 노드» 로 고르면 안 된다.** 룬은 `--flash-to:.rd`(619 9·16회차)로 **자식**이
  플래시 호스트라, `__fxHost` 로 안 거르면 엉뚱한 자리의 장을 집는다(룬만 «표본 0» / «상자 896×160»).

**⑶ ⚑⚑ 「패딩 박스」 라는 것이 없었다 — 15~17회차 산수의 뿌리 오해**

자를 세우려고 세 호스트의 상자를 처음 재 봤더니:

| 호스트 | `border` | `padding` | 액자를 그리는 것 |
|---|---|---|---|
| 훈련 카드 `[data-tr]` | 0/0/0/0 | 0/0/0/0 | `box-shadow: rgb(20,20,20) 0 0 0 **8px inset**` (+ 바깥 2px·3px 금테) |
| 룬 행 `.tr-rn` | 0/0/0/0 | 0/0/0/0 | 같음 |
| 단련 행 `.tr-tp` | 0/0/0/0 | 0/0/0/0 | 같음 |

⇒ 17회차 EP 가 «호스트의 **패딩 박스**보다 좁게 그린다» 로 말한 그 «패딩 박스» 는 **존재하지 않는다**
(padding 0 이라 패딩 박스 = 호스트 rect 다). **실재하는 것은 «액자 링»** — 호스트 안쪽 **0~8px** 띠다.
그리고 15회차 확대식 `pin = s(1−1/PEAK)/2 + RIM` 이 낸 수는 그 띠와 아무 상관이 없었다:

| 호스트 | 15~17회차 들이기 | 흰 테두리(9px)가 앉던 띠 | 액자 링 |
|---|---|---|---|
| 훈련 카드(326) | 11.2px | 11.2 ~ 20.2 | 0~8 |
| **룬 `.rd`(896)** | **30.25px** | **30.25 ~ 39.25** | **없음(0)** |
| 단련 행(998) | 30.25px → 클램프 | 30.25 ~ 39.25 | 0~8 |

**룬의 30.25 가 17회차 EP·EQ 가 «세로 모서리가 라벨 글자 한가운데서 끝난다» 로 잡은 바로 그 자리다** —
상자가 «자기 내용보다 13~47px 좁다» 는 그들의 산수와 정확히 같은 수다.

**⑷ 처방 — 들이는 양의 «출처» 를 바꿨다: 확대식(계산) → 호스트의 액자 링(측정)**

`pin` 을 **`fxRingIn(el)`** 이 읽어 온 링 두께로 바꿨다. 그러면 흰 테두리는 **[ring, ring+9] 띠**에 앉는다 —
① 액자선(0~ring)을 **안 덮고**(⑴ 의 손해가 사라진다) ② 글자에서 **13~22px 물러난다**(17회차 지적).
⚠ **손으로 8 을 안 적었다** — 호스트의 `box-shadow` 에서 `inset` 그림자의 spread 를 그대로 읽는다
(12·13·21회차 배지 규격을 `getComputedStyle` 로 읽는 `fxFlashKeep` 과 같은 처방). 액자 디자인이 바뀌면
들이기도 저절로 따라오고 **새 상수는 0 개**다(폴백 `FXFLASH_RING` 은 «못 읽을 때» 뿐).
⚠ **«액자가 없으면 들이기도 0»** — 폴백을 여기에 쓰면 «없는 액자» 를 피하느라 상자만 좁아진다.
룬 `.rd` 가 정확히 그 자리라 ⓛ 가 Δ **+8** 로 빨개졌고, 0 으로 고쳐 초록이 됐다.
⚠ **`FXFLASH_RIM` 은 한 칸도 안 건드렸다**(16회차가 8 → 2 로 내려 액자선 훼손이 +365% → **+570%** 로
되레 나빠진 값이다 — 17회차 기록이 «금지» 로 못박았다).

**⑸ 실측 — 「0」 과 「링」 의 대조표** (`probe619h`, 같은 세션·같은 자)

| | ⓛ 최악 어긋남(px) | ⓙ 대상 액자선 | ⓚ **이웃** 액자선 |
|---|---|---|---|
| 훈련 — pin 0 | 0(우연히 0) | 1.73% | 99.76% |
| 훈련 — **링 8px** | **0** ✓ | 2.23% | **99.68%** |
| 룬 — pin 0 | 표본 0(자 결함) | 0.09% | (이웃 없음) |
| 룬 — **링 0px** | **0** ✓ | 0.01% | — |
| 단련 — pin 0 | −9.83 ✗ | 0% | **58.21%** ✗ |
| 단련 — **링 8px** | **0.01** ✓ | 0% | **93.13 ~ 99.98%** ✓ |

⚠ **자는 3회 연속 같은 판정이다**(ⓛ 0 / 0 / 0.01 — 세 자리 전부 통과). 흔들리는 것은 ⓚ 뿐인데
(단련 93.13 ~ 99.98%) 그 폭도 문턱 85% 위에 통째로 있다 — 표본 프레임이 봉우리에 걸리느냐 차이다.
문턱을 «최솟값» 으로 잡아 둔 이유가 이것이다(평균으로 잡으면 최악 프레임이 숨는다).

⚑ **가장 큰 소득은 ⓚ 다 — 단련 이웃 행 58.21% → 93.13%.** ES 가 «행간 여백 15px 를 다 먹고 이웃 행
안으로 8~18px 진입, 10/12 프레임에서 이웃 액자가 표백» 으로 잡은 그 자리이고, 링만큼 들이자 그 침범이
문턱(85%) 위로 올라왔다.
⚠ **ⓙ 는 링을 줘도 안 돌아온다(2.23% / 0.01% / 0%).** 뿌리는 상자가 아니라 **`.fx-flash` 의 바깥 글로우**
(`box-shadow 0 0 0 4px` + `0 0 26px 2px`)다 — 상자를 8px 들여도 그 글로우가 0~8px 띠를 바깥에서 덮는다.
그것은 12회차 CSS 머리말이 «강조는 **바깥 림**만으로» 로 정한 설계 자체라 **이 회차의 손잡이가 아니다**
(19회차 몫 — §18-다음 2번). ⓙ 를 문턱 없는 «대가» 축으로 둔 이유가 이것이다.

**⑹ 채점 — ER2 3 / ES2 2**

| | ER2 | ES2 |
|---|---|---|
| ① 타이밍 | 7 | 5 |
| ② 크기 비율 | 7 | 4 |
| ③ 위치·경로 | 6 | 4 |
| ④ 가독성·일관성 | **3** | **2** |
| 점수 | **3** | **2** |
| 화면별 | 훈련 3 · 룬 3 · 단련 3 | 훈련 2 · 룬 2 · 단련 1 |

⚑ **①②③ 은 17회차(EP 4/4/3/3 · EQ 5/5/3/3)보다 올랐고 ④ 만 안 움직였다** — 그리고
④ 를 누른 것은 하나뿐이다: **호스트 자신의 액자선**(⑸ ⚠ · 19회차 1번). 즉 이 회차는 «상자» 축을 닫고 «글로우» 축을 열었다.

⚑⚑ **17회차의 「단 하나」는 실제로 닫혔다 — 두 사람이 각자 세었다.**
룬 스탯행 글리프 절단 **13장 중 7장(17회차) → 12장 중 2장**(ER2 f5·f7 · ES2 f5·f7 — **두 사람이 같은 두 장을 집었다**).
훈련·단련은 **0장**(ES2: «라벨이 상자 세로변에서 100px 이상 떨어져 있어 쪼갬 없음»).
남은 2장의 뿌리도 상자가 아니라 **`.rd` 의 폭**이다 — ES2 실측 «효과행 콘텐츠 스팬 x85..993(909px) vs
그 프레임의 상자 가용 내폭 x91..986(896px) = **13px 부족**».

**게이트** `verify619` **57/57** · `verify583` **37/37** · `verify625` **24/24** · `verify621` **42/42** ·
`probe619h` **신설 · 세 자리 전부 통과** · `probe619e` 두 축 통과 · **SMOKE PASS**.
⚠ **곁다리 등재 780 — `verify488` **81/82**([I1] 룬 «두 봉투가 카드 자식을 안 밟는다» `[[rd] / [rd]]`)는
내 것이 아니다.** 338·344 규칙대로 **내 변경을 스태시한 수리 전 트리에 같은 자를 대 «같은 81/82»** 임을
직접 대조 실행으로 확인했다(상세는 PROGRESS 780 행).

**⚑ 19회차가 볼 것(순서대로)**
1. **`.fx-flash` 의 바깥 글로우 — 이번 회차가 연 축이고, 2인 공통이다.** 링만큼 들여도 호스트 액자선은
   안 돌아온다(ⓙ 2.23% / 0.01% / 0%). 두 비평가가 각자 «36/36 프레임» 으로 같은 결론을 냈고 판정은 둘 다
   **같은 프레임의 이웃**으로 갈랐다(ER2: 이웃 카드 x377..384 는 12/12 런 8 유지 · ES2: 이웃 행 x41..48
   100% × 12/12). 범인은 상자가 아니라 `box-shadow: 0 0 0 **4px** rgba(255,186,54,.95), 0 0 **26px** 2px …`
   — 상자를 8px 들여도 이 글로우가 0~8px 띠를 **바깥에서** 덮는다(ER2 실측: 검은 8px 자리에 «밝기 461 의 금색 링»).
   ⚠ 손잡이는 `FXFLASH_RIM` 이 **아니다**(금지 — 16회차가 내려서 더 나빠졌다). **흐림 26px / 하드 4px** 쪽이다.
   ⚠ 0 으로 만드는 것도 답이 아니다 — 12회차 CSS 머리말이 «강조는 **바깥 림**만으로» 로 정한 설계 자체다.
   ⇒ **`probe619h` ⓙ 로 «내리면 얼마나 돌아오나» 를 먼저 재고 손대라**(자는 이미 서 있다).
2. **룬 `.rd` 는 자기 라벨과 여백이 0 이다** — 링이 없어 들이기가 0 이고, 그래서 흰 테두리 9px 가
   `.rd` 의 바깥 9px = 라벨 시작점에 그대로 앉는다. 고칠 자리는 `pin` 이 아니라 **`--flash-to` 의 대상**
   이거나 `.rd` 자신의 여백이다(16회차가 `.ri` → `.rd` 로 옮긴 근거를 먼저 읽을 것 — 되돌리면
   «룬 몸통에 회당 워시가 없다» 3회차 연속 2인 공통이 되살아난다).


## §6 다음 라운드(2차 폴리시)가 볼 것 — 회차 10/10 소진 · ⏸ 보류(최고 DZ 9 / EA 7)

⚠ **기능·게이트는 완료다** — 주인 지시 본문(«매 강화마다 터진다»)은 `verify619` [B] 1.00/1.00/1.00 ·
[C]·[E]·[R] 이 지킨다. 남은 것은 연출 «채점 8점» 뿐이다. 다음 라운드 후보(마지막 두 회차의 지적):
1. **룬 실패 틱에도 앰버 1버스트**(EA «단 하나») — 1회차의 «성공에만» 은 성공 «세트»(플래시·+1)의
   거짓 신호를 막는 규약이었다. 무채색 앰버 스파크만이면 «레벨이 올랐다» 로 안 읽히므로 구분 가능
   — 단 `verify619` [B] 의 룬 «성공 축» 판정과 [E]·`verify583` [G2] 개수 상한을 같이 볼 것.
2. **플로터 스폰 최소거리 가드**(DZ — 룬 f3 Δ60px · 단련 f4 Δ15px, 같은 뿌리) — 칸 배정이 한 프레임
   무너지는 순간이 있다(hbLng 칸 회피가 순번을 건너뛴 직후로 추정). 스폰 시 «살아 있는 같은 줄기와
   Δ ≥ 글리프 폭» 확인 한 줄.
3. **룬 스파크 크기 상한**(DZ ② · EA ② «48~52px 융합 블롭») — hsc 1.9 상한을 1.5~1.6 으로.
4. **훈련 f1 스파크 저대비**(EA ①④ · DX ②④) — 첫 91ms 의 앰버가 크림 위 유백으로 읽힌다(«타격
   순간» 몫의 색을 진하게 — FXPAL.up 축).
⚠ **하지 말 것**: 단련 레인을 아이콘 밖으로(612 [I6b] 래칫과 충돌 — 주인 판단 없이 못 뒤집는다) ·
룬 레인 이동(띠 여유 0 — [I1]) · 실패에 성공 «세트»(거짓 신호).
⚠ **625 가 이 구간(upFx/fxFlash)의 다음 작업이다** — 1회차 재현까지 끝나 있고 lock 해제를 기다린다.
   처방 ⓓ(«한 호스트에 플래시 한 장 — 재시작»)는 여기 §6-1 과 같은 틱 축을 만지므로 한 세션 묶음 권장.

1. **훈련 ① «새 버스트로 안 읽힌다»**(DP) — 위상 굴림으로 중복 34% → 29% 까지만 왔다. 남은 축 후보:
   ⓐ 링 반경을 틱마다 ±10% 흔든다(현재 개체차 `jt` 0.82~1.18 은 **입자별**이라 링 전체는 안 움직인다)
   ⓑ 수명을 260ms 보다 짧게 줘 «겹쳐 보이는 잔상» 을 줄인다(⚠ 41회차 공용 퇴장 규약과 부딪힌다)
   ⓒ `strict` 가 살려 주는 빈 각이 매번 같은 자리라 **자리 자체를 늘리는 것**(keep-out 여유 14 → 더 줄이기)이 뿌리에 가깝다.
2. **룬 ② «파티클이 옅다»**(DQ 6 · DP 는 8 — 두 사람이 갈렸다) — 파티클 절대 크기는 세 화면이 같고
   룬·단련 호스트가 넓어 % 가 작다. 후보: 넓은 호스트에서 회당 개수를 폭에 비례해 올린다(⚠ `UPFX_CAP` 과 같이 봐야 한다).
   ⚠ **먼저 재라** — DP 는 같은 캡처에서 룬 ②8 을 줬다. 2인 공통이 아닌 지적은 이 저장소 규약상 회차를 태우기 전에 실측으로 가른다.
3. ⚠ **기능·게이트는 끝났다** — 이 셋은 «연출 채점» 축이고, 주인 지시의 본문(«매 강화마다 터진다»)은
   `verify619` [B] 가 1.00/1.00/1.00 으로 지키고 있다. 회차를 더 태우더라도 [B]·[C]·[E] 를 깨지 마라.
