# 491 — UI 쥬시니스(누름 손맛) 비평 루프

> 주인 원문(2026-08-30): «룬강화 부분이랑, 단련부분 너무 안 쥬시해서 **버튼을 누른건지 안누른건지 헷갈릴정도임**.
> 이 게임내에 UI 관련 쥬시함 점수 10점만점에 8점 될때까지 루프 돌리라하셈 비평가 2명 하고»
>
> 루프 규약(PROGRESS 491 행): 비평가 **2명 독립** · 축 4개(①즉시반응 ②결과잔존 ③재화변동 ④일관성) ·
> 통과 = **2명 모두 ≥ 8/10** · 상한 **8회차** · **1회차는 룬·단련 탭 집중**, 2회차부터 전 화면.

---

## 1회차 (2026-08-31, sess-2356-1998 워커 H)

### §1 재현이 먼저다 — 등재문 가설 셋 중 **둘을 기각**했다

등재문은 «60 쥬시가 팝업 껍데기·A5 공용 버튼에는 붙어 있으나 룬·단련 카드/버튼에는 눌림 반응이
**없거나 약하다**» 로 적었고, 처방을 «60 의 눌림 부품을 눌리는 모든 것에 한 규칙으로» 로 열어 뒀다.
그런데 60 의 누름은 개별 CSS 가 아니라 **`#app` pointerdown 위임 한 쌍**이다
(`jzTarget` → `jz-dn` = `scale .94`(both) + `sfx('tap')`). 그러면 «룬·단련만 없다» 는 말은 셋 중 하나여야 한다:

| 갈래 | 뜻 | 1회차 판정 |
|---|---|---|
| ⓐ | `jzTarget` 이 그 버튼을 못 고른다(cursor 가 아니거나 엉뚱한 조상) | **기각** |
| ⓑ | 고르긴 하는데 **누른 노드가 곧바로 사라진다** | **확정** |
| ⓒ | 노드도 클래스도 남는데 «약하다» | **기각 — 약한 게 아니라 0 이었다** |

`tools/probe491.js` 를 신설해 층을 넷으로 갈라 쟀다(선택 / 상태 / **찍힌 픽셀** / 유지).
**수리 전 실측**(1080×2280 · 실제 마우스 pointerdown · bbox 안 «달라진 픽셀 %», tol 12/255):

| id | 자리 | 호스트=자신 | cursor | `jz-dn` 16/50ms | **같은 노드** | **Δpx 누른 채** |
|---|---|---|---|---|---|---|
| R-buy | 룬 [강화] 420×112 | true | pointer | · · | **false** | **0.00%** |
| T-up | 단련 [투자] 300×74 | true | pointer | · · | **false** | **0.00%** |
| T-chg | 단련 [충전] 392×64 | true | pointer | · · | **false** | 76.31% ← **내용 변화지 누름이 아니다** |
| R-sub | 룬 하위 탭 | true | pointer | O O | true | 31.06% |
| T-reset | 단련 [회수] | true | pointer | O O | true | 29.46% |
| T-tab | 팝업 탭 «단련» | true | pointer | O O | true | 29.58% |
| ★X-train | 훈련 카드(대조) | true | pointer | O O | true | 99.22% |
| ★X-tab | 팝업 탭 «훈련»(대조) | true | pointer | O O | true | 27.48% |
| ★X-up | [단계 ↑](대조) | true | pointer | O O | true | 36.13% |

⇒ **아홉 자리 전부 cursor:pointer 이고 호스트도 자기 자신**이다(ⓐ 기각). 그리고 세 자리는 «약한» 게
아니라 **0.00%** — 반응이 아예 없다(ⓒ 기각). 빨간 셋은 정확히 **`rtHoldStart` 를 타는 세 버튼**이다.

⚑ **자를 두 번 고쳐야 진실이 나왔다 — 둘 다 헛초록/헛빨강 함정이다.**
1. «살아 있는가» 를 **셀렉터로** 물으면 헛초록이다. 재렌더가 같은 셀렉터의 **새 노드**를 놓으면
   질의는 그대로 맞는데 `jz-dn` 은 사라진 옛 노드와 함께 죽는다. → 누르기 전에 **도장**(`dataset`)을
   찍고 그 도장으로 «같은 노드인가» 를 묻는다. 1회차 첫 판은 이 항이 전부 초록이었다.
2. **픽셀 변화 = 누름 반응이 아니다.** 「단련 [충전]」은 누름 반응이 0 인데도 머리 띠의 «포인트 n» 이
   통째로 바뀌어 bbox 가 **76.31%** 변한다. 픽셀만 보는 자였으면 그 자리를 초록으로 읽고
   수리 전과 못 갈랐다. → 게이트의 되돌림 주 축을 **노드 생존**으로 잡았다(§R).

### §2 뿌리 — 297 규약이 **첫 프레임에만** 꺼져 있었다 (제품 1자리)

297 이 세운 규약: «꾹 누르는 동안에는 노드를 갈지 않는다 — 누른 노드를 `innerHTML` 로 날리면
터치의 암묵적 포인터 캡처가 끊긴다»(64 교훈 1 · 262 ⓑ). 그 규약은 `rtHoldOn(tag)` 이 참일 때만 걸리는데
`rtHoldStart` 의 **순서가 거꾸로**였다:

```
  rtHoldStop(false);
  if(!o.once()){ … }        ← 첫 발(구매·시도)  … 이 시점에 rtHold 는 아직 null
  o.live();                 ← 첫 렌더           … rtHoldOn(tag) === false ⇒ 통짜 innerHTML 교체
  rtHold = { … };           ← «홀드 중» 표시를 **여기서야** 세운다
```

그 한 프레임에서 `renderRunes`/`renderTemper` 가 `innerHTML` 을 갈아 끼우고, 방금 `#app` 위임이 붙인
`jz-dn` 이 **옛 노드와 함께 사라진다**. 60 은 정상이고, 누른 흔적만 없다.

**고친 것은 순서 하나**(`index.html` `rtHoldStart` — 표시를 첫 발보다 먼저 세우고, 첫 발 실패 갈래에서는
`o.end()` **전에** 도로 내려 통짜 렌더를 지킨다). 판정식·상수·CSS·레이아웃 **0줄 / Δ0px**.

**수리 후 같은 자로 재측정**: R-buy **0.00 → 22.86%** · T-up **0.00 → 25.88%** · 세 자리 모두
«같은 노드» true · `jz-dn` 16/50ms 둘 다 O · `scale 0.94`. `probe491` **55/55 PASS**.

### §3 게이트 `verify491` 신설 — **28/28**

층을 갈라 묻는다(한 층만 물으면 헛초록 — 519 교훈):

| 절 | 무엇을 묻나 | 결과 |
|---|---|---|
| §1 소스 | 표시가 첫 발·첫 렌더보다 먼저다 · 실패 갈래에서 도로 내린다 · `rtHoldOn` 판정 불변 · `sfx('tap')` | 6/6 |
| §2 실행 | 23 훈련 팝업의 **눌리는 요소 전수 25자리**(train 10 · rune 7 · temper 8)에서 «누른 그 노드» 생존 + `jz-dn` | 7/7 |
| §3 픽셀 | 세 버튼 + 대조군이 «누른 채» bbox ≥ **8%** 변한다 | 4/4 |
| §4 소리 | pointerdown 한 번에 `sfx('tap')` 정확히 1회 | 1/1 |
| §5 홀드 | 900ms 꾹 누르면 반복이 돌고(Lv 0→6) **그 내내** 노드가 안 갈리고 `jz-dn` 유지 | 3/3 |
| §R 되돌림 | 옛 순서 사본에서 세 자리 전부 노드 사망·`jz-dn` 0장·픽셀 < 8%, **대조군은 그대로 초록** | 5/5 |
| §Z | 콘솔 에러 0 | 1/1 |

통과선 **8%** 의 근거: 부품 하나(scale .94)가 420×112 버튼에서 내는 최소치가 22.9% 이고 수리 전은 0.00% 다.
그 사이에서 «부품이 통째로 빠지면 반드시 걸리는» 자리로 잡았다 — **넓히지 마라.**
⚠ 자를 무르게 풀지 않았음은 [R-d] 가 못박는다: 같은 되돌림 사본에서 **대조군(훈련 카드)은 98.99% 로 초록**이다
(= 이 자는 «아무거나 빨개지는 자» 가 아니다).

### §4 이관·회귀

`verify488` 71/71 · `verify349` 29/29 · `verify210` 101/101 · `verify519` 31/31 · `verify114` 46/46 ·
`verify203` 91/91 · `verify271` 64/64 · `verify64` 44/44 — **전부 무수정 초록** · **SMOKE PASS**.

### §5 비평 — 캡처와 채점

`tools/cap491.js` 신설. 네 장면(룬 [강화] · 단련 [투자] · 단련 [충전] · ★대조 훈련 카드) ×
세 프레임(`-idle` 손대기 전 / `-down` 누른 채 / `-up` 뗀 뒤 140ms)을 **같은 clip**(팝업 본문 1058×1105)으로 찍는다.

⚑ 규약의 «0/50/150ms 세 장» 을 **idle/down/up 으로 바꾼 근거**: `probe491` 이 16ms·50ms 표본이
**같은 값**임을 찍었다(`jz-dn` 은 `both` 라 누르고 있는 동안 값이 고정). 셋을 다 찍어도 비평가에게는
같은 그림 세 장이라 판단 근거가 안 는다 — «전 / 누른 채 / 뗀 뒤» 로 갈라야 ①과 ②가 갈린다.

⚠ **하네스 함정 하나를 여기 적어 둔다**: 페이지를 둘 만들면(픽셀 비교용 스크래치 등) **뒤로 밀린 쪽의
애니메이션을 크로미움이 재운다.** 팝업 열림 연출(`jz-sh2`)이 첫 프레임에 멈춰 시트 안 좌표가 통째로
**+1245px** 어긋나고, 전 표본이 «화면 밖» 으로 읽힌다. 잴 때는 `page.bringToFront()` 로 게임 페이지를
앞에 두거나, 아예 같은 페이지 안에서 읽어라(`verify491` 은 후자).

#### 1회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| BU | 5 | 3 | 3 | 3 | **3** |
| BV | 5 | 4 | 3 | 4 | **3** |

**⇒ 1회차 미달(통과선 8 · 2명 모두). 2회차로 넘어간다.**
⚑ **둘이 독립으로 같은 값을 냈다** — ① 5/5 · 최저 3/3 이고, 스케일 실측까지 소수점이 겹친다
(BU 0.938/0.940/0.938 · BV 0.938/0.940/0.939). «부품은 확실히 붙었는데 그 부품 하나뿐» 이라는 진단이
2인 공통이다. 즉 **1회차의 수리는 옳았고(0 → 붙었다), 부족한 것은 그 다음 층**이다.

#### BU 지적(수치)

- ① 세 버튼 모두 반응이 «0.94 중심고정 축소» **하나뿐**이다(룬 420×112→394×105 = 0.938 · 투자 0.940 · 충전 0.938).
  **세로 이동이 0.5px** 이고 하단 베벨 `0 5px 0` 은 idle/down 동일 ⇒ «가라앉는» 단서가 없다.
  채움색도 ±1/255 = 사실상 0(대조군은 카드 바디 ΔB **−36**). 버튼 면적 중 바뀐 픽셀 룬 22.3% · 투자 26.6% vs **대조군 98.8%**.
- ② `-down → -up` 에서 새로 생기는 연출 0. 룬 진행바는 Lv0→1 에서 495px 트랙 중 **1px 열**만 찬다(0.2%).
  뗀 뒤 +140ms 에 잔존 연출이 네 장면 모두 **0px**(투자만 23px).
- ③ 룬 «−12» 가 화면에 없고 버튼의 «12 → 13» 은 *다음* 가격이라 **지출과 반대로 읽힌다**.
  충전은 100만을 냈는데 «−1,000,000» 이 0px. 투자만 −/+ 가 있으나 잉크 합계 981px · 평균 Δ35.7
  (대조군 «+20» 단독 5,786px · 최대 Δ249.2 ⇒ **잉크 1/5.9 · 대비 1/7**).
- ④ 배율만 균일(0.938~0.940). 둘레 40px 띠 변화량이 대조군 22,264px(34.9%) vs 룬 1,984(4.1%)·투자 892(2.5%)·충전 1,404(3.3%)
  = **대조군의 1/11~1/25**. 플로터 규격이 **3종**(대조군 80×75 풀대비 / 투자 30×28 14%대비 / 룬·충전 0px).

#### BV 지적(수치 · BU 와 독립)

- ① 스케일은 정확히 0.94(룬 420×112→394×105 · 투자 300×74→282×70 · 충전 좌변 0.939). **그런데 그게 전부다** —
  중심 좌표 이동 **0px**(≤0.5px) · 본체 명도 변화 **0.7%** · 세로 그라데이션 idle/down **완전 동일**(136→104) ·
  바닥 3D 립 **5px 불변**. 손끝에 남는 단서는 짧은 변이 4px(투자·충전)/7px(룬) 들어가는 것 하나뿐이고
  **그 4px 는 손가락이 덮는 자리**다. 대조군은 카드 **바깥으로만 18,344px** 를 바꾼다(글로우 명도 +11.2 · 26~43px 파티클 10개).
  세 버튼의 사각형 **바깥** 변화량은 **0px**.
- ② 룬: 버튼 안 idle→up 변화가 **862px = 면적의 1.83%**, 그것도 «12»→«13» 글리프 하나. 레벨업 게이지는 500px 트랙에 **1px**.
  투자: «−1»(48×37)·«+1»(42×38)이 `-up`(140ms)에서 잉크 델타 **정확히 0** — 140ms 안에 증발.
  충전: 플로팅 **0개**. 대조군은 «+20» 이 **대비 167/255(65%)**.
- ③ 룬 «−12» 가 크롭 어디에도 없다(12→13 은 **낸 값이 아니라 다음 가격**). 투자의 «−1» 은 대비 **43/255(17%)** =
  대조군의 **0.26배**이고 눌린 버튼에서 **가로 330px·세로 110px 떨어진** 자리에 뜬다. 충전은 100만 지출 표기 **0곳**.
- ④ 누름 세기는 고르다 — **고르게 약할 뿐이다**(0.938/0.940/0.939). 버튼 **밖**에 남는 변화량은 룬 0px · 충전 0px ·
  투자 917px · 대조군 18,344px = **0배~20배 편차**. ⚠ 충전은 추가 사고: `-down` 내부 RGB(172.9,161.2,149.0)와
  `-up` 의 **비활성** 회색(174.8,162.8,151.1)이 **Δ2.1/255 = 0.8%** — «눌린 상태» 와 «못 누르는 상태» 가 구분 불가.

#### 2회차가 잡을 것 (BU)

1. 세로 눌림 추가 — down 시 `translate 0 4~6px` + 하단 베벨 5px → 1~2px 로 접기(현재 세로 이동 0.5px).
2. down 시 채움 휘도 최소 ±20 이동(대조군 ΔB −36 수준. 현재 ±1/255).
3. 룬·충전·투자 모두에 대조군 «+20» 규격(80×75px · 배경 대비 Δ200↑) 플로터. 현재 투자 30×28·Δ35.7, 룬·충전 0px.
4. 비용 플로터를 별도로 — 룬 «−12 룬석» · 충전 «−1,000,000» 을 24px 이상 글리프로.
5. 연출 수명 400~600ms(현재 `.fx-plus.hb` 는 **.3s** 라 +140ms 시점에 이미 거의 없다).
6. 충전 버튼은 눌림 직후 초록→회색 disabled(면적 90.4% 변화)로 굳는 게 유일한 큰 변화인데
   이건 «성공» 이 아니라 «비활성/고장» 으로 읽힌다 — 성공 연출을 0.3초 먼저 재생한 뒤 넘겨라.

#### 2회차가 잡을 것 (BV)

1. 세 버튼 전부 press 에 **y +4px + 립 5px→1px(80% 압축)**. 지금은 y 이동 0px · 립 변화 0px.
2. 눌린 순간 본체 평균 명도를 **최소 +18/255(7%)**. 현재 룬 0.7% · 투자 2.6%.
3. 룬·충전에 플로팅 신설 — 대조군 규격(글리프 박스 **78×50 이상** · 대비 **≥150/255**)으로 **버튼 중심에서 60px 이내**.
4. 플로팅 수명 140ms → **최소 600ms**, 대비 43/255 → **150/255**.
5. [충전] 의 **pressed 회색과 disabled 회색 분리**(현재 Δ0.8% → 최소 40/255).
6. 버튼 **바깥**으로 나가는 반응 — 파티클 6개 × 24px 또는 테두리 글로우 명도 +10 중 하나.

#### 2인 공통(= 2회차의 실제 작업 목록)

| # | 2인 공통 지적 | 수치 합의 |
|---|---|---|
| A | 세로 눌림이 없다 + 바닥 립이 안 접힌다 | y +4~6px · 립 5px → 1~2px |
| B | 누른 순간 **명도**가 안 움직인다 | Δ ≥ 18~20/255 (방향은 갈림 — BU «어둡게» · BV «밝게») |
| C | 룬·충전에 플로터가 **0개** | 대조군 규격(≈78×50 이상 · 대비 ≥150/255) · 버튼 중심 60px 이내 |
| D | 플로터 수명이 짧다(`.fx-plus.hb` **.3s**) | 0.5~0.6s |
| E | [충전] pressed ↔ disabled 회색이 구분 불가 | Δ ≥ 40/255 |

⚠ **B 의 방향은 둘이 갈렸다**(어둡게 vs 밝게) — 2회차는 **한쪽을 골라 근거를 적어라**. 이 저장소에는 이미
«어둡게 = 죽은 버튼» 이라는 뜻이 못박혀 있다(`jz-sh` = `brightness(.82)` + shake, 60). 그러므로 **밝게(BV)** 가
어휘 충돌이 없는 쪽이다 — BU 의 «±20» 도 «명도를 움직여라» 지 «어둡게» 를 요구한 것은 아니다.

#### 2회차 착수 메모(다음 세션이 그대로 이어받을 것)

- **부품 하나 + 호스트 목록** 규약을 지켜라(개별 CSS 금지). 후보 세 부품:
  `.jz-dn`(누름) · `.fx-plus.hb`(회당 플로터 30px/.3s) · `hbBeat()`(호출 규약).
- ⚠ `.jz-dn` 에 **`filter:brightness()` 를 얹으면 호스트 자신의 `filter:drop-shadow(…)` 를 대체한다**
  (저장소에 그런 호스트가 여럿이다). 휘도를 건드릴 거면 그 목록을 먼저 세고 대가를 적어라.
- ⚠ 세로 이동은 **`translate:`(독립 속성)** 로만 — 저장소에서 `translate:` 를 쓰는 자리는 `jz122Sweep` 하나뿐이라
  안전하지만, `transform` 을 쓰면 글자 폭 보정(`scaleX`)과 모달 정렬이 통째로 날아간다(60 규약).
- ⚠ `jz-hb`(488 회당 맥박)는 이미 `transform:` 으로 **일부러** 갈라 놨다 — `jz-dn` 의 `scale:` 과 곱해지라고
  그렇게 한 것이므로 되돌리지 마라.

---

## 2회차 (2026-08-31, sess-2356-1998 워커 H · 1회차와 같은 세션)

### §6 고친 것 — 2인 공통 목록 중 ⓐⓑ + ⓒ 절반 (제품 4자리 · 부품만, 개별 CSS 0줄)

| # | 무엇 | 자리 | 근거 |
|---|---|---|---|
| ⓐ | 누름에 **가라앉음** 추가 — `@keyframes jzDn/jzUp` 에 `translate: 0 4px` | 60 절 CSS **부품 하나** | 1회차 2인 공통 «세로 이동 0px» |
| ⓑ | 누름에 **밝아짐** 추가 — `.jz-dn{filter:brightness(1.10)}` | 같은 부품 + 호스트 목록 1줄 | 1회차 2인 공통 «명도 변화 0.7%» |
| ⓒ | 회당 플로터를 **공용 토큰으로 통합** — `.fx-plus.hb` 의 `font-size:30px` 를 걷어 `--fx-plus-fs`(34px) 상속 · 테 5 → 7px | 58 절 CSS 부품 | 1회차 2인 공통 «플로터 규격이 3종» |
| ⓒ' | **단련 [충전] 에만 없던 회당 피드백** 신설 — `rtChargeHold.once()` 에 `hbBeat(sel, true, '+n', '−n')` | `index.html` 1자리 | 2인 공통 «충전은 플로팅 0개 · 100만 지출 표기 0곳» |

⚠ **밝게** 를 고른 근거(둘의 방향이 갈렸다 — BU «±20» · BV «+18 밝게»): 이 저장소에서 **어두워짐은 이미 뜻이 있다** —
`jz-sh` = `brightness(.82)` + shake = «죽은 버튼»(60). 어둡게 하면 «눌림» 과 «못 누름» 이 같은 어휘가 된다.

⚠ `filter` 는 호스트 자신의 `filter` 를 **대체**한다. CSSOM 전수로 세니 `cursor:pointer` 를 선언한 규칙 **91개** 중
`filter` 를 같이 선언한 것은 **둘뿐**이고(`.prb-ar` · `.cl-srch`) 그중 값이 있는 것은 `.prb-ar` 하나다
(`.cl-srch` 는 `filter:none`) → 그 **한 자리만** 합쳐 적었다(`.prb-ar.jz-dn{filter:drop-shadow(…) brightness(1.10)}`).

### §7 ⚑ **«수명 600ms» 는 이번 회차에 «못 했다» 가 아니라 «먼저 사다리를 늘려야 한다» 다**

2인 공통 ⓓ(플로터 수명 .3s → 0.5~0.6s)를 실제로 넣어 보고 **되돌렸다.** `.fx-plus.hb` 를 `.55s` 로 올리자
`verify488` 이 곧바로 **69/71** 로 빨개졌다:

- **[H2]** «서로 겹친 쌍 0 — 칸 5개 × 간격 80px 이 수명 **.3s** 를 정확히 덮는다» → 겹친 쌍 0 → **2**
- **[H4]** «동시 생존이 10장을 안 넘는다» → 4 → **12**

즉 488 의 사다리(칸 수·간격)는 **수명에서 역산된 값**이라 수명만 늘리면 곧바로 무너진다.
⇒ 3회차는 **수명과 사다리를 같이** 다시 잡을 것(칸 5 → 약 9, 또는 홀드 반복분만 수명을 짧게 유지).

⚑ **그리고 «140ms 프레임에 잉크 0» 이라는 1회차 근거의 절반은 하네스 드리프트였다.**
`cap491` 의 옛 순서는 `-down` 캡처(수백 ms)를 찍은 **뒤** 손을 떼므로, 실제로는 ≈360ms 를 누른 것이 되고
`-up` 프레임은 «누름 뒤 ≈500ms» 다 — 수명 .3s 짜리 플로터가 없는 것이 당연하다.
2회차에 **누름을 두 패스로 나눠**(1패스 idle·down / 2패스 «짧게 눌렀다 떼고 140ms») 고쳤다.
`probe491` 이 1회차에 잡은 것과 **같은 종류의 함정**이다(캡처가 시간을 먹는다).

### §8 재측정 — 같은 자(`probe491`), 같은 표본

| id | 1회차 수리 전 | 1회차 수리 후 | **2회차** |
|---|---|---|---|
| 룬 [강화] | 0.00% | 22.86% | **88.66%** |
| 단련 [투자] | 0.00% | 25.88% | **85.08%** |
| 단련 [충전] | 76.31%(내용) | 78.01% | **88.86%** |
| 단련 [회수] | 29.46% | 29.46% | **85.62%** |
| ★대조 훈련 카드 | 99.22% | 98.73% | **99.19%** |

`probe491` 55/55 · `verify491` **28/28** · `verify488` **71/71**(수명 되돌린 뒤) · `verify122` 167/167 ·
`verify102` 85/85 · `verify58` 27/27 · **SMOKE PASS**.
⚠ `verify58` 은 첫 실행이 **26/27** 이었고 재실행 2회가 **27/27** 이다 — 뜨고 지는 항이다.
내 변경(`.fx-plus.hb` 는 58 이 재는 `fx-plus` **기본**값이 아니라 `.hb` 변형이다)과 무관해 보이나
**단정하지 않는다** — 3회차가 3연속 실행으로 성격을 굳히고, 굳으면 곁다리 등재할 것.

### 2회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| BW | **7** | 4 | 4 | 4 | **4** |
| BX | 5 | 3 | 3 | 3 | **3** |

**⇒ 2회차도 미달(통과선 8). 3회차로 넘어간다.** (1회차 BU 3 / BV 3 → 2회차 BW 4 / BX 3)

**움직인 축은 ① 하나다** — BW 5 → **7**. 둘 다 눌림 층 자체는 «균일하다» 고 적었다:
축소율 −6.2/−6.0/−6.1%(대조군 −5.8%, 편차 **0.4pp**) · 휘도 +19.1/+19.7/+18.2 /255(편차 **1.5/255**) ·
라벨 이동 +3.5/+3.7/+3.4px(대조군 +3.5px). BW: «이 축만 보면 8점감».

### 2회차 2인 공통 — 3회차 작업 목록

| # | 2인 공통 지적 | 수치 합의 |
|---|---|---|
| ⅰ | **룬 [강화] 의 플로터가 «0px» 이다** — 화면에 아예 안 나온다 | BW «down·up 어디에도 0px» · BX «플로팅 0개» |
| ⅱ | **눌림 앵커가 «아랫변 고정»** 이라 «가라앉음» 이 아니라 «윗변만 8px 깎임» 으로 읽힌다 | 윗변 +8px / **아랫변 0px** · 그림자 턱 5px **불변**. 대조군은 윗변 +18 / 아랫변 −11 = **중심 스케일** |
| ⅲ | **버튼 «바깥» 으로 번지는 양이 0 에 가깝다** | 바깥 변화 픽셀 룬 **122px** vs 대조군 **29,560px**(폭 14px 후광 + Ø20 파티클 10개) |
| ⅳ | 플로터 **대비·수명** | 투자 «+1/−1» 최대 |ΔL| **48/255** → ≥180 · 수명 ≥200ms(140ms 프레임에 남을 것) |
| ⅴ | **[충전] 만 눌림 어휘가 뒤집혀 있다** | down 순간 초록 → **회색(180,172,155)**(버튼 안 92.7%) = «눌렸다» 가 아니라 «꺼졌다». 회색은 up 이후로 미뤄라 |
| ⅵ | [충전] 플로터가 **잘린다** | «+1,000,000» y49..71 이 카드 상단 테두리에 걸려 3행만 · «−1,000,000» 은 구분선에 ≈50% 가림 → `--hb-y`/`--hb-y2` 재조정 |

#### 3회차 착수 메모

- **ⅰ 이 최우선이다 — 그리고 이건 «연출을 더 넣자» 가 아니라 «있어야 할 것이 안 나온다» 다.**
  `rtRuneHold.once()` 는 `hbBeat(sel0, up, up ? '+1' : '실패', '−' + fmt(cost))` 를 **분명히 부른다**
  (호스트는 버튼이 아니라 **카드** `.tr-rn` — 488 이 주인 지시로 정한 자리). 그런데 두 비평가가
  **독립으로 0px** 를 봤다. ⇒ **338 규칙대로 `probe` 로 먼저 물어라**: `hbFloat` 가 false 로 빠지는가
  (`fxL()`·`hbHost()`·`FXMAX`), 아니면 노드는 생기는데 `--hb-y:248` 자리가 캡처 clip 밖/가림인가.
  «안 나오는 이유» 를 확정하기 전에 새 연출을 얹지 마라 — 1회차의 교훈이 그대로 여기 적용된다.
- **ⅱ 는 부품 한 줄**이다: `jzDn` 의 `scale` 이 **중심 기준**이라 `translate 0 4px` 를 더해도 아랫변이 제자리다
  (위 +4+4 / 아래 −4+4 = 0). 대조군처럼 «위아래가 같이» 들어가려면 `translate` 를 빼고
  **`transform-origin`** 을 아래로 내리거나(아랫변 고정 + 윗변만 → 반대 결과) `translate` 를 **6~8px** 로 키워
  아랫변도 +2~4px 내려가게 해야 한다. **둘 중 하나를 고르고 근거를 적어라** — 그림자 턱(호스트가 `box-shadow: 0 5px 0`
  로 직접 그린다)은 요소와 **같이 움직이므로** «턱이 먹힌다» 는 그림은 부품만으로는 안 나온다(호스트 목록이 필요하다).
- **ⅴ 는 판정이 아니라 순서**다 — `.cg.no` 회색은 `liveTemper()` 가 `classList.toggle('no', …)` 로 즉시 건다.
  누름 60ms 동안은 미루는 «지연 한 줄» 이 처방 후보이고, `verify491` 에 «down 프레임에서 `.no` 가 안 붙는다» 를 더할 것.
- ⓓ(수명)은 **§7 대로 사다리와 같이** 잡아라. 수명만 올리면 `verify488` [H2]·[H4] 가 즉시 빨개진다.

---

## 3회차 (2026-08-31, sess-0106-8236 워커 E)

### §9 ⅰ 재현이 먼저다 — «플로터 0px» 의 갈래 셋 중 **ⓒ 가 맞았다**

2회차의 3회차 착수 메모가 못박은 대로(338 규칙), 새 연출을 얹기 전에 제품에게 먼저 물었다.
`probe491` 에 **[F] 절을 신설**해 룬 [강화] 를 «한 발» 누르고 `#fxl` 을 시각별로 읽었다.

| 갈래 | 뜻 | 3회차 판정 |
|---|---|---|
| ⓐ `hbFloat` 가 false 로 빠진다(`fxL()`·`hbHost()`·`FXMAX`) | 노드가 아예 없다 | **기각** — 60ms 에 두 장(«+1»·«−12») · α **1.0** |
| ⓑ 자리가 캡처 clip 밖이거나 남에게 가린다 | 노드는 있는데 안 보인다 | **기각** — y 1384·1398 로 카드(1155..1855) **안**이다 |
| ⓒ 규격대로 생기는데 **너무 빨리 진다** | 수명이 캡처·눈보다 짧다 | **확정** — **260ms 에 이미 α 0** |

즉 **비평가 둘이 «0px» 로 본 것은 사실이고, 원인은 «없다» 가 아니라 «벌써 갔다»** 였다.
`.fx-plus.hb` 는 `.3s` 이고 그중 불투명 구간은 10~72% = **30~216ms** 뿐이다.
`page.screenshot` 한 장이 수백 ms 를 먹으므로 `-down` 프레임조차 그 창 밖에서 찍힌다 —
2회차가 `-up` 에서만 고쳤던 **하네스 드리프트가 `-down` 에도 그대로 있었던 것**이고,
사람의 눈에도 «누르고 나서 화면을 확인하는» 200~400ms 창에는 아무것도 안 남는다.

### §10 고친 것 (제품 3자리 · 부품만, 개별 CSS 0줄)

| # | 2회차 지적 | 무엇 | 자리 |
|---|---|---|---|
| ⅰ·ⅳ | «플로터 0px» · «수명 ≥200ms» | **«한 발» 만 수명 .56s** — 홀드 반복분은 .3s 그대로 | `.fx-plus.hb.lng` + `hbBeat`/`hbFloat` 의 `lone` 인자 |
| ⅱ | «가라앉음이 아니라 윗변만 8px 깎임» | `jzDn`/`jzUp` 의 `translate` 4 → **8px** | 60 절 CSS 부품 하나 |
| ⅴ | «[충전] 만 눌림 어휘가 뒤집혀 있다» | 누르는 중에는 «회색(.no)» 을 **미룬다** — `jzNo()` 신설 | 60 절 함수 1개 + `liveTemper()` 3자리 |

#### ⅰ — 왜 «전부 길게» 가 아니라 «한 발만» 인가

2회차는 수명을 .55s 로 올려 봤다가 `verify488` **[H2]·[H4]** 가 곧바로 빨개져 되돌렸고,
«사다리(칸 5 × 간격)를 수명과 **같이** 다시 잡아라» 를 3회차 몫으로 넘겼다.
그런데 그 두 항이 재는 자리는 **홀드 한복판**(t=700·1400·2200·3000ms)이다 —
사다리가 수명에서 역산된 값인 이유도 **60ms 주기로 쏟아지는 반복분** 때문이다.
**한 번 톡 누르고 마는 발에는 사다리 압력이 아예 없다**(동시 생존 1~2장).
⇒ 사다리를 건드리지 않고 «한 발» 만 길게 두면 두 요구가 동시에 선다.
«한 발인가» 는 **호스트별**로 판정한다(`WeakMap` · 직전 beat 로부터 `HB_LONE_GAP` 400ms) —
두 화면이 우연히 같은 400ms 안에 울려도 서로의 수명을 깎으면 안 되고,
400ms 는 홀드 주기(60ms)보다 훨씬 커서 «홀드 반복분은 예외 없이 짧다» 가 구조적으로 성립한다.
한 beat 의 두 줄기(결과·비용)는 **beat 단위로 한 번만** 판정한다(둘째가 첫째를 보고 «반복» 이라 읽으면 안 된다).

실측(`probe491` [F]): 뗀 뒤 **140ms · 300ms 에 두 장 다 α 1.0** · 420ms 에 α 0.17.
`verify488` [H] 는 **한 항도 안 움직였다**(t=700/1400/2200/3000 에서 동시 4·8·6·6장 · 겹친 쌍 **0**).

#### ⅱ — 값은 취향이 아니라 부등식에서 나왔다

축소 `.94` 는 **중심 기준**이라 짧은 변의 절반이 위아래로 **각각** 먹힌다(112px 버튼이면 3.36px).
2회차의 `translate 4px` 는 윗변 **+7.4** / 아랫변 **+0.6** 이라 아랫변이 사실상 제자리고,
그림이 «가라앉음» 이 아니라 **«윗변만 깎임»** 이 된다(2인 실측 +8 / 0 · 대조군은 +18/−11 중심 스케일).
조건은 `translate > h(1−s)/2` 다. **8px** 이면 아랫변도 +4.6px 내려가 두 변이 같은 쪽으로 간다.
저장소에서 가장 낮은 누름 호스트(h≈40px)도 먹는 절반이 1.2px 라 8 은 전부 만족한다.
(2회차 메모의 다른 길 — `transform-origin` 을 아래로 내리기 — 는 **아랫변 고정**이라 지적과 정반대 결과다.)

#### ⅴ — 판정이 아니라 «표시의 순서» 다

`liveTemper()` 는 홀드 중 매 틱 돌면서 202 의 `.no`(회색)를 즉시 건다.
그래서 [충전] 은 **누르는 그 프레임에 회색**이 되고, 이 저장소에서 회색·어두워짐은 이미
`jz-sh` = «못 누르는 죽은 버튼» 의 어휘다 — 즉 «눌렸다» 가 «꺼졌다» 로 읽혔다(2인 공통, 버튼 안 92.7%).
`jzNo(el, on)` 는 **`jz-dn` 이 붙어 있는 동안 «꺼짐» 만 미룬다**(«켜짐» 은 즉시 반영 — 그쪽은 어휘가 안 겹친다).
손을 뗀 뒤 `end:` 의 통짜 `renderTrain()` 이나 다음 live 틱이 같은 함수를 다시 부르며 그때 붙으므로
**판정식(`temperUpOk`·`temperResetOk`·`canCharge`)은 한 줄도 안 건드렸다.**

### §11 자 — 전부 초록

`probe491` **61/61**(**[F] 6항 신설**) · `verify491` **28/28** · `verify488` **71/71**(★[H2]·[H4] 무수정) ·
회귀 무수정 `verify122` 167/167 · `verify102` 85/85 · `verify210` 101/101 · `verify519` 31/31 ·
`verify349` 29/29 · `verify64` 44/44 · `verify114` 46/46 · **SMOKE PASS**.

누름 «찍힌 픽셀»(`probe491` [C], bbox 안 달라진 픽셀 %):

| id | 1회차 수리 전 | 1회차 후 | 2회차 | **3회차** |
|---|---|---|---|---|
| 룬 [강화] | 0.00% | 22.86% | 88.66% | **93.54%** |
| 단련 [투자] | 0.00% | 25.88% | 85.08% | **90.53%** |
| 단련 [충전] | 76.31% | 78.01% | 88.86% | **90.06%** |
| 단련 [회수] | 29.46% | 29.46% | 85.62% | **90.44%** |
| ★대조 훈련 카드 | 99.22% | 98.73% | 99.19% | **99.37%** |

⚠ `verify58` 은 2회차가 남긴 대로 **3연속으로 성격을 굳혔다** — 첫 실행 26/27 · 2·3회 27/27
(2회차와 **같은 모양**이다). 실패 항은 `머묾 표본 4개 · 규칙 위반 1개 · 최소 여유 −6.0px
(≥ FX3_KOM 61.5 − FX3_BSFX 11 = 50.5)` 하나로 특정했다. 2회차(내 변경 **전** 트리)에서도 같은
26/27 이 났으므로 내 3자리와 무관하다 — **곁다리 등재**한다(아래 §13).

### §12 이번 회차에 **안 한 것** 둘 — 근거와 다음 회차용 실측

2회차 목록 여섯 중 **ⅲ·ⅵ 은 손대지 않았다.** «못 했다» 가 아니라 **자리를 먼저 재야 하는 것**이라 재 놓고 넘긴다.

**ⅵ [충전] 플로터가 잘린다** — 지금 호스트는 **버튼 `.cg` 자신**(392×64)이고 `--hb-y`/`--hb-y2` 신고가 **없어서**
기본값(높이의 30%/66% = 19/42px)으로 떨어진다 ⇒ 두 줄기가 **버튼 안**에 놓이고 헤더 테두리·구분선에 걸린다.
실측(3회차, 1080×2280 · 단련 탭):

| 요소 | 화면 좌표 | 비고 |
|---|---|---|
| `.tr-temp` | x 41..1039 · y 1032..1932 | 998×900 |
| `.tp-hd`(헤더) | x 41..1039 · y 1032..1120 | 998×**88** |
| `.pv`(포인트 표시) | x 69..303 · y 1048..1104 | |
| `.cg`([충전] 버튼) | x 627..1019 · y 1044..1108 | 392×64 · 레인 신고 **없음** |
| `.tr-tp.k0`(첫 행) | x 41..1039 · y 1134..1356 | |

⇒ **헤더 아래 빈 띠는 14px 뿐**이라 아래로는 길이 없다. 그런데 **헤더 안 왼쪽에 324px 이 비어 있다**
(`.pv` 오른끝 303 ↔ 버튼 왼끝 627). 4회차 처방 후보는 **형제 둘과 같은 꼴로 호스트를 한 칸 넓히는 것**이다 —
호스트를 `.cg` → **`.tp-hd`**(룬은 카드, 투자는 행을 호스트로 쓴다 ⇒ ④ 일관성도 같이 오른다) 로 올리고
그 빈 칸에 레인을 신고한다: `--hb-x/-x2 ≈ .425`(중심 465px) · `--hb-y ≈ 14` · `--hb-y2 ≈ 52` · `--hb-slots:2` · `--hb-sw:104`
(= `.tr-tp` 가 쓰는 값과 같다). ⚠ 호스트를 넓히면 `hbPulse` 도 헤더 전체를 팝 하므로 `verify488` **[G]**(진폭이
이웃 밖으로 안 나가는가)에 그 호스트를 **먼저 등재**하고 998×1.02 = 1018 ≤ `.tr-box` 안폭 1046 을 확인할 것.

**ⅲ 버튼 «바깥» 변화가 0 에 가깝다**(룬 122px vs 대조군 29,560px) — 대조군의 29,560 은 «후광 14px + Ø20 파티클 10개»
= **연출을 하나 더 얹은 값**이다. 60 의 누름 부품에 후광을 더하는 것은 **모든 누름 호스트**를 바꾸는 일이라
(91개 규칙) 수명·사다리처럼 «같이 잡아야 하는» 축이 또 있다 ⇒ 4회차에 **부품 하나로** 설계해 넣을 것.
개별 CSS 로 룬에만 후광을 붙이면 ④ 일관성이 도로 깨진다(1회차가 «규격 3종» 으로 감점받은 이유가 그것이다).

### 3회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| BY | 7 | 6 | 5 | 5 | **5** |
| BZ | 5 | 6 | 4 | 3 | **3** |

**⇒ 3회차도 미달(통과선 8 · 2명 모두). 4회차로 넘어간다.**
(1회차 BU 3 / BV 3 → 2회차 BW 4 / BX 3 → **3회차 BY 5 / BZ 3**)

이번 회차에 고친 셋은 **둘 다에게서 값으로 확인됐다** — 밝아짐 룬 +11.9% · 투자 +11.1%(BZ),
+12.6~14.3%(BY) · 수축 −6.0~6.2% · 그리고 **ⅰ 이 닫혔다**: BY 「룬은 up(140ms) 프레임에서도
«+1»(h23px)·«−13»(h24px) 가 down 과 같은 자리·크기로 **잔존(2/2)**」. 2회차의 «0px» 은 사라졌다.

**남은 감점은 둘 다 «[충전] 한 자리» 로 수렴한다** — 이것이 3회차의 가장 중요한 결과다.

### §13 ⚑ 4회차의 머리 — [충전] 이 «누른 손 밑에서 꺼진다» 의 **진짜 경로를 찍었다**

BZ 가 «충전만 down 에서 휘도 **−10.4%**(어두워짐) · 크기 반응 **0%**» 를, BY 가 «up 프레임에 플로터
**0/2 잔존**» 을 독립으로 냈다. 3회차의 `jzNo()`(live 경로에서 «꺼짐» 미루기)는 **맞았지만 절반이었다.**
직접 재현(누른 채 시각별 표본):

| 시각 | 같은 노드인가 | `jz-dn` | `.no` | 채움색 | 홀드 |
|---|---|---|---|---|---|
| 60ms | ✅ | ✅ | ✗ | **초록**(143,220,51) | 돌고 있다 |
| 200ms | ✅ | ✅ | ✗ | **초록** | 돌고 있다 |
| **400ms** | **✗ 교체됨** | **✗** | **✅** | **회색**(185,178,164) | **멎었다** |
| 700ms · up | ✗ | ✗ | ✅ | 회색 | — |

⇒ 뿌리는 `liveTemper()` 가 아니라 **홀드가 스스로 멎으면서 도는 통짜 렌더**다.
[충전]은 «보유분 전부» 를 한 번에 바꾸므로 **2회째 시도에서 재고가 없어 `once()` 가 false 를 돌려
홀드가 ≈350ms 에 자멸**하고(`rtChargeHold` 머리말이 «2회째는 조용히 멈춘다» 로 적어 둔 그 자리),
그 `end:` 의 `renderTrain()` 이 **손가락 밑에서 노드를 갈아 끼운다** — `jz-dn` 이 함께 죽고
새 노드는 202 의 회색을 입은 채 나온다. 1회차가 룬·투자에서 닫은 «누른 노드가 사라진다» 와
**같은 병의 세 번째 얼굴**이고, 캡처가 수백 ms 를 먹으므로 비평가의 `-down` 은 예외 없이 그 «뒤» 를 본다.

**4회차 처방(권장)**: `end:` 의 통짜 렌더를 **손을 뗄 때까지 미룬다**(`jzDown` 이 살아 있으면
`pointerup`/`pointercancel` 한 번에 예약). 값·판정은 그대로이고 **표시의 순서**만 바꾸는 것이라
3회차의 `jzNo()` 와 같은 계열이다.
⚠ **함정 하나를 먼저 풀어라** — 정상 release 경로에서도 `end:` 가 도는데, 그 순간 `jzDown` 이
아직 안 지워졌으면(60 의 `jzRelease` 와 홀드의 pointerup 핸들러 중 **어느 쪽이 먼저 도는가**)
«다음 탭까지 화면이 안 갱신되는» 정반대 결함이 된다. **등록 순서를 먼저 찍고**(probe) 조건을
«자멸(release 아님)» 로 좁혀라. `rtHoldStart` 내부 순서는 `verify491` [1-b]·[1-c] 가 정규식으로
못박고 있으니 그 두 항을 깨지 않는 자리에 넣을 것.

**그 다음이 ⑥ (§12 의 ⅵ)** — 두 비평가가 3회차에도 [충전] 플로터를 «잉크 10~11px(BY) / 16~20px(BZ),
형제의 23~26px 대비 −55%» + «상단 테두리에 30~40% 잘림» 으로 짚었다. §12 의 실측(헤더 아래 빈 띠 **14px** ·
버튼 왼쪽 빈 폭 **324px**)대로 호스트를 `.cg` → **`.tp-hd`** 로 올리면 **둘이 한꺼번에 풀린다**:
칸이 998폭 기준으로 넓어져 「+1,000,000」(7자리)이 눌려 줄어들지 않고, 레인이 버튼 밖 빈 칸으로 나온다.
(지금 잉크가 작은 이유는 `hbFloat` 의 «긴 문자열은 칸에 눌러 넣는다» 가 **버튼 폭 392 → 칸 80px** 에서
16px 하한까지 내려가기 때문이다 — 폰트 상수를 키우는 처방은 오답이다.)

## 4회차 (2026-08-31, sess-0216-10281 워커 A)

### §14 ⅰ 재현이 먼저다 — 3회차 §13 의 처방이 **범인을 한 칸 비껴 있었다**

3회차는 [충전]이 «누른 손 밑에서 꺼진다» 의 시각별 표본까지 찍고 처방을 «`end:` 의 통짜 렌더를
손 뗄 때까지 미룬다» 로 적어 넘겼다. 338 규칙대로 그 처방을 따르기 **전에** `probe491` 에 **[G] 절을
신설**해 제품에게 먼저 물었다 — `renderTrain` 을 감싸 **호출 시각마다 `jzDown` 의 생사**를 적는다.

| 무엇 | 수리 전(4회차 기준선) |
|---|---|
| 누른 채 400ms | 같은 노드 O · `jz-dn` O · 초록 |
| 누른 채 **700ms** | **노드 교체 ✗ · `jz-dn` 없음 · `.no` 회색** |
| `renderTrain` 호출 중 «`jzDown` 살아 있음» | **0건** |

⇒ **`end:` 의 그 한 번은 범인이 아니었다.** `end:` 는 정상 release 에서만 도는데(60 의 `jzRelease` 가
`pointerup` **캡처**라 이 파일의 `rtHoldStop`(버블)보다 항상 먼저 돌아 `jzDown` 을 비운다),
노드가 갈린 시각은 그보다 **300ms 앞**이었다. 진짜 범인은 **0.35초 주기의 `renderTrainLive()` →
`renderTemper()`** 다 — 297 규약의 가드가 `rtHoldOn('temper')`(«홀드가 도는가»)라서, 홀드가 자멸한
**직후부터 손을 뗄 때까지** 통째로 열려 있었다. 서명(`pts`·`tstone`)은 이미 달라져 있으니 다음 주기가
곧바로 innerHTML 을 갈아 끼운다.

**3회차 처방만 따랐으면 «고쳤는데 그림은 그대로» 였다**(338·341 이 두 번 겪은 자리다).

### §15 고친 것 (제품 `index.html` **5자리** · 부품만, 개별 CSS 0줄)

| # | 무엇 | 자리 |
|---|---|---|
| ⓐ | 가드를 «홀드가 도는가» → **«손가락이 이 안을 누르고 있는가»** 로 넓힘 — `rtDownIn(root)` 신설 | `renderTemper`·`renderRunes` 각 한 줄 + 부품 1개 |
| ⓑ | 미룬 통짜 렌더를 **뗀 뒤** 친다 — `rtRender()`/`rtRenderFlush()`(되튐 `jz-up` 200ms **뒤**) | `rtHoldStop` 이웃 · `end:` 3자리 · pointerup/cancel 2자리 |
| ⓒ | [충전] 회당 피드백 호스트를 버튼 `.cg`(392×64) → **헤더 `.tp-hd`(998×88)** 로 | `rtChargeHold` 1자리 + CSS 신고 1줄 |
| ⓓ | **«칸 간격» 과 «잉크 상한» 을 분리** — `--hb-mw` 신설(신고 없으면 종전 `sw − 8`) | `hbFloat` 1줄 |
| ⓔ | [충전] 비용 줄기 폐지(1:1 전환이라 «−단련석» = «+포인트») | `rtChargeHold` 1자리 |

**⚠ 갈래를 가르는 자를 새로 만들지 않았다** — `jzDown` 하나면 된다. 캡처 단계의 `jzRelease` 가 항상
먼저 돌기 때문에 «`end:` 에서 `jzDown` 이 살아 있다 = 정상 release 가 아니다» 가 **구조적으로** 성립한다
(`probe491` [G4]·[G5] 가 양쪽을 다 단언한다). 그래서 정상 release 는 **종전대로 그 자리에서** 통짜로
돌고, «다음 탭까지 화면이 안 갱신되는» 정반대 결함이 안 생긴다(3회차 메모가 경고한 함정).

**ⓒⓓⓔ 는 한 뿌리다 — 「잉크가 작다」의 원인은 자리가 아니라 폭이었다.** 3회차 비평가 둘이
«형제 23~26px 대비 −55%» 로 짚은 것을 4회차 실측이 갈랐다: 옛 호스트는 레인 신고가 없어 두 줄기가
**버튼 안**에 놓였고, `hbFloat` 의 «긴 문자열은 칸에 눌러 넣는다» 가 버튼 폭 392 → 칸 80px 에서
**fs 16px 하한**까지 밀어 「+1,000,000」(7자리)을 **83×20px** 로 뭉갰다(형제 `+1` 은 fs 34 · 33×43).
자리만 옮기면 칸이 104px 이 되지만 잉크는 여전히 fs 16 · 20px = **형제의 47%** 다 —
**칸 간격(이웃과 안 붙는 거리)과 잉크 상한(칸에 눌러 넣는 폭)이 같은 값일 이유가 없다**는 것이
이 회차의 부품 교훈이고, 한 beat 에 **한 줄기만** 뜨면 이웃 압력 자체가 없다.
⇒ 줄기를 하나로(1:1 이라 둘째가 첫째와 **같은 수**를 말한다 — 488 5회차 교훈) · 칸 간격 24px ·
`--hb-mw` 200px ⇒ **fs 34 · 177×43px = 형제의 100%**.

그 전제(`TEMPER_PT_COST = 1`)는 `verify491` **[6-i]** 가 소스에서 지킨다 — 199 가 전환비를 바꾸면
곧바로 빨개진다(402 «표를 손으로 적지 말고 전제를 자에 걸어라»).

### §16 자리는 실측이 정했다 — 3회차 처방값 `.425` 는 **틀렸다**

3회차 §12 는 «`.pv` 오른끝 **303** ↔ 버튼 왼끝 627 = 빈 칸 324px, 한복판 `.425`» 로 적어 넘겼다.
4회차 재현은 그 값이 **포인트가 세 자리일 때의 값**임을 잡았다 — `.pv` 는 폭이 자동이라 자릿수를 따라
늘어나고, 실제 놀이 상태(1,000,500 pt)에서는 오른끝이 **404**(빈 칸 223px)다. `.425`(중심 465px)면
왼쪽 칸이 `.pv` 를 **39px** 문다. ⇒ 버튼 쪽으로 붙인 **`.475`**(중심 515px) · 세로 레인 34.
실측 결과 **439..615**(빈 칸 403..627 안, 양끝 12px 여유) · **1057..1100**(헤더 1032..1120 안).

맥박 진폭도 같이 신고했다 — 헤더는 룬 카드와 **같은 998폭**이라 기본 1.06 이면 1057.9 로 그릇
`.tr-box` 안폭 1046 을 11.9px 넘는다 ⇒ `--hb-s:1.02`(= 1018). `verify488` **[G6]·[G7] 신설**로 못박았다.

### §17 자 — 전부 초록

`verify491` **41/41**(§6 9항 · §R2 3항 **신설**) · `verify488` **73/73**([G6]·[G7] 신설) ·
`probe491` **70/71**([G] 10항 신설 — 남은 1건은 아래 ⚠).

| id | 1회차 전 | 1회차 후 | 2회차 | 3회차 | **4회차** |
|---|---|---|---|---|---|
| [충전] 누른 채 700ms «같은 노드» | ✗ | ✗ | ✗ | **✗** | **O** |
| [충전] 회당 플로터 잉크(형제 대비) | 0장 | 0장 | 10~20px | 20px(**47%**) | **43px(100%)** |
| [충전] 플로터 자리 | — | — | 버튼 안 | 버튼 안 | **헤더 빈 칸 439..615** |

⚠ `probe491` **[F6]**(«420ms 까지 살아 있다»)는 **내 변경 전 기준선에서도 빨갛다** — 338·344 규칙대로
착수 직후 수리 전 트리에서 먼저 돌려 확인했다(60/61 · 같은 항). 하네스 누적 대기가 표본 시각을
뒤로 미는 드리프트이고 제품 수명(.56s)은 3회차 값 그대로다(같은 실행의 [F4] 140ms·[F5] 300ms 는 초록).
**곁다리 등재**한다(아래 §19).

⚑ **§R(1회차 축)의 사본이 한 겹 늘었다 — 이것 자체가 관측이다.** 4회차 가드는 1회차가 막던 «첫 프레임»
까지 덮는다(60 위임이 `#app` 캡처에서 `jzDown` 을 먼저 세우므로 홀드가 시작되는 그 프레임에도 이미
손이 눌려 있다). 그래서 **순서만** 되돌린 사본이 더는 안 빨개진다(세 자리 alive=true 로 실행 확인).
기대값을 낮추는 대신 **두 겹을 다 걷어낸 사본**으로 물었고(§R 4항 그대로 초록 유지), 1회차 축 자체는
`[1-b]`·`[1-c]` 가 소스에서, 4회차 축은 **§R2 가 따로** 못박는다 — 두 축이 각각 살아 있다는 증명이다.

### 4회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| CA | 8 | 3 | 5 | 5 | **3** |
| CB | 8 | 3 | 4 | 3 | **3** |

**⇒ 4회차도 미달(통과선 8 · 2명 모두). 5회차로 넘어간다.**
(1회차 BU 3 / BV 3 → 2회차 BW 4 / BX 3 → 3회차 BY 5 / BZ 3 → **4회차 CA 3 / CB 3**)

⚠ **점수를 3회차와 나란히 놓고 «내려갔다» 로 읽지 마라 — 자가 바뀌었다.** 4회차에 캡처 장면을
**`-hold`(누른 채 ≈800ms) 한 장**으로 늘렸다(3회차 감점이 전부 «자멸 뒤» 자리인데 60ms 짜리 `-down`
한 장으로는 고쳤는지가 그림에 안 나온다). 새 프레임은 곧 **새 감점 면**이라, 두 비평가의 ② 감점이
거의 전부 그 프레임에서 나왔다. 이번 회차에 고친 축은 둘 다 **값으로 확인됐다**:
CA 「tempchg-hold 의 채움은 down 과 **완전히 동일한 눌린 초록**(rgb 136,224,44 · 93.9% 자세 유지) — ⓐ 통과」
· 「위치(헤더 띠 안)와 글자 크기(32px vs 형제 31px, +3.2%)는 맞췄다 — ⓑ 절반 통과」.

#### 4회차 2인 공통 — 5회차 작업 목록

| # | 지적(2인 독립) | 수치 |
|---|---|---|
| **ⅰ** | **[충전] 플로터가 `-hold`·`-up` 두 프레임에 0px** | CA 4705 → 0 → 0(수명 &lt; 740ms) · CB 4779 → 0 → 0 |
| ⅱ | 홀드 맥박이 안 읽힌다(`down` ↔ `hold` 버튼 bbox 가 **byte 동일**) | tempup n=16,504 동일 · tempchg n=18,148 동일 ↔ 대조군 train +1.8~8.2% |
| ⅲ | 반복 계단이 장면마다 다르다(tempup 은 Δx 0 · 4회 반복이 1회로 읽힌다) | rune Δx 140 · train Δx 60 · tempup **0** |
| ⅳ | [충전]에 감소측 짝(«−n»)이 없다 | «쌍의 50% 결손»(CA) · «형제는 전부 2개»(CB) |

#### 4회차가 **안 받은** 지적 둘 — 근거

- **«플로터를 눌린 요소 bbox 안으로»**(CA·CB 공통, rune 은 «버튼 241px 위») — **기각.**
  이 자리는 취향이 아니라 **주인 지시**다: 488 이 «회당 피드백의 호스트는 버튼이 아니라 **카드**»
  (주인 원문 «카드 자체에»)로 못박았고, 버튼에 걸면 레벨·효과 줄이 안 움직이는 데다 `jz-dn`(scale .94)과
  **같은 노드에서 싸운다**(`rtRuneHold` 머리말). 4회차가 [충전]을 버튼 → 헤더로 올린 것도 **그 규약에
  맞춘 것**이다. 비평가는 그 지시를 모르므로 5회차 프롬프트에 «호스트는 카드/행/헤더가 정답» 을 준다.
- **«+1,000,000 을 축약 표기로»**(CB ④) — **기각.** 150 규약이 «재화·개수는 숫자 그대로»(`fmt`)이고
  접는 것은 골드·전투 수치(`fmtG`/`fmtB`)뿐이다. 폭 문제는 5회차에 **자리**로 풀지 표기로 풀지 않는다.

#### ⅳ(감소측 짝)에 대한 4회차의 답 — 보류하되 근거를 남긴다

전환비가 `TEMPER_PT_COST = 1`(1:1)이라 «−단련석» 과 «+포인트» 는 **글자까지 같은 문자열**이다.
헤더의 빈 칸은 실제 놀이 상태에서 **223px**(`.pv` 가 자릿수를 따라 늘어난다)이고 한 줄기의 잉크가
**177px** 이라, 두 줄기를 같은 규격으로 넣을 자리가 가로에도 세로(88px 띠, 잉크 43px)에도 없다.
⇒ **같은 수를 두 번 말하려고 두 줄기를 다시 눌러 담으면 4회차가 회수한 «잉크 47% → 100%» 가 도로
날아간다.** 199 가 전환비를 바꾸면 `verify491` [6-i] 가 먼저 빨개지고, 그때는 «두 수가 다르다» 는
전제가 생기므로 자리를 다시 잡으면 된다(그 순서가 옳다).

## 5회차 (2026-08-31, sess-0216-10281 워커 A · 4회차와 같은 세션)

### §18 고친 것 — 2인 공통 ⅰ 하나 (제품 **2자리** · 상수 한 벌, 개별 CSS 0줄)

| # | 4회차 지적 | 무엇 | 자리 |
|---|---|---|---|
| ⅰ | «[충전] 플로터가 `-hold`·`-up` 두 프레임에 0px» | «한 발» 수명 **570 → 1300ms**(CSS `.lng` .56s → 1.3s 를 **같이**) | `HB_LIFE_LONE` + `.fx-plus.hb.lng` |

**왜 한 발만 다시 늘리는가** — 형제 셋이 그 시각에 잉크를 유지하는 이유는 홀드가 **계속 새 beat 를
쏘기 때문**이고, [충전]은 «보유분 전부» 를 한 번에 바꿔 **발이 하나뿐**이라 그 한 장의 수명이 곧
«결과가 남아 있는 시간» 이다. 3회차가 .3s → .56s 로 올려 «140·300ms 프레임» 은 샀지만,
**4회차가 자멸 구간까지 눌린 상태로 열어 주면서 비평이 보는 창이 800~940ms 로 넓어졌다** —
즉 이 지적은 4회차의 수리가 만들어 낸 «다음 칸» 이다. 불투명 구간(10~72%)이 **130~936ms** 가 되어
두 프레임을 모두 덮는다.

⚠ **홀드 반복분(`HB_LIFE` 310)은 한 값도 안 건드렸다** — `verify488` [H2]·[H4] 가 재는 사다리는
60ms 주기로 쏟아지는 그쪽 압력에서 역산된 값이고, lone 은 정의상 400ms 안에 이웃이 없다.
실행으로 확인: `verify488` **73/73 무수정**.

### §19 자 — 전부 초록 · 4회차의 곁다리 하나가 **저절로 닫혔다**

`verify491` **43/43**([6-j]·[6-k] 신설 — 비평이 보는 두 시각 800ms·940ms 에 잉크가 남아 있는가를
**약속된 시각**으로 못박는다: α 0.96 · 0.46) · `verify488` **73/73** · `probe491` **71/71** ·
회귀 무수정 `verify122` 167/167 · `verify102` 85/85 · `verify210` 101/101 · `verify519` 31/31 ·
`verify349` 29/29 · `verify64` 44/44 · `verify114` 46/46 · **SMOKE PASS**.

⚑ 4회차에 «곁다리 등재» 하려던 `probe491` **[F6]**(수리 전 트리에서도 빨갛던 항)이 **이 회차에 닫혔다** —
빨간 이유가 «420ms 가 옛 수명 .56s 의 꼬리(불투명 403ms) **바로 뒤**라 하네스 누적 대기 드리프트에
걸린다» 였고, 수명이 1.3s 가 되면서 그 시각이 불투명 구간 **한복판**이 됐다. 등재하지 않는다
(«자를 무르게 해서» 가 아니라 표본 시각이 경계에서 벗어난 것 — 같은 실행의 [F4]·[F5] 는 4회차에도 초록이었다).

### 5회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| CC | 6 | 4 | 6 | 5 | **4** |
| CD | 6 | 6 | 6 | 5 | **5** |

**⇒ 5회차도 미달(통과선 8 · 2명 모두). 6회차로 넘어간다.**
(1회차 3/3 → 2회차 4/3 → 3회차 5/3 → 4회차 3/3(자가 바뀐 회차) → **5회차 CC 4 / CD 5**)

수명 축은 **둘 다에게서 값으로 확인됐다** — CC 「down 4,702 → **hold 4,758px**」(4회차에는 hold 가 0 이었다).
④ 도 3 → 5 로 올라왔고, 4회차에 «잉크 −55%» 로 잡히던 자리는 두 비평 어디에도 안 나온다.

#### 5회차 2인 공통 — 6회차 작업 목록

| # | 지적(2인 독립) | 수치 |
|---|---|---|
| **ⅰ** | **[충전] `-up`(940ms) 만 여전히 0px** | CC 「hold 4,758 → up **0**」 · CD 「940ms 에 소멸 · 형제 셋은 생존」 |
| **ⅱ** | **눌린 순간 «호스트»(카드·행·헤더)가 0.0% 반응** | CC·CD 둘 다 bbox 998 → 998 · 대조군 train 336 → 317(**−5.7%**) |
| **ⅲ** | **[투자] 플로터에 x 지터가 없다** | CC 「IoU 0.640 · 98.3% 재점등」 · CD 「11px 차 **65.6% 겹침**, 둘 다 판독 불가」 |
| ⅳ | 긴 수명인데 이동이 11px 뿐이라 «떠오른다» 가 안 읽힌다 | CC 「740ms 정지 → 940ms 에 0 으로 끊김 = 깜빡이는 라벨」 |

## 6회차 (2026-08-31, sess-0216-10281 워커 A · 4·5회차와 같은 세션)

### §20 ⅰ 은 **제품이 아니라 하네스**였다 — 자 둘이 어긋난 것이 단서였다

`verify491` **[6-k]** 는 같은 시각(뗀 뒤 140ms ≈ 940ms)에 **α 0.46 · 잉크 생존**을 재고 있는데
비평 캡처의 `-up` 은 **0px** 이다. 같은 제품을 두 자가 반대로 읽으면 둘 중 하나가 **다른 것을 재고 있다**.
재현해 보니 `cap491` 의 **2패스**가 원인이었다 — [충전]은 1패스에서 «보유분 전부» 를 이미 바꿔 놓아
2패스의 탭은 **재고 0** 이라 `once()` 가 false 를 돌리고 **beat 자체가 안 난다.**
즉 그 0px 은 «수명이 짧다» 가 아니라 **«아무 일도 안 일어난 탭» 을 찍은 것**이다.
⇒ 2패스 **전에 재고를 되돌린다**(두 패스의 상태 차이는 «시도 1회» 뿐이어야 한다는 하네스 규약).
비평가 둘이 독립으로 같은 것을 봤어도 **제품 결함이 아닐 수 있다** — 자가 둘이면 어긋남이 단서다.

### §21 고친 것 (제품 **2자리** + 하네스 1자리)

| # | 5회차 지적 | 무엇 | 자리 |
|---|---|---|---|
| ⅰ | «`-up` 만 0px» | **하네스** — 2패스 전 재고 복원 | `cap491` 1자리(제품 0줄) |
| ⅲ | «[투자] x 지터 0 · 65.6% 겹침» | 사다리 칸 **2 → 4**(간격 104 유지 · 잉크 상한 96 을 `--hb-mw` 로 **못박아** 안 줄인다) | `.tr-tp` CSS 1줄 |
| ⅳ | «이동 11px — 깜빡이는 라벨» | lone 전용 키프레임 신설 — 이동 24 → **48px**(글리프 높이 33px 의 1.45배) | `.lng` 2줄 + 키프레임 2벌 |

⚑ **ⅲ 은 4회차의 부품 하나가 열어 준 문이다** — 488 5회차가 칸을 2로 줄인 이유는 «잉크 91px 이
칸 70px 을 넘는다» 였는데, 4회차가 **잉크 상한을 칸 간격에서 떼어 놓아서**(`--hb-mw`) 이제 칸을
늘려도 잉크가 안 줄어든다. 중심 x **329/433/537/641**(행 안 281..689) — 겹칠 자리가 없다.
⚠ 홀드 반복분(`.hb` .3s · 24px)은 **한 값도 안 건드렸다** — 이동을 늘리면 사다리가 세로로도 겹친다.

### §22 6회차가 **안 한 것** — ⅱ(호스트 눌림)와 그 근거

두 비평가가 ①의 유일한 원인으로 지목한 «눌린 순간 호스트가 0.0%» 는 **이 회차에 안 건드렸다.**
대조군 `train` 이 −5.7% 로 움직이는 이유는 **눌린 것이 곧 호스트(카드)이기 때문**이고(64 홀드),
룬·단련은 **누르는 것은 버튼, 회당 피드백의 호스트는 카드/행/헤더**라는 488 주인 지시 구조다.
여기에 «호스트 눌림» 을 더하려면 **60 의 누름 어휘를 전역으로 늘리는 일**(누른 노드의 조상 중
호스트를 찾아 같이 누른다)이고, `jz-dn`(scale .94)과 `jz-hb`(1.02) 가 **같은 노드에서 곱해지는**
자리를 하나 더 만든다(488 2회차가 `scale:` → `transform:` 으로 한 번 겪었다) ⇒ `verify488` [G] 의
진폭 규약(998×1.02 ≤ 1046)과 같이 재설계해야 한다. **부품 하나로 설계해 다음 회차 머리로 잡는다** —
개별 CSS 로 세 화면에만 붙이면 ④ 일관성이 도로 깨진다(1회차가 «규격 3종» 으로 감점받은 자리).
**처방 초안**: `jzTarget` 이 고른 버튼에서 `closest('.tr-rn,.tr-tp,.tr-temp>.tp-hd,.tr-card')` 로 호스트를
찾아 `jz-hdn`(scale **.985** — 998폭에서 −15px = 대조군 −19px 의 79%)을 `jz-dn` 과 같은 수명으로 걸고,
[G] 에 «호스트 눌림 + 팝이 그릇을 안 넘는다» 항을 신설한다(1.02 × .985 = 1.0047 로 상한은 오히려 내려간다).

### 6회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| CE | 7 | 4 | 3 | 3 | **3** |
| CF | 5 | 7 | 3 | 4 | **3** |

**⇒ 6회차도 미달. 7회차로 넘어간다.** (3/3 → 4/3 → 5/3 → 3/3 → 4/5 → **3/3**)

⚑ **6회차 점수의 절반은 «내가 만든 캡처 잔상» 이 받은 것이다 — 채점 뒤에 재현으로 갈랐고, 둘 다
하네스였다.** 두 비평가가 **독립으로** ③ 을 3점까지 내린 근거 두 줄이 그것이다:

| 지적 | 실체 | 처방(적용 완료) |
|---|---|---|
| 「tempup `-up` 헤더 잔액이 `-down` 비트맵과 **0px 동일**로 되돌아간다 — 같은 프레임의 «투자한 7 pt» 와 6pt 어긋난다」(CE·CF 공통) | 6회차 하네스가 **2패스 전에 네 재화를 다 되돌린** 탓. 룬·투자·훈련은 1패스 홀드가 값을 여러 칸 움직이므로 되돌리면 `-up` 숫자가 `-hold` 보다 **뒤로 간다** | 되돌림을 **[충전] 장면에만** 건다(재고를 한 번에 다 쓰는 버튼은 그것뿐) |
| 「tempchg `-up` 에 «+1,000,000» 이 **둘**(헤더 + 공격력 단련 행) = +2,000,000 으로 오독」(CE·CF 공통 · CE 는 색까지 갈랐다) | 5회차가 수명을 1.3s 로 늘리면서 **앞 장면(tempup)의 플로터가 다음 장면 프레임까지 살아 넘어왔다.** 그 좌표(y258-291)가 정확히 앞 장면 사다리 자리다 | 장면 사이에 **`#fxl` 을 비운다** |

**교훈은 «수명을 늘리면 캡처의 장면 경계도 같이 길어진다» 이다** — 제품 상수 하나가 하네스 규약을
바꾼다. 두 비평가가 독립으로 같은 것을 봤어도 **제품 결함이 아닐 수 있다**(§20 과 같은 계열이고,
이번엔 자가 아니라 **내가 방금 바꾼 하네스**가 범인이었다).

### §23 CE 가 잡은 **진짜 회귀 하나** — 이동 48px 은 88px 띠에 안 들어간다

「tempchg-hold 잉크가 y48..77 = 30px · 위 4px(11.8%)이 평평하게 잘린 것처럼 보인다」 —
6회차가 이동을 24 → 48px 로 늘리면서 헤더에서만 상변이 위 테두리에 얹혔다. 산수로 닫히지 않는다:
안쪽 면 8..80 에 «시작 L+43 ≤ 80»(L ≤ 37)과 «끝 L−48 ≥ 8»(L ≥ 56)은 동시에 못 선다.
⇒ **이동 거리를 호스트가 신고하게** 했다(`--hb-rise`, 기본 48px · 헤더만 **22px** → 끝 12..55).
키프레임 안의 `var()` 는 요소마다 풀리므로 **부품은 그대로 하나**다.
`verify491` **[6-l] 신설** — «800ms(가장 높이 떠오른 자리)에도 잉크가 헤더 안쪽 면 안» 을 못박는다.

### §24 7회차 머리 — 순서대로

1. **호스트 눌림**(①의 유일한 원인 · 4명 연속 지적: CC·CD·CE·CF) — §22 의 처방 초안 그대로.
   CE·CF 는 «이동» 말고 **가산 오버레이**(대조군의 흰 글로우 링 · 파티클 ~10개)가 통째로 없다는
   것도 같이 짚었다(CF 「외곽 10px 밴드의 88.65% ↔ 0.00%」). 둘은 다른 축이니 **같이** 설계할 것.
2. **[투자] 플로터 x 지터**(3회 연속 2인 공통) — ⚠ **x 축은 막혀 있다**: 6회차가 칸 2 → 4 를 넣자
   `verify488` **[I6]** 이 즉시 빨개졌다(봉투 ±190px 가 `tn`·`tc` 를 밟는다). 다음 회차는 **세로**
   (발마다 레인을 흔든다)나 잉크 폭 쪽으로 가되, [I6] 의 봉투식(`(n−1)/2×sw + 잉크/2`)으로
   **빈 칸을 먼저 재고** 시작해라. 재지 않고 손대면 6회차와 같은 자리를 다시 밟는다.
3. **대조군 `train` 자신의 결함 2건**(CE) — 파티클 흰 코어가 «Lv. n» 숫자 잉크 상자 **안**에 들어가
   글자를 끊고, 글로우가 이웃 카드 테두리를 11px 침범한다. 기준이 흔들리면 ④ 채점이 통째로 흔들린다.
4. **[충전] 회색 전환이 940ms 로 늦다**(CE ①) — 3회차 `jzNo` 가 «누르는 중에는 «꺼짐» 을 미룬다» 로
   **의도한** 동작이고, 4회차가 자멸 구간까지 눌린 상태를 늘리면서 그 미룸이 800ms 까지 길어졌다.
   즉 결함이 아니라 **두 규약의 경계**다 — «눌린 채로 재고가 0 이 된 것» 을 무엇으로 말할지 정할 것
   (라벨은 이미 60ms 에 «0 → 포인트 0» 으로 바뀌어 있다).

## 7회차 (2026-08-31, sess-0413-22194 워커 A)

### §25 ⅰ 호스트 눌림 — **처방 초안이 틀린 속성을 골라 놨었다**(제품 `index.html` 3자리 · 부품만)

6회차 §22 가 남긴 처방 초안은 «`jz-hdn`(scale .985)을 `jz-dn` 과 같은 수명으로 걸어라» 였고,
그대로 쓰면 `.jz-hdn{animation:jzDn …}` 이 된다. **그 시안은 실제로 안 움직인다.**

| 무엇을 쟀나 | 시안 ⓑ(`animation`) | 채택안(정적 `scale:`) |
|---|---|---|
| 클래스가 붙었나 | `jz-hdn` **true** | true |
| `getComputedStyle(host).scale` | **none** | 0.985 |
| 호스트 폭(998 기준) 세 시각 | ×1.0136 / 0.9554 / **0.94** (튄다) | ×0.985 / 0.985 / 0.985 |

뿌리는 **`animation` 이 이 노드에서 이미 임자가 있다**는 것이다 — 홀드가 도는 내내 488 `jz-hb`(맥박)가
60~160ms 마다 같은 호스트에 걸리는데, 둘 다 `animation` **단축**이라 한 요소에서 캐스케이드가 하나만
고른다(뒤에 선언된 `.jz-hb` 가 이긴다). 그래서 «클래스는 붙어 있는데 호스트가 한 픽셀도 안 줄어드는»
상태가 나오고, 맥박 사이 프레임에서는 반대로 `jzDn` 이 이겨 **.94 로 튄다**(호스트에는 과한 값).
⇒ 누름을 **애니메이션이 아니라 정적 값 + 트랜지션**으로 준다.
`scale`/`translate` 는 맥박이 쓰는 `transform` 과 **다른 속성**이라 둘이 곱해진다
(1.02 × .985 = 1.0047 — 팝의 상한이 오히려 내려가 그릇을 안 넘는다).

제품은 세 자리다:
1. `.tr-rn,.tr-tp,.tr-temp>.tp-hd,.tr-temp>.tp-ft{transition:scale .07s ease-out,translate .07s ease-out}` — 호스트 넷이 공유
2. `.jz-hdn{scale:.985;translate:0 6px}` — 뗌은 클래스를 떼면 같은 곡선으로 되돌아간다(`jz-hup` 불요)
3. `JZ_HOST_SEL` + `jzHostOf()` + `jzInit`/`jzRelease` 두 줄 — 누른 버튼의 조상에서 호스트를 찾는다

값은 취향이 아니다. `.tr-rn` 998폭에서 .985 = **−14.97px** 로 대조군 카드의 −19px 의 79% 이고,
`.94` 를 그대로 쓰면 998 → 938 이라 그릇 `.tr-box` 안폭 1046 안에서 이웃이 같이 흔들린다.
가라앉음 6px 은 `jz-dn` 과 같은 부등식(translate > h(1−s)/2 — h=700 에서 5.25px)에서 나왔다.

⚠ **첫 시안이 남긴 함정 하나를 더 잡았다 — 커스텀 속성은 상속된다.** 처음엔 `jzDn` 키프레임을
`var(--jz-s,.94)` 로 열어 호스트가 값을 신고하게 했는데(6회차 `--hb-rise` 선례), 호스트에 둔
`--jz-s:.985` 가 **그 안의 버튼까지 내려가** `.jz-dn` 이 그 값을 집었다 —
`verify491` **[5-c] 가 버튼 scale 을 0.985 로 찍어** 잡았다(폴백은 «값이 없을 때» 만 쓰이므로 상속을 못 막는다).
정적 값으로 갈아타면서 키프레임은 **원래 상수로 되돌렸다** — `jzDn`·`jzUp` 은 한 글자도 안 바뀌었고
기존 25자리 회귀는 0 이다([7-c2] 가 그것을 못박는다).

### §26 ⅱ 첫 발 «가산 오버레이» — 대조군과 **같은 부품을 같은 시점에** (제품 3자리)

CE·CF 가 2인 공통으로 «대조군에는 있고 룬·단련에는 통째로 없다» 고 낸 축이다
(CF 실측 「호스트 외곽 10px 밴드의 변화율 대조군 88.65% ↔ 0.00%」).
대조군의 그 그림은 `trHoldStart` 의 `fxUpOk(card, card, txt)` 한 줄이고, 룬·단련은 488 이
«회당 피드백»(`hbBeat`)만 달아 이 한 세트가 빠져 있었다. ⇒ `rtHoldStart` 의 **첫 발 자리**에
`rtFirstFx(o.host)` 를 놓고 세 호출부가 자기 호스트를 넘긴다(`sel0` · `host` · `HD` — `hbBeat` 와 같은 노드).

⚠ `fxUpOk` 를 통째로 부르지 않고 **두 조각**(`fxFlash` + `fxBurst(…, FXPAL.up, 10)`)만 쓴다. 뺀 둘은 이유가 있다:
- `fxHitEl`(대상 자체 scale) — `transform` 을 쓰는데 §25 에서 본 대로 그 자리는 맥박 임자다.
  그리고 «대상 자체가 튄다» 는 이번 회차의 `jz-hdn` 이 이미 맡는다.
- `fxDelta`(델타 문구) — 회당 «+1»·«−n» 은 `hbBeat` 사다리가 이미 말한다.
  겹치면 58 이 네 라운드 태운 «같은 수가 두 곳에서 말해진다» 가 된다.

⚠ **첫 발에만** 건다(대조군과 같다). 반복분에 걸면 60ms 마다 링이 나 `#fxl` 이 `FXMAX` 로 막힌다.

### §27 자 — `verify491` **77/77**(§7 21항 · §R3 4항 신설)

| 항 | 무엇 | 실측 |
|---|---|---|
| [7-a]·[7-b] | 소스 — 호스트 목록 · «누른 것 자신이면 안 고른다» | — |
| [7-c0]~[7-c2] | `.jz-hdn` 은 정적 값 · 호스트 넷이 트랜지션 한 줄 공유 · **`jzDn` 원본 불변** | — |
| [7-*-b3] ★★ | **호스트 폭이 .985 배**(맥박과 곱해진다) | 998 → **983.03** = ×0.985 (세 자리 전부) |
| [7-*-b2] ★ | 누른 버튼은 여전히 .94 배(호스트가 버튼을 약하게 만들지 않았다) | 420→388.88 · 300→277.77 · 392→362.95 = ×0.9259 (= .94 × .985) |
| [7-*-c] | 찍힌 픽셀 — 호스트가 실제로 달라진다(수리 전 0.00%) | 룬 **20.68%** · 투자 **33.82%** · 충전 **98.56%** |
| [7-*-d] | 첫 발 가산 오버레이 | `.fx-flash` **1장** + 파티클 **10개** (세 자리 전부) |
| [7-train] | ★대조군은 `jz-dn` 하나뿐 — `jz-hdn` 이 안 겹친다 | dn=true · hdn=false |
| §R3 ⓐ | 클래스를 안 붙인 사본 | ×1.0136 / 1 / 1.02 = **안 준다** |
| §R3 ⓑ | 정적 값 → `animation` 사본 | ×1.0136 / **0.9554** / **0.94** = **고르지 않다** |

⚑ **§R3 ⓑ 가 이 회차의 자에서 가장 중요한 항이다.** [7-*-a](«클래스가 붙었나»)만 묻는 자였으면
시안 ⓑ 에서도 **초록**이 났다 — 클래스는 붙어 있었으니까. «화면에서 실제로 줄었는가» 를
**bbox 비**로 묻고, 되돌린 사본이 «고르지 않음» 으로 빨개지는 것까지 봐야 자가 닫힌다.
⚠ 처음엔 `getComputedStyle().scale` 을 70ms 에 읽었는데 같은 트리에서 1.000 ↔ 0.990 ↔ 0.995 로
흔들렸다(트랜지션 .07s·맥박 .08s 한복판). **200ms 에 bbox 로** 재는 것으로 바꿔 결정적이 됐다.

### 7회차 점수

| 비평가 | ① 즉시반응 | ② 결과잔존 | ③ 재화변동 | ④ 일관성 | **최저** |
|---|---|---|---|---|---|
| CG | **8** | **8** | 6 | 7 | **6** |
| CH | **8** | 6 | 6 | 6 | **6** |

**⇒ 7회차도 미달(통과선 8). 추이 3/3 → 4/3 → 5/3 → 3/3 → 4/5 → 3/3 → **6/6**(루프 시작 이래 최고).**

⚑ **①축이 닫혔다 — 이 루프에서 처음이다.** 4회차부터 여섯 명(CC·CD·CE·CF·CG·CH)이 연속으로
«①의 유일한 원인 = 눌린 순간 호스트가 0.0%» 를 지목했는데, 두 사람이 **독립으로 8** 을 줬다.
근거도 같은 방향이다 — CG 「버튼 규격(−7.3~−7.6% · +15~16px 침하)과 호스트 인셋(8px)·틴트
(ΔB 36~42/255)는 세 표적이 완전히 일치한다」 · CH 「호스트 박스 안 변화 픽셀 rune 98.2% /
tempup 99.2% / tempchg 99.2%(대조군 91.3%) … 여기까진 셋 다 같은 값으로 잘 맞춰져 있다」.
**CG 는 틴트가 대조군보다 세다고까지 적었다**(표적 ΔB 36~42 vs 대조군 23 = 1.6~1.8배).

### §28 8회차 머리 — 두 사람이 **독립으로 같은 순위**를 매겼다

두 채점표의 «다음 회차 3개» 가 1·2순위에서 겹친다. 남은 감점은 **내가 이번에 만진 층이 아니라
플로터 레이어**에 몰려 있다(CG 「④의 실점은 전부 플로터 레이어에 있다」).

1. **[충전]의 «낸 것» 이 화면에 0px** — ③의 최대 실점(2인 공통).
   CG 「4프레임 전부에서 «−» 부호 잉크 0px … 넷 중 하나만 반쪽이다」 ·
   CH 「+1,000,000 은 181×40px·코어 2,975px 인데 대응하는 −1,000,000 이 어디에도 없다.
   대조군 −45:+20 = 0.89 · rune −13:+1 = 1.6 인데 tempchg 만 ∞」.
   ⚠ **이것은 4회차의 «의도» 를 뒤집는 일이다** — 그때 «전환비 1:1 이라 «−단련석» 이 «+포인트» 와
   같은 수다» 라며 비용 줄기를 껐고, `verify491` **[6-e]** 가 «한 줄기» 를 못박고 있다.
   두 사람이 독립으로 «100만을 냈다는 사실이 눌린 순간부터 어디에도 없다» 로 읽었으므로
   **게이트를 갈아 끼우고 두 줄기로 간다**(333 처방 — 자리를 비우지 말 것).
   ⚠ 자리를 **재기 전에 고치지 마라**: 헤더 `--hb-x`·`--hb-x2` 가 **둘 다 .475** 라 두 줄기가
   같은 자리에서 시작한다. [6-h] 가 지키는 «`.pv` 오른끝 ↔ 버튼 왼끝» 빈 칸을 **두 몫으로 갈라야**
   하고, CG 가 그 칸의 여유를 이미 실측해 뒀다(`-down` 49px · `-up` 14px — 잔액 자릿수에 따라 변한다).
2. **홀드 중 호스트가 말을 끊는다** — ②의 실점(2인 공통).
   CH 「hold 프레임의 호스트 채움 델타 0~6/255 · 링 0/255. 대조군은 같은 시점에 본문 +20/255 ·
   링 B −83/255」 · CG 「세 표적 모두 `-hold` 호스트 채움이 idle 과 0/255 동일 … 740ms 동안
   남는 신호는 −8px 스케일 하나뿐」. 뿌리는 이번 회차 §26 의 «첫 발에만 건다» 다 —
   가산 오버레이(`fx-flash` .34s)가 340ms 에 꺼지고 나면 홀드 구간에는 맥박의 **기하 팝만** 남는다.
   ⚠ **반복분에 `fxFlash` 를 다시 쏘는 길은 막혀 있다**(60ms 마다면 `#fxl` 이 `FXMAX` 로 막힌다).
   **재현 먼저** — 대조군이 800ms 에 무엇으로 그 틴트를 유지하는지부터 찍어라(`fxUpOk` 는
   대조군도 첫 발 한 번뿐이다 ⇒ 다른 부품이다).
3. **플로터 규격·배치가 네 자리에서 갈린다** — ④의 실점(2인 공통).
   글자 상자 높이 tempup 31 · rune 32~33 · train 38~40 · tempchg 40px(**최대 29% 차**) ·
   글리프당 코어 잉크가 대조군의 **1/1.6~1/2.2** · 반복 시 흩뿌림 규칙이 **셋**
   (rune 가로 65~140px 겹침 0% · train 세로 24px 겹침 27% · tempup 세로 11~12px **겹침 66%**) ·
   ±쌍 좌우 순서가 장면마다 뒤집힌다.
   ⚠ **x 축은 6회차가 이미 막힌 것을 확인했다**(`verify488` [I6] 봉투 ±190px 가 `tn`·`tc` 를 밟는다).

⚠ **채점 뒤 재현으로 갈라야 할 «하네스 혐의» 둘**(6회차 §20·§23 과 같은 계열):
- CH 「흰 «1,000,000» 이 `tempup-idle`·`tempup-hold`·`tempup-up`·`tempchg-idle` **4장**에 상단 4px 만
  노출된 채 남아 있다」 — 손대기 **전** 프레임에 결과물이 있는 것은 제품이 아니라 **캡처 잔상**일 수 있다.
- CH·CG 「tempchg `-up` 에서 재고 0인데 잔액이 1,000,494 → 2,000,494 로 또 는다」 —
  `cap491` 이 **[충전] 장면에서만 2패스 전에 재고를 되돌리는** 6회차 처방의 그림자다(제품 경로가 아니다).
  **둘 다 «제품 결함이 아닐 수 있다» 를 먼저 확인할 것** — 6회차가 같은 자리에서 두 번 속았다.
