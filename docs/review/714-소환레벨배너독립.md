# 714 — 소환 레벨·경험치를 배너 독립 다섯 벌로 (496 «공용 하나» 번복)

**주인 지시(2026-09-02 03:00)**: «무기 방패 목걸이 스킬 펫 소환레벨이랑 경험치 같이 뭉쳐놓은 느낌인데
사실이면 분리해라. 경험치도 당연히 분리»

세션 `sess-1842-25879` (루틴 워커 C) · **1회차 완료** · 지시서 [3]-**(가)** 기계적/구조 작업 —
레퍼런스 대조가 없으므로 **비평가 없음**, 재현 + 자 + 회귀로 닫는다.

---

## 1. 재현이 먼저 — «사실이면» 은 조건문이다

주인 문면이 «사실이면 분리» 라 **재현이 곧 착수 근거**다(338 규칙). `tools/probe714.js` 를 신설해
수리 전 트리(`P714_SRC` 로 겨눈다)에서 찍었다:

| 물음 | 수리 전(496 공용) | 수리 후(714 배너별) |
|---|---|---|
| ⓐ 무기 배너에만 100 뽑 | **다섯이 전부 100** | 무기만 100 · 나머지 넷 0 |
| ⓑ 10 상점 카드 5 장의 Lv 알약 | **같은 값 1 종**(마지막에 쓴 값 하나로 접힌다) | 넣은 값 그대로 5 종 |
| ⓒ 무기만 만렙 → 방패 불멸 확률 | **0.1000%**(한 번도 안 뽑은 배너가 만렙표) | 0.0000% — 오염 0 |
| ⓓ 세이브에 담기는 자리 | `sumLv`·`sumExp` 스칼라 둘 | `sum` 다섯 벌 + `sumVer` |

⇒ 주인 보고가 **그대로 사실**이었다. `probe714` 는 두 트리에서 다 돈다(455·probe496 방식) —
수리 뒤에 못 도는 재현기가 되지 않게, 트리를 스스로 갈라 같은 물음에 반대 답을 단언한다.

## 2. 제품 — 6자리, 표시·마크업 0줄

496 이 «호출부 모양을 지켜 둔 것» 덕에 **화면 코드는 한 줄도 안 건드렸다.**
10 상점 카드는 내내 `sumLv(x.b)` 를, 확률 팝업은 `sumLv(prbBank)` 를 넘기고 있었고,
496 이 그 인자를 **일부러 무시**했을 뿐이다. 그 무시를 걷어내니 표시가 저절로 갈라졌다.

1. **별칭 뷰 `sumAliasView` 선언째 폐지** — 소비처가 0 이 됐고, 남기면 «저장되는 다섯 벌» 과
   «저장 안 되는 별칭» 이 같은 이름을 놓고 다툰다(죽은 코드 금지 규약).
2. **`DEF()`** — `sumLv:1, sumExp:0` → `sumVer: SUM_VER` + `sum: BKEYS.reduce(...)`.
   표를 손으로 안 적는다(배너가 늘면 `BKEYS` 가 따라온다 · LESSONS 106-1).
3. **접근자 셋** — `sumOf(b)` 를 새로 두고 `sumLv`/`sumExp`/`sumAddExp` 가 인자를 다시 읽는다.
   모르는 배너 이름에는 **읽기 전용 Lv1 폴백**(`S.sum` 에 칸을 안 만든다 — 오타 하나로 저장 구조를
   더럽히지 않는다).
4. **`load()` 이관 3판** — 아래 §3.
5. **죽은 스칼라 두 키 `delete`** — 남기면 아무도 안 읽는 값이 계속 저장된다(204 `daily.dun` 처방).
6. 상점 카드 주석 — 496 이 남겨 둔 «알약을 탭 상단 한 곳으로 올릴까» 물음이 **이 지시로 닫혔다**
   (카드마다 값이 다르므로 알약은 카드에 있어야 한다).

## 3. 세이브 이관 — 판이 셋이라 갈래도 셋

⚑ **갈래를 가르는 것이 이 작업에서 가장 조용한 함정이었다.** 496 세이브(`sumLv` 스칼라)와
496 **이전** 세이브(`sum` 다섯 벌 · 구 곡선)는 둘 다 «`sum` 이 있고 판 번호가 없는» 모양으로 보인다 —
`sumLv` 하나로는 못 가른다. 그래서 **`sumVer`** 를 새 세이브에만 적는다.
**KEY 는 안 올렸다**(LESSONS 44-② — 키 하나로 읽는 `load()` 에서 KEY 상승 = 구 세이브 전멸).

| 판 | 판정 | 처리 | 근거 |
|---|---|---|---|
| ⓐ 714 | `sumVer ≥ 2` | 칸마다 클램프만(멱등) | `verify714` C3·C4 |
| ⓑ 496 | `sumLv` 유한 | 그 하나를 **다섯에 복제** | 주인 지시문 «통합값 5배너 복제(손해 0)» |
| ⓒ 496 이전 | 둘 다 없음 | 배너마다 구 곡선(V196)으로 되돌려 세서 **그 배너에** 얹는다 | `verify714` C5·C6 |

⚑ **ⓑ 는 «총 뽑기 수 보존» 이 아니라 «레벨 보존» 이다** — 496 이 다섯 주머니를 한 주머니에
부어 버려서 되쏟을 근거가 없다. 나누면 그 세이브는 **실제로 잃고**, 복제는 아무도 안 잃는다.
주인 지시문이 정확히 그 쪽을 골라 뒀다.

⚑ **ⓒ 는 496 처럼 합치지 않는다** — 배너가 다시 독립이므로 합치면 네 배너를 공짜로 부풀린다.
검산: 구 12/8/3/20/5 세이브 → `skill 21/3350 · weapon 11/2500 · shield 7/440 · amulet 1/250 · pet 3/520`,
되돌려 세면 `51450 · 16050 · 6050 · 250 · 1550` = 구 곡선 뽑기 수와 **칸마다 정확히 같다**.

## 4. 게이트

**신설**
- `tools/verify714.js` **32/32** — [A] 5×5 교차 매트릭스(대각선만 7, 나머지 20 칸 0) ·
  [B] 세이브 모양 · [C] 이관 세 판 + 손댄 세이브 방어 · [D] 카드 5 장·확률 팝업 ·
  [E] `gradeProbs(b)` 오염 0 · [R] 되돌림(공용으로 되돌린 사본에서 [A1] 이 빨개진다)
- `tools/probe714.js` **11/11** — 두 트리 다 돈다 · ⓔ 절이 199 통지 수치를 곡선에서 직접 센다

**이관(333 처방 — 방향만 뒤집고 자리는 안 비웠다)**
- `verify496` **41/41** — [A]·[D]·[E]·[R] 이 같은 물음을 반대 답으로 묻는다.
  **[B] 곡선 · [C] 사다리는 714 가 한 줄도 안 건드렸고 그것이 지금도 496 의 몫**이다.
  ⚠ 이 항들을 지웠으면 «496 이 통째로 사라져도 초록인 게이트» 가 된다(328-330 교훈).
- `verify196` **39/39** — [F1]·[F3] 만 뒤집었다(«한 값으로 접힌다» → «각자 접힌다»)
- `verify76` **30/30**(29 → 30, [전제] 한 항 신설) — §4 ⓐⓑ 가 **두 번 방향이 바뀐 자리**다:
  568 «amulet 키 없음 → {lv1,exp0}» → 496 «다섯을 한 주머니에» → **714 가 다시 {lv1,exp0}**.
  그래도 값을 손으로 안 적는다(568 경고 그대로) — 기대값은 같은 규약을 되돌려 세서 만든다.
- `probe568` **21/21** — [1]②·[5]·[6]·[7] 뒤집음. **568 의 뿌리가 714 로 사라졌다**
  («스킬 칸의 252 뽑이 목걸이 칸으로 흘러오던 것» 이 그 뿌리였다).

**⚠ «안 고쳐도 초록» 이던 자 10종을 같이 옮겼다 — 그것이 이 회차의 교훈이다**
`S.sumLv = 31` 이 **조용한 no-op** 이 되어 카드를 Lv.1 로 그린 채 «Lv.31 알약이 캡슐에 안 물린다» 를
통과시키고 있었다(655 가 잡으라고 세운 그 자). 빨간 자만 좇았으면 **헛초록 10자리**가 남았다:
`verify655` · `probe655` · `verify675` · `probe675` · `probe669` · `cap669` · `probe522` ·
`func196` · `verify106` · `verify195`.

**회귀 무수정**: `verify528` 14/14 · `verify115` 29/29 · `fnchk115` 12/12 · `verify149` 298/298 ·
`verify251` 27/27 · `verify250` 16/16 · `verify186` 34/34 · `verify85` 41/41 · `verify10` 64/64 ·
`verify327` 85/85 · `verify363` 80/80 · `verify514` 64/64 · `verify713` 45/45 · `verify735` 11/11 ·
`probe470` · `probe251` · `probe528` 12/12 · **SMOKE PASS** · `verifyProgress` 종료 코드 0

## 5. ⚑ 199 통지 — 소환 축의 비용이 ×5 가 됐다

| 축 | 496(공용) | 714(배너 독립) |
|---|---|---|
| 만렙까지 | 267,050 뽑 = 26,705,000 다이아 | **1,335,250 뽑 = 133,525,000 다이아** |
| 불멸 해금(Lv49)까지 | 256,560 뽑 | **1,282,800 뽑**(배너당 256,560 × 5) |

199 의 «한 벌» 목표(불멸 장비 3 · 스킬 1 · 펫 1)는 **다섯 배너 전부**에서 불멸을 뽑는 것이므로
이 축을 정면으로 맞는다. 199 결2(«2,730만 = 소환 전용 장부»)는 496 의 267,050 뽑에서 역산한
값이라 **그대로는 안 맞는다** — 계수 확정은 199 몫이고, 손잡이는 셋이다:
① `SUM_EXP_A`/`SUM_EXP_B`(배너당 곡선을 1/5 쪽으로) · ② 배너별 목표를 «한 배너 만렙» 으로 재정의 ·
③ 다이아 수급. **이 행에서 계수는 0줄 건드렸다**(326·331·398 처방 — 구조와 계수를 한 세션에 섞지 않는다).

`tools/bot199.js` 도 같이 옮겼다 — 소환 레벨 «한 눈금» 이 다섯 칸을 같이 올린다.
한 칸만 올리면 나머지 네 배너의 등급 개방이 영영 Lv1 에 묶여 «어울리는 캐릭터» 규약을 벗어난다.

## 6. 곁다리 관측 1건 — **755 등재(선점 안 함)**

`tools/fnchk186.js` 2건 실패(9/11) — 05 장비 팝업의 «미해금 등급 안내» 표본이
662·664(«미개방 정보 공개»)를 못 따라왔다. **수리 전 트리에서도 같은 9/11** 임을 직접 대조로
확인했다(338·344 규칙) — 내 변경과 무관하고 내 작업 단위가 아니라 안 고쳤다.

## 7. 다음 세션이 볼 것

- **소환 레벨을 상태로 만드는 자를 쓸 때 `S.sumLv = n` 은 이제 아무 일도 안 한다.**
  `S.sum[b].lv = n`(한 배너) 또는 `BKEYS.forEach(...)`(카드 잉크를 잴 때)로 적어라.
  틀리면 예외가 아니라 **Lv1 표본**이 나오고, 그건 대개 빨강이 아니라 헛초록이다.
- 세이브 판을 새로 가를 일이 생기면 `sumVer` 를 올려라. `sum` 의 **모양이 같은데 뜻이 다른**
  두 판이 또 생기면 이번과 똑같이 갈래를 못 고른다.
