# 981 — 초승달·4각별 프리미티브를 4종이 나눠 쓴다 (① 분간의 마지막 뿌리)

> T4 폴리시(실루엣 재설계) · 상한 10회차
> 등재: 2026-09-06, sess-0658-28473 루틴 워커 D (792 11회차 곁다리)

---

## 1회차 (2026-09-06, sess-0813-3545 루틴 워커 A)

**제품 `index.html` 1자리**(`rock` 반지름 표 6값) · 자 2벌 신설(`probe981`·`verify981`) ·
레이아웃(HUD·팝업) **Δ0px** · 밸런스·판정 **0줄**.

### ① 첫 숙제 — «어느 쪽이 옳은 질문인가» 를 정했다

등재문이 남긴 숙제가 이것이었다: `verify792` [D1](그리는 자리 그대로 IoU)과 비평가의
«회전·미러 정렬 후» 가 **다른 값**을 내는데(0.79~0.83 ↔ 0.922) 어느 쪽이 ①(분간)의 자인가.

**답은 제품 안에 있었다.** `shotBody` 의 모든 가지가 `ctx.rotate(b.a)`(또는 `b.spin`)로
실루엣을 **진행 방향에 매단다**(index.html 28841~29360, 회전 호출 18자리). 즉 같은 종이
화면에서 온갖 각도로 나타나므로 «각도만 다른 두 종» 은 플레이어에게 같은 그림이다.
절대 각도에 기대는 [D1] 로는 그 사실이 **원리적으로** 안 보인다 — 그래서 [D1] 이 다섯 회차
내내 초록인 동안 비평가 ①이 3~6점을 줬다.

⇒ **[D1] 은 한 글자도 안 건드렸다.** 그것은 710 이 얹은 «후광을 얹느라 실루엣이 서로 붙지
않았나» 회귀 축이고 지금도 옳다. 981 은 그 **옆에** 회전·미러·크기를 지운 축을 새로 세웠다.

두 자가 다른 질문을 잰다는 것은 자 자신이 찍는다 — `arc↔arrow` 는 정렬 전 **0.046**,
정렬 후 **0.512**(+0.466)다. [D1] 에게 «남남» 인 쌍이 정렬 축에게는 절반이 같은 그림이다.

### ② 재현이 등재문을 두 군데 고쳤다

`node tools/probe981.js` — 본체 마스크(α ≥ 0.55)만, 무게중심·면적 정규화 후 회전 240스텝 × 미러 2.

| 등재문이 지목한 쌍 | 정렬 IoU | 정렬 전 | 판정 |
|---|---|---|---|
| `arc↔moon`(slash↔curve) | **0.693** | 0.684 | ✅ 확인(비평가 0.664) |
| `cross↔star`(whirl↔shuri) | **0.636** | 0.566 | ✅ 확인(비평가 0.628) |
| `rock↔rockfall`(stone↔meteor) | **0.813** | 0.763 | ✅ 확인 |
| `spear↔arrow`(ice↔arrow) | **0.390** | 0.383 | ⛔ **기각** — 처음부터 잘 갈려 있다 |

**그리고 등재문이 못 본 것이 본체였다.** 전 136쌍을 재니 상위 다섯이 전부 **다른 무리**였다:

```
rock↔fire   0.872   ball↔bottle 0.848   gem↔rockfall 0.831
rock↔rockfall 0.813 rock↔gem    0.800
```

뿌리는 «육각을 나눠 쓴다» 가 아니라 **«볼록하다»** 다 — `stone`·`boom`·`rico`·`bounce`·
`flask`·`meteor` 여섯이 0.72~0.87 로 한 무리인데, 볼록한 것끼리는 **각도를 맞추면 무엇이든
닮는다**(원 ↔ 육각 ↔ 마름모). 프리미티브를 «공유해서» 가 아니라 **볼록해서** 같아 보인 것이다.

### ③ 고친 것 — `rock` 에 오목을 판다

여섯의 이음매가 `rock` 이었다(상위 5쌍 중 **3쌍**에 낀다). 그 표는 주석이 «울퉁불퉁한 바위» 라고
적어 두고도 실제로는 최소÷최대 **0.690** 이라 거의 볼록한 육각이었다.

```
- const rr = [9.8,7.7,9.4,6.9,10.0,8.1];      (최소÷최대 0.690)
+ const rr = [10.4,4.6,9.6,6.3,10.2,4.4];     (최소÷최대 0.423)
```

**골짜기만 팠다 — 봉우리는 10.0 → 10.4(+4%)뿐이다.** 792 [E1] 이 본체 대각을 중앙값 ±25%
밴드로 묶고 있고, 돌은 `SHOT_SC` 주석이 «856 [B10c] 가 위를 막고 있다» 고 적어 둔 자리라
봉우리를 올리는 길은 애초에 막혀 있다. 오목이 들어오면 정렬을 아무리 맞춰도 볼록 덩어리의
그 부분이 남아 IoU 가 **구조적으로** 떨어진다.

| 쌍 | 수리 전 | 수리 후 |
|---|---|---|
| `rock↔fire` | 0.872 | **0.697** |
| `rock↔rockfall` | 0.813 | **0.719** |
| `rock↔gem` | 0.800 | **0.697** |
| 전 쌍 최악 | 0.872 (`rock↔fire`) | **0.848** (`ball↔bottle`) |

후광(같은 표 ×1.45)·하이라이트가 **같은 표를 읽으므로** 세 층이 함께 파인다 — 792 문법 유지.

### ④ 정직하게 — 1회차가 **안** 내린 것

`verify981` [B2] 의 «비평가 불만선 0.60 위 쌍 수» 는 **20 → 20 으로 그대로다.**
rock 의 세 쌍은 0.87/0.81/0.80 → 0.70/0.72/0.70 으로 내려왔지만 **셋 다 아직 0.60 위**이고,
대신 `rock↔ball` 이 0.661 로 새로 목록에 들어왔다. 1회차가 내린 것은 **최악 쌍**([B1])이다.
래칫 두 개 중 하나만 움직였다는 사실을 자가 [R1] 에서 «되돌린 판의 0.6 위 쌍 20개» 로 같이 찍는다.

### ⑤ 게이트

* `verify981` **10/10 신설** — [A] 축 · [B] 래칫 2 · [C] 1회차가 닫은 자리 2 · [D] 되감기 금지 2 · [R] 되돌림 2
* `probe981` **3/3 신설** — 재현. 자와 재현이 **같은 재는 법**을 쓴다(`module.exports`, 402 «사본을 지운다»)
* 회귀 무수정 — `verify792` **27/27** · `verify710` **14/14** · **SMOKE PASS**
* 되돌림 시험 — 옛 `rr` 로 되돌린 사본에서 rock 쌍 **3개**가 문턱 0.75 위로 돌아온다
  (⚠ «셋» 을 못박지 않고 «≥2» 로 걸었다 — 다음 회차가 그중 하나를 다른 이유로 내렸을 때
  자가 «수리가 풀렸다» 고 거짓말하지 않게. 348 규칙)

### ⑥ 2회차에게 (인계)

1. **남은 무리는 `ball`·`bottle`·`gem`·`fire`·`rockfall` 다섯이다.** 목록 상위:
   `ball↔bottle` 0.848 · `gem↔rockfall` 0.831 · `fire↔rockfall` 0.790 · `gem↔fire` 0.787 ·
   `ball↔rockfall` 0.751 · `gem↔ball` 0.724.
2. **`rockfall` 이 이음매다** — 상위 6쌍 중 4쌍에 낀다. `rock` 과 같은 약(오목)을 쓰면
   **다시 `rock` 과 붙는다**(둘은 원래 같은 육각의 ×1.53 이었다) — 다른 축으로 갈라야 한다.
   1순위 후보는 **긴 쪽 비(aspect)** 다: 종횡비는 회전에 안 변하는 축이라 둥근 무리(1:1)와
   구조적으로 갈린다. ⚠ 다만 비등방 변형은 본체 대각을 키워 792 [E1]·`verify981` [D2] 를
   같이 흔든다 — **면적을 유지하며 늘리는**(x×k, y÷k) 형태로 잡고 [D2] 를 먼저 확인할 것.
3. **`arc↔moon` 0.693 은 «누가 바뀌어야 하는가» 부터 정해라.** `arc` 는 `rake`(호 3겹)·
   `wing`(호 + 뒷깃)의 기준형이라 건드리면 세 종이 같이 움직인다 ⇒ **`moon` 쪽**이 싸다.
   지금 둘 다 ~90° 를 감는 띠다. 달을 **더 많이 감게**(180° 이상, 뿔을 뾰족하게) 하면
   «달» 은 오히려 살고 90° 띠와는 감는 각으로 갈린다.
4. **`spear↔arrow` 는 목록에서 빼라** — 0.390 이라 손댈 자리가 아니다(등재문 오류).
5. 자를 돌리는 데 걸리는 시간: `probe981` ≈ 1분 · `verify981` ≈ 2분(되돌림 판을 한 번 더 잰다).

---

## 2회차 (2026-09-06, sess-0858-4185 루틴 워커 D)

**제품 `index.html` 3자리**(`meteor`·`rico`·`flask` 실루엣) · 자 1벌 갱신(`verify981` 10 → **17항**) ·
남의 자 **이관 3건**(`verify982` [R1] · `verify856` [B10c]·[B10d]) · 레이아웃(HUD·팝업) **Δ0px** ·
밸런스·판정 **0줄** · SMOKE PASS.

| | 최악 쌍 | 0.60 위 쌍 |
|---|---|---|
| 1회차 마감 | 0.848 `ball↔bottle` | 20 |
| **착수 시점(HEAD)** | 0.848 | **24** ← 982 1회차가 올렸다 |
| **2회차 마감** | **0.725** `star↔cross` | **18** |

### ① 착수하자마자 [B2] 가 빨갰다 — 내 탓이 아님을 대조로 못박았다(338·344)

`node tools/verify981.js` 를 **아무것도 안 고친 HEAD 에서** 돌리니 `[B2] 24개 ≤ 21` 로 **FAIL** 이었다.
사이에 들어온 것은 `fc4a623 done(982)` 하나다 — 982 1회차가 성긴 실루엣 넷(`bmr`·`wing`·`arrow`·`cross`)을
`SHOT_FILL` 로 **부풀렸고**, 부푼 실루엣끼리 서로 닮아 0.60 위 쌍이 **20 → 24** 가 됐다
(새로 들어온 쌍: `moon↔bmr` 0.714 · `star↔cross` 0.636 → **0.725** · `arc↔bmr` 0.643 · `cross↔gem` 0.628 …).

대조는 «수리 전 트리 실행» 으로 했다 — `git show 6559e7d:index.html`(981 1회차 마감판)을 저장소 루트에
꺼내 같은 자로 재니 **정확히 20쌍**이었다.

⚑ **이것이 이 축의 구조적 성질이다: 982(채움을 올린다)와 981(서로 안 닮게 한다)은 서로를 밀친다.**
살이 붙을수록 실루엣은 서로 닮는다. 두 작업이 같은 `shotBody` 를 만지는 한 **한쪽의 초록이 다른 쪽을
빨갛게 만들 수 있다** — 다음 워커는 회귀를 돌리기 전에 **상대 번호의 마지막 커밋부터** 보라.

### ② 재기 전에 **탐색**을 세웠다 (이 회차의 본체)

1회차가 `rock` 에 오목을 파서 0.872 → 0.697 을 얻었지만 **왜** 그것이 들었는지는 «오목» 이라는 말뿐이었다.
그래서 이번에는 손으로 형상을 고쳐 보는 대신, **후보 실루엣을 오프라인에서 채점**했다:
17종 마스크를 한 번만 브라우저에서 떠서(`probe981.measure`) 정규화 점집합으로 저장하고,
합성 후보(방사 변조 `r(θ)=S(1+a·cos kθ)` · 고리 · 초승달 · 갈퀴 · 두 마디 · L자 100여 개)를 같은
자로 **나머지 16종 전부와** 겨뤘다. 브라우저를 안 타므로 후보 하나에 1~2초다.

그 표가 답을 한 줄로 줬다:

```
rad k3~k6 a0   (=볼록 다각형)   최악 0.994 ↔ fire      ← 모양이 무엇이든
rad k6 a0.5 e1.3                최악 0.593 ↔ gem
rad k5 a0.5 e1                  최악 0.589 ↔ fire
ring t0.7 (구멍)                최악 0.431 ↔ swirl     ← 이 자에서 가장 센 축
```

⚑⚑ **«볼록한 것은 무엇이든 화구와 같은 그림이다»(0.99).** 육각이든 마름모든 사각이든 상관없다 —
회전·미러·크기를 지우고 나면 볼록 덩어리는 서로 0.8~0.99 로 겹친다. 1회차가 «뿌리는 프리미티브 공유가
아니라 볼록함» 이라고 적은 것을 이 표가 **정량으로** 확인했고, 동시에 **처방까지** 줬다: 이 무리에서
빠져나가는 길은 «다른 볼록 형상» 이 아니라 **오목(a ≥ 0.4)** 하나뿐이다. 그리고 이 자의 바닥은
**0.48~0.55** 다 — 목표선 0.60 은 닿을 수 있지만 **0 은 없다**(17종이 한 세트인 이상).

### ③ 고친 세 자리

| 종 | 수리 전 최악 | 수리 후 최악 | 무엇을 바꿨나 |
|---|---|---|---|
| `rico`(gem · 반사탄) | **0.787** ↔ fire | **0.618** ↔ spear | 마름모(볼록 4각) → **뿔 다섯 결정** `k5 a0.5 e1.3 S10.0` |
| `meteor`(rockfall · 운석) | **0.831** ↔ gem | **0.649** ↔ fire | 육각 + 뭉툭한 쐐기 → **깎인 돌** `k6 a0.5 e1.3 S11.7` + 꼬리를 문턱 아래로 |
| `flask`(bottle · 화염병) | **0.848** ↔ ball | **0.697** ↔ ball | 목을 **길게**(주둥이 −12 → −16.5) · 심지 불을 작게(4.5 → 3.4) |

`k` 는 **서로 달라야 한다**(자가 [C4a] 로 못박는다): 4 는 표창(`shuri`)의 대칭이고, 둘이 같은 k 를 쓰면
그 둘이 서로 붙는다. 그래서 결정 5 · 운석 6 이다.

⚑ **병에서는 «목의 굵기» 가 아니라 «목의 길이» 가 축이었다.** 파라미터 스윕(오프라인 · 몸통·목·불을
합성해 실제 `ball` 마스크와 겨룸)이 갈랐다 — 목을 ±3.2 → ±2.2 로 **좁히면** 0.787, 같은 폭에서
**길이만** 늘리면 0.686 이다(폭 6칸 = 0.06 · 길이 3칸 = 0.10). 좁히는 길은 게다가 `verify856` [B11]
(«③층이 본체 마루를 따라간다»)을 이 종에서만 빨갛게 했다(±2.2 에서 덮임 **0.669** · ±2.9 에서 0.678 ·
문턱 0.75). ⇒ **넓고 긴 목**이 두 자를 같이 지키는 자리다(덮임 0.852).

### ④ ⚑⚑ 이 회차의 가장 큰 교훈 — **자가 재는 것은 «그린 형상» 이 아니라 «합성 결과» 다**

운석을 갈퀴로 파도 값이 0.869 → 0.829 밖에 안 내려갔다. 그린 형상은 주축비 1.49 · 채움 0.455 인데
**마스크는 1.248 · 0.600** 이었다 — 두 반투명 층이 겹쳐 오목을 도로 메우고 있었다:

* 불꼬리 쐐기 α **.50** + 바위 뒤 불빛 원 α **.50** ⇒ 겹치는 데서 `1 − .5·.5 =` **.75** ≥ 본체 문턱 .55

즉 **α 는 겹치면 더해진다**(`1−(1−a)(1−b)`). 문턱 아래 층 둘이 겹치면 문턱 위 층 하나가 된다.
⇒ 뒤 불빛 원을 **걷어내고**(그 일은 792 5회차의 «실루엣에서 구운 후광 링» 이 이미 한다 — 그쪽은
깎인 자리를 **따라간다**) 쐐기를 α .30 으로 내렸다. 그 순간 값이 0.829 → 0.649 로 떨어졌다.
자는 [C4c] 로 그 선언을 지킨다.

### ⑤ ⚠ 크기를 건드리면 **밴드의 중앙값**이 움직인다 (하마터면 두 자를 빨갛게 둘 뻔했다)

결정을 `S 10.5`(대각 147.3px)로 잡았더니 이 종이 17종 대각의 **9번째 = 중앙값 자신**이 되어
792 [E1] 밴드가 통째로 위로 밀렸고(145 → 147.3 · 하한 108.8 → **110.5**), **획 한 줄 안 바꾼**
`stone` 108.9 · `shuri` 110.3 이 밖으로 떨어져 `verify792` [E1] 과 `verify982` [C1] 이 **둘 다** 빨개졌다.
`S 10.0`(140.3px)이면 9번째가 다시 `boomer` 145 라 밴드가 HEAD 와 **글자 그대로 같다**.
⚠ **이 밴드의 하한은 `stone` 이 0.15px 차이로 지키고 있다**(108.9 ↔ 108.75) — HEAD 부터 그랬다.
크기를 만지는 회차는 **중앙값이 움직이는지부터** 재라.

### ⑥ 이관 3건 (전부 «옆 회차가 내 축을 움직였다» 꼴 — 348)

* `verify982` **[R1]** 4 → **3** — 되돌림 하한이 «그 사본의 중앙값 × .75» 라, 981 이 꽉 찬 셋을 깎자
  (0.532 → 0.465 · 0.641 → 0.501 · 0.645 → 0.593) 하한이 0.399 → 0.360 으로 내려가 `whirl` 이 그 아래로
  안 떨어지게 됐다. **982 의 수리가 풀린 것이 아니다** — [B1]·[B3] 은 초록이고 스프레드는 오히려
  2.81 → **1.77** 로 더 좁다(981 이 꽉 찬 쪽을 깎았으므로).
* `verify856` **[B10c]·[B10d]** 머릿수 3 → **2** — «덩어리 무리» 를 가르는 것은 **제품의 규칙**
  (`K·본체폭 ≥ 0.8·상한`)이라, 병의 본체 폭이 ±11 → ±10.5 가 되자 그 종이 규칙상 **획 무리로 옮겨 갔다**.
  옮겨 간 종은 그 자리에서 [B10a] 비율 밴드가 그대로 재므로 **축이 빠진 것이 아니다**. 재는 값은 무수정.
* `verify981` **[R1]** «2쌍» → «1쌍 + 최악이 0.10 이상 뛴다» — 1회차의 셋은 «되돌린 rock 이 볼록 무리와
  붙는다» 는 뜻이었는데 2회차가 그 무리에서 둘을 빼내 갔다(0.872 하나만 남는다). 머릿수 대신 **폭**을
  물으면 무리에 몇이 남았든 «오목이 일하고 있다» 를 그대로 찍는다(실측 0.697 → 0.872 · +0.175).

### ⑦ 게이트

* `verify981` **17/17**(10항 → 17항) — [C3] 세 자리 신설 · [C4] 선언 3항 신설 · **[R2] 되돌림 신설**
* **[R2] 가 이 회차의 주장 전체를 한 줄로 찍는다** — 두 종의 변조 `a` 를 **0 으로만** 바꾼 사본
  (형상·크기·각도 그대로 = 볼록으로 되돌린 판)에서 `gem↔rockfall` **0.963** · `fire↔rockfall` 0.874 ·
  `gem↔fire` 0.864 로 **셋이 0.80 위로 되돌아온다**.
* 회귀 — `verify792` **27/27** · `verify982` **14/14** · `verify710` **14/14** · `probe981` 3/3 · **SMOKE PASS**
* ⚠ `verify856` 은 **19/21** 인데 남은 [B14]·[B15] 는 **HEAD 에서도 같은 둘**이다(내 트리를 치우고
  `git checkout -- index.html` 한 판에서 직접 확인 — 19/21 · 같은 두 항 · 같은 수 1.078·1칸).
  `lance`·`arrow` 폭 축이라 889 계열이고 이 회차와 무관하다. 내가 넣었다 뺀 [B10c]·[B11] 은 초록으로 되돌렸다.

### ⑧ 3회차에게 (인계)

1. **과녁은 `star↔cross` 0.725 다**(표창 ↔ 주변참격). 둘 다 2회차가 **안 건드린** 4각 대칭이고,
   `cross` 는 **982 가 부풀린 넷 중 하나**라 0.636 → 0.725 로 올라온 자리다 — 982 워커와 겹칠 수 있으니
   `docs/claims/982.lock` 을 먼저 보라. 표창(`shuri`)은 «4각 별» 이 정체성이므로 **`whirl` 쪽**이 싸다.
2. 그다음 줄: `moon↔bmr` 0.714(초승달 ↔ 부메랑 — 둘 다 굽은 띠) · `rock↔fire` 0.697 ·
   `arc↔moon` 0.693 · `rock↔ball` 0.661.
3. **탐색 자를 그대로 쓸 수 있다** — 회차 기록 ②의 표를 낸 스크립트는 `probe981` 의
   `measure`·`normalize`·`alignedIoU` 를 그대로 `require` 한 40줄이다(마스크를 JSON 으로 한 번만 뜬다).
   새 종을 고칠 때는 **손으로 고치기 전에** 후보를 그 자로 먼저 채점하라 — 2회차는 그것을 안 하고
   운석에 «비대칭 눈물방울» 을 두 번 그렸다가 **값이 오히려 0.869 로 올라갔다**(비대칭은 이 자에서
   거의 안 먹는다 · 먹는 것은 **오목**과 **구멍**뿐이다).
4. **바닥은 0.48~0.55 다**(②의 표). 0.60 전부는 «남은 몇 종만 고치면» 되는 것이 아니라 **17종을 다
   오목·구멍 쪽으로 옮겨야** 닿는다. 회차가 모자라면 «최악부터 한 종씩» 이 옳다.

---

## 3회차 (2026-09-06, sess-0858-4185 루틴 워커 D · 2회차와 한 세션)

**제품 `index.html` 1자리**(`whirl` 실루엣) · 자 `verify981` **17 → 20항** · 회귀 무수정 · SMOKE PASS.

| | 최악 쌍 | 0.60 위 쌍 |
|---|---|---|
| 2회차 마감 | 0.725 `star↔cross` | 18 |
| **3회차 마감** | **0.714** `moon↔bmr` | **16** |

### ① X 교차 → **참격 고리**

2회차가 남긴 과녁이 `star↔cross` **0.725** 였다. 둘은 **4각 대칭 · 채움 0.44~0.45 의 납작한 별**이라
회전·미러를 지우면 같은 그림이었고, 표창(`shuri`)은 «4각 별» 이 정체성이라 못 건드린다 ⇒ 주변 참격 쪽이 움직였다.

후보 탐색(2회차 §2 의 그 자)에서 **고리가 이 자에서 가장 센 축**이었다 — 토막 셋·틈 0.35rad 에서
나머지 16종과의 최악이 0.50 대다. 뜻도 이쪽이 맞다: 이 스킬은 «주변 참격» 이고, 비평이 1·3회차에
«정지한 십자로 읽힌다» 고 두 번 지적한 것이 바로 그 회전감이다.

`C_R 11.5 · 획 5.5 + 2·pad · 토막 3 · 틈 0.35` ⇒ `star↔cross` **0.725 → 0.406** · 이 종의 최악은
`cross↔fire` **0.642** 다. 982 의 `pad` 는 그대로 지난다(형상은 반지름·토막·틈이고 pad 는 획 폭이다).
크기는 **대각 예산**이 정했다 — 고리의 bbox 대각은 `2·(R + 획/2)·√2` 라 R 을 키우면 금세 792 [E1]
상한을 넘는다(R 11.5 에서 172.5px · 밴드 108.8~181.3).

### ② ⚑⚑ **자가 내 설명을 기각했다** — 축은 «대칭 수» 가 아니라 «구멍» 이다

되돌림 시험 [R3] 을 처음에 «토막을 넷으로 되돌리면 표창과 다시 붙는다» 로 세웠다. 돌려 보니
`star↔cross` 가 0.406 → **0.414**(+0.008)로 **꿈쩍도 안 했다** — 내가 적은 이유(«4각 대칭을 깼다»)가
**틀린 것이다.** 반대쪽을 재니 답이 나왔다: 획을 굵혀 **구멍을 메운** 사본에서 `cross↔fire` 가
**0.995** 로 뛴다(메운 고리 = 볼록 원반 = 2회차 ②의 «볼록하면 무엇이든 화구와 0.99»).

⇒ 문턱을 넓혀 초록으로 되돌리는 대신 **묻는 것을 고쳤다**(979-② · 894 와 같은 처방):
* [C4d]«토막이 셋» 선언 항 **폐기** — 값이 그것을 지탱하지 않는다(헛초록이 될 자리였다).
* **[C5] 신설(측정)** — `cross` 의 본체가 **감싼 배경**(구멍) 화소 ≥ 300. 실측 **2849화소**이고
  **구멍을 가진 다른 종은 0개**다(그래서 이 축이 센 것이다). 테두리에서 배경을 물채우고 남는 배경을 센다.
* **[R3] 다시 세움** — 획 폭 한 값만 굵힌 사본에서 구멍 **0화소** · cross 최악 **0.995 > 0.66**.
* 제품 주석도 고쳤다 — «토막 수는 회전감(뜻)의 몫이고, 분간을 지탱하는 것은 구멍 하나다. 메우지 마라».

### ③ 게이트

`verify981` **20/20** · 회귀 `verify792` 27/27 · `verify982` 14/14 · `verify710` 14/14 ·
`verify193` 79/79 · `verify504` 15/15 · `verify752` 17/17 · `verify86` 38/38 · `verify99` PASS ·
`verify109` 65/65 · `verify484` 16/16 · **SMOKE PASS**.
⚠ `verify856` 19/21 — 남은 [B14]·[B15] 는 **HEAD 에서도 같은 둘**이다(889 계열 · 2회차 §⑦ 에 대조 기록).

### ④ 4회차에게 (인계)

1. **과녁은 `moon↔bmr` 0.714**(초승달 ↔ 부메랑). 둘 다 **굽은 띠**라 2·3회차가 쓴 두 축(오목·구멍)이
   그대로는 안 듣는다 — 띠의 **감는 각**(초승달을 180° 이상 감아 뿔을 세운다)이 1회차 인계문의 처방이고,
   그 자리는 아직 손 안 댔다. 그다음: `ball↔bottle` 0.697 · `rock↔fire` 0.697 · `arc↔moon` 0.693 ·
   `spear↔bottle` 0.685.
2. **구멍은 한 번만 쓸 수 있는 축이 아니다** — [C5] 가 «구멍을 가진 종 0개» 를 같이 찍으므로,
   두 번째 종에 구멍을 주면 그 둘이 서로 붙는지부터 탐색으로 확인하라(고리끼리는 닮는다).
3. 자를 돌리는 시간: `probe981` ≈ 1분 · `verify981` ≈ **4분**(되돌림 판을 **셋** 더 잰다 — [R1]·[R2]·[R3]).
