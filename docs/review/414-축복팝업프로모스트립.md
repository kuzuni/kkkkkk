# 414 — 34 축복 팝업의 초록 프로모 스트립이 1600 에서 앱 탭바를 164px 덮는다

> 세션 `sess-1333-27209` (루틴 워커 B) · 2026-08-29 · **1회차**
> 등재: 2026-08-29, `sess-1032-24096` 워커 B — 351 7회차, `probe351` D7 이 `paints()` 보강으로 처음 본 자리
> 자: `node tools/probe414.js` (재현) · `node tools/verify414.js` (게이트) · `node tools/cap414.js` (짝 캡처)

## 0. 한 줄

**등재문의 값(164px)은 그대로 재현됐고, 처방(«164 를 어딘가에서 뺀다»)은 기각했다.**
제품 `index.html` **0줄** · 레이아웃 **Δ0px** · 밸런스·판정·세이브 0줄.

---

## 1. 재현 (338 규칙 — 처방 전에 재현한다)

`node tools/probe414.js` — 세 프레임에서 팝업을 실제로 열고 잰다. **23/23.**

| frameH | shortf | 잉크 끝 | 탭바 상변 | `.bls` | 스트립 | `.bls-x` | 탭바 덮임 |
|---|---|---|---|---|---|---|---|
| 2280 | false | 142 | 2100 | 345..1502 | 1523..1772 | 1793..1931 | **0** |
| 1920 | false | 142 | 1740 | 165..1322 | 1343..1592 | 1613..1751 | **✕ 11** |
| 1600 | true | 142 | 1420 | 157..1314 | **1335..1584** | 134..272(코너) | **스트립 164** |

- 등재문의 «**164px · 가로 952**» 가 그대로 나왔다.
- 1600 의 여백은 **위(잉크 142 → 블록) 15 · 아래(블록 → 프레임) 16** 이다.

---

## 2. 처방을 기각한 근거 다섯

### ⓐ 기하가 없다 — 띠가 블록보다 **149px 짧다**

쓸 수 있는 띠(HUD 잉크 끝 **142** ↔ 탭바 상변 **1420**) = **1278**
흐름 블록(`.bls` 1157 + 간격 21 + 스트립 249) = **1427**

⇒ 164 를 «어딘가에서 빼는» 유일한 길은 **레퍼런스 절대값을 12.9% 깎는 것**이다.
등재문은 그것을 «주인 재지시 ② 를 쓰는 자리» 라고 적었지만, 재지시 ② 의 이탈 허용 범위는
**«1600 에서 보이게 하는 데 필요한 만큼»** 이다(ROUTINE 351 재지시 ②). 그리고 —

### ⓑ 1600 에서 **이미 다 보인다** — 재지시 ② 의 조건이 성립하지 않는다

| 자 | 2280 | 1920 | 1600 |
|---|---|---|---|
| 프레임 밖 자식 | 0 | 0 | **0** |
| `#blsw` 스크롤(scrollH − clientH) | 0 | 0 | **0** |
| 겹침 띠에서 탭바를 숨겼을 때 바뀌는 픽셀(스트립이 덮은 안쪽) | — | — | **0 / 156,128** |

마지막 줄이 **«가려지는 글자 0»** 의 픽셀 증거다(등재문도 같은 값을 적어 뒀고, 이 세션이 다시 쟀다).
⚠ 코너 포함 전체로 세면 **202px** 이 바뀌는데, 그건 `.bls-promo{border-radius:20px}` 의
**아래 두 코너에서 탭바가 그대로 비치는 자리**다 — 덮인 자리가 아니다. 그래서 자는 두 값을 따로 센다.

### ⓒ 조작 축은 **«판정 불가»** — 406 확정 규약

406 이 «덮였나» 를 버리고 **«닿나»**(`elementFromPoint`)로 축을 갈면서, **2280 에서 이미 안 닿는 것은
판정에서 뺀다**고 못박았다(LESSONS 406-①). 탭 5칸을 그 자로 재면:

| | hero | grow | adv | box | shop |
|---|---|---|---|---|---|
| 2280 | `blsw`(딤) | `blsw` | `blsw` | `blsw` | `blsw` |
| 1600 | `bls-promo` | `ic` | `bls-promo` | `blsAll` | `bls-promo` |

**두 해상도 모두 탭이 안 닿는다.** 팝업이 열린 채 탭을 실제로 클릭해도 세 프레임 전부에서
**탭이 안 켜진다**(2280·1920 은 그 클릭을 **딤이 먹어 팝업이 닫힌다** = 나갈 길, 1600 은 아무 일 없음).

⚑ **1600 에서만 생기는 차이 한 가지는 기록해 둔다** — «box» 탭 중심 자리가 1600 에서는
프로모의 **[이동] 버튼(`#blsAll` → 상점 패스 탭)** 이 된다. 조작을 **잃는** 것은 아니지만
(그 자리는 2280 에서도 탭이 아니다) **같은 화면 좌표가 «닫기» 에서 «이동» 으로 바뀐다.**
나갈 길은 그대로 둘 다 산다 — 딤(스트립 바깥)·✕ 모두 1600 에서 실제로 닫는 것을 자가 확인한다.

### ⓓ 덮임은 **1600 이 새로 만든 것이 아니다**

`.bls-x` 는 **1920**(shortf 문턱 1842 위 — 짧은 프레임이 아니다)에서도 탭바를 **11px** 덮는다.
덮임 자체가 감점이라면 그 프레임이 먼저 빨개졌어야 한다. 391 이 쓴 ⓔ 검산과 같은 꼴이다.

### ⓔ 배치는 **이미 최적이다** — 391 이 세운 자로 재도 그렇다

391 은 같은 계열(19 프로필)에서 «덮임» 을 결함에서 빼고 실재하는 결함을 **여백의 비대칭**으로 잡아,
패널을 «쓸 수 있는 띠의 한가운데» 에 놓는 것으로 닫았다. 34 를 그 자로 재면 **위 15 ↔ 아래 16** 이다.
- 위로 더 밀면 HUD 잉크(142)에 붙는다 — 391 이 «8px 은 여백이 아니라 잘림으로 읽힌다» 고 적은 그 자리.
- 아래로 더 밀면 프레임 밖으로 나간다.

⇒ **351 1회차가 이미 회수를 다 했다.** 자가 그것을 §R 로 못박는다(§4).

---

## 3. 그래서 무엇을 했나 — «규약» 과 «대가»

406-④: **«~해도 된다» 만 적으면 그건 감점을 없애는 면죄부다.** 그래서 `verify414` 는 규약과 대가를 짝으로 세운다.

**규약(§2)** — 34 축복 팝업이 짧은 프레임에서 탭바를 덮는 것은 **설계다.**
**대가(§3)** — 그 대신 아래가 전부 참이어야 한다. 하나라도 무너지면 빨갛다.

| 항 | 무엇을 받는가 |
|---|---|
| [3-a] | 세 프레임 모두 `#blsw` 스크롤 **0** — «스크롤 안 해도 보이게»(351 재지시 ①) |
| [3-b] | 프레임 밖 자식 0 · 콘솔 에러 0 |
| [3-c] | 겹침 띠 안쪽에서 탭바를 숨겨도 바뀌는 픽셀 **0** = 가려지는 글자 0 |
| [3-d] | 나갈 길 둘(딤·✕)이 1600 에서 실제로 닫는다 |
| [3-e] | 블록이 쓸 수 있는 띠(142..1600) **한가운데** — \|위 − 아래\| ≤ 2 |
| [3-f] | 위가 HUD 잉크를 안 물고 아래가 프레임을 안 넘는다 |

그리고 **음성항 [2-a]** — **2280 에서는 덮임이 0 이어야 한다.**
이게 없으면 §2 는 «탭바는 언제 덮여도 된다» 는 게이트가 된다(406 [7-f] 와 같은 짝).

---

## 4. §R 되돌림 시험 — «이미 회수했다» 가 공허하지 않음의 증명

351 1회차가 넣은 회수는 둘이다. 각각을 **뗀 사본**에서 §3 이 실제로 무너진다.

| 항 | 뗀 것 | 1600 에서 무슨 일이 나는가 |
|---|---|---|
| [R-a] | 아래 가드 `clamp(16px, …, 146px)` → 고정 `146px` | `scrollH 1699 > 1600` — **스크롤이 생긴다** |
| [R-b] | `#app.shortf .bls-x{position:absolute;…}` | ✕ 가 흐름으로 돌아와(1574..1712) **112px 넘친다** |
| [R-c] | 두 사본 다 | **2280 은 Δ0** (`.bls` top 345 그대로) ⇒ 회수는 짧은 프레임 전용 |

---

## 4-2. ✕ 자리도 «이미 슬롯 한가운데» 다 (비평 1회차가 데려온 자리)

1회차 비평가 **CK·CL 이 2인 독립으로** 짚은 유일한 «유령이 아닌» 자리가 ✕ 였다 —
2280 에서는 블록 아래 한가운데(1793..1931 · x471..609)인데 1600 에서는
**팝업 우상단 코너(134..272 · x897..1035)** 로 옮겨 가고(351 1회차의 148px 회수),
그 새 자리가 **살아 있는 상단 재화 바 `#top`(하변 104) 바로 아래 30px** 이라는 것이다.

재 보니 **그 자리도 움직일 데가 없다**:

| | 값 |
|---|---|
| 위 벽 — 상단 재화 바 `#top` 하변 | **104** (그 위로 올리면 살아 있는 HUD 를 문다 · 게이트 가드는 126) |
| 아래 벽 — 본문 안내문 `.bls-note` 상변 | **298** (그 아래로 내리면 **글자를 덮는다**) |
| 슬롯 | 194 |
| ✕ 높이 | 138 |
| **남는 것** | **56** — 현재 배분 **위 30 · 아래 26** |

⇒ ✕ 는 **자기 슬롯의 한가운데**에 있다. `verify414` **[3-g]·[3-h]** 가 그것을 잰다.
(1회차에 «아래로 42px 내리면 HUD 와 72px 벌어진다» 를 검토했다가 **`.bls-note` 글자를 덮는 것**이
실측으로 나와 접었다 — 자리를 옮기는 안은 «가려지는 글자 0»(§3 [3-c])을 스스로 깬다.)

## 4-3. 비평 회차 — 1회차 넷 중 셋이 «앵커 유령» 이었다 (335 와 같은 꼴)

**1회차(CK·CL, 같은 짝 캡처 · 앵커 표 없이)**: CK **8/5/7/4** · CL **8/6/10/6**.
두 사람의 1순위가 갈렸고(CK «블록을 70px 더 올려 86/86» · CL «5~7% 축소 또는 하단 세이프에어리어»),
**둘 다 이 저장소의 프레임 모델을 모르는 데서 나왔다**:

| 1회차 지적 | 왜 유령인가 |
|---|---|
| «주 CTA 가 시스템 제스처 바에 물린다»(CK ③ · CL ④a) | 안전영역은 **프레임 바깥**에서 `#wrap{padding:env(safe-area-inset-*)}` 가 흡수한다(index.html 207). 캡처의 y1600 은 콘텐츠 하변이다. |
| «173px 여유를 157/16 으로 잘못 나눴다 — 86/86 이면 된다»(CK 1순위) | 위 **142 는 자유 공간이 아니라 HUD 잉크 가드**다. 쓸 수 있는 띠는 1458 이고 블록이 1427 이라 **여유는 31** — 지금이 15/16 으로 이미 한가운데다. |
| «5~7% 축소하면 해소된다»(CL 판단) | 지시서 [2] — **전체를 비율로 축소하는 것은 금지**. 1157·249 는 레퍼런스 절대값이다. |
| «배너가 탭바·미션 패널·300 젬을 덮어 9개 문자열이 안 읽힌다»(CK ②b·②c·②d·③) | 전부 **딤 뒤 배경**이고 **2280 에서도 안 눌린다**(406 확정 규약). 실측으로 탭 5칸이 두 해상도 모두 안 닿는다. |

⇒ 335 의 처방(«비평가에게 앵커 표를 프롬프트로 줘라»)대로 **2회차는 위 다섯 전제를 적어 주고 재채점**했다.

## 4-4. 2회차 — 앵커를 주자 셋이 8~9 로 올라오고 **한 축만 남았다**

**2회차(CM·CN, 같은 짝 캡처 · 앵커 표 5개를 프롬프트로 줌)**

| | ①잘림 | ②겹침 | ③가독 | ④조작성 | 최저 |
|---|---|---|---|---|---|
| CM | 8 | 7 | 9 | **5** | 5 |
| CN | 9 | 7 | 9 | **6** | 6 |
| (1회차 CK) | 8 | 5 | 7 | 4 | 4 |
| (1회차 CL) | 8 | 6 | 10 | 6 | 6 |

①②③ 은 **넷 다 «못 읽게 된 글자 0자»** 로 일치했고, 앵커를 주자 ①이 8~9 · ③이 9 로 올라왔다.
**남은 것은 ④ 하나이고, 두 사람이 독립으로 같은 것을 1순위로 짚었다** —
«**나가는 길(✕)이 1600 에서 화면 우상단으로 옮겨 가고, 2280 에서 닫기였던 하단 중앙 픽셀은
1600 에서 아무 반응도 없는 자리가 된다**»(CM ②·CN ①).

### 2회차 반론 둘을 «닿나» 로 되물었다 (406-① 의 방법 그대로) — 둘 다 기각

- **CN**: «상단 재화 바는 딤이 안 걸린 밝은 상태 = 살아 있는 터치 영역이라, ✕ 를 위로 빗맞히면 재화 바를 친다.»
  → **`#top` 은 `z-index:6` 이고 `#blsw` 는 `41`** 이다. `#top` 안의 **22개 요소 전부**가
  **두 해상도 모두** `elementFromPoint` 에서 `blsw` 에 먹힌다. 빗맞은 탭은 재화 바가 아니라 **딤에 맞아 닫힌다.**
  게이트 **[2-e]** 가 그것을 잰다.
- **CN**: «2280 에서 ✕ 가 있던 자리를 1600 에서는 [이동] CTA 가 차지한다.»
  → **아니다.** [이동](`#blsAll`)은 x689..956 이고 2280 ✕ 의 대역은 **x471..609** 다.
  그 세로선의 1600 전 구간은 `bls-body`·`vl`·`blsBonus`·`bls-promo` 로 **조작 요소가 하나도 없고**,
  블록 아래(y1590)는 **딤 = 닫힘**이다. 게이트 **[2-f]** 가 그것을 잰다.
  ⇒ **오조작의 결과는 «원치 않는 화면 전환» 이 아니라 «아무 일도 안 남»** 이다(CM 이 그렇게 봤다).

### 남은 한 줄은 이 작업 단위에서 못 닫는다 → **420 으로 등재**

두 사람 다 «✕ 를 하단으로 되돌리는 것은 **불가능**» 이라고 스스로 적었다 —
자유분 **31px** 에 버튼 138 + 간격 21 = **159px** 이 필요하다(5배 초과).
CM 이 낸 유일한 0px 안은 «`.bls-promo` 의 `.gb` 바깥 영역을 딤처럼 «누르면 닫힘» 으로 통과시킨다» 인데,
그건 **레이아웃이 아니라 «다이얼로그 안을 누르면 닫히는가» 라는 조작 설계 결정**이고 **2280 도 같이 바뀐다.**
406 이 413 을 가른 이유와 같다(«가독성 폴리시가 아니라 조작 설계 결정이다») ⇒ **423** 으로 넘긴다.

⚑ CM 의 전제 반론 하나는 **맞고, 기록해 둔다** — «띠 = 142..1600(1458)» 은 저장소의 규칙이 아니라
**이 팝업만의 예외**다. 390 이 못박은 진짜 띠는 **142..1420(1278)** 이고, `#blsw` 는 공용 `#modal` 을
안 지나므로(318·293 «경로가 한 칸 짧다») 자기 clamp 로 하단 가드를 16 까지 내려 그 띠 밖으로 나간다.
§2 [2-d] 가 재는 «149px 부족» 이 정확히 그 예외의 크기다.

---

## 5. 게이트·회귀

- **신설** `tools/probe414.js` **23/23** · `tools/verify414.js` **30/30** · `tools/cap414.js`(짝 캡처)
- 회귀 **무수정**: `verify325` 69/69 · `verify394` 20/20 · `verify391` 42/42 · `verify351` **82/82** ·
  `probe390` 침범 0건 · **`node tools/smoke.js` SMOKE PASS**
- 제품 `index.html` **0줄**이므로 회귀는 전부 «수정 없음» 이다.

## 6. 곁다리 등재 2건

- **423 (조작 설계 결정)** — 2회차 비평가 **CM·CN 2인 독립 일치**: 1600 에서 «나가는 길» 이 우상단
  코너로만 남고, 2280 에서 닫기였던 하단 중앙 픽셀은 1600 에서 **아무 반응도 없는 자리**가 된다.
  ✕ 를 되돌리는 것은 기하가 없어 불가능(31px 자유분 vs 159px 필요)하고, 유일한 0px 안
  («`.bls-promo` 의 `.gb` 바깥을 누르면 닫힘»)은 **2280 도 같이 바뀌는 조작 설계 결정**이라 넘겼다(406→413 선례).
- **424 (게이트 자기모순)** — `probe351` **D7** 은 이 자리를 계속 낸다(`side:bless 164`).
  **406 이 E1 에 준 규약(«2280 에서 이미 안 닿으면 판정 불가»)이 D7 에는 안 갔다** — 두 자가
  같은 자리를 반대로 말한다(373 과 같은 꼴). `probe351.js` 는 351 세션이 잡고 있어 **손대지 않았다**
  (391 이 같은 이유로 D7 3건을 남긴 선례). ⚑ **419 를 잡는 워커가 반드시 먼저 볼 것** — 419 의 17건이
  같은 자로 나온 값이다.

## 7. 남은 것 / 다음 세션이 볼 것

- ⚠ `.bls-x` 가 **1920 에서 탭바를 11px** 덮는다. 이 단위(1600 전용) 밖이라 안 고쳤고,
  §2 [2-b] 가 그 값을 «덮임은 1600 이 새로 만든 것이 아니다» 의 증거로 **계속 잰다**.
- ⚑ **`#blsw` 는 공용 `#modal` 을 안 지난다**(318·293 계열). 390 이 세운 `.shortf` 띠(142/180)를
  이 팝업에 그대로 붙이면 블록 1427 이 띠 1278 에 안 들어가 **스크롤이 생긴다** — 351 재지시 ①
  («스크롤 안 해도 보이게»)과 정면으로 부딪힌다. 붙이지 마라.
