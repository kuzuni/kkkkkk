# 141 — 22 퀘스트 수량 배지가 레퍼런스 대비 폭 −30% · 높이 −26%

> 등재: 2026-08-26, sess-0226-10824 워커 B (126 ②-2 하다 발견, 손대지 않고 등재만)
> 수행: 2026-08-26, **sess-0313-18715 워커 A · 1회차**
> 구간: 공용 `.ifr>.ifq`(104) · 22 `.qs-i` · 게이트 `tools/verify104.js`

---

## §0. 등재된 «선택지» 중 무엇을 골랐나 — ①

PROGRESS 141 이 남긴 세 갈래는 이랬다.

| | 선택지 | 판단 |
|---|---|---|
| ① | `.ifq` 를 화면별 배율 변수로 열어 **통일을 유지한 채 예외**를 준다 | **채택** |
| ② | 공용값을 올리고 나머지 5화면(05·12·53·69·70)을 각자 재측정 | 기각 — 게이트 5개가 통째로 움직인다. 지금 그 화면들은 ref 와 맞다 |
| ③ | 그대로 둔다 (22 는 계속 −30%) | 기각 — 비평가 I·J 가 «칩·배지 숫자열 −20~−45%» 를 ② 1순위로 **독립 지적**했다 |

①이 «예외를 만드는 것» 이 아니라 **104 가 이미 세워 둔 규칙에 그대로 들어맞는다**는 것이 채택 근거다.
104 는 프레임 토큰을 통일하면서 **아이콘 잉크 크기 `--if-ic` 는 화면별 입력으로 남겼고**, 그 이유를
자기 주석에 이렇게 적어 뒀다:

> ⚠ 예외 하나 — 아이콘 잉크 크기 `--if-ic` 는 화면별 입력으로 남긴다.
> ROUTINE [3] «아트 자리 규칙»: 이모지는 **레퍼런스 아트의 잉크 bbox** 를 정확히 차지해야
> 나중에 이미지로 갈아 끼울 때 안 튄다. 프레임(껍데기)은 통일 대상이지만 그 «안에 들어갈
> 아트» 는 화면별 측정값이다.

**숫자 배지도 «프레임 안에 들어가는 잉크»** 다. 껍데기(선 5 · 림 6 · 코너 .233)는 그대로 통일이고,
잉크 크기만 화면이 잰 값을 준다 — `--if-ic` 와 **완전히 같은 구조**다. 그래서 통일을 깨지 않는다.

넣은 것: `.ifr` 에 `--ifq-k`(기본 **.235** = 104 통일값 그대로) 신설 → `font-size:calc(var(--if-w) * var(--ifq-k))`.
**나머지 다섯 화면은 선언 한 줄도 안 바뀌었고 렌더도 한 픽셀 안 움직인다**(§4 회귀).

---

## §1. ★ 측정표에 적힌 «.364» 를 그대로 넣었으면 이번에도 틀렸다

등재 문구와 측정표 §5-2 는 목표 배율을 **.364** 로 적어 뒀다. 그 값의 출처는

```
font-size = 잉크 높이 27 ÷ 0.70     (0.70 = «숫자는 잉크 = 캡하이트» 가정)
          = 38.5  →  38.5 / 106 = .364
```

인데, **0.70 은 서체 교체 «전» 의 값**이다. 지금 서체(Jua)로 우리 화면에서 직접 재면:

| | fs | 흰 채움 높이 | 잉크비 |
|---|---|---|---|
| 우리 실측 | 24.91 | **20** | **0.803** |

→ ref 잉크 높이 27 을 내려면 `fs = 24.91 × 27/20 = ` **33.6** = 106 × **.317** 이다.
`.364` 를 넣었으면 잉크가 **31** 이 돼 −26% 가 **+15%** 로 부호만 바뀌었다.

이것은 126 §9-6 이 이미 경고한 함정과 **같은 함정**이다 — 거기는 한글 라벨의 `ink/0.93`,
여기는 숫자의 `ink/0.70`. **측정표의 «환산 상수»(잉크비)는 전부 옛 서체 것이라 다시 재기 전에는
쓰면 안 된다. 재는 것은 ref 가 아니라 «우리 렌더» 다** — ref bbox 는 이미 맞게 적혀 있다.

---

## §2. 폭은 `font-size` 로도 `scaleX` 로도 혼자 안 맞는다 — 자간이 빠져 있었다

fs 를 33.6 으로 올려도 폭이 안 맞는다. 두 손잡이가 필요한 이유는 **ref 숫자열이 우리보다
«글자가 넓은 것» 이 아니라 «글자 사이가 벌어져» 있기 때문**이다.

| | 2자리 `50` | 3자리 `100` | 자당(=한 자 늘 때 증가분) |
|---|---|---|---|
| ref | 46 | 67 | **21.0** |
| 우리 (fs 33.6, sx 1, ls 0) | 39.2 | 52.3 | **13.1** |

자당이 13.1 → 21.0 이어야 하는데 2자리 총폭은 39.2 → 46 이면 된다. 비율이 서로 다르다
(1.60 vs 1.17) — 그래서 **한 손잡이로는 두 표본을 동시에 못 맞춘다.**
단일 `scaleX` 최선값 1.28 로는 2자리 **+1.8%** / 3자리 **−1.7%** 로 갈린다.

`scaleX` s 와 `letter-spacing` L 을 연립으로 풀면

```
s(39.2 + L)  = 46          →  L = 8.8px (= .262em)
s(52.3 + 2L) = 67          →  s = 0.958
검산: 자당 = s(13.1 + 8.8) = 21.0 = ref ✔
```

즉 답은 «글자를 늘리는 것» 이 아니라 **«글자는 거의 그대로(×0.96) 두고 자간을 벌리는 것»** 이었다.
`text-indent` 는 53 `.bg53-c>b.ifq` 와 같은 이유로 같이 넣는다 — `letter-spacing` 은 **마지막 글자
뒤에도** 붙어서 가운데 정렬을 L/2 만큼 왼쪽으로 민다. `text-indent:L` 이 그것을 되돌린다(실측 §3).

---

## §3. ⚠ 1× 캡처로는 이 연립식을 못 푼다 (도구 교훈)

처음 두 회는 답이 **3px 씩 오락가락**했다. 원인은 식 자체다:

```
25·L = 67·A₂ − 46·A₃      →   dL/dA₂ = 67/25 = 2.68
```

**A₂(2자리 자연 잉크폭)의 ±1px 오차가 L 에서 ±2.7px 로 증폭**된다. 1× 캡처의 ±0.5px 양자화만으로도
답이 «s 1.15 / L 3.4» 와 «s 0.96 / L 8.8» 사이를 왔다 갔다 했다(둘 다 그 회차 측정에는 «정답» 이었다).

→ `tools/ink141.js` 를 **deviceScaleFactor 3** 으로 찍게 했다(±0.17px). 그러자 5행이 전부 같은 값으로
붙었고 답이 한 번에 수렴했다. **연립으로 두 손잡이를 푸는 자리는 1× 로 재지 마라.**

같이 겪은 두 번째 오염 — **B(대상 숨김) 캡처가 한 장이면 그 프레임만의 떨림이 통째로 «잉크» 가 된다.**
회차마다 «잉크 폭 106 / 84 / 82» 같은 값이 무작위 행에서 튀었다(106 = 프레임 폭 전체).
B 를 **두 장** 찍어 «둘 다에서 사라진 픽셀» 만 잉크로 세게 하니 사라졌다. (m126ink 의 N 두 장과 같은 처방을
B 쪽에도 해야 한다 — A 쪽만 막으면 반만 막은 것이다.)

---

## §4. 실측 결과 — 폭 −30% → **±1%**

`node tools/ink141.js` (1080×2280 · DSF 3 · cap22 와 같은 상태 · ref 문자열 `50`/`100` 으로 치환)

| 표본 | 이전 | **지금** | ref | Δw | Δh |
|---|---|---|---|---|---|
| 2자리 `50` | 32×20 | **46 × 27** | 46×27 | **0.0%** | **0.0%** |
| 3자리 `100` | 45×20 | **67.7 × 27.7** | 67×27 | **+1.0%** | **+2.5%** |

| 축 | 이전 | 지금 | ref |
|---|---|---|---|
| 가운데 정렬 (프레임 중심 대비) | 0.0 / −0.5 | **−1.0 / −0.2** | 0 |
| 프레임 하단선 걸침 (위 / 아래) | 18.5 / 1.5 | **21.8 / 5.2 · 22.2 / 5.5** | 21.5 / 5.5 |

세로도 같이 고쳤다 — fs 를 24.9 → 33.6 으로 올리면 줄상자(높이 .243×106 = 25.8) 안에서 베이스라인이
올라가 잉크가 1.3px 떠 있었다. 공용 `bottom`(−.093)은 **다른 다섯 화면 것이라 안 건드리고**
`.qs-i i.ifq` 에서만 −.1053 으로 덮었다.

**최종 선언 (22 만)**

```css
.qs-i{ … --ifq-k:.317}
.qs-i i.ifq{transform:scaleX(.958);letter-spacing:.262em;text-indent:.262em;
  bottom:calc(var(--if-w) * -.1053)}
```

### 회귀 — 나머지 다섯 화면은 안 움직였다

`--ifq-k` 기본값이 .235(104 통일값)라 **05·12·53·69·70 은 선언도 렌더도 무변경**이다.
이것을 눈으로가 아니라 게이트로 못 박았다 — `tools/verify104.js` ⑥ 을 화면별 기대값으로 바꾸면서
**«22 = .317 · 나머지 넷 = .235»** 를 명시적으로 검사하게 했다(스트로크 기대값도 `.16 × 배율` 로 따라간다).
즉 누가 나중에 다섯 화면 중 하나를 건드리면 게이트가 잡는다.

| 게이트 | 결과 |
|---|---|
| `node tools/verify104.js` | **68/68 PASS** |
| `node tools/verify22.js` | **65/65 PASS** |
| `node tools/smoke.js` | **SMOKE PASS** |

---

## §5. 곁가지 — `tools/cap22.js` 가 클라우드 워커에서 못 돌고 있었다

22 를 캡처하려다 발견했다. `cap22.js` 는 아직 옛 2줄(`require('playwright')` + `chromium.launch()`)이라
번들 브라우저가 없는 컨테이너에서 그대로 즉사한다 — 작업 110 이 `tools/pwlaunch.js` 를 만들어
고친 바로 그 증상이고, 다른 게이트는 전부 옮겨 갔는데 이 파일만 남아 있었다.
110 문서가 지시한 2줄 치환을 그대로 적용했다(동작 변화 없음, 폴백만 생긴다).

---

## §6. 다음 사람에게

1. **`--ifq-k` 는 «화면이 잰 값» 을 넣는 자리다. 눈대중 배율을 넣지 마라.**
   넣기 전에 그 화면의 ref 잉크 bbox 를 확인하고, **우리 렌더에서 잉크비를 다시 재서** 역산해라
   (§1). 측정표에 적힌 `ink/0.70`·`ink/0.93` 환산값은 옛 서체 것이다.
2. **나머지 다섯 화면(05·12·53·69·70)은 이번에 «맞다» 고 확인한 것이 아니다** — 안 건드렸을 뿐이다.
   그 화면들의 배지를 ref 와 대조한 적이 있는지는 각 측정표를 봐라. 대조 없이 .235 를 «정답» 으로
   인용하지 마라(104 가 05·53 에서 뽑은 통일 상수이지 ref 실측이 아니다 — 126 §9-6 이 스트로크에서
   같은 지적을 이미 했다).
3. `tools/ink141.js` 는 22 전용이 아니다 — `.ifq` 를 쓰는 다른 화면도 `SAVE`/열기 절차만 바꾸면
   그대로 쓸 수 있다. **DSF 3 · B 두 장**은 그대로 두고 써라(§3).

---

## §7. 1회차 비평 — 비평가 X · Y (2026-08-26, 캡처 `docs/review/22-r7.png`)

| | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ①~④ 최저 | ⑤ | ⑥ |
|---|---|---|---|---|---|---|---|
| X | 8 | 7 | 8 | 7 | **7** | 8 | 6 |
| Y | 7 | 6 | 6 | 7 | **6** | 8 | 6 |

### 7-1. 배지(=141 의 작업 대상) — **두 비평가 모두 «회수됐다» 고 독립 확인**

- Y: «**더 이상 작지 않다 — 폭은 사실상 적정(이전 45×20 대비 +56%), 커진 것도 아니다.**»
  같은 임계로 잰 ref `100` = 65×25 기준 우리값 보정 시 **폭 −1~+10% · 높이 −4%**.
- X: «알맞음에 근접» — 전폭 −30% → **−4~9%**, 중심 x 219.5 로 **프레임 중심 완전 일치**.
- 둘 다 **중심 정렬은 만점**으로 따로 적었다(«배지 중심 x=219.5로 프레임 중심 219.5에 정확히 맞다»).

### 7-2. ⚠ 두 비평가의 «5px 아래로 내려라» 는 **마스크 비대칭 때문이다 — 반영하지 않았다**

X «아래로 4px 부족» · Y «5px 아래로 내려야» 로 방향이 일치했지만, 근거를 뜯어 보면 **두 사람 다
우리 잉크는 자기 임계로 재고 ref 는 «스펙 값(27)» 을 그대로 썼다.**

| | 잉크 높이 | 프레임 하단 기준 위 | 아래 |
|---|---|---|---|
| X 실측(우리) | **24** | **22** | 1~2 |
| 내 실측(우리, DSF3) | **27** | **22.2** | **5.5** |
| ref 스펙 | 27 | 21.5 | 5.5 |
| X 실측(ref) | **25** ← 자기 임계로는 ref 도 2px 깎인다 | — | — |

**위쪽 값(22 vs 22.2)은 셋이 일치한다.** 어긋나는 것은 아래쪽뿐이고, 그 차이(3px)는 정확히
X 의 잉크 높이 부족분(27−24)과 같다 — 즉 **아래쪽 안티에일리어싱을 자기 마스크가 깎은 것**이다.
X 는 자기 마스크가 ref 도 2px 깎는다는 것을 스스로 적어 놓고(ref 25), 비교는 스펙 27 과 했다.
**같은 자를 양쪽에 대면 차이가 사라진다.** (34-⑪ 교훈의 변형 — «방향 일치» 가 «값 일치» 가 아니다.)
Y 의 «오렌지 채움 하단 +12 vs +7» 도 같은 문제 위에 있다 — Y 자신이 ② 에서 **«우리 오렌지 채움이
ref 보다 3px 높다»** 고 적었으므로 그 기준선 자체가 3px 어긋나 있다.

내 측정은 프레임 **border-box 하단**(`getBoundingClientRect().bottom`) 대비이고 ref 는 측정표의
803.5 대비다 — 양쪽 다 «프레임 하단선» 이라는 같은 정의다. 결과는 **위 21.8~22.2 / 아래 5.2~5.5**
(ref 21.5 / 5.5)로 **최대 0.7px** 다. 내리면 오히려 아래가 6.0 으로 넘친다. **그대로 둔다.**

### 7-3. Y 의 «자당 +9%» — 실재하지만 ref 로는 판정 불가다 (서체 성질)

Y: «자당 어드밴스 24.0 vs ref 22.0 (+9%) 라 `260` 같은 넓은 숫자에서만 넘친다.» 실측으로 재현된다:

| 실제 보상값 | 210 | 235 | 224 | 260 |
|---|---|---|---|---|
| 흰 채움 폭 | 69 (+3.0%) | 73.3 (+9.4%) | 74.7 (+11.4%) | 76 (+13.4%) |

**그러나 ref 에는 `50`·`100` 두 표본밖에 없고, 우리는 그 둘을 ±1% 로 맞춘 상태다.**
ref 가 «자릿수에 선형(자당 21~23)» 인 것을 근거로 **고정폭(tabular) 숫자**를 의심해
`font-variant-numeric:tabular-nums` 를 넣어 봤으나 — **Jua 에 `tnum` 피처가 없어 한 픽셀도 안 바뀐다**
(실측 5행 전부 동일). 선언만 남으면 «했다고 착각하게 하는 no-op» 이라 되돌렸다.

게다가 두 표본의 자당은 ref 23.0 → 22.3(**−3%**), 우리 23.0 → 22.6(**−1.7%**)로 **우리 쪽 산포가 오히려 작다.**
즉 «ref 가 고정폭이다» 는 이 두 표본만으로는 증명되지 않는다. 넓은 숫자(2·3·5·6)를 담은 ref 표본이
없으므로 **판정 불가**이며, 지금 상태는 «ref 가 보여 준 자리는 정확히 맞음» 이다. → **아트/서체 대기**로 남긴다.
(참고로 이 산포는 단일 `scaleX` 로 맞췄을 때 더 나쁘다 — `260` 이 +18% 로 벌어진다. 지금 조합이 더 낫다.)

### 7-4. ①~④ 가 8 미만인 이유는 **전부 141 바깥이다 — 손대지 않았다**

두 비평가의 **1순위가 서로 같고, 둘 다 배지가 아니다**:

> X: «**퀘스트 제목 폰트** — 한글 어드밴스 23.25 → 28.5px(+22%)»
> Y: «**퀘스트 제목 폰트를 +17% 키워라**(잉크높이 29~31 → 35~38). 이것 하나로 제목–게이지 간격 28px 도 ref 19px 로 함께 닫힌다»

이것은 **126 ②-2 «서체 교체가 남긴 −14~17% 회수»** 그 자체다(126 §9-6: `ink/0.93` 이 옛 서체 상수라
재보정 안 된 화면이 전부 같은 방향으로 좁아졌다). 실제로 워커 B 가 **지금 126 6회차로 이 화면을 돌고 있고**,
그 회차에서 «보상 받기 122×30 · 모두 받기 161×42 · 일일 69×36» 을 이미 ±3% 로 맞췄다 —
두 비평가가 그 세 자리를 **«합격선»** 으로 확인해 준다(X: 보상받기 −1.5%, 일일 0%). **남은 것이 제목이다.**
`.qs-t` 는 141 구간이 아니고 남이 돌고 있는 화면이라 **지시서 [1]·[6] 대로 건드리지 않았다.**

나머지 감점도 141 바깥이며, 어느 작업 단위에도 안 걸려 있어 **새 작업 단위로 등재했다**(PROGRESS 143·144):

| 지적 | 수치 | 귀속 |
|---|---|---|
| 보상 프레임 **코너 반경** | ref 29(X 실측)~31(측정표 §12) vs 우리 24.7 → **−15~20%** | 104 통일(`--if-rr .233`)의 **두 번째 희생자** — 141 과 같은 모양의 문제 → **143 등재** |
| 프레임 안 **보석 아이콘 잉크** | ref 54~55×55 vs 우리 40×46 → **−26% / −16%** | `--if-ic:50px`. 코드 주석은 «ref 젬 잉크 55×55» 에 맞췄다고 적혀 있는데 지금 안 맞는다 → **144 등재** |
| 전 라벨 **세로 중심 −4~7px** | 헤더·보상받기·모두받기·탭 라벨이 **전부 같은 방향** | 한 자리가 아니라 line-height/baseline 계열 → 126 ②(서체) 소관으로 본다 |
| 버튼 외곽선 두께 | ref 7~8 vs 우리 6 (−14~17%) | 104 `.ifbtn` 공용 |

**141 의 완료 조건은 «수량 배지가 ref 실측과 맞는가» 이고, 그것은 ±1% 로 충족됐다**(§4 · 7-1).
화면 22 의 ①~④ 점수는 위 항목들이 각자 회수되어야 8 로 돌아간다 — **한 세션이 남의 구간을 같이
고쳐서 올릴 점수가 아니다.**
