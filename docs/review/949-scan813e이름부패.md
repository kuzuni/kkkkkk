# 949 — `tools/scan813e.py` 즉사(«없는 이름») · 넷째 자를 되살려 **U3 옆에 세운다**

- 세션 `sess-1728-11367` (루틴 워커 B) · **1회차** · 2026-09-05
- 티어 **T1 «버그(게이트 부패)»** · 지시서 [3]-**(가)** 기계적/게이트 = 자로만 판정 · **비평가 없음**
- **제품 `index.html` 0줄** · 레이아웃 Δ0px · 밸런스 계수 0줄. 고친 것은 `tools/` 셋이다.

## 1. 등재문과 재현

등재(932 7회차 곁다리): `scan813e` 가 `S.find_base` 를 부르는데 905 가 그 이름을
`find_base_u1` 로 개명했다 ⇒ `AttributeError` 로 즉사.

처방 전에 재현했다(338·341·350 규칙) — `node tools/probe949.js` **13/13**:

| | 수리 전 사본(`2612b48`) |
|---|---|
| `python3 tools/scan813e.py` | 코드 **1** · `AttributeError: module 'scan887' has no attribute 'find_base'` |
| 남는 결과 줄 | **0줄** |
| `scan887` 자신 | `find_base` **없음** · `find_base_u1` 있음 · `find_base_u3` 있음 |
| 형제 `scan813c`·`scan813d` | 905 가 이미 **U3** 으로 옮겨 두었다 |
| 저장소 전수 인구조사 | «없는 이름» 호출 **1자리** = `scan813e` 하나 |

⚑ **죽는 것보다 나쁜 것은 «아무 줄도 안 남는 것»이다.** 점수/결과 줄이 하나도 없으면 회귀 스윕은
이 자를 «빨강» 이 아니라 **«없는 자»** 로 지나간다 — 913(pngjs)·937(numpy)이 찍은 얼굴과 글자
그대로 같다. 이 자가 죽어 있던 동안 813/887/905 계열은 **자 넷 중 셋**으로 굴러갔고 아무도 몰랐다.

⚠ 재현기는 **수리 전 사본 위에서** 잰다(`gitrev756`). 현재 트리에 대고 재현하면 수리한 순간
재현기 자신이 빨개져 «게이트 부패» 와 구분이 안 된다.

## 2. 처방 — 이름 한 줄이 아니라 **이 자의 주제**를 고쳤다

등재문의 갈래는 ⓐ(`find_base` → `find_base_u1` 한 줄) · ⓑ(905 처럼 **U1·U3 을 나란히**)였고,
등재문 자신이 «그 자리에서 정말 U1 을 부르려던 것인지 먼저 보라» 고 경고하고 있었다. 봤더니:

- `scan813e` 의 머리말은 **위 끝점 후보를 나란히 놓고 견주는 자**라고 스스로 적는다(U1·U2·U3).
- 그런데 905 는 그 사이에 **약속의 자(U3)** 를 세웠다. 즉 이 자가 부르던 U1 은 «답» 이 아니라
  **대조군**이고, 905 이후 표에서 빠진 것은 U1 이 아니라 **U3** 이다.
- 형제 둘의 선례도 같은 꼴이다 — `scan813c`·`scan813d` 는 답을 `find_base_u3` 로 옮기고
  **U1 은 «옛 자 · 부호» 로 계속 찍는다**(333 처방: 기각한 자를 지우지 않는다).

⇒ **ⓑ 를 골랐다.** U1 호출을 이름만 고쳐 되살리고(대조군), 그 옆에 **U3 을 신설**해 셋을 한 표에 놓는다.
ⓐ 만 했으면 자는 돌아가지만 **905 이전의 표**를 계속 그렸을 것이다.

바뀐 것(파일 셋 · 제품 0줄):

| 파일 | 바꾼 것 |
|---|---|
| `tools/scan813e.py` | `S.find_base` → **`S.find_base_u1`**(대조군) · **U3(`find_base_u3`) 신설** · 사람/`--json` 양쪽에 U3 행 · «자기 판정» 절 신설 · 939 사전(못 재면 코드 3 + 한 줄) |
| `tools/scan813d.py` | 곁다리 — 기본 캡처가 없으면 `FileNotFoundError` + 코드 1 → **코드 3 + 한 줄**(923 처방) |
| `tools/verify937.js` | 이관 — `verify949`·`probe949` 를 `EXEMPT` 에 등재(자식의 **종료 코드 자체**가 측정 대상인 자만) |

## 3. 되살린 자가 실제로 말한 것 — 세 가지가 한 번에 나왔다

```
■ 레퍼런스 (docs/ref/89-유물-팝업.png · 486x687)
    잉크 639..671 · 아래(= 조립체 최상단까지) 9
    U1 밝은 아랫변(⛔905 기각)  행 629 · 위 10 · 아래/위 = 0.900
    U2 마지막 칠해진 행 |Δ|> 8  행 635 · 위  4 · 아래/위 = 2.250
    U2 마지막 칠해진 행 |Δ|>12  행 634 · 위  5 · 아래/위 = 1.800
    U2 마지막 칠해진 행 |Δ|>18  행 630 · 위  9 · 아래/위 = 1.000
    U3 절대 어둠(905 약속의 자) 행 627 · 위 12 · 아래/위 = 0.750   (U1 대비 −2행)

■ 우리 캡처 (1080x2280)
    U1  행 1973 · 위 26 · 0.692        U3  행 1975 · 위 24 · 0.750   (U1 대비 **+2행**)

■ 같은 문장을 두 그림에
    U1 밝은 아랫변(⛔기각)   ref 0.900 · 우리 0.692 · 차이 **−23.1%**
    U2 마지막 칠해진 행      ref 1.800 · 우리 1.000 · 차이 **−44.4%**
    U3 절대 어둠(905)        ref 0.750 · 우리 0.750 · 차이 **+0.0%**
```

① **검산이 두 번 닫혔다** — 되살린 U1 이 887 이 적어 둔 과녁 **0.900** 을 그대로 내고,
신설한 U3 이 905 확정값 **0.750**(위 12 : 아래 9 ref px)을 두 정수까지 그대로 낸다. 배선이 맞다는 뜻이다.

② **905 ②를 넷째 자가 독립으로 재확인했다** — 같은 U1 이 ref 에서는 U3(밑판 외곽선의 마지막 칠해진 행)의
**아래**를 짚고(U3 − U1 = −2행), 우리 캡처에서는 **위**를 짚는다(U3 − U1 = +2행).
**부호가 뒤집히면 같은 물체가 아니다.** 같은 자리에서 U3 은 두 그림이 **0.750 으로 일치**한다.

③ **이 자의 U2 도 자기 규칙에 걸린다.** 머리말이 «문턱으로 답이 바뀌면 그 자도 이 약속을 못 맡는다»
(A3-ⓑ 자기 적용)라고 적어 두었는데, U2 의 행은 문턱 8/12/18 에서 **635·634·630(폭 5행)** 으로 움직이고
비도 2.250 → 1.000 으로 갈린다. 뿌리는 U1 과 **같은 병**이다 — 옆(여백 띠) 대비 자는 레퍼런스의
«아래로 갈수록 어두워지는 배경 기울기»(y624 37 → y638 11)를 물체로 읽는다. U3 은 **절대 어둠**이라 안 흔들린다.
⇒ 그 판정을 이제 이 자가 **자기 입으로** 찍는다(«■ 자기 판정» 절). 자를 지우지 않은 이유는 ②의 대조군이기 때문이다.

## 4. 게이트

- **`node tools/verify949.js` — 26/26 PASS**
  - **[A] 이름 사슬(본체)** — 저장소의 어떤 파이썬 자도 `scan887` 의 «없는 이름» 을 부르지 않는다(전수 인구조사).
    ⚑ `scan813e` 한 줄만 박아 두면 **다음 개명 때 다른 자가 같은 얼굴로 사라진다** — 905 가 형제 둘은 옮기고
    이 한 자를 두고 간 것이 정확히 그 사고였다. 그래서 이 항은 «한 자리» 가 아니라 «사슬» 을 본다.
  - [B] 그 자가 실제로 답한다 — 코드 0 · U1·U2·U3 **세 줄** · `--json` 도 셋.
  - [C] 값의 검산 — ref U1 0.900 · U3 0.750(위 12 : 아래 9) · U2 는 문턱에서 움직인다.
  - [D] 두 그림을 실제로 잰다 — U1 부호 뒤집힘(−2 ↔ +2) · U3 은 두 그림 0.750 일치 · 캡처는 **스스로 찍고 지운다**.
  - [E] 939 사전 — 없는 그림 / 인자 없음 = **코드 3 + 한 줄**(스택 트레이스 0).
  - [R] 되돌림 시험 — 수리 전 사본에서 [A]·[B] 가 실제로 빨갛다(헛초록 아님).
- **`node tools/probe949.js` — 13/13 PASS**(수리 뒤에도 초록 — 사본을 재는 재현기다)
- 이관 **1종** — `verify937` **38/38**(EXEMPT 등재).
- 회귀 **6종 무수정** — `verify905` 8/8 · `verify887` 7/7 · `verify813` 24/24 · `verify920` 6/6 ·
  `verify939` 32/32 · `SMOKE PASS`.

⚠ **`verify939` 6건은 내 변경이 아니라 환경이었다** — `scipy` 미설치로 `scan885e` 가 코드 2(«환경에 없음»)를
내던 것이고, `pip3 install scipy` 한 줄 뒤 **32/32**. 938 이 «조건부 의존은 부딪히는 자리에서 한 줄로»
라고 정한 그대로다(상시 준비 줄에는 없다). 회귀를 돌리는 다음 워커는 이 줄을 먼저 보라.

## 5. 남긴 것 · 넘긴 것

- **879/932 계열(안내문 여백비)에 자가 하나 돌아왔다.** 넷째 자가 이제 «U1 0.900 ↔ U3 0.750» 을 한 화면에
  같이 찍으므로, 옛 과녁 0.90 을 실은 기록을 만나면 **어느 자의 값인지** 그 자리에서 갈 수 있다(920 정오 표식과 같은 축).
- **948**(«과녁·대역이 정수 격자 위에서 세워졌다 — 부분 화소로는 ref 0.7338 ↔ 제품 0.7748»)에게:
  이 자의 U3 은 **정수 행**이다. 948 이 과녁을 부분 화소로 다시 세우면 여기 [C2]·[C3] 의 두 정수(12·9)도
  같이 이관 대상이다 — 지금 값을 «확정» 으로 굳히지 마라.
- 곁다리 등재 **0건**.
