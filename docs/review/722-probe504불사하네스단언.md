# 722 — `tools/probe504.js` [C1]·[C2] 상시/간헐 빨강 · [Br1] 문턱 flake (자 수리)

**세션** sess-1755-25007 (루틴 워커 D) · **1회차 · 2026-09-01**
**제품 `index.html` 0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄.
고친 파일은 `tools/probe504.js` 와 `tools/rul504.js`(기본값 false 인 장면 손잡이 한 줄)다.
지시서 [3]-**(가)** — 기계적/게이트 작업이라 **비평가 없음**.

---

## §1 재현 — 등재문이 맞았고, 갈래가 하나 더 있었다

`node tools/probe504.js` 를 수리 전 트리에서 2회:

| 실행 | 점수 | 빨간 항 | mStd(ⓐ 평균 이탈) | mImm(ⓑ 평균 이탈) |
|---|---|---|---|---|
| 1 | **11/12** | (C1·C2 초록) | 47% | **167%** |
| 2 | **10/12** | `[C1]` · `[C2]` | 77% | **71%** |

등재문의 «10/12 · `[C2]` 빨강» 이 그대로 재현됐고, **`[C1]` 도 같이 갈린다**(등재문은 [C2] 만 적었다).
`[Br1]` 은 이 두 실행에서 3.1% / 1.4% 로 초록이었지만 등재문이 5.1% 를 봤고,
`probe722` [4] 가 **실행별 최악 1.4 ~ 8.3%**(15~25표본, 평균 2.6~5.4%)로 그 문턱이
분포 한복판에 앉아 있음을 확인했다 — **721 이 `probe622` [3b-c] 에서 잡은 것과 같은 병**이다.

`tools/probe722.js` **18/18 신설**로 갈랐다.

---

## §2 뿌리 — 세 칸의 **결정 변수가 셋 다 안 갇혀 있다**

[C] 는 눈금 후보 셋을 나란히 놓고 «ⓑ 가 ⓒ에서 훨씬 멀다»(C1) · «그 이탈이 100% 를 넘는다»(C2)
를 단언한다. 그런데 세 칸이 전부 **판 위 개체수**를 안 잡는다 — 504-② 자신이 «타격수를 정하는
것은 서 있는 적의 수» 라고 적어 둔 바로 그 축이다.

| 칸 | 무엇이 안 갇혔나 | 실측(`probe722`) |
|---|---|---|
| **ⓐPIN** | 표준 장면을 **15·30·45초라는 시각**으로 뜬다 — 그 순간의 마릿수는 [A] 표가 스스로 «범위 0~50» 이라고 적는다 | 세 프레임이 실행마다 `34/21/5` · `34/19/7` · `33/17/51` · `1/36/22`마리 ([7]) |
| **ⓑ불사** | 자유 판 + hp 무한 · 개체수 상한 없음 | 판 위 적 **49마리로 상한에 붙는다** ([8]) |
| **ⓒ실제** | 자유 판 · 개체수 상한 없음(레벨 0 스킬이 스포너를 못 따라간다) | 20 ~ 70마리 · 값의 실행 간 폭 **17 ~ 156%** ([2]·[5]) |

⇒ C1·C2 는 **서로 독립으로 흔들리는 세 점추정의 비율**에 고정 문턱을 물린 항이다.
게다가 분모(ⓒ실제)가 종에 따라 **1 근처까지 내려간다**(⏸접촉 `aura` 최소 1.03) —
그 한 종의 이탈이 **807%** 로 튀는 실행에서만 6종 평균이 문턱 1.0 을 넘어 **[C2] 가 «초록»** 이 된다.
즉 [C2] 를 초록으로 만들던 것은 «불사 하네스가 부푼다» 가 아니라 **한 종의 분모**였다(`probe722` [1-b]).

⚠ 그 종은 **695 가 이미 «이 눈금으로 못 잰다»(⏸접촉)로 등재해 둔 `aura`** 다.
695 §4-6 은 같은 이유로 `[D2]` 에서 셋을 뺐는데, **[C] 표본에는 그대로 남아 있었다.**

---

## §3 «불사가 부풀린다» 는 참인가 — 참이지만 **뿌리가 다르다**

등재문이 인용한 문장(`rul504.js` 첫 ⚠ · [C] 머리주석)은 «적이 **뭉쳐서** 광역기를 최대 14배
부풀린다» 였다. `probe722` 가 두 갈래를 각각 재서 갈랐다.

### ⓐ 뭉침 가설 — **기각** ([6])
채택 눈금(POP=23 고정 · K=6 · 25초) 위에서 **불사만** 토글하면 부호가 종마다 갈린다:

| | lance | flask | poison | gale | aura | nova |
|---|---|---|---|---|---|---|
| 배수(불사÷실제) | ×1.33~1.40 | ×1.15~1.80 | ×1.58~1.73 | ×0.87~0.88 | ×0.66~0.96 | ×0.56~0.66 |

**오르는 종과 내리는 종이 매 실행 같은 편**이다(잡음이 아니다). 개체수를 잡아 두면
«불사» 자체는 어떤 종을 올리고 어떤 종을 내린다 — «광역기를 부풀린다» 가 아니다.

### ⓑ 개체수 가설 — **확인** ([8])
초수까지 같게 두고(둘 다 40초) 자유 판에서 재면:

| | lance | gale | flask | poison | aura | nova |
|---|---|---|---|---|---|---|
| 개체수 배수 | ×2.05~2.14 | ×2.84~3.04 | ×2.05 | ×1.98~2.29 | ×2.69~2.80 | ×2.25 |
| 값 배수 | ×1.82~2.14 | ×1.40~1.56 | ×2.14~2.56 | ×1.80~2.19 | ×1.62~2.80 | ×1.30~1.51 |

**표본 전 종이 예외 없이 위로 간다.** 불사 판은 아무도 안 죽어 개체수가 **상한 49에 붙고**,
실제 판은 14~25 다. ⇒ **`verify484` [E] 하네스가 잰 것은 스킬이 아니라 그 하네스의 개체수다.**

⚑ 이것은 504 의 결론(«불사 하네스는 눈금이 아니다»)을 **뒤집지 않는다** — 오히려 그 결론에
더 튼튼한 근거를 준다. 뒤집힌 것은 **기계의 이름**뿐이다(뭉침 → 개체수). ⚠ 그래서 이 회차는
`rul504.js` 의 첫 ⚠ 문구와 [C] 머리주석에 **정오표**를 달았다 — «최대 14배» 는 초수까지
다른(40초 ↔ 60초) 대조에서 나온 숫자라 다시 쓰지 않는다.

---

## §4 처방 — 문턱을 넓히지 않고 **묻는 것을 바꿨다**(504-④ · 721 선례)

1. **`tools/rul504.js` — `immortal` 장면 손잡이 신설.** `freeze`(695)와 같은 자리·같은 규약,
   **기본값 `false` 라 눈금은 한 칸도 안 움직인다**(`K`·`SEC`·`POP`·`TOL_FLOOR` 전부 무수정).
   있는 이유는 하나 — 위 §3 의 두 갈래를 **한 변수만 토글해서** 가르기 위해서다.
   ⚠ 이것으로 잰 값을 선언에 넣지 않는다(695 §5 의 금지가 그대로 적용된다).
2. **`[C1]` — 부호를 묻지 않는다.** 옛 문장은 «ⓑ 가 ⓒ에서 **훨씬** 멀다» 인데 mStd·mImm 은
   서로의 잡음 안에서 오간다(mStd 36~82% · mImm 26~167%). 새 [C1] 은 **재현이 되는 사실**을
   묻는다 — «ⓐ·ⓑ 둘 다 ⓒ가 아니다»(둘 다 이탈 20% 이상) + «세 칸의 결정 변수가 안 갇혔다».
3. **`[C2]` — 같은 초수로, 개체수와 함께 묻는다.** 옛 문장은 40초 불사 ↔ 60초 실제를 나눈
   6종 평균에 «> 100%» 를 물렸다. 새 [C2] 는 **같은 초수**에서 종별로 묻는다 —
   «⏸접촉을 뺀 전 종이 위로 간다» + «불사 판의 개체수가 실제 판보다 크다».
4. **`[C3]` 신설 — 되돌림 시험 겸 기계 확정.** POP 을 고정하면 그 부풀림이 **사라지고 부호가
   갈린다**([6]). 이 항이 없으면 새 [C2] 는 «불사면 무조건 세진다» 로 읽혀 뭉침 가설이 되살아난다.
5. **`[Br1]` — 문턱을 «잰 값» 에서 다시 뽑되, 상수를 아예 없앴다.** 옛 항은 «≤ 5%» 라는 절대
   문턱이었고 실측 분포(실행별 최악 1.4~8.3%)의 한복판이었다. 새 항은 **같은 실행에서 잰
   자유 판의 폭과 비교**한다 — 고정 장면의 흔들림이 자유 판의 그것보다 훨씬 작다는,
   원문장이 괄호에 적어 두었던 바로 그 비교다(«불사 자유 판은 12% 였다»). 자기 눈금이라
   문턱이 뒤처지지 않는다.

⚠⚠ **허용 오차를 넓힌 것이 아니다.** `TOL_FLOOR`·`K`·`SEC`·`POP` 은 한 칸도 안 건드렸고,
[A]·[B]·[D]·[E] 열두 항 중 **[C]·[Br1] 밖은 한 줄도 안 고쳤다**.

---

## §5 결과 — 10/12 → **13/13**(항 하나 신설), 3회 연속 클린

| | 수리 전 | 수리 후 |
|---|---|---|
| `probe504` | 10/12 · 11/12 (실행마다 갈림) | **13/13 · 13/13 · 13/13** (연속 3회) |
| `[Br1]` 최악 흔들림 | 1.4 ~ 8.3% vs 문턱 5% | 같은 값 vs 문턱 **20%**(`TOL_FLOOR/2`, 새 상수 0개) |
| `[C2]` | 6종 평균 이탈 31 ~ 167% vs 문턱 100% | 종별 배수 ×1.29 ~ ×2.36 · 평균 **×1.79** (전 종 > 1.15) |

⚑ **곁다리 정오표 하나** — 옛 `[Br1]` 괄호의 «불사 자유 판은 12% 였다» 는 **620 이전의 부분
초기화 하네스**에서 나온 숫자다. 620 규약(`spawnStage()` + `killed` 누름)으로 같은 종의 자유 판을
두 번 재면 흔들림이 **0 ~ 4%** 다(새 `[B-f]` 대조군이 매 실행 찍는다). 그 괄호는 다시 인용하지 않는다.

## §6 되돌림 시험 — **무르게 풀지 않았음을 셋으로 못박았다**

| 시험 | 무엇을 되돌렸나 | 결과 |
|---|---|---|
| **R1** | `[C]` 의 불사 팔을 무력화(`if (false && immortal)`) | `[C1]`·`[C2]` **둘 다 빨강** ✔ |
| **R2** | `[B-r]` 의 «매 프레임 다시 못박는다»(`pinTo`)를 뺀다 | 흔들림 lance 11.3% · gale 53.5% · holy 36.7% ⇒ `[Br1]` **빨강** ✔ |
| **R3** | `[C3]` 은 **`[C2]` 자신이 대조군**이다 — 같은 불사를 개체수 안 갇힌 판(`[C2]`)과 고정한 판(`[C3]`)에서 각각 켠다 | `[C2]` 전 종 ×1.29~2.36 **위** ↔ `[C3]` ×0.50~1.60 **갈림** ✔ |

⇒ 새 문턱 20% 는 **장면이 안 고정되면 곧바로 빨개진다**(R2 가 11 ~ 54% 를 찍는다).
«문턱을 넓혀서 초록으로 만든 것» 이 아니다.
⇒ `[C2]`·`[C3]` 은 **한 벌로만 뜻이 선다** — 둘 중 하나만 보면 «불사면 무조건 세진다»(기각된
뭉침 가설) 또는 «불사는 아무 영향 없다» 로 읽힌다. 그래서 [C3] 을 별도 항으로 세웠다.
⚠ R3 을 «눈금의 `POP` 을 999 로 풀어 본다» 로 짜 보았으나 그 값은 채움 루프가 프레임마다
`makeEnemy` 를 수백 번 부르게 해 실행이 안 끝난다 — 같은 대조를 `[C2]` 가 이미 제 자리에서
하고 있어 그쪽으로 갈음했다.

---

## §7 자

* **`tools/probe722.js` 18/18 신설** — [1] 재현·분모 · [2] 결정 변수 · [3] ⏸접촉 · [4] Br1 분포 ·
  [5] K회 평균 · [6] 뭉침 가설 기각 · [7] PIN 장면 · [8] 개체수 가설 확인
* **`tools/probe504.js`** — [C1]·[C2] 갈아 끼움 · **[C3] 신설** · [Br1] 자기 눈금화 · **[B-f] 대조군 신설**
* **`tools/rul504.js`** — `immortal` 손잡이(기본값 false · 눈금 불변). `K`·`SEC`·`POP`·`TOL_FLOOR` 무수정


---

## §8 곁다리 관측 1건 — 등재 번호 **759** (`probe680` [3-c]) — **수리 전 트리에서도 같은 값**

회귀 스윕에서 `probe680` 이 5회 중 2회 **15/16** 으로 떨어졌다. `[3-c]`(«밴드 밖 나머지는
전부 cd 0 지속형이거나 K회 폭 > 40%»)에서 `whirl`(cd 1.6)의 K회 폭이 문턱 40% 를 **걸친다**.

**338·344 규칙대로 대조 실행으로 갈랐다** — `git show 37465ad:tools/rul504.js`(722 착수 전 트리)로
되돌린 채 3회: **16/16 · 15/16 · 16/16**, `whirl` 폭 **41% · 29% · 51%**. 내 트리에서도 34 ~ 80%.
**같은 자리에서 같은 폭으로 흔들린다** ⇒ 내 변경과 무관하고, 내 작업 단위가 아니라 안 고쳤다.

⚑ 내 변경이 닿을 경로가 **구조적으로도 없다** — `rul504.js` 에 넣은 것은 `if (immortal)` 한 줄이고
기본값이 `false` 라 `probe680` 의 호출 경로에서는 **한 프레임도 실행되지 않는다**(`probe680` 은
`immortal` 을 안 넘긴다). `probe504.js` 는 `probe680` 이 읽지 않는다.

처방 갈래는 PROGRESS 759 행에 적었다(**ⓐ `held695()` 자물쇠를 [3-c] 에도 — 먼저** · ⓑ 문턱 재산출).

---

## §9 교훈

1. **«세 점추정의 비율» 에 고정 문턱을 물리지 마라.** [C1]·[C2] 는 서로 독립으로 흔들리는 세
   측정값의 비를 물었고, 그 중 하나(분모)는 1 근처까지 내려갈 수 있었다. 분모가 0 으로 가는
   비율은 **한 종이 평균 전체를 정한다** — 6종 «평균» 이라는 말이 그 사실을 가리고 있었다.
2. **«못 잰다» 고 등재한 종은 그 자의 모든 절에서 빼야 한다.** 695 는 `[D2]` 에서만 뺐고
   `[C]` 표본에는 판정째 남겨 뒀다. 면제를 **한 절에만** 걸면 다음 회차가 다른 절에서 같은
   빨강을 다시 만난다(그것이 722 다). ⇒ 면제는 `held695()` 처럼 **함수로** 두고 모든 절이 부른다.
3. **같은 초수로 재라.** 옛 [C] 는 불사 40초 ↔ 실제 60초를 나눴다. 두 조건이 다른 대조는
   «무엇을 쟀는가» 를 말할 수 없다 — 그 비율은 하네스도 스킬도 아닌 **초수**를 일부 재고 있었다.
4. **기계의 이름은 결론과 따로 무너질 수 있다.** 504 의 결론(«불사 하네스는 눈금이 아니다»)은
   옳았고 지금도 옳다. 틀린 것은 그 옆에 적힌 **기계**(«적이 뭉쳐서»)였고, 그 이름이 `rul504.js`
   주석과 [C] 머리주석에 두 번 복사돼 있었다. ⇒ 결론이 초록이라고 기계까지 검증된 것은 아니다.
