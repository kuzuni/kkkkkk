# 36 패스 — 출석 패스 탭 · 비평 기록

- 측정표 `docs/measure/36-패스출석패스.md` (껍데기는 `docs/measure/35-패스.md` 를 따른다)
- 캡처 `node tools/cap36.js 36-r{n}.png` · 기능 게이트 `node tools/verify36.js` · 자체 픽셀 대조 `python3 tools/scan36.py [캡처]`
- 담당: 2026-08-25, sess-0743-16886 (워커 C)

---

## 껍데기 판정

35 가 만들어 둔 **`#psw` 전용 전체화면 페이지**(딤 없음)를 **그대로 쓴다.**
35 측정표 §9-1 이 «x=4 열 RGB 대조에서 전 y Δ<12» 로 공유를 확정해 뒀고, 이번에 스팟 재검해도 일치했다.
따라서 이 작업의 범위는 **탭이 갈리는 슬롯 4가지**다(35 §9-3 이 지정한 그대로):

| # | 슬롯 | 36 의 값 |
|---|---|---|
| ① | 히어로 아트 + 타이틀 + 부제 + 스탯 2줄 + 가격 | 진파랑 하늘 + 빛살 + 반짝임 + 광차 + 얼음지대 / «출석 패스 1» / «매일매일…» / «접속일 / 2» / ₩5,900 |
| ② | 프리미엄 열 칸 개수 | **1개** (x741..898, 컬럼 중앙 819.5) |
| ③ | 리스트 선두 라벨 알약 | **«접속일»** 172×72 r31 @ ref(454, 865) |
| ④ | 육각 라벨 의미 | 스테이지 → **접속 일수(1일 단위 연속)** |

기능 쪽은 슬롯이 아니라 **규칙**이 갈린다 — 해금 진행도가 `S.best` 가 아니라 **`S.att.n`(접속 누적일)** 이고,
프리미엄과 수령 기록이 **패스마다 따로**여야 한다(§«저장 구조» 참고).

---

## 회차 기록

### 1회차 — 슬롯 구현 (캡처 `36-r1.png`)

- `PASS_TABS.att` 신설 + 탭별 `step`/`n`/`prog`/`head` 추가. 35 의 골격을 그대로 태웠다.
- **저장 구조 변경**(아래 §저장 구조) + 마이그레이션.
- `tools/cap36.js`·`tools/verify36.js` 신설. `VERIFY35 PASS`(회귀 0) 확인.

**비평 A 5 / B 5** — ①6·②6·③5·④6 (A) / ①7·②5·③6·④7 (B). 두 명이 **독립적으로 같은 1순위**를 짚었다:

| # | 지적 | 판정 |
|---|---|---|
| **A1 · B1 [치명]** | 프리미엄(보라) 컬럼 배경이 «접속일» 알약 행 **221px 구간에 통째로 없다**. 그 자리가 리스트 배경(파랑) 단색 | **채택** — 내 버그. 라벨을 «행» 이 아니라 떠 있는 알약 하나로만 그렸다 |
| **B7 [①③④]** | 첫 행 구분홈이 ref 930 vs 캡처 942 (**+12**), 상단 스페이서 218 → 230 | **채택** — 라벨 행에 pitch(229.85)를 그대로 썼다. 정답은 **221.5** |
| **A4 · B6 [⑥]** | 가격 «₩5,900» 잉크 폭 141 → 91 (**−35%**) | **채택**(자체 스캔으로 재확인) |
| **A13 [②]** | 타이틀 잉크 폭 325 → 338 (**+4.0%**) | **채택**(자체 스캔 −> 동일) |
| **B11 [⑥]** | 부제 2줄 블록 폭 468 → 433 (**−7.5%**) | **채택**(자체 스캔 467 → 433) |
| **A10 · B3 [②]** | 프리미엄 칸 자물쇠가 25~35% 작다 | **채택**(자체 스캔: ref 잉크 79~85 폭 vs 우리 63) |
| **A2 [①③]** | 헤더 밴드 프리미엄 배지 중심 x 623 → 649.5 (**+26.5**) | **보류(아트 대기)** — 리본 아트가 이모지라 잉크 폭이 −75%다. 위치만 옮겨도 안 맞는다 |
| **A3 · B5 [⑥]** | 하단 탭 라벨 잉크 높이 +35~42% | **미채택** — 자체 측정으로 **활성 «출석» 라벨 코어는 ref 45**(§9). 비평가는 비활성 탭을 잰 것으로 보인다 |
| **A8 [③]** | 부제 행간 41 → 37 | **미채택** — ref 코어 top 294 / 331 = **37**. 35 측정표도 37. 비평가는 잉크 기준으로 잰 값 |
| **B4 [②]** | 육각이 8각, 측면 현 −14% | **미채택(35 구간)** — 35 가 «45° 챔퍼 8각» 으로 실측해 확정한 껍데기다. 내 측정도 외곽 105×111 로 35 표(108×107)와 4px 차라 확정적이지 않다 |
| **B10 [⑤]** | 무료 칸 채움이 열 배경과 대비 소실 | **채택(다른 원인)** — 색이 아니라 **보상표**가 틀렸다(§보상표). ref 는 전 칸 «에픽 주황» 인데 우리는 등급을 단계별로 굴렸다 |

> **교훈 — 비평가 수치는 «무엇을 기준으로 쟀는지» 확인 전에 반영하지 않는다(LESSONS 05-③·21-④).**
> 이번에 A3/A8/B4 세 건이 그 이유로 미채택됐고, 반대로 A4/A13/B11 세 건은 `tools/scan36.py` 로
> **같은 임계값·같은 창** 스캔을 돌려 재확인한 뒤에 반영했다. 그 스크립트가 «제3의 독립 검산» 이다.

### 2회차 — 비평 반영 (캡처 `36-r2.png` → `36-r3.png`)

1. **라벨 행을 진짜 «행» 으로 만들었다** — `.ps-r.ps-hr` 안에 `.ps-cf`/`.ps-cp` 컬럼 배경 + 알약.
   (A1·B1 치명 해소. 도구 셀렉터도 `.ps-r:not(.ps-hr)` 로 전부 바꿨다)
2. **선두 오프셋 221.5** — 레퍼런스는 스크롤 0 에서 첫 구분홈이 ref **1017.5**(= 리스트 시작 796 + 221.5)다.
   pitch 를 쓰면 행 경계가 통째로 8.35px 내려간다. (B7 해소 — 재측정 933.5, ref 930 대비 Δ3.5)
3. **출석 히어로** — 구름을 끄고(`#psw.att .ps-cl{display:none}`) 진파랑 하늘·빛살 5쐐기·흰 4각 반짝임 3개·
   광차 아트 자리(396×369)·전폭 얼음지대(1080×234)를 깔았다.
4. **보상표를 레퍼런스대로** — 전 행 다이아, 무료 1,000 고정 / 프리미엄 1일차 30,000 그 뒤 10,000, **전 칸 에픽**.
5. **수령완료 = 등급색 × 0.5** — 35 가 CSS 에 박아 둔 `#1C526E`/`#163C60` 은 **파랑 등급 한 칸의 인스턴스**였다.
   주황 등급에서 그대로 쓰면 틀린다. `passDim()` 으로 등급색에서 계산한다(§8-3 근거).
6. **자폭 재조정** — 35 의 `scaleX` 는 35 문자열 기준이라 36 문자열에서 어긋난다. 자체 스캔으로 다시 잡았다:

| 요소 | 반영 전 | 반영 후 |
|---|---|---|
| 타이틀 «출석 패스 1» | 338 (**+4.0%**) | **325 (0.0%)** |
| 부제 L1 | 433 (**−7.3%**) | **467 (0.0%)** |
| 부제 L2 | 200 (−6.5%) | **216 (+0.9%)** |
| 가격 «₩5,900» | 91 (**−35.5%**) | **142 (+0.7%)** |
| 선두 알약 캡슐 | — | **172×95, x454 (0.0%)** |

7. **자물쇠 확대** — 59×69/58px → **79×87/74px + scaleX 1.32**(측정표 §8-6 의 잉크 79×87 목표).
   ⚠ 재측정 결과 아직 폭이 부족하다(잔여 문제 1번).

**게이트**: `VERIFY36 PASS` · `VERIFY35 PASS`(회귀 0) · `SMOKE PASS`.

### 3회차 — 자체 스캔 2건 (캡처 `36-r3.png` → `36-r4.png`)

**자체 스캔으로 먼저 잡은 것 — 얼음 지대 능선이 100px 아래로 내려가 있었다.**
`.ps-ice` 의 `clip-path` 를 만들 때 **레퍼런스 y 를 밴드 비율로 환산하지 않고 그대로 % 로 썼다.**
밴드는 frame 301..534(h234)이고 능선 실측은 ref y384..470 = frame 300..386 이므로 **밴드의 위 0~36%** 인데,
43~74% 로 적혀 있었다. 컬럼별 «첫 밝은 시안 픽셀» 프로파일을 ref 와 대조해 잡았다:

| x | 120 | 220 | 1020 |
|---|---|---|---|
| ref 능선(frame y) | 332 | 324 | 332 |
| 수정 전 | 검출 안 됨(밝은 층이 클립으로 잘려 안 보임) | 〃 | 〃 |
| 수정 후 | 314 | 303 | 317 |

남은 Δ≈−18~21px 은 **레퍼런스 크리스탈의 검정 외곽선(6~10px) + AA** 만큼이다(우리는 외곽선이 없어 시안이 클립 경계에서 바로 시작한다).
실루엣 자체는 맞다.

> ⚠ **운영 사고 — 비평가가 읽는 중에 `36-r3.png` 을 재캡처로 덮어썼다**(LESSONS 52 운영메모의 그 사고).
> 두 비평가는 **얼음 능선 수정 «전»** 상태를 채점했을 가능성이 높다. 수정 «후» 빌드는 **`36-r4.png`** 로 따로 남겼다.
> 3회차 지적 중 얼음 지대에 관한 것은 이 점을 감안해 읽을 것.

**자체 스캔 2 — 행 구분홈이 4~8px 높았다.** 무료 열 좌측 띠(x20..170)의 행별 평균 밝기에서 «국소 최소(구분홈)» 를
ref·캡처 **같은 검출기**로 뽑아 대조했다:

| 홈 # | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| ref (ref y) | 1016 | 1247 | 1479 | 1706 | 1938 |
| 수정 전 | 1012 | 1241 | 1471 | 1701 | 1931 |
| Δ | −4 | −6 | −8 | −5 | −7 |
| **수정 후 (선두 오프셋 221.5 → 226.5)** | **1017** | **1246** | **1476** | **1706** | **1936** |
| Δ | **+1** | **−1** | **−3** | **0** | **−2** |

> **교훈** — 구분홈은 행의 **바닥**이라 «행 top = 리스트 시작 + 오프셋» 에서 바로 유도하면 2~3px 어긋난다.
> 측정표의 «첫 구분홈 1017.5» 를 그대로 오프셋으로 쓰지 말고, **같은 검출기로 양쪽을 재서 잔차를 0 으로 미는** 편이 정확하다.
> (남은 −3px 은 우리 홈이 6px, 레퍼런스 홈이 9px 이라 «국소 최소» 위치가 다른 데서 온다 — pitch 자체는 35 의 229.85 를 그대로 뒀다.)

---

## 저장 구조 (KEY 미변경 · 마이그레이션으로 흡수)

36 이 붙으면서 «패스가 하나» 라는 전제가 깨졌다. 두 가지를 바꿨다:

| | 35 시절 | 지금 |
|---|---|---|
| 수령 기록 | `S.pass.got['단계:칸']` | `S.pass.got['탭:단계:칸']` |
| 프리미엄 | `S.pass.prem` (boolean) | `S.pass.prem[탭]` (맵) |

네임스페이스가 없으면 **스테이지 패스 3단계를 받은 사람이 출석 3일차도 받은 것**이 되고,
프리미엄도 ₩14,900 한 번에 ₩5,900 짜리까지 열린다. `load()` 가 구 세이브를 **전부 stage 로 이관**한다
(44 교훈 2 대로 `KEY` 는 올리지 않는다). `tools/verify35.js` 의 키 단정 2곳도 새 형태로 맞췄다.

---

## 기능 체크 표 (`node tools/verify36.js` — 전 항목 PASS)

| 무엇을 하면 | 무엇이 바뀌나 | 검증 |
|---|---|---|
| 하단바 «출석» 탭 클릭 | `passTab='att'`, 활성 칸 x785 w289, 히어로·타이틀·부제·스탯·가격 전부 교체, `#psw.att` 부착 | ✓ |
| 탭 전환 | 스크롤이 그 탭 진행도에 맞게 다시 잡힌다(`passScroll()`) | ✓ |
| 접속일 2 → 5 로 증가 | 육각 잠금이 2일차까지 → 5일차까지로 풀린다 | ✓ |
| 무료 칸(해금됨) 클릭 | 다이아 **+1,000**, 칸이 `.dn`(등급색×0.5 + ✓ + 회색 수량)으로, `S.pass.got['att:i:0']=1`, `save()` | ✓ |
| 같은 칸 다시 클릭 | **아무 일도 없다**(재수령 불가) | ✓ |
| 미해금 일차 클릭 | **아무 일도 없다** | ✓ |
| 프리미엄 칸(미구매) 클릭 | «프리미엄 패스를 활성화하면…» 팝업, 지급 없음 | ✓ |
| 프리미엄 활성화 후 | 구매 버튼이 사라지고 프리미엄 칸 수령 가능(1일차 30,000 / 그 뒤 10,000) | ✓ |
| 출석 프리미엄 구매 | **스테이지 패스는 여전히 미구매** (탭별 분리) | ✓ |
| 출석 보상 수령 | **스테이지 패스 칸에 수령완료가 안 샌다** (키 네임스페이스) | ✓ |
| reload | 수령 기록·프리미엄 유지, 스테이지는 미구매 그대로 | ✓ |
| 구 세이브(35 시절) 로드 | `'12:0'` → `'stage:12:0'`, `prem:true` → `{stage:1}` | ✓ |
| 깨진 `pass` 필드(null·문자열·숫자·부분객체) | 기본값 흡수, 예외·NaN 0건, 30행 정상 렌더 | ✓ |
| 1080×1920 / 2280 / 2520 | 하단바 bottom = 프레임 높이, 알약·프리미엄 칸 좌표 불변 | ✓ |

---

## 잔여 문제

1. **프리미엄 칸 자물쇠 폭이 아직 −25% 다.** 🔒 이모지는 가로세로가 같이 커져서
   `font-size` 로는 ref 잉크(79×87, 코어 49×56)의 «가로로 넓은» 비율이 안 나온다. `scaleX 1.32` 로 밀었지만
   글리프 자체가 좁다 → **아트 대기**(측정표 §11 «아트 필요» 표에 올려 뒀다).
2. **헤더 밴드의 무료/프리미엄 리본 배지가 좁다**(프리미엄 잉크 폭 −75%, 중심 +26.5px).
   35 가 이모지(🎗️/🏅)로 자리만 잡아 둔 것이고 레퍼런스는 **은/금 날개 리본 125×89 / 125×108** 이다.
   **35 구간이자 아트 대기** — 이모지를 아무리 늘려도 리본 형상이 안 나온다.
3. **육각 배지 형상**(45° 챔퍼 8각 vs 6각)은 35 가 실측으로 확정한 껍데기라 손대지 않았다.
   내 측정(외곽 105×111)과 35 표(108×107)가 4px 어긋나 **확정적이지 않다** — 35 를 다시 잡는 세션이 가릴 것.
4. **비활성 탭 fill** 이 36 실측에서는 바 배경과 같은 `#49322C` 인데 35 표는 `#3F2D29` 로 적혀 있다.
   ⑤ 색 항목이라 통과를 막지 않아 이번 라운드에는 미반영.


### 3회차 비평 결과 — **C 5 / D 6** (①~④ 최저: C 5 · D 6)

| | ① | ② | ③ | ④ | ⑤ | ⑥ | 점수 |
|---|---|---|---|---|---|---|---|
| C | 7 | **5** | 7 | 7 | 9 | 6 | **5** |
| D | 7 | **6** | **6** | 7 | 9 | 8 | **6** |

> ⓐ 두 비평가 모두 **신뢰도 확인 3/3 정답**이었다(알약 캡슐 172×72@454,865 · 프리미엄 칸 중심 819.5 ·
> 첫 구분홈 ref≈1017 · 히어로 하단 618 · 금색 바 25px · 육각 숫자 1~5 · 활성 탭 x786..1074 폭 289).
> ⓑ 둘 다 **얼음 지대 능선(C-4)** 과 **구분홈 −2.5~−4px(C-7·D-10)** 을 짚었는데, 이 둘은 채점 «전» 에
> 자체 스캔으로 이미 고친 것이다(위 3회차 항목). 캡처 덮어쓰기 사고와 같은 원인이라 4회차에서 재확인한다.

### 4회차 — 3회차 비평 반영 (캡처 `36-r5.png`)

| # | 지적 | 조치 | 자체 재측정 |
|---|---|---|---|
| **C-10 [②⑥]** | **보상 수량이 «1.00K / 30.0K» 인데 레퍼런스는 «1,000 / 30,000»** | `fmt()` → `won()`(쉼표) | 무료 폭 105→**107**(ref 105) · 프리미엄 127→**133**(ref 127) |
| **C-6 [②]** | 수령완료 체크 −32% (`scaleY(.82)` 가 세로를 죽이고 있었다) | 세로 살리고 `scaleX(.94)` 로 가로만 조정 | ref 82×66 → **83×65** (Δ+1/−1, 위치 Δ+1/−2) |
| **C-9 · D-1 [②③]** | 자물쇠가 이번엔 **과대**(+20~27%)하고 14px 아래 | 79×87/74px/1.32 → **66px/1.24 · top −35** | 상단 돌출·중심 정렬 회복 |
| **C-8 [④]** | 알약 글자가 캡슐 광학중심에서 **6.5px 아래** | `.ps-lb>i` 를 −7px | 캡슐·글자 중심 일치 |
| **C-5 · A-2 [④]** | 헤더 프리미엄 리본 배지 중심 **+26.5~27px** 우측 | `#psw.att .ps-bdg.p{left:535px}` (35 안 건드리게 스코프) | 잉크 좌단이 컬럼 경계 560 에 |
| **C-3 [①③]** | 버튼 라벨이 리본 배지를 **13px 물고 들어감**(라벨 잉크 +8.6%) | `#psw.att .ps-buy>b.t1>i{scaleX(.83)}` | ref 256 기준으로 좁힘 |
| **D-6 [④]** | 가격 2행 중심이 1행과 **10.5px** 어긋남 | 가격 중심은 버튼 중심(814)이 아니라 **820**(측정표 §5) → `translateX(6px)` | ref 141@750 → **142@748** |
| **D-7 · C-10 [④②]** | 젬이 칸 중심에서 5.5px 위 · 폭 +10% | font 96 + `scaleX(.76)` + top 24 (**att 스코프**) | ref 78×77@221,1553 → **78×72@221,1554** |
| **D-8 · C-10 [①③]** | 부제 블록이 **7px 위** | `#psw.att .ps-sub{top:207px}` | L1 **467×31 (0.0%/0.0%)** |
| C-1·C-2·D-2·D-5 | 하단 탭 아이콘·라벨 비, 뒤로가기 화살표 크기 | **미채택(35 구간 · 비평가끼리 불일치)** — C 는 라벨 «+42%», D 는 블록 «+18%», 내 실측은 활성 라벨 코어 45 로 ref 와 일치 |
| D-11 | 리스트가 24px 보다 3px 더 흡수 | **미채택** — D 는 탭바 높이를 182 로 봤고 C·A 와 35 측정표는 **179** 다(C 가 «양쪽 179 동일» 로 확인) |

> **함정 하나 더** — `.ps-buy>b` 는 `position:absolute; left:0; right:0` 다.
> 여기에 `position:relative; left:6px` 를 주면 **absolute 가 풀려 폭이 통째로 무너진다**(가격이 19px 조각으로 찌그러졌다).
> 위치 미세조정은 `transform:translateX()` 로만 해야 한다.

**4회차 자체 스캔 요약** (`python3 tools/scan36.py docs/review/36-r5.png`)

| 요소 | ref | cap | 오차 |
|---|---|---|---|
| 타이틀 | 325×71 | 325×70 | 0.0% / −1.4% |
| 부제 L1 | 467×31 | **467×31** | **0.0% / 0.0%** |
| 부제 L2 | 214×33 | 216×30 | +0.9% / −9.1% |
| 스탯 라벨 | 103×37 | 107×35 | +3.9% / −5.4% |
| 스탯 값 | 22×30 | 21×31 | −4.5% / +3.3% |
| 가격 ₩5,900 | 141×30 | 142×31 | +0.7% / +3.3% |
| 알약 글자 | 89×31 | 88×31 | −1.1% / 0.0% |
| 알약 캡슐 | 172×95 | 172×95 | **0.0% / 0.0%** |
| 젬 아이콘 | 78×77 | 78×72 | 0.0% / −6.5% |
| 수령완료 ✓ | 82×66 | 83×65 | +1.2% / −1.5% |
| 행 구분홈 5개 | 1016·1247·1479·1706·1938 | +1 / −1 / −3 / 0 / −2 | **±3px** |

**게이트**: `VERIFY36 PASS` · `VERIFY35 PASS`(회귀 0) · `SMOKE PASS`.


### 4회차 부록 — **하단 탭바 지적(A-3·C-1·D-2·D-5)은 «세로 변환» 오적용이었다**

네 건의 «하단 탭 라벨이 +35~42% 크다 / 아이콘이 −22~33% 작다» 는 지적을 자체 스캔으로 반증했다.

> **하단 탭바는 «바닥 앵커» 라 세로 변환이 `ref y − 84` 가 아니라 `ref y − 60` 이다.**
> 프레임(2280)이 레퍼런스 콘텐츠(2256)보다 24px 길고 그 차이를 리스트가 흡수하므로,
> 바닥에 붙은 요소는 **84 − 24 = 60** 만큼만 올라온다(LESSONS 30-⑤ «하단 고정 요소에 ref y − 84 를 쓰면 안 된다» 의 재발).
> 지시서가 비평가에게 «세로는 y−84 하나뿐» 이라고 알려 주기 때문에, **비평가는 구조적으로 하단바를 잘못 대조한다.**

`ref y − 60` 으로 같은 임계값(코어 = `min(RGB)>215`)으로 다시 재면:

| 라벨 | ref | cap | Δ |
|---|---|---|---|
| 「출석」(활성) | **93×57** @x885 y2258 | **99×56** @x881 y2258 | 폭 +6% · 높이 **−1px** · **y 오차 0** |
| 「스테이지」(비활성) | **117×53** @x236 y2258 | **129×55** @x229 y2258 | 폭 +10% · 높이 **+2px** · **y 오차 0** |

높이는 ±2px 이고 세로 위치는 **정확히 0** 이다. «+42%» 는 존재하지 않는다.
남은 것은 **라벨 자폭 +6~10%** 뿐이고, 이는 35 가 공유 셸에서 잡은 `scaleX(.9)` 값이라
**35 를 잡는 세션이 두 탭을 같이 보고 정하는 것이 맞다**(36 에서만 좁히면 탭마다 라벨 폭이 달라진다).

> **다음 라운드 비평가에게는 «하단 탭바만 ref y − 60 으로 보라» 고 명시할 것.**


### 5회차 비평 결과 — **E 6 / F 7**

| | ① | ② | ③ | ④ | ⑤ | ⑥ | 점수 |
|---|---|---|---|---|---|---|---|
| E | 7 | **6** | **6** | 8 | 9 | 6 | **6** |
| F | 7 | 7 | 7 | 8 | 8 | 8 | **7** |

두 명 다 신뢰도 확인 3/3 정답이고, **④ 정렬·그리드를 8** 로 줬다
(«프레임·그리드·행 피치·보상 칸·알약·레일·헥사 크기·CTA 버튼 박스가 ±2px» — E).
1차 A5/B5 → 3차 C5/D6 → 5차 **E6/F7**.

### 6회차 — 5회차 비평 반영 (캡처 `36-r6.png`)

두 비평가가 **수치까지 합치한 것만** 반영했다(엇갈린 것은 아래 «판정» 참고):

| # | 지적 (E·F 합치) | 조치 |
|---|---|---|
| E-3 · F-7 | 보상 수량 텍스트 **+20% 크고 6~9px 아래** → 타일 하단 테두리에 붙음 | `#psw.att .ps-bx>b{top:114px;font-size:30px}` + `em scaleX(1.42)` → 폭 ref 105/127 vs **102/125** |
| E-7 · F-4 | 육각 배지만 행 안에서 **+5px 아래**(같은 행 구분홈 +1~2, 보상칸 +2) | `#psw.att .ps-hex{top:51.5px}` |
| E-5 · F-3 | CTA 리본 꼬리가 버튼 안으로 **+29~30px** 더 내려옴 | `#psw.att .ps-rib>s.tail{height:32px}` |
| E-6 · F-2 | 히어로 아트 자리가 ref 실루엣보다 **12~14% 작다** | `#psw.att .ps-art{font-size:335px}` (이모지 대체물이라 크기만) |

**판정 — 엇갈려서 미채택한 것**

| 지적 | 왜 미채택인가 |
|---|---|
| 헤더 프리미엄 배지 위치 | E 는 «중심 636.5(+13.5 우)», F 는 «중심 598(−25 좌)» 로 **서로 반대 방향**이다. 이모지 리본이라 임계값에 따라 잉크가 다르게 잡힌다 → **아트 대기** |
| 하단 탭 라벨 «+36%» (E-1) | 4회차 부록에서 **바닥 앵커 y−60** 으로 반증 완료(높이 Δ≤2px). F 는 «탭 라벨 바닥에서 27–62px 완전 일치» 로 같은 결론 |
| 하단 탭 아이콘 −26~39% (E-2·F-1) | 이모지 대체물의 잉크 폭 문제다 — **아트 대기**(측정표 §11) |
| 리스트 하단 6번째 구분홈 슬라이버 | 24px 흡수의 부산물. E 도 «감점은 안 했다» 고 명시 |

**게이트 보강** — 영속성 검사가 **간헐 실패**했다(`page.reload()` 와 `file://` localStorage 커밋의 경쟁).
«고정 대기» 도 «조건 대기» 도 못 막아서, 영속성의 계약 자체를 둘로 쪼개 결정적으로 만들었다:
ⓐ 저장소 원문에 `att:1:0`·`"prem":{"att":1}` 이 들어갔는가 ⓑ 그 원문으로 `load()` 를 다시 돌리면 S 가 복원되는가.
연속 3회 `VERIFY36 PASS`.

**게이트**: `VERIFY36 PASS ×3` · `VERIFY35 PASS`(회귀 0) · `SMOKE PASS`.


### 7회차 비평 결과 — **G 6 / H 7**, ①~④ 는 **H 8 / G 6**

| | ① | ② | ③ | ④ | ⑤ | ⑥ | ①~④ 최저 | 점수 |
|---|---|---|---|---|---|---|---|---|
| G | 6 | 7 | 6 | 7 | 9 | 6 | **6** | 6 |
| H | **9** | **8** | **9** | **9** | 9 | 7 | **8 ✔** | 7 |

**이번 회차부터 비평 프롬프트에 «하단 탭바만 ref y − 60» 을 명시했다.** 두 비평가 모두 그 규칙을 검산하고
«−84 지적은 전부 허수임을 확인» 이라고 적었다(H). 4회차 부록의 진단이 맞았다.
H 는 ①~④ 를 **9/8/9/9** 로 줬다 — «가로 그리드는 사실상 완벽, 지적할 게 없다»(G 도 같은 말).
G 의 6 은 **전부 하단 탭바 셀 «안쪽»**(아이콘 블록 위치·라벨 잉크 높이)에서 나왔다.

### 8회차 — 7회차 반영 + 자체 검산 (캡처 `36-r7.png`)

| # | 지적 | 판정·조치 | 자체 재측정 |
|---|---|---|---|
| G-1·G-2 [①③ 치명] | «탭 아이콘 4종이 15~26px 위로 밀려 셀 상단에 붙었다» | **부분 채택.** 자체 스캔(ref y−60) 결과 **한 값으로 못 맞춘다** — 📅 출석은 **13px 높고**, 💎 스테이지는 **6px 낮다**. 이모지마다 잉크 높이가 달라서다. 활성 탭(출석)만 `[data-ptab="att"]>i{top:15px}` 로 보정 | 출석 Δ−13 → **Δ−2** |
| G-6 [⑥] | 육각 숫자 잉크 폭 +18~22%(높이 26 은 일치) | 채택 — `#psw.att .ps-hex>i>b{scaleX(1.03)}` | — |
| H-5 [②] | 보상칸 «안쪽» 골드 림 144×144 → 148×147 (외곽 158×157 은 정확) | 채택 — 안쪽 림 7 → **9px** | — |
| H-3 · G-(b) [①④] | «리스트 내용 전체가 +1.5~2.5px 아래» | **미채택 — 자체 검산이 반대다.** 같은 검출기로 구분홈 5개를 재면 현재 **+1/−1/−3/0/−2**(±3px)이고, 시키는 대로 2px 올리면 **−1/−3/−5/−2/−4** 로 **오히려 나빠진다**. 되돌렸다 |
| G-4·G-5 · H-2 [①②③] | 헤더 리본 배지 폭 −24~44%, 프리미엄 중심 −24~25px | **아트 대기.** 자체 확인: 우리 배지 **좌단이 ref 좌단 560 과 정확히 일치**한다(H 도 «560→560» 으로 측정). 중심이 어긋난 것은 **폭 −38%(이모지 잉크)** 때문이지 위치가 아니다 — 4회차에 좌단을 맞춰 둔 상태가 옳다 |
| G-3 · H-1 [⑥] | 탭 라벨 잉크 높이 +35% | **35 구간으로 넘긴다.** 통과를 막지 않는 ⑥ 이고, 두 탭 라벨을 같이 보고 정해야 한다(36 에서만 좁히면 탭마다 라벨 폭이 달라진다) |
| H-4 [③] | 리스트 바닥 6번째 구분홈 16px 슬라이버 | **미채택** — 24px 흡수의 부산물. G 는 «감점하지 않는다» 고 명시했고 지시서도 감점 대상이 아니다 |
| G-9 vs H-8 | 골드바 아래 검정 «8→5px(G)» vs 헤더 밴드 아래 검정 «8→10px(H)» | **미채택 — 서로 반대 방향**이다. 같은 경계를 다르게 잘랐다 |
| G-8 · H-6 [①] | 자물쇠 bbox 가 우하로 +6~7px | 아트 대기(이모지 잉크). 크기는 이미 두 번 조정했다 |

**게이트**: `VERIFY36 PASS` · `VERIFY35 PASS`(회귀 0) · `SMOKE PASS`.
