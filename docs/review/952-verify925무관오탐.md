# 952 — `verify925` [2d] «무관» 으로 새는 자 (`verify936.js`)

**세션** sess-1757-23537 (루틴 워커 D) · **1회차** · 2026-09-05
**제품 `index.html` 0줄** — 게이트 판별의 오탐이다. 바꾼 것은 `tools/` 셋(신설 2 · 수정 1).

---

## 1. 재현 (338 규칙 — 처방 전에 갈래부터 갈랐다)

```
node tools/verify925.js  →  VERIFY925 31/32  FAIL
  FAIL [2d] «무관» 으로 새는 자가 없다 — 화소를 재면 브라우저를 띄운다 — verify936.js
```

등재문이 적어 둔 갈래 둘:
* ⓐ `verify936` 이 정말 화소를 재는데 브라우저를 안 띄운다 → **936 이 낸 자 자신이 사슬 밖** (고치는 곳 = 그 자에 사슬 한 줄)
* ⓑ 925 의 «화소를 재는 자» 판별이 **주석·설명 문자열**에 걸린 오탐 (고치는 곳 = 판별기)

**ⓑ 가 맞았다 — 다만 자리가 등재문과 한 겹 다르다.**

| 물음 | 실측 |
|---|---|
| `verify936` 이 브라우저를 여는가 | **안 연다** — `require('playwright')` 0건 · `pwlaunch` 0건 (그래서 925 가 «무관» 으로 읽는다) |
| 그 자의 화소 토큰은 어디 있나 | `verify936.js:115` — `[6] 판별기 자기시험`에 먹이는 **인공 표본 `BASE`**(문자열 배열)의 한 줄. `pin.classifySource(BASE, 't.js')` 로 간다 |
| 주석이었나 | **아니다.** 세는 쪽(`probe922.census` → `raster907.stripComments`)은 주석을 이미 벗기고 본다 — 주석만 가진 자는 애초에 인구에 안 든다 |
| 실제로 재는 일은 어디서 도나 | `verify936` [5] 가 자식 `probe936` 을 띄우고, 그 자가 다시 `verify710` 사본을 띄운다 — **띄우는 자는 사슬 안**(`verify710:chain`) |
| 931 탓인가 | 아니다 — 등재문대로 `308d4da^` 트리에서도 31/32 동일 |

⇒ 결손은 **«적어 둔 코드» 를 «도는 코드» 로 센 것**이다. 사슬을 지나라고 요구할 대상 자체가 없는 자리였다.

## 2. 처방 — 인구를 줄이지 않고 갈래를 하나 더 세웠다

918/922 의 `RE_PX`(`/\.screenshot\(|getImageData/`)는 **걷개를 켤지 정하는 자리**다. 거기서는 넉넉한 쪽이
안전하다(안 재는 자에 걷개가 켜져도 무해). 그래서 **`shell918` 은 한 글자도 안 건드렸다** — 인구(326자)는 그대로다.
대신 925 [2d] 가 «의무» 를 물을 때만 갈래를 다시 가른다.

**신설 `tools/quote952.js`** — `strip()` 한 번의 훑기로 **주석 · 문자열/템플릿 리터럴의 속 · 정규식 리터럴의 속**을
같은 길이의 공백으로 지운다(줄·칸 보존). `pixelKind()` 는 그 결과로 셋을 가른다:

| 갈래 | 뜻 | 사슬 의무 |
|---|---|---|
| `code` | 도는 코드에서 잰다 | **그대로 있다** |
| `quote` | 문자열·템플릿·정규식 표본으로 적어 두기만 했다 | 붙을 자리가 없다 |
| `no` | 세는 쪽이 애초에 안 세는 자(주석만 가진 자 포함) | — |

⚠ **템플릿의 `${ … }` 안은 코드라 남긴다** — 거기서 진짜로 재는 자를 놓치면 이 부품이 곧 구멍이다(`verify952` [1f]).
⚠ 규칙의 주인은 옮기지 않았다 — «화소를 재는가» 는 `shell918.RE_PX` 를 **읽는다**(402 «사본을 지운다» · `verify952` [2f]).

**`tools/verify925.js`** — [2d] 를 «**도는 코드로** 재는데 안 띄우는 자 0» 으로 바꾸고, 갈래를 눈에 보이게 두 항을 더했다:
* `[2g]` «무관» 인 자는 한 자도 빠짐없이 «적어 두기만 한 자» 다 — 이름까지 찍는다(오늘 `verify936.js` 하나)
* `[2h]` 그 가름이 «무엇을 해도 quote» 가 아니다 — 도는 코드 표본은 `code` 로 잡힌다

## 3. 무르게 풀지 않았음 (세 겹)

1. **인구 불변** — `[3a]` 화소를 재는 자 327자(922 규모 그대로) · `[3c]` **이 갈래로 초록이 된 자는 오늘 하나**(`verify936.js`)뿐이고,
   나머지 quote 13자는 **원래부터 사슬 안**이었다(`[3d]` chain 12 · inject 1) — 갈래가 남의 의무를 지운 자리가 0 이다.
2. **되돌림 시험 `[R1]~[R3]`** — `verify936` 의 그 표본 한 줄에서 **따옴표만 걷으면** 곧바로 `code` ∧ `none` 이 되어
   925 [2d] 가 다시 빨개진다. 원본만 초록이다.
3. **구멍 시험 `[1f]`·`[R5]`** — 통째로 지우는 사본을 대면 도는 코드까지 quote 로 읽힌다(그 꼴이 아님을 [1f] 가 못박는다) ·
   `strip` 은 **지우기만** 한다(`[4b]` 전 자 1434개에서 길이·개행 보존 · 글자를 새로 만들지 않는다).

## 4. 점수

| 자 | 전 | 후 |
|---|---|---|
| `verify925` | **31/32 FAIL** | **34/34 ALL PASS** ([2g]·[2h] 신설) |
| `verify952` | — | **29/29 신설** |
| 회귀 `verify922` | 28/28 | 28/28 무수정 |
| 회귀 `verify931` | 26/26 | 26/26 무수정 |
| 회귀 `verify936` | 26/26 | 26/26 무수정 |
| 회귀 `verify907`·`verify918`·`verify949`·`verify937` | 15/15 · 24/24 · 26/26 · 38/38 | 전부 무수정 |
| `SMOKE` | PASS | PASS |

⚠ **곁다리 — `verify939` 26/32 는 내 것이 아니라 환경이다**(338·344 규칙). 빨간 [C1] 이 `scan885e.py` 의
«scipy 없음 — pip3 install scipy» 코드 2 였고, ROUTINE [6] 이 적어 둔 대로 `pip3 install scipy` 한 줄 뒤 **32/32 PASS**.
조건부 의존 둘(scipy·soundfile)이 상시 준비 줄에 없다는 938 의 설계 그대로다 — 등재하지 않는다.

## 5. 다음 워커에게

* «화소를 재는가» 를 **의무**로 묻는 자를 새로 짤 때는 `quote952.pixelKind()` 로 갈래를 먼저 가른다.
  세는 쪽(`RE_PX`)은 일부러 넉넉하다 — 그 넉넉함을 의무에 그대로 쓰면 표본을 든 자가 빨개진다.
* 반대로 **걷개를 켜는 쪽**에는 이 가름을 쓰지 마라. 거기서는 «적어 두기만 한 자» 에 걷개가 켜져도 무해하고,
  좁히면 `page.evaluate("…")` 처럼 문자열로 재는 자를 놓친다.
