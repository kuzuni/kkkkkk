# 852 — 197 주석이 보유 축(`ownVal`)을 아직 «종마다 하나씩 곱하는 축» 이라고 적는다 (724 이관 누락)

- **세션** sess-0313-21352 (루틴 워커 A) · **1회차** · 2026-09-03
- **티어** «잠복(주석 부패 · 선언 옆의 낡은 말)» · 837 과 같은 종(코스튬 밖) · 지시서 [3]-**(가)** — 비평가 없음
- **제품 동작 0줄** · 레이아웃 Δ0px · 밸런스 계수 0줄. 고친 것은 **주석 두 자리**다.

---

## 1. 재현 — 처방 전에 «지금 무엇이 참인가» 부터 (338 규칙)

등재문은 «말만 고치는 자리» 였지만, 고쳐 적을 말 안에 **수치**(종수 · 배수 · 기울기 효과)가 들어간다.
그 수를 손으로 계산해 적으면 **그 순간 또 하나의 사본**이고 다음 모델 변경에서 똑같이 썩는다 —
852 가 고치는 병이 정확히 그것이다. 그래서 `tools/probe852.js` 를 신설해 **제품 함수에게 직접 물었다**.

```
[1] 보유 축의 카테고리 «안» 결합 — 2표본
    skill  실측 1.0472 | Σ 기대 1.0472 · Π 기대 1.0478   ⇒ Σ
    equip  실측 1.0444 | Σ 기대 1.0444 · Π 기대 1.0449   ⇒ Σ
    pet    실측 1.0472 | Σ 기대 1.0472 · Π 기대 1.0478   ⇒ Σ
[2] 종수 — 낡은 주석의 «장비 36 + 스킬 27 + 펫 27»(= 90)
    장비 108(그중 무기 36) · 스킬 27 · 펫 35  ⇒ 합 170
[3] 전 종 보유 · Lv 0 — 카테고리 배수
    스킬(atk)        ×3.19    (옛 Π 모델이면 ×7.89)
    장비 무기(atk)    ×5.73    (옛 Π 모델이면 ×69.44)
    펫(gold)         ×5.04    (옛 Π 모델이면 ×39.87)
[4] 기울기 0.180 → 0.012 (Lv 100 · 스킬 전 종)
    지금(Σ) : ×42.61 → ×5.82   = 13.7%
    옛 Π    : ×7.64e+9 → ×71.89 = 9.41e-9
[5] 계단 — 보유 축에 gWear 를 태우면
    스킬 Σ 2.19 → 31.46 (×14.4) · 장비 Σ 14.18 → 472.71 (×33.3)
    GRADE.mul  1 · 1.5 · 2.3 · 3.6 · 6 · 10 · 16 · 26
    GRADE.wear 1 · 3 · 9 · 27 · 81 · 243 · 729 · 2187
PROBE852 10/10 PASS
```

⚑ **재현이 등재문보다 한 겹 아래를 찍었다 — 같은 문장의 «종수» 도 낡은 수다.**
등재문은 «하나씩 곱하는» 이라는 **말**만 지목했는데, 그 말이 낀 괄호 «(장비 36 + 스킬 27 + 펫 27)» 과
그 합으로 쓴 지수 «(1+x)^**90**» 도 지금 값이 아니다(제품은 **108 + 27 + 35 = 170**).
⇒ 그래서 이 작업의 자는 낱말이 아니라 **종수 자체**를 매 실행 제품에 다시 묻는다([B1] 다섯 항).

⚑ **[3]·[4] 의 «옛 Π» 열이 등재문을 못박는다** — Lv100 스킬 전 종의 Π 값 **×7.64e+9** 은
`probe724` 가 수리 전 커밋에서 찍어 둔 `skAllL100.prod` **7.6420e+9** 과 같은 수다(두 자가 독립으로 일치).

## 2. 고친 자리 — 둘 (전부 주석)

| # | 자리 | 낡은 말 | 새 말 |
|---|---|---|---|
| ① | `LV_STEP`/`lvMul` 선언 위 «197 지시 ②» 블록 | «보유는 `bonus()` 가 … 전 종(장비 36 + 스킬 27 + 펫 27)에 **하나씩 곱하는** 축이라 기울기를 같이 낮추면 «(1+x)^90» 이 통째로 줄어 **전 구간 파워가 무너진다**» | 724 가 근거를 갈았다 + 결합 경로(`catSum`/`catApply`) + **probe852 가 찍은 인하 실측**(0.180 → 0.012 ⇒ ×42.61 → ×5.82) · 옛 `sim197` 수는 «724 이전» 딱지로 보존 |
| ② | `ownValAt` 선언 위 «197 gMul/gWear» 블록 | «보유 효과는 … **하나씩 곱하는** 축이라 여기에 계단을 태우면 «(1+계단)^N» 으로 **지수가 두 번 걸려** 터진다» | «카테고리 «안» 에서 **더한 뒤 한 번만 곱하는**» 축(724) + 계단을 태우면 **Σ 의 항이 부푼다**(스킬 ×14.4 · 장비 ×33.3) + 종수는 probe852 에게 묻는다 |

**손대지 않은 것 — 그게 맞다:**
- **값·식 0줄.** `LV_STEP`·`lvMul`·`lvWear`·`ownValAt`·`gMul`·`gWear`·`bonus()` ①~③·`catApply` 전부 무수정.
  [A11]~[A18] 여덟 항이 그것을 못박고, 통과선은 **`verify724` 45/45 무수정**이다.
- **살아 있는 논거는 살렸다** — «91 도감이 «축별로 합산한 뒤 1회 곱» 으로 푼 것과 같은 이유» 는
  724 뒤에도 참이다(724 가 그 규칙을 보유 축까지 **넓힌** 것이므로 오히려 더 참이다). [A9] 가 지킨다.
  837 이 194 의 «(1+2.0)^50 ≈ 7e23» 을 살린 것과 같은 판단이다.
- **결론도 살렸다** — «보유 축 기울기 0.18 은 199 의 몫» 은 그대로다(선형이어도 배수의 **86%**가 날아간다).
  바뀐 것은 **왜** 그런가지 **무엇을 할까**가 아니다. [A5] 가 지킨다.
- **`sim197` 의 3.48e47 → 6.54e18 은 지우지 않고 «724 이전» 딱지만 붙였다** — Π 모델 위의 수라
  지금 값은 아니지만 **내가 확인할 수 없는 남의 역사**다(837 이 197 의 ×45.4 를 다룬 것과 같은 규약).
  같은 이유로 «장비 36» 이 옛 장비 총수인지 무기 한 부위인지도 **단정하지 않았다** — 딱지만 붙였다.
- `bonus()` 머리의 724 주석(23048~)은 이미 옳게 적혀 있다. **겹쳐 적지 않았다.**

## 3. 게이트 — «지운다» 가 아니라 «다시 못 썩게 한다»

⚑ **«하나씩 곱하는» 이라는 낱말만 지웠으면 852 는 절반이다.** 다음 모델 변경에서 같은 자리가 똑같이 썩는다.
`tools/verify852.js` **38/38 신설**은 낱말이 아니라 **말과 제품을 매 실행 맞춰 본다**:

- **[A] 정적 18항** — 옛 주장 0건 + 새 말이 724·결합 경로·출처(`probe852`)를 밝힌다 + 살린 논거·결론이
  아직 있다 + **값·식 0줄**(선언 8자리). ⚠ 옛 주장은 **런타임에 조립**한다(696 함정).
  ⚠ 새 주석은 «옛날에는 그랬다» 로 그 낱말을 **인용**하므로, 자는 낱말이 아니라
  **옛 주장 문장 그대로**를 찾는다 — 낱말 금지로 짰으면 내 주석이 자기 자에 걸렸다.
- **[B] 실측 16항** — 주석이 «지금 값» 이라고 적은 **종수 다섯 · 기울기 둘 · 배수 둘 · 계단 둘 · GRADE 꼭대기 둘**을
  헤드리스로 띄운 제품에게 다시 물어 대조한다. 모델이 또 바뀌면 **말이 먼저 빨개진다.**
- **[R] 되돌림 4항** — R1 ① 블록을 옛 주장으로 → [A1] 빨강 · R2 ② 블록을 옛 주장으로 → [A6]·[A7] 빨강 ·
  **R3 «말은 새것, 종수만 옛 90»** → [B1a]·[B1d] 빨강(852 가 막으려는 병 그 자체) ·
  **R4 제품을 Π 로 되돌림** → [B0] 빨강(말이 아니라 제품이 틀렸을 때도 짖는다).

## 4. 결과

- `verify852` **38/38 신설** · `probe852` **10/10 신설**
- **이관 0종** — 통과선인 `verify724` **45/45 무수정**(제품 0줄의 가장 짧은 증거)
- 회귀 무수정: `verify197` 28/28 · `verify837` 31/31 · `verify194` 54/54 · `probe724` 14/14 · **SMOKE PASS**
- 곁다리 등재 **0건**
