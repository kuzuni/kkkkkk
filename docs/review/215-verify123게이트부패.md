# 215 — `tools/verify123.js` 1/58 FAIL: 아레나 잠금 안내 단언이 149(팝업→토스트) 이후 굳어 빨간 채 방치

> 워커 D · `sess-0758-15222` · 2026-08-27T07:58Z~ · 지시서 [3]-**(가) 기계적 작업 — 비평가 없음**
> (레퍼런스 대조가 아니라 «게이트가 왜 빨간가» 를 가르는 작업이다. 레이아웃은 한 값도 건드리지 않았다.)

**결론: `index.html` 0줄. 게임이 옳고 게이트가 낡았다.** 211·212·213[5]·214 와 같은 판정이다.

---

## 1. 재현

작업 전 트리에서 그대로 재현된다(등재문의 «213 이 `8b02596` 에서 확인» 과 같은 값):

```
[3] 아레나 카드 → 04 세부 팝업
  ✗ 스테이지 미달이면 세부 팝업 대신 «해금» 안내 — got "false/"
VERIFY123 FAIL — 1/58
```

`got "false/"` 는 **두 값이 모두 빈 것**이다 — `#dgdw.on` = false(세부 팝업은 옳게 안 열렸다)이고,
`.modal.on .mhead` 의 본문이 **빈 문자열**. 즉 «팝업이 안 열린다» 는 절반은 이미 초록인데,
«대신 안내가 뜬다» 를 **모달에서 찾고 있어서** 통째로 빨갛다.

## 2. 원인 — 149 가 그 물음을 원리적으로 false 로 만들었다

`index.html` 20142행(아레나 잠금 분기)은 이렇게 되어 있다:

```js
if(arenaLocked()){
  notify('🔒 ' + ARENA.n + ' — 스테이지 <b>' + ARENA.open + '</b> 필요 (현재 ' + S.best + ')');
  return;
}
```

**149**(주인 지시 «부족 알림 같은 건 팝업 말고 토스트로»)가 `notify()` = `fxToast` 로 뒤집은 뒤라
이 경로는 **모달을 한 번도 안 만든다.** 게이트가 `.modal.on` 을 보는 한 답은 영원히 false 다.

실측(이번 회차 게이트 로그):

```
✓ 스테이지 미달이면 세부 팝업 대신 «해금» 안내 (149 토스트 — 스테이지 5 필요)
    — false/🔒 아레나 — 스테이지 5 필요 (현재 1)
```

213 이 `probe213` 으로 잰 문구(«🔒 아레나 — 스테이지 5 필요 (현재 1)»)와 **글자까지 동일**하다.

## 3. 고친 것 — 지우지 않고 **이사**(LESSONS 185-④)

묻는 것(«세부 팝업이 안 열리고, 화면이 해금 조건을 말하는가»)은 그대로 두고 **재는 자리만** 옮겼다.
처방은 213 이 `fnchk97` [5] 에서 세워 둔 것을 그대로 베꼈다(`docs/review/213-레이드기록알약침범.md` §2).

- `.modal.on .mhead` → **`#fxl .fx-toast`**.
- **기대 문구를 리터럴로 박지 않는다**(185-①) — 클릭 직전에 `ARENA.open` 을 읽어
  「스테이지 <필요값>」 이 문구에 들어 있는지 본다. 숫자만 세면 안내문 끝의 «(현재 1)» 에 걸린다.
- **대기 300ms**(185-⑥) — 토스트 수명은 760ms 퇴장 시작 · 1060ms 제거라 500ms 는 위태롭다.
- **«잠금 안내는 팝업이 아니다» 를 단언으로 같이 박았다** — 팝업으로 되돌아가면 그 자리에서 잡힌다.
  (그래서 항목 수가 58 → **59** 로 하나 늘었다.)
- 뒷정리도 모달만 지우던 것에 **토스트 제거**를 같이 넣었다(다음 절이 남은 토스트를 보지 않게).

## 4. 되돌림 시험 — 단언이 실제로 무엇을 보는가

새 단언이 «항상 초록인 항등식»(LESSONS 212-①)이 아님을 세 방향으로 확인했다. 셋 다 `index.html` 을
임시로 되돌려 돌리고 곧바로 `git checkout -- index.html` 했다.

| | 되돌린 것 | 기대 | 결과 |
|---|---|---|---|
| **N1** | 안내 문구에서 「스테이지 \<필요값\>」 제거(«아직 열리지 않았습니다») | 해금 안내 단언 FAIL | **FAIL 1/59** ✔ |
| **N2** | 잠금 가드 무력화(`if(false && arenaLocked())`) → 잠긴 채 세부 팝업이 열림 | 같은 단언 FAIL (`got "true/"`) | **FAIL 1/59** ✔ (`true/`) |
| **N3** | 149 이전처럼 안내를 **모달로도** 띄움 | «팝업이 아니다» FAIL | **FAIL 1/59** ✔ (`got true`) |

N1·N2 는 같은 단언의 **두 절**(문구 · 팝업 안 열림)을 각각 때리고, N3 는 새로 박은 단언만 때린다 —
세 개가 서로 다른 항목을 잡으므로 단언이 겹쳐 있지 않다.

> N3 첫 시도는 `.modal` 클래스로 요소를 찾았다가 `undefined.classList` 로 죽었다 —
> 이 저장소의 공용 모달은 **`#modal` 하나**이고 `.modal` 클래스 요소는 없다.
> (`document.querySelector('.modal.on, #modal.on')` 처럼 **둘 다** 보는 게이트 관례는 그대로 두었다 —
> 다른 화면이 `.modal` 을 쓸 수 있고, 없으면 그냥 안 걸린다.)

## 5. 게이트

- **`node tools/verify123.js` → VERIFY123 PASS 59/59** (수정 전 1/58 FAIL)
- **`node tools/smoke.js` → SMOKE PASS** · 콘솔 에러 0
- `index.html` **0줄 변경** (`git status` 로 확인)

## 6. 남은 것

- 등재문이 «게이트 부패 3연발» 로 묶어 둔 것 중 `verify72`·`fnchk97` = 213, `verify94` = 214,
  `verify123` = 이번 것으로 **셋 다 닫혔다.** 남은 것은 **217**(`verify116` 31/36 — 150 계열)이다.
- 216(1e16 알약 폭)은 **188·199 선행** 대기라 이번 작업 범위가 아니다.
