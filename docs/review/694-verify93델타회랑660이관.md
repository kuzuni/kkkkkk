# 694 — `tools/verify93.js` 실패 3건(23/26) → **실패는 다른 한 항이었다**: [7] 델타 회랑의 660 이관

**세션** sess-1548-9285 (루틴 워커 D) · **1회차** · **2026-09-01**
**제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/verify93.js` 한 절이다.

---

## 1. 등재문과 현실 — 세 항은 이미 초록이었다

등재문(619 17회차 sess-1313-23249 관측)은 **3건 실패(23/26)** 였고 실패 항은
「[4-a] 비트 3회(**≥4**)」·「왕복(fxPunchN) 3회(**≥4**)」·「[4-d 전제] 다이아 비트 3회(**≥4**)」 —
셋 다 «표본이 한 개 모자란다» 였다.

이 회차 첫 실행은 **25/26** 이고 **그 3항은 전부 초록**이다. 이유는 저장소 안에 이미 적혀 있다 —
`tools/verify93.js` 352행:

> ⚑ **644(2026-09-01) — 하한 4 → 사양 밴드 3..6.** 값을 무르게 푼 것이 아니라 **사양으로 옮긴 것**이다.
> … 543 이후 UI 발 슬롯이 4 → 3 이 됐고 3 은 이 화면의 **사양 하한**이다(`verify58` [2]·`verify543` [D] 가 같은 수를 쓴다).

즉 **644 가 이미 이관을 끝냈다.** 등재 세션은 그 이관 **전의 자**를 돌렸다 —
654 가 남긴 교훈(«회귀는 pull 뒤에 돌려라», `verify93` 3항이 «rebase 전의 낡은 자» 였던 그 자리)의 **재발**이고,
이번에는 그 낡은 관측이 **표에 T1 로 등재까지** 됐다. ⇒ **LESSONS 694-①.**

빨간 것은 **한 항, 그리고 다른 항**이었다:

```
[7] 씬 C 델타 회랑 — 훈련 카드기준 y275~396 (58 24·27·30회차)
  ✗ 델타 플로터 표본 0 — 씬 C 가 안 났다 (카드 3개 첫 카드 y1371)
```

---

## 2. 재현 — `tools/probe694.js` (338 규칙: 처방 전에 제품에게 묻는다)

실패문이 스스로 두 갈래를 말하고 있다: «씬 C 가 **안 난 것**» 인가, «씬 C 는 났는데 **델타만 없는 것**» 인가.
전자면 자의 하네스 결함이고, 후자면 660(주인 지시 «훈련·단련·룬 숫자 플로터 폐지»)의 **이관 누락**이다.

`node tools/probe694.js` → **10/10 PASS**

| 물음 | 실측 |
|---|---|
| 씬 C 는 났는가 | **났다** — 훈련 카드 첫 발에 **골드 45 지출**(판정에서 읽음 · 연출과 무관) |
| 그 자리에 무엇이 뜨는가 | **아이콘 버스트 14알**(`.fx-cic` · 660 이 세운 부품) |
| 델타는 | **0장** |
| 부품 `fxDelta` 는 죽었는가 | **아니다** — 50 코스튬 [강화]에서 **1장** · 카드기준 dy **129~209** · dx 84 |
| 소스 — 누가 델타를 부르는가 | `fxUpOk` 호출부 **6곳** 중 텍스트를 넘기는 곳 **2곳**(33692·33818 = 둘 다 코스튬) |
| 훈련 두 자리 | 셋째 인자가 **`null`**(34679·34739) — 660 주석 «숫자 플로터 폐지» 와 정확히 일치 |

⇒ **제품이 옳다.** 종전 [7] 은 «주인이 없애라고 한 것이 **있어야** 통과하는 자» 가 돼 있었다.

⚠ **1회차에 «살아 있는 자리» 를 한 번 잘못 골랐다** — 08 세부 팝업 [강화]는
`bindUpHold` 의 `fx: b => fxUpOk(…, b)` 인데 그 `b` 는 **버튼 노드**(27958 `o.fx(btn)`)라
`fxUpOk` 의 셋째 인자 `txt` 가 아니다 ⇒ **거기서는 델타가 원래부터 안 뜬다.**
«08 도 델타 계열» 은 주석이 만든 착각이고, 소스 전수(6곳)가 그것을 정정했다. ⇒ **LESSONS 694-②.**

---

## 3. 처방 — 자리를 비우지 않는다(333 · 660 이 `verify488`·`verify583` 에서 쓴 규약 그대로)

| 항 | 종전 | 694 이관 |
|---|---|---|
| [7] | 씬 C 델타가 훈련 카드기준 **y275~396** 회랑 안 | — (호스트가 사라졌다) |
| [7-a] | — | **전제 신설** — 씬 C 는 났다(훈련 강화로 **골드가 나갔다**). 없으면 «델타 0장» 이 «씬이 안 났다» 와 구별되지 않는다 |
| [7-b] | — | **방향을 뒤집었다** — 훈련 «+n» 플로터 **0장**(660 이 되돌아가면 빨개진다) |
| [7-c] | — | **양성항 신설** — 그 자리를 **아이콘 버스트**가 대신한다(≥3알). 없으면 «660 이 통째로 사라져도 초록» 이다 |
| [7-d~f] | — | **부품 양성항 신설** — `fxDelta` 는 살아 있다: 50 코스튬에서 뜨고 ⓐ 호스트 **안에서 출발**하고 ⓑ 세로 여정이 **선언과 같고** ⓒ 가로가 호스트 폭 안 |

⚑ **새 임계는 하나도 안 만들었다.** [7-d~f] 의 봉투는 두 곳에서만 온다 —
ⓐ **호스트 카드 상자**(제품이 그린 것: 코스튬 카드 168×171) · ⓑ **`@keyframes fxDelta` 선언**(0% −4px → 100% +80px = **84px**).
종전 회랑 275~396 은 **훈련 카드(326×510)의 «아이콘 아래 ~ 버튼 위» 띠**라 코스튬 카드에 옮겨 적을 수 없다 —
옮겨 적었으면 그것이야말로 눈대중 임계였다.

씬이 하나(**씬 D = 50 코스튬 [강화]**) 늘었다. 버스트 수집기(`.fx-cic` 도장)는 **씬 C·D 에서만** 돈다 —
씬 A·B 의 타이밍 축은 샘플러 부하에 물리므로(570) 거기서는 한 줄도 더 안 돌게 했다.

---

## 4. 무르게 풀지 않았음 — `tools/verify694.js` **13/13 PASS**(되돌림 시험 신설)

334 규약(«무르게 푼 수리가 아님은 되돌림 시험이 못박는다»)대로, 결함을 하나씩 주입한 사본으로
**그 항이 실제로 빨개지는가** 를 물었다(570 이 같은 자에 세운 틀 · 사본 이름에 pid = 646·648 처방).

| 주입 | 빨개져야 하는 항 | 결과 |
|---|---|---|
| §R-a **660 되돌림** — 훈련 첫 발이 다시 «+1» 을 띄운다 | [7-b] | ✗ **23프레임·표본**(초록 → 빨강) |
| §R-b **버스트 폐지** — 알갱이가 `.fx-cic` 를 안 단다 | [7-c] | ✗ **0알** |
| §R-c **부품 사망** — `fxDelta` 가 아무것도 안 만든다 | [7-d] | ✗ 씬 D 델타 표본 0 |
| §R-d **판정 무력화** — 훈련이 골드를 안 쓴다 | [7-a] | ✗ **골드 0 지출** |
| §R-e **자리 이탈** — 버스트를 누른 카드가 아니라 **HUD 에서** 터뜨린다 | [7-c2] | ✗ **밖 4알 / 14알**(702 가 지킨 위치 축) |

⚠ **§R-e 는 1회차에 헛돌았다** — `fxBurst` 안에서 **호스트 상자만** 300px 옆으로 밀었더니
[7-c2] 가 **안 빨개졌다**(밖 0알). 중심은 `p = fxPt(t)` 로 따로 오고 그 상자는 «가둠»(bx0..bx1)일
뿐이라, 밀린 상자와 원래 카드가 아직 겹쳐 입자가 그 겹침 구간에 눌러앉은 것이다. 자리를 묻는
주입은 **발원 자체**를 옮겨야 한다(`upFx` 의 `fxBurst(fxBurstAt(el), …)` 인자). ⇒ **LESSONS 694-④.**

§R-d 가 이 이관의 핵심을 지킨다 — 그것이 없으면 «씬이 안 났다» 를 «폐지됐다» 로 읽는
**헛초록**이 정확히 종전 실패문의 모양으로 되살아난다.

---

## 5. 게이트 · 회귀

* `node tools/verify93.js` → **VERIFY93 33/33 PASS** (종전 25/26 · [7-c2] 포함)
* `node tools/verify694.js` → **VERIFY694 13/13 PASS** (신설 · §0 8줄 + 주입 5축)
* `node tools/probe694.js` → **PROBE694 10/10 PASS** (신설)
* `node tools/verify660.js` → **55/55 PASS** (무수정 · 제품 0줄의 가장 짧은 증거)
* `node tools/smoke.js` → **SMOKE PASS**

---

## 6. 같은 결함이 **702 로도 등재돼 있었다** — 같이 마감

이 회차 중에 워커 A(sess-1518-20796, 671 1회차 곁다리)가 **같은 항**을 `702` 로 독립 등재했다
(«[7] 씬 C 델타 플로터 표본 0 — 659·660 뒤에 남은 게이트 부패»). 처방 방향도 같았다(333 — 뒤집고,
버스트를 새 표본으로). 그 행이 특별히 요구한 «**위치 축을 잃지 마라**» 를 받아
**[7-c2]** 를 더했다 — «버스트가 **누른 카드 상자 안**에서 뜬다»(밖 0알 / 14알 · 카드 326×510) ·
되돌림 **§R-e**(발원을 300px 옆으로 밀면 빨강)가 그것을 못박는다.
**702 행도 이 회차에서 같이 마감**했다 — 안 그러면 다음 워커가 **이미 끝난 자리**를 선점한다
(691 이 문서화한 «한 번호가 두 작업» 의 거울 = «두 번호가 한 작업»). ⇒ **LESSONS 694-③.**

## 7. 곁다리 관측 1건 — **711 등재**(등재 경쟁으로 707 → 711)

씬 D 실측에서 **코스튬 델타가 카드 아래로 38px 새어 나간다**(카드 168×171 · 델타 중심 dy **129 → 209**).
훈련에서는 58 이 24·27·30회차를 들여 상승 구간을 카드 안 «아이콘 없는 띠» 에 가뒀는데,
코스튬 카드는 그 띠가 없고 **아무도 잰 적이 없다**. 실재 결함(아래 카드 글자와 겹침)인지 사양인지
**미판정**이라 [7-f] 는 가로만 묻고 세로 하단은 안 묻는다 — 그 판정을 711 행에 넘긴다.
