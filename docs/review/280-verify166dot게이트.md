# 280 — `tools/verify166.js` [0] «`class="dot"` 렌더 10곳» 리터럴 게이트 부패

- 세션 `sess-0059-4736` (워커 D) · 2026-08-28 · **1회차**
- 티어 **T1 버그(게이트)** · 지시서 **[3]-(가) 기계적 작업** → 레퍼런스 대조가 아니라 «소스 → 성질» 판정이라 **비평가 없음**
- **`index.html` 0줄** — 제품은 옳고, 낡은 것은 게이트의 숫자뿐이었다.
- 결과: **VERIFY166 37/38 FAIL → 41/41 PASS** (3회 연속 순차 실행 3/3 PASS) · **SMOKE PASS**

---

## 1. 무엇이 빨갰나

```
FAIL src `class="dot"` 렌더 10곳 (166 이전 11곳 — 배수 탭 1곳 제거)  [11곳]
```

`(code.match(/class="dot"/g)||[]).length === 10` 한 줄. 실측은 11곳이다.

## 2. 늘어난 1곳은 «탑 카드» 다 — 등재문의 «던전 서브탭» 은 오기

등재문은 11곳의 내역을 «도감 탭 6 · 던전 카드 1 · **던전 서브탭 1** · 측정장 1 · 아레나 1 · 23 훈련 카드 1» 로 적었는데,
**던전 서브탭 배지는 `class="bdg"` 라 이 정규식에 애초에 안 걸린다**(게이트 [4] 가 `.bdg` 로 따로 본다).
실제 전수는 이렇다:

| # | 자리 | index.html | 갈래 | 조건 |
|---|---|---|---|---|
| 1~6 | 21 도감 탭 6칸 | 13020~13025 | **정적 마크업** | CSS `.cltab.alert>s.dot` 이 껐다 켠다 |
| 7 | 03 던전 카드 | 22847 | 조건부 렌더 | `!lock && left>0 && cp()>=d.req(f)` |
| 8 | **탑 카드(시련·절망)** | 22884 `renderTowerPage()` | 조건부 렌더 | `cp() >= t.req(f)` |
| 9 | 측정장 카드 | 23025 | 조건부 렌더 | `!lock && left>0 && !(b.dps>0)` |
| 10 | 아레나 카드 | 23101 | 조건부 렌더 | `!lock && canGo && rec.w+rec.l===0` |
| 11 | 23 훈련 카드 | 26396 | 조건부 렌더 | `c.ok` |

8번이 **209 가 신설하고 210 이 «시련의 탑 · 절망의 탑» 둘로 늘린 탑 카드**다. 202 가 «verify166 32/32 PASS» 를 남긴
뒤에 들어왔고, 조건부 렌더라 **166 이 지키는 성질(«조건 없이 상시 점등» 0건)은 한 번도 안 깨졌다** — 숫자만 굳어 있었다.
(얕은 클론이라 원인 커밋은 이번에도 못 짚었다. 대신 «어느 자리인가» 를 위 표로 확정했다.)

## 3. 처방 — 185-① «리터럴을 자리 목록에서 파생» + 성질 단언

숫자 한 줄을 **두 항목**으로 바꿨다. 자리는 두 갈래뿐이라는 사실 자체를 게이트에 적었다.

```js
const dotStatic = /* line 이 <div class="cltab" data-ct= 인 것 */
const dotCond   = /* line 이 (<조건> ? '…class="dot"…' : '') 꼴인 것 */
const dotLoose  = /* 둘 다 아닌 것 = «조건 없이 늘 찍히는 배지» = 166 이 없앤 그 병 */

ok('정적 자리 = 도감 탭 칸 수 (`#collTabs .cltab` 에서 파생)', dotStatic.length === clTabN && clTabN > 0);
ok('나머지는 전부 조건부 렌더 — «상시 점등» 0건',              dotLoose.length === 0);
```

- 숫자가 다시 늘어도 자리가 ⓐ 정적(도감 탭) · ⓑ 조건부 중 하나면 **통과한다** — 즉 «탑이 셋이 됐다» 로는 안 빨개진다.
- 상시 렌더가 하나라도 생기면 **그 줄을 이름으로 찍으며** 빨개진다.
- 한계를 주석에 명시: ⓑ 판정은 «한 줄 안에 `? '` … `: ''`» 로 본다(현재 5곳 전부 한 줄). 여러 줄 삼항을 새로 쓰면
  «분류불가» 로 빨개지므로, 그때는 한 줄로 모으거나 판정을 넓힌다. — **거짓 통과가 아니라 거짓 실패 쪽으로 기울인 것이 의도다.**

## 4. 곁가지 2건 (같은 파일 · 같은 병)

### (1) 탑 카드가 **감사 밖**에 있었다 → [6b] 신설

[0] 이 10 에서 굳은 근본 이유가 이것이다. 배지가 하나 늘었는데 **거동을 보는 절이 없어** 숫자만 빨개졌다.
166 의 자기 규약(«새 배지를 만들면 감사에 그 호스트를 추가한다») 대로 넣었다 —
요구치 `TOWERS[i].req` 를 갈아 끼워(`cp()` 는 `const` 화살표라 스텁 불가, [3] 과 같은 요령) 두 상태를 만들고 복구한다.

```
ok 탑 카드 — 요구 전투력 미달이면 dot 0 (②)      [dot 0]
ok 탑 카드 — 요구 충족이면 탑 수만큼 켜진다 (③)  [dot 2 / 탑 2]
```

### (2) [4] 던전 서브탭 절이 «뜨고 지는 FAIL» 이었다

1회차 첫 실행에서 `던전 서브탭 배지 — 입장 횟수 0 이면 꺼짐 [block / .alert true]` 로 한 번 빨개졌다(재실행 3회는 전부 PASS).
**플레이크가 아니라 경합이다.** 서브탭 `.alert` 는 `renderDunPage()` 가 아니라 **`renderUI()`(0.35초 주기)** 가 건다.
[3]-(c) 가 입장 횟수를 0 으로 만든 직후의 «켜짐» 은 (b) 의 «요구치 0 = 입장 가능» 에서 굳은 옛 값이고, 다음 틱 전에 읽으면
그대로 잡힌다. 바로 뒤 `sub1` 은 이미 `uiDirty=true; renderUI()` + 대기를 쓰는데 **`sub0` 만 아무 대기 없이 읽고 있었다** —
짝을 맞춰 읽기 전에 강제 갱신하도록 고쳤다. (LESSONS 21-(2) «주기 렌더에 기대는 게이트의 가짜 회귀» 와 같은 계열)

### (3) 도감 절도 [0] 과 같은 병 + **즉사** 구조였다

`cl.length === 6` 리터럴 + `getComputedStyle(t.querySelector('s.dot'))`. `s.dot` 이 없는 칸이 하나라도 생기면
`getComputedStyle(null)` 로 **게이트가 통째로 죽어 한 줄도 안 찍힌다**(228·278 과 같은 계열).
칸 수를 **`COLL_TABS` 에서 파생**시키고(키 순서까지 대조), `s.dot` 이 없으면 죽지 말고 `✖dot없음` 으로 그 칸만 불일치로 잡게 했다.
이로써 **`COLL_TABS` → `.cltab` → `s.dot`** 사슬이 닫힌다 — [0] 의 «정적 dot = `.cltab` 칸 수» 와 물린다.

## 5. «버그를 되살린 빌드» 에 돌려 봤다 (LESSONS 109-3)

게이트가 순환 논증이 아닌지 확인하려고 index.html 을 일부러 망가뜨려 2번 돌렸다. **둘 다 원상 복구했다.**

| 주입 | 기대 | 결과 |
|---|---|---|
| **NEG-A** 탑 카드에 조건 없는 `<s class="dot">` 1개 추가 (166 이 없앤 병 재현) | [0] ② 가 잡는다 | **FAIL 38/41** — `전체 12곳 = 정적 6 + 조건부 5 + 분류불가 1 → +  '<s class="dot"></s>'` (줄까지 지목) |
| **NEG-B** 도감 탭에 `s.dot` 없는 7번째 칸 추가 | [0] ① 이 잡고, 도감 절이 **안 죽고** 잡는다 | 고치기 전: `getComputedStyle` **즉사**(한 줄도 안 찍힘). 고친 뒤: **FAIL 39/41** — `[정적 6곳 / .cltab 7칸]` · `rune✘✖dot없음` |

## 6. 회귀

- `node tools/verify166.js` → **41/41 PASS** · 순차 3회 연속 3/3 PASS (뜨고 지는 항목 없음)
- `node tools/smoke.js` → **SMOKE PASS**
- `index.html` **0줄 변경** — 다른 워커의 구간을 건드리지 않았다.

## 7. 남은 것

- 없음. 다만 **탑이 셋이 되거나 도감 탭이 늘어도 이 게이트는 이제 안 빨개진다**(자리 목록에서 파생하므로).
  반대로 «조건 없이 상시 점등» 이 하나라도 생기면 그 줄을 이름으로 찍으며 빨개진다.
