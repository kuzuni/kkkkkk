# 630 — `verify488` [J3] «진행 >200ms 표본 0개» (T1 「버그(게이트 플레이키/부패)」)

- 세션 `sess-0627-9412` 루틴 워커 A · 2026-09-01 · **1회차**
- 제품 `index.html` **0줄** · 레이아웃 **Δ0px** · 밸런스 계수 **0줄** · 판정 **0줄**
- 고친 파일: `tools/verify488.js` 하나(전부 [J] 절 안) · 신설 `tools/probe630.js`
- 지시서 [3]-**(가)** 기계적/게이트 작업 — **비평가 없음**
- 결과: `verify488` **75/76 → 77/77 · 7회 연속 PASS**(수리 전엔 [J3] 상시급 빨강 + [J5]/[J7] 간헐 빨강)

## 1. 재현 — 등재문 세 갈래(ⓐⓑⓒ)가 전부 «반쯤» 이었고, 실체는 627-③ 꼴이었다

`tools/probe630.js` 신설. [J] 하네스를 복제하되 **스냅숏과 무관한 수명 축**(MutationObserver 로
붙는 순간/떨어지는 순간을 노드 단위 기록)을 따로 세우고, 홀드 판을 둘로 돌렸다(LESSONS 627-②).

```
[1] 실제 수명(붙음→떨어짐)   반복분 .hb n=94 · 중앙 186ms(145..339)   ← 설계 HB_LIFE 310+PAD
                              한 발 .lng n=6 · 중앙 550ms              ← 설계 1300+PAD
    줄기별                    간격 중앙 92ms · 동시 생존 최대 정확히 2 (결과·비용 모두)
[2] 게이트 스냅숏 4장 축      ct>200 = 2 · 0 개 (판마다 다르다 = 플레이키의 정체)
[3] 조밀 83ms 축              ct>200 = 10 · 6 개 (구간은 실재한다)
[4] 홀드 끝 꼬리 40ms 폴링    ct>200 = 8 · 8 개 · ct 300 도달 (페이드가 끝까지 돈다)
```

- **ⓐ(제품이 수명을 자른다)** — 사실이지만 **결함이 아니라 설계다.** 자른 손은 fxBye 가 아니라
  **619 8회차 «줄기당 동시 생존 상한 2»**(3장째 스폰이 가장 오래된 것을 `q.shift()` 로 걷는다 —
  비평가 DT·DU 2인 공통 «f2 6장 스택 · 아이콘 40~60% 상시 가림» 을 잡은 주인 쪽 승인 처방)다.
  동시 생존이 **정확히 ≤2** 이고 수명 중앙 186 ≈ 태어남 간격 92 × 2 가 그 증거다.
- **ⓑ(표본 위상)** — 홀드 «중» 에는 ct>200 노드 자체가 «간격이 우연히 늘어진 순간» 에만 존재하므로
  스냅숏이 아무리 늘어도 **운**이다. 627-① 의 «같은 열차 = 한 위상» 과는 병이 다르다 —
  위상을 늘려도(83ms 조밀 축) 잡히는 개수가 6~10개로 흔들린다. 표본을 늘리는 처방으로는 못 닫는다.
- **ⓒ(574 잔재)** — [J3] 이 «표본» 축에 남은 것은 사실이지만, 574 시점엔 그 축이 **초록이었다**
  (상한 2 는 그 뒤 619 8회차가 넣었다). 부패의 시점은 574 가 아니라 619 8회차다.

⇒ **LESSONS 627-③ 그대로** — 자를 뒤집은 것은 버그가 아니라 **[J3](4회차 신설)보다 뒤에 들어온
설계**다. «고칠 것» 이 아니라 «갈아 끼울 것».

## 2. 처방 — 뜻(«페이드아웃이 실제로 보인다»)은 유지, 자리만 «홀드 끝» 으로

상한 2 설계에서 페이드아웃이 **끝까지 도는** 자리는 홀드가 끝나 새 스폰이 멎은 뒤의 마지막 두 장이다
(플레이어가 실제로 페이드를 보는 자리도 여기다 — 홀드 중엔 다음 장이 갈아타고, 놓는 순간 잔상이 스러진다).

- **[J3] 갈아 끼움**: touchEnd 뒤 40ms 폴링으로 죽을 때까지 ct 를 읽어 «>200ms 표본 ≥ 1» 을 단언.
  폴링이 죽음까지 이어지므로 **위상이 안 낀다**(실측 8~10/10~14 · ct 300 도달). **임계 200 은 안 내렸다**
  (등재문 경고 — 임계를 낮추면 334 가 기각한 처방 ② «페이드가 통째로 사라져도 초록» 꼴).
- **[J3b] 신설**(627-④ — 옛 결론을 죽이지 않고 옆에 세운다): «홀드 중 줄기당 동시 생존 ≤ 2» 래칫.
  상한이 사라지면(수명 310 ÷ 간격 92 ≈ 4장) 이 항이 빨개져 **[J3] 을 홀드 끝으로 옮긴 근거가
  무너졌음**을 알린다 — 그때는 [J3] 을 홀드 중 축으로 되돌리는 것까지가 그 작업의 범위다.
- 옛 축(«홀드 중 ct>200 표본»)은 574 의 α 비율과 같은 **기록만** 줄로 남겼다.

## 3. 같은 뿌리의 두 번째 얼굴 — [J5]/[J7] 간헐 «노드 없음» (수리 중 3/7 회 재현)

574 위상 훑기(`__sw574`)가 `.lng` 노드를 **훑는 시점에 셀렉터로 다시 찾는데**, 같은 상한 2 가
lng(설계 1.3s)를 실측 ~534ms 에 걷어가므로 감지(120ms 폴)와 훑기 사이에 노드가 사라지면
`[J5] 노드 없음` · `[J7] -ms` 로 빨갛다(75/77). 훑기는 애니메이션을 세우고 currentTime 을 손으로
옮기는 축이라 걷힌 노드를 도로 붙여 재도 값(키프레임 봉투)이 같다 — **감지 시점에 참조를 stash**
(`window.__sw630`)하고 훑기·되돌림 사본이 그 참조를 우선 쓰며, 끊겼으면 재부착한다(원래 있던
`isConnected` 재부착 가지의 용도를 입구까지 넓힌 것).

## 4. 게이트·회귀

- `node tools/verify488.js` **77/77 · 7회 연속 PASS** (수리 전: [J3] 빨강 0~2개 요동 · [J5]/[J7] 간헐)
- `node tools/probe630.js` — 갈래 판정 재현(위 표)
- **SMOKE PASS** · 콘솔 에러 0 · 제품 0줄이라 레이아웃·밸런스 회귀 없음
- 되돌림: [J3b] 가 상한 제거를, [J6](기존)이 페이드 제거를 각각 빨갛게 잡는다 — 서로 다른 항만
  빨개지는 분리(627-④ 꼴)

## 5. 남긴 것

- **619 세션에게**: [J3b] 는 619 8회차의 상한 2 를 **밖에서** 잠근 자다. 상한 값을 바꾸면(2→3 등)
  [J3b] 의 «≤ 2» 를 같은 커밋에서 같이 갈 것. `verify621` 40/42 는 630 등재 시점 그대로
  **619 선점 구간이라 손대지 않았다.**
- 훑기 stash(`__sw630`)는 [J] 절 전용 전역이다 — 다른 절에서 재사용하지 말 것(참조가 낡는다).
