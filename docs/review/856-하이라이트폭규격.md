# 856 — ③층(하이라이트) 폭 규격

> 등재: 2026-09-03(sess-0428-6610 워커 B · 792 10회차 비평 2인 CV·CW 독립 일치 · CV 1순위)
> 착수: 2026-09-03 sess-0743-26030 루틴 워커 C
> 주 편집 구간: `index.html` `shotBody()` 의 `spec` 층 · 자 `tools/verify856.js`(신설) · 이관 `tools/verify792.js`

## 1회차 (2026-09-03, sess-0743-26030 워커 C) — 제품 4자리 + 신설 1벌 · 자 신설

### 무엇이 결손이었나

792 10회차 비평 2인이 각자 다른 종을 짚었는데 증상이 8회차 ⓐ 와 **글자만** 달랐다 —
**하이라이트가 본체를 안 따라간다.**

| | CV | CW |
|---|---|---|
| 흩어짐 | 점유율 5.2%(flask) ~ 82.8%(whirl) = **15.9배** | 면적비 7% ~ 89% = **12.7배** |
| 폭의 비 | slash 68% · lance 64% · gale 38% · curve 14% | — |
| 0px | **arrow 축 · boomer 팔 · spiral 리본** | whirl 은 본체 4553 중 4047 이 근백색 = 3층이 사실상 2층 |
| 목표 | **획 폭의 30~40%** | — |

9회차가 ①층(후광)에 한 일 — «형상을 종에서 없애고 한 곳에서 본체에서 판다» — 을
③층은 **아직 안 받았다**. 17종이 각자 손으로 «흰 도형» 을 적으니 폭이 획에 안 매여 있었다.

### 자부터 (지시서 [3] · 792 10회차 인계 1순위)

⚠ **`verify792` 는 이것을 원리적으로 못 본다** — [A2]·[B2] 는 «근백색 화소가 잉크의 1~25%인가»
라는 **면적의 몫**이라 whirl(fSpec 7.8%)도 spiral(2.6%)도 초록으로 지나간다. 비평가가 잰 것은
**폭의 비**다. ⇒ `tools/verify856.js` 신설(792 의 [B10]·[B11] 자리):

- **[B10] 폭의 비** — 실효 폭 **W = 4·mean(D)**(폭 w 인 곧은 띠에서 mean D = w/4 라 정확히 w 다).
  절단면을 어디에 그을지 고르지 않아도 되고 구멍·꺾임·끝단에 둔감하다.
- **[B11] 마루 덮임** — ③층이 본체 **마루(medial axis)를 따라가는가**. 그 자리 두께로 가중해 센다.
- 거리변환은 **자가 제 손으로 짠다**(chamfer 5-7-11) — 제품의 `edt2d`(Felzenszwalb)를 부르면
  그 함수가 틀렸을 때 자와 제품이 **같이** 틀려 조용해진다.
- ⚠ 본체 문턱은 792 의 `A_BODY = 0.55` 가 **아니라 0.80** 이다. 9회차가 링 봉우리를
  `AURA_A = 0.62` 로 못박은 뒤로 0.55 는 **①층(링)을 ②층으로 셌다** — 면적의 몫에는 무해했지만
  **폭**을 재는 자에서는 링 두께가 «본체 폭» 으로 들어와 거짓말이 된다.

### 처방 — 종은 한 획도 안 그린다

`specSprite()` 신설(9회차 `auraSprite` 의 거울상): 본체 실루엣을 재귀로 구워(`SPEC_BAKE` 가 도는
동안 `halo()`·`spec()` 은 아무것도 안 그린다) 문턱 α≥.5 로 굳히고, **그 자리 반쪽폭의 K 만 남기고
깎은 코어**를 굽는다. 호출부 17자리는 **한 글자도 안 건드렸다** — `spec()` 한 곳의 뜻만 바뀐다.
각 종의 옛 흰 도형은 **폴백**으로 남는다(`SPEC_ON = 0`).

⚑ **깎는 자를 세 번 고쳐 잡았다**(9회차가 ①층에서 네 번 고친 것과 같은 자리):

| 안 | 결과 | 왜 틀렸나 |
|---|---|---|
| ⓐ 마루(**여덟 이웃** 최대)에 K·h 원 찍기 | ice 덮임 **0.455** · shuri **0.279** | 뾰족해지는 획은 마루를 따라 D 가 계속 커져 **꼭짓점 한 점**만 남는다 |
| ⓑ 네 축 중 **하나만** 봉우리면 마루 | 폭 비 0.39 → **0.88** · 밴드 **2.25** | 가장자리 화소가 섞여 들어와 코어가 바깥으로 번진다 |
| ⓒ **마루를 안 찾는다** — 화소마다 «자기 거리 안에서 본 D 의 최댓값» = 그 자리 반쪽폭 h ⇒ 코어 = { d ≥ (1−K)·h } | **채택** | 폭 = 2(h−(1−K)h) = **K × 획 폭**(항등식) · 남길 자리(d ≥ h/2)에서는 추정이 **정확**하고 더 바깥에서는 더 엄해질 뿐이라 안전한 쪽으로 틀린다 |

반지름별 최댓값은 **분리 가능한 슬라이딩 최댓값**(단조 덱)을 기하급수 반지름 칸으로 돌려 O(화소)다.
⚠ 굽는 순간 한 번뿐이라 프레임 비용이 아니다(4회차가 매 프레임 blur 로 ×1685 를 맞고 되돌린 자리 —
`verify792` [P1] 이 그것을 지키고 이번 회차에도 초록이다).

⚠ **1회차의 첫 빨강은 거리변환의 씨앗 방향이었다** — 씨앗을 잉크로 두면 잉크 화소가 전부 0 이 되어
마루가 통째로 사라진다(굽기가 `null` 로 조용히 폴백돼 **그림도 게이트도 멀쩡했다** — 자의 [A2]
«구운 종 0종» 이 그것을 잡았다. 9회차가 같은 자리에서 겪은 병이라 자에 그 항을 먼저 세워 뒀다).

### 손으로 적던 자리 셋도 `spec()` 을 지난다 (후광의 [C4] 와 같은 규약)

`fireCore` 흰불 · `lance` 날 끝 섬광 · `swirl` 가운데 흰 점 — 안 지나면 그 종만 옛 문법으로 떨어진다.
(spiral 리본에 ③층이 0px 이던 자리가 정확히 이것이다.)

### 결과 (같은 자로 잰 수리 전 ↔ 수리 후)

| 축 | 수리 전(폴백 = 792 의 세계) | 수리 후 |
|---|---|---|
| 폭 비 밴드(max ÷ min) | **4.18배**(비평가 눈으로는 15.9배) | **1.64배** |
| 목표대(0.22~0.52) 밖 | **9종** | **0종** |
| 마루 덮임 0.75 미만 | **11종** | **0종** |

종별 폭 비 — slash .425 · shuri .305 · stone .368 · curve .405 · multi .501 · ice .353 · whirl .346 ·
arrow .396 · rico .364 · spiral .399 · boom .449 · bounce .359 · boomer .365 · meteor .383 · gale .482 ·
flask .391 · lance .392 (CV 목표 «획 폭의 30~40%» — 자의 실효 폭 정의가 끝단에서 조금 높게 읽는다).

### 게이트

`verify856` **11/11 신설**(되돌림 시험 [R1]~[R3] 포함) · `verify792` **25/25 이관** ·
`verify710` 14/14 · `verify541` 68/68 · `probe792` 5/5 · **SMOKE PASS**.

⚑ **이관은 두 줄이다** — `verify792` 의 `TAG_SPEC`·`TAG_HALO` 가 **줄 전체를 글자로** 붙잡고 있어
층 함수의 머리가 바뀌자 [C1] 이 빨개지고 [R2] 가 **사본을 못 만들어** 조용히 초록이 됐다
(«후광을 껐는데 빠진 종 0» = 자가 자기 사본을 안 고친 얼굴이다). 9회차가 `TAG_HALO` 에 남긴
«**머리만** 붙잡아라» 를 두 상수에 다시 적용했다.

### 1회차 채점 — 비평가 CX·CY 독립 2인 (`docs/shots/856-r1.png`)

| 비평가 | ① 분간 | ② 통일 | ③ 덩치 | ④ 의미 | 최저 |
|---|---|---|---|---|---|
| CX | 4 | 3 | 4 | 5 | **3** |
| CY | 7 | 5 | 5 | 6 | **5** |

**2인 공통 1순위 — «가장 굵은 종이 흰 덩어리로 뒤집힌다»**(둘 다 ③층을 1순위로 적었다):

| | CX | CY |
|---|---|---|
| 삼킨 종 | boom **92%**(실체 48px 중 44px · 본체 테 좌우 2px) · meteor 53% · bounce 52% · lance 70% | meteor **90.5%**(테 1px) · stone 83.3% · shuri 80% · boom 40.6% |
| 얇은 종 | flask 8% · whirl 16% · ice 17% · gale 18% | rico 16.7% · ice 25% · boomer 26.7% |
| 밴드 | 8~92% = **11.5배** | 16.7~90.5% = **5.43배** |

⚠ **자와 눈이 갈린 자리도 둘이 서로 갈렸다** — 같은 종을 CX 는 33%, CY 는 83.3%(stone)로 쟀다.
뿌리는 «본체를 무엇으로 잡는가» 다: CY 는 «밝기 문턱(max채널 > 205)» 으로 잡아 **어두운 몸통**
(stone·meteor 는 최대 채널 140~171)이 통째로 빠졌고, 남은 밝은 부분이 거의 코어라 비율이 부풀었다.
⇒ 2회차 브리핑에 조항 ⓓ(«본체 경계는 배경과의 차이로 잡아라»)를 더했다.
**둘이 실제로 겹친 것은 boom·meteor 두 종**이고, 그 둘이 정확히 **제 손으로 방사 발광을 까는 종**이다.

## 2회차 (2026-09-03, sess-0743-26030 워커 C) — 제품 2자리 · 자 이관 1자리

1회차 공통 지적의 뿌리를 재현이 한 번에 갈랐다 — **씨앗 문턱**과 **규격의 적용 범위** 둘이었다.

1. **씨앗을 «단단한 몸» 으로**(`SPEC_SIL = 0.85`) — 링(9회차)은 α ≥ .5 를 씨앗으로 쓰는데, 불 계열의
   방사 발광은 중심에서 α .5~.85 라 그 문턱에는 **발광 원반이 통째로 «본체»** 로 들어온다.
   그 원반의 35% 는 눈에 보이는 단단한 몸보다 훨씬 넓다. 링은 «발광까지 두르는» 것이 맞지만
   코어는 **단단한 몸의 속**이다 — 같은 층 함수라도 두 문턱은 다른 질문에 답한다.
2. **덩어리에는 상한 폭**(`SPEC_MAXR = 2.6` 로컬px) — «획 폭의 35%» 는 CV 가 **획**을 두고 준 목표다.
   덩어리(화구·바위·공)에 그대로 대면 지름의 35% 짜리 흰 원이 되어 «반짝임» 이 아니라 **채움**이 된다.
   ⇒ 규격을 한 줄 더 못박았다: **코어는 획의 속이지 덩어리의 채움이 아니다.**

자도 그 갈래를 따라 둘로 갈랐다(문턱을 무르게 푼 것이 아니라 **묻는 것을 나눈 것**이다):
`[B10a]·[B10b]` 획 무리 13종은 **비율**이 한 밴드(0.298~0.502 = **1.68배**) ·
`[B10c]` 덩어리 무리 4종(boom·bounce·meteor·flask)은 **폭**이 한 밴드(14.85~15.30 = **1.03배**).
무리를 가르는 것은 자의 취향이 아니라 **제품의 규칙 그 자체**다(K·본체폭 ≥ 상한이면 상한이 정한다).

실측 변화 — boom 코어 폭 30.06 → **15.30**(−49%) · meteor 22.50 → **14.96**(−34%) ·
bounce 23.19 → **14.85**(−36%) · flask 19.33 → **14.98**(−23%). 획 무리는 Δ ≤ 0.3px.

게이트: `verify856` **12/12**([B10c] 신설) · `verify792` 25/25 · `verify710` 14/14 · SMOKE PASS.

### 2회차 채점 — 비평가 CZ·DA 독립 2인 (`docs/shots/856-r2.png`)

| 비평가 | ① 분간 | ② 통일 | ③ 덩치 | ④ 의미 | 최저 |
|---|---|---|---|---|---|
| CZ | 5 | 4 | 5 | 6 | **4** |
| DA | 7 | 5 | 5 | 8 | **5** |

⚑ **덩어리 규격(2회차 처방)은 두 사람이 각자 «통과» 를 적었다** — DA: «상한폭 18.4~21.3px = **±8% 한
밴드** · 흰 판 뒤집힘 없음(면적 1.6~9.0%) · **이 절반은 깨끗하다**» · CZ: «stone 바위 64px 에 21px,
meteor 110px 에 22px 로 «비율 아닌 상한» 은 맞게 동작했다 — **이건 이번 회차의 성과다**».
1회차의 2인 공통 1순위(boom 92% · meteor 90.5%)가 닫혔다.

**2인 공통 1순위(새 뿌리) — «코어가 획을 안 따라간다»**:
- CZ: «한 종 안에서» — ice 한 날에서 **9%(획 22px·코어 2px) ↔ 35% ↔ 67%** · whirl 17~33% · shuri 13~21%
- DA: «획이 굵어져도 코어가 2~6px 에 고정» — 코어폭 대 획폭 **기울기 shuri 0.09 · lance 0.16 ·
  whirl 0.17 · ice 0.19 · arrow 0.25**(규격 0.35) · 부분 소실 5건(shuri 날끝 · ice 앞날 42px · arrow 깃 ·
  lance 밑동 · slash 꼬리)에서 코어 0px

## 3회차 (2026-09-03, sess-0743-26030 워커 C) — 제품 1자리 · 자 1항 신설

**뿌리는 반쪽폭을 재는 «창의 모양» 이었다.** 화소마다 «자기 거리 d 안에서 본 D 의 최댓값» 으로
반쪽폭을 추정하는데, 그 창을 **변 길이 d 짜리 정사각**으로 잡고 있었다 — 정사각은 대각으로 **1.41d**
까지 본다. 가는 획이 굵은 데 붙어 있는 자리(창날 뿌리 · 별 갈래 · 화살 깃)에서 그만큼 **반쪽폭을
부풀려 읽고**, 문턱 (1−K)·h 가 같이 올라가 **그 자리만 코어가 사라진다**. 두 사람이 각자 짚은
다섯 자리가 전부 «가는 것이 굵은 것에 붙은» 자리다.

⇒ 창을 **원에 내접하는 상자**(d/√2)로 바꿨다(제품 1자리). 반쪽폭을 낮게 볼지언정 부풀리지 않는다 —
곧은 획에서는 마루까지 여전히 닿으므로(0.65h ÷ 1.41 = 0.46h ≥ 0.35h) «획 폭의 K» 항등식은 그대로다.

**자 [B12] 신설 — «한 종 안에서도 고른가»**. [B10] 은 종을 한 수로 요약하므로 CZ 의 지적을
원리적으로 못 본다. 주 마루 화소마다 «그 자리 코어 두께 ÷ 본체 두께» 를 내고 **p90 ÷ p50** 을 본다.
⚠ 아래 꼬리를 p10 이 아니라 **중앙값**과 견주는 이유는 납작한 덩어리(육각 돌·병)에서 4이웃 봉우리가
평평한 고원을 통째로 «마루» 로 잡는 잡음이 하위 10% 를 0 으로 만들기 때문이다(그 잡음이 실재인지는
[B11] 이 따로 답한다). 회차 안에서 자를 세 번 고쳐 잡았고 그 기록을 자 주석에 남겼다.

**실측** — [B12] 문턱 초과 종 **9종 → 2종**(whirl 4.46 → 2.17 · arrow 5.40 → 1.60 · shuri 2.45 → 통과).
남은 둘은 stone 2.21 · ice 2.00 이고 **래칫 2.3** 으로 걸었다(목표 1.5 · 356 [B] 선례).

게이트: `verify856` **13/13** · `verify792` 25/25 · `verify710` 14/14 · SMOKE PASS.

### 3회차 채점 — 비평가 DB·DC 독립 2인 (`docs/shots/856-r3.png`)

| 비평가 | ① 분간 | ② 통일 | ③ 덩치 | ④ 의미 | 최저 |
|---|---|---|---|---|---|
| DB | 7 | 4 | 6 | 7 | **4** |
| DC | 4 | 3 | 6 | 6 | **3** |

**2회차 1순위 넷을 두 사람이 각자 다시 재서 판정했다**(브리핑 ⓔ 로 그 넷을 명시해 줬다):

| 자리 | 2회차 | DB | DC | 판정 |
|---|---|---|---|---|
| shuri 날 | 4.2% 고정 | 기울기 **0.254**(절편 해제) · 21.6~29.7% | 기울기 0.208 · 21.2~27.8% | **고정 해제** — 기울기는 규격 0.35 의 59~73% |
| ice 앞날 | 0% | 33.3 / 30.0 / 28.6% | 28.6~36.8% | **닫혔다** |
| arrow 깃 | 0% | **30.9~36.2%**(규격 적중) | 28.6% | **닫혔다** |
| lance 밑동 | 0% | 0.00% | 0.00% | **손대지 않았다 → 865 등재** |

⚑ **회차마다 «닫혔다» 를 다음 회차의 다른 두 사람이 각자 확인해 줬다** — 1회차 1순위는 2회차가,
2회차 1순위는 3회차가. 세 회차의 최저 점수(3 → 4 → 3~4)가 안 오른 이유는 감점이 **다른 축으로
옮겨 갔기** 때문이고, 그 축들이 아래 «남은 문제» 의 3번(이 작업 단위 밖)이다.

**3회차가 남긴 ② 잔여 — «뾰족한 쐐기에서는 아직 굵은 쪽을 본다»**(2인 공통):
DC «획 ≥8px 인데 코어 0px 인 단면 45/1,645 = **2.7%**» — arrow 머리 x=698(획 45px · 코어 0px 인데
7px 옆 x=705 는 40%) · ice 뒤쪽 x=149~163(17단면) · curve 뿔(7단면). DB 도 같은 셋을 재서
«ice 한 날에서 6.5% ↔ 40.3%» 로 적었다. **원인은 3회차가 줄이기만 하고 없애지는 못한 «창의 과잉
도달»** 이고, 처방은 아래 1번이다.

### 남은 문제 · 다음 회차

1. **⚑ 다음 회차의 1순위 — 반쪽폭을 «추정» 하지 말고 «연다»(opening).** 3회차의 내접 상자는
   부풀림을 줄였을 뿐 **뾰족한 쐐기**에서는 여전히 굵은 쪽을 본다(남은 [B12] 초과 stone 2.21 ·
   ice 2.00 이 그 자리다). 정확한 국소 두께는 **«p 를 품으면서 도형 안에 들어가는 가장 큰 공»**
   이다: `T(p) = max{ 2·D(q) : |p−q| ≤ D(q) }` — 조건이 `≤ D(p)` 가 아니라 **`≤ D(q)`** 인 것이 요점이다.
   반지름 칸마다 `{D ≥ r}` 을 r 만큼 부풀려(= 그 집합의 거리변환 ≤ r) 덮는 식으로 계산할 수 있고,
   **`edt2d` 를 칸 수만큼 더 도는 값**이다(굽는 순간 한 번뿐이라 예산 안이지만 첫 프레임 비용을
   재고 넣어라 — `verify792` [P1] 이 그 예산을 지킨다).
2. **whirl 은 아직 ②와 ③이 안 갈린다**(792 10회차 CW) — 본체 색 자체가 근백색인 종이라 ③층 폭을
   맞춰도 «층이 둘» 로 읽힌다. 폭이 아니라 **본체 색**의 문제이므로 이 작업 단위의 축이 아니다.
3. **2인 독립 일치 2건을 새 번호로 등재했다**(792 가 ⏸ 보류라 «그 행이 나중에 한다» 가 성립하지
   않는다 — 838·856 선례): **864** = `boom` ↔ `bounce` 실루엣 IoU **0.951**(DC · CZ 도 같은 자리) —
   ⚠ `verify710` [C1] 은 같은 트리에서 0.79~0.83 으로 **초록**이다(자는 «그리는 자리 그대로», 눈은
   «정렬 후» 를 잰다 — 792 10회차 ⓒ 와 같은 «두 자가 다른 것을 묻는» 자리) · **865** = `lance` 밑동
   43×28px 슬래브가 **어느 층에도 안 든다**(DB·DC 둘 다 1순위) — 856 이 안 고친 것이 맞다(③층이
   아니라 **층 배정**의 문제이고, 씨앗 문턱 α ≥ .85 가 그 판을 본체에서 뺀 것은 설계대로다).
4. **①·③·④ 는 이 작업 단위 밖이다** — 세 회차의 비평가 넷이 되풀이해 적은 감점 중
   ①(slash↔curve IoU 0.78~0.86 · stone↔meteor 0.70~0.72 · shuri↔whirl 0.65) ·
   ③(잉크 6.3~7.8배 · 최장변 2.45~2.65배) · ④(ice 에 자루가 없다 · gale 이 속도선으로 읽힌다)는
   **710·792 가 인계한 실루엣·덩치 축**이고 ③층을 아무리 고쳐도 안 움직인다. 792 가 ⏸ 보류라
   그 축들은 **새 번호로 등재해야 움직인다**(838·856 선례).
5. **자의 실효 폭이 얇은 획에서 조금 높게 읽는다**(gale .478) — 4·mean(D) 가 둥근 캡·끝단에서
   본체를 낮게 잡는 쪽이다. 문턱을 흔들기 전에 **왜 높게 읽는지**를 먼저 재라(344 규칙).

## 이 세션의 마감 (2026-09-03, sess-0743-26030 워커 C)

**◐ 진행 — 3/5 회차. 완료가 아니다.** 통과선(①~④ 최저 8)에 못 닿았고 lock 을 풀었다.
세 회차가 닫은 것과 남긴 것은 위 표대로이고, **다음 세션은 위 «남은 문제» 1번(opening)부터** 잡으면 된다.
회차는 **이어서 센다**(다음이 4회차 · 화면당 누적 상한 10회).

⚑ **다음 세션이 반드시 볼 것 셋**
1. 비평가에게 주는 브리핑에 **ⓓ(«본체 경계는 밝기 문턱이 아니라 배경과의 차이로 잡아라»)** 를 빼지 마라 —
   1회차에 그 조항이 없어 CY 가 어두운 몸통(stone·meteor 는 최대 채널 140~171)을 통째로 놓치고
   비율을 83~90% 로 쟀다. 2·3회차에 넣자 같은 자리가 33~37% 로 잡혔다.
2. **ⓔ(«앞 회차 1순위를 직접 다시 재라»)** 도 빼지 마라 — 3회차에 그 조항 덕에 두 사람이 각자
   같은 넷을 재서 «셋은 닫혔고 하나는 안 고쳤다» 를 **수치로** 갈라 줬다. 이것이 없으면 회차마다
   새 지적만 쌓이고 «수리가 먹혔는가» 는 아무도 답하지 않는다.
3. `verify856` 의 **[B12] 래칫 2.3 을 목표 1.5 에 닿기 전에 올리지 마라.** 올리는 순간 이 자는
   «지금 그대로» 를 초록으로 지나가는 자가 된다(792 9회차 [R4] 가 못박은 자리와 같은 이유).

## 4회차 (2026-09-03, sess-1543-14901 루틴 워커 C) — **제품 0줄** · 등재된 처방을 실측으로 기각

3회차가 «다음 회차의 1순위» 로 남긴 처방(**opening** — 반쪽폭을 추정하지 말고 열어라)을 그대로
구현해서 **같은 자로 재 봤고, 그 처방이 겨냥한 바로 그 축을 나쁘게 만든다**는 것을 확인했다.
그래서 **제품을 되돌렸다**(`index.html` 최종 0줄 · `verify856` 13/13 PASS 유지).
아래 넷은 전부 **같은 트리 · 같은 자 · 같은 실행**으로 잰 수치다.

| 반쪽폭을 읽는 법 | [B10a] 목표대 밖 | [B10b] 밴드 | **[B12] 종내 흩어짐 최대** | [B11] 마루 덮임 최소 |
|---|---|---|---|---|
| **3회차(현행)** 창 = d/√2 · 칸 ×1.45 · «dw 이상» 첫 칸 | 0종 | 1.65 | **2.21** (stone 2.21 · ice 2.00) | 1.000 |
| ⓐ **opening**(등재 처방) `T(p)=max{2·D(q) : |p−q| ≤ D(q)}` | 3종 (slash .539 · multi .528 · gale .583) | 1.79 | **3.40** (ice 3.40) | **0.847** (ice) |
| ⓑ 최급 상승으로 마루 값 읽기 | 1종 (gale .554) | 1.54 | **99** (shuri·ice 는 p50 = 0) | **0.581** (shuri) |
| ⓒ 칸 ×1.08 + «dw 를 **넘지 않는**» 칸 + 바닥 상대 상한 | 1종 (gale .640) | 2.24 | **1.85** (stone 1.85 · **ice 1.33**) | 0.834 (flask) |

### ⓐ 등재 처방(opening)이 왜 틀렸나 — «국소» 를 **넓히는** 쪽이었다

opening 은 «p 를 품으면서 도형 안에 들어가는 가장 큰 공» 이라 정의상 **굵은 쪽의 큰 공이 가는 쪽을
덮는다**. 그것이 3회차가 잡으려던 증상(«가는 것이 굵은 것에 붙은 자리에서 반쪽폭을 부풀려 읽어
그 자리만 코어가 사라진다»)과 **같은 방향**이다 — 3회차의 창(d/√2)은 반지름이 **p 자신의 거리**로
묶여 있어 가는 쪽 화소는 애초에 멀리 못 보는데, opening 은 **q 쪽 반지름**으로 닿으므로 더 멀리서
온다. 실측이 그대로 나왔다: ice 종내 흩어짐 2.00 → **3.40** · ice 마루 덮임 1.000 → **0.847**.
⇒ **3회차의 처방문은 «정확한 국소 두께» 를 opening 으로 적었지만, 이 결손에서 필요한 것은
«더 정확한» 이 아니라 «더 국소적인» 이다.** (등재문 §남은 문제 1번은 이 회차로 **기각**한다.)

ⓑ(거리 오르막을 따라 올라가 마루 값을 읽기)는 더 나빴다 — 넓어지는 쐐기에서는 오르막이
**끝까지** 이어져 날 전체가 두꺼운 쪽 값을 물려받는다(shuri 덮임 .581 · ice .619).
1회차가 안 ⓐ 에서 겪은 «꼭짓점 한 점만 남는다» 와 **같은 병**이고, 그때와 같은 이유로 기각한다.

### ⓒ 진짜 과잉 도달은 «창의 모양» 이 아니라 **«반지름의 칸»** 이었다

3회차가 창을 정사각 → 원 내접(d/√2)으로 좁혔는데, 그 값이 실제로는 **칸으로 반올림돼 있었다** —
칸이 `×1.45` 등비이고 고르는 규칙이 «dw **이상**인 첫 칸» 이라 창이 뜻한 것보다 **최대 45% 크다**.
칸을 `×1.08` 로 촘촘히 하고 «dw 를 **넘지 않는**» 칸을 고르면(= 과잉 도달을 8% 아래로) —
**ice 2.00 → 1.33 · [B12] 최대 2.21 → 1.85 · 초과 종 2 → 1.** 3회차가 남긴 잔여
(«획 ≥8px 인데 코어 0px», 2인 공통)가 **이 한 줄에서 닫힌다.**

### 그런데 ⓒ 를 못 넣는 이유 — 가장 가는 종에서 [B10a] 가 빨개진다

칸이 정확해지면 반쪽폭이 **덜 부풀려** 읽히고, 그러면 «가장 가는 획도 코어를 갖는다» 는 **절대
바닥**(`SPEC_MINR`)이 상대적으로 커져 가장 가는 종의 코어가 굵어진다: gale 0.478 → **0.752**.
바닥에 상대 상한을 씌워 0.640 까지 눌렀고, 계수를 함께 내려 봤지만 **두 축이 정면으로 맞선다**:

| K / 바닥 상대 상한 | gale 비 | ice [B12] |
|---|---|---|
| 0.35 / 0.40 | 0.640 | **1.33** |
| 0.32 / 0.31 | 0.539 | 2.44 |
| 0.30 / 0.32 | 0.536 | 2.59 |

gale 을 밴드(≤ 0.52) 안에 넣으려면 K 를 0.31 아래로 내려야 하고, 그러면 [B12] 가 **래칫 2.3 을
넘는다**. 즉 **이번 회차의 자 안에서는 둘을 같이 만족시키는 계수가 없다.**

⚑ **왜 gale 만 그런가 — 자의 편향을 수치로 잡았다(1회차 §남은 문제 5번이 «먼저 재라» 고 한 것).**
K 를 흔들며 재 보니 gale 의 «잰 비 ÷ 기하학적 비» 가 **약 1.68** 인데 나머지 종은 **약 1.15** 다.
자의 실효 폭 `W = 4·mean(D)` 는 **둥근 캡·끝단에서 본체를 낮게 잡는다** — gale 은 세트에서 가장
가는 종(본체 10.67)이라 그 편향이 **분모**에 몰린다. 즉 gale 의 0.64 는 «코어가 굵다» 가 아니라
**«본체를 얇게 쟀다»** 일 가능성이 크다. 다만 그것은 **자를 고치는 일**이고, 제품을 함께 바꾸는
회차에서 자를 같이 무르게 풀면 «내가 빨갛게 만든 항을 내가 지운» 꼴이 된다(333·328~330 규약).
⇒ **이번 회차는 제품을 되돌리고 자를 안 건드린다.**

### 게이트

`verify856` **13/13 PASS**(되돌린 뒤 · 수치는 3회차와 동일) — 제품 0줄이므로 이관·회귀 없음.

### 남은 문제 · 다음 회차 (5회차)

1. **⚑ 1순위 — 자의 «본체 폭» 편향부터 재고, 그 다음에 ⓒ 를 넣어라.** 순서가 요점이다:
   ① `verify856` 의 `W = 4·mean(D)` 가 **둥근 캡·짧은 획**에서 본체를 얼마나 낮게 잡는지
   **합성 도형**(폭을 아는 곧은 띠 · 같은 폭의 캡 있는 띠 · 쐐기)으로 재서 편향표를 만든다 —
   제품과 무관한 입력이라 «자를 무르게 푼다» 는 의심이 원리적으로 안 생긴다.
   ② 편향이 확인되면 [B10] 의 본체 폭을 그 편향이 없는 자로 갈아 끼우고(되돌림 시험 필수),
   ③ 그 자 위에서 **ⓒ(칸 ×1.08 + «넘지 않는» 칸)** 를 넣는다 — ⓒ 의 이득(ice 2.00 → 1.33)은
   이미 실측돼 있으므로 ③ 은 재탐색이 아니라 **재적용**이다.
2. ⓐ opening · ⓑ 오르막 은 **다시 걷지 마라.** 위 표가 둘 다 기각의 근거다(각각 3.40 · 99).
3. 2·3회차가 남긴 것 중 이 작업 단위 **밖**인 것(whirl 본체 색 · 864 · 865 · ①③④ 축)은 그대로다.
4. `verify856` [B12] 래칫 **2.3** 은 그대로 두었다(목표 1.5). ⓒ 가 들어가면 1.85 로 내려가므로
   **그때 래칫을 1.9 로 조여라** — 조이는 것은 언제나 안전하다.

## 이 세션의 마감 (2026-09-03, sess-1543-14901 루틴 워커 C)

**◐ 진행 — 4/5 회차. 완료가 아니다.** 제품 0줄 · 자 0줄 · `verify856` 13/13 PASS.
비평 채점은 **안 돌렸다** — 제품이 한 줄도 안 바뀌었으므로 3회차 캡처를 다시 채점하는 것은
회차 예산만 태운다(점수는 3회차 DB 4 / DC 3 그대로다). 이 회차가 만든 것은 **다음 회차가
헛걸음을 세 번 덜 하게 하는 실측표**이고, 5회차의 순서는 위 «남은 문제» 1번에 못박아 두었다.
회차는 **이어서 센다**(다음이 5회차 · 화면당 누적 상한 10회).

## 5회차 (2026-09-03, sess-1713-11526 루틴 워커 A) — **제품 0줄** · **자를 갈아 끼웠다(편향 제거)** · ⓒ 는 뿌리를 찍고 되돌렸다

4회차가 못박은 순서(**① 자의 편향부터 재고 → ② 자를 갈아 끼우고 → ③ 그 위에서 ⓒ 를 재적용**)를
그대로 걸었다. ①②는 **끝났고**(자 교체 · 게이트 초록), ③은 **넣어 보고 되돌렸다** — 다만
4회차와 달리 이번엔 «계수가 없다» 가 아니라 **막는 것이 무엇인지**를 수치로 찍었다(아래 ⓓ).

### ① 자의 편향 — `tools/probe856.js` **신설**(합성 도형만 · 제품과 무관한 입력)

참값을 **아는** 도형만 넣었다: 곧은 띠(캡 없음) · 스타디움(같은 폭 + 둥근 캡, 캡 비중 3종) ·
쐐기. 폭은 4~68px. 자 넷을 같은 거리변환(chamfer 5-7-11) 위에서 나란히 쟀다.

| 자 | band+stadium 비 | 밴드 | 가는쪽(≤11) | 굵은쪽(≥32) | **가는÷굵은** |
|---|---|---|---|---|---|
| **A `4·mean(D)`(옛 [B10])** | 0.825~1.500 | **1.82배** | 1.188 | 0.990 | **1.200** |
| B 마루 두께가중 `2·Σ(D²)/Σ(D)` | 0.937~1.000 | 1.07배 | 0.975 | 1.000 | **0.975** |
| C 마루 중앙값 | 0.937~1.000 | 1.07배 | 0.975 | 1.000 | 0.975 |
| D 봉우리 `2·max(D)` | 0.937~1.000 | 1.07배 | 0.975 | 1.000 | 0.975 |

**편향이 둘이고 서로 반대 방향이었다.**
ⓐ **이산화 +2.0px 절대 오프셋** — 곧은 띠에서 **예외 없이 정확히 +2**(w=4 → 6.00 = ×1.50 ·
w=68 → 70.00 = ×1.03). 절대값이라 **가는 것일수록 크게 부푼다**.
ⓑ **둥근 캡·짧은 획은 평균을 끌어내린다**(스타디움 w=48 L=24 → 39.59 = ×0.825).

⚑ **[B10] 은 «코어 폭 ÷ 본체 폭» 이라 가는 것(코어)과 굵은 것(본체)을 같은 자로 재서 나눈다** —
그래서 해로운 것은 «비가 1 이 아니다» 가 아니라 **«가는쪽 ÷ 굵은쪽 = 1.200»** 이고, 그 1.200 이
비에 통째로 곱해지고 있었다. 4회차가 gale 에서 본 «잰 비 ÷ 기하학적 비 ≈ 1.68» 의 뿌리다.
(0.937 은 자의 편향이 아니라 폭 10.67 을 화소로 굽는 쪽의 반올림이다 — 정수 폭에서는 전부 1.000.)

### ② 자 교체 — [B10] 의 폭을 **B(마루 두께가중)** 로

`ridgeW = 2·Σ(D²)/Σ(D)` 를 주 마루(D ≥ .35·Dmax · 4이웃 봉우리)에서 잰다. 재는 자리도 가중도
`ridgeCover`·`spread` 가 **이미 쓰던 것**이라 자 안에서 «폭» 의 뜻이 한 벌이 됐다. 쐐기에서는
**면적 가중 평균폭**을 읽는다(비 1.014~1.043) — «굵은 자리가 그림에서 차지하는 몫도 그만큼
크다» 는 `ridgeCover` 의 가중과 같은 뜻이다. 마루가 4화소 미만이면 **봉우리로 물러선다**(조용한
0 은 [B10] 을 초록으로 지나가게 하는 얼굴이다 — 1회차 [R3] 교훈). 옛 자는 `effW` 로 남겨 표에
**대조군**으로 같이 찍는다(지우지 않았다 — 편향표의 짝이다).

**제품 0줄인데 표가 이만큼 움직였다**(자가 그만큼 틀리고 있었다는 뜻이다):

| | slash | ice | gale | rico | boom | meteor |
|---|---|---|---|---|---|---|
| 본체폭 옛 자 | 16.5 | 22.86 | 10.82 | **41.31** | 67.8 | 60.65 |
| 본체폭 새 자 | 15.91 | 23.19 | 8.88 | **59.6** | 98.8 | 79.31 |

⚑ **rico 가 무리를 갈아탔다** — 옛 자가 본체를 41.31 로 낮게 읽어 **획 무리**에 앉아 있었는데,
59.6 이 되자 `capMode`(상한 폭이 코어를 정하는 종)로 넘어가 **덩어리 무리**가 됐다. rico 는 보석
= 덩어리이므로 **자가 종을 잘못 나누고 있었던 것**이고, 그것을 고친 것이 이 회차의 조용한 성과다.
[R2] 되돌림 시험도 같이 날카로워졌다 — **밴드 4.18배 → 10.30배**(자가 «코어를 끈 세계» 를
훨씬 또렷하게 빨갛게 만든다). `verify856` **13/13 PASS** · 제품 0줄이라 이관·회귀 없음.

### ③ ⓒ 재적용 — **[B12] 는 예고대로 닫혔고, gale 하나에서 [B10a] 가 빨개졌다**

ⓒ(칸 ×1.45 → **1.08** · «dw 이상인 첫 칸» → «넘지 않는 마지막 칸» · 바닥 상대 상한 `SPEC_MINK`)를
넣자 4회차가 **미리 재 둔 이득이 그대로 나왔다**:

| | [B12] 최대 | ice [B12] | meteor 마루덮임 | gale 비 |
|---|---|---|---|---|
| ⓒ 없음(현행) | 2.21 (stone) | 2.00 | 0.853 | 0.469 |
| **ⓒ 넣음** | **1.85** (stone) | **1.33** | **0.939** | **0.651** ❌ |

[B10a] 가 gale 하나로 빨개졌다(0.651 > 0.52) · [B10b] 밴드 2.21배 > 2.

### ⓓ ⚑ **이 회차의 본체 — gale 이 왜 안 움직이는지 찍었다(4회차의 «계수가 없다» 를 대체한다)**

4회차는 gale 을 «자가 본체를 얇게 쟀다» 로 의심하고 5회차에 자를 고치라고 남겼다. **자를 고쳐도
gale 은 0.469 → 0.651 이었다.** 그래서 이번엔 **지렛대를 하나씩 끝까지 밀어 보고 반응을 쟀다**:

| 흔든 것 | 값 | gale 비 | 반응 |
|---|---|---|---|
| `SPEC_MINK`(바닥 상대 상한) | 0.40 / 0.34 / 0.30 / 0.26 | 0.651 / 0.651 / 0.651 / 0.651 | **없음** |
| 칸 고르는 규칙 | 넘지 않는 / **반올림** | 0.651 / 0.651 | **없음** |
| `SPEC_MINR`(절대 바닥) | 0.38 / 0.24 | 0.651 / 0.651 | **없음** |
| `SPEC_K`(목표 비율) | 0.35 / 0.25 | 0.651 / 0.639 | **거의 없음**(−2%. 같은 흔들기에 slash 는 −12%) |

**네 지렛대 전부에 안 움직이는 종은 «계수가 나쁜 종» 이 아니다.** 굽기 실패(폴백)부터 의심했으나
`[A2] 코어를 구운 종 **17종**` — gale 도 구워진다. 남는 설명은 하나다:

⚑ **gale 은 굽는 판에서 획이 너무 가늘어 비율 판정이 놓일 «눈금» 자체가 없다.**
판정은 `d ≥ (1−K)·hh` 인데 `hh` 는 «자기 거리 안에서 본 D 의 최댓값» 이라 **항상 `hh ≥ d`** 다.
획이 가늘어 D 가 사실상 한 값(d = 1)만 갖는 자리에서는 `hh = 1` 이고 `1 ≥ 0.65` 가 **모든 화소에서
참**이 된다 ⇒ **코어 = 실루엣 전체**로 포화한다. 어떤 계수도 이 포화를 못 푼다.
그리고 **옛 ×1.45 칸이 gale 을 살려 주고 있던 것은 «과잉 도달» 이었다** — 칸이 위로 최대 45%
부풀려 `hh` 를 크게 읽어 준 덕에 t 가 올라가 바깥 화소가 떨어져 나갔다. 즉 **4회차가 «과잉
도달» 로 지목해 없애려 한 그 결함이, gale 에서는 유일하게 코어를 얇게 만들던 힘이었다.**

⇒ **gale 의 뿌리는 계수도 창도 아니라 «굽는 판의 해상도» 다.** 폭 2~3화소짜리 획에서
«코어가 있다»([B11])와 «코어가 획의 35%»([B10])는 **동시에 만족될 수 없다** — 35% 를 그리려면
1화소보다 얇아야 한다. 6회차가 할 일이 이것이다(아래 1번).

⚠ **그래서 [B10a] 에 «해상도 바닥에 눌린 종» 예외를 신설하지 않았다** — 그것은 내가 빨갛게
만든 항을 내가 지우는 것이다(333·328~330 규약 · 4회차가 같은 자리에서 같은 이유로 멈췄다).
**제품은 되돌렸다**(`index.html` 최종 0줄 · `verify856` 13/13 PASS).

### 게이트

`verify856` **13/13 PASS**(새 자 · 제품 0줄) · `tools/probe856.js` **신설**(편향표 · 브라우저 불필요) ·
`node tools/smoke.js` **SMOKE PASS** · 제품 0줄이므로 이관·회귀 없음.

### 남은 문제 · 다음 회차 (6회차)

1. **⚑ 1순위 — 코어를 «굽는 판» 의 해상도를 올리고 나서 ⓒ 를 넣어라.** 순서가 요점이다:
   ① 코어 굽기 전용 초과표본 `SPEC_SS`(≥2)를 두어 실루엣을 `HALO_SS × SPEC_SS` 로 그리고
   거리변환·코어를 그 판에서 낸 뒤 스프라이트만 되돌린다(`minR`·`maxR` 도 같이 곱해야 한다 —
   로컬px 상수다). ② 그 위에서 **ⓒ 를 재적용**한다 — ⓒ 의 이득(**[B12] 2.21 → 1.85 · ice 2.00 →
   1.33 · meteor 덮임 0.853 → 0.939**)은 **이미 실측돼 있으므로 재탐색이 아니라 재적용**이다.
   ③ ⓒ 가 들어가면 [B12] 래칫을 **1.9 로 조여라**(조이는 것은 언제나 안전하다).
   ⚠ 비용은 **굽는 순간 한 번**이지만 판 넓이가 4배다 — 첫 프레임 비용을 재고 넣어라
   (`verify792` [P1] 이 그 예산을 지킨다. 4회차가 매 프레임 필터로 ×1685 를 맞고 되돌린 자리다).
2. **ⓐ opening · ⓑ 오르막 · 계수 흔들기(K·MINR·MINK·칸 규칙)는 다시 걷지 마라.** 4회차 표와
   위 ⓓ 표가 각각의 기각 근거다. 특히 **gale 을 계수로 고치려 들지 마라** — 네 지렛대 전부에
   반응이 0 임을 위 표가 못박는다.
3. 2·3회차가 남긴 것 중 이 작업 단위 **밖**인 것(whirl 본체 색 · 864 · 865 · ①③④ 축)은 그대로다.

## 이 세션의 마감 (2026-09-03, sess-1713-11526 루틴 워커 A)

**◐ 진행 — 5/5 회차.** 통과선(①~④ 최저 8)에 못 닿았고 lock 을 풀었다. 지시서 [3]-(나) 5번대로
남은 문제를 위에 적고 종료한다(화면당 누적 상한은 10회이므로 **2차 5회를 새로 잡을 수 있다** —
그때 회차는 6회차부터 이어서 센다).
비평 채점은 **안 돌렸다** — 4회차와 같은 이유다(제품 0줄이면 3회차 캡처를 다시 채점하는 것은
회차 예산만 태운다. 점수는 3회차 DB 4 / DC 3 그대로다). 이 회차가 만든 것은 **① 편향 없는 자**
(제품 0줄인데 rico 의 무리 배정이 바뀌고 [R2] 가 4.18 → 10.30 배로 날카로워졌다)와
**② gale 이 계수로는 안 열린다는 증명**이고, 6회차의 순서는 위 «남은 문제» 1번에 못박아 두었다.

## 6회차 (2026-09-03, sess-1758-9630 루틴 워커 D) — **제품 0줄** · 5회차의 1순위(해상도)를 실측으로 기각

5회차가 못박은 순서(**① 굽는 판을 초과표본 → ② 그 위에서 ⓒ 재적용 → ③ 래칫 1.9**)를 그대로
구현해서 같은 자로 쟀다. **①이 겨냥한 축을 ①이 반대로 민다** — `gale` 비가 0.469 → **0.704** 로
**나빠졌고**, ②를 얹으면 0.800 까지 간다. 그래서 **제품을 되돌렸다**(`index.html` 최종 0줄 ·
`verify856` **13/13 PASS** 복귀). 아래는 전부 **같은 트리 · 같은 자 · 같은 명령**으로 잰 수치다.

| 판 | 칸 | 계수 | `gale` 비 | 밴드 | **[B12] 최대** | `ice` [B12] | `meteor` 마루덮임 | 게이트 |
|---|---|---|---|---|---|---|---|---|
| **현행(5회차)** SS=1 | ×1.45 «이상인 첫» | 기본 | **0.469** | 1.65 | **2.21**(stone) | 2.00 | 0.854 | **13/13** |
| ① SS=**2** | ×1.45 | 기본 | **0.704** ❌ | 2.65 ❌ | **3.38**(stone) ❌ | 1.81 | **0.695** ❌ | 9/13 |
| ①+② SS=2 | **×1.08 «넘지 않는 마지막»** | 기본 | **0.800** ❌ | 3.67 ❌ | 3.38 ❌ | 1.87 | 0.773 | 10/13 |
| ①+② | ×1.08 | `SPEC_K` 0.35 → **0.25** | **0.809** | 5.43 ❌ | 99 ❌ | 99 ❌ | 0.773 | 붕괴 |
| ①+② | ×1.08 | `SPEC_MAXR` 2.6 → **1.8** | **0.799** | 3.67 ❌ | 1.87 | — | 0.771 | 11/13 |
| 대조 SS=1 | **×2.20**(과잉 도달을 되레 키움) | 기본 | **0.469** | 1.86 | 2.21 | 2.40 ❌ | 1 | 11/13 |

### ⓐ 해상도는 `gale` 의 손잡이가 아니다 — **반대 방향으로 민다**

5회차 ⓓ 의 진단은 «획이 2~3화소라 판정이 놓일 눈금이 없다 ⇒ 판을 촘촘히 하면 열린다» 였다.
판을 ×2 로 하자 `gale` 은 **열린 것이 아니라 더 포화**했다(0.469 → 0.704). 뿌리는 5회차가
①에서 스스로 잰 편향표에 이미 있었다 — **이산화 +2.0px 절대 오프셋**(곧은 띠에서 예외 없이 +2).
그 오프셋은 `hh` 를 위로 부풀리고, `hh` 가 크면 문턱 `t = (1−K)·hh` 가 높아져 **코어가 얇아진다**.
즉 **가장 가는 종에서 비를 밴드 안에 잡아 주고 있던 것은 규격이 아니라 판의 이산화 오차**였고,
판을 촘촘히 한 순간 그 받침이 사라진 것이다. (같은 이유로 ②의 «과잉 도달 제거» 도 같은 방향이다 —
0.704 → 0.800.) ⇒ **등재 §남은 문제 1번(초과표본)은 이 회차로 기각한다.**

### ⓑ 그런데 «과잉 도달» 도 손잡이가 아니다 — SS=1 에서는 칸을 **키워도** 안 움직인다

대조로 칸을 ×1.45 → **×2.20** 으로 **더 무르게** 풀어 봤다(과잉 도달을 키우는 쪽). `gale` **0.469
그대로**(소수점까지)이고 대신 `shuri` 가 0.529 로 밴드를 넘고 `ice` [B12] 가 2.40 으로 래칫을 깬다.
⇒ SS=1 에서 `gale` 은 **칸에도 반응이 없다**. 5회차의 «옛 ×1.45 칸이 gale 을 살려 주고 있었다» 는
칸이 아니라 **판의 +2px 오프셋**이 한 일이었다(그 오프셋은 절대값이라 가장 가는 종에 가장 크게 걸린다).

### ⓒ ⚑ 이 회차의 본체 — 지렛대 **여섯 개**가 전부 0 이거나 반대다

| 흔든 것 | 값 | `gale` 비 | 반응 |
|---|---|---|---|
| `SPEC_K` | 0.35 / 0.25 | 0.800 / 0.809 | **없음**(+1%. 같은 흔들기에 `ice` 는 0.274 → 0.194) |
| `SPEC_MAXR` | 2.6 / 1.8 | 0.800 / 0.799 | **없음** |
| 칸(SS=1) | ×1.45 / ×2.20 | 0.469 / 0.469 | **없음** |
| 칸(SS=2) | ×1.45 / ×1.08 | 0.704 / 0.800 | 있음, **나쁜 쪽** |
| 판 배율 | ×1 / ×2 | 0.469 / 0.704 | 있음, **나쁜 쪽** |
| `SPEC_MINK`·`SPEC_MINR` | (5회차 표) | 변화 0 | **없음** |

**어느 계수도 아니고 해상도도 아니면, 남는 것은 판정식 자신이다.** 5회차가 적어 둔 항등식이
`gale` 에서 성립하지 않는다: `hh` 는 «자기 거리 `d` 안에서 본 `D` 의 최댓값» 이라 **항상 `hh ≥ d`**
이고, 반쪽폭이 **자기 거리 안에서 안 잡히는 모양**(얇은 조각·좁아지는 날개)에서는 `hh ≈ d` 로
주저앉아 `d ≥ (1−K)·d` 가 **모든 잉크 화소에서 참**이 된다 ⇒ 코어 = 실루엣 전체.
⚑ **이 포화는 배율에 무관하다** — `d` 와 창 반지름이 같이 커지므로 판을 아무리 촘촘히 해도
같은 부등식이 그대로 참이다. 5회차가 «해상도» 로 지목한 것을 이번 회차가 **배율 실험으로 기각**했고,
남은 뿌리는 **`hh` 추정기의 도달 반경**이다.

⚠ **3회차의 «낮게 볼지언정 부풀리지 않는다(내접 창 d/√2)» 는 이 축에서 안전한 쪽이 아니었다** —
[B10] 에서는 `hh` 를 **낮게 보는 것이 곧 코어를 넓히는 쪽**이다(문턱이 같이 내려간다).
«안전» 을 적을 때 **어느 항이 그 오차를 먹는지**를 같이 적어야 한다는 것이 이 회차의 교훈이다.

### 게이트

`verify856` **13/13 PASS**(되돌린 뒤 · 수치는 5회차와 동일: [B12] 2.21 · `ice` 2.00 ·
`meteor` 덮임 0.854 · `gale` 0.469 · 밴드 1.65) · 제품 0줄이므로 이관·회귀 없음.
`node tools/smoke.js` **SMOKE PASS**.
⚠ **시간 비용은 안 쟀다** — 제품이 0줄이라 `verify792` [P1] 이 볼 대상이 없다. 다음 회차가
초과표본을 다시 든다면(권하지 않는다) 판 넓이 ×4 · 칸 수 ×6 이므로 **[P1] 부터** 재라.

### 남은 문제 · 다음 회차 (7회차)

1. **⚑ 1순위 — `hh` 를 «자기 거리 안» 이 아니라 «닿을 때까지» 읽어라(반복 확장).**
   `r₀ = d` 로 시작해 `h₁ = maxD(r₀)` 를 읽고, **부족하면 `r ← h₁ − d` 로 넓혀** 두세 번만
   되풀이한다(곧은 띠에서는 한 번에 마루에 닿아 `h` 가 정확하고, 좁아지는 날개에서는 필요한
   만큼만 넓어진다). ⚠ **4회차가 기각한 opening 과 다른 것**이 요점이다 — opening 은 **`q` 쪽
   반지름**으로 넘어와 굵은 쪽이 가는 쪽을 덮었고(ice [B12] 3.40), 이건 **`p` 쪽에서 모자라게
   읽는 것**을 고치는 반대 방향이다.
2. **제품보다 자를 먼저 세워라.** `tools/probe856.js` 에 «좁아지는 날개 · 얇은 조각 · 굽은 띠» 를
   넣어 **참값을 아는 도형에서** 추정기를 재고(현행 추정기가 그 셋에서 `h` 를 얼마나 낮게 읽는지
   표로), 그 표가 «낮게 읽는다» 를 확인한 뒤에 제품을 만져라. 5회차가 자를 그렇게 갈아 끼워
   `rico` 의 무리 배정을 고쳤다 — 같은 순서다.
3. **다시 걷지 마라**: ⓐ opening(4회차) · ⓑ 오르막(4회차) · 계수 흔들기 K·MINR·MINK·MAXR ·
   칸 규칙 · **초과표본(이 회차)**. 위 두 표가 각각의 기각 근거다.
4. 2·3회차가 남긴 것 중 이 작업 단위 **밖**인 것(whirl 본체 색 · 864 · 865 · ①③④ 축)은 그대로다.

## 이 세션의 마감 (2026-09-03, sess-1758-9630 루틴 워커 D)

**◐ 진행 — 6/10 회차.** 통과선(①~④ 최저 8)에 못 닿았고 lock 을 풀었다. 제품 0줄 · 자 0줄 ·
`verify856` 13/13 PASS · SMOKE PASS.
비평 채점은 **안 돌렸다** — 4·5회차와 같은 이유다(제품이 한 줄도 안 바뀌면 3회차 캡처를 다시
채점하는 것은 회차 예산만 태운다. 점수는 3회차 DB 4 / DC 3 그대로다).
이 회차가 만든 것은 **① 5회차 1순위(해상도)의 기각 근거**(배율 ×2 가 `gale` 을 0.469 → 0.704 로
민다)와 **② 지렛대 여섯이 전부 0 이거나 반대라는 표**, 그리고 그로부터 **③ 뿌리를 «계수·해상도» 가
아니라 «`hh` 추정기의 도달 반경» 으로 옮긴 것**이다. 7회차의 순서는 위 «남은 문제» 1·2번에 못박아 뒀다.

## 7회차 (2026-09-03, sess-1843-7230 루틴 워커 C) — **제품 0줄** · 6회차의 1순위(`hh` 도달)를 자로 기각하고 **뿌리를 찍었다**

6회차가 못박은 순서(**① 자를 먼저 세운다 → ② 참값을 아는 도형에서 `hh` 를 잰다 → ③ 그 표가
«낮게 읽는다» 를 확인한 뒤에 제품을 만진다**)를 그대로 밟았고, **②에서 가설이 죽었다.**
`probe856` 에 **§2 를 신설**했다(`node tools/probe856.js --hh`) — 참 반쪽폭을 **화소마다 아는**
합성 도형 14개(곧은 띠 5 · 얇은 조각 2 · **좁아지는 날개 4** · **굽은 띠 3**)에 제품의 판정식을
그대로 얹고, 추정기 셋(CUR = 제품 현행 · IT2/IT3 = 7회차 1순위 «반복 확장»)을 나란히 잰다.
자기검산 항(정확 EDT ↔ 해석 거리, 곧은 띠 w=24 최대 오차 **0.500px**)이 자를 먼저 못박는다.

### ⓐ 1순위(«`hh` 를 닿을 때까지 넓혀 읽어라»)는 **기각**이다 — 추정기는 안 틀렸다

| 도형 | CUR `hh`÷참 반쪽폭 | CUR 비 | IT2 비 | IT3 비 |
|---|---|---|---|---|
| 곧은 띠 w=12 | 1.000 | 0.500 | 0.500 | 0.500 |
| 좁아지는 날개 14→2 | 1.027 | 0.483 | 0.483 | 0.483 |
| 좁아지는 날개 8→3 | 1.051 | 0.581 | 0.581 | 0.581 |
| 굽은 띠 R=30 w=3 | 1.193 | 0.671 | 0.671 | 0.671 |

`hh` 는 **낮게 읽지 않는다** — 6회차가 «주저앉는다» 고 본 좁아지는 날개·굽은 띠에서조차
참값의 **1.00~1.19배**(오히려 살짝 높다)이고, 반복 확장(IT2·IT3)은 **소수점까지 같은 값**을 낸다.
⇒ 6회차 «남은 문제» 1번은 이 회차로 **기각**한다. 다시 걷지 마라.

### ⓑ ⚑ 이 회차의 본체 — 뿌리는 추정기가 아니라 **거리의 «반화소 규약»** 이다

같은 표가 **가설을 죽이면서 뿌리를 같이 찍었다**: `hh` 가 정확한 **곧은 띠에서도** 비가
`SPEC_K` 0.35 가 **아니다** — w=12 에서 **0.500** · w=6 에서 **0.667** · w=2 에서 **1.000**.
즉 결손은 «가는 종» 이나 «굽은 획» 이 아니라 **폭에 무관한 절대 오차**이고, 그래서
**가는 획일수록 크게 먹는다**(그 얼굴이 곧 «가는 종에서만 비가 튄다» 였다).

`edt2d` 는 «가장 가까운 **배경 화소 중심**까지» 를 돌려주므로 경계까지의 참 거리보다
예외 없이 **+0.5px** 크다. 그 오프셋은 `d` 와 `hh` 에 **같이** 얹히는데 문턱이 `(1−K)·hh` 라
**K 배만 상쇄**되고 `(1−K)·0.5` 가 남아 코어를 양쪽으로 넓힌다. 교정은 두 줄이다:

```js
const dT = d - 0.5, hT = hh - 0.5;          /* 반화소 규약 — 경계까지의 참 거리로 */
if(hT <= 0 || dT <= 0) continue;
const t = Math.max(hT - maxR, Math.min((1 - SPEC_K) * hT, hT - minR));
if(dT >= t){ core[p] = 1; hit++; }
```

⚠ 이것은 «문턱을 무르게/되게 푸는» 계수 조정이 **아니다** — 비율 갈래만 **+0.5·K = 0.175px**
움직이고 minR·maxR 갈래는 **항등적으로 그대로다**(`hT − minR + 0.5 = hh − minR`).

합성 도형(참값을 아는 입력)에서 **평균편차 0.239 → 0.172** · 곧은 띠 w=12 **0.500 → 0.333** ·
w=6 **0.667 → 0.333** · 날개 14→2 **0.483 → 0.349** · 굽은 띠 R=60 w=8 **0.487 → 0.415**.

### ⓒ 제품에 얹은 실측 — **여섯 지렛대가 못 움직인 `gale` 이 처음 움직였다**

| | 수리 전(6회차까지) | 반화소 교정 |
|---|---|---|
| **`gale` 비** | **0.469**(밴드 최대) | **0.346** ← 목표 0.35 |
| `arrow` | 0.391 | 0.286 |
| `shuri` | 0.42 | 0.396 |
| `lance` | 0.38 | 0.355 |
| **[B10b] 밴드** | **1.66배** | **1.55배** |
| [B10a]·[B10c]·[B11]·[R1~R3] | ok | **ok(전부 유지)** |
| **[B12] `ice` 흩어짐** | 2.00 | **2.57** ❌ 래칫 2.3 |

6회차 표의 지렛대 여섯(K·MINR·MAXR·칸 규칙·판 배율·MINK)이 `gale` 에서 전부 0 이거나 반대였던
이유가 여기 있다 — **그것들은 전부 «비율» 손잡이인데 결손은 «절대 오프셋»** 이었다.

### ⓓ 그런데 못 land 했다 — [B12] `ice` 2.57 은 **잡음이 아니다**(세 실험이 그것을 못박는다)

교정을 켜면 [B12] 가 래칫을 깬다. «1화소 깜빡임» 이라 의심하고 셋을 재 봤는데 **셋 다 2.57 로 같다**:

| 실험 | `gale` 비 | [B12] `ice` | 게이트 |
|---|---|---|---|
| 반화소 교정만 | 0.346 | **2.57** | 12/13 |
| + `SPEC_MINR` 0.38 → **0.55** | 0.859 ❌ | **2.57** | 10/13 |
| + `SPEC_MAXR` 2.6 → **3.1** | 0.347 | **2.57** | 12/13 |
| + 가장자리 **덮임(AA)** 으로 | 0.373 | **2.57** | 11/13(밴드 1.69 · `meteor` 마루덮임 0.721) |

⇒ `ice` 의 흩어짐은 코어 두께의 양자화도, 두 상수도 아니다. **바이어스가 가려 주고 있던
`ice` 자신의 실재하는 불균질**이다 — 6회차가 «가는 종의 비를 밴드 안에 잡아 주던 것은 규격이
아니라 판의 이산화 오차였다» 고 적은 것과 **같은 이야기의 [B12] 판**이다.
래칫은 «목표(1.5)에 닿기 전에 올리지 마라» 이므로 **올리지 않았고**, 게이트가 빨간 채로는
push 하지 않는다(지시서 [6]) ⇒ **제품을 되돌렸다**(`index.html` 최종 **0줄** · `verify856` **13/13 PASS** 복귀).

### 게이트

`verify856` **13/13 PASS**(되돌린 뒤 · 수치는 6회차와 동일: `gale` 0.469 · 밴드 1.65 ·
[B12] 2.21 · `ice` 2 · `meteor` 덮임 0.854) · `probe856 --hh` **자기검산 ok(0.500px)** ·
제품 0줄이므로 이관·회귀 없음 · `node tools/smoke.js` **SMOKE PASS**.

### 남은 문제 · 다음 회차 (8회차)

1. **⚑ 1순위 — `ice` 의 불균질을 먼저 닫아라. 그 다음 위 ⓑ 두 줄을 그대로 얹으면 된다**(패치는
   위에 적혀 있다). 순서가 반대면 8회차도 되돌리기로 끝난다. 재는 자리는 `verify856` 의
   `spread()` — 본체 주 마루 화소마다 «그 자리 코어 두께 ÷ 그 자리 본체 두께» 의 p90÷p50 이다.
   ⚠ 먼저 **어느 자리가 p50 을 끌어내리는지** 찍어라(자루 ↔ 날 ↔ 끝단). `ice` 는 [B11] 마루
   덮임이 1 이라 «코어가 없는 자리» 는 아니다 — **있는데 얇은** 자리다.
2. **다시 걷지 마라**: `hh` 반복 확장(이 회차 ⓐ) · `SPEC_MINR` 상향(ⓓ) · `SPEC_MAXR` 상향(ⓓ) ·
   가장자리 덮임 AA(ⓓ) · opening/오르막(4회차) · 초과표본(6회차) · 칸 규칙(5·6회차).
   ⚑ **남은 미탐색 축은 «비율 손잡이» 가 아니라 «절대 오프셋» 계열이다** — 이 회차가 그 첫 번째다.
3. **자는 이제 참값을 안다** — `probe856 --hh` 는 제품과 무관한 입력만 쓰므로(333 규약) 8회차가
   무엇을 만지든 **먼저 그 도형들에서** 재 보고 제품으로 가라. 새 처방이 곧은 띠 w=12 에서 0.35 를
   못 내면 제품에서도 못 낸다.
4. 2·3회차가 남긴 것 중 이 작업 단위 **밖**인 것(whirl 본체 색 · 864 · 865 · ①③④ 축)은 그대로다.

## 이 세션의 마감 (2026-09-03, sess-1843-7230 루틴 워커 C)

**◐ 진행 — 7/10 회차.** 통과선(①~④ 최저 8)에 못 닿았고 lock 을 풀었다. 제품 **0줄** ·
`probe856` **§2 신설**(추정기 편향표 + 자기검산) · `verify856` 13/13 PASS · SMOKE PASS.
비평 채점은 **안 돌렸다** — 4·5·6회차와 같은 이유다(제품이 한 줄도 안 바뀌면 3회차 캡처를 다시
채점하는 것은 회차 예산만 태운다. 점수는 3회차 DB 4 / DC 3 그대로다).
이 회차가 만든 것은 **① 6회차 1순위(`hh` 도달)의 기각 근거**(참값을 아는 도형에서 `hh`÷참값
1.00~1.19 · 반복 확장 무변화) · **② 뿌리를 «비율 손잡이» 에서 «거리의 반화소 규약» 으로 옮긴 것**
(곧은 띠 w=12 에서도 비가 0.500) · **③ 그 교정이 `gale` 을 0.469 → 0.346 으로 옮긴다는 실측과
그것을 막는 단 하나의 항([B12] `ice`)의 특정**, 그리고 **④ 8회차가 그대로 얹을 수 있는 두 줄짜리
패치**다. 8회차의 순서는 위 «남은 문제» 1번에 못박아 두었다.

---

## 8회차 (2026-09-03, sess-1912-8590 루틴 워커 A) — **제품 3자리** · 7회차가 특정한 단 하나의 항([B12] `ice`)을 닫고 반화소 교정을 같이 얹었다

7회차가 못박은 순서(**① `ice` 불균질을 먼저 닫고 → ② 반화소 두 줄을 그대로 얹는다**)를 그대로 밟았다.
①에서 뿌리가 **7회차가 짐작한 자리가 아니었고**, 그것을 고치자 ②는 저절로 따라왔다.

### ⓐ 먼저 «어느 자리가 p50 을 끌어내리는지» 찍었다 (7회차 «남은 문제» 1번)

`verify856` 에 **진단 전용** 스위치 `--spread <종>` 을 달았다(판정에는 한 글자도 안 쓴다 · 재는 자리·
순서는 `spread()` 그대로다 — 자를 따로 만들면 그것이 곧 사본이다). `ice` 의 주 마루 **30화소 전수**:

| 자리(팔을 따라) | 본체 반쪽폭 | 코어 두께 | 비 |
|---|---|---|---|
| 바깥 끝 | 7.0 | 1.0 | 0.143 |
| **한복판** | **7.6 → 11.0** | **0** | **0** ← 8화소 |
| 안쪽 | 11.8 → 16.6 | 1 → 6 | 0.085 → 0.361 |
| 자루(오른쪽) | 7~8 | 2 | 0.25~0.286 |

**«끝단이 얇다» 가 아니라 «팔 한복판이 통째로 비어 있다»** 였다 — 그리고 그 자리는 본체가
**가장 굵어지는 쪽으로 기울어진 마루**다. 대조로 `multi`(흩어짐 1.00)를 같은 자로 찍으면 마루
165화소가 처음부터 끝까지 0.4 로 고르다 — **긴 곧은 획은 멀쩡하고, 기울어진 마루만 빈다.**

### ⓑ ⚑ 이 회차의 본체 — 이웃이 «내 굵기» 를 대변할 **자격**을 물었다

뿌리는 반쪽폭 추정 `hh` = «자기 거리 안에서 본 D 의 최댓값» 이 **기울어진 마루에서 옆의 더 굵은
딴 자리를 제 굵기로 읽는다**는 것이다(`ice` 팔 한복판에서 D 9 인 자리가 10px 떨어진 D 16 을 본다
⇒ 문턱 `(1−K)·16 = 10.4 > 9` ⇒ 코어 0). 창을 좁히는 손잡이(칸 규칙·해상도·과잉표본)는 4·5·6회차가
전부 밀어 봤고 **가는 종이 포화한다**는 반대편 벽에 막혔다. 이 회차는 창의 **크기**를 정하지 않고
이웃의 **자격**을 정했다:

```
q 가 p 의 반쪽폭을 대변한다  ⟺  D(q) ≥ dist(p,q) + d(p)
```

**q 의 내접원이 p 를 «내 깊이만큼 여유를 두고» 덮을 때만** 그 굵기를 인정한다.
- 곧은 띠에서 제 축은 `dist = h − d`, `D = h` 라 **등호로** 통과한다 ⇒ 가장 가는 종(`gale`)도 제 축을
  본다(5회차가 «칸을 좁히면 포화한다» 로 막힌 자리가 여기서 풀린다).
- 기울어진 마루 옆의 더 굵은 딴 자리는 `dist(10) > D−d (7)` 로 **떨어진다** ⇒ `ice` 팔이 채워진다.
- 3회차가 남긴 처방(«opening: `|p−q| ≤ D(q)`»)의 **«여유 d» 판**이다. 4회차가 기각한 것은 여유 없는
  판이었고(그것은 반대로 **넓혀서** ice 3.40), 여유 한 항이 부호를 뒤집는다.

같은 이유로 **칸을 1px 씩**으로 바꿨다(등비 칸의 «틈» 이 곧 자격 조건의 군더더기다) — 굽는 판이
S² ≈ 6만이라 칸마다 곧바로 접으면 굽기 한 번의 비용이지 프레임 비용이 아니다(`verify792` [P1] 유지).

그리고 **상한 갈래는 옛 읽기(«닿는 데까지 본 가장 굵은 곳»)로 따로 굽는다** — 규격이 두 문장이고
**묻는 것이 다르기 때문**이다: 비율 갈래는 «여기 이 획이 얼마나 굵은가», 상한 갈래는 «상한을 씌워야
할 만큼 큰 덩어리 속인가». 한 벌로 쓰면 길쭉한 덩어리의 가는 자리에서 상한이 코어를 통째로 지운다
(실측: 한 벌로 뒀을 때 `rico` 12.02 · `meteor` 10.60 ⇒ 덩어리 폭 밴드 1.89).

②(7회차의 **반화소 규약** 두 줄)는 예고대로 그대로 얹혔다 — 비율 갈래만 `0.5·K = 0.175px` 움직이고
`minR`·`maxR` 갈래는 항등적으로 그대로다.

### ⓒ 실측 — 7회차가 «단 하나» 로 특정한 항이 닫혔다

| | 7회차까지 | **8회차** |
|---|---|---|
| **[B12] `ice` 흩어짐** | **2.00**(반화소만 얹으면 2.57 ❌) | **1.14** |
| [B12] 최대 | 2.21 (`stone`) | **1.68** (`stone`) |
| [B12] 래칫 초과 종 | 2 | **1** |
| `gale` 비(밴드 최대) | 0.469 | 0.454 |
| `boomer` 비(밴드 최소) | 0.289 | 0.272 |
| [B10b] 획 밴드 | 1.65배 | 1.67배 |
| [B11] `meteor` 마루 덮임 | 0.853 | **0.91** |
| 덩어리 «본체 대비 비» 밴드 | **1.73배** | **1.30배** |

⇒ **[B12] 래칫을 2.3 → 1.7 로 조였다**(4회차가 «ⓒ 가 들어가면 조여라» 로 예고한 자리 · 목표 1.5).

### ⓓ 자 이관 1자리 — [B10c] 는 «폭» 이 아니라 «본체 대비 비» 를 물어야 한다 (333 처방)

[B10c] 는 «덩어리에서는 **상한이** 폭을 정한다» 를 전제로 «폭 자체가 한 밴드» 를 물었다. 그 전제는
**반쪽폭을 넓게 읽던 자** 위에서만 참이었다 — 닿는 데까지 본 가장 굵은 곳이 곧 그 자리의 굵기였으니
덩어리는 **어디서나** 상한에 걸렸다. 굵기를 국소로 읽는 순간 길쭉한 덩어리(`rico`·`meteor`·`flask`)의
**가늘어지는 자리**는 상한이 아니라 그 자리 획 폭의 K 가 정하게 되고 — 그것이 비평가들이 «본체를
안 따라간다» 로 지적한 바로 그 성질이다 — 종을 한 수로 요약한 «폭» 은 더 이상 한 값이 아니다.

⚠ **무르게 푼 것이 아니라 더 센 축으로 옮긴 것이다**: 새 축을 **수리 전 제품**에 대면
`rico` .329 ÷ `meteor` .19 = **1.73배로 빨갛다**(착수 실측). 지금은 1.30배다. 옛 항이 초록이던 자리에서
새 항이 빨갛고 그 반대가 아니다. 규격 문장 자체(«코어는 덩어리의 채움이 아니다»)는 **[B10d] 신설**이
따로 못박고(상한 폭 +5% · 상수는 제품에게 묻는다), 새 축의 **되돌림 시험 [R4] 도 같이 세웠다**
(종별 손그림에서 4.35배로 빨갛다 — 누른 항을 묻는 항을 한 줄 더 넣는다 · 328~330 교훈).

### 게이트

`verify856` **15/15 PASS**([B10d]·[R4] 신설 · 래칫 2.3 → 1.7) · 이관 0종(`verify792` **25/25 무수정** ·
`verify710` 14/14 · `verify541` 68/68) · `node tools/smoke.js` **SMOKE PASS**.

### 8회차 채점 — 비평가 DD·DE 독립 2인 (`docs/shots/856-r8.png`)

| 비평가 | ① 분간 | ② 통일 | ③ 덩치 | ④ 의미 | 최저 |
|---|---|---|---|---|---|
| DD | 8 | 6 | 7 | 8 | **6** |
| DE | 7 | 5 | 6 | 7 | **5** |

**세 회차 만에 최저 점수가 처음 움직였다**(1회차 3·5 → 2회차 4·5 → 3회차 3·4 → **8회차 5·6**).

**ⓔ — 앞 회차 1순위 셋을 두 사람이 각자 다시 재서 판정했다:**

| 자리 | 3회차 | DD | DE | 판정 |
|---|---|---|---|---|
| `ice` 팔(«획 굵은데 코어 0px») | 0% | w≥8 단면 91개 중 **0px 0개** · 팔 한복판 26px→8px = **30.8%** | 본체 112열 중 **107열**에 코어 · 중앙 **33.3%** · 코어0 단면 **0개** | **닫혔다(2인 일치)** |
| `arrow` 머리 · `curve` 뿔 | 2.7% | `curve` **0/74** · `arrow` 7/196 은 좌표를 6배로 확대해 보니 **자루 모서리의 medial-axis 허상** ⇒ 0/263 | `curve` **2/80 = 2.5%** · `arrow` **23/262 = 8.8%**(머리가 아니라 **접합부**로 옮겼다) | **갈렸다** — `curve` 는 둘 다 «거의 닫힘», `arrow` 는 **접합부 잔여**로 9회차가 다시 잰다 |
| 굵은 종 «흰 덩어리»(80~92%) | — | boom **17.9%** · meteor 8.8% · bounce 8.0% · 덩어리 6종 최대 코어 폭 **20.0~21.3px(6.5% 안)** | boom **2.3%** · meteor 5.7% · bounce 6.6% · 덩어리 7개소 폭 **상한 1.16배 안** | **닫혔다(2인 일치)** |

⚑ **9회차 1순위 — 2인 독립 일치. «③층 폭 = 0.35·획» 이 아니라 «4px 바닥 + 0.27·획» 으로 그려져 있다.**
DD 적합 **코어 = 0.273·획 + 1.07px**(단면 1,593개) — 획 8–10 / 10–12 / 12–14px 세 구간의 코어 중앙값이
**셋 다 정확히 4.0px** 로 같다(규격대로면 3.1 / 3.8 / 4.5 로 갈려야 한다) ⇒ **바닥이 규격을 덮어쓴다**.
DE 도 같은 수를 따로 냈다 — 획 8–11px 단면 **555개(32.3%)의 중앙 비율 50.0%** · 코어 폭 p10/중앙/p90 =
**3.0/4.0/4.0px 로 4px 아래로 내려간 종이 없다**. 피해가 가장 큰 종은 **`gale`**(선 3줄이 전부 8.8px라
종 전체가 45.5%로 고정 · 옆 `boomer` 13.8px 는 30.6%) — **[B10a] 가 초록인 채로 눈에는 gale 만 속심이
굵어 보인다**(자는 종을 한 수로 요약하고, 두 사람은 **획 폭 구간별로** 층화해 쟀다).
⇒ 뿌리는 `SPEC_MINR`(«가장 가는 획도 코어를 갖는다» 는 절대 바닥)이고, 5회차가 «가는 종에서는
«코어가 있다»와 «코어가 획의 35%»가 동시에 성립할 수 없다» 고 적어 둔 그 벽이 **이제 자로 보인다**.
**9회차가 세울 자 = [B13] «획 폭 구간별 비의 중앙값이 한 밴드»**(종별 한 수가 아니라 구간별) —
자를 먼저 세우고 그 다음에 바닥을 만져라(338 규칙).

**그 밖에 2인이 각자 적은 것**(9회차 재료):
- 같은 획 폭에서 종끼리 1.4~1.5× 어긋난다(DD: w19~23 에서 `lance` 38.5% vs `shuri` 27.5%).
- 잔여 코어0 단면 — DD `whirl` 1/152 · `slash` 1/76 / DE `slash` 마구리 9/79 · `whirl` 팔끝 22/197.
  **둘 다 «둥근 마구리·팔 끝»** 을 짚었다(자격 조건이 끝단에서 보수적인 쪽으로 틀리는 자리다).
- 덩어리의 **부차 조각**(meteor 위쪽 조각·아래 표식 · rico · bounce)은 6.7~15.3% 로 규격에 못 미친다(DE).
- `meteor` 반투명 꼬리를 ②본체로 볼지 ①후광으로 볼지 **정해져 있지 않다**(DD) — 9회차가 먼저 정하라.

**이 작업 단위 밖(2인 공통, 그대로 남는다)**: ③ 덩치(√면적 2.70~3.28배 · 838 계열) ·
① 실루엣 겹침(`slash`↔`curve` IoU 70.8% · `shuri`↔`whirl` · **864**) · ④ `lance` 종횡비 7.45(**865**).

## 이 세션의 마감 (2026-09-03, sess-1912-8590 루틴 워커 A)

**◐ 진행 — 8/10 회차.** 통과선(①~④ 최저 8)에 못 닿았고 lock 을 풀었다. 남은 회차는 2회다.
이 회차가 만든 것은 **① 4~7회차가 «제품 0줄» 로 넘긴 자리를 실제로 고친 것**(제품 3자리 ·
`ice` 흩어짐 2.00 → **1.14** · [B12] 래칫 2.3 → **1.7** · 덩어리 «본체 대비 비» 밴드 1.73 → **1.30**) ·
**② 세 회차 만에 최저 점수가 처음 오른 것**(3~4 → 5~6) · **③ 앞 회차 1순위 셋 중 둘이 2인 독립으로
«닫혔다» 를 받은 것** · **④ 9회차가 그대로 잡을 «바닥이 규격을 덮어쓴다»(2인 일치·수치 동일)** 다.

⚑ **9회차가 반드시 볼 것 넷**
1. **자를 먼저 세워라** — [B13] «획 폭 **구간별** 비의 중앙값이 한 밴드». 지금 자는 종을 **한 수**로
   요약해 [B10a] 가 초록인데 두 비평가가 각자 «획 8~12px 에서 45~50%» 를 쟀다. 자가 못 보는 것을
   고치려 들면 6회차가 겪은 «지렛대가 안 움직인다» 가 되풀이된다.
2. **`SPEC_MINR` 을 «절대 바닥» 에서 «해상도 바닥» 으로 다시 읽어라** — 4px 은 굽는 판의 한 화소가
   아니라 **AA 를 포함한 렌더 결과**다. 바닥을 내리려면 굽는 판의 해상도(HALO_SS)를 그 종에서만
   올리는 것이 아니라, **바닥을 «획 폭에 비례하는 하한»(SPEC_MINK 계열)으로** 바꾸는 쪽을 먼저 재라.
3. **다시 걷지 마라**: `hh` 반복 확장(7회차) · `SPEC_MINR`/`SPEC_MAXR` 상향(7회차) · 가장자리 AA(7회차) ·
   여유 없는 opening(4회차) · 오르막 마루 읽기(4회차) · 초과표본(6회차) · 해상도(6회차) ·
   **칸 규칙(8회차가 1px 로 못박았다 — 되돌리면 `ice` 가 다시 빈다)**.
4. **[B12] 래칫 1.7 을 목표 1.5 에 닿기 전에 올리지 마라.** 남은 초과는 `stone` 1.68 하나다.
