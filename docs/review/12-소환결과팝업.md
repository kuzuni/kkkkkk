# 12 소환 결과 팝업 — 비평 기록

> 측정표: `docs/measure/12-소환결과팝업.md`
> 캡처: `docs/shots/12-r1.png` (1회차)
> 채점 규칙: 6개 항목(①배치 ②상대크기 ③여백·간격 ④정렬·그리드 ⑤색·명암 ⑥폰트비율) 각 10점,
> **그 중 최저 항목 점수가 그 비평가의 점수**. 비평가 2명의 최저값이 9 이상이면 통과.
> 레퍼런스 1080×2340 / 캡처 1080×1920 — 가로 1:1, 세로는 2종 변환
> (리본·패널·그리드 `ref_y − 84` / 버튼·터치하여닫기 `ref_y − 419`).
> 그 사이 빈 구간이 435 → 100 으로 줄어드는 것은 **의도된 420px 흡수**라 매번 감점 제외로 명시했다.

---

## 껍데기 판정 — A5 3분류에 없는 **(라) 전면 딤 + 전폭 밴드 오버레이**

09(일괄 강화 결과)와 같은 «딤 위 연출» 계열이지만 **09 와도 다르다**.
09 는 패널이 아예 없는 자유 배치인데, 12 는 **불투명 전폭 패널이 하나 있다.**

- 패널 좌우 끝이 **x=0 / x=1079** — `y=795 · 1000 · 1329` 가로 전 라인 스캔에서 전 구간 동일색.
  좌우 테두리도 radius 도 **없다**. 그래서 `.mbox`(A5 공용 모달)로는 표현 자체가 불가능하다.
- 패널 내부는 `#2A2835` **단일색**(무늬·그라데이션 0). 뒤 상점 카드가 전혀 비치지 않음 → 불투명 확정.
- 딤은 `10-상점-팝업-소환-탭.jpg` 와 동일 콘텐츠 영역 20.7만 채널 최소자승 회귀로 **k=0.1979 → α=0.802**.
  09(.80)와 같고 08(.55)·A5(.65)·05(.55)와는 전혀 다르다 — **결과 연출 계열이 공통으로 딤이 깊다.**
- 하단 버튼 3개는 대조 이미지에 **아예 없는 요소** → 딤이 걸린 배경이 아니라 원래 은색 버튼.

→ 전용 오버레이 `#sumw` 를 신설했다.

## 세로 좌표계 — PROGRESS «확정(03)» 2종 변환이 여기서 정확히 맞아떨어졌다

| 그룹 | 변환 | 검증 |
|---|---|---|
| 리본·패널·그리드 | `ref_y − 84` | 패널 상단 793 → **709** (실측 709) |
| 버튼 3개·터치하여 닫기 | `ref_y − 419` | 버튼 상단 1766 → **1347** (실측 1347) |

두 앵커가 모두 참이 되면 패널 하단(1247)과 버튼 상단(1347) 사이가 **100px** 이 된다
(레퍼런스는 435px). **420px 차이가 전부 이 빈 딤 구간에서 흡수된다** — 전투 캔버스가 없는
오버레이 화면에서 «세로 차이를 어디가 먹느냐» 에 대한 답이다. 다른 결과 연출 화면도 같게 쓰면 된다.

`터치하여 닫기` 는 **09 와 앵커가 완전히 같다**(ref 잉크 하단 2164, 탭바 상단 2160 → 프레임 bottom −4).
09 의 `.upr-close{bottom:175px}` 과 1px 차이(174)로 수렴했다.

---

## 1회차 — 구현 전 자가 실측 (비평가에게 보내기 전에 맞춘 것)

04 비고 1 «캡처 상태가 레퍼런스와 다르면 그 회차 비평은 통째로 무효다» 를 피하려고,
비평가를 띄우기 **전에** DOM 실측 + 픽셀 스캔으로 측정표와 대조해 먼저 맞췄다.

### DOM 실측 (getBoundingClientRect, #app 기준) — 전 항목 Δ0

| 요소 | 목표(측정표 → 변환) | 실측 | Δ |
|---|---|---|---|
| 패널 | 0,709 1080×539 | 0,709 1080×539 | **0** |
| 그리드 | 36,815 1007×335 | 36,815 1007×335 | **0** |
| 카드 r0c0 | 36,815 157×157 | 36,815 157×157 | **0** |
| 카드 r0c5 | 886,815 | 886,815 | **0** |
| 카드 r1c0 | 36,985 | 36,985 | **0** |
| 리본 밴드 | 284,641 513×87 | 284,641 513×87 | **0** |
| 꼬리(검정 실루엣) | 243,659 89×93 | 243,659 89×93 | **0** |
| 버튼 ①②③ | 117 / 416 / 716, 1347 247×148 | 동일 | **0** |
| 알약 ① / ② | 204,1423 132×36 / 442,1423 193×36 | 205,1423 / 441,1423 | **1** |
| FREE 뱃지 | 118,1424 73×33 | 119,1424 73×33 | **1** |

카드 열 좌단은 픽셀 스캔으로도 **36 / 206 / 376 / 546 / 716 / 886 전부 일치**(y=894 가로 스캔).

### 텍스트 잉크 — 픽셀 스캔으로 3회 보정

| 텍스트 | ref 잉크 | 최초 | 보정 후 | 조치 |
|---|---|---|---|---|
| `소환 결과` | 170×41 | 162×41 | **170×41 (Δ0)** | scaleX .88 → **.923**, top 20 → 19 |
| `터치하여 닫기` | 256×41 | 248×41 | **256×41 (Δ0)** | scaleX .93 → **.96**, bottom 175 → 174 |
| 버튼 라벨 `10회 소환` | 139×30 | 124×29 | **138×30 (Δ1)** | font-size 32 → **33**, scaleX .9 → **.978**, top 19 → 22 |
| 개수 배지 `1` | 9×25 | 16×26 | **9×26 (Δ0/1)** | `<i>` 에 **scaleX .62**, bottom −14 → −19 |
| `1,000` | 90×25 | 90×26 | **90×26 (Δ0)** | left 66 → 61, top 63 → 65 |
| `0/1` | 50×25 | 46×27 | **50×24 (Δ0/1)** | font-size 36 → 34 + **scaleX 1.15**, top 62 |
| 카드 아이콘 잉크 | w60~77 h67~77 | 86×99 | **71×70** | font-size 78 → **64** |

### 색 — 실측색과 Δ0

패널 본문 `#2A2835` · 리본 밴드 `#FFBE56` · 밴드 하이라이트 `#FEEF88` · 꼬리 `#F69632` ·
버튼 면 `#989898` · 버튼 밝은 림 `#D3D3D3` · 버튼 하단 립 `#767676` · 알약 `#747474` ·
FREE 뱃지 `#FF5D7A`/`#FF3964` — **전부 스캔 결과가 측정표와 정확히 일치**.
꼬리 드룹 그늘도 실측 위치대로: `#DD8335` x292~310, `#A55019` x315~324 (ref 292~310 / 313~324).

---

## 이번에 잡은 함정 3개 (다른 화면에서도 재발한다)

1. **패널 패딩에서 «배지 돌출분» 을 두 번 빼면 안 된다.**
   개수 배지가 카드 바닥 아래로 8px 튀어나와서 그리드 박스를 327 → 335 로 키웠는데,
   패널 아래 패딩을 그대로 두는 바람에 패널이 **539 → 546 으로 7px 길어졌다.**
   `padding-bottom = (본문아래여백 91 − 배지돌출 8) + 하단크롬 15 = 98` 이 맞다.

2. **카드 «면색» 마스크로 아이콘 잉크를 재면 안쪽 밝은 림이 통째로 섞인다.**
   면색만 빼고 스캔하니 잉크가 87×105 로 나왔는데, **림색까지 함께 제외**하니 실제로는 63×64 였다.
   림이 6px 짜리 별도 색이라 «면과 Δ>55» 조건에 전부 걸린 것이다.
   더해서 **개수 배지의 검정 외곽선(9px)이 카드 안쪽까지 올라와** 잉크 높이를 88 로 부풀렸다 —
   `max(RGB)<40` 을 같이 제외해야 정상값(70)이 나온다.

3. **`border-radius` 가 큰 요소는 모서리 근처 열에서 세로 스캔하면 안 된다.**
   카드(radius 34)를 좌단에서 24px 떨어진 x=60 열로 세로 스캔했더니 테두리가 5px 이 아니라
   4px 로, 카드 높이도 157 이 아니라 154 로 읽혔다. **아직 원호 구간**이기 때문이다.
   DOM `getBoundingClientRect` 로 재니 정확히 157 이었다. 원호 밖(중앙 근처) 열을 골라야 한다.

---

## 구현 메모 (다음 담당자용)

- 버튼 3개는 **10 상점 소환 탭이 만든 `freeLeft()` / `SHOP_FREE` / `doSummonFree()` / `summonCost()` 를
  그대로 재사용**한다. 저장 구조를 건드리지 않았다(KEY 유지).
- 레퍼런스 카드 등급색은 **초록 7 / 갈색 2 / 파랑 1** 3종뿐이다. 실측 3종을 `SUM_CARD[0..2]` 에 그대로 넣고
  상위 3등급(영웅·전설·신화)은 같은 «면 + 밝은 림» 명암 구조로 확장했다 — **레퍼런스에 표본이 없다.**
  기존 `GRADE.bg/.c` 는 어두운 남색 계열이라 이 화면에는 못 쓴다(다른 화면 것은 건드리지 않았다).
- **같은 아이템이 여러 번 나오면 칸을 늘리지 않고 개수로 합친다.** 레퍼런스에 개수 배지가 있는 이유가
  이것이다. 그래서 10연을 해도 카드가 10칸이 아닐 수 있다.
- **30연은 5행(837px)이라 우리 프레임의 패널↔버튼 100px 안에 못 들어간다.**
  09 처럼 «앞 N칸만 표시» 로 자르지 않고 **레퍼런스 케이스(2행 335px)를 픽셀 그대로 두고
  초과분만 스크롤**하도록 했다. 스크롤바는 숨겼다.
