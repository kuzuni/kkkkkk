# 667 — 이용권 UI 디자인 재작업 (레퍼런스 원본 대조)

주인 지시(2026-09-02 00:50): «이용권 부분 디자인이 허접한데 원본꺼 참고도 좀 다시해줘야함 디자인».

- 레퍼런스 `docs/ref/151-이용권-카드.png` — **카드 2장 크롭(504×709)**. 표준 변환 «프레임 y = ref y − 84» **해당 없음**.
  비교 단위는 «카드 하나», 환산은 **ref 카드 폭 474.12 → 우리 978, k = 2.0628** 하나뿐이다(측정표 §9).
- 선행 작업 151 이 8회차에 ①~④ **8점**(J 9/8/8/8 · K 8/8/8/8)으로 통과했고, 667 은 그 위에서 **≥9** 를 목표로 한다.
- 하네스 `tools/cap151.js` · 자 `tools/verify151.js` · 잉크 `tools/ink151.py`.

## 착수 전 확인 — 199 연동 (등재문의 선행 조건)

등재문은 «199 결3 ⓑ 로 이용권이 «+4h» → «×배율» 상품으로 개편 중이니 **새 상품 전제**로 그리라,
199 lock 확인 후 착수» 라고 못박았다. **그 개편은 이미 제품에 들어와 있다** —
`PASS_OFF_MUL = 1.2` · `offMul()` · `PASS_ITEMS[].bl` 의 «오프라인 보상 획득량 ×1.2» 가 전부 파생 문구다
(`OFF_MAX_H`·`offMaxH()` 는 선언째 폐지됨). ⇒ «+4h» 로 되그릴 위험이 없어 착수했고,
이번 회차가 만진 것은 **카드 실루엣·몸통 패턴(CSS)** 뿐이라 199 가 잡고 있는 계수 자리와 겹치지 않는다.

## 1회차 (2026-09-01, sess-1426-17297 루틴 워커 B)

### 수리 전 실측

- `verify151` **87/87 PASS** · `ink151` 잉크 **높이** Δ 전 항목 ±6.2% 안(폭 Δ 는 문구가 달라 무의미 — 측정표 규칙).
  ⇒ **글자 크기·상자 기하는 이미 맞았다.** 남은 결손은 «카드가 ref 처럼 안 생겼다» 쪽이고,
  그 자리는 UI-REFERENCE 151 체크리스트에 **`[ ]` 두 줄로 이미 적혀 있었다.**

### 고친 것 ① 카드 우변 «물결 노치» (미구현 → 구현)

측정표 §1-3 은 이것을 «**카드 우변은 손그림 물결**» 이라는 **이 아트의 규칙**으로 적어 뒀다(초록·파랑 둘 다 있다).

| | ref 실측 | ×k 환산 | 우리 구현 |
|---|---|---|---|
| 깊이 | 15.6 | **32.2** | **32** (실측: 곧은 변 바깥 x 1029 → 노치 최심 997) |
| 세로 길이 | 51 | **105.2** | **105** (실측 편차 구간 100 — 양 끝이 서브픽셀로 얕다) |
| 하변에서 노치 하단까지 | 초록 110.7 · 파랑 106.3 | **228 / 219** | **108 + 105 = 213** |

⚑ **«카드 높이의 몇 %» 가 아니라 «하변 기준 상수» 다.** ref 두 장은 높이가 771/534 로 다른데
노치는 **둘 다 하변에서 106~111** 이다 — 비율로 걸었으면 한 장이 반드시 틀렸다(LESSONS 151-② 의 반대 짝:
«ref 값이 둘인데 서로 같으면 그건 함수가 아니라 상수다»). 우리 카드 셋(534/690/690)에 그대로 맞는다.

⚑ **깎지 않고 «구멍 + 덧댄 호» 로 냈다.** LESSONS 151-⑥ 이 «요소 밖으로 나가는 자식이 있는 상자에는
clip-path 를 걸지 마라»(리본 제비꼬리가 금색 판을 −11% 깎았다)고 못박아 뒀다. `.pvc` 에는 밖으로
나가는 자식이 넷(상태 탭·알약·뱃지·리본)이다. ⇒ 마스크는 **자식이 없는 두 칠면**
(`.bg` = 몸통+안쪽 림 · `.fr` = 검정 테)에만 걸고, 뚫린 자리의 검정 외곽선은 `.ntc` 가 **속이 빈 링**으로 덧댄다.

⚑ **속을 «배경색» 으로 칠하지 않은 것이 핵심이다.** 처음 안은 «#272731 로 칠한 도형» 이었는데
캡처를 재 보니 카드 뒤 바탕이 **단색이 아니었다** — 카드 오른쪽 실측이 (54,54,63) ~ (49,49,58) 로 흐른다
(`#shopw` 선언은 `#272731` 인데 그 위에 다른 층이 얹혀 있다). 색을 박았으면 **자리마다 어긋났을 것**이다.
구멍이라 진짜 뒤가 비친다 — 검산: 구멍 안 (51,51,61) ↔ 카드 밖 같은 행 (50,50,59).

- `.pvc{--ntc-fr}` = `.fr` 박스 기준 `radial-gradient(32px 52.5px at 100% calc(100% - 160.5px))`
- `.pvc{--ntc-bg}` = `.bg` 가 사방 10px 안쪽이라 같은 점이 `calc(100% + 10px), calc(100% - 150.5px)`
- 타원 중심이 **카드 우변 위**(x=978)라 왼쪽 절반만 카드 안 ⇒ 깊이 = 가로 반지름 32.

### 고친 것 ② 몸통 «마름모 격자» 대비 (체크리스트 «ref 의 1/4~1/2»)

옛 값 `repeating-linear-gradient(45deg, transparent 0 30px, rgba(0,0,0,.05) 30px 60px)` 은 결손이 **둘**이었다:
ⓐ 마름모가 아니라 **한 방향 사선**이고 ⓑ 대비가 ref 의 절반이다.

⇒ `conic-gradient(from 45deg, …)` 4분면을 60px 타일로 깐다. 타일이 두 대각선으로 삼각형 4개로 갈리고
이웃 타일의 삼각형끼리 붙어 **마름모 체커**가 된다(가로 이웃의 좌·우 삼각형이 한 마름모, 세로 이웃의
상·하 삼각형이 한 마름모). 알파는 ref 실측 비 **52/57 · 179/196 · 128/141 = 0.912** 의 여집합 **.088**.

| | ref | 우리(수리 후 실측) | Δ |
|---|---|---|---|
| 몸통 밝은 톤 | (57,196,141) | (57,194,140) | ≤ 2/255 |
| 패턴 어두운 톤 | (52,179,128) | (52,177,128) | ≤ 2/255 |
| 두 톤 면적비 | 체커 = 1:1 | 12727 : 12138 | 1.05 |

### 게이트·회귀

- `verify151` **87/87 무수정** (마스크·패턴은 `getBoundingClientRect` 를 안 건드린다)
- `ink151` 9항 **수리 전과 같은 값** (잉크 높이 Δ 전 항목 ±6.2%)
- **SMOKE PASS**

## 2회차 (2026-09-01, sess-1426-17297 — 비평 AR·AS 반영)

### ⚠ 먼저 — 비평 2건은 **낡은 캡처를 본 것**이라 지적에서 뺐다

AR·AS 에게 준 캡처가 **1회차 수리 «전»** 파일이었다. 그래서 둘 다 1순위로
«노치 0px 미구현» · «격자가 단방향 줄무늬» 를 냈는데, **같은 자로 두 캡처를 대조하면 갈린다**:

| | 옛 캡처(비평가가 본 것) | 현재 코드 | ref 환산 |
|---|---|---|---|
| 우변 프로파일(초록, 로컬 y470~590) | **1018 고정** | 1018 → **996** → 1018 | 깊이 32 |
| 몸통 어두운 톤 | (54,184,133) | **(52,177,128)** | (52,179,128) |

⇒ 그 둘은 이미 1회차에 들어가 있었다. **나머지 지적은 내가 안 건드린 부품이라 그대로 유효하다.**
⚑ **교훈** — 캡처 파일 이름에 회차를 박고, 비평가에게 주기 전에 «이 파일이 지금 트리의 것인가» 를
한 번 대조하라. 안 하면 비평가 둘의 1순위 지적이 통째로 유령이 된다(회차 하나가 그대로 날아간다).

### 그런데 그 «유령» 이 **진짜 결손 하나를 집어냈다**

두 비평가가 ref 를 **다시 재서** 1회차 구현의 전제를 깼다. 1회차는 §1-2 의 두 색만 보고
«두 톤 마름모 **체커**»(면적 50:50)로 지었는데, ref 는 그게 아니다:

- **AS 전이 실측**(ref 초록 y549 행): 45·52 / 103·110 / 161·168 / 218·225 ⇒ **선 폭 7 · x축 주기 58**(ref px)
  = 어두운 쪽이 면적의 **12%**. «반반» 이 아니다.
- **AR 방향성 실측**(G 채널 1픽셀 차분): ref = (1,1) **3.97** / (1,−1) **3.88** — **양방향 모두** 변한다.
  우리(옛) = 0.26 / 0.72 = 한 방향뿐.

⇒ **가는 선이 교차하는 격자**가 정답이다. 45°·−45° 두 겹의 `repeating-linear-gradient`.
x축 주기 58 → 환산 119.7, 선 폭 7 → 환산 14.4 이고 **45° 선의 수직 간격은 거기에 sin45 를 곱한다**
(주기 **84.6** · 선 폭 **10.2**). 알파는 §12-2 의 .088 그대로.

| | 1회차(체커) | 2회차(교차선) | ref 환산 |
|---|---|---|---|
| x축 주기 | 60 | **119** | 119.7 |
| 선 폭(가로) | — (면적 50%) | **14~15** | 14.4 |
| 어두운 톤 | (52,177,128) | (52,177,128) | (52,179,128) |
| 어두운 면적 | 49% | **13%** | 12% |

### 2인 일치 지적 4건 — 전부 수리

| 부품 | 수리 전 | 수리 후 | ref 환산 | 처방 |
|---|---|---|---|---|
| 별폭죽 뱃지 노랑 두 줄 세로중심 Δ | **−19** | **−5.0** | −4.1 | `.bdg>i{top:38→53}` · `>b{88→103}` |
| ✦ 아이콘 | 24×24 @ (37,51) | **26×26 @ (38,49)** | 27×27 @ (38,48) | `font-size 24→27` · `top 16→13` |
| 제목 잉크 좌단 | 68.5 | **73** | 73.5 | `.pvt>i{left:35→40}` |
| ★ 잉크 판 좌단 인셋 | 20 | **18** | 17.5 | `.pvb>s{left:18→16}` |

⚑ **제목은 `.pvt` 통째로 밀면 안 된다** — AR 처방은 `.pvt{left:38→43}` 이었는데 그러면 **✦ 까지 같이 밀려**
맞던 자리(37 ↔ ref 38)가 깨진다. 움직여야 하는 것은 글자뿐이라 `>i` 만 밀었다.

⚑ **뱃지 글자 «폭 −7.6%» 는 안 고쳤다.** 두 비평가가 같은 값을 냈지만 **높이는 −0.9%(AS) / −3.3%(AR) 로 맞다.**
같은 문자열에서 폭만 좁은 것은 **서체 모양**이고 지시서 [3] 이 감점 사유에서 뺀 자리다.
AR 처방(`font-size ×1.08`)을 따랐으면 **맞던 높이가 +7% 로 어긋난다** — 폭을 좇다 높이를 잃는 380 함정.

### 넘긴 것

- **리본 띠 높이 72 ↔ ref 76.3(초록) / 67.1(파랑)** — 2인 다 짚었으나 **ref 자신이 두 카드에서 다르고**
  측정표 §11 이 «원인 불명» 으로 남긴 자리다(AR 도 «우선순위 낮음» 으로 분류). 피치 134 는 정확하다.
- **아트 자리표시자 충전율**(c1 ▶ 32%·c2 지폐 44%) — §10·§11 이 «아트 대기» 로 등재한 자리. 상자는 ref 와 일치.
- **AS [4] c1 배너 아이콘 중심 +23px** — AR 은 같은 배너를 «분할선 216 ↔ ref 217» 로 정상 판정했고,
  AS 가 준 «크림 칸 57..215 · 아이콘 잉크 133..185» 는 `.ban>em` 상자(57..146)와 **모순**이다.
  LESSONS 151-⑤ 대로 화소·DOM 을 대면해 어느 자가 틀렸는지 가른 뒤에 손대야 한다 — 3회차 몫.

### 게이트·회귀

- `verify151` **87/87 무수정** · **SMOKE PASS**

## 3회차 (2026-09-01, sess-1426-17297 — 비평 AT·AU 반영)

2회차 캡처(`151-r4`)로 새 비평 2인. **1·2회차 수리는 둘 다 «고쳐짐» 으로 확인**했다:

| 자리 | AT 실측 | AU 실측 | 판정 |
|---|---|---|---|
| 격자 주기 / 톤 / 교차 | 120 · (52,177,128) · 양방향 ✓ | 119.6 · (52,177,128) ✓ | 고쳐짐 |
| 뱃지 노랑 두 줄 세로중심 Δ | **−4.0** | **−4.0** | 고쳐짐(ref −4.1) |
| ✦ | 26×26 @ (38,49) | 27×27 @ (37,49) | 고쳐짐 |
| 제목 잉크 좌단 | 73~74 | 73 | 고쳐짐(ref 73.5) |
| ★ 인셋 | 18 | 18 | 고쳐짐(ref 17.5) |

점수 **AT ①8 ②6 ③8 ④8 / AU ①8 ②6 ③7 ④8** — ①~④ 최저 **6**, 통과선 9 미달.
둘 다 병목을 **② 상대 크기 비율**로 짚었고, 1순위 지적이 **같았다**: «노치가 깊이 22 · 세로 85 · 하변 118 로 선언(32/105/108)에 미달».

### ⚑ 그 1순위 지적은 **내 브리핑이 만든 것**이다 — 제품은 옳다

둘 다 «카드 재질이 끝나고 **바탕이 시작되는** 자리» 를 쟀다. 그런데 지시값 32/105/108 은
측정표 §1-3 의 «**바깥선**» = **검정 외곽선의 바깥 모서리** 기준이다. 검정 테가 10px 이므로 두 기준은
정확히 그만큼 어긋난다. 같은 자로 우리 픽셀을 다시 재면:

| 경계 | 우리 실측 | ref × k | Δ |
|---|---|---|---|
| 바깥선(검정 바깥 모서리) 깊이 | **32** (곧은 변 1029 → 최심 997) | 32.2 | 0.2 |
| 구멍(바탕 시작) 깊이 | **22** | 15.6−4.84=10.76 → **22.2** | 0.2 |
| 구멍 세로 길이 | **85** | 51−2×4.84=41.3 → **85.2** | 0.2 |
| 구멍 하단 ↔ 카드 하변 | **118** | 53.7+4.84=58.5 → **120.7** | 2.7 |

⇒ **세 축 모두 맞다.** 행 파형 실측(로컬 y527): x941 몸통 → x945 격자 → **x949·953 검정** → x957~ 바탕.
⚑ **교훈** — 비평가에게 목표 수치를 줄 때 **«어느 경계에서 잰 값인지»** 를 같이 적어라.
안 적으면 검정 테 두께만큼 어긋난 값을 재고, **2인이 독립으로 같은 오답**을 낸다(자가 같으니 당연하다).
LESSONS 151-⑤ 의 «마스크가 다르면 다른 것을 잰다» 의 **경계 판**이다.

### 고친 것 ① 격자 크기 — 1회차 «체커» 가 맞았고 2회차 «가는 선» 이 틀렸다

2회차는 AS 의 «선 폭 7 · 주기 58» 을 그대로 받아 **가는 교차선**으로 지었다. 3회차에 **ref 행 파형을 직접 찍어**
그 읽기가 뒤집힌 것임을 확인했다(ref y556, x25..145 의 G 채널):

```
195×7 → 173×36 → 195×22 → 174×37 → 195…
```

**어두운 쪽이 36 으로 넓다.** «7» 은 선 폭이 아니라 **밝은 틈**이었다. 문턱 대조로도 갈린다 —
ref 어두운 면적 **56.9%** ↔ 2회차 «가는 선» **19.8%**. (톤 히스토그램이 12% 로 나왔던 것은 ref 에 노이즈가
있어 «어두운 톤과 **정확히 같은 값**» 인 화소만 세면 낮게 나오기 때문이다.)

⇒ 형태는 **두 톤 마름모 체커**(1회차)가 맞았고, 1회차가 틀렸던 것은 **크기**뿐이었다: 타일 60 → **120**
(= ref x축 주기 58 × k = 119.7). 실측 행 런 **60/60 = 주기 120** · 어두운 면적 행 기준 **50%**(ref 약 60%).

### 고친 것 ② 리본 띠 높이 — **비평가 4인 전원**이 짚은 유일한 자리

72 한 값으로 고정돼 있었는데 ref 는 카드마다 다르다: 초록 37 → **76** · 파랑 32.5 → **67**.
상변과 피치는 이미 정확하므로(AU: 불릿형 상변 = 하변에서 272·138 · AT: 배너형 로컬 295·413)
**상변을 붙박고 높이만** 바꾸고 `bottom` 을 «상변거리 − 새 높이» 로 다시 적었다.

| | 높이 | 상변 ↔ 카드 하변(실측) | 기대 |
|---|---|---|---|
| 불릿형(초록·보라) rb1 / rb2 | **76 / 76** | **272 / 138** | 272 / 138 |
| 배너형(파랑) rb1 / rb2 | **67 / 67** | **239 / 121** | 239 / 121 |

세로 그라디언트는 절대 px(6/55/72)에서 **백분율**(8.33%/76.4%)로 바꿔 두 높이에 같은 비례가 가게 했고,
크림 캡(64/56)·제비꼬리(76/67)도 같이 비례시켰다.

### 게이트·회귀

- `verify151` **87/87 무수정**(3회차 내내) · **SMOKE PASS** · 9:13.3(1080×1600)에서 노치 32×105·하변 108 동일.

### 다음 세션이 볼 것 (남은 지적 — 4회차 몫)

1. **ref 파랑 카드의 노치는 2개다**(AT ③ — 내가 ref 픽셀로 **확인함**: 우변 y142~178 · y212~246, 깊이 15).
   우리 배너 카드는 1개다. ⚠ 측정표 §1-3 의 «파랑도 y≈190~240 부근에 하나» 는 **부정확**하다.
   ⚠ 초록 카드도 우변 스캔에 4자리가 잡혔는데 일러스트·리본이 섞여 있어 **전용 자가 필요하다** — 눈으로 세지 마라.
2. **노치에 안쪽 밝은 림(베벨)이 안 따라간다**(AT ②). 지금은 마스크가 림을 **잘라내기만** 해서 곡선을 따라 도는
   밝은 테가 없다. ref 는 림이 노치를 따라 휜다.
3. **리본 금색 젬 판** — AT «채움 91×81, 검정 외곽 없음»(ref 91×91 + 외곽) ↔ AU «c2·c3 는 91×91 로 정확».
   **두 비평가가 어긋난다** — LESSONS 151-⑤ 대로 DOM 과 화소를 대면해 어느 자가 틀렸는지 먼저 갈라라.
4. **한글 제목·배너 글자 폭 +10%**(AT ④, 잉크 **높이는 정확**) — 자간 문제로 보인다. 단 AU 는 ⑥ 7 로만 잡았다.
5. **c1 헤더 밴드 112 ↔ ref 파랑 107.5**(AT ⑨·AU ⑥ 일치, 초록은 112 로 정확) — 배너형만 분기 필요.
6. **격자 어두운 면적 50% ↔ ref 약 60%** — 체커는 구조상 50% 가 상한이라 더 맞추려면 마름모를 비대칭으로
   깎아야 한다(3톤이 생긴다). 비용 대비 효과를 재고 결정할 것.

## 4회차 (2026-09-02, sess-0729-24860 루틴 워커 B — 3회차가 넘긴 «전용 자» 를 만들었다)

### 만든 것 — `tools/scan667.py` (우변 실루엣 전용 자)

3회차는 «초록 우변 스캔에 네 자리가 잡히는데 일러스트·리본이 섞여 있어 **전용 자가 필요하다 —
눈으로 세지 마라**» 로 넘겼다. 그 자를 만들었다. 방법은 하나뿐이다 — 행마다 **카드 재질이 끝나는 x**
의 프로파일을 낸다. 일러스트는 카드 **안**이라 이 프로파일을 못 건드리고, 리본은 좌변·뱃지는 우변
**바깥**이라 «곧은 변에서 왼쪽으로 파고든 만큼» 이라는 정의가 셋을 다 갈라 준다.
3회차 교훈(경계 기준)을 자 안에 박아 깊이를 **두 기준으로 같이** 낸다 — `outer`(바탕이 시작되는 자리)와
`hole`(검정 런의 왼쪽 끝 = 실루엣 바깥선). 하나만 내면 2인이 독립으로 같은 오답을 낸다.

### 재현 — **노치는 한 자리가 아니었다** (ref 두 장 다 셋)

| | 초록(불릿형) | 파랑(배너형) |
|---|---|---|
| 노치 하단 ↔ 카드 하변 (우리 px 환산) | **113 · 342 · 567** | **101 · 241 · 382** |
| 피치(하단 간격) | 229 · 225 ⇒ **227** | 140 · 141 ⇒ **140** |

⚑ **피치는 카드 안에서 ±2 로 일정하고 카드 사이에서는 전혀 안 맞는다**(227 ↔ 140). 높이에 비례하지도
않는다(높이 비 1.39 ↔ 피치 비 1.62). ⇒ 리본 띠 높이(76/67)와 같은 **«ref 자신이 두 값인» 자리**.
§12-1 의 «하변 기준 상수» 는 폐기가 아니라 **맨 아래 한 자리로 좁혀진다** — 바뀐 것은 그 위로
무늬가 되풀이된다는 사실뿐이다. 측정표 정오표 **§13** 에 등재.

### 고친 것 (4회차)

| | 4회차 전 | 4회차 후 | 근거 |
|---|---|---|---|
| 노치 개수 | 카드당 1 | **피치가 정한다**(`pvNotches`) | ref 실측 위 표 |
| 노치 안쪽 밝은 림 | 곡선 자리에서 **끊김** | 곡선을 따라 12px | ref 화소 단면(§13-2) |
| 배너형 헤더 밴드 | 112(초록 값) | **106** | ref 파랑 51.5 ref px |

마스크가 여러 겹이 되므로 `mask-composite:intersect` 를 썼다 — 기본 `add` 는 «둘 중 하나라도 보이면
보임» 이라 **구멍이 서로를 메운다**(한 겹일 때도 무해).
⚠ 림을 «구멍 안쪽» 에 그렸다가 한 번 틀렸다 — 림이 구멍을 메워 **바탕 기준 깊이가 22 → 10** 으로 줄었고
`scan667 --cap` 이 그 자리에서 잡았다. 림은 검정 외곽선의 **몸통 쪽**이다.

### 게이트

`verify151` **84/84 무수정**(수리 전 트리도 84/84 — 3회차 기록의 «87/87» 은 그 뒤 자가 바뀐 값이다) ·
`ink151` 잉크 높이 Δ 전 항목 ±6.2% 안(수리 전과 같은 값) · **SMOKE PASS**.

### 비평 AV·AW (캡처 `151-r5`) — ①~④ 최저 **AV 6 / AW 5**

두 사람 다 **① 림 ② 배너형 헤더 밴드는 «고쳐짐»** 으로 확인했다(림 두께 12 ↔ ref 12.4, 끊김 없음 ·
밴드 c1 106 ↔ ref 105.2~106.0 · c2·c3 112 ↔ ref 110.8~111.4). 피치도 «고쳐짐»(140/227 ↔ ref 140.3/229.0).
막은 것은 **② 상대 크기 비율**이고, 2인 일치 지적이 넷이었다 — 5회차가 전부 수리했다.

## 5회차 (같은 세션 — 비평 AV·AW 2인 일치 4건 수리)

### ⚑ 본체 — **깊이 32 는 «다른 기준의 값을 그 자리에 적은 것»** 이었다

AV [2]·AW [1] 이 «우리 22 vs ref 32» 로 2인 일치했다. 3회차는 같은 자리를 «32 로 맞다» 고 닫았는데,
그때 대조한 두 값이 **서로 다른 기준**이었다. 자로 다시 재면(같은 정의를 양쪽에 적용):

| 기준 | ref(초록·파랑 동일) | 우리(4회차) | Δ |
|---|---|---|---|
| 실루엣(검정 바깥선) 깊이 | 19.6 ref = **40.4** | **32** | −8.4 |
| 바탕이 시작되는 깊이 | 14.6 ref = **30.1** | **22** | −8.1 |

⇒ 측정표 §1-3 의 «485.6 → 470 = 15.6» 은 **바탕 기준** 값이고, 1회차가 그것을 **실루엣** 반지름으로
적었다. 검정 두께(우리 10)만큼 노치가 통째로 얕았던 것이다. **반지름 32 → 40.**
⚑ **왜 3회차 검산이 이 오류를 통과시켰나** — «우리 실루엣 32 ↔ ref 15.6×k = 32.2» 로 맞춰 봤는데
좌변은 실루엣이고 우변은 바탕이다. 3회차가 세운 «어느 경계에서 잰 값인지 밝혀라» 는 규칙을
**비평가에게만 적용하고 자기 검산에는 안 적용한 것**이 이 회차의 교훈이다.

### 나머지 2인 일치 3건

| 자리 | 전 | 후 | ref | 근거 |
|---|---|---|---|---|
| 노치 길이 | 아래 105 · 위 70 | **피치 ÷ 2**(배너 70 · 불릿 114) | 초록 56/111 · 파랑 35/68 ref px | AW [2] · AV [4] |
| 배너형 맨 아래 노치 **중심** ↔ 하변 | 160(초록 값) | **130** | 파랑 130.0 | AV [1] · AW [4] |
| 가치 배지 분홍 잉크 bbox | 182 × 180 | **166 × 165** | 167.5 × 170.5 | AV [7] · AW [6] |

⚠ 배지는 **등방 0.92**(356 «확대·축소는 등방») — AV 의 «폭만 163» 안은 종횡을 깬다. 상자가 194 → 178 로
줄었으므로 **안의 두 줄도 8px(=(194−178)/2) 올렸다**. 안 올리면 4회차에 맞춰 둔 «잉크 중심 = 뱃지 중심 −4»
가 그만큼 다시 어긋난다. 글자 «크기» 는 여전히 안 건드렸다(높이가 맞는 자리 — 380 함정).

### 노치 개수 — 막고 있던 것은 **헤더 밴드 클리어런스**였다

4회차는 «노치 위끝이 헤더 밴드를 침범하지 않는 데까지» 로 걸었는데, **ref 파랑의 셋째 노치는 그 밴드
자리로 9px 올라와 있다.** 한계는 밴드가 아니라 **상단 라운드 코너**(44 + 여유 12)다.
⇒ 배너형이 **3자리**(AV [3]·AW [3] 이 «c1 은 카드 높이가 ref 파랑과 **정확히 같은데**(534 = 534)
셋째가 비어 있다» 로 2인 일치. 4회차의 «우리 카드가 짧아서» 는 c1 에 대해선 틀린 전제였다).
불릿형은 2자리 그대로 — 우리 카드가 690(불릿 3행)이고 ref 초록은 771(4행)이라 셋째가 실제로 안 들어가고,
AW 가 «c2·c3 의 셋째는 배지에 가려 무해» 로 확인했다.

### 젬 판 — 3회차가 «두 비평가가 어긋난다» 로 넘긴 자리, **둘 다 반만 맞았다**

화소로 갈랐다. ⓐ **금색 면도 형마다 두 값**이다 — ref 초록 43~44 ref px(= 89~91) · **파랑 38~39(= 79)**.
우리는 세 장 다 91 이라 **배너형만 +15% 과대**였다(⇒ c1 만 79 · 아이콘 52 · 수량 줄도 같이).
ⓑ 금색 면 **밖에 검정 외곽선**이 있다(ref 초록 판 가로 단면 y586: 빨강 → **검정 x180** → 밝은 금 182 →
주황 → 밝은 금 224 → **검정 226** → 몸통). 우리는 그 검정이 0 이라 금색이 리본 빨강·몸통과 바로 맞닿았다
⇒ 자리를 안 넓히려고 `box-shadow:0 0 0 4px #000` 으로 **밖에** 그렸다.
ⓒ 배너형 판은 **하변이 리본 하변과 한 줄**이다(ref 파랑은 위로만 12 돌출 — AV [6]·AW [7] 2인 일치)
⇒ `top:-12`(= 리본 67 − 판 79).

### 수리 후 실측 (`151-r6`)

| | c1(배너) | c2(불릿) | ref 환산 |
|---|---|---|---|
| 노치 개수 | **3** | 2 | 3 / 3(우리 c2 는 카드가 81 짧다) |
| 노치 하단(바탕 기준) ↔ 하변 | 106 · 245 · 384 | 114 · 341 | — |
| 피치 | **139** | **227** | 140 / 227 |
| 깊이 (바탕 / 실루엣) | **30 / 40** | **30 / 40** | 30.1 / 40.4 |
| 젬 판 금색 면 | **79** | 91 | 79 / 89~91 |
| 배지 분홍 bbox | **166 × 165** | 166 × 165 | 167.5 × 170.5 |

### 게이트 (5회차)

`verify151` **84/84 무수정** · `ink151` 9항 **수리 전과 같은 값**(글자 크기 0줄 변경) · **SMOKE PASS**.

### 다음 세션이 볼 것 (남은 지적)

1. **배너 «모든 광고 평생 스킵» 양끝 형상**(AW [8], 1인) — bbox 는 일치(462×113 ↔ ref 460×109.3)하는데
   ref 는 티켓형이라 **양끝에서 높이가 68% 로 잘록**하고 모서리에 뿔이 있다. 우리는 끝까지 100%.
2. **c1 아트 자리 밝은 판**(AV [8], 1인 · AV 스스로 «신뢰도 낮음» 으로 분류) — 세로 −43%, 하변 여백 217 vs 23.
3. **몸통 마름모 격자 어두운 면적** — 주기 120 ↔ ref 119.6 은 맞고 면적만 작다. 체커는 구조상 50% 가
   상한이라 더 맞추려면 3톤이 생긴다(3회차 판단 그대로 — 비용 대비 효과를 재고 결정).
4. **⑥ 서체(통과 무관)** — 제목 잉크가 **폭만이 아니라 높이도 +6~11%** 라는 AV 실측이 3회차의
   «높이는 정확» 기록과 어긋난다. ⑥ 만 막으므로 통과에는 영향 없으나 **다음 회차가 한 번 갈라 둘 것**.
