# 667 — 이용권 UI 디자인 재작업 (레퍼런스 원본 대조)

주인 지시(2026-09-02 00:50): «이용권 부분 디자인이 허접한데 원본꺼 참고도 좀 다시해줘야함 디자인».

- 레퍼런스 `docs/ref/151-이용권-카드.png` — **카드 2장 크롭(504×709)**. 표준 변환 «프레임 y = ref y − 84» **해당 없음**.
  비교 단위는 «카드 하나», 환산은 **ref 카드 폭 474.12 → 우리 978, k = 2.0628** 하나뿐이다(측정표 §9).
- 선행 작업 151 이 8회차에 ①~④ **8점**(J 9/8/8/8 · K 8/8/8/8)으로 통과했고, 667 은 그 위에서 **≥9** 를 목표로 한다.
- 하네스 `tools/cap151.js` · 자 `tools/verify151.js` · 잉크 `tools/ink151.py`.

## 착수 전 확인 — 199 연동 (등재문의 선행 조건)

등재문은 «199 결3 ⓑ 로 이용권이 «+4h» → «×배율» 상품으로 개편 중이니 **새 상품 전제**로 그리라,
199 lock 확인 후 착수» 라고 못박았다. **그 개편은 이미 제품에 들어와 있다** —
`PASS_OFF_MUL = 1.2` · `offMul()` · `PASS_ITEMS[].bl` 의 «오프라인 보상 획득량 ×1.2» 가 전부 파생 문구다
(`OFF_MAX_H`·`offMaxH()` 는 선언째 폐지됨). ⇒ «+4h» 로 되그릴 위험이 없어 착수했고,
이번 회차가 만진 것은 **카드 실루엣·몸통 패턴(CSS)** 뿐이라 199 가 잡고 있는 계수 자리와 겹치지 않는다.

## 1회차 (2026-09-01, sess-1426-17297 루틴 워커 B)

### 수리 전 실측

- `verify151` **87/87 PASS** · `ink151` 잉크 **높이** Δ 전 항목 ±6.2% 안(폭 Δ 는 문구가 달라 무의미 — 측정표 규칙).
  ⇒ **글자 크기·상자 기하는 이미 맞았다.** 남은 결손은 «카드가 ref 처럼 안 생겼다» 쪽이고,
  그 자리는 UI-REFERENCE 151 체크리스트에 **`[ ]` 두 줄로 이미 적혀 있었다.**

### 고친 것 ① 카드 우변 «물결 노치» (미구현 → 구현)

측정표 §1-3 은 이것을 «**카드 우변은 손그림 물결**» 이라는 **이 아트의 규칙**으로 적어 뒀다(초록·파랑 둘 다 있다).

| | ref 실측 | ×k 환산 | 우리 구현 |
|---|---|---|---|
| 깊이 | 15.6 | **32.2** | **32** (실측: 곧은 변 바깥 x 1029 → 노치 최심 997) |
| 세로 길이 | 51 | **105.2** | **105** (실측 편차 구간 100 — 양 끝이 서브픽셀로 얕다) |
| 하변에서 노치 하단까지 | 초록 110.7 · 파랑 106.3 | **228 / 219** | **108 + 105 = 213** |

⚑ **«카드 높이의 몇 %» 가 아니라 «하변 기준 상수» 다.** ref 두 장은 높이가 771/534 로 다른데
노치는 **둘 다 하변에서 106~111** 이다 — 비율로 걸었으면 한 장이 반드시 틀렸다(LESSONS 151-② 의 반대 짝:
«ref 값이 둘인데 서로 같으면 그건 함수가 아니라 상수다»). 우리 카드 셋(534/690/690)에 그대로 맞는다.

⚑ **깎지 않고 «구멍 + 덧댄 호» 로 냈다.** LESSONS 151-⑥ 이 «요소 밖으로 나가는 자식이 있는 상자에는
clip-path 를 걸지 마라»(리본 제비꼬리가 금색 판을 −11% 깎았다)고 못박아 뒀다. `.pvc` 에는 밖으로
나가는 자식이 넷(상태 탭·알약·뱃지·리본)이다. ⇒ 마스크는 **자식이 없는 두 칠면**
(`.bg` = 몸통+안쪽 림 · `.fr` = 검정 테)에만 걸고, 뚫린 자리의 검정 외곽선은 `.ntc` 가 **속이 빈 링**으로 덧댄다.

⚑ **속을 «배경색» 으로 칠하지 않은 것이 핵심이다.** 처음 안은 «#272731 로 칠한 도형» 이었는데
캡처를 재 보니 카드 뒤 바탕이 **단색이 아니었다** — 카드 오른쪽 실측이 (54,54,63) ~ (49,49,58) 로 흐른다
(`#shopw` 선언은 `#272731` 인데 그 위에 다른 층이 얹혀 있다). 색을 박았으면 **자리마다 어긋났을 것**이다.
구멍이라 진짜 뒤가 비친다 — 검산: 구멍 안 (51,51,61) ↔ 카드 밖 같은 행 (50,50,59).

- `.pvc{--ntc-fr}` = `.fr` 박스 기준 `radial-gradient(32px 52.5px at 100% calc(100% - 160.5px))`
- `.pvc{--ntc-bg}` = `.bg` 가 사방 10px 안쪽이라 같은 점이 `calc(100% + 10px), calc(100% - 150.5px)`
- 타원 중심이 **카드 우변 위**(x=978)라 왼쪽 절반만 카드 안 ⇒ 깊이 = 가로 반지름 32.

### 고친 것 ② 몸통 «마름모 격자» 대비 (체크리스트 «ref 의 1/4~1/2»)

옛 값 `repeating-linear-gradient(45deg, transparent 0 30px, rgba(0,0,0,.05) 30px 60px)` 은 결손이 **둘**이었다:
ⓐ 마름모가 아니라 **한 방향 사선**이고 ⓑ 대비가 ref 의 절반이다.

⇒ `conic-gradient(from 45deg, …)` 4분면을 60px 타일로 깐다. 타일이 두 대각선으로 삼각형 4개로 갈리고
이웃 타일의 삼각형끼리 붙어 **마름모 체커**가 된다(가로 이웃의 좌·우 삼각형이 한 마름모, 세로 이웃의
상·하 삼각형이 한 마름모). 알파는 ref 실측 비 **52/57 · 179/196 · 128/141 = 0.912** 의 여집합 **.088**.

| | ref | 우리(수리 후 실측) | Δ |
|---|---|---|---|
| 몸통 밝은 톤 | (57,196,141) | (57,194,140) | ≤ 2/255 |
| 패턴 어두운 톤 | (52,179,128) | (52,177,128) | ≤ 2/255 |
| 두 톤 면적비 | 체커 = 1:1 | 12727 : 12138 | 1.05 |

### 게이트·회귀

- `verify151` **87/87 무수정** (마스크·패턴은 `getBoundingClientRect` 를 안 건드린다)
- `ink151` 9항 **수리 전과 같은 값** (잉크 높이 Δ 전 항목 ±6.2%)
- **SMOKE PASS**
