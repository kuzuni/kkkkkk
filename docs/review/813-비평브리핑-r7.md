# 813 비평 브리핑 (8회차 / 캡처 r7) — 89 유물 소환 팝업 · 프레임 5종

> 1~7회차 브리핑과 **같은 규칙·같은 질문**이다(점수를 회차 간에 비교할 수 있어야 한다).
> 이번 회차에 바뀐 것은 셋 — ① 캡처가 r7 이고 ② **Q2 의 전제가 뒤집혔다**(배수 바가 «들보 아래 벽» 이
> 아니라 **격자 아래**에 있다 — 867 이 옮겼다) ③ **Q3 의 과녁이 1.00 으로 못박혔다**(7회차에 채점자
> 두 사람이 각자 레퍼런스를 재 «위 = 아래» 를 냈고, 내 자의 off-by-one 을 그 둘이 잡았다).

## 무엇을 보는가

레퍼런스 한 장과 **같은 화면의 프레임 5종 캡처**를 나란히 놓고 채점한다.
이 화면은 세로 길이가 다른 기기에서 **패널 높이만 달라지고 부품 크기는 고정**이다.
따라서 이번 채점의 핵심은 «늘어난 세로를 어디에 나눠 줬는가»(여백 배분)다.

## 파일

- 레퍼런스: `docs/ref/89-유물-팝업.png` — **486×687 크롭**. 화면 전체가 아니라 **팝업 패널만** 잘린 그림이다.
  패널 4변에 금색 장식 테두리가 있고 그 바깥은 없다. 환산 배율 **×2.2222**(= 1080/486) ⇒ 패널 환산 **1080×1527**.
- 캡처 5장(전부 폭 1080):
  - `docs/shots/754-r7-89-1600.png` (프레임 높이 1600 — 9:13.3 및 그보다 넓은 기기 전부)
  - `docs/shots/754-r7-89-1841.png`
  - `docs/shots/754-r7-89-1920.png` (9:16)
  - `docs/shots/754-r7-89-2280.png` — **기준 프레임(9:19)**
  - `docs/shots/754-r7-89-2600.png`
  - 5장을 한 장에 붙인 스트립: `docs/shots/754-r7-89-strip.png`

## 비교 규칙 (반드시 지킬 것)

1. **캡처는 화면 전체**이고 레퍼런스는 **패널만**이다. 캡처 안에서 금색 테두리로 둘러싸인 팝업 패널을 먼저 찾고,
   그 패널의 좌상단을 원점으로 삼아 레퍼런스와 비교하라. 패널 밖(뒤 배경·HUD)은 채점 대상이 아니다.
2. 가로는 1:1(둘 다 폭 1080 환산)이다. 세로는 **패널 높이가 프레임마다 다르다** —
   ⇒ **세로 절대 좌표로 «몇 px 어긋남» 을 재지 마라.** 세로는 «부품과 부품 사이 간격», «그 간격이 프레임마다 몇 배로
   변하는가», «부품 크기 대비 여백의 비» 로만 재라.
3. **색·아트 내용·서체 모양은 감점 사유가 아니다.** 아트는 나중에 교체된다.
   단 **크기·bbox·비율에 관한 것**(아이콘 상자 w×h, 글자 잉크 높이·폭, 선 두께)은 ②·⑥ 축이므로 지적하라.
4. 지적은 **수치로만**. «몇 px», «몇 %», «몇 배». 두루뭉술한 총평 금지. **후하게 주지 마라.**

### ⚠ 이 화면의 세로 순서 (867 이후 — 브리핑이 늙어서 7회차 채점자 둘이 각자 잡아 준 자리다)

패널 위 → 아래 순서는 **① 금테 상단 → ② 들보(상인방) → ③ 벽 → ④ 유물 격자(3·4·3) → ⑤ 배수 토글 바
(×1/×10/×100/×1000) → ⑥ 돌 수반(소환 버튼)+가격 알약 → ⑦ 안내문 2줄 → ⑧ 금테 하단** 이다.
**배수 바는 격자 «아래»** 다. 6회차까지의 브리핑은 바를 «들보와 격자 사이» 로 적고 있었는데
그건 867 이전 배치이고 지금 캡처와 다르다 — **그려진 픽셀을 믿어라.**

### ⚠ 잴 자리의 정의 (여기서 갈리면 없는 결함이 보인다 — 못박는다)

- **수반 하변** = 돌 수반(소환 버튼) 아래 **받침 밑판 외곽선의 최하단**이다(사발 바닥이 아니다).
  레퍼런스에서 그 밑판은 **아래로 넓어지는 사다리꼴**이라 위·아래 두 개의 가로줄이 있다.
  화소로 재면 **윗변 y617(가로 연속 113px) · 아랫변 y629(127px)** 이고(원본 486×687 좌표),
  정의(«외곽선의 **최하단**»)를 따르는 것은 **아랫변 y629** 다.
- **들보(상인방) 상변** = 들보 띠의 **최상단 규칙선**이다(그 위의 장식 그림자·글로우가 아니다).
- **격자 하변** = 유물 슬롯 묶음(3·4·3)의 마지막 줄 **슬롯 테두리 하단**이다.
- **안내문** = 패널 맨 아래 크림/탠골드 2줄 글자. **잉크 bbox**(찍히는 화소)로 재라 —
  **Range/DOM 상자는 이 약속의 자가 아니다**(레퍼런스는 그림이라 상자가 없다. 상자로 재면
  이 자리는 위 +3 · 아래 −3 만큼 어긋나고 **비만 상쇄돼** 없는 일치가 보인다 — 887 §1).
- **패널 «안쪽» 하변**(= 안내문 아래 여백의 끝점) = 하단 테두리 **조립체의 최상단**이다.
  테두리는 한 줄이 아니라 **어두운 안쪽 선(가로로 꽉 찬 검정) + 금테 띠**의 조립체이고,
  그 **검정 선도 그려진 테두리**다(배경이 아니다). 「금테의 첫 행」·「금테 안쪽」 으로 재지 마라 —
  띠 두께가 레퍼런스 2px ↔ 우리 캡처 5px 이라 **같은 말이 두 쪽에서 서로 다른 양을 훔친다**(887 §2).

## 채점 (각 10점 · 최저 항목 점수가 당신의 점수)

① 요소 배치 · ② 상대 크기 비율 · ③ 여백·간격 · ④ 정렬·그리드
(⑤ 색·명암 · ⑥ 폰트 크기 비율도 채점해 기록은 남기되 통과를 막지 않는다)

## 답에 반드시 포함할 것

1. **①~⑥ 점수와 최저 항목**
2. **감점 사유 목록 — 심한 순으로. 각 항목에 실측 수치와 «어느 프레임에서»**
3. 아래 자리에 대한 **당신의 독립 판정**(유도하지 않는다 — 결함이 아니면 «결함 아님» 이라고 답하라):
   - **Q1** 유물 격자 하변 ↔ 돌 수반 상변 사이 간격(그 사이에 배수 바가 들어 있다).
     프레임마다 재고, 그 변화가 결함인지 판정하라. 결함이라면 «늘어난 세로가 대신 어디로 가야 하는가» 를
     구체적으로(어느 부품 사이) 적고 **다음 회차가 쓸 비를 숫자로 처방하라**.
   - **Q2** (전제가 바뀌었다) **배수 토글 바의 자리** — 지금 바는 **격자와 수반 사이**에 있다.
     «격자 하변 ↔ 바 상변» 과 «바 하변 ↔ 수반(받침) 상변» 두 간격을 프레임마다 재고,
     그 배분이 옳은지 판정하라. 레퍼런스에는 이 바가 **없다**(우리 게임의 추가 부품이다) —
     그러니 «레퍼런스와 몇 px 다르다» 가 아니라 **«이 바가 아치 안쪽 공간을 어떻게 나누는가»** 로 판정하라.
   - **Q3** 안내문 2줄의 **아래 여백**(안내문 잉크 하변 ↔ 패널 하단 금색 테두리 **안쪽**)과
     **위 여백**(수반 하변 = 밑판 **아랫변** ↔ 안내문 잉크 상변)을 프레임마다 재라.
     ⚑⚑ **정정(887, 2026-09-03) — 확정값은 1.00 이 아니라 0.90 이다**(위 10 : 아래 9 ref px).
     1.00 은 아래 끝점을 «금테 띠 «안»» 으로 잡은 값이고, 그 규약은 띠 두께가 두 그림에서
     달라(ref 2px ↔ 우리 5px) **양쪽에서 서로 다른 양을 훔친다**. 위 «잴 자리의 정의» 대로 재라.
   - **Q4** 들보 **위**의 띠(패널 상단 금색 테두리 ↔ 들보 상변)를 프레임마다 재라.
     **«죽은 검은 여백» 인지, 벽의 결이 이어지는 면인지** 판정하고,
     레퍼런스의 같은 자리가 패널 높이의 몇 %인지 재서 같이 적어라.
   - **Q5** (이번 회차의 새 질문) **프레임 1600 의 세로 예산 배분.**
     1600 은 패널이 가장 짧아 여백이 전부 눌린다. 7회차 채점자 둘이 «남는 세로를 어느 비로 나눠야 하는가» 에
     **서로 다른 답**을 냈다(한 사람은 «벽 : 아치 안쪽 : 안내문 위 : 안내문 아래 = 44.2 : 49.5 : 3.1 : 3.1»,
     다른 사람은 «46 : 33 : 21»). **둘 중 어느 쪽인지 고르지 말고, 당신 스스로 레퍼런스에서 그 비를 재서
     숫자로 적어라**(레퍼런스 패널 1080×1527 환산 기준, 각 구간이 «패널 높이의 몇 %» 인지).
4. **당신이 1순위로 고치라고 하는 것 한 가지**와 그 처방(어느 값을 얼마로).

### ⚠ 이 회차에 «권한 밖» 인 자리 (지적은 하되 1순위로 고르지는 마라)

- **부품 «치수» 자체** — 수반 세로·밑판 아랫변 폭·가격 알약 세로는 **866**, 「유물 소환」 라벨 잉크 폭과
  3열 행 피치는 **860**, 배수 바를 어느 블록에 두는가는 **867** 에 각각 등재돼 **셋 다 이미 수리돼 들어왔다**.
  ⇒ 그 세 자리는 **«고쳐졌는지 확인» 대상**이다. 잔여가 보이면 수치로 적되, 813(여백 배분)의 축이 아니므로
  1순위로는 고르지 마라.
- **패널(그릇) 자체의 길이** — «패널이 레퍼런스 비례보다 길다» 는 **859** 의 축이다.

## 이 회차에 바뀐 것 (사후 확인용 — 채점에 쓰지 말고, 당신 실측이 이것과 어긋나면 그 사실을 적어라)

**813 이 이번 회차에 옮긴 제품 값은 0개다.** 7회차 값(안내문 위 22.0 : 아래 22.0 = 1.00) 그대로이고,
대신 그 사이 **866**(수반·알약 세로, 밑판 아랫변 폭, 배수 바 가로) · **867**(배수 바를 격자 아래로) ·
**860**(라벨 scaleX 제거, 3열 피치 247.5 대칭)이 main 에 들어왔다. 이 회차는 **그 셋이 들어온 화면을
처음으로 눈에게 물리는 회차**다.
