# 647 — `tools/verify301.js` [5] 토스트 쉼표 구분자 부패

**완료(2026-09-01, sess-1236-818 루틴 워커 C · 1회차) — 제품 `index.html` 0줄(게이트 부패).**

## 증상
`verify301` [5] «토스트가 같은 사실을 말한다(156)» 1건 실패(34/35).
- 토스트 실문자열: `🎫 일괄 받기 3칸 —  +3,797`
- 옛 기대 정규식: `new RegExp('\\+' + before.want.dia + '\\b')` = `\+3797\b`
- **자릿수 구분 쉼표**(`+3,797`)에서 갈렸다. `\b` 는 `+3797` 뒤의 경계를 요구하지만 실문자열은 `3` 뒤에 `,` 가 온다.

## 재현으로 갈래 확정(338 규칙)
같은 [5] 절의 다른 항(지급 = 받을 칸 합계 `Δ 0/3797/0` · 표본 비어 있지 않음 · 398 골드·유물조각 0 · 세이브 수령 3칸)은 **전부 초록** = 제품 경로는 살아 있고 토스트도 실제로 뜬다. 결손은 **자의 표기 가정**이다.

뿌리는 등재문 갈래 ⓐ·ⓑ 둘 다에 걸친다:
- 제품 토스트(index.html **34647**)는 `notify('🎫 <b>일괄 받기</b> ' + n + '칸 — ' + curIc('dia') + ' +' + won(sum))` 로 찍는다.
- `won = n => Math.round(n).toLocaleString('en-US')`(index.html **29936**) = **쉼표 구분**.
- 자는 `want.dia` 를 **숫자 그대로**(`\+3797`) 찾는다. 493 이 패스 길이를 40행 → 600행으로 늘리자 `want.dia` 가 4자리를 넘겨 쉼표가 처음으로 끼었고, 그제서야 드러났다.

## 처방 (334·643 규약)
정규식에서 `\b` 만 떼거나 쉼표를 «지나가게» 넣는 재기준은 반려 사항이다(표기 규칙이 또 바뀌면 재부패). 대신 **기대 문자열을 제품의 표기 함수에서 파생**시켰다:
- `before` 캡처(page.evaluate 안, `won` 이 전역이라 그대로 부른다)에 `diaTxt: won(want.dia)` 를 추가.
- 판정을 `\+ + escape(diaTxt) + (?!\d)` 로 바꿨다 — **쉼표는 받되, 뒤에 숫자가 더 붙으면(다른 액수) 빨개진다** = 옛 `\b` 의 경계 뜻을 유지한다.

이제 `won` 의 표기 규칙(쉼표·반올림 등)이 바뀌어도 자가 제품과 함께 움직이므로 이 자리는 안 부패한다.

## 무르게 풀지 않았음 (음성항 보존)
`tools/verify301.js` 를 고치지 않은 상태에서 정규식 로직만 떼어 확인:
| 입력 | 판정 | 뜻 |
|---|---|---|
| `… +3,797` | **초록** | 제품 실제 출력 |
| `… +3,7970` | **빨강** | 뒤에 숫자가 붙은 다른 액수 = 경계 유지 |
| `… +37,970` | **빨강** | 다른 액수 |
| `… +3797` (쉼표 없음) | **빨강** | 표기 규칙이 어긋나면 감지 |

## 결과
- `verify301` **34/35 → 35/35 PASS**
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 판정·밸런스 0줄
- **SMOKE PASS** · `verifyProgress` 0
- 643 의 작업과 무관함을 재확인(643 은 `verify169.js`·`probe643.js` 두 파일만 만졌고 `verify301` 은 그 둘을 안 읽는다).

## 교훈
- **표기 함수를 지나는 값을 자가 «리터럴 재현» 하면 표기가 바뀌는 순간 부패한다.** 자릿수·쉼표·단위처럼 «값이 커진 뒤에야 드러나는» 표기는 특히 위험하다 — 제품의 표기 함수를 그대로 부르는 것이 유일하게 안전하다.
- 493 처럼 «길이·수치를 키우는» 변경은 무관해 보이는 자를 **자릿수 경계**에서 뒤늦게 깨뜨린다. 부패가 등재된 시점(0521)이 아니라 원인(493)을 되짚어야 갈래가 잡힌다.
