# 643 — `verify169` C2 «이전 층 보상 실지급(골드)» 이 +36868 (기대 36864)

- **티어** T1 «버그(게이트 부패 또는 계수 표류 — 갈래 미확정)»
- **세션** sess-1158-10267 (루틴 워커 D) · **1회차** · 2026-09-01
- **제품 `index.html` 0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄. 고친 것은 `tools/verify169.js` 한 파일이다.
- **결과** `verify169` **59/60 → 62/62** · 단독 4회 연속 PASS · **3병렬 3/3 C2 PASS**
- 신설 `tools/probe643.js` **8/8**

---

## §1 등재문의 갈래 셋이 **전부 기각됐다**

등재문(PROGRESS 643 행)은 갈래를 셋으로 적어 두었다.

| 갈래 | 내용 | 판정 |
|---|---|---|
| ⓐ | 199 밸런스 루프가 던전 보상 계수를 밀었다 | **기각** — `rw(4).gold` 는 등재 시각의 36864 그대로다 |
| ⓑ | 자가 기대값을 손으로 박아 뒀다 | **기각** — 자는 `DUNGEONS[0].rw(S.dun.gold − 1).gold` 로 **제품 식에서 파생**한다 |
| ⓒ | 반올림 경계(`Math.floor` ↔ `Math.round`)가 한 항 옮겨졌다 | **기각** — 꼬리는 `1e-11`(36864.00000000001), 어긋남은 `+4` 다 |

셋 다 «지급액이 36868 이다» 를 전제하는데, **그 전제가 틀렸다.**

## §2 실재 — 자가 «저절로 변하는 값» 을 쟀다

`tools/probe643.js` 를 신설해 verify169 [C] 와 **완전히 같은 부팅·같은 목록 대기**로 굴렸다
(⚠ 목록 대기를 뺀 사본에서는 5회 전부 표류 0 이라 «재현이 안 된다» 는 엉뚱한 결론이 나온다 — 첫 판에 실제로 그랬다).

| 자 | 측정 | 값 |
|---|---|---|
| [1] 계수 | `DUNGEONS[0].rw(4).gold` | **36864.00000000001** (Δ 7.3e-12) |
| [2] 대조군 | 소탕을 **안 누르고** 같은 500ms · 5회 | `0 / +4.08 / 0 / 0 / 0` — **+4.08 인 판만 `S.totalKills` +1** |
| [3] 실험군 | C2 와 같은 순서(클릭 → 500ms → 읽기) · 5회 | `36864 / **36868.08** / 36864 / 36864 / 36864` |
| [4] 순간 측정 | 클릭과 **같은 tick** · 5회 | **5회 전부 36864.00000000001** |

⇒ **+4 는 지급액이 아니라 그 500ms 동안 배경 방치 사냥이 몹 하나를 죽여 얻은 골드다.**
`onclick → sweepDungeon → giveReward` 는 전부 동기라 같은 tick 에서는 배경분이 섞일 창 자체가 없다.
초과분이 난 판은 [2]·[3] 양쪽에서 **예외 없이 킬이 +1 인 판**이고, 킬이 0 인 판은 예외 없이 정확히 `rw(4).gold` 다.

**제품은 한 번도 틀리지 않았다.**

## §3 ⚑ 이 자리의 교훈은 이 파일 안에 이미 적혀 있었다

`verify169` **D5** 주석(258, 2026-08-27)이 같은 말을 이미 하고 있다:

> 이 단언은 원래 `S.gold` 도 같이 비교해서 **간헐 FAIL** 이었다. 팝업 뒤에서 방치 사냥이 계속 도므로
> 300ms 안에 몹이 하나만 죽어도 골드가 저 혼자 늘어난다. …
> **소탕이 실제로 도는지는 «저절로 변할 수 없는 값» 으로만 재야 한다.**

258 은 D5 한 자리만 고쳤고 **C2·E1 두 자리는 그 규약을 못 받았다.** 규약이 없어서 부패한 게 아니라,
**규약이 옆 칸에만 적용된 채 굳었다.**

## §4 처방 — 재기준이 아니라 **측정 방식**을 고쳤다

⚠ 기대값을 36868 로 고쳐 적는 재기준은 **반려**다(334 규약) — 계수가 또 바뀌면 그 자리는 또 부패한다.
**허용 오차(`< 1`)는 한 칸도 안 넓혔다.**

1. **C2** — 지급액을 클릭과 **같은 tick** 에서 잰다.
   ```js
   const paid = await p.evaluate(() => {
     const g0 = S.gold;
     document.getElementById('dgdSweep').click();
     return S.gold - g0;
   });
   ```
   `before.gold`·`after.gold` 는 이 항 말고 쓰는 데가 없어져 **선언째 걷어냈다**(죽은 코드 금지 — 295-②·399·460).
2. **E1** — 같은 결함이 음성항에도 있었다(`g0 === S.gold` 를 400ms 뒤에 본다). 빈 핸들러인데도 배경 사냥이
   골드를 올리면 **거짓 빨강**이 된다. `S.gold` 축을 빼고 D5 가 쓰는 «소탕 말고는 안 움직이는» 값 셋
   (입장 횟수 · 클리어 카운터 · `#dclw`)으로 갈아 끼웠다 — **축이 하나 줄고 둘 늘어 오히려 강해졌다.**
3. **E3 되돌림 시험 신설** — 무르게 푼 수리가 아님을 못박는다. 소탕 지급을 **한 층 위 보상**으로 갈아 끼운
   사본에서 C2 의 **새 식**이 같은 허용 오차로 그대로 빨개진다(`지급 58982 vs 기대 36864 · 차 22118`).
   ⚠ `giveReward(d.rw(f));` 는 파일에 **두 자리**다(26676 = [도전] 클리어 · 26772 = 소탕) —
   앞줄 `S.dunTk[d.id]--; S.cnt.dungeon++;` 까지 묶어 소탕 쪽만 집고, **E0c** 가 «한 곳으로 집었는가» 를 먼저 단언한다.

## §5 결과

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `verify169` | **59/60 FAIL** (C2) | **62/62 PASS** (E0c·E3 신설로 항이 둘 늘었다) |
| 단독 반복 | — | **4회 연속 62/62** |
| 3병렬 | — | **C2 3/3 PASS** |
| `probe643` | — | **8/8 PASS** |
| `index.html` | — | **0줄** |

## §6 곁다리 관측 2건 (등재만 · 선점 안 함)

⚠ **번호가 646·647 로 밀렸다** — 등재 push 가 상류와 경쟁해 `644`·`645` 를 다른 세션(sess-1156-27829)이 먼저 가져갔다.
마감 rebase 에서 양쪽을 모두 살리고 내 두 건만 옮겼다(340·361·418·572·607·620·636·639 선례).

- **646** — `verify169` 의 음성항 임시 사본이 **고정 경로**(`.v169-neg1.html`·`.v169-neg2.html`)라
  **같은 자를 2중으로 돌리면** 서로의 사본을 지워 `ENOENT` 로 즉사한다(3병렬 3회 중 **2회**).
  ⚠ **내 변경 전부터 그렇다** — `git show HEAD~1:tools/verify169.js` 에 두 고정 경로가 그대로 있다.
  내가 붙인 E3 도 같은 관례를 따랐으므로 자리는 셋이 됐다. **C2 판정 자체는 3병렬 3/3 PASS** 라 이 건과 무관하다.
- **647** — `tools/verify301.js` **[5] «토스트가 같은 사실을 말한다(156)»** 1건 실패(34/35).
  토스트 실측 `🎫 일괄 받기 3칸 —  +3,797` vs 자의 정규식 `\+3797\b` — **쉼표 구분자**가 갈랐다.
  ⚠ **내 작업과 무관하다**: 이번 변경은 `tools/verify169.js`·`tools/probe643.js` 두 파일뿐이고
  `index.html` 은 0줄이며, `verify301` 은 그 둘을 읽지 않는다.

## §7 교훈

1. **«실패한 값» 이 아니라 «그 값이 어디서 왔는가» 를 먼저 물어라.** 등재문의 갈래 셋은 전부
   «36868 이 지급액이다» 를 전제했고, 그 전제 하나를 대조군으로 깨자 셋이 동시에 무너졌다(338 규칙).
2. **재현은 «같은 순서» 로 해야 한다.** 목록 대기 한 줄을 뺀 사본에서는 5회 전부 표류 0 이었다 —
   배경 사냥의 위상이 달라지면 같은 결함이 안 보인다.
3. **규약은 적어 둔 자리에만 적용된 채 굳는다.** 258 이 D5 에 남긴 «저절로 변할 수 없는 값으로만 재라» 는
   주석이 바로 옆 칸 C2·E1 에는 안 갔다. 규약을 세운 세션은 **같은 파일의 형제 항을 전수로 쓸어야** 한다.
4. **자를 갈아 끼웠으면 되돌림 시험이 같이 와야 한다**(334). 새 식이 진짜 결함을 여전히 잡는지를
   음성항으로 못박지 않으면 «무르게 풀어 초록으로 만든 것» 과 구분이 안 된다.
