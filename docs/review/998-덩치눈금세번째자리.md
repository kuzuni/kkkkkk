# 998 — `verify981` [D2] 도 아직 «대각» 으로 덩치를 잰다 (같은 병 세 번째 자리)

> T1 «버그(잠복 게이트)» · 제품 `index.html` **0줄** · 고친 것은 **자 한 벌**(`tools/verify981.js`)과
> **신설 두 벌**(`tools/bulk998.js` 부품 · `tools/verify998.js` 게이트) 뿐이다.

## 1회차 (2026-09-06, sess-1542-21782 루틴 워커 C · 1회차 · 완료)

### 재현 — 비용 0 이었다(등재문 그대로)

`verify981` [D2] 는 **빨갛지 않았다.** 그래서 판정은 «빨간가» 가 아니라 «무엇을 재는가» 로 했다
(등재문 마지막 줄). 착수 시점 트리에서 **같은 17종·같은 판**을 두 눈금으로 접은 값:

| 접는 법 | 중앙값 | 밴드(±25%) | 밴드 밖 | 스프레드 |
|---|---|---|---|---|
| **대각**(옛 [D2] 눈금 = `own`) | 142.9 | 107.2~178.6 | **0종** ⇒ 초록 | 1.59배 |
| **최대 변**(995 가 792 [E1] 에 세운 눈금 = `bulk`) | 112 | 84~140 | **5종** (shuri·stone·arrow·meteor·lance) | 2.10배 |

⚑ **초록의 이유가 규격이 아니라 눈금이었다.** 뿌리는 산수 한 줄이다 —
`대각 = 최대변 × √(1 + (최소변/최대변)²)` 이라 가산분이 **종횡비에 통째로 달려 있다**:
실측 최대 `boom` **×1.414**(정사각 = √2) · 최소 `lance` **×1.011**(가느다란 조각).
대각은 정사각형 종만 골라 부풀려 **눈이 보는 스프레드를 그 자리에서 눌러 준다**
(989 «상자가 만든 초록» → 995 «눈금이 만든 초록» → **998 이 그 세 번째 자리**).
이 세 줄은 신설 `verify998` [A1]·[A2]·[A3] 가 매 실행 다시 잰다.

### 고른 처방 — 등재문 ⓐ (위임 규약 채택)

등재문의 셋 중 **ⓐ**(네 사본을 다 굽고 견준다)를 골랐다. 근거는 «무엇을 두 번 묻지 않는가» 다:

| 후보 | 무엇을 잃는가 | 판정 |
|---|---|---|
| ⓐ 되돌림 판과 견줌 | — | ✅ **채택** |
| ⓑ 981 이 손댄 종만 «중앙값 대비 자리» | «손댄 종» 목록이 **손 상수**다(회차마다 는다) · 문턱 «밴드 폭 절반» 이 곧 손 상수(825) | ✗ |
| ⓒ 판정을 792 에 넘기고 [D2] 는 기록 | **되감기 금지 축을 통째로 잃는다**(등재문 경고 그대로) | ✗ |

**눈금만 갈면 그 순간 밖 5종으로 빨개진다** — 그리고 그 다섯 중 넷은 981 이 한 획도 안 건드린
종이다. 그래서 **묻는 문장을 같이 갈았다**(982 [C1] 이 989·995 에서 겪은 그 자리 · 995 §«묻는 문장»):

* 옛 [D2] — 792 의 **절대 밴드**를 여기서 한 번 더 묻는다 ⇒ 792 가 배율표(`SHOT_SC`)로 [E1] 을
  닫을 때까지 981 이 **«남의 미완» 때문에** 빨갛다.
* 새 [D2] — **981 이 질 수 있는 것 하나만** 묻는다: «내 처방이 밴드 **안**에 있던 종을 밖으로 냈는가».
  `REVERTS` 표의 되돌림 판을 **다 굽고**, **그 판 전부에서 «안»** 이던 종이 제품에서 밖이면 빨강.
  ⚠ **한 판에서라도 밖이던 종은 안 센다** — 그 종은 내 처방이 아니라도 밖이라는 뜻이다.

**실측이 그 문장의 값어치를 보여 준다** — `meteor` 는 `cvx` 판(오목 a=0)에서만 안(140)이고
나머지 판에서는 밖(155)이다. 즉 981 의 오목이 상자를 **+15px** 늘린 것은 사실이지만,
그 종을 밴드 밖으로 **낸** 것은 981 이 아니라 제 손 불꼬리다(995 «meteor 는 배율이 아니라
꼬리 길이가 손잡이»). 옛 문장이었으면 981 이 그 5종을 통째로 뒤집어썼다.
⚑ 그 사실은 **판정에서 빼되 (기록) 줄에 남긴다** — [D2] 가 판마다 «덩치가 움직인 종» 을 찍는다.

### 고친 자리

| 파일 | 무엇 |
|---|---|
| `tools/verify981.js` | [D2] 눈금 `own` → **`bulk`** · 판정을 **되돌림 판과의 견줌**으로 · `DIAG_TOL` → **`BULK_TOL`**(값 0.25 그대로) · **`REVERTS` 표 신설**(되돌림 앵커 한 벌 — [R1]~[R5] 가 같은 표를 읽는다) · (기록) 3줄 신설 |
| `tools/bulk998.js` | **신설** — 밴드를 접는 법(`band`)과 견주는 법(`newOut`) 한 벌. 픽셀은 안 만진다(재는 법은 `verify792.measure` 하나뿐) |
| `tools/verify998.js` | **신설 12/12** — [A] 재현(두 눈금이 갈린다) · [B] 선언 · [R] 되돌림 |

### ⚑ 앵커를 «한 벌» 로 모은 것이 이 회차의 두 번째 몫이다

[D2] 가 되돌림 판을 **다 굽기** 때문에, 앵커를 [R*] 절과 [D2] 에 따로 적으면 그 순간
**«자기 자신을 견주는» 사본**이 하나 더 생긴다 — 997 이 바로 그 자리였다(`probe683e` 의 BASE 가
아무것도 안 덮어 BASE ≡ FLIP 이 됐다). ⇒ 굽는 법은 `REVERTS` 표 하나뿐이고 [D2]·[R1]~[R5] 가
그 표를 읽는다. `verify998` [B4] 가 «두 벌로 적힌 앵커 0개» 를 매 실행 센다.

⚑ **회차 한복판에 처방이 하나 더 들어왔다.** 이 회차를 쓰는 사이 981 5회차(`88acea4` — 도약탄
셋째 마디)가 올라왔고, 그 회차가 [R5] 와 함께 **다섯 번째 앵커**(`BE2R = 6`)를 얹었다.
rebase 로 받아 **표에 한 줄 더했다** — 안 그랬으면 [D2] 의 «전부에서 안» 이 5회차 처방을
셈에 안 넣은 채 초록이었을 것이다(표 주석에 그 규약을 적어 뒀다: **처방을 얹으면 표에 한 줄**).

### 되돌림 시험 — 무르게 푼 수리가 아니다

새 [D2] 는 절대 밴드를 안 물으므로 «아무것도 안 잡는 항» 이 될 수 있다. 그렇지 않다는 것을
두 겹으로 못박았다:

1. **부품을 합성 표로 굽는다**(`verify998` [R0]~[R3] · 브라우저 없이): 밴드 안에 있던 종을 밖으로
   내면 **잡고**(새로 밖 `[e]`), 한 판에서라도 밖이던 종은 **안 잡고**(0종), 되돌림 판이 0개면
   빈 물음이 된다는 것까지 찍는다(그래서 [D2] 가 판 수를 자기 판정에 넣는다 — [B5]).
2. **자를 되돌린 사본을 물린다**: `node tools/verify998.js <998 이전 verify981.js>` ⇒
   **[B1]~[B5] 다섯이 실제로 빨개진다(7/12 FAIL)**. 원복하면 12/12.

### 게이트

| 자 | 결과 |
|---|---|
| `verify998` | **12/12 PASS** (신설) |
| `verify981` | **26/26 PASS** ([D2] 이관 · 5회차 트리에서 · 되돌림 판 5/5 · 새로 밖 0종) |
| `verify982` | **14/14 PASS** (무수정 회귀) |
| `verify995` | **11/11 PASS** (무수정 회귀) |
| `verify792` | **31/32 — [E1] 만 빨강.** ⚠ **내 것이 아니다** — 995 가 눈금을 갈며 남긴 그대로이고(995 게이트 표에 같은 값), 792 가 배율표로 닫을 몫이다 |
| `node tools/smoke.js` | **SMOKE PASS** (제품 0줄) |

### 다음 세션에게

1. **792 가 [E1] 을 배율표로 닫아도 [D2] 는 그대로 초록이다** — 이제 두 자가 **다른 것을 묻는다**
   (792 = 절대 밴드 · 981 = 내 처방의 대가). 995 «다음 세션에게» 1 의 경고(대각으로 맞춘 배율을
   최대 변으로 역산하라)는 여전히 유효하고, `stone`·`meteor` 의 손잡이도 그 문서에 적힌 그대로다.
2. **981 이 처방을 하나 더 얹으면 `REVERTS` 에 한 줄 더한다** — 안 더하면 [D2] 가 그 회차를
   셈에 안 넣고 초록이다(5회차 `two` 가 그렇게 들어왔다).
3. **곁다리 등재 1001** — 같은 산수의 사본이 하나 더 있다: `verify982` [C1] 이 접는 법·견주는 법을
   **인라인으로 다시 적는다**(173~186행). 지금은 안 틀렸다(995 가 그 자리를 `bulk` 로 이관해 뒀다) —
   **잠복**이다. 792 가 눈금을 또 갈면 989 → 995 → 998 이 세 번 고친 그 얼굴로 네 번째가 된다.
   이 회차가 대신 고치지 않은 이유는 지시서 [1](«잡은 작업 단위 딱 1개»)이고, 처방은 한 줄이다 —
   `bulk998` 을 `require` 해서 인라인 `bulkBand` 를 지운다(제품 0줄 · 이 회차가 981 에서 한 것과 같다).
   ⚠ `verify995` 의 인라인 접기는 **사본이 아니다** — 그 자의 일 자체가 «네 접는 법을 나란히 놓고
   견주는 것» 이라 부품 하나에 묶으면 그 물음을 잃는다.
   찾는 법: `grep -n "V792.measure\|require('./verify792')" tools/`
