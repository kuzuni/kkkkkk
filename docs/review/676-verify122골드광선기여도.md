# 676 — `tools/verify122.js` §21 «골드 광선» 기여도 항 (T1 «버그»)

세션 `sess-1440-25075` (루틴 워커 C) · 1회차 · 2026-09-01

---

## 0. 등재문과 결론

등재문(2026-09-01, sess-1343-16462 — 655 회귀 스윕 곁다리)은 결함을 **둘**로 적었다.

| # | 등재문 | 재현 결과 |
|---|---|---|
| ① | `verify122` §21 «골드 광선이 판에 실제로 보인다» 1건 빨강(기여 **1.59** < 4) | **확인 — 단, 뿌리가 다르다.** 제품이 아니라 **자가 재는 창이 낡았다**(게이트 부패) |
| ② | 그 자가 FAIL 을 내고도 **종료 코드 0** 이라 E2 축이 영원히 초록으로 센다 | **기각.** 종료 코드는 내내 **1** 이었다 — 653 이 등재한 «파이프 착시» 다 |

**제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 고친 것은 `tools/verify122.js` §21 한 절이다.

---

## 1. 재현 — `tools/probe676.js` 신설

338 규칙(처방 전에 재현)대로 갔다. 그런데 **같은 실행의 이웃 두 항이 초록**이라
등재문의 «안 보인다» 는 그 자리에서 이미 모순이었다:

```
✗ 기준선 대비 섹터 최대 편차 1.59   (>=4)
✓ 각도 섹터 산포 46.31              (>=1.5)
✓ 반주기(1250ms) 섹터 최대 변화 16.83 (>=1.5)
```

광선이 «안 보인다» 면 산포도 회전도 같이 죽어야 한다. 그래서 자를 세 축으로 밀었다.

### [A]·[B] — 광선은 멀쩡하고, 끄기도 먹는다

* `#shopList .cn-cd.dia.top>.pn>.ray` 로 잡히는 노드 **1개**(중복 사본 없음).
* `display=block · opacity=1 · animation=jz122Ray · --jz-glow=1 --jz-per=1`.
* `display:none!important` 를 넣으면 계산된 `display` 가 **none**, `jz122Ray` 애니가 **1 → 0**.
  ⇒ **기준선(off)을 만드는 방법 자체는 옳다.**

### [C] — 광선의 잉크가 어느 반지름에 있는가 (본체)

판 클립(256×157)을 세 장 찍어(ON t=0 · ON t=1250 · OFF t=0) **반지름 12px 대역**으로 갈랐다.

| r 대역 | 화소 | ON↔OFF 섹터최대 | ON↔ON(1250) 섹터최대 |
|---|---|---|---|
| 0..12 | 452 | **0.00** | 1.51 |
| 12..24 | 1356 | **0.00** | 24.25 |
| 24..36 | 2256 | **0.00** | 39.38 |
| 36..48 | 3172 | **0.00** | 55.43 |
| 48..60 | 4080 | **0.00** | 12.07 |
| 60..72 | 4968 | **0.00** | 24.40 |
| **72..84** | 5435 | **31.30** | 48.23 |
| **84..96** | 4596 | **67.73** | 31.87 |
| 96..108 | 4298 | 0.13 | 14.20 |
| 108..120 | 4156 | 0.22 | 40.63 |
| 120..132 | 3732 | 0.39 | 29.16 |

**읽는 법이 전부다.**

* 광선의 잉크는 **r 72..96** 에 있다(기여 31.30 · **67.73**). 광선은 뚜렷하게 보인다.
* **r 0..72 는 기여가 예외 없이 0.00** — 아이콘 상자(`.pn>em` **120×157**)가 판의 세로를
  통째로 쓰므로 `|dx| ≤ 60` 인 자리는 구조적으로 광선이 보일 수 없다.
  반지름 60 이하는 정의상 `|dx| ≤ 60` 이라 **한 화소도 남지 않는다.**
* 그런데 §21 이 쓰는 창은 **r 34..76 고리**다. 그 고리는 34..72 를 헛되이 재고
  실제 잉크 대역의 **4px(72..76)만** 스친다 ⇒ 관측값 **1.59**.

⇒ **①의 뿌리는 «켜져 있는데 안 보인다» 가 아니라 «자가 아이콘 자리를 재고 있다» 다.**
15회차가 «아이콘이 덮는 안쪽은 빼고 판이 보이는 고리» 라며 적어 둔 34..76 은 **그때의 아이콘 크기**에 굳은 숫자다.

### [D] — 초록이던 두 항은 «헛초록» 이었다

같은 두 축을 **광선을 끈 판**에서 그대로 다시 쟀다.

| 항 | 광선 ON | 광선 OFF | §21 문턱 |
|---|---|---|---|
| 고리(34..76) 섹터 산포 | 39.39 | **39.88** | ≥1.5 |
| 고리(34..76) 반주기 최대 변화 | 13.40 | **13.40** | ≥1.5 |

* 산포는 광선을 지우면 **오히려 오른다**.
* 회전은 **소수점까지 같다**.

⇒ 두 항은 **광선이 통째로 사라져도 초록**이고, 재고 있던 것은 판에 같이 사는
**아이콘의 둥실(`jz122Float`)** 이었다(ON↔ON(1250) 에서 판 화소의 **43.3%** 가 바뀐다 —
그게 광선이 아니라 아이콘이다).
즉 §21 은 **한 항이 헛빨강 · 두 항이 헛초록**인 절이었다.

---

## 2. 처방 — 문턱이 아니라 «창과 값»

672 규약(«처방은 문턱이 아니라 창»)을 그대로 따랐다. **문턱 셋(4 · 1.5 · 1.5)은 한 칸도 안 건드렸다.**

* **창 = 판 전체.** 반지름 상수를 새로 적지 않았다. 낡은 것은 «34..76» 이라는 **숫자**였고,
  그 자리에 다른 숫자를 적으면 아이콘이 또 자라는 날 같은 부패가 되풀이된다.
* **값 = 광선을 껐다 켠 차분(`on − off`).** 아이콘·판 배경처럼 광선과 무관한 것은 차분에서 상쇄된다
  ⇒ 세 항이 전부 광선만 잰다(헛초록이 구조적으로 불가능해진다).
* **위상은 둘 다 본다**(t=0 · t=1250). 원뿔이 45° 주기라 한 위상만 보면 «그 위상에서 보이는 섹터가
  없다» 는 이유로 헛빨강이 날 수 있다. 기여는 두 위상의 최대, 회전은 두 위상의 차.
  ⚠ 회전을 볼 때 Δt=1250ms(반주기)를 쓰는 15회차의 앨리어싱 주의는 그대로다(패턴은 20s/8 = 2.5s 주기).
* 진단용으로 **옛 고리(34..76) 값을 같이 찍는다** — 낡았다는 사실이 로그에 남게.

### 왜 «아이콘 상자 밖» 이 아니라 «판 전체» 인가 (기각된 1순위 후보)

처음 고른 창은 «제품에서 파생시킨 아이콘 rect 의 **바깥**» 이었다. **실측이 그것을 기각했다** —
기여 **0.13** · 산포 0.04 로 오히려 더 안 보였다.

이유: 아이콘 `em` 은 **120×157** 이지만 그 안의 그림(`.cic`)은 **120×120** 이라
상자 위·아래에 잉크가 없는 띠가 있고, **광선이 실제로 보이는 자리가 바로 그 띠**다
(r 72..96 · `|dx| ≤ 60` 이면 `|dy| ≥ 58.8` — 정확히 아이콘 그림 위·아래).
«아이콘 상자 밖» 창은 **신호가 있는 자리를 통째로 버린다.**

⇒ 자를 새로 짜기 전에 제품에게 물은 것이 처방을 한 번 갈아 끼웠다.

### 실측 (3회 연속 같은 값 — 소수점까지)

| 축 | 현행 트리 | 되돌림(칠만 제거) | 문턱 |
|---|---|---|---|
| 기여 (두 위상 최대) | **8.66** (t=0 8.66 / t=1250 5.38) | 0.06 | ≥4 |
| 섹터 산포 | **2.39** | 0.02 | ≥1.5 |
| 반주기 변화 | **5.38** | 0.05 | ≥1.5 |

### §R 되돌림 시험 — 무르게 푼 수리가 아님

노드·마스크·회전은 그대로 두고 **칠(`background`)만** 걷으면 세 항이 전부 문턱 아래로 내려간다.
15회차의 헛초록이 재발하면 이 절이 즉시 빨개진다. 게이트에 **§R 3항으로 상주**시켰다.

---

## 3. 결함 ② — 종료 코드 (기각)

`tools/verify122.js` 마지막 줄은 내내 `process.exit(fail ? 1 : 0)` 이었고,
얕은 클론 범위에서 이 파일에 **655 이후 커밋이 없다** — 등재 세션이 돌린 것도 같은 파일이다.

직접 실측:

```
node tools/verify122.js            → EXIT=1        (165/167 FAIL)
node tools/verify122.js | tail -1  → $? = 0  ·  PIPESTATUS[0] = 1
```

⇒ **자는 옳았고, `$?` 를 파이프 뒤에서 읽은 것**이다.
**653 이 `neg202.js` 에서 등재하고 기각한 바로 그 «파이프 착시» 의 재발**이다.
재현되지 않는 처방은 적용하지 않았다(338 규칙) — `process.exit` 줄은 **한 글자도 안 건드렸다**.
⚠ 파이프 절단(633 이 push 게이트에서 걷어낸 병)도 같이 확인했다 — 4001줄을 파이프로 흘려도
**한 줄도 안 잘렸다**(마지막 줄 도달 · `wc -l` 4001). 이 자에는 그 병이 없다.

### 653 이 요구한 «`tools/*.js` 전수 스윕» — 첫 표본 결과

등재문은 676 을 그 스윕의 첫 표본으로 지목했다. 스윕을 돌렸다:

* 게이트 계열(`verify*` · `neg*` · `fnchk*` · `scan*`) **417개 중 종료 코드 미설정 9개.**
* 그 9개는 전부 **판정이 없는 덤프**다(`verify48`·`verify49`·`fnchk180`·`fnchk198`·`scan116`·
  `scan120`·`scan126`·`scan470`·`scan72c` — 측정치만 찍고 PASS/FAIL 을 안 낸다).
  **셀 판정이 없으니 세울 코드도 없다** = 653 의 병이 아니다.
* `FAIL` 을 찍으면서 코드를 안 세우는 파일 **10개는 전부 `probe*`/`audit*`/`dlg*`**(진단기)다.
  워커·`verifyProgress` 의 E2 축은 게이트를 읽으므로 초록 오집계 경로가 아니다.

⇒ **653 의 «빨간 채로 영원히 초록» 표본은 현재 트리에 0건이다.**

---

## 4. 곁다리 관측 — 등재 1건

`verify122` §8(스크롤 fps ON/OFF 교차)이 **재화 탭에서 빨갛다**(ON 중앙 17.4 / OFF 중앙 20 → 비 0.870 < 0.9).
등재문의 «166/167» 이 이번엔 **165/167** 인 이유가 이 항이다.

* 쌍별 비율이 **0.87 / 0.921 / 0.774 / 1.175 / 0.872** 로 **1.0 을 사이에 두고 벌어져 있다** —
  «드리프트» 가 아니라 표본이 서로 독립인 잡음이다(자 자신이 그 진단을 찍는다).
* 같은 실행의 **소환 탭은 초록**(비 1.033)이고, 절대 fps 가 **12~21** 로 러너 상한에 눌려 있다.
* **632 가 `verify621` 에서 겪은 바로 그 자리다** — 루틴 워커 컨테이너의 rAF 실측이 20~24fps 라
  틱·fps 를 분모로 삼는 축이 부하 스파이크 한 번에 뒤집힌다.

**⚑ 플레이키임을 같은 트리에서 못박았다**(344·372 규칙) — §21 을 고친 뒤 같은 트리에서 다시 돌린
2회차 실행에서 이 항은 **초록**으로 돌아왔다(빨간 항 0건 · `VERIFY122 170/170 PASS`).
같은 코드에서 빨강 ↔ 초록이 갈리는 항이므로 «§21 수리로 고쳐진 것» 이 아니라 **부하에 흔들리는 항**이다.

⇒ **690 «`verify122` §8 스크롤 fps 항 플레이키»** 로 등재한다
(⚠ **번호 개정: 677 → 681 → 689 → 690** — 등재 push 경쟁에서 다른 워커가 그 번호들을 먼저 올렸다.
286·308·340·412·652 규약대로 **먼저 등재된 쪽을 살리고** 내 행을 옮겼다)(내 작업 단위가 아니라 안 고쳤다 —
작업 단위 1개 원칙). 처방 후보는 632 의 처방(틱·fps 를 분모로 삼는 축을 폐기하고
부하에 안 흔들리는 축으로 갈아 끼우기)이 그대로 쓰인다.

---

## 5. 게이트

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `node tools/verify122.js` | **165/167 FAIL** (§21 기여 1.59 · §8 fps 0.870) | **170/170 PASS · 종료 코드 0** |
| `node tools/probe676.js` | — | **14/14 신설**(재현 자 · 3회 연속 같은 값) |
| `node tools/smoke.js` | — | **SMOKE PASS** |
| `node tools/verifyProgress.js` | — | **PROGRESS OK**(§1~§4 전부 초록 · 종료 코드 0) |

항 수가 167 → **170** 으로 는 것은 §21 이 3항(기여·산포·회전)에서
**3항 + §R 되돌림 3항 = 6항**이 됐기 때문이다. **허용 오차·문턱은 한 칸도 안 넓혔다.**

회귀는 pull 뒤에 돌렸다(654 교훈 — rebase 전의 낡은 자를 돌리면 남의 구간이 빨갛게 보인다):
rebase 후 `verify122` **170/170 PASS** · `smoke.js` **SMOKE PASS** · `verifyProgress.js` **OK** 재확인.

⚠ **마감 push 에서 `claim.js` 가 [4] §4 를 정확히 잡았다** — autostash pop 이 `LESSONS.md`·`PROGRESS.md` 에서
충돌해 트리에 병합 표시가 남았고(그런데 `pull` 자신은 코드 0 이다 — 작업 571 이 등재한 그 병),
`claim.js` 가 코드 1 로 막아 세웠다. 지시서 [4] 대로 **`git add -A` 를 쓰지 않고** 표시 세 줄만 지워
**양쪽을 다 살렸다** — 남의 교훈 블록 307줄 + 내 28줄, 남의 신규 행 677~688 13건 + 내 행(690 로 이동).
내용 삭제 **0줄**이다.

---

## 6. 교훈

1. **한 절 안에서 «빨간 항 하나 · 초록 항 둘» 이 서로 모순되면 의심할 것은 제품이 아니라 자다.**
   광선이 «안 보인다» 는데 같은 판의 산포가 46 이면 둘 중 하나는 거짓말이다. 여기서는 **셋 다** 거짓말이었다.
2. **«아이콘이 덮는 안쪽은 빼고» 를 반지름 상수로 적으면 아이콘이 자라는 날 부패한다.**
   창은 숫자가 아니라 **차분**으로 잡아라 — 광선을 껐다 켠 차이는 아이콘이 어떻게 자라도 광선만 남긴다.
3. **자를 새로 짜기 전에 제품에게 물어라.** 1순위 후보(«아이콘 상자 밖»)는 그럴듯했고 **실측이 기각했다** —
   상자와 그림이 달라(120×157 vs 120×120) 신호가 있는 띠를 통째로 버리는 창이었다.
4. **`$?` 를 파이프 뒤에서 읽지 마라.** 653 이 이미 등재·기각한 착시가 곧바로 재발했다.
   종료 코드를 물을 때는 `PIPESTATUS[0]` 이거나 파이프 없이 파일로 받아라.
