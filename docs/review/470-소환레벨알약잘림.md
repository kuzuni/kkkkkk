# 470 — 10 상점 «소환» 탭 레벨 알약 «Lv.n» 글씨 잘림 (주인 보고 2026-08-30)

> 주인 원문: «소환팝업쪽에 레벨들 글씨가 잘려보임»
> 등재문이 지목한 자리: 소환 배너 카드 `.shp-card .clv`(index.html 5096) + 경험치 바 `.cbar>b`

## 1회차 (2026-08-30, sess-1416-28464 워커 A) — **등재문의 자리가 재현으로 기각됐다. 제품 0줄.**

338 규칙대로 처방을 따르기 전에 재현부터 했다. **등재문의 두 가설이 둘 다 기각**됐고,
그 자리에서 «잘림» 은 **한 표본도 안 나온다**.

### 1. 왜 이 자리가 맞다고 봤는지 — 등재문의 근거는 옳았다

주인 스크린샷의 문자열 «Lv.6 · 5/1500» 은 **이 카드가 맞다**. `sumNeedExp(6)` 이
`SUM_EXP_TABLE[5]` = **1500** 이고(index.html 17585), 카드는 `Lv.{s.lv}` 와 `{s.exp}/{need}`
두 조각을 나란히 그린다(27540·27542). 다른 어떤 화면도 이 두 수를 같이 내지 않는다.
⇒ **화면 지목은 옳다.** 기각된 것은 «그 화면에서 글자가 잘린다» 는 판정이다.

### 2. 자(尺) — `tools/probe470.js` 신설

레이아웃 상자가 아니라 **찍힌 픽셀**로 잰다(340·350 처방). 세 함정을 차례로 밟고 닫았다:

| # | 함정 | 자국 | 처방 |
|---|---|---|---|
| ⓐ | **색으로 잉크를 못 가른다** | 1번 카드(무기) 본문색 `#5BCAF5` 가 글자색 `#7DE5ED` 에서 ΔRGB **44** 라 본문 배경이 통째로 잉크로 읽혔다(잉크 141×96 = 클립 전체) | 색이 아니라 **글자를 껐다 켠 두 장의 차분**으로 마스크를 만든다 |
| ⓑ | **스크롤이 두 캡처 사이에도 움직인다** | 이미 화면 안이던 가운데 카드만 깨끗했다 | `scroll-behavior:auto` + rect 가 **두 프레임 연속 같아질 때까지** 기다린 뒤 캡처 |
| ⓒ | **같은 화면을 두 번 찍어도 픽셀이 다르다** | 카드의 `filter:drop-shadow` 레이어가 다시 래스터되며 본문 그라디언트가 흔들린다 — **널 대조 2,052픽셀 · 최대 Δ51** | 널 대조를 표본마다 같이 찍어 **잡음 바닥을 재고** 잉크 문턱을 그 위(**90**)에 세운다. 바닥이 문턱을 넘으면 그 표본은 «판정 불가» 로 내보낸다 |

⚠ ⓒ 를 안 닫았으면 이 작업은 **정반대 결론**을 냈다 — 문턱 24 로는 30표본 중 24개가
«★ 물림(최소 −26px)» 으로 찍혔고, 그게 다 잡음이었다.

### 3. 실측 — 30표본 전부 초록

카드 5장 × 레벨 1·2·6·10·24·**25**(= `SUM_MAXLV`, 이 화면에서 가능한 가장 긴 문자열).
«알약 실폭» 은 사각 rect 가 아니라 **잉크 높이 구간에서 검정이 실제로 있는 x 범위**다
(알약이 캡슐 r22 라 사각으로 재면 캡 곡선을 놓친다).

| 레벨 | 문자열 | 잉크 w×h | 알약 실폭 | 좌여백 | 우여백 |
|---|---|---|---|---|---|
| 1 | `Lv.1` | 40×17 | 89 | 24 | 25 |
| 2 | `Lv.2` | 46×17 | 89 | 22 | 21 |
| 6 | `Lv.6` | 47×17 | 89 | **22** | **20** |
| 10 | `Lv.10` | 57×17 | 89 | 17 | 15 |
| 24 | `Lv.24` | 63×17 | 89 | **13** | **13** |
| 25 | `Lv.25` | 62×17 | 89 | 14 | 13 |

카드 5장이 **전부 같은 값**이다(문자열이 같으면 픽셀도 같다). 최소 여백 **13px** ·
«여백 < 8» 표본 **0/30**.

- **가설 ⓐ(글자 폭 > 알약 안쪽 폭) 기각** — 가장 긴 문자열에서도 양쪽에 13px 이 남는다.
- **가설 ⓑ(`overflow:hidden` 클리핑) 기각** — `.clv` 도 `.shp-card` 도 `overflow:visible` 이다.
  사슬에서 처음 자르는 조상은 스크롤 그릇 `#shopList` 이고, 그것이 자르는 것은
  «화면 밖으로 스크롤된 카드» 뿐이다(스크롤하면 보인다 = 잘림이 아니다).

### 4. 글리프 지도 — «반쯤 안 보인다» 가 아니다

주인 보고의 «L 좌변과 6 우변이 알약 테두리에 물려 반쯤 안 보인다» 를 픽셀로 직접 확인했다.
`Lv.6`(1번 카드) 알약을 통째로 떠서 찍은 지도(`#` = 시안 잉크 · `.` = 검정 알약):

```
18|........................### .................................. ####.............
23|........................### ......... # ..... ##......####... #  ...............
25|........................### ........####....####......####...#######............
31|........................############ .... #### ..........###...#######..........
33|......................... ########### .... ## .....####.... ...###  ............
```

**「L」의 세로 획(행 18~33) · 가로 발(행 31~33) · 「v」 · 「.」 · 「6」 이 전부 온전하다.**
잘린 획도, 알약 밖으로 나간 잉크도 없다.

### 5. 레퍼런스 대조 — 우리가 ref 와 Δ0 이다

측정표 `docs/measure/10-상점팝업소환탭.md` §2 #6: 알약 **89×44 · r22(캡슐) · 테두리 없음**.
우리 `.clv` 도 **89×44 · r22 · 테두리 없음** ⇒ **Δ0**.
같은 표가 적은 ref 글자 잉크(`Lv.2`)는 **46** 이고 우리 실측도 **46** ⇒ **Δ0**.
⚠ 등재문이 말한 «알약 **테두리**» 는 이 부품에 **없다**(`background:#000` 뿐이다) —
검정 알약과 검정이 아닌 이웃(경험치 바 검정 테두리)이 한 덩어리로 읽힌 것으로 보인다.

### 6. 그러면 «레벨들» 은 어디인가 — `tools/scan470.js` 로 전수를 떴다

한 자리가 초록이면 자리를 다시 찾는 것이 338·341 의 처방이다. 화면 **26개**를 순회하며
«Lv» 를 직접 품은 잎 노드 전부를 두 축으로 쟀다(잉크 = Range 글리프 상자 + 스트로크 바깥 몫):

- **[C] 하드 클립** — 잉크가 «자르는 조상» 의 클라이언트 박스를 넘는가(= 진짜로 안 그려진다)
- **[B] 테두리 물림** — 잉크가 호스트 패딩 박스를 넘어 테두리 위로 올라타는가
- **[S] 획이 채움을 파먹는가** — `-webkit-text-stroke` 인데 `paint-order:stroke fill` 이 없는가(등재문 처방 ②)

⚠ **상태를 안 만들면 이 자는 공허하다.** 1·2회차 주입은 «세이브에 이미 있는 값» 만 올려서
21 도감이 `Lv. 0/1`, 89 유물이 `Lv.0`, 07·26·50 슬롯은 **전 칸 잠금**인 채로 스캔됐다 —
긴 문자열 축이 통째로 안 돈 것이다. 3회차에 **보유·장착부터 만들고**(`SKILLS`·`EQUIPS`·`PETS`·
`RELICS` 전부 `own`, `S.eqSkill` 8칸) 레벨을 `MAX_LEVEL`(100)·`SUM_MAXLV`(25)로 올려 다시 떴다.

**결과 — 26/26 화면 · «Lv» 노드 267개**

| 축 | 건수 | 내용 |
|---|---|---|
| [S] 획 파먹힘 | **0** | 스트로크를 쓰는 «Lv» 노드는 전부 `paint-order:stroke fill` 이다 ⇒ 등재문 처방 ② 는 **손댈 것이 없다** |
| [C] 하드 클립 | **4**(같은 부품 ×4 탭) | **21 도감 카드 `.cd > i.cl2`** — `Lv. 100/n` 잉크 **116.9** vs 카드 안쪽 **111** ⇒ 좌우 각 **−2.9px** 이 `.cd{overflow:hidden}` 에 잘린다 |
| [B] 테두리 물림 | 5 | 89 유물 `.rw-c>u` 아래로 −21.4(자르는 조상 없음 = 보인다) · 21 도감 `i.l` 상하 −2(라인박스 기준, 잉크 아님) |

- **10 상점 소환 탭은 [C]·[B]·[S] 어느 축에도 안 걸린다.**
- 3자리 레벨은 **실제로 도달 가능**하다(`MAX_LEVEL = 100`, `oLv()` 가 그대로 찍힌다) ⇒
  21 도감의 −2.9px 은 합성 상태가 아니라 **진짜 결함**이다. 내 작업 단위가 아니라
  **곁다리로 등재**했다(아래 §8).

### 7. 같이 재 둔 것 — 07·26·50 슬롯 «Lv. n» (411 이관 대기 건)

주인 원문의 «소환» 을 07 스킬 시트(=[스킬 소환] 버튼이 있는 시트)로 읽는 갈래도 재 봤다.
스킬 8칸을 장착시켜 슬롯 줄을 띄우고 실측:

| 값 | 슬롯 기준 |
|---|---|
| 그림 상자 `.sk-si` | 12 .. 103 (411 이 `SLOT_ART.h` 를 80 으로 키운 뒤 실제 그림 **≈24 .. 96**) |
| `Lv. 99` 라인박스 | 78 .. 105 (잉크 ≈83 .. 102) |
| 겹침 | 그림 하변 96 ↔ 잉크 상단 83 = **약 13px** (411 이 «13~18px» 로 넘긴 그 값과 일치) |

⇒ 411 의 «주인 판단 대기» 는 **실재**한다. 다만 `.sk-slv` 는 `.sk-si` **뒤에 오는 형제**라
**글자가 그림 위에 그려진다** — 잉크가 가려지는 것이 아니라 **배경이 시끄러운 것**이다.
좌우도 여백 20.4px 로 안 잘린다. ⇒ **«잘려보임» 의 자리는 여기도 아니다**(대비 문제이고,
되돌리는 손잡이는 411 이 적어 둔 `SLOT_ART.h` 한 줄이다 — **411 의 몫으로 그대로 둔다**).

### 8. 곁다리 등재

- **21 도감 카드 «Lv. n/m» 3자리 레벨에서 좌우 2.9px 씩 잘림** — 별도 번호로 등재(§ PROGRESS).
  «수리 전 트리에서도 같은 값» 임은 이 작업이 `index.html` 을 **한 줄도 안 고쳤다**는 것으로 갈음된다.

### 9. 남은 문제 — 다음 세션이 볼 것

`index.html` 은 **0줄** 고쳤다. 이 화면에서 고칠 것을 **못 찾은 것이 아니라 없다**는 쪽에
증거가 세 겹(rect · 찍힌 픽셀 · 글리프 지도)으로 서 있고, 레퍼런스와도 Δ0 이다.

다음 세션이 할 일은 **자를 다시 대는 것이 아니라 자리를 좁히는 것**이다:

1. **주인 스크린샷 원본을 확보한다.** 등재문은 그림을 본 사람이 적었고 나는 못 봤다 —
   «알약 테두리» 처럼 이 부품에 없는 것이 묘사돼 있어 **다른 부품일 가능성**이 남는다.
2. `scan470` 이 **아직 못 연 팝업**을 채운다 — 소환 확률 팝업(카드 🔍), 12 소환 결과 팝업,
   승급전·레이드 계열. (오프너를 넣는 것까지가 그 세션의 범위다 — 397 «스코프 구멍» 선례.)
3. 그래도 안 나오면 **21 도감 건**이 주인이 본 것일 가능성이 가장 높다(«레벨**들**» 이
   한 화면에 20칸 넘게 있고, 실제로 잘리는 유일한 자리다).

## 4회차 (2026-08-30, sess-1451-9589 워커 H) — **스코프 구멍을 메우고 자를 게이트로 굳혔다. 제품 0줄.**

3회차 §9-2 가 남긴 숙제 두 개 중 **2번(«아직 못 연 팝업을 채운다»)을 끝냈다.**
1번(주인 스크린샷 원본)은 이 환경에서 확보할 길이 없고, 3번(21 도감)은 **487 로 이미 등재돼 있다.**

### 1. 스코프 — 26화면 → **54화면** · «Lv» 노드 267 → **573**

1~3회차가 «전수» 라고 적은 26화면은 **전수가 아니었다.** 한 번도 안 연 자리 28개를 채웠다:

| 묶음 | 채운 화면 |
|---|---|
| **소환 계열**(주인 원문 «소환팝업쪽») | 11 소환 확률 팝업(카드 🔍) · 12 소환 결과 팝업 |
| 세부 팝업 | 08 스킬 · 26 펫 · 50 코스튬 · 05 방패 · 05 목걸이 · 16 유물 · 04 던전 |
| 21 도감 | 방어구 · 장신구(여섯 칸 중 넷만 열려 있었다) |
| ▦ 메뉴 | 우편 · 랭킹 · 55 설정 · 32 가이드 · 56 절전 |
| 35 패스 | 스테이지 · 시련의 탑 · 절망의 탑 · 36 출석 |
| 그 밖 | 33 재화 정보 3자리 · 20 스펙 · 103 채팅 · 승급전 · 16 룰렛 · 09 일괄 강화 결과 |

⚠ **문 셋이 죽어 있었다** — 397 «스코프 구멍» 과 같은 꼴이고, 죽은 문은 **실패가 아니라
«검사가 없던 일»** 이 된다(smoke 351-13회차 교훈). 셋 다 «그럴듯한 선택자» 라서 조용했다:

| 화면 | 죽은 문 | 산 문 |
|---|---|---|
| 26 펫 세부 | `[data-pet]` (아이콘 **캔버스**다 — 상세를 안 연다) | `[data-ptslot]` |
| 04 던전 세부 | `[data-dun]` (03 **패널 본문**의 [도전] 버튼) | `#dunList [data-dcard]` |
| 승급전 | `#promoBtn` (`bSt` 본문 — 그 탭을 여는 길이 없다) | `.side .ibtn[data-pop="promo"]` |

⇒ 못 연 화면은 이제 **이름을 찍고 합계에 «못 연 화면 n건» 으로 남는다.** 조용한 초록 금지.
12 소환 결과는 363 «연출 스킵»(`S.opt.sumSkip`)으로 최종 상태에 세운다(스캔은 정지 화면을 잰다).

### 2. 결과 — 소환 계열은 셋 다 초록, 전수 클립은 여전히 **21 도감 하나뿐**

- **10 상점 소환 탭 · 11 확률 팝업 · 12 결과 팝업** — [C]·[B]·[S] 어느 축에도 안 걸린다.
  최소 좌우 여백 **13.3px**. ⇒ 3회차의 «등재문 자리 기각» 이 **소환 계열 전체로 넓혀져 확정**됐다.
- **[C] 하드 클립 6자리** — 전부 `21 도감 .cd > i.cl2`(−2.9px). 4탭 → **6탭 전부**에서 같은 값이다.
  → **487** 로 등재돼 있다(내 작업 단위가 아니라 안 고쳤다).
- **[S] 획 파먹힘 0** — 573 노드 전수. 등재문 처방 ② 는 손댈 것이 없다(3회차 결론 유지).

### 3. ⚑ **이 회차의 본체 — 자에 «스크롤로 닿는 밖» 조항이 없었다**

게이트를 쓰다가 **스캐너와 자가 같은 화면에서 다른 답을 냈다**(스캐너 6건 ↔ 자 299건).
갈라진 곳은 판정식이 아니라 **[C] 의 정의**였다:

> `#shopList`(세로 스크롤러) **밖으로 스크롤된** 배너 카드의 «Lv.99» 가 `clip.b = −55` 로
> **하드 클립으로 찍힌다.** 3회차 §3 은 이것을 손으로 «스크롤하면 보인다 = 잘림이 아니다»
> 라고 판정했지만, **자에는 그 조항이 없었다.**

⇒ 같은 화면이 **스크롤 위치 하나로 초록도 빨강도 되는** 플레이키 축이었다(4회차에 실제로 그렇게 찍혔다).
처방은 **축마다 «그 그릇이 그 축으로 `auto`/`scroll` 인가»** 를 같이 보는 것이다 —
세로 스크롤러 안에서 **가로로** 넘치는 글자는 여전히 진짜 잘림이므로 두 축을 따로 판정한다.
`overflow:hidden`/`clip` 만 «진짜로 안 그려진다» 로 센다.

⚑ **분류를 두 벌 적은 것이 사고의 뿌리다.** 게이트가 자기 사본으로 세다가 갈라졌다 ⇒
`scan470.js` 가 `classify`/`clipHit`/`padHit`/`strokeHit` 를 **내보내고 자가 그것을 가져다 쓴다.**
정의가 하나여야 자가 스캐너를 지킬 수 있다.

### 4. 게이트 `tools/verify470.js` 신설 — **16/16 PASS**

제품이 0줄이라 «이미 참인 것을 굳히는» 헛초록이 되기 쉬운 자리다(338 이 등재문 처방을
그대로 따랐으면 그랬을 자리). 그래서 이 자는 **값이 아니라 스코프를 지킨다**:

| 절 | 지키는 것 |
|---|---|
| [A] 1~6 | 54화면의 **문이 전부 살아 있다**(못 여는 문 0) · 화면 수 하한 54 · 노드 하한 500 · 소환 계열 3화면이 목록에 실재 |
| [B] 1~4 | 소환 계열 3화면이 세 축 전부 초록 · **B4 가 «노드 0으로 얻은 초록»** 을 막는다 |
| [C] 1~2 | 21 도감 **밖**의 하드 클립 0 · 래칫 REMAIN ≤ **6자리**(원시 135행을 «자리» 로 접어 센다 — 칸 수가 늘면 흔들리는 축을 피한다) |
| [S] 1 | `paint-order` 빠진 «Lv» 노드 0 |
| [R] 1 | 알약을 36px 로 **좁히면 [B] 가 빨개진다** — 양성 통제(0/0 이면 자는 아무것도 안 본다) |
| [R] 2 | `.cd{overflow:visible}` 로 **풀면 [C] 가 6 → 0** — 탐지기가 상수가 아니라 그 자리를 본다 |
| [H] | 페이지 에러 0 |

### 5. 판정 — **등재문 기각 확정. 실재하는 잘림은 487 하나다.**

증거가 이제 네 겹이다: rect · 찍힌 픽셀(30표본) · 글리프 지도 · **전수 573노드**.
주인 원문 «레벨**들**»(복수)에 맞는 자리는 게임 안에 **21 도감 하나뿐**이고,
한 화면에 20칸 넘게 깔린다 — 3회차가 «가장 가능성이 높다» 고 적은 그 자리다.

⇒ **470 은 여기서 닫는다.** 남은 것은 487 이 고칠 −2.9px 이고, 이 회차가 그 자리를
**4탭 → 6탭 전수로 못박아** 넘긴다. 스크린샷 원본이 나중에 확보돼 다른 자리가 드러나면
`scan470` 은 이제 54화면을 열고 있으므로 **그 자리가 목록 안에 있을 확률이 훨씬 높다.**

## 자·산출물

- `tools/probe470.js` — 10 상점 소환 탭 알약 재현(찍힌 픽셀 · 널 대조 · 30표본)
- `tools/scan470.js` — 전 화면 «Lv» 잉크 잘림 전수 스캔(**54화면 · 573노드** · [C]/[B]/[S] 3축).
  4회차에 축 판정(`classify`/`clipHit`/`padHit`/`strokeHit`)을 **내보내도록** 바꿨다 — 게이트가 같은 정의를 읽는다.
- `tools/verify470.js` — 게이트 **16/16 PASS**(스코프 · 등재 자리 · 래칫 · 되돌림 2건)
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄
- 회귀: `verify10` 61/61 · `verify85` 39/39 · `verify240` 36/36 · `probe470` 초록 · **SMOKE PASS**
