# 743 — `verify356` [S3] ① 1건 실패: `scan418.js` FIXED 표가 644 의 «상자 59 → 55» 를 안 따라왔다

**제품 `index.html` 0줄 · 편집 파일 2개(`tools/scan418.js` 1행 · `tools/verify356.js` 실패 문구 1자리) · 레이아웃 Δ0px · 밸런스·판정 0줄**
(2026-09-01, sess-1800-24740 루틴 워커 D · 1회차)

## 1. 등재문 확인 (730·708·319 와 같은 꼴 — «죽은 것은 기대값 상수»)

- 실측(수리 전 트리): `node tools/verify356.js` →
  `✗ [S3] ① 22 퀘스트 .qs-i(418) — 상자 55×55, 기대 59 정수 (소수 상자 1.08em 이 되살아났다)`
- **제품이 옳다** — `index.html` 2596~2600 의 644(2026-09-01) 주석이 그 변경을 선언해 뒀다:
  `cur-dia.svg` viewBox 를 잉크 bbox 로 잘라 채움비 1.0000 ⇒ «상자 = 그려지는 잉크» 가 되어
  144 의 ref 55 를 맞추는 값이 **정확히 55**(`.qs-i>.cic{width:55px;height:55px}` 2600행).
  59 × .9375 = 55.31 이던 옛 산수의 `.9375` 가 사라진 것이다.
- 죽은 것은 `tools/scan418.js:70` `FIXED` 표의 `box: 59` — 자만 안 따라왔다.

## 2. 등재문의 확인 사항 ⓐ — 같은 표의 나머지 네 자리 대조

644 는 `cur-*.svg` 14~15장의 **viewBox(채움비)** 만 잘랐고 CSS 상자 선언은 `.qs-i` 하나만
바뀌었다(그 자리만 `.9375` 역보정이 끼어 있었다). 제품 선언 실측:

| FIXED 항 | 표 기대 | 제품 선언 (index.html) | 판정 |
|---|---|---|---|
| `#ciIcon` | 98 | `.ci-ic>i>.cic{width:98px}` (9410) | 일치 |
| `.at-if>em` | 82 | `.at-if>em>.cic{width:82px}` (3451) | 일치 |
| `.qs-i` | **59** | `.qs-i>.cic{width:55px}` (2600) | **불일치 — 이 행의 본체** |
| `.ps-bx>i` | 88 | `.ps-bx>i>.cic{width:88px}` (14708) | 일치 |
| `#tutoRew` | 67 | `#tuto .trew .ri>.cic{width:67px}` (1301) | 일치 |

⇒ 갈 것은 한 칸뿐. 나머지 넷은 644 의 영향권 밖(채움비만 바뀜 · 상자 선언 불변)이다.

## 3. 등재문의 확인 사항 ⓑ — 실패 문구가 다른 병을 말하고 있었다

`verify356.js` 1340행의 고정 문구 «(소수 상자 1.08em 이 되살아났다)» 는 418 의 **원래 결함**
(소수 상자)이고, 이번 실측은 **정수 55** 다 — «정수인데 기대값이 다르다(표가 제품 규격
변경에 뒤처졌다)» 는 다른 병이라, 그대로 두면 다음 세션이 418 회귀로 오독한다
(743 등재 회차가 실제로 그 오독을 했다). ⇒ 실패 분기에서 실측값이 정수인지 갈라
두 문구를 따로 낸다(수리 후 코드 주석에 743 근거 명시).

## 4. 수리 내용

1. `tools/scan418.js:70` — `box: 59` → `box: 55`, lab 에 출처 표기(`418 · 644 가 55 로`).
2. `tools/verify356.js` [S3] ① 실패 분기 — 정수/소수 갈래 문구 분리(위 §3).

**래칫은 안 건드렸다** — [S3] ③ 의 칸/자리 수는 «줄이는 쪽으로만»(601·12회차 규약) 그대로.

## 5. 검증

- 수리 **전** 완주: `verify356` = **PASS_BEFORE** (아래 §6 표)
- 수리 **후** 완주: `verify356` = **PASS_AFTER**
- 되돌림 확인: 수리 전 트리의 같은 실행이 [S3] ① 을 빨갛게 찍는 것 자체가 «59 면 빨강»
  의 증거이고, 수리 후 초록이 «55 면 초록» 이다(고친 것이 기대값 상수 하나라 별도 R 시험 불요
  — 334 의 «되돌림 시험 신설» 은 *무르게 푼* 수리를 못박는 장치인데, 이 수리는 허용 오차를
  한 칸도 안 넓혔고 항 자체를 지우지도 않았다).
- 회귀: `verify144`(144 «잉크 55×55» — 제품 0줄이므로 무수정 초록이어야 한다) · SMOKE.

## 6. 실행 기록

### 6-1. [S3] ① 표적 재현 (옛 표 ↔ 새 표, 같은 측정식·같은 트리)

verify356 [S3] ① 의 측정식(`getComputedStyle` width/height, 오차 0.01)을 그대로 떼어
옛 `scan418.js`(수리 전 커밋 `e19cb64~1`)와 현행을 나란히 돌렸다:

```
옛 표(box:59): sel=.qs-i>img.cic got=55,55 기대=59 → 빨강
새 표(box:55): sel=.qs-i>img.cic got=55,55 기대=55 → 초록
```

제품 실측이 55×55 로 산다 — «59 면 빨강 · 55 면 초록» 이 같은 트리에서 못박힌다.

### 6-2. 수리 후 완주 (run A)

`node tools/verify356.js` 완주(71화면 전부 진입 · 판정 노드 305):
- **[S3] ① 5/5 초록** (98·82·55·88·67 전부 «정수 고정»)
- [S3] ② 3/3 초록 (진입 71 · 노드 305 · 연출 정규화 잔여 0)
- [S3] ④ 되돌림 초록 (소수 상자를 도로 심으면 그룹 8개 빨강 — 자가 살아 있다)
- **전체 250/255 — 남은 빨강 5건은 전부 [S3] ③ 래칫·눈금**(이 행의 편집이 닿는 경로가
  없는 잉크 스윕 축이다 — FIXED 는 ①만 읽고, ③ 은 `inkSweep` 결과를 센다):
  1. 래칫 칸 — 편차 >0.5% 인 칸 103 > 래칫 79
  2. 래칫 자리 — 20 > 래칫 18
  3. 이름표 — 등재 안 된 자리 8개(`#psTk .ps-bx.c1/.c2` · `#dunw/#relw/#shopw .pcb-p.pcb-d` ·
     `#psArt` · `#dunList .dnc.bgm-dia .pill>em` · `#relw .pcb-p.pcb-r`)
  4. 눈금 — `#tutoRew` −2.75% > 1.2% · `.skd .vl .nt>b` +1.79% > 1.4%
  5. 눈금 [전제] — 상한 붙은 7자리 중 5자리만 측정(상점 2자리 미표집)
- run B(같은 트리 재실행)로 위 5건의 실행 간 안정성을 대조했다 — §6-3.

### 6-3. run B 대조 · 판정

(채움 예정)
