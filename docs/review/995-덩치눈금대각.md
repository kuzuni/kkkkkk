# 995 — `verify792` [E1] 의 덩치 눈금이 «bbox 대각» 이라 잠복 초록을 만든다

> T1 «버그(잠복 게이트)» · 제품 `index.html` **0줄** · 고친 것은 **자 두 벌**(`tools/verify792.js` ·
> `tools/verify982.js`) 과 **신설 한 벌**(`tools/verify995.js`) 뿐이다.

## 1회차 (2026-09-06, sess-1443-26339 루틴 워커 C · 1회차 · 완료)

> ⚠ **곁다리 등재 번호가 996 → 998 로 밀렸다** — 이 회차를 쓰는 사이에 다른 워커가
> **996**(`cap710` 이 시간을 안 얼린다)·**997**(`probe683e` 가 자기 자신을 견준다)을 먼저 올렸다.
> 340 선례대로 **남의 번호를 비켜 줬다**(rebase 충돌을 «양쪽 다 살린다» 로 풀고 내 행만 옮겼다).

### 결손 — 재현 비용 0 이었다

등재문 그대로다. 세 줄이 전부 `verify792` 가 **스스로 찍던 같은 표**(종별 bbox w×h 17행)에서 나오고,
새 자를 짤 필요가 없었다. 착수 시점 HEAD(`d954419`)에서 같은 17행을 세 가지로 접은 값:

| 접는 법 | 중앙값 | 최소~최대 | 스프레드 | ±25% 밖 |
|---|---|---|---|---|
| **대각**(옛 [E1] 눈금) | 142.9 | 108.9 ~ 173.5 | **1.59배** | **0종** |
| **최대 변** | 112 | 77 ~ 162 | **2.10배** | **5종** (shuri·stone·arrow·meteor·lance) |
| **기하평균 √(w×h)** | 98.4 | 57.8 ~ 118.9 | 2.06배 | 4종 (slash·curve·ice·lance) |
| **잉크 √면적** | 61.4 | 40.3 ~ 91.2 | 2.26배 | 5종 (curve·whirl·boom·bounce·gale) |

⚑ **뿌리는 산수 한 줄이다** — `대각 = 최대변 × √(1 + (최소변/최대변)²)` 이라 가산분이
**종횡비에 통째로 달려 있다**. 자가 지금 스스로 찍는 종별 가산분(‥ = `verify995` 기록):

    boom 41.4%(비1) · stone 41.4%(비1) · shuri 41.4%(비1) · whirl 39.6% · bounce 36.9% ·
    boomer 36.8% · spiral 36.1% · flask 29.1% · rico 28.1% · gale 22.1% · multi 18.6% ·
    ice 9.3% · meteor 8.2% · arrow 7.1% · slash 6.5% · curve 2.0% · **lance 1.1%(비6.91)**

14회차 비평 2인(CZ·DA)이 «작다» 고 지목한 둘(`stone`·`shuri`)이 정확히 **정사각형**이고,
«크다» 고 지목한 둘(`arrow`·`lance`)이 정확히 **가느다란 조각**이다. ⇒ 대각은 **눈이 보는
스프레드를 그 자리에서만 골라 눌러 준다.** [E1] 의 초록은 규격이 선 증거가 아니라 **눈금이 만든 것**이었다
(LESSONS 979-② 의 다섯 번째 자리 · 989 «상자가 만든 초록» 의 형제).

### 고른 눈금 — **최대 변**(선택이지 측정이 아니다 · 394 규약 · 위임 규약 채택)

등재문이 «잡는 세션이 먼저 정할 것» 으로 남긴 한 줄을 이 회차가 골랐다. 근거는 하나다 —
**셋 중 최대 변만이 두 비평가가 «눈으로» 낸 판정을 그대로 도로 낸다**:

| 접는 법 | 2인이 «작다» 로 지목(stone·shuri) | «크다» 로 지목(arrow·lance) | 판정 |
|---|---|---|---|
| 대각 | 밴드 **안** | 밴드 **안** | ✗ 아무것도 안 잡는다 |
| **최대 변** | **둘 다 아래로 밖** | **둘 다 위로 밖** | ✅ 눈과 일치 |
| 기하평균 | stone·shuri 안 | **`lance` 가 «가장 작은 종» 으로 뒤집힌다**(57.8) | ✗ 눈과 반대 |
| 잉크 √면적 | 안 | `lance` 가 최소 쪽(48.1) | ✗ 눈과 반대 · 채움 밀도(982 축)를 크기 축에 다시 섞는다 |

**손댄 것은 «접는 법» 하나뿐이다** — 상자(989 «본체 + 제 손으로 깐 반투명 부품»)도, 문턱(±25% ·
10회차 비평가 CV 목표)도 **한 글자도** 안 건드렸다. 문턱을 넓혀 초록으로 되돌리는 것이 이 행이
고치려는 병 그 자체다(979-②).

**되돌림 방법**: `verify792` 의 `bulkBand('bulk')` 한 곳을 `'own'`(대각) · `'ownGeo'` · `'inkR'` 로
바꾸면 그만이다. 버린 셋을 지우지 않고 **[E1n] 표에 같이 찍어 둔 것**이 그 경로다.

### 고친 자리

| 파일 | 무엇 |
|---|---|
| `tools/verify792.js` | measure 가 `obw/obh` · **`bulk`**(=max) · `ownGeo` · `visMax` 를 같이 낸다 · [E1] 판정이 `bulk` · `DIAG_TOL` → **`BULK_TOL`**(값 0.25 그대로) · **[E1n] 신설**(네 접는 법 대조표) · [E1v] 이웃 «상자» 도 같은 법으로 접는다 · [R5] **물음을 바꿨다**(아래) · 표 첫 칸이 대각 → 최대 변 |
| `tools/verify982.js` | [C1] 이관 — 같은 눈금(`bulk`) · **묻는 문장도 바꿨다**(아래) · `DIAG_TOL` → `BULK_TOL` · 되돌림 사본을 [C1]·[R1] 이 **한 번만** 굽는다(402) |
| `tools/verify995.js` | **신설 11/11** — [A] 산수 · [B] 선언 · [R] 되돌림 |

### ⚑ 두 자리에서 «묻는 문장» 이 같이 바뀌었다 — 이것이 이 회차의 본체다

눈금을 고치면 **[E1] 이 빨개지는 것이 정상**이고(등재문 명시 · 배율표 `SHOT_SC` 는 대각 위에서
맞춰진 값이라 최대 변으로 재면 밴드가 열린다), 그러면 **[E1] 의 초록을 전제로 서 있던 항들**이
같이 뜻을 잃는다. 그 둘을 그대로 두면 «남의 미완» 때문에 남의 자가 빨개진다.

* **792 [R5]** — 옛 물음은 «배율표를 비우면 [E1] 이 빨개지는가» 였고, 그것은 [E1] 이 수리 후
  **초록**이라는 전제 위에서만 뜻이 있었다. 지금은 언제나 참인 빈 물음이다.
  ⇒ **견줌**으로 바꿨다: «표가 있는 쪽이 없는 쪽보다 덜 벌어져 있는가».
  실측 — 밴드 밖 **5종 → 8종** · 스프레드 **2.10 → 3.39배** ⇒ 배율표는 최대 변 축에도 **닿아 있다**.
  문턱은 부등호 하나뿐이라(825) 792 가 밴드를 닫으면 이 항은 그대로 더 크게 초록이 된다.
* **982 [C1]** — 옛 물음은 792 의 **절대 밴드**를 여기서 한 번 더 묻는 것이었다.
  ⇒ 982 가 질 수 있는 것은 «내 획 처방이 덩치를 더 나쁘게 만들었는가» 하나뿐이므로
  **자기 되돌림 사본(`SHOT_FILL` 빈 표)과 견준다**: «되돌림에서 **안에 있던 종이 밖으로 나갔는가**».
  실측 — 밖 목록이 `[shuri·stone·arrow·meteor·lance]` 로 **양쪽 동일** ⇒ 새로 밖 **0종**.
  ⚠ **스프레드는 판정에 안 넣었다(기록만 · 2.04 → 2.10배)** — 획을 패딩하면 이미 밖에 나가 있는
  종(`arrow` 155 → 162)의 값이 몇 px 더 커지는 것은 그 처방의 정의상 따라오는 몫이고,
  그것까지 부등호로 묶으면 문턱이 곧 손 상수가 된다(825).

### 게이트

| 자 | 결과 |
|---|---|
| `verify995` | **11/11 PASS** (신설) |
| `verify792` | **31/32 — [E1] 만 빨강.** ⚑ **빨간 것이 이 행의 결과물이다**(등재문 «빨개진 뒤에 792 가 배율표로 닫는다»). 밖 5종 = shuri:78 · stone:77 · arrow:162 · meteor:155 · lance:152 (중앙값 112 · 밴드 84~140) |
| `verify982` | **14/14 PASS** (이관 뒤) |
| `verify981` | **23/23 PASS** (무수정 회귀) |
| `node tools/smoke.js` | **SMOKE PASS** (제품 0줄) |

### 곁다리 등재 — **998** (`verify981` [D2] 도 아직 대각을 읽는다)

[E1] 을 그대로 베낀 자리가 하나 더 있다: `tools/verify981.js` **[D2]**(237~245행)가
`V792.measure()` 를 직접 부르고 그 `own`(대각)으로 같은 밴드를 다시 묻는다. **지금은 초록이다**
— 그래서 이 회차가 아무것도 안 해도 빨간 자는 없지만, 초록의 이유가 «규격이 섰다» 가 아니라
**«대각이 정사각형을 부풀린다»** 라는 것이 바로 995 다(같은 병, 세 번째 자리).

**982 는 이 회차에서 이관하고 981 은 등재로 미룬 이유**는 «되돌림 기준» 이 다르기 때문이다 —
982 의 처방은 표 **하나**(`SHOT_FILL`)라 그것을 비운 사본 한 장이 곧 «내 처방 이전» 이고 그 사본을
[C1] 이 이미 굽고 있었다. 981 의 처방은 **네 사본**(`NEG_ROCK`·`NEG_CVX`·`NEG_SEG`·`NEG_FAT`)에
흩어져 있어 «무엇과 견줄 것인가» 를 981 자신이 골라야 한다. 게다가 981 은 **진행 중(4/10)** 이라
남의 회차 한복판에서 그 선택을 대신하면 그 회차의 점수가 무엇을 잰 것인지 알 수 없게 된다
(14회차가 995 를 갈라 낸 것과 **같은 이유**다).

### 다음 세션에게

1. **792 가 배율표(`SHOT_SC`)로 [E1] 을 닫을 때 «최대 변» 으로 역산하라** — 대각으로 맞춘 값이라
   그대로 두면 계속 빨갛다. 밖 다섯의 지금 값과 밴드는 위 게이트 표에 있다.
   ⚠ `stone`(=`rock`)은 856 [B10c] 코어 비 밴드(1.15)가 **위를 막고 있어** 그 종만 배율로는 못 올린다
   (등재문 경고 그대로 · 최대 변 77 → 밴드 하한 84 까지 **+9.1%** 가 필요하다).
   ⚠ `meteor` 는 본체가 아니라 **제 손 불꼬리**가 상자를 155 로 늘린 종이다(본체 96×64) — 배율이
   아니라 **꼬리 길이가 손잡이**다. ⚑ 이 값은 **792 15회차(`60b85f6` — 운석 재작도)가 들어간 뒤**
   트리에서 잰 것이다(rebase 뒤 재실행). 곧 꼬리는 이미 그려졌으니 다음 회차는 **꼬리를 다시 밀지 말고
   그 길이를 상수로 두고 배율만** 잡아라 — 두 회차가 같은 화소를 서로 밀면 [E1] 과 ④ 축이 번갈아 빨개진다.
2. **998**(`verify981` [D2] 이관)은 981 라운드가 한 회차 쉬는 자리에서 잡는 것이 싸다.
