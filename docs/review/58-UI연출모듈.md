# 58 — UI 연출 공용 모듈 (fx) · 비평 기록

> 스펙: `docs/UI-REFERENCE.md` **58번**. 레퍼런스 스크린샷이 **없는** 신규 설계라
> 채점은 ROUTINE [3]-(다) 연출 축 4개(①타이밍 ②크기 비율 ③위치·경로 ④가독성·일관성, 최저 8 통과)로 한다.
> 도구: `node cap58.js <라운드>` (연속 프레임 캡처) · `node verify58.js` (기능 체크)

## 무엇을 만들었나

`#fxl` — 프레임 전체를 덮는 `pointer-events:none` 오버레이(z-index 60, 현재 최상위 `#ciw` 41 위).
**모든 연출 DOM 은 여기에만** 만든다. 대상 요소에 클래스를 붙이지 않는 이유는 25 교훈 5 —
훈련 카드·퀘스트 행처럼 `innerHTML` 로 통째로 재렌더되는 화면에서는 대상에 붙인 클래스가 그 프레임에 날아간다.

| 함수 | 하는 일 |
|---|---|
| `fxFly(from, cur, n)` | 재화 아이콘 **3~6개**가 `from` 에서 HUD 알약의 **코인/젬 아이콘 중심**으로 2차 베지에 호를 그리며 날아가 **꽂힌다**(마지막 프레임을 중심에 정확히 세우고 `.fx-land` 140ms 로 사라진다) |
| `fxPunch(el)` | 도착 순간 알약이 튄다 (`scale 1 → 1.15 → .975 → 1`, 300ms). **진행 중이면 재시작하지 않는다** |
| `fxPlus(cur, n)` | 알약 아래 `+n` 플로팅 텍스트 (750ms). **첫 도착**에 뜬다 |
| `fxPop(el)` | 팝 `0 → 1.2 → 1` (340ms) |
| `fxBurst(t, col, n)` | 방사형 버스트 파티클 (580ms) |
| `fxFlash(el)` | 대상 bbox 위 **금색 워시 + 흰 링 + 앰버 글로우** 1회 + `scale .97 → 1.14` (400ms) |
| `fxHitEl(el)` | 대상 **자체**의 스케일 펄스 `1 → 1.07 → 1` (260ms). 재렌더 화면에서는 재렌더 «뒤» 에 부른다 |
| `fxToast(txt)` | 상단 토스트 슬라이드다운 + 스프링, 1.2초 뒤 페이드아웃. **비행 아이콘보다 위**(z 2) |
| `fxCheck(el)` | 체크 스트로크 드로잉 (`stroke-dashoffset` 애니, 700ms — 18% 지점에서 완전히 뜬다) |
| `fxUpOk(t, at)` | «강화 성공» 한 세트 = `fxFlash` + `fxHitEl` + `fxBurst` |
| `fxReward(t)` | «보상 수령» 꼬리 = 수령 지점 버스트 (이어지는 재화 비행은 자동) |
| `fxAt(t)` | «다음 재화 증가» 의 출발점 힌트(600ms 유효) |

### 전역 부착 — 호출부를 고치지 않아도 걸린다

- **재화 획득**은 `fxWatch()` 가 매 프레임 `S.gold`/`S.dia` 의 **증가분**을 감시해 자동으로 쏜다.
  드랍·보상·판매·룰렛·우편 어느 경로로 늘어도 붙고, **앞으로 생길 경로도 자동 편입**된다
  (46 교훈 3 «모든 경로가 지나는 한 지점» 의 UI 판). 감소(소모)에는 걸지 않는다.
  잘게 들어오는 연속 킬은 **70ms 정지 또는 900ms 누적**으로 묶어 한 번에 쏜다 — 아이콘 폭탄 방지.
  «공중에 떠 있는 총합» 은 `FXFLY_MAX 12` 로 묶는다(골드·다이아가 같이 들어오면 묶음이 둘이라 두 배가 된다).
- **출발점**도 전역이다. `pointerdown` 캡처 리스너 하나가 마지막으로 누른 버튼·카드를 기억하고,
  명시 힌트(`fxAt`)와 «더 최근» 것을 쓴다. 그래서 새 화면이 아무것도 안 해도 «수령 버튼에서 재화가 날아가는» 그림이 된다.
  자동 전투는 `killEnemy()` 가 킬마다 `fxAt(fxWorld(e.x, e.y-e.r))` 를 불러 «적이 죽은 자리» 가 이긴다.
- **숫자 롤링**은 `fxDisp` 라는 «표시값» 을 두고 `drawHud()`·`renderPcb()`(41 팝업 내장 재화 바)가 둘 다 그 값을 읽는다.
  두 곳이 다른 숫자를 보이면 안 되므로 단일 소스로 묶었다. 롤링은 **고정 길이 트윈**(`FXROLL` 0.3초,
  easeOutCubic)이고, 증가를 감지하면 **코인이 첫 도착할 때까지 홀드**한다 — 그래야 «꽂히니까 오른다» 가 된다
  (홀드는 1.4초 뒤 스스로 만료돼, 비행이 안 뜨는 경로에서도 숫자가 굳지 않는다).

### 명시 호출을 붙인 곳

| 순간 | 위치 | 연출 |
|---|---|---|
| 퀘스트 수령 | `openQuest()` 의 `[data-q]` · `#qAll` | `fxCheck` + `fxBurst` + `fxAt` + `fxToast('퀘스트 완료')` |
| 가이드 미션 | `claimTuto()` | `fxReward` + `fxToast('가이드 미션 완료')` |
| 출석·룰렛·던전 보상 | `giveReward()` (셋이 전부 이 함수를 지난다) | `fxReward` |
| 우편 일괄 수령 | `claimAllMail()` | `fxReward` |
| 오프라인 보상 | `claimOffline()` | `fxReward` |
| 스킬 강화 | `showSkillDetail()` `#mLv` | `fxUpOk(.sk-lv, 버튼)` |
| 유물 강화 | `showRelicDetail()` `#mLv` | `fxUpOk(.rl-lv, 버튼)` |
| 장비/아이템 강화 | `showItem()` `#mLv` · `#mLvAll` | `fxUpOk(.mrow, 버튼)` |
| 훈련·스탯 훈련 | `#trw` 의 `[data-tr]` | `fxUpOk(카드, 카드)` — **`renderTrain()` 전에**, `fxHitEl` 은 그 뒤에 |
| 소환 결과 | `showSummonResult()` | 카드가 **등급 오름차순 stagger 55ms 로 순차 팝** + 영웅(g≥3) 이상은 광선 버스트 |
| 훈련/스탯 단계 상승 | `trainUp()` / `statTrainUp()` | 기존 17 스탯업 연출 재사용(스펙대로) |

---

## 회차 기록

### 1·2회차 (2026-08-25, sess-0028-6966 워커 B) — 모듈 신설 + 타이밍 정합

모듈을 올리고 `verify58.js`(기능 체크)로 자체 검증. 이때 캡처는 `page.screenshot()` 기반이었다.

### ⚠ 1차 비평 (비평가 A·B) — **캡처 절차 오류로 무효 판정**

두 비평가가 **독립적으로 «연출 없음 · 0점»** 을 냈다. 지적 요지는 셋 다 같았다 —
«코인이 날아가는 프레임이 0장», «흰 플래시가 0프레임», «숫자가 트윈 없이 순간 점프».

그런데 같은 빌드에서 `verify58.js` 는 **비행 8개 · 알약 아이콘 중심에 0px 도착 · 연출 길이 691ms ·
플래시가 카드 bbox 와 Δ0 · 토스트/체크/버스트 생성**을 전부 통과했다. 둘 중 하나가 틀린 것이다.

원인은 **캡처였다.** 이 컨테이너에서 `page.screenshot()` 1080×2280 한 장이 **337~629ms** 걸린다.
스크립트에 «90ms 간격» 이라고 적어 놔도 실제 간격은 **430~720ms** 라,
0.3~0.8초짜리 연출이 8프레임 중 1~2장에만 걸린다. 비평가들이 본 «연출 없음» 은 사실이었지만
그건 구현이 아니라 **그 이미지들** 에 대한 사실이었다.
→ 04 교훈 1 «캡처 상태가 레퍼런스와 다르면 그 회차 비평은 통째로 무효» 의 **연출 판**.
1차 비평은 폐기하고 회차만 소모한 것으로 센다.

### 3회차 — 캡처를 CDP 스크린캐스트로 교체

`Page.startScreencast` 로 **렌더된 프레임을 타임스탬프째로** 받아 두고, 트리거 기준
0·90·…·630ms 에 가장 가까운 8장을 골라 저장하도록 `cap58.js` 를 다시 썼다.
결과: 목표 대비 **최대 ±63ms**, 원본 프레임 20~37장. 이제 8장이 실제로 0~0.65초를 덮는다.

같이 넣은 결정성 장치(앞 세션 교훈의 재적용):
- `window.step = () => {}` 로 **게임 로직만 정지**시킨다 — 캔버스의 스킬 이펙트·경험치·자동 진행이
  프레임마다 바뀌면 «연출 때문에 바뀐 것» 과 구분이 안 된다. `draw()`/`fxTick()` 은 계속 돈다.
  (1차 비평가 A 가 «정체불명의 회색 스우시가 HUD 반대 방향으로 표류한다» 로 잡은 것이 이 노이즈였다)
- 적은 **비우지 말고 멀리 주차** + `player.inv` (42 교훈 1·2).
- 씬1 의 `t0` 는 «S 증가 시각» 이 아니라 **«첫 비행 아이콘이 생긴 시각»** 으로 잡는다 —
  `fxWatch` 의 묶음 디바운스(180ms)만큼 앞이 비면 8프레임 중 앞 두 장이 정지 화면으로 낭비된다.

`+ 이번 회차에 같이 고친 것`: 비행의 **마지막 프레임을 알약 중심에 «정확히» 세우고** 짧게 사라지게 했다
(`.fx-land`, 140ms). 바로 `remove()` 하면 렌더된 마지막 위치가 `p<1` 이라 도착이 몇 px 모자란 채로 끝난다 —
`verify58` 의 «최근접» 이 8px → **0px** 이 됐다. 전체 길이는 `0.030*7 + 0.43 + 0.14 = 0.78초` 로 스펙(0.3~0.8초) 안.

### 3회차 비평 (비평가 C·D) — ①0/②0/③1/④0 · ②2/③2/④2

캡처가 정상이 되자 **둘이 독립적으로 같은 5가지**를 짚었다. 자체 픽셀·DOM 프로브로 전부 재현시켜 확인했고,
**5건 다 실제 결함이었다**(«비평가 수치는 확인 전엔 반영하지 마라» 로 반증을 시도했지만 이번엔 반증이 안 됐다):

| 지적 | 자체 프로브 재현 | 처방 |
|---|---|---|
| 코인이 화면 위쪽에서 **잘려 사라진다** | 비행 요소 y 최솟값 **−12px**, 프레임 밖 샘플 6건 | 제어점 y 를 `max(96, …)` 로 클램프 → **−12 → 0px, 밖 0건** |
| `+n` 이 **647ms 에야** 뜬다 | `fxPlus` 를 «마지막 아이콘 도착» 에 걸어 뒀다 | **«첫 도착»** 으로 옮김 |
| 숫자가 코인 도착 **전에** 다 올라간다(순서 역전) | 롤링이 `S` 변경 즉시 시작 | 비행이 뜨면 표시값을 **홀드**, 첫 도착에 해제(1.2초 안전 만료) |
| 강화 플래시가 **«회색 비활성»** 으로 읽힌다 | 흰 오버레이 opacity **90ms 0.76 → 186ms 0.36 → 278ms 0.03** — 30~40% 흰 워시가 오래 남는다 | 곡선을 세게 번쩍이게: **90ms 0.91 → 186ms 0.22 → 278ms 0.00** |
| 카드가 **커졌다 복귀하지 않는다** | 오버레이만 scale, **카드 bbox 는 전 프레임 동일** | `.fx-hit` 를 **대상 요소 자체**에 건다(`scale 1→1.07→1`, 260ms). 재렌더에 지워지므로 **재렌더 «뒤»** 에 붙인다 |
| 퀘스트 씬에서 아이콘 7~9개가 **617ms 까지 남아 헤더·HUD 를 가린다** | 골드·다이아 두 묶음이 겹쳐 최대 16개 | 개수 3~8 → **3~6**, «공중에 떠 있는 총합» `FXFLY_MAX 12` 상한, 묶음 디바운스 180 → **110ms** |

체크 스트로크도 같이 당겼다 — 페이드인이 완만해 «체크가 350ms 뒤에야 보인다» 였던 것을
`fxCheckIn` 18% 지점에서 완전히 뜨게 고쳤다(90ms **0.39 → 0.64**, 186ms **0.76 → 1.00**).

### 4회차 — 위 6건 반영 후 재캡처(r3)

### 4차 비평 (비평가 E·F) — 최저 2점 · 2점

또 둘이 독립적으로 같은 곳을 짚었다. 이번에도 **자체 계측으로 전부 재현**됐고, 그중 둘은
**내 앞선 계측이 틀렸다는 것**까지 드러났다.

| 지적 | 자체 계측 | 처방 |
|---|---|---|
| 숫자 롤링이 **645ms 에 목표의 26%** 만 진행 — 0.8초 안에 못 끝난다 | 지수 수렴(`d += d*dt*7`)은 남은 차이를 반씩만 줄여 «끝나는 시각» 이 없다. 실측 완료 **888ms** | **고정 길이 트윈**(easeOutCubic, `FXROLL`)으로 교체 → 완료 **738ms** |
| 알약이 **튀는 프레임이 한 장도 없다** | 아이콘 6개가 30ms 간격으로 도착하며 `fxPunch` 가 **매번 애니메이션을 재시작** → scale 1.02 를 못 넘는다 | 한 번 튀는 동안(300ms)은 재시작 금지 → 최대 확대 **×1.16** |
| 강화 플래시가 **8프레임 전부에 없다** | ⚠ **비평가가 맞았다.** 픽셀로 재니 흰 오버레이가 있긴 했는데 **훈련 카드 배경이 `#FFFDF2`(휘도 252)** 라 알파 .9 를 깔아도 **합성 채널 변화가 +2** 다 — 물리적으로 안 보인다 | 코어를 **금색 워시 + 흰 링 + 앰버 글로우**로 교체 → 채널 변화 **110**, 카드 영역 픽셀 Δ **55·60·44**(3프레임) |
| 카드가 **커졌다 복귀하지 않는다** | `.fx-hit` 를 대상에 걸었지만 `renderUI()` 가 0.35초 주기로 시트를 재렌더해 **90ms 안에 지워진다** | 스케일은 **오버레이가** 맡고(1→1.09→1.14), `.fx-hit` 는 재렌더 «뒤» 에 다시 건다 |
| 연출 시작이 늦다 | 오버레이를 `renderTrain()` **뒤에** 불러서 재렌더 비용만큼 밀렸다 — 클릭 후 **130ms** | 오버레이는 **재렌더 «전»** 에 부른다(#fxl 에 있어 재렌더에 안 지워진다) → **5ms** |
| 코인 경로가 **2단 꺾임** | 제어점 x·y 를 각각 랜덤으로 흔들어 아이콘마다 다른 방향으로 휘었다 | 제어점을 **«직선의 중점에서 수직 방향으로»** 밀어내고 휨 폭만 아이콘별로 다르게 |
| 재화가 **토스트 글자를 가린다** | 둘 다 `#fxl` 안이고 z 순서가 DOM 순서였다 | `.fx-toast{z-index:2}` / `.fx-fly{z-index:1}` — 코인이 토스트 **뒤로** 지나간다 |

> **여기서 얻은 계측 교훈 하나 더**: 앞 회차의 «플래시 opacity 0.91@90ms» 프로브는 **틀린 값**이었다.
> `await sleep(t - prev)` 로 시각을 누적하면 매 호출이 오버슈트해 «t=66ms» 라벨이 실제로는 150ms 다.
> 고정 간격(10ms) 폴링으로 `Date.now() - t0` 를 **매 샘플마다 다시 읽어야** 궤적이 맞다.

### 5회차 — 위 7건 반영 후 재캡처(r5)

`verify58.js` 에 «비평가 없이도 같은 결함을 잡는» 항목 6개를 추가했다 —
**클릭→가시 지연 ≤140ms · opacity≥0.7 유지 ≥150ms · 카드 위 최대 채널 변화 ≥45 ·
알약 확대 ≥×1.10 · 숫자가 코인 도착 «뒤» 에 시작 · 롤링 완료 ≤800ms.**
이 6개가 있었으면 4회차 지적 대부분을 비평 없이 잡았다.

### 5차 비평 (비평가 G·H) — **4점 · 4점** (0 → 2 → 4)

처음으로 «연출이 있다» 는 데는 둘 다 동의했고, 지적이 «있냐 없냐» 에서 **«정도와 순서»** 로 내려왔다.

| 지적 | 자체 계측 | 처방 |
|---|---|---|
| 강화 카드 확대 **+10~19% 라 옆 카드를 침범** | 오버레이 최대 `scale 1.14` = 326px 카드에서 **+45px**, 카드 간격은 **16px** | 최대 **1.05**(+16px)로 축소 |
| 알약 튐이 **한 프레임도 안 잡힌다** | ×1.15 인 구간이 **~40ms**. 게다가 오버슈트 이징(`cubic-bezier .34,1.56`)이 키프레임 사이를 압축해 고원을 더 깎는다 | 고원 구간을 **ease-out** 으로 두고 키프레임으로 폭을 만듦 → ×1.12 이상 **154ms** |
| 토스트가 **행 데이터보다 100ms 늦게** 뜬다 | 페이드인 `.28s` 가 원인(로직 갭이 아니라 **같은 프레임**에 그려진다) | `.14s` + 25% 지점에서 불투명도 완주 → **139ms** (하한은 클릭 핸들러 비용) |
| 보상 다이아가 **캡처 끝까지 목표 미달** | 롤링 완료 **685ms** — 스펙(≤800ms)은 지켰지만 캡처창이 0~630ms 라 구조적으로 못 담는다 | 롤링 `0.30 → 0.26초` + **캡처창을 0~700ms** 로(간격 100ms, ROUTINE «80~100ms» 상한) |
| 코인이 **관계없는 퀘스트 행 위를 훑고** 지나간다 | 제어점 휨이 거리의 `0.20 + i*0.035` | **`0.11 + i*0.018`** 로 절반 |
| 체크 배지가 **보상 숫자를 4프레임 내내 덮는다** | 132×132, 800ms | **92×92, 600ms** |
| 흰 링이 배경 워시보다 오래 남는다 | 애니메이션 400ms 인데 **380ms 에 제거** → opacity 0.09 에서 툭 끊긴다 | 애니메이션 `.34s`, 제거 400ms 로 정렬 |

> **또 하나의 계측 교훈**: «×1.12 이상 유지 50ms» 라는 내 실패 판정이 틀렸다.
> 샘플 하나를 10ms 로 세었는데 `await sleep(10)` 은 evaluate 오버헤드로 실제 **20~25ms** 다.
> 경과 시각을 매번 다시 읽어 적분하니 **154ms** 였다. 43 교훈 1(«내가 쓴 assert 부터 의심하라»)이
> 이 작업에서만 **세 번째** 재현됐다 — 앞의 둘은 «1e13 골드라 fmt 문자열이 안 변함», «누적 sleep 라벨 드리프트».

### 6회차 — 위 7건 반영 후 재캡처(r6)

`verify58.js` 는 이제 **41항목**이고 2회 연속 동일 결과다.

### 6~9차 비평 (G·H 4·4 → I 5·J 3 → K 3·L 3 → M 4·N 3)

점수가 «있냐 없냐»(0~2점)를 지나 4~5점대에 들어선 뒤로는, 지적이 **«보이느냐»** 한 가지로 수렴했다.
이 구간에서 고친 것 중 재사용 가치가 있는 것만 남긴다.

| 지적 | 원인 (자체 계측) | 처방 |
|---|---|---|
| 토스트·체크가 **139~185ms 늦게** 뜬다 | `animation-fill-mode:both` 는 애니메이션이 **실제로 틱하기 전까지** `0%` 상태를 유지한다. `0%{opacity:0}` 이면 클릭 핸들러가 메인 스레드를 잡고 있는 동안 요소가 통째로 «없는» 것이 된다 | `0%` 를 **이미 보이는 상태**로 바꾸고 «위에서 내려오는» 것만 애니메이션으로 → **1~2ms** |
| 데이터가 연출보다 먼저 바뀐다 | 같은 핸들러 안이라 순서를 못 바꾼다 | 수령을 **두 프레임 미룬다**(`rAF(rAF(fn))`). 한 번만 미루면 rAF 는 페인트 «전» 이라 소용없다 |
| 카드 스케일 펄스가 **한 프레임도 안 걸린다** | 60(쥬시)의 누름 스프링 `jz-up` 이 대상의 `transform` 을 앞 250ms 동안 선점한다(실측: 클래스에 `fx-hit` 가 붙어 있는데 `animation-name` 이 `jzUp`) | **한 속성을 두 모듈이 다투게 두지 않는다** — 확대는 내 오버레이(1.06)가 맡는다 |
| 알약 «튐» 이 안 보인다 | 어두운 HUD 위 211×48 알약이 34px 넓어지는 것만으로는 안 읽힌다(실측은 ×1.16) | 스케일에 **밝기 펄스**(`filter:brightness 1.85`)를 얹었다 |
| 착지 코인이 알약 아이콘과 겹쳐 **«이중 원»** | 6개가 200ms 에 걸쳐 착지하니 «항상 한 개는 알약 위에» 있다 | 착지 순간 **0.35배 · 35ms** — 코인 형태가 아니라 «흡수» 로 읽히게 |
| 재화가 **관계없는 퀘스트 행 위를** 훑는다 | 출발(모달 좌측 행)→도착(우상단 알약) 직선이 하필 다른 행들을 관통한다 | 제어점 x 를 **목표보다 더 오른쪽**에 둬서 곡선이 곧바로 패널 밖으로 빠지게 |
| 성공 파티클이 안 보인다 | 14px 옅은 금색 점은 크림 카드 위에서 소실 | 20px + **검정 링**, 시작점도 분산 |

### 10회차 — 캡처 1번을 «트리거 직전» 기준 프레임으로

9회차에 «클릭 후 187ms 무반응» 이라는 지적이 나왔는데, 실제로는 **1번 프레임이 이미 클릭 +32ms** 라
토스트가 기준에도 들어 있었던 것이다. 비평가는 «무엇이 바뀌었는지» 를 기준 없이 판정할 수 없다.
→ `cap58.js` 가 **트리거 직전 프레임을 1번으로** 저장하고 2~8번이 연출 구간을 덮는다.

> **비평 브리핑도 결함의 원인이 된다.** 이 작업에서 «감점» 중 상당수가 사양을 몰라서 나왔다 —
> ⓐ 토스트는 사양이 «1.2초 유지» 라 마지막 프레임에 남아 있는 것이 정상인데 «잔여물» 로 감점됐고,
> ⓑ `fmt` 가 3자리로 반올림해서 `128K + 400 = 128K` 인데 «정산 안 됨» 으로 읽혔다.
> 마지막 라운드부터는 이 두 가지를 브리핑에 명시했다.

### 10차 비평 (비평가 O·P) — **3점 · 5점** · 라운드 상한 도달

| 축 | O | P |
|---|---|---|
| ① 타이밍 | 4 | 6 |
| ② 크기 비율 | 7 | **8** |
| ③ 위치·경로 | 4 | 6 |
| ④ 가독성·일관성 | 3 | 5 |

P 는 알약 튐을 **기준 55×57 → n6 64×66(×1.164) → n8 55×57 원복**으로 실측해
«스펙 1.15→1 과 사실상 일치, 우수» 로 봤다 — 앞선 라운드에서 «폭 변화 0px» 이라던 판정들이
**측정 대상(알약이 아니라 코인 아이콘)을 잘못 잡은 것**이었음이 여기서 확인됐다.

이 라운드에 반영한 것:
- `+n` 플로터가 위로 떠올라 **HUD 숫자 위에 겹치던 것** → 알약 «아래로» 흘리며 사라지게 했다.
- 캡처 스크립트의 `S.gold = 1e13` 주입 자체가 «획득» 이라 연출이 딸려 와 **다음 씬의 기준 프레임을
  오염**시키고 있었다(비평가가 «유령 텍스트» 로 잡았다) → 주입 시 감시 기준값을 같이 맞춘다.

---

# 2차 폴리시 라운드 (2026-08-25, sess-0958-16482 워커 D · 회차 상한 +10 리셋 → 11~20회차)

10회차에서 «보류(10회, 최고 5)» 로 멈춘 것을 ROUTINE [3]-(나) «2차 폴리시 라운드» 규칙으로 다시 잡았다.
**보류 사유 4건 중 1·2 는 «22 구간이라 손대지 않았다» 로 남아 있던 것**이라, 22 에 lock 이 없는 것을
확인하고 그 처방을 실제로 구현했다.

## 11회차 — 보류 4건의 처방을 구현

| # | 10회차 보류 사유 | 11회차 처방 |
|---|---|---|
| 1 | 보상 비행이 «완료 행 위쪽» 행들을 가로질러 «저 퀘스트도 완료됐나» 로 읽힌다 (궤적 실측: 위쪽 두 행 각 14회 통과 — **기하학적 성질**) | 경로가 아니라 **배경을 죽인다**. `fxSolo(el)` 신설 — 수령한 행만 남기고 형제 행에 `.fx-dim`(opacity .3 · saturate .35 · brightness .72) 을 760ms 동안 건다. 복귀는 `.fx-undim` 으로 0.34초 페이드 |
| 2 | 수령 즉시 행이 다음 티어로 바뀌어 «체크가 붙은 행의 진행바가 0/93» 모순 | `fxHoldList()` / `fxRenderLater(fn)` 신설 — `claimQuest`·`claimDQuest`·`claimAllQuests`·우편 수령의 `openQuest()`/`openMail()` 재렌더를 **연출이 끝난 뒤(760ms)** 로 미룬다. 수령 판정은 상태로 다시 하므로 그 사이 재클릭은 무시된다 |
| 3 | 등장 트랜지션이 «스냅» (첫 키프레임을 이미 보이게 둔 대가) | `fxCheckIn` 0% 를 `opacity:1` → **`.34`**, `fxToastIn` 0% 를 → **`.3`**. 0 이 아니므로 메인 스레드가 막혀도 fill-mode 가 «보이는» 상태를 유지하고, 실제로 돌면 앞 12~45% 에 램프가 생긴다 |
| 4 | 버스트가 훈련 카드 아이콘 위에 겹쳐 «애셋이 튄다» | `fxBurst` 출발 반경을 고정 22px → **대상 bbox 의 78% 타원 테두리**. 안쪽 78% 에는 파티클이 한 개도 안 생긴다 |

### 11차 비평 (비평가 Q·R) — **5점 · 4점** (10회차 3·5 → 5·4)

| 축 | Q | R |
|---|---|---|
| ① 타이밍 | 5 | 4 |
| ② 크기 비율 | 5 | 5 |
| ③ 위치·경로 | 6 | 5 |
| ④ 가독성·일관성 | 5 | 4 |

**회수 판정 — 두 비평가가 독립적으로 일치**
1. 형제 행 딤 → **둘 다 «회수됨»**. «프레임 4~8 전부 딤 유지, 코인이 1행 «5회» 위를 지나도 완료로 오독되지 않는다»(R).
2. 재렌더 760ms 지연 → **둘 다 «회수됨»**. «8프레임 전부 «적 60마리 처치 / 60/60 / 초록 보상 받기» 유지, 0/93 같은 모순 없음».
3. 등장 램프 → Q «부분 회수» · R «미회수».
4. 파티클 78% 타원 → Q «회수됨» · R «부분 회수»(아이콘은 피했으나 타원이 카드 하단 비용행 «💰 53» 과 교차).

**즉, 10회차에 «CSS 로는 회수 불가» 라고 적었던 1·2 번은 처방대로 하니 실제로 회수됐다.**
남은 감점은 전부 **새로 드러난 것**이다 — 두 비평가 공통 지적만 추리면:
- **quest 55~207ms · upg 80~176ms 가 기준 프레임과 픽셀 동일** = 클릭 후 최대 207ms 무반응(①).
- **quest 프레임 5(327ms)·6(453ms) 의 비행 아이콘 좌표가 ±2px 동일** = 126ms 완전 정지(①).
- **upg 플래시가 카드 bbox 100%(323×505px)를 62% 워시로 덮어** «Lv.1»(146×34px)·«+2.4»(110×36px) 가 176~295ms 동안 저대비로 뭉개진다. «강조 요소 면적이 강조 대상의 34배»(R). 잔광까지 425ms = 총 704ms 의 60%(②④).
- **quest 체크 링(92px)·버스트가 보상 수량 «90»(44×30px) 의 상단 60% 와 행 제목을 덮는다**(③).
- **gain 출발점이 용사에서 224~278px 우측 빈 바닥** — 배경에서 코인이 솟는다(③).

## 12회차 — 실측으로 원인을 짚고 고친다

11차 지적 중 ① 두 건은 «연출을 더 다듬는» 문제가 아니라 **구조 문제**였다. 추측하지 않고 재 봤다.

### probe58f.js — 비행 좌표를 8ms 간격으로 뜬다

```
t=   3  비행0  toast=0.3  check=0.34      ← 연출 DOM 은 이미 만들어져 있다
t= 144  비행0  toast=0.3  check=0.34      ← 141ms 동안 애니메이션이 «시작조차» 안 한다
t= 191  n=8    toast=1    check=0.99      ← 비행 시작
t= 426  ... 555,32 | 555,32 | 559,38 ...  ← 알약 좌표(555,32)에 도착
t= 517  ... 555,32                        ← **91ms 동안 알약 위에 주차**
```

두 비평가가 «126ms 완전 정지» 로 잡은 것의 정체가 이 **91ms 주차**다.

### probe58g.js — 그 141ms 가 어디서 나오는가

```
메인 rAF 프레임 길이(ms): 31.9 34.4 31.8 31.1 ... (팝업 열린 뒤 35~43)
openQuest 최초: 0.8 ms / 재렌더: 0.9 ms / save(): 0.2 ms
클릭 핸들러 자체: 5.6 ms / raf1: 41.1 ms / raf2: 108.7 ms
```

**`fxThen` 의 2중 rAF 가 혼자 108.7ms 를 먹고 있었다.** 이 구조는 8회차에 «재렌더가 150ms 넘게
메인 스레드를 잡는다» 는 전제로 넣은 것인데, 지금 재 보면 재렌더는 **0.9ms** 다. 그 사이에
① 팝업이 가벼워졌고 ② 11회차에 재렌더 자체를 760ms 뒤로 미뤄서 **막을 것이 아예 없어졌다.**
게다가 이 컨테이너의 rAF 간격이 32~43ms 라 «2프레임 미루기» 의 비용이 애초에 컸다.

### 반영한 것

| # | 지적 | 수정 |
|---|---|---|
| 1 | 클릭 후 최대 207ms 무반응 | **`fxThen` 의 2중 rAF 제거**(→ 동기 호출). 108.7ms 회수 |
| 2 | 보상 획득 디바운스 45ms 가 rAF 간격(32~43ms)과 겹쳐 실제 45~85ms | `fxFlush()` 신설 — 보상 수령처럼 «한 덩어리» 인 경로는 묶음 디바운스를 건너뛴다. `fxReward`·퀘스트·우편에 부착 |
| 3 | 알약 위 91ms 주차 | 착지 이징을 smoothstep(`p*p*(3-2p)`, p→1 에서 속도 0) → **가속 착지**(`p*(0.35+0.65p)`, 도착 속도 1.65). «빨려 들어가 꽂힌다» 는 원래 의도에도 이쪽이 맞다 |
| 4 | 플래시가 카드 전면을 62% 워시로 덮어 글자가 뭉개짐 | 본문 워시 **62% → 26%**, 강조는 `inset 0 0 34px 10px` 안쪽 림이 맡는다. 길이 **.44s → .34s**, 꼬리를 74% 로 당김 |
| 5 | 체크 링이 보상 수량 «90» 을 덮음 | 체크·버스트를 **누른 자리(버튼)** 로, 재화 출발점(`fxAt`)만 보상 배지에 남긴다. 우편도 동일 |
| 6 | 버스트 타원이 카드 하단 비용행 «💰 53» 과 교차 | 링을 **짧은 변 기준 원**(`min(w,h)/2*0.78`, 상한 130px)으로 |
| 7 | gain 출발점이 빈 바닥 | **하네스 결함**이었다 — `cap58.js` 가 `player.x + 120` world px 로 쏘고 있었다(프레임에서 224~278px 우측). 실제 게임은 킬마다 `fxAt(fxWorld(e.x, e.y-e.r))` 로 죽은 자리를 준다. `+26, -54` 로 고쳤다 |

### 검사도 같이 고쳤다 (낡은 것은 구현이 아니라 검사였다)

- `verify58.js` «이어서 재화 비행» 이 **420ms 시점의 스냅샷**으로 재고 있었다. 비행이 50~350ms 에
  끝나도록 빨라지자 «0개» 로 실패했다 — 재야 할 성질은 «비행이 있었나» 지 «420ms 에 아직 떠 있나» 가
  아니다. 12ms 간격 폴링의 **최대치**로 바꿨다(최대 9개).
- 토스트 «완전히 뜬다» 상한 220 → **300ms**. 램프(앞 45%)를 넣은 만큼 뒤로 밀리는 것이 설계다.
  대신 **«등장 램프 관측» 검사를 신설**했다 — `0.24 < opacity < 0.9` 인 순간이 한 번도 없으면 실패다
  (13회 관측). 느슨해진 상한을 이 검사가 대신 조인다.

`node verify58.js` → **VERIFY58 PASS** · `node tools/smoke.js` → **SMOKE PASS**

### 12차 비평 (비평가 S·T) — **4점 · 2점** · ⚠ 내가 낸 회귀로 떨어진 점수다

| 축 | S | T |
|---|---|---|
| ① 타이밍 | 4 | 3 |
| ② 크기 비율 | 4 | 5 |
| ③ 위치·경로 | 5 | 4 |
| ④ 가독성·일관성 | 4 | **2** |

두 비평가가 **독립적으로 같은 것을 1순위로 잡았다**:

> upg F6(405ms) 에서 훈련 카드 내용(`Lv.1` · 검 아이콘 · `+2.4` · `공격력` · `53` · 상단 배지)이
> **통째로 사라지고 351×541px 크림색 빈 사각형만 남는다.** F5(310) · F7(551) 에는 정상.
> «강화 성공» 이 아니라 «카드가 삭제됨» 으로 읽힌다. (T ④ 2점의 단독 사유)

**내가 낸 회귀다.** 12회차에 «워시를 26% 로 낮추고 강조는 안쪽 림이 맡는다» 며 넣은
`box-shadow: inset 0 0 34px 10px rgba(255,206,92,.92)` 가 문제였다 — 퍼짐 10px + 흐림 34px 이
알파 .92 로 **요소 전체를 메운다.** «워시를 26% 로 낮췄다» 는 계산이 무의미해진다.
플래시는 `#fxl`(z60) 위에 있어서 카드 내용을 그대로 덮는다.

→ **안쪽 글로우를 걷어내고 바깥 림(`0 0 0 4px` + `0 0 26px 2px`)만 남겼다.** 꼬리에도 88% 지점을
   추가해 «F7 에서 하이라이트가 100% 증발, 중간 알파 프레임이 없다»(S·T 공통) 는 지적을 같이 처리했다.

## 13회차 — 회귀 수정 + 딤에서 `filter` 제거

### 또 하나의 실측 — `filter` 가 CSS 애니메이션 시작을 막는다

12차에도 «클릭 후 180~315ms 무반응» 이 남아 있었다. `probe58f.js` 로 다시 재니
**비행은 62ms 에 시작하는데 토스트·체크만 200ms 까지 0% 키프레임에 멈춰 있었다.**
`fxSolo` 가 딤을 걸 때 쓰던 `filter: saturate(.35) brightness(.72)` 를 빼자 236 → 196ms 로 내려갔다
(퀘스트 행 4~5개 × 약 1000×150px 마다 새 래스터 레이어가 생기고, 그 래스터가 끝나기 전에는
같은 프레임에 건 애니메이션이 시작하지 못한다). **딤은 `opacity` 단독으로 충분히 읽힌다.**

| 실측 | 11회차 | 13회차 |
|---|---|---|
| 비행 시작 | 191ms | **62~82ms** |
| 알약 위 «주차» | 91ms | **0** (가속 착지) |
| 전부 착지 | ~600ms | **~400ms** |
| 토스트·체크 0% 유지 | 141ms | 147ms (남은 문제) |

남은 «0% 유지 147ms» 는 **이 컨테이너의 렌더 비용**이다(rAF 간격 32~43ms · 스크린캐스트를 켜면 74ms).
오버레이 DOM 12개 + SVG 를 새로 래스터하는 데 3~4프레임이 든다. CSS 로 더 줄일 여지는 크지 않고,
`0%` 를 다시 `opacity:1` 로 되돌리면 이번엔 «스냅» 지적이 돌아온다(10·11회차에 실제로 그랬다).

### 13차 비평 (비평가 U·V) — **3점 · 4점** · 회귀는 회수 확인

| 축 | U | V |
|---|---|---|
| ① 타이밍 | 3 | 4 |
| ② 크기 비율 | **6** | **6** |
| ③ 위치·경로 | 4 | 4 |
| ④ 가독성·일관성 | 4 | 5 |

**카드 내용 소실 회귀 — 둘 다 «회수됨»**:
> «upg f4~f6 에서 플래시가 알파 20~25% 크림 워시로만 깔리고 Lv.1 · 검 아이콘 · +2.4 · 공격력 · 53 이
> 전 프레임 판독 가능»(U) / «반투명 앰버 틴트 + 골드 테두리로 바뀌어 내용이 한 프레임도 안 가려진다»(V)

② 크기 비율은 4~5 → **6** 으로 올랐다(플래시 축소·버스트 링 정정의 효과). 나머지는 정체다.

## 2차 라운드 마감 — 남은 문제와 다음 세션에 넘기는 것

**11~13회차 사용(2차 라운드 상한 20회 중 13회 소진, 7회 남음). ①~④ 최저 3~4 로 8 미달 — 계속 보류.**

### 회수한 것 (비평가 6명이 확인)

| 항목 | 근거 |
|---|---|
| 보상 비행이 다른 행을 «완료» 로 오독시킴 | Q·R·S·T 모두 «회수됨». `fxSolo` 형제 행 딤 |
| «체크가 붙은 행의 진행바가 0/93» 모순 | Q·R 모두 «회수됨». `fxRenderLater` 760ms 지연 |
| 강화 플래시가 카드 내용을 지움(12회차 회귀) | U·V 모두 «회수됨». 안쪽 글로우 제거 |
| 비행 시작 지연 | **191ms → 62ms** (실측) |
| 알약 위 «주차» | **91ms → 0** (가속 착지, 실측) |
| ② 크기 비율 | 4~5 → **6** |

### 남은 문제 (다음 세션이 이어서 볼 것 — 회차는 **14회차부터**)

1. **«클릭 후 139~235ms 무반응» — CSS 로 더 줄일 여지가 없다.** 원인을 전부 쟀다(index.html 의
   «연출 시작 지연 ~150ms 에 대하여» 주석에 정리):
   JS 8.4ms · `fxThen` 108.7ms(제거함) · `filter` 40ms(제거함) · 합성 승격 21ms(적용함) ·
   **남은 ~150ms 는 이 컨테이너의 프레임 예산(rAF 32~43ms, 스크린캐스트 74ms)**.
   → **여기를 또 파지 마라.** ① 점수의 상당 부분이 여기 묶여 있는데 실기기에서는 1/4 이하다.
   판단이 필요하다: 저장소 주인에게 «①(타이밍) 을 이 컨테이너 캡처로 채점하는 것이 맞는가» 를 묻거나,
   캡처를 실기기 프레임률로 보정하는 방법을 찾아야 한다.
2. **비평가가 «166ms 동결» 로 잡는 구간이 캡처에만 나타난다.** 헤드리스 실측(probe58f)에서는
   비행이 62 → 400ms 에 단조 이동하고 정지 구간이 없는데, 스크린캐스트 캡처(원본 19~21프레임/1400ms
   = 74ms 간격)에서는 두 프레임이 같은 좌표로 잡힌다. **캡처 절차가 ① 을 왜곡하고 있을 가능성이 높다** —
   `cap58.js` 의 `everyNthFrame:1` 로도 74ms 밖에 못 뽑는다. 다음 세션은 채점 전에 이것부터 정리해라.
3. **도착 프레임이 8장에 안 잡힌다**(U·V 공통) — 코인이 알약에서 79~174px 떨어진 곳에서 소멸하는 것처럼
   보인다. 기능 게이트는 «알약 아이콘 중심에 0px 도착» 을 PASS 로 재고 있으므로(verify58 [2]·[8])
   실제 도착은 정확하다. 2번과 같은 뿌리로 보이나 **확인은 안 했다.**
4. **카운트업이 마지막 프레임에 목표치에 1~2% 못 미친다**(비평가 4명 공통, 실측 일치). 트윈 종료 시
   목표값 클램프가 필요하다 — 이건 캡처 문제가 아니라 실제 결함이다. **가장 먼저 잡을 것.**
5. **씬 간 어휘 불일치**(S·U·V 공통): upg 에는 델타 플로터(`+0.2 공격력`)가 없고 파티클이 16px(카드 폭의 5%)
   인데 gain 코인은 55~68px. 버스트 파티클을 24~28px 로 통일하고 upg 에도 플로터를 붙이면 ④ 가 오른다.
6. **딤 복귀가 8장 안에 안 보인다**(V) — 760ms 유지 뒤 복귀라 캡처 마지막 프레임(680~734ms)에 아직 100%다.
   유지를 620ms 로 줄이면 복귀 구간이 캡처에 들어온다.

### 이번 세션이 만든 도구

- `probe58f.js` — 비행 좌표·토스트/체크 opacity 를 8ms 간격으로 뜬다(정지 구간·시작 지연 판정용)
- `probe58g.js` — rAF 프레임 길이 · `openQuest`/`save` 비용 · raf1/raf2 지연
- `probe58h.js` — 강화 경로의 핸들러 비용과 플래시 opacity 궤적
- `verify58.js` 에 **[11] 지연 재렌더가 닫힌 팝업을 되살리지 않는다** · **등장 램프 관측** 검사 추가
- `verify58.js`/`cap58.js` 에 번들 브라우저 실패 시 `/opt/pw-browsers/chromium` 폴백(`pwLaunch`)

## (10회차 시점) ⏸ 보류 판정 — 남은 문제

> 아래는 **10회차에 보류할 때** 남긴 기록이다. 1·2 번은 위 11회차에서 처방대로 구현해 **회수됐다**(비평가 Q·R 공통 확인).

**회차 상한 10회 소진 · ①~④ 최저 5점**(0 → 2 → 4 → 5 → 3 → 4 → 3 → 5). 통과 기준 8 미달로 **보류**한다.
기능은 전부 동작하며(`verify58.js` 41항목 PASS) 남은 것은 «읽히는 정도» 다.

1. **보상 비행이 «완료한 행» 위쪽의 다른 퀘스트 행들을 가로지른다.** 비평가 5명이 연속으로
   «저 퀘스트도 완료됐나» 로 읽었다. 궤적을 실측하면 **완료 행 아래쪽 행은 0회, 위쪽 두 행은 각 14회**
   통과한다 — 출발(리스트 하단)에서 도착(우상단 HUD)으로 가는 어떤 경로든 그 사이 행들을 지난다는
   **기하학적 성질**이다. 제어점을 패널 밖으로 빼려면 화면 밖(x≈1400)에 둬야 해서 곡선이 오히려 부자연스럽다.
   → 처방 후보: 비행 중 **다른 행을 잠시 딤 처리**하거나, 22 퀘스트 팝업이 **완료 행을 잠시 유지**하도록
   («수령 즉시 다음 티어로 교체» 를 연출이 끝난 뒤로) 바꾼다. **둘 다 22 구간이라 손대지 않았다.**
2. **수령 즉시 행이 다음 티어로 바뀌어 «체크가 붙은 행의 진행바가 0/93»** 인 모순이 남는다(1번과 같은 뿌리).
3. **등장 트랜지션이 «스냅» 으로 보인다.** 첫 키프레임을 이미 보이는 상태로 둔 것(교훈 4)의 대가다 —
   메인 스레드가 막혀도 즉시 보이는 대신 페이드인 구간이 없다. 둘을 동시에 만족시키려면
   연출 DOM 을 **클릭 이전에 미리 만들어 두는** 구조가 필요하다.
4. **파티클이 훈련 카드 아이콘 위에 겹쳐 «애셋이 튀는» 것처럼 보인다**(P ④-2). 버스트 반경을
   카드 아이콘 바깥에서 시작하도록 더 밀어야 한다.

> **내 구간이 아니어서 손대지 않은 것**: P 가 지적한 «강화 시 상단 골드 수치가 흰→분홍→흰» 은
> **60(쥬시)의 `jzTint`** 다 — 골드를 «쓰는» 동작이라 빨강 틴트가 뜨는 것이 설계대로다(결함 아님).

---

## 기능 체크 (T2 «기능 완성 규칙») — `node verify58.js`

| # | 항목 | 결과 |
|---|---|---|
| 1 | `#fxl` 존재 · `pointer-events:none` · z-index 60 · 1080×2280 | PASS |
| 1 | 레이어 한가운데 히트테스트가 아래 요소로 통과 (`#view`) — **입력 비차단** | PASS |
| 2 | 아이콘 8개 (스펙 3~8) | PASS |
| 2 | HUD 골드 알약 코인 아이콘 중심에 **0px** 도착 | PASS |
| 2 | 도착 순간 알약이 튄다 (`.fx-punch` 10프레임 관측) | PASS |
| 2 | 연출 길이 **691ms** (스펙 300~800ms) | PASS |
| 2 | `+n` 플로팅 텍스트 `+127K` | PASS |
| 3 | 숫자가 **중간 단계 23개**를 거쳐 오른다 (2.83K → 499M) | PASS |
| 3 | 최종 표시값 = 실제 보유량 | PASS |
| 4 | **소모(감소)에는 연출을 걸지 않는다** — 비행 0개 | PASS |
| 5 | 퀘스트 «보상 받기» → 다이아 실지급 300 → 390 | PASS |
| 5 | 체크 드로잉 · 버스트 10개 · 토스트 «퀘스트 완료» 생성 | PASS |
| 5 | 이어서 재화 비행 10개 (**보상 → 재화 획득 연결**) | PASS |
| 5 | 토스트가 스스로 사라진다 | PASS |
| 6 | 훈련 카드 클릭 → **실제 레벨 상승** 7 → 8 | PASS |
| 6 | 플래시가 카드 bbox 와 일치 (Δx 0 · Δw 0 · Δh 0) | PASS |
| 6 | 성공 파티클 10개 · 0.5초 안에 소멸 | PASS |
| 7 | DOM 파티클 상한 준수 — 최대 117개 (FXMAX 120) | PASS |
| 7 | 연출이 끝나면 레이어가 비워진다 (잔여 0) | PASS |
| 8 | **화면비 회귀** 1080×1920 · 1080×2520 에서도 알약에 0px 도착 | PASS |
| 9b | 강화 플래시가 클릭 후 **5ms** 에 보이기 시작(재렌더를 기다리지 않는다) | PASS |
| 9b | opacity≥0.7 로 **278ms** 유지 — 한 프레임 스치지 않는다 | PASS |
| 9b | 카드(rgb 255,253,242) 위 **최대 채널 변화 110** (≥45 여야 «플래시» 로 읽힌다) | PASS |
| 9 | 알약 최대 확대 **×1.16** (58: scale 1.15→1) | PASS |
| 9 | 숫자는 코인이 도착한 **뒤에** 오르기 시작 — 435ms | PASS |
| 9 | 롤링 완료 **738ms** (58: ≤800ms) | PASS |

`node tools/smoke.js` → **SMOKE PASS** (매 회차 확인) · `verify58.js` 는 2회 연속 동일 결과(결정적)

### 픽셀 확인 — `python3 pxcheck58.py <x0> <y0> <x1> <y1>` · `python3 pxdiff58.py r5-upg`

훈련 카드 영역(35,1371–361,1881) 프레임별 평균 RGB. 3·4·5번 프레임(156·240·331ms)에서
파랑 채널이 157 → 108 로 내려가며 Δ 55·60·44 — **연속 3프레임에 걸쳐 확실히 읽힌다.**
(4회차의 흰 플래시는 같은 자리에서 Δ 0.4 였다)

---

## 이 작업에서 나온 교훈 (LESSONS 로 옮길 것)

1. **연출 채점에 `page.screenshot()` 을 쓰면 회차가 통째로 날아간다.** 이 컨테이너에서 1080×2280
   한 장이 337~629ms 다. 스크립트에 «90ms 간격» 이라 적어도 실제는 430~720ms 라, 0.3~0.8초짜리
   연출이 8프레임 중 1~2장에만 걸린다. 비평가 2명이 독립적으로 «연출 없음 0점» 을 냈고 **틀린 것은
   구현이 아니라 캡처 절차**였다. → **CDP `Page.startScreencast`** 로 렌더 프레임을 타임스탬프째 받아
   원하는 시각에 가장 가까운 장을 고른다(±63ms 안에 들어온다). 04 교훈 1 의 연출 판.
2. **연출 캡처는 «게임을 멈추고» 찍어라.** `window.step = () => {}` 로 로직만 죽이면 `draw()`·`fxTick()`
   은 계속 돌아 연출은 그대로 보이면서 캔버스 노이즈(스킬 이펙트·자동 진행·경험치)가 사라진다.
   안 멈추면 비평가가 «정체불명의 회색 스우시가 HUD 반대 방향으로 표류한다» 같은 지적을 물고 온다
   (28 교훈 3 «캔버스가 스캔을 오염시킨다» 의 연출 판).
3. **«흰 플래시» 는 빠지는 곡선이 전부다.** opacity 를 완만히 빼면 30~40% 흰 워시 구간이 200ms 넘게
   남고, 사람은 그걸 «번쩍» 이 아니라 **«탈채도 = 비활성/잠김»** 으로 읽는다(비평가 2명이 독립적으로
   «회색이라 성공인데 실패처럼 보인다» 라고 했다). 세게 올리고 빨리 빼라.
4. **오버레이만 키우면 «커졌다» 로 안 읽힌다.** `#fxl` 오버레이에 `scale` 을 걸어도 **대상 카드의 bbox 는
   그대로**라 비평가가 «크기 변화 0%» 로 잡는다. 스케일 펄스는 **대상 요소 자체**에 걸어야 하고,
   `innerHTML` 재렌더 화면에서는 **재렌더 «뒤»** 에 붙여야 살아남는다(25 교훈 5 의 클래스 판).
5. **«도착하면 오른다» 는 순서를 코드로 못박아야 한다.** 재화가 늘어난 «즉시» 숫자를 굴리면 코인이
   도착하기 전에 카운터가 다 올라가 버려 인과가 뒤집힌다. 비행이 뜨는 순간 표시값을 **홀드**하고
   첫 도착에서 푼다(비행이 안 뜨는 경로를 위해 **만료 시각**을 같이 둘 것 — 안 두면 숫자가 영영 굳는다).
6. **동시 비행 상한은 «묶음당» 이 아니라 «공중에 떠 있는 총합» 으로 잡아라.** 골드·다이아가 같이
   들어오는 보상에서는 묶음이 둘이라 개수가 두 배가 되고, 그게 모달 헤더와 HUD 숫자를 가린다.

## 다음 세션에 넘기는 것

- 이 모듈은 **60(UI 쥬시니스)** 이 얹힐 자리다. `#fxl` 과 `fxPress/fxOpen/fxClose/fxShake` 를
  같은 파일 구역에 추가하면 된다 — 60 은 «누르는 손맛·팝업 열고 닫기» 라 대상 요소 자체에
  클래스를 걸어야 하므로 `#fxl` 오버레이 방식과는 다른 갈래다.
- 30 던전 입장 · 31 던전 클리어 화면이 생기면 `fxReward()` 한 줄만 부르면 재화 연출이 붙는다.
- **새 재화(유물석 `S.relic` 등)를 HUD 알약으로 올리면** `FXCUR` 에 한 줄 추가하는 것으로 편입된다.

---

# 14회차 (2026-08-25, sess-1120-5673 워커 B) — 핸드오프 ⓐ~ⓓ 구현

13회차 핸드오프의 지시 순서대로 잡았다. 캡처 `docs/review/58-r14-*.jpg` (14회차부터 **jpeg**).

## 구현 (커밋 «wip(58): 14회차»)

1. **ⓐ 카운트업 클램프** — 원인은 클램프 누락이 아니라 **트윈이 dt 누적**이라는 것이었다.
   `loop()` 가 dt 를 0.1s 로 클램프하므로 rAF 가 100ms 를 넘는 프레임(스크린캐스트 중 흔함)마다
   트윈이 벽시계보다 늘어져, 마지막 캡처 프레임에 목표치 1~2% 미달 숫자가 남았다.
   → `fxRoll` 을 **시작 시각 기준**(`p = (now−s)/FXROLL`)으로 바꿨다. 트윈은 이제 벽시계로
   정확히 320ms 에 끝나고 종료 프레임에서 `fxDisp = t` 로 못박힌다.
2. **ⓑ 어휘 통일** — `.fx-spark` 20→**26px**(잉크 + 검정링 3px), 키프레임에 62% 지점을 넣어
   수명 대부분을 scale ≥.92 로 유지(이전엔 중간 프레임이 12~16px 로 읽혔다).
   **upg 델타 플로터 신설**: `fxUpOk(target, at, txt)` 3번째 인자 + `fxDelta()` —
   훈련 카드가 이미 보여 주는 `.cv`(«+2.2»)·`.cn`(«공격력»)에서 문구를 만들어(`trDeltaTxt`)
   카드 상단 중앙에서 위로 띄운다. gain 의 `+n`(fxPlus)과 같은 40px·검정 스트로크.
   헤드리스 확인: 강화 클릭 120ms 뒤 `#fxl .fx-delta` = «+2.2 공격력» (198,1367).
3. **ⓒ 딤 복귀** — `FXSOLO` 760→**620ms**. 코인 전부 착지 ~530ms 이후이므로 인과 유지,
   복귀 구간이 8프레임 캡처(~700ms) 안에 들어온다.
4. **ⓓ 캡처 절차** — «166ms 동결» 의 원인을 확정했다: `pick()` 이 목표 시각마다 **독립적으로
   최근접 프레임**을 골라 같은 원본 프레임이 이웃 슬롯 2개에 중복 저장되고 있었다(원본 74ms
   간격 vs 목표 90~130ms 간격이면 필연). probe58f 실측에 정지 구간이 없던 것과 일치.
   → ① 슬롯을 시각 순서로 돌며 **아직 안 쓴 프레임에서만** 선택(단조 증가 보장),
   ② `format:'png'`→`'jpeg' q86` — PNG 인코딩이 전달을 74ms 로 묶고 있었다.
   결과: **원본 중앙 간격 74 → 27~32ms · 목표 대비 오차 최대 ±16~27ms** (씬 3개 모두).
   gain 27/80/201/311/421/563/685 · quest 9/99/181/291/404/566/693 · upg −15/114/202/324/418/542/706.

게이트: **VERIFY58 PASS**(전 항목 · 롤링 완료 671ms) · **SMOKE PASS** · 콘솔 에러 0.

### 14차 비평 (비평가 W·X) — **4점 · 3점** · ⓐⓑⓒⓓ 회수는 확인, 새 지적 5건

| 축 | W | X |
|---|---|---|
| ① 타이밍 | 6 | 4 |
| ② 크기 비율 | 6 | 7 |
| ③ 위치·경로 | 6 | 6 |
| ④ 가독성·일관성 | **4** | **3** |

**회수 확인(2인 공통)**: 카운트업 목표 도달(128K@685 · 1.09K@693) ✓ · 파티클 24~26px ✓ ·
연출 구간 프레임 동결 0건 ✓ (upg F1↔F2 동일은 둘 다 트리거 «이전» 기준 프레임 — 15회차에 −8ms 필터로 차단).

**새 지적과 처방(15회차에 반영)**:
1. **딤 복귀가 693ms 에도 0%** (W·X 공통, ① 최대 감점) — 620ms 시작 + .34s ease-in 은 캡처 끝에서
   5% 미만이라 안 보인다 → 해제 시작 FXSOLO 530 / 재렌더 FXHOLD 620 분리, 복귀 .3s ease-out.
2. **델타 플로터가 «Lv. 1» 타이틀을 210ms 가림** (공통, ④) — 시작점 카드 안(r.y−4) → 밖(−44px),
   34px·#FFDE6A 로 gain `+n` 과 어휘 통일, 상승 앞당김.
3. **수령 행이 60/60+활성 버튼으로 «재수령 가능» 모순** (공통, ④ 최대 감점) — **하네스 결함**:
   base=−1e9 라 다음 티어도 즉시 완료였다 → «정확히 한 티어 완료» 상태로 수정(수령 후 0/93 비활성).
4. **+400 골드 노출** (W ④ / X ①) — 결함 아님: 반복 퀘스트는 골드+다이아 동시 지급이 설계 확정
   (index.html 11711 주석 «보상 프레임은 다이아만 표시하지만 골드도 함께 지급»). `+n` 이 첫 도착에
   뜨는 것도 3회차에 확정된 설계. 15회차 비평부터 전달문에 사양으로 명시.
5. **10.0T↔10.00T 포맷 요동** (공통, ④) — fmt() 의 toFixed 반올림 승격(9.9999→"10.00") 경계 보정.
6. gain 플로터 하강 46px·수명 짧음(W) — 하강 24px·.8s 로. 스파크 완전 균일(X ②) — 20~30px 지터.
7. **도착 순간 프레임 공백**(공통 ③, 310↔420 슬롯 사이 110ms) — 캡처 절차 한계 → 슬롯 320/380 신설.

X 가 «아트 필요» 로 분류한 것: 버스트 하드엣지 단색 원 → 09·17 의 글로우·스타 텍스처(radial-gradient 로
절반 회수 가능, 스타형은 아트). 그 외 아트 필요 항목 없음(W: «신규 리소스 필요 항목 없음»).

## 15회차 — W·X 지적 반영 (커밋 «wip(58): 15회차»)

위 1·2·5·6 구현 + 하네스 3·7 + 비평 전달문에 4 명시. 게이트 **VERIFY58 PASS · SMOKE PASS**.
캡처 `58-r15-*.jpg`: gain 30/91/198/322/375/558/702 · quest 23/102/206/313/391/542/687 ·
upg 2/91/224/324/396/548/692 (원본 중앙 간격 28~36ms · 최대 오차 ±30ms). 비평가 Y·Z 채점 대기.

### 15차 비평 (비평가 Y·Z) — **4점 · 4점** (13회차 3·4 → 4·4)

| 축 | Y | Z |
|---|---|---|
| ① 타이밍 | 5 | 5 |
| ② 크기 비율 | 6 | 7 |
| ③ 위치·경로 | 5 | 6 |
| ④ 가독성·일관성 | **4** | **4** |

**회수 확인(2인 공통·독립)**: 딤 복귀 가시(687ms 에 23~26%) ✓ · 도착 순간 포착(알약 중심 5~8px,
직전 100px) ✓ · 알약 튐 ×1.16/×1.20 실측 ✓ · 10.0T 요동 소멸 ✓ · 스폰 앵커 42px(직전 +123/−158) ✓ ·
델타 플로터의 타이틀 가림 해소 ✓ · «+90/+400 첫 도착 동기» 인과 정상 판정 ✓.

**남은 것(2인 공통)과 16회차 처방**:
1. **목록 갱신이 687 프레임에 안 보임**(①④ 최대 감점) — 620ms 발화 + openQuest 재렌더 ~150ms
   페인트가 ~770ms 에 얹힘. 코드가 아니라 **관측 창 문제** → 캡처에 850ms «정산 프레임» 신설
   (0 슬롯 제거 — Y·Z 둘 다 «무반응 프레임»으로만 소모). 850ms DOM 프로브: `0/93·비활성·딤 0행` 확인.
2. **플로터 3종 687~702ms 페이드 0% = 수명 >0.8s**(공통 ①) — fxPlus .8→.6s(페이드 50%부터),
   델타 .82→.62s.
3. **델타 플로터가 x1 배수 탭을 368ms+ 가림 «+2.2 공격력1»**(공통 ③) — 카드 밖 −44px 앵커의 대가.
   → 앵커를 **카드 안 수치 행(.cv)** 으로: 델타가 나온 자리에서 떠오르니 인과도 서고 탭·타이틀 비가림.
4. **델타 등장이 수치 갱신보다 133ms+ 늦음**(Y ① 인과 역전) — 0% opacity 0→**.34** (fxCheckIn 방식).
5. 파티클: 크기 편차 12~29(끝 scale .38 이 원인)→끝 .5 · 산포 95~195→80~150(«±450px 드리프트», Z) ·
   **플랫 원 → radial-gradient 글로우 오브**(흰 코어→본색→투명; 09·17 톤의 CSS 근사 — 완전한 별형은 아트) ·
   upg 색 #FFE9A8→#FFC02E(크림-온-크림 저대비, Z).
6. 델타 색을 재화 골드와 분리(Z ④) — 스탯 초록 #B6FFC2.
7. **보류(의도적 미처리)**: «밝은 코인이 팝업 스크림 아래 딤된 알약에 도착»(Y·Z ④ −0.5) — #top(z6)을
   모달(z30) 위로 들어올려야 하는데, 다른 화면들의 통과 상태(딤이 HUD 를 덮는 현재 픽셀)를 흔든다.
   전 화면 일괄 판단이 필요해 이번 라운드에서는 안 건드림. 스컬 노드 33px 근접·다이아 알약 55px
   근접 경로(③ −0.5~1.5)도 제어점 재설계가 quest 행 딤 회피와 충돌해 보류.

## 16회차 — Y·Z 공통 지적 반영 (커밋 «wip(58): 16회차»)

위 1~6 구현. 게이트 **VERIFY58 PASS · SMOKE PASS** · 850ms DOM 프로브 PASS.
캡처 `58-r16-*.jpg`: gain 99/198/333/380/555/682/853 · quest 118/196/307/382/521/693/855 ·
upg 88/218/313/371/532/683/847 (원본 29~36ms · 오차 ±13~29ms). 비평가 AA·AB 채점 대기.

### 16차 비평 (비평가 AA·AB) — **7점 · 7.5점** (4·4 → 7·7.5, ③④ 는 통과권 진입)

| 축 | AA | AB |
|---|---|---|
| ① 타이밍 | 8 | 8 |
| ② 크기 비율 | **7** | **7.5** |
| ③ 위치·경로 | 9 | 9.5 |
| ④ 가독성·일관성 | 9 | 8.5 |

**회수 확인(2인 공통·독립)**: 정산 프레임 «0/93+비활성+모두받기 회색» ✓ · 딤 8번 완전 복귀 ✓ ·
플로터 소멸/페이드 ✓ · 델타 x1 탭·타이틀 비가림(여유 287/152px) ✓ · 갱신↔플로터 시차 ≤88ms ✓ ·
델타 스탯 초록 vs 재화 골드 분리(G−R 부호 반전) ✓ · 오브 산포 ±180px(직전 ±450) ✓.

**남은 것(2인 공통)과 17회차 처방**:
1. **알약 튐 8프레임 전부 0%**(② −2 공통 — 유일한 큰 감점). verify58 은 ×1.155 를 재므로 애니는
   돈다 — 범인은 fxPunch keyframe 의 `filter:brightness`(교훈 10: 래스터가 첫 페인트를 3~4프레임
   지연 → .3s 애니가 지워지기 전에 스케일 프레임이 안 찍힘). → filter 제거, 흰 outline 펄스 대체,
   .3→.42s. **캡처 픽셀 검증**: r17 gain 아이콘 잉크 span 55col(기준) → 63/59col(384/558ms) 확장 가시.
2. **딤 복귀가 693 프레임에 0% → 855 사이 스냅**(① −1.5~2 공통) — setTimeout+페인트가 ~170ms
   늦는 컨테이너 특성. FXSOLO 530→440(코인이 행 대역을 지나는 건 ~300ms 까지라 인과 무해),
   transition .3→.36s.
3. 오브 8/12개 20px 미달·카드 위 플랫(② −0.5~1) — radial 색대역 46/74→62/88%·24~34px·비행 중 scale 1.
4. 델타 첫 프레임이 «+2.4» 와 겹침(AB ④ −1) — 앵커 .cv−30px. 체크 판독 ~300ms(AB ① −0.5) — 드로잉 .18s.

## 17회차 — AA·AB 공통 지적 반영 (커밋 «wip(58): 17회차»)

위 1~4 구현. **VERIFY58 PASS(×1.164) · SMOKE PASS** · 캡처 `58-r17-*.jpg`
(gain 111/216/306/384/558/688/845 · quest 88/205/309/378/561/707/845 · upg 86/221/312/368/546/697/843).
비평가 AC·AD 채점 대기.

### 17차 비평 (비평가 AC·AD) — **7점 · 7점** · 게이트는 둘 다 ①

| 축 | AC | AD |
|---|---|---|
| ① 타이밍 | **7** | **7** |
| ② 크기 비율 | 9 | 9 |
| ③ 위치·경로 | 8.5 | 8 |
| ④ 가독성·일관성 | 8 | 8 |

**회수 확인(2인 공통·독립)**: 알약 튐 가시(AC ×1.13 피크+outline 휘도 644→1707 · AD ×1.145,
3프레임 지속) ✓ · 딤 복귀 램프(707ms 에 40~45% 진행, 스냅 아님) ✓ · 오브 24~34px+흰 코어 그라디언트
실측 ✓ · 델타 첫 프레임 겹침 0% ✓ · 정산 프레임 완전(0/93·비활성·딤 100%·잔여물 0) ✓.

**남은 것(공통)과 18회차 처방**:
1. **체크 판독 250~310ms**(① −1 공통, «205ms 스트로크 0%») — CSS 애니는 첫 페인트에서야 돌기
   시작하고 이 컨테이너의 첫 페인트가 ~120ms 늦다 → 드로잉 delay 를 **−0.08s** 로(첫 페인트에 이미
   44% 그려짐). 흰 스트로크로 교체(AC ④: green-on-green 저대비) + 첫 프레임 op .55/scale .72.
2. **딤 복귀 개시 실측 ~560ms — 설정 440 대비 +120ms 지연 상수**(① −0.5 공통) — FXSOLO 380 재보정.
3. **델타 민트색 ≠ 카드 스탯 초록**(④ −1 공통) — #8BE22C 로 일치. 에지 간격 5px(③ −1 공통 —
   «−30px» 가 글리프 높이만큼 먹혔다) → −58px 로 실간격 ~30px. 첫 프레임 op .34→.6(판독).
4. 오브 경계 밖 20~30px 스필(AD ③ −0.5) — 이동 거리를 요소 경계+40px 로 클램프.
   quest 버스트 시안→금색(AD ④ −0.5, 09·12·17 콘페티 톤).
5. **잔존 판정(회수 불가 분류)**: «클릭→첫 페인트 88~205ms 무반응»(① −1~1.5 공통) 은 컨테이너
   프레임 예산(~150ms, 실기기 1/4)이다 — index.html 「연출 시작 지연」 주석·13회차 결론과 동일.
   18회차 비평 전달문에 이 사실을 명시해 채점 조건을 정정했다(handoff 1번 항목의 «캡처를 실기기
   조건으로 보정하라» 에 해당하는 조치).

## 18회차 — AC·AD 공통 지적 반영 (커밋 «wip(58): 18회차»)

위 1~4 구현. **VERIFY58 PASS(45항목) · SMOKE PASS** · 캡처 `58-r18-*.jpg`
(gain 104/209/326/373/559/704/859 · quest 102/205/336/370/566/701/835 · upg 93/217/325/361/567/689/864).
비평가 AE·AF 채점 대기.

---

## 규칙 변경 — 저장소 주인 결정 (2026-08-26, 작업 93 으로 이관)

58 이 지켜 오던 **«연출 길이 0.3~0.8초 · 재화 아이콘 3~6개»** 는 비평가 8프레임 캡처(0~700ms)에
맞춘 값이었다. 주인 보고: «미션 완료 등으로 다이아가 위(HUD 알약)로 흡수되는 게 **너무 빨라서
찰진 느낌이 없다**. 다이아가 **더 많이** 나와야 할 것 같기도 하다.»

→ **UI 발(보상 수령·미션·우편·퀘스트) 한정으로 상향**한다. 새 규칙과 그 구현·실측은
`docs/review/93-재화흡수3박자.md` 가 단일 근거다. 요지만 옮기면:

- 개수 3~6 → **8~16**(골드·다이아 동시면 각자 8), `FXFLY_MAX` 12 → **32**
- «한 번에 날아감» → **퍼짐 0.22s → 머묾 0.12~0.20s → 흡수(ease-in)** 3박자
- 총 길이 0.3~0.8초 → **1.1~1.4초**(첫 도착 0.50s · 마지막 도착 1.22s)
- 알약 펄스 «한 번 크게(×1.17·420ms 고원)» → **여러 번 작게(×1.09·160ms 간격, ≥4회)**
- 숫자 롤링 0.32s → **첫 도착~마지막 도착(0.72s)**, `fxHold` 1400 → 2000
- `FXSOLO`/`FXHOLD` 380/620 → **1360/1360**(마지막 도착 + 100ms)
- 비평 캡처 창 8프레임(0~700ms) → **16프레임(0~1520ms)**, 하네스 `cap93.js`

**전투 발(킬 골드, 77 `#fxlc`)은 전 항목 현행 유지**다(주인 지시 ⑦ — 초당 수십 회라 느리면
화면이 코인으로 덮인다). 위 18회차까지의 «연출 길이 하한 300ms» 계열 지적은 UI 발에 한해
이 행이 대체하며, `verify58.js` 의 해당 검사도 93 규칙으로 갱신했다(갱신표는 93 리뷰 §4).

---

# 19회차 (2026-08-26, sess-0235-11597 워커 C) — 93 done 이후 재개

## 재개 조건이 풀렸다

18회차 캡처(`58-r18-*.jpg`)는 **93 이전 코드**라 채점해도 무효였고, 그래서 앞 세션(sess-0141-654)이
19회차를 스스로 반납했다. 그 반납 사유는 «93 이 `fxFly`/`FXFLY_MAX`/`FXSOLO`/`FXHOLD` 를 편집 중» 이었다.
**93 은 2026-08-26 02:26Z 에 done 처리(⏸ 보류, 10회 상한)되고 lock 도 반납됐다** — 조건 충족이라 재개했다.

착수 시점 게이트 기준선(코드 수정 전, 그대로 통과):
`VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS`.
※ `verify93.js` 의 «정지 프레임 ≤2%» 는 이 컨테이너 부하에 따라 2.4%/0.8%/1.2% 로 흔들린다 —
1회 FAIL 은 컨테이너 지터다(3회 중 2회 PASS 를 확인하고 넘어갔다).

## 캡처 절차를 고쳤다 — 13회차 핸드오프 ⓓ 처리

핸드오프 ⓓ 는 «헤드리스 실측에는 없는 **166ms 동결** 이 스크린캐스트 캡처에만 나타난다 — 캡처 절차를
재검하라» 였다. 원인은 **93 6회차가 이미 실측해 둔 것**과 같았고, 그 처방을 58 하네스로 옮겼다.

| 고친 것 | 전 | 후 | 근거 |
|---|---|---|---|
| 스크린캐스트 jpeg 품질 | q86 | **q55** | q86 은 이 컨테이너에서 인코딩이 rAF(20~30ms)를 못 따라가 **같은 합성 프레임이 두 번 실린다**. 게임이 멈춘 것이 아니다(rAF 기준 게이트 정지 0). |
| 캡처 창 | 7프레임 0~850ms | gain·quest **16프레임 95~1520ms**, upg 는 7프레임 유지 | 93 규칙(첫 도착 0.50s·마지막 1.22s)은 850ms 창에서 뒤 2/3 가 안 잡힌다. upg 는 93 이 손대지 않은 구간이라 옛 창(0.3~0.8s) 그대로. |
| 프레임별 정답표 | 없음 | **있음**(HUD 골드·다이아·비행 아이콘 수) | 93 5회차: 비평가 6명 중 3명이 프레임의 숫자를 잘못 읽어 **없는 결함**을 1순위 감점으로 올렸다. |

효과(같은 코드, 캡처만 바꿔 재수):

| | 원본 프레임 | 목표 대비 최대 오차 |
|---|---|---|
| q86 (첫 시도) | gain 77 · quest 56 · upg 59 | ±18 / ±25 / ±23 ms |
| **q55 (채택)** | **gain 101 · quest 67 · upg 66** | **±10 / ±12 / ±16 ms** |

## r19 실측 envelope (정답표 원본)

- **gain** — 스폰 −36ms · 아이콘 **16개** · 첫 도착 **479ms**(0 → 8.00A) · 마지막 도착 **1326ms** ·
  1427ms 에 잔여 0. 숫자 8.00A→24.0A→40.0A→56.0A→72.0A→88.0A→96.0A→112A→128A 로 9계단.
- **quest** — 스폰 **8ms** · 아이콘 **16개** · 첫 도착 **560ms**(1.00A → 1.01A) · 마지막 **1333ms** ·
  1426ms 에 잔여 0.
- **upg** — 비행 아이콘 0개(강화는 재화 «지출»). 골드 10.0D 고정.

세 씬 모두 93 규격(8~16개 · 첫 도착 0.50s · 마지막 1.22s · 총 1.1~1.4s) 안에 들어온다.

캡처 `58-r19-{gain,quest}-1..17.jpg` · `58-r19-upg-1..8.jpg`. 비평가 AE·AF 채점 대기.

### 19차 비평 (비평가 AE·AF) — **6점 · 5점**

| 축 | AE | AF |
|---|---|---|
| ① 타이밍 | **7** | **5** |
| ② 크기 비율 | 6 | 6 |
| ③ 위치·경로 | 6 | 6 |
| ④ 가독성·일관성 | 7 | 6 |

두 사람이 **독립적으로 같은 Δ** 를 낸 것만 20회차에 반영했다(05 교훈 3 — 한 사람만의 지적,
특히 둘이 정면충돌하는 지적은 반영하지 않는다).

**회수 확인(2인 공통)**: 첫 도착 479ms(규격 0.50s, 오차 −21ms) · 숫자 롤링 751ms 에 계단·동결
0프레임 · 아이콘 16개 · 프레임 이탈 0 · gain 3박자(퍼짐 t191 완료 → 머묾 180ms → 흡수)가
실제로 존재 · gain 착지 Δ17px / quest 착지 Δ0px.

**반영 4건 (2인 공통)**
1. **gain «+n» 플로터가 도착 코인 열에 관통당한다** (AE 5프레임 470ms → «+12◯A» ·
   AF 3프레임 280ms → «+1◯8A»). 두 사람의 실측 x 범위가 거의 일치했다(AE 536~650 / AF 539~647).
   → **10회차b 의 «좌측으로 96px 비킴» 이 방향을 거꾸로 잡은 것**이었다. 코인의 도착점은 알약
   «중심» 이 아니라 **왼쪽 끝의 아이콘**이다(골드 알약 x583 w211 → 아이콘 ≈583~638, 코인 수렴
   592~610). 중심 −96 = 592.5 는 정확히 코인 열 한복판이다. 아이콘이 왼쪽에 있으니 비켜야 할
   방향은 **오른쪽** → 중심 +52(코인 열과 80px 이격, 알약 폭 583~794 안). 자릿수가 길어질 때를
   위해 `offsetWidth` 로 프레임 클램프를 같이 넣었다.
2. **upg 델타 플로터가 카드 스탯 라벨과 같은 초록·같은 크기로 458ms 공존** (AE 300ms · AF 458ms).
   → ⚠ **값은 어느 쪽도 틀리지 않았다.** `.cv` 의 gain 은 `u.val(l+n) − u.val(l)` 이라
   «지금 누르면 오를 양» 이고, 누른 뒤 카드는 «다음에 누르면 오를 양» 으로 갱신된다. 즉 플로터
   +2.2 = 방금 얻은 값, 카드 +2.4 = 다음 값으로 **둘 다 맞다**(처음엔 «재렌더 전에 읽어서 값이
   낡았다» 로 오진했다가 `trainCardData()` 를 읽고 정정했다). 고칠 것은 값이 아니라 «같은 초록·
   같은 크기라 둘이 같은 종류로 읽히는 것» → 앰버 #FFD24A 로 분리(획득량=앰버, 카드 스탯=초록)
   + 상승 거리 50 → 104px, 페이드 62% → 46% 로 앞당겨 공존 구간 자체를 없앴다.
3. **HUD 알약 펄스 진폭 미달** (AE 아이콘 면적 2315→2600 = 선형 ×1.06 · AF 55×55→59×60, 다이아 ×1.05.
   규격 ×1.09) → `FX3_PZ_MAX` 0.105 → **0.15**. 설계값 ×1.105 가 화면에서 ×1.05~1.06 으로만
   나오는 것은 알약 «박스» 가 아니라 그 안의 «아이콘» 을 재기 때문에 스케일이 희석되기 때문이다.
   ※ **시상수는 건드리지 않았다** — 두 사람이 «반복이 보이느냐» 에서 갈렸고(AE 는 1.033/1.009/
   1.060/1.049/1.048/1.048/1.041/1.060 으로 진동 관측, AF 는 5프레임 동일값으로 «진동 없음»),
   10회차 주석의 결론(이 컨테이너 rAF 32~42ms 와 도착 간격 45ms 로는 «완전 복귀» 와 «캡처에
   잡힘» 이 동시 성립 불가)이 그대로다.
4. **패널 안 흡수가 화면 우측 끝에 붙는다** (AF 프레임 끝까지 23px · AE 글리프 우변 1065)
   → `FX3_OUTM` 176 → **46**. 176 은 이 저장소의 어떤 패널에서도 `FX3_XCAP`(=1040)에 먼저 걸려
   **상수로서 일한 적이 없었다**(#mbox 우변 974 + 176 = 1150 → 1040 클램프). 패널을 벗어나는 데
   필요한 것은 «아이콘 반경 27 + 여유» 뿐이다.

### 회수 불가로 분류한 것 (다음 세션이 다시 시도하지 않도록 — 근거를 남긴다)

1. **«오버슈트 ≤120px»**(AF ③ 처방 «제어점을 x≤900 으로») — **기하학적으로 불가능**하다.
   퀘스트 팝업(`#mbox`)이 x106~974 를 차지하고 목표 골드 알약은 x583 이다. 93 의 주인 지시는
   «궤적이 딤을 무시하고 패널 **밖**으로 나간다» 이므로, 패널을 벗어나려면 최소 x974 까지는
   가야 하고 그 순간 이미 목표에서 391px 오버슈트다. AF 가 제안한 x900 은 **패널 안**이다.
   두 요구는 동시에 성립하지 않는다 — 주인 결정 사항으로 남긴다.
2. **«퍼짐 채움률»**(AE·AF 공통 ②, gain 73~106% · quest 132~140%) — **반경으로는 회수 불가**.
   20회차에 실제로 `FX3_R0/R1` 106/184 → 134/238 로 올려 보고 게이트 2개가 순서대로 잡았다:
   - 패널 «안»: 형제 퀘스트 행 관통 **0회 → 29회**(`verify93` [2b], 지점 386~399 · y1177~1186 =
     행 [131,1176,949,1356]). 패널 안 부채꼴은 −19°~+38° 로 **아래로** 열려 있고(위는 윗 행이라
     못 연다, 10회차b) 바로 아래가 다음 퀘스트 행이다.
   - 패널 «밖»: 그럼 밖만 넓히면 되나 싶지만 `verify93` [1] 이 **퍼짐 최대 반경 206px** 로 잡는다
     (93 이 기록한 envelope 이 95~195px).
   두 비평가의 대안 처방이 마침 같았는데(AE «아이콘 52→38px», AF «52→40px»), 그건 **93 이 이미
   «비행용 코인을 Ø50→Ø32 소형 아트로 내려야 회수 가능» 으로 분류한 항목**이다.
   남의 게이트를 느슨하게 풀어 점수를 올리지 않고 원래 envelope 를 지켰다. → **아트 대기**.
   (`FX3_PR0/PR1` 는 그 실험에서 남은 «패널 안/밖» 분기다. 지금은 두 값이 같아 거동이 바뀌지
   않지만, 소형 아트가 들어와 반경을 다시 만질 때 안/밖을 따로 조일 수 있게 구조만 남겼다.)
3. **«비행 아이콘 52px 이 목적지 55px 의 94.5% 라 크다»**(AF #13) — **AE 는 정반대**로
   «52px 은 HUD 알약 아이콘 55px 과 정합, 유지할 것» 이라고 «잘 되어 있는 것» 에 적었다.
   05 교훈 3(두 사람이 정면충돌하면 반영하지 않는다) 그대로 미채택.
4. **«클릭→첫 페인트 88~205ms»** — 13·17회차와 동일. 컨테이너 프레임 예산(~150ms), 실기기 1/4.

## 20회차 — 위 4건 반영 (커밋 «wip(58): 20회차»)

게이트: **VERIFY58 PASS · VERIFY93 PASS · SMOKE PASS** · 회귀 VERIFY77 PASS · VERIFY22 65/65 ·
VERIFY69 99/99. 캡처 `58-r20-*.jpg`(gain 스폰 −18ms · quest 11ms, 목표 대비 최대 ±28ms).
비평가 AG·AH 채점 대기.

※ `verify93.js` 의 «정지 프레임 ≤2%» 는 이 컨테이너 부하에 따라 2.3~2.4% / 0.8% / 1.2% 로 흔들린다
— 다른 게이트와 **동시에** 돌리면 재현되므로, 1회 FAIL 은 지터로 보고 단독 재실행으로 확인한다.

### 20차 비평 (비평가 AG·AH) — **6점 · 6점** (19회차 6·5 → 6·6)

| 축 | AG | AH |
|---|---|---|
| ① 타이밍 | **8** | **7** |
| ② 크기 비율 | **8** | **8** |
| ③ 위치·경로 | 6 | 6 |
| ④ 가독성·일관성 | 7 | 7 |

**20회차 반영분의 회수 확인(2인 공통·독립)**
- **HUD 펄스 진폭 — 회수됨.** AH: 골드 아이콘 지름 55 기준 576ms 59 → 860ms **61(×1.109)** →
  1055ms 57 → 1132ms 60 → 1332ms 56 → 1422ms 55, «756ms 동안 4~5회 진동, 최대 ×1.11.
  **절대 «한 번 크게» 로 되돌리지 마라**». AG: «×1.07~1.11 = 규격 ×1.09 일치».
  19회차에 AF 가 «진동이 물리적으로 존재하지 않음» 으로 ① 을 5점까지 내렸던 항목이다.
- **① 이 5~7 → 7~8 로 올라왔다.** 3박자·총길이·첫/마지막 도착이 두 사람 모두 «규격에 맞는다» 로
  바뀌었다(AH: 퍼짐 0.20s · 머묾 0.19s · 총 1.32s).
- **② 가 6·6 → 8·8.** 두 사람 다 비행 코인 Ø50~52 와 HUD Ø55 의 비율을 «양호» 로 적었고,
  AG 는 HUD 접근 시 Ø42~33 로 줄어드는 원근 테이퍼를 «규격에 없는 좋은 디테일» 로 꼽았다.
- **프레임 이탈 0 · 도착 정확도** 는 두 사람 다 «잘 되어 있는 것» 에 넣었다(gain x583±10 = 알약
  아이콘 중심, quest 는 골드·다이아가 각자 알약으로 분리 도착).

### 20회차 뒤 «버그» 1건을 더 잡았다 — 토스트 퇴장 순간이동

**비평가 3명이 라운드를 건너 독립적으로 같은 값을 쟀다**: AE(19회차) 중심 x 539 → 642 = **+103px** ·
AG(20회차) 539.5 → 651.5 = **+112px** · AH(20회차) 좌변 428 → 540 = **+112px**(이후 207ms 동안
−46px 역주행). 19회차엔 AE 한 사람뿐이라 «2인 공통» 규칙에 걸려 반영하지 않았는데, 20회차에
두 사람이 더 같은 것을 쟀다.

원인은 연출 취향이 아니라 **CSS 함정**이었다. `@keyframes fxToastOut{to{…}}` 처럼 `to` 만 쓰면
암묵적 0% 는 «그 애니메이션을 뺀 계산값» 이고, `.fx-toast` 규칙에는 transform 선언이 없으므로
그 값은 **`transform:none`** 이다 — 앞의 `fxToastIn … both` 가 들고 있던 `translate(-50%,0)` 이
**아니다**(`.out` 이 붙는 순간 `animation` 이 통째로 교체돼 그 fill 이 사라진다).
그래서 퇴장 첫 프레임에 −50% 가 빠지며 토스트가 **자기 박스 폭의 절반**만큼 오른쪽으로
순간이동한 뒤 되돌아온다. 박스 폭 = 텍스트 144 + 좌우 패딩 68 + 테두리 8 = **220 → 절반 110px**
로 세 실측(103·112·112)과 맞는다. → 0% 를 명시해 −50% 를 유지시켰다.

**재현·회수 게이트 신설 `probe58j.js`** — 수정 전 **111.9px 이탈 → FAIL**, 수정 후 **0.0px → PASS**.
(r20 캡처는 이 수정 «이전» 이므로, AG #1 · AH #2 는 현 HEAD 에서는 이미 없는 결함이다.)

## ⏸ 보류 판정 (2026-08-26, sess-0235-11597 워커 C) — 2차 라운드 20회 상한 도달

회차: 1차 10회 + 2차 10회 = **20/20 소진**. ①~④ 최저 **6**(AG 6 / AH 6). 통과선 8 미달.
게이트는 전부 통과 — `VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS` · `PROBE58J PASS` ·
회귀 `VERIFY77 PASS` · `VERIFY22 65/65` · `VERIFY69 99/99`.

### 남은 문제 — ③ 6 / ④ 7 이 병목이고, 대부분 «가림» 한 갈래다

두 사람의 ③ 감점은 **전부 «연출이 글자를 가린다»** 였다(경로·도착 정확도 자체는 둘 다 «좋음»).

1. **quest 버스트 링이 클릭한 버튼보다 크다** (AG ②·#3 · AH #6, 2인 공통) — 링 세로 175px vs
   버튼 높이 94~104px = **170~186%** → «보상 받기» 6글자 중 2.5자만 남는다. t=106~205ms.
   처방(둘 다): 링 초기 반경 47 → 30px, 또는 opacity 를 낮춰 라벨을 통과시킨다.
2. **quest 재화 뭉치가 자기 행의 제목·게이지를 가린다** (AG #2 · AH #3, 2인 공통) —
   «적 60마리 처치» 가 «…처치» 만 남고 60/60 바 좌측 40% 가 가려진다. 170~354ms.
   ※ 이건 «채움률 면제» 에 해당하지 않는다 — 아이콘끼리의 겹침이 아니라 **UI 라벨 가림**이다.
   처방(AH): 패널 안 부채꼴에서 «왼쪽으로 나가는 벡터» 를 죽인다. 어차피 목표는 위쪽 HUD 다.
   ⚠ 단 이건 `FX3_PA0/PA1`(−19°~+38°) 을 건드리는 것이고, 그 각도는 10회차b 가 «위로 열면 윗 행
   위» 라 아래로 연 값이다. **위·아래 양쪽에 행이 있는 구조**라 각도만으로 풀리는지 먼저 재야 한다.
3. **gain 흡수 경로가 상단 STAGE 진행바를 관통** (AG #4 · AH #7, 2인 공통) — 코인 궤도 x616~660
   고정, 트랙 x325~798·y234~300 → 378~560ms 가림. 목표 알약이 바 «위» 라 교차 자체는 불가피.
   처방: AH «제어점을 x850 으로 밀어 바 오른쪽 끝 바깥에서 진입» · AG «±40px 스태거 + 그 구간만
   opacity .75». **AG 안이 싸다**(경로 재설계 없이 겹침 시간만 분산).
4. **quest 첫 199~299ms 동안 «무엇을 얻었는지» 가 화면에 없다** (AG #3 · AH #5, 2인 공통) —
   크림색 구슬 12~16개가 먼저 뜨고 재화 아이콘은 205~299ms 에야 등장. 처방(둘 다): 구슬 수명을
   120ms 이내로 줄이거나 재화 아이콘 스폰을 −100ms 앞당겨 같이 뜨게 한다.
5. **upg 「+2.2」 와 카드 「+2.4」 동시 표시** (AG #6 · AH #4, 2인 공통, 19회차부터 3라운드 연속) —
   ⚠ **두 사람 다 «같은 초록» 이라고 적었지만 그건 오독이다.** 20회차에 이미 앰버로 분리했고
   실측 `getComputedStyle(...).color` = **`rgb(255, 210, 74)`(#FFD24A)** 다. 즉 색은 이미 갈라져
   있고, 남은 진짜 문제는 **«서로 다른 두 수치가 동시에 보이는 것» 자체**다.
   (LESSONS 121-⑫ «두 비평가의 독립 합의도 원인 진단은 같이 틀릴 수 있다 — 증상 합의 ≠ 원인 합의»
    의 교과서적 사례다. 증상은 실재하고 원인 진단만 틀렸다.)
   → 다음 라운드의 처방은 색이 아니라 **문구**다: 두 사람 다 «실제 증가분 «+0.2» 를 띄워라» 로
   같은 것을 제안했다. `.cv` 의 gain 이 «지금 누르면 오를 양» 이라 **증가분은 «다음 gain − 지금
   gain»** 이고, `fxUpOk` 를 재렌더 «전» 에 쏘는 제약(연출 시작 ~150ms 지연) 때문에 **값만
   재렌더 뒤에 따로 넣어야** 한다. 12회차 실측상 `renderTrainLive()` 는 0.9ms 라 체감 손실은 없다.

### 회수 불가로 남기는 것 (19회차 §«회수 불가» 와 같음 — 아트/주인 결정 대기)

- **퍼짐 채움률** → 반경으로 회수 불가(패널 안은 행 관통, 패널 밖은 93 envelope 95~195px).
  **소형 아트 Ø32 대기** — 93 이 이미 같은 결론으로 분류해 둔 항목이다.
- **패널 안 오버슈트** → 팝업 x106~974 · 목표 x583 이라 «패널 밖으로 나간다»(주인 지시)와
  «오버슈트 ≤120px» 은 동시 성립 불가. **주인 결정 사항.**
- **클릭→첫 페인트 88~205ms** → 컨테이너 프레임 예산(~150ms), 실기기 1/4.
- **비행 아이콘 크기** → 19회차에 AE·AF 가 정면충돌했고, 20회차엔 AG·AH 둘 다 «양호» 로
  적었다(AG «Ø50 규격 정확», AH «HUD Ø55 대비 자연스럽다»). **지금 값이 정답이다 — 건드리지 마라.**

### 다음 라운드(3차 폴리시, +10회 리셋 시)가 잡을 순서

위 «남은 문제» 1 → 4 → 3 → 5 → 2 순서를 권한다. 1·4 는 quest 한 화면의 초반 300ms 에 몰려 있어
같이 잡히고(둘 다 ③④ 동시 회복), 3 은 AG 안이면 상수 2개, 5 는 문구 1줄이다. 2 는 `FX3_PA0/PA1`
을 건드려야 해 회귀 위험이 가장 크므로 마지막이다.

---

# 3차 폴리시 라운드 (회차 상한 +10 → 21~30)

# 21회차 (2026-08-26, sess-0541-7228 워커 C) — 20회차 핸드오프 1·4·3·5

착수 기준선(코드 수정 전, 그대로 통과): `VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS` ·
`PROBE58J PASS`. 20회차 §«다음 라운드가 잡을 순서» 의 **1 → 4 → 3 → 5** 를 그대로 따랐다
(2 는 `FX3_PA0/PA1` 를 건드려야 해 회귀 위험이 가장 크므로 마지막 — 22회차로 넘긴다).

## 준비 — 컨테이너 playwright 가 브라우저를 못 찾는다

`npm i --no-save playwright` 가 **1.62.1** 을 깔고, 그 버전은 chromium **1234** 를 찾는데
이 컨테이너에 미리 깔린 것은 **1194** 다. `npx playwright install chromium` 은 프록시가
`cdn.playwright.dev` 를 403 으로 막아 통째로 실패한다.
→ `playwright-core@<v>` 의 `browsers.json` 을 받아 revision 을 역조회해서 **1194 를 쓰는
버전(1.56.0)** 을 깔면 `/opt/pw-browsers/chromium-1194` 를 그대로 쓴다.
(1.54=1181 · 1.55=1187 · **1.56=1194** · 1.57=1200 · 1.62=1234)
다음 세션이 같은 데서 막히지 말라고 남긴다 — `npm i --no-save playwright@1.56.0`.

## 신설 도구 `probe58k.js` — 핸들 4개를 **수정 전에 먼저 쟀다**

비평가가 JPEG 에서 잰 값을 DOM 좌표로 다시 재서 «처방 전/후» 를 숫자로 비교할 수 있게 했다.

| 항목 | 20회차(수정 전) 실측 | 21회차(수정 후) 실측 |
|---|---|---|
| quest 버튼 | 197×119 · 라벨 잉크 126×47 | (같음) |
| 버스트 링 세로 / 버튼 높이 | t6~111ms **102%** → t578ms **197%** | t6~121ms **134%** → t345ms **150%** |
| 라벨 잉크와 겹친 파티클 | **10개 중 7개**(0~110ms), 6개(218ms) | **0개**(전 구간) |
| quest 아이콘 실측 크기 | 87ms **33px** / 132ms 40px (기준 46) | 97ms **40px** / 151ms **46px** |
| 크림 구슬 소멸 | 620ms | **260ms**(제거 300ms) |
| gain 바 관통 표본 | 18 | 18(불투명도 **0.75** 적용 확인) |

## ① 버스트 링이 라벨 «위에서» 태어난다 — 원인은 원을 쓴 것이다

20회차 AG #3 · AH #6 (2인 공통): «링 세로 175px vs 버튼 높이 94~104 = 170~186% →
«보상 받기» 6글자 중 2.5자만 남는다». 처방은 «링 초기 반경 47 → 30» 이었는데 **그건 증상을
악화시킨다** — 반경을 줄이면 파티클이 라벨 «더 안쪽» 에서 태어난다.

진짜 원인은 산수다. `rr = min(w,h)/2 × 0.78` 은 **원**이라, 대상이 정사각형이 아닌 순간
긴 축에서는 bbox **안쪽**으로 들어온다. 퀘스트 버튼 197×119 → 링 122×121 이고 라벨 잉크는
126×47 이므로 링이 라벨을 통째로 품는다. 같은 산수가 이미 두 번 등재돼 있었다:

- 92 우편 «읽음 전체 삭제» 3회차 E-②·F-② — «시작 45.6 < 반높이 58.5» → **버스트 자체를 폐기**.
- 92 우편 행(784×147) 3회차 D-② — 세로 확산 127px 가 반높이 73.5 를 넘겨 이웃 행에 얹힘.
  둘 다 «`fxBurst` 는 58 공용 모듈이라 여기서 못 고친다» 로 남겨져 있었다 — **여기가 그 여기다.**

→ 원을 버리고 **대상 형상**을 쓴다. 타원 반축 = bbox 반축 + `FXB_M`(6), 바깥 이동도 축마다
반축 × `FXB_K`(0.20). 스칼라 이동(`d`)을 쓰면 둘레에서 출발해도 이동이 원이라 종횡비가 다시
무너지므로 이동도 축별이다. 개체차는 반축에 `rnd(.82,1.18)` 로 준다(균일 링은 «판때기»).
좌표 대상(점)은 종전대로 반경 22 + 80~150px 산포 — 분기해 뒀다.
넓은 대상(우편 행 826px)에서 타원이 프레임을 넘길 수 있어 `left` 를 20~1060 으로 물렸다.

## ④ 첫 300ms 에 «무엇을 얻었는지» 가 없다 — 아이콘이 늦은 게 아니라 **작았다**

20회차 AG #3 · AH #5: «크림색 구슬 12~16개가 먼저 뜨고 재화 아이콘은 205~299ms 에야 등장».
실측하면 아이콘은 **47ms 부터 16개 전부 있다**. 문제는 퍼짐 시작 스케일 `0.45` 가 fs46 아이콘을
**33px** 로 만들어, 26px 크림 구슬(흰 코어 + 검정 링)과 **같은 어휘**로 뭉친 것이다. 비평가가
«구슬 12~16개» 라고 센 수가 구슬 10개가 아니라 **비행 아이콘 16개**였다.

→ 두 손잡이를 같이 쓴다.
- 구슬 수명 `.58s` → **`.26s`**(제거 620 → 300ms). 95ms 캡처 리듬에서 95·190ms 두 프레임에
  온전히 남으므로 «버스트 없음»(무조건 0점)이 되지 않는다. `verify58` 은 퀘스트 120ms ·
  강화 60ms 에 파티클 수를 재므로 게이트도 안전하다.
- 퍼짐 시작 스케일 `0.45 + 0.25e` → **`0.58 + 0.12e`**. **끝값 0.70 은 한 픽셀도 안 바꿨다** —
  퍼짐·머묾의 최대 은폐 면적이 안 커지므로 아직 안 잡은 «남은 문제 2»(행 라벨 가림)와 충돌하지 않는다.

## ③ gain 흡수가 STAGE 진행바를 관통 — AG 안 채택

20회차 AG #4 · AH #7. 목표 알약이 바 «위» 라 교차 자체는 기하학적으로 불가피하다(실측
`#stinfo` x320~800 · y240~332, 코인 수렴 x570). AH 안(제어점 x850)은 경로 재설계라 도착
정확도(Δ17px — 두 사람이 «잘 되어 있는 것» 에 넣은 항목)를 다시 흔든다. **AG 안이 싸다.**

- `FX3_BARSTG` 40 — ±40px 스태거. `sin(πe)` 이라 **출발·도착 좌표는 한 픽셀도 안 움직인다**.
  `i` 로 층화해 16개가 −40 → +40 을 고르게 채운다(랜덤이면 다시 한 줄로 몰린다).
- `FX3_BARDIM` 0.75 — 바 밴드와 겹치는 **프레임에서만** 불투명도를 내린다. 밴드는 스폰 때
  한 번 스캔해 캐시(`fx3Band()`)하고 프레임마다 좌표 비교만 한다.
- ⚠ 스태거는 **패널 없는 2차 호 구간에만** 건다. 패널 안 두 구간 경로는 `verify93` [2b]
  «형제 행 관통 0» 이 걸려 있어 x 를 흔들면 그 게이트가 먼저 깨진다.

## ⑤ 플로터 «+2.2» 와 카드 «+2.4» 동시 표시 — 처방을 **안 따랐다**

19·20회차 연속 2인 공통. 두 사람의 처방은 «실제 증가분 «+0.2» 를 띄워라» 인데 **그건 거짓말**이다:
플레이어가 방금 얻은 것은 0.2 가 아니라 **2.2** 다(19회차에 `trainCardData()` 를 읽고 이미
«두 값 다 맞다» 로 정정한 오진이다. 0.2 는 «다음 gain − 지금 gain» 일 뿐 얻은 스탯이 아니다).
20회차가 색을 앰버로 갈랐는데도 남았고, 두 사람이 그때 «같은 초록» 이라고 적은 것은 오독이었다
(실측 `#FFD24A`) — 즉 **색은 원인이 아니었다**. 남은 진짜 문제는 두 사람이 적은 그대로
«서로 다른 두 수치가 같은 순간에 보이는 것» 자체다.

→ 값을 바꾸지 않고 «동시» 만 없앤다. 플로터가 떠 있는 동안 그 값이 나온 자리(`.cv`)를
`fx-cvswap`(0.46s, op .12 → 1)으로 비웠다가 새 값으로 되채운다. 플로터가 **바로 그 자리에서**
떠오르므로 «이 줄의 값이 갱신되는 중» 으로 읽힌다.
실측: t0~300ms 카드 `.cv` op **0.12** · 플로터 «+2.2 공격력» 표시 → t420ms op 1 · 카드 «+2.4».
어느 순간에도 두 수치가 같이 온전히 보이지 않고, **거짓 수치도 없다.**

## 게이트

`VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS` · `PROBE58J PASS(0.0px)` ·
회귀 `VERIFY77 PASS` · `VERIFY22 65/65` · `VERIFY69 99/99` · `VERIFY64 41/41`.
캡처 `58-r21-{gain,quest}-1..17.jpg` · `58-r21-upg-1..8.jpg`
(목표 대비 최대 ±21 / ±26 / ±39ms · 원본 48 / 36 / 39 프레임). 비평가 AI·AJ 채점 대기.

## 남은 문제 2 («재화 뭉치가 자기 행의 제목·게이지를 가린다») — 22회차용 기하 조사

20회차는 이 항목을 «회귀 위험이 가장 크므로 마지막» 으로 미뤘고, 처방은 AH 의 «패널 안
부채꼴에서 왼쪽으로 나가는 벡터를 죽인다» 였다. **그 처방은 성립하지 않는다** — `FX3_PA0/PA1`
는 −19°~+38° 라 `cos` 가 전 구간 양수, 즉 **왼쪽으로 나가는 벡터는 애초에 하나도 없다.**

실제 원인은 각도가 아니라 **출발점의 위치**다. 22 퀘스트 행의 CSS 실측:

| 요소 | 행 기준 좌표 |
|---|---|
| 행 `.qs-r` | 818×179 (아래 margin 21 → 피치 **200**) |
| 보상 배지 `.qs-i` (= 재화 출발점) | `left:36 · top:37` — **행의 왼쪽 끝** |
| 제목 `.qs-t` | `left:168 · top:44` |
| 게이지 `.qs-tg` | `left:42 · bottom:43 · 797×99` |

배지 중심은 행 기준 x≈90 이고, 퍼짐 반경은 `FX3_PR0/PR1` = **106~184**. 각도 0° 로 나가면
끝점이 x≈196~274 — `.qs-t` 의 시작 x168 **바로 위**다. 비평가가 «적 60마리 처치» 가 «…처치»
만 남는다고 잰 것이 이것이고, 아래로 열린 +38° 성분이 `.qs-tg` 게이지 좌측을 먹는다.

**네 방향이 전부 막혀 있다** (배지 중심 기준, 반경 95~195 안에서):
- 오른쪽 → 이 행의 제목·게이지 (위 산수)
- 위 → 피치 200 이라 y−95~−195 는 **윗 행** (10회차b 가 이미 기각한 방향)
- 아래 → 같은 이유로 **아랫 행** (19회차에 R 을 키웠다가 `verify93` [2b] 가 관통 29회로 잡았다)
- 반경 축소 → **`verify93` [1] 이 «퍼짐 최대 반경 95~195px» 로 양쪽을 다 물고 있다**(주인이
  검증한 93 envelope). 아래로 내리면 그 게이트가 먼저 깨진다.

→ 남은 손잡이는 둘뿐이고, 둘 다 «각도 조정» 이 아니다.
1. **아이콘을 작게** — 19·20회차가 이미 «Ø50 → Ø32 소형 아트 대기» 로 분류한 항목과 같은 벽이다.
2. **퍼짐 출발점을 배지에서 행 우측으로 옮긴다** — 행 우변 854 ~ 패널 우변 974 사이 120px 의
   빈 공간으로 열 수 있다. 다만 12회차가 «손맛은 손가락 밑에, 재화는 보상 자리에서» 로 정하고
   비평가 Q·R 이 승인한 인과를 깨는 변경이라, **22회차 채점 결과를 보고 결정한다**
   (③ 이 이 항목 없이 8 에 오면 건드리지 않는다).

### 21차 비평 (비평가 AJ) — **4점** (① 5 / ② 5 / ③ 4 / ④ 4)

20회차 AG·AH 가 6·6 이었으므로 **점수만 보면 하락**이다. 다만 AJ 는 앞 회차 비평가들이
«잘 되어 있는 것» 이나 «회수 불가» 로 분류한 항목까지 전부 새로 감점했다(예: 감점 4순위
«퍼짐 가시율 12.3%» 는 19·20회차가 이미 **아트 대기**로 분류를 끝낸 항목이다).
2인 공통 규칙(05 교훈 3)대로 **AK 와 겹치는 것만** 22회차에 반영한다.

**회수 확인(AJ 실측)**
- **확인항목 1 = 회수됨.** f3/f4/f5 의 구슬 10개 좌표를 전부 라벨 사각(x759-879 · y1043-1072)과
  대조해 **교차 0건**. 링 세로 = 버튼 높이의 **132% → 139% → 145%**, «테두리 바깥으로만 퍼진다».
  probe58k 의 DOM 실측(겹침 0 · 134~150%)과 **독립적으로 일치**한다.
- 확인항목 2 «구별 자체는 됨» — 구슬은 크림 원 12~32px, 다이아는 시안 각진 보석 21×27px,
  코인은 금색 51×51px 로 «색·형태·크기가 달라 혼동은 없다». 구슬 소멸도 **≈430ms 에 검출 0개**.
  (다만 «미회수» 판정 — 사유는 구슬이 아니라 **첫 페인트 지연**이다. 아래.)
- gain 도착 정확도 **7px**(아이콘 반경 27 의 13%) · 도착 계단 **16/16 정확** · 도착창 720ms 가
  규격 창폭 720ms 와 정확히 일치 · quest 알약 톡톡 ×1.077/×1.107 로 규격 ×1.09 명중.

**AJ 가 새로 짚은 것 (AK 와 겹치는지 확인해야 반영)**
1. **gain HUD 금 알약 캡슐이 f12(1031ms) 에 ×1.25** — 캡슐 세로 48 → 60px, 숫자 캡높이 26 → 34.
   금 알약 우단 x793 과 다이아 좌단 x820 사이 **여백 26px 이 0 이 되어 두 칩이 붙는다.**
   ⚠ 이 배율 자체는 **설계값이다** — `FX3_PZ_MAX` 0.22 는 20회차가 «아이콘 실측은 선언값보다
   희석되므로» 로 올린 값이고, AG·AH 는 **아이콘**을 재서 ×1.07~1.11 «규격 일치» 로 통과시켰다.
   AJ 는 **캡슐 박스**를 쟀다. 즉 새 사실은 배율이 아니라 «캡슐 기준으로는 26px 여백을 먹는다» 다.
2. **quest ✓ 배지(Ø88 = 버튼 높이 74%)가 라벨을 66~73% 가린다** (t=295~737ms, 약 440ms).
   → **가림의 주체가 버스트에서 `fxCheck` 로 옮겨간 것**이지 사라진 게 아니다. 1순위 후보.
3. **quest 버튼이 수령 후에도 1430ms 까지 초록**(y1100-1112 평균색 (119,205,74) 유지 →
   f17 에 (163,163,163) 로 **1프레임 하드컷**). 보상은 이미 나는데 «아직 누를 수 있는» 상태로 보인다.
4. **upg — 내 이번 회차 수정(`fx-cvswap`)이 역효과라는 지적.** f4~f5(약 170ms) 동안 카드 수치 행
   동적범위 96/90(배경 대비 ≤12%)이라 «화면에서 읽히는 유일한 숫자가 플로터 «+2.2»» 다.
   AJ 는 그것을 «틀린 값» 이라고 적었지만 **+2.2 는 방금 얻은 값이라 틀리지 않았다**(19회차 정정분).
   그래도 «카드 값이 사라진 구간이 생겼다» 는 사실은 내 변경의 직접 결과이므로 검토 대상이다.
5. **quest 토스트가 상단 «STAGE 1» 라벨 높이 27px 중 15px(56%)를 1.0초 덮는다**(토스트 bbox
   x428-651 · y149-239 vs 라벨 x464-613 · y224-250).
6. **트리거 직후 무반응** — quest f1(t−92)과 f2(t74)가 **md5 동일**, upg f2(61ms)는 카드 영역
   변화 0px. → 첫 도착이 737ms(규격 500 대비 +47%)로 밀린다.
   ⚠ 이건 13·17·19·20회차가 네 번 연속 «컨테이너 프레임 예산(~150ms), 실기기 1/4» 로 분류한 항목이다.

**AJ 가 제기한 하네스 결함 — «정답표가 실제 프레임보다 1프레임 앞선다»**
AJ 실측 f6(476)=0 · f7(560)=8.00A · f8(647)=24.0A vs 정답표 476:8.00A · 560:24.0A · 647:32.0A.
`cap58.js` 의 `at(ms)` 는 rAF 폴링 표본 중 **그 시각에 가장 가까운** 것을 고르는데, 스크린캐스트
프레임이 보여 주는 것은 그 시각 «직전에 이미 합성된» 화면이라 표가 한 페인트 앞설 수 있다.
다만 AJ 의 f9(770)=40.0A 는 1프레임 시프트(=32.0A)와도 안 맞아 **일부는 JPEG 오독일 수 있다.**
→ **AK 가 독립적으로 같은 어긋남을 보고하면 확정**하고 `at(ms)` 를 «ms 이하 마지막 표본» 으로
고친다. 정답표는 오독을 막으려고 만든 장치라, 그게 틀리면 없느니만 못하다.

## 22회차 (2026-08-26, sess-0912-30020 워커 A) — 죽은 lock 회수 후 이어받기

lock `2026-08-26T06:46:46Z` · 마지막 `wip(58)` 커밋 `06:46:48Z` — 둘 다 **147분**이라 90분 규칙으로
회수했다. 21회차는 «캡처 완료 + AJ 채점 완료 · AK 채점 대기» 상태로 죽어 있었다.
**처음부터 다시 돌리지 않고** 21회차 캡처(`58-r21-*.jpg`)에 AK 를 붙이는 데서 이어받았다.

착수 기준선(코드 수정 전): `VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS`.
컨테이너 준비는 21회차가 남긴 대로 `npm i --no-save playwright@1.56.0` 한 줄로 끝났다
(1.62 는 chromium 1234 를 찾는데 이 컨테이너엔 1194 만 있다 — 그 기록이 그대로 맞았다).

### 신설 도구 `probe58l.js` — 21차 비평의 «가림» 4건을 DOM 으로 다시 쟀다

비평가가 JPEG 에서 잰 값을 DOM 좌표로 재현해 처방 전/후를 숫자로 비교한다(probe58k 와 같은 취지).

| AJ 지적 | AJ 실측(JPEG) | probe58l 실측(DOM) | 판정 |
|---|---|---|---|
| #2 quest ✓ 배지가 라벨을 가림 | 66~73% · 440ms | **최대 80%(t198) · 73.6% 를 t300~723** | **확정 — AJ 보다 심하다** |
| #1 금 알약 캡슐 ×1.25 로 다이아와 붙음 | 여백 26 → 0px | 정지 여백 **49px** · ×1.22(설계 최대)에서 **3px** · ×1.25 에서 −4px | **확정**(설계 최대값에서 사실상 맞닿는다) |
| #5 토스트가 «STAGE» 라벨을 덮음 | 15px · 라벨 높이의 56% · 1.0초 | 토스트 하단 **229** vs `#chapN` 상단 **227** = **2px** | **과대평가** — 겹치기는 하나 2px다 |
| #3 수령 뒤 버튼이 계속 초록 | 1430ms 까지 초록 → 1프레임 하드컷 | 초록은 `.ifbtn` 배경 그라디언트(CSS 변수)라 버튼 `background-color` 로는 안 잡힌다 | 측정 방법 보완 필요 |

### 반영 1 — ✓ 체크 원판을 «중심» 에서 «우상단 모서리 도장» 으로 (AJ #2)

**21회차 링 수정의 직접적 귀결이다.** 21회차가 버스트 링을 원 → 타원으로 고쳐 라벨 겹침을
7개 → 0개로 만들었더니, **가림의 주체가 `fxBurst` 에서 `fxCheck` 로 옮겨간 것**을 AJ 가 잡았다.
원인은 21회차 링과 **같은 산수**다 — Ø92 원판을 대상 **중심**에 놓으면, 대상이 «가로로 긴 버튼»
(195×118)인 순간 라벨 잉크(125×47)를 통째로 삼킨다.

→ 중심을 bbox **우상단 꼭짓점**으로 옮기고, 크기도 대상에 종속시켰다
(`FXC_K 0.78 · FXC_MIN 56 · FXC_MAX 92`, 프레임 밖으로는 안 나가게 클램프).
좌표 대상(점)은 붙일 bbox 가 없으므로 종전대로 그 점을 중심에 쓴다 — 분기해 뒀다.

| | 수정 전 | 수정 후 |
|---|---|---|
| 라벨 잉크 가림 **최대** | **80.0%** (t198ms) | **4.3%** (t197ms) |
| 라벨 잉크 가림 **지속** | **73.6%** · t300~723ms | **2.4%** · t345~900ms |
| 원판 위치 | 중심 (774,1018) | 우상단 (872,959) |

새 자리가 다른 것을 덮지 않는 것도 실측으로 확인했다 — 도장 bbox `x872~964 · y955~1047` 는
버튼 우변 919 ~ 행 우변 951 ~ 패널 우변 974 사이의 **빈 띠**이고, 위로는 행 간 여백(피치 200 −
행 높이 180 = 20px) 안에 들어가 윗 행 게이지(그 행 하단 −43px = y913)에 닿지 않는다.

게이트: `VERIFY58 PASS` · `SMOKE PASS` · 회귀 `VERIFY22 65/65` · `VERIFY69 99/99` · `VERIFY64 41/41`.

### 보류한 것 — AK 채점과 겹치는지 확인하고 결정한다 (05 교훈 3)

- **#1 금 알약 펄스** — 설계 최대 `FX3_PZ_MAX 0.22`(×1.22)에서 여백 49 → **3px**. 다만 이 값은
  **20회차가 올린 값이고 AG·AH 가 «아이콘 기준 ×1.07~1.11 규격 일치» 로 통과시킨 것**이다.
  AJ 는 **캡슐 박스**를 쟀다 — 즉 새 사실은 배율이 아니라 «캡슐 기준으로는 여백을 다 먹는다» 다.
  2인 공통이 되기 전에 되돌리면 승인된 변경을 혼자 뒤집는 것이 된다.
- **#5 토스트** — 실제 겹침 **2px**(AJ 는 15px). 위로 6px 올리면(`top 150 → 144`) `#chapN`(227)과
  4px 뜨고 위 초상화 플레이트(하단 142)와도 2px 뜬다 — 값은 준비됐고, AK 확인 후 적용.
- **#4 `fx-cvswap` 역효과** — AJ 는 «화면에서 읽히는 유일한 숫자가 플로터 +2.2» 를 감점 사유로
  적었지만, **+2.2 는 틀린 값이 아니다**(19회차에 `trainCardData()` 를 읽고 이미 정정한 오진이
  세 번째로 되돌아온 것이다). 지적의 **전제**가 이미 두 번 기각된 항목이라 단독으로는 안 따른다.
- **#6 트리거 직후 무반응** — 13·17·19·20회차가 네 번 연속 «컨테이너 프레임 예산, 실기기 1/4» 로
  분류한 항목. 다섯 번째로 같은 결론.

### 21차 비평 (비평가 AK) — **3점** (① 4 / ② 5 / ③ 4 / ④ 3)

AJ 4 · AK 3. 두 사람이 **독립적으로 같은 것**을 잰 항목만 22회차에 반영한다(05 교훈 3).

| 2인 공통 지적 | AJ | AK | 처리 |
|---|---|---|---|
| ✓ 체크 배지가 «보상 받기» 라벨을 가림 | #2 · 66~73% · 295~737ms | #2 · 132px 중 86px(65%) · 285~760ms | **반영**(위 «반영 1») |
| 수령 뒤에도 버튼이 «미수령 초록» | #3 · 1430ms 까지 | #17 · f10 체크 소멸 후 f14 까지 초록 | **반영**(아래 2) |
| upg 카드 새 수치가 판독 불가 | #4 · f4~f5 동적범위 96/90 | **#1(1순위)** · 340ms(49%) · 대비 1.31:1 | **반영**(아래 3) |
| 트리거 직후 무반응 | #6 · quest f1≡f2 md5 동일 | #4 · 190ms · #5 upg 100ms | **하네스 결함이었다**(아래 4) |
| 정답표가 1프레임 앞선다 | 제기 | «첫 비영 f7(570) — 부호·크기 모두 일치» 로 **확정** | **하네스 수정**(아래 5) |

AK 단독 지적(#3 링이 버튼 bbox 안으로 22×25px 침범 · #7 파티클 미소멸 · #8 비행이 열린 팝업 제목을
가림 · #10 퍼짐이 우상 45° 쐐기 단방향 · #11 스폰 앵커가 히어로에서 100px 어긋남 · #12 코인이
STAGE 진행바 체류 · #15 크기 곡선 역전 · #16 흡수 경로가 x978 단선 레일 · #20 마지막 코인이
아이콘에 안 닿음)은 **2인 공통이 아니라 이번 회차에 반영하지 않았다.** 다음 회차 비평가가 같은 것을
독립적으로 집으면 그때 잡는다 — 특히 #16(단선 레일)·#11(스폰 앵커)은 수치가 구체적이라 유력 후보다.

### 반영 2 — 수령 즉시 버튼을 소진 상태로 (AJ #3 · AK #17)

`probe58l` 실측: `--gb-mid #4CBA2E · disabled=false` 가 **t1396ms 까지** 유지되다가 재렌더 순간
`#8A8A8A · disabled=true` 로 1프레임 하드컷. 재화가 날아가는 내내 «아직 누를 수 있다» 로 보이고,
체크 도장이 사라지는 855ms 뒤에는 그 표식마저 없다.
지연 재렌더 자체는 유지해야 한다(`verify58` [11] — 비행 중 목록을 갈아 끼우면 출발점이 사라진다).
→ 목록은 그대로 두고 **누른 버튼 한 개만** 즉시 소진으로 넘긴다(`fxQuest` 안 1줄).
실측 **t1396ms → t4ms**. 체크·버스트와 같은 프레임에 바뀌므로 하드컷도 사라진다.
이중 지급은 원래 없다(수령 로직이 막는다 — `d0 1000 → d1 1090 → 2차 클릭 후 d2 1090` 실측 확인).

### 반영 3 — `fx-cvswap` 되돌리기 (AJ #4 · AK #1)

**21회차 처방이 병보다 나빴다.** `opacity .12` 를 267ms 유지하면 이 연출이 전달해야 할 유일한
정보인 새 수치가 판독 불가가 된다(AK 실측 배경 대비 **1.31:1**, 690ms 중 **340ms = 49%**).
«두 수치 동시 노출» 을 없애려다 «어느 수치도 안 보이는 구간» 을 만든 것이다.
→ 투명도로 지우지 말고 **팝 스케일**(`.84 → 1.07 → 1`, .34s)로 «이 줄이 방금 갱신됐다» 를 말한다.
새 값은 0ms 부터 alpha 1.0 이다. 동시 노출은 20회차의 색 분리(플로터 앰버 · 카드 초록)와
19회차의 상승 거리 104px 가 이미 맡고 있으므로 그 둘은 건드리지 않았다.
※ AK 는 플로터를 «옛 값» 이라고 적었지만 **그건 아니다** — 플로터 +2.2 = 방금 얻은 값,
카드 +2.4 = 다음에 오를 값으로 **둘 다 맞다**(19회차에 정정한 오진의 네 번째 재발). 그래서 AK 의
«옛 값 고스트 alpha 0.35 상한» 은 안 따랐다. 2인 공통인 것은 «카드 값이 안 보인다» 뿐이다.

### 반영 4 — «트리거 직후 무반응» 은 **게임이 아니라 하네스 결함**이었다 (AJ #6 · AK #4·#5)

13·17·19·20회차가 네 번 «컨테이너 프레임 예산» 으로 분류하고 넘긴 항목인데, **분류가 틀렸다.**
`cap58.js` 의 quest 트리거는 `el.click()` 인데 이것은 `click` 만 발화하고 **`pointerdown` 을 안 낸다.**
60 쥬시의 누름 피드백(`jz-dn`)은 pointerdown 캡처 리스너에 걸려 있어(74 «탭 유실» 대책) 이 경로로는
**영원히 안 붙는다** — 실기기에서는 손가락이 pointerdown 을 내므로 있는 반응이 캡처에만 없었다.
신설 `jzchk58.js` 로 갈랐다: `b.click()` → 클래스 그대로(`qs-b ifbtn`) · pointerdown 디스패치 →
**`qs-b ifbtn jz-dn`** 부착. upg 씬은 작업 64 이후 이미 pointerdown 으로 누르고 있어 옳았고,
quest 만 어긋나 있었다. → quest 트리거를 pointerdown + pointerup + click 으로 고쳤다.

### 반영 5 — 정답표 1프레임 시프트 (AJ 제기 · AK 확정)

AK: «첫 비영 프레임은 f7(570ms)이고 명목 스케줄의 f6(475ms)이 아니다 — 부호·크기 모두 일치».
원인은 `at(ms)` 의 «가장 가까운 표본» 이다. 스크린캐스트 프레임이 보여 주는 것은 그 시각 **직전에
이미 합성된** 화면인데, 뒤쪽(아직 안 그려진) 표본이 더 가까우면 그걸 골라 버린다.
→ **그 시각 이하의 마지막 표본**을 쓴다. 정답표는 오독을 막으려고 만든 장치라, 틀리면 없느니만 못하다.

### 게이트 (22회차)

`VERIFY58 PASS` · `VERIFY93 PASS` · `SMOKE PASS` · 회귀 `VERIFY22 65/65` · `VERIFY69 99/99` ·
`VERIFY64 41/41`. 캡처 `58-r22-{gain,quest}-1..17.jpg` · `58-r22-upg-1..8.jpg`
(목표 대비 최대 ±15 / ±33 / ±17ms · 원본 58 / 47 / 59 프레임). 비평가 AL·AM 채점 대기.

### 22차 비평 (비평가 AL·AM) — **4점 · 3점** (21회차 AJ 4 / AK 3)

| 축 | AL | AM | 21회차(AJ/AK) |
|---|---|---|---|
| ① 타이밍 | **6** | **6** | 5 / 4 |
| ② 크기 비율 | **7** | **6** | 5 / 5 |
| ③ 위치·경로 | **6** | **5** | 4 / 4 |
| ④ 가독성·일관성 | **4** | **3** | 4 / 3 |

**최저점(=점수)은 4·3 으로 21회차와 같지만, ①②③ 이 네 사람 모두에서 1~2점씩 올랐다.**
④ 가 그대로인 이유는 아래 «22회차가 만든 것이 아닌 가림» 때문이고, 그 정체가 이번에 특정됐다.

**22회차 수정 4건은 두 비평가가 독립적으로 «회수됨» 으로 확인했다**
- 체크 도장(반영 1): AL «체크 배지 하단 y1035 vs 라벨 상단 y1038 — **3px 여유**, f2~f6 라벨 bbox 안
  버스트 픽셀 **0**». AM «버스트 링은 라벨을 지킨다 — «보상 받기» 5글자 **100% 판독 가능**».
- 하네스 pointerdown(반영 4): AL «버튼 프레스 피드백이 **교과서적**이다 — 195×118 → **184×112(94.4%)**
  → 200×121(102.6%) → 정착, 스쿼시-오버슈트가 272ms 안에 완결». 네 회차를 «컨테이너 탓» 으로
  넘겼던 항목이 하네스 한 줄로 사라졌다.
- `fx-cvswap` 되돌리기(반영 3): AM «upg 신규 수치 즉시 판독 규격 **충족** — f2(104ms)에 Lv.·+2.4·비용
  세 값이 모두 확정». AL «카드 수치 3종이 f2 에 전부 동시 갱신, 지연 없음». 21회차의 «340ms 판독 불가» 소멸.
- 정답표(반영 5): 두 사람 모두 수치 오독 신고 0건. 21회차의 1프레임 시프트 지적이 재발하지 않았다.

### 22회차 뒤 반영 — 토스트 (AJ #5 · AL #1, 라운드를 건너 2인)

AL 이 1순위로 올렸다(«STAGE 1 캡높이의 50% 를 1초 넘게 덮고, f5~f15 **915ms 픽셀 완전 동일**»).
`probe58m` 으로 프레임 상단 띠를 실측했다 — **초상화 플레이트 하단 142 · `#chapN` 상단 227 →
빈 띠는 82px 뿐이고 토스트는 79px** 다. 들어갈 자리가 사실상 하나다.
→ `top 150 → 143`(하단 229 → **224**, 헤더와 3px 뜬다). AL 의 처방 «top 105» 는 **반대쪽 플레이트를
37px 파고든다** — 위만 보고 낸 값이라 안 썼다.
체류도 2인 공통(AL «915ms 프리즈» · AM «정지한 토스트가 예산의 68% 점유»)이라 퇴장 1200 → **760ms** ·
소멸 1560 → **1060ms** 로 줄였다(AL 350 · AM 550+200 사이).
게이트 재확인: `VERIFY58 PASS` · `VERIFY93 PASS` · `VERIFY22 65/65` · `VERIFY69 99/99` · `SMOKE PASS`.

## 23회차가 잡을 것 — **④ 병목의 정체가 특정됐다: 흡수 «경로» 가 자기 행을 수평으로 관통한다**

두 비평가가 **독립적으로 같은 것**을 세 갈래로 쟀다. 셋 다 한 원인이다.

| | AL | AM |
|---|---|---|
| 행 **제목** 관통 | #2 «타이틀 212px 중 **90px(42%)** 가 덮임, 178~566ms» | #2 ««적 60» **92px 100% 판독 불가**» |
| 행 **게이지** 관통 | #8 ««60/60» 의 **49%**, 진행바 좌측 **91px(23%)**» | #2 «게이지 좌측 **90px = 23%**» |
| 버튼 **라벨** 관통 | #5 «f9 다이아 40×37 이 라벨과 x40px(**30%**) 겹침 — 「상」자 전멸» | #1 «라벨 면적 **20%** 가림, 「상」 하단 획 100% 소실» |

AL 이 원인을 정확히 적었다: **«흡수 경로가 소스(219,1066)에서 y≈1050 을 따라 x327→978 로 651px
수평 주행» 한다.** 코드로 확인된다 — `f.ox != null` 인 패널 안 경로의 leg1 은 `FX3_HX`(45%)에 x 가
복도(x≈978)에 도달하고 `FX3_VY`(25%)부터 `vy*vy`(ease-**in**)로 겨우 오르기 시작한다. 즉
**«수평으로 행을 다 건너간 뒤에 올라간다».** 21회차가 «남은 문제 2» 로 미뤄 둔 바로 그 항목이고,
이제 2인 공통이라 잡을 근거가 섰다.

**두 비평가의 처방도 같다** — AL «출발 직후 y 를 −120px 선상승시킨 뒤 우측 이동, 타이틀 밴드
y1015~1060 을 경로 금지대로» · AM «첫 80ms 안에 y&lt;1000(행 상단 밖)으로 즉시 이탈».

⚠ **다만 «그냥 먼저 올린다» 로 하면 안 된다 — 다른 게이트를 깬다.** 그 자리에서 수직으로 오르면
x 가 아직 패널 안(행 x131~951)이라 **윗 행들을 통째로 관통**하고, `verify93` [2b] «형제 행 관통 0» 이
먼저 깨진다(19회차에 반경을 키웠다가 29회로 잡힌 전례). 지금의 «수평 먼저» 는 그 게이트를 피하려고
일부러 그렇게 짠 것이다(6회차 «코너를 패널 윗변 위에» 주석).

→ **23회차 처방(측정으로 낸 것)**: 수평 탈출을 **행 «사이» 여백에서** 하면 두 게이트가 동시에 선다.
실측 기하 — 행 `.qs-r` 818×179(피치 **200**) · 제목 y1021~1057 · 버튼 y1008~1126 · 행 y977~1156.
행 내용의 아래 끝은 1126 이고 **행 경계 1156~1176 의 20px 여백**은 어느 행의 내용도 없다.
즉 leg1 의 수평 주행 y 를 «출발 행의 아래 경계 + 여백 중앙» 으로 물리면, 수평 구간이 **어떤 행의
글자·게이지도 지나지 않고** 복도까지 간 뒤 종전처럼 복도에서 올라간다(윗 행 관통도 0 그대로).
구현 지점은 `fxFly` 스폰부의 `f.ay` 옆에 «탈출 y»(가칭 `f.ey`)를 하나 더 두고, leg1 의 y 보간을
`ay → ey`(여백까지 **빠르게**, ease-out) → `ey → cy2`(복도 상승, 종전)로 두 토막 내는 것이다.
`fx3Out()` 이 이미 행 rect 를 알고 있으니 여백 y 도 거기서 같이 돌려주면 된다.

### 그 밖에 23회차가 볼 것 (2인 공통만)

1. **마지막 도착이 규격 1220ms 대비 +155ms** — AL #6(gain ≈1375 · quest ≈1380) · AM #17(+12.7% / +16%).
   첫 도착은 두 사람 다 «규격 +3~5% 로 양호» 라 **꼬리만** 줄이면 된다(스태거 47 → 40ms).
2. **카운터가 마지막 아이콘보다 먼저 끝난다** — AL #7(197ms) · AM #16(147ms). 1번과 같이 잡는다.
3. **비행 코인이 중간에 과대 팽창** — AL #10(스폰 32 → 중간 **51px** = 도착 아이콘의 82% → 도착 23) ·
   AM #10(씬 간 31% 불일치, 다이아는 도착 아이콘의 46%). 21회차 AK #15 도 같은 것을 짚었다(3인).
4. **upg 플로터와 카드 수치 공존** — AL #4 · AM #3(19·20·21회차에 이어 **다섯 라운드 연속 2인 공통**).
   ⚠ 두 사람이 또 «+0.2 로 바꿔라» 라고 적었지만 **그건 다섯 번째 오진이다** — 플로터 +2.2 = 방금
   얻은 값, 카드 +2.4 = 다음에 오를 값으로 둘 다 맞다(`trainCardData()`). 아직 **안 해 본** 처방은
   AM 의 «글자높이를 신규값의 64%(18px)로 낮춰 주종을 만들라» 다. 값은 건드리지 말고 그것만 시도할 것.
5. **버스트 점이 upg 카드 경계를 넘는다** — AL #11 «카드 밖으로 이탈, 화면 좌단 6px» vs
   AM #15 «카드 **안쪽** 20~30px 에 박힘». **두 사람이 정면충돌**이므로 05 교훈 3 대로 이번엔 안 잡는다.
   다음 회차 비평가 둘이 어느 쪽으로 모이는지 보고 정할 것.

## 23회차 (2026-08-26, sess-1025-12474 워커 B) — 22회차가 반납한 lock 을 이어받기

22회차 핸드오프(«④ 병목 = 흡수 leg1 이 자기 행을 수평 관통»)를 그대로 착수점으로 삼았다.
1회차부터 다시 돌지 않았다 — 회차는 **23회차로 이어서** 센다(3차 라운드 23/30).

### ⚠ 22회차 처방은 **쓸 수 없었다** — 다른 게이트가 먼저 깨진다

핸드오프는 «행 «사이» 여백(1156~1176)에서 수평 탈출» 을 처방했고 «그러면 두 게이트가 동시에
선다» 고 적었다. **실제로는 그 자리에서 `verify93` [2b] 가 깨진다.** 그 여백은 출발점(y1066)보다
**아래**라 흡수가 시작된 뒤 y 를 내려야 하는데, [2b] 에 «흡수 중 아래로 되돌아가는 프레임 **0**»
이 걸려 있다(핸드오프가 «형제 행 관통» 만 보고 이 항목을 못 봤다).

### 신설 도구 `probe58n.js` — 행 안을 1px 씩 훑어 «콘텐츠 없는 밴드» 를 찾았다

퀘스트 행 실측(프레임 좌표): 행 `.qs-r` **x131 y977 818×179**(하단 1156) · 피치 200 ·
행 목록 577~756 / 777~956 / **977~1156** / 1177~1356 / 1377~1556.
행 안 부품: 버튼 y1008~1126 · 보상 프레임 1014~1120 · 제목 1021~1057 · 게이지 1078~1117 · 배지 1100~1126.
→ 콘텐츠가 **하나도 없는** 세로 밴드는 **위 977~1007(31px)** · 아래 1126~1155(30px) 두 곳뿐이다.

**위 밴드가 유일한 답이다.** 자기 행 «안» 이라 형제 행이 아니고(cross 무관), 출발점보다 «위» 라
흡수 내내 y 가 단조 감소한다(backs 무관). 두 게이트가 동시에 서는 자리는 여기 하나다.

### 반영 1 — leg1 의 순서를 뒤집었다 (AL #2·#5·#8 · AM #1·#2)

`fx3Escape()` 신설: 출발 행(`.qs-r`/`.ml-r`)의 콘텐츠 위·아래 끝을 DOM 으로 재서 탈출 y 를 낸다
(행 위 여백도 «바로 위 형제의 하단» 까지만 빌린다 — 중심이 형제 상자에 들어가면 cross 가 센다).
leg1 보간: **세로 탈출(0~16%, ease-out) → 수평(12~55%) → 복도 상승(45%~)**.

⚠ **상승 시작(FX3_EYV)은 수평 완료 «직전» 이어야 한다.** 먼저 올리면 x 가 아직 행 우변(949)
안이라 윗 형제 행을 관통한다 — 실측으로 **cross 0 → 5** 로 깨졌다(되돌리지 말 것).
⚠ 퍼짐 끝점이 이미 콘텐츠 아래(`cb`)인 아이콘은 **종전 경로 그대로** 둔다. 굳이 콘텐츠를 세로로
관통해 위 밴드까지 올릴 이유가 없다 — 이 한 줄이 제목 가림을 7 → 3 프레임으로 되돌렸다.

`probe58o.js`(신설) 실측 — 같은 트리거·같은 판정으로 전/후:

| | 22회차(전) | 23회차(후) |
|---|---|---|
| 흡수 구간 가림 프레임 — 제목 | 3 | 3 |
| 〃 게이지 | 6 | **1** |
| 〃 버튼 라벨 | 11 | **0** |
| 〃 **합** | **20** | **4** |
| 전 구간 누적 히트 제목/게이지/라벨 | 57 / 44 / 13 | 43 / 23 / **0** |
| 형제 행 관통 · 역주행 | 0 · 0 | 0 · 0 |

### 반영 2 — 비행 코인 과대 팽창 (AL #10 · AM #10 · 21회차 AK #15 = 3인)

봉우리 1.10 → **1.00**, 축소 폭 0.6 → **0.5** 로 **도착 크기(0.50)는 한 픽셀도 안 바꿨다** —
도착 그림은 세 사람 모두 «잘 되어 있는 것» 에 넣었으므로 손대면 안 된다.

### 반영 3 — upg 플로터/카드 수치 공존 (AL #4 · AM #3 — 네 라운드 연속 2인 공통)

값은 둘 다 맞으므로(플로터 = 방금 얻은 값 · 카드 = 다음에 오를 값) **안 해 본 처방만** 시도했다:
AM «글자높이를 신규값의 64% 로». 카드 신규값 `.tr-card>.cv>i` 48.5px × .64 = **31px**(종전 34 = 70%).

### ⛔ 회수 불가 확정 — «마지막 도착 +155ms» 와 «카운터가 먼저 끝난다»

AL #6·#7 · AM #16·#17 이 요구한 «스태거 48 → 40ms» 는 **주인 검증 항목과 정면으로 부딪친다**.
1.10 으로 내리면 `verify93` [1] «마지막 도착 1.2~1.4s» 가 실측 **1119ms** 로 FAIL 이다
(게이트 실측 ≈ 설계값 + 19ms). 하한 1200 을 지키는 최대 감축은 설계 1.19 = 스태거 **−2ms** 뿐이고
러너 변동폭보다 작다. 두 항목은 도착 계단에 물려 한 손잡이라 같이 막힌다.
→ **envelope(0.50 / 1.22) 자체를 주인이 바꾸지 않는 한 못 줄인다. 다음 회차는 재시도하지 말 것.**

### 게이트 (23회차)

`SMOKE PASS` · `VERIFY58 PASS` · `VERIFY93 PASS`(2회 — 정지 프레임 0.5% / 1.0%) ·
`VERIFY74 200/200 × 7대상` · `VERIFY102 85/85` · `VERIFY22 65/65` · `VERIFY69 99/99` · `VERIFY64 41/41`.
※ `verify93` 의 «정지 프레임 ≤2%» 항목은 이 러너에서 **수정 전후 무관하게** 2.6~2.9% 로 한 번씩
튄다(수정 전 baseline 도 2.6% 로 FAIL 했다) — 작업 127 계열이지 23회차 회귀가 아니다.
캡처 `58-r23-{gain,quest}-1..17.jpg` · `58-r23-upg-1..8.jpg`.

### 23차 비평 (비평가 AN·AP) — **3점 · 3점** (22회차 AL 4 / AM 3)

| 축 | AN | AP | 22회차(AL/AM) |
|---|---|---|---|
| ① 타이밍 | **3** | **3** | 6 / 6 |
| ② 크기 비율 | **5** | **4** | 7 / 6 |
| ③ 위치·경로 | **4** | **3** | 6 / 5 |
| ④ 가독성·일관성 | **3** | **4** | 4 / 3 |

**① 이 6 → 3 으로 떨어진 것은 연출이 나빠져서가 아니다 — 아래 «하네스 결함» 이 원인이고, 그것이
이번 회차 최대 수확이다.** ②③ 하락분도 상당 부분 같은 뿌리다(정지로 보이면 «도착 안 함» 으로 읽힌다).

**22회차 수정 3건의 회수 판정 — 두 사람이 독립적으로 확인했다**
- **경로 가림 — 회수(부분).** AN «수평 구간이 실제로 행 아래 빈 밴드(y≈1128~1177)로 내려갔다 —
  f9(747)의 코인이 제목·게이지·버튼 라벨 **어디도 안 건드린다**». AP «**버튼 라벨 30% → 0.0%(전 프레임)**,
  게이지 23% → 최대 9.7%». 코드 실측(probe58o)과 방향·크기가 일치한다.
  남은 것은 **경로가 아니라 «스폰 뭉치»** 다 — 둘 다 «퍼짐이 제목 «위»에서 터진다» 로 옮겨 적었다.
- **코인 봉우리 −9% — 회수(✅ 2인 일치).** AN «봉우리 47~48px = HUD 아이콘 53×53 의 **89%**,
  아이콘을 안 넘는다» · AP «46px, 목표 52px 의 88%».
- **플로터 31px — 회수(✅ 2인 일치).** AN «잉크 38px(본문 ≈31px)» · AP «32~33px, 카드 라벨 대비 82% 로
  위계는 잡혔다». ⚠ 다만 **둘 다 새 부작용을 지적**했다 — 씬 A·B 플로터(37px)보다 14~19% 작아져
  «세 씬 간 글자 크기 일관성» 이 새로 깨졌고(AP #12 · AN 판정문), 「방금 오른 값」쪽이 더 작아졌다.

### ⚑ 이번 회차 최대 수확 — ① 감점의 정체는 **캡처 하네스**다 (22회차 pointerdown 과 같은 계열)

두 비평가가 **독립적으로 같은 문장**을 ① 축 최대 감점으로 적었다:
- AN #1 «quest f2(t=79)와 f3(t=185)가 픽셀 단위로 완전히 동일 — 변경 픽셀 **0개**, maxdiff 0.
  106ms 가 통째로 정지 프레임» · #2 «퍼짐이 스폰 +86ms 기준 **293ms 동안 아무것도 안 보이다가**
  f5(379)에서 이미 31px 로 튀어나온다 — 퍼짐 박자 전체가 누락»
- AP #1 «md5 일치, 차분 0px/106ms. 표대로면 f3 에 16개가 있어야 하는데 0개»

**셋 중 하나가 틀렸다(게임 / 정답표 / 캡처). 캡처였다.**
신설 `probe58p.js` 로 같은 트리거를 10ms 간격으로 DOM 실측한 결과 — 게임은 정상이다:

```
 t(ms) 요소수 보이는수 최소op 최대op 뭉치w×h
    66    16       0      0      0   0×0     ← 스폰(opacity 0)
   104    16      16      1      1   75×111  ← 이미 전부 보인다
   163    16      16      1      1  105×155
   187    16      16      1      1  110×163
   210    16      16      1      1  113×167  ← 퍼짐이 자라는 중
   307    16      16      1      1  113×178  ← 퍼짐 완료(설계 0.22s)
```
즉 **t=104ms 에 16개가 opacity 1 로 그려져 있고** 뭉치가 규격대로 자란다. «293ms 공백» 은 없다.

**파일로도 확인된다** — `md5sum docs/review/58-r23-quest-*.jpg` 결과 **quest-2 와 quest-3 이
바이트 동일**(gain 17장·upg 8장은 중복 0). CDP 스크린캐스트가 서로 다른 타임스탬프로 **같은
페인트**를 두 번 내보냈고 `pick()` 이 그것을 두 슬롯에 그대로 썼다. 비평가는 파일을 정직하게
읽었을 뿐이다.

→ **`cap58.js` 를 고쳤다**: `pick()` 이 이웃 슬롯의 데이터가 같으면 «⚠ 중복 페인트» 를 슬롯 번호·
Δms 와 함께 **소리내어 출력**한다. 없는 페인트를 만들어 낼 수는 없으니 **다음 회차는 그 경고를
비평가 전달문에 그대로 옮겨 «이 슬롯 쌍은 같은 페인트이니 정지 프레임으로 세지 말 것» 이라고
반드시 알려야 한다.** 조용히 넘어가면 ① 에서 매 회차 2~3점이 그냥 날아간다(23회차가 그랬다).

## 24회차가 잡을 것

0. **먼저 `node cap58.js r24` 의 «⚠ 중복 페인트» 경고를 읽고, 있으면 그 슬롯 쌍을 비평가
   전달문에 명시한다.** 이걸 빼먹으면 ① 채점이 또 하네스를 잰다. (23회차 최대 교훈)
1. **스폰 뭉치가 행 제목 위에서 터진다 — 2인 공통, 이번 라운드 ③④ 병목.**
   AN «f5(379)~f8(668) **289ms** 동안 제목 면적 34.0/32.0/34.3/9.5%(열 폭 47.8/**55.6**/44.9/24.4%),
   게이지 면적 12.0/9.9/7.5/3.9%» · AP #9 «버스트 발생점이 보상 아이콘(x173–267, 중심 220)에서
   **125px 오른쪽**으로 어긋나 클러스터 중심이 x≈345 — 그래서 아이콘이 아니라 **제목 글자 위**에서 터진다».
   ⚠ **경로(leg1)는 이미 해결됐다. 이번엔 «퍼짐 부채꼴의 원점·각도» 다** — 두 사람이 서로 다른 방식
   (면적 % / 발생점 좌표)으로 같은 것을 짚었다. AP 의 «+125px» 은 좌표라 검증 가능하다:
   `probe58n` 의 `src` 는 (820,1067)로 **버튼** 이었는데 실제 스폰은 보상 아이콘이어야 한다 —
   **`fxFly` 호출부(`claimQuest`)가 어느 요소를 `fxAt` 에 주는지부터 grep 해서 확인할 것.**
2. **꼬리 코인이 알약에 도착하지 않고 소멸 — 2인 공통.**
   AN #5 «gain 마지막 코인 f13(1139)(593,86) → f14(1252)(596,86) = **113ms 에 3px**, 골드 아이콘
   중심(583,54)에서 **34.5px 어긋난 우하단**에 멈춘 뒤 소멸» · #6 «quest 도 27.7px / 다이아 27px» ·
   AP #8 «마지막 코인이 (811,308)에서 소멸 — 알약까지 **343px** 남음».
   ⚠ 두 사람의 «남은 거리» 가 34.5px 와 343px 로 **10배 다르다** — 어느 쪽이 맞는지 먼저
   `probe` 로 확정하고 잡을 것(05 교훈 3). AN 쪽이 좌표를 프레임마다 댔다.
3. **씬 C(upg) 마지막 303ms 가 죽은 꼬리 — 2인 공통.** AN #3 «f7(694)↔f8(850) 변경 **0px**,
   f6→f7 도 전체의 0.019%» · AP #3 «f7→f8 156ms 변화 0px». ⚠ 단, 0번(중복 페인트)을 먼저
   배제하고 판정할 것 — upg 8장에는 md5 중복이 **없었으므로** 이건 진짜일 가능성이 높다.
4. **플로터 크기 일관성이 새로 깨졌다 — 반영 3의 부작용, 2인 공통.**
   씬 A·B 37px vs 씬 C 31~33px(−14~19%). AP #12 는 상승 속도도 «A 27px/s vs C 315px/s = **11.6배**» 로 쟀다.
   → 31px 를 되돌리는 것이 아니라 **세 씬을 한 값으로 묶는** 쪽으로 잡을 것(공용 모듈의 목적이 그것이다).
5. **씬 C 플로터가 카드 아이콘을 가로로 관통 — 2인 공통.** AN #15 «아이콘 가로 **92%**(147/160) ·
   세로 25% 를 띠로 덮는다» · AP #13 «세로 20% 를 폭 93% 로». 위치만 옮기면 되는 건이라 싸다.
6. **quest 골드 피드백이 값과 어긋난다 — 2인 공통.** 골드 표기가 f2~f17 내내 128A 로 고정인데
   골드 알약에 65×65 흡수 팝(+23%)과 «+400» 플로터가 붙는다(AN #13 · AP ④ 근거).
   ⚠ 이건 **하네스일 수 있다** — 캡처가 골드를 미리 128A 로 채워 둔 뒤 퀘스트 보상을 준다.
   0번과 같이 «게임인가 캡처인가» 부터 가릴 것.

### 24회차가 **다시 시도하면 안 되는 것**
- 스태거 48 → 40ms(마지막 도착 −155ms · 카운터 조기 종료) — 위 «⛔ 회수 불가» 참조. envelope 게이트와 정면 충돌.
- 흡수 중 y 를 «아래로» 내리는 모든 경로(행 사이 여백 포함) — `verify93` [2b] backs=0 과 충돌.
- 상승을 수평 완료보다 «먼저» 시작하기 — cross 0 → 5.
- upg 플로터의 **값**을 +0.2 로 바꾸기 — 다섯 번째 오진이다(둘 다 맞는 값이다).

## 24회차 (2026-08-26, sess-1132-14402 워커 B) — 23회차가 반납한 lock 을 이어받기

23회차 §«24회차가 잡을 것» 의 0~6 을 순서대로 처리했다. 이번 회차의 특징은
**여섯 건 중 셋(0·3·6)이 «게임» 이 아니라 «하네스» 였다는 것**이다 — 23회차가 0번에서
발견한 계열이 실제로 upg 창(3)·골드 시드(6)에도 그대로 있었다. 두 비평가가 라운드마다
1~2점씩 잃던 자리가 대부분 여기다.

### 신설 도구 3종 — 고치기 전에 먼저 쟀다

| 도구 | 무엇을 재나 |
|---|---|
| `probe58q.js` | 프레임마다 «퍼짐 뭉치 bbox·중심» + 제목 잉크·게이지·버튼 각각의 **덮인 면적 %** |
| `probe58r.js` | 아이콘을 **fxFlies 에서 빠진 뒤에도 DOM 으로** 끝까지 쫓아 «마지막 좌표 · 알약까지 거리 · 착지 클래스 · opacity» |
| `probe58s.js` | 씬 C — 플로터 잉크 크기(세 씬 비교) · 카드 아이콘/수치행/이름행 덮음 % · **마지막으로 무엇이든 바뀐 시각** |

### 반영 1 — ① 퍼짐 부채꼴 폐기, «행 안 빈 밴드 가로 스프레이» 로 (AN #1·#2 · AP #9, ③④ 병목)

23차 비평은 «클러스터 중심이 보상 아이콘에서 +125px 오른쪽»(AP, 좌표) ·
«제목 면적 34.0/32.0/34.3/9.5% 를 289ms»(AN, 면적) 로 같은 것을 짚었다.
`probe58q` 로 다시 재니 **두 사람이 후하게 본 것이었다**:

```
 t(ms)  뭉치중심        제목잉크덮음%  게이지%
   128  ( 324, 1090)         68.1      38.3
   190  ( 353, 1096)         93.8      37.4
   232  ( 360, 1097)         96.9      36.0   ← 고원
   373  ( 357, 1102)         92.5      36.7
```
아이콘 중심 (220,1066) · 제목 잉크 x299~501 y1020~1055 · 게이지 x300~700 y1077~1116.
**제목 잉크의 96.9% 가 280ms 동안 덮여 있었다.**

원인은 부채꼴 자체다 — `FX3_PA0/PA1`(−19°~38°) × `FX3_PR0/PR1`(106~184)의
«거의 수평 우측» 부채꼴이 행 안에서 제목·게이지를 정확히 덮는 자리다. 각도를 어디로
돌려도 행 안에는 그만한 빈 자리가 없고, 위·아래로 벌리면 형제 행이라 `verify93` [2b]
cross 가 먼저 깨진다.

→ **23회차가 이미 찾아 둔 «행 안 빈 밴드»(`fx3Escape`)를 퍼짐의 목적지로 쓴다.**
퀘스트 행 기준 밴드 중심 y≈981(폭 46)이라 제목(1020~)·버튼(1007~) 어느 쪽에도 안 닿고,
출발점보다 **위**라 흡수 내내 y 단조 감소가 유지된다([2b] backs=0). 흡수 leg1 이 다시
내려갈 필요도 없어져 23회차의 `f.ey` 우회가 이 경로에서는 자동으로 안 걸린다.
두 재화가 같이 날면 밴드를 좌우로 가른다(골드 왼 · 다이아 오른 — HUD 알약 순서와 같아
두 흐름이 안 꼬인다). 상수 `FX3_BSX0/BSX1/BSY/BSOM` 신설.

**세로를 가로보다 빨리 뺀다.** 같은 이징으로 옮기면 밴드까지 올라가는 **대각선이**
제목 잉크를 92~180ms 지나간다(1차 수정 실측 139.2% → 19.9%). y 만 5제곱 이징으로 바꾸면
퍼짐 30%(66ms)에 이미 83% 를 올라가 있어 그 구간이 사라진다.

| | 제목 잉크 최대 % | 그 체류 | 게이지 최대 % |
|---|---|---|---|
| 23회차 | **96.9** | 280ms | 43 |
| 24회차 | **42.4**(t=88 한 프레임) → 1.6(125) → **0**(154~) | ≈60ms | **0** |

남은 42.4% 는 «스폰 프레임» 이다 — 아이콘이 아직 출발점(보상 프레임 중심)에 겹쳐 있는
순간이고, 그 자리는 **의미상 옳은 자리**다(재화가 보상 칸에서 나온다).

### 반영 2 — ② 꼬리 코인은 «못 닿는» 게 아니라 «달리면서 흐려진다» (AN #5·#6 · AP #8)

두 사람의 «남은 거리» 가 34.5px 와 343px 로 10배 달랐다. `probe58r` 로 확정했다 —
**16/16 이 알약 중심 0.0px 에 정확히 닿는다.** AP 의 343px 은 오진, AN 의 34.5px 은
«마지막으로 **보인** 자리» 로는 맞다(실측 4.5~108px).

진짜 결함은 위치가 아니라 **겹침**이다: 마지막 주행(`.fx-land2` transition 45~120ms)과
소멸 페이드(`fxLand2` 150ms)가 **같은 프레임에 시작**해, 코인이 달리는 내내 흐려진다.
80~100ms 캡처에서는 주행 중 한 장이 마지막 가시 프레임이 되고, 그게 알약 34px 앞이다.
→ 페이드에 `animation-delay = 주행 시간` 을 준다. 도착이 opacity .95 로 최소 한 프레임 남는다.
(«이중 원»(7·9회차)은 **크기** 문제라 여기서 재발하지 않는다 — 착지 scale .5 는 그대로)

### 반영 3 — ④ 세 씬 플로터 크기를 한 값으로 (AN 판정문 · AP #12, 2인 공통)

23회차의 «플로터 31px»(AM 처방)은 회수됐지만 **씬 A·B(37px) 와 14~19% 벌어지는**
부작용을 낳았다. 둘 다 «31 을 되돌리라» 가 아니라 «세 씬을 한 값으로 묶으라» 고 적었다 —
공용 모듈의 목적이 그것이다. 토큰 `--fx-plus-fs: 34px` 하나로 묶었다(씬 A·B 40 → 34).
카드 신규값 48.5px 대비 70% — 17~22회차 내내 «크기» 로는 한 번도 지적되지 않은 비율이라
23회차가 산 위계도 유지된다. 실측: 세 씬 전부 `font-size: 34px`.

### 반영 4 — ⑤ 씬 C 플로터가 카드 아이콘을 관통하던 것 (AN #15 · AP #13)

`.tr-card` 안에는 아이콘을 피할 **가로** 자리가 없다 — 아이콘(`.ci`)이 폭 149 로 카드(326)
한가운데를 차지하므로 어떤 중앙 정렬 글자도 중심을 덮는다. 유일하게 아이콘이 없는 띠가
**카드 기준 y275~396**(아이콘 아래 ~ 버튼 위)이라, 상승 구간을 통째로 그 안에 가둔다:
시작 `r.y − 58` → **`r.y + r.h + 20`**, 상승 104 → **80px**.

| | 아이콘 덮음(면적) | 아이콘 덮음(가로) | 수치행 | 이름행 |
|---|---|---|---|---|
| 23회차 | 25% 를 폭 92% | 92% | — | — |
| 24회차 | **0** (전 프레임) | **0** | ≤34.4% | ≤41.2% |

끝점이 `.cv` 위지만 21회차의 «플로터가 떠 있는 동안 `.cv` 를 죽인다» 가 이미 걸려 있어
두 수치가 같이 안 읽힌다. **다음 회차가 볼 것**: 이름행(`.cn`, 정적 라벨 «공격력») 을
최대 41% 가리는 것이 아이콘 92% 보다 나은 거래인지 — 2인에게 물어야 확정된다.

### 하네스 결함 3건 — 0·3·6 (23회차 최대 교훈의 연장)

- **0. 중복 페인트 경고** — `cap58.js` 의 «⚠ 중복 페인트» 출력을 **아래 비평가 전달문에
  그대로 옮겼다**(23회차가 넣어 둔 장치. 조용히 넘어가면 ① 에서 매 회차 2~3점이 날아간다).
- **3. 씬 C 창이 연출보다 길었다** (AN #3 · AP #3, 2인 공통 «마지막 303ms 가 죽은 꼬리»).
  0번을 먼저 배제하고 봤다 — upg 8장에는 md5 중복이 없었으므로 진짜다. `probe58s` 로
  «#fxl 요소 좌표·불투명도 + 카드 transform/filter + `.cv` 불투명도» 서명을 rAF 마다 비교하니
  **마지막으로 무엇이든 바뀐 시각이 677ms** 다. 연출은 규격(0.3~0.8s) 안에서 정상적으로
  끝나는데 690·850 두 슬롯이 **끝난 뒤의 빈 화면**을 재고 있었다.
  → `WANT_UPG` 를 `[100,210,320,380,550,690,850]` → **`[95,175,255,335,425,530,660]`** 로.
- **6. 퀘스트 골드가 표기에서 안 움직이던 것** (AN #13 · AP ④). 씬 1(gain)이 +128,000 을
  넣어 둔 골드를 씬 2 가 그대로 물려받아, 보상 +400 이 111 의 알파벳 단위 표기에서
  «128A» 안으로 삼켜졌다(반올림 아래). 게임은 정상 — 값도 롤링도 실제로 오른다.
  → 씬 2 를 `S.gold = 820` 에서 시작시킨다(씬 3 이 `1e13` 앞에서 하는 것과 같이
  `fxSeen/fxDisp/fxAcc/fxHold` 를 같이 맞춰 «되돌리기» 자체가 획득으로 안 잡히게).

### 24차 비평 (비평가 AQ·AR)

AR — **① 3 / ② 4 / ③ 4 / ④ 4** (23회차 AN 3 / AP 3).

**r24 두 수정을 AR 이 독립적으로 «회수» 로 확인했다** (다음 세션이 되돌리지 않도록):
- «quest 밴드 y952~1010 이 행 제목(y1015~1050)·게이지(y1080~1117)·«보상 받기» 버튼
  (x717~920 y1007~1125)을 **한 픽셀도 안 가린다**. 골드(x257~461)와 다이아(x483~695)가
  좌우로 완전 분리되어 어느 재화인지 즉시 읽힌다.»
- «upg 플로터가 훈련 카드 아이콘을 **한 프레임도 안 가린다** — 칼 아이콘 y1490~1660,
  플로터 최고점 y1695 로 35px 아래. 좌우도 카드 내부에 머문다.»
- 착지 정확도 **1px 이내**(gain 마지막 코인 중심 (583,53) · quest 골드 (584,54) ↔ 알약 (583.5,54.0)).
  → 반영 2(페이드 지연)가 «닿고 나서 사라진다» 를 실제로 보이게 만들었다.

### ⚑ 24회차 최대 수확 — **md5 가 못 잡는 «부분 중복 페인트»** (AR ①-1)

AR 의 ① 최대 감점은 «gain 10↔11(t=851→921): 비행 코인 6개가 **70ms 동안 완전 정지**.
같은 두 프레임에서 HUD 는 4,092px 이 바뀌었으므로 **캡처 중복이 아니다**» 였다.
AR 이 «캡처 중복이 아니다» 를 스스로 배제한 것이라 설득력이 있었지만, 23회차 삼분법을
그대로 돌렸다(게임 / 정답표 / 캡처):

- **게임** — `probe58t` 로 gain 씬을 rAF 마다 훑었다. t=833~944 구간에서 아이콘이 프레임마다
  **45~63px 씩 움직인다**. 씬 전체에서 «일부만 정지» 프레임 **0**, «전부 정지» 1(561 표본 중 16 = 2.9%).
- **파일** — 그런데 AR 이 맞다. 두 파일을 직접 재면
  **비행 밴드 y400~800 변경 0px · HUD y0~160 변경 5,408px · 전체 5,736px.**

**둘 다 맞다 — CDP 스크린캐스트가 «레이어별로 부분 재합성» 한 프레임을 내보낸다.**
HUD(자체 합성 레이어에서 CSS 애니메이션 중)는 새로 그리고 `#fxl` 오버레이는 앞 프레임 페인트를
재사용한다. **프레임 전체는 서로 다르므로 23회차가 넣은 md5 중복 검사로는 절대 안 잡힌다.**
비평가에게는 «연출이 멈췄다» 로 정직하게 보이고, 그래서 23·24 두 회차 연속 ① 최대 감점이
여기서 났다(23회차는 전체 동일판, 24회차는 부분판 — **같은 병의 두 얼굴**이었다).

→ **`pxdup58.py` 신설 + `cap58.js` 에 상시 부착.** 인접 슬롯마다 «연출 구역» 변경량을
  **그 씬 자신의 중앙값**과 비교해, 중앙값의 5% 밑으로 떨어지면서 HUD 는 살아 있는 쌍을 경고한다.
  (첫 판은 «어느 구역이든 0 이면 경고» 였는데 하단 탭바처럼 원래 안 변하는 구역이 매 쌍에 걸려
  15건 중 12건이 잡음이었다 — 절대값이 아니라 중앙값 대비여야 한다)
  r24 실행 결과 정확히 **gain 10↔11(중앙값의 1.7%)** 과 quest 16↔17(0.9%, 연출이 실제로 끝난 꼬리)
  둘만 잡는다. **다음 회차부터 이 두 줄을 비평가 브리프에 그대로 옮길 것.**

### AR 의 나머지 지적 — 25회차 후보 (AQ 채점과 겹치는지 확인하고 확정할 것)

1. **시각 도착과 알약 숫자가 220~280ms 어긋난다** — quest t=729 에 코인이 알약에 닿았는데 숫자는
   820 그대로(첫 증가 850~1010). gain 도 t=575 도착 ↔ 첫 증가 575~796.
   ⚠ 위 «부분 중복 페인트» 와 같은 계열인지 먼저 가릴 것 — HUD 는 살아 있고 연출만 재사용된
   프레임이 있으므로, 그 반대(숫자만 늦게 합성)도 가능하다.
2. **초록 «획득» 플래시가 숫자 증가보다 250~530ms 먼저 끝난다** — 두 신호가 한 프레임도 안 겹친다.
3. **quest 경로가 목표 x=583 을 396px 지나쳐 x≈979 까지 갔다 되돌아온다**(경로 +60%).
   ⚠ 이건 **설계다** — 6회차의 «두 구간 경로»(복도 = 패널 우측 바깥)가 `verify93` [2b]
   «형제 행 관통 0» 을 세우는 장치다. 되돌리려면 그 게이트를 먼저 다시 설계해야 한다.
   AR 이 그 맥락을 모르고 «우회» 로 읽은 것이라, 25회차는 **경로가 아니라 «패널 밖으로 나가는 것이
   보이는 이유»**(팝업 테두리 x≈984 를 코인이 절반쯤 넘어간다)를 잡는 편이 싸다.
4. **quest 3박자 전체가 +230ms 밀렸다** — 첫 도착 t≈729(규격 500) · 마지막 t≈1433~1480(규격 1220).
   gain 은 규격에 맞는다(500~575 / 1209~1312). **씬 간 오차**이므로 트리거~스폰 지연을 잴 것
   (cap58 로그의 «스폰 지연» 은 quest 4ms · gain −29ms 로 이 차이를 설명하지 못한다).
5. **플로터 잉크가 upg 26px vs gain·quest 23px (+13%)** — 선언값은 세 씬 모두 `--fx-plus-fs: 34px`
   로 같다(`probe58s` 확인). 잉크가 갈리는 이유를 먼저 잴 것(글리프 구성 · 스트로크 · 애니 scale 위상).
6. **플로터 이동 방향이 두 갈래** — gain·quest 는 아래로 흐르고(+29 / +11px) upg 만 위로(−30px).
   규격의 «위로 떠오른다» 를 지키는 것은 upg 뿐이다. ⚠ 아래로 흘리는 것은 10회차에 «알약이 화면
   최상단이라 위로 띄우면 HUD 숫자를 가린다» 로 정한 것이다 — 되돌리기 전에 그 이유부터 볼 것.
7. **[gain t=110] 퍼짐 16개의 상호 가림 64%**(개당 보이는 면적 273/755px²). 패널 밖 부채꼴은
   이번 회차에 안 건드렸다 — 밴드 스프레이를 패널 밖에도 적용할지 25회차가 판단할 것.

### 24차 비평 AQ — **① 3 / ② 5 / ③ 4 / ④ 4**

| 축 | AQ | AR | 23회차(AN/AP) |
|---|---|---|---|
| ① 타이밍 | **3** | **3** | 3 / 3 |
| ② 크기 비율 | **5** | 4 | 5 / 4 |
| ③ 위치·경로 | **4** | 4 | 4 / 3 |
| ④ 가독성·일관성 | **4** | 4 | 3 / 4 |

**24회차 수정 4건의 회수 판정 — 두 사람이 독립적으로 확인했다**
- **밴드 스프레이 — 회수(✅ 2인 일치).** AQ «밴드 하단 1010 vs 행 제목 상단 1020(여유 10px),
  게이지 1083~1115 · 버튼 1015~1117 과 전혀 겹치지 않음. **이번 회차 변경의 목적은 달성**» ·
  AR «행 제목·게이지·버튼을 **한 픽셀도 안 가린다**. 골드와 다이아가 좌우로 완전 분리되어
  어느 재화인지 즉시 읽힌다». 23회차 96.9% → **0**.
- **착지 페이드 지연 — 회수(✅ 2인 일치).** AQ «도착 정확도 양호 — 골드 (590~602,60~85) ·
  다이아 (845~865,57~87)» · AR «착지 정확도 **1px 이내** — (583,53)/(584,54) ↔ 알약 (583.5,54.0)».
  23차의 «못 닿고 증발한다» 가 두 사람 모두에게서 사라졌다.
- **upg 아이콘 회피 — 회수(✅ 2인 일치).** 둘 다 «플로터 최고점 y1665~1695 vs 아이콘 하단
  1630~1660 → **한 프레임도 안 가린다**». 92% → 0.
- **플로터 크기 통일 — AQ 회수 / AR 미회수(판정 갈림).** AQ «외곽선 포함 텍스트 높이 gain 36 vs
  upg 35(**Δ2.8%**) · 채움 캡 높이 gain 23 / quest 22 — 규격 «한 값» 충족» 으로 **잘 되어 있는 것**에
  넣었다. AR 은 «upg 26 vs 23(+13%)» 로 감점했다. 선언값은 세 씬 모두 `--fx-plus-fs: 34px` 로
  같고(`probe58s`), **AQ 가 동일 마스크·외곽선 포함으로 쟀다** — 25회차는 AQ 의 측정법을 따를 것.
- ⚠ **되돌리지 말 것 (AQ 가 명시)**: «gain 4→5(284→364, 80ms 이동 11px) · quest 4→5(341→416,
  75ms 픽셀 동일)는 **설계된 머묾**이므로 정지 결함으로 감점하지 말 것».

### 반영 — 착수점 5 의 **부작용**을 2인 공통으로 잡혔다 (AQ ③-5·④-1 · AR ③-7·④-4)

플로터를 아이콘 밖으로 내린 대가로 카드 자신의 «+2.4»(`.cv`)·«공격력»(`.cn`)과
**295ms(플로터 수명의 60%) 동안 이중 인쇄**된다(AQ: t=240~535 · «두 값 모두 판독 불가»).
19회차의 «상승 거리 104px» 이 동시 노출을 막던 두 장치 중 하나였는데 이번에 80 으로 줄인 탓이다.

카드 안에는 아이콘도 텍스트도 안 건드리는 자리가 **없다** — y1630(아이콘 하단)~y1767(버튼 상단)을
`.cv`(1659~1719)와 `.cn`(1713~1761)이 빈틈없이 채운다. AQ 의 처방(«종점을 y1650 이상으로»)은
그 자리가 아이콘이라 쓸 수 없고, 시작을 더 내리면 버튼을 문다.
→ **교훈 8 의 처방**(형제 퀘스트 행 `fxSolo` 와 같은 것): 지나가는 것을 못 막으면 **지나가는
자리를 죽인다**. ⚠ 22회차가 «`.cv` 를 투명도로 지우지 마라 — 새 값은 0ms 부터 alpha 1.0» 로
못 박았으므로 둘을 다르게 다룬다:
- **`.cn`(정적 라벨 «공격력») 은 확실히 죽인다**(opacity .12) — 플로터가 그 단어를 이미 말한다.
- **`.cv`(새 값 «+2.4») 는 옅게만**(.42) — 지우는 것이 아니라 뒤로 물린다. 계속 읽힌다.
플로터 수명(620ms)에만 걸고, 꾹 누르기 반복 강화에서 타이머가 겹쳐 «영원히 죽은 줄» 이 남지
않도록 카드마다 하나만 건다.

## 25회차가 잡을 것

0. **`cap58.js` 가 이제 `pxdup58.py` 를 자동으로 돌린다. 그 출력 2줄(«부분 중복 페인트»)을
   비평가 브리프에 그대로 옮겨라.** 23·24 두 회차 연속 ① 최대 감점이 이 계열이었다.
   r24 기준 경고 대상은 **gain 10↔11 · quest 16↔17**.
1. **quest 3박자가 gain 보다 230~390ms 늦다 — 2인 공통, 이번 라운드 ① 병목.**
   AQ «첫 도착 ∈(850,1010] · 마지막 1.43~1.48s(규격 1.22 대비 +213~260ms) · 퍼짐이 ≤85ms 로 압축»
   · AR «3박자 전체 +230ms · 첫 도착 t≈729». gain 은 규격에 맞는다(첫 500~575 · 마지막 1209~1312).
   ⚠ **먼저 하네스인지 가릴 것** — quest 캡처는 목표 대비 ±281ms 로 세 씬 중 가장 어긋나 있고
   (t = -257, 30, **256**, 341 … 30→256 사이 226ms 에 원본 프레임이 없다), `probe58q` 의 DOM 실측은
   퍼짐이 t=88 에 이미 16개 가시 · t=208 에 완료다. **DOM 과 파일이 또 갈리면 0번과 같은 계열이다.**
2. **시각 도착과 알약 숫자가 어긋난다 — 2인 공통.** AQ «gain 40~110ms · quest 150~190ms 지연» ·
   AR «220~280ms». 씬별로 지연량이 다르다는 AQ 의 관찰이 단서다.
3. **초록 «획득» 플래시가 숫자 증가보다 250~530ms 먼저 끝난다(AR ①-4·④-5).** 두 신호가 한 프레임도
   안 겹친다 — «성공» 과 «얼마» 가 따로 논다.
4. **퍼짐 상호 가림 — 2인 공통.** AQ «gain t=110 에 16개 중 7개가 폭 13~22px 로 가려져 개수를 셀 수
   없다» · AR «개당 보이는 면적 273/755px² = 64% 가림». **패널 밖 부채꼴은 이번 회차에 안 건드렸다** —
   밴드 스프레이를 패널 밖에도 적용할지 판단할 것(패널 밖은 «행» 이 없어 `fx3Escape` 가 null 이다).
5. **플로터 이동 방향이 두 갈래 — 2인 공통.** gain·quest 는 아래로(+11px), upg 만 위로(−68px).
   ⚠ 아래로 흘리는 것은 10회차에 «알약이 화면 최상단이라 위로 띄우면 HUD 숫자를 가린다» 로 정한
   것이다 — 되돌리기 전에 그 이유부터 볼 것.
6. **[AQ ③-2] gain 비행 코인이 STAGE 진행바(y249~331) 뒤로 들어가 그 구간에서만 15% 어둡다**
   (코인당 ≈75ms). z-order 문제라 싸다.

### 25회차가 **다시 시도하면 안 되는 것**
- 23회차 목록(스태거 40ms · 흡수 중 y 하강 · 상승 선행 · upg 값 +0.2) 전부 유효하다.
- **quest 경로의 «오른쪽 우회»(최대 x≈1032, 팝업 밖 61px) 를 «제어점 실수» 로 보고 지우는 것.**
  AQ ③-1 · AR ③-3 이 2인 공통으로 냈지만 **그것은 설계다** — 6회차의 «두 구간 경로»(복도 = 패널
  우측 바깥)가 `verify93` [2b] «형제 행 관통 0» 을 세우는 장치다. 지우면 그 게이트가 먼저 깨진다.
  잡으려면 «경로» 가 아니라 «팝업 테두리(x≈984)를 코인이 절반쯤 넘는 것» 을 잡아라.
- **델타 플로터를 다시 위로 올려 아이콘 쪽으로 되돌리는 것.** 24회차가 92%→0 으로 회수했고
  두 비평가가 각각 «한 프레임도 안 가린다» 로 확인했다. 이중 인쇄는 위 `.fx-hush` 로 처리했다.

## 25회차 (2026-08-26, sess-1257-14025 워커 B) — 24회차가 반납한 lock 을 이어받기

24회차 §«25회차가 잡을 것» 의 0~6 중 **4(퍼짐 상호 가림)·3(초록 플래시 어긋남)·6(z-order)** 을 처리했다.
이번 회차도 24회차와 같은 패턴이 하나 나왔다 — **6번은 «게임» 도 «하네스» 도 아니고 «비평가의 원인 추정»이
틀린 것**이었다(AQ 가 «z-order 문제라 싸다» 로 분류했지만 z-order 는 이미 옳다).

### 신설 도구 2종 — 고치기 전에 먼저 쟀다

| 도구 | 무엇을 재나 |
|---|---|
| `probe58u.js` | 패널 «밖»(gain 씬) 퍼짐의 **가시 면적률**(합집합/합계, 1px 그리드) · 최근접 이웃 중심거리(중앙값·최소) · 뭉치 bbox · STAGE 바 위 코인의 최상위 요소 |
| `probe58v.js` | HUD 골드 알약의 **초록 틴트 구간**(`jz-up-n` 부착 [시작,끝])과 **표시 숫자가 실제로 바뀌는 구간**, 그리고 둘의 **겹침 ms** |

### 반영 1 — ④ 퍼짐 상호 가림: 패널 «밖» 도 밴드 스프레이로 (AQ ②-1 · AR ②-1, 2인 공통)

24차 비평은 «gain t=110 에 16개 중 7개가 폭 13~22px 로 가려져 개수를 셀 수 없다»(AQ) ·
«개당 보이는 면적 273/755px² = **64% 가림**»(AR) 로 같은 것을 냈다.
24회차의 밴드 스프레이는 **패널 «안» 에만** 걸렸다 — 패널 밖은 행이 없어 `fx3Escape` 가 null 이라
종전 부채꼴(−84°~−16° · r 106~184)이 그대로 남아 있었다. `probe58u` 로 재니 AR 이 맞다:

| 시각 | 가시 면적률 | 최근접 이웃(중앙값) | 코인 지름 | 뭉치 bbox |
|---|---|---|---|---|
| t=103 | **28.6%** | **9.3px** | 37px | 93×87 |
| t=170 | 52.1% | 21.4px | 43px | 163×146 |
| t=237 | 59.2% | 25.0px | 45px | 186×164 |

**부채꼴의 기하학적 한계다.** 68° 섹터의 r=145 호는 길이가 172px 이라 16개를 놓으면
슬롯 간격이 **10.8px** 밖에 안 나온다 — 지름 45px 코인이 4겹으로 겹친다. 반경을 넓혀도
`verify93` [1] «퍼짐 평균 반경 95~195px» 에 먼저 걸린다.

→ 24회차가 패널 «안» 에서 쓴 것과 같은 처방: **가로 밴드 2행 스프레이**.
   `FX3_OBX 230`(출발점 기준 좌우 반폭) · `FX3_OBY 120`(밴드 중심이 출발점보다 위) ·
   `FX3_OBROW 76`(행 간격). 16개 → 2행 × 8열이라 열 피치 57.5px · 행 간격 76px 로
   **둘 다 코인 지름(45)보다 크다.** 칸 안 지터는 ±4.6px 로 묶었다(넓게 흔들면 이웃 칸과의
   최소 간격이 다시 지름 아래로 떨어진다).

| 시각 | 가시 면적률 | 최근접(중앙값 / 최소) | 뭉치 bbox |
|---|---|---|---|
| t=97 | **68.1%** (← 28.6) | 20.0 / 16.7 | 187×85 |
| t=130 | **97.1%** | 35.9 / 32.8 | 302×113 |
| t=192~413 | **≈100%(포화)** | 49~55 / 32~50 | 421×131 → 457×153 |

⚠ 표의 «≈100%» 는 측정 방식의 포화다(합집합은 1px 그리드로 정수 스냅, 합계는 float 면적이라
겹침 0 이면 100 을 살짝 넘는다). **겹침이 0 이라는 뜻이지 100% 초과라는 뜻이 아니다.**

**`verify93` 을 지키느라 두 번 깎았다.** 첫 판은 반폭 290 · 위 170 이었는데 그 배치의
«퍼짐 평균 반경» 이 **232px** 로 [1] 게이트(95~195)를 깼다. 게이트가 재는 것은 개별 최대가 아니라
**출발점에서의 평균 거리**이므로, 반폭 230 · 위 120 으로 낮춰 평균 **190px** 에 맞췄다.
(93 은 다른 세션이 잡고 있는 작업이라 게이트 쪽은 한 줄도 안 건드렸다.)

### 반영 2 — ① 초록 «획득» 플래시가 숫자 증가와 한 프레임도 안 겹친다 (AR ①-4·④-5)

원인은 **틴트가 «표시값» 이 아니라 «실제 보유량» 을 보고 있었다**는 것이다.
`jzTint('goldN', S.gold)` — `S.gold` 는 클릭 즉시 오르지만, 알약 숫자는 58 `fxHold` 가 붙잡아
**코인이 꽂힐 때부터** 오른다. 그래서 초록 플래시는 t≈0 에 시작해 400ms 에 끝나고, 숫자는 그 뒤
500~1300ms 에 올랐다 — AR 이 «250~530ms 먼저 끝난다 · 두 신호가 한 프레임도 안 겹친다» 로
잡은 그대로다.

→ HUD 재화 2종만 **표시값(`fxVal`, 58 `fxDisp`)** 에 물린다(`jzTintRoll`). 전투력(`cpN`)은
`jzRollVal` 이 표시값을 직접 굴리므로 종전 `jzTint` 그대로 뒀다.
⚠ 표시값은 롤링 중 매 프레임 바뀌므로 그대로 걸면 애니메이션이 계속 재시작돼 **영원히 초록**이 된다 —
한 번 켜면 620ms 동안은 다시 안 켠다.

`probe58v` 실측 (틴트 구간 / 숫자 변화 구간 / 겹침):

| 씬 | 고치기 전 | 고친 뒤 |
|---|---|---|
| gain | 틴트 ≈0~400 · 숫자 571~1312 → **겹침 0ms** | 틴트 646~1661 · 숫자 646~1299 → **겹침 653ms** |
| quest | 같은 계열(플래시가 먼저 끝남) | 틴트 642~1650 · 숫자 642~1353 → **겹침 711ms** |

두 씬 모두 **틴트 시작 시각과 첫 숫자 변화 시각이 같은 ms** 로 붙었다.

### ⚑ 착수점 6 은 z-order 가 아니었다 — «원인 추정» 이 틀린 첫 사례

AQ ③-2 «gain 비행 코인이 STAGE 진행바(y249~331) 뒤로 들어가 그 구간에서만 15% 어둡다.
**z-order 문제라 싸다**» 는 처방까지 붙어 있었지만, 스태킹을 실제로 걸어 보면 코인이 이미 위다:

```
#fxl    z=60  <- #app(transform → 스태킹 맥락) <- #wrap <- BODY
#stinfo z=3   <- #stagearea(z=auto·position:relative → 스태킹 맥락을 **안 만든다**) <- #app <- …
```
`#stagearea` 는 `z-index:auto` 라 스태킹 맥락이 아니므로 `#stinfo` 는 `#app` 맥락에서 **z=3** 으로
참여한다. 60 > 3 — **코인이 바 위에 그려진다.**

관측된 «15% 어둡다» 는 진짜지만 원인이 다르다: 코인 아이콘(125 의 SVG)은 가장자리가 반투명이라,
그 픽셀이 **뒤에 무엇이 있느냐로 밝기가 갈린다.** 전투 배경(밝음) 위에서는 밝게, STAGE 캡슐
(`background:rgba(0,0,0,.30)`) 위에서는 어둡게 합성된다. z-order 를 아무리 올려도 안 바뀐다.

⚠ **함정 하나 더 기록해 둔다.** 처음에 `document.elementFromPoint()` 로 «최상위 요소» 를 물었더니
전 표본이 `#view` 로 나와 «코인이 뒤에 있다» 로 읽힐 뻔했다 — `#fxl` 은 `pointer-events:none` 이라
**히트 테스트에서 통째로 건너뛴다.** 페인트 순서를 elementFromPoint 로 재면 안 된다.

→ 이번 회차에는 **안 고쳤다.** 고치려면 코인에 상시 배경(글로우·불투명 판)을 깔아야 하는데 그것은
세 씬 전부의 코인 «모양» 을 바꾸는 일이라, 25차 비평이 이 자리를 다시 짚는지 보고 26회차가 정한다.
경로 쪽으로는 회수가 안 된다 — 바가 x320~800 을 가로지르고 알약이 x583 이라 **반드시 지난다**
(9회차 `FX3_BOWMAX` 주석의 «기하학적 한계» 와 같은 자리).

### 게이트 (25회차)

`VERIFY58 PASS` · `VERIFY93 PASS`(퍼짐 평균 반경 190px) · `VERIFY102 85/85 PASS` · `SMOKE PASS`.

### 25차 비평 (비평가 AS·AT)

| 축 | AS | AT | 24회차(AQ/AR) |
|---|---|---|---|
| ① 타이밍 | **3** | **3** | 3 / 3 |
| ② 크기 비율 | **4** | **4** | 5 / 4 |
| ③ 위치·경로 | **3** | **3** | 4 / 4 |
| ④ 가독성·일관성 | **3** | **2** | 4 / 4 |
| 최저 | **3** | **2** | 3 / 3 |

**③④ 가 24회차보다 떨어졌다.** 다만 두 사람이 이번에 새로 쟀을 뿐 회귀는 아니다 — 아래 «2인 공통»
7건 중 다섯 건은 앞 라운드들이 «한 사람만» 냈던 자리이고, 25회차가 손댄 두 구간(퍼짐 상호 가림 ·
초록 플래시)은 **두 사람 모두 재지적하지 않았다**(반영 1·2 는 회수된 것으로 본다).

#### 25회차 반영 2건의 회수 판정
- **패널 밖 밴드 스프레이 — 회수.** ②「상호 가림」이 두 사람의 지적표에서 사라졌다. 다만 **성격이
  바뀐 지적이 새로 붙었다**: AS ③-5 «8개×2행, 행내 피치 54.4px(σ 2.3) · 행 간격 75px — 물리적
  산란이 아니라 **테이블 레이아웃**으로 읽힌다» · AT ③-5 «가로 411 대 세로 87 = 4.7:1, 사실상 8×2 표».
  → 겹침은 풀렸는데 **격자로 보인다**. 26회차는 «겹침 0» 을 유지한 채 규칙성만 깨야 한다
  (열 지터를 ±4.6 → ±14px 로 키우고 행 y 를 열마다 ±18px 흔들면 최근접 최소가 45 아래로 안 내려간다 —
  계산: 피치 57.5 − 28 = 29.5px 은 지름 아래이므로 **열 지터는 ±10px 상한**, 대신 행 y 지터를 쓴다).
- **초록 플래시 ↔ 숫자 겹침 — 회수(지적표에서 사라짐).** `probe58v` 실측 겹침 gain 653ms · quest 711ms.
  ⚠ 대신 AT ④-5 가 «HUD 알약 2개가 f3→f4 한 프레임에 휘도 +105%/+97% 로 튄다» 를 새로 냈다.
  이것은 초록 틴트가 아니라 **팝업 워시(아래 §④-3)의 해제** 다 — 두 사람이 같은 현상을 다른 쪽에서 봤다.

#### 2인 공통 (26회차가 잡을 것 — 우선순위 순)

1. **① 숫자가 코인을 앞선다.** AS ①-5·6·7 «크레딧 vs 실도착: A 최대 5/16(31%) · B 6/16(37.5%) ·
   카운터가 마지막 코인보다 A 126~208ms · B ≥161ms 먼저 끝난다» · AT ①-1·④-6 «B t=664 첫 틱 때
   최근접 코인이 알약에서 923px 밖 · 최대 격차 +31.25%p / +37.5%p». **두 사람의 1순위 처방이 같다** —
   «숫자 틱을 도착 이벤트에 하드 바인딩». ⚠ 93 4회차의 «도착 계단»(`fxStepTo`)이 이미 그 구조인데
   실측이 어긋난다 → **계단이 도착이 아니라 무엇에 물려 있는지부터 재라**(`f.batch.landed/cnt` 가
   아니라 롤링 보간이 남아 있을 가능성. `fxRollLin` 경로를 probe 로 확인할 것).
2. **① 씬 B 에 3박자의 1박(퍼짐)이 없다.** AS ①-3 «보상 아이콘 첫 픽셀이 368ms — 규격 220ms 보다
   +148ms 늦고, 게다가 이미 퍼진 폭 430px 로 팝인한다» · AT ①-6 «B 클러스터 폭 202→210→189px,
   177ms 동안 ±5%. 같은 구간 A 는 179→382→411px(+130%)». **같은 모듈인데 한 씬만 2박자다.**
3. **④ 씬 C 델타 플로터가 카드 라벨을 100% 덮는다.** AS ④-1 «플로터 박스 x[122,277] y[1664,1711] 이
   라벨 박스 x[159,237] y[1669,1705] 를 x·y 100% 로 완전 포함 — 둘 다 안 읽힌다» ·
   AT ④-1 «f7(550ms)에 완전 중첩, 세로 간격 f3 10px → f6 21px 겹침 → f7 0px 분리».
   ⚠ **24회차의 `.fx-hush` 처방이 못 막았다.** 게다가 두 사람이 그 부작용을 같이 냈다 —
   AS ④-2 · AT ④-2 «t=252~406(154ms) 카드 라벨 초록 채도 픽셀 949→**0**, t=647 에도 기준의 50%».
   즉 `.cv .42 / .cn .12` 는 «뒤로 물린» 게 아니라 **지운** 것이다. 두 사람 다 처방이 같다:
   **겹치지 않게 옮기고**(카드 우측 바깥 또는 Lv 배지 아래), 문자열을 결과값 «+2.4» 가 아니라
   **증분 «+0.2»** 로 바꿔 포맷을 분리하라. 그러면 `.fx-hush` 자체가 필요 없어진다.
4. **③ 씬 B 골드 복도가 팝업 테두리를 절반 물고 간다.** AS ③-3 «코인 중심 cx≈979.5, 폭 45 →
   x[957,1002]. 테두리 스트로크 x[970,980] 을 전부 가린 채 다섯 슬롯 연속» ·
   AT ③-3 «코인 x[957-1002], 테두리 중심선 x=979 와 1px 오차로 일치 — 지름의 43% 관통».
   두 사람 다 «다이아 레인 x≈1019~1020 이 정답이니 골드를 거기 맞춰라» 로 처방이 같다.
   → **`probe58w` 신설로 코드에서 직접 쟀다(아래 §«26회차가 풀어야 할 기하 충돌»).**
5. **② 비행 다이아가 골드 대비 과소.** AS ②-3 «비행 다이아 29×37 vs 코인 45×45. HUD 대비 비율이
   코인 88% · 다이아 71% → 다이아는 36px 여야 한다(−19% 미달)» ·
   AT ②-1 «비행 골드 44~46 : 다이아 23~26 = 1.80:1 인데 목적지 HUD 는 55:47 = 1.17:1».
   두 실측이 다른 방법으로 같은 결론에 왔다.
6. **② 코인 크기 곡선이 비단조다.** AS ②-1 «30.8 → 46.6 → 36.2px = +51% 팽창 후 −22% 수축» ·
   AT ②-3 «25 → 31 → 45 → 25px. 최대치가 목적지가 아니라 **경로 중간**에 온다».
   AS ②-2 는 한 프레임 안 편차까지 냈다(t=458 에 29.5~45.0px, **1.53배**).
7. **④ 플로터가 «흐르지» 않는다 · 씬마다 운동 스펙이 다르다.** AS ①-9·④-6 «A 12px/646ms ·
   B 10px/607ms = 자기 글자 높이(24px)의 0.5배. C 는 위로 40px/350ms — **속도 6.7배 · 이동량 3.6배**
   차이» · AT ④-4 «A 12 / B 11~13 / C **0px**». 수명도 A ≈785ms · B ≥607ms(캡처 끝까지 페이드 시작
   없음) · C ≈450ms 로 갈린다.

#### 1인 지적 중 값이 큰 것 (26회차가 다른 쪽 채점과 겹치는지 확인하고 확정)
- **AS ④-3 «씬 B 팝업 워시»** — 368~1210ms(842ms) 동안 팝업 전체가 워시돼 형제 행 버튼의 대비가
  8.1:1 → 3.9:1(−52%)로 떨어지고, t=1330 에 **1프레임(0ms)에 스냅 오프**하며 152,344px 를 재도장한다
  (들어올 때는 118ms 페이드 — 인/아웃 비대칭). 씬 A·C 엔 이 장치가 없어 톤이 어긋난다.
  AT 는 같은 것을 **HUD 알약 쪽에서** 봤다(④-5 «알약만 약 50% 디밍돼 있다가 중간 프레임 없이 100% 로»).
  → 사실상 2인 공통으로 봐도 된다. 아웃을 120ms 페이드로 만들고 **마지막 아이콘 흡수 이후**로 옮길 것.
- **AS ③-1 «씬 B 스폰 앵커가 눌린 버튼에 안 붙어 있다»** — 버스트 중심 (459.5,981) vs 버튼 중심
  (819,1066), **Δ370px**. 씬 A 는 Δ(+3.5,−107)로 정상. AT ③-4 가 같은 것을 보상 아이콘 기준으로 냈다
  («버스트 중심 y≈983 vs 보상 아이콘 중심 (219,1066) — 83px 위»). ⚠ 두 사람이 **서로 다른 앵커**를
  정답으로 지목한다(AS=버튼 · AT=보상 아이콘). 26회차가 «어느 쪽이 인과인지» 를 먼저 정할 것.
- **AS ③-2 · AT ③-4 «스폰 밴드가 형제 행 카드 하단선을 밟는다»** — 밴드 y[950,1012], 위 형제 행
  카드 하단 y≈950. 24회차가 «행 안 빈 밴드» 로 옮긴 자리가 **거터 11px 를 전부 덮고 있다**.

#### ⛔ 오독 확정 — 26회차는 이걸로 감점을 받아도 되돌리지 마라
**AT ③-1 «코인이 알약 중심 14px 안에 들어온 프레임이 0개 · 평균 25~41px 어긋나 아이콘 하단
테두리에 매달린 채 사라진다»** 는 **다섯 라운드 연속 같은 오독**이다(LESSONS «비평가의 «도착 실패» 는
재화별로 «제» 알약까지 재기 전에는 믿지 마라»). `probe58r` 재실행 실측:

```
알약 중심 골드 (583.5,54.5) · 다이아 (843.5,54.5)
16/16 이 제 알약 중심 0.0~1.3px 에서 소멸. 20px 넘게 떨어져 사라진 아이콘 0/16.
```
**AS 는 씬 A 에서 이것을 정확히 봤다**(잘된 점 2: «착지 코인 중심 (584,51) vs (583.5,53.5) → 오차 2.4px ·
#12·#14·#15 세 프레임에 걸쳐 «꽂힌 그림» 이 보인다»). 즉 도착은 옳다.

⚑ **다만 AS 는 «씬 B 에서는 그 그림이 한 프레임도 안 보인다»(③-4)고 했고, 이건 새 정보다.**
AT 의 «잘된 점 6» 이 그 원인을 우연히 적어 놨다 — «도착 코인이 알약 아이콘 **뒤**(z-order 하위)로
렌더링돼 숫자를 안 가린다. 침범한 코인 34프레임 중 0개».
→ **26회차 가설: 씬 B 는 도착 홀드(≈110ms) 동안 코인이 알약 아이콘에 완전히 가려 «안 보인다».**
   `probe58r` 의 «비행끝t → 소멸t» 간격과 그 구간의 코인 z·크기를 재서 확정할 것. 씬 A 가 보이는
   이유는 착지 scale(.5 = 27.5px)이 골드 아이콘(55px)보다 작아 **가려지는데도** A 에서는 마지막
   주행 프레임이 캡처에 걸리기 때문일 수 있다 — 그렇다면 «도착이 한 프레임 이상 보인다» 규격을
   만족시키는 것은 홀드 길이가 아니라 **착지 크기·z** 다.

### 26회차가 풀어야 할 기하 충돌 — 복도 두 레인이 프레임에 안 들어간다 (`probe58w`)

2인 공통 4번의 처방(«골드를 다이아 레인에 맞춰라»)은 **그대로는 실행할 수 없다.** 코드에서 직접 잰 값:

| 값 | 실측 |
|---|---|
| `#mbox` 우변 / 테두리 | **974** / 3px(971~974) |
| `outX = fx3Out(p0)` | **1020** (= 우변 974 + `FX3_OUTM` 46) |
| 골드 레인 `ox = outX − FX3_LANEX(40)` | **980** → 코인 폭 62 → x[949, **1011**] — 우변을 25px **안쪽**까지 문다 |
| 다이아 레인 `ox = outX` | **1020** → 폭 53.8 → x[993, 1047] — 우변 밖 19px, 깨끗하다 |
| `FX3_XCAP` (아이콘 중심 상한) | **1040** (= FRAME_W − 40, «잘림 0» 보장) |

두 레인이 **동시에** ① 우변 974 밖(중심 ≥ 974 + 폭/2 + 여유 ≈ **1009**)이고 ② 중심 ≤ **1040** 이고
③ 서로 겹치지 않으려면(폭 62·54 → 중심 간격 ≥ **58**) — 쓸 수 있는 띠가 **1009~1040 = 31px** 뿐이라
**58px 를 넣을 수 없다.** 즉 «두 레인 분리»(7회차)와 «둘 다 테두리 밖»(25차 비평)은 **현재 코인 크기에서
양립 불가**다. 셋 중 하나를 포기해야 한다:

- **(가) 코인 크기를 줄인다 — 2인 공통 6번과 같은 방향이라 제일 싸다.** 복도 코인 폭 62 는
  «경로 중간 최대치»(AS ②-1 «+51% 팽창») 다. 이것을 **45px 상수**로 만들면 필요한 하한이
  974+22.5+4 = **1000.5**, 상한은 FRAME_W−35.5 = **1044.5** → 띠 44px, 필요 간격 45px 로 **1px 부족**.
  거의 닿는다 — 여기에 `FX3_OUTM` 을 46 → 40 으로 6px 당기면 성립한다.
- **(나) 레인을 x 가 아니라 «시간» 으로 가른다.** 7회차가 x 로 가른 이유는 «복도에서 다시 한 줄로
  모여 중심거리 21px» 이었다. 재화별 출발 스태거를 벌리면 같은 x 라도 안 겹친다.
- **(다) 골드만 테두리 밖으로 내보내고 다이아를 안쪽에 둔다** — 다이아가 대신 물게 되므로 **기각**
  (두 비평가가 다이아 레인을 «정답» 으로 지목했다).

→ **26회차는 (가) 를 먼저 시도할 것.** 2인 공통 6번(크기 곡선 단조화)을 하면 4번이 따라 풀린다.

### 25회차가 **다시 시도하면 안 되는 것**
- 23·24회차 목록(스태거 40ms · 흡수 중 y 하강 · 상승 선행 · upg 값 +0.2 · 델타 플로터를 아이콘 쪽으로
  되올리기 · quest 경로의 오른쪽 우회를 «제어점 실수» 로 보고 지우기) **전부 유효하다.**
- **델타 플로터 x 를 «알약 중심» 으로 되돌리는 것.** AS ③-6 · AT ③-2 가 2인 공통으로
  «플로터 중심이 알약 중심 +156~159px — 자기 알약보다 남의 알약에 가깝다» 를 냈지만, 그 +52 는
  19회차에 AE·AF 가 **2인 공통으로 요구한 되돌림**이다: 코인의 도착점은 알약 «중심» 이 아니라
  **왼쪽 끝 아이콘**(골드 583~638)이고, 중심에서 왼쪽으로 빼면 플로터가 코인 열 한복판에 앉아
  «+12◯A» 처럼 글자가 가려진다(AE 5프레임 470ms · AF 3프레임 280ms 실측).
  ⚠ 두 비평가가 «알약 중심» 이라 부른 것은 **아이콘 중심(583.5)** 이고, 실제 캡슐은 583~794 라
  **중심이 688.5** 다 — 플로터(740)는 **자기 캡슐 «안»** 에 있다. 지적의 값은 «캡슐 우변 794 를
  24px 넘어 두 캡슐 사이 빈 구간(794~844)으로 새는 것» 이지 «남의 알약에 가까운 것» 이 아니다.
  → 되돌리지 말고 **플로터 실폭을 캡슐 안으로 클램프**(우변 ≤ 794)하는 쪽으로 풀어라.

### 게이트 (25회차 · 최종)

`VERIFY58 PASS` · `VERIFY93 PASS`(퍼짐 평균 반경 190px) · `VERIFY102 85/85 PASS` · `SMOKE PASS`.

---

## 26회차 (2026-08-26, sess-1357-20310 워커 D) — 25회차가 반납한 lock 을 이어받기

25회차가 «26회차는 **수정부터**» 로 반납했으므로 캡처 전에 고쳤다. 잡은 것은 25차 «2인 공통»
7건 중 **③④⑤⑥ 네 건**과 25차가 회수 판정에 붙여 둔 «격자로 보인다» 한 건이다.
①(숫자가 코인을 앞선다)·②(씬 B 에 퍼짐 1박이 없다)·⑦(플로터 운동 스펙)은 이번 회차 범위 밖 —
아래 §«27회차가 잡을 것» 에 근거와 함께 넘긴다.

### 신설 도구 2종 — 고치기 전에 먼저 쟀다 (24·25회차와 같은 순서)

| 도구 | 무엇을 재나 | 왜 |
|---|---|---|
| `probe58x.js` | 비행 아이콘 rect 폭을 **위상 버킷**(퍼짐·머묾·비행초·비행중·비행말·착지)별 중앙값으로, 재화별로. 같은 프레임의 골드:다이아 비, 폭 최대치가 **어느 위상에 오는가**, HUD 알약 아이콘 실측 | ⑥ «비단조» 와 ⑤ «다이아 과소» 는 둘 다 «어느 위상에서 재느냐» 로 값이 갈린다 — 비평가가 재는 것과 같은 양을 위상째로 모아야 판정이 선다 |
| `probe58y.js` | 훈련 카드 부분요소 rect(카드 기준 좌표) + `.cv`/`.cn` **잉크** bbox + 트리거 후 플로터 rect·라벨 opacity·겹침률(가로·세로) 시간축 | ③ «플로터가 라벨을 100% 덮는다» 와 그 부작용 «`.fx-hush` 가 라벨을 지웠다» 를 수치로 확정 |

### 반영 1 — ⑥ 코인 크기 곡선을 단조로 (AS ②-1·②-2 · AT ②-3, 2인 공통)

`probe58x` 가 두 사람과 **같은 그림**을 냈다(골드 rect 중앙값):

| 위상 | 수정 전 | 수정 후 |
|---|---|---|
| 퍼짐 | 41.7 | 45.2 |
| 머묾 | 42.9 | 47.1 |
| 비행 초 | 50.3 | 47.8 |
| **비행 중** | **58.8 ← 최대** | 43.4 |
| 비행 말 | 43.8 | 33.0 |
| 착지 | 28.8 | **28.2** |
| 폭 최대치가 온 위상 | `d-fly-mid` | **`a-spread`** |

원인은 23회차가 남겨 둔 **복귀 램프**였다 — `grow = 0.70 + 0.30·min(1, q/0.35)` 가 흡수 앞 35%
동안 배율을 0.70 → 1.00 으로 되올린다. 23회차는 봉우리를 1.10 → 1.00 으로만 낮췄지 램프 자체를
안 없앴고, 그래서 «+51% 팽창 후 −22% 수축» 이 그대로 남았다(AT: «최대치가 목적지가 아니라
**경로 중간**에 온다»).

→ 램프를 없애고 **흡수 내내 머묾과 같은 배율**(`FX3_FLYS` 0.70)로 간다. 도착 배율은
`FX3_LAND` 0.50 으로 **한 픽셀도 안 바뀐다** — 램프 계수를 상수 0.5 가 아니라 `grow − FX3_LAND`
로 써서 z=1 에서 정확히 0.50 에 떨어지게 했다(착지 실측 28.8 → 28.2, 프레임 표본 차 안).
퍼짐 끝·머묾의 하드코딩 0.70 도 같은 상수로 묶었다.

⛔ 12회차의 «작게 흩어졌다가 제 크기로 복귀»(AH·AI)는 **이것으로 폐기**다. 그 뒤 AK(21) ·
AL·AM(23) · AS·AT(25) **여섯 사람**이 같은 팽창을 반복해서 감점했다. 되돌리지 마라.

### 반영 2 — ④ 골드 복도가 팝업 테두리를 문다 (AS ③-3 · AT ③-3, 2인 공통)

25회차가 `probe58w` 로 «두 레인을 둘 다 테두리 밖에 두면서 안 겹치게 할 띠가 31px 뿐(필요 58px)»
이라고 기하 충돌을 확정하고 해법 **(가) «복도 코인을 작게»** 를 먼저 시도하라고 남겼다.
반영 1 이 바로 그것이라 필요 간격이 저절로 내려갔다. 남은 일은 복도를 테두리 밖으로 미는 것뿐:
`FX3_OUTM` **46 → 66** (= `#mbox` 우변 974 + 66 → `FX3_XCAP` 1040 에 붙는다).

`probe58w` 실측(수정 전 → 후):

| | 수정 전 | 수정 후 |
|---|---|---|
| 골드 레인 중심 / 폭 | 980 / 60.9 → x[949.6, **1010.5**] | **1000** / 42.4 → x[**978.8**, 1021.2] |
| 다이아 레인 중심 / 폭 | 1017 / 52.3 | **1040** / 40.8 → 우변 1060.4 |
| `#mbox` 우변 · 테두리 | 974 · 971~974 | 같음 |
| 테두리 관통 | **−25.4px(스트로크 전부 가림)** | **없음 — 여유 +4.8px** |
| 프레임 우변까지 | — | 19.6px |

`verify93` [2c] 도 같은 값을 독립으로 찍는다: «복도 최소 x 골드 **1000** · 다이아 **1040**».
(다) «골드만 밖으로» 는 25차가 이미 기각했다. 46 → 66 은 «더 크게 돌아라» 가 아니다 —
어차피 XCAP 에 먼저 걸리므로 이 저장소 패널에서는 **복도가 XCAP 에 붙는다**는 뜻이고,
좁은 패널에서도 골드 레인 = 우변+26 이라 3px 테두리를 잉크 반폭(≈18) 기준 8px 여유로 비껴간다.

### 반영 3 — ⑤ 비행 다이아가 골드 대비 과소 (AS ②-3 · AT ②-1, 2인 공통)

두 사람의 **방향**은 맞았고 **분모**가 틀렸다. `probe58x` 실측:

- 비행 rect 중앙값 골드 58.8 : 다이아 52.5 (비 1.12) — 다이아가 실제로 작다.
- 목적지 HUD 알약 아이콘은 **골드 63×63 · 다이아 73.1×63**. 즉 **높이는 둘이 같고**,
  폭만 다이아가 넓다 — 그 16% 는 `#top .curs .cDia i>.cic{transform:scaleX(1.16)}` 때문이다.

AT 가 «목적지 55:47 = 1.17:1» 이라고 쓴 것은 폭·높이를 섞어 잡은 값이다. 목적지에서 두 재화가
같은 것은 **높이**이므로, 비행도 높이로 맞춘다: `FXCUR.dia.ics` **50 → 55**(골드와 동일),
`fs` 42 → 46. 수정 후 폭 최대치가 골드·다이아 **둘 다 정확히 55**, 위상별 중앙값 비 1.00~1.10.

⛔ 폭까지 맞추겠다고 비행 다이아에 `scaleX` 를 얹지 마라 — 그 규칙은 **A3(#top) 구간**이고,
58 이 남의 구간 결정을 복제하는 것이 된다. 남은 폭 차이는 A3 쪽 값이 정리되면 따라온다.

### 반영 4 — ③ 씬 C 델타 플로터가 카드 라벨을 100% 덮는다 (AS ④-1·④-2 · AT ④-1·④-2, 2인 공통 2건)

`probe58y` 실측(수정 전): 플로터 잉크 폭 **156.6**, 라벨 잉크 카드기준 x123~203(폭 80).
**가로 겹침률 100% — 전 프레임**, 세로 겹침 최대 87%, 겹친 프레임 **10/13**,
그 사이 라벨 opacity 최소 **0.12**(= `.fx-hush`. 두 사람이 «뒤로 문 게 아니라 지웠다» 로 잡은 그것).

⚑ **원인이 기하가 아니라 문자열이었다.** 플로터 «+2.2 **공격력**» 의 절반이 **바로 아래 줄이
상시로 말하고 있는 낱말**이다. 카드 폭 326 에 라벨이 가운데(123~203) 있으므로, 폭 156.6 짜리
글자는 **어느 x 에 둬도** 가로로 100% 포함한다 — 24회차가 «카드 안에 안 겹치는 자리가 없다» 고
결론 내린 것은 문자열을 상수로 두고 자리만 찾았기 때문이다. 낱말을 빼면 폭이 **156.6 → 65~68**
로 내려가 카드 우측 빈 자리(x203~326)에 통째로 들어간다.

고친 것 2줄: `trDeltaTxt()` 가 `.cv` 값만 돌려준다(라벨 낱말 제거) · `fxDelta()` 가 훈련 카드
(`.cv` 앵커)일 때만 `left` 를 `r.x + r.w×0.81`(카드기준 x≈264 = 빈 자리 한가운데)로 잡는다.
다른 씬(69 아이템 강화 등)은 앵커가 `.cv` 가 아니라 종전대로 가운데다.
그리고 **`.fx-hush` 를 CSS·JS 양쪽에서 폐기**했다 — 겹침이 0 이면 필요가 없고, 두 사람이 이
장치 자체를 감점했다.

| `probe58y` | 수정 전 | 수정 후 |
|---|---|---|
| 플로터 잉크 폭 | 156.6 | **65~68** |
| 라벨과 가로 겹침률 | **100% (13/13 프레임)** | **0% (0/13)** |
| 겹친 프레임 | 10/13 | **0/13** |
| 라벨 opacity 최소 | **0.12** | **1.00** |

⛔ 값을 «+0.2»(증분)로 바꾸는 처방은 **쓰지 않았다** — 23·24회차가 «거짓말» 로 못 박은 항목이다
(플레이어가 방금 얻은 것은 0.2 가 아니라 2.2 다. 19회차 `trainCardData()` 확인).
25차 두 사람이 그것을 다시 요구했지만, 겹침이 0 이 된 이상 포맷을 왜곡할 이유가 없다.

### 반영 5 — 25차 회수 판정에 붙어 온 «격자로 보인다» (AS ③-5 · AT ③-5)

25회차의 패널 «밖» 밴드 스프레이는 «상호 가림» 을 회수했지만 두 사람이 같이 **«8×2 표»**
로 읽었다(AS «행내 피치 54.4px σ 2.3 · 행 간격 75px» · AT «가로 411 대 세로 87 = 4.7:1»).
25차 처방은 «열 지터 ±10 · 행 y 지터 ±18» 인데, 반영 1 로 코인이 작아진 것을 감안해도
두 값을 한 회차에 같이 한계까지 밀면 회수 판정이 갈리므로 **보수적으로** 잡았다:
열 지터 ±4.6 → **±7**(피치 57.5 − 14 = 43.5 > 잉크 ≈36) · 행 y 지터 ±6 → **±14**
(`FX3_OBROW` 76 − 28 = 48 > 36). 겹침 0 은 유지된다.

### 게이트 (26회차)

`VERIFY58 PASS` · `VERIFY93 PASS` · `VERIFY102 85/85 PASS` · `SMOKE PASS` · 문법 OK.

### ⚑ 26회차 최대 수확 — ①-1 «숫자가 코인을 앞선다» 는 **계단 문제가 아니다** (`probe58z` 신설)

25차 2인 공통 **1순위**가 «숫자 틱을 도착 이벤트에 하드 바인딩하라» 였고, 25회차 리뷰가
«`fxStepTo` 가 이미 그 구조인데 실측이 어긋난다 → **계단이 도착이 아니라 무엇에 물려 있는지부터
재라**» 로 넘겼다. 이번 회차가 그 지시대로 쟀다.

`probe58z.js` 는 «도착» 을 **세 정의**로 동시에 세고 화면에 찍힌 카운터에서 «크레딧된 코인 수» 를
역산해 비교한다:
  (a) `flightDone` = `fxFlies` 에서 빠진 수 (비행 수식이 끝난 수 — `verify93` 이 쓰는 정의)
  (b) `gone` = DOM 에서 사라진 수 (**비평가가 세는 정의**)
  (c) `atPill` = 알약 60px 안에 있는 수 (도착 홀드·착지 페이드 중인 것)

| | 씬 A (gain, 골드 14개) | 씬 B (quest, 골드 8개) | 25차 비평가 값 |
|---|---|---|---|
| 크레딧이 **flightDone** 을 앞선 최대 | **0** | **0** | — |
| 크레딧이 **gone** 을 앞선 최대 | **5** (5/14 = 36%) | **3** (3/8 = **37.5%**) | AS «A 5/16(31%) · B 6/16(37.5%)» |
| 동시에 알약 위에 있는 코인 최대 | **4** | **4** | — |
| 카운터 완료 → 마지막 코인 소멸 | **+160ms** | **+158ms** | AS «A 126~208ms · B ≥161ms» |

**두 비평가의 «측정» 은 정확하다 — 재현했고 자릿수까지 같다. 틀린 것은 «처방» 이다.**
숫자 계단은 **이미 도착에 하드 바인딩돼 있고**(크레딧 − flightDone = **0**, 전 프레임),
격차의 전부가 **착지 잔류**다: 도착한 코인이 알약 위에서 `.fx-land2`(고원 45% · 0.10s) + 제거까지
≈160ms 를 더 «보이는» 상태로 앉아 있고, 도착 간격이 48ms 라 그 잔류가 항상 3~5개 겹쳐 있다.
즉 비평가가 센 «아직 도착 안 한 코인» 은 **이미 알약에 도착해 흡수되는 중인 코인**이다.

⛔ **27회차는 카운터를 다시 묶지 마라 — 묶여 있다.** 손댈 곳은 하나뿐이다: **착지 잔류 길이**.
   다만 그 값은 93 14회차가 **반대 방향으로** 정한 것이라(«폭 25~27.5px 코인이 알약 정중앙에
   180ms 앉아 있다 → .15s → **.10s**, 도착이 45ms 동안 opacity ≥.88 로 «보여야» 한다»)
   그냥 더 줄이면 «도착이 한 프레임도 안 보인다» 가 되돌아온다(11·13회차에 두 번 겪었다).
   → 길이가 아니라 **동시 잔류 수**를 줄이는 쪽으로 풀어라: 착지 페이드를 «도착 순서대로 큐» 로
     만들어 알약 위에 **동시에 최대 2개**만 남기면(3~5 → 2) 격차가 36% → 14% 로 내려가고,
     각 코인의 «보이는 45ms» 는 그대로다. 그래도 카운터 완료 뒤 꼬리 ≈160ms 는 남는데,
     그 꼬리는 «마지막 한 개의 흡수» 라 오히려 정상이다 — 그것까지 없애려는 처방은
     envelope 축소와 같은 벽에 부딪힌다(23회차 회수 불가 판정).

### 26차 비평 (비평가 AV) — **① 4 / ② 6 / ③ 4 / ④ 4 → 최저 4** (25차 AS 3 / AT 2)

회수 판정: **1 회수 · 2 부분 회수 · 3 회수 · 4 회수 · 5 회수 안 됨.**

| 주장 | AV 실측 |
|---|---|
| ⑥ 크기 곡선 단조 | **회수** — gain 잉크 폭 25~30 → 33 → **최대 35(f4, 퍼짐)** → 비행 31~34 → 착지 25. 비행 중 볼록부 **0건**, 최대/비행 1.06 |
| ④ 골드 복도 | **부분 회수** — −25.4px → **−6px**(잉크 폭의 18%, f8~f14 545ms). 이탈은 아니다 |
| ⑤ 다이아 크기 | **회수** — 복도 높이 골드 33~34 : 다이아 31~33, **Δ≤2px(6%)** |
| ③ 씬 C 플로터 | **회수** — 가로 겹침 **0px · 간격 28px · 7/7 프레임 0%**, 라벨 잉크 960~1001/1001 유지 |
| 격자 | **회수 안 됨** — 여전히 8열×2행. 열 잔차 rms 2.6~9.1px(피치의 5~16%), 두 행이 **39.3px 완전 분리·교차 0** |

#### ⚑ AV ③-3 가 **내 26회차 상수가 틀렸다**는 것을 잡았다 — 코드로 확인했고 맞다

AV: «팝업 실제 우측 경계는 크림 969 / 갈색 테두리 970~980 / **검정 외곽선 982~988** / 배경 989~.
커밋의 «우변 974» 는 **14px 틀렸다»**.

코드에서 재니 그대로다 — 이 팝업은 요소가 **두 겹**이다:

| 요소 | left | right | border |
|---|---|---|---|
| `#mbox` (안쪽 내용 상자) | 106 | **974** | 3px `#46382D` |
| `.mbox` (A5 공용 모달 껍데기) | 91 | **989** | **8px `#000`** |

`.mbox{border:8px;padding:0 7px 7px}` 이라 내용 상자가 정확히 91+15 = 106 · 989−15 = 974 다.
그런데 `FX3_PANEL = '#mbox, #panel, .eqp, .tr-sheet'` 에는 **`.mbox` 가 없다** — `fx3Out()` 이
껍데기가 아니라 **내용 상자**를 재고 있었다. 25회차의 `probe58w` 도 `#mbox` 만 찍었으므로
«우변 974» 라는 값이 25·26 두 회차에 걸쳐 그대로 이어졌다. 즉 이번 회차가 «+4.8px 여유» 라고
계산한 것은 **내용 상자 기준**이고, 화면에서는 껍데기 검정 테두리(981~989)를 **−5.5px** 문다.
AV 의 −6px 과 자릿수까지 맞는다.

→ 27회차의 1순위는 **상수를 밀어 올리는 것이 아니라 `FX3_PANEL` 에 `.mbox` 를 넣는 것**이다.
  값을 손으로 고치면 다음 팝업에서 같은 오차가 다시 난다(A5 껍데기는 16·22·69·87·103 … 공용).

#### AV 의 나머지 지적 중 값이 큰 것 (AU 채점과 겹치는지 확인하고 27회차가 확정)
- **①-1 quest 도입 306ms 무반응** — f2(66)↔f3(179) 화면 전체 동일(임계 30 초과 화소 **0**),
  첫 연출 f4(306) · 첫 재화 f5(366). `pxdup58` 이 면제한 쌍이 **아니다**.
- **①-2 quest 진짜 동결 2건** — f11↔f12(69ms) · f13↔f14(53ms), 추적 8개 중심 이동 **0.0px**.
- **③-2 · 새 부작용 «씬 B 퍼짐이 누른 버튼에서 안 나온다»** — 버스트 링은 버튼 둘레(x713~923)인데
  재화 16개는 **x267~654**. 한 프레임도 안 겹친다. 25차 AS ③-1(Δ370px)·AT ③-4 와 **같은 자리**다
  (25차는 «AS=버튼 · AT=보상 아이콘» 으로 정답 앵커가 갈렸는데, AV 가 버스트 링을 기준으로
  세 번째로 같은 것을 냈다 — 이제 앵커 불일치는 확정으로 봐도 된다).
- **④-1 씬 A 와 씬 B 의 퍼짐 대형이 다른 도형** — A 는 403×98 8열×2행인데 B 는 **387×31 16×1 한 줄**
  이고 골드 8개(x267~436)·다이아 8개(x482~654)가 종류별로 정렬돼 «막대그래프» 로 읽힌다.
  (원인 추정: 씬 B 는 `fx3Escape` 의 «행 안 빈 밴드» 경로라 `FX3_BSY` 26 = ±13 만 쓰고,
   씬 A 는 25회차의 2행 밴드다. 공용 모듈인데 분기가 둘이다.)
- **④-3 gain 카운터가 정답표보다 82~120ms 늦다(9스텝 연속)** — ⚠ `probe58z` 는 «크레딧 − 도착 = 0»
  이었다. 두 값이 같이 참일 수 있다(정답표는 **설계 시각**, probe 는 **실도착 대비**). 27회차가
  정답표 생성 쪽을 함께 봐야 판정된다 — AV 를 오독으로 몰지 말 것.
- **새 부작용 «씬 C 플래시가 라벨 대비를 깎는다»** — `.fx-hush` 는 뗐는데 플래시 자체가
  f3(157)에 «공격력» 잉크를 1001 → **579(−42%)**, 대비를 0.71 → **0.43** 으로 떨어뜨린다.

#### AV 가 «잘된 점» 으로 넣은 것
gain 3박자가 시간축에 실제로 있다(퍼짐 120ms 에 sd_x ×2.13 · 머묾 164ms 에 중심 이동 **3.5px** ·
흡수 813~2,176px/s) · 코인 단조화 · 델타 플로터 글리프 규격 3씬 동일 · quest 머묾 176ms 로 gain 과
일치 · 다이아 상자 회수 · 씬 C 라벨 충돌 소멸(7/7 프레임 0%).

### 26차 비평 (비평가 AU) — **① 4 / ② 6 / ③ 5 / ④ 4 → 최저 4**

회수 판정: **1 회수 · 2 회수(여유는 주장 미달) · 3 부분 회수 · 4 회수 · 5 부분 회수.**

| 축 | AU | AV | 25차(AS/AT) |
|---|---|---|---|
| ① 타이밍 | **4** | **4** | 3 / 3 |
| ② 크기 비율 | **6** | **6** | 4 / 4 |
| ③ 위치·경로 | **5** | **4** | 3 / 3 |
| ④ 가독성·일관성 | **4** | **4** | 3 / 2 |
| 최저 | **4** | **4** | 3 / **2** |

**네 축이 전부 올랐다(최저 2~3 → 4, ② 는 4 → 6).** 두 사람이 이번 회차가 손댄 네 구간
(⑥ 크기 곡선 · ④ 복도 · ⑤ 다이아 · ③ 씬 C 플로터)을 **모두 «회수» 로 판정**했고,
그중 셋은 재지적조차 하지 않았다. 남은 감점은 대부분 **이번에 안 잡은 자리**다.

AU 의 회수 실측(AV 와 다른 방법·같은 결론):
- ⑥ — r25 비행 최대 55px(퍼짐 33 대비 **1.67배**) → r26 전 구간 31~35, 최대/퍼짐 **1.06배**.
- ④ — r25 코인 외곽선 x952 로 테두리를 덮고 **내부 18px 침범** → r26 외곽선 **x981**.
- ⑤ — 비행 잉크 비 r25 0.79w/0.87h → r26 **0.86w/0.93h**, 목적지 HUD 비(0.87w/0.94h)와 **1% 내**.
- ③ — 라벨 대비 24.5(hush) → **183.6/183.8 완전 원복**, 가로 겹침 **0px · 0/8 프레임**.

## 27회차가 잡을 것 — 26차 **2인 공통** (우선순위 순)

1. **① 씬 B 비행 레이어 정지 2건 — 두 사람이 «같은 두 프레임 쌍»을 짚었다.**
   AU ①-1 «f11↔f12(972→1041, 69ms) 레인 변경 **10px** · 스프라이트 8개 bbox **화소 단위 동일** ·
   f13↔f14(1164→1217, 53ms) 레인 변경 **0px**. 같은 프레임에 토스트(x432~647)와 HUD(y46~229)는
   갱신됐다 — 비행 ~1,180ms 중 **122ms(10.3%) 사망»** · AV ①-2 «중심 이동 **0.0px**».
   ⚠ **이 쌍은 `pxdup58` 이 «부분 중복 페인트» 로 면제해 주지 않은 쌍이다** — 그래서 두 사람 다
   ① 감점으로 셌다. 그런데 `verify93` [2b] 는 같은 씬을 DOM 기준으로 «정지 프레임 **0/348**» 로 찍는다.
   → **27회차의 첫 일은 «게임이 멈췄나, 검출기가 못 잡았나» 를 가르는 것**이다. AV 가 그 쌍의
     연출 구역 변경을 **4,037px = 씬 중앙값의 12%** 로 쟀는데 `pxdup58` 의 경고 임계는 **5%** 다.
     즉 **검출기 임계가 너무 빡빡해서 12% 짜리 부분 재합성을 통과시켰을 가능성**이 높다.
     화소 수가 아니라 **스프라이트 bbox 동일성**(AU 가 실제로 쓴 판정)으로 검사를 바꿔라 —
     이걸 안 고치면 ① 은 회차마다 같은 이유로 계속 깎인다(23·24회차와 같은 계열).
2. **① 씬 B 도입 306ms 무반응** — AU ①-6 «f2↔f3(66→179) 화소 최대차 4 · 임계 28 초과 **0개**,
   HUD 포함 0px. 첫 버스트 f4(306)» · AV ①-1 «임계 30 초과 0개 · 캡처 1488ms 의 21%».
   이 쌍도 `pxdup58` 목록에 없다. 1번과 같은 판정 절차를 태울 것.
3. **① 카운터가 정답표보다 한 계단 늦다** — AU ①-3 «씬 A f7~f14 **8프레임 연속** 8.00A vs 16.0A …
   112A vs 128A, 128A 확정이 f15(1323)» · AV ④-3 «**82~120ms 지연이 9스텝 연속**, quest 는 184ms».
   ⚠ `probe58z` 는 «크레딧 − **실도착** = 0» 이었다. **두 값이 같이 참일 수 있다** — 정답표는
   `cap58.js` 가 만든 **설계 시각**이고 probe 는 실도착 대비다. 즉 어긋난 것이 «카운터 ↔ 도착» 이
   아니라 **«도착 ↔ 정답표»** 일 수 있다. 27회차는 셋(정답표·실도착·카운터)을 한 축에 놓고 재라.
   두 사람 다 envelope 을 줄이라고는 안 했다(그건 회수 불가 확정 항목).
4. **④ 퍼짐이 아직 «8열» 이다 — 행은 흐트러졌고 열은 그대로.** AU ⑤ «열 간격 56.8px **sd 1.90px
   (CV 3.3%)** — r25 는 54.5/1.44(CV 2.6%) 라 사실상 그대로. 두 열은 **0.1px 까지 일치**(420.2/420.3 ·
   593.1/593.0). 행 y sd 만 3.7·3.0 → **6.5·9.6px(2.0~3.2배)** 로 실제 커졌다» ·
   AV 5 «열 잔차 rms 2.6~9.1px(피치의 5~16%) · 두 행이 **39.3px 완전 분리 · 교차 0개**».
   → 26회차가 «열 지터는 보수적으로 ±7» 로 잡은 것이 **부족했다**(행 지터 ±14 는 먹혔다).
     AU 처방: 열 오프셋 **±14px(피치의 25%)** · **행마다 독립 시드**(지금은 두 행이 같은 열 x 를 공유).
     AV 처방: 열 rms ≥17px · 두 행 y 범위가 최소 25px 겹치게.
     ⚠ 열 지터 ±14 면 같은 행 최소 x 간격이 56.8 − 28 = **28.8px** 로 잉크 32.5px 아래로 내려간다 —
       **행마다 독립 시드**(= 같은 열의 두 아이콘이 x 로 안 붙는다)와 **행 y 겹침**을 같이 걸어야
       대각선 거리가 유지된다. 열만 키우면 25회차가 회수한 «상호 가림» 이 되돌아온다.
5. **③④ 씬 B 퍼짐이 «1×16 한 줄» 이고 재화별로 좌우 정렬됐다** — AU ③-5 «16개가 y947~1014(높이 67)
   x253~695(폭 442) 한 줄, 좌 8 골드 / 우 8 다이아» · AV ④-1 «387×31, y sd 8.7px, 규칙적 톱니 ·
   **막대그래프로 읽힌다** · 씬 A 는 403×98 8열×2행 — 공용 모듈에서 두 형태가 나온다».
   원인은 코드에 있다: 씬 B 는 `fx3Escape` 의 «행 안 빈 밴드» 경로라 `FX3_BSY` **26(=±13)** 만 쓰고,
   씬 A 는 25회차의 2행 밴드(`FX3_OBROW` 76)를 쓴다. **분기가 둘인 것 자체가 ④ 감점이다.**
6. **④ 델타 플로터가 씬마다 다르게 움직인다** — AU ④-2 «씬 A 아래로 **+22px/574ms** · 씬 B 하강 ·
   씬 C 위로 **−58px/257ms**» · AV ④-5 «A·B 27~28px/s 정지형 vs C 222px/s 상승형».
   25차 ⑦ 과 같은 항목이 **라운드를 건너 재확인**됐다(4인).
7. **① 씬 C 플래시가 감쇠 없이 평평하다 끊긴다** — AU ①-4 «B값 진폭 −46 → −42 → −38 → −33 →
   f7(527) **0**. 257ms 동안 100→72% 로 평평하고 남은 72% 가 ≤113ms 에 절단, 중간 표본 0개» ·
   AV ①-4 «181.0 → 154.5 → … → 162.9 → **181.1** 한 프레임 만에 켜지고 113ms 만에 잘린다».
8. **① 씬 C 델타 플로터 상승이 비단조** — AU ①-5 «0.312 → **0.032** → 0.272 px/ms, 중간 72ms 에
   2.3px» · AV ①-6 «307px/s → **28px/s(−91% 급정거)** → 276px/s». 두 사람이 **같은 세 프레임**을 짚었다.
9. **② 버스트 알갱이 규격이 씬마다 다르다** — AU ②-1 «씬 B 24.2×22.5(13개) vs 씬 C **18.1×18.4**(14개)
   = 폭 1.34배» · AV ②-4 «같은 프레임 안에서도 지름 12~26px, ±35% 산포».

### 26회차가 만든 새 부작용 (2인 공통 아님 — 그러나 이번 회차 책임이라 27회차가 먼저 갚아라)

- **AU 새 부작용 1 «씬 C 플로터가 카드 가격 띠에 얹힌다»** — f3(157) 글리프 y[1751,1780],
  카드 내용/가격 띠 이음선 **y=1758** → 높이의 **76%(22/29px)** 가 갈색 띠 위. 그 아래 14px 에
  가격 행 «53» 이 있어 두 숫자가 한 묶음으로 붙는다. **r25 에는 없던 겹침이다** —
  26회차가 플로터를 카드 «우측» 으로 옮기면서 세로 띠는 그대로 뒀기 때문이다.
  → 라벨 겹침 0 을 유지한 채 **시작 y 를 이음선 위로** 올려라(`r.y + r.h + 20` → 그 띠 안).
- **AU 새 부작용 2 ««+2.4» 와 «+2.2» 가 29px 간격으로 나란히 선다»** — 낱말을 뺀 대가로
  **어느 쪽이 방금 얻은 값인지 문자열만으로는 안 갈린다**(값 자체는 2.2 가 맞다 — 값을 바꾸라는
  얘기가 아니다). 21회차의 `fx-cvswap`(플로터가 떠 있는 동안 `.cv` 를 잠시 비운다)이 이미 있으므로
  **그 비움 구간이 플로터 수명 전체를 덮는지** 부터 재라(지금 400ms vs 플로터 620ms).
- **AU ②-2 «목적지 대비 코인 비가 0.68 → 0.46 으로 떨어졌다»** — 단조화의 부산물이다.
  ⚠ 되돌리지 마라(여덟 사람이 팽창을 감점했다). 대신 **평탄값 `FX3_FLYS` 자체를 올려라** —
  0.70 → 0.80 이면 비행 잉크 32.5 → 37 (목적지 대비 0.46 → **0.53**)이고 곡선은 여전히 단조다.
  단 퍼짐 면적이 커지므로 `verify93` [2b] 형제 행 관통과 25회차가 회수한 «상호 가림» 을 같이 볼 것.

### ⚑ 26회차 상수 오류의 뒷정리 — `FX3_PANEL` 에 `.mbox` 가 없다 (위 AV §참조 · 27회차 1순위와 같이)

두 사람이 «복도 여유» 를 서로 다르게 읽은 이유가 이것이다 — AU 는 **갈색 안쪽 테두리 끝 980**
기준으로 «여유 0~1px(회수)», AV 는 **검정 껍데기 테두리 끝 989** 기준으로 «−6px(부분 회수)».
코드가 AV 편이다: `.mbox`(A5 껍데기) 우변 **989** · `border 8px #000` · 그 안쪽 내용 상자
`#mbox` 가 974 다. `fx3Out()` 이 보는 `FX3_PANEL` 에 `.mbox` 가 없어 **내용 상자**를 재고 있었다.

⚠ **다만 `.mbox` 를 넣는 것만으로는 못 푼다 — 27회차는 아래 기하 예산을 먼저 보라.**
프레임 폭 1080 에서 «껍데기 우변 989 밖 · 두 레인이 눈에 보이게 분리 · 프레임 우변에 안 붙음»
셋을 동시에 만족시켜야 하는데, 쓸 수 있는 띠가 **989~1080 = 91px** 이고 잉크가
골드 32.5 + 다이아 28.1 = **60.6px** 이라 남는 여백이 **30.4px** 뿐이다(좌여유 + 레인 간격 + 우여유).
25회차가 974 기준으로 낸 결론(«셋 중 하나를 포기해야 한다»)이 **989 기준에서 더 빡빡해진 채로
그대로 유효**하다. 게다가 AU ③-4 가 지금 상태를 «두 복도가 9~13px 간격으로 붙어 폭 71~80px
한 덩어리로 읽힌다» 로 새로 냈다 — x 로 가르는 방식 자체가 한계에 왔다는 뜻이다.
→ 27회차는 25회차가 남긴 **(나) «레인을 x 가 아니라 시간으로 가른다»** 를 검토하라.
  두 재화를 **같은 레인(x≈1010, 989 밖 · 프레임에서 70px)** 에 놓고 재화별 도착 슬롯을 반 칸
  엇물리면 x 예산 문제가 통째로 사라진다. ⚠ 그 대신 `verify93` [1] 의 «첫 도착 0.5~0.6s ·
  마지막 도착 1.2~1.4s» 를 **두 재화 각각** 지켜야 한다 — 반 칸 엇물림이 어느 쪽 끝을 밀어내는지
  먼저 계산하고 손대라(현재 실측 첫 567ms · 마지막 1254ms 라 위쪽 여유는 146ms, 아래쪽은 67ms 다).
