# 731 — «조용한 소실» 을 잡는 전수 자가 없다 → 공용 차단기를 `pwlaunch` 한 자리에 건다 «게이트 위생 · 일반형»

- **세션** sess-1728-15327 (루틴 워커 B) · **1회차** · 2026-09-01
- **제품 `index.html` 0줄** · 레이아웃 Δ0px · 밸런스 계수 0줄 · 세이브 이관 없음
- **신설** `tools/evguard731.js`(부품) · `tools/verify731.js`(자, **36/36**)
- 고친 파일 셋 — `tools/pwlaunch.js`(배선 3줄) · `tools/probe319.js`·`tools/probe708.js`(신고 1줄씩)
- 지시서 [3]-**(가)** 기계+자 — **비평가 없음**

## 1. 병 (712 가 표본이고, 등재문이 «몇 자에 더 있는지 아무도 모른다» 고 적은 그 자리)

자가 `page.evaluate` **본문에서** 읽던 제품 전역이 폐지되면 페이지 안에서 `ReferenceError` 가 난다.
자마다 복붙된 `ev()`(319 규약 «즉사 대신 그 블록만»)가 그것을 **삼키고**, 뒤따르는 `if (x)` 가
조용히 건너뛰며, 자는 **종료 코드 0** 으로 끝난다. 712 에서 다섯 줄이 사라진 채 `46/46 PASS` 가
나온 것이 그 꼴이다.

- 696 = **빨간 죽음**(정규식이 못 찾아 `process.exit(1)`) — 이미 자가 있다.
- 731 = **초록으로 읽히는 빨강** — 자가 없었다. 712 는 **두 자에 손으로** 위생 항([6-0]·[4-0])을 세웠을 뿐이다.

## 2. 왜 정적 스윕이 아닌가 (등재문의 기각을 그대로 이어받았다)

712 세션의 시제품(evaluate 본문의 대문자 상수 후보 ÷ 제품에 없는 이름)은 **131 파일 · 수백 건**을
물었다 — 도구 자신의 상수·인자·문자열·정규식이 전부 섞이고, 자기 선언 필터를 걸어도 노이즈가 안 죽는다.
⇒ **실제로 난 예외만** 본다(동적).

## 3. 처방 — 자 400개를 한 줄도 안 고친다 (291 선례와 같은 자리)

등재문의 후보 둘 중 **②(공용 부품으로 승격)** 를 골랐고, 부품을 «자마다 부르는 것» 이 아니라
**`pwlaunch.launch()` 한 자리에 배선**했다. 291(settle)이 `waitForTimeout` 한 곳에서 게이트 44개를
동시에 고친 것과 **같은 꼴**이다 — 부품을 안 부르는 자가 남으면 «아무도 모른다» 가 그대로 남기 때문이다.

```
pwlaunch.launch()
  → settle291.armBrowser(browser)      (291 — 입장 연출 정착)
  → evguard731.armBrowser(browser)     (731 — 조용한 소실 차단)   ← 신설 한 줄
        page.evaluate / evaluateHandle 를 감싼다
        예외는 **그대로 다시 던진다** → 자의 try/catch·.catch 는 한 줄도 안 바뀐다
        난 자리에서 `⚠ evaluate 예외 [파일:줄] …` 한 줄
        마감(process 'exit')에서 미신고 치명 예외가 있으면 **종료 코드 1**
```

**바뀌는 것은 «마감» 뿐이다** — 자의 판정 본문은 글자 그대로 같다([D5]·[D6] 이 두 모드의 출력을
줄 단위로 대조해 못박는다).

### 3-1. 거짓 빨강을 안 만드는 것이 이 부품의 전제다

| 갈래 | 지문 | 센다 |
|---|---|---|
| **치명(fatal)** | `is not defined` · `is not a function` · `Cannot read propert…` · `is not iterable` · `is not a constructor` · `Cannot access … before initialization` | ✅ 이름·모양이 **사라졌다**는 뜻 |
| **정상(infra)** | `Execution context was destroyed` · `Target … closed` · `frame was detached` · `Timeout … exceeded` · `Protocol error` | ❌ 게이트가 페이지를 갈아타는 중에 흔히 난다 |
| **그 밖(other)** | 자가 스스로 던진 것 | ❌ |
| **신고(declared)** | `require('./evguard731').expect(/…/)` | ❌ 일부러 내는 예외는 자 머리에 적는다 |

### 3-2. 스위치 (되돌림)

- `EVGUARD=0` — 통째로 끈다. **이 값이면 731 이전과 동작이 같다**([R] 절이 그것을 시험한다).
- `EVGUARD=report` — 적고 찍기만 하고 종료 코드는 안 건드린다(도입·조사용).
- 기본값(strict) — 미신고 치명 예외가 있으면 종료 코드 1.
- 배선 자체를 빼려면 `tools/pwlaunch.js` 의 `const arm = b => evguard.armBrowser(armBrowser(b));`
  한 줄을 `armBrowser(b)` 로 되돌리면 된다.

## 4. 전수 조사 — 거짓 빨강이 몇 건이나 나는가 (도입 전에 잰다)

`EVGUARD=report`(코드 안 건드림)로 실제로 돌려서 셌다. 「삼키는 자」 = `__err` 그물이나
`.catch(() => {})` 를 가진 자 = **145개**가 이 병이 숨을 수 있는 전 모집단이고, 그 밖에
무작위 표본을 얹었다.

| 모집단 | 돌린 자 | `⚠ EVGUARD` 가 난 자 | 그중 **새로 빨개진** 자 |
|---|---|---|---|
| 무작위 표본(verify·probe 12개마다 1개) | 71 | 1 (`probe493b`) | **0** |
| 삼키는 자 전수(위 표본과 겹치는 것 제외) | 130 | 1 (`probe486`) | **0** |
| **합계** | **201** | **2** | **0** |

**거짓 빨강 0건**이다 — 잡힌 둘은 **차단기 이전부터 이미 빨간** 자다(둘 다 `EVGUARD=0` 으로 돌려도
종료 코드 1). 차단기가 한 일은 그 둘에 «무엇이 어디서 죽었나» 한 줄을 더한 것뿐이다.

| 자 | 죽는 자리 | 무엇이 | 종전 코드 |
|---|---|---|---|
| `tools/probe493b.js:24` | `[1]` 강제 레이아웃 | `#psTk .ps-bx` **900번째가 없다** → `Cannot read properties of undefined` 로 **밖으로 나가** 즉사(696 꼴) | 1 |
| `tools/probe486.js:47` | `§D` 58 플로터 | 660 이 선언째 지운 `trDeltaTxt` → 자가 스스로 `❌ evaluate 예외` 로 적고 **11/12** | 1 |

⇒ 둘 다 **내 작업 단위(731) 밖**이라 안 고쳤다(338·344 규약 — 수리 전 상태와 같음을 대조로 확인).
**749** 로 등재했다(⚠ 등재 push 경쟁 — 735 로 적었다가 상류가 748 까지 밀어 749 로 개정 · 286·308 규약).

## 5. 회귀 — 그리고 회귀가 잡은 **세 번째** 곁다리

| 자 | 결과 |
|---|---|
| `verify731` | **36/36 PASS** (pull 뒤 재실행 — 654 규약) |
| `verify498` · `probe498` | PASS (712 가 고친 «조용한 소실» 의 원본 자리) |
| `probe319` | **6/6 PASS · 코드 0** (신고가 실제로 듣는다) |
| `verify324` | PASS |
| `smoke.js` | **SMOKE PASS** |
| `verifyProgress.js` | 코드 0 (§1~§5 전부 빨강 0) |
| `verify712` | **끝나지 않는다 — 420s·900s 두 번 다 timeout** ⇒ 749 로 등재 |

⚑ **`verify712` 는 내 변경과 무관하다** — 멎는 자리는 [E] 스윕의 `bare()`(주석·문자열·정규식
리터럴을 걷는 정규식)이고, 차단기를 **한 줄도 안 지나는 순수 정규식**이다. 같은 `bare()` 를 떼어내
`tools/*.js` 를 훑는 사본으로 재현했다 — `tools/verify385.js` 에서 폭발한다(앞 180줄 **95ms** ·
200줄 **6.2초** · 221줄 전체는 20초 안에 안 끝난다). 구간 이분으로 **181~200행**까지 좁혔고, 그 안
187행이 `ok(…, '문자열' ⏎ \`템플릿\`)` = **인자 사이 쉼표가 빠진 태그드 템플릿**이다.

⇒ 「돌릴 수 없는 자」는 「조용히 비는 자」와 **같은 병의 다른 끝**이다 — 어느 쪽이든 판정이
안 난 채로 지나간다. 731 차단기가 닫은 것은 앞쪽이고, 뒤쪽(멎는 자)은 749 가 잡는다.

**일부러 예외를 내는 자 2개**를 신고로 닫았다 — 그 둘이 이 조사가 잡은 «거짓 빨강» 의 전부다:

| 자 | 무엇을 일부러 죽이나 | 왜 설계인가 |
|---|---|---|
| `tools/probe319.js` | `bagUse is not defined` | 319 처방 ③(«즉사 대신 그 항목만 빨개진다»)을 재현하는 것이 이 프로브의 과녁이다 |
| `tools/probe708.js` | `trDeltaTxt is not defined` | [2] 그물 시험 — 죽는 평가 뒤에도 다음 평가가 도는지를 잰다 |

⚑ **신고를 빼면 그 둘은 «PASS 인데 코드 1»** 이 된다 — 그 자체가 차단기가 살아 있다는 증거라
`verify731` [E] 가 신고본을 초록으로 확인한다.

## 5. 자 — `node tools/verify731.js` **36/36 PASS**

| 절 | 무엇을 |
|---|---|
| [A] 8항 | 배선(부품 존재 · `pwlaunch` 가 부른다 · 감싸는 것이 둘 · **예외를 다시 던진다**) · 스위치 3종 |
| [B] 7항 | 지문 — 치명 3 · 정상 3 · 그 밖 1 (거짓 빨강의 전제를 값으로 고정) |
| [C] 7항 | **병 재현 + 닫힘** — 삼키는 사본 자를 실제로 돌린다 |
| [D] 6항 | 무해함 — 예외 0건이면 코드·출력 그대로 · 두 모드의 **판정 본문이 같다** |
| [E] 4항 | 신고 — `probe319` 가 초록 · 코드 0 · 마감 블록에 안 실린다 |
| [R] 4항 | **되돌림 시험** — 배선을 뺀 `pwlaunch` 사본에서는 다시 조용한 초록이 된다 |

### [C] 가 찍은 것 (병이 여기 있다는 증거)

같은 사본 자를 두 모드로 돌린다. 사본은 712 가 걸린 꼴 그대로 — `p.evaluate(fn).catch(() => {})`
한 줄과 `if (got)`.

```
EVGUARD=0   →  ok  [다른 절] …            FIX731 1/1 PASS      code 0   ← «절» 이 한 줄도 안 남는다
기본값       →  ⚠ evaluate 예외 [.v731-dead-…js:9] page.evaluate: ReferenceError: __NO_SUCH_GLOBAL_731__ is not defined
               ok  [다른 절] …            FIX731 1/1 PASS
               ⚠ EVGUARD 731 — 삼켜진 evaluate 예외 1건 (1종)     code 1
```

⚠ 사본은 끝에서 **`process.exit(0)` 을 명시로** 부른다 — 그 경로에서도 코드가 올라가야 하고,
[C6] 이 그 자리다(마감 훅이 `process.exitCode` 를 덮는다).

⚑ **자 자신의 판정은 여전히 «PASS» 다**([C7]). 차단기는 «자가 틀렸다» 고 말하는 것이 아니라
**«자가 안 돌았다»** 고 말한다 — 고쳐야 할 것은 사라진 절이지 점수가 아니다.

## 6. 되돌림 시험이 없으면 [C] 는 헛항이다 (676 «헛초록» 규약)

[R] 은 배선 한 줄만 뺀 `pwlaunch` 사본을 만들어 [C] 와 **같은 사본 자**를 돌린다 —
코드가 0 으로 되돌아가고 ⚠ 줄이 사라지며 그 절은 여전히 비어 있다. 즉 [C6] 의 빨강은
차단기 덕분이지 사본 자의 다른 성질 때문이 아니다.

## 7. 다음 세션이 볼 것

- **자를 새로 짤 때 `ev()` 를 복붙하지 마라** — `require('./evguard731').helpers(page, ok)` 가
  같은 `ev`/`blk` 짝을 돌려준다. 여기 것은 등록부에도 남으므로 «위생 항 적기를 빼먹어도» 마감에서 잡힌다.
- **일부러 예외를 내는 자는 자 머리에 한 줄** — `require('./evguard731').expect(/…/)`.
- ⚠ **`page.$eval`·`$$eval` 은 아직 안 감싼다** — 이 저장소의 자는 거의 전부 `evaluate` 를 쓴다
  (병의 표본 712 도 그렇다). 필요해지면 `arm()` 의 이름 목록에 두 줄을 더하면 된다.
- ⚠ **차단기는 «예외» 만 본다.** 절이 `if (x)` 로 건너뛰는 다른 이유(값이 0·null·빈 배열)는
  여전히 자기 위생 항이 필요하다(712 [6-0]·[4-0] 이 그 꼴).
