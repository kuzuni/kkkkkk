# 705 — 19 프로필 · 20 종합스탯 팝업 정리 4종

> 주인 지시(2026-09-02 02:25) 원문
> «종합스탯에 햄지 어쩌구로 되있는데 그러지말고 실제 이게임에 스텟들 표시되게 해야함. 햄지 빼 /
> 종합스탯에 아이콘 모습을 코스튬 현재 끼고 있는거로 해야함 방패 모습 말고 /
> 종합스탯쪽팝업이랑 프로필 팝업이 같은 팝업인데 탭이 다른식으로 해야하는데 프로필로 자꾸 클릭하면
> 위치가 바뀜. 그거 제대로 통일 시키기 / 상단에 게이머아이디랑 그런거 일단 업데이트 예정 으로 바꾸기»

세션 `sess-0158-18963` 루틴 워커 D · **2회차**(1 수리+게이트 · 2 비평 2인 채점·반영) · T2(주인 지시 · 기능 완성 규칙 적용).
제품 `index.html` **8자리**(CSS 5 · 마크업 1 · JS 2) · 신설 자 3(`probe705`·`verify705`·`cap705`) ·
이관 4(`verify20`·`verify188`·`verify241`·`verify351`).

---

## §1. 재현부터 — `tools/probe705.js` (338 규칙)

처방을 따르기 전에 넷을 각각 숫자로 찍었다. **등재문의 네 항이 전부 사실이었고, ③ 은 등재문보다 나빴다.**

### ③ 두 팝업 상자 — 프레임 높이별 bbox (수리 **전**, 설계 px)

| 프레임 | 19 프로필 `.pf` | 20 종합스탯 `.spc` | Δ(20 − 19) |
|---|---|---|---|
| 2600 | 92, 431 · 896×1396 | 92, **590.5** · 896×1395 | y **+159.5** · h −1 |
| 2280 | 92, 431 · 896×1396 | 92, 430.5 · 896×1395 | y −0.5 · h −1 |
| 1920 | 92, 431 · 896×1396 | 92, **250.5** · 896×1395 | y **−180.5** · h −1 |
| 1600 | 92, 223 · 896×1296 | 92, **168** · 896×**1240** | y **−55** · h **−56** |

⚑ **기준 프레임(2280)에서만 맞는 통일이었다.** 주인이 «자꾸 클릭하면 위치가 바뀜» 이라고 한 것은
기기 화면비 때문이다 — 19 는 «절대 배치 + `--pfsh` 흡수»(415 계약)로 서고, 20 은 `#specw` 의
**flex 중앙정렬 + 상하 패딩 126/234 + `translateY(42px)` + `max-height:100%`** 로 서 있었다.
두 규칙은 2280 에서 우연히 같은 자리를 냈고, 프레임이 길어지면 20 이 아래로(2600 +159.5),
짧아지면 위로(1920 −180.5) 따로 움직였다.

### ① 종합스탯 13행 (수리 전)

- «햄지» 라벨 **13행 / 13**.
- **상태와 무관한 고정값 5행** — «햄지 일반 공격 피해량 100%» · «일반 몬스터 피해량 100%» ·
  «보스 피해량 100%»(셋 다 소스에 손으로 적힌 리터럴) · «공격 속도 0%»(703 이 축을 옮겨 늘 0) ·
  «이동 속도 0%»(358 규약).
- 반면 **살아 있는 축은 화면 밖에 있었다** — cp(전투력) · `stat.dps` · 관통 · 방어 · 공속 실값은
  이 시트가 한 번도 안 보여 준 값이다.

### ② 아이콘 (수리 전)

`.spc-ava > .in > i` = **🛡️ 이모지 문자**. 코스튬을 갈아입혀도(`S.avatar` av0 → av6) 그대로 🛡️.
19 프로필은 **이미 코스튬 스프라이트**를 쓰고 있었다(201 ③ · `porPaint`) — 즉 **부품은 이미 있었고
20 만 안 읽고 있었다**(318 계열 «경로가 한 칸 짧다»).

### ④ 상단 줄 (수리 전)

`#spcGid` = «Gamer Id: 77000daa-2904-cc8a-c645-c6451da67700» — 닉네임에서 유도한 **가짜 uuid**.
19 `#pfGid` 도 같은 꼴(다른 함수 `pfGamerId`, 값도 다르다 = **같은 팝업인데 아이디가 둘**이었다).

---

## §2. 처방 — 제품 8자리

### ③ 상자: «선언 한 벌을 둘이 읽는다»

```css
.pf, .spc{position:absolute;left:92px;
  --pfsh:clamp(0px, calc(1700px - var(--frameh, 2280px)), 100px);
  top:clamp(223px, 431px, calc(var(--frameh, 2280px) - 1477px));
  width:896px;height:calc(1396px - var(--pfsh))}
```

- `.pf`·`.spc` 에는 **껍데기만** 남겼다(배경·테두리·라운드). 값을 어느 한쪽에 다시 적지 않는다 —
  «상수 두 벌은 조용히 갈라진다»(485·402)가 이 자리에서 실제로 일어난 일이다.
- `#specw` 는 `display:flex`→`block` · `padding:126px 0 234px`→`0`. 절대 배치의 기준 상자를
  패딩이 밀기 때문이다.
- 대가 **1자리**: `.spc-body` 높이가 1356 → **1357**(상자 1395 → 1396)이 되어 바닥 앵커인
  `.spc-tabs{bottom:20px}` 를 **21** 로 올렸다. 351 이 세운 산수 그대로다(1357 − 1242 − 94 = 21).
  안 올리면 탭 줄이 통째로 +1.5px 내려가 `verify20` `.spc-tab-on` 이 빨개진다.
- **수리 후 프레임 4종 전부 Δ0**(2600·2280·1920·1600) — `verify705` [C].

### ① 행: 라벨은 표에서, 값은 상태에서

`renderSpec()` 의 13행을 다시 짰다. **라벨은 강화 표 `UPG` 파생**(`U.atk.name` 꼴)이고
값은 `stat`/`bonus()` 파생이다. 손으로 적은 «100%» 세 줄은 사라지고 그 자리에
**살아 있는 축**(전투력 cp · 관통 · 방어력 · 공격 속도 실값)이 들어왔다.

| # | 라벨(출처) | 값(출처) |
|---|---|---|
| 1 | `U.atk.name` 공격력 | `fmtB(stat.dmg)` |
| 2 | `U.hp.name` 최대 체력 | `fmtB(stat.maxHp)` |
| 3 | `U.regen.name` 체력 재생 | `fmtB(stat.regen)`/s |
| 4 | 전투력 | `fmtB(cp())` |
| 5 | 공격 속도 (703 이 UPG 에서 뺀 축 — 이름을 여기서 짓는 유일한 행) | `stat.rate` 회/s |
| 6 | `U.crit.name` 치명타 확률 | `pct(stat.crit)` |
| 7 | `U.cdmg.name` 치명타 피해 | `×stat.cdmg` |
| 8 | `U.pierce.name` 관통 | `stat.pierce` 명 |
| 9 | `U.def.name` 방어력 | 받는 피해 −`pct(1 − stat.defMul)` |
| 10 | 공격력 증가 | `bonus().atk` |
| 11 | 체력 증가 | `bonus().hp` |
| 12 | `U.gold.name` 골드 획득 | `×stat.goldMul` |
| 13 | 이동 속도(358 규약 — 레퍼런스 줄이라 산다) | `stat.speed / PLAYER_SPEED` ⇒ **0%** |

⚑ **13번의 «0%» 도 손으로 안 적었다.** 358 은 «값은 영구히 0%» 라고 못 박았지만 그것을 리터럴로
적어 두면 «축이 되살아나도 화면은 0%» 다. 살아 있는 식에서 뽑으면 그때 즉시 드러난다.
`verify358` [3-ⓑ] 는 그대로 초록이다(라벨 정규식 `/이동 속도/` · 값 `'0%'`).

⚠ **행 수 13 은 유지**했다 — 레퍼런스 격자(측정표 20 §7-1: 리스트 800×760 안에서 스크롤이 나는 수)다.

### ② 아이콘: 19 가 이미 쓰던 부품을 20 도 읽는다

```html
<div class="spc-ava"><div class="in">
  <canvas class="cos-cv por-cv" id="spcPor" width="132" height="138"
          data-cosav="av0" data-cossc="3" data-coslite></canvas><em class="por-em">🛡️</em></div></div>
```

- `renderSpec()` 이 `porPaint($('spcPor'))` 를 부른다 — 19 초상과 **같은 함수·같은 아틀라스**라
  그림 출처가 둘이 되지 않는다.
- 캔버스 **132×138 = knight idle0 잉크(44×46)의 정수 3배**(356 «확대는 등방» · 정수배라 픽셀이
  안 흐려진다). `.in` 안쪽 아트 자리 139×138 에 딱 든다.
- `.spc-ava` bbox 는 **Δ0**(451,510 179×180 — 측정표 20 §5). 아트 자리 규칙(72)대로 상자는 그대로 두고
  안의 그림만 갈았다.
- 아틀라스가 없으면 옛 🛡️ 가 폴백으로 선다(`porPaint` 가 실패하면 `.por-em` 을 되살린다) —
  `verify705` [R2] 가 그 길을 실제로 걸어 본다.

### ④ 상단 줄: 문구 한 벌 `SPC_TBD`

- `#pfGid`·`#spcGid` 둘 다 **«업데이트 예정»**. 상수는 **한 곳**(`const SPC_TBD`)이라 두 탭이
  다른 말을 할 수 없다(수리 전에는 함수가 둘이라 **값도 달랐다**).
- 죽은 파생 함수 **`pfGamerId` 는 선언째 삭제**(399 처방 — 죽은 분기를 남기면 되살아난다).
- `spcGamerId` 는 **남긴다** — 55 설정 팝업·고객 지원이 아직 부른다. `verify705` [D5] 가 그
  스코프 밖 자리가 그대로임을 짝 항으로 든다.

### ⚑ 위임 규약(2026-09-01 22:35) 채택 1건 — «상단» 의 범위

주인 원문 «상단에 게이머아이디랑 그런거» 와 PROGRESS 등재문의 ««U_178…» 류» 가 가리키는 범위가
둘로 읽힌다. **닉네임(`U_178…` = `S.nick` 기본값)까지 «업데이트 예정» 으로 덮는 안**은 채택하지 않았다:

| 안 | 대가 |
|---|---|
| ⓐ **채택** — 상단 **Gamer Id 줄만** 문구 교체 | 닉네임은 그대로. «U_178…» 은 기본 닉이라 이름을 바꾸면 사라진다 |
| ⓑ 닉네임까지 교체 | 편집 버튼(✎)·`S.nick` 저장·HUD 표시가 **전부 도는 기능**인데 화면에서 죽는다 |

닉네임은 «미구현 표기» 가 아니라 구현된 기능이고(눌러서 바꾸면 저장·HUD 까지 따라간다 —
`verify20` §6 이 그것을 단언한다), Gamer Id 만이 «값을 지어내는» 자리였다.
**되돌림**: `#pfNick`/`#spcNick` 에 `SPC_TBD` 를 쓰면 ⓑ 가 된다(한 줄).

---

## §3. 게이트

### 신설 — `tools/verify705.js` **52/52 PASS**

- **[A] ①** 행 13 · 화면 «햄지» 0행 · **제품 문자열(주석 밖) «햄지» 0건**(369 선례 — 레퍼런스를
  설명하는 주석은 남긴다) · `renderSpec` 안 손 «n%» 0건 · 빈 값 0 · NaN 0 ·
  **[A7] 강화하면 13행 중 9행이 실제로 움직인다** · [A8] 358 규약(이동 속도 0%) ·
  **[A9] 표를 바꾸면 라벨이 따라온다**(`U.atk.name` 주입) · [A10] 원복.
- **[B] ②** 캔버스가 그려졌다 · 이모지 폴백 꺼짐 · 아트 자리 Δ0 · 캔버스 132×138 ·
  **[B6] 코스튬을 갈아입으면 따라온다**(av0 → av6) · [B7] 되돌리면 되돌아온다.
- **[C] ③** 프레임 4종 bbox Δ0 · 2280 절대값(92,431,896,1396) · 탭 줄 상변 1692(351 계약) ·
  1600 탭 줄이 크림 본문 안 · **[C4] 20 탭 줄 = 19 하단 토글 줄과 같은 자리**(4종 전부).
- **[D] ④** 두 팝업 문구 동일 · uuid 0건 · **닉네임 불변** · **`spcGamerId` 는 산다** ·
  `pfGamerId` 0건 · 문구 상수 1곳 · **[D8] 두 줄에 라틴 폭 보정(`scaleX`)이 안 걸려 있다**(2회차 비평 반영).
- **[R] 되돌림** — [R1] `#specw` 를 flex+패딩으로 되돌리면 1920 에서 **Δ180.5px** 로 튄다(=[C] 빨강) ·
  [R2] **칠하기를 실패시키면** 🛡️ 가 돌아온다(=[B] 빨강) · [R2b] 아틀라스를 돌려주면 다시 코스튬 ·
  [R3] «햄지 100%» 주입 시 [A2] 가 세는 값이 1 이 된다.
  ⚠ [R2] 초판은 캔버스에 `display:none` 을 주는 것이었는데 **그건 되돌림이 아니다** —
  `porPaint` 는 «칠하기 실패» 에만 폴백을 켠다. 자가 헛빨강을 냈고 그 자리에서 잡았다.

### 이관 4건 (333 처방 — 자리를 비우지 않고 방향만 뒤집는다)

| 자 | 항 | 무엇을 바꿨나 |
|---|---|---|
| `verify20` | «Gamer Id 렌더» | → «상단 줄 = 업데이트 예정» + **«uuid 가 되살아나면 빨강»** + «19 도 같은 문구» (**32 → 34/34**) |
| `verify188` | ① 싱크 3항 · ③ 행 조회 3항 | 라벨 리터럴 → **`U.<id>.name` 을 짚는다**. 묻는 것은 그대로(«fmtB 를 지나는가») (**91/91**) |
| `verify241` | §6 음성항 치환문 | `height:calc(1396px - var(--pfsh));` → `…)}`(선언이 `.pf, .spc` 공용 블록의 끝이 됐다) (**100/100**) |
| `verify351` | [8-d] · [8-f] | 아래 별항 (**86/86**) |

#### `verify351` §8 이관이 이 작업에서 제일 조심스러운 자리다

- **[8-d]** 는 «패널 **= 1240**» 이라는 **상수**였다. 705 로 1296 이 된다. 새 상수로 갈아 끼우면
  «흡수 구조가 통째로 사라져도 초록» 이 되므로 **관계로 바꿨다** — ⓐ 짧은 프레임에서 패널이 실제로
  눌리고 ⓑ 눌린 만큼(−100)을 리스트가 먹는다(−99). 실측이 그대로 찍힌다.
- **[8-f]** «1600 탭 줄이 앱 탭바를 안 문다» 는 705 뒤로 **20 의 결함이 아니게 됐다** —
  탭 줄이 19 프로필의 하단 토글 줄과 **정확히 같은 자리**(둘 다 상자 로컬 1161 · 1600 에서 프레임 1384)에
  서기 때문이다. 19 는 415 폴리시로 그 자리에서 통과했고 아무도 지적한 적이 없다(팝업은 z 33/34 로
  탭바 **위**에 딤째 얹히므로 탭바가 탭 줄을 가리지도 않는다).
  ⇒ 여기 남긴 것은 **«프레임 밖으로 안 나간다»**(진짜 손실)이고, «19 와 같은 자리인가» 는
  두 팝업을 다 여는 `verify705` **[C4]** 가 든다. 두 자를 짝으로 읽어야 뜻이 완성된다.
  ⚠ 옛 문장으로 되돌리려면 19 의 `.pf` 기하(415 계약 «1700 = 142 + 81 + 1396 + 81»)를 같이 바꿔야 한다 —
  20 만 도로 눌러 놓으면 주인 지시(«위치 통일»)가 깨진다.

### 회귀 (무수정 통과)

`verify415` **119/119** · `verify201` **74/74** · `verify706` **47/47** · `verify358` **68/68** ·
`verify95` **PASS** · `verify137` **152/152** · `verify240` **36/36** · `verify391` **57/57** ·
`verify407` **91/91** · `verify467` **64/64** · `verify576` **49/49** · **SMOKE PASS**(화면비 6종).

⚠ **곁다리 1건 — `verify125` 39/41(786 으로 등재).** A1 «소스에 남은 화폐 이모지 0건» 이 1건을 센다:
`index.html` 27308 의 «💎 환영 n일차 보너스» 우편 제목(739 가 넣은 줄). **수리 전 커밋 `fa7efbe` 에도
같은 문자열이 1건**이고 A1 은 순수 소스 자라 점수가 같다 — 내 변경과 무관하고 내 작업 단위가 아니라 안 고쳤다.

---

## §4. 기능 체크 표 (T2 «기능 완성 규칙» — 눌렀을 때 무엇이 바뀌는가)

| 자리 | 누른다 | 실제로 바뀌는 것 | 확인한 자 |
|---|---|---|---|
| 19 하단 «종합 스탯» | 클릭 | `#pfw` 닫히고 `#specw` 열림 · **상자는 한 픽셀도 안 움직인다** | `verify705` [C] · `verify20` §1 |
| 20 하단 «프로필» | 클릭 | `#specw` 닫히고 `#pfw` 열림 · 역시 Δ0 | `verify20` §7 |
| 강화(훈련) | Lv +500 | 종합스탯 13행 중 **9행**이 새 값으로 | `verify705` [A7] |
| 코스튬 교체 | `S.avatar` 변경 | 20 아이콘이 그 코스튬으로(19 초상과 같은 그림) | `verify705` [B6] |
| 닉네임 ✎ | 편집 | `S.nick`·화면·HUD·localStorage 전부 갱신(**705 가 안 건드린 것**) | `verify20` §6 |

---

## §5. 남긴 것 · 다음 세션에게

1. **딤 색이 아직 둘이다** — 19 `rgba(0,20,50,.85)`(남색) · 20 `rgba(0,0,0,.55)`. 탭을 갈아탈 때
   배경 밝기가 바뀐다. 둘 다 **레퍼런스 실측값**이라(측정표 19 §1 · 20 §1) 이번 지시(«위치»)의
   범위 밖으로 두었고 색은 지시서 [3] 상 통과 판정 밖이다. 주인이 «껍데기까지 한 팝업» 을 원하면
   그때 한쪽으로 모은다 — 자리는 `#pfw`/`#specw` 의 `background` 한 줄씩이다.
2. **20 의 «공격 속도» 는 이제 «n회/s» 다**(수리 전에는 늘 0% 인 증가율). 725(«% → ×N배» 전면 전환)가
   이 시트를 지날 때 **이 행은 배율이 아니라 실스탯**임을 보고 지나가라 — ×배로 덮으면 703 이
   옮겨 놓은 «초당 몇 회» 라는 실값이 다시 사라진다.
3. **행 수 13 은 레퍼런스가 14**(햄지 7 + 매옹이 7)다. 우리는 캐릭터가 하나라 13 이고, 늘리려면
   측정표 20 §7-1 의 리스트 높이(760)와 스크롤 전제를 같이 다시 재야 한다.

---

## §6. 비평 회차 (지시서 [3]-(나) — 캡처 비평 2인 · 1회차)

캡처 `tools/cap705.js r1` — 19·20 을 **짝으로**, 2280·1600 두 프레임(PNG 는 커밋 금지, `.gitignore`).
비평가 둘에게 «레퍼런스 1080×2340(상단 84 상태바) ↔ 캡처 1080×2280, 세로는 ref y − 84» 변환 규칙과
«색·아트 내용·서체 모양은 감점 사유가 아니다» 를 명시하고 독립으로 채점시켰다.

| 비평가 | 화면 | ① 배치 | ② 상대 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|---|
| A | 20 종합스탯 | **9** | 6 | **9** | 7 | 8 | 4 | **6** |
| B | 19 프로필 | 5 | 6 | 6 | 7 | 8 | 7 | **5** |

### ⚑ 두 사람의 감점 중 **705 의 4항인 것은 한 줄뿐**이었다

- **705 가 만든 것(그 회차에서 고쳤다)**: B 의 «상단 1줄 잉크 14 → 18px(+28.6%)».
  뿌리는 `.pf-gid>i{scaleX(1.14)}` · `.spc-gid>i{scaleX(1.155)}` — **라틴 uuid 폭 회수** 보정이라
  한글 «업데이트 예정» 에 걸면 회수가 아니라 **왜곡**이다(380·689 «라틴 폴백 보정» 의 반대 자리).
  ⇒ 둘 다 `transform:none`. `verify705` [D8] 이 그 자리를 못 박는다.
- **나머지는 전부 19·20 화면의 폴리시 잔여**다 — 스탯 행 서체 비(라벨 −14.7% / 값 +13.0%),
  배너·탭 잉크 −10~13%, `.spc-edit` 글리프 오버플로, 19 의 칭호 그리드 4행, 자물쇠 +112px 등.
  **19·20 은 각자 행이 있고 회차 예산도 각자**라(지시서 [1] «잡은 작업 단위 딱 1개만 끝낸다»)
  이 행에서 건드리지 않고 **787 로 등재**했다.

### ⚠ 다음 비평가에게 반드시 줄 것 (335 «앵커가 둘» 과 같은 유형의 유령)

B 가 ① 5점의 최대 감점으로 «탭 3개 → 1개, 501px 밴드가 비었다» 를 적었는데 그것은 결함이 아니라
**201 ①(주인 지시 2026-08-27)로 «초상화»·«프레임» 탭을 폐기한 결과**다. 탭 자체를 안 없앤 이유도
그 자리에 적혀 있다(그것이 이 패널의 유일한 라벨이다). **이 사실을 프롬프트로 안 주면 회차마다
같은 지적이 최대 감점으로 다시 올라온다** — 787 을 잡는 세션은 반드시 같이 줄 것.
