# 814 — 50 코스튬 강화 델타가 호스트 카드의 «n/500» 을 덮는다

**세션** sess-1813-30551 (루틴 워커 A) · **1~2회차** · **2026-09-02**
**제품 `index.html` 5자리 · 레이아웃 Δ0px · 밸런스·판정 0줄.** 자 셋(`probe814`·`verify814`·`cap814`) 신설,
이관 셋(`verify711` [G1]·[G2] · `verify93` [7-d]~[7-f] · `verify694` §R-c 겨냥 항).

---

## 1. 등재문이 남긴 처방 공간 — 셋이 다 막혀 있었다

711 §3 이 이 행을 등재하면서 처방 공간을 셋으로 갈라 놓고 **재측정 금지**를 걸어 뒀다:

| 축 | 등재문이 적어 둔 이유 |
|---|---|
| ⓐ 세로 | 여정 잉크 span 123px vs 카드 171px. 자유 띠는 26px·37px 뿐 — 위로 올리면 아이콘 관통(58 24회차가 되돌린 자리), 아래로 내리면 아래 칸 글자를 문다(§R-a 실측 여유 **−31.4px**) |
| ⓑ 가로 | 58 26회차 훈련 처방(`r.x + r.w*0.87`)은 카드 168 · 라벨 잉크 61 · 델타 잉크 78 이라 **카드 밖**으로 나간다 |
| ⓒ 여정·수명 | 80px/.62s 는 58 30회차가 «한 픽셀도 안 바꾼다» 로 못박은 공용 상수 |

**그래서 이 회차는 «셋 중 하나를 고르는 회차» 가 아니라 «넷째가 있는가» 를 묻는 회차였다**(338 — 처방 전에 제품에게 묻는다).

## 2. 재현 — `tools/probe814.js` **12/12 PASS**

등재문을 그대로 확인했고(**[2]**: 교집합 1275.4px² · 세로 100% · 0~155ms), 등재문이 **안 물은 두 가지**를 찍었다.

### 2-1. ⓐ 넷째 축은 «자리» 가 아니라 **«말»** 이다

| 물음 | 실측 |
|---|---|
| 호출부가 주는 문자열 | `'Lv. ' + cosLvOf(cosSel)` = **«Lv. 0»** |
| 호스트 카드 `.sk-clv` 가 인쇄 중인 문자열 | **«Lv. 0»** |

⇒ **글자까지 같다.** 58 이 19~22·26·34회차 다섯 라운드를 싸운 «이중 인쇄» 는 «두 값이 **다른데** 같은 종류로
읽힌다» 였다(플로터 +2.2 = 방금 얻은 값 · 카드 +2.4 = 다음 값, 둘 다 맞았다). **여기는 그보다 나쁘다** —
델타가 카드가 이미 하고 있는 말을 그대로 한 번 더 하면서, 그 대가로 카드의 진행도 줄을 덮는다.

### 2-2. ⓑ **낱말을 빼도 안 들어간다** — 훈련 처방이 여기서는 산수로 깨진다

빈 자리(«n/500» 잉크 오른끝 189.9 ~ 카드 오른끝 244) = **54.1px**.

| 문자열 | 잉크 **봉우리** 폭 | 판정 |
|---|---|---|
| «Lv. 2»(흔한 레벨) | 77.7 | 못 들어간다 |
| «Lv. 500»(최대 레벨) | 121.6 | 못 들어간다 |
| **«500»**(낱말만 뺀 것 · 최악) | **64.9** | **못 들어간다** |
| «2»(낱말만 뺀 것 · 흔한 레벨) | 21.1 | 들어간다 |

⚠ **봉우리로 재야 한다** — `@keyframes fxDelta` 10% 가 `scale(1.06)` 이라 t=0(`scale(.8)`)에서 재면 26% 작게
읽히고, 실제로 1회차 첫 측정이 ««+1» 은 들어간다» 라는 **헛초록**을 냈다(⇒ LESSONS 814-①).
낱말을 뺀 «500» 조차 못 들어가므로 **ⓑ 는 흔한 레벨에서만 열리는 문이다** — 최대 레벨에서 조용히 다시 깨진다.

⚠ 「+1」 같은 **획득량 표기는 애초에 금지 구역**이다 — **520(주인 지시 «그 코스튬쪽에 + − 표시 없애셈»)**
이 이 화면에서 부호를 걷어냈고, 그 작업의 스펙 칸이 **② 첫 발 연출 `fxUpOk(…, 'Lv. n')`** 을 자리 목록에
적어 두고 있다. 즉 주인의 눈이 이미 이 문자열 위에 있었다.

## 3. 처방 — 520 이 **같은 화면에** 이미 쓴 꼴을 첫 발에도 맞춘다

520 은 홀드 **회당** 피드백에서 «+1 Lv» 문구를 빼면서 **맥박은 그대로 뒀다**(`txt: ''` · `beat: '#mbox .sk-gr'`
— 코드 머리말이 «문구만 없애고 맥박은 회당 그대로 돈다» 라고 적고 있다). 첫 발도 같은 꼴이 된다:

* `[data-cosup]` — `fxUpOk(card, card, 'Lv. n')` → **`fxUpOk(card, card)`** + 재렌더 뒤 `cosLvPop()`
* `showCosDetail` 의 `fx:` — 같은 행동이므로 **같은 말**로(736 «짝인 두 자리의 어긋남»)
* `cosLvPop()` — 값이 실제로 바뀐 줄(`.sk-clv`)에 58 22회차의 팝(`.fx-cvswap` · scale .84→1.07→1 · 340ms)

**남는 것**: 흰 플래시 · 카드 펄스 · 크림 스파크 10알 · 값 줄 팝. **없어지는 것**: 문구 한 줄뿐이다.

⚠ 팝은 **재렌더 «뒤»** 에 건다 — `renderUI()` 가 격자를 통째로 갈아 끼우므로 앞에 걸면 그 노드째 사라진다.
58 의 `.fx-cvswap` 이 훈련에서 «앞» 에 걸려 있는 것은 그 카드가 재렌더를 안 타기 때문이다(같은 부품, 다른 수명).
⇒ **LESSONS 814-②.**

**되돌리는 법**: `fxUpOk(card, card)` 를 `fxUpOk(card, card, 'Lv. ' + cosLvOf(cosSel))` 로,
팝업 `fx:` 를 `fxUpOk(at, at, 'Lv. ' + cosLvOf(id))` 로 되돌리고 `cosLvPop()` 호출을 지운다
(위임 규약 2026-09-01 — 선택지·대가를 적고 최선을 그 회차에서 채택한다).

## 4. 게이트

* `node tools/probe814.js` → **PROBE814 12/12 PASS** (신설 — 재현 + 처방 공간 실측)
* `node tools/verify814.js` → **VERIFY814 19/19 PASS** (신설)
  * [A] 없어진 것 — 실제 클릭 경로에 델타 0장 · **수명 전체**에서 호스트 글자 교집합 **0px²**
  * [B] **남아 있어야 하는 것**(음성항 묶음 방지 — 「연출 없음은 0점」) — 플래시 1 · 스파크 10 · 팝 `fxCvSwap`
  * [C] 정보 손실 0 — 레벨이 실제로 오르고 **새 값을 카드 자신이 말한다**
  * [B5] 그 줄이 앰버로 물든다 · **[B6] 스파크 10/10 알이 카드 상자 안**(밖 0 — 비평가 관측을 자로 기각)
  * [D] 팝업 자리도 같은 어휘 · [E] 공용 부품 무손상(`fxDelta` 직접 호출 · 토큰 34px · 수명 .62s)
  * **§R 되돌림 시험 2축** — [R-a] 옛 호출 부활 ⇒ 교집합 **1186.8px²** 로 빨강 · [R-b] 팝만 제거 ⇒ [B3] 빨강
    (⚠ [R-a] 는 처음에 **199.6px²** 로 읽혀 «무른 자» 가 될 뻔했다 — 클릭 직후 **한 장**만 재고 있었다.
    수명 21칸을 다 재자 1186.8 이 나왔다. ⇒ LESSONS 814-③)
* **이관** `node tools/verify711.js` → **18/18 PASS**
  [G1] 방향 뒤집기(«실재한다» → «실제 경로가 그 자리를 안 지난다») · [G2] 자리를 안 비웠다
  («부품을 지운 게 아니라 호출을 옮겼다» — 합성 호출은 종전 기하 1275.4px² 그대로)
* **이관** `node tools/verify694.js` → **13/13 PASS** — §R-c(«부품 사망»)가 겨누는 항을 [7-d] → **[7-f]** 로 옮겼다
  (주입은 그대로 · 축도 그대로 · [7-d] 가 음성항이 되면서 양성항이 [7-f] 로 내려갔기 때문이다)
* **이관** `node tools/verify93.js` → **33/33 PASS**
  694 가 «`fxDelta` 를 아직 쓰는 유일한 계열» 로 적어 둔 씬 D 를 660 꼴로 갈아 끼웠다:
  [7-d 전제] 씬이 났다(강화석 지출) · [7-d] 델타 0장 · **[7-e] 그 자리를 값 줄 팝이 대신한다** ·
  [7-f] 부품은 살아 있다(직접 호출은 여전히 호스트 안에서 출발해 여정 84px)
* `node tools/smoke.js` → **SMOKE PASS**
* 회귀 무수정 — `verify194` 54/54 · `verify262` 46/46 · `verify346` 72/72

## 4-2. 2회차 — 비평 루프([3]-(다))가 **1회차 캡처를 무효로 만들었다**

비평가 CR1·CR2 가 **독립으로 같은 것을 잡았다**: 채점용 8프레임에 **채점 대상 카드가 한 픽셀도 없었다.**

* 뿌리 ⓐ — `cap814` 가 `all[1].click()` 으로 «선택» 하려 했는데 `cosSel` 기본값이 `cosBest()` 라
  **그 카드가 이미 선택돼 있었고**, 같은 카드를 다시 누르면 `showCosDetail()`(상세 팝업)이다.
  ⇒ 8장 전부 «팝업이 격자를 덮은 화면». **이제 `select()` 가 팝업이 열리면 예외로 멈춘다**(조용한 무효 금지).
* 뿌리 ⓑ — 실시간 스크린샷은 한 장에 200~480ms 라 수명 620ms 연출이 **1~2프레임**만 잡혔다
  (두 사람 다 `md5` 로 «2~8장이 바이트 동일» 을 찍었다). ⇒ **두 벌**로 찍는다:
  `-step-1..8`(CSS 진행도 0/80/…/560ms — 시간축 정확 · 파티클은 JS 라 정지) ·
  `-live-1..6`(실시간 · 입자 실제). 두 벌을 합쳐야 «시간축» 과 «입자» 를 둘 다 채점할 수 있다.
* ⚑ **그들의 실측 중 하나는 이 판에서 기각됐다** — «스파크가 카드 밖으로 흩어진다»(CR1 «0/5알만 안»
  · CR2 «4알 밖»)는 **깨진 캡처에서 카드 사각을 역산한 결과**였다. 자로 직접 재니 **10/10 알이 카드
  168×171 상자 안**이다(`verify814` [B6] 신설 — verify93 [7-c2] 와 같은 축).
* ⚑ **그들의 처방(«플로터 시작을 dy −114 로 = 카드 위로»)은 산수로 기각된다** — 행 pitch 220 이라
  그 띠는 **위 칸 카드의 몸통**이고, dy −114..0 은 위 칸의 «n/500»(우리 기준 dy −82..−61)을
  **통째로** 덮는다. 자기 카드를 덮던 것을 남의 카드로 옮기는 것이라 711 이 닫은 판정이 깨진다.
* ⇒ **받아들인 것**: «새 값을 말하는 축이 약하다». 값 줄 팝에 **58 20회차의 앰버**를 더해
  (`fxCvLit` — 340ms 동안 #FFC02E 로 물들었다 제 색으로) 두 축으로 말하게 했다. 기하 Δ0px.
  ⚠ 이때 **두 번째 클래스로 얹으면 안 된다** — `animation` 은 단축 속성이라 나중 규칙이 `fxCvSwap` 을
  통째로 덮는다(1회차에 실제로 팝이 죽었고 [B4] 가 그 자리에서 빨개졌다). `.sk-clv.fx-cvswap` 한 선언에
  둘을 같이 적었다. ⇒ **LESSONS 814-④.**

## 5. 곁다리 관측 — **835 등재**(내 변경과 무관)

`tools/verify520.js` **20/23 FAIL**. 세 항 전부 **725(«% → ×N배» 표기 전환)** 이 남긴 자리다:
[A3]·[B3] 이 «공격력 39%» 를 기다리는데 제품은 «×1.344배» 를 그리고, §R 의 되돌림 앵커 4곳 중 2곳이
`pct(...)` 문자열이라 안 잡힌다. **수리 전 트리와 대조해 내 변경과 무관함을 못박았다** — 네 앵커의
존재 여부가 `.pre814.html`(HEAD)과 현재 트리에서 **동일**(False/False/True/True)했다(338·344·654 규칙).

## 6. 다음 사람에게

* **814 를 다시 열 이유는 하나뿐이다** — `verify814` [B]·[C] 가 빨개지는 것(= 연출이 «없음» 쪽으로 더 깎였거나,
  값 줄이 새 값을 안 말하게 됐다). [A] 가 빨개지면 그건 옛 호출이 되살아난 것이다.
* **훈련(씬 C)·09·12·17·장비의 델타는 한 값도 안 바뀌었다.** `verify93` [7-f] 와 `verify814` [E] 가 그것을 지킨다 —
  «코스튬이 조용하니 훈련도 조용하게» 로 58 을 되돌리지 마라.
* 코스튬 카드에 **다시 문구를 얹고 싶다면** 먼저 §2-2 표를 보라. 54.1px 자리에 34px 글리프를 넣는 길은
  «글리프 토큰을 호스트 비례로 바꾸는 것» 뿐인데 그 문은 **491 2회차**(«세 씬 한 규격» — 규격이 셋이 되면
  ④ 일관성 감점)가 닫아 뒀다.
