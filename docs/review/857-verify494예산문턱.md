# 857 — `verify494` [2] 예산 문턱이 «시뮬» 이 아니라 «보정» 을 재고 있었다

> T1 «버그(자 · 환경 의존 문턱)» · 지시서 [3]-**(가)** (기계+자 — 비평가 없음) · 제품 `index.html` **0줄**
> 착수 세션 sess-0558-27828(루틴 워커 D · 2026-09-03 · 1회차)

## 0. 번호 개정 — 856 → 857

등재 push 경쟁에서 같은 번호가 둘이었다. 먼저 올라간 쪽(`24cf090` 05:32:17Z — 792 10회차가 낸
«하이라이트 ③층 폭» T4)이 **856 을 지키고**, 뒤에 올라간 이 행(`a7076e0` 05:54:51Z — 199 27회차 곁다리)을
**857 로 개정**했다 — 286·308 규칙(«먼저 등재된 쪽을 살린다»). 807 이 805 → 807 로 한 것과 같은 자리다.

## 1. 재현이 먼저 — `probe857` (338 규칙)

등재문은 «뿌리 가설(재현 먼저 — 아직 확정 아님)» 이라고 스스로 못박아 두었다. 처방을 고르기 전에
**시간을 무엇이 먹는가**부터 갈라 쟀다. 가르는 방법이 이 작업의 절반이다 —
두 실행의 벽시계를 빼서 짐작하면 **그 뺄셈이 또 기계 사정**이므로, `bot199` 가 **스스로**
보정 몫을 찍게 했다(`report.calSec` · `calFloorSec` · `calSecPerAnchor` · `simSec` — 857 신설).

## 2. 실측 — 328.4초의 98.7%가 시뮬이 아니었다

| 실행 | 명령 | 벽시계 | 보정 몫 | 시뮬 몫 | 결과 |
|---|---|---:|---:|---:|---|
| A | `--days=30 --seeds=1 --policy=diligent` (verify494 [2] 와 **같은 명령**) | **328.4초** | 324.0초 | 4.4초 | s386 · cp 4.209e19 · 벽 12개 |
| B | 같은 명령 **+ `--calib=<캐시>`** | **4.4초** | 0.0초 | 4.4초 | s386 · cp 4.209e19 · 벽 12개 |

⚑ **두 실행의 결과가 «같다» 가 아니라 «자리마다 같다»** — 최종 스테이지·전투력·벽 개수가 전부 같은 값이다.
즉 A 가 B 보다 324초를 더 쓰고 **얻은 것이 없다**. 등재문의 가설(«시간은 시뮬이 아니라 보정이 먹는다»)이
숫자로 확인됐고, 그 몫은 등재문이 짐작한 «대부분» 보다 크다 — **98.7%**.

⚠ 등재문의 234.4초와 여기 328.4초가 다른 것은 **기계 사정**이다(이 컨테이너는 러너 부하를 같이 받는다).
그 차이 자체가 이 항의 병이다 — 문턱이 «봇이 빠른가» 가 아니라 «이 기계가 지금 얼마나 한가한가» 에 걸려 있었다.

⚑ **문턱(120초)은 어느 눈금의 값인가**(LESSONS 845-③ — «문턱을 다시 적기 전에 그 문턱이 어느 눈금의
값인지 물어라»). 494 등재문과 그 회차 기록의 «30일 ≤ 2분» 은 **전부 시뮬 수**다:
4회차 «10일 47초» · 5회차 «10일 47초 → 14.8초» · 6회차 «**30일 1시드 20.8초**». κ 표는 그 뒤
**9회차(정정9)** 가 «실행마다 값이 15% 갈린다» 를 고치며 «캐시가 있으면 실측을 생략한다» 로 만든
**별도 축**이다. ⇒ 120초는 시뮬 자의 값이고, 자를 안 바꾼 채 문턱만 올리는 길(등재문 ⓒ)은
«무엇을 재는지» 를 영영 흐린다.

### 2-1. `probe857` 이 찍은 표 (13/14 — 한 항이 빨간 것이 소득이다, §2-2)

| 항 | 물음 | 실측 |
|---|---|---|
| [1] | 캐시 없는 30일 1시드가 정말 120초를 넘는가 | **321.8초** · 앵커 **19개**를 새로 셌다 |
| [2] | 같은 명령 + 캐시 | 벽시계 **4.6초** · 보정 **0.00초** · 앵커 **0개** |
| [3] | `calSec`(자기 계량)이 두 실행의 벽시계 차와 맞는가 | 벽시계 차 **317.2초** ↔ 자기 계량 **317.7초** = 어긋남 **0.2%** |
| [4] | 보정이 전체의 몇 % | **98.9%** (전체 321.2초 · 보정 317.7초) |
| [5] | 캐시를 쓰면 494 의 문턱이 서는가 | 시뮬 **4.0초** ≤ 120초 (여유 **30배**) |

⚑ **[3] 이 이 작업의 신뢰 근거다.** 자기 계량과 벽시계 뺄셈이 0.2% 안에서 만난다 —
그래서 아래 게이트가 `calSec`·`simSec` 을 **자기 눈금으로 써도 된다**.

### 2-2. ★ 재현이 내 처방을 기각했다 — «앵커 한 개 외삽» 은 오답이다 (338 규칙)

[2-c] 의 첫 시안은 «앵커 한 개를 재고 × 앵커 수 = 표 전체» 였다. 그 자리에서 `probe857` [6] 이 **빨개졌다**:

| | 값 |
|---|---|
| 한가운데 앵커(s580) 한 개 | **6.3초** (+ 하한 고정비 1.1초) |
| 균일 외삽 6.3 × 19 + 1.1 | **121.0초** |
| 실측 보정(19앵커) | **317.7초** — 평균 **16.7초/앵커** |
| 어긋남 | **61.9% 모자람** |

방향을 뒤집은 뒤 다시 잰 값(같은 절이 이제 이것을 단언한다):
**s1 1.2초 ↔ s580 6.1초 = 5.13배** · 외삽 117초 < 실측 324초(**63.7% 모자람**) ·
게이트 눈금 **s580 ÷ 하한 = 5.20배**(문턱 15배 — 여유 2.9배).

**앵커 비용은 스테이지를 따라 커진다** — 높은 앵커일수록 목표 화력까지 «펌프» 하는 몫이 크다.
이대로 게이트에 넣었으면 **«보정이 두세 배 느려져도 초록인 자»** 를 만들었을 것이다.
⇒ [2-c] 를 «총액 예산» 에서 **«회귀 감시자»** 로 고쳐 적었고(아래 §3), 프로브의 그 절도
«한 개 외삽은 **하한**이다» 를 단언하도록 방향을 뒤집었다(333 처방 — 자리를 비우지 않는다).

## 3. 처방 — 문턱은 그대로, **무엇을 재는가**를 갈랐다 (등재문 ⓐ)

`tools/verify494.js` [2] 를 다섯 항으로 나눴다. **`index.html` 0줄 · 봇의 판정·계수 0줄**
(`bot199` 변경은 시간 계량과 표 머리글 한 줄뿐 — 시뮬 경로는 한 줄도 안 건드렸다).

| 항 | 무엇을 재는가 | 자 |
|---|---|---|
| [2-a] | **시뮬** 예산 | κ 는 캐시에서 읽고 30일 1시드 **시뮬만** ≤ 120초 (494 의 문턱 그대로) |
| [2-b] | 그 실행이 정말 보정을 안 샀는가 · 캐시 표가 **지금 앵커 목록과 같은가** | `calFrom` 이 «캐시» · `calAnchors === 0` · `rows.length === CAL_STAGES.length` |
| [2-c] | **보정** 예산 | 앵커 한 개를 캐시 없이 재고 표 전체를 외삽. 문턱은 절대 초가 아니라 **하한 보정에 대한 비** |
| [2-d] | 되돌림 시험 | 앵커를 둘로 늘리면 [2-c] 의 축이 실제로 커진다 |
| [2-e] | 전체 보정 실측 | `V494_CALFULL=1` 에서만 — 외삽이 실측과 ±40% 안에서 맞는가 |

⚑ **[2-b] 가 이 처방의 안전핀이다.** «캐시를 줘서 초록» 과 «자를 무르게 풀어서 초록» 은 겉모습이 같다 —
[2-b] 가 없으면 다음 회차가 `--calib` 을 지워도, 캐시 표가 앵커 목록과 안 맞게 낡아도 아무도 안 짖는다.

⚑ **캐시 표는 이름이 아니라 «번호» 로 고른다** — `docs/review/199-calib-r*.json` 중 r 번호가 가장 큰 것.
파일 이름을 손으로 적으면 199 가 r26 을 올리는 날 그 자가 조용히 낡은 표를 잡는다(211·289 «게이트가
자기 사본을 들면 부패한다» 의 파일판).

⚑ **[2-c] 의 문턱이 «비» 인 이유** — 앵커 하나와 하한 보정(`calibrateFloor`) 하나는 둘 다
«새 페이지 부팅 + BOT 주입 + 전투 시뮬» 이라, 기계가 두 배 느려지면 분자·분모가 같이 두 배가 된다.
574·709·825·855 계열(문턱 플레이키)이 전부 **절대 수를 러너에 대고** 있다가 걸린 자리다.

⚑ **[2-e] 를 옵트인으로 뺐으면 «대체 자» 를 같이 둬야 한다**(LESSONS 237-⑥ — «옵트인만 하고 대체 자를
안 넣으면 성능이 진짜 나빠진 것을 영영 못 잡는다»). 여기서 그 대체 자가 [2-c]·[2-d] 다.

## 4. 게이트

| 자 | 결과 |
|---|---|
| `node tools/verify494.js` | **99/99 PASS · 77초** (수리 전에는 **[2] 한 항에만 321.8초** — 그리고 나머지 아홉 번도 같은 값을 샀다) |
| `node tools/probe857.js` | **15/15** 신설 (346초 — [1] 이 캐시 없는 30일을 일부러 사기 때문이다) |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verifyProgress.js` | 종료 코드 **0**(§1~§5 전부 빨강 0건) |

### 4-1. ⚑ [2] 만의 문제가 아니었다 — 자가 봇을 **열 번** 부른다

수리 도중에 드러난 더 큰 자리다. `verify494` 는 [2]·[3]·[5]·[6]·[8]·[9]·[R] 절에서 봇을 **열 번** 부르는데
**그 전부가 `--calib` 없이** 부르고 있었다 — 한 번에 320초씩이면 자 전체가 **한 시간에 가깝다**.
지시서 [4] 가 «push 전에 돌려라» 고 요구하는 자가 그 값이면 **아무도 안 돌린다**(807 이 «모두에게 빨간 자» 로
겪은 병의 다른 얼굴이다 — 실제로 725·754 의 push 가 그 빨강 위에서 나갔다).
⇒ 부르는 자리마다 손으로 붙이지 않고 **입구(`run`) 한 곳**에서 붙였고(402 «사본을 지운다»),
**일부러 캐시를 안 받아야 하는 자리 둘**만 이름을 붙여 남겼다 —
`--calstages`(보정 축 [2-c]·[2-d] · 여기서 캐시를 받으면 그 항이 공허참이 된다)와
`--fresh-cal`([2-e] 옵트인 · 이 자만의 표식이라 `bot199` 로는 안 넘어간다).

**321.8초(한 항) → 77초(자 전체 99항).**

### 4-2. 되돌림 시험이 지키는 것

«캐시를 줘서 초록» 과 «자를 무르게 풀어서 초록» 은 겉모습이 같다. 그래서 세 항이 서로를 지킨다:
**[2-b]** 가 «이 실행이 정말 캐시를 썼나 + 그 표가 지금 앵커 목록과 같은가» 를 묻고,
**[2-d]** 가 «앵커를 둘로 늘리면 몫이 실제로 커지는가» 를 묻는다(실측 7.0초 → **21.8초**).
[2-d] 가 없으면 [2-c] 는 «아무것도 안 재고 늘 초록인 비» 일 수 있다.

수리 전후로 **제품 `index.html` 0줄** · 봇의 시뮬·판정·계수 **0줄**.
`bot199.js` 변경은 넷뿐이다 — 보정 구간 시각 두 개(`tCal0`·`tFloor0`), 파생 필드 다섯
(`calSec`·`calFloorSec`·`calAnchors`·`calSecPerAnchor`·`simSec`), 콘솔 한 줄, 표 머리글 한 줄.

## 5. 남은 것 · 다음 세션에게

- **[2-e] 는 옵트인이다.** `V494_CALFULL=1 node tools/verify494.js` 로 캐시 없는 30일(≈5분)까지 재려면
  그 손잡이를 켜라. 기본 실행은 그 5분을 사지 않고, 대신 [2-c]·[2-d] 가 보정 축을 지킨다.
- **κ 캐시가 낡으면 [2-b] 가 짖는다** — `docs/review/199-calib-r*.json` 중 **r 번호가 가장 큰 표**의
  앵커 수가 `CAL_STAGES` 와 다르면 빨강이다. 199 가 앵커 목록을 바꾸면 **새 확정 표를 같이 올려라**
  (그것이 이 자가 요구하는 유일한 유지비다).
- ⚑ **199 에게**: `bot199` 실행 표 머리글에 이제 «보정 N초 + 시뮬 M초» 가 찍힌다. 27회차가 «자를 돌리는
  기계» 를 의심했던 자리를 이제 표가 스스로 답한다 — 캐시(`--calib`)를 안 주고 돌린 표는
  **시간의 98.9%가 κ 재측정**이라는 뜻이므로, 스윕을 돌릴 때는 `--calib=<확정 표>` 를 붙여라.
- ⚠ **앵커 비용은 스테이지를 따라 커진다**(§2-2 · s1 ↔ s580 ↔ s600 이 6.2 → 14.7초로 벌어진다).
  보정 총액을 예산으로 걸고 싶어지면 **앵커별 표**부터 만들어라 — 균일 외삽은 61.9% 모자란다.
