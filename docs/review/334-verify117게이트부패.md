# 334 — `tools/verify117.js` C3 1건 실패 · 게이트 부패 (34 의 13회차가 알약을 두 노드로 쪼갰다)

작업 종류: **T1 버그(게이트 부패)** — ROUTINE [3]-(가) 기계적/게이트 계열이라 **비평가는 띄우지 않았다.**
1회차로 종료. 제품(`index.html`) **0줄** — 등재문의 예상 그대로 게이트만 썩어 있었다(319·333 선례와 같은 꼴).

담당: 2026-08-28, `sess-1434-23454` 루틴 워커 B.

---

## 1회차 (2026-08-28)

### 재현 (착수 기준선 `826096a`)

```
node tools/verify117.js
…
FAIL C3 카드 클릭 → 축복 경험치 +1 · 타이머 표시 — 1 00:30:00
…
VERIFY117 FAIL — 39/40
```

빨간 것은 **한 줄뿐**이고, 나머지 39 줄은 전부 초록이다. 그중에는 같은 클릭이 만든
`C1`(×1.20) · `C2`(stat.dmg ×1.20) · `C4`(4회 → Lv2) 가 들어 있다 — **경험치도 배율도 정상**이라는 뜻이다.
빨간 줄의 detail 도 `1 00:30:00` 으로 «경험치 1 · 남은 시간 30분» 을 그대로 찍고 있었다(LESSONS 213-①:
«게이트 부패인가 진짜 회귀인가» 는 빨간 줄이 아니라 **초록 줄**이 먼저 답한다).

### 뿌리

34 의 **13회차**가 알약 내용을 두 노드로 쪼갰다 — 한 노드에 «⏱ 00:01:20» 을 통째로 담으면
시계와 숫자의 **크기를 따로 못 고치기** 때문이다(`index.html` ~11239 주석 · `.bls-c .tm>i` 규칙).

| | DOM | `.tm>i` 의 textContent |
|---|---|---|
| 13회차 **전** | `<s class="tm"><i>⏱ 00:30:00</i></s>` | `"⏱ 00:30:00"` |
| 지금 (325 의 `.updot` 포함) | `<s class="tm"><b class="ck">⏱</b><i>00:30:00</i><s class="updot"></s></s>` | `"00:30:00"` |

게이트는 아직 한 노드이던 시절의 **표기 형식**을 물었다:

```js
/^\u23F1 \d\d:\d\d:\d\d$/.test($('#blsC_atk .tm>i').textContent)
```

`"⏱ "` 는 이제 형제 `<b class="ck">` 에 있으므로 이 정규식은 **영영 false** 다.
같은 줄의 `prog === 1` 은 내내 초록이었다 — **값은 정상이고 형식 단언만 옛 DOM 을 본다.**

### 고친 것 — 게이트만 (`tools/verify117.js`, 제품 0줄)

319 선례 그대로 «게이트 부패에 제품을 맞추지 마라». 처방 ①(두 부품을 각각 단언)을 골랐다 —
처방 ②(정규식에서 `⏱ ` 접두어만 빼기)는 **시계가 통째로 사라져도 초록**이라 «타이머 표시» 라는
이 단언의 원래 뜻을 잃는다.

```js
const tm1 = document.querySelector('#blsC_atk .tm'), ck1 = tm1.querySelector('b.ck');
… ck: ck1 ? ck1.textContent.trim() : null, txt: tm1.querySelector('i').textContent
…
ok(C.c1.prog === 1 && C.c1.ck === '\u23F1' && /^\d\d:\d\d:\d\d$/.test(C.c1.txt), 'C3 …(시계 .ck + 숫자 .tm>i 두 부품)')
```

`.ck` 가 없으면 `ck: null` 로 나가 그대로 빨개진다(`querySelector` 널을 술어 밖에서 흡수하지 않았다).

### 되돌림 시험 — 신설 `C3n` (LESSONS 232-① · 289 계열 경고)

게이트 부패의 가장 쉬운 «고침» 은 빨간 단언을 **무르게** 만드는 것이다. 그래서 옛 DOM 두 모양을
일부러 만들어 C3 의 술어가 뒤집히는지를 같은 게이트 안에서 못박았다(DOM 은 즉시 원복 · 끝에 `renderBless()`).

| 만든 모양 | 기대 | 실측 |
|---|---|---|
| 산 DOM (`<b class="ck">⏱</b>` + `<i>00:30:00</i>`) | 초록 | `true` |
| ⓐ `<b class="ck">` 삭제 | **빨강** | `false` |
| ⓑ `<i>` 에 옛 합본 «⏱ 00:30:00» | **빨강** | `false` |
| 원복 | 초록 | `true` |

ⓑ 가 초록이면 «두 노드 시절» 과 «한 노드 시절» 을 구별 못 하는 것이라 부패가 그대로 재발한다.
술어는 C3 와 **같은 것을 그대로** 쓴다 — 여기서만 무른 사본을 쓰면 시험이 거짓말을 한다.

### 결과

```
node tools/verify117.js   → VERIFY117 PASS — 41/41   (39/40 → 40/40 + C3n 신설)
```

### 전수 스윕 (LESSONS 214-①: 하나 고쳤으면 같은 규약을 묻던 게이트를 전부 돌린다)

`grep -rn "⏱" tools/*.js` · `grep -rn "\.tm>" tools/*.js` 로 뽑아 **«시계 글리프를 값과 한 노드에서 재는»
단언은 verify117 C3 하나뿐**임을 확인했다. 나머지 `⏱` 사용처(`verify284`·`285`·`208`·`46`·`162`·`30`)는
전부 **전투 HUD `#bossTm`/`#dunTm`** 쪽이고 숫자(`"15.0"`)나 박스 크기를 재지 시계 글리프와 합본이 아니다.
34 알약을 직접 만지는 게이트는 13회차 **이후**에 쓰여 이미 `b.ck` 를 알고 있었다 — 헛초록(232-①)도 없었다.

| 게이트 | 결과 |
|---|---|
| `node tools/verify117.js` | **41/41 PASS** (수리 대상) |
| `node tools/verify325.js` | 69/69 PASS |
| `node tools/probe325.js` | 18/18 PASS |
| `node tools/verify299.js` | 25/25 PASS |
| `node tools/verify95.js` | PASS (34 축복 잘림 0) |
| `node tools/func117.js` | 콘솔 에러 0 · 만료 행이 카드 «받기» 로 정상 표기 |
| `node tools/smoke.js` | **SMOKE PASS** |

`func117.js` 도 `.tm>i` 를 읽지만 그 자리는 **만료 뒤 «받기»** 를 찍는 자리라 쪼갬과 무관하게 옳다 —
고칠 것이 없어 한 줄도 안 건드렸다.

### 남은 것

없음. 제품 0줄 · 게이트 1개 파일(`tools/verify117.js`) 변경으로 닫는다.
