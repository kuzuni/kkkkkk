# 538 — `verify75.js` 게이트 부패 (2026-08-30, sess-2116-11400 워커 A · 1회차)

**제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정·밸런스 0줄.** 고친 것은 `verify75.js` 한 파일이다.

---

## §1 재현 (338 규칙 — 처방 전에 먼저)

등재문 그대로 즉사한다.

```
$ node verify75.js
page.evaluate: ReferenceError: COLL is not defined
    at /home/user/kkkkkk/verify75.js:43:22
```

`tools/probe538.js` **신설 22/22** 로 갈래를 갈랐다.

### ⚑ 뿌리는 «COLL 한 줄» 이 아니다

이 파일은 `pg.evaluate` 가 **하나뿐**이었다. 그 안 마지막 두 줄이 `COLL.equip.tiers` 를 읽었고,
JS 는 블록을 통째로 던지므로 **17항 전부**가 그 한 줄에 삼켜졌다(532-③).
즉 이 게이트는 «빨간 적이 없는» 것이 아니라 **한 번도 돈 적이 없다.**

크래시를 걷어내자 **6항이 더 빨갰고, 여섯 자리 전부 75 이후의 주인 지시가 지난 자리**였다:

| 옛 단언 | 실측(현행) | 누가 옮겼나 |
|---|---|---|
| 부위당 18종 · 총 **54** | 부위당 36 · 총 **108** | **85** (8등급 × 5종, 최상위만 1종) |
| 등급별 **4,4,3,3,2,2** | **5,5,5,5,5,5,5,1** | **85** |
| «**j=0** 은 v 1.00» | j0 = `weapon0_1`(v0.92) … = **거짓** | **260** (등급 배열을 v 오름차순 재배치 · id 명시) |
| `equipVal` 비 = v 비 **1.12** | **2.25** (v 축이 아니다) | **472** (장착 축 → `EQ_BASE(등급,티어)` 표) |
| `relicVal = v·gMul·lvMul·0.5` | `v·gMul·oLv·0.25` | **89 · 197** (유물은 보유 Lv 선형) |
| 합성은 «같은 j 유지 · **결정적**» | 200회에 다음 등급 **5종 전부** | **85 ⑤ 주인 지시** («다음 등급 5종 중 랜덤») |
| 등급 안 **균등** 추첨 | 13.7 / 15.2 / 20 / 24.2 / 26.8 % | **251** (등급 안 티어 가중) |
| `COLL.equip.tiers[].need` 12/24/33/42/48/54 | 전역 `COLL` **소멸** · `COLL_SETS` 41세트 | **91** (카테고리 티어 표 폐기) |

### 어느 쪽이 틀렸나 — 형제 자가 답을 갖고 있었다 (532-②)

`tools/verify91.js` 63·85행이 **`typeof COLL === 'undefined'`(«구 COLL 폐기»)** 를 단언한다.
`verify75` 는 그 반대를 단언한다. **둘 다 초록일 수 없다** — 333(149 ↔ 295)과 정확히 같은 모양이고,
나중 지시(91)가 옳다. ⇒ **제품 0줄 확정.**

게다가 **같은 부패를 이미 두 자가 겪고 고쳤다** — `verify85` **[I]** 와 `verify86` **[5]** 가
주석까지 남겨 뒀다(«91 이 … `COLL.equip.tiers` 가 사라져 이 절이 즉사하고 있었다»).
처방 선례가 저장소 안에 있었다.

---

## §2 처방 — 값을 갈아 끼운 게 아니라 **묻는 것**을 남겼다

옛 값을 새 값으로 바꾸기만 하면(54 → 108, 4,4,3,3,2,2 → 5,5,5,5,5,5,5,1) **85 다음 지시에서 또 썩는다.**
그래서 **522-①** 대로 종 수·등급 수·기대 분포·도감 구성원을 전부 제품 표에서 **역산**했다
(`EQUIPS` · `GRADE` · `SLOTS` · `WPN_ROWS/COLS` · `tierWeights` · `PRB_STEPS_EQ` · `COLL_SETS`).
게이트에 남은 손으로 적은 상수는 **0개**다.

| 절 | 옛 단언 | 새 단언 (묻는 것은 그대로) |
|---|---|---|
| ① | 총 54 · 등급별 4,4,3,3,2,2 | **등급마다 종이 여럿**(최상위 제외) · 최상위는 부위마다 1종(85 규약) · 부위 3종 대칭 · 총수 = 부위 × 부위당 |
| ① | «j=0 은 v 1.00» | **«구 id(`slot+g`) 는 v 1.00»** — 260 이 j 를 옮겼어도 개체차의 **기준점**은 구 id 다(구 세이브가 그 값에 묶여 있다) |
| ② | 등급 안 전 항목 ≥ 5% | 전 종이 실제로 나온다 **+ 분포가 제품의 `tierWeights` 를 따른다**(최대 편차 ≤ 5%p) |
| ③ | `equipVal` 비 = 1.12 | **보유 축** `ownVal` 비 = v 비 · **장착 축** 에서도 «v 큰 쪽이 크다»(260 이 j 를 v 순으로 정렬) · 스킬/유물 불변 |
| ④ | 같은 j 유지 · 결정적 | 다음 등급 · 같은 부위 **풀 안에서** 나온다 · **풀 전체가 나온다**(85 ⑤ 랜덤이 한 종에 굳지 않았다) · 최상위 null |
| ⑥ | 격자 칸 수 · 확률 팝업 한 단계 | 행마다 앞 칸이 그 등급 아이템 열과 **일치**(36/36종) · 확률 팝업은 **전 단계 합집합**(86 선례) |
| ⑦ | `COLL.equip.tiers` need 표 | **도감 장비 세트가 전 종을 담는가**(108/108) · 세트 = 부위 × 등급 · 구 `COLL` 폐기 확인 |

### 319 처방 — 절을 갈랐다

`evaluate` 를 **7개로 쪼개고 각각 `try` 로 감쌌다**(`blk()`/`alive()`).
이제 한 자리가 죽어도 **그 블록만 빨개지고** 나머지 절은 계속 돈다 — 오늘 같은 «한 줄이 17항을
삼키는» 일이 구조적으로 안 생긴다.

### ⚠ 식을 베끼지 않았다 (523)

③ 은 `ownVal` 식을 사본으로 들지 않는다. **v 를 런타임에 ×1.05 로 흔들어** 보유값이 그만큼 따라
움직이는지, 스킬·유물 값은 안 움직이는지를 본다. 제품이 식을 바꿔도 «축이 살아 있는가» 는 그대로 물린다.

---

## §3 무르게 풀지 않았음 — §R 되돌림 시험 3종 (532-④)

«지금 있는 값으로 갈아 끼우는» 수리의 기본값은 **헛초록**이다. 제품 사본(임시 디렉터리)을 셋으로
망가뜨려 새 단언이 **실제로 빨개지는 길**이 있는지 못박았다. 사본 편집이 안 걸리면 그것도 FAIL 이다.

| | 사본에 넣은 고장 | 결과 |
|---|---|---|
| **§R1** | 일반 등급을 1종으로 접는다 | 등급별 `[1,5,5,5,5,5,5,1]` → ① **빨강** ✓ |
| **§R2** | 구 id `weapon0` 의 v 를 1.00 → **1.09** | ① **빨강** ✓ |
| **§R3** | 도감 빌더에서 세트마다 구성원 1종씩 제외 | **84/108** → ⑦ **빨강** ✓ |

---

## §4 결과

```
verify75   즉사(0항)  →  PASS 32/32   (§R 3항 포함)
probe538   22/22 신설
```

회귀 — **전부 무수정 초록**:
`verify91` 67/67 · `verify85` 39/39 · `verify86` 38/38 · `verify193` 77/77 · **SMOKE PASS**.

⚑ 오늘 «처음 돈» 항이 이 게이트의 전부다. 그중 **초록이 확인된 것**: 구 세이브 장착·레벨·재료 보존
(`weapon3` / Lv12 / 재료 7 / `shield1`) · 소환 시뮬 뒤 장착 불변(263) · NaN 0 · 콘솔 에러 0 ·
격자 36/36종 · 확률 팝업 36/36종 · 도감 108/108종.
**«초록이었던 적이 없다» 와 «빨간 적이 없다» 는 다른 말이다**(532-③) — 이 일곱은 오늘 처음 초록이 됐다.

---

## §5 곁다리 관측 1건 (등재)

**549 — `verify86` 247행 · `verify193` 524행이 `PRB_STEPS[i].unlock` 을 넘긴다.**
`PRB_STEPS` 는 **250 이후 소환 레벨 숫자 배열**(1..`SUM_MAXLV`)이라 `.unlock` 은 `undefined` 다.
`openProbInfo(bank, undefined)` 는 예외 없이 **«현재 레벨»** 로 떨어진다(522-② «숫자가 틀렸는데
에러가 안 나면 가장 오래 사는 부패»). 두 자가 지금 초록인 것은 **바로 다음 줄** `prbStep = i;
renderProbInfo();` 가 단계를 덮어쓰기 때문이고, 그 한 줄이 사라지면 두 자는 **조용히 10행만**
보면서 계속 초록이다. 내가 이 파일에서 같은 함정을 실제로 밟았다(단계 합집합이 25/36 → 10/36).
**수리 전 커밋에서도 같은 상태**임을 확인했다(내 변경과 무관 · 내 작업 단위가 아니라 안 고쳤다).
처방은 값을 그대로 넘기는 것 — 이 파일이 그렇게 했다(`openProbInfo('weapon', lv)`).

---

## §6 다음 세션이 볼 것

- **루트 게이트를 잡으면 `evaluate` 개수부터 세라.** 하나뿐이면 그 파일의 점수는 «몇 항이 초록인가»
  가 아니라 **«전부 아니면 전무»** 다. 529 가 런처를 고친 뒤 처음 돈 7개 중 이 파일이 그 극단이었다.
- **75 처럼 «데이터 확장» 게이트는 뒤 작업이 같은 표를 또 확장한다.** 종 수·등급 수를 손으로 적으면
  다음 확장에서 자동으로 썩는다. 이 파일에는 이제 손으로 적은 종 수가 **0개**다.
