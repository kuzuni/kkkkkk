# 813 — 89 유물 소환 «짧은 프레임 여백 예산 재배분» (T4 폴리시 · 120 의 라운드)

등재: 2026-09-02, sess-1118-6492 워커 A (754 6회차가 «산술적으로 이 작업이 못 닫는 자리» 로 넘긴 것)

---

## 1회차 (2026-09-03, sess-0358-6402 루틴 워커 D) — **제품 0줄 · [❌] 두 쌍이 성격이 갈렸다**

### 결론 먼저

| | 754 가 넘긴 것 | 1회차 판정 |
|---|---|---|
| 쌍ⓑ `.rw-cap ↓ .rw-fc.bl` | 간극붕괴 (기준의 22%) | **유령** — 두 물체의 가로 겹침 **0px**. 보이는 여백은 44.4% 로 붕괴가 아니다 |
| 쌍ⓐ `.rw-lintel ↓ #rwMulBar` | 간극붕괴 (기준의 12%) | **실재** — 비평 2인이 각자 지적. 단 **E 재배분과 한 벌로만** 열린다 |

**제품은 한 줄도 안 바꿨다.** 회차 도중 쌍ⓑ 를 닫는 수리(`--rw-g3` 하한 44 → 50)를 넣었다가
**되돌렸다** — 아래 §3 이 그 이유이고, 이 회차에서 가장 값나가는 관측이다.

### §1 재현 (338 규칙 — 처방 전에 먼저 굴린다)

`tools/probe813.js` 신설. `probe754` 는 **간극만** 재고 그 간극을 만든 예산 변수를 안 보므로,
쌍 간극(probe754 와 **같은 규칙** — `judgeGaps` 를 require 한다)에 더해 예산 변수와 120 의 최소
요구를 같이 찍는다. 등재문 수치가 그대로 재현됐다:

```
  ❌ .rw-lintel ↓ #rwMulBar    13.8  107.3  114.2  114.3  114.3   (기준의 12.1%)
  ❌ .rw-cap ↓ .rw-fc.bl       16.7   31.6   41.9   75.8   75.8   (기준의 22.0%)
  ⚠  #rwGrid ↓ .rw-mid          195  318.2  357.9  604.6  854.1   ← 비평가가 7명째 짚은 자리
```

예산 변수(그려진 기하에서 역산 · 패널 지역 좌표):

| | 1600 | 1841 | 1920 | 2280 | 2600 |
|---|---|---|---|---|---|
| 패널H | 1292.3 | 1529.7 | 1607.5 | 1962.1 | 2277.3 |
| 상인방 상변 `lt` | 19.7 | 19.7 | 40.5 | 114.4 | 180.1 |
| 격자 상변 `gt` | 208.8 | 308.2 | 336 | 409.9 | 475.6 |
| 아치 뻗음 `av` | 111.1 | **186(상한)** | 186 | 186 | 186 |
| 안내문 아래 `g3` | **44(하한)** | 58.2 | 68.5 | **104(상한)** | 104 |
| 벽(상인방 하변→격자 상변) | **124.1** | 223.5 | 230.5 | 230.5 | 230.5 |
| 벽 − 바 98 = 여유 | **26.1** | 125.5 | 132.5 | 132.5 | 132.5 |

### §2 지렛대 스윕 — 등재문의 후보 셋이 **전부 쌍ⓐ 를 못 닫는다**

`tools/probe813b.js` 신설. 세 지렛대가 **서로를 상쇄**하기 때문에(g3 를 올리면 수반이 올라가
`tt` 가 줄고 `av` 가 절반만큼 줄어 **벽이 좁아진다**) 손 산수 대신 브라우저에게 직접 물었다.

| 후보 | 쌍ⓐ 1600/2280 (비) | 쌍ⓑ (비) | 스트립 2280 | 아치h 1600 | 클리어런스 |
|---|---|---|---|---|---|
| 현행 | 13.8 / 114.3 (**12.1%**) | 16.7 (22.0%) | 114.4 | 738.1 | 78.8 |
| ① g3 44→50 | 12.3 (10.8%) | 22.7 (**29.9%** ✓) | 114.4 | 732.2 | 78.8 |
| ② av 174→160 | 17.2 (15.0%) | 16.7 | 114.4 | 738.1 | 65 |
| ② av 174→150 | 19.7 (17.2%) | 16.7 | 114.4 | 738.1 | 55.2 |
| ② av 174→**134** | 27.6 (**24.1%** ✗) | 16.7 | 114.4 | 738.1 | **39.4** |
| ③ lt 300→260 | 13.8 / **74.9** (18.4%) | 16.7 | **153.8** | 738.1 | 78.8 |
| ③ lt 300→243 | 13.8 / 74.9 (18.4%) | 16.7 | 153.8 | 738.1 | 78.8 |

읽는 법 —

- **② 는 하드 하한을 깨고도 미달이다.** «받침 40 + 접합선 띠 13 = 53» 이 아치 발과 수반 사이의
  구조적 최소인데, 24.1% 를 만드는 지점(174 → 134)의 클리어런스가 이미 **39.4** 다. 그러고도
  판정선 25% 에 못 미친다.
- **③ 은 260 에서 «막힌다».** 243 을 넣어도 260 과 **같은 값**이 나온다 —
  `--rw-lt` 의 clamp 상한 인자가 `gt − av − 74`(= 아치 정점 위 8px)라 상인방이 그보다 아래로
  못 내려온다. ⇒ 긴 프레임의 기준 간극에는 **74.9 라는 바닥**이 있고, 1600 의 13.8 은 그래도 18.4% 다.
  게다가 대가가 «상인방 위 죽은 벽» 114.4 → **153.8** 이라, 14회차가 비평가 넷(AB ⑤·AD ②·AE ③·AG ①)의
  지적으로 묶어 둔 축을 되레 연다.

⇒ **쌍ⓐ 를 닫는 길은 셋 다 다른 회차가 비평 근거로 세운 값을 무너뜨린다.**
   열리는 길은 하나뿐이고, 그것이 아래 §4 의 E 재배분이다.

### §3 ⚑ 쌍ⓑ 는 유령이다 — 넣었던 수리를 되돌린 이유

스윕 ① 대로 `--rw-g3` 하한을 44 → 50 으로 올려 쌍ⓑ 를 **16.7 → 22.7 (29.9%)** 로 열었고,
`verify120` 157/157 · `verify700` 26/26 · `verify754` 60/60 · SMOKE PASS 까지 받았다.
그 뒤 두 가지가 그 수리를 무효로 만들었다.

**ⓐ 두 물체가 서로 안 닿는다.** 안내문 잉크는 x **88.4..991.6 / 292.8..787.2**, 코너 브래킷은
x **3..33** — 가로 겹침 **0.0px**(다섯 프레임 전부). 자가 잰 것은 «칠해지는 변과 무관한 박스 값»
이고, 이는 754 6회차 ⓐ 에서 비평가 CD 가 54·06 의 [❌] 를 기각한 것과 **같은 부류**다.
눈에 보이는 여백은 «안내문 잉크 하변 → 패널 안쪽 하변» 이고 그 값은

  **48 / 63.1 / 73.6 / 108 / 108 → 최소/기준 = 44.4%**  (판정선 25%)

로 **애초에 붕괴가 아니다.** 어긋남의 출처는 «브래킷 높이 27 + 상자가 마지막 줄 잉크보다
4px 아래로 남는 몫» = 31px 이다(`verify813` [2c] 가 다섯 프레임에서 이 산수를 확인한다).

**ⓑ 올린 방향이 레퍼런스와 반대다.** 아래 §4 의 비평 2인이 **독립으로** 레퍼런스를 재
«안내문 위 31~38 : 아래 18~23» = 비 **0.58~0.62** 라고 냈다 — 레퍼런스의 안내문은 패널 하변에
붙은 **푸터**다. 우리는 아래/위가 1.16 → **2.74** 로 이미 반대이고, 하한을 올리면 그 역전이
**더 벌어진다**(`verify813` [R] 이 사본으로 1.16 → 1.32 를 찍는다).

⚠ 14회차가 하한을 44 로 올린 근거(«1600 은 아래 34 < 위 41 로 역전»)는 **레퍼런스가 아니라
우리 화면끼리의 비교**였다. 레퍼런스는 그 «역전» 을 원래 갖고 있다. 이 어긋남은 이번 회차가
고칠 자리가 **아니다** — 아래 여백을 줄이면 그만큼의 세로가 곧장 E 로 흘러 1순위 결함을
키우기 때문이다(스윕이 그것을 보여 준다). **E 재배분과 한 벌로** 잡아야 한다.

### §4 비평 — 2인 독립(CF · CG) · **①~④ 최저 3점**

브리핑 `docs/review/813-비평브리핑.md` · 캡처 `docs/shots/754-813r1a-89-{1600,1841,1920,2280,2600}.png`
(커밋 안 함 — .gitignore). 두 사람 다 부품 «크기» 는 레퍼런스와 픽셀 단위로 일치한다고 확인했고
(타일 151×151 · 행 피치 176 · 격자 블록 501~502 · 안내문 잉크폭 899~900 · 열 중심 최대 Δ2px),
**감점이 전부 «세로 여백 배분» 한 축에 몰렸다.**

| | ① | ② | ③ | ④ | ⑤ | ⑥ | 최저 |
|---|---|---|---|---|---|---|---|
| CF | 5 | 7 | **3** | 6 | 8 | 8 | **3 (③)** |
| CG | 5 | 8 | **3** | 6 | 7 | 8 | **3 (③)** |

#### ⚑⚑ 2인 독립 일치 1 — E(격자↔수반)가 늘어난 세로를 **66.9%** 독식

두 사람이 **같은 숫자**를 냈다. 패널이 1600 → 2600 에서 +1000px 자랄 때 배분:

| | E(격자↔수반) | A(패널상변→바상변) | I(안내문 아래) | C(바↔격자) | G(수반↔안내문) |
|---|---|---|---|---|---|
| 증분 지분 | **+669 (66.9%)** | +265 (26.5%) | +60 (6.0%) | +6 | **0** |

- CF: E = 215 / 340 / 380 / 631 / **884** (**4.11배**) · ref 환산 362 대비 1600 은 0.59배, 2600 은 **2.44배**
- CG: E = 220 / 345 / 385 / 636 / **889** (**4.04배**) · 패널 대비 16.9% → **38.6%**(ref 23.5%)
- CF «E/격자 높이 = 0.43 → **1.76**(ref 0.72)» · CG «2600 에서 이 빈 구간이 격자의 1.77배, 수반의 4.29배»

⚑ **이제 비평가가 아홉 명째 같은 자리를 짚었다**(120 18회차 AJ·AK·AL·AM·AO 다섯 + 754 6회차
CD·CE 둘 + 이번 CF·CG 둘). 120 18회차는 «닫으려면 격자(516)·수반(216)이 프레임을 따라 커져야
하는데 그건 89 확정 규격(반려안 ⓓ)» 으로 넘겼는데, **이번 두 사람의 처방은 그것을 요구하지 않는다** —
부품은 그대로 두고 **남는 세로를 다섯 군데로 쪼개라**는 것이다.

#### ⚑⚑ 2인 독립 일치 2 — 쌍ⓐ 는 실재한다 (Q2)

- 들보 하변 → 바 상변: **16(CF) / 22(CG)** · 1841~2600 은 111~124 ⇒ 다른 프레임의 **1/7.7**
- 바 하변 → 격자 상변: **13** (1600) · 19 (나머지)
- 두 사람 다 «19px 은 격자 자신의 행 간(25~26px)보다 좁아 **바가 격자의 0번째 행처럼 읽힌다**» 를
  독립으로 지적했다. CF «비 6.5:1 → 목표 2:1» · CG «바↔격자 = 행 간의 1.5배 38px 고정»

⇒ 754 6회차의 «28 을 8/20 → 14/14 로 나눈다» 는 **방향은 맞았지만 양이 모자랐다.**
   두 사람의 처방(C = 38~48 고정 · 들보↔바 최소 50~124)은 1600 의 벽 **126** 안에 안 들어간다
   (98 + 48 + 50 = 196). ⇒ 벽을 넓혀야 하고, 그 세로의 출처가 **E** 다. §2 와 §4 가 여기서 만난다.

#### 2인 독립 일치 3 — 안내문 위:아래 역전 (Q3)

위 43px **다섯 프레임 고정**, 아래 46~48 → **106~109**(2.3배 진동). 아래/위 = 1.07 → **2.47**.
레퍼런스는 **0.58(CF) / 0.62(CG)**. ⇒ 비율 오차 **4.3배**. 두 사람 다 «안내문이 수반의 캡션이
아니라 패널 하단에 뜬 별개 덩어리로 읽힌다» 고 적었다.

#### 2인 독립 일치 4 — 배수 바의 가로 기준선 없음 (④)

바 x 178..901(폭 724) vs 격자 x 91..986. CF «행1 타일 블록보다 38px 바깥, 행2 블록보다 87px 안쪽» ·
CG «폭이 166px(18.7%) 좁고 좌우 여백이 1.97배 불일치». 세로 중심축(539.5)은 두 사람 다 정상 확인.
⚠ 폭 724 는 713 이 쓴 «칸 181 × 4» 라 **668/700/701 공용 부품**과 얽힌다 — 여기만 바꾸면 세 화면이 갈린다.

#### CG 만 짚은 것 (2인 일치 아님 — 등재만)

- 바 내부 4칸이 등분이 아니다: 선택 칩 ×1 폭 **193** vs 나머지 176.7(**+9.2%**) · 라벨 잉크 간격 172/171/**190**
- 비선택 라벨 대비 3.20:1 (⑤ 축 — 통과를 막지 않음)

#### CF 만 짚은 것 (2인 일치 아님 — 등재만)

- 코스트 알약 폭 **294** vs ref 환산 251 (**+17%**) · 수반 폭 대비 0.89 (ref 0.75)
- «유물 소환» 잉크 130×35 vs ref 140×31 (폭 −7% · 높이 +13%) — CG 도 폭 −11.4% 로 같은 방향

### §5 게이트

- **`tools/verify813.js` 11/11 신설** — [1] 하한 44 원복 · [2a·b·c] **유령 증명**(가로 겹침 0px ·
  보이는 여백 44.4% · 어긋남 = 브래킷 27 + 상자 여유 4) · [3] 위:아래 비를 **수치로 고정**(2회차가
  조용히 못 옮기게) · [4] 아치 752 원복 · [5a·b·c] 쌍ⓐ 의 예산 산수 · **[R] 되돌림 시험**
  (하한을 50 으로 올린 사본에서 [3] 의 역전이 1.16 → 1.32 로 **더 벌어진다** = 1회차가 되돌린 이유를
  자가 직접 보여 준다)
- `tools/probe813.js` 신설(예산 재현) · `tools/probe813b.js` 신설(지렛대 스윕) ·
  `tools/scan813.py` 신설(구간별 잉크 프로필 — 쌍ⓐ 구간이 «죽은 벽» 인지 물었다: 1600 은 행 std **0.0** ·
  고유색 **1.0** 으로 완전 평면, 2280 도 std 3.4 로 사실상 평면. 상인방 **위** 스트립(std 30.1)과 대조된다)
- 회귀 — `verify120` **157/157 무수정** · `verify700` **26/26** · `verify754` **60/60 무수정** · **SMOKE PASS**
- ⚠ `verify700` [C] 에 «등재된 이동만 이름과 값으로 통과» 하는 `ALLOW813` 표를 **비운 채로 심어 뒀다.**
  그 항의 기준이 700 커밋이 아니라 `merge-base origin/main HEAD` 라, **2회차가 E 재배분을 넣는 순간
  먼저 빨개진다.** 그때 «Δ ≤ n 허용» 으로 무르게 풀지 마라 — 700 이 지키려던 «바를 얹느라 다른 것을
  밀지 않았다» 가 통째로 사라진다(333 처방).

### §6 2회차가 할 일 — **E 재배분 한 벌** (우선순위)

세 결함(E 독식 · 쌍ⓐ · 안내문 역전)이 **하나의 예산**이라 따로 고치면 서로를 되돌린다.
두 비평가의 처방이 계수만 다르고 구조가 같다:

| | CF | CG |
|---|---|---|
| E 흡수율 | 66.9% → **40%** | 66.9% → **53%**(A 와 47:53) |
| C(바↔격자) | **48 고정**(행 간 26의 1.8배) | **38 고정**(1.5배) |
| 들보↔바 | 최소 **50** 바닥값 + 가변 | 최소 **124** 보장 + 가변 |
| G(수반↔안내문) | 43 → **60 하한** + 6% 지분 | 43 유지 |
| I(안내문 아래) | 8% 지분 | **24 고정**(ref 23) |

⚑ **먼저 확인할 것** — 이 재배분은 `--rw-gt` 의 «A:B = ref 1:3.797»(18회차 [L])을 바꾸는 일이다.
`verify120` 의 [L1]·⑥ 항이 그 비를 직접 단언하므로 **그 항을 먼저 읽고**, 새 비의 근거를
레퍼런스에서 다시 뽑아라(18회차는 «아치 위 133 : 아래 505» 로 잡았고, 두 비평가는 «패널상변→바상변»
대 «격자↔수반» 이라는 **다른 분해**로 잡았다 — 어느 분해가 옳은지가 2회차의 첫 판단이다).
⚠ 그리고 «남는 세로를 A 로 더 보내라» 는 처방은 **상인방 위 죽은 벽**을 키운다 — 14회차가
비평가 넷의 지적으로 상한 160(18회차 190)에 묶은 축이다. A 안에서 «스트립» 과 «들보↔바» 를
갈라 놓고, 늘어나는 쪽을 **들보↔바**로 못박아야 그 축을 안 건드린다.

⚠ **회차 예산** — 이 화면은 120 이 20회차, 754 가 9회차를 이미 썼다. 813 은 0/5 에서 시작했고
이번이 **1/5** 다. 남은 4회차 안에 ①~④ 8점을 받아야 한다.

---

## 2회차 (2026-09-03, sess-0445-14077 루틴 워커 C) — **제품 5자리 · 쌍ⓐ 12.1% → 28.0%**

### 결론 먼저

| | 1회차 판정 | 2회차 결과 |
|---|---|---|
| 쌍ⓐ `.rw-lintel ↓ #rwMulBar` | **실재** — «E 재배분과 한 벌로만» 열린다 | **닫혔다** — 13.8 → 23.7(1600) · 기준 대비 **12.1% → 28.0%** |
| 쌍ⓑ `.rw-cap ↓ .rw-fc.bl` | **유령**(가로 겹침 0px) | 유령 그대로. 상자 값은 16.7 → 4.9 로 **더 줄었지만**, 눈에 보이는 여백(잉크 기준)은 44.4% → **62.9%** 로 오히려 좋아졌다 |
| E 독식(2인 1순위) | 66.9% 흡수 | **안 건드렸다** — 스윕이 «캡하면 더 나쁘다» 를 보였다(§2) |
| 안내문 위:아래 역전 | 1.16 → 2.74(ref 0.58~0.62) | **0.60 고정**(1600 만 0.64) = ref 대역 안 |

### §1 1회차 §6 의 첫 판단 — «어느 분해가 옳은가»

1회차가 2회차에게 남긴 첫 질문은 **«18회차의 «아치 위:아래» 분해 vs 비평가의 «A vs E» 분해,
어느 쪽이 옳은가»** 였다. 답은 **«분해는 18회차가 옳고, 비평가가 본 결과도 옳다»** 이다 —
둘은 서로 안 부딪힌다.

- 18회차의 비 1:3.797 은 레퍼런스에서 잰 값이라 근거가 있다.
- 그런데 그 «아래 B» 안에는 **자라지 않는 내용**(수반 216 · 안내문 88 · 그 두 여백)이 들어 있다.
  B 가 커지라는 규칙을 주면 커질 수 있는 것은 **E 하나뿐**이다. 그래서 E 가 잉여를 독식한다.
- 즉 비평가가 본 «E 독식» 은 규칙의 오류가 아니라 **고정 아트 + 가변 프레임**의 구조적 결과다.

⇒ 그러면 «E 를 캡하고 잉여를 위로 보내라» 가 답인가? **아니다.** §2 가 그것을 실측으로 기각한다.

### §2 후보 스윕 — **E 를 캡하는 길은 전부 더 나쁘다** (`tools/probe813c.js` 신설)

1회차의 `probe813b` 는 «등재문의 지렛대 하나씩» 을 굴렸다. 2회차는 **예산 규칙 자체를 갈아 끼운
후보**를 굴렸다. E 의 tt 대비 지분을 상수로 묶는 안(D·E·F)을 넣어 보니 —

| 후보 | E 2600 | 상인방 위 스트립 2600 | 판정 |
|---|---|---|---|
| 현행 | 854 | 180 | — |
| E 지분 .50 고정 | **703** | **403** | ✗ 14회차가 «검은 띠» 로 반려한 288 의 1.4배 |
| E 지분 .55 고정 | 773 | 333 | ✗ 같은 이유 |
| E 지분 .60 고정 | 844 | 262 | ✗ 효과는 거의 없는데 스트립만 는다 |

잉여를 «벽(들보↔바)» 으로 보내는 길도 막혀 있다 — 그 칸이 긴 프레임에서 커지면
**쌍ⓐ 의 기준(2280)값이 같이 커져** 1600/기준 비가 **되레 나빠진다**(판정 규칙이 기준 프레임 대비다).

⇒ **E 는 손대지 않는 것이 이 회차의 답이다.** 게이트 [6] 이 그것을 래칫으로 못박았다
(긴 네 프레임의 E 가 한 픽셀도 안 움직였다는 것을 매 실행 확인한다).

### §3 실제로 한 것 — 제품 5자리

뿌리는 **«벽이 아치를 따라간다»** 였다. 격자 상변의 하한이 `av + 94` 뿐이라
벽 = gt − 20(금테) − 66(상인방) = **av + 8** 이 되고, 바 98 을 넣고 남는 여유를 위·아래로
반씩 나눠 13.8 / 13.8 이 된다. 벽을 아치에서 **떼어** 바가 요구하는 만큼을 직접 예약했다.

| 자리 | 값 | 근거 |
|---|---|---|
| **[E1]** `--rw-av` 셋째 인자 | `tt − 285` (= 232 + 53) | 하한이 이겼을 때 «받침 40 + 접합선 띠 13» 이 수반을 넘지 않게 하는 난간(13회차 사고 재발 방지). **지금은 안 이긴다** — 3회차가 232 를 올리면 자동 발동 |
| **[E2]** `--rw-gt` 벽 하한 | **232** | = 20(금테) + 66(상인방) + 24 + 98(바) + 24 |
| **[E3]** `--rw-lt` 벽 폭 | 300 → **294** | 쌍ⓐ 의 **기준**을 6px 내려 여유를 만든다 |
| **[E4]** 안내문 아래 블록 | 총량 불변 · ref 비 0.625:0.375 (`--rw-i` 신설) | 1회차 비평 2인이 독립으로 잰 «위 31~38 : 아래 18~23» |
| **[E5]** `#rwMulBar` 아래 목표 | 20 → **44** | CF·CG 대역 [38,48] · 격자 행 간 25.6 의 1.7배 |

⚑ **232 · 294 · 44 는 «고른» 값이 아니라 게이트 넷이 남긴 창의 좌표다.** 이 화면의 세로 예산은
회차마다 세운 자들이 서로 3~4px 을 놓고 다투는 상태다:

- GTF **240↑** ⇒ [E1] 이 발동해 1600 아치 748.5 → 710 = 1:1.205 ⇒ `verify120` ② «종횡비 ≥ 1:1.25»
  (16회차가 AH·AI·AJ·AK **네 명**의 지적으로 세운 자)가 빨개진다
- GTF **234↑** ⇒ 1600 «접합선 → 수반» 바닥이 18px 밑으로
- GTF **225↓** ⇒ 여유 41 ⇒ 쌍ⓐ 20.5 ⇒ `--rw-lt` 를 어디에 놔도 25% 를 못 넘는다
- `--rw-lt` **291↓** ⇒ 스트립 2600 이 193.8 ⇒ `verify120` ② «스트립 ≤ 190» 이 빨개진다
- `--rw-lt` **297↑** ⇒ 쌍ⓐ 기준이 커져 25% 밑으로

⇒ 창은 **GTF ∈ [226, 233] × lt ∈ [292, 296]** 이고 (232, 294) 가 그 한가운데다.
**이 좁음이 «1회차가 지렛대 셋으로 못 닫았다» 의 정체다.**

### §4 대가 — 딱 한 가지, 그리고 그 값을 어디서 뺐는지

**1600 의 «접합선 → 수반» 바닥이 27 → 20px.** 그 7px 이 배수 바의 위·아래 여백으로 갔다.
16회차의 27 은 «아래 최소 80 = 받침 40 + 띠 13 + 바닥 27» 의 **나머지로 나온 수**이지 잰 값이
아니고(같은 회차 주석이 스스로 «실제 안전 조건은 53» 이라고 적어 뒀다), 그래서 `verify120` ③ 의
문턱을 20 으로 내리되 **«받침 40 + 띠 13 = 53 이 들어가는가» 를 짝 항으로 새로 달았다**
(문턱만 내리면 «바닥이 사라져도 초록» 이 된다 — 328-330 교훈).

**아치는 안 먹었다**(1600 748.5 → 752.0 · 1:1.277). 첫 설계(GTF 240)가 아치를 눌러
`verify120` ② 를 깼고, 그것이 232 를 고른 이유다.

### §5 게이트 이관 — 3파일

| 자 | 전 | 후 |
|---|---|---|
| `verify120` ③ 바닥 | ≥ 27px | **≥ 20px + «받침+띠 53 이 들어간다» 짝 항 신설**(프레임 4종 → 4항 증가) |
| `verify120` ⑥ 배분 미러 | 식에 GT_FLOOR·안내문 비 없음 | `GT_FLOOR = 232` · `iGap = max(32, (38+g3)×.375)` 반영 |
| `verify700` [B4] | 아래 20px 고정(여유 ≥ 40) | 아래 **44px** 고정(여유 ≥ **88**) — 문턱도 같이 옮겼다(안 옮기면 1600 이 «넉넉» 으로 분류돼 영원히 빨갛다) |
| `verify700` [C] | `ALLOW813 = {}`(빈 표) | **이동 12건을 이름과 값으로 등재** — 1회차가 «Δ ≤ n 으로 무르게 풀지 마라» 고 남긴 자리 |
| `verify813` | 11/11(진단 · «아직 안 닫혔다») | **14/14**(방향 전환 + 되돌림 **3겹**) |

`verify700` 에 `P813_DUMP=1` 을 붙이면 등재 전 이동을 전부 찍는다 — 다음 회차는 ALLOW 표를
손으로 추측하지 말고 그 출력을 옮겨 적으면 된다.

**되돌림 3겹**(무르게 풀지 않았음의 증거) —
- **[R1]** [E4] 를 옛 «38 고정» 으로 되돌린 사본: 안내문 비가 0.60 → **2.74**(ref 대역 밖)
- **[R2]** [E2] 벽 하한을 뺀 사본: 쌍ⓐ 27.9% → **16.3%**(다시 판정선 아래)
- **[R3]** [E3] 을 300 으로 되돌린 사본: 쌍ⓐ 27.9% → **26.1%**
  ⚑ R3 은 굴려 보고 **묻는 것을 바꿨다** — [E3] 은 판정선을 넘기는 지렛대가 **아니었다**
  (E2+E5 만으로 26.1% 로 이미 넘는다). [E3] 이 하는 일은 **여유**(2.6px → 1.0px)다.
  1px 짜리 여유는 다음 회차의 어떤 손질에도 뒤집히므로 그 차이를 그대로 항으로 못박았다.

### §6 회귀

`verify120` **161/161**(157 → 161, 짝 항 4개 신설) · `verify700` **26/26** ·
`verify754` **60/60 무수정** · `verify813` **14/14** · **SMOKE PASS**

⚠ **도구 둘이 같은 자리를 다르게 읽는다** — `probe813` 은 2280 의 E 를 604.6 으로, `verify813`·
`verify120` 은 613.9 로 읽는다. 값이 틀린 것이 아니라 **표본**이 다르다(후자는 `S.relic = 1e9` 를
먼저 넣어 팝업을 열어서 패널이 ~17px 더 길다). 1회차부터 있던 어긋남이고, 한 자 안에서만
비교하면 무해하다. 3회차가 두 도구의 수를 **섞어 쓰지 마라.**

### §7 비평 — 2인 독립(CH · CI) · **①~④ 최저 2점** (1회차 3점에서 **내려갔다**)

브리핑 `docs/review/813-비평브리핑-r2.md`(1회차와 **같은 규칙·같은 질문**) ·
캡처 `docs/shots/754-813r2a-89-{1600,1841,1920,2280,2600}.png`(커밋 안 함 — .gitignore).

| | ① | ② | ③ | ④ | ⑤ | ⑥ | 최저 |
|---|---|---|---|---|---|---|---|
| CH | 5 | 6 | **3** | 6 | 6 | 4 | **3 (③)** |
| CI | 4 | 7 | **2** | 6 | 5 | 7 | **2 (③)** |

#### 2회차가 고친 것은 **둘 다 확인했다** (Q2 · Q3)

- **Q2 쌍ⓐ** — 두 사람 다 «1841~2600 네 장은 93 / 44 로 완전히 고정이고 그 배분이 **옳다**»
  고 적었다(CH «2.11:1 이 옳은 배분 — 바가 들보의 부속이 아니라 격자의 머리로 읽힌다» ·
  CI «B 는 전 프레임에서 행간의 1.75배 이상으로 고정해야 한다»). **[E5] 의 44 가 두 사람의
  독립 승인을 받았다.** 남은 것은 1600 한 장(24px)이다.
- **Q3 안내문** — **CI 는 «역전 없음 = 결함 아님»** 으로 판정했다(위:아래 1.59~1.71 vs ref 1.44,
  «허용 범위»). [E4] 가 목표한 자리다. ⚠ **CH 는 반대로 «역전 있음» 이라고 냈는데 근거가 갈렸다** —
  CH 의 ref 환산은 «아래 17.8 : 위 6.7 = 2.66»(위가 **6.7px**)이고 CI 는 «위 28.9 : 아래 20.0 = 1.44» 다.
  **1회차의 CF·CG 는 «위 31~38 : 아래 18~23»(= CI 쪽)이었다** — 3:1 로 CI·CF·CG 가 같은 값이고
  CH 의 6.7 만 혼자다(수반 «하변» 을 받침 밑판이 아니라 사발 하단으로 잰 것으로 보인다).
  ⇒ [E4] 는 유지한다. 다음 회차 브리핑에 **«수반 하변 = 받침 밑판 외곽선 최하단»** 을 명시할 것.

#### ⚑⚑ 2인 독립 일치 — ③ 은 **오직 하나**, 격자↔수반(ⓒ)이 잉여를 독식한다

두 사람이 **같은 숫자**를 냈다(1600→2600 에서 패널 +999~1000px):

| | ⓒ 격자↔수반 | 위(패널상변→격자상변) | 수반↔안내문 | 안내문 아래 |
|---|---|---|---|---|
| 증분 지분 | **+689 (68.9%)** | +251 (25.1%) | +39 (3.9%) | +21 (2.1%) |

- 2280→2600 구간만 보면 **+320 중 253(79.1%)** 이 ⓒ 로 간다(둘 다 같은 값).
- ⓒ / 격자 높이 = **0.39 → 1.75배**(ref 0.68) · ⓒ / 패널 = 15.0% → **38.5%**(ref 23.1%)
- 두 사람 다 «1841·1920 두 장은 ref 와 ±10% 안» 이라고 짚었다 — **규칙이 없는 게 아니라
  양 끝 세 장이 규칙 밖**이라는 진단이다.

**그리고 처방까지 같다** — «남는 세로를 **위 : 가운데 = 47 : 53** 으로 나눠라»
(CH 는 ref 308.9 : 346.7 · CI 는 ref 312 : 349 에서 각자 뽑았다. 두 값의 차 1%).

#### ⚑⚑⚑ 그 처방이 **게이트 셋과 삼각으로 부딪힌다** — 3회차의 본체다

2회차가 이미 굴려 본 자리라 값을 적어 둔다. 47:53 을 그대로 넣으면 2600 에서 위가
474 → 638(+164)인데, 그 164 가 갈 곳이 **둘뿐이고 둘 다 막혀 있다**:

1. **상인방 위 스트립으로** ⇒ 2600 스트립 188 → **342**. `verify120` ② «스트립 ≤ 190»
   (14회차가 AB·AD·AE·AG **네 명**의 «검은 띠» 지적으로 세운 자)가 빨개진다.
2. **들보↔바(Lb)로** ⇒ 2280 의 Lb 가 86 → 240 이 되는데, 1600 은 예산이 없어 24 그대로다
   ⇒ 쌍ⓐ 비가 28.0% → **10%** 로 **754 의 [❌] 가 되살아난다**(판정이 기준 프레임 대비다).
3. 게다가 **CH 의 표는 1600 에서 자기 Q2 와 모순된다** — 47:53 은 1600 의 위를 223 → 195 로
   **줄이라**고 하는데, 같은 사람의 Q2 는 그 구간의 Lb·C 를 24 → 44 로 **늘리라**고 한다.
   1600 에서 두 처방을 동시에 만족시킬 수 없다(13·16·18회차의 «1600 은 예산이 없다» 그대로).

⇒ **세 조건 중 둘만 만족시킬 수 있다.** 3회차의 첫 판단은 «어느 하나를 놓을 것인가» 이고,
   2회차의 권고는 **1번(스트립 상한)** 이다. 근거 셋:
   - 14회차의 반려 근거(«평균 휘도 29.6 · 66.6%가 32 미만 = 디테일 0»)는 **18회차가 이미
     기각했다**(벽 결이 들어온 뒤 행별 고유색 330~378 · std 15.9~16.3 · 휘도 32.7~36.6).
   - **이번 2인은 다섯 프레임을 다 보고도 스트립을 한 줄도 지적하지 않았다** — CH ⑤ 는
     오히려 «패널 평균 휘도가 ref 보다 +35~46% **밝다**» 였다.
   - 나머지 둘은 각각 754 의 자와 «1600 의 예산 0» 이라 **논증이 아니라 산수**다.
   ⚠ 그래도 «상한을 지운다» 가 아니라 **18회차의 방법(그 띠를 다시 재서 결이 있는지 보고,
     캡처를 비평가에게 직접 물어 A/B 판정을 받는다)** 으로 올려야 한다.

#### 2인 일치이나 이번 작업 단위 밖 — 등재만

- **격자 열 정렬 vs 배수 바 폭**(CH [10] · CI [5] 독립 일치): 바 x178..901(724)이 3슬롯 행
  216..862(646)보다 좌우 각 **38px 넓고**, 4슬롯 행 90..987(897)보다 좌우 각 **87px 좁다**.
  ⚠ 폭 724 는 713 이 쓴 «칸 181 × 4» 라 **668/700/701 공용 부품**이다 — 여기만 바꾸면 세 화면이 갈린다.
- **«Lv.n» 라벨**: CI [4] «2280 3행 1열 잉크가 슬롯 하변 밖으로 10px» · CH [6] «잉크 높이 27 vs
  ref 환산 57.8 = 2.14배 작다». **두 사람의 방향이 반대**라 2회차는 판정하지 않는다(재현이 먼저다).
- 수반 위젯 bbox(CH [8] «−12.3% 납작 · 수면 폭 +10.7%») — ② 축, 89 규격 소관.

#### 2회차 자체 평가

**③ 은 2~3점으로 1회차와 같거나 내려갔다.** 2회차가 닫은 것(쌍ⓐ · 안내문 비)은 두 사람이
«옳다» 고 확인했지만 **둘 다 ③ 의 주 감점원이 아니었다** — 주 감점원은 처음부터 ⓒ 하나였고,
2회차는 §2 의 스윕 근거로 그 자리를 **의도적으로 안 건드렸다.** 그 판단이 이번 채점에서
틀렸음이 드러났다: 스윕은 «E 를 캡하면 스트립이 커진다» 까지만 보고 **«스트립이 커지는 것이
정말 더 나쁜가» 를 눈에게 안 물었다.** 3회차가 물어야 할 질문이 그것이다.
