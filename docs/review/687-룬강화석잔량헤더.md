# 687 — 룬 강화 화면 룬강화석 잔량 헤더 (주인 지시 2026-09-02 01:00 · 보강 01:25)

담당: sess-1816-20787 (루틴 워커 A) · 2026-09-01

주인 원문: «룬강화 부분에 현재 갖고있는 룬강화석 개수 몇개인지 표시되게 해줘야함.
일반룬 고급룬 천상룬 탭 위에 표시 되면 좋겟음»
보강(01:25): 표기는 «(아이콘) 수량» 만 — 한글 «룬강화석» 라벨 금지 · **중앙 정렬**.

## §1 실측 순서 ① — 룬강화석은 등급별 재화인가

**아니다 — 공용 하나다.** `S.rstone` 단일 수치(20731 `rstone:0`)이고 `runeCost()`/`runeTry()`
가 등급 무관 같은 재화를 쓴다(490 «룬 강화는 룬강화석으로만» 이 이미 그 축을 하나로 접었다).
⇒ **위임 규약 채택**: 헤더는 하나 · 탭 전환과 무관하게 같은 값(등재문의 «등급별이면 활성 탭 것»
분기는 대상이 없다). 되돌림: 등급별 재화가 생기면 `runeHeadTxt()` 한 함수만 갈면 된다.

## §2 자리 실측과 세로 예산 (측정표 23 §271 정오표 동반)

«탭 위» = 본문 머리(박스 local 34)인데 그 자리는 하위 바(.rn-subs 34~133)가 쓰고 있었고,
룬 열의 세로 예산 920(local 34~954)은 여유가 19px 뿐이었다 — 헤더 한 줄(56)이 설 자리가 없다.
⇒ **카드 −44** 로 자리를 만들었다. 걷은 것은 전부 **잉크 없는 세로 여백**이다(아이콘 200 ·
글줄·바·버튼 높이 전부 불변):

| 요소 | 전 | 후 | 뺀 여백 |
|---|---|---|---|
| `.rn-hd` 헤더 | (없음) | **34~90 (h56)** | — |
| `.rn-subs` 하위 바 | 34~133 | **104~202** | 헤더 아래로 |
| `.tr-runes` 본문 | top 157 · h778 | **top 221 · h722** | — |
| `.tr-rn` 카드 | h700 | **h656** | −44 |
| `.rd` 효과 3줄 | top 280 | **264** | 아이콘 밑 40→24 |
| `.rst` 계단 줄 | top 452 | **436** | rd 밑 10 유지 |
| `.rbt`/`.rmax` | top 512 | **486** | rst 밑 10 (구 20) |
| `.rhint` | top 632 | **606** | 버튼 밑 8 유지 |
| `.rsum` 총효과 | top 732(local) | **676** | 카드 밑 20 (구 32) |
| 바닥 여유(954까지) | 19 | **10** | — |

헤더 껍데기는 단련 `.tp-hd` 와 같은 카드 베벨 체계(688 «재화 잔량 헤더 규약» 이 단련 쪽을
끌어올 때 그대로 한 부품이 된다 — 높이만 88→56, 예산 뺄셈의 결과). 내용은
`runeHeadTxt()` = `curIc('rstone', 36) + <b>fmt(floor(S.rstone))</b>`, 중앙 정렬(text-align).

## §3 갱신 경로

- 마크업은 하위 바처럼 **정적**(#rnHd — renderRunes 0.35초 재렌더에 안 갈린다, LESSONS 50-①).
- `runeHeadPut()` 하나를 `renderRunes`(통짜 경로 — sig 에 S.rstone 이 원래 들어 있다)와
  `liveRunes`(297 홀드 «숫자만» 경로) 둘 다 부른다 ⇒ **꾹 누르는 중에도 잔량이 실시간으로 준다**.

## §4 게이트 (1회차)

- `tools/verify687.js` **27/27 신설** — [2] 표시값=실보유(강화 1회 뒤 즉시 감소) · [3] 아이콘+수뿐
  (한글 0자 · 중앙 Δ0.01px) · [4] 자리·순서·탭별 가시성·등급 공용 · [5] liveRunes 실시간 ·
  [6] 두 프레임(2280/1600) · §R 되돌림 3종(라벨 부활/아이콘 삭제/헤더 소멸이 **각각 다른 항**을 붉힌다).
- `tools/verify271.js` **65/65 이관** — «하위 바 top 34» 를 지우지 않고 **방향을 뒤집어** 갈아 끼웠다
  (333 처방): 머리 자리의 주인이 바 → 헤더로 바뀐 것까지 단언(헤더 34/56 + 바 104 + 겹침·순서에 헤더 편입).
- 회귀 무수정: `verify337` 95/95 · `verify203` 91/91 · `verify210` 96/96 · `verify490` 33/33 ·
  `verify489` 58/58 · `verify584` 54/54 · `verify619` 53/53 · `verify621` 42/42 · `verify660` 55/55 ·
  `verify349` 29/29 · **SMOKE PASS** · 콘솔 에러 0.

## §5 비평 (2인 · 캡처)

(2회차 이후 기록)
