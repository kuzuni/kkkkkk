# 687 — 룬 강화 화면 룬강화석 잔량 헤더 (주인 지시 2026-09-02 01:00 · 보강 01:25)

담당: sess-1816-20787 (루틴 워커 A) · 2026-09-01

주인 원문: «룬강화 부분에 현재 갖고있는 룬강화석 개수 몇개인지 표시되게 해줘야함.
일반룬 고급룬 천상룬 탭 위에 표시 되면 좋겟음»
보강(01:25): 표기는 «(아이콘) 수량» 만 — 한글 «룬강화석» 라벨 금지 · **중앙 정렬**.

## §1 실측 순서 ① — 룬강화석은 등급별 재화인가

**아니다 — 공용 하나다.** `S.rstone` 단일 수치(20731 `rstone:0`)이고 `runeCost()`/`runeTry()`
가 등급 무관 같은 재화를 쓴다(490 «룬 강화는 룬강화석으로만» 이 이미 그 축을 하나로 접었다).
⇒ **위임 규약 채택**: 헤더는 하나 · 탭 전환과 무관하게 같은 값(등재문의 «등급별이면 활성 탭 것»
분기는 대상이 없다). 되돌림: 등급별 재화가 생기면 `runeHeadTxt()` 한 함수만 갈면 된다.

## §2 자리 실측과 세로 예산 (측정표 23 §271 정오표 동반)

«탭 위» = 본문 머리(박스 local 34)인데 그 자리는 하위 바(.rn-subs 34~133)가 쓰고 있었고,
룬 열의 세로 예산 920(local 34~954)은 여유가 19px 뿐이었다 — 헤더 한 줄(56)이 설 자리가 없다.
⇒ **카드 −44** 로 자리를 만들었다. 걷은 것은 전부 **잉크 없는 세로 여백**이다(아이콘 200 ·
글줄·바·버튼 높이 전부 불변):

| 요소 | 전 | 후(3회차 확정) | 뺀 여백 |
|---|---|---|---|
| `.rn-hd` 헤더 | (없음) | **24~112 (h88 · fs52 · 아이콘 TR_CUR_PX 53)** | — |
| `.rn-subs` 하위 바 | 34~133 | **132~230** | 헤더 아래로 (사이 20) |
| `.tr-runes` 본문 | top 157 · h778 | **top 246 · h702** | — |
| `.tr-rn` 카드 | h700 | **h648** | −52 |
| `.rd` 효과 3줄 | top 280 | **256** | 아이콘 밑 40→16 |
| `.rst` 계단 줄 | top 452 | **428** | rd 밑 10 유지 |
| `.rbt`/`.rmax` | top 512 | **478** | rst 밑 10 (구 20) |
| `.rhint` | top 632 | **598** | 버튼 밑 8 유지 |
| `.rsum` 총효과 | top 732(local) · h46 | **664 · h38** | 카드 밑 16 (구 32) |
| 바닥 여유(954까지) | 19 | **6** | — |

헤더 껍데기는 단련 `.tp-hd` 와 **같은 부품 급**(카드 베벨 + 높이 88 동일 — 688 이 단련 쪽을
끌어올 때 그대로 한 부품이 된다). 내용은
`runeHeadTxt()` = `curIc('rstone', TR_CUR_PX) + <b>fmt(floor(S.rstone))</b>`, 중앙 정렬(text-align).

1회차는 h56 · fs36 · 아이콘 36 · 사이 14 였다 — §5 비평 2인이 일치로 짚은 것(«헤더 숫자가
팝업 최소 텍스트 티어(27px) — 서브탭 라벨 39·카드 제목 48 아래» · «띠 63 이 바 97 대비 유일하게
얇다» · «헤더↔바 9px 만 간격 리듬(16~26) 밖» · «아이콘 44 가 안높이의 92% — 테에 닿기 직전»)을
2회차에서 fs46(잉크 ≈34 = 서브탭 티어) · h64 · 사이 22 · 아이콘 40(안높이 48 의 83%)으로 반영했다.

## §3 갱신 경로

- 마크업은 하위 바처럼 **정적**(#rnHd — renderRunes 0.35초 재렌더에 안 갈린다, LESSONS 50-①).
- `runeHeadPut()` 하나를 `renderRunes`(통짜 경로 — sig 에 S.rstone 이 원래 들어 있다)와
  `liveRunes`(297 홀드 «숫자만» 경로) 둘 다 부른다 ⇒ **꾹 누르는 중에도 잔량이 실시간으로 준다**.

## §4 게이트 (1회차)

- `tools/verify687.js` **27/27 신설** — [2] 표시값=실보유(강화 1회 뒤 즉시 감소) · [3] 아이콘+수뿐
  (한글 0자 · 중앙 Δ0.01px) · [4] 자리·순서·탭별 가시성·등급 공용 · [5] liveRunes 실시간 ·
  [6] 두 프레임(2280/1600) · §R 되돌림 3종(라벨 부활/아이콘 삭제/헤더 소멸이 **각각 다른 항**을 붉힌다).
- `tools/verify271.js` **65/65 이관** — «하위 바 top 34» 를 지우지 않고 **방향을 뒤집어** 갈아 끼웠다
  (333 처방): 머리 자리의 주인이 바 → 헤더로 바뀐 것까지 단언(헤더 34/56 + 바 104 + 겹침·순서에 헤더 편입).
- 회귀 무수정: `verify337` 95/95 · `verify203` 91/91 · `verify210` 96/96 · `verify490` 33/33 ·
  `verify489` 58/58 · `verify584` 54/54 · `verify619` 53/53 · `verify621` 42/42 · `verify660` 55/55 ·
  `verify349` 29/29 · **SMOKE PASS** · 콘솔 에러 0.

## §5 비평 (2인 · 캡처)

**1회차 (h56/fs36)** — A: ①6 ②5 ③5 ④9 · B: ①6 ②5 ③5 ④7 ⇒ **둘 다 최저 5**.
일치 지적: ② 헤더 숫자 27px = 팝업 최소 텍스트 티어(서브탭 39 · 카드 제목 48 아래) ·
띠 63 vs 바 97 로 유일하게 얇다 · ③ 헤더↔바 9~10px 만 간격 수열(16~26) 밖 ·
① 알맹이(155px)가 띠 폭의 16% · ④ 는 A9/B7(카드와 0px 정렬 · 중앙 Δ2px — 정상).
⇒ 2회차 반영: fs36→46 · h56→64 · 사이 14→22 · 아이콘 40 · 아이콘↔숫자 사이 10(공백 제거).

⚠ **두 비평가가 같이 짚은 «9:13.3 에서 팝업 타이틀이 ⚔️ 전투력 토스트에 가린다» 는 687 밖이다**
— 687 은 박스 안 룬 열만 옮겼고 시트·타이틀·토스트 좌표는 0줄(1600 평행이동 자체가 기존 동작).
748(12 팝업 · 같은 «짧은 프레임 보호항 ↔ 토스트» 족보)의 자매 자리로 **761 등재만** 했다(752~760 은 등재 경쟁으로 선점됨 · 시트 bbox 687 전후 대조 Δ0 확인).

**2회차 (h64/fs46/아이콘40)** — C: ①8 ②7 ③7 ④9 (최저 **7**) · D: ①8 ②7 ③6 ④9 (최저 **6**).
남은 일치 지적: ② 같은 재화 아이콘이 버튼(TR_CUR_PX 53)과 다른 스케일(26×40 vs 33×53) ·
띠 64 가 여전히 바 98 대비 유일하게 얇다 · 숫자(34)가 탭 라벨(39) 아래 ③ 안여백 4~5px ·
위 26 vs 아래 17 불균형. ④ 는 9/9(그리드 도킹 완벽 · 잉크 중앙 Δ2~2.5px).
⇒ 3회차: 띠 = **.tp-hd 와 같은 88** · 아이콘 = **TR_CUR_PX 53** · fs**52**(잉크 ≈40 > 라벨 39) ·
top 34→**24**(위 여백 16 — 리듬 안). 예산은 rsum 40→38 · 바닥 여유 16→6 으로 치렀다.

**3회차 (h88/fs52/아이콘 53)** — E: ①8 ②8 ③8 ④9 (최저 **8** ✓ · 21회 실측 — «아이콘 33×53 =
버튼과 동일 에셋·크기, 완벽 일치» · «숫자 38 = 라벨 티어 정합» · «띠·카드 좌우 편차 0px» ·
«두 프레임 픽셀 diff 0»). F: ①7 ②7 ③6 ④7 (최저 6) — ⚠ **F 의 감점 근거가 실측과 어긋난다**
(335 «유령» 꼴): «두 프레임에서 띠 높이·패딩이 다르다»(→ E 의 픽셀 diff 0 · DOM h88 동일) ·
«띠 좌변이 카드와 3px 어긋남»(→ E·D 실측 0px) · «숫자 50px ≈ 카드 제목»(→ DOM 잉크 38).
⇒ 335 처방대로 **앵커 표(프레임 픽셀 동일 · 띠 24/24/998/88 = 카드 모서리 0px · 아이콘 53 =
버튼과 동일 · 숫자 잉크 38)를 프롬프트로 준 재채점(비평 G)** 으로 갈음한다.
