# 120 — 89 유물 페이지 «검은 여백 없이 꽉 채우기»

- 담당: sess-2306-19305 (병렬 워커 A) · 2026-08-26
- 지시(저장소 주인): *«유물 팝업 레이아웃이 어중간하다 — 검은 영역이 없게, 하단 네비와 상단 화폐 바를 뺀 나머지를 꽉 채우는 느낌으로.»*
- 검증 구분: ROUTINE [3]-**(나)+(가)** — 기계적 게이트(verify120/100/smoke) + 비평 루프
- 하네스: `node tools/cap120.js <회차>` (1600·1920·2280·2600 **전체 프레임**) · `node tools/probe120.js` (구획 실측)
- 게이트: `node tools/verify120.js` · `node tools/verify100.js` · `node tools/smoke.js`

## 1. 문제 — 100 이 남겨 둔 «검은 띠»

100 은 «패널이 탭바를 덮어 탭이 안 눌리는» 버그를 **균일 축소**(`scale(--rws)`)로 막았다. 그건 맞는 처방이었지만
패널 자체는 여전히 **레퍼런스 크롭 그대로의 절대 1080×1527** 이었고, `#relw` 영역은 `frameH − 284` 로 가변이다.
기준 해상도 2280 에서 실측:

| 구간 | 프레임 y | 높이 | 내용 |
|---|---|---|---|
| 재화 바 | 0 ~ 108 | 108 | 41 `.pcb` (정상) |
| **검정** | **108 ~ 370** | **262** | `#relw{background:#0A0F14}` 노출 |
| 패널 | 370 ~ 1897 | 1527 | `.rw-panel` |
| **검정** | **1897 ~ 2100** | **203** | `#relw` 노출 |
| 탭바 | 2100 ~ 2280 | 180 | (정상) |

→ 영역 1992px 중 **465px(23.3%)이 검정**. 짧은 프레임에서는 축소까지 겹쳐 좌우에도 여백이 생겼다.

## 2. 수정 — 패널을 «영역» 에 앵커한다

### 2-1. 껍데기

```
.rw-panel{position:absolute;left:0;right:0;top:4px;bottom:0;overflow:hidden}
```

`#relw` 는 `top:104/bottom:180` 이고 41 재화 바 `.pcb` 가 프레임 y0~108 을 덮으므로 **`top:4` = 프레임 y108** 이다
(바 바로 아래). `bottom:0` = 탭바 상변. 9:19 에서 **1080×1992**, 다른 화면비에서는 가용 높이 전부.
`.rw-bg`(inset 0) · `.rw-frame`(inset 2) · `.rw-fc`(bottom 3) 는 손댈 필요가 없었다 — 패널이 커지면 따라 커진다.

`#relw` 자체 배경도 `#0A0F14` → `#0D100D`(석벽 톤)로 바꿨다. 패널이 100% 덮으므로 원래 안 보이지만,
전환 프레임에서도 «검정» 이 안 보이게 한다.

### 2-2. 배경 — px 정지점을 % 로 환산

패널이 1527 → 1992 로 늘어나는데 바닥 그라디언트가 px 로 박혀 있으면 **바닥선이 수반보다 훨씬 위에 남는다**.
1527 대비 %로 환산했다:

| 레이어 | 구 | 신 |
|---|---|---|
| 바닥 그라디언트 정지점 | 980 / 1120 / 1400 / 1491 px | 64.178% / 73.347% / 91.683% / 97.642% |
| 석벽 질감 `.rw-bg::before` | `height:1050px` | `height:68.762%` |
| 중앙 아치 `.rw-bg::after` | `top:133px · height:889px` | `top:8.709% · height:58.219%` (가로 589 는 1:1 고정) |

**수반 난색 글로우**(구 `.rw-bg` 1번 레이어 `radial 40%×18% at 50% 88%`)만은 %로 두면 수반과 어긋난다 —
수반의 세로 위치가 «% 고정» 이 아니라 «여백 배분» 으로 정해지기 때문이다. 그래서 **`.rw-mid::before` 로 이관**했다
(864×550 = 구 rx432·ry275, 수반 기준 중심 200,171 = 구 540,1344 − 수반 340,1173). 이제 글로우가 수반을 따라간다.

### 2-3. 내용 — 세로 3구획 + 여백 비율 배분

| 구획 | 요소 | 고정 높이 |
|---|---|---|
| 상 `.rw-grid` | 슬롯 10칸 (3·4·3) | **516** = 3행(피치 176/175, 슬롯 151 → 502) + Lv 라벨 하단 돌출 14 |
| 중 `.rw-mid` | 돌 수반 + 코스트 알약 | **216** (수반 캔버스. 알약 하변 202 < 216) |
| 하 `.rw-cap` | 안내문 2줄 | **88** (line-height 44 × 2) |
| | | **합 820** |

남는 높이 `--rw-sp = 100% − 820px × --rwc` 를 **레퍼런스의 여백 비율 그대로** 네 곳에 나눈다
(ref 1527 기준: 상 320 : 격자↔수반 337 : 수반↔안내문 23 : 하 27 = **707**):

```
.rw-grid{top:calc(var(--rw-sp) * .452617)}
.rw-mid {top:calc(var(--rw-sp) * .929279 + 516px * var(--rwc,1))}
.rw-cap {top:calc(var(--rw-sp) * .961811 + 732px * var(--rwc,1))}
```

실측 여백(상 / 격자↔수반 / 수반↔안내문 / 하):

| frameH | 패널 | 여백 | `--rwc` |
|---|---|---|---|
| 1600 | 1080×1312 | 223 / 234 / 16 / 19 | 1 |
| 1920 | 1080×1632 | 368 / 387 / 26 / 31 | 1 |
| **2280** | **1080×1992** | **530 / 559 / 38 / 45** | 1 |
| 2600 | 1080×2312 | 675 / 711 / 49 / 57 | 1 |

→ **가용이 짧아지면 여백부터, 그것도 네 곳이 같은 비율로 줄어든다.** 내용 픽셀은 안 줄어든다.
내용 총합 820 보다도 가용이 짧아지는 극단에서만 `rwFit()` 이 `--rwc` < 1 로 내용을 균일 축소한다.
지원 범위(frameH 1600~2600 → 가용 1312~2312)에서는 **항상 정확히 1** 이라 ROUTINE [2] 의 «scale 래퍼 금지» 에
걸리지 않는다 — 100 이 남긴 것과 같은 성격의 최후 안전망이다.

### 2-4. 좌표 재기준 (89 규격은 불변)

| 값 | 구 (패널 기준) | 신 |
|---|---|---|
| `RW_POS` 행 y | 320 / 496 / 671 | **0 / 176 / 351** (`.rw-grid` 기준. 피치 176/175 그대로) |
| `.rw-basin` | `340,1173` | `.rw-mid` 의 `0,0` |
| `.rw-cost` | `401,1322` | `.rw-mid` 기준 `61,149` (상대 위치 불변) |

슬롯 151×151 · 수반 400×216 · 코스트 278×53 · 가로 중심 540 은 **전 프레임 높이에서 Δ0** (게이트 ⑦).

### 2-5. 폐기

`RW_PANEL_TOP`·`RW_PANEL_H`·`--rws` 는 사라졌다. `rwFit()` 은 `--rwc`(내용 축소) 하나만 갱신한다.
`#relw{overflow:visible}` 은 **그대로 둔다** — 41 재화 바 `.pcb` 가 `top:-104px` 로 `#relw` 밖에 얹혀 있어
`hidden` 이면 통째로 잘린다(100 의 교훈 3).

## 3. 게이트

### `tools/verify120.js` — 신설 (68 항목, 4개 프레임 높이)

① 패널 = 영역 전체 · ② 배경/프레임/코너가 가장자리 앵커 · **③ «패널이 못 덮은 픽셀» 0** ·
④ 3구획 상호 겹침 0(코스트는 수반 구획 안) · ⑤ 잘림 0 · ⑥ 여백 4곳 양수 + 레퍼런스 비율 ·
⑦ 89 규격 불변 · ⑧ 탭바 25점 히트테스트.

> **③ 을 «`#0A0F14` ±4 픽셀 세기» 로 하면 안 된다.** 지시서 검증절이 그렇게 적혀 있지만, 석벽 최상단이
> `#090C10` 이라 (1,3,4) 차이로 **±4 안에 들어온다** — 멀쩡한 배경이 «검은 띠» 로 잡힌다.
> 대신 `#relw` **배경만** 센티넬 `#FF00FF` 로 바꾸고 «영역 안에 그 색이 한 픽셀이라도 보이면 FAIL» 로 했다.
> «패널이 못 덮은 자리» 를 색으로 직접 잡는 것이라 오탐·미탐이 둘 다 없다.

**음성 대조**: `.rw-panel` 을 `top:266px;bottom:200px` 로 되돌려 주입 → ①③ 이 4개 높이 전부 FAIL
(`60/68`, 노출 픽셀 486,000~498,960). 원복 확인.

### `tools/verify100.js` — ③ 교체

«스케일 = min(1, avail/1527)» · «패널 1080×1527 @y370» 회귀 검사는 120 이후 참이 아니라 폐기하고,
**«패널 = 영역 전체» + «--rwc = 1» + «수반 400×216 · 슬롯 151²·피치 176/175 불변»** 으로 갈아끼웠다.
탭바 비가림(①②④⑤⑥)은 그대로다. 높이 목록에 **2600**(clamp 상한)을 추가했다.

> 이때 «행 피치 176» 으로 게이트를 걸었더니 FAIL 이 났다 — 원본 `RW_POS` 가 320/496/671 이라
> 실제 피치는 **176/175** 다(측정표의 «176» 은 79×2.2222=175.55 의 반올림). 코드가 아니라
> 게이트 쪽 가정이 틀린 것이라 176/175 로 고쳤다. 이 1px 비대칭은 89 원본 그대로 유지된다.

### 결과

| 게이트 | 결과 |
|---|---|
| `verify120` | **68/68 PASS** (음성 대조 60/68 FAIL 확인) |
| `verify100` | **44/44 PASS** |
| `verify119` | **35/35 PASS** (119 와 같은 `.rw-cost` 를 만지므로 회귀 확인) |
| `smoke` | **SMOKE PASS** |
| 콘솔 에러 | 0건 (4개 프레임 높이 전부) |

## 4. 비평 회차

채점 축은 ①요소 배치 ②상대 크기 비율 ③여백·간격 ④정렬·그리드에 **⑤ «꽉 참(영역 채움)»** 을 더한 5축이다
(⑤ 는 이 작업의 지시가 «꽉 채우는 느낌» 이므로 통과 판정에 포함한다). ⑤⑥(색·서체)은 채점하지 않는다.

### 1회차 — 캡처 `120-r1-{1600,1920,2280,2600}.png`

| | ① | ② | ③ | ④ | ⑤ | 최저 |
|---|---|---|---|---|---|---|
| G | 8 | 7 | 7 | 8 | **4** | **4** |
| H | 7 | 6 | 8 | 8 | **6** | **6** |

**두 비평가가 독립적으로 같은 1순위를 짚었다 — «패널 면적의 32~34%가 디테일 0 의 단색 평면»** 이다.
가장자리 검은 띠는 둘 다 0 으로 확인했다(게이트 ③ 과 일치). 즉 **«덮었다» 와 «채웠다» 는 다른 문제**였다.

지적을 스캐너(`tools/scan120.js`)로 재현한 1회차 실측:

| 지표 | 1회차 |
|---|---|
| 아치 내부(x250~826) 행별 고유색 | **최소 1** (y546·596·1194·1243·1293·1343 이 전 폭 단일 RGB) |
| 아치 내부 행평균 휘도 | 15.4 로 평탄 (변화 0) |
| 바닥(x20~1060) 행별 고유색 | **최소 1** (y1579·1616 이 전 폭 단일값) |
| 아치 하변 1차 차분 | 4.2 @y1478 (폭 589 수평 절단선) |

그 밖에 H 가 «가로 1:1 규약 위반 1건» 으로 짚은 것 — **아치 종횡비 붕괴**:
폭은 589 로 고정인데 높이만 `%` 로 늘어나 ref 1:1.51 → **2280 1:1.97 · 2600 1:2.28**.
«같은 아치가 긴 화면에서 16% 더 홀쭉해진다.» 이건 1회차 % 환산의 진짜 설계 결함이었다.

### 2회차 — 캡처 `120-r2-{1600,1920,2280,2600}.png`

**고친 것 4건**

1. **아치를 «패널의 %» 에서 «슬롯 격자 ± 186» 으로 재정의.**
   레퍼런스가 아치 133~1022 · 격자 320~836 이라 **아치 = 격자 상하 186** 이다. 이 관계로 바꾸면
   아치는 어느 프레임에서도 **589×888(ref 종횡비 1:1.51 그대로)** 이고 격자를 따라 움직인다.
   ```
   top:calc(var(--rw-sp) * .452617 - 186px * var(--rwc,1));  height:calc(888px * var(--rwc,1))
   ```
   → 아치 세로는 프레임 높이와 **무관**해졌다(2280·2600·1600 전부 888).
2. **아치 내부 단색 → 세로 명암 5단 + 얼룩 3개 + 하단 8% 알파 소멸.**
3. **아치 하변 수평 절단선 제거** — 아래 §5 참조(box-shadow → drop-shadow).
4. **바닥 68~99% 대역에 결 9겹.** 석벽 얼룩(`.rw-bg::before`)이 상단 68.762% 만 덮어 바닥에는
   결이 하나도 없었다. 최하단 청흑 띠까지 포함해 깔았다.

| 지표 | 1회차 | **3회차(정지 캡처)** |
|---|---|---|
| 아치 행별 고유색 (최소) | 1 | **23~25** |
| 아치 행평균 휘도 진폭 | 0 | **75.4** (14.3 → 89.7) |
| 바닥 행별 고유색 (최소) | 1 | **30~53** |
| 아치 하변 1차 차분 | 4.2 | **1.7~2.8** |
| 아치 종횡비 | 2280 1:1.97 / 2600 1:2.28 | **전 높이 1:1.51** |

> 2회차 캡처의 스캐너 수치(아치 최소 3 · 바닥 최소 34 · 하변 0.3)는 **믿을 수 없다** — 아래 §4-3 의
> 애니메이션 중간 프레임에서 잰 값이다. 위 표는 3회차의 «정지 확인된» 캡처로 다시 잰 것이다.

**반증해 미채택한 지적**

- **G ③ «하단 블록 내부 간격만 배율이 튄다»(수반↔안내문 47px, 안내문↔하단 50px)** — 잉크 기준으로 잰 것이다.
  안내문은 `line-height:44` 라 잉크(≈30)와 라인박스 사이에 상하 7px 씩 half-leading 이 있다.
  **라인박스 기준 여백은 네 곳이 정확히 같은 배수**다(게이트 ⑥ 실측 Δ0.01: 530.5/558.7/38.1/44.8 vs 기대 530.5/558.6/38.1/44.8).
  ref 잉크 기준으로 환산해도 «안내문 잉크 하변 → 패널 하변» 은 ref 34 ×1.658 = 56 인데 우리는 50 으로
  오히려 **더 좁다.** G 가 쓴 «환산 24px» 은 배율을 안 곱한 값이다.
- **H ① «아치 폭이 ref 487 인데 우리는 589 (+21%)»** — 측정표 §측정 표가 ref 265 ×2.2222 = **589** 로 확정한 값이고,
  G 는 아치 폭을 감점 사유로 들지 않았다. 저해상(486×687) 레퍼런스의 아치 에지는 비평가 실측으로 수렴하지 않는다
  (LESSONS 89-①). **3회차 비평 프롬프트에 «선확정 — 재측정 금지» 로 박았다.**
- **G ② / H ②-3 «코스트 알약이 8~11% 납작하다»** — 두 비평가의 ref 실측이 269×49 / 269×52 로 서로 다르고,
  측정표 확정치는 **262×51** 이다. 우리는 278×53 으로 종횡비 차 **2%** 다(89 가 이 값으로 ①~④ 8 을 통과했다).
  같은 이유로 선확정에 박았다.
- **H ③·④ «슬롯 간극 95/98 비대칭 → 97/97 로»** — `RW_POS` x 는 ref 97·208·320 ×2.2222 를 반올림한 값이라
  **간극 95.5/97.8 은 레퍼런스 자체의 성질**이다. 97/97 로 만들면 ref 에서 멀어진다. 선확정에 박았다.
- **H ⑤-4 «금테 안쪽 2px 검은 틈»** — 89 설계의 «검정 + 갈색 2겹 프레임»(`.rw-frame` 의 `inset 0 0 0 2px`)이다.
  120 구간이 아니고 의도된 림이다. 선확정에 박았다.

### 3회차 — ★ 2회차 비평(I 2 / J 2)은 **캡처 결함**이었다

I·J 는 독립적으로 **같은 것을 같은 방향으로** 보고했다. 보통은 «둘이 몰리면 움직인다»(LESSONS 52-④)가
맞지만, 이번 보고는 **DOM 게이트와 정면으로 충돌**했다:

| 비평가 보고 (I·J 공통) | verify120 (DOM 실측) |
|---|---|
| 슬롯이 3·4·3 이 아니라 **4~5칸만** 그려짐 | `.rw-c` **10개**, 전부 151×151 |
| **금색 프레임이 4변 전부 0px** — 아예 없다 | `.rw-frame` 이 패널 인셋 2, `5px solid #B29661` |
| 슬롯 크기가 **145~157 로 제각각**(7.6% 편차) | 10칸 전부 151.0 (Δ0) |
| 슬롯 좌변 209 / 461 / 717 | 216 / 462 / 711 |

**둘 중 하나는 틀렸다.** 픽셀로 확정했다(`tools/audit120.js` 신설 — 브론즈·금색 픽셀을 직접 센다):

- 2회차 캡처: 좌우 가장자리에 금색 픽셀 **0개**, 슬롯 행 y890 에 칸 1개·y1065 에 **0개** → **비평가가 맞다.**
- 그런데 같은 실행에서 DOM 은 10칸 151×151 · 금테 인셋 2 → **게이트도 맞다.**

원인은 **60 쥬시 오픈 팝인**이다. `#relw` 는 `JZ_OVID` 에 들어 있어 열 때 팝인 애니메이션이 돈다.
프레임별 실측(2280, 탭 클릭 기준):

| t | 슬롯 폭 | 상태 |
|---|---|---|
| 80 · 150ms | **130.6** (0.865배, 중심으로 수축) | 팝인 초반 |
| 250ms | 144.0 | |
| 350ms | **153.9** | 오버슛 |
| **450ms 이후** | **151.0** | 안정 |

`cap120.js` 의 고정 대기가 **정확히 450ms** 로 종료 경계에 걸려 있었다 — 실행마다 «수축 프레임» 과
«정지 프레임» 이 갈렸다. 수축 프레임에서는 금테 5px 이 프레임 밖으로 밀려 사라지고, 행 y 가 위로
당겨져 고정 y 로 훑으면 슬롯 행이 통째로 «없는» 것처럼 보인다. **비평가 2명이 수렴한 것은
같은 결함을 본 것이 아니라 같은 잘못된 이미지를 본 것**이었다.

**고친 것** — `cap120.js` 에 두 겹 게이트를 넣어 **잘못된 프레임은 저장 자체를 막는다**:
1. **기하 정지 폴링** — 슬롯 10칸 bbox 서명이 연속 4프레임(≈240ms) 동일할 때까지 대기(상한 6초).
2. **규격 확인** — 정지한 뒤 슬롯 10칸 151×151 · 금테 인셋 2 를 확인. 어긋나면 PNG 를 안 쓰고 실패한다.
`verify120.js` 에도 같은 폴링을 넣었다(이 게이트는 `openRelw()` 직접 호출이라 팝인이 안 붙지만,
다음 세션이 경로를 탭 클릭으로 바꿔도 조용히 틀린 값을 재지 않도록).

3회차 캡처 실측 로그: `settle 600~720ms · 슬롯 10칸 151² · 금테 인셋 2`.
픽셀 재확인(`120-r3-2280.png`):

- 금색 `#B29661` — 좌 **x2~6**, 우 **x1073~1077**, 표본 **488/488 행 전부**
- y700(1행) 슬롯 좌변 **216 / 462 / 711** · y890(2행) **91 / 340 / 587 / 836** · y1065(3행) **216 / 462 / 711**

**즉 I·J 의 ①②③④⑤ 지적 중 «슬롯 누락 · 금테 부재 · 크기 편차 · 좌변 이탈 · 행 피치 185» 는 전부
캡처 결함이며 구현 결함이 아니다.** 2회차 실물 코드는 손댈 것이 없었다.

#### 3회차 채점 (정지 확인된 `120-r3-*` 기준)

| | ① | ② | ③ | ④ | ⑤ | 최저 |
|---|---|---|---|---|---|---|
| K | 9 | 4 | 4 | 9 | **3** | **3** |
| L | 9 | 6 | 7 | 9 | **6** | **6** |

①④ 는 둘 다 9 로 통과권(중심축 편차 ≤1.5px · 4변 대칭 · Lv 라벨 10개 ±0.5px). ②⑤ 가 문제였다.

**K 의 ②: «아치가 아니라 지름 589 정원이고 y1040 에서 끝난다»** — 돔 윤곽을 원 피팅해 중심 (540,745)·반지름
294.5 를 오차 ≤1px 로 얻었다. **L 의 ⑤ 는 같은 것을 다른 말로 짚었다**: «아치 좌우변이어야 할 x=245 / x=834 에서
좌우 휘도차 |Δ| ≤ 1.0 — 경계 없는 소프트 비네트일 뿐 구조물이 아니다».

자체 실측으로 확정했다(아치 안쪽 x300~780 vs 바깥 벽 x60~200·x880~1020 의 행평균 휘도차):

| y (2280) | 안쪽 | 바깥 | 대비(바깥−안쪽) |
|---|---|---|---|
| 487~657 (슬롯 위) | 21~33 | 21~25 | **−6.9 ~ −10.5** ← 아치가 벽보다 **밝다** |
| 1204~1306 (슬롯 아래) | 14~19 | 20~24 | **+5.9 ~ +1.0** ← 거의 구분 불가 |

**2회차에 «명암 구조를 넣는다» 며 넣은 상단 밝은 radial 이 아치를 뒤집어 놨다.** 레퍼런스 아치는
벽(#211D16)보다 **어두운 문간**(#0E100E)인데, 우리는 위가 밝고 아래로 갈수록 사라지는 **빛나는 원반**이 됐다.
K 가 «정원» 으로, L 이 «비네트» 로 읽은 것이 같은 원인이다.

### 4회차 — 캡처 `120-r4-{1600,1920,2280,2600}.png`

**고친 것 2건**

1. **아치를 «어두운 문간» 으로 되돌렸다.** 상단 radial 을 밝은 것(`rgba(62,56,44,.50)`) → 어두운 것
   (`rgba(2,3,2,.45)`)으로 뒤집고, 세로 그라디언트를 `#0E100E → #0B0D0A → #090B08 → #070906` 로
   **전 구간 벽보다 어둡게** 유지하고, 결 얼룩 5개도 전부 «어두운 쪽으로만» 준다. 알파 소멸은 마지막 6% 에서만.

   | y (2280) | 3회차 대비 | **4회차 대비** |
   |---|---|---|
   | 487~657 (슬롯 위) | −6.9 ~ −10.5 | **+4.2 ~ +14.7** |
   | 1204~1340 (슬롯 아래) | +5.9 ~ +1.0 | **+17.7 ~ +7.9** |

   → 아치가 위아래 모두 벽보다 어두워져 **좌우변이 실제 경계로 읽힌다**(L 의 «구조물이 아니다» 해소).

2. **여백 재배분** — 격자↔수반 간극이 슬롯 블록(516)보다 커서 «수반+안내문 덩어리가 하단 프레임에
   붙은 채 떠 보인다»(G ③ · J ③ · K ③ **3인 공통**).

   | frameH | 3회차 (상/격자↔수반/수반↔안내문/하) | **4회차** |
   |---|---|---|
   | 1600 | 235 / 176 / 30 / 50 | **250 / 177 / 16 / 49** |
   | 1920 | 388 / 291 / 50 / 83 | **412 / 292 / 26 / 81** |
   | **2280** | **526 / 572 / 47 / 44** | **595 / 422 / 38 / 117** |
   | 2600 | 668 / 722 / 58 / 56 | **757 / 537 / 49 / 149** |

   격자↔수반 : 하단 여백 = **12:1 → 3.6:1**.

   > **K 와 L 이 정면으로 갈린 지점이 하나 있다** — «수반↔안내문»(gap2). K 는 하단 여백을 키우라 했고,
   > 3회차에 그걸 키웠더니(38→72) L 이 «안내문만 떨어져 나온다(+147%)» 로 지적했다.
   > → **큰 간극(격자↔수반)만 하단으로 옮기고 gap2 는 레퍼런스 비율 그대로 두었다**(2280 에서 38).
   > 두 지적이 갈릴 때는 «둘 다 만족하는 축» 으로만 움직인다(LESSONS 52-④).

**곁가지 — 스캐너 오검출 1건 수정.** `scan120` 의 «아치 하변 차분» 창을 `±40` 으로 잡았더니
**짧은 프레임에서 수반 상단이 창 안에 들어왔다**(1600 → 아치 하변 1045 · 수반 상단 1035).
수반 림은 밝아서 **Δ21.9 짜리 «급점프»** 로 잡히는데 그건 절단선이 아니다. 창을 수반 위로 자르니
전 높이 **Δ2.1 ~ 3.7**. (게이트가 스스로 만든 거짓 경보 — 게이트도 회귀 검사가 필요하다.)

4회차 지표(정지 확인 캡처):

| 지표 | 1회차 | **4회차** |
|---|---|---|
| 아치 행별 고유색 (최소) | 1 | **10 ~ 11** |
| 아치 행평균 휘도 진폭 | 0 | **80.3 ~ 81.5** |
| 바닥 행별 고유색 (최소) | 1 | **30 ~ 63** |
| 아치 하변 1차 차분 | 4.2 | **2.1 ~ 3.7** |
| 아치 안↔밖 대비 (슬롯 미가림 행) | — | **+4 ~ +19** (3회차 −10 ~ +1) |

#### 4회차 채점

| | ① | ② | ③ | ④ | ⑤ | 최저 |
|---|---|---|---|---|---|---|
| M | 9 | 9 | 8 | 9 | **4** | **4** |
| N | 8 | 6 | 7 | 8 | **5** | **5** |

**①③④ 가 8~9 로 통과권에 들어왔다** — 배치·여백·정렬은 끝났고 **⑤ 하나만** 남았다.

M·N 이 독립적으로 같은 1순위를 짚었다 — **«아치에 경계가 없다»**:

| 지표 | 4회차 실측 | ref |
|---|---|---|
| 아치 좌·우변 행별 최대 \|d휘도/dx\| | **0.45 ~ 3.1 /px** (argmax 가 스캔 창 끝에 걸림 = 국소 에지 없음) | 중앙값 **1.45 /px** |
| 안팎 전환 폭(페더) | **약 50~70px** | 약 10px |
| 아치 안쪽 평균 휘도 | **11.7(슬롯 위) / 7.7(슬롯 아래)** — 82%가 휘도 8 미만 | — |
| 24×24 블록 std<1 | **48.9%** | 24.2% |

→ **밝기 차만으로는 «국소 에지» 를 못 만든다.** 3회차에 아치를 어둡게 되돌린 것은 방향은 맞았지만
그것만으로는 «구조물» 로 안 읽혔고, 4회차엔 오히려 너무 어두워져 M 이 «뚫린 구멍» 으로 읽었다.

### 5회차 — 캡처 `120-r5-{1600,1920,2280,2600}.png`

**고친 것 3건 + 되돌린 것 2건**

1. **문설주(jamb) 립을 실제로 세웠다** — `.rw-bg::after` 에 `border:7px`(상·좌·우 3변, **하변 0**).
   ⚠ `*{box-sizing:border-box}` 리셋은 **의사요소를 안 잡는다**(A1 비고의 CSS 함정 2) — 직접 적었다.
2. **아치 안쪽을 «구멍» 에서 «통로» 로** — 세로 그라디언트를 `#2A2721 → #1B1810` 로 올리고
   어둡게 하던 얼룩 5개의 알파를 .38~.50 → .22~.32 로 낮췄다.
3. **석벽 «켜 줄눈» 복원** — 측정표 §배경이 적은 «돌 블록 윤곽 #241D18» 을 되살렸다(가로 47px · 세로 163px,
   서로소 주기). 이전 세션이 «ref 는 또렷한 격자선이 없다» 며 얼룩으로 바꿨는데, 그건 «또렷한 격자» 얘기지
   «결이 없다» 는 뜻이 아니었다. `.rw-bg::before` 를 `inset:0` 으로 바꿔 **바닥까지** 깔았다 —
   긴 프레임에서 커지던 평면(27.7% → 40.0%)이 바닥 대역에 결이 없어서였다.

**되돌린 것 2건 (둘 다 «하변만은 건드리면 안 된다» 는 같은 이유)**

- **립 안쪽 홈을 `inset` 그림자로** 넣어 봤다 → `inset` 은 **4변 전부**를 따라가 하변에도 가로선이 생겼고
  절단선 지표가 3.6 → **10.2** 로 악화. 립 하나로 이미 기울기 22~27 이라 홈은 불필요 — 제거.
- **하변 소멸을 배경 알파로** 두면 **테두리(border)는 불투명하게 남아** 문설주가 하변에서 뚝 끊긴다
  (2600 에서 하변 점프 5.0 vs 창 중앙값 0.7). → `mask-image` 로 교체 — 테두리·배경·그림자를
  **한꺼번에** 지우므로 문틀째 바닥으로 녹아든다.

**5회차 지표 (전 프레임 높이, 목표 대비)**

| 지표 | 1회차 | 4회차 | **5회차** | 목표 |
|---|---|---|---|---|
| 아치 테두리 최대 기울기 | — | 6.1 | **22.2 ~ 26.6 /px** | ≥ 8 (ref 1.45) |
| 아치 안쪽 평균 휘도 | — | 11.7 / 7.7 | **23.3 ~ 29.1** | ≥ 22 |
| 24×24 블록 std<1 | — | 48.9% | **22.9 / 23.6 / 27.4 / 27.8%** | < 30% (ref 24.2%) |
| 아치 행별 고유색(최소) | 1 | 10~11 | **29 ~ 32** | — |
| 아치 하변 절단선 | 4.2 | 3.6 | **없음(4개 높이 전부)** | 없음 |

여백(1600 / 1920 / 2280 / 2600):

| | 상 | 격자↔수반 | 수반↔안내문 | 하 |
|---|---|---|---|---|
| 1600 | 250 | 177 | **38** | 27 |
| 1920 | 412 | 292 | **38** | 70 |
| 2280 | 595 | 422 | **38** | 117 |
| 2600 | 757 | 537 | **38** | 160 |

**gap2 를 «비례» 에서 «고정 38px» 로 바꿨다** — 비례로 키우면 프레임이 길수록 안내문이 수반에서
떨어져 «한 덩어리» 로 안 읽힌다(L +147% · N +43%로 독립 지적). 게이트 ⑥ 도 «3곳 비례 + gap2 고정» 으로 갱신.

**반증해 미채택 (5회차)**

- **N ② «수반 받침 폭 342 > 림 332 라 실루엣이 역전됐다»** — `.rw-stone` SVG 는 **89 구간**이고
  측정표는 «수반 전체 362 · 림 360» 으로 거의 같은 폭을 확정했다(N 이 ref 에서 잰 «받침/림 0.79» 와 어긋난다).
  89 가 이 실루엣으로 ①~④ 8 을 통과했고 다른 비평가 5명은 지적하지 않았다 — 저해상 ref 재측정은
  수렴하지 않는다(LESSONS 89-①). 120 범위 밖이라 선확정에 넣었다. **89 재폴리시 때 볼 것.**
- **H ⑤·N ⑤ «금테 안쪽 2px 검정 실선 = 배경이 금테에 안 닿는다»** — `.rw-frame` 의
  `box-shadow:inset 0 0 0 2px` 로, **89 설계의 «검정 + 갈색 2겹 프레임»** 이다. 배경 위에 프레임이
  그리는 선이지 «배경 미도색» 이 아니다(배경 자체는 `inset:0` 으로 패널 끝까지 간다 — 게이트 ②③ 이 지킨다).

**곁가지 — 스캐너가 스스로 만든 거짓 경보 2건 (둘 다 게이트 쪽 수정)**

1. 4회차: «아치 하변 ±40» 창이 **짧은 프레임에서 수반 상단을 물어** Δ21.9 를 절단선으로 오검출.
   → 창을 수반 위로 잘랐다.
2. 5회차: 켜 줄눈(주기 47px)이 **아치 하변과 우연히 겹치면**(2600 에서 y1567) 줄눈 점프를 절단선으로 오검출.
   → C 를 **«아치 안쪽 − 아치 바깥» 차분**으로 재정의했다. 줄눈은 전 폭이라 상쇄되고, 아치만의 절단선은 남는다.
   재정의 뒤 창 최대 점프가 8.5 → **2.6** 으로 떨어져 «줄눈이었다» 는 것이 확인됐다.

#### 5회차 채점

| | ① | ② | ③ | ④ | ⑤ | 최저 |
|---|---|---|---|---|---|---|
| P | 10 | 7 | 8 | 7 | **7** | **7** |
| Q | 9 | 8 | 8 | 9 | **4** | **4** |

⑤ 가 7 과 4 로 크게 갈렸다 — **«단색 평면» 임계값 차이**다. P 는 24×24 range≤3 으로 재서 **우리 13.6% vs ref 14.6%**
(«ref 이상으로 채워져 있다»)라 했고, Q 는 48×48 채널 σ<3 으로 재서 **우리 53% vs ref 40%** 라 했다.
같은 화면을 두고 «ref 보다 낫다» 와 «ref 보다 나쁘다» 가 동시에 나온다 — **이 축은 임계값에 지배당한다.**
그래서 평면 비율을 더 쫓지 않고, **둘이 «수치까지 같게» 수렴한 지적**만 골라 처리했다.

**곁가지로 확인한 것 — 켜 줄눈은 «지표만» 올리고 있었다.** Q 가 «벽돌 그리드가 아치를 그대로 관통한다» 고 해서
47px 주기 대비를 직접 쟀더니 **아치 안 0.42 · 아치 밖 0.38** — 둘 다 사실상 안 보이는 수준이다(휘도 0.4/255).
즉 5회차의 줄눈은 24×24 std 를 문턱 위로 밀어 올려 **D 지표만 개선**했을 뿐 그림은 거의 안 바꿨다.
Q 의 «관통한다» 는 관찰은 맞지만 «데칼로 읽힌다» 는 결론까지는 못 간다 — 보이지 않는 선은 관통해도 안 보인다.
**지표가 좋아졌다고 그림이 좋아진 게 아니다.**

### 6회차 — 캡처 `120-r6-{1600,1920,2280,2600}.png`

**P·Q 가 «수치까지 같게» 수렴한 지적 4건을 전부 처리했다.**

| # | 지적 (P / Q 공통) | 처방 | 실측 전 → 후 |
|---|---|---|---|
| 1 | **격자↔수반 대역에 수평 구조가 0개** — 열평균 15px 간격 변화량이 25개 표본 전부 ≤2.6 인 단조 램프. ref 는 같은 대역에 **수평 단 7개(Δ4.6~7.7)**. 아치 발밑이 «바닥» 이 아니라 «빈 램프» 라 수반이 벽면 위에 떠 보인다 | **`.rw-floor` 신설** — 바닥선을 **아치 하변과 같은 y** 에 걸어 아치가 착지하게 하고 계단 3단 | 단 0개 → **Δ≥4 단 6개** |
| 2 | **배경 미러 이음매** — x=539/540 에 1px 하드 스텝(P: 329행·최대 Δ15) | 얼룩 타일 **540px** 이 1080 을 정확히 2등분해 생긴 반복 이음매였다. 타일 폭을 1080 의 약수가 아닌 값으로 레이어마다 흩고 위상도 어긋냄 | 329행/Δ15 → **배경 전용 973행 중 2행 / Δ4.7** |
| 3 | **안내문 잉크가 ref 대비 −14%** (P 776 · Q 774 vs ref 902 · 909) | 측정표 §캡션도 «911×29» — **89 가 놓친 미달**이다. `font-size` 31 → **36** (`line-height` 44 유지 → 구획 높이 88 불변) | 776 / 423 → **900 / 493** (ref 902 / 491) |
| 4 | **아치 다리가 하단에서 공중 소멸** (P: 대비 y1240 24 → y1400 **0**, 하변 62px 앞) | 5회차 `mask` 페이드를 76% 에서 시작한 탓. **97%** 로 늦춰 바닥선까지 내려오게 함 | y1400 대비 0 → **2.4**, 바닥선과 접합 |

> **1번의 첫 시도는 과했다** — 단 대비가 Δ13~23 으로 ref(4.6~7.7)의 3배라 «자로 그은 줄» 로 보였다.
> 알파를 1/3~1/2 로 낮춰 최대 12.8 로 맞췄다(바닥선 1개만 12.8, 나머지 단은 4~7).

**미채택 (근거 기록)**

- **Q ⑤ «아치를 프레임과 함께 스케일해라(2600 에서 888 → 1050)»** — 정반대 지적이 이미 있었다.
  1회차에 아치 높이를 %로 늘렸더니 폭 589 고정이라 종횡비가 1:1.51 → 1:1.98 로 무너져 **H 가 ② 를 6 으로** 깎았다.
  888 고정은 그 지적을 받아 2회차에 도입한 것이다. 되돌리면 H 의 지적이 되살아난다 — **두 지적이 배타적**이라
  «ref 종횡비 유지» 쪽을 택했고 그 근거를 여기 남긴다.
- **Q ② «석주를 폭 100px 로(외곽 x190/890 · 개구 x290/790)»** — 개구가 x290~790(폭 500)이 되면
  **2행 슬롯(x91~987)이 아치 밖으로 나간다.** 슬롯 좌표는 ref 1:1 확정값이라 움직일 수 없으므로 성립 불가.
  현재 립 7px 로 경계 기울기 22~35 휘도/px(ref 중앙값 1.45)를 이미 확보했다.

**6회차 지표**

| 지표 | 5회차 | **6회차** | 목표 |
|---|---|---|---|
| 중심 이음매(배경 전용 행) | 329행 / Δ15 | **2행 / Δ4.7** | 0행 |
| 안내문 잉크 폭 1행 / 2행 | 776 / 423 | **900 / 493** | 902 / 491 |
| 격자↔수반 수평 단(Δ≥4) | 0개 | **6개** | ref 7개 |
| 아치 테두리 최대 기울기 | 22.2~26.6 | **31.8 ~ 35.6 /px** | ≥ 8 |
| 24×24 블록 std<1 | 22.9~27.8% | **20.8 / 25.4 / 27.2%** | < 30% |
| 아치 하변 | 접합부 없음 | **전 높이 «바닥선과 만나는 접합부»**(바깥 단 17~19) | 허공 절단 없음 |

**곁가지 — 스캐너 3번째 개선.** C 가 이제 «허공 절단선» 과 «바닥과의 접합부» 를 구분한다.
6회차에 바닥선을 아치 하변과 **같은 y** 에 깔았으므로 아치 안쪽이 거기서 끝나는 것은 정상이다.
둘의 차이는 «바깥에도 단이 있는가» — 허공에서 잘리면 안쪽에만, 바닥에 닿으면 전폭이라 바깥에도 선다.

#### 6회차 채점

| | ① | ② | ③ | ④ | ⑤ | 최저 |
|---|---|---|---|---|---|---|
| R | 9 | 9 | 8 | 9 | **8** | **8** ← 첫 통과선 |
| S | 9 | 9 | 8 | 9 | **7** | **7** |

**①②③④ 가 둘 다 8~9 로 안정됐다** — 배치·비율·여백·정렬은 4회차 이후 회귀 없이 유지된다.
남은 것은 ⑤ 하나이고 R 8 / S 7 로 한 칸 차이다.

**R 이 진짜 버그를 잡았다 — 짧은 프레임에서 바닥선이 수반을 관통한다.**
6회차에 바닥선을 «아치 발» 에 걸었는데, 아치는 격자 아래로 **186px** 내려오고
격자↔수반 간극은 1600 에서 **177px** 뿐이다. 즉 아치 발이 수반보다 **9px 아래**로 내려간다:

| frameH | 격자↔수반 간극 | 아치 발 ↔ 수반 상단 |
|---|---|---|
| **1600** | 177 | **−9px (관통)** |
| 1920 | 292 | +106 |
| 2280 | 422 | +236 |
| 2600 | 537 | +351 |

R 의 실측(«수반 림 상단 1058 · 문지방선 1060~1063 · 클리어런스 0, 림이 5px 관통»)과 정확히 일치했다.
게이트가 못 잡은 이유는 verify120 ④ 가 «격자·수반·안내문» 세 구획의 상호 겹침만 봤고
**배경 구조물(바닥선)과 내용의 충돌은 안 봤기 때문**이다.

### 7회차 — 캡처 `120-r7-{1600,1920,2280,2600}.png`

**고친 것 3건**

1. **아치 높이 클램프** — `min(888, 수반상단 − 28 − 아치상단)`.
   1600 에서만 888 → **851** 로 줄어 클리어런스가 **−9 → +27px**. 1920~2600 은 888 그대로(규격 불변).
   **verify120 에 이 클리어런스 게이트를 신설**(72/72) — 같은 충돌이 다시 나면 즉시 FAIL 한다.
2. **켜 줄눈을 «보이게» 세웠다.** S 의 «전폭 무특징 띠 최장 213px(2600 은 487px, ref 111px)» 의 원인은
   줄눈이 **있는데 안 보이는** 것이었다(대비 0.4/255 — 5회차에 직접 재서 확인했다).
   대비를 제대로 주니 **어떤 전폭 저대비 런도 줄눈 주기(47px)를 못 넘는다.**
3. **아치 안쪽에 «자기 줄눈»** — S 가 «x400 의 y1210~1400 에서 에지 0개, 같은 y 바깥엔 5개» 로
   아치 내부가 무텍스처임을 짚었다. 같은 47px 피치를 **위상 23px 어긋내** 깔았다 —
   그러면 «벽 줄눈이 관통하는 데칼»(Q 의 우려)도 아니고 «무텍스처»(S 의 지적)도 아닌
   **통로 안쪽 벽**으로 읽힌다. 링도 7 → **14px**.

**7회차 지표**

| 지표 | 6회차 | **7회차** | 목표 |
|---|---|---|---|
| 전폭 무특징 띠 최장 런 | 213px (2600 487px) | **5 / 5 / 25px** | < 110 (ref 111) |
| 무특징 행 비율 | — | **2.2 / 4.1 / 7.0%** | — |
| 24×24 블록 std<1 | 20.8~27.2% | **13.0 / 16.6 / 18.2%** | < 30 (ref 24.2) |
| 아치 안쪽 세로 에지 행 | 0 | **10 ~ 16** | > 0 |
| 아치 테두리 최대 기울기 | 31.8~35.6 | **38.8 ~ 45.8 /px** | ≥ 8 |
| 아치 발 ↔ 수반 클리어런스 | **−9px (1600)** | **+27 / +96 / +213 / +324** | ≥ 20 |

**미채택 (근거 기록)**

- **S ⑤ «링 폭 7 → 24px»** — 안쪽으로 24px 면 개구가 x268~809 가 되어 **1·3행 슬롯(x216~862)을 침범**한다.
  바깥으로 키워도 슬롯 좌변 216 을 넘는다. 14px 로 타협했고, 그 상태에서 경계 기울기가
  38.8~45.8(ref 중앙값 1.45)이라 «선이 없다» 는 원 지적은 해소됐다.
- **S ③ «수반 앵커를 격자에서 아치 하단으로 옮겨라»** — 그러면 늘어난 높이가 상단 띠로 전부 가는데,
  S 자신이 상단 띠도 «죽은 평면» 으로 지적했다. **어디로 보내도 «빈 띠» 인 것이 문제의 본질**이라
  «옮기기» 가 아니라 «그 띠를 결로 채우기»(위 2번)로 풀었다 — 전폭 무특징 런이 213 → 5~25px 이 된 것이 그 답이다.

#### 7·8·9회차 채점

| 회차 | 비평가 | ① | ② | ③ | ④ | ⑤ | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| 7 | T | 9 | 8 | 9 | 8 | 8 | **8** |
| 7 | U | 6 | 8 | 5 | 9 | 5 | 5 |
| 8 | V | 9 | 9 | 9 | 9 | 6 | **9** |
| 8 | W | 8 | 9 | 6 | 9 | 4 | 6 |
| 9 | X | 8 | 7 | 8 | 9 | 6 | 7 |
| 9 | Y | 9 | 7 | 6 | 7 | 6 | 6 |

**8·9·10회차에서 고친 것 — 전부 «같은 종류의 실수» 였다**

7회차 이후 세 회차 동안 잡은 결함이 결국 하나의 패턴으로 수렴한다:
**«넣긴 넣었는데 안 보이는» 것을 지표만 보고 «넣었다» 고 판단한 것.**

| 회차 | 넣은 것 | 비평가가 «없다» 고 한 이유 (실측) |
|---|---|---|
| 5 | 석벽 켜 줄눈 | 대비 **0.42/255** — 24×24 std 는 넘겼지만 눈에는 안 보였다 |
| 8 | 지면 평면 | 알파 **.07~.13** — V 가 «벽 텍스처» 로 읽었다 |
| 9 | 필라스터 · 린텔 | 모서리 대비 **6.1** · **9.3** — X·Y 가 «요소 0개» 로 셌다 |
| 9 | 계단 | 알파 **.26 반투명** — 벽 세로 격자선이 그대로 관통해 «와이어프레임» (X) |
| 8 | 접지 그림자 | 수반 밑변보다 **위**에 있어 수반 뒤에 가려짐 (V·W·X·Y 전원 «0개») |

→ **10회차에 전부 «실제로 보이는» 값으로 다시 세웠다.**

| 지표 | 9회차 | **10회차** |
|---|---|---|
| 필라스터 안쪽 모서리 대비 | 6.1 | **24.8** |
| 린텔 대비 | 9.3 | **18.9** |
| 계단 안 벽 세로이음매 진폭 | 벽과 동일(관통) | **0** (완전 차폐) |
| 접지 그림자(수반 밑변 아래) | 안 보임 | 주변보다 **35.5 어두움** |
| 전폭 무특징 띠 최장 런 | 10~12px | **1px** (무특징 행 0.1~0.2%) |

그 밖에 8회차에 U 가 잡은 «지면이 고정 232px 이라 긴 프레임에서 수반에 못 닿는다»(2600 → 119px 부족)를
`bottom:0` 으로 고쳤고, T 가 잡은 «클램프가 하단만 잘라 앵커가 비대칭»(1600 격자 위 186 / 아래 163)을
«줄어든 만큼 아치를 절반 내리는» 식으로 다시 풀어 **1600 에서 상하 149.1 / 149.1 대칭 + 클리어런스 28** 을
동시에 만족시켰다.

**T 가 옳았고 내 선확정이 틀렸다 —「유물 소환」 잉크.** 앞 회차 비평가들이 준 «141» 을 확정치로 박아 뒀는데,
Range 로 글리프 박스를 직접 재니 **119.6×39.9** 였다(«141» 은 검정 외곽선을 포함한 값이었다).
측정표 ref 144 에 맞춰 `scaleX` .84 → **.93**(132.6, 외곽선 포함 ≈142). **선확정은 내가 실측한 것만 박아야 한다.**

**곁가지 — CSS 함정 2건 (둘 다 이 작업에서 처음 밟았다)**

1. **`calc()` 안의 `100%` 는 «쓰는 자리의 컨테이닝 블록» 으로 풀린다.** 계단 높이를 «바닥선~수반 여유» 의 %로
   주려고 `--rw-gap`(안에 `100%` 포함)을 만들어 `.rw-floor` **안에서** 썼더니, 100% 가 패널이 아니라
   `.rw-floor` 높이로 풀려 2280 에서 gap 236 이 **−231** 로 뒤집혀 계단이 9px 로 찌그러졌다.
   → 계단을 **패널 직속 래퍼**(`.rw-steps`)로 옮겨 100% 가 패널 높이로 풀리게 했다.
   커스텀 프로퍼티는 «값» 이 아니라 «토큰» 이라 정의한 자리가 아니라 **쓴 자리**에서 계산된다.
2. **고정 px 구조물은 짧은 프레임에서 반드시 다른 것을 뚫는다.** 계단을 고정 px 로 뒀더니 1600
   (바닥선~수반 여유 **28px**)에서 수반을 152px 뚫었다. 여유에 대한 %로 바꾸고 래퍼에 `overflow:hidden` 을
   걸어 «어떤 프레임에서도 여유 밖으로 못 나가게» 했다.

### 10회차 (마지막) — 캡처 `120-r10-{1600,1920,2280,2600}.png`

| | ① | ② | ③ | ④ | ⑤ | ①~④ 최저 |
|---|---|---|---|---|---|---|
| Z | 9 | 8 | 8 | 9 | 7 | **8** |
| AA | 8 | 8 | 6 | 9 | 6 | 6 |

## 5. 결론 — ⏸ 보류 (10회차 상한 소진)

**주인 지시 본문은 달성했고 게이트로 못 박았다.**

| 지시 검증 항목 | 결과 |
|---|---|
| 영역(x0~1080 · y108~탭바상변)을 패널이 전부 덮는가 | ✅ 4개 프레임 높이 전부 |
| «패널이 못 덮은» 픽셀 0 (센티넬) | ✅ 0px |
| 검은 띠 | ✅ 3회차 이후 비평가 **16명 전원** «검은 띠·미도색 없음» |
| 좌우 여백 0 · 겹침 0 · 잘림 0 | ✅ |
| 탭바 5칸 히트 25/25 회귀 | ✅ |
| 89 규격 불변(슬롯 151²·수반 400×216·피치 176/175) | ✅ |
| smoke | ✅ PASS |
| **비평가 2명 «꽉 차 보이는가» 8↑** | ❌ **최고 R 8 / Z 7 / AA 6** |

마지막 항목 하나가 미달이라 **완료가 아니라 보류**다(ROUTINE «화면당 누적 10회 → ⏸ 보류»).

**10회차 최종 지표**

| 지표 | 1회차 | **10회차** | 목표/ref |
|---|---|---|---|
| 전폭 무특징 띠 최장 런 | 213px (2600 487) | **1px** | < 110 (ref 111) |
| 무특징 행 비율 | — | **0.1 ~ 0.2%** | — |
| 24×24 블록 std<1 | 48.9% | **10.9 ~ 18.8%** | < 30 (ref 24.2) |
| 아치 테두리 최대 기울기 | 6.1 | **38.8 ~ 45.8 /px** | ≥ 8 (ref 1.45) |
| 아치 안쪽 평균 휘도 | 11.7 / 7.7 | **24 ~ 29** | ≥ 22 |
| 아치 하변 | 절단선 4.2 | **바닥선과 만나는 접합부** | 허공 절단 없음 |
| 아치 발 ↔ 수반 클리어런스 | −9px (1600) | **+27 ~ +324** | ≥ 20 |
| 중심 이음매 | 329행 / Δ15 | **2행 / Δ4.7** | 0 |
| 필라스터 / 린텔 대비 | 6.1 / 9.3 | **24.8 / 18.9** | ≥ 15 |

**갈린 축은 하나다 — «늘어난 320px 이 어디로 가는가».**
①~④ 는 R·S·T·V·Z 5명이 8~9 를 줬는데 짝이 된 U·W·X·Y·AA 는 ③(여백)을 5~6 으로 봤다.
내용(격자 516 + 수반 216 + 안내문 88 = 820)이 **고정**이라 늘어난 높이는 전부 여백·배경으로 갈 수밖에 없고,
그걸 «의도된 변경이니 감점 안 함» 으로 보는 쪽과 «320px 중 콘텐츠로 간 픽셀이 0» 으로 보는 쪽이 갈렸다.
**이건 CSS 로 더 밀어서 해결되는 문제가 아니라 «그 여백을 무엇으로 채울 것인가» 의 설계 문제**이므로
다음 라운드(2차 폴리시, 회차 +10 리셋)에서 아래 3건으로 이어서 잡는 것이 맞다.

**남은 문제 3건 → PROGRESS 120 행 비고에 그대로 옮겨 적었다.**
① 지면(벽↔바닥 접합선) 신설 + 받침 캐스트 그림자(현재 받침 아래가 주변보다 **8레벨 밝다** — 부호 반대)
② 상인방 위 스트립을 300px 로 클램프하고 회수분을 받침↔수반 간극·바닥면에 배분
③ 받침 계단 높이 고정 + 2·3단 디딤면/챌면 구분(834×117 단색 슬래브 분리)

**반려된 방향 3건도 같이 적어 뒀다** — 아치 늘이기(종횡비 붕괴) · 링 24px+(슬롯 x216 침범) ·
수반 받침 폭 ref 0.79 비(89 SVG 확정 규격). 되풀이하면 회차만 태운다.

**곁가지 등재**: `138` — `tools/verify119.js` A5 가 125(화폐 아이콘 통일, `50fbbdd`)가 `#rwCost` 의
`<i>🔮</i>` 를 SVG 슬롯으로 바꾸면서 항상 FAIL. 내 구간이 아니라 손대지 않고 등재만 했다(원인·고칠 방향 기록).

---

# 6. 2차 폴리시 라운드 (회차 상한 +10 리셋)

10회차에서 «①~④ 는 최고 9, ⑤ 꽉참만 2인 동시 8 미달» 로 보류했고, 남은 문제 3건을
PROGRESS 120 행에 그대로 적어 뒀다. 11회차는 **그 3건을 그대로 구현한 것**이다.

## 11회차 — 캡처 `120-r11-{1600,1920,2280,2600}.png` (sess-0528-12086 워커 B)

### ② 상인방 위 스트립 300px 클램프

10회차까지 격자 top 을 `--rw-sp × .5075` 로만 잡아 **늘어난 높이의 52%가 «상인방 위 죽은 벽»**
으로 갔다(2280 290px → 2600 455px). 상인방은 격자 위 300px 에 붙어 있으므로
«스트립 = 격자 top − 300» 이고, **격자 top 을 600 에서 묶으면** 스트립이 300 을 못 넘는다.

```
--rw-gt : min(--rw-sp × .5075, 600px)      /* 신설 — 격자 top */
```

| frameH | 격자 top (구) | 격자 top (신) | 상인방 위 스트립 |
|---|---|---|---|
| 1600 | 249.7 | 249.7 | −50.3 (프레임 밖) |
| 1920 | 412.1 | 412.1 | 112.1 |
| 2280 | 594.8 | **594.8 (불변)** | **294.8** |
| 2600 | 757.2 | **600.0** | **300.0** (455 → 300) |

**2280 은 594.8 이라 이미 600 미만 — 기준 프레임은 픽셀 하나 안 움직인다.** 2600 만 157.2px 을
회수했고, 그 회수분은 아래로 흘러 «받침↔수반 간극 + 바닥면» 이 된다(gap1 537.1 → 694.3).
그 구간은 아래 ①·③ 이 실제 구조로 채운다 — «옮기기» 가 아니라 «채우기» 다(7회차 결론과 같은 처방).

세로 앵커를 매번 손으로 다시 쓰지 않도록 `.rw-panel` 에 **공용 토큰 4개**를 세웠다.
`--rw-fl`(바닥선 = 아치 발) · `--rw-bt`(수반 구획 상변) · `--rw-sh`(그 사이 = 계단 구간 높이) ·
`--rw-s2b`(계단 밑변). ⚠ 전부 `100%`(`--rw-sp`)를 품고 있어 **`.rw-panel` 직속 자식에서만** 쓸 수 있다
(10회차에 밟은 함정 — 손자에서 쓰면 100% 가 그 부모 높이로 풀린다).

### ① 지면(벽↔바닥 접합선) + 받침 캐스트 그림자

10회차의 `.rw-ground` 는 두 가지가 틀려 있었다.

1. **위치** — 접합선을 «수반 밑변 높이»(`--rw-sp × .8675 + 732`)에 걸었는데, 그건 받침 구조물보다
   한참 아래라 AA 가 여전히 «벽이 끊김 없이 이어진다» 로 읽었다.
   → **받침 계단의 밑변(`--rw-s2b`)** 으로 올렸다. 이제 구조물이 지면에 «서 있다».
2. **불투명도** — 배경이 알파 `.34` 반투명이라 **벽 켜 줄눈(균일 47px)이 바닥을 그대로 관통**했다.
   이 작업에서 **여섯 번째로 밟은 «넣긴 넣었는데 안 보이는»** 함정이다.
   → 마지막 레이어를 **불투명**으로 깔아 줄눈을 실제로 끊고, 그 위에 «아래로 갈수록 넓어지는»
   원근 이음선(30 → 38 → 48 → 62 → 78 → 100 → 118 → 138px)을 얹어 벽과 결이 다른 면임을 드러냈다.

**캐스트 그림자는 부호가 반대였다**(10회차: 받침 아래가 주변보다 **8레벨 밝음**). 그림자를 실제로 넣되
두 군데서 넣는다 — `.rw-st` 의 `box-shadow:0 7px 12px -3px rgba(0,0,0,.82)`(단 밑 접지) +
`.rw-ground` 의 radial(구조물 밑동에서 바닥으로 퍼짐).

> **CSS 함정 (이 회차에서 새로 밟은 것)** — `radial-gradient(closest-side ellipse at 50% 0%, …)` 는
> 중심이 **위 변 위**라 «가장 가까운 변까지» 가 세로로 **0** 이 되어 그림자가 사실상 안 그려진다.
> 게이트 실측 Δ**−0.7**(= 부호가 그대로 반대). 반지름을 `ellipse 480px 170px` 로 **명시**해서 고쳤다.

> **칠 순서 함정** — `.rw-ground` 를 DOM 앞쪽에 두면 접합선이 계단의 `box-shadow` 와 `.rw-floor` 의
> 반투명 오버레이에 덮여 문지방 대비가 **132 → 57** 로 깎인다(같은 z-index 1 이라 DOM 순서가 칠 순서).
> `.rw-steps`·`.rw-floor` **뒤로** 옮겼다. 접합선은 계단 밑변 아래에서만 시작하므로 계단 자체는 안 가린다.
> 대신 `.rw-floor::after` 비네트를 못 받으니 바닥이 **자기 비네트**(14%, 바닥은 «멀어지는 면» 이라 더 좁게)를 갖는다.

### ③ 받침 계단 — 높이 고정 + 디딤면/챌면 분리

폭은 723 / 843 으로 고정인데 **높이만 `30%` / `34%` 라 프레임 따라 표류**했다
(2단 68 → 103 · 3단 78 → 117, +50%). 레이아웃에서 유일하게 비율이 안 맞는 요소였다.

```
.rw-st1 { top:min(19px, 8%);  height:clamp(15px, 30%, 70px) }
.rw-st2 { top:min(90px, 38%); height:clamp(15px, 34%, 80px) }
```

`clamp`/`min` 을 쓴 이유는 1600 에서 계단 구간이 **28px 뿐**이기 때문이다 — 고정 px 로만 두면
짧은 프레임에서 수반을 뚫는다(10회차 교훈 2). 상한은 px, 하한은 %가 대신한다.

«834×117 단색 슬래브» 는 5층 그라디언트로 갈랐다:
코 하이라이트 4px → 코 밑 그림자 5px → **디딤면 13px(`#4E4331`, 휘도 66.8)** → 단차 4px →
**챌면(`#332B1D`, 휘도 43.5)**. **대비 23.3** — 이 작업의 교훈(«넣었으면 대비 몇인가를 재라»)대로
게이트에 `≥ 12` 로 박았다.

### 게이트 — `tools/verify120.js` **72 → 104 항목**

11회차에 신설한 8항목(프레임 4종 × 2 + 픽셀 프로브):

| 항목 | 2280 실측 | 2600 실측 |
|---|---|---|
| ② 상인방 위 스트립 ≤ 300px | 294.8 | **300.0** |
| ① 지면 접합선 = 계단 밑변 (Δ ≤ 1px) | 1574.4 = 1574.4 | 1580.0 = 1580.0 |
| ① 접합선이 수반 위 · 바닥이 패널 하변까지 | 1574.4..2100.0 | 1580.0..2420.0 |
| ③ 계단 높이 고정 (≤ 70 / 80) · 폭 723 / 843 | 723×70.0 / 843×80.0 | 723×70.0 / 843×80.0 |
| ③ 계단이 구간 안 (잘림·돌출 0) | ✅ | ✅ |
| ① **문지방 대비 ≥ 15**(픽셀) | Δ**84.4** | Δ**84.5** |
| ①-2 **캐스트 그림자 부호**(픽셀) | Δ**17.1** 어두움 | Δ**14.0** 어두움 |
| ③ **디딤면 / 챌면 대비 ≥ 12**(픽셀) | Δ**24.3** | Δ**24.3** |

⑥ 여백 모델도 클램프에 맞춰 고쳤다(`GAP_W = [.5075, .8675]` + `GT_MAX 600`) — 안 고치면
2600 에서 기대 757.2 vs 실측 600 으로 게이트가 «틀린 정답» 을 우긴다.
`tools/scan120.js` 도 같은 이유로 보정했다(2600 을 157px 어긋난 자리에서 재고 있었다).

**결과: `VERIFY120 104/104 PASS` · `VERIFY100 44/44 PASS` · `SMOKE PASS` · 콘솔 에러 0.**

> **smoke 주석 불균형 1건 자초 → 즉시 복구.** 주석 안에 `249.7/412.1/**594.8**` 이라고 썼더니
> «`412.1` + 슬래시 + `**`» 가 **여는 주석**으로 세어졌다(A1 이 같은 것을 밟았다). 가운뎃점으로 바꿨다.

### 11회차 지표 (`tools/scan120.js`)

| 지표 | 10회차 | **11회차** | 목표/ref |
|---|---|---|---|
| 24×24 블록 std<1 (2600) | 10.9 ~ 18.8% | **19.8%** | < 30 (ref 24.2) |
| 전폭 무특징 띠 최장 런 (2600) | 1px | **36px** | < 110 (ref 111) |
| 아치 테두리 최대 기울기 | 38.8 ~ 45.8 | **41.5 /px** | ≥ 8 (ref 1.45) |
| 아치 안쪽 평균 휘도 | 24 ~ 29 | **34.6** | ≥ 22 |
| 아치 하변 | 접합부 | **접합부(점프 6.9 vs 창 최대 15.7)** | 허공 절단 없음 |
| 아치 안쪽 세로 에지 행 | 10 ~ 16 | **33** | > 0 |

무특징 런이 1 → 36px 로 늘어난 것은 **바닥 최하단 청흑 띠를 불투명 바닥이 다시 그리면서**
구 `.rw-bg` 의 얼룩 2개가 덮였기 때문이다. 같은 얼룩을 바닥 앵커로 옮겨 심어 45 → **36px** 로 줄였고,
목표(110)의 3분의 1이라 그대로 뒀다 — ref 자신도 이 자리가 111px 평면이다.

> **캡처 정정** — `120-r11-2280.png` · `-2600.png` 은 위 «하단 얼룩 2개» 를 심은 뒤 **다시 찍었다**.
> 기하는 한 픽셀도 안 바뀌었고(VERIFY120 104/104 동일) 바뀐 것은 최하단 128px 띠의 결뿐이다.

### 11회차 채점

| | ① | ② | ③ | ④ | ⑤ | ①~④ 최저 |
|---|---|---|---|---|---|---|
| AB | 4 | 4 | 3 | 6 | 4 | **3** |

**AB 가 확인해 준 것 (3건 다 «구현됐다» 판정)**

| 확인 항목 | AB 실측 |
|---|---|
| ① 지면 접합선 | **보인다.** y1048/1052(1600)·1299/1303(1920)·1574/1578(2280)·1580/1584(2600), 1080열 중 **100%** 에서 \|Δlum\|>4 · 선 휘도 111~122 |
| ① 벽 vs 바닥 결 구분 | 벽 = **정확히 47px 주기 27켜**, 바닥 = 이음선 **45→57→73→91→115→135→157px 단조 증가**(×1.20~1.28) → «원근 성립» |
| ①-2 캐스트 그림자 부호 | **정상(어두움).** 2600 seam+10 에서 x540 lum 10 vs x180 31 = **−68%** · seam+70 −45% · seam+150 −19%. «앞 회차의 8레벨 밝음 역부호는 해소» |
| ② 상인방 위 스트립 | 2600 **286px**(앞 회차 455 대비 −169, −37%) — 줄긴 줄었으나 «미통과» |
| ③ 계단 폭 고정 | cap 617 · 상단 716 · 하단 836 — **4장 동일** ✅ |
| ③ 계단 높이 고정 | **미통과** — 8/31/70/71px(상단) · 15/37/80/80(하단) |

**점수가 3 으로 내려간 진짜 이유는 «내가 고친 것» 이 아니라 «그 고침이 만든 부작용» 이다.**

1. **2600 에서 접합선(1584) → 수반 상단(1925) 이 341px 비었다** (10회차엔 98~107px 이었다).
   두 변화가 **겹쳐서** 벌어졌다 — ② 클램프가 gap1 을 +157px 늘렸고(537→694),
   ③ 계단 높이 고정이 «늘어나던 계단» 을 −186px 짧게 만들었다. 98 + 157 + 186 = 341.
   **즉 ②·③ 을 각각 지시대로 했는데 둘이 같은 구간에서 만나 서로를 증폭했다.**
   AB 의 1순위 지적이고, 정당하다 — 소환 버튼(CTA)이 받침·지면에서 떨어져 부유한다.
2. **계단 높이는 2280·2600 에서만 고정됐다.** `clamp(15px, 30%, 70px)` 의 **상한(px)만** 걸었기 때문에
   계단 구간이 짧은 프레임(1600 은 **28px**, 1920 은 106px)에서는 %가 그대로 살아 8/15px 로 찌그러진다.
   디딤/챌면 구분도 그래서 1920 은 «디딤 13 · 챌면 5» 로 붕괴하고 1600 은 **챌면이 아예 없다**.
3. **1600 에서 상인방이 통째로 사라진다** — `top: 격자 top − 300` = **−50.3px** 라 패널 밖이다.
   AB: «y122~700 에 행평균 44 초과 전폭 밴드 없음 = 프레임별로 건축 구성이 달라진다».
4. **캐스트 그림자 폭이 구조물보다 좁다** — seam+10 에서 어두운 구간이 x300~780(480px)인데
   그림자를 던지는 계단은 x122~957(836px)이라 **57%** 뿐. 받침 «날개»(x150~380·x700~930)는
   오히려 바깥보다 +24~47% 밝아 «발자국 그림자» 가 아니라 «중앙 라디얼 웅덩이» 로 읽힌다.
   → `ellipse 480px 170px` 의 480 은 **계단 폭이 아니라 내가 임의로 고른 값**이었다. 843 이어야 한다.

> **1600 단독으로는 AB 도 «통과 수준»(무특징 최장 24px · 세로 콘텐츠 점유 65%)이라고 적었다.**
> 무너지는 곳은 프레임이 길어질수록 커지는 **2280·2600** 이다.

## 12회차 — 캡처 `120-r12-{1600,1920,2280,2600}.png`

**AB·AC 가 각자 1순위로 짚은 것이 같았고, 반대쪽 끝의 지적(AB ②·AC ②)과 «같은 병» 이었다.**

| | 11회차 증상 | 두 사람 실측 |
|---|---|---|
| 긴 프레임 | 2600 «접합선 → 수반» 이 텅 빔 | AB **341px** · AC **346px** (1600 대비 26.6배) |
| 짧은 프레임 | 1600 계단이 붕괴 | AB 단 면 **8/15px** · AC **5px·4px**(종횡비 168:1) |

> **진단 — 고정 크기 구조물을 «위쪽 끝»(아치 발)에 앵커한 것이 병의 뿌리다.**
> 위에 붙여 놓으면 구간이 길어질 때 **아래쪽이 남고**, 구간이 짧아지면 **자기가 찌그러진다.**
> 11회차에 ②(클램프, gap1 +157px)와 ③(높이 고정, 계단 −186px)을 지시대로 각각 했는데
> **둘이 같은 구간에서 만나 서로를 증폭**해 98 → 341px 이 됐다. 개별 처방은 맞았고 합이 틀렸다.

### 처방 — 계단을 «수반에 앵커 · 크기 고정 · 개수로 채움» 으로 바꿨다

```
.rw-st  { bottom:0; height:var(--rw-sp1) }        /* --rw-sp1 = 84px, 네 프레임 전부 동일 */
.rw-st1 { width:843px; bottom:0; height:min(var(--rw-sp1),100%) }
.rw-st2 { width:798px; bottom:calc(var(--rw-sp1) * 1) }   /* … st5 까지 45px 씩 좁아진다 */
```

* 맨 아래 단의 밑변 = 계단 구간의 밑변 = **수반 상단** → «접합선 → 수반» 공백이 **네 프레임 전부 0.0px**
* 단 면 높이는 **84px 고정**, 늘어난 높이는 **보이는 단 개수**가 흡수한다 (1600 **0개** → 1920 **1개** → 2280 **3개** → 2600 **5개**)
* 폭은 아래로 갈수록 넓어진다(663 → 708 → 753 → 798 → 843). 맨 위 단 663 이 받침 617 보다 넓어
  «내려갈수록 넓어진다» 가 **어느 프레임에서도 안 뒤집힌다**
* 1600 은 구간이 0px 라 **단을 하나도 안 그린다** — «5px 짜리 찌그러진 단»(AC ②)보다 없는 편이 정직하다
* 1920 처럼 구간이 한 단보다 얕으면 맨 아래 단만 `min(84px,100%)` 로 줄어 **코·디딤면을 잃지 않는다**

### 같이 고친 3건

1. **접합선을 «받침 밑동»(바닥선 + 40, 고정 오프셋)으로** — 11회차의 «계단 밑변» 은 계단이 아래 앵커로
   바뀌면서 프레임마다 움직이는 자리가 됐다. 받침은 아치에 종속돼 있어 관계가 고정이다.
   계단은 접합선보다 **14px 아래**에서 시작한다 — 1920 에서 계단이 접합선 띠를 통째로 덮어
   게이트가 «접합선 밑 그림자 Δ0.5 = 그림자가 없다» 로 잡았다.
2. **상인방 부활(AB ⑦·AC ③)** — `--rw-lt: max(0, min(격자top−300, 격자top−256))` + 높이도 남는 자리에 맞춰
   줄인다. 1600 은 패널상단~아치정점이 **63.7px** 뿐이라 66px 이 안 들어간다 → **59.7px** 로 들어갔다.
3. **하단 여백 clamp(AB ⑥·AC ⑥, 26 → 159px = 6.1배 표류)** — 수반·안내문을 «위에서» 재던 것을
   **패널 바닥에서 역산**으로 바꾸고 `clamp(34px, --rw-sp × .1325 − 38px, 120px)` 로 묶었다.
   **1920·2280 은 clamp 안쪽이라 픽셀 하나 안 움직인다**(2280 117.3 → 117.3).
4. **캐스트 그림자 반지름 480 → 308**(AB ⑧) — 480 은 내가 임의로 고른 값이었고 «중앙 웅덩이» 로 읽혔다.
   이 자리의 그림자를 던지는 것은 **받침(폭 617)** 이므로 617/2 = 308 이 맞다. 계단 폭만큼 넓은
   접지 그림자는 각 단의 `box-shadow` 가 자기 자리에서 만든다.

> **곁가지 — `--rw-ah`(아치 높이 클램프)도 같이 풀어야 했다.** 구 식 `--rw-sp × .72 + 460` 은
> **옛 수반 위치(.8675)에서 역산한 상수**라, 수반을 바닥에서 역산하도록 바꾸자 1600 의
> «아치 발 ↔ 수반» 클리어런스가 28 → **21.2px** 로 깎였다. 상수를 버리고 `--rw-bt` 로 직접 풀었다:
> `fl = gt + 258 + ah/2 ≤ bt − 28` ⟹ `ah ≤ 2 × (bt − gt − 286)`. 1600 만 걸리고(800.6) 나머지는 888 그대로다.

### 12회차 게이트 — `tools/verify120.js` **104 → 114 항목**

| 항목 | 1600 | 1920 | 2280 | 2600 |
|---|---|---|---|---|
| **③ 맨 아래 단 → 수반 상단**(11회차 341px 회귀 방지) | **0.0** | **0.0** | **0.0** | **0.0** |
| ③ 보이는 단 개수 / 면 높이 | 0개 | 1개 / 52.3 | 3개 / **84.0** | 5개 / **84.0** |
| ② 상인방 존재(높이) | **59.7** | 66.0 | 66.0 | 66.0 |
| ⑥ 하단 여백(clamp 34~120) | **34.0** | 69.6 | **117.3(불변)** | **120.0** |
| ① 문지방 대비 | Δ**100.5** | Δ**99.5** | Δ**102.3** | Δ**101.3** |
| ③ 디딤면 / 챌면 대비 | (단 없음) | Δ**24.3** | Δ**24.3** | Δ**24.3** |

> 게이트 자신이 밟은 함정 2개도 같이 고쳤다 — ⓐ «접합선 위 벽» 을 `gy−16..gy−4` 에서 재면
> **받침(바닥선 −16~+40)을 벽으로 착각**해 Δ0.0 이 나온다(받침보다 위인 `gy−76..gy−60` 으로 옮김).
> ⓑ `overflow:hidden` 은 `getBoundingClientRect` 를 안 줄이므로, 1600 에서 래퍼가 0px 인데도
> «단 4개 보임» 으로 셌다 — 래퍼와 겹치는 높이로 «보이는» 것을 세도록 고쳤다.

**`VERIFY120 114/114 PASS` · `VERIFY100 44/44 PASS` · `SMOKE PASS` · 콘솔 에러 0.**

### 12회차 지표 (`tools/scan120.js`)

| 지표 | 11회차 | **12회차** | 목표 |
|---|---|---|---|
| 24×24 블록 std<1 (1600 / 2280 / 2600) | 12.2 / 15.8 / 19.8% | **11.2 / 16.0 / 20.2%** | < 30 |
| 전폭 무특징 띠 최장 런 (1600 / 2280 / 2600) | 24 / 26 / 36px | **2 / 20 / 45px** | < 110 |
| 아치 안쪽 평균 휘도 | 24~34 | **21.8 ~ 30.4** | ≥ 22 |

### 12회차 채점

| | ① | ② | ③ | ④ | ⑤ | ①~④ 최저 |
|---|---|---|---|---|---|---|
| AD | 5 | 5 | 4 | 6 | 5 | **4** |
| AE | 3 | 3 | 3 | 7 | 4 | **3** |

**12회차 처방이 실제로 먹힌 것 (두 사람 독립 확인)**

| 항목 | 11회차 | 12회차 |
|---|---|---|
| 계단 «단 크기» 고정 | 면 5~8 → 58~66px (11.6배) | **면 26px · 피치 84px · 폭 5종 전부 4장 동일** (AD «진짜 개선») |
| 접합선 → 수반 | 13 → 346px (26.6배) | **32 / 64 / 64px** (AD) — 2배 |
| 상인방 | 1600 통째 실종 | **네 프레임 전부 존재** (1600 은 49px 노출) |
| 하단 여백 | 26 → 159px (6.1배) | **33 → 119px (3.46배)** |
| 계단 코 간격·중심 | — | AE «5개 전부 중심 537.5 · 간격 84px 균일 — 이 축은 우수» |

**남은 감점은 두 사람이 «각자 1순위» 로 같은 것을 짚었다 — 전부 1600 한 프레임에 몰려 있다.**

| # | 지적 | AD | AE |
|---|---|---|---|
| 1 | **1600 바닥 0px · 계단 0단 · 수반이 접합선을 관통** | ① 1순위 («벽에 박힌 수반») | ① 1순위 (밝은 줄이 x495~590 **96px 끊김**) |
| 2 | **1600 상인방이 패널 테두리에 잘림** | ⑦ (5px 중 2px) | ② (65px 중 **13px 잘림**) |
| 3 | **2280·2600 상단 283~288px 에 부재 0개** | ② | ③ (y300~400 가로 최대 대비 **3**) |
| 4 | **하단 여백 3.46~3.6배 표류** | ③ | ⑩ |
| 5 | **계단 단수 0 / 1 / 2 / 5 — 하한이 0** | ④ | ⑤ |
| 6 | 세로 중심축 산포 2.5px | ⑨ | ⑦ |
| 7 | **문간 내부가 사실상 검은 사각형**(Δ14~15 = 임계선) | — | ④ |

> **AD 와 나의 계측이 갈린 곳이 있다 — 그리고 AD 가 옳았다.**
> AD 는 «막계단 → 수반 64px» 이라 했고 내 게이트는 같은 자리를 **0.0px** 로 쟀다.
> 둘 다 맞다: rect 상 단은 84px 이고 밑변이 수반에 닿아 있지만, **칠해진 것은 위 26px 뿐**이고
> 나머지 58px 은 밑단 선이 없는 평면이라 **눈에는 «26px 슬래브 + 58px 바닥»** 으로 보인다.
> 05 의 «상자는 맞고 잉크가 어긋난» 유형이다. → 챌면 밑에 접지선을 그어 84px 이 한 단으로 닫히게 했다.

## 13회차 — 캡처 `120-r13-{1600,1920,2280,2600}.png`

**세로 예산을 «위에서 내려오며» 가 아니라 «아치를 가운데 두고» 다시 풀었다.**

12회차까지는 격자 top 을 먼저 정하고 남는 것으로 아래를 채웠다. 1600 은 **남는 것이 없어서**
바닥이 0px 이 됐고, 그 위에서 접합선·수반·상인방이 서로를 밀어냈다. 위·아래 최소 요구를
**동시에** 세우고 아치의 뻗음(`--rw-av`)을 그 예산에서 푼다:

```
위(격자 위) : 8 + 상인방 66 + 8 + av        아래(격자 밑) : av + 받침 40 + 접합선 13 + 계단 84
                        ⟹  av ≤ (T − 219) / 2      (T = 수반 top − 격자 하단)
gt = clamp(av + 82,  min(--rw-sp × .5075, 600),  T − av − 137)
```

| | 1600 | 1920 | 2280 | 2600 |
|---|---|---|---|---|
| `--rw-av`(아치 뻗음) | **100.5** | 186 | **186(불변)** | **186(불변)** |
| 아치 종횡비 | 589×717 = 1:1.22 | 1:1.51 | **1:1.51** | **1:1.51** |
| 격자 위 여백 | 249.7 → **182.5** | 412.1 → **381.4** | **594.8(불변)** | **600(불변)** |
| **바닥(접합선 → 수반)** | 0 → **97.0** | 65 → **97.0** | 195.9 | 508.0 |
| **보이는 단 수** | 0 → **1** | 1 | 3 | 5 |
| **상인방 노출 높이** | 49 → **66.0** | 66.0 | 66.0 | 66.0 |
| 상인방 위 패널 여백 | 0 → **8.0** | 81.4 | 294.8 | 300.0 |

**2280·2600 은 이번에도 픽셀 하나 안 움직였다.** 바뀐 것은 1600·1920 뿐이다.

> **아치를 짧은 프레임에서 «누르는» 것은 새 방향이 아니다.** 6회차부터 하던 것이고(1600 에서 800.6),
> 13회차는 그 눌림의 **기준**을 «수반 클리어런스 28» 에서 «바닥이 실제로 존재할 것» 으로 바꾼 것이다.
> 1회차에 반려된 ⓐ는 **긴 프레임에서 늘이는 것**(1:1.51 → 1:1.98)이라 방향이 반대다.

### 같이 고친 3건

1. **문간 내부 대비**(AE ④) — 켜 줄눈 알파 `.30` → `.62`. 아치 안쪽 평균 휘도 21.8 → **22.5~44.8**.
   이 작업에서 **일곱 번째** «넣었는데 안 보이는» 케이스였다.
2. **단 밑단 접지선** — 챌면 밑 6px 에 `#191409` + `#0A0805`. 84px 블록이 «한 단» 으로 닫힌다.
3. **세로 중심축**(AD ⑨·AE ⑦, 산포 2.5px) — 단 폭을 짝수로(842·798·754·710·666) 바꿔 중심을 540 에 정확히 놓았다.

### 13회차 게이트 — `tools/verify120.js` **114 → 120 항목**

신설 2종 · 갱신 2종:
* `③ 바닥(접합선 → 수반)이 최소 한 단 ≥ 84px` — **1600 의 0px 이 이 게이트다**
* `② 상인방 66px 온전 · 패널 안 8px 이상 · 아치 정점 위` — 12회차엔 «높이 > 0» 이라 49px 잘린 것을 통과시켰다
* ⑥ 여백 모델을 새 예산 식으로 교체 · 아치 정점을 «격자 top − 186» → **«격자 top − av»** 로 정정
  (짧은 프레임에서 아치가 눌리므로 186 고정은 틀린 값이었다 — 게이트가 상인방을 «정점 아래» 로 오판했다)

**`VERIFY120 120/120 PASS` · `VERIFY100 44/44 PASS` · `SMOKE PASS` · 콘솔 에러 0.**

### 13회차 지표

| 지표 | 12회차 | **13회차** | 목표 |
|---|---|---|---|
| 아치 안쪽 평균 휘도 (1600 / 2280 / 2600) | 28.3 / 21.8 / 29.4 | **43.9 / 22.5 / 29.8** | ≥ 22 |
| 24×24 블록 std<1 (1600 / 2280 / 2600) | 11.2 / 16.0 / 20.2% | **13.3 / 15.9 / 18.1%** | < 30 |
| 전폭 무특징 띠 최장 런 (1600 / 2280 / 2600) | 2 / 20 / 45px | **9 / 20 / 45px** | < 110 |

### 반영하지 않은 지적과 근거

* **격자 행 간격 26 vs 열 간격 96~99 (3.8배 이방성)** — AD ⑧·AE ③. **89 측정 확정 규격**(슬롯 151² · 행 피치 176)이고
  `verify120 ⑦`·`verify100` 이 둘 다 못 박고 있다. 120 은 «영역 채움» 작업이지 89 격자 재설계가 아니다.
* **격자 폭 895 > 아치 개구 560~589 (1.6배)** — AE ⑨. 아치 폭 589 는 측정표 확정치이고
  «아치를 넓혀라» 는 **1회차에 반려된 방향 ⓑ**(안쪽으로 키우면 1·3행 슬롯 x216 을 침범)와 같은 자리다.
* **코스트 알약 내용 7.5px 좌편향** — AE ⑥. **의도된 값**이다: 측정표가 «그룹 중심 x530 vs 알약 중심 x540» 이라
  `.rw-cost{padding-right:20px}` 로 10px 왼쪽에 놓았다. AE 실측 7.5px 은 그 10px 이다.
* **계단 최소 2단**(AD ④·AE ⑤) — 1600 에서 2단(168px)을 확보하려면 `av ≤ 58.5` 라 아치가 **589×633 = 1:1.07**
  (거의 반원)이 된다. 1단(84px) 보장이 아치 종횡비를 지키면서 갈 수 있는 한계다. 남은 문제로 등재한다.
* **하단 여백 3.46배** — 상한을 120 아래로 내리면 **2280(117.3)이 같이 움직인다.** 하한(34)을 올리면
  수반이 위로 올라가 1600 의 바닥 97px 을 도로 깎는다. 이번 라운드에서는 1600 바닥을 우선했다.
