# 13 상점 팝업 — 재화 탭 · 구현/비평 기록

- 측정표: `docs/measure/13-상점팝업재화탭.md` (이번 회차에 신규 측정)
- 캡처: `docs/shots/13-r1.png`(1회차) · `docs/shots/13-r2.png`(2회차)
- 껍데기: **(다) 딤·헤더·테두리 없는 전체화면 페이지** — 10 상점 소환 탭과 같은 `#shopw` 를 그대로 쓴다.
  «재화» 는 그 안의 **카테고리**라 오버레이를 새로 만들지 않고 `.shp-list` 의 스크롤 콘텐츠만 갈아끼웠다.

## 좌표계

```
콘텐츠 좌상단 = 레퍼런스 y192      → local y = ref y − 192
헤드리스 캡처(1080x1920) 대응      → capture y = ref y − 88   (스크롤 최상단)
```
콘텐츠 총 높이 **1809**(ref 192~2000, 마일리지 리본 꼬리 끝까지). 뷰포트는 1482~1509 이라
**약 300~320px 을 스크롤이 흡수**한다(측정표 §9). 어떤 요소도 축소하지 않았다.

## 구현한 것

| 구획 | local y | 내용 |
|---|---|---|
| 배너 아트 | 0~356 | 전출혈, 세로 그라디언트 5정지점(측정표 §1 행별 median 그대로) + 방사 광선 + 🎁·💎 근사 |
| `재화 상점` | 143 | 잉크 268x65, `#FFF8AA`, 외곽선 CSS 10px, 중심 x819.5(화면 중앙 아님) |
| `상품` 밴드 | 356~457 | 검정 8 + 2단 채움 85(`#7D5A44`/`#70513D`, 경계 44px) + 검정 8 |
| `광고 상품` 리본 | 503 | 몸통 627x92 r16 + 제비꼬리 2개(V 노치 14). 색 토큰만 바꿔 «마일리지» 리본에 재사용 |
| `⏱ 초기화: nH nM` | 583 | 검정 캡슐 400x56 r28. 실제 일일 초기화까지 남은 시간을 계산해 표시 |
| 상품 카드 3x2 | 689 / 1008 | 278x309, 열 x111/401/691, gap 12/10, 검정 7(오버레이), r35 |
| 평생 광고 제거 | 1374 | 948x249 r20, 단색 `#43BCF5` + 45° 마름모 무늬, 우측 반원 노치 3개, [이동] 269x118 |
| 마일리지 상품 리본 | 1698 | 광고 상품 리본과 **동일 도형**, 보라 토큰 |

### 기능 연결 (레퍼런스 화면을 이 게임에 매핑)

- **광고 상품 6칸** = 하루 n회 무료 수령. `S.daily.adBuy` 에 횟수를 쌓고 **없는 키는 cap 으로 폴백**해
  세이브 마이그레이션 없이 동작한다(`dailyCheck()` 에서 날짜가 바뀌면 초기화).
  보석→💎100 · 공물→🔮50 · 골드 상자→🪙(스테이지 수입 200배) · 상자 열쇠→🔮10 · 강화석→🔮30 ·
  동료 소환 열쇠→펫 무료 소환 1회. 전부 일일 제한이라 룰렛·출석과 같은 급이며 밸런스 영향 없음.
- 수령 횟수가 0이 되면 그 칸이 **구매 완료**(오버레이 `rgba(0,0,0,.335)` + 띠 `rgba(0,0,0,.67)`)로 바뀐다 —
  레퍼런스 ① 칸이 정확히 그 상태다.
- **기존 기능 보존**: 원래 «재화» 탭은 기존 패널 상점(다이아 패키지·아바타·유물석 교환소)으로 튕겼다.
  이제는 진짜 페이지를 그리고, 그 기능들은 «평생 광고 제거» 배너의 **[이동] 버튼**으로 들어간다.

## 1회차 비평 — A **4** / B **5** (최저 4)

| 지적 | 판정 | 조치 |
|---|---|---|
| 스크롤 콘텐츠 전체가 **+59px** 아래 | **진짜 버그** | 아래 «병합 사고» 참조 |
| 카테고리 탭 바가 27px 아래, 라벨이 바 중심보다 17.5px 아래 | 진짜 | 재화 탭 스코프로 덮어씀 |
| `NEW` 리본이 있다(13 레퍼런스엔 없음) | 진짜 | 재화 탭에서 숨김 |
| ✦ 마름모가 2개·13x18 (ref 3개·19x19) | 진짜 | 가운데 ✦ 신설 + 크기·좌표 보정 |
| 카드⑥ 헤더가 카드 우단에서 **클리핑** | 진짜 | 헤더 박스를 advance 보다 넓게(-30px) |
| 카드① `×100` 이 클리핑 | 진짜 | 구매 완료 칸에서는 `×N` 을 숨김(ref 도 띠에 가려 안 보인다) |
| 카드 본문/배너 그라디언트 색이 10% 어긋남 | 진짜 | 측정표 정지점대로 재작성 |
| 탭 라벨 잉크가 6~16% 좁다 | 진짜 | 라벨 fs 33→36, `일일/주간` scaleX .98 |

### 병합 사고 — **다른 세션과 같은 줄에 블록을 추가하면 주석 여는 줄이 사라질 수 있다**

1회차 지적의 절반(+59px 밀림)은 CSS 값이 아니라 **병합 사고**였다.
14번 세션과 내가 **같은 위치(`.shp-soon` 다음)에 각자 CSS 블록을 추가**했고, 두 블록 모두
`  /* ==========================================================================` 라는 **똑같은 첫 줄**로
시작했다. rebase 가 그 줄을 공통 context 로 보고 **한 번만 남기는 바람에** 내 블록의 여는 `/*` 가 사라졌고,
CSS 파서는 주석 본문을 셀렉터로 읽다가 **바로 다음 규칙 `.shp-list.coin{padding-top:0}` 을 통째로 삼켰다.**
그 한 줄이 죽어 상단 패딩 59px 이 남아 콘텐츠 전체가 59px 아래로 밀렸다.

- **문법 오류가 아니라서 콘솔 에러 0건이었다.** 헤드리스 실행·기능 테스트도 전부 통과했다.
- 자가 실측도 `getBoundingClientRect()` 기준이라 **요소끼리는 전부 정상**으로 나왔다(다 같이 밀렸으므로).
  잡아낸 것은 **캡처 픽셀을 프레임 절대좌표로 검증한 비평가 2명**이었다.
- 교훈 3가지:
  1. 병합 후에는 **CSSOM 에 내 규칙이 살아 있는지** 확인할 것 —
     `document.styleSheets` 를 훑어 `selectorText` 로 내 셀렉터를 찾으면 1초면 끝난다.
  2. 자가 검증을 `getBoundingClientRect` 로만 하면 **전체가 평행 이동한 오차는 절대 안 잡힌다.**
     캡처 PNG 에서 **프레임 절대 y** 를 한 번은 재라(예: «배너 파랑이 시작하는 행 = 104 인가»).
  3. 공용 파일에 블록을 추가할 때 **첫 줄을 남과 똑같이 쓰지 마라**(블록 제목을 첫 줄에 넣는 편이 안전하다).

## 3회차 비평 — A **6** / B **6** (최저 6) · 캡처 `13-r3.png`

A(①6 ②6 ③9 ④7 ⑤6 ⑥6) / B(①7 ②8 ③9 ④9 ⑤6 ⑥9). 공통 최대 감점원 2개가 **화면 상태**였다:

| 지적 | 판정 | 조치 |
|---|---|---|
| 하단 nav 가 5버튼+NEW리본 5개 (ref 4버튼+✕ 295px 다크존+점배지 3개) | **진짜 — 상태 미발동** | `openShopPage()` 가 A1 의 `.tab.close`(296px, `#181818` — ref 295px `#151515~181818` 와 일치)를 토글하도록 `syncShopNav()` 신설. NEW 리본은 `S.seen` 상태 문제라 캡처 주입으로 해소 |
| HUD 프로필 블록(방패·ID·브론즈) 노출, ref 는 민바+알약 2개 | 진짜 | `#app:has(#shopw.on .shp-list.coin)` 스코프로 `.pedge/.plate/.prof` 숨김 + `.curs{right:37;gap:85}` (ref 알약 x537~745/838~1044) — **10 소환 탭·02 메인은 영향 없음** |
| A: ×N 잉크 «+34% 과대·순수 검정» | **반증** | 자체 스캔(흰채움>238 동일 기준): ref 79×29 vs 구현 81×31 — Δ2. A 는 구현만 외곽선 포함으로 잰 것 |
| B: 카드⑤ 제목 «폭 −7%» / 탭바 좌단 «x45→54» | **반증** | 폭 ref 237 vs 238 (Δ1) / 좌단 검정 시작 ref x46 vs cap x45 (Δ1) |
| 헤더·×N·(1/1) 잉크 **높이만 +9~14%** (폭은 전부 Δ≤2) | **진짜 — 서체 종횡비** | 카드별 fs ×(ref_h/cap_h), sx ×역수로 폭 보존. `.qt` fs 44→39.7(top 172→169), `.cnt` fs 24→22.4·sx 1.16→1.065. 수정 후 전 항목 **Δ≤2 수렴** 확인 |
| B: 배너 방사광선 — ref 에 없음 | 진짜 | `.ray` 마크업 제거(코인 배너만) |
| 받기 face 상단 +5 밝음/하단 −5 어두움 | 진짜(미세) | 그라 정지점 `#3FD5ED→#3AD0E8`, `#19AFD0→#1EB4D5` |
| 카드 아이콘 4/5 주조색 반전(금↔은 등)·배너 픽셀아트·M 메달 | 아트 한계 | 이모지 대체 천장 — A1·A3·03·05 와 동일 |

- **회귀 테스트**: 상점 열기→✕ 클릭 닫기→영웅 패널 열기/닫기→상점 열린 채 성장 탭 전환, 전부 정상·콘솔 에러 0.
- **교훈**: 전체화면 페이지는 «페이지 자신» 뿐 아니라 **주변 크롬(탭바·HUD)의 상태**까지 레퍼런스와 맞춰야 한다.
  A1 이 만들어 둔 ✕ 칸(296px)은 ref ✕ 존(295px)과 1px 차이로 정확했다 — 발동만 안 되고 있었다.

## 4회차 비평 — C **6** / D **7** (최저 6) · 캡처 `13-r4.png`

C(①8 ②6 ③8 ④9 ⑤8 ⑥6) / D(①7 ②9 ③9 ④9 ⑤8 ⑥8). 서로 반대 방향 지적이 많아 **자체 스캔으로 판정**했다:

| 지적 | 판정 | 조치 |
|---|---|---|
| D: 완료카드에 ×100 이 없다 (ref 는 띠 아래로 회색 잉크가 비침) | **진짜** — ref (283,1070) w89 h9 확인 | 완료 칸도 `×N` 을 항상 렌더(딤 아래 z순서) — 띠 아래 ~11px 비침 재현 |
| D: 완료카드에 ref 에 없는 청색 내부 테두리 | 진짜 — 파랑 몸통이 슬리버로 비침 | `.cn-cd.done>.bg{background:#1C8D5E}` (ref #135E3F = ×0.667) |
| C·D: '재화' 활성 라벨 폭 −12~13% (62 vs 54) | **진짜** | fs 31.2→29.5 + scaleX 1.06 → 62×27 (ref 62×26) |
| C: 활성 알약 fill 240 vs 252, 좌변 795 vs 784 | 진짜(B 246 과 절충) | left −6→1, width 264→256 |
| D: NEW 리본 잔존 + 레드닷 0개 | 진짜 — 캡처 상태 미갱신 | 캡처 주입에 `.fresh` 재계산 + 레드닷 3개 상태(S.sp·collReady·DUNGEONS[0].req) 추가 |
| C: HUD 아이콘 +20%·중심 Δ18, 알약 좌변 ref 541/844 | 진짜 | coin 스코프 right 31·gap 89(알약 538/838), 아이콘 fs 52→46·59→52.5, left −54(중심=알약좌변−22.5) |
| C: 평생 광고 제거 잉크 y Δ10 | 진짜 | `.cn-a2t` top 1402→1392, 그라 상단 #F8EBB4→#F3DC96(C 금색 마스크 보정) |
| C: ×50 h −7% (29 vs 27) | 진짜(미세) | `.qt` fs 39.7→41.2, qx ×0.966 → 82×28 |
| D: '보석' 헤더 명암 반전 (ref 암색) | **반증** — ref 히스토그램: 검정 외곽선+회색160 잉크 = 우리 (170,170,169)와 동일 | 미조치 |
| D: ×50 «−11~12.5% 과소» ↔ C «−7%» ↔ 3회차 A «+34% 과대» | 마스크 기준 차이 — 엄격 동일 마스크로는 Δ≤2 | fs +1.5 만 |
| C: 카드 헤더 −9%(>200 마스크) / C: (1/1)·받기 +9~10% | 반증 — >238 동일 마스크로 Δ≤2 (B·D 도 일치 판정) | 미조치 |
| C↔D 배너 그라 하단 방향 상반(C: ref 어두움 / D: ref 밝음) | 보석더미 아트의 공간 분산 = 노이즈 바닥 | 아트 한계로 분류 |

## 다른 작업 단위와의 경계

- **카테고리 탭 바(`.shp-cats`)는 10 소속**이다. 13 실측치와 어긋나는 곳이 4군데 있었지만
  10 의 화면을 바꾸지 않으려고 **`#shopList.coin ~ #shopCats ...` 스코프로만** 덮어썼다.
  10 담당이 자기 레퍼런스를 다시 재 보고 같은 값이면 전역으로 올리면 된다:
  `bottom:14→41` · `.shp-ct>i{top:41→23.5;font-size:33→36}` · `.shp-cs{15x20→19x19, left 239/485/732}`.
- 가운데 ✦(`.shp-cs.s2`)를 마크업에 **추가**했지만 재화 탭에서만 보이게 했다(10 레퍼런스는 NEW 리본이 덮는다).
- 활성 알약은 **활성 탭 칸 안으로 노드째 옮긴다**(`insertBefore`). 4번째 칸일 때의 규격
  (검정 포함 264x76, 바 우측 7px 돌출)은 `:nth-child(4)` 로만 준다 — 1번째 칸(소환)은 10 의 값 그대로.
