# 464 — 죽은 껍데기 클래스 `rl16` 정리 (T1 «버그(죽은 식별자)»)

- 세션 `sess-1159-10079` (클라우드 루틴 워커 D) · 2026-08-30 · **1회차로 종결**
- 지시서 [3]-**(가) 기계적 작업** — 레퍼런스 대조가 아니라 «식별자·상자» 판정이라 **비평가를 안 띄웠다.**
- 제품 `index.html` **5자리**(remove 목록) + 주석 2곳 · **레이아웃 Δ0.00px** · 판정·밸런스 0줄.

## 1. 등재문 (PROGRESS 464, sess-1107-9632 워커 E — 455 재현 곁다리)

> `remove()` 목록 5곳과 주석에만 있고 `classList.add('rl16')` 하는 곳이 저장소에 없다.
> 455 가 등재문 처방대로 `#modal.rl16` 으로 «지금 룰렛인가» 를 물었더니 판정이 **영원히 거짓**이라
> 가드가 한 번도 안 걸렸다. 처방 ⓐ = 다섯 목록에서 걷어내고 `verify455` [A5] 를 같이 갱신한다.

## 2. 재현 — `tools/probe464.js` 신설 (338·341·350 규칙: 처방 전에 제품에게 직접 묻는다)

**등재문은 기각이 아니라 확인**이다(338·341 은 여기서 기각됐던 자리다). 수리 전 실행값:

| 축 | 물은 것 | 값 |
|---|---|---|
| [1-a] | `classList.add(…rl16)` 제품 줄 | **0건** |
| [1-b] | `remove(…rl16)` 제품 줄 | **5건**(트리에 따라 갈리는 유일한 축) |
| [1-c/2-a] | `.rl16` 를 쓰는 **CSS 규칙** (소스 · CSSOM 전수) | **0건 / 0 중 2650규칙** |
| [2-b] | 대조 — 살아 있는 넷의 규칙 수 | sk8 6 · q22 4 · ml69 9 · at70 7 |
| [3] | 껍데기 5종을 실제로 열어 본 `#modal.className` | `rl16` **0회 등장** (룰렛은 `class="on"` 뿐) |
| [4-a] | 열린 모달에 손으로 `rl16` 을 붙인 프레임의 `#modal`·`.mbox` bbox | **Δ0.00px** |
| [5-a] | 345 `jzShellBack()` 축에서의 분류 | `display:none` ⇒ «껍데기» 로 분류되는 **빈 껍데기** |

⇒ **[4-a] 가 이 작업의 안전 근거다** — 이름이 나르는 선언이 하나도 없으므로 다섯 곳의 `remove()` 는
**이미 no-op** 이었고, 걷어내는 것은 산술적으로 그림을 못 바꾼다. `verify464` [D] 가 그것을
«수리 전 사본과 레이아웃 상자가 정수까지 같다» 로 다시 못박는다.

⚠ **1회차 함정 둘을 재현 자체가 잡았다**
- **CSSOM 오독** — «`cssRules` 가 있으면 컨테이너» 로 갈랐더니 전 규칙이 0건으로 읽혔다.
  지금 크롬은 중첩(nesting) 때문에 **평범한 `CSSStyleRule` 에도 빈 `cssRules`** 가 달려 있다.
  선택자를 **먼저** 세고 자식은 «길이가 있을 때만» 내려가야 한다. **[2-b] 대조군이 이 오독을 잡았다** —
  대조군이 없었으면 «규칙 0건» 을 그대로 결론으로 썼을 것이다(자가 없어서 0인지 진짜 0인지 못 가른다).
- **연출이 섞인 상자** — `getBoundingClientRect` 만 보면 60 쥬시 열림 연출(scale)이 섞여 두 트리가
  서로 다른 프레임에서 찍힌다(실측 q22 902.69×1504.81 ↔ 898×1497 = 연출 0.5% 지점, Δ최대 **20.12px**).
  ⇒ 잣대는 **레이아웃 상자**(`offset*` — transform 무관, `verify345` 가 쓰는 축)로 잡고, 그려진 상자는
  «두 표본이 같아질 때까지» 기다려 참고로만 찍는다.

## 3. 수리 — 제품 5자리

| 자리 | 함수 | 전 → 후 |
|---|---|---|
| ~24837 | `closeModal()` | `remove('on','sk8','rl16','q22','ml69','at70')` → `remove('on','sk8','q22','ml69','at70')` |
| ~24843 | `showModal()` | `remove('sk8','rl16','q22','ml69','at70')` → `remove('sk8','q22','ml69','at70')` (주석 «08·16·22·69·70» → «08·22·69·70») |
| ~25676 | `openAttend()` | `remove('sk8','rl16','q22')` → `remove('sk8','q22')` |
| ~25982 | `openMail()` | `remove('sk8','rl16','q22')` → `remove('sk8','q22')` |
| ~26113 | `openQuest()` | `remove('sk8','rl16','ml69')` → `remove('sk8','ml69')` |

**자리를 비우지 않았다(333)** — `closeModal()` 머리 주석과 345 주석에 «그 이름은 죽은 이름이었고 464 가
걷어냈다 · 되살리려면 `add` 하는 곳을 먼저 만들라» 를 남겼다. `verify464` [A5] 가 그 기록의 존재를 단언한다.

## 4. 게이트

- **`tools/verify464.js` 25/25 신설**
  - [A] 폐기 식별자 — 주석을 걷어낸 제품 줄에 `rl16` 0건 · `add` 0건 · `remove(…rl16)` 0건 · `.rl16` CSS 0건 ·
    **[A5] 주석·기록에는 남아 있다**(다음 세션이 «룰렛 전용 껍데기가 있다» 고 다시 읽지 않도록).
  - [B] **무르게 풀지 않았다** — 다섯 자리의 목록이 «rl16 만 빠진» 그대로다(살아 있는 넷을 같이 흘리면 빨감).
    ⚠ 자를 «껍데기 목록» 으로 좁혔다 — `remove('on'` 을 통째로 세면 **34건**이다(오버레이 스물아홉 개가
    각자 자기 `on` 을 뗀다 · `verify455` [E] 가 적어 둔 같은 함정).
  - [C] 실동작 — 껍데기 4종이 열릴 때 붙고 `closeModal()` 이 전부 뗀다 · **룰렛은 껍데기 없이 열린다**(`class="on"`).
  - [D]+[R] **되돌림 시험** — `rl16` 을 다섯 목록에 도로 끼운 사본에서 [A3] 이 빨개지고([R1]),
    그 사본이 곧 «수리 전 트리» 라 껍데기 4종 `.mbox` 레이아웃 상자를 맞대 **Δmax 0.00px** 를 잰다.
- **이관 `tools/verify455.js` 32/32** — [A5] 를 «add 0건» → «**제품 줄 rl16 0건** + add 0건» 으로 갈아 끼웠다.
  «add 0건» 만 물으면 **목록에 이름만 도로 끼워 넣는 것을 못 본다**(334 «무르게 푼 수리» 방지).
  [E1a] 의 껍데기 목록 정규식도 rl16 을 뺀 형태로 이관.
- 대조군 주석 이관 4건(`verify345`·`probe345`·`probe392`·`probe455`) — 이 자들은 «룰렛에 껍데기가 없다» 를
  재는 대조 표본이라 **판정은 그대로 초록**이고, 이름이 삭제됐다는 사실만 주석으로 남겼다.
- 회귀 무수정: `verify345` **105/105** · `verify410` 22/22 · `probe345` · `probe392` 6/6 · **SMOKE PASS**.

## 5. 곁다리 관측 — **465 등재(잠복)**: 다섯 목록이 서로 비대칭이다

`probe464` [6] 실측 — 닫지 않고 갈아타면 껍데기가 **겹쳐 남는다**:

| 경로 | 결과 클래스 | 남는 껍데기 |
|---|---|---|
| 우편(ml69) → 출석(at70) | `ml69 on at70` | `ml69` (openAttend 는 ml69 를 안 뗀다) |
| 출석(at70) → 우편(ml69) | `ml69 at70 on` | `at70` (openMail 은 at70 을 안 뗀다) |
| 출석(at70) → 퀘스트(q22) | `at70 on q22` | `at70` (openQuest 는 at70 을 안 뗀다) |

**«실해» 로 안 적는다 — [6-b] 가 포인터 경로를 직접 쟀다**: 모달이 열려 있는 동안 사이드 «출석»·«퀘스트»·
▦ 메뉴 버튼은 `elementFromPoint` 가 전부 **`modal`** 로 떨어진다(딤이 전면을 덮는다) ⇒ 사람이 밟을 수 있는
길이 없다 = **잠복**(463 «잠복» 규약과 같은 판정). 뿌리는 LESSONS 265 가 이미 적어 둔 것 —
«그 목록은 이미 다섯 곳에 흩어져 낡는다». 내 작업 단위가 아니라 **안 고쳤다**(PROGRESS 465 행).

## 6. 다음 세션이 볼 것

- **폐기 식별자를 지울 때 «그림이 안 바뀐다» 를 말로 하지 마라** — 이름이 나르는 **CSS 규칙 수를 세고**
  (CSSOM 전수 · 대조군 필수), 열린 상자에 **붙였다 떼 Δ0px** 를 찍으면 한 줄로 끝난다.
- **두 트리의 상자를 맞댈 때는 레이아웃 상자로 재라** — 60 쥬시 열림 연출이 그려진 상자에 최대 20px 를 얹는다.
