# 843 — `tools/verify351.js` §8 「20 스펙」 2건이 굳어 빨간 채 방치 (84/86)

**세션** sess-2214-18813 · 루틴 워커 A · 2026-09-02 · **1회차**
**티어** T1 «버그(게이트 부패)» · 지시서 [3]-**(가)** 기계+자 — 비평가 없음
**제품 `index.html` 0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄. 고친 것은 `tools/verify351.js` 한 파일이다.

---

## 1. 착수 재현 (338 규칙 — 처방 전에 재현)

`node tools/verify351.js` → **VERIFY351 84/86 FAIL**, 등재문 그대로 두 항:

```
NG  [8-d] 1600 패널이 눌리면 그만큼 리스트가 줄어든다 — 패널 1396→1296 · 리스트 760→640
NG  [8-g] 1600 리스트↔탭 줄 간격 = 2280 과 같다 — 58 (기대 38±1)
```

## 2. 갈래를 가른 것 — `tools/probe843.js` 신설 (9/9 PASS)

등재문이 남긴 갈래는 둘이었다.

* ⓐ 리스트 높이가 «패널 − 상수» 여야 하는데 **제품 산수가 틀렸다** → 제품을 고친다
* ⓑ 리스트가 **행 피치 배수로 내림 스냅**된다(812) → 눌린 20px 이 리스트가 아니라 간격으로 가는 것이
  정상 동작이고, **자를 스냅 잔차만큼 허용하도록 이관**한다(333 처방)

가르는 방법은 «찍힌 값» 하나다 — 같은 트리에서 **812 의 스냅 블록만 걷어낸 사본**과 현행을 나란히 잰다.

| 항 | 값 |
|---|---|
| 현행 1600 | 패널 1396 → **1296**(−100) · 리스트 760 → **640**(−120) · 간격 38.5 → **58.5**(Δ +20) |
| [P3] 한 뿌리의 앞뒤 | 리스트가 더 준 **20** = 간격이 벌어진 **20** (정확히 같다) |
| [P4] 스냅 항등식 1600 | **640 = 23(패딩) + 60×10(온전한 행) + 17(ref 슬라이버)** |
| [P5] 스냅 항등식 2280 | **760 = 23 + 60×12 + 17** |
| [P6] 스냅 걷어낸 사본 1600 | 리스트 760 → **661**(−99) ⇒ **[8-d] 초록으로 복귀** |
| [P7] 〃 | 간격 **37.5** ≈ 2280 의 38.5 ⇒ **[8-g] 초록으로 복귀** |
| [P8] 음성항 | 스냅 유무와 무관하게 2280 리스트는 **760 그대로**(min 이 상수 쪽을 고른다) |

⇒ **갈래 ⓑ 확정.** 두 항이 재고 있던 것은 결함이 아니라 **812 그 자체**였다.
812 를 넣은 커밋이 이 자를 안 이관해 굳은 자리이고, **제품은 내내 옳았다**.
이 진단은 `index.html` 10520~10545 의 812 주석이 이미 문장으로 적어 두고 있었다 —
«버리는 21px 은 리스트↔탭 간격이 받는다(37.5 → 58.5)».

⚠ **등재문이 «내 변경과 무관» 을 이미 대조로 못박아 뒀다**(821 수리 전 트리에서도 같은 84/86).
그래서 이번 회차는 그 대조를 다시 하지 않고 **갈래 판정에만** 재현 예산을 썼다.

## 3. 이관 (333 처방 — 항을 눌러 초록으로 만들지 않는다)

**축을 한 곳에 모았다.** `spcAxis()`(패널·리스트·간격의 두 프레임 차를 한 번에 계산) +
`spcAbsorbs()` + `spcGapOk()` 를 모듈 상단에 두고 **§8 과 §R 이 같은 식을 읽게** 했다 —
안 그러면 «본항은 이관했는데 되돌림 시험은 옛 식» 이 된다.

| 항 | 옛 질문 | 새 질문 |
|---|---|---|
| **[8-d]** | 리스트가 패널보다 **더** 줄면 빨강(−1 허용) | 패널이 눌리고 · 리스트가 그 눌린 양을 흡수하고 · **넘치는 것은 내림 스냅 잔차 한 행 미만**(−2 ≤ 잔차 < 피치) |
| **[8-g]** | 1600 간격 = 2280 간격(±1) | **줄어드는 방향만 막는다**(간격이 준다 = 리스트가 탭 줄을 먹는다) · 벌어짐 < 한 행 · **벌어진 양 = [8-d] 의 잔차**(한 뿌리의 앞뒤) |
| **[8-c2]** 신설 | — | 2280 간격 **38 불변**(9:19 Δ0 — 351 7회차의 «596 = 443.5 + 38.5 + 94 + 20» 예산의 한 칸) |

⚑ **행 피치를 손으로 안 적었다.** 상한이 되는 60 은 `box('.spc-row')` 로 **제품에게 묻는다** —
손 상수로 적어 두면 812 가 피치를 바꾼 날 이 자만 옛 세계에 남는다.

⚑ **잔차의 «정체» 는 여기서 다시 묻지 않는다.** «패딩 + 온전한 행 n + ref 슬라이버» 라는 항등식과
«버리는 양 < 한 행», «`--spc-slv` 선언 한 벌» 은 이미 `tools/verify812.js` [B][C][E] 가 든다.
같은 것을 두 자가 적으면 한쪽만 늙는다 — 여기서는 **상한만** 든다.

## 4. 무르게 풀지 않았음 — 되돌림 시험 2건 신설

허용치를 넓힌 자와 구별이 안 되면 이관이 아니다(338 교훈). 사본 둘이 못박는다.

* **[R-l]** 기존 `REVERT`(리스트를 상수 760 으로 되돌린다 = 흡수 구조 자체가 사라진다)에서
  잔차가 **−100**(허용 −2..59) ⇒ **[8-d] 축이 빨개진다.**
* **[R-m]** 신설 사본 `EATGAP`(`#app.shortf .spc-list{height:calc(100% - 520px)}` — 흡수는 하되
  그 대가로 탭 줄을 먹는다)에서 1600 간격이 **−39**(2280 은 38 그대로) ⇒ **[8-g] 축이 빨개진다.**
  ⚠ 사본을 **짧은 프레임에만** 건 것이 핵심이다 — 두 프레임을 같이 밀면 상대 비교가 초록이라
  되돌림 시험이 아무것도 증명하지 않는다.

## 5. 결과

```
VERIFY351  84/86 FAIL → 89/89 PASS   (+3 항: [8-c2] 신설 · [R-l] · [R-m])
PROBE843   9/9 PASS  (신설)
```

회귀 — 전부 **무수정**:

| 자 | 결과 |
|---|---|
| `tools/verify812.js` | **35/35 PASS** (스냅의 주인 — 이관이 812 를 안 건드렸다) |
| `tools/verify705.js` | **53/53 PASS** ([8-f] 의 짝 [C4] 를 드는 자) |
| `tools/verify20.js` | **34/34 PASS** (20 스펙 화면 본체) |
| `node tools/smoke.js` | **SMOKE PASS** |

## 6. 다음 세션이 볼 것

* **812·827 과 한 계열이라는 등재문의 짐작은 절반만 맞았다.** 827(22 퀘스트 마지막 행 잘림)은
  **아직 스냅이 없는** 자리이고, 여기 843 은 **스냅이 이미 있는데 자가 안 따라온 자리**였다.
  827 을 잡는 세션은 `.qs-pn` 에 같은 내림 스냅을 넣을 때 **그 화면의 자들도 같이 이관**해야 한다 —
  스냅은 «버린 양» 을 반드시 어딘가로 보내고, 그 어딘가를 상수로 굳혀 둔 자가 그날 빨개진다.
  이 회차가 남긴 `spcAxis`/`spcAbsorbs`/`spcGapOk` 세 줄이 그 꼴의 **선례**다.
* **곁다리 등재 0건.** §1~§9·§R 전 절을 훑었고 다른 빨강·헛초록은 없었다.
