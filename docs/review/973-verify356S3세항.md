# 973 — `tools/verify356.js` [S3] 세 항 실패 (게이트 부패 · T1)

> 선점 `sess-0457-16431` (루틴 워커 D, 2026-09-06) · 등재는 966 1회차(sess-0228-32153 워커 B)

## §0 등재문이 남긴 것

| 항 | 실측 | 눈금 |
|---|---|---|
| [S3] ② 스윕 | 판정 노드 **428** | `JUDGE_MIN` **480** 이상이어야 한다 |
| [S3] ③ 래칫(자리) | 자리 **23** | `RATCHET_SITES` **22** |
| [S3] ③ 이름표 | 등재 안 된 자리 **2** | `KNOWN_SITES` 에 없는 자리 0 |

등재문이 못박은 것 — **966 의 변경과 무관**하다(`.pvb` 두 값을 되돌린 사본으로 통째로 다시 돌려
점수·항목·수가 글자 그대로 같았다). 등재 안 된 두 자리는 **배너형 이용권 카드의 리본 아이콘**
(`div#shopList>div.cn-wrap.pv>div.pvc.pb.ban1>div.rb.rb1>b>img.cic` · 같은 꼴 `.rb2`).

등재문이 남긴 갈래 — **② 와 ③ 은 방향이 반대다**(② 는 «스코프가 줄었다» · ③ 은 «자리가 늘었다»).
한 뿌리인지부터 갈라야 한다.

## §1 1회차 — 재현 (수리 전 완주, 이 컨테이너)

`node tools/verify356.js` 완주 → **`VERIFY356 260/263 FAIL`** — 966 의 두 실행과 **총점·항목·수가
글자까지 같다.**

```
  ✓ [S3] ② 스윕 — 화면 71개 전부 진입
  ✗ [S3] ② 스윕 — 판정한 노드가 428개뿐(≥480 이어야 한다): 스코프가 줄었다
  ✓ [S3] ② 스윕 — 연출 정규화 … (잔여 0)
  ✓ [S3] ③ 래칫 — 칸 59개 ≤ 60
  ✗ [S3] ③ 래칫 — 자리가 23개(래칫 22): …
  ✗ [S3] ③ 이름표 — 등재 안 된 자리 2개:
        div#shopList>div.cn-wrap.pv>div.pvc.pb.ban1>div.rb.rb1>b>img.cic
        div#shopList>div.cn-wrap.pv>div.pvc.pb.ban1>div.rb.rb2>b>img.cic
  ✓ [S3] ③ 눈금 — 상한이 붙은 12자리 전부 상한 안
  ✓ [S3] ③ 눈금 [전제] · §R 되돌림 — 초록
```

⚑ **세 항은 한 뿌리가 아니다.** 자리 목록 23개를 `KNOWN_SITES` 와 맞춰 보면
**21 은 등재된 자리 그대로**이고 늘어난 둘이 정확히 이름표가 짚은 그 둘이다 ⇒
**③ 두 항(래칫·이름표)은 한 뿌리**(자리가 21 → 23)이고, **② 는 별개 축**이다.
그리고 «칸» 은 59 ≤ 60 으로 **초록** — 자리만 늘고 칸은 안 늘었다.

## §2 ③ 의 뿌리 — 885 10회차가 아이콘 상자를 정수에서 소수로 옮겼다

`git log -S`(얕은 클론을 `--deepen=640` 으로 판 뒤)가 한 커밋을 짚는다 —
**`73368fb wip(885): 9회차 채점 + 10회차 수리` (2026-09-05)**:

```
-  #shopw .pvc.ban1>.rb>b{width:79px;height:79px;top:-19px;border-radius:17px}
-  .pvc.ban1>.rb>b>.cic{width:52px;height:52px}
+  #shopw .pvc.ban1>.rb>b{width:81.6px;height:81.6px;top:-20.3px;border-radius:17.6px}
+  .pvc.ban1>.rb>b>.cic{width:53.7px;height:53.7px}
```

판을 등방으로 79 → 81.6 으로 키우며 «반지름·아이콘도 같은 비» 로 아이콘이 **52(정수) → 53.71(소수)**
가 됐다. **그것이 [S3] 이 잡으라고 있는 결함 그 자체**다(418 — «소수 상자가 만든 찌그러짐»).

표적 재현 — `node tools/probe418.js --screen "124 이용권"` (2화면 · 13노드):

| | 자리 | 값 |
|---|---|---|
| 수리 전 | **3자리** | `.rb1>b>.cic` **+1.89%** (잉크 108×106 · 상자 **53.6875** @ 375.3594,868.6563)<br>`.rb2>b>.cic` **+1.89%** (같은 값 @ 464.3594,986.6563)<br>`#shopw .pcb-p.pcb-d>i>.cic` −2.27%(등재된 자리) |
| `PROBE418_CSS` 로 **54** 주입 | **1자리** | 두 자리가 **통째로 사라진다** |
| `PROBE418_CSS` 로 **53** 주입 | **1자리** | 마찬가지로 사라진다 |

⇒ **418 §5 방식의 그 자리 시험**이 답을 갈랐다: 가르는 것은 «값» 이 아니라 **«정수인가»** 다.
소수 상자 53.6875 가 DSF2 에서 **가로만 1화소 더 먹어**(108 vs 106) +1.89% 가 된다.

**대조군이 같은 부품 안에 있다** — 불릿형 `.pvc>.rb>b>.cic{width:60px}`(정수)는
[S3] 에 **한 번도** 안 걸렸다. 같은 리본·같은 판·같은 그림인데 상자만 정수다.

### 처방 — 아이콘 상자만 정수로(53.7 → **54**), 판은 한 글자도 안 건드린다

- 885 의 의도는 «판 × 52/79» = 81.6 × 0.65823 = **53.71** 이다 ⇒ 가장 가까운 정수는 **54**
  (+0.29px · **+0.54%**). 53 은 −0.71px · −1.3% 로 **두 배 멀다**.
- **판(`b`)은 안 건드린다** — 81.6 · `top:-20.3` 은 885 10회차가 3인 채점(GC·GD·GE)으로 세운 값이고
  `tools/verify833.js` **[7-e]** 가 `|w − 81.6| < 0.3` 으로 물고 있다(82 로 올리면 그 자가 빨개진다).
- 아이콘 상자 53.7 을 **무는 자는 저장소에 없다**(`grep -rn "53\.7" tools/` = 0건).

⚠ **«이름표에 두 줄 더해서 닫는» 길은 여기서 막혀 있다.** 등재문도 그것을 경계했고, 산수도 같은 말을
한다 — 이름표만 채우면 `[S3] ③ 래칫` 이 **자리 23 > 22 로 그대로 빨갛다.** 래칫은 규약상
**줄이는 쪽으로만** 고치므로(601·12회차) 22 를 24 로 올리는 것은 «늘려서 초록을 만드는» 짓이다.
⇒ **자리를 실제로 닫아 21 로 되돌리는 것**이 이 항의 유일한 정답이다.

### ③ 수리 확인 — 자리 23 → **21**, 칸 59 → **57**

수리 뒤 전 화면 스윕(`node tools/probe973.js`) — **832 가 초록으로 적어 둔 값과 소수점까지 같다**:

| | 832(2026-09-03 · 초록) | 착수(수리 전) | **수리 후** |
|---|---|---|---|
| 칸 | 57 | 59 | **57** |
| 자리 | 21 | 23 | **21** |
| 이름표 | 21/21 등재 | 등재 안 된 자리 2 | **21/21 등재** |

⇒ **③ 두 항이 닫혔다.** 늘어난 둘이 정확히 885 10회차가 만든 둘이었다는 것이 이것으로 못박힌다
(래칫을 한 칸도 안 올렸다 — 585·601·750 규약).

## §3 ② 는 별개 축이고, **잃은 곳이 «첫 쪽» 하나로 좁혀진다**

수리 후 같은 스윕의 집계 — 그리고 772 가 게이트에 적어 둔 항등식과 나란히 놓으면:

| | 772·832(초록) | 지금 |
|---|---|---|
| 잰 노드 | 805 | **675** (−130) |
| 판정 | **518** | **428** (−90) |
| 원본비 없음 | 254 | 215 (−39) |
| 가려짐·잘림 | 33 | 32 (−1) |
| **판정 = 첫 쪽 + 스크롤 아래** | **518 = 317 + 201** | **428 = 227 + 201** |

⚑ **잃은 90 은 전부 «첫 쪽» 몫이다 — 스크롤 아래는 201 로 소수점까지 같다.**
그리고 갈래 셋 중 둘이 여기서 기각된다:

- ⓐ **화면이 안 열렸나** — 아니다. 진입 실패 **0** · 화면 71 개 전부(게이트 [S3] ② 첫 항도 초록).
- ⓑ **기준표가 그림을 못 쟀나** — 아니다. 그림 **15/15** 가 기준표에 들어왔다(`srcRef == srcSeen`).
- ⓒ **가림이 늘었나** — 아니다. `가려짐·잘림` 은 33 → **32** 로 오히려 하나 줄었다.

⇒ 남는 것은 **«잰 노드» 자체가 130 줄었다**는 것 하나다 — 판정에서 빠진 게 아니라
**스윕이 그 노드를 아예 안 만난다.**

### §3-1 못을 박은 실행 — **09-03 트리도 이 컨테이너에서는 428 이다**

832 가 «판정 518» 을 초록으로 적은 그 트리(`8974b01`, 2026-09-03)의 `index.html` 을 저장소 루트에
놓고 **같은 스윕을 통째로 다시 돌렸다**(338·344 규칙 · 966 이 966 을 기각할 때 쓴 그 손):

| | **09-03 트리**(832 가 518 을 적은 그 판) | 오늘 트리(973 수리 후) |
|---|---|---|
| 잰 노드 | 673 | 675 |
| **판정** | **428** | **428** |
| 원본비 없음 | 213 | 215 |
| 가려짐·잘림 | 32 | 32 |
| 칸 · 자리 | 57 · 21 | 57 · 21 |
| 그림 기준표 | 15/15 | 15/15 |

낱장 대조에서 **움직인 화면은 1개뿐**이고(12 소환 결과(펫) — 난수로 뽑히는 펫 캔버스 2노드 ·
**판정 기여 0**) **그림별 판정 차 0종 · 자리 차 0자리**다.

⇒ **제품은 90 을 잃은 적이 없다.** `JUDGE_MIN 480` 은 772·832 의 **컨테이너**가 낸 518 의 바닥이고,
이 컨테이너는 **같은 제품에서 428** 을 낸다(Chromium 141.0.7390.37 · playwright 1.63.0 ·
`/opt/pw-browsers/chromium` 폴백). 즉 이 항은 **제품이 아니라 환경을 재고 있었다.**

⚠ **이 위험은 이미 저장소에 적혀 있었다** — `docs/review/743-verify356게이트부패.md` §6-3:
«칸/자리 «수» 는 **환경 간에** 다르다(한 환경 안 2회는 동일)». 743 은 그 경고를 «래칫» 에만 달았고
**절대 수를 눈금으로 쓰는 [S3] ②** 에는 아무도 적용하지 않았다 ⇒ 컨테이너가 갈리는 날 반드시
빨개지게 돼 있던 자리다.

### §3-2 처방 — 절대 수를 «옮겨 적지» 않고 물음을 환경 불변량으로 갈아 끼운다

480 을 428 근처로 내려 적는 길은 **다음 컨테이너에서 똑같이 빨개진다**(그리고 그만큼 헐거워진다).
⇒ `verify356.js` [S3] ② 를 셋으로 짰다:

| 항 | 무엇을 묻나 | 실측 |
|---|---|---|
| **커버리지**(신설) | SCREENS **71화면이 전부 «잰 노드 ≥ 1»** 을 냈는가 — 397 사고(«화면 이름은 있는데 한 번도 간 적이 없다»)를 스윕 자신의 말로 묻는다. «진입했다» 는 «눌렀다» 까지다. | 두 트리 전부 71/71 · 화면당 최소 1노드 |
| **비**(신설) | 판정 ÷ 잰 ≥ **0.55** | **63.4%**(오늘) · **63.6%**(09-03 트리) · **64.3%**(832 의 518/805) — 컨테이너·트리가 갈려도 **소수 첫째까지 붙어 있는 유일한 수**다 |
| `JUDGE_MIN` | 480 → **400** — 이제 «전량 붕괴» 만 보는 바닥(실측 범위 428~518 의 아래) | 428 |

⚠ **`JUDGE_MIN` 을 다시 올리지 마라** — 올리는 순간 그 수를 못 내는 컨테이너에서 자가 죽는다.
조이는 일은 커버리지·비가 한다.
⚑ **커버리지 항에는 §R 되돌림 시험을 붙였다**(334) — 한 화면의 낱장을 빼면 그 항이 **그 화면을 짚는다**.
안 붙이면 «0건» 항과 «아무것도 안 보는» 항이 겉모습이 같다.

### §3-3 다음 회차에 남기는 프런티어 — «판정 ≥ 1 인 화면 수»

비(ratio)의 약점은 분명하다 — **한 화면이 통째로 아이콘을 잃으면 판정과 잰 노드가 같이 줄어
비가 안 움직인다.** 그것을 무는 눈금 후보를 이 회차에 **재 두기만** 했다:

| | 09-03 트리 | 오늘 트리 |
|---|---|---|
| 밑동 화면 | 71 | 71 |
| **판정 ≥ 1 인 화면** | **45** | **45** |
| 두 트리에서 갈리는 화면 | **없음** | |

⇒ 이 수는 «어느 화면에 판정 가능한 `img` 가 사는가» 라는 **구조**라서 트리가 갈려도 안 움직인다.
⚠ **그런데 이번 회차에는 눈금으로 안 세웠다** — 이 컨테이너 둘에서 같다는 것만 알고
**518 을 내던 컨테이너에서 45 인지는 모른다**. 45 를 그대로 박으면 그 컨테이너가 44 를 낼 때
**내가 지금 고치고 있는 바로 그 병**(컨테이너를 재는 눈금)을 다시 만든다.
⇒ 다음 세션이 **두 번째 환경의 실측**을 얻으면 그때 `JUDGE_SCREENS` 로 세워라. 근거는 이 표다.

## §4 검증

### 4-1 수리 전 ↔ 수리 후 완주 (같은 컨테이너)

| | 수리 전 | **수리 후** |
|---|---|---|
| `node tools/verify356.js` | **260/263 FAIL** | **266/266 PASS** |
| [S3] ② 스윕 | ✗ 판정 428개뿐(≥480) | ✓ 판정 428개 (원본비 없음 216 · 가려짐·잘림 32) |
| [S3] ② **커버리지**(신설) | — | ✓ 71화면이 전부 «잰 노드 ≥ 1» 을 냈다 |
| [S3] ② **커버리지 §R**(신설) | — | ✓ «02 메인» 낱장을 빼면 그 화면을 짚는다 |
| [S3] ② **비**(신설) | — | ✓ 428/676 = **63.3%** ≥ 55% |
| [S3] ③ 래칫(칸) | ✓ 59 ≤ 60 | ✓ **57** ≤ 60 |
| [S3] ③ 래칫(자리) | ✗ 23(래칫 22) | ✓ **21** ≤ 22 |
| [S3] ③ 이름표 | ✗ 등재 안 된 자리 2 | ✓ 나온 자리 **21개가 전부 등재된 자리** |

총점이 263 → **266** 으로 는 것은 **신설 3항**(커버리지 · 커버리지 §R · 비)이다 —
항을 지워서 초록이 된 것이 아니다. 래칫·이름표·눈금은 **한 칸도 안 넓혔다**.

### 4-2 회귀 (전부 무수정)

| 자 | 결과 | 왜 이 자인가 |
|---|---|---|
| `verify833` | **281/281 PASS** | 이 카드의 리본 기하 · **[7-e] 가 금색 판 81.6 을 ±0.3 으로 문다**(판을 안 건드렸다는 짝) |
| `verify667` | **92/92 PASS** | 이용권 UI·리본 마감 당사자 |
| `verify966` | **8/8 PASS** | 같은 카드의 직전 회차(불릿형 알약 더미) |
| `verify969` | **15/15 PASS** | 같은 카드의 진행 중 회차(리본 띠 77) |
| `verify923` | **121/121 PASS** | 같은 카드의 노치 |
| `verify596` | **39/39 PASS** | 356 계열 «뺀 것이 곧 감시자» 선례 |
| `verify772` | **18/18 PASS** | 스윕 항등식(판정 = 첫 쪽 + 스크롤 아래)·쪽 루프 — `probe418.sweep` 에 낱장 옵션을 낸 것이 스코프를 안 건드렸다는 짝 |
| `node tools/smoke.js` | **SMOKE PASS** | 필수 게이트 |
| `node tools/verifyProgress.js` | **PROGRESS OK** (종료 0) | 필수 게이트 |

## §5 마감

- **제품 `index.html` 1값**(`.pvc.ban1>.rb>b>.cic` 53.7 → 54) · 레이아웃은 아이콘 한 변 **+0.29px**.
- **자 2파일** — `tools/verify356.js` [S3] ② (커버리지·비 신설 · `JUDGE_MIN` 480 → 400) ·
  `tools/probe418.js` (`opt.rows` — 켠 호출에만 낱장을 붙인다. 기존 호출의 반환은 무변경).
- **재현자 1파일 신설** — `tools/probe973.js`(스윕은 `probe418.sweep` 한 벌 그대로 · 낱장만 접는다).
- 밸런스·판정 **0줄**.
