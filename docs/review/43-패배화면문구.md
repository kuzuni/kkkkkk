# 43 — 18 패배 화면 카드 2 문구 개정 «골드로 훈련하세요»

작업 세션 `sess-2213-12780` (2026-08-24, 워커 A). **기계적 작업**이므로 지시서 [3]-(가) 대로
**비평가를 띄우지 않고** grep 잔여 + 헤드리스 전수 실측(`node verify43.js`) + `node tools/smoke.js` 로만 검증했다.
레이아웃·측정값은 `docs/measure/18-패배화면.md` 그대로이며 **CSS 는 한 줄도 바꾸지 않았다**(문자열만 교체).

## 지시 (PROGRESS 43 · UI-REFERENCE 18)

> 카드 문구 `골드로 햄지와 매옹이를 훈련시키세요` → **`골드로 훈련하세요`**
> (캐릭터 «용사» 1명 확정에 따른 표기 통일). `.df-txt` 두 겹(본문 + 외곽선 `.ov`) 모두 교체,
> 카드 크기·배치 불변·가운데 정렬 유지.

## 변경 1곳 (index.html 4367행)

| # | 위치 | 변경 |
|---|---|---|
| 1 | `.df-card.c2 .df-txt` | 본문 `<i>` 와 외곽선 `<i class="ov">` **두 겹 모두** `골드로 햄지와 매옹이를 훈련시키세요` → **`골드로 훈련하세요`** |

CSS·DOM 구조·카드 좌표·아이콘·버튼은 **전부 그대로**다.
`.df-card.c2 .df-txt i{transform:scaleX(.875)}` 도 유지했다 — 이 값은 «문자열 길이» 가 아니라
**«ref 게임 서체의 장체 비율»** 을 재현하는 카드별 상수이고(c1 .889 / c2 .875 / c3 .907),
지시로 문구가 바뀐 이상 그 문자열의 ref 목표 잉크폭은 더 이상 존재하지 않는다.
같은 조판감을 유지하는 쪽이 맞아 손대지 않았다.

### 손대지 않은 것 (범위 밖 — 다음 작업자에게 넘김)

- 카드 2 아이콘이 아직 **🐹(햄지)·🐱(매옹이) 이모지 2개**(`.df-ic .f1/.f2`)다.
  «용사 1명» 확정과 어긋나지만 **43 의 범위는 `.df-txt` 두 겹**으로 못박혀 있어 건드리지 않았다.
  아트 발주(`docs/measure/18-패배화면.md` «아트 필요» 표) 때 **훈련 아이콘 1개**로 정리하면 된다.
- `renderSpec()` 의 `햄지 공격력` 계열 13행(8404~8416행)도 같은 계열의 표기 잔재지만 **20번(프로필 스펙 정보) 구간**이다.

## 검증 — `node verify43.js` **46/46 PASS** (헤드리스 1080×2280)

캡처 `docs/review/43-r1.png` (1080×2280, `openDefeat()` 로 연 상태. 28-③ 교훈대로 캡처 직전 `#view` 를 내렸다).

### [1] 문구 교체 — 7/7

| 항목 | 결과 |
|---|---|
| 구 문구 «골드로 햄지와 매옹이를 훈련시키세요» 잔재 | **0건** (`#defw` textContent · index.html grep 둘 다) |
| 카드2 본문 `<i>` | `골드로 훈련하세요` |
| 카드2 외곽선 `<i class="ov">` | `골드로 훈련하세요` |
| 두 겹 모두 교체 (innerHTML 등장 횟수) | **2회** |
| 카드1·카드3 문구 | 불변 |

### [2] 카드 크기·배치 불변 — 12/12 (전부 Δ0)

측정표 18 값 `left 91 · w 898 · h 166`, top `705 / 885 / 1065` (프레임 px) — **세 카드 모두 Δ0**.

### [3] 세로 가운데 정렬 유지 · 두 겹 정합 — 9/9

| 항목 | 실측 |
|---|---|
| `.df-txt` `align-items` | `center` (유지) |
| 카드2 글자 잉크 중심 y − 카드 중심 y | **−1px** (±2 이내) |
| 본문 ↔ 외곽선 잉크 left / width / top Δ | **0 / 0 / 0** |
| `scaleX` | `matrix(0.875, …)` 유지 |
| 문구 시작 x (c1 / c2 / c3) | **275 / 275 / 275** — 좌정렬 축 동일 |

> **좌표 함정**: `.df-txt{left:177px}` 은 카드의 **패딩 박스** 기준이라(03 교훈: 테두리가 자식 좌표를 민다)
> 프레임 절대 x 는 `91 + 7(border) + 177 = 275` 다. 검증 1차에서 이걸 `177` 로 단정해 «FAIL» 이 났는데
> **세 카드가 모두 275** 라 우리 단정이 틀린 것이었다 — 05 교훈 3(«비평가 수치는 어디를 기준으로 쟀는지 확인 전엔 반영하지 마라»)이
> 자가 검증 스크립트에서 재현된 사례다. 판정을 «절대값» 이 아니라 **«세 카드 동일»** 이라는 불변식으로 바꿨다.

### [4] 겹침·잘림 0건 — 12/12

| 카드 | 글자 잉크 x..right | 아이콘 right | [→] 버튼 left | 카드 right |
|---|---|---|---|---|
| c1 | 275..756 | 245 | 841 | 989 |
| c2 | **275..485** | 240 | 841 | 989 |
| c3 | 275..766 | 224 | 841 | 989 |

문구가 짧아져 c2 의 글자 우단이 **679 → 485** 로 줄었다(버튼까지 여유 356px). 세로도 `951..983` 로 카드(`885..1051`) 안이다.

### [5]·[6] 런타임 · 기능 체크 (T2 «기능 완성 규칙») — 6/6

| 버튼 | 눌렀을 때 바뀌는 것 | 결과 |
|---|---|---|
| 카드2 `[→]` (`data-dfgo="grow"`) | `#defw` 닫힘 + **23 훈련 시트 `#trw` 열림** | ✅ `{defw:false, trw:true}` — 바뀐 문구 «골드로 훈련하세요» 와 동작이 일치 |
| 카드1 `[→]` (`data-dfgo="shop"`) | `#defw` 닫힘 + 상점 `#shopw` 열림 | ✅ 회귀 없음 |
| `터치하여 닫기` / 배경 | `#defw` 닫힘 | ✅ 회귀 없음 |
| 콘솔 / 페이지 에러 | — | **0건** (기능 체크 후에도 0건) |

카드3 `[→]` 은 `data-dfgo` 가 없어 «닫기만» 하는 것이 원래 동작이다(레드닷 안내 문구라 이동 대상이 없다). 이번 작업과 무관.

### 게이트

`node tools/smoke.js` → **SMOKE PASS** (자동 플레이 · 오프너 전수 · NaN/undefined 0 · 화면비 6종 프레임/시트 잘림 0).

## 회귀 스크립트

`node verify43.js` — `#defw` / `.df-txt` 구간을 다시 손대는 세션은 **손대기 전/후로 한 번씩** 돌려 46/46 을 확인할 것.
