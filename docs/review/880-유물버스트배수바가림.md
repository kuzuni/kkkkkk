# 880 — 89 유물 소환 버스트가 배수 토글 바(`#rwMulBar`)를 밟는다 «버그(가림)»

등재: 2026-09-03 (sess-1558-25897 루틴 워커 D · 838 9회차 비평 DK 씬 B ④ 1순위)
작업: sess-1628-11388 루틴 워커 B

---

## 1회차 (2026-09-03) — 재현. 제품 `index.html` 0줄

### 왜 재현부터인가

등재문의 수치(250ms · «×10» 4.94% · 간격 0px)는 838 9회차가 **저장소에 남지 않은 자**
(`scratch/relicocc`)로 낸 값이다. 338 규칙 — 등재문의 처방을 바로 넣으면 «이미 참인 것을
게이트로 굳히는» 그 사고를 되풀이한다. ⇒ **저장소 안의 자**로 먼저 빨갛게 세웠다.

### 자 — `tools/probe880.js` (신설)

`cap681.js` 의 **씬 B 와 같은 경로·같은 시드(20260902)·같은 시각표**(0/20/45/70/110/175/250/320ms)를
그대로 재사용한다(사본을 새로 안 만든다 — 402 규약). 재는 것 둘:

* **덮임 %** — 배수 칸(`#rwMulBar > [data-mul]`) 넷 각각에 대해, 보이는 알
  (`#fxl > *` · α>0.06 · 최소변 ≥6px — cap681 규약)의 상자 **합집합**이 칸을 덮는 넓이 비율.
  (알 하나씩의 합이 아니다 — 두 알이 겹치면 두 번 세지 않는다.)
* **간격 px** — 칸 상자와 가장 가까운 알 상자 사이 거리(겹치면 0).

### 실측 (수리 전 · 바 `#rwMulBar` = x 220.9 · y 1476.6 · 636.3×96.5)

| t(ms) | 보이는 알 | ×1 | ×10 | ×100 | ×1,000 |
|---|---|---|---|---|---|
| 0 | 13 | 0% / 253.9px | 0% / 213.8px | 0% / 213.8px | 0% / 256.7px |
| 20 | 13 | 0% / 221.1px | 0% / 180.9px | 0% / 180.9px | 0% / 221.1px |
| 45 | 13 | 0% / 185.9px | 0% / 145.7px | 0% / 145.7px | 0% / 181px |
| 70 | 13 | 0% / 155.4px | 0% / 112.6px | 0% / 112.6px | 0% / 146.6px |
| 110 | 12 | 0% / 123.3px | 0% / 74.5px | 0% / 74.5px | 0% / 109.3px |
| 175 | 12 | 0% / 75.8px | 0% / 19.1px | 0% / 19.1px | 0% / 59px |
| **250** | 12 | 0% / 32.5px | **5.01% / 0px** | **6.06% / 0px** | 0% / 32.5px |
| **320** | 11 | 0% / 11.9px | **4.03% / 0px** | **5.34% / 0px** | 0% / 19.7px |

**⇒ 등재문의 현상은 실재한다.** 등재문이 «×10 4.94%» 로 적은 자리를 내 자는 «×10 5.01% ·
×100 4.96~6.06%» 로 읽는다 — 칸 이름과 소수점은 자마다 다르지만 **밟는다는 사실은 같다**.

### ⚑ 이 회차가 가른 것 ① — 밟는 것은 **지불 버스트**(`fx-cic`)다

이 화면에는 알이 두 층이다(지불 버스트 `fx-cic` · 683/753 의 획득 알 `fx-rlic`).
자에 «어느 층이 덮는가» 칸을 넣어 갈랐다 — **덮는 것은 전부 `fx-cic` 한 층**이고
획득 알은 한 프레임도 안 닿는다. 고칠 자리가 `rwGainFx` 가 아니라는 뜻이다.

### ⚑⚑ 이 회차가 가른 것 ② — **등재문의 처방 ⓐ 는 이 자리에서 안 듣는다**

등재문은 «`--burst-keep` 을 호스트 **조상** 안의 셀렉터로 한 칸만 넓힌다» 를 1순위 처방으로
적었다. 코드를 읽어 보니 그 길은 **두 겹으로 막혀 있다**:

1. **호스트가 없다.** 이 버스트는 `fxBurst({x, y:cy}, …)` — **점 대상**이다(`rwSummonFx` ~35173).
   `fxBurst` 안에서 구멍은 `const kh = (r && !IC) ? … : []` 와 `if(IC && r) kh.push(…)` 로
   **둘 다 `r`(요소 상자)이 있어야** 채워진다. 점 대상은 `r === null` 이라 구멍이 **한 개도** 안 판다.
   `--burst-keep` 을 `#rwBasin` 에 신고해도 `fxBurst` 는 그 요소를 아예 안 본다.
2. **본다 해도 덮어써진다.** `rwSummonFx` 는 `fxBurst` 가 앉힌 알을 받아
   `born.forEach(…)` 에서 **`left`·`top`·`--dx`·`--dy` 를 전부 다시 적는다**(~35179~35193 —
   위쪽 반구 원뿔로 재조준). `fxBurst` 의 각도 굴림(`FXB_KTRY`)이 구멍을 피해 고른 각도는
   그 자리에서 **통째로 버려진다.**

⇒ **뿌리는 공용 부품(`fxbKeepHoles`)이 아니라 이 화면의 재조준**이다. 867 이 바를 «수반/소환 버튼»
블록으로 옮겨 비행 사거리(끝 반경 최대 345.9px) 안에 넣은 뒤로, 위쪽 반구로 쏘는 알이
**아무것도 안 물어보고** 그 위에 착지한다. 2회차 처방은 그 자리에서 낸다.

### 자의 흔들림 (873 규율 — 단일 실행 대조 금지)

같은 트리 **3회** 실행: `×10` 250ms **5.01% 고정** · `×100` 250ms **4.96~6.06%(±0.55%p)** ·
간격 ±1.7px. 0% 인 칸은 **세 번 다 0%** 다. ⇒ **판정 축(«덮임 ≥1% 인 칸이 있다»)은 안 흔들린다.**
814 9·10회차 교훈대로 `getAnimations()` 를 **두 번 감게**(스타일 플러시 사이에 끼워) 고친 뒤의 값이다 —
한 번만 감으면 갓 등록된 알이 시간 안 감긴 채 재어져 흔들림이 더 컸다.

### 이 회차 결과

* `probe880` 신설 — **[P1] FAIL(재현됨)** · [P2]·[P3]·[P4] PASS
* 제품 `index.html` **0줄** · 레이아웃 Δ0px

---

## 2회차 (2026-09-03) — 수리. 제품 `index.html` **3자리**(부품 2 + 신고 1) · 레이아웃 Δ0px

### 처방 — «앉을 자리» 가 아니라 **«날아갈 거리»** 를 묻는다

1회차가 등재문의 처방 ⓐ 를 기각했으므로(구멍은 이 자리에 닿지 않는다) 축을 바꿨다.
816 의 `--burst-keep` 과 **짝이면서 뜻이 반대인** 신고를 하나 놓는다:

| 신고 | 뜻 | 하는 일 |
|---|---|---|
| `--burst-keep` (816) | «내 **안**의 이 잉크를 덮지 마라» | 앉을 자리를 뺀다(구멍) |
| **`--burst-block` (880)** | «내 **밖**의 이 상자에 들어가지 마라» | 날아갈 거리를 줄인다(벽) |

* `fxbBlockRects(t, m)` — 호스트가 신고한 셀렉터를 **문서에서** 찾되 **호스트 자신과 그 자손은
  절대 안 담는다**(816 의 «호스트 자신 제외» · 660 의 «스폰은 버튼뿐» 과 같은 규약).
  신고가 없으면 **빈 배열** = 안 신고한 호스트는 한 값도 안 바뀐다.
* `fxbRayLimit(walls, ox, oy, ux, uy, half)` — 그 방향으로 **벽에 닿기 전까지 갈 수 있는 거리**.
  알의 잉크 반폭만큼 상자를 미리 부풀려 재므로(민코프스키) «중심만 피하고 잉크는 걸치는» 자리가
  안 생긴다(816 의 «여유는 입자 크기에서» 와 같은 규칙). 벽이 없으면 `Infinity`.
* `.rw-basin{--burst-block:#rwMulBar}` — **자바스크립트에 이 화면의 요소 이름이 한 개도 안 적힌다.**
  바가 또 옮겨 가면 이 신고 한 줄만 고치면 된다(867 이 옮겼을 때 아무 자도 안 짖은 것이 이 사고의 뿌리다).

⚑ **각도는 한 값도 안 건드렸다.** 838 3회차가 «clamp 가 각도째 지웠다» 로 값을 치른 자리라
(끝 x 폭 1.3px · 4알이 한 줄) 자를 그 반대편에 세웠다 — 이 부품은 «얼마나» 만 답하고
«어디로» 는 부르는 쪽이 이미 정한 그대로다. 위쪽 원뿔·부채 간격·위상 수열·알 수·크기·수명
전부 불변이고, 줄어드는 것은 **벽을 향해 가던 알의 거리 하나**다.
여유는 `FXB_INPAD`(619 14회차의 «잉크와 액자 사이에 늘 4px») — **새 손 상수 0개**.

### 실측 (수리 후 · `probe880`)

| t(ms) | ×1 | ×10 | ×100 | ×1,000 |
|---|---|---|---|---|
| 175 | 0% / 86.8px | 0% / 77.2px | 0% / 77.6px | 0% / 94.6px |
| **250** | 0% / 47.8px | **0% / 44.3px** | **0% / 44.7px** | 0% / 55.6px |
| **320** | 0% / 24.3px | **0% / 22.7px** | **0% / 23.1px** | 0% / 30.3px |

**최악 덮임 6.06% → 0% · 최소 간격 0px → 22.7px** · 보이는 알 수열 **13/13/13/13/12/12/12/11 불변**.

### ⚑ 자를 세우다 잡은 것 — «헛초록» 두 자리와 «반올림 한 톨»

`verify880` 첫 판이 17/17 초록이었는데 **두 항이 아무것도 안 묻고 있었다**:

* `[C7]` 이 `ends.every(p => true)` — 식이 항상 참이었다. 봉투와 무관한 **기하** 단언
  (끝 상자 ↔ 바 간격)으로 갈아 끼우자 **곧바로 빨개졌다**.
* `[R3]` 이 «신고 안 한 요소를 물어 0» 이었다 — «호스트 제외» 규약이 통째로 사라져도 초록인 자다.
  ⇒ 벽 신고를 **그 요소에 직접 얹어 놓고** 0 인지를 묻는 [R3a](자신)·[R3b](자손)로 가르고,
  둘이 헛초록이 아님을 못박는 **[R3c]**(같은 신고를 «남» 이 하면 그 자손은 벽이 된다)를 신설했다.

그렇게 되살아난 [C7] 이 잡은 것은 **결함이 아니라 문턱**이었다 — 벽에 걸린 알은 신고 여유의
**경계에 정확히** 서고 `--dx/--dy` 는 `toFixed(1)` 로 적히므로 반올림 한 톨(≤0.05px)이 그 경계를
파고든다(제품 주석이 같은 자리에서 이미 «딱 맞추면 반올림 한 톨에 [C1] 이 흔들린다 — 574·709·825»
라고 경고한다). ⇒ 한 톨을 빼고 묻는다. 실측 **3.93px**.

### 이 회차 결과

* `verify880` **20/20 신설**(§1 가림 4 · §2 «각도 0줄» 6 · §R 되돌림 10)
* `probe880` 4/4 — 수리 전 [P1] FAIL → 수리 후 **PASS**
* **이관 1종** — `verify666` [A5b] **31/32 → 32/32**. 그 자는 `RW_FX_FLY).toFixed(1)` 이라는
  **최종 식의 모양**을 물고 있었는데 880 이 그 이동에 상한을 얹어 모양만 바뀌었다. 333 처방대로
  **항을 지우지 않고 묻는 것만** 갈아 끼웠다(뜻 셋 — `iv` 안 넘김 · 이 자리에서 `RW_FX_FLY` 로 늘림 ·
  공용 상수 불변 — 은 그대로다). 「벽이 없으면 값이 종전과 같다」 는 `verify880` [C5] 가 실런타임으로 문다.
* **회귀 17종 무수정** — `verify816` 27/27 · `verify818` 41/41 · `verify681` 39/39 · `verify619` 65/65 ·
  `verify660` 55/55 · `verify877` 21/21 · `verify814` 30/30 · `verify838` 19/19 · `verify867` 12/12 ·
  `verify866` 16/16 · `verify813` 21/21 · `verify683` 40/40 · `verify753` 32/32 · `verify700` 26/26 ·
  `verify120` 169/169 · `verify330` 20/20 · `verify299` 27/27
* **SMOKE PASS** · 콘솔 에러 0
* 측정표·UI-REFERENCE 는 **안 건드렸다** — 레이아웃 Δ0px 이고 바뀐 것은 연출의 사거리 하나다.

### 다음 세션에게

* 이 신고는 **89 한 자리에만** 있다. 같은 사고(«남이 내 사거리 안으로 이사 왔다»)는 다른 버스트
  자리에서도 날 수 있고, 그때 고칠 것은 부품이 아니라 **그 호스트의 신고 한 줄**이다.
* `probe880` 은 단일 실행에서 ×100 250ms 가 **±0.55%p** 흔들린다(873 규율 — 3회 실행 실측).
  판정 축(«덮임 ≥1%»)은 안 흔들리지만, **크기를 비교하는 데는 쓰지 마라.**
