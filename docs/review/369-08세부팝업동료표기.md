# 369 — 08 세부 팝업의 «동료» 표기 2건 (173 «펫 전수 통일» 이 놓친 파생 문자열)

**T1 «버그 · 게이트 부패»** · 2026-08-28 · sess-2247-3846 워커 C · **1회차** ·
검증은 지시서 [3]-**(가)** (기계적 = 문자열 교체 · 레퍼런스 대조 없음 → **비평가 안 띄움**, 게이트로만 판정)

**제품 `index.html` 2자리**(문자열 2개 · 주석 4줄) · 레이아웃 **Δ0px** · 판정·밸런스·세이브 **0줄**.

---

## 1. 재현이 먼저 (338 규칙) — 등재문 가설은 맞았고, **자리의 수는 달랐다**

등재문은 두 자리를 «08 세부 팝업 **헤더**·안내문 두 줄» 이라고 적었다. `tools/probe369.js` 를 신설해
**계열 4종 × 보유/미보유 8표본**을 실제로 열어 «찍힌 텍스트» 를 세어 보니 —

| 표본 | 수리 전 «동료» | 어디에 |
|---|---|---|
| 펫 **보유** | **2** | 알약 `.sk-sl` «**동료** 설명» + 본문 «**동료**는 최대 3마리까지 장착합니다» |
| 펫 **미보유** | **2** | 알약 «**동료** 설명» + 본문 «**동료** 소환으로 획득하세요» |
| 장비 보유/미보유 | 0 | — |
| 유물 보유/미보유 | 0 | — |

⚑ **«헤더» 라는 자리는 없다.** 08 껍데기에는 제목 헤더가 없고(같은 절 주석 «08 껍데기에는 닫기 버튼이 없다» 와 같은 구조),
`kindN` 이 먹이는 것은 **① 설명 알약 ② 미보유 안내문** 둘이다. 그리고 이 둘은 **보유·미보유 어느 쪽에서도 각각 2건**이 되므로,
주인이 어떤 상태에서 팝업을 열든 «동료» 가 한 번은 눈에 들어왔다. 등재문의 «두 줄» 은 소스 줄 수(2줄)와는 맞고
**화면 자리 수와도 우연히 2로 같지만, 가리키는 대상이 다르다** — 그래서 이 정오를 여기 남긴다.

## 2. 고친 곳 (제품 2자리)

```
23480  const kindN = cat === 'pet' ? '펫' : cat === 'equip' ? '장비' : '유물';
23504  + '펫은 최대 <em>3마리</em>까지 장착합니다.'      ← «동료는» 은 조사까지 바뀐다
```

변수명(`kindN`) · 주석 · id 는 **그대로 뒀다**(173 원칙 — 바꾸는 것은 사용자에게 보이는 문자열뿐).

## 3. «넘침이 아니라 가운데» — 라벨이 3글자 → 2글자로 줄었다 (A1 교훈)

등재문의 경고대로 본 것은 넘침이 아니라 **정렬**이다. `.sk-sl` 은 `width:300px` 고정에
`display:flex; justify-content:center` 이고 라벨은 `transform:scaleX(.9)` 를 탄다.

| 표본 | 잉크 폭 | 상자 | 잉크 중심 − 상자 중심 | 넘침 |
|---|---|---|---|---|
| 펫 (수리 전 «동료 설명») | 86.64 | 300×58 | −0.01px | 0 |
| 펫 (수리 후 «펫 설명») | **67.87** (−21.7%) | 300×58 | **0.00px** | 0 |
| 장비 «장비 설명» (대조) | 88.66 | 300×58 | −0.01px | 0 |
| 유물 «유물 설명» (대조) | 87.27 | 300×58 | 0.00px | 0 |

⇒ 폭만 줄고 **중심은 Δ0**. flex 가운데 정렬이라 글자 수가 줄어도 자리가 안 밀린다 —
CSS 를 한 줄도 안 건드린 근거가 이 표다. 본문 `.sk-db`(`overflow:hidden`, 안쪽 290px)도 8표본 전부 **넘침 0px**
(펫 미보유 본문 높이 76.07 → 79.69 로 «펫» 이 한 자 짧아지며 줄바꿈이 한 번 달라졌지만 상자 안이다).

## 4. 게이트

| 게이트 | 결과 |
|---|---|
| `tools/verify369.js` | **18/18 신설** — [1] 소스 · [2] 화면(펫 보유/미보유) · [3] 대조(장비·유물 불변) · [4] 기하 · **[R] 되돌림 시험** · [J] 콘솔 |
| `tools/probe369.js` | **6/6 신설**(재현. 수리 전 4/6 FAIL — P1·P2 가 «동료 2건» 으로 빨갰다) |
| `tools/verify173.js` | **28/29 FAIL → 29/29 PASS** (A1 «주석 밖 «동료» 0건» = 0) |
| `tools/verify268.js` **이관** | 148/149 FAIL → **149/149 PASS** — §4 «[펫] 설명 헤더» 기대값 «동료 설명» → «펫 설명» |
| 회귀 | `verify149` 273/273 · `verify171` 32/32 · `verify174` 74/74 · `verify188` 79/79 · `verify194` 54/54 · `verify253` 92/92 · `verify262` 46/46 · `verify269` 59/59 · `verify270` 46/46 · `verify272` 39/39 · `verify346` 68/68 — **전부 무수정** |
| `node tools/smoke.js` | **SMOKE PASS** |

⚑ **왜 `verify173` A1 만으로는 모자란가** — A1 은 **소스 grep**(«주석 밖 «동료» 0건»)이다.
173 이 이 자리를 놓친 이유가 바로 «소스를 화면 목록으로 돌았다» 인데, 같은 자를 한 번 더 대는 것으로는
**화면에 무엇이 찍히는지**를 못 묻는다. 그래서 `verify369` 는 팝업을 실제로 열어 `innerText` 를 센다.

**[R] 되돌림 시험이 이 수리의 못이다** — 두 자리를 **각각** 되돌린 사본을 띄워
R1(«동료 설명» 이 돌아온다) · R2(«동료는 최대» 가 돌아온다)가 둘 다 잡히는 것을 확인했다.
한 항만 두면 «둘 중 하나만 되돌려도 조용한 게이트» 가 된다.

## 5. ⚑ 173 은 왜 여기만 놓쳤나 — 그리고 같은 모양이 또 있나 (등재문 숙제)

**173 은 «화면 목록» 을 돌았다.** 06 서브탭 · 26 시트 · 10 상점 · 21 도감 · 13 재화 탭 · 우편 —
전부 «그 화면에 가서 라벨을 본다» 로 잡히는 자리다. 08 세부 팝업의 `kindN` 은 화면이 아니라
**여러 화면(펫·장비·유물)이 공유하는 파생 문자열**이라 그 목록의 어느 칸에도 안 걸렸다.

같은 모양이 또 있는지 전수로 쓸었다 — 종류 이름을 **파생**으로 만드는 자리는 소스에 **둘뿐**이다:

| 자리 | 값 | 상태 |
|---|---|---|
| `kindN`(23480, 08 세부 팝업) | `pet → '펫'` | **369 가 고쳤다** |
| `COLL_CATN`(16234, 21 도감) | `pet:'펫'` | 173 이 이미 갈았고 `verify173` E1 이 지킨다 |

⇒ 파생 자리는 이제 **둘 다 게이트가 있다.** (`grep -nE "\? *'(펫|장비|유물|스킬|코스튬|동료)'"` · `grep -nE "(KIND|kind|catN|CAT_?N)[A-Za-z]* *= *\{"`)

## 6. 남긴 것

- 없음. 곁다리 관측 0건 · 새 등재 0건.

---

## 7. 후속 — 완료 기록이 되돌아가 369 가 **다시 선점됐다** (2026-08-29, sess-2347-22678 워커 C · 제품 0줄)

이 작업은 `16d96a4 done(369)` 로 **끝나 있었다.** 그런데 47분 뒤 다음 루틴 워커가 지시서 [-1] 의 티어 순서대로
«미완료 T1» 을 고르자 **369 가 다시 잡혔다.**

- **뿌리**: `5fe9d37`(done(370))가 자기 370 행을 쓰고 373 을 등재하면서, rebase 충돌을 «내 사본» 으로 푸는 바람에
  **이웃 두 행(368·369)이 등재 당시의 «미착수» 본문으로 되돌아갔다.** 규칙 8(«양쪽 행을 모두 살린다») 위반이다.
- **손실 범위는 표 두 줄뿐이었다.** 제품(`index.html` 23503·23530) · `tools/verify369.js` · `probe369` ·
  이 review 파일 · LESSONS 369 는 전부 멀쩡했고, 재선점한 세션이 착수 전에 실행으로 확인했다 —
  **`verify369` 18/18 · `verify173` 29/29 · `verify348` 41/41.**
- **복원 방법**: 각 `done()` 커밋이 쓴 행을 **그대로** 되살렸다. 되살리기 전에 «done 이후 그 행에 정당한 편집이 있었는가»
  를 행 단위 이력으로 확인했고 **0건**이었다(있었다면 그것까지 살려야 한다 — 규칙 8 그대로다).
- **재발 방지**는 **374** 로 등재했다(되돌림 탐지 게이트 / 행 편집 도구 / 병합 드라이버 세 후보 · 무르게 푸는 자
  «✅ 가 있는가» 를 기각하는 이유까지). 교훈은 LESSONS **374** 3개.

⚑ **이 절이 남기는 한 줄**: `docs/PROGRESS.md` 의 행은 코드보다 쉽게 부패하고, 그 행이 곧 다음 워커의 **작업 배정 입력**이다.
착수 전 `git log --oneline -40 | grep 'done(<ID>)'` 와 `ls docs/review/<ID>-*.md tools/verify<ID>.js` 한 줄이면 헛일을 막는다.
