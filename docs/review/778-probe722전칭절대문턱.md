# 778 — `probe722` [5-a]·[8-b] 는 «절대 문턱» 과 «전칭» 이었다 (775 가 `probe504` 에서 걷어낸 것과 같은 모양)

- **작업 단위** 778 (T1 «버그(게이트 플레이키)») · **세션** sess-2328-6713 루틴 워커 B · **1회차**
- **제품 `index.html` 0줄** · 자 1파일(`tools/probe722.js` 개정)
- 등재: 2026-09-01, sess-2227-27845 워커 B(775 회귀 스윕의 곁다리 관측 — 등재만·선점 안 함)
- 지시서 [3]-**(가)** 기계적 작업(자 수리) — **비평가 없음**

---

## §1 재현 — 등재문 그대로다

`node tools/probe722.js` 를 수리 전 트리에서 7회 굴렸다.

| 실행 | 결과 | 빨간 항 |
|---|---|---|
| 0(착수 직후) | **FAIL 17/18** | `[8-b]` — `nova ×1.11` |
| 1~5 | PASS 18/18 · PASS · PASS · PASS · PASS | — |
| 6 | **FAIL 17/18** | `[8-b]` — `nova ×1.18` |

**7회 중 2회 빨강**(등재문은 «3회 중 1회 · 16/18»). 빨간 항은 매번 `[8-b]` 하나이고
빨강을 정하는 것은 **언제나 `nova` 한 종**이다 — 문턱 1.2 에 붙어 산다(실측 ×1.11~1.60).

`[5-a]` 는 이 7회에서는 안 터졌지만 실측이 등재문을 뒷받침한다 — `mImm` 이 **22~41%** 로
갈리고 문턱이 **20%** 다(등재문의 빨간 실행이 19%). **문턱이 분포 한복판에 앉아 있다.**

| 수리 전 실측(6회) | #1 | #2 | #3 | #4 | #5 | #6 |
|---|---|---|---|---|---|---|
| `[5-a]` mStd | 56% | 28% | 29% | 28% | 63% | 37% |
| `[5-a]` mImm | 38% | 26% | **23%** | **22%** | 41% | 34% |
| `[8-b]` 값 배수 최솟값(`nova`) | ×1.24 | ×1.29 | ×1.60 | ×1.36 | ×1.35 | **×1.18** |

## §2 뿌리 — 둘 다 775 가 이미 진단한 모양이다

### ⓐ `[8-b]` 는 775 가 걷어낸 `[C2]` 와 **글자 그대로 같다**

```js
const upAll = CIDS.filter((id, i) => valRatio[i] > 1.2);
ok(upAll.length === CIDS.length, '8-b … **표본 전 종이** 위로 간다 …');
```

`every` + 문턱이므로 **빨강을 정하는 것은 표본의 최솟값 하나**다. 그 최솟값은 늘 같은 짝
(`nova`·`gale`)이고, 그것은 잡음이 아니라 **그 종의 성질**이다 — 775 §2-ⓑ 의 항등식
`값 배수 = 개체수 배수 × 도달 몫 비(rx)` 에서 `nova`(`t:'area' · r:250`, 반경이 고정된 제자리
폭발)는 개체수가 ×2.4 로 늘어도 늘어난 적이 반경 **밖**에 쌓여 한 발의 도달 몫이 반토막 난다
(**포화**). 이 자 자신의 실측 `rx` 가 그것을 매 실행 찍는다: `nova` 0.44~0.65 vs 위쪽 0.84~1.53.

⚑ **그리고 이 자는 스스로를 뒤집고 있었다** — 바로 위 `[6-a]` 가 같은 실행에서
«POP 을 고정하면 부호가 **종마다 갈린다**(gale·aura·nova 는 내려간다)» 를 **PASS 로** 찍는다.
775 §7 의 «자 안에서 서로를 부정하는 두 항이 있으면 플레이키는 그 사이에서 난다» 의 실물이다.

### ⓑ `[5-a]` 는 «갈린다» 를 적어 두고 그 한복판에 문턱을 박았다

```js
/* mStd 는 … 프로브 실행 사이에 36~82% 로 갈리고, mImm 은 … */
ok(mStdAvg > 0.2 && mImmAvg > 0.2, '5-a …');
```

주석이 «갈린다» 를 스스로 적어 두고 판정은 그 분포 안(20%)에 절대 문턱을 놓는다.
721-② «문턱이 잡음 폭 안에 있으면 그 항은 자가 아니라 동전» 그대로다.

### ⓒ 문턱을 다시 뽑는 길은 여기서도 답이 아니다

695-④·759·766·775 가 **네 번** 기각한 길이고, 산수도 같은 답을 준다 —
`mImm` 실측 최솟값이 22%(등재문 19%)라 20%를 15%로 내려도 여유가 4~7%p 뿐이고
그 값의 실행 간 sd 가 7%p 다. **여유/σ ≈ 1 = 여전히 동전.**
`[8-b]` 쪽도 같다(`nova` 실측 최솟값 1.11 이라 문턱을 1.1 로 내려도 여유 0.01).

## §3 처방 — 333 처방대로 자리를 비우지 않고 **방향을 갈랐다**

### `[8-b]` — 775 §3 을 그대로 옮겼다(문턱은 내린 게 아니라 **올랐다**)

| 항 | 묻는 것 | 판정 | 실측 |
|---|---|---|---|
| **[8-b]** | 크기 — 하네스가 값을 위로 민다 | 표본 **중앙값 ≥ 1.5** AND **평균 ≥ 1.3** | 중앙값 1.79~2.15 · 평균 1.79~2.53 |
| **[8-b2]** *(신설)* | 왜 종마다 다른가 | `rx` 최대÷최소 **≥ 1.5** AND 값 배수 아래 둘의 `rx` 평균 **≤ 0.8 ×** 위쪽 넷의 평균 | 폭 ×2.04~3.42 · 비 0.46~0.66 |
| **[8-br]** *(신설 · 되돌림)* | 이 바가 잡음만으로 안 넘어간다 | 하네스를 **끈** 짝(실제↔실제 36쌍)의 중앙값이 바 밖 | ×1.00 |

전칭 1.2 → **중앙값 1.5** 로 바를 **올리고도** 여유는 0.09(nova 최악) → **0.29** 로 커졌다.
766-③ «분산이 아니라 **가장자리까지의 여유**를 찍어라» 그대로다.

### `[5-a]` — 절대 문턱을 버리고 **분모 자신의 잔여 흔들림**에 대어 잰다

`[5]` 가 묻고 싶은 것은 «K회 평균이 ⓐ·ⓑ 를 ⓒ 로 데려가는가» 다. 그 «가까움» 의 자연스러운
자는 **분모 ⓒ 자신이 평균 뒤에 남긴 흔들림**이지 20% 라는 손 상수가 아니다.
같은 R회 표본을 **반씩 갈라 평균낸 ⓒ↔ⓒ 짝**(하네스도, 장면 교체도, **새 실행도 0개**)이
그 널이다 — 775 [C2r] 이 쓴 것과 같은 재료다.

| 항 | 묻는 것 | 판정 |
|---|---|---|
| **[5-a]** | K회 평균을 내도 두 이탈이 널 위에 남는다(= 잡음이 아니라 치우침) | `min(mStd, mImm) ≥ BAR × 널` |
| **[5-r]** *(신설 · 되돌림)* | 널이 «우연히 작아서» 초록인 게 아니다 | 서로 겹치지 않는 두 널 추정치가 3배 안 |

⚑ **널은 손 상수가 아니라 그 실행이 잰 값**이라 판정이 실행마다 같이 움직인다 —
판이 시끄러운 실행에서는 널도 같이 커진다. 그것이 절대 문턱과의 결정적 차이다.

## §4 1회차에 내가 **새 동전을 한 번 심었고, 자가 그것을 잡았다**

`[8-b2]` 를 처음에는 775 [C2b] 를 글자 그대로 옮겨 **순위상관 ρ(rx, 값 배수) ≥ 0.7** 로 적었다.
6회 중 1회 **ρ 0.37** 로 빨개졌다:

```
도달 몫 비 rx — lance 1.02 · gale 0.71 · flask 0.97 · poison 0.98 · aura 0.69 · nova 0.49
값 배수      — lance ×2.05 · gale ×2.00 · flask ×2.01 · poison ×2.12 · aura ×2.14 · nova ×1.34
FAIL 8-b2  순위상관 ρ 0.37 (≥0.7)
```

`probe504` [C] 에서는 값 배수가 1.09~2.21 로 **벌어져** 순위가 뜻을 갖는다. 그런데 여기 [8]
자유 판은 **`nova` 하나만 아래로 빠지고 나머지 다섯이 2.00~2.14 로 뭉친다** — 뭉친 다섯의
순위는 그날 잡음이 정한다. 775 §4 가 «분포가 겹치면 최솟값의 **이름**이 갈린다» 로 겪은 것과
같은 병이 **순위 전체**에서 난 것이다.

⚠ 같은 이유로 «아래 둘» 이라는 **짝**도 못 쓴다 — `aura` 는 `rx` 가 낮은데 개체수 배수가 커서
값 배수는 위에 앉는 실행이 있다(`값 배수 = 개체수 배수 × rx` 라 **한 축만 보면 순위가 안 따라온다**).
⇒ 순위·이름을 버리고 **묶음 평균**(아래 둘의 `rx` 평균 ≤ 0.8 × 위쪽 넷의 평균)으로 다시 적었다.

**775 §4 의 «새 항도 K회 돌려라» 가 없었으면 이 동전은 그대로 커밋됐을 것이다.**

## §5 곁다리 관측 — **780 으로 등재만**(선점 안 함 · 작업 단위 1개 규칙)

같은 자의 **[6-b]** 도 같은 병이다: `inflWorst > 1.5 && min(inflAll) < 1` 에서 `inflWorst`
(POP 고정 판의 최대 부풀림)가 실측 **×1.49~1.68** 로 문턱 1.5 에 붙어 산다 — 수리 전 6회에서는
안 터졌고 수리 후 실행에서 **×1.49 로 한 번 빨개졌다**(내 변경과 무관한 자리임을 아래로 확인).
**내 편집이 닿지 않는다** — `[6]` 블록은 한 줄도 안 건드렸고, `[5]`·`[8]` 의 새 항이 쓰는
`med`·`combos` 는 신설 헬퍼라 `[6]` 의 계산에 안 들어간다.

## §6 회귀

*(§7 에 실측 표)*

## §7 다음 세션이 볼 것

- **`every` + 문턱은 «크기» 를 묻는 자가 아니다** — 빨강을 정하는 것은 표본 하나다.
  크기를 묻고 싶으면 중앙값·평균처럼 **표본이 늘수록 좁아지는 통계**에 물려라(775 §7 재확인).
- **절대 문턱을 못 쓰는 자리에서는 «그 실행이 스스로 잰 널» 에 대어 재라.** 손 상수는 판이
  시끄러운 실행에서 같이 못 움직이지만, 널은 같이 움직인다.
- **남의 처방을 옮길 때는 «그 처방이 서 있던 분포» 까지 옮겨졌는지 보라.** 775 의 순위상관은
  값이 벌어진 분포 위에서 옳았고, 뭉친 분포로 옮기자 그 자리에서 동전이 됐다(§4).
