# 942 — «1:1 인데도 ref 만 다르게 읽히는 자» 11개 (②ⓑ 번짐 비대칭)

> 등재: 2026-09-05 (sess-1227-12147 루틴 워커 B — 932 1회차가 갈래 B 로 갈라 넘겼다)
> 티어: **잠복(자)** · 회차 상한 5 · 처방 순서는 932 §ⓓ·§ⓚ 표

## 1회차 (2026-09-05, sess-1458-10731 루틴 워커 D)

제품 `index.html` **0줄** · 레이아웃 Δ0px · 자 **1벌 수리**(`tools/probe409g.py`) ·
게이트 **`tools/verify942.js` 28/28 신설** · 장부 1행 갱신(`probe932` `probe409g.py` **B → S**) ·
`verify932` 래칫 이관(B **11 → 10** · `FIXED942`).

### ⓐ 왜 `probe409g.py` 를 골랐나

B 열한 자 중 **부르는 노드 게이트가 하나도 없는** 자다(`grep -rl probe409g.py tools/` = 자기 자신 ·
`probe932`/`verify932` 뿐). 932 2회차가 `scan667b` 를 고른 것과 **같은 이유**다 — 자를 고쳐도
남의 게이트에 못박힌 수가 안 흔들려 이관 없이 닫을 수 있다. 게다가 결함이 **파일 안에 적혀 있었다**:
`--edge`·`--apex` 는 진작 문턱 교차 보간인데 `--diag` 만 최근접 런이라 **같은 파일 안에서 자가 갈렸다.**

### ⓑ ⚑⚑ 재현이 등재문보다 한 겹 아래를 찍었다 — 결함은 «격자» 가 아니라 **«없는 층»** 이다

등재문(과 932 장부)은 이 갈래를 «번진 ref 의 경사면을 문턱이 버려 **얇게** 읽힌다» 로 적었다.
`probe409g.py --physics` 를 신설해(합성 층더미 · 화소도 브라우저도 안 쓴다) 같은 참값을
«칼같은 판»(cap = PNG)과 «번진 판»(ref = JPEG · σ 1.1px)으로 그려 재니 **한 겹 아래가 나왔다**:

| 판 | 옛 자(`--int`) |
|---|---|
| 참값 | `S3.00 K7.00 D4.00 B7.00` |
| 칼같은 판 | `S3.00 K7.00 D4.00 B7.00` — **정확** |
| 번진 판 | `S3.00 K6.50 **S1.50** D2.00 **F1.50** B6.50` — 층이 **4 → 6** |

두 층 사이 경사면이 **그 사이에 있는 세 번째 팔레트 색**으로 «이긴다»(K↔D 사이의 `S` ·
D↔B 사이의 `F`). 승자독식 분류라 그 표본의 몫이 통째로 **없는 층**에 간다 —
그래서 `K` 는 −0.5, `D` 는 **−2.0(−50%)** 이다. **0.5 격자는 그 결함의 겉모습**이었고,
그것만 고쳤으면 값은 소수가 되면서 **가짜 층은 그대로 남았을** 것이다.

### ⓒ 처방 — 두 가지만 바꾸고 나머지는 한 칸도 안 건드렸다

1. **가짜 층을 접는다.** 좁고(≤`PH_W` 2.0px) 이웃 두 색을 잇는 선분 위에 있고(잔차 ≤`PH_D` 14)
   **사영 t 가 훑고 지나가는**(≥`PH_T` 0.30) 런만 경사면으로 본다.
2. **표본 몫을 이웃한 두 «층» 에 비례로 나눈다**(질량 보존).

⚑ **셋째 조건 `PH_T` 가 이 회차의 핵심이다.** 처음엔 «좁다 + 선분 위» 둘로 접었는데
**진짜 층이 먹혔다** — 덫 더미 `K7 D2 B7` 에서 참값 `D` 는 `K`↔`B` 선분에서 잔차가 겨우 2.8 이라
잔차만 보면 경사면과 구분이 안 된다(실측: `S2.86 K8.52 B9.61` — D 가 통째로 사라졌다).
가르는 것은 **«t 가 훑는가»** 다 — 경사면은 표본마다 t 가 옮겨 가고(0.2 → 0.8), 진짜 층은 제자리에 뭉친다(폭 ≈0).

⚠ **`probe409i.cov_ray` 를 그대로 베끼면 안 된다.** 그 자는 몫을 «가장 가까운 **두 팔레트 색**» 에
나누는데, 이 팔레트는 `S` 가 `K`↔`D` **사이에** 있어 검정 경사면의 몫이 `S` 로 **새어 나간다**.
이 자는 **차례가 정한 이웃 층**에만 나눈다.

### ⓓ 결과

| 자 | 번진 판 ↔ 칼같은 판 |
|---|---|
| 옛 최근접 런 | **층 개수부터 다르다**(6 ↔ 4) |
| 새 자 | 층 4개 · 같은 차례 · **최대 |Δ| 0.13 px** |

ref 실측(`docs/ref/07-스킬-팝업.jpg` 네 코너 · 44층)도 같이 풀렸다 —
옛 값은 **44개 전부 0.5 의 배수**였고 새 값은 **39/44 가 비배수**다.
왼아래 코너 ref 가 `S1.45 **K6.97 D3.99 B6.98** F3.11` 로 읽혀 **352 의 «전부 7» 규약**을 되찾는다
(옛 자는 코너마다 `K5.50`·`D3.00` 처럼 흔들렸다).

### ⓔ 무르게 풀지 않았음 — 네 겹

1. **[4] 질량 보존** — 층 두께의 **합**이 두 모드에서 판·코너마다 |Δ| ≤ 0.01px 이고 창(22.5px)과 같다.
   새 자는 «더 재지» 도 «덜 재지» 도 않았다.
2. **[5] 불변** — 문턱 `EDGE_T 45` · 분류 `cls()` 6색 · 시작점 `apex()` · 걸음 0.5 가 소스에서 그대로다.
3. **[2] 덫** — 진짜 얇은 층 `D`(참값 2.0 · K·B 의 사이 색)가 **1.73 으로 살아 있다**(±0.6 안).
   같은 자리에서 옛 자는 **0.5 로 잘라 먹는다**(−75%).
4. **[R] 되돌림** — `--int` 로 옛 자가 살아 있어 지문(값이 0.5 배수 · 번진 판의 층 차례가 다르다)을
   매 실행 다시 찍는다. 옛 자는 [1-d]·[1-e] 문턱을 못 넘는다.

### ⓕ 남긴 것은 «못 고친 것» 이 아니라 **분해 한계**다

덫 더미에 `F1.15` 하나가 남는다(표본 2개짜리 경사면 — t 훑음이 `PH_T` 아래다).
접기를 더 무르게 하면 **진짜 1px 층**과 못 가른다 — σ 1.1px 번짐에서 둘은 **원리적으로 같은 그림**이다.
⇒ 일부러 남겼고 **[2-d] 가 그 판정을 항으로 들고 있다**. 장부(`probe932` `fix` 칸)에도 적었다.

### ⓖ 게이트 이관 (333 처방 — 지우지 않고 옮겼다)

- `probe932` 장부 `probe409g.py` **B → S** · `axis`·`why`·`fix` 를 이 회차 실측으로 갈아 끼웠다.
- `verify932` [2-e] 주홍 래칫 **11 → 10** · **`FIXED942` + [2-h] 신설** — «주홍이 «비어서» 줄어든 것이
  아니다» 를 못박는다(줄어드는 쪽은 늘어나는 쪽보다 조용해서 위험하다 — 932 3회차 [6-e] 와 같은 꼴).

⚠ **rebase 중에 상류 `wip(932)` 4회차와 같은 줄에서 충돌했다**(그쪽은 `probe866` 을 R 에서 뺐다).
[4] «양쪽을 모두 살린다» 대로 **RED·FIXED(932 4회차)와 FIXED942(이 회차)를 둘 다** 남겼다.

### ⓗ 회귀

`verify942` **28/28 신설** · `verify932` **47/47**(4회차분 포함 · 착수 시점 38/39 였고 그 빨강은
`scan923.py` — 923 워커의 진행분이라 내 것이 아니었다. 상류가 닫아 지금은 초록) ·
`verify409`·`verify462`·`verify352`·`verify384`·`verify449`·`verify96`·`verify866` 무수정 ·
**SMOKE PASS** · 제품 `index.html` **0줄**.

### ⓘ 남은 것 / 다음 회차

1. **B 열 자** — `probe352` · `probe384` · `probe409` · `probe409c` · `probe409f` · `probe409i` ·
   `probe449` · `scan335` · `scanA4` · `scanA4b`. **한 회차에 한 자씩**(자를 고치면 그 자를 부르는
   게이트의 못박힌 수가 같이 움직인다). 부르는 게이트: `probe352` → `verify352`·`verify47` ·
   `probe384` → `verify384` · `probe409`·`probe409f` → `verify409` · `probe449` → `verify449`·`verify384` ·
   `scan335` → `verify96`·`m335` · `scanA4`·`scanA4b` → `edgeA4`. `probe409c`·`probe409i` 는 **부르는
   노드 게이트가 없다**(다음 자리로 가장 싸다).
2. ⚑ **이 회차의 교훈을 그 자들에 먼저 대 보라** — 팔레트로 분류하는 자(`probe384`·`probe409`·
   `probe409c`·`probe409g`·`probe409i`)는 전부 같은 «사이 색이 이긴다» 함정을 안고 있다.
   `probe409i` 는 `cov_ray` 를 이미 들고 있지만 **«가장 가까운 두 팔레트 색» 에 나누므로**
   이 회차가 찾은 새는 자리(K↔D 경사면이 S 로)를 그대로 갖고 있다 — 베끼지 말고 이웃 층으로 좁혀라.
3. `scan335`·`scanA4`·`scanA4b` 는 팔레트가 아니라 **문턱 런**이라 처방이 다르다(932 §ⓓ 의 ⓑ 질량 적분).

---

## 2회차 (2026-09-05, sess-1557-12738 루틴 워커 D)

제품 `index.html` **0줄** · 레이아웃 Δ0px · 자 **1벌 수리**(`tools/probe409c.py`) ·
게이트 **`tools/verify942.js` 28/28 → 44/44**(§7 신설 16항) · 장부 1행 갱신(`probe932` `probe409c.py` **B → S**) ·
`verify932` 래칫 이관(B **10 → 9** · `FIXED942` 두 이름).

### ⓐ 왜 `probe409c.py` 를 골랐나 — 1회차가 적어 둔 순서 그대로다

1회차 §ⓘ 가 «`probe409c`·`probe409i` 는 **부르는 노드 게이트가 없다**(다음 자리로 가장 싸다)» 로 남겼고
둘 중 **작은 쪽**(112줄 ↔ 764줄)을 골랐다. 실제로 `grep -rl probe409c tools/` 는 자기 자신과
`probe409d`(주석) · `probe932`/`verify932` 뿐이라 **남의 게이트에 못박힌 수를 하나도 안 흔든다.**

### ⓑ 재현 — 결함의 크기가 «참값이 얇을수록 커지는 상수 손실» 이다

`--physics` 를 신설했다(1회차의 `probe409g.physics` 를 **걸음 1.0px** = 이 자의 열 걸음으로 그대로 부른다).
같은 참값 기둥 `S3 K? F10` 을 «칼같은 판(cap = PNG)» 과 «번진 판(ref = JPEG · σ 1.1px)» 으로 그려 두 자로 잰다:

| 참값 K | 옛 자 cap → ref | 옛 자 비 | 새 자 cap → ref |
|---|---|---|---|
| 7.0 | 7.00 → **6.00** | **−14.3%** | 7.00 → 7.16 |
| 5.0 | 5.00 → **4.00** | −20.0% | 5.00 → 5.16 |
| 3.0 | 3.00 → **2.00** | −33.3% | 3.00 → 3.08 |
| 2.0 | 2.00 → **1.00** | **−50.0%** | 2.00 → 2.16 |

⚑ **옛 자가 잃는 것은 언제나 정확히 1.00px 다** — 번짐이 검정 기둥의 **두 끝**을 각각 반 화소씩
옆 색에 넘기고 승자독식 분류가 그 화소를 통째로 버린다. 판정값이 0~7px 인 자라서
**절대량은 상수인데 비는 참값이 얇을수록 커진다**(−14% → −50%). 장부가 적어 둔 «±1px 이 곧 ±14%» 는
**가장 두꺼운 자리의 값**이었다.

### ⓒ 처방 — 셈 하나만 갈았다 (사본 0)

`col_black()` 의 `sum(1 for … if cls(...) == 'K')` 한 줄을
**«열 표본 20개를 층으로 갈라 K 층 두께의 합»** 으로 바꿨다. 그 «층으로 가르기» 는
**1회차가 세운 `probe409g.runs_from` 을 그대로 부른다** — 새 알고리즘도, 사본도 만들지 않았다
(402 «사본을 지운다» · 두 자의 팔레트 6색이 RGB 까지 같다).
부를 수 있게 `probe409g.py` 에 **`__main__` 가드 한 줄**만 달았다(셈·문턱·출력 0줄 변경).

**안 건드린 것**(= 무르게 푼 것이 아니라는 증거): 창 `span=20` · 바닥값 열 `dx 45` ·
팔레트 6색 · `cls()` · 층의 첫 차례를 정하는 규칙 · 열 자리. 옛 자는 **`--int` 로 살아 있다.**

### ⓓ 결과 — ref 실측도 정수 격자에서 풀렸다

| ref 위 코너 dx | 8 | 18 | 26 | 34 |
|---|---|---|---|---|
| 옛 자 | 13.00 | 9.00 | 3.00 | 1.00 |
| 새 자 | **12.70** | **8.59** | **3.08** | **0.77** |

옛 자는 두 코너 40열이 **전부 정수**(80/80)였고, 새 자는 0 이 아닌 값의 **59/62 가 비정수**다.
두 자의 차는 최대 **1.04px** — 정의는 그대로고 걸음만 곱아졌다.

### ⓔ 무르게 풀지 않았음 — 네 겹

1. **[7-e] 덫** — 참값 **K2.0**(옛 자가 −50% 를 내는 자리)에서 새 자는 **2.16** 이다.
   접기가 무르면 여기서 층이 통째로 사라진다.
2. **[7-f] 질량 보존** — 층 두께의 **합**이 두 모드에서 같고 기둥 전체(20.00)와 같다.
3. **[7-k~m] 불변** — 창·팔레트·분류가 소스에 그대로이고 옛 자가 `--int` 로 산다.
4. **[7-j] 정의 보존** — 두 자의 값이 열마다 ±1.5px 안이다(옛 자가 잃던 그 1px 이 값의 전부다).

### ⓕ 남긴 것은 «못 고친 것» 이 아니라 **번짐 모형의 바닥**이다

새 자의 잔차 **+0.16px** 은 참값 폭(K2·K7 둘 다 0.16)과 앞 층 두께(S3/S6/S10 에서 같은 7.165)에
**무관한 상수**다 — 팔레트 세 색이 색공간에서 한 직선 위에 있지 않아 사영이 그만큼 어긋난다.
σ 를 흔들면 6.65(σ0.6) / 7.16(σ1.1) / 5.73(σ1.8) 이라 **σ 1.8 은 이 접기의 한계**이고,
그 자리에서 옛 자도 −1.00px 이다. 장부(`probe932` `fix` 칸)에 그대로 적었다.

### ⓖ 곁다리로 고친 것 — **캡처가 없으면 즉사하던 자**

착수하자마자 이 자는 `FileNotFoundError: docs/review/96-full-hero.png` 로 죽었다.
캡처 PNG 는 **커밋 금지 자산**(ROUTINE 서두)이라 **없는 클론이 정상**인데 자가 그것을 전제하고 있었다 —
913 이 «없는 자» 로 지나가던 그 얼굴이다. 409g 선례대로 **없으면 ref 절만 돌게** 했다([7-p]).

### ⓗ 게이트 이관 (333 처방 — 지우지 않고 옮겼다)

- `probe932` 장부 `probe409c.py` **B → S** · `sig` `k0 r1 t6 i0 f0` → **`k0 r1 t15 i0 f1`** · `axis`·`why`·`fix` 재작성.
- `verify932` 주홍 래칫 **10 → 9** · `FIXED942` 에 이름 추가(= [2-h] 가 «비어서 줄었다» 를 막는다).
- `verify942` [6-b]·[6-c] 를 그 값으로 옮기고 **§7 16항 신설**.

### ⓘ 회귀

`verify942` **44/44** · `verify932` **53/53** · `verify409`·`verify352`·`verify384`·`verify449`·`verify866`·`verify96`·`verify462` 무수정 ·
**SMOKE PASS** · 제품 `index.html` **0줄**.

### ⓙ 남은 것 / 다음 회차

**B 열 9자** — `probe352` · `probe384` · `probe409` · `probe409f` · `probe409i` · `probe449` ·
`scan335` · `scanA4` · `scanA4b`. 부르는 게이트가 없는 `probe409i` 가 다음으로 싸다(단 764줄).
⚑ **이 회차가 만든 지렛대** — `probe409g.runs_from` 이 이제 **부를 수 있는 부품**이다.
팔레트로 분류하는 자(`probe384`·`probe409`·`probe409f`·`probe409i`)는 같은 함정을 안고 있으므로
**새 알고리즘을 쓰지 말고 이 부품을 불러라**(자마다 걸음만 다르게 준다 — 이 자는 1.0px, 409g 는 0.5px).

---

## 3회차 (2026-09-05, sess-1657-7061 루틴 워커 D)

제품 `index.html` **0줄** · 레이아웃 Δ0px · 자 **1벌 수리**(`tools/probe409i.py`) ·
게이트 **`tools/verify942.js` 44/44 → 65/65**(§8 신설 21항) · 장부 2행 갱신(`probe932` —
`probe409i.py` **B → S** · `probe409g.py` 신호 `t20 → t21`) · `verify932` 래칫 이관(B **9 → 8** ·
`FIXED942` 세 이름).

### ⓐ 왜 `probe409i.py` 를 골랐나 — 2회차가 적어 둔 순서 그대로다

2회차 §ⓙ 가 «부르는 게이트가 없는 `probe409i` 가 다음으로 싸다(단 764줄)» 로 남겼다.
확인 결과 그 전제가 맞다 — `grep -rl probe409i tools/` 가 **아홉 자**를 물어 오지만
(`probe409e/f/g/h` · `crop409` · `probe932`/`verify932` · **`verify462`**) 그 중 실행하는 자는
`probe932` 계열뿐이고 **나머지는 전부 주석에서 이름만 부른다.** `verify462` 가 못박은
REF_DARK 곡선(2.5 / 3.75 / 5.0)은 이 자가 아니라 **`probe409e` 의 상자 앵커 광선**이 낸 값이라
남의 못박힌 수는 한 칸도 안 움직인다(회귀 `verify462` 52/52 · `verify409` 177/177 무수정이 증인).

### ⓑ ⚑⚑ 재현이 뿌리를 **둘**로 갈랐다 — 그 둘째가 이 자 고유다

`--physics` 를 신설했다(판은 **1회차의 `probe409g` 가 그리고**, 재는 것은 이 자의 두 축이다).
같은 참값 층더미 `S3 K7 D4 B7` 을 «칼같은 판(cap = PNG)» ↔ «번진 판(ref = JPEG · σ 1.1px)» 으로 그려 재면:

| 축 | 옛 자 (번진 판) | 새 자 (번진 판) |
|---|---|---|
| ⓐ 층 자(0.5px · `gate_layers`·`norm_layers`) | `S3.00 K6.50 **S1.50** D2.00 **F1.50** B6.50` — 층 4 → **6** | `S2.89 K7.02 D4.13 B6.97` — Δ **0.13px** |
| ⓑ 덮개 자(0.25px · `cov_ray`) | `K6.22 B6.33 **F1.63** D**1.90** S**4.93**` | `K6.98 B6.97 D4.04 S3.02` — Δ **0.04px** |

⚑ **ⓑ 가 이 회차의 발견이다 — 이 자가 «처방» 으로 들고 있던 부품 자신이 새고 있었다.**
`cov_ray` 는 주석에 «같은 7px 띠가 ref 6.0 / cap 7.0» 이라고 결함을 **실측해 적어 두고**
그것을 고치려고 만든 커버리지 적분이다. 그런데 몫을 «가장 가까운 **두 팔레트 색**» 에 나누는데
이 팔레트는 `S`(43,35,26)가 `K`(0,0,0)↔`D`(65,49,34) **사이에** 있다 ⇒ 검정 경사면의 몫이
통째로 **없는 S 층**으로 간다: **S 3.00 → 4.93(+64%) · D 4.00 → 1.90(−52.5%)**.
**942 1회차가 «`probe409i.cov_ray` 를 베끼지 마라» 라고 남긴 그 자리**이고, 이번에 그것이
«베끼면 안 되는 남의 자» 가 아니라 **고쳐야 할 이 자의 결함**이었음이 수치로 나왔다.

### ⓒ 처방 — 셈 두 줄을 갈고 나머지는 한 칸도 안 건드렸다 (사본 0)

1. **층 자** — 광선 넷(`ray_box`·`ray_shape`·`diag`·`ray_normal`)이 이제 클래스 글자줄과 함께
   **그 줄을 만든 표본 색**을 들고 온다(`Ray(str)` — 호출부 0줄 변경). 두께는 `runs()` 가
   **1회차의 `probe409g.runs_from`** 을 그대로 불러 낸다.
2. **덮개 자** — `cov_ray` 의 알맹이를 `cov_from()` 으로 빼고 그 안의 «가장 가까운 두 팔레트 색»
   분배를 **차례가 정한 이웃 층**(`runs_from` ③)으로 갈았다.
3. 곁다리 — 합성 판을 그리는 셈이 둘이 되지 않게 `probe409g.physics` 에서 **판 그리기만**
   `phys_cols()` 로 뺐다(`physics` 는 그것을 부른다 · 셈은 저장소에 하나뿐이다 · [8-s]).

**안 건드린 것**(= 무르게 푼 것이 아니라는 증거): 표본 자리·각도·보폭 · 문턱 `EDGE_T 45` ·
분류 `cls()` 6색 · 창(`inn`·`out0`) · 윤곽 자(`find_box`·`contour`·`outer_edge`·`inner_edge` —
**처음부터 문턱 교차 보간이었다**) · `gate_layers`/`norm_layers` 의 «어느 층을 고르는가» 규칙.
옛 자는 **`--int` 로 살아 있다.**

### ⓓ 결과 — ref 실측도 0.5 격자에서 풀렸다

| ref D 띠(형상 앵커) | 45° | 60° | 75° |
|---|---|---|---|
| 옛 자 | 3.00 | 4.25 | 6.00 |
| 새 자 | **2.66** | **3.89** | **5.15** |

두 자의 차는 최대 **1.16px** 이고 옛 값은 **12/12 가 0.5 의 배수**, 새 값은 **12/12 가 비배수**다.
⚠ **방향이 «ref 가 두꺼워진다» 가 아니다** — 옛 승자독식이 K↔D·D↔F 경사면을 가짜 층(`S1.0`·`F1.0`)으로
세워 두고 그 몫을 D 에 붙여 주던 자리라, 접고 나면 D 는 오히려 얇아지고 K 가 두꺼워진다.
**질량은 보존된다**([8-i] 가 두 축·두 모드의 합 21.00 을 매 실행 다시 잰다).

### ⓔ 무르게 풀지 않았음 — 다섯 겹

1. **[8-n] ⚑⚑ 두 모드의 클래스 글자줄이 글자까지 같다**(12줄) — 표본 자리·분류·창을 안 건드렸다는
   가장 짧은 증거다. 바뀐 것은 «그 줄을 두께로 바꾸는 셈» 뿐이다.
2. **[8-g] 덫** — 참값 `K7 **D2** B7` 에서 진짜 얇은 D 가 **2.45** 로 살아 있다(옛 자는 **0.79** = −60%).
3. **[8-i] 질량 보존** — 두 축·두 모드·두 판의 합이 전부 21.00(기둥 전체)이다.
4. **[8-q] 불변 + 옛 자 생존** — 문턱·팔레트·윤곽 자가 소스에 그대로이고 `--int` 가 옛 값을 재현한다.
5. **[R] 되돌림 시험(직접 실행)** — ① 층 자를 옛 셈으로 되돌린 사본 → **[8-c]·[8-l] 빨강**
   ② `cov_from` 만 옛 셈으로 되돌린 사본 → **[8-e]·[8-g] 빨강**. 원복하면 65/65.
   ⚠ 되돌린 사본에서 «층 개수가 달라» 게이트가 **즉사**하던 것을 같이 고쳤다(`dmax` → `Infinity`) —
   점수 줄 없이 죽으면 스윕이 «빨강» 이 아니라 **«없는 자»** 로 지나간다(913·937 의 그 얼굴).

### ⓕ 남긴 것은 «못 고친 것» 이 아니라 분해 한계다

덫 더미에서 ⓐ 층 자가 `F1.15` 하나를 남기고(942 1회차 [2-d] 와 **같은 자리**), ⓑ 덮개 자의 D 가
2.45 로 **+0.45px** 크다. 둘 다 σ 1.1px 번짐에서 «표본 2개짜리 경사면 ↔ 진짜 1px 층» 이
원리적으로 같은 그림이기 때문이고, 접기를 더 무르게 하면 **진짜 얇은 층을 먹는다**.
[8-g]·[8-h] 가 그 판정을 항으로 들고 있고 장부(`probe932` `fix` 칸)에도 적었다.

### ⓖ 곁다리로 고친 것 — **캡처가 없으면 즉사하던 자**(2회차 ⓖ 와 같은 얼굴)

착수하자마자 `FileNotFoundError: docs/review/96-full-hero.png` 로 죽었다. 캡처 PNG 는
**커밋 금지 자산**(ROUTINE 서두)이라 없는 클론이 정상이다 — `table`·`ref_curve`·`normal_table`·
두 훑기 절 전부 **없으면 ref 절만** 돌게 했다([8-t]). ⚠ 2회차가 `probe409c` 에서 같은 것을 고쳤는데
**이 자에도 그대로 있었다** — 남은 B 열 자를 잡는 세션은 착수 첫 줄에서 이것부터 확인하라.

### ⓗ 게이트 이관 (333 처방 — 지우지 않고 옮겼다)

- `probe932` 장부 `probe409i.py` **B → S** · `sig` `k0 r1 t33 i0 f1` → **`k0 r1 t42 i0 f1`** ·
  `axis`·`why`·`fix` 재작성. `probe409g.py` 도 신호가 `t20 → t21` 로 움직여(§ⓒ-3 의 주석) 같이 갱신했다 —
  **신호가 바뀌면 판정이 무효**라 안 고치면 `[6-a]` 가 «판정 ?» 로 빨개진다(그 설계가 실제로 작동했다).
- `verify932` 주홍 래칫 **9 → 8** · `FIXED942` 에 세 번째 이름 추가(= [2-h] 가 «비어서 줄었다» 를 막는다).
- `verify942` [6-b]·[6-c] 를 그 값으로 옮기고 **§8 21항 신설**.

### ⓘ 회귀

`verify942` **65/65** · `verify932` **53/53** · `verify409` 177/177 · `verify462` 52/52 ·
`verify352` 68/68 · `verify384` 106/106 · `verify449` 42/42 · `verify866` 18/18 · `verify96` 101/101 ·
`verify47` 201/201 **전부 무수정** · **SMOKE PASS** · 제품 `index.html` **0줄**.

### ⓙ 남은 것 / 다음 회차

**B 열 8자** — `probe352` · `probe384` · `probe409` · `probe409f` · `probe449` ·
`scan335` · `scanA4` · `scanA4b`. ⚠ **여기서부터는 «부르는 게이트가 없는 자» 가 없다** —
`probe352` → `verify352`·`verify47` · `probe384` → `verify384` · `probe409`·`probe409f` → `verify409` ·
`probe449` → `verify449`·`verify384` · `scan335` → `verify96`·`m335` · `scanA4`·`scanA4b` → `edgeA4`.
자를 고치면 그 게이트에 못박힌 수가 같이 움직이므로 **한 회차에 한 자 + 그 게이트 이관까지**가 한 몫이다.
⚑ 팔레트로 분류하는 자(`probe384`·`probe409`·`probe409f`)는 **`runs_from` 을 불러라**(걸음만 다르게).
`scan335`·`scanA4`·`scanA4b` 는 팔레트가 아니라 **문턱 런**이라 처방이 다르다(932 §ⓓ 의 ⓑ 질량 적분).
⚠ 회차 상한 5 중 **3 을 썼다** — 남은 두 회차로 여덟을 못 닫는다. 다음 세션은
«한 자 = 한 회차» 를 지키되 5회차에서 **남은 자 목록과 처방을 이 파일에 남기고** 번호를 새로 등재하라.

---

## 4회차 (2026-09-05, sess-1657-7061 루틴 워커 D · 3회차와 한 세션)

제품 `index.html` **0줄** · 레이아웃 Δ0px · 자 **1벌 수리**(`tools/probe409f.py`) ·
게이트 **`tools/verify942.js` 65/65 → 79/79**(§9 신설 14항) · 장부 1행 갱신(`probe932`
`probe409f.py` **B → S**) · `verify932` 래칫 이관(B **8 → 7** · `FIXED942` 네 이름).

### ⓐ 왜 이 자를 골랐나 — «부르는 게이트» 를 먼저 확인했다

3회차 §ⓙ 가 «여기서부터는 부르는 게이트가 없는 자가 없다» 로 남겼는데, 실제로 세어 보니
`probe409f` 는 **예외**였다: `grep -rl probe409f tools/*.js` 가 `verify409` 를 물어 오지만
그 네 자리(90·267·269·618행)가 **전부 주석**이고 `verify409` 는 같은 축을 **자기 안에서 다시 잰다**
(그 자리는 이 자를 안 부른다). 88줄로 남은 자 중 가장 작기도 하다.

### ⓑ 재현 — 옛 자는 두 판을 **한 화소 통째로** 갈라 놓는다

`--physics` 를 신설했다(판은 3회차가 뺀 `probe409g.phys_cols` 가 그린다 — 번짐 셈 0줄).
검정 기둥의 **참 윗끝을 0.05px 씩** 옮기며 «칼같은 판(cap)» ↔ «번진 판(ref)» 을 두 자로 읽으면:

| 참 윗끝 | 옛 자 cap → ref | 새 자 cap → ref |
|---|---|---|
| 6.00 | 6.00 → 6.00 | 5.96 → 5.83 |
| 6.40 | 6.00 → 6.00 | 5.96 → 6.22 |
| **6.55** | **7.00 → 6.00** | 6.96 → 6.42 |
| **6.60** | **7.00 → 6.00** | 6.96 → 6.42 |
| 6.80 | 7.00 → 7.00 | 6.96 → 6.64 |

**판 사이 최대 편차 1.00 → 0.54px** · **번진 판 읽기 오차 0.60 → 0.18px**.
판정값(어깨)이 0~9px 이라 그 1px 이 곧 ±14~50% 다.

### ⓒ 처방 — 한 줄만 갈았다

`top_of_column` 의 `while y -= 1` 이 잡던 **정수 y** 를, «밝음 → 어두움» 이 문턱을 지나는 자리로
선형 보간했다(932 처방 ⓐ). 열 읽기를 `column()` 으로 뽑아 **합성 줄에도 같은 셈**이 돌게 했고
(그래서 재현이 그림을 안 쓴다), 옛 자는 `--int` 로 산다.

⚑ **좌표 규약 함정 하나를 재현이 잡았다** — 표본은 화소 **중심**에서 오므로 교차 자리에 **+0.5**
를 더해야 옛 자(= 검정인 첫 화소의 윗변)와 같은 공간이다. 빼먹은 첫 판에서는 칼같은 판이
참값 6.00 을 **5.46** 으로 읽어 «새 자가 옛 자보다 나쁘다»(cap 오차 1.04 ↔ 0.50)로 나왔다.
[9-e] 가 그 자리를 지킨다.

**안 건드린 것**: 문턱 `DARK_MAX 42` · 탐침 행 `PROBE_Y 88` · 기준선 열 x=39 ·
창 x0 14 / x1 40 · `BOX` 손 값(17회차가 «옛 읽기 재현용으로 남긴다» 고 적어 둔 그 값).

### ⓓ 결과 — ref 실측도 정수 격자에서 풀렸다

| 어깨(ref) | x16 | x17 | x18 | x19 | x20 | x21 | x22 | x25 |
|---|---|---|---|---|---|---|---|---|
| 옛 자 | 9 | 8 | 8 | 7 | 6 | 5 | 4 | 3 |
| 새 자 | **9.25** | **8.19** | **7.57** | **6.93** | **6.07** | **5.17** | **4.19** | **2.53** |

옛 값은 **26/26 이 정수**, 새 값은 **25/26 이 비정수**이고 두 자의 차는 최대 **0.53px** 이다.

### ⓔ 무르게 풀지 않았음 — 네 겹

1. **[9-d] 남은 편차 0.54px 은 «못 고친 것» 이 아니라 원리다** — 칼같은 판에는 부분 화소 정보가
   없다(계단 경계). 실제로 두 자의 **cap 오차가 0.45 ↔ 0.44 로 같다** = 새 자가 얻은 것은
   전부 **번진 쪽**이다.
2. **[9-i] 정의 보존** — 두 자의 값이 ±1.0px 안(실측 최대 0.53).
3. **[9-j~l] 불변 + 옛 자 생존** — 문턱·탐침 행·기준선 열·상자·창이 소스 그대로이고 `--int` 가 산다.
4. **[R] 되돌림 시험(직접 실행)** — 보간을 빼면 **[9-c]·[9-h]·[9-l] 빨강**(편차 1.00 · 비정수 0/26).

### ⓕ 곁다리 관측 1건 — **내 것이 아니다**

착수 시점 `verify932` 가 **58/59**(빨강 [10-d])였다. `git stash` 로 **내 변경을 뺀 트리에서도
같은 한 건**임을 대조로 확인했다(338·344 규칙) — 상류 `ac13cc2`(932 7회차)가 신설한 §10 의
캡처 의존 항이고, 바로 아래 [10-e] 는 «캡처 0장» 을 **SKIP** 으로 내는데 [10-d] 만 **빨강**으로 낸다
(캡처 PNG 는 커밋 금지 자산이라 **없는 클론이 정상**이다). 932 는 다른 세션이 lock 을 들고 있어
손대지 않았고 **PROGRESS 에 새 번호로 등재**했다.

### ⓖ 회귀

`verify942` **79/79** · `verify932` **58/59**(빨강 1건은 위 ⓕ — 착수 전과 같다) ·
`verify409` 177/177 · `verify462` 52/52 · `verify352` 68/68 · `verify384` 106/106 ·
`verify449` 42/42 · `verify96` 101/101 **전부 무수정** · **SMOKE PASS** · 제품 `index.html` **0줄**.

### ⓗ 남은 것 / 다음 회차 (⚠ 회차 상한 5 중 **4 를 썼다**)

**B 열 7자** — `probe352` · `probe384` · `probe409` · `probe449` · `scan335` · `scanA4` · `scanA4b`.
**남은 회차는 하나뿐이므로 5회차는 «한 자 + 새 번호 등재» 로 쓴다.** 처방은 자마다 이미 갈려 있다:

| 자 | 부르는 게이트 | 처방 |
|---|---|---|
| `probe384` · `probe409` | `verify384` · `verify409` | 팔레트 분류 → **`probe409g.runs_from`**(걸음만 다르게) |
| `probe352` | `verify352` · `verify47` | 런렝스 → 문턱 교차 보간(ⓐ) · 파일 자신이 결론을 폐기해 뒀다 |
| `probe449` | `verify449` · `verify384` | ref «목표값» 의 생산지라 **그 상수까지 같이 옮겨야** 한다 |
| `scan335` · `scanA4` · `scanA4b` | `verify96`·`m335` · (없음) | 팔레트가 아니라 **문턱 런** — 932 §ⓓ ⓑ 질량 적분 |

⚑ **자를 고치면 그 게이트에 못박힌 수가 같이 움직인다** — «한 자 + 그 게이트 이관» 이 한 회차 몫이다.
⚑ **착수 첫 줄에서 «캡처 없으면 즉사/빨강» 부터 확인하라** — 2·3·4회차가 세 번 연속 같은 얼굴을 만났다
(`probe409c`·`probe409i`·`probe409f` 셋 다 `docs/review/96-*.png` 를 전제하고 있었다).
