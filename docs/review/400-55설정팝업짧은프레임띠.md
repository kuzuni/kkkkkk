# 작업 400 — 55 설정 팝업(`#cfw>.cf55`)이 9:13.3 에서 탭바·HUD 를 파고든다

> 세션 `sess-1234-14747` (루틴 워커 B) · 2026-08-29 · **T4(폴리시) · 351 계열**
> 게이트 `node tools/verify400.js` · 재현기 `node tools/probe400.js [--all]` · 캡처 `node tools/cap400.js <태그>`

---

## 0. 한 줄

`#cfw` 는 `#modal` 이 아니라 **자기 오버레이**라 390 이 공용 모달에 못박은 «진짜 띠»(위 142 · 아래 180)를
한 번도 안 지났다 — 띠를 주고, 띠(1278)가 상자(1347)보다 **69px 짧아서 생기는 눌림**을
**흰공간 전체가 지분대로** 받게 했다. **제품 `index.html` 11자리 · 2280·1920 Δ0px.**

---

## 1. 재현 (338 규칙 — 처방 전에 먼저 잰다)

`tools/probe400.js` 신설. 등재문의 두 값이 **그대로 확인됐다**(338·341 처럼 기각되지 않았다).

| 프레임 | `.cf55` | 띠(`.pedge` 하변 ~ 탭바 상변) | 탭바 침범 | HUD 침범 |
|---|---|---|---|---|
| 2280 | 455..1802 (1347) | 142..2100 (1958) | −298 | −313 |
| 1920 | 275..1622 (1347) | 142..1740 (1598) | −118 | −133 |
| **1600** | **115..1462 (1347)** | **142..1420 (1278)** | **+42** | **+27** |

⚑ **재현이 등재문 처방(ⓐ «여백을 띠에 맞춰 가르기»)만으로는 안 닫힌다는 것을 같이 냈다** —
**띠 1278 < 상자 1347** 이라 69px 이 구조적으로 모자라고, 본문 안의 여유는 **11px 뿐**이다.
여백만 갈랐으면 상자가 눌리면서 푸터(`.cf55-n2`, UID/Gamer Id 줄)가 **58px 잘렸을 것**이다.
⇒ «어디서 69px 을 빼는가» 가 이 작업의 본체다.

---

## 2. 처방 — 두 자리

### ① 띠 (390 과 같은 상수)

```css
#app.shortf #cfw{padding:142px 0 180px}
```

142 = `.pedge` 하변 · 180 = 탭바. `.shortf`(frameH < 1842) 안에서만 갈라 **2280·1920 은 선언이 안 붙는다**(351-③).
142+180 = 322 는 프레임과 무관한 상수라, 짧은 프레임에서 상자는 늘 `max-height:100%` 상한에 걸려
**상변이 정확히 142 · 하변이 정확히 탭바 상변**에 선다.

### ② 눌림의 배분 — `--sq` 지분

본문 아래쪽 9자리를 `top` → **`bottom` 앵커**로 바꿨다. 값은 전부 항등식
`bottom = 1228 − (top + height)` 라 **2280 은 원소별 Δ0px**(1228 = 2280 에서의 본문 패딩상자 높이).
그 위에 눌리는 양을 변수로 얹는다:

```css
.cf55{ --sq:clamp(0px, calc(1669px - var(--frameh, 2280px)), 69px) }
```

`1669 = 1347 + 322` 이므로 `--sq` = 상자가 실제로 눌리는 px 다(1600 에서 69 · 1669 이상에서 0).
각 자리는 자기 **지분**만큼 그 값을 뺀다 — `calc(534px - var(--sq) * 0.695652)` 꼴.

---

## 3. ⚑ 1회차가 기각된 이유 — «한 장이 다 받으면 안 된다»

1회차는 «`top`+`bottom` 을 둘 다 가진 리스트 한 장만 줄어든다»(LESSONS 22-④ · 54 랭킹 처방)로 끝냈다.
기하는 완벽했다 — `probe400` 9/9 · `probe351` D7 **0건** · 2280 원소별 Δ0px · SMOKE PASS.
**그런데 비평가 둘이 각각 6·5 를 줬고, 둘 다 최저 축이 ④ 조작성이었다.**

| | 2280 | 1회차 1600 | 판정 |
|---|---|---|---|
| 행 pitch | 84.8 | **73.3** (−13.5%) | |
| 토글 컨트롤 높이 | 69 | 69 (0%) | 안 줄었다 |
| 라벨 잉크 | 30 | 30 (0%) | 안 줄었다 |
| **이웃 토글 사이 데드존** | 15.8 | **4.3** (**−73%**) | ← 여기서 갈렸다 |

두 비평가가 **각자 다른 자로 같은 결론**에 왔다: 잘림 0 · 겹침 0 · 라벨 잉크 0% 변화 ·
행 구분도 또렷 — **오직 «표적 간격» 하나**가 점수를 정했다(CH: «시각적으로는 멀쩡하고 손가락에서만 무너지는 유형»).
그리고 둘 다 같은 뿌리를 짚었다: **버튼 1개에 198px 을 쓰는 계정 패널과 푸터는 그대로 둔 채,
화면에서 유일하게 표적이 조밀한 구역에서만 69px 을 뽑았다.**

⚑ **교훈**: 「눌림은 늘고 줄 수 있는 한 장이 받는다」는 이 저장소의 관용구(22-④)인데,
**그 한 장이 «터치 표적이 늘어선 리스트» 일 때는 관용구가 틀린다.** 자로는 안 보이고 비평가만 본다.

---

## 4. 2회차 — 흰공간 전체를 같은 비율로

1600 에서 줄일 수 있는 흰공간을 전부 세면 **306px** 이다(리스트 슬랙 95 · 계정 패널 안쪽 74 ·
블록 사이 여백 126 · 본문 바닥 11). 69 ÷ 306 = **22.5%** ⇒ **모든 자리를 22.5%씩** 깎는다.

| 자리 | 2280 | 1600 | 지분 |
|---|---|---|---|
| 리스트 `.cf55-list` | 509 (행 84.8) | **488** (행 **81.3**) | 48 |
| 계정 패널 `.cf55-acc` 높이 | 198 | **181** | 17 |
| 리스트 ↔ 계정 패널 | 31 | **24** | 7 |
| 계정 패널 ↔ 버튼 3칸 | 26 | **20** | 6 |
| 버튼 3칸 ↔ 「계정 삭제」 | 34 | **26** | 8 |
| 「계정 삭제」 ↔ UID | 16 | **12.5** | 3.5 |
| UID ↔ Gamer Id | 19 | **15** | 4 |
| 본문 바닥 여유 | 11 | **8.5** | 2.5 |
| **합** | | | **69** |

**이웃 토글 데드존 4.3 → 12.3px**(2280 의 15.8 대비 −22%) · **행 pitch −4.1%**(1회차 −13.5%).
컨트롤·아이콘·글자 크기는 **0% 변화**.

⚠ **볼륨 그룹(본문 0..185)만 지분에서 뺐다** — 거기서 더 빼면 슬라이더(터치 표적)가 HUD 쪽으로
밀리는데 얻는 것이 9px 뿐이다.

⚑ **문턱이 아니라 연속으로 짰다.** `--sq` 는 `--frameh`(15812 에서 JS 가 넣는다)의 함수라
1600↔1669 사이가 매끄럽다. `.shortf` 같은 문턱으로 갈랐다면 **1669 에서 리스트가 49px 튀었을 것**이다
(15807 주석 ⓐ 가 바로 이 용도로 `--frameh` 를 만들어 뒀다).

---

## 5. 게이트 · 회귀

`tools/verify400.js` **134/134 신설** — §1 띠 · §2 Δ0px(2280 20자리 «얼린 표» + 1920 상자 기준 offset) ·
§3 지분(1회차의 440·데드존 4.3 이 다시 나오면 빨갛다) · §4 잘림·겹침 0(7프레임) ·
**§R 되돌림 시험 2건**:
- ⓐ `.shortf` 띠를 뗀 사본 → 탭바 침범 **42** · HUD **27** 이 그대로 되살아난다.
- ⓑ 리스트에 옛 `height:509px` 를 되돌린 사본 → 과잉 지정(top+height+bottom)이라 `bottom` 이 무시돼
  리스트가 안 줄고, 리스트↔계정 패널 여백이 24 → **3px** 로 붕괴한다.

**회귀 무수정**: `verify55` 66/66 · `verify52` 79/79 · `verify351` 82/82 · `verify391` 42/42 ·
`verify356` 93/93 · **SMOKE PASS** · `probe351 --only menu:conf` **D7 0건**(1600 결함 수가 20 → 17 로
2280 과 같아졌다).

측정표 `docs/measure/55-설정팝업.md` 에 **정오표**(«짧은 프레임에서는 이 표가 절대값이 아니다») 신설.

---

## 6. 회차 기록

| 회차 | 무엇 | 결과 |
|---|---|---|
| 1 | `probe400` 재현 → 띠 + 「리스트 한 장이 흡수」 | 자는 전부 초록인데 **비평가 CG 6 / CH 5** — ④ 조작성 |
| 2 | `--sq` 지분 분배로 갈아엎음 | 데드존 4.3 → 12.3 · 행 pitch −13.5% → −4.1% |
| 3 | 재채점 (CI · CJ) | **CI 8 / CJ 7** — 둘 다 최저 축이 ④ 조작성. CJ 가 뿌리를 짚었다(아래 §7) |
| 4 | 헤더·볼륨 그룹까지 지분에 편입 | 데드존 12.4 → **14.3**(2280 대비 −9.5%) · 행 pitch −1.8% |
| 5 | 재채점 (CK · CJ 재소환) | **CK 8**(①10 ②8 ③8 ④8) · **CJ 9**(①10 ②9 ③9 ④9) → **①~④ 최저 8 · 통과** |


---

## 7. ⚑ 2회차도 8을 못 받았다 — «균등하게» 라고 해놓고 두 블록을 면제했다

3회차 채점: **CI 8**(①10 ②9 ③9 ④8) · **CJ 7**(①10 ②9 ③9 ④7). 최저 7 이라 미달.
둘 다 ①②③ 은 9~10 을 줬다 — 잘림 0 · 침범 0/0 · 글자 잉크 **0% 변화**가 실측으로 확인됐다.
갈린 것은 또 ④ 하나였고, **CJ 가 그 이유를 자로 짚었다**:

> «흰공간 전부에서 균등하게» 라고 해놓고 **헤더 53px 패딩과 볼륨 블록 76px 여백 — 합쳐 129px 의
> 공짜 공백 — 을 면제해 준 채**, 그 부담을 데드존과 계정삭제 격리로 떠넘겼다.

⚠ **맞는 지적이었다.** 2회차의 «306px 을 22.5%씩» 은 **분모를 내가 고른 것**이었다 —
헤더(91px 안에 잉크 40px)와 볼륨 그룹(리스트 위 185px)을 «안 건드린다» 고 먼저 정해 놓고
남은 것만 균등하게 나눴다. 그래서 «균등» 은 남은 자리들 사이에서만 균등했다.
CJ 는 헤더에 **컨트롤이 하나도 없다**는 것을 x방향 잉크 스캔으로 못박았고(「설정」 잉크 x505–573 한 덩어리뿐,
✕ 버튼조차 없다), (a)+(b) 로 **39px** 을 손실 0 으로 뽑을 수 있다고 계산했다.

4회차는 그대로 실행했다 — 헤더 **91 → 75**(−16) · 볼륨 그룹 여백 −13 = **29px** 확보:

| | 2280 | 2회차 1600 | **4회차 1600** |
|---|---|---|---|
| 헤더 | 91 | 91 (면제) | **75** |
| 볼륨 여백(제목→슬라이더 / 「0」→구분선 / 구분선→리스트) | 10 / 12 / 19 | 10 / 12 / 19 (면제) | **8 / 9 / 14** |
| 리스트 | 509 (행 84.8) | 488 (행 81.3) | **500 (행 83.3)** |
| **이웃 토글 데드존** | 15.8 | 12.4 (−21.5%) | **14.3 (−9.5%)** |
| 계정 패널 | 198 | 181 | **187** |
| 버튼 ↔ 「계정 삭제」 | 34 | 26 (−23%) | **29 (−15%)** |
| 리스트 ↔ 계정 패널 | 31 | 24 | **27** |
| 「계정 삭제」 ↔ UID / UID ↔ Gamer Id | 16 / 19 | 12.5 / 15 | **14 / 16** |

**슬라이더 바(50px) · 토글(69px) · 버튼 3칸(99px) · 구글 카드(78px) · 글자 잉크는 세 회차 내내 0% 변화**다 —
줄인 것은 처음부터 끝까지 여백뿐이고, 4회차는 그 여백의 **분모를 화면 전체로 넓혔을 뿐**이다.

CJ 가 낸 세 번째 안 (c)(Gamer Id 36자 UUID 줄을 접어 47px 회수)는 **안 했다** —
여백 조정이 아니라 **정보 구조 변경**이라 이 작업 단위 밖이다(필요하면 별도 등재).


---

## 8. 통과 (5회차) — 두 비평가 실측 요약

| | CG(1회차) | CH(1회차) | CI(3회차) | CJ(3회차) | **CK(5회차)** | **CJ(5회차)** |
|---|---|---|---|---|---|---|
| ① 잘림 | 9 | 9 | 10 | 10 | **10** | **10** |
| ② 겹침 | 7 | 8 | 9 | 9 | **8** | **9** |
| ③ 글자 | 9 | 9 | 9 | 9 | **8** | **9** |
| ④ 조작성 | **6** | **5** | **8** | **7** | **8** | **9** |
| 최저 | 6 | 5 | 8 | 7 | **8** | **9** |

**결정 지표(이웃 토글 데드존)** — 2280 기준 15.8px:

| | 1회차 | 3회차 | **5회차** |
|---|---|---|---|
| 실측 | 4.4px | 12.4px | **14.4px** |
| 2280 대비 | −72% | −21.5% | **−8.9%** |

**세 회차 내내 0% 를 지킨 것**(두 비평가 각자 실측 확인): 토글 캡슐 69 · 「한국어」 버튼 ·
하단 버튼 3칸 99 · 구글 카드 78 · 슬라이더 바 · 「설정」 잉크 52×80 · 행 라벨 잉크 · 아이콘 6종 ·
다이얼로그 폭. **줄인 것은 처음부터 끝까지 여백뿐이다.**

### 남은 감점 3건 — 전부 «이 작업 단위로는 못 고치는» 것

- **여유 0px** (CK ② −2 · CJ ② −1): 침범은 0/0 인데 **완충도 0** 이다. 상자(1347)가 띠(1278)보다
  큰 이상 «띠 안에 여유를 두는» 기하가 없다 — 여유를 만들려면 69px 을 더 빼야 한다.
  ⚑ **390 이 공용 `#modal` 에서 이미 같은 값을 골랐다**(짧은 프레임에서 상자가 띠를 꽉 채운다) —
  55 만 다르게 하면 두 껍데기가 어긋난다.
- **헤더 여백 −44%/−38% · 구분선→리스트 −26%** (CK ③): 세 자리 다 **터치 표적·글자 잉크·아이콘에
  안 닿는** 순수 크롬 여백이다. CK 자신이 «사용자가 오조작할 수 있는 곳에서 빼서 알아채지도 못할 곳으로
  옮겼다 — 옮긴 방향이 옳다» 고 적었고, CJ 는 «내가 지목한 자리를 내가 감점하는 건 일관성이 없다» 며
  감점하지 않았다. 잉크 52×80 · 광학중심 오프셋 2.5px 은 **소수점까지 보존**됐다(CK 실측).
- **「계정 삭제」 격리 −13.2~13.5%** (둘 다 ④ 에서 언급): 파괴적 동작이라 완충이 아쉽지만
  둘 다 «7 은 아니다» 로 판정했다. 더 벌리려면 다른 자리를 −25% 아래로 밀어야 한다.

### CJ 가 남긴 «다음 예비고» (전부 여백이 아니라 **스펙 변경** — 이 작업 단위 밖)

1. 볼륨 「0 / MAX」 라벨 줄 폐지 = **−26px**(채움/빈 트랙이 이미 min·max 를 말한다)
2. 슬라이더 바 50 → 44 = −6px (⚠ 터치 표적이라 ④ 를 건드린다)
3. Gamer Id 36자 UUID 줄 접기 = **−47px**

⚑ **탭바가 두꺼워지거나 9:12 보다 짧은 화면이 생기면** 이 셋이 먼저 검토될 자리다 —
순수 흰공간에서 더 나올 수 있는 것은 **~13px 뿐**이고 그것도 이미 깎인 자리를 또 깎는 것이다(CJ 실측).
