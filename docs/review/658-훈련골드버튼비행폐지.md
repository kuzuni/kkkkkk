# 658 — 골드가 훈련 버튼 쪽으로 날아가는 연출 폐지

> 주인 지시 2026-09-01 23:40 — «골드가 훈련 버튼쪽으로 가는 연출 없애기. 존나 후지다»
> 워커 D · `sess-1458-30582` · **1회차** · 제품 `index.html` **0줄**

## 0. 한 줄

**제품은 이미 옳았다** — 660 이 확정 스펙 ③ 으로 이 축을 걷어 놨다. 이 회차가 한 일은
«걷혔다» 를 **재현으로 확인**하고, 그것을 **부품 이름이 아니라 그림(기하)으로 묻는 자**를 세워
같은 연출이 다른 부품으로 되살아나도 잡히게 만든 것이다.

## 1. 왜 제품 0줄인가 — 660 이 이미 걷었다

660(`sess-1348-13644`)의 확정 스펙 ③ 이 이 지시였다: `upFx`·`fxUpOk` 두 곳이 `fxSpend` 의
**유일한 호출부**였고 둘 다 걷혔다(`--spend-to` 신고도 같이). 660 review §1 표 ③ 행이 그 기록이다.

⇒ 338 규칙대로 **처방 전에 재현**했고, 재현이 «고칠 제품이 없다» 를 말했다. 남은 것은 **자**다.

## 2. ⚑ 이 회차의 본체 — «[D2] 가 초록인데도 지시가 깨질 수 있다»

`verify660` [D2] 는 이 축을 이미 지키고 있다. 그런데 그 자는 이렇게 묻는다:

> `fx-spd` **클래스인** 노드가 0장인가

주인이 말한 것은 클래스가 아니라 **그림**이다 — «골드가 **버튼 쪽으로 간다**». 그래서 다른 부품이
같은 그림을 다시 그리면 **[D2] 는 초록인 채로 주인 지시가 깨진다.** (같은 함정을 364 가 겪었다 —
329 가 초록이던 이유는 게이트가 사분면을 «버튼 상자 기준» 으로 쟀기 때문이었다.)

⇒ `verify658` 은 **클래스를 안 보고 기하로** 센다:

| 축 | 정의 | 기대 |
|---|---|---|
| **수렴** | `#fxl` 노드가 **훈련 카드 밖에서 태어나 카드 안에서 죽는다** | **0장** (= 폐지 대상) |
| **발산** | 버튼에서 태어나 **버튼 중심에서 멀어진다** | > 0 (660 스펙 ⑤ 버스트는 남는다) |

도착점은 마크업에 없다 — `.fx-spd` 는 transform 트랜지션으로 나므로 **찍힌 rect** 로만 읽힌다
(350 «찍힌 것으로 물어라» 의 이동 판). `#fxl` 의 `appendChild`/`remove` 를 그 순간에 후킹해
**태어난 자리와 죽은 자리**를 찍는다(`MutationObserver` 는 못 쓴다 — 660 머리말과 같은 이유).

## 3. 실측

### ⓐ 현행 main (`probe658` · 훈련 클릭 5회 + 홀드 2200ms)

| 값 | 실측 |
|---|---|
| 카드(호스트) | 326×510 @(35, 1371) |
| 버튼(`.cb` = `--burst-to`) | 310×106 @(43, 1767) |
| 골드 알약 | @(583, 31) — 카드 **밖**(y 31..79 ↔ 카드 y 1371..1881) |
| `#fxl` 총 노드 | 183장 (`fx-flash` · `fx-keep` · `fx-spark.fx-cic` · `fx-toast`) |
| **수렴** | **0장** / 관측 181장 |
| 발산(버튼 입자 거리 변화 중앙값) | **+19.9 ~ +21.3px** · 표본 138~142알 |
| `.fx-spd` | **0장** |

### ⓑ 되돌림 — 자가 눈멀지 않았다

660 이 걷은 호출(`upFx` 뒤 `fxSpend(cur, host)`)을 **자 안에서만** 되살리면 같은 측정이
**수렴 26~30장**을 본다 — 전부 **골드**(`cur='gold'`), 비행 거리 중앙값 **1,575~1,588px**,
출발 y 중앙값 **54~61**(= HUD 골드 알약). ⇒ ⓐ 의 0 은 **헛초록이 아니다.**

## 4. 함정 2개 (둘 다 자 쪽이다 — 제품은 안 건드렸다)

**ⓐ «발산» 을 상자로 세면 0 이 나온다.** 1회차에 «버튼 상자를 나갔는가» 로 쟀더니 **0장**이었다 —
버튼이 310×106 인데 버스트 반경이 ≈60px 이라 **상자 안에서 끝난다.** 상자가 아니라 **버튼 중심으로부터의
거리**로 물어야 방향이 읽힌다(멀어지면 발산 · 가까워지면 수렴). 짝 항을 잘못 재면 «자가 눈멀었다» 를
«제품이 옳다» 로 오독한다.

**ⓑ «따옴표 안에 `fx-spd`» 로 세면 주석·CSS 가 같이 걸린다.** [A2] 를 그렇게 적었더니 **3곳**으로
읽혔는데 실제 코드 자리는 **1곳**(`el.className = 'fx-fly fx-spd'`)뿐이고 나머지 둘은 **CSS 규칙과
주석 산문**이었다. 이 파일은 CSS·주석·코드가 한 파일에 있으므로 «클래스를 **붙이는** 두 문법»만 센다.

## 5. ⚑ 678 이 무엇을 지우든 이 자는 산다

678(죽은 `fxSpend` 선언째 철거)이 곧 그 함수를 걷는다. 그래서 `verify658` [R] 되돌림은
**제품 함수를 안 부른다** — «골드 알약 → 카드» 한 장을 **자가 직접 날려** [B1] 의 눈을 시험한다.
제품 함수에 매달았으면 이 자는 678 뒤에 통째로 죽었을 것이다.
`probe658` ⓑ 는 상태에 따라 기대를 바꿔 적는다(`probe583` 규약) — 선언이 있으면 제품 경로로,
걷힌 뒤에는 자가 직접 그리는 경로로 되돌린다.

⇒ **678 에게**: `fxSpend`·`fxSpendFrom`·`fxSpendT`·`FX_SPEND_*`·`.fx-spd` CSS 를 걷어도
`verify658` 8/8 은 그대로 초록이어야 한다. [A1]·[A2] 는 걷힌 뒤 **더 강해진다**(호출 0 · 자리 0).

## 6. 게이트

| 자 | 결과 |
|---|---|
| `node tools/verify658.js` | **8/8 PASS** (신설 — [A] 소스 2 · [B] 그림 1 · [C] 짝 항 4 · [R] 되돌림 1) |
| `node tools/probe658.js` | **7/7 PASS** (신설 — 재현 ⓐ 4 · 되돌림 ⓑ 2 · 소스 ⓒ 1) |
| `node tools/verify660.js` | **55/55 PASS** (무수정 — [D2]·[D6] 이 같은 축을 클래스로 지킨다) |
| `node tools/verify583.js` | **37/37 PASS** (무수정) |
| `node tools/verify625.js` | **24/24 PASS** (무수정) |
| `node tools/verify619.js` | **53/53 PASS** (무수정) |
| `node tools/smoke.js` | **SMOKE PASS** |

## 7. 곁다리 등재 1건 — 693 (`verify491` 3건)

`verify491` 이 **69/72**(간헐적으로 68/72)로 빨갛다: [6-f]·[7-d0]·[7-d1] — 셋 다 **583 화폐 축과
회당 플로터**를 묻는 항이라 659·660 이 걷은 자리와 맞닿아 있다.

⚑ **그런데 660 의 것도 아니다.** 338·344·372 규칙대로 대조 실행했다 — `cdc285e^`(660 제품 커밋 직전)의
`index.html` 로 되돌려 같은 자를 돌려도 **같은 3건 · 같은 출력**(69/72)이다. 내 회차는 제품 **0줄**이라
(내 커밋 중 `index.html` 을 건드린 것 0건) 애초에 내 것일 수 없고, 660 직전에도 빨갰으므로
**그보다 앞선 자리**다. 갈래(실재 결함 ⓐ / 게이트 부패 ⓑ)는 안 갈랐다 — 내 작업 단위가 아니다.
⚠ [6-f] 는 **간헐적**이다(두 실행 중 한 번만 빨갰다) — 착수 세션은 그것부터 확인할 것.

⚠ **번호 이동 681 → 690 → 691**(286·308 규약) — 등재 push 경쟁에서 다른 워커가 681~689 를 먼저 올렸다.
**먼저 등재된 쪽을 살리고 내 행을 옮겼다.**

## 8. ⚑ 병합 중 관측 — `681` 번호가 **표에 두 번** 있다 (내 행 아님 · 안 고쳤다)

내 행을 690 으로 옮긴 뒤에도 `awk … | sort | uniq -d` 가 **`681` 하나를 계속 뱉는다** — 다른 두 워커가
같은 회차에 각자 681 을 등재했다(«공용 `.fx-spark` 버스트 곡선…» · «다이아 → 다른 재화 교환…»).
둘 다 **내 것이 아니고 이미 origin/main 에 올라와 있어** [4] «내 작업 구간이 아닌 곳은 남의 버전을
그대로 둔다» 에 따라 **손대지 않았다**(어느 쪽이 먼저인지도 내 자리에서는 못 가른다).
⚠ **그런데 이건 그냥 두면 안 되는 종류다** — 선점은 번호로만 잠그므로(`docs/claims/<ID>.lock`)
**한 번호가 두 작업을 가리키면 두 워커가 같은 lock 을 놓고 다른 작업을 하거나 남의 작업이 통째로
막힌다**(PROGRESS «병렬 세션 규칙» 1 · 작업 281·286 이 그래서 생겼다).
⇒ **둘 중 나중에 등재한 쪽이 286 규약대로 자기 행을 표 끝 번호로 옮겨야 한다.**
