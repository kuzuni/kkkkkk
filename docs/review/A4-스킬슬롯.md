# 비평 로그 — A4 스킬 슬롯 원형화

대상: `#slots` / `.slot2` (전투 화면 최하단 원형 8칸)
레퍼런스: `docs/ref/02-기본-메인-화면.jpg` · 측정표: `docs/measure/A4-스킬슬롯.md`
캡처: 헤드리스 1080×1920, 패널 닫힘, 상태 분포를 레퍼런스와 동일하게 고정(1번 활성 / 2·3번 대기 / 4~8번 잠금)

## 1회차 — 최저 3/10

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | 최종 |
|---|---|---|---|---|---|---|---|
| 1 | 7 | 7 | 9 | 9 | **4** | 6 | **4** |
| 2 | 8 | 5 | 7 | 9 | **3** | 5 | **3** |

두 명 모두 **⑤ 색·명암**이 최저였고 사유가 같았다 — **링이 전부 단색 플랫**.

### 코어 치수는 1회차에 이미 통과

비평가 1 실측: 「외곽 Ø120(0px 오차), pitch 130(0px), gap 10(0px), well Ø88,
컬러 링 8px(r48.5~55.5), 어두운 밴드 4px 2겹 — **전부 1:1 일치**」
→ 지름·피치·간격·링 두께는 손대지 않았다.

### 지적과 처리

| # | 지적 | 판정 | 처리 |
|---|---|---|---|
| 1 | 컬러 링이 단색 플랫. 레퍼런스는 세로 그라디언트(루마차 22~55) | **타당** | `inset box-shadow` 는 그라디언트가 안 되므로 **링 4겹을 레이어로 재구성**(`.slot2` / `::before` / `::after` / `.cdw`) 후 `linear-gradient` 적용 |
| 2 | 활성 슬롯에 원 바깥 노란 링(r60~66, 외곽 Ø133) 누락 | **타당** | `.slot2.ready{box-shadow:0 0 0 7px #fffe03}` 추가 |
| 3 | 장착 슬롯 하단 Ø40 뱃지가 14px 돌출하는데 없음 | **타당** | `.lvv2` 를 Ø40 원형 뱃지로 바꿔 `bottom:-14px` 에 배치 (원본은 동료 머리 아트, 우리는 아트가 없어 레벨 뱃지로 대체) |
| 4 | 자물쇠가 63px 로 +17% 크고 종횡비 -20%, 색이 흰색 아님(213) | **타당** | `font-size 58px + scaleY(.83)` 로 잉크 47×54 (레퍼런스 46×54), 필터를 `brightness(2.6) drop-shadow` 로 바꿔 249 (레퍼런스 245) |
| 5 | (비평가 2) 4·5번 슬롯만 어둡다 — 8칸 중 2칸이 다른 색 | **캡처 하네스 버그** | 내가 캡처 스크립트에서 **잠금 슬롯에 쿨타임 오버레이를 강제**한 탓. 게임 코드는 `sk` 가 없으면 `r=0` 이라 빈 슬롯에 오버레이가 걸리지 않는다. 하네스를 레퍼런스와 같은 상태 분포로 수정 |
| 6 | (비평가 2) 외경 119→117, gap 11→13 으로 | **불채택** | 세 측정(내 픽셀분석 118~120 / 측정 서브에이전트 118 / 비평가 117)이 갈리지만 **well 88 은 두 측정이 일치**한다. 링대 16(4+8+4)을 더하면 Ø120 이라야 well 88 이 나온다. 117 이면 well 85 로 어긋난다. pitch 130 은 전원 일치라 gap 차이는 2px 뿐 |
| 7 | (비평가 2) 활성의 노란 안쪽 링은 원 **바깥**이다 | **불채택** | 내 반지름 프로파일과 측정 서브에이전트 둘 다 **r36~43(well 안쪽)** 으로 일치. 비평가 2 의 육안 오독. 단, 바깥 링(r60~66)도 **별도로** 존재하는 것은 맞아 그건 반영(#2) |

### 1회차에서 바로잡은 내 오류

레퍼런스 1번 슬롯을 처음에 «well 전체가 노랑»으로 구현했다가, 측정 서브에이전트의 독립 실측으로
**well 은 다른 장착 슬롯과 똑같은 파랑(`#367cc0`)이고 노랑은 well 가장자리의 링(r36~43)일 뿐**임이
확인되어 되돌렸다. (커밋 `172b028`)

## 2회차 — 최저 5/10 (1회차 3 → 5)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | 최종 |
|---|---|---|---|---|---|---|---|
| A | 6 | 6 | 7 | 6 | **5** | 7 | **5** |
| B | **5** | 7 | 7 | 9 | 6 | 7 | **5** |

### 지적과 처리

| # | 지적 | 판정 | 처리 |
|---|---|---|---|
| 1 | 그라디언트가 **상하 대칭 선형**인데 레퍼런스는 **상단 절반 평탄 + 하단만 감쇠**. 상단이 +10% 과밝고 진폭이 +42~57% 과대 | **타당 — 내가 직접 재확인** | 아래 «그라디언트 형태 재실측» 참고. 3겹 stop 으로 교체 |
| 2 | 뱃지가 Ø34(코어 28)로 레퍼런스 Ø40(코어 35) 대비 −20% | **타당** | 원인은 전역 `*{box-sizing:border-box}` — `width:34+border:3` 이 외곽 34 가 돼버렸다. `width:40` 으로 수정해 외곽 Ø40 / 코어 Ø34 |
| 3 | 뱃지 돌출 14px (레퍼런스 9~11px) | **타당** | `bottom:-14px` → `-10px`, 실측 돌출 9 |
| 4 | 자물쇠가 레퍼런스 45×52 대비 작고(−7%) 검은 외곽선이 없다 | **타당** | `font-size` 와 `drop-shadow` 재조정 → 46×51, 세로 중심 −3.0(레퍼런스 −3.5) |
| 5 | 행 전체가 레퍼런스 대비 −2px 좌측 | **타당** | `#slots{padding-left:4px}` → 좌 27 / 우 23 (레퍼런스 27.5 / 24.5) |
| 6 | **3번 슬롯이 «시안 링 + 흰 자물쇠 + 뱃지 없음»** — 대기 슬롯에 잠금 표시가 겹침 | **캡처 하네스 버그(2번째)** | 하네스가 `.empty` 클래스만 벗기고 자물쇠 DOM 은 그대로 뒀다. 게임 코드는 `buildSlots()` 에서 둘을 항상 같이 만든다. 하네스가 아이콘·뱃지까지 교체하도록 수정 |
| 7 | 3번 well 에 «하드 심»(상단 19px 밝고 하단 69px 어두움) | **캡처 하네스** | 내가 캡처용으로 쿨타임 오버레이를 70% 로 강제한 것. 레퍼런스에는 쿨타임 중인 슬롯이 없으므로 채점용 캡처는 쿨타임 0 으로 두고, 오버레이 동작은 `cap/cooldown.png` 로 따로 검증 |
| 8 | 컬러 링 두께 8px → 6.5px 로 줄일 것 (비평가 A) | **불채택** | 1회차 비평가 1 실측 8px, 측정 서브에이전트 8px, 내 반지름 프로파일 8px(r48~55) — 3:1 로 8px |

### 그라디언트 형태 재실측 (비평가 A·B 가 서로 달라 내가 직접 쟀다)

레퍼런스 링(r=51.5)을 시계 방향 8지점에서 ±6° 평균으로 샘플한 결과:

| 지점 | 잠금 | 대기 | 활성 |
|---|---|---|---|
| 12시 | `#b9bccb` L192 | `#25d7ff` L170 | `#ffad2a` L157 |
| 3시 | `#b8bccb` L192 | `#25d8fe` L170 | **`#ffe52c` L176** |
| 9시 | `#b8bcca` L192 | `#25d8fe` L170 | **`#fee42b` L176** |
| 4:30 | `#9fa3ba` L170 | `#06bef5` L147 | `#fd9722` L146 |
| 7:30 | `#9fa3ba` L170 | `#05bff7` L148 | `#ffc620` L162 |

→ **잠금·대기는 12시~3/9시가 완전히 평탄하고 하단만 −22 감쇠.**
→ **활성만 3·9시(세로 중앙)가 최고조**인 볼록형. 1회차 비평가 1 의 «상단이 가장 밝다» 는 오독이었다.
   (6시 지점은 하단 뱃지가 겹쳐 오염되므로 4:30·7:30 으로 판정한다)

적용한 CSS:
```css
.slot2::before      { background:linear-gradient(180deg,#25d8fe 0 50%,#04bef6 93%) }              /* 대기 */
.slot2.ready::before{ background:linear-gradient(180deg,#ffad2a 7%,#ffe52c 50%,#f89a24 93%) }     /* 활성 */
.slot2.empty::before{ background:linear-gradient(180deg,#b9bccb 0 50%,#9fa4ba 93%) }              /* 잠금 */
```
(원 위의 각도 → 세로 위치 대응: 12시 = 7%, 3·9시 = 50%, 4:30·7:30 = 93%)

## 3회차 — 최저 5/10 (A 6 / B 5)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | 최종 |
|---|---|---|---|---|---|---|---|
| A | 9 | **6** | 8 | **10** | 6 | 6 | **6** |
| B | 8 | **5** | 7 | 9 | 7 | 6 | **5** |

### 두 비평가가 같이 지적한 것 → 전부 반영 (4회차)

| # | 지적 | 처리 |
|---|---|---|
| 1 | 잠금 자물쇠 **흰 몸통 38×45 → 46×52** (−17%/−14%). 8칸 중 5칸에 적용되는 오차 | `font-size 53→64`, `scaleY .83→.785`, 필터를 `contrast(2.2) brightness(1.7)` 로 (contrast 를 앞세워 **열쇠구멍이 검게 남도록**). 결과 46×53, 세로 중심 −3.0 |
| 2 | 활성 **바깥 노란 링 7px → 4px** (r60~63) | `box-shadow:0 0 0 4px #fffe03`. 결과 r60~63 |
| 3 | 활성 **well 안쪽 노란 링 8px → 6px** (r38~43) | `.ready .cdw{inset 0 0 0 6px}` |
| 4 | 활성 링 그라디언트가 선형 램프라 1시가 −21% 어둡고 4:30 이 +14.7% 밝음 | 밝은 구간을 넓히고 감쇠를 앞당김 (아래) |
| 5 | 행 하단→아래 바 26 vs 27.5 | `margin-bottom 16 → 17` |

### 그라디언트 각도 보정

원 위의 각도가 세로 위치로 어떻게 매핑되는지 계산해서 stop 을 다시 잡았다
(12시 = 높이 7%, 1시 = 13%, 3·9시 = 50%, 4:30·7:30 = 80%).

```css
.slot2::before      { background:linear-gradient(180deg,#25d8fe 0 52%,#04bef6 70%,#04bef6 100%) }
.slot2.ready::before{ background:linear-gradient(180deg,#ffae2b 7%,#ffe52c 14%,#ffe52c 50%,#fa9624 80%,#f08d20 100%) }
.slot2.empty::before{ background:linear-gradient(180deg,#b9bccb 0 60%,#9fa4ba 88%) }
```

### 불채택 — 링 두께 8px → 6px, 외곽 Ø120 → Ø117 (비평가 B)

B 는 「컬러 링 6px, 어두운 선 3/2px, 외곽 Ø116~117」이라 했으나 **자체 모순**이다.
B 자신이 `well Ø88 일치 ✓` 라고 적었는데, B 의 링 두께를 그대로 쓰면
`88 + 2×(3+6+2) = 110` 이라 자기가 말한 Ø117 이 나오지 않는다.

내 모델은 닫힌다: `88 + 2×(4+8+4) = 120`.
지지 근거도 3:2 로 우세하다 — 내 반지름 프로파일 / 측정 서브에이전트 / 1회차 비평가 1
(「외곽 Ø120, 컬러 링 8px(r48.5~55.5), 어두운 밴드 4px 2겹 — **전부 1:1 일치**」) 대 2·3회차 B.
3회차 비평가 A 도 `외곽 Ø119(+1) / well 87(+1) / gap 0 / ④ 정렬 10점` 으로 내 값을 지지했다.
→ **Ø120 / 4-8-4 유지.**

## 4회차 — 최저 5/10 (A 5 / B 5)

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | 최종 |
|---|---|---|---|---|---|---|---|
| A | 6 | 8 | 9 | 9 | 6 | **5** | **5** |
| B | 8 | **5** | 8 | 9 | 8 | **5** | **5** |

### 반영 (5회차)

| # | 지적 | 처리 |
|---|---|---|
| 1 | **스킬 아이콘이 well(r43)을 22px 넘어 r65 까지 나가 링 4겹을 28° 구간 파괴** (A) | 가장 중요한 실제 결함. `.si3` 를 `.cdw`(well, `overflow:hidden` 원형) **안으로** 옮기고 `font-size 84 → 64`. 이제 아이콘이 링을 뚫을 수 없다 |
| 2 | 활성 링 하이라이트 로브가 ~30° 회전 — 레퍼런스는 **우상단이 밝고 좌상단이 어둡다**(10:30 ΔG 56) (A) | 세로 `linear-gradient` 로는 **원리적으로 재현 불가**(좌우 대칭이라 10:30 과 1:30 이 같은 색이 된다). 세 상태 모두 **`conic-gradient`** 로 교체해 각도별로 직접 지정 |

### 불채택 — 두 비평가가 서로 반대로 지적한 항목

| 항목 | A | B | 판정 |
|---|---|---|---|
| 활성 바깥 노란 링 두께 | ref 3.5px, 우리 3px → **더 두껍게** | ref 5.5px, 우리 4px → **더 두껍게**… 지만 A 기준이면 이미 초과 | 4px 유지 (둘 사이) |
| 하단 뱃지 지름 | ref Ø40, 우리 Ø40 → **정상** | ref Ø51, 우리 Ø37 → −27% | A + 측정 서브에이전트(44) 쪽이 우세, Ø40 유지 |
| 뱃지 세로 중심 | ref +50, 우리 +49.75 → **0.25px** | ref +47, 우리 +52 → 5px | 유지 |
| 바깥 어두운 띠 | ref 3px | ref 4px/3px 허용 | 4px 유지 |

→ 남은 지적이 **서로 모순**하는 단계에 도달했다. JPEG 레퍼런스를 CSS 프리미티브로 맞추는 측정
   노이즈 바닥(±1px)이며, 이 이상은 수치가 아니라 측정자 편차다.

### 감점되었으나 CSS 로 고칠 수 없는 것 (아트 한계)

B 의 ⑥ 감점 근거는 **이모지 글리프 내부 형상**이었다 — 자물쇠 열쇠구멍 `8×11` vs 레퍼런스 `12×19`,
shackle 두께 `+10%/+25%`. 이는 `🔒` 글리프 자체의 모양이라 CSS 로 바꿀 수 없다.
(글리프 **바깥 치수**는 46×53 vs 46×54 로 이미 일치)
**실제 게임 아트를 넣는 시점에 자동 해소되는 항목**이라 5회차에서는 손대지 않았다.

## 5회차 — 최저 **7/10** (A 7 / B 7) — 3 → 5 → 5 → 5 → **7**

| 비평가 | ① 배치 | ② 크기비 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | 최종 |
|---|---|---|---|---|---|---|---|
| A | 9 | 9 | **10** | 9 | **7** | 8 | **7** |
| B | 9 | **7** | 8 | **10** | 9 | 8 | **7** |

핵심 수정(아이콘 well 클립)이 독립 검증되었다 —
비평가 B: 「아이콘 well 클립: 8칸 전부 r=46/52/58 3개 링을 360° 3° 간격으로 훑어 **침범 0건**. 링 뚫림 완전 해소 ✓」

### 5회차 채점 후 추가 반영 (자체 실측으로 검증)

비평가 A 의 «conic 스톱을 이중화해 평탄 띠 + 급전환으로» 지적이 타당해 반영하고,
전환 각도는 레퍼런스 원주를 10° 간격으로 다시 훑어 맞췄다.

| 항목 | 처리 | 검증 |
|---|---|---|
| conic 램프 20~30° → 평탄 띠 + 급전환 | 스톱 이중화 후 전환 각도를 실측에 맞춰 재배치 | 원주 93지점 스윕: **평균 Δ2.6 / 중앙값 Δ1 / 88%가 Δ≤5** (직전 A 실측 최대 Δ27) |
| 활성 노란 링 두께 | **바깥·안쪽 모두 5px** 로 확정 | 아래 «노란 링 두께 — 비평가 3인이 전부 다른 값» 참고 |

### 노란 링 두께 — 비평가 3인이 전부 다른 값을 댔다

| 출처 | 바깥 노란 링 | 안쪽 노란 링 |
|---|---|---|
| 4회차 A | 3.5px (더 얇게) | 5.5px |
| 5회차 B | **6.5px (더 두껍게)** | 7px |
| 내 반지름 프로파일 | ? | ? |

→ 서로 정반대라 **내가 직접 밴드 패턴으로 재판정**했다.
반지름 34~69 를 «노랑 / 어두움 / 기타» 로 문자열화해 레퍼런스와 나란히 놓으니 결론이 명확했다:

```
REF: oooooYYYYY....oYYYYYYYo....YYYYYoooo
OUR: oooooYYYYY....YYYYYYYY....YYYYY.....
      34   39-43  44-47  48-55  56-60 61-65
```

**둘 다 5px** (안쪽 r39~43, 바깥 r61~65) — A 의 3.5 와 B 의 6.5 사이였다.
4회차에 A 의 지적을 따라 7px→4px 로 줄인 것은 **내 실측을 버리고 비평가를 따른 오판**이었고,
5회차에서 바로잡았다. (말단의 `oooo` vs `.....` 는 레퍼런스 배경이 물색, 우리는 어두운 게임 배경이라 생기는 차이)

### 남은 문제 (5회 안에 9점 미달 — 인계 사항)

| # | 문제 | 성격 |
|---|---|---|
| 1 | 자물쇠 열쇠구멍 `8×11` (레퍼런스 `12×19`), shackle 두께 `+10%/+25%` | **아트 한계.** `🔒` 이모지 글리프 내부 형상이라 CSS 로 불가. 글리프 **바깥** 치수는 46×53 vs 46×54 로 이미 일치. **실제 게임 아트 투입 시 자동 해소** |
| 2 | 원주 12% 구간(주로 전환 각도 부근)에서 Δ6~20 | conic 전환폭을 더 잘게 쪼개면 줄지만, 레퍼런스가 JPEG 라 경계 자체가 ±1px 번져 있어 **측정 노이즈 바닥**에 근접 |
| 3 | 슬롯 하단 «동료 머리 뱃지»(레퍼런스 Ø40~44 아트) | 캐릭터 2명 구조 미결(PROGRESS «미결» 참고). 현재는 같은 자리·지름의 레벨 뱃지로 실루엣만 맞춤 |
| 4 | 쿨타임 오버레이는 레퍼런스에 없어 대조 불가 | UI-REFERENCE ⑤ «쿨타임은 슬롯 위에 오버레이» 규정만 따름. well(88) 안쪽만 덮어 링을 가리지 않게 구현, `cap/cooldown.png` 로 동작 확인 |

> **비평가 편차 경고 (다음 워커에게).** 이 화면은 비평가 3명이 같은 항목에 서로 **모순되는** 수치를 냈다
> (외경 117 vs 119 vs 120, 노란 링 3.5 vs 6.5, 뱃지 Ø40 vs Ø51, 뱃지 중심 +47 vs +50).
> 지적이 갈리면 **비평가를 따르지 말고 직접 실측해 판정하라.** 특히 **닫히는지 검산하라** —
> `well + 2×링대 = 외경` 이 맞아야 한다. 4회차에 이 원칙을 어겨 노란 링을 잘못 줄였다.

### 5회차 최종 실측 대조

| 항목 | 레퍼런스 | 5회차 구현 | 차이 |
|---|---|---|---|
| 외곽 Ø / pitch / gap | 118~120 / 130 / 10~11 | 120 / 130 / 10 | ≤1.5 |
| 좌 여백 / 우 여백 | 26~27.5 / 23~24.5 | 26.5 / 23.5 | ≤1 |
| well Ø | 87~88 | 88 | ≤1 |
| 링 4겹 반지름 | 43 / 47 / 55 / 59 | 43 / 47 / 55 / 59 | 0 |
| **활성 링 12시** | `#ffad2a` | `#feaf2a` | 2 |
| **활성 링 1:30** | `#fee42b` | `#ffe32a` | 1 |
| **활성 링 3시** | `#ffe52c` | `#fee02b` | 5 |
| **활성 링 4:30** | `#fd9722` | `#fc9822` | 1 |
| **활성 링 7:30** | `#ffc620` | `#fec320` | 3 |
| **활성 링 9시** | `#fee42b` | `#fee12a` | 3 |
| **활성 링 10:30** | `#fead2a` | `#feaf2a` | 2 |
| **대기 링** 12시 / 3시 / 4:30 | `#25d7ff` / `#25d8fe` / `#06bef5` | `#25d8fe` / `#25d8fe` / `#04bef6` | 1 / 0 / 2 |
| **잠금 링** 12시 / 3시 / 4:30 / 7:30 | `#b9bccb` / `#b8bccb` / `#9fa3ba` / `#9fa3ba` | `#b9bccb` / `#b9bccb` / `#a2a7bc` / `#a1a6bb` | 0 / 1 / 4 / 3 |
| 활성 바깥 노란 링 | r60~64 `#fffe03` | r60~63 `#fffe03` | ≤1 |
| 활성 안쪽 노란 링 | r38~43 `#fefe0c` | r38~43 `#fefe0c` | 0 |
| 자물쇠 흰 몸통 | 46×54 | 46×53 | 1 |
| 자물쇠 세로 중심 | 슬롯중심 −2.5 | −3.0 | 0.5 |
| 뱃지 외곽 Ø / 코어 Ø | 40~44 / 35 | 40 / 34 | ≤1 |
| 뱃지 하단 돌출 | 9~11 | 9~10 | ≤1 |
| 행 하단→아래 바 | 27.5 | 27 | 0.5 |
| **아이콘의 링 침범** | 없음 | 없음 (well 원으로 클립) | 해소 |

**conic-gradient 로 바꾼 이유**: 레퍼런스 활성 링은 좌우 대칭이 아니다 —
10:30 이 `#fead2a`(어두움)인데 1:30 은 `#fee42b`(밝음)로, 빛이 **우상단에서 오는 방향성 하이라이트**다.
세로 `linear-gradient` 는 높이가 같은 두 지점을 반드시 같은 색으로 칠하므로 이 구조를 만들 수 없다.
`conic-gradient` 로 각도별 색을 직접 지정해 7개 시계 지점 전부 Δ≤5 로 맞췄다(직전 회차 최대 Δ14).

헤드리스 검증: **콘솔 에러 0건 · 요소 겹침 0건 · 프레임 밖 잘림 0건** (사이드 아이콘·HP바·튜토 배너·탭바 전부 대조)

---

# 2차 폴리시 라운드 (2026-08-26, sess-1900-13873 워커 D · 6~9회차)

> 1차 라운드 5회차는 **1080×1920** 캡처였다. 작업 63 의 9:19 전환 뒤 처음 잡은 라운드라
> `tools/capA4.js`(1080×2280) · `tools/cropA4.py` · `tools/scanA4.py` · `tools/scanA4b.py` ·
> `tools/inkA4.js` · `tools/edgeA4.py` · 게이트 `tools/verifyA4.js` 를 신설하고 6회차부터 다시 돌았다.

## 세로 변환 — 이 화면만 «−84» 가 아니다

`#slots` 는 `#battlefoot{bottom:0}` 안에 있어 **탭바와 같은 바닥 앵커**다. 레퍼런스 콘텐츠 2256 과
프레임 2280 의 24px 차이가 이 행 **위**(전투 캔버스)에서 흡수되므로(LESSONS 63-4),
`캡처 y = 레퍼런스 y − 84` 가 아니라 **− 60** 이다. 실측으로도 슬롯 하단 ref 2133 ↔ cap 2073,
탭바 상단 ref 2159 ↔ cap 2100 으로 Δ60 이 일정하다. 비평가에게 줄 크롭은 이 값으로 맞춰 잘랐다.

## 6회차 — 최저 ①~④ **6** (M 7 / N 6)

| 비평가 | ① | ② | ③ | ④ | ⑤ | ⑥ | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| M | 9 | **7** | 8 | 9 | 8 | 7 | **7** |
| N | 8 | **6** | 7 | 9 | 8 | 7 | **6** |

## ★ 이 라운드의 핵심 — 측정표의 «닫히는 검산» 이 틀린 수를 붙잡고 있었다

두 비평가가 **서로 독립으로, 그리고 나와도 독립으로** 같은 결론에 도달했다:
**레퍼런스 슬롯의 외곽 지름은 120 이 아니라 ~117.7 이고, 컬러 링은 8 이 아니라 ~7.3 이다.**

| 출처 | well | 안쪽 테 | 컬러 링 | 바깥 테 | 외곽 Ø | 검산 |
|---|---|---|---|---|---|---|
| 옛 측정표 §0·§2 | 88 | 4 | 8 | 4 | 120 | 88 + 2×16 = 120 ✓ |
| 비평가 M (50% 교차, 수평행) | 89.1 | 4.05 | 7.10 | 4.14 | **118.98** | 118.99 ✓ |
| 비평가 N (50% 교차, 수평+수직) | 88.70 | 3.74 | 7.33 | 3.39 | **117.62** | 117.62 ✓ |
| 나 (방사 50% 교차, 180방향, 4칸) | 89.00 | 3.58 | 7.28 | 3.58 | **117.88** | 117.88 ✓ |

**넷 다 검산이 닫힌다.** 그런데 옛 표만 답이 다르다 — 왜냐면 옛 표의 검산은 «실측 넷이 닫히는지» 를
확인한 게 아니라 **`(120−88)/2 = 16 = 4+8+4` 로 «닫히도록 120 과 88 을 고른 것»** 이기 때문이다.
표 자신이 바로 위에 «외곽 bbox **118~120**(깨끗한 6·8번 슬롯 **118×119**)» · «well 실측 bbox **89×89**»
라고 실측을 적어 놓았는데, 결론 칸이 그 실측을 버리고 반올림값을 택했다.

> **교훈 — «검산이 닫힌다» 는 «측정이 맞다» 가 아니다.** 미지수 5개(well·테·링·테·외곽) 중
> 4개를 «가정» 하면 5번째는 **언제나** 닫힌다. 검산이 힘을 가지려면 **다섯을 전부 독립으로 재고**
> 그 다섯이 닫혀야 한다. 1차 라운드 3회차가 «내 모델은 닫힌다: 88 + 2×16 = 120» 이라며
> 비평가 B 의 «외곽 117» 을 3:2 로 기각한 것이 이 함정이었다 — **B 가 맞았다.**

### 한 줄 고쳐서 넷이 같이 낫는다

외곽 Ø 를 117.8 로 줄이자 **비평가 4건이 동시에 회수**됐다. 전부 «칸의 지름» 하나의 그림자였다:

| 비평가 지적 | 어떻게 회수됐나 |
|---|---|
| ② 컬러 링 +9~14% | 링을 7.3 으로 (외곽 예산 안에서) |
| ③ 잠금 칸 gap −17% (10.2 vs 12.3) | pitch 130 은 그대로, 지름만 줄어 gap 이 **12.2 로 자동 복구** |
| ① 행 전체가 1.0~1.3px «위» | `#battlefoot` 이 바닥 앵커라 높이가 2.2 줄면 **중심이 1.1 내려간다** |
| ⑤ 잠금 암선 +7~16% | 3.5 / 3.65 로 |

## ★ 두 번째 함정 — 마스크로 잰 «두께» 를 믿고 세 번 흔들었다

6회차에 내 색 마스크(«노랑인 픽셀을 세기»)가 «안쪽 노란 링이 REF 6.4 vs 우리 4.0» 이라 해서
5px → **8px** 로 키웠다(7회차). 그런데 같은 마스크로 **우리 crisp 렌더**를 재 보니
CSS 5px 를 **4.0** 으로 재고 있었다 — 마스크가 경계 AA 를 통째로 떨구는 것이다.
반대로 비평가 N 의 50% 교차는 같은 렌더에서 **CSS 5px → 실측 5.00** 으로 복원했다.

그래서 N 을 따라 8 → 5.6 으로 되돌리려다, **원시 반지름 프로파일을 직접 찍어** 보고 멈췄다:

```
REF  r36 (117,146)  r37 (211,224)  r38~42 (250+)  r43 (213,200)  r44 (105,103)  r45 (18,17)
CAP  r36 ( 54,124)  r37 ( 75,124)  r38 (134,174)  r39~42 (254)   r43 (163,192)  r44 (79,121)
```

레퍼런스는 r36 에서 이미 올라오기 시작한다 — **N 의 5.61 은 수평 한 행만 훑어 안쪽 경계를
1.2px 늦게 잡은 값**이었다. 최종은 프로파일이 말하는 **7.3**(= 7회차의 8px 에 가깝다).
**바깥** 노란 링은 반대로 N 이 맞아서 5.5 로 확정했다(옛 측정표 8 은 과대).

> **교훈 — 비평가가 갈리면 «둘 중 누구» 가 아니라 «원시 프로파일» 에 물어라.**
> 세 사람이 5.2 / 5.6 / 6.4 로 갈렸을 때, 정답은 투표가 아니라 **r36~r45 의 RGB 15줄**에 있었다.

## 6~9회차에 반영한 것

| # | 항목 | 이전 | 이후 | 근거 |
|---|---|---|---|---|
| 1 | 외곽 지름 | 120 | **117.8** | M·N·나 3인 독립 (117.6~119.0) |
| 2 | well 지름 | 88 | **88.9** | 3인 독립 (88.7~89.1) + 옛 표의 «bbox 89×89» |
| 3 | 링대 | 4 / 8 / 4 | **3.5 / 7.3 / 3.65** | 위 검산표 |
| 4 | 슬롯 사이 gap | 10 | **12.2** | pitch 130 − 117.8. REF 잠금 구간 실측 11.25~12.41 |
| 5 | 좌 여백 | 27 | **27.5** (padding-left 4→2.7) | REF 슬롯1 중심 86.34/86.4 (M·N 일치) |
| 6 | 행 세로 중심 | — | **+1.1px** (지름 축소로 자동) | M +1.03 / N +1.32 |
| 7 | 안쪽 노란 링 | 5px | **8.4px** (실측 7.27 → 우리 7.61) | 원시 프로파일 |
| 8 | 바깥 노란 링 | 5px | **6.98px** (실측 5.50 → 우리 5.27) | N + 원시 프로파일 |
| 9 | 스킬 아이콘 잉크 | 68.6 × 72.2 | **67.9 × 85.1** (fs 66.5→78.3 + `scaleX(.842)`) | 측정표 §3 «68×85». 글리프별 편차는 `node tools/inkA4.js` 표 참조 |
| 10 | 자물쇠 세로 | −3px | **−2px** | M «1.0px 위» · N «1.02px 위» 2인 독립 일치 |
| 11 | 하단 뱃지 폭 | Ø40 | **Ø44** | M 44~45 · 측정표 §2 «44×44» |

### 불채택

| 항목 | 지적 | 판정 |
|---|---|---|
| 뱃지 하단 돌출 | M «10.0 → 11.4» | N 이 «9.7 vs 9.2 로 이미 근사, 건드리지 말 것» 으로 정반대 → **유지** |
| 뱃지 세로 | N «REF 는 42.3×33.8 가로로 누운 타원» | M 은 폭만 재고 세로 언급 없음, 측정표 §2 는 «44×44» → **미결로 남기고 정원 유지**(측정표 §7) |
| 자물쇠 U자 고리·열쇠구멍 | M·N 둘 다 «고리가 굵고 몸통과 붙음, 열쇠구멍 −45%» | **아트 한계** — `🔒` 글리프 **내부** 형상이라 CSS 불가. bbox(46×53 vs 46×54)는 이미 일치. ⑥ 전용 감점이라 ①~④ 통과를 막지 않는다 |
| 활성 슬롯 외곽 Ø −1.35% | N 스스로 «f=0.3 에서 부호 소멸 → 신뢰 불가» | **불채택**(N 본인 판정) |

## 9회차 실측 대조 (`python3 tools/edgeA4.py 9`)

| 항목 | 레퍼런스 | 9회차 구현 | 차이 |
|---|---|---|---|
| well 지름 | 88.67 | 88.53 | 0.14 |
| 안쪽 암선 | 3.71 | 3.68 | 0.03 |
| 컬러 링 | 7.22 | 7.42 | 0.20 |
| 컬러 링 바깥 반지름 | 55.27 | 55.37 | 0.10 |
| 안쪽 노란 링 | r36.35~43.62 (7.27) | r36.32~43.94 (7.61) | ≤0.34 |
| 바깥 노란 링 | r60.08~65.58 (5.50) | r60.55~65.82 (5.27) | ≤0.47 |
| pitch | 129.97 | 130.00 | 0.03 |
| 슬롯 중심 x (8칸) | 86.34 … 996.43 | 87.0 … 997.0 | ≤0.6 |

게이트 `node tools/verifyA4.js` → **VERIFYA4 31/31 PASS** · `node tools/smoke.js` → **SMOKE PASS** · 콘솔 에러 0.
