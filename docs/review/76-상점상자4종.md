# 76 — 상점 소환 상자 4종 (무기·방패·목걸이·스킬, «방어구» 명칭 폐기)

- 작업 종류: **T2 기능(저장소 주인 지시, 2026-08-25)** → 지시서 [3]-(가) — 비평 루프 없음.
- 세션: sess-1106-22264 (2026-08-25) · 1회차 완료.
- 게이트: **VERIFY76 22/22 PASS** (`node tools/verify76.js`) · **VERIFY73 64/64 PASS** · **VERIFY61 79/79 PASS** · **SMOKE PASS**.
- 캡처: `docs/review/76-r1-shop.png` (1080×2280, 카드 4장).

## 무엇을 바꿨나

1. **`SHOP_BOXES` 3장 → 4장** — `weapon «무기 상자»` · `shield «방패 상자»`(«방어구 상자» 개명) ·
   **`amulet «목걸이 상자»` 신설**(📿, 호박/앰버 톤 `#A07A2C/#B4924E/#F0C05A→#C08A3C` — 측정표 10 §5) ·
   `skill «스킬 상자»`. 순서 무기→방패→목걸이→스킬. 카드 기하는 기존과 동일(레이아웃 변경 0) —
   4장 총높이 1887 < 리스트 가용 2094 라 스크롤 없이도 다 들어간다.
2. **가격**: `BANNERS.amulet` 은 73-④ 때 이미 `cost:100·flat:1` — 10회 1,000 · 30회 3,000 그대로 적용됨(코드 0줄).
3. **무료 10연**: `daily.freeSum` 은 `SHOP_BOXES` 에서 생성 + `freeLeft()` 의 null→`SHOP_FREE` 폴백이라
   구 세이브(amulet 키 없음)도 자동 호환 — **손댄 코드 0줄, 검증만 추가**(§4ⓐ).
4. **11 확률 정보 팝업**: 카드 🔍 → `openProbInfo('amulet')` 자동 연결(위임 `data-shinfo`) — 코드 0줄, 검증만(§5).
5. **가이드 미션(⑥ — 73 담당 몫을 이 지시가 확정)**: idx5 «방어구 1회 소환하기» → **«방패 1회 소환하기»**
   (보상을 `() => summonCost('amulet',10)` = 1,000 으로 — 보상 체인 규칙) · **idx6 에 «목걸이 1회 소환하기» 삽입**
   (`ban:'amulet'`, `go: gmShop('amulet')`, 보상 500). 체인 20 → 21개.
6. **세이브 이관**: `GUIDE_V` 2→3. `load()` 에서 gv≤2 & idx≥6 이면 **idx +1**(같은 미션을 계속 가리킴 —
   삽입점 앞은 그대로, 완주(20)는 21로 클램프). 기준선은 기존 규칙대로 -1 리셋. KEY 불변.
7. **03 던전 해금 기준 이동**: `DUN_UI.gm` 14/17/20 → **15/18/21** — 해금 마일스톤 미션(스테이지 15/25/40 도달)이
   삽입으로 한 칸 밀렸기 때문. 구 세이브도 idx 이관(+1)과 같이 움직여 **해금 상태 보존**(idx<6 은 양쪽 다 안 밀림).

## 기능 체크 표 (헤드리스 실측 — verify76.js)

| 누른 것 / 상황 | 무엇이 바뀌는지 (실측) | 게이트 |
| --- | --- | --- |
| 상점 열기 | 카드 4장, 이름 «무기/방패/목걸이/스킬 상자», DOM 에 «방어구» 0건, 헤더 색 4종 상이 | §1 ✓ |
| 목걸이 💎10회 소환 | 다이아 −1,000 · `S.cnt.sumEquip` +10 · `S.sum.amulet` exp 증가 · 획득 전부 amulet 부위 | §2 ✓ |
| 목걸이 무료 10회 ×3 | 무료횟수 2→1→0, 3번째는 «무료 소환 소진(목걸이)» 팝업 + 상태 불변, 무료로 20개 획득·다이아 0 유지 | §3 ✓ |
| 구 세이브 로드(3상자 시절) | `freeLeft('amulet')`=2 폴백 · `S.sum.amulet={lv1,exp0}` 생성 · gv2/idx7→idx8 · idx5→5 · gv3 무이관 · 완주 20→21 | §4 ✓ |
| 카드 🔍 | `#prbw` 열림 · `prbBank='amulet'` · 확률 목록 8행 전부 목걸이 | §5 ✓ |
| 가이드 배너(미션 7 미완) 클릭 | 상점 열림 + 목걸이 카드가 뷰 안, idx5 이름/보상·idx6 ban 확인 | §6 ✓ |
| 콘솔 | 에러 0건 | §7 ✓ |

추가 회귀: verify73 §5 «미션 21개 전수 이동» 전부 ✓ (방패/목걸이 상자 스크롤 포함) · verify61 §10 던전 해금 새 번호로 ✓ · smoke 4화면비 ✓.

## 게이트 정비 (이 작업에서 같이 고침)

- `tools/verify73.js` — 새 체인(21개)·4장 기준으로 갱신(§2 amulet 유료 차감 추가 · §3 ban 4개 · §5 인덱스 +1).
- `tools/verify61.js` — §0 21개 · §2/§4 인덱스 +1 · §10 gm 15/18/21 · §8 클램프 21.
  **§0 «보상 전부 다이아» 는 73 때부터 잠재 FAIL 이었다** — 함수형 `dia` 가 `evaluate` JSON 직렬화에서
  undefined 로 떨어진다 → `gmDia(m)` 로 읽게 고침(선언이 아니라 실효값 검사라 오히려 강해짐).
- `tools/verify76.js` 신설(위 표) · verify73/76/32 에 smoke.js 와 같은 **브라우저 폴백**(`/opt/pw-browsers/chromium`)
  추가 — 러너의 playwright 버전이 번들 경로와 어긋나면 launch 가 죽었다.

## 남은 문제 (76 범위 밖)

1. **verify32 «젬 transform 없음» 1건 FAIL 은 기존부터** — 변경 전 index.html 로도 같은 실패
   (`matrix(0.94,0,0,0.79)`). 배너 보상칸 💎 의 잉크 보정 transform 을 두고 32 게이트의 두 검사가
   서로 모순(«보정 있음» OK + «없음» FAIL) — 32/35 담당이 게이트 의도를 정리할 것. 내 구간이 아니라 안 건드림.
2. **펫 소환 경로는 여전히 막다른 길**(26 동료 시트 → 상점에 펫 카드 없음) — 76 지시가 «펫 상자 추가 안 함» 을
   확정했으므로, 26 담당이 [동료 소환] 버튼의 도착지(룰렛 안내 등)를 정해야 한다.
3. 목걸이 상자 아트는 이모지 📿 대체 — 측정표 10 §5 «아트 필요» 표에 기록.
