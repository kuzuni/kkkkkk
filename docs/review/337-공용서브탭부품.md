# 337 — 공용 서브탭 부품 `.stabs` / `.stab` 재측정

> 등재: 2026-08-28, sess-1521-8678 워커 A (335 2회차 · 비평가 AL·AM·AN·AO 4인)
> 착수: 2026-08-28, **sess-1700-10797 워커 D**
> 호스트 **7곳** — 03 던전 · 06 장비 · 07 스킬 · 10 상점 · 13 재화 · 23 훈련 · 47 룬

---

## 1~3회차 (2026-08-28, sess-1700-10797) — ①②③④⑤ 전부 · **①~④ 최저 8 / 8 통과** (제품 **10줄**)

### 0. 이 회차가 한 일 한 줄

**«비평가가 갈린 수치» 를 픽셀로 확정했다** — 등재문 ① 은 −14.3 ~ −21% 로 넷이 갈렸는데,
서로 다른 글자를 비교했기 때문이다. **같은 문자열**(07 «스킬»/«장비», 03 «던전»)이 ref 와 캡처
양쪽에 다 있으므로 그것만 한 마스크로 재면 서체 차이가 안 끼어든다.

### 1. 새 도구 3개

| 도구 | 하는 일 |
|---|---|
| `tools/cap337.js` | 03 던전 페이지를 1080×2280 으로 찍는다(토스트·파티클 끔) |
| `tools/scan337.py` | ref/캡처의 **같은 열**을 세로 단면으로 나란히 찍는다 — 알약 베벨(②)용 |
| `tools/ink337.py` | **같은 문자열**의 라벨 잉크 bbox 를 ref/캡처에서 같은 마스크로 잰다 — ①④ 용 |
| `tools/probe337.js` | 라벨을 키운 뒤 **7 호스트 전부**에서 칸을 넘지 않는지 잰다 |

### 2. 실측 — 라벨 잉크 (①④)

캡처는 `docs/review/96-full-*.png`(기존 `tools/cap96.js` 하네스). ref y 는 **하단 앵커 −60**(335 정오표).

| 표본 | ref | 착수 전 | Δ | 수리 후 | Δ |
|---|---|---|---|---|---|
| 07 활성 «스킬» | 67×38 · cy 2005.5 | 54×30 · cy 2009.5 | −19.4% / −21.1% · **+4.0px** | **67×38 · cy 2005.5** | **Δ0 / Δ0 / Δ0** |
| 07 비활성 «장비» | 71×38 · cy 2010.5 | 61×34 · cy 2005.5 | −14.1% / −10.5% · **−5.0px** | **71×38 · cy 2010.5** | **Δ0 / Δ0 / Δ0** |
| 03 활성 «던전» | 69×37 · cy 2005.0 | 54×31 · cy 2010.0 | −21.7% / −16.2% · **+5.0px** | 68×37 · **cy 2005.0** | −1.4% / Δ0 / **Δ0** |

**최대 크기 편차 21.7% → 1.4%.**

### 3. 뿌리 — «활성만 작았다», 그리고 그 근거는 이미 폐기된 표본이었다

두 ref 가 **활성과 비활성을 같은 크기로 그린다**(둘 다 잉크 높이 38). 우리만 활성이 비활성보다
작았고(30 vs 34), 그 값을 넣은 것은 **126 ② 14회차**(`fs 41` + `scaleY(.937)`)와
**126 ① 15회차**(`.stab.on>i{top:8px}`)다. 둘 다 **10 상점 ref** 로 잰 것인데,
그 10·13 서브탭 실측치는 그 뒤 **96 정오표로 폐기**됐다(측정표 10 §3 · 13 §8).
살아 있는 ref 는 **03 §4-3 · 07 §9 둘뿐이고 둘이 같은 말을 한다** → 두 상태를 한 값으로 되돌린다.

⚠ **96 의 주인 지시 ⓐ(«선택됐을 때 너무 과해 보인다») 를 되돌린 것이 아니다.**
그 지시가 만든 것은 **알약**(면·베벨·radius·`line-height` 차)이고, 라벨 **크기**를 활성만 줄인 것은 126 이다.

### 4. 제품 변경 — 라벨·오프셋 (1회차, index.html 4줄)

| 자리 | 전 | 후 | 근거 |
|---|---|---|---|
| `.stab{font-size}` | 43px | **47px** | ① 잉크 높이 34→38 = ref |
| `.stab>i{transform}` | `scaleX(.914)` | **`scaleX(.97)`** + `position:relative;top:3px` | ① 폭 · ④ 세로 |
| `.stab.on{font-size}` | 41px | **47px** | ① — 두 상태 동일이 ref |
| `.stab.on>i` | `top:8px; scaleX(.893) scaleY(.937)` | **`top:3px`** (크기는 `.stab>i` 가 한 곳에서) | ①④ |
| `.stab.on{background}` | `0 9px / 10px 72px / 72px 78px` | **`0 8px / 9px 72px / 72px 76px`** | ② |

**후보를 둘 재 보고 골랐다** — `fs 48 / scaleX .95` 도 폭은 ±1.5% 로 맞았지만 **높이가 +2.6~5.4%**
로 한쪽에 쏠렸다. 두 축을 같이 보고 고른 것이 **47 / .97** 이다(폭 0/0/−1.4% · 높이 0/0/0).

세로 오프셋이 **두 상태 다 3px** 로 떨어지는 것은 우연이 아니다 — ref 의 «비활성이 활성보다 5px 아래»
는 이미 `line-height` 차(비활성 87 ↔ 활성 77)가 만들고 있었고, 126 이 활성만 8px 더 내려서
**부호가 뒤집혀** 있었다. 공통 오프셋 하나로 두 상태가 동시에 ref ±1px 에 앉는다.

### 5. ② 알약 베벨 — 03·07 두 ref 의 같은 열 세로 단면

알약 상변(cap 1967) 기준 상대좌표. `python3 tools/scan337.py` 가 두 단면을 나란히 찍는다.

| 띠 | ref | 착수 전 | 처방 |
|---|---|---|---|
| 위 밝은 베벨 | rel 1~7 | rel 0~8 | 첫 스톱 9→**8** |
| 면 시작 | rel 9 | rel 10 | 10px→**9px** |
| 아래 밝은 베벨 | rel 72~76 | rel 72~77 | 78px→**76px**(76~78 은 보간) |
| 어두운 인셋 | rel 78~83 | rel 78~83 | **Δ0 — 안 건드림** |
| 알약 높이 | 85 | 85 | **Δ0 — 안 건드림** |

### 6. ③⑤(바 높이 99 → 97) — 2회차에서 닫았다

AL·AM 이 «알약 하단에 바 면이 2px 드러난다»(③), AL·AM·AO 가 «셸 외곽 +1~2px»(⑤) 로 지적한 자리다.
픽셀로 보면 **둘은 같은 것 하나**다 — 바 패딩박스(99 − 6·2 = **87**)가 알약(85)보다 2 크다.

**살아 있는 ref 둘이 97 로 일치한다.** 검정 테두리 행을 직접 세었다:

| ref | 바 검정 상변 | 바 검정 하변 | 외곽 높이 |
|---|---|---|---|
| 03 던전 §4-1 | 2021~2026 | 2112~2117 | **97** |
| 07 스킬 §9 | 2021~2026 | 2112~2117 | **97** |
| 착수 전 우리 | cap 1961~1966 | cap 2054~2059 | 99 |

⚠ 등재문 (ⓑ) 의 «03 은 97, 10·13 은 107 이라 99 는 타협값» 은 **성립하지 않는다** —
10·13 쪽은 **96 정오표로 폐기된** 표본이고(측정표 10 §3 · 13 §8), 07 측정표의 «950 x 99 (2020..2118)»
은 **같은 표의 테두리 행(2021~2026 / 2112~2117)과 어긋난다**(JPEG AA 를 위아래 한 줄씩 먹은 값).
타협할 상대가 없었다. → `docs/measure/07-스킬팝업.md` §9 에 정오표를 남겼다.

**알약(85)은 ref 와 이미 Δ0** 이므로 줄일 것은 셸뿐이고, 그러면 패딩박스 85 = 알약 85 가 되어
③ 의 2px 이 값 하나로 사라진다. `CELL_H 85` 는 한 줄도 안 건드렸다.

높이를 줄이면 **바닥 앵커 호스트의 상변이 2px 내려간다** — 335 가 방금 닫은 03 이음매가 그 위에 얹혀 있다.
그래서 같은 회차에 `bottom` 을 **전부 42 로 통일**했다. 42 는 07 ref 의 «바 하단 → 시트 바닥 42»
(측정표 07 §9)와 같은 값이고, 결과가 이렇게 떨어진다:

| 호스트 | `bottom` | 바 rect 전 | 바 rect 후 | 상변 |
|---|---|---|---|---|
| 03 `.dns-sub` | 40 → **42** | y1961 h99 | **y1961 h97** | Δ0 (335 이음매 불변) |
| 07 `.sk-tabs` | 41 → **42** | y1960 h99 | **y1961 h97** | +1 = ref |
| 06 `#eqTabs` | 41 → **42** | y1960 h99 | **y1961 h97** | +1 (96 «두 바는 같은 바» 유지) |
| 10·13 `.shp-cats` | 41 → **42** | y1960 h99 | **y1961 h97** | +1 |
| 23 `.tr-subs` | 40 → **42** | — | h97 | Δ0 |
| 47 `.rn-subs` | `top:34` **불변** | — | h97 | Δ0 (상단 앵커 — 하변만 2px 올라온다) |

**일곱 호스트의 바가 전부 `y 1961 · h 97` 로 떨어졌다**(`node tools/cap96.js` 출력). 수리 뒤 세로 단면은
ref 와 띠 단위로 같다 — 검정 1961~1966 · 밝은 베벨 1968~1974 · 면 1976~2036 · 밝은 베벨 2039~2043 ·
어두운 인셋 2045~2050 · 검정 2052~2057. **드러나던 바 면 2px 은 0 이 됐다.**

⚠ 바 상변이 1px 내려가면서 라벨도 같이 1px 내려가 `top` 을 **4 → 3** 으로 되잡았다(§2 표는 최종값).

### 6-b. 게이트 이관 (값을 바꿨으니 같이 옮기는 것이 작업 범위다)

| 게이트 | 자리 | 전 → 후 |
|---|---|---|
| `verify47` | `const BAR_H` | 99 → **97** (`BAR_BORDER 6`·`CELL_H 85` 는 불변) |
| `verify45` | 「바 높이 99 (96 공용 부품)」·「재화 탭에서도 바 규격이 같다」 | 99 → **97** |
| `verify271` | 「하위 바가 본문 머리 · 부품 높이 99」 | 99 → **97** |
| `verify335` | `R.barH` / `R.barBottom` | 98 → **97** / 2118 → **2117** («98 ± 2» 로 뭉개 두던 자리) |
| `neg219` | `.stab>i{…}` 를 **문자열로** 들고 패치하는 N5 | 새 줄로 이관 |

### 7. 구조적 위험 하나 — 라벨을 키웠는데 칸이 호스트마다 다르다

`node tools/probe337.js` **69/69 PASS** — 7 호스트 20칸 전부에서 ① 라벨 잉크가 칸 안에 있고
(최소 여백 **73.8px**, 기준 8) ② 옆 칸 라벨과 안 겹치고(최소 gap **148.9px**) ③ 바 세로 범위 안이다.

⚠ **`<i>` 의 레이아웃 상자를 재면 안 된다** — 07·26·50 시트는 `:is(#bSk,#bPet,#bCos) i{display:block}`
(ID 급)이 `.stab>i{display:inline-block}` 을 이겨서 상자가 **칸 폭 전체**가 된다(글자는 `text-align:center`
로 가운데). 처음에 상자를 재서 그 세 시트만 «여백 3.4px» 로 빨개졌다 — `Range.getBoundingClientRect()`
로 글리프 상자를 잡아야 한다(transform 이 반영된 client 좌표라 `scaleX` 도 같이 들어온다).
이것은 **부품이 아니라 자의 결함**이었다.

### 8. 회귀

`verify47` 151/151 · `verify96` 64/64 · `verify45` 36/36 · `verify104` 73/73 · `verify123` 62/62 ·
`verify209` 61/61 · `verify335` 24/24 · `verify72` 267/267 · `verify166` 73/73 · `verify299` 27/27 ·
`verify126sh` 19/19 · `verify271` 62/62 · `verify121` 149/149 · `neg219` 27/27 ·
`probe337` **69/69 신설** · **SMOKE PASS**.

`neg219` 는 `.stab>i{…}` 줄을 **문자열로** 들고 패치하는 자라 그 줄이 바뀌면 같이 이관해야 한다(N5) — 반영했다.

⚠ **흔들리는 게이트 둘 — 내 변경과 무관하다.**
- `verify121` 이 한 번 148/149(«해금 아레나 두 기사 아이들 순환 — 1800ms 관측 고유 5/6»)로 빨갰다가
  재실행에서 149/149. 아레나 스프라이트 **프레임 샘플링 흔들림**이고 `.stab` 과 닿는 데가 없다.
- `verify96` [6] 의 「03 던전 바 좌 151 / 우 135」 2건이 간헐적으로 빨갛다. **재현율을 실제로 셌다** —
  337 수리본 **8회 중 1회**, **337 착수 전 기준선(`2cc8eb9`)에서도 8회 중 1회**. 서명도 뚜렷하다:
  두 값이 늘 «좌 X · 우 (X−16)» 로 **한 배율을 같이 먹은 모양**(157/141 = `scale(.985)` · 153/137 = 연출 중간)
  이라 60·122 쥬시의 **페이지 입장 연출**(`jzPgIn`)이고, `verify47` 머리말이 같은 현상을 이미 적어 뒀다.
  337 이 만든 것이 아니다 → **PROGRESS 353 로 등재**(351 은 같은 시각에 남이 선점해 비켜 줬다). (`verify337` 은 폭으로 배율을 역산해 나누므로 안 걸린다.)

⚑ **`node tools/func50.js` 는 83/84 로 빨갛다 — 「상점 탭 레드닷 없음(코스튬으로는 안 켜진다): true (기대 false)」.**
`git stash` 로 내 변경을 통째로 뺀 기준선에서도 **똑같이 83/84** 다 = 내 작업과 무관한 **게이트/제품 부패**이고,
서브탭 부품과도 닿지 않는다(레드닷 판정식 쪽). **PROGRESS 343 으로 등재**했다.

### 9. 채점 (비평가 AR · AS 2인 독립, 1:1 비교판을 주고)

**비평가에게 준 것** — `docs/review/337-{03,07}-{ref,cap}.png` 네 장. 335 1-m 교훈대로 «앵커가 둘» 을
말로만 알려 주는 대신 **하단 앵커 −60 을 미리 적용해 같은 창으로 잘라** 줬다. 결과: **두 사람 다
«+24px 하향» 유령을 한 줄도 쓰지 않았다**(335 는 그 유령 때문에 세 회차를 6 에 묶여 있었다).
03 의 칸 수 2→3 도 둘 다 «209 주인 지시라 감점 아님» 으로 스스로 걸러 냈다.

| 축 | AR | AS |
|---|---|---|
| ① 요소 배치 | 9 | 9 |
| ② 상대 크기 비율 | **8** | **8** |
| ③ 여백·간격 | 9 | 9 |
| ④ 정렬·그리드 | 9 | 8 |
| ⑤ 색·명암 *(기록만)* | 7 | 9 |
| ⑥ 폰트 크기 비율 *(기록만)* | 9 | 8 |
| **①~④ 최저** | **8** | **8** |

⇒ **통과**(기준 8). 337 이 잡겠다고 한 다섯 항이 둘의 실측에서 전부 닫혔다:

| 등재문 항 | AR 실측 | AS 실측 |
|---|---|---|
| ① 라벨 잉크 −14~21% | «스킬» 67x38 **0%/0%** · «장비» 70x38 0%/−2.6% · «던전» −1.5%/0% | «장비»·«스킬» **0.0%** · «던전» −2.9%/0% |
| ② 알약 상단 여백 +2~3px | 좌 테두리·밝은 림 **≤1px** | 상단 하이라이트 밴드 Δ≤2px |
| ③ 알약 아래 바 면 2px | «알약 하변 ↔ 패딩박스 하변 **0**» | 칩 상·하단이 바 스트로크와 합치 **0** |
| ④ 라벨 세로 +3~5px | 활성·비활성 **둘 다 ref ±0.5px** · «비활성이 5px 아래» 재현 | 라벨 상·하단 y **0px** |
| ⑤ 셸 높이 +1~2px | 바 외곽 **97 = 97 Δ0** | 높이 −1 (AA 한 줄) |

### 9-b. 남은 감점 — **352 로 등재**(전부 «부품 재측정» 밖의 새 자리다)

두 사람이 **독립적으로 같은 것 둘**을 짚었고, 내가 픽셀로 되짚어 값을 확정했다:

| 항목 | AR | AS | 내 실측(같은 마스크) | 판단 |
|---|---|---|---|---|
| 활성 알약 코너 반경 | ref ≈28 ↔ 우리 ≈34 (+21%) | ref ≈40 ↔ 우리 48.5 (+21%) | **ref ≈27 ↔ 우리 36**(칩 좌변 인셋 프로파일 13점) | **실재** — 우리 CSS 는 `.stab.on{border-radius:36px}` 라 내 값이 자를 검산한다(AS 의 48.5 는 셸 값을 읽은 것) |
| 바 좌·우 검정 테두리 | ref **8** ↔ 우리 6 (−25%) | ref 8.0 ↔ 우리 6.0 (−25%) | **ref x66~73 = 8 ↔ 우리 x65~70 = 6** | **실재하나 위험하다** — §352 참조 |
| 셸 코너 반경 | ref ≈44 ↔ 우리 ≈47 (+3, «방법 편의») | ref ≈44 ↔ 우리 48.5 (+10%) | **좌측 코너 프로파일 7점 중 6점이 Δ≤2px** | **기각** — 두 추정은 원호 피팅 편의다 |
| `.stab-sep` 구분선 | top 18→**16** · h 54→**55** (ref 2043~2097) | 세로 길이 −1.8% | — | **실재**(AR 단독, 값은 측정표와 정합) |
| 셸 좌·우 안쪽 밝은 림 7px | **미구현**(ref x40~46 `#705F4B`) | — | — | **실재**(AR 단독, ⑤) |

⚠ **테두리 6 → 8 은 «한 줄» 이 아니다.** 패딩박스가 938 → 934 로 4px 좁아지므로 4칸 격자
(`.stab-c1~c4` = 224/261/223/229, 합 938)이 **통째로 다시 잡혀야** 하고 `verify47` 의 `BAR_BORDER`·
칸 경계 6항이 같이 움직인다. AR 도 «손해가 크니 6 유지를 권한다» 고 적었다. 게다가 ref 자신이
**면마다 다르다**(07 좌·우 8 / 상·하 6 · 03 좌 8 / 우·하 6) — 352 는 **어느 면이 진짜인지부터** 확정해야 한다.

### 10. 337 은 여기서 끝난다 — 남은 것은 352

`verify337.js` **91/91 신설** 완료(§R 되돌림 5항 포함)라 §9 의 «다음 회차» 목록은 이 세션이 다 썼다.
남는 것은 **352**(§9-b) 하나이고, 337 의 잔여 감점 중 **03 «던전» 잉크 폭만 −1.4~2.9%** 는
1px 급이라 `scaleX` 로 밀면 07 두 표본(둘 다 Δ0)이 반대로 어긋난다 — **서체가 바뀌기 전에는 회수 불가**다.

⚑ **다음 워커가 반드시 가져갈 방법 두 가지** (이 세션이 값을 얻은 것은 값을 잘 골라서가 아니다):
1. **«같은 문자열» 로 재라.** 서로 다른 글자를 비교하면 서체 차이가 섞여 −14.3 ~ −21% 처럼 갈린다.
   07 은 «스킬»/«장비» 가 ref 와 우리 양쪽에 다 있어 서체가 안 끼어드는 유일한 표본이었다.
2. **비평가에게 «앵커가 둘» 을 말로 주지 말고, 앵커를 적용해 잘라 준 1:1 비교판을 줘라.**
   335 는 말로 줬고(2회차에 표까지 줬다) 그 앞 세 회차를 유령에 태웠다. 이 회차는 크롭으로 줬고
   **두 비평가가 유령을 한 줄도 안 썼다.**
