# 929 — `verify79` [E] · `probe523` [4] 전투 캔버스 텍셀 역산 (자 수리)

> 1회차 · 2026-09-05 · sess-0913-28183 루틴 워커 A · 제품 `index.html` **0줄**(자 3벌)

## 0. 등재문과 결론

| | 등재문(2026-09-05, sess-0828-21792 워커 B) | 1회차 결론 |
|---|---|---|
| 1순위 가설 | **한 틱 지연** — rAF 두 겹 안에서 읽은 `player.x` 가 그려진 판보다 한 틱 앞선다 | **기각**(`probe929` [5] — 한 틱 전 좌표로 읽어도 Δ 164~206 으로 안 닫힌다) |
| 실재하는 뿌리 ⓐ | — | **541 그리기 배율 `PLAYER_DRAW_SC`(=1.5) 누락** — 자의 역산이 `scale = 1` 이다 |
| 실재하는 뿌리 ⓒ | — | **셰이크** — `draw()` 의 `ox` 에 든 `sx` 를 자가 `cam.x` 로 되계산해 버린다 |

⚑ **523 이 고친 것과 «같은 종류의 부패» 가 한 겹 더 남아 있었다.** 523 은 «역산은 제품에게 묻는다» 로
243 의 가로 보정 `frameXo` 를 되살렸는데, 정작 제품 `drawFrame`(index.html 21015)이 **로컬 좌표에 곱하는
`scale`** 은 그대로 빠져 있었다. 플레이어는 `PLAYER_DRAW_SC`(541)로 그려진다(28228·28236).

## 1. 재현 — `probe929` 신설 (7/7)

무변경 트리에서 프레임 12판 × 색 평평 표본 160개를 **한 판 안에서 여러 벌로** 잰다.

| 벌 | 결과 |
|---|---|
| 현행 식(`scale` 없음 · `cam.x` 되계산) | **12/12 빨강** · Δmax 189~195 |
| 제품 식(`scale` + 발표된 `camOx/camOy`) | **닫힌 판 8~10/12 · Δmin 0** |
| 한 틱 전 좌표(등재문 가설 ⓑ) | **0/11 닫힘** — Δ 164~206 |

- **[3] 산식이 맞는다**: 표본별 어긋남이 `(lx0 + u + 0.5)·(scale − 1)·SC` 예측과 **40/40 ±1px**.
  가로 어긋남 폭이 표본 사이에서 **24~26px** 벌어진다 = «상수 이동» 이 아니다.
- **[4] 이 자가 왜 «플레이키» 로 보였는가**: 어긋남 0 인 자리는 `lx0 + u + 0.5 = 0` 인 **열 하나뿐**
  (실측 `u* = 26.5` · 프레임 폭 49~50). 옛 [E] 는 표본을 «src 사각형 한가운데서 바깥으로» **한 점**만
  골랐고 그 탐색이 하필 그 열 근처에서 시작한다 — **짚은 자리가 그 열이면 Δ=0, 한두 칸 벗어나면 Δ=14~19.**
  프레임마다 애니 프레임·조건이 달라 짚는 자리가 바뀌니 실행마다 갈렸다(등재문 실측 5회 중 2회 빨강).
- **[6] 셰이크는 주입으로 갈랐다**(반복이 아니라 주입 — 등재문 지시): `cam.shake = 12` 를 넣은 판에서
  되계산 `ox` 는 **Δ 195~197 빨강**, 제품이 발표한 `camOx` 는 **Δ 0 초록**. 셰이크 오프셋 2~9 장치px.

## 2. 수리 — 자 두 벌

### `tools/verify79.js` [E]
1. **표본**: 한 점 → **스프라이트 전역의 색 평평 표본 ≤60개**(틴트가 물드는 자리만). 자리를 «운» 에 맡기지 않는다.
2. **자리**: 제품 `drawFrame` 과 같은 식 — `(-fr[6]/2 + fr[4] + xo + u + 0.5) · scale` · `(ly0 + v + 0.5) · scale`,
   `scale = PLAYER_DRAW_SC`(없는 트리에서는 1 — `size590lib` 와 같은 관용구).
3. **카메라**: 되계산 폐기 → 제품이 발표한 `camOx`/`camOy`(index.html 28112 · 셰이크 `sx` 가 이미 들어 있다).
4. **되돌림 시험 두 개 신설** — 문턱(≤3)은 한 칸도 안 넓혔다.
   - `[E-R3]` `scale` 을 빼면 **Δmax 112~178 빨강**
   - `[E-R4]` 셰이크 주입 판에서 되계산 `ox` 는 **빨강**, 발표된 `camOx` 는 **초록**
   - 기존 `[E-R1]`(다른 코스튬 색) · `[E-R2]`(243 `xo` 제거) 유지
5. ⚠ **되돌림 시험의 통계를 판정과 같은 것으로 맞췄다** — 판정이 `Δmax` 이므로 «항을 빼면 빨갛다» 도
   `Δmax` 로 묻는다. 처음에 `Δmin` 으로 적었더니 «표본 60개 중 하나는 우연히 맞는다» 가 곧 초록이 돼
   `[E-R3]`·`[E-R4]` 가 **둘 다 죽은 항**이었다(실측 Δmin=0). `[E-R1]` 만 `Δmin` 이다 — «표본 **전부**가
   다른 코스튬 색과 멀다» 는 더 센 주장이고 그게 참이기 때문이다(Δmin 38).

### `tools/probe523.js` [4]
- 같은 자리 수리(제품 식 + 발표된 `camOx/camOy`) ⇒ 색 평평 표본 76~81개 **Δmax 0**(수리 전 168~184 결정적 빨강).
- **[5] 의 주장을 바로잡았다**: 옛 [5] 는 «어긋남은 프레임마다 상수인 가로 이동» 이라며 *옛 식 ↔ 제품 식*
  차를 표본 전부에서 같은 값으로 요구했다. 배율 항이 섞이면 그 차는 **표본마다 다르다** — 말 자체가 성립하지 않는다.
  ⇒ 두 자리를 다 제품 식으로 세우고 **`xo` 만** 빼서 견준다. 그 몫은 `xo · scale · z · SC`(flip 이면 부호 반전)
  = **예측 −37.5 ↔ 실측 −37px**, 표본별 편차는 각 자리를 따로 반올림한 ±1px 뿐이다.
- **판 고르기**: «첫 판» 대신 프레임을 훑어 **제품 식이 가장 잘 닫히는 판**을 고른다. 스킬·적 그림이
  플레이어 위에 얹히는 판이 섞이기 때문이다(929 실측 12판 중 1~2). ⚠ 이 고르기가 문턱을 무르게 하지 않는
  근거는 `probe929` [1]·[2] 다 — **자리가 틀리면 어느 판도 안 닫힌다**(현행 식 0/12).

## 3. 점수

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `verify79` | **3/5 실행만 PASS**([E] Δ 5~19 로 갈림) | **21/21 PASS · 5회 연속**(신설 R3·R4 포함) |
| `probe523` | **9/10 · [4] 결정적 빨강**(Δmax 168~184) | **10/10 PASS · 3회 연속** |
| `probe929` | (없음) | **7/7 PASS · 3회 연속**(신설) |

회귀·SMOKE 는 §4.

## 4. 회귀 · 게이트

| 자 | 결과 |
|---|---|
| `verify243`(243 `frameXo`) | **43/43 PASS** 무수정 |
| `verify541`(그리기 배율 원장) | **68/68 통과** 무수정 |
| `verify590` · `probe590`(`size590lib` 축) | **45/45** · **16/16** 무수정 |
| `probe446`(아틀라스 역산) | **9/9 PASS** 무수정 |
| `verify925`(무장 사슬) | **30/30 PASS** 무수정 |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verifyProgress.js` | 종료 코드 **0**(§1~§5 빨강 0) |

**이관 0종.** 이 수리는 자 안에서 닫혔고 제품 `index.html` 은 **한 줄도 안 건드렸다** —
`verify541`·`verify590` 이 무수정으로 초록인 것이 그 가장 짧은 증거다(그 둘이 `PLAYER_DRAW_SC` 의 주인이다).

## 5. 곁다리 관측

- **등재 0건.** 회귀 6종이 전부 무수정 초록이었다.
- ⚠ 스킬·적 그림이 플레이어 위에 얹히는 프레임이 12판 중 1~2판 섞인다(`probe929` [2] 실측 8~10/12 닫힘).
  이 자들은 **프레임 재시도**로 그것을 피한다 — 자리가 틀렸다면 어느 판도 안 닫히므로([1] 0/12)
  «닫히는 판을 고른다» 가 문턱을 무르게 하지 않는다. 얹힘 자체를 재는 축이 필요해지면 그때 따로 등재한다.
