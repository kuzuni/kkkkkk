# 958 — `probe932` B 열에 남은 «번짐 비대칭» 자 6개 (942 5회차 이관)

> 942 가 회차 상한 5 를 소진하며 넘긴 자리. **한 자 + 그 게이트 이관까지가 한 회차 몫**이다.
> 남은 여섯: `probe352` · `probe384` · `probe449` · `scan335` · `scanA4` · `scanA4b`.

---

## 1회차 (2026-09-05, sess-2212-1601 루틴 워커 A) — `tools/probe384.py`

제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄.
자 **1벌 수리**(`tools/probe384.py`) · 게이트 **`tools/verify958.js` 39/39 신설** ·
장부 1행 갱신(`probe932` `probe384.py` **B → S**) · `verify932` 래칫 이관(B **6 → 5** ·
`FIXED958` 신설 · [2-i] 신설 · 62 → **63**) · `verify942` [6-b] 이관.

### ⓐ 왜 이 자를 골랐나

942 5회차 §ⓘ 의 표가 순서를 이미 정해 뒀다 — `probe384` 는 «팔레트 분류라 `probe409g.runs_from`
을 부르면 된다(가장 짧은 길)» 로 적힌 자다. 그리고 **부르는 노드 게이트가 이 자의 출력을 안 읽는다**:
`verify384` 는 이름이 주석에만 오르내리고 값은 브라우저에서 제품을 직접 재서 낸다(`docs/ref` 도 안 연다)
— 남의 못박힌 수를 안 흔든다.

⚑ **착수 첫 줄에서 «캡처 없으면 즉사» 를 또 만났다(다섯째다).**
`FileNotFoundError: docs/review/96-full-hero.png` — 942 의 2·3·4·5회차가 `probe409c`·`probe409i`·
`probe409f`·`probe409` 에서 고친 그 얼굴이 **이 자에도 그대로** 있었다(등재문이 미리 경고한 자리).
캡처 PNG 는 커밋 금지 자산이라 **없는 클론이 정상**이므로 없으면 **ref 절만** 돌게 했다(§ⓖ).

### ⓑ ⚑⚑ 재현이 **등재문의 처방을 기각했다**

처방대로 `runs_from` 을 먼저 붙였고, 합성 판이 그것을 되돌리게 했다.
**뿌리는 걸음이다** — 409 계열은 광선을 **0.5px** 로 훑지만 이 자는 가로 행이라 걸음이 **1px** 다.
`runs_from` 의 ② 접기는 «경사면은 사영 t 가 **훑는다**»(`PH_T` 0.30)로 진짜 층과 경사면을 가르는데,
**표본이 한 개뿐인 런은 t 훑음이 언제나 0** 이라 그 조건을 원리적으로 못 넘는다
⇒ **1px 걸음에서 접기는 한 번도 안 돈다.**

| 참값 `S6 K7 D4 B7 F10` | 읽은 층 |
|---|---|
| cap(칼같은 판) | `S6.00 K7.00 D4.00 B7.00 F10.00` |
| ref(번진 판) · `runs_from` | `S6.59 **K6.41** S1.21 D1.75 F2.07 B6.34 F9.63` |

K↔D 경사면이 세운 «없는 층» `S1.21` 이 그대로 남아 K 의 몫을 가져간다 — **번진 판만 −0.59px**
(칼같은 판은 7.00). 고치려던 그 비대칭이 그대로 남는다.
접기를 1표본에도 열면 **진짜 1px D 층**을 먹는다(D 는 K↔B 선분에서 잔차 1.8 밖에 안 된다 ·
942 1회차 [2-d]가 지키는 자리). ⇒ 팔레트 길은 **걸음이 0.5px 인 자에게만** 쓸 수 있다.

`verify958` **[2] 절이 이 기각을 매 실행 다시 재현한다**(자 자신은 그 길을 안 쓴다 — [2-e]).

### ⓒ 처방 — 932 의 ⓐ(문턱 교차 보간). 셈 두 줄만 갈았다

- `black_at`(두께) — «중심이 K 로 분류된 화소의 **개수**» → **두 모서리의 차**.
  `probe866` 이 «테 = 바깥 − 속 = 두 모서리의 차» 로 같은 판정을 내린 자리다(932 3회차 ⓚ).
- `face_left`(인셋) — «F 가 6개 연속으로 시작하는 **글자 번호**» → 고른 런의 **시작 모서리**.

⚠ **문턱은 층 쌍마다 «설계 밝기의 한복판»** 이다(K↔S 17.3 · K↔D 24.7).
한복판이 아니면 **번진 판에서만** 교차점이 밀린다 — 칼같은 판은 계단이라 T 와 무관하게 화소
경계에 서기 때문이다. 한 값으로 뭉치면 그 자체가 **새 비대칭**이라, 고치려는 병을 다시 만든다.
좌표 규약은 옛 자와 같다(«i 번째 화소가 그 층의 첫 화소» = 경계 x = i · 표본 중심 i+0.5)
— 칼같은 판에서 두 자가 **정확히 같은 정수**를 내는 것이 그 검산이다.

**안 건드린 것**: 표본 자리·개수 · 창(코너 표 26 · D 30 · 검정 34 · 인셋 44) · 걸음 1px ·
`cls()` 6색 팔레트 · `BOX` 손 값 · **«어느 런인가» 고르는 규칙**(첫 K · «F 6연속»).
옛 자는 `--int` 로 산다.

### ⓓ 결과 — 위상 6개 평균

**과녁은 «판 사이 |Δ|» 가 아니라 «번진 판 부호 편향» 이다** — 칼같은 판은 경계가 계단이라
어느 자로 재도 ±0.5 를 못 넘고(942 4회차 [9-d]) 그 ±0.5 가 |Δ| 에 그대로 섞인다.
이 자의 병은 «번진 쪽만 **한 방향으로** 깎인다» 이므로 부호 평균이 판정을 진다.

| 축 (판정 대역 K 4~7) | 옛 자 | 새 자 |
|---|---|---|
| 검정 옆띠 두께 — 번진 판 부호 편향 | **−0.333** | **−0.238** |
| 채움면 인셋 — 번진 판 부호 편향 | **−0.083** | **−0.014** |

ref 실측도 정수 격자에서 풀렸다:

| 좌하 코너 검정(px) | rel 56 | 58 | 60 | 62 | 64 | 66 | 68 | 70 | 72 | 74 | 76 | 78 | 80 | 82 | 84 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 옛 자 | 6 | 7 | 8 | 7 | 6 | 6 | 7 | 7 | 8 | 8 | 9 | 10 | 10 | 13 | 15 |
| 새 자 | **7.09** | **7.68** | **7.76** | **6.74** | **6.53** | **6.86** | **8.37** | **7.49** | **8.31** | **9.51** | **9.49** | **10.56** | **10.75** | **14.11** | **15.04** |

**15/15 정수 → 15/15 비정수**이고 두 자의 차는 평균 0.69 · 최대 1.51px, **13/15 칸이 더 두껍다**
(= 옛 자가 버리던 양 끝 반 화소를 되찾은 것). 인셋 쪽은 **자유 원 적합 rms 3.204 → 2.889** —
부분 화소 값이 원에 더 잘 앉는다(정의가 안 바뀌었다는 독립 검산).

### ⓔ 무르게 풀지 않았음 — 다섯 겹

1. **[3-b] 칼같은 판 불변** — 새 자의 cap 값이 **72/72 정수**다. 새 자가 «더 재지» 않았다.
2. **[3-a] 덫** — 참값 4.0px 짜리 얇은 검정이 −0.24 로 살아 있다(먹히지 않는다).
3. **[4-d] 원리적 봉투** — 두 자의 차 ≤ 2.0px · 평균 ≤ 1.0. 그 밖으로 벌어지면 **다른 런을 고른 것**이다.
   봉투 2.0 은 재 보고 정한 값이 아니라 «양 끝 각 1 칸(화소 개수 세기) + 번짐이 분류 경계를 미는 몫» 이다.
4. **[5-a] ⚑⚑ 두 모드의 클래스 런 표가 글자까지 같다**(54행) — 표본·분류·창을 안 건드렸다는 가장 짧은 증거.
5. **[R1]·[R2] 되돌림** — 옛 자를 새 자의 문턱으로 재면 두 축 **4/4 전부 떨어진다**(헛초록 아님).

### ⓕ 남긴 것은 «못 고친 것» 이 아니라 **이웃 층의 두께**다

두께 축에 −0.24px 이 남는다. 뿌리는 분명하다 — K 안쪽 이웃 `D` 가 **4px 뿐**이고 그 너머 밝은
`B` 가 국소 고원을 끌어올려, 설계 밝기로 세운 한복판 문턱이 그만큼 낮게 앉는다(교차점이 왼쪽으로 밀린다).

**다음 지렛대는 «측정 고원»** — 문턱을 설계 밝기가 아니라 **이웃 런 표본의 최댓값**으로 세우면
같은 재현에서 **−0.236 → −0.157** 이다(0.40 → 0.40 판 사이). 값을 그림에서 읽는 만큼 링잉에 열리므로
이 회차에서는 안 넣었다 — 재현이 이미 그 표를 들고 있으니 다음 회차가 고르면 된다.

⚠ **ⓑ(질량 적분)는 재현이 기각했다** — 같은 판에서 **−0.279** 로 ⓐ(−0.236)보다 나빴다.
K→D 쪽 바탕이 상수가 아니라(B 가 새어 든다) 적분의 불변성이 깨진다.
932 장부의 «ⓑ 는 두께 축» 은 **바탕이 상수일 때** 의 말이다.

### ⓖ 곁다리로 고친 것 — 캡처 없이도 도는 자(다섯째)

`main()` 이 `Image.open(CAP7)` 로 무조건 열던 것을 `os.path.exists` 뒤로 옮기고, 없으면
«캡처 없음» 한 줄을 찍은 뒤 **ref 절만** 돈다(`verify958` [8]). ⓐ~ⓗ 전부 cap 줄만 빠진다.
`--all`(03 재현)도 같은 규칙이다.

### ⓗ 게이트 이관 (333 처방 — 지우지 않고 옮겼다)

- `probe932` 장부 `probe384.py` **B → S** · `sig` `k0 r1 t3 i0 f0` → **`k0 r1 t13 i0 f1`** ·
  `axis`·`why`·`fix` 재작성. ⚠ **신호가 바뀌면 판정이 무효**라 sig 를 안 옮기면 `verify932` [2-c] 가
  «판정 ?» 로 빨개진다(실제로 처음 한 번 빨개졌고 그 설계가 작동했다).
- `verify932` 주홍 래칫 **6 → 5** · **`FIXED958` 신설**(942 와 **처방이 다르므로** `FIXED942` 에
  이름을 섞지 않고 목록을 갈랐다) · **[2-i] 신설**(«비어서 줄었다» 를 막는 같은 규칙) · 62 → **63**.
- `verify942` **[6-b] 이관** — 그 항이 못박던 것은 «942 의 다섯이 나갔다» 이고 개수 6 은 그 시점의
  잔량이었다. 958 이 하나를 닫자 5 가 되므로, **못박는 뜻(다섯이 하나도 안 남았다)은 그대로 두고**
  수만 «≤ 6» 으로 열었다. 지금 남은 다섯의 **이름**은 `verify958` [7-d] 가 래칫으로 든다.

### ⓘ 남은 것 / 다음 회차 (회차 상한 10 중 **1** 을 썼다)

**B 열 5자** — `probe352` · `probe449` · `scan335` · `scanA4` · `scanA4b`.

⚑ **이 회차가 표를 한 줄 고쳐 넘긴다** — 942 5회차 §ⓘ 표는 `probe384` 를 «팔레트 길» 로 적었지만
그 길은 **걸음이 0.5px 인 자에게만** 쓸 수 있다. `scan335`·`scanA4`·`scanA4b` 는 **1px 걸음의 문턱 런**
이라 표에 적힌 대로 ⓐ/ⓑ 로 가면 되고, **`probe449` 도 코너 광선이 1px 걸음이면 같은 함정**이다
(착수 첫 줄에서 걸음부터 확인할 것).

| 자 | 부르는 게이트 | 처방 |
|---|---|---|
| `probe352` | `verify352` · `verify47` | 런렝스 → 문턱 교차 보간(ⓐ) · 파일 자신이 «97·85·6 은 정수 문턱이 부분화소를 버린 값» 이라며 결론을 폐기해 뒀다 |
| `probe449` | `verify449` · `verify384` | ref «목표값» 의 **생산지**라 그 상수까지 같이 옮겨야 한다 · ⚠ 걸음을 먼저 볼 것 |
| `scan335` | `verify96` · `m335` | 문턱 런 — 932 §ⓓ 의 ⓑ 질량 적분(바탕이 상수인지 먼저 확인) |
| `scanA4` · `scanA4b` | (없음) | 같은 문턱 런 · 한 회차에 둘이 한 몫 · ⚠ `scanA4` 는 `docs/review/A4-r6.png` 를 전제한다 |

### ⓙ 회귀 (전부 무수정, 이관한 둘 제외)

| 자 | 결과 |
|---|---|
| `verify958` | **39/39 PASS** (신설) |
| `verify932` | **63/63 PASS** (62 → 63 · B 6 → 5 · `FIXED958`) |
| `verify942` | **95/95 PASS** ([6-b] 이관) |
| `verify384` | 106/106 PASS (무수정 — 이 자의 출력을 안 읽는다는 증거) |
| `verify409` | 177/177 PASS |
| `verify352` | 68/68 PASS |
| `verify449` | 42/42 PASS |
| `verify96` | 101/101 PASS |
| `verify47` | 201/201 PASS |
| `verifyProgress` | OK (§1~§5 빨강 0) |
| `smoke` | **SMOKE PASS** |

---

## 2회차 (2026-09-06, sess-0058-25605 루틴 워커 D) — `tools/probe352.py`

제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄.
자 **1벌 수리**(`tools/probe352.py`) · 공용 부품 1줄 확장(`probe409g._phys_px`) ·
게이트 `tools/verify958.js` **§[9] 33항 신설**(39 → **72/72**) ·
장부 1행 갱신(`probe932` `probe352.py` **B → S**) · `verify932` 래칫 이관(B **5 → 4** ·
`FIXED958` 에 이름 추가 · 63/63) · `verify958` **[7-d] 이관** · 곁다리 등재 **968**.

### ⓐ 왜 이 자를 골랐나

1회차 §ⓘ 의 표가 순서를 정해 뒀다 — 남은 다섯 중 표 순서 첫째가 `probe352` 이고,
처방은 «런렝스 → 문턱 교차 보간(ⓐ)» 로 이미 적혀 있었다. 그리고 이 자는
**자기 결론을 스스로 폐기해 둔** 유일한 자다(파일 안에 «97·85·6 은 정수 문턱이
부분화소를 버린 값» 이 437 정오로 적혀 있다) — 갈 자리를 파일이 먼저 가리키고 있었다.

⚑ **1회차가 «걸음부터 확인하라» 고 넘긴 것을 첫 줄에서 했다.** 이 자의 걸음도 **1px** 이라
942 의 팔레트 길(`probe409g.runs_from`)은 여기도 못 쓴다 — 표 그대로 ⓐ 로 갔다.

⚑ **착수 첫 줄에서 «캡처 없으면 즉사» 를 또 만났다(여섯째다).**
`FileNotFoundError: docs/review/96-full-hero.png` — 942 의 2·3·4·5회차와 958 1회차가
다섯 번 고친 그 얼굴이 이 자에도 그대로 있었다. 캡처 PNG 는 커밋 금지 자산이라
**없는 클론이 정상**이므로 없으면 «없다» 한 줄 뒤 **ref 절만** 돈다(`verify958` [9-A]~[9-C]).

### ⓑ 이 자는 축이 셋이고 **셋 다** 같은 병이었다

| 축 | 옛 셈 | 왜 정수인가 |
|---|---|---|
| ⓑ 셸 검정 테두리 두께 | `runlen` | 문턱 이하 화소의 **개수** |
| ⓐ 알약 코너 인셋 | 어두운 런의 시작 x | 런의 **글자 번호** |
| ⓒ `.stab-sep` 구분선 h | 색 상자에 드는 행 런 | 행의 **개수** |

⚑⚑ **이 자가 «검정 문턱을 둘»(느슨 ≤24 · 순검정 ≤4) 들고 있던 것 자체가 증상이었다.**
파일 머리말이 «이 둘을 안 가르면 ref 테두리가 6 인데 8 로 읽힌다» 로 그 갈림을 **설계**로
적어 두었는데, 실은 **같은 런의 같은 두 모서리**를 재면서 시작 표본만 한 칸 다르게 잡은 것이라
네 면에서 예외 없이 1~2 가 벌어진다. 부분 화소에는 그 애매함이 **없다** — 값이 하나다.

### ⓒ 처방 — 932 의 ⓐ(문턱 교차 보간). **팔레트를 짐작할 필요가 없었다**

문턱은 층 쌍마다 **설계 밝기의 한복판**이어야 한다(한복판이 아니면 칼같은 판은 계단이라
안 움직이고 번진 판만 밀려 **새 비대칭**이 생긴다 — 1회차 §ⓒ). 이 부품에서는 그 «설계 밝기»
를 추정할 필요가 없다: **ref 의 고원이 CSS 선언과 같은 색으로 앉는다.**

| 층 | CSS 선언 | ref 실측 고원 | 밝기 |
|---|---|---|---|
| P 시트 크림 | `#F0D9BA` | (240,217,186) | 214.33 |
| K 셸 테두리 | `border:7px solid #000` | (0,0,0) | 0 |
| R 안쪽 밝은 림 | `box-shadow inset #705F4B` | (112,95,75) | 94.0 |
| S 셸 바닥 | `linear-gradient(#61523D…)` | (97,82,61) | 79.67 |
| V 구분선 | `.stab-sep{background:#483B2B}` | (72,59,43) | 58.0 |

⇒ 문턱 셋 — **P↔K 107.2 · K↔R 47.0 · S↔V 68.7**.
두께는 «화소 개수» 가 아니라 **두 모서리의 차**로(`probe866` 선례 · 932 3회차 ⓚ),
인셋은 고른 런의 **시작 모서리**로 낸다. 좌표 규약은 옛 자 그대로다 —
좌 인셋 = (좌 모서리) − `pill_l` · 우 인셋 = (`pill_r`+1) − (우 모서리)
[옛 정수식 `pill_r − runs[-1][-1]` 의 부분 화소 일반화. 마지막 어두운 화소 b 의 오른쪽 모서리가
b+1 이므로 `pill_r − b = (pill_r+1) − (b+1)` — **정의를 안 바꾼다**].

**안 건드린 것**: 표본 자리·창(±25 · span 25 · 97행) · 걸음 1px · 두 문턱 상수(24·4) ·
색 상자(45~95/30~80/20~65 · sum<210) · **«어느 런인가» 고르는 규칙**(첫 런 / 끝 런 /
«3칸 이상» / 가장 긴 행 런) · 원호 역산식. 옛 자는 `--int` 로 산다.

### ⓓ 결과 — 합성 판(위상 6개 평균 · **번진 판 부호 편향**)

| 축 (합성 σ 1.1px) | 옛 자 | 새 자 | 판 사이 \|Δ\| |
|---|---|---|---|
| 셸 검정 테두리 두께 (K 4~8.5) | **−2.333** | **−0.205** | 3.00 → 1.09 |
| 구분선 두께 (V 10~54) | **+0.167** | **+0.000** | 1.00 → **0.00** |
| 알약 코너 인셋(위치 축) | **+0.917** | **−0.017** | 1.00 → 0.45 |

⚠ 두께 축의 옛 편향이 −2.3 으로 큰 것은 **바깥 이웃이 크림(214.3)** 이라 P↔K 경사면이 길고,
느슨 문턱 24 가 그 경사면의 **K 쪽 끝**에 앉기 때문이다(양 끝에서 각 ~1.17px). 합성은 σ 를
1.1 로 고정한 값이라 ref JPEG 의 실제 어긋남은 그보다 작다 — 아래 ⓔ 의 하변 6.99 가 그 증거다.

### ⓔ ref 실측도 정수 격자에서 풀렸다 — 그리고 **CSS 항등식으로 검산된다**

| ref 07 셸 테두리 | 좌 | 우 | 상 | 하 |
|---|---|---|---|---|
| 옛 자 느슨 ≤24 | 8 | 8 | 8 | **6** |
| 옛 자 순검정 ≤4 | 7 | 7 | 6 | **5** |
| **새 자** | **8.38** | **8.38** | **8.38** | **6.99** |

⚑ **가장 짧은 검산은 하변이다.** `.stabs` 의 `box-shadow` 는 검정 립(`--sl` 1.5px)을
**상·좌·우에만** 넣는다(`inset 0 1.5 0 #000` · `inset ±1.5 0 0 #000` — 하변 항이 없다).
⇒ 하변의 참값은 테두리 **7 뿐**이고 새 자가 **6.99** 를 낸다(Δ 0.01).
립이 있는 세 면의 참값은 7 + 1.5 = **8.5** 이고 새 자가 **8.38** 이다(Δ 0.12).
**옛 자는 하변에서 두 문턱이 둘 다 틀렸다**(6 · 5) — 무르게 푼 수리가 아니다([9-l][9-n]).

| 다른 두 축 | 옛 자 | 새 자 | 제품 선언 |
|---|---|---|---|
| 구분선 h | 55 | **54.59** | 54 |
| 구분선 상변(셸 바깥에서) | +22 | **+22.14** | +23 (테두리 7 + top 16) |
| 코너 인셋 16칸 | 16/16 정수 | **15/16 비정수** | — |
| 알약 코너 반경(원호 역산) | 좌 30.1 · 우 33.9 · **평균 32.0** | 좌 28.7 · 우 32.5 · **평균 30.6** | 30 (`verify352 RADIUS`) |

### ⓕ 무르게 풀지 않았음 — 다섯 겹

1. **[9-g] 칼같은 판 불변** — 합성 cap 에서 새 자의 값이 **36/36 정수**다. 새 자가 «더 재지» 않았다.
2. **[9-i] 번진 판만 풀린다** — 같은 판의 ref 쪽은 옛 자 **30/30 정수** ↔ 새 자 **0/30**.
3. **[9-l] 덫** — 립이 없는 하변에서 참값 7.0 이 **6.99** 로 살아 있다(먹히지 않는다).
4. **[9-r] ⚑⚑ 두 모드의 «어느 런인가» 줄이 글자까지 같다** — 표본·문턱·창을 안 건드렸다는 가장 짧은 증거.
5. **[9-R1]~[9-R3] 되돌림** — `--int` 로 되돌리면 세 축의 문턱을 전부 못 넘고 ref 지문도 정수로 되돌아간다.

### ⓖ 공용 부품 1줄 — `probe409g._phys_px` 가 **RGB 삼중항**을 받는다

합성 판을 그리는 셈은 저장소에 하나뿐이어야 한다(942 3회차 규약 · 402 «사본을 지운다»).
그런데 `phys_cols` 는 층을 **`probe409g.PAL` 의 글자**로만 받고, 이 부품의 색 셋(P 크림 ·
S 바 바닥 · V 구분선)은 그 팔레트에 없다 — 게다가 있는 글자 `S` 는 **다른 색**(43,35,26)이다.
⚠ **팔레트에 색을 더하는 길은 막혀 있다** — `cls()` 최근접 분류가 바뀌어 `probe409`·`probe409c`·
`probe409i`·`probe409f`·`probe384` 다섯 자가 통째로 흔들린다.
⇒ `_phys_px` 가 층 이름으로 **글자 또는 RGB 삼중항**을 받게 했다(글자를 주면 옛 걸음 그대로).
`verify942` 95/95 · `verify409` 177/177 · `verify384` 106/106 이 그 무해함을 든다.

### ⓗ 곁다리 등재 — **968**(과녁 재수립 · 폴리시)

**과녁은 한 자도 안 옮겼다.** 새 자의 ref 값이 두 자리에서 제품 선언 쪽으로 움직였는데
(반경 32.0 → **30.6** ↔ 선언 30 · 구분선 h 55 → **54.59** ↔ 선언 54), 그 상수를
`verify352`·`verify47` 에서 좁히는 것은 **제품이 빨개질 수 있는 폴리시 판단**이라
932 7회차 `scan887` 선례대로 **별도 등재**로 넘긴다(PROGRESS **968**).
지금 둘 다 대역 안이라 게이트는 초록이다(`verify352` 68/68 · `verify47` 201/201).

### ⓘ 게이트 이관 (333 처방 — 지우지 않고 옮겼다)

- `probe932` 장부 `probe352.py` **B → S** · `sig` `k0 r1 t12 i0 f0` → **`k0 r1 t24 i0 f1`** ·
  `axis`·`why`·`fix` 재작성. ⚠ 신호가 바뀌면 판정이 무효라 sig 를 안 옮기면 `verify932` [2-c] 가
  «판정 ?» 로 빨개진다(실제로 처음 한 번 빨개졌고 그 설계가 또 작동했다).
- `verify932` 주홍 래칫 **5 → 4** · `FIXED958` 에 `probe352.py` 추가 · [2-i] 문구 갱신. 63/63.
- `verify958` **[7-d] 이관** — 못박던 뜻은 «958 이 닫은 자가 주홍에 없다» 이고 개수 5 는 1회차
  시점의 잔량이었다. **뜻은 그대로 두고** 수만 «≤ 5» 로 열었다 — 남은 넷의 **이름**은
  새로 든 **[9-D]** 가 래칫으로 든다.

### ⓙ 남은 것 / 다음 회차 (회차 상한 10 중 **2** 를 썼다)

**B 열 4자** — `probe449` · `scan335` · `scanA4` · `scanA4b`.

| 자 | 부르는 게이트 | 처방 |
|---|---|---|
| `probe449` | `verify449` · `verify384` | ref «목표값» 의 **생산지**라 그 상수까지 같이 옮겨야 한다 · ⚠ 걸음을 먼저 볼 것(1px 이면 팔레트 길 금지) |
| `scan335` | `verify96` · `m335` | 문턱 런 — 932 §ⓓ 의 ⓑ 질량 적분(**바탕이 상수인지 먼저 확인** — 958 1회차가 ⓑ 를 한 번 기각했다) |
| `scanA4` · `scanA4b` | (없음) | 같은 문턱 런 · 한 회차에 둘이 한 몫 · ⚠ `scanA4` 는 `docs/review/A4-r6.png` 를 전제한다(**일곱째** 얼굴이 거기 있다) |

⚑ **2회차가 표에 한 줄을 더한다** — «부품의 CSS 선언을 먼저 찾아라». 이 자가 팔레트를
추정 없이 세운 것은 ref 고원이 선언과 같은 색으로 앉았기 때문이고, 그 덕에 **하변 같은
독립 검산 자리**(립이 없는 면)가 공짜로 나왔다. 남은 넷도 착수 첫 줄에서 `index.html` 의
그 부품 선언을 찾아 «참값이 무엇인가» 를 먼저 적을 것.

### ⓚ 회귀 (전부 무수정, 이관한 셋 제외)

| 자 | 결과 |
|---|---|
| `verify958` | **72/72 PASS** (39 → 72 · §[9] 33항 신설) |
| `verify932` | **63/63 PASS** (B 5 → 4 · `FIXED958` 둘) |
| `verify352` | 68/68 PASS (무수정) |
| `verify47` | 201/201 PASS (무수정) |
| `verify942` | 95/95 PASS |
| `verify384` | 106/106 PASS |
| `verify409` | 177/177 PASS |
| `verify449` | 42/42 PASS |
| `verify96` | 101/101 PASS |
| `verify437` | 52/52 PASS |
| `verify450` | 61/61 PASS |
| `verify378` | 48/48 PASS |
| `verify379` | 112/112 PASS |
| `smoke` | **SMOKE PASS** |

---

## 3회차 (2026-09-06, sess-0058-25605 루틴 워커 D · 2회차와 한 세션) — `tools/probe449.py`

제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스·판정 0줄.
자 **1벌 수리**(`tools/probe449.py`) · 공용 부품 **덧붙임 인자 1개**(`probe409g.runs_from(…, pal=)` · `cls(c, pal=)`) ·
게이트 `verify958` **§[10] 24항 신설**(72 → **96/96**) · 장부 1행 갱신(`probe932` `probe449.py` **B → S**) ·
`verify932` 래칫 이관(B **4 → 3** · 63/63) · `verify942` **[5-b] 이관 + [5-b2] 신설**(95 → **96/96**) ·
`verify958` **[9-D] 이관**.

### ⓐ ⚑ 표가 시킨 대로 **걸음부터 봤고, 이번엔 팔레트 길이 왔다**

1회차·2회차가 표에 남긴 경고는 «팔레트 길(`probe409g.runs_from`)은 걸음이 0.5px 인 자에게만
쓸 수 있다» 였고, 표는 `probe449` 옆에 «⚠ 걸음을 먼저 볼 것» 을 달아 뒀다.
첫 줄에서 확인하니 **이 자의 코너 광선은 `step=0.5`** 다(409 계열과 같다) ⇒ 팔레트 길이 온다.

**그런데 축이 둘이고 걸음이 서로 달랐다.**

| 축 | 걸음 | 옛 셈 | 처방 |
|---|---|---|---|
| §3 코너 광선 층 두께 | **0.5px** | 승자독식 런(`runs(s, 0.5)`) | 942 5회차의 **팔레트 길** |
| §1 알약 윤곽 · §2 세로 모서리 | **1px** | 정수 클래스 런의 글자 번호 | 932 처방 **ⓐ**(문턱 교차 보간) |

⇒ 한 회차 안에서 **두 처방을 다 썼다.** 표의 «걸음을 먼저 보라» 는 «어느 하나를 고르라» 가
아니라 **«축마다 따로 보라»** 였던 셈이다 — 다음 셋(`scan335`·`scanA4`·`scanA4b`)도 같다.

### ⓑ 팔레트 길을 부르려면 **팔레트를 넘겨야 했다**

`probe449` 는 층 이름을 409 계열과 같이 쓰지만 **셸(트랙) `S` 만 다른 색**이다 —
449 `#61503C`(97,80,60) ↔ 409g `(43,35,26)`. 그냥 부르면 자기 트랙을 남의 색으로 분류한다.
⚠ **`probe409g.PAL` 에 색을 더해 푸는 길은 막혀 있다** — `cls()` 최근접 분류가 바뀌어
`probe409`·`409c`·`409i`·`409f`·`384` 다섯 자가 통째로 흔들린다.
⇒ `runs_from(cols, mode, step, **pal=None**)` · `cls(c, **pal=None**)` 로 **덧붙임 인자**를 달았다
(안 주면 이 자의 PAL 그대로 = 옛 걸음 한 칸도 안 바뀐다).
`verify942` [5-b] 가 `def cls(c)` 를 **글자로** 못박고 있어 그 항을 이관했고,
«기본값이 사라지면 빨개지는» **[5-b2] 를 새로 세웠다**(뜻은 그대로, 서명만 열었다).

### ⓒ 결과 — 합성 판(위상 6개 · 번진 판 부호 편향)

**ⓐ 광선 축**은 942 5회차와 **같은 자**라 그 재현이 그대로 이 자의 재현이다
(`verify958` [10-b][10-c] 가 값으로 다시 찍는다):

| 참값 `S3 K7 D4 B7` | 읽은 층 |
|---|---|
| cap(칼같은 판) · 두 자 | `S3.00 K7.00 D4.00 B7.00` |
| ref(번진 판) · **옛 자** | `S3.00 **K6.50 S1.50 D2.00 F1.50** B6.50` (층 4 → **6**) |
| ref(번진 판) · **새 자** | `S2.89 K7.02 D4.13 B6.97` (층 **4** · 판 사이 |Δ| ≤ 0.30) |

**ⓑ 모서리 축**(F 6+ph · K k · B 8 · F 10 · 참 모서리 = 6+ph+k−0.5):

| 축 | 옛 자 | 새 자 |
|---|---|---|
| 번진 판 부호 편향 | **−0.583** | **−0.109** |
| 판 사이 최대 \|Δ\| | 1.00 | 0.61 |
| 0.5배수 지문(번진 판) | **24/24** | **0/24** |
| 0.5배수 지문(칼같은 판) | 24/24 | **24/24**(계단에는 부분 화소 정보가 없다) |

### ⓓ ⚑⚑ ref 실측이 **437 을 독립으로 재확인한다**

| ref 03 «던전» | 옛 정수 자 | 새 자 | 딴 자가 확정한 값 |
|---|---|---|---|
| 알약 세로 h | **85** | **84.06** | **84** (437 이 색 분류로 확정한 칸 높이) |
| 알약 윤곽 x | 938.5 | **938.05** | — |

⚑ **85 는 437 이 폐기한 바로 그 값이다.** 이 자는 437 과 **전혀 다른 그림**(03 던전)에서
**전혀 다른 셈**(세로 단면 K 런)으로 읽는데, 정수 걸음일 때는 폐기된 85 를 내고
부분 화소로 갈자 437 의 84 로 내려앉았다. 두 자가 서로를 검산한 셈이라
«새 자가 옳다» 의 근거가 이 회차 밖에 하나 더 있다.

### ⓔ ⚠ 옛 양자화에 물려 있던 상수 하나 — `first_solid(mn=1.5)`

BR 코너에서 옛 자는 여섯 각도 모두 «첫실런 **K1.5**» 를 냈다. 그것은 광선이 알약 윤곽에서
출발하므로 시작점에 걸리는 **반 화소 K** 를 승자독식이 1.5 로 부풀린 값이다.
새 자에서 그 K 는 참값 **0.52~1.40** 이라 문턱 1.5 를 못 넘고, «첫실런» 이
**D(어두운 띠 · 5.65~6.23)** 로 넘어간다 — 449 가 실제로 묻는 그 띠다.

⚠ **`mn` 을 옛 출력에 맞춰 되내리지 마라.** 규칙이 바뀐 것이 아니라 **값이 바로잡힌 결과**이고,
되내리면 셸 검정의 꼬리를 다시 «띠» 로 읽는다. `mn=1.5` 는 **한 글자도 안 건드렸다**([10-q]).

### ⓕ 과녁은 한 자도 안 옮겼다

`verify449` 가 든 것은 **대역**이라 새 ref 값이 그대로 안에 든다 —
`MIN_DARK 4.0` ↔ 새 D **5.65~6.23**(옛 5.0~5.5) · `MIN_BEV 4.0` ↔ 새 B **5.69~7.13**(옛 5.5~7.0) ·
`TOP_LO/HI 5.0/8.0` 은 위 코너용이고 그 자리는 옛 자에서도 이미 흔들리던 자리다.
**42/42 무수정** · `verify384` 106/106 무수정(449 이관 문장이 그대로 성립한다).

### ⓖ 무르게 풀지 않았음 — 다섯 겹

1. **[10-i] 칼같은 판 불변** — 새 자의 cap 값이 **24/24 0.5 배수**다.
2. **[10-c] 두 판이 모인다** — 참값 차례를 되찾고 판 사이 |Δ| ≤ 0.30.
3. **[10-n] ⚑⚑ 두 모드의 «어느 런인가» 줄이 글자까지 같다** — 표본·분류·창을 안 건드렸다.
4. **[10-o] `--int` 가 옛 값을 되살린다** — A/B 로 **자릿수(%.1f → %.2f)만** 다르고 값은 한 자도 안 다르다.
5. **[10-R1]~[10-R3] 되돌림** — 옛 자로 되돌리면 세 항이 전부 떨어진다(층 개수 6 · 편향 −0.583 · h 85).

### ⓗ 남은 것 / 다음 회차 (회차 상한 10 중 **3** 을 썼다)

**B 열 3자** — `scan335` · `scanA4` · `scanA4b`. 셋 다 **1px 걸음의 문턱 런**이라 팔레트 길이 못 온다.

| 자 | 부르는 게이트 | 처방 |
|---|---|---|
| `scan335` | `verify96` · `m335` | 932 §ⓓ 의 ⓑ 질량 적분 — ⚠ **바탕이 상수인지 먼저 확인**(958 1회차가 ⓑ 를 한 번 기각했다: K→D 쪽 바탕이 상수가 아니라 −0.279 로 ⓐ 보다 나빴다) |
| `scanA4` · `scanA4b` | (없음) | 같은 문턱 런 · 한 회차에 둘이 한 몫 · ⚠ `scanA4` 는 `docs/review/A4-r6.png` 를 전제한다 — **캡처 즉사 일곱째**가 거기 있다 |

⚑ **3회차가 표에 한 줄 더한다: «걸음을 먼저 보라» 는 축마다다.** `probe449` 는 한 파일 안에서
광선(0.5px)과 모서리(1px)가 갈렸고 처방도 갈렸다. 남은 셋도 **축을 먼저 세고** 각각의 걸음을 적을 것.

### ⓘ 회귀 (전부 무수정, 이관한 넷 제외)

| 자 | 결과 |
|---|---|
| `verify958` | **96/96 PASS** (72 → 96 · §[10] 24항 신설 · [9-D] 이관) |
| `verify942` | **96/96 PASS** (95 → 96 · [5-b] 이관 + [5-b2] 신설) |
| `verify932` | **63/63 PASS** (B 4 → 3 · `FIXED958` 셋) |
| `verify449` | 42/42 PASS (무수정 — 과녁 0칸) |
| `verify384` | 106/106 PASS · `verify409` 177/177 PASS |
| `verify352` 68/68 · `verify47` 201/201 · `verify96` 101/101 | PASS |
| `verify437` 52/52 · `verify450` 61/61 · `verify378` 48/48 · `verify379` 112/112 | PASS |
| `smoke` · `verifyProgress` | **SMOKE PASS** · **PROGRESS OK** |
