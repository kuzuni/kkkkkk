# 923 — 10 이용권 카드 · 885 라운드 마감 잔여 셋 (폴리시 T4)

등재: 2026-09-05 sess-0358-25990 워커 D (885 10회차 채점 GF·GG · 등재만)
선점: **sess-0942-30125 루틴 워커 C** (2026-09-05 09:45Z)

잡은 것 셋(등재문) — ① 노치 «바닥 평탄부» · ② 불릿형 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈 ·
③ 불릿형 제목 잉크 «폭»(⑥ 축). 그 밖에 자가 갈린 자리 둘(ⓐ 배지 분홍 판 우단 · ⓑ 불릿형 노치 렌더 깊이).

---

## 1회차 (2026-09-05, sess-0942-30125) — **① 노치 바닥 평탄부** · 제품 `index.html` 4자리

### §1 먼저 재측정했다 (LESSONS 895 — «등재 수치는 그때의 상태다»)

등재문의 수치는 **다른 자**(GD·GG)의 것이라 그대로 과녁으로 쓰지 않았다. 셋째 자를 세웠다 —
**`tools/scan923.py` 신설**: 원점 = 카드의 곧은 우변(각 그림에서 실측 · 상수로 안 적는다) ·
경계는 **부분화소 50% 교차**(정수 마스크는 ref 쪽만 ×K 로 부푼다 — LESSONS 895-2회차) ·
평탄부 여유 0.5 는 **우리 px 단위**(ref 는 /K 로 환산해 같은 실물 길이) · 문턱 사다리 3단(t 32/40/48) ·
옆면(깊이 30~80%)에 **최소제곱 원**을 맞춰 R_fit 을 같이 낸다.

**수리 전 실측(우리 px · 같은 절차)**

| 축 | ref 배너 | 우리 배너 | ref 불릿 | 우리 불릿 |
|---|---|---|---|---|
| 바닥 평탄부(깊이 ≥ 최대−0.5) | **18.57** | 6.00 | **26.82** | 10.00 |
| 노치 길이(깊이 ≥ 6) | 59.8 | 59.0 | 92.8 | 92.0 |
| 보이는 깊이 | 31.40 | 30.00 | **31.67** | 33.03 |
| R_fit − 깊이 | −3.1 ~ −3.4 | +0.6 | (윗자리 리본 오염) | +21 |

⇒ 등재문 ①(3인 일치)이 **그대로 재현됐다** — ref 는 R_fit < D 라 «원호로는 불가능»(GG 의 원리적 증거)이고
우리는 R_fit ≈ D 인 **정확한 반원**이었다. 평탄부의 절대값은 자마다 다르지만(GD·GG 는 배너 16.5~18.6 ·
불릿 27.4~30.9) **한 자로 두 그림을 나란히 재면 결손의 크기가 배너 −12.6 · 불릿 −16.8** 이다.

### §2 손잡이는 «깊이» 가 아니라 **링의 세로 모서리 반지름**이었다

실루엣을 정하는 것은 마스크(구멍)가 아니라 **링 `s` 의 안쪽 모서리**다(보이는 깊이 = `--ntc-d` − 링 두께 10 ·
`verify833` §12 가 이미 그렇게 적고 있다). 그 모서리가 `border-radius:50%` 로 못박혀 있어 **가로·세로 반지름이
둘 다 상자의 절반** = 타원 = 바닥이 한 점이었다. `50% / var(--ntc-r)` 로 바꾸면 **가로는 그대로 폭의 절반**
(= 깊이 축 불변)이고 **세로만 짧아져 좌변(= 노치 바닥)이 `len − 2r` 만큼 곧아진다.**

값은 자로 역산했다 — 자가 재는 평탄부 = (len − 2r) + α(r − 10) 이고 **α = 0.205** 는 수리 전 두 형에서
같은 값이 나온다(배너 6.00 = α·30.5 · 불릿 10.00 = α·47). 과녁 18.57 / 26.82 를 넣으면
**배너 r = 33.6 · 불릿 r = 47.4** 이고, 같은 식이 예측하는 길이는 60.0 / 92.8 로 **ref 59.8 / 92.8 과 맞는다**
(길이를 따로 맞춘 것이 아니라 한 값에서 같이 떨어졌다 — 모델이 옳다는 방증).

**제품 4자리** — `index.html`: ⓐ `--ntc-r` 폴백 두 줄(`.pvc` 47.4 · `.pvc.ban1` 33.6) ·
ⓑ `#shopw .pvc>.ntc>s{border-radius:50% / var(--ntc-r)}` · ⓒ `…>u{… / calc(var(--ntc-r) + 12px)}`
(림은 `s` 에서 사방 12 바깥이라 세로 모서리도 12 크다 — 호가 나란히 돈다) ·
ⓓ JS `NTC_R = {ban:33.6, bl:47.4}` + `renderPassPage()` 가 카드에 인라인으로 싣는다(`--ntc-d` 와 같은 규약).

⚑ **마스크(구멍)는 한 줄도 안 건드렸다** — 대수로 확인했다: 타원 구멍은 (r ≥ 0.18·len 이면) 스타디움 링의
**안쪽**을 항상 덮고, 링의 **바깥** 안에는 항상 들어간다(t ≤ len/2 이면 참). 그래서 `pvNtcMask()` 의
«겹이 여럿이면 `mask-composite:intersect`» 규약도 그대로다.

**수리 후 실측**(같은 자 · 사다리 3단에서 부호 불변)

| 축 | ref | 수리 전 | **수리 후** |
|---|---|---|---|
| 배너 평탄부 | 18.57 | 6.00 | **19.0 / 17.0** |
| 배너 길이 | 59.8 | 59.0 | **59.0** |
| 불릿 평탄부 | 26.82 | 10.00 | **28.0 / 27.0** |
| 불릿 길이 | 92.8 | 92.0 | **92.0** |
| 보이는 깊이(배너/불릿) | 31.40 / 31.67 | 30.00 / 33.03 | **30.00 / 33.00**(불변) |

### §3 게이트

- **`tools/verify923.js` 32/32 신설** — §A 선언(`--ntc-r` 한 값에서 `s`·`u` 의 세로 모서리가 파생) ·
  §B 화소(찍힌 평탄부·길이·깊이를 `scan923.py` 와 **같은 절차**로 재서 ref 창 안) ·
  §R 되돌림(`border-radius:50%` 로 되돌리면 평탄부가 19/28 → 5/4 로 무너져 [B1] 이 빨개진다).
  ⚠ 1회차 함정 둘을 자 안에 적어 뒀다 — ⓐ `borderTopLeftRadius` 는 «가로 / 세로» 두 값이고 **가로는
  백분율 그대로** 온다(숫자로 파싱하면 50 이 되어 유령 실패) · ⓑ **맨 위 노치는 분홍 배지가 카드 우변을
  덮어 토막 나 있다**(ref 도 같다 — ref 배너 맨 위 노치 길이 39.2 · 평탄부 2.06) ⇒ 온전히 보이는 노치만 채점한다.
- 회귀 **무수정**: `verify833` **280/280** · `verify151` 84/84 · `verify667` 92/92 · `verify124` 76/76 ·
  `verify153` 53/53 · `verify895` 18/18 · `verify866` 16/16 · **SMOKE PASS**.
- **[37] 9:19 ↔ 9:13.3** — 같은 자로 1600 프레임을 재도 배너 평탄부 **20.0 / 17.0** · 길이 59 · 깊이 30.00
  으로 2280(19.0 / 17.0)과 **1px 안**이다. 크롭 픽셀 차분도 최대 |Δ|₁ = 44(= 카드 뒤 바탕 그라디언트)로
  검정↔투명 차이(150+)가 없다 ⇒ **아홉 회차 연속 «기하 차이 없음»**.

### §4 이번 회차가 **안 건드린** 것 (다음 회차가 이어받는다)

1. **② 불릿형 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈** (+2.4~3.7px · 3인) — **재측정만 하고 안 고쳤다.**
   자를 세워 뒀다 — `scan923.py` 의 **`gap_pill_ribbon()`**(행 **중앙값** 밝기로 «알약 채움 → 카드 몸통 →
   리본 검정» 세 고원을 가르고 두 경계를 부분화소 50% 교차로 잡는다. ⚠ 한 열로 재면 흰 글자·★ 가
   그 열을 지배한다 — 6회차 «오염된 창»). 실측: **ref 22.07 ref px = 45.53 우리px ↔ 우리 49.00**
   ⇒ **+3.47px** 로 등재문 범위(+2.4~3.7) 안이다.
   ⚑ **처방을 못 고른 이유를 적어 둔다(다음 회차가 여기서 시작하라)** — 우리 값 49.0 은 **파생값**이다:
   `49 = 카드높이(772) − 리본블록(272) − [.pvl top(133) + 3·피치(82) + 알약높이(72)]`.
   그런데 그 셋이 **각자는 ref 와 맞는다**: `.pvl` top 133 ↔ ref 133.9(Δ−0.9) · 리본1 상단이
   카드 하변에서 272 ↔ ref 272.3(Δ−0.3) · 알약 4줄 스팬 318 ↔ ref 318.7(Δ−0.7) ·
   피치 82 ↔ ref 82.5. **넷을 더하면 우리 카드 높이 772 ↔ ref 770.3(Δ+1.7)** 이라
   **틈은 «카드 높이의 잔차 + 세 자리의 잔차» 로 흩어져 있다.** ⇒ 한 손잡이로 −3.5 를 먹이면
   그 자리가 대신 +2 대로 어긋난다(리본을 올리면 «카드하단−리본1상단» 이, 알약을 내리면 [2] 가
   10회차에 맞춘 «밴드↔첫 알약 21.70» 이 깨진다). ⚠ [36]«줄 사이 틈·피치·알약 높이 = 맞다» 도 닫혀 있다.
   **다음 회차는 «카드 높이 772 → 770» 후보부터 재라** — 그 한 줄이 네 잔차를 같은 방향으로 줄인다
   (`PVC_H_BL4` · 파생이라 3불릿 카드도 같이 따라온다). 단 카드 높이는 노치 개수·리본 자리·격자
   전부의 부모라 **회귀가 넓다** — 자를 먼저 세우고 움직여라.
2. **③ 불릿형 제목 잉크 폭** — **재측정만 했다**(⑥ 축이라 통과를 안 막는다).
   잉크 bbox(밝기 문턱 사다리 170/190/210 · 부호 불변): **ref 278.5 × 47.4 우리px ↔ 우리 283 × 47**
   ⇒ **폭 +1.6% · 높이 −0.8%** 로 등재문(+0.4~2.4% / −0.9%)과 같은 자리다.
   ⇒ 결손은 4.5px 이고 글자 사이가 6~7 칸이니 **칸당 0.65px** 이다(font-size 축이 아니다 — 높이가 맞다).
   ⚠ **배너형 제목은 넷 다 «맞다» 로 닫았으므로 손잡이는 `.pvc:not(.ban1)` 로 갈라야 한다** —
   두 형에 같이 걸면 맞는 쪽이 깨진다. 그 비대칭을 정당화할 근거(ref 두 형의 자간을 각각 잰 표)를
   먼저 만들 것.
3. **ⓐ 배지 분홍 판 우단** — 10회차에 부호가 갈린 자리. **값을 내지 못했다 — 왜 못 냈는지가 이번 회차의 소득이다.**
   색거리 마스크(`|Δ(244,49,113)|₁ < tol` · 브리핑 §2-5ⓐ 의 «전 분홍 덩이 합집합 bbox» · tol 30/45/60)로
   재면 **우리는 tol 셋 다 우단 +24.50**(카드 우변 밖 · 화소 15,931~16,095)으로 **꿈쩍도 안 하는데**
   **ref 는 tol 30 → 60 에서 +0.45 → +12.83(파랑) · −0.78 → +5.40(초록)** 으로 **사다리를 따라 계속 자란다.**
   ⚑ 뿌리는 «우리가 크다» 가 아니라 **마스크가 두 그림에서 다른 것을 세고 있다**는 것이다 —
   ref 판 가장자리는 (153,19,69) 처럼 **어두워진 분홍**(|Δ|₁ = 165)이라 tol < 68 규약에 **원리적으로 안 걸린다**
   (같은 마스크의 ref 잉크 넓이가 우리의 57% 밖에 안 되는데, 그 축은 [39] 가 «181×181 · 2인 독립» 으로
   이미 닫아 둔 자리다 ⇒ 넓이가 아니라 **자가 틀렸다**는 뜻이다).
   ⇒ **이 축은 색거리 마스크로는 못 닫는다 — 선형 언믹싱(면적)으로 가야 한다**(브리핑 §2-3 · [40] ✦ 선례).
   10회차의 부호 갈림(GD·GE·GF +1.0 ↔ GG −1.5)도 «tol 을 어디에 뒀는가» 로 설명된다.
4. **ⓑ 불릿형 노치 «렌더 깊이»** — 셋째 자로 재니 **ref 배너 31.40 ↔ 불릿 31.67(Δ0.27 — 오염을 걷은 뒤 값 · §5)** 인데
   **우리는 30.00 ↔ 33.00(Δ3.00)** 이다. GG 의 자(ref 30.49/30.61 ↔ 우리 30.6/33.6)와 **부호·크기가 같다**
   ⇒ «ref 는 두 형의 깊이가 사실상 같은데 우리만 갈려 있다» 는 **두 자가 독립으로 같은 결론**이다.
   ⚠ **그럼에도 이번 회차는 안 고쳤다** — 923 등재문이 «[20]«43/40» 을 뒤집지 마라» 로 못박았고,
   `verify833` §12 [12-e]·[R11] 이 «두 형의 깊이가 **달라야** 한다»(ref 30.2 ↔ 33.0 · 정수 자)를
   게이트로 들고 있다. 세 자 중 **정수로 세는 자 하나만** 갈림을 보고 부분화소 자 둘은 안 본다는 것이
   이 자리의 전부다(LESSONS 895-2회차 «정수로 세는 자는 해상도가 다른 두 그림을 못 견준다»).
   ⇒ **다음 회차/다음 번호가 «`scan667` 을 부분화소로 갈아 [12] 를 이관할 것인가» 를 정면으로 물어야 한다.**
   되돌릴 것이 없는 관측이므로 여기 기록으로만 남긴다.
   ⚑ **채점 2인이 이 관측을 훨씬 세게 만들었다 — §6-1 을 읽어라**(GK 가 노치 없는 행만 골라 재니 ref 두 형의 차는
   **0.01 우리px** 이고, 내 자가 냈던 0.52 는 **분홍 배지가 곧은 변을 밀어 만든 유령**이었다).


### §5 채점 — **비평 2인 독립(GJ · GK) · ①~④ 최저 «7 / 7» ⇒ 통과선 9 미달, 회차 계속(1/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GJ | 9 | **7** | 8 | 9 | 9 | 8 | **7** |
| GK | 8 | **7** | 8 | 9 | 8 | 9 | **7** |

**① 수리는 두 사람이 다 «방향이 맞다» 로 검산했다** — GJ ㉰ «브리핑 §1 표의 값은 내 자로도 재현된다»
(배너 ref 18.57~20.63 ↔ 우리 19.0~20.0 · 불릿 최대깊이가 정확히 같은 행만 세면 ref 26.8 ↔ 우리 26.0) ·
GK «바닥이 곧은가 = **예, 두 형 다**(순수 반원이 아니다 — 수리 방향은 맞다)».
⚠ GK 는 **평탄부의 «길이» 자체는 여유 사다리(0.0/0.3/0.5/0.8)에서 부호가 뒤집힌다**(ref 바닥 위에
0.3~0.8px 짜리 어깨가 4~5행 더 있다)며 «그 숫자로 손잡이를 더 돌리지 마라» 를 남겼다 — **받아들인다.**

#### 채점이 이 회차에 **되먹인 것 둘**(자 수리 · 이번 커밋에 반영)

- **GJ ㉮ — 자가 노치 «안» 의 배경 장식 점(≈7px)을 물고 있었다.** 그래서 배너 두 노치가 19.0 / **17.0** 으로
  갈려 보였다. 오른쪽 끝 화소 대신 **«몸통에 이어진 덩이»**(틈 3px 이하는 이어 붙이고 두께 12px 이상)의 끝을
  쓰도록 고치니 **19.00 / 19.00 으로 일치**한다(GJ 가 최대 연결성분으로 얻은 값과 같다).
  ⚠ **틈을 이어 붙이는 것이 핵심** — ref 의 검정 외곽선은 4 ref px 뿐이고 안쪽 경계에서 |Δ바탕|₁ 가 한 화소
  26 까지 떨어져(파랑 카드 y120 의 x481) 몸통과 끊겨 보인다. 안 이어 붙이면 **ref 곧은 변이 2px 안쪽으로
  잘못 잡혀 깊이가 통째로 +3px 어긋난다**(1회차에 실제로 한 번 그렇게 만들었다가 되돌렸다).
- **GK — «곧은 변» 추정이 배지에 졌다.** 옛 자는 곧은 변을 «프로파일 상위 40% 의 중앙값» 으로 잡는데
  ref 불릿 카드 창에는 **분홍 배지가 x=498 까지 삐져나와** 곧은 변이 486.26 → 486.88 로 부풀었고,
  그 0.62 ref px 가 **«ref 두 형의 깊이 차 0.52»** 라는 유령을 통째로 만들었다.
  ⇒ 곧은 변을 **최빈값(0.25px 격자 · 봉우리 ±0.5 안의 중앙값)** 으로 바꿨다 — 노치(왼쪽)도 배지(오른쪽)도
  소수 행이라 봉우리를 못 흔든다. 고친 뒤 ref 깊이 = 배너 **31.40** · 불릿 **31.67**(Δ 0.27).

### §6 채점이 낸 결손 — **다음 회차 순서**(둘의 1순위가 갈렸으므로 둘 다 적는다)

1. **노치 깊이 «형별 격차» — [20]«43/40» 을 열 근거가 이제 셋이다.** GK 가 노치 없는 행만 골라 재니
   **ref 배너 31.36~31.44 ↔ ref 불릿 31.37~31.46 ⇒ 차 = 0.01 우리px**(두 노치 바닥의 절대 x 가 둘 다 471.04 ·
   곧은변도 둘 다 486.26). GJ 도 «ref 형간 차 −0.14~+0.96 · 사다리에서 부호 뒤집힘 ⇒ 사실상 같다» ·
   내 자도 오염을 걷으니 0.27 이다. **우리는 30.00 / 33.00 으로 3.00 갈려 있다**(그 5배 이상).
   ⚠ **이번 회차는 안 고쳤다** — 923 등재문이 «[20] 을 뒤집지 마라» 로 못박았고 `verify833` §12 [12-e]·[R11] 이
   «두 형이 달라야 한다» 를 들고 있다. 그 게이트가 근거로 든 «ref 30.2 ↔ 33.0» 은 **정수로 세는 자**
   (`scan667.py`)의 값이다. ⇒ **다음 회차의 1순위는 «[12] 를 이관할 것인가»** 이고, 이관한다면 과녁은
   **두 형 같은 값 ≈ 31.4**(배너 +1.4 깊게 · 불릿 −1.6 얕게 = `NTC_DEP` 를 41~42 한 값으로).
2. **노치 «옆면 호» 가 각지다 — 두 사람이 각자 1순위로 냈다.** 깊이별 세로 폭 dw/d깊이(깊이 8→24):
   **ref 1.79 ↔ 우리 1.33(−26%)**(GK) · 기울기 ref 3.40~3.79 ↔ 우리 2.35~2.40(GJ · 깊이 20→28 구간).
   과녁(GJ): 깊이 25 에서 세로폭 **56~58**(현재 67.1) · 깊이 28 에서 **43~47**(현재 59.0). 배너는 −8% 로 약하다.
   ⚑ **이 축은 `border-radius` 로는 못 닫는다 — 산수로 확인했다.** 실루엣은 링 안쪽 모서리라
   `w(L) = f + 2·r_in·√(1 − (L/D)²)` 이고(우리 값 f 19.2 · r_in 37.4 · D 33 → 깊이 25/28/20 에서 68.0/58.8/78.7 로
   **GJ 실측 67.1/59.0/77.8 과 1px 안에서 맞는다** = 모델 검증), 여기에 ref 의 세 점(73.9/57.2/45.0)을 넣으면
   **f 가 음수(−9.1)로 떨어져 해가 없다**. 곧 ref 의 어깨는 **타원 사분면이 아니다** ⇒ 다음 회차는
   `clip-path`/SVG 경로처럼 **모서리 곡선을 직접 적는 길**을 재야 한다(마스크·링·림 셋을 같은 경로로 맞춰야
   하므로 회귀가 넓다 — `verify833` §15 «림이 검정 외곽선을 안 밟는다» 를 먼저 읽을 것).
3. **리본2 «꼬리» +10.0~10.9px**(GK · 두 카드가 독립으로 같은 값 · 사다리 3단 부호·크기 불변) —
   **라벨 잉크(폭 Δ −0.66)와 금색 칩은 제자리이고 오른쪽 여백만 길다**(칩 우단 기준 꼬리 ref 33.0 ↔ 우리 42.2).
   10회차 미결(GB·GE ↔ GD)의 판정이다 — 부호는 GB·GE 쪽이고 크기는 그 둘보다 크다.
4. **불릿 헤더 밴드 하단 −1.5~−2.9px**(GK) — ⚠ **10회차가 반대 방향으로 0.7 옮긴 자리다**(102 → 101.3).
   ⚠ 단 GJ ㉴ 는 «밴드↔첫 알약 틈 ref 21.18~21.66 ↔ 우리 21.85~22.05 = 과녁 21.70 명중» 으로 적었다 —
   **두 사람이 같은 부품을 다른 끝점으로 쟀다.** 손대기 전에 «어느 경계를 밴드 하단이라 부르는가» 부터 못박아라.
5. **알약↔리본1 틈 +2.39px**(GK) / **+2.33~3.82px**(GJ) — 내 자(+3.47)와 같은 창. §4-1 의 분해가 그대로 유효하다.
6. **알약 «사이 틈» +1.16px · 알약 높이 −1.1**(GK · [36] 을 여는 지적 — 피치는 맞다).
7. **배지 분홍 판** — 두 사람이 독립으로 **GD·GE·GF 쪽(우리가 +0.85~+1.32 바깥)** 을 재현했고 **GG 는 재현 실패**.
   ⚑ 그리고 **과녁 [39]«+25.0» 자체가 ref(+23.2~24.1)보다 +0.9~1.8 크다**(GJ·GK 일치) — 과녁을 먼저 고쳐야 한다.
   ⚠ 마스크 규약 갱신: 밝은 분홍만 물면 **ref 가 146×155 로 나오는 유령**이 생긴다(ref 는 짙은 마젠타 림이
   외곽선이고 우리는 검정 외곽선이다) ⇒ **밝은 분홍 + 짙은 림의 합집합**으로 물어야 [39]«181×181» 이 재현된다(GK).
   나(1회차 §4-3)는 «색거리 마스크로는 못 닫는다» 까지만 갔는데, **GK 가 그 답을 냈다.**
8. **배너 리본 높이 −1.7~3.2 · 리본 블록 아래 여백 +1.8~2.8**(GJ) · **배너 리본2 금색 칩 +3.6**(GK) ·
   **노치 세로 자리 −1.6~2.6 · 배너 노치 피치 −1.4**(GK) — 전부 1~3px 대의 잔여.

**⚑ 두 사람이 각자 기각한 것(다음 회차가 다시 올리지 마라)**: 가격 버튼 폭 = 맞다(GK ㉤ — GD 쪽이 옳고 GE 는 재현 실패) ·
[37] 9:19 ↔ 9:13.3 = **아홉 회차 연속 차이 없음**(둘 다 카드 내부 화소 전부 일치) · 배너 제목 폭 = 맞다(부호 뒤집힘) ·
불릿 리본1 우단 = ref 문구가 «매일 아이템 지급» 으로 달라 **축 자체가 성립 안 한다**(GJ ㉲).

---

## 2회차 (2026-09-05, sess-1113-9765 루틴 워커 A) — **노치 «깊이» 형별 격차** · 제품 `index.html` 4자리

1회차 §6-1 이 «다음 회차의 1순위» 로 넘긴 자리이고, 1회차 unclaim 커밋도 그렇게 적어 두었다.
질문은 하나였다 — **«[12] 를 이관할 것인가»**.

### §1 답: 이관한다. 근거는 **옛 과녁의 두 값이 자의 한 칸이었다**는 것이다

`verify833` §12 [12-e] 와 `verify667` A4 가 «두 형의 깊이가 **달라야** 한다» 를 못박고 있었고,
그 근거는 전부 `tools/scan667.py` 가 낸 «ref 파랑 30.2 ↔ 초록 33.0»(우리px) 하나였다.
**그 자에게 직접 물었다**(`python3 tools/scan667.py --ref`) — 자가 세는 것은 ref **원본 화소**이고
파랑 노치 깊이 **15** · 초록 **16** 이다. ×k(2.0628) 해서 30.9 ↔ 33.0 이 된 것이니
**두 값의 차이는 ref 화소 정확히 한 칸 = 우리 2.06px** 이다.
⇒ 이 자는 **두 형을 그보다 곱게 가를 수 없다**(LESSONS 895-2회차 «정수로 세는 자는 해상도가 다른
두 그림을 못 견딘다»). «두 값» 은 관측이 아니라 **자의 격자**였다.

**부분화소 자 셋이 독립으로 «같다» 를 낸다**(전부 50% 교차 · 사다리에서 부호 불변):

| 자 | ref 배너 | ref 불릿 | 차 |
|---|---|---|---|
| `scan923.py`(1회차 신설 · 이번 회차 재실행) | **31.40** | **31.67** | 0.27 |
| 1회차 채점 **GK**(곧은변 최빈값) | 31.36~31.44 | 31.37~31.46 | **0.01** |
| 1회차 채점 **GJ** | — | — | −0.14~+0.96 (부호 뒤집힘 = 차 없음) |

우리는 **30.00 ↔ 33.00(차 3.00)** — ref 차의 **열 배 이상**이었다.

### §2 처방 — `NTC_DEP` 를 **한 값 41** 로 접는다 (제품 4자리)

- `const NTC_DEP = { ban: 40, bl: 43 }` → **`const NTC_DEP = 41`**
- `pvCard()` 의 `NTC_DEP[p.ban ? 'ban' : 'bl']` → `NTC_DEP` (첨자 자체를 없앴다)
- CSS 폴백 `.pvc{--ntc-d:43px}` → **41px** · **`.pvc.ban1{--ntc-d:40px}` 한 줄 삭제**

⚑ **«두 칸에 41 을 적는» 길을 안 골랐다** — 그러면 다음 회차가 다시 갈라도 아무 자가 안 짖는다
(402 «사본을 지운다»). 첨자를 없애 **구조적으로** 한 값이게 했다.

⚠ **그릴 수 있는 것은 정수뿐이다 — 이것을 모르고는 값을 못 고른다.**
`.ntc` 상자 폭이 `d + 12` 이고 그 값이 정수로 내려앉는다. 같은 캡처·같은 자로 실측:

| `--ntc-d` | 40 | **41** | 41.4 | 41.5 | 42 | 43 |
|---|---|---|---|---|---|---|
| 그려진 «보이는 깊이» | 30.0 | **31.0** | 31.0 | 31.0 | 32.0 | 33.0 |

과녁 31.4 의 최근접 칸은 **31**(|Δ| 0.40)이고 42 는 0.60 이다 ⇒ **41**.
소수(41.4)를 적으면 «선언과 그림이 다른» 유령이 남으므로 **선언을 그림에 맞춰 41 로 적었다**
(«그린 것 = 선언» · 402 [H3] 규약).

**수리 전후**(우리 px · `scan923.py` · 사다리 3단 부호 불변)

| 축 | ref | 수리 전 | **수리 후** |
|---|---|---|---|
| 배너 보이는 깊이 | 31.40 | 30.00 | **30.97 / 31.00** |
| 불릿 보이는 깊이 | 31.67 | 33.00 | **30.99 / 31.03** |
| **두 형의 차** | 0.01~0.27 | **3.00** | **0.04** |
| 배너 평탄부 / 길이 | 18.57 / 59.8 | 19.0 / 59.0 | **19.0 / 59.0**(불변) |
| 불릿 평탄부 / 길이 | 26.82 / 92.8 | 28.0·27.0 / 92.0 | **28.0·27.0 / 92.0**(불변) |

⇒ **1회차가 편 바닥은 한 px 도 안 무너졌다.** 세로 모서리 반지름 `--ntc-r` 은 깊이와 독립이기
때문이다(가로 반지름만 `--ntc-d` 다) — 1회차 §2 의 «손잡이가 둘» 모델이 여기서 검산됐다.

### §3 게이트 — 이관 3벌 (자리를 비우지 않고 **방향을 뒤집었다** · 333 처방)

- **`verify833` §12** — `NDEP` 를 한 값 41 로. **[12-e] 를 «다르다» → «같다» 로 반전**하고
  근거(정수 자의 한 칸 · 부분화소 자 셋)를 절 주석에 적었다. [12-f] 는 «같은 형끼리» →
  **«카드 셋 전부 한 값»** 으로 넓혔다. §14 주석의 «형마다 두 값인 축이 **넷**» → **셋**.
- **`verify833` [R11c] 신설** — 뒤집힌 [12-e] 의 **되돌림 시험**이다. [R11] 사보타주는 불릿만
  40 으로 미므로 두 형이 다시 갈린다 ⇒ 그 순간 [12-e] 의 술어가 거짓이어야 한다.
  ⚑ **이 항이 없으면 «두 형이 같다» 는 단언이 무를 때도 초록일 수 있다**(1회차 §3 의 §R 과 같은 꼴).
- **`verify667` A4** — **두 번째로 방향이 바뀌는 항**이다(5회차 «같은 80» → 833 8회차 «80/86» →
  지금 «같은 82»). 8회차를 되돌린 것이 아니라 **자를 갈아 끼운 것**임을 항 주석에 적었다.
- **`verify923` [B4] 신설** — 깊이를 **ref 화소에 직접** 매단다. 1회차까지 이 자리는 [B3]
  «선언과 그림이 같은가» 만 물었고 **그래서 40/43 이라는 두 값도 초록이었다.**
  창 ±1.2 는 옛 두 값을 **둘 다** 떨어뜨린다(배너 30.00 → |Δ|1.40 · 불릿 33.00 → 1.33).

**점수**: `verify833` **281/281**(280 → +[R11c]) · `verify923` **36/36**(32 → +[B4] 4항) ·
`verify667` **92/92** · 회귀 무수정 `verify151` 84/84 · `verify124` 76/76 · `verify153` 53/53 ·
`verify895` 18/18 · `verify866` 18/18 · **SMOKE PASS** · `verifyProgress` OK.

**[37] 9:19 ↔ 9:13.3** — 1600 프레임을 같은 자로 재도 배너 깊이 **31.00 / 30.98** · 평탄부 19.0 ·
길이 59.0 으로 2280 과 같고, 크롭 화소 차분도 최대 |Δ|₁ = **44**(카드 뒤 바탕 그라디언트)로
검정↔투명 차이(150+)가 없다 ⇒ **열 회차 연속 «기하 차이 없음»**.

### §4 이번 회차가 **안 건드린** 것 (다음 회차가 이어받는다)

1회차 §4·§6 의 목록이 그대로 유효하다. 특히:

1. **노치 «옆면 호» 가 각지다** — 1회차 채점 2인이 각자 1순위. 1회차가 산수로
   «`border-radius` 로는 못 닫는다»(f 가 음수)까지 갔다. ⚠ 깊이가 30/33 → 31 로 바뀌었으니
   **깊이별 세로 폭 표는 이번 캡처에서 다시 재야 한다**(1회차 값은 다른 그림의 것이다).
2. **불릿 «마지막 알약 ↔ 리본1» 틈 +3.47** — 1회차 §4-1 의 분해(카드 높이 772 → 770 후보)가 그대로 1순위.
3. **배지 분홍 판 우단** — 1회차 GK 가 마스크 규약을 정정했다(밝은 분홍 **+ 짙은 마젠타 림 합집합**).
   과녁 [39]«+25.0» 자체가 ref 보다 +0.9~1.8 크다는 지적이 2인 일치다 — **과녁을 먼저 고쳐라**.
4. **리본2 꼬리 +10.0~10.9** · **불릿 헤더 밴드 하단**(⚠ 1회차 두 사람이 **다른 끝점**을 쟀다) ·
   **불릿 제목 잉크 폭 +1.6%**(⑥ 축).

### §5 채점 — **비평 2인 독립(GL · GM) · ①~④ 최저 «8 / 8» ⇒ 통과선 9 미달, 회차 계속(2/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GL | 9 | **8** | 9 | 9 | 9 | 9 | **8** |
| GM | 9 | **8** | 9 | 9 | 8 | 9 | **8** |

**둘 다 ② 를 막은 것은 «노치 옆면 호» 하나다**(1회차 §6-2 가 넘긴 자리). 1회차 7/7 → **8/8**.

#### 이번 수리는 두 사람이 각자의 자로 **전부 확인**했다

- **ref 두 형의 깊이가 같다** — GL(부분화소 · 사다리 3단 9칸): ref 배너 31.48~31.61 ↔ 불릿 31.47~31.60,
  **차 0.01 우리px**. GM(실루엣 자 + **둘째 자**(채움 경계)로 교차검증): 실루엣 +0.01 ↔ 채움 −0.27 로
  **두 자 사이에서 부호가 뒤집힌다** ⇒ «|Δ| ≤ 0.3px = 측정 한계» ⇒ «사실상 같다» 확정.
  **ref 최선 추정 = 31.49 ± 0.20**(GM · 두 자 × 사다리 전 범위 31.22~31.61).
- ⚑ **GM 이 «옛 게이트가 왜 갈렸는지» 를 재현했다 — 내 진단보다 한 겹 아래다.**
  같은 그림을 **정수 화소**로 세면 문턱 t=40·63.5 에서 **두 형 모두 15 화소**(30.94)이고
  t=90 에서 **두 형 모두 16 화소**(33.00)다. 곧 옛 «파랑 30.2 ↔ 초록 33.0» 은 한 칸 양자화 **더하기**
  **두 형에 서로 다른 문턱·창이 걸린 결과**였다. ⇒ «[20]«43/40» 을 되살릴 근거는 이 그림에 없다»(GM).
- **우리 두 형이 같아졌다** — 둘 다 **Δ 0.00~0.01**(내 자의 0.04 보다도 작다). 수리 전 대조본
  (`-before-c1/-c2`)에서만 3.00~3.01 이 재현된다.
- **1회차가 편 바닥이 안 무너졌다** — GL: 전후 Δ 배너 −0.03~−0.31 · 불릿 **+0.61~+1.03**(깊이가
  얕아진 만큼 오히려 뭉툭해졌다) · GM: 수리 전 6.0/10.0 → 지금 21/32.
- **깊이 −0.49px 은 «못 닫는 잔차» 로 둘 다 인정했다** — GM: 41 ↔ 42 의 우열이 **자와 문턱마다 부호가
  뒤집힌다**(실루엣 자는 42 가 0.06 유리 · 채움 자는 41 이 0.10 유리) ⇒ «41 을 고른 판단을 결함으로
  올리지 않는다. 되돌릴 이유도 없다». GL 도 «0.1px 차이는 잡음 바닥 = 측정 한계».
  ⇒ **이 잔차는 [지적 1](옆면 호)을 `clip-path` 로 옮길 때 같이 닫는다.**

#### 채점이 새로 연 것 — **1회차 수리가 «과교정» 이었다**(둘 다 독립으로 같은 방향)

바닥을 펴는 데 성공했지만 **ref 보다 더 평평하게** 폈다: 배너 평탄부가 ref 보다
**+2.4~4.4px**(GM) · **+2.98~4.63px**(GL) 길다. 뿌리는 [지적 1] 과 하나다 — 아래 §6-1.

### §6 채점이 낸 결손 — **다음 회차 순서**(둘의 1순위가 이번엔 **같다**)

1. **★ 노치 «옆면 호» — 두 사람의 1순위이자 ② 를 8 로 막은 유일한 항목.**
   **ref 는 반타원에 가깝다**(GM): 가로 반축 = 깊이 31.5 · 세로 반축 ≈ 30.5 인 반타원이 예측하는 폭이
   ref 실측과 **≤1.5px** 로 맞는다. 우리는 깊이 90% 지점에서 **+7.9(배너) / +7.6(불릿) px** 벌어져 있다.
   **다음 회차의 과녁 표**(우리px · GM · 사다리 3단 부호·크기 불변 · **이번 캡처 r72 에서 새로 잰 값이다**):

   | 깊이 | ref 배너 | 우리 배너 | Δ | ref 불릿 | 우리 불릿 | Δ |
   |---|---|---|---|---|---|---|
   | 25 % | 57.58 | 59.17 | +1.59 | 92.74 | 91.27 | −1.47 |
   | 50 % | 51.39 | 54.50 | **+3.11** | 82.77 | 83.87 | +1.10 |
   | 75 % | 39.32 | 44.72 | **+5.40** | 64.75 | 68.32 | **+3.57** |
   | 90 % | 26.24 | 34.13 | **+7.89** | 44.06 | 51.61 | **+7.55** |

   GL 이 절대 깊이(우리px)로 잰 표도 같은 그림이다 — 깊이 26 에서 +5.07/+4.06 · 28 에서 +6.31/+5.52 ·
   기울기(dw/d깊이 · 깊이 10~26) ref 배너 −1.29 ↔ 우리 −1.15(**−11%**) · ref 불릿 −2.05 ↔ 우리 −1.86(−9%).
   ⚠ **1회차가 낸 «−26%» 는 쓰지 마라** — 깊이가 30/33 이던 다른 그림의 값이다.
   ⚑ **`border-radius` 한 손잡이로는 안 닫힌다는 것을 둘이 각자 논증했다** — GL: Δ 가 깊이 12~20 에서
   +3.4 로 평평하다가 26~28 에서 +6 으로 다시 솟아 **단조가 아니다**(매개변수 둘짜리 타원으로는 불가능).
   GM: 평탄부(ref 18.6/30.9)와 옆면 기울기를 **동시에** 요구하면 `--ntc-r` 하나로는 해가 없다.
   ⇒ **`clip-path`/SVG 경로로 옮기는 것이 다음 회차의 본체다.** 마스크·링·림 **셋이 같은 경로**를 써야
   하므로 회귀가 넓다 — `verify833` §15(«림이 검정 외곽선을 안 밟는다»)와 1회차 §2 의
   «구멍은 링 안쪽을 덮고 바깥 안에 든다» 대수 보장을 **먼저** 읽어라(경로로 바꾸면 그 보장이 깨진다).
   ⚠ **평탄부만 보고 맞추지 마라 — 그게 1회차 과교정의 원인이다**(GM).
2. **불릿 «마지막 알약 ↔ 리본1» 틈 +2.9~3.1px** — GM 이 **두 경계의 절대 좌표까지** 적었다
   (ref 547.09 / 569.38 ref px ↔ 우리 1550.50 / 1599.50). GL 도 «채움 대 채움» 자로 +2.24.
   **1회차까지 합쳐 다섯 번째 재현**이다. 1회차 §4-1 의 분해(카드 높이 772 → 770 후보)가 그대로 1순위.
3. **배지 분홍 판 우단 — GM 이 부호 갈림을 닫았다.** 분홍 계열 **부분화소** 마스크
   (`s(x) = clip((R−G)/dg, 0, 1)` 의 0.5 교차 — GK 의 «밝은 분홍 ∪ 짙은 마젠타» 가 자동으로 들어온다)로
   사다리 3단 전부 **+0.72 / +1.09 / +1.36** ⇒ **GD·GE·GF(+1.0 안팎)가 맞고 GG(−1.5)는 재현 안 된다.**
   ⚑ **과녁 [39]«+25.0» 자체를 +24.0 으로 내려야 한다**(ref +23.6~24.4 · 2인 일치) — **과녁부터 고쳐라.**
   판 크기 «181×181» 은 확인됐다(우리 181.0×181.0).
   ⚠ GL 은 같은 자리를 «±1px 양자화라 부호를 못 세운다» 로 닫았다 — **두 사람이 갈린 유일한 자리**이고,
   GM 쪽이 부분화소라 더 곱다. 다음 회차는 **GM 의 마스크 정의로** 재라.
4. **★ 새 축 — 배지 바깥 «검정 링» 이 ref 에는 없다**(GM ⑤ 축 · 크기 파생분만 채점).
   ref 배지 우단은 밝은 분홍 → **마젠타 림 (152,22,58) 3~4 ref px** → 바탕이고 **검정이 한 화소도 없다**.
   우리는 분홍 → (181,36,83) → **검정 5.9px** → 바탕이다. ⇒ **바깥 실루엣** 우단 ref +24.75 ↔ 우리 +30.36
   (**+5.61px**) · 실루엣 폭 ref ≈183.5 ↔ 우리 193.
   ⚠ **마스크를 안 밝히면 이 축은 부호가 통째로 뒤집힌다** — 분홍끼리 재면 +1.1(위 3번), 실루엣끼리
   재면 +5.6 이다. **두 마스크가 같은 물건을 안 센다.** 다음 회차가 이 축을 잡으려면
   «어느 쪽을 과녁으로 삼는가» 부터 정하고 시작하라.
5. **불릿 제목 잉크 폭 +6.6~8.6px(+2.4~3.2%)**(GM · 런 7개로 «같은 덩이를 셌다» 확인) ·
   GL +4.5~6.6(+1.6~2.4%). 높이는 둘 다 −0.9% 또는 부호 뒤집힘 ⇒ **advance/자간 축**(⑥, 통과 안 막음).
6. **노치 피치** — GL 이 배너 −1.1~−2.0 · 불릿 앵커 −1.4 로 올렸으나 **GM 이 오염으로 기각**했다(아래).

**⚑ 두 사람이 기각한 것 / 오염을 밝힌 것(다음 회차가 다시 올리지 마라)**

- ⚑⚑ **ref 초록 카드 y 385~545 의 «노치» 는 노치가 아니다 — 흰 티켓 일러스트의 톱니다**(GL 이 창 오염을
  자기신고 · GM 이 독립으로 같은 자리를 짚었다). 그 창에서 재면 «깊이 7.8~14.5 ref px» 같은 유령이 나온다.
  **ref 초록에서 쓸 수 있는 깨끗한 노치는 y 594~654 하나뿐이다.** 자를 새로 세우는 사람은 이것부터 읽어라.
- **노치 피치** — GM: 오염 안 된 **윗 옆면**으로 두 노치를 맞추면 ref 불릿 **227.3** ↔ 우리 227.0(Δ−0.3) ·
  ref 배너 138.2 ↔ 우리 139.0(Δ+0.8) ⇒ **맞다.** «끝점» 으로 중심을 잡으면 티켓 아트가 밀어 220.7(Δ+6.3)이
  나온다 — GL 의 −1.1~−2.0 은 그 계열이다. **이 자리를 올리려면 위 오염을 먼저 통과해야 한다.**
- **리본2 «꼬리» +10.0~10.9**(1회차 GK) — **GM 재현 실패**: 빨강 마스크 bbox 우단은 ref 521.1 ↔ 우리
  522.5/521.5 로 **Δ +0.4~1.4** 뿐이다. ⇒ «꼬리가 무엇의 어느 끝인가» 를 밝히지 않으면 못 세운다.
- **불릿 헤더 밴드 하단**(1회차 GK −1.5~−2.9) — **GM −0.70px = 측정 한계 안**(경계 정의를 밝혔다:
  카드 바깥 검정 상변 → 밴드색 ↔ 몸통색 G채널 50% 교차). **10회차 수리(112.0 → 111.0)는 맞았다.**
- **배너 제목 폭** — GL Δ −1.7/−1.7/+2.5 로 부호 뒤집힘 · GM −0.9~+1.4% ⇒ **맞다**(1회차 기각 지지).
- **[37] 9:19 ↔ 9:13.3** — GL: 노치 깊이 30.98~31.00 vs 31.00 · 곧은 우변 Δ ≤ 0.03px ·
  GM: 우변 프로파일 행별 |Δ| 중앙값 **0.039px**(c1) · **0.000px**(c2 전 행) · |Δ|₁ > 60 인 화소 0개.
  ⇒ **열 회차 연속 «기하 차이 없음».**

---

## 3회차 (2026-09-05, sess-1258-25821 루틴 워커 D) — **노치 «옆면 호»** · 제품 `index.html` 6자리

1·2회차 채점 **네 사람이 각자 1순위**로 낸 자리이고, ②(상대 크기)를 8 로 막은 유일한 항목이었다.

### §1 손잡이를 «반지름» 에서 **«프로필 표»** 로 갈았다 (2회차 채점이 그 길을 막아 뒀다)

2회차 채점 둘이 각자 «`border-radius` 한 손잡이로는 못 닫는다» 를 논증했다 — GL: 깊이별 Δ 가
12~20 에서 +3.4 로 평평하다가 26~28 에서 +6 으로 다시 솟아 **단조가 아니다**(매개변수 둘짜리
타원으로는 불가능) · GM: 평탄부와 옆면 기울기를 **동시에** 요구하면 `--ntc-r` 하나로는 해가 없다.
그래서 이번 회차는 값이 아니라 **기계**를 옮겼다.

- **자를 먼저 세웠다(338 규칙)** — `tools/scan923.py` 에 **`--prof`**(깊이별 세로 폭 · `width_at`)를 신설했다.
  2회차 채점이 손으로 만든 표를 자 안으로 들여온 것이다: 회차마다 손으로 다시 만들면 «자·문턱·창» 이
  회차마다 달라져 값이 안 이어진다(885 교훈). 폭도 **부분화소**로 잰다(깊이 u 를 처음·마지막 넘는
  두 행을 바깥 이웃과 선형 보간) — 정수 행으로 세면 한 칸이 그대로 3px 짜리 결손을 덮는다.
- **모양** — `NTC_PROF`(깊이 f ↔ 반폭 v · **열넷**)에서 `clip-path` 폴리곤을 낸다. 세 곡선
  (구멍 안쪽 · 검정 바깥 · 림 바깥)이 **같은 표를 서로 다른 (깊이, 길이) 로** 읽어, 옛 «세 타원이
  한 반지름에서 파생» 규약(833 §12)을 **그대로** 옮겼다.
- ⚑ **x 좌표를 `var(--ntc-d)` 파생으로 남긴 것이 이 수리의 숨은 절반이다.** 폴리곤을 JS 가 만들되
  좌표를 `calc(100% − f·dk·var(--ntc-d))` 로 적었다 — 숫자로 구워 넣었으면 `verify833` §R11
  («`--ntc-d` 한 곳만 되돌려도 셋이 같이 따라오는가»)이 **조용히 초록**이 됐을 것이다.

**값의 출처는 손이 아니라 자다** — 세 번 재고 세 번 고쳤다(옛 타원식 v 에서 «우리 − ref» 를 뺀 뒤
찍어 보고 잔차를 다시 뺐다). 최종 표의 잔차는 아래 §2.

### §2 실측 — 깊이별 세로 폭(우리 px · `scan923.py --prof` · ref 는 오염 안 된 자리만)

| 축 (깊이 %) | ref 배너 | 2회차 | **3회차** | ref 불릿 | 2회차 | **3회차** |
|---|---|---|---|---|---|---|
| 25 % | 56.66 | 58.00 (+1.34) | **57.00 (+0.34)** | 90.23 | 91.00 (+0.77) | **89.00 (−1.23)** |
| 50 % | 52.87 | 56.07 (+3.20) | **53.00 (+0.13)** | 81.26 | 85.02 (+3.76) | **81.00 (−0.26)** |
| 75 % | 40.75 | 45.88 (+5.13) | **41.04 (+0.29)** | 66.06 | 69.00 (+2.94) | **65.01 (−1.05)** |
| 90 % | 28.42 | 34.03 (+5.61) | **29.00 (+0.58)** | 43.88 | 53.00 (+9.12) | **42.99 (−0.89)** |
| 바닥 평탄부 | 18.57 | 19.00 | **18.00** | 26.82 | 28.00 | **27.5** |
| 보이는 깊이 | 31.40 | 31.00 | **31.46** | 31.67 | 31.00 | **31.51** |

**최대 |Δ| 9.12 → 1.23.** 2회차 채점이 «과교정» 으로 낸 축(바닥이 ref 보다 +2.4~4.6 더 평평하다)도
같은 방향으로 줄었다(배너 19.0 → 18.0 · 불릿 28.0 → 27.5).

**곁들여 닫은 것 둘**

1. **깊이 41 → 41.65.** 2회차가 «그릴 수 있는 것은 정수뿐» 이라며 남긴 잔차(−0.49px)는 «상자 + 반지름»
   시절의 제약이었다 — 폴리곤 좌표는 상자 폭 반올림에 안 매인다. 보이는 깊이 **31.46 / 31.51**
   (ref 31.40 / 31.67 · GM 최선 추정 31.49 ± 0.20). 2회차 §5 가 «[지적 1] 을 clip-path 로 옮길 때
   같이 닫는다» 로 넘긴 그 자리다.
2. **구멍(마스크) 세로 반지름 −1px.** 새 프로필은 어깨에서 **타원보다 얇다** ⇒ 옛 타원 구멍이
   깊이 2~10 구간에서 검정 링 바깥으로 최대 **0.63px** 삐져나온다(그 구간은 림이 833 10회차의
   «10px 안쪽» 으로 잘려 있어 가려 주지도 않는다). 세로만 1 줄이면 «구멍 ⊆ 검정 바깥» 이 여유
   0.37px 로, «구멍 ⊇ 검정 안쪽» 은 여유 20px 넘게 지켜진다. — 1회차가 «마스크는 한 줄도 안 건드렸다»
   로 지나간 자리이고, **모양을 바꾸면 그 대수 보장이 깨진다**는 것이 이번 회차의 실무 교훈이다.

### §3 게이트 — 신설 하나 · 이관 넷

- **`verify923` 54/54** — §A 를 폴리곤 파생으로 이관하고(모양이 선언에 있는가 · 두 꼭지가 `--ntc-d`
  파생인가 · **두 형이 한 프로필 표를 읽는가**), **[B5] 깊이별 폭 4자리 × 2형 신설**(±2.5).
  **되돌림이 둘이 됐다** — ⓐ 순수 반원으로 되돌리면 [B1](바닥)이 무너지고 ⓑ **2회차 타원
  (`50% / --ntc-r`)으로 되돌리면 [B5] 만 무너진다**(배너 |Δ|max 7.59 · 불릿 9.12).
  ⚑ **ⓑ 가 왜 따로 필요한지가 이 회차의 소득이다** — ⓐ 만 두면 [B5] 가 **배너에서 안 빨개진다**
  (|Δ|max 1.34). ref 배너 노치는 «거의 반원» 이라 순수 반원이 우연히 어깨까지 맞는다
  (667 7회차 주석이 이미 «배너는 스팬 ≈ 2×깊이» 라고 적어 뒀다). 그래서 [B5] 가 지키는 것은
  «반원이 아님» 이 아니라 **«2회차의 타원이 아님»** 이다. 짝 항 **[R4]** 가 그때 바닥은 초록임을
  같이 찍는다 = **[B1] 만 있었으면 이 결함을 못 봤다**는 증거.
- **이관 `verify833` 281/281** — §12 의 잣대를 «상자 폭» 에서 **폴리곤 꼭지**로 옮겼다(뜻은 8회차 그대로:
  그려진 깊이 = 선언 · 넷이 한 값 파생 · 두께 10/12 불변). §15 는 «10px 안쪽 자르기» 가 `clip-path`
  에서 **상자**(`inset:0 10px 0 0`)로 옮겨진 것을 따라갔다 — 한 요소에 clip-path 를 둘 걸 수 없어서다.
- **이관 `verify667` 92/92** — [A3] 이 «링 상자 높이» 를 묻던 것을 **폴리곤 바깥 곡선의 세로 스팬**으로
  바꿨다(상자를 물면 늘 len+24 라 **항이 늘 초록인 벙어리**가 된다). [R1] 되돌림도 제품 자신의
  `pvNtcPoly(70, …)` 로 «길이 70 짜리 폴리곤» 을 실어 준다.
- **이관 `verify124` 77/77** — [2-s2] 래칫이 «노치 수 × 2» → **0**. 옛 링은 상자보다 넓어 넘쳤고
  `.ntc{overflow:hidden}` 이 잘랐는데, 이제 넘치는 것이 없다. ⚠ 그러면 [R6-b]가 **늘 초록인 항**이
  되므로 **[R6-b2] 신설** — 옛 «2d × len» 링을 되살리고 클리핑을 떼면 자가 다시 «샌다» 로 잡는가
  (679 규약 — 축을 바꾼 것과 항을 끈 것을 가르는 자리다).
- 회귀 무수정: `verify151` 84/84 · `verify153` 53/53 · `verify895` 18/18 · `verify866` 18/18 · **SMOKE PASS**.
- **[37] 9:19 ↔ 9:13.3** — 크롭 차분 **최대 |Δ|₁ 44 · 60 넘는 화소 0개**(카드 뒤 바탕 그라디언트뿐)
  ⇒ **열한 회차 연속 «기하 차이 없음»**.

### §4 이번 회차가 **안 건드린** 것 (다음 회차가 이어받는다)

2회차 §6 의 2~5 가 그대로 유효하다: ① 불릿 «마지막 알약 ↔ 리본1» 틈 +2.9~3.1(다섯 번째 재현 ·
1순위 후보는 카드 높이 772 → 770) · ② 배지 분홍 판 우단(**과녁 [39] 자체를 +24.0 으로 내리는 것이 먼저**) ·
③ 배지 바깥 «검정 링» 이 ref 에는 없다(⑤ 축 · **어느 마스크가 과녁인지부터** 정할 것) ·
④ 불릿 제목 잉크 폭(⑥ 축).

### §5 채점 — **비평 2인 독립(GN · GO) · ①~④ 최저 «7 / 8» ⇒ 통과선 9 미달, 회차 계속(3/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GN | 9 | **7** | 8 | 9 | 8 | 8 | **7** |
| GO | 9 | **8** | 8 | 9 | 9 | 9 | **8** |

**둘 다 ② 를 막은 1순위가 같다 — 노치 «입» 이 곧은변과 직각으로 만난다**(ref 는 접선으로 스며드는 S자).
2회차(7/7 → 8/8) 대비 옆면 25~90% 는 둘 다 «닫혔다» 로 봤다(GO: 배너 +1.6/+3.6/+5.7/+8.0 → +0.6/+1.1/+2.0/+1.8).

#### ⚑⚑ 채점이 **자의 결함 둘**을 찾아냈다 — 3회차가 신설한 그 자다

두 사람이 **각자** 같은 두 곳을 짚었다(GN §4-d·§4-e · GO §5):

- **ⓐ 부호** — `width_at()` 의 아래쪽 교차가 `j + t` 로 **바깥으로** 보간해 폭이 최대 2행 부푼다.
  ⚠ **1행의 실물 길이가 그림마다 다르다**(ref 1행 = 2.0628 우리px · 우리 1행 = 1) ⇒ **ref 가 두 배 더
  부푸는 비대칭 편향**이고, 그래서 «우리가 넓다» 는 키우고 «좁다» 는 감춘다.
- **ⓑ 창** — 이웃을 «깊이 ≥ 6» 인 행 안에서만 찾아 입구 쪽 교차가 경계에 물리면 잘렸다.

**3회차의 `NTC_PROF` 가 그 값으로 만들어졌고, 그것이 불릿형 과교정(−2.8~3.1px)의 뿌리다.**
자를 못 믿게 된 김에 셋째 결함까지 드러났다 — 아래 §3(4회차).

---

## 4회차 (2026-09-05, sess-1258-25821 루틴 워커 D · 3회차와 한 세션) — **자 수리 + 표를 형마다** · 제품 `index.html` 표 2벌

### §1 자부터 고쳤다 (338 규칙 — 처방 전에 자를 믿을 수 있어야 한다)

`tools/scan923.py` · `tools/verify923.js` 양쪽에서 위 ⓐⓑ 를 고쳤다. 그리고 **셋째 결함**이 남아 있었다 —
고친 자로도 ref 불릿 25% 가 **90.7** 인데 비평 2인은 각자 **92.85** 를 냈다.

⇒ **뿌리는 «문턱 자» 자체였다.** 문턱 50% 교차는 **하드 에지에서 한쪽으로 치우치고**(우리 곧은변 참값
1017.50 ↔ 문턱 자 1017.95 — GN 실측), ref 는 JPEG 라 경사면이 넓어 그 치우침이 **두 그림에서 다르게**
걸린다 = «ref 만 얇게 읽힌다». **이것이 저장소에 이미 등재된 942(«번짐 편향 · 자 11개»)의 얼굴이고,
942 의 1순위 처방이 «질량(덮개) 적분» 이다.** 그 처방을 이 자에 적용했다:

> 경계 화소는 «검정 외곽선 ↔ 바탕» 두 색의 섞임뿐이므로 **α = |p − bg|₁ / |검정 − bg|₁ 가 곧 부분화소
> 덮개**다. 꽉 찬 마지막 화소부터 α 를 더한다.

⚠ **분모를 `d` 의 최댓값으로 잡으면 안 된다** — 바탕이 어둡고(54,54,63) 몸통은 크림이라 **몸통의
|Δ바탕|₁(≈579)이 검정의 것(≈171)보다 세 배 크다** ⇒ «꽉 찬 화소» 가 몸통까지 밀려 경계가 4px 안으로
들어간다(4회차에 한 번 밟았다). 분모는 **그 자리에서 가장 검은 화소**다.

**세 자가 만났다** — ref 배너 곧은변 GN(CUE) **485.587** ↔ 이 자 **485.63** ↔ GO(언믹싱) 485.59.
깊이별 폭도 ref 배너 57.53/50.80/38.95/26.17 로 **세 자가 0.2px 안**이다(3회차 자는 56.66/52.87/40.75/28.42 였다).

### §2 표를 **형마다 한 벌**로 (3회차의 «한 표» 를 두 자가 값으로 반박했다)

3회차는 두 형에 한 표를 썼는데 채점 2인의 Δ 가 **반대 방향으로 일치**했다 —
배너 75·90% **+1.8~2.0 넓다** ↔ 불릿 25·50% **−2.8~3.1 좁다**(GN·GO 가 0.1px 안에서 같은 값).
⚑ **근거는 새것이 아니다 — 667 7회차가 이미 «두 형의 스캘럽 모양이 다르다»**(ref 배너는 거의 반원 ·
불릿은 납작한 호)고 적어 두었다. 3회차의 «한 표» 가 그 관측과 어긋나 있었다.

**수리 후(덮개 자 · ref 와 우리에게 같은 절차 · 우리 px)**

| 축 | ref 배너 | **우리** | Δ | ref 불릿 | **우리** | Δ |
|---|---|---|---|---|---|---|
| 깊이 25 % | 57.53 | 57.94 | **+0.41** | 92.61 | 92.30 | **−0.31** |
| 깊이 50 % | 50.80 | 50.94 | **+0.14** | 82.36 | 82.90 | **+0.54** |
| 깊이 75 % | 38.95 | 38.99 | **+0.04** | 63.89 | 64.45 | **+0.56** |
| 깊이 90 % | 26.17 | 26.59 | **+0.42** | 42.26 | 43.02 | **+0.76** |
| 바닥 평탄부 | 17.54 | 18.00 | +0.46 | 24.75 | 24.00 | −0.75 |
| 보이는 깊이 | 31.79 | 31.68 | −0.11 | 32.11 | 31.66 | −0.45 |

**여덟 자리 전부 |Δ| ≤ 0.76.** (3회차 채점이 잰 값으로는 배너 +2.0 · 불릿 −3.1 이었다.)

### §3 게이트

- **`verify923` 54/54** — 자 이관(덮개 적분 · 부호·창 수리) · **과녁 여섯 축을 전부 다시 쟀다**
  (⚠ 옛 문턱 자 값과 섞어 쓰지 마라 — 불릿 길이 92.8 ↔ **97.0** 처럼 2~4px 씩 다르다) ·
  **[A4] 방향 전환**(«두 형이 한 표» → «같은 f 격자를 쓰되 값은 다르다») + **[A4b] 신설**(값이 정말 갈렸는가) ·
  **[R4] 정정** — 3회차는 «되돌리면 두 형 다 바닥은 초록» 이라고 걸었는데, 덮개 자로는 2회차 타원이
  **불릿의 바닥까지** 무너뜨린다 ⇒ 주장을 «**적어도 한 형에서** 바닥 초록 + 폭 빨강» 으로 좁혔다(배너가 그 자리다).
- 회귀 무수정: `verify833` **281/281** · `verify667` **92/92** · `verify124` **77/77** · `verify151` 84/84 · **SMOKE PASS**.

### §4 다음 회차 — **1순위는 «노치 입 필렛»** (채점 2인 공통 1순위 · 이번 회차가 못 건드렸다)

ref 는 노치가 곧은변에 **접선으로** 스며드는데 우리는 **모서리로** 꺾인다.
**램프 길이(깊이 1 → 6 우리px 를 지나는 세로 거리)**: 배너 ref 6.52 ↔ 우리 1.20(**−5.32**) ·
불릿 ref 10.34 ↔ 우리 1.57(**−8.78**) — GO 사다리 4단 부호·크기 안정 · ref 흐림(1.1 ref px)의 7배라 번짐이 아니다.
깊이별 폭으로는 **깊이 2 에서 배너 −6.7 · 불릿 −16.1**, 깊이 1 에서 −10.5 / −21.4 다.

**GO 가 준 과녁 표(깊이 u ↔ 세로 폭 · 우리px)** — 다음 회차가 그대로 쓴다:

| u | ref 배너 | 우리 | ref 불릿 | 우리 |
|---|---|---|---|---|
| 1 | 72.07 | 61.55 | 115.72 | 94.31 |
| 2 | 67.47 | 60.75 | 109.65 | 93.60 |
| 3 | 63.97 | 60.35 | 104.47 | 92.91 |
| 4 | 61.82 | 59.95 | 100.16 | 92.34 |
| 6 | 59.39 | 59.15 | 95.95 | 91.18 |
| 8 | 57.50 | 58.13 | 92.81 | 89.71 |
| 10 | 55.89 | 57.06 | 90.07 | 87.91 |

⚠⚠ **이 수리는 `.ntc` 상자를 세로로 키워야 한다** — 표에 v > 1 을 넣으면 세 곡선이 상자를 넘는다
(불릿 v(0) ≈ 1.23 이면 림 바깥 곡선이 1.23 × 69 = **84.9** 로 상자 반높이 69 를 16 넘는다).
상자를 키우면 `verify667` [A5]«상자는 링보다 위·아래로 12 씩 넓다» 가 같이 이관돼야 하고,
배너 피치 139 ↔ 상자 141 로 이웃 상자가 겹치기 시작한다(폴리곤은 안 겹치므로 그림은 안 바뀐다 —
겹침을 세는 자가 있으면 그 자도 봐라).

**그 다음**: ③ 을 8 로 막은 **불릿 «마지막 알약 ↔ 리본1» 틈 +2.9~3.35**(GO 로 **6번째 재현** ·
GO 분해: 알약 아랫변 +1.85 · 리본1 윗변 −1.00 — **두 경계가 각각** 벌어져 한쪽만 옮기면 안 된다) ·
배지 바깥 «검정 링»(실루엣 우단 +5.0~5.2 · 2인 일치 · 분홍 판 자체는 +0.2~0.65 = 제자리).

**⚑ 두 사람이 각자 기각한 것(다시 올리지 마라)**: «과녁 [39] +25.0 이 ref 보다 크다» — **둘 다 재현
실패**(GN ref +25.4~26.5 · GO ref +24.89, 두 카드가 0.02~0.04px 안에서 일치) ⇒ 2회차 GM 의 그 지적은
닫혔다. 배너형 알약↔리본 틈(+0.63 = 맞다) · 불릿 밴드하단↔첫알약(+0.51 = 맞다) · 두 형의 깊이(맞다) ·
불릿 W90(사다리에서 부호 뒤집힘 = 측정 한계) · [37] 두 프레임(**열두 회차 연속** 차이 없음).

---

## 5회차 (2026-09-05, sess-1428-10253 루틴 워커 B) — **노치 «입» 필렛** · 제품 `index.html` 3자리

4회차 채점 2인(GN·GO)이 **공통 1순위**로 낸 자리이고, 4회차가 못 건드리고 넘긴 하나다.

### §1 자부터 세웠다 (338 규칙) — «입» 은 4회차까지의 자에 **없던 자리**였다

4회차까지 깊이별 폭은 **D 의 비율**(25·50·75·90%)로 읽는다. 그 격자는 **입구 첫 8px 을 통째로
건너뛴다** — 배너 25% 가 이미 깊이 7.9px 이다. 램프(깊이 1 → 6)가 사는 자리가 바로 그 안이라,
채점 둘이 «접선 ↔ 모서리» 를 각자 1순위로 내는데도 **자는 여덟 자리 전부 초록**이었다.

⇒ `tools/scan923.py` **`--mouth`** 신설 · `verify923` **[B6]** 신설 — 깊이를 **절대 px**(u = 1·2·3·4·6)로
읽는다. 자는 새로 만들지 않았다(같은 `width_at`/`wAt` 에 격자만 다르게 넣는다).
⚠ **비율이 아니라 절대 px 인 이유** — 두 그림의 D 가 0.3px 만 달라도 «같은 %» 가 서로 다른 실물
깊이를 가리키고, 램프가 가파른 입구에서는 그 차이가 폭 1px 로 증폭된다.

### §2 처방 — 값 하나가 아니라 **표의 «머리»** (`NTC_PROF` f ≤ .16 신설 · v > 1)

- 3회차가 세운 «프로필 표» 기계를 그대로 쓴다. 표의 머리에 f = 0/.03/.06/.10/.13/.16 을 신설하고
  **v > 1**(배너 머리 **1.088** · 불릿 **1.155**)을 줬다 — v > 1 은 «노치가 곧은변에 닿기 전에
  이미 벌어져 있다» = 접선 블렌드다.
- ⚑ **상자도 표에서 파생시킨 것이 이 수리의 숨은 절반이다.** v > 1 이면 세 곡선이 상자를 넘고
  `.ntc{overflow:hidden}` 이 그 입을 도로 잘라 **«표만 바뀌고 그림은 안 바뀐다»** 가 된다.
  ⇒ `pvNtcMargin(pf, len)` 신설(림 스팬 = vmax·(len+24) 에서 파생 · 손 상수 없음) ·
  여백을 `--ntc-m` 으로 **선언**해 CSS 가 읽는다(§R 되돌림이 그 몫을 되돌린다).
  배너 상자 105 → **117**(피치 139 안) · 불릿 138 → **162**(피치 227 안) ⇒ **이웃 겹침 0**.
- ⚑⚑ **어디서도 «좁히지» 않았다 — 그것이 이번 회차의 제약이었다.** 마스크(타원 구멍)가
  «검정 바깥 곡선 안» 에 들어가는 여유가 3회차 이후 **0.2~0.4px** 뿐이라(3회차 §2-2), 얕은 구간을
  좁히면 그 대수 보장이 깨져 노치 테두리로 바탕이 샌다. 그래서 f ≥ .20(배너)·f ≥ .30(불릿)은
  **한 값도 안 건드렸다.** v > 1 은 구멍을 **덮는 쪽**이라 그 보장을 오히려 강화한다.
  (배너 f = .25 의 `.953` 은 옛 두 점을 잇는 직선 위의 점이다 — 기하 Δ0px · [A4]«두 형이 같은 f 격자»
  를 지키려고 넣은 것이다.)

### §3 실측 — «입 폭»(우리 px · 절대 깊이 · `scan923.py --mouth`)

| 깊이 u | ref 배너 | 4회차 | **5회차** | ref 불릿 | 4회차 | **5회차** |
|---|---|---|---|---|---|---|
| 1 px | 65.01 | 61.11 (−3.90) | **64.49 (−0.52)** | 106.51 | 94.71 (−11.80) | **105.54 (−0.97)** |
| 2 px | 63.88 | 60.70 (−3.18) | **63.47 (−0.41)** | 104.36 | 94.42 (−9.94) | **103.72 (−0.64)** |
| 3 px | 62.75 | 60.29 (−2.46) | **62.45 (−0.30)** | 102.22 | 94.12 (−8.10) | **101.90 (−0.32)** |
| 4 px | 61.61 | 59.88 (−1.73) | **61.43 (−0.18)** | 100.08 | 93.83 (−6.25) | **100.07 (−0.01)** |
| 6 px | 59.35 | 59.05 (−0.30) | **59.40 (+0.05)** | 95.79 | 93.24 (−2.55) | **96.43 (+0.64)** |
| 8 px | 57.49 | 57.84 (+0.35) | **57.93 (+0.44)** | 92.65 | 92.24 (−0.41) | **92.78 (+0.13)** |
| 10 px | 55.89 | 56.51 (+0.62) | **56.54 (+0.65)** | 89.90 | 90.91 (+1.01) | **90.93 (+1.03)** |

**최대 |Δ| 11.80 → 1.03**(그 1.03 은 u10 = 4회차부터 있던 값이고 입 구간은 ≤ 0.97).
4회차가 닫아 둔 여덟 자리(25·50·75·90% × 두 형)는 **|Δ| ≤ 0.73 으로 그대로**이고 바닥·깊이도 불변이다.

### §4 자 갈림을 그대로 남긴다 (934 규약 — 셋째 자가 정한다)

4회차 채점 GO(언믹싱 자)는 같은 ref 를 u1 에서 **72.07 / 115.72** 로 읽는다. 이 자(덮개 적분)는
**65.01 / 106.51** 이다. **u ≥ 4 는 두 자가 0.3px 안에서 일치**하고 갈리는 것은 **입구 3px 뿐**이다
(ref 는 JPEG 라 경사면이 넓다 — 942 «번짐 편향»). ⇒ **둘 중 좁은 쪽을 과녁으로 삼되** f = 0 의
머리만 접선이 되게 조금 더 벌렸다(선형 외삽 1.085/1.156 → 채택 1.088/1.155).
넓은 쪽으로 잡으면 이 자로는 **+7px 과교정**이고, 3회차가 그 방향으로 한 번 데였다(4회차 §2).
**5회차 채점 2인에게 «너희가 셋째 자다» 를 브리핑에 명시했다.**

### §5 게이트 — 신설 둘 · 이관 둘

- **`verify923` 77/77**(54 → 77) — **[B6] 신설**(입 폭 u1~u6 × 두 형 · 창 ±2.0 = [B5] 의 ±2.5 보다 좁다.
  수리 전 값 −3.90/−11.80 은 그 두 배 넘게 밖이다) · **[R5]·[R5b] 신설**(되돌림 셋째 —
  표의 **머리만** v = 1 로 누르면 **[B6] 은 빨강**(배너 3.39 · 불릿 11.76)**이고 [B5] 는 전부 초록**
  (0.49 / 0.74) ⇒ «[B6] 이 [B5] 로 대체되지 않는다» 의 증거. 3회차가 [R4] 로 세운 것과 같은 꼴이다).
  ⚑ **[R1]·[R3] 되돌림 CSS 도 이관했다** — 옛 사보타주는 «상자 = len + 24» 를 전제로
  `inset:12px` · `height:calc(100% − 24px)` 를 적었는데, 상자가 표에서 파생되면서 그 전제가 깨져
  되돌림이 **모양이 아니라 자리를 흔들고 있었다**(평탄부 0.0 · 폭 n/a = 조용한 빨강).
  `var(--ntc-m)` 으로 그 몫을 되돌리게 고쳤다 — 그래서 이 되돌림은 여전히 **모양만** 시험한다.
- **이관 `verify667` 92/92** — **[A3]** 잣대에서 «입» 몫을 나눈다(스팬 ÷ 프로필 vmax = 노치 길이 ·
  vmax 는 제품 `NTC_PROF` 에서 읽는다 ⇒ 표를 바꾸면 자가 따라오고 `NTC_LEN` 을 건드리면 여전히 빨강) ·
  **[A5]** 는 **뜻을 산수 대신 직접** 묻는다 — 옛 항 «상자 = 길이 + 2×12» 는 «림의 호가 안 잘린다» 의
  **대역**이었는데 상자가 표에서 파생되며 그 산수가 깨졌다 ⇒ 이제 «상자 ≥ 림 폴리곤 세로 스팬
  (여유 ≤ 4px) · 림 상자도 같은 높이» 를 묻는다(여백을 손으로 부풀리면 빨강).
- 회귀 무수정: `verify833` **281/281** · `verify124` 77/77 · `verify151` 84/84 · `verify153` 53/53 ·
  `verify895` 18/18 · `verify866` 18/18 · **SMOKE PASS** · 콘솔 에러 0.

### §6 이번 회차가 **안 건드린** 것 (다음 회차가 이어받는다)

4회차 §4 의 «그 다음» 이 그대로 유효하다: ① 불릿 «마지막 알약 ↔ 리본1» 틈 +2.9~3.35
(**여섯 번째 재현** · GO 분해 = 두 경계가 **각각** 벌어졌다) · ② 배지 바깥 «검정 링»(실루엣 우단
+5.0~5.2 · 분홍 판 자체는 제자리 · ⑤ 축 · **어느 마스크가 과녁인지부터**) · ③ 불릿 제목 잉크 폭(⑥ 축).

**곁들여 확인한 것 — «샘» 이 안 생겼다.** 이번 처방이 넓히는 쪽뿐이라 대수적으로는 안전하지만
찍힌 화소로도 확인했다: 카드 우변 안쪽 60px 띠에서 «바탕이 보이는 행» 중 **그 왼쪽 3px 에 검정이
없는 행**(= 검정 링 밖으로 바탕이 새는 행)이 수리 전 **20** · 수리 후 **20** 으로 **같다**
(남는 20 은 상태 탭·배지가 외곽선을 끊는 자리 = ref 도 끊긴다 · 833 9회차 DH).

### §7 채점 — **비평 2인 독립(GP · GQ) · ①~④ 최저 «8 / 8» ⇒ 통과선 9 미달, 회차 계속(5/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GP | 9 | **8** | 9 | 9 | 9 | 9 | **8** |
| GQ | 9 | **8** | 9 | 9 | — | — | **8** |

3회차 7/8 → (4회차 무채점) → **5회차 8/8**. **둘 다 ② 를 막은 1순위가 같다 — «입이 아직 절반만
스며든다»**(GP: 램프 1→6 불릿 4.6~5.0 ↔ ref 8.9~12.1 · GQ: u1 배너 −7.14 · 불릿 −10.59).
**과교정은 둘 다 «없다» 로 독립 확인**했고(u ≤ 4 는 어느 자로도 음수), 3·4회차가 닫아 둔
25~90%·바닥·깊이는 **둘 다 재확인해 초록**이었다(GQ ≤ +0.5px · GP «Δ ≈ 0»).

#### ⚑⚑ 채점이 낸 것 — **과녁이 틀렸다**(둘이 각자 셋째 자를 세웠고 둘 다 «넓은 쪽»)

5회차는 갈린 두 자 중 **좁은 쪽**(ref u1 배너 65.01 · 불릿 106.51)을 과녁으로 골랐다. 그런데:

| ref u1 (우리 px) | 4회차 GO(언믹싱) | 5회차 GP | 5회차 GQ | 5회차 자(`scan923`) |
|---|---|---|---|---|
| 배너 | 72.07 | 68.2~72.5 (중앙 70.9) | **72.1** | 65.01 |
| 불릿 | 115.72 | 113.3~118.9 (중앙 115.5) | **116.5** | 106.51 |

**셋 대 하나**다. GQ 는 «내 자 셋·사다리 아홉 칸·X0 를 −0.5px 까지 밀어도 한 번도 106.5 근처로
안 내려간다» 를, GP 는 «내 자 넷 중 어느 것도 ref 를 그렇게 낮게 안 읽는다» 를 각자 적었다.

## 6회차 (2026-09-05, sess-1428-10253 루틴 워커 B · 5회차와 한 세션) — **자의 «천장» · 과녁 재조준 · 입 전용 구멍** · 제품 `index.html` 5자리

### §1 ⚑⚑ **«자 갈림» 은 갈림이 아니라 내 자의 천장이었다**

6회차는 채점의 과녁(72.1 / 116.5)을 그대로 넣기 전에 **왜 두 자가 갈리는지**부터 팠다.
표를 그 과녁으로 벌리고 찍어 보니 **자가 거의 안 움직였다** — 기하는 72.2 인데 자는 64.9 를 냈다.
⇒ 뿌리는 `width_at()`/`wAt()` 의 **창**이었다:

> 노치 창 `[i0,i1]` 은 «깊이 ≥ 6» 인 행이고, 이웃은 그 밖으로 **3행까지만** 빌린다.
> ⇒ **얕은 u 의 폭이 «창 + 3행» 에서 잘린다.** 배너의 그 천장이 **65.4** 다.

**5회차가 ref 를 65.01 로 읽은 것은 ref 의 입이 그 천장에 눌린 값**이고, 우리 5회차 렌더가
64.5 로 «과녁에 맞은» 것도 같은 천장이었다(기하 65.1 이 우연히 천장과 같았다).
천장을 걷어내니(창 밖으로도 **깊이 ≥ u 이면서 얕아지는 동안** 걸어 나간다) 같은 자가 ref 를

**배너 72.02 / 72.31 · 불릿 118.27**

로 읽는다 — GQ 72.1 / 116.5 · GO 72.07 / 115.72 · GP 68.2~72.5 / 113.3~118.9 와 **같은 자리**다.
⇒ **갈림은 닫혔다.** 넷째 자를 세울 일이 아니었고, 세 사람이 옳았다.
⚠ 걸음에는 **단조 가드**가 필요하다(얕아지는 동안만) — 없으면 ref 초록의 **티켓 톱니**에서 이웃
오목부와 창이 합쳐져 폭이 **음수**로 나온다. 오염된 창에서는 이제 숫자 대신 **n/a** 로 답한다(939).

### §2 처방 — 표의 머리를 새 과녁으로 · **입 전용 구멍 한 겹**

- `NTC_PROF` 머리 재작성(배너 v(0) 1.088 → **1.257** · 불릿 1.155 → **1.315**). f ≥ .20 은 이번에도
  **한 값도 안 건드렸다**(채점 2인이 «u ≥ 6 은 이미 +0.5~1.5 벌어져 있으니 f ≤ .16 만 열어라» 로 일치).
- ⚑⚑ **표만 벌려서는 안 됐다 — 구멍이 입을 잘라 내고 있었다.** 찍힌 실루엣은 «구멍 ∩ 검정 안쪽
  곡선» 인데, 불릿 v(0)=1.315 면 안쪽 곡선이 ±61.8 인 반면 타원 구멍은 ±56 이라 **5.8px 이 잘린다**
  (배너는 ±38.3 < 39.5 라 안 잘린다 — 그래서 **배너만 조금 움직이는** 얼굴로 나타났다).
  ⇒ **입 전용 구멍 한 겹**을 얹었다(`pvNtcMouth` · 마스크는 «구멍» 이라 `intersect` 가 곧 합집합).
  타원 하나로는 못 한다 — 필요한 구멍이 «입은 넓고 가운데는 좁다» 라서 주 타원의 세로를 키우면
  중간 깊이에서 검정 바깥으로 샌다(3회차 §2-2 의 0.2~0.4px 여유가 그 자리다).
  값은 **표에서 파생**한다(세로 = v(0)·안쪽반길이 + 0.5 · 가로 = 주 구멍과 합쳐 안쪽 곡선을 다 덮는
  가장 얕은 깊이 + 2 · 주 구멍이 이미 덮으면 **층을 안 만든다** ⇒ 배너는 층 없음).

### §3 실측 — 천장을 걷은 자로 다시 (우리 px · `scan923.py --mouth`)

| 깊이 u | ref 배너 | 5회차 | **6회차** | ref 불릿 | 5회차 | **6회차** |
|---|---|---|---|---|---|---|
| 1 px | 72.17 | 64.49 (−7.68) | **72.21 (+0.04)** | 118.27 | 105.54 (−12.73) | **116.80 (−1.47)** |
| 2 px | 67.46 | 63.47 (−3.99) | **67.69 (+0.23)** | 109.67 | 103.72 (−5.95) | **110.10 (+0.43)** |
| 3 px | 63.94 | 62.45 (−1.49) | **64.23 (+0.29)** | 105.46 | 101.90 (−3.56) | **104.57 (−0.89)** |
| 4 px | 61.78 | 61.43 (−0.35) | **62.02 (+0.24)** | 100.77 | 100.07 (−0.70) | **100.66 (−0.11)** |
| 6 px | 59.35 | 59.40 (+0.05) | **59.52 (+0.17)** | 95.79 | 96.43 (+0.64) | **96.43 (+0.64)** |
| 8 px | 57.49 | 57.93 (+0.44) | **57.96 (+0.47)** | 92.65 | 92.78 (+0.13) | **92.78 (+0.13)** |
| 10 px | 55.89 | 56.54 (+0.65) | **56.54 (+0.65)** | 89.90 | 90.93 (+1.03) | **90.93 (+1.03)** |

(5회차 칸은 **천장을 걷은 자로 다시 잰 값**이다 — 5회차 표의 «−0.52 / −0.97» 은 천장에 눌린 읽기였다.)
**입 구간 최대 |Δ| 12.73 → 1.47.** 닫혀 있던 축은 그대로다(배너 25% +0.49 · 50% +0.12 · 75% +0.02 ·
90% +0.37 · 불릿 25% +0.25 · 50% +0.51 · 75% +0.60 · 90% +0.71 · 바닥·깊이 불변).

### §4 게이트 — 신설 둘 · 자 수리 둘

- **`verify923` 82/82**(77 → 82) — **자 수리**(`wAt` 천장 제거 + 단조 가드 + 뒤집힌 폭은 `null`) ·
  **[B6] 과녁 재조준**(ref 를 천장 없이 다시 잰 값 = 채점 셋의 자리) ·
  **[B7a]/[B7b] 신설**(구멍 ⊇ 검정 안쪽 · 구멍 ⊆ 검정 바깥 — 6회차가 얹은 구멍 층의 산수를 자로 세운다.
  ⚠ [B7b] 창 −0.6 은 **3회차부터 있던 값**이다: 배너 −0.43 이 4회차 트리에서도 같다) ·
  **[R6] 신설**(되돌림 넷째 — **입 구멍 층만** 떼면 **불릿만** [B6-u1] 이 빨강 7.60 · 배너는 초록 0.02
  ⇒ 그 비대칭이 «이 층이 실제로 일을 한다» 는 증거) · [R5] 는 새 과녁에서도 그대로 빨강(9.96 / 23.54).
- 회귀 무수정: `verify667` 92/92 · `verify833` 281/281 · `verify124` 77/77 · `verify151` 84/84 ·
  `verify153` 53/53 · `verify895` 18/18 · `verify866` 18/18.

### §5 채점 — **비평 2인 독립(GR · GS) · ①~④ 최저 «8 / 8» ⇒ 통과선 9 미달, 회차 계속(6/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GR | 9 | **9** | **8** | 9 | 9 | 8 | **8** |
| GS | 9 | **8** | **8** | 9 | 9 | 8 | **8** |

**⚑ 입은 닫혔다 — 그것이 이 회차의 소득이다.** GR 이 ② 를 **8 → 9** 로 올렸고(«배너 u1~u10 전 구간
|Δ| ≤ 0.8 · r5 의 모서리 꺾임이 r6 에서 사라지고 ref 와 같은 접선 진입»), **과교정은 둘 다 «없다»**
로 닫았다(GS 의 교차보간 없는 «넓이자»: 등가폭 |Δ| ≤ 0.36 · GR 은 불릿 u1 만 «+0.7~1.5 · 신뢰 중»
이라 적고 «내 ref 읽기가 여섯 자 중 가장 좁아 과교정을 최대로 보게 편향돼 있다» 고 스스로 달았다).
⇒ **네 자(GO·GP·GQ·GR·GS)가 이제 u ≥ 2 에서 1px 안에 모인다.** ref 불릿 u1 한 칸만 자마다 갈려
(GS 자 A 114.12 ↔ 자 B 118.05) **측정 한계**로 닫는다.

#### 채점이 새로 연 것 둘 — **7회차 순서**

**① [1순위 · 둘 다] 불릿 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈 +3.0 ~ +3.14px** — **아홉·열 번째 재현**.
5회차까지 «분해가 갈렸다»(GP «리본 제자리·알약이 위» ↔ GQ «둘 다 움직였다»)던 자리를 **셋째·넷째 자가
갈랐다** — 그리고 **둘 다 «세 번째 몫» 을 찾아냈다**:

| 몫 | GR (원점 = 카드 안쪽 변) | GS (원점 = 카드 바깥 변 · 3알약 등가) |
|---|---|---|
| 리본1 윗변 | −0.10 (**제자리**) | −1.22 (아래) |
| 마지막 알약 아랫변 | −1.60 (위 · 그중 −0.76 은 헤더 밴드 몫) | −0.84 (위) |
| **카드 높이 자체** | **+1.28** | **+1.08** |
| 합 | +2.98 ≈ 잰 값 +3.01 ✔ | +3.14 = 잰 값 ✔ |

⇒ **둘 다 산수가 닫힌다**(리본 몫만 갈린다 — GR 은 사다리에서 부호가 뒤집혀 «0 과 구분 안 됨» 이라 적었다).
**공통 결론: 손잡이는 «알약을 내려라» 도 «리본을 올려라» 도 아니고, 셋째 몫(카드 높이 ≈ +1.1~1.3)을
포함한 틈 자체다.** ⚠ 대조 — 배너형 카드 안쪽 높이는 오히려 **−1.7** 이라 «카드가 통째로 길다» 는 아니다.

**② [GS 신설 · 이번 회차가 만든 것] 노치를 따라 검정 띠가 얇아진다** — 곧은변 10.00 ↔ 노치 최소
**8.08**(ref 9.93 = 곧은변 대비 −0.28 만 준다) ⇒ **Δ −1.85** · 문턱 사다리 5단 부호 불변 · 신뢰 상.
**5회차 8.77 → 6회차 8.08 이므로 입 층이 0.69 를 더 깎았다.**
⚑ GS 가 부작용 셋 중 **둘은 «없다» 로 닫았다**(카드 재질이 노치 속에 남는 화소 **0개** · 바탕이 검정
밖으로 새는 화소 **0개**) — 새 구멍 층의 대가는 **띠 두께 하나**다.

### §6 게이트 추가 — **[B8] 띠 두께 래칫**(신설 · `verify923` 84/84)

선언 기하로 두 폴리곤 곡선 사이 수직 거리를 재면 최소 **배너 5.75(u≈4.2) · 불릿 4.22(u≈3.9)** 다.
⚠ GS 의 8.08 과 **정의가 다르다**(찍힌 띠는 구멍이 입 쪽을 덮어 더 두껍게 보인다) — 섞어 쓰지 마라.
래칫 **4.0** 은 그 실측 바로 아래다: 과녁(9.93)까지 올리는 것은 7회차 몫이고, 이 항은 **그 전에 조용히
더 깎이는 것**을 막는다.

### §7 7회차 처방 — **띠를 «오프셋» 으로 낸다** (실측으로 방향을 이미 갈랐다)

뿌리는 833 §12 의 규약 자체다 — «세 곡선이 한 표를 서로 다른 (깊이, 길이)로 읽는다» 는 **어깨에서 두
곡선의 수직 거리를 10 보다 작게** 만든다(입을 벌릴수록 심해진다).

- **처방**: **안쪽 곡선(= 실루엣 · ref 로 닫아 둔 그것)을 과녁으로 두고, 바깥·림을 그 «오프셋» 으로 낸다**
  (바깥 = 안쪽 + 10 · 림 = 안쪽 + 22, 법선 방향). 그러면 띠가 어디서나 10 이고 실루엣은 **한 픽셀도 안 움직인다**.
  6회차가 수로 확인했다: 이 구성에서 불릿 띠 두께는 u0~u30 **전부 10.00**, ⓑ(구멍 ⊆ 바깥) 여유 **+1.50**
  (지금 −0.43 보다 오히려 낫다). ⚠ **배너는 입 부근에서 오프셋 곡선이 접히는 자리가 있다**(같은 자로 8.65) —
  꼭짓점 근처를 어떻게 이을지부터 정해라.
- ⛔ **반대 방향은 실측으로 기각했다** — 바깥을 두고 **안쪽**을 10 안으로 오프셋하면 입이 무너진다
  (u1 배너 **−8.34** · 불릿 **−15.97**). 7회차가 이 길로 가지 마라.
- 이관 예고: `verify667` **[A3]**(바깥 스팬 ÷ vmax = 길이)와 `verify833` §12(넷이 한 값 파생)는
  «바깥이 표에서 나온다» 를 전제로 한다 ⇒ **오프셋 파생으로 방향을 뒤집어 이관**해야 한다(333 처방).
- 그 다음이 **틈 +3.0**(위 ①) — 셋째 몫(카드 높이)까지 같이 봐야 한다.

## 7회차 (2026-09-05, sess-1713-6325 루틴 워커 A) — **띠를 «오프셋» 으로** · 제품 `index.html` 4자리

### §1 6회차 처방을 그대로 넣었다 — 바깥·림은 이제 안쪽 곡선의 **법선 오프셋**이다

6회차까지 세 곡선은 **한 표를 서로 다른 (깊이, 길이)로** 읽었다(바깥 = 표 × (d, len/2) ·
안쪽 = 표 × (d−10, (len−20)/2)). 그 «방사 배율» 은 어깨에서 두 곡선의 **수직** 거리를 10 보다
얇게 만든다 — 6회차 채점 GS 가 찍힌 화소로 «곧은변 10.00 ↔ 노치 최소 8.08(ref 9.93)» 을 냈고
선언 기하로는 **배너 5.75 · 불릿 4.22** 까지 내려갔다.

⇒ **안쪽 곡선(= 실루엣 · ref 로 닫아 둔 그것)을 과녁으로 두고 바깥 = 안쪽 + 10 · 림 = 안쪽 + 22**
(둘 다 법선 방향)로 냈다. **안쪽 곡선은 한 줄도 안 건드렸다** — 그래서 [B5]·[B6] 과녁이 전부 그대로다.

⚑ **«접힘» 은 처리할 것이 아니라 안 만드는 것이었다.** 선분마다 밀면 마디에서 마이터/접힘을
따로 다뤄야 하고(1차 시도 실측 — 평균 법선 8.56 · 마이터 9.33), 그 처리가 곧 새 손잡이가 된다.
대신 **원판 합집합의 바깥 경계**로 냈다 — 노치 중심(카드 우변)에서 각도별로
`max(s + √(t² − q))` 를 재면 볼록 마디에는 원호가, 오목 마디에는 이웃이 덮은 자리가 저절로 나온다.
표본 하나하나가 안쪽에서 **정확히 t** 떨어져 있다는 것이 이 방법의 보장이다(접힘 처리 0줄).
⚠ **바닥 평탄부를 표본에 넣어야 한다**(`ntcInnerSamples`) — 안 넣으면 꼭지의 오프셋이 «점 하나의
원» 이 되어 1회차가 편 바닥이 도로 둥글어진다.
⚠ **균등 각도로 성기게 뜨면 안 된다** — 불릿의 27.5° 꺾임에서 원호를 표본 한둘이 가로질러
그 자리만 9.65 로 얇아졌다(실측). 촘촘히(256) 뜬 뒤 **곡률이 있는 자리만 남긴다**(Douglas–Peucker
`NTC_OFF_E = 0.03`) — 점수는 오히려 줄었고(배너 31 · 불릿 38 · 옛 20) 오차는 0.06px 아래다.

### §2 실측 — 띠·림 두께(선언 기하 · 두 폴리곤 곡선 사이 수직 거리)

| | 6회차 최소 | **7회차 최소~최대** | 곧은변 | ref |
|---|---|---|---|---|
| 배너 검정 띠 | 5.75 (u≈4.2) | **9.97 ~ 10.06** | 10 | 9.93 |
| 불릿 검정 띠 | 4.22 (u≈3.9) | **9.97 ~ 10.03** | 10 | 9.93 |
| 배너 림 | 16.20 ~ 27.65(→ 두께 6.2~15.1) | **11.98 ~ 12.04** | 12 | — |
| 불릿 림 | 13.28 ~ 28.93 | **11.97 ~ 12.07** | 12 | — |

**꼭지는 셋 다 그대로다** — 안쪽 31.65 · 바깥 41.65(= d) · 림 53.65(= d+12) ⇒ 833 §12 의
«넷이 «--ntc-d» 한 값에서 파생» 이 **한 항도 안 바뀌고** 초록이다(281/281 무수정).
곁값: 상자 여백 `m` 배너 28 → **9** · 불릿 40 → **16**(림 끝이 표의 배수가 아니라 오프셋이라 낮다 —
노치 중심은 `cy` 가 같이 따라가므로 **화면 자리는 Δ0**). 구멍 여유(B7b) 배너 −0.43 → **−0.12** ·
불릿 1.69 → **1.71**.

### §3 게이트 — 신설 둘 · 이관 셋 (`verify923` 84 → **87/87**)

- **[B8] 래칫이 과녁으로 올라섰다** — «≥ 4.0» → **«9.9 ≤ 두께 ≤ 10.15»**. ⚠ **두 쪽을 다 묻는다**:
  얇아지는 것만 막으면 «오프셋을 그만두고 통째로 두껍게» 도 초록이 된다.
- **[B8b] 신설** — 림 두께도 같은 자로(11.9~12.15). 833 §12 [12-d]«림 두께 12» 를 **꼭지 하나에서
  곡선 전체로** 넓힌 짝 항이다.
- **[R7] 신설(§R5 되돌림 다섯째)** — 바깥·림을 옛 방사 배율로 되돌리면 [B8] 이 빨개진다
  (배너 5.75 · 불릿 4.22 < 9.9). 이 항이 초록이어야 [B8] 이 «이미 참인 것을 굳힌 항» 이 아니다(338 규칙).
- **[B7b] 이관(사본 제거)** — ⓑ 의 «검정 바깥» 을 자가 **옛 배수식 사본**으로 들고 있었다(402 처방).
  제품 함수(`ntcOffset`)에게 그 곡선을 직접 받게 고쳤고, 창도 −0.6 → **−0.2 로 조였다**(래칫 —
  넓은 창을 두면 이번 소득이 조용히 다시 샌다).
- **[A4] 이관** — «f 격자» 를 **바깥** 곡선에서 읽던 것을 **안쪽** 곡선으로 옮겼다. 표를 이고 있는
  것이 안쪽뿐이 됐기 때문이고, 묻는 것(두 형이 같은 격자를 쓰는가)은 그대로다.
- **`verify667` [A3] 이관** — 산수만 바뀌었다: 스팬 = `(len−20)·v(0) + 2·10` 이므로
  `len = (스팬 − 20) ÷ v(0) + 20` (배너 81 · 불릿 114). 옛 식(스팬 ÷ vmax)은 76.9 / 109.2 로
  4~5px 어긋난 채 빨갛다. **`NTC_LEN` 을 건드리면 여전히 빨개진다**(이 항이 지키는 것 · 불변).
- 회귀 무수정: `verify833` **281/281** · `verify667` 92/92 · `verify124` 77/77 · `verify151` 84/84 ·
  `verify153` 53/53 · `verify895` 18/18 · `verify866` 18/18 · **SMOKE PASS**.

### §4 이번 회차가 **안 건드린** 것 (다음 회차가 이어받는다)

- **불릿 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈 +3.0~3.14px** — 6회차 채점 2인(GR·GS)이 각자
  **세 몫으로 분해**했고 **셋째 몫(카드 높이 +1.1~1.3)** 에서 둘이 만났다. 축이 노치가 아니라
  **카드 세로 사다리**라 이번 회차(노치 띠)와 파일 구간이 다르다.
- 배지 노랑 획 굵기(933 으로 등재돼 있다) · 불릿 제목 잉크 폭(⑥ 축).

### §5 채점 — **비평 2인 독립(GT · GU) · ①~④ 최저 «7 / 7» ⇒ 통과선 9 미달, 회차 계속(7/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GT | 9 | **7** | 8 | 9 | 9 | 8 | **7** |
| GU | 9 | **7** | 8 | 9 | 9 | 8 | **7** |

**⚑ 이번 회차가 한 일은 둘 다 확인했다** — 불릿 입구의 얇은 자리 **8.05~8.12 → 9.66~9.78(+1.6)** ·
노치 전 구간 두께 폭 **1.99~2.02 → 0.27~0.50** · **과교정 0**(GT «10.00 을 넘는 곳 0곳» ·
GU «최대 10.34 는 같은 깊이 ref 9.96~10.72 안»). 닫아 둔 축도 안 무너졌다(옆면 25/50/75/90% ±0.75 ·
입 폭 u1~u10 r6 과 0.02~0.04 안 · 깊이 · 바닥 · 개수 · 자리). 새 구멍 층의 대가로 걱정하던 셋
(림이 우변 검정을 덮는가 · 노치 속 카드 재질 · 검정 밖 바탕 샘)은 **둘 다 0 으로 닫았다**.
⚑ GU 가 6회차 GS 의 «8.08» 을 **8.05 로 재현**했다 — 두 회차의 자가 같은 자리에 서 있다는 뜻이다.

#### ⚑⚑ 채점이 연 것 — **«균일 10» 은 배너의 과녁이지 불릿의 과녁이 아니다**(2인 독립 일치)

7회차의 질문(«띠가 노치를 따라 고른가»)에 둘이 같은 답을 냈다: **우리는 고르다. 그런데 ref 불릿이
고르지 않다.** 곧은변에서는 두 형이 같은데(ref 10.17~10.23 ↔ 우리 10.00) **입구 어깨**에서 갈린다:

| 깊이 u | ref 배너 | **r7 배너** | ref 불릿 | **r7 불릿** |
|---|---|---|---|---|
| 곧은변 | 10.21 | 10.00 | 10.23 | 10.00 |
| 2~4 | 9.53 | 9.99 | **12.54 / 13.16** | 9.89 |
| 4~7 | 9.60 | 9.91 | **13.67 / 13.76** | 9.86 |
| 7~10 | 9.96 | 9.89 | **14.44 / 14.24** | 9.83 |
| 10~16 | 9.99 | 9.88 | **13.84 / 14.40** | 9.89 |
| 16~24 | 10.08 | 9.91 | **12.18** | 9.90 |
| 바닥 | 10.05 | 9.80 | **11.09 / 10.19** | 9.86 |

(두 값이 적힌 칸은 GT / GU · 창이 조금 다르다. **배너는 r7 이 맞다**(Δ −0.02~+0.6) ·
**불릿만 −2.7~−4.8px = −22~−33%.**) 신뢰 둘 다 **상** · GT 는 문턱 4단 × 바탕 3단 · GU 는 문턱 3단 ×
백분위 3단 = **아홉 단에서 부호 불변** · 자도 서로 다르다(GT 덮개적분 모서리 + 최근접거리 ·
GU 두 등고선 최소거리). **밝은 림도 같은 자리에서 같은 방향**이다(GU 12.92 ↔ ref 17.24 · GT 는 전 구간
−0.78~−1.00). ⇒ **ref 불릿은 노치가 곧은변에 «마이터» 로 붙어 어깨의 검정이 두껍다** —
7회차가 깐 «어디서나 10» 은 그 형에서는 과녁이 아니다.

⚠ **이것은 첫 보고다**(6회차 GS 는 «최소값» 만 봤고 그 최소는 입에 있었다). 6회차가 «띠가 얇다» 로
연 축 위에 정확히 얹히지만 **답이 한 칸 더 안쪽**이다 — 7회차의 처방(균일 오프셋)은 배너에서 옳고
불릿에서 절반만 옳다.

#### 채점이 낸 나머지 — **8회차 순서**

**① [1순위 · 2인] 불릿 어깨 띠 두께** — 위 표. **8회차는 «상수 10» 을 «깊이의 함수» 로 갈아야 한다**
(형마다 한 벌 · 값은 ref 에서 · `NTC_PROF` 와 같은 규약). ⚠ **자부터 저장소 안에 세워라**(338) —
지금 이 표는 비평가 둘의 자에만 있다. `scan923.py` 에 «띠 두께» 축이 없다.
⚠ 오프셋 뼈대는 그대로 쓸 수 있다 — `ntcOffset(sm, t, n)` 의 `t` 를 **깊이에 따라 바뀌는 값**으로
받게 하면 된다(원판 반지름이 자리마다 다른 합집합 = 여전히 접힘 처리 0줄).
**② [2순위 · 2인 · 열두 번째 재현] 불릿 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈 +2.23~2.90px** —
분해가 **6회차와 또 갈렸다**: GU «알약이 1.49~2.41 위 · 리본1 윗변 제자리»(5회차 GP 쪽) ↔
GT «이웃 네 구간이 전부 ±1.5 안 · 틈 하나만 국소로 벌어졌다 · 리본1 **자체 높이** −1.24».
공통은 **리본1 윗변이 제자리**라는 것이다. **③ [GT · 신뢰 중] 곧은변 띠 −0.21~0.23**(ref 10.21 ↔ 우리 10.00)
— ①과 한 손잡이(둘 다 «두께») · **④ 림 두께 −0.78~1.00**(ref 12.75~12.98 ↔ 우리 11.97~12.00) ·
**⑤ (⑥축) 불릿 제목 잉크 폭 +2.4~2.5%**(advance/자간 축 · 높이는 −0.8%) · **⑥ 불릿 바닥 평탄부**
(GU −4.8~7.1 신뢰 상 ↔ GT −0.75~4.94 신뢰 중 — 여유 사다리에서 크기가 3~5배 흔들린다).
**9:19 ↔ 9:13.3 은 열네 번째로 «차이 없음»**(c3 의 6×6 = 등재된 레드닷).

⚑ **용어 함정 하나** — GT 가 «실루엣 Δ0 은 사실이 아니다(안쪽이 −1.93 물러났다)» 라고 적었는데,
그 «안쪽» 은 **검정↔림 경계**(= 이 저장소가 «바깥 곡선 · 깊이 d» 라 부르는 것)다. 게이트가 재는
실루엣(검정↔바탕 = 깊이 d−10)은 GT 자신의 자로도 **Δ ≤ 0.055px** 이고 [B5]·[B6] 이 그대로 초록이다.
**두 이름이 반대로 붙어 있으니 다음 회차는 좌표를 먼저 맞추고 읽어라.**

## 8회차 1단계 (2026-09-05, sess-1713-6325 루틴 워커 A · 7회차와 한 세션) — **자부터**(채점 없음 · 제품 0줄)

7회차 채점의 1순위(«ref 불릿은 어깨가 두껍다»)를 제품에 넣기 전에 **338 규칙대로 자를 저장소 안에
세웠다** — 그 표는 비평가 둘의 자에만 있었고 `scan923.py` 에는 두께 축이 아예 없었다.

**신설 `scan923.py --band`** — 행마다 검정 띠의 **두 모서리**를 부분화소로 잡고(바깥 = 검정↔바탕 =
기존 `outer_x` 그대로 · 안쪽 = 검정↔밝은 림 = 밝은 고원의 50% 교차) 두 점구름을 만든 뒤
**안쪽 점 → 바깥 점구름 최소 거리**를 두께로 쓴다. 깊이는 그 두께를 낸 **바깥 점**의 깊이로 적는다.
⚑ 가로 폭을 그대로 쓰면 어깨에서 1/cos 만큼 부푼다(45°면 √2) — 최소 거리는 참 두께의 **하한**이라
부호가 뒤집히지 않는다(채점 GT 와 같은 규약). ⚠ 이웃 행을 12행까지 점구름에 넣어야 **얕은 깊이 칸이
안 빈다**(노치 창이 «깊이 ≥ 6» 이라 u 2~6 은 창 밖에만 있다 — 4행이면 0 → 6 을 건너뛴다).

**검정 결과 — 두 자리는 맞고 한 자리가 어긋난다**

| | 곧은변 | 노치 |
|---|---|---|
| 우리(r7) | **10.05 / 10.06** | 9.79~10.06 (전 구간) |
| ref 배너 | 10.31 | 9.4~10.3 = **고르다** |
| ref 불릿 | **11.81** ⚠ | 11.0~16.2 (오염 자리 포함 · 흔들린다) |

우리 값은 선언(10)·채점(GT «정확히 10.000»)과 맞고, **ref 배너가 «고르다» 는 것도 채점 2인과 같은
결론**이다. 그런데 **ref 불릿 곧은변이 11.81** 로 읽힌다 — 채점 2인은 둘 다 **10.17~10.23** 이었다.
⇒ **자가 그 카드에서 +1.5 편향돼 있다.** 유력한 뿌리는 **초록 카드 바깥의 그림자 경사**(실측 row 520:
검정 → 9 → 27 → 42 → 46 → 배경 49 = 4 ref px 램프)이고, `outer_x` 의 |Δ바탕| 문턱이 그 램프에서
바깥으로 밀린다(측정표 §14 «그림자는 바탕 위에만 깔린다» 가 같은 자리를 적어 뒀다).

⇒ **8회차 2단계 순서**: ① 그림자 편향을 걷는다(바깥 모서리를 «검정 질량» 쪽에서 잡거나 램프를
모형에 넣는다 — 배너에는 없고 불릿에만 있는 것이 실마리다) → ② 그때 ref 불릿 곧은변이 10.2 로
내려오면 **그 자가 낸 깊이별 표**를 과녁으로 삼는다 → ③ 제품은 `ntcOffset(sm, t, n)` 의 `t` 를
**깊이의 함수**로 받게 하면 된다(원판 반지름이 자리마다 다른 합집합 = 접힘 처리 여전히 0줄) →
④ [B8] 을 «상수 10» 에서 «형마다 한 벌짜리 두께 표» 로 갈아 끼운다(333 — 자리는 그대로).
⛔ **자를 못 믿는 동안 제품에 표를 넣지 마라**(338).

## 8회차 2단계 (2026-09-05, sess-2013-2534 루틴 워커 A) — **바탕 표본 수리 + 불릿 과녁 재측정** (채점 없음 · 제품 0줄)

### §1 ⚑⚑ 1단계의 가설(«초록 카드 바깥의 그림자 경사»)은 **기각됐다** — 338 규칙이 값을 벌었다

1단계는 «ref 불릿 곧은변 11.81 ↔ 채점 2인 10.17~10.23» 을 남기며 뿌리를 **그림자 램프**(row 520 실측
`검정 → 9 → 27 → 42 → 46 → 배경 49`)로 지목하고 «걷어내라» 를 2단계에 넘겼다. 처방을 넣기 전에
같은 행을 손으로 재 보니 **그 램프는 노치 «안» 의 행에만 있고**(y520 은 노치 3 한복판이다) 곧은변
행(y560)의 경계는 배너와 똑같이 칼같다(`… 242 | 49 | 0 0 0 0 | 43 48 49 …`). 곧은변만 골라 손으로
재면 불릿은 **10.35** 다 — 즉 그림자는 이 편향의 뿌리가 아니다.

**진짜 뿌리는 자의 «바탕 표본» 이다.** `report_ref` 가 바탕을 **카드 한복판 행의 화소 하나**
(`a[(y0+y1)//2, W-3]`)로 잡는데, 초록 카드의 그 행(y502)은 하필 ref 화면의 **밝은 파랑 패널 띠**
(y454~461 · 463~464 · 466~477 · **500~512** · 값 (45,46,66)) 위에 앉는다. 참 바탕은 (39,39,49) 다.
그 한 화소가 카드 **400행 전부**의 바탕이 된다:

* 덮개 적분의 분모 `full = |검정 − bg|₁` 이 **127 → 157** 로 부풀고,
* 참 바탕 화소마저 `|Δbg|₁ = 30` 을 얻어 α ≈ 0.19 씩 **세 화소** 더 쌓인다
  ⇒ 바깥 모서리가 **+0.57 ref px = +1.18 우리px** 바깥으로 밀린다.

⚑ **이 편향은 «띠» 하나가 아니라 그 카드의 모든 축에 같이 걸려 있었다** — 깊이·평탄부·입 폭·곧은변이
전부 같은 부호로 밀렸다. 배너는 그 띠 밖이라 **한 자리도 안 움직인다**(그래서 «불릿만 이상하다» 로
보였다).

### §2 수리 — 바탕을 **행마다** 잰다 (자 두 벌 다)

`tools/scan923.py` `bg_rows()`/`bg_of()` 신설 — 행마다 오른쪽 여백 **다섯 화소의 채널별 중앙값**을
바탕으로 쓴다(중앙값이라 여백 잡티에 안 흔들린다). `profile()`·`band_prof()` 가 그 표를 읽고,
`report_ref`·`report_cap` 이 «한 색» 대신 그 표를 만든다. 게이트 `tools/verify923.js` 도 **같은 절차**
규약대로 `bgRowFn()` 으로 갈았다(우리 캡처는 바탕이 균일해 값은 안 변하지만 함정은 같이 걷는다).

**검산 — 자가 두 카드의 곧은변을 이제 «같은 x» 로 읽는다**

| | 수리 전 | **수리 후** |
|---|---|---|
| ref 배너 곧은변 x | 485.63 | **485.63** |
| ref 불릿 곧은변 x | 486.35 | **485.63** |
| ref 배너 띠 두께(곧은변) | 10.31 | **10.34** |
| ref 불릿 띠 두께(곧은변) | **11.81** ⚠ | **10.32** |

1회차 채점 GK 가 «노치 없는 행만 골라 재면 ref 두 형의 곧은변 차는 **0.01 우리px**» 이라고 적어 둔
바로 그 값이다(그때는 «상위 40% 중앙값» 이 배지를 물어서 생긴 유령으로 닫았고, 최빈값 자로 갈고도
**바탕 쪽 유령이 남아 있었다**). 그리고 불릿 띠 10.32 는 7회차 채점 GT·GU 의 **10.17~10.23** 과
같은 자리다 — 세 자가 만났다.

### §3 이관 — **불릿 과녁 전부**(`verify923` REF·REF_M · 배너는 0건)

| 축 | 옛 과녁 | **새 과녁** | Δ |
|---|---|---|---|
| `bl.flat` 바닥 평탄부 | 24.75 | **26.82** | +2.07 |
| `bl.dep` 깊이 | 32.11 | **31.87** | −0.24 |
| `bl.wd` 깊이별 폭 25/50/75/90% | 92.61 / 82.36 / 63.89 / 42.26 | **92.83 / 82.70 / 64.33 / 42.69** | +0.22~+0.44 |
| `bl.len` 길이 | 97.0 | 97.0 | 0 |
| `REF_M.bl` 입 폭 u1~u6 | 118.27 / 109.67 / 105.46 / 100.77 / 95.79 | **116.93 / 109.62 / 104.30 / 100.15 / 95.91** | −1.34~+0.12 |
| `REF_M.ban` u1 | 72.17 | 72.07 | −0.10(다섯 화소 중앙값 바탕의 잔차) |

⚑ **`bl.flat` 26.82 는 «새로 나타난 수» 가 아니다** — `verify923.js` 머리말이 4회차 이전부터
«불릿 **26.82**» 를 적어 두고 있었다. 4회차가 «문턱 교차 → 덮개 적분» 으로 갈면서 분모가 바탕에
민감해졌고, 그때 24.75 로 내려앉은 것이다. 옛 문턱 자와 새 덮개 자가 **바탕만 바로잡으면 같은 값**을
낸다 = 두 자가 서로를 검산한다.

`VERIFY923` **87/87 PASS**(새 과녁 위에서) · §R 되돌림 일곱 항 전부 초록. 제품 `index.html` **0줄**.

### §4 ⚑ 새 과녁이 연 것 — **불릿 바닥이 실제로 −2.82 짧다**

옛 과녁에서 우리 불릿 바닥은 24.0 vs 24.75 = **Δ−0.75**(사실상 닫힌 자리로 읽혔다). 새 과녁에서는
24.0 vs **26.82 = Δ−2.82** 다(창 ±4.5 안이라 [B1] 은 초록이지만 **가장 큰 잔차**다). 7회차 채점이
② 상대 크기를 7 로 막은 자리와 같은 축이고, 그 «바닥 평탄부» 는 이 작업의 등재문 ① 그 자체다.

### §5 8회차 3단계(= 9회차) 순서 — 1단계가 남긴 ③④ 는 **아직 유효하다. 단 선행 조건이 하나 붙는다**

① **`--band` 의 «얕은 깊이» 표본이 너무 성기다.** 행 스캔이라 경계가 **가로에 가까운 입구**에서
   한 행에 깊이가 몇 px 씩 뛴다 — 불릿의 온전한 자리(노치 4)에서 u4~7 **n=1** · u10~16 **n=2** ·
   u16~24 n=9 인데 바닥(u24~34)만 n=29 다. **표를 제품에 넣기 전에 이 표본을 채워라**
   (세로(열) 스캔을 같은 점구름에 더하는 것이 1순위 — 경계가 가로에 가까운 자리를 열이 촘촘히 문다).
   ⛔ n=1~2 칸을 과녁으로 굳히지 마라(338 — 자를 못 믿으면 처방도 못 믿는다).
② 그 표가 서면 ③ 제품 `ntcOffset(sm, t, n)` 의 `t` 를 **깊이의 함수**로 →
   ④ [B8] 을 «상수 10» 에서 «형마다 한 벌짜리 두께 표» 로(333 · 자리는 그대로).
③ **곁다리로 닫을 수 있는 자리 = §4 의 바닥 −2.82** — 손잡이는 `NTC_PROF.bl` 의 꼬리(`[1, .235]`)
   한 값이고, 건드리면 [B5] 90% 폭·[B6] 이 같이 움직이므로 **한 회차를 그 축에만** 써라.

⚑ **수리 후 지금 읽히는 ref 불릿 띠(참고용 · 위 ① 때문에 아직 과녁 아님)** — 노치 4:
곧은변 10.29 → u4~7 13.76(1) · u10~16 14.10(2) · u16~24 12.22(9) · **바닥 10.19(29)** ·
노치 3: 10.32 → u4~7 11.23(5) · u16~24 12.69(5) · 바닥 11.39(12).
7회차 채점의 «어깨가 두껍다» 는 **부호가 살아남았다**(크기만 12.5~14.4 → 11~14 로 내려왔다).
⚠ 노치 1·2 는 여전히 오염 자리다(배지 · 티켓 톱니 — u4~10 에서 «1.6» 같은 값이 나온다).

## 8회차 3단계 (2026-09-05, sess-2013-2534 루틴 워커 A · 2단계와 한 세션) — **불릿 바닥을 새 과녁에** · 제품 `index.html` **1줄**

2단계가 연 잔차(§4 — 불릿 바닥 24.0 vs **26.82** = Δ−2.82)를 그 회차에서 닫았다.
바로잡은 과녁 위에서 불릿은 **바닥 하나만 빼고 전부 닫혀 있었다** — 그래서 손잡이도 하나다:

| 축 | 우리(수리 전) | ref | Δ |
|---|---|---|---|
| 깊이 D | 31.64 | 31.87 | −0.23 |
| 길이 | 96.0 | 97.0 | −1.0 |
| 깊이별 폭 25/50/75/90% | 92.85 / 82.88 / 64.34 / 42.65 | 92.83 / 82.70 / 64.33 / 42.69 | **≤0.18** |
| 입 폭 u1~u6 | 116.72 / 109.94 / 104.44 / 100.60 / 96.4 | 116.93 / 109.62 / 104.30 / 100.15 / 95.91 | ≤0.45 |
| **바닥 평탄부** | **24.0** | **26.82** | **−2.82** ⚠ |

**처방 — `NTC_PROF.bl` 꼬리 한 값**(`[1, .235]` → **`[1, .265]`**, index.html 37185).
바닥은 표의 f=1 세로 선분이 정하고(반높이 `v(1)·hin`), 바깥·림은 7회차가 깐 **법선 오프셋**이라
그 선분만 늘이면 검정 띠도 림도 따라온다(접힘 처리 0줄 · [B8] 그대로 9.97~10.03).

**결과 — 바닥 24.0 → 26.0**(Δ−2.82 → **−0.82** · 두 자리 y255·y482 **둘 다 26.0**).
곁축은 안 무너졌다: 깊이별 폭 90% 42.65 → 43.06(ref 42.69 · 창 ±2.5) · 75% 64.34 → 64.49 ·
입 폭 u1~u6 Δ≤0.5 · 길이·깊이 불변. **배너는 한 값도 안 건드렸다**(다른 표 행).

⚑ **«한 값» 으로 멈춘 이유** — 27.0 을 노려 꼬리 셋을 같이 들면(`.95 .350 / .98 .288 / 1 .278`)
평탄부가 **y255 26.0 ↔ y482 28.0** 으로 갈린다(표본이 행 격자를 straddle 한다). 잔차 0.82 를 줄이려다
**같은 카드 두 자리의 편차를 0 → 2.0 으로 키우는** 거래라 기각했다 — 채점은 «두 자리가 같은가» 도 본다.
⚠ 이 축의 자는 «깊이 ≥ 최대−0.5 인 행의 최장 런» 이라 **1.0 단위로 양자화**된다. 26.0 과 27.0 사이에
값이 없으므로 이 잔차는 **0.82 가 하한**이고, 더 밀려면 자의 눈금부터 부분화소로 바꿔야 한다(9회차 후보).

**게이트** — `VERIFY923` **87/87 PASS**(§R 되돌림 일곱 항 포함) · 회귀 `verify667` 92/92 ·
`verify833` 281/281 · `verify860` 34/34 · `verify879` 23/23 · `verify895` 18/18 · `verify932` 59/59 ·
`verify151` 84/84 · `verify956` 19/19 · **SMOKE PASS**. 이관 0건.

### 9회차 순서 (8회차가 남긴 것)

1. **[1순위] `--band` 얕은 깊이 표본 채우기 → 어깨 두께 표 → 제품 `t` 를 깊이의 함수로 → [B8] 이관**
   (2단계 §5 그대로 — 세로(열) 스캔을 점구름에 더하는 것이 선행).
2. **[2순위 · 열두 번째 재현] 불릿 «마지막 알약 아랫변 ↔ 리본1 윗변» 틈 +2.23~2.90px**
   (7회차 채점 GT·GU 공통. 분해는 갈렸지만 «리본1 윗변이 제자리» 는 공통).
3. **[3순위] 바닥 평탄부 잔차 0.82** — 위 ⚠ 대로 **자의 양자화가 하한**이다. 자를 부분화소로
   갈지 않는 한 제품을 더 밀지 마라(밀면 두 자리가 갈린다).
4. **[관측] 곧은변 띠 −0.2~0.3 · 림 두께 −0.78~1.00**(7회차 채점 ③④) — ① 과 한 손잡이다.

### §6 채점 — **비평 2인 독립(GV · GW) · ①~④ 최저 «7 / 7» ⇒ 통과선 9 미달, 회차 계속(8/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GV | 9 | **7** | 8 | 9 | 9 | 7 | **7** |
| GW | 9 | **7** | 9 | 9 | 7 | 8 | **7** |

#### ⚑⚑ 이번 회차의 자 수리는 **둘이 각자 재현해 확인했다** (§3-2 질문의 답)

둘 다 **저장소 자를 안 쓰고** 자기 자를 세웠고(GV = 반값 문턱 · 덮개 적분 · 등고선 최근접거리 셋 ·
GW = 덮개 적분 + 등고선 법선 적분), **고장을 각자 재현했다**:

* GW — 바탕을 밝은 값 (45,46,66) 으로 못박으면 어두운 행이 **+3.44 우리px** 부푼다.
  행마다 재면 두 대역의 갈림이 **+0.018** 이고 문턱 12칸 전부에서 불변.
* GV — 같은 고장을 덮개 자로 재현: 어두운 행 10.98 ↔ 밝은 행 10.32 = **+0.66**, 창을 넓히면 **+2.05**.
* 둘 다 **두 카드의 곧은변 바깥 x 가 같다**고 냈다(GW Δ**0.000** ref px · GV Δ+0.018) — 수리 전 0.72 ref px.

⇒ **«수리 방향은 옳다» 가 2인 독립 결론이다.** 1단계의 그림자 가설이 기각된 것도 확정.

⚠ **다만 저장소 자가 아직 +0.2 높다** — ref 곧은변 띠를 GW **10.112** · GV **10.15(반값)/10.06(덮개)** ·
7회차 GT·GU **10.17~10.23** 으로 읽는데 수리된 저장소 자는 **10.32~10.34** 다. 네 자 중 셋이
10.06~10.23 에 모이므로 **저장소 자가 창 오른쪽 꼬리를 한 화소 더 먹고 있다**(GV 진단).
⇒ **9회차 선행 과제로 등재**(아래 순서 0번). 지금 [B8] 이 쓰는 «ref 9.93» 은 그 영향 밖이라 초록이다.

#### 이번 회차가 한 일도 둘 다 확인했다 (§3-1)

* 바닥 평탄부 **정확히 +2.00 행**(GW «22.00 → 24.00» · GV «22 → 24»). **과교정 0** — 둘 다
  «모든 문턱에서 아직 ref 보다 짧다» 고 냈다(GV δ≥0.2 에서 −0.88~−5.59 · GW 문턱 0.25/0.40 에서 −2.00/−3.77).
* 7회차가 닫은 것 **하나도 안 무너졌다**: 띠 두께 고름(GV 9.78~10.12 = ±2.1% · GW 9.79~10.15) ·
  실루엣 폭(깊이 1~28 |Δ|≤1.15) · 림이 카드 우변 검정을 덮지 않음(둘 다 «0 화소») ·
  노치 속 카드 재질 **0**(GW 2880 표본) · 검정 밖 바탕 샘 **0**(GW 세 카드 전수).
* 9:13.3 은 **열다섯 회차 연속 «차이 없음»**(GW 카드 네 변 Δ≤0.09px).

#### ⛔ 브리핑의 사실 오류 하나 — **GW 가 잡았다. 다음 회차가 그대로 믿으면 안 된다**

브리핑 §2 가 «배너·보라 카드는 제품 0줄 · `-r7-c3` 과 `-r8-c3` 은 같아야 한다» 고 적었는데
**보라(c3)도 바뀌었다** — 두 노치 바닥이 c2 와 **똑같이 22.00 → 24.00** 이다(|Δ|₁ 최대 308 ·
y353-371·394-412·580-598·621-639 / x964-986). 보라 카드가 `NTC_PROF.**bl**` 을 읽기 때문이고,
«불릿형» 이라는 이름이 카드 색이 아니라 **프로필 종류**라는 것을 내가 브리핑에서 흘렸다.
⇒ **무변경 대조본은 `-c1`(파랑) 뿐이다.** 다음 브리핑은 이 문장을 반드시 정정해서 써라.

#### ⚑⚑ 채점이 준 것 — **9회차 과녁이 이제 저장소 밖 두 자로 서 있다**(§4-1 질문의 답)

8회차 2단계가 «표본이 성기다» 로 미룬 바로 그 표다. **둘이 독립으로 냈고 깊이 4~24 에서 만난다**:

| 깊이(우리px) | 0 | 2 | 4 | 6 | 8 | 12 | 16 | 24 | 바닥 |
|---|---|---|---|---|---|---|---|---|---|
| **GV** ref 불릿 | 10.15 | 10.82 | 12.74 | 13.73 | 14.15 | **14.44** | 13.24 | 11.54 | 10.14 |
| **GW** ref 불릿(위/아래 평균) | 10.06 | 13.15 | 14.05 | 14.25 | **14.35** | 14.32 | 13.67 | 11.69 | 10.08 |
| 우리 r8 | 9.97 | 9.81 | 10.02 | 9.84 | 10.12 | 9.78 | 9.86 | 9.80 | 9.88 |
| **Δ(우리−ref)** | −0.1 | −1.0 ~ −3.3 | **−2.7 ~ −4.0** | **−3.9 ~ −4.4** | **−4.0 ~ −4.2** | **−4.4 ~ −4.7** | −3.4 ~ −3.9 | −1.7 | −0.2 |

* **깊이 4~24 는 둘이 ≤1.3 안에서 만난다.** 갈리는 곳은 **깊이 2 하나뿐**(GV 10.82 ↔ GW 13.15) —
  입구 램프가 가팔라 «깊이 2» 가 서로 다른 실물 자리를 가리키는 것으로 보인다(5회차 [B6] 이
  «비율 말고 절대 px» 로 간 것과 같은 함정). **9회차는 깊이 4 부터 과녁을 걸고 u≤3 은 관측으로 둬라.**
* **대조군이 둘 다 있다**: ref **배너** 노치는 전 깊이 **9.1~10.6 으로 고르다**(GV·GW 일치) ⇒
  이 부풀림은 JPEG 번짐도 그림자도 아니고 **ref 불릿형 고유의 획**이다.
  GW 는 한 술 더 떠 **우리 캡처를 K 로 축소해 같은 자로 재니 9.66~10.12** 를 돌려받았다(자 대조군 통과).
* 화소로도 닫힌다 — GW «ref y599·y647 어깨의 순수 (0,0,0) 이 가로로 **10 ref px**, 바닥 y622 는 5» ·
  GV «가로 검정런 8화소 ÷ 기울기 1.20 = 13.7».
* 매개변수 흔들기 GW 18칸 · GV 3단에서 **부호 불변**.

### §7 9회차 순서 (8회차 채점이 정한 것)

**0. [선행] 저장소 자의 +0.2 잔차** — ref 곧은변 띠를 저장소 자만 10.32~10.34 로 읽는다(밖의 자 셋은
   10.06~10.23). 창 오른쪽 꼬리를 한 화소 더 먹는 것을 고치고 나서 과녁을 걸어라 — 안 그러면
   어깨 표 전체가 +0.2 로 굳는다.
**1. [1순위 · 2인 · ② 를 7 로 막은 유일한 항목] 어깨 띠 두께를 «깊이의 함수» 로** — 위 표가 과녁이다
   (깊이 4→13.4 · 6→14.0 · 8→14.3 · 12→14.4 · 16→13.5 · 24→11.6 · 곧은변 10.1 · 바닥 10.1 ·
   **u≤3 은 관측**). 처방은 8회차 2단계가 적어 둔 그대로 — `ntcOffset(sm, t, n)` 의 `t` 를 깊이의
   함수로 받게 한다(원판 반지름이 자리마다 다른 합집합 = 접힘 처리 여전히 0줄) → [B8] 을
   «상수 10» 에서 «형마다 한 벌짜리 두께 표» 로(333). ⚠ **배너 표는 건드리지 마라** — ref 배너는 고르다.
**2. [2순위 · 2인] 바닥↔옆면 이음이 각지다** — GW 깊이 29/30/30.5 에서 폭 −2.27/−4.15/−5.05(12칸
   문턱 전부 음수) · GV 폭비 W(1.0)/W(0.1) ref 1.454 ↔ 우리 1.191. 8회차는 바닥 «선분» 만 늘였고
   **이음 반지름은 그대로**다. 손잡이는 `NTC_PROF.bl` 의 .90~.98 구간(꼬리 하나가 아니다).
**3. [3순위 · 열세 번째 재현] 마지막 알약 아랫변 ↔ 리본1 윗변 틈 +2.65~2.94(+5.7~6.4%)** —
   ⚑ **분해가 이번에 닫혔다**: 리본1 윗변은 카드 아랫변 기준 **제자리**(GW −0.38 · GV −0.90),
   **틈은 전부 알약이 위로 뜬 것**(GW +2.29 · 그중 0.43 은 알약 높이 −0.6%). 7회차의 GT/GU 갈림에서
   **GU 쪽**이 맞았다. 축이 노치가 아니라 카드 세로 사다리다.
**4. [4순위] 밝은 림 두께 −0.87~1.03(−6.8~7.9%)** — 1번과 한 손잡이(둘 다 «오프셋 t»).
**5. [닫자] 곧은변 띠** — GV «Δ −0.06~−0.15 이고 frac 0.4 에서 부호가 뒤집힌다 = 측정 한계».
   7회차 ③ 은 이제 잡음 수준이다.
**6. [기록만] ⑥ 제목 잉크 폭 +1.6~2.4%**(둘 다 재현 · 높이 −0.94%) · GV «배지 분홍판 지름 +7%» ↔
   GW «배지 크기 +0.26%» 로 **갈렸다**(원점이 다르다 — 다음 회차가 셋째 자로 가를 것) ·
   GW «ref 초록 카드 테두리 위8.68/아래11.46 비대칭» 은 신뢰도 하(ref 부산물 혐의, 재현부터).


## 9회차 (2026-09-05, sess-2228-6359 루틴 워커 B) — **띠 두께를 «깊이의 함수» 로** + **자 수리 셋** · 제품 `index.html` **3자리**

8회차 채점(GV·GW)이 정한 §7 순서 그대로 **0번(자) → 1번(어깨 띠 두께)** 을 한 회차에 했다.

### §1 0번 선행 과제 — **저장소 자의 +0.2 잔차. 뿌리가 GV 의 짐작과 달랐다(338 규칙)**

8회차 채점이 «ref 곧은변 띠를 저장소 자만 10.32~10.34 로 읽는다(밖의 자 셋은 10.06~10.23)» 를 남기고
GV 가 뿌리를 «덮개 창의 오른쪽 꼬리를 한 화소 더 먹는다» 로 지목했다. **재현해 보니 그 자리도 맞았지만
본체는 다른 것이었다** — 뿌리가 셋이고, 그 중 가장 큰 것은 **한 띠를 두 자로 재고 있었다**는 것이다.

| # | 무엇 | 왜 값이 밀리나 | 실측 |
|---|---|---|---|
| ① | **안쪽 끝이 아직 «50% 교차»** — 바깥 끝은 4회차에 덮개 적분이 됐는데 짝이 안 따라갔다 | 하드 에지에서 두 자의 치우침이 다르다(942) ⇒ 그 차가 통째로 두께에 실린다 | ref 초록 y660: `g = … 241 \| 49 \| 0 0 0 1 \| 43 …` ⇒ 덮개 c(481)=0.797 이라 참 경계 **480.70**, 50% 교차는 **480.63** ⇒ 두께 10.15 대신 **10.28** |
| ② | **덮개 창의 «받침»** — 카드 바로 오른쪽은 그림자가 깔려 far-margin 바탕과 다르다 | α = 26/127 ≈ 0.20 씩을 «부분 덮개» 로 세어 경계를 밖으로 민다 | ref 초록 y400 의 경계 밖 화소 |Δbg|₁ = 19·26·26·22·23 ⇒ 최대 **+0.56 우리px** |
| ③ | **얕은 깊이 칸이 통째로 빔** — 입 근처에서 노치 경계가 곧은변과 거의 나란하다 | 한 행이 깊이를 여러 px 씩 건너뛴다 | ref 불릿 y298: u2~4 **표본 0** · u7~10 **표본 0** |

처방도 셋이다 — ① 안쪽 끝도 **덮개 적분**(`inner_x`) · ② 받침 = 경계 밖 [i+3, i+9) 의 |Δbg|₁ 중앙값을
빼고 α = (d−ped)/(full−ped) · ③ `band_prof` 가 **열(세로) 스캔**을 점구름에 더한다(한 열은
«몸통 → 검정 → 바탕 → 검정 → 몸통» 이라 위·아래 어깨가 각각 «재료 → 바탕» 한 번씩 — 아래는 뒤집어서
같은 함수에 먹인다). 되돌림은 **`--ruler8`** 한 스위치이고 `verify923` **§R8** 이 매 실행 확인한다.

| | 8회차 자 | **9회차 자** | 채점 2인(GV·GW) |
|---|---|---|---|
| ref 배너 곧은변 띠 | 10.34 | **10.15** | 10.06~10.23 |
| ref 불릿 곧은변 띠 | 10.32 | **10.18** | 10.06~10.23 |
| **우리** 곧은변 띠 | 10.00 | **10.00**(한 값도 안 변한다) | GT «정확히 10.000» |

⚑ **우리 렌더는 왜 안 변하나** — 경계 밖이 참 바탕이라 받침이 0 이고, 경계 화소가 한 장뿐이라
두 자(50% 교차 ↔ 덮개)가 같은 값을 준다. **이 수리는 «ref 를 어떻게 읽었는가» 의 정정**이다.

### §2 그 자가 8회차 채점의 표를 **독립으로 재현했다**

열 스캔으로 얕은 칸이 차자 저장소 자가 ref 불릿(y298 — 오염 안 된 자리)에서 이렇게 읽는다:

| 깊이칸(우리px) | u-2~2 | u2~4 | u4~7 | u7~10 | u10~16 | u16~24 | u24~34 |
|---|---|---|---|---|---|---|---|
| **ref 불릿** | 10.15 | 13.29 | 13.77 | 14.18 | 14.03 | 12.14 | 10.64 |
| 8회차 채점 표(GV/GW 평균) | ~10.1 | 12.0~13.6 | 13.4~14.0 | 14.2 | 14.0~14.4 | 12.5 | ~10.9 |
| **ref 배너**(대조군) | 10.14 | n/a | 9.77 | 9.60 | 9.53 | 9.89 | 9.84 |

⇒ **세 자가 같은 그림**이다: 곧은변 10 → 어깨(깊이 6~14) 14.3 근처 → 바닥 10. 배너는 **고르다**.

### §3 처방 — `ntcOffset()` 의 반지름을 «수 하나» 에서 «표» 로 (제품 3자리)

7회차가 «바깥·림 = 안쪽 곡선의 법선 오프셋(원판 합집합의 바깥 경계)» 으로 깔아 둔 구조 덕에
**손잡이가 이미 그 자리에 있었다** — 원판 반지름 `t` 를 표본의 **깊이에서 읽으면** 된다.

```
NTC_BAND.bl  = [[0, 10], [3, 10], [6, 12.6], [9, 14.0], [12, 14.4], [16, 13.5], [24, 11.4], [31.6, 10]]
NTC_BAND.ban = [[0, 10]]                       // 배너는 ref 가 고르다 — 마디 하나 = 상수
```

제품 3자리 — ⓐ `NTC_BAND` + `ntcT()` 신설 · ⓑ `ntcOffset(sm, t, n, dt)` 이 표를 받고 `dt` 로 림을 얹는다
(`ntcOffset(sm, bt, N)` = 검정 · `ntcOffset(sm, bt, N, 12)` = 림) · ⓒ 캐시 열쇠에 표를 넣는다(§R9 사보타주 대비).

⚠ **두 끝은 정확히 10 이다** — ref 는 10.15/10.64 로 읽히지만 그 0.1~0.6 은 자의 해상도 안이고,
10 을 지키면 833 §12 «꼭지에서 셋이 d−10 · d · d+12» 와 7회차 «곧은변 띠 10» 이 **항등식으로** 남는다.
⚠ **안쪽 곡선(실루엣)은 한 점도 안 움직였다** — [B1] 바닥 · [B5] 깊이별 폭 · [B6] 입 폭 과녁 전부 불변.
⚠ 표를 «자가 읽는 칸» 그대로 베끼면 안 된다 — 자의 깊이는 **바깥 점**의 것이라 원판 반지름만큼 깊어진다.
표는 캡처를 자로 재며 세 번 맞췄다(1차: 어깨는 맞고 입이 +2.75 두꺼움 → 램프를 3px 깊게 → 닫힘).

**결과(같은 자·같은 칸 · `-r9-c2` y255)**

| 깊이칸 | u-2~2 | u2~4 | u7~10 | u10~16 | u16~24 | u24~34 | 곧은변 |
|---|---|---|---|---|---|---|---|
| 8회차 | 9.85 | 9.95 | 9.92 | 9.85 | 9.93 | 9.88 | 10.00 |
| **9회차** | 11.01 | 13.87 | 14.14 | 14.19 | 12.36 | 10.39 | **10.00** |
| ref | 10.15 | 13.29 | 14.18 | 14.03 | 12.14 | 10.64 | 10.18 |
| **Δ(9회차−ref)** | +0.86 | +0.58 | −0.04 | +0.16 | +0.22 | −0.25 | −0.18 |

**Δ −2.7~−4.7 → +0.86 이하**(u-2~2 는 8회차 채점이 갈린 **관측 칸**이다 — GV 10.82 ↔ GW 13.15 이고
우리 11.01 이 그 사이에 있다).

### §4 게이트 — 이관 하나 · 신설 셋 (`verify923` 87 → **94/94**)

* **[B8] 이관(333)** — «어디서나 10» → **«ref 깊이칸 표»**(`REF_B` 신설 · 창 ±0.9). 값을 노드로 옮겨
  적지 않고 **자를 그대로 부른다**(`py(['tools/scan923.py', '--cap', …, '--band', '--json'])` — 사본 금지 402).
  ⚑ 선언 기하로 재면 «바깥 점의 깊이» 가 원판 반지름만큼 깊어져 자의 칸과 **한 칸 반이 밀린다**
  (같은 그림을 선언 기하로 재면 봉우리가 u16~24 · 자로 재면 u10~16) — 그래서 **자와 같은 자리에서 묻는다.**
* **[B8a] 신설(선언 기하 짝)** — 두 끝은 정확히 10 · 불릿은 어깨비 max/min ≥ 1.35 · 배너는 반대로 고르다.
* **§R8 신설(자 되돌림)** — `--ruler8` 사본이 ref 곧은변을 10.3 대로 되돌리는지 매 실행 확인한다.
  자를 고친 회차가 그 수리에 게이트를 안 남기면 «자 수리» 는 말만 남는다.
* **§R9 신설(표 되돌림)** — `NTC_BAND.bl = [[0,10]]` 사본을 **같은 자로** 다시 재면 어깨 칸이
  창 밖(|Δref| **4.3**)이다 ⇒ 이 이관이 «삭제» 가 아니라는 증거(338 규칙).
* **과녁 이관** — 자가 바뀌었으니 `REF`·`REF_M` 도 고친 자로 다시 읽었다. 움직인 것은 사실상
  **배너 바닥 하나**(y136 자리 18.57 → 16.50 ⇒ 두 자리가 둘 다 16.50 이라 평균 17.54 → **16.50**);
  나머지는 Δ≤0.15(ban dep 31.79→31.86 · bl dep 31.87→31.85 · wd·mouth Δ≤0.15). **우리 값은 불변.**
* **남의 장부 한 줄** — `probe932.js` LEDGER 의 `scan923.py` 행(sig `t23 → t26` · 정수 걸음 줄 번호
  L143·L477 → **L146·L555** · 9회차 수리 기록). `verify956` [1-b]·[4-d] 가 그것을 묻는다
  («자를 고치면 여기가 먼저 빨개진다» — 그 항이 실제로 먼저 빨개졌다).

**게이트 결과** — `VERIFY923` **94/94 PASS** · 회귀 `verify667` 92/92 · `verify833` 281/281 ·
`verify860` 34/34 · `verify879` 23/23 · `verify895` 21/21 · `verify932` **62/62**(장부 갱신 뒤) ·
`verify151` 84/84 · `verify956` **19/19**(장부 갱신 뒤) · **SMOKE PASS**.

### §5 채점 — **비평 2인 독립(GX · GY) · ①~④ 최저 «8 / 9» ⇒ 통과선 9 미달, 회차 계속(9/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GX | 10 | **8** | 9 | 10 | 10 | 10 | **8** |
| GY | 9 | 9 | 9 | 9 | 8 | 8 | **9** |

#### 둘이 일치한 것 — **어깨는 실제로 두꺼워졌고 과교정은 0 이다**

* **부호가 뒤집힌 자리 0** — 둘 다 «깊이 7~24 는 ref 와 사실상 일치»(GX Δ −0.01~−0.51 · GY Δ **−0.03~−0.08**),
  **어느 깊이에서도 ref 를 넘지 않는다**(GX «넘어선 자리가 없다» · GY «7개 깊이 전부 r9 ≤ ref»).
  8회차 대비 상승폭은 GX +1.49~**+4.52** · GY +3.4~+4.5.
* **8회차까지 닫은 것이 하나도 안 무너졌다** — GX «실루엣 baseline_inner 108.0000 = 108.0000 · 최대깊이
  31.4603 = 31.4603(소수 넷째까지 동일)» · GY «rim/body 경계 Δ **0.000**». 노치 속 카드 재질 0 ·
  검정 밖 바탕 샘 0 · 림이 카드 우변 검정을 안 덮는다 — **둘 다 0건**.
  GY 는 한 술 더 떠 **r9 ↔ r8 전체 화소 차분**으로 «|Δ|₁>60 인 자리가 x970~1006 · y323~677(노치 두 곳)
  **뿐**» 임을 찍었다 ⇒ 알약·리본·입 폭은 **바이트 단위로 그대로**다(§4 세 자리는 8회차 값 그대로 승계).
* **9:13.3 은 열일곱 회차 연속 «차이 없음»** — GY «|Δ|₁>60 인 화소 **0개**» · GX «깊이 전 항목 소수 둘째까지 동일».

#### ⚑⚑ 자 수리 셋을 둘이 각자 재현했다 (§1 질문의 답)

* **① 두 자 갈림** — GX «50% 교차 4.9166 ↔ 덮개 4.8856 = **0.031 ref px(0.06 우리px)** · 9회차가 든
  480.63 ↔ 480.70 과 자릿수까지 맞는다» · GY «덮개가 문턱 교차보다 **0.43~0.65 ref px 작다**»
  (GY 는 «바깥 끝» 에서 쟀다 — 두 사람이 서로 다른 끝을 재서 크기가 다르고, **갈린다는 사실은 일치**).
* **② 받침** — **둘 다 같은 화소를 읽었다**: ref 초록 y400 경계 밖 |Δ바탕|₁ = **19·26·26·22·23**
  (GX «브리핑 수치와 정확히 일치» · GY «행 의존적 받침 실재 — 단 y560 은 0~13 이라 모든 행이 그렇진 않다»).
* **③ 열 스캔** — GX 가 «y601→602 에서 깊이가 **+4.05 우리px 점프**» 를 찍어 «행만으로는 얕은 칸이 빈다» 를
  확인했다. GY 는 «우리 렌더는 행당 0.3~0.5px 라 안 빈다 · **ref 쪽**(JPEG · 배지에 가린 첫 노치)이 문제이고
  열 스캔이 그 자리를 살릴 것» 으로 **범위를 좁혔다** — 두 사람이 같은 결론의 서로 다른 반쪽을 냈다.

#### ⛔ 남은 결함은 하나 — **입 근처(깊이 2~6)가 아직 얇다.** 다만 **두 자가 크기에서 갈린다**

| 깊이(우리px) | 2 | 4 | 6 | 8 | 12 | 16 | 24 |
|---|---|---|---|---|---|---|---|
| **GX** Δ(우리−ref) | −2.89 | −1.32 | **−1.01** | −0.28 | −0.51 | −0.01 | −0.39 |
| **GY** Δ(우리−ref) | −0.87 | −0.73 | −0.41 | −0.08 | −0.04 | −0.32 | −0.03 |
| 저장소 자 Δ(칸 중앙값) | +0.58(u2~4) | — | −0.04(u7~10) | | +0.16(u10~16) | | +0.22(u16~24) |

⚑ **세 자가 깊이 8~24 에서는 0.5px 안에 모이는데 깊이 2~6 에서는 2px 갈린다** — 8회차 채점이
«u≤3 은 관측 칸»(GV 10.82 ↔ GW 13.15)이라 적어 둔 바로 그 구간이고, 이번에도 같은 자리가 갈렸다.
**저장소 자는 이 구간을 오히려 +0.58 «두껍다» 고 읽는다** — 기준점이 서로 다르기 때문이다:
GY 가 그 뿌리를 못박았다 — «**깊이를 안쪽 점 기준으로 바꾸면 깊이 6 에서 우리가 ref 보다 +0.3~0.4
과다하게 나온다 = 부호가 뒤집힌다**». GX 도 같은 것을 반대편에서 봤다(«안쪽 점 기준으로 재면 피크가
깊이 12→16 으로 밀리고 GV·GW 표와 전혀 안 맞았다»).

#### ⚑ GX 가 연 «림 두께» 는 GY 가 그 자리에서 닫았다

GX 가 «어깨에서 림이 10~11 로 12 에 못 미치는 신호(정밀도 낮음 · 미해결 플래그)» 를 남겼는데,
GY 가 **법선 자**로 같은 자리를 재서 **12.1~12.2**(곧은변 12.0 대비 ≤2%)를 냈고 «가로 자로 나이브하게
재면 14.7 까지 부푼다 — 질문 1 의 왜곡과 같은 뿌리» 라고 원인까지 적었다. **선언 기하도 12(±0.06)** 다
(`verify923` [B8b]). ⇒ 열린 항목이 아니다.

### §6 10회차 순서 (9회차 채점이 정한 것 — **마지막 회차다**)

**0. [선행 · 이 회차의 본체] «깊이를 어디서 재는가» 를 셋째 자로 정하라.** 남은 결함이 하나뿐인데
   그 크기가 자마다 −2.89 ↔ −0.41 ↔ **+0.58** 로 갈리고, **기준점을 바꾸면 부호까지 뒤집힌다**
   (GY 실측). 지금 상태에서 표를 올리면 저장소 자 쪽에서는 **과교정**이 된다 — 934 규약대로
   **셋째 자가 기준을 정하기 전에는 제품을 건드리지 마라.**
   자를 세우는 법은 두 사람이 이미 적어 뒀다: 두께는 **법선(등고선 최근접거리)** 으로 재고(가로 자는
   어깨에서 최대 2배 부푼다 — GX y339 raw 17.6 ↔ 법선 9.9 · GY y328~336 raw 19.5~21.9 ↔ 법선 12~14),
   **깊이는 그 두께를 낸 두 점 중 어느 쪽으로 적을지**를 ref 와 우리에 **똑같이** 적용해 밝힌다.
**1. [1순위] 입 근처(깊이 2~6) 잔차** — 0번이 기준을 정한 뒤, 그 자의 값으로 `NTC_BAND.bl` 의 머리
   두 마디(`[3, 10]`·`[6, 12.6]`)만 올린다. ⚠ 8회차 채점이 «u≤3 은 관측» 이라고 못박아 뒀으므로
   **깊이 4~6 을 과녁으로 걸고 u≤3 은 따라오게** 두는 것이 옳다(`REF_B` 의 u-2~2 도 여전히 null 이다).
**2. [2순위 · 열네 번째 재현] 마지막 알약 아랫변 ↔ 리본1 윗변 틈 +2.65~2.94** — 축이 노치가 아니라
   **카드 세로 사다리**다(8회차가 분해를 닫았고 9회차는 이 자리를 **한 화소도 안 건드렸다** — GY 의
   전체 차분이 그것을 증명한다).
**3. [3순위] 바닥↔옆면 이음이 각지다** — `NTC_PROF.bl` 의 .90~.98 구간(8회차 §7-2 그대로).
**4. [기록만]** ⑤⑥ 축(GY 8/8 · GX 10/10)은 통과를 막지 않는다.

---

## 10회차 (2026-09-06, sess-2343-7049 루틴 워커 C) — **셋째 자로 이름표를 정했다** + **바닥↔옆면 이음** · 제품 `index.html` **2값**

9회차 채점(GX·GY)이 정한 §6 순서 그대로 **0번(셋째 자) → 1번(입 잔차) → 3번(이음)** 을 돌았다.
⚠ **이 라운드의 마지막 회차다**(10/10).

### §1 0번 — «깊이를 어디서 재는가» 를 **되찾기 시험**으로 정했다 (`tools/probe923.js` 신설)

9회차가 남긴 물음은 «남은 결함 하나의 크기가 자마다 GX −2.89 · GY −0.41 · 저장소 자 **+0.58** 로
갈리고 **기준점을 바꾸면 부호까지 뒤집힌다**» 였다. 자를 하나 더 세워 «셋 중 누가 맞나» 를
**다수결로** 정하는 길은 8·9회차가 이미 두 번 걸어 실패한 길이다(네 자가 매번 같은 구간에서 갈렸다).

⚑ **그래서 자를 겨루게 하지 않고 «참값이 있는 그림» 을 찾았다 — 우리 렌더다.**
제품의 손잡이 `NTC_BAND[k]` 는 `ntcOffset()` 이 **안쪽(실루엣) 표본의 깊이 px** 로 원판 반지름을
읽는 표이므로, **우리 카드의 «깊이 ↔ 띠 두께» 참값은 선언에 적혀 있다.** ref 로는 이 시험을 못 한다
(참값을 모르는 그림이다). ⇒ 같은 캡처를 세 이름표로 재서 **선언을 되찾는 이름표**가 옳은 자다.

`scan923.py` 에 `--uat outer|inner|mid` 를 냈다(**기본값 `outer` = 9회차와 바이트 단위로 같은 그림** —
`verify923` 94/94 가 그대로 통과한 것이 그 증거다). 한 두께 표본은 두 점을 잇고 그 둘의 깊이는
띠 두께만큼(10~14 우리px) 다르다 — 어느 쪽으로 적느냐가 표를 통째로 **한 칸 반** 민다.

| 이름표 | 배너(대조군 · 선언 = 어디서나 10) | **불릿 매끈 구간(u≥7) 최대 \|Δ그려짐\|** |
|---|---|---|
| **outer** | 0.15 | **0.30** |
| mid | 0.15 | 1.22 |
| inner | 0.15 | 1.96 |

⇒ **`outer` 다.** 배너가 대조군인 이유가 그 자체로 증거다 — 선언이 **상수**라 **어떤 이름표로 다시
붙여도 상수**이고, 실제로 셋이 0.15 로 같다. **갈림은 «자의 정밀도» 가 아니라 «이름표» 였다.**

#### ⚑⚑ 그리고 같은 자가 **1번(입 잔차)을 닫았다** — 입 칸에는 «두께» 값이 하나가 아니다

같은 두 곡선을 **반대 방향**으로 재면(자 = 림 쪽 점 → 실루엣 · 기하 = 실루엣 → 바깥 폴리라인)
매끈한 구간에서는 **0.30 안에서 만나는데 입 칸(u<4)에서는 2.69 갈린다** — 기하를 **정확히 아는**
우리 카드에서조차 그렇다. 뿌리는 입에서 실루엣이 곧은변으로 접히며 한 실루엣 점이 **여러 깊이의
원판이 만든 바깥 호**를 통째로 떠맡는 것이고, 이것이 8·9회차에서 자 넷이 **깊이 2~6 에서만** 갈린
**구조적** 이유다(정밀도가 아니다).

⛔ **그래서 §6 의 1번(«`NTC_BAND.bl` 머리 두 마디를 올린다»)은 하지 않았다 — 제품 0줄이 정답이다.**
근거가 셋이고 **셋 다 같은 방향**이다:
1. **이미 선언보다 두껍게 그려진다** — 입 칸 u2~4 는 선언 10.00 인데 기하 **11.16** · 화소 **13.86**
   (표의 머리가 3→6 에서 10→12.6 = 기울기 **0.87/px** 라 «두께가 천천히 변한다» 는 합집합 전제 밖이다).
2. **ref 대비 우리가 이미 두껍다** — 저장소 자로 u-2~2 **+0.85** · u2~4 **+0.57**(ours 11.00/13.86 ↔
   ref 10.15/13.29). 표를 올리면 **과교정**이다(9회차 §6 가 미리 경고한 그대로다).
3. **ref 쪽 그 칸의 표본이 2개뿐이다** — `--ref --band` 로 네 노치를 다 보면 오염 안 된 자리의
   u2~4·u4~7·u7~10 이 각각 **n=2** 이고, 오염된 세 자리는 36.83·15.71·11.15 로 제각각이다.
⇒ `REF_B` 의 u-2~2 가 이미 `null`(관측)인 것처럼, **입 칸 전체가 과녁을 걸 수 없는 자리**다.

`probe923` **6/6 신설** — [2] 대조군 · [3] 이름표 판정 · [4] 갈림이 실재(배수 ≥3.0) · [5] 입이
선언보다 두껍다 · [6] 두 방향이 매끈에서 만나고 입에서 갈린다.

### §2 3번 — **바닥↔옆면 이음**(8·9회차 채점 2인의 2순위) · 제품 2값

[B5] 격자는 .25/.50/.75/**.90** 에서 끝난다 ⇒ **이음이 사는 .90~1.00 이 자에 없었다**
(5회차가 «입» 을 [B6] 으로 따로 세운 것과 같은 꼴 — 격자가 없으면 결함도 없다).
`wprofile` 을 .80~.98 격자로 돌려 **저장소 자가 채점 2인을 독립으로 재현**했다:

| frac | .80 | .86 | **.90** | **.94** | **.96** | **.98** |
|---|---|---|---|---|---|---|
| ref 불릿 | 57.40 | 50.22 | 42.76 | 37.35 | 33.15 | 29.48 |
| 수리 전 Δ(우리−ref) | +1.10 | −0.63 | **+0.02** | **−2.90** | **−3.18** | **−3.20** |
| **수리 후 Δ** | +1.13 | −0.62 | +0.13 | **−0.40** | **−0.19** | **−1.06** |
| 배너 Δ(대조군) | −0.28 | −0.14 | +0.37 | +0.56 | +0.61 | +0.74 |

⚑ **결손은 불릿 한 형뿐이다** — 배너는 같은 격자에서 +0.74 안이라 **한 값도 안 건드렸다.**
⇒ `NTC_PROF.bl` 꼬리 **두 마디만**: `.95` .348 → **.380** · `.98` .270 → **.304**.
값은 자로 역산했다 — 폭 ≈ 2·v·hin_eff 이고 실측 hin_eff = 42.77/(2×.455) = **47.0** 이라 Δv = Δ폭/94.

⚠ **f ≤ .90 과 f = 1 은 한 값도 안 건드렸다** — .90 은 이미 Δ+0.02 로 맞았고, `v(1)` 은
`ntcInnerSamples` 가 바닥 **평탄부** 선분의 반길이로 읽는 값이라(1회차가 세운 축) 건드리면
[B1] 이 무너진다. 여는 것은 **이음(.90~.98)뿐**이다.

### §3 게이트 — `verify923` 94 → **121/121** (신설 [B9] 24항 + §R10 3항)

- **[B9-80~98]** — 위 격자·창 ±1.6(수리 후 실측 최대 1.13 · 수리 전 −3.20 은 그 두 배 밖).
  자를 새로 만들지 않았다 — 같은 `wAt` 에 **세 번째 격자**를 먹였다([B5] 비율 · [B6] 절대 · [B9] 바닥).
- **§R10 되돌림 일곱째** — 꼬리 두 마디만 9회차 값으로 되돌린다. 앞의 여섯은 모양을 통째로 갈거나
  머리를 누르거나 띠 표를 지우므로 **이번 수리를 못 되돌린다**. 결과: 불릿 [B9] |Δ| **3.12**(창 밖) ·
  **[R10b] [B5] 는 전부 초록**(0.23 — [B9] 가 [B5] 로 대체되지 않는다) ·
  **[R10c] 배너 [B9] 도 초록**(0.90 — 두 형이 표를 안 나눠 쓴다 = «불릿만 고쳤다» 의 증거).

### §4 장부 — `probe932` scan923 행 갱신 (956 처방 그대로)

신호가 `t26 → t30` 인데 **축은 하나도 안 늘었다** — 늘어난 넷은 `--uat` 의 이름표 산술뿐이고
정수 걸음은 여전히 둘(`i2` 불변)이라 판정 **S 유지**. 956 이 막은 «`sig` 만 베껴 넣기» 를 피해
판정문을 다시 적었고, 굳어 있던 줄 번호도 **L146·L555 → L158·L571** 로 옮겼다
(`verify956` [4-d] 가 그것을 묻는다).

### §5 회귀

`verify923` **121/121** · `probe923` **6/6 신설** · `verify833` **281/281 무수정** ·
`verify667` **92/92 무수정** · `verify932` **65/65** · `verify956` **19/19** · `verify942` **95/95** ·
`verify886` 12/12 · `verify887` 7/7 · `verify933` 22/22 · `verify934` 18/18 · **SMOKE PASS**.

### §6 채점 — **비평 2인 독립(GZ · HA) · ①~④ 최저 «9 / 9» ⇒ 통과선 9 충족 · 통과(10/10)**

| 축 | ① 배치 | ② 상대 크기 | ③ 여백·간격 | ④ 정렬 | (⑤) | (⑥) | 점수 |
|---|---|---|---|---|---|---|---|
| GZ | 9 | 9 | 9 | 9 | 10 | 9 | **9** |
| HA | 9 | 9 | 9 | 10 | 9 | 9 | **9** |

둘 다 저장소 자를 안 쓰고 **자기 자를 세웠다**(GZ = 행 역산 + 열 적분 두 벌 · HA = 국소 1차 바탕 피팅 +
법선 최근접거리). 카드 우변 원점은 셋이 만난다 — 우리 **978.000**(둘 다 «AA 0 · 정확값») ·
ref GZ 486.087 ↔ HA 486.098(Δ0.011 ref px).

#### 둘이 일치한 것 — **이음은 닫혔고 과교정은 0이다**

| f | .80 | .86 | .90 | **.94** | **.96** | **.98** |
|---|---|---|---|---|---|---|
| GZ Δ(r10−ref) | +1.32 | −1.26 | +0.03 | **−0.46** | **−0.35** | **−0.99** |
| HA Δ(r10−ref) | +0.57 | −0.45 | −0.61 | **−0.66** | **−0.84** | **−0.89** |
| 저장소 자 Δ | +1.13 | −0.62 | +0.13 | −0.40 | −0.19 | −1.06 |
| GZ 가 잰 수리량(r10−r9) | — | — | — | **+2.41** | **+3.02** | **+2.27** |
| HA 가 잰 수리량(r10−r9) | — | — | — | **+2.38** | **+3.32** | **+2.28** |

* **세 자가 .94~.98 에서 0.16~0.5 안에서 만난다**(GZ «저장소 자와 0.16 안에서 일치»).
  수리량은 둘이 각자 **+2.3~3.3** 으로 재 브리핑 주장과 맞는다.
* **과교정 0 — 둘 다 명시적으로 답했다.** GZ «ref 를 넘어선 자리는 f=.80 하나뿐인데
  **10회차가 한 화소도 안 건드렸고 r9 와 값이 같다**» · HA «넘어선 자리 **없다**.
  f=.80 의 +0.57 은 문턱 5단(kblk .15~.35)에서 **+0.55 → −0.52 로 부호가 뒤집힌다 = 측정 한계**».
* **바닥도 같이 좋아졌다**(HA · 곁축) — 평탄부 Δ **−2.43 → −0.95**.
* **8·9회차까지 닫은 것이 하나도 안 무너졌다** — 노치 속 카드 재질 **0px** · 림 침범 **0** ·
  깊이 D Δ−0.13~−0.16 · 입 폭 · 띠 두께(어깨 13.87 · 곧은변 9.97~10.00) 전부 유지.
* **9:13.3 은 열아홉 회차 연속 «차이 없음»** — 배경/그림자 반경 2px 를 뺀 뒤 GZ c1 **0px** ·
  HA c1·c3 **0px**(c2 140px 은 전부 아트 가장자리 ±1 LSB).

#### ⛔ 브리핑의 사실 오류 하나 — **GZ 가 잡았다(HA 도 같은 화소를 봤다)**

§3-2 를 «배너 c1 화소 차분이 **0** 이어야 한다» 로 적었는데 **거짓이다** — 실제로 **4,371 px 가 다르다.**
다만 전부 **|Δ| ≤ 5/255** 이고 **|Δ| ≥ 14 인 화소는 0개**이며, 다른 것은 **노치 구멍으로 비치는
배경 밝기뿐**이다(r9 lum 48–49 → r10 52–53 — 두 캡처의 배경 애니 프레임이 다르다).
**물어야 했던 것은 «기하가 안 움직였는가» 였고 그 답은 예다** — GZ «세 노치 max|Δ깊이|
**0.133/0.090/0.100** 우리px» · HA «폭 .80~.98 **≤0.15px** 차 = 같은 프레임 안 노치–노치 산포와 같은 크기».
⚑ **다음에 «화소 차분 0» 을 물을 때는 배경이 도는 화면인지 먼저 보라** — 이 카드 뒤에서는
전투 캔버스가 계속 돈다(캡처 때 `#view` 를 숨기지만 카드 뒤 패널 명암은 프레임마다 다르다).

#### ⚑⚑ 10회차 §2 의 «방향 의존» 을 **HA 가 자기 자로 재현했다**

HA «입 칸(깊이 2~7) — 바깥→안쪽 **11.21 / 12.70** ↔ **반대 방향** 안쪽→바깥 **13.85**(같은 바깥깊이 2–4)».
⇒ `probe923` [6]([2.69 갈림])이 저장소 밖 자에서도 선다. **그 칸에 과녁을 걸지 않은 것이 옳았다.**

#### 남은 것 하나 — **축이 노치가 아니라 «카드 세로 사다리» 다** (둘 다 1순위로 지목)

| | GZ | HA |
|---|---|---|
| 알약↔리본1 틈 | **+2.65** (+5.7%) | **+2.87** (+6.2%) |
| 마지막 알약 아랫변 ← 카드 바닥 | **+1.62** | **+1.85** |
| 리본1 윗변 ← 카드 바닥 | **−1.07** | **−1.03** |
| 알약 높이 / 피치 / 사이 틈 | −0.20 / −0.51 / −0.31 | −0.20 / −0.51 / −0.31 |

⚑ **분해가 두 자에서 소수 첫째까지 같다** — 8·9회차(GV·GW)의 «리본은 (거의) 제자리 · 알약 더미가
위로 떴다» 가 **세 번째·네 번째 자로 확정됐다**. 알약 **크기·피치는 맞다**(±0.5) ⇒ 손잡이는
**«알약 더미의 세로 앵커» 한 곳**이다.

⚑⚑ **HA 가 «다섯 회차 연속 분해가 뒤집힌» 이유까지 잡았다** — 리본 윗변의 «아랫평탄» 창을 검정 4px 을
넘어 **빨강까지 2행만 침범**시키면 ref 값이 3.0 ref px 움직여 Δ가 **−3.40 으로 부호가 뒤집힌다.**
⇒ 이 자리를 잡는 세션은 **창이 검정 안에서 끝나는지부터** 확인해라.
⚠ 둘 다 **원점을 카드 «바닥» 으로 잡아야 한다**고 못박았다 — **ref 는 알약 4개, 우리는 3개**라
윗변 기준으로는 «마지막» 이 같은 서수가 아니어서 분해가 뒤집힌다(GZ·HA 독립 일치).

⇒ **966 으로 등재한다**(노치가 아니라 카드 세로 사다리 — 923 의 주제 밖이다).

### §7 마감 — **통과(10/10 · ①~④ 최저 9/9)**

885 가 10회를 소진하고 남긴 셋 중 **① 노치 바닥 평탄부**(1회차)와 **③ 이음·띠·입**(3~10회차)이 닫혔고,
**② 알약↔리본1 틈**은 축이 노치가 아니라 카드 세로 사다리임이 확정돼 **966** 으로 넘긴다.
