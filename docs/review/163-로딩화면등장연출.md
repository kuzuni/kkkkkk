# 163 — 첫 접속 로딩 화면 «플레이어 등장» 연출

> 주인 지시(2026-08-27): «처음 접속할 때 스프라이트 로딩 화면에 애니메이션이 좀 있으면 좋겠음 —
> 플레이어 나오는 애니메이션 느낌으로». T2 기능(연출).
> **레퍼런스 이미지 없음**(신설 화면) → 지시서 [3]-(다) «연출 작업»: 연속 프레임 6~8장을
> 독립 비평가 2명에게 주고 ①타이밍 ②크기 비율 ③위치·경로 ④가독성·일관성 을 채점, 최저 8 통과.

- 구현 구간: `index.html` 맨 앞 **«163 로딩 화면» script**(`atlas-data.js` 뒤 · 본 스크립트 앞) · CSS `#loading`·`#ldStage`·`#ldGr`·`#ldSh`·`#ldHero`·`#ldTx`·`#ldBar`·`#ldNum` · 본 스크립트의 `loadAtlases`(→ `ldReady`)·부팅 콜백(→ `ldFinish`)
- 하네스: `tools/cap163.js`(연속 프레임) · `tools/probe163.js`(시간축 실측) · `tools/scan163.py`(잉크 bbox)
- 게이트: `node tools/verify163.js`

---

## 연출 (무엇을 만들었나)

1. 페이지가 뜨자마자 — 제목 «스프라이트 불러오는 중...» 이 1.4s 주기로 숨 쉬고, 진행바에
   1.15s 주기 하이라이트가 훑고 지나간다(아직 그릴 스프라이트가 없는 구간).
2. `knight` 아틀라스(25KB, 제일 작아 먼저 온다)가 도착하면 — **캐릭터가 왼쪽 프레임 밖에서
   달려 들어온다.** 420ms · easeOutCubic · 이동 450px · 달리기 8프레임 × 52ms(스트라이드 1회 완결) ·
   달리는 동안 상하 아치 26px · 그림자는 공중에서 작고 옅어진다.
3. 도착 — 착지 스쿼시(몸통 ±6% · 그림자 ±30%, 160ms) 후 `idle` 8fps(전투와 같은 속도)로 대기.
   그동안 진행바가 «n/8» 로 찬다.
4. 아틀라스가 전부 오면 — 게임이 부팅되고 오버레이는 110ms 페이드로 빠진다.

## 설계에서 «시간» 이 제약이 된 이유 (이 작업의 본체)

이 화면은 «오래 보여 줄수록 좋은» 연출이 아니다. 두 개의 벽 사이에 있다.

| 벽 | 값 | 근거 |
|---|---|---|
| 캐릭터를 그릴 수 있게 되는 시각 | **≈380~450ms** | 본 인라인 스크립트가 **672KB** 라 파싱에만 ~350ms. 그동안 메인 스레드가 막혀 이미지 `onload` 도 못 돈다 |
| 오버레이가 **안 보여야** 하는 시각 | **≈850ms** | load 이벤트 **550ms**(아틀라스까지 다 받은 시점) + 저장소 게이트의 최단 대기 **300ms** |

1회차에는 연출 모듈이 본 스크립트 **안에** 있어서 캐릭터가 400ms 에야 나타났고, 아틀라스가 다 오는
450ms 까지 **50ms** 밖에 못 썼다 — 비평가 A·B 가 독립적으로 «보이는 달리기가 38px / 145ms»,
«인지 가능한 창이 155ms» 로 같은 자리를 짚었다.

**2회차에 모듈을 `atlas-data.js` 바로 뒤로 옮겼다.** 아틀라스 로딩도 거기서 시작하고(본 스크립트는
`ldReady()` 로 결과만 넘겨받는다), 부팅 시각은 그대로다. 그래도 파싱 벽 때문에 **캐릭터 등장 시작은
여전히 ~400ms** 다 — 자바스크립트는 파싱 중에 못 돈다. 그래서 남은 것은 «퇴장을 언제 하느냐» 뿐이다.

→ `LD_HARD = 620ms`: **«이 시각을 넘겨서까지 등장을 기다리지는 않는다»**.
- **전부 캐시된 재방문**(로컬 file:// 포함): 부팅이 ~420ms 라 등장이 ~200ms 지점에서 잘린다.
  그때 캐릭터는 이미 이동의 **94%** 를 지나 사실상 서 있다(easeOutCubic 이라 뒤가 평평하다).
- **주인 지시가 가리키는 첫 접속**(1.8MB 를 네트워크로 받는 경우): 부팅이 훨씬 늦으므로
  등장 420ms + 착지 + 대기 idle 이 **통째로** 보인다. 비평 표본(`cap163.js`)이 이 경우를 찍는다.

게임 부팅 시각은 **1회차·2회차 모두 옛날 그대로**다. 오버레이는 부팅 순간 `.thru`
(pointer-events:none)가 되어 남은 체류 동안에도 아래 UI 로 탭이 통과한다.

---

## 1회차 (2026-08-27, sess-0139-24146 워커 B)

캡처 `docs/shots/163-r1-1..8.png` · 배율 5 · 등장 300ms · 시작 −560px.

**비평가 A — ① 3 / ② 5 / ③ 5 / ④ 4 → 최저 3**
**비평가 B — ① 4 / ② 3 / ③ 4 / ④ 4 → 최저 3**

두 사람이 **독립적으로 같은 자리**를 짚은 것이 4건이었다(그래서 전부 반영했다):

| 지적 | A 실측 | B 실측 | 2회차 처방 |
|---|---|---|---|
| 캐릭터가 진행바보다 작다(위계 역전) | 잉크 222px, 바 520px = 2.34배 | 잉크 223px, 바 = 2.39배 | 배율 **5 → 8**(잉크 ≈352×360) · 바 **520 → 300** |
| 걸음이 한 번도 안 끝난다 | run 8프레임 중 5장(53.6%) | 4장 재생 | 프레임 간격 **70 → 52ms** × 8 = 416 ≈ 등장 420ms → **스트라이드 1회 완결** |
| 프레임당 이동이 몸통보다 크다(순간이동) | 첫 스텝 308px = 몸통의 126% | 307.7px = 141% | 이동 **560 → 450px** · 등장 **300 → 420ms** → 첫 스텝 148px = 몸통 352px 의 **42%** |
| 그림자가 안 보인다 | 실측 133×19px, 발 스팬보다 31px 좁다 | 명도비 **1.045:1** | ★ **검정을 버렸다** — 어두운 무대에서 접지를 읽히게 하는 건 그늘이 아니라 **발밑에 고인 빛**. 폭 320(발 스팬 ≈262 보다 넓다) |

A 단독 지적(전부 반영):
- **`#ldHero{transition:opacity .12s}` 가 등장의 78%(439px)를 반투명으로 먹었다** — 완전 불투명이
  되는 순간 캐릭터는 이미 이동의 93% 지점. → **페이드인 제거**(등장이 프레임 밖에서 시작하므로 필요 없다).
- `LD_X0 −560` 은 «잉크가 완전히 프레임 밖» 이라는 주석과 달리 **잉크 우변이 x=+100** — 몸의 39%가
  화면 안에서 켜졌다. → 정지 자세 잉크 우변(432)에서 역산해 **−450**(우변 −18).
  (2회차 중간에 −760 으로 과하게 밀었더니 이번엔 앞 **72ms(17%)** 가 빈 무대였다. 게이트 §4 가
  아틀라스에서 우변을 다시 계산해 대조한다.)
- 경로가 완전 수평(세로 변위 0px) → **상하 아치 26px**.
- 착지 스쿼시가 `sh` 에만 걸려 **몸통 스쿼시 0px** → 몸통에도(±6%).
- 바닥선이 헤어라인 4px → **8px**.

B 단독 지적(반영):
- 바닥선이 경로의 **43%** 를 안 덮는다(잉크 x221..857 뿐인데 캐릭터는 −20→540 을 지난다)
  → **프레임 가로 전체**로.
- 진행바가 **745ms(표본 창의 83%)** 동안 한 픽셀도 안 움직여 «걸린» 것으로 읽힌다
  → 채움과 별개로 **훑고 지나가는 하이라이트**(1.15s 주기)를 얹었다. 받는 양을 모르는 구간에서
  «살아 있다» 를 알리는 건 채움이 아니라 이 스윕이다.
- 화면의 81%가 빈 검정 · 8번 게임 화면과 평균 휘도가 **6.4배** 점프
  → 무대 높이 400 → 600, 글자 28 → 36 / 22 → 26, 배경을 목적지 쪽으로 아주 옅게 데운
  radial-gradient(#181528 → #0b0d18).

## 2회차 (2026-08-27, 같은 세션)

캡처 `docs/shots/163-r2-1..8.png`(등장 +20/90/160/235/320/440/900ms · 전환) ·
게이트 **VERIFY163 45/45 PASS** · **SMOKE PASS**.

DOM transform 실측: −448,0 → −126,−23 → −56,−26 → −14,−22 → 0,−8 → 0,0 scale(1.032,.968) → 0,0.
스캐너(캐릭터+그림자 합산 bbox) 중심x: 260 → 423 → 505 → 503 → 527 → **540**(프레임 중앙) → 533.

**비평가 C — ① 6 / ② 6 / ③ 5 / ④ 4 → 최저 4**
**비평가 D — ① 5 / ② 7 / ③ 5 / ④ 6 → 최저 5**

1회차 지적은 대부분 해소됐다(C: «캐릭터 22.5%, 진행바 대비 위계 정상», «발밑 빛 1.045:1 → 3.22:1»).
그런데 두 사람이 **또 독립적으로 같은 자리**를 짚었고, 그것이 이번 회차의 본체였다.

| 지적 | C 실측 | D 실측 | 3회차 처방 |
|---|---|---|---|
| ★ 캔버스가 **비정수 확대**돼 있다 | 가로 소스픽셀 12px 인데 세로는 13,13,13,13,12 — **1/5 행만 높이가 달라 6.7% 세로로 늘어난다** | 잉크 bbox 540×508(이론 최대 408×400 초과) | `<canvas width=480 height=320>` 인데 CSS 는 720×512 였다 — **backing ↔ CSS 1.5×/1.6× 불일치.** backing 을 CSS 와 **1:1(720×624)** 로 맞추고 배율 8→**12** |
| ★ 스프라이트 위가 **잘린다** | 백버퍼가 40 소스픽셀뿐 → run 프레임의 위 **20%(칼끝·투구 깃)** 가 y=761 에서 평평하게 끊긴다 | — | 위와 같은 수정으로 잘림 **0**(게이트가 아틀라스에서 «발밑 앵커 기준 필요 높이 ≤ 캔버스 높이» 를 유도해 단언) |
| ★ 뒤쪽이 **트레드밀** | 마지막 156ms(길이의 37%)에 25px(이동의 5.5%)만 간다 | 속도가 250ms 안에 **28.6배** 붕괴 · «다리는 도는데 몸이 안 나간다» | easeOutCubic **폐기** → **등속(시간 75%) + 2차 감속** |
| 시작점이 프레임 밖이 아니다 | 첫 프레임에 몸의 **89%** 가 이미 화면 안 | 65.6% 가 화면 안 | `LD_X0` −450 → **−850**(잉크 우변 828 에서 역산) |
| 아치가 걸음과 안 맞는다 | 정점이 **딛는 프레임과 겹친다** | 접지 **0회**(등장 내내 공중) | 아치 주기를 **접지 간격(4프레임=160ms)** 에 묶어 딛는 순간 arc=0, 감속 구간은 arc=0 |
| 스트라이드 1회 | — | «주기로 인식하려면 최소 2회» | 40ms × 8 × **2회** = 640ms |
| 착지 스쿼시가 안 보인다 | — | 실측 **−1.8%**(사양 −6%) | 진폭 **±10%** |
| 그림자에 무게가 없다 | 발밑 빛 3.22:1 로 **개선됨** | 바닥보다 **+37% 밝다** — 접지가 «발광» | 어두운 코어 + 밝은 테(빛만으로는 무게가 안 생긴다) |
| 진행바 | 스윕(정보 없음)만 움직이고 채움은 810ms 정지 — **스윕이 채움으로 오독**된다 | 트랙 대비 **1.10:1** 로 «얼마나 남았나» 가 안 읽힌다 | 트랙 대비 **3:1** 이상 · 폭 300→400 · 스윕을 **균일 사선 줄무늬**(위치 정보 없음)로 |
| 8번 인계 | 휘도 3.5배 점프(1회차 6.4배에서 개선) | 15.8배 · 페이드 110ms 로는 부족 | 페이드 110 → **150ms** |

### ★ 하네스가 두 사람을 나란히 틀리게 했다

C·D 브리핑에 실은 «잉크 bbox» 는 `tools/scan163.py` 가 낸 값인데, 3회차에 배경을 radial-gradient 로
바꾸면서 그 스캐너의 **단색 배경 가정이 깨져** 배경까지 잉크로 세고 있었다(bbox 540px — 이론 최대
408px 을 넘는 값이었다). D 는 그 값을 그대로 받아 «히어로 22.3%» 로 채점했고, C 는 픽셀을 다시 재서
«비정수 확대» 라는 **진짜 원인**에 도달했다.
→ 스캐너를 **배경 플레이트 차분**으로 바꿨다(`cap163.js` 가 «캐릭터 도착 전» 한 장을 같이 남긴다).
   LESSONS 122 «브리핑이 틀리면 두 사람이 나란히 틀린다» 의 네 번째 표본이다.

## 3회차 (2026-08-27, 같은 세션)

캡처 `docs/shots/163-r3-bg.png` + `163-r3-1..8.png` · 게이트 **VERIFY163 50/50 PASS** · **SMOKE PASS**.

배경 플레이트 차분 실측(캐릭터+그림자, 잉크 우변이 궤적의 지표):
x1 = 72 → 212 → 380 → 548 → 734 → 810 → **814**(정지). 정지 잉크 526×504(= idle 프레임 44×42 소스px × 12).

**회귀 확인** — `verify113` 83/83 PASS · `verify106` **52/52 PASS**(같은 커밋 전 베이스라인은 51/52 로,
오히려 베이스라인 쪽이 흔들렸다) · `verify78` 20/20 PASS · `smoke` PASS.
`verify125`(22/24) · `verify119`(35/36) 는 **163 이전 베이스라인에서 같은 값으로 실패**한다 —
내 구간이 아니다(각각 145·138 이 등재해 둔 자리). 판정은 추론이 아니라 163 직전 커밋을 따로 받아
같은 게이트를 돌려서 했다(LESSONS 154-④).

**비평가 E — ① 5 / ② 8 / ③ 4 / ④ 6 → 최저 4**
**비평가 F — ① 7 / ② 9 / ③ 7 / ④ 5 → 최저 5**

2회차 지적은 전부 해소됐다. **② 는 두 사람 다 8·9** — «backing 720×624 = CSS 1:1 · 배율 정수 12 균등 ·
잉크 612×600 · **잘림 0**(F: 측정 잉크폭이 44/46/49 × 12 로 정확히 떨어진다)». 이징도 설계대로였다
(E: 850px 이동에서 예측과 최대 오차 **3px = 0.35%**, 이음매 속도 연속 121.4 vs 121.55px).

남은 감점의 **1순위가 두 사람 다 같은 두 자리**였다.

| 지적 | E 실측 | F 실측 | 4회차 처방 |
|---|---|---|---|
| ★ **발이 미끄러진다** | 접지발은 프레임당 **−7.67 src px** 로 그려져 있는데(f0 34.5 → f3 11.5) 40ms 간격에서는 몸이 요구치의 **66%** 만 나간다 → 프레임당 31px, 등속 480ms 누적 **282px = 등장의 33%** | 보폭 243px = 키의 51% (실제 달리기는 90~110%) → «다리가 지면 속도의 2배로 돈다» | 간격을 **등속 속도에서 역산**: `LD_RUNMS = round(LD_FOOT · LD_SC / 등속속도)` = **61ms**(E 권고 60 · F 권고 64). 게이트가 매 실행 다시 계산해 ±5% 로 대조 |
| ★ **그림자가 발에서 어긋난다** | 발 스팬 중심 483.5 vs 그림자 중심 540 → **56px**, 왼발 **120px** 이 그림자 밖 · 폭도 발 스팬의 69% | 중심 **55px** 어긋남 · 왼발 **88px** 밖 | `#ldSh` 폭 420→**520**, `margin-left` −210→**−316**(중심 484). 원인은 `drawHeroTo` 의 `c0` 가 «칼·망토 포함 잉크» 중심을 맞추기 때문 — 발 스팬 중심이 아니다 |

E 단독:
- **아치 위상이 한 프레임 어긋났다.** 아틀라스 바닥행 실측 f0 발끝 2px · f1 발바닥 9px · f2 밀어내기 4px ·
  **f3 = 0px(체공)** — 4프레임 중 **3프레임이 접지**인데 `|sin(π t/4프레임)|` 은 정점을 **f2**(아직 닿아
  있는 프레임)에 얹었다. 발이 바닥선 2px 이내인 시간이 주기의 **3.2%** 뿐이었다.
  → **접지 3프레임은 arc=0 으로 붙이고 체공 1프레임에만** 아치를 준다(`LD_AIR = .75`).
- 착지 스쿼시 회복의 47%가 페이드 뒤에서 일어난다 → `LD_SQ` 170 → **120**.
- 트랙이 배경 대비 **1.655:1**(처방은 3:1) → 트랙을 중간 명도로.

F 단독:
- 그림자 어두운 코어가 플레이트 대비 **1.10:1**, 밝은 테 **1.70:1** → 테 알파 .30 → **.55**.
- 채움이 트랙 대비 좌단 **1.84:1**.
  ★ **E 와 F 의 요구가 반대 방향이었다** — E «트랙이 배경 대비 3:1» vs F «채움이 트랙 대비 3:1».
  기존 색(어두운 트랙 + 중간 파랑 채움)으로는 동시에 못 만족한다(채움:트랙이 1.3:1 로 붙는다).
  → **3단 명도 사다리**로 풀었다: 배경(어둡다) → 트랙 `#5f6a9c` → 채움 `#a9c6ff→#e6efff`(거의 흰 파랑).
- «진행 표기가 541ms 동안 1px 도 안 움직인다» → **하네스 결함이었다.** `cap163.js` 가 knight 를 뺀
  전부를 **같은 값으로** 늦춰서 7장 내내 «1/8» 이 얼어 있었다. 실제 네트워크는 파일이 하나씩 온다 —
  실제 크기 비율에 맞춰 도착을 어긋나게(`STAGGER`) 바꿨다. (LESSONS 122 «브리핑이 틀리면 두 사람이
  나란히 틀린다» 의 다섯 번째 표본 — 이번엔 브리핑이 아니라 **표본 자체**가 틀렸다.)
- 남은 구조적 한계 2건(기록만): 잉크 612px 가 이동 850px 의 72%라 «온전한 실루엣 + 달리는 구간» 이
  겹치는 시간이 62ms(9.7%)뿐이다 · 8번 인계에서 캐릭터가 6~7배 축소되고 평균 휘도가 3.5~5배 뛴다
  (아트·전환 설계 영역).

## 4회차 (2026-08-27, 같은 세션)

게이트 **VERIFY163 59/59 PASS**. 바꾼 것 — 위 표의 처방 전부 + 하네스 `STAGGER`.
`LD_RUNMS` 는 이제 **역산값**이다(요구 60.6ms · 실제 61ms · 오차 0.6%).
캡처 `docs/shots/163-r4-bg.png` + `163-r4-1..8.png`.

**비평가 G — ① 7 / ② 8 / ③ 8 / ④ 8 → 최저 7**
**비평가 H — ① 5 / ② 8 / ③ 7 / ④ 5 → 최저 5**

★ **②③④ 가 처음으로 통과선에 닿았다.** 3회차 지적이 수치로 해소된 것을 두 사람이 확인했다:
- 발 미끄럼 등속 구간 **282px(33%) → 4.3px(0.51%)** (G) · 접지 비율이 아틀라스의 3/4 와 일치(76.2%)
- 그림자 중심 **55~56px 어긋남 → 1~2px**, 발 스팬 287..683 이 타원 안에 좌우 61~63px 여유로 들어감
- 진행바 **트랙:배경 1.655 → 3.94:1 · 채움:트랙 1.84 → 3.37:1**(3단 사다리 성립)
- 잘림 0 · backing = CSS 1:1 · 위계(캐릭터 > 진행바) 유지

## 5회차 (2026-08-27, 같은 세션) — 4회차 지적 반영

**① 케이던스를 «시간» 에서 «이동 거리» 로.** G 의 1순위였고 H 도 같은 자리를 짚었다 —
`floor(t / LD_RUNMS)` 는 등속 구간만 맞고 **감속 구간에서 그대로 트레드밀**이 된다
(G 실측 미끄럼 f8 23.6% → f9 62.0% → f10 90.6%, 합 120px = 등장의 14.1%).
→ `run[floor((|X0| − |x|) / LD_STEP)]`, `LD_STEP = 접지발 이동 × 배율 = 92.04px`.
**케이던스가 몸속도를 자동으로 따라간다 — 멈추면 다리도 멈춘다.**
재방문의 압축 경로(등속의 5배 속도)도 같은 식으로 저절로 맞는다(시간 기준이었다면 프레임당 미끄럼 439%).
아치도 같은 «거리» 자로 재서 «딛는 프레임에서 arc=0» 이 유지된다.
H 실측 기준 등장 전체 미끄럼 **188.7px(22.2%) → 68.7px(8.1%)**.

**④ 그림자의 «코어» 를 발 스팬보다 넓게.** H 실측 — 어두운 코어가 **180×20px = 발 스팬의 45%** 이고
그나마 발 사이에 있어서 **발이 딛는 자리 100%가 밝은 테 위**였다. 가시 풋프린트의 **81%가 바닥보다 밝다**.
★ 그리고 이 바탕에서는 **순수 검정도 1.13:1** 이 상한이라 «더 어둡게» 로는 회수가 안 된다.
→ 대비를 «바닥 대 그림자» 가 아니라 **«코어 대 테»** 로 재정의하고 코어를 418px(≥ 발 스팬 396px)로 키웠다.
게이트 §12 가 그래디언트의 어두운 구간 반경을 직접 재서 발 스팬을 덮는지 단언한다.

**④ 진행바 전이 .12s → .05s.** H 실측 — 아틀라스가 몰려 도착하면 바가 숫자를 못 따라가
«4/8 인데 39.75%»(−10.3%p) 로 어긋난다. 숫자가 진실이므로 바를 따라붙게 했다.

게이트 **VERIFY163 62/62 PASS** · **SMOKE PASS**.

### 5회 소진 — 남은 문제 (다음 라운드 인계)

지시서 [3]-(다)의 **회차 상한 5회를 다 썼고 두 비평가 동시 8 은 못 찍었다**(최고 G 7 · H 5).
남은 것은 셋이고, 셋 다 «무엇을 해야 하는지» 까지 나와 있다.

1. **① 프레임별 접지 거리(균일 `LD_STEP` 의 한계).** H 실측 — 접지발은 f0→f1 이 **126px**,
   f1→f2 가 **90px**(1.40:1)을 요구하는데 균일 `LD_STEP` 은 92.04px 한 값이라
   f0→f1 에서 **−26.5%** 가 남는다. 처방: **접지 간격별 누적 거리표**
   (f0→f1 126 · f1→f2 90 · f4→f5 126 · f5→f6 84 · 체공 4칸에 310 분배)로 프레임을 뽑는다.
2. **③ 발 축이 다른 요소 축과 −54px 어긋난다.** `drawHeroTo` 의 `c0` 는 «칼·망토 포함 잉크» 중심을
   프레임 중앙에 놓는데, 그러면 **발 스팬 중심은 486**, 바닥선·진행바·문구 축은 **539.5** 다.
   잉크를 중앙에 두면(질량 균형, G 의 판단) 발이 −54px, 발을 중앙에 두면 잉크가 +54px —
   **어느 쪽을 축으로 삼을지는 설계 판단**이라 남긴다.
3. **④ 8번(게임 화면)과의 이음 — «아트/전환 설계 대기».** 캐릭터가 선형 **1/6**(면적 1/36)로 줄고
   발 기준선이 **−243px(프레임의 10.7%)** 뛰며 평균 휘도가 **4.25배** 오른다. 130ms 페이드 하나로는
   회수 불가 — 컷을 넘는 스케일·위치 트윈이나 카메라 정합이 필요하다(로딩 화면만의 문제가 아니다).
   같은 계열: 잉크 612px 가 이동 850px 의 72%라 **온전한 실루엣이 등장의 65% 지점에야** 나온다.

---

## 6회차 (2026-08-27, sess-2023-13086 워커 F) — **2차 폴리시 라운드 시작**

5회차가 «5회 소진» 으로 남긴 인계 3건 중 **①·②를 처리**했다(③ 은 아트·전환 설계 영역이라 그대로 둔다).
캡처 `docs/shots/163-r6-bg.png` + `163-r6-1..8.png` · 게이트 **VERIFY163 77/77 PASS ×3 연속** ·
**PROBE163B 12/12 PASS**(신설) · **SMOKE PASS** · 회귀 `verify79` 17/17 · `verify80` 26/26 ·
`verify243` 40/40 · `verify113` 83/83 · `verify78` 20/20.

### ① 케이던스 — «한 값» 을 **표**로 (인계 1번)

`tools/probe163b.py` 를 새로 만들어 아틀라스에서 **프레임별 접지발**을 직접 쟀다(바닥 2px 밴드 잉크의 x 중심):

| | f0 | f1 | f2 | f3 | f4 | f5 | f6 | f7 |
|---|---|---|---|---|---|---|---|---|
| 접지발 x (src px) | 35.33 | 25.04 | 17.79 | 11.50 | 35.33 | 25.04 | 18.00 | 11.50 |
| 요구 이동 ×12 (px) | **123.5** | 87.1 | 75.4 | (체공 95.3) | **123.5** | 84.5 | 78.0 | (체공 95.3) |

**최대/최소 1.64:1.** 5회차까지의 균일 자(92.04px 한 값)는 f0→f1 에서 요구의 −25% 가 남았다 —
H 가 «126 vs 90 = 1.40:1 인데 자는 한 값» 으로 짚은 그 자리다(H 는 화면에서, 이 표는 아틀라스에서 쟀는데
비율이 1.40 vs 1.64 로 다른 것은 H 가 **화면 표본 두 장의 차이**로 잰 반면 여기는 원본 프레임을 직접 잰 차이다).

구현은 표를 **거리 누적**으로 바꿔 프레임을 뽑는다(`LD_STEPS`/`LD_CUM`/`ldFrameAt`). 파생 관계도 뒤집혔다 —
`LD_FOOT`(7.943)·`LD_STEP`(95.32)·`LD_RUNMS`(59)·`LD_AIR`(.75)가 **전부 표에서 나온다**. 손으로 적은 상수는
표 자체(`LD_FEET`)뿐이고, 그 표는 `probe163b.py --gate` 가 아틀라스 픽셀과 대조한다.

**아치도 같이 옮겼다.** 5회차까지는 «주기의 뒤 25%» 라는 **비율**이었는데, 칸 길이가 프레임마다 다르면
그 비율은 체공 칸과 안 겹친다. 이제 아치는 §2b 의 **체공 프레임(f3·f7)에서만** 뜬다 — 접지 프레임은 arc≡0.

### ② 축 — 발을 프레임 중앙에 (인계 2번, «설계 판단» 항목)

같은 도구가 idle f0 에서 **잉크 중심 26.5 vs 발 스팬 중심 22.0**(src px) → 배율 12 에서 **−54px** 을 확인했다
(5회차 인계문의 −54 와 정확히 일치). 두 선택지 중 **발을 축으로** 삼았다:
바닥선·그림자·진행바·문구가 전부 540 을 쓰는 화면에서 축이 둘이면 안 되고, 이 장면의 축은 «서는 자리» 다.
- `#ldHero{margin-left:-306px}`(= −360 + `LD_FDX` 54) · `transform-origin:42.5% 100%`(발밑 — 50% 면 스쿼시가 발을 5px 흔든다)
- `#ldSh` 는 편심(−436)을 버리고 다시 중앙 정렬(−380). **그림자 중심 = 발 축 = 540.**
- `LD_X0` −850 → **−904**(캔버스가 오른쪽으로 간 만큼 시작점도. 잉크 우변 여백 −22 는 불변)
- 그리기(`ldDraw`)는 한 줄도 안 건드렸다 — 게이트 §10(«게임 `drawHeroTo` 와 drawImage 인자·변환행렬이 동일»)이 계속 초록이다.

### ★ 하네스가 세 회차째 같은 병을 옮기고 있었다 — 이번에 끊었다

6회차 첫 캡처 로그가 증거다. 요청 30·150·280·410·540·660ms → **실제 437·452·564·899·1046·1010ms**.
이 러너에서 `page.screenshot()` 한 장이 300~400ms 를 먹어서, 표본 8장 중 **뒤 넉 장이 전부 «이미 서 있는» 같은 그림**이었다.
그 8장으로 ① 타이밍을 채점하면 비평가는 연출이 아니라 **러너 지연**을 채점한다(3·4회차의 «하네스가 두 사람을
나란히 틀리게 했다» 의 세 번째 판).
→ 제품에 `LD.hold()`(rAF 재예약만 끊는다)·`LD.paint(t)`(그 시각의 화면을 **제품 코드가** 그린다)를 내고,
하네스는 그것을 부른 뒤 시간 압박 없이 찍는다. **하네스가 이징·프레임 선택을 다시 구현하지 않는 것**이 요점이다 —
다시 구현하는 순간 브리핑이 «제품이 아니라 하네스의 주장» 이 된다. 결과: 표본 시각이 30·120·200·300·440·580·700ms **정확**.
표본 위상도 케이던스 표에 맞춰 골라 **체공 프레임 두 장(+200·+440)** 이 들어갔다 — 5회차까지의 등속 간격은
체공을 한 장도 안 잡아서 **아치를 채점할 수 없는 표본**이었다.
«전환» 한 장은 실시간으로 잡을 수 없어(페이드 130ms < 캡처 300ms) **합성**으로 만든다 — 게임이 부팅된 뒤
오버레이를 불투명도 .5 로 되살린다. 브리핑에 «이 한 장은 합성» 이라고 적어서 준다.

### 곁가지 — 게이트의 «뜨고 지는 FAIL» 2건을 끊었다 (index.html 1곳 + 자 2곳)

1. **§11 «페이드가 끝날 때 캐릭터가 중앙에 도착»** — 고치기 전 트리에서도 2회 중 1회 빨갰다(x −139).
   원인을 재 보니 **제품 결함이 하나 섞여 있었다**: 재방문 경로에서 부팅이 메인 스레드를 잡으면
   압축 구간에 rAF 가 **한 번도 안 돌아** 캐릭터가 달리던 자세로 −251px 에 멈춘 채 화면이 녹는다.
   → `ldEnd` 가 끄기 전에 **도착 상태를 한 번 그린다**(마지막으로 남는 그림이 «서 있는 캐릭터» 가 된다).
   판정도 «예정(경과 ≥ 압축된 길이)» 과 «결국 도착(최종 transform)» 둘로 갈랐다 — 그리기 지연은 러너 상태이지 설계가 아니다.
2. **§3 «페이드가 display:none 보다 먼저»** — 관찰기 콜백 한 번에 `out`·`off` 가 둘 다 보이면 두 마크가
   **같은 시각**을 받아 `>` 가 거짓이 된다. 순서는 마크가 쌓인 **차례**로, 시각은 «늦지 않다» 로 본다.

### 6회차 채점 — **비평가 I ① 6 / ② 5 / ③ 6 / ④ 5 → 최저 5 · 비평가 J ① 4 / ② 5 / ③ 5 / ④ 4 → 최저 4**

★ 두 사람 다 «구현이 사양을 어겼나» 를 먼저 검증했고 **둘 다 «사양대로다» 를 픽셀로 확인**했다
(I: 표본 1~6 예측과 오차 0.00 · J: 잉크 171,072px 평균 절대차 0.00). 그래서 이번 감점은
«못 지켰다» 가 아니라 **«그 사양 자체가 화면에서 안 통한다»** 다. 6회차 변경 두 건은 둘 다 인정받았다 —
축 통일(J «발 중심 540.0 · 스쿼시 원점 42.5% 라 눌려도 발이 안 흔들린다»), 접지 정확도
(J «프레임 경계의 월드 접지 좌표가 ±3.2px»).

**2인 일치 지적(=7회차 일감)**

| 지적 | I 실측 | J 실측 |
|---|---|---|
| ★ **핸드오프 절벽** — 640ms 를 들여 세운 히어로가 130ms 만에 사라진다 | 6.00× 축소 · 발선 **259px** 도약 · 평균 휘도 ×4.73 | 6.00× · 발선 **263px** · 상대휘도 **×18.6** |
| ★ **착지에 원인이 없다** | 마지막 체공이 **t=472.4ms** 에 끝나고 **167.6ms(26%)** 평평 → 낙하 없이 스쿼시 | 같은 값 + «스쿼시 k 가 계단 점프, 압축 구간 0ms» |
| **그림자가 그림자로 안 읽힌다 · 바닥선을 지운다** | 발밑 픽셀 **68.1%가 바닥보다 밝다** · 바닥선 코어의 **92%가 원래 휘도의 12%로** | 코어 대비 **1.09:1** · 캐릭터가 선 구간 **75%** 에서 선이 소멸 |
| **진행바가 자기 숫자와 어긋난다** | 라벨 2/8(25%) vs 채움 **17.0%**(−32px) | 같은 값 + 3/8 에서 −14px |
| **잉크 수치 브리핑이 틀렸다** | 정지 실측 **504×540**(브리핑 528×504) | 달리기 최대 **588×588**(브리핑 612×600) — 프레임 박스 값을 잉크로 적었다 |
| **케이던스가 느리다** | f0 한 장이 **108ms** 유지(등속 칸의 1.41배) | 640ms 에 **10장 = 15.6fps** · 등장이 1.19주기뿐 |

I 단독: 체공 진입/이탈에서 아트 자체의 12px 이 하드컷으로 배달된다(도약 32px 중 37.5%).
J 단독: 완전 실루엣이 보이는 창이 **248ms(4포즈)** 뿐 · 그림자 가시 지름이 선언(760)이 아니라 **621px**
(radial 마지막 스톱 82%) — «코어 418 ≥ 발 396» 의 분모가 380 이 아니라 311 이었다.

★ 두 사람의 처방이 **반대**인 것이 하나 있다(기록만 하고 7회차에서는 손대지 않았다):
잉크가 축에서 +54px 치우친 문제를 I 는 «축을 발과 잉크의 중간(FDX 27)으로», J 는 «그림자를 잉크 중심으로»
하라고 했다. 6회차가 방금 «축은 하나» 로 정리한 자리라, 한 회차 만에 되돌리지 않고 다음 회차 판단으로 미룬다.

---

## 7회차 (2026-08-27, 같은 세션) — 6회차 2인 일치 지적 반영

캡처 `docs/shots/163-r7-bg.png` + `163-r7-1..8.png`(위상 30·120·**160 체공**·280·400·**560 체공(마지막 도약)**·700 착지 · 전환 합성).
게이트 **VERIFY163 84/84 PASS** · **PROBE163B 12/12 PASS**.

**① 핸드오프 트윈(두 사람의 공통 1순위).** 페이드 130ms 동안 로딩 히어로를 **게임 히어로의 크기·발자리로 이어 보낸다** —
`ldFinish(fn)` 으로 본 스크립트가 목표(게임 플레이어의 화면 좌표)를 주고, 모듈이 `transform` 으로 배율 12→2 · 발자리까지 트윈한다.
- ⚠ **`player.x/y` 는 월드 좌표다**(화면 = 월드 + `camOx/camOy`). 처음에 그걸 빼먹어 목표가 1,273px 옆으로 갔고 게이트 §13 이 잡았다.
- ⚠ 트윈 기준을 `getBoundingClientRect()` 로 잡으면 **착지 스쿼시가 살아 있는 순간에 배율 1.1 이 섞여** dx·dy 가 10% 어긋난다(Δy 14~19px). 레이아웃 박스(offset 체인)로 바꿨다.
- 게이트 §13 은 판정을 둘로 나눈다: ⓐ 트윈이 **받은 목표**에 갔나(±2px, 스냅숏 `LD.handTo()`) ⓑ 그 목표가 **게임 히어로**였나(±340/±80 — 히어로는 그 사이에도 움직인다).

**② 착지에 원인을 만들었다.** 이동 거리를 **체공 칸이 끝나는 누적 지점**(1,143.9px = 정확히 1.5주기)에 맞춰
`LD_X0` −904 → **−1144**. 이제 «마지막 발이 닿는 순간 = 도착» 이라 스쿼시가 착지의 결과가 된다.
덤으로 같은 640ms 에 거리가 +26% → 케이던스 **10장 → 12장(15.6 → 21fps)**(I·J 의 케이던스 지적).
- 곁가지로 «감속 구간은 아치 0» 이라는 옛 조건을 **뺐다**. 그 조건이 있으면 마지막 도약(감속 구간에 걸친다)만
  «다리는 공중, 몸은 평평» 이 된다 — 7회차 첫 캡처에서 t=560ms 가 실제로 그렇게 나왔다.

**③ 바닥선을 그림자 «위» 로 + 무대 바닥 밴드.** 선이 그림자를 가로지른다(I «코어의 92%가 죽는다» · J «75% 소멸»).
그리고 J 가 근본 원인을 실측했다 — 플레이트 상대휘도 **Y=0.0052 에서는 순흑도 1.13:1 이 상한**이라 «더 어둡게» 로는 회수 불가.
그래서 바닥을 데운 밴드(`#ldStage::after`)를 깔았다. 게임 화면과의 휘도 낙차(J: ×18.6)도 같이 줄어든다.

**④ 진행바 채움 전이 제거.** `.05s` 로도 두 사람이 각각 −32px / −8.0%p 를 실측했다. 숫자가 진실이면 바도 같은 프레임에 진실이어야 한다.

**⑤ 측정표의 잉크 수치 정정.** 달리기 최대 **588×588** · 정지 최대 **540×540**(idle f0 은 504×540).
옛 «612×600 / 528×504» 는 프레임 박스 값이었다. `probe163b.py --gate` 가 **알파 bbox** 를 직접 재서 측정표와 대조한다.

### 7회차 채점 — **비평가 K ① 5 / ② 8 / ③ 4 / ④ 5 → 최저 4 · 비평가 L ① 4 / ② 8 / ③ 5 / ④ 6 → 최저 4**

**② 가 5 → 8 로 올라 두 사람 모두 통과선**에 닿았다(6회차의 잉크 수치 정정과 배율 검증이 반영됐다).
축 통일도 재확인 — K: 발 539.5 · 바닥선 539.5 · 그림자 540 · 진행바 539.5 · 문구 539.0, **전부 1px 안**.
바닥선 z 뒤집기도 실측으로 먹었다(K: 캐릭터가 선 자리에서 선 코어 **96.3% 보존** ← 6회차 8%).
진행바도 «숫자와 같은 프레임» 이 됐다(K: 1/8↔50px · 2/8↔98px · L: 반올림 ±2px 외 어긋남 0).
데운 밴드도 수치로 확인 — L: 코어 대 바닥 **1.09 → 1.38:1**, 코어가 발 스팬을 덮는 비율 **45% → 93%**.

**2인 일치 지적(=8회차 일감)**

| 지적 | K 실측 | L 실측 |
|---|---|---|
| ★ **마지막 도약이 늘어져 착지의 인과가 다시 깨졌다** | 상승 35.7 / 하강 81.5ms(1:2.28) · 착지 수직속도가 이륙의 **4.1%** | 체공 46.6·46.7·**113.4ms** · 유효 중력 **0.17배** · 낙차의 41%가 1프레임 스텝 |
| ★ **표본 8(핸드오프)이 무효 — 하네스 사고** | 발 기준점이 **(843.5, 1914)**, 트윈 절반의 기대는 (546.5, 1242.5) | 같은 진단 + 원인 지목 «`LD.hand(.5)` 를 `display:none` 상태에서 부른다» |
| **도약 진입·이탈에서 발이 12~13px 순간이동** | (6회차 I 가 먼저 지적) | t=517 하단 1343 → 518 **1331**, t=631 1330 → 632 **1343** |
| **케이던스 꼬리가 무너진다** | 마지막 실칸 **117.3ms(8.5fps)** = 최단 칸의 3.18배 · 꼬리 칸 5ms | 같은 값(114ms / 9ms · 편차 **12.7:1**) |
| **등장 앞 ~140ms(22%)가 빈 무대** | 잉크 진입 t=140.0ms · 표본 7장 중 2장이 빈 화면 | t=141ms · 온전한 달리기는 **218ms(34%)** 뿐 |
| **휘도 낙차** | 15.3:1(6회차 18.6 → −18%) | 14.5:1 · 130ms 에 **3.9스톱** |
| **그림자가 아직 절반은 발광** | 가시 픽셀 중 밝음 24.8% vs 어두움 17.2% · 코어:테 2.08:1 | 발밑 아래 행에서 **38%가 바닥보다 밝다** · 접지 기준 3개가 7px 산포 |

L 단독(제품 결함): **핸드오프 중 그림자가 발에서 떨어진다** — 히어로는 `translate(dx,dy)` 인데 그림자는 `translateX` 뿐이라
f=.5 에서 **120px**, f=1 에서 **221px** 벌어진다. K 단독: 착지 순간 실루엣이 **폭 −5.7%** 로 오히려 좁아진다
(스쿼시 +10% 를 run f4 → idle f0 의 폭 −14.3% 가 삼킨다) · 스쿼시 구간에서 12px 격자가 13/11 로 깨진다.

---

## 8회차 (2026-08-27, 같은 세션) — 7회차 2인 일치 지적 반영 (**캡처까지, 채점은 다음 세션**)

게이트 **VERIFY163 86/86 PASS** · **PROBE163B 23/23 PASS** · 캡처 `docs/shots/163-r8-bg.png` + `163-r8-1..8.png`.

1. **체공을 «거리» 에서 «시간» 으로.** 두 사람의 1순위였다. 체공 길이를 **등속 구간에서 그 칸을 가는 시간**
   (`LD_AIRMS` — 손으로 안 적는다)으로 못 박고, 그 시간이 지나면 칸이 안 끝났어도 **다음 접지 프레임으로 넘긴다.**
   덕분에 세 도약의 체공이 같아지고(113ms → 47ms), 감속 중에 도약 자세로 얼어 있지 않는다(K 의 «117ms 정지» 도 같이 줄어든다).
   거리→시각 역산 `ldTimeAt()`(ldAt 의 해석적 역함수)을 새로 뒀다 — 표본으로 재지 않는다.
2. **발 높이 연속(LD_LIFT).** 아틀라스에서 체공 프레임(f3·f7)은 잉크 바닥이 1 src px 위에 그려져 있다(=12px).
   아치에 그 값을 상쇄해 얹어 **도약 진입·이탈의 12~13px 순간이동**을 없앴다. 표는 `probe163b.py --gate` 가 아틀라스와 대조한다.
3. **핸드오프에서 그림자가 세로까지 따라간다**(L 단독 지적, 제품 결함).
4. **하네스 사고 수정 + 제품 가드.** `cap163.js` 가 `LD.hand(.5)` 를 «보이는 상태» 에서 부르도록 순서를 바꾸고,
   `ldHand` 에 `if (!cv.offsetParent) return false` 가드를 넣었다. 8회차 표본 8은 `translate(7.6px,−131.9px) scale(0.5833)` —
   L 이 «제품 코드를 제대로 돌리면 이 값이어야 한다» 며 계산한 `translate(−14.1px,−126.2px) scale(0.5833)` 와 같은 자리다.

### 8회차 인계 — 다음 세션이 할 일

- **r8 캡처 8장은 이미 있다(채점 전).** `docs/shots/163-r8-{bg,1..8}.png` · 위상 30 · 120 · **160(체공)** · 280 · 400 ·
  **560(마지막 도약)** · 700(착지 스쿼시) · 전환 합성(핸드오프 절반). 브리핑은 7회차 것을 그대로 쓰되
  **① 체공은 고정 시간(47ms) ② 아치는 아트 들림(12px)을 상쇄 ③ 표본 8은 유효한 핸드오프 절반** 세 줄만 갱신하면 된다.
- 아직 안 고친 2인 일치분(우선순위 순): **ⓐ 등장 앞 140ms 빈 무대**(K·L — 바닥선 좌우 여백을 120 → 40 으로 줄이거나
  등장 길이를 780ms 로) · **ⓑ 휘도 낙차 14.5~15.3배**(데운 밴드를 프레임 전체 저주파 층으로) ·
  **ⓒ 그림자 가시 부분의 38~25%가 아직 바닥보다 밝다**(밝은 테를 아래 반원에서 제거) ·
  **ⓓ 케이던스 꼬리 칸 5~9ms**(도착 판정을 t=RUN 에 묶고 마지막 접지 칸에 최소 2프레임 보장) ·
  **ⓔ 착지 순간 폭이 −5.7% 로 좁아진다**(K — 스쿼시 동안 run f4 자세를 유지하고 회복 뒤 idle 로).
- 여전히 **판단 대기**: 잉크 축이 +54px 치우친 문제에 두 회차째 처방이 갈린다(6회차 I «축을 중간으로» vs J «그림자를 잉크 중심으로»,
  7회차 L «달릴 때 접지발이 그림자 중심 기준 210px 스윙»). 축 통일을 되돌리는 결정이라 **한 세션에서 뒤집지 말 것.**

---

### 8회차 채점 (2026-08-28, sess-0200-26517 워커 D) — **비평가 M ① 5 / ② 6 / ③ 6 / ④ 5 → 최저 5 · 비평가 N ① 5 / ② 6 / ③ 5 / ④ 6 → 최저 5**

7회차 K 4 / L 4 에서 **둘 다 5** 로 올라왔다. 7회차 처방은 대체로 먹었다 —
축 정합은 두 사람이 각각 «최대 어긋남 **0.5px**»(M: 문구 539.5 · 트랙 539.5 · 숫자 539.5 · 발 539.5 · 바닥선 539.0),
진행바는 «숫자와 같은 프레임»(M 1/8→50px·2/8→98px · N «중간 폭이 관측되지 않는다 = 전이 없음»),
명도 사다리는 전 단계 **3.97:1 이상**, 8회차의 `LD_LIFT` 상쇄도 «접지 3장 잉크 바닥이 모두 1342 · 순간이동 0»(M·N 일치)로 확인됐다.

**★ 이번 회차의 본체는 «8회차가 고치면서 만든 것» 이었다.** 두 사람의 **1순위가 같았다**:

| 지적 | M 실측 | N 실측 |
|---|---|---|
| ★ **착지에 인과가 없다 — 캡이 만든 새 결함** | 발은 t≈564.8ms 에 닿는데 스쿼시는 640ms · 그 사이 **75.6ms(11.8%)** 를 아치 0 인 채 36px 미끄러진다 | 표본7 잉크 높이 **513/540 = 회복 정확히 50% 지점** → 스쿼시 시작 = 700−60 = **640.0ms**, 접지는 564.8ms → **인과 75.2ms** |
| ★ **빈 무대 21%** | 표본1·2 는 차분(임계 8/255) 참 픽셀 **0개** · 잉크 진입 **t=134~140ms** | 표본1·2 는 진행바 밴드를 빼면 최대 차분값이 **정확히 0** · **138ms(21.6%)** · 이동의 24~25%를 프레임 밖에서 소모 |
| ★ **마지막 도약이 −38%** | 체공 거리 95.3 · 95.3 · **59.0px**(−38.1%) | 같은 값(**−37.8%**) · «마지막 도약만 1.61배 가파르다» |
| ★ **꼬리 칸이 최장** | **75.6ms** = 최단(36.7)의 2.06배 · 640ms 에 13장 = 20.3fps | **75.2ms** · 다른 칸(36.9~60.5)보다 24~104% 길다 · 그동안 몸만 36.1px |
| ★ **스쿼시 구간의 비정수 격자** | 표본7 런 길이 가로 13(993회)/12(668회) = **12.60** · 세로 11(1166회)/12(785회) = **11.40**. 표본4·5·6 은 정확히 12/24/36 | 세로 45행 중 **27행 11px · 18행 12px** · 가로 42열 중 **25열 13px** · 120ms 내내 ±1px 지그재그 |
| ★ **그림자가 발광 링으로 읽힌다** | 풋프린트 면적가중 평균 ΔY 가 세 프레임 모두 **양수**(+0.006~+0.016) · 밝은 면적 37.6% vs 어두운 18.5% · 테 +2.66:1 이 코어 −1.39:1 을 이김 | 밝음 신호 0.111 / 어두움 0.0217 = **5.1배** · y1364 에서 x367~711 전 구간이 바닥보다 밝고 **발 스팬 안쪽을 관통**(발밑 4.24배 밝음) |
| **도약이 세로로 안 읽힌다** | 아치 진폭 **20.7px** = 정지 잉크 높이의 4.0% | **22.7px** = 몸 높이의 3.9% · «그림자는 같은 순간 폭이 6.4% 줄어 «떴다» 고 하는데 잉크는 아니다 — **둘이 다른 말을 한다**» |
| **좌측 잘림** | 등장의 **66%(422ms)** 가 잘린 채 · 온전 노출 34% | 같은 값(422.1ms · 온전 **218ms(34%)**) |
| **핸드오프 낙차** | 복원한 게임 평균 Y 0.11895 vs 로딩 0.00778 → 대비 **2.92:1** | 같은 복원식으로 **15.2배 = 대비 2.92:1** · 크기 6배 축소 |

M 단독: 표본8 트윈은 «발끝 (547.5, 1207) 이 로딩(539.5,1342)·게임(≈557,1074)의 중점 (548.3,1208) 과 1px 이내» — 8회차의 하네스 사고 수정이 먹었다.
N 단독: 같은 트윈을 «블록 피치가 정확히 7px = 12×0.5833» 으로 독립 확인. 그리고 **«차분 마스크만 쓰면 34px 틀린다»** —
그림자 테의 청색 글로(채도 44)가 차분에 섞여 표본6 잉크 바닥을 1370 으로 내는데, 팔레트 정확일치는 1336 을 낸다.

---

## 9회차 (2026-08-28, sess-0200-26517 워커 D) — 8회차 2인 일치 5건 반영

게이트 **VERIFY163 89/89 PASS ×3** · **PROBE163B 23/23** · **SMOKE PASS** · 콘솔 에러 0 ·
캡처 `docs/shots/163-r9-bg.png` + `163-r9-1..8.png`.

### ★ 이 회차의 핵심 — «셋을 동시에 만족시킬 수 없다» 를 증명하고 나서 무엇을 놓을지 골랐다

두 사람의 1순위(착지 인과)를 고치는 길은 겉보기에 둘이었다. 8회차가 넣은 체공 캡을 **빼면** 착지 = 도착으로 돌아가지만,
그 도약만 110ms(앞 도약의 2.4배)로 늘어져 **7회차 K·L 이 «유효 중력 0.17배» 로 짚은 자리로 정확히 되돌아간다.**
캡을 **유지**하면 착지가 75ms 일찍 끝난다. 그래서 N 이 제안한 세 번째 길 — «거리를 다시 풀어 마지막 도약이
캡 시간 안에 정상 보폭을 가게 한다» — 을 닫힌 해로 풀어 봤고, **불가능**임이 나왔다:

> 감속이 도착에서 속도 0 으로 끝나므로 «마지막 46.7ms 가 가는 거리» 는 `Δ = D(1−CRZD)·(T/(RUN(1−CRZ)))²` 이고,
> `Δ = D − c` 를 풀면 `D = [c + √(c²+4·15903)]/2` → **Δ = 18.6px**(정상 보폭 95.3px 의 20%). 어떤 D 를 골라도 그렇다.

즉 **«체공 고정 시간» · «정상 보폭» · «착지 = 도착»** 셋은 동시에 성립하지 않는다. 그래서 셋 중 하나를 골라 놓았는데,
놓은 것은 **«스쿼시가 도착에 붙어 있어야 한다»** 였다 — **스쿼시의 원인은 도착이 아니라 착지**이기 때문이다.
이제 스쿼시는 `ldLandAt()`(= 마지막 체공이 끝나는 시각)에 걸리고, 남은 68ms 는 «착지 → 스쿼시 → 미끄러지며 정지» 라는
**정착 구간**으로 읽힌다. **인과 오차 75.2ms → 0.**

### ★ 두 번째 핵심 — 빈 무대는 «길이» 가 아니라 «도착 지점의 제약» 이 만들고 있었다

세 라운드 연속 2인 일치였다(7회차 K 140.0 · L 141 · 8회차 M 134 · N 138ms). 그런데 **등장 길이를 어떻게 바꿔도 21% 는 안 변한다** —
표로 확인했다(640/720/780ms 전부 20~22%). 원인은 이렇다:

- 도착은 «체공 칸 끝» 이어야 한다(7회차에 세운 규칙 — 그래야 착지에 원인이 있다). 그런 지점은 반주기 간격뿐이다: **381.28 · 762.56 · 1143.84 …**
- t=0 에 잉크가 프레임 밖이려면 이동이 **858px 이상**이어야 한다(게이트 §4, 2회차 불변식).
- 그래서 고를 수 있는 값이 **1143.84 하나**뿐이었고, 그 차 **262px = 21%** 가 통째로 빈 무대였다.

M 의 처방 3 은 «잉크를 프레임 안에서 시작해라» 로 그 불변식을 푸는 것이었지만, 그건 1회차 비평 A 가
«몸의 39%가 화면 안에서 켜졌다» 로 만든 자리라 되돌리고 싶지 않았다. 대신 **손댈 자를 바꿨다** —
거리를 줄이는 게 아니라 **케이던스의 시작 위상**을 옮긴다. `LD_P0 = 2`(f2 에서 출발)면 그 위상만큼(210.48px) 앞이 잘려
이동이 **933.36px** 이 되고, 도착은 여전히 1143.84(= 1.5주기, 체공 칸 끝)에 **정확히** 떨어진다.
f2 는 박스 우변이 **858** 로 8칸 중 가장 좁아(f0·f4 는 882) 프레임 밖 요건도 **75px 여유**로 지킨다.

실측(게이트가 다시 잰다): 빈 무대 **138 → 40ms(21.6% → 7.1%)** · 칸 편차 **2.04 → 1.73** · 케이던스 **20.3 → 21.4fps**.
`LD_RUN` 을 640 → **560** 으로 같이 내린 것은 길이를 줄이려는 게 아니라 **등속 발속도를 지키려고** 따라간 값이다
(같은 640ms 를 쓰면 등속이 2.04 → 1.67px/ms 로 느려져 6회차 I·J 가 «15.6fps 는 느리다» 로 짚은 자리로 돌아간다).
`LD_X0` 은 이제 **손으로 안 적는다** — `−(1.5·LD_CYC − LD_PH)` 로 표에서 파생된다.

### 그 밖에 반영한 것

- **스쿼시 배율을 정수 픽셀 격자에 스냅**(M «가로 12.60 / 세로 11.40» · N «45행 중 27행이 11px» — 소수점까지 같은 지적).
  `round(SC·s)/SC` 로 스냅하면 한 화면 안에서 열·행 폭이 전부 같은 정수가 된다. 진폭은 ±10% → **±8.3%**(12→13/11)로 한 칸 줄지만,
  2회차 비평 D 가 «−1.8% 는 임팩트가 없다» 고 한 자리에서 한참 위이고 **지금 문제는 진폭이 아니라 격자가 흔들리는 것**이었다.
- **그림자** — 테 알파 **.50 → .22** · 어두운 구간 반경 **418 → 441px**(발 스팬 396 을 덮는다) · 사다리를 .94/.92/.72/.34 네 칸으로.
  바닥 밴드도 더 데우고 넓혔다(높이 300→360 · 반경 58%→72% · 알파 .30→.46) — M 의 처방 2 대로,
  **바닥이 밝아야 순흑 코어의 대비 상한이 −1.41 에서 −1.9:1 대로 내려간다.**
- **아치 진폭 20 → 56px**(N 이 계산한 균형점 «몸 높이의 10.4%» 를 따랐다. 캡처 실측 잉크 들림 = 정확히 56px).
- **곁가지 버그 1건** — `ldTimeAt` 이 `LD_RUN` 을 쓰고 `ldAt` 은 `ldRun` 을 써서, **재방문의 압축 경로**
  (ldRun 이 1/5 로 줄어드는 길)에서 체공 캡이 엉뚱한 시각을 받고 있었다. 역함수가 되도록 `ldRun` 으로 맞추고,
  캡도 같은 비율로 줄이는 `ldAirMs()` 로 갈랐다.

### 게이트 — 5건 이사 · 3건 신설 (86 → **89**)

제품을 바꿨더니 **자가 못 따라온 것이 5건**이었다. 전부 지우지 않고 **이사**시켰다(185-④):

| 단언 | 왜 빨개졌나 | 어떻게 옮겼나 |
|---|---|---|
| «체공은 고정 시간» | 이름이 `LD_AIRMS` → `ldAirMs()` · `ldSegStart` 로 바뀜 | 새 이름으로 대조 |
| «도착이 체공 칸 끝» | **이동 거리만으로 판정**하고 있었다 — 위상이 0 일 때만 맞는 자 | `(이동 + LD_PH) % 주기` 로 |
| «최소 1.4주기» | 위상을 옮기면 같은 걸음 수에도 주기 수가 준다(1.22주기 = **접지 7걸음**) | 지키려던 것(«다리가 세 걸음 이상»)대로 **접지 칸을 직접 센다** |
| «어두운 코어가 발 스팬을 덮는다» | 그래디언트에서 «알파 **.55**» 라는 **철자**를 찾고 있었다 → 사다리를 다시 깔자 «코어 0px» 로 헛불림 | «알파 ≥ .5 인 **마지막** 정지점» 으로 |
| «t=0 은 시작 오프셋» | `LD_X0` 이 표에서 파생되며 정수가 아니게 됨(−933.36) | `ldAt` 이 `Math.round` 하므로 **±0.5px 허용** |

신설 3건: **«착지 스쿼시가 도착이 아니라 마지막 체공 종료에 걸린다»**(되돌리면 빨개진다) ·
**«스쿼시 배율이 정수 격자에 스냅된다»** · **«등장 앞 빈 무대가 등장의 10% 이하»**(시작 위상을 f0 으로 되돌리면 먼저 빨개진다).

> ⚠ 신설한 빈 무대 단언이 **이 파일이 두 번 밟은 함정에 그대로 빠졌다** — `display:none` 인 채로
> `offsetLeft/offsetWidth` 를 읽어 박스 우변이 858 대신 **264** 로 잡혔고(2회차의 «잉크 우변 −18 이 통과» 와 같은 사고),
> 그 값으로 «빈 무대 11.7%» 라는 **틀린 FAIL** 이 났다. 보이게 해 놓고 다시 재니 858 · 7.1% 였다.
> 시간도 `LD.timeAt`(= `ldRun` 기준, 부팅 때 압축된다)을 안 쓰고 **곡선에서 직접 푼다** — 안 그러면 실행마다 흔들린다.

### 캡처 하네스 — 표본 위상 재조정

`cap163.js` 의 위상을 새 칸 경계에 맞춰 다시 잡았다(45 진입 직후 · **65·265·466 세 도약의 정점** · 120·380 접지 ·
**500 착지 직후 스쿼시 최대** · 전환 합성). **500 은 8회차 비평 N 이 «564.8~640ms 가 표본에 아예 없다 —
다음 회차엔 반드시 넣어라» 고 요구한 창**이고, 9회차의 핵심 변경이 정확히 거기에 있다.

### 9회차 채점 (2026-08-28, 같은 세션) — **비평가 O ① 6 / ② 6 / ③ 5 / ④ 5 → 최저 5 · 비평가 P ① 6 / ② 7 / ③ 6 / ④ 6 → 최저 6**

추이 **7회차 4/4 → 8회차 5/5 → 9회차 5/6.** ① 이 두 사람 다 5 → **6**, P 는 ②③④ 가 전부 6~7 로 올라왔다.

**★ 9회차 변경 5건은 두 사람이 독립적으로 «전부 반영됐다» 고 확인했다** — 그것도 서로 다른 자로:

| 변경 | O 실측 | P 실측 |
|---|---|---|
| 착지 인과 | 표본6 런 `{12:44}` (스쿼시 없음) → 표본7 `{13:44}`/`{11:47}` → 착지와 **≤8.3ms** | 표본6 = `run/frame0003` 12×12 · 표본7 = `run/frame0004` **13×11** 이고 박스 바닥 = 바닥선 상단 → 같은 시각 |
| 빈 무대 | 우변 통과 **40.3ms = 7.2%** | **40.09ms = 7.16%** |
| 스쿼시 격자 | 세로 47행 × 11px **예외 0** · 가로 48열 13px + **1열 12px** | x=13 · y=11 **정확히 정수** · 47×11 = 517 = 실측 |
| 그림자 | 코어 알파 0.956 · 바닥/코어 **1.90** — «발광» 역전 제거 | 발밑 평균 ΔY **−0.0402 / −0.0367**, 어두운:밝은 = **648:0 / 156:0** |
| 도약 진폭 | 정점 57px(사양 56) · 세 정점이 **픽셀 단위로 동일** | 세 표본 잉크 바닥 **1286 / 1286 / 1286** → 들림 **56 / 56 / 56px, 편차 0** |

P 는 궤적을 **아틀라스 재현 완전일치**로 검산했다(79×63 소스박스를 정수배율 nearest 로 렌더해 캡처와 화소 대조 —
표본1~6 불일치 **0px**). 표본별 잉크 우변 실측 대 예측 **최대 오차 0.45px**, 등속 8칸의 몸 이동이 선언 보폭표와
**모두 ≤0.2px** — «케이던스-거리 결합은 제대로 구현됐다».

**2인 일치 지적(=10회차 일감)**

| 지적 | O 실측 | P 실측 |
|---|---|---|
| ★ **그림자가 «그려진 접지발» 이 아니라 앵커에 붙어 있다** | 표본7 발 중심 690.5 vs 그림자 506.6 = **184px** · 발이 그림자 테두리의 **85% 지점**을 밟는다 | **+162.0px** = 그림자 반폭의 49% · 그림자가 잉크 왼끝보다 **51px 더 왼쪽**으로 삐져나온다 |
| ★ **착지에서 그림자만 과하게 커진다** | 415 → 504px = **+21.4%** 인데 스프라이트 가로 스쿼시는 +8.3% — **2.6배 과함** | 반폭 258.5 → **330px(+27.7%)** · 스쿼시로 설명되는 값은 280px, **50px 이 미설명** |
| ★ **밝은 테가 아직 세다** | 테/코어 대비비 **2.87~2.93** — «그림자가 바닥과 갖는 대비(1.90)보다 내부 링의 대비가 1.5배 세다» | 코어 −0.0461 vs 테 +0.0412 → **진폭비 0.89** · «테 알파 .22 는 면적으로만 이긴 것» |
| **마지막 도약의 보폭이 −33%** | «**공중 제동** — 마지막 체공 25.7ms 동안 수평 속도가 40% 깎인다» | 이 칸의 몸 이동 **63.63px vs 그려진 보폭 95.3px = −33.2%** |
| **좌변 잘림이 등장의 61%** | 절단 종료 **~345ms(61.6%)** · 온전 노출 211ms(37.8%) | **342.0ms(61.1%)** · +120ms 에 가시 잉크가 전체의 **28.6%** |
| **핸드오프 낙차** | 로딩 0.0092 → 게임 0.1191 = **13배**(대비 2.86) | 같은 값 **12.9배**(대비 2.87) |

O 단독: «표본8 에 그림자가 없다(f=0.5 에서 알파&lt;0.1)». **확인해 보니 제품은 옳다** — `ldHand` 가 그림자에
`opacity = 1 − f` 와 히어로와 **같은 translate·scale** 을 건다(8회차 수정). O 는 로딩 바닥선 대역(행 1340)을 훑었는데
그림자는 이미 히어로를 따라 y≈1170 으로 올라가 있다. P 는 «게임 타일 무늬 때문에 분리 불가 — **미측정**» 으로 남겼다.
2인 일치가 아니고 측정 전제가 어긋난 건이라 손대지 않는다(기록만).

P 단독(사양 공백): 스쿼시 종료 **611.7ms** 가 도착 560ms 를 51.7ms 넘어간다. 구현은 `tLand` 기준이라 잘리지 않고
idle 로 이어지는데, **사양에 그렇게 적혀 있지 않다** — 아래 §사양에 명시했다.

---

## 10회차 (2026-08-28, 같은 세션) — 9회차 2인 일치 3건 반영

게이트 **VERIFY163 93/93 PASS ×3**(89 → 93, 신설 4) · **PROBE163B 23/23** · **SMOKE PASS** · 콘솔 에러 0 ·
캡처 `docs/shots/163-r10-bg.png` + `163-r10-1..8.png`.

### ★ 그림자를 «앵커» 에서 «그려진 접지발» 로

세 사람이 두 라운드에 걸쳐 같은 자리를 짚었다(8회차 M 147px · 9회차 O 184px · P 162px). 원인은 이랬다 —
그림자는 **앵커**(= idle 발 스팬 중심, 소스 22.0열)에 못 박혀 있는데, run 8프레임의 접지발은
소스 **11.5~35.3열**을 오간다. 배율 12 에서 **−126 ~ +160px** 이다.

★ **이것은 6회차의 «축은 하나» 를 되돌리는 것이 아니다.** 6회차가 정한 것은 **선 자세(idle)의 축**이고,
달리는 동안 발이 프레임 안에서 움직이는 것은 다른 축이다 — idle 에서는 이 보정이 **정확히 0** 이라
도착 시각의 축(540)은 그대로다. 되돌린 게 아니라 **못 보던 축을 하나 더한 것**이다.
표도 새로 안 잰다: `LD_FEET`(probe163b 가 아틀라스에서 잰 값)가 이미 제품에 있다.
체공 칸은 발이 안 닿으므로 이륙발 → 착지발로 **선형 보간**한다(중간에 offset 0 을 지나 몸 아래를 통과).

**실측 대조(r9 → r10, 같은 자로):**

| 표본 | 접지발 중심 | 그림자 코어 중점 r9 | r10 | Δ r9 | **Δ r10** |
|---|---|---|---|---|---|
| 7 (착지 f4) | 677.5 | 516.0 | **672.0** | 161.5px | **5.5px** |
| 5 (접지 f1) | 365.5 | 334.0 | **370.0** | 31.5px | **4.5px** |

> ⚠ **여기서 자를 한 번 잘못 골랐다.** 처음에 그림자 중심을 «ΔY 가중 중심(centroid)» 으로 쟀더니 r10 이 642.5 로 나와
> «Δ 35px 밖에 안 줄었다» 는 결론이 나왔다. 잉크 픽셀을 제외하고 재는 마스크라 **캐릭터가 판 구멍이 비대칭**이면
> 중심이 그쪽으로 끌린다. 어두운 런의 **좌우 끝 중점**으로 다시 재니 672.0(참값 676.0 예측과 4px)이었다.
> 두 비평가가 쓴 자도 «런의 좌우 끝»(P: 행 1339 의 어두운 런 [186..846] 중심 516.0)이라 이쪽이 대조 가능한 자다.

### 그 밖에 반영

- **착지 팽창 .30 → .10** — 스프라이트 가로 스쿼시(+8.33%)와 같은 폭으로. 실측 코어 반폭 **330 → 291px**.
- **밝은 테 알파 .22 → .10** 이고 테를 코어 안쪽으로 당겼다(74~86% → 74~80%), 어두운 구간은 58 → **60%** 로 확대.
- **바닥선 알파 .86 → .96 · 색도 한 단 밝게** — P «선의 실측 불투명도 α≈0.36 이라 히어로 아래에서 10.8% 파인다».

### 게이트 신설 4건 (89 → 93)

- ★ «그림자 x 가 `LD_FEET` 를 따라간다» (옛 `translateX(x)` 로 되돌리면 빨개진다)
- «`LD_FOOTC` 가 run 접지열 범위 근처다» = 보정을 **같은 자로 잰 값**으로 하고 있다
- «보정이 실제로 의미 있는 크기다(최대 160px)» — 이만큼이 8회차까지 어긋나 있었다
- «착지 팽창(.10)이 스프라이트 스쿼시와 같은 폭이다»

### 10회차 인계 — 다음 세션이 할 일

- **r10 캡처 8장은 이미 있다(채점 전).** `docs/shots/163-r10-{bg,1..8}.png` · 위상은 9회차와 같다
  (45 · 65 · 120 · 265 · 380 · 466 · 500 · 전환 합성). 브리핑은 9회차 것을 그대로 쓰되
  «10회차 변경 3건(그림자 접지발 추종 · 착지 팽창 .10 · 밝은 테 .10)» 으로 머리말만 갈면 된다.
- **아직 안 고친 2인 일치분(우선순위 순):**
  ⓐ **마지막 도약의 보폭 −33%(O «공중 제동» · P −33.2%).** 두 사람 다 «감속 시작을 420ms → 착지(491.7ms) 이후로
     미뤄라» 를 처방했다. 그러면 마지막 체공이 통째로 등속 안에 들어와 보폭이 95.3px 로 돌아온다.
     ⚠ 다만 그렇게 하면 게이트 §4 의 «감속 구간에 이동의 12% 이상» 이 빨개진다(감속 거리가 6.5% 로 준다).
     그 12% 는 **CRZ=.75(뒤 25% 시간) 시절에 쓰인 절대값**이라 CRZ 를 올리면 자동으로 깨지는 자다 —
     손대려면 «감속 구간 평균 속도 ÷ 등속» 같은 **비율 단언으로 재기준**하고 그 사실을 여기 적을 것(185-②).
     지키려던 것은 «트레드밀 아님» 인데, 5회차 이후 케이던스가 거리 구동이라 트레드밀은 **구조적으로 불가능**하다.
  ⓑ **좌변 잘림 61%(O 345ms · P 342ms).** 이동 933px 에 잉크 폭 588px 이라 구조적이다 —
     이동을 더 줄이면 «빈 무대» 와 «케이던스 장수» 가 같이 흔들리므로, 줄이기 전에 세 값을 **같이** 표로 놓고 고를 것
     (9회차의 `tune163b/c/d` 식 열거가 유효하다).
  ⓒ **핸드오프 휘도 낙차 12.9~13배** — 6회차부터 «아트·전환 설계 영역» 으로 분류돼 있다. 130ms 페이드 하나로는 회수 불가.
- **P 가 «아트 대기» 로 새로 등재한 것 2건**: ① run 보폭표가 1.64:1 로 고르지 않아 등속인데도 접지 칸이 39.6~64.9ms 로
  흔들린다(발 미끄럼 0 을 지키는 한 코드로는 못 편다) · ② **아틀라스에 `jump_start`·`jump_loop`·`fall_loop` 8프레임이
  있는데 도약에 안 쓰고 run 의 체공 프레임을 쓴다** — 아트 부재가 아니라 **선택**이다. 세 도약이 픽셀 단위로 같아
  «마지막 도약» 이 마지막으로 안 읽히는 문제(P ①)를 여기서 풀 수 있다.

## 게이트 `tools/verify163.js` — 무엇을 지키나

| 절 | 지키는 것 |
|---|---|
| §1 | `#loading` 구조 9종 · 캔버스 720×512 · `image-rendering:pixelated` · 문구 유지 |
| §2 | 상수 정합 — **JS `LD_FADE` = CSS `transition-duration`**(두 곳에 적힌 값) · `LD_HARD + LD_FADE < 850` · **스트라이드(아틀라스에서 프레임 수를 다시 세서) ≈ 등장 길이** · 배율이 정수이고 ≥7 · 모듈이 본 스크립트보다 앞에 있다 |
| §3 | ★ **850ms 전에 «안 보이게» 된다** — 이 저장소 게이트 40여 개의 캡처를 지킨다 |
| §4 | 궤적 — `LD.at(t)` 가 easeOutCubic 과 ±1px · 단조 감속 · 등속이 아님 · **t=0 에 잉크가 완전히 프레임 밖**(아틀라스에서 재계산) · DOM transform 이 그 함수를 따라간다 |
| §5 | 부팅 뒤 오버레이가 탭을 안 막는다(`.thru` = pointer-events:none, elementFromPoint 로 확인) |
| §6 | 진행 표기 «n/총계» 의 총계가 **ATLAS 에서 파생**(마크업 하드코딩 금지) |
| §7 | **무한 로딩 방지** — knight 가 깨져도 로딩이 끝난다(«등장이 끝나야 나간다» 로 되돌리면 빨개진다) |
| §8 | 부팅 분리 — 오버레이가 떠 있어도 게임은 이미 돌고 있다 · 본 스크립트가 아틀라스를 **다시 안 받는다** |
| §10 | ★ **앵커 산식이 게임과 같다** — 로딩의 `ldDraw` 와 79/80 의 `drawHeroTo` 를 같은 프레임·배율로 돌려 **drawImage 인자와 변환행렬**을 대조(file:// 는 캔버스가 tainted 라 픽셀 비교 불가) |

### 계측이 대상을 흔든 사고 (게이트를 두 번 다시 만든 이유)

1. 1차 게이트는 «전이 시각 관찰 + rAF 궤적 추적» 을 **한 페이지에서** 했다. 그 rAF 루프가 이미
   포화된 메인 스레드를 더 밀어 `hero 413→645ms · gone 758→1204ms` 로 **계측 없는 실제보다
   450ms 늦게** 나왔고, 그대로 «800ms 초과» FAIL 을 냈다. → 시간축은 MutationObserver 만,
   궤적은 별도 페이지.
2. 그 다음엔 «표본으로 이징을 재려» 했는데, 로딩 구간에서는 rAF 콜백이 프레임 시각보다
   최대 **140ms** 늦게 실행돼 찍은 x 가 «언제의 값인지» 를 표본 쪽에서 못 정한다(오차 **322px**).
   → 이징을 **순수 함수 `LD.at(t)`** 로 빼서 곡선은 직접 재고, 배선은 한 번만 확인.
3. `display:none` 시각을 기준선으로 삼은 것도 틀렸다. 캡처를 오염시키는 건 «언제 안 보이나» 이고
   불투명도 전이는 **컴포지터**가 돌린다. `display:none` 을 붙이는 타이머는 부팅 직후 메인 스레드에
   밀려 100~350ms씩 늦어 **실행마다 뜨고 지는 FAIL** 이 된다. → 기준은 «페이드 시작 + LD_FADE».

### 10회차 채점 (2026-08-28, sess-0338-17194 워커 G) — **비평가 Q ① 8 / ② 8 / ③ 4 / ④ 7 → 최저 4 · 비평가 R ① 7 / ② 8 / ③ 4 / ④ 6 → 최저 4**

추이 **7회차 4/4 → 8회차 5/5 → 9회차 5/6 → 10회차 4/4.** 점수는 내려갔지만 **①②④ 는 전부 올랐다**
(Q ①8 ②8 ④7 · R ①7 ②8 ④6 — 라운드 최고). 내려간 것은 **③ 하나**이고, 두 사람이 **같은 두 자리**를 짚었다.

**★ 10회차 변경 3건은 두 사람이 독립적으로 «전부 반영됐고 목표 수치에 닿았다» 고 확인했다.**

| 변경 | Q 실측 | R 실측 |
|---|---|---|
| 그림자 → 접지발 추종 | 표본5 **Δ+0.5px** · 표본7 **Δ−1.5px** · 체공 보간 −1.3px (목표 ≤10) | 표본5 **+2.0px** · 표본6 **+0.7px** · 표본7 **−5.0px** |
| 착지 팽창 .30 → .10 | 어두운런 529 → 579px = **+9.45%**(예측 +9.31%) | 537 → 586px = **+9.12%**, 스프라이트 +8.16% |
| 밝은 테 .22 → .10 | 정점비 **2.6:1 어두움 승** · 면적가중 **8.7:1** · 발 스팬 밑 **100%** 가 플레이트보다 어둡다 | 테/코어 대비 **1.21:1**(9회차 2.66:1) · 어두운 코어 1.98:1 로 테를 이긴다 |

Q 는 제품 상수를 파이썬으로 재구현해 표본과 픽셀 대조했고(표본1·2 IoU **1.0000**, bbox 오차 ≤0.5px),
R 은 아틀라스 재현 렌더로 대조했다(표본5·6·7 **0~0.5px**). 부수 확인도 둘 다 통과 —
칸 경계 10칸 전부 브리핑 표와 **0.01ms 이내** · 체공 3회 **전부 50.04ms** · 아치 정점 **정확히 56px** ·
스쿼시 격자 가로 13 / 세로 11px **균일, ±1px 지그재그 0건**(9회차 M·N 이 잡았던 자리) · 빈 무대 **39.63ms = 7.08%**.

**★ 그런데 ③ 이 5·6 → 4 로 내려갔다. 두 사람이 짚은 원인은 같고, 둘 다 «10회차가 새로 만든 것» 이다.**

| 지적 | Q 실측 | R 실측 |
|---|---|---|
| ★★ **선 자세(t ≥ 560)에서 그림자가 발에서 +160px 어긋난다** | «t=555 Δ−2.0 → t=560 **Δ+160.0** → t=1200 **Δ+160.0**(항구적) · 그림자 폭 760px 의 21.1% · 어두운 코어가 436~964 라 캐릭터 왼쪽 1/3(342~436) 밑에 그림자가 없다» | «t=600·900 에서 **+157.5px** · 발 스팬 396px 중 **112px(28.3%)**가 어두운 구간 밖 · 오른쪽으로 **203px** 삐져나온다» |
| ★★ **접지 칸에서 그림자가 몸에 끌려갔다 칸 경계에서 되돌아온다** | «560ms 안에 뒤로 튀는 지점 **7회** · 최대 **−122.5px**(t=154.5·355.0) · 몸 축 대비 진폭 **284.8px** = 잉크 폭의 48.4%» | «한 프레임에 **−122.5px** = 어두운 폭 537px 의 22.8% · 접지 칸 순 전진 230.7px vs 체공 칸 912.8px — **이동의 97.8%를 체공에서 몰아 한다**» |
| ★ **스쿼시 양자화가 그림자에만 안 걸려 회복 중 어긋난다** | «스프라이트는 491.67~561.67ms 의 평평한 **70.0ms 사각 펄스** · 그림자는 연속 감쇠 → t=560 에 **4.02pp**(그림자 폭 기준 30.5px)» | «t≈562 에 스프라이트 1.000 / 그림자 **1.0414** — 10회차가 «같은 폭» 으로 맞추려던 바로 그 자리에서 **4.16pp(반폭 15.8px)**» |
| 도착 프레임 포즈 팝 | «run f4 → idle 한 프레임에 잉크 −84px(−14.3%) · 그려진 발 **−162px**» | «t=560 에 접지발 **702.0 → 539.5 = −162.5px**, 그동안 그림자는 1px 도 안 움직인다» |
| 표본 집합이 결함을 못 본다 | «표본 8장 중 **t ≥ 560 인 것이 하나도 없다** — 브리핑 표본 집합이 이 결함을 구조적으로 못 보게 되어 있다» | «표본 8장은 t ≤ 500 만 담고 있어 보이지 않는다» |

R 단독: ⓐ `air = |arc| / LD_ARC` 가 부호를 잃어 **이·착지 순간에 그림자가 3.9% 작아지고 9.6% 옅어진다**
(실측 t=491ms `scale(0.968) opacity 0.920`, 이때 잉크 하단은 바닥에서 2px) ·
ⓑ 첫 도약(t=45·65)이 «도약» 으로 안 읽힌다(화면 안 잉크 1.1% / 8.2%, 그림자 0px) ·
ⓒ `LD_FEET` 를 바닥 **3행** 평균으로 재서 f6 은 진짜 접지 1행 중점과 12.0px 어긋난다.
Q 단독: 체공 곡선이 `sin` 이라 같은 높이의 포물선 대비 이륙속도 −21.5% · 정점가속 +23.4%.

---

## 11회차 (2026-08-28, 같은 세션) — 10회차 2인 일치 3건 + 부호 오류 1건 반영

게이트 **VERIFY163 93 → 97/97 PASS** · **PROBE163C 9/9**(신설) · **PROBE163B 23/23** · **SMOKE PASS** ·
콘솔 에러 0 · 캡처 `docs/shots/163-r11-{bg,1..9}.png`(**9장 — 정지 표본 신설**).

### ★ 이 회차의 한 줄 — 10회차의 보정은 **옳았는데 «꺼지지 않았다»**

세 결함이 전부 같은 모양이다: 10회차가 더한 `sdx` 보정이 **① `p ≥ 1`(선 자세)에서 안 꺼지고
② 칸 안에서 얼어붙어** 있었다. Q 의 표현대로 «세 건의 변경 자체는 전부 옳은 방향이었고 수치도 목표에 닿았으나,
그 보정이 꺼지지 않고 얼어붙어 있는 두 곳이 남았다».

| # | 고친 것 | 되돌렸을 때(실측) | 고친 뒤(실측) |
|---|---|---|---|
| ⓐ | `fo = p < 1 ? LD_FEET[fi] : LD_FOOTC` — 보정을 «달리는 동안» 으로 가둔다 | 정지 보정 **+160.0px**(항구) | **0.0px** (t=561~760 전부) |
| ⓑ | `if (p < 1 && !LD_AIRF[fi]) sdx -= fr.u * LD_STEPS[fi]` — 접지 칸은 **월드 고정** | 역행 **7회 · 최대 −122.5px** · 칸 안 드리프트 122.5px | 역행 **0회** · 드리프트 **1.00px**(= `toFixed(1)`+정수 x 반올림 하한) |
| ⓒ | `ldSqQ(k)` 신설 — 스프라이트와 그림자가 **같은 한 값**을 쓴다 | 회복 중 **4.13pp** 차 | **0.03pp**(표시 자릿수 하한) |
| ⓓ | `air = Math.max(0, -arc) / LD_ARC` — «위로 뜬 만큼» | 접지 순간 `scale 0.965 · opacity 0.912` | **1.000 / 1.000** (체공 정점은 0.859 로 유지) |

★ **되돌리기 검증을 실제로 돌렸고, 되돌린 값이 두 비평가의 실측과 일치했다** —
−122.5px ×7 · +160px · 4.13pp · 0.965/0.912. 즉 두 사람의 자와 제품의 자가 같은 것을 재고 있었다.

### ⓑ 의 칸 경계 연속성은 «맞춰 넣은 것» 이 아니라 항등식이다

접지 칸 끝 `(FEET[i]−FOOTC)·SC − STEPS[i]` 가 다음 칸 머리 `(FEET[i+1]−FOOTC)·SC` 와 같으려면
`STEPS[i] = (FEET[i]−FEET[i+1])·SC` 여야 하는데, 그것이 접지 칸 `LD_STEPS` 의 **정의**다(6회차 `LD_GAPS`).
체공 칸 진입(τ=0)·이탈(τ=1)도 같은 항등식으로 이어진다 — 그래서 허용오차가 아니라 **0** 이다.

### 도착 프레임의 −160px 은 «결함» 이 아니라 «포즈» 다 — 가르는 자를 세웠다

Q §D·R §B 가 짚은 도착 프레임 발 점프(−162px)는 **아트의 축 차이**(run f4 접지발 35.33열 vs idle 22.0열
= 13.33 src px × 12)라 코드로 못 없앤다. 10회차의 결함은 그 점프가 아니라 **발은 갔는데 그림자만 남은 것**이었다.
그래서 게이트를 «도착에서 안 움직인다» 가 아니라 **«도착에서 그림자가 포즈를 따라간다»**(보정 160 → 0)로 적었다.

### 게이트

- **`tools/verify163.js` 93 → 97** — 10회차의 «착지 팽창 .10 이 스프라이트와 같은 폭» 단언을 **재기준**했다.
  그 단언은 `.10 * k` 라는 **연속값**을 정규식으로 확인하고 있었는데, 비평가들이 증명했듯 그 값은 스프라이트와
  **같은 폭이 아니었다**(회복 중 4.02pp). 규칙을 «두 값이 같은가» → **«출처가 하나인가»**(`ldSqQ`)로 바꿨다.
  새 단언 3건: ⓐ 보정이 `p < 1` 로 갇혀 있다 · ⓑ 접지 칸 월드 고정 · ⓓ `air` 가 «위로 뜬 양» 이다.
- **`tools/probe163c.js` 신설(9항목)** — 위 넷을 **시간축에서** 잰다. 별도 도구인 이유는 캡처가 못 보는 자리라서다.
  ⚠ **반드시 «부팅하지 않는» 페이지에서 재야 한다.** `file://` 로 그냥 열면 아틀라스가 ~450ms 에 다 와서
  `ldRun` 이 **104ms 로 압축**되고, 그 시계로 위상을 찍으면 엉뚱한 칸을 잰다 — 이 도구가 처음에 그렇게 틀렸다
  (`cap163.js` 의 held 페이지와 같은 png 라우팅 지연으로 고쳤다).
  ⚠ 두 번째 함정: 등장 길이를 `x === 0` 으로 역산하면 **반올림 때문에 `ldRun` 보다 8ms 일찍 0** 이 되어
  «정지 자세» 판정이 한 칸 어긋난다(실제로 [A] 가 그 이유로 한 번 빨갛게 나왔다). → `LD.runMs()` 를 신설해 **제품에게 묻는다.**

### 캡처 하네스 — 정지 표본 신설 (두 비평가 공통 요구)

`OFF` 에 **620ms**(도착 +60ms, 선 자세 idle)를 넣어 9장이 됐다. 10회차의 최대 결함이 통째로 숨어 있던 자리이고,
첫 접속에서는 아틀라스 8개 중 6개가 등장 종료 뒤에 도착하므로 **실제로 가장 오래 보이는 화면**이다.

### 11회차 인계 — 다음 세션이 할 일

- **r11 캡처 9장은 이미 있다(채점 전).** `docs/shots/163-r11-{bg,1..9}.png` ·
  위상은 45 · 65 · 120 · 265 · 380 · 466 · 500 · **620(정지, 신설)** · 전환 합성.
  브리핑은 10회차 것을 그대로 쓰되 머리말만 «11회차 변경 4건(정지 보정 끄기 · 접지 칸 월드 고정 ·
  스쿼시 양자화 일원화 · air 부호)» 으로 갈고, **8번이 선 자세라는 것을 반드시 적어라** — 이번에 그 자리를 열었다.
- **아직 안 고친 2인 일치분(우선순위 순):**
  ⓐ **마지막 도약의 보폭 −33%**(9회차 O·P 2인 일치, 10회차에는 재지적 없음).
     이번 회차에 `LD_CRZ` 후보표를 실제로 만들어 봤다(`node tools/tune163e.js`) — 두 사람의 처방
     «감속 시작을 착지 뒤로 미뤄라» 는 **닫힌 해가 없다**(CRZ 를 올리면 착지 시각도 같이 밀려서
     `decelStart ≥ tLand` 는 CRZ→1 에서만 성립 = 감속 자체가 사라진다). 대신 **연속 트레이드오프**다:
     CRZ .75 → .84 면 보폭비 66.7% → **78.7%**, 정착 미끄럼 31.8 → **20.3px** 로 **둘 다** 좋아지고,
     대가는 케이던스 20.0 → 19.0fps 와 제동 거리 133 → 81px(한 보폭의 1.40배 → **0.85배**)다.
     ⚠ 게이트 §4 의 «감속 구간에 이동의 12% 이상» 은 CRZ .75 시절의 절대값이라 .82 부터 빨개진다.
     **재기준할 때 «감속 구간 평균 속도 ÷ 등속» 은 쓰지 마라 — 속도 연속 조건 때문에 구조적으로 항상 정확히 50%다**
     (이번에 확인했다). 쓸 만한 자는 **«제동 거리 ÷ 한 보폭»**(«한 걸음 안에 서지는 않는다»)이다.
  ⓑ **좌변 잘림 61%**(9회차 O·P · 10회차 R 이 «t=342 에 100% 노출, 첫 342ms 가 잘려 있다» 로 재확인).
     R 은 `LD_P0` 를 2 → 3 으로 한 칸 더 옮기는 처방을 냈는데, **f3 우변이 870 이라 «t=0 에 잉크 프레임 밖»
     (게이트 §4)의 여유가 12px 밖에 안 남는다** — 9회차가 f2(858, 여유 75px)를 고른 이유가 그것이다. 먼저 재고 고를 것.
  ⓒ **핸드오프 휘도 낙차 12.9~13배** — 6회차부터 «아트·전환 설계 영역».
- **새로 등재된 «아트 대기» 2건(Q·R 공통)**: ① **착지·제동 전용 포즈가 없다** — run 8장이 전부 정속 스트라이드라
  정착 구간 68.3ms(등장의 12.2%)를 `f4` 한 장으로 버티며 32px 미끄러지고, 도착에서 −162px 발 점프가 난다.
  «착지 플랜트 1장 + 정지 전이 1~2장» 이 있어야 코드로 회수된다. ② **착지 프레임 접지 폭이 26px 뿐**
  (표본7 실측, 같은 순간 그림자 코어는 528px = 20.3배) — 발바닥이 넓은 착지 프레임이 필요하다.
