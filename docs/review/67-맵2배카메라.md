# 67 — 맵 가로·세로 2배 + 카메라 무빙 연출

> 세션 `sess-0213-13454` (워커 A, 2026-08-25). 지시서 [3]-**(가)** 수치 검증 + 연출은 [3]-**(다)** 연속 프레임 채점.
> 레퍼런스 스크린샷이 **없는** 거동·연출 작업이라 `docs/measure/` 측정표는 만들지 않는다 —
> 정답은 PROGRESS 67행의 «설계 확정(저장소 주인 지시)» 다섯 항목이고, 그것을 수치 게이트로 옮긴 것이 `tools/verify67.js` 다.

## 0. 한 줄 요약

`WORLD` 960×1536 → **1920×3072**(칸 20×32 → 40×64), 카메라를 `step()` 밖으로 꺼내 **감쇠 추적 + 리드 + 경계 클램프**로 다시 쓰고,
**보스 스폰 팬·줌인 / 처치 줌인·슬로모·플래시** 를 얹었다. 적 스폰은 «월드 전역 랜덤» → **플레이어 반경 300~700px 링**.

## 1. 바꾼 곳 (index.html, 8 구간)

| # | 구간 | 바뀐 것 |
|---|---|---|
| 1 | `WORLD` / `cam` | `20*T × 32*T` → **`40*T × 64*T`**(1920×3072). `cam` 에 `z`(줌) · `lx/ly`(리드) 추가 |
| 2 | `buildFloor()` | 얼룩 개수 상수 70 → **`70 × (cols*rows)/(20*32)`**(=280). 안 고치면 4배 넓은 바닥의 장식 밀도가 1/4 이 된다 |
| 3 | `makeEnemy()` | 전역 랜덤 + «250px 이내면 재추첨» → **`SPAWN_RMIN 300 ~ SPAWN_RMAX 700` 링**(각도 균등). 링이 월드 밖이면 재추첨(최대 40회) 후 clamp |
| 4 | `step()` 카메라 블록 | **삭제** → `camUpdate()` 로 이전. `step` 에 남은 건 `msgT` 감소뿐 |
| 5 | 신설 «67 카메라» 절 | `CAM_K/CAM_LEAD/CAM_LEADK` · `CINE_*` 상수 · `cine` 상태 · `camDamp/camClamp/camSm/cineWeight/camUpdate` · `cineBossIn/cineBossKill/cineReset/camTimeScale/cineBusy` |
| 6 | `draw()` | `ctx.scale(z,z)` 추가(줌) · 오프셋 식을 `-cam + VW/(2z)` 로 · 셰이크 `/z` · **ox/oy 2차 클램프**(셰이크가 경계에서 검은 띠를 드러내지 않게) · 화면 밖 적 화살표 좌표에 `*z` · **처치 플래시**(캔버스 위 흰 사각) |
| 7 | 트리거 | 스폰 큐가 `boss` 를 꺼내면 `cineBossIn()`(레이드 샌드백 제외) · `killEnemy()` 보스 분기에서 `cineBossKill()` · `spawnStage()`/`startRaid()` 에서 `cineReset()` + 카메라 스냅을 `camClamp` 경유 |
| 8 | `loop()` · 스테이지 클리어 · `fxWorld()` | `step(dt * camTimeScale())` + `camUpdate(dt)`(실시간) · 스테이지 클리어 조건에 **`!cineBusy()`** · `fxWorld` 에 줌 반영 |

### 왜 카메라가 `step()` 밖인가
① 처치 슬로모는 **게임 시간만** 늦춰야 한다. 카메라·연출 시간까지 `dt*0.3` 을 먹으면 «0.3초 슬로모» 가 실제로는 1초가 된다.
② 플레이어가 움직인 **뒤** 좌표를 따라가야 한 프레임 늦게 붙지 않는다.

### 왜 «대상 바꿔치기» 가 아니라 «가중치 blend» 인가
연출 대상을 `cam` 목표에 그냥 꽂으면, 그 프레임에 카메라 속도가 `0 → 0.12×거리` 로 **한 프레임 만에** 뛴다(저크).
`cineWeight()` 가 **smoothstep**(시작·끝 기울기 0)으로 «플레이어 ↔ 연출 대상» 을 섞으므로 팬의 시작·끝 가속이 0 이다.
실측 저크(프레임 간 속도 변화) — 일반 전투 **1.13px/f²** · 스폰 연출 **0.80px/f²** · 처치 연출 **1.76px/f²**(상한 5).

### 처치 연출이 «한 프레임도 안 보이던» 문제 (이번 회차 최대 결함)
보스가 죽으면 그 프레임에 `enemies.length === 0` 이 되고, **다음 프레임에 곧바로 스테이지 클리어 → `spawnStage()` → `cineReset()`** 이 돌아
줌·슬로모·플래시가 통째로 지워졌다(1회차 게이트에서 `zMax 1.000 · 슬로모 1프레임 · 플래시 0프레임` 으로 잡혔다).
→ 스테이지 클리어 조건에 **`!cineBusy()`**(처치 연출 0.9초 동안만 true)를 넣어 «연출이 끝난 뒤 다음 스테이지» 로 만들었다.

## 2. 상수표

| 상수 | 값 | 뜻 |
|---|---|---|
| `WORLD.w/h` | 1920 / 3072 | 40×64 칸 (T=48) |
| `SPAWN_RMIN/RMAX` | 300 / 700 | 스폰 링 |
| `CAM_K` | 0.12 | 감쇠 추적 — 60fps 프레임당 목표까지 좁히는 비율 |
| `CAM_LEAD` / `CAM_LEADK` | 90px / 0.05 | 리드 최대 선행 거리 / 리드 자체의 추적 비율 |
| `CINE_ZIN` / `CINE_ZKILL` | 1.15 / 1.30 | 스폰 팬 줌 / 처치 줌 |
| `CINE_IN_S` / `CINE_HOLD_S` / `CINE_BACK_S` | 0.80 / 0.35 / 0.60 | 팬·줌인 / 유지 / 복귀 (합 1.75초) |
| `CINE_TS` / `CINE_SLOW_S` | 0.30 / 0.30 | 처치 슬로모 배속 / 길이(실시간) |
| `CINE_KILL_S` | 0.20 | 처치 줌인이 차오르는 시간. 0.14 로 두면 그 램프 자체가 **6.6px/f² 저크**로 잡힌다 |

프레임률 독립: `camDamp(k,dt) = 1-(1-k)^(dt*60)`. 구현 이전의 `dt*6` 식 **선형** 계수는 프레임이 튀면(dt 0.1 캡) 한 번에 60% 를 좁혀 그대로 저크가 됐다.

## 3. 검증 — `node tools/verify67.js` → **VERIFY67 PASS (27/27)**

```
[1] WORLD 1920 / 3072 · 바닥 타일맵 1920×3072
[3] 저크 1.133px/f² (<5) · 월드 밖 검은 영역 노출 0프레임(최대 0.00px) · 평상시 z=1
[3b] 리드 최대 89.5px(≤90) · 리드 방향 = 이동 방향 646/647
[2] 스폰 300표본 전부 300~700px (최소 301 · 최대 698 · 중앙 500)
[4] in>hold>back>종료 · 줌 1.150 · 카메라–보스 중심 3px · 복귀 57px·z1.000 · 길이 1.75초 · 저크 0.777
[5] 처치 줌 1.300 · 슬로모 18프레임(=0.3초) · 이후 정상배속 182프레임 · 플래시 최대 0.96·25프레임(≤0.42초) · 줌 복원 1.000 · 저크 1.756
[6] 42 조이스틱 · 59 AI 상수(260,2.6,0.9,0.6) · 28 BOSS_SEC 30 · SC2/VW540 · 콘솔 에러 0
```

`node tools/smoke.js` → **SMOKE PASS** (화면비 6종 프레임·시트 잘림 0, 팝업 전수 오픈, NaN 0).

### 59 구석·벽 체류 재측정 (새 맵 크기 기준 — 지시 이행)

`node tools/verify59.js` (5분 시뮬 ×2 시나리오) → **V59 PASS**

| 빌드 | stage | 표본 | 구석<200 | 벽<150 | 사망/분 | 중심거리 평균 |
|---|---|---|---|---|---|---|
| 67 적용 | 1 | 3000 | **0.0%** | 4.0% | 0.00 | 578px |
| 67 적용 | 30 | 3000 | **0.0%** | **0.0%** | 14.40 | 294px |

59 통과선(구석 <10% · 벽 <25%)을 새 맵에서도 그대로 만족한다. 오히려 좋아졌는데, 이유는 두 가지다 —
① 맵이 4배 넓어져 `AI_WALL_M 260` 이 «월드 대비» 더 좁은 띠가 됐고 ② **스폰 링** 때문에 적이 항상 플레이어 주위에 나와
«멀리 있는 적을 쫓아 벽까지 가는» 경로가 사라졌다. stage 30 사망 14.4/분은 59 세션 실측(13~15/분)과 같은 구간이다 = 회귀 아님.

## 4. 연출 채점 — 지시서 [3]-(다)

캡처: `node tools/cap67.js` → `docs/review/67-in-*.png`(팬·줌인 100ms×8) · `67-back-*.png`(복귀 100ms×8) · `67-kill-*.png`(처치 ~90ms×8).
가상 시계라 **틱 n번 = n/60초** 로 프레임 간격이 정확하고 회차 간 재현된다.

| 프레임 | mode | t(s) | zoom | flash | cam | player |
|---|---|---|---|---|---|---|
| A1 | in | 0.017 | 1.000 | 0 | 958,1539 | 958,1538 |
| A4 | in | 0.317 | 1.052 | 0 | 1071,1562 | 959,1538 |
| A8 | in | 0.717 | 1.145 | 0 | 1451,1634 | 995,1546 |
| B1 | back | 0.000 | 1.150 | 0 | 1526,1638 | 1044,1555 |
| B4 | back | 0.300 | 1.075 | 0 | 1403,1611 | 1078,1560 |
| B7 | – | 0.600 | 1.000 | 0 | 1202,1579 | 1112,1565 |
| C1 | kill | 0.000 | 1.000 | 1.00 | 1194,1577 | 1135,1568 |
| C3 | kill | 0.183 | 1.294 | 0.56 | 1369,1570 | 1138,1569 |
| C6 | kill | 0.450 | 1.253 | 0.00 | 1486,1561 | 1129,1567 |

(전체 24프레임 표는 `tools/cap67.js` 실행 출력에 그대로 나온다)

## 5. 아트 필요

이 작업은 **아트 자산을 새로 요구하지 않는다** — 바닥 타일·장식은 기존 `tileImg` 를 새 크기만큼 더 깐 것이고,
연출은 전부 카메라 변환 + 흰 사각 1장이다. 다만 넓어진 맵에서 «빈 바닥» 이 길어 보이므로,
나중에 **장식 오브젝트(바위·기둥·풀 스프라이트)** 가 들어오면 `buildFloor()` 의 얼룩 루프 자리에 같은 밀도 규칙으로 얹으면 된다.

## 6. 남긴 것 / 다음 세션에

1. **스테이지 시작 스냅은 «의도된 컷»이다** — `spawnStage()` 가 플레이어를 월드 중앙으로 옮기고 카메라를 그 자리에 꽂는다(바닥도 다시 만든다). 게이트는 그 3프레임을 저크 판정에서 제외한다. 부드럽게 이어붙이고 싶으면 그건 별도 작업이다.
2. **`cine.tg` 는 보스 «객체» 를 잡는다**(움직이는 대상을 따라간다). 처치 연출만 좌표 스냅샷을 쓴다 — 시체는 안 움직이므로.
3. **줌은 `cam.z` 하나로만 들어간다** — DOM(60 쥬시 모듈) 쪽에는 손대지 않았다. 화면 플래시도 60 의 `.flash` 가 아니라 캔버스에 그린다(구간 충돌 회피).
4. 58 이 쓰는 `fxWorld()` 에 줌 항을 넣었다(`(x-cam.x)*z + VW/2`). 58 세션이 이 함수를 만지면 이 항이 살아 있는지 확인할 것.
