# 41 — 03·14 팝업 내장 재화 바 (작업 기록)

- 세션: `sess-2113-22980` (워커 A) · 2026-08-24 21:13 UTC ~
- 지시: UI-REFERENCE 3번·14번 «설계 확정(2026-08-25, 작업 41)» —
  «상단 골드·다이아는 메인 HUD(A3) 재사용이 아니라 이 팝업 **자체에 포함된** 재화 바다.
  원본도 그렇고 형태가 메인 HUD 와 **다르다** — 레퍼런스에서 별도 실측해 팝업 내부 요소로 구현하고,
  재화 변동 시 팝업 내 표시도 갱신되게 연결.»
- 티어: **T2 기능 TODO(주인 지시)** → «기능 완성 규칙» 적용(실제 게임 데이터로 동작 + 기능 체크 표 필수).
- 측정표: `docs/measure/41-팝업내장재화바.md` (신규)
- 스크립트: `cap41.js`(캡처+DOM 실측) · `scan41.py`(ref 대조) · `verify41.js`(기능+회귀) · `pxdiff41.py`

---

## 0. 먼저 확인한 것 — «정말 메인 HUD 와 다른가»

지시서가 «형태가 다르다» 고만 적혀 있어 **먼저 픽셀로 확인**했다. 결론: 다르다(측정표 §0).

| | 02 메인 HUD | 03·14 팝업 재화 바 |
|---|---|---|
| 프로필 플레이트 | 있음(x0~468) | **없음** — x0~496 전 구간 전이점 0개 |
| 본체 톤 | 단색 `#42362A` | **하드 2단** `#42362A`(ref y93~143) + `#3A2E22`(147~183) |
| 알약 | 211×48, gap 49 | **254×49**, 좌단 x503/x803(피치 300) |
| 알약 배경 | `#221B13` 단색 | `#231A13` → `#1D160E` 2단 |
| 하단 검정 테두리 | 4px | **6px** |

그리고 **03 과 14 의 바는 서로 픽셀 동일**(y80~200 에서 차이 픽셀 0, 숫자 문자열 제외)이라
**공용 컴포넌트 `.pcb` 1개**를 두 페이지가 같이 쓴다.

### 구조 결정 — 왜 page-local `top:-104px` 인가

레퍼런스에서 이 바는 화면 **최상단 스트립**(ref y87~191 = 프레임 3~107)을 차지한다.
그런데 `#dunw`·`#relicw` 는 `top:104px` 로 잡혀 있고, 그 좌표계 위에 **기존 측정값이 전부 얹혀 있다**.
페이지 top 을 0 으로 내리면 03·14 의 모든 자식 좌표를 다시 써야 한다(회귀 위험).
→ 페이지는 그대로 두고 **바만 `top:-104px; height:108px`** 으로 위로 올려 프레임 0~107 을 덮었다.
아래 4px 은 레퍼런스의 «바 하단 검정 테두리 6px»(ref 186~191) 중 페이지 영역으로 넘어오는 몫이다.

`#relicw` 는 `overflow:hidden` 이라 이 바가 통째로 잘렸다 → **클립을 `#relicBody`(inset:0, 같은 박스)로 이관**했다.
`#dunw` 는 원래 `overflow` 지정이 없어 그대로다.

---

## 1. 1회차 — 신설 + 레퍼런스 대조

`.pcb`(바) / `.pcb-p`(알약) / `.pcb-g`·`.pcb-d`(골드·다이아) / `>i`(아이콘) / `>b`(숫자) 신설.

`node cap41.js dun r1` → `python3 scan41.py` 로 **레퍼런스와 같은 문자열**(`40.77A` / `1,300`)을
넣어 캡처하고(04 교훈 1 — 상태가 다르면 그 회차 대조는 무효) 잉크 bbox 를 대조했다.

| 항목 | ref(프레임) | 1회차 | 수정 후(2회차 캡처) |
|---|---|---|---|
| 골드 아이콘 | (500,27)–(556,83) 57×57 | 49×51, +3/+3 밀림 | **(500,28)–(555,83)** Δ≤1 |
| 다이아 아이콘 | (801,27)–(856,83) 56×57 | 49×49 | **(799,26)–(855,83)** Δ≤2 |
| 골드 숫자 | (593,43)–(712,67) w120 | w129 (+7.5%) | **(591,43)–(709,65)** Δ≤3 |
| 다이아 숫자 | (903,43)–(994,70) w92 | w97, 중심 +8.5 | **(906,43)–(995,70)** Δ≤3 |

수정 4건:
1. 골드 아이콘 `font-size:45.6 → 51px` + `scaleX(1.04)` — 이모지는 세로를 맞추면 가로가 모자란다.
2. 다이아 아이콘 `51.7 → 60px`(`scaleX(.86)` 유지).
3. 숫자 `scaleX 1.22 → 1.135` — 잉크 높이는 이미 ref 와 같았고(23px) **폭만** 7.5% 넘쳤다.
   28 교훈 1(숫자 잉크비 0.69)로 잡은 `font-size:33.3px` 은 건드리지 않았다.
4. 알약에 `padding-right:12px` — ref 의 숫자 중심이 «아이콘 우단~알약 우단» 정중앙보다
   골드 4px · 다이아 8px 왼쪽이었다. 12px 을 주면 둘 다 **Δ2** 로 수렴한다(측정표 §4).

바 배경 2단 톤 경계도 대조 — 프레임 y55~69 구간에서 ref 와 **Δ≤2**.

`scan41.py` → **SCAN41 PASS (0 FAIL)** — 03 페이지·14 페이지 **둘 다** 같은 결과.

---

## 2. 2회차 — 기능 체크 · 회귀

`node verify41.js` → **VERIFY41 PASS (28/28)**.

### 기능 체크 표 (T2 «기능 완성 규칙», 2026-08-25)

| # | 무엇을 | 확인 방법 | 결과 |
|---|---|---|---|
| 1 | 바가 실제로 렌더되는가 | `getComputedStyle` + `getBoundingClientRect` (57 교훈 2 — «안 보이니까 없는 것»이 아니다) | 03·14 둘 다 OK |
| 2 | 상단 HUD 스트립을 덮는가 | 바 rect 가 `#top` rect 를 세로·가로 전부 포함 | OK |
| 3 | 메인 HUD 프로필이 노출되는가 | `.prof` 중심점에 `elementFromPoint` → **`.pcb` 가 잡힘** | 노출 0 |
| 4 | 초기 표기 = 게임 데이터 | 바 텍스트 === `fmt(S.gold)` / `fmt(S.dia)` | OK |
| 5 | **재화 증가가 반영되는가** | 실제 게임 동작 `claimAllMail()` 호출 → 골드 0→35,000 · 다이아 +5.3K | 바 `35.0K` / `5.30K` 로 갱신 |
| 6 | 메인 HUD 표기와 일치 | 바 텍스트 === `#goldN` 텍스트 | OK(같은 소스) |
| 7 | **세이브 반영** | `localStorage[KEY].gold` === `S.gold` | OK |
| 8 | **감소 방향도 반영** | `S.gold` 를 절반으로 → 다음 프레임 | `17.4K` 로 갱신 |
| 9 | 콘솔 에러 | 03·14 각각 | 0건 |

> 갱신 경로는 `drawHud()` → `renderPcb()`. 바가 **페이지마다 하나씩(2개)** 이라 id 가 아니라 클래스로 돌린다.
> 값이 이전과 같으면 DOM 을 건드리지 않는다(매 프레임 도는 함수라 필요).

### 회귀

| 대상 | 결과 |
|---|---|
| 02 메인 요소 9종 좌표 | **Δ0** |
| 03 던전 요소 8종 좌표 | **Δ0** |
| 14 보물상자 요소 4종 좌표 | **Δ0** (`#relicw` overflow 이관에도 불구하고) |
| 02 메인 픽셀 | **Δ0** (`#slots` 쿨다운 애니 박스 제외) |
| `node tools/smoke.js` | **SMOKE PASS** |

> **함정(51 함정 3 의 재현)**: 픽셀 회귀에서 처음 1,586px 이 달랐는데, **같은 파일을 두 번 찍어도**
> 같은 자리가 달랐다 — `#nickN`(`'U_'+Date.now()`)과 `#slots` 쿨다운 링이었다.
> `nickN` 은 `textContent` 를 덮어써도 **다음 프레임에 `S.nick` 에서 되돌아온다** — «소스»(`S.nick`)를
> 고정해야 한다. `#slots` 는 rAF 애니라 대조 박스에서 제외했다. 그러고 나니 **정확히 0**.

---

## 3. 3회차 — 33(재화 정보 팝업) 병합 + 기준 화면비 변경 대응

- `33` 이 main 에 올라오면서 `drawHud()` 에서 충돌 → 지시서 [4] 대로 **양쪽 모두 살렸다**
  (`if(curInfoKey) renderCurInfo();` + `renderPcb();`).
- 33 의 규약은 «재화 아이콘에 `data-cur` 속성 하나 → 위임 핸들러» 다. 33 이 올라왔으므로
  `.pcb-g`·`.pcb-d` 에도 `data-cur="gold"/"dia"` 를 달았다 —
  **03·14 두 페이지 × 아이콘 2개 = 4곳 전부에서 `#ciw` 가 실제로 열리는 것을 헤드리스로 확인**
  (`#ciw` z-index 41 > 페이지 28 이라 페이지 위에 정상적으로 뜬다).
- 다른 워커가 **기준 해상도를 9:16(1080×1920) → 9:19(1080×2280)** 으로 바꿨다(`1ec8379`).
  세로 변환이 «ref y − 84» 로 단일화됐는데 **이 작업이 처음부터 쓰던 변환과 같다** — 재작업 없음.
  캡처만 2280 으로 전환했고, **바 영역(0~112)은 1920 캡처와 픽셀 차이 0** 이었다(바가 상단 고정이라 당연).

---

## 4. 4회차 — 비평 2인(A·B) 채점 → 지적 반영

**A: ①8 ②7 ③7 ④6 (⑤8 ⑥7) → 최저 6 · B: ①7 ②6 ③7 ④6 (⑤6 ⑥6) → 최저 6.**
둘 다 8 미만이라 수정 후 재채점. **두 비평가가 독립적으로 같은 1순위를 지목했다.**

### 결정적 지적 — «알약 내부 2톤 이음선이 바 배경의 이음선과 같은 y 다»

레퍼런스는 알약 **안에서도** 프레임 y**59.9** 에서 톤이 꺾인다. 바 배경의 꺾임(y59.75)과 **같은 그리드**다.
우리는 알약을 «알약 자체 비율(71%)» 로 꺾어서 y65 였다 — **Δ5.0px**.
자체 스캔으로 반증을 시도했지만 **확인됐다**(ref x=1035 열: fy59 까지 (35,26,19) → fy62 부터 (29,22,14)).

> **교훈**: 배경에 톤 밴드가 있는 화면에서, 그 위에 얹힌 요소의 내부 톤 분할은
> **요소의 비율이 아니라 «배경 밴드의 절대 y»** 를 따른다(아트가 밴드를 요소 위에 그대로 그린다).
> 요소별로 «예쁜 비율»(70%)을 잡으면 반드시 어긋난다.

### 수정 7건과 결과 (50% 교차점 기준 실측)

| # | 지적 | 수정 | 결과 |
|---|---|---|---|
| 1 | 알약 톤 이음선 Δ5.0px | 로컬 71% → **29.5px 고정**, 램프 4→2px | ref 59.87 / cap **60.02** (Δ0.15) |
| 2 | 알약 우단이 2px 짧다 | 좌단 503/803 → **505/805** (폭·피치 유지) | ref 758.39·1058.18 / cap **758.26·1058.26** (Δ≤0.13) |
| 3 | 아이콘 아트가 같이 밀리면 안 됨 | 아이콘 오프셋을 칸별로 분리(`-6px` / 다이아 `-4px`) | 아트 좌단 ref 500·801 = cap **500·801** |
| 4 | 아이콘 검정 컨투어가 절반 두께 + **하단 그림자 누락** | 링 2.2→3px + «아래가 두꺼운» 비대칭 그림자(하 5.5px) | ref 어두운 행 24~25/85~89, cap **25~26/84~88** (Δ1) |
| 5 | 바 상단 검정 스트로크 두께 | 스톱을 0.5px 밀어 픽셀 중심에 정렬 | ref y3~7 **5px** = cap y3~7 **5px** |
| 6 | 바 아래 4px 내부 그림자 누락 | `.pcb::after` 신설(`rgba(0,0,0,.18)` → 투명) | ref (31,27,24)…(35,30,26) / cap (32,28,23)…(37,32,27) |
| 7 | 숫자 잉크 높이 −1px | fs 33.3→34.5 + scaleX 1.135→1.096(**폭 유지**), 스트로크 9→8px | 잉크 h 23 → **24** = ref |

> **채택 안 한 지적 2건(반증)**: ⓐ «텍스트 외곽선 두께» — B 는 «우리가 +51% 과다», A 는 «양쪽 동등» 으로
> **정반대**였다. 16 교훈 4(«비평가마다 반대로 나오면 임계값 문제 — Δ≤2px 는 JPEG 노이즈») 그대로라
> 중간값만 반영(9→8px)했다. ⓑ «숫자 캡하이트» 도 A 24 / B 25 로 갈렸다 — A 쪽(24)만 맞췄다.
> ⓒ «글리프 간 간격·획 두께» 는 서체 차이(⑥)라 통과 판정 대상이 아니다.

`SCAN41 PASS` (4항목 Δ≤3) · `VERIFY41 PASS (38/38)` · `SMOKE PASS`

---

## 5. 5회차 — 비평 2인(C·D) 재채점 → 반영

**C: ①9 ②9 ③8 ④8 → 최저 8(통과) · D: ①8 ②7 ③7 ④8 → 최저 7.**
**둘 다 «누락 레이어 없음»** 으로 판정했다 — 4회차에서 되살린 레이어(아이콘 하단 그림자·바 아래 그림자·
알약 톤 이음선)가 차분에서 전부 소거됐다는 뜻이다. D 가 8 미만이라 한 회 더 돌았다.

| # | 지적(C·D 공통 또는 자체 스캔으로 확인된 것) | 수정 | 결과 |
|---|---|---|---|
| 1 | 숫자 베이스라인이 ref 보다 1.06px 위 | `.pcb-p>b{position:relative;top:1px}` | 잉크 y43~66 (ref 43~67) |
| 2 | 골드 숫자 블록이 2.8px 왼쪽 | `padding-right` 16→14.5 | 잉크 좌단 592(ref 593), 우단 710(ref 712) |
| 3 | 코인 아트가 ref 보다 3% 작다 | `font-size` 51→52.5px | 잉크 bbox **(500,27)–(556,84)** vs ref (500,27)–(556,83) — **3변 Δ0** |
| 4 | (3의 부작용) 이모지를 키우니 잉크가 위로 자람 | `top` −4 → −2.5px | 세로 중심 복원 |
| 5 | 젬 실루엣 폭만 3.3px 좁다 | `scaleX(.86)` 이 **검정 링까지** 가로로 줄인다 → x 오프셋을 `/0.86` 로 보정 | 잉크 bbox **Δ0 (4변 전부)** |
| 6 | 젬 아이콘이 1.2px 위 | `top` −4 → −3px | 두 아이콘 세로 중심 일치 |

> **채택 안 한 지적**: D 의 «다이아 숫자 폰트 +3.2%» 와 C 의 «글리프 잉크폭 +13%» 는 둘 다
> **총 문자열 폭은 이미 맞는데 글리프 하나하나의 종횡비가 다르다**는 지적이다 — 서체 차이(⑥)라
> 통과 판정 대상이 아니고, 폰트를 키우면 맞아 있던 골드 폭(Δ0.4)이 깨진다.
> 그리고 «두 알약의 텍스트 오프셋» 은 **레퍼런스 자체가 비대칭**이다(알약 좌단 기준 +147.8 / +144.1).
> 한 규칙으로 둘 다 맞출 수 없어 **평균값으로 균등 분배**했다(최대 오차 2.1px → 노이즈 범위).

### 이 회차에서 잡은 **하네스 버그 2개** (채점 자체를 무효로 만들던 것)

1. **캡처 스크립트가 채점용 문자열을 잃고 있었다.** `cap41.js` 는 레퍼런스와 같은 문자열
   (`40.77A`/`1,300`)을 DOM 에 주입하고 찍는데, `renderPcb()` 는 **값이 바뀔 때만** DOM 을 건드린다.
   전투가 골드를 버는 순간 라이브 값(`969`)으로 덮어써서 **캡처에 엉뚱한 문자열이 찍혔다**.
   → 렌더 루프(`requestAnimationFrame`)를 **먼저 세우고** 주입하고, 주입이 안 붙으면
   스크립트가 **스스로 오류를 내고 종료**하게 고쳤다. (04 교훈 1 «캡처 상태가 다르면 그 회차 비평은 무효» 의 자동화판)
2. **픽셀 회귀가 비결정적이었고, 기준본에 남의 변경이 섞였다.**
   ⓐ 기준본을 «옛 커밋» 으로 잡으니 그 사이 다른 워커가 넣은 변경(작업 63 탭바 테두리 제거)이
   41 의 회귀로 잡혔다 → 기준본을 **«현재 파일에서 `.pcb` 마크업 2개만 제거한 것»** 으로 바꿔
   41 단독 영향만 남게 했다. ⓑ 살아 있는 게임을 두 번 찍으면 골드·쿨다운이 매번 달라 4회 중 1회 실패했다
   → 픽셀 회귀 캡처에서는 **rAF 를 세워** 두 캡처가 같은 초기 프레임에서 멈추게 했다(3/3 재현).

`SCAN41 PASS`(아이콘 Δ≤1 · 숫자 Δ≤3) · `VERIFY41 PASS (39/39)` · `SMOKE PASS`

---

## 6. 남긴 것 / 다음 세션에 넘기는 것

1. **14 레퍼런스 바 좌측의 정체불명 아이콘**(x14~83, y102~172, 70×71, 크림/살구색 3분절).
   03 에는 없고 14 에만 있다. **재화 바가 아니라서 41 범위 밖**으로 두었다 — 측정표 §5 에 실측을 남겼다.
   14 를 다시 잡는 세션(또는 별도 작업 단위)이 처리할 것.
2. ~~작업 33 과의 연결~~ → **완료(3회차).** 33 이 올라온 뒤 `data-cur` 를 달아 4곳 전부 동작 확인.
3. **아트 필요 표**는 측정표 §6 에 있다(골드 57×57 · 다이아 56×57 · 14 좌측 아이콘 70×71).
