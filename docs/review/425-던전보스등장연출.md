# 425 — 던전 보스 «등장 연출» · 제한 시간은 «전투 시작» 부터

> **저장소 주인 지시(2026-08-30)** — «던전은 보스 등장하면 등장하고 나서 전투 시작할때부터 시간 흘러야함.
> 등장할때는 보스 잠깐 비췃다가 플레이어쪽으로 카메라 다시 하고 전투 시작해야함».
>
> 세션 `sess-0537-19157` 워커 B · 2026-08-30 · **제품 `index.html` 5자리**(상수 4줄 · 런 상태 1줄 ·
> `dunBossTick` 1자리 · `step` 던전 분기 1자리 · `camUpdate` 1자리) · 레이아웃 **Δ0px** · 밸런스 계수 **0줄**.

---

## §1 재현 — 등재문의 가설이 **그대로 확인됐다** (338 규칙)

처방을 따르기 전에 `tools/probe425.js` 를 신설해 던전 8종 + 탑 2종을 **실제 진입점**으로 굴렸다.
338·341 은 등재문 가설이 재현으로 기각된 자리였지만, 여기서는 등재문이 한 글자도 안 틀렸다.

**수리 전** (`node tools/probe425.js` · `DUN_SEC` 15s · `DUN_BOSS_DLY` 1.4s)

| id | 보스가 «필드에 선» 시각 | 그때 남은 `dunRun.t` | 샌 양 | HUD 표기 | 감소 시작 | cam↔player 최대 | cam↔boss 최소 |
|---|---|---|---|---|---|---|---|
| gold | 1.400s | 13.600 | **1.400s** | 13.6 | **0.017s** | 17.0 | 6.6 |
| dia | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 16.9 | 55.3 |
| relic1 | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 17.0 | 47.1 |
| relic2 | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 17.0 | 32.7 |
| relic3 | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 17.2 | 118.2 |
| relic4 | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 17.0 | 38.4 |
| stone | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 16.9 | 96.4 |
| rstone | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 16.7 | 69.3 |
| tower(탑) | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 16.6 | 74.7 |
| despair(탑) | 1.400s | 13.600 | **1.400s** | 13.6 | 0.017s | 16.9 | 37.2 |

· **제한 시간이 새는 던전 10 / 10.** 입장 **첫 프레임(0.017s)** 부터 `dunRun.t` 가 깎이고, 보스가 서는
  순간에는 이미 **1.400s = `DUN_SEC` 의 9.33%** 가 사라져 있다. HUD 도 그 값을 그대로 보여 준다(13.6).
  때릴 대상이 하나도 없는 구간에서 시간이 흐르는 것이 결손 자체였다.
· **카메라 왕복 0회.** cam↔player 최대가 **17px**(감쇠 추적의 정상 지연)이라 보스를 **한 번도 안 비춘다** —
  108 이 67 연출을 폐기한 뒤 `camUpdate` 의 목표가 «언제나 플레이어» 한 줄이기 때문이다(등재문 그대로).

---

## §2 처방 — 국면 하나, 팬 하나

### 2-1 상수 셋 (`DUN_PHASE_DLY` 바로 아래)

```
DUN_INTRO_PAN  = 0.35   플레이어 → 보스 편도 팬
DUN_INTRO_STAY = 0.70   보스에 머묾  («잠깐 비췄다가»)
DUN_INTRO_BACK = 0.35   보스 → 플레이어 편도 팬
dunIntroLen() = 1.40s
```

세 상수를 **전부 0 으로 두면 국면이 아예 안 열린다** — 그 자리가 `verify425` §R 되돌림 시험이다.

### 2-2 시간·전투 — 332 와 같은 «배타적 상태», 타이머 체인 금지

**국면은 `step()` 맨 위에서 그 프레임을 통째로 가져간다**(2회차 · 비평 반영 §6-1①):
`introT` 를 굴리고 **스프라이트 애니메이션만** 돌린 뒤 `return` 한다 — 이동·공격·투사체·피해·부활·재생·스킬이
한 줄도 안 돈다. 그래서 «시계는 멈췄는데 전투는 진행» 이 **구조적으로** 불가능하다.
⚠ 애니메이션을 같이 세우지 않은 이유: 1.4초가 정지 화면이 되면 «비춘다» 가 «멎었다» 로 읽힌다.
도는 것은 프레임 번호뿐이고 **좌표·판정은 0줄**이다(`verify425` [Z-a]~[Z-d] 가 그것을 각각 묻는다).

| 상태 | `dunRun.t` | 액터 | 근거 |
|---|---|---|---|
| `introOn` (425 등장 국면) | **안 깎인다** | **전부 정지**(그림만 돈다) | 주인 지시 «전투 시작» |
| `bossDown` (332 격파 시퀀스) | 안 깎인다 | 돈다 | 332 그대로 |
| `fight` (전투) | 깎인다 | 돈다 | — |
| 그 밖(= 보스가 아직 안 선 스폰 딜레이 1.4s) | **안 깎인다** | 돈다 | 때릴 대상이 없다 |

⚑ **타이머 체인이 아니라 런에 얹은 상태 하나**라(332 선례) «연출 도중에 시간이 다 돼 실패로 끝난다» 는
경합이 **구조적으로** 없다. 실제로 `verify255` [E-0]·`verify264` [E-425] 가 그것을 음성항으로 못박는다 —
국면 중에 `t = 0.005` 로 두고 60프레임을 흘려도 런이 안 끝나고 t 는 **한 프레임도 안 깎인다**.

### 2-3 카메라 — 왕복 한 번, `cam` 필드는 그대로

`dunIntroW()` 가 `0 → 1 → 0`(easeInOutQuad) 가중치를 내고 `camUpdate` 가 그만큼 섞는다.

⚠ **섞은 값을 `cam` 에 되먹이면 카메라가 돌아오지 못한다.** 다음 프레임의 감쇠가 «보스 근처» 에서
출발해 다시 보스로 섞이기 때문에 w 가 0.5 로 내려가도 카메라는 보스 옆에 붙어 제자리걸음을 한다
(1회차 설계에서 실제로 그렇게 됐다). 그래서 **«플레이어 추적점» 을 `dunRun.introC` 에서 따로 굴리고**
`cam` 에는 섞은 결과만 쓴다 ⇒ **w 가 0 인 프레임의 좌표는 국면이 없었을 때의 추적 좌표와 완전히 같다**
= 전투 시작에 이음매가 없다(f7 → f8 실측 10.6px → 2.4px, 감쇠 지연 그 자체).

⚑ **비추는 자리는 발밑 앵커가 아니라 «몸통 중심»** 이다(2회차 · §6-1②). 적 좌표 `e.y` 는 지면 앵커라
그대로 가운데 놓으면 그림이 위로 밀린다 — 두 비평가가 독립으로 **100px / 97.5px 위**로 같은 값을 쟀다.
새 상수를 만들지 않고 이미 있는 반지름 `e.r` 을 쓴다(LESSONS 90-① — 같은 값을 두 곳에 적지 않는다):
실측 스프라이트 높이가 앵커 위 `1.96·r` 이라 그 절반 `0.98·r ≈ r` 이 곧 몸통 중심이고, r=90 이면 두 실측 사이에 든다.

⚑ 국면 상태를 `cam` 이 아니라 **런에 얹은** 이유는 둘이다 — ① 108 게이트가 `cam` 필드를
`{shake,x,y,z}` 로 못박아 두었고(`verify108` · `verify425` [B-f]), ② 런이 사라지면(포기·실패·클리어)
국면도 같이 사라져야 «연출만 남아 도는» 경합이 원천적으로 없다.

### 2-4 «둘째부터는 안 비춘다» 의 근거

국면은 `dunBossTick` 이 `r.bossIn` 을 처음 세우는 프레임에 딱 한 번 열린다. `bossIn` 은 한 번 서면
내려가지 않으므로 **페이즈 던전의 2·3번째 보스, «한꺼번에» 던전의 둘째부터는 왕복이 0회**다.
판단 근거: 페이즈 사이는 이미 보스 국면 **안**이고(257 이 그래서 `DUN_PHASE_DLY` 를 0.5 로 줄였다),
둘째부터 또 비추면 «전투가 끊긴다» — 주인 지시의 «등장» 은 런에 한 번뿐인 사건이다.
실측(`verify425` [D]): relic2 2번째 보스 구간 국면 **0프레임 · w 최대 0 · 그 구간 t 는 정상적으로 흐른다**.

### 2-5 안 한 것 — 108 을 되살리지 않았다

주인 지시가 108 보다 우선하는 **의도적 이탈**이지만 범위는 «던전 런의 첫 등장» 뿐이다.
`cam.z`(줌)·`cam.lx/ly`(리드)·`cam.bx/by`(빈사 편향)·`camTimeScale`(슬로모)·플래시는 **한 줄도 안 살렸고**,
`verify425` [G] 가 `verify108` 과 같은 정규식으로 그것을 매 실행 확인한다(폐기 식별자 0건 · `CAM_*` 상수 = `CAM_K` 하나).
28 스테이지 보스·46 레이드·승급전은 `dunRun` 이 없어 가중치가 **언제나 0** 이고 카메라–플레이어 거리가
108 그대로 **≤ 17.2px** 다([C] 3종).

**전투는 안 멈췄다.** 28 보스전 선례(`startBoss` 의 스폰 딜레이는 전투를 멈추지 않는다)와 맞췄고,
주인 지시의 본체는 «시간이 새지 않는다» 이지 «전투 정지» 가 아니다. 그래서 국면 동안 보스는 걸어오고
(f1 cam↔boss 319.7 → f7 140.6) 전투가 시작될 때 이미 사거리 안이다 — 국면이 «비어 있는 1.4초» 가 아니다.

---

## §3 수리 후 실측

`node tools/probe425.js`

| 축 | 수리 전 | 수리 후 |
|---|---|---|
| 제한 시간이 새는 던전 | **10 / 10** (각 1.400s) | **0 / 10** (각 0.000s) |
| 보스가 선 프레임의 HUD | 13.6 | **15.0** |
| t 가 처음 줄어드는 시각 | 0.017s(입장 첫 프레임) | **2.817s** (= 스폰 1.400 + 국면 1.400 + 1프레임) |
| 카메라가 플레이어에게서 떨어지는 최대 | 16.6 ~ 17.2px | **294.1 ~ 323.8px** |
| 등장 국면 가중치 최대 w | 함수 없음(0) | **1.000** |
| 페이즈 2번째 보스 왕복 | — | **0회** (t 는 정상적으로 흐른다) |

### 3-1 연속 프레임 8장 — **2회차**(`node tools/cap425.js` · `docs/review/425-frames.json`)

화면 중앙은 **(270, 570)** 이다. `u` = 국면 시작부터의 경과. 화면 좌표는 **발밑 앵커** 기준이다.

| 프레임 | u | w | HUD | cam↔player | cam↔boss(앵커) | 플레이어 화면 | 보스 화면(발밑) |
|---|---|---|---|---|---|---|---|
| f1 | 0.00s | 0.000 | **15.0** | 0.0 | 304.3 | (270.0, 570.0) | (510.1, 757.0) |
| f2 | 0.18s | 0.546 | **15.0** | 141.5 | 172.7 | (138.8, 517.0) | (378.9, 704.0) |
| f3 | 0.35s | **1.000** | **15.0** | 258.9 | **90.0** | (30.0, 473.0) | **(270.0, 660.0)** |
| f4 | 0.70s | 1.000 | **15.0** | **258.9** | **90.0** | **(30.0, 473.0)** | **(270.0, 660.0)** |
| f5 | 1.05s | 1.000 | **15.0** | **258.9** | **90.0** | **(30.0, 473.0)** | **(270.0, 660.0)** |
| f6 | 1.22s | 0.546 | **15.0** | 141.5 | 172.7 | (138.8, 517.0) | (378.9, 704.0) |
| f7 | 1.40s | 0.000 | **15.0** | **1.1** | 303.3 | (269.0, 569.6) | (509.1, 756.6) |
| f8 | 2.00s | 0.000 | **14.4** | 12.3 | 254.5 | (279.6, 577.7) | (470.8, 726.3) |

· **보스 «그림» 이 화면 정중앙에 선다** — 발밑 앵커가 (270.0, **660.0**) = 몸통 중심(앵커 − 90)이 **(270, 570)**,
  화면 중앙과 오차 **0.0px**. 1회차는 앵커를 중앙에 놓아 그림이 97.5~100px 위로 밀려 있었다(§6-1②).
· **머묾 0.70s 동안 아무것도 안 움직인다** — f3·f4·f5 의 cam↔player·양쪽 화면 좌표가 **완전히 동일**(Δ0.0px).
  1회차에는 같은 구간에서 플레이어–보스 간격이 149.9px(48.6%) 좁혀졌다(§6-1①).
· **왕복이 대칭이다** — f2 와 f6 의 값이 **한 자리까지 같다**(w 0.546 · cam↔player 141.5 · 화면 좌표 동일).
  1회차에는 편도 319.7px / 복귀 158.3px 로 **2.02배** 어긋나 있었다(액터가 움직였기 때문).
· **국면이 끝나면 플레이어로 돌아온다** — f7 에서 cam↔player **1.1px**(1회차 10.6px). 화면 좌표 (269.0, 569.6)
  = 중앙에서 (−1.0, −0.4)px.
· **HUD 는 f1~f7 내내 15.0** 이고 f8 에서 처음 14.4 로 움직인다 = «시간이 안 흐른다» 를 화면이 증명한다.
· 이징 확인 — 0.18s 에 w **0.546**, 대칭 지점 1.22s 에도 **0.546**(easeInOutQuad 이론값 0.5449·0.545, Δ≤0.0012).

---

## §4 T2 기능 체크 표 (기능 완성 규칙 · 헤드리스 실측)

| 조작 | 무엇이 바뀌는가 | 확인 |
|---|---|---|
| 03 던전 카드 [도전] → `challengeDungeon` | 런이 서고 **시계가 15.0 에 멈춘 채** 보스 스폰을 기다린다 | `verify30` «425 — 등장 국면 전에는 시계가 15.0 에 멈춰 있다» |
| 보스가 필드에 선다 | 카메라가 보스로 팬 → 0.70s 머묾 → 플레이어 복귀. 그 동안 시계 불변 | `verify425` [A][B] · `cap425` f1~f7 |
| 그 1.40s 동안 | **아무도 안 움직인다** — 이동·공격·투사체·피해·재생 정지, 그림(스프라이트 프레임)만 돈다 | `verify425` [Z-a]~[Z-d] · `cap425` f3=f4=f5 Δ0.0px |
| 국면이 끝난다 | **다음 프레임부터** 시계가 흐른다(전투 시작) | `verify425` [A-f] `fFightFirst = fIntroEnd + 1` |
| 시간 초과 | 실패 통보 + 층 유지. 단 **보스가 선 뒤에만** 일어난다 | `verify30` [G] · `verify255` [E] · `verify264` [E-425] |
| 보스 격파 | 332 시퀀스 그대로(터짐 → «클리어» → 1초 → 완료 화면) | `verify332` 80/80 무수정 |
| 국면 중 보스 격파(펫·지속 피해) | 국면을 그 자리에서 닫고 격파 시퀀스로 — 카메라가 시체를 계속 비추지 않는다 | `step` 던전 분기 `if(dunRun.introOn){ … fight = true }` |
| ◀ 나가기 | 종전대로 즉시 중단(국면 중에도) — 런이 사라지면 국면도 같이 사라진다 | 국면 상태가 `dunRun` 에 얹혀 있다(§2-3) |
| 26 페이즈 던전 2·3번째 보스 | 왕복 없음 · 시계 정상 진행 | `verify425` [D] |
| 28 보스 · 46 레이드 · 승급전 | **무영향**(가중치 0 · 카메라 108 그대로) | `verify425` [C] |

---

## §5 게이트

| 게이트 | 결과 | 비고 |
|---|---|---|
| `tools/verify425.js` | **56/56 PASS 신설** | A 시간 · B 카메라 왕복 · **Z 정지(2회차 신설 4항)** · C 무영향 3종 · D 페이즈 · E HUD · F 전 10종 · G 108 이관 · R 되돌림 · H 콘솔 |
| `tools/probe425.js` | 신설 | 재현기(수리 전/후 같은 표를 낸다) |
| `tools/cap425.js` | 신설 | 연속 프레임 8장 + 계측 표(ROUTINE [3]-(다)) |
| `tools/verify255.js` | 140 → **143/143** | **이관** — [E-0] 신설 |
| `tools/verify264.js` | 85 → **88/88** | **이관** — [E-425] 신설 |
| `tools/verify30.js` | 49 → **50/50** | **이관** — «425 — 등장 국면 전에는 시계가 멈춰 있다» 신설 |
| `tools/verify257.js` | **110/110** | **이관** — [6-R] R2 의 «40프레임 굴리면 위치가 해소된다» 를 국면 **뒤로** 옮겼다(국면 안이면 아무도 안 움직여 영영 12%) |
| `verify331` 79/79 · `verify338` 179/179 · `verify108` 23/23 · `verify332` 80/80 | 무수정 초록 | 이관 목록 |
| `verify284` 59/59 · `verify285` 34/34 · `verify339` 113/113 · `verify209` 61/61 · `verify178` 75/75 · `verify243` 40/40 · `verify273` 32/32 · `verify123` 62/62 | 무수정 초록 | 던전·탑 계열 전수 |
| `node tools/smoke.js` | **SMOKE PASS** | |
| `node tools/verifyProgress.js` | 종료 코드 0 | |

### 5-1 이관을 «항을 눌러» 하지 않았다 (LESSONS 328)

세 게이트가 빨개진 이유는 하나다 — **«던전의 시간은 언제나 흐른다» 는 전제**가 425 로 깨졌고,
그 전제 위에 `dunRun.t = 0.005` 한 줄로 시간 초과를 만드는 표본이 셋 있었다.
표본을 «전투 시작 뒤» 로 **옮기기만** 했으면 «425 가 통째로 사라져도 초록인 게이트» 가 된다.
그래서 옮긴 자리마다 **바뀐 것을 묻는 항을 새로 세웠다**:

* `verify255` **[E-0]** — 국면 전/중에는 `t = 0.005` 여도 런이 안 끝나고 t 가 한 프레임도 안 깎인다(음성항).
* `verify264` **[E-425]** — 같은 음성항 + «표본이 실제로 보스 미등장이다(스폰 딜레이 **안쪽 60프레임**)».
* `verify30` — «등장 국면 전에는 시계가 15.0 에 멈춰 있다».
* `verify257` **[6-R] R2** — 2회차의 «액터 정지» 로 **네 번째 자리**가 나왔다(«40프레임 굴리면 위치가 해소된다»
  는 40프레임이 국면 안에 들어가면 참일 수 없다). 여기도 표본을 국면 뒤로 옮겼고, «정지» 자체는
  `verify425` **§Z** 가 네 항(좌표 Δ0 · 보스 체력 불변 · 플레이어 체력 불변 · **그래도 애니메이션은 돈다**)으로 묻는다.

### 5-2 «보스 미등장» 은 이제 **방어 분기**다

`finishDunRun` 의 실패 통보는 `r.bossIn ? '보스 체력 n% 남음' : '보스 미등장'` 인데,
425 이후 **시간 초과로는 «미등장» 갈래에 닿을 수 없다**(시간이 흐르려면 `fight` 가 서야 하고,
`fight` 가 서려면 `bossIn` 이 먼저다). 지우지 않은 이유는 «어떤 경로로든 보스 전에 실패로 끝나면»
이 문장이 여전히 참이기 때문이다 — 폐지된 조건의 눈금(LESSONS 295-②)이 아니라 **살아 있는 갈래**다.
`verify264` [4] 가 그 갈래를 `endDunRun(false)` 로 직접 태워 «미등장» 문구를 실제로 받아 확인한다.
⚠ 1차 실측에서 90프레임을 흘렸더니 스폰 딜레이(84프레임)를 넘겨 보스가 서서 «체력 100% 남음» 이 떴다 —
표본을 **60프레임**으로 못박았다(표는 그 자리를 [E-425] 로 묻는다).

---

## §6 비평 회차 (ROUTINE [3]-(다) · 연출 축 4개 · 최저 8 통과)

### 6-1 · 1회차 — **CQ ①8 ②7 ③7 ④3 (최저 3)** · **CR ①7 ②8 ③7 ④4 (최저 4)** — 미달

두 사람이 **2인 독립으로 같은 것을 1순위**로 짚었고, 그것이 이 작업의 진짜 결손이었다.

**① 두 사람의 1순위 = «시계만 멈추고 전투는 돌았다»**
· CQ: «국면 1.400s 중 **0.700s(50%)** 가 시계는 멈췄는데 전투는 진행. f5(intro:true)에 보스 체력 **300 → 280(−6.7%)**,
  피해 수치 «19» 와 피격 링까지 그려진다» (①−2 · ④−2)
· CR: «u=1.05 에 보스 HP 300 → 280. 반대 방향도 성립한다 — 전투 0.60s 시점에 플레이어가 이미 «부활 중…» 이다.
  **보스의 치명타가 시간이 안 흐르는 창에서 들어갔다**» (④−4 · 1순위)
· CR ①−2: «머묾 0.70s 동안 플레이어–보스 간격이 **149.9px(48.6%)** 좁혀진다 — «잠깐 비춘다» 가 아니라
  «달려오는 보스를 따라 본다» 로 읽힌다»
⇒ **채택.** 1회차는 «시간이 새지 않는다» 만 구현했는데, 주인 원문의 «**전투 시작**» 은 시계 한 줄이 아니라
  국면 자체가 «전투 전» 이어야 한다는 말이다. 국면을 `step()` 맨 위의 **배타적 상태**로 올렸다(§2-2).

**② 두 사람이 독립으로 같은 값을 잰 ③ — 보스 그림 중심이 화면 중앙보다 위**
· CQ «bbox 중심이 중앙선보다 **100px 위**(화면 높이의 8.8%)» · CR «**97.5px 위**(8.6%)»
⇒ **채택.** 카메라가 맞추던 것이 그림이 아니라 **발밑 앵커**였다. 목표를 `e.y − e.r`(몸통 중심)로 옮겼다(§2-3).

**③ f1 전면 암전 — 제품이 아니라 «내 캡처» 결함이었다**
· CQ ④−3 «필드 100% 평면 암전 · 파일 **86.6KB** vs 나머지 475~501KB» ← 이 한 항이 CQ 의 점수를 3 으로 눌렀다
· CR 은 같은 것을 보고 **직접 재현해 원인까지 갈랐다**: «같은 상태에서 `step()` 없이 `draw(); drawHud();` 를
  한 번 더 부르면 필드가 정상 렌더된다(평균 rgb(106.8,80.9,110.0)). **제품 결함이 아니라 `tools/cap425.js` 버그**»
⇒ **확인 후 캡처를 고쳤다.** 뿌리는 던전 입장이 `#app.dunrun` 으로 레이아웃을 바꾸고 그 뒤 ResizeObserver → `fit()`
  이 전장 캔버스를 **다시 잡으면서 지우는 것**이고, 그 리사이즈가 `evaluate` 가 끝난 **다음 태스크**에 돌기 때문에
  바로 찍으면 «칠해지기 전» 이 찍힌다. 찍기 직전에 `draw(); drawHud();` 를 한 번 더 부른다(상태는 안 움직인다).
  ⚑ **한 사람만 봤으면 «제품이 검다» 로 굳었을 자리다** — 2인 독립 채점이 실제로 값을 한 것이 여기다.

**④ 안 고친 지적 — 남의 구간이라 등재도 안 했다(이미 있는 자리)**
두 사람이 각각 짚은 «적 머리 위 체력바가 HUD 를 파고든다»(CQ: f7 에서 간격 0px · CR: 바 8행 중 **6행(75%)** 가림)는
`e.y - e.r*3.1 - 6` 이라는 **모든 적 공용** 앵커의 문제이고, 425 가 만든 것이 아니다(적이 화면 위쪽에 오면 언제나 그렇다).
**내 작업 단위가 아니라 안 건드렸다.** 425 의 카메라는 오히려 그 상황을 **덜** 만든다 — 몸통 중심을 맞추면서
보스가 화면 위로 몰리지 않게 됐다(2회차 f7 `bScr.y` 756.6, 1회차 437.2).

**⑤ 반영 안 한 제안 — 근거를 적어 둔다**
· CQ «`DUN_INTRO_LEAD = 0.08` 을 새로 두고 그 동안 w=0» — **f1 암전이 캡처 버그였으므로 전제가 사라졌다.**
  상수를 하나 더 두면 «셋을 0 으로 두면 국면이 사라진다» 는 되돌림 시험의 계약도 넷으로 늘어난다.
· CQ·CR «확대(줌)가 0% 라 «비춘다» 가 약하다»(②) — **줌은 108 이 폐기한 축이고 되살리지 말라고 등재문이 못박았다.**
  주인 지시의 이탈 범위는 «팬 하나» 다. ②는 두 사람 다 7·8 로 통과선 근처이고, 통과를 막는 축도 아니었다.

---

## §7 밸런스 — 계수 0줄, 다만 «판당 실제 시간» 이 늘었다

`DUN_SEC`·`DUN_DMG_K`·`DUN_BOSS_HPK`·`DUN_BOSS_DLY` 를 **한 줄도 안 건드렸다.**
바뀐 것은 «15초가 언제부터 세느냐» 하나다 — 수리 전에는 입장 순간부터라 **때릴 수 있는 시간이 13.6초**
(15 − 1.4)였고, 이제 온전한 **15.0초**다. 즉 판이 **+10.3% 쉬워지는 방향**이고 하드락은 생길 수 없다.

⚑ **199 가 볼 것(이관)**: 331 이 이미 «판당 필요 피해가 절반(최악 판 예산의 76% → 47%)» 으로 쉬워지는
방향을 넘겨 두었고, 425 가 그 위에 **유효 전투 시간 +1.4초(+10.3%)** 를 더한다. 난이도를 되돌릴 지렛대는
331 이 적어 둔 것과 같다(`DUN_BOSS_HPK` 한 줄 — 난이도 축이 전부 `need` 의 «몇 %» 라 나머지가 따라온다).
**수치 확정은 199 몫**이다(326·331 과 같은 처리).

---

## §8 곁다리 관측 — **이미 434 로 등재돼 있다**(중복 등재 안 함)

`tools/verify210.js` **1건 실패(92/93)** — «단련석은 던전 계열이 아니라 입장권을 안 만들었다(125 [H] **5종 유지**)».
430 이 입장권을 **8종**(이름대로 8색)으로 확정한 뒤에도 210 이 «5종» 상수를 그대로 물고 있다 = 게이트 부패.
**내 변경과 무관함을 대조 실행으로 확인했다**(338·344 규칙) — 수리 전 커밋 `612666c` 의 `index.html` 로
바꿔 끼워 돌려도 **같은 92/93 · 같은 항목**이다. 내 작업 단위가 아니라 안 고쳤다.
⚑ 등재하려고 번호를 세어 보니 **같은 관측이 이미 `434`(sess-0546-19844 워커 C)로 등재돼 있었다** —
같은 자리를 두 번 등재하면 두 워커가 같은 lock 을 놓고 다른 작업을 하게 된다(병렬 규칙 1 · 281·286 사고).
**중복 등재하지 않고 434 를 그대로 가리킨다.** 여기 남기는 것은 «425 의 변경과 무관하다» 는 대조 결과다.
