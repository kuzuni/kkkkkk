# 882 — 「씬 A 산포 ×2.9 = 가둠 상자 종횡비」 를 재현이 정정했다 (제품 `index.html` **0줄**)

> 등재(2026-09-03, 838 10회차 sess-1558-25897 워커 D) · **1회차 sess-1643-4572 루틴 워커 C**
> 지시서 [3]-**(가)** — 레퍼런스 대조가 아니라 «자로 재는» 작업이라 비평가 없음.

---

## §0 한 줄

등재문이 남긴 지렛대 **ⓐ(버튼 세로 106 → 155 로 종횡비를 ×2.0 으로)** 를 **실제로 만들어 재 봤고,
산포가 되레 커졌다**(중앙값 ×2.80 → **×3.00**). ⇒ **상자를 안 바꾼다**(위임 규약 채택). 대신
① 스윕 표와 ② «실제 지렛대는 「53」 **구멍**» 이라는 정정, ③ 다음 세션이 같은 실험을 처음부터
다시 하지 않게 **기하 래칫 `verify882`** 를 남긴다. ⓑ 의 본체는 **883** 으로 등재했다.

---

## §1 재현 먼저 (338 규칙) — `tools/probe882.js` 신설 **15/15 PASS**

자는 838 이 아홉 회차 쓴 `tools/travel838.js` **한 벌 그대로**다(402 «두 벌 금지»).
변형판은 `index.html` 사본에 **카드 높이 한 값**만 바꿔 만든다 — `.cb` 가 `top:396px;bottom:8px` 이라
카드가 커지면 **버튼이 아래로** 그만큼 커진다(위쪽 잉크 넷은 Δ0).

### [1] 세로 스윕 — 등재문의 예측과 **반대로** 간다

| 버튼 세로 | 상자 종횡비 | 산포(끝반경 max÷min) 시드 3장 | **중앙값** | 빈 각 | 몸길이 |
|---|---|---|---|---|---|
| **106(현행)** | ×2.92 | 2.90 / 2.69 / 2.80 | **×2.80** | 105.5° | 1.97 |
| 118 | ×2.63 | 2.36 / 2.71 / 2.41 | **×2.41** | 120.2° | 2.16 |
| 130 | ×2.38 | 2.08 / 3.05 / 2.65 | **×2.65** | 120.3° | 2.12 |
| 142 | ×2.18 | 2.62 / 2.99 / 3.06 | **×2.99** | 120.3° | 2.04 |
| **155(등재문 처방)** | **×2.00** | 3.00 / 3.21 / 2.91 | **×3.00** | 120.3° | 1.94 |
| 167 | ×1.86 | 3.09 (시드 1) | — | 135.1° | 1.96 |
| **ⓑ 상한(구멍 없음 · 106)** | ×2.92 | 2.25 / 3.94 / 2.40 | **×2.40** | **80.5°** | **2.33** |

⚑ **정정 ① — «산포 = bw/bh» 는 항등식이 아니다.** 등재문은 «106 → 155 면 ×2.93 → ×2.0» 이라고 적었는데
실측은 **×3.00**(중앙값)으로 **현행보다 나쁘다**. 종횡비를 ×2.0 으로 만들어도 산포는 ×2.0 이 되지 않는다 —
산포의 분자(최장 반경 ≈100px)는 **폭**이 정하고 분모(최단 반경 34~48px)는 «발원 원반 + 「53」 구멍» 이
정하기 때문이다. 세로를 키우면 위·아래 방이 깊어져 **분자 쪽이 같이 커지고**(155 판 최장 102.5),
분모는 구멍이 잡고 있어 안 따라온다.

⚑ **정정 ② — 실제 지렛대는 구멍이다.** 상자를 **한 픽셀도 안 바꾸고** 「53」 keep-hole 만 걷으면
산포 중앙값 ×2.80 → **×2.40** · 빈 각 105.5° → **80.5°** · 몸길이 1.97 → **2.33** 로 **셋이 같이** 좋아진다.
등재문의 «산포는 곡선도 씨앗 각도 아니고 상자다» 는 **절반만** 맞았다.

⚑ **정정 ③ — 이 축은 시드마다 ±0.5 로 흔들린다(최대 ×3.94).** 10알 표본이라 한 알이 얕은 방을 고르면
비가 통째로 움직인다. 등재문의 «×2.90 / ×2.94 / ×2.7 셋이 맞췄다» 는 **한 시드 한 판을 세 사람이 잰 것**이라
서로 독립이 아니다. ⇒ **스윕 최저(버튼 118 · ×2.41)의 이득도 시드 산포 안**이다
(106 [2.69..2.90] ↔ 118 [2.36..2.71] 이 겹친다 · `probe882` [1-e]). 872·873 이 겪은 그 함정이다.

### [2] 세로 예산 — 지렛대 ⓐ 가 쓸 수 있었던 자리

| 프레임 | 박스 | 카드 | 203 서브탭 바 상변 | **빈 세로** |
|---|---|---|---|---|
| 9:19 (2280) | 1093 | 373..883 (h510) | 953 | **70px** |
| 9:13.3 (1600) | 1093 | 373..883 (h510) | 953 | **70px** |

두 프레임이 같다(686 이 단련에서 확인한 «행 기하 동일» 과 같은 꼴). 즉 **자리는 실재했다** —
ⓐ 를 기각한 이유는 «자리가 없어서» 가 아니라 **«그렇게 해도 안 낫기 때문»** 이다.

### [3] 대가 — 키웠다면 무엇이 어긋났나 (스윕 최저 판 = 카드 522 · 버튼 118)

| 요소 | 현행 local | 변형 local | Δ |
|---|---|---|---|
| `.ch` 헤더 · `.ci` 아이콘 · `.cv` +값 · `.cn` 라벨 | 8..97 / 128..261 / 288..348 / 342..390 | 같음 | **0** |
| `.cb` 하단 바 | 396..502 | 396..514 | 상변 0 · **높이 +12** |
| 밴드 안 코인 · 비용 잉크 | y413.4 / y417 | y419.4 / y423 | **+6 씩** |
| 카드 높이 | 510 | 522 | **+2.35%**(레퍼런스 510) |

⚠ **이탈의 본체는 마지막 두 줄이다.** 밴드 안 잉크는 «가운데 정렬» 이라 밴드가 커지면 **따라 내려간다**
(레퍼런스 측정표 23 §6: 골드 잉크 local y424..468). 버튼 155 판이면 **+24.5px** 이다.
23 은 26회차를 들여 ①~④ 8 로 통과한 화면이고, 그 통과의 근거가 이 좌표들이다.

---

## §2 판정 — **ⓒ 채택(상자를 안 바꾼다)** · 위임 규약(2026-09-01) 표식

| 안 | 얻는 것 | 대가 | 판정 |
|---|---|---|---|
| ⓐ 버튼 세로 확대(155) | **없다 — 산포 ×2.80 → ×3.00 으로 악화** | 카드 +9.6% · 밴드 잉크 +24.5px(레퍼런스 이탈) | **기각(자가 기각)** |
| ⓐ′ 스윕 최저(118) | 산포 ×2.80 → ×2.41 **이지만 시드 산포 안** | 카드 +2.35% · 밴드 잉크 +6px | **기각** — 재현 안 되는 이득에 통과한 화면의 좌표를 판다 |
| ⓑ 「53」 을 버튼 밖으로 | 산포 ×2.40 · 빈각 80.5° · 몸길이 2.33 (**셋 다 개선**) | 가격 잉크가 덮인다(816 실측 최대 93.6%) — **지불 방법을 따로 정해야 한다** | **883 으로 등재**(이 행의 지시가 «상자» 라서) |
| **ⓒ 아무것도 안 한다(채택)** | 23 의 통과 좌표 보존 · 제품 0줄 | 838 ③ 은 씬 A 에서 6~7 로 멎는다(등재문이 예고한 값) | ✅ |

**되돌림 방법** — 주인이 «그래도 키워라» 로 뒤집으면 `index.html` 두 값이면 된다:
`.tr-cards{height:510px}` 와 `.tr-card{height:510px}` 를 같은 수로 올린다(버튼은 `bottom:8px` 이라 따라온다).
그때 빨개지는 자는 **`verify882` [1-b][1-c][2-a][3-a] 넷**이고, 그 넷이 곧 이 판정의 기록이다.
⚠ 올리기 전에 위 [1] 표를 다시 읽을 것 — **키운다고 산포가 좋아지지 않는다.**

---

## §3 게이트

### 신설 — `tools/verify882.js` **13/13 PASS**
«안 한 것» 을 지키는 래칫이다. [1] 카드 326×510 · 바 310×106 @ y396(두 프레임) · [2] 밴드 안 잉크가
밴드 가운데(중심 449) · [3] 카드 바닥 ↔ 서브탭 바 **빈 세로 70px** · **[R] 되돌림 시험 5항**
(카드 534 사본을 실제로 만들어 [1][2][3] 이 빨개지는 것을 확인 · 바 **상변**만은 그 사본에서도 안 움직인다).

⚠ **일부러 버스트를 안 굴린다.** 산포 축은 시드마다 ±0.5 로 흔들려(위 [1] 정정 ③) 문턱으로 쓰면
574·709·825·854·855·870·871·872·873 계열의 «플레이키 자» 를 하나 더 만든다. 흔들리는 축은
재현자(`probe882`)가 **기록으로만** 남기고, 게이트는 결정적인 기하만 지킨다.

### 신설 — `tools/probe882.js` **15/15 PASS**
수리 전·후가 같은 트리라(제품 0줄) 항상 초록이다 — 재현자를 «수리 전에만 초록» 으로 두면 다음 세션이
그 빨강을 게이트 부패로 읽는다(686 이 남긴 규약 · 702·730·732 가 그 자리였다).

### 자 손잡이 1자리 — `tools/travel838.js`
`runScene(scene, src, {seed})` **한 줄**을 열었다(기본값 = 종전 `SEED` 라 838·873 의 기준값은 한 자리도 안 움직인다).
한 시드의 제비뽑기를 구조로 읽지 않으려면 같은 스윕을 여러 시드로 돌려야 하는데, 그 손잡이가 없었다.
회귀로 못박았다 — `verify838` **19/19** · `verify873` **7/7** · `probe838` **5/5**.

### 회귀 (제품 0줄이므로 전부 무수정 초록)
`verify818` 41/41 · `verify816` 27/27 · `verify621` 42/42 · `verify686` 32/32 · `verify64` 44/44 ·
`verify203` 91/91 · `verify210` 97/97 · `verify271` 65/65 · **SMOKE PASS**.

---

## §4 838 에게 (2차 라운드가 이 행을 읽는다)

- **③ 의 지렛대는 이 행이 아니라 883 이다.** 상자를 키우는 길은 닫혔다(자가 기각) — 다시 파지 마라.
- 883 이 열려면 «가격 잉크를 어떻게 지킬 것인가» 를 먼저 답해야 한다. 883 에 지불 방법 둘을 적어 두었고,
  그중 **구멍을 «요소 상자» 가 아니라 «잉크» 로 좁히는 길은 레퍼런스를 한 픽셀도 안 건드린다**.
- **산포로 회차를 판정하지 마라** — 시드 3장 이상의 중앙값으로 읽어야 한다(`travel838` 의 `{seed}` 손잡이).
  838 7·8·10회차가 «넣어 보고 기각» 한 판들도 단일 시드였다는 점을 같이 볼 것.
