# 749 — `probe493b` 즉사 · `probe486` §D 실패 (게이트 부패 2건)

- **세션** sess-1813-30551 (루틴 워커 A) · **1회차** · 2026-09-01
- **분류** T1 «버그(게이트 부패)» · 지시서 [3]-**(가)**(기계 + 자 · 비평가 없음)
- **제품 `index.html` 0줄** — 등재문의 예상 그대로였다. 만진 파일은 `tools/probe493b.js` · `tools/probe486.js` 둘뿐.

## 1. 재현 (338 규칙 — 처방을 고르기 전에 제품에게 묻는다)

| 자 | 수리 전 | 수리 후 |
|---|---|---|
| `probe493b` | **즉사** — `page.evaluate: TypeError: Cannot read properties of undefined (reading 'getBoundingClientRect')` · [1]~[3] 통째로 안 돔 | **완주** (코드 0 · EVGUARD 삼킴 0건) |
| `probe486` | **11/12** — `§D ❌ evaluate 예외: ReferenceError: trDeltaTxt is not defined` | **13/13** (코드 0 · EVGUARD 삼킴 0건) |

둘 다 `EVGUARD=0` 으로도 같은 결과 — 731 차단기와 무관한 **수리 전부터의** 빨강임을 등재문대로 확인했다.

## 2. ① `probe493b` — 뿌리는 «패스 행 수가 줄었다» 가 아니라 **526 창 가상화**였다

등재문은 «패스 행 수가 그 뒤로 줄었다» 로 읽었다. 진단(`PASS_TABS.stage.n` 을 600 으로 올린 뒤 DOM 을 센다)이 그 읽기를 **정정**했다:

```
stage n = 600 · cols = 3        →  모델상 칸 1800개
renderPass() 뒤  행 15 · 칸 45
openPass('stage') 뒤 행 24 · 칸 72
```

행 수는 줄지 않았다(600 그대로). **526 이 «창 가상화» 를 올려** `renderPass()` 는 트랙만 세우고
`passFillRows()` 가 창만 채운다(`index.html` 36057 «행은 여기서 안 그린다 … 창이 행을 채운다»).
⇒ 하드코딩 인덱스가 통째로 창 밖이라 `undefined.getBoundingClientRect()` 가
`page.evaluate` **밖으로** 나갔다(696 «빨간 죽음» 꼴 — 삼킴이 아니다).

⚑ **인덱스는 한 자리가 아니라 두 자리였다.** 등재문은 `[900]`([1] 강제 레이아웃) 하나만 지목했는데,
[3] 에도 `.ps-bx[1500]` 이 있다(83행). **900 만 고쳤으면 빨간 죽음이 그 줄로 옮겨 앉았을 뿐**이다
— 즉사가 [1] 에서 먼저 나 [3] 까지 도달한 적이 없어 등재 때 안 보였다.

**처방** — 인덱스를 넓히지 않는다(창 크기가 또 바뀌면 같은 병이다). **살아 있는 마지막 칸**을 잡고,
창이 비면 재는 시늉 대신 ⚠ 를 찍는다(헛초록 금지).

### 곁다리로 드러난 «거짓 그림» — [2] 는 고치지 않으면 거짓말을 한다

[2]는 «±30행만 남기면?» 을 `rows.slice(last-30, last+31)` 로 흉내 냈다. 526 이 ⓐ 를 **실제로 채택**한 뒤
그 산수는 뜻을 잃는다 — `querySelectorAll('.ps-r')[i]` 는 더는 «단계 i» 가 아니고(526 이 `passRowEl(i)`
주석에 못박아 뒀다), 창이 이미 15행뿐이라 slice 가 **빈 배열**을 낸다. 인덱스만 고치고 여기를 뒀으면
이 절은 «해금 육각 15 → **0** · 레드닷 15 → **0**» 이라는 **재앙처럼 보이는 거짓 그림**을 찍었을 것이다
(수리 중 실제로 한 번 찍혔다). ⇒ 흉내를 걷고 **채택된 창 자신**을 재고, «잃는 것» 은 DOM↔DOM 이 아니라
**모델 대비**로 적는다:

```
[2] 후보 ⓐ 창 가상화 — 526 이 이미 채택했다(모의 아님). 창 [0, 15) · 창 한 바퀴 다시 채우기 2.5ms · DOM 행 15 칸 45
    ⚠ 잃는 것(모델 대비): 해금 단계 300/600 중 DOM 해금 육각 15 · 받을 칸 300 중 DOM 레드닷 15
    ⇒ verify428 [D]·verify301 처럼 DOM 으로 세는 자는 창 밖을 못 본다 — `passRowEl(i)` 로 창을 옮겨서 집는다.
```

493 의 «채택 전 비교표» 는 이제 이 프로브가 아니라 그 회차 기록이 갖는다 — 머리 주석에 그렇게 적었다.

## 3. ② `probe486` §D — 707 처방 «입은 폐지됐지만 축은 살아 있다»

§D 는 `trDeltaTxt(card)` 를 불렀고 **660**(주인 지시 «훈련 숫자 플로터 폐지»)이 그 함수를 **선언째** 지웠다
(`index.html` 35210). 707 이 `verify628` 에서, 708 이 `verify486` 에서 이미 이관한 폐지인데 **이 프로브만 남아 있었다.**

486 의 뜻은 «플로터» 가 아니라 **«증가분과 총량은 서로 다른 두 축이고, 알약은 총량을 말한다»** 다.
⇒ 폐지된 입 대신 그 뜻을 물려받은 살아 있는 자리(`trainGainTxt` → `trainCardData().gain`)로 **방향만 뒤집었다.**

⚠ **등재문이 안 본 두 번째 낡음** — 옛 §D 의 기대값 `U.atk.val(l+n) − U.atk.val(l)` 은 **628 이전의 자**다.
628 이 «한 카드 안 두 자» 를 닫으면서 증가분에 배수(`mul = TRAIN_NOW/u.val`)를 곱했으므로, 입만 갈아 끼우고
기대값을 뒀으면 **초록이 안 났다**. ⇒ 실제 증분은 축과 **같은 식을 두 번 적지 않고**
알약이 쓰는 그 수(`TRAIN_NOW.atk()`)를 레벨 앞뒤로 직접 읽어 잰다(628 이 쓴 그 방법).

```
· 폐지된 입 trDeltaTxt = undefined (660) · 증가분 축 «+1.09A» · `.cv` «1.12A»
  · 알약의 자로 잰 실제 증분 +1.09A (x30) · 지금 최종값 1.12A
✅ 증가분 축(trainGainTxt)이 «증가분» 을 말한다 — 알약이 쓰는 자로 잰 실제 증분과 같다(628)
✅ 증가분 축이 «최종값» 을 띄우지 않는다 — 알약(.cv)과 분리돼 있다(486 의 뜻)
```

**겹쳐 세지 않았다(708 규약)** — «폐지된 입이 되살아나지 않는다» 는 `verify486` [F1]·`verify660` [D3] 몫이라
여기서는 `info` 로 적기만 하고 단언하지 않는다.

**헛초록이 아니다**: `.cv` 가 증가분으로 되돌아가면(486 되돌림) 둘째 항이, 축이 총량을 말하면 첫째 항이 즉시 빨개진다.

## 4. 게이트 결과

| 자 | 결과 |
|---|---|
| `probe493b` | **즉사 → 완주** (코드 0) |
| `probe486` | **11/12 → 13/13** |
| `verify486` | **79/79 PASS** (무수정) |
| `probe707` | **20/20 OK** (무수정) |
| `probe708` | **11/11 PASS** (무수정 · §5 참조) |
| `node tools/smoke.js` | **SMOKE PASS** |

## 5. ⚑ 곁다리 관측 — 얕은 클론에서 «git 이력을 읽는 자» 가 거짓 빨강을 낸다 (**756** 로 등재)

`probe708` 이 처음에 **3/4 FAIL** 로 나왔다: `fatal: invalid object name '4757c0f'` —
`[1]` 이 `git show 4757c0f:tools/verify486.js` 로 **수리 전 사본**을 꺼내는데 그 커밋이 클론에 없다.

338·344·372 규칙대로 «내 것인가» 를 먼저 갈랐다 — `git diff --name-only` 가
`tools/probe486.js` · `tools/probe493b.js` 둘뿐이고 `verify486.js` 는 **한 글자도 안 건드렸다**.
확인: 이 환경은 **얕은 클론**(`git rev-parse --is-shallow-repository` = `true`)이고,
`git fetch --unshallow` 뒤 같은 자가 **11/11 PASS**. ⇒ 제품·자 결함이 **아니고 환경**이다.

⚠ **다음 워커가 이걸 «게이트 부패» 로 오독하기 쉽다**(나도 직전까지 그랬다).
`tools/` 에서 git 이력을 읽는 자는 **11개**(그중 `git show` 가 9개: `probe377`·`probe539`·`probe716`·
`probe738`·`verify307`·`verify356`·`verify365`·`verify631`·`verifyProgress`)라 자리가 좁지 않다.
고정 SHA 를 꺼내는 자를 얕은 클론에서 돌리면 **거짓 빨강**이고, 처방은 `git fetch --unshallow`(또는 충분한 `--depth`)다.

## 6. 되돌림

`git checkout <sha>~1 -- tools/probe493b.js tools/probe486.js` 한 줄. 제품은 건드린 데가 없다.
