# 895 — 10 이용권 배지 «2000% / 가치» 잉크 두께

> 등재: 2026-09-04 (sess-2128-30139 루틴 워커 B — 885 4회차 이관)
> 티어: **T4 폴리시** · 회차 상한 10

## 1회차 (2026-09-05, sess-0843-5223 루틴 워커 C)

제품 `index.html` **3자리**(아랫줄 `font-size` · 아랫줄 `-webkit-text-stroke` · 아랫줄 `top`+짝인
`transform-origin`) · 자 **2벌 신설**(`tools/scan895.py` · `tools/verify895.js`) ·
자 **1벌 수리**(`tools/scan885c.py` — 각도 추정기) · 게이트 `verify833` **280/280**(4항 이관).

### ⓐ ⚑⚑ 등재문의 수를 재현하지 못했다 — 그리고 그것이 이 회차의 첫 결과다

338 규칙대로 처방 전에 자부터 세웠다(`tools/scan895.py`). 등재문의 수(윗줄 +10% · 아랫줄 +19% ·
**폭 −3.1%**)는 **회차 기록에 손으로 적힌 값**이었고 저장소에 재현할 자가 없었다.
같은 자로 ref 와 우리를 재니 **폭의 부호가 반대**로 나왔다:

| 축 (우리 px 환산 · ref×K=2.0628) | ref | 등재문의 우리 | **1회차 실측** |
|---|---|---|---|
| 노랑 AABB 폭 | 156.8 | 152.0 (**−3.1%**) | **160.0 (+2.1%)** |
| 두께 윗줄 | 31.6~32.3 | 35.2 (+10%) | 30.20 ↔ **32.07 (+6.2%)** |
| 두께 아랫줄 | 24.8~24.9 | 29.6 (+19%) | 23.26 ↔ **27.22 (+17.0%)** |

**뿌리는 오측이 아니라 시간이다** — 등재는 885 **4회차** 시점의 값이고, 그 뒤 **7회차가
`--bdg-ls` 를 2.9 → 5.2** 로 옮겼다(낱자 피치 축). 자간이 넉 칸에 +2.3px 씩 들어가 폭이
+9.2px 움직였고 그것이 −3.1% 를 +2.1% 로 넘겼다. ref 쪽 값(156.8×90.8)은 내 자가 **소수점까지
같이 재현**하므로 자끼리 어긋난 것이 아니다.
⇒ **등재문의 수는 «그때의 상태» 다.** 3회차 뒤 등재된 번호를 잡는 세션은 **재측정부터** 해야 한다.

### ⓑ ⚑⚑ 결손은 «두 줄 다 두껍다» 가 아니라 **«아랫줄 하나가 통째로 크다»** 였다

자에 축을 셋 세웠다 — 두께(베이스라인 **수직**) · 길이(베이스라인 **방향**) · 검정 획(잉크 밖으로
나온 몫). 회전이 섞이지 않게 셋을 **같은 θ 좌표계**에서 낸다(θ 는 입력이 아니라 추정 결과다).

| 축 | ref | 우리(수리 전) | Δ |
|---|---|---|---|
| 두께 **윗줄** | 30.20 | 32.07 | +6.2% |
| 두께 **아랫줄** | 23.26 | 27.22 | **+17.0%** |
| 길이 **윗줄** | 154.66 | 159.30 | +3.0% |
| 길이 **아랫줄** | 52.32 | 58.96 | **+12.7%** |
| 검정 획 윗줄 | 4.13 | 4.00 | −3.0% |
| 검정 획 **아랫줄** | 2.06 | 3.00 | **+45.4%** |

**아랫줄만 세 축이 같이 지목한다.** 그리고 세 축의 크기가 서로 맞는다 —
두께비로 36 → 30.8 · 길이비로 36 → 31.6 ⇒ **`font-size` 하나가 둘을 같이 닫는다.**
⇒ 이것은 «두껍다» 라는 서체 문제가 아니라 **크기 자체가 틀린 것**이었다.

⚑ **검정 획을 «먼저» 잰 것이 갈랐다.** 등재문이 «손잡이를 고르기 전에 획 두께를 같은 자로 먼저
재라» 고 경고해 뒀는데, 그 자가 낸 값이 **ref 는 아랫줄 획이 윗줄의 정확히 절반**(4.13 ↔ 2.06)인데
우리는 0.75 배(4.00 ↔ 3.00)라는 것이었다. 획은 `font-size` 를 안 따라가는 절대값이라
**fs 를 줄이면 오히려 상대적으로 더 두꺼워진다** — 획을 같이 안 내렸으면 한 축을 맞추고
다른 축을 더 틀리게 했을 것이다.

### ⓒ 수리 — 제품 3자리

1. **`.pvc>.bdg>b{font-size:36px → 31px}`** — 두께 −0.5% · 길이 −1.7%.
2. **`.pvc>.bdg>b{-webkit-text-stroke:7px → 5px}`** — 획 −3.1%(윗줄과 **같은** 잔차 = 두 줄이
   이제 한 자에 같은 값으로 읽힌다).
3. **`top:102.175 → 102.675`** + 짝인 **`#shopw .pvc>.bdg>b{transform-origin:… −5.175 → −5.675}`**
   — 글리프가 줄면 잉크가 line-box 안에서 뜨므로 상자를 0.5px 내려 그림을 되돌린다.
   `transform-origin` 은 **각 줄의 지역 좌표**라 같은 값만큼 따라와야 절대 회전 중심이 안 움직인다
   (강체 규약 `verify833` [1-m]·[1-n] — 한쪽만 고치면 강체가 깨진다).

**상자를 옮긴 것이 «덤» 이 아니라는 근거** (셋이 같이 붙는다):

| | 상자 38.8(안 옮김) | **상자 39.3(옮김)** | ref |
|---|---|---|---|
| 잉크 cen | 38.0 (−1.3%) | 38.9 (+1.2%) | 38.5 |
| 잉크 base | 34.4 (**−3.5%**) | 35.6 (**−0.4%**) | 35.7 |
| 빈 띠 | 8.00 (**−13.8%**) | 9.00 (**−3.0%**) | 9.28 |
| 노랑 AABB 높이 | 91.0 (+0.3%) | 92.0 (+1.4%) | 90.76 |

δ 를 0 · 0.5 · 0.75 로 훑어 |Δ%| 합이 최소인 자리를 골랐다(0 은 18.9 · **0.5 는 6.0** · 0.75 는 7.8).

### ⓓ ⚠ 윗줄은 손대지 않았다 — 그리고 그것이 답이다

윗줄 잔차(두께 **+6.0%** · 길이 +3.0%)에는 **손잡이가 없다.** 833 3회차가 윗줄 다섯 글리프를
열 덩이로 갈라 **글리프 폭 합이 이미 ref +1.5%** 임을 재 놓았고(부족분은 전부 «글자 사이» 였다 =
답은 `letter-spacing`), 그래서 «높이» 를 `font-size` 로 좇으면 **맞아 있던 폭이 −4%로 틀어진다**
(46 → 43.4 로 검산: 글리프 합 132 → 124.5 ↔ ref 130). `scaleY` 로 좇으면 `verify833` [1-h]
(«배율 둘 다 1»)를 되돌리고 검정 획을 같이 눌러 ⑤ 를 깎는다(833 [10-g] 가 치른 대가).

⇒ **«폭은 맞는데 높이가 6% 크다» 는 서체 축**이다(등재문 후보 ⓒ) — 지시서 [3] 이 감점에서 뺀 자리다.
`verify895` **[P2]** 가 그 잔차를 **양성으로 못박는다**(+3.5~8.5% 밖이면 빨강) — 다음 세션이
윗줄 fs 를 건드리면 이 항이 먼저 짖는다. **닫으려면 서체가 와야 한다.**

### ⓔ ⚑⚑ 곁다리 — `scan885c` 의 각도 추정기가 **달아났다**(자 수리 1건)

아랫줄을 줄여 빈 띠가 넓어지자 885 4회차의 자가 **−24.8°**(참값 +15.0°)에서 굳어
cen 을 **+46%**, base 를 +77% 로 읽었다. 뿌리는 목적함수다 — 정규화 Otsu η 는
«두 덩이 거리 ÷ 퍼짐» 이라 **한 덩이를 아무 데서나 갈라도 커질 수 있고**, 두 덩이 크기가
크게 갈리면 그 쪽이 이긴다(4회차가 «비정규화 between» 에서 겪은 것의 **한 겹 아래** 얼굴이다).
⇒ 목적함수를 **«잉크가 한 점도 없는 빈 띠의 두께»** 로 올렸다 — 두 줄이 실제로 갈리는 각에서만
빈 띠가 생기므로 가짜 분할이 **원리적으로** 못 이긴다(η 는 동점 깨기로 내렸다).
**ref 값은 이 교체로 한 칸도 안 변한다**(+15.0/+14.5° · cen 38.5 · base 35.7) — 대조로 확인했다.
⚠ 이 자를 고치지 않았으면 895 의 수리가 «cen 이 +46% 로 깨졌다» 는 **가짜 빨강**을 냈을 것이다.

### ⓕ 게이트

- **`verify895` 18/18 신설.** §전제 4 · §A 아랫줄 두께 3 · §B 검정 획 3 · §C 대가 3 · **§R 되돌림 5**.
  - **[R1]~[R3]** 옛 값(36 · 7px)을 도로 주입하면 세 축이 다시 +17.6% / +12.1% / +45.4% 로 빨개진다.
  - **[R4]** **획만** 되돌리면 §B 만 빨갛고 §A 는 초록 — 두 손잡이가 서로를 안 가린다
    (`paint-order:stroke fill` 이라 획은 노랑 채움을 안 건드린다).
  - **[R5]** 등재문 후보 **ⓑ(세로 배율)** 를 실제로 그려 봤다 — 종횡이 **0.885** 로 깨진다
    (수리본 1.011). **fs 를 고른 근거를 자가 들고 있다.**
  - ⚠ 주입은 `#shopw` 급 특이성으로 한다 — `.pvc>.bdg>b`(0,3,1)로 적으면 5926행 ID 급 규칙에 져서
    **한 픽셀도 안 먹고 시험이 조용히 초록**이 된다(885 4회차가 실제로 진 함정).
- **`verify833` 280/280 — 4항 이관**(333 처방, 지우지 않고 방향·값을 갈아 끼웠다):
  - **[1-j]** «fs 불변 46/48·36/40» → «윗줄 46/48 **불변**(380 함정은 윗줄 이야기다) · 아랫줄 **31**/40
    (895 실측 — 두께·길이 두 축이 같이 지목)». **폭을 좇은 것이 아니므로 380 함정이 아니다.**
  - **[1-k]** «획 8/7 불변» → «8/**5** — 윗줄 불변 · 아랫줄은 ref 실측 «윗줄의 절반»».
    ⚠ 이 항은 **선언**만 묻고 **그려진 획은 `verify895` [B1]~[B3]** 이 화소로 묻는다 —
    한 항에 섞으면 어느 쪽이 틀렸는지 못 가른다.
  - **[1-o]** 상자 중심 거리 38.8 → **39.3**. 상수의 뜻(«그려진 잉크 cen = ref 38.5»)은 그대로고
    아랫줄 fs 가 바뀌어 **그 뜻을 지키는 상자 값이 따라왔다.**
  - **[R17b]** 강체 예측을 **상수에서 빼고 선언에서 팠다**(dx = −d·sin15 · dy = d·cos15 — d 는 그
    자리의 선언된 상자 거리). 이 시험이 묻는 것은 «d 가 얼마인가» 가 아니라 «두 줄이 한 덩이로
    도는가» 인데 d 를 또 적어 두는 바람에 [1-o] 하나를 옮기자 **덩달아 빨개졌다**([1-n] 선례).
- 회귀 **`verify667` 92/92 · `verify151` 84/84 · `verify124` 76/76 전부 무수정 PASS** · **SMOKE PASS**.

### ⓖ 남은 것 / 다음 회차

1. **윗줄 두께 +6.0%** — 서체 축(위 ⓓ). **아트/서체가 오기 전에는 못 닫는다.** [P2] 가 지킨다.
2. **노랑 AABB 폭 +2.1% · 윗줄 길이 +3.0%** — 885 [1-g] 의 자간 5.2 가 정하는 축이라
   **895 의 몫이 아니다**(닫힌 자리를 다시 열지 마라). 다음 885 계열 회차가 볼 관측으로만 남긴다.

## 1회차 채점 — 비평 GH · GI (캡처 `151-r53` + `151-r53-h1600` = 1회차 제품)

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| GH | 7 | **5** | 6 | 7 | 9 | 5 | **5** |
| GI | 9 | **5** | 8 | 9 | 10 | 5 | **5** |

⇒ **불통과**(통과선 8). 회차 **1/10**. 두 사람의 최저가 **둘 다 ②**, 그리고 최저 근거가
**둘 다 «아랫줄 검정 획이 얇다»** 였다 — 즉 **1회차가 만든 회귀**를 두 사람이 각자 잡았다.

### ⓐ ⚑⚑ 두 비평이 «자 결함» 을 찍었고 그 지적이 옳았다 (2회차가 확인·수리)

| | ref 아랫줄 검정 획 | 우리(1회차 5px) | Δ |
|---|---|---|---|
| **895 1회차 자** | **2.06** | 2.00 | −3.1% ← «맞췄다» |
| GH (서브픽셀 옆면) | 3.38 | 2.25 | −33.4% |
| GI (서브픽셀 g=126 교차) | 3.8 | 2.5 | −34% (분포 **겹침 0**) |

**GI 는 숫자만 보고 짚었다** — «895 의 ref 값 2.06 은 K(2.0628) 와 소수 둘째 자리까지 같다».
**GH 는 물리로 짚었다** — «ref 의 얇은 안티에일리어스 가장자리를 절반쯤 깎아 먹는다».
**둘 다 맞았고 뿌리는 하나였다**: `scan895` 의 획 걸음이 «검정 화소를 한 칸씩 세는» **정수**라
값이 언제나 정수였고, ref 는 우리보다 **K = 2.0628 배 작아** 그 바닥깎기가 **우리 px 로 2.06 씩**
손해였다. 직접 확인 — ref 원본에서 나온 걸음 수는 문턱 5단 내내 **정확히 정수 1 · 2**였고
1회차가 «실측» 이라 적은 **2.063 · 4.126 은 그 1 · 2 에 K 를 곱한 값 그대로**다.

⚠ **내 대조 시험은 이 결함을 못 잡았다** — 우리 캡처를 ref 해상도로 내려 재 보니 획이
**×1.031**(오히려 커진다) 이라 «내림 때문에 깎인다» 는 가설이 기각됐다. 깎는 것은 내림이 아니라
**정수 걸음**이었고, 그 둘은 같은 얼굴을 하고 있었다. 자를 **부분 화소**(빨강 채널 한 문턱의
교차점을 선형 보간)로 바꾸고서야 갈렸다.

### ⓑ 2회차 — **1회차의 과교정을 되돌렸다**(제품 1자리)

부분 화소 자로 다시 재니 **ref 아랫줄 3.73**(GH 3.38 ↔ GI 3.8 **사이**)이고 5px 일 때 우리는
2.67 = **−28.3%** 였다. 필요한 선언은 **3.73 ÷ 0.534 = 6.99 ≈ 7** — **원래 값이 맞았다.**
비평 둘도 각자 «7 로 되돌려라» 를 1순위로 냈다(세 자가 독립으로 같은 값에 도달).

⇒ `-webkit-text-stroke:5px` → **7px** 되돌림. 되돌린 뒤 **−1.3%**.
⚠ **`font-size:31` 은 그대로 둔다** — 획은 `paint-order:stroke fill` 이라 노랑 채움을 안 건드리므로
두께·길이는 한 칸도 안 움직였다([R4] 가 그 독립성을 못박는다). 아랫줄 bbox 에 대해 두 비평이
**부호까지 갈렸고**(GH +3.3% / +0.8% · GI −3.1% / −2.6%) 둘 다 «지금 fs 를 더 만지지 마라» 로
같은 결론을 냈다.

**교훈은 «7 이 맞다» 가 아니다** — **«정수로 세는 자로 ref 를 재면 언제나 우리가 더 두껍게 나온다»** 다.
같은 자를 쓰는 축(획·틈·얇은 선)이 이 저장소에 더 있는지는 **932 가 볼 몫**으로 넘긴다.

### ⓒ 게이트 (2회차)

- **`verify895` 18/18** — §B 를 통째로 다시 썼다.
  - **[B1]** 아랫줄 획 ref ±10%(−1.3%).
  - **[B2]** ⚑ **자가 정수로 안 센다는 것 자체를 항으로 세웠다** — ref 두 값이 **K 의 배수에서
    떨어져 있는가**(÷K = 1.808 · 2.614). 누가 걸음을 정수로 되돌리면 값이 다시 K 의 배수로 굳어
    **이 항이 먼저 빨개진다**. 1회차 결함의 되돌림 시험이다.
  - **[B3]** 윗줄 획 잔차(−20.3%)를 **양성으로 등재**(932).
  - **[R3]·[R4]** 되돌림 시험의 부호를 뒤집었다 — 이제 **5px 로 내리는 것**이 빨강이다(−29.6%).
- **`verify833` 280/280** — **[1-k] 를 8/7 로 되돌렸다**(1회차 이관의 취소 · 사유를 주석에 남겼다).
- 회귀 `verify667` 92/92 · `verify151` 84/84 · `verify124` 76/76 무수정 · **SMOKE PASS**.

### ⓓ 두 비평이 낸 곁다리 — **932 · 933 · 934 등재**

- **932 «자» (T1급 자 결함 계열)** — ⓐ 의 정수 걸음이 **이 저장소의 다른 자에도 있는지** 전수로
  본다. + 윗줄 검정 획 잔차(**자 갈림**: GH «맞다»(6.54↔6.58) / GI −15%(4.7~5.2↔4.0~4.2) /
  895 2회차 −20.3%(5.39↔4.30)) — 셋 중 둘이 «가늘다» 지만 크기가 갈리고 `verify833` [1-k] 가
  «윗줄 8 불변» 을 못박고 있어 **한 회차의 자로는 못 옮긴다.** [B3] 이 지킨다.
- **933 «폴리시(T4)»** — **노랑 획 자체가 가늘다**(두 사람 일치, 크기는 갈림):
  GH «\«0\» 링 두께 −11~14% · 속구멍 넓이 +18.9% · 노랑 넓이 윗줄 −7.8% 아랫줄 −13.2%» ·
  GI «윗줄 획 −28.4% · 아랫줄 −37.9% · 잉크 커버리지 윗줄 −17.4% 아랫줄 −27.2%».
  **AABB 는 ±3% 로 맞는데 면적이 −17~27%** ⇒ 손잡이는 크기가 아니라 **`font-weight`**(둘 다
  `font-size`·`scaleX/Y` 를 명시적으로 배제했다 — 닫힌 자리 1). ⚠ 아랫줄은 이미 `font-weight:400` 이다.
- **934 «잠복(자 갈림)»** — **두 줄 덩어리가 별판 안에서 위로 떠 있는가**:
  GH «판 중심보다 **5.4px 위**(판 높이의 2.97%p) — 윗줄 top +4.9 · 아랫줄 +6.4 를 사라» ↔
  GI «판 AABB 중심 대비 어긋남 **−1.2 / −1.3px** = 판 폭의 0.9% 이내 — **제자리다**».
  **부호가 아니라 크기가 4배 갈렸다**(GH 는 분홍 **무게중심**, GI 는 분홍 **AABB 중심** 기준 —
  별판이 별 모양이라 둘이 다른 점이다). **셋째 자 없이 옮기지 마라** — 그 축은 885 [1-n]·§2 가
  이미 «두 줄이 별 잉크 중심을 따라간다» 로 걸어 둔 자리다.
- ⚑ **9:13.3 은 두 사람 다 «없음»** — GH 최대 채널 차 7/255 · GI 6/255, 판 bbox·빈 띠·기울기가
  소수 둘째 자리까지 같다. **1600 회귀 0.**

### ⓔ 남은 것 / 다음 회차

**895 자신의 축(아랫줄 크기)은 닫혔다** — 두께 −0.5% · 길이 −1.7% · 획 −1.3% · 빈 띠 −3.0% ·
AABB 높이 +1.4%. 두 비평의 ② 감점은 이제 **전부 932(윗줄 획)·933(노랑 획 굵기)** 로 넘어갔고
그 둘은 **이 번호가 못 사는 것**이다(윗줄 획은 833 이 못박은 선언 · 노랑 굵기는 `font-weight` 축).
⇒ 3회차는 **재채점이 아니라 933 을 잡는 것**이 옳다(제품 0줄이면 점수가 안 움직인다).
