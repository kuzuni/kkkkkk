# 637 — `tools/verify98.js` §E 음성 대조항 부패 (제품 0줄)

**세션** sess-0943-24180 (루틴 워커 C) · **1회차** · 2026-09-01
**결과** `verify98` **51/52 FAIL → 56/56 PASS** · `probe637` **10/10 신설** · **제품 `index.html` 0줄** ·
레이아웃 Δ0px · 밸런스·판정 0줄 · **SMOKE PASS**

---

## §0 어떻게 찾았나 — 미선점 작업 0 상태의 게이트 스윕

티어 스캔 결과 **미선점·미완료 행이 0**이었다(살아 있는 lock 셋 = `199`·`356`·`619`, 셋 다
회차 상한이 크거나 `∞` 인 루프라 뺏을 수 없고 heartbeat 도 몇 분 전이었다). 620·636 이 같은
상태에서 한 것과 같은 **게이트 스윕**(`tools/verify*.js` 370개)을 돌렸고, 636 이 «56/370 에서
멈춰 있다» 고 남긴 알파벳 앞쪽을 다시 밟지 않으려고 **역순(`ls | tac`)**으로 돌려 `verify98` 에서
첫 빨강이 나왔다(스윕은 이 작업을 잡으면서 멈췄다 · 나머지는 다음 세션 몫).

⚠ **636-⑤ 를 그대로 다시 확인했다 — 스윕의 빨강은 단독 재현 전에는 빨강이 아니다.**
-P 10 으로 처음 돌렸을 때 `verify102`(84/85) · `verify113`(81/82) · `verify123`(1/33) 이 빨갛게
나왔는데 **셋 다 단독으로는 85/85 · 82/82 · 63/63 PASS** 였다(부하 타이밍). 그래서 병렬을 3 으로
내리고 «PASS 문자열» 이 아니라 **종료 코드**로 빨강을 가르게 고친 뒤 다시 돌렸다
(`verifyA2` 는 마지막 줄에 `PASS` 를 안 찍어서 문자열 판정이 그 자체로 거짓 빨강을 만든다).

## §1 등재문 — 무엇이 빨간가

```
§E coin 발화 억제
  ✓ 연속 두들기기 40회/3.36s → 발화 16회, 4.76회/s ≤ 7
  ✓ 전투 60초 — fxFly 호출 0회 → 실제 발화 0회 (0.00회/s) ≤ 7회/s
  ✗ 전투 중 coin 이 실제로 울리긴 함(연출 회귀 아님) — 0회
51/52 — VERIFY98 FAIL
```

빨간 항은 **음성 대조**다 — 바로 위 «≤ 7회/s» 가 «표본이 0 이라 초록» 인 것을 막으려고 세워 둔
안전장치이고, 그것이 0 을 받았다.

## §2 재현 — `probe637` (338 규칙: 처방 전에 값부터 받는다)

`verify98 §E` 와 **같은 하네스**(같은 서버·같은 플래그·같은 부팅 대기·`auLast` 변화로 «실제 발화»
세기)로 세 창을 잰다. 20초 창 실측:

| 창 | 킬 | `sfx('coin')` 호출 | 실제 발화 | `fxAt(…,'combat')` |
|---|---|---|---|---|
| [1] 현행(제품 기본값) | **23** | **0** | **0** | **0** |
| [2] `FX_COMBAT_FX.kill` 만 true 로 되돌림 | 22 | **22** | **22** | 22 |
| [3] 592 가 남긴 ⑵⑶ 보너스 경로 | 21 | **12** | **12** | 13 |

`PROBE637 10/10 PASS`.

**뿌리는 592 다(저장소 주인 지시 2026-08-31 «적죽을때 골드 이펙트 뜨는거 빼줘 안쓸래 그거»).**
`killEnemy` 의 ⑴ 킬 드랍이 `fxAt(…, 'combat')` 대신 `fxSilent('gold', g)` 로 가면서
`fxWatch → fxFly → sfx('coin')` 경로 자체가 사라졌다. 자가 «실사용 조건» 으로 고른 표본이
**주인 지시로 없어진 사건**이었던 것이고, 오디오·억제 경로는 한 곳도 안 변했다 ⇒ **제품 회귀가 아니라
게이트 부패**다. 얕은 클론이라 git 이력으로는 못 가르는 자리를 **런타임 한 표(`FX_COMBAT_FX`)를
되돌리는 대조**로 갈랐다([2]).

## §3 처방 — 자리를 비우지 않고 뜻을 셋으로 갈랐다

⚠ **그냥 지우면 무르게 푸는 것이다.** 이 항을 빼면 바로 위 «≤ 7회/s» 가 **표본 0 으로 영원히 초록**이
된다 — 356 이 스물여덟 회차 동안 싸운 «못 봐서 0»(LESSONS 634-③)이 이 자리에 그대로 생긴다.
333 처방(«폐기된 표본 → 살아 있는 표본»)대로 **한 항을 세 뜻으로** 갈아 끼웠다.

| 새 항 | 무엇을 말하나 | 실측 |
|---|---|---|
| 전투 60초 — 킬이 실제로 났다 | 그 창의 0 이 «표본이 없어서» 가 아님 | **62킬** |
| ⚑ 592 — 킬 드랍은 coin 을 안 울린다 | **0 이 설계값**임을 자가 선언한다(592 축 신설) | 호출 0 · 발화 0 |
| 되돌림 시험 — 스위치를 켜면 다시 운다 | 그 0 이 «꺼져서 0» 이지 «못 봐서 0» 이 아니다 | **23회 발화 / 23킬** |
| ⑵⑶ 에서는 여전히 운다 | 옛 «연출 회귀 아님» 의 뜻을 **살아 있는 표본**이 이어받는다 | **11회 발화** |
| 그 살아 있는 경로도 ≤ 7회/s | 억제 축을 표본이 있는 창으로 옮긴다 | **0.55회/s** |

⚑ **592 가 남긴 것을 골랐다.** 592 는 ⑴만 끄고 ⑵ 스테이지 클리어 · ⑶ 파도 전멸 보너스는
**일부러 켜 뒀다**(그 주석이 «⑵⑶ 은 «적이 죽을 때» 가 아니라 «스테이지가 끝날 때»» 라고 적고 있다).
음성 대조를 그 둘로 옮기면 **주인 지시를 어기지 않으면서** «코인 연출이 통째로 죽었다» 를 계속 잡는다.

⚠ **표본을 «운» 으로 두지 않았다.** ⑶ 은 `killed >= ENEMY_COUNT`(50)에서만 서는데 기본 속도로는
50킬에 40초가 넘어 창 하나에 «설까 말까» 다(344·372 플레이키의 꼴). 눈금을 한 칸 앞에 세워
(`killed = ENEMY_COUNT - 1` 을 1.5초마다) **제품의 그 분기 그대로** 밟게 했다 — 보너스 계산·발원
표시·골드 증가·소리는 **한 줄도 흉내내지 않는다**.

⚑ **계측기는 한 벌이다.** 세 창(현행·되돌림·⑵⑶)이 `coinWindow(sec, killFx, armMs)` **하나**를 쓴다.
사본을 세 벌 적었으면 한 벌만 고쳐지는 자리가 생긴다(LESSONS 90-① · 402 «표 두 벌»).

## §4 사본 스윕 (636-④)

`grep -l '연출 회귀 아님' tools/*.js` → `verify98` · **`verify99`**. 99 의 것은 **같은 꼴이지만 다른 축**
(스킬 시전음)이고 592 와 무관하다 — 실행해 **52/52 PASS** 임을 확인했다(무수정).
`grep -l 'auLast.coin' tools/*.js` → `verify98` · `probe637` 뿐. `FX_COMBAT_FX` 를 읽는 자는
`verify592`·`probe592` 인데 **둘 다 소리 항이 없다** ⇒ 새 «592 — 킬 드랍은 coin 을 안 울린다» 항은
겹치는 게 아니라 **아무도 안 보던 자리**를 새로 덮는다.

## §5 게이트

- `verify98` **51/52 → 56/56 PASS** (2회 연속 같은 값)
- `probe637` **10/10 신설**
- 회귀 무수정: `verify592` 35/35 · `verify518` 42/42 · `verify99` 52/52 · `verify77` PASS · `verify93` 26/26
- **SMOKE PASS**

## §6 ⚠ 다음 세션에게 — smoke `cur:relic` 오프너는 **부하에 플레이키**다 (639 로 등재)

이 세션은 smoke 가 `✗ cur:relic 클릭 실패: page.click: Timeout 3000ms exceeded` 로 **두 번 연속
빨간** 것을 보고 «main 이 깨졌다» 로 30분을 썼다. 결론은 **제품이 아니다**:

- 보물상자 탭(89 유물 페이지)은 부하 아래에서 **1.3~1.5 fps**(메인 화면 20~22 fps)라
  3초 클릭 타임아웃이 통째로 한두 프레임이다.
- **커밋 대조**로 갈랐다 — 처음엔 `ae76b4b`(619 12회차) PASS · `7f5d5a2`(13회차) FAIL 로
  «13회차가 깼다» 로 읽혔으나, 같은 두 트리에서 **오프너만 8회씩** 돌리니 **1/8 vs 2/8** 로
  **차이가 없었다**. 한 번씩만 돌린 smoke 의 PASS/FAIL 이 우연이었던 것이다.
- 부하의 출처는 **내 게이트 스윕 자신**이었다(-P 3 × chromium). 스윕을 끄고 다시 돌리니
  같은 트리에서 **SMOKE PASS**.

⇒ **교훈은 둘이다.** ⓐ smoke 를 «한 번 빨갛다» 로 판단하지 마라 — 이 오프너는 부하에서 8회 중
6~7회 실패한다. ⓑ **스윕을 돌리는 동안에는 smoke·캡처를 같이 돌리지 마라**(자기가 만든 부하를
제품 결함으로 읽는다). 자리 자체(3초 타임아웃 vs 1.4fps 페이지)는 **639** 로 등재했다(번호 개정 638 → 639 — 상류 sess-1040-31598 의 `claim(638)` 과 등재 경쟁에서 밀렸다).
