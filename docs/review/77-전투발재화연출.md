# 77 — 전투 발(發) 재화 연출은 팝업 아래로 【⚑ 버그】

레퍼런스 대조가 없는 버그 수정(기능 작업)이라 채점 루프 없음 — `tools/verify77.js` 게이트 + 캡처 확인.

## 1회차 (2026-08-25, sess-1533-16806 워커 C) — 구현 + 검증

**채택안**: PROGRESS 77행의 (a)+(b)+(d). (c)(모달 열림 시 전투 비행 생략)는 **미채택** — 이유:
오버레이가 15종+이라 «열림» 검출 목록이 깨지기 쉽고, (b) 레이어 분리만으로 시각 요구(팝업 위 불가시)가
충족되며, 숨은 비행의 DOM 비용은 FXMAX/FXFLY_MAX 로 이미 캡이 있다. 팝업의 41 재화 바(.pcb)는
fxDisp 롤링을 그대로 쓰므로 «숫자 롤링만 남는» (c)의 의도된 그림도 자연히 나온다.

**구현**:
- `#fxlc` 신설(z7 · pointer-events:none · #fxl 과 동일 좌표계/합성 승격). 스테이지 오버레이(z3)·사이드(z5)·HUD(z6) 위,
  `#panel`(z9)·`#tabbar`(z10)·모든 팝업(z28+) 아래.
- `fxAt(t, src)` — 출처 태그. 킬 경로(`killEnemy`, 유일한 fxWorld 호출부)만 `'combat'`.
- `fxSrc()` 가 combat 이면 `{x,y,combat:true}` 반환 → `fxFly`/`fxBurst` 가 `#fxlc` 선택,
  `+n`(fxPlus)은 batch.combat 으로 같은 레이어.
- `#dclw` z61 우회 주석 갱신(값 유지 — giveReward 는 UI 발로도 쏨).

**검증(`node tools/verify77.js` — VERIFY77 PASS, 27/27)**:
| 검사 | 결과 |
|---|---|
| [A] fxAt('combat') → fxSrc combat 태그 전달 / 무태그 비오염 | ✓ |
| [B] fxFly·+n 레이어 라우팅 (combat→#fxlc, UI→#fxl) | ✓ |
| [C] 스태킹 프로브(elementFromPoint): 상점·던전·유물·훈련·장비·소환결과·A5모달 7종 모두 #fxlc 가려짐 · #fxl 보임 | ✓ 14/14 |
| [D] 상점 연 채 실전투 10초 — 신규 연출 11건 전부 #fxlc, #fxl 0건 | ✓ |
| [E] 킬 차단 후 UI 획득 — #fxl 6개(팝업 위 정상), #fxlc 0 | ✓ |
| 콘솔 에러 | 0 |

SMOKE PASS. 캡처: `docs/shots/77-shop-combat-f0~5.png`(상점 열고 전투), `77-main-combat-f0~2.png`(메인).
