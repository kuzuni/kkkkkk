# 598 — 「스프라이트 뒤집힘 빈도」 축에 재현성이 없다 「T4 · 자(게이트) 결손」

> 2026-08-31 · sess-0821-18367 루틴 워커 F · **1회차** · **제품 `index.html` 0줄**
> 고친 것은 `tools/verify587.js` 의 **측정 축**이다. 587 의 히스테리시스는 한 줄도 안 건드렸다.

---

## §0 등재문이 시킨 것

587 은 «이동 방향으로 돌리면 잡몹이 깜빡인다» 를 실측으로 잡아 히스테리시스로 고쳤다
(개체당 초당 0.13 → 0.96 → **0.26회**). 그런데 그 값을 **통과선**으로 쓰려 하자 재현이 안 됐다 —
자연 상태로 재면 회차마다 0.26~0.89 로 흔들렸고, 인구를 «좀비 24마리·불사» 로 고정한 뒤에도
히스테리시스를 뺀 사본과 **부호가 두 번 뒤집혔다**(고친 쪽 0.885 ↔ 뺀 쪽 0.852 · 0.621 ↔ 0.36).
⇒ 587 은 `[5-a]`·`[R-b]` 를 **판정에서 빼고** 값만 찍게 두었다.

등재문이 시킨 순서는 ⓐ **무엇이 값을 흔드는지 먼저 찾고** → ⓑ **그 축을 고정한 결정적 시나리오**를
세우고 → ⓒ 그 위에서 히스테리시스 유무의 차가 회차 간 편차보다 큰지 확인하고, **크면 그때** 통과선을
세운다. ⚠ 임계를 넉넉히 벌려 초록을 만드는 것은 답이 아니다.

---

## §1 재현 — `tools/probe598.js` **15/15 신설** (338 규칙)

착수하자마자 기준선으로 돌린 `verify587` 이 **그 자리에서 부호를 뒤집어 줬다**:

```
[5-a] 잡몹 뒤집힘(기록 — 판정 안 함) — 1.038회/s
[R-b] 고친 쪽 1.038회 ↔ 뺀 쪽 1.022회        ← 고친 쪽이 더 떤다(= 축이 뜻을 잃은 상태)
```

`probe598` §A 는 같은 자를 두 트리에서 회차마다 돌린다. 실측(3회차 두 벌, 다른 날 실행 포함):

| 회차 | 고친 쪽 | 뺀 쪽 | 프레임(12초 동안) |
|---|---|---|---|
| 1 | 0.945 | 1.362 | 357 ↔ 334 |
| 2 | 0.657 | 1.025 | 350 ↔ 356 |
| 3 | 0.897 | 1.094 | 340 ↔ 345 |

* **[A1]** 같은 트리 안에서 폭이 0.29회/s — **CV 15.1%**(다른 실행에서는 12.9% · 20.1%)
* **[A3]** 그 CV 가 두 무리의 차(~35%)의 **3분의 1** 이다 ⇒ 몇 회만 돌리면 부호가 뒤집힌다
* **[A4]** 같은 «12초» 에 굴러간 프레임이 **334~357** 로 다르다 — 분모부터 회차마다 다른 자였다

---

## §2 ⚑ 본체 — **흔들던 것은 «축» 이 아니라 «판» 이었다**

`probe598` §B 는 축을 하나씩 못박으며 변동계수를 잰다(같은 시드 3회씩):

| 단계 | 무엇을 못박았나 | 실측 폭 | CV |
|---|---|---|---|
| S1 | 아무것도(587 의 자연 상태) | 0.657~0.945 | **15.1%** |
| S2 | + 프레임 시간(`step(1/60)` 을 손으로 굴린다) | 0.285~0.330 | 6.6% |
| S3 | + 난수 시드(LCG) | 0.396~0.667 | **20.6%** |
| S4 | + 좌표·스월 부호·**배우 전부** | 14.403~14.403 | **0.0%** |

⚑ **S3 가 S2 보다 나쁘다는 것이 이 표의 핵심이다.** 난수를 시드로 묶어도 CV 가 안 내려간 이유는
남은 흔들림이 난수가 아니라 **부팅이 남긴 상태**였기 때문이다 — 워밍업 1.5초 동안 판이 제멋대로 굴러
플레이어 좌표·조준·스킬 쿨·투사체가 회차마다 다르게 남는다. 그것까지 선언으로 세운 S4 에서야
값이 **비트 단위로 같아졌다**(`[B2]` 3/3 · 게이트의 `[5-c]` 가 매 실행 다시 확인한다).

흔드는 것 셋을 순서대로 적으면:

1. **프레임 시간** — rAF 로 굴리면 dt 가 회차마다 다르다(위 334~357). 뒤집힘은 «부호가 바뀐 프레임 수»
   라 분모·분자가 dt 에 서로 다르게 반응한다.
2. **난수** — 스폰 좌표 · `e.ph`(스월 부호) · `dashCd` 가 전부 `Math.random` 이다.
   ⚠ 특히 **돌진 예고 중에는 `spd = 0`** 이라 규칙이 «이동 방향» 에서 «플레이어 쪽» 으로 갈아탄다 —
   돌진이 몇 번 나느냐가 곧 뒤집힘 수다.
3. **부팅 잔여 상태** — 위 S3.

---

## §3 처방 — `tools/flap598lib.js` **신설**(자는 한 벌 · 385 규약)

`probe598`(재현)과 `verify587` §5·§R-b(게이트)가 **같은 함수**를 쓴다(`size590lib`·`size541lib` 선례).

* 게임 루프(rAF)를 **멈추고** `step(1/60)` 을 손으로 굴린다 → 프레임 시간 제거
* `Math.random` 을 시드 LCG 로 갈아 끼운다 → 난수 제거
* 판을 통째로 비우고(`shots`·`parts`·`zones`·`booms`·`bolts`·`drones`·`rings`·`ghosts`·`pets`·`corpses`)
  스킬을 봉인하고(`skillCd` 전 키 1e9 — 둔화가 걸리면 `spd` 가 흔들린다) 좌표·스월 부호를 선언으로 세운다
* 플레이어는 **놓인 표적**이다(등재문 ⓑ) — 매 프레임 제자리로 되돌린다

⚑ **두 트리를 견줄 수 있는 근거를 먼저 확인했다** — `flip` 은 그리기에만 쓰이고 물리로 되먹임되지
않는다(`index.html` 의 `.flip` 참조는 전부 `drawFrame`/`drawFrameC`/시체 승계다). 그래서 히스테리시스를
뺀 사본도 **같은 시드에서 같은 궤적**을 그리고, 두 값의 차는 궤적 차이가 아니라 **규칙 하나**다.
587 이 못 잡던 «부호 뒤집힘» 이 구조적으로 사라지는 자리가 정확히 여기다.

---

## §4 대조 — 통과선을 세울 자격이 있는가 (§C · 시드 5개)

| 시드 | 전체 뒤집힘 고침 ↔ 뺌 | 깜빡임(0.25초 안 왕복) | 전환당 뒤집힘 |
|---|---|---|---|
| 11 | 15.674 ↔ **19.194** | 15.111 ↔ 18.712 | 5.901 ↔ 7.226 |
| 22 | 15.066 ↔ **19.056** | 14.476 ↔ 18.576 | 5.848 ↔ 7.396 |
| 33 | 14.910 ↔ **18.951** | 14.337 ↔ 18.431 | 5.874 ↔ 7.466 |
| 44 | 13.632 ↔ **17.205** | 13.052 ↔ 16.674 | 5.499 ↔ 6.940 |
| 55 | 15.667 ↔ **19.413** | 15.111 ↔ 18.948 | 5.697 ↔ 7.059 |

* **[C1]** 결정적 판에서는 **전체 뒤집힘도 부호가 한 번도 안 뒤집힌다**(5/5) — 축은 처음부터 멀쩡했고
  흔들린 것은 판이었다. (1차안에서 배우를 덜 못박았을 때는 배수가 ×0.53~×1.64 로 **갈렸다** — 그 상태로
  임계를 박았으면 플레이키 게이트가 하나 더 생겼을 것이다.)
* **[C3]** 두 무리가 **안 겹친다**: 고친 쪽 ≤ 15.674 · 뺀 쪽 ≥ 17.205 ⇒ **간극 1.53**
* 배수는 시드마다 **×1.22~×1.26** 로 고르다.

⇒ 통과선 **16.4** 는 두 무리 사이 한복판이고 여유가 **4.6% / 4.7%** 로 대칭이다.
**넉넉히 벌린 값이 아니다** — 뺀 사본은 같은 바에서 **빨개진다**(§R-b 가 그것을 단언한다).

플레이어 축은 각본으로 세웠다 — 0.75초마다 여섯 국면(가로 둘 · **세로만 둘** · **임계 언저리 둘**)을
돈다. 히스테리시스가 일하는 자리가 정확히 그 넷이다(587 §6 «카이팅 중 `vx` 가 임계를 들락거린다»).
각본이 요구하는 가로 전환은 12초에 8번이고 **고친 쪽은 9번(0.750회/s) — 시드 다섯에서 전부 같은 값**이다.
뺀 사본은 0.833~1.333.
⚠ 판정은 «몇 번 도나» 가 아니라 **«돌았다가 0.25초 안에 되돌아오나»** 로 했다 — 바닥이 0 이라
환경이 달라져도 안 흔들리고, 주인이 본 병이 정확히 그것이다. 고친 쪽 **0건** · 뺀 사본 0.333회/s.

---

## §5 게이트 — `verify587` **28 → 34항**

| 항 | 전 | 후 |
|---|---|---|
| `[5-a]` | 자연 상태 값 **기록만** | **결정적 · 시드 11** ≤ 16.4회/s (실측 15.67) |
| `[5-b]` | 플레이어 ≤ 3.20 (자연 상태 — 3.261 로 빨개진 적 있다) | **결정적 · 시드 44** ≤ 16.4회/s (실측 13.63) |
| `[5-c]` | — | **같은 시드를 다시 재면 값이 같다**(오차 0) |
| `[5-d]` | — | 플레이어 각본 중 **0.25초 안 되돌아옴 0건** |
| `[5-e]` | — | 플레이어 뒤집힘 ≤ 1.0회/s (실측 0.75 = 각본이 요구하는 전환) |
| `[R-b]` | 뺀 사본 값 **기록만** | 뺀 사본이 **같은 바를 넘는다**(시드 2개 · 실측 19.19 · 17.21) |
| `[R-c]` | — | 배수 ≥ 1.15 (실측 ×1.22 · ×1.26) |
| `[R-d]` | — | 뺀 사본은 플레이어도 되돌아온다(`[5-d]` 의 **음성항**) |

치환표(`SUB_HYST`)를 `flap598lib` 한 곳으로 옮겨 **두 벌을 없앴다** — `probe598` 과 `verify587` 이
같은 앵커를 본다(402 «표 두 벌» 부패 방지).

---

## §6 회귀 — 새 축은 **5회 연속 같은 값**이다

`verify587` 을 연속으로 5회 돌렸다(같은 트리 · 같은 커밋):

| 실행 | `[5-a]` 시드 11 | `[5-b]` 시드 44 | `[5-c]` 오차 | `[5-d]` 플레이어 깜빡임 | `[R-b:11]` | `[R-b:44]` |
|---|---|---|---|---|---|---|
| 1~5 | **15.67** | **13.63** | **0.0e+0** | **0.00** | 19.19 (×1.22) | 17.20 (×1.26) |

**한 자리도 안 흔들렸다.** 옛 축은 같은 5회에 0.618 · 0.629 · … 로 움직였고 그 중 한 번은
`[R-b]` 부호가 **뒤집혔다**(고친 쪽 0.618 ↔ 뺀 쪽 0.474). 옛 `[5-b]`(플레이어 ≤ 3.20)도
그 5회 중 한 번 **3.371 로 빨개졌다** — 597 이 «판정에서 뺐다» 고 적었지만 파일에는 판정으로
남아 있던 항이고, 이번에 결정적 축(`[5-d]`·`[5-e]`)으로 갈아 끼우면서 함께 닫혔다.

**SMOKE PASS** · 제품 `index.html` **0줄**이라 제품 회귀는 대상이 없다(`git diff --stat` 에 index.html 없음).

---

## §7 남긴 것 — 곁다리 등재 **606**(등재 경쟁으로 604 → 606)

⚠ **`verify587` §4 실동작 항(`[4-a:promo]`·`[4-a:arena]`·`[4-b]`)이 흔들린다.** 5회 실측:
promo **2.1 · 2.9 · 4.7 · 5.7 · 6.6%**(바 4.0) · arena 한 번 4.3% · `[4-b]` 시체 표본 한 번 0구.

**내 변경과 무관함을 대조 실행으로 못박았다**(338·344 규칙) — **옛 게이트(`389f00c` 판 그대로)**를
같은 트리에서 2회 돌렸더니 promo **2.3 · 3.9%** 에 **둘 다 27/28**(각 실행 1항 빨강)이었다.
즉 이 흔들림은 598 이 손댄 §5 와 무관하고 **옛 판에서도 같다.**

⚑ **뿌리는 598 이 §5 에서 찾은 것과 같다** — §3·§4 는 아직 **rAF 표본**이라 머신 부하가 곧
표본 수(promo 105~140프레임)와 창 효과로 들어온다. 587 이 바를 4% 로 정할 때의 실측은
«다크엘프 1.1 · 조이스틱 1.6 · 아레나 2.1%» 였는데, 워커 8개가 도는 지금 같은 항이 6.6% 까지 간다.
⇒ **처방은 바를 벌리는 것이 아니라**(그러면 «반대로 도는 것도 통과» 가 된다) `flap598lib` 의
결정적 구동(`step(1/60)` + 시드 + 배우 못박기)을 **§3·§4 로 넓히는 것**이다. 606 행에 적어 두었다.
