# 486 — 23 훈련 카드 알약 = «지금 최종값»

> 저장소 주인 지시(2026-08-30, 스크린샷) — «골드로 훈련 업글하는거 보면 **최종값이 몇인지 각각 써있어야함**.
> 업글했을때 몇씩되는지는 안궁금함»
>
> **완료(2026-08-30, sess-1706-3451 워커 E · 1회차)** — 제품 `index.html` **6자리** · 레이아웃 **Δ0px** ·
> 밸런스·판정 **0줄** · 세이브 이관 **없음**.
> `verify486` **80/80 신설** · `probe486` **13/13 신설** · 이관 `verify188`(2항 → **4항**) ·
> 회귀 `verify64` 44/44 · `verify326` 88/88 · `verify483` 89/89 · `verify150` 43/43 · `verify113` 82/82 ·
> `verify166` 73/73 · `verify203` 85/85 · `verify167` 30/30 · `verify168` 56/56 · `verify112` 38/38 ·
> `verify354` 29/29 **전부 무수정** · **SMOKE PASS**.

## 1. 재현 먼저 (338 규칙) — `tools/probe486.js`

등재문의 처방을 따르기 전에 제품에게 직접 물었다. 다섯 절을 굴렸고 **셋이 등재문과 달랐다.**

| 절 | 물음 | 수리 «전» 실측 |
|---|---|---|
| §A | 알약이 정말 «증가분» 인가 | `.cv` = **+20 / +100 / +15** · 같은 순간의 최종값 **533 / 2.67A / 364** 는 화면 어디에도 없다 |
| §B | `stat.dmg/maxHp/regen` 이 카드 3장과 1:1 인가 | ✅ 그렇다. 그리고 **배수가 얹힌 값**이다(`U[k].val(lv) × mul*`) — 단계 3→8 로 1045 → 1480 |
| §C | 최종값(최장 6글자)이 알약에 들어오는가 | **들어온다** — 최장 «9.99AA» 잉크 **146.88px** / 예산 **294px** |
| §D | 58 «+n» 플로터는 무엇을 읽는가 | **`.cv` 글자를 그대로 읽는다**(`trDeltaTxt`) |
| §E | 사면 같은 프레임에 값이 오르는가 | `.cv` 는 **+20 에 굳어 있고**(안 움직인다) 최종값만 1.12A → 1.15A 로 움직인다 |

### ⚑ 등재문이 못 본 자리 하나 — 58 «+n» 플로터 (§D)

`trDeltaTxt(card)` 가 **`.cv` 의 글자를 그대로 띄우고 있었다**(58 14회차 주석: «카드가 이미 보여 주는
«+2.4»(.cv)로 델타 플로터 문구를 만든다»). 알약을 최종값으로 갈아 끼우면 강화할 때마다
**«방금 9.64G 를 얻었다»** 가 떠오른다 — 58 의 **23·24·26회차가 «거짓말» 로 못 박은 바로 그 항목**의
부호만 뒤집힌 꼴이다(그때는 «증분 0.2 를 띄우면 거짓말» 이었고, 이번엔 «총량을 띄우면 거짓말»).

⇒ 값 계산을 알약에서 떼어냈다. `trainCardData()` 는 `gain` 을 **계속 들고 있고**(화면에서만 사라졌다)
`trainGainTxt(k)` 가 그것을 꺼내 플로터에 준다. **식을 두 벌로 적지 않았다** — 한 곳(`trainCardData`)에서 꺼낸다.

### ⚑ 등재문 처방 ③ 의 «필요하면 font-size 한 단 아래» 는 **안 탔다** (§C)

`fmtB`(= `fmtG`)가 낼 수 있는 최장 꼴은 «9.99AA»/«99.9AA» = 6글자다. 48.5px · `scaleX(.90)` 에서
잉크 **146.88px** 이고 카드 내부 예산은 **294px**(카드 326 − 안쪽 테 8×2 − 규약 여백 8×2) — **절반이 남는다.**
그래서 **글자 크기도 `.cv` 기하도 한 픽셀 안 건드렸다**(레이아웃 Δ0px). 측정표 23 정오표도 불필요하다.
`verify486` [E] 가 7종 문자열의 좌우 여백을 매 실행 다시 잰다.

## 2. 무엇을 고쳤나 — 제품 6자리

| # | 자리 | 변경 |
|---|---|---|
| 1 | `TRAIN_LABEL.regen` | `체력 회복` → **`체력 회복/초`** — 최종값이 되면서 재생만 단위가 필요해졌다. 단위는 **이름 줄**이 맡고 알약은 수만 담는다(처방 ①의 «+«/초» 는 이름 줄에») |
| 2 | `TRAIN_NOW` 신설 | `{ atk:() => stat.dmg, hp:() => stat.maxHp, regen:() => stat.regen }` — **전투가 실제로 쓰는 값** |
| 3 | `trainCardData()` | `now:fmtB(TRAIN_NOW[k]())` 추가. **`gain` 은 남긴다**(플로터가 읽는다) |
| 4 | `trainGainTxt(k)` 신설 | 58 플로터 전용 접근자 — `trainCardData()` 에서 꺼낸다(식 한 벌) |
| 5 | `renderTrain()` · `renderTrainLive()` | `.cv` = `c.full ? 'MAX' : c.now` (두 경로 모두) |
| 6 | `trDeltaTxt()` · `trHoldStart()` | `.cv` 를 안 읽는다. 문구는 **구매 «전»** 에 잡는다(상한 직전 x30 이 7개만 사지는 자리에서 갈린다) |

### «순수 훈련분» 이 아니라 `stat.*` 를 쓴 근거 (처방 ①의 review 한 줄)

주인이 보고 싶은 것은 «지금 내 공격력» 이지 «훈련이 준 몫» 이 아니다. 같은 화면 안에서
**25 정보 탭·19 스펙 시트·06 장비 알약이 전부 `stat.*` 를 그린다**(`verify188` ③ 이 그 셋을 이미 단언한다) —
훈련 카드만 다른 축을 쓰면 «같은 공격력인데 화면마다 수가 다르다» 가 된다. `probe486` §B 가
`stat.dmg = U.atk.val(lv) × mulAtk` 임을 실측으로 못박아 두었다.

### MAX 를 남긴 이유 (그리고 남은 긴장)

처방 ②(«MAX/상한 표기는 그대로»)를 글자 그대로 지켰다 — 상한 카드는 알약 **MAX** · 버튼 **상한**.
`verify64`([8] 홀드 정지 판정)와 `verify326`([E] 상한 카드 표기) **두 게이트가 이 계약 위에 서 있어**
이관 없이 초록으로 남는다.
⚠ **긴장은 남는다** — 상한에 닿은 카드만은 «최종값이 몇인지» 를 여전히 안 말한다. 지시서가 명시적으로
남기라고 한 표기라 이번 회차에서는 안 건드렸고, 뒤집으려면 주인 판단 + 위 두 게이트 이관이 한 벌로 필요하다.

## 3. 게이트

### 신설 — `tools/verify486.js` **80/80**

| 절 | 내용 |
|---|---|
| [A] | 세 칸 = `fmtB(stat.dmg/maxHp/regen)` — 상태 4벌(부팅 · lv 900 · 단계 40 · 도감 배수). **음성항**: «증가분과 우연히 같은 글자» 헛초록 차단 |
| [B] | «+» 접두 0건(x1·x10·x30) + 소스 스캔(두 렌더 경로가 `c.gain` 을 안 그린다) |
| [C] | 구매 1회 → **같은 프레임** 갱신 · 3수량 × 2경로(통째·가벼운 갱신) 18항 |
| [D] | MAX/«상한» 경로 유지 — verify64·verify326 계약을 안 깼다 |
| [E] | 잉크가 알약 안 — 7종 문자열 좌우 여백 ≥ 8px · `font-size` 48.5px 불변 · 카드 폭 326 불변 |
| [F] | 58 플로터 = **증가분** · 알약 글자와 다르다 · 상한에서는 빈 문자열 · `trDeltaTxt` 본문에 `.cv` 0건 |
| [G] | 라벨 «체력 회복/초» · 단위가 알약에는 안 붙었다 · 잉크가 카드 안(161.87px · 여백 82/82) |
| [H] | 안 건드린 축 — `TRAIN_VAL_K`·`TRAIN_CAP_STEP`·`TRAIN_BONUS`·`TRAIN_QTYS`·`.cv` 기하·글자 크기 불변 · 483 진행바 계약 유지 |
| [R] | **되돌림 시험** — `c.now` → `c.gain` 사본에서 «+ 증가분» · «최종값 없음» · «사도 글자가 안 움직임» 이 그대로 재현 |
| [I] | 콘솔·pageerror 0 |

### 이관 — `tools/verify188.js` (2항 → 4항)

188 은 이 자리에서 **«+» 접두를 정규식에 박아 두고** 있었다(`/^\+\d{1,3}(\.\d+)?[A-Z]{1,2}$/`, 195행·305행).
⛔ **항을 지우지 않았다** — 188 이 묻던 것은 «훈련 카드가 전투 수치 표기층을 타는가» 이고 그 물음은 그대로 살아 있다.
**328 교훈대로 «누른 항을 묻는 항» 을 한 줄 더 넣어** 두 칸으로 갈랐다:

* `trGain` = 알약 = **최종값** → 알파벳 단위 **AND «+» 없음** + `fmtB(stat.maxHp)` 와 글자 일치
* `trDelta` = **58 플로터** = 증가분이 옮겨 간 자리 → «+» + 알파벳 단위

⇒ 486 이 통째로 사라지면 두 칸이 **동시에** 빨개진다(옛 항만 눌렀으면 «486 이 없어도 초록» 이 됐다).
표본 성격(«체력 카드 x30 · lv 900» 으로 접힘을 가른다)은 그대로다.

## 4. 곁다리 관측 — **505** 로 등재 (내 작업 단위 아님)

`verify188` 에 **내 변경과 무관한 빨강 2건**이 있다:
`③ 08 펫 설명문 «장착 효과» 피해 «+15%» 알파벳 단위` 와 그 짝인 `§R3` 이다.

**수리 전 커밋에서도 같은 빨강임을 대조 실행으로 확인했다**(338·344·369 규칙) —
`origin/main`(9724cdf) 트리를 통째로 꺼내 같은 자로 돌려 **`VERIFY188 75/79 FAIL`**, 같은 두 항이 빨갛다.
뿌리는 **485**(펫 «장착 효과 공격력 +n%» 신설)가 그 줄을 **퍼센트**로 바꾼 것이고, 188 은 아직 그 자리를
«전투 수치(알파벳 단위)» 로 묻는다. 제품이 아니라 자가 뒤처진 쪽이다. 상세는 PROGRESS **505** 행.

## 5. 회차 기록

**1회차(2026-08-30, sess-1706-3451)** — 지시서 [3]-**(가) 기계적/기능 작업**이라 비평 루프 없음.
레이아웃 Δ0px 이고 레퍼런스 대조가 필요 없는 «표시값 축 교체» 다. 헤드리스 실행으로 콘솔 에러 0 ·
겹침·잘림 0(SMOKE PASS) 확인, 나머지는 전부 자(`probe486`·`verify486`)로 닫았다.

## 6. 다음 세션이 볼 것

* ⚑ **훈련 카드에 새 값을 얹는 세션은 `.cv` 를 «증가분» 으로 다시 읽지 마라.** 이제 그 알약은
  «지금 총량» 이다. 증가분이 필요하면 `trainGainTxt(k)`(또는 `trainCardData().gain`)를 쓴다 —
  `verify486` [F] 가 `trDeltaTxt` 본문의 `.cv` 재등장을 매 실행 막는다.
* ⚑ **`.cv` 는 아직 초록(`#8BE22C`)이다.** «+증가» 시절의 색이라 «총량» 에는 뜻이 안 맞을 수 있는데,
  ⑤ 색은 통과를 막지 않는 축(지시서 [3]-(나) 4)이라 이번 회차에서 안 건드렸다. 351/491 폴리시 루프가
  이 화면을 잡으면 그때 같이 볼 것.
* ⚑ **상한 카드의 MAX** — 위 §2 «남은 긴장». 뒤집으려면 `verify64` [8] 과 `verify326` [E] 를 같이 이관해야 한다.
