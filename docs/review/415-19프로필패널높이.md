# 415 — 19 프로필 팝업의 패널 높이가 1600 프레임 띠의 95.7% 를 먹는다

- 세션 `sess-0636-28340` 워커 G · 2026-08-30 · **1회차**
- 등재: 2026-08-29 `sess-1118-23616` 워커 A (391 1회차 비평가 **CE·CF 2인 일치** — 잔여 감점이 전부 여기)
- 제품 `index.html` **6자리**(`.pf` 선언 1 + 흡수분을 읽는 5자리) ·
  기준 프레임 **1920·2280·2600 Δ0px** · 판정·밸런스·세이브 **0줄**
- 게이트 `tools/verify415.js` **115/115 신설** · 재현기 `tools/probe415.js` **20/20 신설** ·
  이관 `tools/verify241.js` 86 → **100/100** · `tools/verify391.js` 42 → **57/57** · `tools/probe391.js` 17 → **18/18**

---

## 1. 재현 — `probe415` 20/20 (391 선언 사본 ↔ 현재 트리를 같은 자로)

338·341·350 규칙대로 **처방 전에 재현**했다. 등재문의 값은 소수점까지 그대로 나왔다.

| frameH | 띠 | 패널 전→후 | 위 여백 전→후 | 아래 여백 전→후 | 패널/띠 |
|---|---|---|---|---|---|
| 1600 | 1458 | 1396→**1362** | 31→**48** | 31→**48** | 95.7%→93.4% |
| 1634 | 1492 | 1396→1396 | 65→48 | 31→**48** | 93.6% |
| 1700 | 1558 | 1396→1396 | 131→114 | 31→**48** | 89.6% |
| 1841 | 1699 | 1396→1396 | 272→255 | 31→**48** | 82.2% |
| 1875 | 1733 | 1396→1396 | 289→289 | 48→48 | 80.6% |
| **1920** | 1778 | 1396→1396 | 289→289 | 93→93 | 78.5% |
| **2280** | 2138 | 1396→1396 | 289→289 | 453→453 | 65.3% |
| **2600** | 2458 | 1396→1396 | 289→289 | 773→773 | 56.8% |

## 2. 등재문이 안 적은 것 둘 — 재현이 같이 낸 값

등재문은 «95.7% · 여백 각 31px» 을 정확히 적었다. 그 위에 재현이 두 가지를 더 냈다.

**ⓐ 역전은 «내부 상단 패딩 37» 만이 아니다 — 내부 하단 패딩 40 이 더 크다.**
CE 는 «외곽 31 이 팝업 **내부** 상단 패딩 37 보다 좁다(6px·16% 역전)» 를 근거로 ③=7 을 줬다.
자로 재 보니 팝업의 **하단** 내부 패딩(패널 하변 ↔ «종합 스탯» 토글 하변)은 **40** 이다.
⇒ 외곽 31 은 팝업 자신의 두 패딩을 **둘 다** 밑돌고 있었고(−6 · −9), 등재문에 적힌 «6px» 은 절반이다.
CF 의 독립 근거(«라이브 HUD 잉크 대비 최소선 40px 에 9px 미달»)가 가리킨 값과 **40 으로 같다** —
두 비평가가 서로 다른 말로 같은 자를 짚고 있었다.

**ⓑ 아래 여백 31 은 «1600 만의 값» 이 아니다.**
등재문·PROGRESS 행은 이것을 «1600 프레임» 문제로 적었다. 그런데 391 의 상한은 «여유»형
(`min(431, 100% − 1427)`)이라 **상한 항이 431 을 이기는 구간 전체**에서 아래 여백이 31 로 **고정**된다 —
1600·1634·1700·**1841** 이 전부 31 이고, 33 으로 풀리는 것은 1860, 48 이 되는 것은 1875 부터다.
즉 «9:13.3 전용 결함» 이 아니라 **지원 하단 구간(1600..1858)의 성질**이었다.
9:16(1920)은 93 이라 처음부터 안 걸린다 — 그래서 391 이 얼려 둔 프레임은 이번에도 안 건드려도 된다.

## 3. 처방 — 높이 축을 «연속»으로 연다 (등재문 후보 ⓐ·ⓑ·ⓒ 셋 다 안 골랐다)

등재문의 후보는 ⓐ `.shortf` 갈래로 하단 두 줄만 당기기 · ⓑ 자식 좌표계를 상대값으로 갈아엎기 ·
ⓒ 현행 유지였다. **셋 다 안 골랐다.**

- ⓒ 는 §2-ⓐ 로 기각된다 — 외곽 여백이 팝업 **자기 내부 패딩**보다 좁은 것은 «기하 한계» 가 아니라
  «분배가 틀린 것» 이다. 슬랙 62 를 반으로 가르는 것 말고 **패널을 줄이는 축**이 남아 있었다.
- ⓐ 는 **LESSONS 391-② 가 이미 값을 매겨 둔 길**이다 — «갈래는 공짜가 아니다». 임계 1842 에서
  패널이 34px 튄다. 게다가 §2-ⓑ 대로 결함 구간은 1600..1858 이라 `.shortf`(1842) 문턱과 **어긋난다**.
- ⓑ 는 241 이 얼려 둔 «패널 local» 좌표계를 통째로 갈아야 해서 이 작업 단위보다 크다.

**쓴 것: 흡수분 `--pfsh` 하나.**

```css
.pf{ --pfsh:clamp(0px, calc(1634px - var(--frameh, 2280px)), 34px);
     top:clamp(190px, 431px, calc(var(--frameh, 2280px) - 1444px));
     height:calc(1396px - var(--pfsh)) }
.pf-grid{ height:calc(544px - var(--pfsh)) }        /* 그릇 하나만 짧아진다 */
.pf-msn{ top:calc(1026px - var(--pfsh)) }           /* 그 아래 넷은 같이 올라간다 */
.pf-ftr{ top:calc(1089px - var(--pfsh)) }
.pf-btn{ top:calc(1105px - var(--pfsh)) }
.pf-tgl{ top:calc(1261px - var(--pfsh)) }
```

**세 상수는 전부 역산값이다** — 게이트 §2 가 그것을 되묻는다.

- **1634** = 142(HUD 잉크) + 48 + 1396(패널) + 48 = «패널이 온전한 채로 48/48 이 나오는 첫 프레임».
- **34** = 1634 − 1600 = 그 문턱과 지원 최저 프레임의 차. 흡수분의 상한이지 «고른 값» 이 아니다.
- **1444** = 1396 + 48. 391 의 1427(= 1396 + 31)에서 여유만 바꾼 같은 꼴이다.
- **48** = 내부 패딩 상 37 · 하 40 을 둘 다 넘는 첫 8의 배수. §3 게이트가 두 패딩을 **자로 읽어** 되묻는다.

**흡수는 스크롤 그릇 하나에서만 난다**(403·404 처방). `.pf-grid` 는 이미 `overflow-y:auto` 라
짧아져도 잘림이 아니라 스크롤이고, 실제로 부팅 세이브에서는 내용이 510 안에 그대로 들어간다
(scrollHeight 510 = clientHeight 510 — 스크롤조차 안 쓴다). 그 아래 네 요소는 **같은 34px** 만큼
같이 올라가므로 **서로의 간격과 내부 하단 패딩 40 은 두 프레임에서 Δ0** 이고,
패널 위쪽(`.pf-gid`·`.pf-por`·`.pf-ttl`·`.pf-tab`)은 **한 픽셀도 안 움직인다**.

### 3-1. 연속형이라 임계 점프가 0 이다

`.shortf` 갈래를 안 만든 것이 이 처방의 값이다. 게이트 §4 가 임계 앞뒤를 직접 잰다:

| 경계 | 패널 높이 | 패널 top | 토글 local y |
|---|---|---|---|
| 1633 → 1634 | 1395 → 1396 | 190 → 190 | 1260 → 1261 |
| 1634 → 1635 | 1396 → 1396 | 190 → 191 | 1261 → 1261 |
| 1874 → 1875 | 1396 → 1396 | 430 → 431 | 1261 → 1261 |

세 축 다 **1px 이하**다. ⓐ 를 골랐으면 1841→1842 에서 34px 이 튀었을 자리다.

## 4. 무르게 풀지 않았음 — §R 되돌림 3종

| 사본 | 결과 | 무엇을 못박나 |
|---|---|---|
| 391 선언으로 전부 되돌림 | 아래 여백 **31**, 내부 패딩 37·40 **둘 다 밑돎** | 등재문의 역전이 되살아난다 |
| 흡수분만 제거(상한은 415) | 아래 여백 **14** — 391(31)보다 **나쁘다** | «상한만 올리는 반쪽 수리» 를 막는다 |
| 그릇만 안 줄임(자식만 올림) | 그리드가 «해금 미션» 띠를 **22px 밟는다** | 흡수 그릇이 처방의 **본체**임을 못박는다 |

⚠ **되돌림 사본은 일곱 자리를 «전부» 되돌려야 한다.** 처음에 `.pf` 상자만 되돌린 사본을 썼더니
자식은 34px 올라간 채라 **내부 하단 패딩 74** 라는 «없던 세 번째 상태» 가 나왔고, 그 상태에서
«아래 여백 31» 은 초록이었다(=역전 항이 헛초록). `probe415`·`verify391`·`verify415` 셋 다
되돌림 목록을 **배열 하나**로 두고, 한 자리라도 못 찾으면 **종료 코드 2 로 죽는다**(neg279 처방).

## 5. 게이트 이관

- **`tools/verify241.js` 86 → 100/100** — §3 의 `LOCAL` 표가 **이 작업의 계약**이었다(PROGRESS 415 행).
  1920·2280·2600 은 표 그대로 두고 **1600 만** 갈아 끼웠는데, **새 상수(1071·1227·992)를 안 적었다** —
  적었으면 «흡수 구조가 사라져도 초록» 인 자가 된다(328~330 교훈). 묻는 것은 **관계** 셋이다:
  ① 위쪽 앵커(tab·por·grid top)는 2280 과 **같은 값** ② 아래 셋은 «2280 값 − (1396 − 실측 패널 높이)»
  ③ 그 흡수는 `.pf-grid` 높이에서만 난다. §1 값 항 옆에도 유도 항 셋을 나란히 뒀고,
  §6 음성항 사본은 **241 이전 선언(top 431 고정 · height 1396 고정)** 으로 되돌려 원 증상 **227px** 을 그대로 지켰다
  (상한만 떼면 흡수분이 남아 193 이 되어 원 증상과 달라진다).
- **`tools/verify391.js` 42 → 57/57** — §3(«덮임은 결함이 아니다»)은 **한 줄도 안 바뀌었다.**
  덮임은 오히려 149 → **132** 로 줄었고, «1920 도 87px 덮는다 · 깊은 딤 · 탭 안 눌림 · 띠가 짧다» 는 그대로다.
  갈아 끼운 것은 §1·§2 의 **값**(173/31 → 190/48)과 §R 사본이고, §2 의 **유도**(«여유 = 띠의 남는 절반»)는
  분모가 되는 패널만 실측으로 바뀌어 그대로 성립한다. §R 에 사본을 하나 늘려 셋으로 만들었다.
- **`tools/probe391.js` 17 → 18/18** — 「수리 전」은 여전히 **241 선언**이다(391 등재문의 172px 을 재현하는 자다).
  「수리 후」만 415 로 갱신했고, «띠 한가운데» 는 상수(173)가 아니라 **성질**(위 = 아래 · top = 잉크끝 + 여백)로 되물어
  다음에 여백이 또 바뀌어도 안 굳는다. 415 가 «그 절반을 더 벌렸다» 는 항을 하나 더했다.

**회귀 무수정**: `verify201` 74/74 · `verify240` 36/36 · `verify20` PASS · `verify137` 152/152 ·
`verify125` PASS · `verify95` PASS · `verify351` 86/86 · **SMOKE PASS**.

**`probe351` D7 대조**(같은 자를 수리 전/후 트리에 각각 붙여 실행) — 건수는 **4건으로 같고 값만 줄었다**:

| D7 항목 | 전(391) | 후(415) |
|---|---|---|
| `#pfw>div.pf` covers:tabbar | 149 | **132** |
| `.pf-ring` covers:tabbar | 142 | **125** |
| `.pf-fill` covers:tabbar | 131 | **114** |
| `.pf-grid` covers:tuto | 88 | **71** |

391 §7 이 이미 판정한 대로 앞 셋은 «참이지만 결함이 아니다»(띠 1278 < 패널)이고, 네 번째(`covers:tuto`)는
**수리 전에도 있던 값**이라 내 변경과 무관하다 — 미션 배너를 덮는 자리는 **419** 의 것이다(338·344 규칙대로 대조로 확인).

## 6. 비평 1회차

（아래 §6-1 에 채워 넣음）

## 7. 교훈

1. **«구조적으로 못 넘는다» 는 축을 하나로 고정했을 때만 참이다.** 등재문의 «위·아래 각 31px 을
   구조적으로 못 넘는다» 는 **패널 높이를 상수로 두는 한** 참이었다. 슬랙을 반으로 가르는 축(391) 말고
   **슬랙 자체를 키우는 축**이 남아 있었고, 그 축은 «내용이 스크롤 그릇 안에 있는가» 를 물으면 바로 열린다.
2. **여백의 «작음» 은 프레임이 아니라 그 상자 자신의 내부 패딩으로 잰다.** 31px 이 결함인 이유는
   1600 이 짧아서가 아니라 **팝업 자신이 안쪽에 37·40 을 쓰고 있어서**다. 이 자는 프레임과 무관해서
   두 비평가가 서로 다른 말(«내부 패딩 37» · «최소선 40»)로 같은 값을 짚었다.
3. **«1600 전용 결함» 이라는 딱지는 재현으로 확인할 것.** 여유형 clamp 는 상한 항이 이기는 **구간 전체**에
   같은 값을 박는다 — 1841 도 31 이었다. 한 프레임만 재면 결함의 크기를 놓친다.
4. **되돌림 사본은 «부분 되돌림» 이 가장 위험하다.** 상자만 되돌린 사본은 실제로 존재한 적 없는 상태
   (내부 패딩 74)를 만들고, 거기서 역전 항이 **헛초록**이 된다. 되돌림 목록은 배열 하나로 묶고
   한 자리라도 못 찾으면 죽게 하는 것이 그 사고를 닫는 유일한 방법이다.
