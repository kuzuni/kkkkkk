# 680 — `tools/verify504.js` C2 «선언 ↔ 실측» 이 `poison` 에서 고정 초과 (46% > 40%)

> **1회차 (2026-09-01, sess-1506-6876 루틴 워커 A) — 완료.**
> **제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스 계수 0줄 · 세이브 이관 없음.**
> `probe680` **16/16 신설** · `tools/rul504.js` 신설(눈금 모듈) · `verify504` **12/13 FAIL → 13/13 PASS · ⏸1**.

---

## §1 결론 먼저 — 680 은 **622 의 세 번째 등재**였다

등재문은 «가설을 정하지 말고 재현부터» 라며 후보 셋(ⓐ 선언이 낡았다 · ⓑ 자의 표본 조건이
장판 계열에 불리해졌다 · ⓒ 밴드 40% 가 이 종에만 좁다)을 세웠다. **그 셋은 이미 622 가 갈랐고,
결론은 ⓐ 였다.** 같은 빨강의 이력은 이렇다:

| 언제 | 무슨 일 |
|---|---|
| 620 (09-01 02:40) | 눈금 판 초기화를 고치자 가려져 있던 이 빨강이 드러남 → **622 로 등재**(199 이관) |
| 622 (09-01 03:10) | **뿌리 진단 완료** — 후보 580·502·541 을 되돌림 대조로 전부 기각, «`poison` 선언 한 칸이 낡았다». 계수(`hits`+`m` 한 벌)는 **199 로 이관**. ⚠ 스스로 «[C2] 는 아직 빨갛다 — 안 닫았다» 라고 적음 |
| **680** (09-01 13:57) | 662·663 회귀 스윕이 **같은 빨강**을 다시 보고 다시 등재 |

⇒ **이 작업 단위의 실물은 «poison 을 고치는 것» 이 아니다**(그건 199 몫이고 이미 이관돼 있다).
실물은 **«뿌리 진단이 끝났는데도 게이트가 하드 빨강으로 남아, 회귀 스윕이 볼 때마다 다시
등재되는 구조»** 다. 그것을 닫는 것이 680 이다.

## §2 재현 — 622 의 판정은 오늘 트리에서도 선다

눈금은 622·620 과 **같은 504-RUL**(K=6 · 25초 · POP 23 · 스테이지 20 자유 전투).

```
poison  선언 29.36  ↔  실측 15.85~16.50  =  이탈 44~46%   (허용 ±40%)
K회 값  15.63/16.25/14.50/16.13/16.63/16.13   →  K회 폭 11~13%
```

* **«플레이키라 재실행하면 닫힌다» 를 끊었다** — K회 폭 13% 는 표준오차(폭÷2√K) **2.7%** 이고,
  밴드 ±40% 는 그 **14.6배**다. 이탈 46% 는 밴드를 **고정으로** 넘는다. (`probe680` [2]·[3-b])
* 등재문·622·620 이 각각 잰 값 **15.48 · 16.06 · 15.85~16.50** 이 전부 같은 자리다.

## §3 후보 ⓑ·ⓒ 를 **27종 전수**로 다시 기각했다

622 는 게이트 표본 **11종**으로 봤다. 이번엔 등재문이 요구한 대로 **27종을 나란히** 쟀다.

* **ⓑ 기각** — poison 의 형제 구조(장판·폭발·빔·관통) **7종이 전부 밴드 안**이다.
  특히 `flask` 는 **같은 `zones` 틱 경로를 타는 장판**인데 **12%** 다. 판(이속·크기·개체수·치명도)이
  밀렸다면 형제가 먼저 빨개졌어야 한다.
  → `flask 12% · meteor 4% · nova 14% · holy 2% · laser 18% · boom 6% · lance 4%`
* **ⓒ 기각** — 밴드가 좁아서가 아니라 **멀어서** 걸린다(위 14.6배).

## §4 처방 — «영원히 빨간 게이트» 를 ⏸199 대기 칸으로 (326 `ck199` 선례)

`SKILLS` 표는 **199 의 파일 구간**이다(ROUTINE «199 는 계수(상수 표)만»). 그래서 199 가 값을
넣기 전까지 **아무도 [C2] 를 초록으로 만들 수 없다.** `verify498` §6 이 같은 말을 이미 적어 뒀다 —
«계수 확정은 199 몫이라 여기서 빨갛게 하면 **영원히 빨간 게이트**가 된다».

⇒ **326 이 `sim177`·`sim249` 에 세운 `ck199` 칸과 같은 꼴**로, 실패로 세지 않되 값·이탈%·
«199 가 넣을 한 벌» 을 **매 실행 크게 찍는다**.

```
⏸199 C2-199 `poison` 선언 ↔ 실측 — 계수는 199 몫(622 이관 · 뿌리 진단 끝남)
      선언 29.36 ↔ 실측 15.854 = 이탈 46% (허용 ±40%)
      199 가 넣을 한 벌: hits 29.36 → ≈15.85 · m 0.7074 → ≈1.31
                        (= SK_DPS_REF 6.49 × cd 3.20 ÷ 15.85)
```

### 무르게 푼 수리가 **아님**을 네 겹으로 못박았다

| | 무엇을 막나 | 어디서 |
|---|---|---|
| ⓐ | **허용 오차는 한 칸도 안 넓혔다** — `TOL_FLOOR` 0.40 은 `probe504` [D] 가 «잰» 값 그대로 | `rul504.js` |
| ⓑ | 면제는 **이름이 적힌 종**뿐 — 다른 종이 벗어나면 그대로 빨갛다 | `probe680` [5-a] |
| ⓒ | 면제 조건이 **«낡은 선언 그 값일 때만»** — 199 가 `hits` 를 넣는 순간 스스로 하드로 돌아온다 | `probe680` [5-b] |
| ⓓ | 대기 종이라도 **밴드 안이면 애초에 안 걸린다** — «항상 켜진 우회로» 가 아니다 | `probe680` [5-d] |

⚑ **ⓒ 가 이 처방의 핵심이다.** «199 가 끝나면 이 줄을 지워라» 라는 **손목록**으로 짰으면 620 이
값을 치른 그 자리(«손으로 적은 목록은 또 뒤처진다»)를 그대로 반복했을 것이다. 면제를 **값 자체에
매달아** 두면 지울 사람이 필요 없다.

### 199 가 넣을 값이 «닫히는» 값인지도 확인했다 (`probe680` [4])

622 의 경고(«`hits` 만 갈면 [D1] 이 깨진다»)를 **제품 표에 손대지 않고** 페이지 안에서만
주입해 재확인했다:

| 무엇을 넣었나 | [D1] 등급 안 최대/최소 | 판정 |
|---|---|---|
| 지금 | 1.0001 | 초록 |
| `hits` 만 | **1.8494** | **빨강** (622 가 잰 1.8281 과 같은 자리) |
| `hits` + `m` 한 벌 | 1.0001 | 초록 |

## §5 곁다리 — 눈금을 **한 곳으로 뽑았다**(`tools/rul504.js`)

27종을 재려면 하네스가 필요한데 길이 둘이었다 — 베껴 적거나, 선언을 한 곳으로 뽑거나.
**베끼는 쪽은 이 저장소가 네 번 값을 치른 자리**다(402 «표 두 벌» · 508 «베껴 적던 사슬» ·
553 «시뮬 4벌 + 게이트 1벌» · 620 «손으로 적은 초기화 목록»). 그래서 뽑았다.

* `verify504.js` **117~197행을 한 줄도 안 바꾸고** 옮겼다. 옮긴 것이 맞는지는 `probe680` [0] 이
  **매 실행 대조**한다(게이트가 모듈을 읽는가 · 게이트 안에 하네스 사본이 0건인가).
* 판정(`c2Split`·`held199`)도 모듈에 뒀다 — `probe680` [5] 가 **게이트가 실제로 부르는 그 함수**를
  시험한다. 사본을 시험하면 시험이 통과해도 게이트가 통과한다는 뜻이 아니다.

## §6 이 작업 단위 **밖**으로 등재한 관측 2건

| 번호 | 무엇 | 왜 여기서 안 고쳤나 |
|---|---|---|
| **695** | `orbit`·`aura`(**cd 0 지속형**)·`whirl` 이 밴드 밖 — 이탈 75~78% · K회 폭 **62~132%** | 게이트 표본 11종에 **cd 0 이 하나도 없어 눈금이 한 번도 안 재 본 구조**다. poison(폭 11~13%)과 성질이 다르고, «선언이 낡았나 / 눈금이 지속형에 안 맞나» 는 **재현이 정할 문제**다(338 규칙). 값을 여기서 손대면 «재현 없이 처방» 이 된다 |
| **696** | `sim177`·`sim249`·`verify177` 이 **즉사** — `const OFF_MAX_H` 정규식을 못 찾는다 | 199 의 **21회차가 `OFF_MAX_H` 를 선언째 폐지**(결3 ⓑ)했는데 하류 셋이 안 따라왔다. **내 변경과 무관함이 구조적으로 확실하다 — 이 세션의 커밋은 `index.html` 을 0줄 건드렸다**(`git log -- index.html` 에 내 커밋 없음). 199 의 파일 구간이라 선점하지 않는다 |

⚠ 둘 다 **등재만 · 선점 안 함**(199 판정 규칙 ③ — 미착수가 적을 때 새 곁다리는 등재만 한다).

## §7 게이트·회귀

| 자 | 결과 |
|---|---|
| `verify504` | **12/13 FAIL → 13/13 PASS · ⏸1 → 199 대기** |
| `probe680` | **16/16 신설** |
| `verify484` | 16/16 무수정 |
| `verify260` | 24/24 무수정 |
| `verify86` | 38/38 무수정 |
| `verify193` | 79/79 무수정 |
| `verify114` | 46/46 무수정 |
| `verify109` | 65/65 무수정 |
| `smoke` | **SMOKE PASS** |
| `sim177` · `sim249` · `verify177` | 즉사 — **696 로 등재**(§6 · 내 변경과 무관) |

## §8 다음 세션이 볼 것

* ⚑ **199 에게** — `poison` 한 벌은 `verify504` 를 돌리면 **매 실행 화면에 찍힌다**. 표를 뒤질 필요가
  없고, 값을 넣는 순간 ⏸ 칸이 **스스로** 하드 단언으로 돌아와 넣은 값을 검산해 준다.
* ⚑ **회귀 스윕을 도는 세션에게** — `verify504` 가 `⏸199 1칸` 을 찍는 것은 **정상**이다.
  그 줄은 «아직 안 고쳐진 빨강» 이 아니라 «뿌리 진단이 끝나고 199 가 값만 넣으면 되는 자리» 다.
  **네 번째로 등재하지 마라.**
* ⚑ **새 종을 눈금으로 재는 세션에게** — 게이트 표본 11종에는 **cd 0 지속형이 없다**.
  27종으로 넓히면 `orbit`·`aura`·`whirl` 이 곧바로 나온다(695).
