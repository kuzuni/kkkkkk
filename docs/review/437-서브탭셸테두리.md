# 437 — 서브탭 셸(`.stabs`) 검정 테두리 두께

> 등재문(2026-08-30, sess-0621-13887 워커 F — 409 1회차 비평가 **CV·CW** 2인 독립 일치 + 384 1회차 **BB**):
> «셸 테두리 검정이 ref **7.91** ↔ 우리 **5.69~6.00**(−28%) · ref 안에서는 «셸 테두리 = 알약 좌우 검정»
> (7.91 ↔ 7.82, Δ0.09)인데 우리는 5.69 ↔ 7.22 로 어긋난다».
> ⚠ 이 자리는 **352 §2 가 한 번 기각했다** — «ref 의 순검정은 6, 그 바깥에 AA 한 줄. 느슨 문턱으로 세면 8».
> 그래서 **1회차는 그 기각을 다시 재는 것**이다(338 규칙 — 처방 전에 재현).

---

## 1회차 (2026-08-30, sess-0858-2521 워커 D) — 재현

### 1-1. 자를 먼저 검산했다 — «우리 캡처는 진실을 CSS 가 안다»

문턱 논쟁(«순검정 6 이냐 느슨 8 이냐»)을 말로 풀지 않고, **자 넷을 동시에 대고 우리 PNG 로 먼저 검산**했다.
우리 쪽 진실은 CSS 가 알고 있다 — `.stabs{border:6px}` · `.stab.on{box-shadow:inset 7px 0 0 #000}`.

| 자 | 무엇 | cap 셸(진실 6.00) | cap 알약(진실 7.00) |
|---|---|---|---|
| `pure` | 순검정 ≤4 런 (352 가 쓴 자) | 6.00 | 7.00 |
| `loose` | 느슨 ≤24 런 (352 가 기각한 자) | 6.00 | 7.00 |
| `x50` | 50% 교차 부분화소 (CV·CW 의 자) | 6.00 | 7.00 |
| `cov` | 커버리지 적분 (352 4회차 AW 의 자) | 6.00 | 7.01 |

**넷 다 오차 0.00~0.01 이다.** 우리 캡처는 PNG · DSF1 · 정수 좌표라 AA 가 아예 없어서 어떤 자를 대도 같은 값이 나온다.
⇒ **문턱 논쟁은 ref(JPEG) 쪽에서만 벌어진다.** 그러니 «어느 자가 옳으냐» 를 다투는 대신
**ref 를 색 분류(band map)로 갈라 읽어** 무엇이 무엇인지 이름을 붙였다.

### 1-2. ref 를 색으로 갈라 읽으면 문턱이 필요 없다

`.stabs` 세로 단면을 팔레트(K 검정 · B 베벨 #634F37 · F 채움 #4B3E2D · D 바닥띠 #413122 ·
R 림 #705F4B · S 셸바닥 #61523D · W 시트 바깥)로 분류한 것 — **07 스킬 시트, 활성 알약 «스킬» 열**:

```
ref x330  W..2019  R2020(1)  K2021..2027(7)  B2028..2034(7)  F2035..2097(63)
                   B2098..2102(5) S2103 F2104  D2105..2111(7)  K2112..2117(6)  F2118(1)  W2119..
cap x400  W..1960            K1961..1966(6)  B1967..1973(7)  F1974..(…)
                   B2038..2044(7)            D2045..2051(7)  K2052..2057(6)  W2058..
```

읽히는 대로 적으면 **ref 바 = K7 · [B7 F63 B7 D7] · K6(+AA)** 이다.

* **검정 테두리 = 7** (상 2021..2027 · 하 2112..2117 + AA 2118) — 우리는 **6**.
* **칸(패딩) 상자 = 84** (2028..2111 = 7+63+7+7) — 우리는 **85**.
* **바 바깥 = 98** (부분화소 50% 교차로 2020.24 ↔ 2118.27 = **98.02**) — 우리는 **97.00**.

세 값이 **서로를 검산한다**: 7 + 84 + 7 = 98. 우리 쪽도 6 + 85 + 6 = 97 로 닫혀 있다.
⇒ 등재문의 방향(**ref 가 우리보다 두껍다**)은 **맞다**. 다만 **값은 7.91 이 아니라 7** 이고,
그 대신 **바 바깥 높이가 97 이 아니라 98** 이라는 것이 같이 나왔다 — 셋은 한 덩어리다.

### 1-3. «7.91» 의 정체 — 활성 알약이 없는 열에서만 한 칸이 더 붙는다

같은 자(`cov`)를 열마다 대면 값이 **두 무리**로 갈린다:

| 자리 | ref 상 검정 | ref 하 검정 |
|---|---|---|
| 활성 알약이 닿는 열 (x400·x470) | **7.00** | 7.0 |
| 알약이 없는 열 (x150·x700) | **8.38** | **7.00** |

색 분류가 그 1.4 의 정체를 말한다 — 알약 없는 열은 `K2021..2028(8) S2029 R2030..2035(6)` 이고,
알약 열은 `K2021..2027(7) B2028…`. 즉 **2028 한 줄이 셸 안쪽의 어두운 립**이고, 활성 알약이 그 자리를 덮는다.
좌·우도 같다 — 직선 구간(y 2064..2074)에서:

| ref | 좌 | 우 | 활성 칸 |
|---|---|---|---|
| 06 장비 | **7.01** | 8.39 | 첫 칸 |
| 07 스킬 | 8.38 | 8.39 | 가운데 칸 |
| 23 훈련 | **7.00** | 8.39 | 첫 칸 |

**활성 알약이 닿는 면은 7.0 · 안 닿는 면은 8.4** — 세 화면·네 면이 예외 없이 이 규칙을 따른다
(378 이 «끝 칸이 활성이면 검정을 셸에 넘긴다» 로 적어 둔 것과 같은 자리다).
하변만 립이 없다(7.00).

⇒ **CV·CW 의 7.91 은 «7(테두리) + 1.4(안쪽 립)» 을 한 덩어리로 읽은 값**이고,
**352 의 «순검정 6» 은 ref 의 AA 한 줄을 통째로 버린 값**이다. 둘 다 반쪽이었다.

### 1-4. 등재문의 ⓑ(내부 일관성)는 **기각**된다

CV 의 논거는 «ref 안에서 셸 테두리 = 알약 좌우 검정인데 우리는 아니다» 였다. 재현 결과:

| | 셸 테두리 | 알약 좌 검정 | 비 |
|---|---|---|---|
| ref | **7.00** | **6.99** | **1.00** |
| cap | 6.00 | 7.00 | **0.86** |

ref 는 둘이 **같은 7** 이 맞다 — 그런데 **우리가 어긋난 이유는 알약이 두꺼워서가 아니라 셸이 얇아서**다.
알약 7 은 이미 ref 와 Δ0.01 이고(352 4회차가 세운 «7 규약»), 어긋난 것은 셸 6 하나다.
⇒ **알약을 건드리면 안 된다.** 등재문이 «둘 다 7.9» 라고 읽어 알약까지 올렸다면 그쪽이 Δ0.9 로 틀어졌을 것이다.

### 1-5. 세 화면 교차검증 (같은 부품 · 서로 다른 스크린샷)

`cov` 로 바 안쪽 전 열을 훑은 값(중앙값):

| ref | 상 | 하 |
|---|---|---|
| 06 장비 | 8.49 | 6.97 |
| 07 스킬 | 8.51 | 6.98 |
| 23 훈련 | 8.50 | 6.98 |
| cap 06·07·10·13 | 6.00 | 6.00 |

세 스크린샷이 **8.50 / 6.98 로 소수 둘째 자리까지 같다** — JPEG 개별 잡음이 아니다.

### 1-6. 처방

**`.stabs` 세 값을 한 덩어리로 옮긴다** — 셋 중 하나만 고치면 나머지 둘이 어긋난다.

| | 지금 | ref | 바꾼다 |
|---|---|---|---|
| 바 바깥 높이 | 97 | 98.02 | **98** |
| 검정 테두리 | 6 | 7.00 | **7** |
| 칸(패딩) 상자 | 85 | 84 | **84** |

⚑ **호스트 앵커가 이 처방을 공짜로 만든다** — 여섯 호스트 중 다섯이 `bottom:42px` 다.
바 하변(1960+98 = 2058)은 **Δ0** 이고 상변만 1961 → **1960** 으로 올라간다.
테두리가 6 → 7 이 되면서 **패딩 상자 상변은 1967 그대로**이고(1961+6 = 1960+7), 하변만 2052 → 2051 이다.
⇒ 알약 안 라벨·배지·구분선·`sk-lock` 은 **절대 좌표가 한 픽셀도 안 움직인다**(전부 패딩 상자 기준 top 앵커).
ref 대조: 바 바깥 상변 ref 2020.24 ↔ 우리 2020.00(캡처 1960) = **Δ0.24**(지금은 Δ0.74) ·
하변 ref 2118.27 ↔ 우리 2118.00 = **Δ0.27**.

**안 건드리는 것**: 알약 검정 7 · 알약 베벨 7 · `PILL_LIP 14` · 알약 반경 30 · 셸 반경 43 ·
`.stab-sep top:16` · `.stab>.bdg top:-6` · 라인하이트 87/77.
`.stab.on::before/::after` 의 띠는 **알약 자기 상자 기준**이라 84 로 줄어도 규약(0..7 · 77..84)이 자동으로 따라온다.

**곁다리로 나온 것(437 범위 밖 — 따로 등재)**: ⓐ 셸 안쪽 **1.4px 어두운 립**(상·좌·우, 하변 없음 —
활성 알약이 덮는 면만 없다) · ⓑ 바 바깥 **폭** ref 948.33 ↔ 우리 950.00(**+1.67**).

### 1-7. 자

`python3 tools/probe437.py` — 자 넷 동시 + 우리 캡처 검산 + 세 화면 교차 + 기하 검산.
캡처는 `node tools/cap96.js`(`docs/review/96-full-*.png`, .gitignore 로 커밋 안 됨).

---

## 2회차 (2026-08-30, sess-0858-2521 워커 D) — 제품 + 이관

### 2-1. 제품 (`index.html` 4자리)

| 자리 | 전 | 후 |
|---|---|---|
| `.stabs{height}` | 97 | **98** |
| `.stabs{border}` | `6px solid #000` | **`var(--sb) solid #000`** (`--sb:7px`) |
| `.stabs{background}` 아래 두 정지점 | `78px 85px` / `85px` | **`77px 84px` / `84px`** |
| `.stabs>*{height}` | 85 | **`100%`**(패딩 상자 파생 — 아래 2-3) |
| `.stab.on{background}` 네 정지점 | `0 7 / 7 71 / 71 78 / 78 85` | **`0 7 / 7 70 / 70 77 / 77 84`** |

⚑ **호스트 앵커 덕분에 절대 좌표가 거의 안 움직인다** — 여섯 호스트 중 다섯이 `bottom:42px` 라
바 하변은 **Δ0**, 상변만 1961 → 1960. 테두리가 1 두꺼워지므로 **패딩 상자 상변은 1967 그대로**
(1961+6 = 1960+7)이고 하변만 2052 → 2051 이다. 라벨·배지·구분선·`sk-lock` 은 전부 패딩 상자
top 앵커라 **한 픽셀도 안 움직였다**(`.stab-sep` 실측 x775 y1983 — 변경 전과 같은 값).
`.rn-subs` 만 `top:34` 상단 앵커라 하변이 1 내려간다(`verify271` 이관).

수리 후 재측정(`probe437` ⓕ): **부분화소 바깥 높이 ref 98.02 ↔ 우리 98.00** · 색 분류
`K7 · B7 · F63 · B7 · D7 · K7` 로 ref 와 띠 구성이 같아졌다(전: `K6 · B7 · F64 · B7 · D7 · K6`).

### 2-2. `.stab.on` 정지점이 안 따라오던 것 — 1px 함정

칸 상자를 85 → 84 로 줄이자 **바닥 어두운 띠만 7 → 6** 이 됐다. `.stab.on` 의 네 정지점이
**절대 px** 라 `78px 85px` 가 84 에서 잘린 것이고, 다른 셋은 위 앵커라 멀쩡해서 눈에 잘 안 띈다.
352 의 «7 규약» 은 두께 이야기지 정지점 이야기가 아니었다 — 정지점을 한 칸씩 당겨 복원했다.
(`.stab.on::before/::after` 는 자기 상자 기준이라 손댈 것이 없다.)

### 2-3. 이관 — 8개 자, 108항

착수 직후 한꺼번에 빨개진 것:
`verify47` 28 · `verify352` 20 · `verify337` 17 · `verify379` **31** · `verify384` 9 ·
`verify409` 1 · `verify271` 1 · `verify389` 2.

| 자 | 무엇이 걸렸나 | 어떻게 이관했나 |
|---|---|---|
| `verify47` | `BAR_H 97 · BAR_BORDER 6 · CELL_H 85` | 상수 셋 → 98/7/84 (판정식 0줄) |
| `verify352` | `BORDER 6` + 리터럴 97·85·71·78 | `BORDER 7 · BAR_H 98 · CELL_H 84` 로 빼고 리터럴을 식으로 |
| `verify337` | 같은 셋 + 되돌림 시험 R1 | 상수 셋 + **R1 을 리터럴 99 → 식 `BAR_H+2`** · **R1b 신설** |
| `verify379` | **격자 상수 12·6·4·2·3** | 제품 CSS 를 `--sb` 로 (아래) — 자는 0줄 |
| `verify384` | `PILL_H 85` + `ROWS rel 68·74` | **rel 을 «아래 코너 기준» 으로** (`PILL_H−17`·`PILL_H−11`) |
| `verify409` | `PILL_H 85` | 상수 하나 (각도 표본은 코너 원 기준이라 불변) |
| `verify271` | `.rn-subs` 부품 높이 97 | 98 (상단 앵커라 상변 Δ0) |
| `verify389` | 되돌림 시험 R-b·R-d 의 기대 리터럴 | **주입값에서 되돌리는 식**으로 (`bw+224−C` · `bw+938`) |

⚑ **379 가 이 작업의 가장 큰 배움이다.** 379 는 «바 폭·테두리가 바뀌어도 칸이 저절로 따라간다»
고 적어 뒀지만 **폭에만 참**이었다 — 칸 격자 상수(`calc(33.3333% + 4px)`·`left:-6px` …)가 전부
«테두리 6» 을 손으로 접어 넣은 값이라 테두리가 7 이 되자 칸 폭이 263.98(바깥/3 = 264.67) ·
끝 칸 우끝이 792.98(바깥 794)로 밀렸다. **상수를 다시 손으로 적지 않고 b 를 식으로 되돌렸다** —
`--sb` 한 손잡이로 sp2·sp3·4칸 격자 · 활성 오버행까지 12줄. 다음 번 테두리 변경은 공짜다.

⚠ **384 는 «값을 바꾸는 이관» 이 아니라 «자리를 옮기는 이관» 이었다.** 감김은 **아래 코너**
(중심 y = 알약높이 − 30)의 호에서 나오므로, rel 을 위에서 세면 상자가 1 줄어들 때 표본이 호 위에서
한 칸 미끄러진다 — 실제로 `[5]`(«rel 74 가 rel 68 보다 두껍다»)가 **9 → 8 로 뒤집혀** 빨갰다.
숫자만 68→67 로 갈았으면 «왜» 가 안 남는다.

### 2-4. `.stabs>*{height:100%}` — 결속을 CSS 항등식으로

칸 높이를 `84px` 리터럴로 다시 적는 대신 **패딩 상자에서 파생**시켰다. 절대배치 자식의
`height:100%` 는 담는 상자의 **패딩 상자**(= 바깥 − 테두리×2)라, «바깥 − 칸 = 테두리×2» 가
CSS 안의 항등식이 된다 ⇒ **셋 중 하나만 옮기는 사고가 원리적으로 안 난다.**
`.stab-sep`(높이 54)은 뒤 규칙이 그대로 덮는다.

---

## 3회차 — 게이트 신설 `verify437` (50항)

세 값이 47·352·337 에 **따로따로** 흩어져 있던 것이 이번 사고의 뿌리다. 값을 다시 적어
초록으로 되돌리는 것만으로 끝내면 **셋이 서로 어긋나도 초록인 게이트**가 된다(LESSONS 328-330).
그래서 새 자가 무는 것은 세 «값» 이 아니라 그 **결속**이다.

* **[G]** 호스트 8곳 × «테두리 7 · 바깥 98 · 칸 84» + **★ 바깥 − 칸 = 테두리×2**
* **[P]** 찍힌 픽셀(350 처방 — 캡처를 data URL 로 되돌려 읽는다): 상·하 검정 런 = 7 ·
  그 사이 = 84 · **음성항**(바 바로 바깥 한 줄은 검정이 아니다)
* **[S]** `--sb` 가 진짜 손잡이인가 — `--sb:11` 을 주입하면 테두리도 **칸도 격자도** 따라온다
* **[R]** 되돌림 6항 —
  `R-a` 옛 셋(97/6/85) 통째 · **`R-a2` 그 옛 셋은 «자기들끼리는» 결속이 맞다**(그래서 값 항이
  따로 필요하다는 것을 못박는다) · `R-b`·`R-c` 하나만 되돌리면 칸이 **따라온다**(파생이 살아 있다) ·
  **`R-d` 칸 높이 리터럴 부활 = 결속을 깰 수 있는 유일한 길** · `R-e` `--sb` 를 우회해
  `border-width` 만 바꾸면 **격자가 어긋난다** · `R-f` 원복

⚠ **1회차 실행에서 «13 재화 칸 89.0» 이 한 번 나왔다** — 입장 연출·알약 전환이 도는 동안 읽은
값이다. **플레이키 게이트를 새로 만들지 않는다**(392·395·408·417·421 계열).
⚠ 그런데 47·337 의 `settle`(무한 루프 연출을 **이름으로** 걸러내고 나머지 `getAnimations().finished` 를
기다린다)을 그대로 가져오니 **영영 안 끝났다** — 16분을 매달리다 죽였다. 이 페이지에는 무한 루프
연출이 여럿이고(레드닷 점멸 · 던전 썸네일 bob …) 이름 목록으로는 다 못 걸러낸다.
⇒ **`finished` 를 기다리지 않는 자로 바꿨다** — «읽은 값이 두 번 같을 때까지 다시 읽는다»
(상한 10회 · 160ms). 무엇이 흔들리든 상관없고, **원리적으로 매달릴 수 없다.**

## 회귀

`verify437` **50/50 신설** · 이관 8종 전부 초록(`verify47` 175/175 · `verify352` 62/62 ·
`verify337` 92/92 · `verify379` 101/101 · `verify384` 96/96 · `verify409` 120/120 ·
`verify271` 62/62 · `verify389` 204/204) · 무수정 회귀
`verify96` 93/93 · `verify46` 70/70 · `verify378` 48/48 · `verify164` 31/31 · `verify166` 73/73 ·
`verify202` 42/42 · `verify203` 85/85 · `verify209` 61/61 · `verify210` 96/96 · `verify283` 41/41 ·
`verify299` 27/27 · `verify328` 19/19 · `verify329` 26/26 · `verify335` 24/24 · `verify341` 32/32 ·
`verify404` 157/157 · `verify429` 50/50 · `verify45` 39/39 · **SMOKE PASS**.

## 곁다리 등재

* **450** — 셸 **안쪽 어두운 립 1.4px**(상·좌·우, 하변 없음). 활성 알약이 닿는 면은 7.0,
  안 닿는 면은 8.4 로 갈린다(06 좌 6.98 ↔ 우 8.50 · 23 좌 6.99 ↔ 우 8.51). 우리는 립이 0 이라
  ref 의 «상 8.50 / 하 6.98» 비대칭이 재현되지 않는다. 측정표 07 §9 「바 상단 하이라이트 8px
  (2028~2034)」 이 그 립을 하이라이트로 세고 있던 것도 같이 정정한다(실측 R 은 2030~2035 = 6).
* **바 바깥 폭** — ref 948.33 ↔ 우리 950.00(**+1.67**). 437 범위(테두리 두께) 밖이라 안 건드렸고,
  450 행 꼬리에 같이 적어 둔다.
