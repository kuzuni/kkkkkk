# 742 — `verify471` [T] 두 항이 실행마다 갈린다 (게이트 플레이키)

> 워커 A · sess-1811-25464 · 2026-09-01 · 지시서 [3]-**(가)** 기계적/게이트 작업 — **비평가 없음**
> 제품 `index.html` **0줄** · 고친 파일은 `tools/cap471.js` **한 곳**(+ 신설 자 둘)

## 1회차 — 재현부터 (338 규칙: 처방을 따르기 전에 제품에게 묻는다)

### 등재문이 준 것
같은 트리에서 `verify471` 이 **47/49 ↔ 49/49** 로 갈렸고, 빨간 회차의 값은
- `[T] 시트의 «그림 코너» 가 호스트 상자 **밖**이 아니다` — **#1 146 > 145**(1px 초과)
- `[T] 시트와 자가 같은 코너를 본다 (≤1px)` — **드리프트 10.00px**

두 항이 **한 사건**이라는 것이 첫 단서다: 초록 회차의 #1 잉크 우변은 **136** 이고 146 − 136 = **10.00** 이다.
즉 «상자 밖» 도 «드리프트» 도 `cap471` 이 그 한 칸에서 **10px 오른쪽을 읽은** 한 번의 사건이 낸 두 개의 빨강이다.

### 재현 — `tools/probe742.js` 신설
브라우저를 다시 띄우지 않고(회차 간 유일한 차이가 «시간» 이 되게) 한 판에서 같은 칸을 여러 번,
두 자매 자의 규칙(ⓐ `cap471` = 닷 아닌 자식 전부 숨김 · ⓑ `probe471c` = `.si` 만 숨김)으로 각각 잰다.

| 회차 | cap 규칙 우변 | 471c 규칙 우변 | 드리프트 | 상자 밖? |
|---|---|---|---|---|
| 20회 중 18회 | 136 | 136 | 0 | 안 |
| #4 | **151.5** | 136 | **15.5** | ⚠ 밖 |
| #11 | **151** | 136 | **15** | ⚠ 밖 |

⇒ **자연 경합으로 재현됐다**(20회 중 2회 · 앞선 14회 판에서도 2회). 등재문의 «실행마다 갈린다» 가 확인됐다.

### 갈래 가르기 — 등재문의 ⓑ 를 먼저 (자를 고치기 전에 제품에게 묻는다)
**«경계에 앉은 것» 은 제품 결함이 아니다.** 튀지 않은 회차의 값이 답을 준다 —
`.ibtn` #1 의 글리프 잉크 우변은 **136**, 호스트 상자 우변은 **145** 로 **9px 안쪽**이다.
«146 > 145» 는 제품이 1px 삐져나온 것이 아니라 **그 회차에 남의 화소를 센 것**이다.
⇒ 문턱(1px · ≤1px)은 손댈 이유가 없다. 등재문의 «문턱을 그냥 넓히는 것은 반려» 를 지킬 근거가 여기 있다.

### 뿌리 — «재는 동안 이웃 칸이 뛰고 있었다»
`probe742` 에 «상자 밖에 선 화소의 페이지 좌표 → `elementsFromPoint`» 를 달아 물어봤다:

```
⚠ cap 규칙이 «상자 밖» 화소를 셌다 (12개+) — 145.5,301.5 · 145.5,302 · 146,302 · 146,302.5
```

튄 화소는 **(145.5~146, 301.5~303)** — 재는 칸(#1, y 176..290)이 아니라 **다음 칸(#2, y 310~)의 닷 링**이다.
사슬 셋:

1. `cap471.grab()` 은 7회차에 «호스트 subtree + **조상**» 을 세우게 했다 — **형제 칸은 안 센다.**
2. 잉크는 «호스트 상자 + 여백 30» 클립에서 재는데 02 사이드는 **칸 높이 114 · 피치 134** 라
   아래 여백이 **다음 칸을 10px 먹는다**(30 − 20).
3. 그 자리에 이웃 닷의 바깥 링(`box-shadow` 0 0 0 7.5px #000)이 있고, 그 닷은
   **`jzDotPulse 2s ease-in-out infinite`**(`index.html:13922`) 로 뛴다 — 실측 6칸 중 **4칸**이 돈다.

⇒ A↔B 사이에 위상이 바뀌면 그 링이 «달라진 화소» 로 잡히고, 주기가 2초라 A↔A2 는 같은 위상으로
돌아와 **배제 규칙(j≤10)마저 통과**한다. 이웃의 링이 **내 글리프의 잉크 우변**으로 실린다.

⚑ **왜 «가끔» 이었나도 키프레임이 말해 준다** — `jzDotPulse{0%,72%,100%{scale:1} 84%{scale:1.14}}` 라
한 주기의 **72% 가 평평**하다. B 가 봉우리 구간(28%)에 떨어지고 A·A2 는 평평한 구간에 떨어져야 사건이 난다
⇒ 실측 발생률 **2/20 · 2/14 ≈ 10%** 와 맞는다. **«플레이키» 는 확률이 아니라 이 구조의 그림자였다.**

## 처방 — `cap471.inkCorner()` 가 세 장을 찍기 **직전에 화면 전체**를 센다

`tools/cap471.js` 에 `settleAll()` 을 신설하고 A/B/A2 트리플 **직전**에 부른다.
규칙은 `probe471c` 와 **같다**(무한 → 0프레임 + pause · 유한 → finish). 이 파일의 원칙이 원래 «자매 자를 맞춘다»(385)이고,
`probe471c` 는 처음부터 화면 전체를 세우고 있었다 — **갈린 쪽은 `cap471` 하나였다.**

- 장면마다 새 애니가 나므로 «한 번 세워 두기» 로는 부족하다 ⇒ 트리플마다 센다.
- 되돌림 손잡이 `P471_NOSETTLE=1` 을 남겼다(§R 용 · 평소 미사용).
- **문턱은 한 칸도 안 넓혔다** — `verify471` [T] 의 «상자 밖 0» 도 «≤1px» 도 그대로다.
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄.

## 게이트 — `tools/verify742.js` 신설 (15/15 PASS)

| 절 | 무엇을 못박나 |
|---|---|
| [S] | 처방이 소스에 남아 있는가(`settleAll` 존재 · 규칙이 `probe471c` 와 같음 · **세 장 직전**에 부름) |
| [1] | **노출이 실재한다** — 6칸 · 피치 134 − 높이 114 ⇒ 여백 30 이 다음 칸을 **10px** 먹는다 · 옆 칸(#2)이 ∞ 맥박을 돈다 · 그 닷이 상자 밖 링을 두른다 |
| [2] | **결정적 양성 대조** — 이웃 닷 위상을 A↔B 사이에 봉우리(progress 0.84)로 옮기면 잉크 우변이 **149.5**(상자 145) 로 튄다 |
| [3] | 수리 — `settleAll` 뒤 «running» 애니 **0개** (세 장이 같은 프레임을 본다) |
| [4] | 수리 확인 — **8회 반복 전부 136 · 폭 0px · 상자 밖 0회** |
| [R] | 되돌림 — 맥박은 되살아날 수 있고(∞ 4개), 세우기 없이 같은 위상 이동을 주면 **다시 149.5** · 세운 채로는 **136** |

⚠ [2]·[R] 은 «몇 번 돌리면 나온다» 가 아니라 **결정적**이다(560 규칙 — 자연 경합 10% 에 기대면 시험 자체가 플레이키해진다).
위상 손잡이 `tick(frac)` 은 **실시간이 하는 일만 대신한다** — «도는» 애니만 옮기고 **멎은 것은 안 옮긴다**.
그래서 이 손잡이가 수리를 우회하지 못하고, [R] 의 음성항이 «수리가 실제로 막는다» 를 말한다.

## 검증

- `verify742` **15/15 PASS** (신설)
- `probe742` 신설 — 수리 전 20회 중 2회 튐 / 세운 채 20회 **폭 0px · 상자 밖 0회**
- `verify471` — **4회 연속 49/49 PASS**(아래 표) · **SMOKE PASS**

### `verify471` 반복 실행 — 등재문이 요구한 «착수 세션은 먼저 4회 이상 돌려 분포를 잡을 것»

| 회차 | 시각(UTC) | 결과 | [T] 그림 코너 | [T] 자매 자 드리프트 |
|---|---|---|---|---|
| 1 | 18:28 | **49/49 PASS** | 6칸 전부 상자 안 (3.5~9.0px 왼쪽) | 0.50px |
| 2 | 18:35 | **49/49 PASS** | 6칸 전부 상자 안 (3.5~9.0px 왼쪽) | 0.50px |
| 3 | 18:41 | **49/49 PASS** | 6칸 전부 상자 안 (4.0~9.0px 왼쪽) | 0.50px |
| 4 | 18:47 | **49/49 PASS** | 6칸 전부 상자 안 (3.5~9.0px 왼쪽) | 0.50px |

수리 전 등재문 실측은 같은 트리 3회가 **47/49 · 47/49 · 49/49**(+ 수리 전 트리 1회 49/49)였다.
⇒ 4회 전부 초록이고 **[T] 두 항의 값도 회차마다 같다**(드리프트 0.50px 고정 · 남은 0.5px 는
두 자의 클립 원점이 다른 데서 오는 반올림 한 칸이고 문턱 1px 안이다).

### 회귀

- `node tools/smoke.js` — **SMOKE PASS**
- `node tools/verifyProgress.js` — 종료 코드 0
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄 · 세이브 이관 없음
- 다른 자매 자는 손대지 않았다 — `probe471`(`--ink`)은 이미 `drain()` 으로 **화면 전체**를 세우고 있었고
  `probe471c` 도 그렇다. **세우지 않던 것은 `cap471` 하나뿐**이었다(전수 확인).

## 남은 것 / 다음 세션에게

- 이 수리는 **움직임을 없앤 것**이지 «클립이 이웃을 먹는 것» 을 없앤 것은 아니다.
  `[1]` 이 그 산수(피치 134 − 높이 114 = 20 < 여백 30 ⇒ **10px**)를 항으로 세워 뒀으니,
  훗날 이웃 칸에 **애니가 아닌** 무엇(예: 조건부로 켜지는 뱃지)이 A↔B 사이에 바뀌면 같은 병이 돌아온다.
  그때의 처방은 여백이 아니라 **이웃 상자를 마스크로 빼는 것**이다(여백을 줄이면 이 자의 존재 이유인
  «상자 밖으로 나간 잉크» 를 못 본다).
- `P471_NOSETTLE=1` 은 §R 전용 손잡이다. 평소 실행에 쓰면 옛 병이 그대로 돌아온다.
