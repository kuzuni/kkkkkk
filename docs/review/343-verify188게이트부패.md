# 343 — `tools/verify188.js` 싱크 2건 실패(63/65) «버그(게이트 부패)»

- 등재: 2026-08-29 (sess-1633-16734 워커 B — 338 곁다리 관측)
- 처리: **2026-08-28, sess-1716-28818 워커 A** — 1회차, **제품 `index.html` 0줄**
- 결과: `verify188` **63/65 → 79/79 PASS**

---

## 1. 무엇이 빨갰나

```
FAIL ① 싱크 — 04 스킬 피해  →  false (want true)
FAIL ① 싱크 — 07 펫 피해    →  false (want true)
VERIFY188 63/65 FAIL
```

게이트 `SINK` 표의 두 항이 찾던 것:

```js
['04 스킬 피해', /피해 <b>' \+ fmtB\(skillDmg\(it\)\)/],
['07 펫 피해',  /피해 <b>' \+ fmtB\(petDmg\(it\)\)/],
```

## 2. 뿌리 — 게이트 부패다(등재문 판단이 맞았다)

제품은 **내내 `fmtB` 를 부르고 있었다.** 굳은 것은 게이트의 «문자열 조립 형태» 쪽이다.

| 시점 | 08 세부 팝업의 피해량 자리 |
|---|---|
| 188 당시 | `'피해 <b>' + fmtB(skillDmg(it)) + '</b>'` — 설명문에 이어 붙이는 조립문 |
| **262** | «레벨을 타는 칸» 을 **함수 한 벌**로 뽑았다 → `const dmgNow = () => own ? fmtB(skillDmg(it)) : '—';` (통짜 재렌더와 홀드 중 `mdLive` 갱신이 반드시 같은 식을 보게 하려는 것) |
| **268** | 08 껍데기를 **`.sk-ct` 2열 표**(«쿨타임 \| 피해량»)로 통일 → 값은 표의 4번째 칸에 들어간다 |

즉 «`'피해 <b>' +` 바로 뒤» 라는 **자리**가 두 번의 개편으로 사라졌고, 정규식만 그 자리에 남았다.
`fmtB(skillDmg(it))` · `fmtB(petDmg(it))` 호출 자체는 한 번도 사라진 적이 없다.

**338 과 무관함**은 등재문이 이미 대조 실행으로 확인해 두었고(338 수리 전 커밋에서도 63/65),
이번 착수 시점에서도 제품을 한 줄도 안 건드린 채 79/79 가 됐으므로 재확인됐다.

## 3. 처방 — «자리만 옮기고 묻는 것은 그대로»

LESSONS 328(«눌러서 초록으로 만들지 말고 뜻을 옮긴다»)대로, 물음은 **그대로 «이 값이 `fmtB` 를 지나는가»** 로 두고 자리만 현재 위치로 옮겼다.

### 3-1. ① 싱크 — 2항 → **3항**

```js
['04 스킬 피해 — 세부 표 «피해량» 칸(dmgNow)', /const dmgNow = \(\) => own \? fmtB\(skillDmg\(it\)\) : '—';/],
['07 펫 피해 — 세부 표 «피해량» 칸(ct3Now)', /ct3Now = \(\) => cat === 'pet'\s+\? \(own \? fmtB\(petDmg\(it\)\) : '—'\)/],
['07 펫 피해 — 설명문 «장착 효과» 줄', /장착 효과 — 전투에 참여해 <em>' \+ fmtB\(petDmg\(it\)\) \+ '<\/em> 피해\./],
```

⚑ **펫은 칸을 갈랐다.** 옛 항 하나가 자리 **둘**(표 4번째 칸 · 설명문 «장착 효과» 줄)을 겸하고 있었다 —
한 항으로 두면 한쪽이 사라져도 초록이다(326 교훈 «칸을 갈라 써라»). 그래서 2 → 3 항이다.

### 3-2. ① 반대 방향 1항 신설 — «다른 표기층으로 새지 않았는가»

자리 단언은 «그 줄이 `fmtB` 를 부른다» 만 본다. 새 자리에서 초록인 채로 **옛 자리가 `fmt` 로 부활**하는 것을 막는 짝:

```js
eq('① 스킬·펫 피해 표기층에 남은 비-fmtB 호출',
  (src.match(/\bfmt[A-Za-z]*\((skillDmg|petDmg)\(/g) || []).filter(s => !s.startsWith('fmtB(')).length, 0);
```

접두어를 열어 두어 `fmt(` · `fmtG(` · `fmtCur(` 무엇이든 걸리고 `fmtB(` 만 통과한다.
(기존 `stat.*` 누수 카운터와 같은 꼴이다 — 그 자리에 짝을 맞춰 둔 것.)

### 3-3. ③-b 런타임 5항 신설 — «그 자리가 정말 화면에 나오는가»

등재문 처방 «자리를 옮길 때 그 자리가 실제로 화면에 나오는 전투 수치인지 한 번 확인» 을 게이트 항으로 굳혔다.
소스만 보는 항은 **그 함수가 아무 데서도 안 불려도 초록**이기 때문이다.

| 항 | 실측 |
|---|---|
| ③ 08 스킬 세부 표 헤더 | `피해량` |
| ③ 08 스킬 세부 «피해량» | `67.8A` (알파벳 단위) |
| ③ 08 펫 세부 표 헤더 | `피해량` |
| ③ 08 펫 세부 «피해량» | `28.1A` |
| ③ 08 펫 설명문 «장착 효과» 피해 | `28.1A` |

헤더(«피해량») 까지 같이 못 박아 **칸이 다른 뜻으로 밀려도** 걸린다.
(하네스: `S.trainStage=12` · `S.lv.atk/hp/regen=900` 으로 값을 1000 위로 보낸 뒤 `S.own[id]={l:40,n:5}` ·
`showSkillDetail(SKILLS[0].id)` / `showItem(PETS[0].id)`. 옛 150 규약이었다면 쉼표가 찍힌다.)

### 3-4. §R 되돌림 시험 5항 신설 — «무르게 푼 수리가 아니다»

| 항 | 방식 | 결과 |
|---|---|---|
| §R1 | **소스 사본**에서 `fmtB(skillDmg(it))` → `fmt(skillDmg(it))` | ① 항 거짓 + 누수 카운터 ≥1 ✔ |
| §R2 | 같은 방식으로 펫 | ① **두 항** 다 거짓 + 누수 ≥2 ✔ |
| §R3 | **런타임** — 표기층 `fmtG` 를 센티넬 `⟪B⟫` 로 갈아 끼운다 | 스킬 칸·펫 칸·펫 설명문 **셋 다** `⟪B⟫` ✔ |
| §R3 | 원복 | `67.8A` · `28.1A` 로 복귀 ✔ |

§R1·R2 는 **파일을 안 건드린다**(문자열 사본을 만들어 그 위에서 정규식을 돌린다).
§R3 은 `fmtB = n => fmtG(n)` 이라 `fmtG` 하나만 갈면 표기층 전체가 갈린다는 점을 쓴 것으로,
**화면의 그 글자가 정말 이 표기층을 지나 나온다**는 뜻이다 — 정규식이 못 보는 «호출이 실제로 일어나는가» 를 본다.

## 4. 전수 스윕 — 같은 부패가 더 있는가

`grep -rn "피해 <b>" tools/*.js` → 남은 것은 `verify149` 2건뿐이고 **부패가 아니다**
(264 가 폐기한 «층 실패 — 피해» 문구가 **0건인지** 를 묻는 항 = 반대 방향 단언이라 지금도 옳다).

## 5. 회귀

| 게이트 | 결과 |
|---|---|
| `verify188` | **79/79 PASS** (63/65 → 싱크 +1 · 누수 +1 · 런타임 +5 · 되돌림 +5, 표 항 2→3) |
| `verify262` | 46/46 (08 세부의 «함수 한 벌» 규약 — 이번 자리의 주인) |
| `verify268` | 149/149 (08 껍데기 통일 — `.sk-ct` 2열 표의 주인) |
| `verify109` | 65/65 |
| `verify254` | 31/31 |
| `node tools/smoke.js` | **SMOKE PASS** |

**제품 `index.html` 0줄 · 레이아웃 0px · 밸런스 0줄.**
