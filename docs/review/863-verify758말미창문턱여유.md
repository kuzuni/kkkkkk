# 863 — `verify758` [U4] 「채택 창의 ⓑ 정상성이 문턱에 붙지 않는다」 1.8%p 로 빨강

> **완료 (2026-09-03, sess-0858-11281 루틴 워커 D · 1회차).**
> 제품 `index.html` **0줄** · 레이아웃 Δ0px · **밸런스 계수 0줄** · ④ 판정 줄 **[T1] 1.964 Δ0**.
> `verify758` **64/65 → 66/66** · `probe863` **25/25 신설** · `verify801` 38/38 · `verify494` 99/99 · **SMOKE PASS**.

## 0. 등재문이 남긴 두 갈래 — 답은 ② 다

PROGRESS 863 은 «부패가 아니라 실재 회귀» 라고 못박고 두 처방 후보를 남겼다.

| 후보 | 내용 | 판정 |
|---|---|---|
| ① | 창 사다리를 다시 재고 규약 ⓑ 문턱 15% 의 근거를 실측으로 다시 세운다 | **안 썼다** — 문턱을 건드리는 길이다 |
| ② | 정상성 정의(vs W⌊/2⌋)가 «축 구성이 바뀐» 표에서 **무엇을 재는지** 먼저 확인한다. 축이 바뀌면 W7↔W14 차가 커지는 것이 당연할 수 있고, **그렇다면 자가 잴 것을 잘못 고른 것**이다 | **이것이 답이다** |

그리고 등재문의 ⛔ — «문턱을 넓혀 초록으로 만드는 길은 막혀 있다» — 를 **한 칸도 안 썼다**.
`TAIL_STAT_MAX` 는 **0.15 그대로**이고 `verify758` [U4] 의 여유 **2%p(`stat <= 13`)도 그대로**다.
갈아 낸 것은 **«무엇을 재는가»** 하나뿐이다 — 바로 앞 회차 **857** 이 같은 모양의 자에 쓴 처방이다
(«문턱(120초)은 그대로 두고 무엇을 재는가를 갈랐다»). `probe863` [R4] 가 두 상수를 소스에서 직접 읽어 못박는다.

## 1. 재현 — 시뮬은 한 번도 안 돌렸다

`tools/probe863.js`(**25/25 신설**)는 **커밋된 스냅 넷**(`r26` · `r26-base` · `r801-post` · `r30-after` 의 `.json`)만 읽는다.
그래서 제품 변경으로 안 흔들리고, 몇 번을 돌려도 같은 수를 낸다(플레이키 0 — 이 행이 속한 574·709·825 계보가 바로 그 병이다).

⚠ **초판이 표와 0.2%p 어긋났다.** bot199 의 `med` 는 **위쪽 중앙값**(`w[floor(n/2)]`, 1501행)이지 두 중앙의 평균이 아니다.
평균 중앙값으로 재니 7.6% ↔ 표 7.8% 가 됐다 — 표가 틀린 게 아니라 **자가 다른 정의를 썼다**. 정의를 맞추자 [1b] 가 붙었다.

## 2. 뿌리 — «말미가 나빠졌다» 가 아니라 «분모가 얇아졌다»

옛 규약 자 ⓑ′ 는 `|cont(W) − cont(W⌊/2⌋)| ÷ cont(W)` (두 정책 중 최악)이다. r26 → r30 두 세대를 대면:

| | r26 | r30-after | |
|---|---|---|---|
| 대충 **절대** 어긋남 \|cont(W14) − cont(W7)\| | 1,555/일 | **1,438/일** | **−7.5% (줄었다)** |
| 대충 지속 장부 cont(W14) — **분모** | 15,613/일 | **10,908/일** | −30.1% |
| ⓑ′ 장부 정상성 | 10.0% | **13.2%** | **+32% (올랐다)** |

**자와 실재가 정반대 방향으로 움직였다.** 분모를 얇게 만든 것은 30회차 이관이 깎은
**완전히 평평한 축**(오프라인 `OFF_DIA_PM` 10 → 2.7)이다 — 대충 지속 장부의 **40.4% → 15.6%**.
대충은 퀘스트(일일)을 안 돌아 그 자리를 아무도 안 메웠다(`probe863` [2a]~[2e]).

⇒ ⓑ′ 는 «말미가 정상인가» 와 **«평평한 축이 얼마나 큰가»** 를 같이 재고 있었다.
밸런스 손잡이가 평평한 축을 건드릴 때마다 이 수는 말미와 무관하게 문턱으로 걸어간다.
**«실재 회귀» 는 맞지만, 회귀한 것은 말미가 아니라 장부의 구성이다.**

## 3. 어긋남의 임자 — 축 하나, 그것도 «추세» 가 아니라 «맥박»

| 세대 | 어긋남의 축별 절대 몫 |
|---|---|
| r26 | **던전 +1,295 (82.5%)** · 룰렛 +143 (9.1%) · 퀘스트(업적) +132 (8.4%) |
| r30-after | **던전 +1,295 (89.6%)** · 퀘스트(업적) +136 (9.4%) · 룰렛 +14 (1.0%) |

던전 축의 하루치는 말미 14칸 중 **11칸이 0** 이고 나머지가 ~19,000 이다 — 연속 수급이 아니라 **맥박**이다.
그래서 창 기울기는 «맥박을 몇 개 물었나» 로 결정된다(대충 시드0: 맥박 D11·14·18·21·26 ⇒ **W3 0개 · W7 1개 · W14 3개**).
⚠ **시드 12개의 위상이 같아**(마지막 맥박 날의 폭이 대충 1일 · 부지런 1일) **시드 풀링으로도 안 지워진다**
— 무작위 잡음이 아니라 전투력 벽을 뚫는 구조가 만드는 위상이다(`probe863` [4a]~[4c]).

## 4. 처방 — 규약 자를 «정책 대조 정상성» 으로

`bot199` 에 `rstatOf` 를 세웠다. 창 W 와 그 절반에서 **«부지런 지속기울기 ÷ 대충 지속기울기»** 가 얼마나 어긋나는가다.

- **왜 이것이 옳은가** — ④ 가 읽는 것은 두 정책의 **대조**다. 창이 지켜야 할 성질도 «대조가 창에 안 흔들린다» 이지
  «장부 하나가 안 흔들린다» 가 아니다. 두 정책을 **함께** 미는 손잡이(평평 축의 크기 · 총 유입 척도)는 비에서 **상쇄**되므로
  §2 가 잡은 분모 병이 **구조적으로** 안 생긴다.
- **27회차의 정당화가 글자 그대로 산다** — «이 식의 입력에는 ④ 비가 **한 번도 안 들어간다**(기울기 둘뿐) ⇒ 자를 결과에
  맞추는 것이 구조적으로 불가능하다». 새 식의 입력은 **기울기 넷**이고 ④ 비·목표액·v(30) 은 여전히 한 번도 안 들어간다.
- **가장 좁은 후보(W3)는 비교 짝으로만 쓰고 채택하지 않는다**(⃠). ⓑ′ 시절엔 W3 이 32~36% 라 문턱이 알아서 걸렀는데,
  대조 비는 그 자리에서 오히려 잔잔해질 수 있다(맥박이 양쪽에서 같이 빠진다 — 실측 **4.7%**). 규약이 직접 막는다.
  `probe863` [R3] 이 «이 배제가 실동작한다» 를 못박는다 — 없으면 W14 가 떨어질 때 **3칸 창**이 뽑힌다.
- **ⓑ′ 는 자리를 비우지 않는다**(333 처방). [E4] 표에 **정책별 관측 칸**으로 남는다.

### 새 사다리 (r30-after)

| 창 W | ⓑ 정책 대조 〔규약〕 | ⓑ′ 장부 〔관측〕 | |
|---|---|---|---|
| W3 | 4.7% | 35.9% | ⃠ 비교 짝 전용 |
| W7 | **27.1%** ✗ | 15.2% | 문턱 밖 |
| **W14** | **7.8%** ✓ | 13.2% | **채택 · 여유 7.2%p** |
| W29 | **57.4%** ✗ | 51.6% | 문턱 밖 |

## 5. 무르게 푼 수리가 아님을 네 겹으로 못박았다

1. **[R4] 문턱도 여유도 한 칸 안 넓혔다** — `TAIL_STAT_MAX = 0.15` · [U4] 의 `stat <= 13` 을 소스에서 직접 읽어 확인한다.
2. **[R2] 새 자도 후보를 떨어뜨린다** — 넷 중 **둘**(W7 27.1% · W29 57.4%)이 문턱 밖이다. 전원 통과면 «자» 가 아니다.
3. **[6a] 채택 창이 커밋 스냅 **넷 전부**에서 W14 로 그대로다**(r26 여유 10.6%p · r26-base 10.6%p · r801-post 8.9%p ·
   r30-after 7.2%p) ⇒ ④ 판정 줄 **[T1] 1.964 Δ0** · 밸런스 계수 **0줄**. 자를 바꿔 답을 바꾼 것이 아니다.
4. **[R1] 되돌림 시험** — `rstatOf` 를 `statOf` 로 되돌리면 [U4] 가 **그 자리에서 다시 1.8%p 로 빨개진다**(등재문의 값 그대로).

## 6. 곁다리로 잡은 것 — 표가 «부지런 13.2%» 라고 거짓말하고 있었다

27~30회차의 [E4] 는 정책마다 표를 하나씩 내면서 ⓑ′ 칸에 **`statOf`(두 정책 중 최악)** 를 실었다.
그래서 **부지런 표의 «13.2%» 는 부지런의 수가 아니었다** — 부지런 자신의 값은 **6.4%** 이고, 13.2% 는 대충의 값이다
(`probe863` [5a]·[5b]). 규약의 **선택**은 계속 두 정책 공통이어야 하므로(같은 W 로 안 읽으면 ④ 가 다른 두 창의 비가 된다)
**선택은 합본, 표의 칸은 정책별**로 갈랐다: `statPolOf` 신설 · 규약 각주 줄이 합본 값을 따로 밝힌다.
`verify758` **[U4b] 신설**이 그 칸을 관측으로 찍고, 두 칸이 다시 같아지면 «⚠ 옛 «최악 합본»» 이라고 스스로 말한다.

## 7. ⚠ 1회차에 내가 만든 회귀 — 단일 정책 표를 죽였다

`rstatOf` 는 두 정책을 **이름으로** 부른다(`'diligent'`/`'casual'`). 옛 `statOf` 는 `Object.keys(rep.policies)` 만 돌아
없는 정책을 물은 적이 없었다. `--policy=diligent` 같은 **단일 정책 실행**에서 `tailSplit('casual', …)` 이
`for (const r of undefined)` 로 죽어 **표 인쇄가 통째로 무너졌고**, `verify494` 가 픽스처 실행 넷을 잃어 **91/99** 로 빨개졌다.

⇒ `tailSplit` 입구에 `if (!runs || !runs.length) return null;` 한 줄. 그리고 단일 정책 표의 규약 각주는
«후보가 전부 문턱 밖» 이 아니라 **«정책이 하나라 대조를 못 잰다 — 창을 안 고른다»** 로 갈랐다(단일 정책 표는 애초에 ④ 비를
못 내므로 §0 판정에 안 쓴다). ⚑ **교훈: 회귀는 «판정 표» 만 돌려 보면 안 잡힌다 — 자가 픽스처로 부르는 실행 모양까지 돌려라.**

## 8. 표를 다시 접은 방법 (제품·데이터 0줄)

`docs/review/199-bot-2026-09-03-r30-after.md` 의 [E4] 두 절만 바뀐다.
**시뮬을 다시 안 돌렸다** — 커밋된 `.json` 을 `node tools/bot199.js --replay=…` 로 다시 접었다.
착수 전에 «리플레이가 원본을 그대로 재현하는가» 부터 확인했고, 바뀌는 줄은 **3행(재현줄) 하나뿐**이었다 —
그 3행은 원래 값(실제 시뮬 실행의 출처)으로 되돌려 놨다. 수는 전부 같은 런의 것이고 [E4] 의 **파생 칸만** 바뀐다.

## 9. 199 에 넘기는 것

- **⓵ 던전 축은 맥박이다.** 30일 지평에서 던전은 말미 14칸 중 3칸에만 들어오고 시드 위상까지 같다.
  **말미 기울기를 손잡이로 쓸 때 던전 몫은 «위상» 을 타고 있다** — 다음 손잡이가 던전을 건드리면 ④ 가 창 위상만큼 흔들린다.
- **⓶ 평평 축을 더 깎으면 ⓑ′ 관측 칸이 또 오른다.** 그 수는 이제 판정이 아니지만 **분모가 얼마나 얇아졌는지**를 말한다
  (대충 지속 장부에서 평평 축 몫 40.4% → 15.6%). [U4b] 가 매 실행 찍는다.
- **⓷ 이 행은 계수를 한 줄도 안 건드렸다.** ④ 1.964 · §0 창 1.8~2 · `OFF_DIA_PM` · `DQUESTS` 전부 그대로다.
