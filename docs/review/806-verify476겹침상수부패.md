# 806 — `tools/verify476.js` 「겹침도 등재문 그대로 세로 37 · 가로 342」 1건 실패 (33/34)

> T1 «버그(게이트 부패)» · 지시서 [3]-**(가)** 기계+자 · 비평가 없음
> 세션 `sess-0843-1482` (루틴 워커 C) · 1~2회차 · 2026-09-02
> **제품 `index.html` 0줄** — 고친 것은 `tools/verify476.js` 한 파일이다.

## 1. 재현

```
node tools/verify476.js
  ✗ 겹침도 등재문 그대로 세로 37 · 가로 342 — by30/wide342 / by30/wide342 / by30/wide342
VERIFY476 33/34 FAIL
```

세 화면(`eqslot:weapon`·`shield`·`neck`)이 **전부 같은 값**(`by30/wide342`)이다 ⇒ 플레이키가 아니라
**자리가 옮겨졌다**. 가로 342 는 정확히 맞고 세로만 37 → 30 이다(−7).

## 2. 갈래 — 무엇이 움직였나 (등재문의 «먼저 볼 것»)

`#wpnGrid`(05 장비 팝업 격자)와 `#tuto`(미션 배너)의 상자를 **476 당시 트리**(`9171c3a1`,
2026-08-30 14:52Z — `git show 9171c3a1:index.html > .v806-old.html`)와 **현재 트리**에서 각각
1600(9:13.3)으로 재서 나란히 놓았다:

| 노드 | 476 당시(`9171c3a1`) | 현재 | Δ |
|---|---|---|---|
| `#wpnw .wm` top | 156 | **149** | **−7** |
| `#wpnGrid` top..bottom | 715..**1136** | 708..**1129** | **−7** (높이 421 불변) |
| `#tuto` top..bottom | 1099..1249 | 1099..1249 | **0** |
| 겹침 `by` | **37** | **30** | −7 |
| 겹침 `wide` | 342 | 342 | 0 |

⇒ **배너는 한 픽셀도 안 움직였다.** 움직인 것은 05 상자이고, `.wm` 안쪽 기하(head 92 · body 1147 ·
info 396 · grid 421 · total 46 · btn 131)는 **전부 Δ0** 이라 «상자가 통째로 7px 올라간» 것이다.

## 3. 뿌리 — 558 (`--wm-sk`)

```
git log --oneline -S"--wm-sk" -- index.html
  a1479d30 wip(558): 1회차 — 눌린 프레임 여유 14 를 7/7 로 나눔(--wm-sk) · probe558·verify558 신설 · verify467 [R2] 이관
```

`index.html` `#wpnw`:

```css
--wm-sk: clamp(0px, calc(1805px - var(--frameh, 2280px)), 7px);
padding: calc(126px - var(--wm-sk)) 91px calc(150px + var(--wm-sk));
```

1600 에서 `--wm-sk` = 7 ⇒ 위 패딩 126 → **119**, 아래 150 → **157**. 가운데 정렬이므로 상자가
**7px 올라간다.** 467 이 남긴 «눌린 프레임에서 여유의 합이 14 뿐» 을 558 이 위/아래 7/7 로 나눈,
**의도된 변경**이다(2280·1920·1842 는 문턱 1805 위라 `sk` = 0 ⇒ Δ0px — `verify558` 이 보존).

**제품은 옳다.** 558 은 `verify467` [R2] 는 이관했지만 `verify476` §1 에 굳어 있던 상수 37 은
못 봤다. 전형적인 게이트 부패다.

## 4. 처방 — 상수를 빼고 **제품에게 묻는다**

허용 오차를 넓혀 초록으로 만드는 길은 368 §R2 규약이 막고 있고, 새 상수 30 으로 갈아 적는 것은
**다음에 05 상자가 움직이면 똑같이 부패할 자리**를 다시 만드는 것이다. ⇒ 368 선례(«자리를 상수에서
뺀다») · 212-①(«기대값은 화면이 쓴 식이 아니라 근거 데이터에서»)대로 **기대값을 실측에서 만든다.**

`MEASOV`(신설) 가 1600 에서 `#wpnGrid`·`#tuto` 의 상자를 읽어 `by`/`wide` 를 낸다.

⚠ **`probe351` 의 `clipped` 를 베끼지 않았다** — 베끼면 자가 둘이 되어 385(자매 자 드리프트)를
그대로 다시 만든다(§0 이 `cover351lib` 로 막아 둔 것과 **같은 종류의 사고**다). 대신
**«`#wpnGrid` 를 실제로 자르는 조상이 0 개»** 를 먼저 단언한다 — 그게 참인 동안 `drawn` = `raw`
이므로 raw 파생 기대값이 곧 probe 가 내야 할 값이고, 언젠가 클리핑이 생기면 그 항이 빨개져
**어느 정의가 움직였는지를 이름으로 말한다**(조용히 안 어긋난다).

같이 세운 항 — **«그 겹침이 여전히 D7 문턱(세로 > 2 · 가로 > 40)을 넘는다»**.
안 넣으면 자리가 아예 안 겹치게 됐을 때 §1 이 «0건» 으로 **공허하게 초록**이 된다
(223 이 «보스가 안 나오면 그 뒤 항목이 표본 0개로 자동 초록» 으로 겪은 그 얼굴).

## 5. 무르게 푼 수리가 아님 — §R 되돌림 시험(신설)

«상수를 뺐다» 가 말뿐이면, 제품을 되돌려도 기대값이 안 따라온다. 그래서 자 자신이 되돌린다 —
페이지에서 `--wm-sk` 를 **0** 으로 되돌리면(= 558 이전 상태) 겹침이 그 자리에서 **손잡이 값만큼**
늘어나야 한다:

```
§R 되돌림 시험 — `--wm-sk` 를 0 으로 되돌리면 겹침이 그 손잡이만큼 늘어난다 (806)
  ✓ [R] 되돌리기 전 겹침 = §1 이 쓴 실측값 (기대 30 · 실제 30)
  ✓ [R] 1600 에서 손잡이가 실제로 켜져 있다 (`--wm-sk` = 7px · 0 이면 이 시험은 공허하다)
  ✓ [R] 되돌린 판에서는 손잡이가 0 이다 (기대 0 · 실제 0)
  ✓ [R] 되돌리면 겹침이 손잡이만큼 늘어난다 (558 이 옮긴 그 값 — 상수 아님) (기대 7 · 실제 7)
  ✓ [R] 가로는 `--wm-sk` 와 무관하다 (세로 손잡이다) (기대 342 · 실제 342)
```

이 절이 셋을 한 번에 못박는다 — ① 기대값이 **살아 있는 자**다(상수면 안 움직인다)
② 뿌리가 **558 의 그 손잡이**다 ③ 그 손잡이가 실제로 켜져 있다(공허한 시험이 아니다).
되돌린 값 30 + 7 = **37** 이 곧 등재문의 그 값이다 — 등재 당시 자는 옳았고, **상수라서** 굳었다.

### 5-1. ⚠ 2회차에 내가 심을 뻔한 같은 부패 (자기 교정)

1회차 §R 은 «`--wm-sk` 0 으로 되돌리면 **7px** 늘어난다» · «그 값이 등재문의 **37** 이다» 를
**손 상수로** 적었다. 41/41 초록이었지만 **그 두 줄이 정확히 이 작업이 고친 것과 같은 얼굴**이다 —
05 상자가 다음에 또 움직이면 §1 은 따라오는데 §R 이 굳어 빨개진다. ⇒ 2회차에 둘 다 걷어내고
**손잡이 값을 제품에게 묻게** 했다(`back.by − now.by === now.sk`).

⚠ **`getComputedStyle(el).getPropertyValue('--wm-sk')` 는 못 쓴다** — 등록(`@property`)되지 않은
사용자 속성의 계산값은 `clamp(…)` **문자열 그대로**라 `parseFloat` 가 **0** 을 준다(2회차 1차 시도가
«기대 0 · 실제 7» 로 빨갰다). ⇒ `height:var(--wm-sk,0px)` 를 쓰는 **탐침 노드**를 심어 실제 길이로 잰다.

## 6. 결과

- `verify476` **33/34 FAIL → 42/42 PASS**(항 −1 · +9: `[1]` 노드 생존 · `[1]` 자르는 조상 0 ·
  D7 문턱 · 실측 일치 · §R 5항). **새로 심은 px 상수 0개** — §1·§R 의 기대값이 전부 실측 파생이다.
- 회귀 — `verify467` **64/64** · `verify558` **37/37** · `verify419` **236/236** · `verify435` **17/17** · **SMOKE PASS** · `verifyProgress` OK
- 제품 `index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄

## 7. 곁다리

없음. 등재문이 지목한 «허용 오차를 넓히지 말 것»(368 §R2)과 «상수를 제품에게 묻는 식으로»
둘 다 지켰고, `probe351`·`cover351lib` 는 한 줄도 안 건드렸다.
