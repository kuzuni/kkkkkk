# 955 — rect 를 재는 `probe*.js` 를 정착(291)이 통째로 밖에 둔다

> 티어 **T1 «잠복(자)»** · 지시서 [3]-(가) 기계적 작업(**비평가 없음** — 레퍼런스 대조가 없다)
> 선점 `docs/claims/955.lock` · 세션 `sess-2328-18580` (루틴 워커 B)

## §0 등재문이 말한 것

941 1회차 곁다리 관측(sess-1813-14758):

- `tools/settle291.js` 의 `enabled()` 는 조건 ⓐ 로 **entry 가 `verify*.js` 일 때만** 훅을 켠다.
- 그래서 rect 를 재는 `probe*.js` 356자는 `page.waitForTimeout` 훅 밖에 있고,
  `jzPgIn`/`jzSheetIn` **0% 프레임(scale .985)** 을 잡고도 아무도 안 짖는다.
- probe 는 «자를 새로 세우는 자» 라 **게이트보다 먼저 거짓말을 한다** —
  941 재현이 실제로 그 프레임을 잡아 **등재문을 기각할 뻔했다**.

처방 후보 셋이 함께 적혀 있었다: ⓐ 조건을 넓히기 · ⓑ 356자에 한 줄씩 심기 · ⓒ 판별기로 고르기.

## §1 재현 — `tools/probe955.js` 신설 (338 규칙)

처방을 쓰기 전에 등재문을 이 저장소의 자로 되쟀다.

### [1] 인구조사 (정적)

| 축 | 값 |
|---|---|
| `tools/probe*.js` 총수 | **617자** |
| 그 중 `getBoundingClientRect` 를 부르는 자 | **361자** |
| 정착을 스스로 켜는 자(`PW_SETTLE='1'`) | **3자** — `probe353` · `probe941` · `probe950` |
| 두 팔을 일부러 만드는 자 | 2자 — `probe771` · `probe921` |
| **위험군**(rect + 고정 대기 ≥ 250ms + 정착 없음) | **337자** |

⇒ 처방 ⓑ(356자에 사본 한 줄씩)는 **규모부터 답이 아니다**(402 «사본을 지운다»).

### [2] 훅 판정 — 브라우저 없이 `enabled()` 에 직접 묻는다

| entry | 수리 전 | 수리 후 |
|---|---|---|
| `verify955.js` | true | true |
| **`probe955.js`** | **false** ⚑ | **true** |
| `cap01.js` | false | false (**그대로 — 291 ⓐ 는 옳다**) |
| `probe955.js` + `PW_SETTLE=0` | false | false |
| `cap01.js` + `PW_SETTLE=1` | true | true |

등재문 ⓐ 가 한 항으로 못박혔다(`probe955` [2-c]).

### [3]·[4] 재현 — 값이 «실제로» 움직인다

`openRelw()` + `page.waitForTimeout(300)` 뒤의 `#relw .rw-panel`:

| | 폭×높이 | 좌상단 | 도는 연출 |
|---|---|---|---|
| 정착 off (= 수리 전 probe) | **1063.8 × 1962.12** | (8.1, 122.91) | `jzPgIn:running` |
| 정착 on | 1080 × 1992 | (0, 108) | — |

배율 **0.9850**(= `jzSheetIn`/`jzPgIn` 0% 프레임) · 좌변 밀림 **8.1px** — 291 머리말이 적어 둔
`540·(1/s−1) = 8.22px` 와 같은 지문이다.

자리 스윕(네 자리 × CPU 스로틀 ×1/×6/×16) — **9/12 판에서 값이 움직였다**:

| 자리 | ×1 | ×6 | ×16 |
|---|---|---|---|
| relic (`#relw .rw-panel`) | ⚑ Δw −16.2 | ⚑ Δw −16.2 | ⚑ Δw −16.2 |
| shop (`#shopw .pcb`) | Δ0 | ⚑ Δw −16.2 | ⚑ Δw −16.2 |
| dungeon (`#dunw .pcb`) | Δ0 | ⚑ Δw −16.2 | ⚑ Δw −16.2 |
| train (`#trw .tr-sheet`) | Δ0 | ⚑ **Δy 1245** | ⚑ Δy 1245 |

⚑ **relic 은 한가한 판(×1)에서도 결정적으로 걸린다** — 부하가 있어야 나는 병이 아니다.
⚑ **읽는 법(921 규약)** — Δ0 인 자리는 «멀쩡한 자리» 가 아니라 «이 판에서는 안 걸린 자리» 다.

## §2 처방 — 조건 ⓐ 를 넓히고, **빼는 쪽을 선언으로** 받는다

등재문의 후보 셋 중 **ⓐ 를 골랐다**. 고른 이유와 나머지를 버린 이유:

| 후보 | 판정 | 이유 |
|---|---|---|
| ⓐ 조건을 «`verify*` 또는 `probe*`» 로 넓힌다 | **채택** | 제품 **1줄**. 훅 본체·필터·상한을 한 글자도 안 건드린다. |
| ⓑ 356자에 `PW_SETTLE=1` 한 줄씩 심는다 | 기각 | 위험군만 **337자**다 — 402 가 지운 병(사본)을 337벌 새로 만든다. |
| ⓒ «정착이 필요한 자» 판별기를 세운다 | 기각 | 판별기가 곧 **또 하나의 자**다. 판별기가 틀리면 조용히 다시 거짓말한다. |

⚑ **ⓐ 를 고르면서 «빼는 쪽» 을 어떻게 받느냐가 이 작업의 실제 설계다.**
연출을 **일부러** 한복판에서 재는 자는 정착하면 안 된다(291 조건 ⓐ 가 `cap*` 을 뺀 그 이유).
그 목록을 `settle291.js` 안에 이름으로 적으면 **자가 늘 때마다 뒤처지는 사본**이 하나 더 생긴다.
⇒ 빼는 쪽도 **그 파일 안의 한 줄**로 선언한다 — 이미 있는 옵트인 관례
(`if (!process.env.PW_SETTLE) process.env.PW_SETTLE = '1';` · `probe353`·`probe941`·`probe950`)
의 대칭이고, 사유가 **그 자를 읽는 사람 눈앞에** 남는다.

### 제품 — `tools/settle291.js` 1줄

```
-  return /^verify.*\.js$/.test(entry);
+  return /^(verify|probe).*\.js$/.test(entry);
```

`index.html` **0줄** · 레이아웃 Δ0px · 밸런스 0줄 — 게이트 장치만 고쳤다.

⚑ **옵트인 선언 3자는 남겼다(no-op 이 됐는데도).** 훅은 **이름 규칙**이라 파일 이름이 바뀌거나
조건이 다시 좁아지면 그 자들은 조용히 0% 프레임으로 되돌아간다. 그 한 줄은 «이 자는 정착이
**필요하다**» 는 선언이지 스위치가 아니다(`verify955` [4b] 가 그 셋을 센다).

## §3 게이트 — `tools/verify955.js` 신설

| 절 | 지키는 것 |
|---|---|
| **[1] 훅 조건** | verify · **probe** 는 켜고, `cap*`·`scan*`·`smoke`·`bot*` 은 끈다 · `PW_SETTLE=0/1` 은 종전대로 이긴다 |
| **[2] 훅 본체 불변** | 이름 필터 `^jz(Pg\|Sheet)` 3곳 · `MIN_WAIT` 250 · `CAP_MS` 1500 · §box 무수정 · `enabled()` 는 연출 이름을 안 본다 |
| **[3] 실부착 + §R** | probe entry 가 **1080 폭**을 잰다 / `PW_SETTLE=0` 이면 **1063.8**(0% 프레임)으로 되돌아간다 / `cap` entry 는 **여전히 안 정착한다** |
| **[4] 선언 규약** | `enabled()` 가 probe 이름 목록을 **안 읽는다** · 옵트인 3자는 살아 있다 · 옵트아웃한 자는 **사유(955)를 자기 파일에** 적었다 |
| **[5] 인구조사 래칫** | rect 를 재는 probe ≥355자 · 그 **전부**가 훅 안 |

⚑ **[3b] 가 «무르게 푼 수리가 아님» 을 못박는 자리다** — 훅을 끄면 같은 자리가 그 자리에서
0% 프레임으로 되돌아간다(값이 안 움직이면 «이미 참인 것을 게이트로 굳힌 것» 이다 · 338 규칙).

⚑ **[2] 는 764·950·957 이 적어 둔 경고를 게이트로 옮긴 것이다** — 이름 필터를 넓히면
«시간 자체를 재는» 게이트(64·262·107)가 rAF 두 프레임에 문턱을 넘는다. 955 는 **누구에게 거는가**
만 바꿨고 **무엇을 기다리는가**는 한 글자도 안 건드렸다.

## §4 회귀 — 정착이 켜지면 **되레 깨지는 자**가 있는가

이 수리는 337자의 동작을 한꺼번에 바꾼다. 그래서 «누가 깨지는가» 를 먼저 **정적으로 좁히고**,
좁힌 것만 **전후 A/B** 로 돌렸다.

### 좁히기 — 입장 연출을 «일부러» 재는 자만 위험하다

훅은 두 관문을 지나야 돈다: ① `page.waitForTimeout(ms ≥ 250)` 직후여야 하고
② 그 순간 `jzPg*`/`jzSheet*` 가 **실제로 pending** 이어야 한다(291 의 값싼 선검사).
⇒ 위험한 것은 «그 두 관문을 지나면서 0% 프레임을 **일부러** 읽는 자» 뿐이다.
입장 연출 이름을 언급하는 probe 15자에서 다음이 걸러졌다:

| 자 | 판정 | 근거 |
|---|---|---|
| `probe353`·`probe941`·`probe950` | 해당 없음 | 이미 `PW_SETTLE='1'` 자가점등 |
| `probe771`·`probe921` | 해당 없음 | 정착 on/off **두 팔을 스스로** 만든다(env 를 직접 쓴다) |
| `probe550`·`probe560`·`probe886`·`probe915` | 해당 없음 | 재는 대기가 **페이지 안**(353 꼴)이거나 250ms 미만 — 훅이 구조적으로 안 돈다 |
| `probe605` | **A/B 로 확인** | [D] 가 «연출이 도는 동안» 을 재지만 **한 `page.evaluate` 안에서** 켜고 잰다 — 틱을 안 넘으므로 훅이 낄 자리가 없다 |
| `probe378`·`probe379`·`probe471`·`probe563` | **A/B 로 확인** | 같은 규칙을 **손으로 적어 둔** 자들(«입장 연출 settle — verify47 과 같은 것») |
| `probe586` | 미실행 | 대기가 400+700+2800ms 로 **연출이 끝난 뒤** 다 — 게다가 프레임 타임라인을 페이지 안 rAF 루프로 잰다 |

### A/B — `PW_SETTLE=0`(수리 전과 같은 «훅 없는 세상») ↔ 기본(수리 후 = 켜짐)

| 자 | 전후 차 | 점수 |
|---|---|---|
| `probe471` | **0줄(바이트 동일)** | — (측정표) |
| `probe563` | **0줄(바이트 동일)** | — (측정표) |
| `probe378` | **0줄(바이트 동일)** | — (측정표) |
| `probe605` | 6줄 — 60초 표본 수 **447 → 444**(시계 잡음) | **14/14 → 14/14 ALL PASS** |
| `probe379` | 4줄 | — (측정표) · **아래 참조** |

⚑ **`probe379` 의 4줄은 955 가 만든 것이 아니다 — 같은 팔 «안» 에서도 갈린다.**
LESSONS 107 ③(«되돌림 시험은 끄고 1회가 아니라 같은 팔로 2회 이상»)대로 각 팔을 3회씩 돌렸다:

| | r1↔r2 | r1↔r3 | r2↔r3 |
|---|---|---|---|
| off 팔 | 4줄 | 10줄 | 6줄 |
| on 팔 | **0줄** | — | 10줄 (r2↔r3) |

흔들리는 줄은 언제나 23 훈련 ⓑ 세 줄이고, 지문은 **같은 칸이 두 번 찍히는 것**이다(«칸1·칸2·칸1»
↔ «칸1·칸1·칸3») — 탭을 누른 다음 프레임에 `.on` 이 옮겨 가는데 자가 같은 틱에 다시 읽는다.
정착으로는 안 닫히는 축(훅은 `jzPg*`/`jzSheet*` 만 기다리고, 여기서 늦는 것은 **클래스 토글 뒤의
렌더**다) ⇒ **곁다리 등재 963**.

### 291 계열 게이트 — **전부 무수정 초록**

`verify957` **27/27** · `verify950` **27/27** · `verify941` **19/19** · `verify429` **53/53** ·
`verify46` **70/70** · `verify268` **154/154** · `verify886` **12/12** · **SMOKE PASS** ·
`verifyProgress` **exit 0**(§1~§5 전부 빨강 0건).

⚑ `verify941`·`verify46` 은 점수 줄의 **말투가 달라**(«VERIFY941 PASS — 19/19» · «VERIFY46 PASS 70/70»)
한 정규식 스윕에 «점수 줄 없음» 으로 잡혔다 — 913 이 적어 둔 «없는 자» 와 같은 얼굴이라 하나씩 직접 돌려
확인했다. **스윕의 «점수 줄 없음» 은 초록도 빨강도 아니다 — 그 자를 손으로 돌려 보기 전에는 모른다.**

## §5 등재문이 남긴 물음 — «흔들리는 자 다섯이 이 축에 걸리는가»

등재문은 946 과 한 세션에 묶을 근거로 그것을 물었다. **정적으로 답이 갈렸다** —
이 축에 걸리려면 «rect 를 잰다 + 고정 대기 ≥250ms» 둘 다여야 한다:

| 자 | `getBoundingClientRect` | `page.waitForTimeout ≥ 250` | 이 축인가 |
|---|---|---|---|
| `probe305` | 2건 | 1000 · 500 | **그렇다** |
| `probe552` | **0건** | 1200 · 400 | 아니다 |
| `probe722` | **0건** | 500 | 아니다 |
| `probe766` | **0건** | 없음 | 아니다 |
| `probe779` | **0건** | 없음 | 아니다 |

⇒ **다섯 중 하나뿐**이다. 네 자는 rect 를 아예 안 재므로 955 가 그들의 플레이키 원인일 수 없고,
**946 과 이 번호를 한 세션에 묶을 근거는 서지 않는다**(그래서 안 묶었다 — 946 은 다른 워커가
선점 중이기도 하다). 946 을 잡는 세션에게: `probe305` **한 자**만 이 축을 의심하면 된다.

## §6 결과

| 자 | 결과 |
|---|---|
| `verify955` | **21/21 신설 PASS** |
| `probe955` | **13/13 신설 PASS**(수리 전 12/13 — 빨간 항이 [2-c] 하나였다) |
| 회귀 | `verify957` 27/27 · `verify950` 27/27 · `verify941` 19/19 · `verify429` 53/53 · `verify46` 70/70 · `verify268` 154/154 · `verify886` 12/12 · **SMOKE PASS** · `verifyProgress` exit 0 |
| A/B | `probe471`·`probe563`·`probe378` **바이트 동일** · `probe605` **14/14 → 14/14** · `probe379` 는 같은 팔 안에서도 갈린다(⇒ 963) |
| 곁다리 등재 | **963** — `probe379` ⓑ 오버행 절이 탭 전환 렌더를 안 기다린다(플레이키 · 자) |

**제품 `index.html` 0줄 · 레이아웃 Δ0px · 밸런스 0줄 · 판정 0줄.** 고친 것은 `tools/settle291.js`
**한 줄**이다.
