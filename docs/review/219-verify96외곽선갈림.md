# 219 — `tools/verify96.js` 45/47 FAIL · 공용 서브탭 라벨 외곽선이 «영웅 vs 10 상점» 에서 갈림

> **완료 (2026-08-27, sess-0846-17286 워커 C · T1 버그 · 1회차).**
> 지시서 [3]-**(가)** — 레퍼런스 대조가 아니라 «게이트가 왜 빨간가» 라서 **비평가 안 띄움**.
> `index.html` **0줄**. 고친 자리는 `tools/verify96.js` 하나(+ 신설 `tools/neg219.js`·`probe219.js`).
> **VERIFY96 45/47 → 61/61 PASS** · **NEG219 27/27** · **SMOKE PASS**.

---

## 1. 등재된 증상

```
FAIL 비활성 라벨 — 영웅 vs 10 상점 Δ0 — textShadow: … 8항 ≠ … 8항 + 0px 5.279px 0px, 4.72px 0px 0px
FAIL 활성  라벨 — 영웅 vs 10 상점 Δ0 — textShadow: 4px…3px… ≠ 2.8px…2.1px… + 0px 4.973px 0px, 4.44px 0px 0px
```

등재문(173 곁가지)의 물음은 **«어느 쪽이 옳은지(`--st-*` 변수 계열 · `.ol3/.ol4` 상속)를 먼저 정한 뒤 한쪽으로 모을 것»** 이었다.
답은 **«어느 쪽도 모으면 안 된다»** 다. 아래가 근거다.

## 2. 실측 — `tools/probe219.js`(신설, 읽기 전용)

네 자리의 `.stab>i` 를 그대로 찍었다(뷰포트 1080×2280 · 비활성 fs 43 · 활성 fs 41).

| 자리 | 셀렉터 | 비활성(.ol3) | 활성(.ol4) |
|---|---|---|---|
| 영웅 07 | `#bSk .stabs` | 등방 링 8항 **3/2** | 등방 링 8항 **4/3** |
| 06 장비 | `#eqTabs` | 8항 **3/2** (영웅과 문자열 동일) | 8항 **4/3** (동일) |
| 03 던전 | `#dunSub` | 8항 **3/2** (동일) | 8항 **4/3** (동일) |
| 10 상점 | `#shopCats` | 8항 **3/2** + **덧칠 2항** `0 5.279px` · `4.72px 0` | 8항 **2.8/2.1** + 덧칠 `0 4.973px` · `4.44px 0` |

- 갈리는 축은 **`textShadow` 하나뿐**이다. `fontSize`·`fontWeight`·`color`·보정 `transform`·`webkitTextStrokeWidth`
  다섯 축은 네 자리가 **지금도 Δ0** 이다(게이트가 그대로 초록이었다).
- 덧칠 2항은 **드롭 그림자**다. `:root --sh-drop .053` · `--sh-dropx .040` 과 정확히 맞물린다:
  `5.279 − 3 = 2.279 = .053×43` · `4.72 − 3 = 1.72 = .040×43` · `4.973 − 2.8 = 2.173 = .053×41` · `4.44 − 2.8 = 1.64 = .040×41`.

## 3. 판정 — **게임이 옳고 게이트의 전제가 죽었다**

갈림을 만든 것은 `index.html:9204·9218` 의 `#shopCats .stab>i.ol3/.ol4` 이고, **작업 126 ③ 17·18회차가 일부러 얹은 것**이다.
그 자리 주석이 근거·범위를 다 적어 두었다:

- **17회차(덧칠)** — 비평가 AG·AH 가 10 상점 ref 를 재서 «아래 −43.5% · 우 −45.6%» / «아래 −32% · 우 −26%» 로 일치.
  공용 `.ol*` 은 **등방 링뿐**이라 사방이 같은 두께로 나온다 → **아래 겹·오른쪽 겹만** 얹었다.
- **18회차(활성 링 −30%)** — AI·AJ 가 «드롭 오프셋은 정상, **링만 +1.19~1.78px 과대**» 로 일치 → 링 4/3 → **2.8/2.1**.
  드롭의 «묻히는 몫» 도 링 반지름이라 같이 내렸고, **보이는 드롭 = (링+drop) − 링 = drop 은 값이 안 바뀐다**(불변식).
- 그리고 **범위를 명시로 못 박았다** — “`#shopCats` 를 쓰는 이유 ② **ref 로 구속되는 표본이 10 뿐**이다.
  03·06·13·23·47 로의 **확산은 그 화면 ref 실측이 생긴 뒤에 한다**.”

⇒ «한쪽으로 모으기» 는 둘 다 근거를 잃는다.
① 상점을 공용으로 되돌리면 **ref 실측 4건**(AG·AH·AI·AJ)이 버려진다 — 게다가 126 은 **⏹종료(주인 지시 2026-08-27)** 라 라운드가 닫혔다.
② 공용 쪽을 상점 값으로 올리면 **ref 근거가 없는 세 화면**(영웅·06·03)에 10 의 실측을 무단 확산하는 것이라 126 의 명시 지시를 어긴다.
그리고 그 셋은 **내 작업 구간도 아니다**(병렬 세션 규칙 3).

## 4. 처방 — 지우지 않고 **[1-b] 로 이사**, 잃은 규칙은 **세 벌**로 다시 못 박음

LESSONS 168-② «등식을 잃은 단언은 FAIL 이 아니라 SUPERSEDED» · 185-④ «이사» · 214-④ «이사시키되 잃어버린 규칙도 함께 못 박는다».

1. `INK_PROPS` 에서 **`textShadow` 만 뺀다** — 나머지 다섯 축의 Δ0(3자리 × 2상태 = 6항)은 그대로 지킨다.
   **이것이 «한 부품이면 같다» 의 살아남은 절반**이고, 지금도 초록이다.
2. **[1-b] 신설**(14항) — 상태(비활성·활성)마다 7항:

   | 항 | 무엇을 지키나 | 잡히는 되돌림 |
   |---|---|---|
   | ⓐ 영웅 vs 06 · vs 03 문자열 Δ0 (2항) | ref 실측이 없는 세 자리는 **여전히 한 부품** | N5 |
   | ⓑ 공용 3자리 = 등방 링 `RING` 8항뿐, **덧칠 0** (1항) | 근거 없는 덧칠·링 변경이 **번지면** 잡는다 | N3 · N4 |
   | ⓒ 10 상점 = 링 8항 + **덧칠 2항** (1항) | 126 의 덧칠이 사라지면 잡는다 | N1 · N2 |
   | ⓓ 10 상점 링 = `SHOP` (1항) | 18회차의 −30% 가 공용 4/3 으로 돌아가면 잡는다 | N2 |
   | ⓔ 보이는 드롭 세로·가로 = `--sh-drop`/`--sh-dropx` (2항) | 126 이 적은 **불변식** 자체 | N1 · N2 · N6 |

3. **기대값은 전부 게이트 자기 상수**(`RING`·`SHOP`·`DROP`·`DROPX`)다 — 페이지의 CSS 변수를 다시 읽어 비교하면
   **항등식**이라 토큰이 통째로 망가져도 같이 틀려 초록으로 샌다(LESSONS 212-①). N6 이 이것을 시험한다.
4. 링 비교는 **순서를 안 따지고 «집합»** 으로 본다(`ringOk`) — 항 순서는 규칙이 아니라 작성 순서라 여기 걸면 헛 빨강이 난다.

## 5. 되돌림 시험 — `tools/neg219.js`(신설) **NEG219 27/27**

`index.html` 사본을 한 곳만 갈아 끼워 `.v219-neg.html` 로 쓰고 **그 파일을 새로 열어** `verify96` 를 통째로 돌린다
(`V96_SRC`). 살아 있는 페이지에 CSS 를 주입하면 레이아웃이 다시 안 풀려 **거짓 초록**이 난다(LESSONS 191).
기준선(갈아 끼우지 않은 사본) **FAIL 0건** 을 먼저 확인한다.

| 시험 | 갈아 끼운 곳 | 빨개진 항목 | 초록으로 남아야 하는 항목 |
|---|---|---|---|
| **N1** | `#shopCats … .ol3` 덧칠 제거 | 비활성 ⓒ·ⓔ×2 (3건) | — |
| **N2** | `#shopCats … .ol4` 규칙 통째 제거 | 활성 ⓒ·ⓓ·ⓔ×2 (4건) | — |
| **N3** | 공용 `.ol3` 유틸에 덧칠 **확산** | 비활성 **ⓑ만** (1건) | ⓐ 2건 — 세 자리가 «같이» 움직여 Δ0 은 통과한다 |
| **N4** | 공용 `.ol4` 링 4/3 → **5/4** | 활성 **ⓑ만** (1건) | ⓐ 2건 (같은 이유) |
| **N5** | `#eqTabs` 한 자리만 대각 2 → **1** | 비활성 ⓐ(06) · ⓑ (2건) | ⓐ(03) |
| **N6** | 토큰 `--sh-drop` .053 → **.080** | 비활성·활성 ⓔ **세로만** (2건) | ⓔ 가로 · ⓓ 링 |

**N3·N4 가 이 이사의 핵심이다.** 확산·부풀림은 세 자리를 **함께** 움직이므로 원래 단언(«영웅 vs X Δ0»)으로는
**영원히 초록**이다 — 이사만 하고 ⓑ 를 안 세웠으면 «ref 근거 없는 값이 전 화면으로 번지는» 바로 그 회귀를
게이트가 통과시켰을 것이다(214-④ 가 말한 자리). N6 은 반대로 «기대값을 페이지에서 다시 계산했다면 초록이었을»
회귀를 잡는다 — 링(ⓓ)과 가로 드롭이 초록으로 남는 것이 **항등식이 아니라는 증거**다.

## 6. 게이트·회귀

- **VERIFY96 61/61 PASS** (수리 전 45/47 · 항목 47 → **61**)
- **NEG219 27/27 PASS**
- **SMOKE PASS**
- 회귀 — `verify126sh` **19/19** · `verify166` **30/30** · `verify45` **36/36** · `verify157` **PASS** · `verify164` **31/31**
- `index.html` `git diff --stat` **공백** · 콘솔 에러 0 · **저장 구조 미변경**

## 7. «같은 규약을 검사하던 게이트» 전수 스윕 (213 관례)

- `tools/*.js` 중 `.stab`/`.ol3`/`.ol4`/`#shopCats` 를 만지는 게이트 전부에서 **`text-shadow` 를 재는 것은
  `verify126sh` 하나뿐**이고 19/19 초록이다 — 이 부패는 **`verify96` 단독**이다(217 계열처럼 번지지 않았다).
- **곁가지 등재 1건 → 221.** `verify47` 이 **124~125/126** 으로 **뜨고 지는** FAIL 이다(내 작업 전 트리에서 동일 —
  `index.html` 무변경이므로 선재 결함). 항목 [5] «재진입 — 칸 폭·위치 그대로(Δ≤0.6)»:
  **03 던전 2칸 · 10 상점 3칸이 재진입 후 폭 Δ−8.2px** (위치 Δ0.0). 회차마다 2건/1건으로 갈려 **간헐**이다.
  `.stabs` 재진입 시 폭 재계산(라벨 폭 보정 `scaleX` 재적용 시점) 계열로 보인다 — 96 구간이 아니라 47 소관이라 **안 건드렸다**.

## 8. 남긴 것

- 10 상점의 **비활성** 링은 공용과 같은 3/2 다(126 이 «AI 실측 ZERO ✅ 라 안 건드린다» 고 명시). 활성만 2.8/2.1 이라
  **같은 바 안에서 활성이 비활성보다 얇다** — 공용 3자리는 반대(4/3 > 3/2)다. ref 가 그렇다는 것이 126 의 판정이므로
  게이트는 그 상태를 그대로 못 박기만 했다. 03·06·13·23·47 의 ref 실측이 생겨 확산할 때는 **`RING`/`SHOP` 상수와
  ⓑ 항을 같이 갱신**하는 것이 그 작업의 범위다.
- `#shopCats` 특이도로 공용 `:is(#bSk,…) .ol4`(1,1,0)를 이기고 있다 — 클래스 선택자로는 못 이긴다는 126 의 주석이 그 이유다.
  나중에 «화면별 외곽선» 을 토큰(`--ol-r`/`--ol-d`)으로 여는 편이 깔끔하지만 그것은 126 계열의 설계 변경이라 여기서 안 했다.
