# 832 — `verify356` [S3] ③ 눈금 [전제] 1건 빨강 (게이트 부패 · 자·전제)

> **1회차 (2026-09-02, sess-1758-29795 루틴 워커 D · 제품 `index.html` 0줄 · 자 1파일 · 재현기 1파일 신설)**
> 결론: **자리는 닫혔다.** 등재문의 «판정 스코프 밖» 진단은 재현으로 **기각**했고,
> `KNOWN_SITES` 에서 근거와 함께 뺐다(750·596·601 선례 — «뺀 것이 곧 감시자다»).

---

## §0 착수 기준선 — 등재문 그대로 재현된다

```
node tools/verify356.js        →  VERIFY356 262/263 FAIL
  ✗ [S3] ③ 눈금 [전제] — 상한이 붙은(비 band) 11자리 중 10자리만 이번 실행이 쟀다:
        div#shopList>div.cn-wrap.pv>div.pvc.pb.ban1>div.rb.rb1>b>img.cic
```

빨간 항은 **이 하나뿐**이다(나머지 262 항 초록 · 같은 실행의 [S3] ② 스윕 = 화면 71/71 · 판정 518 ·
칸 57 · 자리 21 · 이름표 21/21 등재 · 눈금 13자리 전부 상한 안 · §R 되돌림 10/10).

## §1 재현 — 338 규칙대로 «처방 전에 그 자리를 제품에게 묻는다»

신설 `tools/probe832.js`. 등재문이 갈래로 남긴 두 답(«문턱 아래» ↔ «판정 스코프 밖») 중
어느 쪽인지를 `probe418.COLLECT` 의 가시 조건을 **한 문씩** 재서 가른다.

```
node tools/probe832.js
  §1 첫 쪽 — #shopList .rb.rb1 3개 · 그 안 b>img.cic 3개 · 화면 전체 img/canvas/svg 41개
     그릇 #shopList — 1740 → 2756 = 쪽 2 · overflowY auto · 보임 true
   · div#shopList>div.cn-wrap.pv>div.pvc.pb.ban1>div.rb.rb1>b>img.cic  ← 등재된 이름과 같다
     상자 52×52 @ 377.5,869.5 · 원본 64×64 · cur-dia.svg
     문: 크기≥8 true · 뷰포트 안 true · 보임 true · display block · object-fit contain
```

**세 문을 전부 통과한다 = 판정 스코프 «안» 이다.** 등재문의 ⓑ 가설은 여기서 끝난다.

## §2 그러면 왜 안 나오는가 — **dev 가 정확히 0 이다**

```
PROBE418_TOL=-1 node tools/probe418.js --screen "124 이용권 탭"
  [probe418] DSF2 · 화면 2개 · 잉크를 잰 노드 13개 (판정 13 · 원본비 없음 0 · 가려짐·잘림 0)
   0%  div#shopList>…>div.rb.rb1>b>img.cic   1칸 · 잉크 104×104
       상자 52.0000×52.0000 @ 377.5000,869.5000 · 화면: 124 이용권 탭
```

* **DSF2 3회 실행 전부 같은 값**(잉크 104×104 · dev 0.00% · 상자·좌표 소수점까지 동일) — 결정적이다.
* **DSF3 도 156×156 · 0.00%** — 배율을 올려도 안 되살아난다.

⚑ **등재문이 «스코프 밖» 이라고 읽은 이유**: 등재는 `PROBE418_TOL=0` 으로 «문턱을 0 으로 낮춰도
안 나온다» 를 근거로 삼았다. 그런데 판정은 `Math.abs(dev) > TOL` 이라 **dev 가 정확히 0 인 자리는
TOL 0 에서도 안 나온다**(엄격 부등호). ⇒ **TOL 0 은 «스코프 밖» 과 «dev = 0» 을 못 가른다.**
가르는 손잡이는 **음수 TOL** 이다(위 실행).

## §3 무엇이 닫았나 — 34회차 주석이 스스로 적어 둔 «한 device px»

34회차 등재문(= `KNOWN_SITES` 의 `why`)이 이렇게 적어 두었다:

> 상자는 이미 정수 52 이고 남은 것은 좌표가 .5(386.5, 876.5)라 래스터가 한 축을 1px 더 먹는
> 그 1px 이다(**한 device px 이 0.96%**).

산수가 그대로 닫힌다 — 그때 잉크가 **104×103** 이었고 `104/103 − 1 = +0.9709%` 다.
즉 그 자리의 편차는 **처음부터 세로 한 device px 이 전부**였다.

667(이용권 UI 재작업)이 5~9회차로 리본을 마감하며 그 1px 이 사라졌다:

| | 34회차(667 4회차 직후) | 지금(667 10회차 마감) |
|---|---|---|
| 배너형 금색 판 `b` | 91×91 | **79×79** (`#shopw .pvc.ban1>.rb>b`) |
| 그 안 `.cic` 상자 | 52.0000 | 52.0000 (`. pvc.ban1>.rb>b>.cic`) |
| 아이콘 좌표 | 386.5, 876.5 | **377.5, 869.5** (`.rb{left:−3 → 0}` · `w1 −3` · `--gx`) |
| 잉크 | 104×**103** | 104×**104** |
| dev | +0.97% | **0.00%** |

상자는 안 바뀌고 **자리가 옮겨 가면서** 래스터가 더는 세로 한 줄을 안 먹는다.
제품에 고칠 것이 없고, 이 작업의 `index.html` 변경은 **0줄**이다.

## §4 처방 — 목록에서 뺀다 (등재문 후보 ⓐ)

등재문이 적어 둔 두 후보 중 ⓐ(«667 이 리본을 마감한 뒤 다시 재고, 닫혔으면 근거와 함께
`KNOWN_SITES` 에서 뺀다»)를 그대로 집행했다. 667 은 `0486ffc done(667)` 로 **10회차 마감**됐다 —
772 세션이 «손대지 않았다» 고 적은 그 이유(«리본이 마감되기 전에 빼면 667 이 되살릴 자리를 미리
지우는 것») 가 사라졌다.

⚑ **dev 가 0 으로 닫힌 자리는 목록에 «남길 수가» 없다 — 구조적이다.**
[전제] 는 그 자리가 `R.groups` 에 있기를 요구하는데 거기 들려면 `|dev| > 0.5%` 여야 한다.
설령 문턱을 낮춰 들여놓아도 바로 아래 **§R 되돌림**이 `|dev| > |dev|/2` 로 **0 을 거른다**
(548 2회차 교훈 2 — «절반을 쓰는 이유는 부호·0 에 안 물리기 위해서다»).
⇒ 이 자리를 남겨 두는 한 [S3] 은 **영원한 빨강**이거나 **헛초록**이고, 둘 다 자를 죽인다.
601 이 `bgm-dia` 를 뺄 때 쓴 말이 같다 — «여기 남겨 두면 헛초록이 아니라 영원한 빨강이 된다».

⚑ **무르게 푼 수리가 아님을 두 가지가 못박는다.**
1. **상한을 안 건드렸다** — `cap` 을 키워서 초록을 만든 것이 아니라(그 길은 «줄이는 쪽으로만»
   규약 위반이다) **자리 자체가 문턱 아래로 닫혔음**을 실측으로 보이고 뺐다.
2. **뺀 것이 곧 감시자다**(750·596·601 선례) — 이 자리가 다시 0.5% 를 넘으면 목록에 없으니
   같은 절의 **[S3] ③ 이름표**가 «등재 안 된 자리» 로 즉시 빨개진다. 감시는 한 칸도 안 줄었다.

## §5 게이트 실행

| 자 | 착수 | 수리 뒤 |
|---|---|---|
| `node tools/verify356.js` | **262/263 FAIL** | **263/263 PASS** |

수리 뒤 같은 실행의 [S3] 절 — **스코프는 한 칸도 안 움직였다**:

```
  ✓ [S3] ② 스윕 — 화면 71개 전부 진입
  ✓ [S3] ② 스윕 — 판정 노드 518개 (원본비 없음 254 · 가려짐·잘림 33)
  ✓ [S3] ③ 래칫 — 칸 57개 ≤ 60
  ✓ [S3] ③ 래칫 — 자리 21개 ≤ 22
  ✓ [S3] ③ 이름표 — 나온 자리 21개가 전부 등재된 자리다 (등재 27개)      ← 등재만 28 → 27
  ✓ [S3] ③ 눈금 — 실측 상한이 붙은 12자리가 전부 상한 안이다             ← 13 → 12
  ✓ [S3] ③ 눈금 [전제] — 상한이 붙은 비 band 10자리를 이번 실행이 전부 쟀다
        (band 2자리 중 2자리는 이번 실행 문턱 아래 — 들락거림 · 정상)
  ✓ [S3] ③ 눈금 §R 되돌림 — 10자리가 전부 «넘었다» 로 잡힌다
```

⚑ **판정 518 · 칸 57 · 자리 21 · 이름표 21/21 이 착수와 소수점까지 같다** — 바뀐 것은
«등재 목록의 길이»(28 → 27) 하나다. 즉 **감시 범위를 줄여서 초록을 만든 것이 아니다.**

## §6 회귀 (전부 무수정)

| 자 | 결과 | 왜 이 자인가 |
|---|---|---|
| `node tools/verify772.js` | **18/18 PASS** | 스윕 항등식(판정 518 = 첫 쪽 317 + 스크롤 아래 201)·쪽 루프 — 스코프를 안 건드렸다는 짝 |
| `node tools/verify596.js` | **39/39 PASS** | 같은 «뺀 것이 곧 감시자» 선례의 자(입장권 회전 3자리) |
| `node tools/verify667.js` | **83/83 PASS** | 자리를 닫은 당사자(이용권 UI · 리본 기하) — 리본이 마감된 상태 그대로임을 못박는다 |
| `node tools/smoke.js` | **SMOKE PASS** | 필수 게이트 |
| `node tools/verifyProgress.js` | 종료 코드 **0** | 표 마감(§1~§5) |

## §7 곁다리 등재 — **0건**

빨강은 착수 때부터 이 한 항뿐이었고(262/263), 수리 뒤 0건이다.
새로 관측된 결함 없음.

## §8 다음 세션에게

* **`PROBE418_TOL=0` 으로 «안 나온다» 를 «스코프 밖» 이라고 읽지 마라.** 판정이 엄격 부등호라
  **dev = 0 인 자리도 안 나온다.** 가르려면 **음수 TOL**(`PROBE418_TOL=-1`)로 판정된 노드를
  편차와 함께 전부 인쇄하거나, `probe832` 처럼 가시 조건을 한 문씩 재라.
* **`KNOWN_SITES` 의 `cap` 자리가 0 으로 닫히면 그 항은 반드시 빠져야 한다** — [전제](R.groups 에
  있어야 한다)와 §R 되돌림(`|dev| > |dev|/2`)이 **둘 다** 0 을 거부한다. 남겨 두면 영원한 빨강이다.
