# 913 — `tools/verify878.js` 즉사(`Cannot find module 'pngjs'`)

**완료(2026-09-05, sess-0412-9101 루틴 워커 A · 1회차)** · 제품 `index.html` **0줄** · 어떤 자의 **점수·판정도 0줄**
· 신설 `tools/png913.js`·`tools/verify913.js` · 자 15개 한 줄씩 · 지시서 [6] «준비» 절 1문단.

지시서 [3]-**(가) 기계적 작업**으로 처리했다 — 레퍼런스 대조가 없는 «자가 돌게 하는» 작업이라 **비평가를 안 띄웠다.**

---

## 1. 재현 — 등재문 그대로다

```
$ node tools/verify878.js
Error: Cannot find module 'pngjs'
    at Object.<anonymous> (/home/user/kkkkkk/tools/verify878.js:29:13)
```

등재문의 «1초 즉사» 는 정확했다. 그리고 **깃발(907)과 무관하다**는 등재문의 실측도 맞다 — require 단계에서
죽으므로 브라우저가 뜨기도 전이다.

## 2. 뿌리가 «자» 가 아니었다 — 의존을 심으니 그 자리에서 8/8

```
$ npm i --no-save playwright pngjs
$ node tools/verify878.js
VERIFY878 8/8 PASS
```

**자 자신은 처음부터 멀쩡했다.** 878 의 점수·판정은 한 줄도 안 건드렸고(등재문의 ⚠ 조항), 건드릴 필요도 없었다.

## 3. 등재문이 «세어 보라» 고 한 것 — 답이 나왔고, 방향이 반대였다

등재문: «⚑ **ⓑ 가 1순위다** — pngjs 를 부르는 자가 몇인지부터 세면(`grep -l pngjs tools/`) 그 답이 나온다.»

세었다. **17개다.**

| 갈래 | 고치는 자 | 남는 즉사 | 자의 측정 로직 |
|---|---|---|---|
| ⓑ 의존을 없앤다(canvas 디코드로 갈아 끼움) | **1** | **16** | 878 의 디코드 경로가 바뀐다 — «판정 건드리지 마라» 와 충돌 |
| ⓐ 의존을 심는다 + 없으면 크게 말한다 | **17** | 0 | **0줄** |

⇒ **답은 나왔지만 등재문이 기대한 방향과 반대였다.** 등재문은 «부르는 자가 한둘일 것» 을 전제로 ⓑ 를 1순위로
적었다. 17개 앞에서 ⓑ 는 «한 자를 살리고 열여섯을 그대로 두는» 길이다. **위임 규약(2026-09-01)** 대로 ⓐ 를
그 회차에서 즉시 채택했다. 되돌리려면 `tools/png913.js` 를 지우고 각 자의 한 줄을 `require('pngjs')` 로
되돌리면 된다(제품은 애초에 0줄이라 되돌림 비용이 자 15줄뿐이다).

## 4. ★ 진짜 결손은 «못 도는 것» 이 아니라 «못 도는 줄 아무도 모르는 것» 이었다

등재문이 스스로 짚은 자리다 — «점수 줄이 아예 안 나오므로 이 자는 «빨강» 이 아니라 **«없는 자»** 로 지나간다».

| | 옛 날 `require('pngjs')` | 913 부트스트랩 |
|---|---|---|
| 종료 코드 | **1** (= claim.js 규약의 «오류») | **2** (= «환경에 없음», 110 `pw()` 와 같은 값) |
| 출력 | `MODULE_NOT_FOUND` 스택 트레이스 12줄 | `pngjs 없음 — npm i --no-save playwright pngjs` **한 줄** |
| 워커가 할 일 | 스택을 읽고 스스로 알아내야 한다 | 줄에 그대로 적혀 있다 |

playwright 쪽은 **110 이 이미 그 길을 닫아 뒀다**(`pwlaunch.pw()` — «playwright 없음 — …» + 코드 2).
pngjs 만 안 닫혀 있었다. ⇒ **같은 말투로** 답하게 했다(`tools/png913.js`). 새 규약을 만들지 않고 있는 규약에 붙였다.

## 5. ⚑⚑ 뿌리의 뿌리 — 지시서가 pngjs 를 **지우고 있었다**

측정 중에 나온 것이고, 이것이 913 이 다시 안 나게 하는 유일한 조항이다.

```
$ npm i --no-save playwright     # → node_modules: playwright, playwright-core
$ npm i --no-save pngjs          # → added 1 package, and **removed 2 packages**
$ ls node_modules                # → pngjs   (playwright 가 사라졌다)
```

이 저장소는 `package.json` 이 **없어서** `--no-save` 를 부를 때마다 `node_modules` 가 통째로 다시 그려진다.
**따로 두 번 부르면 뒤 호출이 앞 패키지를 지운다.**

그런데 이 사실은 **이미 LESSONS 에 세 번 적혀 있었다** — `2507~2509` · `23850~23853` · `30789~30793`.
2509 는 처방까지 정확히 적어 뒀다: «준비는 항상 한 번에: `npm i --no-save playwright pngjs`».

**그런데도 913 이 났다.** 왜냐면 워커가 실제로 따르는 것은 LESSONS 가 아니라 **지시서 [6] «준비» 절**인데,
그 줄은 2026-09-05 까지 이렇게 적혀 있었기 때문이다:

> 준비: `node tools/smoke.js` 가 playwright 를 못 찾으면 `npm i --no-save playwright && npx playwright install chromium` 후 재실행.

즉 **지시서를 성실히 따르는 워커일수록 pngjs 를 확실히 지운다.** 교훈이 세 번 기록되고도 계속 다시 배워진
이유가 이것이다 — 기록된 자리와 읽히는 자리가 달랐다. ⇒ **[6] «준비» 절을 고친 것이 이 작업의 본체다**
(`npm i --no-save playwright pngjs` · «반드시 한 번에» · 왜인지까지). `verify913` [C1]~[C3] 이 그 줄을 지킨다.

곁다리로 옛 줄의 `npx playwright install chromium` 도 정리했다 — 이 환경은 다운로드가 막혀 **실패하는 명령**이고
브라우저는 `/opt/pw-browsers` 에 이미 있어 `pwlaunch` 가 찾아 쓴다(LESSONS 128·1122·17576 이 세 번 적어 둔 것).

## 6. 면제 둘 — 부르되 안 죽는 자는 그대로 뒀다

17 중 둘은 **이미 pngjs 없이도 끝까지 돈다.** 여기에 `exit(2)` 를 심으면 그 폴백을 오히려 죽인다.

- `tools/verify747.js` — `try { require('pngjs').PNG } catch {}` → **python3 PIL** 로 내려간다.
- `tools/verify54.js` — 애초에 안 부른다. 주석이 못박아 뒀다: «PNG 디코드는 크로미움 자신에게 시킨다 —
  npm 의존성 0 (**pngjs 는 이 환경에 없다**)».

⚑ **이 둘이 등재문의 ⓑ 가 옳은 방향이었음을 증언한다** — 앞선 워커 둘이 독립으로 같은 벽을 만나 각자 폴백을
지었다. 다만 그 길은 **자마다 다시 짓는** 길이라 15자에 쓰기엔 값이 너무 크다. 913 은 ⓐ 로 17을 한 번에 살리되,
이미 폴백을 가진 둘은 **건드리지 않는 것**을 택했다. `verify913` [B4]·[B5] 가 그 면제가 헛말이 아님을 묻는다
(«정말로 폴백을 갖는가» 를 소스에서 확인한다).

## 7. 게이트

`node tools/verify913.js` — **16/16 PASS**

- **[A]** 부트스트랩이 있고, 안내문이 «한 번에 부르는» 명령을 110 과 같은 말투로 적는다.
- **[B]** 하드 require **0건** · 부트스트랩 경유 16 · 면제 둘이 실제로 폴백을 갖는다.
- **[C]** 지시서가 두 패키지를 한 명령으로 적고, **pngjs 를 지우는 옛 명령이 안 남아 있다**.
- **[R] 되돌림 시험** — `node_modules/pngjs` 를 실제로 감춘 뒤 굴려 **코드 2 · «한 줄» · 스택 트레이스 없음**을
  확인하고 트리를 되돌린다. [A] 가 «선언» 을 읽는다면 [R] 은 **그 선언이 지켜지는지**를 잰다 — 헛초록이 아님을 못박는다.

## 8. 회귀 — 전부 «없는 자» 였던 자들이다

| 자 | 913 전(새 컨테이너) | 913 후 |
|---|---|---|
| `verify878` | 즉사 (코드 1 · 점수 줄 0) | **8/8 PASS** |
| `verify81` | 즉사 | **11/11 PASS** |
| `verify148` | 즉사 | **16/16 PASS** |
| `verify840` | 즉사 | **24/24 PASS** |
| `verify913` | — | **16/16 PASS** (신설) |
| `smoke` | — | **SMOKE PASS** |

네 자 모두 **점수가 안 바뀐 게 아니라 «점수가 처음 나왔다»** — 새 컨테이너에서는 예외 없이 즉사하던 자들이다.

## 9. 교훈 (LESSONS 로)

1. **«기록됐다» 와 «읽힌다» 는 다르다.** 같은 교훈이 LESSONS 에 세 번 적히고도 네 번째로 사고를 냈다면,
   문제는 워커의 성실함이 아니라 **교훈이 지시서의 «따라 하는 줄» 에 안 들어간 것**이다.
   재발을 막는 편집은 «한 번 더 적기» 가 아니라 **«실제로 실행되는 줄을 고치기»** 다.
2. **점수 줄이 안 나오는 자는 «초록» 도 «빨강» 도 아닌 «없는 자» 다.** 스윕은 없는 자를 안 센다 —
   즉사는 실패보다 조용하다. 자가 죽을 때는 **할 일 한 줄과 구분되는 종료 코드**를 남겨야 스윕에 걸린다.
3. **«몇 개인지 세라» 는 처방은 세고 나서 방향이 뒤집힐 수 있다.** 등재문의 ⓑ 1순위는 «한둘» 전제였고
   실측 17 앞에서 ⓐ 가 됐다. 세라고 적은 등재문이 옳았다 — **세기 전에 따르지 않은 것**이 옳았다.
4. **`package.json` 없는 저장소에서 `npm i --no-save` 는 «추가» 가 아니라 «교체» 다.** 준비는 항상 한 번에.
