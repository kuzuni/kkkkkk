# 885 — 10 상점 «이용권» 카드 폴리시 잔여 (833 라운드 마감분)

833 이 10/10 을 소진하고 넘긴 넷(① 불릿 카드 «아트 자리» · ② 불릿형 노치 «세로 길이» ·
③ 잔여 틈 다섯 · ④ 배너 리본1 «제비꼬리 노치»)을 이 번호로 잡는다.
통과선은 833 과 같은 **①~④ 최저 9**(같은 화면·같은 등재문).

- 회차는 **이 번호에서 새로 센다**(833 은 «⏸ 보류(10회 소진)» 로 마감됐다 — 838·856·881 선례).
- 앞 라운드의 «닫힌 자리» 목록은 그대로 물려받는다(브리핑 `scratch/brief885.md` 에 옮겨 적었다).

---

## 1회차 (2026-09-03, sess-1858-2189 루틴 워커 D)

캡처 `151-r31`(9:19) · `151-r31-h1600`(9:13.3) — **833 10회차 제품**(우변 외곽선 끊김 수리)의
첫 채점본이다. 그 회차는 채점을 안 받고 라운드가 끝났다.

### ⓐ 등재문 ①·② 는 둘 다 «자를 먼저 세워라» 자리였다 — `tools/scan885.py` 신설

등재문이 «착수 세션이 먼저 정하라» 고 못박은 두 축을 **손대기 전에** 화소로 쟀다(338 규칙).

#### ① 아트 자리 — 판은 «ref 에 없는 것» 이 아니라 **ref 일러스트 상자**다 (DH·DI 둘 다 맞다)

자: ref 초록 카드 안에서 **초록 계열이 아닌**(G > R+25 · G > B+15) 밝은(L ≥ 70) 화소 덩이의 bbox.
⚠ «몸통색이 아닌 화소» 로 잡으면 안 된다 — 몸통이 **두 톤 마름모 무늬**(측정표 §12-2)라
카드 전체가 잉크가 된다(실제로 한 번 그렇게 짰고 열 합이 어디서나 60~150 이었다).
★ 목록의 흰 글자는 **열 프로파일의 빈 골**(x 255~261 에서 상대 문턱 아래)로, 별폭죽 배지는
**빈 행**(y≈395)으로 가른다.

| (우리 px 환산 · k = 2.0628) | ref 일러스트 «보이는» 상자 | 우리 «아트 판» | Δ |
|---|---|---|---|
| 우변 (카드-로컬) | **962.3** | 962.0 | **−0.3 ✓** |
| 상변 (카드-로컬) | 140.3 | 135 | **−5.3 ✓** |
| 하변까지 (카드 하변) | 344.5 | 252 | −92.5 |
| 높이 | **280.5** | 303 | **+22.5 (+8.0%)** |
| 폭(보이는 만큼) | 445.6 | 379 (판 462 − ★판이 덮는 83) | −66.6 |

⚠ 세로 두 줄은 **카드 상변을 다시 잡은 뒤의 값**이다 — 아래 ⓒ 를 먼저 보라
(`scan667 --ref` 가 쓰던 302 는 카드가 아니라 그 위에 얹힌 부품을 문 값이고, 진짜 상변은 **328.5** 다).
상변이 −5.3 으로 맞고 하변까지가 −92.5 인 것은 **우리 불릿 카드가 ref 보다 78px 짧기 때문**이다(ⓒ).

⇒ **DI 의 456×303 과 DH 의 458×272 는 «같은 자리를 반대편에서 본 것» 이 맞았다** — 자로 재니
ref 일러스트의 **보이는** 상자가 **445.6×280.5** 로 DH 값(458×272)과 ±3% 안이다.
⇒ **판정(지시서 [3] «아트 자리 규칙» · 위임 규약)**: 이 판은 **아트가 들어올 자리**이므로
«ref 에 없으니 지운다» 가 아니라 **ref bbox 를 차지하게** 만든다. 판 안이 이모지·백판인 것은
⑤ 아트 축이라 감점 밖이고, **판의 w×h·중심은 ①②③④ 로 채점한다** — 이 판정을 비평 브리핑에 적었다.

### ⚠ 그런데 «폭·좌단» 은 이 자로 못 닫는다 — **양쪽 다 ★판이 왼쪽을 덮고 있다**

측정표 §5-2 가 적어 둔 대로 **★판이 일러스트 «위»에 그려진다**(우리도 `verify667` [F6] 로 같은 순서다).
그래서 화소로 잡히는 것은 **일러스트의 왼쪽이 잘린 나머지**이고, 폭·좌단은 **하한**일 뿐이다.
게다가 «★판의 우단» 이 자마다 다른 값을 낸다(이 회차가 실제로 두 번 다른 답을 얻었다):

| 자 | ref ★판 우단(카드-로컬 우리 px) | 우리 |
|---|---|---|
| 색 마스크 «어두운 초록»(G>R+20 · G<140 · G>B+10) · 행 50% | 582.8 | 583 (**일치**) |
| 그 행의 화소를 직접 읽기(y 420·450·500) | **≈512.6** | ≈588 |

⇒ 색 마스크는 ref 에서 **일러스트의 어두운 회록색 면**(53,57,34)·(61,45,36) 까지 ★판으로 세고 있고,
직접 읽기는 «§5-1 ★ 목록은 한 판이 아니라 **행 4개**» 라 **행마다 우단이 다르다**.
**두 자가 서로 다른 것을 재고 있으므로**(A3-ⓑ) 이 회차는 폭·좌단에 **손대지 않는다** —
`verify667` [F5](판 500,135 462×303) · [F7](★판 겹침 86) · `verify833` [14-c] 세 자리가 한 값에 묶여 있어
**하나를 손으로 옮기면 나머지 둘이 유령을 좇는다.** 닫으려면 «★판 행별 우단» 자를 먼저 세워야 한다.
⇒ **① 축은 이 회차 제품 0줄**(수리는 ④ 한 자리다 — 아래 ⓑ). 다음 회차가 볼 순서는 그 자 → 폭·좌단 → 세로.

⚠ **세로 자리(상변 −65.1 · 하변 −42.6)도 이 회차는 안 건드린다** — ref 카드가 우리보다 **135px 길다**
(825 ↔ 690)라 «상변에서 몇 px» 이 그대로 안 옮겨진다. **비율로는 이미 맞다** —
판 세로 중심이 카드 높이의 **ref 41.3% ↔ 우리 41.5%** 다. 어긋난 것은 **높이(+8.0%)** 한 축뿐이다.

#### ② 노치 «세로 길이» — 한 값으로는 못 맞춘다. **길이 축이 아니라 «모양» 축**이다

DH(신뢰 상 «−11.0~16.3%, 세 임계 모두 같은 부호») ↔ DI(신뢰 하 «목표가 안 선다»)가 정반대라
8회차가 배너형에 쓴 **같은 자**(`tools/scan667.py`)를 문턱 스윕으로 돌렸다(LESSONS A3-ⓑ).

| 문턱(우리 px) | ref 불릿 노치 길이(×k) | 우리 | Δ% |
|---|---|---|---|
| 2 | 119.6 | 94 | **−21.4%** |
| 4 | 107.3 | 92 | −14.3% |
| 6 | 97.0 | 92 | −5.2% |
| 8 | 92.8 | 91 | −1.9% |
| 12 | 86.6 | 88 | **+1.6%** |
| 16 | 82.5 | 82 | −0.6% |
| 20 | 76.3 | 76 | −0.4% |

⚑ **부호가 뒤집힌다 — 그런데 «잡음» 이 아니라 «모양» 이다.** 우리 노치는 **타원 하나**(마스크
`radial-gradient(깊이 × 길이/2)` · 링 `s`/`u` 도 같은 타원)라 깊은 쪽(문턱 ≥8)에서는 ref 와 ±2% 안인데,
얕은 쪽(문턱 2~6)에서 ref 가 **꼬리를 길게 끈다**(ref 종횡 119.6/33 = 3.62 ↔ 우리 94/33 = 2.85).
⇒ `NTC_LEN.bl` 을 올려 문턱 2 를 맞추면(+27%) 문턱 12~20 이 그만큼 넘친다. **한 상수로는 답이 없다.**
⇒ 이 회차는 **값을 안 건드린다**. 고치려면 마스크를 «얕고 넓은 타원 ∪ 깊고 좁은 타원» 두 겹으로
바꿔야 하고(그러면 링 `s`·`u` 도 같은 모양이어야 한다) 그것은 «길이 상수» 수리가 아니다 —
다음 회차 이후의 후보로 남긴다. 8회차가 배너형에서 내린 판정(«불릿형은 스윕이 부호를 뒤집는다»)은
**이 표로 재확인**됐고, 이번에는 **왜 뒤집히는지**(종횡비 3.62 ↔ 2.85)까지 적었다.

### ⓑ 수리 — ④ 배너 리본1 «제비꼬리 노치» (제품 `index.html` 1자리 · 게이트 1항 이관 + 1항 신설)

등재문 ④(833 8회차 비평 DF 4순위 · 1인 지적 · 미착수)를 **자로 재서 사실로 만들었다**(338 규칙).
`verify667` **[F9] 가 «리본1 엔 제비꼬리가 없다 — ref 도 라운드 코너뿐» 으로 굳어 있었고 틀렸다.**

| ref 리본 (빨강 채움 행별 우단 · 문턱 60/90/130) | 깊이 ref px → 우리 px | 최심 행 |
|---|---|---|
| 파랑 rb1 | 3~4 → **6.2~8.3** | **36~52% 높이** |
| 파랑 rb2 | 3~4 → 6.2~8.3 | 44~48% |
| 초록 rb1 | **1 → 2.1** | 0~86%(평평) |
| 초록 rb2 | 4 → 8.3 | 43~46% |

⚑ **«라운드 코너»/«제비꼬리» 를 가르는 것은 깊이가 아니라 «최심이 어디냐» 다** — 라운드 코너는 띠의
위·아래 **끝**이 깊고 제비꼬리는 **가운데**가 깊다. 파랑 rb1 의 최심은 36~52% 높이다 ⇒ 제비꼬리다.
⇒ **이것도 «형마다 두 값»** 이다(피치·앵커·노치 길이·노치 깊이·일러스트 자리에 이어 여섯 번째).

**수리**: `.pvc.ban1>.rb1::after` 를 **리본2 규칙에 얹어** 붙였다(사본 0 — 402) ·
불릿형은 안 붙인다. 실측 **우리 파랑 rb1 깊이 0 → 5px**(최심 45~52% 높이 · 리본2 와 같은 값 5) ·
**리본2 는 5 그대로**(닫힌 자리 무변경).

**게이트**: `verify667` [F9] **방향 전환**(333 처방 — 자리를 안 비웠다) + **[F9b] 신설**
(«불릿형 리본1 에는 없다» — 안 넣으면 «네 리본에 다 붙여도 초록» 이 된다) ⇒ **86/86 → 87/87** ·
`verify833` **197/197 무수정** · `verify151` **84/84 무수정** · `probe833` **끊김 0 유지**(10회차 수리 회귀) ·
**SMOKE PASS** · 측정표 **§15-1 정오표** 신설.

## 1회차 채점 — 비평 DL · DM (캡처 `151-r31` + `151-r31-h1600` = 833 10회차 제품)

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| DL | 7 | 7 | 8 | 7 | 9 | 9 | **7** |
| DM | 9 | 8 | 9 | 9 | 9 | 9 | **8** |

⇒ **불통과**(통과선 9 · 한 사람도 못 닿았다). 회차 **1/10**. 둘 다 k 를 재검증했다(DL 2.062±0.003 · DM 2.0646±0.0017).

### DL 이 «닫힌 자리» 로 되돌려 준 것 — ②(노치 세로 길이)는 **맞다**

이 회차의 자와 **독립으로 같은 결론**에 닿았다: «ref 3번 노치 FWHM 40 ref px → 82.5 ↔ 우리 82(Δ −0.5) ·
임계 t=1/2/4/8 스윕에서 t≥4 는 |Δ|≤2 로 수렴하고 t=1·2 의 −14% 는 **ref 다운스케일 소프트에지 꼬리**다».
⇒ 등재문 ②(DH 신뢰 상 ↔ DI 신뢰 하)는 **DI 쪽으로 닫혔다** — 자 둘이 서로 다른 방법으로 같은 답을 냈다.
DL 이 그 밖에 «문제 없음» 으로 닫은 자리: 배너 리본1 꼬리 우단(+0.1) · 상태 탭 우단(−1.0) ·
알약→리본1 틈(+0.7) · 머리띠→첫 알약 틈(−1.0) · 카드 간 틈(−0.3) · 불릿 알약 h/w/피치/좌단(±1.5) ·
헤더 밴드(±1.2). **등재문 ③ «잔여 틈 다섯» 은 DL 의 자에서 전부 ±1 안이다**(탭→쿠폰칩 틈만
문턱 20/40/80/140 스윕에서 **부호가 뒤집혀** «측정 한계» 로 분류 — A3-ⓑ).

### DL [3] 은 이 회차가 **이미 고친** 자리다 (④ 제비꼬리)

DL 은 캡처 `r31`(수리 전)에서 «파랑 rb1 깊이 0 ↔ ref 4 ref px = 8.25 → **−100%**» 를 냈다 —
ⓑ 의 자·값과 같다(문턱 셋 스윕 · 검정 실루엣으로도 같은 값). ⇒ **2인(DF·DL) 일치로 굳었다.**
⚠ **다만 DL 은 «깊이» 도 −39~52% 로 얕다고 본다**(우리 rb2 5 ↔ ref 8.25). 우리 값은 667 8회차가
«ref 3 ref px» 로 굳힌 것(닫힌 자리 [F8])이고 이 회차 스윕은 **문턱 60 에서 3 · 90/130 에서 4** 다 —
±1 ref px 안이라 이 회차는 안 건드렸다. 2인이 «4» 를 내면 폭 12 → 14 한 값이다.

### DL [1][2] — 다음 회차가 볼 두 건 (DM 대조 필요)

- **[1] 아트 판 폭 +17~21.5% · 좌단 −78~86px**(신뢰 상). ⚑ DL 은 ★판에 가려지지 않는 자를 썼다 —
  **티켓 밝은 테두리** 마스크로 ref 일러스트 좌단을 **296 ref px** 로 잡고(밝기 임계 150/170/185/200 에서
  좌단 고정), «ref 는 티켓 좌단 296 이 **불릿 알약 우단 295** 와 맞물린다(+1 ref px)» 는 **구조 검산**을 붙였다.
  우리 판 좌단은 알약 우단보다 **85px 왼쪽**이라 알약 밑으로 파고든다. ⇒ ⓐ① 이 «미판정» 으로 둔
  폭·좌단 축에 **처음으로 ★판과 무관한 자**가 생겼다. DM 이 같은 부호를 내면 그것이 다음 회차 1순위다.
- **[2] 불릿 카드 노치 열이 카드 세로 중심에서 +85.5px 아래**(신뢰 상 · 임계 t=2/4/8/16 불변).
  자체 대조: **우리 배너 카드는 정상**(편차 −1.5)이고 ref 도 대칭(+0.5)인데 **불릿 카드만** 밀렸다.
  ⚑ 뿌리는 알고 있다 — `pvNotches()` 가 **하변 앵커**(`NTC_CEN0` 160)로 찍는데 우리 불릿 카드가
  ref 보다 **135px 짧아**(690 ↔ 825) 세 자리가 **두 자리**로 줄어든 탓이다(세 번째가 37px 모자라 떨어진다).
  ⇒ ref 의 두 사실(«맨 아래 중심 160» · «무늬가 카드에 대칭»)이 **짧은 카드에서는 동시에 성립하지 않는다.**
  자체 실측(같은 자로 ref·우리): ref 편차 **+23.7px** ↔ 우리 **+71px**.
  고치는 길은 `pvNotches` 를 «하변 앵커» 에서 **«무리 가운데 정렬»** 로 바꾸는 것이고,
  그러면 배너형은 **Δ2px**(87.5 ↔ 현행 89.5 — 닫힌 자리 그대로) · 불릿형만 71.5px 올라온다.
  ⚠ `verify667` **[A6]**(맨 아래 노치 중심 = 160)이 그 순간 빨개진다 — **방향 전환이 필요한 이관**이라
  2인 일치를 먼저 받는다.

### ⓒ ⚑⚑ 이 회차의 본체 — **ref 카드의 «상변» 을 우리 자가 27 ref px 위에 잡고 있었다**

DL [2]의 «불릿 노치 열이 카드 세로 중심보다 +85.5px 아래» 를 검산하다 **두 자의 카드 상자가 다른 것**을
발견했다(DL 은 ref 초록 카드를 329..701 로, 저장소의 `tools/scan667.py --ref` 는 **302..701** 로 잡는다).
화소로 판정했다 — 카드 바깥선(검정)이 시작되는 행을 **깨끗한 열**(부품이 안 걸친 x=150·400)에서 읽는다:

| ref 카드 | 검정 시작 행 | 상변 | 하변 | 높이(ref px → 우리 px) | 우리 카드 |
|---|---|---|---|---|---|
| 파랑(배너형) | y32~33 | **32.5** | 291 | 258.5 → **533.4** | 534 (**Δ +0.6 ✓**) |
| 초록(불릿형) | y328~329 | **328.5** | 701 | 372.5 → **768.4** | 690 (**Δ −78.4 = −10.2%**) |

⇒ `scan667` 의 302 는 카드 상변이 아니라 **그 위에 얹힌 부품**(상태 탭·마일리지 알약·별폭죽 배지)의
윗선이다 — 그 자가 헤더 밴드 색으로 밴드를 찾는데, 탭 판이 같은 어두운 색이라 같이 물었다.
x=150·400 처럼 부품이 안 걸친 열에서는 **328.5** 로 딱 떨어진다(배경 → 검정 3px → 헤더 밴드 (33,145,97)).

⚑ **그래서 «우리 카드가 135px 짧다» 는 이 회차의 앞 절 계산도 틀렸다 — 실제로는 78px(−10.2%)** 이고,
**배너 카드는 ref 와 0.6px 안에서 같다.** 어긋난 것은 불릿 카드 하나뿐이다.
뿌리도 보인다 — ref ★ 목록은 **행 4개**(측정표 §5-1)인데 우리 `PASS_ITEMS[].bl` 은 **3개**다.
카드 높이가 내용에서 나오므로 이것은 «레이아웃 상수» 가 아니라 **문구 수** 축이다(주인 문구라 이 회차가 안 건드린다).

⇒ **DL [2] 는 그 78px 의 그림자다.** 같은 자로 다시 재면(카드 상자 = 위 표):
ref 세 노치 무리 중심이 카드 중심에서 **−4px**(사실상 대칭) ↔ 우리 두 노치 무리 중심 **−71.5px**.
`pvNotches()` 가 ref 실측 앵커(맨 아래 노치 중심 = 하변 160)로 아래에서부터 찍는데, 카드가 78 짧아
**세 자리가 두 자리로 줄면서**(세 번째가 37px 모자라 떨어진다) 무리가 통째로 아래로 몰린 것이다.
ref 의 두 사실(«하변 앵커 160» · «무늬가 카드에 대칭»)은 **ref 카드 높이에서만 동시에 성립한다.**

### ⓓ 2인 일치 — 둘 (하나는 이 회차가 이미 고쳤고, 하나를 이 회차가 닫았다)

**1) ④ 배너 리본1 제비꼬리** — DL [3](신뢰 상) · DM [1](신뢰 상). 둘 다 **ref 깊이 4 ref px = 8.3 우리 px** ·
**우리 0** · 문턱 스윕(DL 3종 · DM 40/60/80/120)에서 부호 불변 · 둘 다 «꼬리 우단 자리 자체는 정상»(Δ≤1)을
같이 적었다. DM 은 «같은 화면 안의 자기모순»(리본2 는 깊이 5, 리본1 만 0)까지 붙였다.
⇒ **ⓑ 가 이미 고쳤다**(캡처 `r31` 은 수리 전 판이다). 8회차 DF 까지 세면 **비평가 셋**이 같은 자리를 봤다.

**2) 아트 판 «세로»** — DL [1](h +9.6%) · DM [2](h +7.9% · 신뢰 중) · 이 회차의 자(+8.0%) = **셋이 독립으로 같은 부호**.
ref 높이(우리 px 환산) **DL 276.4 · DM 280.7 · scan885 280.5** ↔ 우리 303. 상변도 셋 다 «우리가 9px 위»
(DL 142.3 · DM 144.2 · scan885 145.4 ↔ 우리 135).

**수리**: `--art-t 135 → 142` · `height 303 → 281` · `em{line-height}` 도 같이(667 6회차와 같은 짝).
실측 대조(`scan885 --art`): **높이 Δ +22.5 → +0.5** · **상변 Δ −9~10 → −3.4** ·
하변까지 −77.5 = **카드 높이 부족분(−78.4)과 같다** ⇒ 세로 축은 «카드가 78 짧다» 하나로 설명된다(ⓒ).
**게이트**: `verify667` [F5] **이관**(세로 두 값 · 폭·좌단은 그대로) · `verify667` E 표 `bl.art.h` 303 → 281 ·
`verify833` [14-c] **이관** ⇒ `verify667` **87/87** · `verify833` **197/197** · `verify151` 84/84 ·
`verify124` 76/76 · `probe833` 끊김 0 · **SMOKE PASS**.

⚠ **폭·좌단은 이 회차가 안 건드렸다** — 두 비평가가 **«어디까지가 티켓인가» 정의로 갈렸다**:
DL «흰 테두리만 → ref 폭 375.4 · 좌단 −86px» ↔ DM «회색 스텁 포함 → ref 폭 445.9 · 좌단 −15.8px».
**부호는 같고 크기가 5배 다르다**(A3-ⓑ). DM 자신이 «좌단만 정의 의존» 이라고 적었다.
⇒ 다음 회차 1순위 = **그 정의를 가르는 자**(DL 의 구조 검산 «ref 는 티켓 좌단이 불릿 알약 우단과 +1px 로 맞물린다»
가 열쇠다 — 우리 ★판 우단은 583~588 이고 우리 판 좌단은 500 이라 **85px 파고든다**).

### ⓔ 1인 지적 — 다음 회차가 볼 것 (전부 «둘이 갈렸다» 라 이 회차는 안 건드렸다)

- **DL [2] 불릿 노치 열이 카드 중심보다 +85.5px 아래**(신뢰 상) ↔ **DM 은 같은 자리를 «정상» 으로 쟀다**
  («하단 기준 노치 중심 ref 149.6 ↔ 우리 149 · 두 번째 377.6 ↔ 376 · 셋째는 카드가 81px 짧아 안 그려진다 —
  불릿 개수 파생이라 **감점 안 함**»). ⇒ 둘 다 **카드가 짧다**(ⓒ)는 같은 사실을 봤고 **감점 여부만 갈렸다.**
  `pvNotches` 를 «하변 앵커» → «무리 가운데 정렬» 로 바꾸면 배너형 Δ2px · 불릿형 71.5px 상승이지만
  `verify667` [A6](ref 실측 앵커 160)이 빨개진다 ⇒ **2인 일치 전에는 안 건드린다.**
- DM [3] 탭→쿠폰칩 틈 −27%(신뢰 중 · 스윕 불변) ↔ **DL 은 같은 자리를 «문턱 20/40/80/140 에서 부호 뒤집힘 =
  측정 한계»** 로 분류. · DM [4] 탭 판 우단 +5.8 · 폭 +3.5% ↔ **DL 은 탭 우단 −1.0 «정상»**.
- DM [5] 보석 박스 가로 −4.2%(신뢰 하 · 두 정의 부호 동일 · 높이는 맞다).
- DL: 우리 제비꼬리 «깊이» 도 ref 대비 −39~52%(rb2 5 ↔ ref 8.3) ↔ **DM 은 rb2 를 «Δ−1.2 정상»** 으로 봤다
  (667 [F8] 이 ref 3 ref px 로 굳힌 값). 2인이 «4 ref px» 를 내면 폭 12 → 14 한 값이다.

### ⓕ 직전 회차(833 10회차) 수리는 **두 사람 다 «0» 으로 확인**했다

우변 외곽선 끊김 **0 구간 / 0px** · 림 사라진 행 **0개**(둘 다 배지·탭이 걸친 구간은 규약대로 제외).
DM 은 노치 4곳 전 구간에서 «검정 외곽선과 밝은 림이 곡선을 따라 연속» 임을 3~4배 확대로 같이 확인했다.
**9:13.3 — 두 사람 다 «없음»**(DM: |Δ|>30 화소 0개 · 평균 1.09/0.78/1.06 · 네 회차째).

### ⚑ 2회차가 할 것 (순서대로)

1. **캡처부터** — `node tools/cap151.js <경로>/151-r34.png --crop` 와 `--crop --h 1600` **둘 다**.
   1회차 수리 둘(리본1 제비꼬리 · 아트 판 세로)이 **아직 채점을 안 받았다.**
2. 브리핑 «닫힌 자리» 에 **23(리본1 제비꼬리 — 배너형만 · 깊이 6)** · **24(아트 판 세로 — t142/h281)** ·
   **25(카드 상변 = 328.5 · 배너 카드 높이는 ref 와 Δ0.6)** 를 더하고, ⓒ 의 **«우리 불릿 카드는 ref 보다
   78px 짧다(★ 목록 3행 ↔ ref 4행) — 카드 하단 기준 자리는 그만큼 당겨져 보인다»** 를 표로 넣어라.
   안 넣으면 «판이 아래로 92 밀렸다»·«노치가 85 아래다» 가 유령으로 계속 올라온다.
3. **1순위 = 아트 판 «폭·좌단»** — 먼저 **정의를 가르는 자**를 세워라(위 ⓓ 끝). DL 의 구조 검산을 그대로 쓰면
   «판 좌단 = ★판 우단» 이 되고 그때 `verify667` [F7](겹침 86)이 함께 뒤집힌다 — **셋을 한 회차에** 옮겨라.
4. 2순위 = DM [3][4](탭 자리·틈)와 DL 의 반대값 중 **어느 자가 무엇을 재는지** 가르기.

---

## 2회차 (2026-09-03, sess-2043-487 루틴 워커 C)

캡처 `151-r34`(수리 전 · 1회차 제품) → 수리 → `151-r35`(9:19 · 9:13.3 둘 다). 채점은 **r35** 로 받는다.

### ⓐ 1순위였던 «아트 판 폭·좌단» — **자를 세워 닫았다** (`tools/scan885.py --edge` 신설)

1회차가 «두 비평가가 «어디까지가 티켓인가» 정의로 갈렸다(부호는 같고 크기가 5배)» 며 미판정으로 남긴 축이다.
DL «흰 테두리만 → ref 좌단 296 ref px · 우리 −86» ↔ DM «회색 스텁 포함 → 우리 −15.8».

⚑ **가른 물음은 «어느 자가 옳으냐» 가 아니라 «알약 띠 뒤로 일러스트가 이어지는가» 하나였다.**
ref 가 스스로 답한다 — 알약은 4줄이고 **줄 사이에 띠가 없는 틈**이 있다:

| 틈(ref y) | 그 틈에서 본 잉크 최좌단(ref x) | 색 | 띠 행에서는 |
|---|---|---|---|
| 428~432 | **256**(검정) / 262(청백) | (175,191,192) | **없다** |
| 467~472 | 260 / 266 | (154,175,190) | **없다** |
| 507~512 | 264 / 272 | (170,175,181) | **없다** |
| (대조) 티켓 속 y445 x400 | — | (240,255,252) | — |
| (대조) 띠 속 y440 x200 | — | (58,97,70) | — |

틈에서만 보이고 띠 행에서 사라진다 ⇒ **일러스트는 띠 «뒤» 로 이어진다.**
⇒ DL 의 296 은 티켓의 모서리가 아니라 **가림선**이고, DL 이 붙인 구조 검산
(«ref 는 티켓 좌단 296 이 불릿 알약 우단 295 와 +1 로 맞물린다»)은 반증이 아니라 **증거**다 —
**가림선은 언제나 가리는 것의 모서리와 맞물린다.** 그 «맞물림» 을 정합의 근거로 쓴 것이 유령의 뿌리였다.
⇒ **덩이 자(= DM 쪽)가 답이고 잔차는 −86 이 아니라 −16.8** 이다.

⚠ 자를 세우다 잡은 것 둘(둘 다 `--edge` 주석에 박았다):
- **띠 우단을 «가운데 행» 으로 재면 네 줄이 261·291·295·267 로 갈린다** — 1·4번 줄은 오른쪽 끝이 티켓 잉크에
  덮인다. 줄의 **20% 이상 행에 띠가 있는 열**로 읽으면 네 줄이 293~295 로 모인다(A3-ⓔ).
- 띠 줄 검출에서 **맨 아래 줄은 위·아래 테가 한 run 으로 붙는다** — 키 25 이상이면 그 자체가 한 줄이다.

### ⓑ 수리 — 제품 `index.html` **1자리**(`.pvc>.art` 두 값)

`--art-l 500 → 517` · `width 462 → 445`. **우변 962 는 그대로**(ref 962.4 · 닫힌 자리)이므로
바뀐 것은 «판이 알약 밑으로 얼마나 파고드는가» 하나다. 세로 두 값(t 142 · h 281)은 1회차 그대로.

| 축(카드-로컬 우리 px) | ref | 수리 전 | 수리 후 | Δ |
|---|---|---|---|---|
| 일러스트 좌단 | 516.8 | 500 | **517** | −16.8 → **+0.2** |
| 폭 | 445.6 | 462 | **445** | +16.4 → **−0.6** |
| 우단 | 962.4 | 962 | 962 | −0.4(불변) |
| 겹침(띠가 덮는 폭) | 67.0 | 86.0 | **69.0** | +19.0 → **+2.0** |
| 높이 / 상변 | 280.5 / 145.4 | 281 / 142 | 281 / 142 | +0.5 / −3.4(불변) |

⇒ **아트 판의 w×h·중심이 이제 네 축 모두 ref 와 ±1** 이다(지시서 [3] «아트 자리 규칙» 충족).
겹침 잔차 +2.0 은 **우리 알약이 ref 보다 2.1 넓은 것**(DL·DM 둘 다 «정상» 으로 닫은 자리)이 넘어온 값이라
판으로 흡수하지 않았다 — 흡수하면 알약의 오차를 판에 떠넘기고 좌단이 ref 에서 2px 멀어진다.

### ⓒ 게이트

- `verify667` **[F5] 이관**(좌단 517 · 폭 445 — 네 값 모두 자 실측) · **[F7] 이관**(겹침 86 → **69**,
  ref 67.0 의 파생값임을 주석에 박았다) · **E 표 `bl.art.w` 462 → 445**(같은 값을 두 곳에 안 적는다).
- **[F5b] 신설 — «유령 방지» 짝**: 겹침 > 40 을 단언한다. 폭·좌단을 «보이는 잉크» 로 다시 읽으면
  판 좌단이 곧바로 ★판 우단으로 끌려가는데(그 자리가 DL 의 −86 이다) 그러면 겹침이 0 이 된다.
  이 항이 없으면 다음 회차가 같은 유령을 또 좇는다.
- **되돌림 시험**: `--art-l/width` 를 500/462 로 되돌리면 **[F5]·[F7]·[D3]×2 = 4건 빨강**(88/88 → 84/88).
- 회귀: `verify667` **88/88**(87 → +1 = F5b) · `verify833` **197/197 무수정** · `verify151` **84/84** ·
  `verify124` **76/76** · `probe833` **끊김 0** · **SMOKE PASS**.
- 측정표 **§16 정오표** 신설(§15-2 의 «미판정» 을 닫고, 확정값 표와 «맞물림을 근거로 쓰지 마라» 를 남겼다).

## 2회차 채점 — 비평 DP · DQ (캡처 `151-r35` + `151-r35-h1600` = 위 ⓑ 수리를 포함한 판)

| | ① 배치 | ② 크기 | ③ 여백 | ④ 정렬 | ⑤ 색 | ⑥ 폰트 | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| DP | 9.6 | 9.0 | 9.7 | 9.8 | 8.8 | 9.0 | **9.0** |
| DQ | 8 | 7 | 8 | 8 | 8 | 9 | **7** |

⇒ **불통과**(통과선 9 · DP 는 «간당간당 통과», DQ 는 7). 회차 **2/10**. 둘 다 k 를 재검산했다(DP 카드 폭 Δ≤2 · DQ 2.0621).
추이 **1회차 7 / 8 → 2회차 9.0 / 7**.

### ⓓ 이 회차 수리(아트 판 폭·좌단)에 대한 채점 — **두 사람 다 «맞다»**
- DP: 닫힌 자리 재검산에서 «[24][26] 아트 판 반증 없음».
- DQ: «아트 판 우단 477 ref ↔ 주장 478.3 — 브리핑대로».
- DP 는 **배너형 아트 판 가로**(아직 아무도 안 닫았던 자리)까지 재서 «ref 387.8~389.9 ↔ 우리 390 · 좌단 546.6↔546.3»
  로 **닫았다**(세로는 ref 아트가 카드 상변에 잘린 마름모라 기준이 없다 ⇒ 측정 한계).

### ⓔ ⚑⚑ 2인 일치 — **상태 탭 «윗모서리 반경»** (이 회차가 바로 고쳤다)
| | 자 | ref | 우리(수리 전) | Δ |
|---|---|---|---|---|
| DP | 탭 상변에서 깊이 d 마다 좌·우단 − 평탄부 = «들어감»(임계 45/65/90) | d8 19.2/16.0 · d16 13.9/11.6 · d22 10.9/8.0 | 9.1 · 4.5 · 2.2 | **ref 의 0.20~0.47배**(r 40~44 ↔ **29**) |
| DQ | 깊이 d 별 탭 실루엣 폭(임계 15/25/45) | d50 196.0~208.3 | 226~228 | **+35px**(r ≈40 ↔ **25**) |

둘 다 **신뢰 상** · 둘 다 임계 3단 스윕에서 부호 불변 · 둘 다 «탭의 **폭·자리 자체는 맞다**» 를 같이 적었다
(좌단 28.7↔28.8 · 폭 251.5↔249.8 · 솟음 55.1↔55.9 · DQ 최대폭 250 ↔ 251.7).
⚑ DP 는 «ref 는 **탭 r > 카드 r** 인데 우리는 뒤집혔다»(카드 본체 모서리는 같은 자로 ref 와 일치 = 자 검증)를 덧붙였다.

**⚑ 그리고 이 한 값이 1회차가 «둘이 갈렸다» 로 남긴 «탭↔쿠폰칩 틈» 을 같이 설명한다.**
DQ 가 깊이별 표로 갈랐다 — 같은 사진을 잰 두 사람이 **다른 높이의 행**을 읽었을 뿐이다:

| 깊이 d | ref | 우리 | Δ |
|---|---|---|---|
| 4~18 | 8.3~12.4 | 12.0 | −0.4 ~ +3.7 **(부호 뒤집힘)** |
| 26 | 10.3~14.4 | 12.0 | −2.4 ~ +1.7 |
| 42 | 22.7~26.8 | 17~19 | **−5.6 ~ −8.8** |
| 50 | 37.1~51.6 | 32~34 | **−5.1 ~ −17.6** |

⇒ 탭 최대폭 구간을 잰 자는 «우리가 넓다», 상단 호 구간을 잰 자는 «좁다» 를 얻는다. **둘 다 참이고
뿌리는 칩이 아니라 이 반경이다** — 칩 자체의 자리는 ±2 안이다(DP 좌단 290.4↔290.9 · DQ 291↔288.8~290.9).
1회차의 DM [3](«탭→쿠폰칩 틈 −27%»)과 DL(«부호 뒤집힘 = 측정 한계»)이 **둘 다 옳았던** 자리다.

**수리**: `.pvc>.stt{border-radius:30px 30px 0 0 → 40px 40px 0 0}`(2인 공통값 — DP 40~44 의 하한 = DQ 점추정).
**반경만** 옮겼다(폭·솟음은 `verify833` [8-d] 가 그대로 지킨다).
실측 대조(같은 자리 크롭 · 배경 대비 60): 탭 좌단이 같은 행에서 **d30 80 → 87 · d40 72 → 77** 로
**5~7px 더 들어왔다**(DP 가 요구한 «+7~10» 방향).
**게이트**: `verify833` **[8-e] 신설**(반경 40 · 아래 두 모서리 0) ⇒ **197/197 → 200/200** ·
`verify667` 88/88 · `verify151` 84/84 · `verify124` 76/76 · `probe833` 끊김 0 · **SMOKE PASS**.

### ⓕ 2인 일치 둘째 — 배지 «2000% / 가치» **가로만** 좁다 (다음 회차 1순위)
- DP: **노랑 잉크 블록**(임계 6단 · 값이 1px 도 안 흔들림) ref 154.7×88.7 ↔ 우리 143×88 ⇒ **폭 −7.6% · 높이 −0.8%**.
- DQ: **분홍 실루엣**(마스크 2종) ref 169.1~171.2 ↔ 우리 160 ⇒ **폭 −5.4~6.5% · 높이 Δ−1.2 ≈ 0**.
⚠ **DP 는 분홍 실루엣 폭을 «측정 한계» 로 분류했다**(임계 50/70/95/120 에서 Δ +1.2/−0.9/−3.0/−11.2 로 부호 뒤집힘).
⇒ **두 사람이 «가로만 좁다» 에는 일치하고 «무엇으로 재느냐» 에서 한 명이 자기 자를 기각했다.**
다음 회차는 **DP 의 노랑 잉크 자**(6단 임계에서 불변)를 쓰고, 손잡이가 배지 판인지 글자인지부터 가를 것.

### ⓖ 1인 지적 — 다음 회차가 볼 것
- DQ [4] 리본 라벨 잉크가 리본 세로 중심에서 **−2.5~−4.0px 위**(라벨 4개 전부 · 임계 무관 · 신뢰 상).
  ⚠ 833 3회차가 **탭 라벨**에서 같은 꼴을 고친 선례가 있다(`[8-a]` top 9 → 16). DP 대조 필요.
- DQ [5] 보석 박스 가로가 **형마다 반대 부호**(배너 −2~4% · 불릿 +2~4%) ↔ **DP 는 같은 자리를 «맞다»**
  (임계 3단 동일값 · 형별 79 ↔ 91 이 ref 39 ↔ 43·44 를 따른다)로 닫았다. **정면으로 갈렸다** — 자부터 가를 것.
- DQ [6] 배너형 rb1↔rb2 틈 −2.6px(불릿형은 +1.1 정상) · [7] 리본 라벨 좌 인셋 +1.7~4.7 · [8] 제목 앞 ◆ +14.5%
  · [9] 불릿 숫자 라벨 −4%(배너형은 정상).
- DP [3] 리본2 상변 −3.0 · 탭 우단 −1.5 · 카드 간 틈 −1.2(전부 카드 폭의 0.3% 이하) · [4] 배너 pill 좌단 검정 테 −25%(⑤).

### ⓗ 쿠폰 칩 «폭» 은 감점 밖 — **문구 수 축이다**(두 사람이 같은 사실을 다르게 처리했다)
DP: «ref 는 알약 2개(🔥+1000 · Ⓜ+100)인데 우리는 «마일리지 쿠폰 +N» 1개 — **글자 수가 정하는 폭**이고
ref 자신도 파랑 301.6 ↔ 초록 354.4 로 다르다» ⇒ 감점 밖. DQ 는 같은 값을 ②·③ 에 실었다(우단 −17~25px).
⇒ **78px 카드 길이와 같은 계열**(브리핑 «문구 수» 규칙)이다. 다음 브리핑에 **칩도 그 규칙 안**이라고 못박을 것.

### ⓘ 9:13.3 — 두 사람 다 «없음»(다섯 회차째)
DP: 세 카드 외곽 좌·우·상·하 **최대 Δ 0.13px** · 카드 본체 평균 |Δ색| 0.000~0.002.
DQ: 크롭 채널 최대차 15/15/12 · 검정 마스크 불일치 화소 17/10/9(0.0025% 이하)이고 **전부 카드 밖**.

### ⚑ 3회차가 할 것 (순서대로)
1. **캡처부터** — `node tools/cap151.js <경로>/151-r37.png --crop` 와 `--crop --h 1600` 둘 다.
   이 회차 수리 둘(아트 판 폭·좌단 · **탭 반경**)에서 **탭 반경은 아직 채점을 안 받았다.**
2. 브리핑 «닫힌 자리» 에 **[27] 아트 판 가로(좌단 517 · 폭 445 · 겹침 69 — 2인 재검 통과)** ·
   **[28] 배너형 아트 판 가로(390 · 좌단 546 — DP 가 닫았다)** · **[29] 탭 윗모서리 반경 40** 을 더하고,
   «문구 수 축» 목록에 **쿠폰 칩 폭**을 카드 78px 과 함께 적어라(ⓗ).
3. **1순위 = 배지 «2000%/가치» 가로 −6~7.6%**(2인 일치 · ⓕ). **DP 의 노랑 잉크 자**로 재고,
   판(분홍 실루엣)을 넓힐지 글자를 넓힐지부터 가를 것 — DP 자신이 분홍 실루엣 자를 기각했다.
4. **2순위 = DQ [4] 리본 라벨 잉크 세로 −2.5~4.0**(833 [8-a] 선례) · **DQ [5] 보석 박스**(DP 와 정면 상충 — 자부터).

---

## 3회차 (2026-09-04, sess-2028-27254 루틴 워커 B)

캡처 `151-r37`(9:19) · `151-r37-h1600`(9:13.3) = **2회차 제품**(아트 판 폭·좌단 · 상태 탭 반경 40).
채점 뒤 이 회차 수리를 얹은 판이 `151-r38` 이다.

### ⓐ ⚠ 먼저 밝힐 것 — **브리핑 파일이 사라져 있었다**

2회차까지 쓰던 `scratch/brief885.md` 는 `.gitignore` 가 `/scratch/` 를 통째로 막는 자리라
**저장소에 없었고**, 컨테이너가 갈리면서 같이 사라졌다(캡처 PNG 와 같은 취급을 받고 있었다).
⇒ 이 회차가 **다시 지었다** — `docs/review/833-*.md` · 이 파일 · 게이트
`tools/verify833.js`(200항) · `tools/verify667.js`(88항) 에서 «닫힌 자리» 를 다시 모았고,
«형마다 두 값» 표 · «문구 수 축» 규칙 · 환산(K = 2.0628) · 통과선 9 를 같이 적었다.
⚑ **이것은 다음 회차도 겪을 자리다** — 브리핑은 회차마다 새로 쓰는 것이 아니라
**이 review 파일에서 재생되는 것**으로 보고, 닫힌 자리를 추가할 때 반드시 여기에도 남겨라.

### ⓑ 1순위(2회차 ⓕ «배지 가로만 좁다») — **세 번째 자가 갈랐다. 손잡이는 판이 아니라 글자다**

2회차 DP·DQ 는 «가로만 좁다» 에 일치했는데 **자가 달랐고**, DP 자신이 자기 분홍 실루엣 자를
기각했다. 게다가 게이트 `verify833` [1-a] 는 ref 분홍 폭을 **160.90/158.84** 로 못박고 있는데
DQ 는 같은 «분홍 폭» 을 **169.1~171.2** 로 읽었다 — **같은 축에 ref 가 둘**이었다.
⇒ `tools/scan885b.py` **신설**: 두 축(분홍 별판 · 노랑 글자)을 **같은 창·같은 문턱 사다리**로 나란히 잰다.

| 문턱 | ① 분홍 별판 ref(위/아래) | 우리 | ② 노랑 글자 ref(위/아래) | 우리 | ② Δ폭 |
|---|---|---|---|---|---|
| 40 / 30 | 208.3 / 181.5 | 160.0 | 156.8 / 156.8 | 144.0 | **−8.1%** |
| 60 / 50 | 208.3 / 181.5 | 160.0 | 156.8 / 156.8 | 144.0 | **−8.1%** |
| 80 / 70 | 208.3 / 181.5 | 160.0 | 156.8 / 156.8 | 144.0 | **−8.1%** |
| 100 / 90 | 202.2 / 173.3 | 160.0 | 156.8 / 154.7 | 143.0 | **−8.2%** |
| 120 / 110 | 185.7 / **144.4** | 160.0 | 154.7 / 154.7 | 143.0 | **−7.6%** |

⚑ **판정이 한 표에서 나왔다** — ① 분홍은 같은 사다리에서 ref 가 **181.5 → 144.4 로 미끄러지고
부호까지 뒤집힌다**(우리 160 이 «작다» → «크다» 로) ⇒ **측정 한계**(DP 의 기각이 옳았고,
게이트 160.90 ↔ DQ 169.1 이 갈린 이유도 이것이다 — 둘이 다른 문턱에 서 있었다).
② 노랑은 **다섯 단 전부 −7.6~−8.2% · 부호 불변**이고 **높이는 90.8↔90.0 · 88.7↔88.0 = Δ−0.9%** 다.
⇒ **손잡이는 판이 아니라 글자이고, 축은 «가로만» 이다.**

⚠ **667 이 이 자리에 남긴 주석을 정정했다.** 그 주석은 «같은 문자열에서 폭만 좁은 것은 서체 모양이고
지시서 [3] 이 감점 사유에서 뺀 자리» 라고 적어 두었는데, 지시서 [3] 은 그 반대를 못박는다 —
«⑤⑥ 지적 중 크기·bbox·비율에 관한 것(… **글자 잉크 높이·폭** …)은 ②·⑥ 비율 항목이므로 반영한다».
667 이 옳았던 것은 **손잡이** 쪽이다: `font-size` 를 키우면 맞아 있던 높이가 같이 커진다(380 함정).
⇒ 높이를 안 건드리는 손잡이 = **가로 배율**(A3 닉네임 1.14 · 전투력 1.243 · 833 [10-a] .9 · [13-a] 선례).

### ⓒ ⚑⚑ **손잡이를 한 번 틀렸다가 자가 잡았다 — «배율» 이 아니라 «글자 사이» 다**

ⓑ 까지의 답(«가로만 −8%»)에서 곧장 **가로 배율**로 갔다. 회전(−10°)이 섞인 AABB 를 풀어
회전 전 잉크를 되살리면 우리 133.6×65.8 ↔ ref 145.7×64.4 ⇒ **1.0906**, 검산도 닫혔다
(1.09 를 먹인 AABB 가 ref 154.7×88.7 **두 축 모두** 일치 — 각도 가정까지 확인됐다).
게이트도 220/220 으로 초록이었다. **그런데 그 답은 틀렸다.**

`docs/LESSONS.md` 92-곁가지3 이 정확히 이 자리를 적어 두고 있다 —
«폭이 안 맞을 때 범인이 항상 글자 크기는 아니다. **자당 증가분과 총폭을 따로** 보면
`scaleX` 인지 `letter-spacing` 인지가 바로 갈린다. 한 손잡이로 두 표본을 맞추려 들면 ±2% 에서 멈춘다.»
⇒ `scan885b.py --glyph` **신설** — 윗줄 «2000%» 를 **열 덩이 다섯**으로 갈라 둘을 따로 냈다(문턱 90):

| | 글리프 폭 합 | 글리프 사이 틈 합 | 총 bbox |
|---|---|---|---|
| ref | **130.0** (22.7·24.8·26.8·26.8·28.9) | **24.8** (6.2 ×4 · 고르다) | 154.7 |
| 우리(수리 전) | **132.0** (+1.5% = **이미 맞다**) | **11.0** (1·3·4·3) | 143.0 |

⇒ **글리프는 처음부터 ref 폭이었고, 모자란 것은 «글자 사이» 뿐이다** —
부족분 24.8 − 11.0 = **13.8** 이 총폭 부족 **11.7** 을 통째로 설명한다.
배율로 맞췄다면 이미 맞아 있던 글리프를 130 → **143.9(+10.7%)** 로 **틀리게** 만들면서 총폭만 맞췄을 것이다.
**두 표본 중 하나를 깨뜨려 다른 하나를 맞추는 수리**였고, 게이트가 초록이었던 것은
그 게이트를 내가 «배율» 축으로 짰기 때문이다(자기가 고른 축은 자기를 못 잡는다).

### ⓓ 수리 — 제품 `index.html` **1자리**(`--bdg-ls` 신설 + 두 줄의 letter-spacing/padding-left)

값: 부족분 11.7 ÷ 틈 4 = 2.925 ⇒ **`--bdg-ls:2.9px`**. 수리 뒤 같은 자로 재면(문턱 50/90/110)

| | 총 bbox | 글리프 폭 합 | 틈 합 |
|---|---|---|---|
| ref | 154.7~156.8 | 123.8~134.1 | 22.7~30.9 |
| 우리(수리 후) | **155.0** | **132~135**(수리 전과 같다) | **20~23**(11.0 에서) |

⇒ **총폭과 «글리프/틈» 두 표본이 같이 맞았다** — 배율 안이 낼 수 없던 결과다.

- ⚑ **구조적으로도 letter-spacing 이 옳다** — 문자열이 «1000%»·«500%» 로 바뀌어도 틈은 글자 수를
  따라가지만, 배율은 짧은 문자열을 그만큼 **덜** 벌린다(카드3 이 실제로 «1000%» 다).
- **한 값을 두 줄이 같이 쓴다**(`--bdg-ls`) — 833 [13-d] «줄마다 손으로 다른 값을 적지 마라».
  아랫줄 «가치» 는 ref 아랫줄과 **문구가 다르다**(ref 는 윗줄과 우측 정렬된 더 긴 말 · 환산 폭 ~110 ↔ 우리 58)
  ⇒ **문구 수 축**이라 따로 맞출 과녁이 없다. 그래서 윗줄이 낸 한 값을 같이 쓴다.
- letter-spacing 은 **마지막 글자 뒤에도** 붙어 가운데 정렬을 그 절반만큼 왼쪽으로 민다
  ⇒ `padding-left` 로 같은 값을 되갚아 잉크 중심을 붙박았다([2-a] 가 그 중심을 본다).
- **판(`>s`)·글리프·font-size·line-height·검정 획은 한 줄도 안 건드렸다.**
- ⑤ 대가 **0** — 배율이었다면 검정 획(8/7px)이 8 → 8.7 로 같이 늘었을 자리다(833 [10-g] 가 치른 값).
- 되돌리려면 `--bdg-ls` 를 0 으로 두면 된다(다른 값은 무변경).

### ⓔ 게이트

`verify833` **§1-2 신설 7항 × 3카드 + §R 되돌림 2항** ⇒ **200/200 → 223/223 PASS**
- `[1-e]` 배지 줄이 둘 · `[1-f]` **두 줄이 한 값** · `[1-g]` letter-spacing 2.9
- `[1-h]` **가로·세로 배율이 둘 다 1** — 글리프 자체 무변경.
  ⚑ 이 항이 이번 회차의 자기 오류를 막는 자리다: 배율이나 fs 로 갈아타면 여기가 먼저 빨개진다
  (둘 다 «이미 ref 폭인 글리프» 를 건드리는 손잡이다). 배율은 computed matrix 에서
  `가로 = hypot(a,b) · 세로 = hypot(c,d)` 로 낸다 — 각도 θ 를 몰라도 나오고 각도를 바꿔도
  이 자는 «배율» 만 잰다(207 «선언만 읽으면 못 잡는다» 함정 회피).
- `[1-i]` padding-left = letter-spacing(가운데 정렬 되갚기) · `[1-j]` fs/line-height 불변 · `[1-k]` 검정 획 8/7 불변
- `[R16]` `--bdg-ls:0` 으로 되돌리면 [1-g] 빨강 · `[R16b]` **그때 advance 가 «글자 수 × 2.9» 만큼 준다**
  (5자 · Δ14.28 · 세 카드 모두) — «그림이 바뀐다» 의 증거이자 **«손잡이가 배율이 아니다»** 의 증거다
  (배율이었다면 줄어드는 양이 문자열 길이와 무관했을 자리다).

회귀: `verify667` **88/88** · `verify151` **84/84** · `verify124` **76/76** — 전부 무수정 PASS.
