# 347 — `tools/func50.js` 「상점 탭 레드닷 없음(코스튬으로는 안 켜진다)」 1건 실패 (83/84)

- **세션** sess-1833-21233 (워커 B, 2026-08-28) · 1회차 · **T1 «버그»**
- **결론: 게이트 부패(등재문 갈래 ⓐ). 제품 `index.html` 0줄 · 레이아웃 Δ0px · 판정식 0줄.**
- 고친 것은 `tools/func50.js` §7 한 자리 · 신설 `tools/probe347.js`.
- 지시서 [3]-**(가)** — 레퍼런스 대조가 아니라 «무엇이 켜는가» 를 실측으로 가르는 작업이라 **비평가 안 띄움**.

---

## 1. 재현이 먼저다 (338 규칙) — `tools/probe347.js` 신설

등재문은 두 갈래를 갈라 달라고 했다.

- **ⓐ 게이트가 낡았다** — 상점 탭이 켜지는 것이 코스튬과 무관한 다른 축 때문이고, 그 축은 지금 옳다.
- **ⓑ 제품이 샜다** — 코스튬 보유가 상점 탭 레드닷을 실제로 움직인다(321 규약 «레드닷 = 지금 누를 수 있다» 위반).

처방을 고르기 전에 `probe347.js` 로 func50 §7 의 그 시점을 그대로 재현했다. **11/11 PASS.**

| 물음 | 실측 |
|---|---|
| 게이트가 본 값 — 상점 탭 `.alert` | **켜짐(true)** |
| 166 축 `sumAnyReady()` (게이트가 끈 축) | false — **게이트의 준비는 제대로 돌고 있었다** |
| 329 축 `coinAdAnyReady()` | **true** ← 켜고 있던 주인 |
| 코스튬 **전부 미보유**(49종) → 닷 | true |
| 코스튬 **전부 보유**로 뒤집음 → 닷 | true (**같은 값 — 한 칸도 안 움직인다**) |
| 다이아 999999 → **0** 으로 떨어뜨림 → 닷 | true (같은 값) |
| 166·329 **두 축 모두** 끔 → 닷 | **false** |
| 광고 한 칸(`a1`)만 되살림 → 닷 | **true** |

⇒ **ⓑ 기각.** 코스튬 축을 양 끝으로 흔들어도 이 닷은 움직이지 않는다. 제품은 옳다.
⇒ **ⓐ 확정.** 켜는 주인은 **329**(재화 탭 «광고 보고 받기»)다.

## 2. 뿌리 — 게이트가 «축이 하나이던 시절» 에 굳었다

`index.html` 29310:

```js
: k === 'shop' ? (sumAnyReady() || coinAdAnyReady())
```

`func50` §7 은 **166 ③ 이 실은 그 시절의 유일한 자기 조건**이던 때 쓰였고, 그래서
«무료 10연을 소진시켜 166 축을 끈 뒤 코스튬 축만 남긴다» 는 준비를 한다.
그 뒤 **329(2026-08-28)** 가 같은 칸에 `coinAdAnyReady()` 를 **OR 로 더했다**(293 «경로 전체»).
준비가 끄는 축은 여전히 하나뿐이라 **코스튬 축만 남기는 데 실패**했고, 남아 있던 329 축이 칸을 켜서
«코스튬으로는 안 켜진다» 가 빨개졌다. 제품이 샌 적은 한 번도 없다.

## 3. 처방 — 축을 하나 더 끄되, **끄는 것으로 끝내지 않았다**

328~330 의 이관 교훈이 여기 그대로 걸린다. 329 축을 조용히 꺼서 초록으로 되돌리는 것만으로 끝내면
**«329 가 통째로 사라져도 초록인 게이트»** 가 된다(빨간 항 하나를 눌러 없앤 것과 같다).
그래서 한 항을 셋으로 나눴다 — **84 → 88 항**.

| # | 항 | 뜻 |
|---|---|---|
| ① | `329 축이 이 칸을 켜고 있다 — coinAdAnyReady()` = true<br>`상점 탭 레드닷 켜짐(광고 잔여가 남아 있는 동안)` = true | **양성항.** 329 가 사라지면 여기가 빨개진다 |
| ② | `광고 잔여 0(329 축 끔)` = false → `상점 탭 레드닷 없음(코스튬으로는 안 켜진다)` = false | 게이트가 **원래 묻던 물음** — 이제야 코스튬 축만 남았다 |
| ③ | `코스튬 49종 전부 보유로 뒤집어도 안 켜짐` = false | **되돌림 시험.** ②만 있으면 «두 축을 껐으니 꺼진 것» 과 «코스튬이 안 켠다» 를 구별 못 한다 |

## 4. 무르게 푼 수리가 아님 — 양방향 되돌림 시험 (실행으로 확인)

| 주입한 변조 | 결과 |
|---|---|
| 제품에서 `|| coinAdAnyReady()` **삭제**(329 를 없앤 셈) | **FUNC50 FAIL 87/88** — ① 이 빨개진다 |
| 제품에 `|| AVATARS.some(a => S.avatars[a.id])` **주입**(코스튬이 샌 셈) | **FUNC50 FAIL 86/88** — ②③ 이 빨개진다 |
| 원복 | **FUNC50 PASS 88/88** |

두 방향 다 잡힌다 = 헛초록이 아니다. 변조는 전부 원복했고 `git diff index.html` 은 **빈 값**이다.

## 5. 게이트 결과

| | |
|---|---|
| `node tools/func50.js` | **88/88 PASS** (전 83/84 FAIL) |
| `node tools/probe347.js` | **11/11 PASS** (신설) |
| `node tools/verify329.js` | 25/25 PASS |
| `node tools/verify166.js` | 73/73 PASS |
| `node tools/verify299.js` | 27/27 PASS |
| `node tools/smoke.js` | **SMOKE PASS** |

회귀 3종은 **한 줄도 안 고쳤다** — 제품 0줄의 가장 짧은 증거다.

## 6. 환경 메모 (다음 워커에게)

클라우드 컨테이너의 번들 크로미움은 **1194**(playwright **1.56** 짝)인데
`npm i --no-save playwright` 는 최신(1.62 · 크로미움 1234)을 끌어와 `npx playwright install chromium` 이
다운로드 실패로 죽는다. **`npm i --no-save playwright@1.56.0`** 이면 미리 깔린 브라우저를 그대로 쓴다
(`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`). 새로 받을 것이 없다.
