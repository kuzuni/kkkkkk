# 937 — 파이썬 자가 «없는 자» 로 사라지지 않는다 (913 의 나머지 반쪽)

**완료(2026-09-05, sess-1058-12563 루틴 워커 D · 1회차) · 제품 `index.html` 0줄**
`verify937` **26/26 신설** · `tools/pydep937.py`·`tools/pydep937.js` 신설 ·
파이썬 자 **100개** · 노드 자 **10개** 이관 · 지시서 [6] «준비» 절 개정 · **SMOKE PASS**

지시서 [3]-**(가)** — 기계적 작업(의존 부트스트랩 이관)이라 **비평가 없음.**
판정은 grep 수치 · 각 자의 점수 줄 · 되돌림 시험(감춘 모듈)으로만 한다.

---

## §1 재현 — 등재문이 적은 그대로였다

착수 트리에서 그대로 재현됐다(수리 전):

```
$ python3 -c "import numpy"   → ModuleNotFoundError: No module named 'numpy'
$ python3 -c "import PIL"     → ModuleNotFoundError: No module named 'PIL'

$ node tools/verify895.js
  … execFileSync (…/verify895.js:71)
  status: 1,
  stderr: 'Traceback (most recent call last):\n
           File "/home/user/kkkkkk/tools/scan895.py", line 33, in <module>\n
           import numpy as np\nModuleNotFoundError: No module named 'numpy'\n'
  ⇒ 종료 코드 **1** · 점수 줄 **0** · 스택 트레이스
```

`pip3 install pillow numpy` 뒤 **같은 자리에서 `VERIFY895` 18/18 PASS**.
⇒ **코드 결함이 아니라 환경 준비**다(등재문의 판단이 그대로 맞다).

**해악은 «못 돈다» 가 아니라 «못 도는 줄 아무도 모른다» 다** — 종료 코드 1 은 claim.js 규약의
«오류» 이고 점수 줄이 한 줄도 안 나오므로 회귀 스윕은 그 자를 «빨강» 이 아니라 **«없는 자»** 로
지나간다. 913 이 pngjs 에서 찍은 얼굴과 **글자 그대로 같다.**

## §2 뿌리 — 지시서 [6] «준비» 줄이 반쪽이었다

`pip3 install pillow numpy` 는 LESSONS 에 **다섯 곳**(2509·6053·7275~7276·23494·23853) 적혀 있는데
**지시서의 «준비» 줄만 npm 을 홀로 적고 있었다.** 913 이 고친 것은 그 줄의 npm 쪽 절반이다.
워커는 지시서를 읽고 LESSONS 는 «한 번 읽는다»([0].1) 뿐이라, 이 줄이 반쪽인 동안
**교훈이 계속 다시 배워졌다**(7275 가 «`tools/*.py` 51개가 PIL 을 쓴다» 를 이미 적어 두었다).

## §3 처방 — 913 과 같은 꼴 셋 (등재문 ①② + 실측이 요구한 ③)

### ① 지시서 [6] «준비» 절 (등재문 ①)
«준비는 두 줄이다 — 노드와 파이썬» 으로 고치고 네 문장(«playwright 없음» ·«pngjs 없음» ·
**«numpy 없음»** ·**«pillow 없음»**)을 한자리에 모았다.
⚠ **913 의 «한 번에» 경고는 옮기지 않았다** — `pip3 install` 에는 그 함정이 없다(등재문의 ⚠ 그대로).
`verify913` [C1]~[C3] 은 **무수정 16/16** — npm 줄을 지우지 않았다.

### ② 공용 부트스트랩 `tools/pydep937.py` (등재문 ②)
`png913.js` 의 파이썬 짝. 한 줄 치환만 한다:

| 옛 줄 | 새 줄 | 자 |
|---|---|---|
| `import numpy as np` | `from pydep937 import np` | 34 |
| `from PIL import Image` | `from pydep937 import Image` | 92 |
| `from PIL import Image, ImageDraw` | `from pydep937 import Image, ImageDraw` | 1 |
| `try: from PIL … except ImportError: … sys.exit(1)` | 같은 한 줄 | 6 |
| (루트) `pxdiff41.py` — `probe534` 가 부른다 | `sys.path` 한 줄 + 같은 한 줄 | 1 |

없으면 **«numpy 없음 — pip3 install pillow numpy» 한 줄 + 종료 코드 2**(110 `pw()`·913 과 같은 말투).
**PEP 562 지연 해석**이라 numpy 만 쓰는 자는 pillow 가 없어도 안 죽는다(그 반대도 같다) —
`scan813e` 가 `scan887` 을 **모듈로** 읽는 자리가 이 성질을 요구한다.

⚑ **옛 가드 6개는 «있었지만 코드 1 이었다».** `probe866`·`scan813c/d/e`·`scan887`·`scan892` 는
이미 «Pillow 없음 — …» 한 줄을 찍고 있었는데 **`sys.exit(1)`** 이었다 — 그 코드로는 «환경에 없음» 과
«오류» 가 구분이 안 돼 스윕이 여전히 못 가른다. 여섯을 전부 같은 부품으로 모았다(`verify937` [B5]).

### ③ 노드 쪽 짝 `tools/pydep937.js` — **이것이 이 작업의 핵심이다**
**파이썬만 고치면 등재된 해악이 그대로 남는다.** 파이썬 자가 곱게 코드 2 로 죽어도,
그 자를 `execFileSync('python3', …)` 로 부른 **노드 게이트**는 잡히지 않은 예외로 죽어
**스택 트레이스 + 코드 1 + 점수 줄 0** 이다 — 스윕이 보는 얼굴은 **수리 전과 똑같다.**
등재문이 지목한 것이 `verify895`(노드 자)라는 점이 그 증거다.

⇒ `py(args, opts)` 는 **한 가지만** 한다: 자식이 코드 **2** 면 그 한 줄을 옮기고 **나도 코드 2** 로 끝난다.
**코드 2 가 아닌 실패는 손대지 않고 그대로 던진다** — 진짜 오류를 «환경 탓» 으로 삼키면 913 의 반대 사고다(`verify937` [R7]).

이관한 노드 자 **10개**: `verify895` · `verify12` · `verify84` · `verify887` · `verify905` ·
`verify901` · `verify747` · `probe866` · `probe534` · `m126r18`.

⚠ **곁다리 셋을 같이 잡았다** — 셋 다 «사슬 밖에 남으면 그 자리만 옛 얼굴» 인 자리다:
- `pxdiff41.py` 는 루트에 있지만 `probe534` 가 부른다 ⇒ 사슬 안(`sys.path` 한 줄).
- `m126r18.js` 의 **인라인 `PY` 문자열**이 `from PIL import Image` 를 적고 있었다.
- `verify747` 은 913 이 «폴백을 가진 자» 로 면제했는데 **그 폴백 자체가 PIL** 이다 —
  pngjs 도 PIL 도 없으면 옛 얼굴 그대로 죽는다. 인라인 `-c` 를 부트스트랩 경유로 돌렸다.

## §4 확인 — 수치

| 자 | 결과 |
|---|---|
| `node tools/verify937.js` | **26/26 PASS** (신설) |
| `node tools/verify895.js` | **18/18 PASS** — 등재가 지목한 자, 제품 0줄 |
| `node tools/verify913.js` | **16/16 PASS** — 무수정(npm 줄 살아 있음) |
| `node tools/verify12.js` | **15/15 PASS** |
| `node tools/verify84.js` | **31/31 PASS** |
| `node tools/verify887.js` | **7/7 PASS** |
| `node tools/verify905.js` | **8/8 PASS** |
| `node tools/verify901.js` | **31/31 PASS** |
| `node tools/verify747.js` | **11/11 PASS** |
| `node tools/probe866.js` | 종료 0 (표 정상) |
| `python3 -m compileall tools/*.py` | 0 (문법 100개 무사) |
| `node --check tools/*.js` | 0건 |
| `node tools/smoke.js` | **SMOKE PASS** |

**되돌림 시험(`verify937` §R)** — 시스템 설치본을 안 건드리고 «감춘 세상» 을 만든다
(import 되는 순간 `ImportError` 를 내는 가짜 `numpy`/`PIL` 을 `PYTHONPATH` 앞에 세운다):

| 항 | 잰 것 | 실측 |
|---|---|---|
| [R1] | 파이썬 자 종료 코드 | **2** (옛 날 import 는 1) |
| [R2] | 스택 트레이스가 아니라 한 줄 | `numpy 없음 — pip3 install pillow numpy` |
| [R3] | **그 자를 부른 노드**의 종료 코드 | **2** (수리 전 1) |
| [R4] | 노드 쪽 출력 | 같은 한 줄 · `MODULE_NOT_FOUND` 없음 |
| [R5] | 같은 줄이 두 번 안 나온다 | 1회 |
| [R7] | 코드 1(진짜 오류)은 그대로 던진다 | 1 |

⚠ **[R5] 는 실제로 한 번 빨갰다** — `execFileSync` 는 `stdio` 를 안 적으면 **자식 stderr 를
부모 stderr 로 그대로 흘린다.** 거기서 다시 찍으면 같은 안내문이 두 번 나온다.
호출부가 `stdio` 를 적었을 때만 옮기도록 고쳤다(`pydep937.js` 의 `passedThrough`).

## §5 안 한 것 · 다음 사람에게

- **`scipy`·`soundfile` 은 이 작업의 몫이 아니다** — `scan885e.py`(`from scipy import ndimage`)와
  `synth99.py`(`import soundfile`)는 **같은 얼굴**(하드 import → 코드 1 + 스택 트레이스)인데
  등재문이 못 박은 준비 줄(`pip3 install pillow numpy`)에 그 둘이 없다. 부품(`need(<모듈>, <이름>, <할 일>)`)은
  이미 아무 모듈이나 받게 돼 있으므로 **한 줄씩이면 끝난다** — **938** 로 등재했다.
- **루트의 옛 파이썬 자 60개**(`s*.py`·`crit*33.py`·`scan20/33/41.py` 등)는 그대로 뒀다.
  «자에게 불리는» 것은 `pxdiff41.py` 하나뿐이라(전수 grep) 스윕이 보는 얼굴에 영향이 없고,
  이관하면 그 자들에 `sys.path` 한 줄씩을 심어야 해서 **얻는 것 없이 60줄이 는다.**
- 이 작업은 **어느 자의 점수·판정도 안 건드렸다**(913 의 «878 의 판정은 913 의 몫이 아니다» 그대로).
  `verify895` 18/18 은 **의존을 심으니 그 자리에서** 나온 값이고 자의 로직은 한 줄도 안 바뀌었다.
