# 988 — `verify856` [B14]·[B15] 가 빨간 채 방치 (게이트 부패 · 927 이관 뒤)

> 워커 A(루틴) · sess-1013-5304 · 2026-09-06 · **1회차 · 제품 0줄**(index.html 한 글자도 안 건드렸다)

## 1. 등재문과 착수 재현

등재(2026-09-06, sess-0858-4185)가 적어 둔 얼굴 그대로 재현됐다 — `node tools/verify856.js` **19/21 FAIL**:

```
FAIL [B14] **폭 축만** — … 1칸 · 최악 lance@0~15° 1.078배(3구간 · 0.354~0.382) ≤ 1.25
FAIL [B15] [B14] 가 견주는 칸은 **기울기가 하나**다 — 1칸 · 섞인 칸 0
```

**두 줄이 찍는 수는 전부 초록이다** — 밴드 1.078 은 래칫 1.25 아래이고 섞인 칸은 0 이다.
빨강을 내는 것은 두 항이 같이 달고 있던 **`sp14a.length >= 2`(견주는 칸이 둘 이상)** 한 조건뿐이라,
빨간 줄이 **무엇이 잘못됐는지 자기 입으로 말하지 못하는** 상태였다.

## 2. 갈래를 가른 자 — 문턱 사다리 (`[진단·988]` 신설)

등재문의 갈래는 둘이었다.
ⓐ 927 이 묶는 축을 «종» → «종 × 마루 기울기 칸» 으로 넓히면서 표본이 세 칸으로 흩어져
   **칸당 표본 20** 을 채우는 칸이 하나로 줄었다(= 문턱이 세다) ·
ⓑ 이 자료로는 원리적으로 두 칸을 못 채운다(= 자가 틀렸다).

**문턱을 내리며 칸이 느는지**를 보면 갈린다 ⇒ 판정과 같은 함수(`b14bands`·`b14floorAt`)를
문턱마다 부르는 사다리를 자 안에 세웠다(출력 전용 · 매 실행 찍힌다):

| 칸당 표본 문턱 | 견주는 칸 | [B14] 최악 밴드 | 잡음 바닥 중앙 · 최악 |
|---|---|---|---|
| 30 | 0 | — | 1.012 · 1.128 |
| 25 | 1 | 1.078 | 1.006 · 1.128 |
| **20**(927) | 1 | 1.078 | 1.006 · 1.128 |
| 16 | 3 | 1.165 | 1.010 · 1.128 |
| **12**(988) | **6** | **1.165** | 1.010 · 1.128 |
| 10 | 6 | 1.165 | 1.010 · 1.128 |
| 8 | 8 | 1.205 | 1.012 · 1.128 |

**칸이 실제로 는다 ⇒ ⓐ 다.** 자료는 두 칸을 채울 수 있고, 못 채우게 한 것은 문턱이었다.

⚑ **덤으로 927 의 근거가 지금 자료에서 재현되지 않는 것**도 이 표가 찍었다. 927 은
«문턱 25·20 → 바닥 최악 1.062 / 12 → 1.114 / 10 → 1.121» 을 근거로 «20 아래로 내리면
자가 흔들린다» 고 못을 박았는데, 지금 바닥은 **문턱 30~8 에서 최악 1.128 · 중앙 1.006~1.012 로
평평하다**. 981 이 실루엣 넷을 다시 그리며 바닥 자체가 1.062 → 1.128 로 올라앉았고,
그 뒤로는 **문턱을 따라 움직이지 않는다** — 곧 문턱을 20 에 붙들어 둘 이유가 «바닥» 에는 없다.

## 3. 고친 것 (자만 · 제품 0줄)

### ① 칸당 표본 문턱 `B14_N` 20 → **12**

- **16 이 아니라 12 인 이유** — 16 은 3칸이라 실루엣 한 종만 바뀌어도 다시 «칸 ≥ 2» 밑으로
  떨어진다. 그 취약함이 이번 결손 자체다(981 2·3회차가 넷을 고쳤을 뿐인데 축이 비었다).
  12 는 **4종 × 3칸 = 6칸**이라 여유가 있다.
- **8 로는 안 내렸다** — 최악이 1.205 로 래칫 1.25 코앞이고, 칸당 표본 8 은 위 바닥 표의 밖이다.
- ⚠ **무르게 한 변경이 아니다.** 칸이 늘면 [B14] 는 그 칸들을 **전부** 견주므로 최악값이
  **1.078 → 1.165 로 오른다**(래칫 `BAND14` 1.25 는 한 글자도 안 건드렸다).
  초록이 되는 칸이 아니라 **재는 칸이** 늘어난 것이다. 무르게 푸는 길은 래칫을 올리는 쪽이다.

### ② «몇 칸을 견줬는가» 를 [B16] 으로 떼어 냈다 (333 처방 — 자리를 비우지 않는다)

- **[B14]** = 밴드만(`칸 ≥ 1` 은 «표본 0 이면 자동 초록» 금지 — LESSONS 185-②) ·
  **[B15]** = 섞인 칸 0 만 · **[B16] 신설** = **커버리지 «칸 ≥ 2»**.
- 조건이 사라진 것이 아니다 — 같은 요구가 **자기 이름으로** 빨개진다. 이 파일 [B14] 머리말이
  이미 «한 축 = 한 항» 이라고 적고 있었고, 이번 결손은 그 규칙이 한 항에서 깨져 있던 자리다.

### ③ 되돌림 시험 둘

- **[R8] 신설** — 채울 수 없는 문턱(표본 1e6)으로 판정과 **같은 함수**를 불러 칸 0 을 확인한다
  (= [B16] 이 하드코딩 초록이 아니라 정말 칸을 센다).
  ⚠ «문턱 20 을 되돌리면 1칸» 을 단언하지 **않았다** — 그 수는 실루엣이 바뀔 때마다 움직여
  제품과 무관하게 빨개진다. 그 취약함이 이번 결손이므로 되돌림도 **구조적으로** 비는 자리를 쓴다.
- **[R6] 이관** — [B14] 의 조건이 갈라졌으므로 되돌림도 그 조건 그대로(`칸 ≥ 1 · 밴드 ≤ 래칫`).
  코어를 끈 사본에서 여전히 빨갛다(5칸 · 최악 밴드 2.40).

## 4. 결과

```
VERIFY856 23/23 PASS      (착수 19/21 · 신설 [B16]·[R8] 둘 = 21 → 23)
  [B14] 6칸 · 최악 gale@15~30° 1.165배(2구간 · 0.329~0.383) ≤ 1.25   (착수 1칸 · 1.078)
  [B15] 6칸 · 섞인 칸 0
  [B16] 6칸(gale@15~30° · shuri@30~45° · lance@0~15° · gale@0~15° · rico@15~30° · meteor@30~45°) ≥ 2
  [R6] 코어를 끄면 5칸 · 최악 2.40 → 빨강   [R7] 종만 묶음이면 섞인 칸 4 → 빨강   [R8] 문턱 1e6 → 칸 0
```

- **두 번 연속 23/23**(같은 트리 · 플레이키 아님).
- 회귀 무수정 초록: `verify981` 20/20 · `verify982` 14/14 · `verify983` 34/34 ·
  `probe889d` 7/7 · `probe981` 3/3 · **SMOKE PASS**.
- 제품(`index.html`) **0줄** — 게이트 부패가 맞았다.

## 5. 다음 사람에게 남기는 것

1. **잡음 바닥이 1.062 → 1.128 로 올라앉았다.** [B14] 의 지금 최악 1.165 는 그 위에 있지만
   여유가 0.037 뿐이다. 실루엣을 또 고치는 회차가 이 두 수를 **같이** 보고 가라 —
   바닥이 판정값을 넘으면 그 밴드는 «잰 것» 이 아니다(그때는 문턱이 아니라 **자**를 고쳐야 한다).
2. **칸 구성은 실루엣이 바뀔 때마다 달라진다.** 사다리(`[진단·988]`)가 매 실행 찍히니
   [B16] 이 다시 빨개지면 **먼저 그 표를 보라** — 칸이 문턱 아래에서 느는지 아닌지가
   ⓐ/ⓑ 를 그 자리에서 가른다(이번에 그 표가 없어서 등재가 «갈래 둘» 로 남았다).
