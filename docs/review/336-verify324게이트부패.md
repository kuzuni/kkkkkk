# 336 — `tools/verify324.js` 3건 실패(26/29) «게이트 부패» (2026-08-28, sess-1548-5543 워커 C)

**결론: 제품(`index.html`) 0줄.** 부패 시점은 **326**(훈련 단계 요구치 증가식)이고, 부패한 것은
324 가 감시하는 경로가 아니라 **게이트 하네스가 «클릭할 자리» 를 만드는 방식**이다.
등재 행의 «단서 ③» 이 정확했다 — 다만 방향은 «상한에 막혀 못 오른다» 가 아니라 **«상한이 커진 만큼
레벨이 올라가 한 칸 값이 지갑을 넘었다»** 였다.

## 1. 증상 (착수 시 재현)

```
FAIL  [6] 진짜 클릭으로 전투력이 올랐다 (기능 완성 규칙 — 실동작) — Δ=0
FAIL  [6] 강화 탭 클릭 → 1장 · 값 = 실측차 — 0장 «undefined» 기대 «⚔️ 전투력 +0»
FAIL  [7] 자동 구매로 전투력이 실제로 올랐다 (판정 재료) — 8737687447 → 8737687447
VERIFY324 26/29
```

나머지 26항(침묵 4종 · 경로 커버리지 8종 · [9] 되돌림 시험)은 전부 초록 — **감시자 자체는 살아 있다.**

## 2. 원인 — 산술 하나로 확정된다 (`node tools/probe336.js`)

세 실패는 **한 뿌리**다. [5] 의 마지막 재료 준비가

```js
['훈련 단계 trainUp', "trainUp();", "TRAIN_STATS.forEach(id => S.lv[id] = trainCap()); markDirty();"]
```

로 훈련 3종을 **그 단계의 상한 자리**에 심는데, 326 이 상한식을 `단계 × 100` → `trainCapAt(n)=100·n(n+1)/2`
로 바꾸면서 그 자리가 **900 → 4500** 으로 밀렸다. 비용은 무릎 위에서 `×1.05^l` 지수라 레벨이 5배가 되면
값이 **10^76 배** 가 된다:

| 항목 | 326 이전(lv 900) | 지금(lv 4500) | 하네스 지갑 |
|---|---|---|---|
| `U.atk.cost(l)` 한 칸 | **3.459e21** | **6.613e97** | `S.gold = 1e30` (상수) |
| x10 합계 `buyInfo().cost` | ~4.4e22 | **8.318e98** | 〃 |
| `buyInfo().ok` | **true** | **false** | 〃 |
| `autoBuyTick` 후보(상한 아래 **＋ 지갑 안**) | 3종 | **0종** | 〃 |

- **[6]** — `#bUp` 클릭 핸들러의 `if(!bi.ok) return;` 에서 **조용히 되돌아간다**. 그래서 클릭은 성공하고
  (셀렉터 항은 초록) `Δ=0`, 토스트 0장. 「Δ=0 은 침묵」이 324 의 규칙이므로 **토스트 쪽은 정상 동작**이었다.
- **[7]** — `autoBuyTick` 은 `c <= S.gold` 인 훈련 스탯만 산다. 후보 0종 → 8회를 밀어도 `cp` 불변.

`probe336` 4/4 가 이 표를 그대로 찍는다(ⓐ 레벨 4500 · ⓑ x10 이 지갑 초과 · ⓒ 후보 전무 · ⓓ 옛 상한
900 에서는 지갑 안 → **부패 시점이 326**).

> 등재 행의 단서 ②(«cp 8.7e9 = 32비트 절단 대역»)는 **원인이 아니었다** — `lv()` 의 `|0` 이 무는 것은
> `S.lv[id]`(4500)이지 `cp()` 값이 아니다. 실제 강화가 동작한다는 단서 ①(43회차 `p58ay`)은 맞았고,
> 그 «제품은 멀쩡하다» 가 이번 진단의 출발점이 됐다.

## 3. 처방 — 상수를 지우고 **제품에게 묻는다**

지갑을 더 큰 상수(`1e120` 따위)로 바꾸는 것은 **같은 부패를 한 번 더 심는 것**이다. 다음에 상한식이나
비용 곡선이 움직이면 또 조용히 빨개진다. 그래서 두 자리 모두 값을 제품에서 역산한다:

```js
/* [6] */ S.gold = costOf(U.atk, lv('atk'), 10) * 1e3;
/* [7] */ S.gold = Math.max(...TRAIN_STATS.map(id => costOf(U[id], lv(id), 12))) * 1e3;
```

그리고 **«클릭했는데 안 올랐다» 와 «애초에 살 수 없는 자리였다» 를 갈라 적는 전제 2항**을 넣었다(326 교훈
«칸을 쓸 때 갈라 써라»의 적용):

- `[6] 클릭 전 — 하네스가 «살 수 있는 자리» 를 만들었다` (`buyInfo(U.atk).ok`)
- `[7] 미는 것 전에 — 자동 구매 후보가 하나 이상` (상한 아래 ＋ 지갑 안)

이 두 항이 빨가면 다음 워커는 **제품을 열 필요가 없다** — 재료 준비만 보면 된다. 이번 실패가
«Δ=0» 이라는 **제품 결함처럼 생긴 문장**으로 3주간 남아 있던 것이 이 갈라 적기의 이유다.

## 4. 무르게 푼 수리가 아님 — 되돌림 시험 `[R]` 신설

전제를 넣어 초록으로 만든 뒤 본 단언이 헐거워졌는지를 **직접 잰다**:

| 항 | 무엇을 무력화 | 기대 | 실측 |
|---|---|---|---|
| R1 | 지갑을 336 이전 상수 `1e30` 로 되돌림 | 전제 2항이 거짓 | `buyInfo.ok=false` · 후보 **0종** |
| R2 | 제품의 `applyBuy` 를 no-op 으로 | 진짜 클릭이 Δ=0 · 0장 | `Δ=0 · 0장` |
| R3 | 원복 | 다시 오르고 1장 | `Δ=19741532 · 1장` |

R1 은 «새 전제가 이번 부패를 실제로 잡는다», R2 는 «본 단언은 전제가 아니라 제품을 본다» 를 못 박는다.

## 5. 게이트 결과

| 도구 | 결과 |
|---|---|
| `node tools/verify324.js` | **34/34 ✓** (26/29 → 전제 2항 · 되돌림 3항 신설) |
| `node tools/probe336.js` | **4/4 ✓** 신설 — 원인 산술을 재현 |
| `node tools/smoke.js` | **SMOKE PASS** |
| 전수 스윕 — `trainCap()` 을 만지는 게이트 7종 | `verify112` 38/38 · `verify167` 30/30 · `verify168` 56/56 · `verify183` 27/27 · `verify256` 62/62 · `verify326` 82/82 · `verify131`(168 로 대체됨) — **같은 부패는 이것 하나뿐** |

**`index.html` 0줄 · 제품 회귀 없음.**
