# 72 — 03 던전 카드 우측 썸네일(몬스터 일러스트) 자리

> 작업 단위 72 (T2 기능 TODO — 저장소 주인 지시). 워커 A · sess-0703-19500 · 2026-08-25
> 레퍼런스 `docs/ref/03-던전-팝업.jpg` (1080×2340, 상단 84px 상태바) · 캡처 1080×2280
> 검증 방식: 지시서 [3]-(나) 레이아웃 루프 (비평가 2명 · ①~④ 최저 8 통과)

## 0. 무엇이 없었나

03 던전 카드(`.dnc`)는 **좌측 텍스트 클러스터(던전명·재화 알약·레벨/횟수 알약)만** 있고
레퍼런스의 **우측 몬스터 일러스트가 통째로 빠져 있었다.** 카드 우측은 선버스트(방사) 배경만 깔려 있다.

## 1. 레퍼런스 실측 (신규 — `tools/scan72.py`)

측정표 `docs/measure/03-던전팝업.md` 에는 «우측 일러스트 바탕색» 만 있고 **일러스트 bbox 는 없었다.**
그래서 이번에 스캐너를 새로 만들어 잰다. 방식: 3px 간격 국소 경사(gradient) 밀도 —
선버스트는 큰 부채꼴이라 경사가 완만(열당 4px 내외)하고, 몬스터 아트는 외곽선·하이라이트로 급하다.

| 카드 | 경사 밀도가 배경 수준(4/열)에서 뛰는 x | x 질량 2% / 50% / 98% | y 질량 2% / 98% (카드 top 기준 상대) |
|---|---|---|---|
| 1 골드 던전 | **713** | 713 / 855 / 1017 | **9 / 336** |
| 2 보석 던전 | **723** | 670 / 860 / 1019 | **9 / 338** |

- 카드 안쪽 박스는 x **57~1022**, 세로는 카드 top+8 ~ top+341 (테두리 좌우 7 · 상하 8).
- → 일러스트는 **카드 안쪽 우측 x 708~1022(w 315)** 를 차지하고 **세로는 안쪽 전체(h 334)** 를 채우며
  **카드 하단에서 잘려 들어간다.** 좌측 경계는 하드 엣지가 아니라 배경으로 **페이드**된다.
- 잠금 카드(3·4·5)는 딤이 걸려 경사 신호가 죽어 별도 측정 불가 — 레이아웃은 5장 동일(측정표 §3-7-4).

## 2. 구현 (1회차)

| 요소 | 값 |
|---|---|
| 슬롯 `.dnc>.th` | `right:7px; top:8px; 315×334; overflow:hidden` → 프레임 x **708~1023**, 카드 안쪽 세로 전체 |
| 좌측 페이드 | `mask-image:linear-gradient(90deg,transparent 0,#000 64px)` (레퍼런스의 페이드 경계 재현) |
| 대체 아트 | 던전별 큰 이모지 — 🐉 황금 동굴 / 🐍 수정 광산 / 🗿 고대 유적 / 👺 수련의 탑 / 👹 마왕의 성 |
| 이모지 박스 | `font-size:310px; line-height:340px; top:8px; width:340px` → 잉크 세로 344 = **슬롯 334 보다 커서 하단 잘림** |
| 폭 정규화 | 이모지 잉크 폭은 글리프마다 **0.81~1.13em**(fs300 실측 242~338px) 로 제각각이라 카드별 `--tsx` scaleX 로 맞춤 (🐉1.03 · 🐍0.88 · 🗿1.21 · 👺0.89 · 👹0.87) |
| z 순서 | `.bg` → **`.th`** → `.sh` → `.fr` → 텍스트 → `.lk`(잠금 딤·자물쇠) — 잠금 카드는 썸네일도 같이 어두워지고 자물쇠가 위에 온다 |

실측(캡처 `--geo`): 카드 5장 전부 `th` = 프레임 x **708~1023 · w315 h334**, 카드 top+8 (ref 환산 y249/609/969/1329/1689) — **설계값과 0px 일치**.
프로브 캡처(`--probe`, 배경 평탄화 + 마스크·필터 해제)로 잰 **이모지 잉크**: 폭 280~291 · 세로는 슬롯 전체(334, 상·하 잘림).

도구: `tools/scan72.py`(레퍼런스 스캐너) · `tools/cap72.js`(캡처 + `--geo` 기하 덤프 + `--probe` 잉크 측정용 평탄화 + `--unlock`)

### 회차 기록

- **1회차** — 슬롯 신설 + 이모지 5종 + scaleX 정규화. 콘솔 에러 0, `SMOKE PASS`. 캡처 `docs/review/72-r1.png`.

## 3. 1회차 비평 (O · P — 독립 2인)

두 비평가 모두 **①~④ 최저 5**. 신뢰도 체크(카드 x50~1029 w980 · pitch 360 · gap 10 · 첫 카드 top 241 ·
테두리 7/8 · 던전명 잉크 51~52)는 **6/6 전원 정답** — 수치를 믿고 반영할 수 있는 비평이었다.

| 지적 | O | P | 우리 1회차 | 목표(교차 확인) |
|---|---|---|---|---|
| 아트 좌경계 (카드1) | 715 | 712 | **743** | **712** (+28~31px 어긋남) |
| 아트 좌경계 (카드2) | 728 | 725 | **744** | **726** |
| 아트 상단 인셋 (카드1, 카드 top 기준) | +38 | +34 | **+16~20** | **+36** |
| 아트 상단 인셋 (카드2) | +54 | +50 | **+18~21** | **+52** |
| 아트 종횡비 w/h | 1.01 | 0.99 | **0.87** | **≈1.0** |
| 우측 100px 밴드 아트 커버리지 | REF 66~71% | — | **29~56%** | REF 수준 |
| 잠금 카드 아트 좌단 | — | 385 / 465 / 395 | **738 / 765 / 768** | **385 / 465 / 395** |
| 잠금 딤·자물쇠 | 합격 (딤 0.348 vs 0.353) | 합격 | — | — |

**핵심 오독 정정** — 1회차 측정은 «경사 밀도» 로 아트 상단을 rel **9** 로 읽었는데, 이건 **선버스트 광선의
경계까지 아트로 세어 버린 것**이었다. 비평가 2명이 서로 다른 방법(팔레트 L1 거리 · 검정 외곽선 검출)으로
독립적으로 **+34~38 / +50~54** 를 냈다 — 이쪽이 맞다. 배경이 «무늬» 인 화면에서 경사 밀도로 아트를 분리하면
무늬가 통째로 아트로 잡힌다(다음 화면에서도 재발할 함정).

또 1회차의 **64px 페이드 마스크는 레퍼런스에 없다** — 좌경계는 검정 외곽선 1~2px 하드 엣지다(P).

## 4. 2회차 수정 (5건)

1. **카드별 슬롯 기하** — `--thw`(폭) · `--tht`(상단 인셋) 도입. 카드1 **311×305 @ top+36**,
   카드2 **296×289 @ top+52**, 잠금 카드 3~5 는 개별 측정 불가라 중간값 **305×299 @ top+42**.
   슬롯은 카드 안쪽 **우단(inset 7)·하단(inset 8) 에 붙여** 두 방향으로 잘리게 했다.
2. **이모지 배치 산식 교체** — 잉크 폭·높이 비가 글리프마다 다르므로(fs300 실측 w 242~338 · h 324~338)
   카드마다 `font-size`(세로) 와 `scaleX`(가로) 를 따로 풀고, 400px 정사각 상자의 중심을
   `--thcx/--thcy` 로 옮겨 **우측 6px · 하단 16~18px 오버플로**를 만든다(= 두 방향 잘림).
   결과: 🐉 fs291/1.147 · 🐍 fs282/0.962 · 🗿 fs281/1.25 · 👺 fs284/1.005 · 👹 fs280/0.986.
3. **페이드 마스크 64px → 12px** (P 지적. 레퍼런스는 하드 엣지).
4. **아트 바닥판** — 레퍼런스 아트는 슬롯을 꽉 채운 그림이라 이모지 하나만 띄우면 우측 100px 커버리지가
   무너진다(O 실측 REF 66~79% vs 우리 25~56%). 슬롯에 우하단 방사 + 좌·하 선형의 **어두운 판**을 깔아
   «그림이 차지한 면» 을 예약했다 → 커버리지 **84% / 82%**, 최하단행 폭 236→302 · 219→288.
5. **잠금 카드 씬 아트 자리 신설(`.dnc>.scn`)** — 레퍼런스 잠금 카드는 우측 썸네일이 아니라
   **카드 절반을 덮는 씬**(잉크 좌단 385/465/395)이다. 그 bbox 를 벽돌 텍스처 판으로 예약하고
   딤(`.lk`)·자물쇠 아래에 넣었다.

실측(`tools/artbox72.py` — 레퍼런스·캡처 **동일 알고리즘**):

| | REF 카드1 | CAP 카드1 | REF 카드2 | CAP 카드2 |
|---|---|---|---|---|
| 최하단행 아트 폭 | 360 | **302** | 277 | **288** |
| 우측 100px 커버리지 | 75% | **84%** | 79% | **82%** |

게이트: `node tools/verify72.js` → **VERIFY72 53/53 PASS** (카드별 슬롯 기하 · 씬 자리 좌단 · z 순서 ·
클릭 통과 · 콘솔 에러 0). 캡처 `docs/review/72-r2.png`.

## 5. 2회차 비평 (Q · R — 독립 2인) → **Q 7 / R 6**

| | Q | R |
|---|---|---|
| ①~④ | ①8 ②8 ③7 ④9 → **7** | ①6 ②6 ③6 ④7 → **6** |
| 신뢰도 체크 | 3/3 정답 | 3/3 정답 |

- **해금 카드 1·2 는 통과권.** R 실측: 카드1 아트 x716(REF 715) · top +36(REF +36) · h303(REF 303),
  카드2 x730(REF 727) · top +52(REF +51). 우측·하단 두 방향 잘림도 확인(«박스 안에 뜬 아이콘 아님»).
  Q 가 카드1 top 을 +51 로 읽었지만 R 실측·우리 프로브 실측이 둘 다 +36~38 이라 **Q 의 단독 오독**으로 판단.
- **감점의 본체는 잠금 카드 3·4·5.** R 실측 — 레퍼런스 잠금 카드 아트는 top **+10/+12/+35** · 높이
  **329/328/305** 로 카드를 거의 다 덮고 우단·하단에 닿는데, 우리 2회차는 top **+45/+46/+54** ·
  우단 접촉 **1%/17%/1%**(REF 28%/26%/60%) 로 «박스 안에 뜬 아이콘» 이었다.
- 씬 판 대비가 **ΔL1 26~31** 로 사실상 비가시(REF 씬 잉크 60~90).

## 6. 3회차 수정 (3건)

1. **잠금 카드 슬롯 확대** — 3·4: `330×329 @ top+12`, 5: `330×306 @ top+35` (R 실측 그대로).
   이모지도 다시 풀었다(🗿 fs312/sx1.30 · 👺 fs312/sx1.028 · 👹 fs289/sx1.063).
   프로브 실측 결과 잉크가 **x694~1021 · top +12 · 높이 329** 로 슬롯을 꽉 채운다.
2. **씬 판 대비 강화** — 텍스처 알파 .055/.07 → **.17/.20/.24/.13** + 우하단 방사 음영,
   좌측 페이드 26px → **8px**(레퍼런스는 하드 엣지).
3. **씬 판 좌단 통일 420(abs 470)** — 비평가 P 는 385/465/395, R 은 479/412/412 로 갈렸다.
   우리 자체 스캔(하단 밴드 rel295~338, 딤 보정 임계 T=25)은 **480~600** 구간에서 신호가 시작돼
   두 값의 중간이다. 3회차는 **470 하나로 통일**하고 다음 회차 비평가에게 «좌단을 못박아라» 고 요청했다.
4. 카드1·2 미세 보정 — 카드1 잉크 top +38 → **+36**(cy −2), 카드2 잉크 폭 293 → **294**, 좌단 729 → **728**.

프로브 실측(3회차):

| 카드 | 잉크 x | 잉크 top(카드 기준) | 가시 높이 | 레퍼런스 기준값 |
|---|---|---|---|---|
| 1 | 715~1021 | **+36** | 305 | x715 · +36 · ~304 |
| 2 | 728~1021 | **+52** | 289 | x727 · +51 · ~289 |
| 3 | 696~1021 | **+12** | 329 | +10 · 329 |
| 4 | 696~1021 | **+12** | 329 | +12 · 328 |
| 5 | 694~1021 | **+12 → +35 로 재조정** | 306 | +35 · 305 |

게이트: `VERIFY72 53/53 PASS`. 캡처 `docs/review/72-r3.png`.

## 7. 3회차 비평 (S · T — 독립 2인) → **S 5 / T 3**

신뢰도 체크: S **3/3 정답**(카드 x50~1029/이너 966 · pitch 360 · 자물쇠 106×121 @+51).
T 는 테두리 6px(실제 7)·던전명 잉크 45px(실제 51~52)로 **2/3 오답** — 수치 자체는 낮게 잡고,
방향(잠금 카드 씬)만 참고했다.

- **해금 카드 1·2 는 두 비평가 모두 사실상 통과** — S 실측 카드1 x715 top+29~36 h302(REF 그대로),
  카드2 x728 top+51. 세로는 ±4px, 우·하단 잘림도 확인.
- **잠금 카드가 여전히 감점의 전부.** 3회차에 슬롯을 330×329 로 키웠지만 여전히 «우측 박스» 였다.
- **S 가 씬 좌단 논쟁을 못 박았다(4가지 방법 교차)**: 레퍼런스 잠금 카드 씬은
  **x 57(카드 이너 좌단) 풀블리드 + 좌측 45% 사선 스크림** 이다.
  앞선 비평가들의 385/465/395·412/479 는 «스크림이 걷혀 눈에 보이기 시작하는 지점» 이었지 아트 좌단이 아니다.

## 8. 4회차 수정 (2건)

1. **잠금 카드 씬 자리를 카드 안쪽 전면으로** — `--scn: 420px → 7px` (abs x57~1022, 세로 8~341 전면).
   좌측 34% 에 선형 스크림을 얹어 레퍼런스의 «좌측 스크림» 구조를 재현하고, 텍스처 대비는
   텍스트 가독을 위해 살짝 낮췄다(.17/.20/.24/.13 → .15/.18/.22/.12 + 우하단 방사 .34).
2. **카드5 잉크 우단 3px 보정** (S 지적: 1019 → 1022). `thcx 168 → 171`.

게이트: `VERIFY72 53/53 PASS` · `SMOKE PASS`. 캡처 `docs/review/72-r4.png`.

## 9. 4회차 비평 (U · V — 독립 2인) → **U 9(통과) / V 6**

신뢰도 체크: U **3/3 정답**(이너 x57~1022/966 · pitch360 gap10 · 던전명 잉크 51~52),
V **3/3 정답**(이너 966 · 첫 카드 이너 top 249 · 자물쇠 106×119 @+53).

- **U: ①9 ②9 ③9 ④9 → 통과.** 실측 «카드1 x717(REF 714)·top+38(REF+37)·h303(REF305),
  카드2 x734(REF727)·top+55(REF+52), 잠금 3장 풀블리드 x57~1022 top+11 — Δ0».
  우단 x1022 접촉 **REF 5/5 → CAP 5/5**, 하단 접촉도 5/5(해금 2장만 1px 부족).
- **V: ①7 ②6 ③7 ④7 → 6.** 감점 1순위는 **카드3 모아이(🗿)가 우단 x955 에서 끊겨 67px 이 빈 것**과
  «잠금/해금 피사체 면적비 3.6× → 1.0×».
- 두 비평가 모두 **«실제 일러스트 없이도 CSS 로 회수 가능»** 이라고 답했다(아트 대기 사유 아님).

## 10. 5회차 수정 (4건)

1. **카드3 대체물 교체 🗿 → 🧌** — V 의 «우단 67px 공백» 은 모아이 글리프의 잉크 폭이
   **0.81em** 밖에 안 돼(다른 글리프는 1.03~1.13em) scaleX 1.30 으로도 슬롯을 못 채운 것이었다.
   잉크비를 7종 실측해(🦖1.117/0.96 · 🐲1.107/0.87 · 🧌1.027/1.11 · 🐙1.097/1.0 · 🏛1.097/1.107 ·
   🗿0.807/1.12 · 🐗1.107/1.05) **세로비까지 맞는 🧌**(1.027 × 1.11)로 갈아끼우고 fs309/sx1.059 로 다시 풀었다.
   → **아트 자리 규칙 교훈**: 이모지를 아트 자리로 쓸 때는 «그 자리 종횡비에 맞는 글리프» 를 먼저 고르는 게
   scaleX 로 늘리는 것보다 낫다. 0.81em 짜리를 1.3배 늘려도 30% 왜곡만 생기고 bbox 는 안 찬다.
2. 카드1 잉크 좌단 **717 → 714** (sx 1.147 → 1.158, cx 158.5 → 157) — U.
3. 카드2 잉크 좌단 **734 → 727** · top **+55 → +52** (sx 0.972 → 0.995, cx 149.5 → 146, cy 152.5 → 149.5) — U.
4. 잠금 씬 좌측 스크림 — 명암 교차점 **x480 → x452**, 좌측 과딤 완화
   (`rgba(0,0,0,.10) 0 34%,transparent 62%` → `.07 0 22%,transparent 48%`) — U.

게이트: `VERIFY72 53/53 PASS` · `SMOKE PASS`. 캡처 `docs/review/72-r5.png`.

## 11. 5회차 비평 (W · X — 독립 2인) → **W 6 / X 7**

신뢰도 체크 두 명 다 **3/3 정답**. 배치·크기 자체는 «수렴 완료» 로 두 명 모두 인정
(W: ①9 ②9 — «슬롯 규격이 8명 합의값과 ≤2px», X: ①8 ②7).

남은 감점은 **한 가지로 수렴한다 — «bbox 는 맞는데 그 안이 실루엣이라 경계 접촉이 절반»**:

| 지표 | REF | CAP(5회차) |
|---|---|---|
| 카드1 우단 8px 행접촉 | 79% | **29%** |
| 카드1 하단 8px 열접촉 | 96% | **28%** |
| 카드2 우측 100px 커버 | 100% | **48%** (🐍 잉크 우단 1000, 슬롯 우단 1022) |
| 카드3 잉크 하단 | +341 | **+316** (25px 공백) |

- W 는 이 항목을 **«아트 리소스 없이는 회수 불가»** 로 분류했다(이모지를 더 키우면 확정 bbox 가 깨진다).
- X 는 «`--tsx/--thcx/--tht` 수치와 잠금 `.th` 레이어 제거로 회수 가능» 으로 봤다.
- X 가 **캡처 오염**을 잡아냈다 — 58 연출 모듈의 재화 파티클이 카드2·3 위를 지나가 스캔에 섞였다.

## 12. 6회차 수정 (5건)

1. **캡처에서 연출 파티클 제거** — `cap72.js` 가 `.fx-fly/.fx-plus/.fx-spark/.fx-toast/.fx-check/.fx-flash`
   를 숨기고 찍는다(LESSONS 30-② «시간 의존 상태는 캡처 전에 지운다» 의 재발).
2. **카드2 잉크 폭** — `--tsx 0.995 → 1.104`(잉크 267 → 296, 우단 1000 → 1022).
3. **잠금 3장 상단 인셋 통일** — `.scn{top:8 → 11}`(W 실측 REF rel +11), `--tht 12/12/35 → 11/11/11`,
   이모지도 fs310/310/305 · sx1.055/0.994/0.977 로 다시 풀어 **잉크가 하단 +341 까지 닿게** 했다.
4. **잠금 카드 이중 감광 제거** — `.dnc>.scn~.th{background:none}`. 씬 판이 이미 이너 전면을 덮는데
   그 위에 썸네일 바닥판까지 깔려 우하단이 두 번 어두워지고 330px 에서 재클리핑됐다(X 지적).
5. **해금 카드 바닥판에 «가장자리 띠»** — 우·하단 12px 에 진한 띠 + 34px 페이드.
   레퍼런스 일러스트가 경계에서 «잘려 붙는» 문법을 임시 대체물로 흉내 낸 것이다(W 지적 ④).

게이트: `VERIFY72 53/53 PASS` · `SMOKE PASS`. 캡처 `docs/review/72-r6.png`.

## 13. 6회차 비평 (Y · Z — 독립 2인) → **Y 7 / Z 6** · 그리고 **보류(아트 대기) 판정**

신뢰도 체크 두 명 다 **3/3 정답**.

| | Y | Z |
|---|---|---|
| ①~④ | ①7 ②7 ③**9** ④**9** → 7 | ①7 ②7 ③7 ④6 → 6 |
| bbox 판정 | «bbox 자체는 사실상 수렴했다» (카드1 Δw −0.6% · 카드2 Δw −1.4% · 잠금 씬 4변 **0px**) | 카드 이너 top 5장 0px, 씬 전면 커버 ○ |
| 남은 감점 | **경계 «접촉»** — 카드1 하단 39%(REF 100%) · 카드2 우단 14%(REF 85%) | 잠금 피사체 우단 1010~1015(목표 1022) |
| 아트 없이 회수 가능? | **«예, 불가»** | «아니오(가능)» |

**우리 자체 프로브 실측(배경 평탄화 + 마스크·스크림 해제)에서는 5장 모두 잉크가 슬롯 4변에 1px 이내로 닿는다.**
즉 «슬롯 기하» 는 끝났고, 비평가들이 재는 건 **글리프 실루엣이 그 bbox 를 얼마나 «채우는가»** 다 —
🐉·🐍 같은 가늘고 구불한 글리프는 실루엣이 고정이라 더 키우면 확정 bbox(±2px)가 깨진다.
**W(5회차)·Y(6회차) 두 명이 이 항목을 «아트 리소스 없이는 회수 불가» 로 분류**했으므로
지시서 [3]-(나) 마지막 항에 따라 **«보류(아트 대기)»** 로 넘긴다.

## 14. 7회차 (마지막) — 검정 스크림 제거

Y 의 유일한 «CSS 로 즉시 회수» 지적을 반영했다. 2~6회차에 깔았던 `.th` 바닥판·가장자리 띠는
**접촉률을 못 올리면서**(우단 40%p 그대로) 우측 명도를 **−57%**(52,30,40 vs REF 121,67,91) 로 떨어뜨려
레퍼런스의 «우측으로 밝아지는 선버스트» 와 **명암 구배가 반대**가 됐다. 전부 걷어내고 슬롯은 기하만 남겼다 —
아트가 들어오면 `.dnc>.th>em` 을 `background-image` 로 바꾸기만 하면 되는 상태다.

게이트: `VERIFY72 53/53 PASS` · `SMOKE PASS`. 최종 캡처 `docs/review/72-r7.png`.

## 15. 결론

| 항목 | 상태 |
|---|---|
| 썸네일 슬롯 기하 | **완료** — 카드별 bbox 가 레퍼런스 실측과 ±2px (비평가 12명 중 후반 6명 전원 인정) |
| 잠금 카드 씬 자리 | **완료** — 카드 이너 전면(x57~1022, top+11~+341) 예약, 딤·자물쇠·텍스트가 그 위 |
| 대체 아트(이모지 5종) | 자리 예약용. 잉크 폭·높이비를 실측해 카드별 `fs`·`scaleX` 로 정규화 |
| 남은 감점 | **경계 접촉률(실루엣 채움)** — 실제 일러스트 5장 + 잠금 씬 1장이 들어와야 회수 (발주서: `docs/measure/03-던전팝업.md` 부록 D) |
| 회차 | **7/10** (상한 전 «보류(아트 대기)» 로 종료 — 지시서 [3]-(나) 마지막 항) |
| 최고 점수 | 개별 최고 **U 9점**(4회차) · 두 명 최저 기준 **7점**(5·6회차) |

---

# 8회차 — 저장소 주인 재지시(2026-08-26): «아트 대기» 해제 · 실제 스프라이트 + «액자»

> 워커 B · sess-0120-30276 · 2026-08-26 · 인계(lock 없음, 7회차에서 «보류(아트 대기)» 로 멈춰 있었다)

## 16. 지시와 그 귀결

> **«던전들 행에 썸네일이 없다 — 넣게 하라» → 아트 대기 해제, 지금 채운다.**
> 이모지 대체가 아니라 97 비고 ② 방식(기존 스프라이트 프레임 1장을 캔버스로, 잠금은 어둡게 + 🔒)으로
> 카드 전부 실제 썸네일. 썸네일 프레임은 69 레퍼런스의 아이템 칸처럼 **«액자»** 로 읽히되
> 색·테두리는 **104 공용 아이템 프레임 토큰**.

1~7회차가 «보류(아트 대기)» 로 멈춘 이유는 **하나**였다 — 비평가 W·Y 가 남은 감점을
«글리프 실루엣이 슬롯 bbox 를 채우지 못한다 → 아트 리소스 없이는 회수 불가» 로 분류했다.
주인 지시는 그 병목을 **다른 방향으로** 푼다: 일러스트를 기다리는 대신 **게임 안에 이미 있는
스프라이트**를 넣고, «bbox 를 꽉 채워야 한다» 는 요구 자체를 **«액자에 담는다»** 로 바꾼다.

### 검증 방식을 [3]-(나) → **[3]-(가)** 로 바꿨다 (비평가 없음)

1~7회차는 [3]-(나) 레이아웃 루프(비평가 2명)였다. 8회차는 **비평가를 띄우지 않았다.** 근거:

- 지시가 **레퍼런스에서 의도적으로 이탈**한다. 레퍼런스에는 액자가 없고, 일러스트가 카드 우·하단으로
  잘려 나간다. 레퍼런스와 대조하는 비평가는 **주인이 지시한 바로 그것**을 감점한다.
- **같은 일(레이드 카드 썸네일)을 한 작업 97 이 이미 [3]-(가) 수치 게이트로 처리**했고 통과했다.
  같은 부품·같은 규칙인데 검증 방식만 갈라 두면 둘이 어긋난다.
- 지시의 내용이 전부 **수치로 떨어진다** — 액자 두께·코너 비율은 104 토큰의 절대값이고,
  «담았다(contain)» 는 «잉크 = 원본 잉크 × 배율 k» 로 ±2px 안에서 잰다.

측정표 정오표: `docs/measure/03-던전팝업.md` **부록 D 정오표**(구 발주서가 무엇을 전제했고 무엇이 바뀌었는지).

## 17. 한 것

### ① 이모지 6종 폐기 → 스프라이트 캔버스 6종

`DUN_UI` 의 이모지 필드(`th` · `thf`(font-size) · `tsx` · `thcx` · `thcy`)를 통째로 걷어내고
97 과 **같은 이름의 필드**(`thk` 아틀라스 · `thf` 프레임 · `thi` 아이들 애니)로 갈아 끼웠다.
`renderDunPage()` 가 `<em>이모지</em>` 대신 `<canvas class="thcv">` 를 찍고, 97 의 페인터
(`raidPaint`/`raidDraw`/`raidEnt`/`raidIdleTick`)를 그대로 탄다 — 던전 카드도 **121-⑥ 아이들 8fps 순환**이
공짜로 따라온다(잠금 카드는 정지, 페이지 닫히면 정지, 절전이면 정지 — 121 지시 ② 그대로).

아트 후보는 `tools/probe72s.js`(신설)로 **아틀라스 7종 × 애니 전부 × 표본 3프레임**의 알파 잉크
bbox·종횡·휘도를 실측해 골랐다. **6장이 전부 다른 아틀라스**다(게이트가 강제한다):

| 카드 | 아틀라스 / 정지 프레임 | 아이들 | 원본 잉크 | 밝기 |
|---|---|---|---|---|
| 1 골드 | `dragon` / `f3` | `fly` | 94×64 (1.469) | ×1.85 |
| 2 다이아 | `bird` / `frame0` | `anim` | 80×93 (0.860) | ×1.00 |
| 3 유물조각 1단 | `elves` / `green_idle_0` 고블린 | `green_idle` | 112×130 (0.862) | ×1.15 |
| 4 유물조각 2단 | `zombie` / `walk_004` | `walk` | 195×195 (1.000) | ×1.45 |
| 5 유물조각 3단 | `robo` / `Running_007` | `Running` | 124×215 (0.577) | ×1.25 |
| 6 유물조각 4단 | `elves` / `blue_idle_0` 군주 | `blue_idle` | 105×132 (0.795) | ×1.15 |

### ② 슬롯 → 액자 (104 공용 토큰)

`.dnc:not(.rd)>.th` 에만 액자를 입힌다. **슬롯 bbox 는 한 픽셀도 안 바꿨다** —
`box-sizing:border-box` 라 테두리가 안쪽으로 그려지고, 그래서 `verify72 [1]` 의 기하 게이트
(311×305/36 · 296×289/52 · 330×330/11 · 우단 inset 7)가 **그대로 산다.**

- 검정 테두리 `--if-bw:5px` · 안쪽 림 `--if-rim:6px` · 코너 `--if-rr:.233` → 폭×0.233 (104 절대 토큰)
- 면·림 **색 두 개만** 화면이 준다(104 규칙) — 면 = 카드 `--i`(어둡게 깔고), 림 = 카드 `--pt`(밝은 파스텔).
  카드 팔레트에서 파생하므로 유물조각 1~4단의 «층별 색 단계» 가 액자에도 공짜로 따라온다.
- 좌측 12px 알파 페이드 마스크는 **여기서만** 걷어냈다 — 액자는 네 변이 다 보여야 한다.
  레이드 카드(`.dnc.rd`)는 97 규칙 그대로 마스크·꽉 채우기가 살아 있다(97 게이트 무손상).

### ③ «늘리기» → «담기»(contain) — `drawSpriteTo` 에 `fit`

97 의 `drawSpriteTo` 는 프레임 rect 를 캔버스에 **늘려** 채운다(«아트 자리 규칙» 의 스프라이트판).
액자에서는 그게 틀린다 — 용 96×64(종횡 1.47)·로봇 126×217(0.58)을 슬롯 종횡 ≈1.0 에 늘리면 찌그러진다.
`o.fit`(사방 여백 px)을 주면 **종횡비를 지켜 담는다.** `o.fit` 이 없으면 `dx/dy=0 · dw/dh=W/H` 라
**97 레이드 경로는 픽셀까지 이전과 같다**(게이트로 확인 — [5] 12항목 전원 통과).

여백은 16px 이다. 이유는 두 개다: 액자로 읽히려면 그림이 림에 닿으면 안 되고,
**121 들썩이 아래로 최대 15px 내려간다** — 16 이면 어떤 위상에서도 안 잘린다.

### ④ 밝기 보정은 CSS 가 아니라 «그리기» 에서

틴트(multiply)는 **어둡게만** 된다. 용(원본 휘도 57)·좀비(77)는 액자 면 위에서 묻힌다.
처음에 `filter:brightness(var(--thbr))` 로 CSS 에 걸었더니 **캔버스 픽셀은 원본 그대로**라
게이트가 «잉크 휘도 55 vs 면 95» 로 읽었다 — **화면과 게이트가 서로 다른 그림을 보는** 상태다.
`drawSpriteTo` 안에서 `g.filter='brightness(n)'` 로 옮겨 픽셀 자체를 밝혔다.
→ 97 [2] 규칙(«잉크 휘도 > 면 휘도 − 20»)을 6장 전원이 통과한다.

> **교훈(LESSONS 후보)**: «화면에만 걸고 게이트가 못 보는 보정» 은 보정이 아니라 **게이트 우회**다.
> CSS filter·mix-blend 로 픽셀을 바꾸면 캔버스 게이트는 그 변화를 못 본다 — 그리기 쪽으로 옮겨라.

### ⑤ 잠금 카드

«어둡게 + 🔒» 는 이미 `.lk`(카드 이너 전면 `rgba(0,0,0,.65)` + 자물쇠 106×123)가 하고 있고,
DOM 순서상 `.lk` 가 `.th` **뒤**라 액자·스프라이트 위로 온다(게이트 [2] 가 카드 4장 전부에서 잰다).
잠금 카드의 씬 아트 자리 `.dnc>.scn`(966×334)은 **손대지 않았다** — 여전히 아트 대기이고 부록 D 그대로다.

## 18. 게이트

`tools/verify72.js` 를 **이모지 게이트 → 액자·스프라이트 게이트**로 바꿨다(97 이 [5] 를 더한 것과 같은 방식).

- **[1] 슬롯 기하** — 그대로. 6장 w×h·상단 인셋·우단 inset 7 (구 게이트와 같은 기대값).
- **[1-1] 액자(신설)** — 검정 5px · 코너 = 폭×0.233 · 안쪽 림 inset box-shadow · 면 색 존재.
- **[1-2] 액자 안 그림(신설)** — 캔버스 존재(이모지 아님) · 픽셀 = 액자 안쪽 · CSS 크기 = 픽셀(1:1) ·
  실제로 그려짐 · 사방 여백 ≥ 15 · **종횡 왜곡 0**(잉크 = 원본 잉크 × k, ±2px) · 짧은 축 여백 = 16 ·
  **잉크 휘도 > 면 휘도 − 20** · 들썩 `thBob` 붙음 · 스쿼시 축 = 캔버스 바닥 · **6장이 서로 다른 아트**.
- **[5-2]** «이모지 유지» → «스프라이트 유지(이모지 0)».

> ⚠ 종횡 왜곡을 **여백**으로 재면 안 된다(한 번 틀렸다). 아틀라스 프레임은 사방에 투명 1~2px 을
> 갖고 있어 «잉크 = 프레임» 이 아니다 — 여백 상한으로 재면 그 패딩만큼 항상 어긋난다.
> **원본 잉크 × 배율 k 와 대조**하는 것이 진짜 게이트다.

### 결과

| 게이트 | 결과 |
|---|---|
| `node tools/verify72.js` | **165/165 PASS** (구 53/53 → 항목 3배) |
| `node tools/smoke.js` | **SMOKE PASS** |
| `node tools/verify121.js` | 114/122 — **8건 전부 내 변경 이전부터 실패**(같은 커밋에서 A/B 확인). 121 이 지금 잡고 있는 진행 중 작업이다 |

**121 게이트 이관**: `verify121.js` 가 `.dnc>.th>em` 을 2곳에서 직접 잡고 있어 내 변경으로 **즉사**했다
(`Cannot read properties of null`). 110 교훈(«구성 변경이 옛 게이트를 앞지르면 다음 사람은 게이트가 죽은 줄도
모른다») 대로 선택자를 `em, canvas` 둘 다 받게 넓히고, 스쿼시 축 기대값도 캔버스용(=자기 높이)으로 갈랐다.
**121 의 판정 로직·기대 수치는 안 건드렸다** — 죽은 호출만 살렸다.

## 19. 9회차 — 성능 회귀를 A/B 로 잡아낸 것 (같은 세션)

verify72 는 **정지 화면**만 본다(165/165 PASS). 느려진 것은 **121 의 스크롤 fps 표**에서만 보였다.

### 실측 (`tools/probe72p.js` 신설 — verify121 §7 의 왕복 스크롤 120프레임만 잘라 낸 프로브)

| 빌드 | 절대 fps(배경 ON) | 121 §7 유지율(3쌍 중앙값) | 중앙 프레임 |
|---|---|---|---|
| main(이모지, 8회차 이전) | 17.2 | **82~84%** | 50ms |
| 8회차 초판 | 16.9 | **58~60%** | 66.7ms |
| + 드롭섀도 제거 | 18.5 | 67~71% | **50ms** |
| + will-change 제거(최종) | — | **73~75%** | 50ms |

### 고친 것 3건

1. **`ctx.filter` 를 매 프레임 걸고 있었다** → `brightFrame()` 프레임 캐시(87 `tintCache` 와 같은 규칙).
   ⚠ 이것만으로는 **안 나아졌다**(58 → 58). 원인이 아니었다 — 그래도 캐시는 옳으므로 남긴다.
2. **드롭섀도가 «화면에 안 보이는데» fps 만 먹고 있었다** — `.th{overflow:hidden}` 이 캔버스 **바깥**으로
   퍼지는 그림자를 전부 잘라 낸다(오프셋 `0 8px 12px`). 던전 캔버스에서 `filter:none`. **+11%**.
   같은 이유로 레이드 캔버스(97)에서도 안 보이지만 **내 구간이 아니라 안 건드렸다** — 97 의 공짜 이득감이다.
3. **액자 6칸을 합성 레이어로 승격시킬 이유가 없었다** — `will-change:translate,scale` 제거. **+4%p**.
   verify121 은 will-change 를 `.bgm` 에서만 재므로(§229·239) 게이트에 영향 없다.

### ⚠ 이때 낸 회귀 1건 (같은 세션에서 잡아 고침)

`will-change` 만 갈라 내려고 `#dunw.on .dnc:not(.rd)>.th>canvas{animation-play-state:running;…}` 로 썼더니
**클래스가 하나 늘어 명시도가 (1,3,1) → (1,4,1)** 이 됐고, 아래에 있는 잠금 정지 규칙
`#dunw .dnc.lkd>.th>canvas{paused}`(1,3,1)를 **이겨 버려 잠금 카드 썸네일이 계속 돌았다.**
121 이 «명시도가 같아 소스 순서로 이긴다 — 순서를 바꾸지 말 것» 이라고 주석에 못을 박아 둔 자리다.
`animation-play-state` 는 원래 명시도(`.dnc>`)로 두고 **`will-change` 만** `.rd` 로 갈랐다.
verify121 §«잠금 썸네일 paused» 4건이 잡아냈다.

### 결과 — `verify121` 은 내 변경으로 **좋아졌다**

| | main(8회차 전) | 최종 |
|---|---|---|
| VERIFY121 | 114/122 | **120/122** |
| 실패 항목 | 슬롯 «불변» 6건 + 스크롤 fps 2건 | **스크롤 fps 2건뿐** |

남은 2건(유지율 73~75% < 90% · 중앙 프레임 50ms > 기준×1.15)은 **main 에서도 실패한다**(82~84% < 90%).
121 이 지금 다른 세션이 잡고 있는 진행 중 작업이고, 그 목표치(≥90%)는 **소프트웨어 렌더 러너에서
배경 레이어만으로도 못 맞추고 있는** 값이다. 썸네일은 주인이 지시한 산출물이라 뺄 수 없고,
뺄 수 있는 낭비(드롭섀도·레이어 승격·필터 재생성)는 위 3건으로 전부 걷어냈다.
**남은 격차(83% → 74%)는 «액자 6칸이 스크롤 중 합성에 드는 실비» 다** — 121 세션이 목표치를
러너 현실에 맞추거나(지금도 그 방향으로 «손실률» 로 옮겨 놨다) 카드 가상 스크롤로 가야 회수된다.

## 20. 10회차 — 시각 검수에서 나온 것 (같은 세션, 캡처 픽셀 계측)

캡처를 픽셀 단위로 계측시켜 «게이트가 통과시킨 것 중 눈에는 나쁜 것» 을 찾았다. 3건 나왔고 2건 고쳤다.

### 고친 것 ①: `cap72.js --unlock` 이 **6장 중 3장을 잠근 채로 찍고 있었다**

`S.guide.idx = 99` 만 올렸는데, **90 이 유물조각 2~4단을 «이전 단 N층 클리어»(`DUN_UI[].pre`)로 바꿔 놓았다.**
그래서 «전부 해금» 캡처가 실제로는 3장 잠금이었고, 그 캡처로 아트를 판단하면 **잠금 딤(65%)을 아트 문제로 오독한다**
(실제로 «카드4 좀비가 판독 불가» 라는 지적이 나왔는데, 딤을 걷으면 멀쩡하다).
`--unlock` 이 `pre` 사슬도 같이 풀도록 고쳤다.

> **교훈**: «해금 상태를 만드는 헬퍼» 는 **해금 조건이 늘어날 때마다 같이 늘려야 한다.**
> 90 이 조건을 하나 더 만들었을 때 이 헬퍼가 안 따라왔고, 아무도 몰랐다(LESSONS 110 계열).

### 고친 것 ②: 97 의 «잉크 휘도 > 면 휘도 − 20» 은 **«있으나 안 보이는» 것을 통과시킨다**

게이트는 6장 전원 통과였는데, 비율로 다시 재니 **카드1 = 1.00 · 카드5 = 1.11** 이었다.
«면보다 어둡기만 안 하면 통과» 라 사실상 아무것도 안 막고 있었다 — 126 의 «있는데 안 보이던 켜 줄눈»(대비 0.4/255)과 같은 종류다.

**게이트를 비율로 바꿨다: `잉크 휘도 ≥ 면(--i) × 1.3`.** 여기 «면» 은 `--i` 원색이고 실제 액자 면은
그 위에 검정 22~52% 가 덮여 더 어둡다 — 즉 ×1.3 은 화면에서 ≈1.9:1 이라 **보수적**이다.
맞추려고 밝기를 다시 풀고, 액자 면 그라디언트도 `.10→.34` 에서 **`.22→.52`** 로 깊게 해 그림이 뜨게 했다.

| 카드 | 밝기(전 → 후) | 잉크 휘도 | 면(`--i`) | 비율 |
|---|---|---|---|---|
| 1 골드 (dragon) | 1.85 → **2.70** | 130 | 95 | **1.37** |
| 2 다이아 (bird) | 1.00 | 157 | 93 | 1.69 |
| 3 유물1 (고블린) | 1.15 → **1.25** | 128 | 85 | 1.51 |
| 4 유물2 (zombie) | 1.45 → **1.60** | 122 | 53 | **2.30** |
| 5 유물3 (robo) | 1.25 → **1.55** | 135 | 102 | **1.32** |
| 6 유물4 (군주) | 1.15 → **1.30** | 116 | 70 | 1.66 |

### 안 고친 것: **액자의 우·하단이 카드 테두리와 붙는다** (알고 남긴다)

계측 결과 «검정 외곽선 + 밝은 이너 림 + 라운드 코너» 는 **네 변 전부**에 있지만,
우·하단은 액자의 검정선이 **카드 자신의 검정 테두리(7~8px)와 이어져 한 덩어리 띠**로 보인다
(우측 간격 0px · 하단 1px). 즉 좌·상 2면이 액자로 강하게 읽히고 우·하는 «카드 모서리에 물린» 것처럼 읽힌다.

**고치려면 슬롯을 카드 안쪽에서 10px 쯤 띄워야 하는데, 그건 §3-8 실측 bbox 를 움직이는 것이다.**
그 bbox 는 (ⓐ) 우리가 가진 **유일한 레퍼런스 실측치**이고 (ⓑ) `verify72 [1]` 과 **`verify121` 의
«슬롯 불변» 6항목**이 동시에 못 박고 있으며 (ⓒ) 121 은 **지금 다른 세션이 잡고 있는 진행 중 작업**이다.
액자를 위해 남의 게이트 기대치를 흔드는 것은 값이 안 맞는다고 판단했다 —
**주인이 «우·하단도 띄워라» 라고 하면 그때 세 곳(측정표·verify72·verify121)을 같이 옮기면 된다.**

### 그 밖에 남긴 관찰

- **카드1 용은 액자를 31% 만 채운다** — 96×64(종횡 1.47) 스프라이트를 종횡 ≈1.0 액자에 «담으면»
  가로가 먼저 차고 세로가 남는다. 늘리면 찌그러지므로 이게 contain 의 정상 동작이다.
  용 아트가 세로로 긴 것으로 바뀌면 저절로 회수된다(발주서 참조).
- **레드닷(`.dot`)이 카드 3~6 의 액자 우상단 코너와 겹친다** — `.dot` 이 `.th` 뒤라 위로 온다.
  배지가 프레임 모서리를 살짝 무는 흔한 형태라 손대지 않았다(잠금 카드는 `.dot` 자체가 없다).
- **잠금 조건 텍스트(`.lk>u`)가 «남은 횟수» 라벨 위로 올라탄다** — 액자와 무관한 **기존 결함**.
  우단이 액자 좌단과 3~7px 로 붙는 것도 같은 뿌리다. 내 구간이 아니라 PROGRESS 비고에만 남겼다.

## 21. «카드4 상단 잘림» 지적 — 실측으로 반증하고, 대신 **게이트를 하나 늘렸다**

시각 검수가 «카드4 트롤 등이 액자 상단에서 수평으로 잘림(상단 여백 0 · 접촉 폭 210px)» 을 1순위 결함으로 짚었다.
**틀렸다.** `tools/probe72f.js`(신설)로 **전 카드 × 아이들 애니 전 프레임**의 잉크 여백을 직접 재니
**최소 16px** 이고 0 인 프레임은 없다:

| 카드 | 아트 | 프레임 수 | 최소 여백 (최악 프레임) |
|---|---|---|---|
| 1 | dragon/fly | 12 | **16** (f0) |
| 2 | bird/anim | 10 | 18 |
| 3 | elves/green_idle | 5 | 18 |
| 4 | zombie/walk | 9 | **17** (walk_000) |
| 5 | robo/Running | 15 | **17** (Running_000) |
| 6 | elves/blue_idle | 5 | 18 |

지적의 정체는 **좀비 스프라이트 자신의 실루엣**이다 — 웅크린 등이 원래 «평평한 수평선» 이라 잘린 것처럼 읽힌다.
캡처 PNG 를 직접 재려던 시도도 여백 0 으로 나왔는데, 그건 `.sh`(카드 이너 하이라이트·좌우 음영)와 `.fr`(카드 테두리)가
**액자 위에 그라디언트를 덧그리기 때문**이다 — 픽셀 배경 추정이 그 두 겹에 오염된다.
그래서 «잘렸나» 는 **캔버스 픽셀**에서 재야 한다(그게 `overflow:hidden` 이 실제로 자르는 면이다).

> **교훈**: 겹쳐 그리는 레이어가 있는 화면에서 «잘림» 을 캡처 PNG 로 재면 오염된다.
> 자르는 주체(여기서는 캔버스 경계)에서 재라.

### 다만 지적이 **진짜 위험을 가리켰다** → `verify72 [1-3]` 신설

[1-2] 는 «한 순간» 만 잰다. 아틀라스 애니는 **프레임마다 rect 크기가 다르다** —
`zombie/walk` 는 201×178 ~ 174×235 로 흔들리고, contain 배율이 프레임마다 다시 풀린다.
즉 «지금 그려진 한 장이 통과» 는 «9장 전부 통과» 가 아니다. 121-⑥ 이 이걸 8fps 로 돌리므로
**한 프레임만 잘려도 사용자는 본다.** [1-3] 이 6장 × 아이들 전 프레임(총 56장)을 전수로 잰다.

**VERIFY72 165/165 → 171/171 PASS.**

---

# 2차 폴리시 라운드 — 11·12회차 (2026-08-28, sess-0523-5397 워커 F)

> T4 선점. T1~T3 이 비어 있었고(모든 버그·기능 행이 닫힘 · 199 «착수 금지» · 224 «주인 판단 대기» ·
> 58·122·163 은 살아 있는 lock), 남은 화면 중 **①~④ 7 로 가장 낮은 것이 72** 라 잡았다.

## 22. 11회차 — 재캡처 + 2인 비평 (AB · AC)

캡처 `docs/review/72-r11.png` (1080×2280 · 콘솔 에러 0). 착수 시점 게이트는 **VERIFY72 220/220 · SMOKE PASS**.

| | AB | AC |
|---|---|---|
| ①~④ | ①8 ②6 ③7 ④7 → **6** | ①6 ②6 ③**4** ④7 → **4** |
| ⑤⑥ | 8 / 6 | 7 / 7 |

**두 사람이 독립적으로 ② 를 6 으로 찍었다** — 그리고 둘 다 근거로 «우측 썸네일» 을 들었다
(AB ①: 잉크 세로 121px vs 레퍼런스 334px · AC ①: 액자가 아트 가용폭을 잘라 먹는다).

### 지적을 셋으로 갈랐다

1. **내 구간이고 진짜인 것** — ② 썸네일 크기. 아래 12회차가 원인을 찾아 고쳤다.
2. **내 구간이지만 주인 지시로 그렇게 된 것** — «액자 테두리가 레퍼런스에 없다»(AB 2 · AC 1).
   레퍼런스는 일러스트가 카드 우·하단으로 흘러넘치지만, **2026-08-26 저장소 주인 재지시**로
   성격이 «액자에 담긴 그림» 으로 바뀌었다(§16 · 측정표 «부록 D 정오표»). 지시가 레퍼런스를 이긴다 —
   **반영하지 않는다.** 비평가에게 이 사실을 안 알려 준 것은 다음 회차 프롬프트에서 보완할 몫이다.
3. **내 구간이 아닌 것** — 아래 «§24 발견(내 구간 아님)» 으로 옮겨 적었다. 손대지 않았다.

## 23. 12회차 — «액자 배율이 프레임마다 다시 풀린다»

### 원인 — 자를 대고 나서야 보였다

`drawSpriteTo(fit)` 는 **그 프레임의 아틀라스 rect** 를 contain 한다. 그래서 한 사이클 안에서 rect
크기가 바뀌는 애니는 **같은 캐릭터가 프레임마다 다시 정규화된다.** 신설 프로브 `tools/probe72k.js`
(도는 프레임 집합 = 121 아이들 창이 있으면 창, 없으면 전 사이클)로 재니:

| 카드 | 아트 | 배율 스윙 (고치기 전) |
|---|---|---|
| 8 룬강화석 | `elves/blue_attack` | **77.3%** (k 2.06 → 1.18) |
| 4 유물조각2 | `zombie/walk` | 8.8% |
| 2 다이아 | `bird/anim` | 3.2% |
| 3 유물조각1 | `elves/green_idle` | 1.5% |
| 1·5·6·7 | — | 0.0~1.4% |

**이건 새 발견이 아니라 «남의 화면에서만 닫힌» 결함이다.** 258 이 04 세부 팝업 배너에서 같은 자를
대고 12.9~45.9% 로 실측했지만 «03 행 카드의 121 들썩은 그대로 둔다(121 구간이고 주인 지적은 팝업
한정)» 로 명시적으로 남겨 뒀다. 그 «남겨 둔 자리» 가 11회차 두 비평가의 ② 6점이다.

### 고친 것 ① — 배율의 기준을 «이 프레임» 에서 «사이클 최대 박스» 로

`thFitBox(key, frame, anim)` 신설(`TH_IDLE` 창이 있으면 창, 없으면 전 사이클의 **성분별 최대 rect**,
메모이즈). `drawSpriteTo` 에 `o.fitBox` 를 받아 그 박스를 contain 한다. `fitBox` 가 없으면 예전과
한 픽셀도 다르지 않으므로 **펫·도감·아레나·레이드 호출부는 불변**이다(레이드는 `fit:0` 이라 애초에 이 경로가 아니다).

성분별 최대(가로 최대 · 세로 최대)에 `k = min(...)` 이라 **어느 프레임도 액자를 넘지 않는다**.
사실 «배율이 상수 + 아무것도 안 잘린다» 를 동시에 만족하는 기준은 최대 박스뿐이다 —
기준 포즈에 맞추면 뻗은 포즈가 잘리고, 프레임마다 맞추면 지금 상태다.

### 고친 것 ② — 카드8 은 «공격 사이클» 을 아이들로 돌고 있었다

①만 넣으면 카드8 이 **정반대로 망가진다**: 최대 박스가 뻗은 포즈(234×134)라 k=1.18 로 잠기고
서 있는 포스가 액자의 **21%** 밖에 안 찬다(126×158 / 276×276). 원인은 배율이 아니라
**`rstone` 카드에 아이들 창이 없어 공격 사이클을 통째로 돈다**는 것이다.

258 이 배너에서 이미 같은 지점에 도달해 있었다 — «아이들 창이 없어 공격 사이클을 돌던 칸이
칼을 뻗은 중간 포즈로 굳는다(`blue_attack_3` 은 기준 포즈의 1.8배 폭) → **기준 포즈로 고정하는 것이
정지 상태의 정답**». 행 카드는 정지가 아니라 들썩이므로 «한 장 고정» 대신 **뻗기 전 3포즈**로 창을
좁혔다: `TH_IDLE['elves/blue_attack'] = _0 _1 _2 _1`(121 6회차가 elves 두 벌에 쓴 왕복 형태 그대로).
rect 107·106·123 → 최대 박스 123×134 → **k 2.06 유지**.

| 카드8 | 11회차 | ①만 | ①+② (최종) |
|---|---|---|---|
| 배율 스윙 | 77.3% | 0.0% | **0.0%** |
| 서 있는 포즈 그려진 크기 | 220×276 (그러나 프레임마다 요동) | 126×158 | **220×276** |

### 게이트 — 단언을 지우지 않고 **자만 옮겼다**(185-④)

- `[1-2] 종횡 왜곡 0` · `짧은 축이 액자를 채운다` 의 기준 rect → `thFitBox` 박스. «늘리면 걸린다» 성질은 그대로.
- `[1-3]` 에 **«배율이 사이클 내내 하나다»** 8건 신설(스윙 ≤ 1.0%).
- `[1-3]` 이 `fitBox` 없이 다시 그려 **화면과 다른 그림을 재고 있던 것**을 제품 경로(`raidDraw(cv, pin)`)로 교체.

> **★ 되돌림 시험이 이 회차의 진짜 소득이다.** 처음 짠 «배율 불변» 단언은 게이트가 `thFitBox` 를
> **자기가 다시 불러** 기대값을 만들었다. 그래서 제품을 되돌려도(`fitBox: null`) **초록으로 남았다** —
> 279 «죽은 되돌림 시험» 을 그대로 재현한 것이다. A/B 로 걸려서, 배율을 `raidDraw` 의 반환
> `{dx,dy,dw,dh}` 에서 **역산**하도록 바꿨다. 그 뒤의 A/B:

| | 고친 뒤 | `fitBox: null` 로 되돌림 |
|---|---|---|
| VERIFY72 | **228/228 PASS** | **220/228 FAIL** (배율 불변 5건 + 종횡 왜곡 3건) |
| 카드8 스윙 | 0.0% | **77.6%** |

임계 1.0% 는 정수 반올림 여유다 — `dw = round(rect.w × k)` 라 역산 오차가 `0.5/rect.w` 이고
가장 좁은 rect(bird 82px)에서 0.61% 까지 나온다. 0.5% 로 조이면 살아 있는 빌드가 간헐 FAIL 한다(245 계열).

### 검증

`VERIFY72` **220/220 → 228/228 PASS** · `VERIFY121` 149/149 · `VERIFY258` 12/12 · `VERIFY90` 107/107 ·
`VERIFY203` 73/73 · `VERIFY209` 61/61 · `VERIFY210` 83/83 · **SMOKE PASS** ·
`probe72f` 전 카드 전 프레임 최소 여백 16 (불변) · `probe72k` **SWING max 77.3% → 0.0%** · 콘솔 에러 0.

## 24. 14회차 — **12회차의 처방을 재어 보고 되돌렸다**

13회차 채점: **AD ①8 ②6 ③6 ④8 → 6** · **AE ①8 ②5 ③7 ④7 → 5** (11회차 AB 6 / AC 4 에서 최저 4 → 5).
두 사람 다 ① 을 8 로 올렸다. 남은 최저는 ②(썸네일 크기)이고, 둘 다 [썸네일]을 근거로 들었다.

### ★ 이 회차의 본체 — 12회차의 `thFitBox` 는 **다른 작업이 이미 재어서 기각한 안**이었다

12회차는 «액자 배율이 프레임마다 다시 풀린다» 를 잡아 **사이클 최대 박스로 k 를 상수화**했다.
그런데 `TH_IDLE` 주석을 끝까지 읽으니 **121 6회차가 같은 처방을 실측하고 기각해 뒀다** —
`tools/inkjit121.js` 의 `uni`(트림 합집합) 열이 바로 그 안이고, 6장 중 4장에서 잉크 top 흔들림이
악화된다고 표까지 남아 있었다. 12회차는 그 표를 못 보고 같은 길을 다시 갔다.

**두 축을 같이 재는 자를 만들어(`probe72k` 에 «잉크 top p2p» 추가) A/B 했다:**

| | 창 + `fitBox` (12회차) | **창만 (14회차 = 현행)** |
|---|---|---|
| 배율 스윙 max | 0.5% | 8.9% (`zombie/walk`) |
| **잉크 top p2p max** | **12px** (`zombie/walk`) | **2px** |
| 121 기준선(9px = thBob 18 의 절반) | **초과** | 통과 |

**되돌렸다.** 그리고 12회차가 실제로 고친 것이 무엇이었는지도 갈렸다 —
카드8 의 77.3% 는 `fitBox` 가 아니라 **아이들 창**이 고친 것이다(창만 넣으면 77.3% → **1.9%**).
즉 원인은 배율식이 아니라 «공격 사이클을 아이들로 쓴 것» 이었고, 원인을 고치면 증상 쪽 장치가 필요 없다.

> **교훈**: 증상(맥동)에 바로 장치를 붙이기 전에, **그 증상을 이미 재 본 사람이 있는지**부터 본다.
> 여기서는 같은 파일 안의 주석 스무 줄 아래에 표가 있었다.

### 남긴 것 ① — `TH_IDLE['elves/blue_attack']`(12회차 신설) 유지

`rstone` 카드가 공격 사이클을 통째로 돌던 것. 창을 주니 배율 스윙 **77.3% → 1.9%**,
잉크 top 흔들림 **58px → 0px**. `INKJIT121` 이 **FAIL 2장 → 1장**이 됐다(내 작업 전 실측으로 확인).

### 남긴 것 ② — `TH_INKC` 잉크 중심 보정 (14회차 신설, 비평가 3명 일치)

11회차 AB #8(+25.5) · 13회차 AD #2(+24) · AE #3(+25.5) 가 독립적으로 같은 것을 찍었다:
**카드1·7(용)의 그림이 액자 중심에서 아래로 내려앉는다.**

원인은 «rect 중심에 앉힌다» 였다. rect 는 스프라이트 시트의 칸이라 잉크가 그 안에서 가운데 있으리라는
보장이 없다 — `tools/scan72c.js`(신설)로 8장이 쓰는 7종을 전수 측정하니 **6종은 타이트 크롭이라 보정 0**,
`dragon` 만 96×64 **무트림 격자**라 `[-1, -11]`(원본 px) = 화면 **−31.6px** 이었다.

보정은 **사이클 전체 잉크 합집합**을 액자 중심에 맞추는 상수 하나다(프레임마다 맞추면 애니가 제자리걸음이 된다).
결과 — 잉크 합집합 중심 이탈 **(3, 31.5) → (0, −0.5)**.

값은 상수표로 둔다(제품은 픽셀을 안 읽는다 — `getImageData` 는 file:// 에서 캔버스 오염으로 막힌다).
표가 늦게 따라오는 병(245·259 계열)은 **게이트가 매번 아틀라스에서 다시 재서** 막는다.

### 게이트

`[1-3]` 의 «배율 불변 ≤1%»(12회차)를 **두 축으로 바꿨다** — 배율 맥동 ≤15% + **잉크 top p2p ≤9px**(121 기준선).
`[1-4]` `TH_INKC` 표 ↔ 아틀라스 실측 대조 · `[1-5]` 그려진 잉크 합집합이 액자 중심(±3px) 신설.

**VERIFY72 220/220 → 259/259 PASS.** 되돌림 시험 2종:

| 되돌린 것 | 결과 |
|---|---|
| `TH_IDLE['elves/blue_attack']` 제거 | **257/259 FAIL** — 카드8 스윙 77.6% · 잉크 top 58px |
| `inkC` 제거 | **249/251 FAIL** — 카드1·7 중심 (3, 31.5) |

`VERIFY121` 149/149 · `VERIFY258` 12/12 · `VERIFY90` 107/107 · `VERIFY203` 73/73 · **SMOKE PASS** ·
`probe72f` 최소 여백 16(불변) · 콘솔 에러 0.

## 25. 남은 감점의 분류 — 두 비평가에게 직접 물었다

지시서 [3]-(나) 마지막 항(«아트 없이는 회수 불가» 분류)을 적용하려고 AD·AE 에게 **독립적으로** 물었다.
둘 다 같은 결론에 도달했고, 근거도 같았다:

> **정사각 액자 + 종횡비 유지(contain) 에서 «짧은 축 채움률» 의 상한은 `min(AR, 1/AR)` 로 수학적으로 고정된다.**

| 카드 | 스프라이트 잉크 종횡(AR) | 짧은 축 채움 **천장** |
|---|---|---|
| 1·7 용 | **2.10** (88×42, 가로로 납작) | **47.6%** |
| 5 로봇 | **0.58** (125×214, 세로로 길쭉) | **58.1%** |
| 4 좀비 | 0.79 | 79.0% |
| 2 타조 | 0.85 | 85.0% ✅ |
| 3 고블린 | 0.88 | 88.0% ✅ |

레퍼런스의 85~97% 를 양축에서 만족하려면 **AR 0.85~1.18** 이어야 하는데 보유 스프라이트는 0.58~2.10 이다.
두 사람 다 «**없다**» 로 단정했다 — cover(크롭)는 카드1 에서 **52%**, 카드5 에서 **42%** 를 잘라내야 해서
«왜곡» 이 «잘림» 으로 바뀔 뿐이고, 카드별 비정사각 액자는 259 가 통일한 격자를 도로 깬다.

**→ 카드1·5(그리고 같은 아트를 쓰는 7)의 짧은 축 공백은 «보류(아트 대기)».**
발주 조건은 측정표 «부록 D» 에 **AR 0.85~1.18** 로 못 박았다.

## 26. 다음 회차의 첫 일감 — «균일 언더스케일»(2인 일치, 아직 안 함)

AD(A) · AE(A-1) 가 **독립적으로 같은 것**을 짚었고 아직 **회수하지 않았다**:
장축이 액자 내부 306 의 **77~87%** 에서 멈춘다. 원인은 사방 `TH_PAD 16px`(= 306 의 10.4%) + 프레임 투명 여백.

**다만 그 여백은 121 들썩의 자리다**(아래로 최대 14px + 스쿼시). 그래서 «패딩 0» 은 불가하고,
움직임이 **아래로만** 가므로 **비대칭 인셋**(위·좌·우 ~4 / 아래 18)이 실제 상한이다 — 장축 기준
가로 +8.7% · 세로 +3.6%. AD 가 말한 «100%» 에는 **못 간다**.
100% 로 가려면 «캔버스를 액자보다 세로로 18px 크게 잡고 들썩 여유를 보이는 영역 **밖**에 두는» 구조 변경이
필요한데, 그건 `verify72` 의 «캔버스 픽셀 = 액자 안쪽 1:1» 과 `verify121` «슬롯 불변» 6항목을 같이 옮기는 일이다.
**회차가 남아 있으므로(2차 라운드 14/20) 다음 세션이 이것부터 잡을 것.** 지금 급히 손대지 않았다.

## 27. 발견 — 내 구간이 아니라 손대지 않았다

- **`INKJIT121` 이 `tower`(시련의 탑, `elves/green_attack`)에서 FAIL** — 잉크 세로 흔들림 **67px**(기준 9).
  `rstone` 과 **같은 병**(공격 사이클을 아이들로 사용)이고 처방도 같은 한 줄(`TH_IDLE` 창)이지만,
  `tower` 는 **`#dunList` 8장에 없는** 209 의 카드이고 렌더 경로(`.dnc.rd` 꽉 채우기)가 달라
  97 의 «잉크가 슬롯을 채운다» 단언과 충돌할 수 있다 — 재지 않고 건드리지 않았다.
  **내 작업 전에도 FAIL 이었다**(실측: 5651565 에서 `INKJIT121 FAIL — 2장` = rstone + tower).
- 13회차 두 비평가의 [카드]·[헤더]·[서브탭]·[탭바] 지적(던전명 잉크 −18%w · 알약 텍스트 −19%w ·
  헤더 재화 아이콘 −14~21% · 탭 라벨 잉크 +28% · 카드 간격 −3~5px)은 **03·A3·47 구간**이라 손대지 않았다.

## 28. 15회차 — 재캡처 후 채점 (AF · AG)

캡처 `docs/review/72-r15.png` (1080×2280 · 콘솔 0). 14회차의 두 처방이 들어간 뒤의 상태다.
비평가 프롬프트에 **«액자는 주인 재지시 · 짧은 축 공백은 아트 대기»** 를 명시해 이미 닫힌 것을 다시
감점하지 않게 했다(11·13회차에 그 두 가지로 회차가 샜다).

### AF — ①7 ②5 ③6 ④7 (⑤9 ⑥5) → **5**

**«오차 0px» 로 확인해 준 것**: 카드 5장 outer top 157/517/877/1237/1597 · pitch 360 · gap 10 —
레퍼런스와 **Δ0**. 카드 좌우 49~1030 · 헤더 8~101 · 락 아이콘 · 좌측 텍스트 컬럼 x 전부 1~2px.

**[썸네일] 지적 2건에 대한 판정**

- **#1 장축 충전율 80~84%** — **사실이고, 아직 안 고쳤다.** AD(A)·AE(A-1)와 **같은 것**이다(3인 일치).
  §26 에 적은 대로 원인은 `TH_PAD 16` 이고 그 여백은 121 들썩의 자리다. **다음 회차 첫 일감.**
- **#2 액자 안 중심 편심(c3 +15px 우편심 등)** — **재현되지 않았다.** 자가 다르다:
  이 회차가 맞춘 것은 **«도는 프레임 전체의 잉크 합집합 중심»** 이고, `verify72 [1-5]` 실측은
  8장 전부 **(0~0.5, 0~0.5)px** 다. AF 가 잰 것은 **캡처 순간의 한 프레임**이라 애니가 움직인 만큼 벗어난다.
  가로 편심은 CSS 로도 생길 수 없다 — `#dunw .dnc>.th>canvas` 의 변환은 `thBob`(세로 translate + scaleY,
  축 `50% --thpiv`) 뿐이고 **가로 변환이 없다**(실측 확인).
  다만 남는 참점: **잉크 bbox 중심 ≠ 몸통 중심**이다(무기·꼬리가 한쪽으로 뻗은 종). 규칙을 «bbox 중심»
  으로 정한 이상 지금은 정합이지만, 아트가 들어올 때 «시각 중심» 으로 다시 정의할 여지가 있다.
- **#3 액자가 카드 안쪽 우·하단에 0px 로 붙는다** — 10회차가 **알고 남긴 것**이다(§20 «안 고친 것»).
  §3-8 실측 bbox 를 움직여야 하고 `verify72 [1]`·`verify121` «슬롯 불변» 6항목이 동시에 못 박고 있다.

**[탭바] #9 «NEW 배지가 잘린다» — 재현했다. 내 구간이 아니라 새 작업으로 등재한다(309).**
자체 프로브 실측: `.nw` 리본 5개가 전부 탭바 상단(y2100)보다 **4px 위(y2096)** 에 있고,
**첫 번째 리본의 좌단이 x = −5.5** 라 화면 밖으로 나가 잘린다. `#tabbar` 는 `overflow:visible` 이라
위쪽 4px 은 «리본이 걸쳐 있는» 의도된 모양일 수 있으나, **좌단 5.5px 잘림은 명백한 결함**이다.

### AG — ①6 ②5 ③6 ④7 (⑤8 ⑥5) → **5**

**«오차 ≤1px» 로 확인해 준 것**: 카드 5장 top 157/517/877/1237/1597 · 높이 350 · 피치 360 · x 50~1029 ·
자물쇠 106×119(cx 539.5) · 칩 숫자 «2/2» · 불꽃 아이콘 45×52 · 헤더 밴드 · 탭바 높이 180.

**[썸네일] 지적 2건에 대한 판정**

- **#2 장축이 액자를 못 채운다(75~87%)** — **사실이고 아직 안 고쳤다.** AD·AE·AF 와 **같은 것**(4인 일치).
  §26 대로 다음 회차 첫 일감이다.
- **#3 «세로 중심이 일관되게 6.5~9.5px 위 · 5장 모두 같은 방향 = 계통 오프셋»** — 관찰은 정확하고,
  **원인은 배치가 아니라 연출의 위상이다.** `thBob` 은 기준선(웅크림)에서 **위로** 6.5px(짧은 들썩)~11px(큰 점프)
  뜨는 애니이고(0% translate 0 · 10% −6.5 · 90% −11), 캡처는 그 사이 한 위상을 찍는다.
  캔버스 내용 자체는 `verify72 [1-5]` 실측으로 8장 전부 중심 ±0.5px 다.
  → 남는 진짜 물음은 «들썩의 **기준선**을 중심에 둘 것인가, **평균**을 중심에 둘 것인가» 인데,
  기준선 `--thby` 는 **121 소유**이고 121 이 예전에 `--thby:14px` 를 넣었다 되돌린 자리다 — 안 건드렸다.
  (AF #2 «가로 편심» 과 같은 종류의 착시: 한 프레임 vs 사이클 전체.)

**[서브탭]·[헤더]·[카드]·[탭바] 지적**(서브탭 바 높이 +15~18px · 헤더 재화 아이콘 −14~18% · 타이틀 자간 −17~19% ·
칩 아이콘 −19~29% · 탭 라벨 −12%)은 전부 **03·A3·47·A1 구간**이라 손대지 않았다.

### 15회차 결론

| 회차 | 비평가 | ①~④ 최저 |
|---|---|---|
| 11 | AB 6 / AC 4 | **4** |
| 13 | AD 6 / AE 5 | **5** |
| 15 | AF 5 / AG 5 | **5** |

**8 미달 — 라운드는 15/20 이라 회차가 남아 있다.** 최저를 붙들고 있는 것은 ②이고, 그 ②는 두 조각이다:
① **장축 미충전 75~87%**(4인 일치 · CSS 로 회수 가능 · **다음 회차 첫 일감** · §26) ·
② **짧은 축 공백**(`min(AR,1/AR)` 천장 · **보류(아트 대기)** · §25).
①③④ 를 누르고 있는 나머지는 대부분 **03·A3·47·A1 구간**이라 72 가 손댈 자리가 아니다.

## 29. 16회차 — «균일 언더스케일» 을 회수했다 (§26 의 다음 회차 첫 일감)

§26 이 «다음 세션이 이것부터» 로 남긴 것이고, 네 비평가(AD·AE·AF·AG)가 회차를 걸쳐 독립적으로
같은 것을 짚은 유일한 항목이다: **장축이 액자 내부 308 의 82~89% 에서 멈춘다.**

### 먼저 «왜 못 채우는가» 의 이유를 처음으로 **쟀다** (`tools/p72fill.js` 신설)

지금까지 그 여백(`TH_PAD` 사방 16px)은 «121 들썩이 들어갈 자리» 라는 **말**로 지켜져 왔고,
§26 은 그 말을 그대로 받아 «움직임이 아래로만 가므로 위·좌·우 4 / 아래 18 이 상한» 이라고 적었다.
**그 방향이 틀렸다.** 새 프로브(카드 8장 × 사이클 14위상. `translate` 와 `scale` 을 애니에서 직접 읽어
축 `--thpiv`(잉크 발밑) 로 합성해 잉크 rect 를 화면 좌표로 옮긴다)로 재니:

| 잰 것 | 실측 |
|---|---|
| 들썩이 잉크를 **아래로** 미는 위상 | **14/14 중 0개** — `thBob` 의 translate 는 0 → −6.5 → −11 (전부 위) 이고 스쿼시(scaleY .96/.94)는 축이 잉크 발밑이라 **위로 압축**한다 |
| 좌우로 미는 양 | **0px** — `translate` 는 세로 1축 · `scale` 은 `1 sy` 로 가로 성분이 아예 없다 |
| 사이클 최소 «천장여유» | **7.5px** (카드4 zombie/walk — 잉크가 293px 로 가장 커서 스쿼시 몫이 가장 크다) |
| 사이클 «바닥여유» | 17~19px 이 **사이클 내내 그대로** — 한 위상도 쓰지 않는다 |

즉 사방 16px 중 **상단만** 실제로 쓰이고(그것도 16 중 8.5), **하단 16 · 좌우 16×2 = 48px 은 순수한 낭비**였다.

### 고친 것 ① — `TH_PAD`(사방 균일) → `TH_INS`(비대칭)

`const TH_INS = { t: 10, r: 3, b: 3, l: 3 }`.
상단 10 = 16 − 7.5(실측 미사용분) + 1.5(반올림 여유) · 나머지 3 = 림(6px) 안쪽에서 3px.
`drawSpriteTo` 의 `o.fit` 이 **숫자(사방 균일) 또는 {t,r,b,l}** 을 둘 다 받게 넓혔다 —
숫자 경로는 식이 동치라 **한 픽셀도 안 바뀐다**(레이드·아레나 `fit:0`, 97 «꽉 채우기» 그대로).

### 고친 것 ② — 덤으로 AG #3 «세로 중심이 계통적으로 위» 가 같이 닫혔다

15회차에 AG 가 «5장 모두 같은 방향으로 6.5~9.5px 위 = 계통 오프셋» 이라고 했고, 그때 답은
«한 프레임을 찍어서 그렇다, 그린 자리는 ±0.5px» 였다. **위상 평균으로 재 보니 AG 가 옳았다** —
그린 자리가 정확히 가운데면 위로만 가는 들썩이 얹혀 **보이는 평균은 항상 −2.7~−3.4px(위)** 이 된다.
비대칭 인셋은 정지 자세를 (t−b)/2 = **+3.5px** 내려 앉히므로 이 계통 성분이 상쇄된다.

### 실측 — 전/후

| 카드 | atlas/anim | 장축 충전율 (잉크 장축 / 액자 내부 308) | 사이클 평균 중심 (액자 중심 기준) |
|---|---|---|---|
| 1 골드 | dragon/fly | 87.7% → **96.1%** | −3.3 → **+0.2** |
| 2 다이아 | bird/anim | 87.7% → **93.8%** | −2.7 → **+0.8** |
| 3 유물1 | elves/green_idle | 88.3% → **94.5%** | −2.9 → **+0.6** |
| 4 유물2 | zombie/walk | 89.0% → **95.1%** | −2.9 → **+0.6** |
| 5 유물3 | robo/Running | 89.0% → **95.1%** | −2.8 → **+0.7** |
| 6 유물4 | elves/blue_idle | 88.3% → **94.5%** | −2.7 → **+0.8** |
| 7 강화석 | dragon/fly | 82.1% → **89.9%** | −3.4 → **+0.1** |
| 8 룬강화석 | elves/blue_attack | 88.3% → **94.5%** | −2.7 → **+0.8** |

**절단 0장 · 사이클 최소 천장여유 1.5px**(카드4). 1.5px 은 «캔버스 상단» 기준이고, 실제 클립 경계
(`.th{overflow:hidden}` 의 패딩 박스)는 캔버스보다 **6px 더 위**(캔버스가 림 안쪽에 놓여 있다)라
잘림까지의 실여유는 7.5px 이다. 즉 정점에서 그림이 림에 살짝 닿을 수는 있어도 **잘리지는 않는다**.

### 게이트 — 자를 옮겼고, **옮긴 자리를 적었다**

- `verify72` **259 → 267 항목**. `PAD 16` 을 지우지 않고 `INS` 를 더했다(옛 기준값 기록 보존).
  - 사방 최소 여백 → **변별 여백**(L/T/R/B 각각 ≥ 인셋−1)
  - [1-3] 아이들 프레임 여백도 «사방 최소» → «인셋 대비 여유의 최소»
  - **[1-5] 의 자를 바꿨다**: «그려 놓은 잉크 중심 = 캔버스 중심» → «**사이클 위상 평균** 잉크 중심 =
    액자 중심»(사람이 보는 값). 옛 자는 지우지 않고 **[1-5a]**(그린 중심 = **인셋 박스** 중심)로 남겼다 —
    그래야 «인셋을 아무렇게나 줘서 [1-5] 만 맞추는» 길이 막힌다.
- ⚠ **죽은 단언 1개를 잡았다.** 옛 «짧은 축이 액자를 채운다» 는 위에서 `PAD` 로 되짚은 `k` 를 그대로
  다시 써서 `slack == PAD` 가 **항상 참인 항등식**이었다 — 비대칭 인셋을 넣은 뒤에도 «16.0 = 16» 으로
  초록이었다(279 «죽은 되돌림 시험» 과 같은 종류). 이제 **화면에 그려진 잉크**에서 배율을 역산해
  (`ink.w / src.w`) 두 축의 여유를 따로 재고, contain 을 물린 축의 여유가 그 축 인셋 합과 같은지 본다.

### 회귀 — 전부 초록

`VERIFY72 267/267 PASS` · `VERIFY121 149/149 PASS` · `VERIFY104 73/73 PASS` ·
`VERIFY194 54/54 PASS` · `VERIFY203 84/84 PASS` · `INKJIT121 PASS 10/10` · `SMOKE PASS` · 콘솔 에러 0.

캡처 `docs/review/72-r16.png` (1080×2280).

## 30. 16회차 채점 (AH · AI)

캡처 `docs/review/72-r16.png`. 비평가 프롬프트에 «액자는 주인 재지시 · 짧은 축 공백은 아트 대기» 를
명시했고, 이번엔 **«이 회차가 바꾼 것이 실제로 나아졌는지 수치로 판정하라»** 를 과제로 못박았다.

| 회차 | 비평가 | ① | ② | ③ | ④ | (⑤/⑥) | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| 15 | AF / AG | 7 / 6 | 5 / 5 | 6 / 6 | 7 / 7 | (9,5 / 8,5) | **5 / 5** |
| 16 | AH / AI | 8 / 7 | 7 / 6 | 7 / 7 | 8 / 7 | (9,6 / 9,5) | **7 / 6** |

**15회차 5/5 → 16회차 7/6.** ①~④ 가 8개 항목 중 8개 전부 올랐다(+1~+2).

### 두 사람이 «달성» 으로 확인해 준 것 (서로 다른 방법으로 같은 수)

- **ⓐ 장축 충전율** — AH: 86.4~88.7% → **92.9~94.8%**(5장 전부 목표 90~95% 안, +6.1~6.5%p) ·
  AI: 82~89% → **90.9~94.2%**(잉크 장축 +16~17px). 두 사람이 «달성» 으로 일치.
- **ⓑ 세로 계통 오프셋 제거** — AH: 평균 −3.9 → **−0.7px**(3.2px 개선) ·
  AI: 평균 −3.5 → **−0.1px**(«상10/하3 의 +3.5px 하향 보정으로 정확히 상쇄»). 두 사람이 일치.
- **림 침범 0px** — AI 가 4변 전부 실측(카드1 x1012–1017 / y173–178·487–492)으로 «미침범» 확인.
- 액자 자체의 기하는 두 사람 다 **0px**: 카드 5장 y 157/517/877/1237/1597 · 피치 360 · 간격 10 ·
  카드 x 51–1029 · 액자 내부 정확히 308×308(내부 상단 = 카드 상단 +22, x 704–1011, 5장 편차 0px).

### 두 사람이 공통으로 짚은 «이번 회차가 과했다» (17회차의 일감)

1. **최소 여유가 5px 까지 무너졌다** — AH: 카드1 우측 17 → **5px** · 카드3 하단 17 → **5px**(«이전 최소는
   12px») · AI: 카드1 우측 13 → **5px**(«림 침범은 0px 이지만 안전 마진 −8px»). **2인 일치.**
2. **좌우 3px 은 채움률에 기여하지 않는다** — AH ⑤-5 실측: c2~c5 는 **세로 제약**이라 좌우 여유가
   여전히 25~75px 이고(c2 30/30 · c3 28/25 · c4 40/39 · c5 75/71), 좌우 축소분 8~13px 은
   **채움률에 0%p 기여**. 실제 이득은 폭 제약인 dragon 카드뿐이다.
3. **수평 중심 산포가 드러났다** — AI: 평균 **+2.5px 우** · 최대 **+5.5px**(카드4). 16px 균일이 흡수하던
   스프라이트 자체 bbox 비대칭이 3px 에서는 그대로 보인다.
- AH 권고 수치: «상 8 / 하 5 / 좌·우 6». AI 판정: «과하지 않음» 이지만 위 세 가지를 위험으로 적었다.

### 반박 — 감점으로 받지 않은 것

- **AI [썸네일]#3 «카드2 상단 잉크가 padding-top 10px 를 2px 초과(8px)»** — 초과가 아니라 **들썩이다.**
  캡처는 `thBob` 의 한 위상을 찍은 것이고 들썩은 위로 최대 11px 뜬다. 정지 자세는 `p72fill` 실측
  **13px**(선언 10 + 아틀라스 투명 여백)이라 패딩 박스를 넘지 않는다. 15회차 AF #2·AG #3 과 같은 종류
  (한 프레임 vs 사이클)이고, 이번엔 그 «사이클» 쪽을 게이트 [1-5] 가 직접 재고 있다.
- **AH ④-2 «그림 중심이 액자마다 다름 — 산포 8.0px»** — `verify72 [1-5]` 실측으로 그려 놓은 잉크
  합집합 중심은 8장 전부 (0~0.5, ±0.5)px 다. 캡처가 카드마다 **다른 위상**을 찍어서 생기는 산포다
  (카드마다 `--thd` 지연이 다르다 — 일부러 어긋나게 돌린다).

### 내 구간이 아니라 손대지 않은 것 (②③ 을 누르고 있는 본체)

두 사람의 ② 감점 대부분이 **72 구간 밖**이다 — [헤더] 재화 아이콘 −11~23% · [서브탭] 바 높이 +11px ·
활성 탭 높이 −25.8% · [카드] 타이틀 잉크 폭 −17.3% · 칩 텍스트 −19.9% · [탭바] 중앙 X 버튼 높이 −10.7%.
각각 **A3 · 47 · 03 · A1** 구간이다. 13·15회차가 같은 지적을 같은 이유로 넘겼다.
서브탭 상단 +24px(카드 리스트와 −9px 겹침 → +14px 간격)도 **47 구간**이다.

## 31. 17회차 — 두 비평가가 공통으로 짚은 «과함» 을 되돌렸다

`TH_INS` **{t:10, r:3, b:3, l:3} → {t:10, r:6, b:5, l:6}**.
상단 10 은 **안 바꿨다** — 사이클 최소 천장여유가 이미 1.5px(카드4)이라 AH 권고의 «상 8» 로 내리면
정점에서 림에 닿는다(`p72fill` 실측). 좌·우 6 은 AH 권고 그대로, 하단 5 도 AH 권고 그대로다.

| 카드 | 장축 충전율 | 최소 좌우 여유 | 최소 하단 여유 | 사이클 평균 중심 |
|---|---|---|---|---|
| 1 골드 | 96.1% → **94.2%** | 6 → **9** | 4 → **6** | +0.2 → **−0.3** |
| 2 다이아 | 93.8% → **93.2%** | 27 → **32** | 6 → **8** | +0.8 → **−0.2** |
| 3 유물1 | 94.5% → **93.8%** | 25 → **26** | 5 → **7** | +0.6 → **−0.4** |
| 4 유물2 | 95.1% → **94.5%** | 38 → **39** | 4 → **6** | +0.6 → **−0.4** |
| 5 유물3 | 95.1% → **94.5%** | 68 → **69** | 4 → **6** | +0.7 → **−0.3** |
| 6 유물4 | 94.5% → **93.8%** | 39 → **40** | 5 → **7** | +0.8 → **−0.2** |
| 7 강화석 | 89.9% → **88.0%** | 6 → **9** | 4 → **6** | +0.1 → **−0.4** |
| 8 룬강화석 | 94.5% → **93.8%** | 38 → **39** | 5 → **7** | +0.8 → **−0.2** |

**충전율 88.0~94.5%**(16회차 89.9~96.1% 에서 −1.9%p, 15회차 82~89% 대비는 여전히 +6%p) ·
**최소 여유 5 → 9px 회복** · **중심 평균 편차 −0.4~−0.2px**(16회차 +0.1~+0.8 보다 0 에 더 가깝다) ·
절단 0장. 게이트 `INS` 상수도 같이 옮겼다(자와 제품이 갈라지면 게이트가 죽는다).

캡처 `docs/review/72-r17.png` (1080×2280 · 콘솔 0).

## 32. 17회차 채점 (AJ · AK) — 그리고 **72 구간은 수렴했다**

| 회차 | 비평가 | ① | ② | ③ | ④ | (⑤/⑥) | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| 15 | AF / AG | 7 / 6 | 5 / 5 | 6 / 6 | 7 / 7 | (9,5 / 8,5) | **5 / 5** |
| 16 | AH / AI | 8 / 7 | 7 / 6 | 7 / 7 | 8 / 7 | (9,6 / 9,5) | **7 / 6** |
| 17 | AJ / AK | 8 / 7 | 6 / 6 | 7 / 6 | 8 / 7 | (8,5 / 9,7) | **6 / 6** |

### 17회차가 회수한 것 — 두 사람 다 «달성» 으로 확인

- **최소 여유 5px → 8~9px** (AJ: «5 → 9.0px, +4px» · AK: «5 → 8px, 5장 중 4장이 9px 이상 확보,
  좌우 3→6 덕에 카드1 우측이 정확히 9px 회복»). 16회차 2인 공통 지적이 닫혔다.
- 액자 자체 기하는 두 사람 다 **산포 0px**: outer 330×330 · 개구부 308×308 · 림 상단이 전 카드
  «카드 top + 16px» 고정 · 카드 5장 bbox x[50,1029] w980 h350 pitch 360 전부 0~1px.

### 대가 — 알고 받았다

장축 충전율이 −0.6~−1.8%p 내려갔다(AJ·AK 실측 일치). 가로장축(dragon) 카드1 이 −5.5px 로 가장 크고
세로장축은 −1.9px(육안 식별 한계). AJ 의 판정이 정확하다: **«좌·우를 6→5 로 1px 만 되돌리면 충전율
88.0→88.4%, 최소 여유 9→8px — ⓐ 와 ⓑ 가 서로를 잡아먹는 구간이다. 현 조합이 두 목표의 실질 경계선.»**
그래서 더 밀지 않았다.

### 반박 — 감점으로 받지 않은 것 (세 회차째 같은 착시)

**AJ ⓒ «미달성 — 중심이 +2.5px 아래» · AK ④-1 «계통 −2.2px»** — 두 사람이 **서로 반대 방향**을 말한다.
AJ 는 «그린 박스 중심» 을(+2.5, 아래로), AK 는 «캡처 잉크 중심» 을(−2.2, 위로) 쟀다. 둘 다 맞고,
**그 둘의 차이가 정확히 들썩이다.** 사람이 보는 값은 셋 중 어느 쪽도 아니고 **사이클 평균**이며,
`p72fill`·`verify72 [1-5]` 실측으로 **−0.2~−0.4px** 다. 캡처는 카드마다 다른 위상(`--thd` 지연이
일부러 다르다)을 한 장 찍은 것이라 5장 샘플로는 평균이 −2 쪽으로 기운다.
15회차 AF #2 · AG #3, 16회차 AI #3 과 **같은 종류**이고, 그래서 16회차에 게이트의 자를 «그린 자리» 에서
«사이클 평균 자리» 로 옮겨 두었다. AJ 의 처방(«상·하를 7/7 로»)을 받으면 상단 여유가 3px 줄어
사이클 최소 천장여유 1.5px 이 **음수가 된다** — 정점에서 림에 닿는다. 받지 않는다.

### ★ 결론 — **①~④ 를 6 에 묶고 있는 것은 72 구간이 아니다**

AJ·AK 의 ②③ 감점 8건 중 **72 구간은 1건**(카드3 충전율)뿐이고 나머지 7건이 전부 밖이다.
AK 가 이번에 그것을 명시적으로 적었다: «**서브탭 블록 +34px 하향 및 높이 +20px — 16→17회차 사이
개선 흔적 없음. ③·②의 최대 실점 원인.**»

| 남은 감점 | 실측 | 구간 |
|---|---|---|
| 서브탭 바 +34px 하향 · 높이 79 → 99px(+25%) · 활성 탭 높이 −25.8% | AJ·AK·AH·AI 4인 일치 | **47** |
| 카드5 하단 ↔ 서브탭 간격 0 → 14px | 4인 일치 | **47** |
| 헤더 재화 아이콘 −9px(−14~20%) | 4인 일치 | **A3** |
| 타이틀·라벨 잉크 폭 −17~24% (글리프 advance −21%) | 4인 일치 | **03 / 서체(웹폰트 미도입)** |
| 서브탭 축 −8.5px 좌 (레퍼런스가 화면중심 +8.5 비대칭) | AJ·AK 일치 | **47** |

**72 가 CSS 로 더 밀어서 회수할 수 있는 것은 남아 있지 않다.** 세 회차(15·16·17)에 걸쳐 여섯 비평가가
72 구간에 준 지적은 «장축 미충전»(회수 완료) · «세로 중심 계통 오프셋»(회수 완료) · «짧은 축 공백»
(아트 대기, §25) 셋뿐이고, 앞의 둘이 이번에 닫혔다.

**2차 라운드 17/20 — 3회차 남음.** 다음 세션이 72 를 다시 잡는다면 **남은 3회차를 여기 쓰지 말 것**:
①~④ 최저를 8 로 올리려면 **47(서브탭)·A3(헤더 아이콘)** 가 먼저 움직여야 한다. 그 둘이 8점이 된 뒤에
72 를 한 회차만 재채점하면 된다(제품 변경 0줄로 점수가 오를 자리다).

## 33. 18회차 — 비평가에게 **«앵커가 둘» 표를 주고** 다시 물었다 (제품 0줄)

17회차(§32)가 «①~④ 를 6 에 묶고 있는 것은 72 구간이 아니다» 로 닫았고, 그 직후 **335**(03 서브탭 블록)이
그 판정의 절반을 뒤집었다: 15~17회차 비평가 넷이 일치 지적한 다섯 줄 중 **넷이 «유령»** 이었고,
뿌리는 이 화면에 **앵커가 둘**이라는 것이다(`docs/measure/03-던전팝업.md` §4 정오표).

ROUTINE [3] 은 비평가에게 «세로는 ref y − 84» 만 알려 주게 돼 있는데, 이 화면의 **서브탭 바·앱 탭바는
하단 앵커(`ref y − 60`)** 라 자동으로 **+24px 어긋나 보인다.** 그래서 18회차는 **제품을 한 줄도 안 고치고**
비평가 AP·AQ 에게 두 앵커 표를 프롬프트로 주고 재채점만 했다.

### 먼저 내 자로 쟀다 (`node tools/m335.js`)

| 항목 | 자 | 현재 | ref | Δ |
|---|---|---|---|---|
| 서브탭 바 상변 | −60 | 1961 | 1961 | **0** |
| 서브탭 바 높이 | — | 99 | 98 | **+1** |
| 활성 알약 높이(바 안쪽) | — | 85 | 85 | **0** |
| 바 중심 x | — | 548 | 547.5 | **+0.5** |
| 앱 탭바 상변 | −60 | 2100 | 2100 | **0** |
| 카드1 상변 | −84 | 157 | 157 | **0** |
| 카드5 하변 | −84 | 1947 | 1946 | **+1** |
| 이음매(카드 하변 ↔ 바 상변) | — | 14 | 0 | +14 = **구조적 24px 몫** |

### 캡처 · 기준선

캡처 `docs/review/72-r18.png`(1080×2280 · 콘솔 0) — **335 의 `.dns-sub{left:143→151}` 이후 첫 재캡처**다.
기준선 `VERIFY72` **267/267** · `VERIFY335` 24/24 · `VERIFY96` 64/64 · `VERIFY121` 149/149 · **SMOKE PASS**.

## 34. 18회차 채점 (AP · AQ) — **유령 셋은 사라졌고, 점수는 그대로 6 이다**

| 회차 | 비평가 | ① | ② | ③ | ④ | (⑤/⑥) | ①~④ 최저 |
|---|---|---|---|---|---|---|---|
| 16 | AH / AI | 8 / 7 | 7 / 6 | 7 / 7 | 8 / 7 | (9,6 / 9,5) | **7 / 6** |
| 17 | AJ / AK | 8 / 7 | 6 / 6 | 7 / 6 | 8 / 7 | (8,5 / 9,7) | **6 / 6** |
| 18 | AP / AQ | 7 / 8 | 6 / 6 | 7 / 6 | 8 / 7 | (9,5 / 9,5) | **6 / 6** |

### ★ 앵커 표는 **효과가 있었다** — 두 사람이 독립으로 유령 셋을 철회했다

15~17회차에 넷이 일치 지적했던 셋을, 18회차 두 사람이 **`−60` 으로 재실측하고 명시적으로 기각**했다:

| 유령 | 15~17회차 지적 | 18회차 AP 재실측 | 18회차 AQ 재실측 |
|---|---|---|---|
| 서브탭 바 «+34px 하향» | 4인 일치 | ref 2021 → cap 1961, **Δ0** | 1961 = 2021−60, **Δ0** |
| 바 높이 «79 → 99(+25%)» | 4인 일치 | 98 → 99, **Δ+1(+1.0%)** | 99 vs 98, **Δ1** |
| 앱 탭바 | — | 상변 Δ1 · 하변 **Δ0** | 상변 **Δ0** |

AQ 는 «**지적 철회**» 라고 적었다. AP 는 이음매 +22px 을 «규정된 24px 프레임 여유 몫이라 감점 안 함» 으로
분류했다. **335 의 정오표가 세 회차를 태우던 오독을 실제로 닫았다.**

### 그런데 점수는 6 그대로다 — 유령이 걷히자 **밑에 깔려 있던 실물**이 드러났다

72 **구간 밖**이 여전히 ①②③ 을 누른다. 그리고 이번엔 **두 사람이 새로 일치**한 것이 둘 있다:

| 남은 감점 | AP 실측 | AQ 실측 | 구간 |
|---|---|---|---|
| **카드 «입장 가능» 레드닷 전량 누락** | ref 카드1·2 에 26×31 dot @x1005..1032 · 캡처 8장 전부 **0개** | ref 28×31 @동일 bbox · 캡처 **0개** | **신규 341** |
| **카드 타이틀 블록이 −5~6px 위** → 타이틀↔칩 간격 **+29.6%** | +5px 내려라 | +6px 내려라(8·9 동시 해소) | **신규 342** |
| 칩 내부 우측 여백 +36~67% | 50 → 68px | 52 → 87px | **신규 342** |
| 서브탭 활성 라벨 잉크 −14~20.6% | ② 지적 | ②-6 · ④-13 | **337**(진행 중) |
| HUD 재화 아이콘 −16~21.8% | ⑥ | ②-3 | **340**(완료 — 내 캡처 이후) |
| 카드 내부 한글 라벨 잉크 −20~23% | ⑥ | ⑥-16 | **서체(웹폰트 미도입)** |

⚑ **내 캡처 r18 은 340 의 수정 이전 상태다.** 340(`.pcb-g>i`·`.pcb-d>i` 재화 아이콘 잉크)이 같은 시각
워커 D 세션에서 완료됐으므로, AQ ②-3 «HUD 아이콘 −20%» 는 **이미 회수됐을 가능성이 높다** —
다음 회차 재캡처가 확인할 몫이다.

### 72 **구간 자체**에서 나온 것은 둘뿐이고, 둘 다 이미 알려진 것이다

1. **액자 수직 여백 2px 비대칭** (AP «상6/하1» · AQ «상4/하1, 중심 +1.5px») — **DOM 실측은 8장 전부
   `330×330 @ x693 · 여백 상11/하9/우7 · 산포 0px`** 다(`node tools/probe72fr.js` 신설).
   두 사람이 «337×327 · 상6/하1/우0» 으로 읽은 것은 **카드 자신의 검은 테두리를 액자 외곽선과 함께 센**
   그린-픽셀 값이다. 실재하는 것은 **2px**(11/9)이고, 뿌리는 `.dnc>.th{top:var(--tht)}` 의 `--tht:11` 과
   높이식 `calc(341px - var(--tht))` 에 박힌 **341** 이다(11+330=341, 350−341=9).
   대칭으로 만들려면 `--tht:10` + `340` 두 곳인데, **캔버스 높이식(`341 - tht - TH_INSET`) · `verify72 [1]` ·
   `verify121` «슬롯 불변» 6항목**이 같이 움직여야 한다 — **8회차가 ⓔ 로 «알고 남긴» 바로 그 자리**다.
   2px 로 ④ 가 7→10 이 되지도 않고 ①②③ 은 전부 구간 밖이라, 이번 회차에도 **안 건드렸다.**
2. **카드1·7 채움률 37.6~88%** (AP ②) — `dragon` AR 2.10 의 **수학적 상한 47.6%**(§25). 아트 대기 확정.

### ★ 결론 — §32 의 지시는 유효하다. **남은 2회차를 여기 쓰지 마라**

18회차가 증명한 것은 두 가지다. ⓐ **앵커 표를 주면 유령은 사라진다**(다음 세션도 반드시 줄 것).
ⓑ 그럼에도 **점수를 6 에 묶는 것은 전부 72 구간 밖**이다 — 이번엔 두 사람이 그것을 각자
«[지적] 남은 것» 의 거의 전부로 적었고, 72 구간 항목은 AP 8건 중 2건 · AQ 14건 중 1건뿐이다.

**2차 라운드 18/20 — 2회차 남음.** 다음 회차는 **337 이 끝난 뒤 재캡처 + 재채점**이다(제품 0줄로 점수가
오를 자리). 340 은 이미 끝났으므로 **337 하나만 기다리면 된다.** 그 전에 재채점하면 회차만 태운다.
