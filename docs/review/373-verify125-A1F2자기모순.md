# 373 — `tools/verify125.js` 의 A1 과 F2 가 같은 🎫 를 반대로 말한다

- 잡은 세션: `sess-2333-6173` (워커 B · 2026-08-28) · 1회차
- 티어: **T1 «버그(게이트 자기모순)»**
- 결론: **게이트 — 제품 `index.html` 0줄.** 레이아웃 Δ0px · 판정·밸런스 0줄.
- 결과: `verify125` **33/33 → 37/37**(F2 판정 교체 + F2b·§R6~§R8 신설) · `probe373` **13/13 신설** · **SMOKE PASS**

---

## 1. 재현 (338 규칙 — 처방을 따르기 전에 먼저 찍는다)

착수 시점 `node tools/verify125.js` = **PASS 33/33**. 즉 «지금 빨간 항» 은 없다 —
등재문이 말한 것은 «F2 가 **초록인 이유가 틀렸다**» 이므로, 재현이 할 일은
**그 초록이 무엇에 기대고 있는지**를 값으로 찍는 것이다. `tools/probe373.js` 신설(`PROBE373 PASS 13/13`).

| # | 잰 것 | 찍힌 값 |
|---|---|---|
| [1] | F2 스윕이 ▦ 메뉴를 여는가 | 20화면 중 **0** · `shut()` 에 `closeMenu` **없음** · 같은 자리를 A1 은 `data-mn="pass"` 로 **면제** |
| [2] | 메뉴를 열면 F2 는 어떻게 되는가 | 닫힘 0건 → 열림 **🎫 1건** · 호스트 `i.mn-i` = **94.8×48.35** (0×0 이 아니라 **보이는 채** 샌다) |
| [3] | 그 자리가 아트 자리인가 | ▦ 메뉴 **7칸 전부** 같은 부품 `i.mn-i` 의 이모지 아트(✉️🏆🗺️🎒🔋⚙️🎫) · 이미지 칸 **0** |
| [4] | 스윕에 메뉴를 더하면 곁다리로 무엇이 빨개지나 | F1 마크업 유출 **false** · F3 NaN/undefined **false** · `#mnw` 안 `img.cic` **0개**(E1 무관) |
| [5] | 등재문 ⓐ(«PURE 를 🪙💰 로 좁힌다»)의 대가 | 「던전 입장권을 이모지로 되돌린 화면」을 좁힌 목록은 **0건 = 놓친다** · 제안 판정식은 **1건 잡는다** |
| [6] | 제안 판정식이 무른가 | 자리 그대로 🪙 → **빨강 1건** · 글리프 그대로 라벨 칸 → **빨강 1건** · 원복 → **초록(면제 1)** |

⇒ 등재문의 진단이 **그대로 맞았다**. F2 가 초록인 것은 판정이 A1 과 일치해서가 아니라
**스윕 20화면이 ▦ 메뉴를 한 번도 안 열기 때문**이다.

## 2. 판정 — 게이트 쪽이고, 두 처방을 **반씩** 골랐다

등재문은 ⓐ(F2 목록이 과하다) / ⓑ(스윕이 좁다 + 제품 글리프를 전용 아트로) 중 하나를 고르라고 했다.
찍힌 값이 **ⓑ 의 앞쪽(스윕)** 과 **ⓐ 의 뜻(아트 자리는 면제)** 을 갈라 놓았다.

1. **스윕은 실제로 좁다(ⓑ 앞쪽 채택).** [1]·[2] — 보는 자가 그 화면을 본 적이 없어서 초록인 게이트는
   «초록» 이라는 말을 할 자격이 없다. ⇒ `steps` 에 **▦ 메뉴** 를 넣고 `shut()` 에 `closeMenu` 를 더했다.
   (⚠ 닫는 손이 없으면 그 뒤 화면이 전부 메뉴를 얹은 채 찍혀 판정이 통째로 흐려진다.)
2. **제품은 안 고친다(ⓑ 뒤쪽 기각).** [3] — 패스 칸만 «전용 아트» 로 바꾸면 **형제 6칸과 규격이 갈린다.**
   `docs/measure/52-메뉴팝업.md` §6-3 «아트 필요» 표는 그 7칸을 **통째로 아트 대기**로 등재해 뒀고
   (패스 행까지 «아트 교체 시 상단 offset 18» 까지 적혀 있다), 아트는 프로젝트 전체가 기다리는 자리다.
   한 칸만 먼저 빼는 것은 이 작업 단위가 할 판단이 아니다.
3. **목록은 안 좁힌다(ⓐ 글자대로는 기각).** [5] — `PURE` 에서 🎟🎫 를 빼면 F2 는
   「던전 입장권을 이모지로 표시」를 **런타임에서 통째로 놓친다**. 125 가 이미지로 통일한 바로 그 자리다.
4. **대신 ⓐ 의 «뜻» 을 구조로 옮겼다.** A1 은 소스에서 «아트 자리»(`ic:`/`art:` 한 글리프)를
   **이름이 아니라 구조**로 면제한다(289 판정). F2 도 같은 층위로 내려가면 된다 —
   판정을 «화면 텍스트 한 덩어리» 가 아니라 **글리프를 담은 노드**로 옮기고,
   **아트 자리 노드만** 면제한다. 그러면 A1 과 F2 가 같은 🎫 를 **같은 이유로** 같게 말한다.

## 3. 처방 — `tools/verify125.js` 한 파일 (제품 0줄)

### 3-1. 스윕이 그 화면을 실제로 본다
- `steps` 에 `['▦ 메뉴', () => openMenu()]` 추가 → **E0 20 → 21화면**(개수는 이제 `steps.length` 에서 읽는다).
- `shut()` 에 `closeMenu` 추가.

### 3-2. F2 판정을 «노드» 로 옮겼다
```js
const ART_NODE = [
  { sel: '#mnw .mn-b[data-mn="pass"] > i.mn-i', g: '🎫', why: '52 ▦ 메뉴 «패스» 칸 — A1 이 이미 면제한 아트 자리' },
];
```
- 재는 대상은 **373 이전과 같다** — `body.innerText`. 달라진 것은 «있으면 빨강» 이 아니라
  그 글리프를 담은 **보이는 호스트 노드**를 찾아 아트 자리인지 가르는 것뿐이다.
- 면제는 **셋이 모두 맞을 때만** 듣는다 — ① 그 자리(선택자) ② 그 글리프 ③ 노드 텍스트가 **그 글리프 하나뿐**.
- 0×0 · `visibility:hidden` · `opacity:0` 은 «화면 텍스트» 가 아니므로 세지 않는다(닫힌 오버레이).
- **`innerText` 에는 있는데 보이는 호스트를 못 찾은 글리프는 «유출» 로 센다** — 판정식이 못 읽는 자리는
  면제가 아니라 실패다(무르게 풀지 않는다).
- 판정 본체는 페이지에 **한 번만** 심고(`window.__f2scan`) 스윕과 §R6~§R8 이 **같은 함수**를 부른다(334 교훈).

### 3-3. 무르게 풀지 않았음을 세 겹으로 못박았다

| 항 | 세우는 것 | 찍힌 값 |
|---|---|---|
| **F2b** | 면제 항목이 **아무것도 안 잡는 채 굳지 않는다**(A3 과 같은 자) | `#mnw .mn-b[data-mn="pass"] > i.mn-i` **×1** |
| **§R6** | 면제는 **글리프까지** 못 박는다 — 같은 자리를 🪙 로 바꾸면 빨강 | `🪙@i.mn-i[95×48]` 1건 |
| **§R7** | 면제는 **그 자리에만** 듣는다 — 같은 🎫 라도 라벨 칸이면 빨강 | `🎫@i.mn-l[169×28]` 1건 |
| **§R8** | 음성 대조 — 원복하면 초록(시험이 «항상 빨강» 이 아니다) | 유출 0 · 면제 1 |

**허용 오차는 한 칸도 안 넓혔다** — PURE 4종 그대로, 스윕은 **넓어졌고**(20→21), 면제는 노드 하나뿐이다.

## 4. 회귀

- `verify125` **37/37 PASS**(33/33 → +4: F2b·§R6·§R7·§R8. F2·E0 은 판정·개수만 갈렸다)
- `probe373` **13/13 신설**
- **SMOKE PASS**
- `index.html` **0줄** — `git diff --stat` 이 `tools/verify125.js` 한 파일뿐임을 못박는다.
  제품이 안 바뀌었으므로 화면 게이트 회귀 대상이 없다(A1·A2·A3·§R1~§R5·B·C·D·E·G·H·J·I 전부 무수정 통과).

## 5. 곁다리 관측

**등재하지 않았다.** 등재문이 경고한 «스윕에 ▦ 메뉴를 더하면 F1·F3·E1 이 그 화면을 처음 본다» 는
`probe373` [4] 와 실제 게이트 실행 둘 다에서 **0건**이었다(F1 0건 · F3 0건 · `#mnw` 안 `img.cic` 0개).
338·344 규칙대로 «대조로 확인 못 한 관측» 은 남기지 않는다.
