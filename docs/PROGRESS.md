# 작업 진행 상황

> **새 세션은 이 파일부터 읽는다.** 컨텍스트가 초기화돼도 여기와 `UI-REFERENCE.md` 체크리스트,
> `git log` 만 보면 이어서 작업할 수 있어야 한다. 매 단계 끝날 때마다 이 파일을 갱신하고 커밋할 것.

## 현재 단계

- [ ] A. 레이아웃 — **프레임/화면비/안전영역 완료**, 탭바 5개·모달화·원형 슬롯·사이드 아이콘·HUD 남음
- [ ] C. 화면 재구성
- [ ] B. 훈련 시스템
- [ ] 밸런스 재측정

**지금 작업 중**: 병렬 진행. `docs/claims/` 에서 미선점 작업을 잡는다 (A1 → A5 → 01 → 23 순)

> **01 에서 얻은 교훈 2개 (다른 화면에서도 그대로 재발한다)**
> - **측정표의 `font-size (추정)` 을 그대로 쓰면 안 된다.** 측정표는 `잉크높이 ÷ 0.75` 로 추정하는데
>   이 프로젝트 폰트의 실제 잉크비는 **0.93** 이다. 그대로 쓰면 글자가 15~25% 커지고, 폭을 맞추려
>   음수 `letter-spacing` 을 넣으면 **어절 공백과 소수점이 통째로 사라진다**(`1.5배 받기`→`15배받기`).
>   → `font-size = 실측 잉크높이 / 0.93`, 폭은 글자를 감싼 `<i>` 의 **`transform:scaleX()`** 로 맞출 것.
> - **`background-clip:text` 와 `-webkit-text-stroke` 는 같이 못 쓴다**(스트로크가 그라데이션을 덮어
>   글자가 통째로 검정이 된다). 외곽선 레이어를 따로 깔고, 그라데이션은 **글자를 직접 담은 요소**에 걸 것.

### A 진행 상황 (2026-08-23)

- [x] 앱 프레임 `1080×1920` (9:16), 폭 고정 · 세로 가변(`clamp 1600~2600`)
- [x] 안전영역(`env(safe-area-inset-*)`) + `visualViewport` 대응
- [x] 전투 캔버스가 남는 높이 전부 흡수 (`#stagearea{flex:1}` + `ResizeObserver`)
- [x] **하단 탭바 5개 · 패널 토글(✕)** — A1 완료 (2026-08-23, 비평 6/10)
- [ ] 모달화 · 원형 스킬 슬롯 · 좌6/우3 사이드 아이콘 · HUD 재구성

#### A1 비고 (통과 기준 9 미달 — 남은 문제)

1. **아이콘이 이모지다.** 레퍼런스는 칸마다 폭이 다른 일러스트(영웅121·성장150·모험109·보물상자125·상점165)라
   칸별 font-size 로 근사했지만, 이모지는 가로세로가 같이 커져 **가로로 넓은 아트(상점 165×115)를 재현할 수 없다.**
   → 아트 리소스가 들어와야 ② 상대 크기 비율이 9점에 도달한다.
2. **라벨 2글자 폭이 +9%** (61 vs 56). 4글자('보물상자')는 120 vs 121 로 일치해서, 자간을 더 줄이면 4글자가 어긋난다.
   레퍼런스의 **좁고 높은 게임용 한글 서체**를 웹폰트로 넣기 전에는 트레이드오프다.
3. 탭바 **바로 위 보라색 튜토리얼 배너**(`#tuto` 의 `border:3px solid #6d54ad`)는 레퍼런스에 없다.
   **A1 구간이 아니라 손대지 않았다** — 별도 작업 단위에서 처리할 것.
4. NEW 리본 기울기는 측정(−22°)과 비평가 실측(−8° / −20.6°)이 엇갈려 **−20°** 로 정했다.

> 참고 — 이번에 잡은 CSS 함정 2개 (다른 화면에서도 그대로 재발한다):
> - **글리프 advance 보다 좁은 박스**에 `text-align:center` 를 쓰면 넘친 글리프가 가운데가 아니라 **좌측 정렬**돼
>   잉크가 우측으로 밀린다. 아이콘 박스는 advance 보다 넓게 잡을 것.
> - `*{box-sizing:border-box}` 리셋은 **의사요소(::before/::after)를 잡지 못한다.** 테두리를 준 의사요소는
>   `box-sizing` 을 직접 써 주지 않으면 테두리 두께만큼 커지고 중심이 밀린다.

**주의 — 이전 세션의 `#legacy{transform:scale(2)}` 스캐폴드 방식은 저장소 주인이 revert 했다.**
(래퍼를 통째로 scale(2) 하는 방식은 프레임 아래쪽에 검은 띠가 남는다)
이번에는 스캐폴드 없이 **CSS px 값을 실제로 2배**하고 **캔버스가 남는 높이를 흡수**하도록 했다. 되돌리지 말 것.

## 병렬 세션 규칙 (2026-08-24 도입)

여러 세션이 **동시에** 돈다. 전부 `index.html` 한 파일이므로 **작업 단위로 선점**해서 나눈다.

1. `docs/claims/<ID>.lock` 이 없는 **가장 앞 순서의 미완료 작업**을 잡는다 (아래 «작업 단위» 표 순서대로).
2. `docs/claims/<ID>.lock` 에 `<UTC시각> <세션식별자>` 한 줄을 쓰고 **커밋 + push**.
   - push 가 거부되면 `git pull --rebase` 후 다시 본다. 내 lock 이 아닌 남의 lock 이 생겼으면 **그 작업은 포기하고 다음 것**을 잡는다.
3. 잡은 작업의 코드 구간만 건드린다. **다른 작업 구간은 절대 손대지 않는다.**
4. 끝나거나 중단하면 성공·실패 무관하게 lock 을 **삭제**하고 push.
5. **90분 넘은 lock 은 죽은 것으로 보고 뺏는다.**
6. 비평 기록은 `docs/review/<ID>-이름.md` 에 **자기 파일로만** 쓴다 (공용 `review-log.md` 에 추가 금지).
7. push 전 항상 `git pull --rebase origin main`. `index.html` 이 충돌하면 **양쪽 변경을 모두 살린다**
   (서로 다른 구간이라 대부분 자동 병합됨). 병합 후 반드시 문법 검사 + 헤드리스 실행으로 확인.
8. `PROGRESS.md` 는 유일한 공용 파일이다. 충돌하면 **양쪽 행을 모두 살린다.**

### 작업 단위 (이 순서대로 잡는다)

| ID | 작업 | 주 편집 구간 | 상태 |
|---|---|---|---|
| A1 | 하단 탭바 6개 → 5개(영웅·성장·모험·보물상자·상점) + 탭 재클릭 시 패널 닫힘 + 열린 탭 자리를 ✕ 로 교체 | `#tabbar`, `#panel`, `setTab()` | **완료 (6/10, 5/5회차)** — 기능 완료, 아트 한계로 9 미달. 측정표 `docs/measure/A1-탭바.md` · 기록 `docs/review/A1-탭바.md` |
| A2 | 좌6/우3 사이드 아이콘 — 아이콘 높이·gap 을 캔버스 높이에 비례시켜 9:16(캔버스 861px)에 맞춤 | `#sideL`, `#sideR` | |
| A3 | 상단 HUD 레퍼런스대로 재구성 | `#top` | **완료 (6/10, 5/5회차)** — 초상화 플레이트(사선 곡선·꼬리 2단 톤) + 닉네임 알약 + 칭호 육각 배너 + 🔥전투력 / 재화 **2종**(골드·젬, 알약 211×48, 아이콘이 좌측 모서리에 물림). 측정표 `docs/measure/A3-상단HUD.md` · 비평 `docs/review/A3-상단HUD.md`. **레이아웃은 9점 도달**(배치9·여백9·정렬9·폰트9), 최종을 6에 묶은 건 **색·명암 단일 항목**이며 원인은 이모지 대체(초상화·코인·젬·🔥) — **실제 아트 리소스 없이는 회수 불가**. ⚠️ **A2 와 겹침**: 플레이트가 바 아래 **34px 돌출**(레퍼런스대로)인데 A2 의 `SIDE.TOP` 이 9:16 에서 좌측 첫 아이콘을 `y=125` 에 놓아 **약 16px 겹침** — 레퍼런스는 아이콘이 플레이트 아래(로컬 166)에서 시작하므로 **`SIDE.TOP` 을 올리는 게 맞다**(A2 구간이라 미수정) |
| A4 | 스킬 슬롯 원형화 | `#slots` | **완료 (7/10, 5/5회차)** — 지름120·pitch130·gap10·링 4겹(4-8-4)·well88, 활성/대기/잠금 3상태 conic 그라디언트, 하단 Ø40 뱃지. 측정표 `docs/measure/A4-스킬슬롯.md` · 비평 `docs/review/A4-스킬슬롯.md`. **남은 것**: 자물쇠 열쇠구멍·shackle 비율(🔒 이모지 글리프 한계 — 실제 아트 넣으면 해소), 원주 12% 구간 Δ6~20(JPEG 경계 노이즈 바닥) |
| A5 | 팝업 모달화(레퍼런스 규격) | `.modal`, `.mbox` | |
| 01~23 | 화면별 재구성 — 아래 «화면별 상태» 표 순서 | 각 화면 렌더 함수 | 표 참고 |

## 화면별 상태

| # | 화면 | 측정표 | 구현 | 최고 점수 | 루프 횟수 | 비고 |
|---|---|---|---|---|---|---|
| 1 | 오프라인보상 팝업 | `docs/measure/01-오프라인보상팝업.md` | ✅ | 5 | 4/5 | 전용 오버레이 `#offw` 신설(모달 바깥 상품 배너 때문에 공용 `#modal` 불가). 박스·좌표·폰트 전부 헤드리스 실측으로 측정표와 1px 이내 일치. 남은 감점은 **배너 금화 3개 부재 + 이모지 대체 + 게임 서체 부재** 로 수렴 — 아트 리소스 필요. 기록 `docs/review/01-오프라인보상팝업.md` |
| 2 | 기본 메인 화면 | – | – | – | 0/5 | |
| 3 | 던전 팝업 | – | – | – | 0/5 | |
| 4 | 던전 세부 팝업 | – | – | – | 0/5 | |
| 5 | 무기 팝업 | – | – | – | 0/5 | |
| 6 | 장비 팝업 | – | – | – | 0/5 | 캐릭터 2명 구조 미결정 |
| 7 | 스킬 팝업 | – | – | – | 0/5 | |
| 8 | 스킬 세부 팝업 | – | – | – | 0/5 | |
| 9 | 일괄 강화 결과 팝업 | – | – | – | 0/5 | |
| 10 | 상점 팝업 — 소환 탭 | – | – | – | 0/5 | |
| 11 | 소환 부분 정보 팝업 | – | – | – | 0/5 | |
| 12 | 소환 결과 팝업 | – | – | – | 0/5 | |
| 13 | 상점 팝업 (재화 탭) | – | – | – | 0/5 | |
| 14 | 유물 팝업 — 보물상자 탭 | – | – | – | 0/5 | |
| 15 | 유물 팝업 — 유물 탭 | – | – | – | 0/5 | |
| 16 | 유물 세부 팝업 | – | – | – | 0/5 | |
| 17 | 스탯업 보너스 팝업 연출 | – | – | – | 0/5 | |
| 18 | 패배 화면 | – | – | – | 0/5 | |
| 19 | 프로필 팝업 | – | – | – | 0/5 | |
| 20 | 프로필 팝업 — 스펙 정보 | – | – | – | 0/5 | |
| 21 | 도감 보너스 팝업 | – | – | – | 0/5 | |
| 22 | 퀘스트 팝업 | – | – | – | 0/5 | |
| 23 | 훈련 팝업 | – | – | – | 0/5 | |

## 결정 사항 / 미결 사항

- **확정(2026-08-24 변경)**: 기준 해상도 **1080×1920 (9:16)**, 폭 고정·세로 가변, 안전영역은 프레임 바깥 흡수
  (레퍼런스 캡처는 1080×2340 이지만 폭이 같으므로 **가로 수치는 1:1**, 세로 차이 420px 은 전투 캔버스가 흡수.
   전체를 1920/2340 으로 축소하지 말 것)
- **확정**: 전투 캔버스는 **월드 상수를 2배로 고치지 않고** `ctx.setTransform(2,0,0,2,0,0)` 로 확대한다
  (논리 좌표계는 계속 540 기준. `VW=540`, `VH = stagearea.clientHeight / 2`. 밸런스 영향 없음)
- **주의**: 기준이 9:16 이 되면서 사이드 아이콘 좌6 은 **캔버스 861px 안에 안 들어감**(756 필요, 여유 부족) — 아이콘 높이·gap 을 캔버스 높이에 비례시킬 것
- **미확인**: 노치 실기기에서 안전영역 동작 (헤드리스는 `env()` 가 항상 0)
- **미결**: 캐릭터 2명(햄지·매옹이) 구조로 갈지 1명 유지할지 — 6·20번 화면에 영향
- **미결**: 색·폰트(⑤⑥) 채점 포함 여부 — 아트 리소스 한계로 9점이 안 나오면 제외 검토

## 다음 세션이 할 일

1. 이 파일과 `UI-REFERENCE.md` 24번 체크리스트를 읽는다
2. **«병렬 세션 규칙»** 대로 `docs/claims/` 를 보고 미선점 작업 하나를 잡는다 (A1 → A5 → 01 → 23 순)
3. 측정표는 `docs/measure/NN-이름.md` 에 있으면 재측정하지 말고 그대로 쓴다
4. 끝나면 이 파일 + 체크리스트 + `docs/review/<ID>-*.md` 를 갱신하고 lock 삭제 후 커밋 · push
