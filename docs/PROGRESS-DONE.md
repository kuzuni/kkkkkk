# 완료 작업 상세 기록 (PROGRESS.md 아카이브)

> `docs/PROGRESS.md` 의 «작업 단위»·«화면별 상태» 표에서 **완료(✅) 행의 전문**을 옮겨 둔 곳이다(2026-08-25).
> PROGRESS.md 에는 한 줄 요약만 남긴다. 완료 화면을 다시 잡을 때(T4 2차 폴리시·아트 투입 후 재검)만 여기를 본다.
> 새로 완료되는 행도 같은 방식으로: PROGRESS 에 한 줄 요약 + 여기에 전문 추가.

## 작업 단위 A1~A5

| ID | 작업 | 주 편집 구간 | 상태 |
|---|---|---|---|
| A1 | 하단 탭바 6개 → 5개(영웅·성장·모험·보물상자·상점) + 탭 재클릭 시 패널 닫힘 + 열린 탭 자리를 ✕ 로 교체 | `#tabbar`, `#panel`, `setTab()` | **완료 (6/10, 5/5회차)** — 기능 완료, 아트 한계로 9 미달. 측정표 `docs/measure/A1-탭바.md` · 기록 `docs/review/A1-탭바.md` |
| A2 | 좌6/우3 사이드 아이콘 — 좌우 공통 행 그리드 + 아이콘 잉크를 레퍼런스 스프라이트에 정렬 | `#sideL`, `#sideR` | **완료 (5/10, 3회차)** — ①배치 7~9 ②크기 6~7 ③여백 7~8 ④정렬 7~8 ⑥폰트 8~9 인데 **⑤ 색·명암 단일 항목이 5** 로 최종을 묶는다. 3회차에 **두 비평가 모두 ⑤ 를 «아트 리소스 없이는 회수 불가»로 분류**했다(이모지 대체). A1(6)·A3(6)·A4(7) 과 같은 천장. 측정표 `docs/measure/A2-사이드아이콘.md` · 비평 `docs/review/A2-사이드아이콘.md`<br>**핵심 변경 3가지** ① 1행이 «라벨 없는 단독 아이콘(아트 101) + 아래 gap 60» 이다. 균등 pitch 134 로 깔면 2~6행이 전부 27px 위로 밀린다. ② `#sideR` 은 1행(= `#menub` 자리)을 정식으로 비우고 2행부터 시작 — 02 가 넣어 둔 `transform:translateY()` 회피 한 줄은 **제거했다**. ③ `SIDE.TOP` 31 → **72** (31 은 폐기된 «캔버스 top 229» 기준). 이걸로 A3 가 남긴 «초상화 플레이트와 16px 겹침» 도 해소(현재 34px 여유).<br>**아이콘 잉크는 레퍼런스 스프라이트 bbox 대비 폭·높이·중심x·top 모두 1px 이내.** 칸마다 `--sf/--sx/--dx/--dy` 를 실측 잉크로 역산해 박아 뒀다 — **이모지를 바꾸면 이 4개 값을 반드시 재측정할 것**(방법은 비평 문서에) |
| A3 | 상단 HUD 레퍼런스대로 재구성 | `#top` | **완료 (6/10, 5/5회차)** — 초상화 플레이트(사선 곡선·꼬리 2단 톤) + 닉네임 알약 + 칭호 육각 배너 + 🔥전투력 / 재화 **2종**(골드·젬, 알약 211×48, 아이콘이 좌측 모서리에 물림). 측정표 `docs/measure/A3-상단HUD.md` · 비평 `docs/review/A3-상단HUD.md`. **레이아웃은 9점 도달**(배치9·여백9·정렬9·폰트9), 최종을 6에 묶은 건 **색·명암 단일 항목**이며 원인은 이모지 대체(초상화·코인·젬·🔥) — **실제 아트 리소스 없이는 회수 불가**. ⚠️ **A2 와 겹침**: 플레이트가 바 아래 **34px 돌출**(레퍼런스대로)인데 A2 의 `SIDE.TOP` 이 9:16 에서 좌측 첫 아이콘을 `y=125` 에 놓아 **약 16px 겹침** — 레퍼런스는 아이콘이 플레이트 아래(로컬 166)에서 시작하므로 **`SIDE.TOP` 을 올리는 게 맞다**(A2 구간이라 미수정) |
| A4 | 스킬 슬롯 원형화 | `#slots` | **완료 (7/10, 5/5회차)** — 지름120·pitch130·gap10·링 4겹(4-8-4)·well88, 활성/대기/잠금 3상태 conic 그라디언트, 하단 Ø40 뱃지. 측정표 `docs/measure/A4-스킬슬롯.md` · 비평 `docs/review/A4-스킬슬롯.md`. **남은 것**: 자물쇠 열쇠구멍·shackle 비율(🔒 이모지 글리프 한계 — 실제 아트 넣으면 해소), 원주 12% 구간 Δ6~20(JPEG 경계 노이즈 바닥) |
| A5 | 팝업 모달화(레퍼런스 규격) | `.modal`, `.mbox` | **완료 (9/10, 5/5회차 · 통과)** — 측정표 `docs/measure/A5-모달.md` · 기록 `docs/review/A5-모달.md` |

## 화면별 상태 (완료)

| # | 화면 | 측정표 | 구현 | 최고 점수 | 루프 횟수 | 비고 |
|---|---|---|---|---|---|---|
| 1 | 오프라인보상 팝업 | `docs/measure/01-오프라인보상팝업.md` | ✅ | 5 | 4/5 | 전용 오버레이 `#offw` 신설(모달 바깥 상품 배너 때문에 공용 `#modal` 불가). 박스·좌표·폰트 전부 헤드리스 실측으로 측정표와 1px 이내 일치. 남은 감점은 **배너 금화 3개 부재 + 이모지 대체 + 게임 서체 부재** 로 수렴 — 아트 리소스 필요. 기록 `docs/review/01-오프라인보상팝업.md` |
| 2 | 기본 메인 화면 | `docs/measure/02-기본-메인-화면.md` | ✅ ② 헤더·우상단 메뉴·④ 좌하단/우하단 배너·배속 | **5** | 3/5 | 1회차 3·1 → **2회차 5·5**. 3회차 수정은 픽셀 실측으로 검증(비평 재채점 안 함). 남은 감점은 **이모지 대체 + 게임 서체 부재** 로 수렴 — A1·A3·A4 와 같은 한계. 기록 `docs/review/02-기본-메인-화면.md` |
| 3 | 던전 팝업 | `docs/measure/03-던전팝업.md` | ✅ | **8** | 3/5 | 1회차 6·6 → 2회차 7·7 → **3회차 8·8**. 레퍼런스는 모달이 아니라 **HUD~탭바 사이를 채우는 불투명 리스트 페이지**(딤·타이틀바·패널 테두리 없음)라 전용 오버레이 `#dunw` 를 신설했다. 카드 5장 top 이 전부 오차 0px. 남은 감점은 **이모지 대체 + 게임 서체 부재** 로 수렴 — 01·02·A1 과 같은 한계. 기록 `docs/review/03-던전팝업.md` |
| 4 | 던전 세부 팝업 | `docs/measure/04-던전세부팝업.md` | ✅ | **8** | 5/5 | 1회차 5 → 2회차 6 → 3회차 7 → 4·5회차 **8**. 껍데기는 «딤(.50) 위 가운데 다이얼로그» 796×1197(radius 42, 검정 6~7 + 갈색 링 9). 5회차 실측 **전 요소 Δ≤2px**(보상 박스·`3` 잉크는 Δ0), 크롬 색 ΔRGB≤1. 5회차 수정 1건 = **입장권 알약 51→45**(1회차 자가 스캔이 티켓 아이콘 AA 행까지 물어 오독한 것 — 측정표 45 가 맞았다). 남은 감점은 **이모지 대체 + 게임 서체 부재**로 수렴 — A1·A3·01·02·05 와 같은 천장. 기록 `docs/review/04-던전세부팝업.md` |
| 5 | 무기 팝업 | `docs/measure/05-무기팝업.md` | ✅ | **7** | 3/5 | **껍데기는 «딤 위 가운데 다이얼로그»** (898x1469 · radius 42 · 딤 .55). 페이지 화살표가 모달 **바깥**이라 전용 오버레이 `#wpnw`. 요소 좌표 전부 **ref y−210 / 가로 1:1 로 0~2px 일치**, 텍스트 잉크 ±5%, 색 Δ≤2. 비평 4→5/6→**7**. 남은 감점은 **이모지 대체 + 게임 서체 부재** 로 수렴(A1·A3·01·02 와 같은 한계). **06 장비 팝업의 무기 슬롯이 정식 진입점**이다. 기록 `docs/review/05-무기팝업.md` |
| 6 | 장비 팝업 | `docs/measure/06-장비팝업.md` | ✅ | **8** | 4/5 | **폭 1080 «바닥 시트»**(자체 오버레이 `#eqw`, 딤 .28). 헤더+카드 컨테이너(2행, 하늘/바닥 아치)+잠금 슬롯 3개+서브탭 바. 좌표 전부 **가로 1:1·세로 ref−421 로 0~3px**. 1회차 A7/B8 → 4회차 **A8/B8**. ①~④·⑥ 은 9점 도달, 최저 ⑤(색) 8 은 **두 비평가가 슬롯 초록 보정 방향을 정반대로 지적**해 JPEG 노이즈 상한. 나머지는 **캐릭터/아이템 아트 이모지 대체**(② 천장, A1·A3·A4·01·03·05 와 동일). ❤️⚔️·자물쇠는 CSS 작화, **저장 구조 미변경(표시 전용)**. 캐릭터 2명 구조는 미결로 남김. 기록 `docs/review/06-장비팝업.md` |
| 7 | 스킬 팝업 | `docs/measure/07-스킬팝업.md` | ✅ | **8** | 4/5 | **폭 1080 «바닥 시트»**(`#panel:has(#bSk.on)`). 이전 세션이 비평 0회로 죽은 것을 인계(lock 164분 회수). 1회차 A6/B5 → 2회차 C4/D6 → 3회차 E8/F8 → 4회차 G8/H7. **회차별 성격이 달랐다** — 1회차=라벨 검정 외곽선 신설(측정표가 색만 적고 스트로크 누락), 2회차=입체 베벨 복원(자물쇠 열쇠구멍·버튼/알약/바 베벨), 3·4회차=카드 폭 170→168·gutter 22·그리드 radius 25 등 2px 정밀화. **측정표 오류 6건 정정**(카드 상단38/하단16 · 라벨 스트로크 · 구분선 #483B2B · 잠금탭 라벨 · 슬롯패널 x21w1037 · 카드 168/gutter22). G/H 기준 **①②③④=9**, 남은 감점은 **카드 전면 아트 이모지 대체**(⑤)뿐 — A1·A3·04·05·06 과 같은 천장. 측정표 `docs/measure/07-스킬팝업.md` · 기록 `docs/review/07-스킬팝업.md` |
| 8 | 스킬 세부 팝업 | `docs/measure/08-스킬세부팝업.md` | ✅ | **8** | 5/5 | 1회차 5 → 2회차 8 → 3회차 7 → 4회차 7 → **5회차 8 (통과 처리)**. A5 공용 다이얼로그 껍데기 + `.sk8` override(딤 55%·무늬 없음·하단 프레임 +6px). 카드(#D7C0A1) 안 절대배치. **5회차에 6축 전부 최소 한 비평가가 9** — 남은 8 2건(A ②게이지·등급칩 / B ⑥본문외곽선)은 서로 상대가 9로 준 ±1px·JPEG 경계 노이즈(수렴 바닥). **다른 화면과 달리 ⑤ 색도 9 도달**(스킬 아이콘 이모지가 박스 안에 들어가 크롬 색으로 채점됨). 기록 `docs/review/08-스킬세부팝업.md`<br>**핵심 교훈 2개** ① `잉크높이÷0.93` 은 **한글 전용** — 「Lv.8」·「4/N」 같은 라틴·숫자는 −20~26% 나온다(÷0.72 + scaleX). ② **외곽선 두께는 bbox 가 아니라 순검정(<35) 세로획 폭으로 재라** — 제목·본문 둘 다 bbox 로 재서 과보정했다가 되돌렸다(제목 11→7, 본문 9→6). |
| 9 | 일괄 강화 결과 팝업 | `docs/measure/09-일괄강화결과팝업.md` | ✅ | **9** | 4/5 | **통과**(G 9/H 9.5). A5 3분류에 없는 **«껍데기 없음» 4번째 유형** — 딤 `rgba(0,0,0,.80)` 위에 리본+카드3칸+«n➡m»+터치닫기만 얹은 연출(`.mbox` 미사용, 전용 `#upw`). 배치·크기·간격·정렬·폰트비율 9.5~10, 최종을 9 에 묶은 건 색·명암 단일 항목이며 원인은 이모지·서체 대체(A1·A3·02 와 같은 천장). `levelUpAll()` 이 `ups` 반환 → 스킬·무기·배너 일괄강화 3경로를 `openUpAll()` 로 연결. 기록 `docs/review/09-일괄강화결과팝업.md`<br>**교훈**: ①숫자 텍스트는 `÷0.93` 이 아니라 `÷0.70`(캡하이트) ②같은 부위 지적이 2회 반복되면 CSS 값이 아니라 겹침 **구조**를 고친다(꼬리를 몸통 앞 «ㄴ자 실루엣»으로 재작화하니 5건이 한 번에 해소) ③그늘 밴드는 **순서**까지 실측대로(안쪽 #DD8433/바깥 #A45119) ④인라인 SVG 는 `fill` 명시 필수 |
| 10 | 상점 팝업 — 소환 탭 | `docs/measure/10-상점팝업소환탭.md` | ✅ | **7** | 2/5 | **(다) 전체화면 페이지** — 03 던전처럼 딤·헤더·테두리 없는 전용 오버레이 `#shopw`(HUD~탭바, 배경 #272731). 상자 카드 3장(무기·방어구·스킬)=기존 소환 배너에 연결. 각 카드 헤더밴드/본문 가로 그라디언트+방사광선/Lv 알약+경험치 바/🔍 확률/소환 버튼 3개(무료10연 일2회·💎10연·💎30연). `doSummonFree`·`S.daily.freeSum` 신설. **진입=상점 탭**, 재화 카테고리→기존 다이아/아바타 상점(패널) 연결로 기능 보존, 보물상자 탭(유물)은 미변경. 비평 최고 7(2회차 C7/D6). 남은 감점은 **보물상자 컨테이너 스프라이트 부재+서체+이모지 = 아트 한계**(A1·A3·05 와 같은 천장). 측정표 `docs/measure/10-상점팝업소환탭.md` · 기록 `docs/review/10-상점팝업소환탭.md`<br>**교훈**: 새 오버레이의 테두리·뱃지 레이어(`.cfr`/`.adbadge`)를 CSS 만 정의하고 렌더 마크업에 빠뜨리면 «테두리 0px» 로 통째 사라진다 — 렌더 함수에 실제 요소를 넣었는지 픽셀 스캔으로 확인할 것. `position:absolute` 는 `.shp-card>*` 처럼 **직속 자식**만 잡으므로 버튼 내부 뱃지는 별도 지정 필요 |
| 11 | 소환 부분 정보 팝업 | `docs/measure/11-소환부분정보팝업.md` | ✅ | **8** | 2/5 | 전용 오버레이 `#prbw` — 딤(.55) 위 **정중앙 다이얼로그** 797×1170(radius30, 검정6+갈색13). 좌표 가로1:1·세로 `ref−210`. 타이틀밴드「확률 정보」+ 단계선택「◀ 단계 N ▶」(탄색박스+양쪽 흰 삼각형)+ 안내문2줄 + 등급헤더바(초록 캡슐)+ 아이템행 알약(크림·pitch85·이름 좌정렬 x304/확률 우정렬 x833 시안). 확률은 실제 `gradeProbs` 로직에서 계산, ◀▶ 로 단계(1·5·15·30·40·MAX) 열람. 진입: 소환박스 헤더 🔍(`renderBanner [data-prob]`)→`openProbInfo(bank,lv)`. 1회차 C7/D5(좌화살표가 경계단계라 흐려짐+타이틀폭+20%)→ **2회차 C8/D7**(레이아웃 ①③④ 전부 9, ≤2px). 남은 감점은 **⑤ 등급바 초록 hue = 게임 팔레트(#6fe08a)** — A1~A4·01·02 와 같은 천장(그보다 높음). 기록 `docs/review/11-소환부분정보팝업.md` |
| 12 | 소환 결과 팝업 | `docs/measure/12-소환결과팝업.md` | ✅ | **7** | 3/5 | 1회차 A7·B6 → 2회차 C6·D5 → **3회차 E7·F8**. 껍데기는 A5 3분류에 없는 **(라) 전면 딤(.80) + 전폭 불투명 밴드 패널** — 패널이 x0~1079 를 그대로 물고 좌우 테두리·radius 가 **0** 이라 공용 `#modal` 로는 표현 불가 → 전용 오버레이 `#sumw`. 09(`#upw`)와 같은 «결과 연출» 계열이지만 09 와 달리 불투명 패널이 하나 있다. DOM 실측이 측정표와 **전 항목 Δ0**(패널 0,709 1080×539 · 그리드 36,815 1007×335 · 카드 157×157 pitch 170 · 리본 밴드 284,641 513×87 · 버튼 117/416/716,1347 247×148). 3회차에 **E 가 ①~⑤ 를 전부 9** 로 줬고 **⑥ 폰트 단독으로 7 에 묶였다**. 기록 `docs/review/12-소환결과팝업.md`<br>**세로 좌표계** — PROGRESS «확정(03)» 2종 변환이 여기서 정확히 맞았다: 리본·패널·그리드 `ref_y−84` / 버튼·터치하여닫기 `ref_y−419`. ref 의 «패널 하단~버튼 상단» 435px 이 우리 프레임에서 **100px** 이 되며 **420px 차이를 이 빈 딤 구간이 전부 흡수**한다 — 전투 캔버스가 없는 오버레이 화면에서 세로 차이를 어디가 먹느냐에 대한 답이다. 다른 결과 연출 화면도 그대로 쓸 것.<br>**남은 감점은 ⑥ 폰트 단독이고 CSS 로 회수 불가**(E·F 가 독립적으로 «웹폰트 교체 없이는 불가» 로 분류): 숫자 `1` 이 ref 는 밑굽 없는 막대형(폭 9~10)인데 우리 서체는 밑굽이 있어 15 다. 캡높이·자간은 이미 맞췄다. 아이콘도 bbox·중심은 정답이고 «그림 내용» 차이뿐 — A1·A3·01~05 와 같은 천장.<br>**버튼 3개는 10 상점 소환 탭의 `freeLeft`/`SHOP_FREE`/`doSummonFree`/`summonCost` 를 그대로 재사용**해 저장 구조 변경 없음(KEY 유지). 30연은 5행이라 프레임에 안 들어가므로 **레퍼런스 케이스(2행 335px)를 픽셀 그대로 두고 초과분만 스크롤**한다 |
| 13 | 상점 팝업 (재화 탭) | `docs/measure/13-상점팝업재화탭.md` | ✅ | **7** | 5/5 | 10 의 `#shopw` 안 콘텐츠 교체(`renderCoinPage`) — 전용 오버레이 없음. 1회차 4(병합 사고로 CSS 규칙 소실 +59px) → 3회차 6·6 → **4회차 D7** → 5회차 6·6. **3회차의 최대 감점은 화면 자체가 아니라 주변 크롬 상태**였다: 전체화면 상점이 열려도 탭바가 5버튼 그대로였음 → `syncShopNav()` 신설로 A1 의 ✕ 칸(296px, ref 295 와 1px 차) 발동 + coin 스코프 HUD 민바(`#app:has()` — 02·10 화면 무영향). 서체 종횡비(폭 정합·높이 +9~14%)를 fs↓+scaleX↑ 로 전 텍스트 Δ≤2 수렴. 레드닷 `.tab .bdg` 37→30(코어 Ø24, 02-비고 통일 스펙). 남은 감점은 **카드 아이콘 6종·배너 보물상자·HUD M메달·내비 아이콘 이모지 대체 + 게임 서체**(A1·A3·03·05 와 같은 천장) + ref 배경의 발자국 패턴 미재현. **비평가 8명 중 6명의 지적 다수가 마스크 기준 차이로 상호 모순** — 자체 동일-마스크 스캔으로 걸러 반영(기록 문서에 판정표). 기록 `docs/review/13-상점팝업재화탭.md` |
| 14 | 유물 팝업 — 보물상자 탭 | `docs/measure/14-유물팝업보물상자탭.md` | ✅ | **7** | 4/5 | **(다) 전체화면 페이지** 신설(`#relicw`, 03 던전·10 상점과 동종). 상단 클러스터 절대매핑 `y = ref y − 87`, 하단 5칸+서브탭 프레임 하단 고정. [오픈 ×1/×30] 은 기존 `doSummon('relic', n)` 에 연결(**저장 구조 미변경 · 표시 전용**). 상자 5종은 표시 전용 매핑(우리 게임은 유물 배너 1종). 「유물」 서브탭은 기존 `#bRel` 배너 패널(15번 담당 예정)로 연결. 1회차 눈대중 비평 두 명이 «전체가 90px 우측/20% 크다» 오독, PIL 자체 스캔으로 **전면 반증**(리본/상자/버튼/5칸 모두 x=540 센터, 폭 ±3%). 2회차부터 «PIL 스캔 필수» 로 4회차까지 **A/B/C/D/E/F/G/H 8명** 채점, ①9 ③9 ④9 도달, 최저 ⑤7·⑥7 은 «중앙상자·5칸·활성화 아이콘 이모지 대체» 아트 한계(A1·A3·A4·03·06 과 동일 천장). 기록 `docs/review/14-유물팝업보물상자탭.md`<br>**교훈 4개** ① **눈대중 비평의 «전체 X% 어긋남» 은 세로 종횡비 착시**(1:1.78 vs 1:2.17). 두 비평가가 독립적으로 같은 방향·같은 크기의 오독을 낸다 — 반드시 PIL 스캔으로 반증. ② **버튼 border-box 를 fill-only 스캔으로 재지 마라** — 오픈버튼 fill h=115 · border-box h=148, 섞으면 «22% 과대» 같은 반대 결론이 나온다. ③ **Playwright `addInitScript`** 로 localStorage 주입해야 첫 load() 가 값을 읽는다 (reload 후 setItem 은 `file://` 컨텍스트에서 초기화됨). ④ **그라디언트 fill 상단 색이 중앙까지 새어든다** — `linear-gradient(#FEC820 0 34%, #FFB514)` 는 y=34% 까지 상단색 유지해 중앙 스캔이 (255,194,29) 로 잡힌다. 목표 색이 하나면 solid, 그라디언트라면 목표 색을 **y=50% 근처에** 두어야 한다. |
| 15 | 유물 팝업 — 유물 탭 | `docs/measure/15-유물팝업유물탭.md` | ✅ | **8** (1회차 A) | 5/5 | **(다) 전체화면 페이지** 전용 오버레이 `#rlw`(03 던전·10 상점과 동종). 등급 리본 6섹션 × 아치 카드 6칸(1행4+2행2) + 스크롤 위에 뜬 하단 서브탭 바. **표시 전용 — RELICS·S.own 저장 구조 미변경.** 좌표 변환은 PROGRESS «확정(03)» 2종 그대로(콘텐츠 `ref y−84` · 탭바 위 고정 `ref y−420`). 1회차 A8/B6 → 2회차 C7/D8 → 3회차 E7/F6 → 4회차(점수 기록 유실) → **5회차 I5/J4**. **5회차가 4회차의 회귀를 잡아냈다**: 4회차가 넣은 `--bvl`/`--bvr` 좌우 그라를 **베이스 규칙에만** 정의한 탓에 `var(--bvl,var(--bv))` 폴백이 안 걸려 **`[data-g]` 등급 틴트가 통째로 죽어 있었다**(고급 카드 쉘이 ref 초록 (63,100,5) 인데 회청색 (79,85,107)). 이것이 ⑤ 를 7~8 → 4~5 로 끌어내린 주범이다. 5회차 수정 7건 — ① 좌우 그라 폐기(등급 틴트 부활, 쉘 색 **Δ0**) ② 파우치 레일 25→**20px**(ref 실측, 3회차 32 는 과보정) ③ 내부면 립 하이라이트 신설(피크 위치 ref 와 동일) ④ 초록 돔 near-black → `#183810`(**Δ≤2**, 아이콘 숨긴 프로브 환형 중앙값 · 은색 대조군 Δ≤5 로 방법 검증) ⑤ 리본 꼬리를 **밴드 앞**으로 재작화 + 바깥 검정 8px(ref 구조) ⑥ 리본 폭 420→**430**(4회차 값은 꼬리 검정을 놓쳤다, 행별 Δ≤2) ⑦ 자물쇠 고리:몸통 배분·열쇠구멍 Ø20×33 · 서브탭 알약 좌우 안쪽 림. 수정 후 자가 실측 **13항목 중 9개가 Δ≤2**(쉘 색·밴드 폭·피치·레일 Δ0). 콘솔 에러 0 · 프레임 이탈 0. **남은 감점(5회 소진, 아트·서체 천장)**: 한글 획 두께 −55%(라틴 `Lv. 2` 는 ref 와 일치 → font-size 아닌 서체 문제) · 꼬리 노치의 손그림 곡선(clip-path 다각형 한계, SVG path 필요) · 배경 해골 텍스처 부재 · 카드 아이콘 이모지 대체 — A1·A3·04~07 과 같은 천장. **재채점 없이 종료**(5회차 점수는 수정 «전» 캡처 기준이다 — 다음에 이 화면을 열면 `15-r9.png` 로 6회차부터 세면 된다). 측정표 `docs/measure/15-유물팝업유물탭.md` · 기록 `docs/review/15-유물팝업유물탭.md` |
| 17 | 스탯업 보너스 팝업 연출 | `docs/measure/17-스탯업보너스팝업.md` | ✅ | **8** | 5/5 | 09 와 동종 «껍데기 없음·딤 위 연출» — 전용 `#statw`. **리본은 09 와 픽셀 동일 에셋**(y−135)이라 `.upr-*` 재사용, 그룹만 top 793 절대 배치(꼬리 드룹 −3px 는 scaleY .969). **딤이 상하 2톤**(상 .885/하 .79, 전환 y916→966 — 23-훈련 ref 와 픽셀 나눗셈으로 산출). 능력 카드 247×248(검정5+핑크링6 #F33E69+#B41C3C+드롭섀도), 설명 1줄은 토큰 분해 렌더(숫자만 골드 «30», 12 의 sm-fat 식 웨이트 보강), 5각 별+실측 헤일로 2개. `openStatUp({ic,desc})` **표시 전용** — **23 훈련의 ↑ 버튼이 이걸 호출하면 된다**(진입점 대기). 1회차 6 → 3회차 8 → 5회차 **B9/C8**. 남은 감점은 **카드 아트 이모지 + 게임 서체 부재**(B·C 합의 «CSS 회수 불가») — A1·A3·04~07 과 같은 천장. 기록 `docs/review/17-스탯업보너스팝업.md`<br>**교훈 3**: ① radial-gradient 는 기본이 farthest-corner — 원형 헤일로는 `circle closest-side` 명시(안 하면 반경 √2 배) ② `attr(data-t)` 스트로크 복제는 **inline 요소에 못 쓴다**(+y 고스트 밴드) — inline-block 화 + 공백은 nbsp ③ 반투명 헤일로 최대 휘도는 «색 자체 lum» 이 상한 |
| 18 | 패배 화면 | `docs/measure/18-패배화면.md` | ✅ | **9** | 5/5 | **통과 (I 9 / J 9).** «껍데기 없음·딤 위 연출» 4번째 유형(09·17 동종), 전용 `#defw`. 딤 `rgba(0,0,0,.62)` 전체 균일. 해골 엠블럼 + 제목 + 가이드 카드 3장(각 아이콘·문구·노란 → 버튼) + «터치하여 닫기». 진입 = 사망 시 `openDefeat()` — **표시 전용, 자동 부활·저장 구조 불변**. → 버튼은 카드1 상점 소환 탭 / 카드2 성장 탭 / 카드3 닫기. 기록 `docs/review/18-패배화면.md`<br>**이 화면이 «이모지 천장» 을 처음으로 깬 사례다.** 5회차 비평가 2명이 모두 «아트/서체 없이 회수 불가한 항목: 없음» 이라고 명시했다(A1 6·A3 6·A4 7·01 5·02 5 가 «아트 한계» 로 남긴 것과 대조). 방법 3가지:<br>① **이모지를 CSS 로 재작화**한다 — 카드1 ⚔️ 는 같은 창에서 잉크가 ref 의 42% 뿐이라 ② 를 회수할 수 없었다. 픽셀맵을 떠 보물상자(뚜껑/몸통+자물쇠/받침 3단)로 다시 그리니 13개 표본 행 전부 Δ0~2.<br>② **«채움색 스트로크» 로 자평을 살찌운다** — 검정 외곽선을 목표의 2배로 키우고 그 위에 «채움색» 스트로크 레이어를 덮으면 흰 잉크가 굵어지고 남는 검정 링은 `(검정−채움)/2` 로 유지된다. 제목·문구·닫기 전부 ref 의 행별 흰 run 중앙값에 도달. **A1·A3·01·02 가 «서체 한계» 로 남긴 감점의 상당 부분이 이 방법으로 회수 가능하다.**<br>③ **비평가는 점수가 아니라 «측정 밀도» 로 신뢰하라** — 5회차 I·J 가 ⑤ 를 9·10 으로 준 항목을 4회차 G(도구 70회·최소제곱 원피팅)는 **5** 로 잡았고 재검증 결과 G 가 전부 옳았다(글로우·버튼 림·레드닷 링). |
| 19 | 프로필 팝업 | `docs/measure/19-프로필팝업.md` | ✅ | **8** | 5/5+검증 | **(가) 딤(남색 rgba(0,20,50,.85)) + 중앙 다이얼로그** 전용 오버레이 `#pfw` 896×1396, **전 요소 `y_ref−210`**. Gamer Id·초상화 클러스터·탭3·칭호 2열 그리드(RANKS 8종 표시 전용 매핑)·해금 미션 바·[장착 중] 버튼·하단 토글(20 `#specw` 상호 연결). **저장 구조 미변경.** 앞 세션(sess-0716)이 수정 5회차까지 돌리고 기록 없이 중단 → 마감 세션이 lock 회수 후 **채점 전용 6회차: A8/B8**. ①④⑤ 9 도달(팔레트 Δ≤1), 최저 ③⑥ 8. 남은 CSS 회수 가능분 7건(활성 탭 테두리 4→8·코너 radius −13·GamerId 폭 −4.5% 등)과 아트·서체 천장(초상화·연필·자물쇠 이모지)은 기록 `docs/review/19-프로필팝업.md` 참고. **교훈: 회차마다 review 파일을 같이 커밋할 것** — 1~5회차 점수가 유실됐다 |
| 23 | 훈련 팝업 **【⚑ 우선】** | `docs/measure/23-훈련팝업.md` | ✅ | **9(E)/7(H·G)** | 5/5 | **시스템·레이아웃 구현 완료, 마감 회차 남음** (sess-1715, 2026-08-24 17:15~19:15).<br>**껍데기**: 하단 고정 «바닥 시트» 전용 오버레이 `#trw` (ref 시트 border-box y920..2164 h1245 → `#trw` 바닥에 붙이고 **자식 좌표 = ref y − 920**). 「상단이 물결 곡선」이라고 적혀 있었으나 **실측은 직선 + 라운드 코너**(1061 컬럼 중 972 가 같은 y924 에서 전환).<br>**시스템**(UI-REFERENCE 23 설계 그대로): 3종(공격력·체력·체력회복) · 상한 = 단계×100 · 진행바 = 3종 합/(3×상한) · 3종 만렙 → ↑ 활성 → **17 연출** → 단계 +1 → 전 스탯 **+10% 누적** · x1/x10/x30 · 서브탭 2 = **스탯 훈련**(같은 구조, 재화만 포인트). 탭 «성장»→«훈련» 라벨 + **탭 직행**(중간 패널 폐기, `syncTrainNav` 로 ✕ 처리).<br>**저장 구조**: `trainStage`·`statStage`·`spRegen` 3개 추가. `DEF()`+`Object.assign` 이라 **구 세이브 그대로 열림 → KEY 유지**. UPG 나머지 7종은 값·계산 유지하고 **구매 경로에서만 제외**(자동구매도 3종만).<br>**비평 5회차 · 비평가 10명**(A5/B4 → C5/D7 → **E9 통과**/F7 → G7/H7 → I7/J8 · 최저 7). 세로 앵커(크림 1063/1069·진행바 1228/1282·배수탭 1328/1400·카드 1436/1945·서브탭 2021/2117) **전부 Δ0**, 중심축 x538~540 일치, 폰트 잉크 높이 8종 ±1px, 색 20종 중 18종 Δ≤7.<br>**남은 것**: ③④⑤ 6건(서브탭 활성 pill 폭 250·칸 색 ΔRGB13·배수탭 레드닷 2·3 앵커·헤더 밴드 2단 하드 스텝·크림박스 상단 그림자 띠 11px·비용 바 하단 radius 30) — **전부 수치가 review 문서에 있다**. ⑥ 폰트는 J 가 10점(잉크 9종 Δ≤1px), ①② 는 9점. 이모지 대체는 ⑤ 천장(A1·A3·04~07 동일). ⚠ **I·J 는 `9c5859e` 이전 캡처를 봤다** — 두 사람의 최대 감점(↑버튼 은색)은 그 커밋에서 이미 해결됐으니 현재 HEAD 로 재캡처 후 남은 6건만 볼 것. 기록 `docs/review/23-훈련팝업.md` (회차별 지적·반영 전부) 레퍼런스 23 이미지 그대로 + UI-REFERENCE 23번 «우리 게임 설계» 절(공격력·체력·체력회복 3종, 단계 상한, ↑ → 17 연출). **하단 탭 «성장» 을 «훈련» 라벨로 바꾸고 탭 클릭 시 이 팝업 직행**까지 이 작업에 포함(중간 성장 패널 폐기) · **서브탭은 «훈련 · 스탯 훈련» 2개만**(2026-08-25 추가, 작업 47 참고) |
| 24 | **A1 개정 — 탭 «모험» → «던전»** | (A1 측정표 재사용) | ✅ | — (기계적) | — | **완료 (2026-08-24, sess-1814).** 라벨 `던전` + `goTab('adv')` → `openDungeon()` **직행**(중간 «모험» 패널 폐기). 변경 4곳 — ① 탭 라벨 문자열 ② `goTab` 의 `adv` 분기 신설(다른 페이지 닫기 · 재클릭 토글 · `S.seen.adv` NEW 소진) ③ `openDungeon/closeDungeon` 에 `syncDunNav` + `closeRelicTab()` 연결, `closeDungeon` 은 닫혀 있으면 early-return ④ `syncDunNav()` 신설(`syncShopNav`/`syncTrainNav` 와 같은 규칙 — 열린 동안 «던전» 칸이 ✕ **296px**). **CSS 0줄 변경**(A1 측정값 그대로). <br>**실제 버그 1건 — 캡처 리스너**: `$('tabbar').addEventListener('click', closeDungeon, true)` 가 `goTab` 보다 **먼저** 돌아 던전을 닫고, 그 뒤 `goTab` 이 «닫혀 있음»을 보고 다시 열어 **재클릭 토글이 죽었다**. 제거함(다른 탭 이동은 `goTab` 분기가 이미 정리). → **어떤 UI 를 탭이 소유하게 바꿀 때는 그 UI 를 정리하던 캡처/전역 리스너부터 grep 할 것.**<br>지시서 [3]-(가) 대로 **비평가 없이** `node --check` OK + `grep 모험` 잔여 0 + 헤드리스 **전이 12전 · 진입 4전** 실측: 열린 페이지 **항상 1개**(배타 100%), 메인 요소 8종 좌표 **전 상태 Δ0**(작업 38 규칙 유지), ✕ 296px, 콘솔 에러 0, 프레임 밖 요소 0, 03→04(`#dgdw`) 경로 회귀 0. 실측표 `docs/review/24-탭던전직행.md`<br>**넘기는 것**: 보물상자 탭(14 `#relicw`)은 열려도 탭이 ✕ 로 안 바뀐다 — 14 구간이라 미수정 |
| 26 | **동료 팝업 (신규)** | `renderPet`/`#bPet` | ✅ | — (07 클론) | — | **완료 (2026-08-24, sess-1844).** `renderPet()`+`#bPet` 핸들러 = `renderSkill()` 1:1 복제. **CSS 0줄 신설** — 기존 `#bSk` 규칙 16개를 `:is(#bSk,#bPet)` 로 공유(특정성 보존, 07 영향 0). 데이터는 기존 `PETS`·`S.eqPet`·`toggleEquip(,'pet')`·`itemIcon` 재사용 → **저장 구조 미변경(KEY 유지)**. 슬롯 3칸 가로중앙(좌 336, 실측 x357/482/607·중심 539.5≈540). 그리드 5×3(펫 9종), 08 세부 재사용. `[동료 소환]`→`goTab('shop')`(10 #shopw 이동). 진입 디스패치 `renderBanner('pet')`→`renderPet()` 교체·배너핸들러 제거. 검증([3] 레퍼런스 없는 클론이라 구조/상호작용): 헤드리스 콘솔에러0·07 회귀0·좌표 07 동일·상호작용 전수(세부/장착토글/서브탭/소환→shop) 정상, **독립 시각QA 5축 PASS = CLEAN CLONE**. 캡처 `docs/review/26-r1.png` · 기록 `docs/review/26-동료팝업.md` |
| 27 | **07 개정 — [스킬 소환] 버튼을 상점 이동으로** | `#panel:has(#bSk.on)` 버튼 핸들러 | ✅ | — (기계적) | — | **완료 (2026-08-24, sess-1913).** 그 자리 소환 제거 — `data-sksum` 핸들러 `doSummon('skill',1)` → **`goTab('shop')`** (26 `[동료 소환]` 과 코드 경로 1:1 동일). **index.html 2줄(주석 1 + 핸들러 1) · CSS 0줄** — `goTab('shop')`(작업 24 개정본)이 이미 «패널 닫기 → `openShopPage()`(shopCat=summon 고정·scrollTop 0·✕ 296px)» 를 전부 하므로 `closeSkill()` 신설 불필요. 지시서 [3]-(가) 대로 **비평가 없이** `node --check` OK + grep `doSummon('skill'` 잔여 0 + 헤드리스 실측: 클릭 후 `panelOpen:false·#bSk off·shopw on·shopCat summon·✕ 296px·openPages 단독`, **다이아·`S.summons`·보유 스킬 전부 불변**(= 그 자리 소환 미발생), 메인 요소 10종 좌표 3상태 **Δ0.00**(작업 38 유지), 프레임 밖 0, 콘솔 에러 0. 회귀 4경로(상점 실소환·시트 재진입·일괄 강화 `#upw`·26 동료 소환) 전부 정상. 실측표 `docs/review/27-07소환버튼상점이동.md` · 캡처 `27-r1.png`<br>**교훈 — 아이들 게임에서 «상태 불변» 을 검사할 때는 유휴 루프가 굴리는 값을 판정에서 빼라.** 첫 스크립트가 골드로 판정해 «소환 발생» 오탐을 냈다. 무클릭 대조군(0.5s) 결과 **골드만 −55 드리프트**, 소환 통화인 다이아·`S.summons` 는 불변이었다.<br>**넘기는 것**: 상점 카테고리 탭은 아직 4개 — **작업 45** 범위라 미수정 |
| 37 | **와이드 화면 프레임 잘림 수정 【⚑ 우선】** | `fit()` (스케일·정렬), `#wrap` 배경 | ✅ | — (기계적) | — | **완료 (2026-08-24, sess-1642).** 버그: 9:16 보다 넓은 화면에서 frameH 가 1600 으로 clamp 되는데 스케일이 `AW/1080`(폭 기준)이라 **프레임이 세로로 넘쳐 잘림**. 수정 2줄 — ① `fit()` 의 `scale` 을 `Math.min(AW/FRAME_W, AH/frameH)` 로 ② `#wrap{background:#000;overflow:hidden}`(남는 영역 필러/레터박스). **중앙 정렬은 `#wrap` 기존 flex center + `transform-origin:center` 로 이미 성립**해 추가 변경 없음(실측 Δx·Δy=0.0). 지시서 [3]-(가) 대로 **비평가 없이** grep 잔여 0건 + `node --check` OK + 헤드리스 **8비율 전수 실측**으로 검증: 프레임 뷰포트 내부 ±0.5px · 중앙 정렬 ±1px · 콘솔 에러 0 · 요소 넘침 0. 수정 전 16:9=1764px, 21:9=2713px, 4:3=749px, 3:4=114px 잘리던 것이 **전부 전체 노출**, **세로 폰 4종(9:16·9:19.5·9:21·9:24)은 scale·frameH·프레임 사각형·stageH 가 수정 전과 완전히 동일 = 회귀 0건**. 필러박스 픽셀 `(0,0,0)` 확인. 실측표 `docs/review/37-와이드화면프레임잘림.md`<br>**넘기는 것**: 넓은 화면에서 `stagearea` 가 1316(9:16 은 1636)이라 `layoutSide()` 비례 축소가 걸린다 — 잘림은 아니므로(넘침 0건) 37 범위 밖, 손보려면 **A2 구간**에서 할 것 |
| 38 | **패널 오버레이화 — 열려도 메인 화면 무영향 【⚑ 우선】** | `#panel` 포지셔닝, `setTab()`, `layoutSide()`/`ResizeObserver` 연동 | ✅ | — (기계적) | — | **완료 (2026-08-24, sess-1658).** 수정 3곳 — ① `#panel{position:absolute;left:0;right:0;bottom:180px;z-index:9}` (`flex:none` 제거, 탭바 위에 얹음) ② `#panel:has(#bSk.on)` 의 `position:relative` 제거(베이스 absolute 를 되돌려 버림) ③ `syncPanel()` 끝의 `resizeCanvas()` 호출 제거(= `layoutSide()` 재배치 유발 금지). `#stagearea` 는 이제 «패널 닫힘» 크기로 항상 고정이라 `ResizeObserver` 가 개폐에 발화하지 않는다. 지시서 [3]-(가) 대로 **비평가 없이** `node --check` OK + 헤드리스 **화면비 4종 × 탭 5개 × 영웅 서브탭 3개 = 68 케이스** 실측: 메인 요소 16종 좌표·CSS 변수 7개·캔버스 버퍼 **전부 Δ0.00 (실패 0건)**, 콘솔 에러 0, 프레임 밖 요소 0. 픽셀 대조(패널 위 영역) **253,816px → 518px**, 남은 차이는 살아 움직이는 `#goldN`·`#facTm` 숫자 2개뿐(좌표 Δ0 확인). 수정 전에는 탭 1개 열 때마다 **33건** 이동 — 캔버스 1636→792, `#sideL` 높이 811→542(−33%), **스킬 시트에서는 사이드 아이콘 높이가 0** 이 되어 통째로 사라졌다. 실측표 `docs/review/38-패널오버레이화.md`<br>**주의**: `bottom:180px` 은 탭바 높이 하드코딩 — 24번(A1 개정)이 탭바 높이를 바꾸면 `#panel`·`#dunw`·`#shopw`·`#relicw`·`#eqw` 5곳을 같이 고칠 것. 원 설계 메모: 하단 탭 팝업이 열릴 때마다 전투 캔버스가 줄어 **사이드 아이콘이 위로 밀리고 찌그러지는 현상 금지.** `#panel` 등 모든 탭 콘텐츠를 `position:absolute/fixed` **오버레이**로 — 캔버스 크기는 «패널 닫힘» 기준으로 항상 고정, 패널 개폐가 `ResizeObserver`/`layoutSide()` 재계산을 유발하지 않게. 딤/다이얼로그/전체화면 페이지 껍데기(`#modal`·`#eqw`·`#dunw` 등)는 이미 오버레이라 무관 — flex 흐름에 남은 `#panel` 계열이 대상. [3]-(가) 검증: 패널 열기 전/후 캡처 픽셀 대조로 **메인 화면 요소 이동 0px** 확인. 상세: UI-REFERENCE 0번 ⚠️(작업 38) |
